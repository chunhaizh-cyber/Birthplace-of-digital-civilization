#pragma once

// 文件头部规则注释模块：
// 1. 世界树类负责承接存在、场景、状态、动态、特征等项目结构入口，事实权威仍在项目通用结构中。
// 2. SQL 控制面板同步只作为显示镜像，不参与世界树事实裁决、业务恢复或动作动态来源判断。
// 3. 修改本文件新增入口前，先确认是否已有同功能函数，避免绕开领域类写权边界。

#include <cstdint>
#include <limits>
#include <vector>
#include <memory>
#include <string>

#include "基础信息类.h"
#include "特征值类.h"
#include "特征类.h"
#include "存在类.h"
#include "场景类.h"
#include "二次特征类.h"
#include "状态类.h"
#include "动态类.h"
#include "因果类.h"

class 二次特征生成模块;

class 世界树类 {
public:
    世界树类();
    ~世界树类();

    世界树类(const 世界树类&) = delete;
    世界树类& operator=(const 世界树类&) = delete;
    世界树类(世界树类&&) = delete;
    世界树类& operator=(世界树类&&) = delete;

    void 初始化();
    void 初始化默认世界();

    基础信息类& 基础信息() noexcept;
    const 基础信息类& 基础信息() const noexcept;
    特征值类& 值池() noexcept;
    const 特征值类& 值池() const noexcept;
    const VecIU64* 取VecU只读指针(VecU句柄 句柄) const noexcept;
    特征类& 特征() noexcept;
    const 特征类& 特征() const noexcept;
    存在类& 存在() noexcept;
    const 存在类& 存在() const noexcept;
    场景类& 场景() noexcept;
    const 场景类& 场景() const noexcept;
    二次特征类& 二次特征() noexcept;
    const 二次特征类& 二次特征() const noexcept;
    二次特征生成模块& 二次特征生成() noexcept;
    const 二次特征生成模块& 二次特征生成() const noexcept;
    状态类& 状态() noexcept;
    const 状态类& 状态() const noexcept;
    动态类& 动态() noexcept;
    const 动态类& 动态() const noexcept;
    因果类& 因果() noexcept;
    const 因果类& 因果() const noexcept;

    基础信息节点类* 世界根() const noexcept;
    // 功能：按主键解析基础信息世界树节点，不创建或修改节点。
    基础信息节点类* 按主键解析节点(const std::string& 主键) const noexcept;
    // 功能：按主键解析存在节点，不创建或修改节点。
    存在节点类* 按主键解析存在节点(const std::string& 主键) const noexcept;
    // 功能：按主键解析场景节点，不创建或修改节点。
    场景节点类* 按主键解析场景节点(const std::string& 主键) const noexcept;
    // 功能：按主键解析特征节点，不创建或修改节点。
    特征节点类* 按主键解析特征节点(const std::string& 主键) const noexcept;
    // 功能：按主键解析状态节点，不创建或修改节点。
    状态节点类* 按主键解析状态节点(const std::string& 主键) const noexcept;
    // 功能：按主键解析动态节点，不创建或修改节点。
    动态节点类* 按主键解析动态节点(const std::string& 主键) const noexcept;
    // 功能：按主键解析二次特征节点，不创建或修改节点。
    二次特征节点类* 按主键解析二次特征节点(const std::string& 主键) const noexcept;
    // 功能：读取世界树节点的父节点，不创建或修改节点。
    基础信息节点类* 获取父节点(const 基础信息节点类* 节点) const noexcept;
    std::vector<基础信息节点类*> 获取子节点(const 基础信息节点类* 父节点) const;
    std::size_t 统计全部节点() const;
    bool 基础节点属于当前世界树(const 基础信息节点类* 节点) const;
    // 功能：按主信息类型读取直接子节点，不创建或修改节点。
    template<class T主信息>
    std::vector<基础信息节点类*> 获取子节点_按主信息类型(const 基础信息节点类* 父节点) const
    {
        std::vector<基础信息节点类*> 输出;
        for (auto* 子节点 : 获取子节点(父节点)) {
            if (基础信息链_.取主信息<T主信息>(子节点)) {
                输出.push_back(子节点);
            }
        }
        return 输出;
    }
    // 功能：按主信息类型枚举世界树节点，不创建或修改节点。
    template<class T主信息>
    std::vector<基础信息节点类*> 枚举节点_按主信息类型() const
    {
        return 基础信息链_.枚举节点_按类型<T主信息>();
    }
    // 功能：按主信息类型统计世界树节点数量，不创建或修改节点。
    template<class T主信息>
    std::size_t 统计节点_按主信息类型() const
    {
        return 基础信息链_.枚举节点_按类型<T主信息>().size();
    }
    std::string 获取名称(const 基础信息节点类* 节点) const;
    // 功能：把当前世界树本体重写到 SQL Server 查询投影。
    bool 重写世界树SQL投影(const char* 来源原因, bool 执行字段恢复比对 = false) const noexcept;

    存在节点类* 取或创建存在概念根();
    bool 是存在概念(const 存在节点类* 节点) const noexcept;
    bool 是实例存在(const 存在节点类* 节点) const noexcept;
    存在节点类* 确保根概念_存在();
    存在节点类* 确保根概念_实物存在();
    存在节点类* 确保根概念_虚拟存在();
    存在节点类* 确保根概念_信息存在();
    存在节点类* 确保根概念_数字生命();
    存在节点类* 确保核心概念_自我();
    存在节点类* 确保核心概念_需求();
    存在节点类* 确保核心概念_任务();
    存在节点类* 确保核心概念_方法();
    存在节点类* 确保核心概念_状态();
    存在节点类* 确保核心概念_事件();
    存在节点类* 确保核心概念_规则();
    bool 初始化核心存在概念树();
    bool 初始化核心概念默认特征槽();
    bool 绑定核心概念语素();
    bool 初始化存在概念系统();

    基础信息节点类* 取或创建抽象特征根();
    抽象特征主信息类* 取抽象特征主信息(抽象特征节点类* 节点) const noexcept;
    const 抽象特征主信息类* 取抽象特征主信息(const 抽象特征节点类* 节点) const noexcept;
    抽象特征节点类* 创建抽象特征(基础信息节点类* 父抽象特征, 抽象特征主信息类* 主信息);
    抽象特征节点类* 创建叶子抽象特征(
        基础信息节点类* 父抽象特征,
        const 语素入口节点类* 名称,
        const 语素入口节点类* 类型,
        特征值域 值域);
    抽象特征节点类* 创建复合抽象特征(
        基础信息节点类* 父抽象特征,
        const 语素入口节点类* 名称,
        const 语素入口节点类* 类型);
    抽象特征节点类* 查找子抽象特征_按类型(
        const 基础信息节点类* 父抽象特征,
        const 语素入口节点类* 特征类型) const;
    bool 校验抽象特征子链(const 基础信息节点类* 抽象特征节点) const;
    bool 匹配抽象特征(const 抽象特征节点类* 抽象特征, const 特征节点类* 实例特征) const;
    bool 初始化常用抽象特征(基础信息节点类* 抽象特征根);
    抽象特征节点类* 取或创建抽象特征_按类型(
        基础信息节点类* 父抽象特征,
        const 语素入口节点类* 特征类型);
    特征节点类* 取或创建实例特征_按抽象特征(
        基础信息节点类* 宿主,
        抽象特征节点类* 抽象特征);
    抽象特征节点类* 解析实例特征命中抽象特征(特征节点类* 实例特征);
    抽象特征节点类* 升格实例稳态为抽象区间(特征节点类* 实例特征);
    bool 求值组合抽象特征(
        基础信息节点类* 宿主,
        抽象特征节点类* 组合抽象特征,
        时间戳 now = 结构体_时间戳::当前_微秒());

    特征节点类* 创建特征(基础信息节点类* 宿主, 特征节点主信息类* 主信息);
    特征节点类* 创建特征_按类型(
        基础信息节点类* 宿主,
        const 语素入口节点类* 特征类型,
        const 特征值& 当前值 = {}
    );
    bool 删除特征(特征节点类* 节点);
    特征节点类* 确保特征(基础信息节点类* 宿主, const 语素入口节点类* 特征类型);
    特征节点类* 取或创建子特征_按类型(基础信息节点类* 宿主, const 语素入口节点类* 特征类型);
    std::vector<特征节点类*> 获取子特征(const 基础信息节点类* 宿主) const;
    特征节点类* 查找子特征_按类型(
        const 基础信息节点类* 宿主,
        const 语素入口节点类* 特征类型
    ) const;
    特征节点主信息类* 取特征主信息(特征节点类* 节点) const noexcept;
    const 特征节点主信息类* 取特征主信息(const 特征节点类* 节点) const noexcept;
    结构_特征状态比较结果 比较状态(
        const 状态节点类* 当前状态,
        const 状态节点类* 目标状态) const;
    特征比较方法结果 获取比较方法结果(
        const 语素入口节点类* 特征类型,
        const 特征值& 左值,
        const 特征值& 右值,
        const 特征节点主信息类* 左特征主信息 = nullptr,
        const 特征节点主信息类* 右特征主信息 = nullptr) const;
    存在节点主信息类* 取存在主信息(存在节点类* 节点) const noexcept;
    const 存在节点主信息类* 取存在主信息(const 存在节点类* 节点) const noexcept;
    bool 是存在节点(const 基础信息节点类* 节点) const noexcept;
    bool 是场景节点(const 基础信息节点类* 节点) const noexcept;
    const 语素入口节点类* 读取存在名称(const 存在节点类* 节点) const noexcept;
    const 语素入口节点类* 读取存在类型(const 存在节点类* 节点) const noexcept;
    std::vector<存在节点类*> 获取概念集(const 存在节点类* 节点) const;
    bool 读取最近观测位置缓存(const 存在节点类* 节点, Vector3D& 输出位置_mm) const noexcept;
    bool 确保存在三类根节点(存在节点类* 节点) const noexcept;
    存在节点类* 创建存在(基础信息节点类* 父节点, 存在节点主信息类* 主信息);
    存在节点类* 取或创建子存在_按名称类型(
        基础信息节点类* 父节点,
        const 语素入口节点类* 名称 = nullptr,
        const 语素入口节点类* 类型 = nullptr
    );
    存在节点类* 取或创建子存在_按类型(基础信息节点类* 父节点, const 语素入口节点类* 类型);
    存在节点类* 查找子存在_按名称(
        const 基础信息节点类* 父节点,
        const 语素入口节点类* 名称
    ) const;
    存在节点类* 查找子存在_按类型(
        const 基础信息节点类* 父节点,
        const 语素入口节点类* 类型
    ) const;
    需求类::节点类* 获取需求根节点(存在节点类* 节点) const noexcept;
    const 需求类::节点类* 获取需求根节点(const 存在节点类* 节点) const noexcept;
    任务类::节点类* 获取任务根节点(存在节点类* 节点) const noexcept;
    const 任务类::节点类* 获取任务根节点(const 存在节点类* 节点) const noexcept;
    方法类::节点类* 获取方法根节点(存在节点类* 节点) const noexcept;
    const 方法类::节点类* 获取方法根节点(const 存在节点类* 节点) const noexcept;
    存在节点类* 取或创建子存在_按类型并按特征I64(
        基础信息节点类* 父节点,
        const 语素入口节点类* 类型,
        const 语素入口节点类* 唯一键特征类型,
        I64 唯一键特征值
    );
    存在节点类* 确保存在概念(
        const 语素入口节点类* 名称,
        存在节点类* 父概念 = nullptr
    );
    存在节点类* 创建存在实例(
        const 语素入口节点类* 名称,
        const std::vector<存在节点类*>& 概念集,
        场景节点类* 所属场景 = nullptr
    );
    bool 添加实例概念(
        存在节点类* 实例,
        存在节点类* 概念,
        bool 是否扩充记忆 = true
    );
    void 根据概念扩充实例记忆(
        存在节点类* 实例,
        const std::vector<存在节点类*>& 概念集
    );
    bool 存在包含概念(const 存在节点类* 实例, const 存在节点类* 概念) const noexcept;
    std::vector<存在节点类*> 获取子存在(const 基础信息节点类* 父节点) const;
    std::vector<存在节点类*> 枚举全部存在() const;
    std::vector<存在节点类*> 查找实例_按概念(
        const 存在节点类* 概念,
        const 基础信息节点类* 范围根 = nullptr
    ) const;
    场景节点类* 创建场景(基础信息节点类* 父节点, 场景节点主信息类* 主信息);
    场景节点类* 创建场景_按名称(
        基础信息节点类* 父节点,
        const 语素入口节点类* 名称,
        枚举_世界类型 世界类型 = 枚举_世界类型::其它
    );
    场景节点类* 取或创建子场景_按名称(
        基础信息节点类* 父节点,
        const 语素入口节点类* 名称,
        枚举_世界类型 世界类型 = 枚举_世界类型::其它
    );
    bool 绑定场景宿主(场景节点类* 节点, 存在节点类* 宿主存在);
    存在节点类* 读取场景宿主存在(const 场景节点类* 场景) const noexcept;
    std::vector<可解析引用<状态节点类>> 读取场景状态索引快照(const 场景节点类* 场景) const;
    std::vector<可解析引用<二次特征节点类>> 读取场景二次特征索引快照(const 场景节点类* 场景) const;
    std::vector<结构_场景三维体素轮廓存在比较结果> 查找存在_按深度或彩图轮廓图(
        const 场景节点类* 场景,
        const std::vector<结构_三维体素轮廓图视角>& 局部视角集合,
        const 结构_三维体素轮廓融合参数& 局部融合参数,
        const 结构_场景三维体素轮廓查找参数& 参数,
        const 存在节点类* 存在概念根) const;
    二次特征节点类* 创建二次特征(基础信息节点类* 父节点, 二次特征主信息类* 主信息);
    二次特征节点类* 创建二次特征_按度量(
        基础信息节点类* 父节点,
        枚举_二次特征种类 种类,
        const std::string& 度量签名_链键);
    std::vector<二次特征节点类*> 获取子二次特征(const 基础信息节点类* 父节点) const;
    二次特征节点类* 获取或创建子二次特征_按签名(
        基础信息节点类* 父节点,
        const 二次特征主信息类& 模板
    );
    二次特征主信息类* 取二次特征主信息(二次特征节点类* 节点) const noexcept;
    const 二次特征主信息类* 取二次特征主信息(const 二次特征节点类* 节点) const noexcept;
    bool 绑定二次特征场景(二次特征节点类* 节点, 场景节点类* 所属场景);
    bool 设置二次特征生成语义(
        二次特征节点类* 节点,
        枚举_二次特征域 域,
        枚举_二次特征粒度 粒度,
        枚举_二次特征基准类型 基准类型 = 枚举_二次特征基准类型::未定义,
        枚举_时间归一方式 时间归一方式 = 枚举_时间归一方式::无,
        枚举_二次特征值形态 值形态 = 枚举_二次特征值形态::未定义);
    bool 绑定二次特征来源(
        二次特征节点类* 节点,
        状态节点类* 来源起始状态,
        状态节点类* 来源结果状态,
        动态节点类* 来源动态 = nullptr,
        场景节点类* 来源场景 = nullptr,
        std::uint32_t 来源动态层级 = 0,
        std::uint64_t 来源动态路径签名 = 0,
        枚举_二次特征来源类型 来源类型 = 枚举_二次特征来源类型::未定义,
        结构_时间段 来源时间段 = {},
        时间戳 分步时长 = 0,
        时间戳 总时长 = 0,
        时间戳 窗口时长 = 0);
    bool 设置二次特征主体客体(二次特征节点类* 节点, 基础信息节点类* 主体, 基础信息节点类* 客体);
    bool 设置二次特征左右对象(二次特征节点类* 节点, 基础信息节点类* 左对象, 基础信息节点类* 右对象);
    bool 写入二次特征差值结果(二次特征节点类* 节点, I64 左值, I64 右值);
    bool 写入二次特征标量值(二次特征节点类* 节点, I64 标量值, bool 是否满足 = false, I64 置信度 = 0);
    bool 写入观察关系二次特征(
        场景节点类* 场景,
        基础信息节点类* 主体,
        基础信息节点类* 客体,
        动态节点类* 来源动态,
        const char* 关系名,
        I64 标量值,
        bool 使用标量值);
    bool 写入二次特征标量值_已取主信息(
        二次特征节点类* 节点,
        二次特征主信息类* 主信息,
        I64 标量值,
        bool 是否满足 = false,
        I64 置信度 = 0,
        时间戳 观测时间戳 = 0,
        bool 跳过内部采样 = false);
    bool 写入二次特征离散值_已取主信息(
        二次特征节点类* 节点,
        二次特征主信息类* 主信息,
        std::int32_t 离散编码,
        const std::string& 离散语义键,
        枚举_二次特征值形态 值形态 = 枚举_二次特征值形态::枚举,
        I64 置信度 = 0,
        时间戳 观测时间戳 = 0,
        bool 跳过内部采样 = false);
    bool 刷新二次特征概念模板(二次特征节点类* 节点);
    void 确保二次特征列表具备默认值(
        const std::vector<可解析引用<二次特征节点类>>& 条件索引,
        const char* 默认离散语义键 = "待填充") noexcept;
    const 二次特征主信息类* 查找场景二次特征主信息(
        场景节点类* 场景,
        const 基础信息节点类* 特征节点,
        方向掩码 方向约束 = 0,
        const 基础信息节点类* 主体 = nullptr,
        const std::optional<枚举_二次特征域>& 域 = std::nullopt,
        bool 取最近观测 = false) const noexcept;
    std::optional<bool> 读取场景二次特征布尔(
        场景节点类* 场景,
        const 基础信息节点类* 特征节点,
        方向掩码 方向约束 = 方向_大于0,
        const 基础信息节点类* 主体 = nullptr,
        const std::optional<枚举_二次特征域>& 域 = std::nullopt) const noexcept;
    bool 写入治理二次特征(
        场景节点类* 场景,
        const 语素入口节点类* 概念入口,
        枚举_二次特征种类 种类,
        枚举_二次特征域 域,
        枚举_二次特征粒度 粒度,
        枚举_二次特征基准类型 基准类型,
        枚举_时间归一方式 时间归一方式,
        枚举_二次特征值形态 值形态,
        基础信息节点类* 主体,
        基础信息节点类* 客体,
        基础信息节点类* 左对象,
        基础信息节点类* 右对象,
        I64 标量值,
        bool 是否满足,
        I64 置信度,
        二次特征节点类** 输出节点 = nullptr);
    std::vector<二次特征节点类*> 获取动态二次特征(const 动态节点类& 动态);
    二次特征节点类* 生成相邻动态协同二次特征(
        场景节点类* 场景,
        动态节点类* 动态,
        时间戳 时间窗口 = 1000000);
    动态节点类* 尝试生成连续同向聚合动态(场景节点类* 场景, 动态节点类* 动态);
    动态节点类* 尝试生成同类事件段聚合动态(场景节点类* 场景, 动态节点类* 动态);
    bool 写入轮廓比较二次特征(
        基础信息节点类* 父节点,
        const 语素入口节点类* 特征类型,
        const 结构_轮廓比较结果& 比较结果,
        特征节点类* 左特征,
        特征节点类* 右特征,
        场景节点类* 来源场景,
        动态节点类* 来源动态);
    状态节点类* 创建状态(场景节点类* 场景, 状态节点主信息类* 主信息);
    状态节点类* 创建状态(
        场景节点类* 场景,
        基础信息节点类* 主体,
        特征节点类* 特征,
        const 特征值& 状态值,
        时间戳 发生时间 = 结构体_时间戳::当前_微秒(),
        时间戳 收到时间 = 结构体_时间戳::当前_微秒()
    );
    状态节点类* 创建内部状态(
        场景节点类* 场景,
        基础信息节点类* 主体,
        特征节点类* 特征,
        const 特征值& 值,
        bool 是否变化 = false,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const 状态类::状态后处理回调& 后处理 = {}
    );
    std::vector<状态节点类*> 获取场景状态(const 场景节点类* 场景) const;
    bool 是否当前活动状态(const 场景节点类* 场景, const 状态节点类* 节点) const;
    结构_状态清理问题快照 读取最近清理问题快照() const;
    void 清除最近清理问题快照() noexcept;
    bool 绑定状态主体特征(状态节点类* 节点, 基础信息节点类* 主体, 特征节点类* 特征);
    bool 写入状态值(
        状态节点类* 节点,
        const 特征值& 值,
        时间戳 发生时间 = 0,
        时间戳 收到时间 = 0
    );
    状态节点主信息类* 取状态主信息(状态节点类* 节点) const noexcept;
    const 状态节点主信息类* 取状态主信息(const 状态节点类* 节点) const noexcept;
    bool 标记状态变化(状态节点类* 节点, bool 是否变化);
    动态节点类* 创建动态(场景节点类* 场景, 动态节点主信息类* 主信息);
    动态节点类* 创建动态(
        场景节点类* 场景,
        动态节点主信息类* 主信息,
        const 动态创建后处理选项& 后处理选项
    );
    动态节点类* 创建动态(
        场景节点类* 场景,
        状态节点类* 初始状态,
        状态节点类* 结果状态,
        时间戳 起 = 0,
        时间戳 止 = 0
    );
    动态节点类* 创建方法动作动态(
        场景节点类* 场景,
        基础信息节点类* 动态主体,
        特征节点类* 动态特征,
        方法节点类* 来源动作,
        const 语素入口节点类* 来源动作名 = nullptr,
        时间戳 开始时间 = 0,
        时间戳 结束时间 = 0,
        bool 执行成功 = false,
        I64 错误码 = 0,
        场景节点类* 输入场景 = nullptr,
        场景节点类* 输出场景 = nullptr,
        状态节点类* 初始状态 = nullptr,
        状态节点类* 结果状态 = nullptr
    );
    bool 追加来源低层动态(动态节点类* 节点, 动态节点类* 来源低层动态);
    动态节点主信息类* 取动态主信息(动态节点类* 节点) const noexcept;
    const 动态节点主信息类* 取动态主信息(const 动态节点类* 节点) const noexcept;
    bool 读取动态双状态时间段(const 动态节点类* 动态, 时间戳& 起, 时间戳& 止) const noexcept;
    bool 动态双状态同源可复算(const 动态节点类* 状态迁移动态, const 动态节点类* 动作致变动态) const noexcept;
    bool 读取动态聚合信息(
        const 动态节点类* 动态,
        std::uint32_t& 动态层级,
        枚举_动态聚合方式& 聚合方式,
        std::uint64_t& 动态路径签名) const noexcept;
    bool 是原子相邻动态(const 动态节点类* 动态) const noexcept;
    bool 读取动态来源低层动态(
        const 动态节点类* 动态,
        std::vector<动态节点类*>& 输出来源低层动态) const;
    std::uint64_t 计算动态列表路径签名(const std::vector<动态节点类*>& 动态列表) const noexcept;
    bool 动态聚合来源链相同(
        const 动态节点类* 聚合动态,
        const std::vector<动态节点类*>& 来源动态列表) const noexcept;
    std::vector<动态节点类*> 获取场景动态(const 场景节点类* 场景) const;
    // 功能：从场景动态事实目录筛出位置变化原子动态候选，不创建或修改节点。
    std::vector<动态节点类*> 读取位置变化原子动态候选(const 场景节点类* 场景) const;
    // 功能：按时间连续性把位置变化原子动态候选归为同主体只读片段组，不创建或修改节点。
    std::vector<std::vector<动态节点类*>> 读取位置变化主体动态片段组(const 场景节点类* 场景, 时间戳 最大时间缺口) const;
    动态节点类* 状态记录后刷新动态(
        场景节点类* 场景,
        状态节点类* 新状态,
        时间戳 now = 0
    );
    bool 设置主体特征(动态节点类* 节点, 基础信息节点类* 主体, 特征节点类* 特征);
    bool 绑定来源方法(
        动态节点类* 节点,
        方法节点类* 来源动作,
        const 语素入口节点类* 来源动作名 = nullptr
    );
    bool 绑定来源场景(动态节点类* 节点, 场景节点类* 输入场景, 场景节点类* 输出场景);
    因果模板节点类* 生成因果信息(
        场景节点类* 场景,
        动态节点类* 动态节点,
        基础信息节点类* 动作主体 = nullptr,
        基础信息节点类* 动作语义 = nullptr,
        const std::string& 动作语义键 = {},
        bool 设为主结果 = true);
    因果主信息类* 取因果主信息(因果节点类* 节点) const noexcept;
    const 因果主信息类* 取因果主信息(const 因果节点类* 节点) const noexcept;
    因果模板主信息类* 取因果模板主信息(因果模板节点类* 节点) const noexcept;
    const 因果模板主信息类* 取因果模板主信息(const 因果模板节点类* 节点) const noexcept;
    因果节点类* 解析因果节点(基础信息节点类* 节点) const noexcept;
    const 因果节点类* 解析因果节点(const 基础信息节点类* 节点) const noexcept;
    因果节点类* 按主键解析因果节点(const std::string& 主键) noexcept;
    const 因果节点类* 按主键解析因果节点(const std::string& 主键) const noexcept;
    bool 是因果节点(const 基础信息节点类* 节点) const noexcept;
    std::string 生成抽象因果自然语言(const 因果模板节点类* 模板) const;
    std::vector<因果模板节点类*> 查找匹配因果模板(
        枚举_因果锚点类型 锚点类型,
        std::uint32_t 状态层级,
        const std::vector<二次特征节点类*>& 条件候选,
        const std::vector<二次特征节点类*>& 结果候选) const;
    void 查询因果动作来源(
        const 因果节点类* 因果,
        方法节点类*& 因方法,
        bool& 有方法模板,
        bool& 有动作语义,
        std::string& 动作语义键) const;
    void 读取因果证据状态(
        const 因果节点类* 因果,
        bool& 是初始模板,
        bool& 已验证,
        std::uint64_t& 证据动态样本数,
        I64& 稳定度,
        bool& 证据足够) const noexcept;
    bool 存在因果条件模板(const 因果节点类* 因果) const noexcept;
    void 枚举因果条件目标状态(
        const 因果节点类* 因果,
        std::vector<状态节点类*>& 条件目标状态集,
        std::vector<结构_因果缺失证据项>& 缺失证据) const;
    void 查询主果方向命中因果(
        const 二次特征节点类* 目标比较,
        bool 包含未验证路径,
        std::size_t& 因果节点数量,
        std::size_t& 主果可解析数量,
        std::vector<因果节点类*>& 同向命中因果,
        std::vector<因果节点类*>& 反向命中因果,
        std::vector<结构_因果缺失证据项>& 缺失证据) const;
    因果模板节点类* 确保被动触发初始因果模板(
        基础信息节点类* 父节点,
        场景节点类* 适用场景,
        二次特征节点类* 条件比较,
        二次特征节点类* 主果比较,
        动态节点类* 证据动态,
        std::uint32_t 适用层级,
        const std::string& 动作语义键,
        bool& 已创建);
    void 查询因果链(
        状态节点类* 目标结果状态,
        特征节点类* 目标结果特征,
        std::uint32_t 最大深度,
        bool 包含未验证路径,
        std::size_t& 边数量,
        std::vector<因果模板节点类*>& 来源因果模板,
        std::vector<结构_因果缺失证据项>& 缺失证据) const;
    void 查询叶子任务目标投影层级(
        任务节点类* 叶子任务,
        需求节点类* 来源需求,
        基础信息节点类* 目标宿主,
        特征节点类* 目标特征类型,
        bool 允许未验证路径,
        std::size_t& 候选投影数量,
        std::vector<std::uint32_t>& 结算贡献层级集,
        std::vector<std::uint32_t>& 因果距离层级集,
        std::vector<结构_因果缺失证据项>& 缺失证据,
        bool& 是否允许形成D0) const;
    void 查询自检原子目标投影(
        任务节点类* 固定来源任务,
        需求节点类* 固定来源需求,
        基础信息节点类* 目标宿主,
        特征节点类* 目标特征类型,
        const 语素入口节点类* 目标特征类型词,
        bool 允许未验证路径,
        std::size_t& 候选投影数量,
        std::vector<std::string>& 来源因果主键集,
        std::vector<结构_因果缺失证据项>& 缺失证据) const;

    bool 写入特征_I64(特征节点类* 节点, I64 值, 时间戳 now = 结构体_时间戳::当前_微秒());
    bool 写入特征值(特征节点类* 节点, const 特征值& 值, 时间戳 now = 结构体_时间戳::当前_微秒());
    bool 写入特征_I64(
        基础信息节点类* 宿主,
        const 语素入口节点类* 特征类型,
        I64 值,
        时间戳 now = 结构体_时间戳::当前_微秒()
    );
    bool 写入特征_指针(特征节点类* 节点, const void* 指针, 时间戳 now = 结构体_时间戳::当前_微秒());
    bool 写入特征_指针(
        基础信息节点类* 宿主,
        const 语素入口节点类* 特征类型,
        const void* 指针,
        时间戳 now = 结构体_时间戳::当前_微秒()
    );
    bool 写入特征_VecU(
        特征节点类* 节点,
        VecIU64 值,
        枚举_轮廓维度 维度 = 枚举_轮廓维度::未定义,
        时间戳 now = 结构体_时间戳::当前_微秒()
    );
    bool 写入特征_VecU(
        基础信息节点类* 宿主,
        const 语素入口节点类* 特征类型,
        VecIU64 值,
        枚举_轮廓维度 维度 = 枚举_轮廓维度::未定义,
        时间戳 now = 结构体_时间戳::当前_微秒()
    );
    bool 写入特征_VecI64(
        特征节点类* 节点,
        const VecI64& 值,
        枚举_轮廓维度 维度 = 枚举_轮廓维度::未定义,
        时间戳 now = 结构体_时间戳::当前_微秒()
    );
    bool 写入特征_VecI64(
        基础信息节点类* 宿主,
        const 语素入口节点类* 特征类型,
        const VecI64& 值,
        枚举_轮廓维度 维度 = 枚举_轮廓维度::未定义,
        时间戳 now = 结构体_时间戳::当前_微秒()
    );
    特征值 读取特征快照(const 特征节点类* 节点) const;
    特征值 读取特征快照(const 基础信息节点类* 宿主, const 语素入口节点类* 特征类型) const;
    bool 读取特征_I64(const 特征节点类* 节点, I64& 输出值) const;
    bool 读取特征_I64(const 基础信息节点类* 宿主, const 语素入口节点类* 特征类型, I64& 输出值) const;
    bool 读取特征_指针(const 特征节点类* 节点, void*& 输出指针) const;
    bool 读取特征_指针(const 基础信息节点类* 宿主, const 语素入口节点类* 特征类型, void*& 输出指针) const;
    const VecIU64* 读取特征VecU(const 特征节点类* 节点) const;
    bool 读取特征VecI64(const 特征节点类* 节点, VecI64& 输出值) const;
    bool 读取特征VecI64(const 基础信息节点类* 宿主, const 语素入口节点类* 特征类型, VecI64& 输出值) const;
    // 从现有 VecIU64 值池只读解码后比较轮廓，不写二次特征。
    结构_轮廓比较结果 比较轮廓特征_按特征类型(
        const 语素入口节点类* 特征类型,
        const 特征节点类* 左特征,
        const 特征节点类* 右特征) const;
    存在比较结果 比较存在(
        const 存在节点类* 左存在,
        const 存在节点类* 右存在,
        const 集合比较参数& 参数 = {}
    ) const;
    std::vector<二次特征节点类*> 刷新存在比较二次特征(
        存在节点类* 左存在,
        存在节点类* 右存在,
        const 集合比较参数& 参数 = {});

    场景节点类* 取或创建默认世界(const std::string& 名称, 枚举_世界类型 世界类型);
    场景节点类* 取或创建自我现实场景();
    存在节点类* 取或创建自我存在();
    场景节点类* 取存在内部世界(const 存在节点类* 宿主存在) const noexcept;
    场景节点类* 取或创建存在内部世界(存在节点类* 宿主存在, const 语素入口节点类* 场景名称 = nullptr);
    bool 读取存在场景绝对坐标(const 存在节点类* 节点, Vector3D& 输出坐标_mm) const;
    bool 写入存在场景绝对坐标(
        存在节点类* 节点,
        const Vector3D& 坐标_mm,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        I64 置信度 = 10000);
    bool 计算场景绝对坐标_由参考存在相对坐标(
        const 存在节点类* 参考存在,
        const Vector3D& 相对坐标_mm,
        Vector3D& 输出绝对坐标_mm) const;
    bool 写入存在场景绝对坐标_由参考存在相对坐标(
        存在节点类* 目标存在,
        const 存在节点类* 参考存在,
        const Vector3D& 相对坐标_mm,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        I64 置信度 = 10000);
    结构_存在空间绝对坐标换算结果 计算场景绝对坐标_由相对坐标表(
        const std::vector<结构_存在空间相对坐标项>& 相对坐标表) const;
    bool 写入存在场景绝对坐标_由相对坐标表(
        存在节点类* 目标存在,
        const std::vector<结构_存在空间相对坐标项>& 相对坐标表,
        时间戳 now = 结构体_时间戳::当前_微秒());
    bool 写入存在观测位置(存在节点类* 节点, const Vector3D& 位置_mm);

public:
    场景节点类* 现实世界 = nullptr;
    场景节点类* 文本世界 = nullptr;
    场景节点类* 想象世界 = nullptr;
    场景节点类* 记忆世界 = nullptr;
    场景节点类* 推理世界 = nullptr;
    场景节点类* 虚拟世界 = nullptr;
    存在节点类* 存在概念根 = nullptr;
    基础信息节点类* 抽象特征根 = nullptr;
    存在节点类* 自我指针 = nullptr;
    场景节点类* 自我所在场景 = nullptr;

private:
    // 功能：把世界树对象节点当前态同步到 SQL 控制面板运行态显示镜像。
    void 同步SQL控制面板世界显示项(const 基础信息节点类* 节点, const char* 阶段) const noexcept;

    基础信息类 基础信息链_{};
    特征值类 值池_{};
    特征类 特征服务_;
    存在类 存在服务_;
    场景类 场景服务_;
    二次特征类 二次特征服务_;
    std::unique_ptr<二次特征生成模块> 二次特征生成服务_;
    状态类 状态服务_;
    动态类 动态服务_;
    因果类 因果服务_;

    I64 默认特征差异度(const 特征值& 左值, const 特征值& 右值) const;
};

extern 世界树类& 世界树;
