#pragma once

// 文件头部规则注释模块：
// 1. 动态类负责动态节点创建、读取和动态来源关系维护，不改变任务 / 需求 / 方法写权边界。
// 2. 方法动作动态必须绑定明确方法；线程、显示层和 SQL 同步不得成为动作来源。
// 3. SQL 控制面板同步只作为显示镜像，不参与动态生成、因果判断或业务恢复。

#include "基础信息类.h"

struct 动态创建后处理选项 {
    bool 生成动态二次特征 = true;
    bool 生成相邻动态协同 = true;
    bool 尝试连续同向聚合 = true;
    bool 尝试同类事件段聚合 = true;
};

class 动态类 {
public:
    explicit 动态类(基础信息类* 基础信息 = nullptr) noexcept;

    void 绑定基础信息(基础信息类* 基础信息) noexcept;

    动态节点主信息类* 取动态主信息(动态节点类* 节点) const noexcept;
    const 动态节点主信息类* 取动态主信息(const 动态节点类* 节点) const noexcept;
    // 功能：按动态结构字段推导名称语义入口。
    static const 语素入口节点类* 推导动态名称(const 动态节点主信息类& 主信息) noexcept;
    // 功能：按动态结构字段推导动态类别入口。
    static const 语素入口节点类* 推导动态类型(const 动态节点主信息类& 主信息) noexcept;

    // 功能：按主键查找并解析动态节点，不创建或修改节点。
    动态节点类* 按主键解析动态节点(const std::string& 主键) const noexcept;

    // 功能：读取动态生成因果所需输入，不创建或修改节点。
    bool 读取动态因果生成输入(
        const 动态节点类* 动态,
        状态节点类*& 初始状态,
        状态节点类*& 结果状态,
        方法节点类*& 来源动作,
        const 语素入口节点类*& 来源动作名,
        const 语素入口节点类*& 动态类型,
        std::uint32_t& 动态层级) const noexcept;

    // 功能：读取动态来源输入场景和输出场景，不创建或修改节点。
    bool 读取动态来源场景(
        const 动态节点类* 动态,
        场景节点类*& 输入场景,
        场景节点类*& 输出场景) const noexcept;
    // 功能：读取动态排序时间，不创建或修改节点。
    时间戳 读取动态排序时间(const 动态节点类* 动态) const noexcept;
    // 功能：读取动态两个状态构成的时间段，不创建或修改节点。
    bool 读取动态双状态时间段(const 动态节点类* 动态, 时间戳& 起, 时间戳& 止) const noexcept;
    // 功能：按主体、特征和双状态时间段包含判断两个动态是否可复算同源。
    bool 动态双状态同源可复算(const 动态节点类* 状态迁移动态, const 动态节点类* 动作致变动态) const noexcept;
    // 功能：读取动态聚合层级、聚合方式和路径签名，不创建或修改节点。
    bool 读取动态聚合信息(
        const 动态节点类* 动态,
        std::uint32_t& 动态层级,
        枚举_动态聚合方式& 聚合方式,
        std::uint64_t& 动态路径签名) const noexcept;
    // 功能：判断动态是否为原子相邻动态，不创建或修改节点。
    bool 是原子相邻动态(const 动态节点类* 动态) const noexcept;
    // 功能：读取动态来源低层动态引用链，不创建或修改节点。
    bool 读取动态来源低层动态(
        const 动态节点类* 动态,
        std::vector<动态节点类*>& 输出来源低层动态) const;
    // 功能：按动态节点列表计算聚合路径签名，用于来源链快速过滤。
    std::uint64_t 计算动态列表路径签名(const std::vector<动态节点类*>& 动态列表) const noexcept;
    // 功能：比较聚合动态的来源低层动态链是否与输入动态列表一致。
    bool 动态聚合来源链相同(
        const 动态节点类* 聚合动态,
        const std::vector<动态节点类*>& 来源动态列表) const noexcept;

    动态节点类* 创建动态(
        场景节点类* 场景,
        动态节点主信息类* 主信息,
        const 动态创建后处理选项& 后处理选项 = {});
    动态节点类* 创建动态(
        场景节点类* 场景,
        状态节点类* 初始状态,
        状态节点类* 结果状态,
        时间戳 起 = 0,
        时间戳 止 = 0
    );
    动态节点类* 创建叶子动态(
        场景节点类* 场景,
        状态节点类* 初始状态,
        状态节点类* 结果状态,
        时间戳 now = 0
    );
    动态节点类* 创建动作动态(
        场景节点类* 场景,
        基础信息节点类* 动态主体,
        特征节点类* 动态特征,
        时间戳 开始时间 = 0,
        时间戳 结束时间 = 0
    );
    动态节点类* 创建方法动作动态(
        场景节点类* 场景,
        基础信息节点类* 动态主体,
        特征节点类* 动态特征,
        方法节点类* 来源动作,
        const 语素入口节点类* 来源动作名 = nullptr,
        时间戳 开始时间 = 0,
        时间戳 结束时间 = 0,
        bool 执行成功 = false,
        I64 错误码 = 0,
        场景节点类* 输入场景 = nullptr,
        场景节点类* 输出场景 = nullptr,
        状态节点类* 初始状态 = nullptr,
        状态节点类* 结果状态 = nullptr
    );
    动态节点类* 创建存在动态(
        场景节点类* 观察场景,
        存在节点类* 被观察存在,
        状态节点类* 初始状态,
        状态节点类* 结果状态,
        动态节点类* 来源低层动态 = nullptr,
        时间戳 now = 0
    );
    动态节点类* 创建场景动态(
        场景节点类* 父场景,
        场景节点类* 子场景,
        状态节点类* 初始状态,
        状态节点类* 结果状态,
        动态节点类* 来源低层动态 = nullptr,
        时间戳 now = 0
    );
    bool 允许生成状态动态(场景节点类* 场景, 状态节点类* 初始状态, 状态节点类* 结果状态) const noexcept;
    bool 允许生成存在动态(场景节点类* 观察场景, 存在节点类* 被观察存在, 状态节点类* 初始状态, 状态节点类* 结果状态) const noexcept;
    bool 允许生成场景动态(场景节点类* 父场景, 场景节点类* 子场景, 状态节点类* 初始状态, 状态节点类* 结果状态) const noexcept;
    bool 允许生成方法动作动态(const 方法节点类* 来源动作) const noexcept;
    bool 删除动态(动态节点类* 节点);

    std::vector<动态节点类*> 枚举全部动态() const;
    std::vector<动态节点类*> 获取场景动态(const 场景节点类* 场景) const;
    状态节点类* 查找相邻前状态(场景节点类* 场景, 状态节点类* 新状态) const;
    动态节点类* 状态记录后刷新动态(
        场景节点类* 场景,
        状态节点类* 新状态,
        时间戳 now = 0
    );

    bool 设置主体特征(动态节点类* 节点, 基础信息节点类* 主体, 特征节点类* 特征);
    bool 设置聚合信息(动态节点类* 节点, std::uint32_t 动态层级, 枚举_动态聚合方式 聚合方式);
    bool 追加来源低层动态(动态节点类* 节点, 动态节点类* 来源低层动态);
    bool 绑定来源方法(
        动态节点类* 节点,
        方法节点类* 来源动作,
        const 语素入口节点类* 来源动作名 = nullptr
    );
    bool 绑定来源场景(动态节点类* 节点, 场景节点类* 输入场景, 场景节点类* 输出场景);

private:
    // 功能：把动态节点当前态同步到 SQL 控制面板运行态显示镜像。
    void 同步SQL控制面板动态显示项(const 动态节点类* 动态, const char* 阶段) noexcept;

    基础信息类* 基础信息_ = nullptr;
};
