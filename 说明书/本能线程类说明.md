# 本能线程类说明

本文说明 `D:\鱼巢\本能线程类.ixx`。

`本能线程类` 当前是接口声明模块，用于表达一些底层本能计算工作流，例如绝对坐标计算和子特征提取。当前仓库中只找到该模块的接口声明，没有找到对应实现体，因此本文按“接口说明和待实现状态”描述。

## 1. 文件定位

模块名：

```cpp
export module 本能线程模块;
```

公开类：

```cpp
class 本能线程类
```

当前导出内容：

- 绝对坐标计算所需快照和结果结构。
- 子特征提取所需快照和结果结构。
- 本能线程类接口。

## 2. 数据结构

### 2.1 `绝对坐标快照`

用途：

- 表示计算绝对坐标所需的输入快照。

字段：

| 字段 | 用途 |
| --- | --- |
| `相对位置_mm` | 三维相对位置，单位毫米。 |
| `世界变换4x4` | 4x4 世界变换矩阵，默认单位矩阵。 |

### 2.2 `绝对坐标结果`

用途：

- 表示某个存在节点计算出的绝对坐标。

字段：

| 字段 | 用途 |
| --- | --- |
| `e` | 目标存在节点。 |
| `绝对位置_mm` | 三维绝对位置，单位毫米。 |

### 2.3 `子特征快照`

用途：

- 表示从父存在提取子特征所需的输入数据。

字段：

| 字段 | 用途 |
| --- | --- |
| `父` | 父存在节点。 |
| `版本签名` | 输入快照版本标识。 |
| `轮廓blocks` | 轮廓块数据。 |
| `裁剪宽` / `裁剪高` | 裁剪图像尺寸。 |
| `裁剪BGR` | 裁剪图像 BGR 数据。 |
| `裁剪mask` | 裁剪图像 mask。 |

### 2.4 `子部件结果`

用途：

- 表示一个被提取出的子部件。

字段：

| 字段 | 用途 |
| --- | --- |
| `部件键` | 子部件标识。 |
| `相对位置_mm` | 子部件相对位置。 |
| `尺寸_mm` | 子部件尺寸。 |
| `轮廓blocks` | 子部件轮廓块。 |
| `颜色_BGR` | 子部件颜色。 |

### 2.5 `子特征结果`

用途：

- 汇总父存在的子特征提取结果。

字段：

| 字段 | 用途 |
| --- | --- |
| `父` | 父存在节点。 |
| `版本签名` | 结果对应的快照版本。 |
| `部件` | 子部件结果列表。 |

## 3. 函数类型

### 3.1 绝对坐标流程函数

| 类型 | 用途 |
| --- | --- |
| `Fn取绝对快照` | 根据存在节点取得绝对坐标输入快照。 |
| `Fn算绝对坐标` | 根据快照计算绝对坐标结果。 |
| `Fn写入绝对坐标` | 将绝对坐标结果写回系统。 |

### 3.2 子特征流程函数

| 类型 | 用途 |
| --- | --- |
| `Fn取子特征快照` | 根据父存在和版本签名取得子特征输入快照。 |
| `Fn算子特征` | 根据快照计算子特征结果。 |
| `Fn写入子特征` | 将子特征结果写回系统。 |

## 4. `本能线程类` 公开方法

### 4.1 构造与析构

```cpp
本能线程类();
~本能线程类();
```

用途：

- 初始化和释放本能线程对象。

当前状态：

- 当前仓库未找到实现体。

### 4.2 拷贝禁用

```cpp
本能线程类(const 本能线程类&) = delete;
本能线程类& operator=(const 本能线程类&) = delete;
```

用途：

- 禁止复制线程对象，避免线程资源和回调状态被隐式复制。

### 4.3 `设置_绝对坐标流程(...)`

用途：

- 设置绝对坐标计算流程的三个回调：
  - 取快照。
  - 计算。
  - 写回。

预期流程：

1. 外部调用设置回调。
2. 请求刷新绝对坐标时，线程读取快照。
3. 计算绝对坐标。
4. 写回结果。

当前状态：

- 当前仓库未找到实现体。

### 4.4 `设置_子特征流程(...)`

用途：

- 设置子特征提取流程的三个回调：
  - 取快照。
  - 计算。
  - 写回。

当前状态：

- 当前仓库未找到实现体。

### 4.5 `启动()`

用途：

- 启动本能线程工作循环。

当前状态：

- 当前仓库未找到实现体。

### 4.6 `停止()`

用途：

- 请求停止本能线程，并释放或等待线程资源。

当前状态：

- 当前仓库未找到实现体。

### 4.7 `是否正在运行()`

用途：

- 查询本能线程是否处于运行状态。

返回：

- `true` 表示线程正在运行。
- `false` 表示未运行。

当前状态：

- 当前仓库未找到实现体。

### 4.8 `请求刷新绝对坐标(...)`

用途：

- 提交一个存在节点的绝对坐标刷新请求。

输入：

- `e`：目标存在节点。

当前状态：

- 当前仓库未找到实现体。

### 4.9 `请求提取子特征(...)`

用途：

- 提交一个父存在的子特征提取请求。

输入：

- `父`：父存在节点。
- `sig`：版本签名。

当前状态：

- 当前仓库未找到实现体。

### 4.10 `通知存在已移除(...)`

用途：

- 通知线程某个存在已被移除，预期用于清理队列或缓存中相关请求。

当前状态：

- 当前仓库未找到实现体。

## 5. 当前实现风险

- 当前文件只有接口声明，没有实现体。
- 若有代码真正构造或调用 `本能线程类` 非内联方法，链接阶段会需要对应实现。
- 目前项目能编译，说明这些接口当前可能没有被链接使用，或实现来自未纳入源码检索的构建产物。

## 6. 后续实现建议

后续实现本能线程时，应明确：

- 线程启动和停止的状态机。
- 请求队列结构。
- 绝对坐标请求和子特征请求是否共用队列。
- 去重策略，例如按存在指针和版本签名合并请求。
- `通知存在已移除` 对队列和正在执行任务的处理方式。
- 回调异常保护。
- 写回必须由线程内执行，还是交给上层消息线程执行。

