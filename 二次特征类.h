#pragma once

#include <cstdint>
#include <optional>
#include <string>
#include <unordered_map>
#include <unordered_set>

#include "基础信息类.h"

struct 结构_二次特征方向签名材料 {
    可解析引用<基础信息节点类> 主体{};
    可解析引用<状态节点类> 来源起始状态{};
    可解析引用<状态节点类> 来源结果状态{};
    三向关系掩码 允许方向 = 0;
    二次特征主信息类::枚举_方向区间 方向区间 =
        二次特征主信息类::枚举_方向区间::未定义;
    std::optional<I64> 差值{};
    bool 有标量值 = false;
    I64 标量值 = 0;
};

class 二次特征类 {
public:
    explicit 二次特征类(基础信息类* 基础信息 = nullptr) noexcept;

    void 绑定基础信息(基础信息类* 基础信息) noexcept;

    二次特征主信息类* 取二次特征主信息(二次特征节点类* 节点) const noexcept;
    const 二次特征主信息类* 取二次特征主信息(const 二次特征节点类* 节点) const noexcept;

    // 功能：按主键查找并解析二次特征节点，不创建或修改节点。
    二次特征节点类* 按主键解析二次特征节点(const std::string& 主键) const noexcept;

    // 功能：读取状态迁移二次特征的来源状态和动态，不创建或修改节点。
    bool 读取状态迁移来源(
        const 二次特征节点类* 二次特征,
        状态节点类*& 来源起始状态,
        状态节点类*& 来源结果状态,
        动态节点类*& 来源动态) const noexcept;

    // 功能：判断节点是否为状态迁移二次特征。
    bool 是状态迁移二次特征(const 二次特征节点类* 二次特征) const noexcept;

    // 功能：读取可用于因果融合的标量区间，不创建或修改节点。
    bool 读取可融合标量区间(const 二次特征节点类* 二次特征, I64区间& 输出区间) const noexcept;

    // 功能：把同向状态迁移候选的可融合标量区间合并到目标二次特征。
    bool 融合同向状态迁移可融合标量区间(
        二次特征节点类* 目标二次特征,
        const std::vector<二次特征节点类*>& 候选集合) const noexcept;

    // 功能：读取方向签名材料，不创建或修改节点。
    static bool 读取方向签名材料_直接(
        const 二次特征主信息类* 主信息,
        结构_二次特征方向签名材料& 输出) noexcept;

    // 功能：判断二次特征是否命中二次特征模板。
    bool 二次特征模板匹配(const 二次特征节点类* 二次特征, const 二次特征节点类* 模板) const noexcept;

    二次特征节点类* 创建二次特征(基础信息节点类* 父节点, 二次特征主信息类* 主信息);
    二次特征节点类* 创建二次特征_按度量(
        基础信息节点类* 父节点,
        枚举_二次特征种类 种类,
        const std::string& 度量签名_链键
    );
    二次特征节点类* 获取或创建子二次特征_按签名(
        基础信息节点类* 父节点,
        const 二次特征主信息类& 模板);
    二次特征节点类* 获取或创建概念树根();
    二次特征节点类* 获取或创建度量链根(
        const std::string& 度量签名_链键,
        枚举_二次特征种类 种类 = 枚举_二次特征种类::未定义
    );
    二次特征节点类* 注册概念小区间(
        二次特征节点类* 任意域节点或链根,
        const I64区间& 区间,
        const 语素入口节点类* 概念名称
    );
    二次特征节点类* 按差值命中概念节点(二次特征节点类* 链根, I64 差值);
    二次特征节点类* 用概念名称反查节点(二次特征节点类* 链根, const 语素入口节点类* 概念名称);
    bool 删除二次特征(二次特征节点类* 节点);

    std::vector<二次特征节点类*> 枚举全部二次特征() const;
    std::vector<二次特征节点类*> 获取子二次特征(const 基础信息节点类* 父节点) const;

    bool 绑定场景(二次特征节点类* 节点, 场景节点类* 所属场景);
    bool 设置生成语义(
        二次特征节点类* 节点,
        枚举_二次特征域 域,
        枚举_二次特征粒度 粒度,
        枚举_二次特征基准类型 基准类型 = 枚举_二次特征基准类型::未定义,
        枚举_时间归一方式 时间归一方式 = 枚举_时间归一方式::无,
        枚举_二次特征值形态 值形态 = 枚举_二次特征值形态::未定义
    );
    bool 绑定来源(
        二次特征节点类* 节点,
        状态节点类* 来源起始状态,
        状态节点类* 来源结果状态,
        动态节点类* 来源动态 = nullptr,
        场景节点类* 来源场景 = nullptr,
        std::uint32_t 来源动态层级 = 0,
        std::uint64_t 来源动态路径签名 = 0,
        枚举_二次特征来源类型 来源类型 = 枚举_二次特征来源类型::未定义,
        结构_时间段 来源时间段 = {},
        时间戳 分步时长 = 0,
        时间戳 总时长 = 0,
        时间戳 窗口时长 = 0
    );
    bool 设置主体客体(二次特征节点类* 节点, 基础信息节点类* 主体, 基础信息节点类* 客体);
    bool 设置左右对象(二次特征节点类* 节点, 基础信息节点类* 左对象, 基础信息节点类* 右对象);
    bool 写入差值结果(二次特征节点类* 节点, I64 左值, I64 右值);
    bool 写入标量值(二次特征节点类* 节点, I64 标量值, bool 是否满足 = false, I64 置信度 = 0);
    bool 写入标量值_已取主信息(
        二次特征节点类* 节点,
        二次特征主信息类* 主信息,
        I64 标量值,
        bool 是否满足 = false,
        I64 置信度 = 0,
        时间戳 观测时间戳 = 0,
        bool 跳过内部采样 = false);
    bool 写入离散值(
        二次特征节点类* 节点,
        std::int32_t 离散编码,
        const std::string& 离散语义键,
        枚举_二次特征值形态 值形态 = 枚举_二次特征值形态::枚举,
        I64 置信度 = 0
    );
    bool 写入离散值_已取主信息(
        二次特征节点类* 节点,
        二次特征主信息类* 主信息,
        std::int32_t 离散编码,
        const std::string& 离散语义键,
        枚举_二次特征值形态 值形态 = 枚举_二次特征值形态::枚举,
        I64 置信度 = 0,
        时间戳 观测时间戳 = 0,
        bool 跳过内部采样 = false);
    bool 刷新度量签名_链键(二次特征节点类* 节点);
    bool 刷新概念模板(二次特征节点类* 节点);

private:
    bool 私有_写入标量值_已取主信息(
        二次特征节点类* 节点,
        二次特征主信息类* 主信息,
        I64 标量值,
        bool 是否满足,
        I64 置信度,
        std::uint64_t 主信息读取耗时微秒,
        时间戳 观测时间戳,
        bool 跳过内部采样);
    bool 私有_写入离散值_已取主信息(
        二次特征节点类* 节点,
        二次特征主信息类* 主信息,
        std::int32_t 离散编码,
        const std::string& 离散语义键,
        枚举_二次特征值形态 值形态,
        I64 置信度,
        std::uint64_t 主信息读取耗时微秒,
        时间戳 观测时间戳,
        bool 跳过内部采样);
    void 私有_确保默认概念刻度(二次特征节点类* 链根);
    std::unordered_map<std::string, 二次特征节点类*> 签名父节点索引_{};
    std::unordered_set<std::string> 签名父节点索引已建立_{};
    基础信息类* 基础信息_ = nullptr;
};
