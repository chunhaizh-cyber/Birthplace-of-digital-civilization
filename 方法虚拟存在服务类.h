#pragma once

#include "方法类.h"

class 方法虚拟存在服务类 {
public:
    static 存在节点类* 确保方法虚拟存在(
        方法类::节点类* 方法首节点,
        存在节点类* 宿主存在 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒());

    static 存在节点类* 取或创建方法虚拟存在(
        方法类::节点类* 方法首节点,
        存在节点类* 宿主存在 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒());

    static bool 补齐方法虚拟存在概念集(
        方法类::节点类* 方法首节点,
        存在节点类* 宿主存在 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒());

    static bool 初始化方法虚拟存在基础信息(
        方法类::节点类* 方法首节点,
        存在节点类* 宿主存在 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒());

    static bool 同步方法首节点结构特征(
        方法类::节点类* 方法首节点,
        存在节点类* 宿主存在 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒());

    static bool 初始化方法统计特征(
        方法类::节点类* 方法首节点,
        存在节点类* 宿主存在 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒());

    static bool 同步方法节点到虚拟存在(
        方法类::节点类* 方法首节点,
        存在节点类* 宿主存在 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒());

    static bool 记录方法虚拟存在特征(
        方法类::节点类* 方法首节点,
        const 语素入口节点类* 特征类型,
        const 特征值& 值,
        存在节点类* 宿主存在 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒());

    static bool 写入方法I64特征(
        方法类::节点类* 方法首节点,
        const 语素入口节点类* 特征类型,
        I64 值,
        存在节点类* 宿主存在 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒());

    static bool 写入方法指针特征(
        方法类::节点类* 方法首节点,
        const 语素入口节点类* 特征类型,
        const void* 指针,
        存在节点类* 宿主存在 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒());

    static bool 记录方法运行统计(
        方法类::节点类* 方法首节点,
        const char* 统计名,
        I64 增量 = 1,
        存在节点类* 宿主存在 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒());

    static bool 初始化方法虚拟存在信息(
        方法类::节点类* 方法首节点,
        存在节点类* 宿主存在 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒());
};
