# 6370 子规范 识别本能方法唯一性收敛

编号：6370
日期：20260701
状态：正式子规范
权威等级：正式规范

## 1. 定位

本规范是 `观察外设与视觉` 主题下由旧规范修订迁回的正式子规范。它只承接当前仍有效的规则，不恢复旧规范中的历史口径、详细设计措辞或已经被根规范吸收的重复条款。

历史来源：
- `规范/旧规范/识别本能方法唯一性收敛规范20260607.md`

上级规范：
- `规范/3300_根规范_方法主信息类_20260701.md`
- `规范/1140_根规范_存在节点主信息类_20260701.md`
- `规范/0050_项目通用结构与禁止性规则总纲_20260701.md`
- `规范/0300_规范冲突与前后矛盾清单_20260701.md`

## 2. 上级约束

1. 本规范服从 `规范/规范目录.md`、0050 总纲、0300 冲突清单和对应根规范。
2. 历史来源中与当前根规范冲突的表述不进入当前权威口径。
3. 旧名称、旧结构和旧阶段口径必须按 `规范/0300_规范冲突与前后矛盾清单_20260701.md` 的 C001-C008 收束，不得在本规范中恢复历史并列结构、旧字段身份或旧执行来源。
4. 任务筹办 / 任务执行只作为任务管理生命周期普通函数；动作来源、方法注册和方法选择必须服从 0050 总纲、3200 任务根规范、3300 方法根规范和 1170 动态根规范。
5. 文本、日志、注释、临时 JSON、自由字段和显示摘要不得承载正式机器逻辑或业务状态。

## 3. 当前有效规则

以下条款由历史来源按当前根规范修订抽取；若条款与第 2 章或上级规范冲突，以上级规范和第 2 章为准。

1. 同一性评分、候选数量、硬冲突数量、证据不足原因和补观察建议，只能作为本轮运行存在或输出结果场景中的过程证据；它们不是识别的语义输出。
2. 识别不得直接写：
3. 如果识别需要这些新特征，必须通过需求生成观察任务，由合法观察 / 扫描 / 跟踪 / 提交入口生成一阶特征值、事实或动态。识别本身只能读取已有特征并返回已有存在指针或空指针。
4. 唯一性必须限定范围：
5. 不得写成：
6. 识别前必须满足：
7. 识别范围必须携带可回查范围句柄或等价引用：
8. 没有明确范围时，不得写：
9. 识别输入只能是已有特征和已确定范围：
10. 识别不得为了完成识别而即时伪造或补写新的一阶特征。缺特征时，语义输出仍为空指针，缺口只能写入本轮运行存在或输出结果场景：
11. `补观察原因` 若进入机器决策，应使用枚举、掩码或二次特征值，不得只靠说明性文字。
12. 识别必须写成集合过滤，不得写成：
13. 只有满足上述条件后，才允许写：
14. 如果平面轮廓已经能唯一，不需要继续比较颜色、空间极值轮廓或局部三维模型。空间位置不能替代轮廓、空间尺寸、子轮廓等细致特征作为识别唯一依据；它只作为范围过滤、反证检查或后续跟踪的辅助信息。
15. 当前识别范围内只有一个自我存在拥有该可观测物理标记，也可唯一。这里的标记必须是外界可观测特征，不得是外设内部候选编号、帧内编号、任务编号或自我世界树内部 ID。
16. 识别使用的比较值域不得凭空产生。来源可以是：
17. 特征比较最大允许误差
18. 识别方法只能使用这些已经合法产生并可追溯的阈值。
19. 候选召回应优先使用低成本、强区分的已有特征。推荐顺序：
20. 该顺序不是固定硬编码，可以由历史区分度、计算成本、外设质量和当前任务严格度调整。但调整结果必须留下可解释的选择依据，不能让识别退化为无来源的黑箱匹配。外设内部候选编号、帧内编号、任务编号和自我世界树内部 ID 只能作为回查引用，不得作为识别是否见过的语义依据。
21. 唯一后不需要把所有特征都比完，但必须做最低限度反证检查。验算不是重新观察，不生成新特征值；验算只使用已有特征判断是否有硬冲突。
22. 空间中心误差 > 最大允许空间误差
23. 平面轮廓点平均误差 > 最大允许平均误差
24. 或 平面轮廓点最大误差 > 最大允许最大误差
25. 这些状态值必须由量化特征、二次特征或冲突掩码推出，不能只靠文字说明。
26. 若某个特征本身强唯一，例如标记ID或空间范围强绑定，可以不要求所有特征多帧齐全，但仍必须满足范围明确、可复验、唯一候选和无硬冲突。
27. 扫描前置必须是：
28. 扫描不得直接生成状态变化。未归属部分必须通过需求任务链进入识别或补观察；已归属部分可以按范围继续扫描，但结果不得扩大为全范围闭合。
29. 跟踪目标必须是：
30. 然后进入识别或补观察。跟踪不能用目标局部稳定替代全场景识别完成。
31. 1. 不得把识别写成全特征比对完成。
32. 2. 不得在识别范围未确定时宣称唯一。
33. 3. 不得把当前范围唯一扩大成全世界唯一。
34. 4. 不得由识别方法直接补写颜色、空间坐标、轮廓、深度等一阶特征值。
35. 5. 不得把候选唯一但硬冲突的结果写成已归属。
36. 6. 不得把候选不唯一写成弱归属或默认归属。
37. 7. 不得把外设报告、ROI、掩码、点集或材料页直接当成自我世界存在真值。
38. 8. 不得把识别输出存在指针反推成扫描完成、跟踪完成、全场景明确、安全明确、需求满足或安全 / 服务值可结算。
39. 9. 不得只用说明性文字表达冲突、证据不足、阈值来源或候选收敛过程。
40. 10. 不得让控制面板显示结果反写识别结论。

## 4. 使用边界

1. 本规范用于计划、详细设计、代码修改和验收前的规则回读。
2. 本规范不能作为代码实现完成、业务闭环完成、自我苏醒完成或初步成熟完成的证据。
3. 需要展开历史论证时，只能把 `规范/旧规范/` 中的原文件作为追溯材料，不得让旧文件反向覆盖本规范。

## 修订记录

| 日期 | 版本 | 修订内容 |
| --- | --- | --- |
| 20260701 | v1.0 | 由历史来源修订迁回为正式子规范，接入当前规范目录。 |
