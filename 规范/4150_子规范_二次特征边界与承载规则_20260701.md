# 4150 子规范 二次特征边界与承载规则

编号：4150
日期：20260701
状态：正式子规范
权威等级：正式规范

## 1. 定位

本规范是 `基础信息树` 主题下由旧规范修订迁回的正式子规范。它只承接当前仍有效的规则，不恢复旧规范中的历史口径、详细设计措辞或已经被根规范吸收的重复条款。

历史来源：
- `规范/旧规范/二次特征规范20260520.md`

上级规范：
- `规范/1180_根规范_二次特征主信息类_20260701.md`
- `规范/0050_项目通用结构与禁止性规则总纲_20260701.md`
- `规范/0300_规范冲突与前后矛盾清单_20260701.md`

## 2. 上级约束

1. 本规范服从 `规范/规范目录.md`、0050 总纲、0300 冲突清单和对应根规范。
2. 历史来源中与当前根规范冲突的表述不进入当前权威口径。
3. 旧名称、旧结构和旧阶段口径必须按 `规范/0300_规范冲突与前后矛盾清单_20260701.md` 的 C001-C008 收束，不得在本规范中恢复历史并列结构、旧字段身份或旧执行来源。
4. 任务筹办 / 任务执行只作为任务管理生命周期普通函数；动作来源、方法注册和方法选择必须服从 0050 总纲、3200 任务根规范、3300 方法根规范和 1170 动态根规范。
5. 文本、日志、注释、临时 JSON、自由字段和显示摘要不得承载正式机器逻辑或业务状态。

## 3. 当前有效规则

以下条款由历史来源按当前根规范修订抽取；若条款与第 2 章或上级规范冲突，以上级规范和第 2 章为准。

1. 所有方向必须来自有符号比较量：
2. 不得作为底层逻辑字段。
3. 不得新增平行字段或平行结构。
4. 双特征变化对应关系
5. 6. 多基准项 / 多响应项。
6. 多特征影响应进入：
7. 不得塞入单个二次特征。
8. 两个不同特征类型变化量之间的对应关系。
9. 当需要记录“具体存在属于某抽象存在”时，不得把来源、置信、证据或冲突情况写入抽象存在本体，也不得新增 `抽象存在主信息类`。
10. 可追溯归类事实应由二次特征或等价关系事实承载：
11. `概念集` 可以作为快速匹配缓存同步更新；需要解释“为什么属于、是否仍然属于、能否撤销或降低置信”时，以二次特征、状态、动态、场景和因果证据链为准。
12. 本节不扩大第一版二元异类变化关系的学习范围；若当前实现尚未具备完整主体 / 客体归类二次特征节点，应先使用现有状态、动态、场景索引和因果模板证据动态样本保持证据链，不得因此把归类证据塞回存在主信息。
13. 二元异类变化关系至少需要：
14. 响应变化项
15. 不得作为：
16. 从一个态到另一个态，两个不同特征类型的变化如何对应。
17. 规范层建议使用以下语义结构描述，具体代码实现可以由现有二次特征、动态、场景和方法结果模板承载，不要求必须新增同名结构体。
18. 比较方向符号或允许方向;
19. 结构_变化项 响应变化项;
20. 响应变化项：
21. 方向必须来自比较关系，不得新增业务解释枚举作为底层事实。
22. 底层方向应表达为：
23. 允许方向
24. 允许方向：
25. 表示比较量 r 的符号是否允许命中。
26. 不得把以下词写入二次特征本体作为权威字段：
27. 这些词只能由：
28. 2. 响应特征类型是否相同。
29. 5. 响应方向是否匹配。
30. 6. 响应变化量是否落入区间。
31. if (样本.响应变化项.特征类型 != 模板.响应变化项.特征类型) return false;
32. if (!方向命中(样本.响应变化项, 模板.响应变化项)) return false;
33. if (!响应变化量落入区间(样本.响应变化项, 模板.响应变化项)) return false;
34. -> 响应变化 [+3, +5]
35. -> 响应变化 +21
36. 不得自动推成：
37. -> 响应变化 +4.2
38. -> 响应变化 [+18, +24]
39. 第 1 单位变化 -> 响应 +4
40. 第 2 单位变化 -> 响应 +5
41. 第 3 单位变化 -> 响应 +3
42. 才允许分别作为单位变化样本。
43. 但不用于更新 +1 单位对应模板。
44. 两个不同特征类型之间能不能形成二元异类变化关系，必须由关系类型注册或等价的模板声明决定。
45. 不得自由比较任意特征类型。
46. 允许示例：
47. 禁止默认成立：
48. 特征类型ID 响应特征类型;
49. 单位ID 响应单位;
50. 允许的基准方向集合;
51. 允许的响应方向集合;
52. bool 是否允许单位步长关系;
53. bool 是否允许多步长关系;
54. 承载。是否新增结构体应另按 `005` 准入约束审查。
55. ├─ 支持的响应特征类型集合
56. 如果同一条件区间下响应方向反复冲突，不得把矛盾吞进单个二元关系模板。应触发：
57. 响应变化项 Y
58. 响应特征类型不同：
59. 响应方向不同：
60. 优先拆分条件区间，或新建结果节点。
61. 响应变化量超出区间但方向相同：
62. 同一条件区间内响应方向反复冲突：
63. 应补充条件特征或拆分条件节点；
64. 不得扩成多特征二次特征。
65. 但不知道动作或条件，不得直接声称完整因果。只能形成：
66. 第一版只需要支持：
67. 一个响应变化项
68. 允许方向 方向;
69. 变化项 响应变化项;
70. 不得在第一版加入：
71. 多响应项
72. 2. 二元异类变化关系只处理两个不同特征类型之间的变化对应。
73. 6. 状态信息不存二元异类变化关系，只能引用相关动态或二次特征。
74. 8. 关系类型必须注册或由模板明确声明，不得任意跨特征比较。
75. 9. 方向不得用解释性业务枚举作为底层事实，必须回到比较量符号、允许方向、方向区间和值域。
76. 10. 二元异类变化关系候选不等于完整因果；缺动作或缺条件时只能作为因果补完材料。
77. 当前只实现“基准特征变化项 -> 响应特征变化项”的二元异类变化关系比较；

## 4. 使用边界

1. 本规范用于计划、详细设计、代码修改和验收前的规则回读。
2. 本规范不能作为代码实现完成、业务闭环完成、自我苏醒完成或初步成熟完成的证据。
3. 需要展开历史论证时，只能把 `规范/旧规范/` 中的原文件作为追溯材料，不得让旧文件反向覆盖本规范。

## 修订记录

| 日期 | 版本 | 修订内容 |
| --- | --- | --- |
| 20260701 | v1.0 | 由历史来源修订迁回为正式子规范，接入当前规范目录。 |
