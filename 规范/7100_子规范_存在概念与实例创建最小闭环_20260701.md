# 7100 子规范 存在概念与实例创建最小闭环

编号：7100
日期：20260701
状态：正式子规范
权威等级：正式规范

## 1. 定位

本规范是 `存在语素与入口` 主题下由旧规范修订迁回的正式子规范。它只承接当前仍有效的规则，不恢复旧规范中的历史口径、详细设计措辞或已经被根规范吸收的重复条款。

历史来源：
- `规范/旧规范/存在概念与实例创建最小闭环规范20260509.md`

上级规范：
- `规范/1140_根规范_存在节点主信息类_20260701.md`
- `规范/0050_项目通用结构与禁止性规则总纲_20260701.md`
- `规范/0300_规范冲突与前后矛盾清单_20260701.md`

## 2. 上级约束

1. 本规范服从 `规范/规范目录.md`、0050 总纲、0300 冲突清单和对应根规范。
2. 历史来源中与当前根规范冲突的表述不进入当前权威口径。
3. 旧名称、旧结构和旧阶段口径必须按 `规范/0300_规范冲突与前后矛盾清单_20260701.md` 的 C001-C008 收束，不得在本规范中恢复历史并列结构、旧字段身份或旧执行来源。
4. 任务筹办 / 任务执行只作为任务管理生命周期普通函数；动作来源、方法注册和方法选择必须服从 0050 总纲、3200 任务根规范、3300 方法根规范和 1170 动态根规范。
5. 文本、日志、注释、临时 JSON、自由字段和显示摘要不得承载正式机器逻辑或业务状态。

## 3. 当前有效规则

以下条款由历史来源按当前根规范修订抽取；若条款与第 2 章或上级规范冲突，以上级规范和第 2 章为准。

1. > 2026-06-27 修订说明：按 `规范/基础抽象命名规则规范20260627.md`，“存在”是存在类信息的根抽象名，也是存在概念链最高节点名称。本文旧写法“存在概念根”统一解释为历史存储容器名或概念侧路径说明；当前规范判断中不得把“存在概念根”当作最高抽象名。概念存在应理解为归入“存在”根抽象链概念侧的普通存在节点。
2. 上述 `置信度 / 来源 / 状态 / 命中特征 / 冲突特征` 不得写入存在主信息或概念集本体；如果归类需要可追溯证据，应由二次特征、状态、动态、场景和因果模板证据动态样本表达。
3. 能力、需求、方法、用途、行为模板等，不进入概念归属关系。它们后续若需要使用，只作为“新建实例时的记忆扩充内容”处理。
4. `存在节点主信息类` 使用 `概念集` 表达一个实例对应的多个概念存在：
5. // 新逻辑归类入口 / 快速匹配字段：实例可以对应多个概念存在。
6. 1. 旧代码写入 概念模板 时，必须同步保证 概念集 包含该概念。
7. 2. 新代码添加第一个概念时，如果 概念模板 为空，应同步写入 概念模板。
8. 3. 读取概念归属时优先读 概念集。
9. 5. 删除或替换 概念模板 时，不得误删 概念集 中的其他概念。
10. 抽象存在不得记录：
11. 概念存在用于自然语言和实例创建，因此应尽量唯一。
12. 应优先查找已有概念，找不到才创建。
13. -> 对应基础信息类型
14. -> 对应基础信息节点
15. 专名是否指向实例，应由调用方在有场景或实例上下文时显式绑定；语素层不默认猜测。
16. 重复特征按以下优先级去重：
17. 1. 实例已有特征优先，不被概念扩充覆盖。
18. 2. 多个概念都提供同一特征时，先加入的概念优先。
19. 2. 指向场景实例、临时动态、当前任务、运行期对象的指针，不应从概念复制到实例。
20. 如果业务明确需要默认指针值，再由具体方法显式填充。
21. 允许：
22. “实例在先，概念在后”是认识论原则，不要求工程上禁止预置概念字典。
23. 集合优先用特征和查询条件表达。
24. 如果某个集合需要被调度、命名或记录因果，例如：
25. 当业务只需要快速筛选、方法候选召回或模板初配时，可以优先读取：
26. 当业务需要来源、置信度、版本、后天修正或冲突处理时，概念归属不得只看 `概念集`，必须回查：
27. 如果为了统一查询需要生成：
28. 不得替代 `概念集`。
29. `概念集` 可以同步更新作为查询缓存；需要判断“为什么属于、是否仍然属于、能否撤销或降低置信”时，以可追溯归类事实和证据链为准。
30. 安全信息中的评估结果 -> 由安全性评估方法生成，优先进入输出结果场景 / 执行回执视图 / 短期评估状态缓存。
31. 这里 `场景存在` 只是模板适用类型，不是动态账本。不得把 `最近伤害时间 / 当前安全度 / 当前伤害可能性 / 当前最低安全度存在` 写入抽象存在本体。
32. 禁止：
33. 实现应满足：
34. 6. 实例存在可以对应多个概念存在。
35. 8. 新建实例时，把概念存在的特征等扩充到实例；需求、方法、行为模板不得直接塞入概念归属关系。
36. 10. 集合优先用特征和查询条件表示。
37. 12. 概念集 是第一阶段归类入口和快速匹配缓存；需要来源、置信或修正时回查二次特征、状态、动态、场景和因果证据。
38. 14. 概念存在按 名称 + 类型 唯一，实例存在允许同名多实例。

## 4. 使用边界

1. 本规范用于计划、详细设计、代码修改和验收前的规则回读。
2. 本规范不能作为代码实现完成、业务闭环完成、自我苏醒完成或初步成熟完成的证据。
3. 需要展开历史论证时，只能把 `规范/旧规范/` 中的原文件作为追溯材料，不得让旧文件反向覆盖本规范。

## 修订记录

| 日期 | 版本 | 修订内容 |
| --- | --- | --- |
| 20260701 | v1.0 | 由历史来源修订迁回为正式子规范，接入当前规范目录。 |
