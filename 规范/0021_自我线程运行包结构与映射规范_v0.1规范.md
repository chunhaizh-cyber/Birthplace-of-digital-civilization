# 0021 自我线程运行包结构与映射规范 v0.1规范

## 1. 目标

本文用于把 [0020_自我线程运行三包规范_v0.1规范.md](D:\鱼巢\规范\0020_自我线程运行三包规范_v0.1规范.md) 中已经定下的三包数据合同，进一步落成一份 `结构体 + 字段拥有者 + 封口规则 + 持久化策略 + 包到节点映射器` 的 `v0.1` 详细设计规范。

本文要固定的是：

- 公共枚举和统一包头
- `自我线程运行包.ixx` 中三包与公共子结构的 C++ 结构定义
- 每个字段的 `拥有者 / 写入时机 / 封口时机 / 是否持久化`
- `自我线程运行包校验器.ixx` 的最小封口、只读和完整性校验接口
- `自我线程运行包映射器.ixx` 的最小 `包 -> 节点` 写回接口
- 第一条落地金路径只先走 `解释 / 预警 / 建议 -> 回流结算 -> 学习候选沉淀`

本文不负责：

- 发明新的世界模型
- 复制现有节点的字段全集
- 直接定义任务管理中轴内部策略算法、学习算法和调度算法
- 一上来就承诺全自动真实执行、自动回滚和自动联动改写

必须先固定一句话：

`包是运行态，节点是持久态；0021 的任务是把三包做成线程里的数据合同和映射合同，而不是再造一套平行世界。`


## 2. 上下位关系

上位规范：

- [0010_自我实现逻辑与运行闭环规范.md](D:\鱼巢\规范\0010_自我实现逻辑与运行闭环规范.md)
- [0011_自我架构总定义规范.md](D:\鱼巢\规范\0011_自我架构总定义规范.md)
- [0013_自我线程运行时实现规范.md](D:\鱼巢\规范\0013_自我线程运行时实现规范.md)
- [0014_自我线程主循环最小职责表规范.md](D:\鱼巢\规范\0014_自我线程主循环最小职责表规范.md)
- [0015_自我线程模块接口总表规范.md](D:\鱼巢\规范\0015_自我线程模块接口总表规范.md)
- [0016_自我线程模块接口总表_v0.1（骨架层）规范.md](D:\鱼巢\规范\0016_自我线程模块接口总表_v0.1（骨架层）规范.md)
- [0020_自我线程运行三包规范_v0.1规范.md](D:\鱼巢\规范\0020_自我线程运行三包规范_v0.1规范.md)
- [0027_自我线程与任务管理工作线程状态流转与事件协议_v0.1规范.md](D:\鱼巢\规范\0027_自我线程与任务管理工作线程状态流转与事件协议_v0.1规范.md)
- [0317_执行前门控最小输入与判定顺序规范.md](D:\鱼巢\规范\0317_执行前门控最小输入与判定顺序规范.md)
- [0322_双值结算最小输入结算顺序与禁止混层规范.md](D:\鱼巢\规范\0322_双值结算最小输入结算顺序与禁止混层规范.md)
- [1245_类职责与函数归位规范.md](D:\鱼巢\规范\1245_类职责与函数归位规范.md)
- [1285_因果分层建模与实例生成规范.md](D:\鱼巢\规范\1285_因果分层建模与实例生成规范.md)
- [1290_因果信息模块实现规范.md](D:\鱼巢\规范\1290_因果信息模块实现规范.md)

当前底座挂点依据：

- `世界链 / 特征值链 / 需求链 / 任务链 / 方法链` 已存在
- `存在节点` 已具备 `需求根 / 任务根 / 方法根`
- `场景节点` 已具备 `状态索引 / 动态索引 / 关系索引 / 实例因果索引`
- `动态节点` 已具备 `来源动作 / 来源输入场景 / 来源输出场景 / 执行成功 / 错误码 / 动态路径签名`
- `因果实例` 已具备 `来源任务主键 / 来源方法主键 / 置信度 / 已验证`

下游影响：

- `自我线程运行包.ixx`
- `自我线程运行包校验器.ixx`
- `自我线程运行包映射器.ixx`
- `自我线程模块.ixx`
- `自我线程类.cpp`
- `任务执行类.ixx`
- `学习类.ixx`


## 3. 核心结论

必须固定以下结论：

1. `0020` 解决的是“线程里流动哪三张票据”，`0021` 解决的是“这三张票据长什么样、谁能写、何时封口、如何回写到现有节点体系”。
2. `自我线程运行包.ixx` 只放类型定义，不放算法逻辑。
3. 三包都必须复用统一包头；`包状态 = 已封口` 后，局部模块只能读，不能改。
4. 字段拥有权必须按模块分段写死，不能谁手上有引用谁就补字。
5. 包正文默认不整包持久化；长期价值只通过 `包 -> 节点` 映射选择性写回。
6. `线程轮次上下文包` 是本轮共识，`一步治理任务包` 是本轮执照，`回流结算包` 是本轮账本。
7. 每个任务包必须对应一个结算包；哪怕授权失败、方法未命中、执行被打断，也不允许“无账结束”。
8. 第一条落地金路径应先锁在 `解释 / 预警 / 建议`，不先打开真实执行、自动回滚和自动联动改写。
9. `方法禁止项` 必须是条件化禁用规则，而不是扁平布尔标记；它至少要带 `触发条件 / 禁止原因 / 证据来源 / 生效范围 / 解禁条件`。
10. `任务入口` 由自我线程创建并派发给任务管理工作线程；`派生需求` 与 `子结果路由` 只能先回主循环，再决定后续去向。

一句话收口：

`到 0021 为止，三包不只是概念合同，而是已经具备结构、所有权、封口纪律和映射纪律的工程合同。`


## 4. 当前应产出的工程文件

### 4.1 文件清单

当前这一轮，最适合直接产出的文件固定为：

1. `自我线程运行包.ixx`
   - 只放 `using / enum class / struct`
   - 不放校验逻辑
   - 不放持久化逻辑
   - 不放线程流程逻辑

2. `自我线程运行包校验器.ixx`
   - 只放 `封口 / 只读 / 字段完整性` 校验
   - 统一负责 `包状态` 和 `封口时间`
   - 不负责节点写回

3. `自我线程运行包映射器.ixx`
   - 只放 `包 -> 节点` 的最小回写逻辑
   - 只负责把有长期价值的摘要、结果、证据写回现有节点体系
   - 不负责替代主循环做重判

### 4.2 硬边界

必须固定：

- `包模块` 不判断业务真值，只描述结构
- `校验器` 不发明新字段，只检查和封口
- `映射器` 不改写根方向，只做选择性落库和挂接


## 5. 公共枚举与统一包头

### 5.1 推荐模块骨架

建议先落一个只放类型、不放逻辑的模块：

`自我线程运行包.ixx`

```cpp
export module 自我线程运行包;

import <cstdint>;
import <string>;
import <vector>;
import <optional>;

export using 主键 = std::uint64_t;
export using 时间戳 = std::int64_t;
export using I64 = std::int64_t;

export enum class 枚举_包状态 : std::uint8_t {
    草稿 = 0,
    已封口 = 1
};

export enum class 枚举_生命周期状态 : std::uint8_t {
    待机 = 0,
    运行 = 1,
    收束 = 2,
    停止 = 3,
    死亡退出 = 4
};

export enum class 枚举_运行模式 : std::uint8_t {
    正常 = 0,
    降级 = 1,
    只读 = 2
};

export enum class 枚举_需求大类 : std::uint8_t {
    服务 = 0,
    安全 = 1
};

export enum class 枚举_服务子类 : std::uint8_t {
    无 = 0,
    直接服务 = 1,
    准备性服务 = 2
};

export enum class 枚举_安全子类 : std::uint8_t {
    无 = 0,
    物理安全 = 1,
    风险安全 = 2
};

export enum class 枚举_动作类型 : std::uint8_t {
    观察 = 0,
    解释 = 1,
    预警 = 2,
    建议 = 3,
    执行 = 4,
    回滚 = 5,
    收束 = 6
};

export enum class 枚举_结算结论 : std::uint8_t {
    无效推进 = 0,
    缩差 = 1,
    持平 = 2,
    恶化 = 3,
    越界 = 4
};

export enum class 枚举_学习样本类别 : std::uint8_t {
    正样本 = 0,
    负样本 = 1,
    待验证 = 2,
    黑名单 = 3
};

export enum class 枚举_方法禁止级别 : std::uint8_t {
    禁用 = 0,
    仅建议态可用 = 1,
    仅人工确认可用 = 2
};

export enum class 枚举_禁止项状态 : std::uint8_t {
    生效中 = 0,
    待复核 = 1,
    已解除 = 2
};

export struct 结构_线程运行包头 {
    主键 包主键 = 0;
    主键 线程主键 = 0;
    主键 轮次主键 = 0;
    主键 宿主存在主键 = 0;
    主键 父包主键 = 0;

    时间戳 创建时间 = 0;
    时间戳 封口时间 = 0;

    std::uint64_t 追踪签名 = 0;
    枚举_包状态 包状态 = 枚举_包状态::草稿;
};
```

### 5.2 硬规则

必须固定：

1. `枚举` 先统一，后续模块不得私造同义状态和动作枚举。
2. `结构_线程运行包头` 只负责追踪和封口，不负责业务裁决。
3. `父包主键` 的语义固定为：
   - `上下文包`：默认可为 `0`
   - `任务包`：指向来源 `上下文包.包头.包主键`
   - `结算包`：指向来源 `任务包.包头.包主键`
4. `包状态` 和 `封口时间` 只能由 `校验器` 写入。


## 6. 三包的 C++ 结构体定义

### 6.1 公共子结构

```cpp
export struct 结构_差额值 {
    I64 目标值 = 0;
    I64 当前值 = 0;
    I64 差额 = 0;
    I64 趋势 = 0;
};

export struct 结构_根需求项 {
    主键 需求节点主键 = 0;
    枚举_需求大类 大类 = 枚举_需求大类::服务;
    枚举_服务子类 服务子类 = 枚举_服务子类::无;
    枚举_安全子类 安全子类 = 枚举_安全子类::无;
    I64 紧迫度 = 0;
    I64 优先级 = 0;
    std::string 触发原因{};
};

export struct 结构_授权约束段 {
    I64 授权等级 = 0;
    I64 资源配额 = 0;
    I64 时间窗_ms = 0;
    I64 并发上限 = 1;

    bool 允许真实写回 = false;
    bool 允许回滚 = false;
    bool 必须人工确认 = false;

    std::vector<枚举_动作类型> 动作白名单{};
    std::vector<std::string> 停止条件{};
};

export struct 结构_方法候选项 {
    主键 方法主键 = 0;
    I64 置信度 = 0;
    std::string 命中原因{};
};

export struct 结构_状态变化项 {
    主键 主体主键 = 0;
    主键 特征主键 = 0;
    I64 变化前 = 0;
    I64 变化后 = 0;
    std::string 变化原因{};
};

export struct 结构_学习候选项 {
    主键 锚点主键 = 0;
    主键 来源任务主键 = 0;
    主键 来源方法主键 = 0;
    std::string 动作语义键{};
    I64 置信度 = 0;
    枚举_学习样本类别 样本类别 = 枚举_学习样本类别::待验证;
    bool 已验证 = false;
};

export struct 结构_方法禁止项 {
    主键 方法主键 = 0;
    枚举_方法禁止级别 禁止级别 = 枚举_方法禁止级别::禁用;
    std::vector<std::string> 触发条件集{};
    std::string 禁止原因{};
    std::string 证据来源{};
    主键 来源需求主键 = 0;
    主键 来源轮次主键 = 0;
    std::string 生效范围{};
    std::string 解禁条件{};
    枚举_禁止项状态 当前状态 = 枚举_禁止项状态::生效中;
};
```

### 6.2 线程轮次上下文包

```cpp
export struct 结构_线程轮次上下文包 {
    结构_线程运行包头 包头{};

    // 边界段
    枚举_生命周期状态 生命周期 = 枚举_生命周期状态::待机;
    枚举_运行模式 运行模式 = 枚举_运行模式::正常;
    bool 停止标志 = false;
    bool 死亡退出标志 = false;
    bool 安全接管标志 = false;

    // 世界快照段
    主键 当前场景主键 = 0;
    std::vector<主键> 关键存在主键集{};
    std::vector<主键> 关键状态主键集{};
    std::vector<主键> 关键关系主键集{};
    std::vector<主键> 异常变化主键集{};

    // 差额段
    结构_差额值 服务差额{};
    结构_差额值 物理安全差额{};
    结构_差额值 风险安全差额{};

    // 根需求段
    std::vector<结构_根需求项> 根需求候选集{};
    std::optional<结构_根需求项> 主导需求{};
    std::vector<主键> 从属需求主键集{};

    // 需求治理段
    std::vector<结构_根需求项> 需求队列摘要集{};

    // 方法禁止项段
    std::vector<结构_方法禁止项> 方法禁止项集{};

    // 全局队列段
    std::vector<主键> 可运行任务主键集{};
    std::vector<主键> 等待任务主键集{};
    std::vector<主键> 回流任务主键集{};
    std::vector<主键> 收束停止任务主键集{};

    // 仲裁段
    I64 推进等级 = 0;
    bool 允许推进 = false;
    bool 要求收束 = false;

    // 资源上限段
    I64 时间预算_ms = 0;
    I64 资源预算 = 0;
    I64 并发上限 = 1;
    I64 最大授权等级 = 0;

    // 回流要求段
    bool 必须记录动态 = true;
    bool 必须生成结算包 = true;
    bool 允许真实写回 = false;
    bool 允许学习候选 = true;
};
```

### 6.3 一步治理任务包

```cpp
export struct 结构_一步治理任务包 {
    结构_线程运行包头 包头{};

    // 来源段
    主键 来源需求主键 = 0;
    主键 来源任务主键 = 0;
    枚举_需求大类 来源需求大类 = 枚举_需求大类::服务;
    std::string 本轮要缩小的差额说明{};

    // 任务入口段
    主键 任务根节点主键 = 0;
    主键 任务入口主键 = 0;
    bool 任务根只读 = true;
    主键 调度工作线程主键 = 0;

    // 治理定义段
    枚举_动作类型 动作类型 = 枚举_动作类型::解释;
    std::string 任务语义键{};
    主键 目标主体主键 = 0;
    主键 目标场景主键 = 0;
    std::string 完成条件{};
    std::string 预期结果{};

    // 授权段
    结构_授权约束段 授权约束{};

    // 方法段
    std::vector<结构_方法候选项> 候选方法集{};
    std::optional<结构_方法候选项> 选定方法{};
    std::vector<主键> 预期输入状态主键集{};
    std::vector<主键> 预期输出状态主键集{};

    // 方法门控段
    std::vector<结构_方法禁止项> 方法禁止项快照{};
    std::vector<结构_方法候选项> 被过滤方法集{};
    std::vector<std::string> 方法过滤原因集{};

    // 监督段
    bool 要求记录动态 = true;
    bool 要求记录副作用 = true;
    bool 要求记录失败原因 = true;
    I64 安全监测等级 = 0;
};
```

### 6.4 回流结算包

```cpp
export struct 结构_回流结算包 {
    结构_线程运行包头 包头{};

    主键 任务包主键 = 0;
    主键 选定方法主键 = 0;

    // 执行事实段
    主键 输入场景主键 = 0;
    主键 输出场景主键 = 0;
    bool 执行成功 = false;
    bool 被中断 = false;
    bool 已超时 = false;
    I64 错误码 = 0;
    std::uint64_t 动态路径签名 = 0;

    // 状态变化段
    std::vector<结构_状态变化项> 状态变化集{};
    std::vector<主键> 副作用状态主键集{};

    // 安全异常段
    bool 触发降级 = false;
    bool 触发回滚 = false;
    bool 触发收束 = false;
    std::string 止损原因{};

    // 派生需求候选段
    std::vector<结构_根需求项> 派生需求候选集{};

    // 结果路由段
    主键 父任务主键 = 0;
    主键 路由目标任务主键 = 0;
    bool 是否已路由父层任务 = false;
    std::string 路由原因{};

    // 缩差结算段
    结构_差额值 服务差额前后{};
    结构_差额值 物理安全差额前后{};
    结构_差额值 风险安全差额前后{};
    枚举_结算结论 结算结论 = 枚举_结算结论::无效推进;
    bool 是否有效推进 = false;

    // 方法禁止项更新段
    std::vector<结构_方法禁止项> 方法禁止项更新建议集{};

    // 学习候选段
    std::vector<结构_学习候选项> 学习候选集{};

    // 下一轮建议段
    bool 建议进入待机 = false;
    bool 建议进入收束 = false;
    bool 建议停止 = false;
    bool 建议死亡退出 = false;
    bool 建议重试 = false;
};
```

### 6.5 三包结构的硬纪律

必须固定：

1. `上下文包` 是本轮共识，`任务包` 是本轮唯一一步，`结算包` 是本轮账本。
2. `上下文包` 仲裁完成后封口。
3. `任务包` 在方法阶段结束后封口；可包含 `选定方法为空` 的未命中失败态，但不得在封口后再改任务语义和授权边界。
4. `结算包` 在根层完成结算并给出下一轮入口后封口。
5. `学习候选集` 默认只能记录 `已验证 = false` 的候选，不得直接写成稳定因果。


## 7. 字段拥有者、写入时机、封口时机与持久化规则

### 7.1 组合字段继承规则

为避免重复，先固定一条规则：

`若某字段是 struct / vector / optional 组合字段，则其叶子字段默认继承该组合字段所在段的拥有者、写入时机、封口时机与持久化策略；除非本文另行显式覆盖。`

### 7.2 公共包头字段

| 字段 | 拥有者 | 首次写入时机 | 封口时机 / 可变性 | 是否持久化 / 去向 |
| --- | --- | --- | --- | --- |
| `包主键` | 包创建器 | 创建包时立即写入 | 写入后只读 | `否`；仅调试审计快照 |
| `线程主键` | 包创建器 | 创建包时立即写入 | 写入后只读 | `条件持久化`；轮次/动态追踪摘要 |
| `轮次主键` | 包创建器 | 创建包时立即写入 | 写入后只读 | `是`；轮次摘要、任务摘要、动态追踪键 |
| `宿主存在主键` | 包创建器 | 创建包时立即写入 | 写入后只读 | `条件持久化`；摘要追踪键 |
| `父包主键` | 包创建器 | 派生子包时写入 | 写入后只读 | `否`；仅调试审计快照 |
| `创建时间` | 包创建器 | 创建包时立即写入 | 写入后只读 | `条件持久化`；调试审计快照 |
| `封口时间` | 运行包校验器 | 封口时写入 | 仅允许写一次 | `条件持久化`；调试审计快照 |
| `追踪签名` | 主循环入口或包创建器 | 创建包时写入或继承 | 封口前不可改；默认写入后只读 | `条件持久化`；动态/审计追踪摘要 |
| `包状态` | 运行包校验器 | 创建时默认 `草稿`，封口时改 `已封口` | 仅允许单向变更 | `否`；运行态字段 |

### 7.3 公共子结构字段

#### 7.3.1 `结构_差额值`

| 字段 | 拥有者 | 首次写入时机 | 封口时机 / 可变性 | 是否持久化 / 去向 |
| --- | --- | --- | --- | --- |
| `目标值` | 继承所属差额段拥有者 | 差额评估或结算时写入 | 随所在包封口后只读 | `条件持久化`；差额摘要 |
| `当前值` | 继承所属差额段拥有者 | 差额评估或结算时写入 | 随所在包封口后只读 | `条件持久化`；差额摘要 |
| `差额` | 继承所属差额段拥有者 | 差额评估或结算时写入 | 随所在包封口后只读 | `是`；差额摘要 |
| `趋势` | 继承所属差额段拥有者 | 差额评估或结算时写入 | 随所在包封口后只读 | `条件持久化`；差额摘要 |

#### 7.3.2 `结构_根需求项`

| 字段 | 拥有者 | 首次写入时机 | 封口时机 / 可变性 | 是否持久化 / 去向 |
| --- | --- | --- | --- | --- |
| `需求节点主键` | 继承所属需求字段拥有者 | 生成候选或选定主导需求时写入 | 随所在包封口后只读 | `条件持久化`；仲裁摘要 |
| `大类` | 继承所属需求字段拥有者 | 生成需求时写入 | 随所在包封口后只读 | `条件持久化`；仲裁摘要 |
| `服务子类` | 继承所属需求字段拥有者 | 生成需求时写入 | 随所在包封口后只读 | `条件持久化`；仲裁摘要 |
| `安全子类` | 继承所属需求字段拥有者 | 生成需求时写入 | 随所在包封口后只读 | `条件持久化`；仲裁摘要 |
| `紧迫度` | 继承所属需求字段拥有者 | 生成需求或选主时写入 | 随所在包封口后只读 | `条件持久化`；仲裁摘要 |
| `优先级` | 继承所属需求字段拥有者 | 生成需求或选主时写入 | 随所在包封口后只读 | `条件持久化`；仲裁摘要 |
| `触发原因` | 继承所属需求字段拥有者 | 生成需求或选主时写入 | 随所在包封口后只读 | `否`；默认只审计 |

#### 7.3.3 `结构_授权约束段`

| 字段 | 拥有者 | 首次写入时机 | 封口时机 / 可变性 | 是否持久化 / 去向 |
| --- | --- | --- | --- | --- |
| `授权等级` | `最小授权接口` | 授权判门通过时写入 | 随任务包封口后只读 | `是`；任务授权摘要 |
| `资源配额` | `最小授权接口` | 授权判门通过时写入 | 随任务包封口后只读 | `是`；任务授权摘要 |
| `时间窗_ms` | `最小授权接口` | 授权判门通过时写入 | 随任务包封口后只读 | `是`；任务授权摘要 |
| `并发上限` | `最小授权接口` | 授权判门通过时写入 | 随任务包封口后只读 | `是`；任务授权摘要 |
| `允许真实写回` | `最小授权接口` | 授权判门通过时写入 | 随任务包封口后只读 | `是`；任务授权摘要 |
| `允许回滚` | `最小授权接口` | 授权判门通过时写入 | 随任务包封口后只读 | `是`；任务授权摘要 |
| `必须人工确认` | `最小授权接口` | 授权判门通过时写入 | 随任务包封口后只读 | `是`；任务授权摘要 |
| `动作白名单` | `最小授权接口` | 授权判门通过时写入 | 随任务包封口后只读 | `条件持久化`；授权摘要 |
| `停止条件` | `最小授权接口` | 授权判门通过时写入 | 随任务包封口后只读 | `条件持久化`；授权摘要 |

#### 7.3.4 `结构_方法候选项`

| 字段 | 拥有者 | 首次写入时机 | 封口时机 / 可变性 | 是否持久化 / 去向 |
| --- | --- | --- | --- | --- |
| `方法主键` | `任务管理中轴（含内部策略器）` | 候选比较时写入 | 随任务包封口后只读 | `条件持久化`；仅选定方法摘要 |
| `置信度` | `任务管理中轴（含内部策略器）` | 候选比较时写入 | 随任务包封口后只读 | `条件持久化`；仅选定方法摘要 |
| `命中原因` | `任务管理中轴（含内部策略器）` | 候选比较时写入 | 随任务包封口后只读 | `否`；默认只审计 |

#### 7.3.5 `结构_方法禁止项`

| 字段 | 拥有者 | 首次写入时机 | 封口时机 / 可变性 | 是否持久化 / 去向 |
| --- | --- | --- | --- | --- |
| `方法主键` | `优先级与冲突仲裁` 或 `结果回流、结算与重判` | 生成或更新方法禁止项时写入 | 随所在包封口后只读 | `是`；禁止项摘要 / 更新摘要 |
| `禁止级别` | `优先级与冲突仲裁` 或 `结果回流、结算与重判` | 生成或更新方法禁止项时写入 | 随所在包封口后只读 | `是`；禁止项摘要 / 更新摘要 |
| `触发条件集` | `优先级与冲突仲裁` 或 `结果回流、结算与重判` | 命中条件并生成禁止项时写入 | 随所在包封口后只读 | `条件持久化`；禁止项摘要 |
| `禁止原因` | `优先级与冲突仲裁` 或 `结果回流、结算与重判` | 生成或更新方法禁止项时写入 | 随所在包封口后只读 | `条件持久化`；禁止项摘要 |
| `证据来源` | `优先级与冲突仲裁` 或 `结果回流、结算与重判` | 生成或更新方法禁止项时写入 | 随所在包封口后只读 | `条件持久化`；禁止项摘要 |
| `来源需求主键` | `优先级与冲突仲裁` 或 `结果回流、结算与重判` | 生成或更新方法禁止项时写入 | 随所在包封口后只读 | `条件持久化`；禁止项摘要 |
| `来源轮次主键` | `优先级与冲突仲裁` 或 `结果回流、结算与重判` | 生成或更新方法禁止项时写入 | 随所在包封口后只读 | `条件持久化`；禁止项摘要 |
| `生效范围` | `优先级与冲突仲裁` 或 `结果回流、结算与重判` | 生成或更新方法禁止项时写入 | 随所在包封口后只读 | `条件持久化`；禁止项摘要 |
| `解禁条件` | `优先级与冲突仲裁` 或 `结果回流、结算与重判` | 生成或更新方法禁止项时写入 | 随所在包封口后只读 | `条件持久化`；禁止项摘要 |
| `当前状态` | `优先级与冲突仲裁` 或 `结果回流、结算与重判` | 生成、续期或解除禁止项时写入 | 随所在包封口后只读 | `是`；禁止项摘要 / 更新摘要 |

#### 7.3.6 `结构_状态变化项`

| 字段 | 拥有者 | 首次写入时机 | 封口时机 / 可变性 | 是否持久化 / 去向 |
| --- | --- | --- | --- | --- |
| `主体主键` | `工作线程承接接口` | 写入状态变化时 | 随结算包封口后只读 | `是`；动态节点 / 场景动态索引 |
| `特征主键` | `工作线程承接接口` | 写入状态变化时 | 随结算包封口后只读 | `是`；动态节点 / 场景动态索引 |
| `变化前` | `工作线程承接接口` | 写入状态变化时 | 随结算包封口后只读 | `是`；动态节点 / 场景动态索引 |
| `变化后` | `工作线程承接接口` | 写入状态变化时 | 随结算包封口后只读 | `是`；动态节点 / 场景动态索引 |
| `变化原因` | `工作线程承接接口` + `结果回流、结算与重判` | 结算前整理变化因果时 | 随结算包封口后只读 | `条件持久化`；动态节点 |

#### 7.3.7 `结构_学习候选项`

| 字段 | 拥有者 | 首次写入时机 | 封口时机 / 可变性 | 是否持久化 / 去向 |
| --- | --- | --- | --- | --- |
| `锚点主键` | `学习候选沉淀接口` | 形成候选样本时写入 | 随结算包封口后只读 | `是`；因果候选 / 样本池 |
| `来源任务主键` | `学习候选沉淀接口` | 形成候选样本时写入 | 随结算包封口后只读 | `是`；因果候选 / 样本池 |
| `来源方法主键` | `学习候选沉淀接口` | 形成候选样本时写入 | 随结算包封口后只读 | `是`；因果候选 / 样本池 |
| `动作语义键` | `学习候选沉淀接口` | 形成候选样本时写入 | 随结算包封口后只读 | `条件持久化`；因果候选 |
| `置信度` | `学习候选沉淀接口` | 形成候选样本时写入 | 随结算包封口后只读 | `是`；因果候选 / 样本池 |
| `样本类别` | `学习候选沉淀接口` | 形成候选样本时写入 | 随结算包封口后只读 | `是`；样本池 / 黑名单 |
| `已验证` | `学习候选沉淀接口` | 初次写入时必须为 `false` | 随结算包封口后只读 | `是`；因果候选 / 样本池 |

### 7.4 `结构_线程轮次上下文包`

| 字段 | 拥有者 | 首次写入时机 | 封口时机 / 可变性 | 是否持久化 / 去向 |
| --- | --- | --- | --- | --- |
| `包头` | 主循环入口 + 运行包校验器 | 唤醒后创建包时写入 | 仲裁完成后封口 | `否`；仅调试审计快照 |
| `生命周期` | `生命周期与边界控制` | 边界检查完成时 | 仲裁完成后只读 | `条件持久化`；轮次摘要 |
| `运行模式` | `生命周期与边界控制` | 边界检查完成时 | 仲裁完成后只读 | `条件持久化`；轮次摘要 |
| `停止标志` | `生命周期与边界控制` | 边界检查完成时 | 仲裁完成后只读 | `条件持久化`；轮次摘要 |
| `死亡退出标志` | `生命周期与边界控制` | 边界检查完成时 | 仲裁完成后只读 | `条件持久化`；轮次摘要 |
| `安全接管标志` | `优先级与冲突仲裁` | 仲裁完成时 | 仲裁完成后只读 | `条件持久化`；仲裁摘要 |
| `当前场景主键` | `世界 / 状态刷新` | 本轮快照形成时 | 仲裁完成后只读 | `否`；默认不落库 |
| `关键存在主键集` | `世界 / 状态刷新` | 本轮快照形成时 | 仲裁完成后只读 | `否`；默认不落库 |
| `关键状态主键集` | `世界 / 状态刷新` | 本轮快照形成时 | 仲裁完成后只读 | `否`；默认不落库 |
| `关键关系主键集` | `世界 / 状态刷新` | 本轮快照形成时 | 仲裁完成后只读 | `否`；默认不落库 |
| `异常变化主键集` | `世界 / 状态刷新` | 本轮快照形成时 | 仲裁完成后只读 | `条件持久化`；仅异常审计摘要 |
| `服务差额` | `服务 / 安全差额评估` | 差额评估完成时 | 仲裁完成后只读 | `条件持久化`；差额摘要 |
| `物理安全差额` | `服务 / 安全差额评估` | 差额评估完成时 | 仲裁完成后只读 | `条件持久化`；差额摘要 |
| `风险安全差额` | `服务 / 安全差额评估` | 差额评估完成时 | 仲裁完成后只读 | `条件持久化`；差额摘要 |
| `根需求候选集` | `根需求生成` | 需求候选生成时 | 仲裁完成后只读 | `否`；默认只审计 |
| `主导需求` | `优先级与冲突仲裁` | 仲裁选主时 | 仲裁完成后只读 | `是`；轮次仲裁摘要 |
| `从属需求主键集` | `优先级与冲突仲裁` | 仲裁选主时 | 仲裁完成后只读 | `条件持久化`；轮次仲裁摘要 |
| `需求队列摘要集` | `优先级与冲突仲裁` | 需求队列生成完成时 | 仲裁完成后只读 | `条件持久化`；需求队列摘要 |
| `方法禁止项集` | `优先级与冲突仲裁` | 方法禁止项刷新完成时 | 仲裁完成后只读 | `条件持久化`；禁止项摘要 |
| `可运行任务主键集` | `优先级与冲突仲裁` | 全局队列刷新完成时 | 仲裁完成后只读 | `条件持久化`；队列摘要 |
| `等待任务主键集` | `优先级与冲突仲裁` | 全局队列刷新完成时 | 仲裁完成后只读 | `条件持久化`；队列摘要 |
| `回流任务主键集` | `优先级与冲突仲裁` | 全局队列刷新完成时 | 仲裁完成后只读 | `条件持久化`；队列摘要 |
| `收束停止任务主键集` | `优先级与冲突仲裁` | 全局队列刷新完成时 | 仲裁完成后只读 | `条件持久化`；队列摘要 |
| `推进等级` | `优先级与冲突仲裁` | 仲裁完成时 | 仲裁完成后只读 | `是`；轮次仲裁摘要 |
| `允许推进` | `优先级与冲突仲裁` | 仲裁完成时 | 仲裁完成后只读 | `是`；轮次仲裁摘要 |
| `要求收束` | `优先级与冲突仲裁` | 仲裁完成时 | 仲裁完成后只读 | `是`；轮次仲裁摘要 |
| `时间预算_ms` | 主循环与仲裁汇总 | 仲裁完成时 | 仲裁完成后只读 | `否`；默认不落库 |
| `资源预算` | 主循环与仲裁汇总 | 仲裁完成时 | 仲裁完成后只读 | `否`；默认不落库 |
| `并发上限` | 主循环与仲裁汇总 | 仲裁完成时 | 仲裁完成后只读 | `否`；默认不落库 |
| `最大授权等级` | 主循环与仲裁汇总 | 仲裁完成时 | 仲裁完成后只读 | `条件持久化`；授权上限摘要 |
| `必须记录动态` | 主循环与仲裁汇总 | 仲裁完成时 | 仲裁完成后只读 | `否`；运行态字段 |
| `必须生成结算包` | 主循环与仲裁汇总 | 仲裁完成时 | 仲裁完成后只读 | `否`；运行态字段 |
| `允许真实写回` | 主循环与仲裁汇总 | 仲裁完成时 | 仲裁完成后只读 | `条件持久化`；授权上限摘要 |
| `允许学习候选` | 主循环与仲裁汇总 | 仲裁完成时 | 仲裁完成后只读 | `否`；运行态字段 |

### 7.5 `结构_一步治理任务包`

| 字段 | 拥有者 | 首次写入时机 | 封口时机 / 可变性 | 是否持久化 / 去向 |
| --- | --- | --- | --- | --- |
| `包头` | `任务管理中轴` + 运行包校验器 | 从上下文包派生任务包时 | 方法阶段结束后封口 | `否`；仅调试审计快照 |
| `来源需求主键` | `任务管理中轴` | 生成一步治理任务包时 | 任务包封口后只读 | `是`；任务链摘要 |
| `来源任务主键` | `任务管理中轴` | 生成一步治理任务包时 | 任务包封口后只读 | `是`；任务链摘要 |
| `来源需求大类` | `任务管理中轴` | 生成一步治理任务包时 | 任务包封口后只读 | `条件持久化`；任务链摘要 |
| `本轮要缩小的差额说明` | `任务管理中轴` | 生成一步治理任务包时 | 任务包封口后只读 | `条件持久化`；任务链摘要 |
| `任务根节点主键` | `自我线程` | 创建任务入口时 | 任务包封口后只读 | `是`；任务入口摘要 |
| `任务入口主键` | `自我线程` | 创建任务入口时 | 任务包封口后只读 | `是`；任务入口摘要 |
| `任务根只读` | `自我线程` | 创建任务入口时 | 任务包封口后只读 | `条件持久化`；任务入口摘要 |
| `调度工作线程主键` | `自我线程` | 派发任务管理工作线程时 | 任务包封口后只读 | `条件持久化`；调度摘要 |
| `动作类型` | `任务管理中轴` | 生成一步治理任务包时 | 任务包封口后只读 | `是`；任务链 / 动态节点来源动作 |
| `任务语义键` | `任务管理中轴` | 生成一步治理任务包时 | 任务包封口后只读 | `是`；任务链摘要 |
| `目标主体主键` | `任务管理中轴` | 生成一步治理任务包时 | 任务包封口后只读 | `是`；任务链摘要 |
| `目标场景主键` | `任务管理中轴` | 生成一步治理任务包时 | 任务包封口后只读 | `条件持久化`；任务链摘要 |
| `完成条件` | `任务管理中轴` | 生成一步治理任务包时 | 任务包封口后只读 | `条件持久化`；任务链摘要 |
| `预期结果` | `任务管理中轴` | 生成一步治理任务包时 | 任务包封口后只读 | `条件持久化`；任务链摘要 |
| `授权约束` | `最小授权接口` | 授权通过时 | 任务包封口后只读 | `是`；任务授权摘要 |
| `候选方法集` | `任务管理中轴（含内部策略器）` | 方法候选比较时 | 任务包封口后只读 | `否`；默认只审计 |
| `选定方法` | `任务管理中轴（含内部策略器）` | 方法命中时；未命中则保持空 | 任务包封口后只读 | `条件持久化`；选定方法摘要 |
| `预期输入状态主键集` | `任务管理中轴（含内部策略器）` | 方法命中时 | 任务包封口后只读 | `条件持久化`；任务方法摘要 |
| `预期输出状态主键集` | `任务管理中轴（含内部策略器）` | 方法命中时 | 任务包封口后只读 | `条件持久化`；任务方法摘要 |
| `方法禁止项快照` | `任务管理中轴（含内部策略器）` | 方法候选过滤前写入 | 任务包封口后只读 | `条件持久化`；方法门控摘要 |
| `被过滤方法集` | `任务管理中轴（含内部策略器）` | 方法过滤完成时写入 | 任务包封口后只读 | `条件持久化`；方法门控摘要 |
| `方法过滤原因集` | `任务管理中轴（含内部策略器）` | 方法过滤完成时写入 | 任务包封口后只读 | `条件持久化`；方法门控摘要 |
| `要求记录动态` | `最小授权接口` + `任务管理中轴（含内部策略器）` | 方法阶段结束前汇总 | 任务包封口后只读 | `否`；运行态字段 |
| `要求记录副作用` | `最小授权接口` + `任务管理中轴（含内部策略器）` | 方法阶段结束前汇总 | 任务包封口后只读 | `否`；运行态字段 |
| `要求记录失败原因` | `最小授权接口` + `任务管理中轴（含内部策略器）` | 方法阶段结束前汇总 | 任务包封口后只读 | `否`；运行态字段 |
| `安全监测等级` | `最小授权接口` + `边界守卫接口` 规则面 | 方法阶段结束前汇总 | 任务包封口后只读 | `条件持久化`；任务监督摘要 |

### 7.6 `结构_回流结算包`

| 字段 | 拥有者 | 首次写入时机 | 封口时机 / 可变性 | 是否持久化 / 去向 |
| --- | --- | --- | --- | --- |
| `包头` | `结果回流、结算与重判` + 运行包校验器 | 回流入口创建结算包时 | 根层给出下一轮入口后封口 | `否`；仅调试审计快照 |
| `任务包主键` | `结果回流、结算与重判` | 创建结算包时写入 | 结算包封口后只读 | `是`；动态节点 / 因果候选追踪键 |
| `选定方法主键` | `工作线程承接接口` | 写执行事实段时写入 | 结算包封口后只读 | `是`；动态节点 / 因果候选追踪键 |
| `输入场景主键` | `工作线程承接接口` | 写执行事实段时写入 | 结算包封口后只读 | `是`；动态节点 |
| `输出场景主键` | `工作线程承接接口` | 写执行事实段时写入 | 结算包封口后只读 | `是`；动态节点 |
| `执行成功` | `工作线程承接接口` | 写执行事实段时写入 | 结算包封口后只读 | `是`；动态节点 |
| `被中断` | `工作线程承接接口` + `边界守卫接口` | 执行结束或被打断时写入 | 结算包封口后只读 | `是`；动态节点 / 安全事件摘要 |
| `已超时` | `工作线程承接接口` | 执行超时判定时写入 | 结算包封口后只读 | `是`；动态节点 |
| `错误码` | `工作线程承接接口` | 执行结束或失败时写入 | 结算包封口后只读 | `是`；动态节点 |
| `动态路径签名` | `工作线程承接接口` | 动态记录完成时写入 | 结算包封口后只读 | `是`；动态节点 |
| `状态变化集` | `工作线程承接接口` + `结果回流、结算与重判` | 汇总状态变化时写入 | 结算包封口后只读 | `是`；动态节点 / 场景动态索引 |
| `副作用状态主键集` | `工作线程承接接口` + `结果回流、结算与重判` | 汇总副作用时写入 | 结算包封口后只读 | `是`；动态节点 / 场景动态索引 |
| `触发降级` | `边界守卫接口` | 发生止损时写入 | 结算包封口后只读 | `是`；安全事件摘要 |
| `触发回滚` | `边界守卫接口` | 发生止损时写入 | 结算包封口后只读 | `是`；安全事件摘要 |
| `触发收束` | `边界守卫接口` | 发生止损时写入 | 结算包封口后只读 | `是`；安全事件摘要 |
| `止损原因` | `边界守卫接口` | 发生止损时写入 | 结算包封口后只读 | `条件持久化`；安全事件摘要 |
| `派生需求候选集` | `结果回流、结算与重判` | 汇总派生需求候选时写入 | 结算包封口后只读 | `条件持久化`；派生需求摘要 |
| `父任务主键` | `结果回流、结算与重判` | 路由子结果前写入 | 结算包封口后只读 | `条件持久化`；结果路由摘要 |
| `路由目标任务主键` | `结果回流、结算与重判` | 生成路由指令时写入 | 结算包封口后只读 | `条件持久化`；结果路由摘要 |
| `是否已路由父层任务` | `结果回流、结算与重判` | 路由完成时写入 | 结算包封口后只读 | `条件持久化`；结果路由摘要 |
| `路由原因` | `结果回流、结算与重判` | 生成路由指令时写入 | 结算包封口后只读 | `条件持久化`；结果路由摘要 |
| `服务差额前后` | `结果回流、结算与重判` | 双值结算时写入 | 结算包封口后只读 | `是`；结算摘要 |
| `物理安全差额前后` | `结果回流、结算与重判` | 双值结算时写入 | 结算包封口后只读 | `是`；结算摘要 |
| `风险安全差额前后` | `结果回流、结算与重判` | 双值结算时写入 | 结算包封口后只读 | `是`；结算摘要 |
| `结算结论` | `结果回流、结算与重判` | 双值结算完成时写入 | 结算包封口后只读 | `是`；结算摘要 |
| `是否有效推进` | `结果回流、结算与重判` | 双值结算完成时写入 | 结算包封口后只读 | `是`；结算摘要 |
| `方法禁止项更新建议集` | `结果回流、结算与重判` | 结算完成后生成禁止项更新建议时写入 | 结算包封口后只读 | `条件持久化`；禁止项更新摘要 |
| `学习候选集` | `学习候选沉淀接口` | 结算后生成候选时写入 | 结算包封口后只读 | `是`；因果候选 / 样本池 / 黑名单 |
| `建议进入待机` | `结果回流、结算与重判` | 给出下一轮建议时写入 | 结算包封口后只读 | `否`；运行态字段 |
| `建议进入收束` | `结果回流、结算与重判` | 给出下一轮建议时写入 | 结算包封口后只读 | `否`；运行态字段 |
| `建议停止` | `结果回流、结算与重判` | 给出下一轮建议时写入 | 结算包封口后只读 | `否`；运行态字段 |
| `建议死亡退出` | `结果回流、结算与重判` | 给出下一轮建议时写入 | 结算包封口后只读 | `否`；运行态字段 |
| `建议重试` | `结果回流、结算与重判` | 给出下一轮建议时写入 | 结算包封口后只读 | `否`；运行态字段 |


## 8. 持久化策略

### 8.1 默认规则

必须固定：

1. 默认不整包持久化 `上下文包 / 任务包 / 结算包` 正文。
2. 默认只在调试或审计开关下保留完整快照。
3. 正式持久化只允许写回有长期价值的：
   - 摘要
   - 追踪键
   - 结果
   - 证据

### 8.2 三包的选择性持久化口径

| 包 | 默认持久化策略 | 推荐写回去向 |
| --- | --- | --- |
| `线程轮次上下文包` | 默认不整包落库；仅持久化 `差额摘要 / 仲裁摘要 / 生命周期摘要 / 需求队列摘要 / 方法禁止项摘要 / 队列摘要` | 轮次摘要、仲裁摘要、调试审计快照 |
| `一步治理任务包` | 默认不整包落库；保留 `任务语义 / 任务入口摘要 / 授权摘要 / 选定方法摘要 / 方法门控摘要` | 任务链、任务节点摘要、授权记录 |
| `回流结算包` | 默认不整包落库；保留 `执行事实 / 状态变化 / 结果路由摘要 / 派生需求摘要 / 结算结果 / 方法禁止项更新建议 / 学习候选` | 动态节点、场景动态索引、因果候选、样本池 |

### 8.3 必须禁止

必须禁止：

- 把整包正文原样塞进现有世界节点
- 把 `候选方法集`、`根需求候选集` 这种高波动字段默认落成长期知识
- 把 `学习候选集` 直接写成 `已验证因果`


## 9. `自我线程运行包校验器.ixx`

### 9.1 最小接口

`0021` 当前建议把用户提出的 `6` 个最小能力拆到 `2` 个文件里。

其中前 `3` 个校验能力，放在：

`自我线程运行包校验器.ixx`

```cpp
export module 自我线程运行包校验器;

import 自我线程运行包;

export bool 校验上下文包(const 结构_线程轮次上下文包& 包);
export bool 校验任务包(const 结构_一步治理任务包& 包);
export bool 校验结算包(const 结构_回流结算包& 包);

export bool 封口上下文包(结构_线程轮次上下文包& 包, 时间戳 当前时间);
export bool 封口任务包(结构_一步治理任务包& 包, 时间戳 当前时间);
export bool 封口结算包(结构_回流结算包& 包, 时间戳 当前时间);
```

### 9.2 校验器职责

校验器至少要负责：

- 必填字段完整性
- `包状态` 是否允许当前写入
- 封口前后只读纪律
- `任务包 -> 结算包` 是否成对
- `学习候选.已验证` 初始值是否为 `false`

### 9.3 封口规则

必须固定：

1. 只有校验器可以把 `包状态` 从 `草稿` 改成 `已封口`。
2. 只有校验器可以写 `封口时间`。
3. `上下文包` 未经仲裁完成，不得封口。
4. `任务包` 未经方法阶段结束，不得封口；若方法未命中，可带 `选定方法为空` 的失败态封口，但不得缺少后续结算。
5. `结算包` 未完成双值结算与下一轮入口建议，不得封口。


## 10. `自我线程运行包映射器.ixx`

### 10.1 最小接口

用户提出的后 `3` 个最小能力，放在：

`自我线程运行包映射器.ixx`

```cpp
export module 自我线程运行包映射器;

import 自我线程运行包;

export void 从上下文包生成任务包(
    const 结构_线程轮次上下文包& 上下文包,
    结构_一步治理任务包& 任务包);

export void 从结算包写入动态节点(
    const 结构_一步治理任务包& 任务包,
    const 结构_回流结算包& 结算包);

export void 从结算包沉淀因果候选(
    const 结构_一步治理任务包& 任务包,
    const 结构_回流结算包& 结算包);

export void 从结算包生成方法禁止项更新(
    const 结构_回流结算包& 结算包);
```

### 10.2 映射纪律

必须固定：

1. 映射器只做 `包 -> 现有节点体系` 的写回，不创建新的世界模型。
2. `上下文包` 默认不直接映射为长期节点，只作为生成 `任务包` 的输入。
3. `任务包` 的长期价值主要挂回 `任务链 / 任务节点摘要 / 授权摘要 / 选定方法摘要`。
4. `结算包` 的长期价值主要写入 `动态节点 / 场景动态索引 / 实例因果候选 / 样本池 / 方法禁止项更新摘要`。
5. 每个任务包必须对应一个结算包；映射器不得允许“有任务、无账本”的暗河。

### 10.3 从结算包写入动态节点

`从结算包写入动态节点` 至少应完成以下映射：

| 来源 | 目标 |
| --- | --- |
| `任务包.动作类型` | `动态节点.来源动作` |
| `结算包.输入场景主键` | `动态节点.来源输入场景` |
| `结算包.输出场景主键` | `动态节点.来源输出场景` |
| `结算包.执行成功` | `动态节点.来源执行成功` |
| `结算包.错误码` | `动态节点.来源错误码` |
| `结算包.动态路径签名` | `动态节点.动态路径签名` |
| `结算包.状态变化集` | `场景动态索引 / 状态变化记录` |
| `结算包.副作用状态主键集` | `场景动态索引 / 副作用摘要` |

### 10.4 从结算包沉淀因果候选

`从结算包沉淀因果候选` 至少应完成以下映射：

| 来源 | 目标 |
| --- | --- |
| `学习候选.来源任务主键` | `因果实例候选.来源任务主键` |
| `学习候选.来源方法主键` | `因果实例候选.来源方法主键` |
| `学习候选.置信度` | `因果实例候选.置信度` |
| `学习候选.已验证 = false` | `因果实例候选.已验证` |
| `学习候选.样本类别` | `样本池 / 黑名单标签` |
| `任务包.任务语义键` + `结算包.执行成功 / 错误码 / 状态变化集` | `因果候选证据面` |

### 10.5 从结算包生成方法禁止项更新

`从结算包生成方法禁止项更新` 至少应完成以下映射：

| 来源 | 目标 |
| --- | --- |
| `方法禁止项更新建议.方法主键` | `方法禁止项集.方法主键` |
| `方法禁止项更新建议.触发条件集` | `方法禁止项集.触发条件集` |
| `方法禁止项更新建议.禁止原因` | `方法禁止项集.禁止原因` |
| `方法禁止项更新建议.证据来源` | `方法禁止项集.证据来源` |
| `方法禁止项更新建议.生效范围` | `方法禁止项集.生效范围` |
| `方法禁止项更新建议.解禁条件` | `方法禁止项集.解禁条件` |
| `方法禁止项更新建议.当前状态` | `方法禁止项集.当前状态` |

### 10.6 映射器禁止越权

映射器不得：

- 直接改 `服务值 / 安全值目标`
- 直接决定下一轮主导需求
- 把一次样本直接升格成已验证因果
- 绕过主循环直接续跑下一轮


## 11. 第一条落地金路径

当前最稳的首条金路径固定为：

`状态刷新 -> 差额评估 -> 生成服务需求 -> 任务管理中轴补齐策略摘要 -> 人工确认 / 模拟执行 -> 回流结算 -> 学习候选沉淀`

这一条链当前只先开放以下动作类型：

- `解释`
- `预警`
- `建议`

这一条链当前暂不开放：

- `真实执行`
- `自动回滚`
- `自动联动改写`

### 11.1 关键闸门

必须固定：

1. `上下文包` 封口后，任务管理只能读，不能改根裁决。
2. `任务包` 封口后，执行层只能按包执行，不能反向扩权。
3. 未生成 `结算包`，本轮不算结束。
4. 未完成结算，学习不得落库。
5. 学习写回只能产生 `候选`，不能直接产生 `已验证`。


## 12. 当前阶段最小验收口径

当前若要承认 `0021` 已最小成立，至少必须满足：

1. `自我线程运行包.ixx` 的公共枚举、统一包头和三包结构已固定。
2. 三包字段的 `拥有者 / 写入时机 / 封口时机 / 是否持久化` 已被写成硬规则。
3. `自我线程运行包校验器.ixx` 的最小封口和完整性接口已固定。
4. `自我线程运行包映射器.ixx` 的最小 `包 -> 节点` 接口已固定。
5. 默认不整包落库、只做选择性映射的持久化纪律已固定。
6. 第一条落地金路径已经明确锁在 `解释 / 预警 / 建议 -> 回流结算 -> 学习候选沉淀`。

最终收口：

`到 0021 为止，自我线程已经不只是“有状态机、有三包”，而是已经拥有可以直接落到 C++ 的结构合同、封口合同和映射合同。`
