# 6240 子规范 已确认存在体素融合与分解

编号：6240
日期：20260701
状态：正式子规范
权威等级：正式规范

## 1. 定位

本规范是 `观察外设与视觉` 主题下由旧规范修订迁回的正式子规范。它只承接当前仍有效的规则，不恢复旧规范中的历史口径、详细设计措辞或已经被根规范吸收的重复条款。

历史来源：
- `规范/旧规范/已确认存在体素融合与分解规范20260630.md`

上级规范：
- `规范/1140_根规范_存在节点主信息类_20260701.md`
- `规范/1150_根规范_场景节点主信息类_20260701.md`
- `规范/0050_项目通用结构与禁止性规则总纲_20260701.md`
- `规范/0300_规范冲突与前后矛盾清单_20260701.md`

## 2. 上级约束

1. 本规范服从 `规范/规范目录.md`、0050 总纲、0300 冲突清单和对应根规范。
2. 历史来源中与当前根规范冲突的表述不进入当前权威口径。
3. 旧名称、旧结构和旧阶段口径必须按 `规范/0300_规范冲突与前后矛盾清单_20260701.md` 的 C001-C008 收束，不得在本规范中恢复历史并列结构、旧字段身份或旧执行来源。
4. 任务筹办 / 任务执行只作为任务管理生命周期普通函数；动作来源、方法注册和方法选择必须服从 0050 总纲、3200 任务根规范、3300 方法根规范和 1170 动态根规范。
5. 文本、日志、注释、临时 JSON、自由字段和显示摘要不得承载正式机器逻辑或业务状态。

## 3. 当前有效规则

以下条款由历史来源按当前根规范修订抽取；若条款与第 2 章或上级规范冲突，以上级规范和第 2 章为准。

1. 规范/禁止性规范20260611.md
2. 当前已确认观察存在已有第一版粗体素特征时，后续观察应继续提升体素精度、颜色和内部结构，而不是停留在一层 AABB 粗长方体。
3. 6. 体素加工线程只能生成候选结果，不能直接写正式世界树事实。
4. 不得作为触发源：
5. 已消费观察体素特征值必须记录消费状态或来源版本，避免融合写回后递归触发自己。
6. 合法输入不等于融合和分解结果直接成为事实。融合和分解只能先生成候选结果包，正式体素更新和内部世界子存在创建必须经提交入口裁决。
7. 包围盒外边界层必须为空：
8. 存在真实尺寸的最长方向应贴近有效区边界；其他方向按实际尺寸比例缩放，不强制撑满。
9. 同一连通体在自身局部有效包围盒中必须压紧：
10. 局部包围盒不能存在整层空白边界。
11. 但不得要求局部包围盒六个面全部填满。真实形状造成的凹陷、孔洞、斜面、弯曲和非立方边界允许保留。
12. 局部边界面不满 -> 正常，不能因此补成立方体。
13. 融合必须存在融合点。满足下列条件之一才允许融合：
14. 不得写入父体素模型；
15. 不得直接扩张父存在尺寸；
16. 不得创建子存在；
17. 只能挂起为待融合观察候选或结构化缺口。
18. 融合结果不得把补全深度或推测空间直接提升为真实占据。补全深度只能降低不完整程度，不能提高真实性。
19. 彩色分解后，必须用深度轮廓补充空间占据信息：
20. 彩色轮廓不能单独证明子存在。红色花纹、阴影、反光、表面纹理或局部颜色块可形成子轮廓候选，但没有独立空间占据或深度支撑时，不得写成内部世界子存在。
21. 子轮廓成为子存在至少需要：
22. 覆盖只表示显示优先级，不表示父存在颜色事实被删除。
23. 父颜色、子颜色和渲染覆盖关系必须分开表达，不能用显示覆盖反向改写父存在基础颜色事实。
24. 父存在必须保留自己的完整正立方体体素模型。
25. 子存在确认后，也必须拥有自己的正立方体局部体素模型。子存在模型初始来源于父体素模型中的子区域，但不得复制父存在完整体素模型。
26. 子存在后续可独立融合、分解、跟踪和递归生成自己的内部世界。没有子体素模型的子结构只能称为颜色区域、表面特征或内部结构候选，不得宣称为可递归存在。
27. 允许新增独立体素加工工作线程，用于执行耗时计算：
28. 体素加工线程不得直接写正式事实：
29. 不得直接修改存在三维体素特征。
30. 不得直接创建内部世界子存在。
31. 不得直接生成方法动作动态。
32. 不得作为动作来源。
33. 不得绕过提交入口。
34. 线程输出只能是结构化候选结果包。正式写入必须回到提交入口或对应领域类裁决。
35. 提交时必须检查基准版本：
36. 提交时父体素版本已变为 V2 -> 不得直接覆盖，必须重算、合并或挂起
37. | 体素加工结果包 | 输出结果场景或运行期候选结构 | I64、VecU、VecIU64、指针句柄 | 体素加工线程 | 提交入口 / 对应领域类 | 版本闸门、融合点、边界状态齐全 |
38. 体素加工不得由外设原始帧、旧当前特征值、融合写回、控制面板刷新或场景体素快照触发。触发源只能是已确认存在新增的未消费观察体素特征值。
39. 新观察体素候选必须与现有体素存在边界相同、边界相邻或可投影重叠的融合点。没有融合点时，只能形成待融合候选或缺口，不得写入父体素模型。
40. 父存在、子存在和局部体素候选都必须使用正立方体包围盒。包围盒最外层出现占据体素时，候选判定为越界、截断或不完整，不得作为稳定模型提交。
41. 正立方体包围盒中的有效占据区为 `1..N-2`，完整存在最长方向最大占据格数为 `N-2`，不是 `N-1`。存在真实尺寸最长方向应贴近有效区边界，其他方向按尺寸比例缩放，不得把所有存在强行补成立方体。
42. 彩色轮廓只能生成颜色分区和子轮廓候选。子轮廓要进入内部世界子存在候选，必须由深度轮廓或空间占据证据补充，不得只凭颜色成为子存在。
43. 父存在保留完整体素模型；子存在确认后拥有自己的局部正立方体体素模型。子模型来源于父体素子区域和观察证据，不得复制父完整模型冒充子存在体素。
44. 父基础颜色和子存在颜色必须分开记录。渲染时子体素颜色可覆盖父颜色显示，但不得用显示覆盖反向改写父颜色事实。
45. 体素加工线程可以融合、分解和统计，但不得直接写世界树事实、创建子存在或生成动作动态。正式入账必须经过提交入口和版本闸门。
46. 允许作为规范和工程守卫证据：
47. 不得作为业务闭合证据：
48. 业务闭合必须等待真实观察材料、自然触发、提交入口裁决、父子体素特征和内部世界子存在投影回证据形成同一窗口证据。
49. 在未完成真实业务证据前，不得宣称：

## 4. 使用边界

1. 本规范用于计划、详细设计、代码修改和验收前的规则回读。
2. 本规范不能作为代码实现完成、业务闭环完成、自我苏醒完成或初步成熟完成的证据。
3. 需要展开历史论证时，只能把 `规范/旧规范/` 中的原文件作为追溯材料，不得让旧文件反向覆盖本规范。

## 修订记录

| 日期 | 版本 | 修订内容 |
| --- | --- | --- |
| 20260701 | v1.0 | 由历史来源修订迁回为正式子规范，接入当前规范目录。 |
