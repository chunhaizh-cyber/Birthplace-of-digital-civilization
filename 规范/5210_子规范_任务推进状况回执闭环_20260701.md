# 5210 子规范 任务推进状况回执闭环

编号：5210
日期：20260701
状态：正式子规范
权威等级：正式规范

## 1. 定位

本规范是 `需求任务方法` 主题下由旧规范修订迁回的正式子规范。它只承接当前仍有效的规则，不恢复旧规范中的历史口径、详细设计措辞或已经被根规范吸收的重复条款。

历史来源：
- `规范/旧规范/任务推进状况回执闭环规范20260508.md`

上级规范：
- `规范/3200_根规范_任务主信息类_20260701.md`
- `规范/0050_项目通用结构与禁止性规则总纲_20260701.md`
- `规范/0050_项目通用结构与禁止性规则总纲_20260701.md`
- `规范/0300_规范冲突与前后矛盾清单_20260701.md`

## 2. 上级约束

1. 本规范服从 `规范/规范目录.md`、0050 总纲、0300 冲突清单和对应根规范。
2. 历史来源中与当前根规范冲突的表述不进入当前权威口径。
3. 旧名称、旧结构和旧阶段口径必须按 `规范/0300_规范冲突与前后矛盾清单_20260701.md` 的 C001-C008 收束，不得在本规范中恢复历史并列结构、旧字段身份或旧执行来源。
4. 任务筹办 / 任务执行只作为任务管理生命周期普通函数；动作来源、方法注册和方法选择必须服从 0050 总纲、3200 任务根规范、3300 方法根规范和 1170 动态根规范。
5. 文本、日志、注释、临时 JSON、自由字段和显示摘要不得承载正式机器逻辑或业务状态。

## 3. 当前有效规则

以下条款由历史来源按当前根规范修订抽取；若条款与第 2 章或上级规范冲突，以上级规范和第 2 章为准。

1. 这些单元只能产出状况、事实、状态变化、条件结果对样本和执行回执。它们不得直接替父需求改树、结账、收束或把父任务标记为最终完成。
2. 必须固定以下边界：
3. 任务完成只能由当前任务目标结果符合确认。父需求结算只能由自我线程在吸收任务目标结果后进行。
4. 这两条不能混用：
5. -> 当前任务有推进，但目标未必达成，不能直接进入完成回执。
6. 如果任务目标是固定值目标，则必须以任务目标值为准；例如目标值为 6 时，结果值 5 即使高于之前值，也只能算推进，不能算完成。
7. 若需要子需求 / 子任务，交给自我线程改需求树和任务树
8. 任务筹办普通函数、任务执行普通函数、学习函数和练习函数按同一边界执行：可以产生事实和回执，不能替自我线程改树、结账或完成父需求。其中学习函数/练习函数若作为方法运行，仍按方法动作动态硬规则落账；筹办/执行普通函数不作为方法动作来源。
9. 2. 判断是否需要等待子任务。
10. 3. 判断是否需要父任务重筹办。
11. 4. 判断是否需要把缺口上交自我线程生成子需求。
12. 7. 禁止结果和风险结果治理。
13. 筹办必须返回结构化状况，不能只返回“找到 / 没找到”。
14. | 方法命中但缺方法动作 | 方法有结果声明但不能执行 | 自我线程 | 创建或复用方法动作状态子需求 |
15. | 参数不可配 | 实际场景缺少方法所需参数特征，当前任务输入未闭合 | 自我线程或任务管理线程 | 创建或复用参数配对状态 / 任务参数绑定状态子需求，或标记等待 |
16. | 方法运行账不足 | 方法可执行性需要更多运行样本支撑 | 自我线程 | 创建或复用方法运行账完整性子需求，由后续筹办按结果能力选择承接方法 |
17. 方法查找未命中不是创建子需求的入口。筹办必须先让“应该存在的方法”出生：创建或复用一个待学习方法头，把父任务目标写入方法头的结果目标，然后父任务待重筹办。
18. `方法查找未命中` 是筹办阶段的内部事实；对外稳定回执优先是：
19. 筹办返回的可执行性缺口必须转换成“让目标方法头可执行”的子需求，不得复制父需求原目标。
20. 对外需求任务也必须先有方法头承接；允许缺的是自我当前可执行的方法动作或动作入口。新建方法头本轮只返回“已建立方法头待重入”；复用既有方法头后若仍缺动作、入口或条件结果对，再按实际缺口进入等待、沟通、外部支撑或学习补齐链。不得把“缺可执行方法动作”写成任务执行失败，也不得伪装成自我已经能读取服务对象内在状态。
21. 结构化回执必须携带缺口特征类型、缺口目标宿主和当前方法。这里的缺口目标宿主是该缺口特征依附的存在或上下文，不表示要整体改变该存在。方法结构、运行账和稳定性缺口的 `缺口目标宿主` 应指向当前方法头或其方法虚拟存在；参数不可配缺口应指向当前任务输入包 / 参数绑定上下文。自我线程创建子需求时必须使用：
22. 不得沿用父需求的业务目标宿主和业务目标特征类型。
23. 新建待学习目标方法头后，除写入 `方法是否可用 = 0` 外，还必须补写：
24. 方法允许自动召回 = 0
25. 方法允许默认执行 = 0
26. 执行阶段也必须返回结构化状况，不得只用“成功 / 失败”吞掉细节。
27. | 动作执行成功无可比较结果 | 有动作事实但不能验证目标 | 否 | 归并事实，父任务重筹办或继续检查 |
28. | 出现需要附带结果 | 非当前目标，但命中其他有效需求 | 不计入当前目标统计 | 路由到对应需求账 |
29. | 出现不需要附带结果 | 非当前目标、非禁止 | 不计入当前目标统计 | 只沉淀事实 |
30. | 出现禁止结果 | 触犯禁止项或风险约束 | 单独记风险账 | 自我线程风险治理 |
31. 只有在方法可执行、动作函数执行成功、产生可比较结果之后，才允许根据目标结果是否符合当前任务目标更新结果符合 / 不符合统计。
32. 没有可比较目标结果时，即使动作函数执行成功，也只能返回：
33. 不得返回任务完成，不得结算父需求，不得更新目标结果符合 / 不符合账。
34. 执行回执中的推进判断必须同步为任务特征：
35. 这些特征属于任务虚拟存在的任务账，不能只保存在运行存在或工作线程临时结构中。
36. 子任务只解决一个阻塞点，不能证明父需求目标已经达成。父任务完成只能由父需求自身目标满足确认。
37. 父任务是否等待不能看：
38. 必须看：
39. 历史子需求可以留在树上，但不得阻止父任务重新筹办并回到自身目标。
40. 旧返回码不得保留作兼容输入。任务推进信息必须由统一结构化回执承载；若历史路径仍只能提供返回码，该路径视为未闭环，必须按 007《禁止性规范20260611》修订。
41. 参数不可配 = 17,
42. 出现需要附带结果 = 34,
43. 出现不需要附带结果 = 35,
44. 出现禁止结果 = 36,
45. `父需求已满足` 是历史枚举名，规范语义是“父需求当前满足判定成立”。实现不得把需求截止时间、历史满足事件或控制面板显示字段当成该状况的唯一依据。
46. 可解析引用<需求节点类> 对应需求{};
47. std::vector<可解析引用<状态节点类>> 禁止结果状态集{};
48. std::vector<可解析引用<状态节点类>> 需要附带结果状态集{};
49. bool 需要自我线程 = false;
50. bool 父任务需要重筹办 = false;
51. bool 需要创建或复用子需求 = false;
52. 可执行状态 = 不可执行
53. 方法结构补齐任务必须遵守：
54. 当前执行方法必须是能产出该缺口目标的方法，可由学习 / 练习 / 尝试学习承接，但不能硬性指定；
55. 不得把被补方法本身绑定成当前执行方法。
56. 方法头存在但不可执行时，缺口状况必须转换成可执行性子需求目标：
57. | 参数不可配 | 参数配对状态 / 任务参数绑定状态 |
58. 子需求目标宿主应指向任务、方法或方法虚拟存在，不得复制父需求目标。实际场景缺参不落到“方法是否可用”，而是生成参数配对状态 / 任务参数绑定状态一类的输入闭合目标。
59. 同一方法根下不得重复创建相同目标结果能力的待学习方法头。去重口径仍按结果目标：
60. 筹办负责发现能不能执行；
61. 每个子任务完成后，父任务必须重新筹办。
62. 方法头不可执行 -> 学习子任务补方法；

## 4. 使用边界

1. 本规范用于计划、详细设计、代码修改和验收前的规则回读。
2. 本规范不能作为代码实现完成、业务闭环完成、自我苏醒完成或初步成熟完成的证据。
3. 需要展开历史论证时，只能把 `规范/旧规范/` 中的原文件作为追溯材料，不得让旧文件反向覆盖本规范。

## 修订记录

| 日期 | 版本 | 修订内容 |
| --- | --- | --- |
| 20260701 | v1.0 | 由历史来源修订迁回为正式子规范，接入当前规范目录。 |
