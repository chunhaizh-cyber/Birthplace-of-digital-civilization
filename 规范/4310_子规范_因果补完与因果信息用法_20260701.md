# 4310 子规范 因果补完与因果信息用法

编号：4310
日期：20260701
状态：正式子规范
权威等级：正式规范

## 1. 定位

本规范是 `动态与因果` 主题下由旧规范修订迁回的正式子规范。它只承接当前仍有效的规则，不恢复旧规范中的历史口径、详细设计措辞或已经被根规范吸收的重复条款。

历史来源：
- `规范/旧规范/因果补完流程规范20260516.md`
- `规范/旧规范/因果信息用途与用法规范20260516.md`
- `规范/旧规范/因果信息在学习中的作用规范20260516.md`

上级规范：
- `规范/1190_根规范_因果主信息类_20260701.md`
- `规范/3300_根规范_方法主信息类_20260701.md`
- `规范/0050_项目通用结构与禁止性规则总纲_20260701.md`
- `规范/0300_规范冲突与前后矛盾清单_20260701.md`

## 2. 上级约束

1. 本规范服从 `规范/规范目录.md`、0050 总纲、0300 冲突清单和对应根规范。
2. 历史来源中与当前根规范冲突的表述不进入当前权威口径。
3. 旧名称、旧结构和旧阶段口径必须按 `规范/0300_规范冲突与前后矛盾清单_20260701.md` 的 C001-C008 收束，不得在本规范中恢复历史并列结构、旧字段身份或旧执行来源。
4. 任务筹办 / 任务执行只作为任务管理生命周期普通函数；动作来源、方法注册和方法选择必须服从 0050 总纲、3200 任务根规范、3300 方法根规范和 1170 动态根规范。
5. 文本、日志、注释、临时 JSON、自由字段和显示摘要不得承载正式机器逻辑或业务状态。

## 3. 当前有效规则

以下条款由历史来源按当前根规范修订抽取；若条款与第 2 章或上级规范冲突，以上级规范和第 2 章为准。

1. 不得直接作为可执行方法候选。
2. 必须有动作来源、状态端点和证据动态。
3. 动作致变因果中的可执行完整因果模板必须同时具备：
4. 这里的“动作”不是方法 ID，也不是自由文本动作名，而是由动态信息归纳出的动作信息。方法只能作为动作信息的生成器、识别器或确认器，记录在来源方法字段中。
5. 则只能建立状态变化因果。状态变化因果在证据不足、尚未稳定或尚未验证时也称为状态变化线索：
6. 它是因果信息的一类，但不是可执行完整因果模板，不能直接进入任务筹办候选方法。
7. 其中“动作”必须区分主动动作和被动动作：
8. 动作位必须能落回本能方法 / 方法动作入口。
9. 动作位只能表达被动触发结构。
10. 不得被任务筹办召回为可调用候选方法。
11. 系统只能在该动态的初始状态与结果状态之间查找候选材料：
12. 这些材料稳定伴随目标动态时，只能先形成候选因果或待验证动作致变因果。经过验证、复现、反例过滤或明确动作来源可归因后，才允许沉淀为已验证因果信息。
13. 本规范中“能建立动作致变因果，就一次性建动作致变因果”的含义是：条件状态、动作信息、结果状态三项骨架可以一次性补齐并记录为动作致变因果 / 可执行完整因果模板；它不表示单次动态样本已经证明该模板稳定成立。单次动态可以追加为证据样本或形成状态变化因果 / 状态变化线索，但不得单独把候选因果标记为已验证。
14. 动作致变因果的输入动态必须在动态后处理前已经具备来源方法、来源动作、动作语义键、动态类型、初始状态、结果状态、输入场景、输出场景、同源变化证据组或可复算同源组，以及证据引用。后处理之后补写这些字段，不得改变既有状态变化因果的判定结果；应补建动作致变动态，并让动作致变因果引用既有状态变化因果。
15. 候选因果、待验证动作致变因果、状态变化因果 / 状态变化线索不得直接作为安全值 / 服务值结算依据。结算只能引用已经落账的任务、方法、需求和世界事实动态，并按结算规范读取可验证证据。
16. 动作需要从场景存在的动态序列中识别 / 推断；
17. 内部治理动态的主体必须是自我内部世界中的治理虚拟存在或因果对象，例如：
18. 禁止把“任务筹办流程”“执行方法流程”“消息处理流程”本身作为动态主体。正确写法是：
19. 自我行动因果处在中间类型：自我动作明确，但结果发生在外部场景时，外部结果仍必须来自观察材料、场景裁决和世界事实提交，不能只凭内部动作记录假定外部结果已经成立。
20. 只能作为状态变化线索、学习线索、观察线索或证据补齐线索。
21. 状态变化因果与动作致变因果应通过同源变化证据组互相印证，但不得重复结算、重复满足需求或重复改变安全 / 服务账务值。
22. 同源变化证据组是双账去重的强制绑定关系。它可以是显式证据组字段 / 句柄，也可以在未新增结构前由既有动态、状态端点时间和证据链复算得到。凡同一次特征变化同时生成状态迁移动态、动作致变动态、状态变化因果和动作致变因果，必须能回解到同一个同源变化证据组或可复算同源组。
23. 允许按采样误差设置 ε，但不得把“初始端点一致 / 结果端点一致 / 时间点接近”作为同源核心。一个完整动态必须由两个状态构成，其时间段来自这两个状态的发生时间；方法执行开始 / 结束时间、动态主信息时间缓存、来源动作、来源方法、输入 / 输出场景、任务、观察帧组、轨迹、坐标系版本、来源低层动态和因果证据动态样本只作为辅助校验和排他性证据。
24. 3. 因果稳定度可以分别更新，但账务值不得重复入账。
25. 任务筹办消费因果时按以下优先级：
26. 1. 优先使用动作致变因果，因为它能映射到可执行方法。
27. 2. 状态变化因果只能作为变化模式线索，用于学习、练习、证据补齐或观察补齐。
28. 因果信息不得按“每次变化直接生成一个确定因果”的方式落定。默认流程必须改为：
29. 经过复现、反例过滤或明确动作来源可归因后，才允许沉淀为已验证因果信息。
30. 同一个主果变化可以有多个并列因果分支。不得把同一主果变化下所有样本直接求总交集；总交集容易只剩背景条件，不能代表真实原因。正式规则是：
31. 当某个存在的某个特征类型生成新状态信息时，系统应查找该特征的相邻前状态，并形成主果变化锚点。
32. 主果变化锚点只表示“本轮要解释哪个变化”，不得直接表示原因、动作能力、任务完成或需求满足。
33. 系统应在主果变化的时间窗口内收集候选条件项。候选条件项不得立即视为已验证原因，只能作为本次因果样本的条件集合。
34. 候选条件项必须按时序和来源分层：
35. 在主果变化前已经成立的状态。优先作为条件候选。
36. 在主果变化期间发生的其它特征变化。只能作为伴随原因、共同结果或分支候选。
37. 在主果变化之后才出现的状态。不得当作条件，只能作为可能后果或副作用候选。
38. 时序未明确前只能弱候选，不得进入硬交集核心。
39. 候选条件项进入交集比较前必须模板化：
40. 不得用具体节点地址、日志文本、自由字符串或临时 JSON 直接做因果交集。模板化后的条件项必须仍可追溯到原始状态节点、动态节点、动作动态、观察事实或治理事实。
41. 因果样本是证据样本，不是已验证因果。单个样本不得直接标记因果已验证，不得直接用于安全 / 服务结算。
42. 新增因果样本加入同一主果变化的因果族后，必须与已有分支比较：
43. 多次正样本共同具备的条件。只能作为必要条件候选，仍需反例检查。
44. 只在部分样本出现，但该部分样本内部稳定。应分裂或维护独立因果分支。
45. 只出现一次、无复现、反例多或时序不符。不得删除证据，只能降权、标记被削弱、被覆盖或淘汰。
46. 反例比较是强制步骤。系统不仅要比较“发生主果变化时有哪些条件”，还要比较“这些条件出现时主果变化是否没有发生”。反样本多的条件只能作为背景条件或弱候选，不得进入稳定核心条件。
47. 只能作为线索。
48. 动作字段必须是动作信息，不得只写方法 ID 或流程名。
49. 动作致变因果 / 可执行完整因果模板不得脱离动态证据或出生期初始假设成为手写结论。它必须满足以下二选一：
50. 因果信息来源只有一个：自我的认知过程。出生期初始模板、学习样本和实际使用证据只是认知过程中的不同阶段材料，不作为因果主信息来源类型。出生期初始因果模板只承载被动动作触发结构。主动动作能力结构从方法系统取得，本能方法的因果信息只能通过学习及实际使用生成；主动动作动态只作为因果证据动态样本、学习样本和运行证据。
51. 只能由主果比较模板及其二次特征比较对象、比较量符号在解释层临时命名。
52. 不得新增或保留为主信息事实字段：
53. 如果接口需要输出“因果置信度 / 置信分 / 稳定度”，必须由基础计数临时计算，作为解释结果返回，不得写回因果主信息事实字段。
54. 应直接建立动作致变因果 / 可执行完整因果模板：
55. 只能建立状态变化因果 / 状态变化线索：
56. 不得直接建立动作致变因果 / 可执行完整因果模板。
57. 原状态变化线索应标记为：
58. 不得继续使用容易误导为两套因果结构的命名，例如：
59. 1. 缺初始状态或结果状态时，返回状态端点缺口，不得建立动作致变因果 / 可执行完整因果模板。
60. 2. 缺来源方法、来源动作或动作语义键时，只能建立状态变化因果 / 状态变化线索或待补动作线索。
61. 3. 缺目标特征的果状态迁移模板时，必须返回结构化缺口或创建候选状态迁移模板，不得静默停止。
62. 4. 动作致变因果 / 可执行完整因果模板创建后仍处于候选或待验证阶段，必须通过证据样本、反例统计或明确归因规则更新验证状态。
63. 5. 后补动作来源时，不得改写既有状态变化因果；必须创建动作致变动态和动作致变因果，并引用既有状态变化因果。
64. 6. 来源方法、来源动作、动作语义键、动态类型、初始状态、结果状态和同源变化证据组属于因果判定字段；动态后处理开始后不得补写这些字段来改变因果类型。
65. 7. 后处理后才发现动作来源时，必须补建动作致变动态和动作致变因果，并绑定原同源变化证据组、回解原可复算同源组或明确创建新的同源变化证据组，不能改写原状态变化因果。
66. 后续发现动作时，不得随意补到旧线索上。状态迁移动态与动作致变动态作为同一次事实的两种记录时，必须匹配：
67. 动作记录若只描述方法调用、任务步骤或治理函数执行状态，或缺少落到被改变存在 E 的 FT-X 上的初始状态 / 结果状态，则只能作为动作过程证据，不能补到该状态变化因果上生成动作致变因果。
68. 匹配不确定时，只能生成：
69. 不得直接升格为稳定因果。
70. `主果比较模板` 是果侧比较关系的主引用。生成、消除、增强、减弱、抑制、转换等词只能由显示层、查询层或二次特征解释器读取 `主果比较模板` 的比较对象和值符号后临时命名，不写入因果主信息，也不得作为底层推理条件。
71. 5. 状态变化因果 / 状态变化线索不得伪装为动作致变因果 / 可执行完整因果模板。
72. 6. 状态变化因果和动作致变因果通过同源变化证据组互证，但不得重复结算、重复满足需求或重复改变安全 / 服务账务值。
73. 不得重复结算、重复满足需求或重复改变安全 / 服务账务值。
74. 不能，就先存状态变化因果 / 状态变化线索，等动作来源具备后再补动作致变因果。
75. 其中 `A` 是由动态信息归纳出的动作信息，不是方法 ID、流程名、自由文本动作摘要，也不是“变化”这个动态根抽象名。方法只作为动作信息的生成、识别或确认来源；任务筹办可以根据主动动作致变因果反查可执行方法，但不得把“方法 M”直接当作因果动作字段。
76. 按 003《基础抽象命名规则规范》，因果中的主果动态和动作信息归纳必须能回到场景抽象、存在抽象、特征抽象、变化抽象、初始状态、结果状态和时间关系。`A` 只能压缩命名一类可复现动态模式；匹配和泛化时不得只凭 `A`、`变化` 或文本签名覆盖实例动态。
77. 主动动作致变因果可直接用于筹办方法；状态变化因果只能作为线索，不能直接当方法。被动动作致变因果有动作语义，但不可被任务筹办召回为普通可调用方法。
78. 但任务筹办消费时仍只能把已满足条件、已通过稳定性要求的主动动作致变因果作为方法候选；
79. 单次内部治理因果样本只能作为学习、补条件、复验或稳定度更新材料。
80. 这些接口是因果消费契约，不表示当前代码已经完整实现。实现时必须使用项目结构中的存在、特征、状态、动态、因果节点、主果变化锚点、因果样本、因果分支和同源变化证据组句柄，不得用自由文本 ID、日志或临时 JSON 承载业务判断。
81. 凡同一次特征变化同时生成状态迁移动态、动作致变动态、状态变化因果和动作致变因果，必须绑定同一个同源变化证据组。结算、需求满足和账务值变化必须按同源变化证据组去重；因果稳定度可以分别更新，但不得重复入账。
82. 不得直接生成可执行方法候选。
83. 只能生成补条件、补来源、补动作或反例验证需求。
84. 不得直接作为动作能力。
85. 安全目标投影、服务目标投影、学习收益投影、自维护投影等，都是从世界树根链上的状态转换因果信息按目标语义和来源需求方向投影出来的只读视图。第一版工程实现可以先返回普通结构体投影，但语义上必须保留：
86. 任务 / 需求 / 方法只能引用这些世界树因果节点，不得复制成另一套权威因果账。
87. 本能方法的因果信息不进入出生期初始因果模板组，只能通过学习及实际使用生成。
88. 自我被动知道什么因会自动带来什么果，必须从初始因果模板或后天证据增强后的因果信息组获取。
89. 2. 因 / 条件 / 果 可解析为状态、特征或二次特征；不得把方法能力伪装成出生期初始因果模板。
90. 5. 若存在本能方法因果模板，必须由学习或实际使用生成，不得登记为出生期初始因果模板。
91. 也不得作为底层因果匹配、任务判断或结算依据。
92. 若本文后续出现“因果置信度”“置信分”“稳定度”“成功率”等说法，均指由基础计数和查询参数计算出来的解释结果，不表示因果主信息中存在对应持久字段。
93. | 用途 | 状态变化因果 | 动作致变因果 | 是否允许生成方法候选 | 是否允许结算 |
94. | 预测动作后果 | 不可单独使用 | 必须使用 | 否，只做执行前评估 | 否 |
95. | 反推可用动作 | 只能做线索 | 主动动作致变因果可直接候选 | 是，仅限主动动作致变因果 | 否 |
96. | 执行前审查 | 可辅助 | 必须使用 | 否，审查既有候选 | 否 |
97. | 结算 | 不能单独结算 | 与状态结果同源后可作为归因证据 | 否 | 需满足双钥匙和副作用审查 |
98. 对应被动果就自动出现。
99. 这里的自动出现由生命周期提交链、后处理链、结算链或对应领域提交链完成，不由普通任务筹办选择，也不由自我把 `被动动作` 当成可调用动作来执行。
100. 该动态必须能回解：
101. 后续若需要同类结果，需要哪些上游事实、条件状态、任务状态或证据材料。
102. 知道需要什么；
103. 被动动作不得被反推成可调用动作候选。
104. 因果反推读到 被动动作 时，只能生成上游事实需求、条件需求或证据补齐需求。
105. 被动因已具备但被动果缺失时，应生成被动提交链 / 后处理链 / 结算链缺口，而不是生成“调用被动动作”的普通任务。
106. 执行前评估，不让任务只看“能不能做”，还要看“做了会发生什么”。
107. <- 这些动作需要哪些条件 C？
108. 可干预因果节点对应的需求候选。
109. 因果信息只能生成需求候选。
110. 正式需求入树必须由自我线程调用需求域方法完成。
111. 因果链生成需求候选必须有事实起点，例如：
112. 候选需求必须能归约为：
113. 因果链不得直接生成任务。
114. 任务只能由正式需求被任务管理链承接后创建。
115. 诊断摘要、等待原因、说明文本、日志字符串只能辅助人工排错，
116. 不得作为因果语义、需求目标、需求去重、任务状态、价值结算或归因判断依据。
117. 是否允许执行
118. 安全因果不应强绑为安全特征树。正确关系：
119. 安全因果距离层级只用于解释不安全事件的上游原因距离、需求投影和优先级衰减，不直接等同安全特征子节点，也不得直接作为安全结算贡献层级使用。
120. 二者不得直接互换。
121. 结算前必须通过：
122. 可以暴露风险、生成应急安全需求、触发原因解释。
123. 其中方法维护状态用于判断自我处理安全任务的能力基础，不得被误读成物理安全已经被直接提高。
124. 因果解释上应保持两段关系：
125. 不得把第一段误写成：
126. 也不得丢掉第二段：
127. 结算时不应只看“任务完成”。
128. 结算必须同时具备双钥匙和同源去重：
129. 该变化可归因于自我动作 / 治理动作 / 允许的被动提交链。
130. 7. 该因果模板是否应增加成立次数或失败次数。
131. 结算方法必须读取因果验收结果，至少包括：
132. 每个副作用必须绑定目标特征、实际变化值、严重度、是否已处理。
133. 可结算 / 仅候选 / 待补证据 / 禁止结算。
134. 只有动作记录而没有目标方向状态迁移，禁止结算。
135. 只有结果变化而没有动作致变归因，禁止结算。
136. 状态迁移账和动作致变账不同源，禁止结算。
137. 这些接口是因果类职责边界，不表示当前阶段必须一次性实现。
138. 候选动作 -> 风险 / 副作用 / 是否允许。
139. 因果信息的用途不是“给方法补条件结果对”这么窄。它应成为自我内部循环的解释与预测层：
140. 因果信息不直接替自我行动，但它告诉自我为什么、会怎样、该找什么、能不能做、做完算不算数。
141. 本文中出现的“置信度 / 稳定度 / 成功率”等说法，均按上级因果规范解释为派生解释值。它们由因果基础计数、证据类型和查询参数临时计算，不表示 `因果主信息类` 中存在对应持久字段。
142. 学到的东西能不能复用。
143. 学习层只能把它作为补动作、补条件、补来源、补证据的线索。
144. 学习层是方法域的候选生成层，只能生成学习候选、方法候选、条件结果对候选、动作候选、参数映射候选、失败缺口和练习收益证据。学习层不得直接宣布方法已好用、因果已验证、任务已完成、需求已满足或安全 / 服务值可结算。最终入账必须由方法运行账、动作动态、因果验收和自我治理入口裁决。
145. 不同因果残缺，对应不同学习需求。
146. 对应学习需求：
147. 禁止直接生成方法候选。只有补出动作致变因果后，才能进入普通方法候选链。
148. 补禁止条件
149. 需要学习：
150. 系统需要反推：
151. 学习层不得把因果族的硬交集、软交集或分化条件直接宣布为已验证因果。它只能生成候选、缺口和证据任务；因果成立仍由后续动作动态、状态迁移动态、证据动态样本、反例统计和因果验收共同裁决。
152. 需要判断是否稳定可复用。
153. 需要补条件、禁止条件或参数边界。
154. 需要更新失败次数、风险结果或不适用条件。
155. 练习不得代替目标方法改写目标方法虚拟存在。禁止由 A 写入 B 的：
156. 若 B 被调用后没有自行更新运行账，A 只能记录：
157. 并输出结构化差距或缺口回执，供后续派生需求 / 排错使用。A 不得补写 B 的运行账来制造好用结果。
158. 这些覆盖值域和稳定运行判据属于后续扩展。第一版不得由练习代写账本绕过。
159. 尝试学习或练习不是因为存在“学习需求”就必然启动。它必须具备一条最小学习收益因果链：
160. 没有这条链，尝试 / 练习不得进入主需求竞争，避免为了学习而学习、为了练习而练习。
161. 启动尝试或练习至少需要满足：
162. 必须先有候选动作；若没有候选动作，只能生成寻找候选动作、观察类似动态或请求外部信息的需求候选。
163. 练习需要已有可练习对象和可验证收益链。它通常需要已有动作致变因果；若目标是让 `自我_练习` 本能方法先运行起来，则至少需要内部治理层的练习收益因果：
164. 第一版允许内部治理练习链作为出生期被动触发假设或治理初始模板，先让练习方法在硬保护壳内运行；但不得伪造目标方法成功，不得代写目标方法运行账，不得把练习收益因果当作目标业务动作致变因果。
165. 练习需要已有动作致变因果或练习收益因果；
166. 尝试需要真实动作入口、受限执行条件和可观测后果；
167. 候选因果只影响尝试优先级，不是启动前置；
168. 必须区分：
169. 它不是后天学习出来的安全知识，而是系统不能越过的运行壳。
170. 它不是第一次尝试前必须已知的前置条件。
171. 不能越权写系统核心结构；
172. 不能无来源提交世界事实；
173. 不能伪造动作动态；
174. 不能直接结算安全值；
175. 不能把 改变特征值 当成业务方法；
176. 不能在无动作句柄时伪造可执行入口；
177. 不能绕过领域提交方法改写任务、需求、方法或世界树事实。
178. 缺少已知安全边界，不等于禁止尝试；
179. 缺少真实动作入口，禁止尝试；
180. 缺少可观测后果，禁止有效学习尝试；
181. 命中先天硬保护壳或已知致命禁区，禁止尝试。
182. 对应关系：
183. | 缺条件 | 条件节点、适用场景、禁止条件 |
184. 因此，因果反推得到的每个动作节点都必须落回一个已实现本能方法或方法动作入口；该动作必须已经有明确输入参数、明确输出结果和可记录动作动态。不能落回已实现本能方法的链段，只能作为学习缺口、观察需求、外部支撑需求或替代动作搜索依据。
185. 若要把它转成自我方法，必须同时满足：
186. 则不能直接固化为自我方法，只能生成学习需求：
187. 如果动作已知但自我不能执行或诱发，则也不能固化为方法，只能生成：
188. 这些步骤如果只返回本轮候选和诊断结果，不持久改写方法、任务、需求或世界树特征状态，可以由普通逻辑代码实现，不需要本能方法，也不生成动作动态。
189. 才进入需要明确动作来源的方法化提交段。
190. 因果转方法前，必须维护“自我可产生状态集”。
191. 若 C 不可产生，则派生：
192. 若 A 不可执行，则派生：
193. 因果转方法必须区分好结果和坏结果。
194. 不能生成“制造 C 或触发 A”的方法。
195. 应反向投影为：
196. 学习后的因果信息应在执行前被使用。
197. 候选动作出来后，自我应先判断：
198. 这样系统不会只看“能不能完成安全目标”，还要看：
199. 可能需要补条件
200. 因果信息应区分两类学习：
201. 有因果信息后，学习需求必须说明：
202. 这让学习从“长野草”变成“长根系”：允许生长，但必须有目标、路径和证据。
203. 8. 防止乱学：要求学习必须绑定目标、证据和因果路径。

## 4. 使用边界

1. 本规范用于计划、详细设计、代码修改和验收前的规则回读。
2. 本规范不能作为代码实现完成、业务闭环完成、自我苏醒完成或初步成熟完成的证据。
3. 需要展开历史论证时，只能把 `规范/旧规范/` 中的原文件作为追溯材料，不得让旧文件反向覆盖本规范。

## 修订记录

| 日期 | 版本 | 修订内容 |
| --- | --- | --- |
| 20260701 | v1.0 | 由历史来源修订迁回为正式子规范，接入当前规范目录。 |
