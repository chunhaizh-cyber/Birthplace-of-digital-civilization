# 4130 子规范 特征值三态比较底层逻辑

编号：4130
日期：20260701
状态：正式子规范
权威等级：正式规范

## 1. 定位

本规范是 `基础信息树` 主题下由旧规范修订迁回的正式子规范。它只承接当前仍有效的规则，不恢复旧规范中的历史口径、详细设计措辞或已经被根规范吸收的重复条款。

历史来源：
- `规范/旧规范/特征值三态比较底层逻辑规范20260518.md`

上级规范：
- `规范/2100_根规范_特征值主信息类_20260701.md`
- `规范/0050_项目通用结构与禁止性规则总纲_20260701.md`
- `规范/0300_规范冲突与前后矛盾清单_20260701.md`

## 2. 上级约束

1. 本规范服从 `规范/规范目录.md`、0050 总纲、0300 冲突清单和对应根规范。
2. 历史来源中与当前根规范冲突的表述不进入当前权威口径。
3. 旧名称、旧结构和旧阶段口径必须按 `规范/0300_规范冲突与前后矛盾清单_20260701.md` 的 C001-C008 收束，不得在本规范中恢复历史并列结构、旧字段身份或旧执行来源。
4. 任务筹办 / 任务执行只作为任务管理生命周期普通函数；动作来源、方法注册和方法选择必须服从 0050 总纲、3200 任务根规范、3300 方法根规范和 1170 动态根规范。
5. 文本、日志、注释、临时 JSON、自由字段和显示摘要不得承载正式机器逻辑或业务状态。

## 3. 当前有效规则

以下条款由历史来源按当前根规范修订抽取；若条款与第 2 章或上级规范冲突，以上级规范和第 2 章为准。

1. 本规范所说的三态比较，不是一个应进入数据层的数据结构，而是一条底层逻辑协议：
2. 方向掩码 / 允许方向：
3. 这些词最多作为人类阅读时的自然语言命名或展示标签，不得作为底层逻辑字段、推理枚举、结算依据或候选选择依据。
4. 底层比较函数应按三态比较协议返回一个有符号比较量：
5. 如果某些实现返回差值幅度，该幅度只能用于：
6. 不得直接作为基础逻辑分支。
7. 但这只是符号提取，不是要求原始比较函数只能返回这三个值。
8. 不得把“符号归一结果”误当作比较器原始返回值约束。
9. 进入比较前，调用方必须确保：
10. 6. 指针值、枚举值、数值、时间值等已经转换到对应比较域。
11. 如果前置条件不满足，不得让比较器返回“未知”或“不可比”。应在比较前阻断，并按错误性质处理：
12. 不可比较不是第四种比较结果。
13. 三态比较协议应当应用在系统的关系生成点，而不是语义解释点。
14. 关系生成点指系统需要判断两个特征值之间是否形成方向关系、命中关系、排序关系、动态关系或因果统计关系的地方。
15. 凡是系统要判断“两个特征值之间的方向关系”时，就应用三态比较协议。
16. 凡是只是保存对象、创建节点、记录原始值时，不应用三态比较协议。
17. 为了避免方向反转，所有关系生成点必须固定左值和右值语义。
18. | 方法前置 | 当前输入值 | 方法要求值 | 是否允许执行 |
19. 状态迁移必须统一为：
20. 任何模块不得私自反转状态迁移的左右值含义。
21. 当某个特征从旧值变为新值时，应比较旧值和新值：
22. 因此，比较层和条件命中层必须分开：
23. 若方法执行后没有任何可比较事实变化，不得伪造解释性动态。是否记录 `比较量 = 0` 的稳定动态，由具体动态记录规范决定。
24. 二次特征应表示比较关系模板：
25. 底层二次特征不得表示：
26. 场景、空间、距离、相似度等逻辑应先计算特征值，再比较特征值。
27. 如果现有 `比较(...)` 在某些路径中返回 `1 / 0` 表示“命中 / 不命中”，它应被视为条件命中函数，不是三态比较器。
28. 不可比是比较前流程失败，不是比较结果。
29. 不得用比较量表达：
30. 不可比
31. 以下操作若没有两个值需要形成关系，不调用三态比较器：
32. 比较器不得输出：
33. 如果底层只需要符号，推荐：
34. 如果确实需要差值幅度，应使用：
35. 但系统基础逻辑仍只能读取：
36. 底层不需要单独保存“不等于、大于等于、小于等于”等关系。
37. 优先级更高
38. 优先级更低
39. 允许方向 / 方向掩码：
40. 表示允许哪些比较方向命中。
41. 底层流程必须固定为：
42. -> 允许方向 / 方向掩码判断是否命中
43. 因此允许方向不是第一步。它只能在已经得到比较量之后，作为模板、条件、方法前置、二次特征、因果和查询筛选的匹配约束。
44. 方向区间也不是第一步。它只能在已经得到比较量或安全差值之后，表达某一方向内部的细分域。例如：
45. 后续若需要更细粒度，可以在不改变三态比较协议的前提下扩展方向区间。因为三态比较协议只规定：
46. `允许方向` 和 `方向区间` 不应互相替代，因为二者回答的问题不同：
47. 允许方向：
48. 回答 比较(左值, 右值) 的符号是否被模板允许。
49. 允许方向 不能当作 方向区间。
50. 方向区间 不能当作 允许方向。
51. `允许方向` 是模板命中约束；`方向区间` 是变化量域特征。
52. 如果使用比较条件枚举，条件判断应基于比较量符号：
53. 如果使用方向掩码，掩码也必须由比较量符号生成：
54. static inline bool 命中方向(std::int64_t r, std::uint8_t 允许方向) noexcept
55. return (比较量方向掩码(r) & 允许方向) != 0;
56. 当前值 与 模板值 / 阈值 比较后，判断 r 是否命中允许方向。
57. 当前输入值 与 方法要求值 比较后，判断是否允许执行。
58. 方向掩码的禁止用途：
59. 1. 原始状态存储不得保存方向掩码。
60. 2. 不得用方向掩码解释语义。
61. 3. 不可比不是方向掩码，不可比应在比较前失败。
62. 4. 不得把方向掩码当作因果作用类型或动态类型。
63. 判断只是二次特征的一种用途。二次特征本体应理解为：
64. 是二次特征模板的允许值域。
65. 二次特征的核心不应是：
66. 而应是：
67. 标量区间 / 允许方向 / 方向区间
68. 如果实现上为了索引或缓存保存比较量，必须约定：
69. 主信息收缩时，不应保存“比较器”或“比较量缓存”这类工具性字段。可以保存：
70. 允许方向
71. 需要方向时运行：
72. 模板二次特征保存允许值域：
73. 实例左右值按需比较所得符号 是否命中 模板允许方向
74. `是否满足` 不应作为底层事实源头。它只能是：
75. 底层事实应来自：
76. `置信度`、成功率、解释分等不属于二次特征主事实字段。若需要显示或排序，应由统计、证据或解释层临时计算，不进入 `二次特征主信息类`。
77. 业务语义只能绑定到这些比较结构之上作为说明，不得反过来成为底层推理依据。
78. 系统不需要知道这叫“推进”“下降”“满足”或“超时”；系统只需要比较量符号。
79. 因果主信息应收束为：
80. 因果主信息不得保存：
81. 这些都必须从果侧比较模板临时解释。
82. 但动态节点不得在底层说：
83. 需求目标应仍然表达为：
84. 但底层判断必须落到比较量：
85. 安全 / 服务结算不得直接使用“促进、抑制、增强、减弱”等解释词作为依据。
86. 结算前必须能还原：
87. 只能说明本轮有推进，不能直接等于完成。
88. 若实际实现没有直接使用差值，也必须通过比较量符号得到等价结论。
89. 系统允许在展示层、人类文档、控制面板或日志中使用解释词：
90. 但必须遵守：
91. 2. 解释词不得进入底层推理字段。
92. 3. 解释词不得作为任务完成、需求满足、方法选择、因果结算的直接依据。
93. 4. 解释词必须能追溯到具体比较对象和比较量符号。
94. 后续实现应遵守：
95. 3. 若确需展示解释词，必须放到解释函数或展示函数中。
96. 6. 任务完成、需求满足、价值结算必须先还原实际值、目标值、前值及比较量符号。
97. 7. 比较前置条件失败不得伪装成第四种比较结果。
98. 8. 系统基础逻辑不得依赖比较量绝对值。
99. 若保留这些名称，也只能作为封装比较量符号组合的高层解释函数。
100. 与因果、二次特征、动作动态、需求满足、任务完成、价值结算相关的下级规范如出现冲突，应按本规范修订：
101. 本规范不改变动作动态必须绑定方法的硬规则，也不改变线程不是动作来源的边界。它只规定：动作造成的事实变化方向必须由特征值比较量符号得到。
102. 应用时机最短版：
103. 当两个特征值需要形成关系、方向、命中、排序、动态或因果统计时应用；
104. 当只是保存单个事实时不应用。

## 4. 使用边界

1. 本规范用于计划、详细设计、代码修改和验收前的规则回读。
2. 本规范不能作为代码实现完成、业务闭环完成、自我苏醒完成或初步成熟完成的证据。
3. 需要展开历史论证时，只能把 `规范/旧规范/` 中的原文件作为追溯材料，不得让旧文件反向覆盖本规范。

## 修订记录

| 日期 | 版本 | 修订内容 |
| --- | --- | --- |
| 20260701 | v1.0 | 由历史来源修订迁回为正式子规范，接入当前规范目录。 |
