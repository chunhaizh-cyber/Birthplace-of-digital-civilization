# 6350 子规范 双目相机外设独占观察线程

编号：6350
日期：20260701
状态：正式子规范
权威等级：正式规范

## 1. 定位

本规范是 `观察外设与视觉` 主题下由旧规范修订迁回的正式子规范。它只承接当前仍有效的规则，不恢复旧规范中的历史口径、详细设计措辞或已经被根规范吸收的重复条款。

历史来源：
- `规范/旧规范/双目相机外设独占观察线程规范20260527.md`

上级规范：
- `规范/0050_项目通用结构与禁止性规则总纲_20260701.md`
- `规范/1150_根规范_场景节点主信息类_20260701.md`
- `规范/0050_项目通用结构与禁止性规则总纲_20260701.md`
- `规范/0300_规范冲突与前后矛盾清单_20260701.md`

## 2. 上级约束

1. 本规范服从 `规范/规范目录.md`、0050 总纲、0300 冲突清单和对应根规范。
2. 历史来源中与当前根规范冲突的表述不进入当前权威口径。
3. 旧名称、旧结构和旧阶段口径必须按 `规范/0300_规范冲突与前后矛盾清单_20260701.md` 的 C001-C008 收束，不得在本规范中恢复历史并列结构、旧字段身份或旧执行来源。
4. 任务筹办 / 任务执行只作为任务管理生命周期普通函数；动作来源、方法注册和方法选择必须服从 0050 总纲、3200 任务根规范、3300 方法根规范和 1170 动态根规范。
5. 文本、日志、注释、临时 JSON、自由字段和显示摘要不得承载正式机器逻辑或业务状态。

## 3. 当前有效规则

以下条款由历史来源按当前根规范修订抽取；若条款与第 2 章或上级规范冲突，以上级规范和第 2 章为准。

1. 若本文与上级 / 约束规范冲突，以上级 / 约束规范为准。本文只定义双目相机外设内部独占观察线程的职责、输出、学习边界和对自我的暴露面，不改变外设本能方法控制 / 检查 / 使用三分类，不允许线程直接成为自我世界树、需求树或存在树的权威写入者。
2. 2026-06-02 口径补充：帧是外设 / 接口层材料来源概念，接口层本能方法和外设线程可以使用当前外设帧、上一外设帧和来源外设帧ID。自我层需求、事实和场景输出不得以帧为认知单位，必须转换成观察材料集、观察范围、可观测单位和对应事实。
3. 相机外面用等待项、命令、三种观察运行模式和对应报告说话；
4. 对应报告为：
5. 其中“识别”表示处理未识别区域、候选区域和冲突区域，建立观察材料到已确认存在、新存在候选、未知区域、冲突区域、静止存在 / 环境存在或噪声 / 无效材料的归属关系；它不表示语义识别、稳定世界存在确认、跨帧身份闭合或目标可见性闭合。旧像素级方案里的“100%”只表示帧内可寻址、可回查、无遗漏、无重复；新的识别闭合标准是有效观察材料没有无主漂浮项。自我认知中不得把“背景”作为归属目标；视觉工程背景必须转写为静止存在、环境存在或低动态存在。
6. 禁止把系统设计成：
7. 允许且推荐：
8. 外设线程按优先级、时效和质量缺口调度输出逐簇识别报告、扫描变化报告或跟踪报告。
9. `双目相机.主观察运行模式` 可以作为对外摘要，取值只限三种外显模式。实现上不得把它写死成相机唯一全局状态，也不得用它表示整场景阶段；内部仍应采用外设观察订阅 / 等待项，允许未识别区域识别、低频扫描变化与高频目标跟踪同时存在。
10. `双目相机.工作模式` 仍可表达物理采集配置，例如高深度质量、低延迟、指定 ROI、曝光或分辨率；但它不得被用作补观察、质量诊断、局部细分等内部策略的唯一全局状态。
11. 对外等待项只应写成：
12. 不得写成：
13. D455 / RGB-D 外设内部可以使用 RealSense、OpenCV、跨帧跟踪、SLAM、TSDF、OctoMap、VOS 等工程技术，但这些技术只能服务外设侧稳定供料，不改变自我侧正式承接对象。
14. L0 原始帧和原始簇不能直接提交为观察存在；
15. 单帧质量优质不等于跨帧观察存在稳定。外设线程必须至少区分：
16. 2. 维护三种观察运行模式对应的订阅项、跟踪目标列表和内部质量缺口状态。
17. 8. 在任务管理提交逐簇识别、扫描变化或目标跟踪等待项时，优先使用已学到的稳定配置。
18. 线程可以作为调用者、调度者或外设内部服务，但不能作为方法动作动态的动作来源。凡是需要落成自我侧动作动态或实例特征值的变化，必须由明确方法承接。
19. 错误 / 不可观测原因报告
20. 取得但只能降级使用的特征，要标记为降级可用。
21. 这些候选的颜色、深度、轮廓、空间坐标、距离等特征在当前配置下稳定 / 降级可用 / 不稳定 / 不可得。
22. 外设线程不能说：
23. 同一个报告结构可以按三种外显运行模式裁剪输出。逐簇识别报告面向“未知视野如何组织成观察像素簇和存在候选输入”；扫描变化报告面向“已识别场景相比基准是否有新增、消失、移动、尺寸变化或高风险未知区域”；跟踪报告面向“这个已有目标簇 / 存在候选是否还能稳定复现特征”。补观察、质量诊断、局部细分、变化监测和配置学习不得成为额外可见外设模式；它们只能通过三种报告字段、外设内部策略或自我后续方法承接。
24. 哪些区域未解释、低置信或不可观测？
25. 逐簇识别报告应至少包含：
26. 不可观测区域掩码
27. 可观测单位对应摘要
28. 对应需求可以写成：
29. 当前观察材料集.可观测单位对应摘要状态 等于 已生成
30. 其中 `当前观察帧.存在候选集合状态` 仍是接口层材料组织状态；`当前观察材料集.可观测单位对应摘要状态` 只能由任务管理承接报告后触发自我侧解释链生成。相机线程直接输出的是观察像素簇、掩码、局部图、稳定特征和质量 / 不可观测原因。
31. 当前观察帧.未解释像素比例 小于 最大允许未解释比例
32. 当前观察帧.冲突像素比例 小于 最大允许冲突比例
33. 如果已经没有可执行观察改善路径，且不可观测区域原因明确率达标，也可以停止，不能无限重试。
34. 有没有高风险未知簇需要任务管理继续承接？
35. 扫描变化是低成本、低频、保最新报告的守望模式，不重新逐个识别所有簇，不默认做局部细分，也不默认承接所有原始帧。没有可比历史时，只能为已稳定对应存在更新当前特征值并建立当前状态基准，不得输出发现存在或状态变化。
36. 扫描变化报告应至少包含：
37. 当前观察帧.扫描变化报告年龄 小于 最大允许扫描变化报告年龄
38. 跟踪要求跨帧连续性，但仍不能直接宣称稳定世界对象。它适合在已有目标存在指针 / 引用的前提下，以空间位置为入口，用空间尺寸、轮廓、ROI / 掩码 / 点集等当前场景可唯一凭证找回目标，并输出新空间坐标或空信息；稳定特征集合、丢失原因和重捕获候选只是过程证据。
39. 跟踪报告应至少包含：
40. 目标观察候选.最近观测年龄 小于 最大允许观测年龄
41. 目标观察候选.特征复现离散度 小于 最大允许离散度
42. 外设线程可以维护内部跟踪和重捕获线索，但不能把内部跟踪身份直接写成稳定世界存在。
43. 作为三种报告里的低质量区域、不可观测区域、质量诊断摘要和失败原因。
44. 该报告必须引用 `050004` 中定义的观察像素簇结构。旧的空间候选 / 空间候选观察事实字段只允许作为归档说明或迁移前只读定位线索，不得继续参与当前机器判断、当前计划验收或新实现。当前语义必须按以下关系解释：
45. 彩色轮廓截图和深度轮廓截图属于候选观察对象或观察像素簇的局部图像证据。它们用于控制面板显示、人工复核、递归细分、跨帧比较和外设配置学习，不是外设线程确认世界存在的依据。外设线程不得因为截图可用而直接宣称候选观察对象已经是自我世界树中的已确认观察存在。
46. 掩码类特征不同于截图证据。掩码不是给人看的附件，而是后续候选生成、子轮廓提取、像素归属判断、三维建模和观察材料集可观测单位对应统计会直接使用的正式特征。外设线程可以提交掩码句柄和掩码状态，但可观测单位对应摘要、可观测单位存在对应闭合状态以及是否入树，仍由自我侧方法承接和裁决。
47. 外设线程输出的每类特征应分级：
48. 可以使用，但必须带低置信或用途限制。
49. 只能作为线索，不能作为确定结论。
50. 不可得特征:
51. 不得伪造默认值，必须记录失败原因。
52. 空间坐标特征状态 等于 不可得
53. 空间坐标不可得原因 等于 深度有效率不足
54. 不得用一个整帧级的：
55. 质量不可用
56. 粗暴覆盖所有局部候选观察对象和所有特征类型。整帧质量诊断只能作为上层摘要；候选对象和特征级稳定性必须保留用途相关的细分结果。
57. 3. 如果存在高优先级跟踪等待项:
58. 全局扫描当前视野，按优先级逐簇处理。
59. 1. 如果有高优先级安全跟踪需求，跟踪优先。
60. 4. 如果服务任务锁定目标，目标跟踪优先，低频扫描变化可后台保留。
61. 8. 如果需要内部结构或三维建模，自我基于报告中的局部图和掩码启动后续方法。
62. 对应三比较需求可以是：
63. 任务管理或自我侧可以用本能方法 / 外设命令表达“需要相机可用、需要检查状态、需要逐簇识别报告 / 扫描变化报告 / 跟踪报告”；
64. 控制类方法可以改变外设自身状态，但必须有明确输入、前置条件、输出和动作动态落账规则。
65. 双目相机.工作质量状态 等于 不可用
66. 检查类方法原则上不启动、重启、修复、切换模式或长期占用外设；如果检查必须改变主要工作状态，应返回控制类缺口。
67. 这些边界动作不进入相机线程内部；相机线程只读取等待项或命令状态，并用内部代码生产相应报告。
68. 不得把下列外设内部步骤暴露为自我层本能方法：
69. 最大允许报告年龄
70. 是否允许缓存报告
71. 逐簇识别等待项的目标区域可以为空，期望输出通常是观察像素簇集合、簇ID掩码、未解释像素掩码、低置信像素掩码、不可观测区域掩码和可观测单位对应摘要。扫描变化等待项基于已建立场景解释或最近缓存，期望输出新增、消失、变化、高风险未知和低质量区域集合。目标跟踪等待项通常携带观察像素簇ID、存在候选ID、ROI、掩码或目标特征摘要等种子，期望输出是跟踪状态、丢失状态、稳定特征集合和重捕获候选集合。
72. ROI、补观察、稳定特征取值、重捕获、质量诊断和配置学习不得作为新的观察运行模式写入等待项；如果确需表达，只能放在目标区域、期望特征类型集合、质量要求或内部策略提示里。
73. 不得采用：
74. 5. 需要任务管理线程先匹配任务上下文、等待项、活跃需求和时效。
75. 自我根需求真正接收的应是经过任务管理承接、匹配、转译后的：
76. 不得把所有原始帧直接塞入自我可见的全局队列。队列和缓存分三层。
77. 如果正式需求需要：
78. 7. 判断是否需要上报自我线程。
79. 当前观察材料集.可观测单位存在对应闭合状态 等于 闭合
80. 期望内容 = 观察像素簇集合 / 簇ID掩码 / 可观测单位对应摘要
81. 是否允许缓存 = 是
82. 没有显式任务等待，但有活跃需求需要 目标区域.稳定特征状态 等于 稳定。
83. 3. 产生安全相关高优先级缺口。
84. 8. 需要正式派生需求入树。
85. 控制面板不得把相机原始画面、外设内部候选或未被需求领走的队列消息直接当作自我识别结果。控制面板复现场景信息应以自我所在场景中的观察帧、像素观察事实、观察像素簇、存在候选或已确认观察存在等已承接特征为数据源，不得反写世界树真值。
86. 它对自我暴露的只能是学习摘要或检查 / 使用结果，例如：
87. 该学习模块不得直接改写自我需求树，不得直接把外设内部学习结论写成世界树真值。
88. 自我线程可以裁决和调度，但不能绕过方法直接把外设报告写成世界树权威事实。
89. 任务筹办和任务执行仍是任务管理生命周期普通函数，不作为本能方法注册、选择或动作动态来源。任务管理可以调用自我侧边界本能方法或发送外设命令，但不得要求相机线程内部用本能方法实现观察算法。
90. 目标存在.距离复现离散度 小于 最大允许离散度
91. 三种相机报告可以支持但不能直接满足的自我认知需求示例：
92. 观察订阅自身应优先用三比较表达：
93. 4. 已识别场景扫描变化低频保留；高优先级目标跟踪订阅可抢占采样预算。
94. 7. 每个特征必须带：
95. 8. 逐簇识别报告输出观察像素簇集合、帧级掩码、低质量区域、未解释区域、不可观测区域、局部图和可观测单位对应摘要。
96. 缺真实 D455 连续样本时，只能记录等待真实样本或外设链路超时，不得用模拟样本、夹具入口或伪造动作动态证明稳定输出。
97. 禁止：
98. 8. 把不稳定或不可得特征伪造成稳定特征。

## 4. 使用边界

1. 本规范用于计划、详细设计、代码修改和验收前的规则回读。
2. 本规范不能作为代码实现完成、业务闭环完成、自我苏醒完成或初步成熟完成的证据。
3. 需要展开历史论证时，只能把 `规范/旧规范/` 中的原文件作为追溯材料，不得让旧文件反向覆盖本规范。

## 修订记录

| 日期 | 版本 | 修订内容 |
| --- | --- | --- |
| 20260701 | v1.0 | 由历史来源修订迁回为正式子规范，接入当前规范目录。 |
