# 8210 子规范 自我动作验证闭环

编号：8210
日期：20260701
状态：正式子规范
权威等级：正式规范

## 1. 定位

本规范是 `运行治理与阶段验收` 主题下由旧规范修订迁回的正式子规范。它只承接当前仍有效的规则，不恢复旧规范中的历史口径、详细设计措辞或已经被根规范吸收的重复条款。

历史来源：
- `规范/旧规范/自我动作验证闭环规范20260612.md`

上级规范：
- `规范/1170_根规范_动态节点主信息类_20260701.md`
- `规范/1190_根规范_因果主信息类_20260701.md`
- `规范/0050_项目通用结构与禁止性规则总纲_20260701.md`
- `规范/0300_规范冲突与前后矛盾清单_20260701.md`

## 2. 上级约束

1. 本规范服从 `规范/规范目录.md`、0050 总纲、0300 冲突清单和对应根规范。
2. 历史来源中与当前根规范冲突的表述不进入当前权威口径。
3. 旧名称、旧结构和旧阶段口径必须按 `规范/0300_规范冲突与前后矛盾清单_20260701.md` 的 C001-C008 收束，不得在本规范中恢复历史并列结构、旧字段身份或旧执行来源。
4. 任务筹办 / 任务执行只作为任务管理生命周期普通函数；动作来源、方法注册和方法选择必须服从 0050 总纲、3200 任务根规范、3300 方法根规范和 1170 动态根规范。
5. 文本、日志、注释、临时 JSON、自由字段和显示摘要不得承载正式机器逻辑或业务状态。

## 3. 当前有效规则

以下条款由历史来源按当前根规范修订抽取；若条款与第 2 章或上级规范冲突，以上级规范和第 2 章为准。

1. 007 禁止性规范20260611
2. 若本文与上级 / 约束规范冲突，以上级 / 约束规范为准。本文中的成功、失败、超时、回读不足等词只作为验证结论或人读状态，不得替代动作动态、结构化回读、特征化事实或错误根因。
3. 事实状态必须来自对应实在域的实际确认通道，而不能直接来自预测。
4. 预测态禁止直接写为事实态。
5. 预测态只能存在于：
6. 只有经过对应实在域实际回读后，系统才允许产生新的事实状态。
7. 动作产生的只是“意图”和“执行事件”。事实必须来自实际回读。
8. 需求 D1 的优先级应为 1。
9. 系统禁止把目标值 `1` 强行写为事实。
10. 当实际回读与预测冲突时，系统应保留实际回读结果，并标注为：
11. 系统禁止因为实际结果“不符合期望”而丢弃真实回读。
12. | 治理域 | 分配需求、调整优先级、选择方法 | 需求、任务、方法、约束对象的实际治理状态 |
13. 系统必须区分以下状态：
14. 从对应实在域实际读取到的状态。
15. 系统承认为对应实在域当前实际情况的状态。
16. 任何动作必须遵循以下通用闭环：
17. 对应实在域实际回读 R1
18. 动作事务是一次动作的生命周期记录。系统中的动作禁止只用裸指令表达，必须由动作事务承载。
19. 目标不可达偏差
20. 以上枚举是验证状态和纠偏策略建议，不得作为裸错误码、裸失败或动作动态根语义使用。代码实现时必须同时保留目标、预测、执行报告、实际回读、偏差、残差、置信度和来源证据。
21. 事实场景可以进入世界链，但必须携带：
22. 目标场景模板禁止直接提交为事实。
23. 预测场景模板必须使用值语义或私有模板结构，禁止作为事实挂入世界链。
24. 在验证与提交策略允许后进入世界链或对应数据仓库。
25. 实际回读场景模板保存执行后从对应实在域读取到的状态。
26. D455 的原始帧不能直接写成最终事实。D455 必须先形成观测场景：
27. 系统应定义统一的实际回读接口：
28. 从需求环境读取实际优先级、状态、约束、分配关系。
29. 预测器必须满足：
30. 1. 只能写入预测场景模板。
31. 2. 禁止写入事实场景。
32. 3. 必须记录预测依据、预测时间、容差范围。
33. 4. 应给出可验证特征，而不是只给自然语言描述。
34. 执行器必须满足：
35. 1. 必须记录事务ID。
36. 2. 必须记录执行开始时间和执行完成时间。
37. 3. 执行器返回值只能作为执行报告态，不得直接作为事实态。
38. 4. 对异步动作，必须记录可追踪的指令ID、帧号、版本号或回调标识。
39. 验证器应支持以下比较方式：
40. 系统禁止以下行为：
41. 1. 禁止把预测场景模板直接挂入事实世界。
42. 2. 禁止把目标态直接写为事实态。
43. 3. 禁止把方法返回的“成功”直接当作治理事实。
44. 4. 禁止把控制指令发送成功当作物理状态已经改变。
45. 5. 禁止在没有实际回读的情况下用预测值补写事实。
46. 正常事实必须记录：
47. 当实际回读置信度足够，但与目标或预测冲突时，必须提交异常事实或待复核事实，而不是提交预测值。
48. 异常事实必须记录：
49. 是否需要恢复动作
50. 是否需要重新规划
51. 当实际回读置信度不足时，系统应进入待复核状态。
52. 低置信回读不得直接覆盖高置信事实，除非恢复策略明确允许。
53. 物理移动必须支持三层验证：
54. D455 观测值必须标明坐标系。典型转换链为：
55. 场景中应显式记录：
56. D455 回读必须满足：
57. 5. 若存在运动模糊或遮挡，应标记低置信或待复核。
58. 6. 对关键动作，应要求连续若干帧稳定后再提交最终事实。
59. 内部治理类动作的事实必须来自方法执行后虚拟存在中的实际值。
60. 系统禁止把以下内容直接当作事实：
61. 内部虚拟存在回读应记录前置版本号和后置版本号。
62. 若动作声称改变了对象，但后置版本号未变化，系统应标记为验证失败或待复核。
63. 内部治理动作验证失败时，系统应区分失败原因：
64. 优先级；
65. 治理动作必须遵循：
66. 1. 治理动作必须有事务ID。
67. 2. 治理动作必须有目标态和预测态。
68. 3. 治理动作执行后必须从实际治理对象回读。
69. 5. 当实际治理状态与目标不一致时，必须保留实际状态并记录冲突。
70. 数据仓库写入动作完成后，必须按主键、父子关系或索引重新读取实际节点。
71. 对链式仓库，回读时应检查：
72. 如果写入后重新读取到的结构与预测不一致，系统必须记录仓库异常事实，禁止用预测结构补写检查结果。
73. 推理域实际回读来自推理过程的可复核输出，而不是来自“我认为结论应当成立”。
74. 推理动作必须至少记录：
75. 如果推理输出与目标不一致，实际输出仍应记录为推理事实或异常推理事实。
76. 所有动作事务必须记录单调时间序列：
77. 对于内部虚拟存在和数据仓库动作，应记录版本号：
78. 若动作预期改变对象，则 `V1` 应大于 `V0`，或者对象状态应有明确变化记录。
79. 实际回读必须包含置信度。
80. 可提交待复核事实，禁止覆盖关键高置信事实。
81. 系统必须遵循：
82. 重计算、视觉解析、推理、匹配、验证应在锁外完成。
83. 禁止拿着 D455 帧锁执行点云重建后再写世界链。
84. 禁止拿着消息队列锁等待外设回调。
85. 禁止拿着世界链写锁执行长时间推理。
86. 禁止在持有后序锁域时反向申请前序锁域。
87. 多锁同时持有时，应遵循系统既有锁顺序原则：
88. 动作闭环实现不得破坏该顺序。
89. 失败处理必须遵循：
90. 4. 决定是否需要停止、回滚、补偿、重试、重新规划或生成新需求。
91. 调整需求优先级
92. 标记方法不可用或低可信
93. 每次验证都应产生残差记录。
94. 残差应按动作类型保存：
95. 状态差异、绑定差异、优先级差异、负载差异。
96. 系统应周期性统计残差，用于修正预测模型和执行策略。
97. 闭环获得偏差之后，不能直接把事实改成预测值或目标值。纠偏的核心规则是：
98. 偏差不是要被擦掉，而是要被分配责任。不同类型的偏差对应不同修正对象。
99. 系统至少应生成三类偏差：
100. 这三种偏差不得混用。
101. 应得到：
102. 对应纠正不是一个动作，而是多账本处理：
103. 2. 目标还差 26mm，若安全和约束允许，生成补偿动作。
104. 不得写成：
105. 物理外部域中，D455 / IMU / 编码器等实际回读是事实候选；内部治理域中，方法执行后的虚拟存在实际值、治理对象实际状态或仓库重新读取结果是事实候选。目标值、预测值和执行报告不得覆盖实际回读。
106. 偏差获得后，必须按五层处理：
107. D455 观察链路应保持：
108. 禁止：
109. 目标偏差 `DeltaG = G - R` 用于判断是否需要补偿。
110. 补偿动作不得裸发，必须重新形成闭环：
111. 模型参数不得被单次异常回读大幅改写，应使用缓慢更新：
112. 执行报告只能作为执行报告态，不得作为事实态。
113. 内部方法纠偏应写入方法虚拟存在或方法统计事实，例如：
114. 有些偏差不是动作或方法错误，而是目标不可达、目标强度不合理或治理约束阻挡。
115. 目标不可达，不应继续机械补偿。
116. 此时应进入治理：
117. 目标不可达
118. 需求 D1 优先级设为 1。
119. 规则约束导致只能设为 2。
120. 生成解除约束、调整目标或重排优先级的治理需求。
121. | 执行报告成功，实际失败 | 报告不可信 | 降低方法 / 执行器报告可信度 |
122. 该矩阵可以作为动作验证模块的策略表，但实现时不得只依赖自由文本；每个策略分支必须有结构化偏差类型、置信度、来源证据和后续承接入口。
123. D455 纠偏不得另开旁路，应沿既有观察承接链：
124. 内部方法偏差不能靠重新写目标值纠正，只能靠读取实际值后生成补偿治理动作。
125. 纠偏动作本身也是动作，也必须经过预测、执行、实际回读和验证。
126. 例如第一次动作前进 300mm，实际 274mm；若生成补偿事务，应表达为：
127. 禁止开环纠偏。任何“纠偏建议执行”都不得直接改事实状态，而应生成新的动作事务、治理需求、补观察需求、重新规划需求或安全降级动作。
128. 系统可定义偏差纠正器，但它只能分析偏差和生成承接动作，不得直接绕过闭环改事实。
129. `生成纠偏承接` 的输出必须进入正式任务、需求、动作或观察链路。它不得裸写世界事实、需求满足、任务完成、方法可靠度或预测模型参数。
130. 每次闭环后的偏差都应沉淀为统计：
131. 方法：调整需求优先级
132. 目标：优先级降至 1
133. 若偏差不可由动作补偿解决，则生成新需求或调整目标
134. 闭环工程化分为四层，不得混在一个函数中：
135. 禁止把 D455、动作线程、任务执行或提交事实入口写成同时采集、验证、提交事实、派生需求和修正模型的混合函数。
136. 动作事务必须有可挂特征的宿主。第一阶段推荐表达为自我内部世界中的虚拟存在：
137. 动作线程已有请求 ID、输入场景、输出场景、目标存在、方法根和动作步骤结果，可作为执行器层基础，但不能直接等同完整验证闭环。
138. D455 驱动层只提供物理回读材料。轮廓、存在、稳定候选、确认事实和提交门禁必须由下游链路统一生成。
139. 用于计算还需要补偿多少。
140. 两种口径不得混写到同一个字段中。
141. 治理类二次特征不得直接执行动作，只能交给需求层、任务治理层或提交入口处理。
142. | `事实提交等级 = 不可提交` | 禁止提交并派生补材料需求 |
143. 派生需求至少应携带：
144. 特征_需求优先级
145. 这些字段若可由需求目标、特征、二次特征、来源动作动态或提交入口记录表达，不得新增到需求主信息类。需求主信息仍按 020003 保持原子目标语义。
146. 学习层不得直接读 D455 原始帧，也不得直接读动作线程结果。学习样本来自：
147. 允许提交观察事实。
148. 方法执行后，实际回读不得来自“返回成功”，而应来自：
149. 所有 `提交_...事实` 类动作都必须经过统一门禁。门禁读取动作事务上的二次特征：
150. 允许写入事实世界。
151. 允许写入异常事实区 / 异常状态，但不能伪装成正常事实。
152. 允许暂存，不覆盖高置信旧事实。
153. 事实提交等级 = 不可提交:
154. 禁止提交，必须生成补观察或治理需求。
155. 提交事实本能方法不得成为绕过验证的暗门。
156. 任务治理逻辑不再只看执行成功或执行失败，而应看：
157. | 事实不可提交 | 禁止任务完成 |
158. 任务状态变化仍必须通过任务域被动状态动作 `提交任务状态变化`。动作验证报告只能作为治理依据，不得裸写任务状态。
159. 方法选择不能只按“曾经执行成功”，必须吸收验证成功率、报告可信度和预测命中率。
160. 经常“报告成功但实际失败”的方法必须被降权，而不是因为返回成功多而被强化。
161. 第一阶段允许新增或修改的方向：
162. 第一阶段需要接入的文件：
163. 高质量才允许提交观察事实。
164. 方法返回成功但目标未达成时，不允许任务完成。
165. 目标不可达时派生重规划需求。
166. 第一阶段必须实现两个样板闭环：
167. 16. 纠偏动作本身必须重新进入动作闭环。
168. 17. 方法可靠度和预测模型只能依据实际回读、残差、置信度和结构化偏差更新。
169. 20. 低质量或低新鲜度 D455 观察不得提交为正常事实。
170. 21. 执行报告成功但实际回读未达成目标时，任务不得仅凭执行报告进入完成。
171. 22. 方法选择不得只按执行成功次数强化，必须吸收验证成功率或报告可信度。
172. 事实来自对应实在域的实际回读。
173. 失败事实仍然是事实，必须被记录、解释和治理。

## 4. 使用边界

1. 本规范用于计划、详细设计、代码修改和验收前的规则回读。
2. 本规范不能作为代码实现完成、业务闭环完成、自我苏醒完成或初步成熟完成的证据。
3. 需要展开历史论证时，只能把 `规范/旧规范/` 中的原文件作为追溯材料，不得让旧文件反向覆盖本规范。

## 修订记录

| 日期 | 版本 | 修订内容 |
| --- | --- | --- |
| 20260701 | v1.0 | 由历史来源修订迁回为正式子规范，接入当前规范目录。 |
