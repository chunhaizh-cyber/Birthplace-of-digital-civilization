# 5200 子规范 任务根据需求初始化

编号：5200
日期：20260701
状态：正式子规范
权威等级：正式规范

## 1. 定位

本规范是 `需求任务方法` 主题下由旧规范修订迁回的正式子规范。它只承接当前仍有效的规则，不恢复旧规范中的历史口径、详细设计措辞或已经被根规范吸收的重复条款。

历史来源：
- `规范/旧规范/任务根据需求初始化规范20260509.md`

上级规范：
- `规范/3200_根规范_任务主信息类_20260701.md`
- `规范/3100_根规范_需求主信息类_20260701.md`
- `规范/0050_项目通用结构与禁止性规则总纲_20260701.md`
- `规范/0300_规范冲突与前后矛盾清单_20260701.md`

## 2. 上级约束

1. 本规范服从 `规范/规范目录.md`、0050 总纲、0300 冲突清单和对应根规范。
2. 历史来源中与当前根规范冲突的表述不进入当前权威口径。
3. 旧名称、旧结构和旧阶段口径必须按 `规范/0300_规范冲突与前后矛盾清单_20260701.md` 的 C001-C008 收束，不得在本规范中恢复历史并列结构、旧字段身份或旧执行来源。
4. 任务筹办 / 任务执行只作为任务管理生命周期普通函数；动作来源、方法注册和方法选择必须服从 0050 总纲、3200 任务根规范、3300 方法根规范和 1170 动态根规范。
5. 文本、日志、注释、临时 JSON、自由字段和显示摘要不得承载正式机器逻辑或业务状态。

## 3. 当前有效规则

以下条款由历史来源按当前根规范修订抽取；若条款与第 2 章或上级规范冲突，以上级规范和第 2 章为准。

1. 任务虚拟存在的独有特征类型不得由任务自身任意生成；必须来自已选择的实现方法虚拟存在、组合方法虚拟存在或治理方法虚拟存在声明的任务特征类型契约。
2. 任务信息节点内应包含 / 引用一个任务虚拟存在。
3. 需求主信息类::对应任务
4. 所以不能改成：
5. 任务信息节点主信息里需要有：
6. 当前 `任务主信息类` 已经有该字段，应保留。它的规范语义是：
7. 可解析引用<需求节点类> 对应需求{};
8. 如果可解析引用<需求节点类> 当前可用，来源需求优先留在任务信息节点主信息。
9. 特征值里的 `指针句柄` 第一阶段主要用于存在 / 基础信息节点等语义对象引用。任务信息节点和需求节点这类运行壳关系，应优先保留结构引用。
10. 对应任务实例
11. 需求节点->主信息.对应任务 = 任务信息节点;
12. 存在节点类* 取需求对应任务虚拟存在(需求节点类* 需求);
13. 需求.对应任务 -> 任务主信息.任务虚拟存在
14. 任务初始化至少需要：
15. 初始优先级
16. 任务初始化流程应为：
17. 8. 将 D.对应任务 = N，并把 N 加入 D.任务列表。
18. └─ 对应任务 = N001
19. 实例通过概念集对应多个概念。
20. 新建任务实例时只能自动实例化任务通用核心特征和来源需求锚定特征。
21. 任务独有特征类型必须等方法虚拟存在声明后再挂载。
22. 优先级
23. 服务触发事实、目标状态、服务前状态、权限边界、需求优先级等。
24. 其中 `CompositeCoordinationFeatures` 也必须来自组合方法虚拟存在，不得由任务临时发明。
25. 任务虚拟存在可以根据自身状态和自我治理需要改变特征类型集合，但这种改变必须表现为：
26. 任务自身和治理需要
27. 不得解释为：
28. 任务虚拟存在因为觉得需要，就直接增加任意特征类型。
29. 每个由方法带入的任务特征类型都必须形成挂载记录：
30. 方法切换时不得原地删除旧特征或覆盖旧规则版本。必须：
31. | 需求本身 | `N.对应需求` | 结构引用，权威 |
32. | 需求优先级 | `N.调度优先级` + `T_exist.优先级镜像` | N 权威，T_exist 可查询 |
33. B-1234飞机产生需求：需要加油
34. ├─ 对应需求 = D001加油需求
35. ├─ 调度优先级 = 普通 / 由需求决定
36. 需求.对应任务
37. 任务信息节点.对应需求
38. 初始化阶段不得把第三层提前写成任务天然特征。通用核心特征可参考概念路径：
39. 填值优先级建议：
40. 不要让通用概念默认值覆盖需求给出的值。需求是这次任务的动因，它的内容优先。
41. 独有特征类型的填值优先级不得从任务概念默认值直接开始，必须先确认来源方法虚拟存在：
42. 若尚未选择方法，任务只能保留候选方法集、当前方法为空、待筹办或待重筹办状态，不得提前创建无来源的独有特征槽。
43. 方法根节点应该在存在下，不应该在任务根节点下。
44. D.对应任务 = N
45. 则优先复用 N，不再新建任务。
46. 允许：
47. 接口应返回任务信息节点，而不是任务虚拟存在：
48. // 1. 如果来源需求已有未结束任务信息节点，优先复用。
49. 设置优先级镜像特征(T_exist, 来源需求);
50. 来源需求->主信息.对应任务 = 引用(N);
51. 任务根据需求初始化时，禁止：
52. 2. 让需求直接以任务虚拟存在作为对应任务权威入口。
53. 3. 新增需求.对应任务实例 作为并列权威字段。
54. 7. 需求.对应任务 仍指向任务信息节点。
55. 需求节点仍然通过 对应任务 指向任务信息节点。

## 4. 使用边界

1. 本规范用于计划、详细设计、代码修改和验收前的规则回读。
2. 本规范不能作为代码实现完成、业务闭环完成、自我苏醒完成或初步成熟完成的证据。
3. 需要展开历史论证时，只能把 `规范/旧规范/` 中的原文件作为追溯材料，不得让旧文件反向覆盖本规范。

## 修订记录

| 日期 | 版本 | 修订内容 |
| --- | --- | --- |
| 20260701 | v1.0 | 由历史来源修订迁回为正式子规范，接入当前规范目录。 |
