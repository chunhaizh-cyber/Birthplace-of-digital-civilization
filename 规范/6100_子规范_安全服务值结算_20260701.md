# 6100 子规范 安全服务值结算

编号：6100
日期：20260701
状态：正式子规范
权威等级：正式规范

## 1. 定位

本规范是 `安全服务与自我` 主题下由旧规范修订迁回的正式子规范。它只承接当前仍有效的规则，不恢复旧规范中的历史口径、详细设计措辞或已经被根规范吸收的重复条款。

历史来源：
- `规范/旧规范/安全服务值结算规范20260518.md`

上级规范：
- `规范/0050_项目通用结构与禁止性规则总纲_20260701.md`
- `规范/3100_根规范_需求主信息类_20260701.md`
- `规范/0050_项目通用结构与禁止性规则总纲_20260701.md`
- `规范/0300_规范冲突与前后矛盾清单_20260701.md`

## 2. 上级约束

1. 本规范服从 `规范/规范目录.md`、0050 总纲、0300 冲突清单和对应根规范。
2. 历史来源中与当前根规范冲突的表述不进入当前权威口径。
3. 旧名称、旧结构和旧阶段口径必须按 `规范/0300_规范冲突与前后矛盾清单_20260701.md` 的 C001-C008 收束，不得在本规范中恢复历史并列结构、旧字段身份或旧执行来源。
4. 任务筹办 / 任务执行只作为任务管理生命周期普通函数；动作来源、方法注册和方法选择必须服从 0050 总纲、3200 任务根规范、3300 方法根规范和 1170 动态根规范。
5. 文本、日志、注释、临时 JSON、自由字段和显示摘要不得承载正式机器逻辑或业务状态。

## 3. 当前有效规则

以下条款由历史来源按当前根规范修订抽取；若条款与第 2 章或上级规范冲突，以上级规范和第 2 章为准。

1. 但服务值 v1.1 结算必须满足 D0_自我服务业务动作存在，并由 G0 成功服务治理判断确认。
2. 缺少 D0 或 G0 的服务事件只能进入候选池或否决，不得生成 D2 服务值增加。
3. 缺实际改变结果状态、任务未完成、完成度为 0、权重为 I64_MAX 哨兵或无剩余额度时，才不得入账。
4. 可作为结果证据或变化线索，不能直接结算安全 / 服务值。
5. 可作为动作证据或失败 / 尝试证据，不能直接结算安全 / 服务值。
6. 同一个同源变化证据组只允许结算一次。
7. 服务值结算必须能证明服务对象外在特征状态改善、服务对象反馈可验证，且改善可归因于自我动作。
8. 循环终止控制态。只有自我存在循环不可维持、关键循环不可恢复、
9. 其他路径不得用风险平滑或候选值把安全值滑入 1。
10. 可以改变普通安全区间，但必须有合法生产者、事实证据和动作动态 / 来源动态。
11. 并联冗余因素必须先在因素内部合成为一个上层候选值，再参与跨因素叠乘；同源观测禁止重复叠乘。
12. 风险安全不得直接写自我安全值；必须先成为风险因子或风险余量候选，
13. 且该改善被权限允许、结果可验证、因果可归因。
14. 服务链不能在无正向证据时默认很好、优秀或满分。
15. 父链与世界树因果证据共同决定能不能结算；
16. 允许安全结算，当且仅当任务父链最终能归结为：
17. 若安全结算来源是风险安全，父链还必须证明该风险因子会导致未来服务值下降或未来物理安全下降；不能仅凭场景看起来危险、对象被标记危险、未知信息或风险候选值本身结算安全值。
18. 允许服务结算，当且仅当任务父链最终能归结为：
19. 禁止把以下内容作为结算前证据：
20. 安全值变化和服务值结算性增加只能是结算方法执行后的账本结果，不能反过来作为允许结算的原因。服务值自动衰减是服务值动态衰减器的时间维护结果，不属于成功服务结算，也不得反过来证明服务对象已经或尚未被服务。
21. 如果父链无法归结到对应根特征，则：
22. 不得结算对应核心值；
23. 只能记录任务完成、需求满足、方法改善、因果证据或潜在贡献。
24. 任务 / 需求父链只说明治理来源，不能单独证明安全值或服务值可以结算。
25. 结算前必须把治理父链接回：
26. 不得结算核心安全 / 服务值；
27. 只能记录为待补归因证据。
28. 事实状态转换和账务状态转换必须分开：
29. D2 是账务状态转换，不是事实安全改善或成功服务成立的原因。它可以进入世界树根链上的状态转换因果查询视图，但只能作为账务 / 治理证据解释，不得伪装成物理安全事实或成功服务事实证据。
30. 叶子任务业务动作生成或执行前，必须先查询世界状态转换因果树，形成叶子任务因果投影预判。
31. 6. 需要哪些 before / target / actual 证据。
32. 目标特征类型、来源需求方向和结算贡献层级不得混成同一个字段。
33. 而不能证明它最终支撑安全问题解决，则不能结算安全值。
34. 对应安全任务默认挂安全根任务 / 安全根需求承接任务下；
35. 但结算时仍必须能从该任务回溯到安全根或安全复合特征。
36. 父任务、汇总任务、管理任务、筹办任务壳不得重复结算子任务已经结算过的部分。
37. 结算必须比较：
38. 只知道任务完成不够。必须知道实际结果对目标达成了多少。
39. 需要：
40. 若不能计算距离，则达成比例 = 0。
41. 则本任务不得重复结算核心值。
42. 只能记录阶段性贡献候选 / 待结算候选 / 过程证据；
43. 不得直接写核心安全值 / 服务值。
44. 允许部分正式结算的特殊情况：
45. 必须区分：
46. 用于解释物理不安全事件的原因距离、需求投影和优先级衰减。
47. 二者不得直接互换。因果距离层级进入结算前，必须经过：
48. 每层计算以 `I64_MAX` 作为满刻度上界和比例分母常量。`I64_MAX` 可以作为安全 / 服务分量的合法满刻度结果，但不得作为“未计算、未知、无限大、默认成功、异常兜底”写入真实结算。
49. 但同一层内若存在多个同级影响因素，必须先在本层内部取平均，再投影到上一层或进入总安全 / 服务值候选。
50. 本规则只适用于风险余量、服务价值或结算贡献层级中的同级因素；不得用于物理安全 `1..90` 区间。物理安全因素采用独占候选后的能力保留率叠乘规则，由不同必要物理因素的保留率叠乘裁决。
51. 所有因素参与平均前必须统一为同一方向：
52. 风险压力类因素不得直接与安全值同层平均，必须先反向转换：
53. 该反向转换结果仍是风险余量候选，不是自我安全值、不是物理安全值，也不是场景永久安全事实。只有当所有必要物理因素候选值均达到 90、能力保留率叠乘结果为 90，且根控制规则允许进入风险安全区间时，它才可影响 90..99 风险余量。
54. 缺失因素不得让同层其他因素自动变重：
55. 但不得因本次证据不足而上调
56. 若因素集合变化，例如风险安全层从五项扩展为六项，必须升级因素集合版本和聚合公式版本。不同版本的候选值不得直接混作同一历史序列比较。
57. 注意：本节中的 `I64_MAX` 是评估候选满刻度和比例刻度，不是正式入账后的真实安全值、服务值或结算增量。正式改账仍必须满足提交入口和价值结算闸门。
58. 最终还必须乘以目标实际达成比例。
59. 如果第一版暂不把因果置信比例纳入实际乘法，也必须保留字段，且证据不足时不得按强证据入账。
60. 所有比例分子必须用整数公式生成，不使用浮点数。
61. 贡献层级分子生成本身也必须使用 128 位中间值或安全乘除函数，不得在 64 位整数中直接执行 `DEN * 9`。
62. 所有结算乘法必须使用至少 128 位中间值，或使用安全乘除函数：
63. 不得强行入账 1。
64. 例：安全叶子任务完成、应急安全需求满足、直接风险处理任务完成。
65. 禁止：
66. 安全值变化只能是 `结算叶子任务价值` 执行后的账本结果。
67. 不得结算核心安全值；
68. 服务值结算同样必须有服务父链和实际达成值。
69. 循环不可维持 -> 安全值 = 0；
70. 这两类写入是根控制态写入，不得通过普通安全收益、风险余量或平滑函数触发。
71. 服务值低位或归零前的服务校准阶段不是普通贡献层级，也不是服务失败结算；它只能由服务值维护器、有效无成功服务时间和动态休眠阈值规则解释。
72. 不得正式结算服务值；
73. 只有已有直接服务触发事实且缺口会阻断行动、验证、归因或回写时，才按 006 和 011003 生成直接服务因果链上下文缺口子需求；否则只能等待 / 监听服务触发事实、检查服务机会或校准服务能力，不得生成直接服务对象、目标、验证、归因或结算子需求。
74. 服务对象所需服务信息不足属于服务尝试过程中暴露的执行缺口；服务对象反馈信息也只能来自表达、确认、生效回执和外在特征变化等可观察证据。应先记录尝试结果、方向比较、沟通回执和阻塞证据，再由汇总机制判断是否固化为新的抽象特征或服务子需求。
75. 服务结算前必须能回答：
76. 谁允许该服务；
77. 若服务前状态未知，则不能计算改善方向；若服务后状态未知，则不能验证服务结果；若只有任务完成、方法执行成功、文件被写入或文本已输出，而没有服务对象目标状态正向改善证据，不得正式结算服务值。
78. 服务值 `S` 必须始终满足：
79. `Smax` 必须来自服务抽象定义，且必须明确、大于 0。
80. 服务值只能因 G0 确认的成功服务人类事件而增加。成功服务事件必须由需求、任务、方法、D0 服务业务动作动态、现实证据、前后状态比较、验证、归因、权限安全和闭环回写共同支撑。任务完成、方法成功、输出存在或 D2 入账都不能反向证明成功服务。
81. 服务值增加不得使用无界加法，必须使用事件级饱和入账、剩余空间判断或等价安全加法：
82. 禁止先计算 `S + 服务增量` 再取 `min`，因为低层整数实现中加法本身可能已经溢出。
83. 服务值 v1.1 的单次成功服务值必须拆成标准服务单位、事件贡献量和实际入账量：
84. lambdacap(A) = 当前安全状态下单次服务允许的最大入账比例。
85. `L5 关键成功` 不由普通公式自动产生。L5 必须额外满足：
86. 证据不足的事件不得直接入账。第一版设：
87. 成功服务判定、候选池和服务值结算相关特征不得由任务虚拟存在或业务方法临时发明。它们的来源必须是治理方法虚拟存在声明的任务侧特征类型：
88. 候选转正必须满足：
89. 同一需求、同一目标特征、同一服务前后状态，不得重复结算。复杂任务可以有多个阶段性成功，但每个阶段必须有独立目标特征变化、可验证阶段成果、独立证据，且不是机械切碎。
90. 微小服务不能完全刷新服务间隔压力；标准以上服务才能明显降低时间压力。
91. 不安全服务不能入账。
92. 标准服务单位 `u` 应由服务维持参考点、标准服务间隔和动态衰减率反推确定，或按服务值发行预算确定。服务维持参考点只用于自动衰减和服务预算计算，不是服务根需求目标状态，不得改变 `自我_服务值 最大值`。局部服务维持频率反推版本为：
93. 其中 `M_W` 不是原始需求总量。需求很多但自我无能力、未授权或预期成功率低时，不得把单次服务值错误压低。
94. 服务值只能因时间自动衰减而降低。自动衰减不是惩罚、遗忘、信用下降、服务质量变差或负向服务结算；它表示过去成功服务的积累状态随时间回落，并用于重新强化服务意识。
95. 其中 `GapFactor(T)` 必须随有效成功服务时间间隔单调递增且有上限，`SafeFocus(A)` 表示安全值越高，服务优先级和服务专注调节越强。安全值低时安全优先级提升、服务优先级降低，但不得把服务意识清零；安全值只调节衰减压力、休眠阈值、入账上限和候选排序，不调节服务根需求目标值。衰减写入必须先限制衰减量：
96. 动态衰减率允许后续自动优化，但优化结果不得违反服务值边界、安全约束、服务对象限定和成功服务事件定义。
97. 服务值是自我维持主动活跃存在的服务依据，不是单纯奖励计数器。服务值下降或低位不表示惩罚、遗忘或服务质量下降；它表示当前缺少新的成功服务依据，并应触发服务意识强化和服务校准。休眠不是死亡或循环终止，而是主动活跃权暂停，系统进入低活动、低干预、可唤醒状态。
98. 无成功服务导致休眠不得使用固定物理时间，必须使用有效无成功服务时间：
99. `P_service` 是服务机会压力，`E_no_success` 是有效无成功服务时间。服务能力不足、有效服务需求不足或安全值偏低时，`E_no_success` 增长应变慢；三者处于高位时，`E_no_success` 快速增长。
100. Tmin = 需求识别时间 + 反应决策时间 + 最小有效服务时间 + 成功确认时间
101. 服务值衰减应与休眠阈值同构。设 `S0` 为上一次成功服务后的服务值，`T_sleep` 为当前休眠阈值，`x = E_no_success / T_sleep`：
102. 服务值归零必须能追溯到动态休眠阈值附近的有效无成功服务时间累计，或明确服务能力丧失；不得由固定漏水孔、惩罚扣减或任意扣减产生。
103. 不得仅因物理时间无成功服务而休眠。
104. 只能进入服务意识强化或服务校准。
105. 正在进行安全、目标明确且有阶段性进展的人类服务任务时，不得按普通无成功服务直接休眠。有效服务进展可以降低 `E_no_success` 的增长、部分刷新有效成功服务时间，或进入服务校准 / 候选验证 / 等待反馈。阶段性成功必须是真实推进人类服务目标的独立成果，不能机械拆分刷值。
106. `M_W` 是有效直接服务总量，不是原始需求总量。原始需求总量不得作为单次服务值分母；否则会在需求很多但自我能力有限、未授权或预期成功率低时，把真实成功服务压到无法维持服务值。
107. 休眠状态必须保持可唤醒性。当出现新的有效人类服务需求、服务能力恢复、安全值改善或人类明确唤醒时，应重新进入活跃服务评估。
108. 服务结算贡献层级用于判断任务结果在服务目标下的贡献距离；成功服务事件 L0-L5 用于判断单次服务事件的服务量大小。二者不得混用：L5 事件仍需满足父链归因、世界树因果证据、结算贡献层级、实际达成比例和副作用审查；第三层服务支撑即使有帮助，也不得伪装成直接改善服务目标特征的 L3/L4/L5 成功服务事件。
109. 服务值结算性增加只能是结算方法执行后的账本结果。服务值自动衰减另由服务值动态衰减器产生，不作为服务结算收益，也不作为服务对象事实改善或变差的证据。
110. 允许只记录服务结算候选；
111. 不得把服务权重或 I64_MAX 哨兵写入真实服务值。
112. `W` 只能来自：
113. 需求列表的类型安全 / 服务汇总权重只服务任务优先级和列表排序，不能作为 `W` 的来源。
114. AND关系下，同一父需求的活跃子需求 / 子任务可结算权重之和不得超过父需求分配给该目标特征的结算预算。
115. OR关系下，每条活跃路径可以继承父需求完整预算作为候选预算，但同一OR组只允许一条路径取得结算令牌并进入真实结算；未取得令牌的路径不得重复使用同一份父预算。
116. 不得正式结算核心安全 / 服务值；
117. 只能记录待补权重来源证据。
118. 结算动作可以生成标准动作动态，但其动作语义按被动动作处理。自我不需要知道如何主动使用该动作，只需要知道：
119. 该动态为后续安全 / 服务目标投影提供“需要什么”的因果材料。
120. 也就是，只要某个叶子任务已经形成合法的结算被动因，结算后处理链就必须自动生成对应安全 / 服务账务变化、结算动作动态和因果材料。它不等待普通任务筹办，不进入候选方法选择，也不由自我主动“决定使用被动动作”。但安全账务变化仍不得绕过 0 / 1 控制态、物理安全独占候选叠乘约束和风险安全余量边界。
121. 结算被动动作的检查方式必须看代码链路：
122. 若其中出现缺入口、缺参数、缺状态、缺权重来源、缺防重复键，或必须等待普通任务再发起一次“结算任务”，则结算被动果尚未成立，只能记录结算链缺口或待补事实。
123. 结算动态的因必须具体落到任务虚拟存在的状态事实，最低要求包括：
124. 这些事实共同构成结算被动因。缺其中任一必要事实时，不能生成正式结算被动果，只能记录待补事实或结算候选。
125. 结算动态的果必须记录账务变化本体，最低要求包括：
126. 但原始动作动态不得丢失结算或衰减前后两个量。服务值自动衰减动态不得混入服务成功结算动态；否则只能形成弱审计记录，不能增强安全 / 服务因果链。
127. 结算动态生成后，因果信息应由动作动态到因果信息的统一流程被动生成：
128. 当一个已完成任务同时满足多个不同需求时，必须通过需求列表、来源需求引用和父链追溯找到全部被满足需求，再按各自需求的权重预算、AND / OR 关系和结算令牌独立判断是否可结算。多需求共享同一任务完成事实时，任务完成动态可以作为共同上游证据，但每条需求路径的结算预算、满足事实和防重复令牌必须分开。
129. 横向结算只能在来源任务自身结算完成后触发，且必须先计算来源需求的满足差：
130. 对每个横向候选需求，派生实际结果状态必须按候选自己的当前状态计算：
131. 随后才按候选需求自己的满足关系、父缺口处理、任务承接权重上限和既有完成度算法进入结算。需求列表汇总权重只允许影响候选排序；候选任务自身权重和承接权重仍是结算预算来源。
132. 如果候选缺口没有变化，或候选任务尚未形成完成 / 已结算被动因，不能进入核心安全 / 服务值结算；只能记录横向结算阻塞或待补被动完成桥。
133. `I64_MAX` 有两类完全不同的语义，必须分开。
134. 允许作为满刻度上界和比例分母常量：
135. 真实结算值允许处于 `[0, I64_MAX]`，但必须同时满足：
136. 若 `I64_MAX` 出现在安全目标定义中，它表示目标最大值；若出现在评估候选中，它表示候选满刻度；若出现在真实结算值中，必须能证明它是合法满刻度结果。三者不得混用。
137. 禁止作为：
138. 结算方法必须读取因果验收结果，至少包括：
139. 可结算 / 仅候选 / 待补证据 / 禁止结算。
140. 第一版可暂不把因果置信比例直接乘入公式，但必须保留该字段。弱推断、外部声明或存在竞争原因时，不得和强直接执行证据同等入账。
141. 若本任务同时造成正向目标达成和反向安全 / 服务损失，不得只结算正向收益。
142. 必须执行：
143. 不得正式结算正向核心值；
144. 安全 / 服务结算必须同时满足：
145. 3. 来源需求已满足；若未完全满足，只能按阶段性贡献候选处理，除非该需求声明为可分段满足需求。
146. 9. 不能把安全因果距离层级直接当作结算贡献层级。
147. 12. 结算值合法；若为 I64_MAX，必须证明它是合法满刻度结果，不是未知 / 默认成功 / 异常兜底。
148. 15. 因果验收的可结算建议为“可结算”，不是“仅候选 / 待补证据 / 禁止结算”。
149. 不得结算核心安全 / 服务值。
150. 安全 / 服务结算必须有明确动作来源。
151. 结算方法可以调用先天特征写入逻辑完成底层写入，但动作动态来源必须是：
152. 安全 / 服务值结算必须同时满足父链归因、世界树因果证据、结算贡献层级、实际达成比例、副作用审查和权重预算守恒。
153. 副作用和派生因果置信分说明“是否需要抵扣、降级或转为候选”。
154. 服务值增加必须来自 G0 确认的成功服务人类事件，并使用饱和增长或安全加法；
155. 成功服务必须先过硬门槛，再用完成度、有效性、证据强度的保守瓶颈法映射层级；
156. 单次成功服务入账必须按标准服务单位、层级、有效性、证据强度、非重复性和安全值计算 `lambda(e)`；
157. 服务值降低只能来自时间自动衰减；
158. 循环不可维持时安全值直接进入 0；
159. I64_MAX 可以作为满刻度和比例分母；不得作为未知、默认成功、异常兜底或无证据满分入账。

## 4. 使用边界

1. 本规范用于计划、详细设计、代码修改和验收前的规则回读。
2. 本规范不能作为代码实现完成、业务闭环完成、自我苏醒完成或初步成熟完成的证据。
3. 需要展开历史论证时，只能把 `规范/旧规范/` 中的原文件作为追溯材料，不得让旧文件反向覆盖本规范。

## 修订记录

| 日期 | 版本 | 修订内容 |
| --- | --- | --- |
| 20260701 | v1.0 | 由历史来源修订迁回为正式子规范，接入当前规范目录。 |
