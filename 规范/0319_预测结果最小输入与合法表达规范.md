# 0319 预测结果最小输入与合法表达规范

## 1. 目标

本文用于把 `预测结果` 在当前体系中的最小输入面和合法表达方式固定下来。

本文要回答的是：

- `预测结果` 到底是什么
- 它最少要怎样表达，才允许进入根层重判或执行前门控
- 为什么不能把自然语言预期、方法名、主键字段组合、摘要直接当预测结果
- 预测结果如何与当前值比较并生成 `预测结果方向`


## 2. 上下位关系

上位规范：

- [0315_根层重判结果与执行前允许边界规范.md](D:\鱼巢\规范\0315_根层重判结果与执行前允许边界规范.md)
- [0320_预测结果方向与需求方向对齐判定表规范.md](D:\鱼巢\规范\0320_预测结果方向与需求方向对齐判定表规范.md)
- [0316_根层重判结果最小字段表规范.md](D:\鱼巢\规范\0316_根层重判结果最小字段表规范.md)
- [0317_执行前门控最小输入与判定顺序规范.md](D:\鱼巢\规范\0317_执行前门控最小输入与判定顺序规范.md)
- [0565_判定依据分层与裁决顺序规范.md](D:\鱼巢\规范\0565_判定依据分层与裁决顺序规范.md)

互补规范：

- [1000_任务执行类实现规范（当前顺序执行骨架）.md](D:\鱼巢\规范\1000_任务执行类实现规范（当前顺序执行骨架）.md)
- [1213_任务管理一步治理结果最小返回面规范.md](D:\鱼巢\规范\1213_任务管理一步治理结果最小返回面规范.md)
- [0014_自我线程主循环最小职责表规范.md](D:\鱼巢\规范\0014_自我线程主循环最小职责表规范.md)


## 3. 核心结论

必须固定以下结论：

1. `预测结果` 不是一句“这个方法大概会成功”的口头预期，而是一组可比较的 `带值特征`。
2. `预测结果` 若要进入根层重判、执行前门控或任务执行比较链，必须先满足合法表达条件。
3. `方法名`、`动作名`、`结果主键`、`自然语言说明`、`摘要文本` 都不能直接充当预测结果本体。
4. 合法预测结果至少必须能回答：
   - 它预测了哪个特征类型
   - 该特征预测结果状态是什么
   - 这个预测结果状态如何与当前状态比较
5. `预测结果方向` 只能来自 `预测结果状态 - 当前状态` 的比较结果；其中 `预测结果状态` 可以是显式 `预测值`，也可以是在初始化结果节点阶段由步骤 `目标状态` 充当的初始化预测状态，不能直接从“看起来会变好/会变坏”的文字判断中生成。
6. `预测结果` 可以进入根层重判，也可以进入方法/任务层的候选比较，但它本身不是最终裁判依据。
7. 若预测结果不能落成可比较的带值特征，则它最多只能保留为候选描述或说明，不得直接进入正式判定链。

一句话收口：

`预测结果只有在被结构化表达为一组可比较的带值特征之后，才允许进入正式判定链；否则它只是描述，不是判断输入。`


## 4. 定义

`预测结果` 指的是：

- 当前方法、步骤或治理路径对“执行后结果特征会怎样变化”的结构化预期表达

它回答的是：

- 哪个特征会变化
- 变化后预计到哪个状态或值
- 预计变化方向是什么

它不是：

- 方法名字
- 结果主键
- 结果摘要
- 成功/失败口号
- 任意自然语言描述


## 5. 合法表达的最小形式

### 5.1 最小合法单位

一个最小合法预测结果项，至少必须包含：

- `特征类型`
- `预测值`，或可比较的 `初始化目标状态`

若要正式进入比较链，还必须能取得：

- `当前值`

因此最小比较单位应为：

```text
特征类型 + 当前值 + 预测结果状态
```

### 5.2 最小合法结果组

一个合法预测结果，最少应表达为：

- `一个或多个预测结果项`

每一项都应是：

- `特征类型 + 预测值`
- 或 `特征类型 + 初始化目标状态`

若某一项不能补齐 `当前值`，则该项当前不允许进入正式方向比较。

补充固定：

- 当步骤节点刚建立、方法侧还未展开出更细的显式预测值时，允许先用该步骤的 `目标状态` 作为初始化预测状态，创建初始化结果节点的方向信息。
- 这条初始化方向仍然只是“预测侧占位方向”，后续若方法结果或实际结果给出了更明确的结果状态，应继续按更明确的结果状态刷新。


## 6. 非法表达清单

以下内容都不能直接当 `预测结果`：

1. `方法名`
2. `动作名`
3. `结果主键`
4. `成功率高`
5. `看起来会更好`
6. `预计能解决问题`
7. `经验上通常有效`
8. 任何未落成带值特征的自然语言描述

这些内容最多只能作为：

- 召回辅助
- 说明文字
- 候选证据

不得直接进入：

- 根层重判
- 执行前门控
- 任务推进方向判定


## 7. 预测结果方向的生成规则

`预测结果方向` 必须按以下链条生成：

```text
预测结果项
-> 取得同特征类型的当前值
-> 取得显式预测值或初始化目标状态
-> 比较 (预测结果状态 - 当前值)
-> 生成 大于 / 小于 / 等于
```

必须固定：

1. `预测结果方向` 只能落在：
   - `大于`
   - `小于`
   - `等于`
2. `预测结果方向` 不得直接手写成抽象词，例如：
   - `变好`
   - `变坏`
   - `可能有利`
3. 若无法取得 `当前值`，则当前不允许正式生成该项方向。
4. 初始化结果节点的方向生成，属于合法预测方向生成的一种早期占位形式；它仍然必须满足“同特征类型 + 同当前值基准”的比较前提。


## 8. 与需求方向的关系

`预测结果` 进入正式判定链时，必须和 `需求方向` 对照。

最小链条应固定为：

```text
需求值 - 当前值 -> 需求方向
预测结果状态 - 当前值 -> 预测结果方向
比较两者方向是否一致
```

必须固定：

1. 若 `需求方向 = 等于`，则 `预测结果方向` 只能是 `等于`。
2. 若 `需求方向 ≠ 等于`，则 `预测结果方向` 必须等于 `需求方向`，才允许继续作为正向候选。
3. `预测结果方向` 与 `需求方向` 不一致时，当前预测结果不得直接作为放行依据。

更细的逐项判定表，按 [0320_预测结果方向与需求方向对齐判定表规范.md](D:\鱼巢\规范\0320_预测结果方向与需求方向对齐判定表规范.md) 执行。


## 9. 与根层重判的关系

`预测结果` 进入根层重判时，必须先满足：

1. 已被表达为 `一组带值特征`
2. 已能与当前值比较
3. 已生成 `预测结果方向`

然后才允许作为以下输入之一进入根层重判：

- `预测结果方向`
- `预测结果特征摘要`

必须固定：

1. 进入根层重判的，不是原始自然语言预测，而是比较后的结果。
2. 根层重判消费 `预测结果方向`，不是直接消费“方法看起来会成功”。


## 10. 与任务执行器的关系

当前 [1000_任务执行类实现规范（当前顺序执行骨架）.md](D:\鱼巢\规范\1000_任务执行类实现规范（当前顺序执行骨架）.md) 中涉及的：

- `方法预测结果值`
- `预测值不能比当前值离目标更远`
- `预测方向不能与目标方向相反`

都应统一理解为：

- 预测结果必须先以带值特征表达
- 再通过比较生成方向
- 再由方向进入候选筛选或执行前判断


## 11. 严格禁止的混层

以下做法必须禁止：

1. 用 `方法名` 直接代表预测结果。
2. 用 `结果主键` 直接代表预测结果。
3. 用“预计有效”直接代表预测方向。
4. 未取得当前值就直接生成预测方向。
5. 把预测结果描述直接当成根层重判输入。
6. 把预测结果本体直接当成最终裁决依据。


## 12. 当前阶段工程结论

当前阶段可先按以下口径理解：

1. [0315_根层重判结果与执行前允许边界规范.md](D:\鱼巢\规范\0315_根层重判结果与执行前允许边界规范.md) 已经写死“预测结果可以进入根层重判，但必须先落成带值特征”。
2. 本文负责把“什么叫落成带值特征、什么叫合法预测结果表达”单独展开。
3. `预测结果方向` 与 `需求方向` 的最小单特征对齐规则，按 [0320_预测结果方向与需求方向对齐判定表规范.md](D:\鱼巢\规范\0320_预测结果方向与需求方向对齐判定表规范.md) 执行。
4. [1000_任务执行类实现规范（当前顺序执行骨架）.md](D:\鱼巢\规范\1000_任务执行类实现规范（当前顺序执行骨架）.md) 中现有预测链，后续都应按本文统一语义理解。
