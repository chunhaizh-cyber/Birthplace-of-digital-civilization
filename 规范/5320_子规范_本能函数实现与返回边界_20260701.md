# 5320 子规范 本能函数实现与返回边界

编号：5320
日期：20260701
状态：正式子规范
权威等级：正式规范

## 1. 定位

本规范是 `需求任务方法` 主题下由旧规范修订迁回的正式子规范。它只承接当前仍有效的规则，不恢复旧规范中的历史口径、详细设计措辞或已经被根规范吸收的重复条款。

历史来源：
- `规范/旧规范/本能函数实现规范20260430.md`
- `规范/旧规范/本能动作管理类返回信息规范20260430.md`

上级规范：
- `规范/3300_根规范_方法主信息类_20260701.md`
- `规范/0050_项目通用结构与禁止性规则总纲_20260701.md`
- `规范/0050_项目通用结构与禁止性规则总纲_20260701.md`
- `规范/0300_规范冲突与前后矛盾清单_20260701.md`

## 2. 上级约束

1. 本规范服从 `规范/规范目录.md`、0050 总纲、0300 冲突清单和对应根规范。
2. 历史来源中与当前根规范冲突的表述不进入当前权威口径。
3. 旧名称、旧结构和旧阶段口径必须按 `规范/0300_规范冲突与前后矛盾清单_20260701.md` 的 C001-C008 收束，不得在本规范中恢复历史并列结构、旧字段身份或旧执行来源。
4. 任务筹办 / 任务执行只作为任务管理生命周期普通函数；动作来源、方法注册和方法选择必须服从 0050 总纲、3200 任务根规范、3300 方法根规范和 1170 动态根规范。
5. 文本、日志、注释、临时 JSON、自由字段和显示摘要不得承载正式机器逻辑或业务状态。

## 3. 当前有效规则

以下条款由历史来源按当前根规范修订抽取；若条款与第 2 章或上级规范冲突，以上级规范和第 2 章为准。

1. 本文主要约束本能函数本体；当涉及任务链路时，以“需求节点对应任务的两阶段壳”为前提，不再以独立任务管理任务或学习任务作为长期权威结构。
2. 本能函数不得把方法名称、动作名称、显示文本、语素文本或函数名称作为执行语义依据。名称只能用于显示、日志和调试。执行语义必须落到特征类型、特征值、区间、抽象特征父子关系、限制包和方法能力包。
3. 本能方法是自我的最小可执行动作。一个函数只有具备明确输入参数、明确输出结果、明确动作入口和明确落账边界时，才可以作为本能方法；否则只能作为普通逻辑、历史入口、底层工具或待学习方法结构。
4. 最小可执行动作必须能回答：
5. 缺输入时不得猜测、默认补齐或继续执行；缺输出时不得把函数运行成功当成任务完成或需求满足。
6. `获取特征值` 若用于外部对象或场景，只能表示自我通过外设执行一次观测并产生新的观察事实。读取世界树中已经存在的特征快照只是普通工具或诊断读取，不能伪装成自我本能方法；通过双目相机获取特征值时，必须具备目标外设、目标宿主、目标特征类型、动作入口、输出结果和动作动态落账，未实现前不得进入默认可执行本能方法表或普通候选集。
7. 涉及双目相机、观察特征帧、当前场景识别、像素观察事实、空间候选、已确认观察存在、存在内部世界或 OpenGL / 控制面板复现时，方法职责分层以 `外设观察特征与自我场景认知本能方法分层规范20260525.md` 为准。外设方法不得宣称发现存在、确认像素归属或理解内部结构；这些认知结果只能由自我本能方法基于外设观察特征生成。
8. -> 对应任务
9. 本能函数失败原因应尽量输出为可派生需求的缺口事实；缺口不是任务终点，而是需求树继续生长的依据。
10. 任务虚拟存在必须把阶段事实写成实例特征：
11. 阶段、缺口和能力值不得只保存在临时结构体或控制分支中。
12. 返回值必须是 `存在节点类*`，表示本轮运行虚拟存在。
13. 返回值不表示单个功能结果。功能结果必须写入 `输出结果场景`，运行过程必须写入本轮运行虚拟存在，并可同步长期统计到方法虚拟存在。
14. 因此，标准函数形态只说明“如何被调用”，不自动证明该函数已经是合格本能方法。合格本能方法还必须在自身规格中声明：
15. 只返回运行虚拟存在、只写日志、只更新兼容成功码，均不能替代明确输出结果。
16. 每个本能函数内部必须按以下四个模块组织：
17. | 执行或匹配 | 对实际目标执行写回，或对候选方法执行匹配。不得在此阶段补造缺失语义。 |
18. 确保阶段不得因为节点已经存在就直接返回。确保完成后必须继续进入绑定、执行和输出。
19. 绑定失败、执行失败时，也必须进入输出阶段。只有无法形成有效运行虚拟存在时，才允许返回空。
20. 每个本能方法在读取输入、执行核心逻辑或处理目标方法之前，必须先确保：
21. 学习 / 练习类方法必须区分两套环境：
22. 目标方法缺口是业务缺口，可以进入状况回执或学习承接；本能方法自身环境缺失是方法系统自举失败，必须记录日志并报错，不允许静默兜底。
23. 本能函数中必须区分四类场景：
24. | 条件场景 | 方法自身 | 方法构建的抽象输入模板，声明本方法需要哪些输入特征。 |
25. 新本能函数应优先读取：
26. 输入条件包必须只承载本轮已经闭合的执行或匹配参数。若参数尚未闭合，应由上游 `查找方法` 和 `配对参数` 阶段完成，不得由执行函数猜测、默认补齐或按名称推断。
27. 可被查找的方法必须在方法虚拟存在中保存 `方法能力包`。
28. `查找方法` 只能用 `需求条件包 ⊆ 方法能力包` 判断候选，不得按方法名或动作名匹配。
29. 必须绑定：
30. | 需求条件包 | 表示任务想要什么。至少应有一个必需条件项。 |
31. | 查找限制包 | 限制抽象层数、允许值类型、允许目标特征上界、允许宿主上界等。 |
32. | 候选范围包 | 限定方法搜索根、是否包含禁用方法、是否允许当前方法自身等。 |
33. | 方法搜索根 | 缺失时可从当前方法首节点推导方法树根，但必须写入运行记录。 |
34. | 缓存方法首节点 | 运行期缓存；执行前必须按 `选中自我能力值` 重新校验或重解析。 |
35. | 缓存方法虚拟存在 | 运行期缓存；不能作为任务长期语义主键。 |
36. 1. 原始需求绑定包必须保留原始口径。
37. 2. 查找时可以向上抽象，但执行时不得把父级能力口径当成最终执行口径。
38. 在任务筹办中，`改变特征值` 不得作为“任意已确定目标特征类型”的通用候选方法被召回。候选方法必须是能够解释目标特征变化的语义方法；语义方法内部可调用本先天逻辑完成底层写入。
39. 执行差值或目标执行后值仍必须有明确来源；缺少改变量或改变量出处时，应由外层语义方法或任务筹办流程输出“确定执行差值或目标值 / 补齐可执行输入参数场景”的筹办条件需求，不得缺省补值，也不得把该缺口误判为无候选方法。
40. | 目标抽象特征 | 要改变的抽象特征。必须是抽象特征，不得是实例特征。 |
41. | 目标值类型 | 必须为 `类型_I64` 或兼容的 I64 值类型标识。 |
42. 执行值来源必须满足以下条件之一：
43. 若 `执行差值` 和 `目标执行后值` 同时存在，必须校验：
44. 不一致时必须参数绑定失败或执行失败，不得任选其一。
45. `改变特征值` 不得从输入参数场景读取当前值作为计算依据。
46. 执行前值必须从目标宿主下的目标实例特征读取：
47. 抽象特征只定义维度、区间和细化链，没有当前值。执行写回必须落到宿主下的实例特征。
48. 若未来允许实例化，也必须由显式 `实例缺失策略` 授权，且当前值初始化不得默认补 `0`。
49. 实际执行差值 必须命中 差值约束包
50. I64 加法和差值计算必须做溢出保护。默认溢出策略为不允许溢出；若启用饱和写入，必须在运行事实中记录发生溢出。
51. `不等于零` 不是单一区间，不得强行写成假区间。
52. V1 应表达为：
53. 执行时应展开为两个区间：
54. 成功或失败都必须返回本轮运行虚拟存在。
55. | 失败原因 | 失败时必须写。 |
56. 运行虚拟存在必须保存输入参数场景、输出结果场景、开始时间、结束时间、运行时长、方法虚拟存在和上述执行摘要。
57. 动作函数执行成功后，执行阶段应产出条件结果对样本：
58. 执行阶段不得直接更新结果符合次数、结果不符合次数、方法可用性或条件结果对稳定性。它只产出事实样本和运行账；条件结果对样本必须进入方法树归并阶段处理。
59. 方法虚拟存在只更新长期运行账：运行次数、动作执行成功/失败、累计运行时长、平均运行时长、最近运行虚拟存在、最近执行情况。不得把本次执行前值、执行后值当成方法自身固定属性。
60. 运行账、条件结果对归并和需求满足判断必须分开：
61. 更新同条件结果符合/不符合账必须先通过事实样本闸门：
62. `练习` 的职责不是泛泛学习，也不是从零生成方法结构。它只能在动作节点已经存在、且已有条件结果对可引用的前提下，完善参数、条件结果对和稳定性统计。
63. | 动作节点不存在或方法只有头节点 | 不生成动作、不再派生缺方法需求；只写执行入口或方法动作缺失事实，后续由治理链展开对应原子需求。 |
64. | 随机练习 | 输入条件包缺少目标特征类型或方向 / 目标值约束。 | `练习` 自己生成未出现过的随机候选实参；不得等待任务补默认参数，也不得把参数缺口直接扩展成新需求。 |
65. | 动作节点或执行入口 | 写执行入口或方法动作缺失事实，后续由治理链展开对应原子需求。 |
66. 若目标方法没有可调用执行入口，`练习` 只能写结构化缺口事实、叶子需求状态和建议下一步能力，不得自建候选动作、候选方法头或伪造试运行结果。可调用入口包括有效本能能力值、外部实现句柄或动作序列句柄。仅有方法名称或动作显示文本不算执行入口。
67. 候选实参和候选区间必须标记为候选或未验证。只有实际运行事实长期支持，条件结果对才能从候选区间升格为稳定区间。
68. 执行阶段调用目标方法前也必须做执行入口自检。执行前自检发现目标方法只有头节点、缺少动作节点或缺少可调用执行入口时，应写 `方法自检结果` 与具体执行缺失事实，并把当前任务阶段写为阻塞；缺失项由自我线程展开为原子需求，下一轮再由筹办补齐方法或由执行补齐条件。执行前自检可以记录能力包、形参包、结果包、条件结果对或试运行限制包缺失，但不应在执行阶段直接补造候选结构。
69. 旧的安全值、服务值专用动作可以保留为交互名称或历史入口，但执行语义应逐步统一进入 `改变特征值`。
70. 旧入口若仍接收 `改变量`，必须先在配对参数阶段转换为明确的 `执行差值` 或 `目标执行后值`，再由外层语义方法调用先天特征写入逻辑完成底层提交。
71. 失败不应静默。常见失败原因与派生需求：
72. 派生需求也必须用特征类型和特征值表达，不用名称表达。`练习` 的失败输出只写缺口事实和建议下一步能力，不直接创建派生需求；是否生成子需求由治理链裁决。
73. 1. 本能函数不得返回单个特征节点作为标准结果。
74. 2. 本能函数不得把输出结果直接塞进函数返回值。
75. 3. 本能函数不得把任务提供的输入参数场景当成方法自身条件场景。
76. 4. 本能函数不得把方法自身结果场景当成本轮输出结果场景。
77. 5. 本能函数不得在确保阶段提前返回成功。
78. 6. 本能函数不得跳过输出阶段。
79. 7. `查找方法` 不得按方法名称、动作名称、显示文本、语素文本或函数名称匹配。
80. 8. `查找方法` 不得直接执行选中方法或生成可执行输入参数场景。
81. 9. `改变特征值` 不得从输入条件包读取当前值作为执行前值。
82. 10. `改变特征值` 不得把抽象特征当成实例特征写当前值。
83. 11. `改变特征值` 不得缺省补执行差值或目标执行后值，唯一例外是差值约束包为 `[0,0]` 的保持场景。
84. 12. `不等于零` 不得表达成单个 I64 区间。
85. 13. `练习` 不得在没有执行入口时试运行目标方法。
86. 14. `练习` 不得生成新需求节点、方法头节点或动作节点。
87. 15. `练习` 不得从零生成候选条件结果对。
88. 16. `练习` 不得把候选实参、候选区间或候选条件结果对直接升格为稳定事实。
89. | 方法能力包 | 可被查找的方法必须声明能力包。 |
90. | 输入条件包 | 优先使用 `输入条件包`，兼容期才回退 `条件集合`。 |
91. | 当前值来源 | 执行前值必须来自目标实例特征。 |
92. | 差值约束 | 实际差值必须命中约束区间集合。 |
93. | 方法运行账 | 更新运行次数、动作执行情况、累计和平均运行时长。结果符合/不符合统计只能在有效结果验证样本成立后更新。 |
94. | 编译验证 | 修改后必须通过工程编译。 |
95. 状态：待按 007《禁止性规范20260611》继续收束；本文中旧兼容迁移口径不再作为实现依据。
96. 本规范用于替换“本能动作函数返回混合结果结构”的旧思路。后续实现不得继续把运行结果、执行结果、结构结果和动作动态混在同一个返回结构中。
97. 本能动作函数的返回值应表达“这次方法有没有正常运行起来”，即运行结果。
98. 运行结果应返回为一个虚拟存在。这个虚拟存在表示本轮本能函数调用形成的运行事实，可视为任务虚拟存在的同类运行账本，但作用域限定在本轮方法调用。
99. | 结构结果 | 本轮条件和结果对是否应沉淀到方法树 | 方法树的条件节点与结果节点 | 否，由执行桥或方法维护逻辑处理 |
100. 运行结果必须是虚拟存在，不是抽象特征定义本体，也不只是单个实例特征。
101. 4. 任务管理侧不应再读取自然语言备注作为权威判断依据。
102. 5. 禁止保留兼容迁移适配函数；运行结果必须直接形成运行虚拟存在中的特征化闭环事实，旧单特征返回读取方式不得作为当前机器判断路径。
103. 7. 本能函数必须继续进入解析绑定、执行和输出阶段；只有在无法形成有效运行虚拟存在时，才允许返回空。
104. 8. 当解析绑定或执行无法继续时，应在运行虚拟存在中写入返回参数信息、返回场景信息和错误状态，再由输出阶段返回该运行虚拟存在。
105. | 动作入口缺失 | -20007 | 需要补方法动作或重新筹办 |
106. | 本能动作未注册 | -20001 | 方法不可执行，提交方法可用性缺口 |
107. 优先落点：
108. 补位落点不是长期运行账本。任务管理侧消费后，应根据任务状态决定是否提交需求、挂起任务或重新筹办。
109. 执行结果必须写入输出结果场景。
110. 执行结果不直接改变任务状态。任务状态必须由任务管理界面线程读取运行结果和输出结果场景后统一改变。
111. 本能动作管理类不得在内部决定：
112. 4. 本能动作函数不得读取任务、需求、控制面板或线程调度状态。
113. 返回值为空表示执行入口自身无法形成有效运行虚拟存在。任务管理侧应将其视为运行结果未知，并记录事件。
114. 3. 方法是否应该被调度。
115. 检查失败时也应形成运行虚拟存在，供任务管理侧处理。优先挂在方法虚拟存在下；没有有效方法虚拟存在时，可临时挂在输出结果场景下。
116. 后续应删除该结构在核心接口中的使用。
117. 条件节点和结果节点必须成对维护。若本轮产生了可以沉淀的方法结构样本，应由执行桥或方法维护逻辑写入方法树。
118. 真实调用方法函数后，应由执行桥或动作执行体生成动作动态。任务管理侧可以通过输出结果场景或方法虚拟存在读取相关引用，但不得把任务消息本身当作真实动作动态。
119. 后续修改本能动作管理类时必须遵守：

## 4. 使用边界

1. 本规范用于计划、详细设计、代码修改和验收前的规则回读。
2. 本规范不能作为代码实现完成、业务闭环完成、自我苏醒完成或初步成熟完成的证据。
3. 需要展开历史论证时，只能把 `规范/旧规范/` 中的原文件作为追溯材料，不得让旧文件反向覆盖本规范。

## 修订记录

| 日期 | 版本 | 修订内容 |
| --- | --- | --- |
| 20260701 | v1.0 | 由历史来源修订迁回为正式子规范，接入当前规范目录。 |
