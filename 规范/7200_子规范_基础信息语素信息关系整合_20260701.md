# 7200 子规范 基础信息语素信息关系整合

编号：7200
日期：20260701
状态：正式子规范
权威等级：正式规范

## 1. 定位

本规范是 `存在语素与入口` 主题下由旧规范修订迁回的正式子规范。它只承接当前仍有效的规则，不恢复旧规范中的历史口径、详细设计措辞或已经被根规范吸收的重复条款。

历史来源：
- `规范/旧规范/基础信息语素信息关系整合规范20260509.md`

上级规范：
- `规范/2210_根规范_语素入口主信息类_20260701.md`
- `规范/0050_项目通用结构与禁止性规则总纲_20260701.md`
- `规范/0300_规范冲突与前后矛盾清单_20260701.md`

## 2. 上级约束

1. 本规范服从 `规范/规范目录.md`、0050 总纲、0300 冲突清单和对应根规范。
2. 历史来源中与当前根规范冲突的表述不进入当前权威口径。
3. 旧名称、旧结构和旧阶段口径必须按 `规范/0300_规范冲突与前后矛盾清单_20260701.md` 的 C001-C008 收束，不得在本规范中恢复历史并列结构、旧字段身份或旧执行来源。
4. 任务筹办 / 任务执行只作为任务管理生命周期普通函数；动作来源、方法注册和方法选择必须服从 0050 总纲、3200 任务根规范、3300 方法根规范和 1170 动态根规范。
5. 文本、日志、注释、临时 JSON、自由字段和显示摘要不得承载正式机器逻辑或业务状态。

## 3. 当前有效规则

以下条款由历史来源按当前根规范修订抽取；若条款与第 2 章或上级规范冲突，以上级规范和第 2 章为准。

1. > 2026-05-10 修订提示：本规范中的“词性”现在拆分为“人类词性”和“信息入口类型”。人类词性只提供自然语言候选；信息入口类型决定主信息类型、存储树和默认根。详见 [信息入口类型与信息存储树路由规范20260510.md](信息入口类型与信息存储树路由规范20260510.md)。本文后续保留的 `词性` 字样，在新实现中应优先理解为兼容字段或人类词性字段。
2. └─ 词性节点：保存 词性 + 对应基础信息类型 + 对应基础信息引用
3. 语素层不得吞掉基础信息层职责。基础信息层也不得复制词面字符串作为自己的权威语义来源。二者通过 `词性节点类*` 和 `可解析引用<基础信息节点类>` 建立稳定桥接。
4. `语素入口主信息类` 必须至少表达：
5. 枚举_主信息类型 对应基础信息类型 = 枚举_主信息类型::未定义;
6. 可解析引用<基础信息节点类> 对应基础信息{};
7. 词性节点不再维护“一个词性下挂多个语义项”的结构。若同一词面确实需要多个语义对象，应通过不同词性、不同上下文基础信息，或基础信息层自身的实例 / 派生关系表达。
8. 基础信息节点可以用 `主信息基类::名称`、`主信息基类::类型` 引用词性节点作为语言标签，但不得把词面字符串复制为权威语义源。
9. 语素层必须提供词性到基础信息类型的默认推断入口：
10. 语素_推断词性对应基础信息类型(...);
11. `抽象特征` 与 `特征` 允许兼容绑定。原因是同一个形容词入口既可能表示特征模板，也可能在具体场景中绑定到特征实例。
12. `基础信息类` 必须提供以词性节点为入口的模板查找 / 创建能力：
13. `确保词性模板` 创建出的基础信息节点必须回写到词性节点的：
14. 对应基础信息类型
15. 对应基础信息
16. 若已有兼容模板，不得重复创建同一词性入口下同类型模板。
17. `语素类` 必须提供从语言入口到基础信息模板的桥接函数：
18. 基础信息节点类* 确保词性对应基础信息(
19. 基础信息节点类* 查找词性对应基础信息(...) const;
20. 枚举_主信息类型 获取词性对应基础信息类型(...) const;
21. auto* 飞机存在 = 语素集.确保词性对应基础信息("飞机", 枚举_词性::名词);
22. auto* 安全特征 = 语素集.确保词性对应基础信息("安全", 枚举_词性::形容词);
23. auto* 严重修饰 = 语素集.确保词性对应基础信息("严重", 枚举_词性::副词);
24. auto* 移动动态 = 语素集.确保词性对应基础信息("移动", 枚举_词性::动词);
25. 特殊主信息还必须参与引用检查：
26. `查找引用词性节点的基础信息` 不只检查 `名称 / 类型`，也必须检查这些特殊字段。
27. 必须为语言功能项提供轻量基础信息类型：
28. 这类节点只保留语言结构信息，不得冒充存在、动态或特征。
29. `绑定词性词到场景基础信息(...)` 不得丢弃 `场景` 参数。
30. 绑定基础信息后，应按基础信息类型写入场景索引：
31. 1. 查找词面对应词节点。
32. 3. 优先返回当前场景索引中命中的基础信息。
33. `添加二次特征词` 必须有实际实现，不得为空。
34. 它应基于：
35. 生成后应：
36. 3. 确保对应基础信息模板存在。
37. 同一词性节点下，不得重复创建相同基础信息类型的通用模板。
38. 场景绑定不得破坏通用模板去重。若同一词性入口在不同场景中指向不同具体实例，应通过场景索引和基础信息实例关系表达。
39. 禁止以下做法：
40. 实现应满足：
41. 场景绑定后，场景查找优先返回场景内绑定对象。

## 4. 使用边界

1. 本规范用于计划、详细设计、代码修改和验收前的规则回读。
2. 本规范不能作为代码实现完成、业务闭环完成、自我苏醒完成或初步成熟完成的证据。
3. 需要展开历史论证时，只能把 `规范/旧规范/` 中的原文件作为追溯材料，不得让旧文件反向覆盖本规范。

## 修订记录

| 日期 | 版本 | 修订内容 |
| --- | --- | --- |
| 20260701 | v1.0 | 由历史来源修订迁回为正式子规范，接入当前规范目录。 |
