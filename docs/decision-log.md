# Decision Log

## 2026-07-04：E6/P8 外设观察先验发布器第一版闭合

背景：

按计划索引连续执行 E6/P8 `外设观察先验发布器实现映射计划`。S0-S6 已完成代码事实扫描、发布入口、SceneVoxelPrior 到目标观察约束映射、D455 非阻塞读取、视觉融合输入接入、发布器缺口标准化和判断型状态治理。

决定：

将 E6/P8 标记为第一版闭合，并把计划索引推进到 E7/P5 `像素归属候选分裂与局部体素证据实现映射计划` S0。

证据：

```text
S6 已将目标观察约束强匹配 / 降级匹配读取点切到 I64 条件状态和先验可用性收束状态，兼容 bool 字段只保留为运行期协议位。
python .\tools\check_specs.py：hard issues = 0，warnings = 0。
Debug x64 构建：0 警告，0 错误。
```

边界：

```text
真实 D455 / 自然样本未执行。
不宣称外设观察先验发布器完整闭合、真实 D455 先验读取已验证、发布器缺口已完成需求派生闭环或场景体素先验引导观察材料前处理已完整实现。
```

## 2026-07-04：E4/P6 结构化信息缺口与需求派生承接第一版闭合

背景：

按计划索引连续执行 E4/P6 `结构化信息缺口与需求派生承接实现映射计划`。此前 S0-S5 已完成缺口来源扫描、缺口大类和值域登记、缺口到派生需求消息映射、外设 / 视觉缺口标准化适配和父子角色建议映射。本轮补完 S6 入树拒绝闭环验证和 S7 索引回写。

决定：

将 E4/P6 标记为第一版闭合，并把计划索引推进到 E5/P7 `场景体素服务增强与快照生成实现映射计划` S0。

证据：

```text
S6 静态路径验证覆盖字段缺失、重复派生、外设内部材料字段、参数诊断、规范缺口和代码缺陷型报告。
任务管理工作线程会过滤字段不完整、稳定键重复、外设内部材料字段和缺失参数数量诊断。
协议层拒绝策略表覆盖字段缺失、外设内部材料字段、参数诊断不需求化、同批重复、近期重复、代码缺陷型报告和规范缺口。
自我线程入树侧保留拒绝回执治理、开发诊断项和拒绝闭环转派项登记路径。
python .\tools\check_specs.py：hard issues = 0，warnings = 0。
```

边界：

```text
本轮 S6 / S7 未修改 C++ 代码，不运行 Debug x64 构建。
未执行真实运行样本。
同批重复 / 近期重复主路径在工作线程稳定键层过滤，不声明已跑通自我线程重复入树拒绝实样本。
不宣称结构化缺口到需求派生完整链、入树拒绝闭环真实样本回归或父子角色写入需求本体。
```

## 2026-07-03：D5 结构化信息缺口与需求派生承接按待 S0 落入

背景：

用户提供 `20260703_结构化信息缺口与需求派生承接详细设计_v0.1.md` 和 `20260703_结构化信息缺口与需求派生承接实现映射计划_v0.1.md` 草稿，并明确判断：当前代码已有外设缺口码、视觉信息缺口、任务管理派生需求消息、字段完整性检查、稳定键去重、入树拒绝回执和拒绝闭环骨架，但缺少统一缺口语法、缺口特征族、缺口到需求派生的收束规则、缺口到父子角色视图的映射规则，以及外设 / 视觉 / 体素 / 方法缺口的标准化适配。

决定：

新增待完成详细设计：

```text
规范/详细设计/待完成/20260703_结构化信息缺口与需求派生承接详细设计_v0.1.md
```

新增实现映射计划：

```text
计划/20260703_结构化信息缺口与需求派生承接实现映射计划_v0.1.md
```

并登记到 `计划/计划索引.md`，状态为：

```text
实现映射计划 / 待 S0 只读扫描
```

核心口径：

```text
缺口是阻断证据；
需求派生是治理结果；
缺口不能自己变成需求；
缺口先成为可回查证据，再由任务管理和需求树角色视图决定它是否成为子需求，以及成为哪一种子需求。
```

分层：

```text
L0 外设材料缺口；
L1 观察域材料缺口；
L2 方法筹办 / 执行缺口；
L3 需求树入树 / 父子角色缺口。
```

不采用的方案：

- 不原样保留“当前代码事实”作为设计权威，统一降权为“当前实现锚点 / S0 待核”。
- 不新增自由 C++ 结构体承载结构化缺口事实。
- 不把外设缺口码、视觉信息缺口、入树拒绝原因或日志文本直接写成需求。
- 不把父子角色写成需求节点本体字段。
- 不把“是否阻塞 / 是否派生 / 是否必须 / 是否可选 / 是否非必须”写成一阶 bool。
- 不把代码缺陷型报告普通需求化。

影响：

```text
P2 桥接型详细设计补齐计划中 D5 已完成，下一步推进 D6 场景体素服务增强与快照生成详细设计；
P6 已加入计划索引，但不是默认可执行代码切片；
后续 D5 若进入代码实施，必须先执行 S0 只读扫描，确认缺口码、派生消息、入树拒绝回执和稳定键材料。
```

后续验证方式：

- 运行 `python .\tools\check_specs.py`。
- 后续 S0 输出派生需求消息字段清单、入树拒绝回执字段清单、外设缺口码清单、视觉信息缺口清单、稳定键材料组成和缺失承载点。

## 2026-07-03：按“少而硬”安装项目级 MCP

背景：

用户提供了 MCP 安装建议，并明确当前工程重点是：仓库事实对账、代码实现核查、文档同步、构建验证。当前目标不是“工具越多越好”，而是让 Codex 优先盯住本地工程现实。

决定：

新增项目级配置文件：

```text
.codex/config.toml
```

在该文件中接入三个最小 MCP：

```text
fishnest-filesystem
project-scripts-mcp
context7
```

其中：

- `fishnest-filesystem` 使用 `@modelcontextprotocol/server-filesystem`，白名单根目录只给 `D:\鱼巢`。
- `project-scripts-mcp` 为仓库内自建 MCP，实体位于：

```text
.codex/mcp/project-scripts-mcp/
```

只开放五个白名单工具：

```text
check_specs
build_debug_x64
run_panel_html
run_self_check_log
git_snapshot
```

- `context7` 使用 `@upstash/context7-mcp`，用于版本相关文档查询。

本轮明确不装：

```text
GitHub 官方 MCP
DeepWiki
Exa
Firecrawl
```

原因：

- 当前 Codex 已启用内置 GitHub 插件，先不重复接入 GitHub MCP。
- DeepWiki / Exa / Firecrawl 会把当前工程从“本地事实优先”拉向外部资料优先，不符合这轮“少而硬”的目标。
- `project-scripts-mcp` 比通用 shell 更适合当前工程，因为它能把检查、构建、回归和 Git 快照收束到白名单命令内。

不采用的方案：

- 不把 filesystem 根目录扩到整个磁盘、桌面或下载目录。
- 不开放任意 shell MCP。
- 不先装一整套搜索型 MCP 再指望靠提示词压住工具发散。
- 不把自建 `project-scripts-mcp` 放到仓库外，因为它服务的是本项目的固定脚本和固定边界。

影响：

- 后续在 `D:\鱼巢` 新开 Codex 线程时，可直接依赖 `fishnest-filesystem` 做项目目录内读写和搜索。
- 后续可通过 `project-scripts-mcp` 运行固定白名单检查，而不必每次重新拼接命令。
- Context7 可作为库文档查询补充，但不替代本地代码和规范事实。

后续验证方式：

- `node --check .codex\mcp\project-scripts-mcp\server.mjs` 通过。
- `npm install` 成功，0 vulnerabilities。
- `project-scripts-mcp` 启动探测能保持运行，说明已进入 stdio 等待态。
- `@modelcontextprotocol/server-filesystem` 和 `@upstash/context7-mcp` 启动探测通过。
- `.codex/config.toml` 可被 `tomllib` 解析。
- 新开 Codex 线程后，确认三个 MCP 出现在工具集中。

## 2026-07-03：Codex 本地事实扫描与 ChatGPT 云端审查分层

背景：

此前围绕 Skill 化工作流讨论时，曾把 `按云端代码检查` 和 Codex 侧事实扫描放在同一建议里。用户随后明确纠正：`按云端代码检查` 是针对 ChatGPT；对 Codex，应针对当前本地代码现状和当前 worktree，而不是默认以云端 `birthplace/main` 为事实基线。

决定：

新增两个项目内 Codex skill：

```text
.codex/skills/yu-chao-current-fact-scan
.codex/skills/yu-chao-pre-implementation-review
```

并建立对应全局目录联接：

```text
C:\Users\zhchh\.codex\skills\yu-chao-current-fact-scan -> D:\鱼巢\.codex\skills\yu-chao-current-fact-scan
C:\Users\zhchh\.codex\skills\yu-chao-pre-implementation-review -> D:\鱼巢\.codex\skills\yu-chao-pre-implementation-review
```

口径固定为：

```text
ChatGPT 侧：
    按 birthplace/main / 云端代码检查

Codex 侧：
    按当前本地代码 / 当前 worktree 检查
```

其中：

- `yu-chao-current-fact-scan` 只做本地只读事实扫描，不替代云端审查。
- `yu-chao-pre-implementation-review` 作为改代码前的刹车片，检查当前本地事实、规范、详细设计、计划边界和 dirty worktree 风险。

原因：

- Codex 工程执行直接作用于当前工作区；若默认把云端当基线，容易误读当前 dirty worktree、误判真实入口，或把尚未同步的本地变化忽略掉。
- ChatGPT 理论审查更适合把云端 `birthplace/main` 当作提交态权威基线。
- 将“本地事实扫描”和“实施前审查”拆成两个 Skill，可以把高频提示词固定成可重复的轨道，而不是每次临时解释边界。

不采用的方案：

- 不再建议新开 `.agents/skills/` 平行体系，因为项目已经以 `.codex/skills/` 作为自定义 skill 实体目录。
- 不把 `按云端代码检查` 作为 Codex 侧默认触发语句，避免本地事实和云端事实混层。
- 不把实施前审查并入 `yu-chao-execute`，因为执行和审查的停止条件、输出契约和自由度不同。

影响：

- 后续用户若说 `按当前代码检查`、`按当前 worktree 检查`、`先扫描本地事实`，应优先触发本地事实扫描思路。
- 后续用户若说 `实施前审查`、`先审查再改`、`改之前先检查`，应优先触发实施前审查思路，而不是直接实现。
- ChatGPT 侧若明确要求 `云端代码检查`，仍由已有 `chatgpt-yu-chao-theory` 承接。

后续验证方式：

- 新增 skill 目录下的 `SKILL.md` frontmatter 中 `name` 与目录名一致，且存在 `description`。
- 新增 `agents/openai.yaml` 的 `default_prompt` 明确区分本地事实扫描和实施前审查。
- `C:\Users\zhchh\.codex\skills\yu-chao-current-fact-scan` 与 `...yu-chao-pre-implementation-review` 可读，且为指向仓库实体目录的 `Junction`。

## 2026-07-03：D3 待讨论点改为两阶段冻结

背景：

用户明确建议：不要等全部缺失详细设计完成后再一起确定，也不要现在把所有细节定死。当前应先冻结边界型结论；等相关桥接详细设计补齐后，再冻结承载型结论、字段 / 状态值域和代码入口。

决定：

修订：

```text
规范/详细设计/待完成/20260703_观察域对象与中间层目标投影详细设计_v0.1.md
```

将第 11 节从普通待讨论点改为“分级决策表”。

现在冻结：

```text
G 是自我需求投影到观察域后形成的目标特征值约束；
G v1 不独立入账，先用目标观察约束 + 等待项 + 运行期只读视图表达；
O v1 不独立入账，先用外设观察报告队列项 + 提交包表达；
R 需要单独详细设计，当前只保留目标宿主类型和结果集合概念位；
目标期望值必须与当前观察值 / 基准值分离；
像素只能归观察候选 C，不能直接归世界树 E。
```

后续冻结：

```text
G / O 是否未来独立存在；
R 的完整关系类型和值域；
F / C / T / O / G_gap 的完整特征表；
G_gap 的需求派生承接规则；
字段、状态值域和代码入口。
```

原因：

- 后续 D4、D5、D6、D7 都依赖这些边界型结论。
- 若现在不冻结，后续实现映射容易在新增 G 结构、O 结构、R 关系候选、C -> E 归属和目标期望值语义上摇摆。
- 若现在全部定死，又会抢在像素归属、局部体素、场景体素先验和结构化缺口设计之前冻结不完整字段。

补充执行口径：

```text
后续若缺少计划或详细设计草稿，优先在 C:\Users\zhchh\Downloads 查找同名或近名草稿。
当前已看到 D5 结构化信息缺口与需求派生承接详细设计 / 实现映射计划草稿、D3 实现映射计划草稿等候选材料。
```

后续验证方式：

- 运行 `python .\tools\check_specs.py`。
- 不因本次 D3 讨论点冻结而宣称任何代码能力已实现。

## 2026-07-03：D3 观察域对象与中间层目标投影按规范派生落入

背景：

用户提供 `20260703_观察域对象与中间层目标投影详细设计_v0.1.md` 草稿，目标是补齐 F/C/R/T/O/G/G_gap 的观察域承载口径，为 D4 `像素归属候选分裂与局部体素证据` 提供上游承接。审查后确认草稿主体可用，但需要把“根据云端代码现状抽取和补全”降权为“当前实现锚点 / S0 待核”，并把目标字段值类型收紧到项目内部身份、句柄、I64 状态、Q10000 评分、版本、时间戳和来源 ID。

决定：

新增待完成详细设计：

```text
规范/详细设计/待完成/20260703_观察域对象与中间层目标投影详细设计_v0.1.md
```

核心口径：

```text
F = D455 观察帧 / 观察材料帧；
C = 观察候选存在 / 观察像素簇候选；
R = 关系候选；
T = 轨迹候选；
O = 观察域结果包；
G = 自我层需求 / 任务投影到观察域后的目标特征值约束；
G_gap = 观察域无法产生合规候选、合规结果或合规提交材料时输出的结构化阻断材料。
```

修订要求：

```text
这些对象都是概念名、观察域存在名、运行期视图名或特征族名；
不新增 C++ struct；
不把观察域对象写成世界树稳定事实；
不让 G 直接表达需求满足或任务完成；
判断型状态必须按 1175 由复合特征、治理方法或指定收束节点产生。
```

影响：

```text
D4 详细设计中的 F/C/O/G_gap 前置口径已补齐；
D4 实现映射计划从“实现映射计划 / 待条件”调整为“实现映射计划 / 待 S0 只读扫描”；
P2 桥接型详细设计补齐计划下一步从 D3 推进到 D5 结构化信息缺口与需求派生承接详细设计；
P5 仍不得跳过 S0 进入代码修改。
```

不采用的方案：

- 不原样保留“根据云端代码现状抽取和补全”的权威表述。
- 不把 `文本 / 指针句柄` 作为机器判断字段的推荐值类型。
- 不新增 `结构_中间层目标`、`结构_观察域结果包`、`结构_观察候选存在` 或 `结构_信息缺口候选`。
- 不让场景体素先验、目标存在 ID 或 ROI 命中直接推出 C == E。

后续验证方式：

- 运行 `python .\tools\check_specs.py`。
- 后续若切入 D4 代码实施，先执行 S0 只读扫描，按 D3 回查 F/C/O/G_gap 的真实代码承载入口。

## 2026-07-03：D4 像素归属候选分裂与局部体素证据按待 S0 落入

背景：

用户提供 `20260703_像素归属候选分裂与局部体素证据详细设计_v0.1.md` 和 `20260703_像素归属候选分裂与局部体素证据实现映射计划_v0.1.md` 草稿，并明确判断：当前代码只解决约 40%，已有句柄、候选摘要、局部图、体素加工线程和 SceneVoxelPrior 融合骨架，但尚未形成完整的“像素归属图契约 -> 像素状态值域 -> 候选分裂规则 -> 观察候选 C 的局部体素证据链 -> 局部体素 / 稳定体素隔离闸”。

决定：

新增待完成详细设计：

```text
规范/详细设计/待完成/20260703_像素归属候选分裂与局部体素证据详细设计_v0.1.md
```

新增实现映射计划：

```text
计划/20260703_像素归属候选分裂与局部体素证据实现映射计划_v0.1.md
```

初始登记到 `计划/计划索引.md` 时状态为“实现映射计划 / 待条件”。本轮 D3 已补齐后，已调整为：

```text
实现映射计划 / 待 S0 只读扫描
```

核心口径：

```text
像素只归属观察候选 C；
候选分裂只产生观察域材料；
局部体素只挂到 C 作为证据；
稳定存在体素只能由自我侧存在体素融合治理更新。
```

当前前置：

```text
D3 观察域对象与中间层目标投影详细设计已生成；
F / C / O / G_gap 的设计口径已固定；
D4 代码实施前必须执行 S0 只读扫描，确认真实代码入口、字段类型和调用路径。
```

原因：

- `0050` 要求实现前给出项目结构映射，不能用自由结构体、日志或说明文本承载机器判断。
- `1175` 要求判断型 bool / 枚举不能伪装成一阶事实。
- D4 详细设计虽然能固定像素归属图、像素状态值域、候选分裂和局部体素证据边界，但它使用的观察候选 C 必须按 D3 口径在 S0 中核查真实承载入口。

不采用的方案：

- 不把用户草稿中的“当前代码已有”写成已实现结论。
- 不让 D4 实现计划跳过 S0 直接进入代码切片。
- 不新增 C++ 结构体承载像素归属图、候选分裂结果或局部体素证据。
- 不把 SceneVoxelPrior 匹配评分写成 C -> E 识别结论。
- 不让局部体素候选直接写稳定存在体素。

影响：

- P2 桥接型详细设计补齐计划中 D3、D4 已完成，下一步推进 D5。
- P5 已加入计划索引，状态为待 S0 只读扫描，不是默认可执行代码切片。
- 后续 D4 若进入代码实施，S4“局部体素证据挂到 C”必须等待 S0 确认 C 的真实承载入口。

后续验证方式：

- 运行 `python .\tools\check_specs.py`。
- 后续 S0 只读扫描输出：现有句柄表、特征类型 ID 表、报告项字段表、体素加工线程入口、材料页读写入口、缺入口清单和 D3 缺失影响清单。

## 2026-07-03：D2 观察材料质量收束进入实现映射计划

背景：

用户提供 `20260703_观察材料质量收束实现映射计划_v0.1.md` 草稿，目标是把已经落入规范目录的 D2 `观察材料质量收束详细设计` 转成实现映射计划 / 代码落地契约。审查后确认草稿主体方向可用，但存在三类需要修订的点：实施切片使用 P1-P7 容易和计划索引 P 编号混淆，部分验证表述把日志 / 控制面板作为人工核对入口写得过强，目标观察约束 bool 和先验冲突预留需要明确只登记兼容债务而不在本计划中扩张。

决定：

新增实现映射计划：

```text
计划/20260703_观察材料质量收束实现映射计划_v0.1.md
```

并登记到 `计划/计划索引.md`，状态为：

```text
实现映射计划 / 待 S0 只读扫描
```

计划修订口径：

```text
切片统一为 S0-S7；
S0 必须先只读确认真实函数名、字段类型、缺口码和值域；
S1 才允许新增或复用 I64 六态质量状态计算函数；
S2 建立质量缺口映射；
S3 将材料页解析状态、TTL 和来源链纳入可回查判断；
S4 让识别 / 扫描 / 跟踪提交包读取质量状态、能力等级和缺口集合；
S5 只登记目标观察约束 bool 兼容债务；
S6 只预留先验冲突，不实现硬门；
S7 做验证、实施记录和索引回写。
```

原因：

- `1175` 要求材料质量状态这类判断型枚举由证据特征族收束，不能由外设线程、日志、普通函数返回值或 bool 字段直接充当一阶事实。
- D2 详细设计已经固定六态质量状态、输入特征族、写入方 / 读取方、失败分流和完成声明边界。
- 实现映射计划必须先核查真实代码入口，不能把草稿里的函数名和字段名直接当作已确认事实。

不采用的方案：

- 不原样保留 P1-P7 切片编号。
- 不把日志、控制面板或摘要文本作为机器验收依据。
- 不新增结构体承载质量收束结果。
- 不新增普通 bool 表达可用、可交付、可识别、可扫描或可跟踪。
- 不在本计划中全面改造目标观察约束，也不硬编码场景体素先验冲突。

影响：

- D2 后续代码实施有了正式计划入口。
- 进入代码前必须先执行 S0；若 S0 发现真实入口、字段类型、缺口码或值域与计划不一致，应先修订计划，不得继续 S1。
- P2 桥接型详细设计补齐计划仍继续承担 D3-D7 的文档补齐。

后续验证方式：

- 运行 `python .\tools\check_specs.py`。
- 后续执行 S0 时输出真实入口列表、字段类型核查、缺口码覆盖情况、可实施切片和必须停机缺口。

## 2026-07-03：工程性能测量材料不得写成自我观察特征族

背景：

用户指出“速度测量特征族”这个叫法有问题：速度测量是为了提升代码效率而进行的，不是自我所需要的特征类型。复核后确认，D1 详细设计和 D1 实现映射计划中存在把采集 / 入队 / 消费速度写成“特征族”并让它参与质量收束输入的表述，容易把工程性能诊断材料误升格为观察材料质量证据。

决定：

修正以下口径：

```text
“速度测量特征族”
    改为
“工程性能测量材料 / 队列治理诊断材料”
```

并明确：

```text
采集耗时、入队耗时、FPS、队列数量、材料页数量、等待项数量、消费延迟等，
只用于代码效率优化、队列治理诊断和调度调参；
不得作为自我所需的观察材料质量特征；
不得作为材料质量收束的直接证据族；
不得产生“材料可用 / 质量可用 / 观察可提交”等判断型状态。
```

原因：

- `1175` 约束的是特征表达和判断收束，要求一阶材料必须是业务对象可直接取得、可测量、可引用、可追溯的材料值。
- 工程性能测量描述的是代码运行效率和队列运行状况，不是外部被观察对象的质量证据。
- 若后续需要判断“速度正常 / 队列拥塞 / 采集过慢”，应另行归入运行治理或诊断状态，不能混入观察材料质量收束。

影响：

- D1 详细设计第 9 节改为“工程性能测量材料”。
- D1 实现映射计划 S5 改为“工程性能测量只读输出”。
- P2、P3、current-state 和历史决策表述同步改为工程性能测量材料。
- D2 观察材料质量收束不把工程性能测量材料列为输入特征族。

后续验证方式：

- 运行 `rg` 确认除本决策记录和 current-state 的纠偏说明外，不再存在“速度测量特征族”“质量收束输入”等误导性表述。
- 运行 `python .\tools\check_specs.py`。

## 2026-07-03：D2 观察材料质量收束按规范派生落入

背景：

用户提供 `20260703_观察材料质量收束详细设计_v0.1.md` 草稿，希望检查、修订并落入详细设计目录。审查后确认草稿方向可用，覆盖来源与可回查、时间与新鲜度、深度、彩色 / 红外、坐标、ROI、任务阈值、跨帧稳定和先验冲突等输入特征族，也覆盖未评估、可用、低质量、不可用、部分可用、需复验等质量状态。但原稿存在三类需要收紧的表达：以 `birthplace/main` 代码事实作为生成依据、把当前实现锚点写得过像权威依据、将 `优质` 与规范质量状态混在同一层级。

决定：

新增规范派生待完成详细设计：

```text
规范/详细设计/待完成/20260703_观察材料质量收束详细设计_v0.1.md
```

修订口径：

```text
依据 1175 生成，不以代码事实反向定义设计；
当前代码只作为实现锚点 / 后续实现映射待核；
材料质量状态是判断型枚举，必须由证据特征族收束；
质量状态统一为 未评估 / 可用 / 低质量 / 不可用 / 部分可用 / 需复验；
优质只作为兼容质量等级或证据强度，不作为第七个质量状态；
现有 bool 字段只能作为兼容输出位，不得继续扩张为一阶特征模式；
D455 采集线程不得直接写入材料质量状态。
```

原因：

- `1175` 明确判断型 bool 和判断型枚举都不得作为外设直接产出的一阶事实。
- `0050` 要求设计进入实现前给出项目结构映射，禁止用日志、说明文本、错误码或自由字段承载机器判断。
- 观察材料质量收束是观察、识别、扫描、跟踪和存在体素融合共同依赖的前置治理节点，必须独立成桥接型详细设计。

不采用的方案：

- 不原样落入“根据代码事实抽取并完善”的表述。
- 不把 `判断外设观察材料质量(...)` 声明为已经合规的最终治理入口。
- 不新增结构体承载质量收束结果。
- 不把 `优质` 写成规范质量状态。
- 不让本详细设计直接进入代码实施。

影响：

- P2 桥接型详细设计补齐计划中 D2 已完成，下一步推进 D3 `观察域对象与中间层目标投影详细设计`。
- 后续若要实现 D2，必须另行生成观察材料质量收束实现映射计划，从 S0 只读扫描开始核查真实函数、字段、调用路径和验证入口。
- D1、D2 都仍只是设计 / 计划输入，不构成外设观察材料稳定入队或质量收束已代码实现的证据。

后续验证方式：

- 运行 `python .\tools\check_specs.py`。
- 后续实现映射计划至少验证采集失败、报告过期、无材料句柄、L1 稳定子集达标、仅平面轮廓无空间坐标、目标约束匹配、generation 失配和先验冲突预留场景。

## 2026-07-03：D1 外设观察材料稳定关联与入队进入实现映射计划

背景：

用户提供 `20260703_外设观察材料稳定关联与入队实现映射计划_v0.1.md` 草稿，目标是把已经落入规范目录的 D1 详细设计转成实现映射计划 / 代码落地契约。审查后确认草稿主体完整，但存在三类风险：把 `generation` 写成文本机器键、实施切片使用 P 编号容易和计划索引混淆、缺少每个切片的允许文件 / 禁止文件 / 父计划回写。

决定：

新增实现映射计划：

```text
计划/20260703_外设观察材料稳定关联与入队实现映射计划_v0.1.md
```

并登记到 `计划/计划索引.md`，状态为：

```text
实现映射计划 / 待 S0 只读扫描
```

计划修订口径：

```text
generation 必须由结构化一阶材料 / 身份材料 / 运行期只读计算结果收束；
若现有字段只能作为自由文本，则不得作为机器判断键继续实现；
切片统一为 S0-S5；
S0 必须先只读确认真实函数名、字段类型、调用路径和验证入口；
幂等去重必须在来源链稳定后后置执行；
采集 / 入队 / 消费耗时等工程性能测量只作为代码效率优化、队列治理诊断和调度调参材料，不作为自我所需的观察材料质量特征。
```

原因：

- `0050` 禁止用自由文本、日志、说明文本或临时结构承载机器判断。
- `1175` 要求判断型 bool / 判断型枚举必须由证据特征族收束，不能作为外设一阶事实。
- D1 详细设计已经把代码事实降为实现锚点，因此实现映射计划必须先做 S0 只读扫描，不能直接把草稿中的代码入口当成已确认事实。

不采用的方案：

- 不原样落入“文本 generation”口径。
- 不把幂等去重放在第一轮直接实施。
- 不把普通 bool / 判断型枚举全量迁移混入本计划。
- 不让本计划直接修改观察、扫描、识别、跟踪方法边界。

影响：

- D1 后续代码执行有了正式计划入口。
- 执行时必须先完成 S0；若发现 generation 承载、L2/L3 来源链、等待项幂等语义存在冲突，需要停下修订计划或详细设计。
- P2 桥接型详细设计补齐计划仍继续承担 D2-D7 的文档补齐。

后续验证方式：

- 运行 `python .\tools\check_specs.py`。
- 后续执行 S0 时输出真实函数名、字段类型、可实施切片、需回退缺口和新增结构体 / 普通 bool 风险。

## 2026-07-03：D1 外设观察材料稳定关联与入队按规范派生落入

背景：

用户提供 `20260703_外设观察材料稳定关联与入队详细设计_v0.1.md` 草稿，希望讨论并落入详细设计目录。审查后确认该草稿覆盖 D1 所需的 L0-L3、材料页、报告 ID、帧 ID、generation、坐标版本、TTL、等待项绑定和工程性能测量材料，但原稿以代码事实作为生成依据，并包含“当前代码已经解决”和“最小落地切片建议”等容易把代码现状反向变成设计依据的内容。

决定：

新增规范派生待完成详细设计：

```text
规范/详细设计/待完成/20260703_外设观察材料稳定关联与入队详细设计_v0.1.md
```

修订口径：

```text
规范依据优先；
代码事实降为当前实现锚点；
不声明代码已实现；
补项目结构映射表；
显式固定 L0=0、L1=1、L2=2、L3=3；
generation 必须纳入来源外设、报告类型、层级、帧号、时间戳、坐标版本、方法版本和目标约束；
资源句柄只能作为 opaque handle，不得依赖自由文本解析完成机器判断；
工程性能测量材料只用于代码效率优化、队列治理诊断和调度调参，不直接生成质量状态或事实裁决。
```

原因：

- `0050` 要求设计进入实现前给出项目结构映射，禁止用自由文本、日志、说明字段或临时结构承载机器判断。
- `1175` 要求判断型 bool 和判断型枚举不得作为外设直接产出的一阶事实。
- D1 是后续质量收束、观察域对象、像素候选、缺口承接、场景体素先验和四方法实现映射的材料链地基。

不采用的方案：

- 不原样落入以代码事实为生成依据的草稿。
- 不把“当前代码已经解决”写成详细设计结论。
- 不把最小落地切片写成代码实现计划。
- 不新增 C++ 结构体表达材料页、幂等键或稳定材料页。

影响：

- D1 已具备后续实现映射计划输入。
- 后续若要落地代码，必须另行生成实现映射计划，逐项扫描 L0 层级写入、来源 generation、L1/L2/L3 来源继承、材料页按 generation 幂等、入队时间戳和消费延迟等入口。
- P2 桥接型详细设计补齐计划剩余 D2-D7 仍需继续生成。

后续验证方式：

- 运行 `python .\tools\check_specs.py`。
- 后续实现映射计划验证 V1-V7：单帧入队、材料同源、L1 来源回查、重复帧幂等、过期材料页、工程性能测量、L2/L3 来源缺失分流。

## 2026-07-03：允许建立详细设计补齐治理计划

背景：

计划索引已经收紧为“没有对应详细设计，不得制定实现计划”。用户随后提出当前缺少多份桥接型详细设计，需要先制定一个计划并加入计划索引，用于补齐外设观察材料稳定入队、观察材料质量收束、观察域对象、中间层目标投影、像素归属、结构化缺口、场景体素快照和外设观察先验发布器等设计空档。

决定：

新增详细设计治理计划：

```text
计划/20260703_桥接型详细设计补齐计划_v0.1.md
```

并在 `计划/计划索引.md` 中增加窄例外：

```text
若计划目标本身就是“补齐详细设计”，允许建立详细设计治理计划；
但该计划只能安排规范回读、详细设计生成、索引和状态回写；
不得作为代码实现入口。
```

原因：

- 详细设计补齐计划不是从详细设计到代码的实现映射计划，因此不能按普通实现计划规则要求“已有对应详细设计”。
- 如果不允许建立这种治理计划，缺失详细设计本身无法进入当前计划索引。
- 该例外必须收紧，否则会重新打开绕过详细设计直接进入代码的口子。

不采用的方案：

- 不把桥接型详细设计补齐计划当成代码落地契约。
- 不把用户提供的讨论内容直接当成正式详细设计。
- 不允许该计划直接实施外设队列、质量收束、像素归属、场景体素服务或先验发布器代码。

影响：

- 后续可以按计划生成 7 份必须桥接型详细设计和 2 份后置候选详细设计。
- 这些详细设计完成后，若要写代码，仍必须分别生成实现映射计划 / 代码落地契约。
- 计划索引保留“实现计划必须有详细设计”的主规则，同时允许“补齐详细设计本身”的文档治理例外。

后续验证方式：

- 检查 `计划/20260703_桥接型详细设计补齐计划_v0.1.md` 明确声明不作为代码实现入口。
- 检查计划索引中 P2 的状态为文档治理计划，而非代码切片。
- 运行 `python .\tools\check_specs.py`。

## 2026-07-03：根目录计划只保留详细设计承接项

背景：

用户指出，`计划/` 根目录下既有计划大多没有对应详细设计，已经不符合“详细设计 -> 实现映射计划 / 代码落地契约 -> 代码实施”的当前规则。继续把这些旧计划留在根目录，会让后续 Codex 误以为可以绕过详细设计直接恢复执行。

决定：

将 `计划/` 根目录中 23 份旧计划移动到：

```text
计划/完成项/
```

保留唯一有对应详细设计承接的根目录实现映射计划：

```text
计划/20260702_需求树父子关系角色视图实现映射计划_v0.1.md
```

并将 `计划/计划索引.md` 清理为短索引，只记录当前有效入口、新计划进入规则、恢复规则和下一次执行入口。

原因：

- 当前计划规则要求制定计划前必须先确认对应详细设计存在。
- 旧根目录计划多数来自早期执行和纠偏链路，不一定符合当前规范、详细设计和代码落地契约分层。
- 根目录应保持干净，避免旧计划被误当成当前默认执行队列。

不采用的方案：

- 不直接删除旧计划，避免丢失历史证据和恢复线索。
- 不逐份修补旧计划，因为本次目标是清理入口，不是恢复旧计划。
- 不把归档到 `计划/完成项/` 解释为业务目标已完成。

影响：

- 后续从旧计划恢复时，必须重新检查当前规范和详细设计；没有详细设计则先生成详细设计。
- `计划/完成项/` 是历史归档和证据入口，不进入默认执行队列。
- 新计划必须明确承接对应详细设计，并包含代码落地契约所需的设计条款、结构映射、代码入口、流程状态、反走样清单、验证剧本和最小切片。

后续验证方式：

- 检查 `计划/` 根目录只剩 `计划索引.md` 和当前合规实现映射计划。
- 检查 `计划/计划索引.md` 不再登记旧 R/W 长表作为默认执行入口。
- 运行 `python .\tools\check_specs.py` 确认规范治理检查无硬错误。

## 2026-07-03：收紧场景体素先验前处理实现前边界

背景：

用户复核《场景体素先验引导观察材料前处理详细设计 v0.1》后确认方向正确，但指出它仍不能直接交给 Codex 写代码：外设观察先验发布器权责不够明确，外设处理先验快照还没有落成“不新增结构体”的项目表达，先验新鲜度等判断型状态容易被误写成外设一阶状态，背景表达和建议后续方法类型材料也需要更硬的反走样约束。

决定：

修订：

```text
规范/详细设计/待完成/20260703_场景体素先验引导观察材料前处理详细设计_v0.1.md
```

补充并收紧：

```text
外设观察先验发布器权责边界；
先验新鲜度状态的依赖特征族、收束写入方、读取方和失败分流；
外设处理先验快照不新增结构体的承载细化表；
背景不得作为存在类别、普通 bool 或世界树事实；
D455 前处理策略函数、视觉中间层和外设观察先验发布器不得直接创建或调用观察 / 扫描 / 识别 / 跟踪；
进入代码实现前必须先生成实现映射计划 / 代码落地契约。
```

原因：

- `1175` 要求先验可用、先验新鲜度、遮挡解释、自由空间冲突和候选一致性等判断型状态必须由证据特征族收束，不能由外设直接写成一阶事实。
- `0050` 要求进入实现前给出项目结构映射，不能用新增结构体、文本或说明字段承载业务判断。
- `6250` 要求先验只能帮助看，不能替代看；外设前处理不能变成事实裁决或四方法调度器。

不采用的方案：

- 不直接进入代码实现。
- 不新增 C++ 结构体承载外设处理先验快照。
- 不让 D455 前处理直接消费世界树存在并裁决 C -> E。
- 不把先验匹配评分接成最终归属。

影响：

- 后续若要落地该设计，第一步必须生成实现映射计划，明确发布器归属、读取入口、写入材料、清理策略和验证剧本。
- 外设处理先验快照、ROI、mask、深度范围图、分区处理频率表、特征提取计划和先验证据必须优先落到现有报告项、目标观察约束、运行期只读视图、VecU / VecIU64 句柄和特征句柄。

后续验证方式：

- 检查实现映射计划是否补齐：业务概念 -> 现有项目承载结构 -> 一阶特征族 -> 判断型状态收束位置 -> 写入方 -> 读取方 -> 清理策略 -> 验证方式。
- 验证无快照默认处理、过期快照降权、坐标失配不裁决、未知区低频处理、先验冲突生成缺口、前处理不直接调用四方法。

## 2026-07-03：存在体素融合内部治理采用保守候选链

背景：

用户提供了 `存在体素融合内部治理详细设计 v0.1` 草稿，目标是把已确认存在的新观察轮廓、局部体素和稳定体素主模型之间的治理链条落入详细设计。该主题容易走偏为外设直接更新世界、工作线程成为动作来源、整组轮廓直接污染稳定体素模型，或用普通 bool / 判断型枚举承载可融合、可提交等裁决。

决定：

新增优化后的待完成详细设计：

```text
规范/详细设计/待完成/20260703_存在体素融合内部治理详细设计_v0.1.md
```

核心口径：

```text
存在体素融合是自我内部治理承接链，不是外设行为；
已确认存在 E 新增同源、可回查、归属可信的体素相关观察材料后，只能先生成待处理材料；
先提取输入证据子轮廓，再生成子体素候选并挂入稳定体素子链；
合格子体素只能生成稳定体素主模型更新候选；
正式主模型更新必须经提交入口或对应领域类裁决；
稳定体素版本更新后只提交场景体素刷新请求，由场景体素服务只读刷新快照。
```

原因：

- `6240` 明确体素加工线程只能生成候选结果包，不能直接写正式世界树事实、创建内部世界子存在、生成方法动作动态或绕过提交入口。
- `1175` 要求可融合、可提交、归属可信、材料可回查等判断型状态必须由证据特征族收束，不能写成外设直接事实或普通 bool。
- 体素融合一旦过度合并，会污染稳定存在体素模型；因此需要采用“默认不合并、先建关系、先审计禁合并证据、多帧确认后才允许融合”的保守口径。

不采用的方案：

- 不把外设原始帧、单帧分割结果、场景体素快照、视觉先验命中、控制面板刷新或融合写回本身作为合法触发源。
- 不让工作线程成为动作动态来源。
- 不让子体素入链等同于稳定体素主模型更新。
- 不新增业务结构体作为默认承载方案；写代码前必须生成实现映射计划。

影响：

- 后续代码前必须先生成 `存在体素融合内部治理` 的实现映射计划 / 代码落地契约。
- 需要扫描现有深度 / 彩色轮廓特征落点、稳定三维体素主模型特征、提交入口、场景体素刷新入口和可复用治理队列。
- 观察、识别、扫描、跟踪与体素融合的关系被固定为缺口 / 材料承接关系，不允许直接互调或互相替代。

后续验证方式：

- 用 V1-V9 正向剧本和 N1-N4 负例验证：正常融合、缺材料、generation 失配、高冲突、幂等去重、默认不合并、场景体素刷新边界、普通 bool 禁止、先验冒充证据禁止、线程动作来源禁止。
- 验证证据必须覆盖输入入口、项目结构承载、候选生成、正式提交、状态变化、调用路径和回归命令。

## 2026-07-03：闭合切片提交后自动同步云端

背景：

此前 Git 规则是闭合切片完成后自动提交，但推送仍由用户明确说“同步到云端”触发。用户现在要求“以后有更改就自动同步到云端”，需要把该要求固化到项目级工作规则，避免后续只提交本地而忘记推送。

决定：

修改 `AGENTS.md` 第 10 章 Git 规则：

```text
每个闭合切片完成后自动提交并自动推送本次 Codex 修改的文件到常用远端。
```

自动同步只包含本次 Codex 切片相关文件。用户已有未提交修改、明确排除的文件、未跟当前切片相关的脏文件，不得被混入提交或推送。

原因：

- 鱼巢项目需要本地 Codex 执行结果及时同步到 `birthplace/main`，供 ChatGPT「理论探讨」项目和后续 Codex 任务读取当前事实。
- 只提交不推送会让本地状态和云端状态短暂分叉，容易导致理论讨论沿用旧云端文件。
- 将规则写入 `AGENTS.md` 后，后续 Codex 可以稳定执行，不依赖对话记忆。

不采用的方案：

- 不把所有工作区变动一律提交推送，因为这会混入用户文件、排除文件或无关脏改。
- 不在远端分叉、推送被拒、凭证失败时强行处理；这些情况必须停止说明。
- 不覆盖用户本轮明确提出的“不要提交 / 不要同步 / 只保留本地”要求。

影响：

- 后续每个闭合切片完成后，默认执行状态检查、精确暂存、提交和推送。
- 最终回复中应说明提交哈希、推送远端 / 分支，以及是否仍有未提交或排除文件。
- 若只完成只读分析、讨论或没有文件更改，则不需要提交和推送。

后续验证方式：

- 闭合切片后检查 `git status --short --branch`，确认本地分支与 `birthplace/main` 对齐。
- 检查最近提交是否只包含本次切片相关文件。
- 若仍有未跟踪或未提交文件，最终回复中明确它们是否被排除。

## 2026-07-03：场景体素先验异步发布为外设处理先验快照

背景：

观察、扫描、识别和跟踪都需要从外设材料中取得当前证据。若每帧都从全图重新处理，处理成本高；若让外设直接读取自我世界树事实并裁决，又会违反“先验可以帮助看，不能替代看”和外设材料只作为证据包的边界。

决定：

采纳“场景体素 / 存在体素反向参与感知”的方向，但工程表达限定为：

```text
自我侧异步发布外设观察先验快照；
外设 / 视觉中间层每帧处理前只读该快照；
快照只用于选择 ROI、mask、预计深度范围、处理频率和特征提取内容；
外设仍只输出当前帧证据、候选、评分、解释和结构化缺口；
事实裁决仍由自我侧观察 / 扫描 / 识别 / 跟踪和提交入口完成。
```

新增待完成详细设计：

```text
规范/详细设计/待完成/20260703_场景体素先验引导观察材料前处理详细设计_v0.1.md
```

原因：

- `6250` 已明确场景体素先验可以帮助看，但不能替代看。
- 当前外设观察报告队列、目标观察约束、处理模式、优先级和处理 mask 已有工程锚点。
- 当前扫描侧已有 `SceneVoxelPrior` 消费方向，但主要发生在报告材料形成之后，不是 D455 每帧处理前的前处理策略。
- 通过异步发布面向外设的先验快照，可以减少外设侧全图处理，同时避免外设直接拥有世界树事实裁决权。

不采用的方案：

- 不让 D455 线程直接读取或修改世界树事实。
- 不把预测 ROI、场景体素投影或先验匹配评分直接当成观察结果。
- 不让“没看到”直接推出存在消失。
- 不新增 C++ 结构体作为默认解决方案；代码落地前必须先生成实现映射计划，确认能否由现有报告项、目标观察约束、运行期只读视图和项目特征句柄承载。

影响：

- 后续外设前处理设计需要增加“外设处理先验快照”这一运行期只读材料层。
- 判断型状态如先验可用、遮挡解释成立、自由空间冲突成立、候选与先验一致等，都必须按 `1175` 由证据特征族收束，不能由外设直接写成一阶事实。
- 后续代码实施前必须生成代码落地契约，明确发布入口、读取入口、D455 每帧前处理入口、候选输出入口和验证剧本。

后续验证方式：

- 证明无快照时 D455 不阻塞采集，能退回默认轻量 / 全局处理。
- 证明过期快照、坐标版本失配或 TTL 到期时只降权、复验或生成缺口，不裁决事实。
- 证明候选输出能携带先验版本、坐标版本、目标约束ID、匹配评分、冲突数量和遮挡解释材料。
- 证明未知区仍被低频处理，先验 ROI 不会永久过滤新存在。

## 2026-07-03：建立工程状态同步记录机制

背景：

ChatGPT「理论探讨」项目和 Codex 工程执行项目之间需要共享当前工程事实。仅靠旧对话记忆容易把已经变化的代码、规范、计划或工作区状态误当成当前事实。

决定：

采用三文件分工：

```text
AGENTS.md 管规则
docs/current-state.md 管现实
docs/decision-log.md 管为什么
```

每次 Codex 任务结束前，应更新 `docs/current-state.md`；如果本次产生重要技术选择，应同步更新 `docs/decision-log.md`。最终回复中提供一份可复制给 ChatGPT「理论探讨」项目的同步摘要。

原因：

- `AGENTS.md` 适合承载工作规则、必读文件和收尾要求。
- `docs/current-state.md` 适合承载当前目标、分支、已完成、未完成、测试状态、风险和下一步。
- `docs/decision-log.md` 适合承载重要设计选择的背景、决定、原因、不采用方案、影响和后续验证方式。
- 将现实状态写入仓库文档，可以减少理论讨论项目沿用旧对话假设的风险。

不采用的方案：

- 不把当前工程状态只放在对话记忆中，因为对话可能压缩、迁移或滞后。
- 不把状态写入 README，因为 README 是对外项目入口，不适合承载频繁变更的执行状态。
- 不把状态写入说明书，因为说明书是人读材料和代码反向说明，不作为实现权威。
- 不把重要技术选择混入 `current-state.md`，避免现实快照和决策理由混在一起。

影响：

- 后续 Codex 任务需要在收尾时检查并更新 `docs/current-state.md`。
- 重要技术选择需要追加到 `docs/decision-log.md`。
- ChatGPT「理论探讨」项目应优先读取这两个文件来获取当前工程现实和决策原因，而不是沿用旧讨论想象。

后续验证方式：

- 每次任务结束检查 `docs/current-state.md` 的最后更新时间、当前目标、测试状态和已知问题是否仍准确。
- 产生重要设计选择时，检查 `docs/decision-log.md` 是否新增对应条目。
- 需要同步给 ChatGPT「理论探讨」项目时，直接复制最终同步摘要，并以仓库文档为事实来源。

## 2026-07-03：自定义 skill 实体进入项目目录

背景：

`C:\Users\zhchh\.codex\skills` 下的自定义 skill 是本地文件，不在 `D:\鱼巢` 仓库中；因此普通 `git push birthplace main` 不会把 `yu-chao-plan` 等自定义 skill 同步到云端。

决定：

将本项目相关自定义 skill 的实体目录移动到：

```text
D:\鱼巢\.codex\skills\
```

同时在原 Codex 默认路径下保留同名目录联接：

```text
C:\Users\zhchh\.codex\skills\<skill> -> D:\鱼巢\.codex\skills\<skill>
```

移动范围只包括自定义 skill：

```text
digital-life-reasoning
discuss
yu-chao-execute
yu-chao-flowchart
yu-chao-plan
yu-chao-root-cause
```

不移动 `.system` 和 `codex-primary-runtime`。

原因：

- 项目内实体目录可以被 Git 跟踪并同步到云端。
- 保留全局目录联接可以兼容 Codex 当前默认 skill 加载路径。
- 不移动系统 skill 和运行时目录，避免把 Codex 自带或缓存内容混入项目仓库。

不采用的方案：

- 不只复制一份到项目目录，因为容易出现全局副本和项目副本分叉。
- 不直接整体移动 `C:\Users\zhchh\.codex\skills`，因为会影响 `.system`、运行时目录和 Codex 默认发现路径。
- 不把 skill 塞进 `docs/`，因为 skill 本身是可执行协作规则，不是普通文档说明。

影响：

- 后续修改自定义 skill 时，应修改 `.codex/skills/` 中的实体文件。
- 克隆到新机器后，仓库能带上自定义 skill 内容，但仍需要建立全局目录联接，Codex 才能从默认路径发现这些 skill。
- 若目录联接失效，Codex 可能无法加载对应自定义 skill。

后续验证方式：

- `git status -- .codex/skills` 能看到自定义 skill 文件。
- `C:\Users\zhchh\.codex\skills\<skill>\SKILL.md` 可读，且目录属性包含 `ReparsePoint`。
- 后续云端同步后，在远端仓库可看到 `.codex/skills/` 下的自定义 skill。

## 2026-07-03：新增 ChatGPT 侧鱼巢理论探讨 skill

背景：

ChatGPT「理论探讨」项目需要一个稳定入口，用于读取云端仓库状态、审查鱼巢理论口径、详细设计、实现映射计划和当前工程状态，避免沿用旧讨论或把未实现设计当成代码事实。

决定：

新增项目内自定义 skill：

```text
.codex/skills/chatgpt-yu-chao-theory
```

并建立全局目录联接：

```text
C:\Users\zhchh\.codex\skills\chatgpt-yu-chao-theory -> D:\鱼巢\.codex\skills\chatgpt-yu-chao-theory
```

原因：

- 该 skill 明确区分 ChatGPT 侧理论审查和 Codex 侧代码执行。
- 它要求以云端 `birthplace/main`、当前规范、详细设计、计划和代码事实为依据，降低旧对话漂移风险。
- 放入 `.codex/skills/` 后可以随仓库同步，同时通过全局联接兼容默认 skill 加载路径。

不采用的方案：

- 不把它并入 `discuss`，因为 `discuss` 是通用讨论入口，而该 skill 专门面向 ChatGPT 侧理论项目和云端状态审查。
- 不让该 skill 承担代码执行、构建、提交或推送职责，避免混淆 ChatGPT 讨论和 Codex 工程执行边界。

影响：

- ChatGPT 侧讨论可用 `$chatgpt-yu-chao-theory` 作为固定入口。
- 后续修改该 skill 时，应直接修改 `.codex/skills/chatgpt-yu-chao-theory/` 下实体文件。
- 克隆到新机器后仍需重建全局目录联接，Codex 才能从默认路径发现该 skill。

后续验证方式：

- `SKILL.md` frontmatter 中 `name` 与目录名一致，且存在 `description`。
- `agents/openai.yaml` 的 `short_description` 保持短描述，`default_prompt` 明确包含 `$chatgpt-yu-chao-theory`。
- 全局路径 `C:\Users\zhchh\.codex\skills\chatgpt-yu-chao-theory\SKILL.md` 可读，且父目录为 `ReparsePoint`。

## 2026-07-03：判断型枚举不得冒充外设一阶事实

背景：

外设感知详细设计中原先存在 `材料质量状态` 等状态词，容易被实现成外设侧直接输出的 I64 枚举。用户指出：枚举型特征值和布尔型没有天然本质区别，只要它是根据其它材料判断出来的状态，就不能伪装成外设直接取得的一阶事实。

决定：

将外设感知共同边界和观察详细设计修订为三层：

```text
原始材料 / 一阶测量值 / 可追溯状态
复合特征判断
内部治理或方法收束状态
```

外设侧只直接提供原始材料句柄、设备状态、时间戳、帧序号、尺寸、深度 / 彩色 / 空间点材料句柄、有效 / 无效像素数量、缺失比例、噪声 / 曝光 / 模糊 / 对齐评分、坐标变换版本、外设错误码和低层诊断候选。

`材料质量状态`、`归属状态`、`稳定复现状态`、`提交候选判定状态`、`遮挡解释状态` 等判断型枚举，必须由复合特征、内部治理节点或方法执行后的阶段性结论收束产生，并保留依赖特征族、收束方法、写入方、读取方、失败分流和可回查证据。

原因：

- 4120 复合特征规则已经要求判断型 bool 展开子特征、判定规则、写入方和来源动态。
- 4140 枚举型实例特征值合法来源规范要求实例枚举值必须有合法生产者和证据。
- 6310 观察质量规范已经明确质量诊断摘要只能作为输入证据，不能直接把状态写成 `可用`。
- 仅把 bool 改成 I64 枚举会保留同一个问题：状态结论没有材料来路。

不采用的方案：

- 不把 `材料质量状态 = 可用 / 低质量 / 不可用 / 部分可用 / 需复验` 写成外设直接输出的一阶普通特征。
- 不为每个枚举值单独新建结构，避免结构体膨胀。
- 不让单个质量评分、外设错误码、日志文本或报告名称直接推出正式材料质量状态。

影响：

- 后续实现外设接口时，应先输出一阶材料值、测量值、评分和低层候选。
- 后续实现观察或中间层质量收束方法时，需要读取外设一阶材料并结合当前任务阈值、最大允许观察年龄、目标特征类型、坐标基准要求和可回查要求。
- 扫描、识别、跟踪读取“材料质量不足”时，只能读取质量收束状态或其可回查证据，不能直接读取外设报告文本下结论。

后续验证方式：

- 代码实现前生成实现映射计划，列出外设一阶材料字段、质量收束入口、写入方和读取方。
- 检查 `外设观察报告队列.ixx`、`自我动作实现.外设模块.ixx`、`任务模块.管理工作线程.ixx` 和扫描入口，确认不存在外设直接写判断型状态参与任务结算。
- 回归时证明材料质量状态的输入特征族、收束方法、状态写入和失败分流均可追溯。

## 2026-07-03：新增特征表达与判断收束根规范

背景：

外设材料质量状态问题暴露出的规则不是 D455 专用问题，也不是观察方法补丁，而是全项目特征系统的基础表达问题：特征系统必须区分材料、测量、状态、判断和收束，不能把判断结果伪装成直接事实。

决定：

新增正式根规范：

```text
规范/1175_根规范_特征表达与判断收束规则_20260703.md
```

该规范作为规则根规范，不新增主信息类和代码字段；它约束 `抽象特征`、`特征节点`、`二次特征`、`特征值`、状态节点、外设材料和治理方法如何表达一阶材料、复合判断、二次判断和判断型状态。

编号选择：

```text
1160 已被状态节点主信息类占用；
1170 已被动态节点主信息类占用；
1190 已被因果主信息类占用；
1195 已被因果模板主信息类占用；
因此采用相邻空位 1175。
```

原因：

- `特征表达` 覆盖一阶材料、测量值、计数值、评分值、句柄值、版本值、来源值、原始分类枚举和生命周期枚举。
- `判断收束` 覆盖判断型 bool、判断型枚举、复合特征结果、二次特征判断和内部治理状态。
- 该规则比“复合特征使用规则”更宽，比“布尔特征禁止规则”更准确，也避免和 `2100_根规范_特征值主信息类` 抢定义边界。

不采用的方案：

- 不使用 `1170`，因为已占用且会覆盖动态节点根规范。
- 不使用 `1160` 或 `1190`，因为也已占用。
- 不命名为“复合特征使用规则”，因为问题不只在复合特征。
- 不命名为“布尔特征禁止规则”，因为判断型枚举同样需要收束。
- 不命名为“特征值类型规则”，避免和 `2100_根规范_特征值主信息类` 混淆。

影响：

- `4120_子规范_抽象特征树与复合特征规则_20260701.md` 和 `4140_子规范_枚举型实例特征值合法来源_20260701.md` 已补入 `1175` 上级规范。
- 外设感知共同边界、观察、扫描、识别、跟踪五份待完成详细设计已补入 `1175` 上级规范引用。
- `docs/工程图谱/05_规则原子索引.md` 已补入 `1175` 作为当前根规范回读来源。
- 后续实现外设接口、观察质量收束、扫描材料质量分流、识别归属和跟踪稳定复现时，都必须回读 `1175`。

后续验证方式：

- `python .\tools\check_specs.py` 应保持 `hard issues: 0`、`warnings: 0`。
- 代码实现前生成实现映射计划，明确哪些值是一阶材料，哪些值是判断型状态，判断型状态由哪个收束方法写入。
- 后续代码审计重点检查：外设报告、普通函数返回值、日志文本、控制面板显示或单个评分是否直接写入判断型状态。

## 2026-07-03：D6 场景体素服务增强与快照生成按待 S0 落入

背景：

用户继续提供第 6 份桥接设计草稿，目标是补齐 `场景体素服务增强与快照生成` 详细设计和对应实现映射计划。该设计承接 D4 像素 / 候选 / 局部体素、D5 结构化缺口，以及后续 D7 外设观察先验发布器。

决定：

新增：

```text
规范/详细设计/待完成/20260703_场景体素服务增强与快照生成详细设计_v0.1.md
计划/20260703_场景体素服务增强与快照生成实现映射计划_v0.1.md
```

计划索引新增 P7，状态为：

```text
实现映射计划 / 待 S0 只读扫描
```

P2 桥接型详细设计补齐计划下一步改为：

```text
D7 外设观察先验发布器详细设计
```

原因：

当前设计口径认为，现有实现锚点大约只覆盖只读快照、SceneVoxelPrior 别名、存在项、缺口、统计、粗查询和维护线程骨架；仍缺少稳定存在体素解码后的空间占据先验、存在体素场景 AABB / 投影索引、ROI / 预计深度 / 遮挡 / 空闲 / 未知 / 自由空间冲突图层、快照新鲜度 / 可用性收束、事件模型扩展和 D5 缺口标准化。

核心边界：

```text
场景体素服务维护运行期只读空间心像，不是事实源。
场景体素服务不写世界树事实、稳定存在体素、需求满足、动作动态或价值结算。
视觉中间层只读 SceneVoxelPrior，只能输出候选、评分、解释、冲突候选和缺口。
SceneVoxelPrior / AABB / 射线粗查询都不能证明当前观察事实、遮挡成立、存在确认或存在消失。
```

不采用的方案：

```text
不把草稿中的当前代码描述直接写成已验证代码事实。
不把先验可用性、快照新鲜度、遮挡解释、自由空间冲突成立写成普通一阶 bool / 枚举。
不新增长期 C++ 结构体作为默认承载方案。
不让 D6 实现映射计划直接进入代码修改；后续实施必须先做 S0 只读扫描。
```

影响：

后续进入 D6 代码实施时，必须先扫描 `场景体素模块.ixx`、`视觉观察融合模块.ixx`、`外设观察报告队列.ixx` 和 D5 缺口承接入口，确认快照字段、缺口值域、查询接口、维护事件入口和 SceneVoxelPrior 使用路径。

后续继续桥接详细设计补齐时，优先生成 D7 `外设观察先验发布器详细设计`，把 D6 只读场景体素快照投影成外设处理先验快照。

后续验证方式：

- 文档落地阶段运行 `python .\tools\check_specs.py` 和 `git diff --check`。
- 代码实施阶段从 D6 实现映射计划 S0 开始，不得跳过只读扫描。
- 后续验收不得把 D6 详细设计 / 计划落地说成场景体素服务增强已代码实现。

## 2026-07-03：D7 外设观察先验发布器按待 S0 落入

背景：

用户继续提供第 7 份桥接设计草稿，目标是补齐 `外设观察先验发布器` 详细设计和对应实现映射计划。该设计承接 D6 场景体素只读快照，向 `场景体素先验引导观察材料前处理` 提供中间发布桥。

决定：

新增：

```text
规范/详细设计/待完成/20260703_外设观察先验发布器详细设计_v0.1.md
计划/20260703_外设观察先验发布器实现映射计划_v0.1.md
```

计划索引新增 P8，状态为：

```text
实现映射计划 / 待 S0 只读扫描
```

P2 桥接型详细设计补齐计划改为：

```text
D1-D7 七份必须桥接设计已生成；
后置 H1 / H2 仅在切入共享关联判断或稳定复现评价前再判断是否生成。
```

原因：

当前设计口径认为，现有锚点已有 SceneVoxelPrior 只读快照、目标观察约束缓冲区、外设报告项处理策略槽和视觉融合输入，但仍缺少一个明确职责链把 `SceneVoxelPrior + 目标观察约束 + 外设位姿 / 坐标版本 / 当前帧时间` 翻译成 D455 每帧前可非阻塞读取的外设处理先验材料。

核心边界：

```text
外设观察先验发布器是场景体素服务伴随发布器 / 运行期只读视图生成链。
它不是外设线程、不是观察 / 扫描 / 识别 / 跟踪方法、不是任务调度器、不是事实裁决者。
第一版只做 SceneVoxelPrior 到目标观察约束 / 本帧处理策略的非阻塞发布桥。
D455 每帧前只读先验；无先验、过期、坐标不一致或投影材料缺失时降级处理，不能阻塞采集。
```

不采用的方案：

```text
不把草稿中的“当前代码事实”直接写成已验证实现。
不新增长期 C++ 结构体作为外设处理先验快照。
不让发布器写世界树事实、稳定存在体素、场景体素缓存、需求满足或动作动态。
不让发布器直接调用四方法。
不把先验可用性、强匹配、降级匹配、遮挡成立或自由空间冲突成立写成普通 bool。
```

影响：

后续进入 D7 代码实施时，必须先扫描 `场景体素模块.ixx`、`外设观察报告队列.ixx`、`视觉观察融合模块.ixx` 和 D5 缺口承接入口，确认 SceneVoxelPrior 读取、目标观察约束缓冲区、报告项处理策略槽、视觉融合输入和缺口标准化入口。

七份必须桥接设计已经全部生成，后续若要实现其中任一项，均必须从对应实现映射计划的 S0 只读扫描开始。

后续验证方式：

- 文档落地阶段运行 `python .\tools\check_specs.py` 和 `git diff --check`。
- 代码实施阶段从 D7 实现映射计划 S0 开始，不得跳过只读扫描。
- 后续验收不得把 D7 详细设计 / 计划落地说成外设观察先验发布器已代码实现。

## 2026-07-03：H1 / D8 四方法共享底层关联判断能力按待 S0 落入

背景：

用户继续提供第 8 份桥接设计草稿，目标是补齐 `四方法共享底层关联判断能力` 详细设计和对应实现映射计划。该设计属于后置候选 H1，承接外设感知共同边界、像素归属 / 局部体素、结构化缺口、场景体素服务和外设观察先验发布器设计。

决定：

新增：

```text
规范/详细设计/待完成/20260703_四方法共享底层关联判断能力详细设计_v0.1.md
计划/20260703_四方法共享底层关联判断能力实现映射计划_v0.1.md
```

计划索引新增 P9，状态为：

```text
实现映射计划 / 待 S0 只读扫描
```

P2 桥接型详细设计补齐计划改为：

```text
D1-D7 七份必须桥接设计已生成；
H1 四方法共享底层关联判断能力已生成；
H2 视觉清晰度与稳定复现评价当时仍按触发条件后续判断；后续已在 H2 / D9 决策中补齐。
```

原因：

当前共同边界已经允许四方法共享底层纯判定能力，但没有单独回答共享能力的输入输出、写入权限、读取方式、缺口承接和禁止互调验证。若不补 H1，后续实现容易把共享能力做成第五个本能方法，或让观察、扫描、识别、跟踪直接互相调用。

核心边界：

```text
观察、扫描、识别、跟踪不得以本能方法身份直接互调。
四方法可以共享像素归属、候选过滤、特征匹配、反证检查、范围内对应计算、局部体素候选生成、目标观察约束解释和 SceneVoxelPrior 匹配评分。
共享能力只输出候选、评分、计数、状态码、缺口材料、建议后续方法类型和可回查证据句柄。
共享能力不得输出世界树稳定存在确认、C -> E 最终唯一归属、稳定存在体素更新、需求满足或动作动态。
```

不采用的方案：

```text
不新增第五个感知本能方法。
不新增长期 C++ 结构体作为共享判定包。
不让共享能力写世界树事实、稳定存在体素、场景体素缓存、需求满足或动作动态。
不让共享能力直接派生需求入树。
不把候选评分、遮挡候选、自由空间冲突候选写成事实。
不把候选可用、唯一归属成立、遮挡解释成立、自由空间冲突成立、可提交写成普通一阶 bool / 枚举。
```

影响：

后续进入 H1 / D8 代码实施时，必须先扫描 `视觉观察融合模块.ixx`、`外设观察报告队列.ixx`、`场景体素模块.ixx`、四方法入口和 D5 缺口承接入口，确认真实函数、共享能力已有函数、四方法调用入口、互调风险和普通 bool 风险。

后续观察、识别、跟踪、扫描实现映射计划必须引用 H1，明确它们读取共享结果的方式，但不得把共享能力写成方法互调入口。

后续验证方式：

- 文档落地阶段运行 `python .\tools\check_specs.py` 和 `git diff --check`。
- 代码实施阶段从 H1 / D8 实现映射计划 S0 开始，不得跳过只读扫描。
- 后续验收不得把 H1 详细设计 / 计划落地说成共享底层关联判断能力已代码实现。

## 2026-07-03：H2 / D9 视觉清晰度与稳定复现评价按待 S0 落入

背景：

用户继续提供第 9 份桥接设计草稿，目标是补齐 `视觉清晰度与稳定复现评价` 详细设计和对应实现映射计划。该设计属于后置候选 H2，承接观察材料质量收束、像素归属 / 局部体素证据、结构化缺口、场景体素服务、外设观察先验发布器和四方法共享底层关联判断能力设计。

决定：

新增：

```text
规范/详细设计/待完成/20260703_视觉清晰度与稳定复现评价详细设计_v0.1.md
计划/20260703_视觉清晰度与稳定复现评价实现映射计划_v0.1.md
```

计划索引新增 P10，状态为：

```text
实现映射计划 / 待 S0 只读扫描
```

P2 桥接型详细设计补齐计划改为：

```text
D1-D7 七份必须桥接设计已生成；
H1 四方法共享底层关联判断能力已生成；
H2 视觉清晰度与稳定复现评价已生成。
```

原因：

当前代码已有散落的帧质量、深度有效率、空间坐标有效率、轮廓置信度、跨帧命中、离散度、SceneVoxelPrior 匹配评分和自由空间冲突候选等输入材料，但没有统一的评价对象、评分收束、判断型状态值域、缺口分流和体素融合前置闸门。若不补 H2，后续实现容易把“单帧高清”误作稳定复现，把“先验匹配高”误作当前存在，或把“局部体素清晰”误作稳定存在体素可更新。

核心边界：

```text
视觉清晰度与稳定复现评价是一把观察域证据尺，不是事实写入权。
评价对象包括 F 观察帧、C 观察候选、V_local 局部体素候选、O 观察域结果包和 S SceneVoxelPrior。
评价输出只能是评分、计数、状态值域、阻断原因、缺口材料和可回查证据句柄。
高单帧清晰度不等于稳定复现。
高先验匹配不等于 E 当前存在。
自由空间冲突不等于 E 消失。
遮挡解释不等于跟踪成功。
局部体素清晰只能作为 C 的证据链输入，不能直接更新 E_voxel。
```

不采用的方案：

```text
不把草稿中的“当前代码事实入口”直接写成已验证实现。
不新增长期 C++ 结构体作为清晰度评价包。
不新增普通一阶 bool：是否清晰、是否稳定、是否可融合、是否可提交、是否先验一致。
不让外设线程直接写判断型状态作为一阶事实。
不让 D9 直接调用观察、扫描、识别、跟踪方法入口。
不让 D9 输出世界树稳定存在确认、C -> E 最终归属、稳定存在体素更新、需求满足或动作动态。
```

影响：

后续进入 H2 / D9 代码实施时，必须先扫描 `外设观察报告队列.ixx`、`双目相机本能适配器.h`、`视觉观察融合模块.ixx`、`场景体素模块.ixx`、局部体素 / 存在体素融合链和 D5 缺口承接入口，确认真实字段、函数、状态值域、缺口标准化入口和普通 bool 风险。

后续观察、识别、跟踪、扫描、存在体素融合实现映射计划如果需要使用清晰度、稳定复现或先验一致性评价，应引用 H2 / D9，但不得把 H2 / D9 写成事实裁决方法。

后续验证方式：

- 文档落地阶段运行 `python .\tools\check_specs.py` 和 `git diff --check`。
- 代码实施阶段从 H2 / D9 实现映射计划 S0 开始，不得跳过只读扫描。
- 后续验收不得把 H2 详细设计 / 计划落地说成视觉清晰度与稳定复现评价已代码实现。

## 2026-07-03：P11 观察候选到稳定存在收束总设计按待 S0 落入

背景：

用户要求按“分层修补 + 渐进重构 / Strangler Pattern”规划计划执行时代码落实方式。当前 P3-P10 已经补齐多份局部桥接设计和实现映射计划，但如果直接逐项孤立执行，容易在 `像素簇 -> C`、`C -> E`、`SceneVoxelPrior 解释偏置` 和 `G_gap 回流` 上重复造判断或让局部模块越权。

决定：

新增：

```text
规范/详细设计/待完成/20260703_观察候选到稳定存在收束总设计_v0.1.md
计划/20260703_观察候选到稳定存在收束实现映射计划_v0.1.md
```

计划索引新增 P11，状态为：

```text
实现映射计划 / 待 S0 只读扫描
```

原因：

当前代码已经有外设观察报告队列、视觉观察融合模块、场景体素模块、任务管理线程、双目相机适配层和需求 / 方法 / 特征系统等半成型分层骨架。问题不是推倒重写，而是语义层未统一、判断权责混杂、层间协议未固化。因此采用渐进重构：保留现有入口，新增语义收束函数，逐步替换旧判断逻辑。

核心边界：

```text
单帧高质量像素簇可以生成临时 C，但不能生成稳定 C，不能写 E。
稳定 C 必须经跨帧复现、清晰度、质量和 generation / 坐标一致性收束。
C -> E 只能生成同一性候选评分、排他性冲突和阻断原因，不能直接写世界树。
SceneVoxelPrior 只能影响 ROI、分裂、评分、遮挡解释候选、自由空间冲突候选和 G_gap，不能生成 C、否定 C 或修改 E。
G_gap 是结构化阻断证据，不是需求本身。
```

不采用的方案：

```text
不全量重构外设观察报告队列、视觉观察融合模块、场景体素模块或四方法入口。
不纯补丁式修修补补。
不新增长期 C++ 结构体承载 C_E 或语义收束包。
不新增普通一阶 bool 表达是否清晰、是否稳定、是否可融合、是否当前存在。
不让 C 直接写 E。
不让局部体素直接写 E_voxel。
不让 SceneVoxelPrior 直接生成 / 否定 C 或写 E。
不让 G_gap 直接需求化。
不让四方法互调。
```

影响：

P11 成为 P3-P10 进入代码前的总语义路线。后续执行时，先做 P11 S0 全局只读扫描，列出旧判断点、可复用入口、缺函数 / 缺入口和普通 bool 风险，再决定最小代码切片。P3-P10 后续实施不得各自发明 C、E、SceneVoxelPrior 或 G_gap 的语义。

后续验证方式：

- 文档落地阶段运行 `python .\tools\check_specs.py` 和 `git diff --check`。
- 代码实施阶段从 P11 实现映射计划 S0 全局只读扫描开始。
- 后续验收不得把 P11 详细设计 / 计划落地说成像素簇到 C、C 到 E、SceneVoxelPrior 解释偏置或 G_gap 回流已代码实现。

## YYYY-MM-DD：决策标题

背景：

决定：

原因：

不采用的方案：

影响：

后续验证方式：
