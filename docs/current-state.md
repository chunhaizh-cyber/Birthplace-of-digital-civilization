# Current State

最后更新：2026-07-03

## 当前目标

补完并落入 D1 桥接型详细设计：`外设观察材料稳定关联与入队详细设计`。

```text
本次输出是规范派生的待完成详细设计，不是代码实现计划；
代码事实只作为实现映射锚点，不作为设计依据；
后续代码仍必须先生成实现映射计划 / 代码落地契约。
```

当前工程主线仍是鱼巢 C++ 原型的需求树、任务、方法、外设感知和规范治理闭环；不得宣称自我苏醒完成、初步成熟完成、外设观察材料稳定入队已代码实现，或任何本轮未实施的代码能力已接入。

## 当前分支

`main`

当前提交和云端同步状态以 `git status --short --branch` 为准。

## 已完成

- 已将 `计划/` 根目录中 23 份旧计划移动到 `计划/完成项/`。
- 已保留 `计划/20260702_需求树父子关系角色视图实现映射计划_v0.1.md`，因为它有对应详细设计：`规范/详细设计/待完成/20260702_需求树父子关系角色视图详细设计_v0.1.md`。
- 已重写 `计划/计划索引.md` 为短索引，只保留当前有效计划入口、清理结果、新计划进入规则和下一次执行入口。
- 已明确 `计划/完成项/` 中的本轮归档只表示退出当前根目录默认队列，不自动证明旧计划业务目标已完成。
- 已保留旧计划文件，没有删除。
- 已新增 `计划/20260703_桥接型详细设计补齐计划_v0.1.md`。
- 已将该计划加入 `计划/计划索引.md`，状态为“文档治理计划 / 可执行”。
- 已在 `计划/计划索引.md` 中补充窄例外：计划目标本身是“补齐详细设计”时，可以建立详细设计治理计划，但不得作为代码实现入口。
- 已根据用户提供草稿和审查意见，新增 `规范/详细设计/待完成/20260703_外设观察材料稳定关联与入队详细设计_v0.1.md`。
- 已将原草稿中的“代码事实依据”降权为“当前实现锚点”，补充文件头、项目结构映射、L0-L3 层级、generation、幂等、TTL、等待项绑定、速度测量、失败分流和验证剧本。
- 已明确 `d455://` 等当前实现资源标识只能作为 opaque handle / 人读锚点，正式机器判断不得依赖自由文本解析。

## 正在处理

当前没有正在处理的代码实现切片。本次是 D1 桥接型详细设计落入和状态回写。

## 未完成

- D1 `外设观察材料稳定关联与入队详细设计` 已生成，但尚未生成实现映射计划 / 代码落地契约。
- 桥接型详细设计补齐计划还剩 D2-D7 六份必须补齐的桥接型详细设计未生成。
- `需求树父子关系角色视图` 仍未进入代码实现。
- `存在体素融合内部治理` 尚未生成实现映射计划 / 代码落地契约。
- `场景体素先验引导观察材料前处理` 仍未生成实现映射计划 / 代码落地契约。
- 观察、扫描、识别、跟踪四个本能方法的完整实现映射计划仍需后续按详细设计生成。
- `1175` 是规范层规则，尚未生成代码落地契约，也未扫描现有代码是否存在判断型状态直接写入偏差。

## 最近修改文件

本次修改：

```text
计划/计划索引.md
docs/current-state.md
docs/decision-log.md
计划/计划索引.md
计划/20260703_桥接型详细设计补齐计划_v0.1.md
规范/详细设计/待完成/20260703_外设观察材料稳定关联与入队详细设计_v0.1.md
```

本次保留的根目录计划：

```text
计划/20260702_需求树父子关系角色视图实现映射计划_v0.1.md
```

## 测试状态

本次是文档和计划目录整理，未运行 C++ 构建。

已运行：

```text
python .\tools\check_specs.py
hard issues: 0
warnings: 0
```

## 已知问题

- 当前工作区仍有 2 个未跟踪 `实施记录/` 文件，按用户要求不纳入本次提交。
- `计划/完成项/` 中包含本轮历史归档计划；这些文件不进入默认执行队列，也不作为业务完成声明。
- 旧计划内可能包含与当前规范、详细设计层级不一致的内容；恢复前必须重新审查。
- 新增 D1 详细设计只是待完成详细设计，不能作为代码实现入口；若要写代码，仍需生成实现映射计划 / 代码落地契约。
- D1 中列出的当前实现锚点尚未逐项做代码事实扫描和实现验收。
- 自动同步规则只适用于本次 Codex 切片相关文件；用户排除文件和无关脏文件不得混入提交或推送。

## 下一步动作

1. 提交并推送本次 D1 详细设计落入和状态文档回写。
2. 后续继续 P2 时，生成 D2 `观察材料质量收束详细设计` 和 D3 `观察域对象与中间层目标投影详细设计`。
3. D1 若进入代码实施，先生成实现映射计划 / 代码落地契约。
