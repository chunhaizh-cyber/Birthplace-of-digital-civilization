# Current State

最后更新：2026-07-03

## 当前目标

检查并优化用户提供的 `存在体素融合内部治理详细设计 v0.1` 草稿，落入项目详细设计目录，固定以下口径：

```text
存在体素融合是自我内部治理承接链，不是外设行为；
已确认存在新增同源、可回查、归属可信的体素相关观察材料后，只能先生成待处理材料；
先提取输入证据子轮廓，再生成子体素候选并挂入稳定体素子链；
合格子体素只能生成稳定体素主模型更新候选；
正式主模型更新必须经提交入口或对应领域类裁决；
稳定体素版本更新后只提交场景体素刷新请求，由场景体素服务只读刷新快照。
```

当前工程主线仍是鱼巢 C++ 原型的需求树、任务、方法、外设感知和规范治理闭环；不得宣称自我苏醒完成、初步成熟完成、外设处理先验快照代码已实现或 D455 前处理已完成。

## 当前分支

`main`

当前提交和云端同步状态以 `git status --short --branch` 为准。

## 已完成

- 已读取并优化 `C:/Users/zhchh/Downloads/20260703_存在体素融合内部治理详细设计_v0.1.md`。
- 已新增 `规范/详细设计/待完成/20260703_存在体素融合内部治理详细设计_v0.1.md`。
- 已在该详细设计中补齐文件头、上级规范链、三层体素分工、内部治理承接模型、项目结构映射、默认不合并规则、复合特征收束、反走样清单、验证剧本和代码落地前契约。
- 已将“存在体素融合内部治理采用保守候选链”作为重要技术选择写入 `docs/decision-log.md`。
- 已修订 `AGENTS.md` 第 10 章 Git 规则：闭合切片完成后自动提交并自动推送本次 Codex 修改的相关文件到常用远端。
- 已明确自动同步不混入用户已有未提交修改、明确排除文件或无关脏文件。
- 已将“闭合切片提交后自动同步云端”作为工作流决策写入 `docs/decision-log.md`。
- 已新增 `规范/详细设计/待完成/20260703_场景体素先验引导观察材料前处理详细设计_v0.1.md`。
- 已在该详细设计中固定三层边界：场景事实层、场景体素先验层、外设处理先验层。
- 已明确外设处理先验快照只能作为只读处理提示，不能作为世界树事实、候选归属裁决、需求满足或安全 / 服务结算依据。
- 已将“异步发布外设观察先验快照”作为重要技术选择写入 `docs/decision-log.md`。
- 已运行 `python .\tools\check_specs.py`，结果为 hard issues 0、warnings 0。
- 已新增 `规范/1175_根规范_特征表达与判断收束规则_20260703.md`。因 `1160`、`1170`、`1190` 均已占用，采用相邻空位 `1175`。
- 已将 `1175` 挂入 `规范/规范目录.md`。
- 已将 `1175` 补入 `规范/4120_子规范_抽象特征树与复合特征规则_20260701.md` 和 `规范/4140_子规范_枚举型实例特征值合法来源_20260701.md` 的上级规范链。
- 已将 `1175` 补入外设感知共同边界、观察、扫描、识别、跟踪五份待完成详细设计的上级规范列表。
- 已将 `1175` 加入 `docs/工程图谱/05_规则原子索引.md` 的当前根规范回读来源，规范治理检查无告警。
- 已修订 `规范/详细设计/待完成/20260702_外设感知本能方法共同边界详细设计_v0.1.md`，补入判断型枚举 / bool 收束规则、外设一阶材料边界和材料质量收束边界。
- 已修订 `规范/详细设计/待完成/20260702_本能方法_观察详细设计_v0.1.md`，把 `材料质量状态` 从外设直接输入修正为观察 / 中间层质量收束结果，并补入质量判定特征族和状态值口径。
- 已修订 `规范/详细设计/待完成/20260702_本能方法_扫描详细设计_v0.1.md`，明确“材料质量不足”必须来自质量收束状态或可回查证据。
- 外设感知本能方法相关待完成详细设计已生成：观察、扫描、识别、跟踪和共同边界。
- 已确认新生成详细设计放入 `规范/详细设计/待完成/`。
- 已生成 `规范/详细设计/待完成/20260702_需求树父子关系角色视图详细设计_v0.1.md`。
- 已生成 `计划/20260702_需求树父子关系角色视图实现映射计划_v0.1.md`，作为详细设计到代码之间的代码落地契约 / 实现映射计划。
- 已新增 `yu-chao-plan` skill，用于制定计划前检查详细设计，并在遇到冲突、不解或不确定时切入讨论、修订详细设计后再继续。
- 已新增 `docs/current-state.md` 和 `docs/decision-log.md`。
- 已在 `AGENTS.md` 中加入三文件分工、工作前阅读和任务结束状态回写规则。
- 已将本项目相关自定义 skill 实体移动到 `.codex/skills/`，并在 `C:\Users\zhchh\.codex\skills\` 下保留目录联接以兼容 Codex 默认加载路径。
- 已新增 `chatgpt-yu-chao-theory` skill，作为 ChatGPT 侧鱼巢理论探讨、状态同步和设计审查入口。

## 正在处理

当前没有正在处理的代码实现切片。本次是详细设计优化和状态回写；提交和同步状态以 Git 状态为准。

## 未完成

- `存在体素融合内部治理` 尚未生成实现映射计划 / 代码落地契约。
- 深度 / 彩色轮廓特征现有落点、稳定三维体素主模型特征、提交入口、场景体素刷新入口和可复用治理队列仍待代码扫描确认。
- 存在体素融合内部治理尚未进入代码实现或 C++ 构建验证。
- `场景体素先验引导观察材料前处理` 仍未生成实现映射计划 / 代码落地契约。
- 外设处理先验快照的具体承载入口、生命周期、清理策略、回查方式和 D455 每帧前读取入口仍待实现映射计划确认。
- D455 前处理仍未实现先验 ROI、mask、预计深度范围、分区频率和按需特征族提取。
- 当前扫描侧已有 `SceneVoxelPrior` 消费方向，但主要发生在报告材料形成之后，尚未前移为外设每帧处理前策略。
- `需求树父子关系角色视图` 仍未进入代码实现。
- 角色视图相关真实代码入口、函数名、调用点和验证剧本仍待后续实现切片扫描确认。
- `1175` 是规范层规则，尚未生成代码落地契约，也未扫描现有代码是否存在判断型状态直接写入偏差。
- 外设材料质量收束方法、判断型枚举合法写入入口和对应验证剧本尚未进入代码实现。
- 观察、扫描、识别、跟踪四个本能方法的完整实现映射计划仍需后续按详细设计生成。
- R24 需求树简化生成与内部治理闭环仍未整体闭合。
- R27 统一动态信息生成入口仍处于待条件状态，等待正式运行自然命中价值结算相关路径。

## 最近修改文件

本次修改：

```text
docs/current-state.md
docs/decision-log.md
规范/详细设计/待完成/20260703_存在体素融合内部治理详细设计_v0.1.md
```

最近已提交的关键文件：

```text
计划/20260702_需求树父子关系角色视图实现映射计划_v0.1.md
计划/计划索引.md
规范/详细设计/待完成/20260702_需求树父子关系角色视图详细设计_v0.1.md
```

## 测试状态

最近一次规范治理检查：

```text
python .\tools\check_specs.py
hard issues: 0
warnings: 0
```

本次是详细设计文档修订，未运行 C++ 构建。

## 已知问题

- 当前工作区存在多项本次任务之外的未提交修改和删除，未在本文件中判定其业务归属。
- 新增的存在体素融合内部治理详细设计仍是待完成详细设计，不能宣称代码已实现。
- 存在体素融合若落地不严，存在工作线程被误当动作来源、子体素候选污染稳定主模型、先验冒充观察证据、过融合污染存在体素模型等风险。
- 自动同步规则只适用于 Codex 完成闭合切片后的本次相关修改；用户排除文件仍不得被混入提交或推送。
- 新增的场景体素先验前处理详细设计仍是待完成详细设计，不能宣称代码已实现。
- 外设处理先验快照若落地不严，存在先验绑架观察、旧 ROI 误导处理、未知区被永久过滤等风险。
- `需求树父子关系角色视图` 只是详细设计和实现映射计划完成，不能宣称代码已实现。
- 本次只生成规范并同步文档引用；外设接口、观察材料质量收束方法和判断型枚举写入链尚未实现。
- `1175` 与现有实现是否存在偏差尚未做代码审计。
- `docs/current-state.md` 是现实快照，可能随仓库变化过期；每次 Codex 任务结束前需要回写。
- `AGENTS.md` 本次发生换行提示，Git 提示未来可能将 CRLF 替换为 LF。
- 全局 skill 路径现在依赖目录联接；如果迁移机器或克隆仓库，需要重新建立从 `C:\Users\zhchh\.codex\skills\自定义skill` 到 `.codex/skills/自定义skill` 的联接。

## 下一步动作

1. 本次详细设计优化完成后按自动同步规则提交并推送到 `birthplace/main`。
2. 若准备落地存在体素融合内部治理，先生成实现映射计划 / 代码落地契约，明确触发入口、待处理材料承接、子轮廓提取、子体素生成、正式提交和场景体素刷新入口。
3. 若准备落地场景体素先验前处理，先生成实现映射计划 / 代码落地契约，明确发布入口、读取入口、D455 前处理入口、候选输出入口和验证剧本。
