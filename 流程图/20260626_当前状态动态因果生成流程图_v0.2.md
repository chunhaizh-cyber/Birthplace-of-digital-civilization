# 当前状态、动态、因果生成流程图

更新时间：2026-06-26

## 依据

```text
docs/00_文档总索引.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
docs/工程图谱/05_规则原子索引.md
规范/方法动作动态硬规则规范20260512.md
规范/动作动态即因果账本规范20260512.md
规范/动态信息分层获取与聚合规范20260430.md
规范/因果补完流程规范20260516.md
规范/因果信息用途与用法规范20260516.md
规范/需求临时因果视图展开规范20260526.md

状态类.cpp:303-363
动态类.h:10-15, 69-107
动态类.cpp:125-135, 451-478, 680-868, 956-1000, 1323-1330
二次特征应用模块.ixx:958-1052, 1078-1114, 1253-1316, 1568-1604, 2770-2780
因果类.cpp:2224-2264, 2267-2568, 2572-2613
任务模块.执行.ixx:119-171
任务模块.筹办.ixx:993-1077, 1944-2230, 5603-5647
自我线程模块.impl.cpp:12941-13018
自我动作实现.扫描.ixx:1336-1374, 1597-1637
自我动作实现模块.ixx:10355-10391, 12235-12279
自我类.cpp:511-546, 591-610, 683-700, 761-778
```

## 说明

本图是 v0.2 修订版：同一次特征值变化不再按“无动作状态变化动态 / 带动作动态”二选一处理，而是按“一变两账”表达。状态迁移动态证明“变了”，动作致变动态证明“这个变化与某个动作有关”；两套账通过同源变化证据组互证，结算只算一次。本图不宣称当前代码已经完全实现该口径。

## 流程图

```mermaid
flowchart TD
    Start["输入入口"] --> E0{"入口类型"}

    subgraph S["状态迁移底账"]
        E0 -->|状态事实写入| S1["状态类::创建状态<br/>主体 / 特征 / 值有效"]
        S1 --> S2["世界树.状态记录后刷新动态"]
        S2 --> S3["查找相邻前状态<br/>同场景 / 同主体 / 同特征 / 时间有序"]
        S3 -->|未找到前状态| S4["只保留当前状态<br/>不生成状态迁移动态"]
        S3 -->|找到前状态| S5["创建或复用 D_state<br/>状态迁移动态 A -> B"]
        S5 --> S6["生成动态二次特征<br/>变化量 / 方向 / 时长 / 来源状态"]
        S6 --> S7{"果状态迁移模板<br/>是否存在"}
        S7 -->|存在| S8["生成或命中 C_state<br/>状态变化因果 / 状态变化线索"]
        S7 -->|缺失| S9["结构化缺口<br/>或创建候选状态迁移模板"]
    end

    subgraph A["动作致变能力账"]
        E0 -->|本能 / 治理 / 任务动作执行| A1["执行动作<br/>读取输入场景 / 写运行存在 / 写结果状态"]
        A1 --> A2["构造动作致变动态创建包<br/>来源方法 / 动作语义键 / 动态类型 / 输入输出场景 / 证据材料"]
        A2 --> A3{"是否有状态端点<br/>或同源 D_state"}
        A3 -->|否| A4["动作事件动态或结构化缺口<br/>不进入动作致变因果链"]
        A3 -->|是| A5["创建 D_action<br/>动作致变动态"]
        A5 --> A6["D_action 绑定 D_state<br/>同源变化证据组 G"]
        A6 --> A7["生成或命中 C_action<br/>动作致变因果"]
        A7 --> A8["C_action 引用 C_state<br/>证明动作能力"]
    end

    subgraph L["后补动作来源"]
        L1["已有 D_state / C_state<br/>先按状态变化处理"] --> L2["后续补齐来源动作 / 方法"]
        L2 --> L3["不得改写 D_state"]
        L3 --> L4["补建 D_action<br/>引用 D_state 和证据组 G"]
        L4 --> A7
    end

    S5 --> G1["同源变化证据组 G<br/>同一次变化的结构化证据组"]
    A6 --> G1
    S8 --> G1
    A8 --> G1

    subgraph C["因果类型与用途"]
        S8 --> C1["状态变化因果<br/>证明变化模式"]
        A8 --> C2["动作致变因果<br/>证明动作能力"]
        C1 --> C3["学习 / 练习 / 证据补齐 / 观察补齐线索"]
        C2 --> C4["任务筹办可直接消费<br/>映射候选方法"]
        C1 -.互证提高评分.-> C2
    end

    subgraph Q["任务筹办消费"]
        C4 --> Q1["优先使用动作致变因果<br/>可映射可执行方法"]
        C3 --> Q2["只有状态变化因果<br/>说明知道能变但不知道怎么让它变"]
        Q2 --> Q3["生成学习需求 / 练习需求<br/>或证据补齐 / 观察补齐需求"]
        Q1 --> Q4["构造候选方法 / 当前方法<br/>进入任务筹办后续流程"]
    end

    subgraph Z["结算边界"]
        G1 --> Z1["双账互证"]
        Z1 --> Z2["单组结算<br/>安全 / 服务 / 任务完成 / 需求满足只按 G 算一次"]
    end

    subgraph R["硬边界"]
        R1["线程不是动作来源"]
        R2["任务筹办 / 任务执行不是本能方法<br/>不进入候选集"]
        R3["外部变化不能伪装成自我方法动作动态<br/>只能是观察 / 外部事件 / 环境 / 未知来源动态"]
        R4["后处理后补动作来源<br/>不得篡改状态迁移账，只能补建动作致变账"]
        R5["输出结果场景只承载世界事实<br/>不承载任务状态回执"]
        R6["状态变化因果不能直接当作方法候选<br/>动作致变因果才可直接筹办消费"]
    end

    R1 -.约束.-> A2
    R2 -.约束.-> Q4
    R3 -.约束.-> A5
    R4 -.约束.-> L3
    R5 -.约束.-> A1
    R6 -.约束.-> Q2
```

## 关键边界

```text
1. 状态迁移动态是事实底账，动作致变动态是能力账；同一次变化允许两者同源共存。
2. 状态变化因果证明变化模式；动作致变因果证明动作能力。任务筹办只把动作致变因果作为直接方法候选。
3. 后补动作来源时，不得把既有 D_state 改成动作动态；必须补建 D_action 并引用 D_state。
4. D_state、D_action、C_state、C_action 必须绑定同源变化证据组；双账互证，但安全 / 服务 / 任务完成 / 需求满足只结算一次。
5. 缺状态端点、缺动作来源或缺果状态迁移模板时，必须返回结构化缺口或候选模板，不得静默停止。
6. 当前代码仍暴露部分动作动态入口缺实际状态端点、部分动作信息后补的问题；本图不宣称扫描需求自然生长闭合、练习链完全闭合、自我苏醒完成或初步成熟完成。
```
