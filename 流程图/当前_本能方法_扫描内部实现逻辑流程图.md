# 当前_本能方法_扫描内部实现逻辑流程图

生成时间：2026-06-07

依据范围：当前代码。本文只描述代码中已经能追踪到的扫描链路，不把外设报告、等待项、目标观察约束或队列项当成正式世界事实。

## 1. 当前代码结论

```text
当前代码中的“扫描”不是 D455 报告直接入账。

实际链路分成五段：

1. 识别 / 对应事实提交后，6150 写入稳定子集桥：
   正式已确认观察存在
   可观测单位到存在映射项
   外设观察存在自我归属状态 == 已归属
   扫描可启动状态 == 1
   扫描目标观察约束特征组

2. 任务管理界面线程按稳定子集可用扫描约束批量创建扫描等待项。

3. D455 独占线程按等待项和目标观察约束生成扫描变化报告。

4. 任务管理工作线程匹配外设报告并做材料质量分级：
   不合格 -> 只回执
   可用 -> 待复验观察承接，不生成扫描变化动态候选
   优质 -> 扫描变化观察承接请求

5. 自我侧 6146 自我_扫描已识别区域 读取报告和正式目标存在：
   有历史 + 材料可生成变化候选 -> 计算变化结果 / 变化事件
   有历史 + 材料只可复验 -> 待重识别 / 补观察缺口
   无历史 -> 首次扫描只生成发现存在或发现特征事实
```

当前边界：

```text
扫描报告只是材料。
目标观察约束只是外设观察输入。
6146 是扫描方法主体，但不是最终提交入口。
首次扫描不写变化动态。
无变化不写变化动态。
只有“有历史 + 优质扫描材料 + 有实际变化”才允许提交观察存在特征值变化事实。
扫描链不直接写需求满足，不直接写安全值。
```

## 2. 代码依据索引

| 链路 | 当前代码位置 | 说明 |
| --- | --- | --- |
| 方法登记 | `本能方法类.cpp:56-64` | 登记 `自我_扫描已识别区域`、`提交_观察存在特征值变化事实`、`提交_当前观察范围可观测单位存在对应事实`、`提交_观察存在发现事实`。 |
| 稳定子集桥 | `自我动作实现.外设模块.ixx:21327-21839` | 6150 写正式已确认观察存在、映射项、扫描可启动状态、扫描目标观察约束。 |
| 扫描等待项 | `任务模块.管理界面线程.impl.cpp:3064-3190` | 读取可用扫描目标观察约束，按稳定子集全量创建 `已识别场景扫描变化` 等待项。 |
| D455 扫描前置 | `外设线程_D455深度相机.ixx:978-991` | 等待项绑定目标观察约束时，必须可强匹配或可降级匹配。 |
| D455 扫描报告生成 | `外设线程_D455深度相机.ixx:1004-1113` | 从逐簇识别报告生成扫描变化报告；首帧缺基准时只建立基准。 |
| D455 循环入队 | `外设线程_D455深度相机.ixx:1550-1599` | 每帧生成逐簇识别报告后，按扫描等待项生成扫描变化报告并入队。 |
| 任务管理质量门 | `任务模块.管理工作线程.ixx:3239-3358` | 扫描报告按质量分为回执、待复验观察承接、扫描变化观察承接。 |
| 6146 扫描主体 | `自我动作实现.外设模块.ixx:19799-20090` | 读取当前存在、历史存在、扫描报告、归属状态、扫描可启动状态和材料质量，生成扫描正式结果。 |
| 发现事实提交 | `自我动作实现.外设模块.ixx:21963-22125` | 首次扫描发现存在或发现特征事实的提交入口。 |
| 变化事实提交 | `自我动作实现.外设模块.ixx:22128-22310` | 有历史、有实际变化、来源质量允许时，提交观察存在特征值变化事实。 |

## 3. 总流程图

```mermaid
flowchart TD
    A["6145 识别结果 / 对应事实材料"]
    B["提交_当前观察范围可观测单位存在对应事实<br/>方法ID: 6150"]
    C{"对应事实可写?"}
    C0["条件不足<br/>不写扫描目标上下文"]
    D["写稳定子集桥<br/>正式已确认观察存在<br/>可观测单位到存在映射项"]
    E["写目标存在扫描前置<br/>外设观察存在自我归属状态=已归属<br/>扫描可启动状态=1"]
    F["提交扫描目标观察约束特征组<br/>目标存在ID + 目标特征ID<br/>ROI / 掩码 / 点集 / AABB<br/>TTL / generation"]
    G["任务管理界面线程<br/>读取可用扫描目标观察约束集合"]
    H["批量创建外设观察等待项<br/>模式=已识别场景扫描变化<br/>期望报告=扫描变化报告<br/>范围=稳定子集全量"]
    I["D455 独占线程采集一帧<br/>生成逐簇识别报告"]
    J{"扫描等待项前置满足?"}
    J0["跳过本等待项<br/>缺目标约束或目标区域"]
    K["生成扫描变化报告<br/>只写外设消息队列"]
    L["任务管理工作线程匹配报告<br/>判断观察材料质量"]
    M{"扫描材料等级"}
    M0["不合格<br/>只生成任务回执"]
    M1["可用<br/>生成待复验观察承接请求<br/>不生成扫描变化动态候选"]
    M2["优质<br/>生成扫描变化观察承接请求"]
    N["自我线程按报告ID回查材料<br/>按目标存在生成 6146 任务主体"]
    O["自我_扫描已识别区域<br/>方法ID: 6146"]
    P{"6146 闸门成立?"}
    P0["失败<br/>写方法失败和条件不足"]
    Q{"是否有历史存在?"}
    R{"材料可生成扫描变化候选?"}
    S["计算匹配关系<br/>计算变化结果<br/>计算变化事件<br/>扫描入账状态=已生成"]
    T["材料不足以生成变化候选<br/>扫描入账状态=待重识别<br/>补观察 / 复验缺口"]
    U["无历史存在<br/>创建或取得观察存在发现事实<br/>首次扫描只写发现事实"]
    V{"提交入口选择"}
    W["提交_观察存在发现事实<br/>发现状态=已发现<br/>不写变化动态"]
    X["提交_观察存在特征值变化事实<br/>要求有实际变化<br/>来源可生成扫描变化候选"]

    A --> B --> C
    C -->|"否"| C0
    C -->|"是"| D --> E --> F --> G --> H --> I --> J
    J -->|"否"| J0
    J -->|"是"| K --> L --> M
    M --> M0
    M --> M1 --> N
    M --> M2 --> N
    N --> O --> P
    P -->|"否"| P0
    P -->|"是"| Q
    Q -->|"有历史"| R
    R -->|"是"| S --> V
    R -->|"否"| T --> V
    Q -->|"无历史"| U --> V
    V -->|"发现事实"| W
    V -->|"变化事实"| X
```

## 4. 外设侧扫描报告生成

```mermaid
flowchart TD
    A["D455 线程循环"]
    B["采集一帧<br/>双目相机本能适配器::采集一帧"]
    C{"采集成功?"}
    C0["本轮无扫描报告"]
    D["读取上一逐簇识别报告<br/>作为可比基准"]
    E["生成当前逐簇识别报告<br/>提交外设观察报告<br/>提交短期观察材料页"]
    F["读取扫描变化等待项集合"]
    G{"等待项前置满足?"}
    G0["跳过<br/>缺已识别场景基准或目标区域"]
    H["读取目标观察约束<br/>判断可强匹配 / 可降级匹配"]
    I{"目标约束命中当前簇?"}
    I0["采集失败报告<br/>差异摘要=约束采集失败<br/>不生成扫描变化动态候选"]
    J{"是否有上一逐簇识别基准?"}
    J0["无基准<br/>扫描变化报告只建立首帧基准<br/>首帧只形成发现存在或发现特征候选"]
    K["有基准<br/>比较跨帧簇ID<br/>统计新增 / 消失 / 变化 / 高风险未知"]
    L["提交扫描变化报告<br/>报告仍是外设材料容器"]

    A --> B --> C
    C -->|"否"| C0
    C -->|"是"| D --> E --> F --> G
    G -->|"否"| G0
    G -->|"是"| H --> I
    I -->|"否"| I0 --> L
    I -->|"是"| J
    J -->|"否"| J0 --> L
    J -->|"是"| K --> L
```

## 5. 任务管理承接质量门

```mermaid
flowchart TD
    A["任务管理工作线程<br/>取得等待项 + 外设报告"]
    B["判断外设观察材料质量"]
    C{"报告类型=扫描变化报告?"}
    C0["其他报告类型<br/>按识别 / 跟踪 / 普通回执分支处理"]
    D{"材料是否不合格?"}
    D0["不合格<br/>处理状态=已生成任务回执<br/>不生成观察事实承接请求"]
    E{"可生成扫描变化候选?"}
    E1["是<br/>处理状态=已生成观察事实承接请求<br/>承接摘要=优质扫描变化报告"]
    F{"可进入验证方法?"}
    F1["是<br/>处理状态=已生成观察事实承接请求<br/>承接摘要=可用扫描材料 / 待复验<br/>不生成扫描变化动态候选"]
    F0["否<br/>只生成任务回执"]
    G["构造任务管理观察事实承接请求<br/>携带等待项ID / 报告ID / 来源需求ID<br/>目标观察约束ID / 目标存在ID<br/>目标特征类型ID / 当前值 / generation"]
    H["上行到自我线程<br/>报告标记已消费<br/>但仍可按报告ID回查直到TTL清理"]

    A --> B --> C
    C -->|"否"| C0
    C -->|"是"| D
    D -->|"是"| D0
    D -->|"否"| E
    E -->|"是"| E1 --> G
    E -->|"否"| F
    F -->|"是"| F1 --> G
    F -->|"否"| F0
    G --> H
```

## 6. 6146 内部实现流程

```mermaid
flowchart TD
    A["进入 自我_扫描已识别区域<br/>方法ID: 6146"]
    B["确保本能方法自身环境"]
    C["解析实际输出场景<br/>解析相机存在 / 目标宿主<br/>解析当前观察存在 / 历史观察存在"]
    D["读取最新扫描变化报告<br/>判定自我侧观察材料质量"]
    E["读取归属状态<br/>外设观察存在自我归属状态"]
    F["读取扫描可启动状态"]
    G{"闸门成立?"}
    G0["失败<br/>执行缺口=当前方法条件不满足"]
    H{"有历史存在?"}
    I{"扫描材料可生成变化候选?"}
    J["计算观察存在匹配关系"]
    K["计算观察存在变化结果"]
    L["计算观察变化事件"]
    M["扫描入账状态=已生成<br/>发现状态=未发现"]
    N{"变化状态"}
    N0["未变化<br/>扫描稳定计数+1<br/>不写变化动态"]
    N1["实际变化<br/>允许后续变化事实提交"]
    N2["冲突 / 不确定<br/>写待复验缺口"]
    O["有历史但材料不足<br/>扫描入账状态=待重识别<br/>发现状态=证据不足<br/>写补观察 / 复验缺口"]
    P["无历史<br/>取或创建观察存在发现事实<br/>发现状态=已发现<br/>扫描入账状态=已生成"]
    Q["写扫描正式结果摘要<br/>报告ID / 归属状态 / 扫描可启动状态<br/>基准差异 / 轮廓变化 / 最近扫描时间<br/>下一轮扫描基准 / 动作动态"]
    R["输出运行虚拟存在<br/>更新方法运行账"]

    A --> B --> C --> D --> E --> F --> G
    G -->|"否"| G0 --> Q
    G -->|"是"| H
    H -->|"有历史"| I
    I -->|"是"| J --> K --> L --> M --> N
    N --> N0 --> Q
    N --> N1 --> Q
    N --> N2 --> Q
    I -->|"否"| O --> Q
    H -->|"无历史"| P --> Q
    Q --> R
```

6146 成功闸门对应当前代码条件：

```text
有当前存在
&& 归属状态已读
&& 归属状态 == 外设观察存在自我归属状态_已归属
&& 扫描可启动状态已读
&& 扫描可启动状态 == 1
&& 扫描材料可复验
```

6146 生成变化候选的额外条件：

```text
有历史存在
&& 扫描材料可生成变化候选
```

## 7. 提交入口流程

```mermaid
flowchart TD
    A["6146 扫描正式结果"]
    B{"结果类型"}
    C["首次扫描 / 无历史<br/>观察存在发现状态=已发现"]
    D["有历史且有实际变化<br/>观察存在变化结果已生成<br/>变化状态 != 未变化"]
    E["有历史但无变化<br/>或材料只可复验<br/>不提交变化事实"]
    F["提交_观察存在发现事实"]
    G{"发现事实提交条件成立?"}
    G0["失败<br/>提交状态=条件不足<br/>发现状态=证据不足"]
    H["成功<br/>观察存在发现事实提交状态=已提交<br/>第一次扫描只写发现存在或发现特征事实<br/>不写变化动态"]
    I["提交_观察存在特征值变化事实"]
    J{"变化事实提交条件成立?"}
    J0["失败<br/>提交状态=条件不足<br/>不写存在特征变化动态"]
    K["成功<br/>观察存在特征值变化事实提交状态=已提交<br/>写存在特征变化动态"]

    A --> B
    B -->|"发现"| C --> F --> G
    G -->|"否"| G0
    G -->|"是"| H
    B -->|"变化"| D --> I --> J
    J -->|"否"| J0
    J -->|"是"| K
    B -->|"无可提交变化"| E
```

发现事实提交条件：

```text
当前存在存在
&& 扫描正式结果可读
&& 观察存在发现状态 == 已发现
&& 来源报告可进入验证方法
```

变化事实提交条件：

```text
当前存在存在
&& 已识别区域扫描入账状态 == 已生成
&& 变化结果已生成
&& 变化状态 != 未变化
&& 来源报告可生成扫描变化候选
```

## 8. 当前运行状态口径

按当前计划索引记录，扫描输入条件桥已经闭合：

```text
6150 已形成稳定子集扫描目标上下文。
任务管理已按稳定子集全量提交扫描等待项。
D455 已生成扫描变化报告。
自我线程已按正式目标存在生成扫描任务主体。
6146 已自然执行。
归属状态已读 == 1。
扫描可启动状态已读 == 1。
扫描闸门成立 == 1。
```

但当前材料质量门仍停在：

```text
当前扫描材料等级 == 可用
可生成扫描变化候选 == 0
可生成跟踪动态候选 == 0
可进入提交入口组织 == 0
```

因此当前代码口径下，这类材料只能作为：

```text
可复验材料
首帧扫描基准
待重识别输入
```

不能宣称已经生成：

```text
扫描变化动态候选
稳定跟踪动态候选
提交入口事实
安全值变化
需求满足
```

## 9. 硬边界

```text
1. D455 是观察材料生产者，不是世界真值裁决者。
2. 外设报告 / 等待项 / 目标观察约束 / 队列项不进入正式认知链作为事实主体。
3. 扫描必须绑定已归属且扫描可启动的当前存在。
4. 首次扫描只能建立发现存在或发现特征事实，不写变化动态。
5. 有历史但材料未达“可生成扫描变化候选”时，只能待重识别或补观察复验。
6. 无实际变化时不提交变化事实。
7. 提交_观察存在特征值变化事实不直接消费外设提交包，只消费 6146 组织出的正式扫描结果和可回查来源报告质量。
8. 扫描链不直接写需求满足，不直接写安全值。
```
