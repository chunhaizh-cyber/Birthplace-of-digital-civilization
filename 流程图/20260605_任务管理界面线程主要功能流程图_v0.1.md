# 任务管理界面线程主要功能流程图 v0.1

更新时间：2026-06-05

## 用途

本图用于梳理任务管理界面线程在鱼巢内部循环中的主要职责。重点区分：

- 自我线程负责需求治理、任务发起意图、调度意图、需求回写和价值结算裁决。
- 任务管理界面线程负责承接任务治理请求、创建 / 复用任务、维护队列、派发工作项、归并工作结果、推进任务状态和上行回执。
- 任务管理工作线程负责执行任务筹办普通函数和任务执行普通函数。
- 任务筹办 / 任务执行不是本能方法，不进入方法候选集，不作为方法动作动态来源。

依据：

- `规范/自我线程与任务管理线程权责边界规范20260512.md`
- `规范/任务筹办与执行边界规范20260512.md`
- `规范/任务根据需求初始化规范20260509.md`
- `规范/任务推进状况回执闭环规范20260508.md`
- `任务模块.管理界面线程.ixx`
- `任务模块.管理线程协议.ixx`
- `任务模块.管理界面线程.impl.cpp`

## 任务管理界面线程总图

```mermaid
flowchart TD
    %% 上游输入
    SELF_REQ["自我线程任务治理请求<br/>发起 / 调度 / 重筹办 / 等待 / 取消 / 更新权重"]
    STATE_REQ["状态推进与回执请求<br/>状态推进 / 权重固化 / 价值结算 / 派生入树回执"]
    WORK_DONE_IN["工作线程完成项<br/>筹办结果 / 执行结果 / 上行消息"]
    PERIPHERAL_WAIT["外设观察等待项<br/>报告命中 / 观察事实承接"]

    %% 界面线程承接
    START["启动或确保任务管理界面线程"]
    IDEMPOTENT["请求登记与幂等处理<br/>请求ID / 幂等键 / 请求表"]
    VALIDATE{"请求是否可承接?<br/>来源需求 / 任务节点 / 创建权限"}
    CREATE_TASK["创建或复用任务信息节点<br/>任务树运行壳 / 调度壳"]
    BIND_NEED["绑定来源需求与任务关系<br/>需求 -> 任务信息节点"]
    TASK_EXIST["确保任务虚拟存在<br/>任务作为语义对象 / 概念集 / 特征槽"]
    INIT_TASK["用需求初始化任务语义<br/>目标 / 场景 / 权重 / 完成判据"]
    BUILD_WORK["构造任务工作项<br/>筹办 / 执行 / 控制态 / 局部运行态"]
    DISPATCH_GATE{"允许派发工作线程?"}
    HOLD_ONLY["只完成承接或等待<br/>后续由调度意图重入"]

    %% 队列和派发
    ENQUEUE["登记等待工作项<br/>待筹办队列 / 待执行队列"]
    QUEUE_SORT["队列排序与派发判断<br/>优先级 / 阶段 / 最早派发 / 租约 / 重试"]
    INFLIGHT["在途工作项索引<br/>任务主键 / 状态版本 / 租约截止"]
    WORKER_POOL["任务管理工作线程池<br/>后台 worker 或本轮同步消费"]

    %% 工作线程生命周期
    PREP["任务筹办普通函数<br/>找路径 / 找方法 / 找条件"]
    PREP_RECEIPT["筹办回执<br/>就绪 / 等待 / 缺口 / 派生建议"]
    READY_GATE{"任务是否就绪?"}
    APPROVAL["批准执行模块（待细化）<br/>执行前治理闸门"]
    APPROVAL_GATE{"是否批准执行?"}
    EXEC["任务执行普通函数<br/>调用当前方法 / 生成输出结果场景"]
    EXEC_RECEIPT["执行回执<br/>结果状态 / 执行缺口 / 派生建议"]
    WORK_RESULT["工作完成项<br/>任务推进回执 / 执行数据 / 上行消息"]

    %% 结果归并和上行
    COMPLETE_QUEUE["完成队列"]
    MERGE_CHECK{"工作结果身份与租约<br/>是否匹配?"}
    MERGE["归并工作结果到任务壳<br/>同步筹办 / 执行回执字段"]
    STATE_SUBMIT["提交任务状态变化<br/>正式提交入口 / 任务状态动作动态证据"]
    UPLINK_BUILD["生成任务管理上行消息<br/>派生需求 / 结果状态 / 观察事实 / 线程状态"]
    UPLINK_QUEUE["上行队列"]
    SELF_READ["自我线程提取上行消息<br/>需求树裁决 / 需求回写 / 价值结算"]

    %% 外设和异常
    PERIPHERAL_HANDLE["巡检外设观察等待项<br/>任务回执或观察事实承接请求"]
    REJECT["拒绝承接 / 校验失败"]
    DEAD["死信 / 租约失效 / 归并拒绝"]
    PRESSURE["全循环压力账材料<br/>失败退出 / 阻断 / 拒绝 / 超时 / 队列堆积"]

    %% 硬边界
    RULE_DEMAND["边界: 不直接写需求树<br/>派生需求交自我线程或正式提交入口裁决"]
    RULE_ACTION["边界: 线程不是动作来源<br/>任务状态变化必须走提交入口"]
    RULE_LIFECYCLE["边界: 筹办 / 执行是生命周期普通函数<br/>不是本能方法，不进候选集"]
    RULE_APPROVAL["边界: 批准执行是执行前闸门<br/>批准后才允许执行当前方法"]

    subgraph INPUT["上游输入"]
        SELF_REQ --> START
        STATE_REQ --> STATE_SUBMIT
        WORK_DONE_IN --> COMPLETE_QUEUE
        PERIPHERAL_WAIT --> PERIPHERAL_HANDLE
    end

    subgraph ACCEPT["请求承接与任务初始化"]
        START --> IDEMPOTENT --> VALIDATE
        VALIDATE -->|可承接| CREATE_TASK --> BIND_NEED --> TASK_EXIST --> INIT_TASK --> BUILD_WORK --> DISPATCH_GATE
        VALIDATE -->|不可承接| REJECT
        DISPATCH_GATE -->|否| HOLD_ONLY
        DISPATCH_GATE -->|是| ENQUEUE
    end

    subgraph SCHEDULE["队列维护与工作项派发"]
        ENQUEUE --> QUEUE_SORT --> INFLIGHT --> WORKER_POOL
        HOLD_ONLY --> UPLINK_BUILD
    end

    subgraph WORKER["任务管理工作线程"]
        WORKER_POOL --> PREP --> PREP_RECEIPT --> READY_GATE
        READY_GATE -->|未就绪 / 等待 / 缺口| WORK_RESULT
        READY_GATE -->|就绪| APPROVAL --> APPROVAL_GATE
        APPROVAL_GATE -->|批准| EXEC --> EXEC_RECEIPT --> WORK_RESULT
        APPROVAL_GATE -->|暂缓 / 拒绝 / 补证据| WORK_RESULT
    end

    subgraph MERGE_UP["完成归并与上行"]
        WORK_RESULT --> COMPLETE_QUEUE --> MERGE_CHECK
        MERGE_CHECK -->|匹配| MERGE --> STATE_SUBMIT --> UPLINK_BUILD --> UPLINK_QUEUE --> SELF_READ
        MERGE_CHECK -->|不匹配| DEAD
        PERIPHERAL_HANDLE --> UPLINK_BUILD
    end

    subgraph PRESSURE_FLOW["压力来源"]
        REJECT --> PRESSURE
        DEAD --> PRESSURE
        READY_GATE -->|反复等待 / 队列堆积| PRESSURE
        APPROVAL_GATE -->|暂缓 / 拒绝 / 补证据| PRESSURE
        STATE_SUBMIT -->|提交失败| PRESSURE
    end

    subgraph RULES["硬规则"]
        RULE_DEMAND -.约束.-> UPLINK_BUILD
        RULE_DEMAND -.约束.-> SELF_READ
        RULE_ACTION -.约束.-> STATE_SUBMIT
        RULE_LIFECYCLE -.约束.-> PREP
        RULE_LIFECYCLE -.约束.-> EXEC
        RULE_APPROVAL -.约束.-> APPROVAL
    end

    classDef self fill:#e8f4ff,stroke:#2f6f9f,color:#102030;
    classDef task fill:#eef7ed,stroke:#3f7f4f,color:#102010;
    classDef worker fill:#fff3dc,stroke:#a0671e,color:#24160a;
    classDef gate fill:#fff8e8,stroke:#9a6a1a,color:#24160a;
    classDef warn fill:#ffecec,stroke:#b84a4a,color:#401111;
    classDef pressure fill:#fff0f3,stroke:#b4234a,color:#3a0d18;
    classDef result fill:#f1edff,stroke:#6c56a4,color:#1d1730;

    class SELF_REQ,STATE_REQ,SELF_READ self;
    class START,IDEMPOTENT,CREATE_TASK,BIND_NEED,TASK_EXIST,INIT_TASK,BUILD_WORK,HOLD_ONLY,ENQUEUE,QUEUE_SORT,INFLIGHT,COMPLETE_QUEUE,MERGE,STATE_SUBMIT,UPLINK_BUILD,UPLINK_QUEUE,PERIPHERAL_HANDLE task;
    class WORKER_POOL,PREP,PREP_RECEIPT,APPROVAL,EXEC,EXEC_RECEIPT,WORK_RESULT worker;
    class VALIDATE,DISPATCH_GATE,READY_GATE,APPROVAL_GATE,MERGE_CHECK gate;
    class WORK_DONE_IN,PERIPHERAL_WAIT result;
    class REJECT,DEAD,RULE_DEMAND,RULE_ACTION,RULE_LIFECYCLE,RULE_APPROVAL warn;
    class PRESSURE pressure;
```

## 本图暂定结论

1. 任务管理界面线程的核心位置是任务域接口层：承接自我线程的治理意图，并把它转换成任务生命周期可处理的任务壳、工作项和队列状态。
2. 它负责创建或复用任务信息节点、绑定来源需求、确保任务虚拟存在、初始化任务语义。
3. 它维护待筹办队列、待执行队列、完成队列、死信队列、在途工作项索引、等待任务索引和挂起任务索引。
4. 它负责派发任务工作项给任务管理工作线程，并按优先级、阶段、最早派发时间、租约和重试次数排序。
5. 任务筹办和任务执行由任务管理工作线程调用，但二者仍是任务生命周期普通函数，不是本能方法。
6. 任务筹办的职责是找路径、找方法、找条件；任务执行的职责是批准后调用当前方法并生成输出结果场景和执行回执。
7. 工作线程完成后，界面线程负责校验身份和租约，归并结果到任务壳，同步回执字段，并通过正式提交入口推进任务状态。
8. 界面线程向自我线程上行派生需求消息、结果状态消息、观察事实承接请求和线程状态消息。
9. 界面线程不直接写需求树，不直接结算安全值 / 服务值；需求树变化和价值结算由自我线程或正式提交入口裁决。
10. 拒绝承接、租约失效、死信、批准拒绝、反复等待、队列堆积等都应作为全循环压力账材料。

## 后续细分图建议

```text
任务治理请求承接细分图
任务工作项派发与租约重派细分图
任务筹办回执归并细分图
执行批准模块细分图
任务完成归并与上行消息细分图
外设观察等待项承接细分图
```
