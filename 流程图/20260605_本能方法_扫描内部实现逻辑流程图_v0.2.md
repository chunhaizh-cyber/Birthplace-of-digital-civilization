# 本能方法：扫描内部实现逻辑流程图 v0.2

更新时间：2026-06-05

## 定位

```text
扫描记变化。

扫描只处理已归属、已有比较对象的存在；
扫描发现已归属存在的特征值变化；
未归属外设观察存在和超值域特征不能进入正常动态生成链。
```

正式输入输出只能承接：

```text
存在
特征
二次特征
状态动态
```

## 流程图

```mermaid
flowchart TD
    START["扫描本能方法启动<br/>目标: 发现已归属存在的特征变化"]
    IN0["输入条件特征<br/>扫描范围明确状态=1<br/>已归属可扫描存在数量>0<br/>未归属外设观察存在数量=0<br/>上一轮扫描基准特征可读"]
    IN1["输入存在与特征<br/>已归属存在集合<br/>当前特征值集合<br/>基准/上一轮特征值集合<br/>存在特征值域集合"]
    G0{"扫描条件是否具备?"}
    GAP0["输出缺口特征<br/>扫描范围不明确<br/>或无已归属可扫描存在<br/>或基准特征不可读"]

    U0{"是否存在未归属外设观察存在?"}
    U1["未归属闸门<br/>未归属外设观察存在数量>0<br/>未归属外设观察存在动态不可生成状态=1<br/>识别线索状态=已生成"]

    A0["逐已归属存在读取当前特征<br/>目标宿主=自我存在 / 已归属观察存在<br/>同一存在 + 同一特征类型"]
    A1{"是否首次扫描<br/>或无可比基准?"}
    FIRST["首次扫描<br/>写入当前特征值<br/>写入下一轮扫描基准特征<br/>不得生成变化动态"]

    CMP["计算变化二次特征<br/>当前特征_基准特征.差异值<br/>变化幅度 / 变化方向 / 变化持续帧数"]
    V0{"当前值是否落入<br/>该存在特征值域?"}
    CONFLICT["值域冲突<br/>已归属存在.特征值域冲突状态=存在<br/>特征值域冲突动态=已生成<br/>复验/识别线索状态=已生成"]
    C0{"变化是否超过记录阈值?"}
    DYN["值域内变化<br/>已归属存在.当前特征值=当前值<br/>已归属存在.特征变化动态=已生成<br/>变化二次特征入账"]
    STATIC["无变化或低于记录阈值<br/>不生成完整变化动态<br/>更新最近扫描时间 / 稳定计数 / 下一轮基准特征"]
    BASE["更新下一轮扫描基准特征<br/>下一轮扫描基准特征状态=已更新"]

    OUT0["输出特征<br/>已归属存在当前特征值<br/>最近扫描时间<br/>稳定计数<br/>下一轮扫描基准特征"]
    OUT1["输出二次特征<br/>特征差异值<br/>变化幅度 / 变化方向<br/>值域冲突状态<br/>未归属外设观察存在数量"]
    OUT2["输出状态动态<br/>值域内特征变化动态<br/>特征值域冲突动态<br/>首次扫描基准建立动态"]
    OUT3["回流线索特征<br/>未归属 -> 识别<br/>超值域 -> 识别/复验<br/>低质量 -> 补观察"]

    R0["硬边界<br/>扫描不能确认新存在"]
    R1["硬边界<br/>未归属外设观察存在不能生成状态/动态"]
    R2["硬边界<br/>首次扫描不是变化动态"]
    R3["硬边界<br/>无变化不大量写动态"]
    R4["硬边界<br/>扫描结果不反推全场景明确"]

    START --> IN0 --> IN1 --> G0
    G0 -->|否| GAP0 --> OUT0
    G0 -->|是| U0
    U0 -->|是| U1 --> OUT1
    U0 -->|否| A0 --> A1
    A1 -->|是| FIRST --> BASE
    A1 -->|否| CMP --> V0
    V0 -->|否| CONFLICT --> OUT1
    V0 -->|是| C0
    C0 -->|超过阈值| DYN --> BASE
    C0 -->|未超过阈值| STATIC --> BASE
    BASE --> OUT0 --> OUT1 --> OUT2 --> OUT3

    R0 -.约束.-> U1
    R1 -.约束.-> U1
    R2 -.约束.-> FIRST
    R3 -.约束.-> STATIC
    R4 -.约束.-> OUT0

    classDef input fill:#e8f4ff,stroke:#2f6f9f,color:#102030;
    classDef process fill:#eef7ed,stroke:#3f7f4f,color:#102010;
    classDef compare fill:#f1edff,stroke:#6c56a4,color:#1d1730;
    classDef output fill:#eef8f7,stroke:#397b77,color:#102827;
    classDef gate fill:#fff8e8,stroke:#9a6a1a,color:#24160a;
    classDef warn fill:#ffecec,stroke:#b84a4a,color:#401111;

    class START,IN0,IN1 input;
    class A0,FIRST,DYN,STATIC,BASE process;
    class CMP compare;
    class G0,U0,A1,V0,C0 gate;
    class OUT0,OUT1,OUT2,OUT3 output;
    class GAP0,U1,CONFLICT,R0,R1,R2,R3,R4 warn;
```

## 条件与结果

| 类别 | 宿主 | 特征类型 | 目标/结果特征值 |
| --- | --- | --- | --- |
| 条件 | 当前扫描范围 | 扫描范围明确状态 | `1` |
| 条件 | 当前扫描范围 | 已归属可扫描存在数量 | `>0` |
| 条件 | 当前扫描范围 | 未归属外设观察存在数量 | `0` |
| 条件 | 已归属存在 | 上一轮扫描基准特征可读状态 | `1`，首次扫描除外 |
| 条件 | 已归属存在 | 存在特征值域可读状态 | `1` |
| 结果特征 | 已归属存在 | 当前特征值 | 当前观测值 |
| 结果特征 | 已归属存在 | 下一轮扫描基准特征状态 | `已更新` |
| 二次特征 | 已归属存在.当前特征_基准特征 | 特征差异值 / 变化幅度 / 变化方向 | 值域内可入账 |
| 二次特征 | 已归属存在 | 特征值域冲突状态 | `存在 / 不存在` |
| 状态动态 | 已归属存在 | 特征变化动态 | 值域内变化超过阈值时生成 |
| 状态动态 | 已归属存在 | 特征值域冲突动态 | 超值域时生成 |

## 扫描完成公式

```text
扫描动态可取得 =
    扫描范围明确状态 == 1
    AND 已归属可扫描存在数量 > 0
    AND 未归属外设观察存在数量 == 0
    AND 当前特征值与目标宿主绑定
    AND 当前特征值与基准/上一轮值可比较
    AND 当前特征值落入该存在特征值域
```

```text
值域内变化 =
    当前特征值落入该存在特征值域
    AND 当前特征_基准特征.差异值 > 最小记录阈值
        -> 已归属存在.特征变化动态 == 已生成
```

```text
超值域 =
    当前特征值不在该存在特征值域内
        -> 已归属存在.特征值域冲突状态 == 存在
        -> 回识别 / 复验
```

一句话：

```text
扫描解决“我的已归属存在有没有变”。
```
