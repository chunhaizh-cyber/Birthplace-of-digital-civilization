# 本能方法：观察内部实现逻辑流程图 v0.4

更新时间：2026-06-05

## 定位

```text
观察立存在。

观察生成外设观察存在：
    第一帧建立基准；
    后续多帧比较基准；
    误差落入各自特征值域后，形成可复验外设观察存在。
```

观察的正式输入和输出只能承接：

```text
存在
特征
二次特征
状态动态
```

线程通信层可以有传输容器，但观察方法的目标、结果、因果节点和需求目标不得承接传输容器。

## 流程图

```mermaid
flowchart TD
    START["观察本能方法启动<br/>目标: 生成可复验外设观察存在"]
    IN0["输入特征<br/>外设材料可用状态=1<br/>观察范围明确状态=1<br/>期望特征类型集合可读<br/>稳定值域集合可读<br/>最小观测帧数 / 最小稳定帧数"]
    G0{"观察条件是否具备?"}
    GAP0["输出缺口特征<br/>观察材料可用状态=0<br/>观察范围明确状态=0<br/>或稳定值域不可读"]

    F0["第一帧建立外设观察存在<br/>外设观察存在.基准状态=已建立<br/>外设观察存在.观察状态=待复现"]
    F1["写入基准特征<br/>来源帧<br/>像素集合掩码<br/>彩色轮廓局部图<br/>深度轮廓局部图<br/>颜色 / 深度 / 空间坐标 / AABB / 轮廓 / 掩码特征<br/>基准特征值集合"]
    D0["生成状态动态<br/>外设观察存在生成动态<br/>基准特征写入动态"]

    N0["第二帧及以后<br/>读取当前现实特征值"]
    N1["匹配基准观察存在<br/>当前特征值 -> 同一外设观察存在候选"]
    N2["计算二次特征<br/>当前特征_基准特征.颜色误差<br/>深度误差 / 空间误差 / AABB误差<br/>轮廓偏差 / 掩码重合率<br/>点集或局部三维范围误差"]
    N3["误差二次特征落账<br/>平均误差 / 最大误差 / 方差<br/>连续稳定帧数 / 连续异常帧数<br/>丢失 / 漂移 / 冲突 / 拆分 / 合并计数"]
    C0{"误差是否持续落入<br/>各特征类型稳定值域?"}

    OK["观察完成<br/>外设观察存在.稳定复现状态=稳定<br/>外设观察存在.观察完成状态=已完成<br/>外设观察存在.可复验状态=可复验"]
    PART["部分完成<br/>逐特征稳定状态写入<br/>稳定特征可有限承接<br/>不稳定特征不得支撑对应判断"]
    FAIL["未完成 / 证据不足<br/>外设观察存在.观察完成状态=未完成/证据不足<br/>质量缺口特征写入"]
    U0{"后续帧是否形成<br/>新稳定中心?"}
    U1["基准更新候选<br/>外设观察存在.基准更新候选状态=已生成<br/>待裁决: 更新基准 / 重建 / 拆分 / 合并"]

    OUT0["输出存在<br/>外设观察存在集合"]
    OUT1["输出特征<br/>基准状态 / 观察状态 / 观察完成状态<br/>可复验状态 / 逐特征稳定状态<br/>像素集合掩码状态 / 局部图状态"]
    OUT2["输出二次特征<br/>颜色/深度/空间/轮廓/掩码误差<br/>误差统计 / 稳定帧数 / 异常计数"]
    OUT3["输出状态动态<br/>外设观察存在生成动态<br/>观察存在特征稳定动态<br/>质量缺口状态动态"]
    OUT4["输出线索特征<br/>待识别外设观察存在数量<br/>可扫描已归属存在线索数量<br/>指定目标跟踪线索状态<br/>补观察线索状态"]

    R0["硬边界<br/>第一帧不是观察完成"]
    R1["硬边界<br/>观察完成不是世界存在永久成立"]
    R2["硬边界<br/>观察不生成变化动态"]
    R3["硬边界<br/>观察只输出存在/特征/二次特征/状态动态"]
    R4["硬边界<br/>观察只输出线索特征<br/>不直接生成正式派生需求"]

    START --> IN0 --> G0
    G0 -->|否| GAP0 --> OUT1
    G0 -->|是| F0 --> F1 --> D0 --> N0
    N0 --> N1 --> N2 --> N3 --> C0
    C0 -->|全部稳定| OK --> OUT0
    C0 -->|部分稳定| PART --> OUT0
    C0 -->|不稳定| FAIL --> U0
    U0 -->|是| U1 --> OUT2
    U0 -->|否| OUT2
    OUT0 --> OUT1 --> OUT2 --> OUT3 --> OUT4

    R0 -.约束.-> F0
    R1 -.约束.-> OK
    R2 -.约束.-> OUT3
    R3 -.约束.-> OUT0
    R4 -.约束.-> OUT4

    classDef input fill:#e8f4ff,stroke:#2f6f9f,color:#102030;
    classDef process fill:#eef7ed,stroke:#3f7f4f,color:#102010;
    classDef dyn fill:#f1edff,stroke:#6c56a4,color:#1d1730;
    classDef output fill:#eef8f7,stroke:#397b77,color:#102827;
    classDef gate fill:#fff8e8,stroke:#9a6a1a,color:#24160a;
    classDef warn fill:#ffecec,stroke:#b84a4a,color:#401111;

    class START,IN0 input;
    class F0,F1,N0,N1,N2,N3,OK,PART,FAIL,U1 process;
    class D0,OUT3 dyn;
    class OUT0,OUT1,OUT2,OUT4 output;
    class G0,C0,U0 gate;
    class GAP0,R0,R1,R2,R3,R4 warn;
```

## 条件与结果

| 类别 | 宿主 | 特征类型 | 目标/结果特征值 |
| --- | --- | --- | --- |
| 条件 | 当前外设材料 | 外设材料可用状态 | `1` |
| 条件 | 当前观察范围 | 观察范围明确状态 | `1` |
| 条件 | 观察方法参数 | 期望特征类型集合可读状态 | `1` |
| 条件 | 观察方法参数 | 稳定值域集合可读状态 | `1` |
| 条件 | 当前观察窗口 | 连续观测帧数 | `> 最小观测帧数` |
| 结果存在 | 当前观察窗口 | 外设观察存在集合状态 | `已生成 / 部分生成 / 未生成` |
| 结果特征 | 外设观察存在 | 基准状态 | `已建立` |
| 结果特征 | 外设观察存在 | 观察完成状态 | `已完成 / 部分完成 / 未完成 / 证据不足` |
| 结果特征 | 外设观察存在 | 可复验状态 | `可复验 / 待复验` |
| 结果二次特征 | 外设观察存在.当前特征_基准特征 | 误差统计特征 | 平均误差 / 最大误差 / 方差 / 稳定帧数 |
| 结果动态 | 外设观察存在 | 外设观察存在生成动态 | `已生成 / 未生成` |
| 结果动态 | 外设观察存在 | 特征稳定动态 | `已生成 / 未生成` |

## 去容器化边界

```text
观察正式输出 =
    外设观察存在
    + 外设观察存在的特征
    + 外设观察存在的误差二次特征
    + 外设观察存在生成/稳定/质量缺口状态动态

观察正式输出 != 线程传输容器
观察正式输出 != 世界存在确认
观察正式输出 != 变化动态
观察正式输出 != 正式派生需求
```

一句话：

```text
观察解决“有没有一个可复验的外设观察存在”。
```
