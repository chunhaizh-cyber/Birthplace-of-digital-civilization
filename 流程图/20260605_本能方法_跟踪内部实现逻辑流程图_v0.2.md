# 本能方法：跟踪内部实现逻辑流程图 v0.2

更新时间：2026-06-05

## 定位

```text
跟踪守动态。

跟踪对指定已归属存在持续取值，
判断其动态特征是否连续、合法、稳定。
```

跟踪不是全场扫描。动态目标的关键问题不是“是否变化”，而是：

```text
当前特征值是否落入合法值域？
当前特征值是否落入预测区间？
当前特征值是否发生合法区间迁移？
是否存在非法跳变？
预测误差是否超限？
```

## 流程图

```mermaid
flowchart TD
    START["跟踪本能方法启动<br/>目标: 持续读取指定存在动态特征"]
    IN0["输入条件特征<br/>指定存在归属状态=已归属<br/>跟踪窗口明确状态=1<br/>跟踪基准特征可读状态=1<br/>期望动态特征类型集合可读"]
    IN1["输入存在与特征<br/>指定存在<br/>上一轮/跟踪基准特征<br/>跟踪特征值域区间<br/>预测区间<br/>最大允许预测误差"]
    G0{"跟踪可启动?"}
    GAP0["输出缺口特征<br/>指定存在跟踪不可启动状态=1<br/>回扫描/识别/观察"]

    READ["读取指定存在当前特征值<br/>位置 / 距离 / 姿态 / 速度趋势 / 接近远离<br/>当前观测年龄 / 遮挡状态 / 丢失状态"]
    V0{"目标身份是否匹配<br/>指定存在?"}
    ID_CONFLICT["身份冲突<br/>指定存在.跟踪身份冲突状态=存在<br/>跟踪身份冲突动态=已生成<br/>回识别"]
    Q0{"跟踪质量是否合格?"}
    LOWQ["质量不足<br/>指定存在.跟踪质量状态=质量不足<br/>质量缺口状态动态=已生成<br/>回观察或调整窗口"]
    L0{"目标是否丢失/遮挡?"}
    LOST["丢失/遮挡<br/>指定存在.跟踪状态=丢失/遮挡<br/>跟踪丢失动态=已生成<br/>重捕获候选状态=已生成"]

    CMP["计算动态二次特征<br/>当前值_上一轮值.变化幅度<br/>当前值_预测值.预测误差<br/>当前值域区间ID<br/>区间迁移类型<br/>非法区间跳变次数"]
    V1{"当前特征值是否落入<br/>合法值域区间?"}
    RANGE_BAD["值域非法<br/>指定存在.动态值域冲突状态=存在<br/>动态值域冲突动态=已生成<br/>回识别/复验"]
    P0{"当前特征值是否落入<br/>预测区间?"}
    P_BAD["预测偏差超限<br/>指定存在.预测误差值 > 最大允许预测误差<br/>预测偏差超限动态=已生成"]
    T0{"区间迁移是否合法?"}
    JUMP["非法跳变<br/>非法区间跳变次数+1<br/>非法区间跳变动态=已生成<br/>回识别/复验"]

    DYN["合法连续动态<br/>指定存在.当前特征值=当前值<br/>指定存在.跟踪状态=稳定<br/>动态区间迁移动态=已生成<br/>指定存在动态信息取得状态=1"]
    STABLE["稳定或低速<br/>当前特征误差 < 稳定阈值<br/>不生成完整变化动态<br/>更新最近观测时间 / 稳定计数"]
    BASE["更新跟踪基准特征<br/>下一轮跟踪基准特征状态=已更新<br/>预测区间状态=已更新"]
    CONT{"持续条件仍满足?"}
    LOOP["继续下一轮跟踪"]
    END["结束跟踪<br/>跟踪完成状态=已完成/已中止"]

    OUT0["输出特征<br/>指定存在当前特征值<br/>跟踪状态 / 丢失状态 / 遮挡状态<br/>当前值域区间ID<br/>最近观测年龄"]
    OUT1["输出二次特征<br/>预测误差值<br/>区间迁移类型<br/>非法区间跳变次数<br/>动态连续性状态"]
    OUT2["输出状态动态<br/>动态区间迁移动态<br/>跟踪丢失动态<br/>重捕获动态<br/>预测偏差超限动态<br/>身份冲突动态"]
    OUT3["回流线索特征<br/>身份冲突/超值域 -> 识别<br/>丢失/重捕获 -> 扫描/识别<br/>质量不足 -> 观察"]

    R0["硬边界<br/>跟踪只盯一个指定存在"]
    R1["硬边界<br/>跟踪不能证明全场扫描完成"]
    R2["硬边界<br/>跟踪丢失不能直接确认新存在"]
    R3["硬边界<br/>无变化不大量写动态"]
    R4["硬边界<br/>跟踪结果不能承接线程传输容器"]

    START --> IN0 --> IN1 --> G0
    G0 -->|否| GAP0 --> OUT0
    G0 -->|是| READ --> V0
    V0 -->|否| ID_CONFLICT --> OUT3
    V0 -->|是| Q0
    Q0 -->|否| LOWQ --> OUT3
    Q0 -->|是| L0
    L0 -->|丢失/遮挡| LOST --> OUT2
    L0 -->|稳定可见| CMP --> V1
    V1 -->|否| RANGE_BAD --> OUT3
    V1 -->|是| P0
    P0 -->|否| P_BAD --> OUT2
    P0 -->|是| T0
    T0 -->|否| JUMP --> OUT3
    T0 -->|是| DYN --> BASE
    CMP -->|低速稳定| STABLE --> BASE
    BASE --> OUT0 --> OUT1 --> OUT2 --> CONT
    CONT -->|是| LOOP --> READ
    CONT -->|否| END

    R0 -.约束.-> IN0
    R1 -.约束.-> END
    R2 -.约束.-> LOST
    R3 -.约束.-> STABLE
    R4 -.约束.-> OUT0

    classDef input fill:#e8f4ff,stroke:#2f6f9f,color:#102030;
    classDef process fill:#eef7ed,stroke:#3f7f4f,color:#102010;
    classDef compare fill:#f1edff,stroke:#6c56a4,color:#1d1730;
    classDef output fill:#eef8f7,stroke:#397b77,color:#102827;
    classDef gate fill:#fff8e8,stroke:#9a6a1a,color:#24160a;
    classDef warn fill:#ffecec,stroke:#b84a4a,color:#401111;

    class START,IN0,IN1 input;
    class READ,DYN,STABLE,BASE,LOOP process;
    class CMP compare;
    class G0,V0,Q0,L0,V1,P0,T0,CONT gate;
    class OUT0,OUT1,OUT2,OUT3,END output;
    class GAP0,ID_CONFLICT,LOWQ,LOST,RANGE_BAD,P_BAD,JUMP,R0,R1,R2,R3,R4 warn;
```

## 条件与结果

| 类别 | 宿主 | 特征类型 | 目标/结果特征值 |
| --- | --- | --- | --- |
| 条件 | 指定存在 | 归属状态 | `已归属` |
| 条件 | 指定存在 | 跟踪窗口明确状态 | `1` |
| 条件 | 指定存在 | 跟踪基准特征可读状态 | `1` |
| 条件 | 指定存在 | 跟踪特征值域区间状态 | `已建立` |
| 条件 | 指定存在 | 预测区间状态 | `已生成` |
| 结果特征 | 指定存在 | 跟踪状态 | `稳定 / 遮挡 / 丢失 / 冲突 / 质量不足` |
| 结果特征 | 指定存在 | 当前特征值 | 当前观测值 |
| 结果特征 | 指定存在 | 当前值域区间ID | 区间引用 |
| 二次特征 | 指定存在.当前值_预测值 | 预测误差值 | `< 最大允许预测误差` 才正常 |
| 二次特征 | 指定存在 | 动态连续性状态 | `连续 / 不连续 / 证据不足` |
| 二次特征 | 指定存在 | 非法区间跳变次数 | `>=0` |
| 状态动态 | 指定存在 | 动态区间迁移动态 | 合法迁移时生成 |
| 状态动态 | 指定存在 | 跟踪丢失动态 | 丢失时生成 |
| 状态动态 | 指定存在 | 预测偏差超限动态 | 预测误差超限时生成 |

## 完成公式

```text
跟踪可启动 =
    指定存在.归属状态 == 已归属
    AND 跟踪窗口明确状态 == 1
    AND 跟踪基准特征可读状态 == 1
```

```text
持续获得动态 =
    目标身份匹配指定存在
    AND 跟踪质量合格
    AND 指定存在.跟踪状态 != 丢失
    AND 当前特征值与上一轮/跟踪基准可比较
    AND 当前特征值落入合法值域区间
    AND 当前特征值落入预测区间
    AND 区间迁移合法
    AND 预测误差值 < 最大允许预测误差
```

一句话：

```text
跟踪解决“这个指定存在正在怎样连续变化”。
```
