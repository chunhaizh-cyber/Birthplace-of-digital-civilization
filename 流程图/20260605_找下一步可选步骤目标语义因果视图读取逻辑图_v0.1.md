# 找下一步可选步骤目标语义因果视图读取逻辑图 v0.1

更新时间：2026-06-05

## 用途

本图是 `任务筹办普通函数状态机流程图 v0.2` 中：

```text
找下一步可选步骤
读取目标语义和因果视图
```

的细分判断图。

本图只负责读取来源需求目标语义、构造目标比较、读取世界树因果信息并生成本轮临时因果视图。它不直接创建需求、不直接写任务状态、不选择最终路径、不调用方法执行。

## 判断原则

```text
1. 先读目标语义，再读因果视图；不得用叶子 / 非叶子、控制面板摘要或临时推断代替目标语义。
2. 缺目标宿主、目标特征、目标比较关系或目标值时，不得生成临时因果视图。
3. 临时因果视图不是需求树、因果树、安全树、服务树，也不是方法候选集合的替代物。
4. 因果来源只能来自世界树因果主信息、遗传因果、需求侧条件、方法条件、动作动态证据链和已入树子需求。
5. 找到促成 / 避免 / 条件补齐 / 方法就绪 / 证据补齐 / 副作用约束 / 学习缺口后，只形成候选步骤视图。
6. 候选步骤视图必须带来源因果、目标比较、证据引用、生成类型、获取途径建议和幂等键，后续再交给归类、过滤、目标化和提交裁决。
7. 没有因果路径不是执行失败，先形成学习缺口候选；若因果数据结构损坏，才进入逻辑错误。
```

## 详细逻辑图

```mermaid
flowchart TD
    INPUT["输入: 找下一步可选步骤请求<br/>当前任务 / 来源需求 / 父需求 / 上下文快照"]
    SNAPSHOT["固定本轮快照<br/>任务版本 / 来源需求版本 / 世界树版本 / 动作动态证据版本"]

    TARGET_READ["读取来源需求目标语义视图<br/>目标宿主 / 目标特征 / 当前状态 / 目标状态 / 满足关系 / 根分区"]
    TARGET_GATE{"目标语义是否完整可解析?"}
    TARGET_CLASSIFY["外部细分图: 不可解析来源分类<br/>逻辑错误 / 目标补齐 / 证据不足 / 版本冲突"]

    SEMANTIC_KIND{"目标语义类型?"}
    ORDINARY_TARGET["普通业务目标<br/>可构造目标比较"]
    ORG_TARGET["逻辑组织目标<br/>AND组 / OR组 / 方法路径组 / 因果子链"]
    STRUCT_GAP_TARGET["结构性缺口目标<br/>治理缺口 / 规范缺口 / 初始化补丁"]
    TOKEN_TARGET["聚合或结算令牌<br/>等待组织节点聚合，不生成普通因果视图"]

    ORG_MODE["组织节点展开模式<br/>按获取途径读取组织规则和成员缺口"]
    ORG_SEED_GATE{"是否能形成组织展开种子?"}
    ORG_STEP_VIEW["组织展开候选步骤视图<br/>生成子路径草案材料 / 等待成员补齐"]
    ORG_GAP["筹办回执: 组织展开材料不足<br/>等待提交或诊断"]

    TARGET_COMPARE["构造标准目标比较<br/>目标宿主.目标特征 比较符 目标值<br/>记录当前值和方向"]
    COMPARE_GATE{"目标比较是否合法?"}
    COMPARE_ERROR["筹办回执: 目标比较非法<br/>比较关系非法 / I64_MAX / 值域冲突"]

    DEDUPE_CONTEXT["读取展开上下文<br/>已入树子需求 / 已有路径组 / 最近草案 / 祖先链"]
    LOOP_GATE{"是否命中已展开路径<br/>或祖先链环路?"}
    LOOP_RECEIPT["筹办回执: 环路或重复展开<br/>复用已有路径 / 等待已有子路径"]

    CAUSAL_SOURCE["读取因果来源集合<br/>世界树因果主信息 / 遗传因果 / 需求侧条件 / 方法条件 / 动作动态证据"]
    SOURCE_GATE{"因果来源集合是否可读取?"}
    SOURCE_ERROR["筹办回执: 因果来源读取异常<br/>结构损坏 / 引用悬空 / 权威来源冲突"]
    EMPTY_SOURCE["无可命中因果来源<br/>允许生成学习缺口候选"]

    CAUSAL_SCAN["遍历因果主信息<br/>读取主果比较模板 / 因模板 / 条件模板 / 方法模板 / 证据统计 / 副作用"]
    EFFECT_GATE{"主果与目标比较关系?"}
    PROMOTE_PATH["加入促成路径候选<br/>因果主果命中当前目标"]
    AVOID_PATH["加入避免路径候选<br/>因果主果反向破坏当前目标"]
    NO_EFFECT["本条因果不相关<br/>跳过"]

    CONDITION_GATE{"命中因果的条件模板<br/>是否满足或已知?"}
    CONDITION_GAP["加入条件补齐候选<br/>条件状态未知或未满足"]

    METHOD_TEMPLATE_GATE{"因果含方法模板?"}
    METHOD_CONTRACT_GATE{"方法最小契约是否完整?<br/>输入 / 前置 / 动作入口 / 输出模式 / 动作动态落账"}
    METHOD_HINT["加入方法候选依据<br/>只作为后续方法筹办材料"]
    METHOD_GAP["加入方法就绪缺口候选<br/>动作入口 / 参数模式 / 输出模式 / 条件结果对"]

    EVIDENCE_GATE{"因果证据是否足够?"}
    EVIDENCE_GAP["加入证据补齐候选<br/>样本不足 / 最近失败 / 未验证"]

    SIDE_EFFECT_GATE{"路径是否存在副作用<br/>会破坏其他根分区目标?"}
    SIDE_EFFECT_GAP["加入副作用约束候选<br/>副作用可接受状态需补齐"]

    VIEW_EMPTY_GATE{"临时视图是否为空?"}
    LEARNING_GAP["加入学习缺口候选<br/>目标特征促成因果待补齐 / 可执行方法待建立"]

    VIEW_NORMALIZE["整理临时因果视图<br/>促成 / 避免 / 条件补齐 / 方法就绪 / 证据补齐 / 副作用约束 / 学习缺口"]
    TARGETIZE_GATE{"候选步骤是否都能<br/>收束成明确目标比较?"}
    PATH_SOURCE_GATE["外部细分图: 缺口草案来源分类<br/>逻辑错误 / 真实缺口 / 证据不足 / 诊断等待"]

    VIEW_FILTER["视图内轻量清理<br/>去重 / 环路标记 / 证据阈值 / 预算标记 / 候选排序提示"]
    STEP_VIEW_READY["输出: 下一步候选步骤视图已生成<br/>供后续归类 / 过滤 / 目标化 / 提交裁决使用"]

    RECEIPT_OUT["输出统一筹办回执<br/>分类 / 原因 / 候选视图 / 唤醒条件 / 压力材料"]
    NO_WRITE_NOTE["边界: 本图不直接写需求树<br/>不生成正式子需求 / 不写任务状态 / 不执行方法"]
    PRESSURE["压力材料<br/>目标长期不可解析 / 因果来源损坏 / 学习缺口长期无结果 / 候选视图反复为空"]

    INPUT --> SNAPSHOT --> TARGET_READ --> TARGET_GATE
    TARGET_GATE -->|否| TARGET_CLASSIFY --> RECEIPT_OUT
    TARGET_GATE -->|是| SEMANTIC_KIND

    SEMANTIC_KIND -->|普通业务目标| ORDINARY_TARGET --> TARGET_COMPARE
    SEMANTIC_KIND -->|逻辑组织目标| ORG_TARGET --> ORG_MODE --> ORG_SEED_GATE
    SEMANTIC_KIND -->|结构性缺口| STRUCT_GAP_TARGET --> RECEIPT_OUT
    SEMANTIC_KIND -->|聚合 / 结算令牌| TOKEN_TARGET --> RECEIPT_OUT

    ORG_SEED_GATE -->|能| ORG_STEP_VIEW --> STEP_VIEW_READY
    ORG_SEED_GATE -->|不能| ORG_GAP --> RECEIPT_OUT

    TARGET_COMPARE --> COMPARE_GATE
    COMPARE_GATE -->|非法| COMPARE_ERROR --> RECEIPT_OUT
    COMPARE_GATE -->|合法| DEDUPE_CONTEXT --> LOOP_GATE

    LOOP_GATE -->|命中重复或环路| LOOP_RECEIPT --> RECEIPT_OUT
    LOOP_GATE -->|未命中| CAUSAL_SOURCE --> SOURCE_GATE

    SOURCE_GATE -->|读取异常| SOURCE_ERROR --> RECEIPT_OUT
    SOURCE_GATE -->|可读取但无来源| EMPTY_SOURCE --> LEARNING_GAP
    SOURCE_GATE -->|可读取且有来源| CAUSAL_SCAN --> EFFECT_GATE

    EFFECT_GATE -->|主果命中| PROMOTE_PATH
    EFFECT_GATE -->|主果反向破坏| AVOID_PATH
    EFFECT_GATE -->|不相关| NO_EFFECT --> VIEW_EMPTY_GATE

    PROMOTE_PATH --> CONDITION_GATE
    AVOID_PATH --> SIDE_EFFECT_GATE

    CONDITION_GATE -->|未满足或未知| CONDITION_GAP --> METHOD_TEMPLATE_GATE
    CONDITION_GATE -->|满足| METHOD_TEMPLATE_GATE

    METHOD_TEMPLATE_GATE -->|有| METHOD_CONTRACT_GATE
    METHOD_TEMPLATE_GATE -->|无| EVIDENCE_GATE
    METHOD_CONTRACT_GATE -->|完整| METHOD_HINT --> EVIDENCE_GATE
    METHOD_CONTRACT_GATE -->|不完整| METHOD_GAP --> EVIDENCE_GATE

    EVIDENCE_GATE -->|不足| EVIDENCE_GAP --> SIDE_EFFECT_GATE
    EVIDENCE_GATE -->|足够| SIDE_EFFECT_GATE

    SIDE_EFFECT_GATE -->|有| SIDE_EFFECT_GAP --> VIEW_EMPTY_GATE
    SIDE_EFFECT_GATE -->|无| VIEW_EMPTY_GATE

    VIEW_EMPTY_GATE -->|为空| LEARNING_GAP --> VIEW_NORMALIZE
    VIEW_EMPTY_GATE -->|非空| VIEW_NORMALIZE

    VIEW_NORMALIZE --> TARGETIZE_GATE
    TARGETIZE_GATE -->|存在不可目标化候选| PATH_SOURCE_GATE --> RECEIPT_OUT
    TARGETIZE_GATE -->|全部可目标化| VIEW_FILTER --> STEP_VIEW_READY --> RECEIPT_OUT

    NO_WRITE_NOTE -.约束.-> STEP_VIEW_READY
    NO_WRITE_NOTE -.约束.-> ORG_STEP_VIEW
    NO_WRITE_NOTE -.约束.-> METHOD_HINT

    TARGET_CLASSIFY -.长期目标不完整.-> PRESSURE
    SOURCE_ERROR -.因果来源异常.-> PRESSURE
    LEARNING_GAP -.长期无因果路径.-> PRESSURE
    VIEW_EMPTY_GATE -.反复空视图.-> PRESSURE
    PATH_SOURCE_GATE -.候选反复不可目标化.-> PRESSURE

    classDef gate fill:#fff8e8,stroke:#9a6a1a,color:#24160a;
    classDef prep fill:#e8f4ff,stroke:#2f6f9f,color:#102030;
    classDef receipt fill:#f1edff,stroke:#6c56a4,color:#1d1730;
    classDef warn fill:#ffecec,stroke:#b84a4a,color:#401111;
    classDef pressure fill:#fff0f3,stroke:#b4234a,color:#3a0d18;

    class TARGET_GATE,SEMANTIC_KIND,ORG_SEED_GATE,COMPARE_GATE,LOOP_GATE,SOURCE_GATE,EFFECT_GATE,CONDITION_GATE,METHOD_TEMPLATE_GATE,METHOD_CONTRACT_GATE,EVIDENCE_GATE,SIDE_EFFECT_GATE,VIEW_EMPTY_GATE,TARGETIZE_GATE gate;
    class INPUT,SNAPSHOT,TARGET_READ,ORDINARY_TARGET,ORG_TARGET,ORG_MODE,ORG_STEP_VIEW,TARGET_COMPARE,DEDUPE_CONTEXT,CAUSAL_SOURCE,EMPTY_SOURCE,CAUSAL_SCAN,PROMOTE_PATH,AVOID_PATH,NO_EFFECT,CONDITION_GAP,METHOD_HINT,METHOD_GAP,EVIDENCE_GAP,SIDE_EFFECT_GAP,LEARNING_GAP,VIEW_NORMALIZE,VIEW_FILTER,STEP_VIEW_READY,RECEIPT_OUT prep;
    class TARGET_CLASSIFY,STRUCT_GAP_TARGET,TOKEN_TARGET,ORG_GAP,COMPARE_ERROR,LOOP_RECEIPT,SOURCE_ERROR,PATH_SOURCE_GATE receipt;
    class NO_WRITE_NOTE warn;
    class PRESSURE pressure;
```

## 输出分类

| 分类 | 判定条件 | 下一步 |
| --- | --- | --- |
| 下一步候选步骤视图已生成 | 目标比较合法，因果视图已生成，候选项可目标化 | 进入候选归类、过滤、目标化和提交裁决 |
| 组织展开候选步骤视图 | 目标是 AND / OR / 方法路径组 / 因果子链，且能形成组织展开种子 | 生成组织子路径草案材料，后续提交裁决 |
| 学习缺口候选 | 无可命中因果路径，或路径来源不足但不是结构错误 | 进入学习缺口候选，后续目标化和提交裁决 |
| 目标不可解析 | 缺目标宿主、目标特征、比较关系或目标值 | 进入不可解析来源分类 |
| 目标比较非法 | 比较关系非法、值域冲突、I64_MAX 进入真实目标 | 逻辑错误或维护回执 |
| 重复或环路 | 祖先链或已有路径已覆盖本轮目标 | 复用已有路径或等待已有子路径 |
| 因果来源异常 | 世界树因果、遗传因果或证据链结构损坏 | 逻辑错误中止或诊断等待 |
| 候选不可目标化 | 候选仍是抽象口号，如只写“避免”而无明确特征比较 | 进入缺口草案来源分类 |

## 接入建议

```text
1. 在 v0.2 的 `PATH_START` 位置调用本判断。
2. `STEP_VIEW_READY` 后再进入 `PATH_VIEW / PATH_CLASSIFY / PATH_FILTER / PATH_TRANSLATE`。
3. 本图生成的是临时因果视图和候选步骤视图，不是正式需求节点。
4. 方法模板完整时只生成方法候选依据，不直接指定唯一执行方法。
5. 方法模板不完整时生成方法就绪缺口候选，后续仍需提交入口裁决。
6. 避免类和副作用类候选必须收束成明确目标特征比较，不能只保留抽象词。
7. 空视图默认转为学习缺口候选；只有因果来源结构损坏才走逻辑错误。
```
