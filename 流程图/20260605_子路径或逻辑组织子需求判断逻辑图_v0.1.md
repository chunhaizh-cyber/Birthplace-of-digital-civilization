# 子路径或逻辑组织子需求判断逻辑图 v0.1

更新时间：2026-06-05

## 用途

本图是 `任务筹办普通函数状态机流程图 v0.2` 中：

```text
是否已有子路径
或逻辑组织子需求?
```

的细分判断图。

本图只判断当前任务目标下是否已有可被筹办继续读取的子路径结构，不判断子路径是否已经闭合，也不选择哪条路径好用。

## 判断原则

```text
1. 待提交草案不算已有子路径，只有需求提交入口回执为已入树或已复用后，才算正式子路径。
2. 子路径必须有合法父子归属、来源需求引用、目标语义视图和关系类型。
3. 逻辑组织子需求必须能识别为 AND组、OR组、方法路径组、因果子链、证据补齐、学习支撑或副作用约束等组织语义。
4. 普通业务子需求只有携带明确关系类型或可由获取途径稳定确定关系时，才算当前父目标的子路径。
5. 历史材料、重复草案、被拒绝草案、已失效候选、纯日志、非阻塞证据材料，不算已有阻塞子路径。
6. 缺父子关系、引用悬空、目标语义非法、逻辑组织获取途径缺失，优先进入诊断或逻辑错误，不直接生成业务分支。
7. 输出为“有子路径”后，下一步仍需进入子路径关系类型判断，再看 AND / OR / 顺序链 / 支撑类路径如何闭合。
```

## 判断逻辑图

```mermaid
flowchart TD
    INPUT["输入: 当前任务与来源需求<br/>父需求 / 目标语义 / 子需求索引 / 子任务索引 / 派生提交回执"]
    SNAPSHOT["固定本轮快照<br/>父需求版本 / 子需求集合版本 / 任务集合版本 / 提交回执版本"]

    PENDING_GATE{"是否存在待裁决草案?<br/>派生需求 / 更新指令 / 学习草案"}
    SUBMIT_WAIT["筹办回执: 等待提交结果<br/>待已入树 / 已复用 / 被拒绝 / 证据不足"]

    COLLECT["汇总候选子项<br/>直接子需求 / 逻辑组织子需求 / 路径组 / 子任务绑定 / 提交回执"]
    VERSION_GATE{"候选集合版本<br/>是否仍一致?"}
    VERSION_CONFLICT["筹办回执: 版本冲突<br/>重读子路径集合 / 待重筹办"]

    RAW_GATE{"是否发现任何候选子项?"}
    NO_RAW_CHILD["无候选子项<br/>当前父目标下没有可读取子结构"]

    EXCLUDE["排除非路径材料<br/>历史日志 / 已拒绝草案 / 重复草案 / 已失效候选 / 非阻塞证据"]
    REMAIN_GATE{"排除后是否仍有候选?"}
    NONBLOCKING_ONLY["仅剩非阻塞材料<br/>不算已有阻塞子路径"]

    PARENT_GATE{"父子归属是否合法?<br/>parent_demand_id / source_task_id / stable_key"}
    ORPHAN_ERROR["结构异常: 子项归属不合法<br/>孤儿子需求 / 父需求不匹配 / 稳定键冲突"]

    TARGET_GATE{"子项目标是否完整可解析?<br/>目标宿主 / 目标特征 / 当前状态 / 目标状态"}
    TARGET_CLASSIFY["外部细分图: 不可解析来源分类<br/>逻辑错误 / 真实目标补齐 / 证据不足 / 版本冲突"]

    SEMANTIC_GATE{"子项语义类型?"}
    LOGIC_ORG["逻辑组织子需求<br/>AND组 / OR组 / 方法路径组 / 因果子链 / 结算令牌"]
    ORDINARY_CHILD["普通业务子需求<br/>可作为路径步骤或叶子目标"]
    SUPPORT_CHILD["支撑类子需求<br/>证据补齐 / 学习支撑 / 副作用约束 / 条件补齐"]
    STRUCT_GAP_CHILD["结构性缺口或治理节点<br/>不作为业务子路径"]

    ACQUIRE_GATE{"逻辑组织获取途径<br/>是否明确且合法?"}
    LOGIC_ORG_VALID["有效逻辑组织子需求<br/>可进入组织节点处理"]
    LOGIC_ORG_ERROR["结构异常: 逻辑组织缺获取途径<br/>或获取途径与语义不一致"]

    RELATION_GATE{"是否有明确关系类型?<br/>AND / OR / 顺序链 / 证据 / 学习 / 副作用"}
    RELATION_VALID["有效子路径<br/>关系类型可进入下一闸门"]
    RELATION_MISSING_KIND{"关系缺失来源?"}
    LEGACY_RELATION_WAIT["筹办回执: 待关系规范化<br/>旧数据 / 兼容迁移 / 需重读归属"]
    RELATION_LOGIC_ERROR["逻辑错误: 草案入树缺关系<br/>记录日志 / 不生成业务分支"]

    SUPPORT_BLOCK_GATE{"支撑类子需求<br/>是否阻塞当前父目标判断?"}
    SUPPORT_PATH["有效支撑子路径<br/>进入支撑关系闭合判断"]
    SUPPORT_NONBLOCK["非阻塞支撑材料<br/>只作为证据材料读取"]

    GROUP_CHILD_GATE{"逻辑组织节点内部<br/>是否已有合法成员?"}
    GROUP_HAS_CHILD["有逻辑组织子需求及成员<br/>进入关系类型判断"]
    GROUP_EMPTY_KIND{"组织节点为空的原因?"}
    GROUP_EXPAND["筹办回执: 组织节点需展开<br/>生成或等待子路径草案"]
    GROUP_WAIT["筹办回执: 等待组织节点提交或聚合材料"]
    GROUP_AGGREGATE["筹办回执: 组织节点可聚合/结算<br/>不进入普通方法候选"]

    HAS_CHILD_PATH["输出: 已有合法子路径<br/>进入子路径关系类型判断"]
    NO_CHILD_PATH["输出: 无合法子路径<br/>进入找下一步可选步骤"]
    DIAGNOSTIC_RECEIPT["筹办回执: 结构诊断等待<br/>不静默兜底 / 不派生业务分支"]
    ERROR_RECEIPT["筹办回执: 逻辑错误中止<br/>弹窗 + 错误日志"]
    RECEIPT_OUT["输出统一筹办回执<br/>分类 / 原因 / 下一步 / 唤醒条件 / 压力材料"]

    PRESSURE["压力材料<br/>子路径长期为空 / 关系长期缺失 / 提交长期无结果 / 组织节点展开失败"]
    NOTE["边界: 已有子路径 ≠ 路径已闭合<br/>闭合判断交给关系类型与路径评价模块"]

    INPUT --> SNAPSHOT --> PENDING_GATE
    PENDING_GATE -->|有待裁决| SUBMIT_WAIT --> RECEIPT_OUT
    PENDING_GATE -->|无| COLLECT --> VERSION_GATE

    VERSION_GATE -->|否| VERSION_CONFLICT --> RECEIPT_OUT
    VERSION_GATE -->|是| RAW_GATE
    RAW_GATE -->|没有| NO_RAW_CHILD --> NO_CHILD_PATH
    RAW_GATE -->|有| EXCLUDE --> REMAIN_GATE

    REMAIN_GATE -->|没有| NONBLOCKING_ONLY --> NO_CHILD_PATH
    REMAIN_GATE -->|有| PARENT_GATE

    PARENT_GATE -->|不合法| ORPHAN_ERROR --> ERROR_RECEIPT
    PARENT_GATE -->|合法| TARGET_GATE

    TARGET_GATE -->|不可解析| TARGET_CLASSIFY --> RECEIPT_OUT
    TARGET_GATE -->|可解析| SEMANTIC_GATE

    SEMANTIC_GATE -->|逻辑组织| LOGIC_ORG --> ACQUIRE_GATE
    SEMANTIC_GATE -->|普通业务子需求| ORDINARY_CHILD --> RELATION_GATE
    SEMANTIC_GATE -->|支撑类| SUPPORT_CHILD --> SUPPORT_BLOCK_GATE
    SEMANTIC_GATE -->|结构性缺口 / 治理节点| STRUCT_GAP_CHILD --> DIAGNOSTIC_RECEIPT

    ACQUIRE_GATE -->|明确合法| LOGIC_ORG_VALID --> GROUP_CHILD_GATE
    ACQUIRE_GATE -->|缺失或冲突| LOGIC_ORG_ERROR --> ERROR_RECEIPT

    RELATION_GATE -->|有| RELATION_VALID --> HAS_CHILD_PATH
    RELATION_GATE -->|没有| RELATION_MISSING_KIND
    RELATION_MISSING_KIND -->|旧数据可兼容| LEGACY_RELATION_WAIT --> RECEIPT_OUT
    RELATION_MISSING_KIND -->|本轮草案漏填| RELATION_LOGIC_ERROR --> ERROR_RECEIPT
    RELATION_MISSING_KIND -->|来源未判明| DIAGNOSTIC_RECEIPT

    SUPPORT_BLOCK_GATE -->|阻塞| SUPPORT_PATH --> HAS_CHILD_PATH
    SUPPORT_BLOCK_GATE -->|不阻塞| SUPPORT_NONBLOCK --> NO_CHILD_PATH

    GROUP_CHILD_GATE -->|有| GROUP_HAS_CHILD --> HAS_CHILD_PATH
    GROUP_CHILD_GATE -->|没有| GROUP_EMPTY_KIND
    GROUP_EMPTY_KIND -->|应继续展开| GROUP_EXPAND --> RECEIPT_OUT
    GROUP_EMPTY_KIND -->|等待提交或材料| GROUP_WAIT --> RECEIPT_OUT
    GROUP_EMPTY_KIND -->|已满足聚合条件| GROUP_AGGREGATE --> RECEIPT_OUT

    HAS_CHILD_PATH --> RECEIPT_OUT
    NO_CHILD_PATH --> RECEIPT_OUT
    DIAGNOSTIC_RECEIPT --> RECEIPT_OUT
    ERROR_RECEIPT --> RECEIPT_OUT

    NOTE -.约束.-> HAS_CHILD_PATH
    NOTE -.约束.-> GROUP_HAS_CHILD

    SUBMIT_WAIT -.长期无结果.-> PRESSURE
    NO_CHILD_PATH -.长期无子路径.-> PRESSURE
    LEGACY_RELATION_WAIT -.长期未规范化.-> PRESSURE
    GROUP_EXPAND -.展开失败或分支爆炸.-> PRESSURE
    DIAGNOSTIC_RECEIPT -.长期未判明.-> PRESSURE

    classDef gate fill:#fff8e8,stroke:#9a6a1a,color:#24160a;
    classDef prep fill:#e8f4ff,stroke:#2f6f9f,color:#102030;
    classDef receipt fill:#f1edff,stroke:#6c56a4,color:#1d1730;
    classDef warn fill:#ffecec,stroke:#b84a4a,color:#401111;
    classDef pressure fill:#fff0f3,stroke:#b4234a,color:#3a0d18;

    class PENDING_GATE,VERSION_GATE,RAW_GATE,REMAIN_GATE,PARENT_GATE,TARGET_GATE,SEMANTIC_GATE,ACQUIRE_GATE,RELATION_GATE,RELATION_MISSING_KIND,SUPPORT_BLOCK_GATE,GROUP_CHILD_GATE,GROUP_EMPTY_KIND gate;
    class INPUT,SNAPSHOT,COLLECT,NO_RAW_CHILD,EXCLUDE,NONBLOCKING_ONLY,LOGIC_ORG,ORDINARY_CHILD,SUPPORT_CHILD,STRUCT_GAP_CHILD,LOGIC_ORG_VALID,RELATION_VALID,SUPPORT_PATH,SUPPORT_NONBLOCK,GROUP_HAS_CHILD,HAS_CHILD_PATH,NO_CHILD_PATH,RECEIPT_OUT prep;
    class SUBMIT_WAIT,VERSION_CONFLICT,TARGET_CLASSIFY,LEGACY_RELATION_WAIT,GROUP_EXPAND,GROUP_WAIT,GROUP_AGGREGATE,DIAGNOSTIC_RECEIPT,ERROR_RECEIPT receipt;
    class ORPHAN_ERROR,LOGIC_ORG_ERROR,RELATION_LOGIC_ERROR,NOTE warn;
    class PRESSURE pressure;
```

## 输出分类

| 分类 | 判定条件 | 下一步 |
| --- | --- | --- |
| 已有合法子路径 | 子项归属合法、目标可解析、关系类型明确，或逻辑组织节点有合法成员 | 进入子路径关系类型判断 |
| 等待提交结果 | 只有待裁决草案，尚未收到已入树或已复用回执 | 等待提交入口回执 |
| 无合法子路径 | 没有候选子项，或仅剩非阻塞材料、历史材料、失效候选 | 进入找下一步可选步骤 |
| 组织节点需展开 | 已有逻辑组织子需求，但内部还没有合法成员且本轮应继续展开 | 生成或等待子路径草案 |
| 组织节点可聚合 | 逻辑组织节点已有满足聚合或结算条件的材料 | 进入聚合 / 结算回执 |
| 待关系规范化 | 旧数据缺关系类型，但不是本轮草案错误 | 诊断等待或兼容迁移，不生成业务分支 |
| 逻辑错误中止 | 子项归属不合法、引用悬空、草案入树缺关系、逻辑组织获取途径缺失或冲突 | 弹窗 + 错误日志 + 结构化回执 |

## 接入建议

```text
1. 在 v0.2 的 `CHILD_GATE` 位置调用本判断。
2. 本判断只确认“是否有可继续读取的子路径结构”，不判断闭合状态。
3. `HAS_CHILD_PATH` 后必须继续进入 `REL_GATE`，再判断 AND / OR / 顺序链 / 支撑类路径。
4. 待提交草案必须等提交结果，不能提前当作已有子路径。
5. 逻辑错误只弹窗、记日志、返回结构化回执，不派生业务需求。
6. 长期没有子路径、关系缺失或组织节点展开失败，都应进入压力材料。
```
