# 自我线程总流程图 v0.1

更新时间：2026-06-05

## 用途

本图用于单独梳理自我线程在鱼巢内部循环中的职责边界。重点区分：

- 自我线程负责需求治理、方向计算、任务发起意图、执行前批准、回执裁决、需求回写和价值结算。
- 任务管理线程负责任务创建、任务状态推进、任务筹办、批准后任务执行和任务回执同步。
- 自检、外设、外部事件都是旁路材料源，只有入账 / 准入后才影响自我线程主链。

依据：

- `规范/自我内部循环实现规范20260512.md`
- `规范/自我线程与任务管理线程权责边界规范20260512.md`
- `规范/特征值三态比较底层逻辑规范20260518.md`
- `规范/需求主信息类规范20260507.md`
- `计划/计划索引.md`

## 自我线程总图

```mermaid
flowchart TD
    %% 输入层
    STATE["已入账自我状态<br/>安全值 / 服务值 / 世界事实 / 需求状态"]
    TASK_MSG["任务管理上行消息<br/>派生需求 / 执行结果 / 观察事实 / 任务回执"]
    SELF_CHECK["自检报告旁路<br/>缺失 / 差距 / 维护候选"]
    PERIPHERAL["外设与外部事件旁路<br/>观察材料 / 外部事件动态"]

    %% 自我线程治理主链
    EMOTION_MODULE["情绪模块（待完善）<br/>分析安全值变化 / 因果值变化 / 压力信息"]
    EMOTION_DATA["情绪基础数据<br/>安全变化量 / 因果变化量 / 压力状态 / 失败退出累积"]
    MAILBOX["治理消息入口<br/>接收情绪基础数据后再消费治理消息"]
    MIRROR["根需求状态镜像<br/>读取安全值 / 服务值当前状态"]
    ROOT_GAP["根缺口与方向重判<br/>安全 / 服务目标差只说明缺口"]
    TARGET_DIR["目标方向计算<br/>目标方向 = 目标值 - 当前值<br/>只取 &gt;0 / =0 / &lt;0"]
    SELF_CHECK_DATA["自检与治理负荷汇总<br/>无方法 / 方法不可用 / 缺执行入口<br/>任务堆积 / 阻塞 / 反复失败"]
    FAIL_EXIT["全循环失败退出事实汇总<br/>需求 / 任务 / 批准 / 执行 / 满足<br/>拒绝 / 阻断 / 不可比 / 无推进"]
    PRESSURE_LEDGER["全循环压力账本 / 压力值<br/>贯穿需求生成 / 任务生成 / 执行批准 / 任务执行 / 需求满足"]
    PRESSURE_FEEDBACK["压力读取与情绪化解释<br/>全循环压力值 + 自检异常 + 治理负荷"]
    DEMAND_ADMIT["需求重判与准入<br/>正式需求 / 派生需求 / 维护报告"]
    DEMAND_TREE["需求树治理<br/>权重刷新 / 活动集 / 当前主需求"]
    NEED_TASK{"当前主需求<br/>需要任务承接?"}
    INTENT["生成任务发起 / 调度意图<br/>自我线程只发意图"]
    WAIT["等待任务管理反馈<br/>不裸写任务状态"]

    %% 任务管理交互
    TASK_IF["任务管理界面线程<br/>接收任务发起 / 调度 / 状态推进意图"]
    TASK_PREP["任务筹办推进<br/>创建任务 / 筹办 / 就绪回执"]
    EXEC_APPROVAL_REQ["执行批准申请<br/>当前任务 / 当前方法 / 输入场景 / 风险价值依据"]
    EXEC_APPROVAL["批准执行模块<br/>执行前治理闸门"]
    APPROVAL_GATE{"是否批准执行?"}
    TASK_EXEC["任务执行推进<br/>批准后才调用当前方法"]
    TASK_RETURN["任务管理上行回执<br/>结果状态 / 缺口 / 派生需求 / 完成证据"]

    %% 回执裁决与方向一致性
    RESULT_READ["读取回执和输出事实<br/>任务目标结果 / 输出结果场景 / 动作动态证据"]
    ACTUAL_DIR["实际变化方向计算<br/>实际方向 = 结果值 - 初始值<br/>只取 &gt;0 / =0 / &lt;0"]
    DIR_GATE{"方向一致性<br/>实际方向是否朝目标方向收束?"}
    SAT_GATE{"需求满足闸门<br/>目标特征是否达成?"}
    WRITEBACK["回写需求满足<br/>需求域正式提交入口"]
    SETTLE["叶子任务价值结算<br/>安全值 / 服务值增量来自合法结算规则"]
    NEXT["更新自我状态账本<br/>进入下一轮需求重判"]

    %% 缺口与反向处理
    GAP["缺口裁决<br/>业务缺口 / 条件缺口 / 学习缺口"]
    DERIVED["派生需求入树裁决<br/>不得越过自我线程直接入树"]
    REPLAN["重筹办 / 等待 / 外部支撑<br/>交回任务管理或等待材料"]
    CONFLICT["方向冲突 / 不可比<br/>拒绝回写 / 记录日志 / 生成冲突材料"]

    %% 硬规则
    RULE_TASK["边界: 任务筹办 / 任务执行<br/>不是自我线程内部动作"]
    RULE_THREAD["边界: 线程不是动作来源<br/>动作动态必须绑定方法或提交入口"]
    RULE_SELF_CHECK["边界: 自检不是主循环<br/>只作为旁路材料"]
    RULE_PRESSURE["边界: 压力值贯穿全循环<br/>缺口本身不是压力，失败退出写入压力账"]
    RULE_COMPRESS["边界: 特征归类压缩<br/>属于细分流程，随遇即执行"]

    subgraph INPUT["输入与旁路材料"]
        STATE --> EMOTION_MODULE
        TASK_MSG -->|进入治理前采样| EMOTION_MODULE
        SELF_CHECK -->|压力与缺失材料| EMOTION_MODULE
        PERIPHERAL -->|外部刺激材料| EMOTION_MODULE
        EMOTION_MODULE --> EMOTION_DATA --> MAILBOX
    end

    subgraph SELF_THREAD["自我线程治理主链"]
        MAILBOX --> MIRROR --> ROOT_GAP --> TARGET_DIR --> DEMAND_ADMIT --> DEMAND_TREE --> NEED_TASK
        MAILBOX --> SELF_CHECK_DATA --> PRESSURE_FEEDBACK --> DEMAND_TREE
        FAIL_EXIT --> PRESSURE_LEDGER --> PRESSURE_FEEDBACK
        PRESSURE_LEDGER -.下一轮压力值.-> EMOTION_MODULE
        NEED_TASK -->|是| INTENT --> WAIT
        NEED_TASK -->|否, 已满足或待机| NEXT
    end

    subgraph TASK_BOUNDARY["自我线程与任务管理线程交互"]
        INTENT --> TASK_IF --> TASK_PREP --> EXEC_APPROVAL_REQ --> EXEC_APPROVAL --> APPROVAL_GATE
        APPROVAL_GATE -->|批准| TASK_EXEC --> TASK_RETURN --> TASK_MSG
        APPROVAL_GATE -->|暂缓 / 拒绝 / 补证据| TASK_RETURN
        APPROVAL_GATE -->|暂缓 / 拒绝 / 补证据| FAIL_EXIT
        WAIT --> TASK_MSG
    end

    subgraph RESULT_JUDGE["回执裁决与方向一致性"]
        TASK_RETURN --> RESULT_READ --> ACTUAL_DIR --> DIR_GATE
        DIR_GATE -->|同向 / 有推进| SAT_GATE
        DIR_GATE -->|零推进| GAP
        DIR_GATE -->|反向 / 不可比| CONFLICT
        SAT_GATE -->|已达成| WRITEBACK --> SETTLE --> NEXT
        SAT_GATE -->|未达成| GAP
    end

    subgraph GAP_FLOW["缺口与重筹办回路"]
        GAP --> DERIVED --> DEMAND_TREE
        GAP --> REPLAN --> TASK_IF
        CONFLICT --> GAP
        GAP --> FAIL_EXIT
        CONFLICT --> FAIL_EXIT
        REPLAN -->|反复重筹办或等待失效| FAIL_EXIT
    end

    subgraph RULES["硬规则闸门"]
        RULE_TASK -.约束.-> TASK_PREP
        RULE_TASK -.约束.-> TASK_EXEC
        RULE_THREAD -.约束.-> WRITEBACK
        RULE_THREAD -.约束.-> SETTLE
        RULE_THREAD -.约束.-> EXEC_APPROVAL
        RULE_SELF_CHECK -.约束.-> SELF_CHECK
        RULE_PRESSURE -.约束.-> ROOT_GAP
        RULE_PRESSURE -.约束.-> PRESSURE_FEEDBACK
        RULE_PRESSURE -.约束.-> SETTLE
        RULE_COMPRESS -.约束.-> MAILBOX
    end

    classDef self fill:#e8f4ff,stroke:#2f6f9f,color:#102030;
    classDef task fill:#eef7ed,stroke:#3f7f4f,color:#102010;
    classDef gate fill:#fff3dc,stroke:#a0671e,color:#24160a;
    classDef side fill:#eaf7f6,stroke:#3c8b86,color:#102827;
    classDef warn fill:#ffecec,stroke:#b84a4a,color:#401111;
    classDef result fill:#f1edff,stroke:#6c56a4,color:#1d1730;
    classDef pressure fill:#fff0f3,stroke:#b4234a,color:#3a0d18;

    class EMOTION_MODULE,EMOTION_DATA,MAILBOX,MIRROR,ROOT_GAP,TARGET_DIR,SELF_CHECK_DATA,FAIL_EXIT,PRESSURE_FEEDBACK,DEMAND_ADMIT,DEMAND_TREE,INTENT,WAIT,EXEC_APPROVAL,WRITEBACK,SETTLE,NEXT self;
    class TASK_IF,TASK_PREP,TASK_EXEC,TASK_RETURN,TASK_MSG,EXEC_APPROVAL_REQ task;
    class NEED_TASK,APPROVAL_GATE,DIR_GATE,SAT_GATE gate;
    class SELF_CHECK,PERIPHERAL,STATE side;
    class GAP,DERIVED,REPLAN,CONFLICT result;
    class PRESSURE_LEDGER pressure;
    class RULE_TASK,RULE_THREAD,RULE_SELF_CHECK,RULE_PRESSURE,RULE_COMPRESS warn;
```

## 本图暂定结论

1. 特征归类压缩不作为本总图里的独立入口模块展开；它属于细分流程中的 `随遇即执行` 机制。
2. 细分流程中再展开：任一事实、材料、回执、失败退出或压力值入账时，都按 `特征类型 + 特征值` 立即归类。
3. 压缩不是简单裁剪数据，也不是自由文本概括；压缩就是归类，通过特征结构把低层具体事实归到高层分类。
4. 例如：整个现实世界可通过高层特征归到 `宇宙`；多种可吃的果实可按特征值归到 `水果`、`干果` 等类别。
5. 底层保留完整事实、日志、动作动态和证据链；高层保留对象类、能力类、需求类、压力类、方向和证据指针。
6. 本总图只表现主循环顺序，不把归类压缩画成每轮开始的集中步骤。
7. 情绪基础数据不是需求、不是价值结算事实，也不能直接改写安全值 / 服务值；它只作为后续需求重判、估权和调度倾向的输入。
8. 自我线程的主入口不是自检，而是 `情绪基础数据 + 已入账状态 + 治理消息`。
9. 自我线程必须计算目标方向：`目标值 - 当前值`，方向只取 `>0 / =0 / <0`。
10. 缺口只说明目标未满足，不等于压力。
11. 压力值不是自我线程局部产物，而是贯穿整个大循环的全循环压力账本。
12. 自我线程负责读取、解释和使用压力值；压力来源可以来自需求生成、任务生成、执行批准、任务执行、需求满足等任一模块。
13. 压力是综合信息的情绪化反馈，主要来自全循环失败退出事实、自检异常和治理负荷；压力值入账时也会立即进入特征归类机制。
14. 任何失败退出逻辑的生成都是压力来源：拒绝入树、拒绝任务化、筹办失败退出、执行批准拒绝 / 暂缓、执行失败退出、不可比、无推进、阻断、等待失效、反复重筹办、拒绝结算等。
15. 任务执行后的回执裁决必须计算实际变化方向：`结果值 - 初始值`。
16. 需求回写前必须经过方向一致性闸门：同向才可能进入满足判断；零推进进入缺口；反向或不可比进入冲突 / 拒绝 / 重筹办。
17. 任务筹办就绪后不能直接执行；必须先提交执行批准申请，经过批准执行模块裁决。
18. 批准执行模块是执行前治理闸门，不是方法动作来源；批准后任务管理线程才进入任务执行推进。
19. 执行批准被暂缓、拒绝或要求补证据时，必须形成任务回执，并写入全循环压力账。
20. 自我线程只生成任务发起、调度、重筹办、执行批准等治理意图；任务状态推进、筹办和执行属于任务管理线程。
21. 任务完成不等于需求满足；需求满足必须由自我线程通过正式需求回写入口登记。
22. 安全值 / 服务值真实变化只能来自叶子任务完成后的合法价值结算链，不能由根缺口、压力反馈、情绪基础数据、分类结构或原始材料直接结算。
23. 自检、外设、外部事件都是旁路材料源，必须先提交、准入或入账，才能影响需求树。

## 下一张图建议

下一张建议拆 `自我线程方向一致性裁决流程图`：

```text
来源需求目标状态
-> 当前值回查
-> 目标方向 = 目标值 - 当前值
-> 任务输出事实
-> 实际方向 = 结果值 - 初始值
-> 同向 / 零推进 / 反向 / 不可比
-> 需求回写 / 缺口 / 冲突 / 重筹办
```
