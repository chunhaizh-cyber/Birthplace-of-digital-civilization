# 本能方法：观察内部实现逻辑流程图 v0.3

更新时间：2026-06-05

## 用途

本图在 v0.2 的多帧稳定复现口径上，补充观察与识别的职责边界。

观察的目标不是直接判断“是不是自我所在场景中的同一个存在”，而是：

```text
根据外设取得的现实特征值发现外设观察存在；
或者根据新取得的现实特征值完善已经存在的外设观察存在 / 待复验存在的特征集合。
```

识别的目标另行处理：

```text
用自我所在场景中的存在特征，
与外设观察存在的特征信息比较，
判断是否为同一个存在。
```

因此：

```text
观察 = 发现存在或完善存在特征。
识别 = 判断外设观察存在是否与自我所在场景中的某个存在相同。
```

## 流程图

```mermaid
flowchart TD
    START["观察本能方法启动<br/>读取输入参数场景"]
    IN0["输入现实特征<br/>观察范围 / ROI / 观察目的<br/>期望特征类型集合<br/>最小观测帧数 / 最小稳定帧数<br/>可选: 指定待完善存在"]
    IN1["外设材料前置<br/>外设可用状态=1<br/>当前观察特征帧取得状态=1<br/>彩色帧 / 深度帧 / 掩码 / 点集材料可回查状态=1"]
    G0{"材料与观察范围是否满足?"}
    GAP0["输出缺口特征<br/>观察材料可用状态=0<br/>观察质量缺口数量>0"]

    F0["提取现实特征值<br/>颜色 / 深度 / 空间坐标<br/>空间范围AABB / 轮廓 / 掩码 / 点集"]
    F1["组织观察特征集合<br/>像素集合掩码<br/>彩色轮廓局部图<br/>深度轮廓局部图<br/>特征类型 -> 特征值"]
    A0{"是否能归入<br/>已有外设观察存在<br/>或指定待完善存在?"}

    NEW0["发现外设观察存在<br/>建立基准观察存在"]
    NEW1["第一帧基准输出<br/>来源帧 / 像素集合掩码<br/>彩色轮廓局部图 / 深度轮廓局部图<br/>基准特征值集合<br/>基准状态=已建立<br/>观察状态=待复现"]
    NEW2["新发现状态<br/>外设观察存在发现状态=已发现<br/>但不等于世界存在确认"]

    ADD0["完善存在特征<br/>把新特征值补入已存在的观察特征集合"]
    ADD1["特征完善输出<br/>新增特征类型数量<br/>新增特征值数量<br/>已有特征值更新候选数量<br/>特征补全状态=部分补全 / 已补全"]
    ADD2["边界<br/>完善特征不等于同一性已确认<br/>跨来源同一性仍交给识别"]

    R0["第二帧及以后<br/>围绕同一外设观察存在持续复现"]
    R1["计算跨帧误差<br/>颜色误差 / 深度误差 / 空间坐标误差<br/>AABB变化 / 轮廓偏差 / 掩码重合率<br/>点集或局部三维范围误差"]
    R2["误差落为特征<br/>误差序列 / 最近N帧平均误差<br/>最大误差 / 方差<br/>连续稳定帧数 / 连续异常帧数"]
    R3["按特征类型读取稳定值域<br/>每种特征类型有自己的允许误差和值域"]

    C0{"关键特征是否多帧稳定复现?"}
    S0["观察稳定输出<br/>稳定复现状态=稳定<br/>观察完成状态=已完成<br/>只限本次任务 / 当前视野 / 当前外设配置 / 当前时间窗口"]
    P0{"是否部分特征稳定?"}
    P1["部分完成输出<br/>逐特征稳定状态<br/>颜色稳定 / 轮廓稳定 / 深度不稳定 / 空间不稳定等"]
    X0["未完成输出<br/>观察完成状态=未完成 / 证据不足<br/>丢失 / 漂移 / 冲突 / 拆分 / 合并计数"]

    OUT0["观察结果场景<br/>外设观察存在集合<br/>发现状态 / 基准状态 / 观察完成状态<br/>逐特征稳定状态<br/>特征补全状态<br/>误差统计特征"]
    OUT1["提交给识别的输入<br/>待识别外设观察存在数量<br/>外设观察存在特征集合<br/>可比较特征类型集合<br/>多帧稳定证据帧数"]
    OUT2["后续分流<br/>需要判断同一存在 -> 识别<br/>已归属存在需动态 -> 扫描<br/>指定存在需持续观测 -> 跟踪"]

    RULE0["硬边界<br/>观察不直接判断是否为自我所在场景的同一个存在"]
    RULE1["硬边界<br/>观察完成不是世界存在永久成立"]
    RULE2["硬边界<br/>输入输出只能是现实特征类型与特征值"]

    START --> IN0 --> IN1 --> G0
    G0 -->|否| GAP0 --> OUT0
    G0 -->|是| F0 --> F1 --> A0
    A0 -->|否| NEW0 --> NEW1 --> NEW2 --> R0
    A0 -->|是| ADD0 --> ADD1 --> ADD2 --> R0
    R0 --> R1 --> R2 --> R3 --> C0
    C0 -->|是| S0 --> OUT0
    C0 -->|否| P0
    P0 -->|是| P1 --> OUT0
    P0 -->|否| X0 --> OUT0
    OUT0 --> OUT1 --> OUT2

    RULE0 -.约束.-> OUT1
    RULE1 -.约束.-> S0
    RULE2 -.约束.-> OUT0

    classDef input fill:#e8f4ff,stroke:#2f6f9f,color:#102030;
    classDef process fill:#eef7ed,stroke:#3f7f4f,color:#102010;
    classDef discover fill:#fff4df,stroke:#a66a13,color:#251506;
    classDef output fill:#eef8f7,stroke:#397b77,color:#102827;
    classDef gate fill:#fff8e8,stroke:#9a6a1a,color:#24160a;
    classDef warn fill:#ffecec,stroke:#b84a4a,color:#401111;

    class START,IN0,IN1 input;
    class F0,F1,ADD0,ADD1,R0,R1,R2,R3,P1 process;
    class NEW0,NEW1,NEW2 discover;
    class OUT0,OUT1,OUT2,S0 output;
    class G0,A0,C0,P0 gate;
    class GAP0,ADD2,X0,RULE0,RULE1,RULE2 warn;
```

## 观察输出的现实特征口径

| 宿主 | 特征类型 | 特征值 | 说明 |
| --- | --- | --- | --- |
| 外设观察存在 | 外设观察存在发现状态 | `已发现 / 未发现` | 表示外设特征已经组织出一个观察层面的存在。 |
| 外设观察存在 | 基准状态 | `未建立 / 已建立` | 第一帧只建立基准，不代表观察完成。 |
| 外设观察存在 | 观察完成状态 | `未完成 / 部分完成 / 已完成 / 证据不足` | 来自多帧稳定复现判断。 |
| 外设观察存在 | 特征补全状态 | `未补全 / 部分补全 / 已补全` | 表示本次观察是否完善了已有存在的特征集合。 |
| 外设观察存在 | 新增特征类型数量 | `>= 0` | 本次观察补入的新特征类型数量。 |
| 外设观察存在 | 新增特征值数量 | `>= 0` | 本次观察补入的新现实特征值数量。 |
| 外设观察存在 | 可比较特征类型集合 | 颜色 / 深度 / 空间 / 轮廓 / 掩码 / 点集等集合 | 供识别比较使用。 |
| 外设观察存在 | 多帧稳定证据帧数 | `>= 0` | 供识别判断是否证据足够。 |
| 外设观察存在 | 误差统计特征 | 平均误差 / 最大误差 / 方差 / 连续稳定帧数 | 误差是稳定性特征，不只是日志。 |

## 与识别的边界

观察能回答：

```text
外设当前看到了哪些可稳定复现的特征？
这些特征是否可以组织成一个外设观察存在？
这些特征是否可以补充到某个已存在的观察特征集合？
```

观察不能直接回答：

```text
它是不是自我所在场景中已经登记的同一个存在？
它是不是世界存在永久成立？
它的语义类别是什么？
它是否已经具备跨场景身份？
```

需要同一性判断时，观察输出进入识别：

```text
外设观察存在特征集合
    + 自我所在场景存在特征集合
    + 同一性比较值域
    -> 识别任务
```

## 结论

观察是现实特征组织方法：它根据外设特征发现外设观察存在，或者完善一个存在的特征集合。识别才是同一性比较方法：它用自我所在场景中的存在特征和外设获取的特征信息比较，判断是否为同一个存在，并且也可能需要多帧才能完整识别。
