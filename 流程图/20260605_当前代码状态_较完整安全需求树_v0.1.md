# 当前代码状态：较完整安全需求树 v0.1

更新时间：2026-06-05

## 口径

本图画的是“当前代码可生成 / 维护 / 承接的安全需求树结构”，不是声明当前真实运行中每个节点都已自然完成。

当前代码的安全需求树已经从旧的固定安全字段，改为：

```text
安全根需求
-> 安全因果风险树维护
-> 按世界树因果模板反推组成因
-> 生成缺口派生需求消息
-> 提交_生成缺口子需求
-> 正式需求入树和任务化
```

## 总图

```mermaid
flowchart TB
    ROOT["安全根需求<br/>目标: 自我_安全值 = 最大值<br/>当前值低于目标时: 安全权重=1"]
    MIRROR["刷新根需求状态镜像<br/>被需求当前状态 / 需求状态<br/>根闭环信息位"]
    CLEAN["清理旧固定安全子需求<br/>旧固定安全需求生成=0<br/>重新根据安全因果链生成=1"]
    HANDOFF["安全需求因果链交由风险树生成<br/>固定安全需求生成=0<br/>创建占位方法头=0"]

    MAINTAIN["自我线程维护安全因果风险树<br/>首次 / 输入变化 / 低频兜底触发<br/>任务筹办更新权=0"]
    SRC["来源因果<br/>自我安全值目标<br/><- 风险安全层明确状态"]
    LAYER["安全需求子目标<br/>风险安全层明确状态 = 已明确"]
    FACTOR_SRC["组成因来源<br/>从因果信息组读取<br/>固定五因素生成=0"]
    SCENE_IMPACT["当前已具备较完整分支<br/>风险安全_场景影响部分已入账状态 = 1"]
    OBS_ALL["场景影响前置<br/>获取当前场景所有可被观测单位 = 1"]

    ORG["逻辑组织<br/>OR组满足状态<br/>多候选替代路径组"]
    P_D455["方法路径完成状态<br/>D455观察确认路径完成状态 = 1"]
    P_CACHE["方法路径完成状态<br/>已有场景缓存路径完成状态 = 1"]
    P_HUMAN["方法路径完成状态<br/>交互者说明路径完成状态 = 1"]

    D455_AND["D455观察确认路径<br/>材料与对应事实闭合"]
    D455_M0["当前观察材料集取得状态 = 1"]
    D455_M1["外设观察材料可回查状态 = 1"]
    D455_M2["当前观察范围有效可观测单位计数事实取得状态 = 1"]
    D455_M3["当前观察范围可观测单位存在稳定对应事实取得状态 = 1"]
    D455_M4["稳定对应存在数量明确状态 = 1"]
    D455_M5["当前观察范围可观测单位存在对应闭合状态 = 1"]

    IDENTIFY["自我_识别外设观察材料<br/>组织外设识别提交包<br/>不直接写世界真值"]
    SUB_CORR["提交_当前观察范围可观测单位存在对应事实<br/>提交状态=已提交<br/>稳定对应事实取得=1<br/>对应闭合=1"]
    SCAN_START["扫描可启动状态 = 1<br/>条件: 稳定对应存在数量 >= 1"]

    SCAN_FACT["并行后续证据<br/>自我_扫描已识别区域"]
    FOUND["提交_观察存在发现事实<br/>第一次扫描: 发现存在 / 发现特征事实"]
    CHANGE["提交_观察存在特征值变化事实<br/>有可比历史后: 变化事实"]
    TRACK["自我_跟踪指定存在<br/>提交_指定存在跟踪事实<br/>仅指定目标, 不是全场扫描"]

    EVAL["安全_评估当前场景安全性<br/>读取已入账观察/扫描/跟踪事实<br/>构造风险安全_场景影响部分候选"]
    EVAL_GATES["评估证据闸门<br/>基础观察事实可用<br/>场景存在集合明确<br/>扫描变化事实已入账<br/>关键特征变化明确<br/>安全因果候选明确<br/>必要时跟踪事实已入账"]
    SUB_SCENE["提交_风险安全场景影响部分状态变化<br/>风险安全_场景影响部分已入账状态=1<br/>安全因果因素已入账状态=1"]
    AGG["风险安全层聚合候选更新<br/>风险安全层候选 / 投影候选<br/>风险安全层明确状态候选"]

    GENERIC["其他安全因素<br/>仅当因果信息组提供组成因时展开"]
    SEARCH["安全_搜索安全因果因素证据<br/>搜索状态=已完成<br/>负向证据/材料缺失/冲突计数"]
    DEFAULT_OK["提交_安全因果因素无负证据默认满足<br/>默认满足允许=1 且无负向/缺失/冲突<br/>安全因果因素已入账=1"]

    MSG["安全因果风险树缺口派生需求消息<br/>携带目标宿主 / 目标特征 / 当前状态 / 目标状态 / 来源因果"]
    SUB_GAP["提交_生成缺口子需求<br/>正式入树<br/>父任务保持等待<br/>派发任务管理工作线程"]

    STOP["边界<br/>不写风险安全层总值<br/>不写自我安全值<br/>不结算价值"]

    ROOT --> MIRROR --> CLEAN --> HANDOFF --> MAINTAIN
    MAINTAIN --> SRC --> LAYER --> FACTOR_SRC --> SCENE_IMPACT --> OBS_ALL --> ORG
    ORG --> P_D455
    ORG --> P_CACHE
    ORG --> P_HUMAN

    P_D455 --> D455_AND
    D455_AND --> D455_M0
    D455_AND --> D455_M1
    D455_AND --> D455_M2
    D455_AND --> D455_M3
    D455_AND --> D455_M4
    D455_AND --> D455_M5
    D455_M3 --> IDENTIFY --> SUB_CORR --> SCAN_START
    D455_M4 --> SUB_CORR
    D455_M5 --> SUB_CORR

    SCAN_START -.->|"不是当前场景可观测单位取得的完成子需求"| SCAN_FACT
    SCAN_FACT --> FOUND
    SCAN_FACT --> CHANGE
    TRACK --> EVAL
    FOUND --> EVAL
    CHANGE --> EVAL
    SUB_CORR --> EVAL
    EVAL_GATES --> EVAL --> SUB_SCENE --> AGG --> LAYER

    FACTOR_SRC --> GENERIC --> SEARCH --> DEFAULT_OK --> AGG

    MAINTAIN --> MSG --> SUB_GAP
    LAYER --> MSG
    SCENE_IMPACT --> MSG
    OBS_ALL --> MSG
    ORG --> MSG
    D455_AND --> MSG

    MAINTAIN --> STOP
    EVAL --> STOP
    SUB_SCENE --> STOP
    AGG --> STOP

    classDef root fill:#fff4d6,stroke:#9a6700,stroke-width:2px,color:#1f2328;
    classDef demand fill:#eef6ff,stroke:#0969da,color:#1f2328;
    classDef causal fill:#f6f8fa,stroke:#57606a,color:#1f2328;
    classDef method fill:#e6ffed,stroke:#1a7f37,color:#1f2328;
    classDef gate fill:#fff1f1,stroke:#cf222e,color:#1f2328;
    classDef later fill:#f0e7ff,stroke:#8250df,color:#1f2328;

    class ROOT root;
    class LAYER,SCENE_IMPACT,OBS_ALL,ORG,P_D455,P_CACHE,P_HUMAN,D455_AND,D455_M0,D455_M1,D455_M2,D455_M3,D455_M4,D455_M5,MSG,SUB_GAP demand;
    class MAINTAIN,SRC,FACTOR_SRC,GENERIC causal;
    class IDENTIFY,SUB_CORR,SCAN_FACT,FOUND,CHANGE,TRACK,EVAL,SUB_SCENE,SEARCH,DEFAULT_OK,AGG method;
    class CLEAN,HANDOFF,EVAL_GATES,STOP gate;
    class SCAN_START later;
```

## 当前较完整的安全需求树形态

```text
安全根需求
└── 自我_安全值 = 最大值
    └── 安全因果风险树
        └── 风险安全层明确状态 = 已明确
            └── 风险安全_场景影响部分已入账状态 = 1
                └── 获取当前场景所有可被观测单位 = 1
                    └── OR组满足状态
                        ├── D455观察确认路径完成状态 = 1
                        │   └── AND: 材料与对应事实闭合
                        │       ├── 当前观察材料集取得状态 = 1
                        │       ├── 外设观察材料可回查状态 = 1
                        │       ├── 当前观察范围有效可观测单位计数事实取得状态 = 1
                        │       ├── 当前观察范围可观测单位存在稳定对应事实取得状态 = 1
                        │       ├── 稳定对应存在数量明确状态 = 1
                        │       └── 当前观察范围可观测单位存在对应闭合状态 = 1
                        ├── 已有场景缓存路径完成状态 = 1
                        └── 交互者说明路径完成状态 = 1
```

## 运行承接链

| 层 | 当前代码口径 | 主要生产者 / 承接者 |
| --- | --- | --- |
| 安全根 | `自我_安全值` 低于目标时激活安全根权重。 | 自我线程刷新根需求状态镜像。 |
| 风险树 | 安全子需求不再由旧固定安全字段生成，交由安全因果风险树。 | `私有_维护安全因果风险树_线程侧`。 |
| 组成因 | 风险安全层组成因来自因果信息组，不从 C++ 固定五因素表生成。 | 初始因果模板 / 后天因果模板。 |
| 当前较完整分支 | 场景影响部分依赖当前场景可观测单位取得。 | `data/initial_causal_templates.tsv`。 |
| D455路径 | D455路径需要材料与对应事实闭合。 | 外设材料包、自我识别组织方法、对应事实提交入口。 |
| 后续安全证据 | 扫描、跟踪提供安全评估证据，但不是“当前场景可观测单位取得”的完成子需求。 | `提交_观察存在发现事实`、`提交_观察存在特征值变化事实`、`提交_指定存在跟踪事实`。 |
| 场景影响入账 | 安全评估只产出候选，提交入口裁决后才写场景影响部分入账。 | `安全_评估当前场景安全性`、`提交_风险安全场景影响部分状态变化`。 |
| 核心值 | 风险树、评估、提交都不得直接写自我安全值。 | 后续结算方法按证据裁决。 |

## 不能误画成已完成的部分

```text
1. 任务筹办不再展开固定风险树因素。
2. 自我线程不再生成固定 D455 / 缓存 / 交互说明 OR 子需求表。
3. 风险安全层不再由 C++ 固定五因素表生成。
4. D455材料、像素簇、报告、材料页、句柄都不是安全值证据本体。
5. 扫描 / 跟踪不是当前场景可观测单位取得的完成子需求，只是后续安全评估证据来源。
6. 安全评估只生成候选视图，不直接写风险安全层总值或自我安全值。
7. 缺自然日志样本时，只能说代码链路具备，不能说真实安全需求树已闭合。
```

## 代码锚点

```text
自我线程模块.impl.cpp
    私有_刷新根特征必要条件子需求_线程侧
    私有_确保安全因果风险树初始模板_线程侧
    私有_维护安全因果风险树_线程侧
    私有_收到派生需求消息时完善需求树
    自我线程/安全因果风险树缺口提交入树

任务模块.筹办.ixx
    筹办_展开安全因果风险树
        当前只记录跳过：任务筹办不再展开固定风险树因素。

需求类.cpp
    OR组满足状态
    方法路径完成状态
    构造获取途径逻辑组织需求更新指令

自我动作实现.外设模块.ixx
    提交_当前观察范围可观测单位存在对应事实
    提交_观察存在发现事实
    提交_观察存在特征值变化事实
    提交_指定存在跟踪事实
    安全_评估当前场景安全性
    提交_风险安全场景影响部分状态变化
    安全_搜索安全因果因素证据
    提交_安全因果因素无负证据默认满足

data/initial_causal_templates.tsv
    风险安全层明确依赖场景影响入账
    场景影响依赖当前场景可观测单位取得
    D455 / 缓存 / 交互说明三条取得路径
    D455观察确认路径材料与对应事实闭合
```
