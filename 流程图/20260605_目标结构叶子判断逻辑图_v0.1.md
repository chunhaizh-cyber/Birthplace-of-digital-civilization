# 目标结构叶子判断逻辑图 v0.1

更新时间：2026-06-05

## 用途

本图是 `任务筹办普通函数状态机流程图 v0.2` 中：

```text
是否已是目标结构叶子?
无需继续拆分路径
```

的细分判断图。

本图只判断当前任务目标在结构上是否已经不需要继续拆路径，不判断方法是否已经可执行。若输出 `ENTER_METHOD_PREP`，也只表示可以进入找方法 / 找条件阶段。

## 判断原则

```text
1. 叶子只是结构前提，不是语义前提。
2. 目标结构叶子必须同时满足：目标可解析、目标语义允许普通任务化、没有未闭合的必经子路径、没有待裁决派生需求、没有仍需展开的路径缺口。
3. AND组 / OR组 / 方法路径组 / 因果子链组织节点，即使当前没有子节点，也不能直接当作普通叶子任务进入方法候选。
4. 证据补齐 / 学习支撑 / 副作用约束等非直接执行材料，不应误判为当前目标已经可执行。
5. OR 替代路径不要求所有分支都闭合；至少一条路径闭合且无强制等待理由时，可以进入路径评价或路径选择。
6. AND 必经路径必须全部闭合；顺序链必须确认前序步骤闭合。
7. 判断结果必须返回结构化回执，由任务管理界面线程消费，筹办普通函数不直接写任务状态。
```

## 判断逻辑图

```mermaid
flowchart TD
    INPUT["输入: 当前任务筹办上下文<br/>任务 / 来源需求 / 目标语义视图 / 子需求视图 / 路径关系视图"]
    SNAPSHOT["固定本轮快照<br/>任务版本 / 来源需求版本 / 子需求版本 / 路径草案版本"]

    TARGET_GATE{"来源目标是否完整可解析?"}
    TARGET_SOURCE_GATE["外部细分图: 不可解析来源分类<br/>逻辑错误 / 真实目标补齐 / 证据不足 / 版本冲突"]

    VERSION_GATE{"快照前后是否一致?"}
    VERSION_CONFLICT["筹办回执: 版本冲突<br/>重读上下文 / 待重筹办"]

    SEMANTIC_VIEW["读取目标特征定义视图<br/>普通目标 / 逻辑组织节点 / 结构性缺口 / 结算令牌"]
    SEMANTIC_GATE{"目标语义类型?"}

    ORDINARY_TARGET["普通可任务化目标<br/>可通过方法结果能力承接"]
    GROUP_TARGET["逻辑组织节点<br/>AND组 / OR组 / 方法路径组 / 因果子链"]
    STRUCT_GAP_TARGET["结构性缺口目标<br/>治理缺口 / 规范缺口 / 初始化补丁"]
    TOKEN_TARGET["路径结算或聚合令牌<br/>用于闭合组状态，不进入普通方法候选"]

    PENDING_SUBMIT_GATE{"是否存在待裁决<br/>派生需求 / 更新指令 / 学习草案?"}
    SUBMIT_WAIT["筹办回执: 等待需求提交结果<br/>已入树 / 已复用 / 被拒绝 / 证据不足"]

    CHILD_VIEW["读取直接子需求 / 子任务 / 路径组视图<br/>活动状态 / 闭合状态 / 关系类型 / 稳定键"]
    ACTIVE_CHILD_GATE{"是否存在未闭合的<br/>活动子需求或阻塞子任务?"}
    REL_GATE{"子路径关系类型?"}

    AND_GATE{"AND 必经路径<br/>是否全部闭合?"}
    OR_GATE{"OR 替代路径<br/>是否至少一条闭合?"}
    OR_FORCE_WAIT{"未闭合 OR 路径<br/>是否有强制等待理由?"}
    SEQ_GATE{"顺序链前序步骤<br/>是否闭合?"}
    SUPPORT_GATE{"支撑类子路径<br/>是否仍阻塞当前判断?"}

    CHILD_WAIT["筹办回执: 等待子路径闭合<br/>携带唤醒条件 / 超时策略 / 失效归因"]
    PATH_EVAL["进入路径评价 / 路径选择<br/>按收益、可执行性、风险和证据选择"]
    PATH_EXPAND_GATE{"目标已有路径是否足够?<br/>无需继续拆分?"}
    PATH_EXPAND["进入找下一步可选步骤<br/>促成 / 避免 / 条件补齐 / 方法就绪 / 证据补齐 / 学习缺口"]

    ORDINARY_CHILD_GATE{"普通目标是否还有<br/>未满足业务子需求?"}
    NONBLOCKING_GATE{"剩余子项是否仅为<br/>非阻塞证据 / 历史材料 / 已失效候选?"}
    LEAF_RECEIPT["目标结构叶子回执<br/>无需继续拆分路径"]
    ENTER_METHOD_PREP["进入方法筹办<br/>填充候选方法集 / 候选回退 / 找条件"]

    GROUP_CHILD_GATE{"组织节点是否已有<br/>合法子路径或聚合材料?"}
    GROUP_EMPTY_KIND{"无子路径原因?"}
    GROUP_EXPAND["筹办回执: 组织节点需展开<br/>生成子路径草案 / 等待提交裁决"]
    GROUP_AGGREGATE["筹办回执: 组织节点聚合或结算<br/>不进入普通方法候选"]

    STRUCT_GAP_RECEIPT["筹办回执: 结构性治理缺口<br/>不任务化 / 不找普通方法"]
    TOKEN_RECEIPT["筹办回执: 路径令牌处理<br/>闭合组状态 / 等待聚合"]

    RECEIPT_OUT["输出统一筹办回执<br/>分类 / 原因 / 下一步 / 唤醒条件 / 压力材料"]
    NO_EXEC_NOTE["边界: 目标结构叶子 ≠ 可执行<br/>是否可执行由方法筹办和条件检查判断"]
    PRESSURE["压力材料<br/>长期等待子路径 / 展开失败 / 提交长期无结果 / 反复版本冲突"]

    INPUT --> SNAPSHOT --> TARGET_GATE
    TARGET_GATE -->|否| TARGET_SOURCE_GATE --> RECEIPT_OUT
    TARGET_GATE -->|是| VERSION_GATE
    VERSION_GATE -->|否| VERSION_CONFLICT --> RECEIPT_OUT
    VERSION_GATE -->|是| SEMANTIC_VIEW --> SEMANTIC_GATE

    SEMANTIC_GATE -->|普通可任务化目标| ORDINARY_TARGET --> PENDING_SUBMIT_GATE
    SEMANTIC_GATE -->|逻辑组织节点| GROUP_TARGET --> GROUP_CHILD_GATE
    SEMANTIC_GATE -->|结构性缺口| STRUCT_GAP_TARGET --> STRUCT_GAP_RECEIPT --> RECEIPT_OUT
    SEMANTIC_GATE -->|结算 / 聚合令牌| TOKEN_TARGET --> TOKEN_RECEIPT --> RECEIPT_OUT

    PENDING_SUBMIT_GATE -->|有| SUBMIT_WAIT --> RECEIPT_OUT
    PENDING_SUBMIT_GATE -->|无| CHILD_VIEW --> ACTIVE_CHILD_GATE

    ACTIVE_CHILD_GATE -->|有| REL_GATE
    ACTIVE_CHILD_GATE -->|无| PATH_EXPAND_GATE

    REL_GATE -->|AND 必经| AND_GATE
    REL_GATE -->|OR 替代| OR_GATE
    REL_GATE -->|顺序链| SEQ_GATE
    REL_GATE -->|证据补齐 / 学习支撑 / 副作用约束| SUPPORT_GATE

    AND_GATE -->|全部闭合| PATH_EVAL
    AND_GATE -->|未全部闭合| CHILD_WAIT
    OR_GATE -->|至少一条闭合| OR_FORCE_WAIT
    OR_GATE -->|无闭合路径| CHILD_WAIT
    OR_FORCE_WAIT -->|有| CHILD_WAIT
    OR_FORCE_WAIT -->|无| PATH_EVAL
    SEQ_GATE -->|前序闭合| PATH_EVAL
    SEQ_GATE -->|前序未闭合| CHILD_WAIT
    SUPPORT_GATE -->|仍阻塞| CHILD_WAIT
    SUPPORT_GATE -->|不阻塞| PATH_EVAL

    PATH_EVAL --> RECEIPT_OUT
    CHILD_WAIT --> RECEIPT_OUT

    PATH_EXPAND_GATE -->|否, 路径仍未明| PATH_EXPAND --> RECEIPT_OUT
    PATH_EXPAND_GATE -->|是| ORDINARY_CHILD_GATE

    ORDINARY_CHILD_GATE -->|有未满足业务子需求| CHILD_WAIT
    ORDINARY_CHILD_GATE -->|无| NONBLOCKING_GATE
    NONBLOCKING_GATE -->|仍有阻塞材料| CHILD_WAIT
    NONBLOCKING_GATE -->|仅非阻塞或无剩余子项| LEAF_RECEIPT --> ENTER_METHOD_PREP
    ENTER_METHOD_PREP --> RECEIPT_OUT

    GROUP_CHILD_GATE -->|已有| REL_GATE
    GROUP_CHILD_GATE -->|没有| GROUP_EMPTY_KIND
    GROUP_EMPTY_KIND -->|本轮应生成子路径| GROUP_EXPAND --> RECEIPT_OUT
    GROUP_EMPTY_KIND -->|等待提交 / 等待聚合材料| CHILD_WAIT
    GROUP_EMPTY_KIND -->|已满足聚合条件| GROUP_AGGREGATE --> RECEIPT_OUT

    NO_EXEC_NOTE -.约束.-> LEAF_RECEIPT
    NO_EXEC_NOTE -.约束.-> ENTER_METHOD_PREP

    CHILD_WAIT -.长期无进展.-> PRESSURE
    PATH_EXPAND -.展开失败或分支爆炸.-> PRESSURE
    SUBMIT_WAIT -.长期无提交结果.-> PRESSURE
    VERSION_CONFLICT -.反复冲突.-> PRESSURE

    classDef gate fill:#fff8e8,stroke:#9a6a1a,color:#24160a;
    classDef prep fill:#e8f4ff,stroke:#2f6f9f,color:#102030;
    classDef receipt fill:#f1edff,stroke:#6c56a4,color:#1d1730;
    classDef warn fill:#fff1e8,stroke:#b85c1f,color:#3a1608;
    classDef pressure fill:#fff0f3,stroke:#b4234a,color:#3a0d18;

    class TARGET_GATE,VERSION_GATE,SEMANTIC_GATE,PENDING_SUBMIT_GATE,ACTIVE_CHILD_GATE,REL_GATE,AND_GATE,OR_GATE,OR_FORCE_WAIT,SEQ_GATE,SUPPORT_GATE,PATH_EXPAND_GATE,ORDINARY_CHILD_GATE,NONBLOCKING_GATE,GROUP_CHILD_GATE,GROUP_EMPTY_KIND gate;
    class INPUT,SNAPSHOT,SEMANTIC_VIEW,ORDINARY_TARGET,GROUP_TARGET,STRUCT_GAP_TARGET,TOKEN_TARGET,CHILD_VIEW,PATH_EVAL,PATH_EXPAND,LEAF_RECEIPT,ENTER_METHOD_PREP,GROUP_EXPAND,GROUP_AGGREGATE,RECEIPT_OUT prep;
    class TARGET_SOURCE_GATE,VERSION_CONFLICT,SUBMIT_WAIT,CHILD_WAIT,STRUCT_GAP_RECEIPT,TOKEN_RECEIPT receipt;
    class NO_EXEC_NOTE warn;
    class PRESSURE pressure;
```

## 输出分类

| 分类 | 判定条件 | 下一步 |
| --- | --- | --- |
| 目标结构叶子 | 普通可任务化目标、没有待裁决草案、没有未闭合阻塞子路径、路径已经足够 | 进入方法筹办 |
| 仍需路径展开 | 普通目标路径未明，或组织节点没有合法子路径 | 进入找下一步可选步骤，生成草案后等待提交结果 |
| 等待子路径闭合 | AND 未全闭合、OR 无闭合路径或有强制等待、顺序链前序未闭合、支撑类材料仍阻塞 | 返回等待回执 |
| 进入路径评价 | AND 全闭合、OR 至少一条闭合且无强制等待、顺序链前序闭合 | 按收益、可执行性、风险和证据选择路径 |
| 组织节点处理 | 目标是 AND组 / OR组 / 方法路径组 / 因果子链 / 结算令牌 | 展开、聚合、结算或等待，不进入普通方法候选 |
| 结构性治理缺口 | 目标是结构性缺口，不是业务执行目标 | 返回治理缺口回执 |
| 版本冲突或目标不可解析 | 快照漂移或目标不可解析 | 进入外部细分分类或重筹办 |

## 接入建议

```text
1. 在 v0.2 的 `STRUCT_LEAF_GATE` 位置调用本判断。
2. 判断函数只返回结构化回执，不直接写需求树和任务状态。
3. `ENTER_METHOD_PREP` 只能说明可以进入找方法，不说明任务已就绪。
4. 组织节点必须先走目标语义视图，不得因为当前没有子节点就当作普通叶子。
5. 等待回执必须携带唤醒条件、超时条件、等待失效归因和压力材料。
```
