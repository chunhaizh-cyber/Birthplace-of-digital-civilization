# 本能方法：观察内部实现逻辑流程图 v0.1

更新时间：2026-06-05

## 用途

本图描述自我侧“观察”本能方法的内部实现逻辑。这里的观察不是相机内部采集算法，而是自我把外设提供的当前可视范围材料组织成现实特征值分区，并决定后续进入扫描、识别或跟踪。

观察方法的目标：

```text
取得当前可视范围内的观察材料，
把可视范围内的特征值按归属状态分区，
为扫描获取动态、识别归入存在、跟踪指定存在提供现实特征输入。
```

## 流程图

```mermaid
flowchart TD
    START["观察本能方法启动<br/>读取输入参数场景"]
    IN0["输入现实特征<br/>观察范围 / ROI / 观察目的 / 期望特征类型集合"]
    IN1["外设材料前置<br/>外设可用状态=1<br/>当前观察特征帧取得状态=1<br/>外设观察材料可回查状态=1"]
    G0{"材料与范围是否满足?"}
    GAP0["输出缺口特征<br/>观察材料可用状态=0<br/>观察质量缺口数量>0"]

    M0["建立当前观察材料集<br/>帧材料 / 材料页 / 可回查句柄只作证据引用"]
    M1["提取可视范围现实特征值<br/>轮廓 / 深度 / 空间占据 / 颜色纹理 / 掩码 / 点集"]
    M2["组织成可观测单位<br/>有效 / 无效 / 低质量 / 背景候选"]
    M3["读取已有归属账本<br/>可观测单位到存在映射表<br/>已稳定对应存在集合"]
    M4["按归属状态分区"]

    P0["已归属分区<br/>特征值已有比较对象<br/>进入扫描候选"]
    P1["无归属分区<br/>特征值没有比较对象<br/>进入识别候选"]
    P2["指定目标分区<br/>目标存在ID / ROI / 跟踪窗口存在<br/>进入跟踪候选"]
    P3["低质量 / 不可观测分区<br/>进入补观察或质量诊断"]

    O0["输出: 可视范围观察材料可用状态=1"]
    O1["输出: 可视范围特征归属分区状态=1"]
    O2["输出: 已归属可扫描存在数量"]
    O3["输出: 无归属特征值数量"]
    O4["输出: 指定跟踪目标数量"]
    O5["输出: 观察质量缺口数量"]

    D0{"无归属特征值数量 > 0?"}
    D1["派生识别需求草案<br/>目标: 无归属特征值归入存在"]
    D2{"已归属可扫描存在数量 > 0?"}
    D3["派生扫描需求草案<br/>目标: 获取已归属存在动态"]
    D4{"指定跟踪目标数量 > 0?"}
    D5["派生跟踪需求草案<br/>目标: 持续获取指定存在动态"]
    END["观察方法输出结果场景<br/>供任务管理回执 / 自我提交入口裁决"]

    B0["边界<br/>观察不直接生成状态变化动态"]
    B1["边界<br/>报告ID/句柄不是机器判断字段"]
    B2["边界<br/>无归属特征值不能进入扫描动态比较"]

    subgraph INPUT["输入与材料闸门"]
        START --> IN0 --> IN1 --> G0
        G0 -->|否| GAP0 --> END
    end

    subgraph ORGANIZE["观察材料组织"]
        G0 -->|是| M0 --> M1 --> M2 --> M3 --> M4
        M4 --> P0
        M4 --> P1
        M4 --> P2
        M4 --> P3
    end

    subgraph OUTPUT["现实特征输出"]
        P0 --> O2
        P1 --> O3
        P2 --> O4
        P3 --> O5
        M4 --> O0 --> O1
    end

    subgraph ROUTE["后续需求分流"]
        O3 --> D0
        D0 -->|是| D1 --> END
        D0 -->|否| D2
        O2 --> D2
        D2 -->|是| D3 --> END
        D2 -->|否| D4
        O4 --> D4
        D4 -->|是| D5 --> END
        D4 -->|否| END
    end

    subgraph RULES["硬边界"]
        B0 -.约束.-> END
        B1 -.约束.-> M0
        B2 -.约束.-> P1
    end

    classDef input fill:#e8f4ff,stroke:#2f6f9f,color:#102030;
    classDef process fill:#eef7ed,stroke:#3f7f4f,color:#102010;
    classDef output fill:#eef8f7,stroke:#397b77,color:#102827;
    classDef gate fill:#fff8e8,stroke:#9a6a1a,color:#24160a;
    classDef warn fill:#ffecec,stroke:#b84a4a,color:#401111;

    class START,IN0,IN1 input;
    class M0,M1,M2,M3,M4,P0,P1,P2,P3 process;
    class O0,O1,O2,O3,O4,O5,D1,D3,D5,END output;
    class G0,D0,D2,D4 gate;
    class GAP0,B0,B1,B2 warn;
```

## 现实层特征口径

| 位置 | 目标宿主 | 特征类型 | 特征值 |
| --- | --- | --- | --- |
| 输入 | 当前外设 / 当前观察材料集 | 外设可用状态 | `1` |
| 输入 | 当前观察材料集 | 当前观察特征帧取得状态 | `1` |
| 输入 | 当前观察材料集 | 外设观察材料可回查状态 | `1` |
| 输入 | 当前观察材料集 / ROI | 观察范围明确状态 | `1` |
| 输出 | 当前可视范围 | 可视范围观察材料可用状态 | `1/0` |
| 输出 | 当前可视范围 | 可视范围特征归属分区状态 | `1/0` |
| 输出 | 当前可视范围 | 已归属可扫描存在数量 | `>=0` |
| 输出 | 当前可视范围 | 无归属特征值数量 | `>=0` |
| 输出 | 当前可视范围 | 指定跟踪目标数量 | `>=0` |
| 输出 | 当前可视范围 | 观察质量缺口数量 | `>=0` |

## 结论

观察方法的核心不是直接判断安全、服务或动态，而是把可视范围内的特征值组织成“已归属 / 无归属 / 指定跟踪 / 低质量”四类现实特征分区。后续扫描、识别和跟踪都读取这些特征值分区，不读取报告说明文本。
