# 相机外设综合工作流程图 v0.1

更新时间：2026-06-07

适用范围：双目相机 / D455 类感知外设。本文只整理规范口径，不证明代码已实现。

核心定案：

```text
自我给外设观察约束，外设给自我外设观察存在；
任务管理分拣消息，自我裁决入账。

正式认知链只承接：
    存在
    特征
    二次特征
    状态动态

包 / 报告 / 队列项只是线程通信容器，不能作为正式世界事实。
```

## 1. 综合总图

```mermaid
flowchart LR
    subgraph SELF["自我侧"]
        A["自我根需求 / 普通需求<br/>安全值 > 基线<br/>服务值 > 基线<br/>某存在.目标特征 比较符 目标值"]
        B["任务筹办<br/>生成临时因果视图<br/>查世界树因果"]
        C{"需要哪类外设材料?"}
        C1["外设观察存在<br/>或新一阶观察特征值"]
        C2["同一性判断<br/>是否为某个自我存在"]
        C3["已归属存在<br/>特征变化比较材料"]
        C4["指定动态目标<br/>持续跟踪比较材料"]
        E["生成目标观察约束特征组<br/>单目标存在 + 单目标特征<br/>当前值 + 值域<br/>ROI/掩码/AABB/TTL/generation"]
        J["自我线程裁决<br/>校验宿主/特征/比较符/目标值<br/>去重/合并/循环截断"]
        K["提交入口<br/>只接受存在 / 特征 / 二次特征 / 状态动态"]
        L["正式入树 / 入世界<br/>更新存在特征<br/>更新二次特征<br/>记录状态动态<br/>更新世界树因果"]
    end

    subgraph TM["任务管理工作线程"]
        F["创建外设等待项 / 订阅项<br/>绑定来源需求 / 任务 / 约束ID"]
        G["消费外设队列<br/>缓存 / 去重 / 时效检查<br/>匹配等待项 / 活跃需求"]
        H{"外设结果类型"}
        H1["外设观察存在 + 新特征"]
        H2["同一性比较材料"]
        H3["扫描变化候选 / 值域冲突"]
        H4["跟踪状态 / 预测误差 / 丢失状态"]
    end

    subgraph DEVICE["外设侧"]
        M["双目相机 / D455 独占线程<br/>独立控制域"]
        N["读取目标观察约束<br/>目标存在ID / 目标特征ID<br/>当前值 / 值域 / ROI / 掩码 / AABB"]
        O{"观察运行方式"}
        O1["陌生环境逐个观察<br/>处理待识别像素<br/>生成外设观察存在"]
        O2["已识别场景扫描<br/>只扫已归属、非背景、可比较区域"]
        O3["目标跟踪观察<br/>只盯指定存在 / 目标窗口"]
        P["写入外设消息队列 / 缓存<br/>队列项只是通信容器"]
    end

    A --> B --> C
    C --> C1 --> E
    C --> C2 --> E
    C --> C3 --> E
    C --> C4 --> E
    E --> F --> M
    M --> N --> O
    O --> O1 --> P
    O --> O2 --> P
    O --> O3 --> P
    P --> G --> H
    H --> H1 --> J
    H --> H2 --> J
    H --> H3 --> J
    H --> H4 --> J
    J --> K --> L
```

## 2. 自我侧综合流程图

```mermaid
flowchart TD
    A["自我根需求 / 普通需求<br/>安全值 > 基线<br/>服务值 > 基线<br/>或某存在.目标特征 比较符 目标值"]

    B["任务筹办<br/>生成临时因果视图<br/>查世界树因果"]

    C{"需求需要什么?"}

    C1["需要新的观察存在<br/>或新的外设观察特征值"]
    C2["需要判断外设观察存在<br/>是否为某个自我存在"]
    C3["需要发现已归属存在<br/>的特征变化"]
    C4["需要持续盯住<br/>指定动态目标"]

    D1["选择观察路径<br/>观察可以合法写入新的外设观察特征值"]
    D2["选择识别路径<br/>识别只比较已有特征<br/>输出同一性二次特征和归属状态"]
    D3["选择扫描路径<br/>扫描比较已有材料与基准<br/>输出变化候选 / 值域冲突 / 状态动态"]
    D4["选择跟踪路径<br/>跟踪比较已有特征序列<br/>输出跟踪状态 / 动态状态"]

    E["生成目标观察约束特征组<br/>单目标存在 + 单目标特征 + 当前值 + 值域 + ROI/掩码/AABB/TTL/generation"]

    F["任务管理创建外设等待项 / 订阅项<br/>绑定来源需求 / 任务 / 约束ID"]

    G["任务管理工作线程消费外设队列<br/>匹配观察/识别/扫描/跟踪结果"]

    H{"外设结果类型"}

    H1["外设观察存在 + 新特征"]
    H2["同一性比较材料"]
    H3["扫描变化候选 / 值域冲突"]
    H4["跟踪状态 / 预测误差 / 丢失状态"]

    I1["自我_承接外设观察存在"]
    I2["自我_识别外设观察存在同一性"]
    I3["自我_扫描已归属存在特征变化"]
    I4["自我_跟踪指定观察存在动态"]

    J["自我线程裁决<br/>校验目标宿主/特征/比较符/目标值<br/>去重/合并/循环截断"]

    K["提交入口<br/>只接受 存在/特征/二次特征/状态动态<br/>不接受包/报告/队列项"]

    L["正式入树 / 入世界<br/>更新存在特征<br/>更新二次特征<br/>记录状态动态<br/>更新世界树因果"]

    A --> B --> C
    C --> C1 --> D1
    C --> C2 --> D2
    C --> C3 --> D3
    C --> C4 --> D4

    D1 --> E
    D2 --> E
    D3 --> E
    D4 --> E

    E --> F --> G --> H

    H --> H1 --> I1
    H --> H2 --> I2
    H --> H3 --> I3
    H --> H4 --> I4

    I1 --> J
    I2 --> J
    I3 --> J
    I4 --> J

    J --> K --> L
```

## 3. 外设侧综合流程图

```mermaid
flowchart TD
    A["双目相机外设控制线程启动<br/>独立控制域 / 独占线程"]

    B["读取命令队列<br/>控制 / 检查 / 使用"]

    C{"命令类型"}

    C1["控制类<br/>相机.可用状态 == 可用<br/>相机.观察配置状态 == 已应用"]
    C2["检查类<br/>相机.运行可用性值 > 0<br/>相机.当前配置适用状态 == 适用"]
    C3["使用类<br/>根据等待项/约束进行观察"]

    D["读取目标观察约束特征组<br/>目标存在ID<br/>目标特征ID<br/>当前值<br/>值域/误差<br/>ROI/掩码/AABB/点集<br/>待识别像素掩码<br/>TTL/generation"]

    E{"观察运行方式"}

    E1["陌生环境逐个观察<br/>处理待识别像素<br/>生成外设观察存在"]
    E2["已识别场景扫描<br/>只扫已归属、非背景、可比较区域"]
    E3["目标跟踪观察<br/>只盯指定存在/目标窗口"]

    F1["观察输出<br/>外设观察存在<br/>新一阶观察特征值<br/>掩码/局部图/空间特征<br/>稳定复现状态"]
    F2["扫描输出<br/>二次比较特征<br/>变化候选<br/>值域冲突<br/>不裸写新一阶特征"]
    F3["跟踪输出<br/>当前区间ID<br/>预测误差<br/>跟踪状态<br/>丢失/遮挡/重捕获状态<br/>不裸写新一阶特征"]

    G["写入外设消息队列 / 缓存<br/>队列项只是通信容器"]

    H["任务管理工作线程消费<br/>命中等待项则生成回执<br/>发现缺口则生成派生需求消息<br/>无需求命中则仅缓存或丢弃"]

    A --> B --> C
    C --> C1 --> G
    C --> C2 --> G
    C --> C3 --> D --> E

    E --> E1 --> F1
    E --> E2 --> F2
    E --> E3 --> F3

    F1 --> G
    F2 --> G
    F3 --> G

    G --> H
```

## 4. 四个本能方法关系图

```mermaid
flowchart LR
    OBS["观察<br/>生成外设观察存在<br/>合法写入新观察特征值"]
    REC["识别<br/>唯一性原则<br/>判定外设观察存在是否归属某自我存在"]
    SCAN["扫描<br/>已归属存在<br/>比较已有特征是否变化"]
    TRACK["跟踪<br/>指定存在<br/>判断动态是否连续合法"]

    OBS -->|"外设观察存在 + 特征"| REC
    REC -->|"归属映射成立"| SCAN
    REC -->|"指定存在稳定归属"| TRACK

    SCAN -->|"发现未归属区域 / 值域冲突"| REC
    SCAN -->|"需要新特征值"| OBS

    TRACK -->|"丢失 / 冲突 / 遮挡"| REC
    TRACK -->|"质量不足 / 需要新值"| OBS
```

## 5. 三种相机运行方式

```text
陌生环境逐个观察：
    处理待识别像素；
    生成外设观察存在、新一阶观察特征值、掩码、局部图、空间特征和稳定复现状态。

已识别场景扫描变化：
    只扫已归属、非背景、可比较区域；
    输出变化候选、值域冲突、无变化事实和下一轮基准更新线索；
    不直接确认新存在。

目标跟踪观察：
    只盯指定存在 / 目标窗口；
    输出跟踪状态、预测误差、丢失 / 遮挡 / 重捕获状态；
    不把外设内部跟踪ID写成自我世界存在ID。
```

## 6. 待识别像素与过滤

```text
粗待识别像素 =
    全部像素 - 已归入外设观察存在的像素

净待识别像素 =
    粗待识别像素
    - 背景像素
    - 不可观测区域像素
    - 低价值区域像素
```

过滤输入：

```text
已观察完成掩码
已归属存在掩码
背景过滤掩码
待识别像素掩码
扫描候选掩码
跟踪目标掩码
空间占位过滤集合
```

## 7. 识别唯一性原则

```text
当前识别范围状态 == 已确定
外设观察存在.可复验状态 == 可复验
候选集合数量 == 1
硬冲突数量 == 0
用于唯一识别特征数量 > 0
归属映射状态 == 已建立
```

只要一个特征或多个特征组合能够在当前场景范围内确定唯一存在，就可以完成识别。

```text
候选数量 > 1:
    继续使用已有特征缩小候选。

已有特征不足以唯一:
    进入观察补完。

唯一但硬冲突 > 0:
    进入观察复验。
```

## 8. 基准规则

```text
初始基准
当前有效基准
滚动基准
```

基准更新条件：

```text
匹配状态 == 匹配
丢失状态 == 未丢失
关键特征误差值 < 最大允许误差
连续稳定帧数 > 最小稳定帧数
非法区间跳变次数 == 0
质量状态 == 可用 / 降级可用
```

基准冻结条件：

```text
掩码重合率 < 最低掩码重合率
轮廓偏差 > 最大允许轮廓偏差
当前值超出特征值域
目标区域待识别像素增加
疑似拆分 / 合并 / 遮挡
```

## 9. 启动相机外设运行的需求因果

```mermaid
flowchart TD
    A["父需求目标"]
    B["临时因果视图"]
    C["选择观察 / 识别 / 扫描 / 跟踪路径"]
    D["发现外设材料前置缺口"]
    E["双目相机线程.运行状态 == 运行中"]
    F["双目相机.可用状态 == 可用"]
    G["双目相机.运行可用性值 > 0"]
    H["外设等待项.状态 == 已建立"]
    I["目标观察约束特征组.状态 == 可用<br/>扫描/跟踪需要约束时"]
    J["外设产生对应观察材料"]
    K["任务管理消费"]
    L["自我方法输出<br/>存在 / 特征 / 二次特征 / 状态动态"]
    M["提交入口裁决"]
    N["父需求目标回查"]

    A --> B --> C --> D --> E --> F --> G --> H --> I --> J --> K --> L --> M --> N
```

## 10. 合法来源规则

```text
所有特征值必须有合法来源。
所有阈值必须有来源。
所有枚举状态必须有量化判定规则。
所有状态动态必须有输入特征、输出特征、来源方法和来源需求。
```

示例：

```text
外设观察存在.匹配状态 == 匹配
```

必须由：

```text
匹配评分 > 最低匹配评分
硬冲突数量 == 0
连续匹配帧数 > 最小连续匹配帧数
```

推出。

## 11. 实时性和舍弃策略

可以舍弃：

```text
过期扫描材料
重复扫描材料
无需求命中的材料
低优先级局部细分
中间跟踪帧
低优先级配置学习样本
```

不能舍弃：

```text
安全事件
活跃任务回执
状态跃迁
跟踪目标丢失
外设错误
影响父需求判断的缺口事实
```

## 12. 最终定案

```text
自我决定看什么、为什么看、结果是否入账；
相机负责按约束稳定给出外设观察存在和可比较特征；
观察写新值，识别定唯一，扫描记变化，跟踪守动态。
```
