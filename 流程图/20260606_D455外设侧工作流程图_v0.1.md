# D455 外设侧工作流程图 v0.1

更新时间：2026-06-06

## 用途

本图只画 D455 外设侧：

```text
外设等待项 / 启动请求
-> D455 外设线程生命周期
-> RealSense 适配器打开与采集
-> 外设内部材料加工
-> 逐簇识别 / 扫描变化 / 目标跟踪报告生成
-> 外设观察报告队列与 d455:// 材料页
```

本图不画自我侧方法执行细节；自我侧只作为等待项来源和报告消费者边界出现。

## 依据

- `规范/双目相机外设独占观察线程规范20260527.md`
- `规范/双目相机外设功能能力与实现途径总结20260527.md`
- `规范/外设独立控制线程与消息承接边界规范20260527.md`
- `双目相机本能适配器.h`
- `双目相机本能适配器.cpp`
- `外设线程_D455深度相机.ixx`
- `外设观察报告队列.ixx`

## 外设线程主循环

```mermaid
flowchart TB
    A["任务管理提交外设观察等待项<br/>目标外设=双目相机/D455深度相机"]
    B{"全局 D455 线程<br/>是否运行中?"}
    C["启动外设线程_D455深度相机<br/>创建线程共享状态"]
    D["D455_线程主循环<br/>生命周期=启动中"]
    E["清空跟踪稳定缓存<br/>清空跨帧观察簇缓存"]
    F{"配置要求启动时打开相机?"}
    G["双目相机本能适配器::打开<br/>检查 realsense2.dll / lz4.dll<br/>打开 D455"]
    H{"打开成功?"}
    I["记录打开失败样本<br/>错误日志<br/>生命周期=故障"]
    J["生命周期=运行中"]
    K{"停止请求或达到目标样本数?"}
    L["双目相机本能适配器::采集一帧<br/>内部尝试 3 帧静态融合"]
    M{"采集成功?"}
    N["生成失败/不可用样本<br/>更新线程稳定性结果"]
    O["读取上一逐簇识别报告<br/>作为扫描变化基准"]
    P["由调用结果生成逐簇识别报告<br/>写帧句柄 / 候选簇 / 质量字段"]
    Q["更新跨帧簇缓存<br/>跨帧ID / 匹配 / 新建 / 稳定 / 丢失"]
    R["提交逐簇识别报告到队列<br/>提交 D455 短期观察材料页"]
    S["读取扫描等待项集合"]
    T{"扫描等待项有目标/基准?"}
    U["跳过扫描报告<br/>日志记录缺目标或基准"]
    V["由逐簇报告 + 上一报告<br/>生成扫描变化报告"]
    W["提交扫描变化报告到队列<br/>写扫描样本卡日志"]
    X["读取跟踪等待项集合"]
    Y{"跟踪等待项有目标种子<br/>且本帧未生成主动跟踪报告?"}
    Z["跳过跟踪报告<br/>日志记录缺种子或本帧已有跟踪"]
    AA["解析目标种子簇ID<br/>命中簇则更新跟踪缓存<br/>未命中则记录丢失/重捕获候选"]
    AB["提交跟踪报告到队列"]
    AC["生成 D455 观察样本摘要<br/>写入线程样本集<br/>评估稳定性"]
    AD["等待采样间隔"]
    AE{"停止时释放相机?"}
    AF["双目相机本能适配器::释放"]
    AG["生命周期=已停止<br/>写停止日志"]
    AH["异常捕获<br/>写错误日志<br/>生命周期=故障"]

    A --> B
    B -->|否| C --> D --> E --> F
    B -->|是| J
    F -->|是| G --> H
    F -->|否| J
    H -->|否| I
    H -->|是| J
    J --> K
    K -->|是| AE
    K -->|否| L --> M
    M -->|否| N --> AC
    M -->|是| O --> P --> Q --> R --> S
    S --> T
    T -->|否| U --> X
    T -->|是| V --> W --> X
    X --> Y
    Y -->|否| Z --> AC
    Y -->|是| AA --> AB --> AC
    AC --> AD --> K
    AE -->|是| AF --> AG
    AE -->|否| AG
    D -.异常.-> AH
    L -.异常.-> AH

    classDef input fill:#eef7ed,stroke:#3f7f4f,color:#102010;
    classDef thread fill:#e8f4ff,stroke:#2f6f9f,color:#102030;
    classDef adapter fill:#fff3dc,stroke:#a0671e,color:#24160a;
    classDef report fill:#f1edff,stroke:#6c56a4,color:#1d1730;
    classDef gate fill:#fff8e8,stroke:#9a6a1a,color:#24160a;
    classDef err fill:#ffecec,stroke:#b84a4a,color:#401111;

    class A input;
    class B,F,H,K,M,T,Y,AE gate;
    class C,D,E,J,AC,AD,AG thread;
    class G,L,AF adapter;
    class O,P,Q,R,S,U,V,W,X,Z,AA,AB report;
    class I,AH err;
```

## 外设侧材料与报告流

```mermaid
flowchart LR
    A["RealSense / D455 原始帧<br/>深度帧 / 彩色帧 / 时间戳"]
    B["双目相机本能适配器::调用结果<br/>3 帧静态融合 / 深度有效率 / 空间坐标有效率 / 帧质量评分"]
    C["空间候选摘要集合<br/>像素数量 / 中心XYZ / AABB / 投影框 / 轮廓置信度"]
    D["D455 逐簇识别报告<br/>观察运行模式=陌生环境逐簇识别"]
    E["d455:// 材料句柄<br/>raw frame / depth / color / aligned / cluster mask / ROI / point set / crop"]
    F["跨帧簇缓存<br/>跨帧簇ID / 连续命中 / 复现离散度"]
    G["扫描变化报告<br/>新增 / 消失 / 变化 / 高风险未知<br/>缺基准时只建首帧基准"]
    H["跟踪稳定缓存<br/>连续成功 / 失败次数 / 最近中心"]
    I["跟踪报告<br/>目标种子 / 跟踪状态 / 丢失状态 / 重捕获候选"]
    J["外设观察报告队列<br/>等待项集合 / 报告队列 / 材料页"]
    K["任务管理按等待项匹配报告<br/>外设侧输出边界"]

    A --> B --> C --> D
    D --> E
    D --> F
    F --> D
    D --> G
    D --> H --> I
    D --> J
    G --> J
    I --> J
    E --> J
    J --> K

    classDef adapter fill:#fff3dc,stroke:#a0671e,color:#24160a;
    classDef report fill:#f1edff,stroke:#6c56a4,color:#1d1730;
    classDef queue fill:#eef8f7,stroke:#397b77,color:#102827;
    class A,B,C adapter;
    class D,E,F,G,H,I report;
    class J,K queue;
```

## 三类报告生成口径

| 报告 | 生成入口 | 外设侧输入 | 外设侧输出 | 失败 / 跳过条件 |
| --- | --- | --- | --- | --- |
| 逐簇识别报告 | `D455_从调用结果生成逐簇识别报告` | `双目相机本能适配器::调用结果`、空间候选列表、质量字段 | 帧句柄、空间候选句柄、像素归属材料、观察像素簇集合、质量和缺口摘要 | 采集失败时不生成正常报告，只生成失败样本和线程稳定性结果 |
| 扫描变化报告 | `D455_从逐簇识别报告生成扫描变化报告` | 当前逐簇识别报告、上一逐簇识别报告、扫描等待项 | 新增 / 消失 / 变化簇数量，高风险未知区域数量，差异摘要 | 等待项缺目标 / 基准则跳过；缺上一报告时只建立首帧基准 |
| 跟踪报告 | `D455_从逐簇识别报告生成跟踪报告` | 当前逐簇识别报告、跟踪等待项目标种子、跟踪稳定缓存 | 跟踪状态、丢失状态、连续成功、失败次数、重捕获候选数量 | 等待项缺目标种子则跳过；种子不可解析或未命中当前簇则记丢失 |

## 外设侧不负责

```text
1. 不直接写自我需求树。
2. 不直接写世界树真值。
3. 不直接确认已确认观察存在。
4. 不直接生成方法动作动态。
5. 不直接结算安全值 / 服务值。
6. 不把内部采集、滤波、聚簇、跨帧匹配、跟踪缓存登记成自我层本能方法。
7. 不把逐簇识别报告、扫描变化报告或跟踪报告当作需求满足结论。
```

## 当前工程锚点

```text
适配器层：
    双目相机本能适配器::打开
    双目相机本能适配器::采集一帧
    双目相机本能适配器::释放

线程层：
    外设线程_D455深度相机类::启动
    D455_线程主循环
    D455_评估稳定性

报告层：
    D455_从调用结果生成逐簇识别报告
    D455_从逐簇识别报告生成扫描变化报告
    D455_从逐簇识别报告生成跟踪报告

数据面：
    结构_外设观察等待项
    结构_外设观察报告队列项
    提交外设观察报告
    提交D455短期观察材料页
    读取外设观察等待项集合
```

## 定案

```text
D455 外设侧只负责：
    控制硬件
    取得观察材料
    做外设内部工程加工
    输出三类可承接报告
    保留可回查材料页和日志证据

正式认知、世界入账、需求满足和价值结算都不在外设侧完成。
```
