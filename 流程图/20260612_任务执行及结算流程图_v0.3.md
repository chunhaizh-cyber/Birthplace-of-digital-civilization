# 任务执行及结算流程图 v0.3

更新时间：2026-06-13

修订对象：`流程图/20260612_任务执行及结算流程图_v0.2.md`

## 依据

```text
用户本轮修订意见：编码前先把 v0.2 流程图补成闭环验证接口契约。
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
规范/自我动作验证闭环规范20260612.md
规范/任务筹办与执行边界规范20260512.md
规范/自我内部循环实现规范20260512.md
规范/安全服务值结算规范20260518.md
流程图/20260612_任务执行及结算流程图_v0.2.md
计划/20260612_动作验证闭环工程落地实施计划_v0.1.md
```

## 说明

本版只做轻量接口契约修订，不表示代码已经实现闭环验证或结算重构。新增三个接口/边界节点：`VX0` 任务执行闭环验证、`VC0` 结算闭环验证、`B8` 执行报告不等于实际事实。后续实现顺序固定为：先落地闭环验证最小竖切片，再逐段接入执行、结算域分类和核心价值结算硬闸门。

## 与 v0.2 的主要差异

```text
1. 新增 VX0：插入 WT12 -> WT13 之间，生成动作验证报告。
2. 新增 VC0：插入 CL0 -> CL1 之间，生成结算闭环验证报告。
3. 新增 B8：执行报告 != 实际事实，完成判断和结算判断必须读取闭环验证报告。
4. VAL1 核心价值结算硬闸门增加对 VC0 报告的依赖。
5. 本图是接口契约图，不实施整张 v0.2 流程图。
```

## 报告接口字段

```text
动作验证报告最小字段：
    动作事务ID
    来源任务
    来源方法
    目标值
    预测值
    实际值
    执行报告值
    目标偏差
    预测偏差
    报告偏差
    回读置信度
    验证状态
    事实提交等级
    纠偏建议

结算闭环验证最小读取：
    动作验证报告
    来源需求
    任务类型 / 任务域
    治理标签
    结果域
    完成度
    价值增量
    因果证据
    是否允许核心价值结算
```

## 流程图

```mermaid
flowchart TD
    Start["任务管理界面线程派发工作项"] --> WT7["调用任务执行普通函数<br/>任务执行不是本能方法"]
    WT7 --> WT11["WT11 执行方法<br/>执行桥.执行方法_同步等待"]
    WT11 --> WT12["WT12 读取本能方法运行闭环视图<br/>只得到执行报告态 / 运行回执"]
    WT12 --> VX0["新增 VX0：任务执行闭环验证<br/>读取目标态G / 预测态P / 执行报告态E / 实际回读态R<br/>生成动作验证报告V"]
    VX0 --> VREP["动作验证报告V<br/>事务ID / 来源任务 / 来源方法<br/>目标值 / 预测值 / 实际值 / 执行报告值<br/>目标偏差 / 预测偏差 / 报告偏差<br/>回读置信度 / 验证状态 / 事实提交等级 / 纠偏建议"]
    VREP --> WT13["WT13 验证实际结果状态与来源需求目标<br/>由闭环验证报告驱动"]
    WT13 --> WT14["WT14 计算完成度<br/>按实际回读态R与目标态G比较"]
    WT14 --> WT15{"WT15 目标达成?"}
    WT15 -- "实际满足目标" --> WT16["推导任务状态=完成<br/>仍需提交任务状态变化"]
    WT15 -- "回读不足" --> WAIT["等待 / 补观察 / 补证据"]
    WT15 -- "实际与目标冲突" --> REPLAN["重筹办 / 纠偏 / 补条件"]
    WT16 --> UP0["结果状态上行消息<br/>携带动作验证报告引用"]

    UP0 --> UP5["私有_落任务管理结果到自我内部世界"]
    UP5 --> CL0["CL0 读取任务结算域材料<br/>任务类型 / 来源需求 / 治理标签 / 结果域 / 完成度 / 价值增量"]
    CL0 --> VC0["新增 VC0：结算闭环验证<br/>读取动作验证报告V、来源需求、完成度、因果证据和价值增量<br/>生成结算闭环验证报告"]
    VC0 --> CL1{"CL1 任务结算域?"}

    CL1 -- "普通价值任务 / 普通叶子任务" --> SET0["普通满足反馈<br/>按验证后的实际结果满足来源需求"]
    SET0 --> VAL0["处理普通任务价值反馈"]
    VAL0 --> VAL1{"VAL1 核心价值结算硬闸门<br/>必须读取 VC0 报告"}
    VAL1 -- "闭环验证通过 + 完成度合法 + 增量合法 + 因果证据成立" --> VAL2["计算安全 / 服务增量<br/>I64_MAX 哨兵不得入账"]
    VAL1 -- "任一条件不通过" --> VAL_STOP["阻断核心值结算<br/>记录待补证据或候选"]
    VAL2 --> VAL3["提交任务已结算状态"]
    VAL3 --> VAL5["调用结算叶子任务价值<br/>仅普通价值任务允许"]

    CL1 -- "内部治理任务" --> GOV0["治理闭账<br/>确认治理变化已落账"]
    GOV0 --> GOV1["提交任务已结算状态<br/>不调用结算叶子任务价值"]
    GOV1 --> PARENT["唤醒治理父任务 / 解锁等待项"]

    CL1 -- "证据搜索 / 特殊无价值任务" --> EVD0["证据闭账<br/>写证据、风险影响或缺口回执"]
    EVD0 --> EVD1["提交任务已结算状态<br/>不写核心安全值 / 服务值"]
    EVD1 --> PARENT

    CL1 -- "仅闭账" --> CLOSE0["仅闭账<br/>完成但缺普通核心价值条件"]
    CLOSE0 --> CLOSE1["提交任务已结算状态<br/>不进入 VAL0 / VAL5"]
    CLOSE1 --> PARENT

    IF5["任务状态动作动态"] -. "仅作为完成 / 已结算被动因证据" .-> VX0
    IF5 -. "仅作为结算证据之一" .-> VC0

    subgraph DiffNotes["v0.2 差异标记"]
        D1["新增：WT12 -> VX0 -> WT13"]
        D2["新增：CL0 -> VC0 -> CL1"]
        D3["新增：B8 执行报告 != 实际事实"]
        D4["调整：VAL1 必须读取 VC0 报告"]
    end
    D1 -. "接口插入" .-> VX0
    D2 -. "接口插入" .-> VC0
    D4 -. "硬闸门依赖" .-> VAL1

    subgraph Boundaries["关键边界"]
        B1["任务筹办 / 任务执行是生命周期普通函数<br/>不进方法候选集，不生成方法动作动态"]
        B2["线程不是动作来源<br/>任务状态变化必须走提交任务状态变化"]
        B3["完成 != 已结算"]
        B7["普通价值结算 != 内部治理闭账"]
        B8["新增 B8：执行报告 != 实际事实<br/>完成判断、结算域分类和核心价值闸门<br/>必须读取闭环验证报告"]
    end
    B1 --> WT7
    B2 --> IF5
    B3 --> CL1
    B7 --> GOV0
    B8 --> VX0
    B8 --> VC0

    classDef added fill:#e6f4ea,stroke:#1a7f37,color:#1f2328,stroke-width:2px;
    classDef changed fill:#fff8c5,stroke:#bf8700,color:#1f2328,stroke-width:2px;
    classDef evidence fill:#ddf4ff,stroke:#0969da,color:#1f2328,stroke-width:2px,stroke-dasharray:5 3;
    classDef boundary fill:#f6f8fa,stroke:#57606a,color:#1f2328;

    class VX0,VC0,B8,VREP added;
    class WT13,WT14,WT15,CL1,VAL1 changed;
    class IF5 evidence;
    class B1,B2,B3,B7 boundary;
```

## 关键边界

```text
1. 本图只把 v0.2 补成接口契约，不代表执行闭环验证、结算域分类或核心价值结算已实现。
2. 执行报告态 E 不能冒充实际回读态 R；任务完成判断必须读取 VX0 动作验证报告。
3. WT13 / WT14 / WT15 后续应由闭环验证报告驱动；方法返回成功不等于目标达成。
4. CL0 / CL1 后续应读取 VC0 结算闭环验证报告；结算域分类不能只靠任务名或执行报告。
5. VAL1 核心价值结算硬闸门必须要求闭环验证通过、完成度合法、价值增量合法和因果证据成立。
6. 内部治理任务、证据任务和仅闭账任务可以进入已结算闭账，但不得调用结算叶子任务价值，不写核心安全值 / 服务值。
7. 后续编码顺序：先实现动作验证报告最小能力，再接 WT11-WT15，再接 CL0/VC0/CL1，最后接 VAL1 和 GOV/EVD/CLOSE。
```
