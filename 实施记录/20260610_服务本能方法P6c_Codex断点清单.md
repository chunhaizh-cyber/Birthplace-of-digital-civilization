# 服务本能方法 P6c Codex断点清单

日期：2026-06-10

## 当前切片

```text
服务本能方法实现计划 P6c：
    服务值结算公式与 D2 结算动作动态 helper。
```

## 依据文件

```text
AGENTS.md
规范/规范目录.md
规范/方法动作动态硬规则规范20260512.md
规范/任务筹办与执行边界规范20260512.md
计划/计划索引.md
实施记录/20260610_服务本能方法实现方案记录.md
实施记录/20260610_服务本能方法P6b_Codex断点清单.md
```

## 允许修改范围

```text
自我动作实现.服务模块.ixx
实施记录/20260610_服务本能方法P6c_Codex断点清单.md
计划/计划索引.md
```

## 禁止修改范围

```text
任务主信息类
需求主信息类
特征值主信息类
任务筹办 / 任务执行 本能方法化
服务事件E独立虚拟存在
无界服务值加法
绕过 G0 已确认和候选可结算直接入账
直接修改旧 自我_结算叶子任务价值 入口
```

## 进入时工作区状态

```text
P0/P1 已提交：8c3673a
P2 已提交：aa2ca33
P3 已提交：1dd4662
P4a 已提交：7d59ece
P4b 已提交：86641b4
P5a 已提交：6f98949
P5b 已提交：a6b2699
P5c 已提交：a88ab30
P5d 已提交：398e6e2
P6a 已提交：37a3a20
P6b 已提交：0ab1a70
存在提交前已有无关修改：
    AGENTS.md
    任务模块.管理工作线程.ixx
    控制面板.html
    实施记录/20260610_观察子任务完成推进_Codex断点清单.md
    消息中间件/

本切片不得混入上述文件。
```

## 预期结果

```text
1. 服务模块提供服务层级系数、lambda 候选和饱和入账计算 helper。
2. 服务值入账只使用：
   DeltaS = (Smax - S) * lambda(e)
3. helper 要求 G0 已确认且候选记录可结算；否则不给出可结算结果。
4. 提供 D2 服务值结算动作动态构造 helper：
   只在调用方传入明确结算方法、值宿主、服务值特征、前后值和来源证据时创建动态。
5. 本切片不主动写自我服务值，不修改旧结算入口。
6. Debug x64 构建通过。
```

## 已运行命令

```text
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\鱼巢_build_20260610_服务模块P6c.log
```

## 关键退出码

```text
Debug x64 构建：ExitCode=0
关键错误检索：未检出 error C / fatal error / MSB / 错误。
```

## 已完成修改

```text
1. 新增服务值结算候选字段：
   lambda候选、lambda使用、标准服务单位、非重复系数、安全调节、服务值上限、
   结算前服务值、服务值入账增量、结算后服务值、服务值结算动作动态、来源治理判断。
2. 新增层级系数 helper：
   L1=0.25，L2=0.50，L3=1.00，L4=2.00，L5=4.00。
3. 新增 lambda 候选计算 helper：
   q = u * K_L * 有效性 * 证据强度 * 非重复系数 * 安全调节；
   lambda = q / Smax。
4. 新增饱和入账计算 helper：
   DeltaS = (Smax - S) * lambda(e)；
   只在 G0 已确认且候选记录可结算时返回可结算结果。
5. 新增服务值结算候选结果回写 helper。
6. 新增 D2 服务值结算动作动态构造 helper：
   只根据调用方传入的结算方法、结算场景、值宿主、服务值特征、前后服务值和 G0/D0/D1 来源证据创建动态；
   本 helper 不主动修改自我服务值，不修改旧结算入口。
```

## 当前判断

```text
P6c 已闭合；服务值饱和入账公式与 D2 构造 helper 可构建。
下一步进入 P7a：父子任务组合汇总 helper。
```
