# 20260625 P13b 项字段键迁移 Codex断点清单

## 0. 恢复摘要

当前切片：

```text
P13b：迁移 `线程生命周期消息模块.ixx::私有_项字段键`。
```

依据：

```text
计划/计划索引.md：当前主动队列为 P2/P13b。
计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md：B 类低风险函数逐个迁移到 全局共享函数类.ixx。
实施记录/20260624_P13_函数自定义主信息分类候选表.tsv：`私有_项字段键` 为 B 类低风险待复核候选。
全局共享函数类.ixx 文件头规则：无领域自定义主信息 / 领域类型参与的通用函数可进入本模块。
```

当前判断：

```text
`私有_项字段键` 只生成文本键值缓存字段名，格式为 `项.<索引>.<键>`。
它不读取 / 写入项目结构，不依赖线程生命周期枚举、结构体或任务 / 需求 / 方法等领域类型。
`线程生命周期消息模块.ixx` 已 import 全局共享函数类，可直接复用全局工具，不引入 import 环。
```

预测验证：

```text
1. 迁移后源码和函数说明文档不再保留 `私有_项字段键` 定义。
2. `索引项字段键` 只出现在 `全局共享函数类.ixx` 定义和线程生命周期调用点。
3. 函数说明文档生成脚本成功。
4. Debug x64 构建成功。
```

禁止宣称：

```text
不宣称 P13 完成。
不宣称函数去重全部完成。
不宣称自我苏醒完成或初步成熟完成。
```

## 1. 执行结果

```text
1. 在 `全局共享函数类.ixx` 新增 `索引项字段键(std::size_t, std::string_view)`。
2. 删除 `线程生命周期消息模块.ixx::私有_项字段键`。
3. `线程生命周期消息模块.ixx` 的缓存字段读取、写入和 contains 判断统一调用全局 `索引项字段键`。
4. `线程生命周期消息模块.ixx` 缺少文件头部规则注释模块，本切片已补最小模块。
```

## 2. 验证证据

```text
源码复扫：
    `私有_项字段键` 未检出。
    `索引项字段键` 仅在 `全局共享函数类.ixx` 定义和 `线程生命周期消息模块.ixx` 4 个调用点出现。

函数说明文档：
    python .\tools\生成函数功能说明文档.py
    结果：成功；源码文件 127；函数条目 7213。
    `说明书/函数功能说明文档.md` 中 `私有_项字段键` 未检出。
    `说明书/函数功能说明文档.md` 中 `索引项字段键` 定义已登记。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo
    结果：成功；0 警告；0 错误。
```

## 3. 当前判断

```text
本切片闭合。
该迁移不改变线程生命周期缓存文件格式，只把字段键生成函数收束到全局共享工具。
不涉及任务、需求、方法、世界事实或动作动态写权变化。
```
