# P10f-an 语素 SQL 行文本字段复核 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-an `语素类.cpp::结构_语素SQL行` 文本字段边界复核。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_ado_query_result.tsv`

复核字段：

- `路径`
- `节点类型`
- `词面`
- `显示文本`
- `词性文本`
- `信息入口类型文本`
- `基础信息类型文本`

复核证据：

- `结构_语素SQL行` 是 `语素类::重写语素SQL投影(...)` 使用的局部 SQL 投影行结构。
- `私有_填充语素SQL主信息(...)` 填充显示文本，同时保留结构化字段：`有词性 / 词性值 / 有信息入口类型 / 信息入口类型值 / 有基础信息类型 / 基础信息类型值 / 对应基础信息主键 / 已绑定基础信息`。
- `私有_构造语素SQL重写脚本(...)` 将这些字段写入 `fishnest.lexeme_node / fishnest.lexeme_main_info`，供 `fishnest.v_current_lexeme_nodes` 查询。
- `控制面板类.cpp` 读取 `word_text / entry_type_text / mapped_main_type_text / bound_basic_key` 用于语素树 SQL 表格、子链 JSON 和页面展示。
- `需求类.cpp` 在需求 SQL 展示视图中 join `fishnest.v_current_lexeme_nodes`，只用语素文本列生成目标特征显示名 / 类型名。
- 语素身份判断、信息入口路由、主信息类型选择和只读查找仍读取语素入口节点、词节点、词性枚举、信息入口类型枚举、主信息类型值和基础信息主键，不读取 SQL 投影文本列。

处理结论：

- 本切片不删除 `结构_语素SQL行` 的文本字段。
- 这些字段登记为语素树 SQL / 控制面板投影文本边界；不得把其中的显示文本作为语素身份判断、信息入口路由、主信息类型选择或机器查找依据。
- 本切片无代码改动，未重新构建。

下一步：

- 返回 P10f 最新复扫清单，跳过已闭合或已登记的 SQL / 控制面板 / 日志显示项后，继续处理剩余非主键类 string 字段。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
