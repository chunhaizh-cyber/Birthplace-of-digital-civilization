# 20260607 运行速度 S1 任务巡检耗时日志 Codex 断点清单

## 当前切片

```text
计划：
    计划/20260607_运行速度常驻路径优先优化计划_v0.1.md

切片：
    S1 任务巡检细分耗时日志。

目标：
    在任务管理后台巡检路径增加阶段耗时日志，定位 13 秒级单轮巡检慢点。
```

## 依据文件

```text
AGENTS.md
规范/规范目录.md
规范/自我线程与任务管理线程权责边界规范20260512.md
规范/任务筹办与执行边界规范20260512.md
规范/任务筹办父方法与协作子方法链规范20260513.md
计划/计划索引.md
计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
实施记录/20260607_运行速度架构缺陷检测报告.md
```

## 允许修改文件

```text
任务模块.管理界面线程.impl.cpp
实施记录/20260607_运行速度S1任务巡检耗时日志_Codex断点清单.md
```

## 禁止修改范围

```text
不改变任务入队语义。
不改变任务裁决规则。
不改变等待工作项登记规则。
不改变任务状态推进规则。
不新增伪任务、伪样本、伪回归入口。
不把任务筹办 / 任务执行恢复为本能方法。
不把摘要中间态写成业务通过。
```

## 执行前预测

```text
1. Debug x64 应能构建通过。
2. 真实运行摘要命令应退出码 0。
3. 日志中应出现任务状态巡检阶段耗时锚点。
4. 新增日志只暴露阶段耗时、最慢任务和最慢阶段，不改变原有巡检计数语义。
5. 如果短摘要命令没有自然触发慢巡检，本切片只证明埋点存在，不伪造 13 秒样本。
```

## 当前已运行命令

```text
已读取 AGENTS.md、规范目录、任务管理相关规范、计划索引、运行速度计划。
已检查 任务模块.管理界面线程.impl.cpp 当前存在大量未提交差异。
已定位 S1 插入点为 私有_巡检全部任务状态并入队。
```

## 实际验证证据

```text
构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0
    警告=1
    警告内容=LNK4075 忽略 /EDITANDCONTINUE，由 /INCREMENTAL:NO 规范触发
    错误=0

短摘要真实运行：
    .\x64\Debug\鱼巢.exe --demand-tree-growth-summary
    退出码=0
    耗时约 9677ms
    输出显示 快照状态=即时读取，中间态=否，请求队列=0，当前等待工作项=0，待筹办=0，待执行=0
    本路径未自然触发新的任务状态巡检锚点

自检真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=1000
    退出码=0
    耗时约 17558ms
    本路径自然触发任务管理巡检

新增日志锚点：
    任务管理界面线程/任务状态巡检/阶段耗时
    任务管理界面线程/任务状态巡检/单任务最慢阶段

日志样本：
    2026-06-07 12:13:45.501 任务管理界面线程/任务状态巡检
        扫描任务=4
        可推进=1
        入队任务=0
        等待=3
        已有工作项=1
        不可推进=3
        耗时微秒=3827

    2026-06-07 12:13:45.501 任务管理界面线程/任务状态巡检/阶段耗时
        总耗时微秒=3827
        任务头收集=598
        任务状态读取=1744
        单任务裁决=1
        已有工作项检查=2
        状态快照记录=1451
        汇总快照锁内=1

    2026-06-07 12:13:45.501 任务管理界面线程/任务状态巡检/单任务最慢阶段
        最慢阶段=任务头收集
        最慢阶段耗时微秒=598
        最慢任务=2
        最慢任务状态=等待中
        最慢任务总耗时微秒=956
        超阈值任务数=0
```

## 当前判断

```text
本切片只做日志埋点。
目标文件已有用户或既有工作区改动，后续提交必须只包含本次新增埋点和本断点清单，不能回滚或混入无关改动。
S1 埋点已构建通过，并在真实自检路径自然触发。
本次样本不是 13 秒慢样本，不宣称任务巡检性能问题已修复。
```

## 下一步

```text
1. 提交本次 S1 埋点与断点清单。
2. 后续等待真实常驻运行样本复现慢巡检。
3. 若最慢阶段稳定指向重复扫描或已有工作项检查，再进入 S2 脏集 / 事件驱动。
4. 若最慢阶段指向日志或状态快照，再优先进入 S3 日志分级和热路径降噪。
```

## 继续执行记录 2026-06-07

```text
用户要求：
    继续，直到计划完成或遇到问题。

当前计划门槛：
    S2 脏集 / 事件驱动的前置条件是 S1 已定位稳定最慢阶段。

继续执行预测：
    使用现有真实入口 --self-check-log 补采样；
    应自然触发任务管理巡检；
    若阶段耗时样本仍未复现 13 秒级慢巡检或最慢阶段不稳定，则不得直接实施 S2；
    若最慢阶段稳定指向重复扫描、已有工作项检查、状态快照或日志，再选择对应下一切片。

禁止：
    不新增伪任务；
    不新增回归入口；
    不用手工调用私有巡检函数伪造样本；
    不绕过任务裁决规则进入脏集改造。
```

## 继续采样证据 2026-06-07 12:22

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000

结果：
    退出码=0
    耗时约 34087ms

关键样本：
    12:22:05.900 扫描任务=1  总耗时=509391us 最慢阶段=等待工作项登记 耗时=502968us 最慢任务=1
    12:22:07.214 扫描任务=3  总耗时=397040us 最慢阶段=等待工作项登记 耗时=392858us 最慢任务=2
    12:22:10.208 扫描任务=5  总耗时=277436us 最慢阶段=等待工作项登记 耗时=270369us 最慢任务=5
    12:22:16.162 扫描任务=11 总耗时=538962us 最慢阶段=等待工作项登记 耗时=525660us 最慢任务=B
    12:22:21.244 扫描任务=13 总耗时=18296us  无入队，主要耗时为任务状态读取 / 状态快照 / 任务头收集
    12:22:26.471 扫描任务=13 总耗时=13308us  无入队，主要耗时为任务状态读取 / 状态快照 / 任务头收集

判断：
    未复现 13 秒级慢巡检；
    当前样本稳定指向入队轮次的“等待工作项登记”；
    无入队巡检轮次约 13ms-18ms，全量扫描本身在该样本中不是主瓶颈；
    不应直接进入 S2 全量脏集 / 事件驱动改造；
    下一闭合切片改为 S1b：等待工作项登记内部阶段耗时日志。
```

## S1b 验证前预测 2026-06-07

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000

预测：
    构建应通过；
    真实自检路径应自然出现任务入队；
    慢登记轮次应输出 任务管理界面线程/等待工作项登记/阶段耗时；
    若本轮没有入队或登记未超过阈值，则可能没有该锚点，不能视为业务失败；
    新锚点只解释等待工作项登记内部耗时，不改变任务裁决或入队语义。
```

## S1b 验证结果 2026-06-07 12:27

```text
构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0
    警告=既有 LNK4075

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    退出码=0
    耗时约 32143ms

关键样本：
    12:27:28.119 巡检总耗时=406965us 等待工作项登记=403637us
    12:27:29.422 巡检总耗时=381742us 等待工作项登记=377835us
    12:27:32.481 巡检总耗时=268336us 等待工作项登记=261112us
    12:27:37.744 巡检总耗时=367463us 等待工作项登记=354994us

S1b 新锚点：
    12:27:43.614 登记总耗时=564392us 最慢阶段=入队前状态提交 563097us
    12:27:45.853 登记总耗时=1236414us 最慢阶段=入队前状态提交 1235926us
    12:27:47.288 登记总耗时=866883us 最慢阶段=入队前状态提交 865588us

判断：
    队列重排、队列快照刷新、队列头诊断日志本轮都不是主要耗时；
    慢点继续收窄到 私有_提交界面任务状态变化；
    下一闭合切片改为 S1c：入队前状态提交内部阶段耗时日志。
```

## S1c 验证前预测 2026-06-07

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000

预测：
    真实自检路径应继续自然触发任务入队；
    慢提交应输出 任务管理界面线程/任务状态提交/阶段耗时；
    若入队前状态提交慢来自完整状态变更，最慢阶段应落在准备本能调用或执行本能方法；
    若慢来自重复同状态提交，最慢阶段应落在同状态快照或生命周期锁等待；
    本切片仍只生成诊断日志，不改变任务状态推进规则。
```

## S1c 验证结果与最小优化预测 2026-06-07 12:32

```text
构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0
    警告=既有 LNK4075

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    退出码=0
    耗时约 31865ms

关键样本：
    12:32:41.069 任务=7 完整提交 总=938578us 生命周期锁等待=434515us 执行本能方法=382200us
    12:32:44.779 任务=9 完整提交 总=458370us 执行本能方法=358885us
    12:32:45.403 任务=B 同状态快照 总=567109us 生命周期锁等待=565105us
    12:32:47.532 任务=B 同状态快照 总=1119844us 生命周期锁等待=1118156us

判断：
    状态提交慢点不是队列重排或日志；
    完整状态变更主要耗在执行本能方法，并被私有_任务生命周期调用互斥串行化；
    同状态重排并不执行本能方法，但会等待生命周期互斥，形成 0.5s-1.1s 放大；
    直接缩短生命周期互斥范围涉及 worker 派发、完成归并、任务壳写入的一致性契约，当前不贸然修改；
    本轮可执行的最小优化：旧工作项重排时跳过入队前状态提交，只在初次入队和真实状态变化时保留提交。

最小优化预测：
    同任务在途重排不再进入入队前状态提交；
    初次入队仍保留完整状态提交和动作动态；
    运行日志中同状态快照卡生命周期锁的样本应显著减少；
    巡检重复重排轮次的等待工作项登记耗时应从数百毫秒降到毫秒级或不再触发慢登记日志。
```

## 最小优化验证结果 2026-06-07 12:37

```text
构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0
    警告=既有 LNK4075

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    退出码=0
    耗时约 42764ms
    stderr=空
    错误日志文件=本轮未生成

代码改动：
    私有_登记等待工作项新增参数 需要入队前状态提交，默认 true；
    同任务在途重排传 false；
    初次入队和普通治理请求不改，仍提交任务状态变化。

验证结果：
    重排日志仍出现，但不再产生“同任务已有在途...”原因的大段慢状态提交；
    慢登记锚点中的 入队前状态提交启用=1 主要来自初次入队或真实状态变化；
    仍存在完整状态提交耗时 0.5s-1.5s，主要由执行本能方法和生命周期互斥等待组成；
    本轮总耗时未改善，原因是样本中完整状态变化更多，不是重复重排状态提交继续存在。

提交边界：
    执行前许可相关重排函数当前属于工作区已有未提交代码，不并入本次提交；
    本次提交只包含可从 HEAD 独立应用的同任务在途重排优化和诊断日志。

当前阻塞点：
    下一层优化需要拆“任务状态提交是否必须同步执行自我_提交任务状态变化本能方法并持有私有_任务生命周期调用互斥”；
    该互斥同时保护 worker 派发、完成归并和任务壳应用；
    未制定动作动态时序与任务状态一致性方案前，不能直接缩短锁范围或异步化状态提交。
```

## 禁止宣称

```text
不得宣称内部循环全部完成。
不得宣称自我苏醒完成。
不得宣称初步成熟完成。
不得宣称任务巡检 13 秒问题已修复。
```

## S1d 执行记录 2026-06-07

```text
当前切片：
    S1d 任务状态提交同步热点治理。

执行前预测：
    1. 不拆锁、不异步化任务状态提交，不改变状态值裁决和动作动态来源。
    2. 同任务已有执行前许可占用导致的重排只做调度诊断，不再进入入队前状态提交。
    3. worker 派发和完成归并应用任务壳持有 私有_任务生命周期调用互斥 时，会在等待或持有超过阈值时输出 生命周期锁/持有耗时 日志。
    4. 自我_提交任务状态变化 本能方法仍作为真实状态变化动作动态来源。
    5. Debug x64 应能构建通过。

已改文件：
    任务模块.管理界面线程.impl.cpp
    实施记录/20260607_运行速度S1任务巡检耗时日志_Codex断点清单.md

当前实现：
    1. 私有_重排同任务执行前许可占用工作项 调用 私有_登记等待工作项 时传入 需要入队前状态提交=false。
    2. 新增 任务管理界面线程/生命周期锁/持有耗时 日志，记录原因、等待耗时、持有耗时、任务、请求、工作项、工作项类型和状态版本。
    3. 已覆盖 后台worker派发工作项、按请求同步消费派发工作项、同步消费派发工作项、完成归并应用任务壳 四个持锁原因。

禁止范围：
    不修改日志模块 flush 策略。
    不修改 D455 / 外设链路。
不修改候选方法索引、因果索引或基础信息索引。
不让工作项在任务状态提交未确认时先派发。
```

## S1d 验证与拆锁前断点 2026-06-07 18:41

```text
构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0
    警告=既有 LNK4075

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    退出码=0

关键证据：
    18:41:30.223 完成归并应用任务壳 等待=24573us 持有=1156660us 任务=8
    18:41:30.237 后台worker派发工作项 等待=1179931us 持有=14115us 任务=1
    18:41:31.330 任务状态提交完整提交 总=425547us 执行本能方法=312983us 生命周期锁等待=0
    18:41:31.852 完成归并应用任务壳 等待=0us 持有=521090us 任务=6

判断：
    新埋点已证实同一把生命周期互斥被 worker 派发、完成归并和状态提交共同竞争；
    worker 工作线程只填写建议状态，真实任务状态写入仍由管理界面线程通过 自我_提交任务状态变化 完成；
    下一步可做最小拆锁：状态提交锁、任务壳应用锁、worker 派发锁分离；
    拆锁后仍必须保持工作项派发在入队前状态提交确认之后。

下一步允许修改：
    任务模块.管理界面线程.impl.cpp

禁止修改：
    日志模块 flush 策略；
    D455 / 外设链路；
    候选方法索引、因果索引、基础信息索引；
    任务筹办/任务执行本能方法边界。
```

## S1d 拆锁后验证前预测 2026-06-07 18:48

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000

预测：
    程序应正常退出，任务状态提交仍通过 自我_提交任务状态变化 生成动作动态；
    旧 任务管理界面线程/生命周期锁/持有耗时 不应再由 worker 派发或完成归并产生；
    新 任务管理界面线程/任务互斥/持有耗时 应区分 任务状态提交锁、任务壳应用锁、任务工作项派发锁；
    状态提交等待 worker 派发锁的样本应消失，若仍慢，应主要落在任务状态提交锁自身或执行本能方法；
    工作项派发仍发生在入队前状态提交确认之后。
```

## S1d 拆锁验证结果与当前阻塞 2026-06-07 18:50

```text
构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0
    警告=既有 LNK4075

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    退出码=0

已改实现：
    1. 新增 任务状态提交锁、任务壳应用锁、任务工作项派发锁。
    2. 私有_提交界面任务状态变化 改用任务状态提交锁。
    3. 完成归并应用任务壳 改用任务壳应用锁；其中状态变化动作动态提交段单独进入任务状态提交锁。
    4. worker 派发改用任务工作项派发锁，不再占用状态提交锁。
    5. 慢锁日志改为 任务管理界面线程/任务互斥/持有耗时，并输出锁名。
    6. 任务状态提交/阶段耗时 字段改为 状态提交锁等待 / 状态提交锁内。

验证证据：
    18:50:32.233 任务状态提交 总=1067680us 状态提交锁等待=449789us 状态提交锁内=617889us 执行本能方法=466882us。
    18:50:35.649 任务互斥 锁=任务状态提交锁 原因=完成归并提交任务状态 等待=260814us 持有=504907us。
    18:50:37.193 任务互斥 锁=任务壳应用锁 原因=完成归并应用任务壳 持有=1527632us。
    18:50:40.058 任务互斥 锁=任务工作项派发锁 原因=后台worker派发工作项 持有=590640us。
    18:41 之后未再出现新的 任务管理界面线程/生命周期锁/持有耗时。

当前判断：
    S1d 第一层去重和第二层拆锁已闭合；
    状态提交不再被 worker 派发锁直接放大；
    当前剩余热点是 自我_提交任务状态变化 本能方法本身长期耗时，并导致任务状态提交锁串行等待；
    任务壳应用锁仍长，主要包含回执整理和状态提交段，后续若继续细分应先拆任务壳应用阶段耗时。

当前阻塞点：
    第三层“状态提交待确认队列”不能在本切片直接编码；
    现有 结构_任务工作项 只有状态版本、租约和幂等键，没有状态提交确认令牌或待确认队列协议；
    要继续必须先落独立方案，定义确认前不可派发规则、失败重排/死信规则、超时诊断、停止收口、状态版本更新和动作动态回填位置；
    未形成该方案前，不能把状态提交裸异步化，也不能让工作项先执行后补账。

最终补充：
    已删除不再使用的旧 私有_任务生命周期调用互斥 入口；
    18:54 重新执行 Debug x64 构建，退出码=0，警告=既有 LNK4075；
    本补充未改变 18:50 已验证的真实运行行为路径。
```

## S1d 本能方法内部清理验证结果 2026-06-07 19:07

```text
触发原因：
    自我_提交任务状态变化 本能方法内部存在高频成功诊断日志和重复任务推进特征同步；
    这些读写会放大任务状态提交锁内耗时，但动作动态、任务状态写入和拒绝完成警告不能删除。

已改实现：
    1. 新增 YUCAO_TASK_STATUS_SUBMIT_DIAG 环境开关，默认关闭任务状态提交成功诊断日志。
    2. 任务状态提交/完成判定：允许完成的成功日志默认不输出；拒绝完成仍输出警告。
    3. 任务状态提交/同步任务推进特征：诊断日志默认不输出。
    4. 任务状态提交/无有效状态阶段变化跳过动作动态：诊断日志默认不输出。
    5. 非完成状态提交不再调用 同步任务推进特征_提交路径；完成提交仅在回执宿主缺少推进特征时补同步。

已改文件：
    自我动作实现模块.ixx
    计划/计划索引.md
    计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
    实施记录/20260607_运行速度S1任务巡检耗时日志_Codex断点清单.md

构建：
    git diff --check -- 自我动作实现模块.ixx
    退出码=0
    备注=仅 CRLF 提示

    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0
    警告=既有 LNK4075

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    退出码=0

日志证据：
    日志=日志\鱼巢_run_20260607.低值g
    19:06-19:07 任务状态提交/同步任务推进特征=0
    19:06-19:07 任务状态提交/完成判定=0
    19:06-19:07 任务状态提交/无有效状态阶段变化跳过动作动态=0
    19:06-19:07 任务状态提交/拒绝完成=0
    19:06-19:07 任务状态提交/动作动态创建逻辑错误=0
    19:06-19:07 任务管理界面线程/任务状态提交/阶段耗时=30

当前判断：
    本次只清理热路径诊断日志和重复推进特征同步；
    任务状态变化仍通过 自我_提交任务状态变化 生成动作动态；
    剩余慢点仍可能来自方法规格确认、方法环境同步、运行账写入和状态提交锁串行等待；
    规格确认快路径与状态提交待确认队列必须分成后续小方案，不在本切片继续扩大修改。

禁止宣称：
    不宣称 S1d 全部完成；
    不宣称任务状态提交已异步化；
    不宣称内部循环全部性能问题已解决。
```

## S1d 调用侧耗时复测与待优化登记 2026-06-07 19:20

```text
用户口径：
    检查 自我_提交任务状态变化 的耗时不能在函数内部增加日志；
    必须在调用函数的地方增加或使用调用侧耗时。

本次动作：
    未修改代码；
    使用 任务管理界面线程/任务状态提交/阶段耗时 调用侧日志复测。

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    退出码=0

统计窗口：
    2026-06-07 19:19:34 之后。

调用侧证据：
    任务状态提交/阶段耗时=29 条。
    完整提交=23 条，平均总耗时 515073us，最大 1043047us。
    完整提交 执行本能方法 平均 259697us，最大 429828us。
    完整提交 状态提交锁等待 平均 161153us，最大 496033us。
    完整提交 准备本能调用 平均 87512us。
    完整提交 提交结果日志 平均 1037us，不是主耗时。
    同状态快照=6 条，执行本能方法=0，慢点基本是状态提交锁等待。

内部诊断日志复核：
    任务状态提交/同步任务推进特征=0
    任务状态提交/完成判定=0
    任务状态提交/无有效状态阶段变化跳过动作动态=0
    任务状态提交/拒绝完成=0
    任务状态提交/动作动态创建逻辑错误=0

待优化登记：
    下一步若继续拆 自我_提交任务状态变化 耗时，只能在调用侧细分 私有_执行任务域本能方法 外层阶段；
    优先拆 方法环境 / 规格确认 / 虚拟存在同步 / 运行账写入；
    不在 自我_提交任务状态变化 本能方法内部新增耗时日志。

下一项检查：
    按计划索引继续 S1d-3 状态提交待确认队列方案；
    先写方案，不直接编码。
```

## S1d-3 状态提交待确认队列方案检查结果 2026-06-07

```text
新增方案：
    计划/20260607_S1d3状态提交待确认队列方案_v0.1.md

代码定位：
    任务模块.管理界面线程.impl.cpp
        私有_登记等待工作项 当前先同步调用 私有_提交界面任务状态变化，成功后才入等待队列；
        状态提交失败会拒绝工作项入队并记录承接失败；
        状态提交成功后从任务状态快照同步 工作项.状态版本；
        worker 派发前仍通过 私有_工作项状态版本兼容 校验状态版本。
    任务模块.管理线程协议.ixx
        结构_任务工作项 只有 工作项ID、幂等键、状态版本、租约、重试次数和入队原因；
        没有状态提交确认令牌、确认状态、确认超时或状态提交失败原因。

方案判断：
    现有同步路径顺序正确，但调用线程同步等待；
    待确认队列必须位于可派发等待队列之前；
    未确认工作项不得进入 待筹办队列、待执行队列 或 在途工作项索引；
    状态提交确认成功后才能写入确认后的状态版本并转入现有等待队列；
    状态提交失败必须反馈承接失败，不得派发 worker。

下一主动切片：
    S1d-3a 待确认队列结构与诊断日志预备；
    只增加内部结构、快照计数和诊断入口；
    不改变当前同步入队路径。
```

## S1d-3a 待确认队列结构预备执行结果 2026-06-07

```text
已改实现：
    1. 任务模块.管理界面线程.impl.cpp 新增 结构_任务状态提交确认令牌。
    2. 任务模块.管理界面线程.impl.cpp 新增 结构_状态提交待确认项。
    3. 任务模块.管理界面线程.impl.cpp 的界面线程状态新增 下一个状态提交确认ID 和 状态提交待确认队列。
    4. 任务模块.管理线程协议.ixx 的快照新增 当前状态提交待确认项数、累计状态提交待确认项数、最近状态提交确认ID 等诊断字段。
    5. 私有_刷新队列快照_已持锁 同步 当前状态提交待确认项数。

行为边界：
    未改变 私有_登记等待工作项 当前同步调用 私有_提交界面任务状态变化 的行为；
    未让 pending 项进入 待筹办队列、待执行队列 或 在途工作项索引；
    未改变 worker 派发门控、状态版本兼容、租约和执行前许可规则。

验证：
    git diff --check
    退出码=0，备注=仅 CRLF 提示。

    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0，警告=既有 LNK4075。

    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    退出码=0。

当前判断：
    S1d-3a 已闭合；
    下一主动切片为 S1d-3b：私有_登记等待工作项 接入 pending 队列，状态提交确认成功后再转入现有等待队列。
```

## S1d-3b pending 队列接入验证预测 2026-06-07

```text
当前切片：
    S1d-3b：私有_登记等待工作项 接入 状态提交待确认队列；
    确认成功后再转入现有 待筹办队列 / 待执行队列；
    pending 项不得进入 worker 可派发等待队列或 在途工作项索引。

已改实现：
    1. 私有_登记等待工作项 改为返回内部登记结果；
    2. 需要入队前状态提交时，只生成 状态提交待确认项 并记录承接为等待状态提交确认；
    3. 界面循环线程被 pending 队列唤醒，先锁外执行 私有_提交界面任务状态变化；
    4. 状态提交确认成功后同步 工作项.状态版本，再以 需要入队前状态提交=false 进入原等待队列；
    5. 同任务已有待确认项会参与“已有入队或在途工作项”去重判断；
    6. 停止任务管理界面线程时清空 pending 队列。

已完成验证：
    git diff --check
    退出码=0，备注=仅 CRLF 提示。

    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0，警告=既有 LNK4075。

真实运行预测：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    预期退出码=0；
    预期日志能看到 状态提交待确认入队 / 状态提交确认完成；
    若本轮没有可推进任务，至少不应出现 状态提交确认失败、工作项状态版本错配、完成队列归并拒绝或 worker 派发越过状态提交确认。
```

## S1d-3b 首轮真实运行发现与修正 2026-06-07

```text
首轮真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    退出码=0。

发现：
    pending 链路已出现 状态提交待确认入队 / 状态提交确认完成；
    但归并后生成下一轮 pending 的同时，自我线程仍可能提交同任务同类型调度请求；
    这会让旧工作项在后续状态版本变化后进入 worker 校验，出现状态版本错配；
    停止窗口里还出现“状态已确认但等待队列登记失败”，应作为停止丢弃诊断，不应记为状态提交确认失败。

已补修正：
    1. 状态提交待确认登记时，在同一把状态锁内检查同任务同类型的 pending / 待筹办 / 待执行 / 在途 / 执行前许可占用；
    2. 命中重复时只记录调度诊断“状态提交待确认跳过重复”，不分配确认 ID，不进入 pending，也不提交任务状态；
    3. 调用方不再把重复跳过覆盖为真实已入队；
    4. 状态确认后如果任务管理已进入停止请求，记录“状态提交确认停止丢弃”，不写错误日志。

复验预测：
    Debug x64 构建应继续通过；
    真实运行应继续退出码=0；
    最新日志中应有 状态提交待确认入队 / 状态提交确认完成 / 状态提交待确认跳过重复；
    不应再出现本轮新增的 状态提交确认失败；
    同任务重复提交应被跳过，worker 状态版本错配数量应下降，若仍出现则需要继续定位是否来自已入真实等待队列后的状态被完成归并改变。
```

## S1d-3b / S1d-3c 最终验证结果 2026-06-07

```text
已改文件：
    任务模块.管理界面线程.impl.cpp
    计划/20260607_S1d3状态提交待确认队列方案_v0.1.md
    计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
    计划/计划索引.md
    实施记录/20260607_运行速度S1任务巡检耗时日志_Codex断点清单.md

实现结果：
    私有_登记等待工作项 需要入队前状态提交时，不再同步执行状态提交；
    先生成 状态提交待确认项，放入 状态提交待确认队列；
    界面循环线程处理 pending，锁外调用 私有_提交界面任务状态变化；
    状态确认成功后同步确认后的 工作项.状态版本，再进入原 待筹办 / 待执行 队列；
    同任务同类型已有 pending / 待筹办 / 待执行 / 在途 / 执行前许可占用时，记录 状态提交待确认跳过重复，不重复提交状态；
    停止窗口中已确认但未入队的项记录为 状态提交确认停止丢弃，不记错误。

验证：
    git diff --check
    退出码=0，备注=仅 CRLF 提示。

    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0，警告=既有 LNK4075。

    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    退出码=0。

关键日志证据：
    日志/鱼巢_run_20260607.低值g
    41551 行：状态提交待确认入队，确认ID=1，任务=1，工作项=1。
    41561 行：状态提交确认完成，确认ID=1，状态版本=178958524024。
    41606 行：状态提交待确认跳过重复，重复位置=在途工作项索引。
    41666 行：状态提交待确认跳过重复，重复位置=状态提交待确认队列。
    42439 行：执行工作项命中执行前许可占用，跳过重复。
    42617 行：停止窗口记录 状态提交确认停止丢弃。

负向核对：
    19:53 以后未检索到 状态提交确认失败；
    19:53 以后未检索到 状态版本错配 / 后台worker工作项进入死信 / 拒绝归并完成项。

当前判断：
    S1d-3b / S1d-3c 闭合；
    状态提交耗时已从调用线程的 等待工作项登记 同步段转移到界面循环 pending 确认段；
    最新最慢阶段仍是 状态提交确认中的 执行本能方法；
    下一主动切片为 S1d-4a：状态提交本能调用外层阶段细分与规格确认快路径方案。
```

## S1d-4a 规格确认快路径执行前预测 2026-06-07

```text
本次最小改动：
    新增 计划/20260607_S1d4a状态提交本能调用外层阶段与规格快路径方案_v0.1.md；
    在 自我动作实现模块.ixx 的 确认提交任务状态变化规格 增加同一方法首节点 / 同一方法虚拟存在的进程内已确认快路径。

预测：
    Debug x64 构建应通过；
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000 应退出码 0；
    任务状态变化仍通过 自我_提交任务状态变化 生成动作动态；
    pending 状态提交确认仍应先确认状态再入等待工作项队列；
    不应出现 状态提交确认失败、状态版本错配、后台worker工作项进入死信、拒绝归并完成项。

边界：
    不在 自我_提交任务状态变化 本能方法内部新增耗时日志；
    快路径只跳过重复规格声明，不跳过本能方法执行、任务状态写入、动作动态和运行账。
```

## S1d-4a 验证结果 2026-06-07 20:03

```text
构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0，警告=既有 LNK4075。

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    退出码=0。

关键日志证据：
    日志/鱼巢_run_20260607.低值g
    42693 行：状态提交待确认入队，确认ID=1，任务=1。
    42709 行：状态提交确认完成，确认ID=1，状态提交耗时=337882us。
    42731 行：状态提交待确认跳过重复，重复位置=在途工作项索引。
    42796 行：状态提交待确认跳过重复，重复位置=状态提交待确认队列。
    43621 行：执行工作项重复跳过，重复位置=执行前许可任务占用表。
    43639 / 43640 / 43648 行：同状态 / 同阶段确认耗时分别为 1082us、2241us、2666us。

统计：
    20:03-20:05 窗口 状态提交确认完成 30 条；
    完整状态提交阶段耗时 27 条；
    完整提交平均总耗时约 463832us，最大 815674us；
    执行本能方法平均约 337040us，最大 681557us；
    准备本能调用平均约 117366us，最大 301357us。

负向核对：
    20:03-20:05 窗口未检索到 状态提交确认失败；
    未检索到 状态版本错配 / 后台worker工作项进入死信 / 拒绝归并完成项。

当前判断：
    S1d-4a 快路径未破坏状态提交待确认链；
    单独跳过重复规格声明不是主收益点；
    最新主耗时仍是 执行本能方法；
    下一主动切片应为 S1d-4b：本能动作管理器外层执行阶段拆分与方法环境同步定位。
```

## S1d-4b 执行功能函数外层阶段诊断预测 2026-06-07

```text
本次最小改动：
    新增 计划/20260607_S1d4b本能动作管理器外层阶段拆分方案_v0.1.md；
    在 本能动作管理类::执行功能函数 外层增加 自我_提交任务状态变化 慢调用阶段耗时日志。

预测：
    Debug x64 构建应通过；
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000 应退出码 0；
    真实状态提交仍通过 自我_提交任务状态变化 生成动作动态；
    日志应出现 本能动作管理/执行功能函数/阶段耗时；
    若 动作函数调用 仍是最慢阶段，下一刀继续定位统一方法环境或状态提交本体。

边界：
    本能动作管理类只记录阶段耗时，不解释任务、需求或执行结果；
    不新增混合返回结构；
    不在 自我_提交任务状态变化 本能方法内部新增耗时日志。
```

## S1d-4b 验证结果 2026-06-07 20:10

```text
构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0，警告=既有 LNK4075。

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    退出码=0。

关键日志证据：
    日志/鱼巢_run_20260607.低值g
    43958 行：本能动作管理/执行功能函数/阶段耗时，动作函数调用=366433us，检查=1597us。
    44000 行：动作函数调用=142006us，检查=850us。
    44012 行：动作函数调用=115472us，检查=649us。
    44537 行：动作函数调用=273167us，检查=24875us。
    44684 行：动作函数调用=491107us，检查=2560us。

统计：
    20:09-20:11 窗口 本能动作管理/执行功能函数/阶段耗时 33 条；
    平均总耗时约 279475us，最大 494529us；
    动作函数调用平均约 275322us，最大 491107us；
    检查是否可调用平均约 3235us，最大 24875us；
    运行码写回平均约 208us，最大 396us。

负向核对：
    20:09-20:11 窗口未检索到 状态提交确认失败；
    未检索到 状态版本错配 / 后台worker工作项进入死信 / 拒绝归并完成项。

当前判断：
    本能动作管理器外层桥不是主耗时；
    最新主耗时在 动作函数调用；
    下一主动切片应为 S1d-4c：统一本能方法自身环境 helper 阶段诊断。
```

## S1d-4c 统一本能方法自身环境阶段诊断预测 2026-06-07

```text
本次最小改动：
    新增 计划/20260607_S1d4c本能方法自身环境阶段诊断方案_v0.1.md；
    在 确保本能方法自身环境 统一 helper 中增加 自我_提交任务状态变化 慢调用阶段耗时日志。

预测：
    Debug x64 构建应通过；
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000 应退出码 0；
    日志应出现 本能方法自身环境/阶段耗时；
    若 同步方法节点到虚拟存在 是最慢阶段，下一刀治理方法虚拟存在同步快路径；
    若环境 helper 不是主耗时，继续定位任务状态写入 / 动作动态输出 / 输出运行虚拟存在 / 更新方法运行账。

边界：
    不在 自我_提交任务状态变化 具体方法体内新增耗时日志；
    不跳过本能方法环境、任务状态动作动态或运行账。
```

## S1d-4c 验证结果 2026-06-07 20:17

```text
构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0，警告=既有 LNK4075。

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    退出码=0。

关键日志证据：
    日志/鱼巢_run_20260607.低值g
    44901 行：本能方法自身环境/阶段耗时，总耗时=143645us，确保本能方法=51575us，首次同步方法虚拟存在=41264us，二次同步=29917us。
    44905 行：任务状态提交/阶段耗时，总耗时=332009us，执行本能方法=227785us，准备本能调用=95839us。
    45651 行：本能方法自身环境/阶段耗时，总耗时=165318us，首次同步=52891us，二次同步=47576us。
    45754 行：任务状态提交/阶段耗时，总耗时=500488us，执行本能方法=353965us，准备本能调用=140324us。

统计：
    20:16-20:18 窗口 本能方法自身环境/阶段耗时 42 条；
    环境 helper 平均总耗时约 135512us，最大 253117us；
    确保本能方法平均约 54935us，最大 108421us；
    首次同步方法虚拟存在平均约 38013us，最大 85884us；
    二次同步方法虚拟存在平均约 34911us，最大 54570us；
    确认规格平均约 0us，最大 1us。
    同窗口 本能动作管理/执行功能函数/阶段耗时 42 条；
    动作函数调用平均约 314430us，最大 499489us。
    同窗口 完整任务状态提交阶段 23 条；
    完整提交平均约 431330us，执行本能方法平均约 316064us，准备本能调用平均约 106432us。

负向核对：
    未出现 状态提交确认失败；
    未出现 后台worker工作项进入死信 / 拒绝归并完成项；
    出现 1 条 任务管理界面线程/终止过期工作项 | 状态版本错配，代码路径为派发前终止过期工作项，未进入死信，未越过状态确认执行。该现象作为旧等待项残留后续跟踪，不阻塞 S1d-4c 耗时定位结论。

当前判断：
    S1d-4c 已证明通用环境 helper 是 自我_提交任务状态变化 的显著成本，但不是全部成本；
    规格确认快路径已生效，不是当前主耗时；
    下一刀先治理可安全快路径化的通用环境成本，尤其 确保本能方法 和 方法虚拟存在两次同步；
    若不允许在 自我_提交任务状态变化 具体方法体内加耗时日志，则剩余约 179ms 的方法本体成本暂不能再细分到任务状态写入、动作动态输出、输出运行虚拟存在和运行账之间。
```

## S1d-4d 通用环境快路径预测 2026-06-07

```text
本次最小改动：
    在 确保本能方法自身环境 中增加 自我_提交任务状态变化 专用进程内环境缓存；
    首次完整环境成功后缓存方法首节点、方法虚拟存在、条件场景、结果场景和方法条件节点；
    后续同一输入方法首节点命中缓存时，跳过重复 确保本能方法 / 首次同步 / 确认规格 / 二次同步，但仍创建本轮运行虚拟存在。

预测：
    Debug x64 构建应通过；
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000 应退出码 0；
    快路径命中样本的 本能方法自身环境/阶段耗时 若输出，应显示 快路径=1，且 确保本能方法 / 首次同步 / 确认规格 / 二次同步 接近 0；
    完整任务状态提交平均耗时应下降，若未下降则说明剩余主耗时在具体方法体写账或其他共享写入。

负向核对：
    不应出现 状态提交确认失败、后台worker工作项进入死信、拒绝归并完成项；
    派发前 终止过期工作项 | 状态版本错配 若仍出现，继续按旧等待项残留登记，不作为本快路径越权执行证据。
```

## S1d-4d 验证结果 2026-06-07 20:26

```text
构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0，警告=既有 LNK4075。

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    退出码=0。

关键日志证据：
    日志/鱼巢_run_20260607.低值g
    46059 行：首条完整环境，总耗时=121000us，快路径=0，写入环境缓存。
    46093 行：快路径=1，总耗时=13627us，确保本能方法=0，首次同步=0，确认规格=0，二次同步=0，本轮运行虚拟存在=13624us。
    46106 行：完整状态提交总耗时=207539us，执行本能方法=105905us，准备本能调用=86225us。
    46233 行：完整状态提交总耗时=162174us，执行本能方法=73276us，准备本能调用=83350us。

统计：
    20:25-20:27 窗口 本能方法自身环境/阶段耗时 6 条，其中快路径 5 条；
    快路径环境平均总耗时约 18622us，最大 35619us；
    快路径 确保本能方法 / 首次同步 / 确认规格 / 二次同步 均为 0；
    快路径 本轮运行虚拟存在平均约 18619us，最大 35616us。
    同窗口 本能动作管理/执行功能函数/阶段耗时 44 条；
    动作函数调用平均约 194768us，最大 452476us。
    同窗口 完整任务状态提交 24 条；
    完整提交平均约 285352us，最大 409974us；
    执行本能方法平均约 170344us，最大 275114us；
    准备本能调用平均约 105677us，最大 180294us。

负向核对：
    未出现 状态提交确认失败；
    未出现 后台worker工作项进入死信 / 拒绝归并完成项；
    出现 2 条 任务管理界面线程/终止过期工作项 | 状态版本错配，仍是派发前终止过期工作项，未进入死信，未越过状态确认执行。

当前判断：
    S1d-4d 快路径有效，未破坏状态提交确认顺序；
    通用环境重复补齐成本已明显下降；
    当前剩余热点转向 调用侧准备本能调用 约 105677us，以及 自我_提交任务状态变化 具体方法体剩余写账成本；
    在不进入具体方法体加耗时日志的边界下，下一刀应先拆分调用侧 准备本能调用 阶段。
```

## S1d-4e 验证结果 2026-06-07 20:31

```text
本次最小改动：
    在任务管理调用侧拆分 私有_准备任务域本能调用 的准备阶段；
    细分字段接入现有 任务状态提交/阶段耗时 日志，不进入 自我_提交任务状态变化 具体方法体。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0，警告=既有 LNK4075。

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    退出码=0。

关键日志证据：
    日志/鱼巢_run_20260607.低值g
    47191 行：准备本能调用=94538us，准备最慢阶段=查找本能方法，查找本能方法=93436us。
    47227 行：准备本能调用=117054us，查找本能方法=104735us。
    47366 行：准备本能调用=83136us，查找本能方法=82511us。
    48084 行：准备本能调用=147509us，查找本能方法=146651us。

统计：
    20:30-20:32 窗口 完整任务状态提交 25 条；
    完整提交平均约 292491us；
    准备本能调用平均约 104655us，最大 175232us；
    准备查找本能方法平均约 100035us，最大 174305us；
    准备最慢阶段分布：查找本能方法 25/25；
    输入场景平均约 910us，输出场景平均约 1372us，输入条件包平均约 713us，写任务指针平均约 1415us。

负向核对：
    未出现 状态提交确认失败；
    未出现 后台worker工作项进入死信 / 拒绝归并完成项；
    出现 2 条 任务管理界面线程/终止过期工作项 | 状态版本错配，仍为派发前终止过期工作项。

当前判断：
    S1d-4e 已定位调用侧准备阶段主耗时为 本能方法类::查找或创建本能方法首节点；
    下一刀应做 S1d-4f：任务状态提交调用侧方法首节点缓存；
    该优化仍在调用侧，不跳过状态动作动态、不跳过本能方法执行。
```

## S1d-4f 调用侧方法首节点缓存预测 2026-06-07

```text
本次最小改动：
    在任务管理调用侧缓存 自我_提交任务状态变化 方法首节点；
    缓存命中时跳过 本能方法类::查找或创建本能方法首节点；
    仍创建输入参数场景、输入条件包、输出结果场景，并执行 自我_提交任务状态变化。

预测：
    Debug x64 构建应通过；
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000 应退出码 0；
    任务状态提交/阶段耗时 中 准备查找本能方法 平均耗时应显著下降；
    准备本能调用平均耗时应下降；
    状态提交确认完成仍应出现。

负向核对：
    不应出现 状态提交确认失败、后台worker工作项进入死信、拒绝归并完成项；
    派发前 终止过期工作项 | 状态版本错配 若仍出现，继续登记为旧等待项残留治理。
```

## S1d-4f 验证结果 2026-06-07 20:45

```text
本次最小改动：
    在任务管理调用侧缓存 自我_提交任务状态变化 方法首节点；
    缓存写入先失效自我存在键，再写方法首节点，最后发布自我存在键；
    状态版本错配的旧工作项不再进入死信，统一终止过期工作项，由当前任务状态重新调度。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0，警告=既有 LNK4075。

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    退出码=0。

关键日志证据：
    日志/鱼巢_run_20260607.低值g
    20:45-20:47 窗口完整任务状态提交 44 条；
    准备查找本能方法平均约 2011us，最大 88426us，缓存命中样本为 0-2us；
    准备本能调用平均约 10392us；
    执行本能方法平均约 455333us；
    本能动作管理器动作函数调用平均约 465213us；
    本能方法自身环境快路径平均约 31083us。

负向核对：
    未出现 状态提交确认失败；
    未出现 后台worker工作项进入死信；
    未出现 拒绝归并完成项；
    出现 2 条 任务管理界面线程/终止过期工作项 | 状态版本错配，均为旧版本工作项终止，未派发旧工作项执行。

当前判断：
    S1d-4f 已闭合，调用侧方法首节点查找成本已压到缓存命中级别；
    状态版本错配旧工作项不再误入死信；
    剩余主耗时仍在 自我_提交任务状态变化 动作函数本体写账；
    在“不在本能方法内部新增耗时日志”的边界下，任务状态写入、动作动态创建、输出运行虚拟存在、更新方法运行账之间不能继续用调用侧日志细分。

后续登记：
    S1d-4g 待优化：自我_提交任务状态变化 本体写账瘦身；
    需要单独方案，并需先确认是否允许在方法体内部做阶段耗时，或仅按代码结构清理明确重复写账。
    当前继续下一项 S3 日志分级 / 批量 flush，避免继续用 S1d 遮蔽日志同步 flush 放大效应。
```

## S3a 验证结果 2026-06-07 20:52

```text
本次最小改动：
    日志模块普通日志不再每条强制 flush；
    普通日志改为 250ms 间隔刷新；
    错误 / 致命日志保持立即 flush。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0，警告=既有 LNK4075。

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    退出码=0。

负向核对：
    未出现 日志模块低级错误；
    未出现 状态提交确认失败；
    未出现 后台worker工作项进入死信 / 拒绝归并完成项。

当前判断：
    S3a 已闭合；
    当前主动切片转入 S4a 需求树生长摘要 offline/live 拆分。
```

## S4a 预测 2026-06-07

```text
本次最小改动：
    --demand-tree-growth-summary 默认改为 offline 只读现成摘要；
    --demand-tree-growth-summary-live 保留旧的完整初始化与即时快照语义；
    offline 分支不调用 私有_确保自我环境已初始化，不启动摘要线程，不创建需求 / 任务 / 方法 / 状态 / 动态结构。

预测：
    Debug x64 构建应通过；
    .\x64\Debug\鱼巢.exe --demand-tree-growth-summary 应快速 0 码返回，并标注 offline、快照来源和无可用快照原因；
    .\x64\Debug\鱼巢.exe --demand-tree-growth-summary-live 应保留旧 live 初始化语义并 0 码返回；
    offline 日志不应出现 命令行/确保自我环境初始化。

禁止宣称：
    不把 offline 无快照输出解释为需求树业务通过或失败；
    不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## S4a 验证结果 2026-06-07 21:03

```text
本次最小改动：
    --demand-tree-growth-summary 默认改为 offline，只读取当前进程已有的控制面板摘要线程最新快照；
    当前进程没有已采集快照时，输出无可用快照原因并 0 码返回；
    --demand-tree-growth-summary-live 新增为显式 live 入口，保留旧完整初始化和即时轻量快照读取语义。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0，警告=既有 LNK4075。

真实运行：
    .\x64\Debug\鱼巢.exe --demand-tree-growth-summary
    退出码=0，耗时约 82ms。

    .\x64\Debug\鱼巢.exe --demand-tree-growth-summary-live
    退出码=0，耗时约 9554ms。

关键日志证据：
    日志/鱼巢_run_20260607.低值g
    56583 行：控制面板命令/需求树生长摘要 | 开始 | 模式=offline。
    56584 行：控制面板命令/需求树生长摘要 | 完成输出 | 模式=offline | 快照序号=0 | 可用=否。
    56599 行：命令行/确保自我环境初始化 | 标记=鱼巢::main/控制面板/需求树生长摘要/live | 成功=是 | 耗时ms=9448。
    56612 行：控制面板摘要/快照读取 | 场景=需求树生长摘要/live | 轻量=是 | 耗时ms=55。

负向核对：
    offline 最新窗口未出现 命令行/确保自我环境初始化；
    S4a 最新窗口未出现 日志模块低级错误、状态提交确认失败、后台worker工作项进入死信、拒绝归并完成项。

当前判断：
    S4a 已闭合；
    默认短摘要不再支付完整自我环境冷启动成本；
    当前主动队列转入 S8a D455 外设性能专线只读定位与第一刀方案。
```

## S8a 预测 2026-06-07

```text
本次切片目标：
    只读定位 D455 报告入队、材料页、等待项巡检和高频日志路径；
    列出真实所有者和下一刀优先级；
    进入代码实现前另行收窄允许修改文件。

预测：
    不修改 D455 / 外设 / 任务管理代码；
    不需要构建；
    应能从外设线程、报告队列、任务管理承接和日志锚点定位第一刀；
    缺真实 D455 硬件时只记录硬件条件不足，不作为业务失败。

禁止宣称：
    不把 D455 材料、报告、队列项或 d455:// 句柄写成世界真值；
    不宣称 D455 业务链路通过；
    不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## S8a 只读定位结果 2026-06-07

```text
代码路径：
    外设线程_D455深度相机.ixx：
        D455_线程主循环 每帧生成逐簇识别报告、提交外设观察报告、提交 D455 短期观察材料页；
        有扫描 / 跟踪等待项时同帧生成扫描变化报告或跟踪报告；
        每帧至少输出 逐簇识别报告入队 和 采样 两条长日志。

    外设观察报告队列.ixx：
        共享状态为 mutex + 报告 deque + D455材料页 deque + 等待项 vector；
        报告队列容量=64，默认有效期=60000ms；
        D455材料页容量=6，保留=60000ms；
        按等待项匹配外设观察报告 倒序扫描报告队列。

    任务模块.管理界面线程.impl.cpp：
        启动任务管理界面线程 时会启动外设承接后台线程；
        外设承接后台线程每 100ms 巡检外设等待项；
        普通任务管理界面循环也会在巡检并派发就绪任务中调用外设等待项巡检。

    自我线程模块.impl.cpp：
        自我线程主循环在规范化消息后仍主动调用 巡检外设观察等待项并生成上行消息。

第一刀选择：
    S8a-1 外设等待项巡检所有者收束；
    先撤出自我线程主动巡检，保留任务管理侧外设承接后台线程和现有任务管理侧路径；
    不改 D455 报告生产、不改外设报告队列、不改任务状态 / 需求满足 / 价值结算 / 外设事实裁决。
```

## S8a-1 预测 2026-06-07

```text
本次最小改动：
    删除或停用 自我线程主循环中对 任务管理界面线程::巡检外设观察等待项并生成上行消息 的主动调用；
    自我线程仍只消费任务管理上行 mailbox 和观察事实承接消息；
    外设等待项巡检继续由任务管理界面线程外设承接后台线程负责。

构建预测：
    Debug x64 应通过。

真实运行预测：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000 应退出码 0；
    日志仍应出现 任务管理界面线程/外设承接后台线程启动；
    日志不应再出现 自我线程主动巡检分支的 派生需求暂停前外设观察承接先行。

负向核对：
    不应出现 状态提交确认失败、后台worker工作项进入死信、拒绝归并完成项；
    不应出现 外设承接后台线程启动失败；
    不宣称 D455 业务链路通过。
```

## S8a-1 验证结果 2026-06-07 21:14

```text
本次最小改动：
    自我线程主循环撤出 任务管理界面线程::巡检外设观察等待项并生成上行消息 主动调用；
    外设等待项巡检继续由任务管理界面线程外设承接后台线程负责；
    自我线程仍消费任务管理上行 mailbox 和观察事实承接消息。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0，警告=既有 LNK4075。

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    退出码=0，耗时约 54135ms。

关键日志证据：
    日志/鱼巢_run_20260607.低值g
    本次运行窗口：56615 行之后，新增日志 1799 行。
    56639 行：任务管理界面线程/外设承接后台线程启动 | 只巡检外设观察等待项=1 | 不派发普通任务=1。

负向核对：
    本次窗口未出现 派生需求暂停前外设观察承接先行；
    未出现 状态提交确认失败；
    未出现 后台worker工作项进入死信；
    未出现 拒绝归并完成项；
    未出现 外设承接后台线程启动失败。

当前判断：
    S8a-1 已闭合；
    外设等待项巡检所有者已收束到任务管理侧；
    本切片不证明 D455 业务链路通过，不改变 D455 报告生产、外设报告队列、任务状态、需求满足、价值结算或外设事实裁决。

下一步：
    S8b：D455 高频报告 / 日志限流小方案；
    先新增方案并收窄允许修改文件，再处理 D455 逐帧长日志和无等待项场景下的完整报告输出频率。
```
