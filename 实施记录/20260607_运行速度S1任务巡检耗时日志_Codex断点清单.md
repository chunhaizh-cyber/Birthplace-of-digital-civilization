# 20260607 运行速度 S1 任务巡检耗时日志 Codex 断点清单

## 当前切片

```text
计划：
    计划/20260607_运行速度常驻路径优先优化计划_v0.1.md

切片：
    S1 任务巡检细分耗时日志。

目标：
    在任务管理后台巡检路径增加阶段耗时日志，定位 13 秒级单轮巡检慢点。
```

## 依据文件

```text
AGENTS.md
规范/规范目录.md
规范/自我线程与任务管理线程权责边界规范20260512.md
规范/任务筹办与执行边界规范20260512.md
规范/任务筹办父方法与协作子方法链规范20260513.md
计划/计划索引.md
计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
实施记录/20260607_运行速度架构缺陷检测报告.md
```

## 允许修改文件

```text
任务模块.管理界面线程.impl.cpp
实施记录/20260607_运行速度S1任务巡检耗时日志_Codex断点清单.md
```

## 禁止修改范围

```text
不改变任务入队语义。
不改变任务裁决规则。
不改变等待工作项登记规则。
不改变任务状态推进规则。
不新增伪任务、伪样本、伪回归入口。
不把任务筹办 / 任务执行恢复为本能方法。
不把摘要中间态写成业务通过。
```

## 执行前预测

```text
1. Debug x64 应能构建通过。
2. 真实运行摘要命令应退出码 0。
3. 日志中应出现任务状态巡检阶段耗时锚点。
4. 新增日志只暴露阶段耗时、最慢任务和最慢阶段，不改变原有巡检计数语义。
5. 如果短摘要命令没有自然触发慢巡检，本切片只证明埋点存在，不伪造 13 秒样本。
```

## 当前已运行命令

```text
已读取 AGENTS.md、规范目录、任务管理相关规范、计划索引、运行速度计划。
已检查 任务模块.管理界面线程.impl.cpp 当前存在大量未提交差异。
已定位 S1 插入点为 私有_巡检全部任务状态并入队。
```

## 实际验证证据

```text
构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0
    警告=1
    警告内容=LNK4075 忽略 /EDITANDCONTINUE，由 /INCREMENTAL:NO 规范触发
    错误=0

短摘要真实运行：
    .\x64\Debug\鱼巢.exe --demand-tree-growth-summary
    退出码=0
    耗时约 9677ms
    输出显示 快照状态=即时读取，中间态=否，请求队列=0，当前等待工作项=0，待筹办=0，待执行=0
    本路径未自然触发新的任务状态巡检锚点

自检真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=1000
    退出码=0
    耗时约 17558ms
    本路径自然触发任务管理巡检

新增日志锚点：
    任务管理界面线程/任务状态巡检/阶段耗时
    任务管理界面线程/任务状态巡检/单任务最慢阶段

日志样本：
    2026-06-07 12:13:45.501 任务管理界面线程/任务状态巡检
        扫描任务=4
        可推进=1
        入队任务=0
        等待=3
        已有工作项=1
        不可推进=3
        耗时微秒=3827

    2026-06-07 12:13:45.501 任务管理界面线程/任务状态巡检/阶段耗时
        总耗时微秒=3827
        任务头收集=598
        任务状态读取=1744
        单任务裁决=1
        已有工作项检查=2
        状态快照记录=1451
        汇总快照锁内=1

    2026-06-07 12:13:45.501 任务管理界面线程/任务状态巡检/单任务最慢阶段
        最慢阶段=任务头收集
        最慢阶段耗时微秒=598
        最慢任务=2
        最慢任务状态=等待中
        最慢任务总耗时微秒=956
        超阈值任务数=0
```

## 当前判断

```text
本切片只做日志埋点。
目标文件已有用户或既有工作区改动，后续提交必须只包含本次新增埋点和本断点清单，不能回滚或混入无关改动。
S1 埋点已构建通过，并在真实自检路径自然触发。
本次样本不是 13 秒慢样本，不宣称任务巡检性能问题已修复。
```

## 下一步

```text
1. 提交本次 S1 埋点与断点清单。
2. 后续等待真实常驻运行样本复现慢巡检。
3. 若最慢阶段稳定指向重复扫描或已有工作项检查，再进入 S2 脏集 / 事件驱动。
4. 若最慢阶段指向日志或状态快照，再优先进入 S3 日志分级和热路径降噪。
```

## 禁止宣称

```text
不得宣称内部循环全部完成。
不得宣称自我苏醒完成。
不得宣称初步成熟完成。
不得宣称任务巡检 13 秒问题已修复。
```
