# 20260704 体素融合触发口径纠偏 Codex断点清单

## 0. 恢复摘要

计划：`计划/20260704_体素融合触发口径纠偏实现映射计划_v0.1.md`

当前切片：S1 触发口径纠偏已完成。

当前判断：详细设计要求由已确认存在 E 上新增同源深度 / 彩色轮廓组触发体素融合；当前代码在观察原始事实写入中直接写粗 `三维体素模型` 并触发 worker，需纠偏。

禁止宣称：

```text
不得宣称真实 D455 自然样本已验证；
不得宣称存在体素融合内部治理链完整闭合；
不得宣称场景体素刷新链已接入；
不得宣称 C -> E 最终裁决已完整实现。
```

## 1. 已读依据

```text
规范/详细设计/待完成/20260703_存在体素融合内部治理详细设计_v0.1.md
规范/6240_子规范_已确认存在体素融合与分解_20260701.md
计划/20260703_存在体素融合内部治理实现映射计划_v0.1.md
docs/current-state.md
自我动作实现.外设模块.ixx
视觉观察融合模块.ixx
说明书/函数功能说明文档.md
```

## 2. 本切片目标

```text
1. 观察原始事实写入不再直接写粗三维体素模型。
2. 合并到已有 E 的同源深度 / 彩色轮廓材料提交现有体素加工 worker。
3. 保留 worker 只产候选包和 P12 轻量写入治理入口。
```

## 3. 待运行验证

```powershell
python .\tools\生成函数功能说明文档.py
python .\tools\check_specs.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

## 4. 验证进展

```text
已运行：
python .\tools\生成函数功能说明文档.py
结果：退出码 0，已生成 7737 条函数条目。

python .\tools\check_specs.py
结果：退出码 0，hard issues = 0，warnings = 0。

git diff --check -- 本切片相关文件
结果：退出码 0；仅提示 说明书/函数功能说明文档.md 下次 Git 触碰时 CRLF 将替换为 LF。

构建前预期：
Debug x64 构建应通过；若失败，优先检查新桥接函数的可见声明、状态常量和类型转换，不通过日志或显示文本绕过。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：退出码 0，0 警告，0 错误。
本地构建日志：实施记录/20260704_体素融合触发口径纠偏_msbuild.log（*.log 按仓库规则忽略，提交内以退出码和错误摘要为证据）
```

## 5. 收口判断

```text
已完成：
1. 观察原始事实写入不再直接调用粗三维体素模型写入。
2. 合并到已有 E 的观察材料路径新增 E 轮廓组体素加工 worker 提交入口。
3. worker 仍只产候选包，P12 轻量入口仍负责稳定三维体素主模型写入。
4. 函数说明文档已刷新，check_specs 0/0，Debug x64 构建通过。

未完成 / 不纳入本切片：
1. 未执行真实 D455 自然样本验证。
2. 未宣称存在体素融合内部治理链完整闭合。
3. 未接入场景体素刷新链。
```
