# D455 稳定轮廓模块重构 Codex 断点清单

更新时间：2026-06-14

## 0. 恢复摘要

```text
默认只读范围：
    只读本节即可判断本文件状态；不要整读 P0-P7 和迁移分割历史。

文件定位：
    本文件记录 D455 稳定轮廓模块重构、P0-P7 验证和后续用户指定重建 D455 外设线程模块的历史切片。
    当前主动主线不在本文件；当前主动恢复入口见：
        计划/计划索引.md 第 1 章
        实施记录/20260614_D455扫描跟踪后续事实链_Codex断点清单.md 的 `## 0. 恢复摘要`

当前状态：
    P0-P7 当前可执行代码切片已闭合。
    Debug x64 构建和真实 D455 capture-once 已通过。
    用户指定重建 D455 外设线程模块切片已完成当前可执行部分：
        新 `外设线程_D455深度相机.ixx` 已迁入无 OpenCV / PCL 依赖的深度切片候选生成层。
        单帧材料包路径已验证，ROI / 掩码 / 点集 / 深度切片 / 候选存在句柄可生成。

仍未覆盖：
    持续外设线程自然等待项命中。
    L1 稳定子集自然入队。
    观察事实承接请求动态生成。

恢复条件：
    只有用户指定 D455 稳定轮廓模块主线、或真实外设自然业务样本恢复时，才按标题小段读取后续章节。
    复核 P7 收束读 `## 24. P7 验收结果`。
    复核重建外设线程读 `## 25. 用户指定重建 D455 外设线程模块切片`。

禁止宣称：
    不宣称 D455 自然业务样本已经通过。
    不宣称识别 / 扫描 / 跟踪边界已经全部动态闭合。
    不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 1. 当前切片

```text
计划：
    计划/20260614_D455稳定轮廓模块重构计划_v0.1.md

当前切片：
    P0 只读审计与断点。

允许修改：
    实施记录/20260614_D455稳定轮廓模块重构_Codex断点清单.md

本轮实际修改：
    新增本断点清单。
    更新 计划/计划索引.md 当前主动队列为 P0 闭合后的暂停状态。

本轮未修改：
    外设线程_D455深度相机.ixx
    外设观察报告队列.ixx
    任务模块.管理界面线程.impl.cpp
    任务模块.管理工作线程.ixx
    自我动作实现.外设模块.ixx
    自我线程模块.impl.cpp
    鱼巢.vcxproj
```

## 2. 已读取依据

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md 第 0-1 章
计划/20260614_D455稳定轮廓模块重构计划_v0.1.md
规范/相机外设综合工作流程规范20260607.md
规范/观察特征质量诊断与认知补偿规范20260525.md
规范/观察像素簇与存在候选分层规范20260527.md
规范/当前场景特征值明确标准规范20260602.md
规范/识别本能方法唯一性收敛规范20260607.md
流程图/20260614_D455稳定轮廓项目适配流程图_v0.2.md
```

当前规范边界复核：

```text
1. D455 / RGB-D 工程技术只能作为外设内部稳定化、关联、建图或供料技术。
2. L0-L3 属于 D455 外设侧，L4 属于自我侧。
3. 外设报告、队列项、材料页和 tracker ID 不能作为正式世界事实主体。
4. 观察是合法写新平面轮廓一阶观察特征值的路径；识别、扫描、跟踪只能读取并生成二次特征、状态动态或缺口。
5. 当前场景特征值明确状态不得直接由 D455 报告、材料页、外设帧、像素簇、扫描包或跟踪包满足。
```

## 3. 只读命令证据

```text
git -c core.quotepath=false status --short --untracked-files=no
    输出为空；无已跟踪未提交改动。

git -c core.quotepath=false status --short --untracked-files=all
    仅有两个与本切片无关的未跟踪运行输出图片：
        运行输出/20260613_稳定子集实际轮廓图_日志投影.png
        运行输出/20260613_稳定子集实际轮廓图_日志投影.svg

rg -n "^" 外设线程_D455深度相机.ixx | Select-Object -Last 5
    最后一行号为 2654。

Get-Item 关键文件
    外设线程_D455深度相机.ixx 长度 122944。
    外设观察报告队列.ixx 长度 136482。
    任务模块.管理界面线程.impl.cpp 长度 411021。
    任务模块.管理工作线程.ixx 长度 205696。
    自我动作实现.外设模块.ixx 长度 1425484。
    鱼巢.vcxproj 长度 14818。
```

## 4. 当前函数分区

### 4.1 导出契约区

证据：

```text
外设线程_D455深度相机.ixx:34-39
    export struct 结构_D455深度相机线程配置

外设线程_D455深度相机.ixx:41-83
    export struct 结构_D455深度相机观察样本

外设线程_D455深度相机.ixx:85-102
    export struct 结构_D455深度相机线程结果

外设线程_D455深度相机.ixx:113-137
    export class 外设线程_D455深度相机类
    export 获取全局外设线程_D455深度相机
    export 启动外设线程_D455深度相机
    export 停止外设线程_D455深度相机
    export 读取外设线程_D455深度相机结果
```

判断：

```text
P1-P5 不应修改这些导出函数签名。
若必须修改导出结构字段，先限定为新增当前正式字段或唯一重命名，不允许新旧字段双写。
```

### 4.2 跨帧缓存区

证据：

```text
外设线程_D455深度相机.ixx:173-181
    结构_D455跟踪稳定缓存项：
        等待项ID、跟踪种子、最近中心、连续成功次数、失败次数。

外设线程_D455深度相机.ixx:184-220
    结构_D455跨帧观察簇缓存项：
        跨帧簇ID、最近中心/投影/空间范围、平滑中心/投影范围、
        连续命中次数、连续稳定命中次数、连续发布次数、
        最近复现离散度、稳定状态、待发布状态、丢失宽限计数、丢失次数。

外设线程_D455深度相机.ixx:394-514
    D455_跨帧簇可匹配：
        使用中心、投影范围、空间范围、像素数量和滞回容差做匹配。

外设线程_D455深度相机.ixx:551-690
    D455_更新逐簇识别报告跨帧采集状态：
        生成匹配、新建、稳定、待发布、丢失统计；
        将稳定状态写入簇.跨帧轨迹稳定状态。
```

判断：

```text
现有 tracker 状态仍是 0/1 型稳定状态 + 待发布状态 + 丢失宽限。
尚未形成 v0.2 要求的轨迹状态枚举：
    新建 / 候选 / 稳定 / 短时丢失 / 已丢失 / 遮挡 / 冲突。
P5 可从 结构_D455跨帧观察簇缓存项 和 结构_外设观察像素簇摘要 同步扩展。
```

### 4.3 逐簇识别报告区

证据：

```text
外设线程_D455深度相机.ixx:772-881
    D455_从调用结果生成逐簇识别报告：
        写入外设ID、观察运行模式、报告类型、帧号、句柄、坐标系版本、
        深度有效率、空间坐标有效率、未归簇/不可观测/未解释区域、观察像素簇集合。

外设线程_D455深度相机.ixx:857-872
    对投影有效簇写入：
        像素集合掩码句柄
        彩色轮廓局部图句柄
        深度轮廓局部图句柄
```

判断：

```text
现有字段有 坐标系版本，但没有主分割坐标系ID、Depth/IR/Color/Mask 分源坐标系ID。
RGB 彩色裁剪句柄和深度裁剪句柄同在簇摘要中，但尚未强制表达“显示补边轮廓不能覆盖主分割轮廓”。
```

### 4.4 L1 稳定子集报告区

证据：

```text
外设线程_D455深度相机.ixx:896-901
    D455_观察像素簇满足稳定子集发布门：
        只看跨帧轨迹稳定状态、连续稳定命中次数、复现离散度。

外设线程_D455深度相机.ixx:904-912
    D455_报告满足L1稳定子集口径：
        报告类型、报告层级、跨帧采集状态、跨帧稳定簇数量、簇集合非空。

外设线程_D455深度相机.ixx:925-971
    D455_从逐簇识别报告生成稳定观察子集报告：
        复制原始报告、报告层级设为 1、来源原始报告ID、来源外设帧ID、
        按稳定子集发布门过滤观察像素簇集合。
```

判断：

```text
现有 L1 是“稳定子集报告”，还没有区分：
    平面轮廓稳定材料
    深度锚定稳定材料
    空间可用稳定材料

现有稳定子集发布门没有 anchor 数量、anchor 密度、anchor 覆盖率。
anchor 不达标不得直接否定平面轮廓稳定材料的规则尚未进入代码。
```

### 4.5 目标观察约束与等待项区

证据：

```text
外设观察报告队列.ixx:476-500
    结构_外设观察等待项：
        观察运行模式、期望报告类型、目标区域或目标簇、目标观察约束ID、
        目标存在ID、目标特征类型ID、generation、允许降级匹配。

外设观察报告队列.ixx:502-557
    结构_目标观察约束特征组：
        ROI引用、像素集合掩码句柄、点集引用、二维范围、空间范围、
        来源报告ID、来源帧ID、generation、TTL、最低质量阈值。

外设线程_D455深度相机.ixx:1163-1178
    D455_写入报告目标观察约束字段：
        将等待项/目标约束 generation 写入报告，并按使用状态设置报告层级。

外设线程_D455深度相机.ixx:2021-2036
    D455_读取并分组等待项：
        只按观察运行模式分为扫描变化等待项集合和跟踪等待项集合。
```

判断：

```text
现有等待项分组没有 v0.2 要求的约束优先级合成。
P4 的最小安全改动应先新增外设分割约束调度辅助函数，输出本帧处理掩码/模式/优先级；
不能直接把扫描和跟踪等待项的出现顺序当作优先级。
```

### 4.6 扫描 / 跟踪报告区

证据：

```text
外设线程_D455深度相机.ixx:1343-1435
    D455_从逐簇识别报告生成扫描变化报告_带上下文：
        输入 L1 稳定子集材料、等待项、目标约束；
        首帧无基准时只写首帧基准差异摘要；
        有基准时按跨帧簇ID比较新增/消失/变化。

外设线程_D455深度相机.ixx:1491-1552
    D455_记录跟踪失败到缓存 / D455_记录跟踪成功到缓存：
        只维护连续成功次数、失败次数、跟踪状态值和丢失状态值。

外设线程_D455深度相机.ixx:1554-1600
    D455_从逐簇识别报告生成跟踪报告：
        输入 L1 稳定子集材料和等待项；
        写跟踪ID=等待项ID、跟踪种子和目标观察约束状态。
```

判断：

```text
扫描 / 跟踪报告已从 L1 稳定子集材料派生，这是正确边界。
但跟踪报告状态仍是跟踪状态值/丢失状态值，未覆盖短时丢失、已丢失、遮挡、冲突等完整状态。
```

### 4.7 线程主循环与外部入口

证据：

```text
外设线程_D455深度相机.ixx:2039-2114
    D455_发布本帧逐簇与稳定子集报告：
        逐簇识别报告入队、短期材料页提交、稳定子集报告入队或跳过。

外设线程_D455深度相机.ixx:2116-2249
    D455_发布扫描变化报告 / D455_发布目标跟踪报告：
        读取等待项集合后按报告类型发布。

外设线程_D455深度相机.ixx:2252-2328
    D455_线程主循环：
        启动相机、采集一帧、读取上一 L1 稳定子集、读取等待项分组、
        发布逐簇与稳定子集报告、发布扫描报告、发布跟踪报告。

任务模块.管理界面线程.impl.cpp:4331-4355
    私有_确保外设观察线程按等待项启动：
        D455 等待项触发外设线程启动。

鱼巢.vcxproj:150-158
    工程已包含 D455相机类、外设观察报告队列、外设线程_D455深度相机。
```

判断：

```text
外设线程_D455深度相机.ixx 当前仍是编排层 + 稳定算法 + 报告构造混合文件。
P6 才适合抽模块；P1-P5 不宜先拆工程文件。
```

## 5. 外设观察报告队列与承接链现状

证据：

```text
外设观察报告队列.ixx:191-220
    结构_外设观察像素簇摘要 已有跨帧簇ID、连续命中、连续稳定命中、
    连续发布、轨迹稳定状态、待发布状态、丢失宽限状态和平滑坐标。

外设观察报告队列.ixx:430-466
    结构_外设观察报告队列项 已有跨帧采集摘要、跟踪状态、目标观察约束、
    报告层级、来源原始报告ID、来源外设帧ID。

外设观察报告队列.ixx:566-577
    结构_观察材料质量判定 当前字段：
        可进入验证方法
        可进入识别方法
        可生成扫描变化候选
        可生成跟踪动态候选
        可进入提交入口组织

外设观察报告队列.ixx:1660-1786
    判断外设观察材料质量 当前质量门：
        检查可回查材料、簇集合、深度/空间质量、逐簇稳定硬门；
        设置 可进入识别方法、可生成扫描变化候选、可生成跟踪动态候选、可进入提交入口组织。

任务模块.管理工作线程.ixx:3747-3864
    任务管理按 判断外设观察材料质量 生成任务回执、观察事实承接请求或稳定特征承接请求。

自我动作实现.外设模块.ixx:11527-11593
    自我侧正式读取口径仍读取 判断外设观察材料质量(...).可进入提交入口组织。

自我动作实现.外设模块.ixx:20456-20563
    6145 识别路径读取正式报告后，同时使用 可进入识别方法 和 可进入提交入口组织 组织确认方案与失败原因。
```

判断：

```text
P1 的核心字段不是单纯显示文案，而是跨 外设观察报告队列 / 任务管理工作线程 / 自我动作实现.外设模块 的机器判断字段。
如果执行 P1，必须一次性把当前机器判断中的“提交入口组织”口径收束为“观察承接 / 可交付观察方法材料”口径；
不得保留新旧字段双写或旧字段别名。
```

## 6. P1 可安全改动字段清单

可作为 P1 首批字段的候选：

```text
外设观察报告队列.ixx:
    结构_观察材料质量判定::可进入提交入口组织
        建议改为：
            外设材料可承接状态
        或：
            可交付观察方法材料

    构造观察材料质量判定摘要 中的 “可进入提交入口组织”
        同步改为外设材料可承接/可交付观察方法材料显示口径。

    判断外设观察材料质量 中设置该字段的位置
        同步改为观察承接条件，不改变扫描/跟踪候选布尔字段。

任务模块.管理工作线程.ixx:
    读取 质量判定.可进入提交入口组织 的分支
        改为读取新字段；
        承接摘要避免写成“等待提交入口裁决”，改为“进入观察方法承接/观察事实承接请求草案”，
        但正式提交入口裁决仍保留在自我侧后续链。

自我动作实现.外设模块.ixx:
    正式报告读取 helper、6145 识别路径和相关摘要中的旧字段读取
        改为新字段；
        识别路径仍只能消费观察方法写出的外设观察存在及合法特征。
```

P1 不宜改动：

```text
启动外设线程_D455深度相机 / 停止外设线程_D455深度相机 / 读取外设线程_D455深度相机结果 签名。
报告ID、来源原始报告ID、来源外设帧ID、generation、TTL 的回查关系。
扫描变化候选、跟踪动态候选两个候选布尔字段。
```

## 7. 当前暂停风险点

### 风险 1：规范文本仍有旧“提交”口径

证据：

```text
规范/相机外设综合工作流程规范20260607.md
    仍出现“可提交观察材料”“外设观察存在提交状态”旧口径。

规范/观察特征质量诊断与认知补偿规范20260525.md
    仍出现“外设观察存在提交状态 == 可提交”旧口径。

规范/观察像素簇与存在候选分层规范20260527.md
    仍出现“可提交观察材料”旧口径。

流程图/20260614_D455稳定轮廓项目适配流程图_v0.2.md
    已要求“提交状态”从外设侧删除，改为“外设材料可承接状态 / 稳定材料可交付状态 / 可交付观察方法材料”。
```

当前判断：

```text
P1 若直接改代码，会与部分仍未修订的规范旧术语发生口径不一致。
按 AGENTS.md 规范优先规则，进入 P1 前应先确认：
    A. 先修订相关 040011 / 050002 / 050004 规范术语；
    或
    B. 用户明确批准本轮代码先按 v0.2 流程图收束，规范术语后续单列切片修订。
```

### 风险 2：旧字段已经进入多处机器判断

证据：

```text
rg -n "可进入提交入口组织" 外设线程_D455深度相机.ixx 外设观察报告队列.ixx 任务模块.管理工作线程.ixx 自我动作实现.外设模块.ixx

命中机器判断位置：
    外设观察报告队列.ixx:572
    外设观察报告队列.ixx:1757-1760
    外设观察报告队列.ixx:1798
    任务模块.管理工作线程.ixx:3760
    自我动作实现.外设模块.ixx:11573
    自我动作实现.外设模块.ixx:11592
    自我动作实现.外设模块.ixx:20529
    自我动作实现.外设模块.ixx:20562
```

当前判断：

```text
P1 不能只做局部替换或日志替换。
如果执行，必须在同一切片内完整更新字段定义、质量判定、任务管理读取、自我动作正式读取与摘要输出。
若发现更多机器判断路径，必须先记录清单，不得新增兼容别名。
```

## 8. P0 结论

```text
1. 不删除 外设线程_D455深度相机.ixx 是正确路线。
2. 当前文件可以按现有基础切片重构：
       P1 先收束质量判定字段命名；
       P2 再加材料能力等级；
       P3 再补坐标系与 anchor；
       P4 再做约束优先级调度；
       P5 再扩展 tracker 状态；
       P6 最后抽模块。
3. P1 的直接代码入口已定位，但存在规范旧术语未同步和机器字段跨模块使用两个风险。
4. 本轮不改代码，不构建。
```

## 9. 下一步

```text
2026-06-14 用户选择方案 A。

已修订相关规范旧“提交”术语：
    规范/相机外设综合工作流程规范20260607.md
    规范/观察特征质量诊断与认知补偿规范20260525.md
    规范/观察像素簇与存在候选分层规范20260527.md
    规范/规范目录.md

复扫命令：
    rg -n "可提交观察材料|外设观察存在提交状态|提交状态|稳定外设观察存在" 规范/相机外设综合工作流程规范20260607.md 规范/观察特征质量诊断与认知补偿规范20260525.md 规范/观察像素簇与存在候选分层规范20260527.md 规范/规范目录.md

复扫结果：
    无命中。

下一步进入 P1 代码字段收口，最小改动范围为：
    外设观察报告队列.ixx
    任务模块.管理工作线程.ixx
    自我动作实现.外设模块.ixx

本轮停止点：
    P0 只读审计与断点已完成。
    P1 前置规范术语修订已完成。
    尚未进入 P1 代码修改。
```

## 10. 禁止宣称

```text
不宣称 D455 稳定轮廓重构已完成。
不宣称 P4 已开始或已完成。
不宣称稳定外设观察材料已经等于外设观察存在。
不宣称识别 / 扫描 / 跟踪边界已经全部代码闭合。
不宣称 D455 自然业务样本已经通过。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 11. P1 代码字段收口构建前记录

```text
时间：
    2026-06-14

本轮目标：
    将 `结构_观察材料质量判定::可进入提交入口组织`
    收束为 `可交付观察方法材料`。

已修改：
    外设观察报告队列.ixx
        字段定义、质量判定赋值、质量摘要输出。

    任务模块.管理工作线程.ixx
        逐簇识别报告质量门读取。
        预承接摘要从“等待提交入口裁决”改为“等待自我侧观察承接 / 结果分流”。

    自我动作实现.外设模块.ixx
        正式逐簇报告读取口径。
        识别、确认观察存在事实、对应事实桥的材料质量门读取。
        来源观察材料质量日志口径从“提交质量依据”改为“观察材料质量依据”。

已运行检查：
    rg -n "可进入提交入口组织|提交入口组织质量|提交入口组织正式对象|可组织正式对象|外设观察存在提交状态|可提交观察材料" -g "*.ixx" -g "*.cpp" -g "*.h"
        无命中。

    rg -n "可交付观察方法材料" -g "*.ixx" -g "*.cpp" -g "*.h"
        命中集中在：
            外设观察报告队列.ixx
            任务模块.管理工作线程.ixx
            自我动作实现.外设模块.ixx

    git diff --check -- 外设观察报告队列.ixx 任务模块.管理工作线程.ixx 自我动作实现.外设模块.ixx
        exit_code=0。

构建前预测：
    Debug x64 构建应通过。
    若失败，优先检查字段重命名遗漏、局部变量重命名遗漏或模块导出重建顺序。
```

## 12. P1 验收结果

```text
时间：
    2026-06-14

字段收口结果：
    `结构_观察材料质量判定::可进入提交入口组织`
        -> `结构_观察材料质量判定::可交付观察方法材料`

旧字段 / 旧材料提交口径复扫：
    rg -n "可进入提交入口组织|提交入口组织质量|提交入口组织正式对象|可组织正式对象|外设观察存在提交状态|可提交观察材料" -g "*.ixx" -g "*.cpp" -g "*.h"
        exit_code=1，无命中。

新字段复扫：
    rg -n "可交付观察方法材料" -g "*.ixx" -g "*.cpp" -g "*.h"
        命中集中在：
            外设观察报告队列.ixx
            任务模块.管理工作线程.ixx
            自我动作实现.外设模块.ixx

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        exit_code=0。
        仅 LNK4075：/EDITANDCONTINUE 被 /INCREMENTAL:NO 忽略。
        构建日志：日志/20260614_D455_P1_代码字段收口_msbuild.log

P1 判断：
    P1 代码字段收口已闭合。
    未修改 D455 外设线程导出函数签名。
    未新增新旧字段双写或旧字段别名。
    仍未进入 P2 稳定材料能力等级实现。
```

## 13. P2 稳定材料能力等级实施前记录

```text
时间：
    2026-06-14

当前目标：
    在外设报告 / 质量判定层显式拆出：
        平面轮廓稳定状态
        深度锚定状态
        空间可用状态
        外设材料可承接状态
        可承接能力等级

当前代码证据：
    外设观察报告队列.ixx
        结构_外设观察像素簇摘要已有 ROI / 像素集合掩码句柄 / 中心 / 深度有效率 / 空间坐标有效率 / 跨帧稳定字段。
        结构_外设观察报告队列项已有 报告层级 / 跨帧稳定观察像素簇数量 / 深度有效率 / 空间坐标有效率。
        结构_观察材料质量判定已有 等级 / 可交付观察方法材料，但未显式输出能力等级。

    外设线程_D455深度相机.ixx
        D455_从逐簇识别报告生成稳定观察子集报告 已按跨帧稳定门过滤 L1 稳定子集。
        当前没有在 L1 报告上写平面 / 深度 / 空间能力状态。

最小实现判断：
    不新增世界树事实字段。
    不修改 `特征值主信息类`。
    不删除或重写 `外设线程_D455深度相机.ixx`。
    先把能力等级作为外设报告与质量判定结构字段输出。
    anchor 三指标仍留给 P3；P2 只确保深度不足不会直接否定平面轮廓稳定材料。

构建前预测：
    新增字段仅由同一模块和 D455 线程写读，Debug x64 构建应通过。
```

## 14. P2 验收结果

```text
时间：
    2026-06-14

代码修改：
    外设观察报告队列.ixx
        新增 `枚举_外设材料能力等级`：
            候选或缺口 / 平面轮廓 / 深度锚定 / 空间可用。
        `结构_外设观察报告队列项` 新增：
            平面轮廓稳定状态
            深度锚定状态
            空间可用状态
            外设材料可承接状态
            可承接能力等级
        `结构_观察材料质量判定` 同步新增同名能力判定字段。
        `判断外设观察材料质量` 允许平面轮廓稳定材料在深度 / 空间不足时保持可用，
        并将 `可交付观察方法材料` 改为依据外设材料可承接状态。

    外设线程_D455深度相机.ixx
        `D455_从逐簇识别报告生成稳定观察子集报告` 写入 L1 稳定子集能力状态。
        `D455_复制观察报告帧摘要` 复制能力状态到扫描 / 跟踪派生报告。
        入队短摘要输出可承接能力等级与平面 / 深度 / 空间状态。

静态检查：
    rg -n "可进入提交入口组织|提交入口组织质量|可提交观察材料|外设观察存在提交状态" -g "*.ixx" -g "*.cpp" -g "*.h"
        exit_code=1，无命中。

    git diff --check -- 外设观察报告队列.ixx 外设线程_D455深度相机.ixx 实施记录/20260614_D455稳定轮廓模块重构_Codex断点清单.md
        exit_code=0。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        exit_code=0。
        仅 LNK4075：/EDITANDCONTINUE 被 /INCREMENTAL:NO 忽略。
        构建日志：日志/20260614_D455_P2_材料能力等级_msbuild.log

P2 判断：
    单帧质量、跨帧稳定和可承接能力等级已可分别输出。
    深度 / 空间不足不再直接否定平面轮廓稳定材料。
    anchor 数量 / 密度 / 覆盖率和坐标系分源 ID 尚未实现，进入 P3。
```

## 15. P3 坐标系与 anchor 三指标实施前记录

```text
时间：
    2026-06-14

当前目标：
    补齐 D455 稳定材料的坐标系声明和 anchor 数量 / 密度 / 覆盖率三指标。

当前代码证据：
    D455相机类.cpp
        使用 rs2::align(RS2_STREAM_COLOR)，rawDepth 与 color 进入同一尺寸。
        `输出.深度已对齐到彩色` 已存在。
        IR 只读取为原始红外图，未映射为强边界材料。

    双目相机本能适配器.h
        调用结果已有：
            彩色深度已对齐
            对齐目标
            深度稳定性平均值
            深度邻域一致性平均值
            融合低稳定像素数量
            空间候选有效点数量
        空间候选摘要已有：
            像素数量
            有效点比例
            轮廓支持像素数
            轮廓深度支持率

    外设观察报告队列.ixx
        当前只有 `坐标系版本`，缺少主分割坐标系ID、分源坐标系ID、一致性状态和 anchor 三指标。

最小实现判断：
    主分割坐标系先表达为 `D455/color-aligned-depth/v1`。
    depth / RGB crop / mask 标记为已对齐到主分割坐标系。
    Left IR 当前未参与强分割，标记为未参与 / 弱提示，不降低主坐标系一致性。
    anchor 三指标先用稳定子集内有效深度 / 空间支持点估算：
        数量 = 像素数量 * 有效点比例 / 10000。
        密度 / 覆盖率 = 万分比。
        边界 anchor 覆盖率优先用候选轮廓深度支持率，否则用有效点比例。

硬规则：
    坐标系一致性状态 != 一致，不得生成空间可用稳定材料。

构建前预测：
    新增字段仍限于外设报告 / D455 线程，Debug x64 构建应通过。
```

## 16. P3 验收结果

```text
时间：
    2026-06-14

代码修改：
    外设观察报告队列.ixx
        新增坐标对齐状态与坐标系一致性状态枚举。
        `结构_外设观察像素簇摘要` 新增：
            稳定锚点数量
            稳定锚点密度
            稳定锚点覆盖率
            边界锚点覆盖率
        `结构_外设观察报告队列项` 新增：
            主分割坐标系ID
            深度材料坐标系ID
            左红外边缘坐标系ID
            RGB裁剪材料坐标系ID
            像素掩码坐标系ID
            分源坐标对齐状态
            坐标系一致性状态
            anchor 三指标与边界 anchor 覆盖率
        `判断外设观察材料质量` 增加：
            anchor 数量 + 密度 + 覆盖率三指标判定。
            `坐标系一致性状态 == 一致` 才允许空间可用。

    外设线程_D455深度相机.ixx
        L0 逐簇识别报告写入主分割坐标系 `D455/color-aligned-depth/v1`。
        Depth / RGB / Mask 标记为对齐到主分割坐标系。
        Left IR 标记为未参与强分割。
        L1 稳定子集聚合稳定 anchor 指标，并用三指标决定深度锚定。

静态检查：
    rg -n "可进入提交入口组织|提交入口组织质量|可提交观察材料|外设观察存在提交状态" -g "*.ixx" -g "*.cpp" -g "*.h"
        exit_code=1，无命中。

    git diff --check -- 外设观察报告队列.ixx 外设线程_D455深度相机.ixx 实施记录/20260614_D455稳定轮廓模块重构_Codex断点清单.md
        exit_code=0。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        exit_code=0。
        仅 LNK4075：/EDITANDCONTINUE 被 /INCREMENTAL:NO 忽略。
        构建日志：日志/20260614_D455_P3_坐标系_anchor_msbuild.log

P3 判断：
    材料摘要可以说明坐标系状态和 anchor 三指标。
    空间可用不再只看深度或 anchor 数量，必须坐标系一致。
    P4 尚未开始：外设分割约束优先级调度仍未实现。
```

## 17. P4 外设分割约束调度前检查

```text
时间：
    2026-06-14

当前目标：
    补齐目标观察约束 / 外设分割约束到本帧处理掩码、处理模式和处理优先级的最小通路。

已复核依据：
    计划/20260614_D455稳定轮廓模块重构计划_v0.1.md
        P4 要求等待项、目标约束结构、D455 等待项读取与分组输出调度字段。
    流程图/20260614_D455稳定轮廓项目适配流程图_v0.2.md
        P4 约束图调度字段为本帧处理掩码 / 本帧处理模式 / 本帧处理优先级。
    规范/相机外设综合工作流程规范20260607.md
        目标观察约束、外设等待项和报告均只是外设供料 / 通信容器，不是世界事实主体。

当前代码证据：
    外设观察报告队列.ixx
        已有 `枚举_外设分割处理模式`。
        等待项 / 报告项已有 `本帧处理模式`、`本帧处理优先级`、`本帧处理掩码句柄`。

    任务模块.管理界面线程.impl.cpp
        `私有_按方法供料要求构造外设观察等待项` 已写入 P4 字段。
        独立问题锚点：
            `私有_按稳定子集全量扫描约束提交外设等待项`
            批量扫描等待项当前只写目标观察约束字段，尚未写入本帧处理模式 / 优先级 / 掩码句柄。
            结果会退回等待项默认值 `全局观察 / 50 / 空`，不符合 P4 扫描候选调度口径。

    外设线程_D455深度相机.ixx
        `D455_写入报告目标观察约束字段` 已把等待项 P4 字段带入报告。
        当前短摘要尚未输出 P4 字段，日志验收不够直接。

已排除路径：
    本切片不改变识别 / 扫描 / 跟踪正式承接逻辑。
    本切片不把目标观察约束、等待项、报告项写成世界事实主体。
    本切片不新增外显观察模式或本能方法。

最小补丁：
    1. 批量扫描等待项写入：
        本帧处理模式 = 扫描候选
        本帧处理优先级 = 30
        本帧处理掩码句柄 = 目标观察约束.像素集合掩码句柄
    2. 批量扫描等待项摘要追加 P4 字段。
    3. D455 报告 / 等待项短摘要追加 P4 字段。

构建前预测：
    仅追加结构字段赋值和日志摘要，不改变运行入口。
    Debug x64 构建应通过；若失败，优先检查枚举 / 摘要函数可见性。
```

## 18. P4 验收结果

```text
时间：
    2026-06-14

代码修改：
    任务模块.管理界面线程.impl.cpp
        `私有_按稳定子集全量扫描约束提交外设等待项` 已补齐：
            本帧处理模式 = 扫描候选
            本帧处理优先级 = 30
            本帧处理掩码句柄 = 目标观察约束.像素集合掩码句柄
        批量扫描等待项质量摘要已输出本帧处理模式 / 优先级 / 掩码。

    外设线程_D455深度相机.ixx
        D455 报告短摘要已输出本帧处理模式 / 优先级 / 掩码。
        D455 等待项短摘要已输出本帧处理模式 / 优先级 / 掩码。

    外设观察报告队列.ixx
        复核已有等待项 / 报告项字段与完整摘要字段，无新增补丁。

静态检查：
    git diff --check -- 外设观察报告队列.ixx 外设线程_D455深度相机.ixx 任务模块.管理界面线程.impl.cpp 实施记录/20260614_D455稳定轮廓模块重构_Codex断点清单.md
        exit_code=0。

    rg -n "兼容|fallback|旧字段|旧入口|旧流程|错误码|本能动作错误_|return .*失败|执行失败|筹办失败|上报失败|生成失败" -g "*.cpp" -g "*.h" -g "*.ixx"
        exit_code=0。
        scan_lines=433。
        日志：日志/20260614_D455_P4_禁止性扫描.txt
        与本切片新增行相关命中=0；当前命中为历史兼容 / 错误码 / 文本显示锚点，未作为 P4 新机器判断依据。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        exit_code=0。
        仅 LNK4075：/EDITANDCONTINUE 被 /INCREMENTAL:NO 忽略。
        构建日志：日志/20260614_D455_P4_分割约束调度_msbuild.log

P4 判断：
    普通按方法供料等待项、批量稳定子集扫描等待项、D455 生成报告三条路径均可携带本帧处理模式 / 优先级 / 掩码。
    同一区域同时命中扫描和跟踪时，等待项 / 报告日志可直接看到目标跟踪优先级 10 与扫描候选优先级 30。
    无约束路径仍保持全局观察 / 50 / 空，不把空约束判为失败。
    未改变正式识别 / 扫描 / 跟踪承接逻辑；未把目标观察约束、等待项或报告项写成世界事实主体。

下一步：
    进入 P5 Tracker 状态扩展与报告构造瘦身前检查。
```

## 19. P5 Tracker 状态扩展实施前记录

```text
时间：
    2026-06-14

当前目标：
    将 D455 跨帧缓存和目标跟踪报告中的分散数值状态整理为材料级 Tracker 轨迹状态输入。

已复核依据：
    计划/20260614_D455稳定轮廓模块重构计划_v0.1.md
        P5 要求新增或整理轨迹状态、轨迹状态 generation、连续帧数、状态变化原因、状态变化动态候选。
        验收要求：短时丢失不等于消失；遮挡不等于消失；冲突进入复验口径。
    流程图/20260614_D455稳定轮廓项目适配流程图_v0.2.md
        Tracker 状态应进入稳定材料结构，供观察承接、补观察、复验和跟踪恢复判断使用。

当前代码证据：
    外设观察报告队列.ixx
        已有 `枚举_跟踪状态`，但它属于提交包跟踪状态，缺少 P5 要求的外设材料级 `新建 / 候选 / 稳定 / 短时丢失 / 已丢失 / 遮挡 / 冲突` 状态。
        `结构_外设观察报告队列项` 目前只有：
            跟踪状态值
            丢失状态值
            连续成功次数
            失败次数
            重捕获候选数量
            稳定特征样本数量

    外设线程_D455深度相机.ixx
        `结构_D455跨帧观察簇缓存项` 已有连续命中、连续稳定命中、连续发布、稳定状态、待发布状态、丢失宽限计数和丢失次数。
        `D455_更新逐簇识别报告跨帧采集状态` 已能区分新建、匹配、稳定、待发布和丢失数量。
        `D455_记录跟踪失败到缓存` / `D455_记录跟踪成功到缓存` 已能区分跟踪成功、连续成功和失败次数。

独立问题锚点：
    Tracker 状态目前散落在数值字段和日志摘要中，缺少一个可由材料承接层直接读取的枚举状态。

最小补丁：
    1. 新增外设材料级 Tracker 轨迹状态枚举和文本函数。
    2. 观察像素簇摘要新增轨迹状态、generation、连续帧数、变化原因、变化动态候选。
    3. 报告项新增同名材料级字段。
    4. D455 跨帧缓存更新时填充簇级轨迹状态：
        新建、候选、稳定、短时丢失、已丢失。
    5. D455 跟踪报告成功 / 失败时填充报告级轨迹状态：
        稳定、候选、短时丢失、已丢失。
    6. 摘要输出轨迹状态字段，便于日志验证。

边界：
    本切片不生成正式动作动态。
    `轨迹状态变化动态候选` 只作为材料候选说明，不作为已发生动作动态或世界事实。
    暂无明确遮挡 / 冲突识别证据时，不伪造遮挡或冲突状态；枚举和值域先进入材料结构。

构建前预测：
    改动限于外设观察报告队列结构、D455 材料填充和摘要输出。
    Debug x64 构建应通过。
```

## 20. P5 验收结果

```text
时间：
    2026-06-14

代码修改：
    外设观察报告队列.ixx
        新增外设材料级 `枚举_外设Tracker轨迹状态`：
            新建 / 候选 / 稳定 / 短时丢失 / 已丢失 / 遮挡 / 冲突。
        `结构_外设观察像素簇摘要` 新增：
            轨迹状态
            轨迹状态generation
            轨迹状态连续帧数
            轨迹状态变化原因
            轨迹状态变化动态候选
        `结构_外设观察报告队列项` 新增同名字段。
        `外设提交_跟踪状态_由报告` 优先按材料级轨迹状态推导提交包跟踪状态。
        报告摘要输出轨迹状态和轨迹连续帧数；跟踪报告摘要输出 generation / 变化原因 / 动态候选。

    外设线程_D455深度相机.ixx
        `D455_更新逐簇识别报告跨帧采集状态` 将跨帧簇状态写成：
            新建 / 候选 / 稳定 / 短时丢失 / 已丢失。
        `D455_记录跟踪失败到缓存` 按失败次数和丢失宽限区分短时丢失与已丢失。
        `D455_记录跟踪成功到缓存` 按连续成功、复现离散度和质量输出候选或稳定。
        D455 报告短摘要输出轨迹状态和轨迹连续帧数。

修正记录：
    首次构建失败：
        日志/20260614_D455_P5_Tracker状态_msbuild.log
        exit_code=1。
        原因：误读 `结构_外设观察像素簇摘要` 存在 `采集时间毫秒` 字段。
    修正：
        簇级 generation 改为由报告时间戳传入，不新增簇时间字段。

静态检查：
    git diff --check -- 外设观察报告队列.ixx 外设线程_D455深度相机.ixx 实施记录/20260614_D455稳定轮廓模块重构_Codex断点清单.md
        exit_code=0。

    rg -n "兼容|fallback|旧字段|旧入口|旧流程|错误码|本能动作错误_|return .*失败|执行失败|筹办失败|上报失败|生成失败" -g "*.cpp" -g "*.h" -g "*.ixx"
        exit_code=0。
        scan_lines=433。
        日志：日志/20260614_D455_P5_禁止性扫描.txt
        本切片两个代码文件相关命中=0。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        首次 exit_code=1，已修正。
        复跑 exit_code=0。
        仅 LNK4075：/EDITANDCONTINUE 被 /INCREMENTAL:NO 忽略。
        构建日志：日志/20260614_D455_P5_Tracker状态_msbuild.log

P5 判断：
    Tracker 状态已进入观察像素簇材料和报告项材料结构。
    短时丢失与已丢失用不同枚举表达，不把短时丢失当成消失。
    遮挡 / 冲突枚举已进入材料状态值域；当前没有明确遮挡 / 冲突证据时不伪造状态。
    本切片未生成正式动作动态，`轨迹状态变化动态候选` 只作为材料候选说明。

下一步：
    进入 P6 外设线程编排层收缩前检查。
```

## 21. P6 外设线程编排层收缩实施前记录

```text
时间：
    2026-06-14

当前目标：
    在不新增模块、不改工程文件的前提下，先收缩 `D455_线程主循环` 的报告发布编排。

已复核依据：
    计划/20260614_D455稳定轮廓模块重构计划_v0.1.md
        P6 要求外设线程逐步只保留导出契约、线程生命周期、采样循环、等待项读取和报告发布编排。

当前代码证据：
    外设线程_D455深度相机.ixx
        已有独立函数：
            D455_发布本帧逐簇与稳定子集报告
            D455_发布扫描变化报告
            D455_发布目标跟踪报告
        `D455_线程主循环` 当前仍直接串联：
            读取上一稳定子集报告
            读取并分组等待项
            发布逐簇 / 稳定子集报告
            发布扫描变化报告
            发布目标跟踪报告
            将逐簇报告摘要写回样本

最小补丁：
    新增局部 helper：
        `D455_发布采集成功后的报告编排`
    该 helper 负责采集成功后的等待项读取、报告组发布、扫描 / 跟踪发布，并返回本帧逐簇识别报告。
    `D455_线程主循环` 保留采样循环、样本构造、样本入队、状态评估和等待间隔。

边界：
    不新增 `.ixx` 模块。
    不修改 `鱼巢.vcxproj`。
    不改变报告ID、来源原始报告ID、generation、TTL 或报告发布顺序。
    不改变 D455 扫描 / 跟踪是否发布的判断。

构建前预测：
    纯函数抽取，外部调用点无需修改。
    Debug x64 构建应通过。
```

## 22. P6 验收结果

```text
时间：
    2026-06-14

代码修改：
    外设线程_D455深度相机.ixx
        新增 `D455_发布采集成功后的报告编排`。
        该 helper 承接：
            读取上一 L1 稳定子集报告
            读取并分组 D455 等待项
            发布逐簇 / 稳定子集报告
            发布扫描变化报告
            发布目标跟踪报告
            返回本帧逐簇识别报告
        `D455_线程主循环` 中采集成功后的报告发布流水线收束为一次 helper 调用。

边界复核：
    未新增 `.ixx` 模块。
    未修改 `鱼巢.vcxproj`。
    未改变报告ID、来源原始报告ID、generation、TTL 或报告发布顺序。
    未改变扫描 / 跟踪是否发布的判断。

静态检查：
    git diff --check -- 外设线程_D455深度相机.ixx 实施记录/20260614_D455稳定轮廓模块重构_Codex断点清单.md
        exit_code=0。

    rg -n "兼容|fallback|旧字段|旧入口|旧流程|错误码|本能动作错误_|return .*失败|执行失败|筹办失败|上报失败|生成失败" -g "*.cpp" -g "*.h" -g "*.ixx"
        exit_code=0。
        scan_lines=433。
        日志：日志/20260614_D455_P6_禁止性扫描.txt
        本切片代码新增行相关命中=0。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        exit_code=0。
        仅 LNK4075：/EDITANDCONTINUE 被 /INCREMENTAL:NO 忽略。
        构建日志：日志/20260614_D455_P6_编排层收缩_msbuild.log

P6 判断：
    主循环已减少一段采集成功后的内联报告编排。
    本轮选择不新增模块，避免引入工程文件和 import 关系风险。
    P6 作为第一轮编排层收缩闭合，后续若继续拆模块需单独计划工程文件同步。

下一步：
    进入 P7 端到端验证与收束。
```

## 23. P7 验证前断点

```text
时间：
    2026-06-14

当前切片：
    P7 端到端验证与收束。

依据文件：
    计划/20260614_D455稳定轮廓模块重构计划_v0.1.md
        P7 要求验证 D455 外设供料链、任务管理承接链和自我侧观察 / 识别 / 扫描 / 跟踪边界。
    流程图/20260614_D455稳定轮廓项目适配流程图_v0.2.md
        第 456-467 行列出 P7 边界检查项。

进入状态：
    git status --short --untracked-files=no
        exit_code=0。
        tracked 工作区干净。

本轮验证计划：
    1. 静态边界扫描：
           日志/20260614_D455_P7_边界静态扫描.txt
           日志/20260614_D455_P7_承接边界扫描.txt
           日志/20260614_D455_P7_结构能力扫描.txt
           日志/20260614_D455_P7_禁止性扫描.txt
    2. 最低构建验证：
           日志/20260614_D455_P7_收束_msbuild.log
    3. 真实 D455 运行入口：
           只使用仓库内明确存在的真实采样 / capture-once / 自然业务入口。
           若未找到可直接执行的真实入口，不用模拟样本或伪造入口替代。

禁止宣称：
    不宣称自我苏醒完成。
    不宣称初步成熟完成。
    不把构建通过等同于真实 D455 业务样本通过。
```

## 24. P7 验收结果

```text
时间：
    2026-06-14

静态边界扫描：
    日志/20260614_D455_P7_边界静态扫描.txt
        exit_code=0。
        scan_lines=23。
        缩小到 `外设线程_D455深度相机.ixx` 后：
            稳定存在 / 已确认存在 / 世界存在 / 提交状态 / 可提交观察材料 / 可进入提交入口组织 /
            提交入口组织质量 / 外设观察存在提交状态 命中=0。
        旧正式名：
            可提交观察材料 / 可进入提交入口组织 / 提交入口组织质量 / 外设观察存在提交状态 命中=0。
        剩余命中为历史通用协议或边界说明：
            `外设观察报告队列.ixx` 中 `已确认存在ID` 属扫描 / 跟踪提交包缺口码和历史宿主类型；
            `自我动作实现模块.ixx` 中 `OR组令牌提交状态` 属 OR 组令牌；
            `任务模块.管理界面线程.impl.cpp` 中 `提交状态` 属任务状态提交或历史结果特征匹配。
            本轮未把这些命中判为 D455 当前输出命名问题。

    日志/20260614_D455_P7_承接边界扫描.txt
        exit_code=0。
        scan_lines=59。
        静态证据覆盖：
            提交外设观察报告；
            按等待项匹配外设观察报告；
            任务管理生成观察事实承接上行消息；
            D455 逐簇 / 稳定子集 / 扫描 / 跟踪报告发布入口。
        本入口只证明代码路径存在，不等同于自然等待项已动态命中。

    日志/20260614_D455_P7_结构能力扫描.txt
        exit_code=0。
        scan_lines=255。
        命中结构证据：
            平面轮廓稳定状态；
            深度锚定状态；
            空间可用状态；
            主分割坐标系ID；
            坐标系一致性状态；
            稳定锚点密度 / 覆盖率；
            本帧处理模式 / 优先级 / 掩码句柄；
            Tracker 轨迹状态枚举和报告字段；
            D455_发布采集成功后的报告编排。

    日志/20260614_D455_P7_禁止性扫描.txt
        exit_code=0。
        scan_lines=433。
        P7 未改代码；命中集合未指向本轮新增代码。

构建：
    git diff --check -- 实施记录/20260614_D455稳定轮廓模块重构_Codex断点清单.md 计划/计划索引.md 计划/20260614_D455稳定轮廓模块重构计划_v0.1.md
        exit_code=0。
        仅工作区换行提示。

    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        exit_code=0。
        日志：日志/20260614_D455_P7_收束_msbuild.log
        关键输出：已成功生成。

真实运行：
    .\x64\Debug\鱼巢.exe --d455-capture-once
        exit_code=0。
        日志：日志/20260614_D455_P7_真实运行_d455_capture_once.log
        关键证据：
            单帧采集/打开：成功=是，相机已打开=是，原因=无，消息=D455 已可用。
            单帧采集/采集：成功=是，尺寸=640x480，观察帧组帧数=3，空间候选数量=12，帧质量评分=8081。
            单帧采集/材料包：报告ID=1，报告类型=逐簇识别报告，材料页=D455材料页#报告1。
            材料包包含主分割坐标系ID、坐标系一致性状态、稳定锚点数量 / 密度 / 覆盖率、
            平面轮廓稳定状态、深度锚定状态、空间可用状态。
            材料包包含 D455逐簇识别样本卡 和 D455扫描变化样本卡。
            边界文本包含：
                只读边界=材料归属和存在候选,不确认世界存在。
                只读边界=扫描变化材料,不确认全场景明确,不写安全结论。
            单帧采集/释放：成功=是，相机已打开=否，原因=无，消息=D455 已释放。

P7 判断：
    P1-P6 代码切片已通过静态边界扫描、Debug x64 构建和真实 D455 capture-once 采样验证。
    当前真实运行覆盖真实相机打开 / 采集 / 单帧材料包证据卡生成 / 释放。
    当前真实运行未覆盖任务管理等待项自然命中、外设线程持续采样中的稳定子集入队、
    观察事实承接请求动态生成。
    不使用模拟样本、伪造入口或伪造等待项补齐上述动态样本。

收束处理：
    D455 稳定轮廓模块重构代码切片收束。
    剩余动态验收条件为真实外设自然业务样本：
        等待 D455 等待项自然命中；
        复核逐簇识别报告入队、稳定子集报告入队、来源报告可回查、
        任务管理按等待项匹配、观察事实承接请求；
        复核稳定材料仍不直连识别 / 扫描 / 跟踪正式逻辑。
    计划移入 `计划/待完成项/`，不得误归为完成项。

禁止宣称：
    不宣称 D455 自然业务样本已经通过。
    不宣称识别 / 扫描 / 跟踪边界已经全部动态闭合。
    不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 25. 用户指定重建 D455 外设线程模块切片

时间：2026-06-14

当前切片：
    用户已将 `外设线程_D455深度相机.ixx` 改名为
    `旧_外设线程_D455深度相机.ixx`，并要求重建新的
    `外设线程_D455深度相机.ixx`，把 `D:\D455` 项目中已实现的图像分割代码迁移进新文件。

已复核现状：
    新旧两个 `外设线程_D455深度相机.ixx` 文件初始字节数一致；
    `git diff -- 外设线程_D455深度相机.ixx` 初始为空，说明新文件当时尚未形成有效迁移差异。
    `D:\D455\D455.cpp` 的分割链路包含 OpenCV / PCL 依赖；
    `鱼巢.vcxproj` 当前仅链接 `realsense2.lib`，没有 OpenCV / PCL 链接项。

本切片边界：
    不把 OpenCV / PCL 依赖直接并入鱼巢工程。
    不让 D455 外设报告直接写世界树、需求树、安全值或服务值。
    迁移方式限定为：
        在 `外设线程_D455深度相机.ixx` 中按 `D:\D455` 的
        深度切片、连通材料、边界过滤、空间摘要、稳定锚点口径
        新建无额外库依赖的候选生成层；
        报告和短期材料页必须共用该候选副本，保证
        `d455://.../candidate-N` 材料句柄仍可回查像素集合。

允许修改：
    外设线程_D455深度相机.ixx
    实施记录/20260614_D455稳定轮廓模块重构_Codex断点清单.md

实现：
    `外设线程_D455深度相机.ixx` 已新增无额外库依赖的 D455 项目分割迁移层：
        滤波深度优先；
        深度范围 250-3500mm；
        深度切片 300mm；
        最小材料面积 900px；
        最大材料数 24；
        连通材料、触边过滤、超大区域过滤、空间 / 深度摘要、轮廓置信度和缺口计数均在 module 内完成。
    持续线程采集成功后先调用迁移层，再进入逐簇报告 / 稳定子集 / 材料页链。
    `提交D455单帧材料包证据卡` 已先复制来源结果并重建分割候选，再生成报告和短期材料页。

验证：
    git diff --check -- 外设线程_D455深度相机.ixx 实施记录/20260614_D455稳定轮廓模块重构_Codex断点清单.md
        exit_code=0。
        仅 Git CRLF 提示；无空白错误。

    rg -n "兼容|fallback|旧字段|旧入口|旧流程|错误码|本能动作错误_|return .*失败|执行失败|筹办失败|上报失败|生成失败" 外设线程_D455深度相机.ixx
        exit_code=1。
        本切片新增模块文件无禁止性关键词命中。

    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        exit_code=0。
        日志：日志/20260614_D455_迁移分割_msbuild_2.log
        关键输出：已成功生成；1 个既有 LNK4075；0 个错误。

    .\x64\Debug\鱼巢.exe --d455-capture-once
        exit_code=0。
        日志：日志/20260614_D455_迁移分割_capture_once_2.log
        关键证据：
            单帧材料包报告ID=1。
            观察簇=15。
            ROI句柄=15。
            掩码句柄=15。
            点集句柄=15。
            深度切片句柄=15。
            候选存在=15。
            候选材料句柄=105。
            空间候选引用=15。
            短期观察材料页提交=1。
            只读边界仍为：材料归属和存在候选，不确认世界存在。

当前判断：
    新 `外设线程_D455深度相机.ixx` 已形成有效迁移差异。
    `D:\D455` 的核心分割口径已迁到鱼巢外设线程 module 内部：
        深度切片候选生成；
        连通材料过滤；
        空间 / 深度摘要；
        候选像素集合保存；
        报告与短期材料页共用同一份候选副本。
    本轮未把 OpenCV / PCL 依赖并入鱼巢工程，未改变外设侧和自我侧权责边界。

未覆盖：
    本轮只验证真实单帧材料包路径。
    未验证持续外设线程自然等待项命中、L1 稳定子集自然入队和观察事实承接请求动态生成。
