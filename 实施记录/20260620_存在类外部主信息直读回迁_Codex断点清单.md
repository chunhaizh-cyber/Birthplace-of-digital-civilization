# 20260620 存在类外部主信息直读回迁 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_存在类逻辑纠偏计划_v0.1.md` 的 1.4，扫描并回迁存在类外部对 `存在节点主信息类` 的直接读取。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_存在类逻辑纠偏计划_v0.1.md
存在类.h
存在类.cpp
```

本切片范围：

```text
1. 全仓扫描存在类外部对 存在节点主信息类 的直读。
2. 分类为系统逻辑、显示 / 语言 / SQL 投影、临时诊断。
3. 当前只收束一个系统逻辑缺口。
4. 显示 / 语言 / SQL 投影路径暂登记，不在本切片扩大。
```

验证前预测：

```text
1. 世界树类型判断、内部世界读取、二次特征空间关系、自我线程名称 / 类型匹配已在 1.2 回迁。
2. 剩余命中应集中在显示 / 语言 / SQL 投影、基础信息类型判断和控制面板展开。
3. 若仍发现系统逻辑直读，优先迁到 存在类 只读功能函数。
4. 代码改动后需要运行函数说明文档生成器和 Debug x64 构建；若只登记分类且无代码改动，可记录未构建原因。
```

## 1. 扫描记录

```text
1. 已运行全仓扫描：
   rg -n "取主信息<存在节点主信息类>|dynamic_cast<.*存在节点主信息类|存在主信息->|有位置历史|最近观测位置_mm|连续静止帧|连续未命中帧" -g "*.h" -g "*.cpp" -g "*.ixx"
   输出写入：日志/20260620_存在类外部主信息直读扫描.txt
   命中：51 行。
2. 已知 1.2 回迁点 世界树类型判断 / 内部世界读取 / 二次特征空间关系 / 自我线程名称类型匹配 未再作为旧直读命中。
```

## 2. 当前缺口

```text
第一缺口：
    多处系统逻辑只需判断“节点是否为存在节点”，仍直接读取 存在节点主信息类。

证据：
    世界树类.cpp:1319
    自我动作实现模块.ixx:4702
    自我动作实现.外设模块.ixx:13645,13674

处理：
    改为调用 存在类::是存在节点。
    控制面板 / 语言 / SQL 投影路径暂不扩大。
```

## 3. 验证结果

```text
1. 回迁后复扫：
   rg 输出已写入 日志/20260620_存在类外部主信息直读扫描_after_system.txt。
   命中从 51 行降至 47 行。
   自我动作实现模块.ixx:4702、自我动作实现.外设模块.ixx:13645,13674 已改为 世界树.存在().是存在节点。
   世界树类::是实例存在 已改为 存在服务_.是存在节点。

2. 剩余命中分类：
   存在类内部：存在主信息自身读取 / 写入。
   基础信息类：存在概念根创建与回查，属于基础信息根初始化路径。
   世界树 SQL 投影、控制面板、语言类：显示 / 语言 / SQL 投影读取，按本切片边界登记，不作为机器判断依据。

3. 函数说明文档：
   python .\tools\生成函数功能说明文档.py
   退出码：0
   日志：日志/20260620_存在类外部主信息直读_函数文档.log
   关键输出：源码文件 128；函数条目 6885。

4. 构建：
   msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
   退出码：0
   日志：日志/20260620_存在类外部主信息直读_msbuild.log
   关键输出：已成功生成。
```

## 4. 当前判断

```text
1. 本切片已闭合：外部系统逻辑存在节点判断已回迁到 存在类::是存在节点。
2. 剩余外部直读为显示 / 语言 / SQL 投影或基础信息根初始化路径，已登记边界。
3. 存在类 1.4 已闭合。
4. 不宣称存在类逻辑纠偏完成。
```
