# 20260620 状态活动判断读取回迁 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.2 P3.3f，回迁 `状态类::是否当前活动状态` 和 `状态类::可从活动状态链清理` 中的场景状态索引读取路径。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
实施记录/20260620_状态获取场景状态读取回迁_Codex断点清单.md
```

本切片范围：

```text
1. 只修改 状态类::是否当前活动状态 和 状态类::可从活动状态链清理。
2. 将两个函数的 场景主信息->状态索引 直接读取改为 场景类::读取场景状态索引快照。
3. 不新增、删除或改名函数。
4. 不处理状态清理函数中的直写 / 计数路径，留给下一切片。
```

验证前预测：

```text
1. 两个函数不再直接读取 场景节点主信息类 或 场景主信息->状态索引。
2. 状态类.cpp 中剩余 场景主信息->状态索引 命中会集中在 清理已处理状态索引、清理超过时间窗口的状态索引、按活动状态限量清理。
3. Debug x64 构建应通过。
4. 函数文档无需重生成，因为本切片未新增、删除或改名函数。
```

禁止宣称：

```text
不宣称状态类场景状态索引直读全部回迁。
不宣称所有场景主信息直读已回迁。
不宣称场景类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 1. 修改结果

```text
已修改：
    状态类.cpp

修改内容：
    状态类::是否当前活动状态 改为调用 场景类::读取场景状态索引快照。
    状态类::可从活动状态链清理 改为调用 场景类::读取场景状态索引快照。
```

## 2. 验证结果

直读检查：

```powershell
rg -n "取主信息<场景节点主信息类>|场景主信息->状态索引|读取场景状态索引快照" 状态类.cpp
```

关键结果：

```text
状态类::获取场景状态：读取场景状态索引快照。
状态类::是否当前活动状态：读取场景状态索引快照。
状态类::可从活动状态链清理：读取场景状态索引快照。
剩余 场景主信息->状态索引 命中集中在状态清理函数。
```

构建验证：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

证据：

```text
日志/20260620_状态活动判断读取回迁_msbuild.log
退出码：0
已成功生成。
0 个错误
```

## 3. 下一步

```text
P3.3g 回迁 状态类::清理已处理状态索引、状态类::清理超过时间窗口的状态索引、状态类::按活动状态限量清理 中的状态索引读取 / 擦除路径。
```
