# 20260622 任务治理协议结构审计 Codex断点清单

## 0. 恢复摘要

当前主动计划：`计划/计划索引.md` 第 1 章 `P10 项目结构体审计与清理计划 - 任务治理协议结构审计首轮`。

当前切片：

```text
索引子切片 P10a：任务治理协议结构审计首轮。
源码对象：任务模块.治理协议.ixx。
首轮候选来自：日志/20260621_P10_结构体低命中候选_after_P4bi.txt。
```

已复核依据：

```text
规范/禁止性规范20260611.md：第 2 章项目通用数据结构闭环，第 5-7 章任务筹办 / 执行和验收约束。
规范/规范目录.md：一级规范 002 / 007 / 030 的优先级。
规范/自我线程与任务管理线程权责边界规范20260512.md：任务治理消息和任务状态提交边界。
规范/任务筹办与执行边界规范20260512.md：任务筹办 / 任务执行是普通生命周期函数，不作为本能方法。
计划/计划索引.md：P10a 当前主动队列。
计划/20260618_项目结构体审计与清理计划_v0.1.md：P3/P4 结构体审计和默认暂停协议结构的口径。
```

当前判断：

```text
本切片只审计源码结构体，不处理 详细设计/ 因果类用途接口草案。
任务治理协议结构默认不直接硬删；先补齐写入方、读取方、跨线程合同职责、项目结构承载和文本字段机器用途判定。
若发现只是未接入壳或局部返回视图，才进入最小代码修改。
用户补充删除闸门：只有定义和赋值的结构体可以直接删除。
本切片执行解释：若存在字段读取、传参 / 返回合同、跨线程协议职责或真实运行路径依赖，不直接删除，只做判定或暂停确认。
```

下一步：

```text
1. live rg 首轮候选定义、构造、字段读取和只服务 helper。
2. 选择 1 到 2 个低命中且含文本键 / 摘要字段的结构写入判定表。
3. 若进入代码修改，先确认函数说明文档中是否已有相关函数，再改源码并构建。
```

## 1. P10a 首轮删除执行记录

已执行对象：

```text
任务模块.治理协议.ixx::结构_任务管理写回结果
任务模块.治理协议.ixx::结构_缺口承接决策
```

删除依据：

```text
1. 用户补充删除闸门：只有定义和赋值的结构体可以直接删除。
2. 两个结构体复扫后只命中结构体定义、字段定义和 `结构_任务管理结果` 中的成员声明。
3. 未发现字段读取、传参 / 返回合同、跨文件引用、控制面板读取或真实运行路径依赖。
4. 删除时同步删除未读成员 `当前写回结果` 和 `当前缺口承接决策`，未新增替代字段。
```

已运行复扫：

```powershell
rg -n "结构_任务管理写回结果|当前写回结果|结构_缺口承接决策|当前缺口承接决策" -g "*.cpp" -g "*.h" -g "*.ixx" -g "!x64/**" -g "!Debug/**" -g "!Release/**" -g "!日志/**" -g "!logs/**" -g "!详细设计/**"
```

复扫结果：

```text
目标结构体和对应成员源码命中 0。
```

已更新：

```text
实施记录/20260618_项目结构体审计_判定表.md
```

待验证：

```text
已完成 Debug x64 构建。
命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
日志：日志/20260622_P10a_任务治理协议结构审计_build.log
结果：exit_code=0，未检出 error / fatal / warning。
```

## 2. P10a 第二组删除执行记录

已执行对象：

```text
任务模块.治理协议.ixx::结构_原语候选项
任务模块.治理协议.ixx::结构_任务管理分身继承面
```

删除依据：

```text
1. 两个结构体复扫后只命中结构体定义和一个成员声明。
2. 未发现字段读取、元素构造、传参 / 返回合同、跨文件引用、控制面板读取或真实运行路径依赖。
3. 删除时同步删除未读成员 `当前升阶候选组` 和 `当前分身`，未新增替代字段。
```

已运行复扫：

```powershell
rg -n "结构_原语候选项|当前升阶候选组|结构_任务管理分身继承面|当前分身\b" -g "*.cpp" -g "*.h" -g "*.ixx" -g "!x64/**" -g "!Debug/**" -g "!Release/**" -g "!日志/**" -g "!logs/**" -g "!详细设计/**"
```

复扫结果：

```text
目标结构体和对应成员源码命中 0。
```

待验证：

```text
已完成 Debug x64 构建。
命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
日志：日志/20260622_P10a_任务治理协议结构审计_第二组_build.log
结果：exit_code=0，未检出 error / fatal / warning。
```

## 3. P10a 第三组删除执行记录

已执行对象：

```text
任务模块.治理协议.ixx::结构_任务管理方法需求位特征面
任务模块.治理协议.ixx::结构_任务管理本能能力缺口面
```

删除依据：

```text
1. 两个结构体复扫后只命中结构体定义和一个成员声明。
2. 未发现字段读取、传参 / 返回合同、跨文件引用、控制面板读取或真实运行路径依赖。
3. 删除时同步删除未读成员 `当前需求位特征` 和 `当前本能能力缺口`，未新增替代字段。
```

已运行复扫：

```powershell
rg -n "结构_任务管理方法需求位特征面|当前需求位特征\b|结构_任务管理本能能力缺口面|当前本能能力缺口\b" -g "*.cpp" -g "*.h" -g "*.ixx" -g "!x64/**" -g "!Debug/**" -g "!Release/**" -g "!日志/**" -g "!logs/**" -g "!详细设计/**"
```

复扫结果：

```text
目标结构体和对应成员源码命中 0。
```

待验证：

```text
已完成 Debug x64 构建。
命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
日志：日志/20260622_P10a_任务治理协议结构审计_第三组_build.log
结果：exit_code=0，未检出 error / fatal / warning。
```

当前停止点：

```text
P10a 首轮已闭合六个“只有定义和成员声明、无读取”的任务治理协议结构：
    结构_任务管理写回结果
    结构_缺口承接决策
    结构_原语候选项
    结构_任务管理分身继承面
    结构_任务管理方法需求位特征面
    结构_任务管理本能能力缺口面
本次未改函数，未刷新函数功能说明文档。
剩余首轮候选仍需按 live rg 逐个判定，不按低命中清单批量删除。
```

下一步：

```text
继续 P10a 首轮源码候选复扫。
优先检查低命中且含文本键 / 摘要字段的结构：
    结构_任务管理主体虚拟存在
    结构_任务管理上下文
若发现字段读取、传参 / 返回合同、跨线程协议职责或真实运行路径依赖，只写判定，不直接删除。
```
