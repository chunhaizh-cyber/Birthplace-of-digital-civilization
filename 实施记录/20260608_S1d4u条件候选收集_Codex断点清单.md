# 20260608 S1d4u 条件候选收集 Codex 断点清单

## 当前切片

- 主动计划：`计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- 执行方案：`计划/20260607_S1d4u条件候选收集内部阶段定位与瘦身方案_v0.1.md`
- 切片：S1d-4u 条件候选收集内部阶段定位 / 瘦身。

## 依据文件

- `AGENTS.md`
- `规范/规范目录.md`
- `规范/方法动作动态硬规则规范20260512.md`
- `规范/二次特征规范20260520.md`
- `规范/远程执行通道与断点续跑规范20260524.md`
- `计划/计划索引.md`
- `计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- `计划/20260607_S1d4t二次特征场景绑定写入内部阶段定位与瘦身方案_v0.1.md`
- `实施记录/20260608_S1d4t二次特征场景绑定写入_Codex断点清单.md`

## 当前判断

- S1d-4t 已闭合并提交：`ef16682`。
- 最新运行线程 T29964：
  - `动态二次特征生成/内部阶段耗时` 样本 19 条。
  - 最慢阶段分布 `条件候选收集=16`、`命中回写因果实例=2`、`分步结果生成=1`。
  - `条件候选收集` 平均约 225225us。
- `条件候选收集` 对应 `私有_收集条件候选`。
- 当前实现存在候选与结果候选嵌套比较，并重复读取节点主键。

## 构建 / 运行前预测

- Debug x64 构建通过。
- 真实运行退出码 0。
- 日志继续出现 `动态二次特征生成/内部阶段耗时`。
- 日志出现 `动态二次特征条件候选收集/内部阶段耗时`，或在 5ms 阈值下无样本但高层动态二次特征生成仍可统计。
- 日志保留 `任务状态动作动态=<动态主键>`。
- 不出现 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`。
- 不出现旧路径锚点 `准备本能调用`、`执行本能方法`、`任务域本能输入包`。

## 允许修改

- `二次特征应用模块.ixx`
- `计划/20260607_S1d4u条件候选收集内部阶段定位与瘦身方案_v0.1.md`
- `计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- `计划/计划索引.md`
- `实施记录/20260608_S1d4u条件候选收集_Codex断点清单.md`

## 已改文件

- `二次特征应用模块.ixx`
- `计划/20260607_S1d4u条件候选收集内部阶段定位与瘦身方案_v0.1.md`
- `计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- `计划/计划索引.md`
- `实施记录/20260608_S1d4u条件候选收集_Codex断点清单.md`

## 已完成修改

- `二次特征应用模块.ixx` 已新增 `动态二次特征条件候选收集/内部阶段耗时`。
- `私有_收集条件候选` 已拆分全部候选枚举、结果候选索引准备、候选过滤、条件候选追加。
- 结果候选排除逻辑已由候选与结果候选嵌套比较改为结果候选指针集 / 主键集查找。
- 本次修改保留条件候选收集和因果模板匹配，只优化候选收集内部重复比较。

## 已运行命令

- `git diff --check -- 二次特征应用模块.ixx 计划/计划索引.md 计划/20260607_S1d4u条件候选收集内部阶段定位与瘦身方案_v0.1.md 实施记录/20260608_S1d4u条件候选收集_Codex断点清单.md`
  - 退出码 0；仅有 CRLF 工作区转换警告。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
  - 退出码 0；0 errors；保留 LNK4075 增量链接相关警告。
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000`
  - 退出码 0；末尾 `D455 打开失败: No device connected` 属硬件条件，不作为本切片失败。

## 验收结果

- 最新日志窗口：`日志/鱼巢_run_20260608.低值g`，启动行 `2026-06-08 11:24:32.582 [T24772] 程序启动`。
- `动态二次特征条件候选收集/内部阶段耗时`：11 条。
- `动态二次特征生成/内部阶段耗时`：10 条。
- `动态二次特征分步结果/内部阶段耗时`：7 条。
- `任务状态动作动态=`：63 条。
- `任务状态被动提交/逻辑错误`：0。
- `任务状态动作动态逻辑错误`：0。
- `准备本能调用`：0。
- `执行本能方法`：0。
- `任务域本能输入包`：0。

## 性能证据

- 条件候选收集内部样本 11 条，平均总耗时约 5872us，最大约 9465us。
- 条件候选收集内部最慢阶段全部为 `候选过滤`。
- 高层 `条件候选收集` 平均约 5054us，较 S1d-4t 的约 225225us 明显下降。
- 动态二次特征生成样本 10 条，平均总耗时约 219887us，最慢阶段分布为 `分步结果生成=7`、`命中回写因果实例=3`。
- 分步结果样本 7 条，平均总耗时约 178166us，最慢阶段全部为 `分步签名查找创建`。
- `分步签名查找创建` 平均约 83114us，`分步值写入` 平均约 55497us，`分步语义绑定` 平均约 17707us。

## 结论

- S1d-4u 已闭合。
- 条件候选收集不再是动态二次特征生成主慢点。
- 当前慢点转到分步结果里的 `分步签名查找创建`。

## 下一步

- 进入 S1d-4v：分步签名查找创建内部阶段定位 / 瘦身。
- 先生成 `计划/20260607_S1d4v分步签名查找创建内部阶段定位与瘦身方案_v0.1.md` 和 `实施记录/20260608_S1d4v分步签名查找创建_Codex断点清单.md`。
- 重点拆解 `获取或创建子二次特征_按签名` 的子节点枚举、签名比较、节点创建和默认签名刷新。

## 禁止宣称

- 不宣称自我苏醒完成。
- 不宣称初步成熟完成。
- 不把定位日志当成性能优化完成。
- 不跳过条件候选收集或因果模板匹配。
