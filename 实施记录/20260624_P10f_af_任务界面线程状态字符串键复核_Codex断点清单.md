# P10f-af 任务界面线程状态字符串键复核 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-af `任务模块.管理界面线程.impl.cpp::结构_任务界面线程状态` string-key map / set 边界复核。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `规范/任务筹办与执行边界规范20260512.md`
- `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_calibration_resource_fields.tsv`

复核字段：

- `幂等索引`
- `外设承接上行幂等表`
- `在途工作项索引`
- `执行前许可任务占用表`
- `执行前许可免审批方法白名单`
- `任务状态快照表`
- `等待任务索引`
- `挂起任务索引`

当前判断：

- `幂等索引` 使用 `请求.幂等键 / 承接.幂等键` 作为请求去重索引；等待态复用判断读取结构化请求状态、调度动作和下一步去向枚举。
- `外设承接上行幂等表` 使用 `私有_任务管理上行消息幂等键(...)` 生成的消息幂等键，防止重复上行。
- `在途工作项索引 / 执行前许可任务占用表 / 任务状态快照表 / 等待任务索引 / 挂起任务索引` 均以 `私有_任务调度键(任务)` 生成的任务主键为 key。
- `执行前许可免审批方法白名单` 以 `方法->获取主键()` 或外部显式传入的方法主键为 key。
- 等待 / 挂起分类由 `结构_调度任务状态快照::任务状态` 的 `枚举_任务状态::等待中 / 挂起` 判断，不通过文本判断。
- 执行前许可免审批由方法主键白名单命中判断，不通过文本说明判断。

检索证据：

- `私有_同步等待挂起索引_已持锁(...)` 先清任务主键索引，再按 `枚举_任务状态` 写入等待或挂起索引。
- `私有_尝试登记在途工作项(...)`、`私有_同任务存在在途工作项(...)`、`私有_任务已有有效入队或在途工作项(...)` 读取任务主键索引和工作项类型枚举。
- `私有_执行前许可命中免审批方法白名单(...)` 读取当前方法主键并查白名单。
- 未发现这些 string-key 容器以自由文本状态、原因、说明或摘要作为机器判断依据。

处理结论：

- 本切片不做代码删除或改名。
- 这些字段按主键 / ID / 幂等键索引登记边界；不纳入当前 string 字段清理对象。
- 未改代码，未运行构建。

下一步：

- 返回 P10f 最新复扫清单，复核 `任务模块.管理线程协议.ixx::结构_任务界面线程快照` 中的最近状态 / 调度动作 / worker 状态文本是否只用于控制面板显示。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
