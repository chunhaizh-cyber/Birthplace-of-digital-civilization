# 20260620 存在主信息字段判定表

## 1. 证据范围

```text
定义位置：
    基础信息主信息类.h:424-474 存在节点主信息类

引用清单：
    日志/20260620_存在主信息字段引用.txt

计数说明：
    计数为字段名全仓文本命中，概念模板 / 概念集 / 内部世界 / 任务根节点 等存在跨结构同名或局部变量同名，不能直接等同于 存在节点主信息类 字段读取次数。
```

## 2. 字段判定表

| 字段 | 写入方 | 读取方 | 是否持久事实 | 是否缓存 | 是否显示专用 | 是否可替代 | 初判 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 概念模板 | 存在类::绑定概念模板 / 添加实例概念 / 创建存在实例链 | 存在类::获取概念集 / 存在包含概念；世界树 SQL 投影；控制面板显示 | 是，概念归属入口 | 是，第一阶段归类缓存 | 否 | 后续可由特征 / 二次特征 / 动态回查增强，但当前不可删除 | 保留；不得当最终事实，后续补读取函数 |
| 概念集 | 存在类::绑定概念模板 / 添加实例概念 / 创建存在实例链 | 存在类::获取概念集 / 查找实例_按概念；世界树 / 任务 / 方法虚拟存在构造 | 是，概念归属入口 | 是，快速匹配缓存 | 否 | 当前不可替代 | 保留；后续检查外部直接读取是否需回迁 |
| 内部世界 | 世界树::取或创建存在内部世界 -> 存在类::绑定内部世界 | 世界树::取存在内部世界；外设 / 自我线程 / 控制面板等内部世界链路 | 是，存在到内部世界场景引用 | 否 | 否 | 当前不可替代 | 保留；后续补 存在类 只读函数或世界树边界说明 |
| 需求根节点 | 存在类::确保存在三类根节点 / 创建存在 | 存在类::获取需求根节点；自我 / 控制面板 / 语言显示 / 需求链 | 是，需求树生命周期入口 | 否 | 否 | 当前不可替代 | 保留；已完成只读 / 显式确保拆分 |
| 任务根节点 | 存在类::确保存在三类根节点 / 创建存在 | 存在类::获取任务根节点；任务管理 / 自我 / 控制面板 / 语言显示 | 是，任务树生命周期入口 | 否 | 否 | 当前不可替代 | 保留；已完成只读 / 显式确保拆分 |
| 方法根节点 | 存在类::确保存在三类根节点 / 创建存在 | 存在类::获取方法根节点；任务筹办 / 方法类 / 控制面板 / 自我 | 是，方法树生命周期入口 | 否 | 否 | 当前不可替代 | 保留；已完成只读 / 显式确保拆分 |
| 有位置历史 | 存在类::写入观测位置 | 二次特征应用模块空间关系生成；控制面板显示 | 否 | 是，观测输入缓存 | 否 | 后续应由空间特征 / 动态事实替代 | 暂留；需补读取函数并标注非权威事实 |
| 上次观测位置_mm | 存在类::写入观测位置 | 存在类::写入观测位置计算位移；控制面板显示 | 否 | 是，观测输入缓存 | 否 | 后续应由状态 / 动态事实替代 | 暂留；当前用于最近位移计算 |
| 最近观测位置_mm | 存在类::写入观测位置 | 二次特征应用模块空间关系生成；存在类位移计算；控制面板显示 | 否 | 是，观测输入缓存 | 否 | 后续应由空间特征 / 动态事实替代 | 暂留；需补读取函数并标注非权威事实 |
| 最近位移_mm | 存在类::写入观测位置 | 控制面板显示；存在类内部更新静止帧 | 否 | 是，派生缓存 | 部分是 | 可由上次 / 最近位置或动态派生 | 暂留；后续可作为删除 / 迁移候选 |
| 连续静止帧 | 存在类::写入观测位置 / 标记观测未命中 | 控制面板显示 | 否 | 是，观测统计缓存 | 是 | 可由观测动态序列派生 | 暂留；列为后续删除 / 迁移候选 |
| 连续未命中帧 | 存在类::标记观测未命中 / 写入观测位置复位 | 控制面板显示 | 否 | 是，观测统计缓存 | 是 | 可由观测动态序列派生 | 暂留；列为后续删除 / 迁移候选 |

## 3. 当前结论

```text
1. 本切片不直接删除字段。
2. 概念模板 / 概念集 / 内部世界 / 三类根节点当前都有结构承载或生命周期入口用途，不能在字段审查阶段删除。
3. 观测缓存字段不是权威治理事实，但二次特征应用模块仍读取 有位置历史 / 最近观测位置_mm 生成空间关系二次特征；删除前必须先迁移读取来源。
4. 最近位移_mm / 连续静止帧 / 连续未命中帧 当前主要是派生缓存和显示材料，列为后续删除 / 迁移候选。
5. 下一步进入 存在类 1.2 功能函数补缺：优先补存在名称 / 类型 / 内部世界 / 概念 / 观测缓存只读函数，并将系统逻辑读取回迁到存在类功能函数。
```
