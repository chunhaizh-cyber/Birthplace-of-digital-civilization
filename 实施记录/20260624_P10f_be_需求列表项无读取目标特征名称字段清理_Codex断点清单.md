# P10f-be 需求列表项无读取目标特征名称字段清理 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-be `自我线程模块.ixx::结构_需求列表项::目标特征名称` 无读取字段清理。

依据文件：

- `AGENTS.md`
- `docs/00_文档总索引.md`
- `docs/工程图谱/00_图谱总索引.md`
- `docs/工程图谱/01_任务需求方法闭环图谱.md`
- `docs/工程图谱/04_Codex改代码前检查清单.md`
- `docs/工程图谱/05_规则原子索引.md`
- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `计划/计划索引.md`
- `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_root_loop_unused_definition_id.tsv`

预测：

- `结构_需求列表项::目标特征名称` 只有声明和三处赋值，没有读取方。
- `私有_需求列表项排序在前(...)` 使用 `目标特征类型` 的主键、节点权重、目标特征权重汇总和需求节点主键排序，不读取 `目标特征名称`。
- 删除该字段后，`目标特征标题` 缓存只为该字段赋值服务，也应同步删除。

已复核证据：

- `rg -n "目标特征名称|目标特征标题" 自我线程模块.ixx 自我线程模块.impl.cpp`：`目标特征名称` 仅声明和赋值；`目标特征标题` 仅为该字段赋值服务。
- `私有_需求列表项排序在前(...)` 已复核不读取 `目标特征名称`。

当前修改计划：

- 删除 `结构_需求列表项::目标特征名称`。
- 删除 `目标特征标题` 缓存构造。
- 删除三处 `目标特征名称` 赋值。
- 生成新的 P10f string 字段复扫 TSV。
- 跑精确 `rg`、`git diff --check` 和 `Debug|x64` 构建。

本轮修改：

- 删除 `结构_需求列表项::目标特征名称`。
- 删除 `私有_从需求树构建需求列表(...)` 中只为该字段服务的 `目标特征标题` 缓存。
- 删除三处 `目标特征名称` 赋值。

验证记录：

- `rg -n "目标特征名称|目标特征标题" 自我线程模块.ixx 自我线程模块.impl.cpp`：无命中。
- `rg -n "\.目标特征名称\s*=" 自我线程模块.ixx 自我线程模块.impl.cpp`：无命中。
- 已生成 `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_demand_list_feature_name.tsv`：剩余记录 514 行，其中主键 / ID / 键 / 句柄类 330 行、非主键类 184 行；目标字段残留 0。
- `Select-String -LiteralPath 实施记录/20260624_P10f_live_结构体string字段声明复扫_after_demand_list_feature_name.tsv -Pattern '目标特征名称|目标特征标题'`：无命中。
- `git diff --check -- 自我线程模块.ixx 自我线程模块.impl.cpp 实施记录/20260624_P10f_be_需求列表项无读取目标特征名称字段清理_Codex断点清单.md 实施记录/20260624_P10f_live_结构体string字段声明复扫_after_demand_list_feature_name.tsv`：通过。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo`：成功，0 警告，0 错误。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
