# P10f-z 外设版本坐标系字段复核 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-z `外设观察报告队列.ixx::结构_外设观察报告队列项` 版本 / 坐标系类 string 字段边界复核。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_material_handle_fields.tsv`

当前判断：

- `相机内参版本 / 相机外参版本` 当前作为标定版本标识写入和显示，不参与候选选择、等待匹配、结算或因果匹配。
- `坐标系版本` 会进入提交包头 `坐标系ID`、外设特征值 `坐标系ID` 和扫描数据项 `坐标系ID`，已是机器结构化 ID，不应继续以“版本”字段名表达。
- `报告生成方法版本` 会与 `报告生成方法ID` 合成报告生成方法标识，用于来源追踪和日志显示，当前应表达为版本 ID。
- 当前不删除这些字段；本切片将版本 / 坐标系类字段收束为 `...ID` 命名，并保持现有机器判断语义不变。
- 已将 `相机内参版本 / 相机外参版本` 收束为 `相机内参版本ID / 相机外参版本ID`。
- 已将 `坐标系版本` 收束为 `坐标系ID`。
- 已将 `报告生成方法版本` 收束为 `报告生成方法版本ID`。
- 已同步 D455 报告写入、稳定子集派生、提交包头生成、特征值构造、扫描数据项坐标系读取和报告 generation 标识生成。

构建前预测：

- 字段命名收束后，现有提交包头、特征构造、扫描数据项和报告摘要逻辑不变。
- 预期 `rg` 复核旧字段访问无残留。
- 预期 `git diff --check` 通过。
- 预期 Debug x64 构建通过，0 警告，0 错误。

完成证据：

- `rg` 复核 `相机内参版本 / 相机外参版本 / 坐标系版本 / 报告生成方法版本` 旧字段访问无残留。
- 最新复扫清单：`实施记录/20260624_P10f_live_结构体string字段声明复扫_after_version_id_fields.tsv`，剩余 621 行，其中主键 / ID / 键 / 句柄类 340 行、非主键类 281 行。
- `git diff --check` 通过。
- `Debug|x64` 构建通过，0 警告，0 错误。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
