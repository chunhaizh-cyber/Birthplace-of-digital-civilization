# 服务值 v1.1 自然运行验证 Codex断点清单

日期：2026-06-10

## 当前切片

服务本能方法实现计划 P8f：服务值 v1.1 自然运行日志验证。

## 目标

```text
通过真实主循环日志验证：
    成功服务G0候选生成
    成功服务G0饱和入账
    服务治理周期维护
    服务值衰减
    休眠候选
```

## 已运行命令

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\鱼巢_build_20260610_服务模块P8e.log

$psi = [System.Diagnostics.ProcessStartInfo]::new()
$psi.FileName = (Resolve-Path '.\x64\Debug\鱼巢.exe')
$psi.WorkingDirectory = (Get-Location).Path
$psi.UseShellExecute = $false
$psi.RedirectStandardInput = $true
$psi.RedirectStandardOutput = $true
$psi.RedirectStandardError = $true
$psi.CreateNoWindow = $true
$p = [System.Diagnostics.Process]::Start($psi)
Start-Sleep -Seconds 3
$p.StandardInput.WriteLine('退出')
$p.StandardInput.Close()
$p.WaitForExit(60000)
```

## 结果

```text
构建退出码：0
正常运行退出码：0
stdout：日志/鱼巢_run_20260610_服务模块P8f_stdout.log
stderr：日志/鱼巢_run_20260610_服务模块P8f_stderr.log
项目运行日志：日志/鱼巢_run_20260610.低值g
```

关键日志：

```text
日志/鱼巢_run_20260610.低值g:4324
    自我线程/主循环 | 运行态已建立

日志/鱼巢_run_20260610.低值g:4330
    程序正常退出

日志/鱼巢_run_20260610.低值g:4354
    自我线程/主循环 | 退出线程函数
```

未命中：

```text
服务治理周期维护
成功服务G0候选生成
成功服务G0饱和入账
服务值直接入账已关闭
任务价值反馈旧服务值直接入账已关闭
定时衰减服务值
休眠候选
```

## 当前判断

```text
P8c/P8d/P8e 代码构建通过。
P8f 未获得足够自然样本。
本次正常运行在主循环建立后很快退出，未达到 P8e 的 4 tick 维护间隔，也未出现自然成功服务候选。
不能使用模拟任务、夹具入口、伪造动作动态或 `--*-check` 回归补样本。
```

## 恢复条件

```text
有一次真实主循环运行满足：
    运行时间足以跨过服务治理维护间隔；
    或自然出现计划服务增量 / 成功服务候选；
    或候选池中已有待复核候选记录。

恢复后检索：
    服务治理周期维护
    成功服务G0候选生成
    成功服务G0饱和入账
    服务值直接入账已关闭
    定时衰减服务值
    休眠候选
```

## 禁止宣称项

```text
不得宣称自然 G0 成功服务入账已通过。
不得宣称服务值 v1.1 真实闭环已完成。
不得宣称自我苏醒完成或初步成熟完成。
```
