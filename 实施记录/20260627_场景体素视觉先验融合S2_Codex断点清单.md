# 20260627 场景体素视觉先验融合 S2 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260625_场景体素视觉先验融合实现计划_v0.1.md` / S2 当前场景抽取和缺口记录。

S1 已完成：

```text
新增 `场景体素模块.ixx`；
新增同步版 `场景体素缓存类`；
Debug x64 构建通过，0 警告 0 错误。
```

S2 目标：

```text
在 SceneVoxelPrior 快照内补齐结构化缺口项；
缺口使用 I64 枚举、存在引用、特征引用、版本号等项目结构类型；
缺体素特征类型、缺坐标、缺三维体素特征、缺三维体素句柄、缺坐标系版本均可被只读回查；
不创建世界树缺口节点，不接入业务提交入口。
```

## 1. 当前判断

S1 已经能从当前场景子存在生成只读快照，但缺口只在统计计数中体现。S2 应把缺口列表纳入快照运行期结构，供后续 S3/S4 使用或落到运行存在 / 输出结果场景时映射。

## 2. 预测

```text
新增缺口枚举和缺口项结构后，函数说明文档应出现缺口结构相关入口；
Debug x64 构建仍应通过；
该缺口列表仍是运行期快照内容，不证明业务闭合。
```

## 3. 待验证命令

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

## 4. 本轮修改

```text
修改：场景体素模块.ixx
修改：说明书/函数功能说明文档.md
修改：实施记录/20260627_场景体素视觉先验融合S2_Codex断点清单.md
```

新增运行期缺口结构：

```text
枚举_场景体素缺口类型
结构_场景体素缺口项
结构_场景体素只读快照::缺口集合
结构_场景体素快照统计::缺口数量
结构_场景体素快照统计::缺坐标系版本数量
```

缺口类型：

```text
缺体素特征类型
缺存在场景绝对坐标
缺三维体素特征
缺三维体素句柄
缺坐标系版本
```

## 5. 验证结果

实际命令：

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

结果：

```text
函数功能说明文档生成退出码 0。
源码文件：128
函数条目：7254

Debug x64 构建退出码 0。
0 个警告，0 个错误。
```

结论：

```text
S2 当前场景抽取和缺口记录已完成工程守卫验证。
缺口仍是 SceneVoxelPrior 运行期快照内容，不写世界树事实。
下一切片进入 S3：视觉观察融合模块。
```
