# P10f-i 治理二次特征候选概念文本复核 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-i `自我线程模块.impl.cpp::结构_治理二次特征候选::概念名称` 字段边界复核。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `规范/二次特征规范20260520.md`
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_causal_structured.tsv`

执行口径：

- 治理二次特征候选应使用内部身份结构承载概念语义。
- 字符串若只作为语素入口注册种子，必须在候选构造阶段立即转换为 `语素入口节点类*`；后续写入、校验和查找不得继续使用文本字段。
- 不新增函数；若可用局部构造直接转语素入口，则不扩展函数说明文档维护面。

当前候选：

- `自我线程模块.impl.cpp:14465` `结构_治理二次特征候选::概念名称`

当前问题记录：

- `概念名称` 当前用于候选有效性校验、日志和 `世界树.写入治理二次特征(...)` 调用。
- 这是治理二次特征生成路径，不是纯日志字段。

下一步：

- 已将 `结构_治理二次特征候选::概念名称` 改为 `const 语素入口节点类* 概念入口` 和 `const 语素入口节点类* 特征入口`。
- 构造候选时已把文本种子立即注册成关系模板入口，并同步生成治理特征类型入口；后续有效性校验和写入不再读取候选文本字段。
- `世界树类::写入治理二次特征` 已改为接收 `const 语素入口节点类* 概念入口`，跨模块入口不再把文本转换为机器写入依据。
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_causal_structured.tsv` 已刷新：剩余 633 行，其中主键类 314 行、非主键类 319 行；`结构_治理二次特征候选` 不再命中 string 字段。
- `Debug|x64` 构建已通过，0 警告 0 错误。

闭合状态：

- P10f-i 已闭合。
- 下一候选按复扫清单前列转入 `本能动作管理类.ixx::结构体_本能动作登记信息::函数名称` 边界复核；该字段进入注册 / 反查路径，优先于控制面板、SQL 投影和纯显示字段。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
