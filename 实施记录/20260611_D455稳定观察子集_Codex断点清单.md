# 20260611 D455稳定观察子集 Codex断点清单

## 当前切片

D455 稳定观察子集与轮廓跨帧稳定化实现计划 P0/P1/P1a。

本切片目标：

```text
P0：补连续逐簇识别报告稳定性诊断日志，不改变任务管理质量门。
P1：增强 D455 外设线程内部跨帧簇轨迹状态，降低静止画面下跨帧 ID 频繁新建。
P1a：对账稳定观察子集所需机器字段，优先复用现有特征，不修改 特征值主信息类。
```

## 依据文件

- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `计划/计划索引.md`
- `计划/20260611_D455稳定观察子集与轮廓跨帧稳定化实现计划_v0.1.md`
- `规范/双目相机外设独占观察线程规范20260527.md`
- `规范/双目相机外设功能能力与实现途径总结20260527.md`
- `规范/外设独立控制线程与消息承接边界规范20260527.md`
- `规范/相机外设综合工作流程规范20260607.md`
- `规范/观察特征质量诊断与认知补偿规范20260525.md`
- `规范/观察像素簇与存在候选分层规范20260527.md`
- `规范/当前场景特征值明确标准规范20260602.md`

## 已确认代码事实

- `外设线程_D455深度相机.ixx` 已有 `D455_跨帧观察簇缓存`、`D455_更新逐簇识别报告跨帧采集状态` 和逐簇识别报告入队短摘要。
- `外设观察报告队列.ixx` 中 `结构_外设观察像素簇摘要` 已有 `外设跨帧簇ID`、`跨帧匹配状态`、`跨帧连续命中次数`、`跨帧丢失次数`、`跨帧复现离散度`。
- `外设观察报告队列.ixx` 中 `结构_外设观察报告队列项` 已有 `报告层级`、`跨帧采集状态`、`跨帧匹配/新建/稳定/丢失观察像素簇数量`。
- 当前任务管理质量门仍主要依据材料句柄、深度有效率、空间坐标有效率、帧质量和报告类型；本切片不改该质量门。

## 允许修改范围

- `外设线程_D455深度相机.ixx`
- `外设观察报告队列.ixx`
- `实施记录/20260611_D455稳定观察子集_Codex断点清单.md`
- 必要时更新 `计划/计划索引.md` 或主动计划证据段。

## 禁止修改范围

- 不修改 `特征值主信息类` 坐标 / 轮廓存储。
- 不新增模拟样本、夹具入口、伪造报告或伪动作动态。
- 不把 D455 材料写成世界真值、需求满足、安全值或服务值。
- 不把外设内部采集、滤波、分割、跟踪、稳定化登记成自我层本能方法。
- 不恢复任务筹办 / 任务执行为本能方法。
- P0/P1/P1a 不提前把 L1 稳定子集改成任务管理正式消费硬门。

## 预测结果

```text
构建应通过。
真实 D455 20-30 秒窗口若硬件可用，应输出逐簇识别报告稳定性诊断短摘要。
日志应能看到：报告ID、深度/彩色帧号、原始簇数量、跨帧匹配/新建/稳定/丢失数量、主簇跨帧ID、连续命中、稳定命中、复现离散度、中心、投影范围、像素数量。
P1 后同一静止目标短暂一帧丢失不应立刻删除轨迹；跨帧匹配不依赖原始簇序号。
P1a 预期优先复用已有跨帧匹配/连续命中/复现离散度特征，不需要修改 特征值主信息类。
```

## 已改文件

- `外设线程_D455深度相机.ixx`
- `外设观察报告队列.ixx`
- `实施记录/20260611_D455稳定观察子集_Codex断点清单.md`

## 已完成修改

### P0 诊断字段

- 逐簇识别报告入队短摘要增加 `稳定性诊断`：
  - 原始簇数量；
  - 跨帧匹配 / 新建 / 稳定 / 丢失数量；
  - 主簇跨帧ID、连续命中、稳定命中、连续发布、稳定状态、待发布状态、复现离散度；
  - 主簇中心、平滑中心、投影范围、平滑投影范围、像素数量。
- D455 观察样本摘要补充主簇稳定命中、稳定状态、复现离散度和投影范围。

### P1 跨帧轨迹状态增强

- `结构_D455跨帧观察簇缓存项` 增加最近投影范围、最近空间范围、平滑中心、平滑投影范围、连续稳定命中、连续发布、最近复现离散度、稳定状态、待发布状态和丢失宽限计数。
- 跨帧匹配规则继续使用工程代码实现，不登记为自我层本能方法；匹配依据扩展为中心、投影范围、空间范围和像素数量，并对已稳定或处于丢失宽限的轨迹使用滞回容差。
- 稳定状态采用连续稳定命中阈值；短暂丢失进入宽限，不立刻删除轨迹。

### P1a 存在特征口径对账

复用字段 / 特征：

```text
外设观察报告ID
外设观察像素簇ID
外设跨帧簇ID
跨帧匹配状态
跨帧连续命中次数
跨帧复现离散度
外设观察存在同一性证据帧数
稳定对应存在数量
扫描可启动状态
```

新增最小报告字段：

```text
跨帧连续稳定命中次数
跨帧连续发布次数
跨帧轨迹稳定状态
跨帧轨迹待发布状态
跨帧丢失宽限状态
平滑中心
平滑投影范围
```

未修改：

```text
特征值主信息类
VecIU64 / VecI64 轮廓解释规则
任务管理质量门
L1 稳定子集正式消费口径
```

## 已运行命令

- `git status --short --untracked-files=no`
  - 退出码：0
  - 输出：无已跟踪改动。
- `git diff --check -- 外设线程_D455深度相机.ixx 外设观察报告队列.ixx 实施记录/20260611_D455稳定观察子集_Codex断点清单.md`
  - 退出码：0
  - 输出：仅有既有 LF/CRLF 提示；无空白错误。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
  - 日志：`日志/鱼巢_build_20260611_D455_stable_subset_P0P1.log`
  - 退出码：0
  - 结果：成功生成；1 个既有 `LNK4075` 链接警告；0 个错误。
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000`
  - 控制台日志：`日志/鱼巢_run_20260611_D455_stable_subset_P0P1_30s_console.log`
  - 业务日志：`日志/鱼巢_run_20260611.低值g`
  - 退出码：0

## 关键日志证据

- `日志/鱼巢_run_20260611.低值g:87945`：报告ID=1 输出 `稳定性诊断`，原始簇=12，跨帧新建=12，主簇跨帧ID=0，主簇连续命中=1。
- `日志/鱼巢_run_20260611.低值g:88290`：报告ID=3，主簇跨帧ID=0，主簇连续命中=2，主簇稳定命中=1，复现离散度=457。
- `日志/鱼巢_run_20260611.低值g:89364`：报告ID=10，原始簇=14，跨帧匹配=14，新建=0，稳定=11；主簇跨帧ID=0，连续命中=9，稳定命中=8，连续发布=6，稳定状态=1。
- `日志/鱼巢_run_20260611.低值g:90759`：报告ID=20，原始簇=15，跨帧匹配=15，新建=0，稳定=15；主簇跨帧ID=0，连续命中=19，稳定命中=18，连续发布=16，稳定状态=1。
- `日志/鱼巢_run_20260611.低值g:91146`：D455 外设线程停止，样本=23，基础采样稳定=稳定，观察候选稳定=稳定，综合稳定=稳定。
- 负向核对：未命中 `日志模块低级错误`、`状态提交确认失败`、`后台worker工作项进入死信`、`直接写需求满足=是`、`直接写世界树=1`、`直接写需求树=1`。

## 预测偏差

- 报告ID=2 出现深度有效率短时波动，后续主簇跨帧ID仍恢复到 0 并进入稳定状态。
- 本切片只生成稳定性诊断和轨迹状态；未生成 L1 稳定子集报告，符合 P0/P1/P1a 范围。

## 当前判断

P0/P1/P1a 已闭合。当前未发现规范与计划冲突。

## 下一步

进入 P2/P3：生成 L1 稳定观察子集报告，并将任务管理队列匹配与质量门收束到 L1 稳定子集。

## P2/P3 续跑断点

当前切片：

```text
D455 稳定观察子集与轮廓跨帧稳定化实现计划 P2/P3。
P2：外设线程从 L0 原始逐簇识别报告派生 L1 稳定观察子集报告。
P3：任务管理外设报告匹配和逐簇识别质量门只允许 L1 稳定子集进入观察事实承接 / 提交入口候选。
```

允许修改范围：

```text
外设线程_D455深度相机.ixx
外设观察报告队列.ixx
任务模块.管理工作线程.ixx
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
必要时更新 计划/计划索引.md 的当前主动证据段。
```

禁止修改范围：

```text
不修改 特征值主信息类 坐标 / 轮廓存储。
不新增模拟样本、夹具入口、伪造报告或伪动作动态。
不把 D455 材料写成世界真值、需求满足、安全值或服务值。
不把外设内部采集、滤波、分割、跟踪、稳定化登记成自我层本能方法。
不恢复任务筹办 / 任务执行为本能方法。
```

预测结果：

```text
构建应通过。
真实 D455 窗口中 L0 原始逐簇识别报告仍按有数据即入队。
当跨帧轨迹达到稳定门时，外设线程生成 L1 稳定观察子集报告，日志同时包含原始报告ID、稳定子集报告ID、原始簇数量和稳定子集簇数量。
任务管理逐簇识别等待项不再匹配 L0 原始报告；生成观察事实承接请求时，报告摘要带 L1 报告层级、来源原始报告ID和稳定簇数量。
无稳定簇时只缓存 L0，不生成正式观察事实承接请求。
```

## P2/P3 实施结果

已修改：

```text
外设观察报告队列.ixx：
    外设观察报告新增 来源原始报告ID / 来源外设帧ID；
    逐簇识别等待项只匹配 L1+ 报告；
    逐簇识别正式承接质量门要求 报告层级>=1、跨帧采集状态、稳定簇集合和稳定硬门。

外设线程_D455深度相机.ixx：
    L0 原始逐簇识别报告入队后，按稳定门派生 L1 稳定观察子集报告；
    L1 报告记录来源原始报告ID、来源外设帧ID、原始簇数量、稳定子集簇数量和稳定门阈值；
    无稳定簇时只记录跳过，不伪造正式承接材料。

任务模块.管理工作线程.ixx：
    逐簇识别观察事实承接改为依据 可进入提交入口组织；
    承接摘要明确为 已匹配L1稳定逐簇识别报告。
```

已运行命令：

```text
git diff --check -- 外设观察报告队列.ixx 外设线程_D455深度相机.ixx 任务模块.管理工作线程.ixx 实施记录/20260611_D455稳定观察子集_Codex断点清单.md
退出码：0

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m > 日志\鱼巢_build_20260611_D455_stable_subset_P2P3.log 2>&1
退出码：0

.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000 > 日志\鱼巢_run_20260611_D455_stable_subset_P2P3_30s_console.log 2>&1
退出码：0

rg -n "2026-06-11 16:34:.*(直接写需求满足=是|直接写世界树=1|直接写需求树=1|直接写安全值=1|直接写服务值=1|不写安全值=0|不写服务值=0|日志模块低级错误|状态提交确认失败|后台worker工作项进入死信)" 日志\鱼巢_run_20260611.低值g
退出码：1
```

关键证据：

```text
构建日志：
    日志/鱼巢_build_20260611_D455_stable_subset_P2P3.log line 140：已成功生成。
    日志/鱼巢_build_20260611_D455_stable_subset_P2P3.log line 147：0 个错误。

真实 D455 运行：
    日志/鱼巢_run_20260611.低值g line 95518：
        稳定子集报告入队，报告ID=39，报告层级=1，来源原始报告ID=38，来源外设帧ID=936，原始簇数量=15，稳定子集簇数量=13。
    日志/鱼巢_run_20260611.低值g line 95527：
        任务管理生成观察事实承接请求，报告ID=39，依据含 报告层级=1、逐簇稳定硬门=1、逐簇稳定硬门簇=13、跨帧稳定簇=13、来源原始报告ID=38。
    日志/鱼巢_run_20260611.低值g line 95530：
        任务管理处理一次，摘要=已匹配L1稳定逐簇识别报告，报告已消费标记=1。
    日志/鱼巢_run_20260611.低值g line 95595：
        自我线程外设观察事实承接完成，报告ID=35，报告层级=1，禁止直接写世界真值=是。
```

当前判断：

```text
P2/P3 已闭合。
L0 原始逐簇识别报告仍作为缓存 / 诊断 / 回查材料；
L1 稳定观察子集已成为任务管理逐簇识别正式承接 / 提交入口候选口径；
本切片未修改 特征值主信息类 坐标 / 轮廓存储；
未把 D455 材料写成世界真值、需求满足、安全值或服务值。
```

下一步：

```text
进入 P4：复核 6150 稳定子集桥是否只消费 L1 来源报告。
```

## P4 续跑断点

当前切片：

```text
D455 稳定观察子集与轮廓跨帧稳定化实现计划 P4。
复核 6150 稳定子集桥是否只消费 L1 来源报告；
若发现自我侧方法仍可能从“最新逐簇报告”读到 L0，则收束为优先指定报告ID / L1 正式报告。
```

允许修改范围：

```text
自我动作实现.外设模块.ixx
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
必要时更新 计划/计划索引.md 的当前主动证据段。
```

禁止修改范围：

```text
不修改 特征值主信息类 坐标 / 轮廓存储。
不新增模拟样本、夹具入口、伪造报告或伪动作动态。
不把 D455 材料写成世界真值、需求满足、安全值或服务值。
不把 L0 原始材料作为 6150 稳定子集桥正式来源。
不恢复任务筹办 / 任务执行为本能方法。
```

预测结果：

```text
构建应通过。
真实 D455 窗口若自然进入 6150，日志中的 来源报告ID 应为 L1 稳定观察子集报告ID。
6150 若失败，应能区分缺来源报告、来源报告未达 L1 正式质量门、缺稳定对应存在、扫描可启动未形成或目标观察约束不可用。
若 6150 成功，稳定子集扫描目标上下文状态应为 1，稳定子集扫描约束写入数量应大于 0。
```

## P4 验证停机阻塞续跑断点

当前观察：

```text
P4 真实运行窗口已生成 L1 稳定观察子集报告和任务管理观察事实承接请求；
但 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000` 未自然退出，
日志停在 `自我线程/停止 | 等待工作线程join开始`；
随后任务管理 worker 仍继续生成上行消息和外设等待项。
```

允许追加修改范围：

```text
自我线程模块.impl.cpp
鱼巢.cpp
```

预测结果：

```text
先停止任务管理工作线程池，再等待自我线程 join，可避免停机期间继续投递任务管理上行消息；
若自我线程仍等待外设输入停止，则程序退出守卫应先停止 D455 外设线程，再停止自我线程；
构建应通过；
再次运行真实窗口时程序应能自然退出，若 6150 未自然命中，只记录未命中，不写成业务通过。
```

## 禁止宣称项

- 不宣称相机原始轮廓已经稳定。
- 不宣称当前场景特征值明确状态已完成。
- 不宣称 6150 / 扫描 / 跟踪业务链已闭合。
- 不宣称自我苏醒完成或内部循环全部完成。

## P4 观察承接报告ID回写续跑断点

当前观察：

```text
P4 stopfix2 真实运行已能自然退出；
任务能选中并调度 6150，但 6150 执行输出仍是方法前置条件未满足；
原因指向观察事实承接只唤醒来源需求重筹办，尚未把 L1 报告ID写回任务可执行输入。
本断点不把 6150 稳定子集桥写成已通过。
```

允许追加修改范围：

```text
任务模块.管理界面线程.impl.cpp
任务模块.筹办.ixx
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
```

预测结果：

```text
观察事实更新唤醒时，报告ID应写入任务虚拟存在；
下一轮筹办应把任务虚拟存在中的外设观察报告ID复制到新可执行输入参数场景；
真实 D455 窗口若再次进入 6150，提交日志应出现 来源报告ID>0、来源报告为逐簇识别=1、来源报告层级=1；
若仍失败，只记录具体缺口，不写成业务通过。
```

## P4 报告ID回写后访问冲突排查断点

当前观察：

```text
P4 reportid_input 构建通过；
真实 D455 30 秒窗口中，观察事实唤醒已能把 L1 报告ID写入任务虚拟存在；
下一轮筹办已能把任务虚拟存在中的外设观察报告ID复制到新的可执行输入参数场景和输入条件包；
但该运行退出码为 -1073741819，属于访问冲突，不能作为业务通过证据。
17:19 日志还显示观察唤醒侧同时尝试直接写已有可执行输入参数场景 / 输入条件包，
该直接写不是 P4 必需路径，且存在与任务管理 worker 正在使用旧输入场景并发的风险。
```

允许追加修改范围：

```text
任务模块.管理界面线程.impl.cpp
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
```

禁止修改范围：

```text
不扩大为任务状态推进改造。
不新增兼容路径或 fallback。
不把报告ID直接写成需求满足、世界真值、安全值或服务值。
不把 6150 写成已通过。
```

预测结果：

```text
观察事实唤醒侧只把报告ID写入任务虚拟存在；
可执行输入参数场景和输入条件包继续由下一轮任务筹办复制；
构建应通过；
真实 D455 窗口应避免 reportid_input 运行中出现的访问冲突；
若 6150 仍未完成，只记录新的结构化缺口。
```

## P4 6150 执行前模式匹配诊断断点

当前观察：

```text
P4 singlepath 真实运行退出码 0，访问冲突未复现；
观察唤醒侧已只写任务虚拟存在，下一轮筹办已把报告ID复制到 6150 的可执行输入参数场景；
但 6150 仍在本能动作管理检查阶段返回 `输入参数不满足`，未进入 `提交_当前观察范围可观测单位存在对应事实` 正文日志。
现有日志不能说明模式匹配缺的是哪个条件节点。
```

允许追加修改范围：

```text
本能动作管理类.ixx
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
```

禁止修改范围：

```text
不改变本能动作是否可调用的判定结果。
不放宽 6150 条件匹配。
不新增兼容路径或旧入口。
不把 6150 写成已通过。
```

预测结果：

```text
只为 `提交_当前观察范围可观测单位存在对应事实` 输出检查失败诊断；
诊断应包含输入场景、条件场景、输入子节点数、条件子节点数、未满足模式节点数和首个未满足模式节点；
构建应通过；
真实 D455 窗口若 6150 仍被模式匹配阻断，应能从日志定位具体缺口。
```

## P4 6150 正文入口与正式报告ID诊断断点

当前观察：

```text
P4 6150_diag 真实运行退出码 0，访问冲突未复现；
观察唤醒侧只写任务虚拟存在，下一轮筹办仍能把外设观察报告ID复制到可执行输入参数场景；
任务执行入口多次选中 6150，但最新 17:30 窗口没有出现 `当前观察范围可观测单位存在对应事实提交` 正文日志；
`本能动作管理/6150检查失败诊断` 也未出现，说明现有诊断点没有覆盖真实阻断位置。
本断点不把 6150 稳定子集桥写成已通过。
```

允许追加修改范围：

```text
自我动作实现.外设模块.ixx
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
```

禁止修改范围：

```text
不改变 6150 是否可调用的判定。
不放宽正式逐簇报告口径。
不新增兼容路径或 fallback。
不把报告ID直接写成需求满足、世界真值、安全值或服务值。
不把 6150 写成已通过。
```

预测结果：

```text
只在 6150 正文入口 / 正文输出摘要处追加诊断日志；
真实 D455 窗口若 6150 正文被调用，应记录输入场景、输出场景、输入报告ID和正式逐簇报告缺口说明；
若正文仍未被调用，则阻断点继续回到执行桥或动作集调用层，不写成业务通过。
```

## P4 6150 动作调用出口诊断断点

当前观察：

```text
P4 body_diag 构建通过，真实运行退出码 0；
最新窗口仍能看到 6150 执行入口和外设观察报告ID筹办复制；
但没有 `当前观察范围可观测单位存在对应事实提交/入口诊断`，
也没有 `本能动作管理/6150检查失败诊断`，
说明阻断点落在执行桥调用动作集前后但尚未定位到实际运行码来源。
本断点不把 6150 稳定子集桥写成已通过。
```

允许追加修改范围：

```text
任务模块.执行.ixx
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
```

禁止修改范围：

```text
不改变执行桥方法结构检查。
不改变动作集调用结果。
不放宽 6150 条件匹配或正式逐簇报告口径。
不把报告ID直接写成需求满足、世界真值、安全值或服务值。
不把 6150 写成已通过。
```

预测结果：

```text
只在执行桥调用 6150 后记录运行存在、运行码、执行缺口类型、输入场景和输出场景；
若运行码仍是输入参数不满足或条件不满足，可据此继续追到动作管理器检查；
若运行码来自正文闭环，则应同步出现 6150 正文入口诊断。
```

## P4 无执行缺口被误转方法前置缺口修正断点

当前观察：

```text
P4 exec_diag 构建通过，真实运行退出码 0；
执行桥已确认 6150 动作调用返回 `运行码=0`、`有执行缺口=0`、`执行缺口类型=1yP`；
同一轮随后仍被任务管理工作线程转成 `执行缺口通用回执` 和 `方法前置条件未满足`。
据此判断当前堵点不再是报告ID读取，而是执行推进回执转换没有排除 `无执行缺口`。
本断点不把 6150 稳定子集桥写成已通过。
```

允许追加修改范围：

```text
任务模块.管理工作线程.ixx
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
```

禁止修改范围：

```text
不改变任务执行普通函数的职责边界。
不把任务执行注册为本能方法。
不直接写需求满足、世界真值、安全值或服务值。
不放宽正式逐簇报告口径。
不把 6150 写成已通过。
```

预测结果：

```text
运行码成功且 `执行缺口类型=无执行缺口` 时，不再进入执行缺口通用回执；
该路径应继续走动作成功后的目标结果状态读取 / 无可比较目标结果处理；
真实 D455 窗口若 6150 仍不能形成扫描约束，应暴露新的后续缺口，而不是继续停在方法前置条件未满足。
```

## P4 6150 动作集注册入口诊断断点

当前观察：

```text
P4 no_gap_fix2 构建通过，真实运行退出码 0；
6150 已不再进入 `执行缺口通用回执 / 方法前置条件未满足`；
同一窗口出现 `任务执行桥/6150动作调用结果诊断 | 运行码=0 | 有执行缺口=0 | 执行缺口类型=1yP`，
随后进入 `动作成功无可比较目标结果 | 原因=缺少实际结果状态`；
但最新窗口没有 `当前观察范围可观测单位存在对应事实提交/入口诊断` 和报告ID读取诊断。
据此预测当前堵点在动作集查询/动作函数进入/运行结果来源之间，尚不能写成 6150 正文已执行或稳定子集桥已通过。
```

允许追加修改范围：

```text
本能动作管理类.ixx
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
```

禁止修改范围：

```text
不改变动作集注册表内容。
不改变 6150 条件匹配、报告ID读取或正式逐簇报告口径。
不改任务执行、任务筹办、需求满足、世界事实、安全值或服务值写入。
不把 6150 写成已通过。
```

预测结果：

```text
只对 6150 记录动作集查询到的函数名、是否通过检查、是否进入动作函数、动作函数返回运行码和运行存在；
若动作函数未进入而仍返回成功，则继续追执行功能函数内的检查结果来源；
若动作函数进入但没有正文入口诊断，则继续追注册到的函数指针或总模块包装函数；
若正文入口诊断出现，则回到报告ID和正式逐簇报告口径验证。
```

## P4 本能动作运行码成功闭环判定修正断点

当前观察：

```text
P4 action_registry_diag 构建通过，真实运行退出码 0；
6150 动作集诊断显示 `阶段=检查失败 | 运行码=0 | 已进入动作函数=0`；
执行桥同步显示 `运行码=0 | 有执行缺口=0`，但任务管理仍只能进入 `动作成功无可比较目标结果`；
代码检查发现 `创建本能动作运行结果实例` 对运行码 0 的检查结果把 `运行准备完成` 写入“方法执行情况”，读取端只认 `执行成功`，
所以 `执行功能函数` 把检查成功结果误读成检查失败并提前返回。
本断点不把 6150 正文调用、扫描约束或稳定子集桥写成已通过。
```

允许追加修改范围：

```text
本能动作管理类.ixx
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
```

禁止修改范围：

```text
不改变 6150 方法注册和动作句柄。
不放宽 6150 条件匹配、报告ID读取或正式逐簇报告口径。
不直接写需求满足、世界真值、安全值或服务值。
不把任务筹办或任务执行改成本能方法。
不把 6150 写成已通过。
```

预测结果：

```text
运行码 0 的本能动作运行结果应写入 `方法执行情况=执行成功`；
6150 的 `检查是否可调用` 不再被 `执行功能函数` 当成检查失败提前返回；
真实 D455 窗口应出现 `动作函数调用前 / 动作函数返回` 或 6150 正文入口诊断；
若正文运行后仍缺正式逐簇报告或扫描约束，则继续按日志暴露后续缺口。
```

## P4 运行结果成功后退出收口修正断点

当前观察：

```text
P4 action_success_fix 构建通过；
真实运行中 6150 已越过动作检查，出现 `动作函数调用前`、6150 正文入口诊断和报告ID读取诊断；
报告ID读取仍显示 `报告ID=0 | 来源报告存在=0 | 正式逐簇报告缺口说明=缺来源报告`，不能写成稳定子集桥已通过；
但运行命令超时，退出守卫停在 `开始停止自我线程` 后，任务管理界面线程仍持续输出关停期巡检日志。
据此预测当前需要先让任务管理界面循环在自我线程 join 前收到停止请求，避免关停期继续巡检干扰收口。
```

允许追加修改范围：

```text
任务模块.管理界面线程.ixx
任务模块.管理界面线程.impl.cpp
自我线程模块.impl.cpp
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
```

禁止修改范围：

```text
不改变任务管理 worker 池停止函数的清理语义。
不清空或改写任务业务结果。
不改变 6150 报告ID读取、正式逐簇报告口径或扫描约束生成逻辑。
不直接写需求满足、世界真值、安全值或服务值。
不把本轮写成 P4 通过。
```

## P4 6150 输出场景旧派生需求清理真实运行前断点

当前预测：

```text
6150 缺正式 L1 逐簇报告时，输出结果场景可能残留上一段方法运行写入的 `派生需求` 标记；
若不先清理，任务执行桥会把旧标记误读成当前 6150 的执行输出缺口，并可能生成错误类型的活动子需求；
本切片只允许在 6150 提交入口开始时清理输出场景旧派生需求标记，并在缺正式逐簇报告快退前写入明确的 `外设观察材料可回查状态 0 -> 1` 派生需求回执；
该回执只表达材料 / 报告可回查缺口，不创建对应事实，不创建映射表，不写世界真值，不写需求满足，不写安全值或服务值；
真实运行后应能看到 6150 缺报告路径的派生需求词从旧像素簇验证类缺口收束为材料可回查缺口，若后续仍被其他活动子需求阻塞，再按新日志继续追。
本断点不把 6150、P4、扫描可启动、稳定对应事实或内部循环写成完成。
```

允许追加修改范围：

```text
自我动作实现.外设模块.ixx
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
```

禁止修改范围：

```text
不放宽 L1 稳定子集正式报告口径，不允许回退读取 L0 最新报告。
不把缺报告快退写成成功。
不在缺报告路径创建对应事实或映射表。
不直接写需求满足、世界真值、安全值或服务值。
不跳过一般父子需求阻塞规则。
不把任务筹办 / 任务执行改成本能方法或动作动态来源。
不把本轮写成 P4 通过。
```

预测结果：

```text
自我线程停止时，先请求任务管理界面循环停止，再等待自我工作线程收口；
完整的任务管理界面线程 join 和队列清理仍由原 `停止任务管理界面线程` 执行；
真实运行应出现任务管理界面后台循环退出，并且 `鱼巢.exe` 能在超时窗口内自然退出；
若退出恢复，后续继续追 6150 的正式报告ID来源缺口。
```

## P4 worker8 停止未退出诊断断点

当前观察：

```text
P4 shutdown_gate_fix 构建通过；
真实运行进入守卫退出后，D455 外设线程已停止，自我线程也已收到停止请求并退出主循环；
停止流程停在 `自我线程/停止 | 停止任务管理工作线程池开始`；
任务管理后台 worker 1-7、9-20 已输出退出日志，但 worker 8 未输出退出日志；
任务管理界面线程仍在关停期巡检，新增的界面循环停止请求尚未执行到，因为 worker 池 join 未返回。
据此预测当前堵点在 worker 8 的工作项派发或派发后收口路径，不能写成关停收口已修复。
```

允许追加修改范围：

```text
任务模块.管理界面线程.impl.cpp
自我线程模块.impl.cpp
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
```

禁止修改范围：

```text
不改变 6150 报告ID读取、正式逐簇报告口径或扫描约束生成逻辑。
不改任务筹办、任务执行、本能动作注册、需求满足、世界事实、安全值或服务值写入。
不把 worker 线程强制 detach 或静默跳过 join。
不把本轮写成 P4 通过或 6150 通过。
```

预测结果：

```text
先补 worker 池停止和 worker 派发阶段诊断，记录每个 worker join 前后、当前工作项、阶段、工作项类型、任务主键和最近原因；
若 worker 8 卡在等待或取项前，应能看到停止请求后自然退出；
若 worker 8 卡在工作项派发内部，应继续追该工作项对应的任务管理工作线程执行路径；
若只是缺少界面循环停止信号，则应在不改变业务语义的前提下调整停止请求顺序。
```

## P4 后台 worker 弹窗阻塞收口修正断点

当前观察：

```text
P4 worker8_diag 构建通过；
真实运行 90 秒未自然退出，进程已杀掉；
新增 join 诊断显示 worker 8 已退出并完成 join，当前卡在 worker 10；
worker 10 当前快照为 `派发调用中 | 工作项=31 | 类型=执行 | 任务=F`；
任务 F 执行 6150 后出现 `执行桥逻辑错误：执行缺失派生需求缺少执行桥输出的目标状态`，随后记录 `弹窗错误提示`，未出现 `弹窗错误提示已返回`；
据此预测 worker 10 被后台线程同步 MessageBox 阻塞，导致任务工作项无法返回，worker 池 join 无法完成。
本断点不把 6150、P4 或退出收口写成完成。
```

允许追加修改范围：

```text
任务模块.管理工作线程.ixx
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
```

禁止修改范围：

```text
不静默吞掉执行桥逻辑错误。
不删除错误日志或弹窗提示语义。
不改变 6150 报告ID读取、正式逐簇报告口径或扫描约束生成逻辑。
不改变派生需求入树、需求满足、世界事实、安全值或服务值写入。
不把本轮写成 P4 通过。
```

预测结果：

```text
执行桥逻辑错误仍必须写 `项目运行错误日志`；
弹窗提示改为不阻塞后台 worker 的异步派发或等价非阻塞提示；
任务工作项 31 应能从 `提交任务工作项` 返回，worker 10 应能退出并完成 join；
若退出收口恢复，下一步继续追 6150 报告ID=0 / 缺来源报告的业务缺口。
```

## P4 报告ID到位后父任务仍被活动子需求阻塞断点

当前观察：

```text
P4 async popup 修正后构建通过，真实 D455 运行窗口退出码 0；
后台 worker join 已能完成，说明弹窗阻塞不是当前继续推进的主堵点；
6150 已进入正文，但初始执行仍显示 `报告ID=0 | 来源报告存在=0 | 正式逐簇报告缺口说明=缺来源报告`；
随后任务管理质量门自然收到 L1 逐簇识别正式报告，并将 `报告ID=13` 写入任务 F 的任务虚拟存在；
任务 F 因观察事实更新被唤醒重筹办，但下一轮筹办在输入参数复制前命中 `存在活动子需求，筹办阻塞`；
据此预测当前堵点是：报告材料到位后，旧的缺报告 / 方法供料活动子需求仍挡住父任务重筹办，导致报告ID无法进入 6150 可执行输入场景。
本断点不把 6150、P4、扫描可启动或稳定对应事实写成完成。
```

允许追加修改范围：

```text
任务模块.筹办.ixx
任务模块.管理界面线程.impl.cpp
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
```

禁止修改范围：

```text
不放宽 L1 稳定子集正式报告口径，不允许回退读取 L0 最新报告。
不跳过一般父子需求阻塞规则，只允许处理已由正式报告材料满足的旧供料缺口。
不直接写需求满足、世界真值、安全值或服务值。
不把任务筹办 / 任务执行改成本能方法或动作动态来源。
不把本轮写成 P4 通过。
```

预测结果：

```text
如果活动子需求只是先前因缺外设观察报告ID生成的供料缺口，且任务虚拟存在已持有正式 L1 报告ID，则父任务下一轮筹办应能继续复制报告ID并重新执行 6150；
如果活动子需求不是报告供料缺口，或正式 L1 报告ID仍不存在，则仍按现有活动子需求阻塞；
真实运行应出现 `外设观察报告ID参数筹办 | ... 报告ID=13` 以及后续 6150 `报告ID读取诊断` 使用同一正式报告ID；
若 6150 使用报告ID后仍有扫描约束或提交入口缺口，再按新日志继续追，不宣称 P4 完成。
```

## P4 材料子需求当前状态刷新真实运行前断点

当前预测：

```text
正式 L1 逐簇报告到位后，来源需求下旧的 `外设观察材料可回查状态=1` 活动子需求应只刷新自身当前状态；
刷新动作不直接截止子需求，不直接写父需求满足，不写世界真值、安全值或服务值；
父任务是否能继续筹办，仍以需求树权重、活动集和真实日志为准；
若该子需求刷新后仍存在其他活动子需求，父任务继续阻塞属于有效结果；
若活动阻塞解除，任务 F 下一轮筹办才应复制正式 L1 报告ID并重新执行 6150。
本断点不把子需求刷新、6150、P4、扫描可启动或稳定对应事实写成完成。
```

允许追加修改范围：

```text
自我线程模块.impl.cpp
任务模块.筹办.ixx
任务模块.管理界面线程.impl.cpp
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
```

禁止修改范围：

```text
不放宽 L1 稳定子集正式报告口径，不允许回退读取 L0 最新报告。
不跳过一般父子需求阻塞规则。
不直接标记旧材料子需求满足或截止。
不直接写父需求满足、世界真值、安全值或服务值。
不把任务筹办 / 任务执行改成本能方法或动作动态来源。
不把本轮写成 P4 通过。
```

## P4 6150 缺报告路径快退真实运行前断点

当前预测：

```text
`工作项=32` 的任务 F 已进入 6150，输入场景 `3fG` 读取到 `报告ID=0` 后没有返回动作调用结果诊断；
据此预测缺正式报告路径仍在创建对应事实、映射表或写提交状态动态，导致后台 worker 停止 join 被占住；
缺正式 L1 报告时，6150 应只写运行虚拟存在和输出场景失败回执、失败原因、报告ID和缺口状态，然后快退返回；
该快退不创建当前观察范围对应事实，不创建可观测单位映射表，不写世界真值，不写需求满足、安全值或服务值；
如果正式 L1 报告存在并满足提交入口组织口径，仍按原稳定子集桥逻辑继续生成扫描目标上下文。
本断点不把停止收口、6150、P4、扫描可启动或稳定对应事实写成完成。
```

允许追加修改范围：

```text
自我动作实现.外设模块.ixx
自我线程模块.impl.cpp
任务模块.筹办.ixx
任务模块.管理界面线程.impl.cpp
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
```

禁止修改范围：

```text
不放宽 L1 稳定子集正式报告口径，不允许回退读取 L0 最新报告。
不把缺报告快退写成成功。
不在缺报告路径创建对应事实或映射表。
不直接写需求满足、世界真值、安全值或服务值。
不把本轮写成 P4 通过。
```

## P4 6150 派生需求运行存在镜像真实运行前断点

当前预测：

```text
上轮真实运行显示，6150 缺正式逐簇报告路径已能在输出场景写入 `外设观察材料可回查状态 0 -> 1` 派生需求；
但输出结果场景是共享宿主，其他外设方法运行会清理或覆盖该场景上的派生需求标记，导致任务执行桥在读取 6150 结果时仍可能误读成旧失败原因或空派生目标；
本切片只允许把当前方法运行产生的派生需求标记同步到本轮运行存在 / 输出运行存在，并让任务执行桥优先读取方法运行结果上的派生需求事实；
该镜像只承载当前方法运行回执，不放宽 L1 正式报告口径，不创建对应事实，不创建映射表，不写世界真值、需求满足、安全值或服务值。
本断点不把 6150、P4、扫描可启动、稳定对应事实或内部循环写成完成。
```

允许追加修改范围：

```text
自我动作实现.外设模块.ixx
任务模块.管理工作线程.ixx
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
```

禁止修改范围：

```text
不放宽 L1 稳定子集正式报告口径，不允许回退读取 L0 最新报告。
不把缺报告快退写成成功。
不把输出场景共享残留作为当前方法运行的权威回执。
不在缺报告路径创建对应事实或映射表。
不直接写需求满足、世界真值、安全值或服务值。
不跳过一般父子需求阻塞规则。
不把任务筹办 / 任务执行改成本能方法或动作动态来源。
不把本轮写成 P4 通过。
```

预测结果：

```text
缺正式 L1 报告的 6150 快退路径，应在方法运行结果上保留 `派生需求词=外设观察材料可回查状态`、派生当前状态和派生目标状态；
任务管理工作线程的 `执行输出派生尾部证据` 应优先显示方法运行结果上的 Tx 派生缺口，不再因共享输出场景被后续方法覆盖而退化成 `24Y` 或空派生目标；
若正式 L1 报告到位后仍失败，应继续追稳定对应存在数量、扫描可启动状态和目标上下文，不把本切片写成 P4 通过。
```

## P4 正式 L1 扫描上下文结构化回执修正断点

当前观察：

```text
上一轮真实运行证明缺报告路径的 Tx 派生缺口已能稳定读到；
正式 L1 路径中，任务 F 的 6150 已读取 报告ID=13，报告层级 L1，可进入提交入口组织=1；
同一轮 6150 已生成 16 个稳定子集扫描约束，扫描可启动状态=1，稳定子集扫描目标上下文状态=1；
但 6150 仍把“全范围对应闭合未达成”折叠成整体方法执行失败，任务执行桥只看到 24Y / 当前方法条件不满足，不能按结构化实际结果继续承接扫描等待项。
本断点不把 6150、P4、全范围对应闭合、当前场景特征值明确或内部循环写成完成。
```

允许追加修改范围：

```text
自我动作实现.外设模块.ixx
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
```

禁止修改范围：

```text
不放宽 L1 稳定子集正式报告口径，不允许回退读取 L0 最新报告。
不把全范围对应闭合未达成写成当前场景特征值明确。
不直接写需求满足、世界真值、安全值或服务值。
不把任务筹办 / 任务执行改成本能方法或动作动态来源。
不把本轮写成 P4 通过。
```

预测结果：

```text
正式 L1 报告可用且稳定子集扫描目标上下文已生成时，6150 的方法运行闭环应记录为成功；
全范围对应事实可写、当前观察范围可观测单位存在对应闭合状态、当前场景特征值明确状态仍按真实值写入，未闭合时保持 0；
任务执行桥应进入目标状态比较或有推进未达成路径，而不是退回 24Y / 当前方法条件不满足；
任务管理界面线程应能看到稳定子集扫描等待项承接数量，不等待全范围闭合。
```

## P4 执行桥通用执行缺口非派生化断点

当前观察：

```text
上一轮真实运行仍出现“执行桥逻辑错误”弹窗；
日志显示多次弹窗的共同形态是执行缺口特征=20x，当前状态和目标状态为空；
这些记录不是 6150 显式 Tx 派生需求缺当前/目标状态，而是普通执行缺口或失败原因被执行桥升级成派生需求字段后触发完整性错误；
执行输出需要派生原子需求函数已把失败原因词记录为“仅作回执诊断”，但执行结果缺最小条件路径仍会调用派生需求构造，构造函数又把失败原因词或回执缺口特征补成需求特征。
用户边界补充：执行时不筹办，发现问题向上反馈；因此执行结果缺最小条件不能现场补做筹办式派生需求构造。
本断点不把执行缺口 20x 建成子需求，不放宽显式派生需求必须同时具备当前状态和目标状态的硬规则。
```

允许追加修改范围：

```text
任务模块.管理工作线程.ixx
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
```

禁止修改范围：

```text
不把任务筹办 / 任务执行改成本能方法或动作动态来源。
不把失败原因词、执行缺口类型或回执摘要当成派生需求目标事实。
不让执行结果缺最小条件路径现场构造派生需求。
不跳过显式派生需求的结构化完整性检查。
不直接写需求满足、世界真值、安全值或服务值。
不把本轮写成 P4 通过。
```

预测结果：

```text
若执行输出没有显式派生需求词，失败原因词只作为执行缺口回执诊断，不再补入派生需求字段；
若执行结果缺最小条件但没有完整显式派生需求当前/目标状态，任务管理工作线程记录“执行阶段缺口向上反馈”，保留执行缺口回执，不现场构造派生需求，不弹出“执行桥逻辑错误”；
若方法显式写出派生需求词但缺当前状态或目标状态，仍按执行桥逻辑错误记录并弹窗；
正式 6150 缺报告 Tx 路径若当前/目标状态完整，应继续走外设内部材料字段拒绝需求化日志，而不是退化成 20x 派生需求。
```

## P4 外设承接后台 debug error 证据化断点

当前观察：

```text
P4 exec_feedback 构建通过；
真实运行 30 秒窗口退出码=3，用户看到 debug error；
同一轮日志中没有新的执行桥逻辑错误弹窗，08:27 之后执行桥逻辑错误计数=0；
日志出现“执行阶段缺口向上反馈”，证明执行结果缺最小条件路径已不再现场构造派生需求；
6150 正式 L1 路径已读取 报告ID=13，来源报告类型=逐簇识别报告，报告层级=L1，可进入提交入口组织=1；
6150 输出 `运行闭环成功=1`，稳定子集扫描目标上下文状态=1，稳定子集扫描约束写入数量=10；
但主线程只记录到 `命令行/自检日志等待开始`，没有 `等待完成` 或程序退出守卫日志；
最后活跃日志来自外设承接后台巡检生成上行，且该后台循环原先为 noexcept，若巡检中的 STL 分配、容器复制或回调外溢异常，会直接触发 std::terminate，在 Debug 下表现为 debug error 且不落项目错误日志。
本断点不把 6150 目标任务完成、当前场景特征值明确或 P4 通过写成完成。
```

允许追加修改范围：

```text
任务模块.管理界面线程.impl.cpp
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
```

禁止修改范围：

```text
不静默吞掉后台异常。
不改变外设观察报告质量门、扫描等待项生成、观察事实承接或任务执行语义。
不改变 6150 报告ID读取、正式逐簇报告口径或稳定子集桥逻辑。
不直接写需求满足、世界真值、安全值或服务值。
不把本轮写成 P4 通过。
```

预测结果：

```text
外设承接后台巡检调用不再因未捕获异常直接触发 Debug Error / std::terminate；
若该巡检路径再次抛出异常，应记录 `任务管理界面线程/外设承接后台巡检异常`，并停止外设承接后台线程；
若不再异常，真实运行应能继续到自检等待完成和守卫停止路径；
执行桥逻辑错误弹窗仍不应在 08:27 后的新运行中复现。
```

验证结果：

```text
构建命令：
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /fileLogger /fileLoggerParameters:"LogFile=日志\鱼巢_build_20260612_D455_P4_exec_feedback2.log;Verbosity=normal" /consoleLoggerParameters:ErrorsOnly
退出码=0。

真实运行命令：
.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000 *> "日志\鱼巢_run_20260612_D455_P4_exec_feedback2_30s_console.log"
退出码=0。

08:33 之后日志统计：
执行桥逻辑错误计数=0；
Debug / debug / 断言 / abort 关键词计数=0；
外设承接后台巡检异常计数=0；
命令行/自检日志等待完成计数=1；
程序退出/守卫 | 自我线程已停止计数=1。

执行边界证据：
任务管理工作线程出现 `执行阶段缺口向上反馈`，处置=不上报派生需求；
说明执行结果缺最小条件路径不再现场构造派生需求。

6150 正式 L1 证据：
任务 F / 方法 1x 多次读取 报告ID=13；
来源报告为逐簇识别=1，来源报告层级=1，可进入提交入口组织=1；
运行闭环成功=1，扫描可启动状态=1，稳定子集扫描目标上下文状态=1；
稳定子集扫描约束写入数量在真实运行中为 9-14；
任务管理界面线程已承接稳定子集全量扫描等待项，且记录 `不等待全范围闭合=1`。

仍未宣称完成项：
全范围对应事实可写仍为 0；
当前场景特征值明确状态仍为 0；
部分轮次仍出现目标结果不符合或执行缺口 24Y，但已作为执行回执向上反馈，不再弹执行桥逻辑错误。
本验证不宣称 P4 完成、自我苏醒完成或内部循环全部完成。
```

## P5 扫描 / 跟踪消费 L1 稳定子集真实运行前断点

当前观察：

```text
上一轮真实运行已证明 P4 正式 L1 路径可闭合到扫描等待项：
6150 多次读取 L1 逐簇识别报告，运行闭环成功=1，扫描可启动状态=1，稳定子集扫描目标上下文状态=1；
任务管理界面线程已承接稳定子集全量扫描等待项，记录 `不等待全范围闭合=1`；
同一日志中自然出现扫描变化报告和目标跟踪报告，报告层级=2，带目标观察约束和稳定子集 generation；
但外设线程生成扫描变化 / 跟踪报告时仍以本帧 L0 `逐簇识别报告` 作为输入，并以最新逐簇报告作扫描基准；
这会让 P5 的正式扫描 / 跟踪材料口径可能退回未稳定 L0，且日志中扫描 / 跟踪报告的来源原始报告ID可能为 0 或生成方法仍显示旧逐簇入口。
本断点不把扫描变化动态、跟踪动态、当前场景特征值明确或 P4/P5 总体验收写成完成。
```

允许追加修改范围：

```text
外设线程_D455深度相机.ixx
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
```

禁止修改范围：

```text
不放宽 L1 稳定子集硬门。
不让扫描 / 跟踪报告从未稳定 L0 原始簇集合生成。
不直接写需求满足、世界真值、安全值或服务值。
不改变 6150 正式逐簇报告读取口径。
不把任务筹办 / 任务执行改成本能方法或动作动态来源。
不把本轮写成 P5 全部通过。
```

预测结果：

```text
外设线程生成扫描变化报告和目标跟踪报告时，当前材料应使用本帧 L1 稳定观察子集报告；
若本帧没有 L1 稳定子集，应跳过扫描 / 跟踪正式报告生成，只记录缺 L1 稳定子集材料；
扫描变化报告的非首帧基准应使用上一条 L1 稳定观察子集报告；没有上一条 L1 时只形成首帧基准说明；
扫描 / 跟踪报告日志应能回溯来源原始报告ID、来源外设帧ID和 L1 稳定子集口径；
真实运行仍不得出现执行桥逻辑错误、Debug Error、世界真值直写或需求满足直写。
```

验证结果：

```text
构建命令：
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /fileLogger /fileLoggerParameters:"LogFile=日志\鱼巢_build_20260612_D455_P5_stable_scan_tracking.log;Verbosity=normal" /consoleLoggerParameters:ErrorsOnly
退出码=0。

真实运行命令：
.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000 *> "日志\鱼巢_run_20260612_D455_P5_stable_scan_tracking_30s_console.log"
退出码=0。

08:39:54 之后日志统计：
执行桥逻辑错误计数=0；
Debug / debug / 断言 / abort 关键词计数=0；
外设承接后台巡检异常计数=0；
命令行/自检日志等待完成计数=1；
程序退出/守卫 | 自我线程已停止计数=1。

P5 口径证据：
扫描变化报告使用 `D455_从L1稳定子集报告生成扫描变化报告`，未再出现旧的 L0 扫描生成方法；
目标跟踪报告使用 `D455_从L1稳定子集报告生成跟踪报告`；
扫描 / 跟踪报告日志可回溯 来源原始报告ID、来源外设帧ID、目标观察约束ID、目标约束使用状态；
同一轮 6150 正式 L1 路径仍保持 报告层级=1、可进入提交入口组织=1、运行闭环成功=1、扫描约束写入数量>0；
本轮未写世界真值、需求满足、安全值或服务值。

新发现堵点：
报告ID=118 是 L1 稳定子集生成的扫描变化报告，报告层级=2，已由观察事实承接请求交给扫描方法链；
但来源需求重筹办通用唤醒又把报告ID=118 写入任务 D 的输入场景 6P0；
随后 6150 从 6P0 读取到 报告ID=118、来源报告类型=扫描变化报告、报告层级=2，触发 `缺报告失败快退`，输出运行=6UF；
根因指向任务桥 `私有_写观察事实报告ID到任务输入` 没有按当前方法供料口径过滤报告类型，扫描 / 跟踪报告会被回灌给仍应消费逐簇识别报告的 6150 任务。
```

## P6 扫描 / 跟踪报告不回灌 6150 任务输入断点

当前观察：

```text
P5 已把扫描 / 跟踪正式报告材料来源改为 L1 稳定子集；
真实运行仍出现 6150 读取 layer=2 扫描变化报告的单点退回；
该退回不是外设报告生成口径错误，而是观察事实更新任务唤醒把扫描报告ID写回原来源任务输入；
6146 / 6147 扫描和跟踪方法链已经存在，扫描 / 跟踪报告应进入对应后续方法，不应回灌给 6150 逐簇识别提交方法。
```

允许追加修改范围：

```text
任务模块.管理界面线程.impl.cpp
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
```

禁止修改范围：

```text
不改变 6146 / 6147 扫描、跟踪报告承接路径。
不让 6150 消费扫描变化报告或跟踪报告。
不放宽 L1 稳定子集和正式逐簇识别报告硬门。
不直接写需求满足、世界真值、安全值或服务值。
不把执行阶段发现的问题现场筹办成新子任务。
不把本轮写成 P6 或内部循环总体验收通过。
```

预测结果：

```text
观察事实更新任务唤醒写任务输入前，应按任务当前方法的外设观察供料口径检查报告类型；
当前方法口径=识别时，只允许逐簇识别报告ID写入输入场景；
扫描变化报告和跟踪报告命中识别口径任务时，应记录跳过日志并保持向上反馈，不写入该任务输入场景；
扫描口径和跟踪口径任务仍可接收对应报告ID；
真实运行中 6150 不应再读取 layer=2 扫描 / 跟踪报告；
执行桥逻辑错误、Debug Error、世界真值直写或需求满足直写仍不应出现。
```

验证结果：

```text
构建命令：
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /fileLogger /fileLoggerParameters:"LogFile=日志\鱼巢_build_20260612_D455_P6b_report_queue_retention.log;Verbosity=normal" /consoleLoggerParameters:ErrorsOnly
退出码=0。

真实运行命令：
.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000 *> "日志\鱼巢_run_20260612_D455_P6b_report_queue_retention_30s_console.log"
工具等待 70 秒超时；进程仍在运行，已手动停止残留进程，未取得自然退出码。

08:51:47 之后日志统计：
执行桥逻辑错误计数=0；
Debug / debug / 断言 / abort 关键词计数=0；
命令行/自检日志等待完成计数=1；
程序退出/守卫 | 自我线程已停止计数=0；
6150 读取 来源报告层级=2 计数=0；
6150 读取 来源报告层级=1 计数=11；
来源报告不可回查计数=0；
观察事实报告ID跳过任务输入计数=31。

P6b 口径证据：
报告ID=13 / 15 在扫描报告洪峰后仍可按 ID 回查为 来源报告类型=逐簇识别报告、来源报告层级=1；
08:52:41 仍能读取 报告ID=15、来源报告存在=1、来源报告层级=1；
说明报告队列容量裁剪不再把 60 秒内的 L1 稳定子集报告提前挤出。

新发现堵点：
08:52:43 程序退出守卫已记录 `D455外设线程已停止`；
随后进入 `开始停止自我线程`，但没有出现 `自我线程已停止`；
08:53 之后任务管理仍在关停期巡检，且 D455 外设线程重新出现稳定子集报告入队；
根因指向任务管理停止请求期间仍允许按外设观察等待项启动 / 驱动 D455 外设线程。
```

## P6c 任务管理停止期禁止重启 D455 外设线程断点

当前观察：

```text
P6b 已解决 L1 报告ID有效期内被容量裁剪的问题；
但真实运行未自然退出，必须手动停止残留进程；
日志显示程序退出守卫先停止 D455，随后停止自我线程期间任务管理仍处于关停期活动，并重新驱动 D455 产出稳定子集报告；
停止期应收口线程和等待项，不应再启动外设观察或生成新的外设承接上行。
```

允许追加修改范围：

```text
任务模块.管理界面线程.impl.cpp
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
```

禁止修改范围：

```text
不改变正常运行期 D455 按等待项启动逻辑。
不取消真实 D455 观察。
不把停止期跳过写成业务完成。
不直接写需求满足、世界真值、安全值或服务值。
不把本轮写成 P6 总体验收通过。
```

预测结果：

```text
任务管理停止请求中，按外设观察等待项启动 D455 应直接跳过；
任务管理停止请求中，外设等待项巡检不再生成上行或关闭等待项；
真实运行 30 秒窗口应能出现 `命令行/自检日志等待完成`、`D455外设线程已停止` 和 `自我线程已停止`；
停止期之后不应再出现新的 D455 报告入队；
P6b 的 L1 报告回查证据应保持：来源报告不可回查不应因容量裁剪复现，6150 不应读取 layer=2。
```

验证结果：

```text
构建命令：
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /fileLogger /fileLoggerParameters:"LogFile=日志\鱼巢_build_20260612_D455_P6c_shutdown_gate.log;Verbosity=normal" /consoleLoggerParameters:ErrorsOnly
退出码=0。

真实运行命令：
.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000 *> "日志\鱼巢_run_20260612_D455_P6c_shutdown_gate_30s_console.log"
退出码=0。

08:55:20 之后日志统计：
执行桥逻辑错误计数=0；
Debug / debug / 断言 / abort 关键词计数=0；
命令行/自检日志等待完成计数=1；
程序退出/守卫 | D455外设线程已停止计数=1；
程序退出/守卫 | 自我线程已停止计数=1；
外设线程_D455深度相机/停止计数=1；
D455 外设线程停止后 D455 报告入队计数=0；
6150 读取 来源报告层级=2 计数=0；
6150 读取 来源报告层级=1 计数=8；
来源报告不可回查计数=0；
观察事实报告ID跳过任务输入计数=3；
旧 L0 扫描生成方法计数=0；
L1 扫描生成方法计数=145；
L1 跟踪生成方法计数=18。

P6c 停机证据：
08:56:18.120 守卫记录 `D455外设线程已停止`；
08:56:18.479 守卫记录 `自我线程已停止`；
D455 停止时间之后未再出现 D455 报告入队。

P6/P6b 保持证据：
6150 多次读取 报告ID=13 / 24 等，来源报告存在=1、来源报告类型=1、来源报告层级=1、可进入提交入口组织=1；
来源报告不可回查未复现；
扫描 / 跟踪报告仍使用 L1 稳定子集生成方法；
扫描报告命中 6150 识别口径任务时仍记录 `观察事实报告ID跳过任务输入`，不写任务虚拟存在、不写输入场景、不写输入条件包。

仍未宣称完成项：
运行早期尚无 L1 时仍有 `缺来源报告` 快退，来源报告ID=0，属于未获得正式报告前的上行缺口；
全范围对应事实可写、当前场景特征值明确、需求满足、安全值 / 服务值仍未宣称完成或写入；
本轮只关闭 P5/P6 口径链条中的扫描/跟踪材料来源、识别口径回灌、报告队列回查和停机重启 D455 问题。
```

验证结果：

```text
构建命令：
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /fileLogger /fileLoggerParameters:"LogFile=日志\鱼巢_build_20260612_D455_P6_report_gating.log;Verbosity=normal" /consoleLoggerParameters:ErrorsOnly
退出码=0。

真实运行命令：
.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000 *> "日志\鱼巢_run_20260612_D455_P6_report_gating_30s_console.log"
退出码=0。

08:47:00 之后日志统计：
执行桥逻辑错误计数=0；
Debug / debug / 断言 / abort 关键词计数=0；
外设承接后台巡检异常计数=0；
命令行/自检日志等待完成计数=1；
程序退出/守卫 | 自我线程已停止计数=1；
6150 读取 来源报告层级=2 计数=0；
6150 读取 来源报告层级=1 计数=10；
旧 L0 扫描生成方法计数=0；
L1 扫描生成方法计数=478；
L1 跟踪生成方法计数=10。

错配门控证据：
观察事实报告ID跳过任务输入计数=26；
样例：任务 F / 当前方法 1x / 当前方法供料口径=识别 收到 报告ID=46 / 报告类型=扫描变化报告 时，记录 `报告类型与当前方法供料口径不匹配`，写任务虚拟存在=0、写输入场景=0、写输入条件包=0；
同一裁决中 `报告ID写入任务输入=0`；
逐簇识别 L1 报告仍可写入识别口径任务，报告ID=13 / 15 等记录 `报告ID写入任务输入=1`。

仍未宣称完成项：
本验证只证明扫描 / 跟踪报告不再回灌 6150 识别口径输入；
同一窗口仍有 `来源报告不可回查`，但均不是 layer=2 类型错配，而是报告ID在任务下一轮读取前从报告队列消失。
```

## P6b L1 报告ID有效期内不被容量裁剪断点

当前观察：

```text
报告ID=13 于 08:47:31.729 生成 L1 稳定观察子集报告，08:47:32.855 被写入任务 F 输入；
08:47:35.257 和 08:47:35.830，6150 仍可按报告ID=13 回查到 来源报告类型=逐簇识别报告、报告层级=1；
08:47:36.703 起，报告ID=13 已变成 `来源报告不可回查`，距离报告生成约 5 秒，远未达到 60 秒 TTL；
同一时间段扫描等待项批量生成 L2 扫描变化报告，报告ID 快速超过 64；
外设观察报告队列总容量为 64，容量裁剪直接 pop_front，不区分 L1 稳定子集报告是否仍在有效期内；
这与任务管理外设承接日志中的 `报告仍可按ID回查直到TTL清理` 承诺冲突。
```

允许追加修改范围：

```text
外设观察报告队列.ixx
实施记录/20260611_D455稳定观察子集_Codex断点清单.md
```

禁止修改范围：

```text
不把报告队列项写成世界真值主体。
不取消 TTL 清理。
不让过期报告继续作为正式材料。
不改变 L1 稳定子集正式读取口径。
不直接写需求满足、世界真值、安全值或服务值。
不把本轮写成 P6 总体验收通过。
```

预测结果：

```text
容量裁剪应优先清理已过期报告；未过期 L1 稳定观察子集报告不得被 L2 扫描 / 跟踪报告洪峰挤出；
当总队列超容量且存在可牺牲报告时，应优先清理非 L1 稳定逐簇报告；
极端情况下若所有报告都属于受保护 L1，才允许退回最老项裁剪以避免无界增长；
真实运行中报告ID写入任务输入后，6150 在下一轮读取应保持 来源报告存在=1、来源报告类型=1、来源报告层级=1；
`来源报告不可回查` 计数应显著下降，至少不应再由 60 秒内的 L1 稳定子集报告容量裁剪造成；
执行桥逻辑错误、Debug Error、世界真值直写或需求满足直写仍不应出现。
```
