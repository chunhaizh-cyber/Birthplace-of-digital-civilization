# 20260622 公共基础数据嵌套结构审计 Codex断点清单

## 0. 恢复摘要

当前主动计划：`计划/计划索引.md` 第 1 章 `P10 项目结构体审计与清理计划` 后续专项。

当前切片：

```text
索引子切片 P10b：公共基础数据嵌套结构审计首轮。
源码对象：基础数据类型.h。
来源：P10a 任务治理协议结构审计首轮闭合后，按计划索引后续专项顺序进入“公共基础数据嵌套结构审计”。
```

已复核依据：

```text
规范/禁止性规范20260611.md：第 2 章项目通用数据结构闭环。
规范/规范目录.md：一级规范 007 的优先级。
计划/计划索引.md：P10 后续专项顺序。
计划/20260618_项目结构体审计与清理计划_v0.1.md：P3/P4 结构体审计和删除前必须确认写入方、读取方、调用路径。
用户补充删除闸门：只有定义和赋值 / 成员声明的结构体可以直接删除。
```

当前判断：

```text
基础数据类型.h 中 `结构体_相机状态`、`结构体_帧时间戳`、`结构体_IMU样本` 已有 D455 写入和双目相机适配器读取路径，不删除。
`结构_抽象特征组合规则` 已挂入 `基础信息主信息类::组合规则`，且控制面板和特征类有读取 / 清理路径，不删除。
`结构体_图像缓冲_f32` 带成员函数，且当前只因 `结构体_存在观测` 嵌套命中；本切片不按直接删除处理。
```

本轮候选：

```text
基础数据类型.h::结构体_ROI
基础数据类型.h::结构体_存在观测
基础数据类型.h::点簇边界框
基础数据类型.h::点簇增强结果
基础数据类型.h::帧处理结果
配套残留：基础数据类型.h::枚举_存在观测缓冲 和只服务该枚举的 inline operator|
```

已运行复扫：

```powershell
rg -n "结构体_ROI|\.ROI\b|结构体_存在观测|存在观测列表\b|点簇边界框|点簇增强结果|点簇增强结果列表\b|帧处理结果\b" -g "*.cpp" -g "*.h" -g "*.ixx" -g "!x64/**" -g "!Debug/**" -g "!Release/**" -g "!日志/**" -g "!logs/**" -g "!详细设计/**"
rg -n "枚举_存在观测缓冲|裁剪BGR|裁剪掩膜|边缘图|置信度图|法线图|融合权重图" -g "*.cpp" -g "*.h" -g "*.ixx" -g "!x64/**" -g "!Debug/**" -g "!Release/**" -g "!日志/**" -g "!logs/**" -g "!详细设计/**"
```

复扫结果：

```text
目标结构体只在基础数据类型.h 内命中结构定义和相互成员声明；未发现外部字段读取、传参、返回或真实运行路径依赖。
配套 `枚举_存在观测缓冲` 和 `operator|` 只命中定义；删除 operator 后已按项目规则刷新函数功能说明文档。
```

构建前预测：

```text
删除上述五个无外部读取的包装结构和只服务它们的存在观测缓冲枚举 / operator，不改变 `结构体_原始场景帧`、D455 帧时间戳 / 相机状态链、结构体_轮廓观测链和抽象特征组合规则；预期 Debug x64 构建通过。
```

待验证：

```text
已执行：python .\tools\生成函数功能说明文档.py
结果：函数条目 7256。
已完成源码复扫和 Debug x64 构建。
复扫结果：目标结构体、配套 enum 和 operator 源码命中 0。
命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
日志：日志/20260622_P10b_公共基础数据嵌套结构删除_build.log
结果：exit_code=0，未检出 error / fatal / warning。
```

当前停止点：

```text
P10b 首轮已闭合公共基础数据嵌套包装组：
    结构体_ROI
    结构体_存在观测
    点簇边界框
    点簇增强结果
    帧处理结果
    枚举_存在观测缓冲
    operator|(枚举_存在观测缓冲, 枚举_存在观测缓冲)
本次删除了 inline operator，已刷新 说明书/函数功能说明文档.md。
本切片不触碰 D455 已读写的 `结构体_帧时间戳 / 结构体_相机状态 / 结构体_IMU样本`，不触碰抽象特征组合规则。
```

下一步：

```text
返回计划索引继续后续专项：外设真实材料结构化改造 -> 函数去重专项。
```
