# 20260627 场景体素视觉先验融合 S7 Codex 断点清单

## 0. 恢复摘要

当前切片：S7 6147 跟踪丢失解释。

当前依据：

```text
计划/计划索引.md 第 1 章当前主动队列
计划/20260625_场景体素视觉先验融合实现计划_v0.1.md S7
规范/场景体素服务与视觉先验融合规范20260625.md
规范/识别本能方法唯一性收敛规范20260607.md 13.2 跟踪
docs/工程图谱/02_外设观察事实图谱.md
docs/工程图谱/05_规则原子索引.md RULE-VISION-PRIOR-001 至 RULE-VISION-PRIOR-005
```

## 1. 目标

```text
1. 6147 跟踪只读消费 SceneVoxelPrior / 观察域结果包在运行存在或输出结果场景中的结构化证据。
2. 用先验辅助目标 ROI / AABB 预计范围和丢失解释。
3. 跟踪仍只输出新空间坐标或空信息。
4. 跟踪丢失不等于存在消失，不直接写消失事实。
```

## 2. 修改边界

允许修改：

```text
自我动作实现.外设模块.ixx
说明书/函数功能说明文档.md
计划/20260625_场景体素视觉先验融合实现计划_v0.1.md
计划/计划索引.md
实施记录/20260627_场景体素视觉先验融合S7_Codex断点清单.md
```

禁止范围：

```text
不修改 D455 驱动和外设线程采集逻辑。
不修改世界树事实写入规则。
不新增伪样本、模拟入口或伪造动作动态。
不把跟踪丢失写成存在消失。
不让 6147 直接读取原始 D455 报告、场景体素快照或 SceneVoxelPrior 作为事实。
```

## 3. 预测

```text
若 S7 只读接入正确：
1. 6147 运行存在会写入跟踪先验解释、预测区间或解释计数过程证据。
2. 目标存在跟踪状态仍由实际跟踪计算结果决定。
3. 跟踪输出仍是新空间坐标或空信息，不写消失事实。
4. Debug x64 构建通过。
```

## 3.1 当前实现切片

```text
已修改：
1. 自我动作实现.外设模块.ixx

本切片新增：
1. 跟踪先验预计范围状态。
2. 跟踪丢失解释状态。
3. 6147 只读读取场景体素先验版本 / 坐标系 / 可用状态、视觉先验匹配、深度一致、自由空间冲突、信息缺口、遮挡、深度缺失和证据不足计数。
4. 新增先验证据只写运行存在、输出结果场景和目标宿主；不写目标存在本体。

当前边界：
1. 6147 不直接读取原始 D455 报告、场景体素快照或 SceneVoxelPrior 作为事实。
2. 6147 不改变跟踪成功判定和提交_指定存在跟踪事实边界。
3. 6147 只读取已落盘 `视野外证据数量`；无该结构化证据时不伪造视野外解释。
```

## 4. 已运行命令

```text
1. python .\tools\生成函数功能说明文档.py
   退出码：0
   关键输出：函数条目 7307。

2. git diff --check
   退出码：0
   关键输出：仅提示 `说明书/函数功能说明文档.md` 下次 Git 触碰时 CRLF 将替换为 LF。

3. msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
   第一次退出码：1
   关键错误：`视野外证据数量` 被误插入 6145 识别段但未声明。
   处理：删除 6145 段误插入，只保留 6147 跟踪段读取 / 写回。

4. python .\tools\生成函数功能说明文档.py
   退出码：0
   关键输出：函数条目 7307。

5. git diff --check
   退出码：0
   关键输出：仅提示 `说明书/函数功能说明文档.md` 下次 Git 触碰时 CRLF 将替换为 LF。

6. msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
   退出码：0
   关键输出：已成功生成，0 警告，0 错误。
```

## 4.1 当前结论

```text
S7 工程接入闭合。
6147 已具备只读消费观察域先验证据并写入跟踪先验预计范围状态 / 跟踪丢失解释状态的结构化过程证据。
S7 不证明真实自然跟踪业务样本已命中。
下一步按计划索引进入 S8 前置条件核查和场景体素维护线程切片。
```

## 5. 当前禁止宣称

```text
不得宣称视觉系统完成。
不得宣称扫描需求自然生长闭合。
不得宣称 D455 自然业务闭合。
不得宣称外部动作提取可启动。
不得宣称自我苏醒完成或初步成熟完成。
```
