# P10f-j 本能动作登记函数名称文本反查复核 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-j `本能动作管理类.ixx::结构体_本能动作登记信息::函数名称` 字段边界复核。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `规范/任务筹办与执行边界规范20260512.md`
- `规范/方法动作动态硬规则规范20260512.md`
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_causal_structured.tsv`

执行口径：

- 本能动作注册、查找、执行准入必须以 `枚举_本能动作ID`、方法节点、动作句柄或函数指针等内部身份结构为依据。
- `函数名称` 若只用于日志显示可保留；若用于 `查询ID`、动作入口反查或准入判断，必须删除或改成结构化入口。
- 不恢复任务筹办 / 任务执行本能方法；不新增文本 fallback。

当前候选：

- `本能动作管理类.ixx:604` `结构体_本能动作登记信息::函数名称`

当前问题记录：

- `函数名称` 当前在 `查询ID(const std::string&)` 中与登记信息和默认函数名称比较，用于从文本反查 `枚举_本能动作ID`。
- `检查是否可调用` 已优先读取 `方法类::方法动作句柄`，但仍在无句柄时通过 `方法类::方法动作名(方法)->获取主键()` 调用 `查询ID` 做文本 fallback。
- 该路径属于机器准入判断，不是纯日志显示。

下一步：

- 已删除 `结构体_本能动作登记信息::函数名称` 字段。
- 已删除 `注册(...)` 的 `std::string 函数名称` 参数，注册合法性改为 `枚举_本能动作ID` + `本能方法类::是有效本能方法能力值(...)`。
- 已删除 `查询ID(const std::string&)` 文本反查入口，以及 `检查是否可调用` 中基于 `方法动作名(方法)->获取主键()` 的文本 fallback；本能动作准入必须通过 `方法动作句柄` 的 `本能函数ID` 和 `本能ID`。
- 6150 诊断日志中的显示名改为按 `动作ID` 渲染默认动作名，仅用于日志显示，不参与机器判断。
- `说明书/函数功能说明文档.md` 已刷新；函数条目 7182。
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_causal_structured.tsv` 已刷新：剩余 632 行，其中主键类 314 行、非主键类 318 行；`结构体_本能动作登记信息` 不再命中 string 字段。
- `Debug|x64` 构建已通过，0 警告 0 错误。

闭合状态：

- P10f-j 已闭合。
- 下一候选按复扫清单前列转入 `度量模板注册表类.ixx::结构_刻度片::名称` 与 `度量模板注册表桥接.h::结构_度量刻度片_桥接::名称` 边界复核；这两个字段同属度量刻度名称链，应合并为 P10f-k 小切片。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
