# 20260624 P13b 尺寸转I64复用全局工具 Codex 断点清单

## 0. 恢复摘要

当前切片：P13b 全局共享迁移第十一小切片。

已完成：

```text
1. `特征类.cpp::私有_尺寸转I64(std::size_t)` 与已迁入全局共享的 `转换像素计数(std::size_t)` 功能等价。
2. `特征类.cpp` 增加 `import 全局共享函数类;`。
3. 删除 `特征类.cpp::私有_尺寸转I64` 本地定义。
4. 两个点链数量写入点改为调用 `转换像素计数`。
5. 已刷新 `说明书/函数功能说明文档.md`，函数条目从 7190 降为 7189。
6. 已按本次触碰范围处理 `说明书/函数功能说明文档.md` 的 LF 换行提示。
```

依据：

```text
计划/计划索引.md：P13b 从 B 类低风险候选中逐个迁移 / 复用不带项目自定义主信息 / 领域类型的通用函数。
计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md：已有同功能函数则复用，不重复新增。
```

验证：

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo
```

结果：

```text
函数说明文档生成成功：源码文件 127，函数条目 7189。
Debug x64 构建通过：0 警告，0 错误。
```

下一步：

```text
继续 P13b，复核剩余 B 类低风险候选。
当前剩余候选多为领域策略、线程 / 日志 / 控制面板协议、数据库转换、任务管理时间函数或基础数据类型已有函数；迁移前必须逐个确认不改变领域边界。
```

禁止宣称：

```text
不宣称全部函数迁移完成。
不宣称全部重复函数已清理完成。
不宣称内部循环完成 / 自我苏醒完成 / 初步成熟完成。
```
