# 20260620 特征类消费待确认结构审查 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_特征类逻辑纠偏计划_v0.1.md` 的 1.1 P2.2，审查消费待确认结构。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
规范/特征类规范20260429.md
规范/主信息字段与结构新增准入约束规范20260519.md
计划/计划索引.md
计划/20260620_特征类逻辑纠偏计划_v0.1.md
实施记录/20260620_特征类结构体必要性审查_Codex断点清单.md
实施记录/20260620_特征类文本字段纠偏预审_Codex断点清单.md
```

本切片范围：

```text
1. 审查 结构_三维体素二分层摘要。
2. 审查 结构_三维体素颜色项。
3. 审查 常用抽象特征初始化结果。
4. 只判断字段是否被公开调用方真实消费；不直接删除代码。
```

验证前预测：

```text
1. 结构_三维体素二分层摘要 可能只作为写入结果子字段存在，字段消费弱。
2. 结构_三维体素颜色项 可能只作为融合结果颜色列表元素存在，字段消费弱。
3. 常用抽象特征初始化结果 至少返回 成功 / 数量摘要，需确认调用方是否读取。
4. 若没有足够消费证据，本切片登记删除 / 收缩候选，不直接改代码。
```

## 1. 审查结果

```text
rg -n "结构_三维体素二分层摘要|摘要\.|\.摘要|三维体素.*摘要" 特征类.h 特征类.cpp 世界树类.cpp 世界树类.h 自我动作实现.外设模块.ixx 外设观察报告队列.ixx
结果：
    特征类.h:108 定义 结构_三维体素二分层摘要。
    特征类.h:149 作为 结构_三维体素链写入结果.摘要 字段。
    特征类.cpp:3382-3392、3419 写入 摘要 字段。
    未发现当前公开调用方读取 结构_三维体素链写入结果.摘要 的字段。

rg -n "结构_三维体素颜色项|体素颜色|颜色_RGBA" 特征类.h 特征类.cpp 世界树类.cpp 世界树类.h 自我动作实现.外设模块.ixx 外设观察报告队列.ixx
结果：
    特征类.h:181 定义 结构_三维体素颜色项。
    特征类.h:192 作为 结构_三维体素轮廓融合结果.体素颜色 元素。
    特征类.cpp:3806 写入 结果.体素颜色。
    特征类.cpp:3814 仅用 结果.体素颜色.size() 回写 颜色体素数量。
    未发现当前公开调用方读取 体素颜色 条目字段。

rg -n "常用抽象特征初始化结果|初始化常用抽象特征|抽象特征数|区间状态数" 特征类.h 特征类.cpp 鱼巢.cpp 自我线程模块.impl.cpp 世界树类.cpp
结果：
    特征类.h:235 定义 常用抽象特征初始化结果。
    特征类.cpp:735 返回该结构，并写入 抽象特征数 / 区间状态数 / 成功。
    世界树类.cpp:1567 调用 初始化常用抽象特征。
    世界树类.cpp:1568 只读取 抽象特征初始化.成功。
    未发现 抽象特征数 / 区间状态数 的外部消费。
```

当前判断：

```text
1. 结构_三维体素二分层摘要 当前是删除候选：现有代码只写入，不读取。
2. 结构_三维体素颜色项 / 体素颜色 当前是删除或收缩候选：现有代码只写入颜色项并取 size，未读取颜色条目字段。
3. 常用抽象特征初始化结果 当前是收缩候选：成功字段被消费，抽象特征数 / 区间状态数 未被消费；后续可评估是否改为 bool 返回，或先删除未消费计数字段。
4. 本切片不改代码，不运行构建。
5. 下一步进入 P2.3，优先选择影响最小的删除候选：结构_三维体素二分层摘要。
```
