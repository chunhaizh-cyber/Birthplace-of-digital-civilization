# 20260620 场景宿主存在读取函数 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.2 P3.2b，新增 `场景类::读取场景宿主存在` 并回迁 `任务类` 中推导任务虚拟存在承载世界的宿主存在直读。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
实施记录/20260620_场景主信息直读直写扫描_Codex断点清单.md
实施记录/20260620_场景二次特征索引快照函数_Codex断点清单.md
```

本切片范围：

```text
1. 只新增 宿主存在 读取函数。
2. 只回迁 任务类::私有_推导任务虚拟存在承载世界 中的宿主存在直读。
3. 不处理 状态索引、动态索引、世界类型 的其它直读路径。
4. 不处理显示 / 语言 / SQL 投影读取路径。
```

函数文档预查：

```text
已查 说明书/函数功能说明文档.md。
未发现 读取场景宿主存在 / 场景宿主存在 同名或同功能函数。
已有相关函数仅包括 场景类::绑定宿主。
```

验证前预测：

```text
1. 新增函数只解析 场景节点主信息类::宿主存在 引用，不创建结构。
2. 任务类改为调用 场景类 函数后，不再直接读取 场景主信息->宿主存在。
3. 函数文档生成后应新增 场景类::读取场景宿主存在 条目。
4. Debug x64 构建应通过。
```

禁止宣称：

```text
不宣称所有场景主信息直读已回迁。
不宣称场景类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 1. 实施与验证

已实施：

```text
1. 场景类.h / 场景类.cpp 新增 `读取场景宿主存在(const 场景节点类* 场景) const noexcept`。
2. 该函数优先返回宿主存在缓存指针，缺指针时按主键通过 基础信息 只读解析。
3. 任务类::私有_推导任务虚拟存在承载世界 改为调用 `世界树.场景().读取场景宿主存在(任务场景)`。
4. 任务类该路径不再直接读取 `场景主信息->宿主存在`。
```

已运行：

```powershell
python .\tools\生成函数功能说明文档.py *> 日志\20260620_场景宿主存在读取函数_函数文档.log
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\20260620_场景宿主存在读取函数_msbuild.log
```

验证结果：

```text
函数文档生成退出码：0
函数文档统计：源码文件 128；函数条目 6888
函数文档新增条目：说明书/函数功能说明文档.md 中已出现 场景类::读取场景宿主存在
Debug x64 构建退出码：0
构建关键日志：已成功生成；0 个错误
```

当前结论：

```text
P3.2b 已闭合。
下一步进入 P3.2c：新增 场景类::读取场景状态索引快照，并回迁 自我线程模块.impl.cpp 中输出结果场景状态查找的状态索引直读。
```
