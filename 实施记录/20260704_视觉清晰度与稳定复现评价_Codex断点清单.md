# 20260704 视觉清晰度与稳定复现评价 Codex断点清单

## 0. 恢复摘要

当前切片：E8/P10 `计划/20260703_视觉清晰度与稳定复现评价实现映射计划_v0.1.md` S8 反走样检查和验证。

当前判断：S0 已完成只读扫描，S1 已把可复用输入按 F / C / V_local / 稳定复现 / S / O 分组冻结，S2 已新增 F 层观察帧清晰度评分，S3 已新增 C 层观察候选清晰度评分，S4 已新增跨帧稳定复现评分，S5 已新增观察域先验一致性评分，S6 已新增局部体素融合前置状态评分，S7 已新增 D9 评价缺口标准化前置材料和 D5 值域映射，S8 已完成反走样检查、check_specs、提交差异检查和 Debug x64 构建复查。E8/P10 第一版闭合，下一步进入 E9/P9 `四方法共享底层关联判断能力实现映射计划` S0。

禁止宣称：不宣称 D9 视觉清晰度与稳定复现评价完整闭合或 D5 需求派生链已完成；不宣称真实 D455 / 自然样本已验证；不宣称局部体素可直接写稳定 E；不把先验高匹配声明为 E 当前存在，不把自由空间冲突声明为 E 消失，不把遮挡解释声明为跟踪成功。

## 1. 依据文件

- `计划/计划索引.md`：E8/P10 当前主动项。
- `计划/20260703_视觉清晰度与稳定复现评价实现映射计划_v0.1.md`：S0 要求输出字段存在 / 缺入口清单、可复用函数 / 缺函数清单、普通 bool 风险清单、可实施最小切片建议。
- `规范/详细设计/待完成/20260703_视觉清晰度与稳定复现评价详细设计_v0.1.md`：D9 评价对象、输入特征族、输出材料、判断型状态值域和反走样清单。
- `规范/1175_根规范_特征表达与判断收束规则_20260703.md`：判断型 bool / 枚举不能作为外设一阶事实，必须由复合特征、二次特征、内部治理节点或指定方法收束。
- `规范/0050_项目通用结构与禁止性规则总纲_20260701.md`：禁止用自由文本、日志、说明字段或临时结构体承载系统逻辑。

## 2. S0 字段存在清单

已存在的 F / 报告级输入：

- `外设观察报告队列.ixx` 的外设特征类型已含 `深度有效率=1011`、`空间坐标有效率=1012`、`空间连续性评分=1013`、`范围稳定性评分=1014`、`轮廓置信度=1015`、`跨帧匹配状态=1016`、`跨帧连续命中次数=1017`、`跨帧丢失次数=1018`、`距离复现离散度=3004`、`空间坐标复现离散度=3005`。
- `结构_外设观察报告项` 已有报告 / 帧 / 材料句柄、坐标系 ID、`深度有效率`、`空间坐标有效率`、`帧质量评分`、`低质量区域数量`、`未归簇区域数量`、`不可观测区域数量`、`未解释区域比例`、跨帧统计和跟踪离散度。
- `双目相机本能适配器.h` 已有融合深度、深度有效率、空间坐标有效率和轮廓置信度输入字段。

已存在的 C / 候选级输入：

- `结构_外设观察像素簇摘要` 已有跨帧簇 ID、跨帧匹配状态、连续命中、丢失、复现离散度、连续稳定命中、连续发布、轨迹状态、平滑中心 / 投影范围、像素数量、空间范围、深度有效率、空间坐标有效率、空间连续性、范围稳定性、轮廓置信度、稳定锚点和局部图 / ROI / 点集 / 像素归属验证材料句柄。
- `视觉观察融合模块.ixx` 的 `结构_视觉融合合法外设材料项` 已有来源报告 / 帧、当前中心 / 空间范围、可回查 / 可比较 / 可生成候选运行期字段、材料质量 Q10000、深度 / 空间有效率、候选像素、遮挡 / 缺深度 / 低质量计数和像素归属质量评分。
- `结构_视觉观察候选` 已有先验评分、候选像素计数、有效深度点、争议像素、遮挡边界、深度缺失、低质量像素、像素归属质量评分、候选使用场景体素版本、先验投影重叠、先验深度一致和自由空间冲突数量。

已存在的局部体素 / P12 输入：

- `结构_视觉观察候选` 已有局部体素来源任务 ID、来源观察证据 ID、来源基准体素版本、来源子候选 ID、候选版本、包围盒边长、占据数量、颜色数量、有效深度支撑数量、空间支撑数量、完整性评分 Q10000 和占据候选。
- `结构_体素子轮廓候选` 已有彩色像素数量、深度支撑像素数量、空间支撑像素数量、深度支撑率 Q10000、局部包围盒边长、局部体素占据候选和包围盒检查结果。
- `读取P12体素融合输入组只读摘要(...)` 和 `P5局部体素到稳定体素隔离闸通过(...)` 已提供存在体素融合前置读取和末端隔离闸的可复用入口。

已存在的 S / O 先验与缺口输入：

- `场景体素模块.ixx` 的 `结构_SceneVoxelPrior` 是 `结构_场景体素只读快照`，含只读状态、版本、坐标系版本、生成时间、场景引用、先验图层句柄、存在项集合、缺口集合和统计。
- `视觉观察融合模块.ixx` 的 `结构_视觉观察域结果包` 已有先验版本、先验坐标系版本、合法材料数量、候选数量、先验匹配数量、自由空间冲突候选数量、信息缺口数量、遮挡候选数量、深度缺失数量和候选 / 缺口集合。
- `结构_视觉信息缺口` 及 D5 映射函数 `写入P11结构化缺口摘要(...)` 已存在，但只覆盖既有缺口类型和 P11 阻断原因，不等于 D9 缺口类型已登记。

## 3. 缺入口清单

- 未发现统一的 `视觉清晰度评分` / `观察帧清晰度评分` / `观察候选清晰度评分` 函数。
- 未发现统一的 `稳定复现评分` 函数；现有 `外设观察存在确认方案项稳定复现状态(...)` 是 P11 确认方案项状态收束，不覆盖 D9 评分链。
- 未发现统一的 `先验一致性评分` / `先验一致性状态` 函数；现有先验匹配评分只在视觉融合结果包内作为材料评分。
- 未发现 `局部体素清晰度评分` 和 D9 `体素融合前置状态` 值域；现有 `枚举_体素加工可提交状态` 只有体素加工线程内部状态。
- 未发现 D9 专属 `清晰度缺口`、`稳定复现缺口`、`局部体素清晰度缺口`、`体素融合前置缺口`、`先验一致性缺口` 标准化缺口类型。
- 未发现可把 D9 输出写入项目结构的统一收束入口；后续不得用日志、说明文本、普通 bool 或临时结构体替代。

## 4. 可复用函数清单

- `被动识别确认方案存在清晰度阻断(...)`：可作为清晰度阻断材料参考，但返回普通 bool，不能直接入账为 D9 判断型状态。
- `被动识别确认方案存在稳定复现阻断(...)`：可作为稳定复现阻断材料参考，但不能替代稳定复现评分。
- `被动识别观察写入闸门阻断原因掩码(...)`：可复用为阻断原因集合来源之一。
- `外设观察存在确认方案项稳定复现状态(...)`：可复用跨帧命中状态收束口径，但需要 D9 评分和证据回查包装。
- `写入观察候选C收束状态(...)`：已有 C 确认状态 / 稳定复现状态写入点，后续若新增 D9 状态需区分既有状态和 D9 状态值域。
- `写入P11结构化缺口摘要(...)`：可作为 D5 缺口承接样式参考，但缺 D9 专属缺口类型。
- `读取P12体素融合输入组只读摘要(...)` 和 `P5局部体素到稳定体素隔离闸通过(...)`：可作为 S6 体素融合前置状态的读取和反走样依据。
- `挂接局部体素证据到观察候选(...)`：已把局部体素证据挂到 C 的运行期视图，可作为局部体素清晰度输入来源。

## 5. 普通 bool 风险清单

- `结构_视觉融合合法外设材料项` 的 `空间中心有效`、`空间范围有效`、`可回查`、`可比较`、`可生成候选` 是运行期辅助字段，不能直接登记为 `清晰 / 稳定 / 先验一致 / 可融合`。
- `结构_体素加工候选结果包` 的 `是否存在融合点`、`是否触碰外边界`、`是否局部包围盒压紧` 是加工结果辅助判断，不能直接作为 D9 `体素融合前置状态`。
- `结构_体素子轮廓候选` 的 `有深度空间支撑` 是辅助字段，不能直接替代局部体素清晰度评分。
- `被动识别确认方案存在清晰度阻断(...)` 和 `被动识别确认方案存在稳定复现阻断(...)` 的 bool 返回只能作为收束过程中的输入或阻断掩码来源，不能作为正式特征值写入。
- `外设观察报告队列.ixx` 中 `可提交观察区域`、`簇跨帧稳定`、`簇深度锚定`、`簇空间可用` 等 bool 只能保留为局部判断，后续要转为评分、计数、状态值域和缺口证据。

## 6. 最小可实施切片建议

1. S1 先冻结输入特征族：把已存在字段按 F / C / V_local / S / O 分组登记，把缺入口列为 `待补`，不新增长期业务结构体。
2. S2 优先实现 F 层观察帧清晰度评分，只读报告项和候选聚合材料，输出 Q10000、状态值域和阻断原因掩码。
3. S3 再实现 C 层候选清晰度评分，复用像素簇摘要、像素归属材料和局部图句柄。
4. S4 实现跨帧稳定复现评分，复用跨帧连续命中、丢失、离散度、稳定锚点和来源链。
5. S5/S6 再接先验一致性和局部体素融合前置状态；不得让先验高匹配或局部体素清晰直接写 E。
6. S7 单独接入 D5 缺口标准化，避免评分函数里直接派生需求。

## 7. 已运行命令

```powershell
rg -n "E8|P10|视觉清晰度|稳定复现|当前主动队列|推进顺序|当前无默认可执行项" "计划/计划索引.md"
rg -n "^#|^##|^###|S0|S1|S2|S3|S4|S5|允许修改|禁止|验证|完成证据|父需求|回写|硬停止" "计划/20260703_视觉清晰度与稳定复现评价实现映射计划_v0.1.md"
rg -n "判断型|bool|枚举|一阶材料|复合特征|收束|评分|Q10000|不得|禁止|结构映射|证据" "规范/1175_根规范_特征表达与判断收束规则_20260703.md"
rg -n "视觉清晰度评分|观察帧清晰度评分|观察候选清晰度评分|稳定复现评分|先验一致性评分|先验一致性状态|体素融合前置状态|局部体素清晰度评分|清晰度缺口|稳定复现缺口|体素融合前置缺口" "外设观察报告队列.ixx" "双目相机本能适配器.h" "视觉观察融合模块.ixx" "场景体素模块.ixx" "自我动作实现.外设模块.ixx" "说明书/函数功能说明文档.md"
```

最后一条命令退出码为 1，表示未命中 D9 统一评分 / 状态 / 专属缺口名称；按 S0 缺入口记录，不按错误处理。

## 8. 已运行验证

- `git diff --check -- 实施记录/20260704_视觉清晰度与稳定复现评价_Codex断点清单.md 计划/计划索引.md`：退出码 0。
- `python .\tools\check_specs.py`：hard issues 0，warnings 0。

## 9. S1 冻结输入特征族

S1 只冻结可用输入族和缺入口，不新增源码结构，不声明评分 / 状态 / 缺口已实现。

| 输入族 | 当前承载结构 | 已冻结可用输入 | 缺入口 / 待补 |
| --- | --- | --- | --- |
| F 观察帧清晰度输入 | `结构_外设观察报告项`、`双目相机本能适配器` 报告字段、外设特征族 | 报告 / 帧编号、材料句柄、坐标系 ID、深度有效率、空间坐标有效率、帧质量评分、低质量 / 未归簇 / 不可观测 / 未解释区域、跨帧统计和跟踪离散度 | 融合深度稳定性、深度空洞区域、彩色深度对齐细分状态、轮廓闭合率、轮廓断裂数量、轮廓颜色 / 空间支持率等 D9 细分项未形成统一入口 |
| C 观察候选清晰度输入 | `结构_外设观察像素簇摘要`、`结构_视觉融合合法外设材料项`、`结构_视觉观察候选` | 像素数量、投影 / 空间范围、深度有效率、空间坐标有效率、空间连续性、范围稳定性、轮廓置信度、稳定锚点、ROI / 掩码 / 点集 / 空间候选 / 像素归属材料句柄、候选像素归属质量评分 | C 层视觉清晰度评分、视觉清晰度等级状态、清晰度阻断原因集合和清晰度缺口集合未实现 |
| V_local 局部体素候选清晰度输入 | `结构_视觉观察候选` 的局部体素证据、`结构_体素子轮廓候选`、`结构_体素加工候选结果包` | 来源任务 / 观察证据 / 基准版本 / 子候选 ID、包围盒边长、占据数量、颜色数量、有效深度支撑数量、空间支撑数量、完整性评分 Q10000、局部体素占据候选、深度支撑率 Q10000 | 局部体素清晰度评分、局部体素视觉清晰度等级、体素融合前置状态、体素融合阻断原因集合未实现 |
| 跨帧稳定复现输入 | `结构_外设观察像素簇摘要`、P11 确认方案项、`外设观察存在确认方案项稳定复现状态(...)` | 跨帧簇 ID、匹配状态、连续命中、丢失、复现离散度、连续稳定命中、连续发布、轨迹状态、平滑中心 / 投影范围、距离 / 空间坐标复现离散度、连续成功 / 失败 / 重捕获计数 | D9 稳定复现评分、D9 稳定复现等级状态、稳定复现阻断原因集合和稳定复现缺口集合未实现；现有 P11 状态不是 D9 评分 |
| S SceneVoxelPrior 输入 | `场景体素模块::结构_SceneVoxelPrior`、`结构_场景体素只读快照` | 只读状态、版本、坐标系版本、生成时间、场景引用、先验图层句柄、存在项集合、缺口集合、统计 | 快照年龄、严重缺口数量、轴对齐包围盒命中数量、最近先验距离等 D9 先验一致性聚合入口未统一 |
| O 观察域结果包先验一致性输入 | `结构_视觉观察域结果包`、`结构_视觉先验匹配评分`、`结构_视觉自由空间冲突候选`、`结构_视觉信息缺口` | 先验版本、先验坐标系版本、合法材料数量、候选数量、先验匹配数量、自由空间冲突候选数量、信息缺口数量、遮挡候选数量、深度缺失数量、AABB 命中数量、空间重叠评分、深度一致评分、材料质量评分、综合评分 | 先验一致性评分、先验一致性状态、自由空间冲突复验缺口、遮挡解释复验缺口和观察域结果包清晰度摘要未实现 |

## 10. S1 反走样结论

- 冻结输入族不等于新增 D9 输出字段；后续 S2-S7 必须逐步生成评分、状态和缺口。
- 普通 bool 只保留为局部运行期辅助材料；后续不得直接写成正式特征值。
- 先验一致性和局部体素清晰度只能作为 C / O 证据链输入，不得直接确认 E 当前存在或更新稳定体素主模型。
- S2 最小入口建议：先在现有 D2 / P11 材料附近实现 F 层观察帧清晰度评分，输入限定为报告项已有 Q10000 / 计数 / 句柄材料，输出仍需保留来源报告、帧、材料页、坐标版本和缺口原因。

## 11. S2 实施记录

修改文件：

- `外设观察报告队列.ixx`
- `说明书/函数功能说明文档.md`

新增项目结构：

- `枚举_视觉清晰度等级状态`：按 D9 值域登记 `未评估 / 不可用 / 低清晰 / 可用 / 高清晰 / 局部可用 / 需复验 / 冲突`。
- `枚举_观察帧清晰度阻断原因`：用 I64 掩码表达缺报告、采集失败、材料不可回查、帧质量不足、深度不足、空间不足、低质量区域、未解释区域偏高、坐标冲突和材料需复验。
- `结构_观察帧清晰度评分`：承载来源报告 ID、来源帧 ID、评分 Q10000、状态值、阻断原因掩码、深度 / 空间 / 帧质量 / 未解释区域 / 低质量区域输入回查材料。

新增函数：

- `计算观察帧清晰度评分(const 结构_外设观察报告队列项&, const 结构_观察材料质量判定&, const 结构_外设观察等待项*)`
- `计算观察帧清晰度评分(const 结构_外设观察报告队列项&, const 结构_外设观察等待项*)`

实现边界：

- 函数只读报告项、质量判定和等待项，不写数据库、世界树事实、动作动态、需求满足或日志状态。
- 评分按已有报告材料归一为 Q10000，状态值由评分、材料质量收束状态、坐标冲突和阻断原因掩码收束。
- 阻断原因是 I64 掩码，不使用普通 bool 作为正式特征值。
- 未接入 C 层候选清晰度、稳定复现、先验一致性、局部体素清晰度、体素融合前置状态或 D5 缺口标准化。

遵守规则：

- RULE-SPEC-003：回读 `规范/1175_根规范_特征表达与判断收束规则_20260703.md`，判断型状态由评分、计数、质量状态、坐标状态和阻断原因收束。
- RULE-STRUCT-001 / RULE-STRUCT-002：输出为项目结构中的 I64、计数、评分和来源 ID，不用日志、说明文本或自由文本承载机器逻辑。
- RULE-STRUCT-005：未新增长期业务结构体承载完整评价包，仅在外设观察报告队列模块新增 F 层评分结果结构。
- RULE-VISION-PRIOR-002：本切片只形成观察域评分材料，不创建存在、不确认消失、不写一阶事实。

验证：

```powershell
python .\tools\生成函数功能说明文档.py
git diff --check -- 外设观察报告队列.ixx 说明书/函数功能说明文档.md
python .\tools\check_specs.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

结果：

- 函数说明文档已更新，新增 `计算观察帧清晰度评分` 两个条目。
- `git diff --check` 退出码 0。
- `check_specs.py` hard issues 0，warnings 0。
- Debug x64 构建通过，0 warning，0 error。

禁止宣称：

- 不宣称 D9 视觉清晰度评价完整实现；当前只完成 F 层观察帧评分入口。
- 不宣称 C 层候选清晰度、稳定复现、局部体素融合前置状态、先验一致性评分或 D9 缺口标准化已实现。
- 不宣称真实 D455 / 自然样本已验证。

## 12. S3 实施记录

修改文件：

- `外设观察报告队列.ixx`
- `说明书/函数功能说明文档.md`

新增项目结构：

- `枚举_观察候选清晰度阻断原因`：用 I64 掩码表达缺候选、缺像素、深度不足、空间不足、空间连续性不足、范围稳定性不足、轮廓置信度不足、缺稳定锚点、缺像素归属材料、缺局部图材料和候选不可提交。
- `结构_观察候选清晰度评分`：承载外设内部簇 ID、来源空间候选 ID、评分 Q10000、状态值、阻断原因掩码、像素数量、深度 / 空间 / 连续性 / 稳定性 / 轮廓 / 锚点 / 像素归属材料评分。

新增函数：

- `计算观察候选清晰度评分(const 结构_外设观察像素簇摘要&)`

实现边界：

- 函数只读像素簇摘要，不写世界树事实、动作动态、需求满足或日志状态。
- `可提交观察区域` 只作为候选不可提交阻断输入，不直接作为正式特征值。
- 像素归属材料和局部图材料以句柄存在性参与评分；未接 D5 缺口标准化。
- 未接入稳定复现评分、先验一致性评分、局部体素清晰度或体素融合前置状态。

验证：

```powershell
python .\tools\生成函数功能说明文档.py
git diff --check -- 外设观察报告队列.ixx 说明书/函数功能说明文档.md
python .\tools\check_specs.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

结果：

- 函数说明文档已更新，新增 `计算观察候选清晰度评分` 条目。
- `git diff --check` 退出码 0。
- `check_specs.py` hard issues 0，warnings 0。
- Debug x64 构建通过，0 warning，0 error。

禁止宣称：

- 不宣称 D9 视觉清晰度评价完整实现；当前只完成 F 层和 C 层评分入口。
- 不宣称稳定复现、局部体素融合前置状态、先验一致性评分或 D9 缺口标准化已实现。
- 不宣称真实 D455 / 自然样本已验证。

## 13. S4 实施记录

修改文件：

- `外设观察报告队列.ixx`
- `说明书/函数功能说明文档.md`

新增项目结构：

- `枚举_稳定复现等级状态`：按 D9 值域登记 `未评估 / 未复现 / 弱复现 / 可复现 / 稳定复现 / 丢失中 / 遮挡保留 / 冲突复现 / 需复验`。
- `枚举_稳定复现阻断原因`：用 I64 掩码表达缺候选、缺跨帧簇、未匹配、连续命中不足、丢失中、复现离散度过高、缺稳定命中、轨迹冲突和遮挡保留。
- `结构_稳定复现评分`：承载外设内部簇 ID、外设跨帧簇 ID、评分 Q10000、状态值、阻断原因掩码、匹配状态、连续命中、丢失、复现离散度、连续稳定命中和连续发布次数。

新增函数：

- `计算观察候选稳定复现评分(const 结构_外设观察像素簇摘要&)`

实现边界：

- 函数只读像素簇摘要，不写 C -> E 事实裁决、世界树事实、动作动态、需求满足或日志状态。
- 轨迹冲突、遮挡和丢失先进入稳定复现状态分流，不能被单个高分覆盖。
- 坐标版本和稳定特征样本数量在当前结构中仍缺统一入口，S4 未伪造字段。
- 未接入先验一致性、局部体素融合前置状态或 D5 缺口标准化。

验证：

```powershell
python .\tools\生成函数功能说明文档.py
git diff --check -- 外设观察报告队列.ixx 说明书/函数功能说明文档.md
python .\tools\check_specs.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

结果：

- 函数说明文档已更新，新增 `计算观察候选稳定复现评分` 条目。
- `git diff --check` 退出码 0。
- `check_specs.py` hard issues 0，warnings 0。
- Debug x64 构建通过，0 warning，0 error。

禁止宣称：

- 不宣称 D9 视觉清晰度与稳定复现评价完整实现；当前只完成 F、C 和稳定复现评分入口。
- 不宣称局部体素融合前置状态、先验一致性评分或 D9 缺口标准化已实现。
- 不宣称真实 D455 / 自然样本已验证。

## 14. S5 实施记录

修改文件：

- `视觉观察融合模块.ixx`
- `说明书/函数功能说明文档.md`

新增项目结构：

- `枚举_先验一致性状态`：按 D9 值域登记 `未评估 / 无先验 / 先验不可比较 / 低一致 / 可参考 / 高一致 / 自由空间冲突 / 遮挡解释候选 / 坐标版本冲突 / 需复验`。
- `枚举_先验一致性阻断原因`：用 I64 掩码表达缺 SceneVoxelPrior、坐标版本缺失、缺合法材料、缺先验匹配、低先验匹配、自由空间冲突、遮挡解释候选、深度缺失、信息缺口和材料质量不足。
- `结构_先验一致性评分`：承载评分 Q10000、状态值、阻断原因掩码、先验版本、坐标系版本、合法材料 / 候选 / 先验匹配计数、AABB 命中、最近距离、空间重叠、深度一致、材料质量、最佳先验综合评分、最佳先验数据项、自由空间冲突 / 遮挡 / 深度缺失 / 信息缺口计数。

新增函数：

- `视觉观察融合类::计算先验一致性评分(const 结构_视觉观察域结果包&)`

实现边界：

- 函数只读 `结构_视觉观察域结果包` 中的 SceneVoxelPrior 版本、坐标版本、AABB 命中、最近先验距离、空间重叠、深度一致、材料质量、自由空间冲突、遮挡、深度缺失和信息缺口材料。
- 评分以最佳先验综合评分为基础，按自由空间冲突、遮挡、深度缺失和信息缺口扣分，并保留阻断原因掩码。
- 状态值按坐标版本缺失、无先验、不可比较、自由空间冲突、遮挡解释、需复验、高一致、可参考、低一致分流。
- 不写世界树事实、稳定存在体素、`C -> E` 最终归属、需求满足、动作动态或日志状态；不把先验匹配高当作 E 当前存在。
- 未接入局部体素融合前置状态或 D5 缺口标准化。

遵守规则：

- RULE-SPEC-003 / `规范/1175_根规范_特征表达与判断收束规则_20260703.md`：先验一致性是判断型状态，由评分、计数、版本、来源材料和阻断原因收束，不作为外设一阶事实。
- RULE-STRUCT-001 / RULE-STRUCT-002：输出为 I64、Q10000、计数、版本和数据项 ID，不用日志、说明文本或自由文本承载机器逻辑。
- RULE-VISION-PRIOR-002：SceneVoxelPrior 只读参与观察域评分，不创建存在、不确认消失、不写事实。

验证：

```powershell
python .\tools\生成函数功能说明文档.py
git diff --check -- 视觉观察融合模块.ixx 说明书/函数功能说明文档.md
python .\tools\check_specs.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

结果：

- 函数说明文档已更新，新增 `计算先验一致性评分` 条目。
- `git diff --check` 退出码 0。
- `check_specs.py` hard issues 0，warnings 0。
- Debug x64 构建通过，0 warning，0 error。

禁止宣称：

- 不宣称 D9 视觉清晰度与稳定复现评价完整实现；当前只完成 F、C、稳定复现和先验一致性评分入口。
- 不宣称局部体素融合前置状态或 D9 缺口标准化已实现。
- 不宣称真实 D455 / 自然样本已验证。

## 15. S6 实施记录

修改文件：

- `视觉观察融合模块.ixx`
- `说明书/函数功能说明文档.md`

新增项目结构：

- `枚举_体素融合前置状态`：按 D9 值域登记 `未评估 / 不可融合 / 条件不足 / 可挂子体素证据链 / 可生成稳定体素候选 / 需识别复核 / 需观察复验 / 先验冲突阻断`。
- `枚举_体素融合前置阻断原因`：用 I64 掩码表达缺观察候选、缺局部体素候选、局部体素占据不足、深度支撑不足、空间支撑不足、局部体素清晰度不足、候选清晰度不足、稳定复现不足、来源链缺失、基准版本缺失、归属可信性不足、先验冲突、自由空间冲突和需复验。
- `结构_体素融合前置状态评分`：承载评分 Q10000、状态值、阻断原因掩码、观察候选 ID、局部体素来源链、基准版本、子候选、候选版本、包围盒、占据 / 颜色 / 深度 / 空间支撑、完整性评分、候选清晰度、稳定复现、先验一致性、归属可信评分和自由空间冲突数量。

新增函数：

- `视觉观察融合类::计算局部体素融合前置状态(const 结构_视觉观察候选&, const 结构_观察候选清晰度评分&, const 结构_稳定复现评分&, const 结构_先验一致性评分*)`

实现边界：

- 函数只读观察候选 C 上已有局部体素证据、S3 候选清晰度评分、S4 稳定复现评分和 S5 先验一致性评分。
- 状态只作为存在体素融合治理链前置材料；不写稳定体素主模型、不创建子存在、不提交 `C -> E` 最终归属。
- 局部体素清晰但归属可信性不足时分流为 `需识别复核`，不是稳定体素可更新。
- 先验冲突或自由空间冲突分流为 `先验冲突阻断`，不写 E 消失。
- 未接入 D5 缺口标准化。

遵守规则：

- RULE-SPEC-003 / `规范/1175_根规范_特征表达与判断收束规则_20260703.md`：体素融合前置状态是判断型状态，由评分、计数、版本、来源链、阻断原因和先验状态收束。
- RULE-STRUCT-001 / RULE-STRUCT-002：输出为 I64、Q10000、计数、版本和来源 ID，不用日志、说明文本或自由文本承载机器逻辑。
- RULE-VISION-PRIOR-002：局部体素和先验材料只作为观察域证据，不越权写稳定事实。

验证：

```powershell
python .\tools\生成函数功能说明文档.py
git diff --check -- 视觉观察融合模块.ixx 说明书/函数功能说明文档.md
python .\tools\check_specs.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

结果：

- 函数说明文档已更新，新增 `计算局部体素融合前置状态` 条目。
- `git diff --check` 退出码 0。
- `check_specs.py` hard issues 0，warnings 0。
- Debug x64 构建通过，0 warning，0 error。

禁止宣称：

- 不宣称 D9 视觉清晰度与稳定复现评价完整实现；当前只完成 F、C、稳定复现、先验一致性和局部体素融合前置状态入口。
- 不宣称 D9 缺口标准化已接入 D5。
- 不宣称真实 D455 / 自然样本已验证。

## 16. S7 实施记录

修改文件：

- `视觉观察融合模块.ixx`
- `说明书/函数功能说明文档.md`

新增项目结构：

- `枚举_D9评价缺口类型`：登记清晰度、稳定复现、坐标稳定、像素归属争议、局部体素清晰度、体素融合前置、先验一致性、自由空间冲突复验和遮挡解释复验缺口。
- `枚举_D9评价缺口来源层`：登记 F 观察帧、C 观察候选、局部体素候选、O 观察域结果包和体素融合前置来源层。
- `结构_D9评价结构化缺口材料`：承载缺口类型、来源层、来源 ID、报告 / 帧、评分、状态、阻断原因、先验 / 坐标版本，以及 D5 前置材料中的结构化缺口大类、阻断阶段和需求化处理建议。

新增函数：

- `D9评价缺口_结构化缺口大类(...)`
- `D9评价缺口_结构化阻断阶段(...)`
- `D9评价缺口_需求化处理建议(...)`
- `标准化D9评价缺口材料(...)`
- `收集D9评价缺口材料(...)`

实现边界：

- D9 缺口只标准化为 D5 前置材料，不创建需求节点，不调用任务管理入树，不写世界树事实、稳定体素、`C -> E` 最终归属、需求满足或动作动态。
- `收集D9评价缺口材料(...)` 只读 S2-S6 评分 / 状态 / 阻断原因材料；自由空间冲突、遮挡解释和坐标版本冲突被分流为复验或坐标缺口，不写 E 消失、跟踪成功或当前存在。
- 体素融合前置缺口只表达治理链前置阻断，不进入稳定体素主模型写入。

遵守规则：

- RULE-SPEC-003 / `规范/1175_根规范_特征表达与判断收束规则_20260703.md`：D9 缺口由评分、计数、版本、来源材料和阻断原因收束，不把单个 bool 或评分当判断结论。
- RULE-STRUCT-001 / RULE-STRUCT-002：缺口前置材料以 I64、Q10000、来源 ID、版本和状态值表达，不用日志、说明文本或自由文本承载机器逻辑。
- RULE-VISION-PRIOR-002 / RULE-VISION-PRIOR-005：视觉融合只输出观察域候选、评分、解释和结构化缺口；版本或坐标不匹配只生成缺口或复验材料。
- RULE-VOXEL-FUSION-008：局部体素和体素融合前置只输出候选 / 缺口材料，不直接写世界树事实、创建子存在或生成动作动态。

验证：

```powershell
python .\tools\生成函数功能说明文档.py
git diff --check -- 视觉观察融合模块.ixx 说明书/函数功能说明文档.md
python .\tools\check_specs.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

结果：

- 函数说明文档已更新，新增 D9 缺口映射、标准化和收集函数条目。
- `git diff --check` 退出码 0。
- `check_specs.py` hard issues 0，warnings 0。
- Debug x64 构建通过，0 warning，0 error。

禁止宣称：

- 不宣称 D9 视觉清晰度与稳定复现评价完整闭合；当前完成的是 S7 缺口标准化前置材料。
- 不宣称 D5 缺口到需求派生完整链已完成。
- 不宣称真实 D455 / 自然样本已验证。

## 17. S8 反走样检查和验证

验证预期：

- D9 输出只应包含评分、状态、计数、版本、来源 ID 和结构化缺口前置材料。
- 不应出现写稳定存在、写 E 当前存在、写 E 消失、写跟踪成功、写需求树、创建需求或生成动作动态的路径。
- 允许命中既有兼容 / 局部运行期 bool 和禁止性注释，但不得把这些 bool 当正式特征值或判断结论。

已运行命令：

```powershell
git show --check --stat --oneline HEAD -- 视觉观察融合模块.ixx 说明书/函数功能说明文档.md 实施记录/20260704_视觉清晰度与稳定复现评价_Codex断点清单.md 计划/计划索引.md
python .\tools\check_specs.py
rg -n "确认稳定存在|E 当前存在|E 消失|跟踪成功|直接派生需求|创建需求|写需求树|写世界树事实|生成动作动态|写稳定体素|普通.*bool|bool.*清晰|bool.*稳定" 视觉观察融合模块.ixx 外设观察报告队列.ixx
rg -n "D9评价缺口|收集D9评价缺口材料|标准化D9评价缺口材料|自由空间冲突复验缺口|遮挡解释复验缺口|体素融合前置缺口" 视觉观察融合模块.ixx 说明书/函数功能说明文档.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

结果：

- `git show --check --stat --oneline HEAD` 退出码 0，S7 提交差异无空白错误。
- `check_specs.py` hard issues 0，warnings 0。
- 反走样 `rg` 检查命中既有兼容 / 局部运行期 bool、禁止性注释、D9 新增前置材料和 D9 函数；未发现 S7 新增写事实、写需求树、创建需求、写稳定体素或生成动作动态路径。
- Debug x64 构建复查通过，0 warning，0 error。

完成声明反证检查：

- 已完成：E8/P10 S0-S8 计划内代码、文档说明、断点、计划索引回写和验证证据。
- 未完成：真实 D455 / 自然样本验证、D5 缺口到需求派生完整链、E9/P9 四方法共享底层关联判断能力。
- 不确定 / 待条件：真实 D455 样本、自然观察材料和后续四方法读取路径。
- 不纳入本轮范围：C -> E 最终裁决、稳定存在体素写入、场景体素刷新链、自我苏醒或初步成熟声明。

下一步：

- 计划索引转入 E9/P9 `四方法共享底层关联判断能力实现映射计划` S0 只读扫描。
