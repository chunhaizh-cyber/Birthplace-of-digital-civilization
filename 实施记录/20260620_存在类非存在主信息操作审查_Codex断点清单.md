# 20260620 存在类非存在主信息操作审查 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_存在类逻辑纠偏计划_v0.1.md` 的 1.3，审查 `存在类.h` / `存在类.cpp` 中非存在主信息直接操作。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_存在类逻辑纠偏计划_v0.1.md
存在类.h
存在类.cpp
说明书/函数功能说明文档.md
```

本切片范围：

```text
1. 扫描 存在类.h / 存在类.cpp 中的 取主信息 / dynamic_cast / ->主信息 / new 主信息 / reinterpret_cast 命中点。
2. 按职责分类：存在主信息内部职责、特征类职责、需求类职责、任务类职责、方法类职责、基础信息树遍历职责。
3. 当前只收束一个最小闭合缺口。
4. 若命中跨类缺口且需要对应类新增能力，按“缺哪个类补哪个类第一优先”转向，不在存在类里补临时绕路。
```

验证前预测：

```text
1. 存在类内部直接操作 存在节点主信息类 是本类职责，不作为缺口。
2. 特征节点主信息类 直接读取 / 创建属于特征类职责，优先判断是否已有特征类功能函数可替代。
3. 需求 / 任务 / 方法根节点入口当前由存在主信息持有，但树内容不得由存在类解释。
4. 本切片完成后需要重新运行函数说明文档生成器和 Debug x64 构建。
```

## 1. 扫描记录

```text
1. 已运行：
   rg -n "取主信息<[^>]+>|dynamic_cast<[^>]+主信息|->主信息|new .*主信息类|特征节点主信息类|状态节点主信息类|动态节点主信息类|二次特征主信息类|因果主信息类|需求节点|任务节点|方法节点|reinterpret_cast" 存在类.cpp 存在类.h
   输出写入：日志/20260620_存在类非存在主信息操作扫描.txt
   命中：21 行。
2. 已复核 取或创建子存在_按类型并按特征I64 调用：
   外部调用均为 世界树.取或创建子存在_按类型并按特征I64。
   存在类::取或创建子存在_按类型并按特征I64 只在 存在类.h 声明和 存在类.cpp 定义中出现。
```

## 2. 当前缺口

```text
第一缺口：
    存在类::取或创建子存在_按类型并按特征I64 属于重复入口。

证据：
    存在类.cpp:341-345 直接查找特征并读取 特征节点主信息类::当前值。
    世界树类.cpp:1715 已有正式同名编排入口，调用 特征类 查找和世界树读写特征。
    外部业务调用命中均为 世界树.取或创建子存在_按类型并按特征I64，不依赖 存在类 版本。

当前处理：
    删除 存在类 声明和定义，收束为 世界树类 唯一正式入口。
```

## 3. 验证结果

```text
1. 删除后扫描：
   rg 输出已写入 日志/20260620_存在类非存在主信息操作扫描_after.txt。
   命中从 21 行降为 20 行；存在类版本 取或创建子存在_按类型并按特征I64 已不再出现。

2. 删除入口复核：
   rg 输出已写入 日志/20260620_存在类非存在主信息操作_删除入口复核.txt。
   结果：存在类版本无命中；只剩 世界树类.h / 世界树类.cpp 正式入口。

3. 函数说明文档：
   python .\tools\生成函数功能说明文档.py
   退出码：0
   日志：日志/20260620_存在类非存在主信息操作_函数文档.log
   关键输出：源码文件 128；函数条目 6887。

4. 构建：
   msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
   退出码：0
   日志：日志/20260620_存在类非存在主信息操作_msbuild.log
   关键输出：已成功生成。
```

## 4. 当前判断

```text
1. 1.3 首个闭合缺口已完成：删除 存在类::取或创建子存在_按类型并按特征I64，收束到 世界树类 唯一正式入口。
2. 1.3 仍有剩余命中：存在类内 创建特征_按类型 / 获取子特征 / 查找子特征_按类型 / 根据概念扩充实例记忆 仍直接操作 特征节点主信息类。
3. 下一步继续 1.3，优先审查存在类特征 helper 是否应迁到 特征类 或删除调用。
4. 不宣称存在类逻辑纠偏完成。
```

## 5. 第二缺口：外部调用迁出存在类特征 helper

```text
问题锚点：
    外部仍通过 世界树.存在().查找子特征_按类型 / 获取子特征 读取特征。

证据：
    自我类.cpp:1055,1082
    自检线程模块.impl.cpp:780
    自我线程模块.impl.cpp:10830,11027
    语言类_存在表达.cpp:121

当前判断：
    特征类.h 已有 查找子特征_按类型 / 获取子特征。
    本缺口不需要新增函数，直接迁到 世界树.特征()。

下一步验证：
    迁移后复跑外部调用检索、函数说明文档生成器和 Debug x64 构建。
```

验证结果：

```text
1. 外部调用复核：
   rg 输出已写入 日志/20260620_存在类特征helper外部调用复核.txt。
   结果：0 行；外部 世界树.存在().查找子特征_按类型 / 获取子特征 / 创建特征_按类型 已清空。

2. 函数说明文档：
   python .\tools\生成函数功能说明文档.py
   退出码：0
   日志：日志/20260620_存在类特征helper外部调用_函数文档.log
   关键输出：源码文件 128；函数条目 6887。

3. 构建：
   msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
   退出码：0
   日志：日志/20260620_存在类特征helper外部调用_msbuild.log
   关键输出：已成功生成。

当前判断：
   1. 第二缺口已闭合：外部特征读取已迁出存在类特征 helper。
   2. 下一焦点收敛为存在类内部 根据概念扩充实例记忆 的概念特征复制路径。
```

## 6. 第三缺口：概念默认特征复制归属

```text
问题锚点：
    存在类::根据概念扩充实例记忆 仍直接读取 特征节点主信息类::类型 / 当前值，并调用存在类内部 创建特征_按类型。

证据：
    存在类.cpp:599-606 直接遍历概念特征、读取 特征节点主信息类，并复制到实例。
    特征类.h 已有 创建特征_按类型 / 获取子特征 / 查找子特征_按类型 / 读取特征值，但缺“按来源宿主复制子特征到目标宿主”的 owning class 函数。

当前判断：
    该能力归 特征类，不应继续留在 存在类 中解释 特征节点主信息类。

处理：
    按“缺哪个类补哪个类第一优先”规则，把缺口转入 计划/20260620_特征类逻辑纠偏计划_v0.1.md。
    返回点：存在类 1.3，根据概念扩充实例记忆 剩余清理。
```

闭合结果：

```text
1. 特征类 1.3 已闭合：新增 特征类::复制子特征到宿主。
2. 世界树 根据概念扩充实例记忆 已改为调用 特征类 复制概念子特征。
3. 存在类 创建特征_按类型 / 获取子特征 / 查找子特征_按类型 / 根据概念扩充实例记忆 已删除。
4. Debug x64 构建通过：日志/20260620_特征类概念默认特征复制_msbuild.log。
5. 下一步返回存在类 1.3，继续处理剩余扫描命中。
```

## 7. 第四缺口：存在类直接子节点 helper 泛型收束

```text
问题锚点：
    私有_直接基础信息子节点_按类型<T主信息> 在存在类内只剩 存在节点主信息类 用途，但名称和实现仍是泛型主信息过滤。

证据：
    日志/20260620_存在类非存在主信息操作扫描_final_candidate.txt。
    存在类.cpp:66 dynamic_cast<T主信息*>(当前->主信息)。

当前处理：
    改为 私有_直接存在子节点，只读取 存在节点主信息类。
```

闭合结果：

```text
1. 已将 私有_直接基础信息子节点_按类型<T主信息> 改为 私有_直接存在子节点。
2. 存在类.cpp / 存在类.h 已无 私有_直接基础信息子节点_按类型 和 dynamic_cast<...主信息> 命中。
3. 复扫日志：日志/20260620_存在类直接存在子节点helper_扫描.txt。
4. 函数说明文档生成退出码 0：日志/20260620_存在类直接存在子节点helper_函数文档.log。
5. Debug x64 构建退出码 0：日志/20260620_存在类直接存在子节点helper_msbuild.log。
```

## 8. 第五缺口：主信息递归释放归属

```text
问题锚点：
    存在类::删除存在 通过 私有_递归释放基础信息主信息 直接 delete 节点->主信息。

证据：
    存在类.cpp:136-137。
    双向链表模板核心.h:476 明确链表模板不拥有指针型主信息生命周期。
    基础信息类.cpp:374-382 已有基础信息类析构阶段主信息清理逻辑。

当前判断：
    释放基础信息节点主信息属于 基础信息类 生命周期职责，不属于 存在类。

处理：
    按“缺哪个类补哪个类第一优先”规则，转入 计划/20260620_基础信息类逻辑纠偏计划_v0.1.md。
    返回点：存在类 1.3，剩余扫描命中分类。
```

闭合结果：

```text
1. 基础信息类 1.1 已闭合：新增 基础信息类::释放节点主信息递归。
2. 存在类::删除存在 已改为调用 基础信息类::释放节点主信息递归。
3. 存在类内 delete 节点->主信息 / 节点->主信息 = nullptr 已清空。
4. Debug x64 构建通过：日志/20260620_基础信息类递归释放主信息_msbuild.log。
5. 下一步返回存在类 1.3，复扫并分类剩余命中。
```
