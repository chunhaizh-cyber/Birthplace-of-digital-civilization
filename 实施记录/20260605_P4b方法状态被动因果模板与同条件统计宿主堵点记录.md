# P4b 方法状态被动因果模板与同条件统计宿主堵点记录

## 检查目标

继续执行计划索引当前 P4b：

```text
方法状态被动因果模板与同条件统计宿主确认
```

## 已检查代码

```text
data/initial_causal_templates.tsv
自我线程模块.impl.cpp
自我动作实现模块.ixx
因果类.cpp
计划/20260605_学习函数量化缺口与方法状态派生纠偏计划_v0.1.md
```

## 证据

1. `data/initial_causal_templates.tsv` 当前只有 6 列：

```text
名称 / 层级 / 主果宿主 / 主果特征 / 因宿主 / 因特征集
```

2. 用户已确认 `data/initial_causal_templates.tsv` 存储的是被动因果，只需要表达：

```text
具备什么条件后必然得到什么结果。
```

因此 TSV 不需要动作语义列，也不应把本能方法写成因动作。

3. `自我线程模块.impl.cpp` 的 TSV 读取器固定 `字段数量 == 6`，并在当前安全风险树应用路径中把每个特征创建为：

```text
安全因果风险树明确状态_未明确 -> 安全因果风险树明确状态_已明确
```

这说明现有 TSV 可以表达“条件特征集合 -> 结果特征”的被动因果；但当前应用入口只服务安全风险树明确状态。

用户已进一步纠正：被动因果不是本能方法，也不是任务筹办候选；它是内部治理自动改值的逻辑依据。

4. `私有_读取或确保被动触发初始因果_线程侧` 能将模板标记为：

```text
因动作语义键 = 被动动作
因动作名称 = 被动动作
```

但动作语义不是 TSV 必要字段；该接口更适合被动触发读取和证据增强。并且该接口在自我线程私有区，`自我线程模块.impl.cpp` 当前已有未提交改动，本切片不应混入跨线程模板接口改造。

5. P4b1 执行前，`结果符合次数 / 结果不符合次数` 写入方法虚拟存在整体，不是条件节点或条件结果对宿主：

```text
条件原已存在 && 结果原已存在 -> 方法虚拟存在.结果符合次数 += 1
条件原已存在 && 条件下原有结果 -> 方法虚拟存在.结果不符合次数 += 1
```

6. P4b1 已将同条件统计宿主改为 条件节点 -> 条件场景，并新增 `新结果分支数量`。

7. 已按 `流程图/20260605_当前代码状态_` 六张流程图重新梳理 P4b2。新的主口径是：

```text
被动因果是内部治理写点的逻辑依据；
被动果由生命周期提交链、后处理链或结算链自动出现；
任务虚拟存在 / 方法虚拟存在特征改值不通过任务筹办查找本能方法实现。
```

8. 安全需求树流程图只作为消费边界，不作为方法 / 任务虚拟存在改值入口。第一次扫描只能提交发现存在或发现特征事实，有可比历史后才提交变化事实。

## 堵点

当前无法安全继续实现 P4b。

原因：

```text
1. TSV 口径已确认：只表达被动条件 -> 必然结果，不需要动作语义；
2. 当前还没有按流程图对账任务虚拟存在 / 方法虚拟存在特征自动改值写点；
3. 方法状态条件需要先有“数量底账已满足”的条件特征或等价读取逻辑；
4. 父任务重筹办、需求收束、结算、因果吸收的权威动作动态归属尚未按流程图确认；
5. 现有 自我_提交方法状态变化 路径是否误把被动自动改值做成本能方法动作，尚未复查；
6. 自我线程私有模板接口所在文件存在既有脏改，不适合在未拆小切片前混入跨模块改造。
```

## 建议确认

继续前需要用户确认：

```text
1. 内部治理自动改值写点：
   方法虚拟存在.方法状态 /
   方法虚拟存在.方法运行账完整性 /
   方法虚拟存在.方法稳定状态 /
   任务虚拟存在.任务状态 /
   父任务待重筹办 等写点分别在哪里。

2. 数量底账已满足特征生成路径：
   二次特征派生 / 方法域提交或归并时写等价布尔特征 / 方法状态派生入口内部临时计算。

3. 是否需要把现有 `自我_提交方法状态变化` 从被动自动改值路径撤出，或降级为内部治理提交函数。
```

## 当前停点

P4a 已完成并提交。

P4b1 已完成统计宿主和新结果分支口径。

P4b2 已改为流程图驱动的内部治理写点对账。

下一步：

```text
P4b2-a 流程图节点到代码写点对账。
```

输出：

```text
实施记录/20260605_P4b2流程图到内部治理写点对账记录.md
```
