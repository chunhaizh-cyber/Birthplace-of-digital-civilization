# 20260703 generation 逻辑清理 Codex 断点清单

## 0. 恢复摘要

当前切片：E0 / P13 `generation` 逻辑清理纠偏，执行 S0 后进入 S1-S4 代码清理。

权威依据：

```text
计划/计划索引.md
计划/20260703_generation逻辑清理纠偏实现映射计划_v0.1.md
规范/详细设计/待完成/20260703_外设观察材料稳定关联与入队详细设计_v0.1.md
规范/0050_项目通用结构与禁止性规则总纲_20260701.md
规范/6200_子规范_基础观察事实可用与风险判断分层_20260701.md
规范/6300_子规范_观察像素簇与存在候选分层_20260701.md
规范/6320_子规范_外设观察特征与自我场景认知分层_20260701.md
规范/6350_子规范_双目相机外设独占观察线程_20260701.md
```

S0 只读扫描结果：

```text
源码 generation 命中集中在 8 个文件：
任务模块.工作线程消息协议.ixx
任务模块.管理界面线程.impl.cpp
任务模块.管理工作线程.ixx
外设线程_D455深度相机.ixx
外设观察报告队列.ixx
自我动作实现.扫描.ixx
自我动作实现.外设模块.ixx
自我线程模块.impl.cpp

函数说明文档命中 2 个旧函数：
D455_构造Tracker轨迹状态generation
外设提交_材料组代际身份
```

已运行命令：

```powershell
rg -n "generation|材料组代际身份" --glob "*.ixx" --glob "*.cpp" --glob "*.h" --glob "说明书/函数功能说明文档.md" --glob "!x64/**" --glob "!Debug/**" --glob "!Release/**" --glob "!logs/**" --glob "!日志/**" .
```

当前判断：

```text
E0 可以按计划继续，不需要新增结构体。
清理方向是删除 generation 字符串字段、函数、枚举、判断、转交和输出逻辑，改用既有结构化字段。
若构建发现某路径只能靠 generation 成立，必须停下记录缺结构 / 缺入口。
```

下一步：

```text
1. 读取将修改代码文件的文件头规则注释模块。
2. 清理外设观察报告队列中的提交包头、目标约束、等待项、报告项和输出逻辑。
3. 清理 D455、任务管理、自我线程、扫描和外设动作实现中的转交逻辑。
4. 重新生成函数说明文档。
5. 执行 rg 清零、git diff --check、check_specs 和 Debug x64 构建。
```

禁止宣称：

```text
不得宣称 generation 逻辑已清理，直到源码和函数说明文档 rg 清零且构建通过。
不得宣称 D1 已闭合、L2/L3 来源链已补强或真实 D455 烟测已完成。
不得宣称自我苏醒完成或初步成熟完成。
```
