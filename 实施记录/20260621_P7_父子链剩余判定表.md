# 20260621 P7 父子链剩余判定表

## 1. 扫描结论

```text
外部 `世界树.基础信息().*` / `世界树->基础信息().*` 直连：0。
`基础信息_->世界根/查找主键/添加子节点/删除节点/枚举节点_按类型/获取读锁/获取写锁`：38，均在领域服务类内部。
父子链原始命中：873，不能按原始数量直接改。
```

## 2. 直接入口分类

| 类别 | 文件 / 范围 | 判定 |
| --- | --- | --- |
| `世界树类` 内部实现 | `世界树类.cpp` | 正式收口层内部实现，保留。 |
| 领域服务类内部 `基础信息_` | `二次特征类.cpp`、`动态类.cpp`、`场景类.cpp`、`存在类.cpp`、`特征类.cpp`、`状态类.cpp` | 领域服务内部实现，不作为 P7 外部直接违规点；后续若收口领域类内部，再按对应领域计划切片。 |
| 基础容器实现 | `基础信息类.cpp`、`双向链表模板核心.h` | 底层容器实现，保留。 |
| 需求 / 任务 / 方法独立树 | `需求类.cpp`、`任务类.cpp`、`任务模块.*`、`方法类.cpp`、`方法虚拟存在服务类.cpp`、`本能方法类.cpp` 等 | 非基础信息世界树操作，按各自独立树边界保留。 |
| 控制面板旧 live 显示 | `控制面板类.cpp` | P6 已将主控制面板正式读源收束为 SQL；旧 live 显示不作为主控制面板读源，后续若处理按自我场景 / live 显示单独切片。 |
| 语素树 | `语素类.cpp` | 非基础信息世界树独立树，保留。 |
| 自我线程需求链 | `自我线程模块.impl.cpp` | 需求树 / 治理链独立结构读取，非 P7 当前基础信息世界树入口。 |

## 3. 已完成 P7 代码收口

```text
`需求类.cpp`：
    1. 删除未使用的 `私有_直接基础信息子节点_按类型`。
    2. `私有_取所属场景` 改用 `世界树.获取父节点(状态节点)`。
```

## 4. 当前保留项

```text
1. `自我动作实现.外设模块.ixx` 中 `上下文.子候选` 是内部世界业务字段，不是 `->子` 链读取；当前无 P7 代码修改项。
2. `控制面板类.cpp` 中大量 `子项` 是 UI 树字段；旧 live 页面的基础信息读取不再是主控制面板正式读源。
3. 父子链原始命中中的日志字段、消息字段和普通业务字段不属于基础信息世界树结构操作。
```

## 5. P7 当前判断

```text
P7 未发现新的外部基础信息世界树直接违规点。
当前 P7 可按扫描证据闭合；若后续要继续压缩领域类内部 `基础信息_`，应转入对应领域类逻辑纠偏计划，而不是在 P7 外部调用清零中混改。
```
