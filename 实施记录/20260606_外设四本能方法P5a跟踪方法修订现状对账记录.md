# 20260606_外设四本能方法P5a跟踪方法修订现状对账记录

## 切片

```text
计划：计划/20260605_外设四本能方法需求树生成因果链修订计划_v0.1.md
切片：P5a 跟踪方法修订现状对账与最小切片拆分
目标：核对跟踪方法当前注册、规格、结果能力和运行体，拆出后续最小实现切片。
```

## 已确认代码事实

```text
方法注册：
    自我_跟踪指定存在 已在默认本能方法表中注册，ID 为 6147。
    规格确认入口为 确认自我跟踪指定存在规格。
    动作入口为 自我跟踪指定存在 / 自我_跟踪指定存在。

规格现状：
    当前形参声明包含 输入条件包、目标存在、目标宿主、当前方法运行存在。
    当前只声明 目标存在 为条件特征。
    结果表已有：
        目标存在跟踪状态
        目标观察窗口状态
        跟踪预测区间状态
        跟踪预测误差状态 / 值
        跟踪区间迁移状态 / 值
        目标存在丢失状态
        目标存在重捕获状态
        跟踪稳定 / 丢失 / 重捕获 / 预测偏差超限动态

运行体现状：
    自我_跟踪指定存在 从输入参数、输出场景或目标宿主读取 目标存在。
    计算指定存在跟踪正式结果 读取目标存在上的投影范围作为预测区间。
    若缺投影区间则写 条件不足 / 证据不足。
    若有投影区间和残差或匹配分数，则计算预测误差、区间迁移、丢失和重捕获状态。
    输出日志已明确 跟踪不替代全场扫描、缺目标不回退当前观察存在、不写安全值。
    提交_指定存在跟踪事实 已读取上游跟踪状态或观察窗口状态，并串接来源动作动态。
```

## 当前缺口

```text
1. 跟踪入口尚未强制读取 指定存在归属状态 == 已归属。
2. 跟踪入口尚未声明或读取 跟踪窗口明确状态。
3. 跟踪入口尚未声明或读取 跟踪基准特征状态。
4. 成功条件当前是 目标存在非空 + 预测区间已建立；
   尚未要求窗口明确、基准可读或误差材料可用。
5. 缺目标、缺窗口、缺基准当前主要写通用失败原因；
   还没有结构化区分缺目标 / 缺窗口 / 缺基准。
6. 跟踪区间迁移目前只有状态和值，没有单独区间迁移动作动态。
```

## 当前判断

```text
P5a 只做代码对账和切片拆分。
未修改业务代码。
未运行构建。
未真实运行鱼巢.exe。
不宣称跟踪自然业务链已通过。
```

## 下一步

```text
进入 P5b：
    跟踪前置闸门最小接入。
    自我_跟踪指定存在 应显式读取：
        外设观察存在自我归属状态 == 已归属
        目标观察窗口状态 == 已更新 或 跟踪窗口明确状态 == 明确
        跟踪基准特征状态 == 可读
    条件不足时只写结构化失败状态和缺口，不兜底成全场扫描。
```
