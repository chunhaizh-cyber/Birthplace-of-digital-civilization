# 20260605_外设四本能方法P0_Codex断点清单

## 当前切片

```text
计划：计划/20260605_外设四本能方法需求树生成因果链修订计划_v0.1.md
切片：P4d 扫描目标特征值域冲突与复验缺口现状对账
目标：对账扫描值域冲突和复验缺口现有实现，拆出超值域变化最小接入切片。
```

## 已读取依据

```text
AGENTS.md
规范/规范目录.md
计划/计划索引.md
计划/20260605_外设四本能方法需求树生成因果链修订计划_v0.1.md
规范/远程执行通道与断点续跑规范20260524.md
流程图/20260605_外设四本能方法关系与状态机_v0.1.md
规范/方法动作动态硬规则规范20260512.md
规范/动作动态即因果账本规范20260512.md
规范/本能函数实现规范20260430.md
规范/本能方法自身环境就绪规范20260510.md
规范/外设观察特征与自我场景认知本能方法分层规范20260525.md
规范/观察像素簇与存在候选分层规范20260527.md
规范/双目相机外设独占观察线程规范20260527.md
规范/双目相机外设功能能力与实现途径总结20260527.md
规范/当前场景特征值明确标准规范20260602.md
```

## 当前判断

```text
用户已要求“删掉原来的方法函数直接重构”。
本轮不再停在 P0a 入口对账记录，而是执行一个闭合代码重构切片。
保留枚举 ID、默认本能方法表和任务筹办可召回入口；
已撤出三方法执行路径中的外设提交包组织函数壳；
已将识别 / 扫描 / 跟踪三方法直接重构为正式结果生产方法；
已将四个提交入口从“外设提交包已组织”闸门改为读取上游正式结果特征。
Debug x64 构建已通过。
本轮未真实运行鱼巢.exe，不宣称真实业务链路已通过。
本轮 P0b 对账范围只写实施记录和断点清单；未把 P0b 扩成 P1 实现。
P0b 已完成，记录见 实施记录/20260605_外设四本能方法P0b结果能力与正式目标特征对账记录.md。
P0b 只做代码对账和记录更新，未修改业务代码，未构建，未真实运行鱼巢.exe。
P0c 已完成，记录见 实施记录/20260605_外设四本能方法P0c条件供料与等待项对账记录.md。
P0c 只做代码对账和记录更新，未修改业务代码，未构建，未真实运行鱼巢.exe。
P0c 结论：
    方法规格已有结构化形参、条件节点、结果表和结果能力；
    任务管理等待项在当前方法缺供料或方法失败后生成，不直接写需求树和世界树；
    观察 / 验证入口尚未接入等待项映射；
    等待项映射仍先看旧结果场景状态，实际主要靠兼容本能ID兜底；
    识别执行未显式读取 外设观察材料可回查状态；
    跟踪执行缺 目标存在 时仍会退回 当前观察存在。
P0d 已完成，记录见 实施记录/20260605_外设四本能方法P0d输出场景提交入口与因果账本对账记录.md。
P0d 只做代码对账和记录更新，未修改业务代码，未构建，未真实运行鱼巢.exe。
P0d 结论：
    四个提交入口主要消费正式状态特征 / 指针，不再以 外设提交包组织状态_已组织 为闸门；
    提交动作动态来源为当前提交入口方法首节点，未发现线程作为动作来源；
    提交入口不直接写需求满足、安全值或服务值；
    当前提交动作动态尚未显式串接上游识别 / 扫描 / 跟踪方法运行动态；
    提交_当前观察范围可观测单位存在对应事实 仍兼容读取 外设提交包候选事实数量；
    跟踪提交入口缺 目标存在 时仍会退回 当前观察存在。
P0e 已完成，记录见 实施记录/20260605_外设四本能方法P0e需求日志证据与P1入口决策记录.md。
P0e 只使用已有真实证据，未新增模拟测试、伪造样本、伪造入口或 --*-check 回归命令。
P0e 结论：
    2026-06-05 D455 真实单帧材料、逐簇识别样本卡和扫描变化样本卡可作为材料证据；
    未找到直接重构后四方法自然运行、提交入口入账和动作动态串链的完整业务日志；
    允许进入 P1 的静态 / 代码结构对账小切片；
    不宣称四方法业务链已通过。
下一步进入 P1a。
P1a 已完成，记录见 实施记录/20260606_外设四本能方法P1a正式特征清单与去容器化对账记录.md。
P1a 只做代码结构和计划断点对账，未修改业务代码，未构建，未真实运行鱼巢.exe。
P1a 结论：
    当前代码已有像素归属、候选确认、扫描入账、发现状态、跟踪状态、动作动态和范围类型等正式特征可复用；
    外设观察报告ID、外设提交包*、外设识别 / 扫描 / 跟踪结果场景状态、观察帧容器状态只能作为材料回查、日志或兼容读取；
    识别仍缺同一性状态 / 评分、自我归属状态、归属目标、归属建立 / 冲突动态的正式特征链；
    扫描和跟踪仍缺基准差异、值域冲突、预测误差、区间迁移、丢失和重捕获等正式特征链；
    下一步进入 P1b：识别同一性与归属最小特征接入。
P1b 已完成，记录见 实施记录/20260606_外设四本能方法P1b识别同一性与归属最小特征接入记录.md。
    自我动作实现.外设模块.ixx 新增外设观察存在同一性 / 自我归属状态常量、正式特征入口和聚合 helper；
    确认自我识别外设观察材料规格已接入同一性状态、同一性评分、自我归属状态、归属目标、归属建立 / 冲突动态结果表和结果能力；
    自我_识别外设观察材料 已写入聚合同一性 / 归属摘要和逐项方案节点同一性 / 归属细账；
    特征类.cpp 已初始化同一性状态、同一性评分和自我归属状态值域；
    git diff --check 退出码 0，仅 LF/CRLF 提示；
    Debug x64 msbuild 退出码 0，0 错误，1 个 LNK4075 链接警告；
    本轮未真实运行鱼巢.exe，不宣称四方法自然业务链已通过；
    下一步进入 P1c：扫描基准差异与值域冲突最小特征接入。
P1c 当前代码改动已落，待构建验证：
    自我动作实现.外设模块.ixx 新增扫描基准特征状态、已归属存在当前特征_基准特征差异状态 / 值、目标存在特征值域状态、已归属存在特征值域冲突状态 / 动态；
    观察变化上下文已从现有中心差、范围重叠、尺寸差、距离差派生基准差异和值域冲突；
    自我_扫描已识别区域 规格和执行摘要已写入上述特征；
    首次扫描仍只写发现 / 当前特征状态，不写值域冲突动态；
    特征类.cpp 已初始化 P1c 状态值域和差异值区间；
    git diff --check 退出码 0，仅 LF/CRLF 提示；
    Debug x64 msbuild 退出码 0，0 错误，1 个 LNK4075 链接警告；
    P1c 已完成，记录见 实施记录/20260606_外设四本能方法P1c扫描基准差异与值域冲突最小特征接入记录.md；
    本轮未真实运行鱼巢.exe，不宣称四方法自然业务链已通过；
    下一步进入 P1d：跟踪预测误差与丢失重捕获最小特征接入。
P1d 修改前预测：
    自我_跟踪指定存在 将撤掉缺目标时回退 当前观察存在 的旧路径；
    目标存在和预测窗口材料同时成立时才写 目标存在跟踪状态_已更新 / 目标观察窗口状态_已更新；
    目标缺失或窗口材料不足时只写证据不足 / 条件不足，不把跟踪证据当作全场扫描或安全值证据；
    预计修改 自我动作实现.外设模块.ixx、特征类.cpp、计划索引和 P1d 实施记录；
    预计运行 git diff --check 和 Debug x64 msbuild，构建通过仍不等于真实业务链通过。
P1d 已完成，记录见 实施记录/20260606_外设四本能方法P1d跟踪预测误差与丢失重捕获最小特征接入记录.md：
    自我_跟踪指定存在 已接入跟踪预测区间、预测误差、区间迁移、丢失 / 重捕获状态和值；
    缺 目标存在 时不再回退 当前观察存在；
    目标存在和预测区间材料同时成立时才写跟踪状态 / 观察窗口已更新；
    git diff --check 退出码 0，仅 LF/CRLF 提示；
    Debug x64 msbuild 退出码 0，0 错误，1 个 LNK4075 链接警告；
    本轮未真实运行鱼巢.exe，不宣称四方法自然业务链已通过；
    下一步进入 P1e：提交入口因果串接与兼容字段撤退。
P1e 修改前预测：
    提交入口仍由当前提交方法生成自己的方法动作动态；
    上游识别 / 扫描 / 跟踪方法动作动态只写入 提交动作动态.来源动作动态 和提交结果摘要，不替代当前提交动作动态；
    提交_当前观察范围可观测单位存在对应事实 将不再读取 外设提交包候选事实数量；
    提交_指定存在跟踪事实 缺 目标存在 时不再回退 当前观察存在；
    预计只修改 自我动作实现.外设模块.ixx、P1e 实施记录、本断点清单和计划索引；
    预计运行 git diff --check 和 Debug x64 msbuild，构建通过仍不等于真实业务链通过。
P1e 已完成，记录见 实施记录/20260606_外设四本能方法P1e提交入口因果串接与兼容字段撤退记录.md：
    四个提交入口规格结果表已补 来源动作动态；
    提交入口保留当前提交方法动作动态，并把上游方法动作动态写入 来源动作动态；
    提交_当前观察范围可观测单位存在对应事实 不再读取 外设提交包候选事实数量；
    提交_指定存在跟踪事实 缺 目标存在 时不再回退 当前观察存在；
    git diff --check 退出码 0，仅 LF/CRLF 提示；
    Debug x64 msbuild 退出码 0，0 错误，1 个 LNK4075 链接警告；
    本轮未真实运行鱼巢.exe，不宣称四方法自然业务链已通过；
    下一步进入 P2a：观察方法收口现状对账与切片拆分。
P2a 已完成，记录见 实施记录/20260606_外设四本能方法P2a观察方法收口现状对账记录.md：
    当前观察方法族已经形成单帧基础材料链；
    现有代码能写观察帧容器、像素覆盖、像素特征承接、完备性、质量、新鲜度和基础观察事实可用状态；
    当前代码尚未闭合外设观察存在基准状态、稳定复现状态、观察完成状态和可复验状态；
    P2b 最小代码切片确定为 观察基准状态最小特征接入；
    P2a 只做对账和切片拆分，未修改业务代码，未构建，未真实运行鱼巢.exe。
P2b 已完成，记录见 实施记录/20260606_外设四本能方法P2b观察基准状态最小特征接入记录.md：
    自我_建立观察帧存在 已接入外设观察存在基准状态、稳定复现状态、观察完成状态和可复验状态；
    单帧观察帧容器成立时只写 基准状态=已建立；
    同一摘要显式写 稳定复现状态=待复现、观察完成状态=未完成、可复验状态=待复验；
    git diff --check 退出码 0，仅 LF/CRLF 提示；
    Debug x64 msbuild 退出码 0，0 错误，1 个 LNK4075 链接警告；
    本轮未真实运行鱼巢.exe，不宣称观察自然业务链已通过；
    下一步进入 P2c：观察多帧稳定复现材料路径对账。
P2c 已完成，记录见 实施记录/20260606_外设四本能方法P2c观察多帧稳定复现材料路径对账记录.md：
    双目相机取得当前观察特征帧已有 3 帧静态融合尝试；
    建立静态多帧融合诊断 至少 2 帧可用才写融合结构和多帧稳定性；
    观察特征帧摘要已写入并可回读观察帧组、融合依据、融合有效率、低稳定像素和稳定性平均值；
    自我_执行多帧重采样并择优 仍是占位，不作为 P2d 已闭合能力；
    P2c 未修改业务代码，未构建，未真实运行鱼巢.exe；
    下一步进入 P2d：观察稳定复现状态最小派生接入。
P2d 修改前预测：
    自我_建立观察帧存在 将保持 基准状态=已建立；
    单帧或缺少融合材料时保持 稳定复现=待复现、观察完成=未完成、可复验=待复验；
    有多帧融合材料但融合有效率、稳定性或低稳定像素不达标时写 部分稳定 / 部分完成 / 证据不足；
    多帧材料、融合有效率、稳定性和低稳定像素满足门槛时才写 稳定 / 已完成 / 可复验；
    不改相机采集入口，不启用 自我_执行多帧重采样并择优，占位方法仍保持未闭合；
    预计修改 自我动作实现.外设模块.ixx、计划索引、P2d 实施记录和本断点清单；
    预计运行 git diff --check 和 Debug x64 msbuild，构建通过仍不等于观察自然业务链通过。
P2d 已完成，记录见 实施记录/20260606_外设四本能方法P2d观察稳定复现状态派生接入记录.md：
    自我_建立观察帧存在 已新增多帧状态派生 helper；
    单帧或缺融合材料仍保持 待复现 / 未完成 / 待复验；
    多帧材料成立但质量或稳定性不足时写 部分稳定 / 部分完成 / 证据不足；
    达标时才写 稳定 / 已完成 / 可复验；
    自我_执行多帧重采样并择优 仍是占位，未被宣称为闭合；
    git diff --check 退出码 0，仅 LF/CRLF 提示；
    Debug x64 msbuild 退出码 0，0 错误，1 个 LNK4075 链接警告；
    本轮未真实运行鱼巢.exe，不宣称观察自然业务链已通过；
    下一步进入 P3a：识别方法修订代码现状对账与最小切片拆分。
P3a 已完成，记录见 实施记录/20260606_外设四本能方法P3a识别方法修订现状对账记录.md：
    自我_识别外设观察材料 已写同一性状态、同一性评分、归属状态、归属目标和归属动态；
    识别日志已明确不组织外设提交包、识别不写变化动态、不结算安全值；
    提交入口已消费识别正式结果并串接来源动作动态；
    当前缺口是识别尚未读取 P2d 的稳定复现 / 观察完成 / 可复验状态作为前置闸门；
    当前规格结果能力尚未覆盖冲突 / 证据不足；
    P3a 未修改业务代码，未构建，未真实运行鱼巢.exe；
    下一步进入 P3b：识别前置观察完成闸门与结果能力补齐。
P3b 修改前预测：
    自我_识别外设观察材料 将新增 外设观察存在稳定复现状态 / 观察完成状态 / 可复验状态 作为条件特征；
    只有 稳定复现=稳定、观察完成=已完成、可复验=可复验 时才生成同一性 / 归属方案；
    观察未达标时输出当前方法条件不满足，并优先派生对应观察状态补齐需求；
    规格结果能力将补齐 同一性冲突 / 证据不足、归属冲突 / 证据不足；
    不改外设报告读取方式，不把逐簇识别报告写成正式识别结果，不写变化动态；
    预计修改 自我动作实现.外设模块.ixx、计划索引、P3b 实施记录和本断点清单；
    预计运行 git diff --check 和 Debug x64 msbuild，构建通过仍不等于识别自然业务链通过。
P3b 已完成，记录见 实施记录/20260606_外设四本能方法P3b识别前置观察完成闸门接入记录.md：
    自我_识别外设观察材料 已新增稳定复现 / 观察完成 / 可复验条件特征；
    未达到 稳定 / 已完成 / 可复验 时不再生成同一性 / 归属方案；
    观察未达标时优先派生对应观察状态补齐需求；
    识别规格结果能力已补冲突 / 证据不足；
    git diff --check 退出码 0，仅 LF/CRLF 提示；
    Debug x64 msbuild 退出码 0，0 错误，1 个 LNK4075 链接警告；
    本轮未真实运行鱼巢.exe，不宣称识别自然业务链已通过；
    下一步进入 P3c：识别多帧同一性证据累计路径对账。
P3c 已完成，记录见 实施记录/20260606_外设四本能方法P3c识别多帧同一性证据累计路径对账记录.md：
    D455 逐簇识别报告已有跨帧簇 ID、跨帧匹配状态、连续命中次数和复现离散度材料；
    当前确认方案项尚未承接这些材料为正式同一性证据；
    当前构造确认方案只用本帧投影重叠、中心距离和深度差计算匹配分数；
    自我_匹配当前观察存在与历史观察存在 不能直接替代 P3 同一性累计证据；
    P3c 未修改业务代码，未构建，未真实运行鱼巢.exe；
    下一步进入 P3d：识别多帧同一性证据最小接入。
P3d 修改前预测：
    新增 外设观察存在同一性证据帧数、外设观察存在跨帧匹配状态、外设观察存在跨帧连续命中次数、外设观察存在跨帧复现离散度 四个正式特征；
    结构_外设观察存在确认方案项 将承接上述跨帧证据字段；
    构造外设观察存在确认方案 将要求 跨帧匹配状态 > 0 且 跨帧连续命中次数 >= 2 后才进入合并 / 新建 / 冲突判断；
    跨帧证据不足时方案项写证据不足，不伪装为已归属；
    自我_识别外设观察材料 将在方案项节点写入四个证据字段；
    预计修改 自我动作实现.外设模块.ixx、特征类.cpp、计划索引、P3d 实施记录和本断点清单；
    预计运行 git diff --check 和 Debug x64 msbuild，构建通过仍不等于识别自然业务链通过。
P3d 已完成，记录见 实施记录/20260606_外设四本能方法P3d识别多帧同一性证据最小接入记录.md：
    已新增四个正式特征并在特征类初始化中登记值域；
    确认方案项已承接逐簇报告跨帧匹配状态、连续命中次数和复现离散度；
    跨帧匹配状态未成立或连续命中次数 < 2 时写证据不足并禁止提交；
    自我_识别外设观察材料 已把跨帧证据字段写入方案项节点；
    Debug x64 msbuild 退出码 0，0 错误，1 个 LNK4075 链接警告；
    未真实运行鱼巢.exe，不宣称识别自然业务链已通过。
下一步进入 P4a：扫描方法修订代码现状对账与最小切片拆分。
P4a 已完成，记录见 实施记录/20260606_外设四本能方法P4a扫描方法修订现状对账记录.md：
    扫描执行已有首次扫描发现事实路径，首次扫描不进入变化结果 / 变化事件计算；
    变化提交入口需要变化结果，首次扫描不会直接提交变化事实；
    扫描入口尚未显式消费 外设观察存在自我归属状态 == 已归属 和 扫描可启动状态 == 1；
    有历史但未变化时仍可能生成变化结果并被变化提交入口消费；
    P4a 未修改业务代码，未构建，未真实运行鱼巢.exe。
下一步进入 P4b：扫描入口已归属与扫描可启动闸门最小接入。
P4b 修改前预测：
    确认自我扫描已识别区域规格 将新增 外设观察存在自我归属状态 和 扫描可启动状态 条件特征；
    自我_扫描已识别区域 将读取当前存在 / 目标宿主 / 输出场景上的归属状态和扫描可启动状态；
    只有 当前存在有效、归属状态=已归属、扫描可启动状态=1 时，才生成首次扫描发现事实或历史变化结果；
    未达闸门时只写执行失败和条件缺口，不生成发现事实、变化结果或变化动态；
    预计修改 自我动作实现.外设模块.ixx、P4b 实施记录、计划索引和本断点清单；
    预计运行 git diff --check 和 Debug x64 msbuild。
P4b 已完成，记录见 实施记录/20260606_外设四本能方法P4b扫描入口已归属闸门接入记录.md：
    确认自我扫描已识别区域规格 已新增 外设观察存在自我归属状态 和 扫描可启动状态 条件特征；
    自我_扫描已识别区域 已读取当前存在 / 目标宿主 / 输出场景上的正式状态；
    未满足 当前存在有效、归属状态=已归属、扫描可启动状态=1 时，不生成发现事实或变化结果；
    Debug x64 msbuild 退出码 0，0 错误，1 个 LNK4075 链接警告；
    未真实运行鱼巢.exe，不宣称扫描自然业务链已通过。
下一步进入 P4c：无变化不提交变化事实最小接入。
P4c 修改前预测：
    自我_扫描已识别区域 有历史存在但变化状态=未变化时，仍可记录扫描入账和基准结果，但不写 存在特征变化动态；
    提交_观察存在特征值变化事实 将声明并读取 变化状态 条件；
    提交_观察存在特征值变化事实 只有变化状态已读且不等于 未变化 时才成功提交；
    未变化或变化状态缺失时，提交入口只写条件不足和动作动态，不写 存在特征变化动态；
    预计修改 自我动作实现.外设模块.ixx、P4c 实施记录、计划索引和本断点清单；
    预计运行 git diff --check 和 Debug x64 msbuild。
P4c 已完成，记录见 实施记录/20260606_外设四本能方法P4c无变化不提交变化事实接入记录.md：
    自我_扫描已识别区域 在 变化状态=未变化 时不写 存在特征变化动态；
    提交_观察存在特征值变化事实 已新增 变化状态 条件；
    只有变化状态已读且不等于 未变化 时才成功提交变化事实；
    Debug x64 msbuild 退出码 0，0 错误，1 个 LNK4075 链接警告；
    未真实运行鱼巢.exe，不宣称扫描自然业务链已通过。
下一步进入 P4d：扫描目标特征值域冲突与复验缺口现状对账。
```

## P0 子切片队列

```text
P0a 方法注册与规格确认入口对账：
    输出 实施记录/20260605_外设四本能方法P0a方法注册与规格确认对账记录.md。

P0b 结果能力与正式目标特征对账：
    输出 实施记录/20260605_外设四本能方法P0b结果能力与正式目标特征对账记录.md。

P0c 条件节点、供料要求与等待项对账：
    输出 实施记录/20260605_外设四本能方法P0c条件供料与等待项对账记录.md。

P0d 输出结果场景、提交入口与因果账本对账：
    输出 实施记录/20260605_外设四本能方法P0d输出场景提交入口与因果账本对账记录.md。

P0e 需求日志证据口径与 P1 入口决策：
    输出 实施记录/20260605_外设四本能方法P0e需求日志证据与P1入口决策记录.md。
```

## 已运行命令

```text
git status --short --untracked-files=no
Get-Content AGENTS.md
Get-Content 计划/计划索引.md
Get-Content 计划/20260605_外设四本能方法需求树生成因果链修订计划_v0.1.md
Get-Content 规范/远程执行通道与断点续跑规范20260524.md
Get-Content 流程图/20260605_外设四本能方法关系与状态机_v0.1.md
rg 规范关键口径
rg MEMORY.md 外设/识别/扫描/跟踪相关记忆
rg 四方法入口代码锚点
rg 旧外设提交包组织函数残留
apply_patch 自我动作实现.外设模块.ixx 删除旧组织函数并改写提交入口闸门
git diff --check
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
Get-Content AGENTS.md / 规范目录 / 计划索引 / 当前计划 / 断点清单
rg 结果能力 / 正式目标 / 容器化残留相关规范与代码
Get-Content 自我动作实现.外设模块.ixx 结果能力和执行写入关键片段
apply_patch 新增 P0b 对账记录
Get-Content 任务模块.管理界面线程.impl.cpp 等待项生成和工作结果尾部关键片段
Get-Content 任务模块.筹办.ixx 候选集填充后选择当前方法关键片段
Get-Content 自我动作实现.外设模块.ixx 四方法规格和执行条件关键片段
apply_patch 新增 P0c 对账记录并更新断点清单 / 计划索引
Get-Content 方法动作动态 / 动作动态即因果账本 / 观察像素簇规范关键片段
Get-Content 自我动作实现.外设模块.ixx 四个提交入口规格和实现关键片段
Get-Content 动态类.cpp 创建方法动作动态 / 允许生成方法动作动态关键片段
rg 提交入口、动作动态、外设提交包残留、任务筹办提交入口边界
apply_patch 新增 P0d 对账记录并更新断点清单 / 计划索引
Get-ChildItem 日志 / 运行输出 / logs 最新日志清单
rg 四方法、提交入口、等待项、候选方法、当前方法、结构化缺口相关真实日志
Get-Content P0a-P0d 实施记录和 D455 P2/P3 真实样本记录
apply_patch 新增 P0e 对账记录并更新断点清单 / 计划索引
rg P1 / 同一性 / 归属 / 去容器化相关计划和代码锚点
Get-Content 自我动作实现.外设模块.ixx 特征定义、写入摘要、规格结果能力和提交入口关键片段
Get-Content 特征类.cpp 抽象特征初始化关键片段
apply_patch 新增 P1a 对账记录并更新断点清单 / 计划索引
apply_patch P1b 识别同一性 / 归属正式特征和执行写入
git diff --check -- 自我动作实现.外设模块.ixx 特征类.cpp
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
apply_patch 新增 P1b 实施记录并更新断点清单 / 计划索引
apply_patch P1c 扫描基准差异和值域冲突正式特征接入
git diff --check -- 自我动作实现.外设模块.ixx 特征类.cpp 实施记录/20260605_外设四本能方法P0_Codex断点清单.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
apply_patch 新增 P1c 实施记录并更新断点清单 / 计划索引
Select-String / Get-Content 跟踪函数、跟踪规格、当前帧分区项和计划断点关键片段
apply_patch 追加 P1d 修改前预测
apply_patch P1d 跟踪预测误差与丢失重捕获正式特征接入
git diff --check -- 自我动作实现.外设模块.ixx 特征类.cpp 实施记录/20260605_外设四本能方法P0_Codex断点清单.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
apply_patch 新增 P1d 实施记录并更新断点清单 / 计划索引
git commit -m "接入跟踪预测误差正式特征"
Select-String / Get-Content P1e 提交入口、动态写入和提交读取 helper 关键片段
apply_patch 追加 P1e 修改前预测
apply_patch P1e 提交入口因果串接与兼容字段撤退
git diff --check -- 自我动作实现.外设模块.ixx 实施记录/20260605_外设四本能方法P0_Codex断点清单.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
apply_patch 新增 P1e 实施记录并更新断点清单 / 计划索引
git commit -m "完成提交入口因果串接"
rg / Get-Content P2a 观察方法族规格、实现、流程图和正式特征缺口
apply_patch 新增 P2a 对账记录并更新断点清单 / 计划索引
git commit -m "完成观察方法P2a对账"
apply_patch P2b 观察基准状态最小特征接入
git diff --check -- 自我动作实现.外设模块.ixx 特征类.cpp
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
apply_patch 新增 P2b 实施记录并更新断点清单 / 计划索引
git commit -m "接入观察基准状态特征"
rg / Get-Content 适配器多帧融合、观察特征帧摘要、观察报告稳定材料、D455 跨帧稳定锚点
apply_patch 新增 P2c 多帧稳定复现材料路径对账记录并更新断点清单 / 计划索引
apply_patch 追加 P2d 修改前预测
apply_patch P2d 观察稳定复现状态派生接入
git diff --check -- 自我动作实现.外设模块.ixx 实施记录/20260605_外设四本能方法P0_Codex断点清单.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
apply_patch 新增 P2d 实施记录并更新断点清单 / 计划索引
git commit -m "接入观察稳定复现状态派生"
rg / Get-Content 识别方法规格、执行、同一性 / 归属 helper、提交入口消费和任务等待项映射关键片段
apply_patch 新增 P3a 识别方法修订现状对账记录并更新断点清单 / 计划索引
git commit -m "完成识别方法P3a对账"
apply_patch 追加 P3b 修改前预测
apply_patch P3b 识别前置观察完成闸门与结果能力补齐
git diff --check -- 自我动作实现.外设模块.ixx 实施记录/20260605_外设四本能方法P0_Codex断点清单.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
apply_patch 新增 P3b 实施记录并更新断点清单 / 计划索引
git commit -m "接入识别观察完成闸门"
rg / Get-Content 外设报告跨帧材料、确认方案项、同一性匹配分数和历史观察存在匹配路径
apply_patch 新增 P3c 多帧同一性证据累计路径对账记录并更新断点清单 / 计划索引
git commit -m "完成识别方法P3c对账"
apply_patch 追加 P3d 修改前预测
apply_patch P3d 识别多帧同一性证据最小接入
git diff --check -- 自我动作实现.外设模块.ixx 特征类.cpp 实施记录/20260605_外设四本能方法P0_Codex断点清单.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
apply_patch 新增 P3d 实施记录并更新断点清单 / 计划索引
git commit -m "接入识别多帧同一性证据"
rg / Get-Content 扫描规格、扫描执行、扫描提交入口、扫描规范段和已归属 / 扫描可启动状态写点
apply_patch 新增 P4a 扫描方法修订现状对账记录并更新断点清单 / 计划索引
git commit -m "完成扫描方法P4a对账"
apply_patch 追加 P4b 修改前预测
apply_patch P4b 扫描入口已归属与扫描可启动闸门最小接入
git diff --check -- 自我动作实现.外设模块.ixx 实施记录/20260605_外设四本能方法P0_Codex断点清单.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
apply_patch 新增 P4b 实施记录并更新断点清单 / 计划索引
git commit -m "接入扫描已归属闸门"
apply_patch 追加 P4c 修改前预测
apply_patch P4c 无变化不提交变化事实最小接入
git diff --check -- 自我动作实现.外设模块.ixx 实施记录/20260605_外设四本能方法P0_Codex断点清单.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
apply_patch 新增 P4c 实施记录并更新断点清单 / 计划索引
```

## 关键退出码和日志证据

```text
git diff --check：
    退出码 0；仅 LF/CRLF 提示。

首次 msbuild：
    退出码 1；
    自我动作实现.外设模块.ixx 中 取或创建观察存在发现事实 / 读取提交入口指针 声明顺序错误。

二次 msbuild：
    退出码 0；
    0 个错误；
    1 个链接警告 LNK4075：因 /INCREMENTAL:NO 忽略 /EDITANDCONTINUE。

P1d git diff --check：
    退出码 0；
    仅 LF/CRLF 提示。

P1d msbuild：
    退出码 0；
    0 个错误；
    1 个链接警告 LNK4075：因 /INCREMENTAL:NO 忽略 /EDITANDCONTINUE。

P1e git diff --check：
    退出码 0；
    仅 LF/CRLF 提示。

P1e msbuild：
    退出码 0；
    0 个错误；
    1 个链接警告 LNK4075：因 /INCREMENTAL:NO 忽略 /EDITANDCONTINUE。

P2b git diff --check：
    退出码 0；
    仅 LF/CRLF 提示。

P2b msbuild：
    退出码 0；
    0 个错误；
    1 个链接警告 LNK4075：因 /INCREMENTAL:NO 忽略 /EDITANDCONTINUE。

P2d git diff --check：
    退出码 0；
    仅 LF/CRLF 提示。

P2d msbuild：
    退出码 0；
    0 个错误；
    1 个链接警告 LNK4075：因 /INCREMENTAL:NO 忽略 /EDITANDCONTINUE。

P3b git diff --check：
    退出码 0；
    仅 LF/CRLF 提示。

P3b msbuild：
    退出码 0；
    0 个错误；
    1 个链接警告 LNK4075：因 /INCREMENTAL:NO 忽略 /EDITANDCONTINUE。

P3d git diff --check：
    退出码 0；
    仅 LF/CRLF 提示。

P3d msbuild：
    退出码 0；
    0 个错误；
    1 个链接警告 LNK4075：因 /INCREMENTAL:NO 忽略 /EDITANDCONTINUE。

P4b git diff --check：
    退出码 0；
    仅 LF/CRLF 提示。

P4b msbuild：
    退出码 0；
    0 个错误；
    1 个链接警告 LNK4075：因 /INCREMENTAL:NO 忽略 /EDITANDCONTINUE。

P4c git diff --check：
    退出码 0；
    仅 LF/CRLF 提示。

P4c msbuild：
    退出码 0；
    0 个错误；
    1 个链接警告 LNK4075：因 /INCREMENTAL:NO 忽略 /EDITANDCONTINUE。
```

## 禁止宣称

```text
不得宣称四方法实现已完成。
不得宣称需求树自然生成四方法目标已通过。
不得宣称 D455 材料可直接满足世界真值、安全值、服务值或需求完成。
不得把任务筹办 / 任务执行恢复为本能方法。
```

## 下一步

```text
代码锚点：
    自我动作实现.外设模块.ixx
    计算观察存在变化结果 / 已归属存在特征值域冲突状态 / 已归属存在特征值域冲突动态

输出：
    实施记录/20260606_外设四本能方法P4d扫描值域冲突复验缺口对账记录.md

完成后：
    更新本断点清单；
    如修改业务代码则运行 Debug x64 构建；
    更新计划索引为下一切片或记录具体阻塞；
    自动提交本切片修改；
    真实业务验证必须另走日志、状态快照、动作动态和错误日志。
```
