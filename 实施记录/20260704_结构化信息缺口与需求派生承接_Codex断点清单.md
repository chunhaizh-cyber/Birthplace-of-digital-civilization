# 20260704 结构化信息缺口与需求派生承接 Codex 断点清单

## 0. 恢复摘要

当前切片：

```text
E4 / P6：结构化信息缺口与需求派生承接实现映射计划
S0：代码事实扫描
```

依据文件：

```text
计划/计划索引.md
计划/20260703_结构化信息缺口与需求派生承接实现映射计划_v0.1.md
规范/详细设计/待完成/20260703_结构化信息缺口与需求派生承接详细设计_v0.1.md
规范/0050_项目通用结构与禁止性规则总纲_20260701.md
规范/1175_根规范_特征表达与判断收束规则_20260703.md
规范/5150_子规范_需求入树拒绝回执与开发诊断闭环_20260701.md
```

当前判断：

```text
S0 已完成只读扫描。
未命中 S0 停止条件。
父需求主键、目标特征类型、当前 / 目标状态和满足关系已有结构化槽位。
稳定键由派生需求类型、因果路径生成类型、阻塞级别、父需求、来源因果、目标主体、目标特征、满足关系、当前状态和目标状态组成，不依赖自由文本语义解析。
缺失点是统一缺口大类、阻断阶段、需求化处理建议和来源层级到派生消息的标准化映射。
```

禁止宣称：

```text
不宣称结构化缺口已实现。
不宣称外设 / 视觉缺口已标准化。
不宣称缺口到需求派生已代码接入。
不宣称父子角色建议已接入。
不宣称入树拒绝闭环已通过完整回归。
```

下一步：

```text
进入 S3：外设缺口标准化适配。
把外设提交缺口码标准化为结构化缺口候选。
重点验证缺像素归属账本不直接需求化。
```

## 1. S0 只读扫描证据

### 1.1 派生需求消息字段清单

锚点：

```text
任务模块.工作线程消息协议.ixx:187
任务模块.管理工作线程.ixx:808
任务模块.管理工作线程.ixx:903
任务模块.筹办.ixx:1361
```

字段材料：

```text
消息头：发出者主键、任务管理任务主键、任务主键、父任务主键、步骤主键、关联场景主键、产生时间、回复目标消息主键、执行尝试号、执行尝试主键、路径签名、幂等键、允许正式资产提交。
来源材料：当前方法主键、方法虚拟存在指针 / 主键、来源因果指针 / 主键、来源动态指针 / 主键、来源方法指针 / 主键。
需求承接材料：父需求主键、派生需求类型、因果路径生成类型、阻塞级别、需求特征类型指针 / 主键、需求目标主体指针 / 主键、需求当前状态指针 / 主键、需求目标状态指针 / 主键、满足关系。
治理摘要材料：是否本地不可消化、是否阻塞当前步骤、是否影响父任务推进、是否需要外部支撑、是否可能影响服务、是否可能影响安全、是否建议线程重判、是否疑似需要新任务入口、派生需求类型值、方法需求位值、本能能力缺口类型值、阻塞级别值、贡献层级、层级权重、稳定度摘要。
```

字段完整性检查要求：

```text
父需求主键非空；
派生需求类型、因果路径生成类型、阻塞级别均非未定义；
目标特征类型、目标主体、当前状态、目标状态均必须同时具备指针和主键；
满足关系不能为 0。
```

### 1.2 入树拒绝回执字段清单

锚点：

```text
任务模块.管理线程协议.ixx:85
任务模块.管理线程协议.ixx:94
任务模块.管理线程协议.ixx:103
任务模块.管理线程协议.ixx:135
任务模块.管理线程协议.ixx:146
任务模块.管理线程协议.ixx:167
任务模块.管理线程协议.ixx:241
任务模块.管理线程协议.ixx:520
自我线程模块.impl.cpp:9957
自我线程模块.impl.cpp:10542
```

值域和结构：

```text
派生需求入树处理结果：已新增、已更新、已合并、已拒绝、已延后。
拒绝阶段：目标归一阶段、消息规范化阶段、自我入树闸门阶段、需求更新指令阶段、去重合并阶段。
拒绝原因：字段缺失、父需求缺失、父需求引用损坏、父域可重判、父为结构根可映射、父为结构根且无合法映射、目标主体不可判定、目标特征缺失、当前状态缺失、目标状态缺失、来源因果不可解析、来源动态不可解析、来源动态本应存在却缺失、贡献层级越界、外设内部材料字段、参数诊断不需求化、父子目标自复制、同批重复、近期重复、节点类型不符合预期、指针为空但逻辑不允许、枚举值非法、需求树结构损坏、任务状态机非法跳转、代码缺陷型报告、其他入树证据不合法、规范缺口。
拒绝闭环类型：内部治理闭环、来源任务内部闭环、外设材料闭环、学习闭环、观察验证闭环、开发诊断闭环、合并冷却闭环。
后续处理类型：自动补槽后重提、退回来源模块补槽、挂起来源任务等待补齐、合并到既有需求或任务、绑定到既有任务或缺口、转观察验证缺口、转学习缺口、转方法内部处理、转外设材料处理、转代码诊断、禁用或降级来源路径、冷却后关闭、请求人类或开发者处理、来源任务重筹办、参数绑定重试、方法候选降权、升级规范缺口。
拒绝回执处理状态：未处理、处理中、已补槽重提、已合并关闭、已转来源任务内部处理、已转观察验证、已转学习、已转外设材料闭环、已进入开发诊断项、已隔离路径、已冷却关闭、已请求外部处理、已升级规范缺口、处理失败。
派生需求入树回执字段：任务信息节点、父需求、派生需求、处理结果、是否阻塞父任务推进、是否携带拒绝回执、拒绝回执、当前时间、原派生消息幂等键。
```

### 1.3 外设缺口码清单

锚点：

```text
外设观察报告队列.ixx:191
外设观察报告队列.ixx:343
外设观察报告队列.ixx:752
外设观察报告队列.ixx:2495
```

值域：

```text
无、缺报告、缺原始观察帧、缺深度帧、缺彩色帧、缺对齐帧、缺空间候选、缺像素归属账本、缺材料句柄、材料过期、低质量、深度无效、空间坐标无效、缺可比历史、缺已确认存在ID、缺目标种子、外设采集失败、报告类型不匹配。
```

来源材料槽：

```text
提交包已有缺失材料、质量问题、不可比较原因、证据不足原因、原因、缺口原因等集合。
当前已有质量缺口收集函数可从报告项、质量判定和材料页解析结果收集外设提交缺口码。
```

### 1.4 视觉信息缺口清单

锚点：

```text
视觉观察融合模块.ixx:43
视觉观察融合模块.ixx:104
视觉观察融合模块.ixx:338
```

值域：

```text
无、缺SceneVoxelPrior、缺合法外设材料、材料空间范围无效、材料不可比较、深度缺失、先验坐标系版本缺失、先验结构缺口、证据不足。
```

字段：

```text
类型、数据项ID、来源报告ID、来源帧ID、先验版本、证据值、相关存在。
```

结论：

```text
视觉信息缺口已经携带来源报告 / 帧 / 数据项 / 先验版本等回查材料。
```

### 1.5 已有任务状态 / 等待缺口唤醒入口

锚点：

```text
任务类.h:205
任务类.h:238
任务类.cpp:2269
任务类.cpp:2450
任务模块.治理协议.ixx:29
任务模块.治理协议.ixx:174
任务模块.治理协议.ixx:214
任务模块.治理协议.ixx:221
任务模块.筹办.ixx:122
```

入口：

```text
任务类::读取任务等待缺口唤醒 / 写入任务等待缺口唤醒。
任务类::读取任务转入缺口需求次数 / 写入任务转入缺口需求次数。
任务治理协议已有任务管理缺口类型、本能能力缺口类型、缺口承接类型、缺口反馈消费策略、触发事件类型中的缺口回流 / 缺口反馈。
任务筹办结果已有待上报派生需求消息集、缺口目标主体、缺口特征类型和父任务需要重筹办等运行期回执槽。
```

注意：

```text
等待缺口唤醒等 bool 只能作为既有任务协议兼容位或生命周期收束结果使用，不得扩张为新增一阶事实。
```

### 1.6 稳定键材料组成

锚点：

```text
任务模块.管理工作线程.ixx:903
任务模块.管理工作线程.ixx:957
任务模块.管理工作线程.ixx:1014
```

组成：

```text
派生需求类型；
因果路径生成类型；
阻塞级别；
父需求主键；
来源因果主键；
需求目标主体主键；
需求特征类型主键；
满足关系；
非学习缺口追加当前状态主键和目标状态主键；
学习缺口使用固定学习信息补齐状态标记；
末尾追加满足关系 I64 值。
```

判断：

```text
稳定键虽然使用字符串拼接实现，但材料来自结构化枚举、主键和值域槽，不依赖日志文本或自由语义解析。
```

### 1.7 缺失承载点

缺失项：

```text
统一缺口大类值域尚未登记。
缺口来源层级、来源模块、阻断阶段、需求化处理建议尚未形成统一映射。
外设缺口码、视觉信息缺口、任务筹办缺口、入树拒绝原因之间没有统一标准化入口。
外设 / 视觉 / 方法缺口到结构_任务管理派生需求消息的映射函数尚未形成。
父子角色建议尚未从缺口链进入父需求角色视图，只能依赖 P1 已有只读角色视图作最终裁决。
外设内部材料字段、参数诊断、代码缺陷型报告已有拒绝原因，但 D5 尚未把它们统一纳入结构化缺口分流表。
```

## 2. 已运行命令

```powershell
Get-Content -LiteralPath '计划\计划索引.md' -TotalCount 220
Get-Content -LiteralPath '计划\20260703_结构化信息缺口与需求派生承接实现映射计划_v0.1.md' -TotalCount 260
Get-Content -LiteralPath '规范\详细设计\待完成\20260703_结构化信息缺口与需求派生承接详细设计_v0.1.md' -TotalCount 260
rg -n "结构_任务管理派生需求消息|派生需求消息|稳定键|幂等键|父需求|目标特征类型|满足关系" -- "任务模块.工作线程消息协议.ixx" "任务模块.管理工作线程.ixx" "说明书/函数功能说明文档.md"
rg -n "派生需求入树|拒绝回执|拒绝原因|拒绝阶段|闭环|后续处理|处理结果|结构_派生需求入树回执" -- "任务模块.管理线程协议.ixx" "任务模块.管理工作线程.ixx" "需求类.h" "需求类.cpp" "说明书/函数功能说明文档.md"
rg -n "缺报告|缺原始观察帧|缺深度帧|缺彩色帧|缺对齐帧|缺空间候选|缺像素归属账本|缺材料句柄|材料过期|低质量|深度无效|空间坐标无效|缺口码|提交缺口" -- "外设观察报告队列.ixx"
rg -n "结构_视觉信息缺口|视觉信息缺口|缺SceneVoxelPrior|缺合法外设材料|材料空间范围无效|材料不可比较|深度缺失|先验坐标系版本缺失|先验结构缺口|证据不足" -- "视觉观察融合模块.ixx"
rg -n "唤醒|等待缺口|等待|重筹办|缺口|派生需求|筹办缺口" -- "任务模块.筹办.ixx" "任务模块.治理协议.ixx" "任务类.h" "任务类.cpp" "需求类.h" "需求类.cpp"
```

验证性质：

```text
S0 是只读扫描，不运行 C++ 构建，不声明运行验证。
```

## 3. S1 缺口大类和值域登记

本切片改动：

```text
规范/详细设计/待完成/20260703_结构化信息缺口与需求派生承接详细设计_v0.1.md
计划/20260703_结构化信息缺口与需求派生承接实现映射计划_v0.1.md
计划/计划索引.md
实施记录/20260704_结构化信息缺口与需求派生承接_Codex断点清单.md
```

登记内容：

```text
1. 外设缺口码 -> 缺口大类 / 阻断阶段 / 默认处理建议。
2. 视觉信息缺口 -> 缺口大类 / 阻断阶段 / 默认处理建议。
3. 方法、体素和入树拒绝来源 -> 缺口大类 / 阻断阶段 / 默认处理建议。
```

S1 结论：

```text
已覆盖外设、视觉、方法、体素和入树拒绝五类来源。
处理建议只是治理建议，不是父子角色最终事实。
必须 / 可选路径 / 非必须仍由父需求视角角色视图裁决。
未新增代码枚举、普通布尔值或自由结构体。
```

验证：

```text
待运行 git diff --check。
待运行 python .\tools\check_specs.py。
S1 未修改 C++ 代码，不运行 Debug x64 构建。
```

## 4. S2 缺口到派生需求消息映射

本切片改动：

```text
任务模块.工作线程消息协议.ixx
任务模块.管理工作线程.ixx
任务模块.筹办.ixx
说明书/函数功能说明文档.md
计划/20260703_结构化信息缺口与需求派生承接实现映射计划_v0.1.md
计划/计划索引.md
实施记录/20260704_结构化信息缺口与需求派生承接_Codex断点清单.md
```

代码结果：

```text
新增 任务管理派生需求消息必备槽完整：集中检查父需求、派生需求类型、因果路径生成类型、阻塞级别、目标特征类型、目标主体、当前状态、目标状态和满足关系。
新增 填充任务管理派生需求消息结构化缺口槽：把结构化缺口必备槽填入现有 结构_任务管理派生需求消息，并同步语义槽。
任务模块.筹办.ixx 的 构造正式派生需求消息草案_筹办查询 已复用该填充入口。
任务模块.筹办.ixx 和 任务模块.管理工作线程.ixx 的字段完整性检查已复用同一必备槽完整性入口。
```

边界：

```text
没有新增业务结构体。
没有新增代码枚举。
没有新增普通布尔值。
没有改需求树写入入口。
没有绕过字段完整性检查、稳定键和工作线程去重。
```

验证：

```text
python .\tools\生成函数功能说明文档.py：成功，函数条目 7670。
git diff --check：通过，仅提示 说明书/函数功能说明文档.md 行尾转换警告。
python .\tools\check_specs.py：hard issues = 0，warnings = 0。
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m：0 警告，0 错误。
```
