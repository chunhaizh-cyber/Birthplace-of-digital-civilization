# P10f-al 相机调用结果消息字段边界复核 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-al `双目相机本能适配器.h::调用结果::消息` 与派生 `外设线程_D455深度相机.ixx::结构_D455深度相机观察样本::消息` 文本字段边界复核。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_ado_query_result.tsv`

复核字段：

- `双目相机本能适配器.h::调用结果::消息`
- `外设线程_D455深度相机.ixx::结构_D455深度相机观察样本::消息`

复核证据：

- `双目相机本能适配器.cpp::打开 / 释放 / 检查 / 采集一帧 / 从帧生成结果` 写入 `结果.消息`，同时用 `调用结果::成功 / 相机已打开 / 失败原因 原因 / 帧尺寸 / 数量 / 质量评分` 承载机器状态。
- `外设线程_D455深度相机.ixx::D455_从调用结果生成样本(...)` 把 `来源.消息` 复制到 `样本.消息`，同时把 `来源.原因` 写为 `样本.失败原因` 数值，把 `来源.成功` 写为 `样本.采集成功`。
- `外设线程_D455深度相机.ixx::D455_从调用结果生成逐簇识别报告(...)` 只在失败时把 `来源.消息` 复制到 `报告.失败原因` 展示文本；报告机器判断仍读取 `采集成功`、观察运行模式、报告类型、尺寸、帧号、材料句柄、候选数量和质量数值。
- `鱼巢.cpp` 命令行相机命令输出摘要时打印 `结果.消息`，退出码读取 `结果.成功` 或 `结果.原因`，不读取消息文本。
- 定向检索 `if/while/switch/==` 未发现 `消息` 文本参与运行模式、结果选择、任务推进、等待状态、调度、结算或因果匹配；命中为空判断只用于日志摘要是否追加字段。

处理结论：

- 本切片不删除 `消息` 字段。
- `调用结果::消息` 和 `结构_D455深度相机观察样本::消息` 登记为诊断 / 日志 / 控制面板可读文本边界。
- 本切片无代码改动，未重新构建。

下一步：

- 返回 P10f 最新复扫清单；失败原因字段已按 P10f-v 登记为显示 / 日志说明的部分不重复打开，下一候选转向 `需求类.cpp::结构_需求树SQL行` 文本字段边界复核。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
