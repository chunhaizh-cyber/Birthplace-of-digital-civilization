# 20260629 日志输出预处理分级控制 Codex断点清单

## 0. 恢复摘要

```text
当前切片：日志、运行日志、预测日志、逻辑错误排查日志等输出面统一纳入预处理总开关和细分开关。
用户口径：日志不能进入正式机器逻辑，不能承载业务状态；关闭日志后用日志文件快照验证不新增、不增长。
禁止宣称：不宣称业务链通过；不宣称逻辑错误不存在；不宣称自我苏醒完成或内部循环全部完成。
```

## 1. 依据

```text
AGENTS.md
docs/00_文档总索引.md
规范/禁止性规范20260611.md
规范/规范目录.md
docs/工程图谱/00_图谱总索引.md
docs/工程图谱/05_规则原子索引.md
计划/完成项/20260612_日志输出预编译控制实时计划_v0.1.md
实施记录/20260612_日志输出预编译控制_Codex断点清单.md
实施记录/20260613_日志输出预编译控制P4P5_Codex断点清单.md
```

## 2. 初始证据

```text
当前已有非本切片脏改：
    数据库ADO模块.cpp
    计划/计划索引.md
    说明书/函数功能说明文档.md

已确认直接输出风险点：
    日志模块.ixx：低级错误兜底路径仍直接 OutputDebugStringW、写 鱼巢_popup_fatal.log、MessageBoxW。
    控制面板WebView2.cpp：初始化失败直接 MessageBoxW 只受 UI 直接提示输出控制。
    鱼巢.cpp：裸 std::cout 命中点已在 鱼巢_开关_启用控制台输出 块内。
    D455相机类.cpp：std::cerr 命中点已在 鱼巢_D455控制台输出 宏内。
```

## 3. 本轮修改范围

```text
允许修改：
    AGENTS.md
    规范/禁止性规范20260611.md
    docs/工程图谱/05_规则原子索引.md
    预处理开关变量.h
    日志接入.h
    日志模块.ixx
    控制面板WebView2.cpp
    计划/20260629_日志输出预处理分级控制计划_v0.1.md
    本断点清单

禁止修改：
    任务状态、需求满足、方法选择、动作动态、世界树、因果账本、价值结算。
    接手前已有未提交改动。
```

## 4. 验证方法

```text
静态扫描：
    rg 直接输出点，确认日志文件、MessageBoxW、OutputDebugStringW、std::cout、std::cerr 均有开关边界。

构建：
    默认 Debug x64 构建。
    鱼巢_开关_启用所有输出=0 的 Debug x64 Rebuild。

运行：
    全关构建产物运行短入口。
    运行前后记录 ./日志 文件名、大小、LastWriteTimeUtc。
    验收口径：无新增日志文件；已有日志文件大小不增长。
```

## 5. 已完成修改

```text
规则层：
    AGENTS.md 新增当前有效硬规则第 13 条。
    规范/禁止性规范20260611.md 第 2 章新增日志输出面硬规则。
    docs/工程图谱/05_规则原子索引.md 新增 RULE-LOG-001。

代码层：
    预处理开关变量.h 新增运行日志、预测日志、逻辑错误排查日志细分开关及取值校验。
    日志接入.h 新增关闭分类时的表达式安全 no-op 宏。
    日志模块.ixx 将运行日志、调试日志、逻辑错误日志和低级错误兜底输出纳入开关。
    控制面板WebView2.cpp 将初始化失败诊断日志构造和直接弹窗纳入开关。
```

## 6. 已运行验证

```powershell
git diff --check -- AGENTS.md 规范/禁止性规范20260611.md docs/工程图谱/05_规则原子索引.md 预处理开关变量.h 日志接入.h 日志模块.ixx 控制面板WebView2.cpp 计划/20260629_日志输出预处理分级控制计划_v0.1.md 实施记录/20260629_日志输出预处理分级控制_Codex断点清单.md

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> '日志\20260629_log_switch_default_build.log'

$env:CL='/D鱼巢_开关_启用所有输出=0'; msbuild .\鱼巢.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> '日志\20260629_log_switch_all_off_rebuild.log'

.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=800

msbuild .\鱼巢.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> '日志\20260629_log_switch_default_rebuild_restore.log'

python .\tools\check_specs.py *> '日志\20260629_log_switch_check_specs.log'
```

## 7. 关键结果

```text
默认构建：
    exit_code=0
    日志/20260629_log_switch_default_build.log
    0 个错误

全关 Rebuild：
    第一次失败：日志接入.h 的 no-op 宏使用 do/while，不能替代 鱼巢_不允许空指 内部逗号表达式。
    修正：no-op 宏统一改为表达式安全的 ((void)0)。
    第二次 exit_code=0
    日志/20260629_log_switch_all_off_rebuild.log
    0 个错误

全关运行：
    命令：x64/Debug/鱼巢.exe --self-check-log --self-check-log-wait-ms=800
    run_exit_code=0
    before_file_count=1137
    after_file_count=1137
    added_count=0
    grown_count=0
    touched_count=0
    removed_count=0
    stdout_bytes=0
    stderr_bytes=0

默认 Rebuild 恢复：
    exit_code=0
    日志/20260629_log_switch_default_rebuild_restore.log
    0 个错误

规范治理检查：
    exit_code=0
    日志/20260629_log_switch_check_specs.log
    warnings=72
    非 strict 模式未阻断；本轮不把既有治理 warning 当作代码构建失败。
```

## 8. 当前判断

```text
本轮规则已落入正式禁止性规范和工程图谱规则原子。
全关构建和全关运行已证明：本次短入口运行不会新增日志文件，也不会增长或触碰既有日志文件。
低级错误保护路径不再在全关构建下写 鱼巢_popup_fatal.log、不发 OutputDebugStringW、不弹 MessageBoxW，但仍保留 terminate 语义。
仍不把“关闭输出后没有日志”宣称为“没有逻辑错误”；逻辑错误排查必须使用对应日志开关打开的构建。
```
