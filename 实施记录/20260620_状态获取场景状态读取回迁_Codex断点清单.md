# 20260620 状态获取场景状态读取回迁 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.2 P3.3e，回迁 `状态类::获取场景状态` 中的场景状态索引读取路径。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
实施记录/20260620_场景主信息直读直写复扫_Codex断点清单.md
```

本切片范围：

```text
1. 只修改 状态类::获取场景状态。
2. 将该函数的 场景主信息->状态索引 直接读取改为 场景类::读取场景状态索引快照。
3. 不新增、删除或改名函数。
4. 不处理 是否当前活动状态、可从活动状态链清理 和状态清理函数，留给后续小切片。
```

验证前预测：

```text
1. 状态类.cpp 中 获取场景状态 函数不再直接读取 场景节点主信息类 或 场景主信息->状态索引。
2. Debug x64 构建应通过。
3. 函数文档无需重生成，因为本切片未新增、删除或改名函数。
```

禁止宣称：

```text
不宣称状态类场景状态索引直读全部回迁。
不宣称所有场景主信息直读已回迁。
不宣称场景类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 1. 修改结果

```text
已修改：
    状态类.cpp

修改内容：
    状态类::获取场景状态 不再通过 基础信息_->取主信息<场景节点主信息类> 读取 场景主信息->状态索引。
    该函数改为构造 场景类 场景服务，并调用 场景类::读取场景状态索引快照。
```

## 2. 验证结果

直读检查：

```powershell
rg -n "取主信息<场景节点主信息类>|场景主信息->状态索引|读取场景状态索引快照" 状态类.cpp
```

关键结果：

```text
状态类::获取场景状态 命中：读取场景状态索引快照。
其它 场景主信息->状态索引 命中仍属于后续 是否当前活动状态、可从活动状态链清理 和清理函数切片。
```

构建验证：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

证据：

```text
日志/20260620_状态获取场景状态读取回迁_msbuild.log
退出码：0
已成功生成。
0 个错误
```

## 3. 下一步

```text
P3.3f 回迁 状态类::是否当前活动状态 和 状态类::可从活动状态链清理 的状态索引读取路径，改为调用 场景类::读取场景状态索引快照。
```
