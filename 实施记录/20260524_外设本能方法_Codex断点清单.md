# 外设本能方法 Codex 断点清单

## 当前切片

等待真实 D455 样本验证

## 远程执行状态

```text
中断于远程压缩
```

该状态只表示远程执行通道中断，不改变项目实现状态、验收状态或业务结论。

## 项目最近已知状态

```text
P3/P5 可闭合子集已构建通过。
S1 已核对：5 个双目相机外设本能方法存在于枚举、默认本能方法种子、运行时动作注册和专属规格分派路径。
按用户要求先执行 S4：双目相机外设动作已从 `自我动作实现模块.ixx` 抽出到 `自我动作实现.外设模块.ixx`，原模块保留注册入口和薄委托。
S2 已核对：5 个方法的结果能力声明符合控制 / 检查 / 使用三分类边界。
S3 已补齐并核对：动作动态显式落账目标外设、目标宿主、目标特征类型、输入参数场景、输出结果场景和当前方法运行存在；使用类结果优先落到输入目标宿主。
S5 已核对：任务筹办仍先按结果能力完整填充候选集，再从候选集中选择当前方法；未发现双目相机或三分类硬编码分派。
S6 已补齐并核对：两个使用类方法将双目相机外设可用性、运行状态声明为必需形参和必需条件；缺失时走任务筹办既有“必需实参未绑定 -> 参数不可配 -> 精确缺口特征派生”路径，不由使用类动作绕过筹办直接调用控制 / 检查方法。
S8 已准备真实 D455 验证观察点：五个双目相机本能动作统一输出“双目相机本能方法结果”日志，带方法、成功状态、相机状态、失败原因、输入 / 输出场景、目标外设、目标宿主、动作动态、输出运行、帧宽高、深度 / 彩色帧号和轮廓数量。
真实 D455 需求链路仍等待真实硬件与真实目标样本验证。
```

## 已完成

- [x] 将计划的规范依据补入 `070 远程执行通道与断点续跑规范20260524`
- [x] 将第一轮闭合目标收束为 5 个已注册方法
- [x] 将暂缓项从当前默认可执行方法和普通候选中排除
- [x] 将 remote compact / stream disconnected 类问题归类为远程执行通道中断
- [x] 写入可恢复切片 S1-S8
- [x] S1 核对 5 个已注册外设方法的登记路径
- [x] S4 新增 `自我动作实现.外设模块` C++20 模块
- [x] S4 将双目相机外设规格确认和动作执行入口移动到外设模块
- [x] S4 原 `自我动作实现模块` 保留本能动作注册入口，并通过 `外设模块::` 委托
- [x] S7 对本次 S4 代码拆分执行 Debug x64 构建
- [x] S2 核对控制类方法结果能力只声明外设可控特征
- [x] S2 核对检查类方法结果能力只声明外设状态特征
- [x] S2 核对使用类方法结果能力声明目标宿主与使用类目标特征
- [x] S3 核对并补齐动作动态显式落账字段
- [x] S3 控制 / 检查类目标宿主保持为双目相机外设
- [x] S3 使用类从输入参数场景解析目标宿主，缺失时退回输出结果场景
- [x] S3 使用类结果写入目标宿主、输出结果场景和方法运行存在
- [x] S7 对本次 S3 代码变化执行 Debug x64 构建
- [x] S5 核对任务筹办候选集先填充后选择
- [x] S5 核对候选填充来源仍为方法结果能力命中目标特征
- [x] S5 确认 `任务模块.筹办.ixx` 未出现双目相机 / 外设三分类硬编码分派
- [x] S6 将 `双目相机_刷新当前观察场景` 的外设可用性 / 运行状态声明为必需前置输入
- [x] S6 将 `双目相机_发现候选存在` 的外设可用性 / 运行状态声明为必需前置输入
- [x] S6 核对必需实参缺失会进入 `参数不可配` 并携带精确缺口特征类型
- [x] S6 核对使用类动作未直接调用控制 / 检查方法
- [x] S7 对本次 S6 代码变化执行 Debug x64 构建
- [x] S8 核对 `双目相机本能适配器` 返回真实 D455 调用结果和失败原因
- [x] S8 补齐五个双目相机本能动作的统一结果日志
- [x] S8 日志记录方法名、成功状态、相机状态、失败原因、输入 / 输出场景、目标外设、目标宿主、动作动态、输出运行和采集摘要
- [x] S8 记录真实 D455 链路仍等待真实硬件与真实目标样本验证
- [x] S7 对本次 S8 代码变化执行 Debug x64 构建

## 修改文件

- `计划/20260524_外设本能方法三分类与双目相机方法登记实施计划_v0.1.md`
- `实施记录/20260524_外设本能方法_Codex断点清单.md`
- `自我动作实现.外设模块.ixx`
- `自我动作实现模块.ixx`
- `鱼巢.vcxproj`
- `鱼巢.vcxproj.filters`

S2 未修改业务代码，只更新本断点清单。
S3 修改 `自我动作实现.外设模块.ixx`，补齐动作动态和使用类目标宿主落账。
S5 未修改业务代码，只更新本断点清单。
S6 修改 `自我动作实现.外设模块.ixx`，补齐使用类方法的外设可用性 / 运行状态前置输入声明。
S8 修改 `自我动作实现.外设模块.ixx`，补齐真实 D455 验证所需的统一结果日志。

## 验收命令

```powershell
rg -n "双目相机_(使达到可用状态|释放|检查运行状态|刷新当前观察场景|发现候选存在)|获取特征值|判断目标可见性|在场状态" -g '!x64/**' -g '!Debug/**' -g '!logs/**' -g '!日志/**' -g '!计划/**' -g '!详细设计/**' -g '!规范/**' -g '!实施记录/**'

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m

# S2 静态核对：按函数体截取后确认控制 / 检查 / 使用类结果能力声明边界
PowerShell 读取 `自我动作实现.外设模块.ixx`，分别截取：
    确认双目相机控制规格
    确认双目相机检查运行状态规格
    确认双目相机刷新当前观察场景规格
    确认双目相机发现候选存在规格

# S3 静态核对：按函数体截取后确认动作动态落账字段
PowerShell 读取 `自我动作实现.外设模块.ixx`，分别截取：
    写入双目相机I64事实与动态
    用双目相机刷新当前观察场景
    用双目相机发现候选存在

# S5 静态核对：按函数体截取后确认筹办候选填充路径
PowerShell 读取 `任务模块.筹办.ixx`，分别截取：
    解析或创建任务目标方法
    填充任务方法候选集
    填充候选集_按结果能力
    方法结果能力命中目标特征
    从筹办候选集中选择当前方法

# S6 静态核对：按函数体截取后确认使用类前置输入和通用缺口派生路径
PowerShell 读取 `自我动作实现.外设模块.ixx`，分别截取：
    确认双目相机刷新当前观察场景规格
    确认双目相机发现候选存在规格
    用双目相机刷新当前观察场景
    用双目相机发现候选存在

PowerShell 读取 `任务模块.筹办.ixx`，分别截取：
    检查候选方法必需实参
    尝试构造候选方法输入参数场景
    返回方法结构缺口 中 `参数不可配` 分支

rg -n "双目相机|外设三分类|控制类|检查类|使用类|D455|RealSense|方法分类|三分类" 任务模块.筹办.ixx

# S8 静态核对：确认真实 D455 验证观察点
PowerShell 读取 `双目相机本能适配器.h/.cpp` 和 `自我动作实现.外设模块.ixx`，确认：
    适配器返回成功状态、相机打开状态、失败原因、消息、帧宽高、深度 / 彩色帧号、轮廓数量
    五个双目相机本能动作均调用 `记录双目相机本能动作结果`
    统一日志包含方法、成功状态、相机状态、失败原因、输入 / 输出场景、目标外设、目标宿主、动作动态、输出运行和采集摘要

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

S1 只核对登记状态。S4 按用户要求拆分新外设模块，不扩大双目相机方法登记范围。

## 验收结果

```text
通过。
5 个方法均在以下路径出现：
    本能方法类.h：枚举_本能方法ID
    本能方法类.cpp：g_默认自我本能方法表
    自我动作实现模块.ixx：取本能动作集().注册
    自我动作实现模块.ixx：确认默认本能方法专属规格分派
    自我动作实现模块.ixx：对应动作函数入口

未发现 `获取特征值`、`判断目标可见性`、`在场状态` 在业务代码中被登记为默认可执行本能方法。

S4 通过。
构建命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
构建结果：通过，0 个错误，1 个 LNK4075 警告（/INCREMENTAL:NO 导致忽略 /EDITANDCONTINUE）。

S2 通过。
静态核对结果：
    控制规格声明外设可控特征：PASS
    检查规格声明外设状态特征：PASS
    刷新观察场景声明目标宿主与使用类结果：PASS
    发现候选存在声明目标宿主与使用类结果：PASS
    控制规格未声明使用类结果：PASS
    检查规格未声明使用类结果：PASS

S2 未重新执行构建；本切片未修改业务代码。

S3 通过。
静态核对结果：
    动作动态写入目标外设：PASS
    动作动态写入目标宿主：PASS
    动作动态写入目标特征类型：PASS
    动作动态写入方法运行存在：PASS
    动作动态写入输入参数场景：PASS
    动作动态写入输出结果场景：PASS
    刷新观察场景解析目标宿主：PASS
    发现候选存在解析目标宿主：PASS
    刷新观察场景写入目标宿主结果：PASS
    发现候选存在写入目标宿主结果：PASS

S3 构建命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
S3 构建结果：通过，0 个错误，1 个 LNK4075 警告（/INCREMENTAL:NO 导致忽略 /EDITANDCONTINUE）。

S5 通过。
静态核对结果：
    筹办先填充候选集：PASS
    筹办从候选集中选择当前方法：PASS
    候选填充调用结果能力路径：PASS
    结果能力填充遍历方法树：PASS
    结果能力填充按目标特征命中：PASS
    结果能力命中读取能力开关：PASS
    结果能力命中读取结果包：PASS
    选择函数遍历候选集：PASS
    任务筹办无双目相机或三分类硬编码：PASS

S5 未重新执行构建；本切片未修改业务代码。

S6 通过。
静态核对结果：
    刷新观察场景声明 `外设可用性` 为必需形参和必需条件：PASS
    刷新观察场景声明 `运行状态` 为必需形参和必需条件：PASS
    发现候选存在声明 `外设可用性` 为必需形参和必需条件：PASS
    发现候选存在声明 `运行状态` 为必需形参和必需条件：PASS
    必需实参缺失返回 `缺必需实参` 与首个缺失参数特征：PASS
    缺必需实参在筹办末端转为 `参数不可配` 并传递覆盖缺口特征类型：PASS
    `参数不可配` 分支调用精确主体特征需求状态填充：PASS
    任务筹办无双目相机或三分类硬编码：PASS
    使用类动作未直接调用控制 / 检查方法：PASS

S6 构建命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
S6 构建结果：通过，0 个错误，1 个 LNK4075 警告（/INCREMENTAL:NO 导致忽略 /EDITANDCONTINUE）。

S8 通过。
静态核对结果：
    适配器结果含成功状态：PASS
    适配器结果含相机打开状态：PASS
    适配器结果含失败原因和消息：PASS
    适配器结果含帧宽高、深度 / 彩色帧号、轮廓数量：PASS
    五个双目相机本能动作均调用统一结果日志：PASS
    统一结果日志含输入 / 输出场景：PASS
    统一结果日志含目标外设 / 目标宿主：PASS
    统一结果日志含动作动态 / 输出运行：PASS
    统一结果日志含采集摘要：PASS

S8 构建命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
S8 构建结果：通过，0 个错误，1 个 LNK4075 警告（/INCREMENTAL:NO 导致忽略 /EDITANDCONTINUE）。

真实 D455 验证：未执行。
原因：本切片只准备日志和观察点；缺真实 D455 硬件接入、真实目标样本和真实需求链路运行日志时，只记录等待验证，不宣称业务链路通过。
```

## 未完成

- 真实 D455 需求链路验证：待真实 D455 硬件、真实目标样本和真实运行日志

## 下一切片

等待真实 D455 样本验证

目标：

```text
在具备真实 D455 硬件、真实目标样本和真实需求链路运行条件后触发验证。
用真实日志核对：
    使用需求先命中使用类方法；
    缺外设可用性 / 运行状态时派生控制 / 检查子需求；
    控制 / 检查 / 使用动作动态落账来源清楚；
    `双目相机本能方法结果` 日志含帧号、轮廓数量和失败原因。
```

## 禁止宣称

- 不宣称真实 D455 链路通过
- 不宣称 `获取特征值` 已可执行
- 不宣称目标可见性 / 在场状态已闭合
- 不宣称内部循环全部完成
- 不宣称自我苏醒完成或初步成熟完成
