# 20260626 P13b 指针句柄显示文本工具迁移 Codex断点清单

## 0. 恢复摘要

当前切片进行中。目标是将 `语言类_基础信息命名.cpp::私有_指针句柄文本(std::uintptr_t)` 收束到 `全局共享函数类.ixx`，删除语言类本地重复显示工具函数，并保持原有“空指针 / 指针#...”显示语义。

依据：

```text
计划/计划索引.md：当前主动队列为 P13b，从剩余 B 类低风险候选中选择小函数族。
计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md：B 类函数迁移前必须查全局共享函数类和函数说明文档，已有同功能则复用 / 删除重复定义，没有才迁移。
实施记录/20260624_P13_函数自定义主信息分类候选表.tsv：`私有_指针句柄文本` 为 B 类低风险待复核候选。
全局共享函数类.ixx 文件头规则：无领域自定义主信息 / 领域类型参与的通用函数可进入本模块。
```

当前判断：

```text
`私有_指针句柄文本` 只依赖 `std::uintptr_t` 和字符串格式化，不读取 / 写入项目结构。
全局已有 `指针日志文本(const void*)`，但只输出地址数字；它不等价于 `空指针 / 指针#...` 显示语义，不能直接复用替代。
可新增全局 `指针句柄显示文本(std::uintptr_t)`，语言类改用全局函数。
`语言类_基础信息命名.cpp` 当前缺少文件头部规则注释模块，本切片需先补最小模块。
```

预期验证：

```text
1. `全局共享函数类.ixx` 新增 `指针句柄显示文本(std::uintptr_t)`。
2. `语言类_基础信息命名.cpp` import 全局共享函数类。
3. 删除本地 `私有_指针句柄文本`。
4. 原调用点改为 `指针句柄显示文本`。
5. 运行 `python .\tools\生成函数功能说明文档.py`。
6. Debug x64 构建通过。
7. 复扫确认本地私有函数清零，新增全局函数进入源码和函数说明文档。
```

禁止宣称：不宣称 P13 完成，不宣称函数去重全部完成，不宣称自我苏醒完成或初步成熟完成。

## 1. 构建前状态

已执行：

```text
1. `全局共享函数类.ixx` 新增 `指针句柄显示文本(std::uintptr_t)`。
2. `语言类_基础信息命名.cpp` 补最小文件头部规则注释模块。
3. `语言类_基础信息命名.cpp` import 全局共享函数类。
4. 删除本地 `私有_指针句柄文本`。
5. `私有_特征值文本` 中指针句柄显示改为调用 `指针句柄显示文本`。
6. 已运行 `python .\tools\生成函数功能说明文档.py`，源码文件 127 个，函数条目 7213。
```

复扫证据：

```text
`语言类_基础信息命名.cpp` 中 `私有_指针句柄文本` 源码定义已清除。
`指针句柄显示文本` 已进入 `全局共享函数类.ixx`，并被 `语言类_基础信息命名.cpp` 调用。
```

构建预测：

```text
Debug x64 构建应通过；若失败，优先检查 `语言类_基础信息命名.cpp` 的 module import 可见性。
```
