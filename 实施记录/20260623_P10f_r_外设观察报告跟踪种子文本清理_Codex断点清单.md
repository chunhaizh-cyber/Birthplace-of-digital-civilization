# P10f-r 外设观察报告跟踪种子文本清理 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-r `外设观察报告队列.ixx::结构_外设观察报告队列项::跟踪种子` 边界复核。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_observation_report_device_id.tsv`

当前问题：

- `结构_外设观察报告队列项::跟踪种子` 是非主键类 `std::string` 字段。
- 报告项中已有 `跟踪ID / 目标观察约束ID / 目标存在ID` 等结构化依据；该文本字段只由 D455 从等待项 `目标区域或目标簇` 复制，并在提交包生成中作为“是否缺目标种子”的辅助判断。
- D455 稳定缓存和等待项 `目标区域或目标簇` 仍有更大的文本解析问题，本切片先删除报告队列项的文本承载，不扩大到等待项协议。

构建前预测：

- 删除报告项 `跟踪种子` 字段后，提交包缺目标种子判断改用 `跟踪ID / 目标观察约束ID / 目标存在ID`。
- 删除 D455 写入 `报告.跟踪种子` 和报告摘要中的 `跟踪种子` 显示。
- 预期 Debug x64 构建通过，0 警告，0 错误。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。

## 1. 完成证据

- 已删除 `结构_外设观察报告队列项::跟踪种子`。
- 已删除 D455 跟踪报告生成时对 `报告.跟踪种子` 的文本复制。
- `构造外设跟踪提交包_由观察报告` 的缺目标种子判断改为读取 `跟踪ID / 目标观察约束ID / 匹配等待项ID`，不再读取报告文本字段。
- 已删除报告摘要中的 `跟踪种子` 文本输出。
- 已生成 `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_tracking_seed.tsv`，剩余记录 626 行，其中主键 / ID 类 316 行、非主键类 310 行。
- 首次构建暴露 `结构_外设观察报告队列项` 无 `目标存在ID`，已改为报告内实际存在的 `匹配等待项ID`。
- `rg` 复核 `报告.跟踪种子 / 报告项.跟踪种子` 无残留。
- `git diff --check` 通过。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 第二次通过，0 警告，0 错误。

## 2. 下一候选

- `外设观察报告队列.ixx::结构_外设观察等待项::目标区域或目标簇` 仍是非主键类 `std::string` 字段，承担目标存在 ID、目标簇文本和临时扫描说明三类语义。
- `外设线程_D455深度相机.ixx::结构_D455跟踪稳定缓存项::跟踪种子` 仍复制该字段，并用于缓存项匹配。
- `D455_尝试解析跟踪种子簇ID(...)` 仍从 `目标区域或目标簇` 文本解析簇 ID；下一切片应拆分为明确的目标存在 ID / 目标簇 ID / 目标说明显示文本。
