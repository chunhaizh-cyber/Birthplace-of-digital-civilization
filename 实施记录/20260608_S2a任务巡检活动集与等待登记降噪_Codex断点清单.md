# 20260608 S2a 任务巡检活动集与等待登记降噪 Codex 断点清单

## 当前切片

```text
切片：S2a 任务巡检活动集与等待登记降噪。
执行状态：计划已建立，尚未实施代码。
方案文件：计划/20260608_S2a任务巡检活动集与等待登记降噪方案_v0.1.md
索引状态：计划/计划索引.md 已将当前主动切片切到 S2a。
```

## 依据文件

```text
规范/规范目录.md
规范/任务筹办与执行边界规范20260512.md
规范/方法动作动态硬规则规范20260512.md
计划/计划索引.md
计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
日志/鱼巢_run_20260608.低值g
```

## 当前真实日志基线

```text
日志窗口：2026-06-08 14:20:26 程序启动后窗口。
需求树：需求数=49，已满足=1，未满足=48。
任务状态动作动态：276 条。
负向锚点：
    任务状态被动提交/逻辑错误=0
    任务状态动作动态逻辑错误=0
    准备本能调用=0
    执行本能方法=0
    任务域本能输入包=0
错误级别：
    ERR=1，FTL=0。
    唯一 ERR 为 D455 观察材料条件不足，不是任务状态提交逻辑错误。
```

性能基线：

```text
任务状态巡检/阶段耗时：
    扫描任务=49 样本 14 条；
    平均约 7.27s；
    最大 17.905s。

等待工作项登记/阶段耗时：
    5353 条；
    平均约 62.8ms；
    最大 4.802s；
    主慢阶段为 主锁等待。

任务状态提交/阶段耗时：
    301 条；
    平均约 143.7ms；
    最大约 670.9ms。

任务状态被动提交/内部阶段耗时：
    32 条；
    平均约 192.8ms；
    最大约 503.4ms。
```

## 允许修改范围

```text
任务模块.管理界面线程.impl.cpp
计划/计划索引.md
计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
计划/20260608_S2a任务巡检活动集与等待登记降噪方案_v0.1.md
实施记录/20260608_S2a任务巡检活动集与等待登记降噪_Codex断点清单.md
```

如必须修改其他任务管理文件，先补充 S2a 方案并说明原因。

## 禁止宣称

```text
不得宣称自我苏醒完成或初步成熟完成。
不得宣称 D455 观察路径已闭合。
不得把 D455 条件不足写成需求满足。
不得把等待 / 未满足任务写成完成。
不得跳过任务状态动作动态。
不得恢复任务筹办 / 任务执行本能方法。
```

## 下一步

```text
1. 写实施前预测。
2. 从 S2a-1 重复等待 / 重复重排幂等裁决开始。
3. 优先降低重复登记和主锁等待，不先做全量事件驱动。
4. 构建 Debug x64。
5. 真实运行 self-check-log。
6. 用日志核对任务状态动作动态、负向锚点、任务巡检、等待登记、主锁等待和快照耗时。
```

## 实施前预测 2026-06-08 S2a-1

```text
第一刀：
    1. 在 任务模块.管理界面线程.impl.cpp 中为等待工作项登记增加锁内等价等待项合并。
    2. 等价条件限定为同一任务、同一工作类型、兼容状态版本且现有等待项仍有效。
    3. 合并时只更新现有等待项的最早派发时间 / 入队原因 / 快照摘要，不新增队列项。
    4. 同任务在途重排如果读不到在途租约截止，用默认工作项租约形成退避，避免 50ms 反复出队。

预测：
    任务状态动作动态仍输出。
    旧完整本能调用锚点仍为 0。
    任务状态逻辑错误锚点仍为 0。
    等待工作项登记条数、重排条数或主锁等待最大值至少有一项下降。
    D455 条件不足 ERR 可以继续存在，不作为本切片失败。
```

## 实施后验证 2026-06-08 S2a-1/S2a-2

```text
代码改动：
    1. 任务模块.管理界面线程.impl.cpp 新增等待工作项锁内等价合并。
    2. 合并条件限定为同任务、同工作类型、兼容状态版本且现有等待项租约仍有效。
    3. 合并成功时更新现有等待项最早派发时间 / 入队原因 / 快照摘要，不再 push 新队列项。
    4. 同任务在途重排缺少租约截止时补默认租约，避免 50ms 反复重排。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0。

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    退出码=0。

最新日志窗口：
    日志=日志/鱼巢_run_20260608.低值g
    程序启动锚点行=18122
    ERR=1，WRN=13，FTL=0
    任务状态动作动态=125
    任务状态被动提交/逻辑错误=0
    任务状态动作动态逻辑错误=0
    准备本能调用=0
    执行本能方法=0
    任务域本能输入包=0

性能结果：
    任务状态巡检/阶段耗时：198 条，平均约 33499.93us，最大 102166us。
    等待工作项登记/阶段耗时：0 条。
    同任务已有在途工作项，延后重排：0 条。
    同任务已有执行前许可占用，延后重排：0 条。
    任务状态提交/阶段耗时：123 条，平均约 90048.64us，最大 239823us。
    任务状态被动提交/内部阶段耗时：8 条，平均约 139472.38us，最大 298736us，最慢阶段=动作动态创建。

结论：
    S2a 重复等待 / 重复重排 / 等待登记降噪已通过真实日志验收。
    当前第一慢点已转回任务状态提交内部动作动态创建。
    D455 识别条件不足 ERR 继续存在，按计划记录为业务条件不足，不作为本性能切片失败。
```
