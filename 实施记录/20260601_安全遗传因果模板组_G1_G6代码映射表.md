# 20260601 安全遗传因果模板组 G1-G6 代码映射表

## 依据

执行 `计划索引.md` 当前主动队列 S2 / P0：

- `计划/20260601_安全遗传因果模板组纠偏计划_v0.1.md`
- `规范/自我存在服务与生存基本因果闭环规范20260601.md`
- `规范/基础观察事实可用与基础风险判断分层规范20260526.md`
- `规范/自我根特征抽象定义与自检缺口规范20260523.md`

本记录只建立映射表，不新增“安全因素定义表”，不直接生成需求，不创建任务，不写安全值，不结算价值。

## 已有代码锚点

1. 遗传因果模板入口：
   - `自我线程模块.impl.cpp:1385` 的 `私有_查找遗传因果模板_线程侧` 目前按主果比较、单个因比较、单个因方法查重。
   - `自我线程模块.impl.cpp:1414` 的 `私有_确保遗传因果模板_线程侧` 复用现有 `因果主信息类`，写 `来源类型=遗传`、`主果比较模板`、`因比较模板`、`因方法模板`。
   - 该入口日志明确 `直接生成需求=0 / 直接创建任务=0 / 直接结算价值=0`。

2. 当前已落的安全风险树锚点：
   - `自我线程模块.impl.cpp:4699` 的 `私有_确保安全因果风险树遗传来源_线程侧` 确保 `遗传因果.安全风险树.自我安全值依赖风险安全层明确`。
   - 这只是 G1/G2 之间的核心锚点，不等于 G1-G6 模板组完整入账。

3. 风险安全层 v1 因素代码材料：
   - `自我线程模块.impl.cpp:99` 记录 `安全因果风险树同层因素集合版本_风险安全_线程侧 = 1`。
   - `自我线程模块.impl.cpp:101` 到 `自我线程模块.impl.cpp:105` 定义场景、运行、资源、权限、未知五个因素。
   - `自我线程模块.impl.cpp:2710` 附近的风险安全层聚合候选会读取五因素、计算缺失数量、候选值和投影候选，并写 `不写风险安全层入账值=1`。

4. 场景影响提交边界：
   - `自我线程模块.impl.cpp:3199` 的场景影响提交路径会读取候选、可结算状态、不可结算原因、已入账状态和变化方向。
   - 日志明确不写风险安全层总值。

5. 当前场景特征值缺口：
   - `自我线程模块.impl.cpp:5104` 读取 `当前场景特征值明确状态`。
   - `自我线程模块.impl.cpp:5126` 生成 `当前场景特征值明确状态 等于 已明确` 的缺口，并携带 OR 路径草案。

6. D455 材料链路：
   - `外设观察报告队列.ixx:34` 和 `外设观察报告队列.ixx:76` 保存簇级、帧级观察材料。
   - `外设观察报告队列.ixx:595` 提交 D455 短期材料页。
   - `外设线程_D455深度相机.ixx:523` 生成逐簇识别报告。
   - `外设线程_D455深度相机.ixx:1184` 在真实外设线程路径提交短期材料页。

## G1-G6 映射表

| 模板 | 模板名称 | 主果比较 | 因侧比较或因方法 | 适用层级 | 来源类型 | 后天验证入口 | 禁止直接写入项 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| G1 | 遗传因果.安全根.多层安全因果链影响自我安全值 | 自我安全值达到目标维护状态 / 自我安全值下降候选 | 物理安全、风险安全、第三层安全因果链、第四层安全因果链等层级输入 | 1 | 遗传 | 任务完成后的安全值结算反馈链；世界树因果证据；副作用审查 | 不直接写自我安全值；不关闭安全根需求；不把风险安全层明确当自我安全值已满足 |
| G2 | 遗传因果.风险安全层.v1五因素聚合 | 风险安全层明确 / 风险安全层候选可聚合 | 场景影响、运行影响、资源影响、权限影响、未知影响五因素 | 2 | 遗传 | 风险安全层聚合候选日志；五因素搜索状态；入账状态；缺失因素数量 | 不自动放大已知因素权重；未知不默认安全；不写风险安全层总值 |
| G3 | 遗传因果.风险安全.场景影响部分来自当前场景风险安全值 | 风险安全_场景影响部分候选或可结算状态 | 当前场景风险安全值候选、场景影响因素节点、提交入口状态 | 2/3 | 遗传 | `安全_评估当前场景安全性` 输出；`提交_风险安全场景影响部分状态变化` 入账结果 | 不越级写风险安全层；不写自我风险安全值；不写自我安全值 |
| G4 | 遗传因果.当前场景风险安全值来自已确认存在集合 | 当前场景风险安全值候选 / 当前场景各存在安全评估明确 | 当前场景已确认存在集合、各存在风险安全值候选、当前场景特征值明确状态 | 3 | 遗传 | 提交确认观察存在事实；当前场景存在集合明确状态；场景特征值材料门 | 外设报告、像素簇、验证通过候选不得直接当已确认存在集合；不直接写风险明确 |
| G5 | 遗传因果.存在风险安全值来自当前特征值和历史因果证据 | 存在风险安全值候选 / 存在风险评估明确 | 存在当前特征值、自我关键特征变化观察、历史动态、归因证据、无负向证据搜索结果 | 4 | 遗传 | 存在风险评估日志；当前特征值材料门；历史动态 / 因果归因读取；无负向证据搜索结果 | 特征值未知不能落成零风险且明确；存在名不是风险依据；不写常驻安全画像 |
| G6 | 遗传因果.当前场景观察材料链取得场景特征值 | 当前场景特征值明确状态 / 当前场景相关特征值可用于评估 | D455观察确认路径、已有场景缓存路径、交互者说明路径；D455 路径含观察帧取得、材料可回查、候选验证、候选确认、提交入账 | 3/4 | 遗传 | OR 路径提交链；D455 材料页；候选验证和确认方案；提交确认观察存在事实；坐标变换与场景绝对坐标提交 | 不新增需求主信息 OR 字段；不把读取快照冒充新观察动作；不把 D455 材料直接写成风险明确或安全值增加 |

## 坐标口径对 G4-G6 的补充

G4 到 G6 涉及空间位置时，统一采用 2026-06-01 坐标口径：

```text
正式存储：存在_场景绝对坐标 / 存在_场景绝对空间范围
自我参照：自我_场景绝对坐标 / 自我_场景绝对姿态
派生视图：自我视图_存在相对坐标 / 相对距离 / 边界关系
观察材料：D455 相机坐标、ROI、掩码、点集、深度切片
```

D455 材料只能作为 G6 的观察材料来源；经验证、确认、坐标变换和提交入口后，才可成为 G4/G5 使用的场景绝对坐标事实。自我边界关系只在自我视图中派生，不进入存在本体主事实。

## P1 前置结论

P0 已完成映射表。进入 P1 时需要解决的代码点是：

```text
当前 私有_确保遗传因果模板_线程侧 只接收单个因比较；
G2 / G4 / G5 / G6 需要同一主果下多个因侧比较；
应复用 因果主信息类.因比较模板，增加多因侧比较封装和查重能力。
```
