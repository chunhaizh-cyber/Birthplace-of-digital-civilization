# 20260620 场景类非场景主信息操作扫描 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.3 P4.1，扫描 `场景类.h / 场景类.cpp` 中非场景主信息直接操作并分类。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
实施记录/20260620_剩余场景主信息读取分类_Codex断点清单.md
```

本切片范围：

```text
1. 只扫描和分类。
2. 不修改源码。
3. 不运行构建。
```

## 1. 扫描命令

```powershell
rg -n "取主信息<[^>]+>|dynamic_cast<[^>]+主信息|->主信息|new .*主信息类|状态节点主信息类|特征节点主信息类|动态节点主信息类|二次特征主信息类|因果主信息类|需求主信息类|方法主信息类" 场景类.cpp 场景类.h
```

输出文件：

```text
日志/20260620_场景类非场景主信息操作扫描.txt
```

## 2. 分类结果

```text
总命中：19

场景主信息内部职责：
    场景类::取场景主信息
    场景类::是场景节点
    场景类::按主键解析场景节点
    场景类::创建场景_按名称

容器枚举：
    私有_直接基础信息子节点_按类型
    场景类::获取子场景
    场景类::获取子存在
    场景类::获取子特征

特征类职责疑似越界：
    私有_查找子特征_按类型 直接读取 特征节点主信息类::类型。
    私有_确保场景默认特征已初始化 直接创建 特征节点主信息类 并写入 场景安全度 当前值。

状态类职责疑似越界：
    场景类::追加状态 为排序直接读取 状态节点主信息类::发生时间 / 收到时间。

动态类职责疑似越界：
    场景类::追加动态 为排序直接读取 动态节点主信息类::时间。
```

## 3. 下一步

```text
P4.2 第一优先：复核 私有_确保场景默认特征已初始化。
原因：
    该路径在场景类中直接创建 / 写入特征节点，并写入 场景安全度 半刻度值。
    它比排序读取更接近业务语义和安全值误判风险。

P4.2 先查 场景安全度 引用和当前初始化用途；再决定删除旧默认值，或转入 特征类 owning helper。
```

禁止宣称：

```text
不宣称 P4 非场景主信息直接操作审查完成。
不宣称场景类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
