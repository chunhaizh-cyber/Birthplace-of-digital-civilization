# 20260627 抽象存在生成存在树纠偏 Codex 断点清单

## 0. 恢复摘要

当前切片：
    `计划/20260627_抽象存在生成存在树纠偏计划_v0.1.md` / P0 现状审计。

切换来源：
    用户明确要求优先执行该旁路计划，并随后输入“执行”。

当前目标：
    先完成只读审计，确认现有代码对“存在概念根 / 概念根 / 存在”、核心概念树、实例创建入口和概念节点事实写入的真实情况。

已改文件：
    `计划/计划索引.md`
    `实施记录/20260627_抽象存在生成存在树纠偏_Codex断点清单.md`

已运行命令：
    `git status --short --untracked-files=no`
    `Get-Content -Path '计划\计划索引.md' -TotalCount 180`
    `Get-Content -Path '计划\20260627_抽象存在生成存在树纠偏计划_v0.1.md' -TotalCount 260`
    `Get-Content -Path 'docs\00_文档总索引.md' -TotalCount 120`
    `Get-Content -Path 'docs\工程图谱\00_图谱总索引.md' -TotalCount 160`
    `Select-String -Path 'docs\工程图谱\05_规则原子索引.md' -Pattern '存在|抽象|概念|实例|只读|确保|I64_MAX|自由文本|项目结构'`
    `Get-Content -Path '规范\禁止性规范20260611.md' -TotalCount 180`
    `Get-Content -Path '规范\基础抽象命名规则规范20260627.md' -TotalCount 220`
    `Get-Content -Path '规范\存在概念与实例创建最小闭环规范20260509.md' -TotalCount 220`
    `Get-Content -Path '规范\存在根概念元特征规范20260509.md' -TotalCount 220`
    `Get-Content -Path '规范\抽象特征规范20260518.md' -TotalCount 160`

当前判断：
    该计划已经满足接入计划索引条件；下一步仍是 P0 只读审计，不先改业务代码。

下一步：
    使用 Graph RAG 精确定位 `基础信息类::取或创建存在概念根`、`基础信息类::选择信息入口默认根`、`世界树类::初始化存在概念系统`、`世界树类::初始化核心存在概念树`、`世界树类::确保存在概念`、`世界树类::创建存在实例`、`世界树类::根据概念扩充实例记忆`、`存在类::确保存在概念`、`存在类::创建存在实例`、`自我类::读取自我初始化结果`，再用源码复核 P0 检查项。

禁止宣称项：
    不宣称抽象存在纠偏完成。
    不宣称概念侧 / 实例侧边界已接入闭环。
    不宣称运行时业务验证已通过。
    不提前宣称自我苏醒完成或初步成熟完成。

## 1. P0 审计依据

命中规则：
    `RULE-STRUCT-001`
    `RULE-STRUCT-002`
    `RULE-STRUCT-003`
    `RULE-ABSTRACT-001`

来源规范：
    `规范/禁止性规范20260611.md`
    `规范/基础抽象命名规则规范20260627.md`
    `规范/存在概念与实例创建最小闭环规范20260509.md`
    `规范/存在根概念元特征规范20260509.md`
    `规范/抽象特征规范20260518.md`

## 2. P13b 返回点

原主动计划：
    `计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md`

原当前切片：
    P13b：继续选择剩余 B 类低风险小函数族。

原恢复断点：
    `实施记录/20260627_P13b_JSON字符串转义工具迁移_Codex断点清单.md`

恢复条件：
    本计划第一优先闭合切片完成、阻塞或用户要求返回后，再恢复 P13b。

## 3. P0 审计记录

### 3.1 旧根和权威根现状

源码锚点：
    `基础信息类.cpp:355` `基础信息类::取或创建存在概念根`
    `世界树类.cpp:1830` `世界树类::取或创建存在概念根`
    `世界树类.cpp:1849` `世界树类::确保根概念_存在`
    `世界树类.cpp:1921` `世界树类::初始化核心存在概念树`
    `语素主信息类.h:30` `存在概念入口` 默认根名

现状：
    `基础信息类::取或创建存在概念根()` 当前创建 `名称=存在概念根`、`类型=概念根` 的存在节点。
    `世界树类::取或创建存在概念根()` 只是把该旧根缓存到 `世界树类::存在概念根`。
    `世界树类::确保根概念_存在()` 当前会在旧根下面创建名为 `存在` 的子概念。
    `初始化核心存在概念树()` 先确保旧根，再确保 `存在 / 实物存在 / 虚拟存在 / 信息存在 / 数字生命 / 自我 / 需求 / 任务 / 方法 / 状态 / 事件 / 规则`。

判断：
    当前运行结构是“旧容器根 `存在概念根` -> 权威抽象概念 `存在` -> 子概念”。
    P1/S1 不能只改显示文案；还要保留概念侧范围根语义，避免破坏 `是存在概念 / 是实例存在` 的树范围判定。

### 3.2 概念节点与实例节点类型写入现状

源码锚点：
    `世界树类.cpp:2483` `世界树类::确保存在概念`
    `存在类.cpp:533` `存在类::确保存在概念`
    `存在类.cpp:545` `存在类::创建存在实例`
    `存在类.cpp:712` `存在类::添加实例概念`

现状：
    `存在类::确保存在概念()` 底层创建概念时传 `类型=nullptr`。
    `世界树类::确保存在概念()` 随后在类型为空时写 `类型=名称`。
    `存在类::创建存在实例()` 创建实例时也传 `类型=nullptr`，之后仅写入 `概念集` 和首个 `概念模板`。

判断：
    这是 P2/S2 的类型字段纠偏对象，不混入当前 S1 根名切片。

### 3.3 实例创建入口现状

源码锚点：
    `世界树类.cpp:2512` `世界树类::创建存在实例`
    `存在类.cpp:545` `存在类::创建存在实例`
    `世界树类.cpp:2388` `取或创建子存在_按类型`
    `世界树类.cpp:2449` `取或创建子存在_按类型并按特征I64`

现状：
    显式实例创建入口只有 `世界树类::创建存在实例` 和 `存在类::创建存在实例`。
    仍存在通用按类型创建子存在入口，直接调用 `存在服务_.创建存在_按名称类型(...)`。

判断：
    通用按类型子存在入口是否属于实例生成链问题，需要 P3/S3 单独逐项判定；当前 S1 不扩大到实例生成规则。

### 3.4 概念扩充实例记忆现状

源码锚点：
    `世界树类.cpp:2538` `世界树类::根据概念扩充实例记忆`

现状：
    该函数收集概念及上位概念，随后对实例执行 `添加实例概念` 和 `复制子特征到宿主`。

判断：
    缺失特征补齐、已有事实不覆盖、冲突特征处理属于 P5/S5；当前 S1 不处理。

### 3.5 只读 / 确保路径现状

源码锚点：
    `自我类.初始化.cpp:164` `私有_计算自我初始化结果`
    `自我类.初始化.cpp:390` `读取自我初始化结果`

现状：
    `读取自我初始化结果()` 调用 `私有_计算自我初始化结果(..., true)`。
    `私有_计算自我初始化结果()` 计算 `概念层已就绪` 时调用 `世界树.初始化存在概念系统()`，会触发确保路径。

判断：
    这是明确的“读取路径隐式确保”问题，但属于 P6/S4；当前 S1 不处理。

### 3.6 控制面板 / SQL 显示依赖

源码锚点：
    `世界树类.cpp:1776` `同步SQL控制面板世界显示项`
    `世界树类.cpp:330` `私有_世界树SQL节点短标题`

现状：
    显示项从节点主信息的名称 / 类型生成短标题；未发现控制面板代码用 `存在概念根` 作为业务判断。
    `确保存在概念/新建` 只作为显示阶段文本写入 SQL/控制面板镜像。

判断：
    SQL/控制面板只作为显示 / 诊断 / 存储验证，不能反向作为本计划业务证据。

### 3.7 概念节点事实账本检查

只读扫描：
    未发现 `创建状态 / 创建动态 / 写入状态 / 安全` 同一行直接拿 `确保根概念 / 确保核心概念 / 确保存在概念 / 存在概念根` 当主体写事实账本。

判断：
    暂未发现概念节点直接承载动态历史、安全评估、伤害记忆或任务账务值的高危锚点；后续若切到 P5/S5 仍需按函数级路径复核。

### 3.8 需要兼容的旧节点清单

需要识别但不得继续作为最高抽象名：
    `名称=存在概念根`
    `类型=概念根`

需要保留语义或迁移设计的范围根：
    `世界树类::存在概念根`
    `存在类::是存在概念(节点, 存在概念根)`
    `存在类::是实例存在(节点, 存在概念根)`

### 3.9 允许修改文件清单

S1 最小允许：
    `基础信息类.cpp`
    `基础信息类.h`
    `世界树类.cpp`
    `世界树类.h`
    `语素主信息类.h`
    `说明书/函数功能说明文档.md`
    `实施记录/20260627_抽象存在生成存在树纠偏_Codex断点清单.md`

S1 暂不主动修改：
    `存在类.cpp`
    `存在类.h`
    `自我类.初始化.cpp`

### 3.10 禁止修改范围

本切片不新增 `抽象存在主信息类`。
本切片不新增 `枚举_存在形态`。
本切片不处理类型字段纠偏、实例生成链纠偏、只读 / 确保拆分、概念扩充冲突处理。
本切片不把 `存在概念根` 继续解释成最高抽象名。
本切片不依赖控制面板 / SQL 文本作为机器判断证据。

## 4. S1 根名纠偏实施记录

### 4.1 修改范围

已修改：
    `基础信息类.cpp`
    `基础信息类.h`
    `世界树类.cpp`
    `语素主信息类.h`
    `说明书/函数功能说明文档.md`

实施内容：
    `基础信息类::取或创建存在概念根()` 改为优先返回 `名称=存在, 类型=存在` 的权威根。
    旧 `名称=存在概念根, 类型=概念根` 只作为迁移查找来源；若旧根下已有 `存在` 子概念，优先把该子概念作为概念侧根；否则迁移旧根本身。
    新建根节点时只创建 `名称=存在, 类型=存在`。
    `世界树类::确保根概念_存在()` 改为直接返回 `取或创建存在概念根()`，避免在权威根下再创建一个子 `存在`。
    `语素主信息类.h` 中 `存在概念入口` 默认根名改为 `存在`，不再把 `存在概念根` 暴露为当前默认根名。
    被触碰且缺失头部规则注释模块的 `基础信息类.cpp`、`基础信息类.h`、`语素主信息类.h` 已补最小模块。

### 4.2 已运行工程守卫

`python .\tools\生成函数功能说明文档.py`
    退出码：0
    结果：已生成 `D:\鱼巢\说明书\函数功能说明文档.md`；源码文件 127；函数条目 7214。

`msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
    退出码：0
    结果：已成功生成；0 个警告；0 个错误。

### 4.3 真实运行预测

命令：
    `.\x64\Debug\鱼巢.exe --panel-html --self-check-log --self-check-log-wait-ms=30000`

预测：
    程序退出码应为 0。
    初始化后概念侧权威根应为 `名称=存在, 类型=存在`。
    不应再新建当前权威根 `名称=存在概念根, 类型=概念根`。
    核心概念链不应出现 `存在` 作为 `存在概念根` 下的重复子概念。
    若控制面板 HTML 发生刷新，只能作为显示镜像检查，不作为业务闭合证据。

实际：
    第一次运行命令 `.\x64\Debug\鱼巢.exe --panel-html --self-check-log --self-check-log-wait-ms=30000`。
    退出码：0。
    关键输出：自我线程启动前环境初始化 15 个步骤均成功；存在概念树与核心语素绑定成功；自检线程运行并写入 `./日志`。
    局限：`控制面板.html` LastWriteTime 仍为 2026-06-26 09:36:46，说明混合命令没有刷新静态 HTML，不能把该 HTML 当本次运行产物。
    日志检索：`日志\鱼巢_run_20260627.低值g` 未命中 `存在概念根` / `概念根`；命中旧有 `VX0异常成功` 文字属于历史/业务日志用语，不是本次根名纠偏新增弹窗或崩溃。

补充验证计划：
    单独运行 `.\x64\Debug\鱼巢.exe --panel-html`，确认静态 HTML 是否刷新，再检索 `存在概念根` / `概念根`。

### 4.4 补充运行与规范治理检查

`.\x64\Debug\鱼巢.exe --panel-html`
    退出码：0。
    结果：已生成 `D:\鱼巢\控制面板.html`。
    来源：ADO-SQL 运行态显示镜像。
    `控制面板.html` 更新时间：2026-06-27 13:26:20。
    `存在概念根` 命中数：0。
    `概念根` 命中数：0。
    显示镜像观察：核心概念显示未再暴露旧根名；该结果只作为显示 / 诊断辅助，不作为机器判断证据或业务闭合证据。

`python .\tools\check_specs.py`
    退出码：0。
    结果：spec files 67；indexed specs 67；rule atoms 49；hard issues 0；warnings 73。
    判断：未出现硬性规范治理阻断；warnings 为既有规范治理告警，不在 S1 根名纠偏范围内处理。

### 4.5 S1 收束判断

S1 已完成：
    代码创建口径已改为 `名称=存在, 类型=存在`。
    `世界树类::确保根概念_存在()` 已避免在权威根下重复创建子 `存在`。
    语素入口默认根名已改为 `存在`。
    函数说明文档已刷新。
    构建、规范治理检查和静态 HTML 显示镜像检查均通过工程守卫。

S1 不宣称：
    不宣称抽象存在生成存在树纠偏全部完成。
    不宣称概念侧 / 实例侧边界已接入闭环。
    不宣称业务运行闭合、自我苏醒完成或初步成熟完成。

下一切片：
    S2 / P2 类型字段纠偏。
    重点锚点：`世界树类::确保存在概念()` 当前仍需确认概念节点类型是否表达上位抽象归属；`存在类::创建存在实例()` 当前仍需确认实例类型是否写主抽象归属。
    边界：S2 不混入只读 / 确保拆分、实例生成链重构、归类证据链或概念扩充冲突处理。

## 5. S2 类型字段纠偏执行记录

### 5.1 Graph RAG 尝试与降级边界

尝试：
    调用 `codebase-memory-mcp/list_projects`。
    调用 `codebase-memory-mcp/search_graph(project="D", label="Function", name_pattern=".*确保存在概念.*")`。

实际：
    两次均返回 `Transport closed`。

判断：
    这是 Graph RAG / MCP 工具通道不可用，不是鱼巢项目逻辑失败。
    本切片改为按本地查询规约降级：`rg` 精确定位 -> 函数说明文档命中 -> 源码函数段复核 -> 必要时构建和真实运行验证。
    C++ outbound 图谱本来也只能辅助；S2 的修改依据以源码为准。

### 5.2 S2 修改记录

修改范围：
    `存在类.cpp`
    `存在类.h`
    `世界树类.cpp`
    `说明书/函数功能说明文档.md`

实施内容：
    `存在类.cpp/.h` 补充文件头部规则注释模块。
    `存在类::确保存在概念()` 新建概念时，从父概念主信息推导子概念 `类型`：优先父概念名称，其次父概念类型。
    `存在类::创建存在实例()` 新建实例时，从输入概念集第一个有效概念推导实例主抽象类型：优先概念名称，其次概念类型。
    `世界树类::确保存在概念()` 对已存在概念执行同口径类型纠偏，不再把空类型补成自身名称。

边界：
    不新增 `抽象存在主信息类`。
    不新增 `枚举_存在形态`。
    不改变实例同名多实例规则。
    不处理只读 / 确保拆分、归类证据链、概念扩充冲突处理。

### 5.3 已运行工程守卫与真实运行预测

`python .\tools\生成函数功能说明文档.py`
    退出码：0。
    结果：已生成 `D:\鱼巢\说明书\函数功能说明文档.md`；源码文件 127；函数条目 7216。

`msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
    退出码：0。
    结果：已成功生成；0 个警告；0 个错误。

预测命令：
    `.\x64\Debug\鱼巢.exe --panel-html`

预测：
    程序退出码应为 0。
    控制面板 HTML 应刷新。
    核心概念显示中：
        `实物存在` / `虚拟存在` / `信息存在` / `数字生命` 的类型应为 `存在`。
        `自我` 的类型应为 `数字生命`。
        `需求` / `任务` / `方法` 的类型应为 `虚拟存在`。
        `状态` / `事件` / `规则` 的类型应为 `信息存在`。
    该验证只作为显示镜像辅助；正式判断仍以源码创建口径和构建通过为准。

实际：
    `.\x64\Debug\鱼巢.exe --panel-html`
        退出码：0。
        输出：`控制面板 HTML 已生成: D:\鱼巢\控制面板.html | 来源=ADO-SQL`。
        `控制面板.html` 更新时间：2026-06-27 13:40:45。
        `存在概念根` 命中数：0。
        `概念根` 命中数：0。

    限定 `currentWorld` section 抽取核心概念摘要：
        `实物存在(存在)` parent=8。
        `虚拟存在(存在)` parent=8。
        `信息存在(存在)` parent=8。
        `数字生命(存在)` parent=8。
        `自我(数字生命)` parent=C。
        `需求(虚拟存在)` parent=A。
        `任务(虚拟存在)` parent=A。
        `方法(虚拟存在)` parent=A。
        `状态(信息存在)` parent=B。
        `事件(信息存在)` parent=B。
        `规则(信息存在)` parent=B。

    说明：
        `currentWorld` 表格的 `category/type` 列显示的是节点类别口径；存在主信息类型通过摘要短标题 `名称(类型)` 体现。
        HTML/SQL 仍只作为显示镜像辅助，不替代源码创建口径和项目结构验收。

`python .\tools\check_specs.py`
    退出码：0。
    结果：spec files 67；indexed specs 67；rule atoms 49；hard issues 0；warnings 73。

### 5.4 S2 收束判断

S2 已完成：
    概念节点类型新建口径已改为父概念上位归属。
    已存在概念节点在 `世界树类::确保存在概念()` 路径会按父概念归属纠偏。
    实例节点新建时可从输入概念集写入主抽象类型。
    函数说明文档已刷新。
    构建、真实 `--panel-html` 运行和规范治理检查均通过工程守卫。

S2 不宣称：
    不宣称实例树生成规则全部闭合。
    不宣称归类证据链、概念扩充冲突处理或只读 / 确保拆分已完成。
    不宣称业务闭合、自我苏醒完成或初步成熟完成。

下一切片：
    S3 / P3 实例存在树生成规则。
    重点锚点：实例创建必须进入场景 / 内部世界；概念归属进入概念集；实例不得挂在概念侧父节点下。
    边界：S3 先做实例入口审计和最小纠偏，不混入归类证据链和概念扩充冲突处理。

## 6. S3 实例存在树生成规则执行记录

### 6.1 入口审计与第一个独立问题

审计命中：
    显式 `存在类::创建存在实例()` 当前只由 `世界树类::创建存在实例()` 包装调用。
    `世界树类::创建存在实例()` 会把实例挂到传入 `所属场景`，或现实世界 / 自我现实场景下。
    任务虚拟存在、方法虚拟存在、线程存在、外设内部快照等大量现有运行存在走 `取或创建子存在_按名称类型` / `取或创建子存在_按类型`，这些属于更大的通用运行存在入口，不在本小步一次性泛化重构。

第一个独立问题：
    `世界树类::取或创建自我存在()` 把自我实例创建为 `名称=自我, 类型=自我类型`。
    初始化链随后通过 `私有_绑定当前自我概念集()` 给自我实例加入 `存在 / 数字生命 / 自我` 概念集。

证据：
    源码锚点：`世界树类.cpp:3750` 附近 `取或创建自我存在()`。
    控制面板镜像：`F0` 位于 `自我初始场景` 下，摘要仍为 `自我(自我类型)`；概念集关系中已有 `数字生命(存在)` 和 `自我(数字生命)`。

判断：
    自我实例已经进入场景树，概念集也已绑定；当前不一致集中在实例主信息 `类型` 未使用主抽象概念 `自我`。
    本切片先修正自我实例类型口径，不扩大到全部通用运行存在入口。

### 6.2 S3 最小修改与预测

修改范围：
    `世界树类.cpp`
    `说明书/函数功能说明文档.md`

实施内容：
    `世界树类::取或创建自我存在()` 的类型词从 `自我类型` 改为存在概念入口 `自我`。
    先按名称查找当前场景下已有自我实例；若已有但类型不是 `自我`，只纠偏该实例类型，不创建第二个自我实例。
    不改变自我实例名称、不改变父场景、不改变概念集绑定逻辑。

`python .\tools\生成函数功能说明文档.py`
    退出码：0。
    结果：源码文件 127；函数条目 7216。

`msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
    退出码：0。
    结果：已成功生成；0 个警告；0 个错误。

预测命令：
    `.\x64\Debug\鱼巢.exe --panel-html`

预测：
    程序退出码应为 0。
    控制面板 HTML 应刷新。
    `currentWorld` 中自我实例仍位于 `自我初始场景`。
    自我实例摘要不应再出现 `自我(自我类型)`；应显示为 `自我` 或 `自我(自我)` 这类主抽象概念口径。
    自我实例概念集仍应包含 `数字生命(存在)` 和 `自我(数字生命)`。
    该验证仍只作为显示镜像辅助。

实际：
    `.\x64\Debug\鱼巢.exe --panel-html`
        退出码：0。
        输出：`控制面板 HTML 已生成: D:\鱼巢\控制面板.html | 来源=ADO-SQL`。
        `控制面板.html` 更新时间：2026-06-27 13:50:31。
        `自我(自我类型)` 命中数：0。
        `存在概念根` 命中数：0。
        `概念根` 命中数：0。

    限定 `currentWorld` section 抽取：
        `D | parent=C | category=存在 | summary=确保存在概念/新建 | 自我(数字生命)`。
        `F0 | parent=Ez | category=存在 | summary=取或创建自我存在/新建 | 自我`。

    概念集关系抽取：
        `targetText="数字生命(存在)"` 命中 1。
        `targetText="自我(数字生命)"` 命中 1。

`python .\tools\check_specs.py`
    退出码：0。
    结果：spec files 67；indexed specs 67；rule atoms 49；hard issues 0；warnings 73。

### 6.3 S3a 收束判断

S3a 已完成：
    自我实例仍位于 `自我初始场景`，未挂到概念侧。
    自我实例类型不再使用旧 `自我类型` 文本入口。
    自我实例概念集仍保留 `数字生命` 和 `自我` 概念归属。
    构建、`--panel-html` 和规范治理检查均通过工程守卫。

S3a 不宣称：
    不宣称所有通用运行存在入口已统一改造。
    不宣称任务 / 方法 / 外设快照等运行存在的概念集边界已全部审完。
    不宣称归类证据链、概念扩充冲突处理或只读 / 确保拆分已完成。

下一切片：
    S3b：通用运行存在入口概念集 / 场景边界审计。
    重点：`取或创建子存在_按名称类型`、`取或创建子存在_按类型`、`取或创建子存在_按类型并按特征I64` 的调用是否都落在场景 / 内部世界 / 宿主存在下；是否已有概念集补齐链；哪些不应在本计划中泛化处理。

## 7. S3b 通用运行存在入口概念集 / 场景边界执行记录

### 7.1 入口审计与第一个独立问题

审计命中：
    `世界树类::创建存在实例()` 显式实例创建入口会调用 `存在类::创建存在实例()`，实例挂到传入 `所属场景` 或现实世界 / 自我现实场景，并从输入概念集推导主类型、写入概念集。
    `世界树类::取或创建子存在_按名称类型()`、`取或创建子存在_按类型()`、`取或创建子存在_按类型并按特征I64()` 是任务虚拟存在、方法虚拟存在、线程存在、治理存在、外设观察材料和运行记录等通用运行存在入口。

第一个独立问题：
    三个通用运行存在入口只写 `类型`，不会把类型对应的已绑定存在概念补入 `概念集`。

证据：
    `世界树类.cpp:2372`、`世界树类.cpp:2388`、`世界树类.cpp:2452` 的创建路径均直接调用 `存在服务_.取或创建子存在_按名称类型` 或 `创建存在_按名称类型`。
    `存在类::创建存在实例()` 已有概念集写入逻辑，但通用入口绕过了该函数。

判断：
    S3b 不应批量创建所有未建模类型概念，否则会把外设观察材料、特征模板入口或业务临时类型强行概念化。
    最小闭合方案是在世界树薄封装层只解析“语素入口已绑定到存在概念节点”的类型词，并补入该实例概念集；解析失败时不创建新概念、不写日志字段、不扩大概念树。

### 7.2 S3b 最小修改与预测

修改范围：
    `世界树类.cpp`
    `说明书/函数功能说明文档.md`

实施内容：
    新增文件内私有辅助：
        `私有_解析已绑定存在概念(...)`：只查 `语素入口对应基础信息`，不创建新概念。
        `私有_补齐类型概念集(...)`：若类型词已绑定存在概念，则调用 `存在服务.添加实例概念(..., false)` 补缓存。
    三个通用运行存在入口在新建或复用已有节点时调用该辅助；若已有节点发生概念集补齐，同步控制面板显示镜像。

预测：
    构建应通过。
    `--panel-html` 应通过。
    控制面板世界显示不应恢复 `存在概念根` / `概念根`。
    该修改只补“已绑定存在概念”的概念集缓存，不证明未建模类型已完成归类证据链。

`python .\tools\生成函数功能说明文档.py`
    退出码：0。
    结果：源码文件 127；函数条目 7218。

`msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
    退出码：0。
    结果：已成功生成；0 个警告；0 个错误。

实际：
    `.\x64\Debug\鱼巢.exe --panel-html`
        退出码：0。
        输出：`控制面板 HTML 已生成: D:\鱼巢\控制面板.html | 来源=ADO-SQL`。

    控制面板 HTML 抽取：
        `存在概念根` 命中数：0。
        `概念根` 命中数：0。
        `自我(自我类型)` 命中数：0。
        `数字生命(存在)` 命中数：4。
        `自我(数字生命)` 命中数：4。
        `虚拟存在(存在)` 命中数：170。
        `方法(虚拟存在)` 命中数：170。
        `任务(虚拟存在)` 命中数：2。

    关系表示例：
        `F0 -> 概念集 -> 存在 / 数字生命(存在) / 自我(数字生命)`。
        `1uj -> 概念集 -> 存在 / 虚拟存在(存在) / 方法(虚拟存在)`。
        `1rv -> 概念集 -> 存在 / 虚拟存在(存在) / 方法(虚拟存在) / 内部方法(方法) / 本能方法(方法) / 用双目相机取得当前观察特征帧(方法)`。

`python .\tools\check_specs.py`
    退出码：0。
    结果：spec files 67；indexed specs 67；rule atoms 49；hard issues 0；warnings 73。

### 7.3 S3b 场景边界审计与收束判断

代表调用点复核：
    `任务类.cpp` 任务虚拟存在：父节点来自任务承载场景 / 内部世界推导，不是概念侧父节点。
    `方法类.cpp` 方法虚拟存在：优先挂到宿主存在内部世界，缺宿主时挂虚拟世界。
    `自我类.cpp` 线程池 / 线程存在：挂自我内部世界或线程池存在下。
    `自我线程模块.impl.cpp` 治理主体：挂自我内部世界；安全风险树节点挂调用方传入父宿主下。
    `自我动作实现*.ixx` 运行存在、服务治理视图、候选池、外设观察材料、内外接口、场景识别快照、OpenGL复现数据、自我边界参照：均以方法虚拟存在、任务虚拟存在、输出结果场景、自我宿主、内部世界或调用方宿主为父节点，未见挂到概念侧父链的代表反例。

S3b 已完成：
    三个通用运行存在入口对已绑定存在概念的类型词补概念集缓存。
    未绑定类型词不创建新概念，保留后续归类证据链 / 概念扩充切片处理。
    代表调用点父节点边界未发现概念侧挂实例反例。

S3b 不宣称：
    不宣称所有外设观察材料、服务治理临时类型和运行记录类型都已形成正式抽象概念。
    不宣称概念集更新已有归类证据链。
    不宣称概念扩充冲突处理或只读 / 确保拆分已完成。

下一切片：
    S4：只读 / 确保边界纠偏。
    重点：`读取自我初始化结果()` 不应通过读取路径隐式调用 `初始化存在概念系统()`。
