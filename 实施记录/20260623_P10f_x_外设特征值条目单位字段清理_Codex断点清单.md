# P10f-x 外设特征值条目单位字段清理 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-x `外设观察报告队列.ixx::结构_外设特征值条目::单位` 边界复核与清理。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_feature_value_entry.tsv`

当前判断：

- `结构_外设特征值条目::单位` 只在 `外设提交_构造I64特征(...)` 内被赋值，没有读取点。
- 单位字符串未进入候选选择、等待匹配、状态推进、稳定键、结算或因果匹配。
- 当前无消费字段直接删除；后续若需要单位语义，应由特征类型 / 值域或枚举承载，不恢复自由文本字段。
- 已删除 `结构_外设特征值条目::单位` 字段。
- 已删除 `外设提交_构造I64特征(...)` 的 `单位` 参数。
- 已删除所有调用点中的 `"像素" / "毫米" / "百万分比" / "数量" / "枚举值"` 单位文本传参。

构建前预测：

- 删除 `结构_外设特征值条目::单位`。
- 删除 `外设提交_构造I64特征(...)` 的 `单位` 参数及所有调用点文本传参。
- 预期 Debug x64 构建通过，0 警告，0 错误。

完成证据：

- 固定字符串复扫：目标文件中 `.单位 / std::string 单位 / const std::string& 单位 / "像素" / "毫米" / "百万分比" / "数量" / "枚举值"` 无残留。
- 最新复扫清单：`实施记录/20260623_P10f_live_结构体string字段声明复扫_after_feature_value_unit.tsv`，剩余 621 行，其中主键 / ID / 键 / 句柄类 325 行、非主键类 296 行。
- `git diff --check` 通过。
- `Debug|x64` 构建通过，0 警告，0 错误。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
