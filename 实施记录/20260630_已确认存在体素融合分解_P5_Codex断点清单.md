# 20260630 已确认存在体素融合分解 P5 Codex 断点清单

## 0. 恢复摘要

当前切片：P5 父体素提交和版本闸门。

目标：

```text
为父体素模型增加正式 I64 版本特征；
体素候选提交前检查基准体素版本是否仍等于当前父体素版本；
版本一致才允许继续裁决或写父体素；
版本变化时只挂起候选，不覆盖父体素模型。
```

边界：

```text
本切片不做彩色分解；
不创建内部世界子存在；
不让体素加工工作线程写世界树事实；
不宣称父体素融合完整提交闭环完成，只闭合版本闸门代码口径。
```

## 1. 依据

```text
规范/已确认存在体素融合与分解规范20260630.md
详细设计/已确认存在体素融合与分解实现方案20260630.md
计划/20260630_已确认存在体素融合分解施工计划_v0.1.md
docs/工程图谱/05_规则原子索引.md RULE-VOXEL-FUSION-008
```

## 2. 构建前预测

```text
新增版本闸门 helper 后，Debug x64 构建应通过。
同一父存在当前体素版本等于候选基准版本时，候选状态可进入待裁决。
当前体素版本已变化时，候选状态应转为版本变化挂起，不能调用父体素写入。
成功写入父体素后，体素模型版本应更新为本次观察证据 ID。
```

## 3. 已改文件

```text
自我动作实现.外设模块.ixx
说明书/函数功能说明文档.md
实施记录/20260630_已确认存在体素融合分解_P5_Codex断点清单.md
计划/20260630_已确认存在体素融合分解施工计划_v0.1.md
```

## 4. 验证记录

```text
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：失败。
失败锚点：自我线程模块.impl.obj LNK2019，缺 `结构_父体素候选版本闸门结果::可裁决` 和 `版本变化挂起`。
当前判断：匿名命名空间结构的成员函数被导入方引用时没有生成可链接定义；改为匿名命名空间普通 inline helper。

python .\tools\生成函数功能说明文档.py
结果：通过；函数条目 7449。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：通过；0 个警告，0 个错误。

git diff --check -- 自我动作实现.外设模块.ixx 说明书/函数功能说明文档.md 实施记录/20260630_已确认存在体素融合分解_P5_Codex断点清单.md 计划/20260630_已确认存在体素融合分解施工计划_v0.1.md
结果：通过；仅出现 说明书/函数功能说明文档.md 的 CRLF/LF 提示，无空白错误。
```

## 5. 代码改动

```text
新增 `体素模型版本` I64 特征类型。
新增父体素候选版本闸门状态常量。
新增 `结构_父体素候选版本闸门结果`。
新增 `体素版本值有效`、`检查父体素候选版本闸门` 和两个闸门判断 helper。
`写入观察存在三维体素特征` 改为读取 `体素模型版本` 作为基准版本。
父体素写入前复查当前版本；版本不匹配时写 `版本变化挂起` 状态并返回，不调用父体素写入。
父体素写入成功后，把 `体素模型版本` 更新为本次观察证据 ID。
```

## 6. 剩余边界

```text
本切片只实现当前粗体素写入路径的版本闸门。
独立体素加工结果包的长期持久化、重算、合并策略和彩色分解仍属 P6 及后续。
```
