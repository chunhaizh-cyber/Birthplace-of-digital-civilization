# P10f-y 外设材料句柄字段复核 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-y `外设观察报告队列.ixx` 外设材料句柄类 string 字段边界复核。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_feature_value_unit.tsv`

当前判断：

- `结构_外设观察报告队列项::空间候选队列引用 / 像素归属账本引用` 会进入材料句柄集、缺失材料判断、材料页摘要匹配和 D455 句柄解析，不是日志文本。
- `结构_外设识别提交包::像素归属账本` 由报告项像素归属账本材料透传，当前承载句柄。
- `结构_外设已知存在扫描结果 / 结构_外设跟踪提交包::当前ROI / 当前掩码 / 当前点集 / 当前深度切片` 由簇材料句柄透传，下游用于可回查判断和日志显示。
- 当前不删除这些字段；本切片只把实际句柄字段收束为 `...句柄` 命名，并同步下游白名单、线程侧特征入口和日志显示。
- 已将 `空间候选队列引用 / 像素归属账本引用` 收束为 `空间候选队列句柄 / 像素归属账本句柄`。
- 已将识别提交包 `像素归属账本` 收束为 `像素归属账本句柄`。
- 已将扫描 / 跟踪提交包 `当前ROI / 当前掩码 / 当前点集 / 当前深度切片` 收束为 `当前ROI句柄 / 当前掩码句柄 / 当前点集句柄 / 当前深度切片句柄`。
- 已同步 D455 写入、外设材料汇总 / 缺口判断、材料页摘要、扫描可回查判断、自我线程日志、外设方法验证、任务管理白名单和线程侧特征入口。

构建前预测：

- 字段命名收束后，原有材料可用性、可回查和句柄解析逻辑不变。
- 预期 `rg` 复核旧字段访问无残留。
- 预期 `git diff --check` 通过。
- 预期 Debug x64 构建通过，0 警告，0 错误。

完成证据：

- `rg` 复核 `空间候选队列引用 / 像素归属账本引用 / 当前ROI / 当前掩码 / 当前点集 / 当前深度切片 / 像素归属账本` 旧字段访问无残留。
- 最新复扫清单：`实施记录/20260623_P10f_live_结构体string字段声明复扫_after_material_handle_fields.tsv`，剩余 621 行，其中主键 / ID / 键 / 句柄类 336 行、非主键类 285 行。
- `git diff --check` 通过。
- `Debug|x64` 构建通过，0 警告，0 错误。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
