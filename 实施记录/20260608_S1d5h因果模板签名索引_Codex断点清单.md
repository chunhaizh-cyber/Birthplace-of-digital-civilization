# 20260608 S1d5h 因果模板签名索引 Codex 断点清单

## 当前切片

- 主动计划：`计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- 执行方案：`计划/20260607_S1d5h因果模板签名索引瘦身方案_v0.1.md`
- 切片：S1d-5h 因果模板状态 / 动作签名索引瘦身。

## 依据文件

- `AGENTS.md`
- `规范/规范目录.md`
- `规范/方法动作动态硬规则规范20260512.md`
- `规范/动作动态即因果账本规范20260512.md`
- `规范/二次特征规范20260520.md`
- `规范/远程执行通道与断点续跑规范20260524.md`
- `计划/计划索引.md`
- `计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- `计划/20260607_S1d5g按动态创建因果信息内部定位方案_v0.1.md`
- `实施记录/20260608_S1d5g按动态创建因果信息内部定位_Codex断点清单.md`

## 当前判断

- S1d-5g 已闭合。
- `按动态补全两层因果/内部阶段耗时` 91 条。
- `动作模板查找` 平均约 15348us，`状态模板查找` 平均约 11523us。
- S7 因果模板签名索引恢复条件成立。

## 构建 / 运行前预测

- Debug x64 构建通过。
- 真实运行退出码 0。
- 日志继续出现 `按动态补全两层因果/内部阶段耗时`。
- `状态模板查找` 和 `动作模板查找` 平均耗时下降。
- 日志保留 `任务状态动作动态=<动态主键>`。
- 不出现 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`。
- 不出现旧路径锚点 `准备本能调用`、`执行本能方法`、`任务域本能输入包`。

## 索引项级刷新补充预测

- 第一版索引如果在创建模板和首次证据动态样本追加后整体失效，会导致状态模板查找 / 动作模板查找反复重建索引。
- 改为索引已有效时只刷新单个模板节点后，`状态模板查找` 和 `动作模板查找` 应下降到接近哈希查找与低频项级刷新成本。
- 删除因果和绑定基础信息仍保留整体失效，避免跨基础信息或删除节点后留下悬空索引。
- 真实运行仍应保留 `任务状态动作动态=<动态主键>`，且不出现任务状态逻辑错误或旧完整本能执行路径锚点。

## 允许修改

- `因果类.h`
- `因果类.cpp`
- `计划/20260607_S1d5h因果模板签名索引瘦身方案_v0.1.md`
- `计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- `计划/计划索引.md`
- `实施记录/20260608_S1d5h因果模板签名索引_Codex断点清单.md`

## 已改文件

- `计划/20260607_S1d5h因果模板签名索引瘦身方案_v0.1.md`
- `实施记录/20260608_S1d5h因果模板签名索引_Codex断点清单.md`
- `因果类.h`
- `因果类.cpp`

## 下一步

1. 更新 S1d-5h 方案、性能计划和计划索引。
2. 提交 S1d-5h 闭合切片。
3. 进入 S1d-5i：同类事件段聚合回潮定位 / 瘦身。

## 已运行命令

- `git diff --check -- '因果类.h' '因果类.cpp' '实施记录/20260608_S1d5h因果模板签名索引_Codex断点清单.md'`：退出码 0，仅 CRLF 提示。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`：退出码 0；仅既有 LNK4075 `/EDITANDCONTINUE` 与 `/INCREMENTAL:NO` 提示。
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000`：首次直接前台运行被 Codex 工具 180s 超时中断，遗留进程已终止；该次不作为闭合验收。
- PowerShell `Start-Process` 重定向 stdout/stderr 复跑 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000`：退出码 0，stdout=`日志/codex_S1d5h_rerun_stdout_20260608_134119.txt`，stderr=`日志/codex_S1d5h_rerun_stderr_20260608_134119.txt`。

## 验收结果

- 最新有效日志窗口：`日志/鱼巢_run_20260608.低值g:78951`。
- `程序退出/守卫`：6 条，确认重定向复跑正常退出。
- `任务状态动作动态=<动态主键>`：80 条。
- 未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
- `按动态补全两层因果/内部阶段耗时`：16 条，平均约 7830us，最大约 17726us。
- `状态模板查找` 平均约 92us；`动作模板查找` 平均约 48us。
- 对比 S1d-5g：`状态模板查找` 从约 11523us 降到约 92us；`动作模板查找` 从约 15348us 降到约 48us。
- 最新高层 `创建动态/内部阶段耗时` 19 条，最慢阶段全部为 `同类事件段聚合`，下一切片转入 S1d-5i。

## 禁止宣称

- 不宣称自我苏醒完成。
- 不宣称初步成熟完成。
- 不跳过因果账本。
- 不恢复 `任务筹办` / `任务执行` 本能方法。
