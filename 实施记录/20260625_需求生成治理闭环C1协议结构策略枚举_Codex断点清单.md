# 20260625 需求生成治理闭环 C1 协议结构策略枚举 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260625_需求生成与治理闭环代码修订计划_v0.1.md` 的 C1“协议结构与策略枚举”。

当前目标：

```text
1. 在任务管理协议层定义入树拒绝原因、拒绝闭环类型、拒绝处理状态和合法终态。
2. 增加结构化需求入树拒绝回执，并让派生需求入树回执可携带该回执。
3. 建立拒绝原因到闭环类型、后续处理类型和终态的静态策略表。
4. 本切片不接入自我线程裁决、不实现拒绝回执治理处理器、不宣称闭环已实现。
```

依据文件：

```text
规范/禁止性规范20260611.md
规范/规范目录.md
规范/需求树生长机制规范20260512.md
规范/需求临时因果视图展开规范20260526.md
规范/任务筹办与执行边界规范20260512.md
规范/需求入树拒绝回执与开发诊断闭环规范20260625.md
docs/工程图谱/00_图谱总索引.md
docs/工程图谱/01_任务需求方法闭环图谱.md
docs/工程图谱/05_规则原子索引.md
计划/计划索引.md
```

计划触碰文件：

```text
任务模块.管理线程协议.ixx
计划/计划索引.md
实施记录/20260625_需求生成治理闭环C1协议结构策略枚举_Codex断点清单.md
```

已知无关脏改：

```text
控制面板.html
鱼巢.vcxproj
鱼巢.vcxproj.filters
```

禁止宣称：

```text
不得宣称拒绝回执治理闭环已实现。
不得宣称开发诊断闭环已实现。
不得宣称需求生成与治理闭环已完成。
不得宣称自我苏醒或初步成熟完成。
```

## 1. 预测

本切片只增加协议结构和静态策略表，预期不改变运行行为。构建通过后，只能说明协议骨架已具备，不说明拒绝回执已经由自我线程产出或被任务线程处理。

## 2. 实施结果

已修改：

```text
任务模块.管理线程协议.ixx
```

实际完成：

```text
1. 补入文件头部规则注释模块，明确本文件只承载协议结构，不写处理逻辑。
2. 新增 枚举_需求入树拒绝阶段。
3. 新增 枚举_需求入树拒绝原因。
4. 新增 枚举_拒绝闭环类型。
5. 新增 枚举_拒绝后续处理类型。
6. 新增 枚举_拒绝回执处理状态。
7. 新增 结构_需求入树拒绝策略。
8. 新增 需求入树拒绝策略表_常量，并用 static_assert 保证每个正式拒绝原因都有策略项。
9. 新增 结构_需求入树拒绝回执。
10. 扩展 结构_派生需求入树回执，使其可携带结构化拒绝回执。
```

未做：

```text
1. 未接入 自我线程模块.impl.cpp 的入树裁决分支。
2. 未实现拒绝回执治理处理器。
3. 未实现开发诊断项账本。
4. 未改变任务执行、筹办、结算或父链重判行为。
```

## 3. 验证证据

```powershell
python .\tools\check_specs.py
```

结果：

```text
hard issues: 0
warnings: 81
```

warnings 为既有规范治理告警，本切片未新增 hard issue。

```powershell
rg -n -e "兼容" -e "fallback" -e "旧字段" -e "旧入口" -e "错误码" -e "return .*失败" -e "执行失败" -e "筹办失败" -e "上报失败" -e "生成失败" -e "std::string 状态" -e "std::string 类型" -e "std::string 归属" -e "证据不足" -e "冲突" -- '任务模块.管理线程协议.ixx'
```

结果：

```text
命中：任务模块.管理线程协议.ixx:534 I64 错误码 = 0;
git blame：3450fab5，2026-05-11 已存在。
判断：既有调试字段，不是本切片新增拒绝回执字段；本切片不扩大该字段使用。
```

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

结果：

```text
已成功生成。
0 个警告
0 个错误
```

## 4. 当前判断

C1 已闭合。当前只证明协议骨架、结构化回执载体和静态策略表已存在，并通过构建；尚不能证明拒绝回执治理闭环或开发诊断闭环已实现。

下一切片应进入 C2：自我线程生成结构化拒绝回执。
