# 20260704 四方法共享底层关联判断能力 Codex断点清单

## 0. 恢复摘要

当前切片：E9/P9 `计划/20260703_四方法共享底层关联判断能力实现映射计划_v0.1.md` S3 外设观察报告队列适配。

本轮边界：

```text
S0 只读扫描已完成并提交；
S1 已在视觉观察融合模块登记第一版共享关联判断值域并提交；
S2 已在视觉观察融合模块文件头补齐共享承载边界并提交；
S3 已在外设观察报告队列新增共享关联外设材料只读输入包和读取入口；
未修改规范、详细设计或计划索引。
```

下一步：

```text
进入 S4 四方法读取共享结果。
S4 应让观察 / 识别 / 跟踪 / 扫描只读共享结果或共享输入，不直接调用其它公开本能方法。
```

禁止宣称：

```text
不宣称共享底层关联判断能力已代码实现；
不宣称四方法已完成共享能力接入；
不宣称互调风险已代码清零；
不宣称共享能力缺口已进入需求治理链；
不宣称真实 D455 / 自然样本验证已执行。
```

## 8. S4 四方法读取共享结果进行中

当前切片：

```text
S4 四方法读取共享结果。
```

已改文件：

```text
自我动作实现.外设模块.ixx
自我动作实现.扫描.ixx
说明书/函数功能说明文档.md
```

实现口径：

```text
外设三方法侧新增 `写入共享关联外设材料输入摘要(...)`，只把 `结构_共享关联外设材料输入` 投影为既有报告 ID、可观测单位数量、补观察缺口状态和质量缺口状态。
识别、跟踪、观察只读取外设队列共享输入或按报告 ID 回读共享输入，不调用其它公开本能方法。
扫描侧读取同一共享外设输入，并把共享输入缺口数量并入既有视觉信息缺口数量，不新建扫描事实裁决。
```

已运行命令：

```powershell
python .\tools\生成函数功能说明文档.py
git diff --check -- '自我动作实现.外设模块.ixx' '自我动作实现.扫描.ixx' '说明书/函数功能说明文档.md'
python .\tools\check_specs.py
```

当前结果：

```text
函数说明文档已生成：源码文件 129，函数条目 7727。
git diff --check：退出码 0；仅提示函数说明文档 CRLF/LF 工作区换行警告。
check_specs.py：hard issues = 0，warnings = 0。
```

构建前预测：

```text
Debug x64 构建应通过；若失败，优先检查 std::optional 条件表达式、std::size_t 类型和共享输入结构在两个触碰模块中的可见性。
```

构建验证：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

结果：

```text
退出码 0。
Debug x64 构建已成功生成 `D:\鱼巢\x64\Debug\鱼巢.exe`。
0 个警告，0 个错误。
构建锁 `.codex_locks/build-run.lock` 已释放。
```

S4 当前判断：

```text
S4 已闭合：观察 / 识别 / 跟踪 / 扫描均读取共享外设输入或共享结果摘要；未发现需要四方法公开本能入口互调；未新增第五个感知本能方法；未新增世界树稳定事实、稳定体素写入、需求满足或动作动态写权。
真实 D455 / 自然样本仍未执行，不作为本切片完成证据。
```

## 1. 依据文件

```text
AGENTS.md
docs/current-state.md
docs/decision-log.md
README.md
规范/规范目录.md
规范/0050_项目通用结构与禁止性规则总纲_20260701.md
规范/详细设计/待完成/20260703_四方法共享底层关联判断能力详细设计_v0.1.md
计划/计划索引.md
计划/20260703_四方法共享底层关联判断能力实现映射计划_v0.1.md
```

## 2. 已运行命令

```powershell
git -c core.quotepath=false status --short --untracked-files=no
Get-Content -LiteralPath '计划\计划索引.md' | Select-Object -First 160
Get-Content -LiteralPath '规范\规范目录.md' | Select-Object -First 160
Get-Content -LiteralPath '规范\0050_项目通用结构与禁止性规则总纲_20260701.md' | Select-Object -First 160
Get-Content -LiteralPath '计划\20260703_四方法共享底层关联判断能力实现映射计划_v0.1.md' | Select-Object -First 220
Get-Content -LiteralPath '规范\详细设计\待完成\20260703_四方法共享底层关联判断能力详细设计_v0.1.md' | Select-Object -First 220
rg -n "从扫描提交包提取合法外设材料|融合合法外设材料|计算先验一致性评分|计算局部体素融合前置状态|收集D9评价缺口材料|挂接局部体素证据到观察候选|读取体素加工候选结果包集合|生成子轮廓候选集合|选择局部体素证据子候选|计算局部体素完整性评分Q10000|检查融合点" 视觉观察融合模块.ixx 说明书\函数功能说明文档.md
rg -n "计算观察帧清晰度评分|计算观察候选清晰度评分|计算观察候选稳定复现评分|外设提交缺口_结构化|外设观察先验发布缺口_结构化|目标观察约束|像素归属账本|只读回查像素观察归属图材料页" 外设观察报告队列.ixx 说明书\函数功能说明文档.md
rg -n "场景体素缺口_结构化|同步当前场景快照|查询AABB|统计AABB|射线粗查询|读取场景体素维护线程最新快照|SceneVoxelPrior|场景体素先验" 场景体素模块.ixx 说明书\函数功能说明文档.md
rg -n "任务管理派生需求消息必备槽完整|填充任务管理派生需求消息结构化缺口槽|结构化缺口|派生需求|建议后续方法|方法类型" 任务模块.工作线程消息协议.ixx 任务模块.管理工作线程.ixx 说明书\函数功能说明文档.md
rg -n "识别外设观察材料|扫描当前场景|跟踪指定存在|观察单个空间候选|持续观察当前场景|确认外设观察存在候选|验证外设观察像素簇候选" 本能方法类.h 自我动作实现模块.ixx 自我动作实现.外设模块.ixx 自我动作实现.扫描.ixx 说明书\函数功能说明文档.md
rg -n "\b(bool|const bool)\b.*(可用|可提交|唯一|归属|需复验|可复验|成立|确认|稳定|清晰|成功|可进入|可交付|可强匹配|可降级匹配)" 视觉观察融合模块.ixx 外设观察报告队列.ixx 场景体素模块.ixx 自我动作实现.外设模块.ixx 自我动作实现.扫描.ixx
```

## 3. S0 扫描结论

### 3.1 当前主动队列

`计划/计划索引.md` 当前第一项未闭合为：

```text
E9 / P9 `计划/20260703_四方法共享底层关联判断能力实现映射计划_v0.1.md`
状态：实现映射计划 / 待 S0 只读扫描
下一步：确认视觉融合模块、外设观察报告队列、场景体素模块、四方法入口、D5 缺口链和 D9 评价结果读取路径
```

### 3.2 可复用共享能力锚点

视觉融合模块已有第一版共享材料能力：

```text
视觉观察融合模块.ixx:1340 从扫描提交包提取合法外设材料(...)
视觉观察融合模块.ixx:1378 融合观察域结果(...)
视觉观察融合模块.ixx:1569 计算先验一致性评分(...)
视觉观察融合模块.ixx:1672 计算局部体素融合前置状态(...)
视觉观察融合模块.ixx:1202 收集D9评价缺口材料(...)
视觉观察融合模块.ixx:2124 读取体素加工候选结果包集合()
视觉观察融合模块.ixx:2130 挂接局部体素证据到观察候选(...)
视觉观察融合模块.ixx:883 detail::生成子轮廓候选集合(...)
视觉观察融合模块.ixx:681 detail::选择局部体素证据子候选(...)
视觉观察融合模块.ixx:653 detail::计算局部体素完整性评分Q10000(...)
```

这些入口输出候选、评分、计数、缺口和运行期证据，不是第五个本能方法。

### 3.3 外设队列 / 目标约束 / 像素归属材料

外设观察报告队列已有可复用输入材料：

```text
外设观察报告队列.ixx:4091 读取像素观察归属图材料页(...)
外设观察报告队列.ixx:4412 判断目标观察约束可用性(...)
外设观察报告队列.ixx:4512 读取目标观察约束_按等待项(...)
外设观察报告队列.ixx:4535 读取目标观察约束_按目标特征(...)
外设观察报告队列.ixx:4554 选择最新可用目标观察约束(...)
外设观察报告队列.ixx:4581 读取可用目标观察约束集合_按目标特征(...)
外设观察报告队列.ixx:4760 读取最新外设处理先验_按目标(...)
外设观察报告队列.ixx:2986/3077 计算观察帧清晰度评分(...)
外设观察报告队列.ixx:3086 计算观察候选清晰度评分(...)
外设观察报告队列.ixx:3188 计算观察候选稳定复现评分(...)
```

外设提交缺口和外设观察先验发布缺口已有 D5 映射入口：

```text
外设观察报告队列.ixx:3274 外设提交缺口_结构化缺口大类(...)
外设观察报告队列.ixx:3297 外设提交缺口_结构化阻断阶段(...)
外设观察报告队列.ixx:3352 外设观察先验发布缺口_结构化缺口大类(...)
外设观察报告队列.ixx:3374 外设观察先验发布缺口_结构化阻断阶段(...)
```

### 3.4 场景体素只读先验材料

场景体素模块已有只读先验和查询能力：

```text
场景体素模块.ixx:810 场景体素服务类::同步当前场景快照(...)
场景体素模块.ixx:1137/1155 查询AABB(...)
场景体素模块.ixx:1163/1181 统计AABB(...)
场景体素模块.ixx:1218/1287 射线粗查询(...)
场景体素模块.ixx:1565 读取场景体素维护线程最新快照()
场景体素模块.ixx:732 场景体素缺口_结构化缺口大类(...)
场景体素模块.ixx:750 场景体素缺口_结构化阻断阶段(...)
```

### 3.5 四方法真实入口

本轮按当前代码定位到的相关本能入口：

```text
观察侧：
  自我动作实现.外设模块.ixx:30179 自我观察单个空间候选并组合存在假设(...)
  自我动作实现.外设模块.ixx:25498 自我验证外设观察像素簇候选(...)
  自我动作实现.外设模块.ixx:25929 自我确认外设观察存在候选(...)

识别：
  自我动作实现.外设模块.ixx:24213 自我识别外设观察材料(...)

扫描：
  自我动作实现.扫描.ixx:2650 自我扫描当前场景(...)

跟踪：
  自我动作实现.外设模块.ixx:24858 自我跟踪指定存在(...)
```

包装和注册路径：

```text
本能方法类.h:83-85 登记 自我_识别外设观察材料 / 自我_扫描当前场景 / 自我_跟踪指定存在
自我动作实现模块.ixx:5432-5452 内部函数包装
自我动作实现模块.ixx:11322-11372 总模块转发
自我动作实现.外部模块.ixx:800-841 外部模块转发
自我动作实现模块.ixx:8208-8219 规格确认分派
```

### 3.6 当前共享读取路径

扫描已经直接消费视觉融合共享材料：

```text
自我动作实现.扫描.ixx:2769 从扫描提交包提取合法外设材料(...)
自我动作实现.扫描.ixx:2774 融合观察域结果(...)
```

识别和跟踪已读取 P11/D6/D7/D9 形成的共享摘要特征：

```text
识别读取场景体素先验版本、坐标系版本、可用状态、视觉先验匹配数量 / 评分、深度一致评分、自由空间冲突候选、视觉信息缺口、遮挡、深度缺失、证据不足等 I64 特征。
跟踪读取同类先验和视觉证据特征，并收束为跟踪视觉先验匹配状态、跟踪先验预计范围状态、跟踪丢失解释状态。
观察侧通过 `被动识别外设观察材料_内部治理(...)` 使用内部治理结果，不调用公开识别本能入口。
```

### 3.7 互调风险

当前没有看到四方法公开本能入口之间直接互调：

```text
扫描入口直接调用视觉融合模块，不调用观察 / 识别 / 跟踪入口。
识别入口读取观察摘要、报告、先验和确认方案，不调用扫描 / 跟踪 / 观察本能入口。
跟踪入口读取目标存在、报告、基准、先验和轮廓比较，不调用扫描 / 识别 / 观察本能入口。
观察入口调用 `被动识别外设观察材料_内部治理(...)`，这是内部共享治理函数，不是 `自我识别外设观察材料(...)` 本能入口。
```

保留风险：

```text
`被动识别外设观察材料_内部治理(...)` 名称仍带“识别”，S1/S2 冻结共享能力时应明确其身份是底层治理 / 共享材料处理，而不是观察调用识别本能。
```

### 3.8 普通 bool 风险

已发现两类 bool：

```text
1. 局部计算 / 写入成功 / 条件闸门 bool：
   例如 `观察前置证据达标`、`识别确认方案已生成`、`跟踪前置成立`、`场景体素先验可用于跟踪`、`清晰度合格`、`稳定合格`。
   当前用途主要是函数局部流程控制，最终输出大多落到 I64 状态、评分、数量或缺口。

2. 兼容运行期协议位 bool：
   外设观察报告队列.ixx:759 `允许降级`
   外设观察报告队列.ixx:769 `可强匹配`
   外设观察报告队列.ixx:770 `可降级匹配`
   文件注释已标明新读取方不得作为机器判断依据，正式判断应读取 `枚举_目标观察约束可用性` 等状态值域。
```

S1/S2 风险要求：

```text
新共享能力不得新增 `候选可用=true`、`唯一归属成立=true`、`可提交=true` 等普通一阶事实。
若需输出判断，落为 I64 状态 / 评分 / 数量 / 掩码 / 缺口材料 / 可回查句柄。
```

### 3.9 缺口链可用性

已有 D5 前置材料：

```text
视觉信息缺口_结构化缺口大类 / 阻断阶段
D9评价缺口_结构化缺口大类 / 阻断阶段 / 标准化D9评价缺口材料 / 收集D9评价缺口材料
外设提交缺口_结构化缺口大类 / 阻断阶段
外设观察先验发布缺口_结构化缺口大类 / 阻断阶段
场景体素缺口_结构化缺口大类 / 阻断阶段
任务管理派生需求消息必备槽完整(...)
填充任务管理派生需求消息结构化缺口槽(...)
```

尚未看到 E9 专属共享能力缺口结构：

```text
关联判断结果类型
关联判断来源类型
建议后续方法类型
共享能力缺口到 D5 的专用适配入口
```

这些应由 S1 先冻结清单和值域落点，再由后续代码切片接入。

## 4. 当前判断

S0 已满足：

```text
真实函数名已确认；
已有共享能力和材料链已确认；
四方法入口和包装 / 注册路径已确认；
未发现公开本能方法直接互调；
普通 bool 风险已分为局部流程控制和兼容协议位；
D5 / D9 / 外设 / 场景体素缺口映射入口已确认；
E9 专属共享能力值域和缺口适配入口仍缺，进入 S1。
```

S1 已满足：

```text
`视觉观察融合模块.ixx` 已登记 `枚举_共享关联判断结果类型`：
0 未评估，1 候选匹配，2 候选不唯一，3 证据不足，4 硬冲突，5 遮挡候选，6 视野外候选，7 深度缺失，8 自由空间冲突，9 材料不可比较，10 先验不可用，11 需复验，12 可交给目标方法承接。

`视觉观察融合模块.ixx` 已登记 `枚举_共享关联判断来源类型`：
0 未定义，1 像素归属，2 候选过滤，3 特征匹配，4 反证检查，5 范围内对应计算，6 局部体素候选生成，7 场景体素先验快照匹配，8 目标观察约束解释。

`视觉观察融合模块.ixx` 已登记 `枚举_共享关联建议后续方法类型`：
0 无建议，1 补观察，2 识别，3 跟踪，4 扫描，5 体素融合，6 场景体素刷新，7 材料质量收束，8 目标观察约束重建。
```

S1 边界：

```text
三组值域只作为运行期 I64 / 枚举兼容值；
不登记第五个本能方法；
不写世界树事实、稳定存在体素、场景体素缓存、需求满足或动作动态；
不直接派生需求入树。
```

S2 已满足：

```text
`视觉观察融合模块.ixx` 文件头部规则注释模块已明确：
1. 本模块只把合法外设材料和 SceneVoxelPrior 融合为观察域候选、评分、解释和缺口。
2. 本模块不得创建存在、确认消失、写一阶特征、写需求满足、写价值结算或生成动作动态。
3. 本模块作为四方法共享底层判定承载时，不得直接派生需求，不得调用观察 / 扫描 / 识别 / 跟踪公开本能入口。
```

S2 边界：

```text
本切片只补人读文件头契约，不新增机器判断字段；
注释不作为业务状态、验收证据或机器逻辑承载；
未新增第五个本能方法；
未新增长期业务结构体。
```

S3 已满足：

```text
`外设观察报告队列.ixx` 新增 `结构_共享关联外设材料输入`，统一汇总：
来源报告 / 帧 / 外设 / 坐标系；
空间候选队列句柄、像素归属账本句柄；
感兴趣区域、点集、空间候选、彩色轮廓局部图、深度轮廓局部图句柄集合；
目标观察约束 ROI / 掩码 / 点集 / 二维范围 / 空间范围 / 质量阈值；
目标观察约束可用性状态、强匹配条件状态、降级匹配条件状态、先验可用性收束状态；
材料质量收束状态、可承接能力等级和外设提交缺口集合。

`构造共享关联外设材料输入_由观察报告(...)` 从已有报告项和可选等待项构造只读输入包。
`读取共享关联外设材料输入_按报告ID(...)` 从外设队列按报告 ID 读取后构造只读输入包。
```

S3 边界：

```text
不新增可归属 / 可提交 / 可识别 / 遮挡成立等普通布尔事实；
不写世界树事实、需求满足、动作动态或报告消费状态；
不直接派生需求；
目标观察约束兼容 bool 只由既有判定函数内部使用，新输入包只输出 I64 状态。
```

## 5. 验证计划

S0 是只读扫描 + 实施记录切片，已完成。
S1 涉及代码枚举值域，已运行函数说明生成、diff check、check_specs 和 Debug x64 构建。
S2 只补文件头部规则注释模块，不新增 / 删除 / 改名函数，不重新生成函数说明文档，不运行 Debug x64 构建。
S3 新增函数，需要运行函数说明生成、diff check、check_specs 和 Debug x64 构建。

已运行：

```powershell
git diff --check -- 实施记录/20260704_四方法共享底层关联判断能力_Codex断点清单.md
python .\tools\生成函数功能说明文档.py
git diff --check -- 视觉观察融合模块.ixx 实施记录/20260704_四方法共享底层关联判断能力_Codex断点清单.md
python .\tools\check_specs.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
git diff --check -- 视觉观察融合模块.ixx 实施记录/20260704_四方法共享底层关联判断能力_Codex断点清单.md
python .\tools\check_specs.py
python .\tools\生成函数功能说明文档.py
git diff --check -- 外设观察报告队列.ixx 实施记录/20260704_四方法共享底层关联判断能力_Codex断点清单.md 说明书/函数功能说明文档.md
python .\tools\check_specs.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
S1:
python .\tools\生成函数功能说明文档.py：退出码 0。
git diff --check：退出码 0。
python .\tools\check_specs.py：hard issues = 0，warnings = 0。
Debug x64 构建：0 警告，0 错误。

S2:
git diff --check：退出码 0。
python .\tools\check_specs.py：hard issues = 0，warnings = 0。

S3:
python .\tools\生成函数功能说明文档.py：退出码 0。
git diff --check：退出码 0。
python .\tools\check_specs.py：hard issues = 0，warnings = 0。
Debug x64 构建：0 警告，0 错误。
```

## 6. 已改文件

```text
视觉观察融合模块.ixx
外设观察报告队列.ixx
实施记录/20260704_四方法共享底层关联判断能力_Codex断点清单.md
说明书/函数功能说明文档.md
```
