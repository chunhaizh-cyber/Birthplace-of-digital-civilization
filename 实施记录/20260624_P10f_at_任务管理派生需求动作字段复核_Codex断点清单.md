# P10f-at 任务管理派生需求动作字段复核 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-at `任务模块.工作线程消息协议.ixx::结构_任务管理派生需求消息` 触发动作字段复核。

依据文件：

- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `计划/计划索引.md`
- `实施记录/20260624_P10f_as_线程状态消息文本字段复核_Codex断点清单.md`
- `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_thread_state_message_fields.tsv`

复核字段：

- `结构_任务管理派生需求消息::触发动作主体主键`
- `结构_任务管理派生需求消息::触发动作语义键`

当前证据：

- `触发动作语义键` 在 `自我线程模块.消息协议.ixx::从任务管理派生需求消息构造治理消息(...)` 中写入 `治理消息.语义.动作语义键`，属于机器语义入口。
- `触发动作主体主键` 与同结构内 `当前方法主键 / 方法虚拟存在主键 / 来源动态主键 / 来源方法主键` 等文本主键字段同属跨线程协议旧文本主键槽；本切片先确认是否有对应指针 / 数值槽可直接替换。

下一步：

- 复核 `任务模块.工作线程消息协议.ixx`、`任务模块.管理工作线程.ixx`、`任务模块.筹办.ixx`、`自我线程模块.消息协议.ixx` 中这两个字段的写入、补齐、稳定键、日志和落账使用。
- 若字段进入动作主体、动作语义、稳定键、去重或因果匹配，改为主键数值、枚举、语素入口或特征承载。

处理结论：

- 已删除 `结构_任务管理派生需求消息::触发动作主体主键 / 触发动作语义键`。
- `任务模块.工作线程消息协议.ixx`、`任务模块.管理工作线程.ixx`、`任务模块.筹办.ixx` 中未发现这两个字段参与完整性判断、稳定键、派生需求构造或日志文本。
- `自我线程模块.消息协议.ixx::从任务管理派生需求消息构造治理消息(...)` 不再从 `触发动作语义键` 覆盖 `治理消息.语义.动作语义键`；治理动作类型由 `枚举_动作类型::派生需求上报` 承载。

验证结果：

- `rg -n "触发动作主体主键|触发动作语义键|任务管理派生需求上报" 任务模块.工作线程消息协议.ixx 自我线程模块.消息协议.ixx 任务模块.管理工作线程.ixx 任务模块.筹办.ixx 自我线程模块.impl.cpp`：无命中。
- `git diff --check` 通过。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`：成功，0 警告，0 错误。
- 已生成 `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_task_derived_action_fields.tsv`：基于上一版权威 TSV 差分过滤并用 live `rg` 复核目标字段已不存在，剩余 568 行，其中主键 / ID / 键 / 句柄类 332 行、非主键类 236 行。

下一步：

- 更新计划索引，继续按最新 TSV 处理剩余字段；优先复核仍进入动作语义、稳定键、查找、等待 / 结算状态或因果匹配的 string 字段。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
