# P10f-h 二次特征写入请求概念文本复核 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-h `二次特征应用模块.ixx::结构_场景二次特征写入请求` 概念名称 / 词性字段边界复核。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `规范/二次特征规范20260520.md`
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_causal_structured.tsv`

执行口径：

- 二次特征底层方向必须来自有符号比较量或枚举 / 数值方向，不得由自然语言词决定。
- `概念入口` 若已经提供内部身份结构，后续机器逻辑应优先用 `语素入口节点类*` 或二次特征枚举字段。
- `概念名称 / 概念词性` 若只用于首次注册语素入口的名称种子，必须立即转成 `概念入口` 后参与后续逻辑；不得在查找、生成、匹配、稳定键或判断中继续使用文本。

当前候选：

- `二次特征应用模块.ixx:390` `结构_场景二次特征写入请求::概念名称`
- `二次特征应用模块.ixx:392` `结构_场景二次特征写入请求::概念词性`

预测：

- 若文本只作为语素入口注册种子，并在请求构造后统一落到 `概念入口`，可保留或局部收窄。
- 若写入流程用 `概念名称 / 概念词性` 直接决定二次特征类型或生成结果，需要改为 `概念入口`、枚举值、具体特征类型和方向。

## 本轮问题记录

问题锚点：`二次特征应用模块.ixx::结构_场景二次特征写入请求`。

证据行：

- `概念词性` 只有字段定义和赋值，未被读取。
- `概念名称` 只作为 `写入场景二次特征` 内部注册 `概念入口` 的文本种子；同一结构已经有 `const 语素入口节点类* 概念入口`。

当前判断：

- `概念词性` 符合“只有定义和赋值”的直接删除条件。
- `概念名称` 应在请求构造处立即转换为 `概念入口`，写入流程只消费内部身份结构。

下一步：

- 删除 `结构_场景二次特征写入请求::概念名称 / 概念词性`。
- 将所有请求构造处的概念文本赋值改为 `请求.概念入口 = 私有_取概念名称词(...)`。
- `写入场景二次特征` 只读取 `请求.概念入口`。

已处理：

- 删除 `结构_场景二次特征写入请求::概念名称 / 概念词性`。
- `二次特征应用模块.ixx` 内所有写入请求构造处已改为直接设置 `概念入口`。
- `写入场景二次特征` 只读取 `请求.概念入口`，不再在写入流程中读取文本字段后注册语素。
- `世界树类.cpp::写入治理二次特征` 已在跨模块入口处把外部 `概念名称` 注册为关系模板语素入口，再写入 `请求.概念入口`。

验证记录：

- 第一次 `Debug|x64` 构建失败：`世界树类.cpp:2418-2419` 仍给已删除的 `概念名称 / 概念词性` 赋值。
- 已修复 `世界树类.cpp::写入治理二次特征` 漏改点。
- `rg -n "请求\.概念名称|请求\.概念词性|概念词性|std::string 概念名称|std::string 概念词性" -g '*.cpp' -g '*.ixx' -g '*.h'`：二次特征写入请求链无残留；全仓剩余 `自我线程模块.impl.cpp::结构_治理二次特征候选::概念名称` 属于下一切片候选。
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_causal_structured.tsv` 已刷新：剩余 634 行，主键类 314 行，非主键类 320 行。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo`：成功，0 警告，0 错误。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
