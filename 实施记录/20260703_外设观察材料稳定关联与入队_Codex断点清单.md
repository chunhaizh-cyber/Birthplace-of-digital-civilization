# 20260703 外设观察材料稳定关联与入队 Codex断点清单

## 0. 恢复摘要

当前切片：计划索引 E1/P3 `外设观察材料稳定关联与入队实现映射计划`，执行 S0 代码事实扫描。

依据文件：

```text
AGENTS.md
docs/current-state.md
docs/decision-log.md
README.md
docs/00_文档总索引.md
docs/工程图谱/00_图谱总索引.md
docs/工程图谱/02_外设观察事实图谱.md
docs/工程图谱/05_规则原子索引.md
规范/规范目录.md
规范/0050_项目通用结构与禁止性规则总纲_20260701.md
规范/1175_根规范_特征表达与判断收束规则_20260703.md
规范/6200_子规范_基础观察事实可用与风险判断分层_20260701.md
规范/6300_子规范_观察像素簇与存在候选分层_20260701.md
规范/详细设计/待完成/20260703_外设观察材料稳定关联与入队详细设计_v0.1.md
计划/计划索引.md
计划/20260703_外设观察材料稳定关联与入队实现映射计划_v0.1.md
```

已改文件：

```text
实施记录/20260703_外设观察材料稳定关联与入队_Codex断点清单.md
```

已运行命令：

```text
git status --short --untracked-files=no
```

关键退出码：

```text
git status: 0
```

当前判断：

```text
计划索引当前主动队列指向 E1/P3，状态为实现映射计划 / 待 S0 只读扫描。
S0 只允许确认真实入口、字段类型、调用方、缺入口和新增结构体 / 普通 bool 风险。
本断点写入时尚未修改代码，尚未运行构建或真实 D455 验证。
```

下一步：

```text
只读扫描 外设观察报告队列.ixx、外设线程_D455深度相机.ixx、双目相机本能适配器.h、双目相机本能适配器.cpp、说明书/函数功能说明文档.md。
输出 S0 扫描清单后判断 S1 是否具备实施条件。
```

禁止宣称：

```text
不宣称 D455 材料稳定入队、来源链、幂等、TTL 或工程性能测量已代码实现。
不宣称外设材料已写成世界树事实、观察质量已收束、稳定存在已确认或真实 D455 样本已验证。
```

## 1. S0 代码事实扫描结果

扫描范围：

```text
外设观察报告队列.ixx
外设线程_D455深度相机.ixx
双目相机本能适配器.h
双目相机本能适配器.cpp
说明书/函数功能说明文档.md
```

真实入口：

```text
L0 逐簇识别报告生成：外设线程_D455深度相机.ixx:1468 D455_从调用结果生成逐簇识别报告
L1 稳定观察子集生成：外设线程_D455深度相机.ixx:1960 D455_从逐簇识别报告生成稳定观察子集报告_v2
本帧 L0/L1 发布编排：外设线程_D455深度相机.ixx:3210 D455_发布本帧逐簇与稳定子集报告
扫描变化报告发布：外设线程_D455深度相机.ixx:3296 D455_发布扫描变化报告
目标跟踪报告发布：外设线程_D455深度相机.ixx:3367 D455_发布目标跟踪报告
外设报告提交：外设观察报告队列.ixx:2666 提交外设观察报告
D455 短期材料页提交：外设观察报告队列.ixx:2687 提交D455短期观察材料页
D455 材料句柄解析：外设观察报告队列.ixx:2740 解析D455观察材料句柄
消费标记：外设观察报告队列.ixx:3529 标记外设观察报告已消费
队列状态读取：外设观察报告队列.ixx:3574 读取外设观察队列状态
D455 线程结果读取：外设线程_D455深度相机.ixx:4192 读取外设线程_D455深度相机结果
```

字段和承载：

```text
报告项已有 报告ID、匹配等待项ID、来源外设ID、报告类型、时间戳、帧宽高、深度帧号、彩色帧号、坐标系ID、报告层级、来源原始报告ID、来源外设帧ID、目标观察约束ID、消费等待项ID、消费时间毫秒。
短期材料页已有 报告ID、创建时间毫秒、过期时间毫秒、来源调用结果、报告副本。
队列共享状态已有 报告队列、D455材料页队列、等待项集合、目标观察约束缓冲区、队列容量、D455材料页容量、D455材料页保留毫秒。
工程性能材料已有线程结果中的观察报告采集尝试帧数、采集成功帧数、入队帧数、入队统计帧数、入队窗口毫秒、入队FPSx100。
```

关键缺口：

```text
1. 来源材料 generation 当前未达到 D1/S1 的结构化承载条件。
   证据：结构_外设观察报告队列项 只有 std::string 约束generation、目标特征generation、来源材料generation；结构_外设提交包头 也只有 std::string generation。
   证据：外设提交_generation 只把 报告生成方法ID + "#" + 报告生成方法版本ID 拼成字符串，缺来源外设、报告类型、报告层级、深度帧号、彩色帧号、采集时间、坐标版本、来源原始报告和目标约束 generation。
2. 报告入队尚未做同源幂等。
   证据：提交外设观察报告 只补 报告ID / 时间戳 后 push_back；未按 generation、来源外设帧、报告层级、等待项或目标约束判断复用 / 替换。
3. 材料页幂等只按 报告ID。
   证据：提交D455短期观察材料页 只在 已有.报告ID == 报告ID 时替换；未按同 generation 或来源帧组复用。
4. 普通 bool 风险已存在但本计划不做全量 1175 重构。
   证据：报告项有 bool 采集成功、bool 已消费；等待项有 bool 是否允许缓存报告；适配器调用结果也有 bool 成功、相机已打开。
```

S0 结算：

```text
S0 已完成，只读扫描清单已形成。
S1 generation 与 L0/L1 来源链最小切片当前不可直接执行。
阻塞类型：缺承载契约 / 缺计划前置条件。
恢复条件：先修订 D1 详细设计和 P3 实现映射计划，明确第一版 generation 是否允许作为现有字符串 opaque identity 的过渡、是否改用已有 I64 / VecI64 / VecIU64 只读计算结果、以及不新增结构体 / 普通 bool 字段时如何落入现有报告项。
```

已运行命令增量：

```text
rg -n ... 外设观察报告队列.ixx
rg -n ... 外设线程_D455深度相机.ixx
rg -n ... 双目相机本能适配器.h 双目相机本能适配器.cpp
rg -n ... 说明书/函数功能说明文档.md
```

下一步更新：

```text
回写 计划/计划索引.md 和 docs/current-state.md：E1/P3 S0 已完成，S1 因 generation 承载不满足实施条件转为明确堵点。
本轮不修改代码，不运行构建，不运行真实 D455 验证。
```

## 2. S2b 恢复扫描与独立问题记录

恢复依据：

```text
计划/计划索引.md：E1/P3 下一步为从 S2b 检查 L2/L3 是否具备正式来源链。
计划/20260703_外设观察材料稳定关联与入队实现映射计划_v0.1.md：S2a 已由 E0/P13 完成，S2b 目标是 L2/L3 不脱离 L1 或目标约束来源链。
规范/详细设计/待完成/20260703_外设观察材料稳定关联与入队详细设计_v0.1.md：D455-LAYER-003 要求 L2/L3 继承 L1 来源原始报告编号、来源外设帧编号、来源报告时间戳和坐标系版本，缺失时不得进入扫描 / 跟踪正式承接。
```

已确认闭合点：

```text
外设线程_D455深度相机.ixx:1679 D455_L1来源链完整 要求来源原始报告ID、来源外设帧ID、时间戳毫秒和坐标系ID齐全。
外设线程_D455深度相机.ixx:1966 D455_复制观察报告帧摘要 会复制时间戳毫秒、坐标系ID、来源原始报告ID和来源外设帧ID。
外设线程_D455深度相机.ixx:2341 D455_从逐簇识别报告生成扫描变化报告_带上下文 会从输入 L1 复制来源链后写报告层级 2。
外设线程_D455深度相机.ixx:2522 D455_从逐簇识别报告生成跟踪报告 会从输入 L1 复制来源链后写报告层级 3。
外设线程_D455深度相机.ixx:3277 D455_发布扫描变化报告 在正式提交 L2 前检查 L1 来源链。
外设线程_D455深度相机.ixx:3371 D455_发布目标跟踪报告 在正式提交 L3 前检查 L1 来源链。
```

独立问题：

```text
外设线程_D455深度相机.ixx:3957 提交D455单帧材料包证据卡 属于单帧诊断 / 证据卡路径。
该路径从 L0 逐簇识别报告直接调用 D455_从逐簇识别报告生成扫描变化报告_带上下文，并在外设线程_D455深度相机.ixx:3990 调用 提交外设观察报告。
由于单帧 L0 报告不是 L1 稳定子集，来源原始报告ID / 来源外设帧ID 可能为空，形成“诊断路径正式提交 L2”的旁路。
当前判断：该旁路违反 S2b 的“L2/L3 不脱离 L1 或目标约束来源链”。应最小修改为：单帧证据卡可以输出诊断摘要，但缺 L1 来源链时不得正式提交扫描变化报告。
```

下一步：

```text
仅修改 外设线程_D455深度相机.ixx，移除单帧证据卡路径中的无来源 L2 正式入队。
随后运行 git diff --check、check_specs.py、Debug x64 构建。
```

## 3. S2b 实施结果

已改文件：

```text
外设线程_D455深度相机.ixx
计划/20260703_外设观察材料稳定关联与入队实现映射计划_v0.1.md
计划/计划索引.md
docs/current-state.md
实施记录/20260703_外设观察材料稳定关联与入队_Codex断点清单.md
```

代码结果：

```text
正式扫描变化报告发布入口仍在 D455_L1来源链完整 检查之后提交 L2。
正式目标跟踪报告发布入口仍在 D455_L1来源链完整 检查之后提交 L3。
提交D455单帧材料包证据卡 不再从单帧 L0 诊断路径直接正式提交 L2；缺 L1 来源链时只输出扫描样本卡诊断摘要，并返回 扫描变化正式入队=0。
```

已运行命令：

```text
rg -n "D455_从逐簇识别报告生成扫描变化报告_带上下文\(|提交外设观察报告\(扫描变化报告\)|提交外设观察报告\(跟踪报告\)|D455_L1来源链完整\(" 外设线程_D455深度相机.ixx
rg -n "generation|材料组代际身份" 外设线程_D455深度相机.ixx 外设观察报告队列.ixx 说明书/函数功能说明文档.md
git diff --check
python .\tools\check_specs.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

关键退出码：

```text
rg 来源链锚点：0
rg generation / 材料组代际身份：1，无命中
git diff --check：0
check_specs.py：0，hard issues = 0，warnings = 0
msbuild Debug x64：0，0 警告，0 错误
```

真实运行状态：

```text
未运行真实 D455 队列烟测；本切片只完成静态路径核查和 Debug x64 构建验证。
```

下一步：

```text
计划索引 E1/P3 已回写为 S2b 已闭合，下一步进入 S3 报告一次性消费。
连续执行时从 S3 读取正式读取路径、消费标记和 D455 跨帧内部历史读取例外。
```

禁止宣称：

```text
不宣称 D455 材料稳定入队完整闭合。
不宣称材料页一次性消费已完成。
不宣称真实 D455 队列烟测已执行。
不宣称工程性能测量已完成。
```

## 4. S3 报告一次性消费证据闭合

当前切片：

```text
计划索引 E1/P3 `外设观察材料稳定关联与入队实现映射计划`，执行 S3 报告一次性消费。
```

代码判断：

```text
外设观察报告队列.ixx:3288 读取最新外设观察报告 默认转入 包含已消费=false。
外设观察报告队列.ixx:3296 读取最新外设观察报告 在锚点选择和近组返回阶段均跳过已消费报告。
外设观察报告队列.ixx:3378 读取外设观察报告队列视图 在 包含已消费=false 时跳过已消费报告。
外设观察报告队列.ixx:3452 按等待项匹配外设观察报告 在等待项直绑、近组锚点和近组返回阶段均跳过已消费报告。
外设观察报告队列.ixx:3516 标记外设观察报告已消费 写入 已消费、消费等待项ID 和 消费时间毫秒。
任务模块.管理工作线程.ixx:3563 任务管理外设消息承接生成上行请求后调用 标记外设观察报告已消费。
外设线程_D455深度相机.ixx:1690 D455 跨帧内部历史上下文显式调用 读取最新外设观察报告(..., true)，该路径是外设内部历史回查例外，不作为新业务输入。
```

等待项和目标约束边界：

```text
外设观察报告队列.ixx:1733 外设观察_报告满足等待项 检查目标外设、观察运行模式、报告类型、等待项ID、目标观察约束ID、L1 层级和最大允许报告年龄。
不同等待项或不同目标约束不会被同一读取结果吞并。
```

已运行命令：

```text
rg -n "读取最新外设观察报告|读取外设观察报告_按ID|标记外设观察报告已消费|已消费|包含已消费|读取最新L1稳定子集报告|上一逐簇识别报告|上一稳定子集报告" 外设观察报告队列.ixx 外设线程_D455深度相机.ixx 自我动作实现.外设模块.ixx 自我动作实现.扫描.ixx 任务模块.管理工作线程.ixx
rg -n "外设观察_报告满足等待项|目标观察约束ID|匹配等待项ID|最大允许报告年龄|允许缓存|报告层级|已消费" 外设观察报告队列.ixx
git diff --check
python .\tools\check_specs.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

关键退出码：

```text
rg 消费入口锚点：0
rg 等待项匹配锚点：0
git diff --check：0
check_specs.py：0，hard issues = 0，warnings = 0
msbuild Debug x64：0，0 警告，0 错误
```

S3 结算：

```text
S3 已闭合；本切片未修改代码，只登记现有代码证据和计划索引状态。
下一步进入 S4 材料页一次性消费和过期清理。
```

禁止宣称：

```text
不宣称短期材料页一次性消费已完成。
不宣称真实 D455 队列烟测已执行。
不宣称 D1 完整闭合。
```

## 5. S4 材料页 TTL 回查和过期清理证据闭合

当前切片：

```text
计划索引 E1/P3 `外设观察材料稳定关联与入队实现映射计划`，执行 S4 材料页一次性消费和过期清理。
```

代码判断：

```text
外设观察报告队列.ixx:805 结构_D455短期观察材料页 是内部短期页，未导出独立业务消费入口。
外设观察报告队列.ixx:823 D455材料页队列 只在外设观察报告队列模块内部维护。
外设观察报告队列.ixx:1173 D455_清理短期材料页_已加锁 按过期时间和容量 pop_front。
外设观察报告队列.ixx:2655 提交D455短期观察材料页 按报告ID提交 / 替换材料页，并设置创建时间和过期时间。
外设观察报告队列.ixx:2708 解析D455观察材料句柄 在解析前清理过期材料页；命中过期页时返回 已过期，不伪造成功。
外设观察报告队列.ixx:3539 清理外设观察报告队列 统计报告、材料页和视频帧清理数量。
```

边界结论：

```text
材料页不是独立业务可消费队列，外部只通过句柄解析和三维体素轮廓视角读取做 TTL 内回查。
新业务输入是否可重复消费由报告队列的 已消费 门控制；材料页保留用于已承接链路的多句柄回查。
不能把 解析D455观察材料句柄 改成读一次即删除，否则同一报告的原始帧、深度帧、空间候选、像素归属账本、掩码、ROI 和点集连续回查会被破坏。
本切片不新增材料页消费 bool，不新增业务结构体。
```

已运行命令：

```text
rg -n "D455短期观察材料页|提交D455短期观察材料页|解析D455观察材料句柄|D455_清理短期材料页|材料页|过期|消费|移除|读取.*材料页" 外设观察报告队列.ixx 外设线程_D455深度相机.ixx 自我动作实现.外设模块.ixx 自我动作实现.扫描.ixx
rg -n "export .*D455|export .*材料页|D455材料页队列|解析D455观察材料句柄|构造D455簇三维体素轮廓图视角|读取最新D455控制面板视频快照" 外设观察报告队列.ixx
rg -n "D455材料页队列|结构_D455短期观察材料页|解析D455观察材料句柄|构造D455簇三维体素轮廓图视角" --glob "*.ixx" --glob "*.cpp" --glob "*.h"
git diff --check
python .\tools\check_specs.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

关键退出码：

```text
rg 材料页入口锚点：0
rg 材料页导出 / 内部队列锚点：0
git diff --check：0
check_specs.py：0，hard issues = 0，warnings = 0
msbuild Debug x64：0，0 警告，0 错误
```

S4 结算：

```text
S4 已闭合；本切片未修改代码，只登记材料页 TTL 回查、过期清理和不独立承载新业务消费的证据。
下一步进入 S5 工程性能测量只读输出。
```

禁止宣称：

```text
不宣称真实 D455 队列烟测已执行。
不宣称工程性能测量已完成。
不宣称 D1 完整闭合。
```
