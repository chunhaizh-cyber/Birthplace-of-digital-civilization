# P12a 计划目录未完成项重排 Codex 断点清单

## 0. 恢复摘要

当前切片：P12a，梳理 `计划/` 根目录还未全部完成的计划，并重排下一阶段执行顺序。

依据文件：
- `规范/规范目录.md`
- `计划/计划索引.md`

已完成：
- 扫描 `计划/` 根目录 11 个计划文件。
- 确认 P10 非主键 string / 摘要 / 原因类字段机器判断复核已收束，不再作为当前主动计划。
- 将 P11 函数去重与无效代码清理排为下一阶段第一候选：先 live 无调用复扫，只处理未命中当前未提交脏改文件的 clean 候选。
- 将 P10 结构体 / string 清理降为触发式复开：只有新增 live 证据显示非主键文本进入机器决策时重开。
- 将需求 / 任务 / 状态 / 动态 / 特征 / 二次特征 / 存在 / 场景 / 因果类计划统一整理为新缺口触发式复开。
- 复核 `动态类::读取动态排序时间` 已在当前工作区实现并被场景类调用，已补写 `计划/20260620_动态类逻辑纠偏计划_v0.1.md` 的 P9 闭合记录。

未做：
- 未移动计划文件到 `计划/完成项/` 或 `计划/待完成项/`。
- 未修改代码。
- 未运行构建；本切片只整理计划索引和计划正文状态。

下一步：
- 用户继续要求执行时，优先进入 P11j live 无调用复扫；执行前先检查当前脏改文件，避免混入用户改动。

