# 需求主信息类型字段删除 Codex 断点清单

当前切片：删除 `需求主信息类::类型` 字段，改用目标宿主、目标特征类型、状态与结构角色表达需求语义。

依据文件：
- `规范/规范目录.md`
- `计划/计划索引.md`
- `规范/需求主信息类规范20260507.md`
- `规范/主信息字段与结构新增准入约束规范20260519.md`

已确认：
- 当前工作树进入切片前干净。
- `主信息.类型` 的核心引用集中在需求创建、根需求查找、任务默认命名、需求列表排序/显示、控制面板详情与刷新范围。

预期修改：
- 删除 `需求主信息类::类型`。
- 删除或停用 `结构_需求树更新指令::需求类型` 的需求主信息写入口径。
- 根需求查找改为按自我存在 + 目标特征类型识别。
- 任务默认命名改为按来源需求目标特征类型识别根任务，普通任务名回退到目标特征类型。
- 需求列表排序/汇总改为按目标特征类型。
- 控制面板不再展示需求类型字段，改显示目标特征类型/根需求显示名。

已运行命令：
- `git status --short --untracked-files=no`
- `rg` 引用盘点若干次
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
- `.\x64\Debug\鱼巢.exe --panel-html`
- `rg -n "需求列表项 \| 类型=|需求主信息.*类型|主信息\.类型|指令\.需求类型|需求树更新\.需求类型|const 语素入口节点类\* 类型" ...`
- `git diff --check`

关键退出码：
- 第一次构建失败：残留 `需求主信息类::类型` 比较与合并逻辑。
- 第二次构建失败：任务类重复 helper、控制面板 helper 前置声明缺失。
- 第三次构建成功：0 错误，1 个既有 LNK4075 链接警告。
- `--panel-html` 成功生成 `控制面板.html`。

关键证据：
- `需求主信息类::类型` 已删除。
- `结构_需求树更新指令::需求类型` 已删除。
- 安全 / 服务根需求查找改为按目标特征 `自我_安全值` / `自我_服务值`。
- 控制面板需求列表项已显示为 `需求列表项 | 主键=... | 子节点数量=... | 目标特征类型=... | 目标特征值=...`，不再显示 `类型=...`。
- 源码残余 `主信息.类型` 只属于任务主信息或特征主信息，不属于需求主信息。

当前判断：
- 本切片只删字段和替代读取逻辑，不宣称内部循环阶段完成。

下一步：
- 如后续继续清理，可把日志/变量名中的 `需求类型标题` 逐步改名为 `目标特征标题`，但当前不影响字段删除。
