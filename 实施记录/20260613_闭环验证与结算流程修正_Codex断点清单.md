# 闭环验证与结算流程修正 Codex 断点清单

更新时间：2026-06-13

## 当前切片

```text
P0 现状对齐与断点清单。
只做计划和断点落盘，不改业务代码。
下一切片：P1 动作验证报告最小结构。
```

## 依据文件

```text
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
规范/自我动作验证闭环规范20260612.md
规范/任务筹办与执行边界规范20260512.md
规范/自我内部循环实现规范20260512.md
规范/安全服务值结算规范20260518.md
流程图/20260612_任务执行及结算流程图_v0.2.md
流程图/20260612_任务执行及结算流程图_v0.3.md
计划/20260613_闭环验证与结算流程修正计划_v0.1.md
```

## 当前代码锚点

```text
执行段：
    任务模块.执行.ixx:46-65
        结构_任务步骤执行结果_最小 尚无动作验证报告字段。
    任务模块.执行.ixx:1576-1599
        执行桥.执行方法_同步等待 后读取本能方法运行闭环视图，并把方法执行成功/缺口推导为记账结果。
    任务模块.管理工作线程.ixx:2013-2125
        已有完成度计算和执行推进回执雏形，但未统一成 VX0 动作验证报告。
    任务模块.管理工作线程.ixx:2678-2725
        结果状态消息直接由目标成功/完成度填充成功、完成度和任务价值反馈比例。

协议段：
    任务模块.工作线程消息协议.ixx:314-339
        结构_任务管理结果状态消息 缺动作验证报告ID、验证状态、事实提交等级、回读置信度和偏差摘要。
    自我线程模块.消息协议.ixx:140-146, 271-273, 1206-1212
        已有执行尝试验证状态/对账状态，但它是消息对账状态，不是完整动作验证报告。

结算段：
    自我线程模块.impl.cpp:14650-14686
        UP5 后直接调用 私有_按成功结果满足来源需求并唤醒父任务_任务管理新链，缺 CL0/VC0/CL1 前置分类。
    自我线程模块.impl.cpp:8262-8378
        结果落账后直接进入任务满足反馈链。
    自我线程模块.impl.cpp:4352-4612
        私有_处理任务满足反馈_线程侧 先定位/收束来源需求，再做特殊旁路，分类点偏晚。
    自我线程模块.impl.cpp:1800-2144
        私有_处理任务价值反馈_线程侧 有硬闸门雏形，但没有读取 VC0 结算闭环验证报告。
    自我动作实现模块.ixx:12667-12898
        结算叶子任务价值 是最后写账动作，要求已结算、完成度、合法增量和实际结果状态，不承担结算域分类职责。
```

## 已发现偏差

```text
1. 执行报告态 E、实际回读态 R、目标态 G、预测态 P 尚未统一生成动作验证报告 V。
2. 执行尝试验证状态只是消息对账状态，不能替代动作验证报告。
3. 结果状态消息尚未携带动作验证报告引用，后续 CL0/VC0/VAL1 无统一输入。
4. 结算流程仍接近 UP5 -> SET0；v0.2 要求 UP5 -> CL0 -> CL1。
5. 内部治理任务和证据任务的分类仍在来源需求收束之后，存在先进入普通满足反馈语义的风险。
6. 核心价值结算硬闸门尚未读取闭环验证报告。
```

## 下一步 P1

```text
目标：
    实现动作验证报告最小结构，只创建、写入、读取、打印。

允许新增候选：
    动作验证报告类.h
    动作验证报告类.ixx
    动作闭环特征定义.h
    动作闭环特征定义.ixx

第一版字段：
    动作事务ID
    来源任务
    来源方法
    目标值
    预测值
    实际值
    执行报告值
    目标偏差
    预测偏差
    报告偏差
    回读置信度
    验证状态
    事实提交等级
    纠偏建议

边界：
    不改变任务完成判断。
    不改变结果上行。
    不改变结算域分类。
    不改变核心价值结算。
    缺实际回读时不得标为验证通过。
```

## 已运行命令

```text
本切片只读定位和文档编辑，未运行构建。
已运行：
    rg 定位任务执行、结果消息、自我线程结算、结算叶子任务价值代码锚点。
    Get-Content 小段读取规范、计划索引、v0.2流程图和关键代码函数。
待运行：
    git diff --check
```

## 禁止宣称

```text
不宣称动作验证闭环已完成。
不宣称任务执行闭环已完成。
不宣称任务执行及结算流程图已完整实现。
不宣称结算域分类已闭合。
不宣称核心价值结算硬闸门已闭合。
不宣称学习主线完成。
不宣称自我苏醒完成或内部循环全部完成。
```
