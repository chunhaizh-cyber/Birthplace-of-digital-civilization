# P10f-am 需求树 SQL 行文本字段复核 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-am `需求类.cpp::结构_需求树SQL行` 文本字段边界复核。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_ado_query_result.tsv`

复核字段：

- `路径`
- `节点名称`
- `目标语义`
- `逻辑组织类型`

复核证据：

- `结构_需求树SQL行` 是 `需求类::重写需求树SQL投影(...)` 使用的局部 SQL 投影行结构。
- `私有_收集需求树SQL行(...)` 填充这些文本字段：
  - `路径` 来自遍历路径。
  - `节点名称` 来自根 / 安全 / 服务固定显示名，或 `语素集.安全获取词(目标特征类型)` 的显示文本。
  - `目标语义` 来自 `需求目标语义视图.语义名称`。
  - `逻辑组织类型` 来自 `需求类::逻辑组织需求类型文本(...)`。
- 同一行同时保留结构化承载字段：`节点主键 / 父节点主键 / 描述信息主键 / 需求主体主键 / 需求场景主键 / 被需求存在主键 / 当前状态主键 / 目标状态主键 / 目标特征类型主键 / 对应任务主键 / 满足关系 / 权重 / 结算值 / 截止时间`。
- `需求类` 内部机器逻辑读取 `需求目标语义视图(...)` 的枚举、逻辑组织类型、目标特征类型、目标状态、需求节点和主键，不读取 SQL 投影文本字段。
- `控制面板类.cpp` 的 SQL 控制面板读取 `fishnest.v_current_demand_panel_nodes` 的 `node_name / tree_shape / target_semantics / logic_group_type` 等列，用于表格、子链 JSON 和页面展示。
- 控制面板非 SQL 路径也直接读取需求节点、目标特征类型和 `需求目标语义视图` 枚举来做诊断统计，不以 SQL 文本列驱动任务推进、目标比较、调度、结算或因果匹配。

处理结论：

- 本切片不删除 `路径 / 节点名称 / 目标语义 / 逻辑组织类型`。
- 这些字段登记为需求树 SQL / 控制面板投影文本边界；不得把其中的显示文本作为需求生成、任务化、筹办、结算或因果匹配依据。
- 本切片无代码改动，未重新构建。

下一步：

- 返回 P10f 最新复扫清单；已闭合或已登记的外设失败原因、线程生命周期显示文本、状态变化跟踪文本不重复打开，下一候选转向 `语素类.cpp::结构_语素SQL行` 文本字段边界复核。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
