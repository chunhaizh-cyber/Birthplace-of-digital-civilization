# 20260621 D1 需求类结构体 / 枚举必要性判定表

## 0. 判定口径

```text
所属计划：计划/20260620_需求类逻辑纠偏计划_v0.1.md P1。
执行边界：本表只做结构体 / 枚举必要性判定，不删除结构、不改代码。
上级边界：
1. 需求主信息不是任意业务值存储，只保留需求目标关系、任务承接和必要治理字段。
2. 获取途径只用于生成规则选择，不进入需求主信息。
3. 文本字段只能用于日志 / 显示 / 诊断，不得进入机器判断。
4. 控制面板 SQL 是查询投影，不是需求树权威写源。
```

## 1. 判定表

| 名称 | 字段 / 值 | 写入函数 | 读取函数 | 公开接口 | 协议边界 | 含文本字段 | 初判 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `枚举_需求树更新指令类型` | 未定义 / 新增需求 / 更新需求 / 满足需求 / 删除需求 | 构造各类需求更新指令 | `执行更新指令_已加锁` 分派 | 是 | 是，需求树更新协议 | 否 | 保留；P3 可审查是否拆成专用入口，但当前仍是统一更新协议的必要类型。 |
| `枚举_枚举目标生产者分级` | 未登记 / 专用方法生产 / 通用流程生产 / 规则生产 / 初始化生产 / 待补齐 | 枚举目标生产者查询 | 枚举目标校验 | 是 | 否，校验结果内部分类 | 否 | 保留；用于枚举目标合法性判断，不是显示文本。 |
| `结构_枚举目标校验结果` | 检查布尔、目标特征类型、生产者分级、`说明` | `校验枚举目标合法性` / `校验更新指令枚举目标` | 枚举目标校验调用方 | 是 | 否 | 是，`说明` | 保留但约束：`说明` 只能用于诊断 / 日志，不得作为机器判断；P2/P7 复核枚举目标生产者来源是否仍需跨类 helper。 |
| `结构_需求树更新指令` | 父/目标需求、主体、场景、目标宿主、状态、满足关系、权重、阻塞、观测统计 | 外部构造点、`构造逻辑组织需求更新指令` 等 | `执行更新指令` / `执行更新指令_已加锁` | 是 | 是，需求树统一更新协议 | 是，主键字符串 | 保留但进入 P3：当前是新增 / 更新 / 满足 / 删除统一协议；主键字符串是项目节点身份辅助，不得承载自由说明。 |
| `结构_子需求平分权重` | 安全权重 / 服务权重 | `计算子需求权重` / `计算子需求平分权重` | 权重刷新和控制面板诊断 | 是 | 否 | 否 | 保留；是需求权重分配结果值，不是主信息扩展字段。 |
| `结构_需求目标视图项` | 需求节点、目标主体、目标特征、当前 / 目标状态、方向、必需性 | `收集直接子需求目标视图_已加锁` | 筹办 / 目标重叠 / 显示 | 是 | 否，需求只读视图 | 否 | 保留；属于只读视图，不写入需求主信息。 |
| `枚举_需求方向比较结果` | 不可比较 / 不同目标 / 同向 / 反向 | `比较需求方向签名` | 方向比较调用方 | 是 | 否 | 否 | 保留；机器判定值，不依赖文本。 |
| `结构_需求方向签名` | 目标主体主键、目标特征主键、方向掩码、方向区间、差值、标量值 | `生成需求方向签名` | `比较需求方向签名` | 是 | 否，方向比较只读签名 | 是，主键字符串 | 保留但进入 P7：二次特征方向 / 差值 / 标量值应继续确认是否全部通过二次特征类 helper。 |
| `结构_任务初始化上下文` | 来源需求、发起存在、场景、作用对象、状态、满足关系、目标特征、权重 | `生成任务初始化上下文` | 任务创建 / 任务语义初始化 | 是 | 是，需求到任务初始化边界 | 否 | 保留；这是需求到任务的结构化输出，不是任务筹办本能化入口。 |
| `枚举_逻辑组织需求类型` | AND组 / OR组 / 方法路径组 / 因果子链支撑 | 目标特征 / 获取途径映射 | 逻辑组织视图、构造更新指令 | 是 | 否，需求生成规则分类 | 否 | 保留；P2 复核它只参与生成规则与目标语义解释。 |
| `枚举_需求目标语义` | 普通可任务化目标、逻辑组织目标、OR组结算令牌等 | 目标特征 / 获取途径映射 | 筹办准入、视图、日志 | 是 | 否，目标语义只读解释 | 否 | 保留；P2 复核语义不写回需求主信息。 |
| `枚举_需求获取途径` | 普通目标、筹办缺口、执行回执、自检、观察反馈、结算后处理、逻辑组织等 | 外部生成需求更新输入时显式给出 | `需求获取途径语义视图` / 构造获取途径更新指令 | 是 | 是，生成规则选择入口 | 否 | 保留但进入 P2：只能用于入树前生成规则，不得成为需求主信息字段。 |
| `结构_需求目标语义视图` | 语义、逻辑组织类型、目标特征、语义名称、准入布尔 | `需求目标语义视图` | 筹办准入 / 显示 / 禁止原因 | 是 | 否，只读视图 | 是，`语义名称` | 保留；`语义名称` 只可显示，机器判断使用枚举 / 布尔字段。 |
| `结构_需求获取途径语义视图` | 获取途径、名称、默认语义、默认类型、默认目标特征、布尔能力 | `需求获取途径语义视图` | 构造获取途径逻辑组织指令 | 是 | 是，生成规则解释视图 | 是，`获取途径名称` | 保留但进入 P2；名称只可显示，机器判断使用枚举 / 布尔字段。 |
| `结构_逻辑组织需求更新输入` | 父需求、主体、场景、目标宿主、状态、满足关系、权重、阻塞、统计 | 外部逻辑组织需求生成点 | `构造逻辑组织需求更新指令` 系列 | 是 | 是，构造更新指令输入包 | 是，主键字符串 | 保留但进入 P3：当前可减少外部手写 `结构_需求树更新指令`，后续审查是否拆成更窄专用入口。 |
| `结构_需求目标键` | 目标宿主主键、目标特征主键、当前 / 目标状态主键、满足关系 | `生成需求目标键_已加锁` | 目标重叠查找 / 比较 | 否，私有结构 | 否，内部去重键 | 是，主键字符串 | 保留；主键是项目身份，不是说明文本。 |
| `结构_需求树SQL行` | 需求树 SQL 投影列：节点、父、目标、状态、权重、结算摘要、时间 | `私有_收集需求树SQL行` | `重写需求树SQL投影` | 否，cpp 内部结构 | 是，SQL 查询投影 | 是，SQL 字符串 | 保留；只作控制面板 SQL 查询投影，不作为需求树权威写源。 |

## 2. 后续登记

```text
P2 重点：
1. 复核 `枚举_需求获取途径` 与 `结构_需求获取途径语义视图`：获取途径只服务入树前生成规则。
2. 复核 `结构_需求目标语义视图` 中显示名称和机器布尔 / 枚举的边界。
3. 复核 OR 组结算令牌、结算后处理原子目标是否仍只由目标特征和专用提交链表达。

P3 重点：
1. 统计 `结构_需求树更新指令` 外部构造点。
2. 判断是否能用新增 / 更新 / 满足 / 删除专用函数减少手写协议字段。
3. 复核 `结构_逻辑组织需求更新输入` 是否已经足够替代外部直接构造更新指令。

P7 重点：
1. `结构_需求方向签名` 的方向区间、差值、标量值读取需继续核对二次特征类 helper 边界。
2. 枚举目标合法性校验的生产者来源需继续核对方法 / 因果 / 状态类 helper 边界。
```
