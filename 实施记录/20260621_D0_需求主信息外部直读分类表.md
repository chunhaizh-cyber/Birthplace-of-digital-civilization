# 需求主信息外部直读分类表

日期：2026-06-21

对应计划：`计划/20260620_需求类逻辑纠偏计划_v0.1.md` P0。

## 1. 扫描证据

扫描文件：

```text
日志/20260621_D0_需求主信息外部直读_计划模式扫描.txt
日志/20260621_D0_需求主信息外部直读_宽模式扫描.txt
日志/20260621_D0_需求主信息关注字段扫描.txt
日志/20260621_D0_需求类既有函数扫描.txt
日志/20260621_D0_需求主信息机器逻辑候选.txt
日志/20260621_D0_需求主信息显示候选.txt
```

宽模式命中文件统计：

| 文件 | 命中数 | 初判 |
| --- | ---: | --- |
| `自我线程模块.impl.cpp` | 173 | 机器逻辑，后续独立切片回迁 |
| `任务模块.筹办.ixx` | 41 | 机器逻辑，后续独立切片回迁 |
| `任务模块.管理界面线程.impl.cpp` | 27 | 任务承接 / 任务镜像逻辑，后续独立切片回迁 |
| `控制面板类.cpp` | 13 | 显示 / SQL 投影为主，后置处理 |
| `任务类.cpp` | 8 | 机器逻辑，本轮第一代码切片 |
| `自我动作实现模块.ixx` | 8 | 机器逻辑，后续独立切片回迁 |
| `任务模块.管理工作线程.ixx` | 7 | 机器逻辑，后续独立切片回迁 |
| `控制面板摘要线程模块.impl.cpp` | 6 | 摘要显示 / 统计视图，后置处理 |
| `自我类.cpp` | 6 | 目标特征同一性相关，后续按需复核 |
| `鱼巢.cpp` | 2 | 控制命令路径，后续按需复核 |
| `自检线程模块.impl.cpp` | 1 | 自检报送链，后续按需复核 |

## 2. 分类口径

| 类别 | 判定标准 | 处理 |
| --- | --- | --- |
| 机器逻辑必须回迁 | 用 `需求->主信息.*` 直接决定状态解析、候选、权重、满足、派发、重筹办或结算 | 改为调用 `需求类` 只读函数；缺函数先补 `需求类` |
| 显示 / 日志 / SQL 投影 | 只用于控制面板、摘要、日志输出、SQL 查询投影 | 后置；不得反向成为机器决策来源 |
| 可复用需求类函数 | 已有 `读取需求目标特征类型`、`生成任务初始化上下文`、`来源需求有安全方向` 等 | 优先复用，不重复造入口 |
| 缺需求类函数 | 状态引用、需求场景、权重读取等无统一只读边界 | 本计划 P0 补最小 helper |
| 跨类缺口 | 需求类自身解释状态 / 特征 / 二次特征 / 世界树主信息 | 登记到对应类纠偏计划，按“缺哪个类补哪个类第一优先”处理 |

## 3. 本轮第一代码切片

选择 `任务类.cpp`，原因：

```text
1. 命中点少，范围可闭合。
2. 命中点均为外部读取需求主信息字段的机器逻辑。
3. 需要的需求类只读 helper 明确，不涉及需求树语义修改。
```

待迁移点：

| 文件位置 | 当前直读字段 | 目标处理 |
| --- | --- | --- |
| `任务类.cpp:207` | `来源需求->主信息.需求状态` | `需求类::解析需求目标状态` |
| `任务类.cpp:216` | `来源需求->主信息.被需求当前状态` | `需求类::解析需求当前状态` |
| `任务类.cpp:659-660` | `来源需求->主信息.需求场景.指针` | `需求类::解析需求场景` |
| `任务类.cpp:702-706` | `来源需求->主信息.服务权重 / 安全权重` | `需求类::读取需求服务权重 / 读取需求安全权重` |

本轮拟新增需求类只读函数：

```text
读取需求当前状态引用 / 解析需求当前状态
读取需求目标状态引用 / 解析需求目标状态
读取需求场景引用 / 解析需求场景
读取需求安全权重 / 读取需求服务权重 / 读取需求综合权重
```

## 4. 后续缺口

```text
1. 任务模块.筹办.ixx、任务模块.管理工作线程.ixx、任务模块.管理界面线程.impl.cpp 仍有机器逻辑直读，需要在后续切片处理。
2. 自我线程模块.impl.cpp 命中量最大，暂不作为首个切片，避免把 P0 扩成大范围改造。
3. 控制面板和 SQL 投影只登记为显示后置，不阻塞本轮任务类代码切片。
4. 需求类自身跨类读取另按计划第 6 章分类，不混入当前外部直读回迁切片。
```

## 5. 本轮执行结果

已完成：

```text
1. `需求类` 新增最小只读 helper：状态引用、状态解析、需求场景解析、安全 / 服务 / 综合权重读取。
2. `任务类.cpp` 的目标状态、当前状态、需求场景、服务权重和安全权重直读已回迁到 `需求类` helper。
3. `rg -n "来源需求->主信息\.(需求状态|被需求当前状态|需求场景|服务权重|安全权重)" 任务类.cpp` 无命中。
4. 已重新生成 `说明书/函数功能说明文档.md`。
5. Debug x64 构建通过：`日志/20260621_D0_需求类只读helper_任务类回迁_build.log`，退出码 0。
```

继续执行结果：

```text
1. `需求类` 新增 `读取需求满足关系` helper。
2. `任务模块.管理工作线程.ixx` 中目标特征类型、目标状态、当前状态、满足关系直读已回迁到 `需求类` helper。
3. `rg -n "需求->主信息\.(目标特征类型缓存|需求状态|被需求当前状态|满足关系)" 任务模块.管理工作线程.ixx` 无命中。
4. 已重新生成 `说明书/函数功能说明文档.md`。
5. Debug x64 构建通过：`日志/20260621_D0_需求类满足关系_工作线程回迁_build.log`，退出码 0。
```
