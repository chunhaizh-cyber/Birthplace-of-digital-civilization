# 20260620 存在主信息字段必要性审查 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_存在类逻辑纠偏计划_v0.1.md` 的 1.1，建立存在主信息字段判定表。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_存在类逻辑纠偏计划_v0.1.md
基础信息主信息类.h
存在类.h
存在类.cpp
```

本切片范围：

```text
1. 只建立字段判定表。
2. 不删除字段。
3. 不迁移观测缓存字段读取来源。
4. 不处理存在类中特征创建 / 读取职责迁移。
```

验证前预测：

```text
1. 基础信息主信息类.h 中存在节点主信息字段可定位。
2. 全仓字段引用清单可落盘到 日志/20260620_存在主信息字段引用.txt。
3. 判定表应只给出保留 / 暂留 / 后续候选，不宣称存在类逻辑纠偏完成。
```

## 1. 实施记录

```text
1. 已读取 基础信息主信息类.h:424-474，确认 存在节点主信息类 当前字段。
2. 已运行字段引用检索，并将完整输出写入 日志/20260620_存在主信息字段引用.txt。
3. 已提取字段命中计数和主要文件分布。
4. 已生成 实施记录/20260620_存在主信息字段判定表.md。
```

## 2. 验证结果

```text
1. 字段定义：
   基础信息主信息类.h:426-442 包含 概念模板 / 概念集 / 内部世界 / 需求根节点 / 任务根节点 / 方法根节点 / 有位置历史 / 上次观测位置_mm / 最近观测位置_mm / 最近位移_mm / 连续静止帧 / 连续未命中帧。

2. 字段引用清单：
   rg 输出已写入 日志/20260620_存在主信息字段引用.txt。

3. 字段判定表：
   实施记录/20260620_存在主信息字段判定表.md 已记录字段、写入方、读取方、缓存 / 持久事实判断和初判。
```

## 3. 当前判断

```text
1. 本切片已闭合：存在主信息字段必要性审查已形成判定表。
2. 本切片不删除字段。
3. 下一步进入 存在类 1.2 功能函数补缺，优先补存在名称 / 类型 / 内部世界 / 概念 / 观测缓存只读函数。
```
