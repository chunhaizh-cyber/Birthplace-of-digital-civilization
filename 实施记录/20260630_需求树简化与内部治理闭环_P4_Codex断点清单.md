# 需求树简化与内部治理闭环 P4 Codex断点清单

日期：2026-06-30

## 0. 恢复摘要

当前切片：R24 P4 草稿停放；先回退到 P3c-P3f 动态信息盘点与因果需求提取。

本轮目标：

```text
不改需求树主提交行为、不迁移历史枚举、不新增持久结构体，
先在 需求类 补普通只读准入函数：
    四类缺口选择获取途径；
    判断获取途径是否属于四类简化准入输出；
    判断内部治理缺陷是否禁止普通入树。
同时把兼容枚举 `普通目标直接生成` 的显示文本收束为“正式目标承接”，
避免继续显示或语义化为凭空普通目标。
```

口径修正：

```text
主动因果由本能方法执行产生。
进入 P4 需求入树收束前，必须先提取：
    本能方法中的现有主动动态信息；
    内部治理函数中的现有被动动态信息。
再根据盘点结果列出因果需求，
最后按因果需求复用已有本能方法、复用普通治理函数，或提出缺失本能方法候选。

因此，本清单中已编辑的 P4 helper 只能作为未构建、未验收、待复核草稿；
不得作为“需求树简化入口已闭合”或“主动因果已可任务化”的证据。
```

## 1. 计划修改文件

```text
需求类.h
需求类.cpp
说明书/函数功能说明文档.md
实施记录/20260630_需求树简化与内部治理闭环_P4_Codex断点清单.md
```

## 2. 预测

```text
1. 新函数只读，不创建需求节点、不写需求树、不创建世界树事实。
2. 四类准入输出只能是：
   根特征缺口；
   任务筹办缺口；
   执行期真实变化缺口；
   可验证服务触发事实。
3. 多个缺口同时为真或全部为假时返回未定义，并由输出参数标记冲突或不足。
4. `普通目标直接生成` 仍作为历史枚举值保留，但显示文本不再输出“普通目标直接生成”。
5. 内部治理函数缺失、结构字段缺失或协议缺入口只返回“禁止普通入树”，不生成普通需求。
```

## 3. 待运行验证

```text
python .\tools\生成函数功能说明文档.py
git diff --check -- 需求类.h 需求类.cpp 说明书\函数功能说明文档.md 实施记录\20260630_需求树简化与内部治理闭环_P4_Codex断点清单.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

## 3.1 已完成修改

```text
1. `需求类::需求获取途径文本` 中，历史兼容枚举 `普通目标直接生成` 的显示文本改为“正式目标承接”。
2. 新增获取途径枚举值 `可验证服务触发事实`。
3. 新增 `需求类::需求获取途径是简化准入输出`。
4. 新增 `需求类::需求_按缺口类型选择获取途径`。
5. 新增 `需求类::需求_内部治理缺陷禁止普通入树`。
6. `可验证服务触发事实` 已接入获取途径文本、目标语义和逻辑组织类型映射。
```

## 3.2 已运行验证

```text
python .\tools\生成函数功能说明文档.py
结果：成功；函数条目=7516。

git diff --check -- 需求类.h 需求类.cpp 说明书\函数功能说明文档.md 实施记录\20260630_需求树简化与内部治理闭环_P4_Codex断点清单.md
结果：通过；仅提示函数说明文档 CRLF 下次触碰会转 LF。
```

## 3.3 当前阻塞

```text
阻塞类型：
    1. 口径修正：P4 入口前置条件不足，必须先执行 P3c-P3f。
    2. 构建 / 运行互斥锁被其它窗口占用。
锁文件：.codex_locks/build-run.lock
锁内容：
    owner=Codex
    action=run
    project=鱼巢
    configuration=Debug
    platform=x64
    exe=.\x64\Debug\鱼巢.exe
    pid=49068
    purpose=P8c2 longer natural window for voxel candidate root cause
PID 检查：进程 49068 仍为正在运行的 鱼巢.exe，启动时间 2026/6/30 15:26:47。

当前状态：
    P4 代码已编辑；
    函数说明文档已更新；
    静态 diff 检查已通过；
    Debug x64 构建未运行；
    P4 未提交；
    P4 helper 暂停进入闭合验证；
    下一步先更新计划并执行 P3c-P3f 动态信息盘点、因果需求清单和本能方法映射。
```

## 3.4 后续恢复条件

```text
只有满足以下条件后，才允许恢复 P4：
1. 已列出本能方法现有主动动态信息。
2. 已列出内部治理函数现有被动动态信息。
3. 已从动态信息缺口生成因果需求清单。
4. 已将因果需求映射为：
   复用已有本能方法；
   复用普通治理函数读取 / 复核 / 回执；
   缺失本能方法候选。
5. 已确认 P4 helper 与因果需求清单不冲突。
6. 构建 / 运行互斥锁释放后，重新运行 Debug x64 构建。
```

## 4. 禁止宣称项

```text
不得宣称需求树简化生成已闭环。
不得宣称所有入树入口都已迁移到新准入函数。
不得宣称内部治理闭环已完成。
```
