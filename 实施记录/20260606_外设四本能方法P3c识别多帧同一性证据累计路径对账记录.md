# 外设四本能方法 P3c 识别多帧同一性证据累计路径对账记录

## 目标

对账当前代码是否已有材料支撑 P3 中“多帧累计同一性证据”，并拆出下一片最小代码接入。

## 依据

```text
计划/20260605_外设四本能方法需求树生成因果链修订计划_v0.1.md
实施记录/20260606_外设四本能方法P3b识别前置观察完成闸门接入记录.md
```

## 已有材料

```text
外设线程_D455深度相机.ixx：
    逐簇识别报告会更新跨帧观察簇缓存；
    每个观察像素簇会写 外设跨帧簇ID；
    会写 跨帧匹配状态；
    会写 跨帧连续命中次数；
    会写 跨帧复现离散度；
    报告级会写 跨帧匹配观察像素簇数量、跨帧新建观察像素簇数量、跨帧稳定观察像素簇数量、跨帧丢失观察像素簇数量。

外设观察报告队列.ixx：
    结构_外设观察像素簇摘要 已包含 外设跨帧簇ID、跨帧匹配状态、跨帧连续命中次数、跨帧丢失次数、跨帧复现离散度；
    结构_外设观察报告队列项 已包含报告级跨帧统计；
    D455 逐簇识别样本卡会输出新候选、已有匹配候选和像素归属账本材料。
```

## 当前识别接入状态

```text
自我动作实现.外设模块.ixx：
    结构_外设观察存在确认方案项 当前只有方案类型、来源候选簇ID、来源空间候选编号、允许提交状态、匹配分数、冲突原因、证据不足原因、目标已知存在和簇材料；
    外设簇与已确认观察存在匹配分数 当前只用投影重叠率、中心距离和深度差；
    构造外设观察存在确认方案 当前只按本帧簇和当前已确认观察存在集生成合并 / 新建 / 冲突 / 证据不足；
    没有把 跨帧匹配状态、跨帧连续命中次数、跨帧复现离散度 写成同一性证据字段；
    没有让连续命中不足阻止同一性 / 归属提交。
```

## 相关但不能直接替代的路径

```text
自我_匹配当前观察存在与历史观察存在 已存在：
    输出观察存在匹配关系、历史对应状态、中心坐标差、范围坐标重叠率、尺寸差、匹配置信度。

该路径属于当前 / 历史观察存在匹配关系；
不能直接替代 P3 识别阶段的外设观察存在与自我场景存在同一性累计证据。
```

## P3c 判断

```text
可以继续进入 P3d。
```

理由：

```text
材料已经存在于外设报告和像素簇摘要；
缺的是自我侧正式特征、确认方案项承接和方案生成条件；
不需要伪造样本或新增模拟入口。
```

## P3d 最小代码片

允许修改：

```text
自我动作实现.外设模块.ixx
特征类.cpp
计划/计划索引.md
实施记录/20260605_外设四本能方法P0_Codex断点清单.md
实施记录/20260606_外设四本能方法P3d识别多帧同一性证据最小接入记录.md
```

拟实现：

```text
新增最小正式特征：
    外设观察存在同一性证据帧数；
    外设观察存在跨帧匹配状态；
    外设观察存在跨帧连续命中次数；
    外设观察存在跨帧复现离散度。

结构_外设观察存在确认方案项：
    承接上述跨帧证据字段。

构造外设观察存在确认方案：
    从簇材料填入跨帧证据；
    跨帧匹配状态未成立或连续命中次数 < 2 时，将方案项判为证据不足；
    达标后再进入当前的本帧匹配分数 / 冲突 / 合并 / 新建逻辑。

自我_识别外设观察材料：
    在方案项节点写上述正式证据字段。
```

禁止：

```text
不得把报告级跨帧统计直接写成单个存在的同一性结论；
不得把当前 / 历史观察存在匹配方法当作 P3 已闭合；
不得把证据不足伪装成已归属；
不得新增模拟测试、夹具测试、伪造入口或伪造动作动态。
```

验证：

```text
先写预测；
运行 git diff --check；
运行 Debug x64 构建；
不真实运行鱼巢.exe，除非后续有自然运行条件或用户明确要求。
```

## 验收状态

```text
P3c 已完成；
P3c 未修改业务代码；
P3c 未构建；
P3c 未真实运行鱼巢.exe。
```
