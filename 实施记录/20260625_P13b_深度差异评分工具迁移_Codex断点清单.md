# 20260625 P13b 深度差异评分工具迁移 Codex断点清单

## 0. 恢复摘要

当前切片已闭合。已处理 `双目相机本能适配器.cpp::深度差异评分(std::int64_t)`。

依据：

```text
计划/计划索引.md：P13b 全局共享迁移；B 类候选逐个判定迁移 / 复用 / 保留。
实施记录/20260624_P13_函数自定义主信息分类候选表.tsv：`深度差异评分` 为 B 类候选，未命中自定义主信息 / 节点 / 结构 / 协议对象。
全局共享函数类.ixx 文件头规则：无领域自定义主信息 / 领域类型参与的通用函数可进入本模块。
```

当前判断：

```text
`深度差异评分` 只按毫米差值计算 Q10000 稳定性评分，不读取项目结构，不写机器状态。
该函数带深度领域语义，但不依赖领域自定义类型；迁入全局共享可减少双目适配器局部纯工具。
`全局共享函数类.ixx` 和 `说明书/函数功能说明文档.md` 未发现同功能函数。
```

已完成：

```text
1. 在 `全局共享函数类.ixx` 新增 `深度差异评分(I64)`。
2. 给 `双目相机本能适配器.cpp` 补最小“文件头部规则注释模块”。
3. 删除双目适配器本地 `深度差异评分`。
4. 运行函数说明文档生成脚本：扫描源文件 127 个，函数条目 7163。
5. Debug x64 构建通过：0 警告，0 错误。
```

复扫结果：

```text
`说明书/函数功能说明文档.md` 已登记 `全局共享函数类.ixx::深度差异评分`。
`双目相机本能适配器.cpp` 只保留对 `深度差异评分` 的调用，不再保留本地定义。
```

禁止宣称：不宣称 P13 完成，不宣称函数去重全部完成，不宣称自我苏醒完成或初步成熟完成。
