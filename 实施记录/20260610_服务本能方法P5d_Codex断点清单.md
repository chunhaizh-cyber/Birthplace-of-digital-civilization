# 服务本能方法 P5d Codex断点清单

日期：2026-06-10

## 当前切片

```text
服务本能方法实现计划 P5d：
    G0 候选组装 helper。
```

## 依据文件

```text
AGENTS.md
规范/规范目录.md
规范/方法动作动态硬规则规范20260512.md
规范/任务筹办与执行边界规范20260512.md
计划/计划索引.md
实施记录/20260610_服务本能方法实现方案记录.md
实施记录/20260610_服务本能方法P5c_Codex断点清单.md
```

## 允许修改范围

```text
自我动作实现.服务模块.ixx
实施记录/20260610_服务本能方法P5d_Codex断点清单.md
计划/计划索引.md
```

## 禁止修改范围

```text
任务主信息类
需求主信息类
特征值主信息类
任务筹办 / 任务执行 本能方法化
服务事件E独立虚拟存在
裸写服务值
D2 结算实现
最终成功服务确认
用任务完成或方法返回成功证明成功服务
```

## 进入时工作区状态

```text
P0/P1 已提交：8c3673a
P2 已提交：aa2ca33
P3 已提交：1dd4662
P4a 已提交：7d59ece
P4b 已提交：86641b4
P5a 已提交：6f98949
P5b 已提交：a6b2699
P5c 已提交：a88ab30
存在提交前已有无关修改：
    AGENTS.md
    任务模块.管理工作线程.ixx
    控制面板.html
    实施记录/20260610_观察子任务完成推进_Codex断点清单.md
    消息中间件/

本切片不得混入上述文件。
```

## 预期结果

```text
1. 服务模块提供 G0 候选组装输入 / 输出结构。
2. 组装 helper 能把任务字段、方法运行存在、D0、D1、前后状态比较、三评分写入 G0。
3. 组装 helper 能按当前已有事实写入部分硬门槛状态：
   任务服务对象/交互者、来源需求、目标字段、权限边界、D0合法性、前后状态可比较、目标特征变化。
4. 安全通过、结果可验证、强归因、非重复、闭环回写等未有证据项保持待确认。
5. 组装 helper 只输出候选状态，不最终确认成功服务，不写服务值，不生成 D2。
6. Debug x64 构建通过。
```

## 已运行命令

```text
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\鱼巢_build_20260610_服务模块P5d.log
```

## 关键退出码

```text
Debug x64 构建：ExitCode=0
关键错误检索：未检出 error C / fatal error / MSB / 错误。
```

## 已完成修改

```text
1. 新增 G0 候选组装输入 / 输出结构。
2. 新增任务字段到硬门槛状态的基础映射 helper。
3. 新增 D0 校验到硬门槛状态的映射 helper。
4. 新增 G0 候选基础硬门槛写入 helper：
   已有任务服务对象 / 交互者、来源需求、目标字段、权限边界、D0合法性、
   前后状态可比较、目标特征变化可写入对应硬门槛；
   安全、验证、归因、非重复、闭环回写保持输入控制，默认待确认。
5. 新增组装G0成功服务候选 helper：
   创建 / 读取 G0；
   写 D0 校验；
   写硬门槛；
   可选写三评分；
   回写候选状态和层级候选。
6. 未最终确认成功服务，未写服务值，未生成 D2。
```

## 当前判断

```text
P5d 已闭合；G0 候选组装 helper 可构建。
下一步进入 P6a：成功服务候选池记录 helper。
```
