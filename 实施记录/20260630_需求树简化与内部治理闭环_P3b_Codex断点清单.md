# 需求树简化与内部治理闭环 P3b Codex断点清单

日期：2026-06-30

## 0. 恢复摘要

当前切片：R24 P3b，主动因果信息只读治理函数补齐。

本轮目标：

```text
在不新增持久结构体、不写世界树事实、不接入任务筹办行为的前提下，
给 因果类 补主动因果来源动态完整性、主动动作来源、验证状态、
同源变化证据可回解和可任务化裁决的普通只读函数。
```

## 1. 计划修改文件

```text
因果类.h
因果类.cpp
说明书/函数功能说明文档.md
实施记录/20260630_需求树简化与内部治理闭环_P3b_Codex断点清单.md
```

## 2. 预测

```text
1. 新接口只读，不创建或修改因果节点、动态节点、需求节点或任务节点。
2. 被动动作语义 `被动动作` / `被动触发.*` 不会被判为主动因果。
3. 有主动动作语义但无方法模板时，裁决为学习或方法头承接缺口，不判为可直接任务化。
4. 有方法模板但方法结构未满足最低可用判据时，裁决为方法结构缺口。
5. 只有主动因果、来源动态完整、证据状态足够且方法满足最低结构判据时，才裁决为可生成原子任务。
6. 可能风险点是方法节点类型转换、const/noexcept 签名和函数说明文档生成。
```

## 3. 待运行验证

```text
python .\tools\生成函数功能说明文档.py
git diff --check -- 因果类.h 因果类.cpp 说明书/函数功能说明文档.md 实施记录/20260630_需求树简化与内部治理闭环_P3b_Codex断点清单.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

## 3.1 已完成修改

```text
1. 新增 `因果类::验证主动因果来源动态完整`。
2. 新增 `因果类::读取主动因果动作来源`。
3. 新增 `因果类::读取主动因果验证状态`。
4. 新增 `因果类::检查主动因果同源变化证据可回解`。
5. 新增 `因果类::判定主动因果可任务化`。
6. 新增内部 helper：基础节点同一性判断、方法可作为主动动作来源判断。
```

## 3.2 已运行验证

```text
python .\tools\生成函数功能说明文档.py
结果：成功；函数条目=7510；新增主动因果 5 个定义和 5 个声明已进入函数说明文档。

git diff --check -- 因果类.h 因果类.cpp 说明书\函数功能说明文档.md 实施记录\20260630_需求树简化与内部治理闭环_P3a_Codex断点清单.md 实施记录\20260630_需求树简化与内部治理闭环_P3b_Codex断点清单.md
结果：通过；仅提示函数说明文档 CRLF 下次触碰会转 LF。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
日志：日志/20260630_R24_P3b_msbuild_Debug_x64.log
退出码：0
关键行：已成功生成；0 个警告；0 个错误。
锁处理：运行前无 `.codex_locks/build-run.lock`；构建期间创建锁，构建后已释放。
```

## 3.3 当前收束判断

```text
1. P3b 已补齐主动因果只读治理接口。
2. 新接口不改变当前生成因果、需求入树或任务筹办行为。
3. 主动因果可任务化裁决已能区分主动因果、来源动态完整、已验证 / 单次样本候选 / 证据足够、可生成原子任务、方法结构缺口、学习缺口和不可任务化。
4. 下一步进入 P4 简化需求树生成入口；若接入筹办消费，应留到 P5。
```

## 4. 禁止宣称项

```text
不得宣称主动因果闭环已完成。
不得宣称任务筹办已消费主动因果裁决。
不得宣称需求树简化生成已闭环。
不得宣称内部治理闭环已完成。
```
