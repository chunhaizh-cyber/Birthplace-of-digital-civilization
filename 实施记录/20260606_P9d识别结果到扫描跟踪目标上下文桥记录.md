# 20260606 P9d 识别结果到扫描跟踪目标上下文桥记录

## 当前切片

计划：`计划/20260606_外设四本能方法任务真实运行条件包闭环计划_v0.1.md`

切片：P9d，识别结果到扫描 / 跟踪目标上下文桥。

目标：让 `自我_识别外设观察材料` 产出的稳定对应事实能被 `提交_当前观察范围可观测单位存在对应事实` 消费，形成扫描 / 跟踪前置上下文。

## P9c 闭合证据

真实日志区间：`日志/鱼巢_run_20260606.低值g:66177`。

```text
67065-67069：6145 写入目标方法实际结果状态，包含 UG=当前观察范围可观测单位存在稳定对应事实取得状态。
67071：6145 成功，报告ID=22，可提交=23，观察稳定复现状态=1，观察完成状态=1，观察可复验状态=1，上游验证状态=1。
67072-67081：任务 B 读取实际状态 4V5，实际值=1，接受=1，目标达成=1，允许完成=1。
67084：任务 B 实际任务状态=完成。
```

判断：

```text
P9c 已闭合。
6145 已能通过任务真实运行并完成。
```

## 当前堵点

同一日志区间：

```text
67142：提交_当前观察范围可观测单位存在对应事实 执行，但 识别方案取得状态=0，已稳定对应存在可观测单位数量=0，稳定对应存在数量=0，映射缺口原因掩码=44。
67143-67153：任务 C 失败，目标是 稳定对应存在数量明确状态。
67211：同一提交入口再次执行仍失败。
67212-67222：任务 D 失败，目标是 当前观察范围可观测单位存在对应闭合状态。
```

判断：

```text
6150 没有正确消费 6145 已写的识别方案和稳定对应数量。
直接原因有两层：
1. 读取提交入口I64 先读输入参数场景，旧 0 会遮蔽宿主 / 输出场景上的新值。
2. 6150 把 观察存在事实提交状态 或 已验证观察存在数量 当成提交确认链前置，导致“提交前必须已有提交结果”的倒置。
```

## 本轮修改

已改文件：

```text
自我动作实现.外设模块.ixx
```

修改点：

```text
1. 6145 识别摘要补写 已验证观察存在数量=已稳定对应单位数量。
2. 6150 对识别确认状态、方案取得状态、报告ID、可提交数量、冲突数量、证据不足数量、已验证观察存在数量，允许从宿主 / 输出场景读取最新值覆盖输入场景旧 0。
3. 6150 在缺 已验证观察存在数量 时，按 已稳定对应存在可观测单位数量 和 识别确认可提交数量兜底。
4. 6150 的提交确认链可读条件允许使用可提交数量或识别方案取得状态，不再要求提交前已有提交状态。
5. 6150 写入自身目标方法实际结果状态，覆盖提交状态、计数事实、稳定对应事实、稳定数量明确、对应闭合、扫描可启动和当前场景特征值明确。
```

## 验证前预测

```text
1. 任务 B 仍应完成。
2. 6150 应读取到 识别方案取得状态=1、已稳定对应存在可观测单位数量>0、稳定对应存在数量>0。
3. 任务 C 目标 稳定对应存在数量明确状态 应能读取实际状态并完成。
4. 若证据不足候选仍存在，任务 D 的 当前观察范围可观测单位存在对应闭合状态 可能仍为 0；这会成为真实业务堵点，而不是提交入口读取失败。
```

## 真实验证结果

构建：

```text
Debug x64 构建通过，0 错误，1 个 LNK4075 警告。
```

真实日志区间：`日志/鱼巢_run_20260606.低值g:67611`。

```text
68522-68535：6145 仍成功，任务 B 完成。
68602：6150 成功，已稳定对应存在可观测单位数量=15，未稳定对应存在可观测单位数量=13，稳定对应存在数量=15，稳定数量明确状态=1，对应闭合状态=0，扫描可启动状态=1。
68603-68615：任务 C 目标 稳定对应存在数量明确状态 实际值=1，接受=1，完成。
68680-68682：任务 D 目标 当前观察范围可观测单位存在对应闭合状态，实际值=0，目标值=1，接受=0。
68748-68750：任务 D 重跑仍为实际值=0，继续待重筹办。
```

当前判断：

```text
提交入口读取链已修正。
P9d 当前停点不是代码读不到 6145 结果，而是当前真实观察范围存在未稳定 / 证据不足候选，不能把 当前观察范围可观测单位存在对应闭合状态 写成 1。
扫描可启动状态已经为 1，但父因果路径仍等待更强的“全范围对应闭合”目标。
```

当前堵点：

```text
需要明确下一步口径：
1. 若扫描 / 跟踪允许基于稳定子集启动，则应把 P9d 下一切片改为“稳定子集扫描目标上下文桥”，避免被全范围闭合目标 D 阻断。
2. 若必须全范围闭合后才能继续，则需要补观察 / 复验未稳定候选，把未稳定 13 个候选降到 0 后才能完成任务 D。
```

## 稳定子集扫描目标上下文桥补充

断点清单：`实施记录/20260606_P9d稳定子集扫描目标上下文桥_Codex断点清单.md`。

本轮补充修改：

```text
1. 6150 在不写全范围闭合的前提下，新增稳定子集扫描目标上下文桥。
2. 稳定子集只消费已稳定对应单位和通过提交质量门的材料。
3. 每个允许项创建或复用正式 已确认观察存在，写入可观测单位到存在映射项，并提交 6146 可消费的目标观察约束。
4. 首帧缺扫描结果时只写基准降级，不生成扫描变化动态。
5. 报告、队列项、材料页和目标观察约束仍只作为来源材料或通信容器，不作为事实主体。
```

构建结果：

```text
Debug x64 构建通过，0 错误，1 个 LNK4075 警告。
```

真实日志证据：

```text
日志/鱼巢_run_20260606.低值g:72430
    成功=1；
    有效可观测单位数量=24；
    已稳定对应存在可观测单位数量=21；
    未稳定对应存在可观测单位数量=3；
    当前观察范围可观测单位存在对应闭合状态=0；
    当前场景特征值明确状态=0；
    扫描可启动状态=1；
    稳定子集扫描目标上下文状态=1；
    稳定子集正式目标存在数量=21；
    稳定子集映射写入数量=21；
    稳定子集扫描约束写入数量=21；
    稳定子集扫描约束强匹配数量=21；
    稳定子集扫描约束失败数量=0；
    首帧扫描基准降级数量=21；
    不把扫描可启动当全范围闭合=1；
    来源报告只作材料=1；
    提交主体类型=当前观察范围可观测单位存在对应事实；
    观察材料等级=优质。
```

当前判断：

```text
稳定子集目标上下文桥已闭合。
P9d 全量仍未闭合：全范围对应闭合状态仍为 0，当前场景特征值明确状态仍为 0。
本轮真实运行未自然触发 6146 / 6147。
下一步应继续检查稳定子集目标观察约束进入 6146 等待项和任务执行承接的路径。
```

## 6146 承接定位结果

代码定位：

```text
任务模块.管理界面线程.impl.cpp：
    已能把 已识别区域扫描入账状态 映射为扫描等待项口径；
    扫描 / 跟踪等待项创建时会按目标存在ID + 目标特征类型ID 选择最新可用目标观察约束；
    缺目标区域或目标簇时拒绝创建扫描等待项。

外设线程_D455深度相机.ixx：
    D455_扫描变化等待项前置满足 会读取目标观察约束_按等待项 并调用统一可用性判断；
    扫描变化报告由扫描变化等待项触发，不会裸生成。

自我动作实现.外设模块.ixx：
    6146 自我_扫描已识别区域 已注册并有方法体；
    6146 对优质扫描材料才生成变化候选，首次扫描不写变化动态。
```

真实日志定位：

```text
72430：6150 已创建稳定子集目标上下文和 21 条目标观察约束。
72498 / 72564：任务 D 仍以 当前本能ID=6150 执行。
72530-72548：任务 D 的目标特征仍是 当前观察范围可观测单位存在对应闭合状态。
72717 / 72803：后续 E/F 进入 6060 自我_尝试学习，目标是 方法动作数量，不是 6146。
检索 当前本能ID=6146 / 自我_扫描已识别区域 未命中。
```

当前具体堵点：

```text
稳定子集目标观察约束已经可供 6146 等待项使用，
但当前自然需求链还没有合法生成“已识别区域扫描入账状态”的扫描任务主体。

已定下一切片口径：
    采用稳定子集可先扫描；
    扫描范围为当前已稳定对应的正式 已确认观察存在 全量；
    不选首个目标；
    不等待未稳定候选全范围闭合。

实现边界：
    任务管理负责全量扫描等待项 / 任务承接；
    6150 只写目标上下文、映射项和目标观察约束；
    扫描任务主体和等待项必须保持正式存在为主体，而不是报告 / 队列项 / 目标观察约束。
```

## 稳定子集全量扫描等待项与材料承接结果

断点清单：`实施记录/20260606_P9d稳定子集扫描目标上下文桥_Codex断点清单.md`。

代码推进：

```text
1. 外设观察报告队列新增按目标特征读取每个目标最新可用目标观察约束集合的只读 API。
2. 任务管理在 6150 执行后，按 已识别区域扫描入账状态 批量读取稳定子集可用目标观察约束，并全量创建 已识别场景扫描变化 等待项。
3. D455 目标观察约束判定时间源改为与外设观察队列一致的 steady_clock 毫秒口径。
4. 已消费外设观察报告取消 5 秒特殊清理，改为按正常 TTL / 队列容量清理，保证自我线程承接时可按报告 ID 回查。
```

构建结果：

```text
Debug x64 构建通过，0 错误，1 个 LNK4075 警告。
```

真实日志证据：

```text
日志/鱼巢_run_20260606.低值g:77798
    6150 成功；
    有效可观测单位数量=55；
    已稳定对应存在可观测单位数量=50；
    未稳定对应存在可观测单位数量=5；
    当前观察范围可观测单位存在对应闭合状态=0；
    当前场景特征值明确状态=0；
    扫描可启动状态=1；
    稳定子集扫描目标上下文状态=1；
    稳定子集扫描约束写入数量=50；
    稳定子集扫描约束强匹配数量=50。

日志/鱼巢_run_20260606.低值g:77809
    任务管理提交稳定子集全量扫描等待项；
    可用扫描目标观察约束数量=39；
    扫描等待项提交数量=39；
    不等待全范围闭合=1；
    不选首个目标=1；
    不把目标观察约束当事实主体=1。

日志/鱼巢_run_20260606.低值g:78163-78170
    自我线程已能对已消费扫描报告按 ID 回查成功；
    扫描材料承接完成 成功=是、报告可回查=是；
    等级=可用；
    可进入验证方法=1；
    可生成扫描变化候选=0；
    可进入提交入口组织=0。
```

当前具体堵点：

```text
稳定子集全量扫描等待项、D455 扫描报告、任务管理质量门和自我材料承接已闭合。

仍未自然触发：
    当前本能ID=6146；
    已识别区域扫描正式结果；
    6147 跟踪链。

下一切片应转为：
    扫描材料承接到 6146 任务主体桥。

边界：
    6146 的被作用对象必须是正式稳定对应的 已确认观察存在；
    报告、队列项、材料页和目标观察约束只能作为材料或通信容器；
    可用但非优质扫描材料只能形成材料承接 / 待复验事实，不得生成扫描变化动态候选。
```
