# 20260621 P5 任务回执结构审查表

## 1. 审查结论

P5 已复核任务筹办 / 执行回执结构：

```text
1. 任务推进与筹办结果的机器字段主要为枚举、布尔、任务 / 需求 / 方法 / 场景引用、特征入口、状态引用、动作验证报告摘要和数值完成度。
2. `摘要 / 说明 / 方法运行说明 / 等待原因 / 诊断摘要` 当前定位为日志、显示、等待展示或事件说明，不作为状态推进、候选选择、派生需求生成或结算权威。
3. 已发现并修复一处文本字段参与机器判断：`控制拒绝原因.empty()` 不再参与 `控制请求被拒绝`，该判断只读取结构化 `当前控制响应状态`。
4. P5 不宣称任务模块协议结构全部完成；协议结构总审查转入 P6。
```

不宣称任务类逻辑纠偏完成。

## 2. 结构分类

| 结构 / 字段 | 分类 | 当前处理 |
| --- | --- | --- |
| `结构_任务推进回执::阶段 / 状况 / 建议任务状态 / 建议任务阶段` | 结构化机器回执 | 保留 |
| `结构_任务推进回执::当前任务 / 对应需求 / 当前方法 / 候选方法集 / 可执行输入参数场景` | 项目结构引用 | 保留 |
| `结构_任务推进回执::目标特征类型 / 目标满足关系 / 缺口特征类型 / 缺口满足关系` | 结构化目标与缺口 | 保留 |
| `结构_任务推进回执::实际结果状态集 / 禁止结果状态集 / 需要附带结果状态集` | 状态引用集合 | 保留 |
| `结构_任务推进回执::需要自我线程 / 父任务需要重筹办 / 需要创建或复用子需求 / 当前任务目标达成 / 本轮是否有推进` | 结构化治理布尔 | 保留 |
| `结构_任务推进回执::摘要` | 显示 / 诊断文本 | 保留，但不得作为机器判断 |
| `结构_任务筹办结果::说明` | 日志 / 诊断文本 | 保留，但不得作为机器判断 |
| `结构_任务步骤执行结果_最小::方法运行说明` | 执行诊断文本 | 保留；只进入等待说明和日志 |
| `结构_任务状态推进请求 / 结构_任务状态推进结果` | 任务状态正式提交请求 / 结果 | 保留；状态推进仍由被动提交动作完成 |
| `结构_任务管理结果状态消息` | 工作线程结果状态表面 | 保留；权威字段是 `结果状态 / 结果值 / 是否成功 / 动作验证报告摘要 / 任务价值反馈` |

## 3. 已修复问题

```text
问题：
    `自我线程模块.impl.cpp` 读取任务管理线程补充特征 `控制拒绝原因`，
    并用 `!控制拒绝原因.empty()` 辅助计算 `控制请求被拒绝`。

修复：
    `控制请求被拒绝` 改为只比较结构化 `当前控制响应状态 == 已拒绝`。
    `控制拒绝原因` 仍可进入二次特征候选的原因说明文本。
```

## 4. 验证证据

```text
回执结构定义初扫：
    日志/20260621_P5_回执结构定义初扫.txt

回执文本字段使用初扫：
    日志/20260621_P5_回执文本字段使用初扫.txt

文本回执传播复扫：
    日志/20260621_P5_文本回执传播复扫.txt

文本字段机器分支复扫：
    日志/20260621_P5_文本字段机器分支复扫.txt
    结果：剩余命中主要为日志拼接、说明字段填充、展示性空值判断或结构化特征入口存在性判断。

控制拒绝原因文本分支复扫：
    日志/20260621_P5_控制拒绝原因文本分支复扫.txt
    结果：`控制拒绝原因.empty()` 已无命中；`控制请求被拒绝` 仍保留为结构化状态判断结果。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> '日志\20260621_P5_回执文本分支收口_build.log'
    结果：BUILD_OK。
```

## 5. 下一步

进入 P6：任务模块协议结构审查。
