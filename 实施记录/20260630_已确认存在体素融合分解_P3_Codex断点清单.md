# 20260630 已确认存在体素融合分解 P3 Codex断点清单

## 0. 恢复摘要

当前切片：执行 `计划/20260630_已确认存在体素融合分解施工计划_v0.1.md` 的 P3。

目标：

```text
新增单工作线程或复用合适工作线程承接体素加工任务；
线程只读输入快照并输出候选结果包。
```

本切片不实现：

```text
深度轮廓融合算法；
彩色轮廓分解算法；
父体素提交版本闸门；
内部世界子存在提交；
P1 新增观察证据到 P3 线程的正式触发接线。
```

禁止宣称：

```text
体素融合已经完成；
彩色分解已经完成；
体素加工结果已经正式入账；
线程已经成为事实写入方；
视觉系统完成；
自我苏醒完成或初步成熟完成。
```

## 1. 依据

```text
规范/已确认存在体素融合与分解规范20260630.md
规范/场景体素服务与视觉先验融合规范20260625.md
计划/20260630_已确认存在体素融合分解施工计划_v0.1.md
```

命中规则：

```text
RULE-VOXEL-FUSION-001：体素加工只由新增观察证据触发
RULE-VOXEL-FUSION-008：体素加工线程只能返回候选
RULE-VISION-PRIOR-002：视觉融合只产观察域结果
```

## 2. 已改文件

```text
视觉观察融合模块.ixx
说明书/函数功能说明文档.md
计划/20260630_已确认存在体素融合分解施工计划_v0.1.md
```

## 3. 代码改动口径

在 `视觉观察融合模块.ixx` 中新增体素加工工作线程骨架：

```text
枚举_体素加工线程生命周期状态
枚举_体素加工任务处理状态
枚举_体素加工可提交状态
结构_体素加工任务
结构_体素加工候选结果包
结构_体素加工工作线程配置
结构_体素加工工作线程摘要
体素加工工作线程类
获取全局体素加工工作线程
启动体素加工工作线程
停止体素加工工作线程
提交体素加工任务
读取体素加工候选结果包集合
读取体素加工工作线程摘要
```

实现边界：

```text
提交任务只进入运行期队列。
线程只读取 `结构_体素加工任务` 中的输入快照。
处理函数只调用 P2 的正立方体包围盒检查 helper。
输出只进入内存候选结果包集合。
不写世界树事实。
不创建子存在。
不生成方法动作动态。
```

当前 P3 候选状态：

```text
输入非法 -> 条件不足。
包围盒检查无效或无占据 -> 条件不足。
检查可生成父体素候选 -> 处理状态为已生成候选，但可提交状态仍为待融合点。
是否存在融合点固定为 false，留给 P4 深度轮廓融合候选实现。
```

## 4. 验证预测

```text
Debug x64 构建应通过。
函数说明文档应新增体素加工线程、任务和候选结果包相关函数条目。
```

## 5. 验证结果

构建：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

结果：

```text
退出码 0。
Debug x64 构建通过。
0 个警告，0 个错误。
输出：D:\鱼巢\x64\Debug\鱼巢.exe
```

函数说明：

```powershell
python .\tools\生成函数功能说明文档.py
```

结果：

```text
退出码 0。
已生成：D:\鱼巢\说明书\函数功能说明文档.md
源码文件：129
函数条目：7441
```

## 6. 当前结论

```text
P3 已完成当前代码切片。
体素加工工作线程骨架具备启动、停止、提交任务、读取候选结果包和读取摘要接口。
线程仍只产候选，不接正式事实写入；P4 需要补深度轮廓融合候选和融合点判断。
```
