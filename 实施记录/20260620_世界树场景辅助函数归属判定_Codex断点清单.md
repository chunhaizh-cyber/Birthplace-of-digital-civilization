# 20260620 世界树场景辅助函数归属判定 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.4 P5.1，为世界树场景辅助函数建立归属判定表。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
```

## 1. 问题锚点

```text
世界树类 当前承载一批场景相关辅助结构和函数：
    查找场景存在_按深度或彩图轮廓图
    读取存在场景绝对坐标
    写入存在场景绝对坐标
    计算场景绝对坐标_由参考存在相对坐标
    写入存在场景绝对坐标_由参考存在相对坐标
    计算场景绝对坐标_由相对坐标表
    写入存在场景绝对坐标_由相对坐标表
    场景相对坐标项 / 场景绝对坐标换算结果 / 世界树场景轮廓查找与比较结构
```

当前判断：

```text
本切片只建立归属判定表，不迁移代码、不移动结构体、不改调用方。
扫描结果写入 日志/20260620_世界树场景辅助函数归属扫描.txt。
```

验证前预测：

```text
1. 判定表应列出函数、读写字段、调用服务、返回结构、调用方、建议归属。
2. 计划索引应切到 P5.2 或明确下一步，不直接宣称 P5 完成。
3. 本切片不改 C++ 代码，通常不需要构建。
```

## 2. 当前执行进展

已完成：

```text
1. 扫描世界树场景辅助结构 / 函数声明、定义和外设调用点。
2. 建立归属判定表：实施记录/20260620_世界树场景辅助函数归属判定表.md。
3. 判定下一步第一优先为存在类坐标 helper：读取 / 写入 / 参考换算存在场景绝对坐标。
```

证据：

```text
扫描日志：日志/20260620_世界树场景辅助函数归属扫描.txt。
判定表：实施记录/20260620_世界树场景辅助函数归属判定表.md。
```

验证结果：

```text
本切片只新增文档和计划路由，不改 C++ 代码，未运行构建。
```

禁止宣称：

```text
不宣称世界树场景辅助函数迁移完成。
不宣称场景类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
