# 20260621 计划目录未完成计划索引整理

## 1. 扫描范围

```text
规范/规范目录.md
计划/计划索引.md
计划/*.md 顶层计划文件
实施记录/20260620_语素信息操作入口判定表.md
实施记录/20260620_语素类逻辑纠偏_Codex断点清单.md
```

## 2. 顶层计划纳入结果

```text
计划/ 根目录当前除 计划索引.md 外共有 13 个计划文件。
本轮已全部纳入 计划/计划索引.md 第 2 章当前未完成计划优先队列。
计划/完成项/ 和 计划/待完成项/ 不回拉到当前未完成队列；仍按恢复条件读取。
```

## 3. 堵点检查

```text
当前主动计划仍为语素类逻辑纠偏计划 P2.1。
语素 P2.1 不是整体堵死；剩余重复 helper 中仍有可执行小切片。

已确认的局部堵点：
    自我类.cpp::私有_语素入口相同 只做指针级比较。
    该 helper 位于自我根需求扫描热路径，注释说明用于避免异常陈旧入口指针触发主键字符串拷贝崩溃。
    不能直接替换为当前 语素入口同一，因为 语素入口同一 会在指针不同且非空时读取主键比较。

不堵塞当前继续执行的后续项：
    需求类.cpp::私有_语素入口主键相等 属于指针 + 非空主键比较 helper。
    但该 helper 双空返回 false，替换时应显式保留非空守卫，不能裸替换为 语素入口同一。
    任务模块.管理工作线程.ixx、自我动作实现模块.ixx、自我动作实现.外设模块.ixx、自我线程模块.impl.cpp 仍需后续分小簇复核。
```

## 4. 调整后的执行顺序

```text
P0 继续语素类逻辑纠偏计划 P2.1 的未堵小切片，下一步优先 需求类.cpp。
P1-P9 按 owning-class 边界纠偏池排列：需求、任务、方法、状态、动态、特征、二次特征、存在、场景。
P10 因果类功能函数补完在类级缺口不阻塞后恢复。
P11-P12 横向结构体审计和函数去重清理晚于类级边界与因果主线。
```

## 5. 本轮未运行

```text
未修改源码。
未运行函数说明文档生成。
未运行 Debug x64 构建。
```
