# 20260620 世界树坐标薄封装回迁 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.4 P5.2a，世界树存在场景绝对坐标单参考函数改为薄封装调用存在类 helper。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
计划/20260620_存在类逻辑纠偏计划_v0.1.md
实施记录/20260620_世界树场景辅助函数归属判定表.md
实施记录/20260620_存在场景绝对坐标helper_Codex断点清单.md
```

## 1. 问题锚点

```text
世界树类::读取存在场景绝对坐标
世界树类::写入存在场景绝对坐标
世界树类::计算场景绝对坐标_由参考存在相对坐标
世界树类::写入存在场景绝对坐标_由参考存在相对坐标
```

当前判断：

```text
存在类 1.5 已补齐读取 / 写入 / 单参考换算 helper。
本切片只把世界树四个单参考坐标函数改为薄封装；世界树侧仍保留二次特征刷新编排。
相对坐标表 DTO / 批量换算函数暂不处理。
```

验证前预测：

```text
1. 世界树四个单参考坐标函数内部应调用 存在服务_ 对应 helper。
2. 世界树::写入存在场景绝对坐标 写入成功后仍按所属场景刷新场景存在型基础二次特征。
3. 世界树::写入存在场景绝对坐标_由参考存在相对坐标 不再直接写坐标来源特征，由存在类 helper 承接来源引用写入。
4. 函数说明文档生成退出码应为 0。
5. Debug x64 构建应通过。
```

## 2. 当前执行进展

已修改：

```text
1. 世界树类::读取存在场景绝对坐标 改为直接调用 存在服务_.读取存在场景绝对坐标。
2. 世界树类::写入存在场景绝对坐标 改为调用 存在服务_.写入存在场景绝对坐标，写入成功后仍按所属场景刷新场景存在型基础二次特征。
3. 世界树类::计算场景绝对坐标_由参考存在相对坐标 改为调用 存在服务_.计算场景绝对坐标_由参考存在相对坐标。
4. 世界树类::写入存在场景绝对坐标_由参考存在相对坐标 改为调用 存在服务_.写入存在场景绝对坐标_由参考存在相对坐标，写入成功后仍刷新目标场景存在型基础二次特征。
```

已验证：

```text
rg -n "世界树类::读取存在场景绝对坐标|世界树类::写入存在场景绝对坐标|世界树类::计算场景绝对坐标_由参考存在相对坐标|世界树类::写入存在场景绝对坐标_由参考存在相对坐标|存在服务_\\.读取存在场景绝对坐标|存在服务_\\.写入存在场景绝对坐标|刷新场景存在型基础二次特征" 世界树类.cpp 说明书/函数功能说明文档.md
结果：世界树四个单参考坐标函数已转调 存在服务_ helper；两条写入薄封装仍保留刷新场景存在型基础二次特征调用。

python .\tools\生成函数功能说明文档.py
结果：exit=0，输出见 日志/20260620_世界树坐标薄封装回迁_函数文档.log。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：exit=0，构建日志见 日志/20260620_世界树坐标薄封装回迁_msbuild.log；0 警告。
```

当前判断：

```text
P5.2a 已闭合。
下一项应处理 世界树类::查找场景存在_按深度或彩图轮廓图 的场景候选召回归属；该函数牵涉参数 / 结果 DTO，下一切片先做迁移前置审查与拆分。
```

禁止宣称：

```text
不宣称世界树场景辅助函数迁移完成。
不宣称场景类逻辑纠偏完成。
不宣称存在类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
