# P10f-d live string 字段复扫 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-d 结构体 string 字段 live 复扫与下一清理候选选择。

依据文件：

- `计划/计划索引.md`
- `实施记录/20260623_P10f_因果投影缺失证据结构化接口_Codex断点清单.md`
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_causal_structured.tsv`

复扫结果：

- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_causal_structured.tsv` 已生成。
- 原始结构体 string 字段命中 551 行，其中主键类 248 行，非主键类 303 行。
- P10f-c 旧堵点 `std::vector<std::string> 缺失证据` 在因果 / 世界树 / 自我线程 / 任务筹办接口链已无命中。

当前可执行候选：

- `自我线程模块.ixx::结构_主派发决议` 中以下 string 字段只在结构体内声明，未发现赋值或读取：
  - `可运行队列裁决`
  - `等待队列裁决`
  - `回流队列裁决`
  - `收束停止队列裁决`
  - `任务管理当前去向`
  - `摘要`

当前判断：

- 上述字段不承载当前机器判断，也没有日志输出路径；属于可直接删除的低引用字段组。

下一步：

- 删除 `结构_主派发决议` 上述 6 个未使用 string 字段。
- 构建验证。
- 复扫是否还有同类“只声明未使用”的非主键 string 字段组。

已完成：

- 已删除 `自我线程模块.ixx::结构_主派发决议` 的 6 个未使用 string 字段。
- 已删除 `自我线程模块.消息批次执行器.ixx::结构_线程裁决结果::上层反馈摘要`，以及生成 / 导出 / 传参后被 `(void)` 丢弃的 `生成上层反馈摘要` 文本链。
- 已删除 `自我线程模块.ixx::结构_根层判定结果::上层反馈摘要`。
- 已刷新 `说明书/函数功能说明文档.md`；函数条目为 7185。

验证：

- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo` 已通过，0 警告，0 错误。
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_causal_structured.tsv` 已重新生成，剩余记录数 543。

后续判断：

- 控制面板、SQL 投影、线程快照和日志摘要类字段暂不硬删，除非确认进入机器判断。
- 下一步继续从复扫清单中过滤“非主键类、非显示日志、低引用或进入机器判断”的 string 字段。

## 本轮新增问题记录

问题锚点：`任务模块.治理协议.ixx` / `任务模块.工作线程消息协议.ixx` / `自我线程模块.消息协议.ixx` 中的 `影子验证状态`、`当前恢复点类型`、`排序关系` 等非主键 string 字段。

证据行：

- `任务模块.治理协议.ixx:340` `结构_高阶排序结果::排序关系` 仅声明，无赋值 / 读取命中。
- `任务模块.治理协议.ixx:419-428`、`:470-471`、`:606-607`、`:630-631`、`:636`、`:674`、`:677-679` 多个文本状态 / 恢复字段进入协议结构，但 live 搜索未发现机器消费路径。
- `任务模块.工作线程消息协议.ixx:147` `结构_任务管理消息头::影子验证状态` 只被 `自我线程模块.消息协议.ixx` 复制到治理消息来源诊断，未发现任务侧赋值或结构化判断。

已排除路径：

- 控制面板 / SQL 投影 / 自我线程快照中的摘要类显示字段暂不作为当前机器逻辑堵点。
- `自我动作实现.外设模块.ixx::首个失败说明` 当前只用于日志输出，暂不进入本轮删除。

当前判断：

- 上述任务治理协议文本状态字段不是主键 / ID / 句柄类字段；其中 `影子验证状态`、`当前恢复点类型`、`排序关系` 名义上属于状态 / 排序 / 恢复协议，但没有结构化消费证据，继续保留会违反“状态不通过文字描述判断”的清理口径。

下一步验证：

- 删除无消费文本字段及对应复制语句；构建验证；复扫 TSV，确认这些字段不再命中。

## 本轮收束记录

已完成删除：

- `任务模块.治理协议.ixx`
  - 删除 `结构_高阶排序结果::排序关系`。
  - 删除 `结构_任务管理请求 / 结构_任务管理单步决策 / 结构_任务管理结果` 中无消费的 `来源主观察特征 / 影子验证状态 / 当前影子验证状态 / 当前恢复点类型` 文本字段。
  - 删除 `结构_治理恢复事件镜像 / 结构_治理恢复快照` 中无消费的文本状态、恢复点、快照版本和动态标题字段。
  - 删除 `结构_原语判定结果::主域 / 小域`。
- `任务模块.工作线程消息协议.ixx`
  - 删除 `结构_任务管理消息头::影子验证状态`。
  - 删除 `结构_任务管理派生需求消息::触发动作主体类型文本`。
- `自我线程模块.消息协议.ixx / 自我线程模块.impl.cpp / 自我线程模块.ixx`
  - 删除治理消息来源诊断中的 `影子验证状态 / 来源主观察特征` 复制链。
  - 删除 `投递治理外部反馈事件` 的无消费 `来源主观察特征` 参数。
  - 删除 `结构_主派发输出快照 / 结构_根层判定结果` 中只声明的治理桥文本占位。
  - 删除 `结构_动态V1摘要::变化方向语义`，离散方向只读取数值编码。
  - 删除 `结构_派发占用登记快照::占用队列`。
- `任务模块.运行包.ixx`
  - 删除 `结构_授权约束段::停止条件`。

复扫结果：

- `rg` 确认本轮删除字段无源码残留。
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_causal_structured.tsv` 已刷新。
- 最新复扫记录 641 行，其中主键类 315 行，非主键类 326 行。

验证：

- 多轮 `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo` 通过，最终构建 0 警告、0 错误。
- 未新增 / 删除 / 改名函数；本轮新增字段删除不需要刷新函数说明文档。前序 `上层反馈摘要` 函数删除已刷新 `说明书/函数功能说明文档.md`。

剩余边界：

- `摘要 / 原因 / 说明` 关键词路径复核未发现候选排序、状态推进、稳定键、等待状态或机器判断直接读取文本字段。
- 外设观察报告队列中的 `失败原因 / 配置摘要 / 差异摘要 / 跨帧采集摘要` 当前主要用于日志 / JSON / 控制面板输出；只按 empty/non-empty 控制输出，不作为当前 P10f-d 机器逻辑堵点。
- 控制面板、SQL 投影、线程生命周期、自我线程快照、语言表达和外设报告证据卡显示文本继续按“发现进入机器判断时再清理”的口径处理。

下一步：

- P10f-d 可收束。
- 下一小切片建议为 P10f-e：外设观察报告队列与线程 / 控制面板显示文本边界复核；只在发现文本进入机器判断、候选选择、状态推进或稳定键时删除 / 结构化替代。
