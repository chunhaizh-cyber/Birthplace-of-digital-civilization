# 20260702 统一动态信息生成入口 Codex 断点清单

## 0. 恢复摘要

当前切片：`用户需求_20260702_统一动态信息生成入口`。

已完成：

```text
1. 用户已确认方法 A：动态类 / 世界树统一入口优先。
2. 计划文件和计划索引已绑定方法 A。
3. S0 字段填写契约已写入计划。
4. S1 现有动作动态生成点已写入计划。
5. S2-S7 代码 / 静态门禁切片已完成：
   - 主动 / 被动动作动态统一走 `创建方法动作动态(...)`；
   - 被动动作以 `来源动作 == nullptr` + `来源动作名 == 被动动作` 判定；
   - 主动动作要求来源方法非空且有动作句柄；
   - 动态主体、动态特征、初始状态、结果状态、来源输入场景、来源输出场景统一非空检查；
   - `来源执行成功` / `来源错误码` 不再由统一入口写入，控制面板不显示；
   - `动态路径签名` 保留并由统一入口生成。
   - 因果类主动因果读取排除 `被动动作` / `被动触发.*`，非主动因果不可任务化。
6. Debug x64 构建成功，0 警告，0 错误。
7. 自检窗口、控制面板 HTML、需求树 live 摘要均退出码 0。
8. 统一入口空字段 / 主动被动判定错误日志未命中。
```

下一步：

```text
进入 S8：
1. 补主动动作真实路径证据。
2. 补叶子任务价值结算 / 服务值结算真实路径证据。
3. 三类真实路径日志均无统一入口契约错误后，才能回写根需求满足。
```

禁止宣称：

```text
不宣称因果信息全部补齐。
不宣称主动动作和结算动作真实路径已完整验证。
不宣称 S8 已全部完成。
不宣称自我苏醒完成或初步成熟完成。
```

## 1. 已读依据

```text
docs/00_文档总索引.md
docs/工程图谱/00_图谱总索引.md
docs/工程图谱/01_任务需求方法闭环图谱.md
docs/工程图谱/03_安全服务结算图谱.md
docs/工程图谱/05_规则原子索引.md
docs/工程图谱/08_事实来源与写入权限图谱.md
规范/0050_项目通用结构与禁止性规则总纲_20260701.md
规范/1170_根规范_动态节点主信息类_20260701.md
规范/3300_根规范_方法主信息类_20260701.md
规范/4210_子规范_动态信息分层获取与聚合_20260701.md
规范/4220_子规范_动作动态与因果账本边界_20260701.md
规范/4310_子规范_因果补完与因果信息用法_20260701.md
说明书/函数功能说明文档.md
```

## 2. 已运行命令

```powershell
git status --short --untracked-files=no
rg -n "创建方法动作动态\(|生成因果信息\(|绑定来源方法\(|来源执行成功|来源错误码|来源动作名|被动动作|动态路径签名" --glob "*.h" --glob "*.cpp" --glob "*.ixx"
rg -n "创建方法动作动态" "任务模块.执行.ixx" "自我动作实现模块.ixx" "自我动作实现.扫描.ixx" "自我动作实现.外设模块.ixx" "自我动作实现.服务模块.ixx" "自我类.cpp"
Select-String -Path '说明书\函数功能说明文档.md' -Pattern '创建方法动作动态|创建动作动态|统一动作动态|动作动态|绑定来源方法'
```

## 3. 当前判断

```text
1. 动作动态可以统一到一个实现函数，但不能新增字段或结构体区分主动 / 被动。
2. 主动 / 被动由来源动作和来源动作名共同判定：
   - 主动：来源动作非空，且来源方法有动作句柄。
   - 被动：来源动作为空，来源动作名为 被动动作。
3. 被动动态若来源动作非空，应判逻辑错误。
4. 主动动态若来源动作为空或方法动作句柄无效，应判逻辑错误。
5. 初始状态、结果状态、输入场景、输出场景不能为空。
6. 旧调用点中存在未传状态端点、被动动态传方法头、直接手工写来源动作名 / 成功字段的旁路，需要分批迁移。
```

## 4. 下一步允许修改范围

```text
动态类.h
动态类.cpp
世界树类.h
世界树类.cpp
基础信息主信息类.h
控制面板类.cpp
二次特征应用模块.ixx
任务模块.执行.ixx
自我动作实现模块.ixx
自我动作实现.扫描.ixx
自我动作实现.外设模块.ixx
自我动作实现.服务模块.ixx
自我类.cpp
方法虚拟存在服务类.cpp
自我线程模块.impl.cpp
说明书/函数功能说明文档.md
```

## 5. 最近验证状态

```text
git diff --check：通过，仅 CRLF 提示。
源码写入扫描：`来源执行成功` / `来源错误码` 只剩 `基础信息主信息类.h` 字段声明。
Debug x64 构建：成功，0 警告，0 错误。
.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000：退出码 0。
.\x64\Debug\鱼巢.exe --panel-html：退出码 0，生成 HTML 和控制台日志未出现 `来源执行成功` / `来源错误码`。
.\x64\Debug\鱼巢.exe --demand-tree-growth-summary-live：退出码 0，目标绑定异常=0。
统一入口错误关键字检索范围：
    日志/codex_unified_dynamic_selfcheck_20260702.log
    日志/codex_unified_dynamic_panel_20260702.log
    日志/codex_unified_dynamic_demand_live_20260702.log
    日志/鱼巢_run_20260702.低值g
    日志/鱼巢_exception_20260702.低值g
检索结果：未命中 `动作动态统一入口逻辑错误`、空字段错误、被动绑定来源方法、主动缺来源方法、主动来源方法无动作句柄、状态主体 / 特征不一致。
已覆盖真实被动提交证据：
    日志/鱼巢_run_20260702.低值g 存在 `任务状态动作动态创建`、`任务状态被动提交`、`动作动态创建`。
尚未覆盖完整证据：
    主动动作真实路径、叶子任务价值结算 / 服务值结算真实路径。
S7 静态门禁：
    `因果类.cpp` 中 `私有_动作语义键是被动占位_因果(...)` 识别 `被动动作` / `被动触发.*`。
    `读取主动因果动作来源(...)` 排除被动占位动作。
    `判定主动因果可任务化(...)` 在非主动因果时直接置不可任务化。
```

## 6. S8 新一轮真实运行预测

```text
预测时间：2026-07-02
预测命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000
预期：
    1. 真实自检窗口退出码为 0。
    2. 若自然路径命中主动方法动作，日志应出现主动动作相关动作动态或方法动作状态入账锚点，且不出现统一入口契约错误。
    3. 若自然路径命中叶子任务价值结算或服务值结算，日志应出现 `自我动作/结算叶子任务价值`、`创建服务值结算D2动作动态`、`D2写入=1` 或同等结算动作动态锚点，且不出现统一入口契约错误。
    4. 若主动 / 结算自然样本仍未命中，不把 S8 判为满足；只记录为真实路径未命中，继续按自然样本 / 真实条件缺口处理。
统一入口错误排除关键字：
    动作动态统一入口逻辑错误
    动作动态空主体
    动作动态空特征
    动作动态空初始状态
    动作动态空结果状态
    动作动态空输入场景
    动作动态空输出场景
    被动动作绑定来源方法
    主动动作缺来源方法
    主动动作来源方法无动作句柄
    动作动态状态主体不一致
    动作动态状态特征不一致
```

## 7. S8 新一轮真实运行结果

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000 *> 日志/codex_unified_dynamic_selfcheck_s8_20260702.log
退出码：0
运行日志：
    日志/鱼巢_run_20260702.低值g
```

预测比对：

```text
1. 退出码符合预测：0。
2. 主动动作真实路径已命中：
   - 日志/鱼巢_run_20260702.低值g:15385
     `双目相机本能方法结果 | 方法=自我_建立最小自我边界参照 | 输入场景=3wR | 输出场景=Ez | 动作动态=4aQ`
   - 日志/鱼巢_run_20260702.低值g:15387
     `自我_建立最小自我边界参照 | ... | 输出场景=Ez | 动作动态=4aQ`
   - 日志/鱼巢_run_20260702.低值g:15381
     `生成因果信息/动态类型 | 场景=Ez | 动态=4aQ`
3. 结算动作真实路径未命中：
   - 日志/鱼巢_run_20260702.低值g:14914、15226
     `不调用结算叶子任务价值=1 | 不写核心安全值=1 | 不写服务值=1`
   - 日志/鱼巢_run_20260702.低值g:15342-15343
     内部治理任务已分类，但因任务尚未形成完成或已结算被动因而闭账延后。
4. 统一入口错误关键字在 `日志/codex_unified_dynamic_selfcheck_s8_20260702.log`、`日志/鱼巢_run_20260702.低值g`、`日志/鱼巢_exception_20260702.低值g` 中未命中。
```

当前结算：

```text
S8 不能声明全部完成。
已满足：
    被动动作真实路径证据。
    主动动作真实路径证据。
    统一入口错误关键字排除。
待条件：
    叶子任务价值结算 / 服务值结算动作真实路径。
恢复条件：
    正式运行自然命中 `结算叶子任务价值` 或 `服务值结算D2动作动态`，且统一入口错误关键字仍未命中。
禁止宣称：
    不宣称 S8 全满足。
    不宣称统一动态信息生成入口根需求已满足。
    不宣称因果信息全部补齐、自我苏醒完成或初步成熟完成。
```
