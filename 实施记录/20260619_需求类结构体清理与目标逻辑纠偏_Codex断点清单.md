# 需求类结构体清理与目标逻辑纠偏 Codex 断点清单

日期：2026-06-19

## 0. 恢复摘要

当前主动计划：

```text
计划/20260619_需求类头文件结构体清理与目标逻辑纠偏计划_v0.1.md
```

当前切片：

```text
4.5 因果来源生产者分级依赖缺口。
```

来源返回点：

```text
计划/20260619_因果类功能函数补完计划_v0.1.md
P4 因果动作来源查询函数。
```

已确认缺口：

```text
1. 因果类当前缺少 因果类::查询因果动作来源。
2. 方法类当前缺少 方法类::方法首节点生产目标特征。
3. 需求类.cpp 只有私有_方法首节点生产目标特征，不能按本切片继续让需求类解释方法结果能力。
```

执行边界：

```text
不修改需求类业务逻辑。
先补因果类动作来源查询函数。
若需要方法类公开功能函数，生成或修改方法类逻辑纠偏计划并提升为第一优先。
```

禁止宣称：

```text
不宣称需求类结构体清理完成。
不宣称需求目标逻辑纠偏完成。
不宣称因果类功能补完完成。
```

## 1. 因果动作来源查询函数闭合

已改文件：

```text
因果类.h
因果类.cpp
说明书/函数功能说明文档.md
实施记录/20260619_需求类结构体清理与目标逻辑纠偏_Codex断点清单.md
```

新增函数：

```text
因果类::查询因果动作来源
```

验证命令：

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> .\日志\20260619_需求类4_5因果动作来源_clean_msbuild.log
```

验证证据：

```text
函数说明文档生成成功：源码文件 128，函数条目 6850（临时 clean worktree，排除未提交的 需求类.h 改动）。
Debug x64 构建退出码：0（HEAD + 因果类动作来源补丁的临时 clean worktree）。
```

剩余缺口：

```text
方法类仍缺少公开的 方法类::方法首节点生产目标特征。
按跨类缺口转向规则，下一步生成或修改方法类逻辑纠偏计划，并把该缺口放到第一优先。
```
