# 需求类结构体清理与目标逻辑纠偏 Codex 断点清单

日期：2026-06-19

## 0. 恢复摘要

当前主动计划：

```text
计划/20260619_因果类功能函数补完计划_v0.1.md
```

当前切片：

```text
P4 因果动作来源查询函数迁移收口。
已按最新执行指令继续原计划 P4，迁移需求类来源因果生产者分级，并收掉任务筹办同范围验收命中的因果动作来源直接读取。
```

来源返回点：

```text
计划/20260619_因果类功能函数补完计划_v0.1.md
P4 因果动作来源查询函数。
```

已闭合依赖：

```text
1. 因果类::查询因果动作来源 已补齐。
2. 方法类::方法首节点生产目标特征 已补齐。
3. 需求类.cpp::私有_查询来源因果生产者分级 已改为组合 因果类::查询因果动作来源 与 方法类::方法首节点生产目标特征。
4. 任务模块.筹办.ixx 当前 P4 验收范围内的 因方法模板 / 因动作语义键 / 因动作名称 / 取因果主信息 直接读取已迁移。
```

执行边界：

```text
不新增结构体。
不在需求类或任务筹办中直接解释因果动作来源字段。
若后续发现其它类缺少当前切片必需功能函数，按跨类缺口转向规则修改或生成对应类逻辑纠偏计划，并只执行第一优先缺口。
```

禁止宣称：

```text
不宣称需求类结构体清理完成。
不宣称需求目标逻辑纠偏完成。
不宣称因果类功能补完完成。
```

## 3. P4 来源因果生产者分级迁移

已改文件：

```text
计划/20260619_因果类功能函数补完计划_v0.1.md
计划/20260619_需求类头文件结构体清理与目标逻辑纠偏计划_v0.1.md
需求类.cpp
任务模块.筹办.ixx
```

当前静态证据：

```powershell
rg -n "因方法模板|因动作语义键|因动作名称|取因果主信息\(" -S 需求类.cpp 任务模块.筹办.ixx
```

结果：

```text
无命中。
```

待验证：

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
函数说明文档生成成功：源码文件 128，函数条目 6855。
Debug x64 构建退出码：0。
构建日志：日志/20260619_因果P4来源生产者迁移_msbuild.log。
日志尾部：已成功生成；0 个警告；0 个错误。
```

## 4. P5 被动触发初始因果模板确保函数

当前判断：

```text
1. 现有线程侧 结构_被动触发初始因果规则_线程侧 带规则名、描述、幂等键等文本字段。
2. 现有 私有_按动态生成被动触发因果信息_线程侧 只对证据动态调用 世界树.因果().生成因果信息(动态)，没有把条件比较、果比较、适用层级交给因果类确保初始模板。
3. P5 不需要新增因果主信息字段；因果主信息类已有 条件比较模板、因动作语义键、主果比较模板、适用层级、证据动态样本。
4. P5 不使用规则名、描述或幂等键做机器判断；只把明确的动作语义键作为因果主信息既有字段写入。
```

预期编辑：

```text
1. 因果类.h/.cpp 新增 因果类::确保被动触发初始因果模板。
2. 自我线程模块.impl.cpp 的被动触发增强链调用该函数。
3. 证据动态为空时仍可生成出生期初始模板；证据动态存在时命中或创建模板后追加证据动态样本。
```

待验证：

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

## 1. 因果动作来源查询函数闭合

已改文件：

```text
因果类.h
因果类.cpp
说明书/函数功能说明文档.md
实施记录/20260619_需求类结构体清理与目标逻辑纠偏_Codex断点清单.md
```

新增函数：

```text
因果类::查询因果动作来源
```

验证命令：

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> .\日志\20260619_需求类4_5因果动作来源_clean_msbuild.log
```

验证证据：

```text
函数说明文档生成成功：源码文件 128，函数条目 6850（临时 clean worktree，排除未提交的 需求类.h 改动）。
Debug x64 构建退出码：0（HEAD + 因果类动作来源补丁的临时 clean worktree）。
```

剩余缺口：

```text
方法类缺口已转入 计划/20260619_方法类逻辑纠偏计划_v0.1.md 并闭合。
```

## 2. 方法目标生产判断函数闭合

已改文件：

```text
方法类.h
方法类.cpp
说明书/函数功能说明文档.md
计划/20260619_方法类逻辑纠偏计划_v0.1.md
计划/20260619_需求类头文件结构体清理与目标逻辑纠偏计划_v0.1.md
计划/20260619_因果类功能函数补完计划_v0.1.md
计划/计划索引.md
实施记录/20260619_需求类结构体清理与目标逻辑纠偏_Codex断点清单.md
```

新增函数：

```text
方法类::方法首节点生产目标特征
```

验证命令：

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> .\日志\20260619_方法类目标生产函数_clean_msbuild.log
```

验证证据：

```text
函数说明文档生成成功：源码文件 128，函数条目 6852（临时 clean worktree，排除未提交的 需求类.h 改动）。
Debug x64 构建退出码：0（HEAD + 方法类目标生产函数补丁的临时 clean worktree）。
```

返回状态：

```text
方法类逻辑纠偏计划第一优先已闭合并停止。
需求类计划 4.5 所需依赖函数已闭合并停止。
计划索引已返回 因果类功能函数补完计划 P4。
继续 P4 迁移会触碰 需求类.cpp；按用户“不改需求类，生成缺口然后停下来”的边界，本轮停止在迁移前。
```
