# 20260625 P13b D455 计数平滑工具迁移 Codex断点清单

## 0. 恢复摘要

当前切片：P13b 函数按自定义主信息 / 领域类型分类与全局共享迁移线。

目标函数族：

```text
外设线程_D455深度相机.ixx::D455_计数容差
外设线程_D455深度相机.ixx::D455_正面积
外设线程_D455深度相机.ixx::D455_带滞回容差
外设线程_D455深度相机.ixx::D455_平滑I64
```

当前判断：

```text
四者只使用整数、bool 和标准库比较 / 算术。
不读取 / 写入 D455 结构、外设协议、节点、主信息或项目领域对象。
适合迁移到 `全局共享函数类.ixx`，D455 调用点改为通用函数。
本切片只做去重迁移，不改变原有整数溢出和截断语义。
```

预测：

```text
1. 全局共享模块新增四个纯 I64 / U64 工具。
2. D455 本地四个 helper 删除，调用点改为全局通用函数。
3. 重新生成函数说明文档。
4. `git diff --check`、`python .\tools\生成函数功能说明文档.py` 和 Debug x64 构建通过。
```

## 1. 已执行

```text
1. 在 `全局共享函数类.ixx` 新增：
   - `计数容差I64`
   - `正面积U64`
   - `滞回容差I64`
   - `二比一平滑I64`

2. 删除 `外设线程_D455深度相机.ixx` 本地重复定义：
   - `D455_计数容差`
   - `D455_正面积`
   - `D455_带滞回容差`
   - `D455_平滑I64`

3. D455 调用点改为调用全局通用函数。

4. 重新生成 `说明书/函数功能说明文档.md`。
```

## 2. 验证

```text
旧函数复扫：
    `D455_计数容差`
    `D455_正面积`
    `D455_带滞回容差`
    `D455_平滑I64`
    已从源码和函数说明文档清除。

新函数复扫：
    `计数容差I64`
    `正面积U64`
    `滞回容差I64`
    `二比一平滑I64`
    已进入 `全局共享函数类.ixx`、D455 调用点和 `说明书/函数功能说明文档.md`。

python .\tools\生成函数功能说明文档.py
    成功；源码文件 127，函数条目 7175。

git diff --check
    通过。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    第一次链接失败：`LNK1104`，原因是 14:24:30 启动的 `鱼巢.exe` 进程占用输出文件。
    停止占用进程后重跑构建成功；0 警告，0 错误。

python .\tools\check_specs.py
    hard issues 0，warnings 81。
```

## 3. 停止点

```text
本切片已闭合。
下一步仍按 P13b 从剩余 B 类低风险候选中选择一个不在当前脏改文件中的小函数族。
禁止宣称 P13 完成、函数去重全部完成、自我苏醒完成或初步成熟完成。
```
