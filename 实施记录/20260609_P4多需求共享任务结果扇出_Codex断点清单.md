# P4 多需求共享任务结果扇出 Codex 断点清单

日期：2026-06-09

## 当前切片

```text
P4 多需求共享任务结果扇出。

执行依据：
    计划/计划索引.md
    计划/20260608_任务持续执行与同目标任务去重纠偏计划_v0.1.md
    规范/任务筹办与执行边界规范20260512.md
    规范/方法动作动态硬规则规范20260512.md

进入条件：
    P3b 已闭合并提交 317c736。
    P3c 已落地并提交 1160a15；新强目标升级样本待自然复核。
```

## 允许修改

```text
自我线程模块.impl.cpp
任务模块.管理界面线程.impl.cpp
需求类.cpp
需求类.h
计划/计划索引.md
计划/20260608_任务持续执行与同目标任务去重纠偏计划_v0.1.md
计划/20260609_P4多需求共享任务结果扇出小方案_v0.1.md
实施记录/20260609_P4多需求共享任务结果扇出_Codex断点清单.md

代码修改需在只读梳理后由 P4 小方案明确。
```

## 禁止范围

```text
不得优化相机外设四个方法。
不得修改 结构_任务工作结果。
不得裸写任务状态。
不得绕过 提交任务状态变化。
不得把任务筹办 / 任务执行恢复成本能方法。
不得新增 mock、fixture、伪入口、伪动作动态或 `--*-check` 回归入口。
```

## 实施前预测

```text
预测 1：
    需求类::绑定对应任务 已能把多个需求挂接到同一任务，但完成 / 结算链可能只按任务来源需求处理。

预测 2：
    P4 第一刀应先补只读挂接需求收集和完成后检查入口，不直接裸写所有需求满足。

预测 3：
    若缺正式需求满足回写或价值结算防重复接口，本切片应停在方案 / 日志证据，不强行实现重复结算。
```

## 只读梳理结论

```text
1. 需求类::绑定对应任务 同时写入 对应任务 和去重后的 任务列表，可承载多需求共享同一任务。
2. 自我线程 私有_按任务查找非根需求_线程侧 当前只返回第一个命中需求。
3. 结果状态消息入口 私有_按成功结果满足来源需求并唤醒父任务_任务管理新链 只先处理单个来源需求。
4. 横向结算路径会跳过 候选任务 == 来源任务，不能覆盖同一任务多需求扇出。
5. 价值结算已有任务+需求+结果目标去重键和增量反馈事件键；P4 不新增重复核心价值结算结构。
```

## 本切片实现口径

```text
计划/20260609_P4多需求共享任务结果扇出小方案_v0.1.md 已补。
代码只改 自我线程模块.impl.cpp：
    新增按任务收集挂接需求集；
    用收集函数替换旧单命中扫描；
    结果状态消息处理后对同一任务其他挂接需求逐条走既有满足反馈和父任务唤醒路径。
```

## 运行前预测

```text
构建预测：
    Debug x64 构建应通过；允许继续出现既有 D455相机类.obj LNK4075 警告。

真实运行预测：
    self-check-log 应退出码 0。
    日志应出现 自我线程/共享任务挂接需求扇出扫描。
    若自然共享任务未完成，扇出处理数可为 0，但不能出现任务状态裸写、旧任务域本能调用锚点、任务状态逻辑错误或 [FTL]。
```

## 实施结果

```text
已修改：
    自我线程模块.impl.cpp

实现：
    新增 私有_按任务收集非根需求集_线程侧。
    旧 私有_按任务查找非根需求_线程侧 不再自带扫描循环，只返回收集集首项。
    新增 私有_处理共享任务挂接需求满足反馈_线程侧。
    结果状态成功 / 收束完成 / 任务域已完成路径处理来源需求后，会对同一任务的其他挂接需求逐条调用既有 私有_处理任务满足反馈_线程侧，并独立唤醒父任务。
    共享反馈事件键使用 `原反馈事件|共享|需求|任务`，避免与来源需求增量反馈事件键冲突。

未修改：
    相机外设四个方法。
    结构_任务工作结果。
    任务状态提交入口。
```

## 验证结果

```text
构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码 0；仅既有 D455相机类.obj LNK4075 警告。

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    退出码 0。

日志：
    文件：日志/鱼巢_run_20260609.低值g
    最新窗口起点：81798
    同目标任务复用命中：2
    任务管理结果状态：88
    部分完成结果：84
    成功结果：0
    收束完成结果：0
    共享任务挂接需求扇出扫描：0
    共享任务挂接需求扇出处理：0
    任务状态动作动态：96
    准备本能调用：0
    执行本能方法：0
    任务域本能输入包：0
    任务状态逻辑错误：0
    [FTL]：0
    [ERR]：1，仍为既有 D455 识别条件不足。
```

## 当前判断

```text
P4 最小代码实现已落地并通过构建 / 真实运行。
本轮自然运行未出现共享任务成功或收束完成样本，不能伪造任务完成或动作动态补验收。
后续恢复条件：自然运行出现同一任务挂接多个需求且该任务进入成功 / 收束完成 / 完成态结果入口。
```
