# P10f 因果投影缺失证据结构化接口 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-c 后续：因果投影缺失证据结构化接口专项。

依据文件：

- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `规范/因果信息用途与用法规范20260516.md`
- `计划/计划索引.md`
- `实施记录/20260623_P10f_string字段复扫当前停止点_Codex断点清单.md`
- `实施记录/20260623_P10f_正式结算证据缺失项文本接口阻塞分析.md`

当前问题锚点：

- `因果类.h/.cpp` 与 `世界树类.h/.cpp` 公开查询接口仍以 `std::vector<std::string>& 缺失证据` 输出因果投影缺失项。
- `自我线程模块.impl.cpp::结构_线程结算因果投影预判` 和 `结构_叶子任务正式结算证据` 因此继续保存 `缺失证据 / 摘要` 文本字段。

本切片目标：

1. 将因果缺失证据源头改为结构化缺失项，缺失类型使用枚举，相关节点 / 因果 / 特征 / 状态 / 动态使用指针或主键槽。
2. `任务模块.筹办.ixx` 只保留数量统计，不按文本判断。
3. `自我线程模块.impl.cpp` 删除上述两个正式结算证据结构体的 `摘要` 字段；缺失证据改用结构化缺失项，日志即时渲染。

预测验证：

- `rg -n "std::vector<std::string>.*缺失证据|std::vector<std::string>& 缺失证据" 因果类.h 因果类.cpp 世界树类.h 世界树类.cpp 自我线程模块.impl.cpp 任务模块.筹办.ixx` 不再命中本切片接口链。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo` 通过。

实际完成：

- `因果类.h/.cpp` 新增 `枚举_因果缺失证据类型` 与 `结构_因果缺失证据项`，因果缺失项源头改为枚举 + 关联节点 / 因果 / 特征 / 状态 / 动态 / 主键槽。
- `世界树类.h/.cpp` 的因果投影、条件目标、主果方向、因果链、自检投影接口同步改为结构化缺失项。
- `任务模块.筹办.ixx` 只读取结构化缺失项数量，不再接收文本缺失数组。
- `自我线程模块.impl.cpp::结构_线程结算因果投影预判` 与 `结构_叶子任务正式结算证据` 已删除 `摘要` 字段，`缺失证据` 改为结构化缺失项；日志文本改为即时渲染。
- 已运行 `python .\tools\生成函数功能说明文档.py`，更新 `说明书/函数功能说明文档.md`。

实际验证：

- 首次构建失败 1 次，原因为 `私有_移除正式结算缺失证据_线程侧` 签名改造后缺前置声明；已补前置声明。
- 复跑 `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo` 通过，0 警告，0 错误。
- `rg -n "std::vector<std::string>.*缺失证据|std::vector<std::string>& 缺失证据" 因果类.h 因果类.cpp 世界树类.h 世界树类.cpp 自我线程模块.impl.cpp 任务模块.筹办.ixx` 无命中。
- `git diff --check -- 因果类.h 因果类.cpp 世界树类.h 世界树类.cpp 自我线程模块.impl.cpp 任务模块.筹办.ixx 说明书\函数功能说明文档.md 实施记录\20260623_P10f_因果投影缺失证据结构化接口_Codex断点清单.md` 通过。

当前下一步：

- P10f-c 已闭合；返回 P10 后续 live 复扫，重新生成结构体 string 字段清单，确认是否还有进入机器判断的 `备注 / 摘要 / 说明 / 原因 / 等待原因 / 缺失证据` 类字段。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
