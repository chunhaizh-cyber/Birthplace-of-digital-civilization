# 20260703 存在体素融合内部治理 Codex断点清单

## 0. 恢复摘要

当前计划：

```text
P12：计划/20260703_存在体素融合内部治理实现映射计划_v0.1.md
当前切片：S6 验证和实施记录
当前结论：P12 S5 已从重型“正式提交入口”收窄为轻量内部写入治理入口并完成代码接入；S6 静态检查和 Debug x64 构建通过；P12 第一版代码切片已闭合。
```

禁止宣称：

```text
不得宣称存在体素融合内部治理链已代码实现；
不得宣称真实 D455 样本已触发稳定体素主模型更新；
不得宣称 C -> E 最终裁决已实现；
不得宣称局部体素已经能合法更新 E_voxel；
不得宣称场景体素刷新链已完整接入；
不得宣称 D5 缺口到需求派生链已完成。
```

下一步：

```text
P12 第一版无新的默认代码实施切片。
若继续 P12，应做真实 D455 自然样本验证或按新计划承接场景体素服务增强 / 先验发布；
若切入 P3-P10 任一桥接实现，必须从对应实现映射计划 S0 只读扫描开始。
```

## 13. S5 / S6 代码实施与验证

当前切片：

```text
P12 S5：E 体素特征主模型写入治理入口
P12 S6：验证和实施记录
```

已实施编辑：

```text
规范/详细设计/待完成/20260703_存在体素融合内部治理详细设计_v0.1.md
    明确 P12 第一版闭合到 E 体素特征主模型写入治理入口；
    派生轮廓重算、可见性索引和场景体素刷新请求改为后续 D6 / D7 或场景体素服务增强计划承接；
    验证剧本改为：S5 最小校验失败直接丢弃候选，不自动派生需求或写缺口。

计划/20260703_存在体素融合内部治理实现映射计划_v0.1.md
    移除 S6 场景体素刷新请求实施切片；
    S6 改为验证和实施记录；
    项目结构映射、代码入口映射和流程状态表不再把场景体素刷新请求列为 P12 第一版目标。

自我动作实现.外设模块.ixx
    新增 P12 第一版主模型候选判断；
    新增 P12 主模型融合候选最小写入校验；
    新增 P12 合格主模型融合候选写入 E 稳定三维体素主模型的轻量治理入口；
    复用 `世界树.特征().写入三维体素二分层链` 写入 E 的 `特征_三维体素模型`；
    摘要仍可写入多个运行宿主，但 E 稳定三维体素主模型写入只允许一次受控调用，避免同一次观察重复写 E。

视觉观察融合模块.ixx
    体素加工候选结果包携带 `父体素占据候选快照`，供 S5 写入治理入口复用融合后的候选体素模型。

说明书/函数功能说明文档.md
    已运行生成脚本更新新增 / 变更函数条目。
```

当前判断：

```text
S5 是自我内部治理写入入口，不是 C -> E 最终事实裁决入口；
无旧稳定体素主模型时允许作为第一版主模型候选；
最小校验失败直接丢弃候选，不自动派生需求或写 D5 缺口；
P12 第一版不提交场景体素刷新请求、不新增存在体素变化事件、不直接调用场景体素服务。
```

已运行命令：

```text
python .\tools\生成函数功能说明文档.py
git diff --check
python .\tools\check_specs.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
函数说明生成：成功，函数条目 7650。
git diff --check：无空白错误，仅提示 `说明书/函数功能说明文档.md` 后续 Git 触碰时 CRLF 将替换为 LF。
check_specs：hard issues = 0，warnings = 0。
Debug x64 构建：成功，0 警告，0 错误。
```

未验证边界：

```text
未用真实 D455 自然样本验证 S5 写入路径实际触发；
未验证 E 稳定三维体素主模型在自然观察链中真实发生更新；
未接入场景体素刷新请求；
未接入 D5 缺口到需求派生链。
```

## 12.1 S5 口径修订记录

用户裁决：

```text
体素加工是自我内部治理过程；
P12 S5 不需要按 C -> E 事实提交那样重新确认 E、强制要求旧稳定版、维护复杂来源 / 消费状态或失败分流；
融合后只需用融合后的体素模型与深度 / 彩色轮廓做最小可行性校验；
写入可复用特征值写入函数或补世界树 / 领域类中的 E 体素专用写入函数；
失败第一版直接丢弃。
```

已同步修订：

```text
规范/详细设计/待完成/20260703_存在体素融合内部治理详细设计_v0.1.md
计划/20260703_存在体素融合内部治理实现映射计划_v0.1.md
计划/计划索引.md
docs/current-state.md
```

当前 S5 目标：

```text
新增或接入轻量 E 体素特征主模型写入治理入口；
内部可复用 `特征类` 底层三维体素写入函数；
入口本身只承载最小材料一致性校验和 E 稳定三维体素主模型写入；
不得由外设、视觉融合层、场景体素服务、控制面板或体素加工线程绕过该入口裸写 E 主模型。
```

## 12.2 S5 旧阻塞记录（已被 12.1 修订口径覆盖）

当前切片：

```text
P12 S5：旧口径下的正式提交入口对接。
```

扫描结论：

```text
未找到 `P12 主模型融合候选 -> E 稳定体素主模型正式提交` 的专用入口。

已找到但不能替代 S5 的入口：
    特征类::写入三维体素二分层链 / 写入三维体素长方体二分层链：
        低层体素写入 API，不包含 P12 主模型候选裁决、版本闸门、拒绝分流和来源证据治理。
    写入观察存在三维体素特征：
        根据观察簇空间 AABB 写入已确认观察存在的粗三维体素模型，并提交体素加工任务；不是 P12 主模型融合候选正式提交。
    写入内部世界子存在局部体素模型：
        提交入口裁决后为内部世界子存在写入局部正立方体体素模型；目标是子存在，不是 E 稳定体素主模型更新。
```

旧口径下的当前判断：

```text
按当时 `计划/20260703_存在体素融合内部治理实现映射计划_v0.1.md` 第 8 章停机条件，S5 必须停止。
继续用低层写入 API 或子存在写入入口会绕过正式提交裁决。
不得宣称稳定体素主模型更新已接入。
S6 场景体素刷新请求不能继续执行，因为没有合法稳定体素版本更新来源。
```

已运行命令：

```text
rg -n "提交.*体素|体素.*提交|稳定体素.*更新|更新.*稳定体素|写入.*三维体素|读取三维体素特征值|写入三维体素|三维体素模型|体素模型版本|体素融合主模型|主模型候选|体素融合主模型候选" 自我动作实现.外设模块.ixx 特征类.h 特征类.cpp 存在类.h 存在类.cpp 世界树类.h 世界树类.cpp 场景体素模块.ixx 视觉观察融合模块.ixx
```

旧恢复条件：

```text
补齐 `P12 主模型融合候选 -> E 稳定体素主模型正式提交` 的入口设计和代码落点后，从 S5 重新执行。
```

## 11. S4 主模型融合候选

当前切片：

```text
P12 S4：主模型融合候选。
```

已实施编辑：

```text
自我动作实现.外设模块.ixx
    新增 P12 体素融合主模型候选状态、阻断原因掩码、来源观察证据ID、基准体素版本、融合点数量、冲突数量、越界数量、主模型更新候选数量特征模板；
    将 S4 特征族纳入既有 P11 / P12 共享关联判断结果特征登记；
    扩展 P12 只读摘要运行期材料，保存主模型候选状态、阻断原因、来源证据、基准版本和候选计数；
    复用体素加工候选结果包的融合点检查、父候选外边界检查、子候选局部体素检查，以及 S2 / S3 前置状态，生成主模型融合候选摘要；
    基准版本失配、缺融合点、越界、冲突、缺可生成子体素候选或缺待处理材料时，只写主模型候选阻断原因。

说明书/函数功能说明文档.md
    已运行生成脚本更新新增函数条目。
```

当前判断：

```text
S4 只生成主模型融合候选摘要，写入 I64 状态、掩码、来源证据、版本和计数；
不覆盖稳定体素主模型；
不创建内部世界子存在；
不生成动作动态；
不提交场景体素刷新事件；
正式写事实仍留给 S5 提交入口对接。
```

已运行命令：

```text
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
python .\tools\check_specs.py
git diff --check
```

验证结果：

```text
msbuild：成功，0 警告，0 错误。
check_specs：hard issues = 0，warnings = 0。
git diff --check：无空白错误，仅函数说明文档 CRLF 提示。
```

## 10. S3 输入证据子轮廓和子体素候选

当前切片：

```text
P12 S3：输入证据子轮廓和子体素候选。
```

已实施编辑：

```text
自我动作实现.外设模块.ixx
    新增 P12 体素融合子轮廓候选状态、子轮廓候选阻断原因掩码、子轮廓候选数量、可生成局部子体素候选数量、父候选触碰外边界数量、无融合点候选包数量特征模板；
    将 S3 特征族纳入既有 P11 / P12 共享关联判断结果特征登记；
    扩展 P12 只读摘要运行期材料，保存子轮廓候选状态、阻断原因和候选计数；
    复用既有体素加工候选结果包、子轮廓候选集合、局部子体素候选检查、父候选外边界和融合点检查材料，收束 S3 阻断原因；
    待处理材料未生成或未命中同键已存在时，S3 只写缺待处理材料阻断。

说明书/函数功能说明文档.md
    已运行生成脚本更新新增函数条目。
```

当前判断：

```text
S3 只把体素加工候选结果包中的子轮廓、局部子体素、深度支撑、父候选外边界和融合点材料收束为 I64 状态、掩码和计数；
外边界占据、无融合点、质量不足、缺深度支撑或缺待处理材料只进入候选阻断原因；
不写稳定体素主模型；
不创建内部世界子存在；
不生成动作动态；
不提交场景体素刷新事件。
```

已运行命令：

```text
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
python .\tools\check_specs.py
git diff --check
```

验证结果：

```text
msbuild：成功，0 警告，0 错误。
check_specs：hard issues = 0，warnings = 0。
git diff --check：无空白错误，仅函数说明文档 CRLF 提示。
```

## 9. S2 待处理材料和幂等闸

当前切片：

```text
P12 S2：待处理材料和幂等闸。
```

已实施编辑：

```text
自我动作实现.外设模块.ixx
    新增 P12 体素融合待处理状态、待处理幂等键、待处理挂起原因掩码、待处理重复命中次数特征模板；
    将 S2 特征族纳入既有 P11 共享关联判断结果特征登记；
    扩展 P12 只读摘要运行期材料，保存待处理状态、幂等键、挂起原因和重复命中次数；
    新增 P12 幂等键构造逻辑：按目标已确认存在 E、观察证据ID、基准体素版本和来源报告ID混合为 I64 键；
    在同一宿主发现同键待处理项时，只标记同键已存在并累计重复命中次数。

说明书/函数功能说明文档.md
    已运行生成脚本更新新增函数条目。
```

当前判断：

```text
S2 只把同一 E、同一观察证据和同一基准体素版本收束为同一待处理材料；
输入组阻断、缺幂等材料或基准版本失配时只写挂起原因；
重复触发只更新待处理状态和重复命中次数；
不生成子体素候选；
不写稳定体素主模型；
不创建内部世界子存在；
不生成动作动态；
不提交场景体素刷新事件。
```

已运行命令：

```text
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
python .\tools\check_specs.py
git diff --check
```

验证结果：

```text
msbuild：成功，0 警告，0 错误。
check_specs：hard issues = 0，warnings = 0。
git diff --check：无空白错误，仅函数说明文档 CRLF 提示。
```

## 8. S1 体素融合输入组只读视图

当前切片：

```text
P12 S1：体素融合输入组只读视图。
```

已实施编辑：

```text
自我动作实现.外设模块.ixx
    新增 P12 体素融合输入组状态和阻断原因掩码；
    新增 P12 体素融合输入观察证据ID / 基准版本 / 当前版本 / 候选包数量 / 局部子体素候选数量 / 有深度支撑子候选数量特征模板；
    新增 读取P12体素融合输入组只读摘要；
    新增 写入P12体素融合输入组只读摘要；
    将 P12 特征族纳入既有 P11 共享关联判断结果特征登记；
    在被动识别外设观察材料摘要写入后生成 P12 只读摘要。

说明书/函数功能说明文档.md
    已运行生成脚本更新新增函数条目。
```

当前判断：

```text
S1 只读摘要只读取被动识别确认方案项、目标已知存在、稳定体素模型句柄、体素模型版本、体素加工基准版本、体素加工候选结果包和子轮廓候选数量。
摘要只写 I64 状态、掩码、版本、证据ID和计数；
不写稳定体素主模型；
不创建内部世界子存在；
不生成动作动态；
不提交场景体素刷新事件。
```

已运行命令：

```text
python .\tools\生成函数功能说明文档.py
git diff --check
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
python .\tools\check_specs.py
```

验证结果：

```text
msbuild：成功，0 警告，0 错误。
check_specs：hard issues = 0，warnings = 0。
git diff --check：无空白错误，仅函数说明文档 CRLF 提示。
```

## 1. S0 依据

读取依据：

```text
docs/current-state.md
docs/decision-log.md
README.md
docs/00_文档总索引.md
docs/工程图谱/00_图谱总索引.md
docs/工程图谱/02_外设观察事实图谱.md
docs/工程图谱/05_规则原子索引.md
规范/规范目录.md
规范/0050_项目通用结构与禁止性规则总纲_20260701.md
规范/1175_根规范_特征表达与判断收束规则_20260703.md
规范/6240_子规范_已确认存在体素融合与分解_20260701.md
规范/6250_子规范_场景体素服务与视觉先验融合_20260701.md
规范/详细设计/待完成/20260703_存在体素融合内部治理详细设计_v0.1.md
计划/20260703_存在体素融合内部治理实现映射计划_v0.1.md
计划/计划索引.md
```

关键约束：

```text
1175：可融合、可提交、归属可信、材料可回查等判断型状态不得由普通 bool 或外设直接写成一阶事实。
6240：体素加工线程只能生成候选结果包，正式体素更新和内部世界子存在创建必须经提交入口裁决。
6250：场景体素快照只读，视觉先验只能辅助候选、评分、解释和缺口，不能替代事实源。
P12：S0 只做代码事实扫描，不改 C++。
```

## 2. S0 扫描范围

扫描命令摘要：

```text
rg -n "体素融合|体素加工|局部体素|稳定体素|三维体素|子体素|子轮廓|融合候选|候选结果包|场景体素刷新|SceneVoxelPrior" 说明书/函数功能说明文档.md

rg -n "结构_体素|体素加工|局部体素|稳定体素|三维体素|子体素|子轮廓|候选结果包|融合候选|读取.*体素|写入.*体素|比较.*体素" 特征类.h 特征类.cpp 存在类.h 存在类.cpp 场景类.h 场景类.cpp 场景体素模块.ixx 视觉观察融合模块.ixx 自我动作实现.外设模块.ixx 外设观察报告队列.ixx

rg -n "映射缺口原因掩码|观察候选同一性|同一性评分|目标已确认观察存在|局部体素|体素子轮廓|P11共享|共享关联" 自我动作实现.外设模块.ixx 视觉观察融合模块.ixx 外设观察报告队列.ixx

rg -n "bool .*体素|可融合|可提交|归属可信|材料可回查|可回查|可生成|可用|融合点|外边界|基准.*版本|generation|坐标系版本" 特征类.h 特征类.cpp 场景体素模块.ixx 视觉观察融合模块.ixx 自我动作实现.外设模块.ixx 外设观察报告队列.ixx
```

## 3. 已有可复用入口

### 3.1 体素加工候选链

```text
视觉观察融合模块.ixx:
    结构_体素加工任务
    结构_体素加工候选结果包
    结构_体素子轮廓候选
    检查体素融合点
    生成子轮廓候选集合
    体素加工工作线程类::提交任务
    体素加工工作线程类::处理任务_
    读取体素加工候选结果包集合
```

事实：

```text
体素加工工作线程只把任务转换成候选结果包；
候选结果包保存父存在、输入观察证据ID、基准体素版本、融合点检查、父体素检查、子轮廓候选集合、子候选数量和处理状态；
`处理任务_` 明确注释为“正式入账留给提交入口”。
```

### 3.2 局部体素 / 子轮廓材料

```text
结构_体素子轮廓候选：
    候选ID
    局部子体素候选ID
    状态
    深度支撑率Q10000
    深度 / 空间支撑像素数量
    局部体素占据候选
    局部体素检查
```

事实：

```text
彩色连通区域无深度空间支撑时只成为表面区域；
有深度空间支撑时才生成局部体素占据候选；
局部体素候选已经过正立方体包围盒检查。
```

### 3.3 稳定体素读写基础

```text
特征类.h:
    写入三维体素特征值
    读取三维体素特征值
    写入三维体素二分层链
    读取三维体素占据位
    查询三维体素二分层链
    比较局部三维体素与三维体素链
    比较局部轮廓深度图与三维体素链
```

事实：

```text
已有稳定三维体素的基础读写和只读比较函数；
S1 可先读稳定体素句柄和版本，不应直接调用写入主模型。
```

### 3.4 P11 C_E 候选材料

```text
自我动作实现.外设模块.ixx:
    特征_目标已确认观察存在
    特征_外设观察存在_自我存在同一性评分
    特征_外设观察存在_自我存在同一性状态
    特征_外设观察存在自我归属状态
    特征_外设观察存在同一性证据帧数
    特征_映射缺口原因掩码
```

事实：

```text
识别摘要和候选方案项已写入同一性状态、同一性评分、归属状态、目标已确认观察存在指针和冲突 / 证据不足材料；
这些可以作为 P12 S1 只读输入，不能当成 C -> E 最终事实。
```

### 3.5 观察体素证据与版本闸门

```text
自我动作实现.外设模块.ixx:
    特征_体素观察证据ID
    特征_体素观察证据消费状态
    特征_体素观察证据来源报告ID
    特征_体素观察证据来源簇ID
    特征_体素模型版本
    特征_体素加工基准体素版本
    特征_体素加工候选结果包状态
    检查父体素候选版本闸门
    写入观察存在三维体素特征
```

事实：

```text
`写入观察存在三维体素特征` 会写观察证据ID、来源报告ID、来源簇ID、基准体素版本、候选结果包状态和消费状态；
现有路径会写粗三维体素长方体链，并尝试提交体素加工任务；
版本不匹配时已有“版本变化挂起”状态。
```

### 3.6 子候选映射和内部世界子体素

```text
自我动作实现.外设模块.ixx:
    查找体素颜色候选结果包
    查找体素子候选映射
    写入体素子候选映射与颜色事实
    写入内部世界子存在局部体素模型
    提交_确认内部世界子存在事实
```

事实：

```text
已有内部世界子存在局部体素写入入口；
该入口服务于“内部世界子存在确认”，不是稳定体素主模型融合治理；
P12 不应复用它绕过稳定体素主模型提交入口。
```

### 3.7 场景体素刷新入口

```text
场景体素模块.ixx:
    结构_场景体素维护事件
    枚举_场景体素维护事件类型::同步当前场景快照
    提交场景体素维护事件
    同步当前场景快照
```

事实：

```text
目前事件域只有“同步当前场景快照”；
经 2026-07-03 用户裁决，场景体素刷新请求已移出 P12 第一版；
后续若需要刷新，由 D6 / D7 或场景体素服务增强计划重新承接；
场景体素服务不得被体素融合链直接修改。
```

## 4. 主要缺口

```text
1. 没有稳定体素主模型更新候选结构化承载入口。
2. 没有稳定体素主模型正式更新提交入口。
3. 没有融合缺口到 D5 结构化缺口链的完整映射。
4. 场景体素刷新事件值域仍窄，只有同步当前场景快照。
5. 体素加工候选包状态仍主要服务已有粗体素 / 子存在颜色链，尚未形成 P12 的稳定体素主模型治理链。
```

## 5. 普通 bool / 判断型枚举风险

发现的普通 bool / 判断型状态包括：

```text
结构_体素融合点检查结果::有融合点()
结构_体素颜色平均统计::有效
结构_体素子轮廓候选::有深度空间支撑
结构_体素加工候选结果包::是否存在融合点
结构_体素加工候选结果包::是否触碰外边界
结构_体素加工候选结果包::是否局部包围盒压紧
场景体素模块::结构_正立方体体素包围盒检查结果 中的 输入有效 / 外边界为空 / 触碰外边界 / 有占据 / 可作为完整体素候选
自我动作实现.外设模块.ixx 中的 父体素候选版本闸门可裁决 / 体素子轮廓候选具备局部子体素 / 内部世界子存在局部体素输入可用
```

处理口径：

```text
这些可作为运行期辅助判断或局部函数返回；
不得直接扩张为项目一阶事实；
S1 若需要后续读取，必须收束为 I64 状态、计数、评分、版本、缺口原因掩码或可回查句柄。
```

## 6. S0 结论

```text
S1 可执行。
建议 S1 只做“体素融合输入组只读视图 / 阻断摘要”，不写稳定体素。
S1 第一版复用现有 P11 C_E 候选材料、体素观察证据材料、体素加工候选结果包、局部子体素候选和基准版本材料。
S1 输出应落到既有特征族或运行期只读视图；不得新增持久业务结构体。
```

## 7. S0 验证

```text
S0 为只读扫描 + 文档回写。
未修改 C++。
未运行 C++ 构建。
后续文档切片需运行：
    python .\tools\check_specs.py
    git diff --check
```
