# 20260701 外色视频接口 30fps 入队优化 Codex断点清单

## 0. 恢复摘要

当前切片：对 D455 外设线程的色视频材料入队路径做性能优化，使外设线程默认按 30fps 目标向 `外设观察报告队列` / `D455短期观察材料页` 供料。

已改文件：

```text
外设线程_D455深度相机.ixx
双目相机本能适配器.h
双目相机本能适配器.cpp
控制面板WebView2.ixx
控制面板WebView2.cpp
说明书/函数功能说明文档.md
```

核心改动：

```text
1. D455 外设线程默认采样间隔从 300ms 调整为 33ms。
2. 外设线程采样循环从“处理完成后 wait_for 固定间隔”改为按下一帧目标时间 wait_until，避免处理耗时叠加到采样周期。
3. 双目相机适配器 `采集一帧` 增加可选融合帧数参数，旧调用默认仍为 3 帧；外设线程显式传 1 帧，避免色视频队列生产端被三帧融合压到约 10fps。
4. 外设线程运行中结果统计改为增量维护，避免每帧全量扫描和复制历史样本。
5. 停止时新增入队速率摘要日志：`外设线程_D455深度相机/入队速率摘要`，验证口径看 `入队FPSx100 >= 3000`。
```

遵守边界：

```text
1. 外设线程仍只供料，不直接写世界树真值、需求树或价值结算。
2. 入队速率日志只作诊断证据，不作为机器逻辑或业务状态。
3. 中间层材料仍只写内存队列和短期材料页，不入数据库。
```

已运行命令：

```powershell
git diff --check -- 外设线程_D455深度相机.ixx 双目相机本能适配器.h 双目相机本能适配器.cpp
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
python .\tools\生成函数功能说明文档.py
git diff --check -- 控制面板WebView2.ixx 控制面板WebView2.cpp 说明书\函数功能说明文档.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

结果：

```text
外色视频优化文件 git diff --check：通过。
首次 Debug x64 构建：失败，失败点在未修改文件 `控制面板类.cpp:17085`，缺少 `保存控制面板WebView2自我场景窗口截图` 标识符。
已补齐 `控制面板WebView2` 模块导出和真实 WebView2 `CapturePreview` PNG 截图实现，重新生成函数说明文档。
控制面板截图修复文件 git diff --check：通过；仅有 `说明书/函数功能说明文档.md` 的 CRLF/LF 工作区提示。
第二次 Debug x64 构建：通过，0 警告，0 错误。
```

当前判断：

```text
代码层优化已完成；既有控制面板截图函数缺口已补齐，全项目 Debug x64 构建已通过。
真实 30fps 不能用模拟样本证明；需要可用 D455 / RealSense 环境启动外设线程，停止后检查日志 `入队FPSx100` 是否达到或超过 3000。
```

下一步：

```text
1. 停止旧 `D:\鱼巢\x64\Debug\鱼巢.exe` 进程，改用当前 worktree 的 `x64\Debug\鱼巢.exe` 运行。
2. 在真实 D455 硬件环境下启动外设线程，停止后检查 `外设线程_D455深度相机/入队速率摘要`。
3. 若 `入队FPSx100 < 3000`，继续查采集转换、队列复制和 RealSense 取帧耗时。
```

禁止宣称：

```text
不宣称真实硬件 30fps 已验证。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
不把已修复的控制面板构建阻塞写成本次外色视频逻辑失败。
```
