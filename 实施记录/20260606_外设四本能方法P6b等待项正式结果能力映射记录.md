# 20260606_外设四本能方法P6b等待项正式结果能力映射记录

## 切片

```text
计划：计划/20260605_外设四本能方法需求树生成因果链修订计划_v0.1.md
切片：P6b 等待项口径正式结果能力映射最小接入
目标：让任务管理等待项口径先通过识别 / 扫描 / 跟踪正式结果能力命中，兼容本能ID只保留为兜底。
```

## 修改前预测

```text
私有_外设观察需求口径_按方法供料要求 只命中旧 外设识别 / 扫描 / 跟踪结果场景状态 时，P1-P5 后的正式四方法会落到兼容本能ID兜底。
补入正式结果能力特征映射后，识别 / 扫描 / 跟踪应优先以方法结果能力确定等待项口径。
本切片不按需求目标特征名映射，不把等待项写入需求树或世界树。
```

## 已修改

```text
任务模块.管理界面线程.impl.cpp：
    私有_外设观察需求口径_按方法供料要求 新增正式结果能力映射。

识别口径新增命中特征：
    外设观察存在候选确认方案取得状态
    外设观察存在_自我存在同一性状态

扫描口径新增命中特征：
    已识别区域扫描入账状态
    已归属存在当前特征_基准特征差异状态

跟踪口径新增命中特征：
    目标存在跟踪状态
    跟踪预测区间状态
    跟踪区间迁移状态
    目标存在丢失状态
    目标存在重捕获状态

旧 外设识别 / 扫描 / 跟踪结果场景状态 兼容路径保留。
兼容本能ID兜底保留。
```

## 验证

```text
git diff --check -- 任务模块.管理界面线程.impl.cpp：
    退出码 0；
    仅 LF/CRLF 提示。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m：
    退出码 0；
    0 个错误；
    1 个链接警告 LNK4075：因 /INCREMENTAL:NO 忽略 /EDITANDCONTINUE。

未真实运行鱼巢.exe。
不宣称任务筹办到外设等待项自然业务链已通过。
```

## 下一步

```text
进入 P6c：
    对账观察 / 验证入口是否需要单独等待项口径；
    若需要，再切最小实现，不直接把观察口径混入识别口径。
```
