# 20260625 P13b 指针日志文本复用全局工具 Codex断点清单

## 0. 恢复摘要

当前切片已闭合。已删除 `自我动作实现模块.ixx` 和 `自我动作实现.外设模块.ixx` 中重复的 `指针日志文本(const void*)`，改为复用 `全局共享函数类.ixx` 的通用日志格式化函数。

依据：

```text
计划/计划索引.md：P13b 全局共享迁移。
计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md：B 类函数已有同功能全局函数则复用，不重复新增；确需新增通用函数时放入全局共享函数类。
说明书/函数功能说明文档.md：当前存在两个同名同签名 `指针日志文本` 本地定义。
```

当前判断：

```text
两个本地定义完全一致：`std::to_string(reinterpret_cast<std::uintptr_t>(指针))`。
该函数只用于日志文本，不承载机器判断、状态推进、候选选择或项目结构身份。
两个调用模块均已 import `全局共享函数类`，迁移不会新增领域 import。
```

下一步：

```text
1. 继续 P13b，从剩余 B 类候选中选择下一个低风险同功能重复 helper。
2. 迁移 / 复用前继续先查 `全局共享函数类.ixx` 和 `说明书/函数功能说明文档.md`。
```

已完成：

```text
1. 在 `全局共享函数类.ixx` 新增 `指针日志文本(const void*)`。
2. 按文件头规则给本次触碰且缺少头部规则模块的代码文件补最小模块。
3. 已删除两个本地 `指针日志文本` 定义，调用点直接使用全局函数。
4. 已运行函数说明文档生成脚本和 Debug x64 构建。
```

验证：

```text
rg -n "inline std::string 指针日志文本|export inline std::string 指针日志文本|指针日志文本\\(" 全局共享函数类.ixx 自我动作实现模块.ixx 自我动作实现.外设模块.ixx
结果：只剩全局定义和调用点；本地定义为 0。

python .\tools\生成函数功能说明文档.py
结果：扫描源文件 127 个，生成函数条目 7168。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo
结果：构建成功，0 警告，0 错误。
```

禁止宣称：不宣称 P13 完成，不宣称函数去重全部完成，不宣称自我苏醒完成或初步成熟完成。
