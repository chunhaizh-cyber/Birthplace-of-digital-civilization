# 20260620 计划索引执行顺序优化 Codex 断点清单

## 0. 恢复摘要

当前切片：梳理 `计划/计划索引.md`，优化当前未完成计划的执行顺序。

依据文件：

```text
AGENTS.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_世界树类逻辑纠偏计划_v0.1.md
计划/20260620_语素类逻辑纠偏计划_v0.1.md
计划/20260618_项目结构体审计与清理计划_v0.1.md
计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md
实施记录/20260620_动态排序时间读取函数_Codex断点清单.md
实施记录/20260620_场景动态排序读取回迁_Codex断点清单.md
```

## 1. 当前判断

```text
1. 计划索引当前主动队列已清空，但第 2 章仍把已闭合的场景类旧切片列为 P0。
2. 动态类 P9 已有断点和构建证据，不能误排为下一主动切片。
3. 场景类 / 存在类当前最近切片已闭合；下一可执行优先应转为尚未执行的 owning-class 入口审计。
4. 世界树结构操作入口审计影响基础信息世界树解析、枚举、父子归属和创建删除边界，优先于语素类入口审计。
5. 结构体审计和函数去重属于横向清理线，排在 owning-class 入口审计之后。
```

## 2. 本切片修改范围

```text
计划/计划索引.md
```

不修改源码，不运行构建。

## 3. 执行结果

```text
1. 计划索引第 1 章已恢复当前主动计划：
   P0 世界树类逻辑纠偏计划。
2. 当前执行切片已设为：
   计划/20260620_世界树类逻辑纠偏计划_v0.1.md 的 1：世界树结构操作入口审计。
3. 第 1 章证据指针已压缩为当前最小集合，历史证据默认不展开。
4. 第 2 章未完成计划优先队列已调整为：
   P0 世界树类逻辑纠偏计划；
   P1 语素类逻辑纠偏计划；
   P2 项目结构体审计与清理计划；
   P3 项目功能函数去重与无效代码清理计划。
5. 动态类 P9 已按断点证据视为闭合，不排入当前主动队列。
```

验证结果：

```text
已复核 计划/计划索引.md 第 1 章当前主动队列。
已复核 计划/计划索引.md 第 2 章当前未完成计划优先队列。
git diff --check：仅有 LF 将替换为 CRLF 的工作区换行提示，无空白错误。
```

禁止宣称：

```text
不宣称世界树类逻辑纠偏完成。
不宣称语素类逻辑纠偏完成。
不宣称结构体清理完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
