# 初始因果模板种子数据化 P4 续查记录

日期：2026-06-04

## 1. 目标

```text
把 P4 剩余的启动期初始因果模板种子从 C++ 固定清单迁出。

迁出对象：
    当前场景可观测单位取得路径；
    当前场景特征值明确旧名兼容路径；
    D455观察确认路径内部材料与对应事实闭合组成因。

不迁出：
    因果主信息结构；
    任务筹办运行期逻辑；
    风险安全层固定五因素。
```

## 2. 代码修改

```text
data/initial_causal_templates.tsv
    新增初始因果模板种子数据文件。
    每行字段：
        名称；
        层级；
        主果宿主；
        主果特征；
        因宿主；
        因特征集。

自我线程模块.impl.cpp
    新增最小 TSV 读取器；
    支持从以下路径读取：
        data/initial_causal_templates.tsv；
        ../data/initial_causal_templates.tsv；
        ../../data/initial_causal_templates.tsv。

    删除 C++ 中的：
        路径完成特征集[]；
        D455路径组成特征集[]；
        当前场景可观测单位路径模板=3 固定日志；
        D455路径组成因模板数量 固定统计。

    改为：
        自我线程/初始因果模板种子文件读取；
        初始模板种子文件；
        初始模板种子数量；
        初始模板应用数量；
        初始模板因比较数量；
        初始模板缺口数量；
        C++固定清单=0。

说明书/因果类说明.md
    删除现行说明中的 枚举_因果来源类型 / 来源类型 / 置信度 字段描述；
    按当前因果主信息字段同步为 因比较模板 / 因方法模板 / 果比较模板 / 主果比较模板 / 证据动态样本 和基础计数。
```

## 3. 边界

```text
没有新增因果主信息字段。
没有新增独立遗传因果结构。
没有把安全因素定义表搬到数据文件。
没有恢复风险安全层固定五因素。
文件缺失时记录错误，不回退 C++ 固定清单。
```

## 4. 验证

```text
已运行：
    git diff --check
        通过；仅有 CRLF 提示。

    rg 路径完成特征集 / D455路径组成特征集 / 当前场景可观测单位路径模板=3
        旧 C++ 固定清单无命中；
        仅保留 自我线程固定场景OR路径=0 边界日志。

    rg 说明书/因果类说明.md 中 枚举_因果来源类型 / 因果来源类型 / 来源类型 / 置信度
        无命中。

    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        退出码=0；
        警告=1；
        警告内容=既有 D455相机类.obj LNK4075，/INCREMENTAL:NO 导致忽略 /EDITANDCONTINUE。

未运行：
    --self-check-log。

原因：
    用户已要求跳过验收；本轮只验证构建和静态迁出，不做自然日志验收。
```

## 5. 当前结论

```text
P4 的启动期路径 / D455 组成因种子已从 C++ 固定清单迁出到数据文件。
运行期仍通过现有因果主信息和因比较模板创建初始因果模板。
后续若要继续推进，需要补真实运行日志锚点，或转入下一主动队列项。
```
