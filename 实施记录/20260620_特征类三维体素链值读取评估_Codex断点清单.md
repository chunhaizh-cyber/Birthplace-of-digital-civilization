# 20260620 特征类三维体素链值读取评估 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_特征类逻辑纠偏计划_v0.1.md` 的 1.2，评估三维体素链读取 `特征值类` 节点 `主信息.值` 是否需要转入 `特征值类` 只读函数缺口。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_特征类逻辑纠偏计划_v0.1.md
特征值类.h
特征类.cpp
说明书/函数功能说明文档.md
```

已复核事实：

```text
1. 特征类.cpp 三维体素链读取直接访问 特征值类 节点 主信息.值：
   特征类.cpp:376
   特征类.cpp:3491
   特征类.cpp:3511
   特征类.cpp:3532
   特征类.cpp:3796

2. 特征值类.h 已有 取VecU只读指针(VecU句柄)，但当前三维体素链持有的是 特征值类::节点类*。

3. 说明书/函数功能说明文档.md 未发现 特征值类 节点指针版本的 VecU 只读函数。
```

当前判断：

```text
1. 缺口属于 特征值类 owning class。
2. 需要按“缺哪个类补哪个类第一优先”新建 计划/20260620_特征值类逻辑纠偏计划_v0.1.md，并临时补充 特征值类::取节点VecU只读指针。
3. 特征值类切片闭合后，返回本计划 1.2，替换 特征类.cpp 三维体素链 direct 主信息.值。
```

## 1. 验证结果

```text
1. 已新建 计划/20260620_特征值类逻辑纠偏计划_v0.1.md。
2. 特征值类::取节点VecU只读指针 已闭合，证据见：
   实施记录/20260620_特征值类节点VecU只读指针_Codex断点清单.md
   日志/20260620_特征值类节点VecU只读指针_msbuild.log
3. 下一步返回 特征类 1.2，使用 特征值类::取节点VecU只读指针 替换三维体素链 direct 主信息.值。
```
