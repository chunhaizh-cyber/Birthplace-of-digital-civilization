# 20260624 P13b 绝对差I64迁移 Codex断点清单

## 0. 恢复摘要

当前切片：P13b 第二小切片，迁移 B 类低风险重复 helper `绝对差I64`。

已完成：

```text
1. 已在 全局共享函数类.ixx 新增并导出：
   绝对差I64(I64 左值, I64 右值) noexcept
2. 已删除两个本地重复定义：
   自我动作实现.扫描.ixx::绝对差I64
   自我动作实现.外设模块.ixx::绝对差I64
3. 自我动作实现.扫描.ixx 已 import 全局共享函数类；
   自我动作实现.外设模块.ixx 已在上一切片 import 全局共享函数类。
4. 已运行 python .\tools\生成函数功能说明文档.py。
5. 函数说明文档现在只登记一处 `绝对差I64`：
   全局共享函数类.ixx:28
```

边界：

```text
本切片只统一既有非饱和绝对差语义；
未把调用改成 本能动作类.ixx::饱和绝对差，也未改变极端 I64 跨符号边界行为。
```

验证：

```text
第一次构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo
    失败于 LINK : fatal error LNK1104: 无法打开文件 D:\鱼巢\x64\Debug\鱼巢.exe
    根因：构建输出 exe 被运行中的 鱼巢.exe 占用。

处理：
    停止占用的 鱼巢.exe 后重跑构建。

第二次构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo
    已成功生成，0 个警告，0 个错误。
```

下一步：

```text
继续 P13b，从 P13a 表中选择下一个不在当前脏改文件中的 B 类低风险小函数族；
优先处理同名重复 helper。
```
