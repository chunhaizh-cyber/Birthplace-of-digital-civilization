# 20260626 P13b 轮廓匹配评分工具迁移 Codex断点清单

## 0. 恢复摘要

当前切片进行中。目标是将 `特征类.cpp::私有_计算轮廓匹配评分Q10000(I64, I64, I64)` 收束到 `全局共享函数类.ixx`，删除特征类本地重复工具函数，并保持原有 Q10000 评分语义。

依据：

```text
计划/计划索引.md：当前主动队列为 P13b，从剩余 B 类低风险候选中选择小函数族。
计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md：B 类函数迁移前必须查全局共享函数类和函数说明文档，已有同功能则复用 / 删除重复定义，没有才迁移。
实施记录/20260624_P13_函数自定义主信息分类候选表.tsv：`私有_计算轮廓匹配评分Q10000` 为 B 类低风险待复核候选。
全局共享函数类.ixx 文件头规则：无领域自定义主信息 / 领域类型参与的通用函数可进入本模块。
```

当前判断：

```text
`私有_计算轮廓匹配评分Q10000` 只依赖 I64、饱和加法和 Q10000 常量，不读取 / 写入项目结构。
全局共享函数类已有相邻的 Q10000 / ROI / 轮廓评分合并数值工具；未发现同名或同功能定义。
`特征类.cpp` 已导入 `全局共享函数类`，迁移不增加 import 环。
`特征类.cpp` 当前缺少文件头部规则注释模块，本切片需先补最小模块。
```

预期验证：

```text
1. `全局共享函数类.ixx` 新增 `计算轮廓匹配评分Q10000(I64, I64, I64)`。
2. `特征类.cpp` 删除本地 `私有_计算轮廓匹配评分Q10000`。
3. 原调用点改为全局 `计算轮廓匹配评分Q10000`。
4. `特征类.cpp` 补最小文件头部规则注释模块。
5. 运行 `python .\tools\生成函数功能说明文档.py`。
6. Debug x64 构建通过。
7. 复扫确认本地私有函数清零，新增全局函数进入源码和函数说明文档。
```

禁止宣称：不宣称 P13 完成，不宣称函数去重全部完成，不宣称自我苏醒完成或初步成熟完成。

## 1. 构建前状态

已执行：

```text
1. `全局共享函数类.ixx` 新增 `计算轮廓匹配评分Q10000(I64, I64, I64)`。
2. `特征类.cpp` 补最小文件头部规则注释模块。
3. 删除本地 `私有_计算轮廓匹配评分Q10000`。
4. `特征类.cpp` 调用点改为全局 `计算轮廓匹配评分Q10000`。
5. 已运行 `python .\tools\生成函数功能说明文档.py`，源码文件 127 个，函数条目 7213。
```

复扫证据：

```text
`特征类.cpp` 中 `私有_计算轮廓匹配评分Q10000` 源码定义已清除。
`计算轮廓匹配评分Q10000` 已进入 `全局共享函数类.ixx`，并被 `特征类.cpp` 调用。
```

构建预测：

```text
Debug x64 构建应通过；若失败，优先检查全局共享函数导出可见性或特征类调用点解析。
```

## 2. 闭合结果

当前切片已闭合。

实现结果：

```text
1. 新增全局定义：`全局共享函数类.ixx::计算轮廓匹配评分Q10000(I64, I64, I64)`。
2. 删除本地重复定义：`特征类.cpp::私有_计算轮廓匹配评分Q10000`。
3. `特征类.cpp` 轮廓比较评分调用点改为全局 `计算轮廓匹配评分Q10000`。
4. `特征类.cpp` 补最小文件头部规则注释模块。
```

验证证据：

```text
python .\tools\生成函数功能说明文档.py 成功；源码文件 127 个，函数条目 7213。
第一次 msbuild 失败：LINK LNK1104 无法打开 `D:\鱼巢\x64\Debug\鱼巢.exe`。
根因：存在运行中的 `D:\鱼巢\x64\Debug\鱼巢.exe`，PID 47840，占用链接输出文件。
处理：结束 PID 47840 后重跑构建。
第二次 msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo 成功；0 警告，0 错误。
复扫确认：`私有_计算轮廓匹配评分Q10000` 已从源码和函数说明文档清除。
复扫确认：`计算轮廓匹配评分Q10000` 已进入 `全局共享函数类.ixx`、`特征类.cpp` 调用点和 `说明书/函数功能说明文档.md`。
```

下一步：

```text
返回 P13b，从剩余 B 类低风险候选中继续选择一个不在当前脏改文件中的小函数族。
```
