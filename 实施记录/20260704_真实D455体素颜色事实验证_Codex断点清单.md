# 20260704 真实D455体素颜色事实验证 Codex断点清单

## 0. 恢复摘要

当前入口：

```text
计划/计划索引.md 第 1 章：E0-E9、P11、P12 均已闭合。
计划/计划索引.md 第 4 章：P12 后续若继续，只能围绕真实样本验证或新批准的后续计划进入。
```

当前切片：

```text
真实 D455 / 场景体素颜色事实验证与根因分流。
```

当前结论：

```text
D455 彩色帧已采集进调用结果；
当前显示的体素默认红色不是因为 D455 没有 RGB 帧；
当前真实样本中 RGB 没有进入体素颜色事实；
已观察到体素加工任务未提交，直接原因是读取轮廓视角失败：未找到 D455 簇三维体素轮廓材料页；
场景体素快照当前只有初始化体素项，未证明真实 D455 样本已触发稳定体素主模型颜色事实。
```

禁止宣称：

```text
不得宣称真实 D455 样本已触发稳定体素主模型更新；
不得宣称体素颜色事实链已闭合；
不得宣称 C -> E 最终裁决已实现；
不得宣称局部体素已合法更新稳定 E；
不得把控制台日志或 UI 颜色当作机器事实；
不得把粗体素写入成功说成颜色事实写入成功。
```

下一步：

```text
继续根因切片：追查 读取D455簇三维体素轮廓视角(...) 为什么在已有 D455 报告和空间候选时找不到材料页；
优先核对短期材料页、簇句柄、来源空间候选 ID、报告 ID、TTL 清理和消费路径；
若能在当前轮定位为小范围代码缺陷，按代码修改前规则执行最小修复；
若涉及详细设计或计划范围变化，先回写计划 / 详细设计，不直接改代码。
```

## 1. 已运行命令

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
.\x64\Debug\鱼巢.exe --d455-status
.\x64\Debug\鱼巢.exe --d455-capture-once
.\x64\Debug\鱼巢.exe --d455-fine-report-queue-smoke
.\x64\Debug\鱼巢.exe --scene-voxel-maintenance-smoke
Select-String -Path '日志\鱼巢_run_20260704.低值g' -Pattern '彩图轮廓数量|轮廓颜色支持率|颜色RGB结构状态|体素加工|父基础颜色|颜色事实|场景体素快照统计|观察体素特征写入诊断'
```

## 2. 关键证据

构建：

```text
Debug x64 构建成功，0 警告，0 错误。
```

D455 状态：

```text
--d455-status 退出码 0。
状态 | 成功=是 | 相机已打开=否 | 原因=无 | 消息=D455 未打开
```

D455 单帧采集：

```text
--d455-capture-once 退出码 0。
颜色像素数量=307200
颜色RGB结构状态=1
彩色帧号=8
彩图轮廓数量=0
轮廓颜色支持率=0
轮廓数量=0
```

D455 精细报告队列：

```text
--d455-fine-report-queue-smoke 退出码 0。
入队帧数=30
观察报告采集成功=30
观察报告入队=30
综合稳定=稳定
```

场景体素快照：

```text
--scene-voxel-maintenance-smoke 退出码 0。
快照版本=1
枚举存在=1
有坐标存在=1
有体素存在=1
入选存在=1
缺口=7
```

体素加工 / 颜色事实阻断：

```text
观察体素加工任务提交诊断 | 阶段=读取轮廓视角 | 成功=0 | 原因=未找到D455簇三维体素轮廓材料页
观察体素特征写入诊断 | 阶段=提交体素加工 | 原因=体素加工任务未提交 | 成功=0
观察体素特征写入诊断 | 阶段=粗体素写入 | 原因=三维体素粗模型已写入 | 成功=1 | 已提交体素加工任务=0
```

## 3. 当前判断

```text
第一段：D455 颜色采集成立。
第二段：颜色没有形成体素颜色事实。
第三段：控制面板显示默认红色，是因为体素项没有 colorFactState=3/4 且 colorRgba>0 的可用颜色事实。
```

代码锚点：

```text
D455相机类.cpp：采集一帧后读取对齐彩色，并在启用轮廓提取时调用 提取轮廓。
双目相机本能适配器.cpp：颜色RGB结构状态由 颜色RGB.size == 预期像素数 判定。
自我动作实现.外设模块.ixx：体素加工任务提交前调用 读取D455簇三维体素轮廓视角(...)；失败则不提交体素加工任务。
自我动作实现.外设模块.ixx：粗体素写入成功后尝试从候选包写父基础颜色事实；候选包缺失时不会写入有效颜色事实。
控制面板类.cpp：只有 colorFactState=3/4 且 colorRgba>0 时显示真实体素色，否则父体素走默认红色。
```

## 4. 待查问题

```text
Q1：短期材料页中是否保存了报告 4191 对应的空间候选 / 局部图材料页。
Q2：观察簇里的点集、深度轮廓、像素集合掩码、空间候选、彩色轮廓局部图句柄是否为空或报告 ID 不匹配。
Q3：读取D455簇三维体素轮廓视角(...) 是否只依赖彩色轮廓局部图句柄，还是可从空间候选句柄重建局部体素视角。
Q4：材料页是否在被动识别 / 体素写入前因 TTL 或消费清理被移除。
Q5：若句柄缺失，缺口应修在材料页构造、簇摘要承接，还是体素加工读取入口的候选材料兼容路径。
```

## 5. 2026-07-04 续跑预测与当前修复点

本轮修复点：

```text
外设观察报告队列.ixx：新增 D455 短期材料页保护集合、来源报告 ID 解析和 保护D455短期观察材料页(...)。
自我动作实现.外设模块.ixx：自我侧被动识别选中 D455 逐簇识别报告后，短期保护其来源材料页 5000ms。
自我动作实现.外设模块.ixx：新增 补写已完成体素父基础颜色事实_由候选包集合(...)，并在观察体素写入路径冲刷已由 worker 产出的父基础颜色候选事实。
说明书/函数功能说明文档.md：已由生成脚本补入新增函数条目。
```

验证预测：

```text
若根因确为容量清理抢先移除短期材料页，则默认真实运行触发自我观察路径后，不应继续出现同一批次的“未找到D455簇三维体素轮廓材料页”。
下一步期望看到 观察体素加工任务提交诊断 阶段=提交任务 成功=1，随后体素加工候选结果包或父基础颜色事实写入日志。
若运行只完成初始化而未触发自我观察路径，本轮只能结算为缺真实触发条件 / 缺验证入口，不能宣称体素颜色事实链已验证闭合。
```

运行锁处理：

```text
.codex_locks/build-run.lock 记录 pid=3468 / run-default-20s / 2026-07-04T09:07:46+08:00。
续跑前检查未发现运行中的 鱼巢.exe，按陈旧锁处理后重新获取运行锁。
```

## 6. 2026-07-04 真实验证结果

构建与静态检查：

```text
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：0 警告，0 错误。

python .\tools\生成函数功能说明文档.py
结果：已生成函数说明文档，函数条目 7735。

git diff --check -- 外设观察报告队列.ixx 自我动作实现.外设模块.ixx 说明书/函数功能说明文档.md 实施记录/20260704_真实D455体素颜色事实验证_Codex断点清单.md
结果：无 whitespace error；仅提示 说明书/函数功能说明文档.md 工作区 CRLF 将在 Git 触碰时转 LF。

python .\tools\check_specs.py
结果：hard issues = 0，warnings = 0。
```

真实 D455 默认运行验证：

```text
第一次默认运行 90 秒：新增日志 3241 行。
结论：未再出现“未找到D455簇三维体素轮廓材料页”；观察体素加工任务提交诊断 4 条，全部 阶段=提交任务 / 成功=1；worker 候选包 4 条，均 父基础颜色有效=1 / 父基础颜色像素数量=6400。

第二次默认运行 90 秒：新增日志 2849 行。
结论：未再出现“未找到D455簇三维体素轮廓材料页”；观察体素加工任务提交诊断 15 条，worker 候选包 15 条，均 父基础颜色有效=1 / 父基础颜色像素数量=6400。

第三次默认运行 75 秒：新增日志 2316 行。
结论：未再出现“未找到D455簇三维体素轮廓材料页”；观察体素加工任务提交诊断 7 条，worker 候选包 7 条，均 父基础颜色有效=1 / 父基础颜色像素数量=6400；粗体素写入诊断的写入数量从旧路径常见 11 上升到 15，说明同一写入路径已拿到候选包并写入父基础颜色相关特征。
```

本轮事实结论：

```text
颜色采集：成立。D455 单帧采集已有 颜色像素数量=307200 / 颜色RGB结构状态=1 / 彩色帧号=8。
颜色进入候选包：成立。真实默认运行中 worker 输出 父基础颜色有效=1 / 父基础颜色像素数量=6400。
旧的颜色没显示第一原因：不是 D455 没采集颜色，而是体素加工任务因短期材料页被容量清理抢先移除而未提交。
旧的颜色没显示第二原因：即使候选包异步生成，原写入路径只在粗体素写入时同步查一次候选包，查不到时写入缺候选包状态，缺少后续自我侧补写入口。
修复后当前验证：材料页找不到的问题未复现；真实运行已进入候选包颜色有效路径，并在第三轮观察到写入数量增加到包含父基础颜色事实字段的路径。
```

仍不扩大宣称：

```text
本轮不宣称存在体素融合内部治理链全闭合。
本轮不宣称 C -> E 最终裁决已实现。
本轮不宣称稳定体素主模型更新、场景体素刷新链或 D5 缺口派生链已完整闭合。
控制面板最终 WebGL 颜色显示尚未用截图单独验收；但控制面板读取侧代码只在 colorFactState=3/4 且 colorRgba>0 时使用真实色，当前修复目标是让父基础颜色事实进入该读取链。
```
