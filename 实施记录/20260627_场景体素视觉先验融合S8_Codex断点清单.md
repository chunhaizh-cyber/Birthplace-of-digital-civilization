# 20260627 场景体素视觉先验融合 S8 Codex 断点清单

## 0. 恢复摘要

当前切片：S8 场景体素维护线程。

当前依据：

```text
计划/计划索引.md 第 1 章当前主动队列
计划/完成项/20260625_场景体素视觉先验融合实现计划_v0.1.md S8
规范/场景体素服务与视觉先验融合规范20260625.md
docs/工程图谱/02_外设观察事实图谱.md
docs/工程图谱/05_规则原子索引.md RULE-VISION-PRIOR-001 至 RULE-VISION-PRIOR-005
实施记录/20260627_P4真实外设自然样本复验_Codex断点清单.md
```

## 1. 前置条件核查

```text
S8 前置：S1-S7 同步链路可构建，并至少有一轮真实材料或可回查材料页证据。

已满足证据：
1. S1-S7 均已有 Debug x64 构建通过记录。
2. P4 真实外设自然样本复验记录中，D455 材料包证据卡报告ID=1，短期观察材料页提交=1；逐簇识别样本卡和扫描变化样本卡均有真实日志锚点。

边界：
1. 这些证据只允许 S8 进入维护线程工程实现。
2. 不宣称视觉系统完成、真实视觉业务闭合或 D455 自然业务闭合。
```

## 2. 修改边界

允许修改：

```text
场景体素模块.ixx
鱼巢.cpp
说明书/函数功能说明文档.md
计划/完成项/20260625_场景体素视觉先验融合实现计划_v0.1.md
计划/计划索引.md
实施记录/20260627_场景体素视觉先验融合S8_Codex断点清单.md
```

禁止范围：

```text
不修改 D455 驱动和外设线程采集逻辑。
不写世界树事实、需求满足、价值结算或方法动作动态。
不新增伪样本、模拟入口或伪造外设报告。
维护线程只维护场景体素缓存和快照发布，不参与事实裁决。
```

## 3. 预测

```text
若 S8 接入正确：
1. 场景体素模块具备维护事件队列、维护线程启动 / 停止和快照原子切换入口。
2. 维护线程只调用既有只读快照生成逻辑，不写世界树事实。
3. Debug x64 构建通过。
4. 若执行真实运行验证，启动 / 停止日志应显示维护线程不阻塞现有线程。
```

## 4. 已运行命令

```text
python .\tools\生成函数功能说明文档.py
退出码：0
结果：函数条目 7323。

git diff --check
退出码：0
结果：仅提示 说明书/函数功能说明文档.md CRLF 将被 LF 替换。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
退出码：0
结果：已成功生成，0 个警告，0 个错误。

.\x64\Debug\鱼巢.exe --scene-voxel-maintenance-smoke
退出码：0
关键输出：
    场景体素维护线程 | 生命周期=4 | 已启动=否 | 停止请求=否 | 队列长度=0 | 接收=1 | 丢弃=0 | 处理=1 | 同步成功=1 | 事件失败=0 | 最近事件结果=1 | 快照版本=1 | 坐标系版本=1
判定：维护线程可启动、处理一次真实初始化场景的只读同步事件并停止；该 smoke 不是视觉业务闭合证据。
```

## 5. 完成内容

```text
1. 场景体素缓存补互斥保护，最新快照只在较新版本完成后切换，读取侧只返回副本。
2. 新增场景体素维护事件、维护线程配置、维护线程摘要、生命周期枚举和事件结果枚举。
3. 新增全局场景体素维护线程启动 / 停止 / 提交事件 / 摘要 / 最新快照入口。
4. 新增 `--scene-voxel-maintenance-smoke` 命令行工程守卫入口，只用于线程启动、事件消费和停止验证。
5. 计划文件已归档至 `计划/完成项/20260625_场景体素视觉先验融合实现计划_v0.1.md`。
```

## 6. 当前禁止宣称

```text
不得宣称视觉系统完成。
不得宣称 D455 自然业务闭合。
不得宣称扫描需求自然生长闭合。
不得宣称维护线程已通过长时真实业务验证。
不得宣称自我苏醒完成或初步成熟完成。
```
