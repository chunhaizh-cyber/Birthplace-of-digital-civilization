# SQL 控制面板运行态同步写入 S4 断点清单

## 0. 恢复摘要

当前切片：S4 需求更新提交点接入。

目标：

```text
需求更新指令成功、需求任务绑定变化等正式需求类提交点成功后，
同步写入 SQL 控制面板当前需求显示项和事件流水。
SQL 只作为控制面板显示镜像，不参与需求树裁决，不作为恢复源。
```

依据：

```text
docs/00_文档总索引.md
规范/禁止性规范20260611.md
规范/规范目录.md
规范/自我线程与任务管理线程权责边界规范20260512.md
规范/需求树生长机制规范20260512.md
规范/需求入树拒绝回执与开发诊断闭环规范20260625.md
docs/工程图谱/00_图谱总索引.md
docs/工程图谱/01_任务需求方法闭环图谱.md
docs/工程图谱/05_规则原子索引.md
docs/工程图谱/08_事实来源与写入权限图谱.md
计划/计划索引.md
计划/20260625_SQL控制面板运行态同步写入计划_v0.1.md
```

已确认：

```text
当前主动计划为 SQL 控制面板运行态同步写入计划 v0.1。
A0、S1、S2、S3 已闭合并提交。
当前执行切片为 S4：需求更新提交点接入。
```

待执行：

```text
1. 补齐 需求类.cpp 文件头部规则注释模块。
2. 增加需求显示项同步 helper。
3. 在需求类正式提交点成功后调用同步 helper。
4. 刷新函数说明文档。
5. Debug x64 构建。
6. 真实运行 self-check 路径并只读查询 SQL 运行态表。
7. 更新计划和计划索引证据后提交本切片。
```

## 1. 真实验证预测

```text
命令：.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=5000
预测：
1. 程序退出码为 0。
2. 启动时会清空控制面板运行态 SQL 表并写入新运行批次。
3. 自检路径触发需求更新或需求任务绑定后，[鱼巢].[当前需求显示项] 行数应大于 0。
4. [鱼巢].[控制面板事件流水] 应包含需求域新增或更新事件。
5. 日志中不应出现 `需求SQL控制面板同步失败`、`需求SQL控制面板删除同步失败`、ADO 写入错误或当前需求显示项相关错误。
```

禁止宣称：

```text
不得宣称实时同步写库已完整实现。
不得宣称 SQL Server 成为业务权威源。
不得宣称控制面板 SQL 显示等同业务闭环完成。
不得宣称自我苏醒完成或初步成熟完成。
```

## 2. 实施结果

已修改：

```text
需求类.cpp
需求类.h
说明书/函数功能说明文档.md
```

实现：

```text
1. 为需求类补齐文件头部规则注释模块。
2. 新增 `需求类::同步SQL控制面板需求显示项`。
3. 新增 `需求类::同步删除SQL控制面板需求显示项`。
4. `需求类::执行更新指令` 成功后：
   - 非删除指令同步当前需求显示项；
   - 删除指令删除当前需求显示项。
5. `需求类::绑定对应任务` 成功后同步当前需求显示项。
6. SQL 写失败只写 SQL 同步状态和警告日志，不回滚需求树写入。
```

## 3. 验证证据

```text
命令：python .\tools\生成函数功能说明文档.py
结果：退出码 0；函数条目 7207。

命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：已成功生成；0 警告；0 错误。

命令：.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=5000
结果：退出码 0。

SQL 只读查询：
控制面板运行批次=1
控制面板事件流水=78
当前任务显示项=4
当前需求显示项=7
SQL投影同步状态=1

当前需求显示项样例：
需求=5; 父=2; 满足关系=2; 已满足=0; 摘要=需求树更新指令成功

事件流水样例：
领域=需求; 操作=新增或更新; 对象=5; 摘要=需求树更新指令成功

日志检索：
需求SQL控制面板同步失败=0
需求SQL控制面板删除同步失败=0
当前需求显示项=0
ADO=0
```

## 4. 当前判断

```text
S4 已闭合。
需求更新指令成功和需求任务绑定变化已经接入 SQL 控制面板运行态显示镜像。
SQL 仍只是控制面板显示镜像，不参与需求树裁决，不作为业务恢复源。
```

## 5. 下一步

```text
进入 S5：方法、语素、世界树、动态逐步接入。
优先从方法树初始化 / 方法状态变化选择一个最小提交点切片。
```
