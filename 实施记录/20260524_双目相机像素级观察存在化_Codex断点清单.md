# 双目相机像素级观察存在化 Codex 断点清单

## 当前切片

P13：空间候选拆分 / 合并与已验证观察存在闭合。

## 已完成

- [x] 新增 `双目相机_取得当前观察帧` 本能方法 ID、默认种子、运行时动作注册和专属规格分派。
- [x] 新增 `双目相机_建立当前帧像素特征索引` 本能方法 ID、默认种子、运行时动作注册和专属规格分派。
- [x] `双目相机本能适配器` 从真实采集帧计算帧级像素特征摘要：预期像素数量、像素特征数量、颜色像素数量、深度有效像素数量、点云有效像素数量、像素覆盖状态、空间坐标单位毫米。
- [x] `自我动作实现.外设模块` 写入当前观察帧和当前帧像素特征索引摘要，动作动态绑定具体双目相机方法。
- [x] 统一 `双目相机本能方法结果` 日志扩展像素特征摘要字段。
- [x] 新增 `自我_提取空间候选` 本能方法 ID、默认种子、运行时动作注册和专属规格分派。
- [x] `双目相机本能适配器` 从深度有效且点云有效的像素提取空间候选摘要，不以轮廓列表作为存在确认主依据。
- [x] 空间候选摘要包含候选编号、像素数量、中心空间坐标、范围坐标 AABB、投影范围、平均 / 最近 / 最远深度、空间连续性评分和范围稳定性评分。
- [x] `自我动作实现.外设模块` 写入空间候选集合摘要、候选数量、有效点数量和主候选关键空间特征。
- [x] 新增 `自我_判断空间候选相对独立性` 本能方法 ID、默认种子、运行时动作注册和专属规格分派。
- [x] 相对独立性判断只基于空间候选摘要、空间连续性、范围稳定性、AABB 间隔、深度跨度和候选相邻关系，不把轮廓作为确认主依据。
- [x] 输出相对独立、不独立、待拆分、待合并、不确定候选数量，以及主空间候选状态和分离度评分。
- [x] 新增 `自我_生成空间候选观察队列` 本能方法 ID、默认种子、运行时动作注册和专属规格分派。
- [x] 观察队列按空间连续性、范围稳定性、中心接近评分、有效点比例、范围大小评分和相对独立状态惩罚生成默认优先级。
- [x] 输出空间候选观察队列状态、队列候选数量、首选观察候选编号、首选优先级评分、首选相对独立状态、中心接近评分和范围大小评分。
- [x] 新增 `自我_观察单个空间候选并组合存在假设` 本能方法 ID、默认种子、运行时动作注册和专属规格分派。
- [x] P6 只从 P5 首选空间候选生成待验证存在假设摘要，不直接确认观察存在。
- [x] 存在假设摘要输出来源候选编号、验证状态、候选像素掩码状态、投影轮廓状态、距离、尺寸 X/Y/Z、方位 X/Y、有效点比例、轮廓支持评分、中心空间坐标和范围坐标 AABB。
- [x] 新增 `自我_用空间范围投影覆盖原帧验证像素归属` 本能方法 ID、默认种子、运行时动作注册和专属规格分派。
- [x] `双目相机本能适配器` 在空间候选摘要中基于真实帧投影范围、范围坐标 AABB 和深度区间生成像素归属验证摘要。
- [x] 像素归属验证摘要输出空间覆盖 / 匹配 / 冲突像素数、轮廓支持 / 冲突像素数、深度一致率、范围内点比例、投影覆盖率、轮廓吻合率、遮挡 / 未解释像素数和像素归属验证状态。
- [x] 新增 `自我_确认观察存在并更新帧解释状态` 本能方法 ID、默认种子、运行时动作注册和专属规格分派。
- [x] P8 以空间连续性、范围稳定性、有效点比例、范围内点比例、深度一致率和投影覆盖率作为确认主依据；轮廓吻合率只作为辅助摘要字段。
- [x] P8 输出观察存在确认状态、已验证观察存在数量、像素归属更新、未解释区域更新和帧解释状态；未满足阈值时只记录待确认 / 待拆分 / 待合并 / 已否定，不直接宣称观察存在已确认。
- [x] P9 验证预测已确认：若真实观察帧宽度为 W、高度为 H，则当前观察帧下应生成 W * H 个像素特征索引项，并且像素坐标 / 帧内编号可唯一回查。
- [x] P9 环境复查已完成：`Get-PnpDevice -PresentOnly` 已检测到 `Intel(R) RealSense(TM) Depth Camera 455` 深度和彩色设备。
- [x] 新增命令行真实相机控制入口：`--camera-status`、`--camera-open`、`--camera-release`、`--camera-capture-once`。
- [x] `--camera-capture-once` 已真实打开 D455、采集一帧并释放，相机控制结果写入标准输出和运行日志。
- [x] P9 单帧采集结果：宽度 640，高度 480，预期像素数量 307200，像素特征数量 307200，像素覆盖状态 1，深度有效像素数量 302000，点云有效像素数量 302000，空间候选数量 12。
- [x] 控制面板快照新增自我所在场景复现字段，读取自我现实场景及其子节点中的当前观察帧、像素特征索引、空间候选、存在假设、像素归属验证和观察存在确认摘要。
- [x] 控制面板新增“自我所在场景”菜单项。
- [x] 控制面板新增自我所在场景页面、快照统计区和 WebGL 空间复现画布。
- [x] WebGL 复现画布优先绘制坐标基准、空间候选中心点和范围坐标 AABB；未命中场景快照时只显示坐标基准，不伪造识别结果。
- [x] 生成的 `控制面板.html` 已包含 `self-scene` 页面、`self-scene-gl` 画布和 `自我场景复现数据` JSON。
- [x] 新增 `--self-scene-observe-once` 真实认知入口：执行 `双目相机_使达到可用状态 -> 自我_确认观察存在并更新帧解释状态 -> 双目相机_释放`，输出结果只通过自我方法动作链写入自我所在场景。
- [x] 新增 `--self-scene-html` 真实认知证据入口：先执行同一方法动作链写入自我所在场景，再从自我所在场景快照生成 `控制面板.html`。
- [x] 真实运行 `--self-scene-html` 成功写入自我所在场景：640x480，307200 个像素特征，10 个空间候选，主候选中心 `(-116,-251,2168)`，主候选 AABB `(-1098,2966;-2656,1485;1441,4464)`。
- [x] 本次真实结果形成空间候选和存在假设，但观察存在确认状态为 `3`（待拆分），已验证观察存在数量为 0。
- [x] P13 修正观察队列首选策略：优先观察相对独立空间候选，避免大范围待拆分候选长期压住可验证小候选。
- [x] P13 修正像素归属验证主口径：范围内点比例、深度一致率和投影覆盖率按候选自身真实像素集合计算；AABB 外接矩形只保留为轮廓 / 边界辅助证据。
- [x] P13 修正小候选确认门槛：不再要求候选占全帧有效点比例达到 50%，改为候选自身像素规模、空间连续性、范围稳定性和归属验证共同通过。
- [x] P13 新增待合并候选摘要级合并：对真实帧中空间间隔和深度差足够近的候选合并为一个存在假设，再进入确认链。
- [x] P13 控制面板证据面修正：`自我场景复现数据` 的绘制候选编号优先使用存在假设来源编号，已验证观察存在使用确认色；画布仍只读取自我所在场景快照。
- [x] P13 真实运行 `--self-scene-html` 已形成已验证观察存在：640x480，307200 个像素特征，46 个空间候选，观察存在确认状态 `1`（已验证），已验证观察存在数量 16，帧解释状态 1。

## 修改文件

- `本能方法类.h`
- `本能方法类.cpp`
- `双目相机本能适配器.h`
- `双目相机本能适配器.cpp`
- `自我动作实现.外设模块.ixx`
- `自我动作实现模块.ixx`
- `控制面板类.ixx`
- `控制面板类.cpp`
- `控制面板.html`
- `计划/20260524_双目相机像素级观察存在化阶段目标计划_v0.1.md`
- `计划/计划索引.md`
- `实施记录/20260524_双目相机像素级观察存在化_Codex断点清单.md`
- `鱼巢.cpp`

## 验收命令

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
.\x64\Debug\鱼巢.exe --camera-capture-once
.\x64\Debug\鱼巢.exe --panel-html
.\x64\Debug\鱼巢.exe --self-scene-html
```

## 验收结果

- Debug x64 构建通过。
- P10/P11 复验构建：`日志/p10_self_scene_build_20260525.txt`，0 个警告，0 个错误。
- 真实 D455 单帧采集：通过。
- 命令：`.\x64\Debug\鱼巢.exe --camera-capture-once`
- 结果：退出码 0；打开成功、采集成功、释放成功。
- 证据：`日志/p9_camera_capture_stdout_20260525.txt` 和 `日志/鱼巢_run_20260525.低值g` 均记录真实 D455 单帧采集摘要。
- 控制面板 HTML 生成：通过。
- 命令：`.\x64\Debug\鱼巢.exe --panel-html`
- 结果：退出码 0，输出 `D:\鱼巢\控制面板.html`。
- 证据：`日志/p10_self_scene_panel_html_20260525.txt`。
- 浏览器可视验证：Codex 浏览器因本地 `file:///D:/鱼巢/控制面板.html` 访问策略拦截，未继续绕行；实机窗口点击仍待后续验证。
- 自我场景真实认知入口：通过。
- 命令：`.\x64\Debug\鱼巢.exe --self-scene-html`
- 结果：退出码 0；已打开 D455、执行自我观察存在确认方法、释放 D455，并从自我所在场景生成 `控制面板.html`。
- 证据：`日志/p12_self_scene_cognition_stdout_20260525.txt`、`日志/p12_self_scene_cognition_build_20260525.txt` 和 `日志/鱼巢_run_20260525.低值g`。
- P13 候选像素归属与待合并处理构建：通过。
- 命令：`msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
- 结果：退出码 0，仅有既有 `LNK4075` 警告。
- 证据：`日志/p13_verified_scene_json_build_20260525.txt`。
- P13 真实自我场景认知入口：通过。
- 命令：`.\x64\Debug\鱼巢.exe --self-scene-html`
- 结果：退出码 0；真实 D455 帧写入自我所在场景，观察存在确认状态 `1`，已验证观察存在数量 `16`，帧解释状态 `1`。
- 证据：`日志/p13_verified_scene_json_stdout_20260525.txt` 和 `日志/鱼巢_run_20260525.低值g`。

## 未完成

- 当前帧每个像素对应的世界树节点化或可分页索引结构尚未展开。
- 按像素坐标 / 帧内像素编号 100% 回查尚未做真实运行日志验证。
- P9 只验证了命令行真实 D455 单帧采集和适配器摘要；自我所在场景挂载、方法动作动态自然链路、像素坐标 / 帧内编号逐项回查尚未由自然任务链验证。
- 空间候选质量、P4 阈值合理性、P5 排序合理性、P6 存在假设质量、P7 像素归属质量、P8 观察存在确认质量仍需更多真实 D455 画面日志验证。
- 当前 `--self-scene-html` 生成的控制面板 HTML 已命中自我所在场景真实快照，`自我场景复现数据.ok=true`，但它只是证据画面，不是业务目标本身。
- P14 已形成可枚举的 `已确认观察存在` 子存在和全帧像素归属摘要账；2026-05-26 真实 `--self-scene-html` 返回 0，最新结果为 `verifiedCount=1`、`fullFrameOwnershipState=1`、`assignedPixels=82292`、`unexplainedPixels=224908`、`ownershipConflictPixels=0`、`ownershipRatio=2678`。
- 当前尚未建立已归属像素图、未解释像素图和“每个像素归属于哪个观察存在”的逐像素真实场景账。
- 控制面板自我所在场景页面的实机窗口点击仍待后续验证。

## 下一切片

Q8.2 控制面板诊断摘要层；若要继续 P15，需先制定逐像素归属表 / 真实视觉分割 / 完整 GPU 顶点缓冲方案。

## 禁止宣称

- 不宣称真实 D455 全帧认知链路完全通过。
- 不宣称当前帧像素 100% 回查已由真实样本验证。
- 不宣称空间候选已闭合为稳定外部对象。
- 不宣称 P14 的全帧像素归属摘要账等同于逐像素归属表。
- 不宣称目标可见性 / 在场状态已闭合。
