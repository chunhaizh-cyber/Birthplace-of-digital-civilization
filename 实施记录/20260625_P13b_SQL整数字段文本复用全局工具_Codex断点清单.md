# 20260625 P13b SQL整数字段文本复用全局工具 Codex断点清单

## 0. 恢复摘要

当前切片已闭合。已把语素类和世界树类中不依赖领域结构的 SQL 整数 / 可空整数字段文本 helper 收束到 `全局共享函数类.ixx`，并删除本地重复定义。

依据：

```text
计划/计划索引.md：P13b 全局共享迁移。
计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md：B 类函数已有同功能全局函数则复用，不重复新增；确需新增通用函数时放入全局共享函数类。
docs/工程图谱/05_规则原子索引.md：RULE-STRUCT-001 / RULE-STRUCT-002。
规范/禁止性规范20260611.md：文本不得承载系统机器逻辑。
```

对象：

```text
语素类.cpp::私有_SQL可空整数(bool, int)
语素类.cpp::私有_语素SQL字段整数(int)
语素类.cpp::私有_语素SQL字段可空整数(bool, int)
世界树类.cpp::私有_世界树SQL可空整数(bool, int)
世界树类.cpp::私有_世界树SQL字段整数(int)
世界树类.cpp::私有_世界树SQL字段可空整数(bool, int)
```

当前判断：

```text
上述 helper 只把 int 格式化为 SQL / 字段恢复比对文本。
`有值=false` 的 SQL 流输出口径为 "NULL"；字段恢复比对口径为空串。
本切片不处理 SQL 字符串转义、主键、显示文本、入口文本或任何语素 / 世界树机器判断。
```

下一步：

```text
1. 继续 P13b，从剩余 B 类候选中选择下一个低风险同功能重复 helper。
2. 迁移 / 复用前继续先查 `全局共享函数类.ixx` 和 `说明书/函数功能说明文档.md`。
```

已完成：

```text
1. 在 `全局共享函数类.ixx` 新增 `SQL整数文本`、`SQL可空整数文本`、`SQL字段可空整数文本`。
2. 按文件头规则给本次触碰且缺少头部规则模块的代码文件补最小模块。
3. 删除语素类和世界树类上述 6 个本地 helper 并替换调用点。
4. 已运行函数说明文档生成脚本和 Debug x64 构建。
```

验证：

```text
rg -n "私有_SQL可空整数|私有_语素SQL字段整数|私有_语素SQL字段可空整数|私有_世界树SQL可空整数|私有_世界树SQL字段整数|私有_世界树SQL字段可空整数" 语素类.cpp 世界树类.cpp 全局共享函数类.ixx
结果：源码残留为 0。

python .\tools\生成函数功能说明文档.py
结果：扫描源文件 127 个，生成函数条目 7168。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo
结果：构建成功，0 警告，0 错误。
```

禁止宣称：不宣称 P13 完成，不宣称函数去重全部完成，不宣称自我苏醒完成或初步成熟完成。
