# 20260620 场景主信息直读直写复扫 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.2 P3.3d，复扫剩余 `场景节点主信息类` 字段直读 / 直写路径并重新分类下一优先。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
实施记录/20260620_二次特征绑定追加场景索引回迁_Codex断点清单.md
```

本切片范围：

```text
1. 只复扫剩余直读 / 直写路径并分类。
2. 不做源码修改。
3. 若发现仍有可闭合系统逻辑回迁项，写入下一小切片。
4. 若剩余路径属于显示 / SQL / 语言投影，转入 P3.4 标注显示读取或进入后续归属审查。
```

验证前预测：

```text
1. 关系索引应无字段访问残留。
2. 方法类、任务类、自我线程、创建 / 删除 / 绑定路径的已回迁点不应再命中原字段直读。
3. 剩余主要会集中在 状态类 / 动态类 的内部清理扫描路径，以及 控制面板 / 世界树 / 语言 / 语素层。
```

禁止宣称：

```text
不宣称所有场景主信息直读已回迁。
不宣称场景类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 1. 复扫命令

```powershell
rg -n "取主信息<场景节点主信息类>|dynamic_cast<.*场景节点主信息类|场景主信息->|\.状态索引|\.动态索引|\.关系索引|\.二次特征索引|\.宿主存在|\.世界类型" -g "*.h" -g "*.cpp" -g "*.ixx" -g "!x64/**" -g "!Debug/**" -g "!日志/**" -g "!logs/**"
```

输出文件：

```text
日志/20260620_场景主信息直读直写复扫_全仓引用.txt
```

## 2. 复扫结果

```text
总命中：60
关系索引字段访问残留：0

按文件分布：
状态类.cpp 15
控制面板类.cpp 9
语素类.cpp 8
场景类.cpp 6
动态类.cpp 4
世界树类.cpp 4
语言类_二次特征表达.cpp 3
语言类_基础信息命名.cpp 3
二次特征应用模块.ixx 2
二次特征类.cpp 1
基础信息主信息类.h 1
任务模块.管理工作线程.ixx 1
因果类.cpp 1
自我动作实现.外设模块.ixx 1
自我动作实现模块.ixx 1
```

## 3. 当前分类

```text
已回迁后未再命中：
    方法类 场景二次特征索引读取路径。
    任务类 宿主存在读取路径。
    自我线程输出结果场景状态读取路径。
    状态 / 动态 / 二次特征 创建、删除、绑定的核心索引追加 / 移除路径。

仍需优先回迁的系统逻辑路径：
    状态类.cpp:
        获取场景状态 仍直接读取 场景主信息->状态索引。
        是否当前活动状态 / 可从活动状态链清理 仍直接读取状态索引。
        清理已处理状态索引 / 清理超过时间窗口的状态索引 / 按活动状态限量清理 仍直接读取或擦除状态索引。

    动态类.cpp:
        私有_查找已有状态动态 仍直接读取 场景主信息->动态索引。
        查找相邻前状态 仍直接读取 场景主信息->状态索引。

后续需单独判断的系统边界路径：
    语素类.cpp 的 私有_场景索引包含 / 私有_把基础信息挂入场景索引 仍直接检查和追加状态、动态、二次特征索引。
    二次特征类.cpp、任务模块.管理工作线程.ixx、因果类.cpp、二次特征应用模块.ixx、自我动作实现模块相关命中主要是场景身份判断或父节点场景锚点判断，需后续按 owning class 边界单独收束。

显示 / 投影路径：
    控制面板类.cpp、语言类_二次特征表达.cpp、语言类_基础信息命名.cpp 当前按显示 / 命名读取登记。
    世界树类.cpp 当前按 SQL / 投影 / 编排辅助读取登记。
    场景类.cpp 当前为 owning class 内部维护路径，不作为外部直读缺口。
```

## 4. 结论和下一步

```text
P3.3d 复扫切片闭合。

下一小切片：
    P3.3e 回迁 状态类::获取场景状态 的状态索引读取路径，改为调用 场景类::读取场景状态索引快照。

选择理由：
    获取场景状态 是状态类公开读取入口，且改动范围小，不需要新增函数。
    先处理该入口可减少后续调用方继续依赖 场景节点主信息类::状态索引 的必要性。

验证方式：
    Debug x64 构建。
```
