# 20260606 P9b 观察验证最小闭环记录

## 当前切片

计划：`计划/20260606_外设四本能方法任务真实运行条件包闭环计划_v0.1.md`

切片：P9b，6127 观察验证最小闭环。

目标：让 `自我_验证外设观察像素簇候选` 通过任务等待项、D455 逐簇识别报告、任务管理承接、自我观察事实落账和来源需求重筹办真实运行。

## 本轮修改

已改文件：

```text
任务模块.管理界面线程.ixx
任务模块.管理界面线程.impl.cpp
外设观察报告队列.ixx
自我线程模块.impl.cpp
自我线程模块.ixx
自我线程模块.消息协议.ixx
自我动作实现.外设模块.ixx
```

已做的最小接入：

```text
1. 任务管理界面线程新增上行消息直接通知回调，避免外设承接只停在任务管理内部上行队列。
2. 任务管理侧新增外设承接后台线程，只巡检外设观察等待项，不派发普通任务。
3. 外设等待项命中报告并成功上行后关闭等待项，避免同一等待项被连续 D455 报告反复上行淹没自我邮箱。
4. 任务管理观察事实承接消息在自我线程内提升为紧急可抢占消息。
5. 自我线程收到观察事实承接后写入观察材料、候选簇事实，并按来源需求尝试重筹办原等待任务。
6. 自我线程新增观察事实承接抢占落账步骤，并在主派发任务发起 / 任务治理提交返回点先行消费已到达的观察承接消息。
```

## 构建前预测

下一次真实运行预期：

```text
1. 能看到外设观察验证等待项生成。
2. 能看到 D455 逐簇识别报告匹配等待项。
3. 能看到任务管理外设等待项巡检生成上行，直接通知自我线程 > 0，已关闭等待项 > 0。
4. 能看到自我线程/外设观察事实承接开始与完成。
5. 能看到观察事实更新命中来源需求或来源需求重筹办跳过的明确日志。
6. 若 P9b 未闭合，下一堵点应是 6127 重入执行后的正式条件或提交入口问题，而不是缺少 D455 逐簇识别报告。
```

## 已运行命令

```text
git diff --check -- 外设观察报告队列.ixx 任务模块.管理界面线程.impl.cpp 自我线程模块.消息协议.ixx 自我线程模块.impl.cpp
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

结果：

```text
git diff --check：无空白错误，仅 LF/CRLF 提示。
Debug x64 构建：0 错误，1 个已知 LNK4075 警告。
```

## 真实运行前预测

下一次 `--self-check-log --self-check-log-wait-ms=30000` 真实运行预期：

```text
1. 能看到 6127 首次执行因缺 D455 逐簇识别报告进入等待。
2. 能看到外设等待项命中 D455 报告，并直接通知自我线程。
3. 能看到 自我线程/观察事实承接冻结诊断、规范化诊断、外设观察事实承接开始与完成。
4. 能看到 观察事实更新命中来源需求 或明确跳过来源需求的原因。
5. 若仍失败，新的堵点应在来源任务重筹办 / 6127 重入 / 条件消费，而不是观察承接消息停在 mailbox。
```

## 调度让步补丁预测

新增接入：

```text
1. 任务管理界面线程新增调度让步回调，消费泵在完成项归并后触发。
2. 自我线程启动时注册调度让步回调。
3. 自我线程新增公开薄包装，由类内创建主循环上下文并调用私有观察事实承接抢占落账。
4. 抢占落账入口增加线程内重入保护，避免观察承接落账过程中反向递归。
```

已运行命令：

```text
git diff --check -- 任务模块.管理界面线程.impl.cpp 任务模块.管理界面线程.ixx 自我线程模块.impl.cpp 自我线程模块.ixx
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

结果：

```text
git diff --check：无空白错误，仅 LF/CRLF 提示。
首次构建失败：类外回调不能访问 自我线程类::结构_主循环骨架上下文 和 私有抢占方法。
修正：新增公开薄包装 尝试抢占落账观察事实承接，不公开内部上下文结构。
第二次 Debug x64 构建：0 错误，1 个已知 LNK4075 警告。
```

下一次真实运行预测：

```text
1. D455 报告直接通知自我线程后，若自我线程仍在任务管理消费泵内，应看到 任务管理界面线程/消费泵让步。
2. 让步后应看到 自我线程/观察事实承接抢占落账完成。
3. 随后应看到 自我线程/外设观察事实承接开始 与 完成。
4. 若 6127 仍未闭合，下一堵点应进入 来源需求/来源任务重筹办 或 6127 二次执行条件消费，而不是观察承接停留在 mailbox。
```

## 调度让步后第二层修正

最新真实运行证据：

```text
--self-check-log --self-check-log-wait-ms=30000 退出码 0。
本次启动行：日志/鱼巢_run_20260606.低值g:58581。
6127 首次执行仍因缺 D455 逐簇识别报告进入等待。
D455 报告已直接通知自我线程，等待项已关闭：
    59200 / 59291 均为 直接通知自我线程=1、已关闭等待项=1。
自我 mailbox 已收到观察承接：
    59199 mailbox=12 / 观察承接=1 / 队首观察承接=1。
    59290 mailbox=23 / 观察承接=2 / 队首观察承接=1。
未看到 消费泵让步、抢占落账完成、外设观察事实承接开始/完成。
```

判断：

```text
调度让步回调已接入，但抢占入口原先要求 本轮冻结治理批次_ 为空；
实际运行时自我线程在处理已有冻结批次并进入任务管理消费泵，外设观察承接异步进入 mailbox；
因此抢占入口不能再依赖全局冻结批次为空。
```

新增修正：

```text
1. 抢占入口从治理 mailbox 中只抽取 任务管理观察事实承接请求，形成局部批次。
2. 局部批次复用既有规范化落账流程，但不清空外层正在处理的全局冻结批次。
3. 规范化上下文新增 规范化后清空全局冻结批次 标记，普通主循环仍保持原清理行为。
4. 新增 自我线程/观察事实承接抢占抽取完成 日志，用于确认局部抽取是否发生。
```

已运行命令：

```text
git diff --check -- 自我线程模块.impl.cpp 自我线程模块.ixx 任务模块.管理界面线程.impl.cpp 任务模块.管理界面线程.ixx
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

结果：

```text
git diff --check：无空白错误，仅 LF/CRLF 提示。
Debug x64 构建：0 错误，1 个已知 LNK4075 警告。
```

下一次真实运行预测：

```text
1. D455 上行通知后应看到 自我线程/观察事实承接抢占抽取完成。
2. 应看到 自我线程/观察事实承接规范化诊断。
3. 应看到 自我线程/外设观察事实承接开始 与 完成。
4. 任务管理消费泵应记录 消费泵让步。
5. 若仍失败，堵点应进入 来源需求/任务重筹办 或 6127 重新执行后的条件消费。
```

## 抢占落账后真实运行证据与第三层修正

真实运行证据：

```text
命令：.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000
本次启动行：日志/鱼巢_run_20260606.低值g:59857。
运行超出工具超时，进程仍在处理观察承接；已停止本轮自检进程。

已验证通过的链：
1. 6127 首次执行因缺 D455 逐簇识别报告进入等待。
2. D455 报告直接通知自我线程，等待项关闭。
3. 自我线程从 mailbox 抽取观察承接并形成局部批次。
4. 外设观察事实承接开始并完成，来源需求触发重筹办。
5. 来源任务重筹办后 6127 二次执行成功，外设观察像素簇候选验证状态=1。
```

关键堵点：

```text
1. 6127 成功后，任务管理读取执行目标状态时 实际状态 为空。
   结果：任务管理记录 动作成功无可比较目标结果，并把来源任务继续判为待重筹办。
2. 外设观察承接同步写入完整簇集合，簇数量升高时单轮处理过慢。
   结果：自检等待时间结束后进程仍在处理观察承接，不能作为闭合证据。
```

新增修正：

```text
1. 自我_验证外设观察像素簇候选 成功后，显式写入目标方法实际结果状态：
   - 外设像素簇候选验证状态
   - 外设观察像素簇候选已验证状态
   - 外设观察材料可回查状态
2. 外设观察事实承接同步条件包只入账前 8 个簇；
   完整簇集合仍保留在外设报告缓存，由报告 ID 提供回查，不伪造、不丢弃材料。
```

下一次真实运行预测：

```text
1. D455 报告直接通知后，应看到 自我线程/观察事实承接抢占抽取完成。
2. 应看到 自我线程/外设观察事实承接开始、截断簇入账、完成。
3. 来源需求重筹办后，6127 应再次执行并成功。
4. 应看到 自我动作/目标方法实际结果已写入。
5. 任务管理工作线程/执行目标状态读取 中 实际状态 应非空。
6. 不应再出现任务 9 因 动作成功无可比较目标结果 而重复进入待重筹办。
7. 自检进程应能在等待窗口后正常退出。
```

## 第三层修正后真实运行证据

已运行命令：

```text
git diff --check -- 任务模块.管理界面线程.ixx 任务模块.管理界面线程.impl.cpp 外设观察报告队列.ixx 自我线程模块.impl.cpp 自我线程模块.ixx 自我线程模块.消息协议.ixx 自我动作实现.外设模块.ixx 实施记录/20260606_P9b观察验证最小闭环记录.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000
```

结果：

```text
git diff --check：无空白错误，仅 LF/CRLF 提示。
Debug x64 构建：0 错误，1 个已知 LNK4075 警告。
真实运行：退出码 0。
本次启动行：日志/鱼巢_run_20260606.低值g:61348。
```

关键日志证据：

```text
61888：任务 9 首次执行 6127。
61896-61897：首次执行缺少 D455 逐簇识别报告，进入外设供料等待。
61987-62025：观察承接被抢占抽取，外设观察事实承接开始，前 8 个簇同步入账，完整 31 簇保留在报告缓存，并完成承接。
62026-62032：观察事实更新命中来源需求 9，并将任务 9 标记为待重筹办。
62112-62125：任务 9 二次执行 6127 成功，报告ID=19，候选簇=40，通过簇=40，材料可回查=1，验证状态=1。
62121-62123：6127 写入目标方法实际结果状态，TB 实际值=1。
62126-62127：任务管理读取实际状态=4O3，并比较接受=1。
62128：任务 9 目标达成=1，推进量=1，剩余差距=0，完成度=1000000。
62137-62138：完成判定允许完成=1，任务 9 实际任务状态=完成。
```

当前判断：

```text
P9b 6127 观察验证最小闭环已闭合。
已解决上一轮的两个堵点：
1. 观察事实不再停留在 mailbox，能进入自我内部世界并触发来源需求重筹办。
2. 6127 成功后任务管理能读取可比较实际状态，不再因 缺少实际结果状态 重复待重筹办。
```

## 禁止宣称

```text
1. 不宣称四个外设方法自然链已通过。
2. 不宣称自我苏醒完成或初步成熟完成。
3. 不把外设线程作为动作动态来源。
4. 不把任务筹办 / 任务执行恢复为本能方法。
5. 不把 D455 报告本身宣称为世界树真值；它仍是材料和回查索引。
```
