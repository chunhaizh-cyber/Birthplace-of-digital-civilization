# 20260630 已确认存在体素融合分解 P8d Codex断点清单

## 0. 恢复摘要

当前切片：P8d 子轮廓候选到内部世界子存在身份映射。

依据：

```text
规范/已确认存在体素融合与分解规范20260630.md
计划/20260630_已确认存在体素融合分解施工计划_v0.1.md
docs/工程图谱/05_规则原子索引.md RULE-VOXEL-FUSION-005/006/007/008
实施记录/20260630_已确认存在体素融合分解_P8_Codex断点清单.md
```

## 1. 当前问题

```text
P8b 已经把父基础颜色写入项目结构特征。
子平均颜色仍停在“缺子候选映射”。
体素加工候选结果包按父存在、观察证据 ID、基准体素版本可定位，但一个包内可能有多个子轮廓候选。
内部世界子存在当前按来源空间候选编号建立，不能按数组顺序或第一候选猜测到体素子轮廓候选。
```

## 2. P8d 预测

```text
1. 新增子候选映射事实特征：体素子候选ID、体素局部子体素候选ID、体素子候选映射状态。
2. 提交入口只在父存在、观察证据 ID、基准版本和子候选 ID 唯一闭合时写映射成功。
3. 无候选、多候选、缺候选包、版本不一致或输入非法时，只写结构化映射状态。
4. 缺少无歧义映射时，体素子平均颜色事实状态仍为“缺子候选映射”，不得写猜测颜色。
5. 控制面板只读显示映射事实，不参与映射裁决。
```

## 3. 待验证

```text
python .\tools\生成函数功能说明文档.py
git diff --check -- 自我动作实现.外设模块.ixx 场景体素模块.ixx 控制面板类.ixx 控制面板类.cpp 视觉观察融合模块.ixx 实施记录/20260630_已确认存在体素融合分解_P8d_Codex断点清单.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

## 4. 禁止宣称

```text
不宣称 P8e/P9 完成。
不宣称自然链已写入子平均颜色。
不宣称视觉系统完成、自我苏醒完成或初步成熟完成。
```

## 5. 实施结果

```text
视觉观察融合模块.ixx：
  体素子轮廓候选增加局部子体素候选 ID；只有可生成局部体素候选时才赋值。

自我动作实现.外设模块.ixx：
  新增体素子候选 ID、体素局部子体素候选 ID、体素子候选映射状态三个项目结构特征入口。
  新增子候选映射状态码：已映射、缺候选包、无可映射候选、多可映射候选、版本不匹配、输入非法。
  提交_确认内部世界子存在事实 在子体素模型写入后，按父存在、观察证据 ID、基准体素版本和可选既有子候选 ID 查找无歧义映射。
  只有映射唯一且颜色统计有效时写体素子平均颜色；否则写结构化映射状态并保持子平均颜色事实状态为缺子候选映射或候选统计无效。

场景体素模块.ixx / 控制面板类.*：
  场景体素只读快照读取内部世界子体素的映射事实。
  控制面板 JSON 和列表显示子候选映射状态、子候选 ID、局部子体素候选 ID、颜色来源观察证据和基准版本。
```

## 6. 验证结果

```text
python .\tools\生成函数功能说明文档.py
  退出码：0
  函数条目：7526

git diff --check -- 自我动作实现.外设模块.ixx 场景体素模块.ixx 控制面板类.ixx 控制面板类.cpp 视觉观察融合模块.ixx 说明书/函数功能说明文档.md 实施记录/20260630_已确认存在体素融合分解_P8d_Codex断点清单.md
  结果：无空白错误；仅有控制面板类.cpp 和函数说明文档 CRLF/LF 提示。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
  日志：日志/msbuild_P8d_体素子候选映射_20260630.log
  结果：已成功生成；0 个警告；0 个错误。
```

## 7. 当前判断

```text
P8d 代码切片已闭合到结构化映射契约。
该切片同时具备 P8e 的安全写入路径：只有映射成功才写子平均颜色，否则拒绝颜色落账。
仍需 P9 用真实自然链验证是否出现无歧义映射和子平均颜色事实写入。
若自然链只有多子候选且没有逐子候选调用上下文，P9 应记录为内部世界逐子候选调度缺口，而不是伪造映射。
```

## 8. P9 真实运行补充

```text
过期构建运行锁：
  .codex_locks/build-run.lock 记录 pid=7532，action=run self-check P9 voxel mapping。
  Get-Process -Id 7532 无结果，判定为上一轮已退出进程留下的 stale lock，后续运行前可移除。

--panel-html：
  命令：.\x64\Debug\鱼巢.exe --panel-html *> 日志\run_P8d_panel_html_20260630.log
  退出码：0。
  边界：生成的 控制面板.html 未包含新增 childCandidateMappingState / voxelChildCandidateId 等模板字段；日志来源为 ADO-SQL，说明该路径使用 SQL / 静态投影，不等同于自我场景嵌入模板验证。

真实自然链：
  命令：.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=90000 *> 日志\r23_P9_voxel_mapping_20260630.txt
  退出码：0。
  日志/鱼巢_run_20260630.低值g:151754 观察体素加工任务提交成功，父存在=401，观察证据ID=33000003，基准体素版本=0。
  日志/鱼巢_run_20260630.低值g:151760 观察体素加工任务提交成功，父存在=41I，观察证据ID=33000013，基准体素版本=0。
  日志/鱼巢_run_20260630.低值g:154532 worker 候选结果包生成，父存在=4Fp，观察证据ID=37000002，基准体素版本=33000002，融合点=1，子轮廓候选数量=128，局部子体素候选数量=128。
  检索体素子候选映射状态 / 提交_确认内部世界子存在事实 / 自我_在内部世界提取子候选 / 自我_确认内部世界子存在，未命中 18:10-18:12 本轮自然运行的执行日志。
```

## 9. P9 堵点

```text
堵点锚点：
  worker 已经生成多子轮廓候选包，但自然任务链没有调度 6107/6108/6137 内部世界子候选提取、确认和提交入口。

已排除路径：
  1. 不是候选包完全不存在：真实日志已有多个体素加工候选包。
  2. 不是 P8d 映射写入函数编译失败：Debug x64 构建通过。
  3. 不是提交函数运行后拒绝映射：本轮日志没有提交函数运行痕迹。

当前判断：
  R23 P8d 代码切片闭合；P9 业务闭合遇到“体素加工候选包 -> 内部世界逐子候选任务 / 输入上下文”的调度契约缺口。
  继续强行把多候选包映射成一个子存在会违反 P8d 禁止按数组顺序、第一候选或显示颜色猜测映射的要求。

恢复条件：
  新增或确认一个正式调度契约：当候选包含有可生成局部体素候选的子轮廓时，由提交入口、任务管理或自我线程按项目通用结构生成逐子候选输入上下文，再自然调用 6107/6108/6137；每个子存在必须携带父存在、观察证据 ID、基准体素版本、子轮廓候选 ID 和局部子体素候选 ID。

禁止宣称：
  不宣称 P9 闭合，不宣称子平均颜色自然落账，不宣称内部世界子存在映射自然写入。
```
