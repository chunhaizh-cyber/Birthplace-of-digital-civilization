# 20260620 特征类非特征主信息直读审查 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_特征类逻辑纠偏计划_v0.1.md` 的 1.2，非特征主信息直接操作审查。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
规范/特征类规范20260429.md
规范/主信息字段与结构新增准入约束规范20260519.md
计划/计划索引.md
计划/20260620_特征类逻辑纠偏计划_v0.1.md
```

本切片范围：

```text
1. 审查 特征类.cpp 中非特征主信息直接读取。
2. 区分允许保留、应回迁 owning class、需新增功能函数的路径。
3. 本切片先形成判定表，不一次性改造全部路径。
```

验证前预测：

```text
1. 特征类::比较状态 直接读取 状态节点主信息类，应列为优先回迁 / 状态类功能函数候选。
2. 三维体素链函数读取 特征值类 节点主信息.值，属于值池内部 VecU 编解码访问，可能暂保留或后续补 特征值类 只读函数。
3. 特征类内部对 特征节点主信息类 / 抽象特征主信息类 的读取属于本类职责。
```

## 1. 审查结果

```text
rg -n "状态节点主信息类|取主信息<状态节点主信息类>|状态特征|状态值" 特征类.cpp
结果：
    特征类.cpp:4045-4046 直接取 状态节点主信息类。
    特征类.cpp:4052-4053 直接读取 状态特征。
    特征类.cpp:4063-4064 直接读取 状态值。
判定：
    应回迁到 状态类 功能函数；状态类当前已有 读取状态主体与特征 / 读取状态特征 / 读取状态主体，但缺少读取状态值或状态特征和值的只读函数。

rg -n "特征值类::节点类|取节点\(|->主信息\.值|主信息\.值" 特征类.cpp
结果：
    三维体素链查询和比较路径读取 特征值类 节点主信息.值。
判定：
    该路径属于 VecU / 三维体素链解释；特征值类已有 取VecU只读指针(VecU句柄)，但当前遍历需要节点结构，先登记为后续单独评估，不阻塞状态读取回迁。

rg -n "dynamic_cast<.*主信息|取主信息<.*主信息|->主信息" 特征类.cpp
结果：
    私有_直接基础信息子节点_按类型 只实例化为 抽象特征主信息类 / 特征节点主信息类。
    比较特征集合 dynamic_cast<const 特征节点主信息类*> 属于特征类本类职责，可保留或后续改用 取特征主信息 做局部风格统一。
```

当前判断：

```text
1. 当前阻塞项属于状态类 owning class 缺口：状态类缺少“读取状态特征和值”的只读功能函数。
2. 按跨类缺口转向规则，应把该缺口加入 计划/20260620_状态类逻辑纠偏计划_v0.1.md，并临时提升为第一优先切片。
3. 返回点：计划/20260620_特征类逻辑纠偏计划_v0.1.md 的 1.2；待状态类第一优先闭合后，用该函数替换 特征类::比较状态 中对 状态节点主信息类 的直接读取。
4. 本切片不改代码，不运行构建。
```
