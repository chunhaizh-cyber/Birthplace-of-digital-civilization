# 20260621 方法节点种类双事实源审计表

## 0. 范围

审计对象：`公共.节点种类 =` 写入口。

扫描命令：

```powershell
rg -n -P "公共\.节点种类\s*=(?!=)" -g "*.h" -g "*.cpp" -g "*.ixx" --glob "!x64/**" --glob "!Debug/**" --glob "!.git/**" .
```

## 1. 结论

```text
总命中：7 处。
构造期设置：3 处，均在 方法主信息类.h 的构造首 / 条件 / 结果节点函数中。
方法类内部归一化：3 处，均在 方法类.cpp 注册 / 补齐方法首节点路径中。
外部直接改写：1 处，在 本能方法类.cpp 默认本能方法种子补齐路径中。
```

## 2. 判定表

| 文件 | 行 | 写入口 | 分类 | 当前判断 | 第一优先建议 |
| --- | ---: | --- | --- | --- | --- |
| 方法主信息类.h | 247 | `构造首节点` | 构造期设置 | 允许保留；与 `角色=结构_方法首节点信息` 同步写入 | 后续节点种类收口时改为由 variant 推导 |
| 方法主信息类.h | 258 | `构造条件节点` | 构造期设置 | 允许保留；与 `角色=结构_方法条件节点信息` 同步写入 | 后续节点种类收口时改为由 variant 推导 |
| 方法主信息类.h | 266 | `构造结果节点` | 构造期设置 | 允许保留；与 `角色=结构_方法结果节点信息` 同步写入 | 后续节点种类收口时改为由 variant 推导 |
| 方法类.cpp | 1799 | `注册方法首节点` 已有节点分支 | 方法类内部归一化 | 已有节点由方法首节点查找入口取得，当前写入是冗余归一化 | 后续统一收口为方法主信息类内部 helper 或删除冗余写 |
| 方法类.cpp | 1896 | `注册需求位方法首节点` 结果方向分支 | 方法类内部归一化 | 已有节点由方法首节点查找入口取得，当前写入是冗余归一化 | 后续统一收口为方法主信息类内部 helper 或删除冗余写 |
| 方法类.cpp | 2001 | `补齐本能方法首节点` | 方法类内部归一化 | 属于方法类内部补齐路径；仍是双事实源写入 | 后续统一收口为方法主信息类内部 helper 或删除冗余写 |
| 本能方法类.cpp | 358 | 默认本能方法种子补齐 | 外部直接改写 | 外部类直接写方法主信息节点种类，违反“外部不得直接改 `公共.节点种类`”计划约束 | 下一切片回迁：改为调用方法类补齐 / 注册入口或方法主信息类受控入口，不在本能方法类裸写 |

## 3. 下一切片

```text
P0.3a：回迁 本能方法类.cpp:358 外部直接改写。
目标：本能方法类默认种子补齐不再裸写 方法主信息类::公共.节点种类。
边界：不把本能方法类职责塞入方法类；只收口方法主信息节点种类写入口。
验证：复扫 `公共.节点种类 =` 后，外部直接改写应为 0；若删改函数，重生函数说明文档并 Debug x64 构建。
```
