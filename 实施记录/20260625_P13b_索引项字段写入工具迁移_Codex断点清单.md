# 20260625 P13b 索引项字段写入工具迁移 Codex断点清单

## 0. 恢复摘要

当前切片：

```text
P13b：迁移 `线程生命周期消息模块.ixx::私有_写项字段`。
```

依据：

```text
计划/计划索引.md：当前主动队列为 P2/P13b。
计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md：B 类低风险函数逐个迁移到 全局共享函数类.ixx。
上一切片已新增 `全局共享函数类.ixx::索引项字段键`。
`私有_写项字段` 只组合索引项字段键和 `写入转义键值字段行`，不读取 / 写入项目结构。
```

预测验证：

```text
1. 迁移后源码和函数说明文档不再保留 `私有_写项字段` 定义。
2. `写入索引项转义键值字段行` 进入全局共享函数类，线程生命周期缓存写入点统一调用它。
3. 函数说明文档生成脚本成功。
4. Debug x64 构建成功。
```

禁止宣称：

```text
不宣称 P13 完成。
不宣称函数去重全部完成。
不宣称自我苏醒完成或初步成熟完成。
```

## 1. 执行结果

```text
1. 在 `全局共享函数类.ixx` 新增 `写入索引项转义键值字段行(std::ostream&, std::size_t, std::string_view, std::string_view)`。
2. 删除 `线程生命周期消息模块.ixx::私有_写项字段`。
3. `线程生命周期消息模块.ixx` 的线程信息表缓存字段写入点统一调用全局 `写入索引项转义键值字段行`。
```

## 2. 验证证据

```text
源码复扫：
    `私有_写项字段` 未检出。
    `写入索引项转义键值字段行` 仅在 `全局共享函数类.ixx` 定义和 `线程生命周期消息模块.ixx` 缓存写入调用点出现。

函数说明文档：
    python .\tools\生成函数功能说明文档.py
    结果：成功；源码文件 127；函数条目 7213。
    `说明书/函数功能说明文档.md` 中 `私有_写项字段` 未检出。
    `说明书/函数功能说明文档.md` 中 `写入索引项转义键值字段行` 定义已登记。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo
    结果：成功；0 警告；0 错误。
```

## 3. 当前判断

```text
本切片闭合。
该迁移不改变线程生命周期缓存文件格式，只删除本地薄包装并复用全局键值字段写入工具。
不涉及任务、需求、方法、世界事实或动作动态写权变化。
```
