# 20260627 函数清理最终无调用复扫 Codex断点清单

## 0. 恢复摘要

当前切片：

```text
20260618_项目功能函数去重与无效代码清理计划_v0.1 最终无调用 / 无效代码复扫。
```

当前目标：

```text
1. 基于当前源码和 说明书/函数功能说明文档.md 再跑一轮 live 无调用候选复扫。
2. 只删除满足以下条件的硬证据候选：
   源码无直接调用；
   无构造 / 析构隐式调用、virtual / override、入口 / 回调 / 导出、注册表、消息分发、模板实例、宏拼接或外部公开合同引用；
   不承担项目结构读写证据。
3. 若复扫未发现可直接删除对象，则把 20260618 函数去重与无效代码清理计划按当前边界标记完成。
```

已读取依据：

```text
AGENTS.md 用户提供版本
docs/00_文档总索引.md
规范/禁止性规范20260611.md
规范/规范目录.md
docs/工程图谱/00_图谱总索引.md
docs/工程图谱/05_规则原子索引.md
计划/计划索引.md
计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md
实施记录/20260618_函数去重与无效代码清理_Codex断点清单.md
实施记录/20260627_P13b_剩余B低风险候选复核_Codex断点清单.md
```

本轮禁止越界：

```text
不继续 P1e 自我所在场景复杂动作提取审计。
不把领域函数、模块状态函数或项目结构读写函数硬迁入 全局共享函数类.ixx。
不把 C++ 粗筛 call_like_count 直接当作删除依据。
不混入上一轮 P1e 未跟踪断点文件。
不宣称自我苏醒完成或初步成熟完成。
```

下一步：

```text
生成当前源码 live 无调用候选表，复核最硬候选；根据结果删除或登记无可删结论。
```

## 1. 新版 codebase-memory-mcp 试用记录

用户要求试用：

```text
D:\工具\codebase-memory-mcp\bin\v0.8.1-cjk-calls-clean\codebase-memory-mcp.exe
指南：D:\工具\codebase-memory-mcp\fishnest-query-rules\enable-new-version-guide.md
```

已验证：

```text
1. exe 存在，版本为 codebase-memory-mcp 0.8.1-cjk-calls-clean。
2. `codex mcp list/get codebase-memory-mcp` 显示当前启用命令已指向 clean 版。
3. CLI `search_graph` 查询 `任务管理外设消息承接` 返回中文结果正常，无乱码。
4. CLI `trace_path outbound` 对
   `D.任务模块.管理工作线程.任务管理外设消息承接_处理一次`
   已返回 CALLS 下游函数。
5. 当前会话 MCP 工具可用：
   `list_projects` 显示 `D` 项目 root_path 为 `D:/鱼巢`；
   `index_status` 显示 nodes=34104、edges=78958、status=ready；
   `get_graph_schema` 显示 CALLS=19554；
   `trace_path` 一层 outbound 返回 9 个直接下游；
   `get_code_snippet` 可按 qualified_name 读取源码片段。
```

当前判断：

```text
新版 MCP 可作为后续结构定位和调用链初筛工具。
C++ 删除判断仍必须以源码复核和构建验证兜底，不能单凭图谱 CALLS 判定删除。
```

## 2. 最终 live 无调用复扫

初始复扫输出：

```text
实施记录/20260627_P2_final_live_无调用候选重扫.tsv
扫描定义：6520
唯一函数名：5467
源码文件：127
call_like_count=0：0
call_like_count=1：829
call_like_count<=2：2809
```

复核结论：

```text
1. 大量 `call_like_count=1` 为类成员定义按限定名仅命中定义行，不作为直接删除依据。
2. 非类成员 `call_like_count=1` 中，析构、operator、模板、RAII 守卫、线程主体、窗口过程、回调、公开成员和项目结构读写接口均保留。
3. `度量模板注册表类.ixx::获取或抛` 虽然全名只出现一次，但属于导出类公开成员，保留。
4. `本能方法类.cpp` 中以下匿名命名空间残余函数当前只在定义处出现，且已不再被注册流程调用：
   `私有_本能方法首节点已可执行`
   `私有_补齐本能方法结构`
5. 删除上述两项后，继续复扫发现其专属残余 helper 同样无源码调用，且属于旧“启动注册创建方法虚拟存在 / 条件场景 / 结果场景 / 分类特征”链路；
   该链路与当前文件头规则“默认本能方法注册必须保持启动轻量，只确保方法头和本能动作句柄”不一致。
```

本轮删除：

```text
本能方法类.cpp：
    #include "方法虚拟存在服务类.h"
    私有_方法虚拟存在名称词
    私有_方法虚拟存在类型词
    私有_方法特征_方法类型
    私有_方法特征_本能方法分类
    私有_推导本能方法类型
    私有_写入方法类型特征
    私有_写入本能方法分类特征
    私有_条件场景名称词
    私有_结果场景名称词
    私有_本能方法首节点已可执行
    私有_补齐本能方法结构
```

删除后复扫输出：

```text
实施记录/20260627_P2_final_live_无调用候选重扫_after_round1.tsv
扫描定义：6509
唯一函数名：5459
源码文件：127
call_like_count=0：0
call_like_count=1：827
非类成员 call_like_count=1：69
```

删除后剩余边界：

```text
非类成员 `call_like_count=1` 中，按全名文本出现次数复核后，只剩：
    析构 / operator / 构造 / 模板 / RAII 守卫；
    线程主体、窗口过程、回调函数；
    公开成员或公开基础结构 API；
    项目结构读写接口或消息批次 / 路由 / 协议内部入口；
    过短函数名导致的中文文本粗筛噪声。
当前未再发现“私有、无调用、非入口 / 回调 / 注册 / 公开合同、且不承担项目结构读写证据”的可直接删除函数。
```

## 3. 工程守卫验证

已运行：

```text
python .\tools\生成函数功能说明文档.py
    退出码：0
    源码文件：127
    函数条目：7209

git diff --check
    退出码：0
    仅输出 CRLF 提示，无 whitespace error。

python .\tools\check_specs.py
    退出码：0
    hard issues：0
    warnings：73，均为既有规范治理 warning。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码：0
    0 警告，0 错误。
```

当前判断：

```text
20260618_项目功能函数去重与无效代码清理计划_v0.1 在当前边界下可以标记完成：
    P11 无调用直接删除队列已多轮清理；
    P13b 低风险 B 类迁移已复核到边界；
    本轮最终 live 复扫删除 11 个残余函数定义后，未再发现可直接删除的硬证据候选。
```
