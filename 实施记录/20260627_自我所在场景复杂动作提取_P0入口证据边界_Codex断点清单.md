# 20260627 自我所在场景复杂动作提取 P0 入口证据边界 Codex断点清单

## 0. 恢复摘要

当前切片：`计划/20260626_自我所在场景复杂动作提取计划_v0.1.md` / P0：冻结输入入口和证据边界。

切换原因：用户要求函数迁移先告一段落，并调整 `计划/计划索引.md` 执行其他任务；P13b 低风险候选线暂止，不继续硬迁移剩余候选。

本轮目标：

```text
1. 只读定位已入账当前场景状态 / 动态入口。
2. 只读定位观察材料、扫描 / 跟踪输出到场景事实提交链的入口。
3. 固定动作提取允许读取和禁止读取的证据边界。
4. 判断是否存在“已入账当前场景动态入口”；不存在则不进入 P1，转场景 / 动态类入口缺口。
```

禁止项：

```text
1. 不改业务代码。
2. 不新增函数；后续若进入代码实现，必须先查 说明书/函数功能说明文档.md。
3. 不把原始相机帧、未归属观察候选、场景体素、运行期心像缓存或控制面板显示当作事实裁决来源。
4. 不把外部场景动态伪装成自我方法动作动态。
5. 不宣称稳定动作规则已完成、外部动作已成为自我方法候选、动作提取链已闭合。
```

依据文件：

```text
AGENTS.md
docs/00_文档总索引.md
规范/禁止性规范20260611.md
规范/规范目录.md
规范/方法动作动态硬规则规范20260512.md
规范/自我所在场景认知工作区规范20260525.md
规范/当前场景特征值明确标准规范20260602.md
规范/相机外设综合工作流程规范20260607.md
docs/工程图谱/00_图谱总索引.md
docs/工程图谱/02_外设观察事实图谱.md
docs/工程图谱/05_规则原子索引.md
docs/工程图谱/08_事实来源与写入权限图谱.md
```

预测输出：

```text
动作提取输入入口清单
可读取场景动态类型清单
禁止读取证据清单
当前阻塞判断
```

## 1. P0 只读审计记录

### 1.1 工具边界

`codebase-memory-mcp` 本轮辅助定位失败：

```text
list_projects / index_status -> Transport closed
处置：按鱼巢查询规约降级为 rg 精确检索 + 源码短片段复核。
判断：这是工具通道边界，不记为项目实现失败。
```

### 1.2 动作提取输入入口清单

允许作为 P1+ 读取入口的材料：

| 类别 | 当前入口 | 源码证据 | P0 判断 |
| --- | --- | --- | --- |
| 已入账当前场景动态 | `世界树类::获取场景动态` -> `动态类::获取场景动态` -> `场景类::读取场景动态索引快照` | `世界树类.cpp:3243-3246`、`动态类.cpp:1290-1297`、`场景类.cpp:329-339` | 可作为动作提取第一读取入口。 |
| 场景动态索引写入 | `动态类::创建动态` 创建成功后调用 `场景类::追加动态` | `动态类.cpp:912-924`、`场景类.cpp:404-420` | 说明场景动态目录是已入账动态索引，不是显示缓存。 |
| 动态基础读取 | `读取动态因果生成输入`、`读取动态来源场景`、`读取动态来源低层动态` | `动态类.cpp:503-531`、`动态类.cpp:534-558`、`动态类.cpp:637-655` | 可回查主体、初始状态、结果状态、来源动作、动态类型、层级和来源场景。 |
| 已裁决观察材料派生事实 | `提交_确认观察存在事实` 写入已确认观察存在、当前场景集合状态、特征可读状态，并通过 `写入双目相机I64事实与动态` 生成状态迁移 / 方法动作动态 | `自我动作实现.外设模块.ixx:17380-17456`、`自我动作实现.外设模块.ixx:23000-23018`、`自我动作实现.外设模块.ixx:23037-23060` | 可作为事实提交后的输入，不可跳过提交链。 |
| 扫描事实 / 动态输出 | `自我_扫描当前场景` 从扫描报告构造已合法数据项，写当前特征值、状态动态、扫描闭合动作动态和因果信息数量 | `自我动作实现.扫描.ixx:1817-1830`、`自我动作实现.扫描.ixx:1862-1883`、`自我动作实现.扫描.ixx:1992-2119`、`自我动作实现.扫描.ixx:2135-2160` | 可作为扫描方法执行后的事实 / 动态入口；扫描报告本身不是最终事实。 |
| 跟踪事实提交 | `提交_指定存在跟踪事实` 只在目标存在、上游跟踪状态 / 窗口状态、跟踪来源质量成立后写跟踪状态和动作动态 | `自我动作实现.外设模块.ixx:24630-24780` | 可作为正式提交后的动态来源；跟踪报告只作材料。 |
| 运行期心像 / 复现快照 | `自我_导出当前场景识别快照`、`自我_导出OpenGL复现数据` | `自我动作实现.外设模块.ixx:26120-26218` | 只可作复现 / 只读参考；日志明确“不读取相机预览，不回写控制面板”。 |

### 1.3 可读取场景动态类型清单

当前可读动态层级 / 类型：

| 类型 | 形成入口 | 可读取字段 | 边界 |
| --- | --- | --- | --- |
| 原子相邻状态变化动态 | `动态类::创建动态(场景, 初始状态, 结果状态, 起, 止)` | 初始状态、结果状态、主体、特征、时间段、路径签名、层级 0、聚合方式 `原子相邻` | 可作为 P1 原子动态标准化主材料。 |
| 存在动态 | `动态类::创建存在动态` | 被观察存在、初始状态、结果状态、来源低层动态、层级 1 | 表示已确认存在的状态变化，不等同方法动作。 |
| 场景动态 | `动态类::创建场景动态` | 父场景、子场景、初始状态、结果状态、来源低层动态、层级 2 | 表示场景 / 关系变化，适合外部复杂动作候选读取。 |
| 方法动作动态 | `动态类::创建方法动作动态` / `世界树类::创建方法动作动态` | 来源方法、来源动作名、输入场景、输出场景、执行状态、状态端点 | 只能表示自我方法运行事实，不得用外部变化伪装。 |
| 聚合动态 | `读取动态聚合信息` + `读取动态来源低层动态` | 聚合层级、聚合方式、路径签名、来源低层动态链 | 可用于片段组 / 复杂动作候选，但要回查低层动态证据。 |

动态类型推导依据：

```text
动态类.cpp:108-121：优先取来源动作名 / 方法动作名；有初始或结果状态时为“状态变化”；否则按动态特征或“未知动作”。
动态类.cpp:124-135：来源动作指针 -> 主动动作动态；来源动作名或无状态端点的主体 / 特征动态 -> 被动动作动态；其余为无动作状态变化动态。
```

### 1.4 观察材料到事实提交链

已确认链路：

```text
D455 / 外设线程
-> 外设观察报告队列：逐簇识别报告 / 扫描变化报告 / 跟踪报告
-> 判断外设观察材料质量
-> 任务管理外设消息承接生成 结构_任务管理观察事实承接请求
-> 自我线程落观察事实承接请求到自我内部世界
-> 后续 6146 / 6147 任务主体确保
-> 对应本能方法执行后写入状态、动态和因果材料
```

关键证据：

```text
外设观察报告队列.ixx:41-45 定义报告类型。
外设观察报告队列.ixx:2078-2198 判定材料质量、扫描变化候选和跟踪动态候选。
任务模块.管理工作线程.ixx:3439-3525 只有质量通过后才生成观察事实承接请求并标记报告已消费。
任务模块.工作线程消息协议.ixx:266-287 定义观察事实承接请求结构字段。
自我线程模块.impl.cpp:13526-13567 回查报告并校验约束 generation。
自我线程模块.impl.cpp:13587-13801 只落内部世界治理变化，并明确“禁止直接写世界真值=是”。
自我线程模块.impl.cpp:13802-13812 再确保扫描 / 跟踪任务主体。
```

### 1.5 禁止读取证据清单

P1+ 不得直接读取以下材料作为动作提取事实：

| 禁止材料 | 禁止原因 | 可用替代 |
| --- | --- | --- |
| 原始相机帧、深度帧、彩色帧 | 规范禁止外设线程、报告、队列项或控制面板显示直接成为世界树事实 | 读取事实提交后的状态 / 动态 / 已确认观察存在。 |
| 未归属观察候选、空间候选、像素簇 | 观察候选仍在证据层，未完成质量验证、同一性比较、归属判断和事实提交 | 读取 `提交_确认观察存在事实` 后的已确认观察存在。 |
| 扫描变化报告 / 跟踪报告本身 | 报告只是材料；质量判定只生成承接请求或后续任务，不直接完成动作事实 | 读取 `自我_扫描当前场景` / `提交_指定存在跟踪事实` 的状态、动态、动作动态。 |
| 场景体素、OpenGL 复现数据、识别快照 | 运行期心像 / 复现层可辅助调试，不改变事实来源权 | 只作候选证据或可视化回放，不作为动作候选最终事实。 |
| 控制面板显示 / SQL 镜像 | 显示镜像不参与世界树事实裁决 | 读取世界树、状态、动态、因果链。 |

### 1.6 P0 阻塞判断

```text
是否找到已入账当前场景动态入口：是。
是否只有观察材料、没有事实提交后的状态 / 动态：否；已有提交确认、扫描、跟踪事实提交和动态写入路径。
是否需要场景体素辅助：可作为候选证据，但不改变事实来源权。
```

结论：

```text
P0 无入口阻塞，可以进入 P1“原子动态标准化”的映射 / 设计切片。
进入 P1 时仍不得新增专用结构体、主信息字段或并行权威账本；先复用动态节点、状态端点、来源低层动态、动态层级、聚合方式、来源场景和因果输入读取入口。
```

## 2. P1a 原子动态标准化映射审计

### 2.1 P1 合同字段映射

P1 验收要求：每个原子动态必须能回查动态主体、初始状态、结果状态、记录场域和证据来源。

现有结构承载：

| P1 合同字段 | 现有承载 | 源码证据 | P1a 判断 |
| --- | --- | --- | --- |
| 动态主体 | `动态节点主信息类::动态主体`，也可由状态端点主体补齐 | `基础信息主信息类.h:497-503`、`动态类.cpp:437-450` | 已有。 |
| 动态特征 | `动态节点主信息类::动态特征`，状态端点也有 `状态特征` | `基础信息主信息类.h:481-483`、`基础信息主信息类.h:497-503` | 已有。 |
| 初始状态 / 结果状态 | `动态节点主信息类::初始状态` / `结果状态` | `基础信息主信息类.h:497-500` | 已有。 |
| 初始值 / 结果值 | 通过 `状态类::读取状态主体特征和值引用` 或 `读取状态特征和值` 回读状态值 | `状态类.cpp:160-181`、`状态类.cpp:221-237` | 已有。 |
| 记录场域 | `动态节点主信息类::来源输入场景` / `来源输出场景`，以及动态所属场景索引 | `基础信息主信息类.h:505-515`、`动态类.cpp:534-558`、`场景类.cpp:404-420` | 已有。 |
| 时间窗口 | `动态节点主信息类::时间`，可用 `读取动态双状态时间段` | `基础信息主信息类.h:497-503`、`动态类.cpp:568-582` | 已有。 |
| 证据来源 | `来源动作`、`来源动作名`、`来源低层动态`、聚合层级 / 聚合方式 / 路径签名 | `基础信息主信息类.h:505-515`、`动态类.cpp:604-655` | 已有。 |
| 原子 / 聚合判定 | `动态层级` + `聚合方式`，其中 `原子相邻` 已有枚举 | `基础信息主信息类.h:24-29`、`动态类.cpp:626-635` | 已有。 |

P1a 结论：原子动态标准化不需要第一步新增主信息字段或并行账本；应先做只读分类视图。

### 2.2 首批原子动态类型覆盖判断

| P1 原子动态类型 | 当前可映射证据 | 覆盖判断 | 下一步 |
| --- | --- | --- | --- |
| 位置变化 | `自我_扫描当前场景` 已有空间位置写入、旧 / 新空间坐标、变化量和状态变化动态；`扫描空间位置变化量` 可形成动作动态。 | 强覆盖。 | P1b 可先以位置变化作为最小样本。 |
| 速度变化 | 可由同主体连续位置动态 + 时间窗口派生，但未见直接速度状态生产入口。 | 派生可行，直接生产不足。 | 需要只读派生规则，不新增底层字段。 |
| 方向变化 | 现有 `改变方向`、比较模板方向区间等通用材料，但未见外部场景移动方向的正式状态动态生产入口。 | 部分材料。 | 后续按位置连续动态推导方向候选。 |
| 高度变化 | 有局部边界框高度、范围坐标 Y、相机帧高度等材料，但未见统一高度状态动态入口。 | 部分材料。 | 先列为候选映射，需事实提交链复核。 |
| 姿态变化 | 只命中自我场景姿态明确状态等材料，未见通用外部主体姿态变化生产链。 | 覆盖不足。 | 暂不进入首批代码实现。 |
| 可见性变化 | 跟踪链有目标存在丢失状态、窗口状态；扫描有未命中 / 消失候选材料。 | 部分材料。 | 可作为跟踪 / 扫描派生候选，不直接宣称通用可见性动态。 |
| 接触关系变化 | 命中 `存在_接触自我边界候选状态`，偏自我边界候选，不是任意两主体接触关系。 | 部分材料。 | 限定为自我边界接触候选，暂不泛化。 |
| 支撑关系变化 | 本轮未命中稳定生产入口。 | 覆盖不足。 | 登记待补证据，不实现。 |
| 遮挡关系变化 | 命中当前帧遮挡判断状态、遮挡像素数、关键区域遮挡候选状态。 | 部分材料。 | 可作为观察证据候选，需事实提交后再归入原子动态。 |
| 相对距离变化 | 可由两个主体的位置动态派生，但未见关系状态动态生产入口。 | 派生可行，直接生产不足。 | 后续从位置动态窗口派生，不新增关系账本。 |
| 包含关系变化 | 本轮未命中稳定生产入口。 | 覆盖不足。 | 登记待补证据，不实现。 |
| 开合状态变化 | 本轮未命中稳定生产入口。 | 覆盖不足。 | 登记待补证据，不实现。 |

关键证据：

```text
计划/20260626_自我所在场景复杂动作提取计划_v0.1.md:230-259 定义 P1 目标、首批原子动态类型和验收。
自我动作实现.扫描.ixx:1385-1415 创建扫描空间位置变化动作动态。
自我动作实现.扫描.ixx:1418-1488 写入场景当前特征值_空间位置，并在状态变化成立时生成动态。
自我动作实现.扫描.ixx:1978-1985 明确当前只实现空间位置统一写入；没有写入摘要时保留写入接口缺口。
自我动作实现.外设模块.ixx:272-279 已有观察变化事件类型枚举材料，但不能替代正式动态链。
自我动作实现.外设模块.ixx:21771-22139 跟踪链写目标观察窗口状态、目标存在丢失状态和跟踪相关动态。
```

### 2.3 P1a 阻塞判断

```text
结构阻塞：无。动态 / 状态主信息和读取入口能支撑 P1 合同字段。
覆盖阻塞：有。首批 12 类原子动态不是全部已有生产链；当前只适合先拿“位置变化”做最小闭合样本。
实现边界：不得直接新增动作提取结构体、主信息字段或新账本；P1b 若进入代码，先查 说明书/函数功能说明文档.md，再决定复用或新增对应类内只读 helper。
```

P1a 结论：

```text
P1 原子动态标准化应拆为 P1b 最小样本：基于已入账场景动态读取“位置变化”原子动态视图。
其它类型暂登记为候选映射和覆盖缺口，不进入首批代码实现。
```

## 3. P1b 位置变化原子动态视图最小样本

### 3.1 预测

```text
输入：已入账场景动态目录中的动态节点。
处理：逐条读取动态双状态端点，再读取初始 / 结果状态的主体、特征和值。
命中条件：
    1. 初始状态和结果状态都存在；
    2. 初始 / 结果状态主体一致；
    3. 初始 / 结果状态特征一致；
    4. 特征语义类型命中位置变化类结构入口；
    5. 初始值和结果值不同；
    6. 动态仍可通过既有来源场景、时间段、来源低层动态等入口继续回查证据。
输出：位置变化原子动态候选节点列表。
禁止：不新增主信息字段、不新增动作提取持久结构、不读取原始外设报告或控制面板缓存、不把外部变化写成自我方法动作动态。
```

### 3.2 项目结构映射

| 业务概念 | 项目承载结构 | 值类型 | 写入方 | 读取方 | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| 场景动态目录 | 场景节点动态索引 -> 动态节点 | 动态节点指针 | 动态类 / 场景类既有写入链 | 动态类 / 世界树类只读入口 | `获取场景动态` 回读 |
| 位置变化候选 | 动态节点 | 指针句柄 / 动态节点 | 已有扫描或事实提交链 | 新增只读 helper | 构建 + 源码复核 |
| 动态主体 | 初始 / 结果状态的状态主体 | 可解析引用 | 状态类既有写入链 | 状态类读取函数 | 同主体比较 |
| 动态特征 | 初始 / 结果状态的状态特征 | 可解析引用 / 语素入口 | 状态类既有写入链 | 状态类 + 特征类读取函数 | 同特征 + 特征语义类型命中 |
| 初始 / 结果值 | 状态值 | 特征值 | 状态类既有写入链 | 状态类读取函数 | `特征值` 比较 |

### 3.3 本切片改动范围

```text
动态类.h / 动态类.cpp：
    新增只读函数：读取位置变化原子动态候选。

世界树类.h / 世界树类.cpp：
    新增薄封装：读取位置变化原子动态候选。

说明书/函数功能说明文档.md：
    代码新增函数后由脚本刷新。
```

### 3.4 P1b 实施与验证证据

```text
Graph RAG 辅助定位：
    codebase-memory-mcp index_status project=D -> ready，nodes=34104，edges=78958。
    search_code 读取动态因果生成输入 -> 命中 动态类.cpp 中既有只读入口。
    本轮只用作结构定位和交叉验证，不把 C++ outbound 调用链当完整事实来源。

代码实现：
    动态类::读取位置变化原子动态候选：
        从 获取场景动态 读取已入账场景动态目录；
        用 读取动态因果生成输入 回读初始 / 结果状态；
        用 状态类::读取状态主体特征和值引用 回读主体、特征和值；
        用 私有_解析特征引用 支持特征引用的指针或主键只读解析；
        仅在同主体、同特征、值发生变化且特征语义类型命中位置变化范围时返回动态节点。
    世界树类::读取位置变化原子动态候选：
        仅薄封装转发到动态服务，不新增事实写入路径。

函数说明文档：
    命令：python .\tools\生成函数功能说明文档.py
    结果：已生成，源码文件 127，函数条目 7216。

构建：
    命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    结果：已成功生成，0 个警告，0 个错误。
```

P1b 收口判断：

```text
状态：代码接入闭合。
证据边界：本轮只证明只读视图入口、函数说明文档一致性和 Debug x64 构建通过。
自然样本：未构造、未伪造、未用旧日志替代；后续若需要真实行为证据，等待自然场景动态样本或真实运行日志。
禁止宣称：不宣称动作提取链已闭合、不宣称稳定动作规则已完成、不宣称外部动作已成为自我方法候选。
```
