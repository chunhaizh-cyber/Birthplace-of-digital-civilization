# 20260608 S1d4l 动态类索引引用解析指针优先 Codex 断点清单

## 当前切片

- 主动计划：`计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- 执行方案：`计划/20260607_S1d4l动态类索引引用解析指针优先方案_v0.1.md`
- 切片：S1d-4l 动态类索引引用解析指针优先。

## 依据文件

- `AGENTS.md`
- `规范/规范目录.md`
- `计划/计划索引.md`
- `规范/动态信息未实现功能补齐详细设计20260430.md`
- `规范/方法动作动态硬规则规范20260512.md`
- `规范/动作动态即因果账本规范20260512.md`
- `计划/20260607_S1d4k状态创建后处理内部阶段定位方案_v0.1.md`
- `实施记录/20260608_S1d4k状态创建后处理内部阶段定位_Codex断点清单.md`

## 当前判断

- S1d-4k 已闭合并提交：`49b8148`。
- 当前主耗时是 `状态记录后刷新动态`。
- 规范要求状态落账后自动尝试生成状态动态，不能为提速删除该流程。
- `动态类.cpp` 中状态 / 动态引用解析当前主键优先；场景状态索引和动态索引通常已有指针+主键双锚点。
- 本切片只改解析顺序：指针有效且主键为空或匹配时先返回指针，主键查找和全量扫描仍兜底。

## 构建 / 运行前预测

- Debug x64 构建通过。
- 真实运行退出码 0。
- 日志保留 `任务状态动作动态=<动态主键>`。
- 日志可继续出现 `状态创建/内部阶段耗时`。
- 不出现 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`。
- 不出现旧路径锚点 `准备本能调用`、`执行本能方法`、`任务域本能输入包`。

## 已改文件

- `计划/20260607_S1d4l动态类索引引用解析指针优先方案_v0.1.md`
- `实施记录/20260608_S1d4l动态类索引引用解析指针优先_Codex断点清单.md`
- `动态类.cpp`

## 已完成修改

- `动态类.cpp` 新增本地模板 `私有_尝试按指针解析引用`。
- `私有_解析状态引用` 改为指针有效、主信息类型正确、主键为空或匹配时先返回指针。
- `私有_解析动态引用` 同口径改为指针优先。
- 主键不匹配时不使用指针，仍保留旧主键查找和全量扫描兜底。

## 已运行命令

- `git diff --check -- '动态类.cpp' '计划/20260607_S1d4l动态类索引引用解析指针优先方案_v0.1.md' '实施记录/20260608_S1d4l动态类索引引用解析指针优先_Codex断点清单.md'`
- `Get-Process | Where-Object { $_.ProcessName -eq '鱼巢' }`
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000`
- `rg -n "任务状态动作动态=|状态创建/内部阶段耗时|任务状态被动提交/内部阶段耗时|任务状态动作动态创建/内部阶段耗时" '日志/鱼巢_run_20260608.低值g'`
- `rg -n "任务状态被动提交/逻辑错误|任务状态动作动态逻辑错误|准备本能调用|执行本能方法|任务域本能输入包|后台worker工作项进入死信|拒绝归并完成项" '日志/鱼巢_run_20260608.低值g'`
- PowerShell 汇总最新线程状态创建 / 被动提交 / 动作动态创建内部阶段采样。

## 实际验证结果

- `git diff --check` 通过，仅有工作区 LF/CRLF 提示。
- Debug x64 构建通过；链接阶段仅见既有 `/EDITANDCONTINUE` 与 `/INCREMENTAL:NO` 组合提示。
- 真实运行退出码 0。
- 最新日志：`日志/鱼巢_run_20260608.低值g`，更新时间 2026-06-08 09:34:36。
- 正向证据：最新线程 T39044 有 56 条 `任务状态动作动态=<动态主键>` 状态提交日志。
- 负向证据：未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`、`后台worker工作项进入死信`、`拒绝归并完成项`。

## 关键耗时

- 最新线程：T39044。
- 最新线程状态创建内部采样：28 条。
- 最新线程状态创建最慢阶段分布：`状态动态刷新=28`。
- 最新线程平均状态创建总耗时：约 642827us。
- `状态动态刷新`：平均约 642089us，最大 1366770us。
- `状态写入后整理`：平均约 486us。
- 任务状态被动提交内部采样：30 条，最慢阶段分布 `动作动态创建=30`。
- `动作动态创建`：平均约 621559us。
- 任务状态动作动态创建内部采样：29 条，最慢阶段分布 `新状态节点=29`。
- `新状态节点`：平均约 640002us。
- `方法动作动态`：平均约 426us。

## 当前结论

- S1d-4l 没有破坏任务状态动作动态生成和旧路径删除边界。
- 指针优先解析降低了 `状态动态刷新` 平均耗时，但热点仍在 `状态动态刷新`。
- 下一步应进入 S1d-4m：拆 `动态类::查找相邻前状态` 内部阶段，确认慢点是状态索引快照复制、倒序扫描规模，还是候选匹配判定。

## 下一步

1. 更新计划索引和总性能计划，登记 S1d-4l 闭合证据。
2. 自动提交 S1d-4l 闭合切片。
3. 转入 S1d-4m：`动态类::查找相邻前状态` 内部阶段定位。

## 禁止宣称

- 不宣称自我苏醒完成。
- 不宣称初步成熟完成。
- 不把指针快路径当成跳过状态动态刷新。
- 不把构建通过当作任务状态动作动态业务链通过。
