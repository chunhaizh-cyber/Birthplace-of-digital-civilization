# 20260621 任务类逻辑纠偏 Codex 断点清单

## 0. 恢复摘要

当前切片：

```text
计划/20260620_任务类逻辑纠偏计划_v0.1.md 的 P0：任务主信息外部直读 / 写入扫描、分层和最小 helper 缺口登记。
```

依据文件：

```text
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_任务类逻辑纠偏计划_v0.1.md
规范/任务筹办与执行边界规范20260512.md
规范/自我线程与任务管理线程权责边界规范20260512.md
```

硬边界：

```text
1. 任务筹办 / 任务执行仍是任务管理生命周期普通函数，不登记为本能方法，不进入候选方法集，不作为动作动态来源。
2. 自我线程不得直接裸写任务状态；任务状态推进由任务管理线程编排提交。
3. 机器查找 / 生成逻辑不得读取 `语素集.安全获取词(...)` 得到的文本；必须用语素入口身份、具体特征类型、方向 / 枚举值、状态 / 特征节点和结构化值。
4. 本切片先扫描分类，不直接改任务生命周期语义。
```

## 1. P0 扫描分类构建前预测

计划修改范围：

```text
1. 生成任务主信息外部直读扫描清单。
2. 生成任务模块结构扫描清单。
3. 生成任务类自身跨类直读扫描清单。
4. 形成分类表：机器逻辑 / 任务管理提交链内部 / 显示日志 SQL / 已有任务类函数可复用 / 缺任务类函数 / 跨类缺口。
```

预测：

```text
本切片只新增实施记录和日志，不新增函数，不需要重新生成函数说明文档，不需要运行 Debug x64 构建。
若扫描发现明确机器逻辑直读且已有任务类函数可复用，下一小切片再做代码回迁。
```

## 2. P0a 任务管理工作线程任务主信息直读回迁结果

实际修改范围：

```text
1. 新增任务类只读 / 解析 helper：
   读取任务对应需求引用 / 解析任务对应需求；
   读取任务虚拟存在引用 / 解析任务虚拟存在；
   读取任务场景引用 / 解析任务场景；
   读取任务目标状态引用 / 解析任务目标状态；
   读取任务创建时间 / 读取任务启动时间 / 读取任务完成时间。
2. 回迁 `任务模块.管理工作线程.ixx` 内直接读取任务主信息的调用点。
3. 更新 `说明书/函数功能说明文档.md` 和 `计划/20260620_任务类逻辑纠偏计划_v0.1.md` 的 P0a 证据。
```

验证证据：

```text
函数说明文档生成：
    python .\tools\生成函数功能说明文档.py
    结果：函数条目 7186。

复扫：
    日志/20260621_T0a_任务管理工作线程直读复扫.txt
    结果：无命中。

全局外部任务主信息直读复扫：
    日志/20260621_T0a_任务主信息外部直读复扫.txt
    结果：剩余 89 行；下一候选集中在 `任务模块.筹办.ixx` 与 `任务模块.管理界面线程.impl.cpp`。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> '日志\20260621_T0a_任务管理工作线程任务主信息回迁_build.log'
    结果：已成功生成；0 个错误。

差异检查：
    git diff --check -- 任务类.h 任务类.cpp 任务模块.管理工作线程.ixx 说明书/函数功能说明文档.md 计划/20260620_任务类逻辑纠偏计划_v0.1.md
    结果：无空白错误。
```

当前判断：

```text
P0a 闭合；不宣称任务类逻辑纠偏完成。
下一步继续 P0，优先选择 `任务模块.筹办.ixx` 的单字段簇，避免直接进入 `任务模块.管理界面线程.impl.cpp` 的写入边界大切片。
```

## 3. P0b 任务筹办模块任务主信息直读回迁预测

计划修改范围：

```text
1. 只修改 `任务模块.筹办.ixx`。
2. 将 `对应需求 / 场景 / 任务虚拟存在` 三类任务主信息字段直读改为任务类只读 / 解析 helper。
3. 不改变任务筹办生命周期语义，不新增本能方法，不新增任务状态推进入口。
```

预测：

```text
本切片不需要新增 / 删除 / 改名函数，不运行函数说明文档生成器。
`任务模块.筹办.ixx` 任务主信息直读复扫应为无命中。
Debug x64 构建应通过。
```

## 4. P0b 任务筹办模块任务主信息直读回迁结果

实际修改范围：

```text
1. `任务来源需求` 改为经 `任务类::读取任务对应需求引用` 读取结构化引用，再保留原有主键解析路径。
2. 任务场景读取改为经 `任务类::解析任务场景`。
3. 任务虚拟存在读取改为经 `任务类::解析任务虚拟存在` 或 `任务类::取或创建_任务虚拟存在` 搭配任务场景 helper。
4. 未改变任务筹办回执、候选集填充、任务状态提交或动作动态来源。
```

验证证据：

```text
复扫：
    日志/20260621_T0b_任务筹办直读复扫.txt
    结果：无命中。

全局外部任务主信息直读复扫：
    日志/20260621_T0b_任务主信息外部直读复扫.txt
    结果：剩余 72 行。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> '日志\20260621_T0b_任务筹办任务主信息回迁_build.log'
    结果：已成功生成；0 个错误。

差异检查：
    git diff --check -- 任务模块.筹办.ixx 实施记录/20260621_任务类逻辑纠偏_Codex断点清单.md
    结果：无空白错误。
```

当前判断：

```text
P0b 闭合；不宣称任务类逻辑纠偏完成。
下一步继续 P0，小切片优先级：
    1. `任务模块.执行.ixx`：2 行，适合先闭合。
    2. `自我动作实现.服务模块.ixx`：2 行，适合后续闭合。
    3. `任务模块.管理界面线程.impl.cpp`：31 行，含目标状态写入边界，单独作为较大切片。
```

## 5. P0c 任务执行模块任务主信息直读回迁预测

计划修改范围：

```text
1. 只修改 `任务模块.执行.ixx`。
2. 将任务虚拟存在和任务场景的直接读取改为任务类解析 helper。
3. 不改变执行桥的结果验证、动作验证报告、任务状态建议或提交边界。
```

预测：

```text
本切片不新增 / 删除 / 改名函数，不运行函数说明文档生成器。
`任务模块.执行.ixx` 任务主信息直读复扫应为无命中。
Debug x64 构建应通过。
```

## 6. P0c 任务执行模块任务主信息直读回迁结果

实际修改范围：

```text
1. `私有_读取任务可执行输入参数场景_执行桥_最小_v0` 改为经 `任务类::解析任务虚拟存在` 读取任务虚拟存在。
2. 执行桥当前场景读取改为经 `任务类::解析任务场景`。
3. 未改变执行桥的任务状态建议、执行结果回执、动作验证报告或正式提交边界。
```

验证证据：

```text
复扫：
    日志/20260621_T0c_任务执行直读复扫.txt
    结果：无命中。

全局外部任务主信息直读复扫：
    日志/20260621_T0c_任务主信息外部直读复扫.txt
    结果：剩余 70 行。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> '日志\20260621_T0c_任务执行任务主信息回迁_build.log'
    结果：已成功生成；0 个错误。

差异检查：
    git diff --check -- 任务模块.执行.ixx 实施记录/20260621_任务类逻辑纠偏_Codex断点清单.md
    结果：无空白错误。
```

当前判断：

```text
P0c 闭合；不宣称任务类逻辑纠偏完成。
下一步继续 P0，优先处理 `自我动作实现.服务模块.ixx` 的 2 行小字段簇。
```

## 7. P0d 服务模块任务主信息直读回迁预测

计划修改范围：

```text
1. 只修改 `自我动作实现.服务模块.ixx`。
2. 将服务输入任务的来源需求和任务虚拟存在读取改为任务类解析 helper。
3. 不改变服务动作实现、任务字段特征写入或服务结算逻辑。
```

预测：

```text
本切片不新增 / 删除 / 改名函数，不运行函数说明文档生成器。
`自我动作实现.服务模块.ixx` 任务主信息直读复扫应为无命中。
Debug x64 构建应通过。
```

## 8. P0d 服务模块任务主信息直读回迁结果

实际修改范围：

```text
1. `读取任务来源需求` 改为经 `任务类::解析任务对应需求` 获取来源需求。
2. `读取任务虚拟存在` 改为经 `任务类::解析任务虚拟存在` 获取任务虚拟存在。
3. 未改变服务字段特征写入、服务动作实现或结算逻辑。
```

验证证据：

```text
复扫：
    日志/20260621_T0d_服务模块直读复扫.txt
    结果：无命中。

全局外部任务主信息直读复扫：
    日志/20260621_T0d_任务主信息外部直读复扫.txt
    结果：剩余 68 行。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> '日志\20260621_T0d_服务模块任务主信息回迁_build.log'
    结果：已成功生成；0 个错误。

差异检查：
    git diff --check -- 自我动作实现.服务模块.ixx 实施记录/20260621_任务类逻辑纠偏_Codex断点清单.md
    结果：无空白错误。
```

当前判断：

```text
P0d 闭合；不宣称任务类逻辑纠偏完成。
下一步继续 P0：`任务模块.管理界面线程.impl.cpp` 命中最多且含目标状态写入，先拆出前置只读 helper 小簇，再单独处理写入边界。
```

## 9. P0e 管理界面线程前置只读 helper 回迁预测

计划修改范围：

```text
1. 只修改 `任务模块.管理界面线程.impl.cpp`。
2. 仅回迁前置只读 helper：
   `私有_推导承载场景`；
   `私有_任务虚拟存在_只读`；
   `私有_解析任务来源需求`；
   `私有_解析任务虚拟存在`。
3. 对来源需求 / 任务虚拟存在保留原有指针优先、主键回查语义。
4. 不处理 `目标状态` 写入，不改变任务状态提交、等待缺口唤醒或管理线程协议。
```

预测：

```text
本切片不新增 / 删除 / 改名函数，不运行函数说明文档生成器。
`任务模块.管理界面线程.impl.cpp` 的前置 helper 命中应消失，但该文件仍会保留其它任务主信息直读点，后续按写入边界和调用簇继续拆分。
Debug x64 构建应通过。
```

## 10. P0e 管理界面线程前置只读 helper 回迁结果

实际修改范围：

```text
1. `私有_推导承载场景` 改为经 `任务类::解析任务场景` 读取任务场景。
2. `私有_任务虚拟存在_只读` 改为经 `任务类::解析任务虚拟存在` 读取任务虚拟存在。
3. `私有_解析任务来源需求` 改为经 `任务类::读取任务对应需求引用` 读取结构化引用，并保留主键回查。
4. `私有_解析任务虚拟存在` 改为经 `任务类::读取任务虚拟存在引用` 读取结构化引用，并保留主键回查。
5. 未处理目标状态写入、等待缺口唤醒和任务状态提交边界。
```

验证证据：

```text
复扫：
    日志/20260621_T0e_管理界面前置helper复扫.txt
    结果：管理界面线程剩余 20 行；前置 helper 命中已消失。

全局外部任务主信息直读复扫：
    日志/20260621_T0e_任务主信息外部直读复扫.txt
    结果：剩余 57 行。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> '日志\20260621_T0e_管理界面前置helper回迁_build.log'
    结果：已成功生成；0 个错误。

差异检查：
    git diff --check -- 任务模块.管理界面线程.impl.cpp 实施记录/20260621_任务类逻辑纠偏_Codex断点清单.md
    结果：无空白错误。
```

当前判断：

```text
P0e 闭合；不宣称任务类逻辑纠偏完成。
下一步继续 P0：拆分 `任务模块.管理界面线程.impl.cpp` 剩余 20 行，优先处理场景读取调用簇；目标状态写入边界单独评估。
```

## 11. P0f 管理界面线程场景读取调用簇回迁预测

计划修改范围：

```text
1. 只修改 `任务模块.管理界面线程.impl.cpp`。
2. 将任务场景直接读取改为 `任务类::解析任务场景` 或复用当前函数内已解析的场景变量。
3. 不处理 `任务->主信息.目标状态` 读写，不处理任务虚拟存在指针比较。
4. 不改变任务状态提交、等待缺口唤醒、权重固化或价值结算语义。
```

预测：

```text
本切片不新增 / 删除 / 改名函数，不运行函数说明文档生成器。
`任务模块.管理界面线程.impl.cpp` 剩余命中应只剩目标状态读写和任务虚拟存在指针比较。
Debug x64 构建应通过。
```

## 12. P0f 管理界面线程场景读取调用簇回迁结果

实际修改范围：

```text
1. 任务观察报告 ID 写入、任务运行壳准备、任务创建初始化、工作结果应用、调度请求、目标升级承载场景、状态推进结果、父任务通知、任务价值结算、权重固化等路径的任务场景读取，改为 `任务类::解析任务场景` 或复用已解析场景变量。
2. 未处理 `任务->主信息.目标状态` 读写。
3. 未处理任务虚拟存在指针比较。
```

验证证据：

```text
复扫：
    日志/20260621_T0f_管理界面场景读取复扫.txt
    结果：管理界面线程剩余 4 行：目标状态读写 3 行、任务虚拟存在指针比较 1 行。

全局外部任务主信息直读复扫：
    日志/20260621_T0f_任务主信息外部直读复扫.txt
    结果：剩余 41 行。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> '日志\20260621_T0f_管理界面场景读取回迁_build.log'
    结果：已成功生成；0 个错误。

差异检查：
    git diff --check -- 任务模块.管理界面线程.impl.cpp 实施记录/20260621_任务类逻辑纠偏_Codex断点清单.md
    结果：无空白错误。
```

当前判断：

```text
P0f 闭合；不宣称任务类逻辑纠偏完成。
下一步继续 P0，单独评估 `任务模块.管理界面线程.impl.cpp` 的目标状态读写和任务虚拟存在指针比较；若需要新增任务类写入 helper，先查函数说明文档。
```

## 13. P0g 管理界面线程目标状态写入与任务虚拟存在比较回迁预测

函数查重：

```text
说明书/函数功能说明文档.md 与 `任务类.h/.cpp` 已有：
    任务类::读取任务目标状态引用
    任务类::解析任务目标状态
    任务类::读取任务虚拟存在引用
    任务类::解析任务虚拟存在

未发现任务目标状态写入 helper。
```

计划修改范围：

```text
1. 在任务类新增最小 `写入任务目标状态` helper，用于写入任务目标状态引用。
2. 将 `任务模块.管理界面线程.impl.cpp` 中目标状态读写改为任务类读取 / 写入 helper。
3. 将任务虚拟存在指针比较改为读取任务类结构化引用。
4. 运行 `python .\tools\生成函数功能说明文档.py` 更新函数说明文档。
5. 不改变目标升级、任务创建或虚拟存在创建的业务语义。
```

预测：

```text
`任务模块.管理界面线程.impl.cpp` 任务主信息直读复扫应为无命中。
全局外部任务主信息直读剩余应集中到其它文件。
Debug x64 构建应通过。
```

## 14. P0g 管理界面线程目标状态写入与任务虚拟存在比较回迁结果

实际修改范围：

```text
1. 新增 `任务类::写入任务目标状态`，由任务类统一写入任务目标状态引用。
2. `私有_准备任务运行壳` 和同目标任务目标升级路径改为调用任务类目标状态读取 / 写入 helper。
3. 任务虚拟存在指针比较改为读取 `任务类::读取任务虚拟存在引用` 的结构化引用。
4. 更新 `说明书/函数功能说明文档.md`。
```

验证证据：

```text
函数说明文档：
    python .\tools\生成函数功能说明文档.py
    结果：函数条目 7188；包含 `任务类::写入任务目标状态`。

复扫：
    日志/20260621_T0g_管理界面目标状态复扫.txt
    结果：无命中。

全局外部任务主信息直读复扫：
    日志/20260621_T0g_任务主信息外部直读复扫.txt
    结果：剩余 37 行。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> '日志\20260621_T0g_管理界面目标状态回迁_build.log'
    结果：已成功生成；0 个错误。

差异检查：
    git diff --check -- 任务类.h 任务类.cpp 任务模块.管理界面线程.impl.cpp 说明书/函数功能说明文档.md 实施记录/20260621_任务类逻辑纠偏_Codex断点清单.md
    结果：无空白错误。
```

当前判断：

```text
P0g 闭合；`任务模块.管理界面线程.impl.cpp` 外部任务主信息直读已清零；不宣称任务类逻辑纠偏完成。
下一步继续 P0：处理剩余全局外部任务主信息直读，优先小簇或显示 / 控制命令分层。
```

## 15. P0h 自我动作实现模块任务主信息读取回迁预测

计划修改范围：

```text
1. 只修改 `自我动作实现模块.ixx`。
2. 将任务场景、目标状态、实际结果状态、来源需求、任务虚拟存在指针比较改为任务类读取 / 解析 helper。
3. 不改变任务状态被动提交、动作动态创建、结算被动因判定或任务推进特征写入语义。
```

预测：

```text
本切片不新增 / 删除 / 改名函数，不运行函数说明文档生成器。
`自我动作实现模块.ixx` 任务主信息直读复扫应为无命中。
Debug x64 构建应通过。
```

## 16. P0h 自我动作实现模块任务主信息读取回迁结果

实际修改范围：

```text
1. 任务状态被动提交动作动态创建使用 `任务类::解析任务场景`。
2. 任务完成前检查和推进特征同步使用 `任务类::解析任务实际结果状态` / `任务类::解析任务目标状态`。
3. 任务提交前一结果状态使用 `任务类::解析任务对应需求`。
4. 任务推进特征写入复用已解析任务场景。
5. 任务结算被动因匹配使用 `任务类::读取任务虚拟存在引用`。
```

验证证据：

```text
复扫：
    日志/20260621_T0h_自我动作实现模块直读复扫.txt
    结果：无命中。

全局外部任务主信息直读复扫：
    日志/20260621_T0h_任务主信息外部直读复扫.txt
    结果：剩余 23 行。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> '日志\20260621_T0h_自我动作实现模块回迁_build.log'
    结果：已成功生成；0 个错误。

差异检查：
    git diff --check -- 自我动作实现模块.ixx 实施记录/20260621_任务类逻辑纠偏_Codex断点清单.md
    结果：无空白错误。
```

当前判断：

```text
P0h 闭合；不宣称任务类逻辑纠偏完成。
下一步继续 P0：处理 `自我线程模块.impl.cpp` 的 10 行任务主信息直读；显示 / 控制命令项后置分层。
```

## 17. P0i 自我线程模块任务主信息读取回迁预测

计划修改范围：

```text
1. 只修改 `自我线程模块.impl.cpp`。
2. 将任务虚拟存在、来源需求引用、任务场景读取改为任务类 helper。
3. 保留自我线程原有需求树归属校验和任务状态动作动态读取逻辑。
4. 不改变自我线程调度、缺口承接、快照展示或任务治理消息语义。
```

预测：

```text
本切片不新增 / 删除 / 改名函数，不运行函数说明文档生成器。
`自我线程模块.impl.cpp` 任务主信息直读复扫应为无命中。
Debug x64 构建应通过。
```

## 18. P0i 自我线程模块任务主信息读取回迁结果

实际修改范围：

```text
1. 任务状态动作动态读取、任务虚拟存在基础节点读取、任务管理新链任务虚拟存在读取改为 `任务类::解析任务虚拟存在`。
2. 任务来源需求读取改为经 `任务类::读取任务对应需求引用` 后继续走自我线程原有需求树归属校验。
3. 任务虚拟存在输入参数、等待缺口唤醒读取改为经 `任务类::解析任务场景` 获取场景。
4. 快照中当前任务虚拟存在主键改为读取任务类结构化引用。
```

验证证据：

```text
复扫：
    日志/20260621_T0i_自我线程模块直读复扫.txt
    结果：无命中。

全局外部任务主信息直读复扫：
    日志/20260621_T0i_任务主信息外部直读复扫.txt
    结果：剩余 13 行。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> '日志\20260621_T0i_自我线程模块回迁_build.log'
    结果：已成功生成；0 个错误。

差异检查：
    git diff --check -- 自我线程模块.impl.cpp 实施记录/20260621_任务类逻辑纠偏_Codex断点清单.md
    结果：无空白错误。
```

当前判断：

```text
P0i 闭合；不宣称任务类逻辑纠偏完成。
下一步继续 P0：处理 `鱼巢.cpp` 控制命令项、`控制面板摘要线程模块.impl.cpp` 摘要显示项、`自检线程模块.impl.cpp` 自检引用判断项；显示层不得反向进入机器判断。
```

## 19. P0j 控制命令 / 摘要 / 自检剩余任务主信息读取回迁预测

计划修改范围：

```text
1. 修改 `鱼巢.cpp` 控制命令显示辅助函数，改为任务类 helper 读取任务虚拟存在、对应需求和目标状态。
2. 修改 `控制面板摘要线程模块.impl.cpp` 摘要时间读取，改为任务类启动 / 完成时间 helper。
3. 修改 `自检线程模块.impl.cpp` 安全根任务来源判断，改为任务类对应需求引用 helper。
4. 不改变控制命令输出含义、控制面板摘要口径或自检判定语义。
```

预测：

```text
本切片不新增 / 删除 / 改名函数，不运行函数说明文档生成器。
全局外部任务主信息直读复扫应为无命中。
Debug x64 构建应通过。
```

## 20. P0j 控制命令 / 摘要 / 自检剩余任务主信息读取回迁结果

实际修改范围：

```text
1. `鱼巢.cpp` 控制命令辅助函数改为经任务类 helper 读取任务虚拟存在引用、来源需求、目标状态和完成时间。
2. `控制面板摘要线程模块.impl.cpp` 摘要时间读取改为经 `任务类::读取任务启动时间` / `任务类::读取任务完成时间`。
3. `自检线程模块.impl.cpp` 安全根任务来源判断改为经 `任务类::读取任务对应需求引用`。
4. 未改变控制命令输出、控制面板摘要或自检判定语义。
```

验证证据：

```text
全局外部任务主信息直读复扫：
    日志/20260621_T0j_任务主信息外部直读复扫.txt
    结果：无命中。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> '日志\20260621_T0j_剩余控制显示自检回迁_build.log'
    结果：已成功生成；0 个错误。

差异检查：
    git diff --check -- 鱼巢.cpp 控制面板摘要线程模块.impl.cpp 自检线程模块.impl.cpp 实施记录/20260621_任务类逻辑纠偏_Codex断点清单.md
    结果：无空白错误。
```

当前判断：

```text
P0j 闭合；外部任务主信息直读复扫已无命中；不宣称任务类逻辑纠偏完成。
下一步继续 P0：复扫任务类自身跨类直读和任务模块协议结构，决定是否进入 P1 字段必要性判定或转入 owning-class 缺口。
```

## 21. P0k 任务类自身跨类直读与协议结构复扫结果

复扫证据：

```text
任务类自身跨类直读广义复扫：
    日志/20260621_T0k_任务类自身跨类直读复扫.txt
    结果：47 行。
    判读：主要为任务类自身主信息读写、SQL 投影、任务虚拟存在和世界树取特征主信息，不等同于任务类读取其它类主信息。

任务类自身非任务主信息窄扫：
    rg -n "来源需求->主信息\.|需求->主信息\.|状态节点->主信息\.|方法->主信息\.|存在->主信息\.|场景->主信息\." 任务类.cpp 任务类.h
    结果：无命中。

任务模块协议结构复扫：
    日志/20260621_T0k_任务结构协议复扫.txt
    结果：1021 行。
    判读：属于任务模块协议 / 回执 / 工作包结构审查范围，不按任务主信息外部直读处理。
```

当前判断：

```text
P0 外部任务主信息直读回迁闭合；任务类自身直接读取非任务主信息未复现；协议结构审查后移到 P6。
下一步进入 P1：任务主信息字段必要性判定表。
```

## 22. P1 任务主信息字段必要性判定表结果

产物：

```text
实施记录/20260621_P1_任务主信息字段必要性判定表.md
```

结论摘要：

```text
1. `名称 / 类型 / 创建时间 / 启动时间 / 完成时间` 不作为任务状态或任务完成判断权威；保留为显示、审计、结构归类入口。
2. `对应需求 / 任务虚拟存在 / 场景 / 目标状态 / 实际结果状态` 是任务结构闭环所需结构化引用，应保留。
3. 外部任务主信息直读已清零；后续外部读取必须经任务类 helper。
4. 协议结构是否存在旧镜像或文本原因字段，转入 P6 单独审查。
```

当前判断：

```text
P1 闭合；不宣称任务类逻辑纠偏完成。
下一步进入 P2：任务类现有函数覆盖表。
```

## 23. P2 任务类现有函数覆盖表结果

产物：

```text
实施记录/20260621_P2_任务类现有函数覆盖表.md
```

结论摘要：

```text
1. 当前任务主信息字段均已有满足外部读取 / 写入收束所需的任务类函数，暂不需要新增函数。
2. `名称 / 类型` 不新增通用外部读取 helper，避免显示字段反向进入机器判断；后续显示读取走任务类快照 / SQL 投影专用路径。
3. P2 闭合后可进入 P3：任务类自身跨类读取回迁复核。
```

当前判断：

```text
P2 闭合；不宣称任务类逻辑纠偏完成。
下一步进入 P3：任务类自身跨类读取回迁复核。
```

## 24. P3 任务类自身跨类读取复核结果

产物：

```text
实施记录/20260621_P3_任务类自身跨类读取复核表.md
```

结论摘要：

```text
1. 未发现必须临时提升其它 owning-class 计划的阻塞项。
2. 需求主信息、状态主信息、存在 / 场景主信息直读均未复现。
3. 特征和世界树访问保留在任务类封装任务虚拟存在特征的范围内。
4. 后续如 P4/P5/P6 暴露具体跨类缺函数，再按“缺哪个类补哪个类第一优先”单独登记。
```

当前判断：

```text
P3 闭合；不宣称任务类逻辑纠偏完成。
下一步进入 P4：任务状态写入入口审查。
```

## 25. P4 任务状态写入入口审查预测

计划修改范围：

```text
1. 扫描 `写入任务状态 / 应用任务状态 / 标记任务* / 确保任务初始状态` 调用点。
2. 删除当前无外部调用、且会绕过任务状态被动提交动作链的公开标记 / 应用函数。
3. 保留任务创建初始化和任务状态被动提交动作内部的 `任务类::写入任务状态` 低层写入。
4. 更新 `说明书/函数功能说明文档.md`。
```

预测：

```text
删除后 `标记任务待重筹办 / 标记任务等待 / 标记任务已承接 / 标记任务已派发 / 标记任务负载等待 / 应用任务状态` 源码扫描应无命中。
`任务类::写入任务状态` 外部调用应只剩任务创建初始化和任务状态被动提交动作内部写入。
Debug x64 构建应通过。
```

## 26. P4 任务状态写入入口审查结果

实际修改范围：

```text
1. 删除 `任务类::标记任务待重筹办 / 标记任务等待 / 标记任务已承接 / 标记任务已派发 / 标记任务负载等待 / 应用任务状态` 声明和实现。
2. 更新 `说明书/函数功能说明文档.md`。
3. 新增 `实施记录/20260621_P4_任务状态写入口审查表.md`。
```

验证证据：

```text
函数说明文档生成：
    python .\tools\生成函数功能说明文档.py
    结果：函数条目 7182。

旧公开入口源码复扫：
    日志/20260621_P4_任务状态旧公开入口复扫.txt
    结果：无命中。

函数说明旧入口复扫：
    日志/20260621_P4_函数说明旧入口复扫.txt
    结果：无命中。

写入任务状态复扫：
    日志/20260621_P4_写入任务状态复扫.txt
    结果：5 行，仅剩声明、定义、初始化内部调用和被动提交动作内部调用。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> '日志\20260621_P4_任务状态写入口收口_build.log'
    结果：BUILD_OK。
```

当前判断：

```text
P4 闭合；不宣称任务类逻辑纠偏完成。
下一步进入 P5：任务筹办 / 执行回执结构审查。
```

## 27. P5 回执文本字段机器分支问题记录

问题锚点：

```text
`自我线程模块.impl.cpp` 读取任务管理线程补充特征 `当前等待原因 / 控制拒绝原因`。
其中 `控制拒绝原因.empty()` 被用于计算 `控制请求被拒绝`，属于文本字段进入机器布尔判断。
```

证据：

```text
日志/20260621_P5_文本回执传播复扫.txt：
    自我线程模块.impl.cpp:16432 读取 当前等待原因 文本。
    自我线程模块.impl.cpp:16438 读取 控制拒绝原因 文本。

源码小段复核：
    `控制请求被拒绝 = 控制响应状态值 == 已拒绝 || !控制拒绝原因.empty();`
```

已排除路径：

```text
`当前等待原因` 当前只进入原因说明字符串和展示性二次特征原因文本；未发现用于决定 `依赖满足 / 当前步骤可执行 / 是否推进`。
```

当前判断：

```text
`控制拒绝原因` 只能作为说明文本；机器判断必须只读取结构化 `当前控制响应状态` 数值 / 枚举值。
```

下一步验证：

```text
最小修改 `自我线程模块.impl.cpp`，删除 `!控制拒绝原因.empty()` 对 `控制请求被拒绝` 的参与。
复扫该条件和 Debug x64 构建。
```

## 28. P5 任务回执结构审查结果

实际修改范围：

```text
1. `自我线程模块.impl.cpp` 中 `控制请求被拒绝` 改为只读取结构化 `当前控制响应状态 == 已拒绝`。
2. 新增 `实施记录/20260621_P5_任务回执结构审查表.md`。
3. 更新计划和计划索引，P5 闭合后进入 P6。
```

验证证据：

```text
控制拒绝原因文本分支复扫：
    日志/20260621_P5_控制拒绝原因文本分支复扫.txt
    结果：`控制拒绝原因.empty()` 已无命中。

文本字段机器分支复扫：
    日志/20260621_P5_文本字段机器分支复扫.txt
    结果：剩余命中主要为日志拼接、说明字段填充、展示性空值判断或结构化特征入口存在性判断。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> '日志\20260621_P5_回执文本分支收口_build.log'
    结果：BUILD_OK。
```

当前判断：

```text
P5 闭合；不宣称任务类逻辑纠偏完成。
下一步进入 P6：任务模块协议结构审查。
```

## 29. P6 任务模块协议结构审查预测

计划修改范围：

```text
1. 扫描 `任务模块.*` 中 `结构_任务管理* / 枚举_任务管理* / 结构_任务工作* / 枚举_任务工作* / 结构_任务推进* / 枚举_任务推进*`。
2. 生成协议结构判定表：治理协议 / 工作包 / 上行消息 / 运行快照 / 显示摘要 / 旧镜像 / 可删除项。
3. 本步先生成判定表；若发现文本字段参与机器判断或旧镜像结构无调用，再进入单结构最小代码切片。
```

预测：

```text
协议结构数量较多，初步应大多属于线程间治理协议、工作包、上行消息和运行快照。
本轮不一刀切删除协议结构；若无明确可删项，只产出审查表并进入 P7。
```

## 30. P6 任务模块协议结构审查结果

产物：

```text
实施记录/20260621_P6_任务模块协议结构审查表.md
```

验证证据：

```text
任务协议结构清单：
    日志/20260621_P6_任务协议结构清单.txt
    结果：68 行。

任务协议引用总扫：
    日志/20260621_P6_任务协议引用总扫.txt
    结果：707 行。

协议文本字段清单：
    日志/20260621_P6_协议文本字段清单.txt
    结果：181 行。

任务模块安全获取词复扫：
    日志/20260621_P6_任务模块安全获取词复扫.txt
    结果：任务模块安全获取词无命中。

协议文本字段机器比较复扫：
    日志/20260621_P6_协议文本字段机器比较复扫.txt
    结果：未发现任务协议文本字段机器比较；唯一命中为 `自我线程模块.impl.cpp:1177` 的 `摘要.缺失证据` 容器查重，非任务模块协议字段。
```

当前判断：

```text
P6 闭合；未发现可直接删除的任务模块协议结构；不宣称任务类逻辑纠偏完成。
下一步进入 P7：控制面板 / SQL / 日志后置审查。
```

## 31. P7 控制面板 / SQL / 日志后置审查预测

计划修改范围：

```text
1. 复扫任务类 SQL 投影、控制面板摘要、任务管理界面文本输出和日志显示路径。
2. 确认 `语素集.安全获取词(...)` 在任务相关路径只用于显示 / SQL 投影 / 日志，不进入机器查找、候选生成、需求生成、方法选择或排序。
3. 若发现显示文本参与机器判断，按最小代码切片改为结构化特征 / 状态 / 枚举。
```

预测：

```text
P0a-P0j 已回迁外部任务主信息直读，P7 预计主要产出后置判定表。
若没有发现显示文本反向进入机器判断，P7 文档闭合后任务类逻辑纠偏计划可收束为“当前计划切片闭合”，但不宣称项目结构闭环全部通过。
```

## 32. P7 控制面板 / SQL / 日志后置审查结果

产物：

```text
实施记录/20260621_P7_任务显示SQL日志后置审查表.md
```

验证证据：

```text
任务显示路径安全获取词复扫：
    日志/20260621_P7_任务显示路径安全获取词复扫.txt
    结果：2 行，均为 SQL / 控制面板显示。

任务显示 SQL 日志路径复扫：
    日志/20260621_P7_任务显示SQL日志路径复扫.txt
    结果：1711 行，均落入显示 / SQL / 日志路径，需要按判定表分层读取。

显示文本机器分支复扫：
    日志/20260621_P7_显示文本机器分支复扫.txt
    结果：87 行；未发现任务显示文本参与机器查找、候选生成、需求生成、方法选择或排序。

外部任务主信息直读复扫：
    日志/20260621_P7_外部任务主信息直读复扫.txt
    结果：外部任务主信息直读无命中。
```

当前判断：

```text
P7 闭合；任务类逻辑纠偏计划当前可执行切片已收束。
不宣称任务类逻辑纠偏完成，不宣称项目结构闭环全部通过。
下一步按计划索引进入方法类逻辑纠偏计划。
```
