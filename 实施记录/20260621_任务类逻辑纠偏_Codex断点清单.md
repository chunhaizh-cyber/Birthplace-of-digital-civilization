# 20260621 任务类逻辑纠偏 Codex 断点清单

## 0. 恢复摘要

当前切片：

```text
计划/20260620_任务类逻辑纠偏计划_v0.1.md 的 P0：任务主信息外部直读 / 写入扫描、分层和最小 helper 缺口登记。
```

依据文件：

```text
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_任务类逻辑纠偏计划_v0.1.md
规范/任务筹办与执行边界规范20260512.md
规范/自我线程与任务管理线程权责边界规范20260512.md
```

硬边界：

```text
1. 任务筹办 / 任务执行仍是任务管理生命周期普通函数，不登记为本能方法，不进入候选方法集，不作为动作动态来源。
2. 自我线程不得直接裸写任务状态；任务状态推进由任务管理线程编排提交。
3. 机器查找 / 生成逻辑不得读取 `语素集.安全获取词(...)` 得到的文本；必须用语素入口身份、具体特征类型、方向 / 枚举值、状态 / 特征节点和结构化值。
4. 本切片先扫描分类，不直接改任务生命周期语义。
```

## 1. P0 扫描分类构建前预测

计划修改范围：

```text
1. 生成任务主信息外部直读扫描清单。
2. 生成任务模块结构扫描清单。
3. 生成任务类自身跨类直读扫描清单。
4. 形成分类表：机器逻辑 / 任务管理提交链内部 / 显示日志 SQL / 已有任务类函数可复用 / 缺任务类函数 / 跨类缺口。
```

预测：

```text
本切片只新增实施记录和日志，不新增函数，不需要重新生成函数说明文档，不需要运行 Debug x64 构建。
若扫描发现明确机器逻辑直读且已有任务类函数可复用，下一小切片再做代码回迁。
```

## 2. P0a 任务管理工作线程任务主信息直读回迁结果

实际修改范围：

```text
1. 新增任务类只读 / 解析 helper：
   读取任务对应需求引用 / 解析任务对应需求；
   读取任务虚拟存在引用 / 解析任务虚拟存在；
   读取任务场景引用 / 解析任务场景；
   读取任务目标状态引用 / 解析任务目标状态；
   读取任务创建时间 / 读取任务启动时间 / 读取任务完成时间。
2. 回迁 `任务模块.管理工作线程.ixx` 内直接读取任务主信息的调用点。
3. 更新 `说明书/函数功能说明文档.md` 和 `计划/20260620_任务类逻辑纠偏计划_v0.1.md` 的 P0a 证据。
```

验证证据：

```text
函数说明文档生成：
    python .\tools\生成函数功能说明文档.py
    结果：函数条目 7186。

复扫：
    日志/20260621_T0a_任务管理工作线程直读复扫.txt
    结果：无命中。

全局外部任务主信息直读复扫：
    日志/20260621_T0a_任务主信息外部直读复扫.txt
    结果：剩余 89 行；下一候选集中在 `任务模块.筹办.ixx` 与 `任务模块.管理界面线程.impl.cpp`。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> '日志\20260621_T0a_任务管理工作线程任务主信息回迁_build.log'
    结果：已成功生成；0 个错误。

差异检查：
    git diff --check -- 任务类.h 任务类.cpp 任务模块.管理工作线程.ixx 说明书/函数功能说明文档.md 计划/20260620_任务类逻辑纠偏计划_v0.1.md
    结果：无空白错误。
```

当前判断：

```text
P0a 闭合；不宣称任务类逻辑纠偏完成。
下一步继续 P0，优先选择 `任务模块.筹办.ixx` 的单字段簇，避免直接进入 `任务模块.管理界面线程.impl.cpp` 的写入边界大切片。
```

## 3. P0b 任务筹办模块任务主信息直读回迁预测

计划修改范围：

```text
1. 只修改 `任务模块.筹办.ixx`。
2. 将 `对应需求 / 场景 / 任务虚拟存在` 三类任务主信息字段直读改为任务类只读 / 解析 helper。
3. 不改变任务筹办生命周期语义，不新增本能方法，不新增任务状态推进入口。
```

预测：

```text
本切片不需要新增 / 删除 / 改名函数，不运行函数说明文档生成器。
`任务模块.筹办.ixx` 任务主信息直读复扫应为无命中。
Debug x64 构建应通过。
```

## 4. P0b 任务筹办模块任务主信息直读回迁结果

实际修改范围：

```text
1. `任务来源需求` 改为经 `任务类::读取任务对应需求引用` 读取结构化引用，再保留原有主键解析路径。
2. 任务场景读取改为经 `任务类::解析任务场景`。
3. 任务虚拟存在读取改为经 `任务类::解析任务虚拟存在` 或 `任务类::取或创建_任务虚拟存在` 搭配任务场景 helper。
4. 未改变任务筹办回执、候选集填充、任务状态提交或动作动态来源。
```

验证证据：

```text
复扫：
    日志/20260621_T0b_任务筹办直读复扫.txt
    结果：无命中。

全局外部任务主信息直读复扫：
    日志/20260621_T0b_任务主信息外部直读复扫.txt
    结果：剩余 72 行。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> '日志\20260621_T0b_任务筹办任务主信息回迁_build.log'
    结果：已成功生成；0 个错误。

差异检查：
    git diff --check -- 任务模块.筹办.ixx 实施记录/20260621_任务类逻辑纠偏_Codex断点清单.md
    结果：无空白错误。
```

当前判断：

```text
P0b 闭合；不宣称任务类逻辑纠偏完成。
下一步继续 P0，小切片优先级：
    1. `任务模块.执行.ixx`：2 行，适合先闭合。
    2. `自我动作实现.服务模块.ixx`：2 行，适合后续闭合。
    3. `任务模块.管理界面线程.impl.cpp`：31 行，含目标状态写入边界，单独作为较大切片。
```
