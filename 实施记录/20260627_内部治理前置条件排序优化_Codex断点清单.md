# 20260627 内部治理前置条件排序优化 Codex断点清单

## 0. 恢复摘要

当前切片：

```text
计划口径优化。
根据云端 birthplace/main@bf6503fa78ea665677d1e90cd1be12b2a6a6b52a 后续分析，
修正 P1c / P0.5b 的前置条件排序。
本轮不改 C++ 业务代码。
```

本轮结论：

```text
当前不是继续扫描业务闭合、P4 真实扫描验证、D455 手工运行或外部动作提取。
当前主动切片仍是：
    P1c 因果链驱动扫描需求自然生长计划 v0.1
    / P0.5b 动作动态入口与真实状态端点核查。

扫描需求自然生长不是第一层目标，而是内部治理练习链之后的结果。
当前缺口不是扫描按钮，而是内部治理动作的真实前后状态端点。
```

下一步：

```text
继续 P0.5b：
1. 查 自我_练习 的结果写入是创建状态节点，还是仅写特征值。
2. 查方法域提交是否能形成真实状态端点。
3. 查任务执行桥创建方法调用动作动态时是否拿到真实初始状态和结果状态。
4. 查本能方法输出结果是否有可供动作致变动态引用的实际状态端点。
5. 若没有端点，登记任务执行桥 / 本能动作输出结果状态端点补齐缺口。
```

禁止宣称：

```text
不宣称扫描需求自然生长闭合。
不宣称内部治理练习链闭合。
不宣称 D455 当前可验证业务闭合。
不宣称外部动作提取链可恢复。
```

## 1. 前置条件排序

```text
1. 内部治理动作能产生真实状态端点。
2. 状态迁移动态证明“什么存在的什么状态真的变了”。
3. 动作致变动态证明“哪个本能方法 / 治理函数造成了变化”。
4. 两类动态按同主体、同特征、双状态时间段包含关系完成同源可复算。
5. 动态沉淀为内部治理因果。
6. 方法缺口自然生成练习需求。
7. 自我_练习 真实调用目标方法，并由目标方法留下运行账和可追踪动态证据。
8. 任务筹办只消费因果类 / 世界树查询结果，生成派生需求草案。
9. 需求树自然生长到外设观察 / D455 工作需求。
10. D455 正常工作并提供真实外部数据后，才恢复扫描真实验证或外部动作提取。
```

## 2. 执行闸门

```text
A. 路由闸门：
    必须执行 P0.5b，不转入外部动作提取、P4 扫描验证或 D455 手工运行。

B. 状态端点闸门：
    关键结果写入必须创建状态节点，并形成带前态、后态、主体、特征和场域的状态迁移动态。

C. 动作端点闸门：
    本能方法 / 治理函数动作动态若要进入动作致变因果，必须带真实初始状态和结果状态。

D. 互补绑定闸门：
    先用既有同源可复算能力闭合，不新增同源变化证据组结构。

E. 内部治理练习闸门：
    方法缺口必须自然生成练习需求；自我_练习 必须真实调用目标方法并留下可追踪动态证据。

F. 因果消费闸门：
    任务筹办只能消费因果类 / 世界树查询结果，不得把需求当前 / 目标状态反向当作因果来源。

G. 扫描后置闸门：
    扫描可启动、6146、D455 和外部动作提取均在内部治理链闭合后再恢复。
```

## 3. 已优化文件

```text
计划/计划索引.md
    增加当前执行闸门 A-G，并增加本断点记录入口。

计划/待完成项/20260626_因果链驱动扫描需求自然生长计划_v0.1.md
    增加 1.2 前置条件重排。
    优化 P0.5b-1 / P0.5b-2 / P0.5b-3 的检查重点。
    明确 P2 扫描需求因果模板补齐是后置闸门，不是当前 P0.5b 修补对象。
```

## 4. 验证

```text
本轮为计划和实施记录文档优化，未修改 C++ 业务代码，未运行 Debug x64 构建。
已执行 git diff --check，未发现空白格式错误。
已核对计划索引和 P1c 计划正文中的闸门入口。
```
