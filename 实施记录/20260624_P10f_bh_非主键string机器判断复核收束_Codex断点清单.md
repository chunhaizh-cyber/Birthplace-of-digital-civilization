# P10f-bh 非主键 string 机器判断复核收束 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-bh，收束 P10f 最新复扫清单中的非主键 string 机器判断复核。

依据文件：
- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `计划/计划索引.md`
- `docs/工程图谱/05_规则原子索引.md`

复核入口：
- `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_control_panel_node_field_flag.tsv`

已复核结论：
- 对非主键 string 字段做成员访问条件判断扫描后，剩余高频命中主要是同名字段误报，例如 `类型 / 状态 / 失败原因 / 缺口类型` 命中的是枚举或指针字段，不是 TSV 中的 string 字段。
- `摘要 / 消息 / 失败原因` 等真实 string 字段剩余命中主要是 `.empty()` 后补日志、诊断、页面显示或 JSON 展示文本，不参与需求 / 任务 / 方法 / 因果机器落账。
- 全仓成员字段与字符串字面量直接比较复核仅剩 `任务模块.管理界面线程.impl.cpp:4160` 的 `等待项.目标外设ID == "双目相机/D455深度相机"`；该字段属于 ID / 句柄类 string，按当前用户口径排除，不纳入非主键 string 清理。

当前判断：
- P10f 当前非主键 string 字段机器判断可执行清理项已收束；未发现新的无赋值、空输出、只写不读或非主键文本直接决策字段。

验证：
- `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_control_panel_node_field_flag.tsv` 剩余 514 行，其中主键 / ID / 键 / 句柄类 330 行，非主键类 184 行。
- `rg` 全仓成员字段与字符串字面量直接比较只剩 ID 类边界项。

禁止宣称：
- 不宣称自我苏醒完成。
- 不宣称初步成熟完成。
- 不宣称项目结构闭环全部通过。

