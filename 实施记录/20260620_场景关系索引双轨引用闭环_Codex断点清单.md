# 20260620 场景关系索引双轨引用闭环 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.1 P2.2，审查 `场景节点主信息类::关系索引` 与 `场景节点主信息类::二次特征索引` 的引用闭环。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
规范/主信息字段与结构新增准入约束规范20260519.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
实施记录/20260620_场景结构字段判定表.md
```

本切片范围：

```text
1. 只审查并收束 关系索引 / 二次特征索引 双轨。
2. 不处理 状态索引 / 动态索引 / 宿主存在 / 世界类型 的功能函数补缺。
3. 不处理世界树场景坐标 / 轮廓 DTO 归属。
4. 若确认需要新增、删除或改名函数，再按 AGENTS 规则扫描函数说明文档并更新函数文档。
```

验证前预测：

```text
1. 关系索引 与 二次特征索引 均为 std::vector<可解析引用<二次特征节点类>>。
2. 场景类::追加二次特征 与 二次特征类创建 / 删除路径会同时维护两者，属于双写风险。
3. 方法类系统逻辑读取 二次特征索引；关系索引主要用于 SQL / 控制面板 / 语素引用检查。
4. 若未发现关系索引独立业务语义，应删除 关系索引，保留 二次特征索引 作为场景二次特征事实目录。
```

禁止宣称：

```text
不宣称场景类逻辑纠偏完成。
不宣称所有场景主信息直读已回迁。
不宣称结构体清理完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 1. 静态引用闭环

已运行：

```powershell
rg -n "关系索引|二次特征索引" -g "*.h" -g "*.cpp" -g "*.ixx" -g "!x64/**" -g "!Debug/**" -g "!日志/**" -g "!logs/**"
```

证据文件：

```text
日志/20260620_场景关系索引双轨引用闭环_全仓引用.txt
日志/20260620_场景关系索引双轨引用闭环_修改后引用.txt
```

当前判定：

```text
1. 关系索引 与 二次特征索引 均指向 二次特征节点类 引用集合。
2. 方法类系统逻辑只读取 二次特征索引。
3. 关系索引 仅体现为双写 / 双删、SQL 投影、控制面板展示和语素挂载分流，没有独立业务闭环。
4. 语素入口名称种子 `场景关系索引` 暂留；它不是 场景节点主信息类 字段，也不参与字段双写。
```

已实施：

```text
1. 删除 场景节点主信息类::关系索引 字段。
2. 场景类、二次特征类、语素类只维护 二次特征索引。
3. 世界树 SQL 投影和控制面板树展示不再读取 / 展示 关系索引 字段。
```

构建前预测：

```text
1. 若仍有字段残留，构建会报 场景节点主信息类 无成员 关系索引。
2. 若函数签名同步不完整，构建会报 私有_追加二次特征索引 参数不匹配或未声明变量。
3. 若构建通过，P2.2 可进入计划和索引收束。
```

## 2. 构建验证

已运行：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\20260620_场景关系索引双轨引用闭环_msbuild.log
```

结果：

```text
退出码：0
关键日志：已成功生成；0 个错误
```

静态残留复查：

```text
字段访问残留：`->关系索引` / `.关系索引` / 关系索引快照 / 关系索引查重 / 关系索引追加 / 关系索引已存在 均无命中。
剩余文本：语素主信息类.h:40 的 `场景关系索引` 是关系实例入口名称种子，不是 场景节点主信息类 字段。
```

函数文档：

```text
本切片没有新增、删除或改名函数，未运行 tools/生成函数功能说明文档.py。
```

当前结论：

```text
P2.2 / P2.3 中 关系索引 与 二次特征索引 双轨字段问题已闭合。
下一步按计划索引进入 计划/20260620_场景类逻辑纠偏计划_v0.1.md 的 1.2 P3.1：扫描场景节点主信息类字段直读 / 直写路径并分类。
```
