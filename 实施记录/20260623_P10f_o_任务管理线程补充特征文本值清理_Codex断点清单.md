# P10f-o 任务管理线程补充特征文本值清理 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-o `任务模块.工作线程消息协议.ixx::结构_任务管理线程补充特征::文本值` 边界复核。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `规范/任务筹办与执行边界规范20260512.md`
- `规范/任务筹办父方法与协作子方法链规范20260513.md`
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_parent_strategy.tsv`

当前问题：

- `结构_任务管理线程补充特征::文本值` 是非主键类 `std::string` 字段。
- 仓内复核只发现一个写入点，写入内容为 `步骤主键`。
- 未发现任何读取 `文本值` 的路径；同一消息头已经保存 `步骤主键`，补充特征本身还有 `指针值`。

下一步：

- 已删除 `结构_任务管理线程补充特征::文本值`。
- 已调整唯一聚合初始化点，不再传递步骤主键文本；步骤主键仍由消息头 `步骤主键` 保存。
- 首次构建暴露 `自我线程模块.impl.cpp::私有_读取线程补充特征文本` 仍读取 `文本值`；已删除该文本读取入口。
- 自我线程治理判断改为读取 `当前步骤主键` 补充特征的 `指针值`，并以消息头 `步骤主键` 作为结构化补充判断；控制请求存在判断改为读取 `线程消息.控制请求主键`。
- 已刷新 `说明书/函数功能说明文档.md`；函数条目 7186。
- 已生成 `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_thread_extra_feature.tsv`，按既有清单口径剩余记录 627 行，其中主键类 314 行、非主键类 313 行；`结构_任务管理线程补充特征::文本值` 不再命中 string 字段。
- `rg` 复核 `私有_读取线程补充特征文本 / .文本值 / 文本值` 在目标任务管理线程补充特征路径无残留。
- `Debug|x64` 构建通过，0 警告 0 错误。

闭合状态：

- P10f-o 已闭合。
- 下一候选转入 `任务模块.工作线程消息协议.ixx::结构_任务管理观察事实承接请求::来源外设` 边界复核。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
