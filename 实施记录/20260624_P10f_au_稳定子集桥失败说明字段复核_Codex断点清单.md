# P10f-au 稳定子集桥失败说明字段复核 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-au `自我动作实现.外设模块.ixx::结构_稳定子集扫描目标上下文桥结果::首个失败说明` 复核。

依据文件：

- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `计划/计划索引.md`
- `实施记录/20260624_P10f_at_任务管理派生需求动作字段复核_Codex断点清单.md`
- `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_task_derived_action_fields.tsv`

当前证据：

- `首个失败说明` 只在稳定子集扫描目标上下文桥内赋值，并在最终运行日志 `稳定子集桥失败说明=` 中输出。
- 机器判断读取 `目标上下文状态 / 方案项数量 / 扫描约束写入数量 / 扫描约束失败数量 / 首个扫描目标观察约束ID` 等结构化字段，不读取 `首个失败说明`。

下一步：

- 将 `首个失败说明` 改为枚举原因和必要状态值；日志输出时按枚举即时渲染文本，不在结构体中保存说明字符串。

处理结论：

- 已删除 `结构_稳定子集扫描目标上下文桥结果::首个失败说明`。
- 新增局部枚举 `枚举_稳定子集桥失败原因`，并在桥结果中保存 `首个失败原因 / 首个失败可用性状态`。
- 原先赋值说明文本的位置已改为记录枚举原因；目标观察约束不可用时保留 `枚举_目标观察约束可用性` 状态。
- 最终日志仍输出 `稳定子集桥失败说明=`，但文本由枚举和状态即时渲染，不再由结构体 string 字段承载。
- 机器判断仍读取 `目标上下文状态 / 方案项数量 / 扫描约束写入数量 / 扫描约束失败数量 / 首个扫描目标观察约束ID` 等结构化字段。

验证结果：

- `rg -n "std::string 首个失败说明|首个失败说明\\{\\}|首个失败说明 =" 自我动作实现.外设模块.ixx`：无命中。
- `git diff --check` 通过。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`：成功，0 警告，0 错误。
- 已生成 `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_stable_subset_bridge_reason.tsv`：基于上一版权威 TSV 差分过滤并用 live `rg` 复核目标字段已不存在，剩余 567 行，其中主键 / ID / 键 / 句柄类 332 行、非主键类 235 行。

下一步：

- 更新计划索引，继续按最新 TSV 处理剩余字段；优先复核仍带说明 / 消息 / 原因且未登记为日志边界的字段。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
