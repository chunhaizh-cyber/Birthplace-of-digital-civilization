# 20260623 P10f 控制面板自我线程摘要字段结构化 Codex 断点清单

## 0. 恢复摘要

当前主动计划：`计划/计划索引.md` 第 1 章 `P10f-c 后续：string / 摘要 / 原因类字段专项清理`。

当前切片：

```text
`控制面板摘要线程模块.ixx::结构_控制面板摘要快照::自我线程摘要` 由 `读取自我线程最小状态快照()` 的结构化值拼接生成。
该字段跨摘要线程快照、控制面板快照和 CLI 离线摘要继续传递文本。
本切片改为在摘要线程快照中保存自我线程生命周期、阶段、去向、Tick 和 mailbox 的 I64 数值，显示端临时渲染。
```

依据：

```text
规范/禁止性规范20260611.md 第 2 章：不得用字符串字段承载系统内部逻辑或状态。
规范/线程生命周期状态上报与控制面板线程信息规范20260609.md：线程状态信息必须结构化，显示摘要只服务展示。
计划/计划索引.md：P10f-c 继续清理摘要类 string 字段；可由结构化状态替代的显示链优先删除。
```

预测：

```text
删除 `自我线程摘要 / 控制面板摘要线程摘要` 字段不影响机器判断。
控制面板显示和 CLI 离线摘要仍能通过结构化 I64 值渲染生命周期、阶段、去向、Tick 和 mailbox。
目标复扫中 `自我线程摘要 / 控制面板摘要线程摘要 / 私有_采集自我线程摘要` 应无源码命中。
删除旧文本 helper 后需刷新 `说明书/函数功能说明文档.md`。
Debug x64 构建应通过。
```

## 1. 本次修改

```text
已删除 `控制面板摘要线程模块.ixx::结构_控制面板摘要快照::自我线程摘要`。
已删除 `控制面板类.ixx::结构_控制面板快照::控制面板摘要线程摘要`。
摘要线程快照新增结构化数值槽：
    自我线程生命周期值
    自我线程最近阶段值
    自我线程最近去向值
    自我线程Tick计数值
    自我线程Mailbox待消费数值

`控制面板摘要线程模块.impl.cpp::私有_采集自我线程摘要` 改名为 `私有_采集自我线程状态槽`，不再返回拼接文本。
已删除旧拼接文本 helper：
    私有_自我线程生命周期文本
    私有_自我线程阶段文本
    私有_自我线程去向文本

控制面板线程节点显示和 CLI 离线摘要改为从结构化值现场渲染。
```

## 2. 复扫与构建

目标字段复扫：

```powershell
rg -n "自我线程摘要|控制面板摘要线程摘要|私有_采集自我线程摘要|私有_自我线程生命周期文本|私有_自我线程阶段文本|私有_自我线程去向文本" -g "*.cpp" -g "*.h" -g "*.ixx" -g "!x64/**" -g "!Debug/**" -g "!日志/**"
```

结果：无命中。

函数说明文档：

```powershell
python .\tools\生成函数功能说明文档.py
```

结果：已生成 `说明书/函数功能说明文档.md`；源码文件 127，函数条目 7212。已对本次触碰的函数说明文档做 LF 归一化。

构建：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo
```

结果：首轮因 `控制面板类.cpp` 早于 `私有_页面摘要` 定义处调用该 helper 失败，已改为直接使用已有显示字段；复跑 `exit_code=0`。日志：`日志/20260623_P10f_控制面板自我线程摘要字段结构化_build.log`。

## 3. 当前边界

```text
本切片只删除自我线程最小状态快照的拼接文本承接。
`运行事实摘要 / 自检摘要` 仍需后续分别判断是否属于显示证据、诊断日志或可由结构化状态替代。
未触碰外设报告证据卡、动作验证报告摘要、自检候选证据摘要和服务 / 风险安全聚合日志摘要。
```
