# 20260623 P11i live 无调用函数复扫 Codex断点清单

## 0. 恢复摘要

当前主动计划：

```text
计划/计划索引.md 第 1 章：
P11 项目功能函数去重与无效代码清理计划 - live 无调用函数优先复扫。
当前切片：P11i live 无调用函数优先复扫与循环删除。
```

本轮依据：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md
docs/工程图谱/00_图谱总索引.md
docs/工程图谱/04_Codex改代码前检查清单.md
docs/工程图谱/05_规则原子索引.md
实施记录/20260622_P11h_无调用函数循环清理_Codex断点清单.md
实施记录/20260622_P11h_live_无调用候选重扫_after_round4.tsv
```

本轮预测：

```text
1. 先刷新函数说明文档，再生成当前源码 live 无调用候选表，避免沿用旧 TSV 残留。
2. 只有 live 确认无直接调用，且未命中构造 / 析构隐式调用、virtual / override、入口 / 回调 / 导出、注册、消息分发、模板实例、宏拼接、公开合同和项目结构证据责任的函数才删除。
3. 同文件 / 同族满足条件的函数可批量删除。
4. 若没有新的可直接删除函数，记录停止点并返回 P10f-c string / 摘要 / 原因类字段专项。
5. 如发生删除，必须刷新 `说明书/函数功能说明文档.md`、Debug x64 构建、目标函数复扫。
```

当前状态：

```text
工作区进入本轮前为干净状态。
本文件为本轮新增断点。
```

## 1. 扫描尝试记录

```text
已运行 `python .\tools\生成函数功能说明文档.py`：
    通过；源码文件 127，函数条目 7212。

第一次 live 统计尝试：
    复用函数说明文档解析器后，对每个函数逐文件正则匹配；
    命令超时，未得到有效候选表；
    判定为扫描算法复杂度问题，不是项目源码或构建问题。

下一步：
    改为先收集当前函数名集合，再对每个源码文件分词一次并累计命中位置。
```

## 2. 第一轮 live 删除

```text
已生成保守版 live 候选表：
    实施记录/20260623_P11i_live_无调用候选重扫.tsv
    源码文件 127，函数定义 6517；
    call_like_count=0: 37，均为析构 / operator / RAII 守卫类隐式调用或运算符，排除；
    call_like_count=1: 62，优先复核同文件同族普通 helper。

已删除源码只剩定义、非构造析构 / operator / virtual / override / 入口 / 回调 / 导出 / 注册 / 公开合同的普通 helper 30 个：
    任务模块.管理工作线程.ixx：
        私有_写入场景节点引用；
        私有_特征_建议任务状态；
        私有_特征_建议任务阶段；
        私有_特征_方法运行结果；
        私有_值_执行缺口_需求未满足；
        私有_读取任务可执行输入参数场景；
        私有_从筹办运行回执推导状况。
    控制面板类.cpp：
        私有_结果变化方向_控制面板；
        私有_追加唯一片段_控制面板；
        私有_查找根需求_按目标特征_控制面板；
        私有_需求首个任务_控制面板；
        私有_需求目标宿主基础节点_控制面板。
    自我动作实现.服务模块.ixx：
        服务绝对差距；
        有效性评分；
        证据强度评分。
    世界树类.cpp：
        私有_节点在子树。
    因果类.cpp：
        私有_解析场景引用_因果；
        私有_状态引用命中目标；
        私有_状态迁移标量区间_因果。
    自我线程模块.impl.cpp：
        私有_文本或默认。
    鱼巢.cpp：
        私有_包含片段。
    任务模块.筹办.ixx：
        解析状态引用_筹办查询；
        按方法主键查找方法；
        方法结果项承接任务需求。
    自我动作实现.外设模块.ixx：
        计算风险安全第二层投影候选；
        特征_安全因果层级；
        特征_安全因果因素ID。
    自我动作实现模块.ixx：
        实际结果场景复现既有结果_条件结果对归并；
        查找抽象特征_按类型；
        抽象节点相同或类型相同。

已运行 `python .\tools\生成函数功能说明文档.py`：
    通过；源码文件 127，函数条目 7182。

下一步：
    Debug x64 构建；
    构建通过后复扫 live 候选，若仍有可直接删除项继续下一轮。
```

## 3. 第一轮验证与第二轮删除

```text
第一轮验证：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo
    通过；0 警告，0 错误。

第一轮后复扫：
    实施记录/20260623_P11i_live_无调用候选重扫_after_round1.tsv
    函数定义 6487；
    call_like_count=1 从 62 降到 42。

第二轮新增删除对象 11 个：
    任务模块.管理工作线程.ixx：
        私有_值_无筹办缺口；
        私有_值_筹办缺口_任务信息缺失；
        私有_值_筹办缺口_需求目标不可解析；
        私有_值_筹办缺口_无确定候选方法；
        私有_值_筹办缺口_候选方法不可用；
        私有_值_筹办缺口_候选方法缺执行入口；
        私有_值_筹办缺口_候选方法条件不满足；
        私有_值_筹办缺口_参数配对失败；
        私有_读基础节点指针特征。
    因果类.cpp：
        私有_解析状态引用_因果。
    外设线程_D455深度相机.ixx：
        D455_按簇ID查找观察像素簇。

已运行 `python .\tools\生成函数功能说明文档.py`：
    通过；源码文件 127，函数条目 7171。

第二轮验证：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo
    通过；0 警告，0 错误。

下一步：
    生成 after_round2 复扫表；
    继续判断是否仍有符合 P11i 直接删除规则的无调用函数。
```

## 4. 第三轮与最终复扫

```text
第二轮后复扫：
    实施记录/20260623_P11i_live_无调用候选重扫_after_round2.tsv
    函数定义 6476；
    call_like_count=1 从 42 降到 31。

第三轮新增删除对象 1 个：
    需求类.cpp：
        私有_查询来源因果生产者分级。

已运行 `python .\tools\生成函数功能说明文档.py`：
    通过；源码文件 127，函数条目 7170。

第三轮验证：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo
    通过；0 警告，0 错误。

最终复扫：
    实施记录/20260623_P11i_live_无调用候选重扫_after_round3.tsv
    函数定义 6475；
    call_like_count=0: 37；
    call_like_count=1: 30；
    call_like_count=2: 1746。

最终保留边界：
    call_like_count=0 均为构造 / 析构 / operator / RAII 守卫隐式项；
    call_like_count=1 中公开 / 模板 / 基础结构 API 不按普通 helper 删除；
    `私有_消费任务调度队列_按请求 / 私有_消费任务调度队列` 仍按 P11h 任务管理调度专项保留；
    自我线程状态上报、需求创建、安全风险树相关 call_like_count=1 项仍按 P11h 项目结构证据边界保留；
    当前未发现新的“私有、无调用、无入口/回调/注册/公开合同、且不承担项目结构读写证据”的可直接删除函数。

本轮收尾：
    已移除 Python 导入生成的 `tools/__pycache__`；
    已仅对本轮触发 CRLF 提示的 `世界树类.cpp`、`因果类.cpp`、`说明书/函数功能说明文档.md` 归一化为 LF；
    已更新 `计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md` 和 `计划/计划索引.md`；
    P11i 当前可执行无调用函数清理收束，主动队列返回 P10f-c。
```
