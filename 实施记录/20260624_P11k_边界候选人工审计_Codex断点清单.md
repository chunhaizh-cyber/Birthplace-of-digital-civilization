# 20260624 P11k 边界候选人工审计 Codex 断点清单

## 0. 恢复摘要

当前切片：P11k，承接 P11j clean 无调用函数清理后的剩余边界候选人工审计。

依据文件：
- `规范/规范目录.md`
- `计划/计划索引.md`
- `计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md`
- `实施记录/20260624_P11j_live_无调用复扫_Codex断点清单.md`

已确认：
- P11j 已删除 34 个 clean 无调用函数。
- P11j 最终复扫表 `实施记录/20260624_P11j_live_当前源码未限定名候选_after_round6.tsv` 显示 clean_candidates=0。
- 剩余低出现次数候选多数属于 D455 外设实现、公开类接口、构造 / 析构 / operator、header inline、公开合同、入口回调、线程入口或项目结构读写证据。
- 当前工作区已有未提交脏改文件：`任务类.cpp`、`方法类.cpp`、`需求类.cpp`、`语素类.cpp`、`数据库ADO模块.cpp`、`数据库ADO模块.ixx`。本切片不触碰这些文件。

当前目标：
- 不再按 P11j clean 规则批量删除。
- 只对未命中当前脏改文件的边界候选做人工审计，先区分：可删、保留、转专项、待条件。
- 若发现同文件同族、非公开合同、非入口回调、非项目结构证据的低风险函数，再进入最小删除切片。

禁止宣称：
- 不宣称 P11 完成。
- 不宣称项目无无效函数。
- 不宣称内部循环完成 / 自我苏醒完成 / 初步成熟完成。

## 1. 第一小切片闭合记录

删除对象：
- `动作验证报告类.h::摘要语义相同`
- `基础数据类型.h::特征快照有值`
- `基础数据类型.h::结构体_时间戳::时间差_微秒`
- `基础数据类型.h::结构_场景帧::深度_at`
- `基础数据类型.h::结构_场景帧::颜色_at`

删除依据：
- 目标函数在当前源码中只命中定义处；不命中调用点。
- 目标函数不是析构 / operator / virtual / override / 入口 / 回调 / 导出 / 注册 / 线程入口。
- 目标函数不写项目结构，不承担项目结构闭环证据。
- 保留 `基础信息主信息类.h::设置当前值` 和 `度量模板注册表类.ixx::获取或抛`：前者直接写特征主信息当前值，后者是合同读取入口。

已运行命令：
- `python .\tools\生成函数功能说明文档.py`
  - 结果：成功；源码文件 127；函数条目 7193。
- `rg -n "摘要语义相同|特征快照有值|时间差_微秒|深度_at|颜色_at" -g "!x64/**" -g "!Debug/**" -g "!Release/**" -g "!日志/**" -g "!logs/**" -g "!计划/**" -g "!实施记录/**"`
  - 结果：无命中。
- `git diff --check -- 动作验证报告类.h 基础数据类型.h 说明书/函数功能说明文档.md 说明书/特征值类说明.md 计划/计划索引.md 实施记录/20260624_P11k_边界候选人工审计_Codex断点清单.md`
  - 结果：无空白错误；`说明书/函数功能说明文档.md` 仍提示 CRLF 将被 LF 替换。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo`
  - 第一次结果：失败，`LINK : fatal error LNK1104`，原因是 `D:\鱼巢\x64\Debug\鱼巢.exe` 被运行中进程占用。
  - 复核：`Get-Process` 查到 `鱼巢.exe`，随后进程已自行退出。
  - 第二次结果：构建通过，0 警告，0 错误。

当前剩余边界：
- `出现次数=1` 的剩余候选主要是析构 / operator、header inline 基础 API、公开合同、项目结构写入或读取入口。
- 这些对象不能按“无调用直接删除”规则继续批量删除。
- 若继续 P11，需要另开明确专项，例如 D455 外设实现专项、公开合同 API 专项或 header inline API 逐项废弃专项；否则当前直接删除队列已清空。
