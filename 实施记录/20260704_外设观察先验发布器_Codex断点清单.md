# 20260704 外设观察先验发布器 Codex 断点清单

## 0. 恢复摘要

当前切片：

```text
E6 / P8：外设观察先验发布器实现映射计划
S0：代码事实扫描
```

依据：

```text
计划/计划索引.md
计划/20260703_外设观察先验发布器实现映射计划_v0.1.md
规范/详细设计/待完成/20260703_外设观察先验发布器详细设计_v0.1.md
规范/6250_子规范_场景体素服务与视觉先验融合_20260701.md
规范/6320_子规范_外设观察特征与自我场景认知分层_20260701.md
规范/6350_子规范_双目相机外设独占观察线程_20260701.md
规范/6360_子规范_相机外设综合工作流程_20260701.md
规范/1175_根规范_特征表达与判断收束规则_20260703.md
```

边界：

```text
S0 只读扫描场景体素模块、外设观察报告队列、视觉观察融合模块和 D5 缺口承接入口。
允许修改本实施记录。
禁止修改源码、规范、详细设计和计划索引。
```

禁止宣称：

```text
不宣称外设观察先验发布器已代码实现。
不宣称 D455 已接入非阻塞先验读取。
不宣称场景体素先验快照已能自动生成外设处理先验。
不宣称发布器缺口已进入需求治理链。
不宣称场景体素先验引导观察材料前处理已代码实现。
```

## 1. S0 只读扫描记录

### 1.1 已读取依据

```text
docs/current-state.md
docs/decision-log.md
README.md
docs/00_文档总索引.md
docs/工程图谱/00_图谱总索引.md
docs/工程图谱/05_规则原子索引.md
docs/工程图谱/02_外设观察事实图谱.md
规范/规范目录.md
规范/0050_项目通用结构与禁止性规则总纲_20260701.md
规范/1175_根规范_特征表达与判断收束规则_20260703.md
规范/6240_子规范_已确认存在体素融合与分解_20260701.md
规范/6250_子规范_场景体素服务与视觉先验融合_20260701.md
计划/计划索引.md
计划/20260703_外设观察先验发布器实现映射计划_v0.1.md
规范/详细设计/待完成/20260703_外设观察先验发布器详细设计_v0.1.md
```

关键边界：

```text
6250：先验可以帮助看，不能替代看；场景体素快照不得写世界树事实、需求满足、安全 / 服务结算或方法动作动态。
6250：视觉融合层读取合法外设材料和 SceneVoxelPrior 后，只能输出观察域候选、评分、解释和结构化缺口。
1175：先验可用、新鲜度、强匹配、降级匹配、遮挡成立、自由空间冲突等判断型状态不能作为外设、日志或普通函数直接写入的一阶事实。
规则原子：RULE-VISION-PRIOR-001、002、003、004、005 命中；按来源规范 6250 和 1175 回读。
```

### 1.2 场景体素快照入口

代码文件：`场景体素模块.ixx`

可复用入口：

```text
结构_场景体素只读快照：169 起。
结构_SceneVoxelPrior = 结构_场景体素只读快照：187。
场景体素缓存类::同步当前场景快照：810 起。
场景体素缓存类::读取最新快照：1128 起。
场景体素维护线程::读取最新快照：1426 起。
读取场景体素维护线程最新快照：1565 起。
```

已具备的 D6 基础：

```text
快照含版本、坐标系版本、生成时间、存在项集合、缺口集合、统计材料。
快照含先验感兴趣区域、预计深度范围、预计遮挡区域、已知空闲区、未知区、自由空间冲突候选区和可见性预计算结果句柄。
场景体素缺口已具备 D5 结构化缺口大类 / 阻断阶段 / 需求化处理建议映射函数。
读取最新快照返回副本，适合作为发布器只读输入。
```

### 1.3 外设观察报告队列和目标观察约束入口

代码文件：`外设观察报告队列.ixx`

可复用承载：

```text
结构_外设观察报告队列项：455 起。
报告项目标观察约束ID / 本帧处理模式 / 本帧处理优先级 / 本帧处理掩码句柄 / 目标约束使用状态：524-528。
结构_外设观察等待项 同类处理策略槽：562、568-572。
结构_目标观察约束特征组：575 起。
目标观察约束缓冲区：866 起。
```

可复用函数：

```text
提交目标观察约束特征组：3569 起。
读取目标观察约束_按等待项：3593 起。
读取目标观察约束_按目标特征：3615 起。
选择最新可用目标观察约束：3634 起。
读取可用目标观察约束集合_按目标特征：3660 起。
判断目标观察约束可用性：3506 起。
```

风险记录：

```text
结构_目标观察约束特征组 仍有 允许降级 / 允许缓存 bool 字段，结构_目标观察约束可用性判定 仍有 可强匹配 / 可降级匹配 bool 字段。
这些字段只能按既有运行期兼容位处理，S1-S7 不得扩张为项目一阶事实或发布器机器判断承载。
报告项和等待项的处理掩码句柄当前是 std::string；若 S1 需要写入策略身份，必须优先复用已有目标约束 / 报告项槽位，不新增自由文本机器判断。
```

### 1.4 D455 每帧前处理入口

代码文件：`外设线程_D455深度相机.ixx`

已存在流程：

```text
默认全局观察：1451-1452。
原始报告到正式报告复制目标约束 / 本帧处理策略：1733-1737。
D455_目标约束使用状态_由判定：2131 起。
按等待项应用目标约束与处理策略：2160-2166。
等待项路径读取目标观察约束并判定可用性：2313-2333、2426-2428、2538-2549。
```

当前判断：

```text
D455 已能非阻塞读取目标观察约束和等待项策略槽。
未看到 D455 每帧前读取 场景体素模块::读取场景体素维护线程最新快照 或外设处理先验发布器输出。
未看到 D455 等待场景体素维护线程或发布器。
```

### 1.5 视觉融合和 D5 缺口入口

代码文件：

```text
视觉观察融合模块.ixx
任务模块.工作线程消息协议.ixx
```

可复用入口：

```text
结构_视觉融合目标约束摘要：55 起。
结构_视觉融合输入：136 起，已含 SceneVoxelPrior。
融合观察域结果：893 起，已读取 SceneVoxelPrior 版本、坐标系版本、缺口集合和增强图层句柄。
视觉信息缺口_结构化缺口大类 / 阻断阶段 / 需求化处理建议：790、814、838 起。
结构_任务管理派生需求消息：187 起。
填充任务管理派生需求消息结构化缺口槽：398 起。
```

当前缺口：

```text
未看到发布器专用缺口值域。
未看到发布器缺口 -> D5 结构化缺口链的专用适配函数。
未看到“生成外设处理先验_由场景体素先验快照”“发布外设观察先验_由场景体素先验快照”“读取最新外设处理先验_按目标”等函数。
```

### 1.6 S0 当前判断

```text
S0 已完成代码事实扫描。
当前代码已有 SceneVoxelPrior 只读快照、目标观察约束缓冲区、D455 每帧目标约束读取、报告项处理策略槽、视觉融合 SceneVoxelPrior 输入和 D5 缺口承接基础。
当前代码还没有独立外设观察先验发布器入口、发布器缺口值域、发布器到目标观察约束 / 报告项处理策略的生成函数、发布器缺口 D5 标准化函数。
本轮 S0 未修改源码、规范、详细设计或计划索引。
```

下一切片准入：

```text
允许进入 S1：发布函数最小入口。
S1 修改前必须读取相关代码文件头部规则注释模块，并扫描 说明书/函数功能说明文档.md 中近名函数。
S1 不得新增长期业务结构体，不得写世界树事实、稳定存在体素、场景体素缓存、需求满足、动作动态或价值结算。
```

已执行命令摘要：

```text
git status --short --untracked-files=no
git log -3 --oneline --decorate
Get-Content / Select-String 读取必读文档、计划、详细设计、规范和工程图谱
rg 场景体素模块.ixx / 外设观察报告队列.ixx / 外设线程_D455深度相机.ixx / 视觉观察融合模块.ixx / 任务模块.工作线程消息协议.ixx
```

## 2. S1 发布函数最小入口

### 2.1 修改范围

```text
外设观察报告队列.ixx
说明书/函数功能说明文档.md
实施记录/20260704_外设观察先验发布器_Codex断点清单.md
```

未修改：

```text
D455 采集线程
视觉观察融合模块
场景体素缓存写入链
世界树事实写入链
稳定存在体素融合链
需求树 / 任务管理
规范
详细设计
计划索引
```

### 2.2 代码变更

新增普通导出函数：

```text
发布外设观察先验_由场景体素先验快照(...)
生成目标观察约束_由场景体素先验快照(...)
读取最新外设处理先验_按目标(...)
```

实现口径：

```text
导入 场景体素模块，读取 SceneVoxelPrior 只读快照副本。
快照无效、坐标系版本无效、目标存在 / 目标特征缺失、快照过期或无可投影材料时返回 0 / nullopt，作为后续缺口承接信号。
已有可用目标观察约束时返回既有约束 ID，不重复提交。
无既有可用约束时，把快照版本、坐标系、生成时间、严重缺口数量、先验 VecU 句柄锚点和粗空间 AABB 汇总写入现有 结构_目标观察约束特征组。
提交仍走既有 提交目标观察约束特征组(...) 和目标观察约束缓冲区。
```

边界：

```text
本切片不新增长期 C++ 业务结构体。
本切片不改 D455 采集线程，也不让 D455 等待发布器。
本切片不写世界树事实、稳定存在体素、场景体素缓存、需求满足、动作动态或价值结算。
本切片不声明发布器缺口已标准化到 D5；S5 再处理专用缺口映射。
本切片新增的 vecu://scene-prior/... 字符串只作为现有句柄槽的不透明锚点，不作为自由文本判断逻辑。
```

### 2.3 函数说明文档

执行：

```text
python .\tools\生成函数功能说明文档.py
```

结果：

```text
已生成：D:\鱼巢\说明书\函数功能说明文档.md
源码文件：129
函数条目：7693
```

### 2.4 待验证

```text
git diff --check -- 外设观察报告队列.ixx 说明书/函数功能说明文档.md 实施记录/20260704_外设观察先验发布器_Codex断点清单.md
python .\tools\check_specs.py
Debug x64 构建
```

### 2.5 验证结果

```text
git diff --check -- 外设观察报告队列.ixx 说明书/函数功能说明文档.md 实施记录/20260704_外设观察先验发布器_Codex断点清单.md：通过；说明书/函数功能说明文档.md 有 CRLF -> LF 的 Git 提示，不是检查失败。
python .\tools\check_specs.py：hard issues = 0，warnings = 0。
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m：0 警告，0 错误。
```

构建锁：

```text
构建前已创建 .codex_locks/build-run.lock。
构建完成后已释放 .codex_locks/build-run.lock。
```

未执行：

```text
真实 D455 / 自然样本未执行。
```

### 2.6 当前判断

```text
S1 已完成发布函数最小入口。
入口只把 SceneVoxelPrior 只读快照转换为目标观察约束材料，不接入 D455 每帧读取，不生成动作动态，不写事实。
下一切片可进入 S2：场景体素先验快照到目标观察约束映射补强。
```
