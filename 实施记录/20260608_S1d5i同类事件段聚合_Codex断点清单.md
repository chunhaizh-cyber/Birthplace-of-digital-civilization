# 20260608 S1d5i 同类事件段聚合 Codex 断点清单

## 当前切片

- 主动计划：`计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- 执行方案：`计划/20260607_S1d5i同类事件段聚合回潮定位方案_v0.1.md`
- 切片：S1d-5i 同类事件段聚合回潮定位 / 瘦身。

## 依据文件

- `AGENTS.md`
- `规范/规范目录.md`
- `规范/方法动作动态硬规则规范20260512.md`
- `规范/动作动态即因果账本规范20260512.md`
- `规范/二次特征规范20260520.md`
- `规范/远程执行通道与断点续跑规范20260524.md`
- `计划/计划索引.md`
- `计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- `计划/20260607_S1d5h因果模板签名索引瘦身方案_v0.1.md`
- `实施记录/20260608_S1d5h因果模板签名索引_Codex断点清单.md`

## 当前判断

- S1d-5h 已闭合。
- `状态模板查找` 平均约 92us，`动作模板查找` 平均约 48us。
- 最新有效窗口 `日志/鱼巢_run_20260608.低值g:78951` 中 `创建动态/内部阶段耗时` 19 条。
- `创建动态/内部阶段耗时` 最慢阶段全部为 `同类事件段聚合`，`同类事件段聚合` 平均约 185084us。

## 构建 / 运行前预测

- Debug x64 构建通过。
- 真实运行退出码 0。
- 日志继续出现 `创建动态/内部阶段耗时`。
- 日志出现 `同类事件段聚合/内部阶段耗时`。
- 日志保留 `任务状态动作动态=<动态主键>`。
- 不出现 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`。
- 不出现旧路径锚点 `准备本能调用`、`执行本能方法`、`任务域本能输入包`。

## 允许修改

- `二次特征应用模块.ixx`
- `动态类.cpp`
- `计划/20260607_S1d5i同类事件段聚合回潮定位方案_v0.1.md`
- `计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- `计划/计划索引.md`
- `实施记录/20260608_S1d5i同类事件段聚合_Codex断点清单.md`

## 已改文件

- `计划/20260607_S1d5i同类事件段聚合回潮定位方案_v0.1.md`
- `计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- `计划/计划索引.md`
- `实施记录/20260608_S1d5i同类事件段聚合_Codex断点清单.md`
- `二次特征应用模块.ixx`
- `动态类.h`
- `动态类.cpp`

## 下一步

1. 对 `二次特征应用模块.ixx` 和断点清单执行 `git diff --check`。
2. 构建 Debug x64。
3. 真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000`。
4. 解析最新日志窗口，判断下一刀。

## 已实施

- `二次特征生成模块::尝试生成同类事件段聚合动态` 增加 `同类事件段聚合/内部阶段耗时` 低频采样。
- 阶段字段：当前片段解析、场景动态快照、逆序链收集、聚合路径签名计算、已有聚合扫描、聚合主信息创建、递归创建动态。
- 计数字段：场景动态数量、逆序扫描数量、解析成功数量、值形态跳过数量、同主体特征命中数量、同来源命中数量、状态链断开数量、时间倒置数量、链片段数量、已有聚合扫描数量、已有聚合候选数量、路径签名过滤数量、来源链精确比较数量。
- 真实日志初判显示最慢阶段 96 条全部为 `递归创建动态`，平均约 150850us；扫描、签名和精确比较不是主耗时。
- 已增加递归边界：当前动态本身已经是 `同类事件段` 聚合时，不再生成新的同类事件段聚合。

## 递归阻断补充预测

- Debug x64 构建通过。
- 真实运行退出码 0。
- `同类事件段聚合/内部阶段耗时` 中 `递归创建动态` 不再成为主慢点，或日志数量明显下降。
- `创建动态/内部阶段耗时` 中 `同类事件段聚合` 平均耗时下降。
- 任务状态动作动态和旧路径负向锚点保持不变。

## 递归阻断验证结果

- Debug x64 构建通过。
- 真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0。
- 最新窗口 `日志/鱼巢_run_20260608.低值g:85298` 中 `任务状态动作动态` 80 条。
- 未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
- `同类事件段聚合/内部阶段耗时` 107 条，最慢阶段仍全部为 `递归创建动态`，该阶段平均约 168406us。
- `创建动态/内部阶段耗时` 20 条，最慢阶段分布 `同类事件段聚合=19`、`连续同向聚合=1`，`同类事件段聚合` 平均约 177152us。
- 结论：仅阻止聚合动态再次进入同类事件段聚合不足以闭合，主耗时是聚合动态创建时继续触发动态二次特征 / 协同 / 聚合派生后处理。

## 聚合动态后处理瘦身预测

- Debug x64 构建通过。
- 真实运行退出码 0。
- 同类事件段聚合动态仍写入场景动态索引，保留动态层级、聚合方式、动态路径签名和来源低层动态链。
- `同类事件段聚合/内部阶段耗时` 的 `递归创建动态` 平均耗时明显下降。
- `创建动态/内部阶段耗时` 中 `同类事件段聚合` 不再是主慢点，或平均耗时明显下降。
- 任务状态动作动态和旧路径负向锚点保持不变。

## 聚合动态后处理瘦身验证结果

- `动态类::创建动态` 已增加后处理选项，默认保持旧行为。
- 同类事件段聚合创建出的聚合动态关闭动态二次特征、相邻动态协同、连续同向聚合和同类事件段聚合派生后处理。
- 同类事件段聚合动态本体、场景动态索引、动态层级、聚合方式、动态路径签名和来源低层动态链保留。
- Debug x64 构建通过，仅出现既有 LNK4075 警告。
- 真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0。
- 最新窗口 `日志/鱼巢_run_20260608.低值g:88598` 中 `任务状态动作动态` 159 条。
- 未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
- `同类事件段聚合/内部阶段耗时` 11 条，平均约 5626us，`递归创建动态` 平均约 3100us。
- `创建动态/内部阶段耗时` 1 条，`同类事件段聚合` 平均约 3002us。
- S1d-5i 已闭合；下一步进入 S1d-5j 状态动态刷新 / 动态后处理回潮定位。

## 禁止宣称

- 不宣称自我苏醒完成。
- 不宣称初步成熟完成。
- 不跳过同类事件段聚合。
- 不跳过因果账本。
- 不恢复 `任务筹办` / `任务执行` 本能方法。
