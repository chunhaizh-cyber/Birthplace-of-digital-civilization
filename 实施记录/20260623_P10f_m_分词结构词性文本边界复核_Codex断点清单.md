# P10f-m 分词结构词性文本边界复核 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-m `基础数据类型.h::结构体_分词` 的 `词 / 词性` 边界复核。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_causal_structured.tsv`

执行口径：

- 语素词面文本允许作为语言入口种子；注册后必须转为语素入口、枚举或项目内部身份结构再参与判断。
- 人类词性可以从外部文本边界转换，但结构体内部不应继续保存 `std::string 词性` 作为机器路径参数。

当前证据：

- `结构体_分词::词` 只作为语素词面入口种子进入 `语素类::添加词 / 查找词节点`。
- `结构体_分词::词性` 当前是 `std::string`，在 `语素类::查找人类词性入口节点(结构体_分词)` 和 `语素类::添加词性词(结构体_分词)` 中继续传给文本词性重载，再由 `枚举_词性_工厂::根据文本获取枚举值` 转为枚举。
- 仓内未发现 `结构体_分词` 的外部构造调用；改字段类型的影响范围集中在 `基础数据类型.h`、`语素类.cpp` 和函数说明文档。

下一步：

- 已将 `结构体_分词::词性` 改为 `枚举_词性`。
- 已将 `结构体_分词` 构造函数改为接收 `枚举_词性`。
- `语素类` 的结构体重载不再经由文本词性重载，直接走枚举词性路径。
- `结构体_分词::词` 保留为语素词面入口种子；注册后仍由语素入口节点承接。
- 已刷新 `说明书/函数功能说明文档.md`；函数条目 7185。
- 已生成 `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_lexeme_token.tsv`，按既有清单口径剩余记录 629 行，其中主键类 314 行、非主键类 315 行；`结构体_分词::词性` 不再命中 string 字段。
- 已处理本次触碰的 `说明书/函数功能说明文档.md` CRLF 提示，未做全仓换行。
- `Debug|x64` 构建通过，0 警告 0 错误。

闭合状态：

- P10f-m 已闭合。
- 下一候选跳过控制面板 / SQL 投影显示批量字段，转入 `任务模块.筹办.ixx::结构_临时因果视图::父需求继续策略` 边界复核。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
