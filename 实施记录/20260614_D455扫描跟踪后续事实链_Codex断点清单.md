# 20260614 D455 扫描/跟踪后续事实链 Codex 断点清单

## 0. 恢复摘要

```text
默认只读范围：
    只读本节即可恢复当前主动切片；后续章节只在需要复核具体证据时按标题小段读取。

当前切片：
    D455 6150 观察范围对应闭合后，继续复核扫描、跟踪和观察事实动态提交链。

已闭合证据：
    1. 扫描等待项和扫描变化报告已能按目标观察约束精确匹配。
    2. 下一轮扫描基准观察存在已能被后续扫描读取，不再全部走首次扫描分支。
    3. 扫描 / 跟踪方法已优先读取承接请求中的指定外设观察报告ID，不再裸读全局最新同类报告。
    4. 当前真实材料中 `提交_观察存在特征值变化事实=0` 不作为异常；无新增 / 消失 / 变化簇时不应写变化事实。

当前堵点：
    `提交_观察存在发现事实=0` 的自然命中链路未闭合。
    下一步只追：
        首次扫描 / 发现状态=已发现
        -> `提交_观察存在发现事实` 为什么没有自然命中
        -> 是否缺方法候选、缺条件结果对，或任务重筹办未把发现状态作为目标特征继续推进。

关键证据指针：
    日志/鱼巢_run_20260614.低值g
    日志/20260614_D455扫描约束精确匹配_selfcheck_30000ms.console.log
    日志/20260614_D455扫描基准承接_selfcheck_30000ms.console.log
    日志/20260614_D455指定报告读取_selfcheck_30000ms.console.log

已验证命令：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000

禁止宣称：
    不宣称内部循环完成。
    不把 D455 报告直接当作世界事实。
    不把扫描 / 跟踪未触发归因到原始相机质量，除非后续日志证明扫描报告生成前质量门失败。
```

## 追加断点：发现状态已写但发现事实提交未自然命中

问题锚点：

```text
自我_扫描已识别区域 已经在真实日志中写出 发现状态=1；
但同一追加区间未出现 提交_观察存在发现事实 的自然执行。
```

已复核证据：

- `日志/鱼巢_run_20260614.低值g` 行号 `128579` 之后：
  - `131509`、`131697`、`131734`、`132788`、`132946`、`134949` 行均可见 `自我_扫描已识别区域 | 成功=1`，且 `发现状态=1`、`扫描入账状态=1`、`扫描材料等级=优质`。
  - 同一区间检索 `提交_观察存在发现事实`：0 条。
  - 同一区间检索 `本能ID=6151`、`观察存在发现事实提交状态`、`观察存在发现状态`：0 条。
- 全日志中 `提交_观察存在发现事实` 当前只命中自检报告型候选：
  - `17123`、`47008` 行：`本能ID=6151 | 本能动作=提交_观察存在发现事实`，处置为 `目标本能不允许练习，等待领域治理入口`。

已排除路径：

```text
D455 原始材料质量；
6150 有效可观测单位计数 / 对应闭合；
扫描等待项目标观察约束精确匹配；
下一轮扫描基准承接；
扫描 / 跟踪按指定外设观察报告ID读取；
提交_观察存在特征值变化事实=0 的无变化口径。
```

当前判断：

```text
6151 方法结构存在，但自然链没有把扫描发现结果推进成
观察存在发现事实提交状态 / 观察存在发现状态 的任务目标或候选命中。
下一步先追需求 / 任务派生与候选召回，不改 D455 原始材料链。
```

下一步验证：

```text
追 自我_扫描已识别区域 的方法实际结果写入后，
任务管理是否生成目标特征为 观察存在发现事实提交状态 或 观察存在发现状态 的后续任务；
若任务不存在，修派生/路由；
若任务存在但 6151 未命中，再查结果能力匹配和候选过滤。
```

已实施待验证：

- 修改 `自我动作实现.外设模块.ixx`：
  - `自我_扫描已识别区域` 在首次扫描成功、`发现状态=已发现`、`扫描入账状态=已生成`、且存在当前观察存在时，写入宿主派生需求提示：
    - 目标主体：当前观察存在。
    - 目标特征：`观察存在发现事实提交状态`。
    - 当前状态：`未提交`。
    - 目标状态：`已提交`。
  - 该修改只生成任务管理可上报的派生需求提示，不直接执行 `提交_观察存在发现事实`，也不把 D455 材料写成世界事实。

验证预测：

```text
1. Debug x64 构建应通过。
2. 真实 self-check 追加区间中，首次扫描成功后应出现
   自我动作/宿主派生需求已写入 | 来源=自我_扫描已识别区域/首次发现提交入口。
3. 若派生链继续闭合，应进一步出现 任务管理工作线程/派生需求构造，
   目标特征为 观察存在发现事实提交状态，随后自然命中 6151 或进入明确候选/条件缺口。
4. 若仍没有 6151 执行，则下一步只追该派生需求消息是否入树、任务是否生成、候选是否命中。
```

## 追加断点：成功输出派生需求被任务管理闸门阻断

问题锚点：

```text
自我_扫描已识别区域 已把首次发现提交入口写到输出结果场景；
但任务管理工作线程的成功输出派生闸门把非学习承接方法的显式派生需求挡掉。
```

已复核证据：

- `日志/鱼巢_run_20260614.低值g` 最新追加区间：
  - `139214` 行：`自我动作/宿主派生需求已写入 | 来源=自我_扫描已识别区域/首次发现提交入口 | 目标主体=4et | 特征类型=Vj`。
  - `139215` 行：`自我_扫描已识别区域 | 当前存在=4et | 发现状态=1 | 扫描入账状态=1`。
  - `139226` 行：`任务管理工作线程/执行输出派生尾部证据 | 派生需求词=Vj | 派生当前状态=58p | 派生目标状态=58q`。
  - `139227` 行：`执行输出存在派生原子需求=0 | 执行输出需要派生需求=0`。
  - `139327`、`139329`、`139362`、`139363` 行出现同样模式。

已排除路径：

```text
6146 输出结果场景派生需求写入已生效；
D455 原始材料、扫描报告匹配、发现状态写入不是当前新堵点。
```

当前判断：

```text
规范/需求树生长机制规范20260512.md 允许“当前方法执行输出明确给出派生需求特征”形成派生需求消息；
任务模块.管理工作线程.ixx::私有_执行输出需要派生原子需求
当前把“动作成功 + 非学习承接方法”直接返回 false，
导致 6146 的显式发现事实提交派生需求没有进入既有派生消息构造链。
```

最小修正点：

```text
只放开“动作成功且有显式派生需求词”的既有派生消息路径；
没有显式派生需求词的成功动作仍不派生；
缺失参数数量、外设内部材料字段、缺当前/目标状态的既有阻断继续保留。
```

验证预测：

```text
1. Debug x64 构建应通过。
2. 真实 self-check 追加区间中，6146 首次发现输出写入 Vj 后，
   任务管理工作线程/执行输出派生判定 应显示：
       执行输出存在派生原子需求=1
       执行输出需要派生需求=1
3. 应出现 任务管理工作线程/派生需求构造，
   需求特征=Vj，目标主体为当前观察存在，当前/目标状态完整=1。
4. 若派生需求入树和任务生成继续闭合，后续应自然命中 6151；
   若未命中，下一步只追 Vj 派生消息入树、任务生成和候选召回。
```

验证进展：

```text
构建：
    命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    日志：日志/20260614_D455发现事实提交派生_成功输出闸门_msbuild.log
    退出码：0
    关键输出：0 个错误

真实运行：
    命令：.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000
    控制台日志：日志/20260614_D455发现事实提交派生_成功输出闸门_selfcheck_30000ms.console.log
    stderr：日志/20260614_D455发现事实提交派生_成功输出闸门_selfcheck_30000ms.stderr.log
    退出码：-1073741819
    stderr 字节数：0
```

当前追加判断：

```text
本次运行在 21:57:49 左右重新启动后，日志追加到 145014 行；
未进入 6146 首次发现提交入口验证窗口，不能把这次退出直接判为 Vj 派生链失败。
退出码 -1073741819 属于访问冲突类崩溃；项目异常日志未追加同时间错误。
下一步按崩溃根因处理：抓 crash dump / 栈，再判断是否与本次任务管理改动相关。
```

崩溃复核：

```text
命令：.\tools\debug-stack\Invoke-FishnestProcDump.ps1 -ExePath .\x64\Debug\鱼巢.exe -Mode Crash -Arguments '--self-check-log','--self-check-log-wait-ms=30000'
目录：日志/dump/run_Crash_20260614_220046
结果：进程正常退出，Exit Code 0x00000000，未生成 dump。
判断：前一次 -1073741819 非稳定复现；当前派生链验证继续以 22:01 追加日志为准。
```

22:01 追加验证证据：

```text
Vj 派生消息已构造并进入自我线程：
    148230 / 148239 / 148240：
        自我_扫描已识别区域 写入 Vj；
        执行输出存在派生原子需求=1；
        任务管理工作线程/派生需求构造 完整=1。
    148412：
        自我线程/任务管理上行消息入队 | 消息=任务管理派生需求 | 需求特征=Vj | 需求目标主体=4t2。
    148556：
        同类 Vj 派生消息入队，目标主体=4u7。
    148920：
        同类 Vj 派生消息入队，目标主体=4rx。

本 30 秒窗口尚未看到：
    自我线程/派生需求落账开始 | ... 需求特征=Vj
    提交_生成缺口子需求 | 成功=是 | ... 目标特征=观察存在发现事实提交状态
    本能ID=6151 / 提交_观察存在发现事实

窗口末尾：
    149157-149163 显示停止请求到来，规范化消息阶段跳过剩余冻结消息。
当前判断：
    Vj 已过任务管理派生闸门并入自我线程邮箱；
    30 秒窗口不足以证明入树和 6151 候选召回是否闭合。
下一步：
    用 60 秒 self-check 复跑，优先检索 Vj 派生落账、缺口子需求入树、任务化和 6151 自然命中。
```

22:05 60 秒追加验证证据：

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000
控制台日志：
    日志/20260614_D455发现事实提交派生_成功输出闸门_selfcheck_60000ms.console.log
stderr：
    日志/20260614_D455发现事实提交派生_成功输出闸门_selfcheck_60000ms.stderr.log

已复核运行日志追加区间：
    日志/鱼巢_run_20260614.低值g 行号 149193 之后。

Vj 派生输出继续成立：
    152057 / 152220 / 152919 / 153246：
        自我动作/宿主派生需求已写入 | 来源=自我_扫描已识别区域/首次发现提交入口 | 特征类型=Vj。
    152073-152076：
        执行输出派生尾部证据 读到 派生需求词=Vj；
        执行输出存在派生原子需求=1；
        执行输出需要派生需求=1；
        任务管理工作线程/派生需求构造 | 需求特征=Vj | 完整=1。
    152201 / 152526 / 153482 / 153516 / 153527：
        自我线程/任务管理上行消息入队 | 消息=任务管理派生需求 | 需求特征=Vj。

本 60 秒窗口仍未看到：
    自我线程/派生需求落账开始 | ... 需求特征=Vj
    提交_生成缺口子需求 | ... 需求特征=Vj
    提交_生成缺口子需求 | ... 目标特征=观察存在发现事实提交状态
    本能ID=6151 / 提交_观察存在发现事实

验证进程状态：
    22:11:24 复查时 PID=4408 仍在运行；
    命令行为 .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000；
    stderr 字节数=0；
    运行日志尾部停在 154663 行，位置为 程序退出/守卫 -> 停止任务管理工作线程池 -> 后台worker join诊断。
    22:14:23 已抓取现场 dump：
        日志/dump/attach_Snapshot_20260614_221423
    22:14:24 已终止该超时验证进程；
    22:14:25 复查无残留 鱼巢.exe 进程。
```

当前判断：

```text
成功输出派生需求被任务管理闸门阻断的问题已修到可过闸门：
Vj 已从 6146 输出结果场景进入任务管理派生需求构造，并进入自我线程上行消息队列。

当前新堵点变为：
Vj 派生消息入队后，未见自我线程执行 派生需求落账 / 提交_生成缺口子需求 / 任务化 / 6151 候选命中。

22:05 运行进程超时未退出属于验证过程阻塞点；
不把它记成 D455 自然业务链通过或失败。
```

下一步：

```text
1. 先清理仍在运行的 60000ms 验证进程，保证后续不并发运行 鱼巢.exe。
2. 只追自我线程处理 任务管理派生需求 类型消息的链路：
   入队后是否被冻结、规范化、过滤、丢弃，或因 类型=执行缺失 / 阻塞级别=严重 未进入缺口子需求落账。
3. 若代码路径明确拦截 Vj，再做最小修正；不要回到 D455 原始材料质量、6150、扫描报告匹配或扫描基准旧路径。
```

已实施待验证：

```text
修改文件：
    自我线程模块.ixx
    自我线程模块.impl.cpp

最小修正：
    1. 增加任务管理派生需求抢占落账入口。
    2. 从治理 mailbox 中抽取任务管理派生需求消息，复用现有 步骤_规范化消息并落一次特征账_。
    3. 在主循环各既有抢占点和规范化逐条落账后接入派生需求抢占。

不改变：
    D455 原始材料链；
    6146 发现状态写入；
    任务管理成功输出派生闸门；
    提交_生成缺口子需求 入树闸门。
```

验证预测：

```text
1. Debug x64 构建应通过。
2. 真实 self-check 中，6146 写入 Vj 并由任务管理构造派生需求后，应出现：
       自我线程/任务管理派生需求抢占落账
       自我线程/派生需求落账开始 | ... 需求特征=Vj
3. 若 Vj 入树闸门通过，应出现：
       提交_生成缺口子需求 | 成功=是 | ... 需求特征=Vj
       目标特征=观察存在发现事实提交状态
4. 若后续仍未自然命中 6151，再只追 Vj 子需求任务化和候选召回。
```

## 当前切片

在 `6150 提交_当前观察范围可观测单位存在对应事实` 已能稳定闭合之后，继续检查后续的扫描、跟踪和观察事实动态提交链。

## 已复核证据

- 依据最新真实运行日志 `日志/鱼巢_run_20260614.低值g`，从行号 `98000` 之后统计：
  - `当前场景特征值明确状态=1`：6 次。
  - `自我_扫描已识别区域`：0 次。
  - `提交_观察存在发现事实`：0 次。
  - `提交_观察存在特征值变化事实`：0 次。
  - `自我_跟踪指定存在`：0 次。
  - `提交_指定存在跟踪事实`：0 次。
- 同一窗口内任务管理已生成外设等待项：
  - `99223`、`99224`、`99236`、`99237`、`99238` 行可见等待项的 `观察运行模式=已识别场景扫描变化`、`报告类型=扫描变化报告`，但状态均为 `无匹配报告`。
- 同一窗口内 D455 L1 识别报告仍在被承接：
  - `98410`、`98411`、`99220`、`99221` 行可见 `陌生环境逐簇识别` L1 稳定报告被任务管理匹配并生成观察事实承接请求。
- 追加复核显示，扫描报告并非没有生成：
  - 从行号 `98000` 之后，`外设线程_D455深度相机/扫描变化报告入队`：1184 次。
  - `98940` 行可见 `匹配等待项ID=6`，但报告字段为 `目标观察约束ID=26`。
  - `98821` 行同批扫描等待项承接显示 `首个等待项ID=6`、`首个目标观察约束ID=25`。
  - `外设观察_报告满足等待项` 对等待项ID和目标观察约束ID都有强一致门控，因此这种串线报告不会被任务管理匹配。

## 当前判断

本轮已不再是原始 D455 分割质量问题，也不是 6150 计数闭合问题。第一个后续断点收束为：

```text
扫描等待项已经存在，扫描变化报告也已入队；
但 D455 生成报告时按等待项读取目标观察约束发生宽松串线，
导致报告的目标观察约束ID与等待项目标观察约束ID不一致，
随后被任务管理强一致门控拒绝。
```

## 下一步

只追查以下最短路径：

```text
任务管理创建 已识别场景扫描变化 等待项
-> 外设线程是否读取/接收该等待项或处理模式请求
-> D455 外设线程是否生成 扫描变化报告
-> 任务管理是否能匹配并上行
```

当前最小修正点：

```text
目标观察约束_匹配等待项：
等待项已经携带显式 目标观察约束ID 时，只允许按该 ID 精确匹配；
不得再回退到目标存在ID / 目标区域宽松匹配。
```

## 已实施

- 修改 `外设观察报告队列.ixx`：
  - `目标观察约束_匹配等待项` 在 `等待项.目标观察约束ID != 0` 时直接返回 ID 精确匹配结果。
  - 保留无显式目标观察约束ID时的来源等待项 / 目标存在 / 目标区域兼容匹配。

## 验证证据

- 构建：
  - 命令：`msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
  - 日志：`日志/20260614_D455扫描约束精确匹配_msbuild.log`
  - 退出码：0。
  - 关键输出：仅有既有 `LNK4075`。
- 真实运行：
  - 命令：`.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000`
  - 控制台日志：`日志/20260614_D455扫描约束精确匹配_selfcheck_30000ms.console.log`
  - stderr：`日志/20260614_D455扫描约束精确匹配_selfcheck_30000ms.stderr.log`
  - 退出码：0，stderr 字节数=0。
- 真实运行追加区间 `日志/鱼巢_run_20260614.低值g` 行号 `107504` 之后：
  - `外设线程_D455深度相机/扫描变化报告入队`：263 次。
  - `已匹配优质扫描变化报告`：1 次。
  - `已匹配可用扫描材料`：83 次。
  - `已识别区域扫描正式结果`：18 次。
  - `目标跟踪观察 | 报告类型=跟踪报告 | 命中报告=1`：21 次。
  - `提交_观察存在发现事实`：0 次。
  - `提交_观察存在特征值变化事实`：0 次。
- 关键样本：
  - `118077` 行：等待项 `115` 命中扫描报告 `184`，`目标观察约束ID=5`，报告内同为 `目标观察约束ID=5`，`可生成扫描变化候选=1`。
  - `118078` 行：任务管理状态为 `已生成观察事实承接请求`，摘要为 `已匹配优质扫描变化报告`。
  - `118247` 行：`已识别区域扫描正式结果` 成功，扫描报告ID=`186`，`扫描闸门成立=1`，`首次扫描不写变化动态=1`，`有实际变化=0`。
- 异常日志：
  - `日志/鱼巢_exception_20260614.低值g` 未追加新的 D455 / 扫描 / 跟踪 / 6150 相关错误。

## 下一步判断

本切片闭合到：

```text
扫描变化报告 -> 任务管理匹配上行 -> 自我_扫描已识别区域
```

仍未闭合到：

```text
提交_观察存在发现事实 / 提交_观察存在特征值变化事实
```

当前证据显示未提交变化事实的直接原因不是扫描报告无法匹配，而是扫描正式结果仍处在首轮基准或无实际变化口径。下一切片应只复核可比较历史扫描基准、实际变化触发条件和提交事实方法自然命中。

## 追加断点：扫描基准承接断链

- 复核追加区间 `日志/鱼巢_run_20260614.低值g` 行号 `107504` 之后：
  - `已识别区域扫描正式结果`：18 次。
  - `首次扫描不写变化动态=1`：18 次。
  - `扫描基准特征状态=1`：0 次。
  - `有实际变化=1`：0 次。
  - `提交_观察存在发现事实`：0 次。
  - `提交_观察存在特征值变化事实`：0 次。
- 多个当前存在已经重复进入扫描，例如 `4gs`、`4hz`、`4j5`、`4kA`、`4lF`，但第二次扫描仍显示 `历史存在=` 为空。
- 代码对账：
  - `自我_扫描已识别区域` 成功时写入 `特征_下一轮扫描基准观察存在()`。
  - `解析历史观察存在` 只读取 `历史观察存在 / 上一观察存在`，未读取 `下一轮扫描基准观察存在`。
  - `自我_跟踪指定存在` 已把 `下一轮扫描基准观察存在` 当作基准闸门来源，说明该特征不是单纯日志字段。

当前判断：

```text
扫描方法第一轮已建立下一轮扫描基准；
但下一轮扫描没有把该基准作为历史观察存在读取，
导致重复进入“首次扫描”分支，扫描基准状态无法变为已取得。
```

当前最小修正点：

```text
解析历史观察存在：
显式承接 下一轮扫描基准观察存在；
优先从当前观察存在自身读取，避免共享目标宿主上的其他候选基准串线。
```

## 已实施：扫描基准承接

- 修改 `自我动作实现.外设模块.ixx`：
  - `解析历史观察存在` 拆成逐项读取，避免一个无效指针命中后阻断后续候选。
  - 显式读取 `特征_下一轮扫描基准观察存在()`。
  - 对 `下一轮扫描基准观察存在` 允许与当前观察存在为同一节点，用于承接扫描自身第一轮写出的基准。
  - 优先从当前观察存在自身读取下一轮扫描基准，避免共享目标宿主上的其他候选基准串线。

## 验证证据：扫描基准承接

- 构建：
  - 命令：`msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
  - 日志：`日志/20260614_D455扫描基准承接_msbuild.log`
  - 退出码：0。
  - 关键输出：仅有既有 `LNK4075`。
- 真实运行：
  - 命令：`.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000`
  - 控制台日志：`日志/20260614_D455扫描基准承接_selfcheck_30000ms.console.log`
  - stderr：`日志/20260614_D455扫描基准承接_selfcheck_30000ms.stderr.log`
  - 退出码：0，stderr 字节数=0。
- 真实运行追加区间 `日志/鱼巢_run_20260614.低值g` 行号 `118465` 之后：
  - `外设线程_D455深度相机/扫描变化报告入队`：22 次。
  - `已匹配可用扫描材料`：22 次。
  - `已识别区域扫描正式结果`：36 次。
  - `历史存在` 非空：27 次。
  - `首次扫描不写变化动态=1`：9 次。
  - `首次扫描不写变化动态=0`：27 次。
  - `无变化不写变化动态=1`：27 次。
  - `扫描基准特征状态=1`：0 次。
  - `有实际变化=1`：0 次。
  - `提交_观察存在发现事实`：0 次。
  - `提交_观察存在特征值变化事实`：0 次。
- 关键样本：
  - `125217` 行：当前存在 `5sb`，历史存在 `5sb`，`首次扫描不写变化动态=0`。
  - `125419` 行：当前存在 `5r2`，历史存在 `5r2`，`首次扫描不写变化动态=0`。
- 异常日志：
  - `日志/鱼巢_exception_20260614.低值g` 未追加新的 D455 / 扫描 / 跟踪 / 6150 相关错误。

## 下一步判断：扫描材料变化候选

本切片已闭合：

```text
下一轮扫描基准写入 -> 历史观察存在可读 -> 不再重复全部走首次扫描分支
```

尚未闭合：

```text
历史存在可读 -> 可生成扫描变化候选 -> 扫描基准特征状态=已取得 -> 变化事实提交方法自然命中
```

新的直接断点是：

```text
本次真实运行中扫描材料均为可承接 / 可复验，
但进入扫描方法时 `可生成扫描变化候选=0`，
导致有历史存在后仍走待重识别 / 复验缺口分支，
没有进入基准差异和轮廓变化计算。
```

## 追加断点：扫描/跟踪报告读取串线

- 代码对账：
  - `自我_观察陌生目标` 已使用 `读取指定或最新正式外设观察报告`，优先按输入条件包 / 输出场景 / 宿主上的 `外设观察报告ID` 回查。
  - `自我_扫描已识别区域` 仍直接调用 `读取最新外设观察报告(...扫描变化报告)`。
  - `自我_跟踪指定存在` 仍直接调用 `读取最新外设观察报告(...跟踪报告)`。
- 真实日志证据：
  - 任务管理在本次追加窗口生成多个扫描承接请求，例如等待项 `23-31` 分别命中扫描报告 `36-44`。
  - 扫描方法运行时却反复显示 `扫描报告ID=44`，当前存在和历史存在不同但报告未随承接请求切换。

当前判断：

```text
任务管理已经按等待项匹配了具体扫描 / 跟踪报告；
自我扫描和跟踪方法未优先使用承接请求中的报告ID，
而是读取全局最新同类型报告，
会造成当前存在、等待项和报告材料串线。
```

当前最小修正点：

```text
自我_扫描已识别区域 / 自我_跟踪指定存在：
使用已有 `读取指定或最新正式外设观察报告`；
优先按输入报告ID回查，只有无指定报告时才回退到最新正式报告。
```

## 已实施：扫描/跟踪指定报告读取

- 修改 `自我动作实现.外设模块.ixx`：
  - `自我_扫描已识别区域` 改为调用 `读取指定或最新正式外设观察报告`。
  - `自我_跟踪指定存在` 改为调用 `读取指定或最新正式外设观察报告`。
  - 两者都优先按输入条件包 / 输出场景 / 宿主上的 `外设观察报告ID` 回查，避免读取全局最新同类报告。

## 验证证据：扫描/跟踪指定报告读取

- 构建：
  - 命令：`msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
  - 日志：`日志/20260614_D455指定报告读取_msbuild.log`
  - 退出码：0。
  - 关键输出：仅有既有 `LNK4075`。
- 真实运行：
  - 命令：`.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000`
  - 控制台日志：`日志/20260614_D455指定报告读取_selfcheck_30000ms.console.log`
  - stderr：`日志/20260614_D455指定报告读取_selfcheck_30000ms.stderr.log`
  - 退出码：0，stderr 字节数=0。
- 真实运行追加区间 `日志/鱼巢_run_20260614.低值g` 行号 `128579` 之后：
  - `外设线程_D455深度相机/扫描变化报告入队`：344 次。
  - 扫描承接请求：122 次。
  - `已匹配可用扫描材料`：10 次。
  - `已识别区域扫描正式结果`：10 次。
  - 扫描方法读取的报告ID不再固定为全局最新，出现 `18 / 24 / 29` 等不同报告ID。
  - 4 次扫描方法运行时没有有效指定报告，保持失败，不回退到错误最新报告。
  - `提交_观察存在特征值变化事实`：0 次。
- 关键样本：
  - `134903` 行：任务筹办把 `报告ID=24` 写入任务输入。
  - `134949` 行：扫描方法读取 `扫描报告ID=24`，证明已按指定报告承接。
  - `133977` / `133981` 行：报告 `24` 的回查详情显示 `新增簇=0`、`消失簇=0`、`变化簇=0`、`目标特征误差值=0`。
  - `135088` 起的 D455 扫描变化样本卡显示报告 `206+` 已是 `有可比历史`，但变化摘要仍为 `新增=0;消失=0;变化=0`。
- 异常日志：
  - `日志/鱼巢_exception_20260614.低值g` 未追加新的 D455 / 扫描 / 跟踪 / 6150 相关错误。

## 下一步判断：发现事实提交

当前已收束：

```text
扫描报告可按任务管理承接请求指定ID读取；
`可生成扫描变化候选=0` 在当前真实材料中主要来自无新增 / 消失 / 变化簇，
不是 D455 原始质量不足，也不是扫描报告读取串线。
```

因此，`提交_观察存在特征值变化事实` 为 0 在当前窗口不再作为异常处理；没有实际变化就不应生成变化事实。后续若继续推进，应转入：

```text
首次扫描 / 发现状态=已发现
-> `提交_观察存在发现事实` 为什么没有自然命中
-> 发现事实是否缺方法候选、缺条件结果对，或任务重筹办未把发现状态作为目标特征继续推进
```

## 禁止宣称

- 不宣称内部循环完成。
- 不把 D455 报告直接当作世界事实。
- 不把扫描/跟踪未触发归因到原始相机质量，除非后续日志证明扫描报告生成前质量门失败。

## 追加断点：6151 来源报告ID=0 的只读定位

本节只记录只读定位结果，未修改代码。

已确认闭合点：

```text
Vj 派生需求已入树；
Vj 子任务已任务化；
任务筹办已能命中 6151 / 提交_观察存在发现事实；
6151 已发生真实执行；
至少一个真实样本提交成功。
```

关键日志证据：

```text
日志/鱼巢_run_20260614.低值g:157740
    自我_扫描已识别区域 成功=1，当前存在=4lo，
    扫描报告ID=19，发现状态=1，扫描入账状态=1，动作动态=5QG。

日志/鱼巢_run_20260614.低值g:158416
    提交_观察存在发现事实 对同一当前存在=4lo 执行；
    扫描发现状态=1，扫描入账状态=1，来源动作动态=5QG；
    但 来源报告ID=0，观察材料质量依据=缺外设观察报告。

日志/鱼巢_run_20260614.低值g:157875
    自我_扫描已识别区域 成功=1，当前存在=4kj，
    扫描报告ID=24，发现状态=1，扫描入账状态=1，动作动态=5WT。

日志/鱼巢_run_20260614.低值g:158997
    提交_观察存在发现事实 对当前存在=4kj 执行成功；
    来源报告ID=30，观察材料等级=优质，来源动作动态=5WT。
```

对照判断：

```text
4lo 失败样本已经串到正确的扫描来源动作动态 5QG；
因此当前问题不是 6151 未命中，也不是动作因果链完全丢失；
更可能是报告 ID 没有稳定进入 6151 的可执行输入 / 可读摘要层。
```

代码锚点：

```text
自我动作实现.外设模块.ixx:15680-15708
    确认提交观察存在发现事实规格 声明了输入条件包、当前观察存在、
    目标宿主、当前方法运行存在等指针形参；
    未声明 外设观察报告ID 为本方法输入形参。

任务模块.筹办.ixx:5579-5892
    通用绑定候选方法输入参数时，只按方法形参扫描并从目标宿主复制实参。
    日志中方法 20 / 6151 的形参=4，复制I64=0。

任务模块.筹办.ixx:5311-5364
    补齐外设观察报告ID输入参数 只从任务虚拟存在读取报告ID；
    本次 Vj 子任务 O / T / a 未见同窗口 外设观察报告ID参数筹办 日志。

自我动作实现.外设模块.ixx:22725-22734
    读取提交入口I64 的读取顺序为输入参数场景、目标宿主、输出结果场景。

自我动作实现.外设模块.ixx:23863-24025
    提交观察存在发现事实 读取 外设观察报告ID 后，
    以报告质量作为成功条件之一。

自我动作实现.外设模块.ixx:23964-23972
    失败分支也会把当前读取到的 报告ID 写入摘要宿主；
    当报告ID=0 时，后续重试可能继续读到 0，需后续复核是否造成放大效应。
```

当前最可能问题所在：

```text
Vj 派生需求只携带 观察存在发现事实提交状态 的当前/目标状态和目标主体，
未显式携带 6146 扫描报告ID。
6151 规格未把 外设观察报告ID 作为输入条件声明；
筹办层因此不会把报告ID稳定写入可执行输入参数场景。
执行阶段只能从输入场景 / 目标宿主 / 输出场景临时读取报告ID，
在 4lo 样本中该值读成 0，导致材料质量判为 缺外设观察报告。
```

已排除或暂不重追：

```text
不重追 D455 原始材料质量；
不重追 6150 计数闭合；
不重追扫描报告匹配和指定报告读取；
不重追 Vj 入树、任务化、6151 候选召回。
```

下一步只读验证：

```text
1. 复核 Vj 子任务创建时，任务虚拟存在是否有 外设观察报告ID。
2. 复核 6151 可执行输入参数场景中是否缺 外设观察报告ID。
3. 复核失败分支写入 报告ID=0 是否覆盖了目标宿主或当前存在上的有效报告ID。
4. 若三项成立，再进入最小代码切片；本节不改代码。
```

## 追加断点：6151 当前任务O报告ID未稳定补入

本节只记录只读定位结果，未修改代码。

新增日志证据：

```text
日志/鱼巢_run_20260614.低值g:162968-162978
    任务=O 已按目标特征 Vj 命中 方法=20 / 本能ID=6151；
    候选方法通用形参扫描显示 形参=4、复制I64=0、输入场景=89k。

日志/鱼巢_run_20260614.低值g:163760-163784
    任务=O 第二轮重筹办仍命中 6151；
    形参=4、复制I64=0、输入场景=8WZ。

日志/鱼巢_run_20260614.低值g:164125-164137
    任务=O 第三轮重筹办仍命中 6151；
    形参=4、复制I64=0、输入场景=8fR。

日志/鱼巢_run_20260614.低值g:164525-164527
    任务=O 执行 6151 时使用可执行输入参数场景 8fR。

日志/鱼巢_run_20260614.低值g:164604
    同一任务 O / 当前存在=4lo 再次执行失败；
    来源报告ID=0，观察材料质量依据=缺外设观察报告，
    来源动作动态已经变成上一轮失败提交动态 8cC。

只读检索：
    在 162900-164650 当前窗口内检索
    `外设观察报告ID参数筹办 | 任务=O` 无命中。
    对比旧窗口 133982 / 134903 曾出现任务 O 的报告ID补参，
    说明该机制存在，但当前 4lo / Vj 子链未稳定触发。
```

新增代码锚点：

```text
任务模块.管理界面线程.impl.cpp:1453-1474
    `私有_写观察事实报告ID到任务输入` 只把报告ID写入任务虚拟存在；
    输入场景和输入条件包明确写 0，等待下一轮筹办复制。

任务模块.管理界面线程.impl.cpp:9088-9111
    `观察事实更新任务唤醒裁决` 负责调用上述写入并记录
    `报告ID写入任务输入`。

任务模块.筹办.ixx:5311-5364
    `补齐外设观察报告ID输入参数_筹办` 只从任务虚拟存在读取报告ID；
    若任务虚拟存在缺报告ID或报告ID<=0，则静默返回 true。

任务模块.筹办.ixx:5754-5884
    通用参数绑定先调用报告ID补齐，再扫描方法形参；
    6151 当前形参=4，复制I64=0，仍被判定参数闭合。

自我动作实现.外设模块.ixx:15680-15708
    `确认提交观察存在发现事实规格` 未声明 `外设观察报告ID` 形参。
    对比 `自我_识别外设观察材料`、`提交_当前观察范围可观测单位存在对应事实`、
    `自我验证外设观察像素簇候选` 等规格均声明报告ID形参。

自我动作实现.外设模块.ixx:23901-23919
    `提交观察存在发现事实` 执行时读取 `外设观察报告ID`，
    并把报告质量作为成功条件之一。

自我动作实现.外设模块.ixx:23964-23992
    失败分支仍会把当前读到的报告ID写回运行虚拟存在、输出场景、
    目标宿主和当前观察存在；当报告ID=0时，可能放大后续重试的0值。
```

当前最可能问题所在：

```text
第一可疑点：
    当前 4lo / Vj 子链的任务 O 没有在观察事实唤醒阶段稳定写入
    `外设观察报告ID` 到任务虚拟存在，导致筹办补参无源可读。

第二可疑点：
    6151 方法规格没有把 `外设观察报告ID` 声明为必需输入形参，
    因此通用形参扫描即使 `复制I64=0` 也会判定输入闭合，
    让任务进入执行并在提交函数内部才失败。

第三可疑点：
    失败摘要把 `报告ID=0` 写回多个宿主后，
    后续重试的来源动作动态从原始扫描动态变成失败提交动态，
    存在把缺报告状态放大的风险。
```

下一步建议仍保持只读：

```text
1. 只追 `任务=O / 来源需求=Q / 目标宿主=4lo / 目标特征=Vj`
   的创建、同目标复用和观察事实唤醒写入路径。
2. 确认报告ID本应从 6146 扫描结果、Vj 派生需求、还是观察事实唤醒通知进入任务O。
3. 若确认任务O确实缺输入材料引用，再决定最小修复点；
   修复前不要重追 D455 原始材料、6150、扫描报告匹配或 6151 候选召回。
```
