# 20260608 S2b 任务壳应用锁持有阶段定位 Codex断点清单

## 当前切片

```text
计划索引当前主动切片：S2b 任务壳应用锁持有阶段定位 / 瘦身方案。
执行方案：计划/20260608_S2b任务壳应用锁持有阶段定位方案_v0.1.md。
目标：先拆 `完成归并应用任务壳` 锁内阶段，确认主耗时来源；首轮只定位，不改任务状态语义。
```

## 依据文件

```text
AGENTS.md
规范/规范目录.md
规范/方法动作动态硬规则规范20260512.md
规范/任务筹办与执行边界规范20260512.md
规范/自我内部循环实现规范20260512.md
计划/计划索引.md
计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
计划/20260608_S2b任务壳应用锁持有阶段定位方案_v0.1.md
```

## 开工前证据

```text
2026-06-08 15:27/15:28 真实窗口显示：
    高扇出因果模板路径已归零。
    任务壳应用锁持有 65 条，平均约 183160us，最大约 1176170us。
    任务工作项派发锁持有 64 条，平均约 58011us。
    任务工作项派发锁等待 64 条，平均约 36696us。
    任务状态动作动态=71。
    任务状态逻辑错误和旧本能调用锚点均为 0。
    [FTL]=0。
```

## 当前代码判断

```text
任务模块.管理界面线程.impl.cpp 中 `任务壳应用锁` 只包住 `私有_应用工作结果到任务壳(...)`。
`私有_应用工作结果到任务壳(...)` 内部包含：
    回执方法同步
    实际结果状态兼容摘要
    状态提交回执同步
    外设等待项提交
    等待缺口唤醒写入
    任务运行镜像同步
    工作结果应用尾部证据日志
```

## 已改文件

```text
任务模块.管理界面线程.impl.cpp
计划/20260608_S2b任务壳应用锁持有阶段定位方案_v0.1.md
实施记录/20260608_S2b任务壳应用锁持有阶段定位_Codex断点清单.md
```

## 已运行命令

```text
rg -n "性能|S2b|任务壳应用锁|当前主动切片|允许修改|验证证据|后续优先级" 计划/计划索引.md
rg -n "完成归并应用任务壳|任务壳应用锁|任务工作项派发锁|任务状态提交锁|任务互斥/持有耗时|阶段耗时|记录.*耗时|耗时" 任务模块.管理界面线程.impl.cpp
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
```

## 首轮定位证据

```text
构建：Debug x64 通过；仅有 /EDITANDCONTINUE 与 /INCREMENTAL:NO 的既有链接警告。
真实运行：退出码 0。
最新窗口锚点：从 `任务壳应用锁/内部阶段耗时` 首次出现后的日志窗口统计。

任务壳应用锁/内部阶段耗时 55 条：
    内部总耗时平均约 298775us，最大约 1893063us。
    回执方法同步平均约 233800us，最大约 1697978us。
    状态提交回执同步平均约 41984us，最大约 84958us。
    状态提交锁持有平均约 41135us，最大约 78009us。
    最慢阶段分布：回执方法同步 36 条，状态提交回执同步 18 条，运行镜像同步 1 条。

安全核对：
    任务状态动作动态锚点 67 条。
    任务状态被动提交/逻辑错误=0。
    任务状态动作动态逻辑错误=0。
    准备本能调用=0。
    执行本能方法=0。
    任务域本能输入包=0。
    [FTL]=0。
    [ERR]=1，仍为 D455 `自我_识别外设观察材料` 条件不足，非本切片新增错误。
```

## 第三轮尝试与当前预测

```text
第三轮同值快路径尝试后，真实窗口未显示收益：
    `普通整理回执到任务壳/内部阶段耗时` 仍由候选同步、当前方法同步和方法运行结果写入主导。
    同值快路径在锁内增加读特征，不能作为已验证优化保留。

当前代码已撤回 typed 指针 / I64 wrapper 内的同值读取判断。
本轮复测预测：
    阶段定位日志仍应存在。
    主慢点仍应集中在 `回执方法同步` 或 `状态提交回执同步`。
    若 `回执方法同步` 继续主导，下一步需要单独切片处理候选 / 当前方法同步，而不是在任务壳锁内做无证据快路径。
任务状态动作动态必须仍存在；任务状态提交逻辑错误、旧本能调用锚点和 [FTL] 必须仍为 0。
```

## 第四轮瘦身预测

```text
代码已在 `私有_取或创建回执方法虚拟存在_普通同步` 增加方法虚拟存在已有指针复用：
    方法首节点已有 `方法虚拟存在.指针` 时直接返回。
    没有现成指针时仍走原 `取或创建方法虚拟存在 + 同步方法节点到虚拟存在`。

预测收益：
    `普通整理回执到任务壳/内部阶段耗时` 中 `候选同步` 和 `当前方法同步` 应下降。
    `任务壳应用锁/内部阶段耗时` 中 `回执方法同步` 应下降。

边界预测：
    任务状态动作动态仍存在。
    任务状态被动提交/逻辑错误=0。
    任务状态动作动态逻辑错误=0。
    准备本能调用 / 执行本能方法 / 任务域本能输入包仍为 0。
    [FTL]=0。
```

## 第四轮闭合证据

```text
构建：Debug x64 通过；仅有 /EDITANDCONTINUE 与 /INCREMENTAL:NO 的既有链接警告。
真实运行：退出码 0。
本轮窗口：2026-06-08 16:02:56 后，日志/鱼巢_run_20260608.低值g。

普通整理回执到任务壳/内部阶段耗时 91 条：
    总耗时平均约 37309us，最大约 209786us。
    候选同步平均约 3557us，最大约 85550us。
    当前方法同步平均约 4803us，最大约 37715us。
    方法运行结果写入平均约 12110us，最大约 28207us。
    最慢阶段分布：方法运行结果写入 71 条，当前方法同步 11 条，候选同步 4 条，执行回执字段写入 4 条，候选集读取 1 条。

任务壳应用锁/内部阶段耗时 91 条：
    总耗时平均约 101855us，最大约 370557us。
    回执方法同步平均约 37415us，最大约 209896us。
    状态提交回执同步平均约 45621us，最大约 125299us。
    最慢阶段分布：状态提交回执同步 72 条，回执方法同步 19 条。

互斥与状态链：
    任务壳应用锁持有 91 条，平均约 101960us，最大约 370629us。
    任务工作项派发锁持有 90 条，平均约 54096us，最大约 452740us。
    任务工作项派发锁等待 90 条，平均约 30033us，最大约 998771us。
    任务状态提交阶段 94 条，平均约 63437us，最慢阶段全部为执行被动提交。
    任务状态巡检阶段 124 条，平均约 31243us，等待工作项登记平均约 107us。

安全核对：
    任务状态动作动态锚点 96 条。
    任务状态被动提交/逻辑错误=0。
    任务状态动作动态逻辑错误=0。
    准备本能调用=0。
    执行本能方法=0。
    任务域本能输入包=0。
    [FTL]=0。
    [ERR]=1，仍为 D455 `自我_识别外设观察材料` 条件不足，非本切片新增错误。

当前判断：
    S2b 已闭合。
    无收益 typed 指针 / I64 wrapper 同值读取快路径已撤回。
    保留的优化仅为任务壳整理侧复用已有方法虚拟存在指针；没有现成指针时保持原同步路径。
    下一候选切片应单独复核 `状态提交回执同步` 与 `任务工作项派发锁`，不在 S2b 中继续混改。
```

## 下一步

```text
S2b 已闭合。
若继续性能优先，下一候选切片为 S2c：
    复核 `状态提交回执同步` 与 `任务工作项派发锁`。
    先制定小方案，再决定是否进入实现。

不要在 S2b 中继续混改：
    状态提交链；
    worker 派发锁；
    日志 flush 策略；
    D455 外设专线；
    基础信息 / 候选方法 / 因果主果索引。
```

## 禁止宣称项

```text
不得宣称自我苏醒完成或初步成熟完成。
不得宣称 S2b 已优化闭合，除非有构建、真实运行和日志证据。
不得把任务状态变化改成裸写或绕开 `提交任务状态变化`。
不得把任务筹办 / 任务执行恢复为本能方法。
```
