# 20260620 基础信息类递归释放主信息 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_基础信息类逻辑纠偏计划_v0.1.md` 的 1.1，补 `基础信息类::释放节点主信息递归`，用于承接存在类删除存在时的主信息生命周期缺口。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_存在类逻辑纠偏计划_v0.1.md
计划/20260620_基础信息类逻辑纠偏计划_v0.1.md
实施记录/20260620_存在类非存在主信息操作审查_Codex断点清单.md
基础信息类.h
基础信息类.cpp
存在类.cpp
双向链表模板核心.h
```

本切片范围：

```text
1. 新增 基础信息类::释放节点主信息递归。
2. 存在类::删除存在 改为调用基础信息类生命周期函数。
3. 不改变 基础信息类::删除节点 的摘链行为。
4. 不改变双向链表模板“不拥有指针型主信息生命周期”的规则。
```

验证前预测：

```text
1. 双向链表模板不会自动 delete 指针型主信息，基础信息类已有析构清理主信息逻辑。
2. 新函数应只释放主信息并置空，不删除节点。
3. 存在类中 delete 节点->主信息 / 节点->主信息 = nullptr 命中应清空。
4. 函数说明文档生成器和 Debug x64 构建应通过。
```

## 1. 实施记录

```text
1. 已在 基础信息类.h / 基础信息类.cpp 新增 释放节点主信息递归(节点类* 节点) noexcept。
2. 新函数递归释放指定节点及子链节点的主信息，并将主信息指针置空，不删除节点本身。
3. 已将 存在类::删除存在 改为调用 基础信息类::释放节点主信息递归 后再调用 基础信息类::删除节点。
4. 已删除 存在类 内部 私有_递归释放基础信息主信息。
```

## 2. 验证结果

```text
1. 复核：
   rg 输出已写入 日志/20260620_基础信息类递归释放主信息_复核.txt。
   结果：存在类.cpp 只剩 基础信息_->释放节点主信息递归(节点)，delete 节点->主信息 / 置空命中已归入 基础信息类.cpp。

2. 函数说明文档：
   python .\tools\生成函数功能说明文档.py
   退出码：0
   日志：日志/20260620_基础信息类递归释放主信息_函数文档.log
   关键输出：源码文件 128；函数条目 6885。

3. 构建：
   msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
   退出码：0
   日志：日志/20260620_基础信息类递归释放主信息_msbuild.log
   关键输出：已成功生成。
```

## 3. 当前判断

```text
1. 本切片已闭合：主信息递归释放职责已迁回 基础信息类。
2. 已返回存在类 1.3，继续对剩余扫描命中做分类。
3. 不宣称基础信息类逻辑纠偏完成。
```
