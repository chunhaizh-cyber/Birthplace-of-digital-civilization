# 20260627 自我所在场景存在显示 Codex断点清单

## 0. 恢复摘要

当前切片：控制面板自我场景显示口径纠偏。

用户口径：

```text
将自我所在场景指针中存在显示出来。
这是自我认知的世界。
场景体素显示出来只是为了调试使用。
```

当前结论：

```text
主显示源：结构_控制面板快照.自我场景存在复现项列表。
来源指针：自我.获取自我现实场景() 写入的 自我所在场景指针。
调试层：观察候选、诊断区域、快照覆盖层只作调试参考。
```

禁止宣称：

```text
不得宣称视觉系统完成。
不得宣称场景体素是自我认知世界事实源。
不得用控制面板 / OpenGL 显示证明世界树事实、需求满足、扫描闭合或 D455 自然业务闭合。
```

## 1. 依据

```text
规范/自我所在场景认知工作区规范20260525.md：
    自我所在场景是当前认知工作区，只保存自我认知过程继续使用的信息。

规范/场景体素服务与视觉先验融合规范20260625.md：
    场景体素服务只读发布快照，不写世界树事实；快照可用于显示，但不是事实源。

docs/工程图谱/05_规则原子索引.md：
    场景体素和 OpenGL 复现快照属于运行期心像缓存，不直接裁决世界树事实。
```

## 2. 已改文件

```text
控制面板类.cpp
```

修改内容：

```text
1. 自我场景页面右侧首屏改为“自我认知世界 / 自我所在场景存在”。
2. 新增存在列表显示，读取既有 existences JSON，不新增事实来源。
3. WebGL 状态提示改为以存在绘制数量为主。
4. WebGL 视角范围优先由存在中心 / AABB 决定；只有没有可绘制存在时才用观察候选兜底。
5. 观察候选、诊断区域和覆盖层文案降级为调试快照 / 调试图层。
```

## 3. 已运行命令

```powershell
git diff --check -- 控制面板类.cpp
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
.\x64\Debug\鱼巢.exe --panel-html
```

结果：

```text
git diff --check：通过；仅有 CRLF/LF 提示。
msbuild：通过，0 警告，0 错误。
--panel-html：退出码 0，SQL 投影静态页生成成功。
```

说明：

```text
--panel-html 生成的是 SQL 投影静态页，不是本切片修改的 live 自我场景 WebGL 页。
生成后产生的 控制面板.html 时间戳 / 运行批次噪声已丢弃，未纳入本切片差异。
```

## 4. 当前判断

```text
代码显示口径已纠偏为：自我所在场景存在是主显示对象。
体素 / 观察候选 / 诊断区域仍仅是调试辅助。
本切片不改变世界树事实写入、不改变需求树、不改变动作动态、不改变场景体素维护线程。
```

## 5. 下一步

```text
若要进一步做视频化显示，应以自我所在场景存在列表 / OpenGL 复现为主画面。
场景体素可作为可开关调试层或离线诊断页，不应替代自我所在场景存在显示。
```

## 6. 直达显示入口补充

追加原因：

```text
用户反馈“没有显示画面”。
原因是 `--panel-html` 只生成 SQL 投影静态页，不能显示 live 自我场景 WebGL 页面；
原有路径需要先打开控制面板，再点“自我所在场景 / 打开独立窗口”，入口不直观。
```

已补入口：

```powershell
.\x64\Debug\鱼巢.exe --panel-self-scene
```

等价别名：

```text
--self-scene-window
--scene-window
```

补充验证：

```text
python .\tools\生成函数功能说明文档.py：通过，7325 函数。
git diff --check：通过，仅 CRLF/LF 提示。
msbuild Debug x64：通过，0 警告，0 错误。
Start-Process .\x64\Debug\鱼巢.exe --panel-self-scene 后进程保持运行；
窗口标题确认：鱼巢自我所在场景。
```
