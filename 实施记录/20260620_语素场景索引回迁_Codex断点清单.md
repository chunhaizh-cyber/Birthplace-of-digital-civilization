# 20260620 语素场景索引回迁 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.2 P3.3i，回迁 `语素类.cpp` 中 `私有_场景索引包含` 和 `私有_把基础信息挂入场景索引` 的场景索引检查 / 追加路径。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
实施记录/20260620_动态场景索引读取回迁_Codex断点清单.md
```

本切片范围：

```text
1. 修改 语素类.cpp。
2. 私有_场景索引包含 改为调用 场景类::读取场景状态索引快照 / 读取场景动态索引快照 / 读取场景二次特征索引快照。
3. 私有_把基础信息挂入场景索引 改为调用 场景类::追加状态 / 追加动态 / 追加二次特征。
4. 不新增、删除或改名函数。
```

验证前预测：

```text
1. 语素类.cpp 不再直接包含 场景索引同步.h。
2. 语素类.cpp 不再直接调用 借用场景索引全局互斥。
3. 语素类.cpp 不再直接读取 / 写入 场景主信息->状态索引、动态索引、二次特征索引。
4. Debug x64 构建应通过。
5. 函数文档无需重生成，因为本切片未新增、删除或改名函数。
```

禁止宣称：

```text
不宣称语素类全部逻辑纠偏完成。
不宣称所有场景主信息直读已回迁。
不宣称场景类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 1. 修改结果

```text
已修改：
    语素类.cpp

修改内容：
    私有_场景索引包含 增加基础信息仓库参数，并改为调用 场景类 三类索引快照函数。
    私有_把基础信息挂入场景索引 增加基础信息仓库参数，并改为调用 场景类::追加状态 / 追加动态 / 追加二次特征。
    语素类.cpp 移除不再使用的 场景索引同步.h include。
```

## 2. 验证结果

直读检查：

```powershell
rg -n "取主信息<场景节点主信息类>|dynamic_cast<.*场景节点主信息类|场景主信息->|\.状态索引|\.动态索引|\.二次特征索引|借用场景索引全局互斥|场景索引同步|追加状态|追加动态|追加二次特征|读取场景.*索引快照" 语素类.cpp
```

关键结果：

```text
语素类.cpp 只剩 场景类 快照 / 追加函数调用。
```

构建验证：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

证据：

```text
日志/20260620_语素场景索引回迁_msbuild.log
退出码：0
已成功生成。
0 个错误
```

剩余复扫：

```text
日志/20260620_语素场景索引回迁_剩余场景主信息引用.txt
总命中：33
语素类.cpp：0
剩余主要为 控制面板 / 语言 / 世界树显示投影，以及少量父节点是否场景的身份判断。
```

## 3. 下一步

```text
P3.3j 复核剩余非显示身份判断路径，优先补 场景类::是场景节点 并回迁可闭合的系统身份判断。
```
