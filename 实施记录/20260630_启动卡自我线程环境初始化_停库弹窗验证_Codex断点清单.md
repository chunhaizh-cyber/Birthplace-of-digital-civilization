# 20260630 启动卡自我线程环境初始化停库弹窗验证 Codex 断点清单

## 0. 恢复摘要

- 当前切片：`追根因_20260629_启动卡自我线程环境初始化`。
- 当前计划状态：代码修复、正常 SQL 状态下启动验证、Debug x64 构建证据已有；剩余待补为 SQL Server 停止状态下数据库未启动弹窗真实分支。
- 本轮执行口径：用户已明确计划索引内未完成计划不需要授权；本轮可临时停止 `MSSQL$SQLEXPRESS` 做真实验证，验证后必须恢复服务。
- 禁止宣称：不宣称内部循环完成、自我苏醒完成或初步成熟完成；不把控制面板 SQL 显示镜像失败当业务结构失败。

## 1. 依据

- `计划/追根因_20260629_启动卡自我线程环境初始化_v0.1.md`。
- `数据库ADO模块.cpp:140`：SQL Server 未启动或拒绝访问时一次性弹窗。
- `数据库ADO模块.cpp:167`：标记控制面板运行态 SQL 显示镜像不可用。
- `鱼巢.cpp:1503`：启动时初始化控制面板运行态 SQL 投影；失败只影响控制面板 SQL 显示镜像。

## 2. 预测

- 停止 `MSSQL$SQLEXPRESS` 后启动 `鱼巢.exe`，应记录 `弹窗错误提示 | 标题=鱼巢 - 数据库未启动提醒`。
- 关闭弹窗后，应记录 `弹窗错误提示已返回 | 标题=鱼巢 - 数据库未启动提醒`。
- 后续控制面板同步应快速返回 `SQL控制面板运行态不可用`，不应反复长时间等待 SQL 连接。
- 验证完成后恢复 `MSSQL$SQLEXPRESS` 到原始运行状态。

## 3. 本轮执行结果

- 当前 SQL 服务状态：`MSSQL$SQLEXPRESS` 为 `Running`，`SQLBrowser` 为 `Running`。
- 尝试执行停库验证脚本：脚本在服务控制阶段失败，未生成 17:25 之后的新 `鱼巢_run_20260630.低值g` 或 `鱼巢_exception_20260630.低值g` 运行证据。
- `Start-Service -Name 'MSSQL$SQLEXPRESS' -ErrorAction Stop` 即使在服务已运行状态下也返回 `Cannot open 'MSSQL$SQLEXPRESS' service on computer '.'`。
- `sc.exe query 'MSSQL$SQLEXPRESS'` 可读到服务为 `RUNNING`，但当前 PowerShell 服务控制接口不能可靠打开该服务做停止 / 启动闭环。
- 已清理残留 `.codex_locks/build-run.lock`；当前未见 `鱼巢.exe` 进程。

## 4. 单项堵点

- 堵点：当前执行环境没有可靠的 SQL Server 服务控制权限，不能安全完成“停止 `MSSQL$SQLEXPRESS` -> 启动鱼巢 -> 关闭数据库未启动弹窗 -> 恢复 SQL 服务”的实机验证闭环。
- 恢复条件：在有管理员服务控制权限的终端中复跑，或由用户先提供 SQL Server 已停止状态，再执行 `鱼巢.exe` 并关闭 `鱼巢 - 数据库未启动提醒` 弹窗后检查日志。
- 当前不能宣称：数据库未启动弹窗分支已实机跑通。
