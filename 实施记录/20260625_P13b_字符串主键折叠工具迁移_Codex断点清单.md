# 20260625 P13b 字符串主键折叠工具迁移 Codex断点清单

## 0. 恢复摘要

当前切片已闭合。已处理 `自检线程模块.impl.cpp::私有_折叠主键(const std::string&)`。

依据：

```text
计划/计划索引.md：P13b 全局共享迁移；B 类候选逐个判定迁移 / 复用 / 保留。
实施记录/20260624_P13_函数自定义主信息分类候选表.tsv：`私有_折叠主键` 为 B 类候选，未命中自定义主信息 / 节点 / 结构 / 协议对象。
全局共享函数类.ixx 文件头规则：无领域自定义主信息 / 领域类型参与的通用函数可进入本模块。
```

当前判断：

```text
`私有_折叠主键` 对已有主键字符串做稳定 U64 折叠，用作自检候选数值槽，不按自由文本内容做业务状态分支。
该函数只依赖标准字符串视图和 U64 运算，适合迁入 `全局共享函数类.ixx`。
`全局共享函数类.ixx` 和 `说明书/函数功能说明文档.md` 未发现同功能函数。
```

已完成：

```text
1. 在 `全局共享函数类.ixx` 新增 `折叠字符串主键(std::string_view)`。
2. 给 `自检线程模块.impl.cpp` 补最小“文件头部规则注释模块”。
3. 删除本地 `私有_折叠主键` 前置声明和定义，调用点改为全局函数。
4. 运行函数说明文档生成脚本：扫描源文件 127 个，函数条目 7163。
5. Debug x64 构建通过：0 警告，0 错误。
```

复扫结果：

```text
`说明书/函数功能说明文档.md` 已登记 `全局共享函数类.ixx::折叠字符串主键`。
`自检线程模块.impl.cpp::私有_折叠主键` 不再出现在函数说明文档和当前源码中。
```

禁止宣称：不宣称 P13 完成，不宣称函数去重全部完成，不宣称自我苏醒完成或初步成熟完成。
