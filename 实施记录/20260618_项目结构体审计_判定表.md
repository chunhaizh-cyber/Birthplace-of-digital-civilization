# 20260618 项目结构体审计判定表

## 第一批：任务管理消息协议

| 结构体名 | 是否有用 | 是否不可替代 | 项目结构映射 | 不合规点 | 外部读取证据 | 结论 | 最小修改切片 | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `结构_任务管理消息头` | 有用 | 当前不可直接替代 | 发出者/任务/场景文本主键 -> `安全解析或散列任务管理主键` -> 治理消息头数值主键 | 源协议仍使用文本主键；需要后续结构化改造，不能在本切片硬删 | `自我线程模块.消息协议.ixx:1590`、`:1746` 等构造治理消息路径读取 | 保留为后续修改候选 | 本切片只登记，不删除 | 后续专项改造时给出主键字段结构映射和构建验证 |
| `结构_任务管理未满足项` | 无独立用途 | 可替代 | 无。写入后没有落到项目结构，只有 `未满足项集.size()` 参与对账 | `未满足项类型` 和 `作用主体主键` 是文本字段；结构体只剩内部集合计数/对账视图 | `任务模块.管理工作线程.ixx:3185` 写入；`自我线程模块.消息协议.ixx:1229` 只比较 size | 删除 | 删除结构体、结果消息中的 `未满足项类型文本`/`未满足项集`、写入构造和 size 比较 | Debug x64 构建 |
| `结构_任务管理状态变化项` | 目标用途有用，但结构体本身不合格 | 已替代 | 服务对象外在特征变化证据 -> `结构_叶子任务正式结算证据.before状态 / actual状态` + `D0_业务动作动态` + `任务完成动作动态` -> G0 门槛特征 | 原结构无写入方，且主体/特征/原因依赖文本字段；不能继续作为证据结构 | 新路径：`自我线程模块.impl.cpp` 的正式结算证据评估、服务状态动态证据同步函数；源码已无结构体引用 | 已迁移后删除 | 删除结构体、结果消息 vector、归并/落账/参数传递；服务证据改由状态和动态承担 | `rg` 源码引用 0；Debug x64 串行构建 0 错误 |
| `结构_任务管理关键中间状态变化项` | 目标用途有用，但结构体本身不合格 | 已替代 | 关键中间状态恢复判断 -> `结构_任务管理结果状态消息` 的结构化状态位和结果状态枚举 | 原结构无写入方，且动作主体/变化主体/特征类型依赖文本字段；不能继续作为恢复证据结构 | 新路径：`私有_任务管理结果存在未恢复关键中间状态_线程侧` / `私有_任务管理结果存在已恢复关键中间状态_线程侧`；源码已无结构体引用 | 已迁移后删除 | 删除结构体、结果消息 vector、归并/落账路径；恢复判断改由结果状态结构化字段推导 | `rg` 源码引用 0；Debug x64 串行构建 0 错误 |

## 当前不扩大范围

本轮已处理 `结构_任务管理未满足项`、`结构_任务管理状态变化项`、`结构_任务管理关键中间状态变化项`。`结构_任务管理消息头` 和治理消息中的文本摘要 / 原因键仍属于同一协议结构化大切片，但仍有跨模块读取和治理消息路由职责，需另切片审计，不能在本次删除。

## 第二批：自我线程执行尝试内部视图

| 结构体名 | 是否有用 | 是否不可替代 | 项目结构映射 | 不合规点 | 外部读取证据 | 结论 | 最小修改切片 | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `结构_执行尝试标识` | 目标用途有用，但结构体本身只是内部视图 | 可替代 | 执行尝试归并基准 -> `结构_任务管理消息头` 即时数值投影 + `结构_执行尝试汇总` 数值基准字段；汇总/对账结果继续使用枚举状态和值槽 | 结构体复制 `执行尝试主键`、`任务主键`、`父任务主键`、`步骤主键`、`幂等键` 等文本字段，只用于内部对账，未独立进入存在/场景/特征/特征值/方法/需求结构 | 原引用仅在 `自我线程模块.消息协议.ixx` 的对账和汇总函数内部；删除后源码引用为 0 | 已分解后删除 | 删除结构体和旧提取函数；新增 `任务管理消息头执行尝试主键值`、`建立执行尝试汇总基准`、`执行尝试汇总基准可对齐`；汇总保存数值基准 | `rg` 源码引用 0；函数文档刷新；Debug x64 构建 0 错误；真实自检窗口 exit_code=0、stderr=0 |

## 验证中发现并处理的阻塞问题

| 问题 | 根因 | 修改 | 验证 |
| --- | --- | --- | --- |
| `鱼巢 - 自检需求入树逻辑错误` | `私有_裁决自检候选父需求_线程侧` 漏掉 `方法状态 -> 方法维护` 父域映射，导致自检方法状态好用候选落入未判定并弹窗 | 新增 `私有_目标特征是方法维护字段_线程侧`，将 `方法状态` 目标特征转入现有方法维护管理分支 | 复跑同一自检窗口 exit_code=0、stderr=0、异常日志未增长；运行日志出现 `父域=方法维护` 和 `自检正式需求入树` |

## 第三批：控制面板 OR 组令牌显示视图

| 结构体名 | 是否有用 | 是否不可替代 | 项目结构映射 | 不合规点 | 外部读取证据 | 结论 | 最小修改切片 | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `结构_OR组令牌只读视图_控制面板` | 显示目标有用，结构体本身无独立承载价值 | 可替代 | OR 组令牌状态/路径证据 -> 世界树特征 `OR组结算令牌状态` / `OR组领取路径`；控制面板只生成显示文本 | 结构体只保存两个 `std::string` 显示字段，未进入项目结构，也不承担状态或动态证据 | 原引用只在 `控制面板类.cpp` 三个内部调用点，删除后源码引用为 0 | 已分解后删除 | 删除结构体和旧返回函数；新增 `私有_OR组令牌状态文本_控制面板`、`私有_OR组令牌当前激活路径文本_控制面板` | `rg` 源码引用 0；函数文档刷新；结构体清单 536/298；Debug x64 构建 0 错误 |
| `结构_控制面板缺口承接快照` | 显示目标有用，结构体本身无独立承载价值 | 可替代 | 缺口承接状态 -> 自我特征 `自我_待处理方法数量` + `自我线程类::结构_缺口恢复接口快照`；控制面板只复制到页面快照 | 结构体只是内部聚合视图，含摘要文本字段，没有外部读取，也不写入项目结构 | 原引用只在 `控制面板类.cpp` 定义函数和主快照构造点，删除后源码引用为 0 | 已分解后删除 | 删除结构体和旧聚合函数；新增 `私有_读取自我待处理方法数量_控制面板`、`私有_缺口恢复反馈摘要_控制面板` | `rg` 源码引用 0；函数文档刷新；结构体清单 535/297；Debug x64 构建 0 错误 |
| `结构_方法直接结构计数` | 显示和诊断目标有用，结构体本身只是计数包 | 可替代 | 方法树结构计数 -> 当前函数局部变量；控制面板只用于显示/异常提示，不作为方法能力证据 | 两处同名结构体只保存四个 `std::size_t` 计数，没有外部读取，也不写入项目结构 | 原引用在 `控制面板类.cpp` 和 `控制面板摘要线程模块.impl.cpp` 内部，删除后结构体源码引用为 0 | 已分解后删除 | 删除两处结构体；统计函数改为引用输出局部计数 | `rg` 源码引用 0；函数文档刷新；结构体清单 533/297；Debug x64 构建 0 错误 |
| `结构_方法结构计数` | 自检判断目标有用，结构体本身只是计数包 | 可替代 | 方法树结构计数 -> 当前函数局部变量；自检缺口仍由原有需求/特征/日志链路承接 | 结构体只保存条件数/结果数，没有外部读取，也不写入项目结构 | 原引用只在 `自检线程模块.impl.cpp` 内部，删除后源码引用为 0 | 已分解后删除 | 删除结构体；`私有_统计方法直接结构_自检` 引用输出局部计数 | `rg` 源码引用 0；函数文档刷新；结构体清单 531/297；Debug x64 构建 0 错误；self-check 异常日志不增长 |
| `结构_条件结果对自检统计` | 自检判断目标有用，结构体本身只是计数包 | 可替代 | 条件结果对计数 -> 当前函数局部变量；自检缺口仍由原有需求/特征/日志链路承接 | 结构体只保存数量/命中总和/最大命中，没有外部读取，也不写入项目结构 | 原引用只在 `自检线程模块.impl.cpp` 内部，删除后源码引用为 0 | 已分解后删除 | 删除结构体；`私有_统计方法条件结果对_自检` 引用输出局部计数 | `rg` 源码引用 0；函数文档刷新；结构体清单 531/297；Debug x64 构建 0 错误；self-check 异常日志不增长 |
| `结构_自检基础信息预检结果` | 预检目标有用，结构体本身只是返回视图 | 可替代 | 基础信息预检闸门 -> bool；摘要 -> 日志/重复记录去重文本 | 结构体含 `std::string 摘要`，只在自检线程内部返回，不写入项目结构 | 原引用只在 `自检线程模块.impl.cpp` 内部，删除后源码引用为 0 | 已分解后删除 | 删除结构体；预检函数返回 bool，摘要通过输出参数返回 | `rg` 源码引用 0；函数文档刷新；结构体清单 530/296；Debug x64 构建 0 错误；self-check 异常日志不增长 |
| `结构_重复目标诊断` | 控制面板重复目标诊断目标有用，结构体本身只是局部显示/计数视图 | 可替代 | 重复目标组数/需求数 -> 当前函数局部计数；样本主键 -> 运行日志展示文本；不作为需求树状态证据 | 结构体只保存 `std::size_t` 和 `std::vector<std::string>`，只在控制面板快照构造函数内部聚合，不写入项目结构，也没有外部读取 | 原引用只在 `控制面板类.cpp` 内部局部块；删除后源码引用为 0 | 已分解后删除 | 删除局部结构体；用 `std::unordered_map<std::string, std::size_t>` 计数，用 `std::unordered_map<std::string, std::vector<std::string>>` 保存样本 | `rg` 源码引用 0；函数文档刷新；结构体清单 529/295；Debug x64 构建 0 错误 |
| `结构_二次特征索引追加阶段耗时` | 慢路径耗时诊断目标有用，结构体本身只是内部日志视图 | 可替代 | 二次特征绑定场景写入 -> `场景节点主信息类::关系索引` / `场景节点主信息类::二次特征索引`；阶段耗时只进运行日志 | 结构体只保存阶段耗时和查重 bool，不写入项目结构，也不作为状态或动态证据 | 原引用只在 `二次特征类.cpp` 内部 helper 和 `二次特征类::绑定场景`；删除后源码引用为 0 | 已分解后删除 | 删除结构体；`私有_追加二次特征索引` 改为显式输出各阶段耗时和查重结果引用；调用点使用局部变量保持原日志字段 | `rg` 源码引用 0；函数文档刷新；结构体清单 528/295；Debug x64 构建 0 错误 |
| `结构_线程状态页统计` | 控制面板线程状态页统计目标有用，结构体本身只是页面计数视图 | 可替代 | 线程状态来源 -> 控制面板线程信息表 / 消息中间件 / 快照字段；页面统计 -> 局部计数和展示文本 | 结构体只保存 5 个 `std::size_t`，不写入项目结构，不作为任务、需求、世界事实、动作动态或结算证据 | 原引用只在 `控制面板类.cpp` 的线程状态页构建和 HTML 摘要；删除后源码引用为 0 | 已分解后删除 | 删除结构体；计入/计算函数改为显式引用输出 5 个局部计数；统计文本函数接收 5 个计数 | `rg` 源码引用 0；函数文档刷新；结构体清单 527/295；Debug x64 构建 0 错误 |
| `结构_需求列表分页结果` | 控制面板需求列表分页目标有用，结构体本身只是页面返回视图 | 可替代 | 需求事实 -> 需求树节点；分页显示 -> 局部节点集、更多标志和遍历计数 | 结构体只在控制面板分页读取链内部返回，不写入项目结构，也不作为需求状态、任务状态、动作动态或结算证据 | 原引用只在 `控制面板类.cpp` 的第一页、页面刷新和继续加载调用点；删除后源码引用为 0 | 已分解后删除 | 删除结构体；`私有_读取需求列表分页` 改为显式输出节点集、还有更多、已遍历数量 | `rg` 源码引用 0；函数文档刷新；结构体清单 526/295；Debug x64 构建 0 错误 |
| `结构_需求满足情况_控制面板` | 控制面板需求满足展示目标有用，结构体本身只是显示评估视图 | 可替代 | 需求事实 -> 需求节点当前状态 / 目标状态 / 满足关系；控制面板展示 -> 显示态枚举和局部计数 | 结构体含 `std::string 满足情况`，且原递归用文本判断子需求满足显示态；不写入项目结构，也不作为结算证据 | 原引用只在 `控制面板类.cpp` 需求详情构建链；删除后源码引用为 0 | 已分解后删除 | 删除结构体；新增控制面板私有显示态枚举；评估函数显式输出比较状态和计数，递归判断改用枚举 | `rg` 源码引用 0；函数文档刷新；结构体清单 525/294；Debug x64 构建 0 错误 |
| `结构_因果路径派生消息草案统计` | 因果路径派生需求消息草案统计日志目标有用，结构体本身只是函数内部计数包 | 可替代 | 派生需求正式承接 -> `输出正式消息集` 中的 `结构_任务管理派生需求消息`；字段完整性 -> `派生消息字段完整_筹办查询`；日志统计 -> 局部计数变量 | 结构体只保存 `bool` / `std::size_t` 统计，不写入项目结构，不作为需求树事实、动作动态或结算证据 | 原返回值调用方 `(void)` 丢弃；删除后源码结构体引用为 0，正式消息输出参数保持 | 已分解后删除 | 删除结构体；统计函数改为 `void` 和局部计数变量，保留 `输出正式消息集` 写入条件 | `rg` 源码结构体引用 0；函数文档刷新，函数条目 7308；结构体清单 524/294；Debug x64 构建 exit_code=0 |
| `结构_因果路径查询壳统计` | 因果路径查询统计日志目标有用，结构体本身只是函数内部统计返回视图 | 可替代 | 派生需求正式承接 -> `结构_因果路径筹办入口壳结果::派生需求消息集`；查询统计 -> 局部计数变量；因果依据 -> 世界树因果和需求目标比较 | 结构体只保存 `bool` / `std::size_t` 查询统计和一个临时消息集合，不写入项目结构，不作为状态 / 动态证据 | 原调用方只读取 `派生需求消息集`；删除后源码结构体引用为 0，消息集合改由输出参数直接写入入口壳结果 | 已分解后删除 | 删除结构体；`执行因果路径查询壳` 改为 `void` + 输出参数；统计字段改为局部计数变量 | `rg` 源码结构体引用 0；函数文档刷新，函数条目 7307；结构体清单 523/294；Debug x64 构建 exit_code=0 |
| `结构_直接子需求聚合视图_筹办` | 直接子需求聚合判断目标有用，结构体本身只是函数内部临时视图 | 可替代 | 子需求满足判断 -> 来源需求节点的直接子需求链和需求主信息状态；筹办结果 -> `结构_任务筹办结果::回执`；说明主键 -> 日志 / 摘要文本 | 结构体含 `std::string 首个未满足子需求主键`，但该文本只进说明 / 日志；结构体不写入项目结构，不作为需求或任务状态证据 | 原引用只在 `任务模块.筹办.ixx` 内部；删除后源码结构体引用为 0，回执字段写入保持 | 已分解后删除 | 删除结构体；`分析直接子需求聚合视图_筹办` 改为 `统计直接子需求聚合_筹办` 输出局部计数和日志主键 | `rg` 源码结构体引用 0；函数文档刷新，函数条目 7304；结构体清单 522/293；Debug x64 构建 exit_code=0 |
| `结构_任务节点统计` | 自我实现检查任务树日志计数有用，结构体本身只是内部计数包 | 可替代 | 任务事实 -> 世界树任务根节点和任务节点主信息；日志计数 -> 局部 `std::size_t` | 结构体只保存头结点数和步骤节点数，不写入项目结构，不作为任务状态、需求状态、动作动态或结算证据 | 原引用只在 `鱼巢.cpp` 的统计函数和自我实现检查日志；删除后源码结构体引用为 0 | 已分解后删除 | 删除结构体；`私有_统计任务节点` 改为引用输出两个局部计数 | `rg` 源码结构体引用 0；函数文档刷新，函数条目 7304；结构体清单 521/293；Debug x64 构建 exit_code=0 |
| `结构_消息中间件维护统计` | 消息中间件维护日志计数有用，结构体本身只是内部维护计数包 | 可替代 | 线程生命周期消息事实 -> 消息文件 / 已消费目录 / 归档目录；维护计数 -> 局部 `std::size_t` | 结构体只保存维护计数，不写入项目结构，不作为线程消息状态、任务状态、动作动态或结算证据 | 原引用只在 `线程生命周期消息模块.ixx` 维护链内部；删除后源码结构体引用为 0 | 已分解后删除 | 删除结构体；删除文件和三个维护目录 helper 改为引用输出局部计数 | `rg` 源码结构体引用 0；函数文档刷新，函数条目 7304；结构体清单 520/293；Debug x64 构建 exit_code=0 |
| `结构_任务调度消费统计` | 任务调度消费快照计数有用，结构体本身只是局部统计 / 未读聚合视图 | 可替代 | 工作结果事实 -> `结构_任务界面承接结果` 的推进事件和上行消息集；快照计数 -> 局部 `std::uint64_t` 后累计到界面线程快照 | 结构体的事件 / 上行消息 vector 只写未读；结构体不写入项目结构，不作为任务状态、动作动态或上行消息证据 | 原引用只在 `任务模块.管理界面线程.impl.cpp` 内部；删除后源码结构体引用为 0，真实上行仍由承接结果路径推入 | 已分解后删除 | 删除结构体和只写未读的 `私有_累计工作结果消息`；归并 / 消费函数改为引用输出局部计数 | `rg` 源码结构体引用 0；函数文档刷新，函数条目 7304；结构体清单 519/293；Debug x64 构建 exit_code=0 |
| `结构_需求列表刷新决策` | 未用需求列表刷新返回视图无当前有效用途 | 可替代 | 当前无有效调用路径；真实需求树刷新由消息处理结果和需求树变更路径直接驱动 | 结构体只保存两个 bool，且引用函数无源码调用点 | 原引用只在 `自我线程模块.消息处理器.ixx` 的未调用函数和导出声明；删除后源码引用为 0 | 已删除 | 删除结构体、未调用函数 `判定需求列表刷新决策` 和对应导出 | `rg` 源码引用 0；函数文档刷新，函数条目 7303；结构体清单 518/293；Debug x64 构建 exit_code=0 |
| `结构_治理消息轨迹` | 治理消息重放分代数值有用，单字段结构体本身无不可替代价值 | 可替代 | 治理消息重放分代 -> `结构_治理消息::重放代数` 数值字段 | 结构体只包装一个 `std::uint32_t`，不独立传递，不写入项目结构，不作为状态或动态证据 | 原引用只在 `结构_治理消息` 嵌入和 `投递治理消息` 幂等对账读取；删除后旧结构引用为 0 | 已分解后删除 | 删除结构体和导出；`结构_治理消息` 直接保存 `重放代数`，对账读取直接字段 | `rg` 旧结构和旧字段路径引用 0；函数文档刷新，函数条目 7303；结构体清单 517/293；Debug x64 构建 exit_code=0 |
| `结构_结果回流提示段` | 未用结果回流提示布尔段无当前有效用途 | 可替代 | 结果回流事实 -> `结构_任务结果回流消息` 的任务主键、最终结果状态、结果值和关键中间变化集 | 结构体四个 bool 无源码读写点，不写入项目结构，不作为结果回流事实或动作动态证据 | 原引用只在 `自我线程模块.消息协议.ixx` 的嵌入字段和导出声明；删除后源码引用为 0 | 已删除 | 删除结构体、`结构_任务结果回流消息::提示段` 和对应导出 | `rg` 源码引用 0；函数文档刷新，函数条目 7303；结构体清单 516/293；Debug x64 构建 exit_code=0 |
| `结构_匹配结果` / `二次特征匹配结果_桥接` | 匹配统计目标当前无有效用途 | 可替代 | 无。两个结构体没有写入/读取路径，未进入二次特征节点、状态或动态证据链 | 只保存命中数、摘要数、目标数、精确率、召回率、得分等统计字段，且无源码引用 | 原引用仅为两个结构体定义；删除后源码引用为 0 | 已删除 | 删除两个未引用结构体定义；不改 `二次特征_满足` 和桥接满足函数 | `rg` 源码引用 0；函数文档刷新，函数条目 7303；结构体清单 514/291；Debug x64 构建 exit_code=0 |
| `结构_治理实例堆快照` | 治理实例堆快照当前无有效用途 | 可替代 | 无。结构体没有写入/读取路径，未进入任务状态、治理恢复状态或动作动态证据链 | 只保存总数、最近摘要和实例列表，且无源码引用 | 原引用仅为结构体定义；删除后源码引用为 0 | 已删除 | 删除未引用结构体定义；不改 `结构_任务管理结果`、治理恢复事件镜像和治理恢复快照 | `rg` 源码引用 0；函数文档刷新，函数条目 7303；结构体清单 513/290；Debug x64 构建 exit_code=0 |
| `结构_二次特征比较语义` | 二次特征比较语义临时返回目标有用，结构体本身无独立承载价值 | 可替代 | 比较依据 -> 二次特征主信息 / 语素入口主键 / 比较结果编码；比较过程使用局部变量和 bool 有效性返回 | 结构体只在 `方法类.cpp` 内部函数返回后立即读取，未写入项目结构，不作为方法能力、状态或动态证据 | 原引用只在 `私有_二次特征比较语义` 和 `私有_二次特征比较语义相同` 内部；删除后源码结构体引用为 0 | 已分解后删除 | 删除结构体；`私有_二次特征比较语义` 改为 bool 返回和输出参数，调用方用局部变量比较 | `rg` 源码结构体引用 0；函数文档刷新，函数条目 7302；结构体清单 512/289；Debug x64 构建 exit_code=0 |
| `结构体_本能回执最小字段` | 本能回执最小字段当前无有效用途 | 可替代 | 无。结构体没有写入/读取路径，未进入运行存在、输出结果场景、状态或动作动态证据链 | 只保存成功、错误码、诊断、耗时和系统时间，且无源码引用；错误码/诊断壳不能替代本能函数闭环事实 | 原引用仅为结构体定义；删除后源码引用为 0 | 已删除 | 删除未引用结构体定义；不改场景模式匹配结构和本能动作函数签名 | `rg` 源码引用 0；函数文档刷新，函数条目 7302；结构体清单 511/288；Debug x64 构建 exit_code=0 |
| `结构_任务执行前许可请求` | 执行前许可请求目标有用，但该请求结构体未接入 | 可替代 | 实际许可请求语义 -> 治理消息语义键和自我线程审批函数；许可结果 -> `结构_任务执行前许可结果` | 结构体未被构造、传参、返回或嵌入，不写入任务状态、执行许可结果或动作动态证据 | 原引用仅为结构体定义；删除后源码结构体引用为 0；实际审批函数仍存在 | 已删除 | 删除未接入请求结构；保留许可结果结构和自我线程审批逻辑 | `rg` 源码结构体引用 0；函数文档刷新，函数条目 7302；结构体清单 510/287；Debug x64 构建 exit_code=0 |
| `结构_方法上下文` | 方法上下文当前无有效用途 | 可替代 | 无。结构体没有写入/读取路径，未进入任务执行结果、输出结果场景、状态或动作动态证据链 | 只保存时间、场景指针和方法首节点指针，且无源码引用 | 原引用仅为结构体定义；删除后源码引用为 0 | 已删除 | 删除未接入执行上下文结构；保留任务步骤执行结果和执行任务结果结构 | `rg` 源码引用 0；函数文档刷新，函数条目 7302；结构体清单 509/287；Debug x64 构建 exit_code=0 |
| `结构_任务管理显式事件控制` / `结构_任务管理最小原语签名` / `结构_任务管理正式原语资产概览` | 显式事件控制、最小原语签名和正式原语资产概览当前无有效用途 | 可替代 | 无。三个结构体没有写入/读取路径，未进入任务治理请求、决策、结果、缺口承接或动作动态证据链 | 只保存控制摘要、原语签名文本和资产概览字段，且无源码引用 | 原引用仅为三个结构体定义；删除后源码引用为 0 | 已删除 | 删除三个未接入治理壳；保留控制承接记录、本能能力缺口面、任务管理上下文等承载结构 | `rg` 源码引用 0；函数文档刷新，函数条目 7302；结构体清单 506/284；Debug x64 构建 exit_code=0 |
| `结构_任务推进裁决` | 任务推进裁决目标有用，结构体本身只是内部返回视图 | 可替代 | 工作类型 / 调度事件类型 -> 枚举局部变量；原因文本 -> 日志、摘要和入队原因 | 结构体只在 `私有_裁决任务推进` 返回后由同文件调用点读取，未写入项目结构，不作为任务状态或动作动态证据 | 原引用只在内部返回和调用点；删除后源码结构体引用为 0 | 已分解后删除 | 删除结构体；`私有_裁决任务推进` 改为 `void` 和引用输出参数 | `rg` 源码结构体引用 0；函数文档刷新，函数条目 7302；结构体清单 505/283；Debug x64 构建 exit_code=0 |
| `结构体_场景模式匹配绑定` | 场景模式匹配过程需要记录已选输入节点，但该结构体本身只是内部元素视图 | 可替代 | 已选输入节点 -> 私有递归函数局部 `std::vector<基础信息节点类*>` 输入节点栈；匹配结果对外仍只暴露匹配成功、总分、歧义和未满足模式节点 | `模式节点` 和 `匹配分` 保存后没有外部读取；结构体不写入项目结构，不作为输入参数场景、条件场景、状态或动态证据 | 原引用仅为结构体定义和 `结构体_场景模式匹配结果::绑定表` 字段；删除后源码结构体引用为 0，`.绑定表` 引用为 0 | 已分解后删除 | 删除结构体和结果中的绑定表字段；递归匹配函数显式传递局部输入节点栈，OR 分支复制局部栈保持择优回滚语义 | `rg` 源码结构体引用 0；`.绑定表` 引用 0；函数文档刷新，函数条目 7302；结构体清单 504/283/301；Debug x64 构建 exit_code=0 |
| `结构_线程消息输入` / `结构_线程控制消费` / `结构_线程状态回报` / `结构_线程结果上行` / `结构_线程消息处理合同` | 线程消息合同目标当前无有效用途 | 可替代 | 无。五个结构体未进入工作线程响应、推进事件、任务管理上行消息、线程状态或动作动态证据链 | 仅定义在 `任务线程消息合同` 命名空间并通过 export using 导出，没有构造、传参、返回或读取路径 | 原引用仅为定义、内部嵌入和 export using；删除后五个结构体和命名空间源码引用为 0 | 已删除 | 删除未接入合同命名空间、五个结构体和对应 export using；保留 `结构_任务推进事件` 和语义槽同步函数 | `rg` 源码引用 0；函数文档刷新，函数条目 7302；结构体清单 499/283/301；Debug x64 构建 exit_code=0 |
| `结构_需求方法可用性评估` | 控制面板需求方法可用性显示目标有用，结构体本身只是内部返回视图 | 可替代 | 候选方法事实 -> 方法树和方法可用性特征读取；控制面板展示 -> 局部候选方法列表、局部计数和显示状态枚举 | 结构体只在控制面板内部返回并被两个显示路径读取，不写入需求、方法、状态、动态、场景或结算证据 | 原引用只在 `控制面板类.cpp` 的评估函数和两个显示调用点；删除后源码结构体引用为 0 | 已分解后删除 | 删除结构体；`私有_评估需求方法可用性_控制面板` 改为引用输出目标特征、候选列表、计数、满足标志、方向和显示状态 | 旧结构和旧字段访问引用 0；函数文档刷新，函数条目 7302；结构体清单 498/283/300；Debug x64 构建 exit_code=0 |
| `结构_任务管理控制响应记录` | 任务管理控制响应记录当前无有效用途 | 可替代 | 控制响应状态 -> `结构_任务管理线程状态消息::当前控制响应状态` / 治理协议当前响应状态；显示文本 -> `任务管理控制响应状态文本` | 结构体只保存请求主键、执行尝试主键、响应状态、说明等字段，未被构造、传参、返回或嵌入 | 原引用仅为结构体定义；删除后源码引用为 0 | 已删除 | 删除未接入响应记录壳；保留控制请求、控制响应状态枚举和线程状态消息中的当前控制响应状态 | `rg` 源码引用 0；函数文档刷新，函数条目 7302；结构体清单 497/282/299；Debug x64 构建 exit_code=0 |
| `结构_评估包` / `枚举_评估阶段` | 当前无有效用途 | 可替代 | 无。结构体没有写入/读取路径，未进入状态、动态、特征、方法、需求或结算证据链 | `结构_评估包` 只保存 `坏因` / `缺口` 文本集合和计数字段，且没有源码引用；`枚举_评估阶段` 只服务该未接入壳 | 原引用仅为 `基础数据类型.h` / `基础数据类型.ixx` 定义；删除后源码引用为 0 | 已删除 | 删除 `基础数据类型.h` / `基础数据类型.ixx` 中未接入评估包和阶段枚举；不改抽象特征组合规则、特征使用事件、特征暴露事件和特征使用边 | `rg` 源码引用 0；函数文档刷新，函数条目 7302；结构体清单 496/281/299；Debug x64 构建 exit_code=0 |
| `结构_匹配配置` / `二次特征匹配配置_桥接` | 二次特征匹配默认开关目标有用，两个结构体本身只是参数包 | 可替代 | 匹配策略 -> `二次特征_满足` / `二次特征满足_桥接` 的三个显式 bool 参数；默认策略保持不变 | 两个结构体只保存 `使用对象绑定`、`条件空指针视为通配`、`允许用概念名称弱匹配` 三个布尔开关，且没有外部构造或项目结构写入 | 原引用只在 `二次特征应用模块.ixx` 内部函数签名和桥接转换；删除后源码结构体引用为 0 | 已分解后删除 | 删除两个配置结构体；`二次特征_满足` 和两个桥接重载改为显式 bool 默认参数 | `rg` 源码结构体引用 0；函数文档刷新，函数条目 7302；结构体清单 494/279/299；Debug x64 构建 exit_code=0 |
| `二次特征生成配置` | 动态二次特征默认生成策略目标有用，结构体本身只是未接入参数包 | 可替代 | 默认生成策略 -> `私有_获取动态二次特征` 内部显式默认常量；生成结果仍写入二次特征节点和因果证据动态 | 结构体只保存生成变化量、生成方向、持续时间、因果模板匹配、回写因果证据动态和生成版本，未发现外部传入非默认配置 | 原引用只在 `二次特征应用模块.ixx` 类方法声明、私有函数参数和方法定义；删除后源码引用为 0 | 已分解后删除 | 删除结构体和配置参数；内部用 `constexpr` 默认常量保持原默认行为，`动态类.cpp` 仍调用无配置参数入口 | `rg` 源码结构体引用 0；函数文档刷新，函数条目 7302；结构体清单 493/279/299；Debug x64 构建 exit_code=0 |
| `结构_SQL控制面板查询` | SQL 控制面板批量查询目标有用，结构体本身只是初始化表项 | 可替代 | 查询表项 -> `std::tuple<std::string_view, std::string_view, SQL控制面板目标行集>`；真实数据仍写入 `结构_SQL控制面板数据` | 结构体只保存名称、SQL 和目标行集成员指针，不写入项目结构，不作为 SQL 投影事实、状态或动作动态证据 | 原引用只在 `控制面板类.cpp::私有_读取SQL控制面板数据` 内部定义和遍历；删除后源码引用为 0 | 已分解后删除 | 删除结构体；新增成员指针别名，查询集改为标准 tuple，循环结构化绑定读取名称/SQL/目标行集 | `rg` 源码结构体引用 0；函数文档刷新，函数条目 7302；结构体清单 492/278/299；Debug x64 构建 exit_code=0 |
| `结构_待记录满足项` | 需求满足追踪日志目标有用，结构体本身只是局部缓存视图 | 可替代 | 需求满足事实 -> 需求节点和生命周期判断函数；日志追踪材料 -> 三个局部 vector 临时缓存 | 结构体只在 `记录需求满足项追踪_筹办` 内部缓存需求指针、上下文和判断结果，最终只用于 `记录筹办日志` 文本 | 原引用只在局部定义、vector 和记录循环；删除后源码引用为 0 | 已分解后删除 | 删除局部结构体；改用 `已满足需求`、`已满足上下文`、`已满足判断` 三个局部 vector 按索引记录 | `rg` 源码结构体引用 0；函数文档刷新，函数条目 7302；结构体清单 491/278/298；Debug x64 构建 exit_code=0 |
| `结构_因果搜索输入上下文_筹办` | 来源因果搜索方向补写目标有用，结构体本身只是两字段返回视图 | 可替代 | 来源因果是否可用和固定证据方向 -> 调用点局部 `bool` / `I64`；实际补写仍进入输入条件包特征 | 结构体只保存 `有效` 和 `证据方向`，构造函数只在来源因果存在时返回固定负向证据方向 | 原引用只在 `任务模块.筹办.ixx` 内部结构定义、构造函数和一个调用点；删除后结构体和构造函数源码引用为 0 | 已分解后删除 | 删除结构体和构造函数；调用点直接用 `来源因果可用于搜索` 和 `来源因果证据方向` | `rg` 源码结构体/函数引用 0；函数文档刷新，函数条目 7301；结构体清单 490/278/297；Debug x64 构建 exit_code=0 |
| `结构_必需实参检查结果` / `结构_练习目标实参检查结果` | 候选方法实参检查目标有用，结构体本身只是函数返回视图 | 可替代 | 实参缺口事实 -> `结构_候选方法输入闭合结果`；检查中间值 -> 局部计数和首个缺失特征输出参数 | 两个结构体只在 `任务模块.筹办.ixx` 内部传递计数和首个缺失参数特征，未写入项目结构或动作动态 | 原引用只在两个检查函数和一个通用绑定调用链；删除后结构体和旧字段访问引用为 0 | 已分解后删除 | 两个检查函数改为 bool 返回，通过引用输出计数和首个缺失特征；调用点改用局部变量 | `rg` 源码结构体/旧字段访问引用 0；函数文档刷新，函数条目 7299；结构体清单 488/278/295；Debug x64 构建 exit_code=0 |
| `结构_界面优先级结果` | 界面优先级输出目标当前无有效用途 | 可替代 | 无。结构体没有调用路径，未进入任务调度、工作线程快照、控制面板或动作动态证据链 | 结构体只由未接入函数 `计算任务管理界面优先级结果` 输出；该函数源码无调用点 | 原引用仅为结构体定义、函数声明和函数定义；删除后结构体和函数源码引用为 0 | 已删除 | 删除结构体、函数声明和函数定义；保留工作线程实例快照、壳状态和真实推进路径 | `rg` 源码结构体/函数引用 0；函数文档刷新，函数条目 7298；结构体清单 487/278/294；Debug x64 构建 exit_code=0 |
| `结构_工作项排序键` | 工作项排序目标有用，结构体本身只是临时返回视图 | 可替代 | 排序依据 -> `结构_任务工作项` 字段、任务调度优先级读取和现有排序 helper；调度队列仍由 `结构_任务调度队列项` 承载 | 结构体含 `std::string 任务排序键`，只由 `私有_计算工作项排序键` 返回后在比较函数内拆字段，不写入调度队列、任务状态、线程快照或动作动态 | 原引用仅在同文件结构体、计算 helper 和比较函数内部；删除后结构体和 helper 源码引用为 0，`私有_工作项排序在前` 与重排调用仍存在 | 已分解后删除 | 删除结构体和 `私有_计算工作项排序键`；比较函数直接读取左右工作项排序依据并按原顺序比较 | `rg` 源码结构体/helper 引用 0；函数文档刷新，目标 helper 说明书引用 0，函数条目 7298；结构体清单 486/277/293；Debug x64 构建 exit_code=0 |
| `结构_同目标任务复用结果_任务管理` | 同目标复用 / 目标升级查找目标有用，结构体本身只是临时返回视图 | 可替代 | 复用任务 / 待升级任务 -> 任务节点指针局部变量；升级仍走任务状态提交链；说明文本只用于日志和承接说明 | 结构体只由 `私有_查找可复用同目标任务` 返回后一个调用点拆字段，不写入任务状态、调度队列、线程快照、动作动态或世界树 | 原引用仅在同文件结构体、查找函数返回和调用点；删除后结构体和旧字段访问引用为 0，查找函数与调用点仍存在 | 已分解后删除 | 删除结构体；查找函数改为 bool 返回直接复用命中，通过输出参数返回复用任务、复用说明、待升级任务和升级说明 | `rg` 源码结构体/旧字段访问引用 0；函数文档刷新，函数条目 7298；结构体清单 485/276/292；Debug x64 构建 exit_code=0 |
| `结构_扫描子特征动作动态统计` | 扫描子特征动作动态计数目标有用，结构体本身只是两个计数包 | 可替代 | 动作动态事实 -> `创建扫描I64子特征动作动态` 生成的动态节点和 `扫描低层动态集合`；数量 -> 局部 I64 计数并写入扫描运行宿主特征 | 结构体只保存 `动态数量` / `因果数量` 两个 I64，不保存动态节点或因果节点，也不作为状态事实 | 原引用仅在结构体定义、追加函数输出参数和一个调用点；删除后结构体和旧字段访问引用为 0，动态创建和集合写入仍存在 | 已分解后删除 | 删除结构体；`追加扫描子特征动作动态` 改为两个 I64 引用输出计数，调用点用局部变量计算预计数量和总数 | `rg` 源码结构体/旧字段访问引用 0；函数文档刷新，函数条目 7298；结构体清单 484/276/291；Debug x64 构建 exit_code=0 |
| `结构_扫描子特征状态项` | 扫描子特征动作动态参数目标有用，结构体本身只是内部参数包 | 可替代 | 子特征变化事实 -> 显式特征类型指针和 I64 初始 / 结果值参数；动作动态事实仍由 `创建扫描I64子特征动作动态` 生成的动态节点承载 | 结构体只保存特征类型、初始值和结果值，调用点立即构造临时对象，不保存动态节点、因果节点或状态事实 | 原引用仅在结构体定义、追加函数参数和一个调用点；删除后源码引用为 0，动态创建和集合写入仍存在 | 已分解后删除 | 删除结构体；`追加扫描子特征动作动态` 改为显式接收特征类型、初始值和结果值，调用点直接转发参数 | `rg` 源码结构体引用 0；函数文档刷新，函数条目 7298；结构体清单 483/276/290；Debug x64 构建 exit_code=0 |
| `结构_扫描特征绑定` / `结构_扫描存在类型绑定` | 扫描语素绑定表目标有用，两个结构体本身只是初始化表项 | 可替代 | 语素入口名称种子 -> `扫描特征绑定名称` / `扫描存在类型绑定名称` 返回后由 `语素集.添加信息入口词` 注册；机器判断仍使用枚举 ID 和语素入口指针 | 两个结构体只保存枚举 ID 和 `const char* 名称`，不写入状态、动态、场景、方法或需求；文本只作为语素入口名称种子 | 原引用仅在结构体定义、两个 constexpr 表和两个名称读取函数；删除后两个结构体源码引用为 0，绑定表和名称函数仍存在 | 已分解后删除 | 删除两个结构体；绑定表项改为 `std::pair<枚举ID, const char*>`，名称读取改为 `first` / `second` | `rg` 两结构体源码引用 0；函数文档刷新，函数条目 7298；结构体清单 481/274/290；Debug x64 构建 exit_code=0 |
| `子任务服务摘要输入` / `父任务组合汇总输入` / `子任务服务摘要结果` / `OR胜出路径记录输入` | 对应写回目标有用，但当前函数组未接入 | 可替代 | 当前无调用路径；正式服务价值反馈仍由 `结构_任务价值反馈段`、服务值结算 D2 动作动态和任务完成反馈链承担 | 4 个结构体只服务未调用函数组；函数组虽然写项目特征，但没有任务价值反馈链调用，不能作为当前闭环证据 | 全源码只命中定义和函数内部引用；删除后 4 个结构体、3 个写回函数和 `节点指针键` 引用为 0；正式任务价值反馈段和服务值结算动作动态仍存在 | 已删除 | 删除 4 个结构体、`记录子任务服务摘要`、`回写父任务组合汇总`、`记录OR胜出路径`、`节点指针键` | `rg` 目标符号引用 0；函数文档刷新，函数条目 7297；结构体清单 477/272/286；Debug x64 构建 exit_code=0 |

## 第四批：单体暂停候选复核

| 结构体名 | 是否有用 | 是否不可替代 | 项目结构映射 | 不合规点 | 外部读取证据 | 结论 | 最小修改切片 | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `度量模板注册表类.ixx::结构_模板参数` | 有真实用途；作为度量模板虚接口参数对象，被特征度量模板用于校验特征类型主键并生成特征度量链键 | 当前不宜在本窗口直接替代；可替代方向是把虚接口参数改为结构化特征类型身份或直接参数，但会触碰注册表 `virtual / override` 边界和下游度量签名体系 | 当前：特征类型身份 -> `结构_模板参数::特征类型主键` / `二次特征主信息类::度量签名_链键` 字符串链键；规范目标：特征类型身份应由 `语素入口节点类*`、特征类型节点或项目内部身份结构承载，链键只可作为显示 / 兼容索引候选 | `特征类型主键` 和 `度量签名_链键` 仍以 `std::string` 参与模板匹配、校验和签名查找，不符合 007 规范第 2 章对机器逻辑身份承载的长期要求 | `结构_模板参数` 在 `度量模板注册表类.ixx` 虚接口和多个 override 中读取；`度量模板注册表桥接.cpp` 通过 `获取度量默认刻度_由链键` 进入；`二次特征类.cpp::私有_确保默认概念刻度` 读取默认刻度并写入二次特征概念刻度 | 保留为后续修改候选；暂停删除 | 后续单独做“度量签名结构化”切片：先统一 `度量签名_链键` / 特征类型身份的项目结构承载，再改注册表虚接口；本窗口不硬删 | 本轮仅做 `rg` 引用审计和判定登记；未改源码，未构建 |
| `控制面板类.cpp::结构_自我场景内容统计` | 有真实用途；用于统计自我场景直接层和子树中的存在、场景、特征、状态、动态、二次特征数量，并采集少量存在样例供控制面板展示 | 可被机械替换为多个局部计数、样例 vector 和输出参数，但替换会扩大参数面；该结构体当前是控制面板内部聚合视图，不是低风险删除优先项 | 场景真实事实 -> 自我所在场景 / 复现宿主子树节点；显示统计 -> `结构_自我场景内容统计` 临时聚合 -> `结构_控制面板快照` 的数值字段和摘要字段 -> JSON / HTML | 含 `std::vector<std::string> 存在样例` 和摘要文本，但用途是显示快照和页面摘要；未发现作为需求、任务、方法、状态、动态或结算机器判断依据 | 源码引用只在 `控制面板类.cpp` 内部；写入 `结构_控制面板快照` 的场景统计字段，随后由 `私有_自我场景复现JSON` 和页面渲染读取 | 保留；暂停删除 | 后续若做控制面板显示层瘦身，可单独评估改为局部计数输出；当前不改源码、不构建 | 本轮 `rg` 结构体名和字段路径；读取定义、累计、子树统计、快照写入、JSON/HTML 输出路径 |
| `状态类.h::结构_最近两次I64状态方向结果` | 当前无有效调用用途；仅作为 `状态类::比较最近两次I64状态方向` 的返回结构，源码内未发现调用方 | 可替代；若后续确需最近两次 I64 状态方向，可直接由调用方读取状态节点或用输出参数返回可比较标志、方向和值/时间 | 状态事实 -> 场景状态节点和 `状态节点主信息类::状态值 / 状态域 / 状态主体 / 状态特征`；方向计算 -> `获取状态二次特征(*上次, *最新)`；该结构体只是未接入 public 查询结果壳 | 结构体本身字段均为 bool / I64 / 时间戳，无文本承载问题；主要问题是未被调用，未进入状态、动态、特征、方法、需求或结算闭环 | `rg` 仅命中 `状态类.h` 定义 / public 声明和 `状态类.cpp` 函数定义 / 内部赋值；未发现源码调用点 | 后续删除候选；本窗口暂停删除 | 删除需同时删除 public 函数声明/定义，并刷新函数功能说明文档；因另一个窗口正在函数清理，本窗口只登记，不触碰源码 | 本轮 `rg` 引用审计；未改源码，未构建 |
| `自我动作实现.扫描.ixx::结构_扫描判断结果` | 判断目标有真实用途；`状态` 控制扫描数据项是否进入对应关系和写入链，并写入数据项处理记录的 `处理闭合证据状态` | 结构体本身可替代；当前只有 `状态` 被读取，`分值` 和 `原因` 没有读路径，可改为 `枚举_扫描判断状态` 返回值或输出参数 | 外设数据项可处理判断 -> `枚举_扫描判断状态` -> `扫描数据项处理记录` 子存在上的 `处理闭合证据状态` I64 特征；缺口原因另由 `记录缺口` 写入结构化缺口类型和状态字段 | `const char* 原因` 是文本字段且未写入项目结构；当前未参与机器判断，但属于无效残留字段；`分值` 也未被读取 | `rg` 命中结构体定义、`判断外设数据项可处理` 返回、主扫描循环读取 `判断.状态`、`记录数据项处理` 写 `处理闭合证据状态`；未发现 `判断.分值` / `判断.原因` 读取 | 后续修改 / 删除候选；本窗口暂停源码删除 | 后续单独小切片：删除 `分值` / `原因` 或直接删除结构体并让判断函数返回 `枚举_扫描判断状态`；需要复核扫描真实链日志后构建 | 本轮 `rg` 引用审计；未改源码，未构建 |
| `自我动作实现.扫描.ixx::结构_外设特征材料` | 有当前用途，但用途较窄；作为 `结构_已合法外设数据项::特征材料集合` 的元素，证明非已知存在类数据项携带了外设特征材料，并参与 `可回查` 合并 | 可替换；当前扫描链只读取材料集合是否为空和构造后 `可回查`，未读取该结构体中的具体 I64/F64/文本值、generation 或来源材料句柄明细 | 外设扫描报告 -> `结构_外设扫描提交包::当前特征值 / 特征变化值` -> `结构_外设特征材料` 临时镜像 -> `结构_已合法外设数据项` 合同检查；正式缺口仍由 `记录缺口` 写结构化缺口类型和状态 | 含 `std::string 当前文本值`、`特征值generation`、`来源材料句柄` 等外设材料文本元数据；当前没有作为机器判断字段直接读取，但作为未使用镜像字段会扩大结构体负担 | `rg` 命中定义、`构造外设特征材料`、`展开已合法外设数据项` 写入 `特征材料集合`、合同检查 `特征材料集合.empty()`、上游契约诊断日志材料数量；无跨文件读取 | 保留为后续瘦身候选；暂停删除 | 后续外设扫描材料专项：若只需要合同和回查状态，可把材料集合收束为材料数量 / 可回查 / 可比较；若需要真实特征值写入，则应改为项目特征值承载而非文本镜像 | 本轮 `rg` 引用审计并读取上游 `结构_外设特征值条目` / `结构_外设扫描提交包`；未改源码，未构建 |
| `自我动作实现.扫描.ixx::结构_已合法外设数据项` | 有真实用途；是外设扫描报告展开后的主处理材料，供合同检查、可处理判断、对应关系判断、覆盖索引记录、空间位置写入和缺口记录使用 | 当前不可直接替代；可拆分为合同字段、材料质量字段、已知存在空间字段和缺口字段，但拆分会触碰扫描真实链多个函数 | 外设扫描报告 / 扫描提交包 -> `结构_已合法外设数据项` -> `记录数据项处理` / `记录覆盖索引` / `写入场景当前特征值_空间位置` -> 运行存在子记录、目标存在空间坐标、动作动态和输出结果场景 | 含 `来源外设ID`、`来源观察存在ID`、`可能归属存在ID`、generation、坐标系、缺口原因等文本字段；其中部分参与合同或缺口判断，长期应转为外设身份、约束版本、材料句柄和缺口枚举等项目结构 | 源码引用覆盖 `展开已合法外设数据项`、`已合法外设数据项合同满足`、`判断外设数据项可处理`、`记录数据项处理`、`判断外设数据项对应关系`、`记录覆盖索引`、`写入场景当前特征值_空间位置` 和主扫描循环 | 保留；列为后续结构化改造候选 | 后续按字段族拆分：先把未读文本字段瘦身，再把仍参与机器判断的外设身份 / generation / 坐标系 / 缺口原因迁移为项目身份或枚举状态；当前不硬删 | 本轮 `rg` 引用审计并读取合同、对应、覆盖和写入路径；未改源码，未构建 |

## 第五批：2026-06-21 当前复核

| 结构体名 | 是否有用 | 是否不可替代 | 项目结构映射 | 不合规点 | 外部读取证据 | 结论 | 最小修改切片 | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `自我动作实现.扫描.ixx::结构_扫描关注集合` | 有真实用途；`应覆盖存在集合` 驱动外设数据项对应关系和未覆盖解释，`需补条件存在集合` 驱动缺空间位置缺口 | 当前不可整体替代；它是扫描运行中从当前场景子存在到运行宿主特征写入的内部材料视图 | 当前场景子存在 -> `结构_扫描关注集合::应覆盖存在集合 / 需补条件存在集合` -> `记录关注存在` / `记录缺口` / `判断外设数据项对应关系` -> 运行宿主 I64 特征、输出结果场景和扫描动作动态 | 原 `可忽略存在集合` vector 无写入方，只被读取 size 后写出 0 状态，属于死字段；其余两个 vector 承载真实扫描链 | `rg` 命中定义、`生成本轮扫描关注集合` 写入两个有效集合、`判断外设数据项对应关系` 读取应覆盖集合、主扫描循环写入数量和集合状态；无跨文件读取 | 已瘦身；保留结构体 | 删除无写入方字段 `可忽略存在集合`；`本轮可忽略存在数量` 显式保持 0，继续写入数量和集合状态特征 | `rg "可忽略存在集合|结构_扫描关注集合|本轮可忽略存在数量" 自我动作实现.扫描.ixx`；`日志/20260621_P10bc_扫描关注集合瘦身_build.log`，Debug x64 构建 exit_code=0 |
| `状态类.h::结构_最近两次I64状态方向结果` | 当前复核无源码命中，旧判定候选已过期 | 不适用 | 不适用；当前源代码已无该结构体、函数声明或函数定义命中 | 无当前源码对象可处理 | `rg "结构_最近两次I64状态方向结果|比较最近两次I64状态方向" -g "*.h" -g "*.cpp" -g "*.ixx"` 返回无命中 | 跳过；不作为当前候选 | 无代码修改 | 本轮仅复核，未构建 |
| `二次特征应用模块.ixx::结构_动态视图片段` | 有真实用途；用于把动态节点解析为状态迁移、主体、特征类型、特征值、方向和时间视图 | 当前不可替代；该结构体是二次特征动态生成、协同关系和动态聚合的内部规范化视图 | 动态节点 / 初始状态 / 结果状态 -> `结构_动态视图片段` -> 二次特征主信息模板、差值结果、来源动态层级 / 路径签名、连续同向段和同类事件段聚合 | 未发现自由文本业务字段；字段为项目节点指针、特征值、I64、方向编码和时间戳，符合当前内部视图承载 | `rg` 命中定义、`私有_解析动态视图片段`、`私有_标准化动态单元列表`、动态二次特征生成、协同关系和两类动态聚合链；无跨文件读取 | 保留；暂停删除 | 无代码修改 | 本轮 `rg` 引用审计和关键函数小段读取；未构建 |
| `双目相机本能适配器.cpp::深度空洞区域收集结果` | 深度空洞区域收集目标有用，但结构体本身只是单 helper 返回壳 | 可替代 | 原始场景帧深度空洞 -> 局部 `输出总数量` 和 `std::vector<诊断区域摘要>` -> `结构体_双目相机观察结果::深度空洞区域数量 / 诊断区域列表` | 结构体没有外部读取，不写入项目结构；只是把总数和区域列表打包返回给同文件一个调用点 | `rg` 仅命中结构体定义、`收集深度空洞诊断区域` 返回值和调用点；修改后 `深度空洞区域收集结果` 源码引用为 0 | 已分解后删除 | 删除结构体；`收集深度空洞诊断区域` 返回诊断区域 vector，并通过 `std::size_t&` 输出总数量 | `rg "深度空洞区域收集结果|收集深度空洞诊断区域" 双目相机本能适配器.cpp`；`日志/20260621_P10bd_深度空洞区域返回壳删除_build.log`，Debug x64 构建 exit_code=0 |
| `自我线程模块.impl.cpp::结构_治理影响项` | 当前无有效用途；结构体只有定义，无构造、写入或读取路径 | 可替代；配套两个 enum 也只服务该死结构 | 无项目结构映射；未进入存在 / 场景 / 特征 / 状态 / 动态 / 方法 / 需求闭环 | 含 `std::string 来源二次特征名称 / 成立条件摘要 / 影响说明`，且没有语素入口或项目结构转换路径 | 修改前 `rg` 只有结构体定义和只服务字段的 `枚举_治理影响维度 / 枚举_治理影响方向`；修改后三者源码命中 0 | 已删除 | 删除结构体和只服务它的两个 enum | `rg "结构_治理影响项|枚举_治理影响维度|枚举_治理影响方向" -g "*.h" -g "*.cpp" -g "*.ixx"`；`日志/20260621_P10be_治理影响项死结构删除_build.log`，Debug x64 构建 exit_code=0 |
| `基础数据类型.h::抓取位姿建议 / 点簇结果 / 观测提取参数` | 当前无有效用途；三个结构体均只有定义，无构造、写入或读取路径 | 可替代；`枚举_抓取类型` 只服务 `抓取位姿建议` | 无项目结构映射；未进入当前外设观察、D455 适配器、存在观测或世界树写入链 | `观测提取参数` 和 `抓取位姿建议` 是旧公共头文件壳；未形成当前项目结构闭环 | 修改前 `rg` 只命中定义；修改后 `枚举_抓取类型 / 抓取位姿建议 / 点簇结果 / 观测提取参数` 源码命中 0；`点簇边界框 / 点簇增强结果 / 帧处理结果` 保留 | 已删除 | 删除三个未接入结构体和只服务它的抓取类型 enum | `rg "枚举_抓取类型|抓取位姿建议|点簇结果|观测提取参数" -g "*.h" -g "*.cpp" -g "*.ixx"`；`日志/20260621_P10bf_基础数据未接入结构删除_build.log`，Debug x64 构建 exit_code=0 |
