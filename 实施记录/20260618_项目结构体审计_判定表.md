# 20260618 项目结构体审计判定表

## 第一批：任务管理消息协议

| 结构体名 | 是否有用 | 是否不可替代 | 项目结构映射 | 不合规点 | 外部读取证据 | 结论 | 最小修改切片 | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `结构_任务管理消息头` | 有用 | 当前不可直接替代 | 发出者/任务/场景文本主键 -> `安全解析或散列任务管理主键` -> 治理消息头数值主键 | 源协议仍使用文本主键；需要后续结构化改造，不能在本切片硬删 | `自我线程模块.消息协议.ixx:1590`、`:1746` 等构造治理消息路径读取 | 保留为后续修改候选 | 本切片只登记，不删除 | 后续专项改造时给出主键字段结构映射和构建验证 |
| `结构_任务管理未满足项` | 无独立用途 | 可替代 | 无。写入后没有落到项目结构，只有 `未满足项集.size()` 参与对账 | `未满足项类型` 和 `作用主体主键` 是文本字段；结构体只剩内部集合计数/对账视图 | `任务模块.管理工作线程.ixx:3185` 写入；`自我线程模块.消息协议.ixx:1229` 只比较 size | 删除 | 删除结构体、结果消息中的 `未满足项类型文本`/`未满足项集`、写入构造和 size 比较 | Debug x64 构建 |
| `结构_任务管理状态变化项` | 目标用途有用，但当前未接入写入链 | 不可直接替代，除非先设计真实状态变化输入来源 | 预期应为任务结果中的服务对象外在特征变化 -> 自我内部世界特征值 / 服务结算证据 | 未找到任务管理侧写入方；主体类型/特征类型是文本字段；落账推断函数当前忽略文本并返回默认 | `自我线程模块.impl.cpp:1130` 服务结算证据读取；`:13611` 落自我内部世界；写入搜索无 `push_back/emplace_back/insert` 命中 | 暂停确认 | 不能直接删除。需先补“状态变化来源 -> 项目结构字段 -> 服务结算证据”的设计 | 恢复后需构建，并跑触发任务结果回流的真实路径 |
| `结构_任务管理关键中间状态变化项` | 目标用途有用，但当前未接入写入链 | 不可直接替代，除非先设计关键中间状态输入来源 | 预期应为任务执行关键中间状态 -> 自我内部世界特征值 / 恢复判断 | 未找到任务管理侧写入方；动作主体/变化主体/特征类型为文本字段；落账推断函数当前忽略文本并返回默认 | `自我线程模块.impl.cpp:13654` 落自我内部世界；`:17347` 用执行成功/已恢复判断风险；写入搜索无 `push_back/emplace_back/insert` 命中 | 暂停确认 | 不能直接删除。需先补“关键中间状态来源 -> 项目结构字段 -> 恢复/风险判断”的设计 | 恢复后需构建，并跑触发关键中间状态回流的真实路径 |

## 当前不扩大范围

本轮只处理已经证明“只剩内部计数/视图”的 `结构_任务管理未满足项`。`结构_任务管理消息头`、`结构_任务管理状态变化项`、`结构_任务管理关键中间状态变化项` 和治理消息中的文本摘要 / 原因键属于同一协议结构化大切片，但仍有跨模块读取和落账路径，需另切片审计，不能在本次删除。
