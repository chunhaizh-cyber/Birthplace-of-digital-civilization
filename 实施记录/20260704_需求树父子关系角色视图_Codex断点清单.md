# 需求树父子关系角色视图 Codex 断点清单

## 0. 恢复摘要

当前切片：E3/P1 `需求树父子关系角色视图实现映射计划` S5 显示与回归已闭合。

当前判断：

```text
S0 已完成只读扫描。
S1 已完成第一版只读角色视图函数。
S2 已完成父需求结算接入。
S3 已完成父任务收束只读角色统计。
S4 已完成第一版可选路径收口。
S5 已完成控制面板只读显示与 CLI 回归。
当前代码已有 `需求类::读取父子关系角色视图(...)` 可只读推导“必须子需求 / 可选路径子需求 / 非必须子需求”。
父需求结算和活动直接子需求判断已读取该角色视图：未满足必须子需求阻塞父需求满足，非必须子需求不阻塞父需求满足；可选路径第一版按任一路径满足解除父侧可选组阻塞。
任务管理工作线程快照和控制面板快照已显示最近父任务收束角色分布；需求详情和需求树生长摘要已显示父子关系角色视图。
下一步应进入 E4/P6 `结构化信息缺口与需求派生承接实现映射计划` S0 只读扫描。
```

禁止宣称：

```text
不得宣称非必须子需求放弃 / 转挂链路已实现。
不得宣称真实或自然样本已覆盖 OR 组令牌领取和同组未领取路径收束。
不得宣称数据库查询投影已新增专门 SQL 字段；本切片完成的是控制面板 live 只读投影和 CLI 回归。
不得宣称自我苏醒完成或初步成熟完成。
```

## 1. 本切片依据

计划：

```text
计划/20260702_需求树父子关系角色视图实现映射计划_v0.1.md
```

详细设计：

```text
规范/详细设计/待完成/20260702_需求树父子关系角色视图详细设计_v0.1.md
```

上级规范限载回读：

```text
规范/0050_项目通用结构与禁止性规则总纲_20260701.md
规范/3100_根规范_需求主信息类_20260701.md
规范/3200_根规范_任务主信息类_20260701.md
规范/3300_根规范_方法主信息类_20260701.md
规范/1170_根规范_动态节点主信息类_20260701.md
规范/1190_根规范_因果主信息类_20260701.md
```

## 2. 已运行命令

```powershell
git status --short --untracked-files=no
Get-Content -LiteralPath '计划/计划索引.md' -TotalCount 140
Get-Content -LiteralPath '计划/20260702_需求树父子关系角色视图实现映射计划_v0.1.md' -TotalCount 220
Get-Content -LiteralPath '规范/详细设计/待完成/20260702_需求树父子关系角色视图详细设计_v0.1.md' -TotalCount 220
rg -n "结构_需求树更新指令|标记已满足|使用阻塞父任务执行|阻塞父任务执行|需求目标语义视图|子需求权重采用OR关系|构造OR组需求更新指令|构造方法路径组需求更新指令|收集直接子需求目标视图|查找直接子需求_按目标重叠|OR组结算令牌" 需求类.h 需求类.cpp
rg -n "结构_任务推进回执|父任务需要重筹办|统计直接子需求聚合_筹办|返回直接子需求聚合筹办结果|存在活动直接子需求|父需求继续策略|派生后父任务继续|是否阻塞当前步骤|是否影响父任务推进|需要创建或复用子需求" 任务模块.筹办.ixx
rg -n "存在活动直接子需求|需求目标语义可由直接子需求聚合|按成功结果满足来源需求并唤醒父任务|观察事实更新后触发来源需求重筹办|增强父任务重筹办|解析派生需求父需求|收到派生需求消息时完善需求树|构造派生需求入树拒绝回执" 自我线程模块.impl.cpp
rg -n "截止|取消活动|转挂|活动承接|放弃|删除需求指令|构造删除需求指令|已截止|切断" 需求类.h 需求类.cpp 自我线程模块.impl.cpp 任务模块.筹办.ixx 任务模块.管理工作线程.ixx
rg -n "父子关系角色|角色视图|必须子需求|可选路径子需求|非必须子需求|取消活动承接|转挂|父需求未满足必须|非必须活动子需求" 需求类.h 需求类.cpp 任务模块.筹办.ixx 任务模块.执行.ixx 任务模块.管理工作线程.ixx 任务模块.管理线程协议.ixx 任务模块.工作线程消息协议.ixx 说明书/函数功能说明文档.md
python .\tools\生成函数功能说明文档.py
git diff --check -- '需求类.h' '需求类.cpp' '说明书/函数功能说明文档.md'
python .\tools\check_specs.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
Get-Content -LiteralPath '.codex_locks/build-run.lock'
Get-Process -Id 32724 -ErrorAction SilentlyContinue
Remove-Item -LiteralPath '.codex_locks/build-run.lock'
git diff --stat -- '任务模块.筹办.ixx' '自我线程模块.impl.cpp' '说明书/函数功能说明文档.md'
git diff --check -- '任务模块.筹办.ixx' '自我线程模块.impl.cpp' '说明书/函数功能说明文档.md'
python .\tools\生成函数功能说明文档.py
python .\tools\check_specs.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

## 3. 代码事实扫描结果

已存在入口：

```text
需求类.h:66 定义 `结构_需求树更新指令`，承接需求树新增、满足、截止和阻塞父任务相关指令。
需求类.h:84-97 现有历史字段 `标记已满足`、`使用阻塞父任务执行`、`阻塞父任务执行`，其中满足事件语义已注释为本轮活动承接收束。
需求类.h:242 定义 `结构_需求目标语义视图`。
需求类.h:617 定义 `子需求权重采用OR关系`，依赖需求目标语义视图。
需求类.h:632 / 642 已有直接子需求按目标重叠查找和目标视图收集入口。
需求类.cpp:1244 / 1328 / 1397 / 2401 已有 OR 组结算令牌目标特征类型、语义识别和 OR 组需求更新指令。
任务模块.筹办.ixx:547 / 595 已有直接子需求聚合统计和筹办结果返回。
任务模块.筹办.ixx:989 已有活动直接子需求判断。
任务模块.筹办.ixx:1029-1037 已有父需求继续策略，当前只有继续 / 等待子需求。
任务模块.筹办.ixx:1296-1315 派生需求草案会把 `派生后父任务继续` 映射到阻塞当前步骤和影响父任务推进。
自我线程模块.impl.cpp:5764 / 5788 / 7819 / 8310 已有活动直接子需求、直接子需求聚合语义、成功结果唤醒父任务和观察事实更新触发来源需求重筹办连接点。
任务模块.管理线程协议.ixx 已有派生需求入树结果、拒绝阶段、拒绝原因、闭环类型和回执结构，可作为后续 D5 承接材料。
```

缺口：

```text
未发现统一的 `父子关系角色视图`、`必须子需求`、`可选路径子需求`、`非必须子需求` 读取函数或值域。
未发现可直接表达 `取消活动承接` 的独立入口；当前主要可用材料是需求有效截止和删除需求指令，其中删除指令不符合详细设计对正式入树需求的优先表达。
未发现 `转挂` 的正式通用入口；仅有派生需求父需求解析、自检候选父需求裁决、维护父需求等相邻治理入口。
现有 `是否阻塞父任务执行`、`是否阻塞当前步骤`、`是否影响父任务推进` 等布尔位是兼容 / 协议材料，不能直接扩张为三角色事实字段。
```

## 4. 下一步

## 4. S1 执行结果

已修改文件：

```text
需求类.h
需求类.cpp
说明书/函数功能说明文档.md
```

新增只读入口：

```text
需求类::枚举_父子需求关系角色
需求类::父子需求关系角色文本(...)
需求类::读取父子关系角色视图(...)
```

第一版推导规则：

```text
1. 父需求或子需求为空，或子需求不是父需求直接子节点 -> 未定义。
2. 子需求已截止，或不阻塞父任务执行 -> 非必须子需求。
3. 当前方法路径目标特征类型命中子需求目标特征类型 -> 必须子需求。
4. 父需求目标语义为 OR组 或 方法路径组 -> 可选路径子需求。
5. 其他活跃阻塞直接子需求 -> 必须子需求。
```

已验证：

```text
python .\tools\生成函数功能说明文档.py：成功，函数条目 7663。
git diff --check：无空白错误；函数说明文档出现既有换行提示，不影响检查退出码。
python .\tools\check_specs.py：hard issues = 0，warnings = 0。
Debug x64 构建：0 警告，0 错误。
```

未完成：

```text
S2 父需求结算尚未接入角色视图。
S3 父任务收束尚未接入角色视图。
S4 可选路径收口尚未实现。
S5 显示与真实路径验证尚未执行。
```

## 5. 下一步

进入 S2：

```text
父需求结算前读取 `需求类::读取父子关系角色视图(...)`。
未满足必须子需求阻塞父需求当前满足判定。
非必须子需求不得阻塞父需求满足。
可选路径子需求第一版保持现有 OR 聚合语义，S4 再补同组收口。
```

## 6. S2 执行结果

已修改文件：

```text
任务模块.筹办.ixx
自我线程模块.impl.cpp
说明书/函数功能说明文档.md
```

接入点：

```text
任务模块.筹办.ixx：
1. `统计直接子需求聚合_筹办(...)` 读取 `需求类::读取父子关系角色视图(...)`，统计必须 / 可选路径 / 非必须子需求数量。
2. `返回直接子需求聚合筹办结果(...)` 改为必须子需求全部满足，或无必须子需求时任一可选路径满足，才可输出父侧目标结果符合。
3. `记录需求满足项追踪_筹办(...)` 的活动阻塞子统计改为只统计参与父需求满足的角色。
4. `存在活动直接子需求(...)` 改为未满足必须子需求阻塞；可选路径只在存在未满足路径且没有已满足路径时阻塞；非必须子需求不阻塞父需求。

自我线程模块.impl.cpp：
1. `私有_需求状态已满足_任务管理新链(...)` 改为必须子需求全部满足，或无必须子需求时任一可选路径满足，才通过直接子需求聚合判断父需求满足。
2. `私有_存在活动直接子需求_任务管理新链(...)` 采用同一角色视图规则。
3. `私有_回查父需求目标_任务管理新链(...)` 增加必须 / 可选路径 / 非必须统计，并在父需求回查日志中输出只读统计材料。
```

验证结果：

```text
python .\tools\生成函数功能说明文档.py：成功，函数条目 7664。
git diff --check：无空白错误；函数说明文档出现既有换行提示，不影响检查退出码。
python .\tools\check_specs.py：hard issues = 0，warnings = 0。
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m：0 警告，0 错误。
恢复时发现上轮构建锁 `.codex_locks/build-run.lock` 遗留，记录 PID 32724 已不存在后已释放。
```

未完成：

```text
S3 父任务收束尚未接入角色视图。
S4 可选路径收口尚未实现。
S5 显示与真实路径验证尚未执行。
```

## 7. 下一步

进入 S3：

```text
父任务准备结束或等待解除判断时读取父子关系角色视图。
非必须活动子需求第一版只登记收束边界，不实现放弃 / 转挂正式入口。
不得让自我线程裸写任务状态；父任务状态裁决仍由任务管理层提交入口承接。
```

## 8. S3 执行结果

已修改文件：

```text
任务模块.管理工作线程.ixx
说明书/函数功能说明文档.md
```

接入点：

```text
任务模块.管理工作线程.ixx 新增 `私有_记录父任务收束角色视图_工作线程(...)`。
该函数只在父任务收束 / 父需求已满足 / 存在活动子需求边界读取 `需求类::读取父子关系角色视图(...)`，统计活动必须、活动可选路径和活动非必须子需求数量。
该函数不修改任务状态、不截止需求、不取消活动承接、不转挂需求。
```

已运行：

```text
python .\tools\生成函数功能说明文档.py：成功，函数条目 7665。
git diff --check：无空白错误；函数说明文档出现既有换行提示，不影响检查退出码。
python .\tools\check_specs.py：hard issues = 0，warnings = 0。
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m：0 警告，0 错误。
```

未完成：

```text
S4 可选路径收口尚未实现。
S5 显示与真实路径验证尚未执行。
非必须子需求放弃 / 转挂正式入口仍不存在，未在本切片伪造实现。
```

## 9. 下一步

进入 S4：

```text
检查现有 OR 组结算令牌和可选路径相关入口。
实现或登记第一版可选路径收口，要求任一路径满足后其它同组路径不继续消耗同一父需求预算。
若缺正式活动承接取消 / 截止入口，不得伪造；按问题升级规则拆出前置切片。
```

## 10. S4 构建前断点

当前切片：

```text
S4 可选路径收口。
```

已完成代码改动：

```text
自我动作实现模块.ixx：
1. 新增 `需求树根_动作模块(...)`，只读向上定位需求树根。
2. 新增 `需求路径经过节点_动作模块(...)`，只读判断领取路径是否经过同组直接子节点。
3. 新增 `收束OR组未领取直接路径_动作模块(...)`，在 OR 组令牌首次领取后，遍历 OR 组直接子需求，跳过已领取路径所在分支，只收束角色视图为 可选路径子需求 且未截止的同组路径。
4. `提交领取OR组结算令牌(...)` 首次领取分支在提交 OR 组结算收束后调用同组未领取路径收束，并记录收束数量。
```

边界确认：

```text
1. 不新增需求主信息类字段。
2. 不物理删除正式入树需求节点。
3. 不把日志作为机器判断依据。
4. `构造满足需求指令(...)` 在当前规范语义中只表示本轮活动承接收束和结算证据，不写永久满足字段。
5. 本切片不实现非必须子需求放弃 / 转挂。
```

构建前已运行：

```text
python .\tools\生成函数功能说明文档.py：成功，函数条目 7668。
git diff --check -- 自我动作实现模块.ixx 说明书/函数功能说明文档.md：无空白错误；函数说明文档出现既有换行提示，不影响检查退出码。
python .\tools\check_specs.py：hard issues = 0，warnings = 0。
```

下一步预测：

```text
Debug x64 构建应通过；若失败，进入根因切片，不通过屏蔽日志或绕过 S4 逻辑处理。
```

## 11. S4 执行结果

已修改文件：

```text
自我动作实现模块.ixx
说明书/函数功能说明文档.md
```

接入点：

```text
`提交领取OR组结算令牌(...)` 首次领取 OR 组令牌后：
1. 仍先写入 OR 组令牌记录、领取路径、领取任务和领取动作动态。
2. 仍调用 `需求类::提交需求结算收束(...)` 收束 OR 组自身。
3. 新增调用 `收束OR组未领取直接路径_动作模块(...)`，只收束 OR 组直接子链中未被领取路径经过、未截止、且角色视图为 可选路径子需求 的路径。
```

第一版边界：

```text
1. 同组未领取路径通过既有需求更新指令收束当前活动承接，不物理删除需求节点。
2. 本切片不新增需求主信息类字段，不新增父子角色持久字段。
3. 本切片不实现非必须子需求放弃 / 转挂正式入口。
4. 本切片不实现控制面板 / 数据库查询 显示投影。
5. 本切片不声明真实运行路径已覆盖 OR 组令牌领取。
```

已运行：

```text
python .\tools\生成函数功能说明文档.py：成功，函数条目 7668。
git diff --check -- 自我动作实现模块.ixx 说明书/函数功能说明文档.md：无空白错误；函数说明文档出现既有换行提示，不影响检查退出码。
python .\tools\check_specs.py：hard issues = 0，warnings = 0。
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m：0 警告，0 错误。
```

未完成：

```text
S5 显示与真实路径验证尚未执行。
真实或自然可复跑路径尚未覆盖 OR 组令牌领取和同组未领取路径收束。
非必须子需求放弃 / 转挂正式入口仍不存在，未在本切片伪造实现。
```

## 12. 下一步

进入 S5：

```text
检查控制面板 / 数据库查询 现有只读投影入口。
补充角色视图和活动承接状态的显示或等价只读回查证据。
补充可复跑验证剧本；若缺真实路径条件，按待条件切片登记，不伪造自然样本。
```

## 13. S5 执行结果

已修改文件：

```text
任务模块.管理工作线程.ixx
控制面板类.ixx
控制面板类.cpp
说明书/函数功能说明文档.md
```

接入点：

```text
1. `任务管理工作线程::结构_工作线程实例快照` 增加最近父任务收束角色视图只读统计字段。
2. `私有_记录父任务收束角色视图_工作线程(...)` 在完成直接子需求角色统计后，把任务主键、来源需求主键、直接子需求数、活动必须 / 可选路径 / 非必须子需求数和截止子需求数写入工作线程快照。
3. `结构_控制面板快照` 增加当前主需求父子关系角色视图和最近父任务收束角色统计字段。
4. 控制面板需求详情、需求列表详情、任务管理摘要、线程工作节点详情和需求树生长摘要显示这些只读字段。
```

边界：

```text
1. 不新增需求主信息类字段。
2. 不让控制面板文本、日志或函数说明文档参与机器判断。
3. 不改变任务状态、不截止需求、不取消活动承接、不转挂需求。
4. 本切片不新增 SQL 控制面板专门字段；数据库查询投影仍可通过现有需求显示项和 live 控制面板路径人工回查。
```

已运行：

```text
python .\tools\生成函数功能说明文档.py：成功，函数条目 7668。
git diff --check -- 任务模块.管理工作线程.ixx 控制面板类.ixx 控制面板类.cpp：无空白错误；控制面板类.cpp 出现 CRLF/LF 提示，不影响检查退出码。
python .\tools\check_specs.py：hard issues = 0，warnings = 0。
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m：0 警告，0 错误。
.\x64\Debug\鱼巢.exe --task-mgmt-summary：退出码 0；输出包含 `最近父任务收束角色视图`。
.\x64\Debug\鱼巢.exe --demand-tree-growth-summary-live：退出码 0；输出包含 `父子角色=必须子需求`。
```

未覆盖：

```text
真实或自然样本尚未覆盖 OR 组令牌领取和同组未领取路径收束。
非必须子需求放弃 / 转挂正式入口仍不存在，未在本计划伪造实现。
```

## 14. P1 完成判断和下一步

P1 `需求树父子关系角色视图实现映射计划` 已完成 S0-S5 第一版闭合：

```text
S0 代码事实扫描：已完成。
S1 只读角色视图：已完成。
S2 父需求结算接入：已完成。
S3 父任务收束统计接入：已完成。
S4 可选路径收口：已完成第一版。
S5 显示与回归：已完成控制面板 live 只读投影和 CLI 回归。
```

下一步按计划索引进入：

```text
E4/P6 `结构化信息缺口与需求派生承接实现映射计划` S0 只读扫描。
```
