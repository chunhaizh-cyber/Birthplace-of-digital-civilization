# 20260621 P2 任务类现有函数覆盖表

## 0. 判定依据

```text
依据计划：
    计划/20260620_任务类逻辑纠偏计划_v0.1.md P2。

前置证据：
    日志/20260621_T0j_任务主信息外部直读复扫.txt：无命中。
    实施记录/20260621_P1_任务主信息字段必要性判定表.md。
```

## 1. 字段函数覆盖表

| 字段 | 已有任务类函数 | 是否需要新增函数 | 可回迁调用点状态 |
| --- | --- | --- | --- |
| 名称 | 暂无外部只读 helper；任务类内部创建 / SQL 投影使用 | 否 | 外部直读已清零；名称仅显示 / SQL 投影，暂不新增外部 helper。 |
| 类型 | `读取任务节点种类`、任务类内部概念补齐路径 | 否 | 外部直读已清零；任务分类不应直接读主信息类型。 |
| 创建时间 | `读取任务创建时间` | 否 | 外部直读已清零。 |
| 启动时间 | `读取任务启动时间` | 否 | 外部直读已清零；控制面板摘要已回迁。 |
| 完成时间 | `读取任务完成时间` | 否 | 外部直读已清零；控制面板摘要 / 控制命令已回迁。 |
| 对应需求 | `读取任务对应需求引用`、`解析任务对应需求`、`绑定对应需求` | 否 | 外部直读已清零；自检、自我线程、管理界面线程、服务模块和控制命令均已回迁。 |
| 任务虚拟存在 | `读取任务虚拟存在引用`、`解析任务虚拟存在`、`取或创建_任务虚拟存在` | 否 | 外部直读已清零；筹办、执行、自我线程、自我动作、服务模块、管理界面线程和控制命令均已回迁。 |
| 场景 | `读取任务场景引用`、`解析任务场景` | 否 | 外部直读已清零；筹办、执行、自我线程、自我动作和管理界面线程均已回迁。 |
| 目标状态 | `读取任务目标状态引用`、`解析任务目标状态`、`写入任务目标状态` | 否 | 外部直读已清零；管理界面线程目标状态写入已回迁。 |
| 实际结果状态 | `读取任务实际结果状态`、`解析任务实际结果状态`、`写入任务实际结果状态` | 否 | 外部直读已清零；自我动作实现、控制命令、管理界面线程均已回迁。 |

## 2. 当前结论

```text
1. 当前任务主信息字段均已有满足外部读取 / 写入收束所需的任务类函数，暂不需要新增函数。
2. `名称 / 类型` 不新增通用外部读取 helper，避免显示字段反向进入机器判断；如后续显示层确需读取，应走任务类快照 / SQL 投影专用路径。
3. P2 闭合后可进入 P3：任务类自身跨类读取回迁复核。
4. 不宣称任务类逻辑纠偏完成。
```
