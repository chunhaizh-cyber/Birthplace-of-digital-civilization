# 20260630 控制面板点击无响应 Codex 断点清单

## 0. 恢复摘要

- 当前切片：`追根因_20260629_控制面板点击无响应`。
- 当前判断：按区段读取实现已在当前 `HEAD`，本轮重点是复核真实构建 / 查询 / 静态输出证据，并把计划索引从 `可执行` 收口到完成或明确堵点。
- 禁止宣称：不宣称内部循环完成、自我苏醒完成或初步成熟完成；不把控制面板显示恢复等同于业务闭环完成。

## 1. 依据

- `规范/禁止性规范20260611.md`：控制面板和日志只作显示 / 诊断，不承载机器逻辑。
- `docs/工程图谱/05_规则原子索引.md`：`RULE-LOG-001`、`RULE-THREAD-004`。
- `计划/追根因_20260629_控制面板点击无响应_v0.1.md`。

## 2. 预测

- `demandTree` 页面刷新应只读取 `demandTree` SQL 投影，不触发 `causalInfo`、`causalInfoRelations`、`worldTree`、`worldRelations` 等重区段。
- WebView2 点击刷新应在后台线程生成页面 JSON，再投递回 UI 线程执行脚本，避免 SQL 查询阻塞 WebView2 UI 线程。
- 若数据库可用，需求树投影直查应返回非空根节点；若数据库不可用，本切片只能记录环境堵点，不能伪造 SQL 证据。

## 3. 已核对代码锚点

- `控制面板类.cpp:7035`：`私有_读取SQL控制面板数据` 接受 `指定区段ID`。
- `控制面板类.cpp:7069`：`demandTree` 区段只允许查询 `demandTree`。
- `控制面板类.cpp:8160`：`私有_SQL控制面板区段刷新JSON` 将目标区段传入 SQL 数据读取。
- `控制面板WebView2.cpp:1201`：页面刷新 JSON 在后台线程读取。

## 4. 本轮待验证

- Debug x64 构建。
- SQL 需求树投影直查。
- `--panel-html` 静态 HTML 生成和需求树内容检查。

## 5. 本轮验证结果

- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`：通过，0 警告，0 错误。
- SQL 连接：`.\SQLEXPRESS` / `鱼巢投影库` 可连接。
- SQL 视图列复核：`[鱼巢].[当前需求面板节点]` 当前列名为 `行号`、`父节点行号`、`显示深度`、`节点名称` 等加工视图字段，旧列名 `节点序号`、`父序号`、`深度`、`名称` 不再适用。
- SQL 需求树直查：当前总数 12，根层数 2；根层节点为 `安全`、`服务`。
- `.\x64\Debug\鱼巢.exe --panel-html`：退出码 0，输出 `控制面板 HTML 已生成: D:\鱼巢\控制面板.html | 来源=ADO-SQL`。
- `控制面板.html` 检查：`<section id="demandTree">` 存在，需求树表格包含根节点 `安全`、`服务`。

## 6. 收口判断

- 根因切片已闭合：点击刷新卡住的主因候选已由按区段 SQL 读取和异步页面 JSON 投递收束；当前证据足以把 `追根因_20260629_控制面板点击无响应` 从主动队列移出。
- 剩余风险：未在用户当前 WebView2 交互窗口复点菜单；若后续仍停在“正在刷新”，按本计划记录继续查 `控制面板WebView2/页面刷新较慢`、`页面刷新脚本执行失败`、`页面刷新JSON转宽字串失败`。
