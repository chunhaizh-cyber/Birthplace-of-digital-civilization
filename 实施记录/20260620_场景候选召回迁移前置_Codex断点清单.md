# 20260620 场景候选召回迁移前置 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.4 P5.2b，审查并拆分 `世界树类::查找场景存在_按深度或彩图轮廓图` 迁入场景类的前置范围。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
实施记录/20260620_世界树场景辅助函数归属判定表.md
实施记录/20260620_世界树坐标薄封装回迁_Codex断点清单.md
```

## 1. 问题锚点

```text
世界树类::查找场景存在_按深度或彩图轮廓图
结构_世界树场景轮廓查找参数
结构_世界树场景轮廓存在比较结果
自我动作实现.外设模块.ixx D455 局部簇三维体素查找调用方
```

当前判断：

```text
场景候选召回入口应归场景类；体素生成 / 比较仍归特征类。
本切片先审查函数、DTO 和调用方边界，避免把世界树命名 DTO 直接长期迁入场景类。
```

验证前预测：

```text
1. 应能定位候选召回函数声明 / 定义、参数 / 结果 DTO 和外设调用方。
2. 若现有 DTO 阻塞直接迁移，应登记明确的 DTO 迁名 / 迁位前置项，并保留返回点。
3. 本切片若只形成审查结论，不改 C++，通常不需要构建。
```

## 2. 当前执行进展

已复核：

```text
1. 世界树类.h:35 定义 结构_世界树场景轮廓查找参数。
2. 世界树类.h:43 定义 结构_世界树场景轮廓存在比较结果。
3. 世界树类.h:242 声明 世界树类::查找场景存在_按深度或彩图轮廓图。
4. 世界树类.cpp:2090 定义候选召回函数。
5. 自我动作实现.外设模块.ixx:11112 / 11122 / 11135 使用世界树命名 DTO 和世界树候选召回入口。
```

第一问题锚点：

```text
世界树类::查找场景存在_按深度或彩图轮廓图 内部不仅读取场景直接子存在，还调用 特征类::比较局部三维体素与三维体素链，并传入 世界树类::值池_。
场景类当前只持有 基础信息类*，没有 特征值类 值池依赖。
```

当前判断：

```text
不能直接把该函数整体搬进场景类，否则要么把世界树值池依赖塞入场景类，要么继续让场景类使用世界树命名 DTO。
下一步先查是否已有特征类 / 世界树薄封装可承接体素比较与值池，再决定是补特征类 helper 还是登记 DTO / 值池依赖拆分切片。
```

第一问题收束：

```text
特征类只有静态体素融合 / 比较函数，且显式要求调用方传入 const 特征值类& 值池。
若后续新增 场景类 候选召回 helper，应以参数方式接收值池或由世界树薄封装继续协调值池；不应把 世界树类::值池_ 成员依赖塞入 场景类。
```

第二问题锚点：

```text
世界树类::查找场景存在_按深度或彩图轮廓图 使用 世界树类::是实例存在 过滤候选。
世界树类::是实例存在 依赖 世界树类::存在概念根，场景类当前没有存在概念根上下文，存在类也没有带概念根参数的实例判定公开 helper。
```

第二问题收束：

```text
候选召回迁入场景类前，需要补 owning-class 边界：
1. 场景候选召回 DTO 不能继续使用 结构_世界树... 命名。
2. 实例存在判定应由存在类承接，或由场景类 helper 显式接收实例过滤条件；不得在场景类中反向依赖世界树存在概念根。
3. 值池应作为参数传入候选召回 helper，或保留世界树薄封装协调值池。
```

已生成判定表：

```text
实施记录/20260620_场景候选召回迁移前置判定表.md
```

当前判断：

```text
P5.2b 前置审查已闭合。
下一步按“缺哪个类补哪个类第一优先”转入 存在类逻辑纠偏计划，补带存在概念根上下文的实例存在判定 helper。
本切片只新增断点和判定表，未改 C++，未运行构建。
```

禁止宣称：

```text
不宣称场景候选召回已迁移完成。
不宣称世界树场景辅助函数迁移完成。
不宣称场景类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
