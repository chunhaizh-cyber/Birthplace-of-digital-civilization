# P10f-f 任务管理界面线程 string 键边界复核 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-f 任务管理界面线程 string 键 / 等待状态相关字段边界复核。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `规范/任务筹办与执行边界规范20260512.md`
- `规范/任务筹办父方法与协作子方法链规范20260513.md`
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_causal_structured.tsv`

执行口径：

- 主键 / ID / 句柄 / generation 类 string 不作为本切片清理目标。
- `工作项类型 / 状态` 若只是后台 worker 快照显示文本，不硬删。
- `幂等索引 / 外设承接上行幂等表 / 在途工作项索引 / 执行前许可任务占用表 / 执行前许可免审批方法白名单 / 任务状态快照表 / 等待任务索引 / 挂起任务索引` 若键由任务主键、方法主键、消息 ID、时间戳、枚举值等结构化字段组合生成，先记录为主键组合类，不按字段名硬删。
- 若发现键由自由文本原因、显示状态、显示类型或用户可读摘要参与等待状态、许可判断、候选路由或状态推进，则必须结构化替代。

当前候选：

- `任务模块.管理界面线程.impl.cpp:167` `结构_后台worker当前工作快照::工作项类型`
- `任务模块.管理界面线程.impl.cpp:168` `结构_后台worker当前工作快照::状态`
- `任务模块.管理界面线程.impl.cpp:232` `结构_任务界面线程状态::幂等索引`
- `任务模块.管理界面线程.impl.cpp:233` `结构_任务界面线程状态::外设承接上行幂等表`
- `任务模块.管理界面线程.impl.cpp:242` `结构_任务界面线程状态::在途工作项索引`
- `任务模块.管理界面线程.impl.cpp:243` `结构_任务界面线程状态::执行前许可任务占用表`
- `任务模块.管理界面线程.impl.cpp:244` `结构_任务界面线程状态::执行前许可免审批方法白名单`
- `任务模块.管理界面线程.impl.cpp:249` `结构_任务界面线程状态::任务状态快照表`
- `任务模块.管理界面线程.impl.cpp:250` `结构_任务界面线程状态::等待任务索引`
- `任务模块.管理界面线程.impl.cpp:251` `结构_任务界面线程状态::挂起任务索引`

预测：

- 快照字段大概率只服务控制面板 / 日志显示。
- 多数 map / set 键大概率是任务主键、方法主键或消息结构字段组合；若确认如此，应从最新清单中标注为当前无堵点，而不是强删。
- 重点风险是幂等键或白名单若拼入文本类型 / 文本状态 / 显示摘要，则需要改为枚举值、数值 ID 或明确主键组合。

## 本轮问题记录

问题锚点：

- `任务模块.管理界面线程.impl.cpp::私有_构造任务治理幂等键`
- `任务模块.管理界面线程.impl.cpp::私有_尝试构造外设观察等待项_任务管理`

证据行：

- 任务治理幂等键当前首段使用 `私有_请求类型文本(类型)`。
- 外设观察等待项幂等键当前使用 `私有_外设观察需求口径文本_任务管理(外设观察口径)` 和 `外设观察运行模式文本(输出->观察运行模式)`。

当前判断：

- 这些键用于 `幂等索引` 和外设等待项去重，属于机器去重/路由辅助键，不应把枚举显示文本作为键值组成。
- 应改为枚举数值；中文文本继续只用于日志和控制面板显示。

下一步：

- 将任务治理幂等键中的请求类型改为 `static_cast<int>(类型)`。
- 将外设观察等待项幂等键中的观察口径和观察运行模式改为枚举数值。

已处理：

- `私有_构造任务治理幂等键` 不再使用 `私有_请求类型文本(类型)`，改为 `请求类型值=<枚举数值>`。
- 外设观察等待项幂等键不再使用 `私有_外设观察需求口径文本_任务管理(...)` 或 `外设观察运行模式文本(...)`，改为 `结构化口径值 / 模式值`。
- 外设观察等待项幂等键中的空主键 / 空 generation 不再写入中文 `"空"` 占位，改为空值段。
- `结构_后台worker当前工作快照::工作项类型 / 状态` 只由后台 worker 快照写入并进入控制面板 / 日志显示；机器判断继续读取 `工作项.工作类型` 枚举。
- `等待任务索引 / 挂起任务索引 / 任务状态快照表` 键为任务主键，状态为 `枚举_任务状态`。
- `在途工作项索引 / 执行前许可任务占用表` 键为任务主键，值为工作项记录或工作项 ID。
- `执行前许可免审批方法白名单` 键为方法主键，由方法节点主键注册和查找。

验证记录：

- `rg` 复核 `输出 << 私有_请求类型文本`、`|结构化口径=`、`|模式=` 与 `外设观察运行模式文本` 拼入幂等键、`幂等键` 拼入 `std::string("空")`：无命中。
- `git -c core.quotepath=false diff --check -- 任务模块.管理界面线程.impl.cpp 实施记录/20260623_P10f_f_任务管理界面线程string键边界复核_Codex断点清单.md`：通过。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo`：成功，0 警告，0 错误。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
