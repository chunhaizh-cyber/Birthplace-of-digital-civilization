# P10f-p 观察事实承接来源外设字段复核 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-p `任务模块.工作线程消息协议.ixx::结构_任务管理观察事实承接请求::来源外设` 边界复核。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `规范/任务筹办与执行边界规范20260512.md`
- `规范/任务筹办父方法与协作子方法链规范20260513.md`
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_thread_extra_feature.tsv`

当前问题：

- `结构_任务管理观察事实承接请求::来源外设` 是非主键类 `std::string` 字段名。
- 写入值来自外设报告 `报告.来源外设`，该值由 D455 线程写入外设 ID。
- 字段进入任务管理观察事实承接请求日志和自我线程消息幂等摘要，应按外设身份处理，不应命名为普通文本。

下一步：

- 将该字段改名为 `来源外设ID`。
- 同步更新任务管理工作线程写入、协议日志、自我线程日志和消息幂等摘要。
- 刷新 P10f string 复扫清单，构建后复核。

## 1. 构建前预测

- 本切片只将观察事实承接请求中的外设身份字段由 `来源外设` 改名为 `来源外设ID`，同步更新任务管理写入、自我线程日志和消息幂等摘要。
- 外设报告队列中的 `报告.来源外设` 暂不混入本切片；它仍是后续候选。
- 预期 Debug x64 构建通过，不出现旧字段 `请求.来源外设` 或 `承接请求.来源外设` 编译错误。

## 2. 完成证据

- 已将 `结构_任务管理观察事实承接请求::来源外设` 改名为 `来源外设ID`。
- 已同步更新 `任务模块.管理工作线程.ixx` 写入、`任务模块.工作线程消息协议.ixx` 协议日志、`自我线程模块.消息批次执行器.ixx` 幂等摘要、`自我线程模块.impl.cpp` 两处诊断日志。
- 已生成 `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_observation_device_id.tsv`，剩余记录 627 行，其中主键 / ID 类 315 行、非主键类 312 行。
- `rg` 复核目标文件中无 `请求.来源外设` / `承接请求.来源外设` / `观察事实承接请求.来源外设` / `std::string 来源外设{}` 残留。
- `git diff --check` 通过。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 通过，0 警告，0 错误。

## 3. 下一候选

- `外设观察报告队列.ixx::结构_外设观察报告队列项::来源外设` 仍是非主键类字段名，且已在等待项目标外设匹配、按外设ID查询和包头外设ID生成路径中出现。
- 下一切片应复核并收束为 `来源外设ID` 或更明确的内部身份字段；不得把显示日志文本混入判断。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
