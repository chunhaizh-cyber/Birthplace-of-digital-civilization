# P10f-bg 控制面板节点字段文本判断清理 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-bg，处理 `控制面板类.cpp` 中控制面板树节点用 `子项.文本 == "节点字段"` 识别字段分组的问题。

依据文件：
- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `计划/计划索引.md`
- `docs/工程图谱/05_规则原子索引.md`

问题锚点：
- `控制面板类.cpp::私有_移除节点字段子项(...)`
- `控制面板类.cpp::私有_提取节点字段详情(...)`

当前判断：
- 这是控制面板 UI 展示树内部逻辑，不是业务落账；但它直接用显示文本识别字段分组，可以用结构化布尔位替代。

计划修改：
- `结构_控制面板树节点` 增加 `bool 是字段分组`。
- 所有 `私有_新节点("节点字段")` 创建的字段组节点置 `是字段分组 = true`。
- 移除 / 提取字段分组时读取布尔位，不再比较显示文本。

已完成修改：
- `控制面板类.ixx::结构_控制面板树节点` 增加 `bool 是字段分组`。
- `控制面板类.cpp` 中 5 个 `私有_新节点("节点字段")` 字段组创建点均设置 `字段节点.是字段分组 = true`。
- `私有_移除节点字段子项(...)` 和 `私有_提取节点字段详情(...)` 改为读取 `子项.是字段分组`，不再比较 `子项.文本 == "节点字段"`。

验证：
- 精确检索 `文本 == "节点字段"` 无残留。
- `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_control_panel_node_field_flag.tsv` 已生成；本切片未增删 string 字段，剩余 514 行，其中主键 / ID / 键 / 句柄类 330 行，非主键类 184 行。
- `git diff --check` 通过。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo` 通过，0 警告，0 错误。

禁止宣称：
- 不宣称自我苏醒完成。
- 不宣称初步成熟完成。
- 不宣称项目结构闭环全部通过。
