# 20260623 P10f 控制面板运行事实摘要字段结构化 Codex 断点清单

## 0. 恢复摘要

当前主动计划：`计划/计划索引.md` 第 1 章 `P10f-c 后续：string / 摘要 / 原因类字段专项清理`。

当前切片：

```text
`控制面板摘要线程模块.ixx::结构_控制面板摘要快照::运行事实摘要` 由运行证据方法数、失败任务数、等待任务数、最近方法主键和时间拼接生成。
其中还包含 `运行虚拟存在索引=未接入` 纯说明文本。
本切片删除该拼接摘要字段，改为保留结构化计数、主键和时间字段。
```

预测：

```text
删除 `运行事实摘要 / 控制面板摘要_运行事实 / 私有_采集运行事实摘要` 不影响机器判断。
控制面板线程详情仍能显示运行证据方法数、失败任务数、等待任务数、最近方法主键和时间。
目标复扫中旧字段 / 旧 helper 应无源码命中。
删除并改名 helper 后需刷新 `说明书/函数功能说明文档.md`。
Debug x64 构建应通过。
```

## 1. 本次修改

```text
已删除 `控制面板摘要线程模块.ixx::结构_控制面板摘要快照::运行事实摘要`。
已删除 `控制面板类.ixx::结构_控制面板快照::控制面板摘要_运行事实`。

摘要线程快照新增结构化运行事实槽：
    运行事实有运行证据方法数
    运行事实失败任务数
    运行事实等待任务数
    运行事实最近方法主键
    运行事实最近方法时间
    运行事实最近任务时间

控制面板快照新增对应显示读取槽：
    控制面板摘要_运行证据方法数
    控制面板摘要_失败任务数
    控制面板摘要_等待任务数
    控制面板摘要_最近方法主键
    控制面板摘要_最近方法时间
    控制面板摘要_最近任务时间

`私有_采集运行事实摘要` 改名为 `私有_采集运行事实状态槽`，不再返回拼接文本。
已删除旧文本 `运行虚拟存在索引=未接入`。
```

## 2. 复扫与构建

目标字段复扫：

```powershell
rg -n "运行事实摘要|控制面板摘要_运行事实|私有_采集运行事实摘要|运行虚拟存在索引" -g "*.cpp" -g "*.h" -g "*.ixx" -g "!x64/**" -g "!Debug/**" -g "!日志/**"
```

结果：无命中。

函数说明文档：

```powershell
python .\tools\生成函数功能说明文档.py
```

结果：已生成 `说明书/函数功能说明文档.md`；源码文件 127，函数条目 7212。已对本次触碰的函数说明文档做 LF 归一化。

构建：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo
```

结果：首轮因 `控制面板类.ixx` 未暴露 `时间戳` 类型失败，已改为 `std::uint64_t` 时间槽；复跑 `exit_code=0`。日志：`日志/20260623_P10f_控制面板运行事实摘要字段结构化_build.log`。

## 3. 当前边界

```text
本切片只删除运行事实拼接摘要承接。
`自检摘要` 仍需后续判断是否属于显示证据、诊断日志或可由结构化状态替代。
未触碰外设报告证据卡、动作验证报告摘要、自检候选证据摘要和服务 / 风险安全聚合日志摘要。
```
