# 20260608 S1d5c 二次特征值写入批次观测时间戳 Codex 断点清单

## 当前切片

- 主动计划：`计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- 执行方案：`计划/20260607_S1d5c二次特征值写入批次观测时间戳复用瘦身方案_v0.1.md`
- 切片：S1d-5c 二次特征值写入批次观测时间戳复用瘦身。

## 依据文件

- `AGENTS.md`
- `规范/规范目录.md`
- `规范/方法动作动态硬规则规范20260512.md`
- `规范/动作动态即因果账本规范20260512.md`
- `规范/二次特征规范20260520.md`
- `规范/远程执行通道与断点续跑规范20260524.md`
- `计划/计划索引.md`
- `计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- `计划/20260607_S1d5b二次特征值写入主信息复用瘦身方案_v0.1.md`
- `实施记录/20260608_S1d5b二次特征值写入主信息复用_Codex断点清单.md`

## 当前判断

- S1d-5b 已闭合，`分步值写入` 平均约 70124us，仍为当前主慢点。
- `二次特征值写入/内部阶段耗时` 0 条，单次值写入不是主慢点。
- `结构_统计::记录观测` 只维护 `创建时间`、`最后观测时间` 和 `命中次数`。
- 下一步在同一分步结果生成批次内复用一次非零观测时间戳；每个节点仍调用 `统计.记录观测`，不减少命中次数。

## 构建 / 运行前预测

- Debug x64 构建通过。
- 真实运行退出码 0。
- 日志继续出现 `动态二次特征分步结果/内部阶段耗时`。
- `分步值写入` 或标量 / 离散值写入均值下降。
- 日志保留 `任务状态动作动态=<动态主键>`。
- 不出现 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`。
- 不出现旧路径锚点 `准备本能调用`、`执行本能方法`、`任务域本能输入包`。

## 允许修改

- `二次特征类.h`
- `二次特征类.cpp`
- `二次特征应用模块.ixx`
- `计划/20260607_S1d5c二次特征值写入批次观测时间戳复用瘦身方案_v0.1.md`
- `计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- `计划/计划索引.md`
- `实施记录/20260608_S1d5c二次特征值写入批次观测时间戳_Codex断点清单.md`

## 已改文件

- `计划/20260607_S1d5c二次特征值写入批次观测时间戳复用瘦身方案_v0.1.md`
- `实施记录/20260608_S1d5c二次特征值写入批次观测时间戳_Codex断点清单.md`

## 下一步

1. 提交 S1d-5b 闭合切片和 S1d-5c 入口登记。
2. 修改 `二次特征类.h`、`二次特征类.cpp`、`二次特征应用模块.ixx`。
3. 构建 Debug x64。
4. 真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000`。

## 禁止宣称

- 不宣称自我苏醒完成。
- 不宣称初步成熟完成。
- 不改变二次特征生成语义。
- 不跳过观测记录。
- 不删除合法分步结果。
