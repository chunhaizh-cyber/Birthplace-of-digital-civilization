# 20260630 已确认存在体素融合分解 P8c Codex断点清单

## 0. 恢复摘要

当前切片：P8c 体素加工候选结果包诊断。

计划依据：

```text
计划/20260630_已确认存在体素融合分解施工计划_v0.1.md
计划/计划索引.md R23
规范/已确认存在体素融合与分解规范20260630.md
详细设计/已确认存在体素融合与分解实现方案20260630.md
```

目标：

```text
只读暴露体素加工候选结果包诊断字段。
确认候选包是否存在、父基础颜色统计是否有效、子轮廓候选数量、每个子候选颜色/深度/局部体素候选状态。
不给子平均颜色落账，不建立子候选到内部世界子存在映射。
```

禁止宣称：

```text
不宣称子平均颜色事实已闭合。
不宣称体素子轮廓已经成功提取并提交为子存在。
不宣称 P9 自然链路已验证。
不宣称视觉系统完成、自我苏醒完成或初步成熟完成。
```

## 1. 已读取依据

```text
docs/00_文档总索引.md
规范/规范目录.md
计划/计划索引.md
规范/已确认存在体素融合与分解规范20260630.md
详细设计/已确认存在体素融合与分解实现方案20260630.md
docs/工程图谱/00_图谱总索引.md
docs/工程图谱/05_规则原子索引.md
```

命中规则：

```text
RULE-VOXEL-FUSION-001：体素加工只由新增观察证据触发。
RULE-VOXEL-FUSION-005：彩色分解必须由深度补空间。
RULE-VOXEL-FUSION-006：父体素和子体素分别建模。
RULE-VOXEL-FUSION-007：颜色覆盖只属于渲染优先级。
RULE-VOXEL-FUSION-008：体素加工线程只能返回候选。
```

## 2. 当前判断

```text
P8b 已闭合父基础颜色事实写入和控制面板只读显示。
子平均颜色当前仍停在结构化缺口“缺子候选映射”。
P8c 需要先证明运行期候选包中是否存在子轮廓候选和局部子体素候选。
控制面板可作为只读诊断展示面，不能反向参与事实裁决。
```

## 3. 计划修改文件

```text
控制面板类.ixx
控制面板类.cpp
实施记录/20260630_已确认存在体素融合分解_P8c_Codex断点清单.md
```

## 4. 待执行验证

```text
Debug x64 构建。
运行 --panel-html 或自我场景 JSON 路径，检索体素加工候选诊断字段。
根据真实输出记录候选包数量和子候选分支。
```

## 5. 构建前预测

```text
预期 Debug x64 构建通过。
本切片只新增控制面板只读诊断字段、JSON 输出和 HTML 渲染，不改变体素加工线程写权，不新增事实写入。
若构建失败，优先检查控制面板导入视觉观察融合模块、JSON 输出语法和新增快照字段引用。
```

## 6. 构建结果

```text
命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
日志：日志/msbuild_P8c_体素候选诊断_20260630.log
退出码：0
关键结果：已成功生成；0 个警告；0 个错误。
```

## 7. 运行前预测

```text
预期 .\x64\Debug\鱼巢.exe --panel-html 退出码为 0。
控制面板.html 中应包含 voxelCandidatePackageCount、voxelCandidatePackages、scene-voxel-candidate-branch-stat 和体素加工候选结果包诊断列表入口。
若当前进程没有自然体素加工任务，候选包数量可以为 0；这只能证明诊断面存在，不证明子轮廓提取成功。
```

## 8. 已排除的错误验证面

```text
命令：.\x64\Debug\鱼巢.exe --panel-html
日志：日志/run_P8c_panel_html_20260630.out.log；日志/run_P8c_panel_html_20260630.err.log
退出码：0
结果：控制面板 HTML 已生成，来源=ADO-SQL。
判断：该命令生成的是 SQL 静态控制面板，不是自我场景复现 HTML / JSON；不能作为 P8c 候选结果包诊断字段的有效验证。
```

## 9. 自我场景验证预测

```text
命令：.\x64\Debug\鱼巢.exe --self-scene-capture --self-scene-capture-wait-ms=5000 --self-scene-capture-path=实施记录\P8c_self_scene_capture.png
预期：退出码为 0，保存自我场景 WebView2 PNG。
验证点：运行路径必须经过生成自我场景独立窗口 HTML / 生成自我场景复现 JSON；截图只能证明自我场景页面可渲染，候选结果包字段还需结合自我场景 HTML / JSON 字段或运行日志检索确认。
可能分支：候选包数量为 0 时，只能证明诊断显示面接入，不能进入 P8d / P8e。
```

## 10. 自我场景验证结果

```text
命令：.\x64\Debug\鱼巢.exe --self-scene-capture --self-scene-capture-wait-ms=5000 --self-scene-capture-path=实施记录\P8c_self_scene_capture.png
标准输出：日志/run_P8c_self_scene_capture_20260630.out.log
标准错误：日志/run_P8c_self_scene_capture_20260630.err.log
退出码：0
截图：实施记录/P8c_self_scene_capture.png，102203 字节。
运行日志证据：日志/鱼巢_run_20260630.低值g:108444 记录截图命令和输出路径；108579 记录截图成功保存。
WebView2 证据：日志/鱼巢_exception_20260630.低值g:13 记录自我场景 NavigateToString 请求，HTML字节=299257。
自我场景 HTML 字段检索：voxelCandidatePackageCount、voxelCandidatePackages、scene-voxel-candidate-package-stat、scene-voxel-candidate-branch-stat、体素加工候选结果包诊断均存在。
自我场景初始 JSON 解析：
  voxelCandidatePackageCount=0
  voxelCandidateLatestChildContourCount=0
  voxelCandidateLatestLocalChildVoxelCount=0
  voxelCandidateDiagnosticBranch=0
  voxelCandidatePackages.count=0
```

## 11. P8c 结论

```text
P8c 只读诊断入口已接入自我场景 HTML / JSON，Debug x64 构建和自我场景截图路径均通过。
当前真实运行没有体素加工候选结果包，因此没有子轮廓候选和局部子体素候选证据。
验收分支：无候选包；不进入 P8d 身份映射，不进入 P8e 子平均颜色落账。
恢复条件：真实合法观察链生成至少一个体素加工候选结果包；若最新包子轮廓候选数量 > 0 且局部子体素候选数量 > 0，才允许继续 P8d。
```
