# 20260620 特征类比较状态回迁状态类 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_特征类逻辑纠偏计划_v0.1.md` 的 1.2，替换 `特征类::比较状态` 中的 `状态节点主信息类` 直读。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_特征类逻辑纠偏计划_v0.1.md
计划/20260620_状态类逻辑纠偏计划_v0.1.md
实施记录/20260620_状态类读取状态特征和值_Codex断点清单.md
```

本切片范围：

```text
1. 特征类::比较状态 改用 状态类::读取状态特征和值。
2. 特征类.cpp 不再直接取 状态节点主信息类。
3. 不处理 特征值类 节点主信息.值 读取。
4. 运行函数说明文档生成器和 Debug x64 构建。
```

验证前预测：

```text
1. rg -n "状态节点主信息类|取主信息<状态节点主信息类>" 特征类.cpp 应无命中。
2. 特征类::比较状态 仍保留原比较语义：特征类型一致后调用 获取比较方法结果。
3. Debug x64 构建应通过。
```

## 1. 实施记录

```text
1. 特征类.cpp 增加 状态类.h 引用。
2. 特征类::比较状态 不再通过 基础信息_->取主信息<状态节点主信息类> 读取状态节点主信息。
3. 特征类::比较状态 改为构造 状态类 状态服务，通过 状态类::读取状态特征和值 分别读取当前状态 / 目标状态的状态特征和值。
4. 后续比较逻辑保持原路径：取特征主信息 -> 检查特征类型一致 -> 调用 获取比较方法结果。
5. 已运行 python .\tools\生成函数功能说明文档.py，函数说明文档随源码行号变化更新。
```

## 2. 验证结果

```text
1. rg -n "状态节点主信息类|取主信息<状态节点主信息类>" 特征类.cpp
   结果：无命中，退出码 1，符合预测。

2. rg -n "读取状态特征和值|状态服务|当前状态值|目标状态值" 特征类.cpp
   结果：命中 特征类.cpp:4046、4049、4050、4051、4052、4068、4069。

3. rg -n "取主信息<[^>]+>|dynamic_cast<[^>]+>|->主信息|\\.主信息\\.值|状态节点主信息类|场景节点主信息类|动态节点主信息类|二次特征主信息类|因果主信息类|需求主信息类|方法主信息类" 特征类.cpp 特征类.h
   结果：状态节点主信息类无命中；仍保留已登记残留：
        特征类.cpp:97 私有_直接基础信息子节点_按类型 的泛型 dynamic_cast。
        特征类.cpp:355、3470、3490、3511、3775 三维体素链读取 特征值类 节点 主信息.值。
        特征类.cpp:4108-4109 比较特征集合 中直接读取 节点->主信息 并 dynamic_cast<const 特征节点主信息类*>。
        特征主信息 / 抽象特征主信息只读函数命中属于特征类职责内读取，未在本切片处理。

4. python .\tools\生成函数功能说明文档.py
   结果：退出码 0；源码文件 128；函数条目 6878；已生成 说明书/函数功能说明文档.md。

5. msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\20260620_特征类比较状态回迁状态类_msbuild.log
   结果：退出码 0；已成功生成；0 个警告；0 个错误；生成 D:\鱼巢\x64\Debug\鱼巢.exe。
```

## 3. 当前判断

```text
1. 本切片已闭合：特征类::比较状态 的 状态节点主信息类 直读已回迁到 状态类::读取状态特征和值。
2. 不宣称特征类逻辑纠偏完成。
3. 下一步仍回到 特征类 1.2，按已登记顺序优先处理 私有_直接基础信息子节点_按类型 的泛型主信息穿透。
```
