# 删除需求结构角色 Codex 断点清单

## 0. 恢复摘要

- 当前切片：删除 `枚举_需求结构角色`、`结构角色` 字段及“管理需求 / 执行需求”分类口径。
- 依据文件：`规范/禁止性规范20260611.md`、`规范/规范目录.md`、`计划/计划索引.md`、需求主信息与需求树生长相关规范。
- 当前判断：需求树只能展示树形事实；叶子 / 非叶子可作为控制面板派生视图，不能回写为需求主信息语义分类。
- 当前已知残留：源码残留检索只剩 `执行需求树` 作为命令/函数动词命名；`控制面板.html` 仍有旧世界树抽象特征投影，需通过真实运行刷新验证。
- 下一步验证：
  - `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
  - `.\x64\Debug\鱼巢.exe --demand-tree-growth-summary-live`
  - `.\x64\Debug\鱼巢.exe --panel-html`
- 禁止宣称：不得宣称内部循环全部完成；不得把叶子 / 非叶子重新命名成管理需求 / 执行需求。

## 1. 已复核结果

- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`：成功，0 警告，0 错误。
- `.\x64\Debug\鱼巢.exe --demand-tree-growth-summary-live`：成功；摘要输出 `结构: 有子=1 | 叶子=4 | 活动叶子=4 | 未任务化叶子=4`。
- `.\x64\Debug\鱼巢.exe --refresh-sql-projection`：成功；刷新 SQL 世界树 / 需求树 / 任务树 / 方法树 / 语素读模型。
- `.\x64\Debug\鱼巢.exe --panel-html`：成功；`控制面板.html` 来自 ADO-SQL。
- 残留检索：`控制面板.html` 不再包含 `需求结构角色`、`管理需求`、`执行需求`；源码残留仅为 `执行需求树` 命令动词，以及 `特征类.cpp` 中清理旧抽象节点的兼容删除入口。
