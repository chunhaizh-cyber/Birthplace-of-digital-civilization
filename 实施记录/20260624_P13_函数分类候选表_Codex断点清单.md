# 20260624 P13 函数分类候选表 Codex断点清单

## 0. 恢复摘要

当前切片：P13a 函数按自定义主信息 / 领域类型分类候选表。

已完成：

```text
1. 已按当前 P13 口径生成分类表：
   实施记录/20260624_P13_函数自定义主信息分类候选表.tsv
2. 分类字段：
   函数名 / 定义位置 / 原始签名 / 参数类型 / 返回类型 /
   函数体领域依赖 / 分类A或B / 迁移候选 / 保留原因 / 模块import风险 / 下一步处理。
3. 统计结果：
   总函数定义条目 6475；
   非函数误识别条目剔除 21；
   A 类 6240；
   B 类候选 235，其中低风险待复核 92，中风险待复核 143。
4. 分类口径：
   命中主信息、节点、结构、协议、领域对象、字段/指针成员访问、成员函数、
   构造析构、operator、程序入口或已位于全局共享模块的，保守列为 A；
   未命中自定义主信息 / 节点 / 结构 / 协议对象的自由函数列为 B 候选。
```

下一步：

```text
P13b 从 B 类低风险候选中选择一个小函数族；
迁移前先查 全局共享函数类.ixx 和 说明书/函数功能说明文档.md 是否已有同功能函数；
若已有则复用或删除重复定义，若没有才迁移；
每个迁移切片后刷新函数说明文档并构建。
```

禁止宣称：

```text
不宣称 P13 全部完成。
不宣称 B 类候选可批量无审查迁移。
不宣称分类表是编译器级精确解析结果；它是 P13b 人工复核和迁移排序依据。
```

## 1. 依据

```text
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md
说明书/函数功能说明文档.md
```

## 2. 验证

```text
已生成 TSV 并抽样检查低风险 B 候选。
本切片只生成实施记录，不修改业务代码；未运行构建。
```
