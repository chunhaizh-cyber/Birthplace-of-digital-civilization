# 20260620 因果类外部数据读取纠偏 Codex 断点清单

## 0. 恢复摘要

当前切片：

```text
P7 因果类回迁：删除因果类中对应直接主信息读取和私有解析函数。
```

已完成：

```text
1. 按规范必读要求读取：
    规范/禁止性规范20260611.md
    规范/规范目录.md
    计划/计划索引.md
2. 生成因果类外部数据读取纠偏协调计划。
3. 生成动态类、状态类、二次特征类、特征类逻辑纠偏计划。
4. 修改需求类计划，追加安全 / 服务方向只读函数缺口。
5. 修改方法类计划，追加方法节点按主键解析缺口。
6. 修改因果类功能函数补完计划，登记 20260620 外部数据读取缺口和返回点。
7. 修改计划索引，将第一优先切到动态类逻辑纠偏计划。
8. 动态类第一优先已补齐并通过 Debug x64 构建。
9. 状态类第一优先已补齐并通过 Debug x64 构建。
10. 二次特征类第一优先已补齐并通过 Debug x64 构建。
11. 特征类第一优先已补齐并通过 Debug x64 构建。
12. 需求类第一优先已补齐并通过 Debug x64 构建。
13. 方法类第一优先已补齐并通过 Debug x64 构建。
14. 方法类 P7 新增缺口已补齐并通过 Debug x64 构建。
```

当前主动队列：

```text
第一优先计划：
    计划/20260620_因果类外部数据读取纠偏协调计划_v0.1.md

当前执行切片：
    P7 因果类回迁：
        删除因果类中对应直接主信息读取和私有解析函数

来源协调计划：
    计划/20260620_因果类外部数据读取纠偏协调计划_v0.1.md

原计划返回点：
    计划/20260619_因果类功能函数补完计划_v0.1.md
    P5 被动触发初始因果模板确保函数。
```

本轮边界：

```text
动态类、状态类、二次特征类、特征类、需求类、方法类第一优先已闭合。
因果类 P7 回迁已开始预扫描；方法类 P7 可调用的基础信息主键解析缺口已闭合。
当前恢复 P7。
P7 闭合前不继续推进因果类功能函数补完计划 P5。
```

下一步：

```text
若用户要求继续执行，先执行 因果类外部数据读取纠偏协调计划 P7。
回迁范围只限因果类.h / 因果类.cpp。
P7 闭合后，停止协调计划，返回 计划/20260619_因果类功能函数补完计划_v0.1.md 的 P5。
```

禁止宣称：

```text
不宣称因果类外部数据读取纠偏完成。
不宣称因果类功能函数补完完成。
不宣称所有因果信息操作已统一走因果类。
不宣称动态类逻辑纠偏完成。
不宣称状态类逻辑纠偏完成。
不宣称二次特征类逻辑纠偏完成。
不宣称特征类逻辑纠偏完成。
不宣称需求类逻辑纠偏完成。
不宣称方法类逻辑纠偏完成。
不宣称结构体清理完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 1. 计划文件

新增计划：

```text
计划/20260620_因果类外部数据读取纠偏协调计划_v0.1.md
计划/20260620_动态类逻辑纠偏计划_v0.1.md
计划/20260620_状态类逻辑纠偏计划_v0.1.md
计划/20260620_二次特征类逻辑纠偏计划_v0.1.md
计划/20260620_特征类逻辑纠偏计划_v0.1.md
```

修改计划：

```text
计划/计划索引.md
计划/20260619_因果类功能函数补完计划_v0.1.md
计划/20260619_需求类头文件结构体清理与目标逻辑纠偏计划_v0.1.md
计划/20260619_方法类逻辑纠偏计划_v0.1.md
```

## 2. 当前缺口顺序

```text
P1 动态类：读取动态因果生成输入。
P2 状态类：读取状态主体 / 状态特征 / 状态特征类型。
P3 二次特征类：读取状态迁移来源和可融合标量区间。
P4 特征类：读取特征语义类型。
P5 需求类：读取安全 / 服务方向。
P6 方法类：解析方法引用。
P7 因果类回迁：删除因果类中对应直接主信息读取和私有解析函数。
```

## 3. 工作区注意

```text
本轮开始时工作区已有其它修改和未跟踪文件。
本断点只记录本轮计划生成范围和本轮已验证切片，不判断既有源码改动是否全部闭合。
提交或继续执行前必须重新检查 git status，并只处理当前切片相关文件。
```

## 4. 动态类第一优先实施

已改文件：

```text
动态类.h
动态类.cpp
```

新增函数：

```text
动态类::按主键解析动态节点
动态类::读取动态因果生成输入
```

当前判断：

```text
1. 新函数只读动态主信息，不创建或修改节点。
2. 业务语义类型输出来自 动态类::推导业务语义类型。
3. 当前切片未执行因果类 P7 回迁；因果类中动态主信息直接读取将在协调计划 P7 统一删除。
```

验证结果：

```text
函数说明文档生成成功：源码文件 128，函数条目 6859。
Debug x64 构建退出码：0。
构建日志：日志/20260620_动态类P1因果生成输入_msbuild.log。
日志尾部：已成功生成；0 个警告；0 个错误。
```

返回关系：

```text
动态类第一优先已闭合，停止动态类计划。
返回 计划/20260620_因果类外部数据读取纠偏协调计划_v0.1.md。
下一优先切片：P2 状态类逻辑纠偏计划。
```

## 5. 状态类第一优先实施

已改文件：

```text
状态类.h
状态类.cpp
```

新增函数：

```text
状态类::按主键解析状态节点
状态类::读取状态主体与特征
状态类::读取状态特征
状态类::读取状态主体
```

当前判断：

```text
1. 新函数只读状态主信息，不创建或修改状态。
2. 当前切片未执行因果类 P7 回迁；因果类中状态主信息直接读取将在协调计划 P7 统一删除。
3. 状态类第一优先闭合后停止状态类计划，返回协调计划 P3。
```

验证结果：

```text
函数说明文档生成成功：源码文件 128，函数条目 6863。
Debug x64 构建退出码：0。
构建日志：日志/20260620_状态类P2主体特征读取_msbuild.log。
日志尾部：已成功生成；0 个警告；0 个错误。
```

返回关系：

```text
状态类第一优先已闭合，停止状态类计划。
返回 计划/20260620_因果类外部数据读取纠偏协调计划_v0.1.md。
下一优先切片：P3 二次特征类逻辑纠偏计划。
```

## 6. 二次特征类第一优先实施

已改文件：

```text
二次特征类.h
二次特征类.cpp
```

新增函数：

```text
二次特征类::按主键解析二次特征节点
二次特征类::读取状态迁移来源
二次特征类::是状态迁移二次特征
二次特征类::读取可融合标量区间
二次特征类::二次特征模板匹配
```

当前判断：

```text
1. 新函数只读二次特征主信息，不创建或修改二次特征。
2. 读取状态迁移来源通过 状态类 / 动态类 只读函数解析引用，不在二次特征类内解释状态或动态主信息。
3. 当前切片未执行因果类 P7 回迁；因果类中二次特征主信息直接读取将在协调计划 P7 统一删除。
```

验证结果：

```text
函数说明文档生成成功：源码文件 128，函数条目 6868。
Debug x64 构建退出码：0。
构建日志：日志/20260620_二次特征类P3状态迁移读取_msbuild.log。
日志尾部：已成功生成；0 个警告；0 个错误。
```

返回关系：

```text
二次特征类第一优先已闭合，停止二次特征类计划。
返回 计划/20260620_因果类外部数据读取纠偏协调计划_v0.1.md。
下一优先切片：P4 特征类逻辑纠偏计划。
```

## 7. 特征类第一优先实施

已改文件：

```text
特征类.h
特征类.cpp
```

新增函数：

```text
特征类::按主键解析特征节点
特征类::读取特征语义类型
```

当前判断：

```text
1. 新函数只读特征主信息，不创建或修改特征。
2. 读取特征语义类型只返回 特征节点主信息类::类型，不用名称或文本兜底。
3. 当前切片未执行因果类 P7 回迁；因果类中特征主信息直接读取将在协调计划 P7 统一删除。
```

验证结果：

```text
函数说明文档生成成功：源码文件 128，函数条目 6870。
Debug x64 构建退出码：0。
构建日志：日志/20260620_特征类P4语义类型读取_msbuild.log。
日志尾部：已成功生成；0 个警告；0 个错误。
```

返回关系：

```text
特征类第一优先已闭合，停止特征类计划。
返回 计划/20260620_因果类外部数据读取纠偏协调计划_v0.1.md。
下一优先切片：P5 需求类计划 4.6。
```

## 8. 需求类第一优先实施

已改文件：

```text
需求类.h
需求类.cpp
```

新增函数：

```text
需求类::来源需求有安全方向
需求类::来源需求有服务方向
```

当前判断：

```text
1. 新函数只读需求主信息安全 / 服务权重，不修改需求树和权重。
2. 判断口径与因果类现有私有函数一致：权重 > 0 视为有对应方向。
3. 当前切片未执行因果类 P7 回迁；因果类中需求主信息权重直接读取将在协调计划 P7 统一删除。
```

验证结果：

```text
函数说明文档生成成功：源码文件 128，函数条目 6874。
Debug x64 构建退出码：0。
构建日志：日志/20260620_需求类P5安全服务方向_msbuild.log。
日志尾部：已成功生成；0 个警告；0 个错误。
```

返回关系：

```text
需求类第一优先已闭合，停止需求类计划。
返回 计划/20260620_因果类外部数据读取纠偏协调计划_v0.1.md。
下一优先切片：P6 方法类逻辑纠偏计划。
```

## 9. 方法类第一优先实施

已改文件：

```text
方法类.h
方法类.cpp
```

新增函数：

```text
方法类::按主键解析方法节点
方法类::按主键解析方法节点 const
```

当前判断：

```text
1. 新函数只读方法树，不创建或修改方法节点。
2. 当前切片未执行因果类 P7 回迁；因果类中方法引用私有解析将在协调计划 P7 统一删除。
```

验证结果：

```text
函数说明文档生成成功：源码文件 128，函数条目 6877。
Debug x64 构建退出码：0。
构建日志：日志/20260620_方法类P6方法节点解析_msbuild.log。
日志尾部：已成功生成；0 个警告；0 个错误。
```

返回关系：

```text
方法类第一优先已闭合，停止方法类计划。
返回 计划/20260620_因果类外部数据读取纠偏协调计划_v0.1.md。
下一优先切片：P7 因果类回迁。
```

## 10. 因果类 P7 回迁

当前切片：

```text
删除因果类中动态、状态、二次特征、特征、需求、方法对应直接主信息读取和私有解析函数。
改为调用 动态类 / 状态类 / 二次特征类 / 特征类 / 需求类 / 方法类 已补齐函数。
```

边界：

```text
只改因果类.h / 因果类.cpp。
不新增平行结构体。
不恢复旧枚举或旧目标投影维度枚举。
不推进因果类功能函数补完计划 P5。
```

待验证：

```powershell
rg -n "dynamic_cast<.*主信息|->主信息|\\.主信息|基础信息->查找主键|获取基础信息集\\(\\).*查找主键" 因果类.cpp
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

当前暂停点：

```text
P7 预扫描发现：因果类只有 基础信息类*，已补的 方法类::按主键解析方法节点 成员函数不能直接替代 私有_解析方法引用_因果。
已按跨类缺口转向规则，把 方法类 基于基础信息解析方法节点主键函数提升为第一优先。
```

## 11. 方法类新增第一优先实施

已改文件：

```text
方法类.h
方法类.cpp
```

新增函数：

```text
方法类::按主键解析方法节点(基础信息类*, const std::string&)
```

当前判断：

```text
1. 新函数只读基础信息主键，不创建或修改方法节点。
2. 不新增方法主信息字段，不新增方法引用结构体。
3. 函数闭合后返回因果类 P7。
```

验证结果：

```text
函数说明文档生成成功：源码文件 128，函数条目 6879。
Debug x64 构建退出码：0。
构建日志：日志/20260620_方法类P7基础信息方法解析_msbuild.log。
日志尾部：已成功生成；0 个警告；0 个错误。
```

返回关系：

```text
方法类新增第一优先已闭合，停止方法类计划。
返回 计划/20260620_因果类外部数据读取纠偏协调计划_v0.1.md P7。
```

## 12. 场景类新增第一优先实施

触发点：

```text
因果类 P7 验收正则会检查 基础信息->查找主键。
P7 预扫描发现 因果类.cpp::私有_解析场景引用_因果 仍直接通过 基础信息->查找主键 解析场景引用。
函数文档未发现 场景类::按主键解析场景节点 或同功能公开函数。
```

处理规则：

```text
按跨类缺口转向规则，生成 计划/20260620_场景类逻辑纠偏计划_v0.1.md。
当前缺口提升为场景类第一优先。
第一优先闭合后停止场景类计划，返回因果类外部数据读取纠偏协调计划 P7。
```

已改文件：

```text
场景类.h
场景类.cpp
```

新增函数：

```text
场景类::按主键解析场景节点
```

当前判断：

```text
1. 新函数只读基础信息主键并校验 场景节点主信息类。
2. 不创建、不删除、不修改场景节点。
3. 函数闭合后返回因果类 P7，用于替换 因果类.cpp::私有_解析场景引用_因果 中的直接查找主键。
```

验证结果：

```text
函数说明文档生成成功：源码文件 128，函数条目 6880。
Debug x64 构建退出码：0。
构建日志：日志/20260620_场景类P1场景节点解析_msbuild.log。
日志尾部：已成功生成；0 个警告；0 个错误。
```

返回关系：

```text
场景类第一优先已闭合，停止场景类计划。
返回 计划/20260620_因果类外部数据读取纠偏协调计划_v0.1.md P7。
```

## 13. 二次特征类新增第一优先实施

触发点：

```text
因果类 P7 回迁发现 因果类.cpp::私有_融合主果状态迁移区间_因果 会直接写 二次特征主信息类::标量区间。
现有 二次特征类 只有 读取可融合标量区间，没有同向状态迁移区间融合写回函数。
```

处理规则：

```text
按跨类缺口转向规则，修改 计划/20260620_二次特征类逻辑纠偏计划_v0.1.md。
当前缺口提升为二次特征类新增第一优先。
第一优先闭合后停止二次特征类计划，返回因果类外部数据读取纠偏协调计划 P7。
```

已改文件：

```text
二次特征类.h
二次特征类.cpp
```

新增函数：

```text
二次特征类::融合同向状态迁移可融合标量区间
```

当前判断：

```text
1. 新函数只在二次特征类内读取并写回二次特征主信息标量区间。
2. 调用方只提供目标二次特征和候选集合，不直接解释二次特征主信息。
3. 函数闭合后返回因果类 P7，用于替换 因果类.cpp::私有_融合主果状态迁移区间_因果 的直接写回。
```

验证结果：

```text
函数说明文档生成成功：源码文件 128，函数条目 6881。
Debug x64 构建退出码：0。
构建日志：日志/20260620_二次特征类P7标量区间融合_msbuild.log。
日志尾部：已成功生成；0 个警告；0 个错误。
```

返回关系：

```text
二次特征类新增第一优先已闭合，停止二次特征类计划。
返回 计划/20260620_因果类外部数据读取纠偏协调计划_v0.1.md P7。
```

## 14. 动态类新增第一优先实施

触发点：

```text
因果类 P7 回迁发现 因果类::生成因果信息(动态节点类*) 会直接读取 动态节点主信息类::来源输出场景 / 来源输入场景。
现有 动态类 没有动态来源场景只读函数。
```

处理规则：

```text
按跨类缺口转向规则，修改 计划/20260620_动态类逻辑纠偏计划_v0.1.md。
当前缺口提升为动态类新增第一优先。
第一优先闭合后停止动态类计划，返回因果类外部数据读取纠偏协调计划 P7。
```

已改文件：

```text
动态类.h
动态类.cpp
```

新增函数：

```text
动态类::读取动态来源场景
```

当前判断：

```text
1. 新函数只读动态来源输入场景和输出场景。
2. 场景引用解析调用 场景类::按主键解析场景节点。
3. 函数闭合后返回因果类 P7，用于替换 因果类::生成因果信息(动态节点类*) 的直接来源场景读取。
```

验证结果：

```text
函数说明文档生成成功：源码文件 128，函数条目 6880。
Debug x64 构建退出码：0。
构建日志：日志/20260620_动态类P7来源场景读取_msbuild.log。
日志尾部：已成功生成；0 个警告；0 个错误。
```

返回关系：

```text
动态类新增第一优先已闭合，停止动态类计划。
返回 计划/20260620_因果类外部数据读取纠偏协调计划_v0.1.md P7。
```

## 15. 因果类 P7 回迁闭合

已改文件：

```text
因果类.cpp
```

回迁范围：

```text
1. 状态 / 特征 / 二次特征 / 动态 / 方法 / 场景引用解析改为调用对应类按主键解析函数。
2. 状态主体、状态特征、特征语义类型、需求安全/服务方向改为调用对应类读取函数。
3. 动态因果生成输入、动态来源场景改为调用 动态类。
4. 二次特征模板匹配、状态迁移来源、可融合区间和区间融合写回改为调用 二次特征类。
5. 因果类保留 因果主信息类 / 因果模板主信息类 自身读取。
```

验证结果：

```text
函数说明文档生成成功：源码文件 128，函数条目 6880。
Debug x64 构建退出码：0。
构建日志：日志/20260620_因果类P7外部读取回迁_msbuild.log。
残留扫描命中 3 行，均为 因果主信息类 dynamic_cast / 节点->主信息，属于因果类自身边界。
```

返回关系：

```text
因果类外部数据读取纠偏协调计划 P1-P7 已闭合。
停止本协调计划，返回 计划/20260619_因果类功能函数补完计划_v0.1.md 的 P5。
```
