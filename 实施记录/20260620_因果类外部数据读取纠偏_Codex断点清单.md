# 20260620 因果类外部数据读取纠偏 Codex 断点清单

## 0. 恢复摘要

当前切片：

```text
生成相关数据结构纠偏及补全功能函数计划。
```

已完成：

```text
1. 按规范必读要求读取：
    规范/禁止性规范20260611.md
    规范/规范目录.md
    计划/计划索引.md
2. 生成因果类外部数据读取纠偏协调计划。
3. 生成动态类、状态类、二次特征类、特征类逻辑纠偏计划。
4. 修改需求类计划，追加安全 / 服务方向只读函数缺口。
5. 修改方法类计划，追加方法节点按主键解析缺口。
6. 修改因果类功能函数补完计划，登记 20260620 外部数据读取缺口和返回点。
7. 修改计划索引，将第一优先切到动态类逻辑纠偏计划。
```

当前主动队列：

```text
第一优先计划：
    计划/20260620_动态类逻辑纠偏计划_v0.1.md

当前执行切片：
    动态类第一优先缺口：
        按主键解析动态节点
        读取动态因果生成输入

来源协调计划：
    计划/20260620_因果类外部数据读取纠偏协调计划_v0.1.md

原计划返回点：
    计划/20260619_因果类功能函数补完计划_v0.1.md
    P5 被动触发初始因果模板确保函数。
```

本轮边界：

```text
只生成和修订计划 / 断点清单。
未修改 动态类.h/.cpp、状态类.h/.cpp、二次特征类.h/.cpp、特征类.h/.cpp、需求类.h/.cpp、方法类.h/.cpp、因果类.h/.cpp。
未执行构建。
```

下一步：

```text
若用户要求继续执行，先执行 动态类逻辑纠偏计划 第一优先缺口。
新建函数前必须先扫描 说明书/函数功能说明文档.md 中 动态类 相关条目。
动态类第一优先闭合后，停止动态类计划，返回协调计划 P2。
```

禁止宣称：

```text
不宣称因果类外部数据读取纠偏完成。
不宣称因果类功能函数补完完成。
不宣称所有因果信息操作已统一走因果类。
不宣称动态类逻辑纠偏完成。
不宣称结构体清理完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 1. 计划文件

新增计划：

```text
计划/20260620_因果类外部数据读取纠偏协调计划_v0.1.md
计划/20260620_动态类逻辑纠偏计划_v0.1.md
计划/20260620_状态类逻辑纠偏计划_v0.1.md
计划/20260620_二次特征类逻辑纠偏计划_v0.1.md
计划/20260620_特征类逻辑纠偏计划_v0.1.md
```

修改计划：

```text
计划/计划索引.md
计划/20260619_因果类功能函数补完计划_v0.1.md
计划/20260619_需求类头文件结构体清理与目标逻辑纠偏计划_v0.1.md
计划/20260619_方法类逻辑纠偏计划_v0.1.md
```

## 2. 当前缺口顺序

```text
P1 动态类：读取动态因果生成输入。
P2 状态类：读取状态主体 / 状态特征 / 状态特征类型。
P3 二次特征类：读取状态迁移来源和可融合标量区间。
P4 特征类：读取特征语义类型。
P5 需求类：读取安全 / 服务方向。
P6 方法类：解析方法引用。
P7 因果类回迁：删除因果类中对应直接主信息读取和私有解析函数。
```

## 3. 工作区注意

```text
本轮开始时工作区已有其它修改和未跟踪文件。
本断点只记录本轮计划生成范围，不判断既有源码改动是否闭合。
提交或继续执行前必须重新检查 git status，并只处理当前切片相关文件。
```
