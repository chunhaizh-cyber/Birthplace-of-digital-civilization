# 20260620 特征类直接子节点明确函数 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_特征类逻辑纠偏计划_v0.1.md` 的 1.2，处理 `私有_直接基础信息子节点_按类型` 的泛型主信息穿透。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_特征类逻辑纠偏计划_v0.1.md
实施记录/20260620_特征类比较状态回迁状态类_Codex断点清单.md
说明书/函数功能说明文档.md
```

本切片范围：

```text
1. 扫描 私有_直接基础信息子节点_按类型 的全部调用点。
2. 若只服务 特征节点主信息类 / 抽象特征主信息类，删除任意 T主信息 模板穿透。
3. 改为明确的 特征类 内部直接子节点辅助函数。
4. 不处理 比较特征集合 直接 dynamic_cast。
5. 不处理 三维体素链读取 特征值类 节点 主信息.值。
```

已复核事实：

```text
1. rg -n "私有_直接基础信息子节点_按类型" 特征类.cpp 特征类.h
   结果：定义 1 处，调用 10 处。
2. 调用点实例化类型只有 抽象特征主信息类 和 特征节点主信息类。
3. 说明书/函数功能说明文档.md 未发现同名 私有_直接基础信息子节点_按类型；已有公开函数 获取子特征 / 查找子抽象特征 不覆盖匿名命名空间内部枚举需求。
```

验证前预测：

```text
1. 特征类.cpp / 特征类.h 不再命中 私有_直接基础信息子节点_按类型。
2. 特征类.cpp 不再在该位置命中 dynamic_cast<T主信息*>。
3. 宽口径审查仍会命中已登记的 比较特征集合 直接 dynamic_cast 和 三维体素链 主信息.值。
4. 函数说明文档生成器和 Debug x64 构建应通过。
```

## 1. 实施记录

```text
1. 删除匿名命名空间模板函数 私有_直接基础信息子节点_按类型<T主信息>。
2. 新增明确 helper：私有_直接特征子节点，仅枚举 特征节点主信息类 子节点。
3. 新增明确 helper：私有_直接抽象特征子节点，仅枚举 抽象特征主信息类 子节点。
4. 将 10 个调用点全部替换为明确 helper。
5. 不处理 比较特征集合 直接 dynamic_cast；不处理三维体素链 主信息.值。
6. 已运行 python .\tools\生成函数功能说明文档.py，新增 helper 后函数条目为 6880。
```

## 2. 验证结果

```text
1. rg -n "私有_直接基础信息子节点_按类型|dynamic_cast<T主信息" 特征类.cpp 特征类.h
   结果：无命中，退出码 1，符合预测。

2. rg -n "私有_直接特征子节点|私有_直接抽象特征子节点" 特征类.cpp 特征类.h
   结果：定义 2 处，调用 10 处；调用点已全部改为明确 helper。

3. rg -n "取主信息<[^>]+>|dynamic_cast<[^>]+>|->主信息|\\.主信息\\.值|状态节点主信息类|场景节点主信息类|动态节点主信息类|二次特征主信息类|因果主信息类|需求主信息类|方法主信息类" 特征类.cpp 特征类.h
   结果：原泛型模板 dynamic_cast 已消失；仍保留已登记残留：
        特征类.cpp:376、3491、3511、3532、3796 三维体素链读取 特征值类 节点 主信息.值。
        特征类.cpp:4129-4130 比较特征集合 中直接读取 节点->主信息 并 dynamic_cast<const 特征节点主信息类*>。
        特征类.cpp:97、118 以及其它 特征节点主信息类 / 抽象特征主信息类 取主信息 命中属于特征类职责内明确读取。

4. python .\tools\生成函数功能说明文档.py
   结果：退出码 0；源码文件 128；函数条目 6880；已生成 说明书/函数功能说明文档.md。

5. msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\20260620_特征类直接子节点明确函数_msbuild.log
   结果：退出码 0；已成功生成；0 个警告；0 个错误；生成 D:\鱼巢\x64\Debug\鱼巢.exe。
```

## 3. 当前判断

```text
1. 本切片已闭合：特征类中任意 T主信息 的直接子节点模板穿透已移除，改为特征 / 抽象特征明确 helper。
2. 不宣称特征类逻辑纠偏完成。
3. 下一步仍回到 特征类 1.2，优先处理 比较特征集合 中直接读取 节点->主信息 并 dynamic_cast<const 特征节点主信息类*>。
```
