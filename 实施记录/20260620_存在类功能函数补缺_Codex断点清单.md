# 20260620 存在类功能函数补缺 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_存在类逻辑纠偏计划_v0.1.md` 的 1.2，补存在类只读功能函数并回迁系统逻辑直读点。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_存在类逻辑纠偏计划_v0.1.md
实施记录/20260620_存在主信息字段判定表.md
存在类.h
存在类.cpp
世界树类.cpp
二次特征应用模块.ixx
自我线程模块.impl.cpp
```

本切片范围：

```text
1. 新增存在类只读函数：存在名称 / 类型 / 内部世界 / 概念模板引用 / 概念集快照 / 最近与上次观测位置缓存。
2. 优先回迁系统逻辑读取：世界树类型判断、世界树内部世界读取、自我线程名称 / 类型读取、二次特征空间关系观测缓存读取。
3. 显示 / 语言 / SQL 投影路径暂不扩大，只保留为展示读取，不作为机器判断依据。
4. 不删除存在主信息字段，不迁移观测缓存的权威事实来源。
```

验证前预测：

```text
1. 函数说明文档当前没有 1.2 候选只读函数，需要新增接口后重新生成。
2. 系统逻辑直读点应能改为调用存在类函数，不需要新增兼容层或自由文本业务字段。
3. 观测缓存读取函数只返回是否存在位置历史与位置值，不作为需求满足、任务完成或结算事实。
4. 运行函数说明文档生成器和 Debug x64 构建应通过；若构建失败，按第一处编译错误定位。
```

## 1. 实施记录

```text
1. 已在 存在类.h / 存在类.cpp 新增只读入口：
   是存在节点、读取存在名称、读取存在类型、读取内部世界、读取概念集快照、读取概念模板引用、读取最近观测位置缓存、读取上次观测位置缓存。
2. 已将 世界树类::取或创建子存在_按类型并按特征I64 的类型读取回迁到 存在类::读取存在类型。
3. 已将 世界树类::取存在内部世界 回迁到 存在类::读取内部世界。
4. 已将 二次特征生成模块::刷新场景存在空间关系二次特征 的最近观测位置读取回迁到 存在类::读取最近观测位置缓存。
5. 已将 自我线程 私有_线程存在匹配线程状态消息 的存在名称 / 类型读取回迁到 存在类::读取存在名称 / 读取存在类型。
6. 已重新生成 说明书/函数功能说明文档.md。
```

## 2. 验证结果

```text
1. 直读残留检索：
   rg 输出已写入 日志/20260620_存在类功能函数补缺_直读残留.txt，共 76 行。
   本切片回迁目标 世界树类.cpp:1724 / 世界树类.cpp:2204 / 二次特征应用模块.ixx:3516,3521 / 自我线程模块.impl.cpp:12675,12682 已不再命中旧直读。
   剩余命中集中在存在类内部、基础信息类型判断、控制面板 / 语言展示读取，按本切片边界暂不扩大。

2. 函数说明文档：
   python .\tools\生成函数功能说明文档.py
   退出码：0
   日志：日志/20260620_存在类功能函数补缺_函数文档.log
   关键输出：源码文件 128；函数条目 6889。

3. 构建：
   msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
   退出码：0
   日志：日志/20260620_存在类功能函数补缺_msbuild.log
   关键输出：已成功生成。
```

## 3. 当前判断

```text
1. 本切片已闭合：存在类只读功能函数已补齐，系统逻辑优先直读点已回迁。
2. 显示 / 语言 / SQL 投影直读仍按计划保留为后续 1.4 审查范围，不作为本切片机器判断依据。
3. 下一步进入 存在类 1.3：非存在主信息直接操作审查。
4. 不宣称存在类逻辑纠偏完成。
```
