# 需求树简化与内部治理闭环 P3a Codex断点清单

日期：2026-06-30

## 0. 恢复摘要

当前切片：R24 P3a，主动因果信息生成链路只读对账。

本轮目标：

```text
在不改代码、不新增结构体、不创建世界树事实的前提下，
核对主动方法动作动态 D0、动作致变因果生成、任务筹办消费和方法结构缺口分类是否已有闭环锚点。
```

当前状态：只读对账已完成主要代码锚点，尚未进入 P3b 代码补齐。

## 1. 已读取依据

```text
AGENTS.md（由用户提供）
docs/00_文档总索引.md
规范/规范目录.md
规范/禁止性规范20260611.md
docs/工程图谱/00_图谱总索引.md
docs/工程图谱/05_规则原子索引.md 命中项
计划/计划索引.md
计划/20260630_需求树简化生成与内部治理闭环计划_v0.1.md P3
规范/方法动作动态硬规则规范20260512.md 命中段
规范/动作动态即因果账本规范20260512.md 命中段
规范/因果补完流程规范20260516.md 命中段
规范/因果信息用途与用法规范20260516.md 命中段
规范/任务筹办与执行边界规范20260512.md 命中段
```

## 2. 只读证据卡

### 2.1 D0 方法动作动态

```text
入口：
    动态类::创建方法动作动态
    世界树类::创建方法动作动态

证据：
    动态类.cpp:1491-1507
        创建方法动作动态必须有场景、动态主体、动态特征和允许生成方法动作动态(来源动作)。
    动态类.cpp:1538-1546
        写入来源动作、来源动作名、来源输入场景、来源输出场景、来源执行成功、来源错误码、初始状态和结果状态。
    动态类.cpp:1696-1699
        允许生成方法动作动态依赖来源动作具备可执行动作入口。
    世界树类.cpp:3609-3625
        世界树入口只是转发到动态服务，不另造动作来源。

判断：
    D0 入口已具备方法来源硬门槛和前后状态 / 场景字段。
    线程没有在该入口中被当成动作来源。
```

### 2.2 主动动作致变因果生成

```text
入口：
    因果类::生成因果信息
    因果类::私有_生成因果信息并返回模板
    世界树类::生成因果信息

证据：
    因果类.cpp:2754-2768
        从动态读取初始状态、结果状态、来源动作、来源动作名、动态类型和动态层级。
    因果类.cpp:2802-2814
        形成动作语义键；无动作状态变化动态不升格为动作。
    因果类.cpp:2839-2889
        先生成或命中状态变化关系并追加证据动态。
    因果类.cpp:2892-2953
        有动作时再生成或命中动作致变关系，写入因方法模板、因动作语义键、因动作名称，并追加证据动态。
    因果类.cpp:2967-2968
        优先返回动作致变关系，其次返回状态变化关系。
    世界树类.cpp:3800-3815
        世界树入口只是调用因果服务生成因果信息。

判断：
    主动动作致变因果生成链路已有主路径。
    单次样本仍只是证据动态追加，不等于已验证稳定因果。
```

### 2.3 主动因果动作来源读取

```text
入口：
    因果类::查询因果动作来源
    世界树类::查询因果动作来源

证据：
    因果类.cpp:1753-1782
        从因果主信息读取因方法模板、因动作语义键或因动作名称。
    世界树类.cpp:3888-3896
        世界树只读转发，不创建或修改节点。

判断：
    已有只读读取入口，但目前只返回方法模板和动作语义，不负责判定主动 / 被动、可任务化、验证状态或结构缺口分类。
```

### 2.4 任务筹办消费

```text
入口：
    任务模块::筹办::吸收因果方法就绪缺口_筹办查询
    任务模块::筹办::填充任务方法候选集
    任务模块::筹办::解析任务目标方法
    任务模块::筹办::候选方法不可就绪状况

证据：
    任务模块.筹办.ixx:1638-1657
        筹办只读查询因果动作来源；没有方法模板则返回。
    任务模块.筹办.ixx:3898-3970
        候选集先按已绑定方法和目标特征结果能力填充；没有候选时挂起，不创建占位方法头。
    任务模块.筹办.ixx:5301-5321
        先填充候选方法集，再从候选集中选择当前方法。
    任务模块.筹办.ixx:5566-5607
        已区分缺方法动作、方法缺可执行入口、方法缺条件结果对等结构缺口。
    任务模块.筹办.ixx:5983-6013
        候选方法不可就绪时返回精确方法结构缺口。

判断：
    筹办已遵守候选集先填充再选择。
    被动状态变化因果当前不会从候选集路径直接变成候选方法。
    但筹办侧尚未有统一的“主动因果可任务化裁决”只读入口来封装主动 / 被动、验证状态和结构缺口。
```

## 3. 当前判断

```text
1. D0 方法动作动态生成入口存在，并带方法来源硬门槛。
2. 因果生成主路径已有状态变化关系 + 有动作时动作致变关系双账生成逻辑。
3. 筹办侧能读取因果方法模板并按候选方法结构缺口返回回执。
4. 当前主要缺口不是新增持久结构体，而是缺少因果类普通只读治理函数：
   主动因果来源完整性检查；
   主动 / 被动动作语义裁决；
   主动因果验证状态读取；
   主动因果可任务化裁决；
   主动因果同源变化证据组可回解检查。
5. P3b 应先补只读函数，不改变需求入树和任务筹办行为。
```

## 4. 下一步预测

```text
P3b 代码补齐范围优先限定在 因果类.h / 因果类.cpp。
如需任务筹办消费统一裁决，先提供因果类只读函数，后续 P5 再接入任务筹办。
不新增持久业务结构体；若需要返回多项结果，优先使用现有枚举 / bool / 引用参数 / 局部临时诊断结构。
新增函数前必须检索 说明书/函数功能说明文档.md，避免重复函数。
新增函数后运行 tools/生成函数功能说明文档.py。
```

## 5. 禁止宣称项

```text
不得宣称主动因果闭环已完成。
不得宣称需求树简化生成已闭环。
不得宣称内部治理闭环已完成。
不得把状态变化因果直接作为方法候选。
不得把被动动作动态学习成主动方法。
不得把单次动态样本直接升格为已验证稳定因果。
```
