# 20260626 P13b 饱和绝对差工具迁移 Codex断点清单

## 0. 恢复摘要

当前切片进行中。目标是将 `本能动作类.ixx::本能动作模块_detail::饱和绝对差(I64, I64)` 收束到 `全局共享函数类.ixx`，删除本能动作模块本地重复工具函数，并保持原有饱和绝对差语义。

依据：

```text
计划/计划索引.md：当前主动队列为 P13b，从剩余 B 类低风险候选中选择小函数族。
计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md：B 类函数迁移前必须查全局共享函数类和函数说明文档，已有同功能则复用 / 删除重复定义，没有才迁移。
实施记录/20260624_P13_函数自定义主信息分类候选表.tsv：`饱和绝对差` 为 B 类低风险待复核候选。
全局共享函数类.ixx 文件头规则：无领域自定义主信息 / 领域类型参与的通用函数可进入本模块。
```

当前判断：

```text
`饱和绝对差(I64, I64)` 只依赖 I64 和数值边界判断，不读取 / 写入项目结构。
全局已有 `饱和减少`、`饱和绝对值I64` 和非饱和 `绝对差I64`；非饱和函数不等价，不能复用替代。
可新增全局 `饱和绝对差I64(I64, I64)`，由 `饱和减少` + `饱和绝对值I64` 表达饱和差值。
`本能动作类.ixx` 当前缺少文件头部规则注释模块，本切片需先补最小模块。
```

预期验证：

```text
1. `本能动作类.ixx` import 全局共享函数类。
2. 本地 `本能动作模块_detail::饱和绝对差` 删除。
3. 原调用点改为 `饱和绝对差I64`。
4. 运行 `python .\tools\生成函数功能说明文档.py`。
5. Debug x64 构建通过。
6. 复扫确认本地重复定义清零，`饱和绝对差I64` 进入全局共享函数类、调用点和函数说明文档。
```

禁止宣称：不宣称 P13 完成，不宣称函数去重全部完成，不宣称自我苏醒完成或初步成熟完成。

## 1. 构建前状态

已执行：

```text
1. `全局共享函数类.ixx` 新增 `饱和绝对差I64(I64, I64)`，复用 `饱和减少` 和 `饱和绝对值I64`。
2. `本能动作类.ixx` 补最小文件头部规则注释模块。
3. `本能动作类.ixx` import 全局共享函数类。
4. 删除本地 `本能动作模块_detail::饱和绝对差`。
5. 两个调用点改为 `饱和绝对差I64`。
6. 已运行 `python .\tools\生成函数功能说明文档.py`，源码文件 127 个，函数条目 7213。
```

复扫证据：

```text
`本能动作类.ixx` 中 `饱和绝对差` 本地定义已清除。
`饱和绝对差I64` 已进入 `全局共享函数类.ixx`，并被 `本能动作类.ixx` 两个调用点使用。
```

构建预测：

```text
Debug x64 构建应通过；若失败，优先检查 C++20 module import 位置或全局共享函数导出可见性。
```

## 2. 闭合结果

当前切片已闭合。

实现结果：

```text
1. 新增全局定义：`全局共享函数类.ixx::饱和绝对差I64(I64, I64)`。
2. 删除本地重复定义：`本能动作类.ixx::本能动作模块_detail::饱和绝对差`。
3. `本能动作类.ixx` 两个调用点改为全局 `饱和绝对差I64`。
4. `本能动作类.ixx` 补最小文件头部规则注释模块，并导入 `全局共享函数类`。
```

验证证据：

```text
python .\tools\生成函数功能说明文档.py 成功；源码文件 127 个，函数条目 7213。
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo 成功；0 警告，0 错误。
复扫确认：`本能动作类.ixx` 中 `饱和绝对差` 本地定义已清除。
复扫确认：`饱和绝对差I64` 已进入 `全局共享函数类.ixx`、`本能动作类.ixx` 调用点和 `说明书/函数功能说明文档.md`。
```

下一步：

```text
返回 P13b，从剩余 B 类低风险候选中继续选择一个不在当前脏改文件中的小函数族。
```
