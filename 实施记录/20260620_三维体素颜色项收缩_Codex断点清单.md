# 20260620 三维体素颜色项收缩 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_特征类逻辑纠偏计划_v0.1.md` 的 1.1 P2.3，处理 `结构_三维体素颜色项 / 体素颜色` 删除或收缩候选。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
规范/特征类规范20260429.md
规范/主信息字段与结构新增准入约束规范20260519.md
计划/计划索引.md
计划/20260620_特征类逻辑纠偏计划_v0.1.md
实施记录/20260620_特征类消费待确认结构审查_Codex断点清单.md
实施记录/20260620_三维体素二分层摘要删除_Codex断点清单.md
```

本切片范围：

```text
1. 删除无消费证据的 结构_三维体素颜色项。
2. 删除 结构_三维体素轮廓融合结果.体素颜色 明细向量。
3. 保留 颜色体素数量，改为直接由有颜色体素计数得到。
4. 不处理 常用抽象特征初始化结果。
```

验证前预测：

```text
1. 删除后 rg 不应命中 结构_三维体素颜色项 或 体素颜色。
2. 颜色体素数量 仍由结构化计数生成。
3. Debug x64 构建应通过。
```

## 1. 实施记录

```text
已删除：
1. 特征类.h 中 结构_三维体素颜色项 定义。
2. 结构_三维体素轮廓融合结果.体素颜色 明细向量。
3. 特征类.cpp 中 RGBA 累积、平均颜色计算和 体素颜色.push_back。

已保留：
1. 结构_三维体素轮廓融合结果.颜色体素数量。
2. 特征类.cpp 中通过 颜色数量[idx] > 0 直接累计 颜色体素数量。
```

## 2. 验证结果

```text
rg -n "结构_三维体素颜色项|体素颜色" 特征类.h 特征类.cpp
结果：无命中，退出码 1。

rg -n "颜色体素数量" 特征类.h 特征类.cpp
结果：
    特征类.h:182 保留字段。
    特征类.cpp:3762 由结构化计数写入。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\20260620_三维体素颜色项收缩_msbuild.log
结果：退出码 0；日志显示 0 个警告、0 个错误。
```

当前判断：

```text
结构_三维体素颜色项 / 体素颜色 收缩闭合。
本切片不宣称特征类结构体清理完成。
下一步继续 P2.3：处理 常用抽象特征初始化结果 收缩候选。
```
