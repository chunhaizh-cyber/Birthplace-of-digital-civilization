# 20260630 已确认存在体素融合分解 P1 Codex断点清单

## 0. 恢复摘要

当前切片：执行 `计划/20260630_已确认存在体素融合分解施工计划_v0.1.md` 的 P1。

目标：

```text
定义新增观察体素特征值的消费口径；
定义体素加工任务 / 候选结果包的最小结构锚点；
确保同一观察特征值只触发一次当前粗体素写入。
```

本切片不实现：

```text
体素加工线程；
深度融合候选算法；
彩色分解算法；
子存在局部体素模型；
提交入口版本闸门完整链。
```

禁止宣称：

```text
完整体素模型已经形成；
体素融合已经完成；
彩色分解已经完成；
体素加工线程已经接入；
视觉系统完成；
自我苏醒完成或初步成熟完成。
```

## 1. 依据

```text
规范/已确认存在体素融合与分解规范20260630.md
详细设计/已确认存在体素融合与分解实现方案20260630.md
计划/20260630_已确认存在体素融合分解施工计划_v0.1.md
docs/工程图谱/05_规则原子索引.md
```

命中规则：

```text
RULE-VOXEL-FUSION-001：体素加工只由新增观察证据触发
RULE-VOXEL-FUSION-008：体素加工线程只能返回候选
```

## 2. 已改文件

```text
自我动作实现.外设模块.ixx
```

## 3. 代码改动口径

在已确认观察存在节点上新增结构化体素观察证据锚点：

```text
体素观察证据ID
体素观察证据消费状态
体素观察证据来源报告ID
体素观察证据来源簇ID
体素加工基准体素版本
体素加工候选结果包状态
```

当前 P1 只把粗体素写入路径改成幂等消费：

```text
来源报告ID + 像素簇ID -> 体素观察证据ID
同一体素观察证据ID且消费状态为终止态 -> 不重复写入三维体素模型
新证据 -> 写入观察证据结构特征 -> 写入当前粗体素 -> 标记粗体素已写入或拒绝
```

## 4. 验证预测

静态预测：

```text
同一观察报告和同一像素簇第二次进入写入函数时，`体素观察证据已消费` 返回 true，函数返回 0，不重复写入体素链。
无有效来源报告ID或像素簇ID时，函数不生成体素观察证据，也不触发粗体素写入。
体素尺寸非法、缺体素特征或占据位块为空时，消费状态写为拒绝，避免同一非法证据反复触发。
```

构建预测：

```text
Debug x64 构建应通过；若失败，优先检查新增函数声明、调用签名和函数说明文档生成后的行号变化。
```

## 5. 待运行命令

```powershell
python .\tools\生成函数功能说明文档.py
git diff --check -- 自我动作实现.外设模块.ixx 说明书/函数功能说明文档.md 实施记录/20260630_已确认存在体素融合分解_P1_Codex断点清单.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

## 6. 验证结果

实际执行：

```powershell
python .\tools\生成函数功能说明文档.py
```

结果：

```text
退出码 0。
已生成：D:\鱼巢\说明书\函数功能说明文档.md
源码文件：129
函数条目：7416
```

第一次构建：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

结果：

```text
退出码 1。
错误集中在 自我动作实现.外设模块.ixx:7627 / 7629 / 7651。
原因：P1 新增代码位于本模块已有 `读取基础节点I64` 定义之前，缺少前向声明。
处理：补充本模块内部 `读取基础节点I64` 前向声明，不新增公共函数，不改变读取语义。
```

第二次构建：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

结果：

```text
退出码 0。
Debug x64 构建通过。
0 个警告，0 个错误。
输出：D:\鱼巢\x64\Debug\鱼巢.exe
```

最终静态检查：

```powershell
git diff --check -- 自我动作实现.外设模块.ixx 说明书/函数功能说明文档.md 实施记录/20260630_已确认存在体素融合分解_P1_Codex断点清单.md
```

结果：

```text
退出码 0。
无空白错误。
仅有 说明书/函数功能说明文档.md 的 CRLF/LF 工作区提示。
```

## 7. 当前结论

```text
P1 已完成当前代码切片：
已确认观察存在的粗体素写入路径具备观察证据 ID、来源报告 ID、来源簇 ID、消费状态、基准版本锚点和候选结果包状态锚点。
同一来源报告 + 像素簇形成的观察证据进入终止消费状态后，不再重复触发当前粗体素写入。
```

边界：

```text
本切片只完成数据契约和消费状态锚点。
`体素加工基准体素版本` 当前使用上一体素观察证据 ID 作为 P1 阶段的最小版本锚点；P5 仍需实现正式父体素版本闸门。
未实现体素加工线程、融合算法、彩色分解、子存在局部体素模型和提交入口版本裁决。
```
