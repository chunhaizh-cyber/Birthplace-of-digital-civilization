# 20260624 P13b 外设几何数学工具迁移 Codex断点清单

## 0. 恢复摘要

当前切片：P13b 第五小切片，迁移 B 类低风险外设几何数学 helper。

已完成：

```text
1. 已在 全局共享函数类.ixx 新增并导出：
   限制I64(I64 值, I64 下界, I64 上界) noexcept
   计算比例万分比I64(I64 分子, I64 分母) noexcept
   ROI面积(I64 最小X, I64 最大X, I64 最小Y, I64 最大Y) noexcept
   ROI重叠面积(...)
   正范围(I64 最小值, I64 最大值) noexcept
   投影尺寸(I64 最小值, I64 最大值, I64 兜底) noexcept
   最大3(I64 a, I64 b, I64 c) noexcept
   范围轴重叠率(I64 aMin, I64 aMax, I64 bMin, I64 bMax) noexcept
2. 已删除 自我动作实现.外设模块.ixx 内对应本地定义。
3. 原调用点继续调用统一全局函数。
4. 已运行 python .\tools\生成函数功能说明文档.py。
5. 函数说明文档已登记全局定义。
```

验证：

```text
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo
已成功生成，0 个警告，0 个错误。
```

下一步：

```text
继续 P13b，从 P13a 表中选择下一个不在当前脏改文件中的 B 类低风险小函数族；
优先处理只依赖 I64 / std / 基础数据类型的 helper。
```
