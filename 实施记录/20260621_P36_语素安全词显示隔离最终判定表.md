# P36 语素安全词显示隔离最终判定表

## 0. 结论

P8 最终复核未发现新的 `语素集.安全获取词(...)` 词面进入机器查找、候选生成、需求生成、方法选择或排序决策路径。

已确认需要改的机器路径已在 P27-P35 子切片中改为结构化入口、具体特征节点、方向约束、条件定义节点主键或语素入口身份比较。

本轮剩余命中归类为：

```text
1. SQL 投影显示文本。
2. 控制面板显示文本。
3. 日志 / 错误诊断文本。
4. 语言表达文本。
5. 需求列表或需求树 SQL 行显示名称。
```

## 1. 扫描证据

已运行扫描：

```powershell
rg -n "语素集\.安全获取词\(" -g "*.cpp" -g "*.h" -g "*.ixx" > 日志/20260621_P36_安全词面调用扫描.txt
rg -n "安全获取词\([^\r\n]*(==|!=|<|>|find\(|contains\(|count\(|push_back|insert|emplace|return)" -g "*.cpp" -g "*.h" -g "*.ixx" > 日志/20260621_P36_疑似机器判断文本扫描.txt
rg -n "私有_安全词\(|私有_需求类型标题\(|私有_抽象子节点类型文本_线程侧\(" 自我线程模块.impl.cpp > 日志/20260621_P36_自我线程安全词包装扫描.txt
rg -n "(const auto|auto|std::string) [^=]*(文本|词面|标题|名称|类型|原因|键)[^=]*= .*安全获取词|= 私有_安全词\(|= 私有_需求类型标题\(|= 私有_抽象子节点类型文本_线程侧\(" -g "*.cpp" -g "*.h" -g "*.ixx" > 日志/20260621_P36_安全词结果赋值扫描.txt
rg -n "(目标特征文本|条件键|检查宿主语义|目标状态文本|缺口需求名称文本|事实类型|目标语义要求|目标特征标题|目标特征名称|节点名称)" -g "*.cpp" -g "*.h" -g "*.ixx" > 日志/20260621_P36_安全词派生字段使用扫描.txt
```

扫描计数：

```text
安全词面调用扫描：44 行。
疑似机器判断文本扫描：30 行，全部落在语言表达文件。
自我线程安全词包装扫描：27 行。
非语言安全词面剩余：14 行。
安全词结果赋值扫描：39 行。
安全词派生字段使用扫描：76 行。
```

## 2. 非语言剩余命中归类

| 位置 | 当前用途 | 判定 |
| --- | --- | --- |
| `世界树类.cpp:319` | `私有_世界树SQL入口文本` | SQL 投影显示文本，另有入口主键字段承载身份。 |
| `任务类.cpp:805` | `私有_任务SQL入口文本` | SQL 投影显示文本，另有引用主键字段承载身份。 |
| `方法类.cpp:1075` | `私有_方法SQL入口文本` | SQL 投影显示文本，另有方法节点 / 动作名入口主键承载身份。 |
| `控制面板类.cpp:545` | `私有_词文本` | 控制面板显示文本。 |
| `需求类.cpp:147` | 需求树 SQL 行 `节点名称` | SQL 行显示名称；需求结构字段仍由节点主键、目标特征入口和状态引用承载。 |
| `鱼巢.cpp:1640` | 控制命令显示文本 | 命令输出显示文本。 |
| `自我动作实现模块.ixx:6674` | `语素日志文本` | 日志文本。 |
| `自检线程模块.impl.cpp:505` | `私有_安全词_自检` | 自检日志文本。 |
| `自我线程模块.impl.cpp:85` | `私有_安全词` | 自我线程内部显示 / 日志包装；机器判断已按结构化入口复核。 |
| `自我动作实现.外设模块.ixx:11775/11779/11793/11800/19784` | 本能动作结果日志 | 日志文本；运行结果状态、失败原因入口和输出特征仍由结构字段承载。 |

## 3. 自我线程包装复核

- `目标特征文本` 只进入自检候选因果投影裁决日志和 `父裁决.原因` 文本；`父裁决.域 / 父需求 / 来源因果主键` 承载机器分流结果。
- 根闭环信息位查找使用 `根特征类型 + 事实特征类型 + 目标值`；`闭环位置 / 事实类型 / 目标语义要求` 只用于日志输出。
- 需求列表排序使用权重、目标特征入口主键和需求节点主键；`目标特征名称` 只用于列表显示。
- 根闭环宿主解析使用 `检查宿主类型` 同词存在概念入口；`检查宿主语义` 只保留为日志 / 显示文本和手工条件入口种子。

## 4. 后续

P8 可按最终复核闭合。语素类计划仍保留 `自我类.cpp::私有_语素入口相同` 指针级比较局部堵点，下一切片应回到该堵点，补齐语素类侧指针级同一性入口或形成明确豁免。
