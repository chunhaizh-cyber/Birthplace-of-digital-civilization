# 20260601 安全遗传因果模板多因侧封装实施记录

## 依据

执行 `计划索引.md` 当前主动队列 S2 / P1：

- `计划/20260601_安全遗传因果模板组纠偏计划_v0.1.md`
- `实施记录/20260601_安全遗传因果模板组_G1_G6代码映射表.md`

目标是复用现有 `因果主信息类.因比较模板`，为 G2 / G4 / G5 / G6 提供同一主果下多个因侧比较的封装；不新增 `结构_遗传因果模板`。

## 修改文件

- `自我线程模块.impl.cpp`

## 修改内容

1. 新增因侧比较集合整理能力：
   - 去掉空节点。
   - 按指针或主键去重。

2. 新增多因侧查找封装：
   - 仍要求 `来源类型=遗传`。
   - 仍按 `主果比较模板` 匹配主果。
   - 对多因侧初始化使用精确因比较集合查重。
   - 对旧单因侧查找保留原行为，不破坏现有调用。

3. 新增多因侧确保封装：
   - 同一因果模板可写入多个 `因比较模板`。
   - 日志新增 `因比较数量` 和因比较主键列表。
   - 日志继续保留 `直接生成需求=0 / 直接创建任务=0 / 直接结算价值=0`。

4. 保留原单因侧函数签名：
   - 现有调用不需要改动。
   - 原单因侧入口内部转调多因侧封装。

## 验证

预测：

```text
原有单因侧遗传因果模板调用应继续编译；
多因侧封装不新增主信息结构；
构建应通过。
```

实际：

```text
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

结果：

```text
退出码 0
0 个错误
1 个警告：LNK4075 忽略 /EDITANDCONTINUE，由 /INCREMENTAL:NO 触发
```

该警告与本次修改无直接关系，构建通过。

## 后续

S2/P0-P1 已完成。继续推进 S3 前，仍需记住当前剩余问题：

```text
代码尚缺最小自我场景绝对坐标 / 姿态 / 自我视图坐标系 / 边界完整性承载和合法生产者。
```
