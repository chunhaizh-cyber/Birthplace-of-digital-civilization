# 20260620 场景子节点枚举模板收口 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.3 P4.5，收口 `私有_直接基础信息子节点_按类型` 的通用主信息模板穿透。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
```

## 1. 问题锚点

```text
场景类.cpp 当前有模板函数 私有_直接基础信息子节点_按类型<T主信息>，
内部用 dynamic_cast<T主信息*>(当前->主信息) 过滤任意主信息类型。

调用点只有：
    场景类::获取子场景
    场景类::获取子存在
    场景类::获取子特征
```

当前判断：

```text
该模板实际只服务场景容器枚举，不需要保留任意 T主信息 的通用穿透入口。
改为与特征类.cpp 既有局部模式一致的 私有_直接基础信息子节点，只枚举直接子节点；
三个公开函数各自显式过滤场景 / 存在 / 特征节点主信息。
```

修改范围：

```text
场景类.cpp
说明书/函数功能说明文档.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
```

验证前预测：

```text
1. 场景类.cpp 不再包含 私有_直接基础信息子节点_按类型。
2. 场景类.cpp 不再包含 dynamic_cast<T主信息*>。
3. 获取子场景 / 获取子存在 / 获取子特征 仍只返回对应类型直接子节点。
4. 新增 / 删除私有函数后需重生成函数说明文档。
5. Debug x64 构建应通过。
```

## 2. 当前执行进展

已修改：

```text
1. 删除 场景类.cpp 私有_直接基础信息子节点_按类型<T主信息>。
2. 新增局部 helper 私有_直接基础信息子节点，只枚举直接子节点，不解释任意主信息类型。
3. 获取子场景 / 获取子存在 / 获取子特征 分别显式过滤对应主信息类型后返回对应节点。
```

已验证：

```text
rg "私有_直接基础信息子节点_按类型|dynamic_cast<T主信息|私有_直接基础信息子节点|获取子场景|获取子存在|获取子特征|取主信息<场景节点主信息类>|取主信息<存在节点主信息类>|取主信息<特征节点主信息类>" 场景类.cpp 场景类.h
结果：通用模板名和 dynamic_cast<T主信息*> 无命中；三个公开函数保留显式类型过滤。

python .\tools\生成函数功能说明文档.py
结果：exit=0，输出见 日志/20260620_场景子节点枚举模板收口_函数文档.log。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：exit=0，构建日志见 日志/20260620_场景子节点枚举模板收口_msbuild.log。
```

禁止宣称：

```text
不宣称 P4 非场景主信息直接操作审查完成。
不宣称场景类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
