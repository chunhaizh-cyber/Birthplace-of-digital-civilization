# 20260606_外设四本能方法P8b真实D455单帧材料补充验证记录

## 切片

```text
计划：计划/20260605_外设四本能方法需求树生成因果链修订计划_v0.1.md
切片：P8b 真实 D455 单帧材料入口补充验证
目标：用真实 D455 单帧入口补充外设材料证据，同时明确它不能替代任务管理 -> 自我方法 -> 提交入口自然链验收。
```

## 验证前预测

```text
若 D455 硬件和 RealSense 运行条件可用：
    命令应输出真实材料包证据卡；
    日志应出现 命令行相机控制/单帧采集、D455材料包证据卡、逐簇识别样本卡和扫描变化样本卡；
    该路径只证明真实外设材料入口可用。

若 D455 硬件或运行条件不可用：
    命令应返回非零退出码；
    日志应记录真实失败原因；
    不伪造样本，不宣称四方法链路通过。
```

## 计划命令

```powershell
.\x64\Debug\鱼巢.exe --d455-capture-once
```

## 边界

```text
D455 单帧命令行入口不是任务管理 -> 自我方法 -> 提交入口自然链；
即使采集成功，也只能作为真实外设材料证据；
四方法自然业务链仍必须由任务、方法执行、提交入口、动作动态和来源动作动态日志证明。
```

## 执行结果

```text
命令：
    .\x64\Debug\鱼巢.exe --d455-capture-once

输出文件：
    运行输出/20260606_外设四本能方法P8b_d455_capture_once.log

退出码：
    0

最新运行日志：
    日志/鱼巢_run_20260606.低值g
```

## 真实材料证据

```text
命令行相机控制/单帧采集/打开：
    成功=是；
    相机已打开=是；
    原因=无。

命令行相机控制/单帧采集/采集：
    成功=是；
    尺寸=640x480；
    深度帧号=1；
    彩色帧号=10；
    像素特征数量=307200；
    深度有效像素数量=204504；
    点云有效像素数量=204504；
    观察帧组帧数=3；
    融合深度有效率=6657；
    空间候选数量=22。

D455材料包证据卡：
    报告ID=1；
    来源外设=双目相机/D455深度相机；
    报告类型=逐簇识别报告；
    观察簇=22；
    材料句柄总数=160；
    采集成功=1；
    短期观察材料页提交=1。

D455逐簇识别样本卡：
    观察像素簇=22；
    有效观察区域=22；
    未识别区域=21；
    候选存在=22；
    报告证据不足=1；
    只读边界=材料归属和存在候选,不确认世界存在。

D455扫描变化样本卡：
    报告ID=2；
    状态=首帧基准；
    上一状态=缺可比历史；
    发现候选=22；
    变化簇=0；
    首帧基准=1；
    事实口径=首次扫描仅可形成发现存在或发现特征候选；
    只读边界=扫描变化材料,不确认全场景明确,不写安全结论。
```

## 核对判断

```text
P8b 已证明真实 D455 单帧材料入口可用；
P8b 没有触发 自我_验证外设观察像素簇候选 / 自我_识别外设观察材料 / 自我_扫描已识别区域 / 自我_跟踪指定存在；
P8b 没有触发四个提交入口或 来源动作动态；
P8b 不证明四方法自然业务链已通过。
```

## 下一步

```text
进入 P8c：延长 --self-check-log 真实运行窗口。
目的只是排除 P8a 等待窗口过短；若仍未自然触发四方法 / 提交入口，则把自然链验证转待条件，不再用命令行 D455 材料入口替代。
```
