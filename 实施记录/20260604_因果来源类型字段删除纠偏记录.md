# 因果来源类型字段删除纠偏记录

日期：2026-06-04

## 1. 用户纠偏

```text
来源类型=遗传 这个可以删除。
因果信息来源只有一个就是自我的认知过程，没必要单独列出来，没有任何意义。
```

## 2. 口径收束

```text
因果信息来源：
    只有自我的认知过程。

不再保留：
    枚举_因果来源类型；
    因果主信息类.来源类型；
    来源类型=遗传；
    因果来源类型日志字段；
    按来源类型统计遗传 / 推断 / 执行 / 观察因果。

保留：
    动态来源类型；
    二次特征来源类型；
    因果主信息中的形态、锚点、层级、因比较、果比较、方法动作线索、证据动态样本、基础计数和已验证标记。
```

## 3. 代码修改

```text
基础信息主信息类.h
    删除 枚举_因果来源类型；
    删除 因果主信息类.来源类型。

因果类.h / 因果类.cpp / 世界树类.h / 世界树类.cpp
    删除 按动态创建因果信息 / 按动态补全两层因果 / 按动态集合补全两层因果 的因果来源类型参数；
    动态沉淀因果只记录因果结构、证据动态样本和基础计数。

任务模块.筹办.ixx
    删除 临时因果路径项.来源因果类型；
    删除 因果来源类型文本和命中因果来源类型日志；
    将 遗传因果数量 改为 初始因果模板数量；
    初始模板判据改为 抽象因果 + 未验证 + 证据动态样本为空。

自我线程模块.impl.cpp
    删除 来源类型=遗传 写入和筛选；
    安全风险树、被动触发、结算前置读取改为按因果模板结构读取；
    日志改写为 因果信息来源=自我认知过程 / 初始因果模板。

控制面板类.cpp
    删除因果来源类型显示字段；
    动态来源类型显示保留。
```

## 4. 当前剩余口径

```text
出生期初始因果模板：
    不是因果来源类型；
    只是证据状态和生命周期阶段；
    可被后天证据强化、削弱、分化或覆盖。

本能方法因果信息：
    只能由学习及实际使用生成；
    不登记为出生期初始因果模板。

被动动作触发结构：
    可登记为出生期初始因果模板；
    运行期被动动作动态只负责命中、增强或暴露缺口。
```

## 5. 验证状态

```text
已运行：
    git diff --check
        通过。

    Debug x64 构建
        通过；仅有既有 /EDITANDCONTINUE 与 /INCREMENTAL:NO 链接警告。

    --self-check-log
        退出码 0。

最新日志：
    日志/鱼巢_run_20260604.低值g

本轮 12:58 运行段核对：
    来源类型=遗传 = 0；
    因果来源类型 = 0；
    遗传因果= = 0；
    推断生成因果= = 0；
    命中因果来源类型 = 0；
    因果信息来源=自我认知过程 > 0；
    初始因果模板 > 0；
    ERR / FTL / 访问冲突 / Exception = 0。
```
