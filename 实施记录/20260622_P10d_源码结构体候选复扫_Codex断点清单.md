# 20260622 P10d 源码结构体候选复扫 Codex断点清单

## 0. 恢复摘要

当前主动计划：`计划/计划索引.md` 第 1 章 `P10 项目结构体审计与清理计划 - 源码结构体候选复扫与下一专项选择`。

当前切片：

```text
P10d：源码结构体候选复扫与下一专项选择。
先重新生成或复核当前源码结构体低命中 / 直接引用候选，排除已闭合对象、详细设计草案、待条件项和 P11 已暂止函数候选。
只选择一个无读取、无合同职责、无真实运行路径依赖的单一结构体或结构族进入最小闭合切片。
```

已复核依据：

```text
规范/禁止性规范20260611.md：第 2 章项目通用数据结构闭环。
计划/计划索引.md：当前主动队列 P10d。
计划/20260618_项目结构体审计与清理计划_v0.1.md：P3/P4 结构体审计和分类处理口径。
用户补充删除闸门：只有定义和赋值 / 成员声明、无读取 / 合同职责 / 真实运行路径依赖的结构体才直接删除。
```

本轮预测：

```text
1. live 复扫会排除已闭合对象和详细设计草案，剩余候选多落在协议、外设材料、控制面板 DTO、RAII 守卫或真实运行证据结构。
2. 若发现只剩定义 / 成员声明 / 赋值且无读取的结构体，进入一个最小删除切片。
3. 若没有无堵点删除对象，只形成短表并更新索引/断点，不硬删。
```

禁止范围：

```text
1. 不处理 `详细设计/因果类用途接口草案20260516.ixx`。
2. 不批量删除结构体。
3. 不恢复 P11 普通低风险无调用 helper 删除线。
4. 不删除自我线程核心链、任务管理调度、公开接口、D455 外设实现、析构函数、世界树公开接口或方法规格确认函数。
5. 不用字符串、日志、临时 JSON 或说明字段替代被删除结构体的业务承载职责。
```

已运行命令：

```text
Get-Content 规范/禁止性规范20260611.md -TotalCount 140
Get-Content 规范/规范目录.md -TotalCount 140
Get-Content 计划/计划索引.md -TotalCount 170
Get-Content 计划/20260618_项目结构体审计与清理计划_v0.1.md -TotalCount 330
git status --short --untracked-files=no
第一次结构体扫描脚本：因默认 git ls-files 中文路径转义导致 source_files=0。
第二次结构体扫描脚本：已改用 core.quotepath=false，但逐结构体 Select-String 计数阶段 120s 超时。
```

已发现工具问题：

```text
锚点：P10d live 复扫脚本。
证据：第一次 source_files=0；第二次 timeout=120000ms。
当前判断：不是项目实现堵点，是扫描脚本计数方式过慢。
下一步验证：改为一次性读取 tracked 源码文件并在内存中统计结构体名出现次数。
```

已完成候选复扫：

```text
第三次复扫已改为一次性读取 tracked 源码并在内存计数，生成：
日志/20260622_P10d_结构体当前扫描.txt
日志/20260622_P10d_结构体引用计数.tsv
日志/20260622_P10d_结构体低引用候选.tsv

复扫结果：source_files=128；struct_defs=404；low_ref_count=114。
```

当前最小删除候选：

| 结构体 | 文件 | live 证据 | 历史审计证据 | 判定 |
| --- | --- | --- | --- | --- |
| `点阵图` | `基础数据类型.h` | `rg` 源码范围只命中定义；字段名不作为结构体专属读取证据；`说明书/函数功能说明文档.md` 无命中 | `实施记录/20260618_项目结构体审计_结构体清单.tsv` 仅列为待 P3 审计；无写入方 / 读取方 / 外部读取证据 | 符合“只有定义、无读取 / 合同职责 / 真实运行路径依赖”闸门，进入单结构体删除切片 |

下一步：

```text
删除 `基础数据类型.h::点阵图`，运行 Debug x64 构建验证。
```

## 1. P10d-a `点阵图` 单结构体删除

修改：

```text
基础数据类型.h：删除未被引用的 `struct 点阵图` 定义。
基础数据类型.h：按 `.gitattributes` 的 `eol=lf` 对本次触碰文件归一换行；未做全仓归一。
```

验证：

```text
rg 源码范围复扫 `点阵图`：无命中。
git diff --check -- 基础数据类型.h：无输出。
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：exit code 0；0 个警告；0 个错误；生成 D:\鱼巢\x64\Debug\鱼巢.exe。
```

当前判断：

```text
P10d-a 已闭合。仅删除只剩定义的单结构体，未替换为文本字段或兼容壳，未触碰外设材料 / 协议 / 控制面板 DTO。
```

## 2. P10d-b `结构体_图像缓冲_u8/f32` 结构族候选

候选证据：

| 结构体 | 文件 | live 证据 | 历史审计证据 | 判定 |
| --- | --- | --- | --- | --- |
| `结构体_图像缓冲_u8` | `基础数据类型.h` | 源码范围只命中定义；无构造、字段赋值、字段读取、传参或返回命中 | `实施记录/20260618_项目结构体审计_结构体清单.tsv` 仅列为待 P3 审计；无写入方 / 读取方 / 外部读取证据 | 符合“只有定义 / 成员函数、无读取 / 合同职责 / 真实运行路径依赖”闸门，进入同族删除 |
| `结构体_图像缓冲_f32` | `基础数据类型.h` | 源码范围只命中定义；无构造、字段赋值、字段读取、传参或返回命中 | `实施记录/20260618_项目结构体审计_结构体清单.tsv` 仅列为待 P3 审计；无写入方 / 读取方 / 外部读取证据 | 符合“只有定义 / 成员函数、无读取 / 合同职责 / 真实运行路径依赖”闸门，进入同族删除 |

下一步：

```text
删除 `基础数据类型.h::结构体_图像缓冲_u8/f32`。
因结构体内含 inline 成员函数，删除后运行 `python .\tools\生成函数功能说明文档.py` 刷新函数说明文档，再运行 Debug x64 构建。
```

修改：

```text
基础数据类型.h：删除未被引用的 `结构体_图像缓冲_u8` 与 `结构体_图像缓冲_f32`。
说明书/函数功能说明文档.md：运行生成脚本刷新；函数条目从 7249 降为 7245，删除 4 个对应 inline 成员函数条目并更新后续行号。
说明书/函数功能说明文档.md：按 `.gitattributes` 的 `eol=lf` 对本次触碰文件归一换行；未做全仓归一。
```

验证：

```text
rg 源码范围复扫 `结构体_图像缓冲_u8|结构体_图像缓冲_f32|图像缓冲_u8|图像缓冲_f32`：无命中。
python .\tools\生成函数功能说明文档.py：exit code 0；源码文件 128；函数条目 7245。
git diff --check -- 基础数据类型.h 说明书/函数功能说明文档.md 实施记录/20260622_P10d_源码结构体候选复扫_Codex断点清单.md：无输出。
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：exit code 0；0 个警告；0 个错误；生成 D:\鱼巢\x64\Debug\鱼巢.exe。
```

当前判断：

```text
P10d-b 已闭合。删除对象只剩定义 / inline 成员函数，无读取、合同职责或真实运行路径依赖。
```

禁止宣称：

```text
不宣称结构体清理完成。
不宣称项目结构闭环全部通过。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
