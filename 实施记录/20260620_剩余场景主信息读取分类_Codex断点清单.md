# 20260620 剩余场景主信息读取分类 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.2 P3.4，标注剩余显示 / 投影 / owning class 内部读取路径。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
实施记录/20260620_场景身份判断回迁_Codex断点清单.md
日志/20260620_场景身份判断回迁_剩余场景主信息引用.txt
```

本切片范围：

```text
1. 只分类剩余 27 条命中。
2. 不修改源码。
3. 不运行构建。
```

## 1. 剩余命中分类

```text
场景类.cpp 7：
    场景类 owning class 内部实现，包括 取场景主信息、是场景节点、按主键解析场景节点，以及私有初始自我场景识别。
    不作为外部直读缺口。

控制面板类.cpp 9：
    控制面板统计、树遍历、节点类型文本、字段树展示和节点字段展开。
    属于显示 / 调试投影读取；不作为机器判断依据。

语言类_基础信息命名.cpp 3：
    基础信息命名显示读取场景名称和世界类型。
    属于语言命名显示读取。

语言类_二次特征表达.cpp 3：
    二次特征表达中的节点可读名称读取。
    属于语言表达显示读取。

世界树类.cpp 4：
    私有_取存在所在场景、世界树 SQL 引用关系投影、世界树 SQL 辅助文本、状态写入后刷新场景存在型基础二次特征。
    前两类中 SQL 相关为投影读取；场景辅助 / 二次特征刷新路径进入 1.4 P5 世界树场景辅助函数归属审查，不在 P3.4 中修改。

基础信息主信息类.h 1：
    场景节点主信息类::equals 内部类型比较。
    属于主信息类内部比较器，不作为外部业务直读缺口。
```

## 2. 结论

```text
P3.4 闭合。
1.2 场景类功能函数补缺范围闭合：状态类、动态类、二次特征类、方法类、任务类、语素类、自我动作和因果类中的可闭合系统逻辑读取 / 写入 / 身份判断已回迁到 场景类 功能函数。
剩余不宣称全部清零；显示 / 投影读取和世界树辅助归属审查继续保留后续路线。
```

## 3. 下一步

```text
转入 1.3 P4.1：扫描 场景类.h / 场景类.cpp 中非场景主信息直接操作，按场景职责、容器枚举、状态类职责、特征类职责和其它类职责分类。
```

禁止宣称：

```text
不宣称所有场景主信息直读已回迁。
不宣称世界树场景辅助函数归属审查完成。
不宣称场景类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
