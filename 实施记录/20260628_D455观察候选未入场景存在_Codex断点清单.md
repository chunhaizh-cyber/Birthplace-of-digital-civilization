# 20260628 D455观察候选未入场景存在 Codex断点清单

## 0. 恢复摘要

当前切片：R20 `D455观察候选未入场景存在追根因`。

目标：沿“扫描需求 -> 扫描缺口 -> 观察 / 识别派生需求 -> 观察事实提交”链修复，直到自我所在场景通过观察提交多个存在。

当前判断：D455 供料不是第一堵点；最新口径要求自我先生成扫描需求，扫描缺合法材料 / 对应关系 / 当前特征值时记录结构化缺口，再由需求树和任务筹办派生观察 / 识别需求。此前直接追观察等待项会跳过扫描父需求，需要改查扫描需求、6146 选择、扫描缺口记录和缺口派生需求链。

禁止宣称：不宣称视觉系统完成，不宣称所有候选正确入账，不宣称当前场景全部存在明确，不宣称安全 / 服务闭环完成，不宣称自我苏醒或初步成熟完成。

## 1. 依据

- `计划/计划索引.md`：当前默认主动项 R20。
- `计划/追根因_20260628_D455观察候选未入场景存在_v0.1.md`：硬边界为候选不能直接变成存在，扫描需求先行，观察 / 识别由扫描缺口派生。
- `规范/禁止性规范20260611.md`：业务逻辑必须通过项目通用结构闭合，禁止用日志或文本替代事实。
- `规范/相机外设综合工作流程规范20260607.md`：内部治理生成扫描需求；扫描比较自我内部场景和已合法外设当前数据集；比较缺口作为需求来源进入需求、任务、方法闭环。
- `docs/工程图谱/02_外设观察事实图谱.md`：外设采集 -> 报告队列 -> 任务管理承接 -> 观察方法 -> 事实提交。
- `docs/工程图谱/08_事实来源与写入权限图谱.md`：外设线程只写观察材料，世界树事实由提交入口写入。

## 2. 第一独立问题

问题锚点：

```text
D455 稳定观察报告存在；
活跃等待项=0；
D455等待项数=0；
观察事实承接请求和 6150 提交链未命中。
```

当前疑点收束：

```text
需求树 / 任务筹办可能没有先生成或激活扫描需求；
或 6146 扫描没有记录结构化缺口；
或扫描缺口没有派生观察 / 识别需求；
因此任务管理没有创建 D455 等待项；
因此稳定观察材料无法进入观察事实承接和候选确认提交链。
```

已排除：

```text
1. 不按 D455 报告直接创建场景存在。
2. 不把场景体素或历史日志作为当前场景存在事实。
3. 不把 D455 稳定材料本身当作世界树事实。
```

下一步验证：

```text
1. 查当前需求树是否生成扫描父需求，尤其是“当前观察范围稳定对应存在扫描事实 / 动态取得状态”等目标。
2. 查任务筹办是否能从扫描父需求选择 6146。
3. 查 6146 是否记录结构化缺口，并让缺口进入需求生成。
4. 查缺口是否派生观察 / 识别需求，再追 6085 / 6145 / 6150 和等待项创建。
5. 用数据库投影作为主验证，日志作为辅助定位。
```

## 2.1 最新口径后的第一代码断点

问题锚点：

```text
自我_扫描当前场景已经在运行存在和输出结果场景写入：
    需求来源状态
    信息缺口集合
    信息缺口数量
    扫描治理输出状态
    后续承接入口状态

但扫描方法内部不应直接调用观察 / 识别 / 跟踪；
缺口必须由任务执行结果后处理或任务管理上行消息转成派生需求。
```

当前判断：

```text
下一步不改扫描方法直接调用观察 / 识别；
先追 6146 执行完成后，任务管理是否读取扫描输出结果场景 / 运行存在中的缺口特征，
并生成观察 / 识别派生需求消息。
如果没有该桥，最小缺口属于任务执行结果后处理 / 派生需求上报，而不是 D455 供料。
```

## 2.2 反向触发口径删除结果

本次按最新口径删除两类旧反向机器路径：

```text
1. 删除自我线程中“外设观察事实承接完成后直接确保 6146 / 6147 任务主体”的路径。
   影响文件：
       自我线程模块.impl.cpp
   删除入口：
       私有_扫描材料承接后确保6146任务主体_线程侧
       私有_扫描材料承接后确保6147任务主体_线程侧
   删除调用点：
       自我线程/外设观察事实承接完成 后的 6146 / 6147 直接任务确保调用。

2. 删除任务管理界面线程中“稳定子集全量扫描等待项”路径。
   影响文件：
       任务模块.管理界面线程.impl.cpp
   删除入口：
       私有_按稳定子集全量扫描约束提交外设等待项
       私有_构造稳定子集扫描入账派生需求上行消息
       稳定子集扫描入账派生需求幂等键和配套只读/状态构造辅助函数
   同步清理：
       工作结果应用尾部证据不再输出“稳定子集全量扫描等待项”；
       慢阶段日志不再统计“稳定子集等待项”。
```

当前正向保留路径：

```text
1. 任务管理仍可按“当前方法供料要求”创建外设观察等待项。
2. 扫描方法仍只输出扫描结果、状态变化和结构化缺口，不直接调用观察 / 识别 / 跟踪。
3. 后续必须验证：扫描需求由自我内部治理/因果筹办链生成，任务筹办查询因果选择 6146，6146 输出缺口后再由需求树和任务筹办派生观察 / 识别 / 提交链。
```

验证：

```text
python .\tools\生成函数功能说明文档.py
    退出码 0，函数条目 7318。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码 0，Debug x64 构建通过，0 警告，0 错误。
```

## 3. 已运行命令

```text
Get-Content docs/00_文档总索引.md
Get-Content 规范/规范目录.md
Get-Content 规范/禁止性规范20260611.md
Get-Content 计划/计划索引.md
Get-Content 计划/追根因_20260628_D455观察候选未入场景存在_v0.1.md
Get-Content docs/工程图谱/02_外设观察事实图谱.md
Get-Content docs/工程图谱/08_事实来源与写入权限图谱.md
Select-String docs/工程图谱/05_规则原子索引.md
git status --short --untracked-files=no
rg "私有_扫描材料承接后确保6146任务主体_线程侧|私有_扫描材料承接后确保6147任务主体_线程侧|私有_构造稳定子集扫描入账派生需求上行消息|私有_按稳定子集全量扫描约束提交外设等待项|稳定子集全量扫描等待项|稳定子集等待项" -n
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

## 4. 本轮四方法梳理

当前正式四方法口径：

```text
6085 自我_观察单个空间候选并组合存在假设 = 观察
6145 自我_识别外设观察材料 = 识别
6146 自我_扫描当前场景 = 扫描
6147 自我_跟踪指定存在 = 跟踪
```

辅助入口口径：

```text
6127 自我_验证外设观察像素簇候选 = 候选验证辅助入口，不属于四方法本体。
6150 提交_当前观察范围可观测单位存在对应事实 = 观察事实提交入口，不属于四方法本体。
```

已改代码：

```text
任务模块.管理界面线程.impl.cpp
    将外设观察默认许可集拆分为“观察四方法”和“辅助验证提交”两组。
    旧 6127/6145/6146/6147/6150 混合默认集不再作为四方法口径。
    等待项供料口径新增“观察”，6085 的观察存在假设 / 空间候选观察队列可创建逐簇识别等待项。
    旧“观察验证”口径改为“候选验证”，避免把 6127 当成观察方法。

自我线程模块.impl.cpp
    删除固定返回 false 的当前场景可观测单位硬编码 OR 展开残留。
    删除日志字段“当前场景特征值OR路径展开”，避免旧 D455 固定子需求路线继续作为当前判断锚点。
```

已确认不能删除：

```text
6127 和 6150 的实现不能删除。
原因：它们不是四方法本体，但仍是观察候选验证和事实提交链的辅助入口。
删除会破坏候选验证 / 提交链，不会解决候选未入场景存在问题。
```

当前剩余堵点：

```text
四方法口径已经收束，但当前追踪路线需要按最新口径改为扫描父需求先行；
短运行中仍可出现活跃等待项=0 / D455等待项数=0；
下一步应继续追“扫描需求是否生成 -> 6146 是否被选择 / 执行 -> 扫描缺口是否派生观察 / 识别需求 -> 6085 / 6145 / 6150 是否创建等待项和提交事实”。
```

验证结果：

```text
函数说明文档已重新生成。
Debug x64 构建通过：0 错误，0 警告。
本轮未执行 DB 投影复核，不能宣称自我所在场景已通过观察提交多个存在。
```

## 2.3 扫描缺口派生需求桥修复结果

第二个独立问题：

```text
6146 自我_扫描当前场景能输出结构化缺口事实，
但此前缺少显式交给执行后处理消费的“扫描缺口作为下轮因果筹办目标”。
如果执行后处理拿不到结构化派生需求目标，
扫描缺口不会自然进入需求树和任务筹办，
观察 / 识别需求也不会沿正向链路生成。
```

本次最小修复：

```text
影响文件：
    自我动作实现.扫描.ixx
    规范/相机外设综合工作流程规范20260607.md
    说明书/函数功能说明文档.md

扫描输出结果场景新增执行桥三件套：
    派生需求 = 观察存在假设
    派生需求当前状态 = 观察存在假设: 0
    派生需求目标状态 = 观察存在假设: 1

触发条件：
    扫描缺口数量 > 0。

输出状态修正：
    扫描缺口数量 > 0 时，扫描治理输出状态明确为 信息缺口。
```

边界：

```text
1. 不新增结构体。
2. 不恢复观察事实承接后直接确保 6146 / 6147。
3. 不恢复稳定子集全量扫描等待项。
4. 不让扫描方法直接调用观察 / 识别 / 跟踪。
5. 三件套只表达下轮因果筹办目标，后续仍必须由执行后处理、需求树、任务筹办和方法选择承接。
```

防回归：

```text
规范/相机外设综合工作流程规范20260607.md 已新增防回归硬规则：
    不得在观察事实承接完成后反向直接确保 6146 / 6147 任务主体；
    不得由稳定观察子集、外设报告或观察事实承接结果直接创建稳定子集全量扫描等待项；
    扫描缺口进入下一轮筹办必须写出派生需求三件套；
    三件套不等于扫描方法直接调用观察、识别或跟踪。
```

已验证：

```text
rg 旧反向入口：
    私有_扫描材料承接后确保6146任务主体_线程侧
    私有_扫描材料承接后确保6147任务主体_线程侧
    稳定子集全量扫描等待项
    按稳定子集全量扫描约束提交外设等待项
结果：
    0 命中。

rg 新桥入口：
    扫描缺口派生需求桥
    特征_执行桥派生需求
    特征_观察存在假设
结果：
    命中 自我动作实现.扫描.ixx 和 说明书/函数功能说明文档.md。

git diff --check：
    退出码 0，仅提示函数说明文档 CRLF/LF 工作区警告。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码 0，Debug x64 构建通过，0 警告，0 错误。
```

未验证：

```text
尚未跑真实主循环和数据库投影。
下一步必须以数据库数据为主证据，验证：
    扫描输出结果场景存在派生需求三件套；
    执行后处理消费三件套并生成派生需求上行 / 入树；
    任务筹办查询因果后选择观察 / 识别 / 提交链；
    等待项消费 D455 稳定报告；
    自我所在场景直接子存在数量 > 1。
日志只能作为辅助定位。
```

## 2.4 扫描状态 / 动态取得验证切片

本次用户收紧目标：

```text
继续追，直到自我所在场景中有通过扫描获取到的状态信息、动态信息。
```

本切片验收口径：

```text
最低目标不是“候选已经变成多个存在”，
而是先证明 6146 自我_扫描当前场景 真实执行后，
自我所在场景或扫描输出结果场景出现由扫描取得的状态信息和动态信息。

主证据：
    SQL Server / 鱼巢投影库 / 鱼巢 schema 中的世界树、当前世界显示项、当前动态显示项。

辅助证据：
    最新运行日志中的 6146、扫描输出、状态动态、扫描缺口桥等锚点。
```

运行前 DB 基线：

```text
当前运行ID：
    B5D3E833-3AEB-4554-A95C-2233992719CC

[鱼巢].[当前世界显示项]：
    285 行。

[鱼巢].[当前动态显示项]：
    26 行。

自我所在场景 Ez 下状态：
    只有安全值、服务值相关状态。

自我所在场景 Ez 下动态：
    只有安全值、服务值状态变化动态。

当前判断：
    基线尚未显示扫描取得的状态信息 / 动态信息；
    需要真实运行后复查 DB 投影。
```

预测：

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000

若扫描链已贯通到状态 / 动态：
    SQL 当前世界或世界树中应出现扫描相关状态节点；
    SQL 当前动态或世界树中应出现扫描相关动态节点；
    日志应能辅助定位到 6146 扫描、扫描输出结果场景或状态动态创建。

若未出现：
    不判定为业务闭合；
    继续追 6146 是否被任务筹办选中、是否执行、是否写入状态、状态是否进入场景索引、动态是否生成、SQL 同步是否漏投影。
```

运行后证据：

```text
SQL Server：
    实例 = .\SQLEXPRESS
    数据库 = 鱼巢投影库
    最新运行ID = F53718C6-89D5-40F7-B8B5-ED31C3217227
    状态 = 运行中

[鱼巢].[当前方法主信息]：
    方法 1 = 练习 / 本能函数ID:6036 / 结果项数量=0 / 有结果能力=0
    方法 b = 扫描当前场景 / 本能函数ID:6146 / 结果项数量=0 / 有结果能力=0

[鱼巢].[当前任务主信息]：
    当前任务仍是安全 / 服务 / 方法治理链任务；
    未见扫描父任务或扫描方法当前执行。

[鱼巢].[当前世界显示项]：
    只见“扫描当前场景(方法)”等方法存在；
    未见通过扫描取得的场景状态信息。

[鱼巢].[当前动态显示项]：
    未见扫描相关动态。
```

日志辅助证据：

```text
日志/鱼巢_run_20260628.低值g：
    6146 只见“注册外设观察执行前许可免审批默认集方法”，未见扫描执行。
    任务筹办多次候选集填充为 0。
    任务 5 / 6 的当前方法仍为 1，当前本能ID=6036。
    任务管理工作线程/默认本能方法规格确认在 10:55:24 才完成：
        默认本能方法规格确认 | 已确认=82 | 默认数量=82 | 耗时ms=17107
```

当前问题锚点：

```text
专属规格确认入口存在，但当前实现用进程内 atomic 标记“已尝试确认”。
标记在确认开始时即置位，确认耗时约 17 秒。
并发任务工作线程可能在确认完成前跳过确认并进入任务筹办，
从而按半成品方法树做结果能力召回。

即使确认完成，当前 SQL 方法投影仍显示 6146 结果能力为 0，
说明“专属规格确认完成 -> 方法树结果能力可被 SQL 验证面读到”也未闭合。
```

下一步：

```text
1. 修复任务管理工作线程首次筹办前的默认本能专属规格确认互斥 / 完成标记。
2. 确认完成后刷新方法树 SQL 投影或使用已有方法树投影刷新入口，使数据库成为结果能力验证主证据。
3. 构建后真实运行，再查 6146 是否变为有结果能力。
4. 若 6146 有结果能力但仍未被选中，再继续追扫描父需求 / 目标特征是否命中 6146 结果能力。
```

## 2.5 默认本能专属规格确认互斥修复结果

修复文件：

```text
任务模块.管理工作线程.ixx
```

修复内容：

```text
将首次任务筹办前的默认本能专属规格确认从 atomic “已尝试”标记改为互斥完成闸门。
其它工作线程必须等确认完成后才能进入任务筹办。
确认完成后刷新方法树 SQL 投影，保证数据库验证面能看到方法结果能力。
```

验证：

```text
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码 0
    0 警告
    0 错误

.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000
    退出码 0

日志/鱼巢_run_20260628.低值g：
    方法树SQL投影完成 | 来源=任务管理工作线程/运行态默认本能方法规格确认 | 节点数=249
    任务管理工作线程/运行态默认本能方法规格确认 | 已确认=82 | 默认数量=82 | 确认完成=1 | 方法树SQL投影刷新=1

SQL 最新运行ID：
    73908420-89F0-452D-A1F7-AE9B059C3BE9

[鱼巢].[当前方法主信息]：
    方法 1 = 练习 / 本能函数ID:6036 / 主结果特征=15y / 结果项数量=2 / 有结果能力=1
    方法 b = 扫描当前场景 / 本能函数ID:6146 / 主结果特征=ez / 结果项数量=4 / 有结果能力=1
```

当前剩余堵点：

```text
6146 已具备结果能力，但当前需求树没有扫描目标。

[鱼巢].[当前需求主信息] 仍只包含：
    安全值 Q4
    服务值 QO
    服务触发事实监听状态 1FU
    服务机会检查状态 1Fb
    服务能力校准状态 1Fi
    方法维护管理状态 1CL
    方法状态 5s

当前世界 / 动态投影：
    仍未出现通过扫描取得的场景状态信息或扫描动态。
```

下一步：

```text
追“扫描需求应由哪里产生”：
    1. 查规范中扫描需求生成口径；
    2. 查代码中安全 / 服务 / 当前场景维护是否生成目标特征=已识别区域扫描入账状态 / 外设扫描结果场景状态 / 当前场景扫描组合状态；
    3. 若缺少正式生成入口，按规范补最小正向入口，不恢复观察事实反向触发。
```

## 2.6 当前场景维护扫描需求生成切片

本轮目标：

```text
不从 D455 报告、观察事实承接或等待项反向触发扫描；
由自我内部治理在需求整理阶段正向生成当前场景维护扫描需求；
让任务筹办按目标特征命中 6146 自我_扫描当前场景；
用数据库投影验证扫描取得的状态信息 / 动态信息。
```

预测：

```text
修改点：
    自我线程模块.impl.cpp

预期结构：
    服务根需求
        -> 当前场景维护扫描需求
            目标宿主 = 自我存在
            需求场景 = 自我所在场景
            目标特征 = 已识别区域扫描入账状态
            当前状态 = 0
            目标状态 = 1

真实运行后若链路闭合：
    [鱼巢].[当前需求主信息] 出现目标特征“已识别区域扫描入账状态”；
    [鱼巢].[当前任务主信息] 出现该需求对应任务，筹办选择 6146；
    [鱼巢].[当前世界显示项] 出现扫描输出状态；
    [鱼巢].[当前动态显示项] 出现扫描相关动态。

若只出现扫描需求但未筹办 6146：
    继续追任务筹办方法能力匹配、目标特征缓存、任务场景和候选方法集。

禁止：
    不恢复观察事实承接后直接确保 6146 / 6147；
    不恢复稳定子集全量扫描等待项；
    不把任务筹办 / 任务执行恢复为本能方法；
    不用日志替代数据库事实证据。
```

## 2.7 自检运行态 SQL 投影验收开关切片

当前堵点：

```text
真实运行日志已显示当前场景维护扫描需求入树、任务筹办选择 6146、6146 扫描执行并产生扫描状态 / 动态；
但 `--self-check-log` 默认不标记“本进程已执行控制面板 SQL 投影”，
退出守卫跳过最终 SQL 投影刷新，数据库读模型可能停留在旧批次或启动态。

因此数据库无法作为同一次扫描运行的主证据。
```

本次最小修复：

```text
影响文件：
    鱼巢.cpp

新增显式验收开关：
    --self-check-log-refresh-sql
    --self-check-log-sql-projection

行为：
    仅当自检命令携带上述开关时，
    自我线程启动前按需初始化 / 刷新 SQL 读模型，
    自检等待结束前刷新治理 SQL 投影和需求树 SQL 投影，
    程序退出守卫最终刷新世界树、任务树、方法树、语素树和需求树 SQL 投影。

边界：
    SQL 仍只是只读验证投影；
    不反写业务状态；
    不用日志替代数据库事实；
    不改变普通 `--self-check-log` 默认轻量行为。
```

预测：

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-refresh-sql --self-check-log-wait-ms=30000

若链路闭合：
    [鱼巢].[最新批次] 变为本次运行时间；
    [鱼巢].[当前需求主信息] 出现目标特征“已识别区域扫描入账状态”的扫描维护需求；
    [鱼巢].[当前任务主信息] 出现对应任务，当前方法为 6146 扫描；
    [鱼巢].[当前世界显示项] 出现扫描输出状态；
    [鱼巢].[当前动态显示项] 出现扫描相关动态。

若仍未出现：
    继续追 SQL 投影源表是否遗漏输出结果场景 / 动态显示项，
    或追扫描状态 / 动态是否只在日志中出现而未挂入世界树 / 动态结构。
```

验证结果：

```text
构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码 0，0 警告，0 错误。

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-refresh-sql --self-check-log-wait-ms=30000
    外层命令超时码 124；随后确认进程自然退出，退出守卫完成最终 SQL 投影刷新。

运行态 SQL 批次：
    [鱼巢].[控制面板运行批次]
        运行ID = 2A288F7F-9C7B-4F6E-AFCE-26940C0C46B1
        来源标记 = 鱼巢::main/命令行/自我实现检查
        进程ID = 9512
        同步开关 = 1

SQL 当前显示项计数：
    [鱼巢].[当前世界显示项] = 1157
    [鱼巢].[当前动态显示项] = 537
    [鱼巢].[当前需求主信息] = 13
    [鱼巢].[当前任务主信息] = 12
```

扫描需求 / 任务证据：

```text
[鱼巢].[当前需求主信息]：
    需求 6
    场景主键 = Ez
    目标宿主主键 = F0
    当前状态主键 = 1wv
    目标状态主键 = 1ww
    目标特征主键 = ez
    任务主键 = 5

[鱼巢].[当前任务主信息]：
    任务 5
    名称文本 = 已识别区域扫描入账状态
    需求主键 = 6
    虚拟存在主键 = 2Bi
    场景主键 = Ez
    目标状态主键 = 1ww
    结果状态主键 = 1wv
    当前状态 = 等待中

[鱼巢].[当前世界树节点] / 任务虚拟存在 2Bi：
    任务选中自我能力 = 6146
    候选方法数量 = 1
    当前方法运行存在 = 指针句柄
    输出结果场景 = 指针句柄
```

扫描状态信息证据：

```text
[鱼巢].[当前世界树节点] / 扫描方法运行虚拟存在 3QF：
    已识别区域扫描入账状态 = 1
    外设扫描结果场景状态 = 1
    当前场景扫描组合状态 = 1
    扫描闭合状态 = 1
    扫描运行状态 = 2
    扫描治理输出状态 = 2
    信息缺口数量 = 2
    扫描主动因果信息数量 = 10
    扫描状态变化动作动态数量 = 9

[鱼巢].[当前世界树节点] / 自我所在场景 Ez：
    状态 1ww = 已识别区域扫描入账状态，值 = 1
    状态 1wv = 已识别区域扫描入账状态，值 = 0
    当前场景扫描组合状态 = 1
    外设扫描结果场景状态 = 1
    已识别区域扫描入账状态 = 1
    信息缺口数量 = 2
```

扫描动态信息证据：

```text
[鱼巢].[当前世界树节点] / 自我所在场景 Ez：
    3aX = 扫描当前场景 动态
    3ZL / 3Yk / 3YD / 3Wh / 3Vv / 3Tv / 3TP / 3TB / 3Sk / 3S5
        = 扫描当前场景(主动动作动态)
    3Z6 / 3Yt / 3YV / 3Y2 / 3WK / 3UC / 3Tz / 3Td / 3TS / 3TH / 3T3 / 3Rv
        = 状态变化(无动作状态变化动态)

[鱼巢].[当前动态显示项]：
    来源主键 = 3QF 的状态变化动态 12 条。
```

当前结论：

```text
阶段目标“自我所在场景中有通过扫描获取到的状态信息、动态信息”已用数据库主证据确认。

仍未完成的 R20 总目标：
    通过扫描缺口继续派生观察 / 识别 / 提交链，
    让 D455 观察候选经提交入口成为自我所在场景中的多个存在。
```

## 2.5 观察本能 6085 未执行追踪断点（2026-06-28）

当前三段确认：

1. 6085 `自我_观察单个空间候选并组合存在假设` 已被任务 7 选为当前方法 / 执行方法，任务虚拟存在 `3f1` 上保留 `任务执行方法=1B`、`任务选中自我能力=6085`。
2. D455 等待项 2 已被报告 5 消费，报告 ID 已写入任务 7 的任务虚拟存在，`外设观察报告ID=5`，`任务等待缺口唤醒=1`。
3. 日志未出现 `任务管理工作线程/执行任务入口 | 管理任务=7 | 当前本能ID=6085`，也未出现 `观察存在假设 |` 或 `双目相机本能方法结果 | 方法=自我_观察单个空间候选并组合存在假设`。

当前直接堵点：

```text
任务 7 收到报告并进入待重筹办后，下一轮筹办返回“存在活动子需求”。
数据库显示需求 9 的直接子需求 B 仍为 已截止=0、阻塞父任务=1；任务 9 已完成。
因此任务 7 不是缺外设报告，而是被未收束的子需求 B 挡住。
```

关键证据：

```text
SQL: 当前需求树节点
9 -> B -> C
B: 已截止=0, 阻塞父任务=1, 任务=9
C: 已截止=1, 阻塞父任务=1, 任务=A

SQL: 当前任务树节点
7: 等待中, 需求=9
9: 完成, 需求=B, 结果状态=4Wy
```

下一步：

```text
先用更长真实运行窗口复验是否只是 30 秒停机窗口不足；
若仍卡住，修任务完成后来源需求 B 收束 / 父需求 9 任务 7 唤醒路径。
```

## 2.6 观察本能 6085 调用确认修正（2026-06-28 14:00 后）

本节修正 `2.5` 中“6085 未执行”的旧判断。新证据显示：

```text
1. 6085 会被任务筹办选为当前方法：
   任务筹办跟踪 | 选择当前方法完成 | 任务=7 | 方法=1B | 本能ID=6085

2. 6085 会进入任务执行入口：
   任务管理工作线程/执行任务入口 | 管理任务=7 | 当前方法=1B | 当前本能ID=6085

3. 任务筹办会给 6085 写入输入参数场景和报告 ID：
   外设观察报告ID参数筹办 | 任务=7 | 报告ID=162 | 输入场景=5G6 | 写输入场景=1 | 写输入条件包=1
   候选方法筹办参数已闭合 | 任务=7 | 当前方法=1B | 必需实参数=3 | 已绑定实参数=3 | 输入场景=5G6

4. 6085 函数体会运行并输出失败结果：
   双目相机本能方法结果 | 方法=自我_观察单个空间候选并组合存在假设 | 成功=0
   失败原因=执行缺口_当前方法条件不满足
   消息=缺少已落账当前观察特征帧
   目标宿主=Ez
   动作动态=5I6
```

因此当前三段确认结论为：

```text
是否被调用：
    是。6085 被任务 7 选中并进入任务执行入口。

是否收到参数并运行：
    是，但输入只闭合到外设观察报告 ID / 输入条件包；
    6085 运行后读取不到已落账当前观察特征帧。

是否向世界树写入信息：
    写入了失败运行结果和动作动态；
    未写出成功的观察存在假设；
    数据库投影显示 Ez 直接子存在仍只有 F0 自我，
    Ez 下观察存在假设 I64 结果节点数量为 0。
```

当前根因方向：

```text
6085 依赖 `读取已落账观察特征帧摘要`：
    目标宿主 / 输入参数场景 / 输出结果场景
    必须能读到 `当前观察特征帧 > 0` 和宽高摘要。

但当前任务筹办写入的是外设观察报告 ID，
没有把报告中的当前观察特征帧摘要、空间候选逐项账本
转换或挂接到 6085 可读取的输入参数场景 / 输出结果场景 / 目标宿主。

所以真实堵点不是“观察本能没有被调用”，
而是“观察本能收到的输入材料层级不满足它的读取契约”。
```

本轮新增诊断改动：

```text
本能动作管理类.ixx：
    将原仅服务 6150 的动作调用诊断扩展到 6085。
    只诊断，不改变检查判定，不绕过任务筹办 / 任务执行 / 本能动作管理链。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    结果：0 错误，0 警告。

运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-refresh-sql --self-check-log-wait-ms=30000
    结果：90 秒未自然退出，已手动收束进程。
    本次 14:00 运行中任务 7 已结算，未再次自然推进到 6085；
    D455 报告仍在入队，但活跃等待项=0。
```

下一步最小切片：

```text
追查 `当前观察特征帧` / `空间候选逐项账本` 的正式写入来源。

候选修复方向：
    1. 若应由 `双目相机_取得当前观察特征帧` 先行生成，
       则任务筹办不能直接执行 6085，必须先经因果链派生并执行前置观察特征帧取得步骤。
    2. 若 D455 稳定报告承接已经具备等价材料，
       则只能把材料转换 / 挂接为观察证据层的项目结构，
       不得直接写成场景稳定存在事实。
```

## 2.7 观察中间层被动动作接入

用户最新口径：

```text
主动观察本能函数发现自身执行所需的中间层输出缺失时，
可以直接调用独立的中间层被动动作函数；
中间层原本的主动本能函数不改成被动 / 主动双模式。
被动动作函数执行时自动生成被动动作动态。
```

本次实现：

```text
1. `写入双目相机I64事实与动态` 增加动作语义覆盖参数。
   默认仍使用方法动作名；被动调用时写 `被动动作`。

2. `双目相机_取得当前观察特征帧` 保持正常主动本能函数入口。
   采集和写账逻辑下沉为普通共享 helper `执行双目相机取得当前观察特征帧写账`。

3. 新增独立被动动作函数 `被动动作_取得当前观察特征帧`：
   - 只由主动观察函数内部触发；
   - 不作为新的主动本能方法候选；
   - 动作动态语义写为 `被动动作`；
   - 仍通过 6097 对应方法头写入中间层取得观察特征帧结果。

4. `自我_观察单个空间候选并组合存在假设` 首次读取
   `当前观察特征帧 / 空间候选逐项账本` 失败时：
   - 触发 `被动动作_取得当前观察特征帧`；
   - 重新读取已落账中间层输出；
   - 若本轮观察动作生成动态，则把被动动态追加为来源低层动态，
     并在观察动态上写入 `来源动作动态`。
```

已改文件：

```text
自我动作实现.外设模块.ixx
说明书/函数功能说明文档.md
实施记录/20260628_D455观察候选未入场景存在_Codex断点清单.md
```

验证：

```text
git diff --check -- 自我动作实现.外设模块.ixx
结果：通过。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：0 错误，0 警告。

python .\tools\生成函数功能说明文档.py
结果：已生成函数功能说明文档，函数条目 7329。

2026-06-28 14:54 修正后复验：
git diff --check -- 自我动作实现.外设模块.ixx
结果：通过。

python .\tools\生成函数功能说明文档.py
结果：已生成函数功能说明文档，函数条目 7329。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：0 错误，0 警告。

.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-refresh-sql --self-check-log-wait-ms=45000
结果：60 秒未自然退出，已停止进程。
日志判断：本次 14:45 运行命中当前场景维护扫描需求和 SQL 投影刷新；
         未自然推进到 6085 执行，未出现 `观察中间层被动动作` 或 `观察中间层被动补齐`。
```

当前禁止宣称：

```text
本次只证明代码接入和构建通过；
尚未证明真实 D455 运行中 6085 已成功生成观察存在假设；
尚未证明 6150 提交后 Ez 直接子存在数量大于 1。
被动中间层路径尚未取得自然业务命中证据。
```

下一步：

```text
运行真实 D455 / SQL 刷新路径，观察日志和数据库：
1. 是否出现 `观察中间层被动动作`。
2. 是否出现 `观察中间层被动补齐 | 重读成功=1`。
3. 是否出现 `观察存在假设`。
4. 数据库中 Ez 下观察中间层状态、动态、来源动作动态是否成链。
```

## 2.8 被动动作接入后真实运行预测

预测：

```text
若任务链再次推进到 6085：
    6085 首次读取当前观察特征帧 / 空间候选账本失败；
    触发 `被动动作_取得当前观察特征帧`；
    日志出现 `观察中间层被动动作`；
    随后出现 `观察中间层被动补齐`。

若被动动作能把 6085 需要的中间层输出写到同一目标宿主 / 输入参数场景 / 输出结果场景可读位置：
    重读成功=1；
    6085 生成 `观察存在假设`；
    后续才可能继续进入候选验证、确认和提交。

若仍未命中：
    需要按数据库和日志区分是 6085 未再次执行，
    还是被动动作执行但写入位置与 6085 读取契约不一致。
```

本轮禁止宣称：

```text
真实运行前不宣称被动动作链闭合；
若没有数据库投影或真实日志命中，不用构建通过替代业务闭合；
Ez 直接子存在数量未大于 1 前，不宣称观察发现多个存在。
```

## 2.8 6085 筹办前置实参堵点（2026-06-28）

问题锚点：最新运行中任务筹办已选择 6085，但在执行前停于候选方法通用参数绑定，必需=3、绑定=1、缺失=2。

证据：日志出现 \任务筹办跟踪 | 候选方法通用参数绑定 | 任务=7 | 当前方法=1B | 目标宿主=Ez | 目标特征=1wP | 必需=3 | 绑定=1 | 缺失=2\，随后返回 \参数不可配\，未进入 6085 执行体。

当前判断：6085 规格继承 \确认自我提取空间候选规格\，把 \外设可用性\ 和 \运行状态\ 声明为必需形参 / 必需条件，导致观察函数在执行体触发中间层被动动作前被筹办拦截。按最新口径，观察函数应能进入执行体并在缺当前观察特征帧时触发被动动作；外设状态不足应由执行体或被动动作返回失败事实，不应作为筹办硬阻断。

下一步：将空间候选提取链中的 \外设可用性\ / \运行状态\ 改为可选形参和可选条件，构建后真实运行验证 6085 是否进入执行体并出现 \观察中间层被动动作\。

## 2.9 扫描变化等待项消费后未推进观察需求链（2026-06-28）

问题锚点：最新真实运行中，6146 扫描执行后已通过 `扫描缺口派生需求桥` 输出扫描缺口三件套，并由任务管理执行后处理构造派生需求；但任务 5 随后只创建了 `已识别场景扫描变化` 等待项。该等待项消费扫描变化报告后，任务仍停在等待中，未继续推进到观察 / 识别需求筹办，后续 D455 逐簇识别和稳定子集报告因活跃等待项为 0 而无人消费。

证据：

```text
55867 自我_扫描当前场景/扫描缺口派生需求桥 | 输出场景=Ez | 派生目标特征=1wP | 当前状态=3eT | 目标状态=3eU | 缺口数量=2 | 直接调用观察识别跟踪=0 | 交给执行后处理=1
55870-55872 任务管理工作线程/执行输出派生...任务=5 | 需求=6 | 当前方法=b | 派生需求词=1wP | 派生当前状态=3eT | 派生目标状态=3eU | 执行输出需要派生需求=1
55881 按当前方法供料要求创建外设观察等待项 | 来源需求=6 | 观察运行模式=已识别场景扫描变化 | 期望报告=扫描变化报告 | 直接写需求树=0 | 直接写世界树=0
56094-56096 外设观察事实承接开始/回查成功 | 等待项ID=1 | 报告ID=6 | 观察运行模式值=2 | 类型=扫描变化报告 | 生成候选=否 | 统计识别=否 | 来源需求=6
56132-56133 逐簇识别报告/稳定子集报告入队 | 像素簇=10 | 空间候选队列句柄=1 | 活跃等待项=0 | D455等待项数=0
```

当前判断：外设报告生产和等待项消费机制可运行；当前堵点不在 D455 线程，也不在外设报告队列，而在扫描缺口执行后处理后的任务 / 需求承接链。需要确认扫描缺口派生需求是否真正入树并创建后续任务，或扫描变化报告承接完成后是否缺少对来源任务 / 来源需求的等待缺口唤醒与重筹办。

下一步最小核查：

```text
1. 查 `派生需求来源任务承接`、`提交_生成缺口子需求`、需求入树和任务创建入口，确认任务 5 的扫描缺口派生需求是否入树。
2. 查外设观察事实承接完成后是否会唤醒来源任务等待缺口；若只标记报告消费而未提交等待缺口满足事实，则修任务管理承接链。
3. 若派生需求已入树但未派发，追任务状态巡检和工作项派发条件；若未入树，追执行输出派生到需求提交之间的缺口。
```

禁止修法：

```text
不恢复扫描直接调用观察 / 识别 / 跟踪；
不让外设线程直接写世界树真值；
不把扫描变化报告消费直接当作观察候选入账；
不把候选直接变存在。
```

补充核查结论：

```text
14:01 历史同路径已出现：
    外设观察事实承接完成
    -> 观察事实更新命中来源需求
    -> 任务5 待重筹办
    -> 提交_生成缺口子需求，新需求=9，目标特征=观察存在假设
    -> 任务7 进入观察存在假设筹办并选择 6085

15:15 最新运行只到：
    外设观察事实承接回查成功
    -> 持续生成内部世界动态 / 因果
    -> 未出现外设观察事实承接完成

当前判断从“后续链路缺失”修正为：
    扫描变化承接后的后续链路存在；
    最新运行未越过外设观察事实承接入账阶段，可能是运行窗口不足或承接入账 / 动态因果生成耗时放大。
```

下一步预测：

```text
延长真实运行时间后，若承接入账最终完成，应出现：
    外设观察事实承接完成
    观察事实更新命中来源需求
    观察事实更新任务唤醒裁决
    提交_生成缺口子需求，目标特征=观察存在假设
    任务7 / 6085 筹办或执行日志

若仍不出现，则追查自我线程在外设观察事实承接入账阶段是否被动态 / 因果生成拖住。

## 2.10 6085 后续链路新堵点与修正预测（2026-06-28）

已确认：

```text
扫描缺口 -> 派生观察存在假设需求 -> 任务7 / 6085
    -> 观察中间层被动动作
    -> 观察存在假设
    -> 任务7 结算
    -> 父任务5 重筹办
    -> 派生需求 E，目标特征=已识别区域扫描入账状态
    -> 任务 D 进入筹办
```

新堵点：

```text
任务 D 筹办时目标特征=已识别区域扫描入账状态；
候选集填充全树遍历新增候选数=0；
任务 D 返回“方法查找未命中”并挂起。
```

根因判断：

```text
6150 `提交_当前观察范围可观测单位存在对应事实` 的执行体会写入
`已识别区域扫描入账状态`，但规格确认没有把该特征声明为方法结果能力。

任务筹办按方法结果能力填充候选集，因此无法通过该目标召回 6150。
```

修正：

```text
在 6150 规格确认中补充：
    结果表包含 `已识别区域扫描入账状态`；
    方法结果能力声明 `已识别区域扫描入账状态 = 已生成`。
```

预测：

```text
构建通过后重新真实运行；
任务 D 的候选集不再为 0；
若参数和条件可闭合，应进入 6150 执行；
若条件不闭合，应进入结构化条件 / 证据补齐链，而不是“方法查找未命中”。
```

禁止宣称：

```text
6150 未真实执行前，不宣称事实提交链闭合；
Ez 直接子存在数量未大于 1 前，不宣称观察发现多个存在。
```
```

## 2.11 6085 筹办硬拦修正预测（2026-06-28）

问题锚点：

```text
扫描缺口已经能派生观察存在假设需求；
任务进入 6085 观察链筹办后，通用参数绑定显示必需=3、绑定=1、缺失=2；
首个缺失为运行状态，随后派生检查双目相机运行状态子需求。
```

当前判断：

```text
6085 执行体已经具备“缺少已落账空间候选集合时触发中间层被动动作”的逻辑；
但 6085 规格继承 6082/6083/6084 的外设可用性和运行状态硬必需形参，
导致任务筹办阶段先被相机运行状态拦住，无法进入 6085 执行体。
这与最新口径冲突：观察函数应在执行体内按需触发中间层被动动作，
中间层被动动作执行时自动生成被动动态信息。
```

本次最小修正：

```text
影响文件：
    自我动作实现.外设模块.ixx

修正方式：
    给空间候选规格链增加“中间层状态筹办必需”开关；
    6082 / 6083 / 6084 默认保持原硬条件；
    6085 观察入口继承空间候选结果能力时，将外设可用性 / 运行状态降为非硬筹办条件。
```

预测：

```text
Debug x64 构建应通过；
真实运行中，观察存在假设需求对应任务不应再因运行状态缺失先派生检查双目相机运行状态；
应能进入 6085 执行体，并出现：
    观察中间层被动动作
    观察中间层被动补齐
    观察存在假设
后续继续追 6150 事实提交和 Ez 直接子存在数量 > 1。
```

禁止宣称：

```text
本修正只解除 6085 筹办硬拦；
未看到 6150 提交和数据库投影前，不宣称观察发现多个存在；
不把中间层被动动作当作外设线程直接写世界树事实。
```

## 2.12 6085 成功后方法治理缺口阻塞修正预测（2026-06-28）

新增证据：

```text
6085 筹办硬拦解除后，真实运行已出现：
    自我_观察单个空间候选并组合存在假设
    观察存在假设
    目标达成=1

但执行后处理随后把 `方法最近连续成功次数` 当成执行缺失派生需求，
任务 7 保持等待中，父任务 5 因存在活动子需求不能回到 6150 提交链。
```

根因判断：

```text
方法最近连续成功次数 / 方法稳定状态 / 方法运行账完整性属于方法治理稳定性证据；
当当前业务任务目标已经达成时，它们不应阻塞当前观察事实链。
真正的结构缺口仍然必须阻塞：
    缺方法动作
    缺可执行入口
    缺条件节点
    缺结果节点
    缺条件结果对
    参数不可配
```

本次最小修正：

```text
影响文件：
    任务模块.管理工作线程.ixx

修正方式：
    新增窄判定：
        已达成任务 + 方法状态 / 方法稳定状态 / 方法运行账完整性 / 方法最近连续成功次数
        -> 不派生阻塞子需求，只保留为方法治理证据。
```

预测：

```text
Debug x64 构建应通过；
真实运行中，6085 达成后应出现：
    任务管理工作线程/已达成任务方法治理缺口不阻塞
    执行输出需要派生需求=0
    任务 7 可进入完成 / 收束
父任务 5 应解除存在活动子需求阻塞，继续重筹办；
当报告ID可用时，6150 不应再长期停留在候选输入=-100，应进入提交或给出更具体提交条件缺口。
```

禁止宣称：

```text
未看到 6150 执行与数据库投影 Ez 直接子存在数量 > 1 前，
仍不宣称观察发现多个存在。
```

## 2.13 扫描入账中间主体条件缺口阻塞修正预测（2026-06-28）

问题锚点：

真实运行中 6085 已能生成 `观察存在假设`，且已达成任务的方法治理缺口不再阻塞。
父任务 5 仍停在 `存在活动子需求`，活动阻塞子来自派生需求 B / 任务 A。

证据：

```text
任务=A | 来源需求=B | 目标宿主=3UB | 目标特征=ez | 当前状态=5NW | 目标状态=3Wr
候选集填充完成 | 任务=A | 候选数=0
方法查找未命中 | 任务=A
提交_生成缺口子需求 | 类型=缺条件 | 阻塞当前步骤=是 | 需求特征=ez | 需求目标主体=3UB | 目标特征=已识别区域扫描入账状态
```

当前判断：

该子需求不是新的外部观察事实提交目标，而是扫描因果路径中“同一已识别区域扫描入账状态”落到运行 / 中间主体 `3UB` 后形成的条件补齐缺口。
它没有可召回方法，若作为阻塞需求入树，会阻断父任务 5 回到 6150 提交链。

修正：

在 `任务模块.筹办.ixx` 的因果条件缺口吸收阶段新增受限判定：

```text
若条件缺口目标特征 = 已识别区域扫描入账状态
且来源扫描需求目标特征相同
且派生目标主体 != 来源需求目标主体
则该条件缺口标记为 派生后父任务继续。
```

预测：

下一次真实运行应出现：

```text
因果条件缺口扫描入账中间主体降阻塞
任务管理派生需求 ... 阻塞当前步骤=否
父任务 5 不再长期停在 活动阻塞子=1
父任务 5 能继续重筹办并尝试 6150 提交链
```

禁止宣称：

未看到 6150 执行和数据库投影 `Ez` 直接子存在数量 > 1 前，不宣称观察发现多个存在。

## 2.14 非阻塞派生需求不应打断 6150 执行窗口修正预测（2026-06-28）

问题锚点：

真实运行中父任务 5 已能筹办到 `6150 提交_当前观察范围可观测单位存在对应事实`，且参数闭合：

```text
选择当前方法完成 | 任务=5 | 方法=f | 本能ID=6150
筹办完成进入执行 | 任务=5 | 当前方法=f
```

但同一时间窗内，扫描入账中间主体缺口已被标记为非阻塞：

```text
阻塞当前步骤=否
派生消息携带父筹办阻塞关系=否
```

任务管理界面线程仍在上行等待入树阶段把来源任务裁成等待 / 阻塞，导致 6150 执行窗口被抢掉。

当前判断：

```text
非阻塞派生需求可以上报、入树和记录，但不应把来源任务从就绪 / 排队 / 执行中打回等待。
阻塞派生需求仍必须承接为等待缺口，不能削弱真实缺口约束。
```

本次最小修正：

```text
影响文件：
    任务模块.管理界面线程.impl.cpp

修正方式：
    1. 上行派生需求承接阶段，若 `是否阻塞当前步骤=false` 且 `是否影响父任务推进=false`，只记录“不等待入树”，不提交来源任务等待状态。
    2. 入树回执阶段，若回执不阻塞父任务且来源任务处于就绪 / 排队 / 执行中，跳过来源任务状态抢占。
```

预测：

```text
Debug x64 构建应通过；
真实运行中应出现：
    已上报非阻塞派生需求不等待入树
    非阻塞入树回执不抢占来源任务执行窗口
并进一步出现：
    执行任务入口 | 管理任务=5 | 当前本能ID=6150
    提交_当前观察范围可观测单位存在对应事实
随后继续用数据库投影验证 Ez 直接子存在数量是否大于 1。
```

禁止宣称：

```text
本修正只保护非阻塞派生需求不打断父任务执行窗口；
未取得 6150 执行结果和数据库事实投影前，不宣称观察发现多个存在。
```

## 2.15 扫描变化报告不得污染观察事实提交输入修正预测（2026-06-28）

新增证据：

非阻塞派生需求打断修正后，任务 5 已真实进入 6150：

```text
执行任务入口 | 管理任务=5 | 当前本能ID=6150
```

6150 首次失败时拿到的 `来源报告ID=6` 是扫描变化报告：

```text
来源报告存在=1
来源报告为逐簇识别=0
来源报告层级=1
正式逐簇报告缺口说明=来源报告类型不是逐簇识别报告
```

同一运行中，外设侧已经产生逐簇识别稳定子集报告，例如：

```text
报告ID=5 | 类型=逐簇识别报告 | 报告层级=1
报告ID=28 | 类型=逐簇识别报告 | 报告层级=1
```

后续也出现 6150 使用逐簇报告进入正式判定，但新的扫描变化报告又会写入同一个任务输入槽，导致后续 6150 再次快退。

当前判断：

```text
任务虚拟存在.外设观察报告ID 是观察 / 识别 / 提交链的通用输入材料引用；
扫描变化报告只能作为扫描方法输入或观察事实承接材料，不能在当前方法未明确为扫描供料口径时覆盖该输入槽。
```

本次最小修正：

```text
影响文件：
    任务模块.管理界面线程.impl.cpp

修正方式：
    1. 当前任务无当前方法或当前方法供料口径未映射时，扫描变化报告不匹配任务输入。
    2. 扫描变化报告只有在当前方法供料口径明确为扫描时，才允许写入任务虚拟存在的外设观察报告ID。
    3. 进一步收紧：扫描变化报告不写入“观察事实报告ID”这个通用任务输入槽，只作为扫描承接材料和观察事实承接请求材料。
```

预测：

```text
Debug x64 构建应通过；
真实运行中应出现：
    观察事实报告ID跳过任务输入 | 原因=报告类型与当前方法供料口径不匹配
或：
    观察事实报告ID跳过任务输入 | 原因=扫描变化报告只供扫描方法输入
或：
    观察事实报告ID跳过任务输入 | 原因=扫描变化报告只作扫描承接材料
6150 应优先继承逐簇识别报告 ID，而不是扫描变化报告 ID。
下一堵点若仍存在，应进入 6150 内部的对应事实创建条件：
    识别正式结果
    计数事实
    已验证观察存在数量
    场景体素先验版本闸门
```

禁止宣称：

```text
本修正只保证报告类型不污染 6150 输入；
未看到世界树新增场景存在和数据库投影 Ez 直接子存在数量 > 1 前，不宣称观察发现多个存在。
```

## 2.16 扫描任务逐簇报告应作为 6150 候选提交材料修正预测（2026-06-28）

问题锚点：

真实运行显示任务 5 目标特征仍是 `已识别区域扫描入账状态`，筹办候选同时存在：

```text
6146 自我_扫描当前场景
6150 提交_当前观察范围可观测单位存在对应事实
```

但任务当前方法为 6146 时，`观察事实报告ID` 写入函数按当前方法供料口径期待扫描变化报告，会拒收逐簇识别报告；而 6150 又依赖 `外设观察报告ID=逐簇识别报告` 才能输入闭合和执行。由此形成：

```text
当前方法=6146
    -> 逐簇报告不写入任务输入
        -> 6150 输入闭合预估扣分
            -> 继续选择 6146
```

本次最小修正：

```text
影响文件：
    任务模块.管理界面线程.impl.cpp

修正方式：
    扫描任务当前方法口径为扫描，但收到逐簇识别报告时，
    不把该报告当作 6146 扫描输入；
    只允许作为 6150 提交入口候选材料写入任务虚拟存在的 `外设观察报告ID`，
    并唤醒下一轮任务筹办。

保持边界：
    扫描变化报告仍不写入观察事实通用输入槽；
    逐簇报告只作为任务输入材料引用，不直接写世界真值、需求满足或场景存在。
```

预测：

```text
Debug x64 构建应通过；
真实运行应出现：
    观察事实报告ID按提交候选材料接收
    外设观察报告ID参数筹办 | 任务=5 | 报告ID=逐簇识别报告
    任务=5 的 6150 输入闭合预估不再为 -100
    选择当前方法完成 | 任务=5 | 本能ID=6150

若 6150 执行后仍失败，下一堵点应收敛到：
    场景体素先验可用状态=0
    视觉证据不足数量
    稳定子集桥失败说明
    是否允许首帧基准降级创建当前观察范围对应事实
```

禁止宣称：

```text
本修正只打通逐簇报告进入 6150 筹办的输入材料链；
未看到数据库投影 Ez 直接子存在数量 > 1 前，不宣称自我所在场景发现多个存在。
```

## 2.17 中间层不能省略口径纠偏（2026-06-28）

用户纠偏：

```text
中间层不能省。
观察和扫描仍需要从中间层生成的数据队列中取数，
而不是由自我的方法直接调用中间层的方法。
```

废止此前 2.7 / 2.8 的临时方向：

```text
6085 观察函数内部直接触发 `被动动作_取得当前观察特征帧`；
用“观察中间层被动动作 / 观察中间层被动补齐”作为推进证据；
把中间层函数当作观察函数执行体里的同步补料步骤。
```

本次纠偏代码处理：

```text
影响文件：
    自我动作实现.外设模块.ixx
    规范/相机外设综合工作流程规范20260607.md
    计划/追根因_20260628_D455观察候选未入场景存在_v0.1.md

已撤销：
    1. 删除 `被动动作_取得当前观察特征帧`。
    2. 删除 6085 内部缺材料时直接调用中间层取帧函数的逻辑。
    3. 删除 6085 动作动态绑定中间层被动动态为来源动作动态的逻辑。
    4. 恢复空间候选提取 / 独立性判断 / 观察队列规格的单一路径，
       中间层状态、外设可用性、运行状态仍作为筹办前置。

保留：
    `双目相机_取得当前观察特征帧` 仍是独立主动本能方法；
    观察 / 扫描 / 提交链仍只能从已提交队列、报告、材料页、任务输入或合法外设当前数据集中取数。

规范 / 计划：
    相机外设综合工作流程规范已补“中间层不能被自我本能方法省略或内联调用”硬口径；
    R20 计划已补“缺中间层数据 = 任务输入 / 等待项 / 队列供料缺口”。
```

验证：

```text
rg 源码与函数说明文档：
    被动动作_取得当前观察特征帧
    观察中间层被动
结果：
    自我动作实现.外设模块.ixx 与 说明书/函数功能说明文档.md 均无命中。

python .\tools\生成函数功能说明文档.py
    退出码 0，函数条目 7333。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码 0，0 警告，0 错误。
```

## 2.7 观察读取旧报告导致材料页淘汰后的证据不足

问题锚点：

```text
20:33:41 对照成功：
    报告ID=53
    上游验证状态=1
    观察新建存在数量=10
    观察写入特征数量=240

21:15:57 当前失败：
    报告ID=229
    材料可进入识别=1
    上游验证状态=2
    同一性状态=4
    归属状态=4
    观察新建存在数量=0

21:15:35 自我线程曾回查报告ID=229成功，报告摘要显示：
    像素簇=6
    可回查材料簇=6
    可承接能力等级=空间可用

但 21:15:35 到 21:15:57 之间 D455 继续提交 231/233/.../263 等近组材料。
中间层短期材料页容量为 6，材料页读写路径会按容量和 TTL 清理。
因此报告队列记录 229 仍可回查，不等于 229 的簇材料页仍可被句柄解析。
```

根因判断：

```text
`读取指定或最新正式外设观察报告` 之前优先使用任务输入里的指定报告ID。
这会让观察方法消费已经脱离中间层近组、且材料页可能已淘汰的旧报告。
随后 `计算外设逐簇报告候选验证状态` 对簇句柄做真实材料解析，解析不到旧材料页后返回证据不足。

这不是创建存在函数失效，也不是报告级质量失败；
是观察读取口径绕过“扫描和观察只取中间层近一组数据”的规则。
```

本次最小修复：

```text
影响文件：
    自我动作实现.外设模块.ixx

修复内容：
    `读取指定或最新正式外设观察报告` 先读取中间层近组队列视图。
    指定报告ID只有仍属于当前近组、且满足正式读取口径时才使用。
    指定报告不在近组或不满足正式口径时，改用当前近组正式报告。
    若近组没有正式报告，则返回空报告。
    替换时输出诊断：
        外设正式报告读取按近组替换指定报告
        指定报告ID
        指定报告在近组
        指定报告正式可用
        使用报告ID
        近组报告数
        只取中间层近组=1
        不跨帧回退旧材料=1
```

边界：

```text
1. 不放宽 `计算外设逐簇报告候选验证状态` 的材料验证条件。
2. 不把报告记录存在等同于材料页可用。
3. 不跨帧回退旧材料伪造队列非空。
4. 不把中间层材料写入数据库。
5. 不绕过需求任务链，不恢复反向直连 6146 / 6147。
```

预测：

```text
再次真实运行进入 6085 时：
    如果任务输入仍携带旧报告ID，应出现 `外设正式报告读取按近组替换指定报告`。
    `观察被动识别闭环` 中的报告ID应变为当前近组正式报告ID，而不是旧输入报告ID。
    若近组正式报告的逐簇句柄仍可解析，上游验证状态应由 2 变为 1。
    若状态为 1 且无冲突，观察应继续创建或更新已确认观察存在。
```

## 2.25 自我所在场景子链口径纠偏（2026-06-28）

用户纠偏：

```text
此前“已确认观察存在不应该直接摊在自我所在场景根层”的理解有问题。
观察发现的存在只能写在自我所在场景的子链上。
```

本次修正口径：

```text
像素观察事实、外设材料、候选、归属掩码和来源证据仍由观察帧隔离；
观察发现并经自我验证链确认的存在节点，必须写入自我所在场景子链；
观察帧、输出结果场景、输入场景、集合引用或统计摘要不能替代场景存在节点。
```

代码预测：

```text
自我动作实现.外设模块.ixx：
    取或创建已确认观察存在 固定以 世界树.取或创建自我现实场景() 为父宿主；
    写入观察识别归属结果 对新建 / 更新观察存在写 观察存在与当前场景关联状态=1；
    读取当前场景已确认存在集合 优先扫描自我所在场景子链。
```

同步文档：

```text
规范/自我所在场景认知工作区规范20260525.md
docs/工程图谱/02_外设观察事实图谱.md
docs/工程图谱/05_规则原子索引.md
计划/追根因_20260628_D455观察候选未入场景存在_v0.1.md
```

下一步验证：

```text
1. 重新生成函数功能说明文档。
2. Debug x64 构建。
3. 真实运行后查数据库：
       Ez 子链中类型为“已确认观察存在”的存在节点数量；
       观察存在与当前场景关联状态；
       6150 当前观察范围可观测单位存在对应事实提交状态。
```

验证结果：

```text
python .\tools\生成函数功能说明文档.py
    退出码 0，函数条目 7354。

git diff --check -- 自我动作实现.外设模块.ixx 规范/自我所在场景认知工作区规范20260525.md docs/工程图谱/02_外设观察事实图谱.md docs/工程图谱/05_规则原子索引.md 计划/追根因_20260628_D455观察候选未入场景存在_v0.1.md 实施记录/20260628_D455观察候选未入场景存在_Codex断点清单.md 说明书/函数功能说明文档.md
    退出码 0，仅提示规范和函数说明文档 CRLF/LF 工作区换行提示。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码 0，0 警告，0 错误。

.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-refresh-sql --self-check-log-wait-ms=45000
    90 秒未自然退出，已停止本次启动进程 PID=36708。

日志/鱼巢_run_20260628.低值g：
    本次 20:57 左右只命中 6146 扫描内部治理记录；
    未在 90 秒窗口内再次命中 6085 观察被动识别闭环或新建观察存在。

SQL：
    最新批次处于运行中，当前世界树节点为 0；
    说明本次超时停止前未完成最终 SQL 投影刷新，不能作为 Ez 子链验收证据。
```

当前判断：

```text
口径和代码落点已完成，构建通过；
真实业务验收仍未闭合。
下一步需要让任务链自然推进到 6085 / 6150，或先恢复一次完整 SQL 投影后，再检查 Ez 子链中“已确认观察存在”节点数量。
```

## 2.24 真实运行复验扫描 / 观察治理计数（2026-06-28）

预测：

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-refresh-sql --self-check-log-wait-ms=45000

若 6146 扫描进入执行体：
    日志应出现 自我_扫描当前场景/函数自身结果。
    同行应包含 扫描状态改变信息数量、扫描内部治理记录状态、扫描被动治理动作动态。

若 6085 观察进入执行体：
    日志应出现 观察被动识别闭环。
    同行应包含 观察新建存在数量、观察更新已有存在数量、观察写入特征数量、被动治理动作动态。

若业务链真正闭合：
    结束前 SQL 投影刷新后，自我初始场景 Ez 直接子存在数量应大于 1。

禁止宣称：
    capture-once 或外设报告命中不等于场景存在入账；
    如果进程访问冲突，只记录已落日志证据和最新阻塞，不把异常退出视作业务失败或业务闭合。
```

## 2.23 观察 / 扫描内部治理记录结构化计数（2026-06-28）

用户补充定案：

```text
扫描的内部治理记录不应只记录运行一次；
应记录发现了几条状态改变信息、缺口数量等结构化结果。

观察的内部治理记录应记录发现或更新几个特征、
创建或更新几个存在等结构化结果。
```

本次代码处理：

```text
自我动作实现.扫描.ixx：
    新增 扫描内部治理记录。
    6146 在主动扫描闭合动作动态之后，
    向自我内部世界写入扫描内部治理记录，
    并生成 被动动作 动态。

    记录字段包括：
        扫描治理输出状态
        场景维护结果状态
        外设数据项总数
        已闭合处理数据项计数
        信息缺口数量
        场景写入摘要数量
        已写入特征数量
        已写入状态动态数量
        扫描状态改变信息数量
        扫描状态变化动作动态数量
        覆盖冲突数量
        本轮异常未覆盖存在数量
        消失候选数量
        视觉观察候选数量
        视觉信息缺口数量

自我动作实现.外设模块.ixx：
    新增 观察内部治理记录。
    6085 每次观察先执行被动识别内部治理闭环；
    若被动识别返回空指针且同一性=未命中已知存在、归属=待建立、材料可交付，
    观察可直接创建新的已确认观察存在并添加特征。
    若识别命中已有存在或确认方案为合并，则更新已有存在特征。

    记录字段包括：
        被动识别同一性状态
        被动识别归属状态
        候选确认新建 / 合并 / 冲突 / 证据不足 / 可提交数量
        观察新建存在数量
        观察更新已有存在数量
        观察写入特征数量
        观察治理处理项数量
        观察治理写入失败数量
```

边界：

```text
识别仍不创建存在，不补写一阶特征；
识别只返回已有存在指针或空指针。

观察可以在识别空指针且材料满足条件时创建存在并添加特征。

扫描内部治理记录只记录扫描产出的结构化结果；
扫描状态改变信息数量 = 已写入状态动态数量 + 未覆盖状态信息数量，
用于表达本轮扫描发现的状态改变 / 异常状态线索条数，不把首次建立基准算作变化。
扫描仍不直接调用观察 / 识别 / 跟踪。

中间层材料仍由队列、材料页和稳定缓存承载；
本次没有把中间层原始数据写入数据库。
```

已改文件：

```text
自我动作实现.外设模块.ixx
自我动作实现.扫描.ixx
计划/追根因_20260628_D455观察候选未入场景存在_v0.1.md
实施记录/20260628_D455观察候选未入场景存在_Codex断点清单.md
说明书/函数功能说明文档.md
```

已运行命令：

```text
git diff --check -- .\自我动作实现.外设模块.ixx .\自我动作实现.扫描.ixx
    退出码 0。

python .\tools\生成函数功能说明文档.py
    退出码 0，函数条目 7352。

git diff --check -- 自我动作实现.扫描.ixx 自我动作实现.外设模块.ixx 计划/追根因_20260628_D455观察候选未入场景存在_v0.1.md 实施记录/20260628_D455观察候选未入场景存在_Codex断点清单.md 说明书/函数功能说明文档.md
    退出码 0，仅有 说明书/函数功能说明文档.md CRLF/LF 提示。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码 0，0 警告，0 错误。
```

待验证：

```text
1. 真实运行后检查日志：
    观察被动识别闭环 中应出现观察新建/更新/写入特征数量。
    自我_扫描当前场景/函数自身结果 中应出现扫描状态改变信息数量、扫描内部治理记录状态和扫描被动治理动作动态。
2. 数据库 / 世界树投影仍需验证 Ez 直接子存在数量是否大于 1；未验证前不得宣称业务闭合。
```

新的下钻方向：

```text
1. 查中间层 / D455 是否已把逐簇识别报告、稳定子集报告、空间候选账本写入队列。
2. 查任务管理是否根据扫描缺口和任务筹办因果结果创建观察 / 识别 / 提交所需等待项。
3. 查等待项是否消费正确类型报告，而不是只消费扫描变化报告。
4. 查 6085 / 6146 / 6150 是否只读取已提交任务输入、报告队列和材料页。
5. 最终仍用数据库投影验证 Ez 直接子存在数量 > 1。
```

禁止宣称：

```text
不再把“中间层被动动作”作为当前观察链推进方案；
不把缺材料时的内部直调当作合法观察；
未取得数据库事实投影前，不宣称观察发现多个存在。
```

## 2.18 中间层被动动作链口径再收束（2026-06-28）

用户补充口径：

```text
中间层走被动动作的套路，
不走需求、任务、方法的过程，
由被动动作串联整个数据生成链路。
```

本次修订结论：

```text
需要区分两层：

1. 外设 / 中间层内部 L0-L3 数据生产链：
       原始帧
       -> 稳定化
       -> 跨帧关联
       -> 稳定观察子集
       -> 报告队列 / 材料页 / 稳定缓存

   该链可以由被动动作自动串联；
   不进入普通需求树、任务筹办和方法选择；
   不把 `被动动作` 学成自我可主动调用方法。

2. 自我侧 L4 观察 / 识别 / 扫描 / 跟踪链：
       需求 / 任务筹办
       -> 等待项 / 订阅项
       -> 消费报告队列 / 材料页 / 稳定缓存
       -> 提交入口裁决

   该链不得在 6085 / 6146 / 6145 / 6147 执行体内直接调用中间层函数现场造材料。
```

已改文件：

```text
规范/相机外设综合工作流程规范20260607.md
计划/追根因_20260628_D455观察候选未入场景存在_v0.1.md
实施记录/20260628_D455观察候选未入场景存在_Codex断点清单.md
```

下一步：

```text
1. 检查 `外设线程_D455深度相机.ixx` 中：
       D455_发布采集成功后的报告编排
       D455_发布本帧逐簇与稳定子集报告
       D455_从逐簇识别报告生成稳定观察子集报告
       D455_从逐簇识别报告生成扫描变化报告
       D455_从逐簇识别报告生成跟踪报告

2. 确认这些 L0-L3 内部转换是否已有被动动作动态证据。

3. 若没有，补最小被动动作动态出口：
       动态主体 = 外设观察报告 / 材料页 / 稳定子集承载对象
       动态特征 = 报告生成状态 / 稳定子集生成状态 / 材料页提交状态
       动作语义 = 被动动作
       来源低层动态 / 来源报告ID / 输出报告ID 可回查

4. 不改 6085 / 6146 为直调中间层函数。
```

禁止宣称：

```text
本次只收束规范和计划口径；
尚未证明中间层被动动作动态出口已补齐；
尚未证明 6150 提交和 Ez 直接子存在数量大于 1。
```

## 2.19 观察事实承接被动动作动态锚点补齐（2026-06-28）

本切片目标：

```text
不在外设队列 / 中间层直接写世界事实；
不让 6085 内联调用中间层函数；
只在自我线程消费外设报告并写入内部治理状态时，
保留正式动态节点作为观察事实承接的被动动作动态锚点。
```

当前判断：

```text
`私有_落单个治理变化到自我内部世界` 已经调用 `私有_输出自我治理动作动态`，
后者通过 `世界树.创建动态` 写入初始状态 / 结果状态和来源动作名。

动态类推导规则表明：
    有来源动作名、没有来源动作方法的动态
    会被推导为 `被动动作动态`。

原堵点是该辅助函数只返回 bool，
调用方无法拿到真实动态节点，
后续只能在日志里写 `来源动作动态=任务管理观察事实承接请求`，
不是正式动态主键。
```

已改文件：

```text
自我线程模块.impl.cpp
```

已改内容：

```text
1. `私有_落单个治理变化到自我内部世界` 增加可选输出参数：
       动态节点类** 输出动态节点

2. 旧调用保持兼容；
   需要动态锚点的调用方可取得本次治理状态变化生成的动态节点。

3. `私有_落任务管理观察事实承接请求到自我内部世界`
   保存“外设观察报告承接状态”对应的动态节点。

4. `私有_观察事实更新后触发安全根需求重筹办_线程侧`
   日志中的 `来源动作动态` 改为真实动态主键；
   若为空，表示承接动态未生成，可继续追根因。
```

预测：

```text
真实运行若产生外设观察事实承接请求，
应出现：
    自我线程/外设观察事实承接完成 | 成功=是
    自我线程/观察事实更新事件发布 | 来源动作动态=<动态主键> | 来源动作=任务管理观察事实承接请求

若仍为空：
    说明承接状态动态生成失败；
    下一步追 `私有_创建治理状态节点` / `世界树.创建动态` / 自我内部世界或主体特征缺失。
```

验证：

```text
git diff --check -- 自我线程模块.impl.cpp
    退出码 0。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码 0，0 警告，0 错误。
```

下一步：

```text
运行真实路径，优先用数据库 / SQL 投影判断；
日志只辅助确认观察事实承接事件中的来源动作动态是否为真实动态主键。
```

## 2.20 创建存在前必须先识别的口径修正（2026-06-28）

用户补充口径：

```text
创建存在前需要先识别。
```

当前证据：

```text
规范层：
    识别只回答有没有见过，输出已有存在指针或空指针；
    候选经过验证后才可以成为已确认观察存在。

运行层：
    报告ID=37 已由任务管理生成观察事实承接请求：
        生成候选=是
        统计识别=是
        上行消息数=1
    但后续日志未出现：
        自我线程/外设观察事实承接开始 | 报告ID=37
        自我_识别外设观察材料
        外设观察材料识别归属结果
        提交_确认观察存在事实
```

当前判断：

```text
观察未生成存在不是中间层没数据；
当前缺口在自我侧 L4 链路：
    中间层逐簇识别报告 / 稳定子集报告
    -> 任务管理等待项消费
    -> 自我线程承接
    -> 6145 正式识别结果
    -> 候选验证 / 候选确认
    -> 6150 或提交_确认观察存在事实创建已确认观察存在

6150 不应绕过 6145 / 识别链直接从外设报告创建场景存在；
若识别正式结果缺失，应先派生或恢复识别链，而不是继续补特征写入。
```

下一步：

```text
1. 追报告ID=37 为什么没有进入自我线程承接，或确认是否被后续请求覆盖 / 进程退出前未处理。
2. 查任务筹办是否会为创建存在前置目标召回 6145，而不是直接落到 6085 / 6150。
3. 查 6150 的输入判定是否强制读取 6145 的正式识别结果；缺失时应生成识别缺口。
4. 确认创建已确认观察存在之后，再进入给存在追加一阶观察特征 / 当前状态特征的后续能力。
```

禁止宣称：

```text
未看到 6145 正式识别结果、提交_确认观察存在事实和数据库 Ez 直接子存在数量 > 1 前，
不宣称观察已经生成存在。
```

## 2.21 观察关联场景存在应走识别而不是跨帧入参续接（2026-06-28）

用户继续修正口径：

```text
中间层提供给观察的信息有跨帧信息；
但观察函数是一次次运行的；
如果每次运行都需要传入上次观察的存在信息，就成了跟踪；
观察还是需要通过识别来关联场景中的存在。
```

当前定案：

```text
中间层 L0-L3：
    可以持续维护跨帧材料、轨迹、稳定子集、空间候选队列、报告队列和材料页。
    这些跨帧信息是识别 / 候选确认的证据，不是自我观察函数的“上次存在入参”。

6085 观察：
    消费当前观察材料，形成本次观察候选 / 观察存在假设。
    不靠传入上次观察存在来续接同一个对象。

6145 识别：
    用本次观察材料及其中间层跨帧证据，关联场景中的已有存在；
    命中则输出已有存在关联，未命中则给后续候选确认 / 新建提供依据。

跟踪：
    才是以指定已有存在为目标，连续寻找同一存在位置 / 状态的链路。
```

对 2.20 的修正解释：

```text
“创建存在前需要识别”保留；
但理由不是“中间层没有跨帧信息”；
而是“观察函数不能靠上次观察存在入参续接对象，否则会变成跟踪”。

创建 / 关联场景存在前，
应由 6145 用当前观察材料 + 中间层跨帧证据完成场景存在关联判断。
```

下一步：

```text
1. 查 6085 是否只生成观察候选 / 假设，而没有要求上次观察存在作为必需入参。
2. 查 6145 是否能消费报告中的跨帧稳定信息，并把本次观察材料关联到场景已有存在或给出未命中。
3. 查 6150 / 提交_确认观察存在事实 是否只在识别 / 候选确认结构已落账后创建已确认观察存在。
```

## 2.22 识别双入口与观察内被动识别闭环（2026-06-28）

用户继续定案：

```text
识别的主动动作函数和被动动作函数都需要；
主动动作函数就是本能方法；
被动动作函数可以看做内部治理函数；
观察每次必须识别，所以观察内部不能再把这一步当作普通主动动作筹办。
```

本次代码口径：

```text
主动识别：
    `6145 自我_识别外设观察材料` 保留为本能方法。
    它仍可由明确识别需求经任务筹办选择和执行。

被动识别：
    新增 `被动识别外设观察材料_内部治理`。
    它不注册进本能动作集，不进任务筹办候选，不创建存在，不补写一阶特征。
    它只消费已经提交到报告队列 / 材料页 / 当前任务输入中的逐簇识别材料，
    用当前场景已确认存在集合计算同一性，
    返回值只能是：
        识别到的已有存在指针
        或空指针。
```

已改文件：

```text
自我动作实现.外设模块.ixx
计划/追根因_20260628_D455观察候选未入场景存在_v0.1.md
实施记录/20260628_D455观察候选未入场景存在_Codex断点清单.md
```

实现要点：

```text
1. 新增 `结构_被动识别外设观察材料结果`，用于承载内部治理过程证据。
2. 新增 `被动识别外设观察材料_内部治理`，复用现有逐簇报告、材料质量门、候选验证状态、当前场景已确认存在集合和确认方案计算逻辑。
3. 新增 `写入被动识别外设观察材料摘要`，把报告ID、候选簇数、同一性状态、归属状态、计数和命中指针写入运行存在 / 输出场景 / 目标宿主。
4. 6085 `自我_观察单个空间候选并组合存在假设` 每次执行时调用被动识别闭环，并输出 `观察被动识别闭环` 诊断日志。
```

预测：

```text
真实运行进入 6085 后应出现：
    观察被动识别闭环

若已识别到唯一已有存在：
    返回已有存在=<存在主键>
    结构中写入 目标已确认观察存在 / 外设观察存在归属目标。

若没见过、证据不足或冲突：
    返回已有存在为空；
    后续仍由候选确认 / 6150 提交链决定是否创建新已确认观察存在。
```

禁止宣称：

```text
本切片只补观察内部被动识别闭环；
不宣称 6150 已提交；
不宣称 Ez 直接子存在数量已经大于 1；
不把被动识别函数学习或注册成新的主动方法。
```

验证：

```text
python .\tools\生成函数功能说明文档.py
    退出码 0，函数条目 7333。

rg 被动识别外设观察材料_内部治理 说明书/函数功能说明文档.md
    命中新函数条目。

git diff --check -- 自我动作实现.外设模块.ixx 计划/追根因_20260628_D455观察候选未入场景存在_v0.1.md 实施记录/20260628_D455观察候选未入场景存在_Codex断点清单.md
    退出码 0。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码 0，0 警告，0 错误。
```

## 2.23 报告 33 已入任务 8 但旧运行状态子需求仍阻塞（2026-06-28）

问题锚点：

```text
最新 21:41-21:42 真实运行中，D455 逐簇识别报告 33 已经被等待项 3 消费，
观察事实承接已完成，并写入来源需求 A 的任务 8 输入。
任务 8 被观察事实更新唤醒后进入重筹办，
但筹办仍返回“存在活动子需求”，没有进入 6085 观察执行体。
```

日志证据：

```text
日志/鱼巢_run_20260628.低值g

140146 自我线程/外设观察事实承接开始 | 等待项ID=3 | 报告ID=33 | 生成候选=是 | 统计识别=是 | 来源需求=A
140436 自我线程/外设观察事实承接完成 | 成功=是 | 报告可回查=是 | 等待项ID=3 | 报告ID=33 | 来源需求=A
140438 任务管理界面线程/观察事实报告ID写入任务输入 | 任务=8 | 报告ID=33 | 写任务虚拟存在=1
140448 任务管理界面线程/观察事实更新任务唤醒裁决 | 任务=8 | 来源需求=A | 报告ID=33 | 报告ID写入任务输入=1
140454 任务管理工作线程/调用任务筹办普通函数 | 任务=8
140457 任务筹办跟踪 | 存在活动子需求，筹办阻塞 | 任务=8 | 来源需求=A
```

同一阻塞链的旧子需求证据：

```text
139647 提交_生成缺口子需求 | 父需求=A | 新需求=C | 目标特征=运行状态 | 阻塞当前步骤=是 | 关联任务=8
139675 任务管理工作线程/执行任务入口 | 管理任务=A | 当前方法=1F | 当前本能ID=6092
139698 任务管理工作线程/执行目标状态读取 | 任务=A | 需求=C | 方法=1F | 目标特征=1Ov
139700 任务管理工作线程/执行结果任务特征评价 | 任务=A | 需求=C | 目标达成=1 | 完成度百万分比=1000000
139701 任务管理工作线程/执行输出派生尾部证据 | 任务=A | 需求=C | 当前方法=1F | 方法运行成功=1 | 推进状况=目标结果符合
139709 任务管理界面线程/工作结果应用尾部证据 | 任务=A | 建议任务状态=完成 | 实际任务状态=完成
```

当前判断：

```text
报告供料链已经到达任务 8，不是中间层空队列或报告写入失败。
6085 未再次执行的直接原因是任务筹办的活动子需求判定仍认为来源需求 A 存在阻塞子需求。
需要检查 `任务模块.筹办.ixx::存在活动直接子需求`、需求满足 / 已截止判定、
以及子任务完成后父任务等待条件变化通知链，确认已达成子需求为何仍阻塞父任务。
```

下一步：

```text
1. 查 `存在活动直接子需求` 是否把已完成任务 / 已达成需求仍计为活动阻塞。
2. 查子任务 A 完成后是否应更新需求 C 的满足 / 截止 / 阻塞父任务状态。
3. 查父任务等待条件变化通知是否应在 C 完成后唤醒任务 8 重筹办并放行。
4. 修复后重新验证 6085 是否进入执行体，并检索 `观察被动识别闭环`。
```

## 2.24 指定报告失效时不得偷换近组报告（2026-06-28）

问题锚点：

```text
`读取指定或最新正式外设观察报告` 上一版在任务输入携带指定报告ID时，
如果指定报告不在中间层近组或不满足正式读取口径，
会自动改用当前近组最新正式报告。
```

当前判断：

```text
该逻辑虽然避免了跨帧读取旧材料，
但会把“指定报告ID绑定的任务输入 / 等待项来源”偷换成另一份报告材料，
造成来源报告、观察事实、任务输入之间的因果锚点不一致。

正确边界：
    有指定报告ID时，只能使用同一报告ID，且该报告必须仍在近组并满足正式口径；
    指定报告失效时返回空，让上游等待或重新承接新材料；
    只有没有指定报告ID的主动读取，才允许取近组最新正式报告。
```

修改：

```text
`自我动作实现.外设模块.ixx`
    将指定报告失效后的日志改为：
        外设正式报告读取按近组拒绝指定报告
    返回值改为 `std::nullopt`。
    日志补充：
        可用近组报告ID
        使用报告ID=0
        不偷换指定报告来源=1
```

预测：

```text
如果任务输入携带旧报告ID，观察 / 扫描不会再用另一份近组报告冒充该输入来源；
应出现结构化缺报告 / 条件不足结果，随后由等待项或新材料承接链重新写入当前报告ID。
```

验证：

```text
git diff --check -- 自我动作实现.外设模块.ixx 实施记录/20260628_D455观察候选未入场景存在_Codex断点清单.md
退出码：0

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
退出码：0
结果：已成功生成，0 个警告，0 个错误
```

未完成：

```text
尚未跑真实 D455 业务路径。
下一步真实运行应检索：
    外设正式报告读取按近组拒绝指定报告
    使用报告ID=0
    不偷换指定报告来源=1
    观察 / 扫描是否随后由等待项或新材料承接链写入当前报告ID
```

## 2.25 删除旧 6085 观察流程并重建（2026-06-28）

用户口径：

```text
当前观察函数需要删除旧流程后重建。
6085 观察函数的业务形参只有一个：当前观察场景 / 自我所在场景指针。
中间层材料不作为函数参数传入；观察函数内部直接取中间层近组数据。
观察每次先走被动识别：识别命中返回已有存在指针，未命中返回空指针。
识别不创建存在、不补写一阶特征；观察在识别为空且材料可交付时创建新存在，然后添加 / 更新特征。
观察发现存在只能写入自我所在场景子链。
```

修改：

```text
`自我动作实现.外设模块.ixx`
    1. 6085 规格不再继承 `确认自我生成空间候选观察队列规格`。
    2. 6085 规格只声明必需形参 `当前观察场景`，结果表改为观察发现、当前特征、报告 ID、识别归属状态和内部治理计数。
    3. 新增 `解析观察函数自我所在场景参数`，只读取并校验 `当前观察场景` 指针，不回退到目标宿主或输出场景。
    4. 新增 `读取近组最新正式外设观察报告`，只从中间层近组队列选最新正式材料。
    5. `被动识别外设观察材料_内部治理` 增加“只取中间层近组最新材料”模式。
    6. 6085 函数体删除旧主路径：
        读取已落账空间候选集合
        计算空间候选独立性判断
        生成空间候选观察队列
        组合观察存在假设
        写入观察存在假设摘要
    7. 6085 新主路径：
        解析当前观察场景
        -> 内部取中间层近组最新正式材料
        -> 被动识别
        -> 写入观察识别归属结果
        -> 写入观察内部治理记录
        -> 写运行结果和 `观察本能闭环` 日志。

`说明书/函数功能说明文档.md`
    已重新运行函数说明生成脚本。
```

预测：

```text
方法筹办不应再因为 6085 规格要求 `输入条件包` / `外设可用性` / `运行状态` 而先派生旧外设检查缺口。
6085 执行时应只从 `当前观察场景` 取得业务输入。
6085 不应再输出 `观察存在假设 |` 主日志。
6085 成功路径应输出 `观察本能闭环`，并包含：
    当前观察场景
    报告ID
    观察新建存在数量
    观察更新已有存在数量
    观察写入特征数量
    被动治理动作动态
    中间层近组取数=1
    只取中间层近组最新材料=1
    忽略输入报告ID=1
```

待验证：

```text
1. `git diff --check`。
2. Debug x64 构建。
3. 真实 D455 路径是否自然进入 6085，并在日志中出现 `观察本能闭环`。
4. 自我所在场景直接子存在数量是否增长或已有存在特征是否更新。
```

验证：

```text
git diff --check -- 自我动作实现.外设模块.ixx 说明书/函数功能说明文档.md 实施记录/20260628_D455观察候选未入场景存在_Codex断点清单.md
退出码：0
备注：函数说明文档提示工作区 CRLF 下次 Git 接触会转 LF；未发现空白错误。

python .\tools\生成函数功能说明文档.py
退出码：0
结果：已生成 `说明书/函数功能说明文档.md`，函数条目 7355。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
退出码：0
结果：已成功生成，0 个警告，0 个错误
```

未完成：

```text
尚未跑真实 D455 业务路径。
真实运行下一步检索：
    观察本能闭环
    当前观察场景=
    中间层近组取数=1
    只取中间层近组最新材料=1
    忽略输入报告ID=1
    观察新建存在数量=
    观察更新已有存在数量=
    观察写入特征数量=

若 6085 仍未自然执行，下一步不回滚观察函数；
应继续查扫描缺口派生后的任务创建、筹办候选选择和当前观察场景形参供给链。
```

## 2.26 扫描桥目标特征迁移缺口（2026-06-29）

复跑证据：

```text
日志/鱼巢_run_20260629.低值g
    6650：6146 扫描函数自身结果缺口数量=2，直接调用观察识别跟踪=0。
    6666：任务 5 创建 D455 扫描变化等待项，等待项ID=1，期望报告=扫描变化报告。
    6838-6855：任务 7 来源需求 9 目标特征=26U，候选数=0，方法查找未命中。
    6870：提交_生成缺口子需求成功，但目标特征=观察存在假设。
    6981：D455 扫描变化报告入队，报告ID=6，匹配等待项ID=1。
    6995-6997：任务管理生成观察事实承接请求并消费报告。
    7008-7152：自我线程完成外设观察事实承接。
```

当前判断：

```text
中间层 / 外设队列不是空；扫描等待项和扫描变化报告消费链已跑通。
当前阻断不是“扫描和观察拿不到 D455 数据”，而是扫描缺口派生目标仍指向旧 6085 口径。
6085 新观察函数声明的正式结果能力为：
    观察存在发现状态
    存在当前特征状态
    观察内部治理记录状态
旧 `观察存在假设` 不再是 6085 的正式结果能力，导致派生任务筹办时候选集为 0。
```

下一步：

```text
1. 将 `自我动作实现.扫描.ixx` 中扫描缺口派生目标改为 `观察存在发现状态: 0 -> 1`。
2. 将 `任务模块.管理界面线程.impl.cpp` 中按方法供料口径补齐 6085 新结果特征到观察口径。
3. 运行函数说明生成脚本、diff check、Debug x64 构建。
4. 真实运行检索 6085 是否被选中并出现 `观察本能闭环`。
5. 若 6085 被选中但缺当前观察场景参数，再切到当前观察场景形参绑定链。
```

## 2.27 扫描桥目标迁移构建前断点（2026-06-29）

已改文件：

```text
自我动作实现.扫描.ixx
    扫描缺口派生目标从旧 `观察存在假设` 改为 `观察存在发现状态: 0 -> 1`。

任务模块.管理界面线程.impl.cpp
    按方法供料口径把 6085 新结果特征映射为观察口径：
        观察存在发现状态
        存在当前特征状态
        观察内部治理记录状态

说明书/函数功能说明文档.md
    已重新生成，新增 / 更新 `特征_扫描缺口观察存在发现状态` 条目。
```

构建前静态检查：

```text
git diff --check -- 自我动作实现.扫描.ixx 任务模块.管理界面线程.impl.cpp 说明书/函数功能说明文档.md 计划/追根因_20260628_D455观察候选未入场景存在_v0.1.md 实施记录/20260628_D455观察候选未入场景存在_Codex断点清单.md
结果：仅提示 `说明书/函数功能说明文档.md` CRLF 将转 LF；未见空白错误。
```

下一验证：

```text
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

构建验证：

```text
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
退出码：0
结果：已成功生成，0 个警告，0 个错误
```

真实运行验证：

```text
.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000 --self-check-log-sql-projection
退出码：0

日志/鱼巢_run_20260629.低值g
    15104：外设线程_D455深度相机/稳定子集报告入队，报告ID=39，类型=逐簇识别报告，像素簇=12，质量=7832，空间可用状态=1。
    15201：任务 B 从结果能力选中方法 1B，本能ID=6085，评分中输入=120。
    15206：候选方法通用参数绑定，目标宿主=Ez，目标特征=VY，必需=1，绑定=1，缺失=0。
    15207：候选方法筹办参数已闭合，输入场景=4bv。
    15240：任务 B 进入执行，当前本能ID=6085。
    15280：自我_观察单个空间候选并组合存在假设成功，消息=观察已通过被动识别写入存在特征，输入场景=4bv，输出场景=Ez，目标宿主=Ez。
    15281：观察本能闭环，当前观察场景=Ez，报告存在=1，报告ID=39，材料可进入识别=1，同一性状态=2，归属状态=1，观察新建存在数量=12，观察写入特征数量=300，观察治理写入失败数量=0，中间层近组取数=1，只取中间层近组最新材料=1，不从输入接收中间层数据=1，忽略输入报告ID=1，不使用上次观察存在=1，不在识别创建存在=1，不在识别补写一阶特征=1。
    15283-15285：目标特征 VY 的实际值=1，接受=1，VX0验证状态=验证成功。
    15297：任务 B 完成。
```

SQL 投影视图验证：

```text
sqlcmd -S .\SQLEXPRESS -d 鱼巢投影库 -E -b -f 65001 -W -s "`t" -Q "SET NOCOUNT ON; SELECT COUNT(*) AS Ez直接子节点数 FROM [鱼巢].[当前世界树节点] WHERE [父节点主键]=N'Ez'; SELECT COUNT(*) AS Ez直接子存在数 FROM [鱼巢].[当前世界树节点] WHERE [父节点主键]=N'Ez' AND [节点类型]=N'存在'; SELECT TOP (40) [节点主键],[节点类型],[显示文本],[主信息类型文本],[名称文本],[类型文本],[直接子数量],[路径文本] FROM [鱼巢].[当前世界树节点] WHERE [父节点主键]=N'Ez' AND [节点类型]=N'存在' ORDER BY [行号];"
退出码：0
结果：
    Ez直接子节点数=1395
    Ez直接子存在数=13
    其中 F0 是自我存在；其余 12 个直接子存在为 4ev、4eU、4e4、3lz、3lU、3kx、3kV、3k5、3jX、3j1、3iS、3hz。
    这 12 个存在的父节点主键均为 Ez，路径均为 WORLD_ROOT/2/Ez/<节点主键>，类型文本均为 2Cr，直接子数量均为 25。

sqlcmd -S .\SQLEXPRESS -d 鱼巢投影库 -E -b -f 65001 -W -s "`t" -Q "SET NOCOUNT ON; SELECT TOP (80) [字段名],[字段类型],[字段值],[指针主键],[指针类型],[指针名称] FROM [鱼巢].[当前世界树节点详情] WHERE [节点主键]=N'4ev' ORDER BY [排序];"
退出码：0
结果：
    详情视图可按节点主键直接读字段；指针类字段以指针主键、指针类型、指针名称三列投影。
    抽查 4ev：父节点主键=Ez，主信息类型=存在，名称=2Ka，类型=2Cr，路径文本=WORLD_ROOT/2/Ez/4ev。
```

当前结论：

```text
1. 扫描和观察能够拿到中间层数据：稳定子集报告入队后，观察本能内部读取近组报告ID=39，报告存在=1，材料可进入识别=1。
2. 观察不从输入参数接收中间层数据，日志明确 `不从输入接收中间层数据=1`、`忽略输入报告ID=1`、`中间层近组取数=1`。
3. 被动识别已作为观察内部治理闭环运行：识别不创建存在、不补写一阶特征；识别返回空时由观察创建存在并写特征。
4. 观察发现的存在已经写入自我所在场景 `Ez` 的子链：SQL 视图显示 `Ez` 直接子存在为 13，除 F0 外 12 个观察存在均直接挂在 `WORLD_ROOT/2/Ez/...`。
5. 本切片闭合到“6085 参数绑定修复 + 真实运行 + SQL 投影视图确认”。不宣称视觉系统全量完成、自我苏醒完成或初步成熟完成。
```

构建验证：

```text
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
退出码：0
结果：已成功生成，0 个警告，0 个错误
```

真实运行预测：

```text
6085 观察任务再次被选中时，`候选方法通用参数绑定` 应由 必需=1 | 绑定=0 | 缺失=1 变为 必需=1 | 绑定=1 | 缺失=0。
后续若任务执行进入 6085，应出现 `观察本能闭环`，并输出观察新建存在数量 / 观察更新已有存在数量 / 观察写入特征数量。
若仍未出现观察执行，下一步只追 6085 就绪到执行许可 / 等待项供料链，不回滚当前观察场景绑定。
```

构建验证：

```text
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
退出码：0
结果：已成功生成，0 个警告，0 个错误
```

真实运行预测：

```text
扫描缺口派生需求日志中的目标特征应从 `观察存在假设` 改为 `观察存在发现状态`。
派生出的观察任务筹办应不再因旧目标特征候选数=0而挂起。
后续若 6085 被选中但失败，优先检查 `当前观察场景` 必需形参是否完成绑定。
```

## 2.28 6085 当前观察场景参数缺口（2026-06-29）

复跑证据：

```text
日志/鱼巢_run_20260629.低值g
    11503：扫描缺口派生需求桥已输出新目标特征。
    11831：提交_生成缺口子需求成功，目标特征=观察存在发现状态。
    11858：任务 D 已从结果能力选中方法 1B，本能ID=6085。
    11867：任务 D 当前方法=1B，本能ID=6085。
    11878：候选方法通用参数绑定 | 任务=D | 当前方法=1B | 目标宿主=Ez | 目标特征=VY | 必需=1 | 绑定=0 | 缺失=1 | 首个缺失=1wf。
    11879：候选方法筹办参数缺口 | 必需实参未绑定。
    11889：任务 D 筹办结果=参数不可配。
```

当前判断：

```text
扫描桥目标特征迁移已生效，6085 已进入候选并被选中。
中间层 / D455 供料仍不是当前第一堵点；后续日志还能看到逐簇识别等待项、报告匹配和观察事实承接完成。
当前第一堵点是任务筹办没有把派生观察需求的目标宿主 `Ez` 绑定给 6085 的唯一业务形参 `当前观察场景`。
```

修复边界：

```text
1. 不把 6085 改成从输出场景、历史报告或目标宿主兜底取数。
2. 不让扫描直接调用观察、识别或跟踪。
3. 不绕过任务筹办参数闭合；`当前观察场景` 必须在筹办输入参数包里显式闭合。
4. 观察函数仍保持一个业务参数：自我所在场景指针；中间层数据由函数内部从已提交队列 / 报告 / 材料页读取。
```

下一步：

```text
1. 在 `任务模块.筹办.ixx` 的通用参数绑定中，为指针形参 `当前观察场景` 绑定需求目标宿主 / 筹办上下文目标宿主。
2. 同步更新候选方法输入闭合预估分，让 `当前观察场景` 视为可由目标宿主闭合。
3. 复跑函数说明生成脚本、diff check、Debug x64 构建和真实运行。
4. 验证日志中 6085 参数绑定变为 必需=1 | 绑定=1 | 缺失=0，并继续追 `观察本能闭环`、新建 / 更新存在和写入特征数量。
```

## 2.29 6085 当前观察场景参数绑定构建前断点（2026-06-29）

已改文件：

```text
任务模块.筹办.ixx
    新增筹办侧 `特征_当前观察场景_筹办`。
    新增 `目标宿主可作为当前观察场景_筹办`，只允许场景节点作为观察本能当前观察场景实参。
    `方法候选输入闭合预估分` 将必需形参 `当前观察场景` 视为可由需求目标宿主闭合。
    `通用绑定候选方法输入参数_筹办` 在形参为 `当前观察场景` 且目标宿主是场景节点时，向输入条件包写入目标宿主指针。

说明书/函数功能说明文档.md
    已重新生成，函数条目 7368。
```

静态检查：

```text
git diff --check -- 任务模块.筹办.ixx 说明书/函数功能说明文档.md 实施记录/20260628_D455观察候选未入场景存在_Codex断点清单.md
退出码：0
备注：仅提示 `说明书/函数功能说明文档.md` CRLF 下次 Git 接触会转 LF；未见空白错误。
```

下一验证：

```text
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

## 2.30 最后一次对话补记：R20 观察链闭合提交（2026-06-29）

补记原因：

```text
上一次最终答复只在对话中说明了闭合结果和提交号，未在断点清单末尾形成可恢复记录。
本节补记该最终结论，不新增业务口径。
```

最终确认：

```text
1. 扫描和观察已经能够拿到中间层数据。
2. 观察函数仍保持一个业务参数：当前观察场景指针。
3. 中间层数据由观察函数内部读取已提交近组报告，不从输入参数接收中间层数据。
4. 被动识别是观察内部治理闭环：有既有存在则返回存在指针；未识别到则返回空，由观察创建新存在并补充特征。
5. 观察发现存在已写入自我所在场景 Ez 的子链；SQL 投影视图显示 Ez 直接子存在数为 13，其中除 F0 外 12 个观察存在路径为 WORLD_ROOT/2/Ez/<节点主键>。
```

最终证据锚点：

```text
日志/鱼巢_run_20260629.低值g
    15104：报告ID=39 的 D455 稳定子集报告入队，像素簇=12。
    15201：任务 B 选中 6085。
    15206-15207：当前观察场景形参已绑定，必需=1，绑定=1，缺失=0。
    15280-15281：观察成功，当前观察场景=Ez，报告ID=39，观察新建存在数量=12，观察写入特征数量=300，中间层近组取数=1。
    15283-15285：目标状态比较接受，VX0验证状态=验证成功。
    15297：任务 B 完成。

SQL 投影视图：
    Ez直接子节点数=1395。
    Ez直接子存在数=13。
    12 个观察存在直接挂在 Ez 下，类型文本=2Cr，直接子数量=25。
```

提交记录：

```text
695bb63 闭合D455观察存在入场景链
```

禁止扩大宣称：

```text
不宣称视觉系统全量完成。
不宣称自我苏醒完成。
不宣称初步成熟完成。
```

## 2.31 当前控制面板仍未显示观察存在复现断点（2026-06-29）

当前实测：

```text
用户反馈：当前还是没有存在被发现。
当前 SQL 投影视图：
    SELECT COUNT(*) FROM [鱼巢].[当前世界树节点] = 7470。
    Ez 直接子存在数 = 1。
    Ez 唯一直接子存在 = F0，自我，路径 WORLD_ROOT/2/Ez/F0。
    以“已确认观察存在 / 观察存在”检索 [当前世界树节点]，只命中抽象特征和方法节点，未命中实例观察存在。
当前运行态显示项：
    [鱼巢].[当前世界显示项] 数量 = 218。
    Ez 直接子项仅 F0。
    未命中已确认观察存在实例。
```

关键新证据：

```text
日志/鱼巢_run_20260629.低值g：
    56245、57453、61764、64710、65613 均记录观察本能闭环报告存在=1，并记录观察新建存在数量=19/21。
    11:30:13 后没有世界树SQL投影完成记录。
    11:35:09、11:35:40 又出现两次程序启动，只完成自我初始化，未记录观察写入，也未记录最终世界树SQL投影刷新。
代码现状：
    控制面板世界树区段读取 [鱼巢].[当前世界树节点]。
    观察创建节点时会同步 [鱼巢].[当前世界显示项]，但不等价于刷新 [鱼巢].[当前世界树节点] 快照。
    默认启动 self thread 时未传入刷新 SQL 投影参数，守卫退出时可能跳过最终世界树快照刷新。
```

当前判断：

```text
当前“控制面板看不到观察存在”至少包含显示读模型未刷新问题。
下一步先跑带 --self-check-log-sql-projection 的真实验证，确认退出前刷新世界树快照后 Ez 直接子存在是否恢复大于 1。
若刷新后仍为 1，再回到观察创建/挂接路径继续追根因。
```

下一验证：

```powershell
.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=90000 --self-check-log-sql-projection
sqlcmd 查询 [鱼巢].[当前世界树节点] 的 Ez 直接子存在数和已确认观察存在实例。
```

## 2.32 当前无观察存在显示的根因修正与验证（2026-06-29）

修正内容：

```text
预处理开关变量.h
    新增 `鱼巢_开关_启用程序退出SQL读模型刷新`。
    默认跟随 `鱼巢_开关_启用SQL控制面板同步写入`。

鱼巢.cpp
    程序退出守卫不再要求“本进程已执行控制面板SQL投影”才刷新最终读模型。
    只要本进程启动过自我线程，就按 `鱼巢_开关_启用程序退出SQL读模型刷新` 刷新世界树、任务树、方法树、语素、启动需求树 SQL 读模型。
```

根因收束：

```text
观察业务链可以创建观察存在。
当前用户看到“没有存在被发现”的直接原因是控制面板世界树读取 [鱼巢].[当前世界树节点] 快照，而默认/后续启动路径可能未在观察写入后刷新该快照。
因此旧快照会显示 Ez 只有 F0，掩盖内存树里已写入的观察存在。
```

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\r20_build_exit_sql_projection_20260629.txt
退出码：0

.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=90000 *> 日志\r20_self_check_exit_guard_projection_20260629.txt
退出码：0
```

关键日志证据：

```text
日志/鱼巢_run_20260629.低值g
    74759：观察本能闭环，当前观察场景=Ez，报告ID=12，观察新建存在数量=22。
    76000：观察本能闭环，当前观察场景=Ez，报告ID=57，观察新建存在数量=22。
    76412：观察本能闭环，当前观察场景=Ez，报告ID=70，观察新建存在数量=21。
    77206：程序退出守卫触发世界树SQL投影完成，节点数=19323。
    77207：世界树SQL投影刷新，标记=程序退出/守卫，成功=是。
```

SQL 验证结果：

```text
[鱼巢].[当前世界树节点]
    总节点数 = 19323。
    Ez直接子节点数 = 1345。
    Ez直接子存在数 = 27。
    Ez直接观察存在数 = 26。
    已确认观察存在路径形如 WORLD_ROOT/2/Ez/<节点主键>。
```

当前结论：

```text
当前已经能在 SQL 世界树投影视图中看到观察发现存在。
本次只修正显示读模型最终刷新条件，不扩大宣称视觉系统全量完成。
```

## 2.33 20260630 当前 SQL 快照复核

复核命令：

```powershell
sqlcmd -S .\SQLEXPRESS -d 鱼巢投影库 -E -b -f 65001 -W -s "`t" -Q "SET NOCOUNT ON; SELECT COUNT(*) AS Ez直接子节点数 FROM [鱼巢].[当前世界树节点] WHERE [父节点主键]=N'Ez'; SELECT COUNT(*) AS Ez直接子存在数 FROM [鱼巢].[当前世界树节点] WHERE [父节点主键]=N'Ez' AND [节点类型]=N'存在';"
```

结果：

```text
[鱼巢].[当前世界树节点] 总节点数 = 7485。
Ez直接子节点数 = 0。
Ez直接子存在数 = 0。
```

当前判断：

```text
当前 SQL 快照与 20260629 R20 闭合证据不一致，不能用旧投影结果宣称当前控制面板可见。
按计划复跑真实链路，并在退出守卫刷新 SQL 读模型后重新查询 Ez 直接子存在数。
```

下一验证：

```powershell
.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=90000 *> 日志\r20_self_check_exit_guard_projection_20260630.txt
```

## 2.34 20260630 真实复跑与当前场景主键修正

运行命令：

```powershell
.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=90000 *> 日志\r20_self_check_exit_guard_projection_20260630.txt
```

结果：

```text
退出码 = 0。
```

关键日志：

```text
日志/鱼巢_run_20260630.低值g
    146829：自我线程/外设观察事实承接完成，等待项ID=1，报告ID=6，报告可回查=是。
    147057：任务 8 选中 6085，当前观察场景参数绑定成功，目标宿主=F2，目标特征=W4，必需=1，绑定=1，缺失=0。
    147281：观察本能闭环，当前观察场景=F2，报告ID=22，观察新建存在数量=23，观察写入特征数量=851。
    148258：观察本能闭环，当前观察场景=F2，报告ID=31，观察新建存在数量=24，观察写入特征数量=895。
    149782-149783：程序退出守卫刷新世界树 SQL 投影，节点数=20669，成功=是。
```

SQL 投影验证：

```text
[鱼巢].[当前世界树节点]
    总节点数 = 20669。
    F2直接子节点数 = 1722。
    F2直接子存在数 = 25。
    F2 直接子存在包含 F3 自我，以及 24 个 `已确认观察存在.*` 实例。
    观察存在路径形如 WORLD_ROOT/5/F2/<节点主键>。
```

当前结论：

```text
旧计划中使用的 `Ez` 已不是当前自我初始场景主键；当前投影视图显示 `F2` 是自我初始场景，`F3` 是自我存在。
按当前场景主键 `F2` 验证，观察发现存在已经写入自我所在场景子链，R20 当前验收条件“直接子存在数量 > 1”成立。
本结论只闭合 D455 观察候选经合法链路入自我所在场景存在，不扩大宣称视觉系统完成、自我苏醒完成或初步成熟完成。
```
