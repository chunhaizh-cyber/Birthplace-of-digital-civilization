# 20260628 D455观察候选未入场景存在 Codex断点清单

## 0. 恢复摘要

当前切片：R20 `D455观察候选未入场景存在追根因`。

目标：沿“扫描需求 -> 扫描缺口 -> 观察 / 识别派生需求 -> 观察事实提交”链修复，直到自我所在场景通过观察提交多个存在。

当前判断：D455 供料不是第一堵点；最新口径要求自我先生成扫描需求，扫描缺合法材料 / 对应关系 / 当前特征值时记录结构化缺口，再由需求树和任务筹办派生观察 / 识别需求。此前直接追观察等待项会跳过扫描父需求，需要改查扫描需求、6146 选择、扫描缺口记录和缺口派生需求链。

禁止宣称：不宣称视觉系统完成，不宣称所有候选正确入账，不宣称当前场景全部存在明确，不宣称安全 / 服务闭环完成，不宣称自我苏醒或初步成熟完成。

## 1. 依据

- `计划/计划索引.md`：当前默认主动项 R20。
- `计划/追根因_20260628_D455观察候选未入场景存在_v0.1.md`：硬边界为候选不能直接变成存在，扫描需求先行，观察 / 识别由扫描缺口派生。
- `规范/禁止性规范20260611.md`：业务逻辑必须通过项目通用结构闭合，禁止用日志或文本替代事实。
- `规范/相机外设综合工作流程规范20260607.md`：内部治理生成扫描需求；扫描比较自我内部场景和已合法外设当前数据集；比较缺口作为需求来源进入需求、任务、方法闭环。
- `docs/工程图谱/02_外设观察事实图谱.md`：外设采集 -> 报告队列 -> 任务管理承接 -> 观察方法 -> 事实提交。
- `docs/工程图谱/08_事实来源与写入权限图谱.md`：外设线程只写观察材料，世界树事实由提交入口写入。

## 2. 第一独立问题

问题锚点：

```text
D455 稳定观察报告存在；
活跃等待项=0；
D455等待项数=0；
观察事实承接请求和 6150 提交链未命中。
```

当前疑点收束：

```text
需求树 / 任务筹办可能没有先生成或激活扫描需求；
或 6146 扫描没有记录结构化缺口；
或扫描缺口没有派生观察 / 识别需求；
因此任务管理没有创建 D455 等待项；
因此稳定观察材料无法进入观察事实承接和候选确认提交链。
```

已排除：

```text
1. 不按 D455 报告直接创建场景存在。
2. 不把场景体素或历史日志作为当前场景存在事实。
3. 不把 D455 稳定材料本身当作世界树事实。
```

下一步验证：

```text
1. 查当前需求树是否生成扫描父需求，尤其是“当前观察范围稳定对应存在扫描事实 / 动态取得状态”等目标。
2. 查任务筹办是否能从扫描父需求选择 6146。
3. 查 6146 是否记录结构化缺口，并让缺口进入需求生成。
4. 查缺口是否派生观察 / 识别需求，再追 6085 / 6145 / 6150 和等待项创建。
5. 用数据库投影作为主验证，日志作为辅助定位。
```

## 2.1 最新口径后的第一代码断点

问题锚点：

```text
自我_扫描当前场景已经在运行存在和输出结果场景写入：
    需求来源状态
    信息缺口集合
    信息缺口数量
    扫描治理输出状态
    后续承接入口状态

但扫描方法内部不应直接调用观察 / 识别 / 跟踪；
缺口必须由任务执行结果后处理或任务管理上行消息转成派生需求。
```

当前判断：

```text
下一步不改扫描方法直接调用观察 / 识别；
先追 6146 执行完成后，任务管理是否读取扫描输出结果场景 / 运行存在中的缺口特征，
并生成观察 / 识别派生需求消息。
如果没有该桥，最小缺口属于任务执行结果后处理 / 派生需求上报，而不是 D455 供料。
```

## 2.2 反向触发口径删除结果

本次按最新口径删除两类旧反向机器路径：

```text
1. 删除自我线程中“外设观察事实承接完成后直接确保 6146 / 6147 任务主体”的路径。
   影响文件：
       自我线程模块.impl.cpp
   删除入口：
       私有_扫描材料承接后确保6146任务主体_线程侧
       私有_扫描材料承接后确保6147任务主体_线程侧
   删除调用点：
       自我线程/外设观察事实承接完成 后的 6146 / 6147 直接任务确保调用。

2. 删除任务管理界面线程中“稳定子集全量扫描等待项”路径。
   影响文件：
       任务模块.管理界面线程.impl.cpp
   删除入口：
       私有_按稳定子集全量扫描约束提交外设等待项
       私有_构造稳定子集扫描入账派生需求上行消息
       稳定子集扫描入账派生需求幂等键和配套只读/状态构造辅助函数
   同步清理：
       工作结果应用尾部证据不再输出“稳定子集全量扫描等待项”；
       慢阶段日志不再统计“稳定子集等待项”。
```

当前正向保留路径：

```text
1. 任务管理仍可按“当前方法供料要求”创建外设观察等待项。
2. 扫描方法仍只输出扫描结果、状态变化和结构化缺口，不直接调用观察 / 识别 / 跟踪。
3. 后续必须验证：扫描需求由自我内部治理/因果筹办链生成，任务筹办查询因果选择 6146，6146 输出缺口后再由需求树和任务筹办派生观察 / 识别 / 提交链。
```

验证：

```text
python .\tools\生成函数功能说明文档.py
    退出码 0，函数条目 7318。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码 0，Debug x64 构建通过，0 警告，0 错误。
```

## 3. 已运行命令

```text
Get-Content docs/00_文档总索引.md
Get-Content 规范/规范目录.md
Get-Content 规范/禁止性规范20260611.md
Get-Content 计划/计划索引.md
Get-Content 计划/追根因_20260628_D455观察候选未入场景存在_v0.1.md
Get-Content docs/工程图谱/02_外设观察事实图谱.md
Get-Content docs/工程图谱/08_事实来源与写入权限图谱.md
Select-String docs/工程图谱/05_规则原子索引.md
git status --short --untracked-files=no
rg "私有_扫描材料承接后确保6146任务主体_线程侧|私有_扫描材料承接后确保6147任务主体_线程侧|私有_构造稳定子集扫描入账派生需求上行消息|私有_按稳定子集全量扫描约束提交外设等待项|稳定子集全量扫描等待项|稳定子集等待项" -n
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

## 4. 本轮四方法梳理

当前正式四方法口径：

```text
6085 自我_观察单个空间候选并组合存在假设 = 观察
6145 自我_识别外设观察材料 = 识别
6146 自我_扫描当前场景 = 扫描
6147 自我_跟踪指定存在 = 跟踪
```

辅助入口口径：

```text
6127 自我_验证外设观察像素簇候选 = 候选验证辅助入口，不属于四方法本体。
6150 提交_当前观察范围可观测单位存在对应事实 = 观察事实提交入口，不属于四方法本体。
```

已改代码：

```text
任务模块.管理界面线程.impl.cpp
    将外设观察默认许可集拆分为“观察四方法”和“辅助验证提交”两组。
    旧 6127/6145/6146/6147/6150 混合默认集不再作为四方法口径。
    等待项供料口径新增“观察”，6085 的观察存在假设 / 空间候选观察队列可创建逐簇识别等待项。
    旧“观察验证”口径改为“候选验证”，避免把 6127 当成观察方法。

自我线程模块.impl.cpp
    删除固定返回 false 的当前场景可观测单位硬编码 OR 展开残留。
    删除日志字段“当前场景特征值OR路径展开”，避免旧 D455 固定子需求路线继续作为当前判断锚点。
```

已确认不能删除：

```text
6127 和 6150 的实现不能删除。
原因：它们不是四方法本体，但仍是观察候选验证和事实提交链的辅助入口。
删除会破坏候选验证 / 提交链，不会解决候选未入场景存在问题。
```

当前剩余堵点：

```text
四方法口径已经收束，但当前追踪路线需要按最新口径改为扫描父需求先行；
短运行中仍可出现活跃等待项=0 / D455等待项数=0；
下一步应继续追“扫描需求是否生成 -> 6146 是否被选择 / 执行 -> 扫描缺口是否派生观察 / 识别需求 -> 6085 / 6145 / 6150 是否创建等待项和提交事实”。
```

验证结果：

```text
函数说明文档已重新生成。
Debug x64 构建通过：0 错误，0 警告。
本轮未执行 DB 投影复核，不能宣称自我所在场景已通过观察提交多个存在。
```

## 2.3 扫描缺口派生需求桥修复结果

第二个独立问题：

```text
6146 自我_扫描当前场景能输出结构化缺口事实，
但此前缺少显式交给执行后处理消费的“扫描缺口作为下轮因果筹办目标”。
如果执行后处理拿不到结构化派生需求目标，
扫描缺口不会自然进入需求树和任务筹办，
观察 / 识别需求也不会沿正向链路生成。
```

本次最小修复：

```text
影响文件：
    自我动作实现.扫描.ixx
    规范/相机外设综合工作流程规范20260607.md
    说明书/函数功能说明文档.md

扫描输出结果场景新增执行桥三件套：
    派生需求 = 观察存在假设
    派生需求当前状态 = 观察存在假设: 0
    派生需求目标状态 = 观察存在假设: 1

触发条件：
    扫描缺口数量 > 0。

输出状态修正：
    扫描缺口数量 > 0 时，扫描治理输出状态明确为 信息缺口。
```

边界：

```text
1. 不新增结构体。
2. 不恢复观察事实承接后直接确保 6146 / 6147。
3. 不恢复稳定子集全量扫描等待项。
4. 不让扫描方法直接调用观察 / 识别 / 跟踪。
5. 三件套只表达下轮因果筹办目标，后续仍必须由执行后处理、需求树、任务筹办和方法选择承接。
```

防回归：

```text
规范/相机外设综合工作流程规范20260607.md 已新增防回归硬规则：
    不得在观察事实承接完成后反向直接确保 6146 / 6147 任务主体；
    不得由稳定观察子集、外设报告或观察事实承接结果直接创建稳定子集全量扫描等待项；
    扫描缺口进入下一轮筹办必须写出派生需求三件套；
    三件套不等于扫描方法直接调用观察、识别或跟踪。
```

已验证：

```text
rg 旧反向入口：
    私有_扫描材料承接后确保6146任务主体_线程侧
    私有_扫描材料承接后确保6147任务主体_线程侧
    稳定子集全量扫描等待项
    按稳定子集全量扫描约束提交外设等待项
结果：
    0 命中。

rg 新桥入口：
    扫描缺口派生需求桥
    特征_执行桥派生需求
    特征_观察存在假设
结果：
    命中 自我动作实现.扫描.ixx 和 说明书/函数功能说明文档.md。

git diff --check：
    退出码 0，仅提示函数说明文档 CRLF/LF 工作区警告。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码 0，Debug x64 构建通过，0 警告，0 错误。
```

未验证：

```text
尚未跑真实主循环和数据库投影。
下一步必须以数据库数据为主证据，验证：
    扫描输出结果场景存在派生需求三件套；
    执行后处理消费三件套并生成派生需求上行 / 入树；
    任务筹办查询因果后选择观察 / 识别 / 提交链；
    等待项消费 D455 稳定报告；
    自我所在场景直接子存在数量 > 1。
日志只能作为辅助定位。
```

## 2.4 扫描状态 / 动态取得验证切片

本次用户收紧目标：

```text
继续追，直到自我所在场景中有通过扫描获取到的状态信息、动态信息。
```

本切片验收口径：

```text
最低目标不是“候选已经变成多个存在”，
而是先证明 6146 自我_扫描当前场景 真实执行后，
自我所在场景或扫描输出结果场景出现由扫描取得的状态信息和动态信息。

主证据：
    SQL Server / 鱼巢投影库 / 鱼巢 schema 中的世界树、当前世界显示项、当前动态显示项。

辅助证据：
    最新运行日志中的 6146、扫描输出、状态动态、扫描缺口桥等锚点。
```

运行前 DB 基线：

```text
当前运行ID：
    B5D3E833-3AEB-4554-A95C-2233992719CC

[鱼巢].[当前世界显示项]：
    285 行。

[鱼巢].[当前动态显示项]：
    26 行。

自我所在场景 Ez 下状态：
    只有安全值、服务值相关状态。

自我所在场景 Ez 下动态：
    只有安全值、服务值状态变化动态。

当前判断：
    基线尚未显示扫描取得的状态信息 / 动态信息；
    需要真实运行后复查 DB 投影。
```

预测：

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000

若扫描链已贯通到状态 / 动态：
    SQL 当前世界或世界树中应出现扫描相关状态节点；
    SQL 当前动态或世界树中应出现扫描相关动态节点；
    日志应能辅助定位到 6146 扫描、扫描输出结果场景或状态动态创建。

若未出现：
    不判定为业务闭合；
    继续追 6146 是否被任务筹办选中、是否执行、是否写入状态、状态是否进入场景索引、动态是否生成、SQL 同步是否漏投影。
```

运行后证据：

```text
SQL Server：
    实例 = .\SQLEXPRESS
    数据库 = 鱼巢投影库
    最新运行ID = F53718C6-89D5-40F7-B8B5-ED31C3217227
    状态 = 运行中

[鱼巢].[当前方法主信息]：
    方法 1 = 练习 / 本能函数ID:6036 / 结果项数量=0 / 有结果能力=0
    方法 b = 扫描当前场景 / 本能函数ID:6146 / 结果项数量=0 / 有结果能力=0

[鱼巢].[当前任务主信息]：
    当前任务仍是安全 / 服务 / 方法治理链任务；
    未见扫描父任务或扫描方法当前执行。

[鱼巢].[当前世界显示项]：
    只见“扫描当前场景(方法)”等方法存在；
    未见通过扫描取得的场景状态信息。

[鱼巢].[当前动态显示项]：
    未见扫描相关动态。
```

日志辅助证据：

```text
日志/鱼巢_run_20260628.低值g：
    6146 只见“注册外设观察执行前许可免审批默认集方法”，未见扫描执行。
    任务筹办多次候选集填充为 0。
    任务 5 / 6 的当前方法仍为 1，当前本能ID=6036。
    任务管理工作线程/默认本能方法规格确认在 10:55:24 才完成：
        默认本能方法规格确认 | 已确认=82 | 默认数量=82 | 耗时ms=17107
```

当前问题锚点：

```text
专属规格确认入口存在，但当前实现用进程内 atomic 标记“已尝试确认”。
标记在确认开始时即置位，确认耗时约 17 秒。
并发任务工作线程可能在确认完成前跳过确认并进入任务筹办，
从而按半成品方法树做结果能力召回。

即使确认完成，当前 SQL 方法投影仍显示 6146 结果能力为 0，
说明“专属规格确认完成 -> 方法树结果能力可被 SQL 验证面读到”也未闭合。
```

下一步：

```text
1. 修复任务管理工作线程首次筹办前的默认本能专属规格确认互斥 / 完成标记。
2. 确认完成后刷新方法树 SQL 投影或使用已有方法树投影刷新入口，使数据库成为结果能力验证主证据。
3. 构建后真实运行，再查 6146 是否变为有结果能力。
4. 若 6146 有结果能力但仍未被选中，再继续追扫描父需求 / 目标特征是否命中 6146 结果能力。
```

## 2.5 默认本能专属规格确认互斥修复结果

修复文件：

```text
任务模块.管理工作线程.ixx
```

修复内容：

```text
将首次任务筹办前的默认本能专属规格确认从 atomic “已尝试”标记改为互斥完成闸门。
其它工作线程必须等确认完成后才能进入任务筹办。
确认完成后刷新方法树 SQL 投影，保证数据库验证面能看到方法结果能力。
```

验证：

```text
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码 0
    0 警告
    0 错误

.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000
    退出码 0

日志/鱼巢_run_20260628.低值g：
    方法树SQL投影完成 | 来源=任务管理工作线程/运行态默认本能方法规格确认 | 节点数=249
    任务管理工作线程/运行态默认本能方法规格确认 | 已确认=82 | 默认数量=82 | 确认完成=1 | 方法树SQL投影刷新=1

SQL 最新运行ID：
    73908420-89F0-452D-A1F7-AE9B059C3BE9

[鱼巢].[当前方法主信息]：
    方法 1 = 练习 / 本能函数ID:6036 / 主结果特征=15y / 结果项数量=2 / 有结果能力=1
    方法 b = 扫描当前场景 / 本能函数ID:6146 / 主结果特征=ez / 结果项数量=4 / 有结果能力=1
```

当前剩余堵点：

```text
6146 已具备结果能力，但当前需求树没有扫描目标。

[鱼巢].[当前需求主信息] 仍只包含：
    安全值 Q4
    服务值 QO
    服务触发事实监听状态 1FU
    服务机会检查状态 1Fb
    服务能力校准状态 1Fi
    方法维护管理状态 1CL
    方法状态 5s

当前世界 / 动态投影：
    仍未出现通过扫描取得的场景状态信息或扫描动态。
```

下一步：

```text
追“扫描需求应由哪里产生”：
    1. 查规范中扫描需求生成口径；
    2. 查代码中安全 / 服务 / 当前场景维护是否生成目标特征=已识别区域扫描入账状态 / 外设扫描结果场景状态 / 当前场景扫描组合状态；
    3. 若缺少正式生成入口，按规范补最小正向入口，不恢复观察事实反向触发。
```

## 2.6 当前场景维护扫描需求生成切片

本轮目标：

```text
不从 D455 报告、观察事实承接或等待项反向触发扫描；
由自我内部治理在需求整理阶段正向生成当前场景维护扫描需求；
让任务筹办按目标特征命中 6146 自我_扫描当前场景；
用数据库投影验证扫描取得的状态信息 / 动态信息。
```

预测：

```text
修改点：
    自我线程模块.impl.cpp

预期结构：
    服务根需求
        -> 当前场景维护扫描需求
            目标宿主 = 自我存在
            需求场景 = 自我所在场景
            目标特征 = 已识别区域扫描入账状态
            当前状态 = 0
            目标状态 = 1

真实运行后若链路闭合：
    [鱼巢].[当前需求主信息] 出现目标特征“已识别区域扫描入账状态”；
    [鱼巢].[当前任务主信息] 出现该需求对应任务，筹办选择 6146；
    [鱼巢].[当前世界显示项] 出现扫描输出状态；
    [鱼巢].[当前动态显示项] 出现扫描相关动态。

若只出现扫描需求但未筹办 6146：
    继续追任务筹办方法能力匹配、目标特征缓存、任务场景和候选方法集。

禁止：
    不恢复观察事实承接后直接确保 6146 / 6147；
    不恢复稳定子集全量扫描等待项；
    不把任务筹办 / 任务执行恢复为本能方法；
    不用日志替代数据库事实证据。
```

## 2.7 自检运行态 SQL 投影验收开关切片

当前堵点：

```text
真实运行日志已显示当前场景维护扫描需求入树、任务筹办选择 6146、6146 扫描执行并产生扫描状态 / 动态；
但 `--self-check-log` 默认不标记“本进程已执行控制面板 SQL 投影”，
退出守卫跳过最终 SQL 投影刷新，数据库读模型可能停留在旧批次或启动态。

因此数据库无法作为同一次扫描运行的主证据。
```

本次最小修复：

```text
影响文件：
    鱼巢.cpp

新增显式验收开关：
    --self-check-log-refresh-sql
    --self-check-log-sql-projection

行为：
    仅当自检命令携带上述开关时，
    自我线程启动前按需初始化 / 刷新 SQL 读模型，
    自检等待结束前刷新治理 SQL 投影和需求树 SQL 投影，
    程序退出守卫最终刷新世界树、任务树、方法树、语素树和需求树 SQL 投影。

边界：
    SQL 仍只是只读验证投影；
    不反写业务状态；
    不用日志替代数据库事实；
    不改变普通 `--self-check-log` 默认轻量行为。
```

预测：

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-refresh-sql --self-check-log-wait-ms=30000

若链路闭合：
    [鱼巢].[最新批次] 变为本次运行时间；
    [鱼巢].[当前需求主信息] 出现目标特征“已识别区域扫描入账状态”的扫描维护需求；
    [鱼巢].[当前任务主信息] 出现对应任务，当前方法为 6146 扫描；
    [鱼巢].[当前世界显示项] 出现扫描输出状态；
    [鱼巢].[当前动态显示项] 出现扫描相关动态。

若仍未出现：
    继续追 SQL 投影源表是否遗漏输出结果场景 / 动态显示项，
    或追扫描状态 / 动态是否只在日志中出现而未挂入世界树 / 动态结构。
```

验证结果：

```text
构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码 0，0 警告，0 错误。

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-refresh-sql --self-check-log-wait-ms=30000
    外层命令超时码 124；随后确认进程自然退出，退出守卫完成最终 SQL 投影刷新。

运行态 SQL 批次：
    [鱼巢].[控制面板运行批次]
        运行ID = 2A288F7F-9C7B-4F6E-AFCE-26940C0C46B1
        来源标记 = 鱼巢::main/命令行/自我实现检查
        进程ID = 9512
        同步开关 = 1

SQL 当前显示项计数：
    [鱼巢].[当前世界显示项] = 1157
    [鱼巢].[当前动态显示项] = 537
    [鱼巢].[当前需求主信息] = 13
    [鱼巢].[当前任务主信息] = 12
```

扫描需求 / 任务证据：

```text
[鱼巢].[当前需求主信息]：
    需求 6
    场景主键 = Ez
    目标宿主主键 = F0
    当前状态主键 = 1wv
    目标状态主键 = 1ww
    目标特征主键 = ez
    任务主键 = 5

[鱼巢].[当前任务主信息]：
    任务 5
    名称文本 = 已识别区域扫描入账状态
    需求主键 = 6
    虚拟存在主键 = 2Bi
    场景主键 = Ez
    目标状态主键 = 1ww
    结果状态主键 = 1wv
    当前状态 = 等待中

[鱼巢].[当前世界树节点] / 任务虚拟存在 2Bi：
    任务选中自我能力 = 6146
    候选方法数量 = 1
    当前方法运行存在 = 指针句柄
    输出结果场景 = 指针句柄
```

扫描状态信息证据：

```text
[鱼巢].[当前世界树节点] / 扫描方法运行虚拟存在 3QF：
    已识别区域扫描入账状态 = 1
    外设扫描结果场景状态 = 1
    当前场景扫描组合状态 = 1
    扫描闭合状态 = 1
    扫描运行状态 = 2
    扫描治理输出状态 = 2
    信息缺口数量 = 2
    扫描主动因果信息数量 = 10
    扫描状态变化动作动态数量 = 9

[鱼巢].[当前世界树节点] / 自我所在场景 Ez：
    状态 1ww = 已识别区域扫描入账状态，值 = 1
    状态 1wv = 已识别区域扫描入账状态，值 = 0
    当前场景扫描组合状态 = 1
    外设扫描结果场景状态 = 1
    已识别区域扫描入账状态 = 1
    信息缺口数量 = 2
```

扫描动态信息证据：

```text
[鱼巢].[当前世界树节点] / 自我所在场景 Ez：
    3aX = 扫描当前场景 动态
    3ZL / 3Yk / 3YD / 3Wh / 3Vv / 3Tv / 3TP / 3TB / 3Sk / 3S5
        = 扫描当前场景(主动动作动态)
    3Z6 / 3Yt / 3YV / 3Y2 / 3WK / 3UC / 3Tz / 3Td / 3TS / 3TH / 3T3 / 3Rv
        = 状态变化(无动作状态变化动态)

[鱼巢].[当前动态显示项]：
    来源主键 = 3QF 的状态变化动态 12 条。
```

当前结论：

```text
阶段目标“自我所在场景中有通过扫描获取到的状态信息、动态信息”已用数据库主证据确认。

仍未完成的 R20 总目标：
    通过扫描缺口继续派生观察 / 识别 / 提交链，
    让 D455 观察候选经提交入口成为自我所在场景中的多个存在。
```

## 2.5 观察本能 6085 未执行追踪断点（2026-06-28）

当前三段确认：

1. 6085 `自我_观察单个空间候选并组合存在假设` 已被任务 7 选为当前方法 / 执行方法，任务虚拟存在 `3f1` 上保留 `任务执行方法=1B`、`任务选中自我能力=6085`。
2. D455 等待项 2 已被报告 5 消费，报告 ID 已写入任务 7 的任务虚拟存在，`外设观察报告ID=5`，`任务等待缺口唤醒=1`。
3. 日志未出现 `任务管理工作线程/执行任务入口 | 管理任务=7 | 当前本能ID=6085`，也未出现 `观察存在假设 |` 或 `双目相机本能方法结果 | 方法=自我_观察单个空间候选并组合存在假设`。

当前直接堵点：

```text
任务 7 收到报告并进入待重筹办后，下一轮筹办返回“存在活动子需求”。
数据库显示需求 9 的直接子需求 B 仍为 已截止=0、阻塞父任务=1；任务 9 已完成。
因此任务 7 不是缺外设报告，而是被未收束的子需求 B 挡住。
```

关键证据：

```text
SQL: 当前需求树节点
9 -> B -> C
B: 已截止=0, 阻塞父任务=1, 任务=9
C: 已截止=1, 阻塞父任务=1, 任务=A

SQL: 当前任务树节点
7: 等待中, 需求=9
9: 完成, 需求=B, 结果状态=4Wy
```

下一步：

```text
先用更长真实运行窗口复验是否只是 30 秒停机窗口不足；
若仍卡住，修任务完成后来源需求 B 收束 / 父需求 9 任务 7 唤醒路径。
```

## 2.6 观察本能 6085 调用确认修正（2026-06-28 14:00 后）

本节修正 `2.5` 中“6085 未执行”的旧判断。新证据显示：

```text
1. 6085 会被任务筹办选为当前方法：
   任务筹办跟踪 | 选择当前方法完成 | 任务=7 | 方法=1B | 本能ID=6085

2. 6085 会进入任务执行入口：
   任务管理工作线程/执行任务入口 | 管理任务=7 | 当前方法=1B | 当前本能ID=6085

3. 任务筹办会给 6085 写入输入参数场景和报告 ID：
   外设观察报告ID参数筹办 | 任务=7 | 报告ID=162 | 输入场景=5G6 | 写输入场景=1 | 写输入条件包=1
   候选方法筹办参数已闭合 | 任务=7 | 当前方法=1B | 必需实参数=3 | 已绑定实参数=3 | 输入场景=5G6

4. 6085 函数体会运行并输出失败结果：
   双目相机本能方法结果 | 方法=自我_观察单个空间候选并组合存在假设 | 成功=0
   失败原因=执行缺口_当前方法条件不满足
   消息=缺少已落账当前观察特征帧
   目标宿主=Ez
   动作动态=5I6
```

因此当前三段确认结论为：

```text
是否被调用：
    是。6085 被任务 7 选中并进入任务执行入口。

是否收到参数并运行：
    是，但输入只闭合到外设观察报告 ID / 输入条件包；
    6085 运行后读取不到已落账当前观察特征帧。

是否向世界树写入信息：
    写入了失败运行结果和动作动态；
    未写出成功的观察存在假设；
    数据库投影显示 Ez 直接子存在仍只有 F0 自我，
    Ez 下观察存在假设 I64 结果节点数量为 0。
```

当前根因方向：

```text
6085 依赖 `读取已落账观察特征帧摘要`：
    目标宿主 / 输入参数场景 / 输出结果场景
    必须能读到 `当前观察特征帧 > 0` 和宽高摘要。

但当前任务筹办写入的是外设观察报告 ID，
没有把报告中的当前观察特征帧摘要、空间候选逐项账本
转换或挂接到 6085 可读取的输入参数场景 / 输出结果场景 / 目标宿主。

所以真实堵点不是“观察本能没有被调用”，
而是“观察本能收到的输入材料层级不满足它的读取契约”。
```

本轮新增诊断改动：

```text
本能动作管理类.ixx：
    将原仅服务 6150 的动作调用诊断扩展到 6085。
    只诊断，不改变检查判定，不绕过任务筹办 / 任务执行 / 本能动作管理链。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    结果：0 错误，0 警告。

运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-refresh-sql --self-check-log-wait-ms=30000
    结果：90 秒未自然退出，已手动收束进程。
    本次 14:00 运行中任务 7 已结算，未再次自然推进到 6085；
    D455 报告仍在入队，但活跃等待项=0。
```

下一步最小切片：

```text
追查 `当前观察特征帧` / `空间候选逐项账本` 的正式写入来源。

候选修复方向：
    1. 若应由 `双目相机_取得当前观察特征帧` 先行生成，
       则任务筹办不能直接执行 6085，必须先经因果链派生并执行前置观察特征帧取得步骤。
    2. 若 D455 稳定报告承接已经具备等价材料，
       则只能把材料转换 / 挂接为观察证据层的项目结构，
       不得直接写成场景稳定存在事实。
```

## 2.7 观察中间层被动动作接入

用户最新口径：

```text
主动观察本能函数发现自身执行所需的中间层输出缺失时，
可以直接调用独立的中间层被动动作函数；
中间层原本的主动本能函数不改成被动 / 主动双模式。
被动动作函数执行时自动生成被动动作动态。
```

本次实现：

```text
1. `写入双目相机I64事实与动态` 增加动作语义覆盖参数。
   默认仍使用方法动作名；被动调用时写 `被动动作`。

2. `双目相机_取得当前观察特征帧` 保持正常主动本能函数入口。
   采集和写账逻辑下沉为普通共享 helper `执行双目相机取得当前观察特征帧写账`。

3. 新增独立被动动作函数 `被动动作_取得当前观察特征帧`：
   - 只由主动观察函数内部触发；
   - 不作为新的主动本能方法候选；
   - 动作动态语义写为 `被动动作`；
   - 仍通过 6097 对应方法头写入中间层取得观察特征帧结果。

4. `自我_观察单个空间候选并组合存在假设` 首次读取
   `当前观察特征帧 / 空间候选逐项账本` 失败时：
   - 触发 `被动动作_取得当前观察特征帧`；
   - 重新读取已落账中间层输出；
   - 若本轮观察动作生成动态，则把被动动态追加为来源低层动态，
     并在观察动态上写入 `来源动作动态`。
```

已改文件：

```text
自我动作实现.外设模块.ixx
说明书/函数功能说明文档.md
实施记录/20260628_D455观察候选未入场景存在_Codex断点清单.md
```

验证：

```text
git diff --check -- 自我动作实现.外设模块.ixx
结果：通过。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：0 错误，0 警告。

python .\tools\生成函数功能说明文档.py
结果：已生成函数功能说明文档，函数条目 7329。

2026-06-28 14:54 修正后复验：
git diff --check -- 自我动作实现.外设模块.ixx
结果：通过。

python .\tools\生成函数功能说明文档.py
结果：已生成函数功能说明文档，函数条目 7329。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：0 错误，0 警告。

.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-refresh-sql --self-check-log-wait-ms=45000
结果：60 秒未自然退出，已停止进程。
日志判断：本次 14:45 运行命中当前场景维护扫描需求和 SQL 投影刷新；
         未自然推进到 6085 执行，未出现 `观察中间层被动动作` 或 `观察中间层被动补齐`。
```

当前禁止宣称：

```text
本次只证明代码接入和构建通过；
尚未证明真实 D455 运行中 6085 已成功生成观察存在假设；
尚未证明 6150 提交后 Ez 直接子存在数量大于 1。
被动中间层路径尚未取得自然业务命中证据。
```

下一步：

```text
运行真实 D455 / SQL 刷新路径，观察日志和数据库：
1. 是否出现 `观察中间层被动动作`。
2. 是否出现 `观察中间层被动补齐 | 重读成功=1`。
3. 是否出现 `观察存在假设`。
4. 数据库中 Ez 下观察中间层状态、动态、来源动作动态是否成链。
```
