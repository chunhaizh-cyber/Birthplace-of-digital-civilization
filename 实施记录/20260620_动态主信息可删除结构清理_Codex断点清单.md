# 20260620 动态主信息可删除结构清理 Codex 断点清单

## 0. 恢复摘要

当前切片：

```text
计划/20260620_动态类逻辑纠偏计划_v0.1.md
P7 动态主信息可删除结构清理。
```

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
规范/方法动作动态硬规则规范20260512.md
规范/动作动态即因果账本规范20260512.md
规范/动态信息分层获取与聚合规范20260430.md
计划/计划索引.md
计划/20260620_动态类逻辑纠偏计划_v0.1.md
```

当前判断：

```text
1. 枚举_动态因果生成类别 可删除，动态类型语素入口已承载主动动作动态 / 被动动作动态 / 无动作状态变化动态。
2. 枚举_动作事件相位 与 来源动作相位 可删除，不再用完成 / 失败作为动作动态本体分类轴。
3. 枚举_动态聚合方式 整体暂不可删除；只删除未落地的 窗口聚合 / 路径压缩。
```

下一步：

```text
1. 删除 动态因果生成类别 枚举、函数、输出参数和调用点。
2. 删除 动作事件相位 枚举、字段、接口参数和调用点。
3. 删除 动态聚合方式 未落地枚举值和显示 / 映射分支。
4. 重新生成 说明书/函数功能说明文档.md。
5. 运行 Debug x64 构建。
```

禁止宣称：

```text
不宣称动态类逻辑纠偏完成。
不宣称因果类功能补完完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 1. 初始扫描证据

初始残留扫描命令：

```powershell
rg -n "枚举_动态因果生成类别|动态因果生成类别|枚举_动作事件相位|来源动作相位|窗口聚合|路径压缩" -g "*.h" -g "*.cpp" -g "*.ixx"
```

命中范围摘要：

```text
基础信息主信息类.h：枚举与字段定义。
动态类.h / 动态类.cpp：动态因果类别推导、动作相位参数与字段写入。
因果类.cpp：读取动态因果生成输入后的类别判断与日志。
二次特征应用模块.ixx：聚合粒度映射、同来源事件比较、聚合动态字段复制。
控制面板类.cpp：旧字段显示文本。
任务模块.执行.ixx、自我类.cpp、自我动作实现*.ixx、方法虚拟存在服务类.cpp、自我线程模块.impl.cpp：相位传参或字段写入。
```

## 2. 闭合证据

已改范围：

```text
基础信息主信息类.h
动态类.h
动态类.cpp
因果类.cpp
二次特征应用模块.ixx
控制面板类.cpp
任务模块.执行.ixx
自我类.cpp
自我动作实现模块.ixx
自我动作实现.扫描.ixx
自我动作实现.服务模块.ixx
自我动作实现.外设模块.ixx
方法虚拟存在服务类.cpp
自我线程模块.impl.cpp
说明书/函数功能说明文档.md
计划/20260620_动态类逻辑纠偏计划_v0.1.md
计划/计划索引.md
```

删除结果：

```text
1. 删除 枚举_动态因果生成类别 及动态因果生成类别文本 / 推导函数。
2. 删除 枚举_动作事件相位 与 来源动作相位 字段。
3. 删除 创建方法动作动态 / 绑定来源方法 的 相位 参数。
4. 删除 窗口聚合 / 路径压缩 预留枚举值。
```

保留结果：

```text
枚举_动态聚合方式 整体保留；原子相邻、连续同向段、同类事件段仍为二次特征聚合链的结构字段。
来源执行成功 / 来源错误码 未在本切片处理。
```

验证命令与结果：

```powershell
rg -n "枚举_动态因果生成类别|动态因果生成类别|枚举_动作事件相位|来源动作相位|窗口聚合|路径压缩" -g "*.h" -g "*.cpp" -g "*.ixx"
# 无输出，退出码 1。

python .\tools\生成函数功能说明文档.py
# 已生成：D:\鱼巢\说明书\函数功能说明文档.md
# 源码文件：128
# 函数条目：6866

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
# 退出码：0
# 日志：日志/20260620_动态主信息结构清理_msbuild.log
# 关键行：已成功生成；0 个错误。
```

当前返回点：

```text
计划/20260619_因果类功能函数补完计划_v0.1.md
P5 被动触发初始因果模板确保函数。
```
