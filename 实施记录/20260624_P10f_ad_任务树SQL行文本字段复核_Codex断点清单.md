# P10f-ad 任务树 SQL 行文本字段复核 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-ad `任务类.cpp::结构_任务树SQL行` 文本字段边界复核。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `规范/任务筹办与执行边界规范20260512.md`
- `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_calibration_resource_fields.tsv`

复核字段：

- `路径`
- `节点种类文本`
- `任务状态文本`
- `名称文本`
- `类型文本`

当前判断：

- `结构_任务树SQL行` 是 `任务类::重写任务树SQL投影(...)` 的局部 SQL 投影行结构。
- `节点种类文本` 由 `枚举_任务节点种类` 渲染；同一 SQL 行同时保留 `节点种类值`。
- `任务状态文本` 由 `枚举_任务状态` 渲染；同一 SQL 行同时保留 `任务状态值`。
- `名称文本 / 类型文本` 来自任务节点主信息的语素入口显示文本，用于 SQL 展示名。
- `路径` 写入 `fishnest.task_tree_node.path_text`，用于 SQL 投影树显示和控制面板按 SQL 直接子层查询。

检索证据：

- `任务类.cpp` 中文本字段只在收集 SQL 行、建表 / 建视图、生成 INSERT 脚本中出现。
- `控制面板类.cpp` 读取 `fishnest.v_current_task_tree_nodes` 的 `node_kind_text / task_state_text` 用于 SQL 控制面板任务树表格和子层 JSON。
- `需求类.cpp` 只在需求 SQL 展示视图中用 `task.name_text / task.type_text / task.node_kind_text / task.task_state_text` 生成展示名和类型名。
- 未发现这些文本字段进入任务状态推进、候选选择、等待判断、调度选择、结算或因果匹配。

处理结论：

- 本切片不做代码删除或改名。
- 这些字段按 SQL / 控制面板投影文本登记边界；机器状态仍由任务树节点、任务状态枚举、主键和引用字段承载。
- 未改代码，未运行构建。

下一步：

- 返回 P10f 最新复扫清单，复核 `任务模块.管理界面线程.impl.cpp::结构_后台worker当前工作快照` 的 `工作项类型 / 状态` 是否只用于线程显示。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
