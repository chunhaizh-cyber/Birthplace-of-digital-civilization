# 需求树简化与内部治理闭环 P0 Codex断点清单

日期：2026-06-30

## 0. 恢复摘要

当前切片：R24 `计划/20260630_需求树简化生成与内部治理闭环计划_v0.1.md` 的 P0 现状对账。

执行入口：用户已明确要求“执行计划”，满足 R24 在 `计划/计划索引.md` 中“需授权 -> 用户明确要求执行该计划时，从 P0 现状对账开始”的进入条件。

本轮边界：

```text
只读核查当前根需求、根任务、因果生成、任务筹办和服务触发事实准入代码现状；
形成现状差距表；
不改业务代码；
不把旧实施记录、控制面板投影或历史日志当成当前代码闭环；
不宣称自我苏醒完成或初步成熟完成。
```

当前状态：P0 现状对账已完成；未进入代码修改。

下一步：进入 R24 P1a 前，先修正根目标最大值读取口径；代码改动前按 AGENTS.md 再读取目标文件头部规则和函数说明文档命中段。

## 1. 依据文件

```text
AGENTS.md
docs/00_文档总索引.md
规范/禁止性规范20260611.md
规范/规范目录.md
docs/工程图谱/05_规则原子索引.md
计划/计划索引.md
计划/20260630_需求树简化生成与内部治理闭环计划_v0.1.md
```

## 2. 已运行命令

```text
git status --short --untracked-files=no
Get-Content docs/00_文档总索引.md -TotalCount 80
Get-Content 规范/规范目录.md -TotalCount 120
Select-String 规范/禁止性规范20260611.md -Pattern "结构体|布尔|项目通用数据结构|禁止用自由文本|闭环"
Select-String docs/工程图谱/05_规则原子索引.md -Pattern "结构体|布尔|需求|任务筹办|因果|服务|服务值|安全值"
Get-Content docs/工程图谱/00_图谱总索引.md -TotalCount 120
Select-String docs/工程图谱/01_任务需求方法闭环图谱.md -Pattern "需求|任务筹办|因果|根需求|任务完成|规则原子|代码入口"
Select-String docs/工程图谱/03_安全服务结算图谱.md -Pattern "安全|服务|G0|D2|结算|根需求|服务值|规则原子|代码入口"
rg -n "私有_吸收筹办派生需求消息|派生需求消息|派生消息字段完整|筹办_按因果反推必要状态|直接写需求树|实际发送派生需求消息" "任务模块.管理工作线程.ixx" "任务模块.筹办.ixx"
rg -n "私有_刷新根需求状态镜像|私有_服务根闭环触发事实存在|私有_生成根闭环|私有_收束根特征必要条件子需求|私有_生成根特征必要条件子需求|私有_增强任务结算被动因果|私有_增强父任务重筹办被动因果|私有_调用结算叶子任务价值本能方法" "自我线程模块.impl.cpp"
rg -n "获取或创建G0治理视图|写入G0硬门槛状态|汇总G0硬门槛状态|计算成功服务值饱和入账|写入服务值结算候选结果|创建服务值结算D2动作动态|候选可结算|成功服务候选状态" "自我动作实现.服务模块.ixx"
rg -n "普通目标直接生成|枚举_需求获取途径|构造获取途径逻辑组织需求更新指令|获取途径" "需求类.h" "需求类.cpp"
rg -n "同源|证据组|来源低层动态|状态迁移|动作致变|读取动态因果生成输入" "动态类.cpp" "动态类.h" "因果类.cpp" "世界树类.cpp"
```

## 3. 当前判断

R24 已注册在计划索引 R24。用户已明确要求“执行计划”，满足该项从“需授权”进入 P0 的条件。本轮按计划第 7 章“第一轮只执行 P0”收束为只读对账，不进入 P1 代码修改。

第一优先缺口是根目标最大值读取口径不一致：`自我类.cpp:194-221` 仍查找抽象特征子项“高位”并读取 `目标区间.低值`，而线程侧根特征定义读取 `自我线程模块.impl.cpp:625-632` 已优先查“最大值”再兼容“高位”。进入 P1a 时应先收束该口径，且不得新增目标字段或结构体。

## 4. P0差距表

| 链路 | 现有入口 | 现有承载结构 | 缺口 | 可复用函数 | 禁止修改范围 | 下一阶段 |
| --- | --- | --- | --- | --- | --- | --- |
| 根目标最大值读取 | `自我类.cpp:194-221`、`自我线程模块.impl.cpp:594-657` | 抽象特征树目标状态、`I64区间`、自我安全值 / 服务值当前 I64 特征 | 自我类仍查“高位”并取 `目标区间.低值`；线程侧已优先查“最大值”再兼容“高位”，两条读法不一致 | `私有_读取自我根特征抽象目标值_自我类`、`私有_读取抽象根特征定义_线程侧`、`根目标安全值()`、`根目标服务值()` | 不新增根目标字段；不把“安全值高/低 -> 服务目标值升降”写成根定义 | P1a |
| 根任务承接策略 | `自我类.cpp:2109-2158`、`自我类.cpp:2250-2310`、`自我类.初始化.cpp:238-260`、`任务类.cpp:2513-2565` | 根需求节点、任务根节点、任务主信息对应需求、任务虚拟存在 | `确保根任务_` 已存在但初始化链当前只确保根需求和方法头；根任务允许延后，根需求未唯一承接到安全 / 服务根任务 | `自我类::确保根任务_`、`任务类::创建任务头结点`、`任务类::绑定对应需求`、默认根任务命名 `任务类.cpp:292-315` | 不把任务筹办 / 任务执行任务化；不新增根任务结构体 | P1b |
| 根闭环因果信息 | `自我线程模块.impl.cpp:11317-11505`、`自我线程模块.impl.cpp:11980-12190`、`因果类.cpp:2162-2263` | 根闭环信息位、根需求状态镜像、因果投影缺失证据、需求节点 | 根闭环信息位只记录“直接生成需求=0 / 仅挂载不入树=1”；服务根直接触发事实固定返回 false；来源因果主键可能为空并产生 `缺来源因果信息` | `私有_生成根闭环信息位_线程侧`、`私有_记录根闭环信息位挂载_线程侧`、`查询自检原子目标投影` | 不用日志或信息位文本替代可追溯因果结构；不让历史子需求反推服务触发事实 | P1c/P2 |
| 状态变化因果 / 线索 | `因果类.h:66-69`、`因果类.cpp:2266-2306`、`因果类.cpp:2476-2525` | 状态迁移二次特征、动态证据样本、状态变化因果模板 | 状态变化因果生成链存在，但必须已有果状态迁移模板；缺果状态迁移模板时不会生成因果模板 | `因果类::生成因果信息`、`私有_查找动态果状态迁移模板`、`查询因果链` | 不把单次状态线索直接宣称已验证因果；不以文本说明冒充状态迁移二次特征 | P2 |
| 被动因果闭环 | `因果类.h:163-172`、`自我线程模块.impl.cpp:6493-6520`、`自我线程模块.impl.cpp:6523-6548` | 被动触发初始因果模板、任务已结算动作动态、D2 结算动作动态、父任务状态动作动态 | 结算和父任务重筹办的被动因果增强入口存在；仍需核查被动因果是否完整承接到根闭环和后续任务筹办消费，不得只停留在增强日志 | `因果类::确保被动触发初始因果模板`、`私有_按动态生成被动触发因果信息_线程侧`、`私有_增强任务结算被动因果_线程侧` | 被动因果不得进入普通方法候选；不得把线程当动作来源 | P2 |
| 主动因果生成 | `因果类.cpp:2309-2610` | 方法动作动态、动作语义键、因方法模板、状态迁移二次特征、动作致变因果模板 | 有动作动态时可生成动作致变关系并追加证据动态；下一阶段需确认初始任务能由主动因果承接，而不是只由被动线索或规则投影承接 | `因果类::生成因果信息`、`动态类::读取动态因果生成输入`、`私有_因果模板动作命中动态` | 不把外部变化伪装成自我方法动作动态；不把线程作为动作来源 | P3 |
| 同源变化证据组 | `动态类.h:134-150`、`动态类.cpp:933-1007`、`动态类.cpp:2219-2225`、`世界树类.cpp:3665-3703` | 动态双状态材料、来源低层动态引用链、聚合动态来源链、状态 / 动作双账可复算关系 | 未看到独立“同源变化证据组”持久业务结构；当前更像通过同源可复算函数和来源低层动态链重算。后续需决定是否以可复算视图满足，不新建结构体 | `动态双状态同源可复算`、`读取动态来源低层动态`、`比较动态来源低层动态链`、`追加来源低层动态` | 不新增同源证据组业务结构体；不重复结算同一次变化 | P2/P3/P8 |
| 需求入树准入 | `需求类.h:224-238`、`需求类.cpp:1360-1434`、`需求类.cpp:2301-2320`、`需求类.cpp:2488-2500` | 需求主信息、获取途径枚举、需求目标语义视图、需求树更新指令、枚举目标校验 | `普通目标直接生成` 仍有文本语义并映射为普通可任务化目标；与 R24 “只作兼容枚举，不再作为凭空普通目标来源”的口径不一致 | `需求获取途径语义视图`、`需求目标语义_按获取途径`、`构造获取途径逻辑组织需求更新指令`、枚举目标入树校验 | 不新增准入 DTO；不把内部治理缺口普通需求化；布尔综合目标必须先展开子特征集合 | P4 |
| 任务筹办消费因果 | `任务模块.筹办.ixx:1-4`、`任务模块.筹办.ixx:1065-1088`、`任务模块.筹办.ixx:2202-2305`、`任务模块.筹办.ixx:5803-5832`、`任务模块.管理工作线程.ixx:948-1061` | 临时因果视图、派生需求消息草案、任务推进回执、任务管理上行消息 | 筹办侧只形成草案，工作线程规范化、字段完整校验、去重并上行；正式入树仍需自我线程 / 需求类裁决链继续核查 | `筹办_按因果反推必要状态`、`构造正式派生需求消息草案_筹办查询`、`私有_吸收筹办派生需求消息` | 筹办不是本能方法、不是动作动态来源；不得直接写需求树 | P5 |
| 安全 / 服务主轴裁决 | `自我类.cpp:1779-1827`、`自我线程模块.impl.cpp:12370-12437` | 根需求压力、根需求权重阻塞更新指令、根需求状态镜像、根闭环信息位 | 安全 / 服务按差值和权重形成压力选择；需按最新口径收束为“安全高则服务优先级提升，安全低则安全优先级提升”，不写成服务目标值根定义升降 | `自我类::评估根需求压力`、`私有_刷新根需求状态镜像_线程侧`、`需求类::构造需求权重阻塞更新指令` | 不把服务目标值升降固化为根定义；不让控制面板摘要参与裁决 | P6 |
| 直接服务触发事实准入 | `自我线程模块.impl.cpp:11317-11326`、`自我动作实现.服务模块.ixx:2542-2698` | 服务根需求、服务触发事实入口、G0 治理视图、硬门槛特征、候选状态特征 | 直接服务触发事实函数当前固定 false；G0 治理视图和硬门槛写入路径存在，但服务根不能靠自身或历史子需求生成具体服务任务 | `私有_服务根闭环触发事实存在_线程侧`、`获取或创建G0治理视图`、`写入G0硬门槛状态`、`汇总G0硬门槛状态` | 不用服务根需求本身充当服务任务触发；不把任务完成或输出存在反推服务成功 | P7 |
| 结算回写根状态 | `需求类.cpp:1781-1841`、`自我动作实现.服务模块.ixx:3544-3684`、`自我线程模块.impl.cpp:7177-7248` | 需求结算收束结果、G0 确认候选、D2 服务值结算动作动态、输入 / 输出场景、来源 D0/D1 | 需求结算摘要、服务值饱和入账和 D2 动态生成入口存在；还需核查 D2 / 需求收束 / 根状态镜像如何进入下一轮根需求判断 | `需求类::提交需求结算收束`、`计算成功服务值饱和入账`、`写入服务值结算候选结果`、`创建服务值结算D2动作动态`、`私有_调用结算叶子任务价值本能方法_线程侧` | 任务完成 != 需求满足；D2 服务值上升不得反推成功服务；`I64_MAX` 不得入账 | P8 |
| 结构体新增 / 替代审查 | `规范/禁止性规范20260611.md:100-105`、`docs/工程图谱/05_规则原子索引.md:210-238`、现有 `结构_根闭环信息位_线程侧`、`结构_临时因果视图`、`结构_任务管理派生需求消息` | 已有局部 / 运行期辅助结构、项目通用结构、存在 / 场景 / 特征 / 状态 / 动态 / 因果 / 需求 / 方法节点 | 当前代码已有多处辅助结构；P0 不清理。后续代码切片默认不得新增持久业务结构体，若确需新增必须先证明项目通用结构无法承载 | 复用现有项目通用结构；必要时复用已有局部 helper 结构 | 不新增持久业务结构体；不让 helper 结构成为跨域权威事实 | P1-P8 |
| 布尔型综合状态特征展开审查 | `规范/禁止性规范20260611.md:109-114`、`docs/工程图谱/05_规则原子索引.md:240-275`、`自我动作实现.服务模块.ixx:437-608`、`任务模块.筹办.ixx:1069-1080` | 抽象特征树、值类结果、硬门槛 I64 状态、候选状态、任务筹办临时状态 | 已存在 `成功服务候选状态`、`候选可结算`、服务硬门槛和若干“明确 / 可用 / 完整”类状态；后续新增布尔综合特征前必须展开子特征集合、值域、判定规则、写入方、来源动态和回读验证 | 服务 G0 硬门槛写入 / 汇总函数、任务筹办状态值域、抽象特征规范 | 不裸建 `是否 / 已 / 可 / 成功 / 明确 / 开 / 关 / 连接` 等布尔叶子结论 | P4/P6/P8 |

## 4.1 P0收束判断

```text
1. P0 只读对账完成，未改业务代码。
2. 进入 P1 前不需要构建或运行鱼巢.exe；本轮没有构建 / 运行锁需求。
3. 第一优先执行 P1a：统一根目标最大值读取。
4. P1a 代码前必须先查 说明书/函数功能说明文档.md 中同名 / 近名函数段，并读取目标代码文件头部规则注释模块。
5. 后续 P1-P8 若改动较大，按 R24 约束先用同名 + 版本号新函数承载替代实现，确认后再替换旧函数。
```

## 5. 禁止宣称项

```text
不得宣称需求树简化生成已闭环。
不得宣称内部治理闭环已完成。
不得宣称自我苏醒完成。
不得宣称初步成熟完成。
不得用构建通过、日志、SQL 投影或控制面板显示替代机器结构证据。
```
