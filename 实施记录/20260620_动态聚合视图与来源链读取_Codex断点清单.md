# 20260620 动态聚合视图与来源链读取 Codex 断点清单

## 0. 恢复摘要

当前切片：

```text
计划/20260620_动态类逻辑纠偏计划_v0.1.md
P8 动态聚合视图与来源链读取函数补齐。
```

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
规范/方法动作动态硬规则规范20260512.md
规范/动作动态即因果账本规范20260512.md
规范/动态信息分层获取与聚合规范20260430.md
计划/计划索引.md
计划/20260620_动态类逻辑纠偏计划_v0.1.md
计划/20260620_二次特征类逻辑纠偏计划_v0.1.md
说明书/函数功能说明文档.md
```

已改文件：

```text
动态类.h
动态类.cpp
二次特征应用模块.ixx
```

当前判断：

```text
1. 动态聚合层级、聚合方式、路径签名和来源低层动态仍由 动态节点主信息类 承载。
2. 本切片只收口读取 / 判断 / 来源链比较入口，不新增动态字段，不新增解释性摘要字段，不删除 来源低层动态。
3. 二次特征应用模块的聚合读取点已改为调用 动态类 功能函数；聚合动态创建仍写入现有权威字段。
```

构建前预测：

```text
1. rg 验收命令不再命中 二次特征应用模块.ixx 中直接读取 动态层级 / 聚合方式 / 动态路径签名 / 来源低层动态 的位置。
2. python .\tools\生成函数功能说明文档.py 会把新增 动态类 功能函数登记进 说明书/函数功能说明文档.md。
3. Debug x64 构建应通过；若失败，优先检查新增函数签名、模块导入和局部变量作用域。
```

下一步：

```text
1. P8 已完成验证，下一步更新计划索引返回 二次特征类逻辑纠偏计划 1.3。
2. 二次特征类 1.3 已随本切片完成调用点回迁，更新计划索引返回 因果类功能函数补完计划 P6。
3. 继续读取 因果类功能函数补完计划 P6 范围。
```

验证结果：

```text
1. rg -n "动态主信息->动态层级|动态主信息->聚合方式|动态主信息->动态路径签名|动态主信息->来源低层动态|\\.动态层级|\\.聚合方式|\\.动态路径签名|\\.来源低层动态" 二次特征应用模块.ixx
   退出码：1；无命中。
2. python .\tools\生成函数功能说明文档.py
   退出码：0；函数条目 6875。
3. msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
   退出码：0；0 个警告，0 个错误。
```

禁止宣称：

```text
不宣称动态类逻辑纠偏完成。
不宣称二次特征类逻辑纠偏完成。
不宣称因果类功能函数补完完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
