# 20260620 世界树类逻辑纠偏 Codex断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_世界树类逻辑纠偏计划_v0.1.md` 的 `1：世界树结构操作入口审计`。

当前依据：

```text
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_世界树类逻辑纠偏计划_v0.1.md
```

## P2.5 第二十一小簇：状态内部创建公开入口收口

切片目标：

```text
1. 补 `世界树类::创建内部状态`，由世界树公开入口承接状态内部创建参数形态。
2. 先迁移 `任务模块.执行.ixx` 中同一职责的当前状态 / 目标状态两处调用。
3. 不批量迁移自我动作、外设、扫描、方法虚拟存在等其它簇。
```

验证前预测：

```text
1. `说明书/函数功能说明文档.md` 应新增 `世界树类::创建内部状态` 条目。
2. `rg -n "世界树\.状态\(\)\.创建内部状态" 任务模块.执行.ixx` 应无命中。
3. Debug x64 构建应通过。
```

已改文件：

```text
世界树类.h
世界树类.cpp
任务模块.执行.ixx
说明书/函数功能说明文档.md
```

验证结果：

```text
1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6965。
2. `说明书/函数功能说明文档.md` 第 306 行出现 `世界树类::创建内部状态`。
3. `rg -n "世界树\.状态\(\)\.创建内部状态" 任务模块.执行.ixx` 无命中。
4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_任务执行状态创建入口收口_build.log` 显示 0 个错误。
```

后续状态：

```text
本小簇已闭合。
P2.5 未整体完成；下一步继续迁移剩余 `世界树.状态().创建内部状态(...)` 外部调用簇。
```

## P2.5 第二十二小簇：方法域状态创建入口收口

切片目标：

```text
1. 迁移 `方法类.cpp` 中方法条件场景初始状态创建入口。
2. 迁移 `方法虚拟存在服务类.cpp` 中方法状态旧 / 新状态节点创建入口。
3. 复用已补齐的 `世界树类::创建内部状态`，不新增函数。
```

验证前预测：

```text
1. `rg -n "世界树\.状态\(\)\.创建内部状态" 方法类.cpp 方法虚拟存在服务类.cpp` 应无命中。
2. Debug x64 构建应通过。
```

已改文件：

```text
方法类.cpp
方法虚拟存在服务类.cpp
```

验证结果：

```text
1. `rg -n "世界树\.状态\(\)\.创建内部状态" 方法类.cpp 方法虚拟存在服务类.cpp` 无命中。
2. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_方法域状态创建入口收口_build.log` 显示 0 个错误。
```

后续状态：

```text
本小簇已闭合。
P2.5 未整体完成；下一步继续迁移自我动作 / 外设 / 扫描剩余 `创建内部状态` 外部调用簇。
```

## P2.5 第二十三小簇：自我动作模块状态创建入口收口

切片目标：

```text
迁移 `自我动作实现模块.ixx` 内剩余 `世界树.状态().创建内部状态(...)` 调用到 `世界树类::创建内部状态`。
```

验证前预测：

```text
1. `rg -n "世界树\.状态\(\)\.创建内部状态" 自我动作实现模块.ixx` 应无命中。
2. Debug x64 构建应通过。
```

已改文件：

```text
自我动作实现模块.ixx
```

验证结果：

```text
1. `rg -n "世界树\.状态\(\)\.创建内部状态" 自我动作实现模块.ixx` 无命中。
2. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_自我动作模块状态创建入口收口_build.log` 显示 0 个错误。
```

后续状态：

```text
本小簇已闭合。
P2.5 未整体完成；下一步继续迁移外设 / 扫描剩余 `创建内部状态` 外部调用簇。
```

## P2.5 第二十四小簇：外设模块状态创建入口收口

切片目标：

```text
迁移 `自我动作实现.外设模块.ixx` 内剩余 `世界树.状态().创建内部状态(...)` 调用到 `世界树类::创建内部状态`。
```

验证前预测：

```text
1. `rg -n "世界树\.状态\(\)\.创建内部状态" 自我动作实现.外设模块.ixx` 应无命中。
2. Debug x64 构建应通过。
```

已改文件：

```text
自我动作实现.外设模块.ixx
```

验证结果：

```text
1. `rg -n "世界树\.状态\(\)\.创建内部状态" 自我动作实现.外设模块.ixx` 无命中。
2. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_外设模块状态创建入口收口_build.log` 显示 0 个错误。
```

后续状态：

```text
本小簇已闭合。
P2.5 未整体完成；下一步迁移扫描模块剩余 `创建内部状态` 外部调用簇。
```

## P2.5 第二十五小簇：扫描模块状态创建入口收口

切片目标：

```text
迁移 `自我动作实现.扫描.ixx` 内剩余 `世界树.状态().创建内部状态(...)` 调用到 `世界树类::创建内部状态`。
```

验证前预测：

```text
1. `rg -n "世界树\.状态\(\)\.创建内部状态" 自我动作实现.扫描.ixx` 应无命中。
2. 全仓 `rg -n "世界树\.状态\(\)\.创建内部状态" -g "*.cpp" -g "*.h" -g "*.ixx"` 应无命中。
3. Debug x64 构建应通过。
```

已改文件：

```text
自我动作实现.扫描.ixx
```

验证结果：

```text
1. `rg -n "世界树\.状态\(\)\.创建内部状态" 自我动作实现.扫描.ixx` 无命中。
2. 全仓 `rg -n "世界树\.状态\(\)\.创建内部状态" -g "*.cpp" -g "*.h" -g "*.ixx"` 无命中。
3. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_扫描模块状态创建入口收口_build.log` 显示 0 个错误。
```

后续状态：

```text
本小簇已闭合。
P2.5 未整体完成；下一候选为外部 `世界树.动态().创建方法动作动态(...)` 公开入口收口。
```

## P2.5 第二十六小簇：方法动作动态创建公开入口收口

切片目标：

```text
1. 补 `世界树类::创建方法动作动态`，由世界树公开入口承接方法动作动态创建。
2. 迁移全仓外部 `世界树.动态().创建方法动作动态(...)` 调用。
```

验证前预测：

```text
1. `说明书/函数功能说明文档.md` 应新增 `世界树类::创建方法动作动态` 条目。
2. 全仓 `rg -n "世界树\.动态\(\)\.创建方法动作动态" -g "*.cpp" -g "*.h" -g "*.ixx"` 应无命中。
3. Debug x64 构建应通过。
```

已改文件：

```text
世界树类.h
世界树类.cpp
任务模块.执行.ixx
自我动作实现模块.ixx
自我动作实现.外设模块.ixx
自我动作实现.扫描.ixx
自我动作实现.服务模块.ixx
说明书/函数功能说明文档.md
```

验证结果：

```text
1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6966。
2. `说明书/函数功能说明文档.md` 第 317 行出现 `世界树类::创建方法动作动态`。
3. 全仓 `rg -n "世界树\.动态\(\)\.创建方法动作动态" -g "*.cpp" -g "*.h" -g "*.ixx"` 无命中。
4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_方法动作动态入口收口_build.log` 显示 0 个错误。
```

后续状态：

```text
本小簇已闭合。
P2.5 未整体完成；下一步继续审查动态类剩余公开入口。
```

## P2.5 第二十七小簇：动态来源低层追加入口收口

切片目标：

```text
1. 补 `世界树类::追加来源低层动态`，由世界树公开入口承接动态来源链写入。
2. 迁移外部 6 处 `世界树.动态().追加来源低层动态(...)` 调用。
```

验证前预测：

```text
1. `说明书/函数功能说明文档.md` 应新增 `世界树类::追加来源低层动态` 条目。
2. 全仓 `rg -n "世界树\.动态\(\)\.追加来源低层动态" -g "*.cpp" -g "*.h" -g "*.ixx"` 应无命中。
3. Debug x64 构建应通过。
```

已改文件：

```text
世界树类.h
世界树类.cpp
自我动作实现模块.ixx
自我动作实现.扫描.ixx
自我动作实现.服务模块.ixx
说明书/函数功能说明文档.md
```

验证结果：

```text
1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6968。
2. `说明书/函数功能说明文档.md` 第 417 行出现 `世界树类::追加来源低层动态`。
3. 全仓 `rg -n "世界树\.动态\(\)\.追加来源低层动态" -g "*.cpp" -g "*.h" -g "*.ixx"` 无命中。
4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_动态来源追加入口收口_build.log` 显示 0 个错误。
```

后续状态：

```text
本小簇已闭合。
P2.5 未整体完成；下一步继续审查动态主信息读取 / 动态元数据写入口。
```

## P2.5 第二十八小簇：动态主信息读取入口收口

切片目标：

```text
1. 补 `世界树类::取动态主信息` const / 非 const 只读入口。
2. 迁移外部 8 处 `世界树.动态().取动态主信息(...)` 调用。
```

验证前预测：

```text
1. `说明书/函数功能说明文档.md` 应新增 `世界树类::取动态主信息` 条目。
2. 全仓 `rg -n "世界树\.动态\(\)\.取动态主信息" -g "*.cpp" -g "*.h" -g "*.ixx"` 应无命中。
3. Debug x64 构建应通过。
```

已改文件：

```text
世界树类.h
世界树类.cpp
任务模块.执行.ixx
方法虚拟存在服务类.cpp
自我动作实现.服务模块.ixx
自我类.cpp
自我线程模块.impl.cpp
说明书/函数功能说明文档.md
```

验证结果：

```text
1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6970。
2. `说明书/函数功能说明文档.md` 第 333 / 334 行出现 `世界树类::取动态主信息`。
3. 全仓 `rg -n "世界树\.动态\(\)\.取动态主信息" -g "*.cpp" -g "*.h" -g "*.ixx"` 无命中。
4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_动态主信息读取入口收口_build.log` 显示 0 个错误。
```

后续状态：

```text
本小簇已闭合。
P2.5 未整体完成；下一步继续审查动态元数据写入口。
```

## P2.5 第二十九小簇：动态元数据写入口收口

切片目标：

```text
1. 补 `世界树类::设置主体特征`、`世界树类::绑定来源方法`、`世界树类::绑定来源场景`、`世界树类::状态记录后刷新动态`。
2. 迁移剩余外部 `世界树.动态().*` 直连调用。
```

验证前预测：

```text
1. `说明书/函数功能说明文档.md` 应新增上述 4 个 `世界树类` 条目。
2. 全仓 `rg -n "世界树\.动态\(\)\." -g "*.cpp" -g "*.h" -g "*.ixx"` 应无命中。
3. Debug x64 构建应通过。
```

已改文件：

```text
世界树类.h
世界树类.cpp
方法虚拟存在服务类.cpp
状态类.cpp
自我类.cpp
自我线程模块.impl.cpp
说明书/函数功能说明文档.md
```

验证结果：

```text
1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6976。
2. `说明书/函数功能说明文档.md` 第 382 / 398 / 399 / 412 行出现新增 `世界树类` 动态元数据函数。
3. 全仓 `rg -n "世界树\.动态\(\)\." -g "*.cpp" -g "*.h" -g "*.ixx"` 无命中。
4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_动态元数据写入口收口_build.log` 显示 0 个错误。
```

后续状态：

```text
本小簇已闭合。
动态类公开入口直连已清空；P2.5 未整体完成，下一步继续审查状态 / 特征 / 存在 / 场景服务直连。
```

## P2.5 第三十小簇：状态主信息读取入口收口

切片目标：

```text
1. 补 `世界树类::取状态主信息` const / 非 const 只读入口。
2. 机械迁移外部 `世界树.状态().取状态主信息(...)` 调用。
```

验证前预测：

```text
1. `说明书/函数功能说明文档.md` 应新增 `世界树类::取状态主信息` 条目。
2. 全仓 `rg -n "世界树\.状态\(\)\.取状态主信息" -g "*.cpp" -g "*.h" -g "*.ixx"` 应无命中。
3. Debug x64 构建应通过。
```

已改文件：

```text
世界树类.h
世界树类.cpp
任务模块.筹办.ixx
任务模块.管理界面线程.impl.cpp
控制面板类.cpp
方法类.cpp
自我动作实现.服务模块.ixx
自我动作实现模块.ixx
自我类.cpp
自我线程模块.impl.cpp
需求类.cpp
鱼巢.cpp
说明书/函数功能说明文档.md
```

验证结果：

```text
1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6978。
2. `说明书/函数功能说明文档.md` 第 349 / 350 行出现 `世界树类::取状态主信息`。
3. 全仓 `rg -n "世界树\.状态\(\)\.取状态主信息" -g "*.cpp" -g "*.h" -g "*.ixx"` 无命中。
4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_状态主信息读取入口收口_build.log` 显示 0 个错误。
```

后续状态：

```text
本小簇已闭合。
P2.5 未整体完成；下一步继续审查特征 / 存在 / 场景服务直连。
```

## P2.5 第三十一小簇：特征主信息读取入口收口

切片目标：

```text
1. 补 `世界树类::取特征主信息` const / 非 const 只读入口。
2. 机械迁移外部 `世界树.特征().取特征主信息(...)` 调用。
```

验证前预测：

```text
1. `说明书/函数功能说明文档.md` 应新增 `世界树类::取特征主信息` 条目。
2. 全仓 `rg -n "世界树\.特征\(\)\.取特征主信息" -g "*.cpp" -g "*.h" -g "*.ixx"` 应无命中。
3. Debug x64 构建应通过。
```

验证结果：

```text
已改文件：
    世界树类.h
    世界树类.cpp
    二次特征应用模块.ixx
    任务模块.筹办.ixx
    任务类.cpp
    控制面板类.cpp
    方法类.cpp
    自我动作实现.外设模块.ixx
    自我动作实现.服务模块.ixx
    自我动作实现模块.ixx
    自我类.cpp
    自我线程模块.impl.cpp
    需求类.cpp
    鱼巢.cpp
    说明书/函数功能说明文档.md
    实施记录/20260620_世界树类逻辑纠偏_Codex断点清单.md

证据：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6980。
    2. `说明书/函数功能说明文档.md` 第 349 / 350 行出现 `世界树类::取特征主信息`。
    3. `rg -n "世界树(\.|->|_->)特征\(\)\.取特征主信息|世界树_?->特征\(\)\.取特征主信息|世界树\.特征\(\)\.取特征主信息" -g "*.cpp" -g "*.h" -g "*.ixx"` 退出码 1，无命中。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_特征主信息读取入口收口_build.log` 显示“已成功生成”和 0 个错误。
```

后续状态：

```text
本小簇已闭合。
P2.5 未整体完成；下一步继续审查特征 / 存在 / 场景服务直连，优先选择剩余高频公开入口直连的最小闭合簇。
```

## P2.5 第三十二小簇：存在三类根节点读取入口收口

切片目标：

```text
1. 补 `世界树类::获取需求根节点`、`世界树类::获取任务根节点`、`世界树类::获取方法根节点` const / 非 const 只读入口。
2. 机械迁移外部 `世界树.存在().获取需求根节点(...)`、`世界树.存在().获取任务根节点(...)`、`世界树.存在().获取方法根节点(...)` 调用。
3. 不混入 `确保存在三类根节点` 或其它存在域服务入口；它们另行切片。
```

验证前预测：

```text
1. `说明书/函数功能说明文档.md` 应新增 6 条 `世界树类::获取需求根节点` / `获取任务根节点` / `获取方法根节点` 定义条目。
2. 全仓 `rg -n "世界树(\.|->|_->)存在\(\)\.获取(需求|任务|方法)根节点" -g "*.cpp" -g "*.h" -g "*.ixx"` 应无命中。
3. Debug x64 构建应通过。
```

验证结果：

```text
已改文件：
    世界树类.h
    世界树类.cpp
    任务模块.筹办.ixx
    任务模块.管理界面线程.impl.cpp
    控制面板摘要线程模块.impl.cpp
    控制面板类.cpp
    方法类.cpp
    本能方法类.cpp
    自我动作实现模块.ixx
    自我类.cpp
    自我类.初始化.cpp
    自我线程模块.impl.cpp
    自我线程模块.消息处理器.ixx
    自检线程模块.impl.cpp
    语言类_存在表达.cpp
    需求类.cpp
    鱼巢.cpp
    说明书/函数功能说明文档.md

证据：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6986。
    2. `说明书/函数功能说明文档.md` 第 405 / 406 / 414 / 415 / 417 / 418 行出现 `世界树类::获取任务根节点`、`世界树类::获取方法根节点`、`世界树类::获取需求根节点`。
    3. `rg -n "世界树(\.|->|_->)存在\(\)\.获取(需求|任务|方法)根节点" -g "*.cpp" -g "*.h" -g "*.ixx"` 退出码 1，无命中。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_存在三类根节点读取入口收口_build.log` 第 205 行显示“已成功生成”，第 207 行显示 0 个错误。
```

后续状态：

```text
本小簇已闭合。
P2.5 未整体完成；下一步继续审查特征 / 存在 / 场景服务直连，优先选择剩余高频公开入口直连的最小闭合簇。
```

## P2.5 第三十三小簇：抽象特征主信息读取入口收口

切片目标：

```text
1. 补 `世界树类::取抽象特征主信息` const / 非 const 只读入口。
2. 机械迁移外部 `世界树.特征().取抽象特征主信息(...)` 调用。
3. 不混入 `查找子抽象特征_按类型`、`比较状态`、抽象特征创建或结果能力相关入口；它们另行切片。
```

验证前预测：

```text
1. `说明书/函数功能说明文档.md` 应新增 2 条 `世界树类::取抽象特征主信息` 定义条目。
2. 全仓 `rg -n "世界树(\.|->|_->)特征\(\)\.取抽象特征主信息" -g "*.cpp" -g "*.h" -g "*.ixx"` 应无命中。
3. Debug x64 构建应通过。
```

验证结果：

```text
已改文件：
    世界树类.h
    世界树类.cpp
    任务模块.筹办.ixx
    自我动作实现模块.ixx
    自我类.cpp
    自我线程模块.impl.cpp
    自检线程模块.impl.cpp
    需求类.cpp
    说明书/函数功能说明文档.md
    实施记录/20260620_世界树类逻辑纠偏_Codex断点清单.md

证据：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6988。
    2. `说明书/函数功能说明文档.md` 第 349 / 350 行出现 `世界树类::取抽象特征主信息`。
    3. `rg -n "世界树(\.|->|_->)特征\(\)\.取抽象特征主信息" -g "*.cpp" -g "*.h" -g "*.ixx"` 退出码 1，无命中。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_抽象特征主信息读取入口收口_build.log` 第 205 行显示“已成功生成”，第 207 行显示 0 个错误。
```

后续状态：

```text
本小簇已闭合。
P2.5 未整体完成；下一步继续审查特征 / 存在 / 场景服务直连，优先选择剩余高频公开入口直连的最小闭合簇。
```

## P2.5 第三十四小簇：特征状态比较入口收口

切片目标：

```text
1. 补 `世界树类::比较状态` 只读入口。
2. 机械迁移外部 `世界树.特征().比较状态(...)` 调用。
3. 不混入 `获取比较方法结果`、特征值读写或其它比较函数；它们另行切片。
```

验证前预测：

```text
1. `说明书/函数功能说明文档.md` 应新增 `世界树类::比较状态` 定义条目。
2. 全仓 `rg -n "世界树(\.|->|_->)特征\(\)\.比较状态" -g "*.cpp" -g "*.h" -g "*.ixx"` 应无命中。
3. Debug x64 构建应通过。
```

验证结果：

```text
已改文件：
    世界树类.h
    世界树类.cpp
    任务模块.筹办.ixx
    任务模块.管理工作线程.ixx
    任务模块.管理界面线程.impl.cpp
    控制面板类.cpp
    方法类.cpp
    自我动作实现模块.ixx
    自我线程模块.impl.cpp
    说明书/函数功能说明文档.md
    实施记录/20260620_世界树类逻辑纠偏_Codex断点清单.md

证据：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6990。
    2. `说明书/函数功能说明文档.md` 第 381 行出现 `世界树类::比较状态`。
    3. `rg -n "世界树(\.|->|_->)特征\(\)\.比较状态" -g "*.cpp" -g "*.h" -g "*.ixx"` 退出码 1，无命中。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_特征状态比较入口收口_build.log` 第 205 行显示“已成功生成”，第 207 行显示 0 个错误。
```

后续状态：

```text
本小簇已闭合。
P2.5 未整体完成；下一步继续审查特征 / 存在 / 场景服务直连，优先选择剩余高频公开入口直连的最小闭合簇。
```

## P2.5 第三十五小簇：存在主信息读取入口收口

切片目标：

```text
1. 补 `世界树类::取存在主信息` const / 非 const 只读入口。
2. 机械迁移外部 `世界树.存在().取存在主信息(...)` 调用。
3. 不混入存在类型 / 名称读取、三类根确保或其它存在域入口；它们另行切片。
```

验证前预测：

```text
1. `说明书/函数功能说明文档.md` 应新增 2 条 `世界树类::取存在主信息` 定义条目。
2. 全仓 `rg -n "世界树(\.|->|_->)存在\(\)\.取存在主信息" -g "*.cpp" -g "*.h" -g "*.ixx"` 应无命中。
3. Debug x64 构建应通过。
```

验证结果：

```text
已改文件：
    世界树类.h
    世界树类.cpp
    任务类.cpp
    控制面板类.cpp
    自我动作实现.外设模块.ixx
    说明书/函数功能说明文档.md
    实施记录/20260620_世界树类逻辑纠偏_Codex断点清单.md

证据：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6992。
    2. `说明书/函数功能说明文档.md` 第 335 / 336 行出现 `世界树类::取存在主信息`。
    3. `rg -n "世界树(\.|->|_->)存在\(\)\.取存在主信息" -g "*.cpp" -g "*.h" -g "*.ixx"` 退出码 1，无命中。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_存在主信息读取入口收口_build.log` 第 205 行显示“已成功生成”，第 207 行显示 0 个错误。
```

后续状态：

```text
本小簇已闭合。
P2.5 未整体完成；下一步继续审查特征 / 存在 / 场景服务直连，优先选择剩余高频公开入口直连的最小闭合簇。
```

## P2.5 第三十六小簇：状态主信息读取指针形态补收口

切片目标：

```text
1. 复用已存在的 `世界树类::取状态主信息`。
2. 机械迁移剩余 `世界树->状态().取状态主信息(...)`、`世界树_->状态().取状态主信息(...)` 调用。
3. 不混入状态写入、标记变化或其它状态域入口；它们另行切片。
```

验证前预测：

```text
1. 全仓 `rg -n "世界树(\.|->|_->)状态\(\)\.取状态主信息" -g "*.cpp" -g "*.h" -g "*.ixx"` 应无命中。
2. 不新增函数，不需要重新生成函数说明文档。
3. Debug x64 构建应通过。
```

验证结果：

```text
已改文件：
    二次特征应用模块.ixx
    实施记录/20260620_世界树类逻辑纠偏_Codex断点清单.md

证据：
    1. `rg -n "世界树(\.|->|_->)状态\(\)\.取状态主信息" -g "*.cpp" -g "*.h" -g "*.ixx"` 退出码 1，无命中。
    2. 未新增函数，未重新生成函数说明文档。
    3. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_状态主信息读取指针形态补收口_build.log` 第 125 行显示“已成功生成”，第 127 行显示 0 个错误。
```

后续状态：

```text
本小簇已闭合。
P2.5 未整体完成；下一步继续审查特征 / 存在 / 场景服务直连，优先选择剩余高频公开入口直连的最小闭合簇。
```

## P2.5 第三十七小簇：抽象特征按类型查找入口收口

切片目标：

```text
1. 补 `世界树类::查找子抽象特征_按类型` 只读入口。
2. 机械迁移外部 `世界树.特征().查找子抽象特征_按类型(...)` 调用。
3. 不混入抽象特征创建、取或创建、实例特征解析或世界树类内部 helper；它们另行切片。
```

验证前预测：

```text
1. `说明书/函数功能说明文档.md` 应新增 `世界树类::查找子抽象特征_按类型` 定义条目。
2. 全仓 `rg -n "世界树(\.|->|_->)特征\(\)\.查找子抽象特征_按类型" -g "*.cpp" -g "*.h" -g "*.ixx"` 应无命中。
3. Debug x64 构建应通过。
```

验证结果：

```text
已改文件：
    世界树类.h
    世界树类.cpp
    任务模块.筹办.ixx
    自我类.cpp
    自我线程模块.impl.cpp
    自检线程模块.impl.cpp
    需求类.cpp
    说明书/函数功能说明文档.md
    实施记录/20260620_世界树类逻辑纠偏_Codex断点清单.md

证据：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6994。
    2. `说明书/函数功能说明文档.md` 第 378 行出现 `世界树类::查找子抽象特征_按类型`。
    3. `rg -n "世界树(\.|->|_->)特征\(\)\.查找子抽象特征_按类型" -g "*.cpp" -g "*.h" -g "*.ixx"` 退出码 1，无命中。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_抽象特征按类型查找入口收口_build.log` 第 205 行显示“已成功生成”，第 207 行显示 0 个错误。
```

后续状态：

```text
本小簇已闭合。
P2.5 未整体完成；下一步继续审查特征 / 存在 / 场景服务直连，优先选择剩余高频公开入口直连的最小闭合簇。
```

## P2.5 第三十八小簇：特征比较方法结果入口收口

切片目标：

```text
1. 补 `世界树类::获取比较方法结果` 只读入口。
2. 机械迁移外部 `世界树.特征().获取比较方法结果(...)` / 指针形态调用。
3. 不混入状态比较、特征值读写或其它比较函数；它们另行切片。
```

验证前预测：

```text
1. `说明书/函数功能说明文档.md` 应新增 `世界树类::获取比较方法结果` 定义条目。
2. 全仓 `rg -n "世界树(\.|->|_->)特征\(\)\.获取比较方法结果" -g "*.cpp" -g "*.h" -g "*.ixx"` 应无命中。
3. Debug x64 构建应通过。
```

验证结果：

```text
已改文件：
    世界树类.h
    世界树类.cpp
    二次特征应用模块.ixx
    自我线程模块.impl.cpp
    需求类.cpp
    说明书/函数功能说明文档.md
    实施记录/20260620_世界树类逻辑纠偏_Codex断点清单.md

证据：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6995。
    2. `说明书/函数功能说明文档.md` 第 422 行出现 `世界树类::获取比较方法结果`。
    3. `rg -n "世界树(\.|->|_->)特征\(\)\.获取比较方法结果" -g "*.cpp" -g "*.h" -g "*.ixx"` 退出码 1，无命中。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_特征比较方法结果入口收口_build.log` 第 205 行显示“已成功生成”，第 207 行显示 0 个错误。
```

后续状态：

```text
本小簇已闭合。
P2.5 未整体完成；下一步继续审查特征 / 存在 / 场景服务直连，优先选择剩余高频公开入口直连的最小闭合簇。
```

## P2.5 第三十九小簇：状态变化显示缓存入口收口

切片目标：

```text
1. 补 `世界树类::标记状态变化`，仅转调状态节点显示缓存写入。
2. 机械迁移外部 `世界树.状态().标记变化(...)` 调用。
3. 不把该入口当作权威动作动态、需求满足或结算事实来源；不混入状态值写入和主体特征绑定。
```

验证前预测：

```text
1. `说明书/函数功能说明文档.md` 应新增 `世界树类::标记状态变化` 定义条目。
2. 全仓 `rg -n "世界树(\.|->|_->)状态\(\)\.标记变化" -g "*.cpp" -g "*.h" -g "*.ixx"` 应无命中。
3. Debug x64 构建应通过。
```

验证结果：

```text
已改文件：
    世界树类.h
    世界树类.cpp
    自我类.cpp
    自我线程模块.impl.cpp
    说明书/函数功能说明文档.md
    实施记录/20260620_世界树类逻辑纠偏_Codex断点清单.md

证据：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6997。
    2. `说明书/函数功能说明文档.md` 第 381 行出现 `世界树类::标记状态变化`。
    3. `rg -n "世界树(\.|->|_->)状态\(\)\.标记变化" -g "*.cpp" -g "*.h" -g "*.ixx"` 退出码 1，无命中。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_状态变化显示缓存入口收口_build.log` 第 205 行显示“已成功生成”，第 207 行显示 0 个错误。
```

后续状态：

```text
本小簇已闭合。
P2.5 未整体完成；下一步继续审查特征 / 存在 / 场景服务直连，优先选择剩余高频公开入口直连的最小闭合簇。
```

## P2.5 第四十小簇：存在 / 场景节点判定入口收口

切片目标：

```text
1. 补 `世界树类::是存在节点`、`世界树类::是场景节点` 只读判定入口。
2. 机械迁移外部 `世界树.存在().是存在节点(...)`、`世界树.场景().是场景节点(...)` 及指针形态调用。
3. 不混入存在 / 场景读取、创建、绑定或索引快照入口；它们另行切片。
```

验证前预测：

```text
1. `说明书/函数功能说明文档.md` 应新增 `世界树类::是存在节点`、`世界树类::是场景节点` 定义条目。
2. 全仓 `rg -n "世界树(\.|->|_->)(存在\(\)\.是存在节点|场景\(\)\.是场景节点)" -g "*.cpp" -g "*.h" -g "*.ixx"` 应无命中。
3. Debug x64 构建应通过。
```

验证结果：

```text
已改文件：
    世界树类.h
    世界树类.cpp
    二次特征应用模块.ixx
    任务模块.管理工作线程.ixx
    自我动作实现.外设模块.ixx
    自我动作实现模块.ixx
    自我线程模块.impl.cpp
    说明书/函数功能说明文档.md
    实施记录/20260620_世界树类逻辑纠偏_Codex断点清单.md

证据：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6999。
    2. `说明书/函数功能说明文档.md` 第 373 / 375 行出现 `世界树类::是场景节点`、`世界树类::是存在节点`。
    3. `rg -n "世界树(\.|->|_->)(存在\(\)\.是存在节点|场景\(\)\.是场景节点)" -g "*.cpp" -g "*.h" -g "*.ixx"` 退出码 1，无命中。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_存在场景节点判定入口收口_build.log` 第 205 行显示“已成功生成”，第 207 行显示 0 个错误。
```

后续状态：

```text
本小簇已闭合。
P2.5 未整体完成；下一步继续审查特征 / 存在 / 场景服务直连，优先选择剩余高频公开入口直连的最小闭合簇。
```

## P2.5 第四十一小簇：通用特征值读写入口收口

切片目标：

```text
1. 补 `世界树类::写入特征值` 通用写入入口。
2. 复用既有 `世界树类::读取特征快照` 承接 `世界树.特征().读取特征值(...)`。
3. 机械迁移外部 `世界树.特征().写入特征值(...)` / `读取特征值(...)` 调用。
4. 不混入 typed 写入、VecU / VecI64 专用读取或特征值池操作；它们另行切片。
```

验证前预测：

```text
1. `说明书/函数功能说明文档.md` 应新增 `世界树类::写入特征值` 定义条目。
2. 全仓 `rg -n "世界树(\.|->|_->)特征\(\)\.(写入特征值|读取特征值)" -g "*.cpp" -g "*.h" -g "*.ixx"` 应无命中。
3. Debug x64 构建应通过。
```

验证结果：

```text
已改文件：
    世界树类.h
    世界树类.cpp
    自我动作实现.外设模块.ixx
    自我动作实现.服务模块.ixx
    自我动作实现模块.ixx
    说明书/函数功能说明文档.md
    实施记录/20260620_世界树类逻辑纠偏_Codex断点清单.md

证据：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 7000。
    2. `说明书/函数功能说明文档.md` 第 305 行出现 `世界树类::写入特征值`；第 442 / 443 行保留既有 `世界树类::读取特征快照`。
    3. `rg -n "世界树(\.|->|_->)特征\(\)\.(写入特征值|读取特征值)" -g "*.cpp" -g "*.h" -g "*.ixx"` 退出码 1，无命中。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_通用特征值读写入口收口_build.log` 第 205 行显示“已成功生成”，第 207 行显示 0 个错误。
```

后续状态：

```text
本小簇已闭合。
P2.5 未整体完成；下一步继续审查特征 / 存在 / 场景服务直连，优先选择剩余高频公开入口直连的最小闭合簇。
```

## P2.5 第四十二小簇：存在域尾项公开入口收口

切片目标：

```text
1. 补 `世界树类::读取存在名称`、`世界树类::读取存在类型`、`世界树类::获取概念集`。
2. 补 `世界树类::读取最近观测位置缓存`、`世界树类::确保存在三类根节点`。
3. 机械迁移剩余外部 `世界树.存在().*` 低频直连。
4. 不混入存在创建、内部世界读取、概念模板引用或空间坐标换算；它们另行切片。
```

验证前预测：

```text
1. `说明书/函数功能说明文档.md` 应新增上述 5 个 `世界树类` 定义条目。
2. 全仓 `rg -n "世界树(\.|->|_->)存在\(\)\.(读取最近观测位置缓存|确保存在三类根节点|读取存在名称|读取存在类型|获取概念集)" -g "*.cpp" -g "*.h" -g "*.ixx"` 应无命中。
3. Debug x64 构建应通过。
```

验证结果：

```text
已改文件：
    世界树类.h
    世界树类.cpp
    二次特征应用模块.ixx
    任务模块.管理界面线程.impl.cpp
    自我类.cpp
    自我线程模块.impl.cpp
    说明书/函数功能说明文档.md
    实施记录/20260620_世界树类逻辑纠偏_Codex断点清单.md

证据：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 7006。
    2. `说明书/函数功能说明文档.md` 第 398 / 427 / 436 / 438 / 439 行出现 `世界树类` 存在域尾项入口。
    3. `rg -n "世界树(\.|->|_->)存在\(\)\.(读取最近观测位置缓存|确保存在三类根节点|读取存在名称|读取存在类型|获取概念集)" -g "*.cpp" -g "*.h" -g "*.ixx"` 退出码 1，无命中。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_存在域尾项入口收口_build.log` 第 205 行显示“已成功生成”，第 207 行显示 0 个错误。
```

后续状态：

```text
本小簇已闭合。
P2.5 未整体完成；下一步继续审查场景 / 状态 / 特征服务直连尾项。
```

## P2.5 第四十三小簇：场景域尾项公开入口收口

切片目标：

```text
1. 补 `世界树类::绑定场景宿主`、`世界树类::读取场景宿主存在`。
2. 补 `世界树类::读取场景状态索引快照`、`世界树类::读取场景二次特征索引快照`。
3. 补 `世界树类::查找存在_按深度或彩图轮廓图`。
4. 机械迁移剩余外部 `世界树.场景().*` 低频直连。
```

验证前预测：

```text
1. `说明书/函数功能说明文档.md` 应新增上述 5 个 `世界树类` 定义条目。
2. 全仓 `rg -n "世界树(\.|->|_->)场景\(\)\.(绑定宿主|读取场景宿主存在|读取场景二次特征索引快照|读取场景状态索引快照|查找存在_按深度或彩图轮廓图)" -g "*.cpp" -g "*.h" -g "*.ixx"` 应无命中。
3. Debug x64 构建应通过。
```

已改文件：

```text
世界树类.h
世界树类.cpp
任务类.cpp
方法类.cpp
自我动作实现.外设模块.ixx
自我线程模块.impl.cpp
说明书/函数功能说明文档.md
```

验证结果：

```text
1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 7015。
2. `说明书/函数功能说明文档.md` 第 383 / 414 / 438 / 439 / 440 行出现上述 5 个 `世界树类` 定义条目。
3. 全仓 `rg -n "世界树(\.|->|_->)场景\(\)\.(绑定宿主|读取场景宿主存在|读取场景二次特征索引快照|读取场景状态索引快照|查找存在_按深度或彩图轮廓图)" -g "*.cpp" -g "*.h" -g "*.ixx"` 无命中。
4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_场景域尾项入口收口_build.log` 显示已成功生成、0 个错误。
```

后续状态：

```text
本小簇已闭合。
P2.5 未整体完成；下一步继续审查状态 / 特征服务直连尾项。
```

## P2.5 第四十四小簇：状态域尾项公开入口收口

切片目标：

```text
1. 补 `世界树类::是否当前活动状态`、`世界树类::读取最近清理问题快照`、`世界树类::清除最近清理问题快照`。
2. 补 `世界树类::绑定状态主体特征`、`世界树类::写入状态值`。
3. 机械迁移剩余外部 `世界树.状态().*` 低频直连。
```

验证前预测：

```text
1. `说明书/函数功能说明文档.md` 应新增上述 5 个 `世界树类` 定义条目。
2. 全仓 `rg -n "世界树(\.|->|_->)状态\(\)\.(是否当前活动状态|读取最近清理问题快照|清除最近清理问题快照|绑定主体特征|写入状态值)" -g "*.cpp" -g "*.h" -g "*.ixx"` 应无命中。
3. Debug x64 构建应通过。
```

已改文件：

```text
世界树类.h
世界树类.cpp
任务模块.管理工作线程.ixx
自我线程模块.impl.cpp
自检线程模块.impl.cpp
说明书/函数功能说明文档.md
```

验证结果：

```text
1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 7024。
2. `说明书/函数功能说明文档.md` 第 306 / 375 / 395 / 421 / 448 行出现上述 5 个 `世界树类` 定义条目。
3. 全仓 `rg -n "世界树(\.|->|_->)状态\(\)\.(是否当前活动状态|读取最近清理问题快照|清除最近清理问题快照|绑定主体特征|写入状态值)" -g "*.cpp" -g "*.h" -g "*.ixx"` 无命中。
4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_状态域尾项入口收口_build.log` 显示已成功生成、0 个错误。
```

后续状态：

```text
本小簇已闭合。
P2.5 未整体完成；下一步继续审查特征服务直连尾项。
```

## P2.5 第四十五小簇：特征域尾项公开入口收口

切片目标：

```text
1. 补 `世界树类::初始化常用抽象特征`。
2. 复用既有 `世界树类::取或创建实例特征_按抽象特征`、`世界树类::解析实例特征命中抽象特征`。
3. 机械迁移剩余外部 `世界树.特征().*` 低频直连。
```

验证前预测：

```text
1. `说明书/函数功能说明文档.md` 应新增 `世界树类::初始化常用抽象特征` 定义条目，并保留上述 2 个既有入口。
2. 全仓 `rg -n "世界树(\.|->|_->)特征\(\)\.(初始化常用抽象特征|取或创建实例特征_按抽象特征|解析实例特征命中抽象特征)" -g "*.cpp" -g "*.h" -g "*.ixx"` 应无命中。
3. 全仓 `rg -n "世界树(?:\.|->|_->)(状态|特征|存在|场景)\(\)\.[\p{Han}A-Za-z0-9_]+" -g "*.cpp" -g "*.h" -g "*.ixx"` 应无命中。
4. Debug x64 构建应通过。
```

已改文件：

```text
世界树类.h
世界树类.cpp
自我类.cpp
自我动作实现模块.ixx
说明书/函数功能说明文档.md
```

验证结果：

```text
1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 7026。
2. `说明书/函数功能说明文档.md` 第 326 行出现 `世界树类::初始化常用抽象特征`，第 348 / 439 行保留既有 `世界树类::取或创建实例特征_按抽象特征`、`世界树类::解析实例特征命中抽象特征`。
3. 全仓 `rg -n "世界树(\.|->|_->)特征\(\)\.(初始化常用抽象特征|取或创建实例特征_按抽象特征|解析实例特征命中抽象特征)" -g "*.cpp" -g "*.h" -g "*.ixx"` 无命中。
4. 全仓 `rg -n "世界树(?:\.|->|_->)(状态|特征|存在|场景)\(\)\.[\p{Han}A-Za-z0-9_]+" -g "*.cpp" -g "*.h" -g "*.ixx"` 无命中。
5. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P2_5_特征域尾项入口收口_build.log` 显示已成功生成、0 个错误。
```

后续状态：

```text
本小簇已闭合。
P2.5 当前限定范围内 `状态 / 特征 / 存在 / 场景` 访问器外部直连已清零。
世界树类逻辑纠偏计划未整体完成；下一步转入 P3 基础信息访问器收口。
```

## P3 第一小簇：基础信息主信息读取入口收口

切片目标：

```text
1. 复用既有 `世界树类::取存在主信息`、`世界树类::取动态主信息`。
2. 补 `世界树类::取因果主信息`，承接因果主信息只读读取。
3. 机械迁移外部 `世界树.基础信息().取主信息<...>(...)` 直接调用。
```

验证前预测：

```text
1. `说明书/函数功能说明文档.md` 应新增 `世界树类::取因果主信息` 定义条目，并保留存在 / 动态主信息入口。
2. 全仓 `rg -n "世界树(\.|->|_->)基础信息\(\)\.取主信息" -g "*.cpp" -g "*.h" -g "*.ixx"` 应无命中。
3. Debug x64 构建应通过。
```

已改文件：

```text
世界树类.h
世界树类.cpp
控制面板类.cpp
自我线程模块.impl.cpp
说明书/函数功能说明文档.md
```

验证结果：

```text
1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 7028。
2. `说明书/函数功能说明文档.md` 第 338 / 339 行出现 `世界树类::取因果主信息`，第 336 / 337 行保留 `世界树类::取动态主信息`，第 340 / 341 行保留 `世界树类::取存在主信息`。
3. 全仓 `rg -n "世界树(\.|->|_->)基础信息\(\)\.取主信息" -g "*.cpp" -g "*.h" -g "*.ixx"` 无命中。
4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P3_基础信息主信息读取入口收口_build.log` 显示已成功生成、0 个错误。
```

后续状态：

```text
本小簇已闭合。
P3 未整体完成；下一步继续审查 `世界树.基础信息().枚举全部节点` 使用点。
```

## P3 第二小簇：基础信息全量节点访问器收口

切片目标：

```text
1. 补 `世界树类::统计全部节点`，承接控制面板只读总数需求。
2. 补 `世界树类::基础节点属于当前世界树`，承接指针归属判断需求。
3. 机械迁移外部 `世界树.基础信息().枚举全部节点()` 直接调用，不新增宽泛枚举公开入口。
```

验证前预测：

```text
1. `说明书/函数功能说明文档.md` 应新增上述 2 个 `世界树类` 定义条目。
2. 全仓 `rg -n "世界树(\.|->|_->)基础信息\(\)\.枚举全部节点" -g "*.cpp" -g "*.h" -g "*.ixx"` 应无命中。
3. 全仓 `rg -n "世界树(?:\.|->|_->)基础信息\(\)\.[\p{Han}A-Za-z0-9_]+" -g "*.cpp" -g "*.h" -g "*.ixx"` 剩余命中只允许是临时判定为后续 P3 小簇的 `世界树.基础信息()` 引用传递。
4. Debug x64 构建应通过。
```

已改文件：

```text
世界树类.h
世界树类.cpp
控制面板类.cpp
本能动作管理类.ixx
自我动作实现模块.ixx
自我动作实现.外设模块.ixx
说明书/函数功能说明文档.md
```

验证结果：

```text
1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 7032。
2. `说明书/函数功能说明文档.md` 第 368 行出现 `世界树类::基础节点属于当前世界树`，第 426 行出现 `世界树类::统计全部节点`。
3. 全仓 `rg -n "世界树(\.|->|_->)基础信息\(\)\.枚举全部节点" -g "*.cpp" -g "*.h" -g "*.ixx"` 无命中。
4. 全仓 `rg -n "世界树(?:\.|->|_->)基础信息\(\)\.[\p{Han}A-Za-z0-9_]+" -g "*.cpp" -g "*.h" -g "*.ixx"` 无命中；剩余 13 处 `世界树.基础信息()` 均为引用传递点。
5. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260621_P3_基础信息全量节点访问器收口_build.log` 显示已成功生成、0 个错误。
```

后续状态：

```text
本小簇已闭合。
P3 未整体完成；下一步继续分类剩余 `世界树.基础信息()` 引用传递点。
```

## 1. 当前问题锚点

```text
用户口径：
    世界树类负责维护世界树，所有世界树的操作都应该调用这个类中的函数。

计划锚点：
    当前切片先审计外部绕过 世界树类 的基础信息世界树结构操作入口。
```

## 2. 已运行命令

```powershell
rg -n "基础信息\(\)\.世界根|基础信息\(\)\.查找主键|基础信息\(\)\.添加子节点|基础信息\(\)\.删除节点|基础信息\(\)\.枚举节点_按类型|基础信息\(\)\.获取读锁|基础信息\(\)\.获取写锁" -g "*.h" -g "*.cpp" -g "*.ixx"
rg -n "基础信息_->世界根|基础信息_->查找主键|基础信息_->添加子节点|基础信息_->删除节点|基础信息_->枚举节点_按类型|基础信息_->获取读锁|基础信息_->获取写锁" -g "*.h" -g "*.cpp" -g "*.ixx"
rg -n -g "*.h" -g "*.cpp" -g "*.ixx" -- "->父|->子|->下|\.父|\.子|\.下"
```

输出文件：

```text
日志/20260620_世界树结构操作入口扫描.txt
```

命令结果：

```text
三组扫描命令退出码均为 0。
扫描日志 1042 行；源码命中约 1023 行。
```

## 3. 关键证据

直接入口统计：

```text
基础信息().世界根：11
基础信息().查找主键：45
基础信息().枚举节点_按类型：11
基础信息_->世界根：4
基础信息_->查找主键：7
基础信息_->添加子节点：7
基础信息_->删除节点：7
基础信息_->枚举节点_按类型：8
基础信息_->获取读锁：4
```

判定表：

```text
实施记录/20260620_世界树结构操作入口判定表.md
```

关键分类：

```text
1. `世界树类.cpp` 内部和 `基础信息类.cpp` / 双向链表底层属于正式收口层或底层容器，不作为外部回迁对象。
2. `存在类.cpp` / `场景类.cpp` / `特征类.cpp` / `状态类.cpp` / `动态类.cpp` / `二次特征类.cpp` 的 `基础信息_` 操作属于领域服务和主信息维护边界，后续由 `世界树类` 编排调用。
3. `方法类.cpp` 的 4 处 `世界树.基础信息().查找主键` 是无独立树歧义的世界树引用解析，适合作为下一最小代码回迁切片。
4. `任务模块.筹办.ixx`、`自我线程模块.impl.cpp`、`需求类.cpp`、`任务类.cpp` 混合了世界树引用解析和需求 / 任务 / 方法独立树遍历，需拆切片处理。
5. `控制面板类.cpp` 大量父子链命中含 UI 树 `子项` 和显示层骨架，不可按原始命中数直接判为机器逻辑违规。
```

## 4. 当前判断

```text
世界树结构操作入口审计切片已形成判定表。
下一最小回迁切片建议为：P1.5 按主键解析入口补缺与 方法类.cpp 引用解析回迁。
该切片先补 `世界树类::按主键解析节点`，再把 `方法类.cpp` 内二次特征 / 特征 / 状态 / 基础信息引用解析从 `世界树.基础信息().查找主键` 改为 `世界树类` 或领域服务入口。
```

## 5. 下一步

```text
1. 执行 P1.5 按主键解析入口补缺与 方法类.cpp 引用解析回迁。
2. 新建函数前先扫描 说明书/函数功能说明文档.md。
3. 新增函数后运行 python .\tools\生成函数功能说明文档.py。
4. 构建 Debug x64。
```

## 6. P1.5 按主键解析入口补缺与方法类回迁

切片目标：

```text
1. 补 `世界树类::按主键解析节点`，只读解析基础信息世界树节点，不创建或修改节点。
2. 将 `方法类.cpp` 内二次特征 / 特征 / 状态 / 基础信息引用解析从 `世界树.基础信息().查找主键` 回迁到 世界树类 或领域服务入口。
```

已运行命令：

```powershell
rg -n "按主键解析节点|按主键解析.*节点|查找主键|世界树类::.*主键|世界树类.*解析" 说明书\函数功能说明文档.md
rg -n "世界树\.基础信息\(\)\.查找主键|按主键解析节点" 方法类.cpp 世界树类.h 世界树类.cpp
python .\tools\生成函数功能说明文档.py
rg -n "世界树类::按主键解析节点|按主键解析节点" 说明书\函数功能说明文档.md 世界树类.h 世界树类.cpp
```

已改文件：

```text
世界树类.h
世界树类.cpp
方法类.cpp
说明书/函数功能说明文档.md
```

验证前预测：

```text
1. `方法类.cpp` 不再直接出现 `世界树.基础信息().查找主键`。
2. `说明书/函数功能说明文档.md` 出现 `世界树类::按主键解析节点` 定义条目。
3. Debug x64 构建应通过；若失败，应优先检查新增声明 / 定义签名和 typed resolver 可见性。
```

验证结果：

```text
1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6927。
2. `说明书/函数功能说明文档.md` 第 347 行出现 `世界树类::按主键解析节点`。
3. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0，构建日志显示 0 个错误。
4. `rg -n "世界树\.基础信息\(\)\.查找主键" 方法类.cpp` 退出码 1，无命中，符合预期。
```

后续最小切片建议：

```text
P1.6 任务筹办状态 / 特征引用解析回迁。

范围：
    任务模块.筹办.ixx 第 1204 行 `解析特征引用_筹办查询`
    任务模块.筹办.ixx 第 1218 行 `解析状态引用_筹办查询`

边界：
    暂不触碰第 431 行 `任务来源需求`，该处涉及需求节点是否属于需求独立树，需要需求类正式引用解析入口确认。
```

## 7. P1.6 任务筹办状态 / 特征引用解析回迁

切片目标：

```text
1. 将 `解析特征引用_筹办查询` 改用 `世界树.特征().按主键解析特征节点`。
2. 将 `解析状态引用_筹办查询` 改用 `世界树.状态().按主键解析状态节点`。
3. 暂不触碰 `任务来源需求` 的需求节点解析。
```

已改文件：

```text
任务模块.筹办.ixx
```

验证前预测：

```text
1. `rg -n "世界树\.基础信息\(\)\.查找主键" 任务模块.筹办.ixx` 应只剩第 431 行 `任务来源需求`。
2. Debug x64 构建应通过；若失败，优先检查 `特征类::按主键解析特征节点` 和 `状态类::按主键解析状态节点` 返回类型是否匹配。
```

验证结果：

```text
1. `rg -n "世界树\.基础信息\(\)\.查找主键" 任务模块.筹办.ixx` 退出码 0，仅剩第 431 行 `任务来源需求`。
2. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0，构建日志显示 0 个错误。
```

后续最小切片建议：

```text
P1.7 二次特征应用模块动态引用解析回迁。

范围：
    二次特征应用模块.ixx 第 1066 行。

建议：
    将动态引用解析改用 `世界树->动态().按主键解析动态节点`。
```

## 8. P1.7 二次特征应用模块动态引用解析回迁

切片目标：

```text
1. 将 `二次特征应用模块.ixx` 第 1066 行动态引用解析从 `世界树->基础信息().查找主键` 回迁到 `世界树->动态().按主键解析动态节点`。
2. 保留引用指针优先和主键一致性检查逻辑。
```

已改文件：

```text
二次特征应用模块.ixx
```

验证前预测：

```text
1. `rg -n "基础信息\(\)\.查找主键" 二次特征应用模块.ixx` 应无命中，退出码 1。
2. Debug x64 构建应通过；若失败，优先检查模块中 `世界树->动态()` 可见性和返回类型。
```

验证结果：

```text
1. `rg -n "基础信息\(\)\.查找主键" 二次特征应用模块.ixx` 退出码 1，无命中。
2. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0，构建日志显示 0 个错误。
```

后续最小切片建议：

```text
P1.8 需求类状态 / 特征引用解析回迁。

范围：
    需求类.cpp 第 610、628、735、753 行。

建议：
    状态引用改用 `世界树.状态().按主键解析状态节点`。
    特征引用改用 `世界树.特征().按主键解析特征节点`。
```

## 9. P1.8 需求类状态 / 特征引用解析回迁

切片目标：

```text
1. 将 `需求类.cpp` 中状态引用解析改用 `世界树.状态().按主键解析状态节点`。
2. 将 `需求类.cpp` 中特征引用解析改用 `世界树.特征().按主键解析特征节点`。
3. 不改需求树、方法候选树或 `私有_直接基础信息子节点_按类型`。
```

已改文件：

```text
需求类.cpp
```

验证前预测：

```text
1. `rg -n "世界树\.基础信息\(\)\.查找主键" 需求类.cpp` 应无命中，退出码 1。
2. Debug x64 构建应通过；若失败，优先检查状态 / 特征 typed resolver 返回类型。
```

验证结果：

```text
1. `rg -n "世界树\.基础信息\(\)\.查找主键" 需求类.cpp` 退出码 1，无命中。
2. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0，构建日志显示 0 个错误。
```

后续最小切片建议：

```text
P1.9 鱼巢.cpp 控制命令引用解析回迁。

范围：
    鱼巢.cpp 第 1682、1717、1796 行。

建议：
    状态引用改用 `世界树.状态().按主键解析状态节点`。
    特征引用改用 `世界树.特征().按主键解析特征节点`。
    任务虚拟存在基础节点引用改用 `世界树.按主键解析节点`。
```

## 10. P1.9 鱼巢.cpp 控制命令引用解析回迁

切片目标：

```text
1. 将控制命令状态引用解析改用 `世界树.状态().按主键解析状态节点`。
2. 将控制命令状态特征引用解析改用 `世界树.特征().按主键解析特征节点`。
3. 将任务虚拟存在基础节点引用解析改用 `世界树.按主键解析节点`。
```

已改文件：

```text
鱼巢.cpp
```

验证前预测：

```text
1. `rg -n "世界树\.基础信息\(\)\.查找主键" 鱼巢.cpp` 应无命中，退出码 1。
2. Debug x64 构建应通过；若失败，优先检查 `世界树.按主键解析节点` const 返回适配。
```

验证结果：

```text
1. `rg -n "世界树\.基础信息\(\)\.查找主键" 鱼巢.cpp` 退出码 1，无命中。
2. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0，构建日志显示 0 个错误。
3. 剩余 `世界树(->|.)基础信息().查找主键` 主要分布：自我线程模块 14、控制面板 7、任务管理界面线程 4、任务类 2、自检线程 2、控制面板摘要线程 1、任务筹办 1。
```

后续最小切片建议：

```text
P1.10 存在按主键解析入口补缺。

原因：
    剩余调用中多处解析任务虚拟存在、方法虚拟存在、目标主体或被需求存在。
    当前只有 `场景 / 特征 / 状态 / 动态 / 二次特征 / 因果 / 方法` typed resolver，缺存在 typed resolver。

范围建议：
    存在类.h
    存在类.cpp
    世界树类.h
    世界树类.cpp
    说明书/函数功能说明文档.md
    实施记录/20260620_世界树类逻辑纠偏_Codex断点清单.md
```

## 11. P1.10 存在按主键解析入口补缺

切片目标：

```text
1. 在 `存在类` 补 `按主键解析存在节点`，只读解析存在节点，不创建或修改节点。
2. 在 `世界树类` 补同名编排入口，为后续任务虚拟存在 / 方法虚拟存在 / 目标主体引用解析回迁提供正式入口。
```

已改文件：

```text
存在类.h
存在类.cpp
世界树类.h
世界树类.cpp
```

验证前预测：

```text
1. `python .\tools\生成函数功能说明文档.py` 应写入 `存在类::按主键解析存在节点` 与 `世界树类::按主键解析存在节点` 条目。
2. Debug x64 构建应通过；若失败，优先检查 `存在类.h` 的 `<string>` include 和 `世界树类` 声明 / 定义签名。
```

验证结果：

```text
1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6929。
2. `说明书/函数功能说明文档.md` 中出现 `世界树类::按主键解析存在节点` 和 `存在类::按主键解析存在节点`。
3. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0，构建日志显示 0 个错误。
```

后续最小切片建议：

```text
P1.11 任务类状态 / 任务虚拟存在引用解析回迁。

范围：
    任务类.cpp 第 208、1887 行。

建议：
    状态引用改用 `世界树.状态().按主键解析状态节点`。
    任务虚拟存在引用改用 `世界树.按主键解析存在节点`。
```

## 12. P1.11 任务类状态 / 任务虚拟存在引用解析回迁

切片目标：

```text
1. 将 `任务类.cpp` 状态引用解析改用 `世界树.状态().按主键解析状态节点`。
2. 将任务虚拟存在引用解析改用 `世界树.按主键解析存在节点`。
3. 不改任务树父子链和任务 SQL 投影逻辑。
```

已改文件：

```text
任务类.cpp
```

验证前预测：

```text
1. `rg -n "世界树\.基础信息\(\)\.查找主键" 任务类.cpp` 应无命中，退出码 1。
2. Debug x64 构建应通过；若失败，优先检查 `世界树.按主键解析存在节点` 可见性。
```

验证结果：

```text
1. `rg -n "世界树\.基础信息\(\)\.查找主键" 任务类.cpp` 退出码 1，无命中。
2. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0，构建日志显示 0 个错误。
3. 剩余 `世界树(->|.)基础信息().查找主键` 分布：自我线程模块 14、控制面板 7、任务管理界面线程 4、自检线程 2、控制面板摘要线程 1、任务筹办 1。
```

后续最小切片建议：

```text
P1.12 自检线程方法虚拟存在引用解析回迁。

范围：
    自检线程模块.impl.cpp 第 369、400 行。

建议：
    两处均改用 `世界树.按主键解析存在节点`。
```

## 13. P1.12 自检线程方法虚拟存在引用解析回迁

切片目标：

```text
1. 将 `自检线程模块.impl.cpp` 两处方法虚拟存在引用解析改用 `世界树.按主键解析存在节点`。
2. 不改自检线程方法评分、特征读取和任务生成逻辑。
```

已改文件：

```text
自检线程模块.impl.cpp
```

验证前预测：

```text
1. `rg -n "世界树\.基础信息\(\)\.查找主键" 自检线程模块.impl.cpp` 应无命中，退出码 1。
2. Debug x64 构建应通过。
```

验证结果：

```text
1. `rg -n "世界树\.基础信息\(\)\.查找主键" 自检线程模块.impl.cpp` 退出码 1，无命中。
2. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0，构建日志显示 0 个错误。
3. 剩余 `世界树(->|.)基础信息().查找主键` 分布：自我线程模块 14、控制面板 7、任务管理界面线程 4、控制面板摘要线程 1、任务筹办 1。
```

后续最小切片建议：

```text
P1.13 控制面板摘要线程方法虚拟存在引用解析回迁。

范围：
    控制面板摘要线程模块.impl.cpp 第 110 行。

建议：
    改用 `世界树.按主键解析存在节点`。
```

## 14. P1.13 控制面板摘要线程方法虚拟存在引用解析回迁

切片目标：

```text
1. 将 `控制面板摘要线程模块.impl.cpp` 第 110 行方法虚拟存在引用解析改用 `世界树.按主键解析存在节点`。
2. 不改控制面板摘要线程统计和输出逻辑。
```

已改文件：

```text
控制面板摘要线程模块.impl.cpp
```

验证前预测：

```text
1. `rg -n "世界树\.基础信息\(\)\.查找主键" 控制面板摘要线程模块.impl.cpp` 应无命中，退出码 1。
2. Debug x64 构建应通过。
```

验证结果：

```text
1. `rg -n "世界树\.基础信息\(\)\.查找主键" 控制面板摘要线程模块.impl.cpp` 退出码 1，无命中。
2. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0，构建日志显示 0 个错误。
3. 剩余 `世界树(->|.)基础信息().查找主键` 分布：自我线程模块 14、控制面板 7、任务管理界面线程 4、任务筹办 1。
```

后续最小切片建议：

```text
P1.14 任务管理界面线程无歧义引用解析回迁。

范围：
    任务模块.管理界面线程.impl.cpp 第 3876、3900、3930 行。

暂不触碰：
    第 3861 行 `对应需求` 解析，原因同任务筹办第 431 行，需需求类正式引用解析入口确认。
```

## 15. P1.14 任务管理界面线程无歧义引用解析回迁

切片目标：

```text
1. 将任务虚拟存在引用解析改用 `世界树.按主键解析存在节点`。
2. 将状态引用解析改用 `世界树.状态().按主键解析状态节点`。
3. 将状态特征引用解析改用 `世界树.特征().按主键解析特征节点`。
4. 暂不触碰 `对应需求` 解析。
```

已改文件：

```text
任务模块.管理界面线程.impl.cpp
```

验证前预测：

```text
1. `rg -n "世界树\.基础信息\(\)\.查找主键" 任务模块.管理界面线程.impl.cpp` 应仅剩第 3861 行 `对应需求`。
2. Debug x64 构建应通过。
```

验证结果：

```text
1. `rg -n "世界树\.基础信息\(\)\.查找主键" 任务模块.管理界面线程.impl.cpp` 退出码 0，仅剩第 3861 行 `对应需求`。
2. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0，构建日志显示 0 个错误。
3. 剩余 `世界树(->|.)基础信息().查找主键` 分布：自我线程模块 14、控制面板 7、任务筹办 1、任务管理界面线程 1。
```

后续最小切片建议：

```text
P1.15 控制面板显示层引用解析回迁。

范围：
    控制面板类.cpp 第 130、3127、3143、3214、3583、4529、9648 行。

建议：
    方法虚拟存在引用改用 `世界树.按主键解析存在节点`。
    通用基础信息引用改用 `世界树.按主键解析节点`。
    特征引用改用 `世界树.特征().按主键解析特征节点`。
    存在判断改用 `世界树.按主键解析存在节点`。
```

## 16. P1.15 控制面板显示层引用解析回迁

切片目标：

```text
1. 将方法虚拟存在引用解析改用 `世界树.按主键解析存在节点`。
2. 将通用基础信息引用解析改用 `世界树.按主键解析节点`。
3. 将状态特征 / 目标特征引用解析改用 `世界树.特征().按主键解析特征节点`。
4. 将存在判断改用 `世界树.按主键解析存在节点`。
```

已改文件：

```text
控制面板类.cpp
```

验证前预测：

```text
1. `rg -n "世界树\.基础信息\(\)\.查找主键" 控制面板类.cpp` 应无命中，退出码 1。
2. Debug x64 构建应通过。
```

验证结果：

```text
1. `rg -n "世界树\.基础信息\(\)\.查找主键" 控制面板类.cpp` 退出码 1，无命中。
2. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0，构建日志显示 0 个错误。
3. 剩余 `世界树(->|.)基础信息().查找主键` 分布：自我线程模块 14、任务筹办 1、任务管理界面线程 1。
```

后续最小切片建议：

```text
P1.16 自我线程引用解析回迁。

范围：
    自我线程模块.impl.cpp 当前 14 处 `世界树.基础信息().查找主键`。

建议：
    基础信息节点引用改用 `世界树.按主键解析节点`。
    状态引用改用 `世界树.状态().按主键解析状态节点`。
    特征引用改用 `世界树.特征().按主键解析特征节点`。
    存在引用改用 `世界树.按主键解析存在节点`。
    场景引用改用 `世界树.场景().按主键解析场景节点`。
```

## 17. P1.16 自我线程引用解析回迁

切片目标：

```text
1. 将自我线程模块.impl.cpp 当前 14 处 `世界树.基础信息().查找主键` 回迁到世界树或领域解析入口。
2. 任务 / 需求独立树解析只替换底层主键查找入口，继续保留独立树归属判断。
3. 本切片不新增函数，不修改需求类正式解析入口。
```

验证前预测：

```text
1. `rg -n "世界树\.基础信息\(\)\.查找主键" 自我线程模块.impl.cpp` 应无命中，退出码 1。
2. Debug x64 构建应通过；若失败，优先检查 typed resolver 返回类型与旧 `dynamic_cast` / `reinterpret_cast` 的类型差异。
3. 剩余旧入口应只集中在需求解析待切片位置：任务模块.筹办.ixx、任务模块.管理界面线程.impl.cpp。
```

已改文件：

```text
自我线程模块.impl.cpp
```

验证结果：

```text
1. `rg -n "世界树\.基础信息\(\)\.查找主键" 自我线程模块.impl.cpp` 退出码 1，无命中。
2. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0，构建日志显示“已成功生成”。
3. 剩余 `世界树(->|.)基础信息().查找主键` 仅两处：
   - 任务模块.筹办.ixx:431，对应需求主键解析。
   - 任务模块.管理界面线程.impl.cpp:3861，对应需求主键解析。
```

后续最小切片建议：

```text
P1.17 任务侧对应需求解析回迁。

范围：
    需求类.h / 需求类.cpp
    任务模块.筹办.ixx
    任务模块.管理界面线程.impl.cpp
    说明书/函数功能说明文档.md

建议：
    补 `需求类::按主键解析需求节点(需求根节点, 主键)`，只在给定需求树根下只读定位需求节点。
    两处任务侧 `对应需求` 解析改用需求类入口，不再直接 `世界树.基础信息().查找主键`。
```

## 18. P1.17 任务侧对应需求解析回迁

切片目标：

```text
1. 新增 `需求类::按主键解析需求节点(需求根节点, 主键)`，只读解析给定需求树根下的需求节点。
2. 将任务筹办和任务管理界面线程的 `对应需求` 主键解析改用需求类入口。
3. 本切片不把需求独立树裸归入世界树，不修改任务筹办 / 任务执行本能边界。
```

新建函数前检查：

```text
`rg -n "按主键解析需求节点|需求类::按主键解析需求节点" 说明书\函数功能说明文档.md 需求类.h 需求类.cpp`
仅命中自我线程模块私有函数，无 `需求类::按主键解析需求节点` 公开入口。
```

验证前预测：

```text
1. `python .\tools\生成函数功能说明文档.py` 应成功并登记新函数。
2. `rg -n "世界树(->|\.)基础信息\(\)\.查找主键" -g "*.cpp" -g "*.ixx" -g "*.h"` 应无命中，退出码 1。
3. Debug x64 构建应通过。
```

已改文件：

```text
需求类.h
需求类.cpp
任务模块.筹办.ixx
任务模块.管理界面线程.impl.cpp
说明书/函数功能说明文档.md
```

验证结果：

```text
1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6931。
2. `说明书/函数功能说明文档.md` 已登记 `需求类::按主键解析需求节点`。
3. `rg -n "世界树(->|\.)基础信息\(\)\.查找主键" -g "*.cpp" -g "*.ixx" -g "*.h"` 退出码 1，无命中。
4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0，构建日志显示“已成功生成”。
```

后续最小切片建议：

```text
P2.2 外部 `世界树.基础信息().世界根()` 回迁。

范围：
    先从 `本能动作管理类.ixx`、`自我动作实现.外设模块.ixx`、`自我线程模块.impl.cpp`、`控制面板类.cpp` 中外部世界根读取分类。

建议：
    能直接读根指针的调用改为 `世界树.世界根()`。
    需要枚举或快照的调用优先判断是否已有更具体世界树结构化快照入口。
```

## 19. P2.2 外部世界根读取回迁

切片目标：

```text
1. 将外部 `世界树.基础信息().世界根()` 回迁为 `世界树.世界根()`。
2. 本切片只处理世界根读取，不顺带处理 `枚举节点_按类型` 或父 / 子 / 下 链。
```

分类结果：

```text
当前命中 11 处：
    本能动作管理类.ixx 1 处
    控制面板类.cpp 5 处
    自我线程模块.impl.cpp 4 处
    自我动作实现.外设模块.ixx 1 处

判断：
    均为根指针读取、根初始化判断或显示骨架入口，可直接改为 `世界树.世界根()`。
```

验证前预测：

```text
1. `rg -n "世界树\.基础信息\(\)\.世界根" -g "*.cpp" -g "*.ixx" -g "*.h"` 应无命中，退出码 1。
2. Debug x64 构建应通过。
```

已改文件：

```text
本能动作管理类.ixx
控制面板类.cpp
自我线程模块.impl.cpp
自我动作实现.外设模块.ixx
```

验证结果：

```text
1. `rg -n "世界树\.基础信息\(\)\.世界根" -g "*.cpp" -g "*.ixx" -g "*.h"` 退出码 1，无命中。
2. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0，构建日志显示“已成功生成”。
3. 后续外部直接基础信息入口剩余：`世界树.基础信息().枚举节点_按类型` 11 处。
```

后续最小切片建议：

```text
P2.3 外部 `世界树.基础信息().枚举节点_按类型` 回迁。

建议：
    先补世界树类只读枚举 / 计数入口，再替换控制面板和自我动作实现模块的外部枚举调用。
    父 / 子 / 下 链回迁顺延到外部枚举入口闭合后。
```

## 20. P2.3 外部按类型枚举入口回迁

切片目标：

```text
1. 新增世界树类只读入口，承接按主信息类型枚举 / 统计基础信息世界树节点。
2. 将外部 `世界树.基础信息().枚举节点_按类型` 调用回迁到世界树类入口。
3. 本切片不处理父 / 子 / 下 链直接访问。
```

新建函数前检查：

```text
`rg -n "枚举节点_按类型|枚举.*按.*类型|统计.*节点|节点数量|世界树类::.*枚举|世界树类::.*统计" 说明书\函数功能说明文档.md 世界树类.h 世界树类.cpp`
未发现可复用的 `世界树类` 按主信息类型枚举 / 统计入口。
```

验证前预测：

```text
1. `python .\tools\生成函数功能说明文档.py` 应成功并登记新入口。
2. `rg -n "世界树\.基础信息\(\)\.枚举节点_按类型" -g "*.cpp" -g "*.ixx" -g "*.h"` 应无命中，退出码 1。
3. Debug x64 构建应通过。
```

已改文件：

```text
世界树类.h
控制面板类.cpp
自我动作实现模块.ixx
说明书/函数功能说明文档.md
```

验证结果：

```text
1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6931。
2. `世界树类.h` 已新增 `枚举节点_按主信息类型` / `统计节点_按主信息类型` 模板入口；生成器未将模板入口单独列入函数表。
3. `rg -n "世界树\.基础信息\(\)\.枚举节点_按类型" -g "*.cpp" -g "*.ixx" -g "*.h"` 退出码 1，无命中。
4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0，构建日志显示“已成功生成”。
```

后续最小切片建议：

```text
P2.4 外部父 / 子 / 下 链读取分类与回迁。

建议：
    先筛掉 世界树类.cpp、双向链表模板核心、任务 / 需求 / 方法独立树、UI 子项字段。
    对确属基础信息世界树的外部父子链读取，补或复用 `世界树类` 关系查询 / 子节点枚举入口。
```

## 21. P2.4 父 / 子 / 下 链分类与第一回迁簇

扫描结果：

```text
`rg -n -g "*.cpp" -g "*.ixx" -g "*.h" -- "->父|->子|->下|\\.父|\\.子|\\.下" > 日志/20260620_P2_4_父子链扫描.txt`
退出码 0，总命中 919 行。

高频文件：
    控制面板类.cpp 183
    自我线程模块.impl.cpp 174
    自我动作实现模块.ixx 52
    双向链表模板核心.h 47
    自我动作实现.外设模块.ixx 42
    任务模块.筹办.ixx 41
```

第一分类：

```text
1. 世界树类.cpp、双向链表模板核心.h：正式收口层或底层容器，暂不作为外部回迁对象。
2. 任务 / 需求 / 方法独立树、控制面板 UI `子项`：不按基础信息世界树处理。
3. 控制面板类.cpp `私有_构建因果信息树` 第 5345-5352 行：确属基础信息世界树父链读取，用于判断因果节点是否已有因果祖先。
```

本切片第一回迁簇：

```text
1. 补 `世界树类::获取父节点`，只读返回基础信息世界树父节点。
2. 将 `私有_构建因果信息树` 的父链读取改为 `世界树.获取父节点`。
3. 因果类型判断复用 `世界树.因果().是因果节点`。
```

新建函数前检查：

```text
`rg -n "获取父节点|世界树类::获取父节点" 说明书\函数功能说明文档.md 世界树类.h 世界树类.cpp`
退出码 1，无同名入口。
```

验证前预测：

```text
1. `python .\tools\生成函数功能说明文档.py` 应成功并登记 `世界树类::获取父节点`。
2. `控制面板类.cpp` 该因果根祖先判断簇不再直接读取 `->父`。
3. Debug x64 构建应通过。
```

已改文件：

```text
世界树类.h
世界树类.cpp
控制面板类.cpp
说明书/函数功能说明文档.md
```

验证结果：

```text
1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6932。
2. `说明书/函数功能说明文档.md` 已登记 `世界树类::获取父节点`。
3. `rg -n "因果节点 \? static_cast<基础信息节点类\*>\(因果节点->父\)|世界树\.基础信息\(\)\.取主信息<因果主信息类>\(父节点\)|父节点->父" 控制面板类.cpp` 退出码 1，无命中。
4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0，构建日志显示“已成功生成”。
```

后续状态：

```text
P2.4 未整体完成。
下一步继续从 `日志/20260620_P2_4_父子链扫描.txt` 分类，选择下一个确属基础信息世界树的外部父子链读取簇。
```

第二回迁簇：

```text
范围：
    二次特征应用模块.ixx:832-833
    二次特征应用模块.ixx:2774

判断：
    两处均从状态 / 动态等基础信息节点读取父场景，属于基础信息世界树父链读取。

修改：
    复用 `世界树类::获取父节点`。
    `私有_取所属场景` 改为接收 `世界树类*`，由世界树实例负责父节点读取和场景类型判断。
```

验证前预测：

```text
1. `rg -n "节点->父|动态节点->父" 二次特征应用模块.ixx` 应无命中，退出码 1。
2. Debug x64 构建应通过。
```

第二回迁簇验证结果：

```text
已改文件：
    二次特征应用模块.ixx

验证结果：
    1. `rg -n "节点->父|动态节点->父" 二次特征应用模块.ixx` 退出码 1，无命中。
    2. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0，构建日志显示“已成功生成”。
```

后续状态：

```text
P2.4 未整体完成。
下一步继续从更新后的父子链扫描中分类，优先筛选确属基础信息世界树且不属于领域类内部服务的外部簇。
```

第三回迁簇：

```text
范围：
    本能动作管理类.ixx:761-769

判断：
    本地 `子节点数` lambda 直接遍历基础信息节点子链，只用于统计直接子节点数量。

修改：
    复用既有 `世界树类::获取子节点`。
```

验证前预测：

```text
1. `rg -n "父->子|当前->下" 本能动作管理类.ixx` 不再命中该 `子节点数` lambda。
2. Debug x64 构建应通过。
```

第三回迁簇验证结果：

```text
已改文件：
    本能动作管理类.ixx

验证结果：
    1. `rg -n "父->子|当前->下" 本能动作管理类.ixx` 退出码 1，无命中。
    2. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0，构建日志显示“已成功生成”。
```

后续状态：

```text
P2.4 未整体完成。
下一步继续父 / 子 / 下 链分类。
```

P2.5 第九小簇：二次特征子节点读取 / 按签名创建公开入口收口

```text
范围：
    世界树类.h
    世界树类.cpp
    二次特征应用模块.ixx
    自我线程模块.impl.cpp
    需求类.cpp

判断：
    `二次特征类::获取子二次特征` 读取父节点直接子二次特征；
    `二次特征类::获取或创建子二次特征_按签名` 可能创建基础信息世界树子节点；
    外部调用方经 `世界树.二次特征()` / `世界树->二次特征()` 直接调用这两个入口，属于世界树结构读 / 显式创建公开入口绕过。

修改：
    新增 `世界树类::获取子二次特征`。
    新增 `世界树类::获取或创建子二次特征_按签名`。
    将 11 处外部调用迁移到 `世界树类` 包装。

静态复查：
    `rg -n "二次特征\(\)\.(获取子二次特征|获取或创建子二次特征_按签名)\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 退出码 1，无命中。

验证前预测：
    1. 函数说明文档生成应登记两个新增 `世界树类` 函数。
    2. Debug x64 构建应通过；若失败，优先检查 `世界树类.h` 声明签名、`二次特征主信息类` 引用可见性和调用点 const / 指针类型。
    3. 迁移后仍保留 `取二次特征主信息`、`绑定场景`、`设置生成语义` 等二次特征领域主信息操作入口，不扩大本切片范围。

验证结果：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6943。
    2. `说明书/函数功能说明文档.md` 出现 `世界树类::获取子二次特征` 和 `世界树类::获取或创建子二次特征_按签名` 定义条目。
    3. `rg -n "二次特征\(\)\.(获取子二次特征|获取或创建子二次特征_按签名)\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 退出码 1，无命中。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260620_P2_5_二次特征子节点入口收口_build.log` 显示“已成功生成”。

当前判断：
    P2.5 第九小簇已闭合。
    二次特征类内部子节点读取 / 按签名创建仍归类为领域服务实现；外部二次特征子节点结构入口已收口到 `世界树类`。

下一步：
    继续 P2.5，审查 `二次特征类::创建二次特征` / `删除二次特征` / `枚举全部二次特征` 剩余入口是否存在外部绕过调用或缺少 `世界树类` 包装。
```

P2.5 第十小簇：二次特征直接创建剩余入口收口与删除 / 枚举分类

```text
范围：
    自我动作实现.外设模块.ixx
    因果类.cpp 只作分类记录，不改代码。

判断：
    1. `自我动作实现.外设模块.ixx` 中 `auto& 二次特征服务 = 世界树.二次特征()` 后调用 `二次特征服务.创建二次特征`，属于外部经领域访问器绕过 `世界树类::创建二次特征` 的基础信息世界树节点创建。
    2. `二次特征类::删除二次特征` 未发现外部调用，不新增未使用包装。
    3. `因果类.cpp` 中 `二次特征类 二次特征服务(base)` 后调用 `枚举全部二次特征` 依赖传入 `base` 的领域服务实例，直接改成全局 `世界树` 会改变基础信息实例边界；本小簇先登记为跨领域服务边界分类，不改代码。

验证前预测：
    1. `rg -n "二次特征服务\.创建二次特征\(" 自我动作实现.外设模块.ixx` 应无命中。
    2. `rg -n "二次特征\(\)\.(创建二次特征|删除二次特征|枚举全部二次特征)\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 应无命中。
    3. Debug x64 构建应通过。

验证结果：
    1. `rg -n "二次特征服务\.创建二次特征\(" 自我动作实现.外设模块.ixx` 退出码 1，无命中。
    2. `rg -n "二次特征\(\)\.(创建二次特征|删除二次特征|枚举全部二次特征)\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 退出码 1，无命中。
    3. `rg -n "删除二次特征\(|枚举全部二次特征\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 只剩二次特征类声明 / 定义和 `因果类.cpp` 中按 `base` 构造的本地领域服务枚举。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260620_P2_5_二次特征创建剩余入口收口_build.log` 显示“已成功生成”。

当前判断：
    P2.5 第十小簇已闭合。
    二次特征创建 / 删除 / 枚举公开绕过入口已分类并处理；二次特征类内部 `基础信息_` 小域分类完成。

下一步：
    继续 P2.5，转入下一个领域类内部 `基础信息_` 操作分类。
```

P2.5 第十四小簇：特征按主键解析公开入口收口

```text
范围：
    世界树类.h
    世界树类.cpp
    任务模块.筹办.ixx
    任务模块.管理界面线程.impl.cpp
    控制面板类.cpp
    方法类.cpp
    自我线程模块.impl.cpp
    需求类.cpp
    鱼巢.cpp

判断：
    `特征类::按主键解析特征节点` 内部 `基础信息_->查找主键` 属于特征领域只读解析实现；外部按主键解析特征节点应由 `世界树类` 对外承接。

验证前预测：
    1. `rg -n "特征\(\)\.按主键解析特征节点\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 应无命中。
    2. 函数说明文档生成应登记 `世界树类::按主键解析特征节点`。
    3. Debug x64 构建应通过。

验证结果：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6950。
    2. `说明书/函数功能说明文档.md` 出现 `世界树类::按主键解析特征节点` 定义条目。
    3. `rg -n "特征\(\)\.按主键解析特征节点\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 退出码 1，无命中。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260620_P2_5_特征按主键解析入口收口_build.log` 显示“已成功生成”。

当前判断：
    P2.5 第十四小簇已闭合。
    特征按主键解析公开入口已收口到 `世界树类`。

下一步：
    继续 P2.5，审查特征子链读取 / 按类型查找公开入口。
```

P2.5 第十五小簇：特征子节点读取公开入口收口

```text
范围：
    世界树类.h
    世界树类.cpp
    任务模块.筹办.ixx
    任务类.cpp
    自我动作实现.外设模块.ixx
    自我动作实现.服务模块.ixx
    自我动作实现模块.ixx
    语言类_存在表达.cpp

判断：
    `特征类::获取子特征` 是特征领域只读子链枚举实现；外部读取基础信息世界树子特征应由 `世界树类` 对外承接。

验证前预测：
    1. `rg -n "特征\(\)\.获取子特征\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 应无命中。
    2. 函数说明文档生成应登记 `世界树类::获取子特征`。
    3. Debug x64 构建应通过。

验证结果：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6952。
    2. `说明书/函数功能说明文档.md` 出现 `世界树类::获取子特征` 定义条目。
    3. `rg -n "特征\(\)\.获取子特征\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 退出码 1，无命中。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260620_P2_5_特征子节点入口收口_build.log` 显示“已成功生成”。

当前判断：
    P2.5 第十五小簇已闭合。
    特征子节点读取公开入口已收口到 `世界树类`。

下一步：
    继续 P2.5，审查 `特征类::查找子特征_按类型` 公开入口。
```

P2.5 第十六小簇：特征按类型查找公开入口收口

```text
范围：
    世界树类.h
    世界树类.cpp
    任务模块.筹办.ixx
    自我类.cpp
    自我动作实现.服务模块.ixx
    自我动作实现模块.ixx
    自检线程模块.impl.cpp
    自我线程模块.impl.cpp
    自我动作实现.外设模块.ixx

判断：
    `特征类::查找子特征_按类型` 是特征领域只读子链查找实现；外部按类型查找基础信息世界树子特征应由 `世界树类` 对外承接。

验证前预测：
    1. `rg -n "特征\(\)\.查找子特征_按类型\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 应无命中。
    2. 函数说明文档生成应登记 `世界树类::查找子特征_按类型`。
    3. Debug x64 构建应通过。

验证结果：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6954。
    2. `说明书/函数功能说明文档.md` 出现 `世界树类::查找子特征_按类型` 定义条目。
    3. `rg -n "特征\(\)\.查找子特征_按类型\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 退出码 1，无命中。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260620_P2_5_特征按类型查找入口收口_build.log` 显示“已成功生成”。

当前判断：
    P2.5 第十六小簇已闭合。
    特征按类型查找公开入口已收口到 `世界树类`。

下一步：
    继续 P2.5，审查 `特征类::创建特征_按类型` / `删除特征` 剩余公开入口。
```

P2.5 第十七小簇：特征创建 / 删除剩余公开入口收口

```text
范围：
    世界树类.h
    世界树类.cpp
    任务类.cpp
    自我动作实现模块.ixx
    自我动作实现.外设模块.ixx

判断：
    1. `特征类::创建特征_按类型` 内部创建基础信息世界树子节点，外部直接创建应由 `世界树类` 承接；不能替换成 `取或创建子特征_按类型`，否则会改变允许多个同类型候选特征的语义。
    2. `特征类::删除特征` 内部删除基础信息节点，外部删除应由 `世界树类` 承接。

验证前预测：
    1. `rg -n "特征\(\)\.(创建特征_按类型|删除特征|枚举全部特征)\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 应无命中。
    2. 函数说明文档生成应登记 `世界树类::创建特征_按类型` 和 `世界树类::删除特征`。
    3. Debug x64 构建应通过。

验证结果：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6957。
    2. `说明书/函数功能说明文档.md` 出现 `世界树类::创建特征_按类型` 和 `世界树类::删除特征` 定义条目。
    3. `rg -n "特征\(\)\.(按主键解析特征节点|获取子特征|查找子特征_按类型|创建特征_按类型|删除特征|枚举全部特征|创建特征\()" -g "*.h" -g "*.cpp" -g "*.ixx"` 退出码 1，无命中。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260620_P2_5_特征创建删除入口收口_build.log` 显示“已成功生成”。

当前判断：
    P2.5 第十七小簇已闭合。
    特征类按主键解析 / 子节点读取 / 按类型查找 / 创建 / 删除公开绕过入口已清零；特征类 P2.5 小域分类完成。

下一步：
    继续 P2.5，复查剩余领域访问器公开结构入口，选择下一最小可闭合簇或收束 P2.5。
```

P2.5 第十八小簇：场景子场景 / 子存在剩余公开入口收口

```text
范围：
    方法类.cpp
    本能方法类.cpp
    自我动作实现.扫描.ixx

判断：
    1. `场景类::取或创建子场景_按名称` 已有 `世界树类` 同名包装，外部应直接走 `世界树类`。
    2. `场景类::获取子存在` 可由已新增 `世界树类::获取子存在` 承接，不新增重复包装。

验证前预测：
    1. `rg -n "场景\(\)\.(取或创建子场景_按名称|获取子存在)\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 应无命中。
    2. 不新增函数，不需要生成函数说明文档。
    3. Debug x64 构建应通过。

验证结果：
    1. `rg -n "场景\(\)\.(取或创建子场景_按名称|获取子存在)\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 退出码 1，无命中。
    2. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260620_P2_5_场景子场景子存在入口收口_build.log` 显示“已成功生成”。

当前判断：
    P2.5 第十八小簇已闭合。
    场景类子场景 / 子存在剩余公开结构入口已收口。

下一步：
    继续 P2.5，审查存在子存在创建 / 查找公开入口。
```

P2.5 第十九小簇：存在子存在创建 / 查找公开入口收口

```text
范围：
    世界树类.h
    世界树类.cpp
    任务类.cpp
    方法类.cpp
    本能方法类.cpp
    自我类.cpp
    自我动作实现.外设模块.ixx
    自我线程模块.impl.cpp

判断：
    `存在类::取或创建子存在_按名称类型` / `查找子存在_按名称` / `查找子存在_按类型` 操作基础信息世界树存在子链；外部应由 `世界树类` 对外承接。

验证前预测：
    1. `rg -n "存在\(\)\.(取或创建子存在_按名称类型|查找子存在_按名称|查找子存在_按类型)\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 应无命中。
    2. 函数说明文档生成应登记三个新增 `世界树类` 函数。
    3. Debug x64 构建应通过。

验证结果：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6962。
    2. `说明书/函数功能说明文档.md` 出现 `世界树类::取或创建子存在_按名称类型`、`查找子存在_按名称`、`查找子存在_按类型` 定义条目。
    3. `rg -n "存在\(\)\.(取或创建子存在_按名称类型|查找子存在_按名称|查找子存在_按类型|获取子存在|枚举全部存在|删除存在|创建存在\()" -g "*.h" -g "*.cpp" -g "*.ixx"` 退出码 1，无命中。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260620_P2_5_存在子存在创建查找入口收口_build.log` 显示“已成功生成”。

当前判断：
    P2.5 第十九小簇已闭合。
    存在类子存在创建 / 查找公开结构入口已收口；存在类公开结构入口复查无命中。

下一步：
    继续 P2.5，复查状态 / 动态领域剩余公开结构入口。
```

P2.5 第二十小簇：场景状态集合读取公开入口收口

```text
范围：
    世界树类.h
    世界树类.cpp
    任务模块.管理工作线程.ixx
    方法类.cpp
    自我动作实现模块.ixx

判断：
    `状态类::获取场景状态` 读取场景状态索引并解析状态节点，属于基础信息世界树状态集合读取；外部应由 `世界树类` 对外承接。

验证前预测：
    1. `rg -n "状态\(\)\.获取场景状态\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 应无命中。
    2. 函数说明文档生成应登记 `世界树类::获取场景状态`。
    3. Debug x64 构建应通过。

验证结果：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6964。
    2. `说明书/函数功能说明文档.md` 出现 `世界树类::获取场景状态` 定义条目。
    3. `rg -n "状态\(\)\.获取场景状态\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 退出码 1，无命中。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260620_P2_5_场景状态集合入口收口_build.log` 显示“已成功生成”。

当前判断：
    P2.5 第二十小簇已闭合。
    场景状态集合读取公开入口已收口到 `世界树类`。

下一步：
    继续 P2.5，审查 `状态类::创建内部状态` 剩余公开入口。
```

P2.5 第十二小簇：存在子节点读取公开入口收口

```text
范围：
    世界树类.h
    世界树类.cpp
    二次特征应用模块.ixx
    控制面板类.cpp
    自我动作实现.外设模块.ixx
    自我线程模块.impl.cpp
    语言类_存在表达.cpp

判断：
    `存在类::获取子存在` 只读枚举父节点直接子存在，属于存在领域只读实现；外部读取基础信息世界树子存在应由 `世界树类` 对外承接。

验证前预测：
    1. `rg -n "存在\(\)\.获取子存在\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 应无命中。
    2. 函数说明文档生成应登记 `世界树类::获取子存在`。
    3. Debug x64 构建应通过。

验证结果：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6947。
    2. `说明书/函数功能说明文档.md` 出现 `世界树类::获取子存在` 定义条目。
    3. `rg -n "存在\(\)\.获取子存在\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 退出码 1，无命中。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260620_P2_5_存在子节点入口收口_build.log` 显示“已成功生成”。

当前判断：
    P2.5 第十二小簇已闭合。
    存在子节点读取公开入口已收口到 `世界树类`。

下一步：
    继续 P2.5，审查 `存在类::枚举全部存在` 剩余外部入口。
```

P2.5 第十三小簇：存在全量枚举公开入口收口

```text
范围：
    世界树类.h
    世界树类.cpp
    方法类.cpp

判断：
    `存在类::枚举全部存在` 内部调用 `基础信息_->枚举节点_按类型`，属于存在领域只读枚举实现；外部全量枚举基础信息世界树存在节点应由 `世界树类` 对外承接。

验证前预测：
    1. `rg -n "存在\(\)\.枚举全部存在\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 应无命中。
    2. 函数说明文档生成应登记 `世界树类::枚举全部存在`。
    3. Debug x64 构建应通过。

验证结果：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6949。
    2. `说明书/函数功能说明文档.md` 出现 `世界树类::枚举全部存在` 定义条目。
    3. `rg -n "存在\(\)\.(删除存在|枚举全部存在|创建存在|创建存在实例|按主键解析存在节点)\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 退出码 1，无命中。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260620_P2_5_存在全量枚举入口收口_build.log` 显示“已成功生成”。

当前判断：
    P2.5 第十三小簇已闭合。
    存在创建 / 删除 / 枚举 / 按主键解析公开绕过入口已清零；存在类 P2.5 小域分类完成。

下一步：
    继续 P2.5，转入下一个领域类内部 `基础信息_` 操作分类。
```

P2.5 第十一小簇：场景按主键解析 / 按名称创建公开入口收口

```text
范围：
    世界树类.h
    世界树类.cpp
    任务模块.筹办.ixx
    自我线程模块.impl.cpp

判断：
    1. `场景类::按主键解析场景节点` 内部 `基础信息_->查找主键` 属于场景领域只读解析实现；外部按主键解析场景节点应由 `世界树类` 对外承接。
    2. `场景类::创建场景_按名称` 内部最终调用 `基础信息_->添加子节点`，属于场景领域创建实现；外部按名称创建场景应由 `世界树类` 对外承接。
    3. `场景类::删除场景` / `枚举全部场景` 当前未发现外部调用，不新增未使用包装。
    4. `场景类::读取场景宿主存在` 是场景主信息引用读取，保留为领域只读函数。

验证前预测：
    1. `rg -n "场景\(\)\.(按主键解析场景节点|创建场景_按名称)\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 应无命中。
    2. 函数说明文档生成应登记 `世界树类::按主键解析场景节点` 和 `世界树类::创建场景_按名称`。
    3. Debug x64 构建应通过。

验证结果：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6945。
    2. `说明书/函数功能说明文档.md` 出现 `世界树类::按主键解析场景节点` 和 `世界树类::创建场景_按名称` 定义条目。
    3. `rg -n "场景\(\)\.(按主键解析场景节点|创建场景_按名称)\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 退出码 1，无命中。
    4. `rg -n "场景\(\)\.(删除场景|枚举全部场景)\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 退出码 1，无命中。
    5. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260620_P2_5_场景解析创建入口收口_build.log` 显示“已成功生成”。

当前判断：
    P2.5 第十一小簇已闭合。
    场景类内部 `基础信息_->查找主键 / 添加子节点 / 删除节点 / 枚举节点_按类型` 保留为领域服务实现；外部场景解析 / 创建公开入口已收口到 `世界树类`；场景类 P2.5 小域分类完成。

下一步：
    继续 P2.5，转入下一个领域类内部 `基础信息_` 操作分类。
```

第八回迁簇：

```text
范围：
    本能动作类.ixx:64-96 本地基础信息子节点枚举 helper。
    本能动作类.ixx:104 OR 组子节点判断。
    本能动作类.ixx:169-180 条件场景直接子节点查重。
    本能动作类.ixx:380 递归匹配叶子节点判断。

预测：
    1. 本能动作模板 / 条件场景子链读取改用 `世界树.获取子节点` 后，
       `本能动作类.ixx` 不再出现用于基础信息世界树读取的 `->子` / `->下` 直接遍历。
    2. `基础信息.添加子节点` 仍保留为后续结构创建入口收口问题，
       不作为本 P2.4 父 / 子 / 下 链读取回迁验收项。
    3. Debug x64 构建应通过。
```

第八回迁簇验证结果：

```text
已改文件：
    本能动作类.ixx

验证结果：
    1. `rg -n "父节点->子|当前->下|条件场景->子|模式节点->子|static_cast<基础信息节点类\*>\(当前->下\)|static_cast<const 基础信息节点类\*>\(当前->下\)|基础信息\.获取读锁\(\)" 本能动作类.ixx` 退出码 1，无命中。
    2. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0，构建日志显示“已成功生成”。
    3. 更新扫描 `日志/20260620_P2_4_父子链扫描_更新4.txt` 退出码 0，总命中 882 行。
```

后续状态：

```text
P2.4 未整体完成。
下一步继续父 / 子 / 下 链分类。
```

第九回迁簇：

```text
范围：
    自检线程模块.impl.cpp:94-111 本地按类型直接基础信息子节点 helper。
    自我线程模块.impl.cpp:60-77 本地按类型直接基础信息子节点 helper。
    语言类_存在表达.cpp:47-63 本地直接基础信息子节点 helper。

预测：
    1. 三处外部模块本地基础信息子节点 helper 改用 `世界树.获取子节点` 后，
       对应 `父->子` / `当前->下` / `基础信息.获取读锁` 命中清零。
    2. 自检 / 自我线程中方法树、需求树独立树的 `->父` / `->子` / `->下` 遍历不属于本回迁簇，不改动。
    3. Debug x64 构建应通过。
```

第九回迁簇验证结果：

```text
已改文件：
    自检线程模块.impl.cpp
    自我线程模块.impl.cpp
    语言类_存在表达.cpp

验证结果：
    1. `rg -n "基础信息\.获取读锁\(\)|基础信息\.世界根\(\)|static_cast<基础信息节点类\*>\(父->子\)|static_cast<基础信息节点类\*>\(当前->下\)" 自检线程模块.impl.cpp 自我线程模块.impl.cpp 语言类_存在表达.cpp` 退出码 1，无命中。
    2. `rg -n "父->子|当前->下" 语言类_存在表达.cpp` 退出码 1，无命中。
    3. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0，构建日志显示“已成功生成”。
    4. 更新扫描 `日志/20260620_P2_4_父子链扫描_更新5.txt` 退出码 0，总命中 873 行。
```

后续状态：

```text
P2.4 未整体完成。
下一步继续父 / 子 / 下 链分类。
```

第十回迁簇：

```text
范围：
    控制面板类.cpp:2630-2648 `私有_枚举子节点` 基础信息节点专用分支。
    控制面板类.cpp:2652-2670 `私有_遍历全部子节点` 基础信息节点专用分支。
    控制面板类.cpp:2779-2799 `私有_计数子树节点` 基础信息节点专用分支。

预测：
    1. 控制面板基础信息世界树骨架、基础信息子结构统计和延迟展开路径经 `世界树.获取子节点` 读取。
    2. 控制面板中需求 / 任务 / 方法独立树仍保留原直接链表遍历，不纳入本回迁簇。
    3. Debug x64 构建应通过。
```

第十回迁簇验证结果：

```text
已改文件：
    控制面板类.cpp

验证结果：
    1. `控制面板类.cpp` 的 `私有_枚举子节点` / `私有_遍历全部子节点` / `私有_计数子树节点` 均新增 `基础信息节点类` 专用分支，调用 `世界树.获取子节点`。
    2. Debug x64 构建退出码 0，构建日志显示“已成功生成”。
    3. 更新扫描 `日志/20260620_P2_4_父子链扫描_更新6.txt` 退出码 0，总命中 873 行；行数未下降的原因是非基础信息分支仍保留需求 / 任务 / 方法独立树直接链表遍历。
```

后续状态：

```text
P2.4 未整体完成。
下一步继续父 / 子 / 下 链分类，重点排除已确认的独立树与领域类内部服务命中。
```

P2.4 收口分类：

```text
依据：
    日志/20260620_P2_4_父子链扫描_更新6.txt
    关键剩余模式复查：
        基础信息节点类.*->父 / ->子 / ->下
        世界树.基础信息().枚举全部节点
        基础信息.添加子节点

分类结论：
    1. `世界树类.cpp`、`基础信息类.cpp`、`双向链表模板核心.h` 属于正式收口层或底层容器，不作为外部回迁对象。
    2. `二次特征类.cpp`、`场景类.cpp`、`存在类.cpp`、`特征类.cpp`、`状态类.cpp`、`动态类.cpp`、`需求类.cpp` 中的基础信息父子链和 `基础信息_` 操作转入 P2.5 领域类内部服务分类。
    3. 任务 / 需求 / 方法独立树、控制面板 UI `子项` 字段和显示层独立树骨架不属于 P2.4 外部基础信息世界树读取回迁。
    4. `世界树.基础信息().枚举全部节点` 与 `基础信息.添加子节点` 是后续基础信息访问器 / 创建入口收口问题，不属于 P2.4 父 / 子 / 下 链读取。

当前判断：
    P2.4 外部父 / 子 / 下 链读取回迁当前无新的可执行外部簇。
    下一步转入 P2.5：领域类内部 `基础信息_` 直接结构操作分类。
```

P2.5 第一小簇：状态创建公开入口收口

```text
范围：
    状态类.cpp 内部 `基础信息_` 创建操作先保留为状态领域主信息维护服务。
    外部 `世界树.状态().创建状态(场景, 主体, 特征, 值, 时间...)` 调用已有明确世界树结构创建语义，且缺少同签名 `世界树类` 包装。

预测：
    1. 新增 `世界树类::创建状态(场景, 主体, 特征, 状态值, 发生时间, 收到时间)` 只委托 `状态服务_.创建状态`，不新增状态字段，不隐式创建其它结构。
    2. 外部 11 处 `世界树.状态().创建状态(` 会迁到 `世界树.创建状态(`。
    3. `状态类.cpp` 内部 `基础信息_->添加子节点` 保留为领域服务实现，不在本小簇删除。
    4. 函数说明文档需更新，Debug x64 构建应通过。
```

P2.5 第一小簇验证结果：

```text
已改文件：
    世界树类.h
    世界树类.cpp
    任务模块.筹办.ixx
    自我动作实现.服务模块.ixx
    自我类.cpp
    自我动作实现模块.ixx
    自我线程模块.impl.cpp
    说明书/函数功能说明文档.md
    实施记录/20260620_世界树类逻辑纠偏_Codex断点清单.md

验证结果：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6933。
    2. `rg -n "世界树\.状态\(\)\.创建状态\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 退出码 1，无命中。
    3. `说明书/函数功能说明文档.md` 出现两条 `世界树类::创建状态` 定义：主信息指针重载和主体 / 特征 / 值重载。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260620_P2_5_状态创建入口收口_build.log`。

当前判断：
    P2.5 第一小簇已闭合。
    状态类内部 `基础信息_->添加子节点` 仍归类为领域服务实现；外部状态创建结构入口已收口到 `世界树类`。

下一步：
    继续 P2.5，优先审查 `状态类` 剩余 `按主键解析状态节点` / `删除状态` / `枚举全部状态` / `统计状态引用数量` 是否已有或缺少世界树公开包装。
```

P2.5 第二小簇：状态按主键解析公开入口收口

```text
范围：
    状态类.cpp 内部 `基础信息_->查找主键` 保留为状态领域只读解析实现。
    外部 `世界树.状态().按主键解析状态节点(...)` 迁到 `世界树类` 公开包装。

预测：
    1. 新增 `世界树类::按主键解析状态节点` 只委托 `状态服务_.按主键解析状态节点`，不创建或修改节点。
    2. 外部 9 处 `世界树.状态().按主键解析状态节点(` 会迁到 `世界树.按主键解析状态节点(`。
    3. 直接构造 `状态类 状态服务(base)` 的领域内部读取不纳入本小簇，后续按跨领域服务边界单独分类。
    4. 函数说明文档需更新，Debug x64 构建应通过。
```

P2.5 第二小簇验证结果：

```text
已改文件：
    世界树类.h
    世界树类.cpp
    任务模块.筹办.ixx
    任务类.cpp
    任务模块.管理界面线程.impl.cpp
    方法类.cpp
    自我线程模块.impl.cpp
    需求类.cpp
    鱼巢.cpp
    说明书/函数功能说明文档.md
    实施记录/20260620_世界树类逻辑纠偏_Codex断点清单.md

验证结果：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6934。
    2. `rg -n "世界树\.状态\(\)\.按主键解析状态节点\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 退出码 1，无命中。
    3. `说明书/函数功能说明文档.md` 出现 `世界树类::按主键解析状态节点` 定义。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260620_P2_5_状态按主键解析入口收口_build.log`。

当前判断：
    P2.5 第二小簇已闭合。
    状态类内部 `基础信息_->查找主键` 仍归类为领域只读解析实现；外部状态按主键解析入口已收口到 `世界树类`。

下一步：
    继续 P2.5，审查 `状态类::删除状态` / `枚举全部状态` / `统计状态引用数量` 是否存在外部绕过调用或缺少世界树公开包装。
```

P2.5 第三小簇：状态剩余删除 / 枚举 / 引用统计分类

```text
范围：
    状态类::删除状态
    状态类::枚举全部状态
    状态类::统计状态引用数量
    状态类::删除状态 内部 `节点->父`

复查命令：
    rg -n "世界树\.状态\(\)\.(删除状态|枚举全部状态|统计状态引用数量)\(|\.删除状态\(|\.枚举全部状态\(|\.统计状态引用数量\(" -g "*.h" -g "*.cpp" -g "*.ixx"
    rg -n "删除状态\(|枚举全部状态\(|统计状态引用数量\(" 状态类.h 状态类.cpp 世界树类.h 世界树类.cpp 说明书\函数功能说明文档.md

验证结果：
    1. 外部调用复查退出码 1；未发现 `世界树.状态().删除状态` / `枚举全部状态` / `统计状态引用数量` 或其它外部点式调用。
    2. 函数定义只存在于 `状态类.h/.cpp`；`统计状态引用数量` 仅由 `状态类::按活动状态限量清理` 内部调用。
    3. `状态类::删除状态` 的 `节点->父` 仅用于删除前同步场景状态索引，属于状态领域服务内部主信息维护边界。

当前判断：
    状态类 P2.5 小域分类完成。
    不新增未使用的世界树公开删除 / 枚举 / 引用统计包装。

下一步：
    继续 P2.5，转入下一个领域类内部 `基础信息_` 操作分类；优先从 `动态类.cpp` 或 `二次特征类.cpp` 选择一个最小可闭合簇。
```

P2.5 第四小簇：动态按主键解析公开入口收口

```text
范围：
    动态类.cpp 内部 `基础信息_->查找主键` 保留为动态领域只读解析实现。
    外部 `世界树->动态().按主键解析动态节点(...)` 迁到 `世界树类` 公开包装。

预测：
    1. 新增 `世界树类::按主键解析动态节点` 只委托 `动态服务_.按主键解析动态节点`，不创建或修改节点。
    2. 外部 1 处 `世界树->动态().按主键解析动态节点(` 会迁到 `世界树->按主键解析动态节点(`。
    3. 直接构造 `动态类 动态服务(base)` 的领域内部读取不纳入本小簇，后续按跨领域服务边界单独分类。
    4. 函数说明文档需更新，Debug x64 构建应通过。
```

P2.5 第四小簇验证结果：

```text
已改文件：
    世界树类.h
    世界树类.cpp
    二次特征应用模块.ixx
    说明书/函数功能说明文档.md
    实施记录/20260620_世界树类逻辑纠偏_Codex断点清单.md

验证结果：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6935。
    2. `rg -n "世界树(\.|->)动态\(\)\.按主键解析动态节点\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 退出码 1，无命中。
    3. `说明书/函数功能说明文档.md` 出现 `世界树类::按主键解析动态节点` 定义。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260620_P2_5_动态按主键解析入口收口_build.log`。

当前判断：
    P2.5 第四小簇已闭合。
    动态类内部 `基础信息_->查找主键` 仍归类为领域只读解析实现；外部动态按主键解析入口已收口到 `世界树类`。

下一步：
    继续 P2.5，审查 `动态类::创建动态` / `删除动态` / `枚举全部动态` 是否存在外部绕过调用或缺少世界树公开包装。
```

P2.5 第五小簇：动态状态对创建公开入口收口

```text
范围：
    动态类.cpp 内部 `基础信息_->添加子节点` 保留为动态领域创建实现。
    外部 `世界树.动态().创建动态(场景, 初始状态, 结果状态, 起, 止)` 迁到 `世界树类` 公开包装。

预测：
    1. 新增 `世界树类::创建动态(场景, 初始状态, 结果状态, 起, 止)` 只委托 `动态服务_.创建动态`，不新增动态字段，不改变后处理逻辑。
    2. 外部 5 处状态对动态创建调用会迁到 `世界树.创建动态(`。
    3. `动态类::创建动态(场景, 动态节点主信息类*, 后处理选项)` 与方法动作 / 存在 / 场景动态专用创建不纳入本小簇。
    4. 函数说明文档需更新，Debug x64 构建应通过。
```

P2.5 第五小簇验证结果：

```text
已改文件：
    世界树类.h
    世界树类.cpp
    方法虚拟存在服务类.cpp
    自我类.cpp
    自我线程模块.impl.cpp
    说明书/函数功能说明文档.md
    实施记录/20260620_世界树类逻辑纠偏_Codex断点清单.md

验证结果：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6936。
    2. `rg -n "世界树\.动态\(\)\.创建动态\(" 方法虚拟存在服务类.cpp 自我类.cpp 自我线程模块.impl.cpp` 退出码 1，无命中。
    3. `说明书/函数功能说明文档.md` 出现两条 `世界树类::创建动态` 定义：主信息指针重载和状态对重载。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260620_P2_5_动态状态对创建入口收口_build.log`。

当前判断：
    P2.5 第五小簇已闭合。
    动态类内部 `基础信息_->添加子节点` 仍归类为领域创建实现；外部状态对动态创建入口已收口到 `世界树类`。

下一步：
    继续 P2.5，审查 `动态类::删除动态` / `枚举全部动态` 以及主信息后处理重载是否存在外部绕过调用。
```

P2.5 第六小簇：动态主信息后处理创建公开入口收口

```text
范围：
    动态类.cpp 内部 `基础信息_->添加子节点` 保留为动态领域创建实现。
    外部 `世界树_->动态().创建动态(场景, 动态节点主信息类*, 动态创建后处理选项)` 迁到 `世界树类` 公开包装。

预测：
    1. 新增 `世界树类::创建动态(场景, 动态节点主信息类*, 动态创建后处理选项)` 只委托 `动态服务_.创建动态`，不新增动态字段，不改变后处理语义。
    2. 外部 2 处主信息后处理动态创建调用会迁到 `世界树_->创建动态(`。
    3. `动态类` 方法动作 / 存在 / 场景动态专用创建不纳入本小簇。
    4. 函数说明文档需更新，Debug x64 构建应通过。
```

P2.5 第六小簇验证结果：

```text
已改文件：
    世界树类.h
    世界树类.cpp
    二次特征应用模块.ixx
    说明书/函数功能说明文档.md
    实施记录/20260620_世界树类逻辑纠偏_Codex断点清单.md

验证结果：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6938。
    2. `rg -n "动态\(\)\.创建动态\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 退出码 1，无命中。
    3. `说明书/函数功能说明文档.md` 出现三条 `世界树类::创建动态` 定义：主信息指针重载、主信息后处理重载和状态对重载。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260620_P2_5_动态主信息后处理创建入口收口_build.log`。

当前判断：
    P2.5 第六小簇已闭合。
    动态类内部 `基础信息_->添加子节点` 仍归类为领域创建实现；外部动态主信息后处理创建入口已收口到 `世界树类`。

下一步：
    继续 P2.5，审查 `动态类::删除动态` / `枚举全部动态` 是否存在外部绕过调用。
```

P2.5 第七小簇：动态删除 / 枚举剩余入口分类

```text
范围：
    动态类::删除动态
    动态类::枚举全部动态
    因果类.cpp 中 `动态类 动态服务(base)` 后调用 `枚举全部动态`

复查命令：
    rg -n "动态\(\)\.(删除动态|枚举全部动态)\(|\.删除动态\(|\.枚举全部动态\(" -g "*.h" -g "*.cpp" -g "*.ixx"
    rg -n "动态类 动态服务|动态服务\.枚举全部动态\(|动态服务\.删除动态\(" -g "*.h" -g "*.cpp" -g "*.ixx"
    rg -n "世界树\.|世界树->|世界树_" 因果类.cpp 因果类.h

验证结果：
    1. `动态类::删除动态` 未发现外部调用。
    2. `动态类::枚举全部动态` 只剩 `因果类.cpp` 中 `动态类 动态服务(base)` 后调用。
    3. `因果类.cpp/.h` 未发现直接使用全局 `世界树`；因果类按自身 `基础信息_` / `获取基础信息集()` 选择基础信息实例。

当前判断：
    动态类 P2.5 小域分类完成。
    不新增未使用的世界树公开删除 / 枚举包装。
    不把 `因果类.cpp` 的 `动态类 动态服务(base)` 改为全局 `世界树`，避免改变基础信息实例边界。
    该点登记为后续跨领域服务边界分类，不阻塞当前世界树 P2.5。

下一步：
    继续 P2.5，转入 `二次特征类.cpp` 内部 `基础信息_` 操作分类。
```

P2.5 第八小簇：二次特征按主键解析公开入口收口

```text
范围：
    二次特征类.cpp 内部 `基础信息_->查找主键` 保留为二次特征领域只读解析实现。
    外部 `世界树.二次特征().按主键解析二次特征节点(...)` 迁到 `世界树类` 公开包装。

预测：
    1. 新增 `世界树类::按主键解析二次特征节点` 只委托 `二次特征服务_.按主键解析二次特征节点`，不创建或修改节点。
    2. 外部 1 处 `世界树.二次特征().按主键解析二次特征节点(` 会迁到 `世界树.按主键解析二次特征节点(`。
    3. 直接构造 `二次特征类 二次特征服务(base)` 的领域内部读取不纳入本小簇，后续按跨领域服务边界单独分类。
    4. 函数说明文档需更新，Debug x64 构建应通过。
```

P2.5 第八小簇验证结果：

```text
已改文件：
    世界树类.h
    世界树类.cpp
    方法类.cpp
    说明书/函数功能说明文档.md
    实施记录/20260620_世界树类逻辑纠偏_Codex断点清单.md

验证结果：
    1. `python .\tools\生成函数功能说明文档.py` 退出码 0，函数条目 6939。
    2. `rg -n "世界树(\.|->)二次特征\(\)\.按主键解析二次特征节点\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 退出码 1，无命中。
    3. `说明书/函数功能说明文档.md` 出现 `世界树类::按主键解析二次特征节点` 定义。
    4. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0；日志 `日志/20260620_P2_5_二次特征按主键解析入口收口_build.log`。

当前判断：
    P2.5 第八小簇已闭合。
    二次特征类内部 `基础信息_->查找主键` 仍归类为领域只读解析实现；外部二次特征按主键解析入口已收口到 `世界树类`。

下一步：
    继续 P2.5，审查 `二次特征类::创建二次特征` / `删除二次特征` / `枚举全部二次特征` 以及子二次特征读取 / 创建入口。
```

第七回迁簇：

```text
范围：
    自我动作实现模块.ixx:6643-6658
    自我动作实现模块.ixx:6687-6701
    自我动作实现模块.ixx:6734-6776
    自我动作实现模块.ixx:6790-6804
    自我动作实现模块.ixx:6914-6929

判断：
    形参表、候选实参表和源包都是基础信息节点集合；这些代码只读遍历直接子节点。
    方法类节点子链仍属于方法独立树，暂不处理。

修改：
    复用 `世界树类::获取子节点`。
```

验证前预测：

```text
1. `自我动作实现模块.ixx` 中 `形参表->子`、`候选实参表->子`、`源包->子` 相关基础信息子链遍历应消失。
2. Debug x64 构建应通过。
```

第七回迁簇验证结果：

```text
已改文件：
    自我动作实现模块.ixx

验证结果：
    1. `rg -n "形参表->子|候选实参表->子|源包->子|static_cast<基础信息节点类\*>\(当前->下\)" 自我动作实现模块.ixx` 退出码 1，无命中。
    2. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0，构建日志显示“已成功生成”。
```

后续状态：

```text
P2.4 未整体完成。
下一步继续父 / 子 / 下 链分类。
```

第六回迁簇：

```text
范围：
    自我动作实现模块.ixx:6368-6374
    自我动作实现模块.ixx:6414-6416

判断：
    `子节点数量` / `离散集合第N项` 直接遍历基础信息集合子链，属于基础信息世界树子链读取。
    同文件方法类节点子链、需求类父链、形参表批量遍历暂不混入本小簇。

修改：
    复用 `世界树类::获取子节点`。
```

验证前预测：

```text
1. `rg -n "集合->子|当前->下" 自我动作实现模块.ixx` 仍会命中其它未处理簇，但不应再命中 `子节点数量` / `离散集合第N项` 的集合子链直接读取。
2. Debug x64 构建应通过。
```

第六回迁簇验证结果：

```text
已改文件：
    自我动作实现模块.ixx

验证结果：
    1. `子节点数量` / `取离散候选节点` 已改用 `世界树.获取子节点`。
    2. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0，构建日志显示“已成功生成”。
    3. `自我动作实现模块.ixx` 剩余 `当前->下` 命中属于方法树或后续形参表 / 实参表簇。
```

后续状态：

```text
P2.4 未整体完成。
下一步继续父 / 子 / 下 链分类。
```

第五回迁簇：

```text
范围：
    自我线程模块.impl.cpp:3638
    自我线程模块.impl.cpp:4037

判断：
    两处均从基础信息节点读取父层宿主，用于风险安全层聚合 / 兄弟入账评分，属于基础信息世界树父链读取。
    同文件其它大量需求 / 任务父链命中属于独立树，跳过。

修改：
    复用 `世界树类::获取父节点`。
```

验证前预测：

```text
1. `rg -n "因素宿主->父|当前->父" 自我线程模块.impl.cpp` 不再命中这两处基础信息父链。
2. Debug x64 构建应通过。
```

第五回迁簇验证结果：

```text
已改文件：
    自我线程模块.impl.cpp

验证结果：
    1. `rg -n "因素宿主->父|static_cast<基础信息节点类\*>\(当前->父\)" 自我线程模块.impl.cpp` 退出码 1，无命中。
    2. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0，构建日志显示“已成功生成”。
```

后续状态：

```text
P2.4 未整体完成。
下一步继续父 / 子 / 下 链分类。
```

第四回迁簇：

```text
范围：
    自我动作实现.外设模块.ixx:13633、13638

判断：
    `取存在所在场景` 沿存在节点父链向上查找所属场景，属于基础信息世界树父链读取。
    同文件 8770 附近为方法树父链，属于独立方法树，跳过。

修改：
    复用 `世界树类::获取父节点`。
```

验证前预测：

```text
1. `rg -n "节点->父|当前->父" 自我动作实现.外设模块.ixx` 仍可能命中方法树父链，但不应再命中 `取存在所在场景` 的基础信息父链。
2. Debug x64 构建应通过。
```

第四回迁簇验证结果：

```text
已改文件：
    自我动作实现.外设模块.ixx

验证结果：
    1. `rg -n "节点 \? static_cast<基础信息节点类\*>\(节点->父\)|static_cast<基础信息节点类\*>\(当前->父\)" 自我动作实现.外设模块.ixx` 退出码 1，无命中。
    2. `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 退出码 0，构建日志显示“已成功生成”。
```

后续状态：

```text
P2.4 未整体完成。
下一步继续父 / 子 / 下 链分类。
```
