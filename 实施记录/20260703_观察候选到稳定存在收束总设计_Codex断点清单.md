# 20260703 观察候选到稳定存在收束总设计 Codex断点清单

## 0. 恢复摘要

当前切片：根据用户“执行”要求，补齐 `观察候选到稳定存在收束总设计` 与对应实现映射计划，登记到计划索引，并继续执行 P11 S0 全局只读扫描。

本切片性质：

```text
文档治理 / 设计与计划登记 / S0 只读代码事实扫描；
不修改 C++ 代码；
不执行构建；
不声明任何代码能力已实现。
```

## 1. 依据文件

```text
docs/00_文档总索引.md
规范/规范目录.md
规范/0050_项目通用结构与禁止性规则总纲_20260701.md
规范/1175_根规范_特征表达与判断收束规则_20260703.md
计划/计划索引.md
docs/current-state.md
```

相关已生成桥接设计：

```text
规范/详细设计/待完成/20260703_像素归属候选分裂与局部体素证据详细设计_v0.1.md
规范/详细设计/待完成/20260703_结构化信息缺口与需求派生承接详细设计_v0.1.md
规范/详细设计/待完成/20260703_场景体素服务增强与快照生成详细设计_v0.1.md
规范/详细设计/待完成/20260703_外设观察先验发布器详细设计_v0.1.md
规范/详细设计/待完成/20260703_四方法共享底层关联判断能力详细设计_v0.1.md
规范/详细设计/待完成/20260703_视觉清晰度与稳定复现评价详细设计_v0.1.md
```

## 2. 计划索引锁

已创建：

```text
计划/.计划索引.lock
owner=codex
target=观察候选到稳定存在收束总设计
action=register P11
```

## 3. 当前判断

当前 P3-P10 都是局部桥接实现映射计划，均要求从 S0 只读扫描开始。若直接逐项执行，容易在 `像素簇 -> C`、`C -> E`、`SceneVoxelPrior 解释偏置`、`G_gap 回流` 上重复造判断。

本轮先补一份总收束设计和总实现映射计划，作为后续 P3-P10 进入代码前的统一路线。

S0 只读扫描后的补充判断：

```text
1. D455 和外设观察报告队列已经有像素簇、空间候选、质量评分、跨帧稳定和材料句柄。
2. 视觉观察融合模块已经有候选、先验评分、自由空间冲突候选和信息缺口输出。
3. 场景体素模块已经有 SceneVoxelPrior 只读快照和 AABB / 射线粗查询边界。
4. 任务模块已有派生需求消息、字段完整性、稳定键和入树拒绝闭环骨架。
5. 观察写入路径仍存在旧判断点，尤其是 `被动识别可进入观察写入` 可触发观察存在发现 / 特征更新写入。
6. S1 不应先做 C -> E 写入，而应先给旧判断点加语义收束闸门和缺口分流。
```

## 4. 待完成动作

```text
1. 已新增 规范/详细设计/待完成/20260703_观察候选到稳定存在收束总设计_v0.1.md。
2. 已新增 计划/20260703_观察候选到稳定存在收束实现映射计划_v0.1.md。
3. 已更新 计划/计划索引.md，登记 P11。
4. 已更新 docs/current-state.md。
5. 已更新 docs/decision-log.md。
6. 已运行 python .\tools\check_specs.py。
7. 已运行 git diff --check。
8. 已提交本切片相关文件；不纳入既有未跟踪实施记录文件。
9. 已继续执行 P11 S0 全局只读扫描。
```

## 4.1 S0 旧判断点清单

```text
1. `外设观察报告队列.ixx` 的 `判断外设观察材料质量`：
   - 使用 `可进入验证方法`、`可进入识别方法`、`可生成扫描变化候选`、`可生成跟踪动态候选`、`外设材料可承接状态`、`可交付观察方法材料` 等 bool 型判断结果。
   - 使用跨帧稳定、平面轮廓稳定、深度锚定、空间可用等内部 bool 组合。

2. `双目相机本能适配器.cpp`：
   - D455 端已生成深度有效率、空间坐标有效率、轮廓置信度、未解释像素数、缺失原因、补观察建议、帧质量评分。
   - 仍有 `像素归属验证状态`、`低置信轮廓状态`、`补观察缺口状态` 等判断型状态，需要作为材料或收束结果使用，不得升级为一阶事实。

3. `自我动作实现.外设模块.ixx`：
   - `被动识别外设观察材料_内部治理` 读取质量判定并生成同一性 / 归属状态。
   - `自我_观察单个空间候选并组合存在假设` 中 `被动识别可进入观察写入` 为旧事实写入闸门。
   - `写入观察识别归属结果` 可新建 / 更新观察存在数量，是 P11 第一优先风险入口。

4. `自我动作实现.扫描.ixx`：
   - 扫描数据项有 `可生成扫描变化候选`、`空间中心有效`、`空间范围有效`、`可回查`、`可比较` 等 bool。
   - 文件头边界已禁止绕过合法外设材料层或提交入口写世界树事实。

5. `视觉观察融合模块.ixx`：
   - `融合观察域结果` 输出候选、先验评分、自由空间冲突候选和信息缺口。
   - 体素加工线程只保存候选结果包，但仍有 `是否存在融合点`、`可提交状态` 等待后续收束。
```

## 4.2 S0 可复用入口清单

```text
1. D455 调用结果：
   - 空间候选摘要、帧质量评分、深度有效率、空间坐标有效率、轮廓置信度、缺失原因、待补观察区域数量。

2. 外设观察报告队列：
   - `结构_外设观察像素簇摘要`
   - `结构_外设观察报告队列项`
   - `判断外设观察材料质量`
   - 识别 / 扫描 / 跟踪提交包构造函数
   - 像素归属账本句柄、空间候选队列句柄、D455短期材料页。

3. 视觉观察融合模块：
   - `结构_视觉观察域结果包`
   - `结构_视觉观察候选`
   - `结构_视觉先验匹配评分`
   - `结构_视觉自由空间冲突候选`
   - `结构_视觉信息缺口`
   - `融合观察域结果`
   - `生成子轮廓候选集合`

4. 场景体素模块：
   - `结构_SceneVoxelPrior`
   - `读取最新快照`
   - `查询AABB`
   - `射线粗查询`
   - 场景体素缺口集合。

5. 任务模块：
   - `结构_任务管理派生需求消息`
   - 派生需求字段完整性检查
   - 稳定键和去重
   - 入树拒绝回执。
```

## 4.3 S0 缺函数 / 缺入口清单

```text
1. 缺统一的像素簇 -> 临时 C / 稳定 C 收束函数。
2. 缺统一的 C -> E 同一性候选评分和排他性冲突收束函数。
3. 缺 SceneVoxelPrior 到 ROI / 分裂 / 遮挡 / 冲突解释偏置的发布器落地入口。
4. 缺 G_gap 标准化映射函数，把外设、视觉、体素和写入阻断统一接入 D5 缺口链。
5. 缺稳定 C / 局部体素进入稳定体素融合前置闸门。
6. 缺把 `被动识别可进入观察写入` 前置到 P11 语义收束结果的最小代码入口。
```

## 4.4 S0 bool / 判断型枚举风险清单

```text
高风险：
    `被动识别可进入观察写入`
    `可交付观察方法材料`
    `可进入识别方法`
    `外设材料可承接状态`
    `外设观察存在_自我存在同一性状态`
    `外设观察存在自我归属状态`

中风险：
    `可生成扫描变化候选`
    `可生成跟踪动态候选`
    `平面轮廓稳定状态`
    `深度锚定状态`
    `空间可用状态`
    `像素归属验证状态`
    `补观察缺口状态`
    `低置信轮廓状态`

允许作为内部临时变量但不得新增为一阶事实：
    `空间中心有效`
    `空间范围有效`
    `可回查`
    `可比较`
    `是否存在融合点`
    `有局部子体素候选`
```

## 4.5 S0 推荐第一代码切片

```text
切片目标：
    P11-S1a：外设材料质量判定到观察写入路径的语义收束闸门。

允许修改范围：
    外设观察报告队列.ixx
    自我动作实现.外设模块.ixx
    说明书/函数功能说明文档.md（若新增 / 改名函数）
    实施记录/20260703_观察候选到稳定存在收束总设计_Codex断点清单.md

首选做法：
    保留旧入口；
    新增或复用只读收束函数；
    把 `判断外设观察材料质量` 输出转为评分、计数、缺口原因和状态值；
    让 `被动识别可进入观察写入` 读取收束结果；
    不在本切片实现 C -> E 写入；
    冲突 / 证据不足转缺口材料。

验证方式：
    check_specs；
    Debug x64 构建；
    若无 D455 硬件 / 自然样本，不伪造运行证据，只记录缺条件。
```

## 4.6 S1a 代码切片记录

```text
切片目标：
    外设材料质量判定到观察写入路径的语义收束闸门。

已修改文件：
    自我动作实现.外设模块.ixx
    说明书/函数功能说明文档.md
    实施记录/20260703_观察候选到稳定存在收束总设计_Codex断点清单.md

代码变更：
    1. 新增内部只读函数 `被动识别结果满足观察写入收束闸门`。
    2. 闸门集中检查：已运行、材料可交付、上游验证已通过、候选数量、可提交数量、冲突数量、证据不足数量、同一性状态、归属状态、新建 / 合并数量。
    3. `写入观察识别归属结果` 改为复用该闸门。
    4. `自我_观察单个空间候选并组合存在假设` 中的 `被动识别可进入观察写入` 改为复用该闸门。

没有做：
    未实现像素簇 -> C。
    未实现 C -> E 同一性候选评分。
    未实现 SceneVoxelPrior 解释偏置发布。
    未实现 G_gap 标准化映射。
    未让局部体素更新稳定体素。

验证预测：
    函数说明文档可生成；
    check_specs hard issues = 0，warnings = 0；
    Debug x64 构建应通过；
    本切片无真实 D455 自然样本运行验证，不伪造业务通过证据。
```

## 4.7 S1b 代码切片记录

```text
切片目标：
    把观察写入收束闸门的阻断原因从内部 bool 判断推进为可回查 I64 掩码材料。

已修改文件：
    自我动作实现.外设模块.ixx
    说明书/函数功能说明文档.md
    计划/计划索引.md
    docs/current-state.md
    实施记录/20260703_观察候选到稳定存在收束总设计_Codex断点清单.md

代码变更：
    1. 新增内部 I64 掩码常量：未运行、缺可交付材料、上游未验证、缺候选簇、缺可提交项、存在冲突、证据不足、同一性不成立、归属不成立、缺新建或合并数量。
    2. 新增 `被动识别观察写入闸门阻断原因掩码`，集中收束上述阻断原因。
    3. `被动识别结果满足观察写入收束闸门` 改为复用阻断原因掩码归零判定。
    4. `写入被动识别外设观察材料摘要` 与 `写入观察内部治理记录` 写入既有 `特征_映射缺口原因掩码()`。

没有做：
    未新增长期业务结构体。
    未新增普通一阶 bool 特征。
    未实现 D5 缺口标准化完整链。
    未推进 C -> E 写入或稳定体素更新。

验证预测：
    函数说明文档可生成；
    check_specs hard issues = 0，warnings = 0；
    Debug x64 构建应通过；
    本切片无真实 D455 自然样本运行验证，不伪造业务通过证据。
```

## 4.8 S2 代码切片预期

```text
切片目标：
    在 S1b 观察写入闸门阻断原因掩码中，继续纳入质量 / 清晰度 / 稳定复现阻断原因。

最小实现边界：
    1. 复用 `结构_外设观察存在确认方案` 和 `结构_外设观察存在确认方案项`。
    2. 复用候选簇中的深度有效率、空间坐标有效率、轮廓置信度、跨帧匹配状态、跨帧连续命中次数、跨帧复现离散度。
    3. 只新增内部 I64 掩码位和普通只读收束函数。
    4. 不新增业务结构体，不新增普通一阶 bool 特征，不写世界树稳定存在事实。

验证预测：
    函数说明文档可生成；
    Debug x64 构建应通过；
    check_specs hard issues = 0，warnings = 0；
    git diff --check 通过；
    本切片无真实 D455 自然样本运行验证，不伪造业务通过证据。
```

## 4.9 S2 代码切片记录

```text
已修改文件：
    自我动作实现.外设模块.ixx
    说明书/函数功能说明文档.md
    计划/计划索引.md
    docs/current-state.md
    实施记录/20260703_观察候选到稳定存在收束总设计_Codex断点清单.md

代码变更：
    1. 新增 `观察写入闸门阻断_清晰度不足` 和 `观察写入闸门阻断_稳定复现不足` 两个 I64 阻断位。
    2. 新增 `被动识别确认方案存在清晰度阻断`，复用确认方案项证据不足原因、候选簇深度有效率、空间坐标有效率和轮廓置信度。
    3. 新增 `被动识别确认方案存在稳定复现阻断`，复用跨帧匹配状态、跨帧连续命中次数和缺跨帧同一性证据原因。
    4. `被动识别观察写入闸门阻断原因掩码` 继续输出同一个 `特征_映射缺口原因掩码()` 可承接材料。

没有做：
    未新增业务结构体。
    未新增普通一阶 bool 特征。
    未实现完整 D9 清晰度评价包。
    未实现像素簇 -> C 的完整生成规则。
    未推进 C -> E 写入或稳定体素更新。
```

## 6. 验证结果

```text
python .\tools\check_specs.py
hard issues: 0
warnings: 0

git diff --check
通过

S0 证据更新后再次验证：

python .\tools\check_specs.py
hard issues: 0
warnings: 0

git diff --check
通过

S1a 代码切片静态文档更新：

python .\tools\生成函数功能说明文档.py
已生成：D:\鱼巢\说明书\函数功能说明文档.md

S1a 构建验证：

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
已成功生成
0 个警告
0 个错误

S1a 提交前检查：

python .\tools\check_specs.py
hard issues: 0
warnings: 0

git diff --check
无空白错误；仅提示 `说明书/函数功能说明文档.md` 下次 Git 触碰时 CRLF 会替换为 LF。

S1b 代码切片静态文档更新：

python .\tools\生成函数功能说明文档.py
已生成：D:\鱼巢\说明书\函数功能说明文档.md
函数条目：7605

S1b 构建验证：

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
已成功生成
0 个警告
0 个错误

S1b 构建锁清理：

.codex_locks/build-run.lock 记录 pid=23880；
恢复后检查该 pid 不存在，按锁规则移除旧构建锁。

S1b 提交前检查：

python .\tools\check_specs.py
hard issues: 0
warnings: 0

git diff --check
无空白错误；仅提示 `说明书/函数功能说明文档.md` 下次 Git 触碰时 CRLF 会替换为 LF。

S2 代码切片静态文档更新：

python .\tools\生成函数功能说明文档.py
已生成：D:\鱼巢\说明书\函数功能说明文档.md
函数条目：7607

S2 构建验证：

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
已成功生成
0 个警告
0 个错误

S2 提交前检查：

python .\tools\check_specs.py
hard issues: 0
warnings: 0

git diff --check
无空白错误；仅提示 `说明书/函数功能说明文档.md` 下次 Git 触碰时 CRLF 会替换为 LF。
```

## 7. 当前停止点

P11 详细设计和实现映射计划已落入，计划索引已登记。S0 全局只读扫描已完成；S1a 观察写入收束闸门已实现；S1b 观察写入闸门阻断原因掩码已接入；S2 质量 / 清晰度 / 稳定复现阻断掩码已接入；S3 像素簇到 C 收束状态已接入。下一步进入 S4，对齐 P7 + P8，限制 SceneVoxelPrior 只作为解释偏置来源。本轮未做真实 D455 自然样本运行验证，不伪造业务通过证据。

## 4.10 S3 代码切片预期

目标：

```text
对齐 P11 / P5 的像素簇 -> C 口径，在现有确认方案项写入路径中补机器可读的 C 收束状态：
    单帧或跨帧不足材料只能落为待确认 / 待复现；
    跨帧稳定材料才可落为已验证 / 稳定；
    本切片不实现 C -> E 同一性候选，不修改 SceneVoxelPrior 解释偏置，不接 G_gap 全链路。
```

预计修改：

```text
自我动作实现.外设模块.ixx：
    新增确认方案项到观察候选确认状态的内部收束函数；
    新增确认方案项到稳定复现状态的内部收束函数；
    在已有观察候选写入点写入 特征_观察存在确认状态 与 特征_外设观察存在稳定复现状态。
```

验证预期：

```text
python .\tools\生成函数功能说明文档.py 成功；
Debug x64 构建成功；
python .\tools\check_specs.py hard issues = 0，warnings = 0；
git diff --check 无新增空白错误。
```

## 4.11 S3 代码切片记录

实际修改：

```text
自我动作实现.外设模块.ixx：
    新增 外设观察存在确认方案项稳定复现状态；
    新增 外设观察存在确认方案项观察候选确认状态；
    新增 写入观察候选C收束状态；
    在 写入观察识别归属结果、提交确认观察存在事实路径、稳定子集桥接路径写入：
        特征_观察存在确认状态；
        特征_外设观察存在稳定复现状态。

说明书/函数功能说明文档.md：
    已由生成脚本更新，函数条目 7610。
```

边界：

```text
本切片没有新增 C++ 业务结构体；
没有新增普通一阶 bool 特征；
没有实现 C -> E 同一性候选；
没有改 SceneVoxelPrior 解释偏置；
没有接入 G_gap 全链路；
状态写入只使用既有 I64 状态特征。
```

验证：

```text
python .\tools\生成函数功能说明文档.py
已生成：D:\鱼巢\说明书\函数功能说明文档.md
函数条目：7610

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
已成功生成
0 个警告
0 个错误

python .\tools\check_specs.py
hard issues: 0
warnings: 0

git diff --check
无新增空白错误；仅提示 `说明书/函数功能说明文档.md` 下次 Git 触碰时 CRLF 会替换为 LF。
```

## 5. 禁止宣称

```text
不得宣称像素簇到 C 全链路已代码实现。
不得宣称 C 到 E 同一性裁决已代码实现。
不得宣称 SceneVoxelPrior 解释偏置已代码实现。
不得宣称 G_gap 回流已代码实现。
不得宣称观察、扫描、识别、跟踪或存在体素融合已接入总收束链。
```
