# P10f-e 显示文本边界复核 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-e 外设观察报告队列与线程 / 控制面板显示文本边界复核。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `规范/线程生命周期状态上报与控制面板线程信息规范20260609.md`
- `规范/双目相机外设独占观察线程规范20260527.md`
- `规范/双目相机外设功能能力与实现途径总结20260527.md`
- `规范/外设独立控制线程与消息承接边界规范20260527.md`
- `实施记录/20260623_P10f_d_live_string字段复扫_Codex断点清单.md`
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_causal_structured.tsv`

执行口径：

- `失败原因 / 摘要 / 说明 / 状态 / 类型` 等文本字段若只用于日志、JSON、控制面板显示、证据卡展示或人类排错，不硬删。
- 文本字段若进入候选选择、状态推进、等待状态、稳定键、路由、结算判断或因果匹配，必须删除或改为结构化项目数据承载。
- 线程生命周期显示摘要只服务控制面板和日志展示，不作为任务状态、需求树、世界事实、方法动作动态或安全 / 服务结算依据。
- 外设观察报告是外设观察材料；正式入树 / 入世界必须经过任务管理承接和提交入口裁决。

当前下一步：

1. P10f-e 已闭合。
2. 后续按 `计划/计划索引.md` 进入 P10f-f，从最新 string 字段清单继续复核任务管理界面线程 string 键 / 等待状态相关字段。

## 本轮新增问题记录

问题锚点：`线程生命周期消息模块.ixx::私有_解析事件类型`。

证据行：

- 修改前消费线程生命周期消息时优先读取 `事件类型代码`，但缺失或非法时仍允许根据 `事件类型` 文本 `"创建" / "退出前" / "已退出" / "故障" / "异常退出"` 反解析机器事件。
- 当前发送端已写入 `事件类型代码`，`事件类型` 文本只用于文件可读显示。

当前判断：

- 文本反解析属于旧显示文本兼容判断，不是控制面板展示必需项；应收束为只从数值事件代码恢复机器事件。

已处理：

- `线程生命周期消息模块.ixx` 已改为只从 `事件类型代码` 解析 `枚举_线程生命周期消息事件`。
- 缺失或非法事件代码的消息文件按无效线程生命周期消息处理；`事件类型` 文本继续写入文件，仅作人类可读显示。

待验证：

- `rg` 复核不再存在 `"创建" / "故障"` 等事件文本反解析。
- `Debug|x64` 构建验证。

## 第二问题记录

问题锚点：`任务模块.工作线程消息协议.ixx::结构_任务管理观察事实承接请求` 与 `任务模块.管理工作线程.ixx::结构_外设消息承接结果`。

证据行：

- `结构_任务管理观察事实承接请求` 跨线程携带 `观察运行模式文本 / 报告类型文本`。
- `任务管理外设消息承接_处理一次` 从外设报告枚举生成上述文本后放入观察事实承接请求；同步语义槽当前未读取这些文本，主要用于日志文本构造。

当前判断：

- 即使当前只用于日志，跨线程协议不应携带显示文本字段；应改为数值枚举值或结构化状态值，日志按需渲染。

已处理：

- 删除观察事实承接请求和外设消息承接结果中的 `观察运行模式文本 / 报告类型文本` 字段。
- 外设消息承接结果改保存 `枚举_外设观察运行模式 / 枚举_外设观察报告类型`。
- 观察事实承接请求跨协议只保存 `观察运行模式值 / 报告类型值`。

验证记录：

- `rg -n '文本\s*==\s*"(创建|退出前|已退出|故障|异常退出)"|观察运行模式文本\s*[=;)]|报告类型文本\s*[=;)]|\.观察运行模式文本|\.报告类型文本' -g '*.ixx' -g '*.cpp' -g '*.h'`：无命中。
- `rg -n '"创建"|"退出前"|"已退出"|"故障"|"异常退出"' 线程生命周期消息模块.ixx`：剩余命中为生命周期状态赋值和枚举到显示文本渲染，不再作为文本反解析入口。
- `rg -n "观察运行模式值|报告类型值|观察运行模式文本|报告类型文本" 任务模块.工作线程消息协议.ixx 任务模块.管理工作线程.ixx 自我线程模块.消息批次执行器.ixx`：跨线程协议字段只剩 `观察运行模式值 / 报告类型值`；`任务模块.管理工作线程.ixx` 中 `外设观察运行模式文本(...) / 外设观察报告类型文本(...)` 仅用于结果摘要日志。
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_causal_structured.tsv` 已刷新：剩余 636 行，主键类 314 行，非主键类 322 行；旧 `观察运行模式文本 / 报告类型文本` 字段不再命中。
- `git -c core.quotepath=false diff --check`：通过。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo`：成功，0 警告，0 错误。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
