# S8f D455 首次本能方法自身环境暖账复核 Codex 断点清单

日期：2026-06-09

## 当前切片

```text
S8f D455 首次本能方法自身环境暖账复核。

执行依据：
    计划/计划索引.md
    计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
    计划/20260609_S8f_D455首次本能方法自身环境暖账复核方案_v0.1.md
    规范/方法动作动态硬规则规范20260512.md
    规范/任务筹办与执行边界规范20260512.md
    规范/外设观察特征与自我场景认知本能方法分层规范20260525.md
    规范/相机外设综合工作流程规范20260607.md
```

## 允许修改

```text
方法虚拟存在服务类.cpp
方法虚拟存在服务类.h
本能方法类.cpp
本能方法类.h
自我动作实现.外设模块.ixx
计划/计划索引.md
计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
计划/20260609_S8f_D455首次本能方法自身环境暖账复核方案_v0.1.md
实施记录/20260609_S8f_D455首次本能方法自身环境暖账复核_Codex断点清单.md
```

## 禁止范围

```text
不得跳过方法虚拟存在首次创建。
不得让缺报告、材料不合格或等待状态写成完成 / 满足。
不得跳过失败动作动态、失败回执、派生缺口或任务状态提交。
不得新增 mock、fixture、伪入口、伪动作动态或 `--*-check` 回归入口。
```

## 实施前预测

```text
预测 1：
    6145 / 6150 首次自身环境暖账主因不是缺报告业务写账，而是方法头补齐与方法虚拟存在同步的重复幂等写入。

预测 2：
    若只做进程内“同一方法首节点已同步”缓存，只能优化同进程重复样本，不能降低每个新进程的首次暖账。

预测 3：
    若要降低首次暖账，需要复核 本能方法类::补齐本能方法首节点 和 方法虚拟存在服务类::同步方法节点到虚拟存在 内部是否存在可用的已完整结构判断、结构版本或等值快路径。
```

## 已运行命令

```text
已读取：
    计划/计划索引.md
    计划/20260609_S8f_D455首次本能方法自身环境暖账复核方案_v0.1.md
    自我动作实现.外设模块.ixx 中 确保本能方法 / 确保本能方法自身环境
    方法虚拟存在服务类.cpp 中 同步方法节点到虚拟存在 及其子入口

已执行：
    暂无 S8f 构建或真实运行；S8e 最终构建和真实运行见上一断点清单。
```

## 当前判断

```text
S8e 已证明：
    6150 重复提交样本已被进程内缓存压低。
    剩余首次暖账仍在 确保本能方法、首次同步方法虚拟存在、二次同步方法虚拟存在。

S8f 初步只读判断：
    方法虚拟存在服务类::同步方法节点到虚拟存在 当前每次顺序调用：
        补齐方法虚拟存在概念集
        私有_同步方法身份与默认特征
        初始化方法虚拟存在基础信息
        同步方法首节点结构特征
        私有_同步方法状态特征
        初始化方法统计特征
    多个子入口内部会再次 确保方法虚拟存在 / 补齐概念集。
    当前未看到明确结构版本或全局同步完成标记。
```

## 下一步

```text
1. 继续只读复核 本能方法类::补齐本能方法首节点，确认 6145 / 6150 已完整方法头能否跳过首次补齐。
2. 若已有可读完整性判断，做最小方法头补齐快路径。
3. 若没有可证明契约，先记录阻塞点，不改首次同步语义。
```

## 2026-06-09 首轮实施

```text
只读复核结论：
    本能方法类.cpp 内已有 私有_本能方法首节点已可执行 判断，核心条件为：
        方法首节点
        有方法动作
        已有方法条件节点
    公共 本能方法类::补齐本能方法首节点 当前仍无条件下沉到 方法类::补齐本能方法首节点。

已落实：
    在 自我动作实现.外设模块.ixx 的 确保本能方法 调用侧增加 D455 缺报告方法头快路径。
    仅当本能ID为 6145 或 6150，且方法首节点动作句柄匹配、已有条件节点时，跳过本轮 补齐本能方法首节点。
    后续 方法虚拟存在同步、专属规格确认、条件节点解析、动作动态和失败回执均保留。

验证预测：
    6145 / 6150 首次自身环境中的 确保本能方法 阶段应明显下降。
    首次同步方法虚拟存在 和 二次同步方法虚拟存在 仍可能保持数十毫秒。
    缺报告失败边界、任务状态动作动态和负向锚点应保持。
```

## 2026-06-09 二轮实施前预测

```text
预测 1：
    6150 首次环境中 `确认规格=0` 说明规格已在本轮调用前由同进程路径确认过，二次同步方法虚拟存在属于可跳过的重复幂等同步。

预测 2：
    若 6145 规格在本轮调用前未确认，则本轮仍必须保留二次同步；新增缓存只会让后续同进程重复调用具备跳过条件。

预测 3：
    只在 D455 缺报告相关 6145 / 6150 方法上启用规格预确认判断，不改变通用方法虚拟存在服务层语义，不跳过动作动态、失败回执或任务状态提交。
```

## 2026-06-09 二轮实施

```text
已落实：
    将 6145 / 6150 的规格确认缓存提升为 D455 缺报告环境可见的局部 helper。
    仅当规格调用前已经确认时跳过二次 同步方法节点到虚拟存在。
    若本轮刚补规格，仍保留二次同步。

验证命令：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000

验证结果：
    构建退出码 0；仅 LNK4075 既有警告。
    真实运行退出码 0；最新日志 日志/鱼巢_run_20260609.低值g。
    最新 D455 窗口起点按日志行 39249 统计。

关键日志证据：
    6150 自身环境：总耗时微秒=79791，首次同步方法虚拟存在=78914，确认规格=0，二次同步方法虚拟存在=0，规格调用前已确认=1，二次同步已跳过=1。
    6145 自身环境：总耗时微秒=78390，首次同步方法虚拟存在=77763，确认规格=0，二次同步方法虚拟存在=0，规格调用前已确认=1，二次同步已跳过=1。
    6150 提交内部阶段总耗时：129597 / 55643 / 48384 微秒。
    对应事实节点存在=0 次数=3；映射表节点存在=0 次数=3。
    不写需求满足=1 次数=3；不写安全值=1 次数=3。
    任务状态动作动态=75；准备本能调用=0；执行本能方法=0；任务域本能输入包=0。
    任务状态被动提交/逻辑错误=0；任务状态动作动态逻辑错误=0；[FTL]=0；[ERR]=1，仍为 6145 缺报告失败。

当前判断：
    S8f 二轮已压掉二次同步首次暖账。
    剩余主耗时转为首次 同步方法节点到虚拟存在，6145 / 6150 均约 78ms。

下一步：
    继续只读复核 方法虚拟存在服务类::同步方法节点到虚拟存在 的内部阶段，优先判断是否存在 D455 方法已完整虚拟存在的只读快路径或子阶段可缓存边界。
```

## 2026-06-09 三轮实施前预测

```text
预测 1：
    剩余 6145 / 6150 首次同步约 78ms 仍在 方法虚拟存在服务类::同步方法节点到虚拟存在 内部。

预测 2：
    当前同步函数没有内部阶段采样，直接加快路径风险过高；应先只补 D455 专用阶段耗时日志。

预测 3：
    新日志只记录 6145 / 6150 的同步阶段，不改变概念集、身份默认特征、基础信息、结构特征、状态特征和统计特征写入顺序，不改变动作动态生成边界。
```

## 2026-06-09 用户口径变更 / 转出

```text
用户口径：
    相机外设的四个方法可以先不用优化，因为后续有计划要改整体流程。

转出决定：
    S8f 停在二轮验证状态，不继续执行三轮。
    不继续追 6145 / 6150 首次 同步方法节点到虚拟存在 暖账。
    不在 方法虚拟存在服务类 中保留 D455 专用阶段日志、结构签名或结构版本快路径方向。

当前代码边界：
    方法虚拟存在服务类.cpp 已保持无 D455 专项改动。
    自我动作实现.外设模块.ixx 中已验证的缺报告快退、D455 规格确认缓存和二次同步跳过证据保留为 S8e/S8f 二轮结果。

恢复条件：
    等 计划/20260608_任务持续执行与同目标任务去重纠偏计划_v0.1.md 推进到 P5；
    或真实 D455 报告 / 稳定对应材料重新证明相机四方法专项耗时是当前主瓶颈。

下一步：
    按 计划/计划索引.md 转入 P2a 线程消息边界瘦身与写后触发只读复核 / 小切片方案。
```
