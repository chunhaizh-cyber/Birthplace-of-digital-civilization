# 20260620 场景身份判断回迁 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.2 P3.3j，复核并回迁剩余非显示场景身份判断路径。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
实施记录/20260620_语素场景索引回迁_Codex断点清单.md
说明书/函数功能说明文档.md
```

本切片范围：

```text
1. 新增 场景类::是场景节点。
2. 回迁二次特征类、任务管理工作线程、因果类、自我动作实现、自我动作实现.外设模块、二次特征应用模块中的可闭合场景身份判断。
3. 不处理控制面板 / 语言层显示读取。
4. 不处理世界树场景坐标 / 轮廓辅助函数，保留到后续世界树场景辅助函数归属审查。
```

验证前预测：

```text
1. 相关系统身份判断不再直接 dynamic_cast<场景节点主信息类> 或 基础信息.取主信息<场景节点主信息类>。
2. 剩余复扫命中应主要集中在 场景类 owning class 内部、控制面板、语言层和世界树辅助 / SQL 投影。
3. 函数文档生成应通过。
4. Debug x64 构建应通过。
```

禁止宣称：

```text
不宣称所有场景主信息直读已回迁。
不宣称世界树场景辅助函数归属审查完成。
不宣称场景类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 1. 修改结果

```text
已修改：
    场景类.h
    场景类.cpp
    二次特征类.cpp
    任务模块.管理工作线程.ixx
    因果类.cpp
    自我动作实现.外设模块.ixx
    自我动作实现模块.ixx
    二次特征应用模块.ixx
    说明书/函数功能说明文档.md

新增函数：
    场景类::是场景节点

回迁路径：
    二次特征类::删除二次特征 的父节点场景判断。
    任务模块.管理工作线程 私有_读取输出状态指针 的宿主场景判断。
    因果类 缺主果初始因果写入路径的父节点场景锚点判断。
    自我动作实现.外设模块 取存在所在场景 的父链场景判断。
    自我动作实现模块 结构化节点引用归类中的场景判断。
    二次特征应用模块 私有_取所属场景 / 获取动态二次特征 中的父节点场景判断。
```

## 2. 验证结果

函数文档：

```powershell
python .\tools\生成函数功能说明文档.py
```

证据：

```text
日志/20260620_场景身份判断回迁_函数文档.log
退出码：0
源码文件：128
函数条目：6897
```

目标文件直读检查：

```powershell
rg -n "取主信息<场景节点主信息类>|dynamic_cast<.*场景节点主信息类|场景主信息->|是场景节点" 二次特征类.cpp 任务模块.管理工作线程.ixx 因果类.cpp 自我动作实现.外设模块.ixx 自我动作实现模块.ixx 二次特征应用模块.ixx 场景类.h 场景类.cpp
```

关键结果：

```text
目标业务路径只剩 场景类::是场景节点 调用。
场景类.cpp 内部命中属于 owning class 内部实现。
```

构建验证：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

证据：

```text
日志/20260620_场景身份判断回迁_msbuild.log
退出码：0
已成功生成。
0 个错误
```

剩余复扫：

```text
日志/20260620_场景身份判断回迁_剩余场景主信息引用.txt
总命中：27
控制面板类.cpp：9
场景类.cpp：7
世界树类.cpp：4
语言类_二次特征表达.cpp：3
语言类_基础信息命名.cpp：3
基础信息主信息类.h：1
```

## 3. 下一步

```text
P3.4 标注剩余显示 / 投影 / owning class 内部读取路径；若确认无可闭合系统逻辑回迁项，关闭 1.2 场景类功能函数补缺范围并转入 1.3。
```
