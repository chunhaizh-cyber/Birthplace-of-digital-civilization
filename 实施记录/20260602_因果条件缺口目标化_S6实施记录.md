# 20260602 因果条件缺口目标化 S6 实施记录

## 对应计划

```text
计划/20260601_因果条件按特征值判断纠偏计划_v0.1.md
P0 盘点条件模板和值来源
P1 修正任务筹办因果条件缺口目标化
```

## 运行前预测

```text
1. 条件比较模板不能继续生成无宿主、无特征、无当前值、无目标值的裸条件缺口。
2. 若条件模板能通过证据动态样本回查动作前状态，且当前特征值可读，则生成完整目标化条件缺口。
3. 若当前值等于目标值，则条件已满足，不生成补齐需求。
4. 若缺证据动态、目标状态、I64 目标值或当前 I64 值，则只记录不可目标化统计，不伪造条件需求。
5. 本切片不新增因果主信息字段，不把任务筹办改造成领域硬编码规则。
```

## 修改范围

```text
任务模块.筹办.ixx
自我线程模块.impl.cpp
```

## 代码结果

```text
1. 新增特征引用、动态引用解析 helper。
2. 新增从 因果.证据动态样本 回查条件目标状态的路径：
   条件比较模板 -> 条件特征 -> 证据动态初始状态 -> 条件目标状态。
3. 新增 目标化条件比较路径项_筹办查询：
   - 读取目标状态 I64 值；
   - 读取当前特征 I64 值；
   - 当前值不同于目标值时，创建本轮筹办当前状态；
   - 生成带目标宿主、目标特征、当前值、目标值、比较符和来源因果的条件缺口项。
4. 吸收因果条件缺口_筹办查询 不再推入裸条件缺口：
   - 可目标化且未满足：进入 条件缺口；
   - 已满足：跳过；
   - 不可目标化：只记统计。
```

## 验证结果

```text
1. git diff --check：
   - 退出码=0；
   - 仅提示 LF/CRLF 行尾警告。

2. Debug x64 构建：
   - 命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
   - 退出码=0；
   - 结果=已成功生成；
   - 警告=1 个 LNK4075 /EDITANDCONTINUE 与 /INCREMENTAL:NO 组合警告。

3. 默认真实运行：
   - 命令：.\x64\Debug\鱼巢.exe *> .\日志\20260602_S6条件目标化默认运行_output.txt
   - 退出码=0；
   - 本次日志：日志/鱼巢_run_20260602.低值g。

4. 验证边界：
   - 默认运行没有自然触发任务筹办因果路径查询；
   - 因此本轮只验证构建、启动和默认退出正常；
   - 条件缺口目标化统计仍需后续真实观察事实承接后的自然重筹办链触发验证。
```

## 遇到的问题

```text
当前默认入口不能稳定触发任务筹办因果路径查询。
S6 运行期证据仍缺真实观察事实驱动的自然触发链，不能宣称条件缺口目标化业务链已通过。

若条件模板没有证据动态样本，或证据动态初始状态不是 I64 状态，
当前实现只能记录不可目标化统计，不能生成正式条件补齐需求。
这是结构边界，不应在任务筹办里硬造目标值。
```

## 2026-06-02 口径修订

```text
S6 下一步不是构造样本，也不是直接调用任务筹办函数验证计算结果。

下一切片：
    S6-N1 真实观察事实更新驱动安全根需求重筹办。

目标：
    真实 D455 采集报告、材料页、d455:// 句柄和观察动态承接完成后，
    发布观察事实更新事件，
    由需求裁决层命中安全根需求或其自然派生需求，
    标记重筹办，
    再由任务管理自然进入任务筹办因果路径查询。

禁止：
    人工造来源需求；
    人工造当前值；
    人工造证据动态；
    直接调用筹办函数伪造业务链；
    安全评估直接写风险明确、自我风险安全值、自我安全值或结算。

验收：
    S6 统计日志由真实观察事实承接后的自然重筹办触发；
    裸条件缺口生成数量=0；
    可目标化、已满足跳过、不可目标化三种真实结果都可以作为业务链触发成功，
    但任务筹办因果路径查询未被自然触发不能通过。
```

## 2026-06-02 S6-N1 实施与真实运行停点

```text
已补代码：
    自我线程模块.impl.cpp 新增 观察事实承接完成后的事实更新事件发布日志；
    真实观察事实承接成功且报告可回查时，命中真实安全根需求；
    对安全根需求既有非终态任务提交 待重筹办 + 任务管理重筹办治理请求；
    若安全根需求尚无承接任务，只提交真实安全根需求任务发起意图；
    不直接写世界真值、风险状态、安全值、当前值或子需求。

新增关键日志：
    自我线程/观察事实更新事件发布
    需求裁决/观察事实更新命中安全根需求
    自我线程/安全根需求标记重筹办
    任务管理领取重筹办
```

验证：

```text
1. git diff --check -- 自我线程模块.impl.cpp
   退出码=0；
   仅 LF/CRLF 行尾警告。

2. Debug x64 构建
   命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
   退出码=0；
   结果=已成功生成；
   警告=1 个 LNK4075。

3. 默认真实长运行
   输出：日志/20260602_S6_N1真实默认长运行_output.txt
   项目日志：日志/鱼巢_run_20260602.低值g
   退出码=0。
```

真实日志结论：

```text
已自然出现：
    当前场景特征值OR路径展开；
    D455路径激活=1；
    任务筹办因果路径查询壳统计；
    筹办_按因果反推必要状态。

未出现：
    任务管理界面线程/自动创建外设观察等待项；
    任务管理界面线程/按外设观察等待项启动D455线程；
    D455真实报告产生；
    短期观察材料页提交；
    任务管理观察事实承接请求；
    自我线程/外设观察事实承接完成；
    自我线程/观察事实更新事件发布。

实际停点：
    任务4 关键特征变化观察状态可由方法15完成；
    任务5 安全因果候选构造状态 选择方法18 安全_构造场景影响因果候选；
    方法18 失败原因=缺少已确认场景存在；
    日志明确 外设条件不足=0 / 等待外设条件=0；
    因此当前真实链没有把“缺少已确认场景存在”转为外设观察等待项。
```

当前结论：

```text
S6-N1 唤醒链代码已落地并构建通过；
但真实观察事实承接没有发生，唤醒链尚未被真实数据触发；
S6 业务链仍未验收。

下一问题切片：
    S6-N2 让当前场景存在 / 特征值明确缺口自然生成 D455 外设观察等待项，
    或至少补足任务管理等待项生成日志，证明为何没有创建等待项。
```

## 2026-06-02 S6-N2 口径修正：先定义当前场景特征值明确标准

用户校正：

```text
当前场景特征值明确状态 的标准不应从布尔门或单个特征值出发；
应从外设能获取哪些特征类型、这些特征类型中哪些真正对自我有影响出发。
```

已落文档：

```text
新增规范：
    规范/当前场景特征值明确标准规范20260602.md
    编号=050005

已更新：
    规范/规范目录.md
    计划/计划索引.md
    计划/20260601_安全因果链反推当前场景特征值需求纠偏计划_v0.2.md
    计划/20260601_当前场景特征值明确链路纠偏计划_v0.1.md
```

新标准摘要：

```text
当前场景特征值明确状态(范围) =
    指定范围中可能影响自我的存在、区域或变化，
    其与自我相关的关键特征值已经达到可读、可比较、可追溯；
    不能读取的部分具有明确缺失原因，并能继续派生补观察或补解释需求。

三层来源：
    外设可产出的原始观察特征
    -> 自我可解释的场景事实特征
    -> 对自我有影响的安全 / 服务 / 边界相关特征
```

代码边界修正：

```text
任务模块.管理界面线程.impl.cpp 保留外设观察等待项未生成诊断日志；
撤回把 当前场景特征值明确状态 直接加入 D455 逐簇识别等待项目标列表的临时映射；
后续等待项只能由材料型、确认型、扫描型或跟踪型子需求自然触发。
```

下一问题切片：

```text
S6-N2a 当前场景特征值明确标准子链展开：
    按 050005，把总目标拆成：
        外设可产出原始观察特征；
        自我可解释场景事实；
        对自我安全 / 服务 / 边界有影响的特征；
        缺失 / 过期 / 冲突 / 不可观测的解释缺口。

禁止：
    不把 D455 报告、像素簇候选、材料页或单个特征值直接解释为 当前场景特征值明确；
    不造样本；
    不手动调用筹办链；
    不直接写风险状态、安全值或需求满足。
```

### 二次定案补充

用户进一步定案：

```text
当前场景特征值明确状态(范围) =
某个指定范围内，当前安全评估所需的、由外设或场景事实可提供的关键特征值，
已经对该范围内的已确认存在形成可追溯、可比较、可解释缺口的读取结果。
```

新增到 050005 的关键口径：

```text
1. 标准必须从两个源头共同生成：
       外设实际能提供哪些特征类型；
       这些特征类型里哪些能进入自我安全因果链。

2. 转换链是：
       外设原始特征
       -> 场景存在特征
       -> 自我相对特征
       -> 安全因果候选。

3. 当前场景特征值明确状态 与 当前场景特征值缺口可解释状态 必须分离。
       缺口能解释，只能说明缺口可解释；
       不能说明特征值明确。

4. 第一版最小特征集由当前激活安全因果模板决定：
       需要靠近趋势时必须有跨帧可比状态；
       只判断当前是否进入边界时，可不要求趋势，但必须有空间值、坐标转换和自我边界参照。

5. 空集合可以明确，未知集合不能明确。

6. 当前场景特征值明确 只是让风险证据搜索 / 安全评估有材料；
       不等于风险因素默认满足；
       不等于风险安全。
```

当前执行结论：

```text
本轮只修订标准与计划口径；
不新增 当前场景特征值缺口可解释状态 的代码字段；
不继续扩大外设等待项映射；
下一步应先按 050005 做外设特征类型与安全影响关系表 / 最小特征集 / 缺口可解释状态的代码映射对账。
```

### 三类外设能力定义补充

用户进一步定案：

```text
识别 = 建立场景信息到存在的归属关系。
扫描 = 在已识别场景基准上发现已知存在的特征值变化。
跟踪 = 对指定存在进行持续观察，主动忽略非目标存在。
```

已同步规范：

```text
规范/观察像素簇与存在候选分层规范20260527.md
规范/双目相机外设独占观察线程规范20260527.md
规范/双目相机外设功能能力与实现途径总结20260527.md
规范/当前场景特征值明确标准规范20260602.md
规范/规范目录.md
```

关键边界：

```text
识别闭合不是所有像素都变成已确认存在；
识别闭合是有效观察材料没有无主漂浮项。

归属结果可以是：
    已确认存在；
    新存在候选；
    未知区域；
    冲突区域；
    背景 / 场景结构；
    噪声 / 无效材料。

扫描必须有已识别场景基准；
没有基准时应退回识别或输出需要识别基准。

跟踪必须有目标种子；
跟踪主动忽略非目标存在，因此不能证明全场存在集合明确或全场风险状态明确。
```

下一步对账入口：

```text
S6-N2a 不从 当前场景特征值明确状态 这个状态名反推实现；
先从识别包、扫描包、跟踪包分别列出：
    外设可提供特征；
    场景存在特征；
    自我相对特征；
    可进入安全因果候选的影响特征；
    缺口可解释字段。
```

### 三类能力区域化修正

用户进一步修正：

```text
场景可以是部分识别状态。
识别、扫描、跟踪不是按“整场景阶段”串行排队，
而是按“区域归属状态”并行分工。

识别：处理未识别区域。
扫描：处理已识别区域中已知存在的特征值变化。
跟踪：处理已识别区域中的指定目标存在，忽略非目标存在。
```

已同步规范和计划：

```text
规范/当前场景特征值明确标准规范20260602.md
规范/观察像素簇与存在候选分层规范20260527.md
规范/双目相机外设独占观察线程规范20260527.md
规范/双目相机外设功能能力与实现途径总结20260527.md
规范/规范目录.md
计划/计划索引.md
计划/20260601_安全因果链反推当前场景特征值需求纠偏计划_v0.2.md
计划/20260601_当前场景特征值明确链路纠偏计划_v0.1.md
```

本次新增关键口径：

```text
当前帧材料先分区：
    已识别区域；
    未识别区域；
    候选区域；
    冲突区域；
    无效区域；
    背景区域。

已识别区域 -> 扫描；
未识别 / 候选 / 冲突区域 -> 识别；
指定目标所在已识别区域 -> 跟踪。

当前场景特征值明确状态必须带范围：
    全场景；
    已识别区域；
    指定区域；
    指定存在；
    跟踪目标。

局部已识别区域明确，不等于全场景明确。
目标跟踪明确，不等于场景扫描明确。
扫描发现未解释区域，应转交识别，不得直接确认新存在。
```

下一步对账入口修正为：

```text
S6-N2a 先做：
    范围化目标状态；
    当前帧区域归属状态；
    识别包 / 扫描包 / 跟踪包与区域状态的映射；
    每个范围内的外设可提供特征、场景存在特征、自我相对特征和安全影响特征；
    缺口可解释字段。

代码修改前先查现有目标特征和值域，避免再把“当前场景特征值明确状态”裸映射成 D455 等待项。
```

### 外设三分法技术储备入计划

用户给出 D455 / RGB-D、识别、扫描、跟踪四层技术储备清单，并强调技术储备应按系统能力分层，而不是按算法名堆叠。

已新增计划：

```text
计划/20260602_外设三分法技术储备与实施路线_v0.1.md
```

计划索引和 v0.2 主计划已补引用。

本次收束口径：

```text
A. D455 基础材料与质量：
    RealSense D400/D455 datasheet、depth post-processing、projection / texture-mapping / occlusion、
    self-calibration、visual presets / tuning。

B. 识别：
    第一版优先 D455 深度聚类、ROI、掩码、像素归属账本；
    SAM / SAM2 / Mask2Former / Grounding DINO / YOLOv10 只作为候选增强。

C. 扫描：
    第一版优先 AABB、中心点、深度中值、掩码面积、未解释变化；
    OctoMap / Voxblox / RTAB-Map / ORB-SLAM3 / DynaSLAM / RAFT / Scene Flow 只作为地图和变化储备。

D. 跟踪：
    第一版优先目标 ID、上一帧 ROI / 掩码 / 点集、IoU / 距离 / 深度残差；
    SAM2 / XMem / Cutie / ByteTrack / RAFT 只作为目标记忆和关联增强。
```

边界：

```text
模型输出 = 候选；
几何聚类 = 候选；
地图更新 = 扫描材料；
轨迹关联 = 跟踪材料；
自我确认 = 事实；
提交入口 = 入账。

本次没有代码实现，不标记 S6-N2a 业务通过。
```
