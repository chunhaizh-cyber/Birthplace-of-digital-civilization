# D455 逐簇识别样本卡 P2 实施记录

日期：2026-06-05

## 目标

按 `计划/20260604_外设三分法真实D455样本实施切片计划_v0.1.md` 的 P2，补齐逐簇识别报告最小样本卡与控制面板只读展示。

## 修改

```text
外设观察报告队列.ixx
    新增 构造D455逐簇识别样本卡摘要。
    样本卡从逐簇识别报告和识别提交包读取：
    观察像素簇、有效观察区域、无效区域、未识别区域、冲突区域、候选存在、
    ROI / 掩码 / 点集 / 深度切片句柄、空间候选引用、像素归属账本、证据不足原因。

外设线程_D455深度相机.ixx
    单帧真实采集提交材料包时，同步写出逐簇识别样本卡日志和命令行摘要。

控制面板类.ixx / 控制面板类.cpp
    控制面板快照新增 D455 逐簇识别样本卡只读字段。
    相机页新增“最新逐簇识别样本卡”区块。

控制面板.html
    已按 --panel-html 重新生成。
```

## 边界

```text
本切片只表达材料归属、区域候选和证据不足。
不确认世界存在。
不写需求满足。
不写安全 / 服务结论。
不构造假帧或伪造样本。
```

## 验证

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

结果：

```text
退出码=0。
仅有既有 LNK4075 链接警告。
```

真实单帧验收预测：

```text
--d455-capture-once 成功时，应输出 D455逐簇识别样本卡；
样本卡应包含观察簇、有效观察区域、未识别 / 无效 / 冲突区域、候选存在、证据不足；
样本卡只声明材料归属和存在候选，不确认世界存在。
```

真实单帧验收：

```powershell
.\x64\Debug\鱼巢.exe --d455-capture-once
```

结果：

```text
退出码=0。
输出 D455逐簇识别样本卡。
观察像素簇=11。
有效观察区域=11。
无效区域=0。
未识别区域=10。
冲突区域=0。
不可观测区域=1063。
候选存在=11。
ROI句柄=11。
掩码句柄=11。
点集句柄=11。
深度切片句柄=11。
候选材料句柄=77。
空间候选引用=11。
报告证据不足=1。
候选证据不足=0。
失败簇=0。
只读边界=材料归属和存在候选,不确认世界存在。
```

证据：

```text
日志/20260605_D455逐簇识别样本卡_P2_capture_once.log
日志/鱼巢_run_20260605.低值g
```

HTML 结构验收：

```powershell
.\x64\Debug\鱼巢.exe --panel-html
rg -n "最新逐簇识别样本卡|D455逐簇识别只读字段" 控制面板.html
```

结果：

```text
退出码=0。
控制面板.html 包含“最新逐簇识别样本卡”和“D455逐簇识别只读字段”。
该 HTML 为新进程快照，未携带单帧采集进程内队列，因此显示待真实 D455 逐簇识别报告是预期结果。
```

## 结论

```text
P2 已闭合。
下一切片切到 P3：已识别区域扫描变化样本。
```
