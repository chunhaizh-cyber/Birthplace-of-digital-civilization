# 20260617 因果信息生成三分口径修正 Codex断点清单

## 0. 恢复摘要

```text
当前切片：
    执行 计划/待完成项/20260617_因果信息生成三分口径修正计划_v0.1.md。

依据文件：
    规范/禁止性规范20260611.md
    规范/规范目录.md
    规范/方法动作动态硬规则规范20260512.md
    规范/动作动态即因果账本规范20260512.md
    规范/二次特征规范20260520.md
    规范/因果补完流程规范20260516.md
    计划/待完成项/20260617_因果信息生成三分口径修正计划_v0.1.md

本轮目标：
    1. 因果主信息字段收束为 条件比较模板 / 因方法模板 / 果比较模板 / 主果比较模板 / 证据动态样本。
    2. 果比较模板存结果特征；主果比较模板存状态迁移二次特征方向。
    3. 按动态自身推导无动作状态变化、主动动作致变、被动动作致变；外部参数不得把无动作动态升格为动作因果。
    4. 保持世界树 SQL 投影和控制面板引用字段可回查，不新增数据库旁路写入。

开工前已知工作区状态：
    基础信息主信息类.h 已有未提交改动：果比较模板被改名为结果比较模板，且类型改为特征引用。
    实施记录/20260614_D455扫描跟踪后续事实链_Codex断点清单.md 已有未提交改动。
    自我动作实现.外设模块.ixx 已有未提交改动。
    本切片不回滚 D455 相关既有改动。

已发现独立问题：
    字段只在 基础信息主信息类.h 改成结果比较模板，其余代码仍访问果比较模板。
    按禁止性规范，不允许保留果比较模板/结果比较模板双字段兼容。
    当前判断：收束为唯一字段 果比较模板，类型为 std::vector<可解析引用<特征节点类>>。

预期验证：
    rg -n "结果比较模板|果比较模板|主果比较模板" -g "*.cpp" -g "*.h" -g "*.ixx"
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m

已完成修改：
    基础信息主信息类.h：
        因果主信息类保留唯一字段 果比较模板，类型为 特征节点引用集合。
    因果类.cpp：
        新增动态因果生成类别局部推导。
        果比较模板只写结果特征。
        主果比较模板只写状态迁移二次特征。
        无动作状态变化动态不再因外部动作参数生成动作致变因果。
    二次特征应用模块.ixx：
        按动态创建因果信息时不再传入动态主体 / 动态特征作为动作参数。
    自我线程模块.impl.cpp：
        初始因果模板不再把主果二次特征写入果比较模板。

已运行命令：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        结果：exit_code=0；Debug x64 构建通过；0 warning；0 error。
    python .\tools\生成函数功能说明文档.py
        结果：exit_code=0；已生成 说明书/函数功能说明文档.md；函数条目 7326。

真实运行预测：
    命令：.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000
    预期：
        exit_code=0；
        日志中出现 按动态补全两层因果/动态因果生成类别；
        普通无动作状态变化动态应记录 类别=无动作状态变化 且 外部参数升格动作=0；
        不出现新增异常弹窗或 stderr 业务错误；
        若缺果状态迁移模板，只创建证据实例，不创建完整抽象因果模板。

真实运行结果：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000
        结果：exit_code=0。
        控制台输出：自检线程完成，未发现需提交缺口。
        日志文件：日志/鱼巢_run_20260617.低值g。
        分类日志：按动态补全两层因果/动态因果生成类别 共 210 条；
            当前样本全部为 类别=无动作状态变化；
            样本均包含 外部参数升格动作=0。
        未命中：
            拒绝创建缺结果特征抽象因果模板；
            拒绝创建缺主果方向抽象因果模板；
            拒绝创建空果抽象因果模板；
            外部参数升格动作=1。

当前新增问题：
    P5 数据库投影偏差：
        日志/鱼巢_exception_20260617.低值g 中 2026-06-17 11:52:40 出现：
        世界树SQL投影失败 | 原因=世界树SQL投影重写失败 |
        ADO执行命令失败 | HRESULT=0x80040E31 | 描述=查询超时已过期 |
        节点数=37501 | 关系数=57683。
    当前判断：
        代码主路径和 self-check 运行已通过，但数据库落库 / 投影验收尚未闭合。
    下一步：
        定位 世界树SQL投影重写失败 和 ADO执行命令超时配置，判断能否用项目 ADO 正式通道提高投影重写命令超时或拆分执行。

SQL 投影超时修正：
    修改 数据库ADO模块.ixx / 数据库ADO模块.cpp：
        执行ADO命令 增加 命令超时秒 参数，默认 30 秒。
    修改 世界树类.cpp：
        世界树SQL投影重写阶段调用 执行ADO命令 时使用 180 秒超时。
        建库 / 建表 / 视图阶段仍使用默认 30 秒。
    已运行：
        msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
            结果：exit_code=0；0 warning；0 error。
        python .\tools\生成函数功能说明文档.py
            结果：exit_code=0；已生成 说明书/函数功能说明文档.md；函数条目 7325。
    复跑预测：
        .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000
            exit_code=0；
            继续出现 动态因果生成类别 日志；
            不再出现 世界树SQL投影重写失败 / HRESULT=0x80040E31 查询超时。

复跑结果与数据库投影证据：
    第二次运行：
        .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000
            结果：exit_code=0。
        日志/鱼巢_run_20260617.低值g：
            世界树SQL投影完成=18；
            世界树SQL投影失败=0；
            HRESULT=0x80040E31=0；
            按动态补全两层因果/动态因果生成类别=400；
            类别=无动作状态变化=400；
            类别=主动动作致变=0；
            类别=被动动作致变=0；
            外部参数升格动作=0=400；
            外部参数升格动作=1=0。
        日志/鱼巢_exception_20260617.低值g：
            最新写入时间仍为 2026-06-17 11:52:40；
            只保留第一次复跑前的旧失败：
                世界树SQL投影失败=3；
                HRESULT=0x80040E31=1；
                查询超时已过期=1；
            第二次运行后未新增 exception 记录。

    SQL Server 当前投影：
        查询库：.\SQLEXPRESS / FishnestProjection。
        最新 world_tree_snapshot：
            captured_at=2026-06-17T04:01:06.537 UTC；
            source_reason=程序退出最终重写；
            node_count=36011；
            relation_count=54445。
        当前因果节点数：
            causal_nodes=315。
        当前因果字段关系计数：
            果比较模板=315；
            主果比较模板=125；
            证据动态样本=204；
            条件比较模板=0；
            因方法模板=0。
        当前仍存在旧口径关系：
            因比较模板=130。

静态复核：
    rg -n "结果比较模板|果比较模板|主果比较模板" -g "*.cpp" -g "*.h" -g "*.ixx"
        结果：未命中 结果比较模板；
        果比较模板 写入点为 因果类.cpp 的结果特征写入；
        主果比较模板 写入点为状态迁移二次特征引用。
    rg -n "条件比较模板|因方法模板|果比较模板|主果比较模板|证据动态样本" -g "*.cpp" -g "*.h" -g "*.ixx"
        结果：世界树 SQL 投影和控制面板读取路径均包含五个目标字段。

当前阻塞：
    1. 当前真实 self-check 样本只产生无动作状态变化动态，
       未覆盖主动动作致变和被动动作致变，不能用真实日志证明两类动作因果的入库结果。
    2. 当前 SQL 快照中无 条件比较模板 / 因方法模板 行；
       其中 因方法模板 缺失与当前样本无动作一致，不能靠伪造样本补验。
    3. 代码和 SQL 投影中仍有 因比较模板，且当前快照有 130 条关系；
       它来自既有初始因果 / 筹办链路，不属于本轮动态生成修正已闭合范围。
       若严格按本计划把因果主信息字段收束为五项，需要另开切片确认：
           因比较模板 是继续作为因侧比较原因字段保留，
           还是迁移/退役到 条件比较模板 与 因方法模板 口径下。

当前判断：
    P0-P4 和 SQL 投影超时修正已完成并通过构建、自检、静态字段检查。
    P5/P6 的数据库实证只闭合到无动作状态变化因果：
        果比较模板 / 主果比较模板 / 证据动态样本 已入 SQL 当前投影。
    主动动作因果、被动动作因果、条件比较模板和因方法模板的真实入库验收，
    需要真实主动/被动动态样本或先确认 因比较模板 的迁移边界。

用户确认：
    旧逻辑代码直接删除，禁止兼容。

新增执行目标：
    删除 因比较模板 机器路径：
        不保留 因比较模板 字段；
        不投影 因比较模板 SQL 关系；
        任务筹办不再从 因比较模板 生成促成/避免路径；
        自我线程不再用 因比较模板 创建初始因果模板或安全风险树前置缺口。
    不把旧 因比较模板 迁移塞入 条件比较模板，避免形成兼容迁移。

旧逻辑删除进展：
    已修改：
        基础信息主信息类.h：删除 因果主信息类::因比较模板。
        世界树类.cpp：删除 因比较模板 SQL 关系投影。
        任务模块.筹办.ixx：
            删除从 因比较模板 吸收促成路径 / 避免路径的旧逻辑；
            删除仅服务该路径的目标化状态比较路径辅助函数。
        自我线程模块.impl.cpp：
            删除初始因果模板写入 因比较模板 的路径；
            旧初始因果种子不迁移到 条件比较模板，直接跳过并记录；
            被动触发初始因果不再由旧因侧比较生成模板；
            安全风险树不再从旧因侧比较展开前置缺口。
    静态检查：
        rg -n "因比较模板" -g "*.cpp" -g "*.h" -g "*.ixx"
            结果：无命中。
    构建：
        msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
            结果：exit_code=0；0 warning；0 error。

旧逻辑删除后运行预测：
    python .\tools\生成函数功能说明文档.py
        预期：exit_code=0，函数说明文档更新。
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000
        预期：
            exit_code=0；
            不出现 因比较模板 代码投影关系；
            SQL 当前投影中 因比较模板 关系计数为 0；
            仍不出现 世界树SQL投影失败 / HRESULT=0x80040E31。

旧逻辑删除后运行结果：
    python .\tools\生成函数功能说明文档.py
        结果：exit_code=0；函数条目 7319。
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000
        结果：exit_code=0。
        日志/鱼巢_run_20260617.低值g：
            世界树SQL投影完成累计=20；
            当前最新完成：2026-06-17 12:25:30.373；
            世界树SQL投影失败=0；
            HRESULT=0x80040E31=0；
            外部参数升格动作=1=0；
            本次可见旧逻辑删除日志：
                自我线程/拒绝旧因比较初始因果；
                原因=旧因侧比较逻辑已删除。
        日志/鱼巢_exception_20260617.低值g：
            最新写入仍为旧 2026-06-17 11:52:40；
            本次未新增 exception。
        SQL Server 当前投影：
            snapshot_id=CD7E96A6-DB30-481D-AA82-7BD8E8698FD2；
            captured_at=2026-06-17T04:25:27.936 UTC；
            node_count=18160；
            relation_count=33886；
            因比较模板=0；
            当前因果节点=3；
            果比较模板=3；
            证据动态样本=3。

新增问题：
    删除旧初始模板后，当前动态样本未生成完整抽象因果模板：
        本次动态 4iU 日志显示 果状态迁移模板=0；
        结果=缺果状态迁移模板，仅创建证据实例。
    当前判断：
        旧主果比较模板数量此前主要来自旧初始模板；
        真正按动态生成的二次特征虽然绑定了来源动态 / 起始状态 / 结果状态，
        但生成时没有标记为 二次特征主信息类::枚举_形态::状态迁移，
        因果侧 私有_查找动态果状态迁移模板 只接受 形态=状态迁移，
        所以按动态生成因果找不到主果方向。

新增修正：
    二次特征应用模块.ixx：
        动态初始状态 -> 结果状态的标量结果二次特征设置 形态=状态迁移；
        动态初始状态 -> 结果状态的离散结果二次特征设置 形态=状态迁移；
        多动态协同关系不改为状态迁移。

新增验证预测：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        预期：exit_code=0。
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000
        预期：
            exit_code=0；
            果状态迁移模板不再全部为 0；
            SQL 当前投影中出现 主果比较模板；
            因比较模板 仍为 0。

状态迁移形态验证偏差：
    已运行：
        msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
            结果：exit_code=0；日志 日志/20260617_因果三分_状态迁移形态_msbuild.log；关键错误=0。
        python .\tools\生成函数功能说明文档.py
            结果：exit_code=0；函数条目 7319。
        .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000
            结果：exit_code=0；控制台日志 日志/20260617_因果三分_状态迁移形态_self_check_console.log。
    证据：
        日志/鱼巢_run_20260617.低值g：
            世界树SQL投影失败=0；
            HRESULT=0x80040E31=0；
            外部参数升格动作=1=0；
            果状态迁移模板=0 仍出现；
            缺果状态迁移模板 仍出现；
            主果比较模板日志命中=0。
        日志/鱼巢_exception_20260617.低值g：
            最新写入仍为旧 2026-06-17 11:52:40；本次未新增 exception。
    当前判断：
        二次特征模板构造已设置 形态=状态迁移，但复用已有二次特征节点后，
        后续语义绑定只同步 种类 / 规则版本 / 生成语义，未把主信息 形态 同步为状态迁移，
        因果侧 私有_查找动态果状态迁移模板 因 形态 不匹配继续过滤。
    下一步：
        在动态初始状态 -> 结果状态的标量结果、离散结果两个写入点，
        将主信息 形态 强制同步为 二次特征主信息类::枚举_形态::状态迁移。

因果查询接口与字符串签名清理：
    用户新增口径：
        在因果类中实现因果相关功能函数，方便其他地方调用；
        包括条件查询、结果查询、因果链查询等；
        新建函数按规范添加函数说明和索引；
        严禁使用类似 std::string& 状态变化签名 的字符串签名查询代码。

    已修改：
        因果类.h / 因果类.cpp：
            新增只读查询接口：
                查询条件相关因果；
                查询结果相关因果；
                查询动作相关因果；
                查询因果链。
            删除旧字符串签名路径：
                结构_两层因果构建结果::状态变化签名；
                结构_两层因果构建结果::动作致变签名；
                计算动态状态变化签名；
                计算动态动作致变签名；
                计算模板状态变化签名；
                计算模板动作致变签名；
                查找状态变化关系模板；
                查找动作致变关系模板；
                查找导致状态变化的动作关系；
                因果模板签名索引及刷新/失效函数；
                状态转换因果节点/边中的 节点签名 / 边签名 字段。
            按动态补全两层因果改为结构匹配：
                用结果特征、主果状态迁移二次特征、动态初始状态、动态结果状态匹配状态变化因果；
                动作因果在此基础上匹配 因方法模板 / 因动作语义键 / 因动作名称；
                不再计算、保存或查询状态变化字符串签名。

        二次特征应用模块.ixx：
            动态初始状态 -> 结果状态的标量结果和离散结果在复用已有节点后，
            强制同步 主信息.形态=状态迁移。

    静态检查：
        rg -n "状态变化签名|动作致变签名|节点签名|边签名|签名索引|STATE_TEMPLATE|ACTION_TEMPLATE|TEMPLATE_EDGE|因比较模板|结果比较模板" 因果类.cpp 因果类.h
            结果：无命中。
        rg -n "因比较模板|结果比较模板" -g "*.cpp" -g "*.h" -g "*.ixx"
            结果：无命中。

    已运行：
        msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
            结果：exit_code=0；日志 日志/20260617_因果查询接口_结构匹配_msbuild.log；关键错误=0。
        python .\tools\生成函数功能说明文档.py
            结果：exit_code=0；日志 日志/20260617_因果查询接口_函数文档.log；函数条目 7314。
            函数说明文档已包含：
                因果类::查询条件相关因果；
                因果类::查询结果相关因果；
                因果类::查询动作相关因果；
                因果类::查询因果链。
        .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000
            结果：exit_code=0；控制台日志 日志/20260617_因果查询接口_结构匹配_self_check_console.log。

    最新运行证据：
        日志/鱼巢_run_20260617.低值g 的 13 点后运行段：
            动态因果生成类别=3；
            外部参数升格动作=1 为 0；
            缺果状态迁移模板=0；
            果状态迁移模板=0 为 0；
            果状态迁移模板非零=1；
            完整构建=1；
            样本：2026-06-17 13:05:19.464，动态=4j8，
                果状态迁移模板=2，
                创建状态变化关系=1，
                创建证据实例=1，
                结果=完整构建。
        日志/鱼巢_exception_20260617.低值g：
            最新写入仍为旧 2026-06-17 11:52:40；本次未新增 exception。
        SQL Server 当前投影：
            snapshot_id=3BC586A7-0883-4A3E-B217-1F6D820B2067；
            captured_at=2026-06-17 05:05:59.587；
            node_count=18534；
            relation_count=37058；
            因果节点=6；
            果比较模板=6；
            主果比较模板=6；
            证据动态样本=6；
            条件比较模板=3；
            因方法模板=0；
            因比较模板=0。

    当前剩余边界：
        本次真实样本仍只覆盖无动作状态变化动态；
        主动动作致变和被动动作致变的真实入库证据仍需等待真实主动/被动动态样本，
        不用伪造样本补验。

本轮收口复核预测：
    python .\tools\生成函数功能说明文档.py
        预期：exit_code=0；函数说明文档仍包含新增因果查询接口。
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        预期：exit_code=0；关键编译错误为 0。
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000
        预期：
            exit_code=0；
            不出现 状态变化签名 / 动作致变签名 / 因比较模板 路径回潮；
            外部参数升格动作=1 继续为 0；
            本轮若仍无真实主动/被动动态样本，不伪造样本补验 因方法模板。

旧签名与旧因比较残留补充修正：
    发现：
        自我线程模块.impl.cpp 中仍有旧初始因果空入口、旧初始模板种子读取/跳过路径、
        安全风险树因比较目标字符串签名结构，以及旧因侧跳过日志。
    已修改：
        删除 私有_查找初始因果模板_线程侧 / 私有_确保初始因果模板_线程侧 /
            私有_确保根必要条件初始因果_线程侧 / 私有_回写根必要条件来源因果_线程侧。
        删除初始因果模板种子读取、解析、应用和跳过路径。
        删除安全风险树因比较目标结构及其字符串签名解析函数。
        安全因果风险树维护不再尝试旧初始模板补建，缺真实来源因果时按现有缺口跳过。
        被动初始因果辅助结构中的局部 因比较 命名收束为 条件比较。
    静态检查：
        rg -n "旧因比较|旧因侧|因比较模板|结果比较模板|状态变化签名|动作致变签名|签名索引|STATE_TEMPLATE|ACTION_TEMPLATE|TEMPLATE_EDGE" \
            因果类.cpp 因果类.h 自我线程模块.impl.cpp 基础信息主信息类.h 世界树类.cpp 任务模块.筹办.ixx 二次特征应用模块.ixx
            结果：无命中。
        rg -n "因比较" 自我线程模块.impl.cpp 因果类.cpp 因果类.h 基础信息主信息类.h 世界树类.cpp 任务模块.筹办.ixx 二次特征应用模块.ixx
            结果：无命中。
        rg -n "节点签名|边签名" 因果类.cpp 因果类.h 自我线程模块.impl.cpp
            结果：无命中。
    复核预测：
        python .\tools\生成函数功能说明文档.py
            预期：exit_code=0；函数条目随删除旧函数下降；新增因果查询接口仍在索引中。
        msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
            预期：exit_code=0；关键编译错误为 0。
        .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000
            预期：exit_code=0；最新运行段不再出现旧因比较 / 旧因侧 / 状态变化签名 / 动作致变签名。

补充修正复核结果：
    python .\tools\生成函数功能说明文档.py
        结果：exit_code=0；日志 日志/20260617_因果收口_删除旧因比较_函数文档.log；
        函数条目=7302。
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        结果：exit_code=0；日志 日志/20260617_因果收口_删除旧因比较_msbuild.log；
        关键编译错误=0。
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000
        结果：exit_code=0；控制台日志 日志/20260617_因果收口_删除旧因比较_self_check_console.log。
    最新运行段证据：
        日志/鱼巢_run_20260617.低值g 的 13:29 后运行段：
            动态因果生成类别=3；
            外部参数升格动作=1 为 0；
            旧因比较=0；
            旧因侧=0；
            因比较=0；
            状态变化签名=0；
            动作致变签名=0；
            世界树SQL投影失败=0；
            HRESULT=0x80040E31=0。
        世界树SQL投影完成：
            2026-06-17 13:30:18.844；
            节点数=14106；
            关系数=7021。
        日志/鱼巢_exception_20260617.低值g：
            最新写入仍为旧 2026-06-17 11:52:40；
            本次未新增 exception。
    SQL Server 当前投影：
        snapshot_id=4591fe63-318c-4050-8018-94d6408ed6d4；
        captured_at=2026-06-17 05:30:18 UTC；
        因果节点=6；
        果比较模板=6；
        主果比较模板=6；
        条件比较模板=3；
        证据动态样本=6；
        因比较模板=0；
        因方法模板=0。

禁止宣称：
    不宣称 D455 自然业务样本完整通过。
    不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
