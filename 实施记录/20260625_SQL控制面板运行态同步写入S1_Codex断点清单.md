# SQL 控制面板运行态同步写入 S1 Codex 断点清单

更新时间：2026-06-25

## 0. 恢复摘要

当前切片：

```text
计划：计划/20260625_SQL控制面板运行态同步写入计划_v0.1.md
切片：S1 运行态表结构与启动清库
目标：建立控制面板运行态 SQL 表，启动时清空运行态显示表，并写入本次运行批次。
```

已读取依据：

```text
docs/00_文档总索引.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260625_SQL控制面板运行态同步写入计划_v0.1.md
docs/工程图谱/00_图谱总索引.md
docs/工程图谱/05_规则原子索引.md
docs/工程图谱/08_事实来源与写入权限图谱.md
规范/自我线程与任务管理线程权责边界规范20260512.md
规范/线程生命周期状态上报与控制面板线程信息规范20260609.md
```

允许修改范围：

```text
数据库ADO模块.ixx
数据库ADO模块.cpp
鱼巢.cpp
说明书/函数功能说明文档.md（仅新增 / 删除 / 改名函数后由脚本刷新）
计划/20260625_SQL控制面板运行态同步写入计划_v0.1.md（仅补 S1 真实证据）
计划/计划索引.md（仅必要时更新当前停止点和证据入口）
本断点清单
```

禁止修改范围：

```text
任务 / 需求 / 方法 / 世界事实增删改提交点接入（留到 S3-S5）
控制面板 UI 重写或读取路径切换（留到 S6）
删除或替换既有全量 SQL 投影
把 SQL 作为业务权威源或恢复源
把 SQL 写失败变成业务结构回滚条件
```

当前状态：

```text
A0 预处理宏开关已提交：a64b948。
S1 已新增 ADO 运行态表初始化 / 启动清库入口，并在启动 SQL 投影链路中调用。
下一步：执行 Debug x64 构建，验证新增模块接口、启动调用和函数说明文档刷新结果。
```

已改文件：

```text
数据库ADO模块.ixx
数据库ADO模块.cpp
鱼巢.cpp
说明书/函数功能说明文档.md
实施记录/20260625_SQL控制面板运行态同步写入S1_Codex断点清单.md
```

构建前预测：

```text
Debug x64 构建应通过。
默认宏 鱼巢_开关_启用SQL控制面板同步写入=1 时，新增 ADO 初始化函数可参与编译。
若运行时 SQL Server 不可用，应只形成控制面板 SQL 显示镜像初始化失败警告，不回滚业务结构初始化。
```

运行前预测：

```text
命令：.\x64\Debug\鱼巢.exe --refresh-sql-projection
预期：命令进入 SQL 投影刷新入口；先尝试控制面板运行态 SQL 初始化，再执行旧全量 SQL 投影刷新。
若本机 SQL Server 可用，应出现“控制面板运行态SQL初始化完成”并写入运行ID。
若本机 SQL Server 不可用，应出现“控制面板运行态SQL初始化失败”警告，程序仍按业务初始化结果继续收口。
```

## 1. 验证结果

构建：

```text
命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：退出码 0；0 警告，0 错误。
```

真实运行：

```text
命令：.\x64\Debug\鱼巢.exe --refresh-sql-projection
结果：退出码 0。
控制台关键输出：SQL 投影已刷新。
```

日志证据：

```text
日志：日志/鱼巢_run_20260625.低值g
关键行：2026-06-25 16:31:57.662 [INF] 控制面板运行态SQL初始化完成 | 标记=鱼巢::main/命令行/刷新SQL投影 | 运行ID=108738AE-68CF-4F2E-A4AC-1D468B7228CE | 影响=已清空运行态显示镜像并写入本次运行批次
```

SQL 只读查询证据：

```text
控制面板运行批次=1
控制面板事件流水=1
当前任务显示项=0
当前需求显示项=0
当前方法显示项=0
当前世界显示项=0
当前动态显示项=0
SQL投影同步状态=1
运行批次：108738AE-68CF-4F2E-A4AC-1D468B7228CE;运行中;鱼巢::main/命令行/刷新SQL投影
```

当前判断：

```text
S1 已闭合。
SQL 仍只是控制面板显示镜像；未接入任务 / 需求 / 方法 / 世界事实提交点。
下一步应执行 S2：同步写入器最小接口。
```

禁止宣称：

```text
不得宣称实时同步写库已完整实现。
不得宣称数据库成为业务权威源。
不得宣称控制面板 SQL 显示等同业务闭环完成。
不得宣称自我苏醒完成或初步成熟完成。
```
