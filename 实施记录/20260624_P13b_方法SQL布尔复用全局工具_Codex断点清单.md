# 20260624 P13b 方法SQL布尔复用全局工具 Codex断点清单

## 0. 恢复摘要

当前切片已闭合。已删除 `方法类.cpp::私有_方法SQL布尔` 和 `方法类.cpp::私有_方法SQL字段布尔` 两个本地重复 helper，并将 6 个调用点改为复用 `全局共享函数类.ixx::布尔文本_一或零`。

依据：

```text
计划/计划索引.md：P13b 全局共享迁移。
计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md：B 类函数已有同功能全局函数则复用，不重复新增。
说明书/函数功能说明文档.md：`布尔文本_一或零` 保留为全局共享定义；方法类两个本地 SQL bool helper 已清零。
```

当前判断：

```text
`私有_方法SQL布尔(bool)` 与 `私有_方法SQL字段布尔(bool)` 均只返回 "1" / "0"。
`布尔文本_一或零(bool)` 返回同样的 const char*，可直接用于 stream 输出；字段 vector 处用 std::string 包装。
本切片只处理 SQL 投影文本 helper，不涉及方法机器判断。
```

验证证据：

```text
python .\tools\生成函数功能说明文档.py
结果：已生成 D:\鱼巢\说明书\函数功能说明文档.md；源码文件 127；函数条目 7175。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo
结果：已成功生成；0 个警告；0 个错误。
```

下一步：

```text
继续 P13b，从剩余 B 类低风险候选中选择下一个小函数族。
```

禁止宣称：不宣称 P13 完成，不宣称函数去重全部完成，不宣称自我苏醒完成或初步成熟完成。
