# 20260626 SQL 控制面板需求任务投影读窗口 Codex 断点清单

## 0. 恢复摘要

当前切片：修复控制面板读取需求面板节点时，运行态 SQL 投影刷新短窗口内表 / 视图被删除导致的加载失败。

根因：需求投影刷新已去掉自身 DROP 后，普通启动探针仍在第 45 秒出现 `对象名 '鱼巢.当前任务树节点' 无效`。需求面板视图依赖任务树当前视图，任务投影建表脚本仍在每次刷新时 DROP 当前任务视图和任务表。

已改文件：

```text
需求类.cpp
任务类.cpp
```

本次修改：

```text
1. 需求树 SQL 建表脚本不再在每次刷新时 DROP 需求表 / 当前视图。
2. 需求树 SQL 重写事务内增加同快照需求树节点数与需求主信息数一致性检查，不一致直接 THROW 51003。
3. 任务类.cpp 补文件头部规则注释模块。
4. 任务树 SQL 建表脚本不再在每次刷新时 DROP 任务表 / 当前视图。
5. 任务树 SQL 重写事务内增加同快照任务树节点数与任务主信息数一致性检查，不一致直接 THROW 51012。
```

验证证据：

```text
Debug x64 构建：0 警告，0 错误。

普通启动 80 秒 SQL 探针：
16 次查询全部 sqlcmd_exit=0。
最终需求树节点=9，需求主信息=9。
最终任务树节点=7，任务主信息=7。
探针期未再出现 当前任务树节点 / 当前需求面板节点 对象无效。

字段恢复比对：
.\x64\Debug\鱼巢.exe --refresh-sql-projection
退出码=0。
日志显示 任务树SQL投影完成 字段恢复比对=通过。
日志显示 需求树SQL投影完成 字段恢复比对=通过。
```

当前判断：控制面板需求面板读模型的主要读窗口堵点已收束到对象稳定存在；SQL 投影仍只是控制面板显示镜像，不作为需求 / 任务业务权威源。

禁止宣称：

```text
1. 不宣称全域数据库实时写入能力完整实现。
2. 不宣称控制面板 SQL 显示等同业务闭环完成。
3. 不宣称自我苏醒完成或初步成熟完成。
```
