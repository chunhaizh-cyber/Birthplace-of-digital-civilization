# 20260703 外设观察临时队列一次性消费 Codex 断点清单

## 0. 恢复摘要

当前切片：E1 / P3 / S1，外设观察材料稳定关联与入队。

目标：按已修订的 D1 / P3 口径，将旧 `generation` 核心身份方向改为分类临时队列、未消费读取、消费后退出可消费集合、过期清理和结构化来源回查。当前最小代码切片只处理外设观察报告队列的可消费读取和消费语义，不写世界树事实，不改四方法事实裁决。

依据文件：

```text
规范/详细设计/待完成/20260703_外设观察材料稳定关联与入队详细设计_v0.1.md
计划/20260703_外设观察材料稳定关联与入队实现映射计划_v0.1.md
计划/计划索引.md
规范/0050_项目通用结构与禁止性规则总纲_20260701.md
docs/工程图谱/05_规则原子索引.md
```

已确认入口：

```text
外设观察报告队列.ixx：提交外设观察报告、提交D455短期观察材料页、读取最新外设观察报告、读取外设观察报告队列视图、按等待项匹配外设观察报告、标记外设观察报告已消费
外设线程_D455深度相机.ixx：L0 / L1 报告生成和入队入口
```

禁止宣称：

```text
不宣称 D455 材料稳定入队完整闭合；
不宣称一次性消费已覆盖所有四方法；
不宣称观察材料质量收束已实现；
不宣称外设材料进入世界树事实。
```

下一步：

```text
读取外设观察报告队列相关函数小段；
最小修改读取函数，默认排除已消费材料；
检查等待项匹配是否已消费后不可复用；
运行函数说明文档生成、check_specs、Debug x64 构建。
```

## 1. S1 已执行修改

修改文件：

```text
外设观察报告队列.ixx
外设线程_D455深度相机.ixx
自我动作实现.外设模块.ixx
说明书/函数功能说明文档.md
```

实际修改：

```text
1. `读取最新外设观察报告(外设ID, 报告类型)` 改为默认只读取未消费报告。
2. 新增 `读取最新外设观察报告(外设ID, 报告类型, 包含已消费)` 重载；只有显式传入 `true` 的内部历史上下文读取才允许包含已消费报告。
3. D455 跨帧内部历史上下文读取显式传入 `true`，避免破坏 L1 稳定子集跨帧判断。
4. 自我侧正式外设材料读取视图改为 `读取外设观察报告队列视图(..., false)`，不再把已消费报告作为新的正式业务输入。
5. `读取外设观察报告_按ID` 新增 `包含已消费` 重载；原单参数入口保持历史回查语义，任务管理正式写任务输入路径改用 `false`，避免已消费报告按显式 ID 再次成为新任务输入。
6. `外设提交_generation` 改回中文函数名 `外设提交_材料组代际身份`；该值仍只保留为提交包头既有兼容字段，不作为材料身份或消费判断依据。
7. 重新生成 `说明书/函数功能说明文档.md`。
```

当前边界：

```text
本切片只关闭“默认读取、自我正式读取、任务输入按 ID 正式承接不复用已消费报告”的最小代码面。
`读取外设观察报告_按ID` 单参数入口仍可按显式 ID 回查已消费报告，用于 UI / 调试 / 内部世界历史材料回查；正式业务承接路径必须使用 `包含已消费=false` 或队列视图未消费读取。
短期材料页消费后退出可消费集合、L2/L3 来源链补强、真实 D455 烟测尚未完成。
```

## 2. S1 验证证据

已运行命令：

```powershell
python .\tools\生成函数功能说明文档.py
git diff --check -- 外设观察报告队列.ixx 外设线程_D455深度相机.ixx 自我动作实现.外设模块.ixx 说明书/函数功能说明文档.md 实施记录/20260703_外设观察临时队列一次性消费_Codex断点清单.md
python .\tools\check_specs.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

结果：

```text
函数说明文档生成：通过。
diff 空白检查：通过；仅提示 `说明书/函数功能说明文档.md` 下次 Git 触碰时 CRLF 将替换为 LF。
规范治理检查：hard issues 0，warnings 0。
Debug x64 构建：通过，0 warning，0 error，输出 `D:\鱼巢\x64\Debug\鱼巢.exe`。
```

## 3. 当前判断

```text
S1 最小代码切片：代码侧闭合，真实硬件烟测未执行。
已闭合：分类临时队列的默认最新读取不复用已消费报告；自我侧正式读取视图排除已消费报告；任务管理按 ID 写任务输入排除已消费报告；D455 内部跨帧历史读取显式标记为例外路径。
未闭合：短期材料页一次性消费、L2/L3 来源链、真实 D455 队列烟测。
下一步：按计划进入 S2，检查 L2/L3 来源链是否具备正式来源字段；若没有入口，停下修订详细设计或计划。
禁止宣称：不得宣称外设观察材料稳定关联与入队完整闭合；不得宣称四方法材料承接已实现一次性消费；不得宣称材料质量收束已实现。
```
