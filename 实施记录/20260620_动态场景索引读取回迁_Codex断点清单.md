# 20260620 动态场景索引读取回迁 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.2 P3.3h，回迁 `动态类::私有_查找已有状态动态` 和 `动态类::查找相邻前状态` 中的场景动态 / 状态索引读取路径。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
实施记录/20260620_状态清理索引回迁_Codex断点清单.md
```

本切片范围：

```text
1. 修改 动态类.cpp。
2. 私有_查找已有状态动态 改为调用 场景类::读取场景动态索引快照。
3. 动态类::查找相邻前状态 改为调用 场景类::读取场景状态索引快照。
4. 不新增、删除或改名函数。
```

验证前预测：

```text
1. 动态类.cpp 不再直接包含 场景索引同步.h。
2. 动态类.cpp 不再直接调用 借用场景索引全局互斥。
3. 动态类.cpp 不再直接读取 场景节点主信息类、场景主信息->动态索引、场景主信息->状态索引。
4. Debug x64 构建应通过。
5. 函数文档无需重生成，因为本切片未新增、删除或改名函数。
```

禁止宣称：

```text
不宣称动态类逻辑纠偏完成。
不宣称所有场景主信息直读已回迁。
不宣称场景类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 1. 修改结果

```text
已修改：
    动态类.cpp

修改内容：
    私有_查找已有状态动态 改为调用 场景类::读取场景动态索引快照。
    动态类::查找相邻前状态 改为调用 场景类::读取场景状态索引快照。
    动态类.cpp 移除不再使用的 场景索引同步.h include。
```

## 2. 验证结果

直读检查：

```powershell
rg -n "取主信息<场景节点主信息类>|场景主信息->|\.状态索引|\.动态索引|借用场景索引全局互斥|场景索引同步|读取场景动态索引快照|读取场景状态索引快照" 动态类.cpp
```

关键结果：

```text
动态类.cpp 只剩 场景类::读取场景动态索引快照 和 场景类::读取场景状态索引快照 调用。
```

构建验证：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

证据：

```text
日志/20260620_动态场景索引读取回迁_msbuild.log
退出码：0
已成功生成。
0 个错误
```

剩余复扫：

```text
日志/20260620_动态场景索引读取回迁_剩余场景主信息引用.txt
总命中：41
状态类.cpp：0
动态类.cpp：0
语素类.cpp：8
```

## 3. 下一步

```text
P3.3i 回迁 语素类.cpp 中 私有_场景索引包含 和 私有_把基础信息挂入场景索引 的场景索引检查 / 追加路径。
```
