# 20260606_外设四本能方法P1c扫描基准差异与值域冲突最小特征接入记录

## 1. 切片范围

```text
计划：计划/20260605_外设四本能方法需求树生成因果链修订计划_v0.1.md
切片：P1c 扫描基准差异与值域冲突最小特征接入
目标：给 自我_扫描已识别区域 补齐当前特征、基准差异、值域冲突的最小正式特征。
边界：不改变跟踪逻辑；首次扫描只写发现 / 当前特征，不把首次扫描写成变化动态。
```

## 2. 实施内容

```text
自我动作实现.外设模块.ixx：
    新增扫描正式特征：
        扫描基准特征状态
        已归属存在当前特征_基准特征差异状态
        已归属存在当前特征_基准特征差异值
        目标存在特征值域状态
        已归属存在特征值域冲突状态
        已归属存在特征值域冲突动态
    观察变化上下文新增：
        扫描基准特征状态
        基准差异状态 / 值
        目标特征值域状态
        值域冲突状态
    计算观察存在变化结果：
        有历史且匹配时写基准已取得、差异已生成、值域内、无冲突；
        有历史但冲突时写基准已取得、差异冲突、值域冲突、有冲突；
        缺历史时保持缺基准 / 未生成，不创建变化动态。
    自我_扫描已识别区域：
        结果表和结果能力接入基准差异 / 值域冲突特征；
        执行摘要写入上述特征；
        仅在有历史且值域冲突时写 已归属存在特征值域冲突动态。

特征类.cpp：
    初始化扫描基准特征状态、基准差异状态、基准差异值、目标存在特征值域状态、已归属存在特征值域冲突状态。
```

## 3. 验证

```text
git diff --check -- 自我动作实现.外设模块.ixx 特征类.cpp 实施记录/20260605_外设四本能方法P0_Codex断点清单.md
    退出码 0；仅 LF/CRLF 提示。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码 0；
    0 个错误；
    1 个 LNK4075 链接警告：因 /INCREMENTAL:NO 忽略 /EDITANDCONTINUE。
```

## 4. 当前判断

```text
P1c 代码切片完成。
扫描已有基准差异和值域冲突的最小正式特征和结果能力。
首次扫描仍不写变化动态。
本次未真实运行鱼巢.exe，不宣称四方法自然业务链已通过。
```

## 5. 下一步

```text
进入 P1d：跟踪预测误差与丢失 / 重捕获最小特征接入。
P1d 不替代全场扫描，不把跟踪证据当成全场明确。
```
