# 需求树简化与内部治理闭环 P6 Codex断点清单

日期：2026-06-30

## 0. 恢复摘要

当前切片：R24 P6 安全 / 服务治理主轴裁决。

本轮目标：

```text
把“安全值高提升服务优先级，安全值低提升安全优先级并降低服务优先级”
落实为调度 / 任务化扫描顺序的治理主轴。

服务根目标值仍由抽象根特征定义读取最大值，
不得随安全值升降而改写。
```

本轮边界：

```text
不写服务根目标值。
不写服务对象事实、服务目标明确事实、G0 或 D2。
不新增持久结构体。
不碰 `自我线程模块.impl.cpp` 中已有的独立树扫描未提交改动。
```

## 1. 现状核查

```text
1. `私有_从需求树构建需求列表` 已读取安全 / 服务抽象根特征定义高值作为根目标值。
2. `自我类::应用按双值重算服务衰减步长` 已按服务值和安全值重算服务衰减步长。
3. `私有_选择当前主任务候选_按任务优先级` 与
   `私有_提交活动需求任务化意图_线程侧` 现状固定先扫描服务优先列表再扫描安全优先列表。
4. 固定先扫服务列表会在低安全且分数相同的样本中形成服务优先偏置，
   与 P6 “低安全样本下安全列表优先”的验收预测不一致。
```

## 2. 计划改动

```text
1. 新增只读普通函数：
   `私有_安全服务治理主轴服务优先_线程侧`。
2. 新增日志文本 helper：
   `私有_安全服务治理主轴文本_线程侧`。
3. 在任务化意图提交和主任务候选选择两处，
   根据治理主轴选择扫描顺序：
       服务优先：服务列表 -> 安全列表；
       安全优先：安全列表 -> 服务列表。
4. 日志记录当前安全值、服务值、安全压力、服务压力和
   `服务根目标值写入=0`。
```

## 3. 验收预测

```text
低安全或安全压力大于 0 时，扫描顺序应为安全列表优先。
安全压力为 0 时，扫描顺序应为服务列表优先。
服务根目标值仍保持 `自我_服务值 最大值`，本切片不写目标值。
```

## 4. 已完成修改

```text
1. `自我线程模块.impl.cpp` 新增：
   `私有_安全服务治理主轴服务优先_线程侧`；
   `私有_安全服务治理主轴文本_线程侧`；
   `私有_记录安全服务治理主轴裁决_线程侧`。
2. `私有_提交活动需求任务化意图_线程侧`：
   根据治理主轴选择服务列表 / 安全列表扫描顺序。
3. `私有_选择当前主任务候选_按任务优先级`：
   根据治理主轴选择服务列表 / 安全列表扫描顺序。
4. 裁决日志显式记录：
   安全值、服务值、安全压力、服务压力、影响排序 / 调度、
   `服务根目标值写入=0`、`G0写入=0`、`D2写入=0`。
5. `说明书/函数功能说明文档.md` 已同步，函数条目=7520。
```

## 5. 待运行验证

```text
git diff --check -- 自我线程模块.impl.cpp 说明书/函数功能说明文档.md 实施记录/20260630_需求树简化与内部治理闭环_P6_Codex断点清单.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

## 5.1 验证进展

```text
python .\tools\生成函数功能说明文档.py
结果：成功；函数条目=7520。

git diff --check -- 自我线程模块.impl.cpp 说明书/函数功能说明文档.md 实施记录/20260630_需求树简化与内部治理闭环_P6_Codex断点清单.md
结果：通过；仅提示函数说明文档 CRLF 下次触碰会转 LF。

构建锁检查：
    `.codex_locks/build-run.lock` 存在。
    owner=Codex
    action=run
    project=鱼巢
    pid=33760
    timestamp=2026-06-30T16:22:54.2491905+08:00

PID 检查：
    `Get-Process -Id 33760` 未返回进程。

处理：
    判定为进程已退出后的过期锁；
    允许删除锁后重新获取构建锁。

构建：
    已删除过期锁并重新获取构建锁。
    命令：
        msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    日志：
        日志/msbuild_R24_P6_20260630.log
    结果：
        退出码 1。
        ClCompile / Link 显示所有输出均为最新。
        PostBuildEvent 复制 `realsense2.dll` / `lz4.dll` 时失败。
        0 个警告，1 个错误。
    关键错误：
        The process cannot access the file because it is being used by another process.

占用复核：
    当前仍有 `鱼巢.exe` 进程运行：
        PID=22152
        StartTime=2026/6/30 16:23:04
    `.codex_locks/build-run.lock` 当前不存在。

阻塞判断：
    默认 Debug x64 输出目录被其它窗口运行进程占用；
    不能抢占或杀掉该进程；
    P6 代码切片已通过静态检查，但默认构建未闭合。

复跑构建：
    复核 `Get-Process -Id 22152` 已无进程；
    `.codex_locks/build-run.lock` 不存在；
    已重新获取构建锁。
    命令：
        msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    日志：
        日志/msbuild_R24_P6_retry_20260630.log
    结果：
        退出码 0。
        已成功生成。
        0 个警告，0 个错误。
    判断：
        P6 原构建堵点已消除；继续执行真实运行日志核对。
```

## 5.2 真实运行预测

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=90000

预测：
    运行日志应出现 `自我线程/安全服务治理主轴裁决`。
    低安全或安全压力大于 0 时，裁决主轴应为安全优先。
    日志必须显式显示：
        服务根目标值写入=0
        G0写入=0
        D2写入=0
    该运行只验证治理主轴排序 / 调度日志，不宣称内部治理闭环完成。
```

## 5.3 真实运行结果

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=90000

控制台日志：
    日志/run_R24_P6_self_check_20260630.log

项目运行日志：
    日志/鱼巢_run_20260630.低值g

工具状态：
    外层 shell 超过 180 秒超时；
    复核后 `鱼巢.exe` 已退出；
    复核后锁中脚本 PID 已不存在；
    已删除过期 `.codex_locks/build-run.lock`。

运行收口证据：
    18:32:31 程序退出守卫开始关闭控制面板；
    18:33:32 自我线程已停止；
    18:33:43 世界树 / 任务树 SQL 投影刷新成功；
    18:33:44 方法树 SQL 投影刷新成功；
    18:33:55 语素 SQL 投影刷新成功；
    18:33:56 需求树 SQL 投影刷新成功。

P6 预测核对：
    18:30:57 自我线程/安全服务治理主轴裁决：
        调用点=主任务候选选择；
        主轴=安全优先；
        服务根目标值写入=0；
        G0写入=0；
        D2写入=0。
    18:31:05 自我线程/安全服务治理主轴裁决：
        调用点=活动需求任务化意图；
        主轴=安全优先；
        服务根目标值写入=0；
        G0写入=0；
        D2写入=0。
    18:31:07 自我线程/活动需求任务化意图：
        已提交=4；
        治理主轴=安全优先。

判断：
    P6 安全 / 服务治理主轴裁决代码切片已取得工程守卫和真实运行日志证据；
    只证明治理主轴影响排序 / 调度且未写服务根目标、G0、D2；
    不宣称内部治理闭环完成。
```

## 6. 提交边界

```text
`自我线程模块.impl.cpp` 存在其它窗口未提交改动：
    `私有_按主键扫描独立树` 扫描顺序调整。

该改动不是 P6 工作内容。
P6 提交时必须只暂存本切片相关 hunks：
    安全 / 服务治理主轴 helper；
    活动需求任务化意图扫描顺序；
    主任务候选选择扫描顺序；
    P6 断点清单；
    函数说明文档。
```

## 7. 禁止宣称项

```text
不得宣称内部治理闭环已完成。
不得宣称服务衰减 / 休眠全公式已完成。
不得宣称服务业务 D0 闭环已完成。
```
