# 20260616 函数功能说明文档 Codex 断点清单

## 0. 恢复摘要

当前切片：建立项目函数功能说明文档，并固化“新建函数前先查文档、跨模块共享函数放入全局共享函数模块”的规则。

依据文件：

```text
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
AGENTS.md
```

已改文件：

```text
AGENTS.md
全局共享函数类.ixx
鱼巢.vcxproj
鱼巢.vcxproj.filters
tools/生成函数功能说明文档.py
说明书/函数功能说明文档.md
实施记录/20260616_函数功能说明文档_Codex断点清单.md
```

已运行命令：

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

关键退出码：

```text
python 生成命令退出码：0
Debug x64 构建退出码：0
```

关键证据：

```text
源码文件：130
函数条目：7348
已在 鱼巢.vcxproj 登记的函数条目：7110
需要后续人工复核的名称推断条目：7265
```

当前判断：

```text
函数功能说明文档已生成。
文档已注明新建函数前先搜索本文档。
全局共享函数类.ixx 已创建并加入 vcxproj / filters。
生成器已排除 third_party、构建目录、日志目录和运行输出目录。
Debug x64 构建已通过。
```

下一步：

```text
后续新建函数前先查 说明书/函数功能说明文档.md。
新增、删除或改名函数后重新运行 tools/生成函数功能说明文档.py。
```

禁止宣称：

```text
不宣称自我苏醒完成。
不宣称初步成熟完成。
不宣称本次函数说明已经人工逐条语义复核；名称推断条目仍需后续随函数维护逐步精修。
```
