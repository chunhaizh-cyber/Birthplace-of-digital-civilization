# 20260627 场景体素视觉先验融合 S4 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260625_场景体素视觉先验融合实现计划_v0.1.md` / S4 6146 扫描先接入。

S0-S3 已完成：

```text
S0：规范准入，提交 91deceb。
S1/S2：场景体素只读快照和结构化缺口，提交 532684f。
S3：视觉观察融合模块，提交 04a7e6f。
```

S4 目标：

```text
6146 扫描主链生成并读取 SceneVoxelPrior；
已合法外设数据项增加先验版本、先验匹配评分、深度一致评分和视觉解释类型；
运行存在写入先验版本、匹配评分、自由空间冲突候选数量、遮挡候选数量、视觉信息缺口数量等工程证据；
未覆盖解释至少能区分自由空间冲突、遮挡候选、深度缺失和证据不足；
不直接把未覆盖写成消失事实。
```

允许修改：

```text
自我动作实现.扫描.ixx
说明书/函数功能说明文档.md
实施记录/20260627_场景体素视觉先验融合S4_Codex断点清单.md
计划/20260625_场景体素视觉先验融合实现计划_v0.1.md
计划/计划索引.md
```

禁止修改：

```text
6150 提交入口
6145 识别
6147 跟踪
外设线程
世界树 / 需求树 / 动作动态底层写入逻辑
```

## 1. 前置核查

```text
`自我动作实现.扫描.ixx` 当前缺文件头部规则注释模块；本切片触碰前需先补最小头部规则注释。
`结构_已合法外设数据项` 是扫描模块私有结构，可在本切片内补字段。
`视觉观察融合模块` 已提供 `从扫描提交包提取合法外设材料` 和 `融合观察域结果`。
```

## 2. 预测

```text
S4 修改后，扫描模块能编译并引用 `场景体素模块`、`视觉观察融合模块`；
运行存在能写入先验版本和融合统计特征；
构建通过只证明扫描链代码接入，不证明真实扫描业务闭合。
```

## 3. 待验证命令

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

## 4. 实施结果

```text
已补 `自我动作实现.扫描.ixx` 文件头部规则注释模块。
扫描模块已 import `场景体素模块` 和 `视觉观察融合模块`。
扫描主链生成只读 SceneVoxelPrior，并把先验版本、坐标系版本、可用状态、入选存在数量和缺口数量写入运行存在 / 输出结果场景。
扫描提交包经 `视觉观察融合类::从扫描提交包提取合法外设材料` 后进入 `融合观察域结果`。
已合法外设数据项记录先验版本、先验坐标系版本、先验匹配评分、深度一致评分和视觉解释类型。
未覆盖存在不再直接写成 `消失候选`；当前按融合结果映射为自由空间冲突、遮挡候选、深度缺失或证据不足。
当 SceneVoxelPrior 缺坐标系版本、缺三维体素特征类型或缺入选存在时，只登记结构化缺口 / 证据不足，不把自由空间冲突当事实。
```

## 5. 验证结果

```text
python .\tools\生成函数功能说明文档.py
退出码：0
结果：已生成 `D:\鱼巢\说明书\函数功能说明文档.md`；函数条目 7290。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
退出码：0
结果：Debug x64 构建通过，0 警告，0 错误。
```

## 6. 当前判断

```text
S4 已形成代码接入闭合。
构建通过只能证明 6146 扫描链已能读取 / 记录场景体素先验和视觉融合工程证据；
不能证明真实视觉业务闭合、扫描需求自然生长闭合或 D455 自然业务闭合。
```

## 7. 下一步

```text
返回计划索引，进入 S5：6150 提交入口版本闸门。
S5 才允许触碰 `自我动作实现.外设模块.ixx`，仍不得改变观察证据裁决边界。
```
