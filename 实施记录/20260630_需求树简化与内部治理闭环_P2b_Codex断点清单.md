# 需求树简化与内部治理闭环 P2b Codex断点清单

日期：2026-06-30

## 0. 恢复摘要

当前切片：R24 P2b，被动因果信息闭环普通治理函数补齐。

本轮目标：

```text
在不新增结构体、不创建世界树事实、不改变因果生成逻辑的前提下，
给 `因果类` 补只读函数，用于读取被动因果闭环状态、来源动态完整性、
上游主动因果缺口、状态变化线索状态和根闭环来源可追溯状态。
```

当前状态：代码已编辑，尚未完成构建验证。若构建失败，优先修复本切片新增接口或调用依赖，不回滚既有未提交改动。

## 1. 本轮修改文件

```text
因果类.h
因果类.cpp
说明书/函数功能说明文档.md（待运行生成脚本后更新）
实施记录/20260630_需求树简化与内部治理闭环_P2b_Codex断点清单.md
```

## 2. 已完成修改

```text
1. `因果类.h` 补最小文件头部规则注释模块。
2. 新增 `验证被动因果来源动态完整`。
3. 新增 `读取被动因果上游来源`。
4. 新增 `检查状态变化因果是否只可作为线索`。
5. 新增 `检查根闭环因果来源是否可追溯`。
6. 新增 `读取被动因果闭环状态`。
7. 新增内部 helper：被动占位动作识别、首选动作语义读取、主动动作来源判定。
```

## 3. 预测

```text
1. `因果类` 新接口只读，不会创建或修改因果节点。
2. 被动动作语义 `被动动作` 和 `被动触发.*` 不会被判为上游主动因果。
3. 无主动动作来源的状态变化因果会被标记为只可作为线索。
4. 证据动态必须能回解初始状态、结果状态、状态主体和状态特征，才算来源动态完整。
5. 代码可能需要修正的风险点是 const / noexcept / 引用类型匹配。
```

## 4. 待运行验证

```text
python .\tools\生成函数功能说明文档.py
git diff --check
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

## 4.1 已运行验证

```text
python .\tools\生成函数功能说明文档.py
结果：成功；函数条目=7499；新增 5 个因果类定义和 5 个声明已进入 `说明书/函数功能说明文档.md`。

git diff --check -- 因果类.h 因果类.cpp 说明书/函数功能说明文档.md 实施记录/20260630_需求树简化与内部治理闭环_P2b_Codex断点清单.md
结果：通过；仅提示函数说明文档 CRLF 下次触碰会转 LF。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
日志：日志/20260630_R24_P2b_msbuild_Debug_x64.log
退出码：0
关键行：已成功生成。

msbuild 复核：
中间日志：日志/20260630_R24_P2b_msbuild_Debug_x64_recheck.log
退出码：1
原因：为避免纳入既有未提交基础信息选择链，曾误把 `按主键解析因果节点` 中的 `私有_选择基础信息` 改回旧符号，导致 `获取基础信息集` 找不到标识符。
处置：恢复当前工作树的 `私有_选择基础信息` 写法。
最终日志：日志/20260630_R24_P2b_msbuild_Debug_x64_recheck2.log
最终退出码：0

.\x64\Debug\鱼巢.exe --demand-tree-growth-summary-live
日志：日志/20260630_R24_P2b_demand_summary_live_stdout.log
退出码：0
锁处理：运行前发现旧 `.codex_locks/build-run.lock` 无活进程承接，按执行规则清理后运行。
```

运行输出关键证据：

```text
需求树生长摘要：目标绑定完整=5，异常=0；任务承接任务数=2，任务头=2。
当前主需求：有任务=是，普通筹办=是。
任务管理：请求=0，工作项=0，上行=0，待筹办=0，待执行=0。
```

最新运行日志关键证据：

```text
自我类/根任务确认 | 安全根任务=1 | 服务根任务=2
自我线程/根闭环信息模板生成 | 根=2 | 根特征=服务 | 直接生成需求=0 | 直接创建任务=0 | 直接创建方法头=0
自我线程/服务需求因果链等待直接服务触发事实 | 直接服务需求生成=0 | 间接服务维护需求允许=1 | 创建占位方法头=0
任务筹办跟踪 | 因果路径筹办查询壳统计 | 直接写需求树=0 | 实际发送派生需求消息=0
任务筹办跟踪 | 候选集填充跳过任务完成后处理方法
任务筹办跟踪 | 候选集填充跳过安全服务值空占位方法
任务筹办跟踪 | 无既有候选方法进入挂起 | 创建占位方法头=0 | 直接生成子需求=0
```

## 4.2 当前收束判断

```text
1. P2b 代码补齐已完成并通过构建。
2. 新接口尚未改变现有运行路径，只提供后续 P3/P5/P6 可复用的普通只读治理函数。
3. P2 的被动因果口径已具备代码读取入口：可读取来源动态完整性、缺上游主动因果、状态变化线索和根闭环来源可追溯状态。
4. P2 仍不等于内部治理总闭环完成；下一步进入 P3 主动因果信息生成链路。
```

## 5. 禁止宣称项

```text
不得宣称需求树简化生成已闭环。
不得宣称内部治理闭环已完成。
不得把被动因果当作主动方法来源。
不得把状态变化线索直接标记为已验证因果。
```
