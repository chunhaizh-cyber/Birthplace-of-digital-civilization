# 20260620 状态排序时间读取函数 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_状态类逻辑纠偏计划_v0.1.md` 的 1.4，新增 `状态类::读取状态排序时间(const 状态节点类*) const noexcept` 只读函数。

来源返回点：

```text
计划/20260620_场景类逻辑纠偏计划_v0.1.md 的 1.3 P4.3：
复核 场景类::追加状态 中 状态节点主信息类 时间排序读取路径。
```

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
计划/20260620_状态类逻辑纠偏计划_v0.1.md
说明书/函数功能说明文档.md
```

## 1. 问题锚点

```text
场景类::追加状态 当前在排序状态索引时直接读取 状态节点主信息类::发生时间 / 收到时间。
状态排序时间属于状态节点事实解释，应由 状态类 提供只读函数。
```

已复核：

```text
说明书/函数功能说明文档.md 当前只列出 状态类::私有_取状态排序时间，未列出公开 读取状态排序时间。
状态类.h/.cpp 当前已有私有排序时间辅助，但调用方不能复用。
```

当前判断：

```text
需要新增 状态类::读取状态排序时间(const 状态节点类*) const noexcept。
该函数只读解析状态主信息，不创建状态，不修改状态值；状态不存在或主信息不存在时返回 0。
闭合后返回场景类 P4.3，用该函数替换 场景类::追加状态 的状态主信息直读。
```

验证前预测：

```text
1. 状态类.h/.cpp 新增公开只读函数 读取状态排序时间。
2. 本切片不修改 场景类.cpp。
3. 函数文档生成应通过。
4. Debug x64 构建应通过。
```

## 2. 当前执行进展

已修改：

```text
1. 状态类.h 新增公开函数声明：读取状态排序时间(const 状态节点类* 状态) const noexcept。
2. 状态类.cpp 新增函数定义，内部通过 取状态主信息 只读解析状态主信息，并复用 私有_取状态排序时间。
3. 状态为空或主信息不存在时返回 0，不创建或修改任何状态。
```

已验证：

```text
rg "读取状态排序时间|状态节点主信息类|私有_取状态排序时间" 状态类.h 状态类.cpp
结果：公开声明 / 定义存在；状态主信息读取仍限定在状态类内部。

python .\tools\生成函数功能说明文档.py
结果：exit=0，输出见 日志/20260620_状态排序时间读取函数_函数文档.log。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：exit=0，构建日志见 日志/20260620_状态排序时间读取函数_msbuild.log。
```

禁止宣称：

```text
不宣称场景类 P4.3 已闭合。
不宣称状态类逻辑纠偏完成。
不宣称场景类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
