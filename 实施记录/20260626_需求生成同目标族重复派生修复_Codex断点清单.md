# 需求生成同目标族重复派生修复 Codex 断点清单

## 0. 恢复摘要

当前切片：修复需求生成中同一目标宿主、同一目标特征反复派生子需求的问题。

根因判断：

```text
任务筹办的因果条件缺口过滤只在目标状态相同的时候跳过自目标。
当父需求和条件缺口同为 v6.方法状态，但目标状态不同，筹办仍会上报派生需求草案。
入树侧随后按父子目标自复制拦截并生成开发诊断弹窗，说明下游闸门正确，但上游生成侧仍有重复草案源头。
```

本次修改：

```text
1. 自我线程入树侧保留父链同族目标闸门：父链任一未截止需求与候选同目标宿主、同目标特征，即归类为父子目标自复制。
2. 任务筹办上游过滤改为同目标族判定：来源需求目标宿主 + 目标特征与条件缺口一致时直接跳过，不比较目标状态和满足关系。
3. 任务模块.筹办.ixx 补最小文件头部规则注释模块。
```

已改文件：

```text
自我线程模块.impl.cpp
任务模块.筹办.ixx
实施记录/20260626_需求生成同目标族重复派生修复_Codex断点清单.md
```

验证：

```text
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：成功，0 警告，0 错误。

.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000
结果：进程 75 秒窗口未自行退出，已停止进程。
日志窗口：日志/鱼巢_run_20260626.低值g 13:16 后未出现新的 父子目标自复制保护 或 需求入树代码缺陷型拒绝。
异常日志：日志/鱼巢_exception_20260626.低值g 最后相关记录仍为 13:02 旧诊断，未新增同类异常。
本轮真实运行走到 21J 分支，未复现原 20Q/v6/5s 样本；验证结论按“未新增同类异常，原样本未完全复现”记录。
```

禁止宣称：

```text
不得宣称需求生成治理闭环完整实现。
不得宣称数据库实时同步完整实现。
不得宣称自我苏醒或初步成熟完成。
```
