# 20260627 P3 任务筹办调用边界收口 Codex断点清单

## 0. 恢复摘要

当前切片：

```text
P1c 因果链驱动扫描需求自然生长计划 v0.1 / P3
任务筹办调用边界收口。
```

只读核查结论：

```text
P3 未发现需要修改代码的边界缺口。

1. `任务模块.筹办.ixx` 未命中 `因果节点->主信息`、`因果->主信息`
   或 `取因果主信息` 一类直接拆读因果主信息路径。

2. 因果筹办查询通过封装入口消费材料：
      世界树.查询主果方向命中因果(...)
      世界树.读取因果证据状态(...)
      世界树.枚举因果条件目标状态(...)
      世界树.存在因果条件模板(...)

3. “扫描 / 观察 / 外设”命中主要属于：
      观察质量缺口状态值域；
      可作为练习目标的本能方法枚举白名单；
      外设观察报告 ID 输入参数补齐。
   未发现按自然语言词硬编码扫描派生链。

4. 派生需求消息草案字段完整性检查覆盖：
      父需求
      派生需求类型
      因果路径生成类型
      阻塞级别
      目标特征类型
      目标主体
      当前状态
      目标状态
      满足关系
   草案构造保留来源因果、父需求、目标主体、目标特征、当前状态、目标状态。

5. 入树拒绝和重复派生由自我线程结构化承接：
      父子目标自复制保护；
      同目标子需求复用；
      派生需求入树拒绝回执构造；
      拒绝回执治理处理和账本登记。
```

证据锚点：

```text
任务模块.筹办.ixx
    rg `因果.*主信息|因果节点->主信息|因果->主信息|取因果主信息`
        仅命中统计字段 `有主信息数量 = 因果节点数量`，不是拆读因果主信息。

任务模块.筹办.ixx:1951-2078
    `执行因果路径查询壳(...)` 通过世界树封装查询因果、读取证据状态并吸收为临时视图。

任务模块.筹办.ixx:1261-1321
    `派生消息字段完整_筹办查询(...)` 和
    `构造正式派生需求消息草案_筹办查询(...)` 覆盖 P3 要求字段。

任务模块.筹办.ixx:2420-2505
    `本能方法ID可作为练习目标_筹办(...)` 是枚举能力白名单，不是字符串词派生链。

任务模块.筹办.ixx:4224-4281
    `补齐外设观察报告ID输入参数_筹办(...)` 只补输入参数，
    日志明确 `直接写需求满足=0`、`直接写世界真值=0`。

自我线程模块.impl.cpp:10160-10300
    父子目标自复制保护和同目标子需求复用。

自我线程模块.impl.cpp:9859-10568
    派生需求入树拒绝回执构造、治理处理和回执日志。
```

当前判断：

```text
P3 只读收口闭合。
下一步进入 P4：真实运行验证。
但 P4 的扫描需求自然生长业务验证需要自然命中稳定子集扫描约束；
若真实运行仍未命中，必须按自然样本待条件处理。
```

禁止宣称：

```text
不宣称扫描需求自然生长业务闭合。
不宣称 P2c 派生需求自然上行已命中。
不宣称自我苏醒完成或初步成熟完成。
```
