# 20260627 P13b 文本计数摘要工具迁移 Codex断点清单

## 0. 恢复摘要

当前切片目标：

```text
P13b：从剩余 B 类低风险候选中处理一个小函数族。
对象：
    控制面板类.cpp::私有_累计文本计数(std::vector<std::pair<std::string, std::size_t>>&, std::string)
    控制面板类.cpp::私有_文本计数摘要(std::vector<std::pair<std::string, std::size_t>>, std::size_t)
```

依据：

```text
计划/计划索引.md：P13b 继续选择低风险 B 类小函数族。
计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md：B 类纯工具函数迁入 全局共享函数类.ixx。
全局共享函数类.ixx 文件头规则：无领域自定义主信息 / 领域类型参与的通用函数可进入本模块。
```

Graph RAG 与源码复核：

```text
search_code：
    定位到 控制面板类.cpp:2822-2876 的文本计数统计 / 摘要 helper；
    定位到 2948、2950、2962、2964、2972、2974、2982、2984 的调用点。
    MCP 中文符号输出存在问号化，qualified name 不作为直接事实来源。

rg 源码复核：
    `私有_累计文本计数` 1 个本地定义、4 个控制面板调用点；
    `私有_文本计数摘要` 1 个本地定义、4 个控制面板调用点；
    函数只维护文本计数统计和展示摘要，不读取 / 写入项目结构。
```

预测结果：

```text
迁移后：
    全局共享函数类.ixx 新增 累计文本计数(std::vector<std::pair<std::string, std::size_t>>&, std::string)。
    全局共享函数类.ixx 新增 文本计数摘要(std::vector<std::pair<std::string, std::size_t>>, std::size_t)。
    删除控制面板类本地 helper。
    控制面板调用方改用全局函数。

验证：
    函数说明文档刷新成功。
    Debug x64 构建成功。
    源码与函数说明文档不再出现本地 `私有_累计文本计数` / `私有_文本计数摘要` 定义。
```

禁止宣称：

```text
不宣称 P13 完成。
不宣称函数去重全部完成。
不宣称自我苏醒完成或初步成熟完成。
```

## 1. 执行结果

已完成：

```text
新增：
    全局共享函数类.ixx::累计文本计数(std::vector<std::pair<std::string, std::size_t>>&, std::string)
    全局共享函数类.ixx::文本计数摘要(std::vector<std::pair<std::string, std::size_t>>, std::size_t)

删除：
    控制面板类.cpp::私有_累计文本计数(std::vector<std::pair<std::string, std::size_t>>&, std::string)
    控制面板类.cpp::私有_文本计数摘要(std::vector<std::pair<std::string, std::size_t>>, std::size_t)

替换：
    控制面板类.cpp 内基础信息 / 需求 / 任务 / 方法子层摘要的 8 个统计和摘要调用点改用全局函数。
```

文档：

```text
已运行 python .\tools\生成函数功能说明文档.py。
第一次保留默认参数时，`文本计数摘要` 未被生成器记录；
改为显式 `展示上限` 参数后重新生成。
结果：源码文件 127 个，函数条目 7213。
```

验证：

```text
rg 残留扫描：
    `私有_累计文本计数` / `私有_文本计数摘要` 在源码和函数说明文档中无命中。
    `累计文本计数` / `文本计数摘要` 命中全局共享函数类定义、控制面板调用点和函数说明文档。

git diff --check：
    未发现空白错误；仅有既有 CRLF/LF 提示。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo
    结果：已成功生成，0 个警告，0 个错误。
```

当前判断：

```text
本切片闭合。
两个新 helper 是纯显示文本统计 / 摘要工具，不读取 / 写入项目结构，不承载业务判断。
后续 P13b 继续从低风险 B 类候选中选择下一个小函数族。
```
