# 消息中间件清理归档策略 Codex 断点清单

更新时间：2026-06-09 15:52 +08:00

## 当前切片

```text
计划：计划/20260609_线程状态控制面板消息收口实施计划_v0.1.md
范围：P7 消息中间件清理与归档策略。
目标：对 线程生命周期消息模块 的消息中间件文件建立最小清理 / 归档规则：
    根目录有效正式 .msg 不按时间清理；
    已消费有价值消息归档后删除；
    普通已消费消息短期保留后删除；
    陈旧 .tmp 和无效消息保留期后删除。
```

## 依据文件

```text
AGENTS.md
规范/规范目录.md
规范/线程生命周期状态上报与控制面板线程信息规范20260609.md
计划/计划索引.md
计划/20260609_线程状态控制面板消息收口实施计划_v0.1.md
实施记录/20260609_线程状态控制面板消息收口_Codex断点清单.md
```

## 已改文件

```text
线程生命周期消息模块.ixx
规范/线程生命周期状态上报与控制面板线程信息规范20260609.md
计划/20260609_线程状态控制面板消息收口实施计划_v0.1.md
实施记录/20260609_消息中间件清理归档策略_Codex断点清单.md
```

## 禁止混入

```text
不删除根目录中有效且尚未确认消费的 thread_lifecycle_*.msg。
不把归档作为任务状态、需求、世界事实、动作动态或线程信息表权威来源。
不让业务线程发送消息时同步等待归档 / 清理。
不新增 mock、伪入口、伪线程样本或伪动作动态。
不处理既有执行桥 debug error 弹窗；该问题另开切片。
不宣称自我苏醒完成、初步成熟完成或内部循环整体完成。
```

## 本轮验证预测

```text
预测时间：2026-06-09 15:52 +08:00
预测 1：Debug x64 构建应通过；若仅出现 /EDITANDCONTINUE 与 /INCREMENTAL:NO 相关 LNK4075，不作为失败。
预测 2：运行 `.\x64\Debug\鱼巢.exe --panel-html` 会触发控制面板消费路径和消息中间件维护。
预测 3：`消息中间件/已消费` 中有价值线程消息会追加到 `消息中间件/归档/thread_lifecycle_YYYYMMDD.archive`，归档记录包含 `记录类型=线程生命周期消息`。
预测 4：`消息中间件/已消费` 中旧普通状态消息会被删除，旧有 613 个已消费消息文件数量应明显下降。
预测 5：日志出现 `线程生命周期消息/消息中间件维护`，且不出现 `线程生命周期消息/归档失败`、`消息中间件删除失败`。
预测 6：控制面板 HTML 仍显示消息中间件项目线程快照，包含 self-main、task-interface、task-worker-pool、task-worker-1、control-panel-summary，不显示 OS / 系统线程枚举。
```

## 下一步

```text
1. 运行 Debug x64 构建。
2. 运行 panel-html 触发消费 / 维护。
3. 检查已消费目录数量、归档文件、维护日志和控制面板 HTML。
4. 用真实结果补写本断点清单和实施计划。
5. 若闭合，按 Git 规则只提交本切片相关文件。
```

## 本轮实际结果

```text
实际时间：2026-06-09 16:01 +08:00

代码结果：
    线程生命周期消息模块新增消息中间件维护逻辑。
    控制面板消费路径在应用消息、写入线程信息表缓存、移动已消费文件后触发维护。
    根目录有效正式 thread_lifecycle_*.msg 不按时间清理；陈旧 .tmp 和无效消息按保留期删除。
    已消费目录中的有价值消息追加到 消息中间件/归档/thread_lifecycle_YYYYMMDD.archive 后删除原已消费文件。
    普通已消费状态消息短期保留后删除。
    归档分桶时间以可信发生时间为准；早于 2020-01-01 的旧消息发生时间只保留在原始记录中，归档分桶改用归档当天。

文档结果：
    002002 规范新增消息中间件维护规则 9-15。
    实施计划新增 P7 消息中间件清理与归档策略和验证证据项。
```

## 本轮验证证据

```text
差异检查：
    git diff --check -- 线程生命周期消息模块.ixx 规范/线程生命周期状态上报与控制面板线程信息规范20260609.md 计划/20260609_线程状态控制面板消息收口实施计划_v0.1.md 实施记录/20260609_消息中间件清理归档策略_Codex断点清单.md
    结果：Exit 0。

构建命令：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    结果：三次 Exit 0；仅有 D455相机类.obj LNK4075 (/EDITANDCONTINUE 被 /INCREMENTAL:NO 忽略) 警告。

真实运行命令：
    .\x64\Debug\鱼巢.exe --panel-html
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=1000
    .\x64\Debug\鱼巢.exe --panel-html
    结果：三次均 Exit 0；未新增 debug error 弹窗。

目录证据：
    维护前 消息中间件/已消费：613 个文件。
    维护后 消息中间件/已消费：0 个文件。
    最新 panel-html 退出后 消息中间件 根目录保留 2 个正式 thread_lifecycle_*.msg；判断为退出前新写未消费消息，维护逻辑未删除。
    消息中间件/归档/thread_lifecycle_20260609.archive 存在，包含 记录类型=线程生命周期消息 668 条。

日志证据：
    日志/鱼巢_run_20260609.低值g 最新维护摘要：
        2026-06-09 15:56:03 归档=615，归档失败=0，删除失败=0。
        2026-06-09 15:58:25 归档=4，归档失败=0，删除失败=0。
        2026-06-09 15:59:57 归档=50，归档失败=0，删除失败=0。
        2026-06-09 16:00:23 归档=29，归档失败=0，删除失败=0。

控制面板证据：
    控制面板.html 中 已知项目线程=24 出现 2 次。
    控制面板.html 中 消息中间件/项目线程快照 出现 1 次。
    控制面板.html 中 self-main、task-interface、task-worker-pool、task-worker-1、control-panel-summary 均出现。
    控制面板.html 中 系统线程列表、当前进程线程数、ToolHelp、TID= 均为 0。
```

## 当前判断

```text
P7 消息中间件清理与归档策略已闭合。
运行生成物 控制面板.html 和 消息中间件/ 不纳入本切片提交。
本切片不处理既有执行桥逻辑错误；15:56 之后本轮验证未出现新的执行桥弹窗。
```
