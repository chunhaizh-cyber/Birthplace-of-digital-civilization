# 20260621 P7 任务显示 SQL 日志后置审查表

## 1. 审查结论

P7 已复核任务相关控制面板、SQL 投影和日志显示路径：

```text
1. 外部任务主信息直读复扫仍无命中。
2. 任务相关 `语素集.安全获取词(...)` 只剩两处：
   - `任务类.cpp:834`：SQL 投影入口文本。
   - `控制面板类.cpp:545`：控制面板显示词文本。
3. 未发现 `语素集.安全获取词(...)` 进入机器查找、候选生成、需求生成、方法选择或排序。
4. 控制面板 SQL 字段比较、节点字段过滤和前端状态文本分支属于显示组织，不作为项目机器决策依据。
```

不宣称任务类逻辑纠偏完成。

## 2. 分类表

| 路径 / 命中 | 分类 | 当前处理 |
| --- | --- | --- |
| `任务类.cpp:834` `私有_任务SQL入口文本` | SQL 投影显示 | 保留；词面为空时回退入口主键，仅用于 SQL 显示行 |
| `控制面板类.cpp:545` `私有_词文本` | 控制面板显示 | 保留；只构造树节点 / 字段显示文本 |
| `控制面板类.cpp:7749` `私有_SQL字段(行, 3) == "因果"` | 控制面板 SQL 展示统计 | 保留；只统计世界树页面因果节点数 |
| `控制面板类.cpp:9278 / 9288` `子项.文本 == "节点字段"` | 控制面板树展示整理 | 保留；只隐藏 / 提取显示详情 |
| `控制面板类.cpp` 前端 `状态文本` 分支 | HTML / JS 显示样式 | 保留；只决定页面样式 |
| `任务模块.*` 构造消息文本 / 日志文本 | 日志 / 显示 | 保留；不得反向进入机器判断 |

## 3. 验证证据

```text
任务显示路径安全获取词复扫：
    日志/20260621_P7_任务显示路径安全获取词复扫.txt
    结果：2 行，均为 SQL / 控制面板显示。

任务显示 SQL 日志路径复扫：
    日志/20260621_P7_任务显示SQL日志路径复扫.txt
    结果：1711 行，均落入显示 / SQL / 日志路径，需要按判定表分层读取。

显示文本机器分支复扫：
    日志/20260621_P7_显示文本机器分支复扫.txt
    结果：87 行；未发现任务显示文本参与机器查找、候选生成、需求生成、方法选择或排序。

外部任务主信息直读复扫：
    日志/20260621_P7_外部任务主信息直读复扫.txt
    结果：外部任务主信息直读无命中。
```

## 4. 当前计划收束

```text
任务类逻辑纠偏计划当前可执行切片 P0a-P0k / P1 / P2 / P3 / P4 / P5 / P6 / P7 已闭合。
不宣称任务类逻辑纠偏完成，不宣称项目结构闭环全部通过。
```

下一步按计划索引进入方法类逻辑纠偏计划。
