# 20260606 P9d 稳定子集扫描目标上下文桥 Codex 断点清单

## 当前切片

```text
当前主动计划：
    计划/20260606_外设四本能方法任务真实运行条件包闭环计划_v0.1.md

当前主动切片：
    P9d：识别结果到扫描 / 跟踪目标上下文桥。

本轮最小闭合目标：
    在不强写“当前观察范围可观测单位存在对应闭合状态=1”的前提下，
    让 6150 对应事实提交入口把已稳定对应子集组织成 6146 可消费的正式目标上下文。
```

## 依据文件

```text
AGENTS.md
规范/规范目录.md
规范/当前场景特征值明确标准规范20260602.md
规范/观察像素簇与存在候选分层规范20260527.md
规范/外设观察特征与自我场景认知本能方法分层规范20260525.md
规范/外设独立控制线程与消息承接边界规范20260527.md
计划/计划索引.md
计划/20260606_外设四本能方法任务真实运行条件包闭环计划_v0.1.md
计划/20260606_目标观察约束特征组实现计划_v0.2.md
详细设计/20260606_目标观察约束特征组实现方案_v0.2.md
实施记录/20260606_P9d识别结果到扫描跟踪目标上下文桥记录.md
实施记录/20260606_目标观察约束特征组P7自我承接提交记录.md
```

## 当前已知证据

```text
P9c 已闭合：
    6145 自我_识别外设观察材料 真实运行成功，任务 B 完成。

P9d 已推进：
    6150 已能读取 6145 写入的识别结果；
    任务 C 稳定对应存在数量明确状态 已完成。

当前堵点：
    任务 D 当前观察范围可观测单位存在对应闭合状态 实际值=0、目标值=1。
    旧日志中已稳定对应存在可观测单位数量=15、未稳定对应存在可观测单位数量=13、扫描可启动状态=1。

当前判断：
    这不是识别结果读取失败；
    也不能把闭合状态强制写成 1。
```

## 本轮预测

```text
1. 构建应通过。
2. 6150 仍只在全范围都稳定对应时写 当前观察范围可观测单位存在对应闭合状态=1。
3. 当稳定对应存在数量 >= 1 且来源材料质量门允许提交入口组织正式对象时：
       6150 创建或复用正式 已确认观察存在；
       写入可观测单位到存在映射项；
       向目标观察约束缓冲区提交扫描用目标观察约束；
       日志显示提交主体是当前观察范围可观测单位存在对应事实 / 已确认观察存在。
4. 报告、包、队列项、材料页和目标观察约束仍只作为来源材料或通信容器，不作为事实主体。
5. 6146 / 6147 仍按质量门消费：可用材料不直接生成变化动态或稳定跟踪动态，优质材料才进入动态候选。
```

## 已运行命令

```text
git status --short --untracked-files=no
Select-String 计划/计划索引.md
Select-String 规范/当前场景特征值明确标准规范20260602.md
Select-String 计划/20260606_目标观察约束特征组实现计划_v0.2.md
Select-String 详细设计/20260606_目标观察约束特征组实现方案_v0.2.md
Get-Content 实施记录/20260606_P9d识别结果到扫描跟踪目标上下文桥记录.md
rg 自我动作实现.外设模块.ixx
```

## 当前允许修改

```text
自我动作实现.外设模块.ixx
任务模块.工作线程消息协议.ixx
任务模块.管理工作线程.ixx
自我线程模块.impl.cpp
计划/计划索引.md
实施记录/20260606_P9d稳定子集扫描目标上下文桥_Codex断点清单.md
```

## 禁止宣称项

```text
不得宣称自我苏醒完成。
不得宣称初步成熟完成。
不得宣称当前场景特征值明确状态已全范围闭合，除非闭合状态真实为 1。
不得把 D455 报告、报告队列项、材料页、目标观察约束特征组作为事实主体。
不得把队列项消费解释成需求满足。
不得用模拟样本、夹具入口、伪造动作动态或 --*-check 回归命令验收。
```

## 本轮修改结果

```text
已改文件：
    自我动作实现.外设模块.ixx

主要落点：
    6150 提交_当前观察范围可观测单位存在对应事实 增加稳定子集扫描目标上下文桥。
    稳定子集只在 扫描可启动状态>0 且提交质量门允许 时组织正式目标上下文。
    对每个允许项创建或复用 已确认观察存在；
    写入 可观测单位到存在映射项；
    提交 目标观察约束，用于后续 6146 扫描消费；
    首帧扫描入账状态缺失时仅写基准降级，不写扫描变化动态。
```

## 构建证据

```text
命令：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m

结果：
    构建通过，0 错误。
    保留 1 个 LNK4075 警告，来源为 /INCREMENTAL:NO 与 /EDITANDCONTINUE 冲突。
```

## 真实运行证据

```text
预测：
    6150 应能基于稳定子集创建正式目标上下文；
    不能把 当前观察范围可观测单位存在对应闭合状态 或 当前场景特征值明确状态 强写成 1；
    报告和队列项仍只能作为来源材料。

命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000

运行边界：
    两次均由外部等待超时后结束进程；
    结束后未保留 鱼巢.exe 残留进程。

关键证据：
    日志/鱼巢_run_20260606.低值g:72430
        成功=1；
        来源报告ID=25；
        有效可观测单位数量=24；
        已稳定对应存在可观测单位数量=21；
        未稳定对应存在可观测单位数量=3；
        当前观察范围可观测单位存在对应闭合状态=0；
        当前场景特征值明确状态=0；
        扫描可启动状态=1；
        稳定子集扫描目标上下文状态=1；
        稳定子集正式目标存在数量=21；
        稳定子集映射写入数量=21；
        稳定子集扫描约束写入数量=21；
        稳定子集扫描约束强匹配数量=21；
        稳定子集扫描约束失败数量=0；
        首帧扫描基准降级数量=21；
        首个扫描目标存在ID=4hy；
        首个扫描目标观察约束ID=1；
        不把扫描可启动当全范围闭合=1；
        来源报告只作材料=1；
        提交主体类型=当前观察范围可观测单位存在对应事实；
        观察材料等级=优质。
```

## 当前判断

```text
本轮稳定子集扫描目标上下文桥已形成代码闭合和真实日志证据。

仍未闭合：
    当前观察范围可观测单位存在对应闭合状态 仍为 0；
    当前场景特征值明确状态 仍为 0；
    本轮真实运行未自然触发 6146 自我_扫描已识别区域 或 6147 自我_跟踪已扫描存在。

因此不能宣称 P9d 全部完成。
```

## 下一步

```text
继续 P9d 时，下一最小切片应检查：
    已生成的稳定子集目标观察约束如何进入 6146 等待项 / 任务执行承接；
    重复 6150 时是否应识别已有稳定子集上下文为可复用，而不是只统计新提交数量；
    任务管理当前生成学习任务 E/F 的原因，是否缺 6146 可消费等待项或方法动作承接路径。
```

## 6146 承接定位

```text
代码事实：
    任务管理界面线程已能按方法结果能力把 已识别区域扫描入账状态 映射为扫描等待项。
    扫描 / 跟踪等待项创建时会尝试选择最新可用目标观察约束，并写入目标观察约束ID。
    D455 线程已按扫描变化等待项读取目标观察约束，并能生成扫描变化报告。
    6146 自我_扫描已识别区域 已注册，也有正式方法体。

真实日志事实：
    当前自然链仍在任务 D 上重跑 6150；
    任务 D 目标特征是 当前观察范围可观测单位存在对应闭合状态；
    当前方法仍为 6150，未自然进入 当前本能ID=6146；
    任务 E/F 是方法动作数量学习缺口，不是扫描等待项任务。

结论：
    稳定子集目标观察约束已经生成，但还没有一个合法的扫描任务主体把目标特征设为 已识别区域扫描入账状态。
    若让 6150 直接为 21 个稳定目标扇出扫描任务，会引入任务主体选择 / 扇出策略。
    当前计划没有明确是 选首个、全量扇出、批处理父任务 还是 等全范围闭合后再扫描。
```

## 当前具体堵点

```text
已定计划口径：
    采用“稳定子集可先扫描”。
    扫描范围为当前已稳定对应的正式 已确认观察存在 全量。
    不选首个目标。
    不等待未稳定候选全范围闭合。
    不把未稳定候选强行纳入扫描范围。

下一实现切片：
    稳定子集全量扫描等待项承接桥。

边界：
    任务管理负责等待项 / 任务承接。
    6150 只写正式目标上下文、可观测单位到存在映射项和目标观察约束。
    不允许 6150 把报告、目标观察约束或队列项作为事实主体。
    不允许把扫描可启动状态当成全范围闭合。
```

## 稳定子集全量扫描等待项承接预测

```text
本轮新增实现：
    外设观察报告队列增加按目标特征读取每个目标最新可用约束的只读 API。
    任务管理在 6150 执行后批量读取 已识别区域扫描入账状态 的可用目标观察约束。
    任务管理为每个稳定目标约束创建 已识别场景扫描变化 等待项。

预测：
    构建应通过。
    真实运行中 6150 形成稳定子集扫描目标上下文后，
    日志应出现 任务管理界面线程/稳定子集全量扫描等待项承接。
    扫描等待项提交数量应等于当前可用稳定子集扫描目标观察约束数量。
    D455 随后应能看到扫描变化等待项，并生成扫描变化报告或给出目标约束前置跳过原因。

禁止判断：
    不能把等待项创建解释为 6146 已闭合。
    不能把扫描可启动状态解释为全范围闭合。
    不能把目标观察约束、报告或队列项当事实主体。
```

## D455 目标观察约束时间源修正预测

```text
代码定位：
    目标观察约束写入时间和队列清理使用外设观察队列 steady_clock 毫秒口径；
    D455_扫描变化等待项前置满足 读取约束后又使用 system_clock 毫秒口径二次判定；
    因此刚写入的目标观察约束会被 D455 侧误判为 TTL 已过期。

本轮最小修改：
    将 D455_当前时间毫秒 改为与外设观察报告队列一致的 steady_clock 毫秒口径。

预测：
    构建应通过。
    真实运行中 D455 不应再因为时间源失配把扫描等待项统一跳过为 缺少已识别场景基准或目标区域。
    若目标约束能命中当前簇，应出现 扫描变化报告入队。
    若目标约束不能命中当前簇，应出现扫描变化报告入队但报告采集成功=0，失败原因指向目标观察约束未命中当前观察簇。
    仍不得把首次扫描报告解释为变化动态已生成或 6146 已闭合。
```

## 扫描报告消费后回查保留预测

```text
真实日志定位：
    D455 已生成扫描变化报告入队；
    任务管理外设消息承接已按质量门生成观察事实承接请求；
    但自我线程承接扫描报告时出现 报告缓存不可回查。

代码定位：
    外设观察报告_应清理 对已消费报告设置了 5 秒清理窗口；
    批量扫描等待项生成多个上行承接请求时，自我线程处理到后续扫描报告前，报告可能已被 5 秒窗口清理；
    这与任务管理承接日志中的 报告仍可按ID回查直到TTL清理 口径不一致。

本轮最小修改：
    取消已消费报告 5 秒特殊清理，让已消费报告同样按外设提交包默认 TTL / 队列容量清理。

预测：
    构建应通过。
    真实运行中扫描报告承接请求被自我线程处理时，报告可回查应为 是。
    对质量门为可用但非优质、可生成扫描变化候选=0 的扫描材料，自我只能承接材料或待复验事实，不能生成扫描变化动态，也不能宣称 6146 已闭合。
```

## 稳定子集全量扫描等待项与材料承接验证结果

```text
构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    结果：通过，0 错误，1 个 LNK4075 警告。

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000
    外部 100 秒超时后结束进程；
    结束后未发现残留 鱼巢.exe 进程。

最新日志区间：
    日志/鱼巢_run_20260606.低值g:77798
        6150 成功；
        有效可观测单位数量=55；
        已稳定对应存在可观测单位数量=50；
        未稳定对应存在可观测单位数量=5；
        当前观察范围可观测单位存在对应闭合状态=0；
        当前场景特征值明确状态=0；
        扫描可启动状态=1；
        稳定子集扫描目标上下文状态=1；
        稳定子集扫描约束写入数量=50；
        稳定子集扫描约束强匹配数量=50。
    日志/鱼巢_run_20260606.低值g:77809
        任务管理界面线程/稳定子集全量扫描等待项承接；
        可用扫描目标观察约束数量=39；
        扫描等待项提交数量=39；
        扫描范围=稳定子集全量；
        不等待全范围闭合=1；
        不选首个目标=1；
        不把目标观察约束当事实主体=1。
    日志新增区间统计：
        扫描变化报告入队=39；
        自我线程/外设观察事实承接回查成功=5；
        自我线程/外设观察事实承接证据不足=0；
        当前本能ID=6146=0；
        已识别区域扫描正式结果=0；
        可生成扫描变化候选=1 的扫描材料=0。
    日志/鱼巢_run_20260606.低值g:78163-78170
        已消费扫描报告可按 ID 回查成功；
        自我线程承接完成 成功=是、报告可回查=是；
        质量门显示 等级=可用、可进入验证方法=1、可进入识别方法=0、可生成扫描变化候选=0、可进入提交入口组织=0。
```

## 当前判断

```text
已闭合：
    稳定子集全量扫描等待项承接；
    D455 基于目标观察约束生成扫描变化报告；
    任务管理按扫描报告质量门生成观察事实承接请求；
    自我线程对已消费扫描报告按 ID 回查并承接材料。

仍未闭合：
    6146 自我_扫描已识别区域 未自然执行；
    6147 自我_跟踪已扫描存在 未自然执行；
    当前场景特征值明确状态 仍不能写 1；
    当前观察范围可观测单位存在对应闭合状态 仍不能写 1。

下一具体堵点：
    扫描报告已经成为可承接材料，但还没有合法的 6146 扫描任务主体把正式稳定对应存在作为被作用对象。
    当前扫描材料按质量门只是可用、非优质，不得越级生成扫描变化动态候选。

下一切片：
    扫描材料承接到 6146 任务主体桥。

边界：
    不能把扫描报告、队列项、材料页或目标观察约束当事实主体；
    不能把可用但非优质扫描材料当作扫描变化动态证据；
    只有达到可生成扫描变化候选质量门时，才允许进入扫描变化动态候选或提交入口组织。
```

## 观察承接抢占卡住复验预测

```text
现象：
    稳定子集全量扫描后会在短时间内生成多条任务管理观察事实承接请求；
    旧日志中出现一次抢占抽取 39 条观察承接后长时间无完成日志；
    单条观察承接也会在承接完成后同步触发来源需求 / 安全根需求重筹办并立即派发任务工作项，
    导致任务管理调度让步回调长时间占用当前调度路径。

本轮最小修改：
    自我线程观察事实承接抢占默认一次只抽取 1 条；
    自我线程冻结治理消息时最多冻结 4 条观察事实承接，剩余保留在治理邮箱；
    观察事实更新触发来源需求 / 安全根需求重筹办时，只入任务管理等待队列并启用任务级幂等复用，
    不在观察承接落账函数内同步派发工作线程。

预测：
    构建应通过；
    真实运行中仍能生成并承接外设观察事实；
    抢占日志应出现 抢占上限=1；
    全量扫描后的观察承接不应再一次性抽取 39 条；
    观察事实更新触发的重筹办日志应显示 同步派发=否、后续巡检消费=是；
    调度路径不应长时间停在 任务管理界面线程/消费调度工作项 后无后续日志。

边界：
    只调整承接调度节奏，不改变报告 / 队列项 / 材料页 / 目标观察约束的事实主体边界；
    质量门仍按 可用 / 优质 / 可生成扫描变化候选 分级消费；
    不把等待项创建或材料承接解释为 6146 已闭合。
```

## 观察承接抢占卡住复验结果

```text
构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    结果：通过，0 错误，1 个 LNK4075 警告。

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=70000
    监控约 112 秒后外部结束进程；
    运行期间日志持续增长，最大无增长间隔=0 秒；
    结束后未发现残留 鱼巢.exe 进程。

本轮日志区间：
    起点：2026-06-06 19:11:54；
    终点：2026-06-06 19:13:43；
    新增日志约 626727 字节；
    新增日志行数=1314。

关键证据：
    自我线程/观察事实承接抢占抽取完成 次数=4；
    抢占抽取最大数量=1；
    抢占上限=1 次数=4；
    一次抽取 39 条观察承接 次数=0；
    抢占抽取与抢占落账完成 未配对数量=0；
    观察事实承接完成 次数=4；
    观察事实更新触发重筹办时 同步派发=否 次数=2；
    来源需求 / 安全根需求重筹办仍同步派发 次数=0；
    稳定子集全量扫描等待项承接 次数=1；
    扫描变化报告入队 次数=18；
    外设观察事实承接证据不足 次数=0。

判断：
    旧卡住形态已经被切断：
        不再在任务管理调度让步回调中一次性抽取 39 条观察承接；
        观察事实更新触发的来源需求 / 安全根需求重筹办不再在观察承接落账函数内同步派发任务工作线程。
    112 秒真实运行未复现日志停止增长或残留进程。

仍未闭合：
    当前本能ID=6146 命中 0；
    已识别区域扫描正式结果 命中 0；
    可生成扫描变化候选=1 命中 0。

下一堵点保持不变：
    扫描材料承接到 6146 任务主体桥。
    需要让 6146 的任务主体绑定正式稳定对应的 已确认观察存在，
    仍不得把报告 / 队列项 / 材料页 / 目标观察约束当事实主体。
```

## 扫描材料承接到 6146 任务主体桥预测

```text
新证据：
    控制面板任务列表显示任务节点=20、头=20、步骤=0；
    C 稳定对应存在数量明确状态 与 D 当前观察范围可观测单位存在对应闭合状态 仍显示等待中；
    任务树中没有 已识别区域扫描入账状态 任务头。

代码定位：
    任务管理已能把 已识别区域扫描入账状态 映射为扫描等待项口径；
    D455 已能按目标观察约束生成扫描变化报告；
    自我线程已能按报告 ID 回查并承接扫描材料；
    当前承接请求只携带等待项 / 报告 / 来源需求，未结构化携带目标存在 ID、目标特征类型和目标观察约束 ID；
    因而扫描材料落账后没有合法扫描任务主体进入 6146。

本轮最小修改：
    任务管理观察事实承接请求补目标存在 ID、目标特征类型 ID、目标观察约束 ID 和目标特征当前值；
    任务管理生成扫描观察承接请求时从等待项填入这些字段；
    自我线程承接扫描材料成功后，只以正式目标存在为被需求存在，生成 / 复用 已识别区域扫描入账状态 子需求；
    若该需求没有非终结任务，再提交任务发起请求，让任务管理自然选择 6146。

预测：
    构建应通过；
    真实运行中仍会生成稳定子集扫描等待项和扫描变化报告；
    扫描观察承接日志应带 目标存在ID / 目标特征类型ID / 目标观察约束ID；
    自我线程应出现 扫描材料承接生成6146任务主体 或 扫描材料承接复用6146任务主体 日志；
    后续任务管理日志应出现 当前本能ID=6146 或 已识别区域扫描正式结果；
    可用但非优质扫描材料仍只能输出待复验 / 待重识别，不得生成扫描变化动态候选。

边界：
    报告 / 队列项 / 材料页 / 目标观察约束仍只作来源材料或通信容器；
    6146 任务主体必须是目标存在 ID 对应的正式存在；
    不把 C/D 等待项状态强写完成；
    不把 当前观察范围可观测单位存在对应闭合状态 强写 1。
```

## 扫描材料承接到 6146 任务主体桥验证结果

```text
代码修改：
    任务模块.工作线程消息协议.ixx
        任务管理观察事实承接请求新增目标观察约束ID、目标存在ID、目标特征类型ID、目标特征当前值、目标特征generation、约束generation。
    任务模块.管理工作线程.ixx
        扫描观察承接请求从等待项 / 报告填入目标字段。
    自我线程模块.impl.cpp
        扫描材料承接成功后，按正式目标存在创建 / 复用 已识别区域扫描入账状态 子需求；
        若该子需求没有非终结任务，则提交任务发起请求，由任务管理自然选择 6146。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    结果：通过，0 错误，1 个 LNK4075 警告。

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=70000
    监控约 80 秒后外部结束进程；
    运行期间日志持续增长，最大无增长间隔=6 秒；
    结束后未发现残留 鱼巢.exe 进程。

关键证据：
    日志/鱼巢_run_20260606.低值g:83167
        任务管理界面线程/稳定子集全量扫描等待项承接；
        可用扫描目标观察约束数量=15；
        扫描等待项提交数量=15；
        首个目标存在ID=4ia；
        首个目标观察约束ID=16；
        扫描范围=稳定子集全量；
        不把目标观察约束当事实主体=1。
    日志/鱼巢_run_20260606.低值g:83321
        自我线程/外设观察事实承接开始；
        等待项ID=2；
        报告ID=42；
        目标观察约束ID=16；
        目标存在ID=4ia；
        目标特征类型ID=1aZ；
        来源需求=C；
        扫描材料等级=可用；
        可生成扫描变化候选=0。
    日志/鱼巢_run_20260606.低值g:83369
        任务管理工作线程/执行任务入口；
        管理任务=E；
        当前本能ID=6146；
        选中自我能力值=6146。
    日志/鱼巢_run_20260606.低值g:83372
        已识别区域扫描正式结果；
        成功=0；
        当前存在=4ia；
        归属状态已读=0；
        扫描可启动状态已读=0；
        扫描材料等级=可用；
        扫描材料可复验=1；
        可生成扫描变化候选=0；
        扫描闸门成立=0；
        不组织外设提交包=1；
        首次扫描不写变化动态=1；
        不写安全值=1。
    日志/鱼巢_run_20260606.低值g:83393
        自我线程/扫描材料承接生成6146任务主体；
        成功=是；
        需求=E；
        任务=E；
        来源需求=C；
        目标存在=4ia；
        目标特征=1aZ；
        目标观察约束ID=16；
        报告只作材料=1；
        目标观察约束只作路由证据=1。

判断：
    扫描材料承接到 6146 任务主体桥已形成闭合证据；
    报告 / 队列项 / 材料页 / 目标观察约束没有被当作事实主体；
    6146 已自然触发，但因输入条件缺失而未成功；
    扫描材料质量门保持分级消费：可用但非优质，只能复验 / 待重识别，未生成扫描变化动态候选。

下一具体堵点：
    6146 输入条件场景未能读取正式目标存在上的 外设观察存在自我归属状态 与 扫描可启动状态；
    下一切片应定位 6146 被作用对象 / 输入结果场景如何携带或回查目标存在条件，
    而不是继续把堵点放在报告队列、等待项或目标观察约束层。
```

## 6146 输入条件场景 / 目标存在条件桥预测

```text
代码定位：
    6146 自我_扫描已识别区域 已能解析当前存在=4ia；
    6146 读取 外设观察存在自我归属状态 和 扫描可启动状态 时，
    先读输入参数场景 / 输出结果场景，再读 当前存在节点 / 目标宿主；
    6150 稳定子集桥已创建正式目标存在并写目标观察约束，
    但没有把 外设观察存在自我归属状态=已归属 与 扫描可启动状态=1 写到每个正式目标存在上。

本轮最小修改：
    在 6150 稳定子集桥组织正式目标存在时，
    对每个允许提交的正式目标存在写入：
        外设观察存在自我归属状态=已归属；
        扫描可启动状态=当前 6150 的扫描可启动状态。

预测：
    构建应通过；
    真实运行仍应生成稳定子集扫描等待项、扫描变化报告和扫描材料承接；
    6146 正式结果中 当前存在 应为正式目标存在；
    归属状态已读 应从 0 变为 1，归属状态应为 已归属；
    扫描可启动状态已读 应从 0 变为 1，扫描可启动状态应为 1；
    若扫描材料仍只是 可用 且 可生成扫描变化候选=0，
    6146 仍不得生成扫描变化动态候选，只能输出待复验 / 待重识别。

边界：
    不把 当前观察范围可观测单位存在对应闭合状态 强写为 1；
    不把 当前场景特征值明确状态 强写为 1；
    不把扫描报告、等待项、材料页或目标观察约束当事实主体；
    只补正式目标存在上的输入条件特征，不改变报告质量分级。
```

## 6146 输入条件场景 / 目标存在条件桥验证结果

```text
代码修改：
    自我动作实现.外设模块.ixx
        6150 稳定子集桥在每个正式 已确认观察存在 上写入：
            外设观察存在自我归属状态=已归属；
            扫描可启动状态=1。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    结果：通过，0 错误，1 个 LNK4075 警告。

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=70000
    监控约 96.7 秒后外部结束进程；
    新增日志 683468 字节；
    最大无增长间隔=6 秒；
    结束后未发现残留 鱼巢.exe 进程。

新增日志统计：
    稳定子集全量扫描等待项承接=1；
    扫描变化报告入队=29；
    外设观察事实承接开始=3；
    扫描材料承接生成6146任务主体=1；
    当前本能ID=6146=3；
    已识别区域扫描正式结果=3；
    归属状态已读=1 的 6146 结果=3；
    扫描可启动状态已读=1 的 6146 结果=3；
    扫描闸门成立=1 的 6146 结果=3；
    可生成扫描变化候选=1 的材料=0。

关键证据：
    日志/鱼巢_run_20260606.低值g:84365
        任务管理界面线程/稳定子集全量扫描等待项承接；
        可用扫描目标观察约束数量=19；
        扫描等待项提交数量=19；
        首个目标存在ID=4ij；
        首个目标观察约束ID=21；
        不把目标观察约束当事实主体=1。
    日志/鱼巢_run_20260606.低值g:84622
        任务管理工作线程/执行任务入口；
        管理任务=E；
        当前本能ID=6146。
    日志/鱼巢_run_20260606.低值g:84625
        已识别区域扫描正式结果；
        成功=1；
        当前存在=4ij；
        归属状态=2；
        归属状态已读=1；
        扫描可启动状态=1；
        扫描可启动状态已读=1；
        扫描材料等级=可用；
        可生成扫描变化候选=0；
        扫描闸门成立=1；
        不组织外设提交包=1；
        首次扫描不写变化动态=1；
        不写安全值=1。
    日志/鱼巢_run_20260606.低值g:84711
        自我线程/扫描材料承接生成6146任务主体；
        需求=E；
        任务=E；
        目标存在=4ij；
        目标观察约束ID=21；
        报告只作材料=1；
        目标观察约束只作路由证据=1。
    日志/鱼巢_run_20260606.低值g:84819
        第二个目标 4po 同样 当前本能ID=6146 后 成功=1；
        归属状态已读=1；
        扫描可启动状态已读=1；
        可生成扫描变化候选=0。

判断：
    6146 输入条件场景 / 目标存在条件桥已闭合；
    6146 可以以正式目标存在作为被作用对象自然执行；
    扫描材料质量门仍保持分级消费：可用但非优质，未进入扫描变化动态候选或提交入口组织；
    当前观察范围可观测单位存在对应闭合状态 和 当前场景特征值明确状态 仍不得强写为 1。

下一具体堵点：
    6147 自我_跟踪指定存在 未自然出现；
    当前扫描材料日志显示 可生成跟踪动态候选=0；
    下一切片应转入 6147 跟踪目标上下文 / 任务主体 / 跟踪等待项定位，
    继续保持报告、队列项、材料页、目标观察约束不作事实主体。
```
