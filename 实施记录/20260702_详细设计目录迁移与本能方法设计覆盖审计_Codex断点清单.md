# 20260702 详细设计目录迁移与本能方法设计覆盖审计 Codex 断点清单

## 0. 恢复摘要

当前切片：按用户要求，取消根目录 `详细设计/`，将当前保留的派生设计统一放在 `规范/详细设计/`，并根据正式规范、计划和代码默认本能方法表检查缺少详细设计承接的本能方法列表。

用户口径：

```text
说明书是反向读取代码生成的，不能作为实现代码依据。
详细设计应移动到规范目录下，表明详细设计依据规范生成。
以后修改规范时要同步检查详细设计内容是否一致。
旧的详细设计已经删除；审计只按当前仍存在的 `规范/详细设计/` 文件判断覆盖。
```

当前依据：

```text
docs/00_文档总索引.md
规范/规范目录.md
规范/0050_项目通用结构与禁止性规则总纲_20260701.md
计划/计划索引.md
本能方法类.cpp 中 g_默认自我本能方法表
```

禁止宣称：

```text
不把详细设计当作正式规范。
不把说明书当作实现依据。
不宣称缺口清单证明本能方法实现正确或闭环。
不宣称自我苏醒完成或初步成熟完成。
```

## 1. 已知工作区状态

开工前 `git status --short --untracked-files=no` 已有非本轮改动：

```text
实施记录/20260701_R24服务机会到扫描需求_Codex断点清单.md
自我动作实现.扫描.ixx
说明书/函数功能说明文档.md
鱼巢.cpp
```

本轮不应修改或提交上述既有改动，除非后续用户另行要求。

## 2. 待执行

1. 统计 `g_默认自我本能方法表` 当前默认本能方法数量和名称。
2. 以 `规范/`、`计划/`、`规范/详细设计/` 为依据统计逐方法命中情况，不读取说明书作为依据。
3. 根目录 `详细设计/` 已不存在；当前仅保留 `规范/详细设计/` 下仍存在的派生设计文件。
4. 更新路径引用、规范目录治理说明、文档索引和检查脚本。
5. 生成缺少详细设计承接的本能方法清单。
6. 运行规范检查和差异检查。

## 3. 当前执行结果

已完成：

```text
1. 根目录 `详细设计` 已不存在；当前 `规范/详细设计/` 保留 11 个文件。
2. 新增审计文件：docs/规范治理/20260702_本能方法详细设计覆盖审计.md。
3. 审计结果：默认本能方法 82 项；完整覆盖 0 项；部分覆盖 17 项；缺当前详细设计 65 项。
4. 已更新文档权力关系：详细设计路径为 `规范/详细设计/`，只能依据规范解释实现，不覆盖规范。
5. 已更新规范目录、文档总索引、规范状态管理、规则原子和改代码前检查清单；规范变更需同步检查相关详细设计一致性。
6. 已更新 tools/sql_projection/fishnest_sql_projection.py 的保留详细设计读取路径。
```

已运行验证：

```text
python .\tools\check_specs.py
结果：hard issues 0，warnings 0。

git diff --check -- AGENTS.md docs/00_文档总索引.md docs/工程图谱/04_Codex改代码前检查清单.md docs/工程图谱/05_规则原子索引.md docs/规范治理/规范状态管理.md docs/规范治理/20260701_子规范修订迁移总账.md docs/规范治理/20260702_本能方法详细设计覆盖审计.md 规范/规范目录.md tools/sql_projection/fishnest_sql_projection.py 实施记录/20260702_详细设计目录迁移与本能方法设计覆盖审计_Codex断点清单.md
结果：无 whitespace error；仅 AGENTS.md 和 tools/sql_projection/fishnest_sql_projection.py 的 CRLF/LF 提示。
```

待复核：

```text
1. 根据用户最新口径，已删除的旧详细设计不恢复、不作为覆盖证据。
2. 最终提交前需只提交本轮文档治理、路径更新、审计文件和用户已删除旧详细设计，不混入既有 R24、扫描实现、说明书、鱼巢.cpp 等非本轮改动。
```
