# 需求树简化与内部治理闭环 P1c Codex断点清单

日期：2026-06-30

## 0. 恢复摘要

当前切片：R24 `计划/20260630_需求树简化生成与内部治理闭环计划_v0.1.md` 的 P1c 根需求到根任务最小闭环验证。

进入依据：

```text
P1a 已提交：d15442d 修正根目标最大值读取。
P1b 已提交：05a7b20 唯一化根任务初始化承接。
P1c 要求用同一轮真实启动或自检路径串起：根特征差距 -> 根需求活动 -> 根任务承接。
```

验证入口：

```text
计划中的 .\x64\Debug\鱼巢.exe --self-check-log --demand-tree-growth-summary 在当前 main 中会先处理 --self-check-log 并返回；
当前实际存在的需求树摘要入口是 --demand-tree-growth-summary-live。
因此本切片拆为两条真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=1500
    .\x64\Debug\鱼巢.exe --demand-tree-growth-summary-live
```

禁止范围：

```text
不新增机器逻辑；
不伪造样本、入口或动作动态；
不把日志 / stdout 当业务状态承载；
不把根任务承接成功扩大宣称为安全 / 服务闭环已完成。
```

## 1. 预测

```text
1. 自检启动路径可以完成自我初始化。
2. 初始化日志应出现根需求与根任务初始化入口、根任务确认、安全 / 服务根任务就绪。
3. 自我实现检查日志应显示根需求=是、根任务(诊断)=是。
4. 需求树摘要 live 至少能读取根需求活动 / 主派发 / 任务树摘要中的根任务承接证据。
5. 默认本能方法表不包含“任务筹办”或“任务执行”作为本能方法种子；根任务只作为任务节点承接壳。
```

## 2. 验证

```text
运行互斥：
    运行前 `.codex_locks/build-run.lock` 不存在；
    未发现正在运行的 `鱼巢.exe` 进程；
    自检日志和需求树摘要 live 持有同一运行锁顺序执行，结束后释放。

.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=1500
结果：
    退出码 0；
    stdout `日志/20260630_R24_P1c_self_check_stdout.log`。

.\x64\Debug\鱼巢.exe --demand-tree-growth-summary-live
结果：
    退出码 0；
    stdout `日志/20260630_R24_P1c_demand_summary_live_stdout.log`。

最新主日志：
    `日志/鱼巢_run_20260630.低值g`

关键证据：
    108743-108744：安全 / 服务抽象根特征定义读取，目标区间为 [9223372036854775807,9223372036854775807]。
    108811、108814：自检线程根特征自检显示安全 / 服务当前值低于最大值目标，满足目标=否。
    108736-108737：自我类/根任务确认，安全根任务=1，服务根任务=2；根需求与根任务初始化入口成功。
    108890：自我实现检查显示根需求=是，根任务(诊断)=是，运行环境=是。
    stdout live 第 15-16 行：当前主需求有任务=是；任务承接任务数=2、任务头=2。
    108988-108991：任务管理工作线程调用任务筹办普通函数，安全 / 服务根任务进入任务生命周期普通筹办。
    109002-109014：根任务筹办只走因果路径查询、候选集填充和挂起；直接写需求树=0、实际发送派生需求消息=0、创建占位方法头=0。
    本能方法类.cpp 默认本能方法表中未命中 `"任务筹办"` 或 `"任务执行"`；仅有 `提交任务状态变化`、`结算叶子任务价值` 等本能方法。
```

## 3. 小修

```text
验证时发现 `自我线程模块.impl.cpp` 中根需求状态镜像注释仍写“目标下限值”；
P1a 已改为读取抽象根特征目标值，本切片只更正注释为“目标值”，不改机器逻辑。
```

## 4. 静态与构建

```text
rg -n "目标下限值|目标状态保持为抽象根特征目标值|根需求与根任务初始化入口|根任务确认" ...
结果：
    代码中根需求状态镜像注释已为“目标值”；
    “目标下限值”只保留在本断点清单的问题记录中。

git diff --check -- "自我线程模块.impl.cpp" "实施记录/20260630_需求树简化与内部治理闭环_P1c_Codex断点清单.md"
结果：退出码 0。

函数说明文档：
    本切片未新增、删除或改名函数，不运行生成脚本。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：退出码 0；日志 `日志/20260630_R24_P1c_msbuild_Debug_x64.log`；关键摘要：已成功生成。
```

## 5. 收束判断

```text
P1c 已验证：
    根特征差距来自最大值目标；
    根需求活动可被真实自检 / 摘要路径读取；
    安全 / 服务根任务存在并绑定为任务承接壳；
    根任务由任务管理生命周期普通筹办推进；
    未把任务筹办 / 任务执行注册为本能方法候选。

禁止扩大宣称：
    不宣称安全 / 服务闭环已完成；
    不宣称需求树简化生成已闭环；
    不宣称内部治理闭环已完成。
```
