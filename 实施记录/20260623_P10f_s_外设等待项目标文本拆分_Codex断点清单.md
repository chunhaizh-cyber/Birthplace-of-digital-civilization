# P10f-s 外设等待项目标文本拆分 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-s `外设观察报告队列.ixx::结构_外设观察等待项::目标区域或目标簇` 与 `外设线程_D455深度相机.ixx::结构_D455跟踪稳定缓存项::跟踪种子` 边界复核。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_tracking_seed.tsv`

当前问题：

- `目标区域或目标簇` 同时承载目标存在 ID、目标簇文本和临时显示说明，并进入目标约束匹配、D455 前置判断和跟踪种子解析。
- `结构_D455跟踪稳定缓存项::跟踪种子` 复制该文本并参与缓存匹配。
- `D455_尝试解析跟踪种子簇ID(...)` 从文本里解析数字作为簇 ID，属于自由文本参与机器逻辑。

构建前预测：

- 删除 `目标区域或目标簇` 文本字段，已有 `目标存在ID` 承担存在主键，新增数值 `目标观察簇ID` 承担目标簇 ID。
- D455 稳定缓存改为记录 `目标观察簇ID`，不再记录文本跟踪种子。
- 删除 `D455_尝试解析跟踪种子簇ID(...)`，跟踪降级匹配只读取 `目标观察簇ID`。
- 删除或改写临时扫描等待项中的显示文本赋值，不把说明文本写入等待项协议。
- 预期 Debug x64 构建通过；删除函数后刷新 `说明书/函数功能说明文档.md`。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。

## 1. 完成证据

- 已将 `结构_外设观察等待项::目标区域或目标簇` 替换为数值字段 `目标观察簇ID`，已有 `目标存在ID` 继续承载目标存在主键。
- 已将任务管理界面线程构造等待项逻辑改为直接写 `目标存在ID`，不再通过 `目标区域或目标簇` 中转。
- 已删除目标观察约束匹配和可用性判定中基于 `目标区域或目标簇` 的兼容 fallback。
- 已将 D455 跟踪稳定缓存项的 `跟踪种子` 改为 `目标观察簇ID`。
- 已删除 `D455_尝试解析跟踪种子簇ID(...)`，跟踪降级匹配只读取 `等待项.目标观察簇ID`。
- 已删除临时扫描等待项中的 `"单帧材料包证据卡"` 文本赋值。
- 已刷新 `说明书/函数功能说明文档.md`，函数条目 7185。
- 已生成 `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_wait_target_split.tsv`，剩余记录 624 行，其中主键 / ID 类 316 行、非主键类 308 行。
- `rg` 复核 `目标区域或目标簇 / D455_尝试解析跟踪种子簇ID / .跟踪种子 / std::string 跟踪种子{}` 仅剩自我线程特征模板 `跟踪种子`，不属于本切片目标结构字段。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 通过，0 警告，0 错误。

## 2. 下一候选

- `外设观察报告队列.ixx::结构_外设观察等待项::目标外设` 仍是非主键类 `std::string` 字段。
- 该字段进入外设观察报告满足等待项的目标外设匹配，应按外设 ID 字段收束为 `目标外设ID`。
