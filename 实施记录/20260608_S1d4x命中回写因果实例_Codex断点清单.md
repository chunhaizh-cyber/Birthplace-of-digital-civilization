# 20260608 S1d4x 命中回写因果实例 Codex 断点清单

## 当前切片

- 主动计划：`计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- 执行方案：`计划/20260607_S1d4x命中回写因果实例内部阶段定位与瘦身方案_v0.1.md`
- 切片：S1d-4x 命中回写因果实例内部阶段定位 / 瘦身。

## 依据文件

- `AGENTS.md`
- `规范/规范目录.md`
- `规范/方法动作动态硬规则规范20260512.md`
- `规范/动作动态即因果账本规范20260512.md`
- `规范/二次特征规范20260520.md`
- `规范/远程执行通道与断点续跑规范20260524.md`
- `计划/计划索引.md`
- `计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- `计划/20260607_S1d4w分步值写入聚合调用定位与瘦身方案_v0.1.md`
- `实施记录/20260608_S1d4w分步值写入_Codex断点清单.md`

## 当前判断

- S1d-4w 已闭合并提交：`6d1309e`。
- 最新运行线程 T13700：
  - `动态二次特征生成/内部阶段耗时` 样本 9 条。
  - 最慢阶段分布为 `分步结果生成=5`、`命中回写因果实例=4`。
  - `命中回写因果实例` 平均约 67955us，最大约 113942us。
- `命中回写因果实例` 对应 `私有_回写因果模板命中`。
- 当前函数未拆分命中模板证据样本追加与 `按动态创建因果信息`。

## 构建 / 运行前预测

- Debug x64 构建通过。
- 真实运行退出码 0。
- 日志继续出现 `动态二次特征生成/内部阶段耗时`。
- 若 `命中回写因果实例` 仍超过 5000us，日志出现 `动态二次特征命中回写因果实例/内部阶段耗时`。
- 日志保留 `任务状态动作动态=<动态主键>`。
- 不出现 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`。
- 不出现旧路径锚点 `准备本能调用`、`执行本能方法`、`任务域本能输入包`。

## 允许修改

- `二次特征应用模块.ixx`
- `因果类.cpp`
- `计划/20260607_S1d4x命中回写因果实例内部阶段定位与瘦身方案_v0.1.md`
- `计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- `计划/计划索引.md`
- `实施记录/20260608_S1d4x命中回写因果实例_Codex断点清单.md`

## 已改文件

- `二次特征应用模块.ixx`
- `计划/20260607_S1d4x命中回写因果实例内部阶段定位与瘦身方案_v0.1.md`
- `计划/计划索引.md`
- `实施记录/20260608_S1d4x命中回写因果实例_Codex断点清单.md`

## 已完成修改

- `二次特征应用模块.ixx` 已新增 `动态二次特征命中回写因果实例/内部阶段耗时`。
- `私有_回写因果模板命中` 已拆分命中模板证据动态追加与按动态创建因果信息。
- 日志字段包括命中模板数、证据动态追加调用数、证据动态追加成功数和创建 / 更新因果结果。
- 本切片未跳过合法因果实例回写，未改变因果模板命中语义。

## 已运行命令

- `git diff --check -- 二次特征应用模块.ixx 计划/计划索引.md 实施记录/20260608_S1d4x命中回写因果实例_Codex断点清单.md`
  - 退出码 0；仅有 CRLF 工作区转换警告。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
  - 退出码 0；0 errors；保留 LNK4075 增量链接相关警告。
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000`
  - 退出码 0；末尾 `D455 打开失败: No device connected` 属硬件条件，不作为本切片失败。

## 验收结果

- 最新日志窗口：`日志/鱼巢_run_20260608.低值g`，启动行 `2026-06-08 11:54:23.243 [T15496] 程序启动`。
- `动态二次特征命中回写因果实例/内部阶段耗时`：101 条。
- `动态二次特征生成/内部阶段耗时`：9 条。
- `动态二次特征分步结果/内部阶段耗时`：6 条。
- `任务状态动作动态=`：61 条。
- `任务状态被动提交/逻辑错误`：0。
- `任务状态动作动态逻辑错误`：0。
- `准备本能调用`：0。
- `执行本能方法`：0。
- `任务域本能输入包`：0。

## 性能证据

- 命中回写因果实例内部样本 101 条，平均总耗时约 60065us，最大约 132868us。
- 最慢阶段分布为 `命中模板证据动态追加=78`、`按动态创建因果信息=23`。
- `命中模板证据动态追加` 平均约 36839us，最大约 95946us。
- `按动态创建因果信息` 平均约 23224us，最大约 73919us。
- 平均命中模板数约 396，最大 631。
- 动态二次特征生成样本 9 条，平均总耗时约 203370us，`命中回写因果实例` 平均约 53457us。

## 结论

- S1d-4x 已闭合。
- 当前主因是命中模板证据动态追加多模板循环。
- 下一片进入批量追加证据动态样本，保留每个模板的证据和计数，只收束全局互斥粒度。

## 下一步

1. 进入 S1d-4y：命中模板证据动态样本批量追加瘦身。
2. 允许增加 `因果类::批量追加证据动态样本` 之类批处理入口。
3. 不跳过合法证据动态样本，不改变模板计数语义。

## 禁止宣称

- 不宣称自我苏醒完成。
- 不宣称初步成熟完成。
- 不跳过合法因果实例回写。
- 不改变因果模板命中语义。
