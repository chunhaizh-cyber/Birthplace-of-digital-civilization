# 20260613 日志输出预编译控制 P4/P5 Codex 断点清单

## 当前切片

```text
计划：
    计划/20260612_日志输出预编译控制实时计划_v0.1.md

切片：
    P4 分类文件与命名复核
    P5 验证闭合

入口：
    计划/计划索引.md 第 1 章当前主动队列。
```

## 边界

```text
允许：
    计划/计划索引.md
    计划/20260612_日志输出预编译控制实时计划_v0.1.md
    实施记录/20260612_日志输出预编译控制登记表.md
    本断点清单
    必要时仅修改日志 / 输出分类相关代码。

禁止：
    不改任务状态、需求满足、方法选择、动作动态、世界树、因果账本或价值结算。
    不把逻辑错误降级为调试日志。
    不用关闭日志掩盖弹窗、Debug Error、abort、访问冲突或业务缺口。
    不混入当前已有控制面板类.cpp / 控制面板类.ixx 脏改。
```

## 当前工作区

```text
基线提交：
    8951f9d docs: 修订计划索引全量切片路由

已有非本切片脏改：
    控制面板类.cpp
    控制面板类.ixx
```

## 预测

```text
P4：
    若现有文件命名和登记表已经能区分 run / exception 或 logic_error / debug / selfcheck_orphan_requirement，
    则不为命名再改代码，只更新验收记录。

P5：
    默认构建应通过。
    全关构建应通过。
    全关运行不应新增 run / exception / debug / selfcheck_orphan_requirement 内容；
    如旧日志文件已有内容，以运行前后大小不增长为准。
```

## 证据记录

```text
静态输出入口扫描：
    命令输出：
        日志/20260613_P4P5_输出入口静态扫描.txt
    命中总数：
        scan_lines=854
    主要命中：
        自我线程模块.impl.cpp 274
        任务模块.管理界面线程.impl.cpp 121
        鱼巢.cpp 106
        自我动作实现.外设模块.ixx 73
        自我动作实现模块.ixx 64

P4 分类问题：
    锚点：
        日志模块.ixx:111 原 `日志::运行_错误` 写入 `枚举_日志类别::运行`。
        日志模块.ixx:550 / 559 项目错误入口因此进入 run 文件。
    判断：
        逻辑错误日志输出开关已生效，但文件分类仍混入 run 文件；
        与 P4 “exception 或 logic_error 文件承载逻辑错误日志”不一致。
    修正：
        日志模块.ixx:
            `日志::运行_错误` 改写 `枚举_日志类别::异常`。
        实施记录/20260612_日志输出预编译控制登记表.md:
            同步登记 `项目运行错误日志` 和 `项目弹窗错误提示` 写入 exception 文件。

待补：
    无。

默认构建：
    日志/20260613_P4P5_default_msbuild.log
    exit_code=0
    仅 LNK4075。

全关构建：
    日志/20260613_P4P5_all_output_off_rebuild.log
    exit_code=0
    仅 LNK4075。

默认 Rebuild 恢复：
    日志/20260613_P4P5_default_rebuild_restore.log
    exit_code=0
    仅 LNK4075。

默认运行：
    命令：
        x64/Debug/鱼巢.exe --self-check-log --self-check-log-wait-ms=5000
    控制台：
        日志/20260613_P4P5_default_run_5000ms.console.log
    stderr：
        日志/20260613_P4P5_default_run_5000ms.stderr.log
    结果：
        exit_code=0
        console_bytes=7144
        stderr_bytes=0

全关运行：
    构建：
        日志/20260613_P4P5_all_output_off_rebuild_for_run.log
        build_exit_code=0
    命令：
        x64/Debug/鱼巢.exe --self-check-log --self-check-log-wait-ms=5000
    控制台：
        日志/20260613_P4P5_all_off_run_5000ms.console.log
    stderr：
        日志/20260613_P4P5_all_off_run_5000ms.stderr.log
    结果：
        run_exit_code=0
        console_bytes=0
        stderr_bytes=0
        日志/鱼巢_run_20260613.低值g delta=0
        日志/鱼巢_exception_20260613.低值g delta=0
        日志/鱼巢_selfcheck_orphan_requirement_20260613.低值g delta=0

最终默认 Rebuild 恢复：
    日志/20260613_P4P5_default_rebuild_restore_after_all_off_run.log
    exit_code=0
    仅 LNK4075。
```

## 禁止宣称

```text
不宣称业务链通过。
不宣称逻辑错误不存在。
不宣称任务执行及结算流程图已完整实现。
不宣称自我苏醒完成或内部循环全部完成。
```
