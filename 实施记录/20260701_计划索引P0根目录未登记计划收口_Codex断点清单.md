# 20260701 计划索引 P0 根目录未登记计划收口 Codex断点清单

## 0. 恢复摘要

当前切片：继续执行 `计划/计划索引.md` 的 P0 根目录计划清零，收口根目录中未被总账登记或已闭合但仍停留在根目录的计划文件。

本切片不改代码，不做业务闭环声明。

## 1. 输入和依据

```text
docs/00_文档总索引.md
规范/规范目录.md
规范/0050_项目通用结构与禁止性规则总纲_20260701.md
计划/计划索引.md
```

计划索引要求：根目录计划进入 `完成归档 / 待条件停放 / 明确堵点 / 阻塞承接 / 删除过时项` 之一。

## 2. 发现

根目录实际文件与 `计划/计划索引.md` 对照后，发现 3 个未按文件名进入总账的计划：

```text
计划/20260629_任务治理被动动态与动态抽象实现计划_v0.1.md
计划/20260629_日志输出预处理分级控制计划_v0.1.md
计划/追根因_20260630_R23体素候选包为0_v0.1.md
```

另有两个追根因文件只因索引使用短名登记，文件名精确比对误报；它们已在当前默认主动项登记为明确堵点。

## 3. 判定

```text
20260629_日志输出预处理分级控制计划_v0.1.md：
    实施记录显示默认构建、全关 Rebuild、全关运行和默认 Rebuild 恢复均有证据；
    全关运行无新增日志、无增长、stdout/stderr 为 0；
    判定为已闭合归档。

追根因_20260630_R23体素候选包为0_v0.1.md：
    文件自身记录“已闭合”，下一步转入 R23/P8d；
    当前计划索引 R23 已承接 P8d/P9 明确堵点；
    判定为已由 R23 承接，当前追根因文件归档。

20260629_任务治理被动动态与动态抽象实现计划_v0.1.md：
    文件当前停止点为“尚未修改代码，后续执行前应先处理 P0：规范口径确认或规范补丁”；
    该计划只有在当前切片需要任务治理被动动态、复合状态快照、动态抽象或抽象因果样本链时才应进入；
    判定为阻塞承接，不作为默认可执行项。
```

## 4. 实际操作

```text
移动到完成项：
    计划/20260629_日志输出预处理分级控制计划_v0.1.md
    -> 计划/完成项/20260629_日志输出预处理分级控制计划_v0.1.md

    计划/追根因_20260630_R23体素候选包为0_v0.1.md
    -> 计划/完成项/追根因_20260630_R23体素候选包为0_v0.1.md

更新计划索引：
    当前归档文件数改为 419；
    完成项归档池计数改为 419；
    新增 R25：任务治理被动动态与动态抽象实现计划，状态=阻塞承接；
    下一次执行入口补充本轮归档和登记记录。
```

## 5. 验证

```text
完成项文件数：419。
两个已闭合计划文件已位于 计划/完成项/。
根目录未登记精确文件只剩两个短名登记误报。
当前仍无默认可执行项；待条件项等待外部条件，阻塞承接项只在当前切片实际暴露对应缺口时进入。
```

## 6. 禁止宣称

```text
不宣称任务治理被动动态计划已实现。
不宣称日志输出以外的业务链通过。
不宣称 R23/P8d/P9 完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
