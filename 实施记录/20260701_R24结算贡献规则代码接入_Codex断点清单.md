# 20260701 R24 结算贡献规则代码接入 Codex 断点清单

## 0. 恢复摘要

当前切片承接 R24 P8：`规范/6100_子规范_安全服务值结算_20260701.md` 第 4 章已生成结算贡献权重规则，计划索引记录后续仍需代码接入并真实运行验证。

本轮用户补充口径：权重是在自我线程中新增需求时自动计算实现的。代码核查确认：自我线程通过统一需求树更新入口新增 / 复用需求后调用 `需求类::刷新子需求权重`，需求类负责执行父子权重刷新。

本轮用户补充口径：子需求整数平分时余数舍弃，不向任意子需求补余数。

## 1. 当前切片

目标：把 6100 第 4 章的最低代码口径接入现有路径：

1. 子需求权重平分时不再把整数余数任意加给靠前子需求。
2. 任务承接权重固化时继承来源需求安全 / 服务权重，不再按同一需求下多个任务评分拆分预算。
3. 普通价值结算贡献量按任务目标 actual 当前值 / target 目标值比例计算候选增量。
4. 对 `已识别区域扫描入账状态` 补结算贡献层级判定，消除 P8 中的 `缺结算贡献层级判定规则` 这一规则缺口；不绕过任务等待、观察事实承接、G0 / D2 和直接服务触发事实闸门。

## 2. 已读依据

- `计划/计划索引.md`：R24 当前仍为明确堵点，后续需代码接入并真实运行验证。
- `计划/20260630_需求树简化生成与内部治理闭环计划_v0.1.md`：P7a 已满足，P8 未闭合原因包括任务等待、子需求 9、缺贡献层级规则、无直接服务触发事实和 G0 / D2。
- `规范/6100_子规范_安全服务值结算_20260701.md` 第 4 章：结算贡献权重生成规则。
- `docs/工程图谱/03_安全服务结算图谱.md`、`docs/工程图谱/05_规则原子索引.md`：RULE-SETTLE-008。

## 3. 当前判断

当前计划可执行，但本轮只能关闭“规则代码接入”切片。即使构建和运行通过，只要任务 3 / 子需求 9 仍等待观察事实承接，P8 和 R24 根需求仍不得宣称完成。

恢复后编译失败锚点：`自我线程模块.impl.cpp(2250,9): error C3861`，原因是 `私有_按目标当前值计算结算达成比例百万分比_线程侧` 定义位于调用点之后且缺少前置声明。当前处理：补同 namespace 内部声明，不改变业务逻辑。

## 4. 禁止宣称

- 不宣称 R24 完成。
- 不宣称 P8 完成。
- 不宣称内部治理闭环完成。
- 不宣称直接服务正向触发事实路径已接入。
- 不把任务完成、actual 满足、等待项、日志、扫描方法动作动态或服务值衰减当成 G0 / D2 证据。

## 5. 验证预测

构建预测：

```text
Debug x64 构建应通过；若失败，优先定位本轮新增函数、结算比例调用和因果层级辅助函数的编译错误。
```

真实运行预测：

```text
运行 `.\x64\Debug\鱼巢.exe --self-check-log --demand-tree-growth-summary` 后，P7a 的扫描需求硬证据仍应保留：来源因果、来源动态和正式扫描需求节点可回查。
若任务 3 / 子需求 9 仍未形成完成或已结算被动因，P8 仍不得判定满足。
本轮代码应消除 `已识别区域扫描入账状态` 在安全 / 服务来源下的 `缺结算贡献层级判定规则` 规则缺口；若仍出现该缺口，继续定位因果投影目标特征解析。
无直接服务触发事实时，G0 / D2 正向服务入账仍应为 0。
```

## 6. 验证结果

- `python .\tools\生成函数功能说明文档.py`：退出码 0，函数条目 7522。
- `python .\tools\check_specs.py`：退出码 0，`hard issues: 0`，`warnings: 0`。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`：退出码 0，0 警告，0 错误。
- `.\x64\Debug\鱼巢.exe --self-check-log --demand-tree-growth-summary`：退出码 0，控制台输出落盘 `日志/codex_R24_settlement_run_20260701_1410.log`。
- 本轮真实运行证据：`日志/鱼巢_run_20260701.低值g:4591` 显示 `来源因果=26F`、`来源动态=26A`、`派生需求=6`、`目标特征=已识别区域扫描入账状态`；`4592` 显示 `正式派生需求入树=1` 和 `P7a来源因果链证据=1`。
- 本轮当前运行段从 `4333` 行后检索 `缺结算贡献层级判定规则` 数量为 0，说明扫描证据补齐目标的结算贡献层级规则缺口已被本切片消除。
- 本轮未越过服务正向入账闸门：`4387` 显示 `G0写入=0`、`D2写入=0`；`4500` 显示 `结算治理 | 已生成=否 | 有效推进=否 | 服务净变化=0 | 安全净变化=0`。
- 本轮剩余堵点未消除：`4583` 显示任务 3 / 需求 6 的外设观察等待项创建延后；`4620-4621` 显示任务 2 仍为 `推进状况=存在活动子需求`、`等待既有子需求=1`。

## 7. 结论

本切片“结算贡献规则代码接入”可以关闭。R24 P8 和 R24 根需求仍不得关闭；剩余恢复条件仍是任务 3 / 子需求 9 形成完成或已结算被动因，或取得正式观察事实承接条件。服务正向入账验收仍必须先补直接服务触发事实契约和 G0 / D2 最低信息位。
