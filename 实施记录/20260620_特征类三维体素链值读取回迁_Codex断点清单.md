# 20260620 特征类三维体素链值读取回迁 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_特征类逻辑纠偏计划_v0.1.md` 的 1.2，使用 `特征值类::取节点VecU只读指针` 替换三维体素链 direct `主信息.值`。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_特征类逻辑纠偏计划_v0.1.md
计划/20260620_特征值类逻辑纠偏计划_v0.1.md
实施记录/20260620_特征值类节点VecU只读指针_Codex断点清单.md
特征类.cpp
特征值类.h
```

本切片范围：

```text
1. 只替换 特征类.cpp 三维体素链读取中的 direct 主信息.值。
2. 使用 特征值类::取节点VecU只读指针 取得 const VecIU64* 后解析。
3. 不改变三维体素链编码、写入、查询、评分语义。
```

验证前预测：

```text
1. 特征类.cpp 不再命中 ->主信息.值。
2. 三维体素链解析仍通过 解析三维体素链节点VecU 执行。
3. 函数说明文档生成器和 Debug x64 构建应通过。
```

## 1. 实施记录

```text
1. 私有_查找三维体素子节点 中 当前->主信息.值 已改为 特征值类::取节点VecU只读指针(当前) 后解析。
2. 查询三维体素链节点 中 根节点 / 当前节点 / 子节点 的 主信息.值 已改为 特征值类::取节点VecU只读指针 后解析。
3. 比较三维体素占据链 中 根节点->主信息.值 已改为 特征值类::取节点VecU只读指针 后解析。
4. 失败路径保持原语义：取不到值或解析失败时仍返回原空结果 / 拒绝结果 / break。
5. 已运行 python .\tools\生成函数功能说明文档.py，函数条目保持 6881。
```

## 2. 验证结果

```text
1. rg -n -- "->主信息\\.值|\\.主信息\\.值" 特征类.cpp 特征类.h
   结果：无命中，退出码 1，符合预测。

2. rg -n "取节点VecU只读指针|解析三维体素链节点VecU" 特征类.cpp 特征值类.h
   结果：特征类.cpp:376、3493、3514、3536、3801 使用 特征值类::取节点VecU只读指针；解析仍走 解析三维体素链节点VecU。

3. rg -n "取主信息<[^>]+>|dynamic_cast<[^>]+>|->主信息|\\.主信息\\.值|状态节点主信息类|场景节点主信息类|动态节点主信息类|二次特征主信息类|因果主信息类|需求主信息类|方法主信息类" 特征类.cpp 特征类.h
   结果：非特征主信息 direct 读取已清空；剩余命中均为 特征节点主信息类 / 抽象特征主信息类 的明确取主信息，属于特征类职责内读取。

4. python .\tools\生成函数功能说明文档.py
   结果：退出码 0；源码文件 128；函数条目 6881；已生成 说明书/函数功能说明文档.md。

5. msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\20260620_特征类三维体素链值读取回迁_msbuild.log
   结果：退出码 0；已成功生成；0 个警告；0 个错误；生成 D:\鱼巢\x64\Debug\鱼巢.exe。
```

## 3. 当前判断

```text
1. 本切片已闭合：特征类.cpp 三维体素链 direct 主信息.值 已回迁到 特征值类::取节点VecU只读指针。
2. 特征类 1.2 已登记的非特征主信息 direct 操作残留已处理完：状态主信息、泛型主信息穿透、比较特征集合 direct cast、三维体素链 主信息.值 均已闭合。
3. 不宣称特征类逻辑纠偏完成；下一步按 计划/计划索引.md 选择下一个未完成主动计划。
```
