# P10f-q 外设观察报告来源外设字段复核 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-q `外设观察报告队列.ixx::结构_外设观察报告队列项::来源外设` 边界复核。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_observation_device_id.tsv`

当前问题：

- `结构_外设观察报告队列项::来源外设` 是非主键类 `std::string` 字段名。
- 写入值来自 D455 外设 ID，当前进入等待项匹配、按外设过滤、提交包头外设 ID 生成、任务管理观察事实承接请求写入。
- 字段语义是身份，不是自由文本；应收束为 `来源外设ID`。

构建前预测：

- 将字段改名为 `来源外设ID` 后，目标路径旧字段访问应无残留。
- `配置摘要 / 失败原因 / 差异摘要 / 跟踪种子` 等字段不混入本切片。
- 预期 Debug x64 构建通过，0 警告，0 错误。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。

## 1. 完成证据

- 已将 `结构_外设观察报告队列项::来源外设` 改名为 `来源外设ID`。
- 已同步更新 D455 报告写入、等待项目标外设匹配、提交包头外设 ID 生成、按外设 ID 查询 / 过滤、任务管理观察事实承接写入、正式读取口径匹配和报告摘要日志。
- 已生成 `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_observation_report_device_id.tsv`，剩余记录 627 行，其中主键 / ID 类 316 行、非主键类 311 行。
- `rg -n "\.来源外设\b|std::string 来源外设\{\}|来源外设\s*=" -g "*.ixx" -g "*.cpp" -g "*.h"` 无命中。
- `git diff --check` 通过。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 通过，0 警告，0 错误。

## 2. 下一候选

- `外设观察报告队列.ixx::结构_外设观察报告队列项::跟踪种子` 仍是非主键类 `std::string` 字段。
- 该字段进入 `构造跟踪提交包_由观察报告` 的缺目标种子判断；D455 跟踪路径还通过 `D455_尝试解析跟踪种子簇ID` 从文本解析簇 ID。
- 下一切片应复核是否可改为明确的跟踪种子 ID / 簇 ID 结构字段，避免用自由文本作为机器解析入口。
