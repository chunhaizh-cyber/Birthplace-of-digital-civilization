# 20260620 场景主信息直读直写扫描 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.2 P3.1，扫描 `场景节点主信息类` 字段直读 / 直写路径并按调用方分类。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
实施记录/20260620_场景关系索引双轨引用闭环_Codex断点清单.md
```

本切片范围：

```text
1. 只扫描和分类 场景节点主信息类 字段直读 / 直写路径。
2. 本切片不处理世界树场景坐标 / 轮廓 DTO 归属。
3. 本切片不新增函数；若分类后需要新增函数，另开 P3.2 小切片并先扫描 说明书/函数功能说明文档.md。
4. 显示 / 语言 / SQL 投影读取先登记为显示读取，不作为机器判断依据。
```

验证前预测：

```text
1. 状态类、动态类、二次特征类仍会直接维护 场景主信息 的状态 / 动态 / 二次特征事实目录。
2. 方法类系统逻辑会直接读取 二次特征索引，应成为 P3.2 优先补快照函数的候选。
3. 任务类可能读取 宿主存在；控制面板、世界树和语素类可能属于显示 / 语言 / 投影读取。
4. 关系索引字段删除后，不应再出现 `.关系索引` 或 `->关系索引` 字段访问。
```

禁止宣称：

```text
不宣称场景类逻辑纠偏完成。
不宣称所有场景主信息直读已回迁。
不宣称结构体清理完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 1. 扫描结果

已运行：

```powershell
rg -n "取主信息<场景节点主信息类>|dynamic_cast<.*场景节点主信息类|场景主信息->|\.状态索引|\.动态索引|\.关系索引|\.二次特征索引|\.宿主存在|\.世界类型" -g "*.h" -g "*.cpp" -g "*.ixx" -g "!x64/**" -g "!Debug/**" -g "!日志/**" -g "!logs/**"
```

证据文件：

```text
日志/20260620_场景主信息直读直写扫描_全仓引用.txt
```

命中统计：

```text
总命中：88
状态类.cpp：24
动态类.cpp：15
控制面板类.cpp：9
语素类.cpp：8
场景类.cpp：6
世界树类.cpp：4
二次特征类.cpp：3
语言类_基础信息命名.cpp：3
语言类_二次特征表达.cpp：3
任务类.cpp：3
自我线程模块.impl.cpp：2
其它文件：10
```

分类结论：

```text
1. 场景类.cpp 内部读取属于 owning class 内部操作，不作为外部直读缺口。
2. 状态类.cpp / 动态类.cpp / 二次特征类.cpp 仍直接维护场景事实目录索引；场景类已有 追加状态 / 追加动态 / 追加二次特征，但缺移除函数和快照函数。
3. 方法类.cpp 的 私有_场景二次特征索引 是方法匹配系统逻辑读取，应优先回迁到 场景类::读取场景二次特征索引快照。
4. 任务类.cpp 读取 宿主存在 用于推导任务虚拟存在承载世界，应后续回迁到 场景类::读取场景宿主存在。
5. 自我线程模块.impl.cpp 读取 状态索引 用于结果场景状态查找，可后续改用 场景类 快照或 状态类::获取场景状态。
6. 控制面板类.cpp / 世界树类.cpp / 语言类_* 属于显示、SQL 投影或命名读取，先登记为显示读取，不作为机器判断依据。
7. 字段访问中没有 `.关系索引` 或 `->关系索引` 残留。
```

当前结论：

```text
P3.1 扫描分类已闭合。
下一步进入 P3.2a：新增 场景类::读取场景二次特征索引快照，并将 方法类::私有_场景二次特征索引 改为调用场景类函数。
```
