# P2/P3 学习本能量化结果能力与缺口输出实施记录

## 执行范围

依据：

```text
计划/20260605_学习函数量化缺口与方法状态派生纠偏计划_v0.1.md
计划/20260605_P2P3学习本能量化结果能力与缺口输出纠偏小切片方案_v0.1.md
计划/计划索引.md 当前主动切片 P2/P3
```

修改文件：

```text
自我动作实现模块.ixx
```

## 已完成

1. 补齐自我动作模块内的量化特征入口：

```text
方法动作数量
方法条件结果对数量
```

`方法可执行入口数量` 和 `方法最近连续成功次数` 原已存在，本切片复用。

2. 补齐本能侧数量读取桥：

```text
方法动作数量_动作模块：
    按 目标方法->主信息.有方法动作() 读出 0 / 1。

方法可执行入口数量_动作模块：
    按 方法具有已注册本能执行入口() 读出 0 / 1。
```

3. `自我_尝试学习`：

```text
结果表新增：
    方法条件结果对数量

结果能力新增：
    方法条件结果对数量 = 1

失败派生缺口改为：
    目标方法.方法条件结果对数量 当前值 -> 1

输出结果新增：
    方法条件结果对数量 = 方法真实条件结果对数量(目标方法)
```

旧 `方法条件结果配对状态` 输出保留为兼容证据。

4. `自我_定向学习`：

```text
结果表新增：
    方法动作数量
    方法可执行入口数量

结果能力新增：
    方法动作数量 = 1
    方法可执行入口数量 = 1

目标识别新增：
    方法动作数量
    方法可执行入口数量
```

执行收紧：

```text
方法动作数量目标：
    只有真实 方法动作数量 > 0 才算闭合。

方法可执行入口数量目标：
    只有真实 方法可执行入口数量 > 0 才算闭合。

候选动作节点：
    只作为候选证据，不冒充真实动作数量或真实可执行入口数量。
```

失败派生缺口改为：

```text
目标为 方法动作数量：
    输出 目标方法.方法动作数量 当前值 -> 1

目标为 方法可执行入口数量：
    输出 目标方法.方法可执行入口数量 当前值 -> 1
```

旧 `方法动作状态` 输出保留为兼容证据。

5. `自我_练习`：

```text
结果表新增：
    方法最近连续成功次数

结果能力新增：
    方法最近连续成功次数 = 4
```

练习执行路径原已能在连续成功次数不足时输出：

```text
目标方法.方法最近连续成功次数 当前值 -> 4
```

本切片未改动该派生逻辑，只补齐可被方法查找命中的结果能力和结果表。

## 验证

构建命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

结果：

```text
构建通过。
警告：LNK4075 忽略 /EDITANDCONTINUE，由 /INCREMENTAL:NO 引起。
错误：0
```

## 仍需后续承接

1. `候选动作数量` 当前没有独立特征入口、宿主和写入桥；本切片没有把候选动作节点冒充为真实方法动作数量。
2. `结果符合次数 / 结果不符合次数` 当前由方法运行账更新路径写入，尚未形成“同一条件命中超过 3 次且无新结果对”的独立任务目标宿主。
3. `新结果分支数量` 当前缺少明确特征入口、宿主和写入桥。
4. `方法状态` 仍保留兼容输出；按计划应在 P4 由量化特征被动派生，避免学习本能主动反推状态。
