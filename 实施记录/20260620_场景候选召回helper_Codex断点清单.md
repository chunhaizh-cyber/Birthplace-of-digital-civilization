# 20260620 场景候选召回 helper Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.4 P5.2d，新增场景候选召回 DTO / helper，并让世界树旧入口薄封装适配。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
实施记录/20260620_场景候选召回迁移前置判定表.md
实施记录/20260620_存在实例判定helper_Codex断点清单.md
```

## 1. 问题锚点

```text
世界树类::查找场景存在_按深度或彩图轮廓图
结构_世界树场景轮廓查找参数
结构_世界树场景轮廓存在比较结果
```

当前判断：

```text
场景候选召回入口应归场景类；体素生成 / 比较仍归特征类。
值池和存在概念根作为显式参数传入场景 helper；世界树旧入口只做旧 DTO 到新场景 DTO 的适配。
```

验证前预测：

```text
1. 场景类.h / 场景类.cpp 新增场景候选召回 DTO / helper。
2. 世界树类::查找场景存在_按深度或彩图轮廓图 不再直接遍历候选和调用特征类体素比较，只做薄封装适配。
3. 函数说明文档生成退出码应为 0。
4. Debug x64 构建应通过。
```

## 2. 当前执行进展

已修改：

```text
1. 场景类.h 新增 结构_场景三维体素轮廓查找参数 和 结构_场景三维体素轮廓存在比较结果。
2. 场景类.h / 场景类.cpp 新增 场景类::查找存在_按深度或彩图轮廓图。
3. 场景 helper 显式接收 const 特征值类& 值池 和 const 存在节点类* 存在概念根，内部调用存在类实例判定 / 坐标读取 helper 和特征类体素比较。
4. 世界树类::查找场景存在_按深度或彩图轮廓图 改为薄封装，适配旧世界树 DTO 到新场景 DTO。
```

已验证：

```text
rg -n "结构_场景三维体素轮廓|场景类::查找存在_按深度或彩图轮廓图|世界树类::查找场景存在_按深度或彩图轮廓图|场景服务_\\.查找存在_按深度或彩图轮廓图|结构_世界树场景轮廓" 场景类.h 场景类.cpp 世界树类.h 世界树类.cpp 自我动作实现.外设模块.ixx 说明书/函数功能说明文档.md
结果：场景类已承接候选召回 helper；世界树旧入口只做薄封装；外设调用方仍使用世界树命名 DTO，留给下一切片。

python .\tools\生成函数功能说明文档.py
结果：exit=0，输出见 日志/20260620_场景候选召回helper_函数文档.log。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：exit=0，构建日志见 日志/20260620_场景候选召回helper_msbuild.log；0 警告。
```

当前判断：

```text
P5.2d 已闭合。
下一步进入 P5.3a：外设调用方切换到场景类 DTO / helper，之后再删除世界树命名 DTO / 旧包装入口。
```

禁止宣称：

```text
不宣称世界树场景辅助函数迁移完成。
不宣称场景候选召回外设调用方已改完。
不宣称场景类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
