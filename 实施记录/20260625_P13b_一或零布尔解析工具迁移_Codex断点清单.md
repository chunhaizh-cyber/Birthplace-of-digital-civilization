# 20260625 P13b 一或零布尔解析工具迁移 Codex断点清单

## 0. 恢复摘要

当前切片已闭合。目标是处理 `线程生命周期消息模块.ixx::私有_解析布尔(const std::string&)`。

依据：

```text
计划/计划索引.md：P13b 全局共享迁移；B 类候选逐个判定迁移 / 复用 / 保留。
实施记录/20260624_P13_函数自定义主信息分类候选表.tsv：`私有_解析布尔` 为 B 类候选，未命中自定义主信息 / 节点 / 结构 / 协议对象。
规范/禁止性规范20260611.md：禁止保留旧文本、旧日志格式或 fallback 作为当前判断依据。
```

当前判断：

```text
当前线程生命周期消息写入方统一用 `布尔文本_一或零` 写入 `1` / `0`。
本地 `私有_解析布尔` 仍接受 `"true"` 和 `"是"`，属于旧文本兼容口径。
本切片将解析函数迁入全局共享函数类，并收束为只认 `"1"` 为真；其它文本为 false。
该解析只服务文本键值文件协议读取，不承载项目结构状态或业务事实本体。
```

已执行：

```text
1. 在 `全局共享函数类.ixx` 新增 `解析一或零布尔文本(std::string_view)`。
2. 删除 `线程生命周期消息模块.ixx::私有_解析布尔`，调用点改为全局函数。
3. 运行函数说明文档生成脚本和 Debug x64 构建。
```

验证证据：

```text
python .\tools\生成函数功能说明文档.py 成功；扫描源文件 127 个，函数条目 7163。
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo 成功；0 警告，0 错误。
复扫确认：`私有_解析布尔` 已从源码和函数说明文档清除。
复扫确认：`文本 == "true"` 和 `文本 == "是"` 旧布尔兼容判断已从线程生命周期消息模块和全局共享函数类清除。
复扫确认：`解析一或零布尔文本` 已进入 `全局共享函数类.ixx`、线程生命周期消息模块调用点和函数说明文档。
```

禁止宣称：不宣称 P13 完成，不宣称函数去重全部完成，不宣称自我苏醒完成或初步成熟完成。
