# P10f-aw 二次特征表达方向模板文本匹配清理 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-aw `语言类_二次特征表达.cpp::结构_二次特征方向模板` 文本匹配清理。

依据文件：

- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `计划/计划索引.md`
- `实施记录/20260624_P10f_av_双目相机调用结果消息字段复核_Codex断点清单.md`
- `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_stable_subset_bridge_reason.tsv`

问题锚点：

- `语言类_二次特征表达.cpp` 中 `私有_二次特征轴键(...)` 读取 `语素集.安全获取词(主信息->概念名称)` 和 `度量签名_链键`。
- `私有_匹配二次特征方向模板(...)` 再按 `"左右" / "距离" / "高低"` 等文本关键词选择方向结果。
- 该路径属于自然语言表达层，但方向生成仍由文本关键词驱动，需改为读取 `种类 / 域 / 粒度 / 基准类型 / 值形态 / 方向区间 / 差值 / 标量值 / 是否满足` 等已有结构字段。

下一步：

- 删除 `结构_二次特征方向模板` 与文本关键词匹配路径。
- 用结构化方向编码生成 `正向 / 负向 / 相当 / 成立 / 不成立` 等显示文本。
- 保留 `语素集.安全获取词(...)` 仅作为节点名称、方法名、概念名等人类可读输出，不再用于方向模板匹配。

处理结论：

- 已删除 `结构_二次特征方向模板`、`私有_匹配二次特征方向模板(...)`、`私有_二次特征轴键(...)`、`私有_二次特征方向模板_按主信息(...)` 和无调用的空间关系文本拼装 helper。
- 新增 `私有_二次特征方向编码(...)`，方向结果读取 `方向区间 / 差值 / 标量值 / 值形态 / 是否满足 / 域`。
- `私有_二次特征维度短语(...)` 改为按 `枚举_二次特征种类` 生成，不再读取概念名或度量签名作为方向轴键。
- `语素集.安全获取词(...)` 在本文件仍用于节点名、方法名、概念名等人类可读输出；不再用于方向模板匹配或方向结果选择。

验证结果：

- `rg -n "结构_二次特征方向模板|私有_匹配二次特征方向模板|私有_二次特征轴键|私有_二次特征方向模板_按主信息|私有_二次特征空间关系结果|关键词|正向结果|反向结果|中性结果" 语言类_二次特征表达.cpp`：无命中。
- `python .\tools\生成函数功能说明文档.py`：成功，函数条目 7183。
- 已生成 `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_language_direction_template.tsv`：剩余 563 行，其中主键 / ID / 键 / 句柄类 331 行、非主键类 232 行。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`：成功，0 警告，0 错误。
- 本次触碰 `说明书/函数功能说明文档.md` 时出现 CRLF 提示，已仅对该文件做 LF 规范化；未做全仓换行处理。

当前下一步：

- 当前主动队列暂无新的无堵点执行切片；后续若复扫发现 string / 摘要 / 原因 / 说明进入候选选择、状态推进、等待状态、稳定键、结算或因果匹配，再按 P10f 规则重开。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
