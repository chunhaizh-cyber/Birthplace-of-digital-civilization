# 20260625 P13b 稳定时钟计时工具迁移 Codex断点清单

## 0. 恢复摘要

当前切片已闭合。目标是处理 `状态类.cpp::私有_状态创建阶段时间点()` 与 `状态类.cpp::私有_状态创建阶段耗时微秒(...)`。

依据：

```text
计划/计划索引.md：P13b 全局共享迁移；B 类候选逐个判定迁移 / 复用 / 保留。
实施记录/20260624_P13_函数自定义主信息分类候选表.tsv：`私有_状态创建阶段时间点` 为 B 类候选，未命中自定义主信息 / 节点 / 结构 / 协议对象。
全局共享函数类.ixx 文件头规则：无领域自定义主信息 / 领域类型参与的通用函数可进入本模块。
```

当前判断：

```text
`私有_状态创建阶段时间点` 只返回 `std::chrono::steady_clock::now()`。
`私有_状态创建阶段耗时微秒` 只计算两个 steady_clock 时间点之间的微秒耗时。
二者是纯标准库计时工具，不读取 / 写入项目结构。
`私有_状态创建阶段采样序号` 携带状态类慢创建采样策略和静态计数，保留在状态类本地。
```

已执行：

```text
1. 在 `全局共享函数类.ixx` 新增稳定时钟当前时间点与耗时微秒工具。
2. `状态类.cpp` import 全局共享函数类，删除本地两个计时 helper，调用点改为全局函数。
3. 运行函数说明文档生成脚本和 Debug x64 构建。
```

验证证据：

```text
python .\tools\生成函数功能说明文档.py 成功；扫描源文件 127 个，函数条目 7163。
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo 成功；0 警告，0 错误。
复扫确认：`私有_状态创建阶段时间点`、`私有_状态创建阶段耗时微秒` 已从源码和函数说明文档清除。
复扫确认：`稳定时钟时间点`、`稳定时钟耗时微秒` 已进入 `全局共享函数类.ixx`、状态类调用点和函数说明文档。
复扫确认：`私有_状态创建阶段采样序号` 保留在状态类本地，仍只承接状态创建慢路径采样策略。
```

禁止宣称：不宣称 P13 完成，不宣称函数去重全部完成，不宣称自我苏醒完成或初步成熟完成。
