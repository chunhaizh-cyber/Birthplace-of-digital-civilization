# 20260621 T0 任务主信息外部直读分类表

## 0. 扫描命令

```powershell
rg -n "任务->主信息\.|任务头->主信息\.|来源任务->主信息\.|父任务->主信息\.|子任务->主信息\.|任务节点->主信息\.|当前任务->主信息\." -g "*.cpp" -g "*.ixx" -g "*.h" -g "!任务类.*" -g "!任务主信息类.h" > 日志/20260621_T0_任务主信息外部直读扫描.txt
rg -n "struct .*任务|结构_.*任务|enum class .*任务|枚举_.*任务|结构_.*回执|枚举_.*回执|结构_.*执行|结构_.*筹办" -g "任务模块*.ixx" -g "任务类.h" -g "任务主信息类.h" > 日志/20260621_T0_任务结构协议扫描.txt
rg -n "需求主信息类|状态节点主信息类|特征节点主信息类|方法主信息类|因果主信息类|存在节点主信息类|场景主信息类|取.*主信息|dynamic_cast<.*主信息|->主信息\." 任务类.cpp 任务类.h > 日志/20260621_T0_任务类自身跨类直读扫描.txt
```

## 1. 扫描结果

```text
任务主信息外部直读扫描：100 行。
任务模块结构 / 协议扫描：1021 行。
任务类自身跨类直读扫描：39 行。
```

外部直读按文件分布：

```text
任务模块.管理界面线程.impl.cpp：31
任务模块.筹办.ixx：17
自我动作实现模块.ixx：14
任务模块.管理工作线程.ixx：11
自我线程模块.impl.cpp：10
鱼巢.cpp：9
控制面板摘要线程模块.impl.cpp：3
任务模块.执行.ixx：2
自我动作实现.服务模块.ixx：2
自检线程模块.impl.cpp：1
```

## 2. 分类

| 分类 | 命中范围 | 当前判定 |
| --- | --- | --- |
| 机器逻辑需回迁到任务类 | `任务模块.管理工作线程.ixx` 的场景、任务虚拟存在、对应需求、目标状态、启动时间读取 | 第一小切片 P0a。先补任务类只读 helper，再迁移该文件。 |
| 机器逻辑需分簇回迁 | `任务模块.筹办.ixx`、`任务模块.管理界面线程.impl.cpp`、`自我动作实现模块.ixx`、`自我线程模块.impl.cpp` | 后续按文件和字段分小切片；若缺需求 / 状态 / 场景 / 方法 helper，按 owning-class 规则转向。 |
| 显示 / 命令输出 / 摘要 | `鱼巢.cpp`、`控制面板摘要线程模块.impl.cpp` | 后置；不得反向作为机器判断依据。 |
| 任务模块协议结构 | 1021 行协议 / 回执 / 工作包结构命中 | 不在 P0 删除；后续 P6 单独审查。 |
| 任务类自身跨类读取 | `任务类.cpp` 39 行 | 先登记；其中需求主信息直接读取已被需求类 helper 覆盖，任务虚拟存在特征读写属于任务类封装，剩余跨类点后续单独审查。 |

## 3. P0a 最小切片

```text
目标：
    1. 在任务类补齐当前扫描已确认缺少的结构化只读 helper：
       读取 / 解析任务对应需求、任务虚拟存在、任务场景、任务目标状态；
       读取任务创建时间、启动时间、完成时间。
    2. 只迁移 `任务模块.管理工作线程.ixx` 的 11 个任务主信息直接读取点。

预测：
    需要新增函数，必须先查函数说明文档，修改后重新运行 `python .\tools\生成函数功能说明文档.py`。
    Debug x64 构建应通过。
```
