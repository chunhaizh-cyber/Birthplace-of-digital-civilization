# 20260618 函数去重与无效代码清理 Codex断点清单

## 0. 恢复摘要

当前切片：P0 计划与候选清单。

当前目标：

```text
1. 使用 说明书/函数功能说明文档.md 作为函数清单基准。
2. 生成重复函数名清单、无调用候选清单和函数审计判定表。
3. 本切片不修改源码。
4. 下一切片只删除 1 到 3 个证据最硬的无效函数。
```

已读取依据：

```text
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
docs/codex/实现验收审计模式.md
实施记录/20260616_重复定义函数共享引用_Codex断点清单.md
```

当前已知状态：

```text
说明书/函数功能说明文档.md 已存在。
函数清单统计：源码文件 130，函数条目 7297，定义 6757，声明 540。
结构体审计计划已停在范围确认点，不作为当前主动队列继续删除。
工作区已有两份规范文件存在非本切片修改，当前切片不得混入提交。
```

P0 已生成：

```text
实施记录/20260618_函数去重_重复函数名清单.tsv
    重复函数名组 929。
实施记录/20260618_函数去重_重复函数功能签名清单.tsv
    重复函数功能签名清单条目 929。
实施记录/20260618_函数去重_无调用候选清单.tsv
    删除 3 个未接入 helper 后重生成：
    自由函数定义名粗筛 4569；无调用粗筛候选 773；
    未命中入口 / 证据关键词候选 468。
实施记录/20260618_函数去重_判定表.md
    记录本轮统计、功能签名规则、首批待复核候选和已删函数证据。
```

用户补充计划要求已纳入计划：

```text
1. 无项目自定义数据类型参与的通用函数，迁移到 全局共享函数类.ixx。
2. 有项目自定义数据类型参与的通用函数，迁移回各自专用类、功能类或对应头文件。
3. 涉及结构体时，回查结构体审计和本计划断点；若结构体可删，服务该结构体的函数可一并删除。
4. 重复函数判定不只看函数名：
   先梳理每个函数功能并备注，再生成原始 C++ 签名和标准功能签名，
   最后按迁移归属改变原引用方式。
```

下一步：

```text
1. 提交本切片相关文件，不混入用户已有规范修改。
2. 下一切片进入重复函数迁移专项：
   先从 实施记录/20260618_函数去重_重复函数功能签名清单.tsv
   选择功能签名一致、迁移归属明确的函数组。
```

## 1. P1 已删函数证据

| 函数 | 原位置 | 删除依据 | 删除后复核 |
| --- | --- | --- | --- |
| `安全风险树因素需要通用证据搜索_筹办查询` | `任务模块.筹办.ixx:2213` | 仅定义、无真实调用、非入口 / 回调、未写项目结构。 | 源码 `rg -F` 未检出；函数说明文档未检出。 |
| `包目标值类型为I64` | `自我动作实现模块.ixx:5847` | 仅定义、无真实调用、非入口 / 回调、未写项目结构；同模块已有实际在用的 `参数值类型为I64`。 | 源码 `rg -F` 未检出；函数说明文档未检出。 |
| `差值约束为唯一零区间` | `自我动作实现模块.ixx:5932` | 仅定义、无真实调用、非入口 / 回调、未写项目结构；未参与差值约束判断调用链。 | 源码 `rg -F` 未检出；函数说明文档未检出。 |

已运行：

```text
python .\tools\生成函数功能说明文档.py
    成功；源码文件 130；函数条目 7294；定义 6754；声明 540。
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    成功；0 警告；0 错误。
```

禁止宣称：

```text
不宣称函数清理完成。
不宣称所有重复函数已经等价。
不宣称所有无调用函数都可删除。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
