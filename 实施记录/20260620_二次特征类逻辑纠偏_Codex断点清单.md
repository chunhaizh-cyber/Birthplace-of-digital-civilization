# 20260620 二次特征类逻辑纠偏 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_二次特征类逻辑纠偏计划_v0.1.md` 的 P8 二次特征信息操作入口审计。

依据：

```text
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_二次特征类逻辑纠偏计划_v0.1.md
```

预测：

```text
1. `二次特征类.cpp` 内部直接读取 / 写入 `二次特征主信息类` 属于 owning class 实现，原则上可保留。
2. `二次特征应用模块.ixx` 中明确归属二次特征生成模块、且最终经二次特征类创建 / 写入函数闭合的路径可先分类为二次特征内部生成链；若存在外部机器调用点直接解释字段，必须登记回迁。
3. 需求、方法、任务、因果、场景、世界树、自我线程或外设实现中的字段读取若参与查找、候选生成、目标判断、方向判断、条件匹配或结算，应列为机器逻辑回迁项。
4. 控制面板、SQL 投影、语言类自然语言表达只可作为显示 / 语言文本，不得反向参与机器判断。
5. 审计阶段先生成判定表；不直接改源码，不运行构建。
```

计划命令：

```powershell
rg -n "dynamic_cast<.*二次特征主信息类|new 二次特征主信息类|二次特征主信息类::|->形态|->域|->粒度|->来源起始状态|->来源结果状态|->来源动态|->来源场景|->主体|->客体|->差值|->标量值|->标量区间|->值形态|->概念名称|->方向区间" -g "*.h" -g "*.cpp" -g "*.ixx"
rg -n "枚举节点_按类型<二次特征主信息类>|取主信息<二次特征主信息类>|创建二次特征\(|追加二次特征\(" -g "*.h" -g "*.cpp" -g "*.ixx"
```

扫描结果：

```text
日志：日志/20260620_二次特征信息操作入口扫描.txt
scan1_exit=0，scan1_count=280
scan2_exit=0，scan2_count=22
判定表：实施记录/20260620_二次特征信息操作入口判定表.md
```

当前第一优先问题：

```text
方法类.cpp 和 任务模块.筹办.ixx 直接读取二次特征方向区间、差值、值形态、离散编码和是否满足，生成比较结果 / 需求目标方向编码。
这是方法能力匹配和任务筹办机器逻辑，应回迁为二次特征类只读编码入口。
```

当前改动范围：

```text
二次特征类.h
二次特征类.cpp
方法类.cpp
任务模块.筹办.ixx
说明书/函数功能说明文档.md
计划/20260620_二次特征类逻辑纠偏计划_v0.1.md
实施记录/20260620_二次特征信息操作入口判定表.md
实施记录/20260620_二次特征类逻辑纠偏_Codex断点清单.md
```

P8a 执行结果：

```text
1. 二次特征类.h / 二次特征类.cpp 已新增：
   - 二次特征类::读取方向比较结果编码_直接(const 二次特征主信息类*, std::int32_t&)
   - 二次特征类::读取方向比较结果编码_直接(const 二次特征节点类*, std::int32_t&)
2. 方法类.cpp 中方法结果能力方向比较编码已改为调用二次特征类入口。
3. 任务模块.筹办.ixx 中需求目标方向编码已改为调用二次特征类入口。
4. 该入口只读取结构化方向区间、差值、枚举编码和布尔满足，不读取语素词面文本。
```

P8a 验证结果：

```text
python .\tools\生成函数功能说明文档.py
    退出码=0；函数条目=7248。
窄口径复扫：
    日志/20260621_P8a_二次特征方向编码回迁_窄口径复扫.txt
    rg_exit=1；match_count=0。
git diff --check：
    退出码=0；仅有 LF/CRLF 工作区换行提示。
Debug x64 构建：
    日志/20260621_P8a_二次特征方向编码回迁_build.log
    退出码=0；已成功生成；0 个错误。
```

P8a 结论：

```text
已闭合。方法类和任务筹办不再直接解包二次特征方向编码字段。
未闭合后续项仍包括：方法类二次特征来源状态 / 左右对象特征类型推断、状态类二次特征引用计数、自我线程二次特征值读取、外设动作实现直接创建二次特征主信息、显示 / SQL / 语言层收口。
```

P8b 执行结果：

```text
1. `结构_二次特征方向签名材料` 已补充 左对象 / 右对象 引用。
2. 二次特征类::读取方向签名材料_直接 已填充 左对象 / 右对象。
3. 方法类.cpp 的二次特征特征类型推断已改为读取二次特征类方向签名材料，不再直接解包 来源起始状态 / 来源结果状态 / 左对象 / 右对象。
```

P8b 验证结果：

```text
python .\tools\生成函数功能说明文档.py
    退出码=0；函数条目=7248。
复扫：
    日志/20260621_P8b_方法二次特征特征类型材料回迁_复扫.txt
    rg_exit=1；match_count=0。
git diff --check：
    退出码=0；仅有 LF/CRLF 工作区换行提示。
Debug x64 构建：
    日志/20260621_P8b_方法二次特征特征类型材料回迁_build.log
    退出码=0；已成功生成；0 个错误。
```

P8b 结论：

```text
已闭合。方法类.cpp 当前二次特征主信息直接读取复扫清零。
下一项进入状态类二次特征引用计数回迁。
```

P8c 执行结果：

```text
1. 二次特征类.h / 二次特征类.cpp 已新增 二次特征类::统计状态引用数量。
2. 状态类::统计状态引用数量 已改为调用二次特征类统计二次特征中的状态引用次数。
3. 状态类.cpp 不再直接遍历 / 读取 二次特征主信息类 的来源起始状态、来源结果状态、左对象、右对象。
4. 状态类.cpp 中只剩定义的 私有_基础引用匹配状态 已删除。
```

P8c 验证结果：

```text
python .\tools\生成函数功能说明文档.py
    退出码=0；函数条目=7250。
复扫：
    日志/20260621_P8c_状态二次特征引用计数回迁_复扫.txt
    rg_exit=1；match_count=0。
git diff --check：
    退出码=0；仅有 LF/CRLF 工作区换行提示。
Debug x64 构建：
    日志/20260621_P8c_状态二次特征引用计数回迁_build.log
    退出码=0；已成功生成；0 个错误。
```

P8c 结论：

```text
已闭合。状态类二次特征引用计数直接读取已回迁到二次特征类。
下一项进入自我线程二次特征值读取回迁。
```

P8d 执行结果：

```text
1. 二次特征类.h / 二次特征类.cpp 已新增：
   - 二次特征类::读取差值_直接
   - 二次特征类::读取标量值_直接
   - 二次特征类::读取离散值_直接
2. 自我线程模块.impl.cpp 中刚写入二次特征差值后的回读已改为调用 二次特征类::读取差值_直接。
3. 自我线程模块.impl.cpp 中动态 V1 摘要的标量值 / 离散值读取已改为调用 二次特征类只读入口。
```

P8d 验证结果：

```text
python .\tools\生成函数功能说明文档.py
    退出码=0；函数条目=7257。
复扫：
    日志/20260621_P8d_自我线程二次特征值读取回迁_复扫.txt
    rg_exit=1；match_count=0。
git diff --check：
    退出码=0；仅有 LF/CRLF 工作区换行提示。
Debug x64 构建：
    日志/20260621_P8d_自我线程二次特征值读取回迁_build.log
    退出码=0；已成功生成；0 个错误。
```

P8d 结论：

```text
已闭合。自我线程模块.impl.cpp 当前二次特征值字段直接读取复扫清零。
下一项进入外设动作实现二次特征创建入口收口。
```

禁止宣称：

```text
不宣称二次特征类逻辑纠偏完成。
不宣称项目结构闭环全部通过。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
