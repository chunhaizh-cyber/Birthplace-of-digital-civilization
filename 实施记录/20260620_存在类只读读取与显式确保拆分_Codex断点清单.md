# 20260620 存在类只读读取与显式确保拆分 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_存在类逻辑纠偏计划_v0.1.md` 第一优先，拆分 `存在类` 只读读取与显式确保。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_存在类逻辑纠偏计划_v0.1.md
存在类.h
存在类.cpp
说明书/函数功能说明文档.md
```

已复核事实：

```text
1. 存在类::取存在主信息(存在节点类*) 当前读取后会调用 私有_确保存在具备三类信息根节点。
2. 存在类::取存在主信息(const 存在节点类*) 当前读取后通过 const_cast 调用 私有_确保存在具备三类信息根节点。
3. 外部显式 确保存在三类根节点 调用命中为非 const 自我存在路径。
4. 未发现外部调用 确保存在三类根节点(const 存在节点类*)。
```

本切片范围：

```text
1. 取存在主信息 两个重载改为只读，不创建、不补齐、不修改。
2. 删除 const 版 确保存在三类根节点，避免 const 路径补齐。
3. 保留非 const 显式确保入口。
4. 不处理存在观测缓存字段删除。
5. 不处理存在类中特征创建 / 特征读取职责迁移。
```

验证前预测：

```text
1. 存在类.cpp 不再命中 const_cast<存在节点主信息类。
2. 取存在主信息 函数体不再调用 私有_确保存在具备三类信息根节点。
3. 确保存在三类根节点 只保留非 const 入口。
4. 函数说明文档生成器和 Debug x64 构建应通过。
```

## 1. 实施记录

```text
1. 存在类::取存在主信息(存在节点类*) 改为纯只读：只调用 基础信息_->取主信息<存在节点主信息类>。
2. 存在类::取存在主信息(const 存在节点类*) 改为纯只读：不再 const_cast，不再补齐三类根节点。
3. 删除 存在类::确保存在三类根节点(const 存在节点类*) 声明和定义。
4. 保留 存在类::确保存在三类根节点(存在节点类*) 作为唯一显式补齐入口。
5. 获取需求根节点 / 获取任务根节点 / 获取方法根节点 继续只读返回已有根节点，不隐式创建。
6. 已运行 python .\tools\生成函数功能说明文档.py，函数条目从 6881 回到 6880。
```

## 2. 验证结果

```text
1. rg -n "const_cast<存在节点主信息类|bool 存在类::确保存在三类根节点\\(const|确保存在三类根节点\\(const" 存在类.cpp 存在类.h
   结果：无命中，退出码 1，符合预测。

2. rg -n "私有_确保存在具备三类信息根节点|取存在主信息\\(|确保存在三类根节点" 存在类.cpp 存在类.h
   结果：取存在主信息 函数体不再调用 私有_确保存在具备三类信息根节点；显式确保仅保留非 const 入口。

3. python .\tools\生成函数功能说明文档.py
   结果：退出码 0；源码文件 128；函数条目 6880；已生成 说明书/函数功能说明文档.md。

4. msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\20260620_存在类只读读取与显式确保拆分_msbuild.log
   结果：退出码 0；已成功生成；0 个警告；0 个错误；生成 D:\鱼巢\x64\Debug\鱼巢.exe。
```

## 3. 当前判断

```text
1. 本切片已闭合：存在类主信息读取不再隐式创建三类根节点，const 补齐路径已删除。
2. 不宣称存在类逻辑纠偏完成。
3. 下一步按 计划/20260620_存在类逻辑纠偏计划_v0.1.md 进入 1.1 存在结构 / 字段必要性审查与删除候选。
```
