# 20260625 P13b 需求SQL布尔复用全局工具 Codex断点清单

## 0. 恢复摘要

当前切片已闭合。已删除 `需求类.cpp::私有_SQL布尔` 和 `需求类.cpp::私有_需求SQL字段布尔` 两个本地重复 helper，并将调用点改为复用 `全局共享函数类.ixx::布尔文本_一或零`。

依据：

```text
计划/计划索引.md：P13b 全局共享迁移。
计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md：B 类函数已有同功能全局函数则复用，不重复新增。
说明书/函数功能说明文档.md：`布尔文本_一或零` 已是全局共享定义；需求类仍有两个本地 SQL bool helper。
```

当前判断：

```text
`私有_SQL布尔(bool)` 与 `私有_需求SQL字段布尔(bool)` 均只返回 "1" / "0"。
`布尔文本_一或零(bool)` 返回同样的 const char*，可直接用于 stream 输出；字段 vector 处用 std::string 包装。
本切片只处理 SQL 投影文本 helper，不涉及需求机器判断。
```

下一步：

```text
1. 继续 P13b，从剩余 B 类候选中选择下一个低风险同功能重复 helper。
2. 迁移 / 复用前继续先查 `全局共享函数类.ixx` 和 `说明书/函数功能说明文档.md`。
3. 下一候选优先检查语素类 SQL bool helper 是否可复用 `布尔文本_一或零`。
```

已完成：

```text
1. `需求类.cpp` 已 import `全局共享函数类`。
2. 已删除两个本地 SQL bool helper。
3. 2 个 SQL 字符串流输出点已改为 `布尔文本_一或零(...)`。
4. 2 个字段 vector 输出点已改为 `std::string(布尔文本_一或零(...))`。
5. 已运行函数说明文档生成脚本并刷新 `说明书/函数功能说明文档.md`。
```

验证：

```text
python .\tools\生成函数功能说明文档.py
结果：扫描源文件 127 个，生成函数条目 7173。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo
结果：构建成功，0 警告，0 错误。
```

禁止宣称：不宣称 P13 完成，不宣称函数去重全部完成，不宣称自我苏醒完成或初步成熟完成。
