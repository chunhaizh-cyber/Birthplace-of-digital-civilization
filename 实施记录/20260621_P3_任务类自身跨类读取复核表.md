# 20260621 P3 任务类自身跨类读取复核表

## 0. 复核依据

```text
计划：
    计划/20260620_任务类逻辑纠偏计划_v0.1.md P3。

复扫：
    日志/20260621_T0k_任务类自身跨类直读复扫.txt：广义命中 47 行。
    非任务主信息窄扫：无命中。
```

## 1. 分类复核

| 类别 | 复扫证据 | 当前判定 | 是否阻塞 |
| --- | --- | --- | --- |
| 需求主信息直读 | 窄扫 `来源需求->主信息 / 需求->主信息` 无命中；`私有_取需求目标状态` / `私有_取需求当前状态` 已调用 `需求类::解析需求目标状态` / `需求类::解析需求当前状态`；`私有_推导基准优先级` 已调用 `需求类::读取需求服务权重` / `读取需求安全权重`。 | 已回迁到需求类 helper。 | 否 |
| 状态主信息直读 | 窄扫 `状态节点->主信息` 无命中；任务目标和实际结果状态经任务类 / 状态引用 helper 流转。 | 未发现任务类直接读状态主信息字段。 | 否 |
| 特征主信息读取 | 广义命中 `世界树.取特征主信息(特征)`。 | 任务类读取任务虚拟存在上的任务特征主信息，用于任务虚拟存在特征封装；属于任务类封装任务特征，不是外部绕过。 | 否 |
| 存在 / 场景主信息直读 | 窄扫 `存在->主信息 / 场景->主信息` 无命中。 | 未发现任务类直接操作存在或场景主信息字段。 | 否 |
| 世界树功能调用 | 广义命中任务虚拟存在创建、场景推导、任务特征读写。 | 任务类作为任务域封装方可以调用世界树功能函数；未发现直接改写非任务世界树结构。 | 否 |
| 任务主信息自身读写 | 广义命中 `头结点->主信息.*`、SQL 投影、任务字段 helper。 | 属于任务类 owning-class 内部读写。外部直读已清零。 | 否 |

## 2. 当前结论

```text
1. P3 未发现必须临时提升其它 owning-class 计划的阻塞项。
2. 需求主信息、状态主信息、存在 / 场景主信息直读均未复现。
3. 特征和世界树访问保留在任务类封装任务虚拟存在特征的范围内。
4. 后续如 P4/P5/P6 暴露具体跨类缺函数，再按“缺哪个类补哪个类第一优先”单独登记。
5. 不宣称任务类逻辑纠偏完成。
```
