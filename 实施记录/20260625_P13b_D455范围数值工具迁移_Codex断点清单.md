# 20260625 P13b D455 范围数值工具迁移 Codex断点清单

## 0. 恢复摘要

当前切片：P13b 函数按自定义主信息 / 领域类型分类与全局共享迁移线。

目标函数族：

```text
外设线程_D455深度相机.ixx::D455_范围端点最大差
外设线程_D455深度相机.ixx::D455_范围跨度容差
外设线程_D455深度相机.ixx::D455_区间重叠
```

当前判断：

```text
三者签名只包含 std::int64_t / 指针输出。
函数体只做整数范围比较、重叠长度、绝对差和容差计算。
未读取 / 写入 D455 结构、外设协议、节点、主信息或项目领域对象。
可按 P13b 迁移到 `全局共享函数类.ixx`，并把 D455 本地定义改为调用全局通用函数。
```

已读依据：

```text
docs/00_文档总索引.md
docs/工程图谱/00_图谱总索引.md
docs/工程图谱/05_规则原子索引.md
规范/规范目录.md
计划/计划索引.md
计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md
说明书/函数功能说明文档.md
```

预测：

```text
迁移后 D455 本地三个 helper 删除，调用点改为全局通用函数。
全局共享模块新增三个纯 I64 范围工具，不引入项目领域头。
需要刷新函数说明文档并构建验证。
```

## 1. 已执行

```text
1. 在 `全局共享函数类.ixx` 新增：
   - `闭区间重叠I64`
   - `区间端点最大差I64`
   - `区间跨度容差I64`

2. 删除 `外设线程_D455深度相机.ixx` 本地重复定义：
   - `D455_区间重叠`
   - `D455_范围端点最大差`
   - `D455_范围跨度容差`

3. D455 调用点改为调用全局通用函数。

4. 重新生成 `说明书/函数功能说明文档.md`。
```

## 2. 验证

```text
旧函数复扫：
    `D455_区间重叠`
    `D455_范围端点最大差`
    `D455_范围跨度容差`
    已从源码和函数说明文档清除。

新函数复扫：
    `闭区间重叠I64`
    `区间端点最大差I64`
    `区间跨度容差I64`
    已进入 `全局共享函数类.ixx` 和 `说明书/函数功能说明文档.md`。

python .\tools\生成函数功能说明文档.py
    成功；源码文件 127，函数条目 7175。

python .\tools\check_specs.py
    hard issues 0，warnings 81。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    已成功生成；0 警告，0 错误。

git diff --check
    通过。
```

## 3. 停止点

```text
本切片已闭合。
下一步仍按 P13b 从剩余 B 类低风险候选中选择一个不在当前脏改文件中的小函数族。
禁止宣称 P13 完成、函数去重全部完成、自我苏醒完成或初步成熟完成。
```
