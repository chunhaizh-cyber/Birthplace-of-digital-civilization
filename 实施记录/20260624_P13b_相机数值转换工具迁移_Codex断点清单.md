# 20260624 P13b 相机数值转换工具迁移 Codex 断点清单

## 0. 恢复摘要

当前切片：P13b 全局共享迁移第七小切片。

已完成：

```text
1. 将 `双目相机本能适配器.cpp` 中不依赖外设材料结构的纯数值 helper 迁入 / 复用到 `全局共享函数类.ixx`：
   - `转换像素计数(std::size_t)`
   - `转换毫米(double)`
   - `比例万分比(std::size_t, std::size_t)`
2. 删除 `双目相机本能适配器.cpp` 本地重复定义。
3. `绝对差毫米` 未新增全局重复函数，调用点改为复用已存在的 `绝对差I64(I64, I64)`。
4. `深度差异评分` 属于深度质量评分策略，暂留在双目相机适配器本地，不作为本轮通用迁移对象。
5. 已刷新 `说明书/函数功能说明文档.md`。
6. 已按本次触碰范围处理 `双目相机本能适配器.cpp` 和 `说明书/函数功能说明文档.md` 的 LF 换行提示。
```

依据：

```text
计划/计划索引.md：P13b 从 B 类低风险候选中逐个迁移不带项目自定义主信息 / 领域类型的通用函数。
计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md：P13 分类迁移规则。
规范/禁止性规范20260611.md：禁止用文本 / 兼容路径冒充机器逻辑；本切片只移动纯数值函数，不新增业务承载结构。
```

验证：

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo
```

结果：

```text
函数说明文档生成成功：源码文件 127，函数条目 7190。
Debug x64 构建通过：0 警告，0 错误。
```

下一步：

```text
继续 P13b，从剩余 B 类低风险候选中选择不在当前脏改文件中的小函数族。
优先处理纯数学 / 转换 helper；对参数生成策略、日志、数据库、控制面板、线程生命周期和领域评分策略先保留或另开专项复核。
```

禁止宣称：

```text
不宣称全部函数迁移完成。
不宣称全部重复函数已清理完成。
不宣称内部循环完成 / 自我苏醒完成 / 初步成熟完成。
```
