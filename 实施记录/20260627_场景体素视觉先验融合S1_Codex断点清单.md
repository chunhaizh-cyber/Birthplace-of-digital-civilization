# 20260627 场景体素视觉先验融合 S1 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260625_场景体素视觉先验融合实现计划_v0.1.md` / S1 场景体素同步只读模块。

S0 已提交：

```text
提交：91deceb 新增场景体素视觉先验融合规范准入
验证：python .\tools\check_specs.py 退出码 0；hard issues: 0。
```

S1 目标：

```text
新增同步版 `场景体素模块.ixx`；
提供场景体素缓存类、SceneVoxelPrior 只读快照、点查询、AABB 查询和射线粗查询；
只从当前场景子存在、存在绝对坐标和三维体素特征句柄读取；
不写世界树事实、不写需求满足、不生成动作动态。
```

允许修改：

```text
场景体素模块.ixx
鱼巢.vcxproj
鱼巢.vcxproj.filters
说明书/函数功能说明文档.md
实施记录/20260627_场景体素视觉先验融合S1_Codex断点清单.md
计划/20260625_场景体素视觉先验融合实现计划_v0.1.md
计划/计划索引.md
```

禁止修改：

```text
6145 / 6146 / 6147 / 6150 业务入口
外设线程
世界树、需求树、任务管理或动作动态写入逻辑
伪造样本、夹具入口、模拟外设报告或伪造动作动态
```

## 1. 前置核查

```text
git status --short --untracked-files=no -- '鱼巢.vcxproj' '鱼巢.vcxproj.filters'：无输出，项目文件当前无未归属脏改。
说明书/函数功能说明文档.md：未命中现成“场景体素服务 / SceneVoxelPrior / 体素快照”函数；可复用现有场景、存在、特征只读入口。
```

复用入口：

```text
场景类::获取子存在
特征类::查找子特征_按类型
特征类::读取三维体素特征值
存在类::读取存在场景绝对坐标
全局共享函数类::坐标转I64三元
```

## 2. 预测

```text
S1 只生成运行期只读快照；
快照项只收录同时具备存在指针、三维体素根句柄和绝对坐标的场景子存在；
缺体素特征类型、缺坐标、缺体素句柄只计数，不创建缺口节点；
构建通过只能证明接口可用，不证明视觉业务闭合。
```

## 3. 待验证命令

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

## 4. 本轮修改

```text
新增：场景体素模块.ixx
修改：鱼巢.vcxproj
修改：鱼巢.vcxproj.filters
修改：说明书/函数功能说明文档.md
修改：实施记录/20260627_场景体素视觉先验融合S1_Codex断点清单.md
```

模块输出：

```text
结构_场景体素同步参数
结构_场景体素存在项
结构_场景体素快照统计
结构_场景体素只读快照 / 结构_SceneVoxelPrior
结构_场景体素AABB / 结构_场景体素AABB统计
结构_场景体素射线粗查询参数 / 结构_场景体素射线粗命中项
场景体素缓存类
```

已提供入口：

```text
场景体素缓存类::同步当前场景快照
场景体素缓存类::读取最新快照
场景体素缓存类::查询AABB
场景体素缓存类::统计AABB
场景体素缓存类::查询点附近
场景体素缓存类::射线粗查询
```

## 5. 验证结果

实际命令：

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
git diff --check
```

结果：

```text
函数功能说明文档生成退出码 0。
源码文件：128
函数条目：7254

Debug x64 构建退出码 0。
0 个警告，0 个错误。

git diff --check 退出码 0。
仅提示 说明书/函数功能说明文档.md、鱼巢.vcxproj、鱼巢.vcxproj.filters 后续被 Git 触碰时 CRLF 将转 LF；无 whitespace error。
```

结论：

```text
S1 同步版场景体素只读模块已完成工程接入和构建验证。
S1 只证明模块接口和工程接入可用，不证明视觉融合业务闭合。
下一切片进入 S2：从当前场景已确认存在集合生成 SceneVoxelPrior，并对缺坐标、缺体素、缺坐标系版本形成结构化缺口口径。
```
