# P10f-l 方法树 SQL 行文本字段边界复核 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-l `方法类.cpp::结构_方法树SQL行` 文本字段边界复核。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_causal_structured.tsv`

执行口径：

- string 字段进入方法查找、排序、选择、执行准入、状态推进或稳定键时必须改为项目内部结构、枚举、入口或句柄。
- SQL 投影、控制面板显示和日志诊断文本未进入机器判断时不按字段名硬删。

复核对象：

- `方法类.cpp:1077` `结构_方法树SQL行::路径`
- `方法类.cpp:1079` `结构_方法树SQL行::节点种类文本`
- `方法类.cpp:1080` `结构_方法树SQL行::动作名文本`
- `方法类.cpp:1081` `结构_方法树SQL行::动作句柄文本`
- `方法类.cpp:1083` `结构_方法树SQL行::来源文本`

证据：

- `结构_方法树SQL行` 在 `方法类.cpp` 内局部定义，只由 `私有_收集方法树SQL行` 和 `私有_填充方法树SQL角色字段` 从方法节点、枚举、动作句柄和主键生成。
- `节点种类文本 / 来源文本 / 动作名文本 / 动作句柄文本 / 路径` 只写入 `fishnest.method_tree_node`、`fishnest.method_main_info` 和 `fishnest.v_current_method_tree_nodes`。
- 机器可用结构化列仍保留：`节点种类值 / 来源值 / 方法虚拟存在主键 / 条件场景主键 / 结果场景主键 / 主结果特征主键 / 结果项数量 / 允许自动查找 / 有方法动作 / 有结果能力`。
- 仓内反查 `v_current_method_tree_nodes / method_main_info / node_kind_text / action_name / action_handle / source_text`，除 `方法类.cpp` SQL 投影生成外，只命中 `控制面板类.cpp` 的根层查询和子层 JSON 查询。
- `控制面板类.cpp` 只把这些列作为 SQL 控制面板树数据显示，不回流到方法候选查找、排序、选择或执行准入。

当前判断：

- 本切片属于 SQL 投影 / 控制面板显示边界，未发现文本字段进入机器判断。
- 不改代码，不构建。

下一步：

- P10f-l 闭合。
- 下一候选按复扫清单转入 `基础数据类型.h::结构体_分词` 的 `词 / 词性` 边界复核。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
