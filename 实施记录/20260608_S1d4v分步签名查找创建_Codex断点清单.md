# 20260608 S1d4v 分步签名查找创建 Codex 断点清单

## 当前切片

- 主动计划：`计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- 执行方案：`计划/20260607_S1d4v分步签名查找创建内部阶段定位与瘦身方案_v0.1.md`
- 切片：S1d-4v 分步签名查找创建内部阶段定位 / 瘦身。

## 依据文件

- `AGENTS.md`
- `规范/规范目录.md`
- `规范/方法动作动态硬规则规范20260512.md`
- `规范/动作动态即因果账本规范20260512.md`
- `规范/二次特征规范20260520.md`
- `规范/远程执行通道与断点续跑规范20260524.md`
- `计划/计划索引.md`
- `计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- `计划/20260607_S1d4u条件候选收集内部阶段定位与瘦身方案_v0.1.md`
- `实施记录/20260608_S1d4u条件候选收集_Codex断点清单.md`

## 当前判断

- S1d-4u 已闭合并提交：`517cc6c`。
- 最新运行线程 T24772：
  - `条件候选收集` 高层平均约 5054us，较 S1d-4t 的约 225225us 明显下降。
  - `动态二次特征生成/内部阶段耗时` 样本 10 条，最慢阶段分布 `分步结果生成=7`、`命中回写因果实例=3`。
  - `动态二次特征分步结果/内部阶段耗时` 样本 7 条，最慢阶段全部为 `分步签名查找创建`。
  - `分步签名查找创建` 平均约 83114us。
- `分步签名查找创建` 对应 `二次特征类::获取或创建子二次特征_按签名`。
- 当前实现未拆内部阶段，且模板已有签名时仍会先复制模板。

## 构建 / 运行前预测

- Debug x64 构建通过。
- 真实运行退出码 0。
- 日志继续出现 `动态二次特征分步结果/内部阶段耗时`。
- 日志出现 `二次特征签名查找创建/内部阶段耗时`，或在 5ms 阈值下无样本但高层分步结果仍可统计。
- 日志保留 `任务状态动作动态=<动态主键>`。
- 不出现 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`。
- 不出现旧路径锚点 `准备本能调用`、`执行本能方法`、`任务域本能输入包`。

## 允许修改

- `二次特征类.cpp`
- `计划/20260607_S1d4v分步签名查找创建内部阶段定位与瘦身方案_v0.1.md`
- `计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- `计划/计划索引.md`
- `实施记录/20260608_S1d4v分步签名查找创建_Codex断点清单.md`

## 已改文件

- `二次特征类.cpp`
- `计划/20260607_S1d4v分步签名查找创建内部阶段定位与瘦身方案_v0.1.md`
- `计划/计划索引.md`
- `实施记录/20260608_S1d4v分步签名查找创建_Codex断点清单.md`

## 已完成修改

- `二次特征类.cpp` 已新增 `二次特征签名查找创建/内部阶段耗时`。
- `获取或创建子二次特征_按签名` 已拆分模板签名准备、子节点枚举、签名比较、节点创建。
- 模板已有 `度量签名_链键` 时直接用于查找，不再先复制模板；模板签名为空时仍复制并刷新后再查找 / 创建。
- 新建节点仍复制最终有效模板，保留正式二次特征创建和签名语义。

## 已运行命令

- `git diff --check -- 二次特征类.cpp 计划/计划索引.md 实施记录/20260608_S1d4v分步签名查找创建_Codex断点清单.md`
  - 退出码 0；仅有 CRLF 工作区转换警告。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
  - 退出码 0；0 errors；保留 LNK4075 增量链接相关警告。
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000`
  - 退出码 0；末尾 `D455 打开失败: No device connected` 属硬件条件，不作为本切片失败。

## 验收结果

- 最新日志窗口：`日志/鱼巢_run_20260608.低值g`，启动行 `2026-06-08 11:37:55.227 [T24124] 程序启动`。
- `二次特征签名查找创建/内部阶段耗时`：0 条。
- `动态二次特征分步结果/内部阶段耗时`：5 条。
- `动态二次特征生成/内部阶段耗时`：9 条。
- `动态二次特征条件候选收集/内部阶段耗时`：10 条。
- `二次特征值写入/内部阶段耗时`：0 条。
- `二次特征概念模板刷新/内部阶段耗时`：0 条。
- `二次特征绑定场景/内部阶段耗时`：0 条。
- `任务状态动作动态=`：54 条。
- `任务状态被动提交/逻辑错误`：0。
- `任务状态动作动态逻辑错误`：0。
- `准备本能调用`：0。
- `执行本能方法`：0。
- `任务域本能输入包`：0。

## 性能证据

- 分步结果样本 5 条，平均总耗时约 141344us，最大约 248903us。
- 分步结果最慢阶段分布为 `分步值写入=4`、`分步签名查找创建=1`。
- `分步签名查找创建` 平均约 43477us，最大约 89788us；较 S1d-4u 的约 83114us 下降。
- `分步值写入` 平均约 54277us，最大约 87468us。
- `分步语义绑定` 平均约 16784us。
- 动态二次特征生成样本 9 条，平均总耗时约 170511us，最慢阶段分布为 `分步结果生成=6`、`命中回写因果实例=3`。
- `条件候选收集` 平均约 4511us，仍保持 S1d-4u 后的低位。

## 结论

- S1d-4v 已闭合。
- 单次签名查找创建未超过 5000us 慢采样阈值，剩余聚合耗时来自分步内多次调用。
- 当前慢点转到 `分步值写入` 聚合调用。

## 下一步

1. 进入 S1d-4w：分步值写入聚合调用次数定位 / 瘦身。
2. 优先统计分步内各类值写入调用次数和单次耗时阈值命中情况。
3. 未确认重复写入前，不跳过正式值写入。

## 禁止宣称

- 不宣称自我苏醒完成。
- 不宣称初步成熟完成。
- 不把定位日志当成性能优化完成。
- 不跳过正式二次特征创建或改变签名语义。
