# 20260620 存在比较归一化差异度接入 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_特征类逻辑纠偏计划_v0.1.md` 的 1.1 P2.2 字段级引用闭环，优先处理 `存在比较结果::归一化差异度`。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
规范/特征类规范20260429.md
规范/主信息字段与结构新增准入约束规范20260519.md
计划/计划索引.md
计划/20260620_特征类逻辑纠偏计划_v0.1.md
实施记录/20260620_特征类结构体必要性审查_Codex断点清单.md
```

已复核锚点：

```text
特征类.h:15 存在比较结果 包含 归一化差异度。
特征类.cpp:4244 计算 归一化差异度。
二次特征应用模块.ixx:2965-2977 当前只消费 总差异度、命中特征数、比较特征数。
```

本切片范围：

```text
1. 不删除 存在比较结果 结构体。
2. 对 归一化差异度 做字段级闭环：接入二次特征生成，或如证据不支持则删除字段和计算。
3. 不处理 说明 / 失败原因 / 不可比较原因 文本字段。
4. 不处理 特征类 1.2 非特征主信息直接操作审查。
```

验证前预测：

```text
1. 归一化差异度已有计算来源，且属于存在比较摘要，接入二次特征生成比删除字段更符合项目结构闭环。
2. 代码只需在 二次特征生成模块::由存在比较结果生成二次特征 中新增一个标量结果写入。
3. Debug x64 构建应通过。
```

## 1. 实施记录

```text
已采用接入方案，不删除字段：
二次特征应用模块.ixx:2969-2973 在 二次特征生成模块::由存在比较结果生成二次特征 中新增：
    概念名称：归一化差异度
    种类：枚举_二次特征种类::存在比较
    值：比较结果.归一化差异度
    度量签名后缀：summary:normalized_diff
```

## 2. 验证结果

```text
rg -n "归一化差异度|summary:normalized_diff|summary:total_diff" 特征类.h 特征类.cpp 二次特征应用模块.ixx
结果：
    特征类.h:15 定义字段。
    特征类.cpp:4244 计算字段。
    二次特征应用模块.ixx:2970-2973 写入二次特征。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\20260620_存在比较归一化差异度接入_msbuild.log
结果：退出码 0；日志显示 0 个警告、0 个错误。
```

当前判断：

```text
存在比较结果::归一化差异度 已形成 定义 -> 计算 -> 二次特征结构承载 的闭环。
本切片不宣称特征类结构体清理完成。
下一步继续 P2.2 / P2.4 剩余高风险项：文本字段和消费待确认结构。
```
