# 20260622 函数去重自我线程兼容 helper Codex断点清单

## 0. 恢复摘要

当前主动计划：`计划/计划索引.md` 第 1 章 `函数去重专项前置复核`。

当前切片：

```text
索引子切片 P11a：自我线程核心链单函数复核。
源码对象：自我线程模块.impl.cpp。
目标函数：私有_状态需求条件兼容_线程侧。
```

已复核依据：

```text
计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md：普通低风险 helper 删除暂止，剩余自我线程核心链候选必须单独复核。
实施记录/20260618_函数去重与无效代码清理_Codex断点清单.md：非成员普通候选剩余，需要从自我线程核心链专项选择 1 到 3 个目标，先回查规范和调用路径。
```

当前证据：

```text
`rg -n "私有_状态需求条件兼容_线程侧" -g "*.cpp" -g "*.h" -g "*.ixx"` 只命中 `自我线程模块.impl.cpp` 中的函数定义。
函数说明文档仅有定义条目。
函数去重无调用候选清单包含该函数。
函数体只解析已有状态并调用 `私有_状态语义相同_线程侧` 返回 bool，无写入、无提交、无动作动态、无状态推进副作用。
```

构建前预测：

```text
删除 `私有_状态需求条件兼容_线程侧` 不影响仍被调用的 `私有_状态语义相同_线程侧`、`私有_状态节点特征类型_线程侧`、`私有_状态主体主键_线程侧`；预期函数文档刷新后 Debug x64 构建通过。
```

待验证：

```text
已执行源码删除、函数说明文档刷新、候选清单更新、复扫和 Debug x64 构建。
函数文档命令：python .\tools\生成函数功能说明文档.py
函数文档结果：函数条目 7254。
候选清单：已从 实施记录/20260618_函数去重_无调用候选清单.tsv 移除目标行。
复扫结果：目标函数在源码、函数说明文档和无调用候选清单中均无命中。
构建命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
构建日志：日志/20260622_P11a_状态需求条件兼容helper删除_build.log
构建结果：exit_code=0，未检出 error / fatal / warning。
```

当前停止点：

```text
P11a 已闭合：删除 `自我线程模块.impl.cpp::私有_状态需求条件兼容_线程侧`。
未删除仍被调用的 `私有_状态语义相同_线程侧`、`私有_状态节点特征类型_线程侧`、`私有_状态主体主键_线程侧`。
```

下一步：

```text
若继续 P11，仍从非成员普通候选中一次复核 1 个目标；自我线程核心链、任务管理调度和公开接口不得批量删除。
```
