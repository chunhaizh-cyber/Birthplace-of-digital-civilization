# 20260622 P11h 无调用函数循环清理 Codex断点清单

## 0. 恢复摘要

当前主动计划：

```text
计划/计划索引.md 第 1 章 P11h：无调用函数循环删除。
```

用户最新口径：

```text
旧_外设线程_D455深度相机.ixx 已经过期，直接删除。
无用函数清理优先；一个函数没有被调用过就直接删除。
全部删除完成后复扫；如果还有无用函数，继续删除，直到没有无用函数。
```

执行口径：

```text
1. “没有被调用过”必须用 live 源码证据判定。
2. 直接删除范围：无直接调用、无构造 / 析构隐式调用、无 virtual / override、非入口 / 回调 / 导出、非注册表 / 消息分发 / 模板实例 / 宏拼接 / 公开合同引用，且不承担项目结构写入 / 读取证据。
3. 同文件 / 同族满足条件的函数可以批量删除。
4. 每轮删除后刷新函数说明文档、Debug x64 构建、复扫候选。
```

本轮预测：

```text
1. 删除未跟踪过期文件 `旧_外设线程_D455深度相机.ixx` 不影响构建。
2. 旧 TSV 中的构造 / 析构、公开类接口、世界树创建 / 确保 / 写入函数不能按固定字符串粗筛直接删除。
3. 需要先用 live 扫描重建候选，选择证据最硬的一批无调用普通函数删除。
4. 删除函数后 `python .\tools\生成函数功能说明文档.py` 与 Debug x64 构建应通过；若失败，先修正真实调用证据或回滚本轮删除对象。
```

已读取依据：

```text
AGENTS.md
docs/00_文档总索引.md
docs/工程图谱/00_图谱总索引.md
docs/工程图谱/04_Codex改代码前检查清单.md
docs/工程图谱/05_规则原子索引.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md
实施记录/20260618_函数去重与无效代码清理_Codex断点清单.md
实施记录/20260622_函数去重剩余候选边界复核_Codex断点清单.md
```

## 1. 已完成动作

```text
已删除过期未跟踪文件：旧_外设线程_D455深度相机.ixx。
已更新计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md：加入 2026-06-22 当前优先清理规则。
已更新计划/计划索引.md：维护规则新增无调用函数清理优先；当前主动队列切换到 P11h。
```

## 2. 首轮 live 复扫与删除

```text
已生成 live 重扫表：实施记录/20260622_P11h_live_无调用候选重扫.tsv。
旧 TSV 中 call_like_count=0 的候选多数已被前序切片删除，本轮不按旧行号处理。
旧 TSV 中 call_like_count=1 的 12 个候选已复核：
    任务 / 自我线程 / 需求创建 / 安全风险树 / 线程状态落账相关函数命中项目结构读写或生命周期专项；
    基础信息主信息类.h::设置当前值 是特征值写入 API；
    基础数据类型.h::颜色_at 是原始场景帧相邻读取接口；
    本轮不按普通无调用 helper 删除。
```

首轮删除对象：

```text
语言类.h:
    删除未调用声明：
        生成二次特征反义词结果
        生成二次特征介词短语
        生成存在有什么表达
        生成存在是什么表达
        生成存在能什么表达
        生成存在会什么表达
        生成存在需要什么表达

语言类_二次特征表达.cpp:
    删除上述两个二次特征文本包装函数定义。

语言类_存在表达.cpp:
    上述五个存在问句式表达函数删除后，该文件只剩匿名命名空间私有 helper；
    该文件未接入 鱼巢.vcxproj，已整体删除。

说明书:
    已刷新 说明书/函数功能说明文档.md；
    已同步 说明书/语言类说明.md 的现行接口和删除记录。
```

当前待验证：

```text
1. Debug x64 构建。
2. 删除后复扫无调用候选；若仍有符合规则的可删项，继续下一轮。
```

首轮验证结果：

```text
python .\tools\生成函数功能说明文档.py 通过：
    源码文件：127
    函数条目：7219

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m 通过：
    0 警告
    0 错误
```

## 3. 第二轮 live 复扫与删除

```text
已生成复扫表：实施记录/20260622_P11h_live_无调用候选重扫_after_round1.tsv。
第二轮可直接删除对象：
    自检线程模块.ixx / 自检线程模块.impl.cpp::自检线程类::读取最近事件列表。

判定：
    live 只有声明和定义；
    无调用点；
    不是入口 / 回调 / 导出 / virtual / override；
    返回诊断字符串列表，不写项目结构；
    读取摘要() 已承载当前摘要输出中的 最近事件列表。
```

第二轮处理：

```text
已删除 自检线程类::读取最近事件列表 的声明和定义。
已刷新 说明书/函数功能说明文档.md：
    源码文件：127
    函数条目：7218
```

第二轮验证结果：

```text
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m 通过：
    0 警告
    0 错误
```

## 4. 第三轮 live 复扫与删除

```text
已生成复扫表：实施记录/20260622_P11h_live_无调用候选重扫_after_round2.tsv。
第三轮可直接删除对象：
    自我线程类::读取最近Tick时间
    自我线程类::读取上次故障时间

判定：
    两个函数 live 只有声明和定义；
    无调用点；
    不是入口 / 回调 / 导出 / virtual / override；
    属于诊断时间 getter，不写项目结构；
    读取自我线程最小状态快照() 已承载当前 最近Tick时间值 输出。
    读取上次故障摘要 / 读取最近恢复摘要 / 读取最近故障摘要 / 读取最近运行摘要 有调用点，未删除。
```

第三轮处理：

```text
已删除上述 2 个函数的声明和定义。
已刷新 说明书/函数功能说明文档.md：
    源码文件：127
    函数条目：7216
```

第三轮中间构建修正：

```text
第一次构建失败：
    控制面板类.cpp:9380 调用 自我线程类::读取最近恢复摘要；
    鱼巢.cpp:704 调用 自我线程类::读取上次故障摘要；
    鱼巢.cpp:705 调用 自我线程类::读取最近恢复摘要。

修正：
    读取上次故障摘要 / 读取最近恢复摘要 属于有调用函数，不属于本轮“无调用直接删除”对象；
    已恢复其声明和原空实现，后续若要清理，应走 string / 显示摘要专项。
```

第三轮验证结果：

```text
修正后 msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m 通过：
    0 警告
    0 错误
```

## 5. 第四轮 live 复扫与删除

```text
已生成复扫表：实施记录/20260622_P11h_live_无调用候选重扫_after_round3.tsv。
第四轮可直接删除对象：
    自我类::重置时序步长为默认值_

判定：
    live 只有私有声明和定义；
    无调用点；
    不是入口 / 回调 / 导出 / virtual / override；
    只改私有运行态缓存，当前无入口使用，不承担当前项目结构闭环证据。
```

第四轮处理：

```text
已删除 自我类::重置时序步长为默认值_ 的声明和定义。
已刷新 说明书/函数功能说明文档.md：
    源码文件：127
    函数条目：7215
```

第四轮验证结果：

```text
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m 通过：
    0 警告
    0 错误
```

## 6. 当前复扫结论

```text
已生成复扫表：实施记录/20260622_P11h_live_无调用候选重扫_after_round4.tsv。

当前剩余 live call_like_count=1：12 项。
判定：
    私有_消费任务调度队列_按请求：任务调度队列消费专项，存在调度副作用；
    设置当前值：特征值写入 API；
    颜色_at：原始场景帧相邻读取接口；
    自我线程 / 安全风险树 / 需求创建 / 任务创建相关 9 项：命中项目结构创建、写入、落账或生命周期专项。

当前剩余 live call_like_count=2：111 项。
复核优先项：
    自我类::确保根任务_ 是项目结构创建 helper，不按普通无调用 helper 删除；
    枚举_词性_工厂::批量添加动态映射 / 清除动态映射 是公开映射维护接口，本轮按公开合同排除；
    其余前列候选主要是任务 / 状态 / 特征 / 动态 / 因果 / 场景 / 存在的声明+定义型结构读写 API。

当前结论：
    本轮未发现新的“私有、live 无调用、无入口/回调/注册/公开合同、且不承担项目结构读写证据”的可直接删除函数。
    P11h 本轮可执行无调用函数清理收束。
```
