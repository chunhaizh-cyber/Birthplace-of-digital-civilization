# 20260630 已确认存在体素融合分解 P8 Codex断点清单

## 0. 恢复摘要

当前切片：P8 控制面板和场景体素只读显示。

P7 已提交：

```text
fe20a7e feat: add internal child voxel submission
```

P8 目标：

```text
显示父体素基础颜色；
子体素颜色覆盖对应区域；
显示层只读，不写回事实。
```

## 1. 依据

```text
规范/已确认存在体素融合与分解规范20260630.md
规范/场景体素服务与视觉先验融合规范20260625.md
详细设计/已确认存在体素融合与分解实现方案20260630.md
docs/工程图谱/05_规则原子索引.md RULE-VISION-PRIOR-001、RULE-VOXEL-FUSION-007
```

## 2. 首个独立问题

问题锚点：

```text
P8 要求读取父基础颜色和子颜色。
当前 P6 颜色统计字段仍在 视觉观察融合模块.ixx 的运行期候选结果包中。
当前 P7 只给内部世界子存在写入局部体素模型和父子坐标映射，未写父基础颜色或子平均颜色事实。
```

当前判断：

```text
控制面板不能用默认颜色、显示文本或 CSS 颜色冒充父/子颜色事实。
P8 当前可安全先做只读父子体素可见性和颜色事实缺失状态；真实颜色覆盖闭合需要后续提交入口把颜色统计写成项目结构特征。
```

## 3. P8a 预测

```text
1. 场景体素只读快照可选包含已确认存在内部世界下的子体素模型。
2. 控制面板 JSON 能区分父体素项和内部世界子体素项。
3. 子体素项使用父存在场景原点 + 子存在局部原点生成显示原点。
4. 显示层只读，不写世界树事实、不生成动作动态、不触发体素加工。
5. 颜色字段只暴露事实读取状态，不用默认显示色冒充真实颜色。
```

## 4. 待验证

```text
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
git diff --check
git diff --cached --check
```

## 5. P8a 实施结果

已完成当前只读显示子切片：

```text
场景体素模块.ixx：
  结构_场景体素同步参数 增加 包含内部世界子体素 开关；
  控制面板传入局部坐标原点 X/Y/Z 特征入口时，快照可枚举父存在内部世界下的子存在体素；
  子体素显示原点 = 父存在场景原点 + 子存在局部原点；
  I64 特征读取使用同步函数内部局部 lambda，不新增 C++ 函数；
  默认同步路径不包含内部世界子体素，避免改变视觉先验业务默认范围。

控制面板类.ixx / 控制面板类.cpp：
  自我场景体素复现项增加 parentPtr、role、mappingState、colorFactState；
  JSON 能区分父体素和内部世界子体素；
  列表和 WebGL 使用显示角色提示色，但不输出或冒充真实父 / 子颜色值；
  稳定签名已混入新增字段；
  拆分 HTML 原始字符串块，避免 MSVC 单字符串过大。
```

颜色事实状态：

```text
父基础颜色事实、子平均颜色事实当前仍缺项目结构落点。
P8a 只显示颜色事实缺失状态，不宣称父颜色 / 子颜色覆盖闭合。
后续需要从提交入口把 P6 颜色统计转为项目结构特征后，再闭合 P8 颜色读取和覆盖显示。
```

## 6. 验证结果

```text
python .\tools\生成函数功能说明文档.py
  已运行通过；随后将新增 helper 改为局部 lambda，未保留函数说明文档差异。

git diff --check -- 场景体素模块.ixx 控制面板类.ixx 控制面板类.cpp
  通过。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
  日志：日志/msbuild_P8a_体素只读显示_20260630.log
  结果：已成功生成；0 个警告；0 个错误。
```

## 7. 下一步

```text
P8b：在提交入口或对应事实写入链中，把父基础颜色和子平均颜色作为项目结构特征落账；
随后控制面板只读读取真实颜色事实，渲染时按子体素覆盖父基础色显示。
```
