# 20260624 P13b D455绝对差复用全局工具 Codex断点清单

## 0. 恢复摘要

当前切片已闭合。已删除 `外设线程_D455深度相机.ixx::D455_I64绝对差` 本地重复 helper，并将本文件内 34 个调用点改为复用 `全局共享函数类.ixx::绝对差I64`。

依据：

```text
计划/计划索引.md：P13b 全局共享迁移。
计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md：B 类函数迁移前先查全局共享函数类和函数说明文档，已有同功能则复用，不重复新增。
说明书/函数功能说明文档.md：`D455_I64绝对差` 已清零；`绝对差I64` 保留为全局共享定义。
```

当前判断：

```text
`D455_I64绝对差(std::int64_t, std::int64_t)` 与 `绝对差I64(I64, I64)` 均为非饱和整数绝对差。
本切片只复用已有全局函数，不新增全局函数。
`D455_万分比(std::uint64_t, std::uint64_t)` 与现有 I64 万分比口径不同，本切片不处理。
```

验证证据：

```text
python .\tools\生成函数功能说明文档.py
结果：已生成 D:\鱼巢\说明书\函数功能说明文档.md；源码文件 127；函数条目 7178。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo
结果：已成功生成；0 个警告；0 个错误。
```

下一步：

```text
继续 P13b，从剩余 B 类低风险候选中选择下一个小函数族。
`D455_万分比(std::uint64_t, std::uint64_t)` 与现有 `计算比例万分比I64(I64, I64)` 入参和边界口径不同，不在本切片直接替换。
```

禁止宣称：不宣称 P13 完成，不宣称函数去重全部完成，不宣称自我苏醒完成或初步成熟完成。
