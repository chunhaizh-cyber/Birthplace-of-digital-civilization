# 20260620 特征类文本字段纠偏预审 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_特征类逻辑纠偏计划_v0.1.md` 的 1.1 P2.4 文本字段纠偏预审。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
规范/特征类规范20260429.md
规范/主信息字段与结构新增准入约束规范20260519.md
计划/计划索引.md
计划/20260620_特征类逻辑纠偏计划_v0.1.md
实施记录/20260620_特征类结构体必要性审查_Codex断点清单.md
```

本切片范围：

```text
1. 审查 结构_特征状态比较结果::说明。
2. 审查 结构_三维体素轮廓融合结果::失败原因。
3. 审查 结构_三维体素局部轮廓相似度结果::不可比较原因。
4. 只判断文本字段是否进入机器判断；不处理 1.2 非特征主信息直接操作。
```

验证前预测：

```text
1. 结构_特征状态比较结果::说明 只用于日志输出，机器判断使用 可比较 / 关系 / 差值。
2. 结构_三维体素轮廓融合结果::失败原因 只在失败路径拼接到不可比较说明，机器判断使用 成功。
3. 结构_三维体素局部轮廓相似度结果::不可比较原因 当前仅赋值，无下游机器分支读取。
4. 若预测成立，本切片不修改代码，只登记文本字段边界。
```

## 1. 审查结果

```text
rg -n "比较\.说明|结构_特征状态比较结果|比较\.可比较|比较\.关系|比较\.差值" 任务模块.管理工作线程.ixx 特征类.cpp
结果：
    特征类.cpp:4109 定义 特征类::比较状态 返回 结构_特征状态比较结果。
    任务模块.管理工作线程.ixx:1685-1690 日志同时输出 可比较 / 关系 / 差值 / 接受 / 说明。
    任务模块.管理工作线程.ixx:1882-1883、2038、2418、2457-2462、2494 机器判断使用 可比较 / 关系，不使用 说明。

rg -n "局部体素\.失败原因|局部体素\.成功|结果\.失败原因 =|结构_三维体素轮廓融合结果" 特征类.cpp 世界树类.cpp 世界树类.h
结果：
    特征类.cpp:3563、3567 定义并创建 结构_三维体素轮廓融合结果。
    特征类.cpp:3573 在拒绝路径写入 失败原因。
    世界树类.cpp:2068 机器判断使用 局部体素.成功。
    特征类.cpp:3866 失败原因只拼入不可比较说明。

rg -n "\.不可比较原因|不可比较原因|比较结果\.可比较|比较结果\.相似度Q10000|结构_三维体素局部轮廓相似度结果" 特征类.cpp 世界树类.cpp 世界树类.h
结果：
    特征类.cpp:3825、3832 定义并创建 结构_三维体素局部轮廓相似度结果。
    特征类.cpp:3837 写入 不可比较原因。
    世界树类.cpp:2098 机器判断使用 可比较 / 相似度Q10000。
    世界树类.cpp:2103 消费 相似度Q10000。
```

当前判断：

```text
1. 结构_特征状态比较结果::说明 当前只作为日志 / 诊断文本使用。
2. 结构_三维体素轮廓融合结果::失败原因 当前只作为失败诊断文本使用；机器分支使用 成功。
3. 结构_三维体素局部轮廓相似度结果::不可比较原因 当前只在特征类内赋值；世界树候选过滤使用 可比较 / 相似度Q10000。
4. 本切片不改代码，不运行构建。
5. 后续若任一文本字段进入候选、去重、自检、派生需求或机器判断，必须先改为枚举 / I64 / 节点引用或既有特征值结构。
```
