# 20260623 P10f 控制面板摘要重复文本字段删除 Codex 断点清单

## 0. 恢复摘要

当前主动计划：`计划/计划索引.md` 第 1 章 `P10f-c 后续：string / 摘要 / 原因类字段专项清理`。

当前切片：

```text
控制面板摘要线程中 `需求树摘要 / 任务树摘要 / 方法树摘要` 为重复显示文本。
这些字段由摘要线程按结构化计数拼接，随后控制面板再截断显示；对应计数字段已经存在。
本切片删除这三个重复文本字段，显示端改为直接使用结构化计数渲染。
```

依据：

```text
规范/禁止性规范20260611.md 第 2 章：系统内部逻辑不得用自由文本承载。
规范/线程生命周期状态上报与控制面板线程信息规范20260609.md：控制面板线程信息只服务展示和诊断，不反向定义事实。
docs/工程图谱/08_事实来源与写入权限图谱.md：控制面板为只读显示层。
计划/计划索引.md：P10f-c 继续清理摘要 / 原因类 string 字段；仅显示链需确认边界。
```

预测：

```text
删除三个重复摘要字段不影响机器判断。
控制面板和离线摘要仍可通过已有计数字段展示需求、任务、方法概况。
目标字段复扫中 `快照.需求树摘要 / 快照.任务树摘要 / 快照.方法树摘要 / 控制面板摘要_需求树 / 控制面板摘要_任务树 / 控制面板摘要_方法树` 应无源码命中。
Debug x64 构建应通过。
```

## 1. 本次修改

```text
已删除 `控制面板摘要线程模块.ixx::结构_控制面板摘要快照` 中：
    需求树摘要
    任务树摘要
    方法树摘要

已删除 `控制面板类.ixx::结构_控制面板快照` 中：
    控制面板摘要_需求树
    控制面板摘要_任务树
    控制面板摘要_方法树

`控制面板摘要线程模块.impl.cpp` 不再拼接这三个文本摘要。
`控制面板类.cpp` 的线程摘要详情和控制台摘要改读已有结构化计数。
`鱼巢.cpp` 的离线需求树生长摘要改读摘要线程快照中的计数字段。
```

## 2. 复扫与构建

目标字段复扫：

```powershell
rg -n "快照\.需求树摘要|快照\.任务树摘要|快照\.方法树摘要|控制面板摘要_需求树|控制面板摘要_任务树|控制面板摘要_方法树" -g "*.cpp" -g "*.h" -g "*.ixx" -g "!x64/**" -g "!Debug/**" -g "!日志/**"
```

结果：无命中。

构建：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo
```

结果：`exit_code=0`，日志：`日志/20260623_P10f_控制面板摘要重复文本字段删除_build.log`。

## 3. 当前边界

```text
本切片只删除控制面板摘要线程中已有结构化计数可替代的三个重复文本字段。
`自我线程摘要 / 运行事实摘要 / 自检摘要` 仍需后续分别判断是否属于显示证据、诊断日志或可由结构化状态替代。
未触碰外设报告证据卡、动作验证报告摘要、自检候选证据摘要和服务 / 风险安全聚合日志摘要。
```
