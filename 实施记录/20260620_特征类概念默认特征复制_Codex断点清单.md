# 20260620 特征类概念默认特征复制 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_特征类逻辑纠偏计划_v0.1.md` 的 1.3，补 `特征类::复制子特征到宿主`，用于承接存在类 1.3 的概念默认特征复制缺口。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_特征类逻辑纠偏计划_v0.1.md
计划/20260620_存在类逻辑纠偏计划_v0.1.md
实施记录/20260620_存在类非存在主信息操作审查_Codex断点清单.md
特征类.h
特征类.cpp
存在类.h
存在类.cpp
世界树类.cpp
```

本切片范围：

```text
1. 新增 特征类::复制子特征到宿主。
2. 世界树 根据概念扩充实例记忆 负责概念层级收集和实例概念绑定，并调用 特征类 复制子特征。
3. 存在类 不再直接解释 特征节点主信息类，不保留存在类特征 helper。
4. 不改变概念集结构，不新增主信息字段。
```

验证前预测：

```text
1. 特征类当前已有创建 / 获取 / 查找 / 读取特征值能力，但缺复制子特征到宿主函数。
2. 新函数可在 特征类 内读取 特征节点主信息类::类型 / 当前值，因为这是 owning class 职责。
3. 存在类内部特征 helper 删除后，外部仍通过 世界树 添加实例概念 / 创建存在实例 保持概念默认特征扩充行为。
4. 函数说明文档生成器和 Debug x64 构建应通过；若失败，以首个编译错误为准。
```

## 1. 实施记录

```text
1. 已在 特征类.h / 特征类.cpp 新增 std::size_t 复制子特征到宿主(基础信息节点类* 目标宿主, const 基础信息节点类* 来源宿主)。
2. 新函数只复制来源宿主的直接子特征类型和当前值；目标宿主已有同类型特征时不覆盖。
3. 已将 世界树类::根据概念扩充实例记忆 改为：
   世界树收集概念层级 -> 存在类绑定实例概念 -> 特征类复制概念子特征。
4. 已将 世界树类::创建存在实例 / 添加实例概念 接回世界树扩充路径。
5. 已删除 存在类 中 创建特征_按类型 / 获取子特征 / 查找子特征_按类型 / 根据概念扩充实例记忆。
```

## 2. 验证结果

```text
1. 结构复核：
   rg 输出已写入 日志/20260620_特征类概念默认特征复制_结构复核.txt。
   结果：存在类特征 helper 无命中；复制子特征到宿主 只在 特征类.h / 特征类.cpp / 世界树类.cpp 出现。

2. 函数说明文档：
   python .\tools\生成函数功能说明文档.py
   退出码：0
   日志：日志/20260620_特征类概念默认特征复制_函数文档.log
   关键输出：源码文件 128；函数条目 6882。

3. 构建：
   msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
   退出码：0
   日志：日志/20260620_特征类概念默认特征复制_msbuild.log
   关键输出：已成功生成。
```

## 3. 当前判断

```text
1. 本切片已闭合：概念默认特征复制函数归入 特征类，存在类不再直接解释 特征节点主信息类。
2. 已返回存在类 1.3，继续处理存在类内部剩余扫描命中。
3. 不宣称特征类逻辑纠偏完成。
```
