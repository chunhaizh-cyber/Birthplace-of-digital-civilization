# 20260620 场景状态排序读取回迁 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.3 P4.3，用 `状态类::读取状态排序时间` 回迁 `场景类::追加状态` 的状态时间排序读取。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
计划/20260620_状态类逻辑纠偏计划_v0.1.md
实施记录/20260620_状态排序时间读取函数_Codex断点清单.md
```

## 1. 问题锚点

```text
场景类::追加状态 当前在 std::stable_sort 中直接调用 基础信息_->取主信息<状态节点主信息类>，
并读取 状态节点主信息类::发生时间 / 收到时间 形成排序键。
```

当前判断：

```text
场景状态索引保留按状态排序时间排序，以维持现有状态索引顺序语义。
排序键读取改由 状态类::读取状态排序时间 承接；场景类只维护引用集合和排序调用，不解释状态主信息字段。
```

修改范围：

```text
场景类.cpp
说明书/函数功能说明文档.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
```

验证前预测：

```text
1. 场景类.cpp 不再包含 私有_取状态排序时间。
2. 场景类::追加状态 不再直接出现 取主信息<状态节点主信息类>。
3. 场景类.cpp 会调用 状态类::读取状态排序时间。
4. 删除私有函数后需重生成函数说明文档。
5. Debug x64 构建应通过。
```

## 2. 当前执行进展

已修改：

```text
1. 场景类.cpp 引入 状态类.h。
2. 删除 场景类.cpp 私有_取状态排序时间。
3. 场景类::追加状态 排序改为调用 状态类::读取状态排序时间。
```

已验证：

```text
rg "私有_取状态排序时间|取主信息<状态节点主信息类>|状态节点主信息类|读取状态排序时间|状态类.h" 场景类.cpp 场景类.h
结果：场景类.cpp 仅剩 状态类.h include 和 读取状态排序时间 调用；无 状态节点主信息类 直读命中。

python .\tools\生成函数功能说明文档.py
结果：exit=0，输出见 日志/20260620_场景状态排序读取回迁_函数文档.log。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：exit=0，构建日志见 日志/20260620_场景状态排序读取回迁_msbuild.log。
```

禁止宣称：

```text
不宣称 P4 非场景主信息直接操作审查完成。
不宣称场景类逻辑纠偏完成。
不宣称状态类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
