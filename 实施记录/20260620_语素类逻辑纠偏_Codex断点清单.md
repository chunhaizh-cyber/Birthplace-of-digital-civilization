# 语素类逻辑纠偏 Codex 断点清单

## 0. 恢复摘要

当前主动计划：`计划/20260620_语素类逻辑纠偏计划_v0.1.md`。

当前切片：P1 语素信息操作入口审计，已转入第一最小代码簇准备阶段。

已复核依据：

- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `计划/计划索引.md`
- `计划/20260620_语素类逻辑纠偏计划_v0.1.md`
- `日志/20260621_语素信息操作入口扫描.txt`
- `实施记录/20260620_语素信息操作入口判定表.md`

当前判断：

- `语素类.h::语素入口同一` 已存在，语义覆盖多个模块重复实现的入口同一 / 主键相等 helper。
- 第一最小代码簇 `任务类.cpp` 已完成：删除局部 `私有_语素入口相同` 并改用 `语素入口同一`。
- 控制面板主读源应为 SQL 数据库投影；不再把 live 指针快照作为控制面板主读源前提。

下一步：

1. 提交本闭合切片。
2. 继续选择 `方法类.cpp` 或 `二次特征类.cpp` 的重复入口主键相等 helper 小簇。
3. 替换为 `语素入口同一` 后重新生成函数说明文档并构建。

禁止宣称：

- 不宣称语素类逻辑纠偏全部完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。

## 1. 已运行命令

```powershell
Select-String -LiteralPath '日志\20260621_语素信息操作入口扫描.txt' -Pattern ...
rg -n "语素入口同一|入口同一|入口主键相同|入口主键相等|读取入口主键|安全获取词" 语素类.h 语素类.cpp 语素主信息类.h
rg -n "私有_语素入口主键相等|语素入口主键相等|语素入口主键相同|私有_语素入口同一|私有_语素入口相同|语素入口相同" -g "*.cpp" -g "*.h" -g "*.ixx"
rg -n "语素集\.获取词节点指针|语素集\.查找词节点|语素集\.查找信息入口节点|语素集\.根指针" -g "*.cpp" -g "*.h" -g "*.ixx"
rg -n "私有_语素入口相同|语素入口同一" 任务类.cpp
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

## 2. 当前预测

`任务类.cpp` 替换后：

- `rg -n "私有_语素入口相同" 任务类.cpp` 无命中。
- `任务类.cpp` 中原 3 处调用改为 `语素入口同一`。
- 函数功能说明文档已重新生成：源码文件 128，函数条目 7097。
- Debug x64 构建通过，退出码 0；日志：`日志/20260621_P1_任务类语素入口同一回迁_build.log`。

## 3. P2.1 方法类子切片

当前目标：

- 删除 `方法类.cpp::私有_语素入口主键相等`。
- 将 6 处调用改为 `语素入口同一`。

已复核：

- `方法类.cpp` 已包含 `语素类.h`。
- 本地 helper 与 `语素入口同一` 都是先比较指针，再比较非空主键。

预测：

- `rg -n "私有_语素入口主键相等" 方法类.cpp` 无命中。
- `python .\tools\生成函数功能说明文档.py` 已成功；源码文件 128，函数条目 7096。
- Debug x64 构建通过，退出码 0；日志：`日志/20260621_P2_方法类语素入口同一回迁_build.log`。

后续：

- 继续处理 `二次特征类.cpp` 或 `二次特征应用模块.ixx` 的重复入口主键相等 helper。

## 4. P2.1 二次特征类子切片

当前目标：

- 删除 `二次特征类.cpp::私有_语素入口主键相等`。
- 将 5 处调用改为 `语素入口同一`。

已复核：

- `二次特征类.cpp` 已包含 `语素类.h`。
- 本地 helper 与 `语素入口同一` 都是先比较指针，再比较非空主键。

预测：

- `rg -n "私有_语素入口主键相等" 二次特征类.cpp` 无命中。
- `python .\tools\生成函数功能说明文档.py` 已成功；源码文件 128，函数条目 7095。
- Debug x64 构建通过，退出码 0；日志：`日志/20260621_P3_二次特征类语素入口同一回迁_build.log`。

后续：

- 继续处理 `二次特征应用模块.ixx`，该文件内存在两个重复 helper，需拆成更小簇或一次性替换后构建确认。

## 5. P2.1 二次特征应用模块子切片

当前目标：

- 删除 `二次特征匹配模块_detail::语素入口主键相等`。
- 删除 `二次特征生成模块_detail::私有_语素入口主键相等`。
- 将本文件内 10 处调用改为 `语素入口同一`。

已复核：

- `二次特征应用模块.ixx` 已包含 `语素类.h`。
- 两个本地 helper 与 `语素入口同一` 都是先比较指针，再比较非空主键。

预测：

- `rg -n "语素入口主键相等|私有_语素入口主键相等" 二次特征应用模块.ixx` 无命中。
- `python .\tools\生成函数功能说明文档.py` 已成功；源码文件 128，函数条目 7093。
- Debug x64 构建通过，退出码 0；日志：`日志/20260621_P4_二次特征应用语素入口同一回迁_build.log`。

后续：

- 继续处理剩余重复 helper，优先选择调用点较少的 `自检线程模块.impl.cpp`、`任务模块.执行.ixx` 或 `任务模块.管理界面线程.impl.cpp`，调用点多的 `任务模块.管理工作线程.ixx` 和自我动作模块后置。

## 6. P2.1 任务管理界面线程子切片

当前目标：

- 删除 `任务模块.管理界面线程.impl.cpp::私有_语素入口相同_任务管理`。
- `私有_目标特征类型等于任务特征_任务管理` 改为显式非空后调用 `语素入口同一`。

已复核：

- 文件已包含 `语素类.h`。
- 本地 helper 对双空指针返回 false；替换时保留两侧非空守卫。

预测：

- `rg -n "私有_语素入口相同_任务管理" 任务模块.管理界面线程.impl.cpp` 无命中。
- `python .\tools\生成函数功能说明文档.py` 已成功；源码文件 128，函数条目 7093。
- Debug x64 构建通过，退出码 0；日志：`日志/20260621_P5_任务管理界面语素入口同一回迁_build.log`。

后续：

- 继续处理 `自检线程模块.impl.cpp` 或 `任务模块.执行.ixx`。

## 7. P2.1 自检线程子切片

当前目标：

- 删除 `自检线程模块.impl.cpp::私有_语素入口主键相等_自检`。
- 4 处调用改为非空守卫后调用 `语素入口同一`。

已复核：

- 文件已包含 `语素类.h`。
- 本地 helper 对双空指针返回 false；替换时保留非空守卫。

预测：

- `rg -n "私有_语素入口主键相等_自检" 自检线程模块.impl.cpp` 无命中。
- `python .\tools\生成函数功能说明文档.py` 已成功；源码文件 128，函数条目 7092。
- Debug x64 构建通过，退出码 0；日志：`日志/20260621_P6_自检线程语素入口同一回迁_build.log`。

后续：

- 继续处理 `任务模块.执行.ixx` 的重复入口同一 helper。

## 8. P2.1 任务执行模块子切片

当前目标：

- 删除 `任务模块.执行.ixx::私有_语素入口同一_执行桥_最小_v0`。
- 11 处调用改为 `语素入口同一`。

已复核：

- 文件已包含 `语素类.h`。
- 本地 helper 与 `语素入口同一` 空指针语义一致：先比较指针，双空也返回 true。

预测：

- `rg -n "私有_语素入口同一_执行桥_最小_v0" 任务模块.执行.ixx` 无命中。
- `python .\tools\生成函数功能说明文档.py` 已成功；源码文件 128，函数条目 7091。
- Debug x64 构建通过，退出码 0；日志：`日志/20260621_P7_任务执行语素入口同一回迁_build.log`。

后续：

- 继续选择剩余调用点少的小簇，优先 `自我动作实现.扫描.ixx` 或 `自我类.cpp`。

## 9. P2.1 扫描动作模块子切片

当前目标：

- 删除 `自我动作实现.扫描.ixx::语素入口主键相同`。
- 唯一调用改为 `语素入口同一`。

已复核：

- 文件已包含 `语素类.h`。
- 调用点在进入比较前已要求 `目标特征类型` 非空。

预测：

- `rg -n "语素入口主键相同" 自我动作实现.扫描.ixx` 无命中。
- `python .\tools\生成函数功能说明文档.py` 已成功；源码文件 128，函数条目 7090。
- Debug x64 构建通过，退出码 0；日志：`日志/20260621_P8_扫描动作语素入口同一回迁_build.log`。

后续：

- 继续处理 `自我类.cpp` 或 `需求类.cpp`。

## 10. P2.1 需求类子切片

当前目标：

- 删除 `需求类.cpp::私有_语素入口主键相等`。
- 将 13 处调用改为 `语素入口同一`。
- 因本地 helper 对双空指针返回 false，而 `语素入口同一(nullptr, nullptr)` 返回 true，替换时在可能接收空目标的入口保留显式非空守卫。

已复核：

- `需求类.cpp` 已包含 `语素类.h`。
- 本地 helper 语义为：非空指针同一，或两侧非空且左主键非空并相等。
- `语素入口同一` 语义为：指针同一，或两侧非空且左主键非空并相等；差异仅在双空指针返回 true。

预测：

- `rg -n "私有_语素入口主键相等" 需求类.cpp` 无命中。
- `python .\tools\生成函数功能说明文档.py` 成功，函数条目减少 1。
- Debug x64 构建通过，退出码 0；构建日志写入 `日志/20260621_P9_需求类语素入口同一回迁_build.log`。

执行结果：

- `rg -n "私有_语素入口主键相等|语素入口主键相等" 需求类.cpp` 无命中。
- `python .\tools\生成函数功能说明文档.py` 成功；源码文件 128，函数条目 7089。
- Debug x64 构建通过，退出码 0；`日志/20260621_P9_需求类语素入口同一回迁_build.log` 显示 0 个警告、0 个错误。

后续：

- 继续处理 `自我线程模块.impl.cpp::私有_语素入口主键相等_线程侧`；该 helper 与需求类本地 helper 一样对双空返回 false，替换时保留非空语义。

## 11. P2.1 自我线程模块子切片

当前目标：

- 删除 `自我线程模块.impl.cpp::私有_语素入口主键相等_线程侧`。
- 删除同文件 `私有_语素入口同一_线程侧`。
- 将 7 处调用改为 `语素入口同一`；对原 helper 依赖“双空返回 false”的调用保留显式非空守卫。

已复核：

- `自我线程模块.impl.cpp` 已包含 `语素类.h`。
- 两个本地 helper 都是非空后比较指针或非空主键；与 `语素入口同一` 的差异仍是双空指针返回值。

预测：

- `rg -n "私有_语素入口主键相等_线程侧|私有_语素入口同一_线程侧" 自我线程模块.impl.cpp` 无命中。
- `python .\tools\生成函数功能说明文档.py` 成功，函数条目减少 2。
- Debug x64 构建通过，退出码 0；构建日志写入 `日志/20260621_P10_自我线程语素入口同一回迁_build.log`。

执行结果：

- `rg -n "私有_语素入口主键相等_线程侧|私有_语素入口同一_线程侧" 自我线程模块.impl.cpp` 无命中。
- `python .\tools\生成函数功能说明文档.py` 成功；源码文件 128，函数条目 7087。
- Debug x64 构建通过，退出码 0；`日志/20260621_P10_自我线程语素入口同一回迁_build.log` 显示 0 个警告、0 个错误。

后续：

- 继续处理 `自我动作实现.外设模块.ixx::语素入口主键相同`；该文件调用点少于 `任务模块.管理工作线程.ixx` 和 `自我动作实现模块.ixx`。

## 12. P2.1 自我动作实现外设模块子切片

当前目标：

- 删除 `自我动作实现.外设模块.ixx::语素入口主键相同`。
- 将 8 处调用改为 `语素入口同一`。

已复核：

- `自我动作实现.外设模块.ixx` 已包含 `语素类.h`。
- 本地 helper 与 `语素入口同一` 空指针语义一致：指针相同直接返回 true，双空也返回 true；指针不同再比较非空主键。

预测：

- `rg -n "语素入口主键相同" 自我动作实现.外设模块.ixx` 无命中。
- `python .\tools\生成函数功能说明文档.py` 成功，函数条目减少 1。
- Debug x64 构建通过，退出码 0；构建日志写入 `日志/20260621_P11_外设模块语素入口同一回迁_build.log`。

执行结果：

- `rg -n "语素入口主键相同" 自我动作实现.外设模块.ixx` 无命中。
- `python .\tools\生成函数功能说明文档.py` 成功；源码文件 128，函数条目 7086。
- Debug x64 构建通过，退出码 0；`日志/20260621_P11_外设模块语素入口同一回迁_build.log` 显示 0 个警告、0 个错误。

后续：

- 继续处理 `自我动作实现模块.ixx::语素入口主键相同`。

## 13. P2.1 自我动作实现模块子切片

当前目标：

- 删除 `自我动作实现模块.ixx::语素入口主键相同`。
- 将 13 处调用改为 `语素入口同一`。

已复核：

- `自我动作实现模块.ixx` 已包含 `语素类.h`。
- 本地 helper 与 `语素入口同一` 空指针语义一致：指针相同直接返回 true，双空也返回 true；指针不同再比较非空主键。

预测：

- `rg -n "语素入口主键相同" 自我动作实现模块.ixx` 无命中。
- `python .\tools\生成函数功能说明文档.py` 成功，函数条目减少 1。
- Debug x64 构建通过，退出码 0；构建日志写入 `日志/20260621_P12_自我动作模块语素入口同一回迁_build.log`。

执行结果：

- `rg -n "语素入口主键相同" 自我动作实现模块.ixx` 无命中。
- `python .\tools\生成函数功能说明文档.py` 成功；源码文件 128，函数条目 7085。
- Debug x64 构建通过，退出码 0；`日志/20260621_P12_自我动作模块语素入口同一回迁_build.log` 显示 0 个警告、0 个错误。

后续：

- 继续处理 `任务模块.管理工作线程.ixx::私有_语素入口同一_工作线程`；`自我类.cpp::私有_语素入口相同` 仍保持局部堵点登记，不直接替换。

## 14. P2.1 任务管理工作线程子切片

当前目标：

- 删除 `任务模块.管理工作线程.ixx::私有_语素入口同一_工作线程`。
- 将调用改为 `语素入口同一`，并保留原 helper 对任一空指针返回 false 的语义。

已复核：

- `任务模块.管理工作线程.ixx` 已包含 `语素类.h`。
- 本地 helper 先判空再比较指针，因此 `nullptr, nullptr` 返回 false；直接替换为 `语素入口同一` 会改变双空语义。

预测：

- `rg -n "私有_语素入口同一_工作线程" 任务模块.管理工作线程.ixx` 无命中。
- `python .\tools\生成函数功能说明文档.py` 成功，函数条目减少 1。
- Debug x64 构建通过，退出码 0；构建日志写入 `日志/20260621_P13_任务管理工作线程语素入口同一回迁_build.log`。

执行结果：

- `rg -n "私有_语素入口同一_工作线程" 任务模块.管理工作线程.ixx` 无命中。
- `python .\tools\生成函数功能说明文档.py` 成功；源码文件 128，函数条目 7084。
- Debug x64 构建通过，退出码 0；`日志/20260621_P13_任务管理工作线程语素入口同一回迁_build.log` 显示 0 个警告、0 个错误。

后续：

- P2.1 机器逻辑重复同一性 helper 除 `自我类.cpp::私有_语素入口相同` 局部堵点外已清零；`自我动作实现.外设模块.ixx::语素入口日志文本` 属于日志显示侧，转入 P2.6/P8 后续显示诊断函数收口。
- 下一步进入 P2.2/P4：外部全局信息入口定义 helper 收口。

## 15. P2.2/P4 信息入口定义 helper 收口第一切片

当前目标：

- 在 `语素类` 增加信息入口定义只读访问器。
- 将 `基础信息类.cpp` 和 `控制面板类.cpp` 的外部 `语素_取信息入口...` / `语素_信息入口...` 调用改为 `语素类` 访问器。

项目结构映射：

| 业务概念 | 项目承载结构 | 值类型 | 写入方 | 读取方 | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| 信息入口定义 | `语素主信息类.h::语素_信息入口定义表`，对外由 `语素类` 只读访问器读取 | `结构_信息入口定义`、`枚举_信息入口类型`、`枚举_主信息类型`、`枚举_信息存储树`、`枚举_词性` | 语素主信息定义表 | `基础信息类`、控制面板诊断文本经 `语素类` | 扫描外部 `语素_取信息入口...` / `语素_信息入口...` 调用；构建 |

已复核：

- 外部直接调用当前集中在 `基础信息类.cpp` 和 `控制面板类.cpp`。
- `语素类` 已有 `推断人类词性默认基础信息类型`，但缺信息入口定义读取函数。

预测：

- `基础信息类.cpp`、`控制面板类.cpp` 不再直接调用 `语素_取信息入口...` / `语素_信息入口...`。
- `python .\tools\生成函数功能说明文档.py` 成功，函数条目按新增 `语素类` 访问器增加。
- Debug x64 构建通过，退出码 0；构建日志写入 `日志/20260621_P14_信息入口定义访问器收口_build.log`。

执行结果：

- `基础信息类.cpp`、`控制面板类.cpp` 已改为调用 `语素类::读取信息入口...` / `语素类::信息入口...`。
- `rg -n "语素_取信息入口|语素_信息入口|语素_选择默认信息入口类型|语素_推断人类词性" 基础信息类.cpp 控制面板类.cpp` 无命中。
- `python .\tools\生成函数功能说明文档.py` 成功；源码文件 128，函数条目 7098。
- Debug x64 构建通过，退出码 0；`日志/20260621_P14_信息入口定义访问器收口_build.log` 显示 0 个警告、0 个错误。

后续：

- 进入 P2.3：扫描并回迁外部 `语素入口主信息类` 字段直读。

## 16. P2.3 语素入口主信息字段直读收口第一切片

当前目标：

- 在 `语素类` 补充语素入口主信息只读访问器：是否语素入口节点、人类词性、显式对应基础信息类型。
- 将 `基础信息类.cpp` 和 `控制面板类.cpp` 的外部 `dynamic_cast<语素入口主信息类>` 与字段直读改为 `语素类` 只读访问器。

项目结构映射：

| 业务概念 | 项目承载结构 | 值类型 | 写入方 | 读取方 | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| 语素入口人类词性 / 信息入口类型 / 显式基础信息类型 | `语素入口节点类` + `语素入口主信息类`，对外由 `语素类` 访问器读取 | `枚举_词性`、`枚举_信息入口类型`、`枚举_主信息类型` | 语素入口创建与绑定流程 | `基础信息类`、控制面板诊断文本经 `语素类` | 扫描外部 `dynamic_cast<语素入口主信息类>` 和字段直读；构建 |

已复核：

- 外部字段直读当前集中在 `基础信息类.cpp` 和 `控制面板类.cpp`。
- `语素类` 已有 `获取语素入口信息入口类型` 和推断型 `获取语素入口对应基础信息类型`；控制面板当前需要保留“显式类型优先，否则按信息入口定义显示”的诊断语义，因此新增显式类型读取入口。

预测：

- `基础信息类.cpp`、`控制面板类.cpp` 不再直接出现 `dynamic_cast<const 语素入口主信息类*>` 或读取 `词性 / 信息入口类型 / 对应基础信息类型` 字段。
- `python .\tools\生成函数功能说明文档.py` 成功，函数条目按新增 `语素类` 访问器增加。
- Debug x64 构建通过，退出码 0；构建日志写入 `日志/20260621_P15_语素入口主信息字段读取收口_build.log`。

执行结果：

- `基础信息类.cpp` 改为通过 `语素集.获取语素入口信息入口类型` 读取信息入口类型，并移除直接包含 `语素主信息类.h`。
- `控制面板类.cpp` 改为通过 `语素集.是语素入口节点`、`获取语素入口人类词性`、`获取语素入口信息入口类型`、`获取语素入口显式对应基础信息类型` 生成诊断文本。
- `rg -n "语素入口主信息类|->词性|->信息入口类型|->对应基础信息类型|->对应基础信息|\.词性|\.信息入口类型|\.对应基础信息类型|\.对应基础信息" 基础信息类.cpp 控制面板类.cpp` 无命中。
- `python .\tools\生成函数功能说明文档.py` 成功；源码文件 128，函数条目 7103。
- Debug x64 构建通过，退出码 0；`日志/20260621_P15_语素入口主信息字段读取收口_build.log` 显示 0 个警告、0 个错误。

后续：

- 进入 P2.4：两段式词节点 + 信息入口节点查找回迁。

## 17. P2.4 两段式词入口查找收口第一切片

当前目标：

- 在 `语素类` 补充按词值直接只读查找信息入口节点 / 人类词性入口节点的函数。
- 将 `因果类.cpp`、`自我线程模块.impl.cpp`、`控制面板类.cpp` 的 `查找词节点 / 获取词节点指针 + 查找信息入口节点 / 查找人类词性入口节点` 两段式组合改为单一 `语素类` 查找入口。

项目结构映射：

| 业务概念 | 项目承载结构 | 值类型 | 写入方 | 读取方 | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| 词值到语素入口的只读查找 | `词节点类` + `语素入口节点类` 子链，对外由 `语素类` 查找函数读取 | `std::string` 入口种子、`枚举_信息入口类型`、`枚举_词性`、`语素入口节点类*` | 语素入口创建流程 | 因果类、自我线程、控制面板经 `语素类` | 扫描外部两段式组合；构建 |

已复核：

- 现有 `语素类::查找信息入口节点(const 词节点类*, 枚举_信息入口类型)` 已有，但缺 `const std::string&` 重载。
- 控制面板还存在 `查找词节点 + 查找人类词性入口节点` 两段式显示辅助，可同切片回迁到 `语素类` 只读查找入口。

预测：

- 外部不再出现 `查找词节点 / 获取词节点指针` 紧接 `查找信息入口节点 / 查找人类词性入口节点` 的两段式组合。
- `python .\tools\生成函数功能说明文档.py` 成功，函数条目按新增 `语素类` 查找重载增加。
- Debug x64 构建通过，退出码 0；构建日志写入 `日志/20260621_P16_两段式词入口查找收口_build.log`。

执行结果：

- `语素类` 新增按词值只读查找信息入口节点 / 人类词性入口节点的重载，不隐式创建词或入口。
- `因果类.cpp`、`自我线程模块.impl.cpp`、`控制面板类.cpp` 已改为单一 `语素类` 查找入口。
- `rg -n "查找词节点|获取词节点指针" 因果类.cpp 自我线程模块.impl.cpp 控制面板类.cpp` 无命中。
- `python .\tools\生成函数功能说明文档.py` 成功；源码文件 128，函数条目 7109。
- Debug x64 构建通过，退出码 0；`日志/20260621_P16_两段式词入口查找收口_build.log` 显示 0 个警告、0 个错误。

后续：

- 进入 P2.5：语素树根和节点链直接访问清零。

## 18. P2.5 语素树根与归属读取收口第一切片

当前目标：

- 在 `语素类` 补充语素树根只读读取、节点归属判断、按主键查找语素入口节点的只读函数。
- 将 `控制面板类.cpp`、`自检线程模块.impl.cpp`、`自我线程模块.impl.cpp` 的外部 `语素集.根指针` 直接读取改为 `语素类` 函数。

项目结构映射：

| 业务概念 | 项目承载结构 | 值类型 | 写入方 | 读取方 | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| 语素树根 / 语素节点归属 / 入口主键回查 | `语素类` 内部链表根与 `语素节点类` 全链 | `语素节点类*`、`语素入口节点类*`、主键字符串 | 语素类创建与链表维护 | 控制面板、自检线程、自我线程经 `语素类` | 扫描外部 `语素集.根指针`；构建 |

已复核：

- 外部 `语素集.根指针` 直接读取当前 4 处：控制面板树根、自检安全词、自我线程安全词、自我线程按指针/主键解析入口。
- 自检 / 自我线程路径需要的是“节点是否属于语素树”和“按主键查找入口”，不应在调用方扫描语素链。

预测：

- `rg -n "语素集\.根指针" -g "*.h" -g "*.cpp" -g "*.ixx"` 除 `语素类` 内部外无外部命中。
- `python .\tools\生成函数功能说明文档.py` 成功，函数条目按新增 `语素类` 只读函数增加。
- Debug x64 构建通过，退出码 0；构建日志写入 `日志/20260621_P17_语素树根归属读取收口_build.log`。

执行结果：

- `语素类` 新增 `获取语素树根节点`、`节点属于语素树`、`按主键查找语素入口节点`。
- `控制面板类.cpp`、`自检线程模块.impl.cpp`、`自我线程模块.impl.cpp` 已移除外部 `语素集.根指针` 直接读取。
- `rg -n "语素集\.根指针" -g "*.h" -g "*.cpp" -g "*.ixx"` 无命中。
- `python .\tools\生成函数功能说明文档.py` 成功；源码文件 128，函数条目 7112。
- Debug x64 构建通过，退出码 0；`日志/20260621_P17_语素树根归属读取收口_build.log` 显示 0 个警告、0 个错误。

后续：

- 进入 P2.6/P8：显示层词面和日志诊断读取收口。

## 19. P2.6/P8 外设模块语素日志文本收口

当前目标：

- 在 `语素类` 增加成员函数 `安全获取词`。
- 将 `自我动作实现.外设模块.ixx::语素入口日志文本` 删除，并改为调用 `语素集.安全获取词`。

已复核：

- `自我动作实现.外设模块.ixx::语素入口日志文本` 只用于日志显示，不参与机器判断。
- 旧全局 `语素_安全获取词` 暂时转调 `语素类` 成员函数，待 P2.6 后续批量清理其它显示层调用。

预测：

- `rg -n "语素入口日志文本" 自我动作实现.外设模块.ixx` 无命中。
- `python .\tools\生成函数功能说明文档.py` 成功，函数条目净变化为新增 `语素类::安全获取词`、删除局部日志 helper。
- Debug x64 构建通过，退出码 0；构建日志写入 `日志/20260621_P18_外设模块语素日志文本收口_build.log`。

执行结果：

- `语素类` 新增成员函数 `安全获取词`，旧全局 `语素_安全获取词` 转调该成员函数。
- `自我动作实现.外设模块.ixx::语素入口日志文本` 已删除，5 处日志调用改为 `语素集.安全获取词`。
- `rg -n "语素入口日志文本" 自我动作实现.外设模块.ixx` 无命中。
- `python .\tools\生成函数功能说明文档.py` 成功；源码文件 128，函数条目 7112。
- Debug x64 构建通过，退出码 0；`日志/20260621_P18_外设模块语素日志文本收口_build.log` 显示 0 个警告、0 个错误。

后续：

- 继续 P2.6/P8：逐步将其它显示/日志层 `语素_安全获取词` 或直接 `语素集.获取词` 调用改为 `语素类` 成员入口。

## 20. P2.6/P8 全局安全词面 helper 调用清零

当前目标：

- 将外部 `语素_安全获取词(...)` 调用替换为 `语素集.安全获取词(...)`。
- 删除 `语素类.h` 中旧全局 `语素_安全获取词` 转调函数。

已复核：

- `语素_安全获取词` 已在上一切片改为转调 `语素集.安全获取词`，本切片替换调用点不改变行为。
- 直接 `语素集.获取词` 可能涉及异常语义，本切片暂不混入。

预测：

- `rg -n "语素_安全获取词" -g "*.h" -g "*.cpp" -g "*.ixx"` 无命中。
- `python .\tools\生成函数功能说明文档.py` 成功，函数条目减少旧全局 helper 条目。
- Debug x64 构建通过，退出码 0；构建日志写入 `日志/20260621_P19_全局安全词面helper调用清零_build.log`。

执行结果：

- `二次特征应用模块.ixx`、`任务类.cpp`、`控制面板类.cpp`、`特征类.cpp`、`自我线程模块.impl.cpp`、`自检线程模块.impl.cpp`、`需求类.cpp`、`语言类_二次特征表达.cpp`、`语言类_基础信息命名.cpp` 的外部 `语素_安全获取词(...)` 已改为 `语素集.安全获取词(...)`。
- `语素类.h` 中旧全局 `语素_安全获取词` 转调函数已删除。
- `rg -n "语素_安全获取词" -g "*.h" -g "*.cpp" -g "*.ixx"` 无命中。
- `python .\tools\生成函数功能说明文档.py` 成功；源码文件 128，函数条目 7111。
- Debug x64 构建通过，退出码 0；`日志/20260621_P19_全局安全词面helper调用清零_build.log` 显示 0 个警告、0 个错误。

后续：

- 继续 P2.6/P8：对剩余直接 `语素集.获取词(` 调用建立判定表；只替换显示/日志层安全读取，保留需要异常语义的直接读取路径。

## 21. P2.6/P8 直接获取词调用安全读取收口

当前目标：

- 对剩余直接 `语素集.获取词(` 调用建立最小判定表。
- 将不依赖异常语义的显示、日志、SQL 投影和名称种子读取改为 `语素集.安全获取词`。

判定表：

| 文件 | 调用点 | 用途 | 处理 |
| --- | --- | --- | --- |
| `世界树类.cpp` | `私有_世界树SQL入口文本` | SQL 投影显示词面；异常时原本退回主键 | 改为安全读取，空值退回主键 |
| `任务类.cpp` | `私有_任务SQL入口文本` | SQL 投影显示词面；异常时原本退回主键 | 改为安全读取，空值退回主键 |
| `方法类.cpp` | `私有_方法SQL入口文本` | SQL 投影显示词面；异常时原本退回主键 | 改为安全读取，空值退回主键 |
| `方法虚拟存在服务类.cpp` | `私有_动作名转方法概念词` | 从动作名入口取名称种子并注册方法概念入口；异常时原本返回空指针 | 改为安全读取，保留空值返回空指针 |
| `自我动作实现模块.ixx` | `语素日志文本` | 日志显示词面；空词面退回主键 | 改为安全读取，空值退回主键 |
| `鱼巢.cpp` | `私有_语素入口显示文本_控制命令` | 控制命令显示词面；异常时原本退回主键 | 改为安全读取，空值退回主键 |

预测：

- `rg -n "语素集\.获取词\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 无命中。
- Debug x64 构建通过，退出码 0；构建日志写入 `日志/20260621_P20_直接获取词安全读取收口_build.log`。

执行结果：

- `世界树类.cpp`、`任务类.cpp`、`方法类.cpp` 的 SQL 投影入口文本读取已改为 `语素集.安全获取词`，空词面继续退回入口主键。
- `方法虚拟存在服务类.cpp::私有_动作名转方法概念词` 已改为安全读取动作名词面，空词面继续返回空指针。
- `自我动作实现模块.ixx::语素日志文本`、`鱼巢.cpp::私有_语素入口显示文本_控制命令` 已改为安全读取，空词面继续退回主键显示。
- `rg -n "语素集\.获取词\(" -g "*.h" -g "*.cpp" -g "*.ixx"` 无命中。
- `python .\tools\生成函数功能说明文档.py` 成功；源码文件 128，函数条目 7111。
- Debug x64 构建通过，退出码 0；`日志/20260621_P20_直接获取词安全读取收口_build.log` 显示 0 个警告、0 个错误。

后续：

- P2.6/P8 当前直接词面读取收口完成；下一步进入 P3 语素主信息结构必要性审查。

## 22. P3 语素主信息结构必要性审查

当前目标：

- 扫描 `结构_信息入口定义`、`结构_人类词性到信息入口候选`、`语素入口主信息类`、`短语主信息类`、`语素语言记录主信息类`、`词主信息类` 的公开使用点。
- 建立最小判定表：保留 / 私有化 / 删除 / 转其它计划。

预测：

- 语素主信息结构定义本身大概率仍在 `语素主信息类.h` 内部必要存在。
- 外部调用若只通过 `语素类` 访问器读取，不作为 P3 立即删除对象。
- 若发现外部直接读写桥接字段或主信息字段，先登记为独立小切片，不在 P3 审查阶段直接大改。

第一发现：

- `语素主信息类.h:469` 存在 `using 入口主信息类 = 语素入口主信息类;` 兼容旧名。
- `rg -n "using 入口主信息类|入口主信息类\s*=" -g "*.h" -g "*.cpp" -g "*.ixx"` 仅命中该定义。
- 当前判断：旧名无人使用，且禁止性规范不允许继续保留兼容旧入口；可作为 P3 第一小清理删除。

P3 第一小清理预测：

- `rg -n "using 入口主信息类|入口主信息类\s*=" -g "*.h" -g "*.cpp" -g "*.ixx"` 无命中。
- Debug x64 构建通过，退出码 0；构建日志写入 `日志/20260621_P21_语素主信息兼容旧名删除_build.log`。

执行结果：

- 已删除 `语素主信息类.h` 中无人使用的 `using 入口主信息类 = 语素入口主信息类;` 兼容旧名。
- `rg -n "using 入口主信息类|入口主信息类\s*=" -g "*.h" -g "*.cpp" -g "*.ixx"` 无命中。
- Debug x64 构建通过，退出码 0；`日志/20260621_P21_语素主信息兼容旧名删除_build.log` 显示 0 个警告、0 个错误。

后续：

- 继续 P3 审查剩余语素主信息结构：短语 / 语言记录主信息当前仅定义和内部类型识别命中，先完成必要性判定表，再决定是否生成删除或私有化切片。

第二发现：

- `短语主信息类`、`短语子节点主信息类`、`语素语言记录主信息类` 没有创建或写入入口；`rg -n "new\s+(短语主信息类|短语子节点主信息类|语素语言记录主信息类)"` 无命中。
- `短语主信息类` 和 `短语子节点主信息类` 只被 `语素类.cpp` 的诊断快照 `dynamic_cast` 识别；`语素语言记录主信息类` 只剩定义内部比较。
- 当前判断：这些是公开但未接入运行创建链的语素层结构壳；基础信息层 `语言记录主信息类` 仍是正式语言记录树结构，本切片不触碰。

P3 第二小清理预测：

- 删除 `语素主信息类.h` 中三个未接入的语素层主信息结构壳。
- 删除 `语素类.cpp` 中对应短语 / 短语子诊断分支。
- `rg -n "短语主信息类|短语子节点主信息类|语素语言记录主信息类" -g "*.h" -g "*.cpp" -g "*.ixx"` 无命中。
- Debug x64 构建通过，退出码 0；构建日志写入 `日志/20260621_P22_语素主信息未接入结构壳删除_build.log`。

执行结果：

- 已删除 `语素主信息类.h` 中未接入创建链的 `短语主信息类`、`短语子节点主信息类`、`语素语言记录主信息类`。
- 已删除 `语素类.cpp` 诊断快照中对应短语 / 短语子识别分支。
- `rg -n "短语主信息类|短语子节点主信息类|语素语言记录主信息类" -g "*.h" -g "*.cpp" -g "*.ixx"` 无命中。
- `python .\tools\生成函数功能说明文档.py` 成功；源码文件 128，函数条目 7106。
- Debug x64 构建通过，退出码 0；`日志/20260621_P22_语素主信息未接入结构壳删除_build.log` 显示 0 个警告、0 个错误。

后续：

- 继续 P3：剩余结构重点转为 `结构_信息入口定义`、`结构_人类词性到信息入口候选` 及相关全局内联函数是否需继续公开。

## 23. P4 基础信息类型模板创建判断入口收口

当前目标：

- 将 `基础信息类.cpp` 对 `语素_基础信息类型可由语素入口模板创建` 的外部全局调用回迁到 `语素类`。

已复核：

- `说明书/函数功能说明文档.md` 中没有既有 `语素类::基础信息类型可由语素入口模板创建`。
- `基础信息类.cpp:251` 是当前唯一外部直接调用；其余调用位于 `语素主信息类.h` 或 `语素类.cpp` 内部。

预测：

- 新增 `语素类::基础信息类型可由语素入口模板创建` 静态只读函数，内部转调现有映射规则。
- `基础信息类.cpp` 改为调用 `语素类::基础信息类型可由语素入口模板创建`。
- `rg -n "语素_基础信息类型可由语素入口模板创建" -g "*.h" -g "*.cpp" -g "*.ixx"` 除 `语素主信息类.h` 内部定义外无外部命中。
- `python .\tools\生成函数功能说明文档.py` 成功，函数条目增加 1。
- Debug x64 构建通过，退出码 0；构建日志写入 `日志/20260621_P23_基础信息类型模板创建判断收口_build.log`。

执行结果：

- `语素类.h/cpp` 新增 `语素类::基础信息类型可由语素入口模板创建` 静态只读入口。
- `基础信息类.cpp::主信息类型是否可由语素入口模板创建` 已改为调用 `语素类` 静态入口。
- `rg -n "语素_基础信息类型可由语素入口模板创建" -g "*.h" -g "*.cpp" -g "*.ixx"` 仅剩 `语素主信息类.h` 内部定义和 `语素类.cpp` 转调。
- `python .\tools\生成函数功能说明文档.py` 成功；源码文件 128，函数条目 7108。
- Debug x64 构建通过，退出码 0；`日志/20260621_P23_基础信息类型模板创建判断收口_build.log` 显示 0 个警告、0 个错误。

后续：

- 继续 P4：判定并收口剩余 `语素_取/查找/推断/选择` 内联函数公开边界。

## 24. P4 信息入口映射旧全局内联函数内部化

当前目标：

- 删除剩余顶层 `语素_查找信息入口定义`、`语素_取信息入口...`、`语素_选择默认信息入口类型`、`语素_推断人类词性默认基础信息类型`、`语素_人类词性是否兼容基础信息类型` 等公开 helper 名。
- 保留 `语素主信息类.h` 构造函数需要的映射逻辑，但收进 `语素主信息内部` 命名空间。
- `语素类.cpp` 作为正式外部入口实现方，改为调用内部映射函数。

已复核：

- 旧顶层 helper 仅在 `语素主信息类.h` 和 `语素类.cpp` 内部使用。
- 外部调用方已通过 `语素类` 静态入口读取信息入口定义和基础信息类型判断。

预测：

- `rg -n "语素_查找信息入口定义|语素_取信息入口|语素_信息入口需要上下文限定|语素_信息入口裸词默认模板入口|语素_选择默认信息入口类型|语素_推断人类词性默认基础信息类型|语素_基础信息类型可由语素入口模板创建|语素_人类词性是否兼容基础信息类型|语素_信息入口定义表|语素_人类词性到信息入口候选表" -g "*.h" -g "*.cpp" -g "*.ixx"` 无命中。
- `python .\tools\生成函数功能说明文档.py` 成功，旧顶层 helper 条目移除。
- Debug x64 构建通过，退出码 0；构建日志写入 `日志/20260621_P24_信息入口映射旧全局内部化_build.log`。

执行结果：

- `语素主信息类.h` 中信息入口定义表、候选表和映射函数已收进 `语素主信息内部` 命名空间。
- `语素类.cpp` 已改为调用 `语素主信息内部` 映射函数；对外正式入口仍为 `语素类` 静态函数。
- `rg -n "语素_查找信息入口定义|语素_取信息入口|语素_信息入口需要上下文限定|语素_信息入口裸词默认模板入口|语素_选择默认信息入口类型|语素_推断人类词性默认基础信息类型|语素_基础信息类型可由语素入口模板创建|语素_人类词性是否兼容基础信息类型|语素_信息入口定义表|语素_人类词性到信息入口候选表" -g "*.h" -g "*.cpp" -g "*.ixx"` 无命中。
- `python .\tools\生成函数功能说明文档.py` 成功；源码文件 128，函数条目 7108。
- Debug x64 构建通过，退出码 0；`日志/20260621_P24_信息入口映射旧全局内部化_build.log` 显示 0 个警告、0 个错误。

后续：

- P4 信息入口定义函数收口当前闭合；下一步转入 P6 语素入口桥接字段外部直读 / 写入扫描。

## 25. P6 语素入口桥接字段外部直读 / 写入扫描

当前目标：

- 扫描外部是否仍直接读取或写入 `语素入口主信息类::对应基础信息类型`、`对应基础信息`。

扫描证据：

- 完整扫描日志：`日志/20260621_P25_语素入口桥接字段扫描.txt`。
- `external_field_direct = 0`。
- 外部正式 API 调用仅剩：
  - `世界树类.cpp`：`语素集.添加对应基础信息`。
  - `二次特征应用模块.ixx`：`语素集.确保语素入口对应基础信息`、`语素集.获取语素入口对应基础信息类型`。
  - `控制面板类.cpp`：`语素集.获取语素入口显式对应基础信息类型`。

判定：

- `语素类.cpp` 和 `语素主信息类.h` 内部读写桥接字段属于语素类实现边界。
- 当前没有外部字段直读 / 写入需要生成代码清理切片。

后续：

- 进入 P7：复核外部 `语素集.根指针`、语素节点父 / 子 / 下 链直接访问是否已清零。

## 26. P7 语素树结构直接访问清零复核

当前目标：

- 复核外部是否仍直接访问 `语素集.根指针` 或语素节点父 / 子 / 下链。

扫描证据：

- `日志/20260621_P26_语素树结构直接访问扫描.txt`：粗扫包含语素入口类型正常使用，`root_ptr = 0`。
- `日志/20260621_P26_语素树链字段访问扫描.txt`：链字段命中主要为通用链表、基础信息、需求、任务、方法等其它树遍历。
- `rg -n "语素集\.根指针" -g "*.h" -g "*.cpp" -g "*.ixx"` 无命中。
- 控制面板语素树根读取为 `语素集.获取语素树根节点()`；语素树展示主路径为 SQL 投影 / `语素类` 入口。

判定：

- 当前无外部 `语素集.根指针` 直接读取。
- 当前未发现外部绕过 `语素类` 直接遍历语素树父 / 子 / 下链的闭合清理点。

后续：

- 进入 P8：复核显示和 SQL 投影隔离，确认词面 / 诊断文本不作为机器判断依据。

## 27. P8 场景二次特征词面机器查找问题记录

当前目标：

- 处理 P8 复核发现的词面参与机器查找问题。
- 用户已明确修正口径：该逻辑必须改成通过具体的特征和方向查找。

问题锚点：

- `二次特征应用模块.ixx::查找场景二次特征主信息` 当前以 `概念名称` 字符串为入参，并用 `语素集.安全获取词(主信息->概念名称) != 概念名称` 过滤命中项。
- `自我线程模块.impl.cpp::私有_读取场景二次特征布尔`、`私有_读取场景最新动态V1摘要` 通过 `"当前任务是否阻塞"`、`"变化方向"` 等概念词读取二次特征。
- `自我线程模块.impl.cpp::私有_写治理二次特征候选` 当前只把概念词构造成“二次特征概念锚点”并放到右对象；这仍是概念文本锚点，不是具体特征节点。

当前判断：

- `语素集.安全获取词` 只能用于显示 / 诊断 / SQL 投影；不得作为机器查找条件。
- 本切片不应改成“入口词同一性”即可收口；查找键应收束为项目结构中的具体特征节点和方向条件。
- 现有可用承载位为：`二次特征主信息类::左对象 / 右对象`、`标量值 / 是否满足 / 方向区间 / 离散编码`，以及世界树已有 `确保特征` / `取或创建子特征_按类型`。

下一步：

1. 在治理二次特征候选写入时，用概念词作为入口种子创建 / 查找具体特征节点，并将该特征节点写入二次特征结构键。
2. 将场景二次特征读取接口从 `概念名称` 字符串查找改为按特征节点和方向条件查找。
3. 保留概念词仅用于名称种子、摘要、日志和 SQL 显示，不再参与机器命中判断。

执行结果：

- `二次特征生成模块::查找场景二次特征主信息` 已改为按 `特征节点 + 方向约束 + 主体 + 域` 查找；不再读取 `语素集.安全获取词(主信息->概念名称)` 做机器过滤。
- `世界树类::查找场景二次特征主信息` / `读取场景二次特征布尔` 已同步改为结构化特征节点接口。
- `自我线程模块.impl.cpp` 写治理二次特征候选时，先在主体存在下确保具体特征节点，并将该特征节点写入二次特征结构键；旧“二次特征概念锚点”路径已删除。
- `自我线程模块.impl.cpp` 读取旧阻塞和动态 V1 摘要时，先只读查找已存在治理特征节点，再按特征节点和方向约束查询场景二次特征。
- 已重新生成 `说明书/函数功能说明文档.md`；源码文件 128，函数条目 7109。
- Debug x64 构建通过，退出码 0；构建日志写入 `日志/20260621_P27_二次特征特征方向查找_build.log`。

验证证据：

- `rg -n "安全获取词\(主信息->概念名称\)|语素集\.安全获取词\(.*\) !=|二次特征概念锚点|概念锚点" 二次特征应用模块.ixx 自我线程模块.impl.cpp 世界树类.cpp 世界树类.h` 无命中。
- `Select-String -LiteralPath '日志\20260621_P27_二次特征特征方向查找_build.log' -Pattern 'error C\d+|fatal error|warning C\d+|Build FAILED|生成失败'` 无命中。

后续：

- 继续 P8：复核剩余词面、诊断文本、SQL 投影字段是否仍参与机器判断；若只用于显示则记录判定表，若发现结构缺口则拆独立小切片。
