# 20260620 场景状态索引移除函数 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.2 P3.2e，新增 `场景类::移除场景状态索引` 并回迁 `状态类::删除状态` 中的状态索引直写。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
实施记录/20260620_场景主信息直读直写扫描_Codex断点清单.md
实施记录/20260620_场景动态索引快照函数_Codex断点清单.md
```

本切片范围：

```text
1. 只新增 状态索引 移除函数。
2. 只回迁 状态类::删除状态 中的状态索引 erase 直写。
3. 不处理 状态类 内部其它状态索引清理 / 追加 / 扫描路径。
4. 不处理 动态索引 / 二次特征索引 移除函数。
```

函数文档预查：

```text
已查 说明书/函数功能说明文档.md。
未发现 移除场景状态索引 / 场景状态索引移除 同名或同功能函数。
已有相关函数仅包括 状态类::删除状态 和 状态类::私有_按时间追加状态索引。
```

验证前预测：

```text
1. 新增函数只从 场景节点主信息类::状态索引 移除指定状态引用，不删除状态节点。
2. 状态类::删除状态 改为调用 场景类 函数后，该函数不再直接 erase 场景主信息->状态索引。
3. 函数文档生成后应新增 场景类::移除场景状态索引 条目。
4. Debug x64 构建应通过。
```

禁止宣称：

```text
不宣称所有场景主信息直读已回迁。
不宣称场景类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 1. 实施与验证

已实施：

```text
1. 场景类.h / 场景类.cpp 新增 `移除场景状态索引(场景节点类* 场景, 状态节点类* 状态节点)`。
2. 该函数只移除场景状态事实目录引用，不删除状态节点。
3. 状态类::删除状态 改为调用 `场景类::移除场景状态索引`。
4. 状态类::删除状态 不再直接 erase `场景主信息->状态索引`。
```

已运行：

```powershell
python .\tools\生成函数功能说明文档.py *> 日志\20260620_场景状态索引移除函数_函数文档.log
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\20260620_场景状态索引移除函数_msbuild.log
```

验证结果：

```text
函数文档生成退出码：0
函数文档统计：源码文件 128；函数条目 6894
函数文档新增条目：说明书/函数功能说明文档.md 中已出现 场景类::移除场景状态索引 和声明条目
Debug x64 构建退出码：0
构建关键日志：已成功生成；0 个错误
```

当前结论：

```text
P3.2e 已闭合。
下一步进入 P3.2f：新增 场景类::移除场景动态索引，并回迁 动态类::删除动态 中的动态索引直写。
```
