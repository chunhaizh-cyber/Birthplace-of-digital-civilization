# 20260607 P9d 6147 跟踪目标上下文桥 Codex断点清单

## 当前切片

```text
计划：计划/20260606_外设四本能方法任务真实运行条件包闭环计划_v0.1.md
恢复点：P9d 6147 自我_跟踪指定存在 目标上下文 / 任务主体 / 跟踪等待项定位。
用户指令：性能优化线先等其他完成后再回头处理。
```

## 已定位

```text
1. 6146 扫描主体桥已闭合，正式目标存在可自然进入 自我_扫描已识别区域。
2. 当前任务管理能按 6147 方法结果能力映射为 目标跟踪观察 等待项。
3. D455 只在存在目标跟踪等待项时生成跟踪报告，不会裸产正式跟踪报告。
4. 6150 稳定子集桥当前只写 已识别区域扫描入账状态 的目标观察约束，没有写 目标存在跟踪状态 的目标观察约束。
5. 自我线程只有 扫描材料承接生成6146任务主体 入口，没有对应的 6147 任务主体入口。
```

## 修改前预测

```text
本轮最小修改：
    1. 6150 稳定子集桥继续只写正式目标存在 / 映射 / 约束，不直接建任务；
    2. 在每个正式目标存在上补入跟踪目标上下文：
        目标存在跟踪状态=未更新；
        目标观察窗口状态=已更新；
        跟踪基准特征状态=可读；
        并提交 目标存在跟踪状态 对应的目标观察约束；
    3. 自我线程在扫描材料承接到正式目标存在后，生成 / 复用 目标存在跟踪状态 子需求并提交任务发起请求；
    4. 任务管理自然选择 6147 后，按方法供料创建目标跟踪等待项；
    5. D455 后续只按目标跟踪等待项生成跟踪报告。

预期日志：
    当前观察范围可观测单位存在对应事实提交 中出现 稳定子集跟踪约束写入数量>0；
    自我线程出现 跟踪目标上下文承接生成6147任务主体 或 复用6147任务主体；
    任务管理后续出现 当前本能ID=6147 或 按当前方法供料要求创建 目标跟踪观察 等待项；
    若跟踪材料仍只是可用或条件不足，6147 可以失败 / 待复验，但不得生成稳定跟踪动态候选。
```

## 边界

```text
不把扫描报告、跟踪报告、等待项、材料页或目标观察约束当事实主体。
不把 当前观察范围可观测单位存在对应闭合状态 强写为 1。
不把 当前场景特征值明确状态 强写为 1。
不让 D455 直接证明需求满足、跟踪成功、安全提升或世界真值。
不把任务筹办 / 任务执行注册成本能方法。
不新增伪样本、伪入口或 --*-check 回归命令。
```

## 已修改文件

```text
自我动作实现.外设模块.ixx
自我线程模块.impl.cpp
```

## 已完成修改

```text
1. 6150 稳定子集桥新增目标存在跟踪状态对应的目标观察约束构造。
2. 6150 对每个正式目标存在补写：
    目标存在跟踪状态=未更新；
    目标观察窗口状态=已更新；
    跟踪基准特征状态=可读。
3. 6150 继续只提交事实、映射和目标观察约束，不直接创建 6147 任务。
4. 自我线程新增扫描材料承接后确保 6147 任务主体入口：
    从扫描报告目标存在ID和来源需求生成 / 复用 目标存在跟踪状态 子需求；
    通过任务管理提交任务发起请求；
    不伪造跟踪报告和跟踪成功动态。
```

## 已运行命令

```text
git diff --check -- 自我动作实现.外设模块.ixx 自我线程模块.impl.cpp 实施记录/20260607_P9d_6147跟踪目标上下文桥_Codex断点清单.md
退出码：0
备注：仅出现工作区既有 LF/CRLF 换行警告。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
退出码：0
结果：0 error，1 warning LNK4075。

.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=70000
退出码：0
耗时：92489ms
日志：日志/鱼巢_run_20260607.低值g 从 70460225 增至 85369660 字节。
```

## 首次真实运行证据

```text
1. 新增 6150 跟踪约束计数字段已进入真实日志。
2. 本次 70s 窗口内，12:57 左右 6150 未自然进入稳定子集成功样本，跟踪约束写入数量仍为 0。
3. 日志显示 D455 已提交逐簇识别报告，任务管理承接了报告 ID 1 / ID 2。
4. 本次窗口内未出现：
    跟踪目标上下文承接生成6147任务主体；
    当前本能ID=6147；
    目标跟踪观察等待项；
    指定存在跟踪正式结果。
```

## 当前判断

```text
代码路径已接上并通过构建，但首次真实运行没有验证到 6147 自然链闭合。
当前不是 6147 代码编译阻塞，而是本次自然样本没有走到可供 6147 承接的稳定子集扫描成功结果。
下一步做更长真实运行取证；若仍没有稳定子集成功样本，则把上游自然样本未满足作为本切片阻塞点记录。
```

## 后续验证与新增发现

```text
第二次真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=120000
    退出码：0
    耗时：99460ms
    日志：85369660 -> 101405364 字节
    结果：
        D455 逐簇识别报告和任务管理观察事实承接请求已生成；
        自我线程只看到 任务管理上行消息入队 / 观察事实承接消息投递诊断；
        未看到 外设观察事实承接开始 / 完成；
        未看到 外设观察材料识别归属成功；
        未看到 6150 稳定子集成功；
        未看到 6147 任务主体或目标跟踪观察等待项。

新增停止前 drain：
    在主循环停止检查里，先抢占落账最多 4 条观察事实承接消息，再退出本轮。
    构建通过：0 error，1 warning LNK4075。

第三次真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=90000
    退出码：0
    耗时：137119ms
    日志：101405364 -> 141557728 字节
    结果：
        停止前观察事实承接落账=1；
        但落账发生在停止阶段，报告缓存已经不可回查；
        自我线程/外设观察事实承接完成 为 成功=否 / 报告可回查=否；
        因此仍无法推进 6145 -> 6150 -> 6147。

尝试过但已撤回：
    曾将 任务管理上行通知后观察事实承接 同步接入通知回调。
    该尝试能让自我线程在报告缓存有效期内承接成功，日志出现：
        自我线程/观察事实承接抢占抽取完成 | 来源=任务管理上行通知后观察事实承接；
        自我线程/外设观察事实承接完成 | 成功=是 | 报告可回查=是。
    但真实运行命令超过工具超时，进程继续运行，日志从 141557728 膨胀到 736109452 字节；
    已手动停止进程，撤回通知回调同步抢占代码。
    撤回后重新构建通过：0 error，1 warning LNK4075。
```

## 当前阻塞点

```text
6147 跟踪目标上下文桥代码已接入并可构建，但自然运行仍卡在更前置的观察承接时序：
    任务管理已经把观察事实承接请求投递到自我线程；
    自我线程主循环在普通运行窗口内没有及时消费观察承接；
    停止前 drain 太晚，报告缓存可能已不可回查；
    通知回调内同步抢占虽然能抢到有效报告，但会导致过重同步推进 / 日志暴涨，不适合作为低风险修复。

下一步应拆一个独立小切片：
    设计“观察承接专用轻量异步唤醒 / 有预算 drain”；
    不在任务管理上行通知回调里同步跑完整落账；
    不扩大普通治理消息重排；
    保证报告缓存有效期内完成观察承接；
    再恢复 6145 -> 6150 -> 6146/6147 自然链验证。
```

## 日志清理切片

```text
用户指令：
    先清理所有生成日志的代码。

执行边界：
    先清理本轮 P9d/6147 与观察承接阻塞定位新增的高频诊断日志；
    不删除全项目日志框架；
    不删除错误日志；
    不删除既有真实验证所依赖的核心日志入口。

预期：
    保留 6150 跟踪目标上下文和 6147 任务主体桥接业务逻辑；
    删除新增的 6147 skip/success 诊断输出；
    删除 6150 跟踪约束新增计数字段输出；
    撤回停止前观察承接 drain 的额外日志触发路径。

实际完成：
    已删除 6146 / 6147 任务主体桥接中的跳过、失败、复用、成功运行日志；
    已删除目标 I64 子需求生成失败分支的日志主题参数和运行日志；
    已删除 6150 稳定子集跟踪约束的日志计数和输出字段；
    已撤回主循环停止检查中的停止前观察事实承接落账调用，仅保留停止阶段日志；
    保留稳定子集跟踪状态特征写入、跟踪目标观察约束写入和 6147 任务主体业务逻辑。

验证：
    rg 确认已无扫描材料承接生成6146 / 跟踪目标上下文承接生成6147 / 停止前观察事实承接落账日志标识；
    剩余“稳定子集跟踪约束”仅为构造函数和本地变量名，不是日志输出；
    git diff --check：通过，仅 LF/CRLF 提示；
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m：退出码 0，0 error，1 warning LNK4075。
```

## 观察承接轻量唤醒切片

```text
目标：
    让任务管理调度让步回调只做自我线程唤醒和待处理判断；
    不在任务管理线程回调里同步执行观察事实承接落账；
    实际落账继续由自我线程主循环冻结 / 规范化 / 抢占路径完成。

边界：
    不改变任务状态裁决；
    不把等待项、报告或目标观察约束当事实主体；
    不新增回归入口或伪样本；
    清理观察承接投递 / 冻结 / 规范化 / 抢占诊断日志，仅保留核心开始 / 完成 / 失败证据。

预期：
    任务管理消费泵遇到自我线程已有观察承接待处理时让出一轮；
    自我线程通过 condition_variable 被唤醒；
    观察承接不再在任务管理回调栈内同步跑完整世界落账。
```

## 观察承接轻量唤醒与停止收口验证

```text
实际完成：
    1. 任务管理调度让步回调仍只唤醒自我线程，不同步执行观察事实承接落账。
    2. 自我线程在规范化消息后和逐条规范化后增加观察事实承接抢占入口，仍由自我线程自己落账。
    3. 清理观察承接投递 / 冻结 / 规范化 / 抢占诊断日志，以及任务管理消费泵让步、队列头部、同任务重排热日志。
    4. 修复同任务重排入队原因递归增长：重排时只写当前原因，不拼接历史入队原因。
    5. 自我线程停止时先请求停止，再停止任务管理 worker 池，最后等待自我线程退出并停止任务管理界面线程。
    6. 任务管理停止请求中不再登记等待工作项、不再发送 / 重排执行前许可工作项，三套消费路径均在停止请求后退出消费。
    7. 任务管理轻量让步待处理判断同时识别 观察事实承接请求 和 执行前许可请求。

已运行命令：
    git diff --check -- 任务模块.管理界面线程.impl.cpp 自我线程模块.impl.cpp
    退出码：0，仅 LF/CRLF 提示。

    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码：0，0 error，1 warning LNK4075。

    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000
    退出码：0
    耗时：126309ms
    日志：1405597811 -> 1406236893 字节，新增 639082 字节。
    进程残留：无。

新增日志段统计：
    行数=1627
    最大行长=968
    程序退出/守卫=6
    自我线程/停止 | 停止任务管理工作线程池开始=1
    自我线程/停止 | 停止任务管理工作线程池完成=1
    自我线程/停止 | 等待工作线程join完成=1
    自我线程/停止 | 停止任务管理界面线程完成=1
    自我线程/主循环 | 退出线程函数=1
    外设线程_D455深度相机/停止=1
    队列头部诊断=0
    同任务在途工作项重排=0
    同任务执行前许可占用重排=0
    同任务已有执行前许可占用，延后重排=1
    执行工作项等待自我审批=12
    执行前许可请求上行=2
    自我线程/执行前许可审批=2
    执行前许可结果入队=2
    外设观察事实承接开始=2
    外设观察事实承接回查成功=2
    外设观察事实承接完成=2
    报告可回查=是=4
    当前本能ID=6150=3
    当前本能ID=6146=0
    当前本能ID=6147=0

当前判断：
    观察承接时序阻塞已缓解：本次真实运行中报告可回查成功，且日志不再暴涨。
    停止收口已恢复：自我线程、任务管理界面线程、外设承接后台线程和 D455 外设线程均自然停止。
    本次样本仍未自然出现 6146 / 6147，P9d 主链需要继续回到 6150 之后的扫描 / 跟踪任务主体自然触发链定位。
```

## 父任务唤醒后重筹办工作项补链切片

```text
用户补充证据：
    承接成功后来源任务确实被唤醒；
    任务 B 从等待中改到待重筹办；
    后续 25 秒内没有看到 6145 / 6150 / 6146 / 6147 自然执行；
    旧日志文件过大已删除。

代码定位：
    私有_唤醒派生需求来源任务_任务管理新链 已调用 提交父任务等待条件变化通知；
    提交父任务等待条件变化通知 只通过任务状态推进请求写入 待重筹办 / 筹办 / 等待缺口唤醒；
    该路径此前没有同步提交 重筹办任务 治理请求，因此可能只改状态、不登记工作项。

修改前预测：
    父任务等待条件变化通知成功后，应立即向任务管理界面线程提交同一父任务的 重筹办任务 治理请求；
    该请求只登记等待工作项，不由自我线程同步执行筹办；
    后台 worker 后续消费该工作项，任务 B 不应长期停在待重筹办；
    若自然样本成立，日志应出现 父任务等待条件变化裁决 后紧跟 任务管理领取父任务等待条件变化重筹办，并出现重筹办工作项ID。

本次修改：
    自我线程模块.impl.cpp：
        父任务唤醒项区分 已提交重筹办治理 / 已登记重筹办工作项 / 重筹办工作项ID；
        父任务等待条件变化通知成功且任务进入待重筹办后，提交 重筹办任务 治理请求；
        治理请求设置 允许派发工作线程=false，后续仍由任务管理后台 worker 消费；
        汇总日志增加 已提交重筹办治理 / 已登记重筹办工作项 计数。

待验证：
    git diff --check；
    msbuild Debug x64；
    写预测后真实运行 --self-check-log，核对父任务唤醒后是否登记重筹办工作项，并继续观察 6145 / 6150 / 6146 / 6147 自然链。
```

## 父任务唤醒后重筹办工作项补链验证

```text
已运行命令：
    git diff --check -- 自我线程模块.impl.cpp 实施记录/20260607_P9d_6147跟踪目标上下文桥_Codex断点清单.md
    退出码：0，仅 LF/CRLF 提示。

    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    首次退出码：1；
    原因：父任务唤醒函数位于文件前段，新增代码引用的 私有_解析存在引用_任务语义 / 私有_解析场景引用_任务语义 缺少前向声明；
    修复：补前向声明。

    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码：0，0 error，1 warning LNK4075。

    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=90000
    退出码：0
    耗时：117270ms
    日志：日志/鱼巢_run_20260607.低值g，547602 字节
    进程残留：无。

真实日志证据：
    本次没有命中 私有_唤醒派生需求来源任务_任务管理新链 的 父任务等待条件变化 路径：
        父任务等待条件变化=0；
        父任务等待条件变化重筹办=0；
        派生需求解除父需求任务等待=0。
    因此本次真实运行只证明补链代码可构建，不能证明派生父任务等待条件变化样本已自然通过。

    用户关注的来源任务 B 路径已出现工作项和自然执行：
        14:15:46 任务 B 当前方法=1i，筹办后进入就绪；
        14:15:47 任务 B 就绪 -> 排队中；
        14:15:47 任务管理工作线程执行任务 B，当前本能ID=6145；
        14:15:49 自我_识别外设观察材料 成功=1，报告ID=2，可提交=22，扫描可启动状态=1；
        14:15:49 任务 B 目标达成=1，完成度百万分比=1000000。
    结论：
        任务 B 本次不是只改状态没有派发工作项；它已自然执行 6145。

    后续 6150 / 6146 / 6147 状态：
        当前本能ID=6150 出现 3 次；
        当前本能ID=6146 出现 0 次；
        当前本能ID=6147 出现 0 次。
        6150 日志显示 来源报告ID=0、观察材料等级=不合格、提交质量依据=缺外设观察报告；
        稳定子集全量扫描等待项跳过，原因为 无可用扫描目标观察约束。

    观察事实承接与来源需求重筹办：
        14:16:11 外设观察事实承接完成，成功=是，报告可回查=是；
        14:16:12 来源需求任务 9 从等待中 -> 待重筹办；
        14:16:13 工作项=38 已登记，入队原因=观察事实更新触发来源需求重筹办；
        14:16:13 任务管理领取来源需求重筹办，已承接=是，后续巡检消费=是；
        14:16:15 安全根任务 2 工作项=39 已登记。
    备注：
        上述重筹办登记发生在停止收口期，不能扩大宣称为后续工作项已完成消费。

当前判断：
    本轮已回答任务 B 被唤醒后是否入队：真实日志显示 B 已有工作项并自然执行 6145；
    6146 / 6147 未出现的当前证据不再是 B 没有派发，而是 6150 / 扫描目标约束链仍在缺有效报告或缺可用扫描目标观察约束；
    已补上派生父任务等待条件变化路径的重筹办工作项登记代码，但本次样本未命中该路径，需等待自然样本复验。
```

## 6145 -> 6150 来源报告与目标约束继续定位

```text
用户续跑要求：
    旧日志文件过大已删除；
    继续按计划执行，直到遇到问题。

已读取代码：
    自我动作实现.外设模块.ixx：
        6145 自我_识别外设观察材料 成功后会向运行存在、实际输出场景、目标宿主写入 外设观察报告ID、可提交数量、已稳定对应数量、扫描可启动状态；
        6150 提交_当前观察范围可观测单位存在对应事实 执行时会从输入参数场景、实际输出场景、目标宿主读取 外设观察报告ID；
        6150 在可写对应事实且扫描可启动后，才写入稳定子集扫描/跟踪目标观察约束。
    任务模块.筹办.ixx：
        通用筹办会为候选方法创建 可执行输入参数场景，并把形参表中的参数从目标宿主或任务上下文拷入输入包；
        6150 规格当前只把 输入条件包 / 目标宿主 / 当前方法运行存在 声明为形参，外设观察报告ID 只是条件特征，不会被通用形参绑定器主动当成输入实参补入。
    任务模块.管理界面线程.impl.cpp：
        6150 工作结果后会按稳定子集全量扫描目标约束创建扫描等待项；
        若目标观察约束集合为空，会记录 稳定子集全量扫描等待项跳过 | 原因=无可用扫描目标观察约束。

当前代码判断：
    6145 的输出事实已经包含报告ID；
    6150 是否能读到报告ID，取决于本次 6150 所属任务的输入参数场景或目标宿主是否承接了 6145 输出事实；
    若 6150 目标宿主不是 6145 写入报告ID的同一宿主，或筹办输入未显式带入报告ID，则 6150 会以 来源报告ID=0 运行，无法建立稳定子集目标观察约束。

下一次真实运行预测：
    若当前断点复现，日志会出现 6145 成功且报告ID>0；
    随后 6150 仍可能出现 来源报告ID=0 / 提交质量依据=缺外设观察报告；
    稳定子集全量扫描等待项仍会因 无可用扫描目标观察约束 跳过；
    6146 / 6147 仍不会自然出现。
    若新日志显示 6150 来源报告ID>0 且稳定子集扫描约束写入数量>0，则当前假设失效，应转查扫描等待项消费和外设扫描报告承接。

待运行：
    msbuild Debug x64；
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=90000；
    从新日志核对 6145 / 6150 / 稳定子集扫描等待项 / 6146 / 6147。
```

## 停止收口期 worker 过早停止修正

```text
本次复核旧日志时间线：
    16:25:40 程序退出守卫开始停止自我线程；
    16:25:40 自我线程先停止任务管理工作线程池，20 个后台 worker 立即退出；
    16:25:41 D455 报告ID=2 入队，外设承接后台仍生成观察事实承接上行；
    16:26:43 自我线程落账该观察事实，任务 B 等待中 -> 待重筹办；
    16:26:44 工作项 41 登记，入队原因=观察事实更新触发来源需求重筹办；
    16:27:13 自我线程退出。

定位结论：
    工作项 41 未被消费，不是 worker 消费慢或工作项状态版本阻断；
    原因是自我线程停止流程先停任务管理 worker，再等待自我线程主循环收口；
    但外设承接后台和自我线程仍会在停止收口期继续落账观察事实并登记任务工作项。

修改前预测：
    停止请求后若 worker 先退出，停止收口期新增的来源需求重筹办工作项只能入队，不能自然执行；
    日志会出现 后台worker退出 早于 外设观察事实承接完成 / 任务管理领取来源需求重筹办。

本次修改：
    自我线程模块.impl.cpp：
        自我线程停止顺序改为 请求停止 -> 等待自我工作线程收口 -> 停止任务管理工作线程池 -> 停止任务管理界面线程；
        等待自我工作线程收口期间，任务管理 worker 保持运行，用于消费停止收口期自然产生的任务工作项；
        未新增同步消费入口，未在自我线程中直接执行任务方法。

验证预测：
    新日志中 停止任务管理工作线程池开始 应晚于 等待工作线程join完成；
    若停止收口期再次出现 B 的观察事实承接和重筹办工作项，后台 worker 应有机会消费该工作项；
    若 6145 带报告ID重新执行后仍无法推动 6150，则转入 6145 输出宿主 / 6150 输入参数场景承接关系定位。
```

## 按请求归并完成后上行丢失修正

```text
复核新日志：
    B 在停止收口期已经完成 6145，报告ID=70；
    B 任务目标对比实际值 1、目标值 1，并记录任务 B 实际状态=完成；
    但 B 最终成功结果没有出现 自我线程/任务管理上行消息入队 | 消息=任务管理结果状态；
    自我线程也没有继续记录 任务满足反馈收束判定 / 来源需求收束 / 父需求等待解除。

代码定位：
    任务模块.管理工作线程.ixx 已为任务工作结果构造 任务管理结果状态 上行消息；
    任务模块.管理界面线程.impl.cpp 的通用巡检消费路径在归并完成项后会推入上行消息集、刷新快照、记录承接结果；
    按请求消费路径 私有_消费任务调度队列_按请求 成功归并完成项后只写消费日志，未把承接.上行消息集推入上行队列。

修改前预测：
    请求绑定路径完成 B 时，任务自身可被置为完成；
    但 B 成功结果状态不会进入自我线程上行队列；
    因此来源需求 B 不会被自我线程收束，父任务等待条件不会被解除，后续 6150 / 6146 / 6147 链自然断开。

本次修改：
    在 私有_消费任务调度队列_按请求 成功归并完成项后，复用通用巡检消费路径：
        私有_推入上行消息集(承接.上行消息集, 承接)；
        私有_刷新快照(承接, 承接.说明)；
        私有_记录承接结果(..., 完成时刻)。

验证预测：
    下一次真实运行中，B 完成后应出现 任务管理结果状态 成功上行；
    自我线程应继续记录任务满足反馈收束、来源需求收束和父需求等待解除；
    若父任务继续重筹办，6150 应有机会在来源报告ID>0的上下文中重跑；
    若仍只有 来源报告ID=0 或 6146/6147 仍不出现，再转查父任务重筹办后的 6150 输入上下文承接。
```

## 后台完成队列结果上行未触发自我线程提取修正

```text
本次真实运行新增证据：
    B 工作项=63 在 16:55:40 执行 6145 成功；
    任务管理工作线程记录目标状态比较：目标值=1，实际值=1，接受=1；
    任务管理界面线程在 16:55:44 记录任务 B 完成判定允许完成；
    16:55:46 后台完成队列归并工作项=63，进入收束=1，建议任务状态=完成；
    但 16:55:46 之后自我线程只收到 E 的执行前许可请求，未收到 B 的 任务管理结果状态 成功消息。

代码定位：
    同步消费路径 私有_消费任务调度队列 / 私有_消费任务调度队列_按请求 在归并完成项后会触发任务管理调度让步；
    后台完成队列路径 归并任务工作完成队列 只把 承接.上行消息集 推入任务管理上行队列，未触发自我线程让步；
    自我线程只有在特定 checkpoint 提取任务管理上行队列，后台归并结果可能滞留，导致 B 成功状态未及时落账。

本次修改：
    归并任务工作完成队列 在成功归并且承接.上行消息集非空后，调用 私有_触发任务管理调度让步("后台完成队列归并后")；
    若让步成功，本轮先停止继续生成后续任务工作项，让自我线程优先提取并落账任务管理上行消息。

验证预测：
    下一次真实运行中，若 B 再次完成 6145，应出现 自我线程/任务管理上行消息入队 | 消息=任务管理结果状态 | 尝试=B|执行任务 | 结果=成功；
    随后应出现任务满足反馈收束、来源需求收束、父任务等待解除或父任务等待条件变化重筹办；
    若成功上行恢复但 6150 仍以 来源报告ID=0 运行，则下一阻塞点转为 6145 输出报告ID到父任务 6150 输入上下文的承接关系。
```

## 任务状态提交动作动态等值状态报错定位

```text
本次真实运行新增证据：
    B/C 成功结果上行已经恢复；
    D 已越过旧的 6150 来源报告ID=0 阻断，6150 承接到 可用扫描目标观察约束数量=12，扫描等待项提交数量=12；
    随后出现 任务状态提交/动作动态创建逻辑错误，原因为 动态节点创建失败：
        方法=A，任务=D，场景=EK；
        方法=A，任务=1，场景=EK。

代码定位：
    提交任务状态变化 先写任务状态，再按 任务状态 特征创建动作动态；
    调用方可能提交 旧状态==新状态 但 建议任务阶段 发生变化的推进；
    动态层 创建动态 对初始状态值和结果状态值相等的动作动态返回空，这是方法动作动态硬规则；
    当前实现把“仅任务阶段变化”误编码成“任务状态等值变化”，因此触发非正常逻辑错误。

修改前预测：
    继续运行会在任务阶段推进但任务状态未变化时反复弹 任务状态动作动态逻辑错误；
    该错误不是 6150/6147 目标上下文缺失本身，而是任务管理状态提交链在阶段变更场景的动态特征选择错误。

拟修改：
    自我动作实现模块.ixx：
        增加 特征_任务当前阶段；
        输出任务状态提交动作动态 增加旧阶段/新阶段入参；
        若任务状态变化，仍按 任务状态 生成动作动态；
        若任务状态未变化但任务阶段变化，按 任务当前阶段 生成动作动态；
        不放行等值动作动态，不新增模拟验证入口。

验证预测：
    下一次真实运行不应再出现 任务状态提交/动作动态创建逻辑错误 或对应弹窗；
    B/C 成功上行应保持；
    D 的 6150 稳定子集扫描等待项承接应继续出现；
    若后续仍未进入 6146/6147，则下一阻塞点转入扫描等待项到识别/跟踪任务生成链。
```

## 任务阶段动作动态修正待验证

```text
已修改：
    自我动作实现模块.ixx：
        增加 特征_任务当前阶段，复用 自我特征定义类::类型_任务_当前阶段；
        输出任务状态提交动作动态 新增旧阶段/新阶段入参；
        动态特征选择改为：
            旧状态 != 新状态 时记录 任务状态；
            旧状态 == 新状态 且建议阶段有效、旧阶段 != 新阶段 时记录 任务当前阶段；
        提交任务状态变化 在写入前读取旧阶段，写入建议阶段后传入动作动态创建函数。

已运行静态检查：
    git diff --check -- 自我动作实现模块.ixx 实施记录/20260607_P9d_6147跟踪目标上下文桥_Codex断点清单.md
    退出码=0，仅有 LF/CRLF 提示。

下一步：
    构建 Debug x64；
    确认无残留鱼巢.exe 后真实运行；
    用新日志核对任务状态提交报错是否消失，以及 P9d 链是否继续推进到 6146/6147 或新的具体阻塞点。
```

## 任务阶段动作动态修正构建通过

```text
已运行：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m

结果：
    退出码=0；
    错误=0；
    警告=1，LNK4075，原因是 /INCREMENTAL:NO 与 /EDITANDCONTINUE 的既有组合。

真实运行前预测：
    不应再出现 任务状态提交/动作动态创建逻辑错误；
    若出现新阻塞，优先按新日志分段定位，不把 remote/运行中断当成项目逻辑失败；
    若运行结束时仍出现 禁止同步消费任务调度队列，则单独判断是否是停止窗口内 worker 已停后的残留工作项。
```

## 无有效状态阶段变化提交报错定位与修正待验证

```text
本次真实运行结果：
    旧的 D 阶段推进报错未复现；
    但仍出现同类弹窗：
        17:19:55，方法=A，任务=E，原因=动态节点创建失败；
        17:22:31，方法=A，任务=1，原因=动态节点创建失败；
        17:23:05，方法=A，任务=1，原因=动态节点创建失败。

新增定位：
    报错前存在大量同状态快照路径，它们在任务管理界面线程的 私有_提交界面任务状态变化 中能正确跳过；
    报错前也出现 普通整理回执到任务壳 | 任一写入=1，随后由 工作结果.治理结果.宿主任务新状态 路径直接调用 自我_提交任务状态变化；
    该直接调用路径没有任务管理界面线程的同状态快照短路，导致状态/阶段没有真实变化时仍进入动作动态创建。

本次修正：
    自我动作实现模块.ixx：
        提交任务状态变化 写状态/阶段后计算：
            状态变化 = 旧状态 != 新状态；
            阶段变化 = 建议阶段有效 && 旧阶段 != 新阶段；
        只有 状态变化 || 阶段变化 时才调用 输出任务状态提交动作动态；
        无有效状态阶段变化时记录 任务状态提交/无有效状态阶段变化跳过动作动态，提交仍为成功；
        不放行动作动态硬规则中的等值动态。

验证预测：
    下一轮真实运行中不应再出现 任务状态提交/动作动态创建逻辑错误 弹窗；
    日志应出现少量 无有效状态阶段变化跳过动作动态，用于替代之前的弹窗点；
    若任务继续卡住，下一阻塞点优先看执行前许可在途/重复重排或扫描等待项消费链。
```

## 无有效状态阶段变化修正构建通过

```text
已运行：
    git diff --check -- 自我动作实现模块.ixx 实施记录/20260607_P9d_6147跟踪目标上下文桥_Codex断点清单.md
    退出码=0，仅 LF/CRLF 提示。

    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0，错误=0，警告=1（LNK4075）。

下一步：
    确认无残留鱼巢.exe；
    真实运行；
    新日志核对：
        任务状态提交/动作动态创建逻辑错误 是否为 0；
        弹窗错误提示 是否为 0；
        任务状态提交/无有效状态阶段变化跳过动作动态 是否出现；
        6150 扫描等待项、6146/6147 或下一具体阻塞点。
```

## 执行前许可占用未纳入工作项去重导致队列风暴

```text
本次真实运行结果：
    任务状态提交/动作动态创建逻辑错误=0；
    弹窗错误提示=0；
    任务状态提交/无有效状态阶段变化跳过动作动态=4；
    但运行 240 秒未自然退出，被强制停止；
    新段中 执行工作项等待自我审批=189，同任务已有执行前许可占用=1916；
    6150 出现 3 次，6146/6147 未出现。

关键日志：
    请求 47 / 工作项 45 / 任务 E 在 17:29:06 上行执行前许可请求；
    17:29:08 到 17:29:58 期间，同任务 E 的多个工作项 71/53/68/77/42 持续因 占用工作项=45 重排；
    请求 47 在 17:29:53 才被批准；
    请求 63 / 工作项 62 / 任务 F 也出现同类占用重排。

代码定位：
    任务模块.管理界面线程.impl.cpp：
        执行前许可等待期间会写入 执行前许可任务占用表；
        私有_处理执行前许可门控 会用该表阻止同任务第二个执行工作项直接执行；
        但 私有_任务已有有效入队或在途工作项 只检查待筹办队列、待执行队列和在途工作项索引；
        它未把 执行前许可任务占用表 计入“已有有效执行工作项”；
        因此任务状态巡检仍可能继续生成同任务执行工作项，后续派发时才被占用表拦下并重排，形成队列风暴。

拟修改：
    在 私有_任务已有有效入队或在途工作项 中，当指定类型为空或执行时，若任务主键存在于 执行前许可任务占用表，直接返回 true；
    这只减少重复入队，不改变执行前许可审批语义，也不绕过自我线程审批。

验证预测：
    下一轮真实运行中 同任务已有执行前许可占用 数量应显著下降；
    执行前许可请求仍应出现并被自我线程审批；
    若仍超时，下一阻塞点再看执行前许可审批延迟来源或外设观察等待项消费。
```

## 执行前许可占用去重修正构建通过

```text
已修改：
    任务模块.管理界面线程.impl.cpp：
        私有_任务已有有效入队或在途工作项 在查询类型包含执行时，若任务主键已经存在于 执行前许可任务占用表，则返回 true。

已运行：
    git diff --check -- 任务模块.管理界面线程.impl.cpp 自我动作实现模块.ixx 实施记录/20260607_P9d_6147跟踪目标上下文桥_Codex断点清单.md
    退出码=0，仅 LF/CRLF 提示。

    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0，错误=0，警告=1（LNK4075）。

下一步真实运行核对：
    任务状态提交弹窗仍为 0；
    同任务已有执行前许可占用应不再大量刷屏；
    若仍有超时，按新日志确定下一阻塞点。
```

## 执行前许可占用去重真实运行结果与当前阻塞

```text
已运行：
    真实运行段：20260607_173309；start_line=29145；
    运行 240 秒未自然退出，按规则强制停止；
    新日志行数约 3759。

验证结果：
    任务状态提交/动作动态创建逻辑错误=0；
    弹窗错误提示=0；
    任务状态提交/无有效状态阶段变化跳过动作动态=2；
    当前本能ID=6150 出现 3 次；
    当前本能ID=6146 出现 0 次；
    当前本能ID=6147 出现 0 次；
    稳定子集全量扫描等待项承接=0；
    目标跟踪观察=0；
    执行工作项等待自我审批=79；
    同任务已有执行前许可占用=0；
    执行前许可结果入队=5；
    自我线程/执行前许可审批=5；
    拒绝工作项入队=0；
    后台worker工作项进入死信=3。

关键判断：
    任务状态提交弹窗报错已由“无有效状态/阶段变化不再创建动作动态”修正闭合；
    执行前许可占用未纳入去重导致的同任务重排风暴已明显缓解，且本轮没有再刷 同任务已有执行前许可占用；
    当前未闭合点不再是弹窗，而是 P9d 主线仍未回到 6146/6147：
        6150 本轮只停在外设观察等待项创建延后 / 目标观察约束ID=0 / 目标区域优质=0 / 可生成扫描变化候选=0 / 可生成跟踪动态候选=0；
        已出现外设观察事实承接，但未形成稳定子集全量扫描等待项或目标跟踪等待项；
        运行超时尾段仍有执行前许可等待与外设观察报告持续进入，未自然静默。

当前具体阻塞：
    P9d 的 6147 跟踪目标上下文桥仍缺少自然触发证据；
    下一步必须定位 6150 在本轮为什么没有拿到可用扫描目标观察约束，
    以及任务管理为什么没有从正式目标存在生成 6146/6147 的任务主体和目标跟踪等待项。

下一步最小切片：
    搜索 6150 的稳定子集扫描等待项生成路径；
    定位 目标观察约束ID=0 的来源；
    核对 私有_按稳定子集全量扫描约束提交外设等待项 与 目标跟踪等待项生成条件；
    不把外设报告、目标观察约束或材料页当成正式任务主体。
```
