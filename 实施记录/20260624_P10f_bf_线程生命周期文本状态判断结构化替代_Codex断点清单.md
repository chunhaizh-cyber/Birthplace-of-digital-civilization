# P10f-bf 线程生命周期文本状态判断结构化替代 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-bf，处理 `控制面板类.cpp` 中 `结构_控制面板线程信息项::生命周期状态` 文本参与统计分类的问题。

依据文件：
- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `计划/计划索引.md`
- `docs/工程图谱/05_规则原子索引.md`

问题锚点：
- `控制面板类.cpp` 的 `私有_计入线程信息项统计(...)` 读取 `线程.生命周期状态 == "故障" / "已退出" / "已停止" / "未启动"` 做统计分类。

当前判断：
- 这是控制面板展示统计，不是需求 / 任务 / 方法业务落账；但已有生命周期枚举值域，应改为结构化数值字段承载统计分类，文本只保留展示和缓存兼容读写。

计划修改：
- 在线程生命周期消息和控制面板线程信息项中增加生命周期状态数值字段，默认未设置。
- 自我线程发送生命周期消息时写入旧 / 新生命周期数值。
- 线程生命周期消息发送默认补全显示文本时同步补全新生命周期数值。
- 控制面板线程统计只读取数值和布尔位，不再比较生命周期文本。

已完成修改：
- `线程生命周期消息模块.ixx` 新增 `线程生命周期状态值_未设置`，并在 `结构_线程生命周期消息` / `结构_控制面板线程信息项` 中增加生命周期状态数值字段。
- 消息文件和线程信息表缓存开始读写生命周期状态数值。
- 消息应用到控制面板线程信息项时，优先使用 `新生命周期状态值` 更新 `生命周期状态值`；终态事件缺少数值时按事件类型写入终态数值。
- `自我线程模块.impl.cpp` 发送自我线程生命周期消息时同步写入旧 / 新生命周期数值。
- `控制面板类.cpp::私有_计入线程信息项统计(...)` 不再比较 `生命周期状态` 文本，改为读取 `是否故障 / 是否已退出 / 生命周期状态值 / 创建消息已到`。

验证：
- 精确检索 `生命周期状态 == / 生命周期状态 !=` 无残留。
- 剩余 `生命周期状态.empty()` 位于控制面板线程状态文本函数，只用于显示兜底。
- `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_thread_lifecycle_structured_status.tsv` 已生成；本切片未增删 string 字段，剩余 514 行，其中主键 / ID / 键 / 句柄类 330 行，非主键类 184 行。
- `git diff --check` 通过。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo` 通过，0 警告，0 错误。

禁止宣称：
- 不宣称自我苏醒完成。
- 不宣称初步成熟完成。
- 不宣称项目结构闭环全部通过。
