# 20260620 存在实例判定 helper Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_存在类逻辑纠偏计划_v0.1.md` 的 1.6，补带存在概念根上下文的实例存在判定 helper。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_存在类逻辑纠偏计划_v0.1.md
实施记录/20260620_场景候选召回迁移前置判定表.md
```

## 1. 问题锚点

```text
世界树类::是实例存在 当前依赖 世界树类::存在概念根。
场景候选召回迁入场景类时，场景类不应反向依赖世界树成员。
```

当前判断：

```text
存在概念 / 实例存在判定属于存在类只读 helper。
本切片补带存在概念根参数的 helper，不新增存在主信息字段，不创建或修改节点。
```

验证前预测：

```text
1. 存在类.h / 存在类.cpp 新增 是存在概念 / 是实例存在 helper。
2. 世界树类::是存在概念 / 是实例存在 可改为薄封装调用存在类 helper。
3. 函数说明文档生成退出码应为 0。
4. Debug x64 构建应通过。
```

## 2. 当前执行进展

已修改：

```text
1. 存在类.h / 存在类.cpp 新增 是存在概念(节点, 存在概念根) 和 是实例存在(节点, 存在概念根)。
2. 是存在概念 只读判断节点是否位于存在概念根子树内。
3. 是实例存在 只读判断节点是存在节点且不属于存在概念根子树。
4. 世界树类::是存在概念 / 是实例存在 改为薄封装调用存在类 helper。
```

已验证：

```text
rg -n "存在类::是存在概念|存在类::是实例存在|世界树类::是存在概念|世界树类::是实例存在|存在服务_\\.是存在概念|存在服务_\\.是实例存在" 存在类.h 存在类.cpp 世界树类.cpp 说明书/函数功能说明文档.md
结果：存在类新 helper 与世界树薄封装均有命中；函数说明文档包含新增条目。

python .\tools\生成函数功能说明文档.py
结果：exit=0，输出见 日志/20260620_存在实例判定helper_函数文档.log。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：exit=0，构建日志见 日志/20260620_存在实例判定helper_msbuild.log；0 警告。
```

当前判断：

```text
存在类 1.6 已闭合。
下一步返回 计划/20260620_场景类逻辑纠偏计划_v0.1.md 的 1.4 P5.2d：新增场景候选召回 DTO / helper。
```

禁止宣称：

```text
不宣称存在类逻辑纠偏完成。
不宣称场景候选召回已迁移完成。
不宣称场景类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
