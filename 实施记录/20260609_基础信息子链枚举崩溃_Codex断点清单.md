# 基础信息子链枚举崩溃 Codex 断点清单

更新时间：2026-06-09 16:46 +08:00

## 后续清理记录

```text
清理时间：2026-06-09 16:42 +08:00
清理依据：用户要求删除 基础信息类::枚举子节点 / 枚举子节点_按类型 中“防止基础信息子链异常枚举崩溃”的逻辑代码。
清理结果：
    已移除子节点归属检查、100000 次保护计数、`基础信息类/子链枚举异常` 日志函数及相关 include/import。
    `基础信息类::枚举子节点` 与 `基础信息类::枚举子节点_按类型` 接口仍保留；当前仍有世界树、特征、存在、场景、控制面板等调用点依赖这些枚举接口。
验证：
    git diff --check -- 基础信息类.h 基础信息类.cpp
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    构建 Exit 0，0 警告，0 错误。
提交：
    5156f16 移除基础信息子链枚举防崩保护
```

下方内容保留为 2026-06-09 16:17 当时的历史断点记录，不再代表当前代码状态。

## 当前切片

```text
问题来源：用户提供 debug error 调用栈。
崩溃点：基础信息类::取主信息<特征节点主信息类>(节点) 行 56。
上游调用：特征类::查找子特征_按类型 -> 外设模块 读取基础节点I64 -> 提交当前观察范围可观测单位存在对应事实。
目标：防止基础信息父节点子链中出现不属于当前主链的子节点时继续解引用该子节点主信息，避免 debug runtime 崩溃。
```

## 当前判断

```text
读取基础节点I64 已经能挡空宿主和空特征类型。
解析双目相机目标宿主 已经校验目标宿主是否属于当前世界树。
栈在 取主信息<特征节点主信息类> 崩溃，说明枚举出的子特征节点本身可能已经不是当前基础信息主链有效节点，或子链环/指针被破坏。
最小修复点应放在 基础信息类 子节点枚举：对每个子节点先确认仍属于当前主链，再访问主信息和下一个指针。
```

## 允许修改文件

```text
基础信息类.h
基础信息类.cpp
实施记录/20260609_基础信息子链枚举崩溃_Codex断点清单.md
```

## 禁止混入

```text
不改任务状态提交入口。
不改外设业务事实入账语义。
不把外设观察结果伪造成世界树真值、需求满足或动作动态。
不新增 mock、伪入口、伪样本。
不处理消息中间件运行生成物。
不宣称自我苏醒完成、初步成熟完成或内部循环整体完成。
```

## 本轮验证预测

```text
预测时间：2026-06-09 16:12 +08:00
预测 1：Debug x64 构建应通过；若仅出现 /EDITANDCONTINUE 与 /INCREMENTAL:NO 相关 LNK4075，不作为失败。
预测 2：短等待 self-check-log 应 Exit 0，且不会在基础信息类::取主信息<特征节点主信息类> 处触发 debug runtime 崩溃。
预测 3：若真实运行中出现基础信息子链异常，应记录 `基础信息类/子链枚举异常` 日志，而不是继续解引用异常子节点。
预测 4：该切片只防止无效子链解引用崩溃，不宣称修复所有执行桥逻辑错误或 D455 材料不足。
```

## 本轮实际结果

```text
实际时间：2026-06-09 16:17 +08:00

代码结果：
    基础信息类::枚举子节点 增加子节点归属检查和 100000 次保护计数。
    基础信息类::枚举子节点_按类型 增加同样的子节点归属检查和保护计数。
    检出子节点不属于当前基础信息主链或保护计数超限时，记录 `基础信息类/子链枚举异常` 并停止本轮子链枚举。
    该修复避免在异常子链上继续读取 `子节点->主信息` 或 `子节点->下`。

验证命令：
    git diff --check -- 基础信息类.h 基础信息类.cpp 实施记录/20260609_基础信息子链枚举崩溃_Codex断点清单.md
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=1000

验证结果：
    diff check Exit 0。
    Debug x64 构建 Exit 0；仅有 D455相机类.obj LNK4075 (/EDITANDCONTINUE 被 /INCREMENTAL:NO 忽略) 警告。
    第一次 self-check-log 因冷启动超过 120 秒工具超时，但进程随后自然退出，日志显示自检等待完成和正常停止。
    第二次 self-check-log 使用 300 秒超时，Exit 0。

日志证据：
    日志/鱼巢_run_20260609.低值g 中 2026-06-09 16:08 之后：
        `基础信息类/子链枚举异常` 0 条。
        `取主信息` 0 条。
        `执行桥逻辑错误` 0 条。
        `弹窗错误提示` 0 条。
        `命令行/自检日志等待完成` 2 条。
        `程序退出/守卫 | D455外设线程已停止` 2 条。
    当前无残留 `鱼巢.exe` 进程。
```

## 当前判断

```text
基础信息子链异常防崩切片已闭合。
本轮未自然复现用户给出的 `取主信息<特征节点主信息类>` debug runtime 栈，但已在真实运行中验证没有再触发该崩溃。
该切片不声明修复所有执行桥业务错误；15:40 的执行桥弹窗属于旧日志窗口。
运行生成物 控制面板.html 和 消息中间件/ 不纳入本切片提交。
```
