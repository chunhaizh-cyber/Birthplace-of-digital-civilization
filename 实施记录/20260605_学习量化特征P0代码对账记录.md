# 20260605_学习量化特征P0代码对账记录

## 1. 对账范围

```text
当前主动计划：
    计划/20260605_学习函数量化缺口与方法状态派生纠偏计划_v0.1.md

当前切片：
    P0：量化特征 / 宿主 / 读写桥 / 匹配能力代码对账。

本记录只做代码对账，不修改代码。
```

已检查：

```text
规范/规范目录.md
计划/计划索引.md
规范/学习相关本能函数规范20260505.md
规范/方法规范20260425.md
规范/任务筹办与执行边界规范20260512.md
规范/任务筹办按方法能力查找到就绪因果链规范20260513.md
规范/因果信息在学习中的作用规范20260516.md
计划/20260605_学习函数量化缺口与方法状态派生纠偏计划_v0.1.md
任务模块.执行.ixx
任务模块.筹办.ixx
任务模块.管理工作线程.ixx
自我动作实现模块.ixx
方法类.cpp
方法虚拟存在服务类.cpp
特征类.cpp
世界树类.cpp
```

## 2. 总结论

```text
P0 已完成代码对账。

当前代码已经有：
    方法动作数量 / 方法可执行入口数量 / 方法条件节点数量 / 方法结果节点数量 的执行桥；
    方法条件结果对数量 的结构计数函数；
    方法运行次数 / 方法成功次数 / 方法最近连续成功次数 的方法虚拟存在统计账；
    结果符合次数 / 结果不符合次数 的归并写账入口；
    任务筹办按方法结果能力填充候选集，并支持 I64 目标值和 > / >= 满足关系适配。

当前代码仍缺：
    方法条件结果对数量 的同名特征类型入口和派生需求输出；
    缺方法动作 -> 方法动作数量 的量化缺口输出；
    缺条件结果对 -> 方法条件结果对数量 的量化缺口输出；
    结果符合次数 / 结果不符合次数 的同一条件作用域宿主；
    新结果分支数量 / 新条件结果对候选数量 的正式计数特征；
    三个学习本能对量化目标的结果能力声明。

因此 P0 不阻塞；下一步应进入 P1a 小切片：
    先把任务筹办缺口映射改成量化目标。
```

## 3. 特征对账表

| 特征 | 当前宿主 / 读写桥 | 需求匹配 / 缺口生成 | 结果能力声明 | P0 判断 |
| --- | --- | --- | --- | --- |
| 方法动作数量 | `任务模块.执行.ixx:310` 有特征词；`任务模块.执行.ixx:539` 由 `主信息.有方法动作()` 读出 0/1。 | `任务模块.筹办.ixx:3472` 已接纳为方法结构补齐目标，但 `任务模块.执行.ixx:883` 当前输出缺口仍写 `方法动作状态`。 | `自我_定向学习` 和 `提交方法可执行入口` 当前声明 / 写入 `方法动作状态=1`，不是 `方法动作数量>0`。 | 读桥存在，缺口输出和结果能力需改量化。 |
| 方法可执行入口数量 | `任务模块.执行.ixx:328` 有特征词；`任务模块.执行.ixx:594` 由已注册本能入口读出 0/1。 | `任务模块.执行.ixx:902` 已输出 `方法可执行入口数量 0->1`；`任务模块.筹办.ixx:6023` 已将缺可执行入口映射到该特征。 | `提交方法可执行入口` 实际能让读桥变成 1，但 `自我动作实现模块.ixx:7175` 仍声明 `方法动作状态` 结果能力。 | 缺口输出可用，结果能力需补量化声明。 |
| 方法条件节点数量 | `任务模块.执行.ixx:272` 有特征词；`任务模块.执行.ixx:520` 统计方法条件节点。 | `任务模块.执行.ixx:921` 已输出 `方法条件节点数量 0->1`；`任务模块.筹办.ixx:3472` 接纳。 | 三个学习本能当前没有直接声明该量化结果能力。 | 缺口输出可用，后续 P2/P3 需确认承接方法。 |
| 方法结果节点数量 | `任务模块.执行.ixx:322` 有特征词；`任务模块.执行.ixx:563` 统计条件节点下的结果节点。 | `任务模块.执行.ixx:940` 已输出 `方法结果节点数量 0->1`；`任务模块.筹办.ixx:3472` 接纳。 | 三个学习本能当前没有直接声明该量化结果能力。 | 缺口输出可用，后续 P2/P3 需确认承接方法。 |
| 方法条件结果对数量 | `任务模块.执行.ixx:601` 有结构计数函数，但没有同名特征词入口；当前特征仍是 `方法条件结果配对状态`。 | `任务模块.执行.ixx:959` 当前输出 `方法条件结果配对状态 0/1->1`；`任务模块.筹办.ixx:6023` 也映射到配对状态。 | `自我_尝试学习` 当前声明 `方法条件结果配对状态=1` 和 `方法状态=可用`。 | P1a 必须新增同名特征词并改缺口输出。 |
| 方法运行次数 | `自我动作实现模块.ixx:584` 有特征词；`方法虚拟存在服务类.cpp:690` 初始化；`自我动作实现模块.ixx:3551` 读取、`3578` 写入。 | 任务筹办当前未把它作为方法结构缺口目标；更多是运行账证据。 | 学习本能未声明该结果能力。 | 可读写，但不是 P1a 主缺口。 |
| 方法成功次数 | `自我动作实现模块.ixx:590` 有特征词；`方法虚拟存在服务类.cpp:691` 初始化；`自我动作实现模块.ixx:3552` 读取、`3579` 写入。 | 任务筹办当前未把它作为方法结构缺口目标；更多是运行账证据。 | 学习本能未声明该结果能力。 | 可读写，但不是第一版好用硬目标。 |
| 方法最近连续成功次数 | `自我动作实现模块.ixx:596` 有特征词；`方法虚拟存在服务类.cpp:692` 初始化；`自我动作实现模块.ixx:9000` 读取目标方法运行账；`9506` 写入输出结果。 | `任务模块.管理工作线程.ixx:1186` 有执行输出特征；`1332` / `1764` 可识别为学习承接缺口。 | `自我_练习` 当前仍声明 `方法状态=好用`，不是 `方法最近连续成功次数>=4`。 | 可搬运，但 P2 需补结果能力。 |
| 结果符合次数 | `自我动作实现模块.ixx:560` 有特征词；`自我动作实现模块.ixx:3208` 在条件结果对归并时增加。 | 任务筹办 / 工作线程当前未识别为学习承接缺口。 | 学习本能未声明该结果能力。 | 写账存在，但宿主当前是方法虚拟存在，不足以表达严格“同一条件”。 |
| 结果不符合次数 | `自我动作实现模块.ixx:566` 有特征词；`自我动作实现模块.ixx:3210` 在同条件新结果分支时增加。 | 任务筹办 / 工作线程当前未识别为学习承接缺口。 | 学习本能未声明该结果能力。 | 写账存在，但宿主当前是方法虚拟存在，不足以表达严格“同一条件”。 |
| 新结果分支数量 / 新条件结果对候选数量 | 未找到正式同名特征；仅有 `自我类.初始化.cpp:270` 的 `缺口_条件结果对候选` 历史/缺口特征。 | 任务筹办 / 工作线程未识别。 | 学习本能未声明。 | 正式计数特征缺失；后续不能直接按该目标查找方法。 |

## 4. 结果能力匹配对账

```text
任务筹办候选集填充：
    任务模块.筹办.ixx:4623 填充候选集_按结果能力；
    任务模块.筹办.ixx:4692 调用 方法结果能力命中任务需求。

I64 目标值和满足关系适配：
    任务模块.筹办.ixx:3680 方法结果项目标值适配需求；
    任务模块.筹办.ixx:3747 方法结果能力命中任务需求；
    任务模块.筹办.ixx:3794 方法结果能力命中精度。

结论：
    > / >= 的目标值适配能力存在；
    当前主要问题不是匹配函数完全缺失，
    而是学习本能结果能力仍声明为粗状态，无法被量化缺口稳定命中。
```

## 5. 当前堵点分类

```text
可在 P1a 解决：
    缺方法动作 输出从 方法动作状态 改为 方法动作数量；
    缺条件结果对 输出从 方法条件结果配对状态 改为 方法条件结果对数量；
    任务结构补齐目标接纳 方法条件结果对数量。

应放入 P1b：
    工作线程派生消息对 方法条件结果对数量 / 方法动作数量 的无损识别和状况推导。

应放入 P1c：
    自我线程学习任务去重键扩展，避免同一目标方法不同量化缺口互相吞并。

应放入 P2/P3：
    自我_定向学习 / 自我_尝试学习 / 自我_练习 的量化结果能力声明和输出缺口纠偏。

应放入 P4：
    结果符合次数 / 结果不符合次数 的同一条件宿主；
    新结果分支数量特征；
    方法状态被动派生模板。
```

## 6. 下一步

```text
进入 P1a：
    计划/20260605_P1a任务筹办缺口映射量化目标小切片方案_v0.1.md
```
