# 20260624 P11j live 无调用复扫 Codex 断点清单

## 0. 恢复摘要

当前切片：P11j live 无调用复扫。

权威路由：`计划/计划索引.md` 第 1 章当前主动队列；P11 当前优先清理规则见 `计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md`。

当前边界：

```text
1. 只删除 live 复扫确认没有直接调用，且未命中构造 / 析构隐式调用、operator、virtual / override、入口 / 回调 / 导出、注册表、消息分发、模板实例、宏拼接、公开合同或项目结构读写证据的函数。
2. 避开当前已有未提交脏改文件，避免混入用户改动或上一切片未闭合改动。
3. 删除 / 改名函数后必须重新运行 python .\tools\生成函数功能说明文档.py，并运行 Debug x64 构建。
```

本轮已确认：

```text
已重新生成 说明书/函数功能说明文档.md。
已生成候选表：实施记录/20260624_P11j_live_无调用候选重扫.tsv。
候选表统计：1794 行；call_like_count=0 为 44 行；call_like_count=1 为 1268 行；call_like_count=2 为 482 行。
早先一次全函数 regex 候选生成曾出现全部 call_like_count=0 的异常结果，已判定不可作为删除证据，不使用。
当前 git status 显示多个 .cpp 已有修改；P11j 第一轮复核不触碰这些脏文件。
```

当前候选复核入口：

```text
优先复核未脏文件中的 call_like_count=0 候选。
明显排除：构造 / 析构、operator、模板/基础公开 API、窗口过程、线程入口、控制面板入口、公开合同、项目结构读写证据。
第一批候选：语言类_二次特征表达.cpp、自我线程模块.消息处理器.ixx、自我线程模块.消息批次执行器.ixx、自我线程模块.impl.cpp 中未命中当前脏文件的普通 helper。
```

禁止宣称：

```text
不宣称 P11 完成。
不宣称项目无无效函数。
不宣称自我苏醒完成或初步成熟完成。
```

## 1. 本轮闭合记录

删除范围：

```text
自我线程模块.impl.cpp：
    删除 P11j live 复扫确认无调用的旧安全风险树缺口派生链、旧当前场景特征值逻辑组织子需求链、旧线程状态落账 / 上报链及其二级残留 helper，共 31 个函数。

任务模块.管理界面线程.impl.cpp：
    删除无调用的旧任务调度队列消费链 helper 3 个：
        私有_消费任务调度队列_按请求
        私有_消费任务调度队列
        私有_弹出完成队列项_按工作项
```

刷新与验证：

```text
python .\tools\生成函数功能说明文档.py：
    最终函数条目：7149。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo：
    最终构建通过，0 警告，0 错误。

git diff --check -- 自我线程模块.impl.cpp 任务模块.管理界面线程.impl.cpp 说明书\函数功能说明文档.md：
    通过。
```

复扫证据：

```text
最终候选表：
    实施记录/20260624_P11j_live_当前源码未限定名候选_after_round6.tsv

最终统计：
    定义数：6452。
    clean_candidates=0。

说明：
    当前“未限定名出现次数=1、非头文件、非公开成员、非入口线程、非当前脏文件、无风险标记”的干净候选已清空。
    剩余低引用 / 低出现次数函数仍可能属于构造析构、operator、header/inline API、公开成员、入口回调、线程入口、公开合同或项目结构读写证据，不能按本轮直接删除规则继续批量删除。
```
