# 因果类功能函数补完 Codex 断点清单

日期：2026-06-19

## 0. 恢复摘要

当前主动计划：

```text
计划/20260619_因果类功能函数补完计划_v0.1.md
```

当前切片：

```text
P0 函数清单和外部绕过点冻结 + P1 节点解析和证据状态函数。
```

已读依据：

```text
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260619_因果类功能函数补完计划_v0.1.md
规范/因果信息用途与用法规范20260516.md
规范/因果补完流程规范20260516.md
规范/状态转换因果树与目标投影层级规范20260518.md
规范/需求临时因果视图展开规范20260526.md
```

当前判断：

```text
1. P1 不恢复因果 wrapper 结构体，不新增因果主信息字段。
2. P1 只补结构化读取函数：因果节点解析、按主键解析、证据状态 / 计数读取。
3. 日志、控制面板、SQL 和显示层不作为正式补完目标。
```

下一步：

```text
1. 在 因果类.h/.cpp 增加 P1 函数。
2. 迁移 任务模块.筹办.ixx、自我线程模块.impl.cpp、需求类.cpp 中对应解析和证据状态读取。
3. 运行 python .\tools\生成函数功能说明文档.py。
4. 运行 Debug x64 构建。
```

禁止宣称：

```text
不宣称因果类功能补完完成。
不宣称所有因果操作已统一走因果类。
不宣称因果解释 / 预测 / 反推 / 审查 / 验收全部实现。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 1. P0 函数清单冻结

函数文档检索：

```powershell
Select-String -Path 说明书/函数功能说明文档.md -Pattern "因果节点|主果|证据状态|证据计数|因果动作来源|稳定度|解析因果|证据足够" | Select-Object -First 80
```

已确认相关现有函数：

```text
因果类::查询动作相关因果
因果类::查询条件相关因果
因果类::查询结果相关因果
任务模块.筹办.ixx::因果证据足够_筹办查询
自我线程模块.impl.cpp::私有_枚举主果命中因果信息_线程侧
因果类.cpp::私有_融合主果状态迁移区间_因果
因果类.cpp::私有_补入因果果侧模板_因果
任务模块.筹办.ixx::解析因果信息节点_筹办
基础信息主信息类.h::因果主信息类::计算稳定度
```

P1 未发现已存在的 `因果类::解析因果节点`、`因果类::按主键解析因果节点`、`因果类::读取因果证据状态` 同功能公开函数。

## 2. P0 外部绕过点冻结

检索命令：

```powershell
rg -n "取因果主信息\(|取模板主信息\(|枚举全部因果\(|证据动态样本|主果比较模板|因方法模板|因动作语义键|因动作名称|已验证|计算稳定度\(" -S 任务模块.筹办.ixx 自我线程模块.impl.cpp 需求类.cpp 因果类.h 因果类.cpp
```

P1 直接迁移范围：

```text
任务模块.筹办.ixx:1394 因果证据足够_筹办查询 直接读取 因果主信息类 计数字段。
任务模块.筹办.ixx:1431-1432 构造临时因果路径项读取稳定度和已验证。
任务模块.筹办.ixx:4452 解析因果信息节点_筹办 通过 取因果主信息 判断因果节点。
自我线程模块.impl.cpp:2608 派生需求来源因果解析使用基础信息主信息读取。
自我线程模块.impl.cpp:6704-6711 构造安全风险树缺口派生消息直接读取稳定度、证据动态样本数、已验证。
需求类.cpp:1120-1123 来源因果主键解析后直接取因果主信息。
```

P2 以后迁移范围：

```text
任务模块.筹办.ixx:2024-2117 主果方向扫描。
任务模块.筹办.ixx:2146-2154 安全因果来源因果查找。
自我线程模块.impl.cpp:6109-6117 安全因果来源因果查找。
自我线程模块.impl.cpp:6144-6150 主果命中枚举。
任务模块.筹办.ixx:1445 条件比较模板 / 证据动态样本反推条件目标状态。
任务模块.筹办.ixx:1687 方法就绪缺口读取因方法模板。
需求类.cpp:1127-1134 来源因果动作来源判断。
```

## 3. P0 当前证据状态

```text
git status --short --untracked-files=no：当前无已跟踪脏改。
```

## 4. P1 节点解析和证据状态函数实施结果

已改文件：

```text
因果类.h
因果类.cpp
任务模块.筹办.ixx
自我线程模块.impl.cpp
需求类.cpp
说明书/函数功能说明文档.md
实施记录/20260619_因果类功能函数补完_Codex断点清单.md
```

新增因果类功能函数：

```text
因果类::是因果节点
因果类::解析因果节点
因果类::按主键解析因果节点
因果类::读取因果证据状态
```

迁移结果：

```text
1. 任务模块.筹办.ixx 的本地因果节点解析改为调用 因果类::解析因果节点。
2. 任务模块.筹办.ixx 的证据足够判断、临时因果路径项稳定度和已验证读取改为调用 因果类::读取因果证据状态。
3. 自我线程模块.impl.cpp 的派生需求来源因果解析改为先通过 因果类::解析因果节点 校验节点身份。
4. 自我线程模块.impl.cpp 的安全因果风险树缺口消息稳定度、样本数、初始模板和后天验证读取改为调用 因果类::读取因果证据状态。
5. 需求类.cpp 的来源因果主键解析改为调用 因果类::按主键解析因果节点。
```

保留到后续切片的直接主信息读取：

```text
任务模块.筹办.ixx:2043 主果方向扫描，归入 P2。
任务模块.筹办.ixx:2158 安全因果来源因果查找，归入 P2。
自我线程模块.impl.cpp:6114 安全因果来源因果查找，归入 P2。
自我线程模块.impl.cpp:6146 主果命中枚举，归入 P2。
需求类.cpp:1121 来源因果动作来源判断，归入 P4。
```

已运行命令：

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> .\日志\20260619_因果类P1功能函数补完_msbuild.log; $LASTEXITCODE
```

验证证据：

```text
函数说明文档生成成功：源码文件 128，函数条目 6844。
Debug x64 构建退出码：0。
构建日志：日志/20260619_因果类P1功能函数补完_msbuild.log。
```

当前判断：

```text
P1 节点解析和证据状态读取切片已闭合。
因果类功能补完总计划仍未完成，下一步应进入 P2 主果方向 / 因果命中查询函数补完。
```
