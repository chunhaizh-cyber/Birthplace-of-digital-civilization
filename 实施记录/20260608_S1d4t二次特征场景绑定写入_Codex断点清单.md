# 20260608 S1d4t 二次特征场景绑定写入 Codex 断点清单

## 当前切片

- 主动计划：`计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- 执行方案：`计划/20260607_S1d4t二次特征场景绑定写入内部阶段定位与瘦身方案_v0.1.md`
- 切片：S1d-4t 二次特征场景绑定写入内部阶段定位 / 瘦身。

## 依据文件

- `AGENTS.md`
- `规范/规范目录.md`
- `规范/方法动作动态硬规则规范20260512.md`
- `规范/二次特征规范20260520.md`
- `规范/远程执行通道与断点续跑规范20260524.md`
- `计划/计划索引.md`
- `计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- `计划/20260607_S1d4s分步语义绑定剩余阶段定位与瘦身方案_v0.1.md`
- `实施记录/20260608_S1d4s分步语义绑定剩余阶段定位_Codex断点清单.md`

## 当前判断

- S1d-4s 已闭合并提交：`596bf53`。
- 最新运行线程 T16620：
  - `动态二次特征分步语义绑定/内部阶段耗时` 样本 6 条。
  - 最慢阶段全部为 `场景绑定写入`。
  - `场景绑定写入` 平均约 89238us，最大约 176646us。
- `场景绑定写入` 对应 `二次特征类::绑定场景`。
- `绑定场景` 当前会写所属场景引用，并追加 `关系索引` 和 `二次特征索引`。
- 本切片不能删除场景索引账，只能优化重复查重、重复主键读取、锁内工作或已存在快路径。

## 构建 / 运行前预测

- Debug x64 构建通过。
- 真实运行退出码 0。
- 日志继续出现 `动态二次特征分步语义绑定/内部阶段耗时`。
- 日志出现 `二次特征绑定场景/内部阶段耗时`，或在 5ms 阈值下无样本但高层分步结果仍可统计。
- 日志保留 `任务状态动作动态=<动态主键>`。
- 不出现 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`。
- 不出现旧路径锚点 `准备本能调用`、`执行本能方法`、`任务域本能输入包`。

## 允许修改

- `二次特征类.cpp`
- `计划/20260607_S1d4t二次特征场景绑定写入内部阶段定位与瘦身方案_v0.1.md`
- `计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- `计划/计划索引.md`
- `实施记录/20260608_S1d4t二次特征场景绑定写入_Codex断点清单.md`

## 已改文件

- `二次特征类.cpp`
- `计划/20260607_S1d4t二次特征场景绑定写入内部阶段定位与瘦身方案_v0.1.md`
- `实施记录/20260608_S1d4t二次特征场景绑定写入_Codex断点清单.md`

## 已完成修改

- `二次特征类.cpp` 已新增 `二次特征绑定场景/内部阶段耗时`。
- `绑定场景` 已拆分主信息读取、场景主信息读取、所属场景引用写入、索引追加。
- `私有_追加二次特征索引` 已拆分节点引用准备、场景索引锁等待、关系索引查重、关系索引追加、二次特征索引查重、二次特征索引追加。
- 索引查重已改为一次性取得节点主键并复用，避免在每个索引项比较时重复读取节点主键。
- 本次修改保留所属场景写入、`关系索引` 和 `二次特征索引`。

## 下一步

1. 更新总性能计划和计划索引。
2. 提交 S1d-4t 闭合切片。
3. 进入 S1d-4u：条件候选收集内部阶段定位 / 瘦身。

## 已运行命令

```text
git diff --check -- 二次特征类.cpp 计划/计划索引.md 计划/20260607_S1d4t二次特征场景绑定写入内部阶段定位与瘦身方案_v0.1.md 实施记录/20260608_S1d4t二次特征场景绑定写入_Codex断点清单.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
```

## 验证结果

```text
git diff --check：
    退出码 0；
    仅有 LF/CRLF 工作区转换警告。

Debug x64 构建：
    退出码 0；
    0 error；
    1 个 LNK4075 常规警告：`/EDITANDCONTINUE` 因 `/INCREMENTAL:NO` 被忽略。

真实运行：
    退出码 0；
    最新启动行：`2026-06-08 11:17:25.511 [T29964] 程序启动`；
    末尾 `D455 打开失败: No device connected`，属于当前机器缺真实 D455 的硬件条件，不作为本切片失败。
```

## 关键日志证据

```text
本轮窗口统计：
    `动态二次特征分步结果/内部阶段耗时` 5 条；
    `动态二次特征分步语义绑定/内部阶段耗时` 5 条；
    `二次特征绑定场景/内部阶段耗时` 0 条；
    `动态二次特征生成/内部阶段耗时` 19 条；
    `任务状态动作动态=` 55 条。

负向锚点：
    `任务状态被动提交/逻辑错误` 0；
    `任务状态动作动态逻辑错误` 0；
    `准备本能调用` 0；
    `执行本能方法` 0；
    `任务域本能输入包` 0。

分步结果：
    样本 5 条；
    最慢阶段分布 `分步值写入=3`、`分步签名查找创建=2`；
    平均总耗时约 129704us；
    `分步语义绑定` 平均约 12217us；
    `分步值写入` 平均约 50029us；
    `分步签名查找创建` 平均约 50774us。

分步语义绑定：
    样本 5 条；
    最慢阶段分布 `场景绑定写入=4`、`概念名称取词=1`；
    平均总耗时约 12217us；
    `场景绑定写入` 平均约 6366us，最大约 11423us；
    `概念名称取词` 平均约 4392us；
    `生成语义设置` 平均约 1273us。

动态二次特征生成：
    样本 19 条；
    最慢阶段分布 `条件候选收集=16`、`命中回写因果实例=2`、`分步结果生成=1`；
    平均总耗时约 441874us；
    `条件候选收集` 平均约 225225us；
    `分步结果生成` 平均约 134553us。
```

## 当前结论

- S1d-4t 已闭合。
- `绑定场景` 单次未超过 5000us 慢采样阈值；语义绑定平均已从 S1d-4s 约 93866us 降到约 12217us。
- 分步结果慢点转到 `分步值写入` 和 `分步签名查找创建`，动态二次特征生成整体慢点转到 `条件候选收集`。
- 下一片应进入 S1d-4u：条件候选收集内部阶段定位 / 瘦身。

## 禁止宣称

- 不宣称自我苏醒完成。
- 不宣称初步成熟完成。
- 不把定位日志当成性能优化完成。
- 不跳过二次特征所属场景或场景索引账。
