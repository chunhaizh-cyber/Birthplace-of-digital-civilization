# 20260605_外设四本能方法P0b结果能力与正式目标特征对账记录

## 切片定位

```text
来源计划：计划/20260605_外设四本能方法需求树生成因果链修订计划_v0.1.md
当前切片：P0b 结果能力与正式目标特征对账
目标：
    确认观察、识别、扫描、跟踪当前结果能力是否可映射到存在、特征、二次特征和状态动态。
    确认是否仍以工程通信容器、等待项、提交包摘要或材料页作为正式结果口径。
```

## 依据

```text
AGENTS.md
规范/规范目录.md
计划/计划索引.md
计划/20260605_外设四本能方法需求树生成因果链修订计划_v0.1.md
规范/方法规范20260425.md
规范/任务筹办按方法能力查找到就绪因果链规范20260513.md
规范/外设观察特征与自我场景认知本能方法分层规范20260525.md
规范/观察像素簇与存在候选分层规范20260527.md
规范/当前场景特征值明确标准规范20260602.md
自我动作实现.外设模块.ixx
本能方法类.h
本能方法类.cpp
```

关键口径：

```text
任务筹办按 方法结果能力 -> 目标特征类型 命中候选方法。
正式目标不能写成方法调用需求，也不能把外设报告、材料页、提交包或等待项当作世界事实。
识别只产出对应 / 归属，不产出被观察存在状态动态。
扫描首次只写发现事实，后续有历史基准才写变化动态。
跟踪只盯指定存在，不能替代全场扫描。
```

## 结果能力写入机制

```text
自我动作实现.外设模块.ixx：
    确保方法结果能力I64 会写入 方法首节点.能力.结果包.结果项集；
    结果项携带：
        关系目标特征类型
        目标值
        允许方向
    因此只要目标特征类型和目标值足够正式，任务筹办可以按结果能力命中。
```

## 观察方法族

```text
当前已声明 / 写入的结果能力：
    双目相机_取得当前观察特征帧：
        当前观察特征帧 = 1
        当前观察特征帧取得状态 = 1
        观察帧质量诊断摘要 = 1
        观察帧组状态 = 1
        融合深度毫米结构状态 = 1

    自我_建立观察帧存在：
        当前观察帧存在 = 1
        当前观察帧存在状态 = 1
        观察帧容器状态 = 1

    自我_建立观察帧到自我场景接口：
        观察帧到自我场景接口 = 1

    自我_建立像素观察事实索引：
        像素观察事实索引 = 1
        像素覆盖状态 = 1
        坐标回查可用状态 = 1
        帧内编号回查可用状态 = 1

    自我_承接像素特征值：
        像素特征值承接状态 = 1

    自我_校验观察事实完备性：
        观察事实完备状态 = 1

    自我_评估观察帧质量：
        质量可用状态 = 1

    自我_判断基础观察事实可用性：
        基础观察事实可用状态 = 1
```

对账结论：

```text
可映射：
    当前观察特征帧、观察帧存在、像素观察事实索引、像素特征值承接状态、观察事实完备状态、质量可用状态、基础观察事实可用状态，都属于存在 / 特征 / 状态口径。

容器化残留：
    观察帧容器状态 仍作为 自我_建立观察帧存在 的结果能力。
    它适合做工程容器状态或兼容证据，不适合作为需求树正式目标特征。

P1 / P2 处理建议：
    不急删观察帧容器状态词条；
    但需求树目标应优先命中 当前观察帧存在 / 当前观察帧存在状态 / 当前观察特征帧取得状态 / 基础观察事实可用状态；
    后续观察收口时，将 观察帧容器状态 从正式结果目标降级为兼容或回查字段。
```

## 识别方法

```text
方法：自我_识别外设观察材料

当前结果能力：
    外设观察存在候选确认方案取得状态 = 1
    当前观察范围可观测单位存在稳定对应事实取得状态 = 1
    扫描可启动状态 = 1

执行侧实际写入：
    外设观察存在候选确认状态
    外设观察存在候选确认方案取得状态
    外设观察存在候选确认新建 / 合并 / 冲突 / 证据不足 / 可提交数量
    当前观察范围有效可观测单位数量
    已稳定对应存在可观测单位数量
    多重对应冲突可观测单位数量
    稳定对应存在数量
    当前观察范围可观测单位存在稳定对应事实取得状态
    稳定对应存在数量明确状态
    扫描可启动状态
    动作动态
```

对账结论：

```text
已去掉旧提交包组织函数壳，执行日志明确写 不组织外设提交包=1。
结果能力已能被任务筹办按正式状态特征命中。
识别没有写被观察存在变化动态，符合边界。

不足：
    计划要求的 同一性二次特征、自我归属状态、归属目标 仍未显式成为结果能力。
    当前用 外设观察存在候选确认方案 / 稳定对应事实 近似承接归属链，够做 P0 过渡，但不够作为 P3 最终口径。

P1 / P3 处理建议：
    复用或新增：
        外设观察存在_自我存在同一性状态
        同一性评分
        自我归属状态
        归属目标
    并把这些作为 自我_识别外设观察材料 的正式结果能力。
```

## 扫描方法

```text
方法：自我_扫描已识别区域

当前结果能力：
    观察存在发现状态 = 已发现
    观察存在变化结果 = 已生成
    已识别区域扫描入账状态 = 已生成

执行侧实际写入：
    当前观察存在
    历史观察存在
    观察存在发现状态
    已识别区域扫描入账状态
    观察存在变化结果
    观察变化事件集合
    存在当前特征状态
    观察存在发现来源动作动态 或 存在特征变化动态
    动作动态
```

对账结论：

```text
扫描已区分首次扫描和有历史扫描：
    首次扫描写发现事实，不写变化动态；
    有历史存在时才生成观察存在变化结果 / 观察变化事件。
结果能力可映射到发现事实、变化结果、扫描状态和状态动态。

不足：
    计划要求的 已归属存在当前特征_基准特征差异二次特征、值域冲突动态、下一轮扫描基准、最近扫描时间特征 未显式成为结果能力。
    当前结果能力足够承接 P0 的扫描发现 / 变化过渡，但不够作为 P4 完整扫描契约。

P1 / P4 处理建议：
    复用或新增扫描基准、值域冲突、差异二次特征和最近扫描时间相关特征；
    保持首次扫描不生成变化动态。
```

## 跟踪方法

```text
方法：自我_跟踪指定存在

当前结果能力：
    目标存在跟踪状态 = 已更新
    目标观察窗口状态 = 已更新

执行侧实际写入：
    目标存在
    目标存在跟踪状态
    目标观察窗口状态
    目标存在特征变化动态
    动作动态
```

对账结论：

```text
结果能力可映射到指定目标的跟踪状态和窗口状态。
执行日志明确写 跟踪不替代全场扫描=1。

不足：
    计划要求的 预测误差二次特征、区间迁移二次特征、丢失动态、重捕获动态、预测偏差超限动态 未显式成为结果能力。
    目标存在跟踪状态=已更新 只能证明跟踪方法有输出，不能证明动态连续、合法、稳定。

P1 / P5 处理建议：
    增加或复用跟踪值域区间、预测区间、预测误差、区间迁移、丢失、重捕获相关特征；
    继续禁止把跟踪证据当全场景扫描证据。
```

## 四个提交入口

```text
提交_当前观察范围可观测单位存在对应事实：
    结果能力：
        当前观察范围可观测单位存在对应事实提交状态 = 已提交
        当前观察范围有效可观测单位计数事实取得状态 = 1
        当前观察范围可观测单位存在稳定对应事实取得状态 = 1
        稳定对应存在数量明确状态 = 1
        当前观察范围可观测单位存在对应闭合状态 = 1
        扫描可启动状态 = 1
        当前场景特征值明确状态 = 1
    输入已从旧 外设识别结果场景状态 改为 外设观察存在候选确认状态 / 方案取得状态。

提交_观察存在发现事实：
    结果能力：
        观察存在发现事实提交状态 = 已提交
        观察存在发现状态 = 已发现
    输入已从旧 外设扫描结果场景状态 改为 观察存在发现状态 / 已识别区域扫描入账状态。

提交_观察存在特征值变化事实：
    结果能力：
        观察存在特征值变化事实提交状态 = 已提交
    输入已从旧 外设扫描结果场景状态 改为 已识别区域扫描入账状态 + 观察存在变化结果。

提交_指定存在跟踪事实：
    结果能力：
        指定存在跟踪事实提交状态 = 已提交
    输入已从旧 外设跟踪结果场景状态 改为 目标存在跟踪状态 / 目标观察窗口状态。
```

对账结论：

```text
四个提交入口已经改为消费正式结果状态，不再以 外设提交包组织状态_已组织 为闸门。
提交入口写入世界事实、状态或动态，不直接写安全 / 服务结论。

残留：
    提交_当前观察范围可观测单位存在对应事实 仍兼容读取 外设提交包候选事实数量。
    外设观察报告ID 仍作为来源回查 ID 写入多处结果宿主。

判断：
    外设观察报告ID 可作为材料回查 / 日志来源保留；
    外设提交包候选事实数量 不应作为正式目标特征，P0d / P1 应继续确认并替换为 外设观察存在候选确认数量链。
```

## 容器化残留总表

```text
仍存在但当前未作为三方法正式结果能力的词条：
    外设提交包ID
    外设提交包类型
    外设提交包组织状态
    外设提交包材料可回查状态
    外设提交包过期状态
    外设提交包generation明确状态
    外设提交包可比较状态
    外设提交包缺失材料数量
    外设提交包质量问题数量
    外设提交包材料句柄数量
    外设提交包特征值条目数量
    外设识别 / 扫描 / 跟踪结果场景状态
    外设提交包组织结果

当前判断：
    这些词条可暂作历史兼容、材料回查或后续清理对象；
    不得作为需求树正式目标、世界事实成立条件或因果账本节点。
```

## P0b 结论

```text
P0b 可关闭。

已经满足：
    识别、扫描、跟踪和四个提交入口具备可被结果能力查找命中的正式状态特征；
    旧外设提交包组织函数本体已删除；
    三方法执行路径不再以提交包组织摘要作为正式结果；
    四个提交入口不再以 外设提交包组织状态_已组织 作为闸门。

尚未满足，需后续切片处理：
    观察方法族仍有 观察帧容器状态 作为结果能力；
    识别缺显式同一性 / 归属二次特征；
    扫描缺显式基准差异 / 值域冲突 / 最近扫描时间 / 下一轮基准；
    跟踪缺显式预测误差 / 区间迁移 / 丢失 / 重捕获；
    当前场景特征值明确状态 写入时需要继续绑定 当前观察范围 口径，避免裸解释成全场景；
    提交入口中的 外设提交包候选事实数量 兼容读需要在 P0d / P1 继续去容器化。

下一步：
    进入 P0c 条件节点、供料要求与等待项对账。
```

## 验证

```text
本切片只做代码对账和记录更新。
未修改业务代码。
未运行构建。
未真实运行鱼巢.exe。
不宣称真实业务链路已通过。
```
