# 上下文压缩控制 Codex 断点清单

日期：2026-06-12

## 当前切片

```text
切片编号：CTX-20260612-01
目标：降低 remote compact / 上下文压缩错误的触发频率，把恢复入口从“全文必读”收束为“当前主动队列 + 最近断点 + 按需展开”。
依据文件：
    规范/禁止性规范20260611.md
    规范/规范目录.md
    规范/远程执行通道与断点续跑规范20260524.md
    AGENTS.md
    计划/计划索引.md
允许修改范围：
    AGENTS.md
    计划/计划索引.md
    实施记录/20260612_计划索引最近闭合历史摘录归档.md
    实施记录/20260612_上下文压缩控制_Codex断点清单.md
禁止修改范围：
    业务代码
    任务状态、需求满足、方法选择、动作动态、世界树、因果账本、价值结算
    当前主动主线的业务判断和验收结论
```

## 已完成事项

```text
1. AGENTS.md 的上下文与工具输出控制已补充限载读取规则：
    - 计划索引默认只读第 0 章和第 1 章当前主动队列。
    - 规范目录默认先读一级目录和相关条目。
    - AGENTS.md 恢复执行时优先读必要章节，不做全文审计。
    - 单次工具输出超过 200 行或文件超过 20KB 时，先落文件再提关键证据。
2. 计划/计划索引.md 已增加默认恢复读取范围。
3. 计划/计划索引.md 的 `### 1.1 最近闭合历史摘录` 已移出默认必读索引。
4. 历史摘录已归档到 `实施记录/20260612_计划索引最近闭合历史摘录归档.md`，供按需小段读取。
```

## 验证记录

```text
验证命令：
    git diff --check -- AGENTS.md 计划/计划索引.md 实施记录/20260612_计划索引最近闭合历史摘录归档.md 实施记录/20260612_上下文压缩控制_Codex断点清单.md
退出码：
    0
关键结果：
    未发现 whitespace error；仅有 Git 提示 AGENTS.md 和 计划/计划索引.md 下次被 Git 触碰时 LF 可能转 CRLF。
体量变化：
    计划/计划索引.md 当前约 26KB。
    实施记录/20260612_计划索引最近闭合历史摘录归档.md 当前约 24KB。
构建：
    未运行；本切片只改代理说明、计划路由和实施记录，不改 C++ 代码。
真实运行：
    未运行；本切片不改变鱼巢运行逻辑。
```

## 当前判断

```text
remote compact / stream disconnected 仍按执行通道中断处理，不记成项目实现、业务验证或计划逻辑失败。
本切片的控制点是减少恢复时的默认输入体量和工具输出体量，不改变追根因规则本身。
```

## 追加控制点：问题触发切片

```text
时间：2026-06-12 20:25
触发原因：
    上一轮在“父组成功结果消息落账 / 执行尝试对账纠偏”定位中，连续排查多个候选路径；
    第一个独立问题未及时写入断点，第二个独立问题出现后仍继续携带过多检索信息；
    随后 remote compact / stream disconnected 中断，导致未沉淀的定位上下文丢失。

新增控制规则：
    发现第一个独立问题后，先记录问题锚点、证据行、已排除路径、当前判断和下一步验证。
    发现第二个独立问题时，先把第一个问题收束为独立切片或待办项，再切换到第二问题。
    切换切片前清理检索信息，只保留必要文件、函数、日志锚点和验证命令。

已落地文件：
    AGENTS.md
    C:\Users\zhchh\.codex\skills\yu-chao-execute\SKILL.md

边界：
    本追加只改变 Codex 执行规则，不改变鱼巢业务代码、任务状态、需求满足、方法选择或验证结论。
```

## 下一步

```text
1. 继续执行业务主线时，从计划索引第 0 章和第 1 章恢复。
2. 只读取当前主动队列指向的断点清单。
3. 若需要旧闭合证据，再按标题读取 `实施记录/20260612_计划索引最近闭合历史摘录归档.md` 的对应小段。
```

## 禁止宣称项

```text
不宣称上下文压缩错误已彻底消除。
不宣称任何业务链路、构建、真实运行或内部循环因此通过。
不把远程执行通道错误解释为鱼巢业务失败。
不把本次文档控制改动扩展为任务、需求、方法、世界树或因果账本修改。
```
