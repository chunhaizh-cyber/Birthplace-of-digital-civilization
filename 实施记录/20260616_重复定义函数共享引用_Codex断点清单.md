# 20260616 重复定义函数共享引用 Codex断点清单

## 0. 恢复摘要

当前切片：检查重复定义函数并按共享函数规则引用。

已读取依据：
- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `计划/计划索引.md` 第 0 章与第 1 章当前主动队列
- `说明书/函数功能说明文档.md` 中“语素入口”相关条目

已确认证据：
- `说明书/函数功能说明文档.md` 已列出多处 `语素入口相同` / `语素入口同一`，但 `全局共享函数类.ixx` 尚无对应共享实现。
- 纯重复体范围：指针相同返回真、任一空指针返回假、否则比较 `获取主键()`。
- 非本次修改范围：带空主键保护的比较变体，以及 `自我类.cpp` 中明确只做指针比较的热路径变体。

下一步：
- 在 `全局共享函数类.ixx` 新增共享函数。
- 将纯重复局部函数改为调用共享函数。
- 重新生成 `说明书/函数功能说明文档.md`，构建验证后提交本切片。

已执行：
- 新增 `全局_语素入口同一` 作为无主信息类参与的语素入口共享比较函数。
- 将已确认等价的局部重复比较函数改为引用 `全局_语素入口同一`。
- 保留 `任务类.cpp` 空主键保护版和 `自我类.cpp` 指针限定版，未误改非等价变体。
- 已重生成 `说明书/函数功能说明文档.md`；文档已有 `全局_语素入口同一` 条目。

验证：
- `python .\tools\生成函数功能说明文档.py --write-comments`：成功；源码文件 128，函数条目 7193，新增功能注释 0。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`：成功；0 警告，0 错误。
- `git diff --check`：无空白错误；仅提示工作区换行将按 Git 规则转换。

禁止宣称：
- 不宣称已清理所有重复函数。
- 不改变任务筹办、任务执行、D455 当前主动计划的业务逻辑。
