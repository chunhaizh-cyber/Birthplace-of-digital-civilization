# P4b2-b 学习本能方法虚拟存在服务类收口方案记录

日期：2026-06-05

## 本轮操作

按用户最新口径制定 P4b2-b 小切片方案：

```text
计划/20260605_P4b2b学习本能方法虚拟存在服务类收口方案_v0.1.md
```

随后按方案执行代码最小改动：

```text
自我动作实现模块.ixx
```

## 已确认口径

```text
方法类负责方法树本体结构。
方法虚拟存在服务类负责方法虚拟存在功能实现和状态派生。
学习本能方法若需要改写方法虚拟存在特征值，应调用方法虚拟存在服务类。
学习 / 练习不应直接通过 自我_提交方法状态变化 写 方法状态=可用 / 好用。
```

## 代码锚点

已复核以下锚点：

```text
方法虚拟存在服务类.h:37
    同步方法节点到虚拟存在

方法虚拟存在服务类.cpp:407
    私有_同步方法状态特征

方法虚拟存在服务类.cpp:698
    同步方法节点到虚拟存在

自我动作实现模块.ixx:8630
    自我_尝试学习 成功后确保 自我_提交方法状态变化

自我动作实现模块.ixx:9585
    自我_练习 成功后确保 自我_提交方法状态变化
```

## 当前结论

```text
P4b2-b 已完成代码最小改动。
本轮未新增服务接口，已复用 方法虚拟存在服务类::同步方法节点到虚拟存在。
自我_尝试学习 / 自我_练习 不再直接依赖 自我_提交方法状态变化 写 方法状态。
```

## 修改内容

```text
自我_尝试学习：
    条件结果对提交成功后，调用 方法虚拟存在服务类::同步方法节点到虚拟存在；
    成功判断改为 条件结果阶段成功 && 同步后目标方法虚拟存在存在；
    输出回执中的 方法状态 改为读取服务类派生后的目标方法状态。

自我_练习：
    练习目标达成后，调用 方法虚拟存在服务类::同步方法节点到虚拟存在；
    方法稳定状态通过 方法虚拟存在服务类::写入方法I64特征 写入；
    输出回执中的 方法状态 改为读取服务类派生后的目标方法状态。
```

## 后续禁止扩大

```text
本切片不处理任务筹办直接调用 自我_提交方法可执行入口。
本切片不修改 data/initial_causal_templates.tsv。
本切片不修改自我线程学习任务创建逻辑。
```

## 验收状态

```text
方案已制定。
代码已修改。
git diff --check -- 自我动作实现模块.ixx 计划/计划索引.md：通过，仅 CRLF 提示。
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m：通过，0 错误，1 个 /INCREMENTAL:NO 链接警告。
```
