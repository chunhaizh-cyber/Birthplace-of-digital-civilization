# 待执行计划违背检查记录

日期：2026-06-02

## 检查范围

```text
规范/规范目录.md
计划/计划索引.md
计划/20260601_安全因果链反推当前场景特征值需求纠偏计划_v0.2.md
计划/待完成项
```

## 结论

```text
发现 1 处会影响继续执行的索引违背：
    计划索引仍把当前主动切片写成 P0 文本扫描旧逻辑清理。

事实：
    主动计划正文已记录 P0 文本扫描旧逻辑完成；
    D455 材料包已具备；
    当前停点是需求树尚未自然生成材料型 / 确认型 / 扫描型 / 跟踪型结构化子需求。

处理：
    已把计划索引当前主动切片改为 S6-N2a 需求树生长结构化子需求检查；
    当前目标改为检查 派生需求消息 / 需求树更新指令 / 自我线程提交入口 是否自然生成外设观察运行模式子需求；
    明确不得恢复文本扫描，不得把总目标直接映射为 D455 外设等待项。
```

## 未发现的违背

```text
主动计划中的 任务筹办直接写需求树 / 世界树真值 / 安全值 只作为停止条件出现，不是待执行动作。

主动计划禁止：
    文本扫描兜底；
    模拟样本；
    伪造 D455 报告；
    人工创建来源需求、当前值或证据动态；
    直接调用任务筹办函数伪造业务链。

这些口径与当前硬规则一致。
```

## 待完成项说明

```text
计划/待完成项 中存在历史 --*-check 回归命令、夹具或旧隔离回归记录。

当前判断：
    这些文件处于 待条件具备后继续，不属于当前主动队列；
    多数文件头部已有 禁止新增或依赖 --*-check 回归命令 的历史口径覆盖；
    若未来从待完成项恢复执行，必须先按当前规范重写验证口径，不能直接恢复旧命令。
```

## 下一步

```text
按计划索引继续执行 S6-N2a：
    检查需求树生长是否经 派生需求消息 / 需求树更新指令 / 自我线程提交入口 生成材料型 / 确认型 / 扫描型 / 跟踪型子需求；
    未生成时暴露结构化缺口；
    不允许文本 contains 或目标名称扫描兜底。
```
