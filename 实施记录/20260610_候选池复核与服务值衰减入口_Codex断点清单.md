# 候选池复核与服务值衰减入口 Codex断点清单

日期：2026-06-10

## 当前切片

服务本能方法实现计划 P8e：候选池复核自然入口 / 服务值衰减与休眠入口。

## 目标

```text
在自我治理周期中补齐最小入口：
    1. 成功服务候选池复核；
    2. 已确认候选的 G0 饱和入账；
    3. 服务值按时间自动衰减；
    4. 服务值归零后的休眠候选判断。
```

## 允许修改范围

```text
自我线程模块.impl.cpp
自我动作实现.服务模块.ixx
计划/计划索引.md
实施记录/20260610_候选池复核与服务值衰减入口_Codex断点清单.md
```

## 禁止修改范围

```text
不伪造现实世界证据。
不把候选池复核写成普通任务筹办 / 任务执行。
不把服务值衰减解释成失败惩罚。
不让服务值归零直接等于休眠。
不提前宣称服务值 v1.1 全链路完成。
```

## 预测

```text
自我治理周期可调用候选池复核入口；
候选硬门槛和证据仍不足时只保持待确认，不写服务值；
候选复核为已确认可结算时复用 P8d 饱和入账；
服务值衰减只按时间入口降低服务值；
休眠只形成候选判断或日志，不直接替代现有线程生命周期控制。
```

## 当前验证状态

```text
已修改代码：
    自我线程模块.impl.cpp
    计划/计划索引.md
    实施记录/20260610_候选池复核与服务值衰减入口_Codex断点清单.md

已运行命令：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\鱼巢_build_20260610_服务模块P8e.log

退出码：
    0

关键证据：
    Debug x64 构建通过。
    构建日志：日志/鱼巢_build_20260610_服务模块P8e.log

当前判断：
    P8e 代码接入闭合。
    自我主循环已接入服务治理周期维护入口。
    每 4 tick 最多复核 8 个成功服务候选记录；已结算候选按 D2 引用跳过，避免重复入账。
    候选复核为已确认可结算时复用 P8d G0 饱和入账 helper。
    服务值按时间入口调用既有服务衰减本能方法。
    服务值归零只形成休眠候选日志，不直接替代现有线程生命周期控制。
    未运行伪造入口或 `--*-check` 回归；真实自然样本日志待后续运行观察。

下一步：
    需要后续真实运行样本验证 `服务治理周期维护`、`成功服务G0饱和入账`、服务衰减和休眠候选日志。
```

## 禁止宣称项

```text
不得宣称候选复核已获得真实外部反馈，除非真实日志证明。
不得宣称服务休眠策略全量完成。
不得宣称自我苏醒完成或初步成熟完成。
```
