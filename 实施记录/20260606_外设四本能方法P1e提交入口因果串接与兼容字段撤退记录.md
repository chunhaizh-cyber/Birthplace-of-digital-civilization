# 20260606_外设四本能方法P1e提交入口因果串接与兼容字段撤退记录

## 切片目标

```text
收口四个提交入口对上游方法动作动态的显式串接。
撤退 提交_当前观察范围可观测单位存在对应事实 对 外设提交包候选事实数量 的兼容读取。
撤退 提交_指定存在跟踪事实 缺 目标存在 时退回 当前观察存在 的旧路径。
提交入口仍由当前提交方法生成自己的方法动作动态，不把上游动态伪装成当前提交动作。
```

## 修改内容

```text
自我动作实现.外设模块.ixx：
    四个提交入口规格结果表补充 来源动作动态。
    新增 读取提交入口上游动作动态 / 串接提交入口来源动作动态 helper。
    提交入口先读取上游正式结果动态，再创建当前提交方法动作动态。
    当前提交动作动态写 来源动作动态 指针；提交结果摘要也写 来源动作动态 指针。

提交_当前观察范围可观测单位存在对应事实：
    不再读取 外设提交包候选事实数量。
    候选事实数量只由正式确认可提交数量、冲突数量和证据不足数量合成。
    日志补充 不读取外设提交包候选事实数量=1 和 来源动作动态。

提交_观察存在发现事实：
    提交动作动态保留当前提交方法来源。
    来源动作动态 指向扫描 / 发现相关上游动作动态。

提交_观察存在特征值变化事实：
    提交动作动态保留当前提交方法来源。
    来源动作动态 指向扫描变化结果、存在变化或值域冲突相关上游动作动态。

提交_指定存在跟踪事实：
    缺 目标存在 时不再回退 当前观察存在。
    来源动作动态 指向跟踪稳定、丢失、重捕获、预测偏差或目标变化相关上游动作动态。
    日志补充 缺目标不回退当前观察存在=1 和 来源动作动态。
```

## 验证证据

```text
git diff --check -- 自我动作实现.外设模块.ixx 实施记录/20260605_外设四本能方法P0_Codex断点清单.md
    退出码 0；
    仅 LF/CRLF 提示。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码 0；
    0 个错误；
    1 个链接警告 LNK4075：因 /INCREMENTAL:NO 忽略 /EDITANDCONTINUE。
```

## 边界和未完成项

```text
本切片未真实运行鱼巢.exe，不宣称四方法自然业务链已通过。
来源动作动态只作为因果引用，不替代当前提交方法动作动态。
提交入口仍不写需求满足、安全值或服务值。
P1 正式特征与提交入口去容器化收口完成后，下一步进入 P2 观察方法收口，并继续拆成小切片执行。
```
