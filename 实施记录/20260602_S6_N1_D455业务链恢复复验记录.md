# S6-N1 D455 业务链恢复复验记录

日期：2026-06-02

## 目标

```text
在相机外设已确认可正常工作的条件下，
复验真实 D455 报告是否能自然进入：
    外设观察等待项
    -> D455 线程
    -> 短期材料页 / d455://可回查材料
    -> 逐簇识别报告
    -> 观察事实承接请求
    -> 观察事实更新事件
    -> 安全根需求重筹办。
```

## 运行前预测

```text
若正式业务链已自然闭合：
    默认正式长运行应出现自动创建外设观察等待项、启动 D455 线程、逐簇识别报告入队、
    任务管理外设消息承接生成观察事实承接请求。

若仍有真实阻塞：
    默认正式长运行会启动自我线程和任务管理线程，
    但不会出现 D455 等待项 / 报告 / 观察事实承接请求；
    日志应给出未生成等待项的业务原因。
```

## 真实运行

```text
命令：
    .\x64\Debug\鱼巢.exe

方式：
    保持 stdin 打开约 45 秒后写入 退出。

结果：
    退出码 0；
    stdout 显示控制台命令模式启动、控制面板独立窗口启动、收到退出后正常结束。
```

## 关键日志证据

```text
硬件可用前置：
    S6-N2b 已用 --d455-capture-once 验证 D455 打开、单帧采集和释放均成功。

当前复验停点：
    2026-06-02 11:42:00.928
    任务管理界面线程/外设观察等待项未生成
    原因=当前场景特征值汇总目标不直接映射D455等待项
    目标特征=当前场景特征值明确状态
    需要先拆成材料型/确认型/扫描型/跟踪型子需求
    直接写需求树=0
    直接写世界树=0

后续派生目标仍未匹配外设观察运行模式：
    关键特征变化观察状态
    安全因果候选构造状态
    当前场景持续观察需求明确状态
    当前场景持续观察状态
    安全因果因素证据搜索状态

本轮 11:42 未出现：
    任务管理界面线程/自动创建外设观察等待项
    任务管理界面线程/按外设观察等待项启动D455线程
    外设线程_D455深度相机/逐簇识别报告入队
    任务管理外设消息承接/生成观察事实承接请求
    外设等待项巡检生成上行
```

## 对账结论

```text
相机外设不可用不再是阻塞点。

S6-N1 业务链当前仍未验收：
    真实 D455 报告 -> 短期材料页 -> d455://句柄 -> 外设观察事实承接
    没有在本轮自然运行中出现。

本轮遇到的问题是：
    当前场景特征值明确状态 作为汇总目标被任务管理等待项生成闸门拦截；
    代码要求它先拆成材料型 / 确认型 / 扫描型 / 跟踪型子需求；
    因此不能把汇总目标直接映射为 D455 等待项，也不能恢复旧诊断入口绕过。

下一步：
    按 050005 标准补正式子需求拆解链，
    让自然需求树先生成可被外设等待项识别的观察运行模式目标。
```
