# 20260620 常用抽象特征初始化结果收缩 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_特征类逻辑纠偏计划_v0.1.md` 的 1.1 P2.3，处理 `常用抽象特征初始化结果` 收缩候选。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
规范/特征类规范20260429.md
规范/主信息字段与结构新增准入约束规范20260519.md
计划/计划索引.md
计划/20260620_特征类逻辑纠偏计划_v0.1.md
实施记录/20260620_特征类消费待确认结构审查_Codex断点清单.md
```

已复核锚点：

```text
世界树类.cpp:1567 调用 特征服务_.初始化常用抽象特征。
世界树类.cpp:1568 只读取 抽象特征初始化.成功。
特征类.h:235-239 常用抽象特征初始化结果 只有 成功 被外部消费。
```

本切片范围：

```text
1. 收缩 初始化常用抽象特征 返回值到当前真实消费的 bool。
2. 删除 常用抽象特征初始化结果 结构。
3. 删除未消费的 抽象特征数 / 区间状态数 计数写入。
4. 运行函数说明文档生成器和 Debug x64 构建。
```

验证前预测：

```text
1. 代码中不应再命中 常用抽象特征初始化结果、抽象特征数、区间状态数。
2. 世界树类.cpp 调用点改为直接读取 bool。
3. python .\tools\生成函数功能说明文档.py 应成功。
4. Debug x64 构建应通过。
```

## 1. 实施记录

```text
已收缩：
1. 删除 特征类.h 中 常用抽象特征初始化结果 结构。
2. 将 特征类::初始化常用抽象特征 返回类型改为 bool。
3. 删除 特征类.cpp 中 抽象特征数 / 区间状态数 计数写入。
4. 将 世界树类.cpp:1567 调用点改为直接使用 bool 返回值。
```

## 2. 验证结果

```text
rg -n "常用抽象特征初始化结果|抽象特征初始化\.成功|结果\.(抽象特征数|区间状态数)|初始化常用抽象特征" 特征类.h 特征类.cpp 世界树类.cpp
结果：
    特征类.h:260 bool 初始化常用抽象特征(基础信息节点类* 抽象特征根);
    特征类.cpp:735 bool 特征类::初始化常用抽象特征(基础信息节点类* 抽象特征根)
    世界树类.cpp:1567 直接使用 bool 返回值。
    未命中 常用抽象特征初始化结果 / 抽象特征初始化.成功 / 计数字段。

python .\tools\生成函数功能说明文档.py
结果：退出码 0；源码文件 128；函数条目 6877；函数说明文档已更新。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\20260620_常用抽象特征初始化结果收缩_msbuild.log
结果：退出码 0；日志显示 0 个警告、0 个错误。
```

当前判断：

```text
常用抽象特征初始化结果 收缩闭合。
特征类 1.1 当前登记的 P2.1 / P2.2 / P2.3 / P2.4 已闭合。
下一步按计划进入 1.2 非特征主信息直接操作审查。
```
