# 20260608 S1d5a 二次特征签名父节点索引 Codex 断点清单

## 当前切片

- 主动计划：`计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- 执行方案：`计划/20260607_S1d5a二次特征签名父节点索引瘦身方案_v0.1.md`
- 切片：S1d-5a 二次特征签名父节点索引瘦身。

## 依据文件

- `AGENTS.md`
- `规范/规范目录.md`
- `规范/方法动作动态硬规则规范20260512.md`
- `规范/动作动态即因果账本规范20260512.md`
- `规范/二次特征规范20260520.md`
- `规范/远程执行通道与断点续跑规范20260524.md`
- `计划/计划索引.md`
- `计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- `计划/20260607_S1d4z证据动态样本追加键缓存瘦身方案_v0.1.md`
- `实施记录/20260608_S1d4z证据动态样本键缓存_Codex断点清单.md`

## 当前判断

- S1d-4z 已闭合，`命中模板证据动态追加` 平均降到约 16649us。
- 最新高层 `动态二次特征生成/内部阶段耗时` 9 条，最慢阶段全部为 `分步结果生成`。
- 最新 `动态二次特征分步结果/内部阶段耗时` 7 条：
  - `分步签名查找创建` 平均约 69292us，最大约 150463us。
  - `分步值写入` 平均约 57884us，最大约 85458us。
- 代码证据：`二次特征类::获取或创建子二次特征_按签名` 当前先枚举父节点全部二次特征，再逐个比较 `度量签名_链键`，然后才创建缺失节点。
- 下一步做局部父节点 + 签名缓存，不进入基础信息全局索引。

## 构建 / 运行前预测

- Debug x64 构建通过。
- 真实运行退出码 0。
- 日志继续出现 `动态二次特征分步结果/内部阶段耗时`。
- `分步签名查找创建` 平均耗时下降，或 `二次特征签名查找创建/内部阶段耗时` 中子节点枚举 / 签名比较不再构成累计瓶颈。
- 日志保留 `任务状态动作动态=<动态主键>`。
- 不出现 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`。
- 不出现旧路径锚点 `准备本能调用`、`执行本能方法`、`任务域本能输入包`。

## 允许修改

- `二次特征类.h`
- `二次特征类.cpp`
- `计划/20260607_S1d5a二次特征签名父节点索引瘦身方案_v0.1.md`
- `计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- `计划/计划索引.md`
- `实施记录/20260608_S1d5a二次特征签名父节点索引_Codex断点清单.md`

## 已改文件

- `计划/20260607_S1d5a二次特征签名父节点索引瘦身方案_v0.1.md`
- `实施记录/20260608_S1d5a二次特征签名父节点索引_Codex断点清单.md`

## 下一步

1. 在 `二次特征类.h/.cpp` 增加父节点 + 度量签名缓存。
2. 在 `绑定基础信息`、`获取或创建子二次特征_按签名`、`删除二次特征` 同步维护缓存。
3. 构建 Debug x64。
4. 真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000`。
5. 汇总 `动态二次特征分步结果/内部阶段耗时`。

## 禁止宣称

- 不宣称自我苏醒完成。
- 不宣称初步成熟完成。
- 不改变二次特征生成语义。
- 不进入基础信息全局索引实现。
