# 20260620 存在相对坐标表 helper Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_存在类逻辑纠偏计划_v0.1.md` 的 1.7，补存在空间相对坐标表 DTO / 批量换算 helper。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_存在类逻辑纠偏计划_v0.1.md
实施记录/20260620_世界树场景辅助函数归属判定表.md
实施记录/20260620_世界树候选召回旧入口删除_Codex断点清单.md
```

## 1. 问题锚点

```text
世界树类.h:
    场景相对坐标项
    场景绝对坐标换算结果

世界树类.cpp:
    世界树类::计算场景绝对坐标_由相对坐标表
    世界树类::写入存在场景绝对坐标_由相对坐标表

自我动作实现.外设模块.ixx:
    构造场景相对坐标表_同源观察
```

当前判断：

```text
相对坐标表 DTO 和批量换算属于存在空间关系 helper，应迁入存在类。
世界树写入入口暂保留为薄封装，用于维持场景存在型二次特征刷新边界。
```

验证前预测：

```text
1. 存在类.h / 存在类.cpp 新增存在空间相对坐标表 DTO 和批量换算 helper。
2. 世界树类.h / 世界树类.cpp 不再定义 场景相对坐标项 / 场景绝对坐标换算结果。
3. 外设模块改用存在空间坐标 DTO。
4. 函数说明文档生成退出码应为 0。
5. Debug x64 构建应通过。
```

## 2. 执行结果

```text
1. 存在类.h / 存在类.cpp 已新增 结构_存在空间相对坐标项、结构_存在空间绝对坐标换算结果。
2. 存在类 已新增 计算场景绝对坐标_由相对坐标表 / 写入存在场景绝对坐标_由相对坐标表。
3. 世界树类 不再定义 场景相对坐标项 / 场景绝对坐标换算结果；相对坐标表入口改为存在类 DTO 薄封装。
4. 世界树写入薄封装仍保留二次特征刷新边界；本切片不删除该入口。
5. 自我动作实现.外设模块.ixx 已改用 结构_存在空间相对坐标项 和 构造存在空间相对坐标表_同源观察。
```

验证结果：

```text
旧 DTO 残留检索：场景相对坐标项 / 场景绝对坐标换算结果 无代码命中。
新 DTO 命中范围：存在类.h / 存在类.cpp / 世界树类.h / 世界树类.cpp / 自我动作实现.外设模块.ixx。
函数说明文档生成：exit=0；源码文件 128；函数条目 6926。
Debug x64 构建：exit=0；已成功生成；0 个警告；0 个错误。
git diff --check：仅有 LF 将替换为 CRLF 的工作区换行提示，无空白错误。
```

证据文件：

```text
日志/20260620_存在相对坐标表helper_函数文档.log
日志/20260620_存在相对坐标表helper_msbuild.log
```

当前判断：

```text
存在类 1.7 已闭合。
场景类 P5.4 的剩余相对坐标表 DTO / 批量换算迁移已闭合。
世界树相对坐标表写入入口当前只作为跨服务薄封装和二次特征刷新边界保留，不作为旧 DTO 兼容入口。
```

禁止宣称：

```text
不宣称世界树场景辅助函数迁移完成。
不宣称相对坐标表世界树入口已删除。
不宣称场景类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
