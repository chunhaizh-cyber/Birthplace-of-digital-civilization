# 需求类逻辑纠偏 Codex 断点清单

## 0. 恢复摘要

当前主动计划：`计划/20260620_需求类逻辑纠偏计划_v0.1.md`。

当前切片：P7d 枚举目标校验跨类读取与说明字段机器边界预判定已完成；P0、P1、P2、P3、P3.1、P3.2、P3.3、P7 首轮分类、P7a、P7b、P7c 已完成。下一步进入 P7e `获取需求方向` 状态 / 特征跨类读取回迁。

已复核依据：

- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `规范/主信息字段与结构新增准入约束规范20260519.md`
- `规范/需求主信息类规范20260507.md`
- `规范/需求树生长机制规范20260512.md`
- `规范/需求临时因果视图展开规范20260526.md`
- `计划/计划索引.md`
- `计划/20260620_需求类逻辑纠偏计划_v0.1.md`
- `计划/20260620_方法类逻辑纠偏计划_v0.1.md`

当前目标：

1. 继续需求类 P7 自身跨类读取回迁。
2. P7e 聚焦 `需求类::获取需求方向` 中的状态 / 特征跨类读取。
3. 机器逻辑必须读取结构化特征、状态、值域、生产者分级和方向材料，不得读取说明文本或 `语素集.安全获取词(...)` 词面。

预测：

- `获取需求方向` 会命中初始 / 结果状态的状态主体、状态特征、状态值读取。
- 若现有二次特征类或世界树比较入口缺少状态材料结构化输入，先登记对应类缺口，不在需求类自写主信息解释。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不改需求树语义。
- 不把获取途径写入需求主信息。
- 不把 `语素集.安全获取词(...)` 读出的文本用于机器查找 / 生成逻辑。

## 1. P0.1/P0.2 扫描与分类

已生成扫描日志：

```text
日志/20260621_D0_需求主信息外部直读_计划模式扫描.txt
日志/20260621_D0_需求主信息外部直读_宽模式扫描.txt
日志/20260621_D0_需求主信息关注字段扫描.txt
日志/20260621_D0_需求类既有函数扫描.txt
日志/20260621_D0_需求主信息机器逻辑候选.txt
日志/20260621_D0_需求主信息显示候选.txt
```

分类表：

```text
实施记录/20260621_D0_需求主信息外部直读分类表.md
```

当前判断：

```text
1. `任务类.cpp` 命中点少且均为机器逻辑，作为本轮第一代码切片。
2. 先补 `需求类` 只读 helper，再迁移 `任务类.cpp`，不修改需求树语义。
3. 自我线程和任务模块大范围命中后续切片处理；控制面板 / SQL / 摘要显示后置。
```

下一步：

```text
执行 P3 更新指令协议简化预切片：
    P3.3 新增原子需求专用构造函数；
    迁移 `自我线程模块.impl.cpp` 中新增需求手写点；
    保留消息协议必要字段，不新增平行输入包。
```

## 2. P0.3 任务类小切片构建前预测

已改文件：

```text
需求类.h
需求类.cpp
任务类.cpp
说明书/函数功能说明文档.md
实施记录/20260621_D0_需求主信息外部直读分类表.md
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
1. `需求类` 新增状态引用、状态解析、需求场景解析、权重读取的只读 helper。
2. `任务类.cpp` 的目标状态、当前状态、需求场景、服务权重、安全权重读取改为调用 `需求类` helper。
3. 未修改需求树入树、满足、权重传播、任务状态推进语义。
```

构建前预测：

```text
Debug x64 构建应通过；若失败，优先检查新增声明 / 定义签名、`I64` 类型可见性和新增 helper 的链接符号。
```

## 3. P0.3 构建结果

验证命令：

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
函数说明文档生成：退出码 0，函数条目 7129。
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_需求类只读helper_任务类回迁_build.log` 显示“已成功生成”。
`任务类.cpp` 对 来源需求->主信息.需求状态 / 被需求当前状态 / 需求场景 / 服务权重 / 安全权重 的直读扫描无命中。
```

当前判断：

```text
任务类小切片闭合；需求类 P0 尚未完成，后续继续处理任务模块和自我线程等机器逻辑直读。
```

## 4. P0.4 任务管理工作线程小切片构建前预测

已改文件：

```text
需求类.h
需求类.cpp
任务模块.管理工作线程.ixx
说明书/函数功能说明文档.md
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
1. `需求类` 新增 `读取需求满足关系` 只读 helper。
2. `任务模块.管理工作线程.ixx` 中目标特征类型、目标状态、当前状态、满足关系读取改为调用 `需求类` helper。
3. 该文件对 目标特征类型缓存 / 需求状态 / 被需求当前状态 / 满足关系 的需求主信息直读扫描无命中。
```

构建前预测：

```text
Debug x64 构建应通过；若失败，优先检查 `读取需求满足关系` 声明 / 定义签名和模块编译依赖。
```

## 5. P0.4 构建结果

验证命令：

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
函数说明文档生成：退出码 0，函数条目 7130。
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_需求类满足关系_工作线程回迁_build.log` 显示“已成功生成”。
`任务模块.管理工作线程.ixx` 对 目标特征类型缓存 / 需求状态 / 被需求当前状态 / 满足关系 的需求主信息直读扫描无命中。
```

当前判断：

```text
任务管理工作线程小切片闭合；需求类 P0 继续进入 `任务模块.筹办.ixx` 小范围回迁。
```

## 6. P0.5 任务筹办生命周期上下文小切片构建前预测

已改文件：

```text
需求类.h
需求类.cpp
任务模块.筹办.ixx
说明书/函数功能说明文档.md
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
1. `需求类` 新增需求主体 / 目标宿主引用读取与解析 helper。
2. `任务模块.筹办.ixx` 的需求目标特征、组节点满足回执目标状态引用、需求生命周期上下文构造、状态主键相等判断已改为调用 `需求类` helper。
3. 本切片未处理因果条件缺口去重、方法结果方向 / 评分、后段回执赋值和日志追踪中的剩余直读。
```

构建前预测：

```text
Debug x64 构建应通过；若失败，优先检查新增需求主体 / 目标宿主 helper 的声明定义和 `任务模块.筹办.ixx` 中 `可解析引用` 使用范围。
```

## 7. P0.5 构建结果

验证命令：

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
函数说明文档生成：退出码 0，函数条目 7139。
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_任务筹办生命周期上下文回迁_build.log` 显示“已成功生成”。
```

当前判断：

```text
任务筹办生命周期上下文小切片闭合；`任务模块.筹办.ixx` 剩余直读继续按因果缺口去重、方法结果方向 / 评分、后段回执赋值和日志追踪拆分。
```

## 8. P0.6 任务筹办因果缺口去重小切片构建前预测

已改文件：

```text
任务模块.筹办.ixx
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
`因果条件缺口重复来源需求目标_筹办查询` 中来源需求目标主体、目标状态、目标状态主键和满足关系读取已改为调用 `需求类` helper。
```

构建前预测：

```text
未新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过；若失败，优先检查 `可解析引用<状态节点类>` 在该模块中的可见性。
```

## 9. P0.6 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_任务筹办因果去重回迁_build.log` 显示“已成功生成”。
```

当前判断：

```text
任务筹办因果缺口去重小切片闭合；`任务模块.筹办.ixx` 下一步处理方法结果方向 / 评分函数中的当前状态、目标状态、满足关系直读。
```

## 10. P0.7 任务筹办方法结果方向评分小切片构建前预测

已改文件：

```text
任务模块.筹办.ixx
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
`方法结果项方向适配需求` 和 `方法结果项目标值一致分` 中当前状态、目标状态和满足关系读取已改为调用 `需求类` helper。
```

构建前预测：

```text
未新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过；若失败，优先检查函数调用可见性。
```

## 11. P0.7 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_任务筹办方法评分回迁_build.log` 显示“已成功生成”。
```

当前判断：

```text
任务筹办方法结果方向 / 评分小切片闭合；`任务模块.筹办.ixx` 下一步处理目标主体推导函数中的被需求存在、目标状态、当前状态直读。
```

## 12. P0.8 任务筹办目标主体推导小切片构建前预测

已改文件：

```text
任务模块.筹办.ixx
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
`任务方法结构补齐目标方法` 中目标宿主、目标状态和当前状态读取已改为调用 `需求类` helper。
```

构建前预测：

```text
未新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过。
```

## 42. P0.23 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_自我线程来源需求状态读取回迁_build.log` 显示“已成功生成”且 0 个错误。
安全因果风险树维护入口中的来源需求当前状态 / 目标状态读取已回迁。
```

当前判断：

```text
自我线程来源需求状态读取小切片闭合；来源需求收束写侧缺口已登记，后续不阻塞继续处理任务来源需求读取和后段准入裁决读取。
```

## 43. P0.24 自我线程任务来源与准入裁决读取小切片构建前预测

已改文件：

```text
自我线程模块.impl.cpp
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
1. 任务派发目标日志中的任务来源需求目标状态读取改为调用 `需求类::解析需求目标状态`。
2. 任务派发治理请求中的任务来源需求主体、目标宿主和场景读取改为调用 `需求类` helper。
3. 自检维护管理父需求和自检候选父需求裁决中的截止读取改为调用 `需求类::需求已截止`。
4. 派生需求补齐和父子目标重叠判断中的当前 / 目标状态、满足关系读取改为调用 `需求类` helper。
5. 后段当前主需求 / 当前主任务准入中的需求截止判断改为调用 `需求类::需求已截止`。
```

构建前预测：

```text
未新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过。
```

## 44. P0.24 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_自我线程任务来源准入裁决回迁_build.log` 显示“已成功生成”且 0 个错误。
任务派发来源需求、维护父需求准入裁决、派生需求补齐 / 目标重叠判断、后段当前主需求 / 当前主任务准入中的本轮需求主信息读取已回迁。
```

当前判断：

```text
自我线程任务来源与准入裁决读取小切片闭合；下一步继续 `自我线程模块.impl.cpp` 剩余治理请求主体 / 场景读取和子 / 根需求截止、状态、满足关系读取切片。
```

## 45. P0.25 自我线程治理请求主体场景读取小切片构建前预测

已改文件：

```text
自我线程模块.impl.cpp
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

计划修改范围：

```text
1. `私有_提交任务发起请求到任务管理界面线程_线程侧` 中来源需求主体、目标宿主、场景读取改为调用 `需求类` helper。
2. 观察事实更新来源需求 / 安全根需求重筹办治理请求中的主体、目标宿主、场景读取改为调用 `需求类` helper。
3. 不处理父需求更新指令中的主体 / 场景 / 权重读取，不处理子 / 根需求状态和满足关系读取。
```

构建前预测：

```text
不新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过。
```

## 46. P0.25 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_自我线程治理请求主体场景回迁_build.log` 显示“已成功生成”且 0 个错误。
自我线程三处任务治理请求构造中的来源需求主体、目标宿主和场景读取已回迁。
```

当前判断：

```text
自我线程治理请求主体 / 场景读取小切片闭合；下一步继续子 / 根需求截止、状态、满足关系读取中的最小机器逻辑切片。
```

## 47. P0.26 自我线程观察事实材料子需求读取小切片构建前预测

已改文件：

```text
自我线程模块.impl.cpp
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

计划修改范围：

```text
1. `私有_刷新观察事实更新材料子需求_线程侧` 中待刷新筛选的子需求截止 / 阻塞读取改为调用 `需求类` helper。
2. 同函数中刷新循环的子需求截止、目标状态和满足关系读取改为调用 `需求类` helper。
3. 不处理根特征必要条件收束 / 删除 / 刷新函数，不处理目标特征类型文字命中规则。
```

构建前预测：

```text
不新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过。
```

## 48. P0.26 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_自我线程观察事实材料子需求回迁_build.log` 显示“已成功生成”且 0 个错误。
观察事实更新材料子需求刷新中的子需求截止、阻塞、目标状态和满足关系读取已回迁。
```

当前判断：

```text
自我线程观察事实材料子需求读取小切片闭合；下一步继续根特征必要条件子需求收束 / 删除 / 刷新读取。
```

## 49. P0.27 自我线程根特征子需求收束删除读取小切片构建前预测

已改文件：

```text
自我线程模块.impl.cpp
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

计划修改范围：

```text
1. `私有_收束根特征必要条件子需求_线程侧` 中子需求截止、当前 / 目标状态引用、满足关系读取改为调用 `需求类` helper。
2. `私有_删除遗留根子需求_线程侧` 和遗留固定服务 / 安全子需求清理中的子需求截止读取改为调用 `需求类::需求已截止`。
3. 不处理根特征必要条件刷新主循环，不处理根需求差距子需求退出活动集。
```

构建前预测：

```text
不新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过。
```

## 50. P0.27 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_自我线程根特征子需求收束删除回迁_build.log` 显示“已成功生成”且 0 个错误。
根特征必要条件子需求收束 / 删除 / 遗留清理中的子需求截止、当前 / 目标状态引用和满足关系读取已回迁。
```

当前判断：

```text
自我线程根特征子需求收束 / 删除读取小切片闭合；下一步继续根特征必要条件刷新主循环和根需求差距子需求退出活动集读取。
```

## 51. P0.28 自我线程根需求差距子需求退出读取小切片构建前预测

已改文件：

```text
自我线程模块.impl.cpp
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

计划修改范围：

```text
1. `私有_刷新根特征必要条件子需求_线程侧` 中根需求截止读取改为调用 `需求类::需求已截止`。
2. `私有_需求目标特征匹配_线程侧` 中目标特征缓存、目标状态、当前状态读取改为调用 `需求类` helper。
3. `私有_停用根需求差距执行子需求_线程侧` 中根需求 / 子需求截止、根需求当前 / 目标状态引用、满足关系和日志截止时间读取改为调用 `需求类` helper。
4. 不处理父需求更新指令、权重读取和已登记的来源需求收束写侧缺口。
```

构建前预测：

```text
不新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过。
```

## 52. P0.28 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_自我线程根差距子需求退出回迁_build.log` 显示“已成功生成”且 0 个错误。
根特征必要条件刷新、需求目标特征匹配和根需求差距子需求退出活动集路径中的目标特征、状态引用、满足关系和截止读取已回迁。
```

当前判断：

```text
自我线程根需求差距子需求退出读取小切片闭合；下一步继续父需求更新指令和权重读取切片。
```

## 53. P0.29 自我线程目标特征匹配查找读取小切片构建前预测

已改文件：

```text
自我线程模块.impl.cpp
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

计划修改范围：

```text
1. `私有_查找根特征必要条件子需求_线程侧` 中子需求截止和目标宿主引用读取改为调用 `需求类` helper。
2. `私有_需求目标特征类型_线程侧` 中目标特征、目标状态、当前状态读取改为调用 `需求类` helper。
3. 派生需求同目标子需求查找、直接子需求目标特征查找中的截止读取改为调用 `需求类::需求已截止`。
4. 不处理父需求更新指令主体 / 场景 / 权重读取，不处理来源需求收束写侧缺口。
```

构建前预测：

```text
不新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过。
```

## 54. P0.29 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_自我线程目标特征匹配查找回迁_build.log` 显示“已成功生成”且 0 个错误。
目标特征匹配 / 子需求查找路径中的截止、目标宿主引用、目标特征和状态读取已回迁。
```

当前判断：

```text
自我线程目标特征匹配查找读取小切片闭合；下一步继续父需求更新指令主体 / 场景 / 权重读取。
```

## 55. P0.30 自我线程派生需求入树父需求读取小切片构建前预测

已改文件：

```text
自我线程模块.impl.cpp
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

计划修改范围：

```text
1. `私有_确保派生需求入树_线程侧` 中父需求主体、场景读取改为调用 `需求类` 引用 helper。
2. 同函数中父需求安全 / 服务方向判定改为调用 `需求类::来源需求有安全方向 / 来源需求有服务方向`。
3. 不处理当前场景特征值逻辑组织子需求、根特征必要条件子需求生成函数和来源需求收束写侧缺口。
```

构建前预测：

```text
不新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过。
```

## 56. P0.30 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_自我线程派生需求父需求回迁_build.log` 显示“已成功生成”且 0 个错误。
派生需求入树路径中的父需求主体、场景和安全 / 服务方向读取已回迁。
```

当前判断：

```text
自我线程派生需求入树父需求读取小切片闭合；下一步继续当前场景特征值逻辑组织子需求父需求读取。
```

## 57. P0.31 自我线程子需求生成父需求读取小切片构建前预测

已改文件：

```text
自我线程模块.impl.cpp
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

计划修改范围：

```text
1. `私有_创建当前场景特征值逻辑组织子需求_线程侧` 中父需求主体、场景、安全 / 服务方向读取改为调用 `需求类` helper。
2. `私有_生成根特征必要条件子需求_线程侧` 中父需求主体、场景、安全 / 服务方向读取改为调用 `需求类` helper。
3. 不处理自检候选父需求权重、自我线程候选评分和已登记的来源需求收束写侧缺口。
```

构建前预测：

```text
不新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过。
```

## 58. P0.31 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_自我线程子需求生成父需求回迁_build.log` 显示“已成功生成”且 0 个错误。
当前场景特征值逻辑组织子需求和根特征必要条件子需求生成路径中的父需求主体、场景和安全 / 服务方向读取已回迁。
```

当前判断：

```text
自我线程子需求生成父需求读取小切片闭合；下一步继续自检候选父需求权重、候选评分、挂接截止等剩余零散读取。
```

## 59. P0.32 自我线程剩余零散只读点小切片构建前预测

已改文件：

```text
自我线程模块.impl.cpp
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

计划修改范围：

```text
1. 根分区推导中的安全 / 服务方向读取改为调用 `需求类::来源需求有安全方向 / 来源需求有服务方向`。
2. 共享任务挂接需求扇出中的挂接需求截止读取改为调用 `需求类::需求已截止`。
3. 活动需求任务化零权重日志判定和任务候选评分中的权重读取改为调用 `需求类::读取需求综合权重`。
4. 自检正式需求父方向承接中的父需求安全 / 服务方向读取改为调用 `需求类` helper。
5. 不处理 `自我线程模块.impl.cpp:6909-6989` 已登记的来源需求 / 父需求结算写侧缺口；`当前->主信息.取首节点信息()` 是方法节点读取，不属于需求主信息直读。
```

构建前预测：

```text
不新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过。
```

## 60. P0.32 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_自我线程剩余零散只读点回迁_build.log` 显示“已成功生成”且 0 个错误。
根分区推导、共享挂接截止、零权重日志判定、自检父方向承接和候选评分中的剩余只读点已回迁。
```

当前判断：

```text
自我线程剩余零散只读点小切片闭合；下一步复扫自我线程剩余需求主信息直读，仅保留已登记写侧缺口和非需求节点读取。
```

## 61. P0.33 控制命令来源需求状态读取小切片构建前预测

已改文件：

```text
鱼巢.cpp
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

计划修改范围：

```text
1. `鱼巢.cpp` 控制命令诊断输出中的来源需求当前状态 / 目标状态读取改为调用 `需求类::解析需求当前状态 / 解析需求目标状态`。
2. 不改变主控制面板 SQL 读源；本切片只处理命令行诊断显示路径。
3. 不处理控制面板 live 快照和摘要线程显示路径。
```

构建前预测：

```text
不新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过。
```

## 62. P0.33 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_控制命令来源需求状态回迁_build.log` 显示“已成功生成”且 0 个错误。
控制命令诊断输出中的来源需求当前状态 / 目标状态读取已回迁。
```

当前判断：

```text
控制命令来源需求状态读取小切片闭合；下一步继续控制面板 / 摘要线程 live 显示读取分类。
```

## 63. P0 收束与 P1 转入

复扫结论：

```text
1. P0 机器逻辑只读回迁当前范围闭合。
2. 剩余 `自我线程模块.impl.cpp:6909-6989` 与 `自我动作实现模块.ixx:12335-12340` 是结算摘要 / 需求收束写侧缺口，不混入只读 helper 回迁。
3. 控制面板 / 摘要 / SQL 显示按 P6 后置；主控制面板正式入口保持 SQL 读模型。
4. `当前->主信息.取首节点信息()` 等命中为方法节点读取，不属于需求主信息直读。
```

下一步：

```text
转入 P1，生成 `实施记录/20260621_D1_需求类结构体枚举必要性判定表.md`。
```

## 64. P1 需求类结构体 / 枚举必要性判定表

已生成文件：

```text
实施记录/20260621_D1_需求类结构体枚举必要性判定表.md
```

判定结果：

```text
1. `结构_需求树更新指令`、`结构_逻辑组织需求更新输入` 保留，但进入 P3 协议简化审查。
2. `枚举_需求获取途径`、`结构_需求获取途径语义视图`、`结构_需求目标语义视图` 保留，但进入 P2 获取途径 / 目标语义边界收口。
3. `结构_需求方向签名` 保留，但进入 P7 二次特征 helper 边界审查。
4. `结构_需求树SQL行` 保留为 SQL 查询投影，不作为需求树权威写源。
```

当前判断：

```text
P1 判定表闭合；下一步转入 P2 目标语义与获取途径收口。
```

## 38. P0.21 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_自我线程满足关系直接收束回迁_build.log` 显示“已成功生成”且 0 个错误。
`自我线程模块.impl.cpp` 中来源需求满足关系读取和已完成叶需求直接收束截止读取已回迁。
```

当前判断：

```text
自我线程满足关系与直接收束截止读取小切片闭合；下一步处理父需求回查 / 活动子需求扫描中的状态、满足关系、截止和权重读取。
```

## 39. P0.22 自我线程父需求回查与活动子需求小切片构建前预测

已改文件：

```text
自我线程模块.impl.cpp
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
1. `私有_需求状态已满足_任务管理新链` 中状态、满足关系、子需求截止 / 阻塞 / 权重读取改为调用 `需求类` helper。
2. `私有_回查父需求目标_任务管理新链` 中父需求状态、满足关系、目标特征类型、直接子截止 / 阻塞读取改为调用 `需求类` helper。
3. `私有_需求可提交活动任务化意图_线程侧` 和 `私有_选择活动叶子需求_任务管理新链` 中截止、目标宿主、状态、满足关系、阻塞、权重读取改为调用 `需求类` helper。
4. 派生需求父任务重筹办治理请求中的父需求主体、目标宿主和场景读取改为调用 `需求类` helper。
```

构建前预测：

```text
未新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过；若失败，优先检查治理请求字段类型是否接受 `需求类` 解析出的存在 / 场景节点。
```

## 40. P0.22 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_自我线程父需求回查活动子需求回迁_build.log` 显示“已成功生成”且 0 个错误。
父需求回查、活动子需求扫描、任务化意图准入和派生需求父任务重筹办治理请求中的本轮需求主信息读取已回迁。
```

当前判断：

```text
自我线程父需求回查 / 活动子需求小切片闭合；下一步继续自我线程后续来源需求状态读取与写侧状态回写分类。
```

## 41. P0.23 自我线程来源需求状态读取与写侧分类构建前预测

已改文件：

```text
自我线程模块.impl.cpp
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
1. 安全因果风险树维护入口中的来源需求当前状态 / 目标状态读取改为调用 `需求类` helper。
```

已登记的独立缺口：

```text
`自我线程模块.impl.cpp:6934-6989` 仍直接写来源需求当前状态、截止时间、累计结算和最近结算摘要，并在日志中读取结算摘要。
该段是来源需求收束写侧，不能混入只读 helper 回迁；后续需要单独按需求类更新入口 / 结算摘要清理切片处理。
```

构建前预测：

```text
未新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过。
```

## 34. P0.19 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_自我线程候选需求读取回迁_build.log` 显示“已成功生成”且 0 个错误。
`自我线程模块.impl.cpp` 中候选需求状态、满足关系和截止读取已回迁；`候选需求->主信息.` / `候选.需求->主信息.` 专项扫描无命中。
```

当前判断：

```text
自我线程候选需求读取小切片闭合；下一步复核需求对应任务 / 任务列表读取是否已有需求类 helper，缺 helper 时先登记再补。
```

## 35. P0.20 需求任务承接引用读取小切片构建前预测

已改文件：

```text
需求类.h
需求类.cpp
自我线程模块.impl.cpp
说明书/函数功能说明文档.md
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
1. `需求类` 新增 `读取需求对应任务引用` 和 `读取需求任务列表引用` 只读 helper。
2. `自我线程模块.impl.cpp` 中需求对应任务 / 任务列表读取改为调用 `需求类` helper。
3. `自我线程模块.impl.cpp` 对需求对应任务 / 任务列表直接读取专项扫描无命中。
```

构建前预测：

```text
函数说明文档生成应通过；Debug x64 构建应通过。若失败，优先检查新增 helper 声明 / 定义签名和 `任务节点类` 可见性。
```

## 36. P0.20 构建结果

验证命令：

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
函数说明文档生成：退出码 0，函数条目 7148。
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_需求任务承接引用回迁_build.log` 显示“已成功生成”且 0 个错误。
`自我线程模块.impl.cpp` 对需求对应任务 / 任务列表直接读取专项扫描无命中。
```

当前判断：

```text
需求任务承接引用读取小切片闭合；下一步继续自我线程活动需求截止 / 状态 / 满足关系读取段，写侧状态回写另行登记。
```

## 37. P0.21 自我线程满足关系与直接收束截止小切片构建前预测

已改文件：

```text
自我线程模块.impl.cpp
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
1. 叶子任务正式结算证据摘要中的来源需求满足关系读取改为调用 `需求类::读取需求满足关系`。
2. 已完成叶需求直接收束入口中的需求截止读取改为调用 `需求类::需求已截止`。
3. 后续函数段中的截止、状态回写和日志显示命中继续拆分。
```

构建前预测：

```text
未新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过。
```

## 32. P0.18 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_自我线程截止阻塞权重回迁_build.log` 显示“已成功生成”且 0 个错误。
`自我线程模块.impl.cpp` 中 3490 / 3491 / 4284 / 4337 / 4375 的需求主信息读取已回迁。
```

当前判断：

```text
自我线程截止 / 阻塞 / 权重读取小切片闭合；下一步继续候选需求状态 / 满足关系读取段。
```

## 33. P0.19 自我线程候选需求读取小切片构建前预测

已改文件：

```text
自我线程模块.impl.cpp
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
1. 横向候选需求目标状态、当前状态读取改为调用 `需求类::解析需求目标状态` / `需求类::解析需求当前状态`。
2. 横向候选需求满足关系读取改为调用 `需求类::读取需求满足关系`。
3. 横向候选需求截止判断改为调用 `需求类::需求已截止`。
4. `候选需求->主信息.` / `候选.需求->主信息.` 专项扫描无命中。
```

构建前预测：

```text
未新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过。
```

## 30. P0.17 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_自我线程来源需求解析回迁_build.log` 显示“已成功生成”且 0 个错误。
`自我线程模块.impl.cpp` 中 2768 / 2773 / 2778-2782 / 2795 / 2800 / 3087 / 3090 / 3093 的需求主信息读取已回迁。
```

当前判断：

```text
自我线程来源需求解析第一小切片闭合；下一步继续自我线程中后续相邻机器逻辑读取点，不处理写侧状态回写。
```

## 31. P0.18 自我线程截止阻塞权重小切片构建前预测

已改文件：

```text
自我线程模块.impl.cpp
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
1. `私有_计算任务承接权重上限_线程侧` 中来源需求安全 / 服务权重读取改为调用 `需求类` helper。
2. 任务满足反馈收束判定中的来源需求截止读取改为调用 `需求类::需求已截止`。
3. 父任务缺口判断中的来源需求阻塞读取改为调用 `需求类::需求阻塞父任务执行`。
4. 后段状态回写、结算摘要和日志显示命中后续单独切片处理。
```

构建前预测：

```text
未新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过。
```

## 28. P0.16 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_自我动作模块需求读取点回迁_build.log` 显示“已成功生成”且 0 个错误。
`自我动作实现模块.ixx` 中需求当前状态读取和目标特征类型读取点已清理；剩余命中为 `12335-12340` 父需求结算摘要写侧缺口。
```

当前判断：

```text
`自我动作实现模块.ixx` 可直接读取点小切片闭合；结算摘要写侧缺口已登记，后续不阻塞继续处理 `自我线程模块.impl.cpp` 的机器逻辑读取切片。
```

## 29. P0.17 自我线程来源需求解析第一小切片构建前预测

已改文件：

```text
自我线程模块.impl.cpp
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
1. `私有_需求目标宿主基础节点_线程侧` 中来源需求目标状态、当前状态和目标宿主读取改为调用 `需求类` helper。
2. `私有_需求目标特征节点_线程侧` 中来源需求目标状态和当前状态读取改为调用 `需求类` helper。
3. `私有_收集叶子任务正式结算证据_线程侧` 中 before / target 状态和来源需求收束判断改为调用 `需求类` helper。
4. 本切片只处理相邻解析函数组；自我线程后续结算、任务列表和状态回写命中后续拆分。
```

构建前预测：

```text
未新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过。
```

## 26. P0.15 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_自检线程安全根需求截止回迁_build.log` 显示“已成功生成”且 0 个错误。
`自检线程模块.impl.cpp` 对需求主信息外部直读专项扫描无命中。
```

当前判断：

```text
`自检线程模块.impl.cpp` 安全根需求截止小切片闭合；下一步处理 `自我动作实现模块.ixx` 中当前状态读取，结算摘要写入先分类后处理。
```

## 27. P0.16 自我动作模块读取点小切片构建前预测

已改文件：

```text
自我动作实现模块.ixx
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
1. `任务提交前一结果状态` 中需求当前状态读取改为调用 `需求类::解析需求当前状态`。
2. `需求节点目标是OR组_动作模块` 中目标特征类型读取改为调用 `需求类::读取需求目标特征类型`。
```

已登记的独立缺口：

```text
`自我动作实现模块.ixx:12335-12340` 仍直接写父需求结算摘要字段并读取起点需求最近结算摘要。
该段属于需求主信息写侧 / 兼容结算摘要字段，不与当前只读 helper 回迁混改；后续需单独按需求类更新入口或结算摘要清理计划处理。
```

构建前预测：

```text
未新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过。
```

## 24. P0.14 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_自我类来源需求权重回迁_build.log` 显示“已成功生成”且 0 个错误。
`自我类.cpp` 对 `来源需求->主信息.(服务权重|安全权重)` 及需求主信息外部直读专项扫描无命中。
```

当前判断：

```text
`自我类.cpp` 来源需求权重小切片闭合；下一步处理 `自检线程模块.impl.cpp` 中安全根需求截止读取。
```

## 25. P0.15 自检线程安全根需求截止小切片构建前预测

已改文件：

```text
自检线程模块.impl.cpp
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
1. `自检线程模块.impl.cpp` 中安全根需求截止判断改为调用 `需求类::需求已截止`。
2. `自检线程模块.impl.cpp` 对需求主信息外部直读专项扫描无命中。
```

构建前预测：

```text
未新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过。
```

## 22. P0.13 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_任务管理界面剩余需求读取回迁_build.log` 显示“已成功生成”且 0 个错误。
`任务模块.管理界面线程.impl.cpp` 对 `需求->主信息.` / `来源需求->主信息.` / `新需求->主信息.` / `父需求->主信息.` / `子需求->主信息.` / `需求节点->主信息.` / `当前->主信息.` 的扫描无命中。
```

当前判断：

```text
任务管理界面线程当前范围小切片闭合；下一步按小文件优先处理 `自我类.cpp` 中来源需求安全 / 服务权重读取。
```

## 23. P0.14 自我类来源需求权重小切片构建前预测

已改文件：

```text
自我类.cpp
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
1. `自我类::确保根任务_` 中来源需求服务 / 安全权重读取改为调用 `需求类::读取需求服务权重` 和 `需求类::读取需求安全权重`。
2. 保留原有优先级规则：服务权重非 0 时优先使用服务权重，否则使用安全权重。
3. `自我类.cpp` 对来源需求服务 / 安全权重直读扫描无命中。
```

构建前预测：

```text
未新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过。
```

## 21. P0.13 任务管理界面线程目标区域与重绑定小切片构建前预测

已改文件：

```text
任务模块.管理界面线程.impl.cpp
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
1. `私有_按方法供料要求构造外设观察等待项` 中目标区域 / 目标簇读取改为调用 `需求类::读取需求目标宿主引用` 和 `需求类::解析需求目标宿主`。
2. `私有_提交同目标任务目标升级` 中新需求目标状态读取、任务目标状态赋值和新基准优先级读取改为调用 `需求类` helper。
3. `任务模块.管理界面线程.impl.cpp` 对 `需求->主信息.` / `来源需求->主信息.` / `新需求->主信息.` / `父需求->主信息.` / `子需求->主信息.` / `需求节点->主信息.` / `当前->主信息.` 的扫描无命中。
```

构建前预测：

```text
未新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过。
```

## 21. P0.12 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_任务管理界面签名匹配回迁_build.log` 显示“已成功生成”。
```

当前判断：

```text
任务管理界面线程签名匹配小切片闭合；下一步处理同文件目标区域输出和新需求重绑定路径直读。
```

## 19. P0.11 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_任务管理界面任务镜像回迁_build.log` 显示“已成功生成”。
```

当前判断：

```text
任务管理界面线程任务镜像小切片闭合；下一步继续同文件处理需求签名和可复用需求匹配中的直读。
```

## 20. P0.12 任务管理界面线程签名匹配小切片构建前预测

已改文件：

```text
任务模块.管理界面线程.impl.cpp
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
1. `私有_构造同目标任务签名` 中目标状态、目标宿主、需求场景和满足关系读取改为调用 `需求类` helper。
2. `私有_需求仍可复用_任务管理` 中需求有效截止读取改为调用 `需求类::读取需求有效截止`。
```

构建前预测：

```text
未新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过。
```

## 15. P0.9 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_任务筹办满足关系回迁_build.log` 显示“已成功生成”。
`任务模块.筹办.ixx` 中 `来源需求->主信息.满足关系` 直读已清零。
```

当前判断：

```text
任务筹办剩余满足关系小切片闭合；`任务模块.筹办.ixx` 下一步处理需求有效截止、阻塞父任务执行和安全 / 服务权重读取。
```

## 16. P0.10 任务筹办截止阻塞权重小切片构建前预测

已改文件：

```text
需求类.h
需求类.cpp
任务模块.筹办.ixx
说明书/函数功能说明文档.md
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
1. `需求类` 新增 `读取需求有效截止`、`需求已截止`、`需求阻塞父任务执行`。
2. `任务模块.筹办.ixx` 中需求有效截止、阻塞父任务执行、安全权重、服务权重关键字段读取已改为调用 `需求类` helper。
3. `任务模块.筹办.ixx` 对目标状态、当前状态、目标宿主、满足关系、截止、阻塞、权重等需求主信息关键字段直读扫描无命中。
```

构建前预测：

```text
Debug x64 构建应通过；若失败，优先检查新增 helper 声明 / 定义和函数说明文档生成后的行号漂移无关性。
```

## 17. P0.10 构建结果

验证命令：

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
函数说明文档生成：退出码 0，函数条目 7144。
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_任务筹办截止阻塞权重回迁_build.log` 显示“已成功生成”。
`任务模块.筹办.ixx` 对目标状态、当前状态、目标宿主、满足关系、截止、阻塞、权重等需求主信息关键字段直读扫描无命中。
```

当前判断：

```text
任务筹办 P0 小切片当前范围闭合；下一步转入 `任务模块.管理界面线程.impl.cpp` 或 `自我动作实现模块.ixx` 的较小机器逻辑直读切片。
```

## 18. P0.11 任务管理界面线程任务镜像小切片构建前预测

已改文件：

```text
任务模块.管理界面线程.impl.cpp
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
1. `私有_推导承载场景` 中需求场景读取改为 `需求类::解析需求场景`。
2. `私有_用需求初始化任务虚拟存在` 中需求主体、目标宿主、当前状态、目标状态、需求场景、满足关系、安全 / 服务 / 综合权重读取改为调用 `需求类` helper。
3. `私有_准备任务运行壳` 中目标状态、实际结果状态和基准优先级读取改为调用 `需求类` helper。
```

构建前预测：

```text
未新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过。
```

## 13. P0.8 构建结果

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
Debug x64 构建：退出码 0，日志 `日志/20260621_D0_任务筹办目标主体回迁_build.log` 显示“已成功生成”。
```

当前判断：

```text
任务筹办目标主体推导小切片闭合；`任务模块.筹办.ixx` 下一步处理剩余满足关系读取，其中回执赋值为机器字段，日志输出只作显示。
```

## 14. P0.9 任务筹办剩余满足关系小切片构建前预测

已改文件：

```text
任务模块.筹办.ixx
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
1. `进入筹办` 日志改为读取 `需求上下文.满足关系`。
2. 筹办回执 `目标满足关系` 和枚举目标校验改为调用 `需求类::读取需求满足关系`。
3. `任务模块.筹办.ixx` 中 `来源需求->主信息.满足关系` 直读已清零。
```

构建前预测：

```text
未新增函数，不需要重新生成函数说明文档。Debug x64 构建应通过。
```

## 19. P2 目标语义与获取途径收口判定

证据表：

```text
实施记录/20260621_D2_需求目标语义获取途径收口判定表.md
```

已运行扫描：

```powershell
rg -n "枚举_需求获取途径|结构_需求获取途径语义视图|需求获取途径语义视图|需求获取途径文本|获取途径名称|语义名称|需求目标语义文本|普通候选方法筹办禁止原因|结构_需求目标语义视图" -g "*.cpp" -g "*.h" -g "*.ixx" -g "!x64/**" -g "!Debug/**" -g "!日志/**" -g "!运行输出/**"
rg -n "语义名称\s*==|语义名称\s*!=|获取途径名称\s*==|获取途径名称\s*!=|strcmp\([^\n]*(语义名称|获取途径名称)|需求目标语义文本\([^\n]*\)\s*==|需求获取途径文本\([^\n]*\)\s*==" -g "*.cpp" -g "*.h" -g "*.ixx" -g "!x64/**" -g "!Debug/**" -g "!日志/**" -g "!运行输出/**"
rg -n "需求获取途径语义视图\([^\n]*\)|需求目标语义_按获取途径\([^\n]*\)|逻辑组织需求类型_按获取途径\([^\n]*\)|需求获取途径默认目标特征类型\([^\n]*\)|构造获取途径逻辑组织需求更新指令\([^\n]*\)" -g "*.cpp" -g "*.h" -g "*.ixx" -g "!x64/**" -g "!Debug/**" -g "!日志/**" -g "!运行输出/**"
```

当前判断：

```text
P2 当前范围没有发现 `语义名称` / `获取途径名称` 参与机器判断。
获取途径的正式消费点是入树前生成规则：`自我线程模块.impl.cpp:10101-10165` 传枚举，`需求类.cpp:1999-2014` 转更新指令。
入树后的目标语义由 `需求类::需求目标语义视图(目标特征类型)` 读取目标特征类型并生成枚举 / 布尔视图。
控制面板、SQL 和日志中的文本显示点继续归 P6 后置，不作为 P2 阻塞。
本切片未改源码，未新增 / 删除 / 改名函数，不运行函数说明生成和构建。
```

下一步：

```text
转入 P3 更新指令协议简化预切片。
```

## 20. P3 更新指令协议简化预判定

证据表：

```text
实施记录/20260621_D3_需求树更新指令协议简化预判定表.md
```

已运行扫描：

```powershell
rg -n "结构_需求树更新指令|枚举_需求树更新指令类型|执行更新指令|构造.*需求.*更新指令" -g "*.cpp" -g "*.h" -g "*.ixx" -g "!x64/**" -g "!Debug/**" -g "!日志/**" -g "!运行输出/**"
rg -n "结构_需求树更新指令 指令\{\}|结构_需求树更新指令 指令\{\};|指令\.指令类型 = 枚举_需求树更新指令类型" 自我线程模块.impl.cpp
```

当前判断：

```text
统一 `结构_需求树更新指令` 仍是消息协议和 `需求类::执行更新指令` 的必要载荷，不能直接删除。
外部手写构造点主要集中在 `自我线程模块.impl.cpp`，可按满足 / 删除、更新、新增原子需求三组分小切片收口。
第一代码切片选择 P3.1：满足 / 删除专用构造函数，迁移三处字段量小且验证边界明确。
```

P3.1 构建前预测：

```text
新增需求类构造函数后必须运行 `python .\tools\生成函数功能说明文档.py`。
Debug x64 构建应通过；若失败，优先检查新增函数声明 / 定义签名以及三处调用点的返回类型。
```

## 21. P3.1 满足 / 删除专用构造函数

已改文件：

```text
需求类.h
需求类.cpp
自我线程模块.impl.cpp
说明书/函数功能说明文档.md
```

已完成的代码动作：

```text
1. `需求类` 新增 `构造满足需求指令` 和 `构造删除需求指令`，只构造统一更新指令，不直接修改需求树。
2. `自我线程模块.impl.cpp` 中根特征必要条件子需求收束、遗留根子需求删除、根需求差距子需求退出活动集三处改为调用需求类构造函数。
3. `自我线程模块.impl.cpp` 对 `指令.指令类型 = 枚举_需求树更新指令类型::满足需求 / 删除需求` 的手写构造扫描无命中。
```

验证命令：

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\20260621_D3_需求更新满足删除构造函数_build.log
```

验证结果：

```text
函数说明文档生成：退出码 0，函数条目 7152。
Debug x64 构建：退出码 0，日志 `日志/20260621_D3_需求更新满足删除构造函数_build.log` 显示“已成功生成”。
```

下一步：

```text
转入 P3.2 更新专用构造函数。
```

## 22. P3.2 更新专用构造函数

已改文件：

```text
需求类.h
需求类.cpp
自我线程模块.impl.cpp
说明书/函数功能说明文档.md
```

已完成的代码动作：

```text
1. `需求类` 新增 `构造需求权重阻塞更新指令` 和 `构造需求目标关系更新指令`。
2. `自我线程模块.impl.cpp` 中已完成叶需求清权重、观察事实材料子需求状态刷新、派生需求补齐主信息、根特征必要条件子需求重激活、根需求权重更新五处改为调用需求类构造函数。
3. `自我线程模块.impl.cpp` 对 `指令.指令类型 = 枚举_需求树更新指令类型::更新需求` 的手写构造扫描无命中。
```

验证命令：

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\20260621_D3_需求更新专用构造函数_build.log
```

验证结果：

```text
函数说明文档生成：退出码 0，函数条目 7156。
Debug x64 构建：退出码 0，日志 `日志/20260621_D3_需求更新专用构造函数_build.log` 显示“已成功生成”。
```

下一步：

```text
转入 P3.3 新增原子需求专用构造函数。
```

## 23. P3.3 新增原子需求专用构造函数

已改文件：

```text
需求类.h
需求类.cpp
自我线程模块.impl.cpp
说明书/函数功能说明文档.md
```

已完成的代码动作：

```text
1. `需求类` 新增 `构造新增原子需求指令`，只构造统一更新指令，不直接修改需求树。
2. `自我线程模块.impl.cpp` 中自检维护管理分支确保、自检正式需求入树、任务管理派生需求入树、根特征必要条件子需求入树、扫描入账子需求五处改为调用需求类构造函数。
3. `自我线程模块.impl.cpp` 对 `指令.指令类型 = 枚举_需求树更新指令类型` 的手写构造扫描无命中；`构造新增原子需求指令` 调用点为 9091、9441、9927、11510、13322。
4. 本切片未新增平行输入包，未把获取途径写入 `需求主信息类`，未把 `语素集.安全获取词(...)` 读出的文本用于机器查找 / 生成逻辑。
```

验证命令：

```powershell
python .\tools\生成函数功能说明文档.py
rg -n "指令\.指令类型 = 枚举_需求树更新指令类型|构造新增原子需求指令" 自我线程模块.impl.cpp
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\20260621_D3_需求新增原子构造函数_build.log
```

验证结果：

```text
函数说明文档生成：退出码 0，函数条目 7158。
Debug x64 构建：退出码 0，日志 `日志/20260621_D3_需求新增原子构造函数_build.log` 显示“已成功生成”。
```

当前判断：

```text
P3 三组外部手写更新指令构造已收口；统一 `结构_需求树更新指令` 仍保留为消息协议和需求类统一写入口载荷。
下一步按计划索引转入 P7 需求类自身跨类读取回迁首轮分类。
```

## 24. P7 需求类自身跨类读取首轮分类预测

当前切片：

```text
P7 只做 `需求类.h` / `需求类.cpp` 中直接读取非需求类主信息字段的首轮分类。
本切片先不改代码；若发现阻塞性跨类函数缺口，按“缺哪个类补哪个类第一优先”转入对应计划。
```

预测：

```text
1. 扫描会命中状态节点主信息、特征节点主信息、二次特征主信息、基础信息树父子链和 SQL / 显示投影混合路径。
2. `重写需求树SQL投影` 和 `语素集.安全获取词(...)` 相关文本只应归显示 / SQL 后置，不得作为机器查找或生成逻辑。
3. 机器逻辑中若需要状态主体、状态特征类型、状态特征值，应优先检查状态类是否已有函数；缺函数时转入状态类计划。
4. 机器逻辑中若需要二次特征方向签名或比较方向，应优先检查二次特征类是否已有结构化读取函数；不得用文本方向词替代。
```

拟运行扫描：

```powershell
rg -n -- "(?:->主信息|\.主信息\.)" 需求类.h 需求类.cpp
rg -n "状态主信息_已加锁|状态特征类型主键_已加锁|生成需求方向签名|校验枚举目标|生成任务初始化上下文|重写需求树SQL投影|语素集\.安全获取词" 需求类.h 需求类.cpp
```

## 25. P7 首轮分类结果

分类表：

```text
实施记录/20260621_D7_需求类自身跨类读取分类表.md
```

扫描结果：

```text
`日志/20260621_D7_需求类跨类主信息读取扫描.txt`：103 行命中。
`日志/20260621_D7_需求类P7锚点扫描.txt`：23 行命中。
```

当前判断：

```text
1. `需求类.cpp:147` 的 `语素集.安全获取词(...)` 只在 SQL 投影中生成显示节点名，不作为机器查找 / 生成逻辑。
2. `需求类.h:719-740`、`需求类.h:845-875`、`需求类.h:995-1019`、`需求类.cpp:2482-2523` 是第一优先机器逻辑缺口：需求类直接拆状态 / 特征主信息来生成目标键和任务初始化上下文。
3. 状态类已有 pointer 级读取函数，但缺少保留主键回退的可解析引用级状态主体 / 状态特征 / 状态值读取入口。
4. 特征类已有实例级 `读取特征语义类型`，但需求类头文件内不能安全绑定基础信息实例，缺少不创建不修改的静态特征类型读取入口。
```

下一步：

```text
按“缺哪个类补哪个类第一优先”临时提升：
    计划/20260620_状态类逻辑纠偏计划_v0.1.md 的 1.5；
    计划/20260620_特征类逻辑纠偏计划_v0.1.md 的 1.4。
闭合后返回 需求类 P7a，用状态 / 特征类新入口回迁需求类目标材料读取。
```

## 26. P7a 状态 / 特征目标材料读取回迁构建前预测

已改文件：

```text
状态类.h
状态类.cpp
特征类.h
特征类.cpp
需求类.h
需求类.cpp
计划/计划索引.md
计划/20260620_状态类逻辑纠偏计划_v0.1.md
计划/20260620_特征类逻辑纠偏计划_v0.1.md
实施记录/20260621_D7_需求类自身跨类读取分类表.md
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
1. 状态类新增静态只读 `读取状态主体特征和值引用`，复制状态主体 / 状态特征的可解析引用和状态值。
2. 特征类新增静态只读 `读取特征语义类型_直接`，返回特征语义类型，不使用文本词面。
3. 需求类目标键、状态补齐、任务初始化上下文已改用状态 / 特征类入口；本切片不处理 `获取需求方向` 的二次特征生成路径。
```

构建前预测：

```text
函数说明文档生成应通过；Debug x64 构建应通过。
若失败，优先检查 `需求类.h` 新增 include 的模块依赖、静态函数声明 / 定义签名，以及 `可解析引用<特征节点类>` 的可见性。
```

验证命令：

```powershell
python .\tools\生成函数功能说明文档.py
rg -n "状态主信息_已加锁|dynamic_cast<const 状态节点主信息类|dynamic_cast<const 特征节点主信息类|主体->主信息" 需求类.h 需求类.cpp
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\20260621_P7a_状态特征目标材料读取_build.log
```

验证结果：

```text
函数说明文档生成：退出码 0，函数条目 7161。
旧 direct cast 扫描：退出码 1，无 `状态主信息_已加锁` / 状态主信息 direct cast / 特征主信息 direct cast / `主体->主信息` 命中。
Debug x64 构建：退出码 0，日志 `日志/20260621_P7a_状态特征目标材料读取_build.log` 显示“已成功生成”。
```

当前判断：

```text
P7a 状态 / 特征目标材料读取回迁闭合；返回需求类 P7，下一步处理二次特征方向签名跨类读取。
```

## 27. P7b 二次特征方向签名预判定

预判定表：

```text
实施记录/20260621_D7b_二次特征方向签名预判定表.md
```

当前判断：

```text
1. `需求类.cpp:1733-1780` 直接读取 二次特征主信息类::来源结果状态 / 来源起始状态 / 主体 / 允许方向 / 方向区间 / 差值 / 值形态 / 标量值。
2. `需求类.cpp:1784-1854` 的方向比较消费结构化签名，可保留。
3. 二次特征类已有 `读取状态迁移来源` 和 `读取可融合标量区间`，但无法一次性返回方向签名所需的目标身份、方向掩码、方向区间、差值和标量值。
```

下一步：

```text
按“缺哪个类补哪个类第一优先”临时提升 计划/20260620_二次特征类逻辑纠偏计划_v0.1.md 的 1.4。
闭合后返回需求类 P7b，用二次特征类新入口回迁 `生成需求方向签名`。
```

## 28. P7b 二次特征方向签名材料读取回迁构建前预测

已改文件：

```text
二次特征类.h
二次特征类.cpp
需求类.cpp
计划/计划索引.md
计划/20260620_二次特征类逻辑纠偏计划_v0.1.md
实施记录/20260621_D7b_二次特征方向签名预判定表.md
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
1. 二次特征类新增 `结构_二次特征方向签名材料` 和静态只读 `读取方向签名材料_直接`。
2. 需求类 `生成需求方向签名` 改为消费二次特征类返回的结构化材料，不再直接解包 `比较主信息->...` 字段。
3. 本切片不处理方法类生产者查询和枚举目标校验。
```

构建前预测：

```text
函数说明文档生成应通过；Debug x64 构建应通过。
若失败，优先检查 `结构_二次特征方向签名材料` 的可见性、`<optional>` include、以及 `需求类.cpp` include 顺序。
```

验证命令：

```powershell
python .\tools\生成函数功能说明文档.py
rg -n "比较主信息->|读取方向签名材料_直接|结构_二次特征方向签名材料" 二次特征类.h 二次特征类.cpp 需求类.cpp 说明书\函数功能说明文档.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\20260621_P7b_二次特征方向签名材料_build.log
```

验证结果：

```text
函数说明文档生成：退出码 0，函数条目 7163。
局部扫描：`需求类.cpp` 中无 `比较主信息->...` 直接解包；命中只剩二次特征类入口、需求类调用和函数说明文档。
Debug x64 构建：退出码 0，日志 `日志/20260621_P7b_二次特征方向签名材料_build.log` 显示“已成功生成”。
```

当前判断：

```text
P7b 二次特征方向签名材料读取回迁闭合；返回需求类 P7，下一步处理方法类生产者查询跨类读取。
```
## 45. P7c 方法类生产者查询回迁构建前预测

当前切片：

```text
需求类计划 P7c：方法类生产者查询跨类读取预判定与最小回迁。
```

依据：

```text
1. `计划/计划索引.md` 当前主动队列指向 P7c。
2. `计划/20260620_需求类逻辑纠偏计划_v0.1.md` 要求需求类自身跨类读取回迁。
3. `计划/20260620_方法类逻辑纠偏计划_v0.1.md` 已把需求类中的方法引用解析和方法结果能力生产目标判断旧 helper 列为必须回迁的机器逻辑。
4. `规范/禁止性规范20260611.md` 要求机器逻辑不得由自由文本、日志或说明字段承载。
```

当前判断：

```text
1. `需求类.cpp` 中 `私有_方法首节点生产目标特征` 与 `方法类::方法首节点生产目标特征` 语义重复，可直接回迁。
2. `需求类.cpp` 中 `私有_方法树存在生产者` 自行遍历方法树并解释方法能力，属于方法类职责；方法类当前没有完整树级生产者查询入口，需要按“缺哪个类补哪个类第一优先”补一个窄入口。
3. `语素集.安全获取词(...)` 在当前 P7c 机器逻辑中不应参与查找或生成，生产者判定只读取方法结果项的目标特征入口和方向结构。
```

构建前预测：

```text
1. 新增 `方法类::方法树存在生产目标特征` 后，需求类可删除本地重复的结果项 / 首节点 / 方法树生产者 helper。
2. Debug x64 构建应通过；若失败，优先检查新增声明 / 定义签名、`方法节点类` 与 `方法类::节点类` 类型一致性，以及需求类替换后的函数引用。
```

禁止宣称：

```text
不宣称需求类逻辑纠偏完成。
不混入枚举目标校验切片。
不把 `语素集.安全获取词(...)` 读出的文本用于机器查找 / 生成逻辑。
```

## 48. P7d 枚举目标校验回迁构建结果

已改文件：

```text
特征类.h
特征类.cpp
需求类.cpp
说明书/函数功能说明文档.md
```

已完成的代码动作：

```text
1. `特征类` 新增 `抽象特征是离散点枚举` 与 `特征值命中抽象特征值域` 两个只读静态入口。
2. `需求类.cpp::校验枚举目标合法性` 改为调用特征类入口判断枚举目标和值域命中。
3. `需求类.cpp` 删除本地 I64 点值域、抽象特征离散点枚举、枚举子状态值域命中 helper。
4. `私有_解析状态节点_需求校验` / `私有_解析状态特征节点_需求校验` / `私有_状态特征类型_需求校验` 改为调用 `状态类::读取状态主体特征和值引用` 与 `特征类::读取特征语义类型_直接`。
5. `结构_枚举目标校验结果::说明` 仅保留为日志摘要文本，不参与机器裁决。
```

验证命令：

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
rg -n "私有_I64值域项是点|私有_值域是I64点值域|私有_特征值命中I64值域|私有_抽象特征是离散点枚举|私有_目标值命中枚举子状态值域" 需求类.cpp
rg -n "说明" 需求类.cpp 需求类.h
```

验证结果：

```text
函数说明文档生成：退出码 0，函数条目 7162。
Debug x64 构建：退出码 0，日志 `日志/20260621_P7d_枚举目标校验回迁_build.log` 显示“已成功生成”。
旧需求类枚举值域 helper 扫描无命中。
`说明` 仅在结构字段、摘要拼接和赋值位置出现，未进入 `if` 条件或机器查找 / 生成逻辑。
```

当前判断：

```text
P7d 闭合；下一步进入 P7e `需求类::获取需求方向` 状态 / 特征跨类读取回迁。
```

## 46. P7c 方法类生产者查询回迁构建结果

已改文件：

```text
方法类.h
方法类.cpp
需求类.cpp
说明书/函数功能说明文档.md
```

已完成的代码动作：

```text
1. `方法类` 新增 `方法树存在生产目标特征`，在方法类内部完整遍历方法树并调用 `方法首节点生产目标特征` 判断生产者。
2. `需求类.cpp` 删除本地重复的 `私有_方法结果项精确生产目标特征`、`私有_方法首节点生产目标特征`、`私有_查找方法节点_按主键_需求校验`、`私有_解析方法引用_需求校验`。
3. `需求类.cpp::私有_方法树存在生产者` 改为只取得自我方法根节点后调用 `方法类::方法树存在生产目标特征`。
```

验证命令：

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
rg -n "私有_方法结果项精确生产目标特征|私有_方法首节点生产目标特征|私有_查找方法节点_按主键_需求校验|私有_解析方法引用_需求校验|能力\.结果包\.结果项集" 需求类.cpp
```

验证结果：

```text
函数说明文档生成：退出码 0，函数条目 7161。
Debug x64 构建：退出码 0，日志 `日志/20260621_P7c_方法类生产者查询_build.log` 显示“已成功生成”。
旧需求类 helper 与 `需求类.cpp` 直接读取 `能力.结果包.结果项集` 扫描无命中。
```

当前判断：

```text
P7c 闭合；机器查找未使用 `语素集.安全获取词(...)` 词面文本，生产者判定通过方法类结构化结果能力读取完成。下一步进入 P7d 枚举目标校验跨类读取与说明字段机器边界预判定。
```

## 47. P7d 枚举目标校验预判定

当前切片：

```text
需求类计划 P7d：枚举目标校验跨类读取与说明字段机器边界预判定。
```

已复核代码范围：

```text
需求类.cpp:1027-1039 私有_构造枚举目标校验摘要
需求类.cpp:2115-2204 校验枚举目标合法性 / 校验更新指令枚举目标
需求类.cpp:797-907 枚举目标值域与抽象特征 helper
```

当前判断：

```text
1. `结构_枚举目标校验结果::说明` 只在错误日志摘要中输出；未参与 `合法`、`生产者分级`、`有合法生产者` 或入树拒绝条件。
2. 当前机器判定读取的是结构化字段：目标特征入口、状态值、满足关系、枚举目标布尔、值域命中布尔、生产者分级。
3. 需求类仍自写抽象特征离散点枚举和值域命中逻辑，并直接读取抽象特征主信息；该能力属于特征类，按“缺哪个类补哪个类第一优先”临时提升特征类窄入口。
```

构建前预测：

```text
1. 在 `特征类` 新增只读静态入口后，需求类可删除本地 `私有_I64值域项是点`、`私有_值域是I64点值域`、`私有_特征值命中I64值域`、`私有_抽象特征是离散点枚举`、`私有_目标值命中枚举子状态值域`。
2. Debug x64 构建应通过；若失败，优先检查新增特征类声明 / 定义签名和需求类调用点。
```

禁止宣称：

```text
不宣称需求类逻辑纠偏完成。
不改需求树语义。
不让说明文本参与机器判断。
不把 `语素集.安全获取词(...)` 读出的文本用于机器查找 / 生成逻辑。
```

## 48. P7e 获取需求方向状态特征回迁预判定

当前切片：

```text
需求类计划 P7e：`需求类::获取需求方向` 状态 / 特征跨类读取回迁。
```

已复核代码范围：

```text
需求类.cpp:2261-2381 `需求类::获取需求方向`
状态类.h/.cpp `读取状态主体特征和值引用` / `读取状态排序时间`
特征类.h/.cpp `读取特征语义类型_直接` / `获取比较方法结果`
```

当前判断：

```text
1. `获取需求方向` 当前仍直接读取 `状态节点主信息类` 的状态主体、状态特征、状态值和时间。
2. 当前仍在需求类中取得 `特征节点主信息类` 并解释特征类型；比较入口依赖特征主信息保留连续测量误差区间。
3. 状态主体、状态特征、状态值可回迁到 `状态类::读取状态主体特征和值引用`；状态时间可回迁到 `状态类::读取状态排序时间`。
4. 需要在 `特征类` 增加窄入口，由特征类内部按特征引用读取类型和误差区间后完成比较，避免需求类继续解释特征主信息。
```

构建前预测：

```text
1. `需求类.cpp::获取需求方向` 不再调用 `世界树.取状态主信息`、`私有_取特征主信息`，也不再直接读取 `状态主体`、`状态特征`、`状态值`、`发生时间` 或 `收到时间` 字段。
2. 方向生成仍使用项目结构：状态引用、特征节点引用、特征值、语素入口同一性、二次特征模板和世界树二次特征写入；不使用词面文本。
3. Debug x64 构建应通过；若失败，优先检查新增特征类入口声明 / 定义签名和 `需求类.cpp` 调用点。
```

禁止宣称：

```text
不宣称需求类逻辑纠偏完成。
不改需求树语义。
不把 `语素集.安全获取词(...)` 读出的文本用于机器查找 / 生成逻辑。
```

## 49. P7e 获取需求方向状态特征回迁构建结果

已改文件：

```text
特征类.h
特征类.cpp
需求类.cpp
说明书/函数功能说明文档.md
计划/20260620_需求类逻辑纠偏计划_v0.1.md
计划/计划索引.md
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
1. `特征类` 新增 `比较同语义特征值`，由特征类内部按特征引用解析特征节点、读取特征类型和连续测量误差区间后调用既有比较入口。
2. `需求类::获取需求方向` 改为用 `状态类::读取状态主体特征和值引用` 读取状态主体 / 状态特征 / 状态值，用 `世界树.状态().读取状态排序时间` 读取来源时间段。
3. 删除需求类本地 `私有_读取状态时间`、`私有_取特征主信息`。
4. SQL 投影根需求显示名分支改为比较 `自我特征定义类::类型_自我_安全值()` / `类型_自我_服务值()`，`语素集.安全获取词(...)` 只保留为最终显示文本。
```

验证命令：

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
Select-String -Path '需求类.cpp' -Pattern '== "安全值"|== "服务值"|节点名称 ==|安全获取词\(需求类::读取需求目标特征类型|私有_取特征主信息\(|私有_读取状态时间\('
```

验证结果：

```text
函数说明文档生成：退出码 0，函数条目 7162。
Debug x64 构建：退出码 0，日志 `日志/20260621_P7e_获取需求方向回迁_build.log` 显示“已成功生成”，0 个错误。
需求类旧本地 helper、词面根需求分支和 `安全获取词(读取需求目标特征类型(...))` 扫描无命中。
```

当前判断：

```text
P7e 闭合；`获取需求方向` 的方向生成仍走状态引用、特征值比较、二次特征模板和世界树写入，不再由需求类解释状态 / 特征主信息字段，不使用 `语素集.安全获取词(...)` 词面文本做机器查找或生成。
下一步复扫需求类自身跨类读取剩余路径，选择下一最小闭合切片。
```

## 50. P7f 状态主体主键需求方向回迁预判定

当前切片：

```text
需求类计划 P7f：`私有_状态主体主键_需求方向` 状态主体主键读取回迁。
```

已复核代码范围：

```text
需求类.cpp:683-689 `私有_状态主体主键_需求方向`
需求类.cpp:1447-1484 `生成需求方向签名`
日志/20260621_P7_需求类自身跨类读取复扫.txt
```

当前判断：

```text
1. `私有_状态主体主键_需求方向` 仍通过 `世界树.取状态主信息` 读取 `状态主体`。
2. 该读取只需要状态主体引用主键，已有 `状态类::读取状态主体特征和值引用` 可直接提供状态主体引用。
3. 本切片不新建函数，不改变需求方向签名语义。
```

构建前预测：

```text
1. `私有_状态主体主键_需求方向` 不再调用 `世界树.取状态主信息`。
2. Debug x64 构建应通过；若失败，优先检查 `状态类::读取状态主体特征和值引用` 调用签名。
```

## 51. P7f 状态主体主键需求方向回迁构建结果

已改文件：

```text
需求类.cpp
计划/20260620_需求类逻辑纠偏计划_v0.1.md
计划/计划索引.md
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
`私有_状态主体主键_需求方向` 改为调用 `状态类::读取状态主体特征和值引用` 读取状态主体引用，并由既有引用主键 helper 生成主键。
```

验证命令：

```powershell
Select-String -Path '需求类.cpp' -Pattern '世界树\.取状态主信息|取状态主信息\('
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
`需求类.cpp` 中 `世界树.取状态主信息` / `取状态主信息(` 扫描无命中。
Debug x64 构建：退出码 0，日志 `日志/20260621_P7f_状态主体主键回迁_build.log` 显示“已成功生成”，0 个错误。
```

当前判断：

```text
P7f 闭合；需求方向签名中的状态主体主键读取不再由需求类解释状态主信息字段。
下一步继续复扫需求类自身跨类读取剩余路径，选择下一最小闭合切片。
```

## 52. P7g 二次特征节点方向签名材料回迁预判定

当前切片：

```text
需求类计划 P7g：`生成需求方向签名(二次特征节点*)` 二次特征主信息读取回迁。
```

已复核代码范围：

```text
需求类.cpp:1449-1497 `生成需求方向签名`
二次特征类.h/.cpp `读取方向签名材料_直接`
日志/20260621_P7_需求类自身跨类读取复扫2.txt
```

当前判断：

```text
1. `生成需求方向签名(const 二次特征节点类*)` 当前通过 `世界树.取二次特征主信息` 取得二次特征主信息，再转给主信息重载。
2. `二次特征类` 已有按主信息读取方向签名材料的静态入口；缺少按二次特征节点读取方向签名材料的窄入口。
3. 本切片应把二次特征节点 -> 主信息 -> 方向签名材料读取封装进二次特征类，需求类只消费 `结构_二次特征方向签名材料`。
```

构建前预测：

```text
1. 新增 `二次特征类::读取方向签名材料_直接(const 二次特征节点类*, ...)`。
2. `需求类::生成需求方向签名(const 二次特征节点类*)` 不再调用 `世界树.取二次特征主信息`。
3. 新增函数后重新生成函数说明文档；Debug x64 构建应通过。
```

## 53. P7g 二次特征节点方向签名材料回迁构建结果

已改文件：

```text
二次特征类.h
二次特征类.cpp
需求类.cpp
说明书/函数功能说明文档.md
计划/20260620_需求类逻辑纠偏计划_v0.1.md
计划/计划索引.md
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
1. `二次特征类` 新增 `读取方向签名材料_直接(const 二次特征节点类*, ...)`，节点到主信息读取封装在二次特征类内部。
2. `需求类.cpp` 新增文件内 `私有_生成需求方向签名_从材料`，两个 `生成需求方向签名` 重载只消费方向签名材料。
3. `生成需求方向签名(const 二次特征节点类*)` 不再调用 `世界树.取二次特征主信息`。
```

验证命令：

```powershell
python .\tools\生成函数功能说明文档.py
Select-String -Path '需求类.cpp' -Pattern '世界树\.取二次特征主信息\(比较节点\)|取二次特征主信息\(比较节点\)'
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
函数说明文档生成：退出码 0，函数条目 7165。
目标扫描无命中。
Debug x64 构建：退出码 0，日志 `日志/20260621_P7g_二次特征方向签名节点读取_build.log` 显示“已成功生成”，0 个错误。
```

当前判断：

```text
P7g 闭合；需求方向签名的二次特征节点读取不再由需求类解释二次特征主信息。
下一步处理 `需求类::获取需求方向` 中二次特征差值回读。
```

## 54. P7h 获取需求方向二次特征差值回读预判定

当前切片：

```text
需求类计划 P7h：`需求类::获取需求方向` 二次特征差值回读回迁。
```

已复核代码范围：

```text
需求类.cpp:2372-2381 `获取需求方向` 差值写入 / 标量写入
二次特征类.cpp:1045-1056 `写入差值结果`
基础信息主信息类.h:703-713 `设置差值结果`
```

当前判断：

```text
1. `获取需求方向` 写入二次特征差值结果后，仍通过 `世界树.取二次特征主信息` 读取 `差值`。
2. `设置差值结果` 会根据左右比较值重新计算并保存差值；直接改用比较结果的差值可能绕开该存储后的饱和 / 区间行为。
3. 应通过 `二次特征类::读取方向签名材料_直接(const 二次特征节点类*, ...)` 回读结构化差值，消除需求类直接解释二次特征主信息。
```

构建前预测：

```text
1. `需求类::获取需求方向` 不再出现 `世界树.取二次特征主信息(节点)`。
2. Debug x64 构建应通过。
```

## 55. P7h 获取需求方向二次特征差值回读构建结果

已改文件：

```text
需求类.cpp
计划/20260620_需求类逻辑纠偏计划_v0.1.md
计划/计划索引.md
实施记录/20260621_需求类逻辑纠偏_Codex断点清单.md
```

已完成的代码动作：

```text
`需求类::获取需求方向` 写入二次特征差值结果后，改为通过 `二次特征类::读取方向签名材料_直接(节点, 差值材料)` 读取结构化差值材料，再写入二次特征标量值。
```

验证命令：

```powershell
rg -n "状态节点主信息类|特征节点主信息类|二次特征主信息类|取状态主信息|取特征主信息|取二次特征主信息|dynamic_cast<.*主信息|->状态值|->状态特征|->状态主体|->类型|->允许方向|->方向区间|->差值|->标量值" 需求类.h 需求类.cpp
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
扫描结果已保存到 `日志/20260621_P7_需求类自身跨类读取复扫3.txt`。
扫描显示 `取状态主信息` / `取特征主信息` / `取二次特征主信息` 读取调用已清零，剩余命中为类型声明、枚举类型引用和 `获取需求方向` 的二次特征模板构造。
Debug x64 构建：退出码 0，日志 `日志/20260621_P7h_获取需求方向二次特征差值回读_build.log` 显示“已成功生成”，0 个错误。
```

当前判断：

```text
P7h 闭合；需求类自身跨类读取剩余命中需做一次收束复核，确认是否还有机器逻辑直读阻塞。
```
