# 20260621 P6 任务模块协议结构审查表

## 1. 审查结论

P6 已完成任务模块协议结构清单和初判：

```text
1. `任务模块.*.ixx` 中任务协议相关定义共 68 行，引用总扫 707 行。
2. 当前未发现可一刀切删除的协议结构；主要分布在实体运行态、工作线程上行消息、筹办回执、治理协议和管理线程协议。
3. `任务模块.*.ixx` 未发现 `语素集.安全获取词(...)`。
4. 当前未发现任务协议文本字段直接作为状态推进、候选选择、派生需求生成或结算机器判断依据。
```

不宣称任务类逻辑纠偏完成。

## 2. 分类表

| 文件 / 结构族 | 分类 | 当前处理 |
| --- | --- | --- |
| `任务模块.实体.ixx` `结构_任务根节点 / 结构_任务控制态 / 结构_任务局部运行态` | 运行态快照 | 保留；用于工作项、工作结果和控制面板可见性 |
| `任务模块.工作线程协议.ixx` `结构_任务管理消息头 / 线程状态 / 派生需求 / 结果状态 / 观察事实承接 / 执行前许可 / 上行消息` | 任务管理上行消息协议 | 保留；枚举、指针、数值为机器字段，文本字段为主键、摘要或日志展示 |
| `任务模块.筹办.ixx` `结构_任务推进回执 / 结构_任务筹办结果` | 任务筹办 / 执行过程回执 | 保留；P5 已审查文本字段边界 |
| `任务模块.治理协议.ixx` `结构_任务管理上下文 / 请求 / 单步决策 / 写回结果 / 一步结果 / 结果` | 任务治理内部结构 | 保留；承载治理枚举、方法需求位、缺口类型、候选、控制记录等结构化字段 |
| `任务模块.管理线程协议.ixx` `结构_任务治理请求 / 状态推进请求 / 工作项 / 工作结果 / 完成项 / 界面承接结果 / 快照` | 管理界面线程协议和工作包 | 保留；区分请求、提交、工作包、结果、快照 |
| `构造*文本` 函数族 | 日志 / 显示投影 | 保留；不得反向成为机器判断 |

## 3. 文本字段边界

```text
协议中的 `主键 / ID / 幂等键` 是跨线程和外设边界的定位字段；当前不在 P6 中替换。
协议中的 `摘要 / 说明 / 原因 / 文本` 只允许作为日志、展示、诊断或人工可读输出。
如果后续发现这些文本字段参与机器分支，按单结构最小切片改为枚举、布尔、特征入口、引用或数值。
```

## 4. 验证证据

```text
任务协议结构清单：
    日志/20260621_P6_任务协议结构清单.txt
    结果：68 行。

任务协议引用总扫：
    日志/20260621_P6_任务协议引用总扫.txt
    结果：707 行。

协议文本字段清单：
    日志/20260621_P6_协议文本字段清单.txt
    结果：181 行。

任务模块安全获取词复扫：
    日志/20260621_P6_任务模块安全获取词复扫.txt
    结果：任务模块安全获取词无命中。

协议文本字段机器比较复扫：
    日志/20260621_P6_协议文本字段机器比较复扫.txt
    结果：未发现任务协议文本字段机器比较；唯一命中为 `自我线程模块.impl.cpp:1177` 的 `摘要.缺失证据` 容器查重，非任务模块协议字段。
```

## 5. 下一步

进入 P7：控制面板 / SQL / 日志后置审查。
