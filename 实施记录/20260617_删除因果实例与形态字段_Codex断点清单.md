# 20260617 删除因果实例与形态字段 Codex断点清单

## 0. 恢复摘要

```text
当前切片：
    用户指定主线：从删除 因果主信息类::形态 字段开始，删除 因果实例主信息类相关代码和逻辑。

依据文件：
    规范/禁止性规范20260611.md
    规范/规范目录.md
    规范/动作动态即因果账本规范20260512.md
    规范/因果补完流程规范20260516.md

本轮目标：
    1. 删除 因果主信息类 中的 枚举_因果形态 形态 字段。
    2. 删除 因果实例主信息类 / 因果实例节点类 相关机器路径。
    3. 按动态补完因果时不再创建因果实例，直接把动态写入因果模板的 证据动态样本。
    4. 因果链查询和表达接口不再返回或依赖 因果实例。
    5. 同步修订规范中旧 因果实例 口径，避免代码与规范冲突。

禁止兼容：
    不保留 因果实例主信息类 别名。
    不保留 创建因果实例 / 取实例主信息 / 枚举因果实例 / 追加证据实例。
    不新增形态替代字段或新旧判断分支。
    不用旧实例字段做查询 fallback。

已知工作区状态：
    已有未提交 D455 相关改动：
        实施记录/20260614_D455扫描跟踪后续事实链_Codex断点清单.md
        自我动作实现.外设模块.ixx
    本切片不提交、不回滚上述文件。

验证预测：
    静态扫描：
        rg -n "因果实例|因果形态|是实例因果|是抽象因果|创建因果实例|取实例主信息|追加证据实例|枚举因果实例" -g "*.cpp" -g "*.h" -g "*.ixx"
            预期：当前机器代码无命中；历史详细设计可保留为归档但不参与机器路径。
    函数文档：
        python .\tools\生成函数功能说明文档.py
            预期：exit_code=0。
    构建：
        msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
            预期：exit_code=0，关键编译错误=0。
    真实运行：
        .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000
            预期：exit_code=0；动态因果生成仍直接写证据动态样本；不新增 exception。

禁止宣称：
    不宣称 D455 自然业务样本完整通过。
    不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 1. 本切片完成记录

```text
已完成修改：
    1. 删除 因果主信息类::形态 字段及 枚举_因果形态。
    2. 删除 因果实例节点类 / 因果实例主信息类 别名和实例层接口路径。
    3. 删除 存在主信息 / 场景主信息 中的实例因果索引和追加函数。
    4. 语素入口删除 因果实例入口，场景索引不再收因果节点。
    5. 因果生成不再创建实例节点，改为向因果模板追加 证据动态样本。
    6. 控制面板、语言表达、筹办统计、二次特征阶段命名改为 因果模板 / 因果证据动态。
    7. 同步规范和说明书，函数功能说明文档已重新生成。

静态扫描：
    rg -n "因果实例|实例因果|因果形态|是实例因果|是抽象因果|创建因果实例|取实例主信息|追加证据实例|枚举因果实例|因果实例节点类|因果实例主信息类|实例因果索引|缺实例因果|已具备实例因果" -g "*.cpp" -g "*.h" -g "*.ixx" -g "!详细设计/**"
        exit_code=1，编译路径无命中。
    rg -n "创建因果实例|枚举因果实例|生成实例因果|追加实例因果|是实例因果|取实例主信息|因果形态|因果实例节点类|实例因果索引|是抽象因果" 说明书/函数功能说明文档.md 说明书/因果类说明.md 说明书/存在类说明.md 说明书/场景类说明.md
        exit_code=1，函数说明和关键说明书无命中。
    rg -n "因果实例|实例因果|因果实例入口|因果形态|枚举因果实例|创建因果实例|追加实例因果|是实例因果|是抽象因果" 规范
        exit_code=1，规范目录无命中。

函数说明文档：
    python .\tools\生成函数功能说明文档.py
        exit_code=0，函数条目=7280。

构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        exit_code=0，0 个警告，0 个错误。

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000
        exit_code=0。
        自检线程运行中，健康=是；日志已写入 ./日志。

剩余说明：
    详细设计/因果类用途接口草案20260516.ixx 是归档草案文件，仍保留历史旧接口示例，不参与编译路径。
    本切片未验证 D455 自然业务样本完整通过。
    本切片不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
