# 20260606_外设四本能方法P7a提交入口因果账本现状对账记录

## 切片

```text
计划：计划/20260605_外设四本能方法需求树生成因果链修订计划_v0.1.md
切片：P7a 提交入口和因果账本现状对账
目标：确认四方法执行结果是否通过提交入口入账，动作动态来源是否绑定明确方法，提交入口是否只写世界事实 / 二次特征 / 状态动态，不直接写安全 / 服务结论。
```

## 读取依据

```text
规范/方法动作动态硬规则规范20260512.md
规范/动作动态即因果账本规范20260512.md
计划/20260605_外设四本能方法需求树生成因果链修订计划_v0.1.md
自我动作实现.外设模块.ixx
```

## 代码证据

```text
写入双目相机I64事实与动态：
    使用 方法首节点 调用 世界树.动态().创建方法动作动态；
    动作名来自 方法首节点->主信息.首节点信息().动作名；
    动态节点写入 目标外设、目标宿主、目标特征类型、当前方法运行存在、输入参数场景、输出结果场景。

读取提交入口上游动作动态：
    优先读取发现来源动作动态、存在特征变化动态、跟踪稳定 / 区间迁移 / 丢失 / 重捕获 / 预测偏差超限动态、归属建立 / 冲突动态；
    最后兜底读取 动作动态；
    不读取线程作为动作来源。

串接提交入口来源动作动态：
    当前提交入口动作动态仍由提交方法自身生成；
    上游动作动态写入当前动态节点和摘要宿主的 来源动作动态；
    当前动态和来源动态相同则跳过，避免自引用。
```

## 四个提交入口

```text
提交_当前观察范围可观测单位存在对应事实：
    消费识别确认状态、确认方案取得状态、可提交 / 冲突 / 证据不足计数；
    写对应事实、映射表、扫描可启动状态、当前场景特征值明确状态；
    日志声明 不读取外设提交包候选事实数量=1、不写需求满足=1、不写安全值=1。

提交_观察存在发现事实：
    消费扫描发现状态、扫描入账状态和当前观察存在；
    写发现事实提交状态、发现状态、发现时间、存在当前特征状态；
    日志声明 第一次扫描只写发现存在或发现特征事实=1、不写变化动态=1、不写需求满足=1、不写安全值=1。

提交_观察存在特征值变化事实：
    消费扫描入账状态、变化结果状态、变化状态；
    只有有实际变化时才提交变化事实；
    写变化事实提交状态、存在当前特征状态、扫描入账状态、变化状态和动作动态；
    日志声明 无变化不提交变化事实=1、不直接消费外设提交包=1、不写需求满足=1、不写安全值=1。

提交_指定存在跟踪事实：
    消费目标存在、上游目标跟踪状态和目标观察窗口状态；
    缺目标不回退当前观察存在；
    写指定存在跟踪事实提交状态、目标存在跟踪状态、目标观察窗口状态和动作动态；
    日志声明 不直接消费外设提交包=1、不写需求满足=1、不写安全值=1。
```

## 结论

```text
P7a 未发现需要立即修改的提交入口结构缺口：
    当前提交入口动作动态来源均为明确提交方法；
    上游观察 / 识别 / 扫描 / 跟踪动作动态通过 来源动作动态 串接；
    提交入口不直接写需求满足、安全值或服务值；
    提交入口不把外设线程当动作来源。

注意：
    特征_需求满足情况 在目标方法实际结果写入 helper 中出现，不属于四个提交入口直接写入；
    P7a 只是静态对账，不等于自然运行链已通过。
```

## 验证状态

```text
P7a 未修改业务代码；
未构建；
未真实运行鱼巢.exe；
不宣称四方法自然业务链已通过。
```

## 下一步

```text
进入 P8a：真实日志验证准备与预测。
先写预测：
    目标特征需求；
    任务筹办候选方法 / 当前方法；
    方法缺供料等待项或正式执行结果；
    提交入口动作动态与来源动作动态。
再走真实运行路径，用日志模块、状态快照、动作动态和错误日志核对。
```
