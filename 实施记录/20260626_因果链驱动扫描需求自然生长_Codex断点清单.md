# 20260626 因果链驱动扫描需求自然生长 Codex断点清单

## 0. 恢复摘要

当前切片：
    P1c 因果链驱动扫描需求自然生长计划 v0.1 / P0.5b 两类动态生成入口同步生成因果信息核对。

当前目标：
    1. 核对状态信息生成时是否自动生成动态信息，并在动态生成时自动生成可查询因果信息。
    2. 核对治理函数和本能函数执行时是否直接生成动态信息，并在动态生成时自动生成可查询因果信息。
    3. 核对自动生成的因果信息是否可被任务筹办按需求目标查询。
    4. 核对需求是否只根据查询到的因果信息分解，不把需求节点当作因果信息来源。

依据文件：
    - `计划/计划索引.md`
    - `计划/20260626_因果链驱动扫描需求自然生长计划_v0.1.md`
    - `规范/动作动态即因果账本规范20260512.md`
    - `规范/因果信息用途与用法规范20260516.md`
    - `规范/需求临时因果视图展开规范20260526.md`
    - `docs/工程图谱/01_任务需求方法闭环图谱.md`
    - `docs/工程图谱/02_外设观察事实图谱.md`
    - `docs/工程图谱/08_事实来源与写入权限图谱.md`

预测：
    当前应先区分两类动态生成入口：
    1. 状态信息生成时自动生成动态信息，且动态生成时自动生成因果信息。
    2. 治理函数和本能函数执行时直接生成动态信息，且动态生成时自动生成因果信息。
    若只有动态日志而没有可查询因果，P0.5b 不闭合；若已有可查询因果但任务筹办未按需求目标查询和分解，继续定位筹办查询链。

禁止宣称：
    - 不宣称扫描需求自然生长已闭合。
    - 不宣称练习链完全闭合。
    - 不宣称自我苏醒完成或初步成熟完成。

新增约束：
    - 不得随意新增函数；需要实现时必须先检查 `说明书/函数功能说明文档.md`、现有公用函数和对应类特有功能函数。
    - 当前 P0.5b 先做只读审计和现有函数复用审查，不新增函数。

## 1. P0 对账记录

### 1.1 当前因果模板来源清单

1. 运行期动态生成因果：
    - `世界树类.cpp:3236`：`世界树类::生成因果信息(...)` 公开包装入口。
    - `因果类.h:146` / `因果类.h:154`：因果类公开 `生成因果信息` 两个重载，语义为“按动态信息生成或命中因果信息；已有相同条件、动作和结果时只追加证据动态并返回现有因果”。
    - `因果类.cpp:2572` / `因果类.cpp:2598`：公开入口进入 `私有_生成因果信息并返回模板`。
    - `因果类.cpp:2440-2476`：无动作状态变化模板按动态条件特征和果状态迁移模板查找/创建。
    - `因果类.cpp:2492-2545`：有动作时按动作来源、动作语义键、条件和结果查找/创建动作致变模板并追加证据动态。
    - 直接调用点：
        - `二次特征应用模块.ixx:1306`：动态二次特征回写后调用 `世界树->生成因果信息(...)`。
        - `自我动作实现.扫描.ixx:1368`、`自我动作实现.扫描.ixx:1631`：扫描方法动作动态创建后调用 `世界树.生成因果信息(...)`。
        - `自我类.cpp:604`、`自我类.cpp:694`、`自我类.cpp:772`：自我内部状态变化动态后调用 `世界树.生成因果信息(...)`。
        - `自我动作实现模块.ixx:12203`：服务 / 价值反馈相关动态后调用 `世界树.生成因果信息(...)`。

2. 被动触发初始因果模板：
    - `世界树类.cpp:3388`：`世界树类::确保被动触发初始因果模板(...)` 公开包装入口。
    - `因果类.h:163`：因果类公开 `确保被动触发初始因果模板(...)`。
    - `因果类.cpp:1763`：按父节点、条件比较、主果比较、适用层级和动作语义键命中或创建被动触发初始因果模板。
    - `因果类.cpp:1784-1798`：缺主果比较时，若有证据动态则转入 `生成因果信息`；若无证据动态则拒绝手写缺主果初始因果。
    - `自我线程模块.impl.cpp:6415`：线程侧 helper `私有_按动态生成被动触发因果信息_线程侧(...)` 调用 `世界树.确保被动触发初始因果模板(...)`。
    - `自我线程模块.impl.cpp:6481`：任务结算触发叶子任务价值结算被动因果。
    - `自我线程模块.impl.cpp:6566`：子目标满足触发父任务重筹办被动因果。

3. 因果查询消费入口：
    - `任务模块.筹办.ixx:1916`：任务筹办调用 `世界树.查询主果方向命中因果(...)`。
    - `任务模块.筹办.ixx:1941`：任务筹办调用 `世界树.读取因果证据状态(...)` 区分初始模板和后天验证因果。
    - `任务模块.筹办.ixx:1991`：日志输出 `因果路径筹办查询壳统计`。

### 1.2 无效或历史因果种子清单

1. `data/initial_causal_templates.tsv`
    - 当前文件存在，内容 16 行。
    - 代码和工程文件中未命中 `initial_causal_templates`、`.tsv` 或 `tsv` 引用。
    - 当前没有可见加载 / 解析 / 应用调用链。
    - 判定：当前只能列为历史材料或待迁移输入，不得当作当前运行依据。

2. TSV 内容缺口：
    - `data/initial_causal_templates.tsv:3-10` 只覆盖安全风险树、当前场景可观测单位取得、D455观察确认路径、旧名当前场景特征值明确路径和材料与对应事实闭合。
    - 未发现 `扫描可启动状态 -> 当前观察范围稳定对应存在扫描事实 / 动态取得状态` 模板。

### 1.3 真实日志证据

日志：`日志/鱼巢_run_20260626.低值g`

统计：
    - `生成因果信息/动态类型`：4365 条。
    - `自我线程/被动触发因果信息生成`：0 条。
    - `因果路径筹办查询壳统计`：83 条。
    - `命中初始因果模板=`：83 条。

样本：
    - `日志/鱼巢_run_20260626.低值g:73973` 等多条显示 `生成因果信息/动态类型 | 场景=F9 | 动态=4BO | 类型=1Bc | 外部参数升格动作=0`。
    - `日志/鱼巢_run_20260626.低值g:66536` 等多条显示任务筹办查询壳读取因果模板，且样本 `命中初始因果模板=0`、`命中后天验证因果=0`、`主果同向命中=1`、`直接写需求树=0`。

错误日志：
    - `日志/鱼巢_exception_20260626.低值g` 未命中 `因果 / initial_causal / 初始因果 / 生成因果 / 被动触发` 相关异常。

### 1.4 P0 结论

P0 已闭合：
    1. 当前正式因果信息来源不是 `data/initial_causal_templates.tsv`。
    2. 当前有效来源是运行期动作 / 状态动态经 `世界树类::生成因果信息 -> 因果类::生成因果信息` 生成或命中因果模板，以及代码显式触发的 `确保被动触发初始因果模板`。
    3. 当前 TSV 应标为历史材料或待迁移输入；只修改 TSV 不能证明扫描因果链生效。
    4. 当前日志存在大量运行期因果生成证据；当天未出现被动触发初始因果生成样本。
    5. 下一步必须进入 P0.5 内部治理练习链闭合，不得直接进入 P1 / P2。

## 2. P0.5 内部治理练习链审计记录

### 2.1 已证明的链路

1. `自我_练习` 是已注册本能方法入口：
    - `本能方法类.h:33`：`自我_练习 = 6036`。
    - `本能方法类.cpp:28`：默认动作名称为 `练习`。

2. 练习能真实调用目标方法动作入口并读取目标方法运行账：
    - `自我动作实现模块.ixx:9474-9479`：满足可试运行和输入闭合后，调用 `取本能动作集().执行功能函数(...)` 执行目标本能动作。
    - `自我动作实现模块.ixx:9481-9511`：读取目标方法运行闭环视图、运行码、运行账、同条件统计，并推导练习后方法状态。
    - `自我动作实现模块.ixx:9517-9532`：练习动作执行完成与练习目标达成分开；目标方法需达到 `好用`、连续成功次数阈值和同条件样本阈值。
    - `自我动作实现模块.ixx:9999-10002`：练习结果写入练习运行存在、输出结果场景，并更新练习方法自身运行账。

3. 当练习目标未达稳定阈值时，系统会生成结构化执行缺失子需求：
    - `自我动作实现模块.ixx:9816-9833`：连续成功次数不足时调用 `写入目标方法I64特征派生需求(...)`，目标特征为 `方法最近连续成功次数`。
    - `日志/鱼巢_run_20260626.低值g:8868`、`:23318`、`:25261`、`:27569` 等：`提交_生成缺口子需求 | 类型=执行缺失 | 目标特征=方法最近连续成功次数 | 来源因果键=空 | 成功=是`。

4. 任务筹办能处理因果条件缺口并入树为方法状态需求：
    - `任务模块.筹办.ixx:1487-1544`：通过 `世界树.枚举因果条件目标状态(...)` 目标化因果条件缺口。
    - `任务模块.筹办.ixx:1818-1829`、`:5536-5580`：字段完整时构造正式派生需求消息并进入筹办结果。
    - `日志/鱼巢_run_20260626.低值g:60655-60666`：来源因果 `268` 生成 `条件补齐` 派生需求，目标特征 `方法状态`，实际发送派生需求消息。
    - `日志/鱼巢_run_20260626.低值g:60779`、`:60848`、`:60879`：`提交_生成缺口子需求 | 类型=缺条件 | 因果生成类型=条件补齐 | 来源因果键=268 | 目标特征=方法状态 | 成功=是`。

5. 方法状态需求后续可命中 `自我_练习`：
    - `任务模块.筹办.ixx:2297-2303`：`自我_练习` 归入学习补齐本能方法。
    - `任务模块.筹办.ixx:4760-4811`：当前方法为 `自我_练习` 时检查目标方法练习实参闭合。
    - `日志/鱼巢_run_20260626.低值g:8642`、`:8876`、`:67062`、`:68683` 等：`候选方法练习目标参数预检 | 当前方法=1 | 练习目标方法=... | 本能ID=6143 | 条件节点数=1 | 条件结果对数=1`。

### 2.2 未闭合点

1. 因果路径的 `方法就绪缺口` 当前未目标化：
    - `任务模块.筹办.ixx:1565-1588` 只调用 `世界树.查询因果动作来源(...)`，当有方法模板时向 `视图.方法就绪缺口` 放入路径项。
    - 该路径项没有写入 `派生目标主体`、`派生目标特征类型`、`派生当前状态`、`派生目标状态`，因此不能稳定形成字段完整派生需求消息。
    - 当天日志中 `因果路径筹办临时视图统计` 共 83 条；命中 `方法就绪缺口=[1-9]` 的样本为 0。

2. 泛化学习缺口不是当前练习链的有效入口：
    - `任务模块.筹办.ixx:1658-1728` 只有在路径总数为 0、非安全 / 服务根、非递归学习信息补齐目标且 `观察材料质量目标=1` 时才生成学习缺口。
    - 当天日志中 `学习缺口=0`、`学习缺口目标化=0`，没有因果路径学习缺口入树样本。

3. 已证明的自然链路是：
    - `来源因果=268 -> 条件补齐 -> 方法状态需求 -> 方法筹办命中 自我_练习 -> 练习执行后继续派生方法最近连续成功次数需求`。
    - 这属于 P0.5 部分闭合，不等同于“方法就绪缺口 / 学习缺口已完整目标化为练习需求”。

### 2.3 现有函数复用审查

已检查 `说明书/函数功能说明文档.md` 和现有代码，下一步实现前优先复用以下现有函数，不得直接新增函数：

```text
因果读取：
    因果类::查询因果动作来源
    因果类::枚举因果条件目标状态
    因果类::读取因果证据状态
    世界树类对应包装函数

筹办派生消息：
    构造正式派生需求消息草案_筹办查询
    目标化条件目标状态路径项_筹办查询
    创建筹办I64状态节点
    返回方法结构缺口

方法与练习：
    方法虚拟存在服务类::取或创建方法虚拟存在
    方法虚拟存在服务类::同步方法节点到虚拟存在
    方法类::方法条件结果对数量
    方法可作为练习目标_筹办
    检查练习目标方法必需实参_筹办
```

### 2.4 P0.5 当前判定

P0.5 当前判定为部分闭合，不能进入扫描因果模板补齐：

```text
已闭合：
    自我_练习 可筹办 / 可执行；
    练习真实调用目标方法；
    目标方法运行账由目标方法自身更新；
    练习输出能生成结构化方法稳定度继续补齐需求；
    因果条件缺口可生成方法状态需求并入树。

未闭合：
    方法就绪缺口未目标化；
    学习缺口没有当天自然入树样本；
    还不能声明“任意方法缺口都能经内部治理因果自然生成练习需求”。

下一步：
    在不新增函数前提下，优先改造现有 `吸收因果方法就绪缺口_筹办查询` 或其邻近逻辑，
    复用现有因果读取、状态创建和派生消息草案函数，将因果动作来源方法目标化为方法状态 / 条件结果对 / 可执行入口类结构化缺口。
    若现有函数确实不足，再登记需要新增的函数、所属类、不能复用原因和项目结构映射。
```

## 3. P0.5a 方法就绪缺口目标化修补

### 3.1 本轮修改

修改文件：
    - `任务模块.筹办.ixx`

修改内容：
    - 未新增函数。
    - 复用 `世界树.查询因果动作来源(...)` 读取因果动作来源方法。
    - 复用 `方法虚拟存在服务类::取或创建方法虚拟存在(...)` 和 `同步方法节点到虚拟存在(...)` 定位目标方法虚拟存在。
    - 复用 `方法类::方法最低结构状态值(...)` 计算目标方法当前最低结构状态。
    - 复用 `创建筹办I64状态节点(...)` 物化当前状态和目标状态。
    - 将 `方法就绪` 路径项目标化为：
        目标主体 = 因果动作来源方法的虚拟存在；
        目标特征 = `方法状态`；
        当前状态值 = 目标方法当前最低结构状态；
        目标状态值 = `枚举_方法状态::可用`。

项目结构映射：
    - 方法就绪缺口 -> 方法虚拟存在 / 方法状态特征 / I64状态节点。
    - 写入方 -> 任务筹办只创建派生需求草案所需状态节点，不直接写需求树。
    - 读取方 -> 自我线程和需求类裁决入树后，任务筹办按已有方法结构补齐目标解析逻辑继续处理。

### 3.2 构建前预测

```text
1. 构建应通过；若失败，优先排查 `吸收因果方法就绪缺口_筹办查询` 签名变化、状态枚举引用和模块可见性。
2. 真实运行若命中有动作来源方法且该方法未达到 `方法状态=可用`，日志应出现 `因果方法就绪缺口目标化统计 | 目标化=1`。
3. 对应临时视图统计中 `方法就绪缺口` 应可大于 0，并且派生消息草案统计的 `已目标化/字段完整/状态物化` 应随之增加。
4. 该修改不直接证明扫描链闭合；只用于闭合 P0.5a 的方法就绪缺口目标化。
```

### 3.3 构建与真实运行结果

构建：
    - 命令：`msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
    - 结果：退出码 0，0 个警告，0 个错误。

真实运行：
    - 命令：`.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000`
    - 结果：退出码 0。
    - 日志：`日志/鱼巢_run_20260626.低值g`，最后写入时间 2026-06-26 15:18:57。

15:18 本轮日志统计：
    - `因果路径筹办查询壳统计`：7。
    - `因果路径筹办临时视图统计`：7。
    - `主果同向命中=1`：7。
    - `证据缺口=1`：7。
    - `方法就绪缺口=[1-9]`：0。
    - `因果方法就绪缺口目标化统计`：0。
    - `提交_生成缺口子需求`：2。
    - `候选方法练习目标参数预检`：2。
    - `自我动作/练习目标方法运行账`：3。
    - `学习缺口=1`：0。

错误日志：
    - `日志/鱼巢_exception_20260626.低值g` 未命中 15:18 本轮 `因果路径 / 练习 / 方法就绪 / 任务模块.筹办` 相关异常。

### 3.4 P0.5a 判定

```text
已完成：
    1. 未新增函数。
    2. 方法就绪缺口目标化代码已接入现有 `吸收因果方法就绪缺口_筹办查询`。
    3. 构建通过。
    4. 真实运行未破坏已有练习链：本轮仍有练习候选参数预检和练习目标方法运行账。

未验证：
    1. 本轮自然样本没有命中带动作来源方法的因果路径。
    2. 因此没有出现 `方法就绪缺口>0` 或 `因果方法就绪缺口目标化统计 | 目标化=1`。

不能宣称：
    1. 不能宣称方法就绪缺口自然入树已验证。
    2. 不能宣称练习链完全闭合。
    3. 不能进入扫描因果模板补齐。
```

### 3.5 新增下一切片

P0.5b：动作来源因果进入查询链核对。

目标：
    - 查清方法动作动态是否能把来源方法写入因果模板的 `因方法模板`。
    - 查清任务筹办当前命中的主果因果为什么只有证据缺口，没有动作来源方法模板。
    - 若缺口属于因果类功能函数缺失或调用参数缺失，先复查现有函数；不足时生成对应类补全计划或在本计划登记最小实现切片。

已知证据：
    - `因果类::私有_生成因果信息并返回模板` 在有动作且有 `来源动作` 时会写入 `因方法模板`。
    - 当前 15:18 运行生成了 `生成因果信息/动态类型` 日志，也生成了练习目标方法运行账。
    - 当前任务筹办查询命中的 7 个因果路径均为证据缺口，未触发方法就绪缺口。

## 4. P0.5b 动作来源因果进入查询链核对

### 4.1 根因定位

```text
已确认：
    1. 动态类已有 `读取动态因果生成输入`、`创建方法动作动态`、`绑定来源方法`。
    2. 因果类 `私有_生成因果信息并返回模板` 在动态有来源动作时会创建或命中动作致变因果模板，并写入 `因方法模板`。
    3. 动态创建默认后处理会调用动态二次特征，动态二次特征回写会调用 `世界树.生成因果信息`。
    4. 任务筹办吸收逻辑对同一命中因果先查 `查询因果动作来源`，再查证据缺口；没有在证据缺口前过滤动作模板。

缺口：
    1. 方法域提交函数已经创建旧状态和新状态，但创建方法动作动态时没有传入这两个状态端点。
    2. 动态缺少初始 / 结果状态时，二次特征无法形成状态迁移模板；因果生成只能停在无动作或不可形成主果方向的路径。
    3. 因此任务筹办命中的主果因果仍表现为证据缺口，未出现带 `因方法模板` 的方法就绪路径。
```

### 4.2 本轮修改

修改文件：
    - `自我动作实现模块.ixx`

修改内容：
    - 未新增函数。
    - 复用 `世界树.创建内部状态(...)` 返回的旧 / 新状态节点。
    - 复用 `世界树.创建方法动作动态(...)` 已有入参，把发生变化时的旧 / 新状态端点传入方法域提交动作动态。
    - 覆盖两个现有提交点：
        1. `提交目标方法I64特征_方法域动作(...)`。
        2. `提交方法可执行入口(...)` 内部方法动作状态动态。

项目结构映射：
    - 业务概念：方法域提交导致目标方法特征变化。
    - 项目承载结构：方法虚拟存在 / 方法特征 / 内部状态节点 / 方法动作动态 / 因果模板。
    - 值类型：I64 状态值、状态节点引用、来源方法引用。
    - 写入方：方法域提交方法。
    - 读取方：动态二次特征、因果类、任务筹办因果查询。
    - 验证方式：构建；真实运行日志中检查动态二次特征回写因果、生成因果信息、方法就绪缺口目标化统计。

### 4.3 构建前预测

```text
1. 构建应通过；若失败，优先排查 `创建方法动作动态` 入参顺序和状态节点变量作用域。
2. 真实运行中，方法域提交发生状态变化时，动作动态应带初始 / 结果状态端点进入动态二次特征和因果生成。
3. 若自然样本仍没有命中方法就绪缺口，需要继续核对动作致变因果模板是否被 `查询主果方向命中因果` 的目标比较同向规则命中。
4. 本修补仍不证明扫描链闭合，也不证明练习链完全闭合。
```

### 4.4 构建与真实运行后收束

```text
构建：
    命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    结果：退出码 0，0 警告，0 错误。

真实运行：
    命令：.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000
    结果：退出码 0。

日志统计：
    动态二次特征回写因果信息：10
    生成因果信息/动态类型：110
    因果路径筹办临时视图统计：7
    因果路径筹办派生消息草案统计：7
    自我动作/练习目标方法运行账：3
    提交_生成缺口子需求：2
    方法域提交/方法状态被动派生动态：0
    提交方法可执行入口：0
    因果方法就绪缺口目标化统计：0

收束结论：
    方法域提交动态端点已修补，构建和真实运行未破坏现有练习链。
    本轮真实运行没有命中方法域提交自然样本，因此该修补只能记为代码闭合、待后续自然样本复核。
    不能宣称方法就绪缺口自然入树已验证。
```

### 4.5 第二独立问题登记：练习尾部派生需求来源因果为空

```text
问题锚点：
    日志/鱼巢_run_20260626.低值g:76855
        自我动作/目标方法派生需求已写入 | 目标方法=Y | 方法虚拟存在=v6 | 输出场景=F0 | 特征类型=15u | 当前状态=3Rq | 目标状态=3Rr
    日志/鱼巢_run_20260626.低值g:77087
        提交_生成缺口子需求 ... 来源因果键=空 | 目标特征=方法最近连续成功次数

当前判断：
    1. 自我动作模块已有 `特征_来源因果信息` / `特征_来源观察动态`。
    2. `自我_练习` 的输入绑定结果已有 `绑定.来源因果信息` / `绑定.来源观察动态`。
    3. 工作线程派生需求消息已有 `来源因果指针` / `来源因果主键` / `来源动态指针` / `来源动态主键`。
    4. 自我线程入树路径已有来源因果解析和不可解析拒绝逻辑。
    5. 当前缺口不是缺数据结构，而是执行输出场景和执行桥消息之间漏传现有来源字段。

本轮约束：
    不新增函数，不新增结构体。
    优先复用 `写入基础节点指针`、`世界树.读取特征_指针`、`世界树.解析因果节点`、`世界树.取因果主信息` 和现有派生需求消息字段。

预计最小修补：
    1. 派生需求输出场景写入时同步写入来源因果 / 来源观察动态现有特征。
    2. 任务管理执行桥从执行输出场景读取现有来源特征，并只在可解析为因果 / 动态主信息时填入消息来源槽。
    3. 清理执行输出派生需求标记时同步清理来源特征，避免旧来源残留。
```

### 4.6 第二问题修补与真实运行预测

```text
修改文件：
    - `自我动作实现模块.ixx`
    - `任务模块.管理工作线程.ixx`

修改内容：
    - 未新增函数。
    - `清理执行输出派生需求标记(...)` 同步清理 `来源因果信息` / `来源观察动态`。
    - `写入目标方法特征派生需求(...)` 复用现有来源特征，可选写入来源因果 / 来源观察动态。
    - `自我_尝试学习`、`自我_定向学习`、`自我_练习` 复用条件绑定结果中的来源因果 / 来源观察动态，不再把观察动态混写到来源因果字段。
    - `私有_补充执行输出派生需求字段(...)` 复用现有消息字段，从执行输出读取来源因果 / 来源观察动态，并只在可解析为因果 / 动态主信息时填入消息槽。

项目结构映射：
    - 业务概念：练习派生需求的来源因果。
    - 项目承载结构：输出结果场景 / 来源因果信息特征 / 来源观察动态特征 / 任务管理派生需求消息来源槽。
    - 值类型：基础信息节点指针，进入消息后为指针 + 主键。
    - 写入方：自我动作实现模块的学习 / 练习动作输出。
    - 读取方：任务管理工作线程执行桥、自我线程派生需求入树逻辑。
    - 验证方式：构建；真实运行日志检查 `自我动作/目标方法派生需求已写入`、`任务管理工作线程/派生需求来源槽补充`、`提交_生成缺口子需求`。

构建：
    命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    结果：退出码 0，0 警告，0 错误。

真实运行预测：
    若本轮练习输入携带来源因果，则应出现：
        1. `自我动作/目标方法派生需求已写入 ... 来源因果=非空`；
        2. `任务管理工作线程/派生需求来源槽补充 ... 来源因果=非空`；
        3. `提交_生成缺口子需求 ... 来源因果键=非空`。
    若本轮练习输入未携带来源因果，则来源仍可能为空；此时只能证明漏传修补不破坏现有链路，不能证明来源因果自然贯通。
```

### 4.7 第二问题 60 秒自然复跑预测

```text
复跑目的：
    延长 `--self-check-log` 等待窗口，确认 4.6 修补是否能在自然链路中把练习尾部派生需求来源因果传到提交缺口子需求。

命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000

预测：
    1. 若自然运行进入练习尾部派生需求，且练习输入携带来源因果，应出现：
        - `自我动作/目标方法派生需求已写入 ... 来源因果=非空`
        - `任务管理工作线程/派生需求来源槽补充 ... 来源因果=非空`
        - `提交_生成缺口子需求 ... 来源因果键=非空`
    2. 若运行窗口仍未进入练习尾部派生需求，只能记录为未命中自然样本，不新增函数、不继续扩大实现。
    3. 若练习尾部派生需求出现但来源仍为空，下一步先查输入条件包和筹办构造是否已经携带现有来源因果；不得直接新增函数。
```

### 4.8 第二问题 60 秒自然复跑结果

```text
真实运行：
    命令：.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000
    结果：退出码 0。
    日志：日志/鱼巢_run_20260626.低值g，最新写入 2026-06-26 16:04:10。

16:03 本轮统计：
    自我动作/练习目标方法运行账：2
    自我动作/目标方法派生需求已写入：2
    任务管理工作线程/派生需求来源槽补充：0
    提交_生成缺口子需求：1
    来源因果不可解析：0
    来源动态不可解析：0
    执行桥逻辑错误：0
    弹窗：0
    逻辑错误：0
    因果路径筹办临时视图统计：3
    因果路径筹办派生消息草案统计：3
    生成因果信息/动态类型：110
    动态二次特征回写因果信息：10
    因果方法就绪缺口目标化统计：0

关键锚点：
    日志/鱼巢_run_20260626.低值g:78503
        自我动作/目标方法派生需求已写入 ... 来源因果= | 来源动态=
    日志/鱼巢_run_20260626.低值g:78636
        提交_生成缺口子需求 ... 来源因果键=空 | 目标特征=方法最近连续成功次数
    日志/鱼巢_run_20260626.低值g:78720
        自我动作/目标方法派生需求已写入 ... 来源因果= | 来源动态=

结论：
    1. 4.6 修补没有引入运行异常，且练习尾部派生需求自然样本已经命中。
    2. 来源槽补充没有触发，不是解析失败；动作输出阶段记录的来源因果 / 来源动态本身为空。
    3. 下一步应查练习输入条件包和筹办构造是否把派生需求 / 父需求的现有来源因果传入 `自我_练习` 输入绑定。
    4. 继续执行前不得新增函数；先复查 `说明书/函数功能说明文档.md`、现有公用函数和任务 / 自我动作 / 需求 / 因果类现有功能函数。
```

### 4.9 口径纠正与计划切换撤回

```text
用户纠正：
    需求根据因果信息分解，而不是因果信息的来源。
    动态信息生成时自动生成因果信息。
    动态信息有两条生成路：状态信息生成时自动生成动态信息；治理函数和本能函数执行时直接生成动态信息。

撤回判断：
    1. 撤回 4.9 原先把“派生需求来源因果持久化与读回”归为当前阻塞的判断。
    2. 撤回临时切换到 `计划/20260620_需求类逻辑纠偏计划_v0.1.md` / P0x 的执行路由。
    3. 练习尾部派生需求的来源因果 / 来源动态为空，只能作为追溯材料后置缺口观察，不作为因果链权威缺口。

已修订计划：
    - `计划/计划索引.md`
        当前主动队列恢复为 P1c / P0.5b。
    - `计划/20260626_因果链驱动扫描需求自然生长计划_v0.1.md`
        将 P0.5b 恢复为两类动态生成入口同步生成因果信息 -> 筹办查询 -> 需求分解链核对。
    - `计划/20260620_需求类逻辑纠偏计划_v0.1.md`
        将 P0x 降级为后置追溯材料缺口，不作为当前第一优先。

下一步：
    留在 P1c / P0.5b：
        1. 查状态信息生成时是否自动生成动态信息，并在动态生成时自动生成因果信息。
        2. 查治理函数和本能函数执行时是否直接生成动态信息，并在动态生成时自动生成因果信息。
        3. 查任务筹办是否按需求目标查询因果信息并分解需求。
        4. 继续遵守不得随意新增函数的约束；先查现有公用函数和对应类特有功能函数。
```

### 4.10 两类动态生成入口与自动因果生成代码核对

```text
只读核对结论：
    1. 状态信息生成自动动态入口存在：
        - `状态类.cpp:303` 的 `状态类::创建状态(...)` 在状态节点追加到场景后调用 `世界树.状态记录后刷新动态(场景, 节点, 刷新时间)`。
        - `世界树类.cpp:3206` 的 `世界树类::状态记录后刷新动态(...)` 转入 `动态类::状态记录后刷新动态(...)`。
        - `动态类.cpp:1323` 的 `动态类::状态记录后刷新动态(...)` 查找相邻前状态后调用 `创建叶子动态(...)`。

    2. 治理函数 / 本能函数直接生成动态入口存在：
        - `世界树类.cpp:3112` 的 `世界树类::创建方法动作动态(...)` 转入 `动态类::创建方法动作动态(...)`。
        - `动态类.cpp:956` 的 `动态类::创建方法动作动态(...)` 写入来源动作、动作名、输入场景、输出场景、执行结果、初始状态和结果状态后调用 `创建动态(...)`。

    3. 动态生成时自动生成因果信息的后处理链存在：
        - `动态类.h:11` 的 `动态创建后处理选项::生成动态二次特征` 默认 `true`。
        - `动态类.cpp:680` 的 `动态类::创建动态(...)` 创建动态节点后默认调用 `世界树.获取动态二次特征(*节点)`。
        - `二次特征应用模块.ixx:1306` 的动态二次特征回写路径调用 `世界树->生成因果信息(...)`。
        - 日志中存在 `动态二次特征回写因果信息/内部阶段耗时` 和大量 `生成因果信息/动态类型` 证据。

    4. 重要边界：
        - `写入基础节点I64(...)` / `写入基础节点指针(...)` 只转入 `世界树.写入特征_I64(...)` / `世界树.写入特征_指针(...)`。
        - `世界树类.cpp:3480-3514` 和 `特征类.cpp:2732-2754` 显示这类路径只是写特征值，不自动创建状态节点。
        - 因此，治理函数 / 本能函数如果只写特征值，不等同于“状态信息生成”，也不会天然触发状态动态和因果生成；必须显式创建状态节点，或显式创建方法动作动态。

当前判断：
    P0.5b 后续不能再泛称“治理函数写入结果就会生成因果”。
    必须逐个核对关键结果写入点是：
        A. 创建状态节点，从状态自动动态进入因果；
        B. 直接创建动态，从动态后处理进入因果；
        C. 仅写特征值，未进入动态 / 因果链。

下一步：
    继续查 `自我_练习`、方法域提交和任务执行桥的关键结果写入点属于 A / B / C 哪一类；
    若关键结果只落在 C 类，则登记对应函数缺口，先复用现有 `创建内部状态` 或 `创建方法动作动态`，不得新增函数。
```

### 4.11 自我_练习与通用执行桥 A/B/C 分类

```text
只读核对结论：
    1. `自我_练习` 的目标方法运行账更新当前主要是 C 类：
        - `自我动作实现模块.ixx:4036` 的 `输出运行虚拟存在(...)` 写运行存在的结束时间、运行时长、输入 / 输出场景指针。
        - `自我动作实现模块.ixx:4073` 的 `更新方法运行账(...)` 写方法虚拟存在上的运行次数、成功次数、连续成功次数、方法状态等特征值。
        - 这些写入通过 `写入基础节点I64/指针` 进入特征写入，不自动创建状态动态。

    2. 方法域提交路径是 A+B 类：
        - `自我动作实现模块.ixx:8479` 的 `提交目标方法I64特征_方法域动作(...)` 创建旧 / 新内部状态。
        - 同函数调用 `世界树.创建方法动作动态(...)`，并把发生变化时的旧 / 新状态端点传给动态。
        - 该路径能进入动态创建后处理和因果生成链。

    3. 任务状态提交路径是 A+B 类：
        - `自我动作实现模块.ixx:10378` 附近的任务状态被动提交创建旧 / 新状态，再创建方法动作动态。
        - 日志存在 `任务状态动作动态` 和 `任务状态动作动态创建/内部阶段耗时` 证据。

    4. 通用任务执行桥直接动作动态是 B 类但缺状态端点：
        - `任务模块.执行.ixx:119` 的 `私有_创建方法调用动作动态_执行桥_最小_v0(...)` 调用 `世界树.创建方法动作动态(...)`。
        - 当前调用点 `任务模块.执行.ixx:1341` 只传输入场景、输出场景、执行成功和错误码，没有传初始 / 结果状态。
        - 同文件 `私有_填充主体特征需求状态_执行桥_最小_v0(...)` 会在输出场景写派生需求当前 / 目标状态，但这些是缺口目标，不等于动作实际造成的结果状态。

当前缺口：
    通用执行桥的动作动态能证明“方法被调用”，但缺少可归因的实际状态端点；
    不能直接用派生需求当前 / 目标状态冒充动作实际结果，否则会把“需要达到的目标”误写成“动作已经造成的结果”。

下一步：
    继续查通用执行桥或本能动作输出结果中是否已有真实实际状态端点；
    若没有，登记任务执行桥 / 本能动作输出结果的状态端点补齐缺口。
    补齐时优先复用现有 `创建内部状态`、`创建方法动作动态` 和输出结果场景结构，不新增函数。
```
