# 20260615 自我扫描当前场景函数自身实现 Codex断点清单

## 0. 恢复摘要

当前切片：只实现扫描本能函数自身。

用户最新边界：

```text
这里先不管其它问题，只专注实现本能函数自身。
执行计划。
```

依据已读取：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260615_自我扫描当前场景本能方法重建计划_v0.1.md
流程图/20260615_扫描本能方法内部流程图_v0.13.md
说明书/自我侧观察识别扫描跟踪本能方法规格说明.md 第 7 节
```

当前判断：

```text
1. 本切片不推进 D455 自然命中追根因。
2. 本切片不处理任务筹办、界面线程、自检线程等旁路。
3. 本切片优先新增 自我动作实现.扫描.ixx，做扫描函数自身的结构化闭环骨架。
4. 旧扫描入口仍存在，后续清理切片处理；本切片不把旧函数当 fallback。
```

当前已复核代码事实：

```text
旧扫描函数主体位于 自我动作实现.外设模块.ixx：
    确认自我扫描已识别区域规格
    自我扫描已识别区域

当前没有 自我动作实现.扫描.ixx。

外设观察报告队列已提供：
    结构_外设观察报告队列项
    枚举_外设观察报告类型::扫描变化报告
    构造外设扫描提交包_由观察报告
    读取最新外设观察报告
    读取外设观察报告_按ID
```

下一步：

```text
1. 审计可在新模块中直接使用的公开 API。
2. 新增 自我动作实现.扫描.ixx。
3. 若需要构建验证，同步工程文件登记新模块。
4. 不改动已有脏文件中的旧扫描函数体，除非编译必须且只做最小连接。
```

## 1. 当前验证预测

验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

预测：

```text
本切片只新增扫描函数自身模块和工程登记。
若失败，优先预期为新模块导出语法、公开 API 签名或工程模块依赖顺序问题；
不应暴露 D455 原始材料质量、任务筹办自然命中或旧扫描入口清理问题。
```

结果：

```text
第一次构建 exit_code=1：
    自我动作实现.扫描.ixx 存在 C++20 module 导出范围错误；
    原因是 export namespace 导出了匿名命名空间内部实现，同时 场景节点类 / 存在节点类 均为 基础信息节点类 别名，不能重载 基础节点。

已修正：
    自我动作实现.扫描.ixx 改为普通 namespace，仅导出两个函数；
    基础节点 合并为 基础信息节点类* 单一入口。

第二次构建 exit_code=1：
    自我动作实现.扫描.ixx 已通过编译阶段；
    当前唯一错误来自既有脏文件 自我动作实现.外设模块.ixx:23559：
        写入已确认观察存在节点原始事实 函数不接受 4 个参数。

当前阻塞：
    整工程 Debug x64 构建被外设模块既有脏改阻断。
    按用户“只专注实现本能函数自身”的边界，本切片不修改该旁路。
```

静态检查：

```text
git diff --check -- 自我动作实现.扫描.ixx 鱼巢.vcxproj 鱼巢.vcxproj.filters 实施记录/20260615_自我扫描当前场景函数自身实现_Codex断点清单.md
    结果：无空白错误；仅提示 vcxproj / filters 换行下次 Git 触碰会转 CRLF。

rg -n "兼容|fallback|旧字段|旧入口|旧流程|错误码|本能动作错误_|return .*失败|执行失败|筹办失败|上报失败|生成失败|自我扫描已识别区域|自我_扫描已识别区域" 自我动作实现.扫描.ixx
    命中：特征_写入失败数量。
    判断：这是规格要求的结构化运行特征，不是裸失败返回、兼容路径或旧扫描入口。
```

当前本切片文件：

```text
新增：
    自我动作实现.扫描.ixx
    实施记录/20260615_自我扫描当前场景函数自身实现_Codex断点清单.md

修改：
    鱼巢.vcxproj
    鱼巢.vcxproj.filters
```

阶段提交状态：

```text
当时未提交。
原因：整工程构建尚未通过，阻塞点在本切片范围外的既有脏文件。
```

## 2. 继续执行：外设模块构建阻塞

问题锚点：

```text
自我动作实现.外设模块.ixx:23559
写入已确认观察存在节点原始事实 函数不接受 4 个参数。
```

当前判断：

```text
函数声明需要参数：
    观察存在, 假设, 验证, const 结构_外设观察像素簇摘要* 簇, now

同文件既有正确调用：
    写入已确认观察存在节点原始事实(观察存在, 假设, 验证, &项.簇, now)

本处位于稳定子集项处理循环内，当前上下文同样具备 项.簇。
```

修复预测：

```text
将 23559 处调用补齐 &项.簇 后，当前 C2660 应消失。
若仍失败，应进入下一条真实编译错误，不把旧扫描入口清理纳入本修复。
```

修复结果：

```text
已修复：
    自我动作实现.外设模块.ixx:23559
    写入已确认观察存在节点原始事实(观察存在, 假设, 验证, &项.簇, now)

验证命令：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /fl /flp:"logfile=日志\20260615_自我扫描当前场景函数自身_msbuild.log;verbosity=normal"

验证结果：
    exit_code=0
    已成功生成
    0 个警告
    0 个错误

差异边界：
    自我动作实现.外设模块.ixx 在本轮前已有大段未提交改动；
    本轮只新增 23559 处 &项.簇 参数修复。
    该文件未纳入扫描函数骨架提交，避免把既有其它改动混入。
    该一行修复仍保留在工作区，用于当前脏工作区构建闭合。
```

最终提交状态：

```text
已提交：
    a2b4d80 实现自我扫描当前场景函数骨架

提交范围：
    自我动作实现.扫描.ixx
    鱼巢.vcxproj
    鱼巢.vcxproj.filters
    本断点清单

未提交保留：
    自我动作实现.外设模块.ixx:23559 的 &项.簇 构建修复。
    原因：该文件已有大段未提交上下文，单独提交本行无法形成自洽补丁。
```

禁止宣称：

```text
不宣称 D455 自然业务样本完整通过。
不宣称识别 / 扫描 / 跟踪边界已经全部动态闭合。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
