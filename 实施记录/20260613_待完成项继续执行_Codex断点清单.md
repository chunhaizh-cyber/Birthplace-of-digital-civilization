# 待完成项继续执行 Codex 断点清单

日期：2026-06-13

## 当前切片

从 `计划/待完成项/` 中提取可用真实运行窗口复核的旧堵点，先处理不依赖 D455 硬件、不依赖伪造入口、不依赖旧 `--*-check` 回归命令的项目。

## 已读依据

```text
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/待完成项/20260516_方法可执行入口真实动作来源规则计划_v0.1.md
计划/待完成项/20260522_自我动作实现类本能动作函数边界收口改造计划_v0.1.md
```

## 当前判断

```text
20260516 计划：
    P2b / P2b.1 / P4 代码侧已在计划中记录为首轮实现；
    当前代码也存在 因果类::反推自我动作组合 与 返回自我动作组合缺口；
    剩余堵点是自然动作组合样本或动作序列入口规则，不能伪造入口。

20260522 计划：
    剩余可用真实日志复核的旧堵点是主动动作组合候选是否仍出现
    有因果但无可映射方法 / 被动整理动作误入候选 / 治理镜像误入候选。
```

## 验证预测

```text
运行真实自检窗口：
    x64/Debug/鱼巢.exe --self-check-log --self-check-log-wait-ms=20000

预期：
    进程 exit_code=0；
    不出现 Debug Error / abort / heap assertion / Access violation；
    若未出现 返回自我动作组合缺口 / 自我动作组合反推候选，
        按用户口径记录旧主动动作组合映射堵点本轮未复现，默认收束到完成项；
    若出现候选，
        不得出现 被动动作 / 同步筹办回执到任务 / 同步执行回执到任务 /
        同步可用方法数量镜像 / 自我_当前主需求切换 / 自我_当前主任务切换 /
        自我_当前主方法切换 被当作自我动作组合反推候选。
```

## 禁止宣称

```text
不宣称动作序列入口规则已定义。
不宣称 D455 样本项已完成。
不宣称普通价值核心入账自然样本已通过。
不宣称自我苏醒完成或内部循环全部完成。
```

## 实际验证

```text
命令：
    x64/Debug/鱼巢.exe --self-check-log --self-check-log-wait-ms=20000

输出：
    日志/20260613_pending_continue_selfcheck_20000ms.console.log
    日志/20260613_pending_continue_selfcheck_20000ms.stderr.log
    日志/20260613_pending_continue_run_tail.txt

结果：
    exit_code=0
    console_bytes=14134
    stderr_bytes=0
    latest_run_log=日志/鱼巢_run_20260613.低值g
```

## 关键检索

```text
异常关键词：
    Debug Error / abort / _CrtIsValidHeapPointer / Assertion /
    访问冲突 / Access violation / 弹窗错误提示 / [FTL] / [ERR]
    命中=0。

旧主动动作组合映射堵点：
    返回自我动作组合缺口 / 自我动作组合反推候选 /
    有因果但无可映射方法 / 动作序列待定义 / 多候选方法
    命中=0。

禁止候选误入：
    被动动作 / 同步筹办回执到任务 / 同步执行回执到任务 /
    同步可用方法数量镜像 / 自我_当前主需求切换 /
    自我_当前主任务切换 / 自我_当前主方法切换
    命中=0。
```

## 本轮收束

```text
已移动到完成项：
    计划/完成项/20260522_自我动作实现类本能动作函数边界收口改造计划_v0.1.md

依据：
    该计划剩余可校验堵点是主动动作组合语义映射和被动 / 治理镜像误入候选。
    当前真实运行窗口未复现旧堵点；
    按用户口径，真实环境中旧问题不再出现则默认已解决。

仍保留待完成：
    20260516 方法可执行入口计划仍含动作序列入口规则未定义；
    该规则属于缺契约，不在本轮伪造实现。
```

## 追加收束

```text
已移动到完成项：
    计划/完成项/20260612_创建子任务异常分支代码逻辑修订计划_v0.1.md

依据：
    计划自身记录 P1-P5 代码层已闭合，P6 已命中父子目标自复制保护样本；
    本轮真实自检窗口未复现字段缺失、稳定键重复、入树拒绝分类或父子目标自复制回潮。

关键检索：
    创建子任务异常 / 父子目标自复制 / 字段缺失 / 稳定键重复 /
    派生需求入树原因分类 / 缺父需求 / 目标状态缺失 /
    当前状态缺失 / 父需求是结构根 / 入树拒绝分类
    命中=0。

当前判断：
    按用户口径，真实环境中旧问题不再出现则默认已解决；
    后续若真实日志复现，再按新样本重新开追踪项。
```

## 旧结算计划归档

```text
已移动到完成项：
    计划/完成项/20260518_D0D1Actual正式结算路径替换过渡路径方案_v0.1.md
    计划/完成项/20260518_状态转换因果树与目标投影层级生成计划_v0.2.md

依据：
    两个文件均已有 2026-05-19 至 2026-05-23 的实现、验证和口径修订记录；
    D2 硬闸门已修订为叶子任务完成、实际改变结果状态、完成度和任务自身权重；
    D0 / D1 转为因果解释证据，不再作为独立硬阻断。

承接：
    后续闭环验证报告、结算域分类、VAL1 硬闸门、普通价值 / G0 自然样本复核，
    均由 计划/待完成项/20260613_闭环验证与结算流程修正计划_v0.1.md 承接。
```

## 自我关键特征因果计划刷新

```text
计划：
    计划/待完成项/20260601_自我关键特征负向变化定义因果纠偏计划_v0.1.md

本轮动作：
    刷新 P0-P2 代码对账，不进入代码实现。

旧堵点变化：
    旧结论“未发现自我边界承载”已过期；
    当前代码已有 自我边界参照状态 / 自我相对视图坐标系状态 / 自我_边界完整性状态，
    并已有 自我_建立最小自我边界参照 方法。

当前真实缺口：
    自我_读取自我关键特征变化 仍固定 关键特征负向变化数量=0；
    第一批 运行 / 资源 / 边界 / 物理 / 风险负向变化 -> 安全值下降候选
    定义遗传因果模板组仍待 P3 收束。

下一步：
    执行 P3 前先核对初始模板种子文件和 私有_确保初始因果模板_线程侧；
    能用现有入口则补模板种子或复用封装；
    若需要新增模板契约，停止并拆设计切片。
```

## 自我关键特征因果 P3 种子补齐

```text
已修改：
    data/initial_causal_templates.tsv

补齐模板：
    关键特征负向变化 / 运行状态负向变化 / 资源状态负向变化 /
    边界完整性负向变化 / 物理安全下降 / 风险安全下降
    -> 自我安全值下降候选状态。

边界：
    数据化初始因果模板种子；
    不生成需求，不创建任务，不写安全值，不结算价值。

后续仍待：
    P4 自我_读取自我关键特征变化 真实读取；
    P5 安全场景影响候选追溯关键特征模板；
    P6 需求树展开口径复核；
    P7 价值结算边界复验。
```

验证：

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=20000

结果：
    exit_code=0
    stderr_bytes=0
    当前运行窗口异常关键词命中=0

关键日志：
    日志/鱼巢_run_20260613.低值g
        P3 模板入账 6 条；
        初始模板种子数量=15；
        初始模板应用数量=15；
        初始模板缺口数量=0；
        直接写安全值=0 / 不写自我安全值=1。
```

## 自我关键特征因果 P4 代码层修正

```text
已修改：
    自我动作实现.外设模块.ixx

修正：
    自我_读取自我关键特征变化 不再目标宿主存在即成功；
    不再固定 关键特征变化观察状态=已明确；
    不再固定 关键特征负向变化数量=0。

第一版样本来源：
    自我_边界完整性状态；
    存在_进入自我边界缓冲区候选状态；
    存在_接触自我边界候选状态；
    存在_靠近自我候选状态；
    存在_遮挡关键观察区域候选状态。

验证：
    msbuild exit_code=0，仅既有 LNK4075；
    self-check exit_code=0，stderr=0；
    当前真实窗口未自然命中 自我_读取自我关键特征变化，
    所以业务路径待自然样本复核。

后续仍待：
    P5 安全场景影响候选追溯关键特征模板；
    P6 需求树展开口径复核；
    P7 价值结算边界复验。
```
