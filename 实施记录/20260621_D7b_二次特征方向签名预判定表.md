# 20260621 D7b 二次特征方向签名预判定表

## 0. 判定口径

```text
所属计划：计划/20260620_需求类逻辑纠偏计划_v0.1.md P7b。
范围：需求类.cpp:1733-1780 的 生成需求方向签名，以及 1784-1854 的方向比较消费。
边界：
    方向判断只能读取结构化字段：目标主体、目标特征类型、允许方向、方向区间、差值、标量值。
    不允许用方向文本、日志说明或 `语素集.安全获取词(...)` 作为机器判断。
```

## 1. 当前读取点

| 位置 | 当前读取 | 用途 | 判定 |
| --- | --- | --- | --- |
| `需求类.cpp:1741-1749` | `二次特征主信息类::来源结果状态 / 来源起始状态 / 主体` | 解析方向签名的目标主体和目标特征类型 | 机器逻辑，应通过二次特征类返回结构化材料 |
| `需求类.cpp:1755-1763` | `允许方向 / 方向区间 / 差值` | 方向同向 / 反向比较 | 机器逻辑，应通过二次特征类统一读取 |
| `需求类.cpp:1765-1768` | `值形态 / 标量值` | 无显式方向时的标量方向兜底 | 机器逻辑，应通过二次特征类统一读取 |
| `需求类.cpp:1784-1854` | `结构_需求方向签名` | 比较结构化签名 | 可保留；消费的是结构化数值和枚举，不是文本 |

## 2. 可复用函数检查

```text
二次特征类当前已有：
    读取状态迁移来源
    读取可融合标量区间
    融合同向状态迁移可融合标量区间

不足：
    读取状态迁移来源只返回状态 / 动态节点，不返回允许方向、方向区间、差值、标量值、主体引用。
    读取可融合标量区间只服务区间融合，不保留方向掩码、方向区间和来源目标身份。
```

## 3. 结论

```text
需要按“缺哪个类补哪个类第一优先”临时提升二次特征类：
    新增结构化只读材料和读取函数；
    需求类只把材料转换为自身的 结构_需求方向签名；
    不新增文本说明字段，不新增二次特征主信息字段。
```

建议新增：

```cpp
struct 结构_二次特征方向签名材料 {
    可解析引用<基础信息节点类> 主体{};
    可解析引用<状态节点类> 来源起始状态{};
    可解析引用<状态节点类> 来源结果状态{};
    三向关系掩码 允许方向 = 0;
    二次特征主信息类::枚举_方向区间 方向区间 =
        二次特征主信息类::枚举_方向区间::未定义;
    std::optional<I64> 差值{};
    bool 有标量值 = false;
    I64 标量值 = 0;
};

static bool 读取方向签名材料_直接(
    const 二次特征主信息类* 主信息,
    结构_二次特征方向签名材料& 输出) noexcept;
```

验证：

```text
函数说明文档生成。
Debug x64 构建。
需求类.cpp 中 `生成需求方向签名` 不再直接读取 二次特征主信息类 字段。
```

## 4. 执行结果

```text
已新增：
    结构_二次特征方向签名材料
    二次特征类::读取方向签名材料_直接

已回迁：
    需求类.cpp::生成需求方向签名。

已验证：
    python .\tools\生成函数功能说明文档.py 退出码 0，函数条目 7163。
    Debug x64 构建退出码 0，日志 日志/20260621_P7b_二次特征方向签名材料_build.log 显示“已成功生成”。
    `需求类.cpp` 中无 `比较主信息->...` 直接解包。

后续：
    返回需求类 P7，下一小切片处理方法类生产者查询跨类读取。
```
