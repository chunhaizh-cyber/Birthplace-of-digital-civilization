# 20260625 需求生成治理闭环 C5 开发诊断闭环 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260625_需求生成与治理闭环代码修订计划_v0.1.md` 的 C5“开发诊断闭环”。

当前目标：

```text
1. 定义结构化开发诊断项或等价诊断账本结构。
2. 代码缺陷型拒绝进入开发诊断项，记录来源回执、任务、需求、方法、失败阶段、失败原因、版本和复现上下文。
3. 运行期只负责诊断、隔离、报告和阻断；关闭必须依赖代码修正、编译和回归。
4. 来源任务继续由 C4 的回执承接进入实现缺陷阻断，不在自我线程裸写任务状态。
```

依据文件：

```text
规范/需求入树拒绝回执与开发诊断闭环规范20260625.md
规范/任务筹办与执行边界规范20260512.md
docs/工程图谱/01_任务需求方法闭环图谱.md
docs/工程图谱/05_规则原子索引.md
计划/计划索引.md
实施记录/20260625_需求生成治理闭环C4来源任务挂起回执承接_Codex断点清单.md
```

计划触碰文件：

```text
任务模块.管理线程协议.ixx
自我线程模块.impl.cpp
说明书/函数功能说明文档.md
计划/计划索引.md
计划/20260625_需求生成与治理闭环代码修订计划_v0.1.md
实施记录/20260625_需求生成治理闭环C5开发诊断闭环_Codex断点清单.md
```

已知无关脏改：

```text
控制面板.html
鱼巢.vcxproj
鱼巢.vcxproj.filters
tools/__pycache__/
```

## 1. 预测

```text
本切片预期只补运行期开发诊断项建立、账本记录、日志和弹窗提示。
不会实现开发期代码修复、编译后自动关闭或完整诊断 UI。
构建通过后，只能说明代码缺陷型拒绝不再停留于拒绝回执文本，而是有结构化诊断项和诊断 ID 可追踪。
```

## 2. 实际修改

```text
任务模块.管理线程协议.ixx
    - 新增 `枚举_开发诊断项状态`。
    - 新增 `结构_开发诊断项`，承载诊断项 ID、来源回执 ID、来源幂等键、来源任务 / 需求 / 方法、原始拒绝原因、失败阶段、文件 / 函数 / 行号、错误摘要、复现上下文、版本信息、弹窗 / 日志 / 隔离标记和关闭条件。

自我线程模块.impl.cpp
    - 新增自我线程侧开发诊断项账本。
    - 新增 `私有_需求入树拒绝回执需要开发诊断项_线程侧`。
    - 新增 `私有_登记开发诊断项_线程侧`，按幂等键 + 拒绝原因复用未关闭诊断项，避免同一路径重复建立诊断项。
    - `私有_处理需求入树拒绝回执_线程侧` 对开发诊断闭环 / 必须隔离来源路径的拒绝建立开发诊断项，并把诊断项 ID 回填到拒绝回执。
    - 开发诊断项建立时写错误日志并调用 `项目弹窗错误提示`；关闭条件仍记录为代码修正 + 编译 + 回归。

说明书/函数功能说明文档.md
    - 已按项目规则重新生成，新增 2 个函数条目。
```

## 3. 验证

```text
git diff --check -- 任务模块.管理线程协议.ixx 自我线程模块.impl.cpp
    结果：通过。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    结果：成功，0 警告，0 错误。

python .\tools\生成函数功能说明文档.py
    结果：已生成，源码文件 127，函数条目 7172。

python .\tools\check_specs.py
    结果：hard issues 0，warnings 81。
```

## 4. 当前判断

```text
C5 代码切片已闭合：
1. 代码缺陷型拒绝已有结构化开发诊断项承载，不再只停留在日志或拒绝回执文本。
2. 开发诊断项记录来源回执、来源任务 / 需求 / 方法、拒绝阶段、拒绝原因、复现上下文和关闭条件。
3. 开发诊断项 ID 已回填到拒绝回执，C4 来源任务阻断可继续读取同一回执。
4. 本切片没有实现开发期自动修复、编译后自动关闭或诊断 UI；开发诊断闭环不能宣称完整实现。
```

## 5. 下一步

```text
进入 C6：观察验证、学习、材料和参数闭环。
重点补不可判定转观察验证、学习缺口分类、外设材料闭环和参数诊断闭环的结构化承接。
```
