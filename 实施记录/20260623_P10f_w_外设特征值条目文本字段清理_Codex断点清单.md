# P10f-w 外设特征值条目文本字段清理 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-w `外设观察报告队列.ixx::结构_外设特征值条目::文本值 / 来源材料 / 来源动态` 边界复核与清理。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_cluster_handle_fields.tsv`

当前判断：

- `文本值` 未被写入或读取。
- `来源动态` 未被写入或读取。
- `来源材料` 经构建暴露由 `自我动作实现.扫描.ixx` 读取，用于判断外设特征数据项是否可回查；该字段不能直接删除，应按实际语义收束为句柄集合。
- 材料句柄仍由提交包头、候选材料句柄集、ROI / 掩码 / 点集 / 深度句柄集等结构承载；特征条目侧保留的是指向这些材料的 `来源材料句柄集`。

构建前预测：

- 删除 `结构_外设特征值条目` 的 `文本值 / 来源材料 / 来源动态` 字段。
- 将 `结构_外设特征值条目::来源材料` 改名为 `来源材料句柄集`，同步构造函数和扫描读取点。
- 删除 `结构_外设特征值条目` 的 `文本值 / 来源动态` 字段。
- 预期 Debug x64 构建通过，0 警告，0 错误。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。

## 1. 完成证据

已改文件：

- `外设观察报告队列.ixx`
- `自我动作实现.扫描.ixx`
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_feature_value_entry.tsv`

完成内容：

- 已删除 `结构_外设特征值条目::文本值`。
- 已删除 `结构_外设特征值条目::来源动态`。
- 已将 `结构_外设特征值条目::来源材料` 收束为 `来源材料句柄集`。
- 已同步更新 `外设提交_构造I64特征(...)` 参数、所有特征构造调用点，以及 `自我动作实现.扫描.ixx` 中的 `可回查` 判断。

复核证据：

- 首次构建失败暴露 `自我动作实现.扫描.ixx` 读取 `来源材料`；已改为读取 `来源材料句柄集`。
- `rg -n "文本值|std::vector<std::string> 来源动态|std::vector<std::string> 来源材料\\{|\\.来源材料(\\.|\\(|\\s|$)" 外设观察报告队列.ixx 自我动作实现.扫描.ixx` 无目标残留。
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_feature_value_entry.tsv` 已生成，剩余记录 622 行，其中主键 / ID / 键 / 句柄类 325 行、非主键类 297 行。
- 已处理本次触碰的 `自我动作实现.扫描.ixx` CRLF 提示，未做全仓换行。
- `git diff --check` 通过。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 第二次通过，0 警告，0 错误。

下一候选：

- P10f-x：复核 `外设观察报告队列.ixx::结构_外设特征值条目::单位` 是否仅定义 / 赋值无读取，可否直接删除。
