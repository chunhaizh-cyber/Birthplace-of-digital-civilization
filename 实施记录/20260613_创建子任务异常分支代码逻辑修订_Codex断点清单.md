# 20260613 创建子任务异常分支代码逻辑修订 Codex断点清单

## 当前切片

```text
计划：计划/20260612_创建子任务异常分支代码逻辑修订计划_v0.1.md
切片：P0 执行前断点与基线，准备进入 P1 字段不完整改为构造逻辑错误诊断。
触发：用户输入“执行”，当前计划索引无自动继续代码切片，按最小可执行原则选择不依赖自然样本/外设复现的创建子任务异常分支切片。
分支：main
HEAD：6886e6f
```

## 已读取依据

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
规范/需求树生长机制规范20260512.md
规范/任务筹办与执行边界规范20260512.md
规范/远程执行通道与断点续跑规范20260524.md
计划/计划索引.md
计划/20260612_创建子任务异常分支代码逻辑修订计划_v0.1.md
```

## 当前工作区边界

```text
本切片开始前已有未提交改动，涉及多处代码文件。
与本切片直接相关且已有未提交改动：
    任务模块.管理工作线程.ixx
    自我线程模块.impl.cpp
    自我线程模块.消息协议.ixx
本切片不得回滚这些既有改动；如需修改同文件，只在计划锚点附近小范围贴合现状。
```

## 代码基线

```text
任务模块.管理工作线程.ixx:1044
    私有_筹办派生需求消息字段完整 已检查父需求、派生需求类型、因果路径生成类型、阻塞级别、目标特征、目标主体、当前状态、目标状态、满足关系。

任务模块.管理工作线程.ixx:1187-1195
    字段不完整当前仍记录：
        任务管理工作线程/因果路径派生需求上报跳过 | 原因=字段不完整
    需要改为构造逻辑错误诊断，并列出缺失槽位。

任务模块.管理工作线程.ixx:1213-1244
    稳定键重复当前合并为 重复消息 计数，尚未拆分同批候选重复、响应内已包含、最近缓存命中。

自我线程模块.impl.cpp:10689
    私有_确保派生需求节点 是派生需求入树裁决入口。

自我线程模块.impl.cpp:10797-10805
    外设内部材料字段当前记录 自我线程/派生需求入树拒绝。

自我线程模块.impl.cpp:11005
    父目标重叠当前记录 自我线程/派生需求同目标停等，并会标记关联任务或父任务等待。

自我线程模块.impl.cpp:11425
    新需求为空时，回执原因摘要仍使用 派生需求已由提交入口裁决拒绝入树。
```

## 当前判断

```text
P0 基线完成。
P1 可以先在任务管理工作线程侧补“缺失槽位列表”和“派生需求消息构造逻辑错误”日志。
P1 不新增跨线程协议字段，不改变派生需求发送成功路径，不直接写需求树。
如发现任务管理工作线程没有可用逻辑错误弹窗入口，本切片先按计划记录错误日志，并把弹窗入口作为后续待补，不用吞错或降级为普通跳过。
```

## 下一步

```text
1. 修改 任务模块.管理工作线程.ixx：
       新增缺失槽位列表 helper；
       字段不完整时记录 任务管理工作线程/派生需求消息构造逻辑错误；
       日志包含缺失槽位、任务、执行尝试键、稳定键、消息文本、直接写需求树=0、实际发送派生需求消息=0。
2. 运行 git diff --check 的路径限定验证。
3. 若代码层通过，再决定是否构建 Debug x64。
```

## P1 代码修改记录

```text
已改文件：
    任务模块.管理工作线程.ixx

已完成：
    新增 私有_筹办派生需求消息缺失槽位文本。
    字段不完整分支从普通运行日志改为 项目运行错误日志。
    日志名改为 任务管理工作线程/派生需求消息构造逻辑错误。
    日志新增 缺失槽位、执行尝试键、稳定键。
    仍保持 直接写需求树=0、实际发送派生需求消息=0。

已运行：
    git diff --check -- .\任务模块.管理工作线程.ixx

结果：
    0 个空白错误；仅出现 LF/CRLF 工作区提示。

构建前预测：
    Debug x64 应能编译通过；
    若失败，优先检查新增 helper 的局部 lambda、std::vector<std::string> 或中文字符串拼接类型。
```

## P2-P5 代码修改记录

```text
已改文件：
    任务模块.管理工作线程.ixx
    自我线程模块.impl.cpp

已完成：
    P2 稳定键重复拆分为 同批候选重复、响应内已包含、最近缓存命中。
    P2 首个重复样本记录 任务管理工作线程/派生需求消息幂等去重。
    P3 新增派生需求入树原因分类枚举和 自我线程/派生需求入树原因分类 日志。
    P4 父需求与候选目标重叠不再标记任务等待，改为 自我线程/父子目标自复制保护。
    P5 派生需求入树回执和 提交_生成缺口子需求 日志携带入树原因分类。

已运行：
    git diff --check -- .\自我线程模块.impl.cpp
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m

构建结果：
    日志/20260613_P5_创建子任务异常分支_build.log
    退出码=0
    0 个错误
    1 个既有 LNK4075 警告

真实运行前预测：
    程序应正常退出；
    不应出现新增 [ERR]、[FTL]、弹窗错误提示、abort、Debug Error；
    若自然命中派生需求字段缺失，应出现 任务管理工作线程/派生需求消息构造逻辑错误；
    若自然命中重复派生需求，应出现 任务管理工作线程/派生需求消息幂等去重；
    若自然命中入树拒绝，应出现 自我线程/派生需求入树原因分类；
    若自然命中父目标重叠，应出现 自我线程/父子目标自复制保护；
    本次真实窗口不要求四类样本全部自然出现，未出现只能记为等待自然样本。
```

## P6 真实运行验证

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=20000

控制台日志：
    日志/20260613_P6_创建子任务异常分支_run.console.log

业务日志窗口：
    日志/鱼巢_run_20260613.低值g
    START_LINE=120274

结果：
    EXIT_CODE=0
    [ERR]=0
    [FTL]=0
    弹窗错误提示=0
    abort=0
    Debug Error=0

命中证据：
    日志/鱼巢_run_20260613.低值g:121434
        自我线程/派生需求入树原因分类 | 原因=父子目标自复制
    日志/鱼巢_run_20260613.低值g:121435
        自我线程/父子目标自复制保护 | 直接置父任务等待=0

本轮未自然命中：
    任务管理工作线程/派生需求消息构造逻辑错误=0
    任务管理工作线程/派生需求消息幂等去重=0

当前判断：
    P1-P5 代码层闭合，Debug x64 构建通过。
    P6 真实窗口验证通过且命中父子目标自复制保护自然样本。
    字段缺失和重复派生需求样本本轮未自然出现，不能宣称这两类业务样本已通过；后续只需等待自然样本复核日志口径。
```

## 禁止宣称

```text
不宣称创建子任务异常分支计划完成。
不宣称需求树真实生长已全量通过。
不宣称内部循环全部完成。
不把字段缺失、父子目标自复制或边界字段误入普通需求处理成普通跳过、正常停等或自由文本机器决策。
不恢复任务筹办 / 任务执行为本能方法。
```
