# 20260622 外设真实材料结构化改造 Codex断点清单

## 0. 恢复摘要

当前主动计划：`计划/计划索引.md` 第 1 章 `P10 项目结构体审计与清理计划 - 后续专项串行清理`。

当前切片：

```text
索引子切片 P10c：外设真实材料结构化改造前置审计。
源码对象：自我动作实现.扫描.ixx。
当前最小闭合切片：瘦身 `结构_外设特征材料`，保留真实扫描合同需要的材料数量和可回查状态。
```

已复核依据：

```text
规范/禁止性规范20260611.md：第 2 章项目通用数据结构闭环。
计划/20260618_项目结构体审计与清理计划_v0.1.md：P3/P4 结构体审计和删除前必须确认写入方、读取方、调用路径。
用户补充删除闸门：只有定义和赋值 / 成员声明的结构体可以直接删除；有字段读取或真实运行路径的结构只做结构化改造 / 瘦身。
```

当前证据：

```text
`结构_外设特征材料` 有构造函数 `构造外设特征材料`，并通过 `数据项.特征材料集合.front().可回查` 被读取，因此不能按“只剩定义”直接删除。
但实际读取只需要两个事实：
    1. 非已知存在类数据项有无特征材料，用于合同检查。
    2. 特征材料是否可回查，用于 `数据项.可回查` 合并。
未发现 `当前文本值 / 特征值generation / 来源材料句柄 / 当前I64值 / 当前F64值` 等镜像字段被后续读取。
```

项目结构映射：

| 业务概念 | 当前承载结构 | 目标承载结构 | 值类型 | 写入方 | 读取方 | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- |
| 特征材料存在性 | `std::vector<结构_外设特征材料> 特征材料集合` | `结构_已合法外设数据项::特征材料数量` | `std::uint32_t` | `展开已合法外设数据项::追加特征项` | `已合法外设数据项合同满足`、上游契约诊断日志 | 源码复扫、Debug x64 构建 |
| 特征材料可回查状态 | `结构_外设特征材料::可回查` | `结构_已合法外设数据项::可回查` | `bool` | `展开已合法外设数据项::追加特征项` | 后续扫描数据项处理和合同诊断链 | 源码复扫、Debug x64 构建 |

构建前预测：

```text
删除 `结构_外设特征材料` 和 `构造外设特征材料`，把 `特征材料集合.empty()/size()` 改成 `特征材料数量`，把 `.front().可回查` 改成对来源材料句柄 / 包头材料句柄的直接布尔合并；预期 Debug x64 构建通过。
```

待验证：

```text
已执行源码修改、函数说明文档刷新、源码复扫和 Debug x64 构建。
函数文档命令：python .\tools\生成函数功能说明文档.py
函数文档结果：函数条目 7255。
复扫结果：`结构_外设特征材料 / 构造外设特征材料 / 特征材料集合 / 当前文本值 / 特征值generation / 来源材料句柄` 源码命中 0。
构建命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
构建日志：日志/20260622_P10c_外设材料结构瘦身_build.log
构建结果：exit_code=0，未检出 error / fatal / warning。
```

当前停止点：

```text
P10c 第一小切片已闭合：
    删除 `结构_外设特征材料`。
    删除 `构造外设特征材料`。
    `结构_已合法外设数据项::特征材料集合` 改为 `特征材料数量`。
    合同检查从 `特征材料集合.empty()` 改为 `特征材料数量 == 0`。
    上游契约诊断日志从 `特征材料集合.size()` 改为 `特征材料数量`。
    可回查状态由 `项.来源材料` 和 `包.包头.材料句柄集` 直接合并到 `数据项.可回查`。
未硬删 `结构_已合法外设数据项`，因为它仍承担扫描报告展开、合同检查、对应关系判断、空间位置写入和缺口记录的真实运行路径。
```

下一步：

```text
返回计划索引；P10 后续专项进入函数去重专项或按索引重新选择下一个无堵点切片。
```
