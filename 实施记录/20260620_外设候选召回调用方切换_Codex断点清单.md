# 20260620 外设候选召回调用方切换 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.4 P5.3a，外设调用方切换到场景类 DTO / helper。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
实施记录/20260620_场景候选召回helper_Codex断点清单.md
```

## 1. 问题锚点

```text
自我动作实现.外设模块.ixx:11112 使用 结构_世界树场景轮廓存在比较结果。
自我动作实现.外设模块.ixx:11122 使用 结构_世界树场景轮廓查找参数。
自我动作实现.外设模块.ixx:11135 调用 世界树.查找场景存在_按深度或彩图轮廓图。
```

当前判断：

```text
本切片只切换外设调用方到场景类 DTO / helper。
世界树旧 DTO / 包装入口暂不删除，待调用方切换验证后进入下一切片。
```

验证前预测：

```text
1. 自我动作实现.外设模块.ixx 不再使用结构_世界树场景轮廓查找参数 / 结构_世界树场景轮廓存在比较结果。
2. D455 候选召回调用改为 世界树.场景().查找存在_按深度或彩图轮廓图，并显式传入 世界树.值池() 和 世界树.存在概念根。
3. 函数说明文档生成退出码应为 0。
4. Debug x64 构建应通过。
```

## 2. 当前执行进展

已修改：

```text
1. 自我动作实现.外设模块.ixx 的三维体素查找结果集合改用 结构_场景三维体素轮廓存在比较结果。
2. 查找参数改用 结构_场景三维体素轮廓查找参数。
3. 候选召回调用改为 世界树.场景().查找存在_按深度或彩图轮廓图，并显式传入 世界树.值池() 和 世界树.存在概念根。
```

已验证：

```text
rg -n "结构_世界树场景轮廓查找参数|结构_世界树场景轮廓存在比较结果|查找场景存在_按深度或彩图轮廓图|结构_场景三维体素轮廓|场景\\(\\)\\.查找存在_按深度或彩图轮廓图" 自我动作实现.外设模块.ixx 世界树类.h 世界树类.cpp 说明书/函数功能说明文档.md
结果：外设调用方已切换到场景 DTO / helper；旧世界树 DTO 和旧包装入口只剩世界树类自身及函数说明文档。

python .\tools\生成函数功能说明文档.py
结果：exit=0，输出见 日志/20260620_外设候选召回调用方切换_函数文档.log。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：exit=0，构建日志见 日志/20260620_外设候选召回调用方切换_msbuild.log；0 警告。
```

当前判断：

```text
P5.3a 已闭合。
下一步进入 P5.3b：删除世界树命名 DTO 和旧包装入口。
```

禁止宣称：

```text
不宣称世界树命名 DTO 已删除。
不宣称世界树场景辅助函数迁移完成。
不宣称场景类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
