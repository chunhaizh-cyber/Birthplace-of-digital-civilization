# 20260618 基础数据类型.ixx 函数用途签名审计

生成时间：2026-06-18 23:50:36

## 1. 审计口径

本表按用户补充迁移方式生成：先打开单个源代码定义文件，逐函数分析用途并生成功能签名，再判断公用 / 专用归属。

最新口径：`I64`、`比较量`、`三向关系掩码`、`方向掩码`、`VecI64`、`VecIU64` 等基础别名 / 基础容器可以进入 `全局共享函数类.ixx`；限制领域自定义类型进入全局共享模块的目的，是防止头引用环、模块 import 环和领域对象耦合。

仍需专用处理：结构体成员函数、领域结构体、节点类、主信息类、场景 / 特征 / 需求 / 任务 / 方法 / 因果 / 动态 / 外设材料 / 控制面板 DTO。

## 2. 汇总

| 项目 | 数量 |
| --- | ---: |
| 基础数据类型.ixx 函数条目 | 99 |
| 专用域函数 | 83 |
| 全局共享候选 | 16 |

## 3. 迁移结论

本轮发现若干全局共享候选，主要是模块顶层的比较量、三向关系和时间戳等基础工具函数。迁移前必须先在 `全局共享函数类.ixx` 查找同功能函数，并确认不会形成 `基础数据类型模块 -> 全局共享函数类 -> 基础数据类型模块` 的 import 环。

### 3.1 2026-06-18 复核结果

`全局共享函数类.ixx` 当前只有模块声明和边界注释，没有同功能函数。16 个候选均依赖 `基础数据类型.h/.ixx` 当前定义的 `I64`、`比较量`、`枚举_三向关系`、`三向关系掩码`、`方向掩码`、`VecI64` 或 `VecIU64`。

当前不能按普通重复函数迁移到 `全局共享函数类.ixx`：

```text
1. 若 全局共享函数类.ixx 为使用这些类型而 import 基础数据类型模块，
   再让 基础数据类型模块 import / export 全局共享函数类 以保留现有调用名，
   会形成 基础数据类型模块 -> 全局共享函数类 -> 基础数据类型模块 的环。
2. 若直接把这些基础别名 / 枚举 / 容器下沉到 全局共享函数类.ixx，
   等于改变基础类型权威归属，不是单函数迁移切片。
3. 若只在 全局共享函数类.ixx 新增实现并在 基础数据类型.h/.ixx 保留转发函数，
   会保留旧入口 / 双入口，不满足本计划“迁移后删除原重复定义并改变引用方式”的约束。
4. `基础数据类型.h` 仍被多个当前源码入口 include，不是可直接删除的无引用头文件。
```

因此本轮对这 16 个候选的处理决定为：暂不迁移、暂不删除，转入“基础类型权威归属 / 头文件与模块双入口收束”专项确认。

### 3.2 2026-06-19 最终处理结果

按用户最新口径，`.ixx` 合并遇到 MSVC 模块 / STL 重定义问题后，基础数据类型入口改为 `.h` 单入口：

```text
1. `基础数据类型.h` 保留为唯一基础数据类型定义文件。
2. `基础数据类型.ixx` 已删除，并从 `鱼巢.vcxproj` / `鱼巢.vcxproj.filters` 移除。
3. 全部 `import 基础数据类型模块` 已改为 `#include "基础数据类型.h"`。
4. `说明书/函数功能说明文档.md` 已重生成，基础工具函数只剩 `基础数据类型.h` 条目。
5. Debug x64 串行构建成功，0 警告，0 错误。
```

本文件第 4 章和第 5 章保留为删除前的 `.ixx` 审计证据；其中 `export` 签名和 `.ixx` 行号仅作历史定位，不再代表当前源码入口。当前源码入口以 `基础数据类型.h` 和 `说明书/函数功能说明文档.md` 为准。

## 4. 全局共享候选

| 函数 | 行号 | 原始签名 | 功能签名 | 迁移前置条件 |
| --- | ---: | --- | --- | --- |
| `三向关系被接受` | 73 | `export inline bool 三向关系被接受( const 枚举_三向关系 关系, const 三向关系掩码 掩码) noexcept {` | 类别=比较/判定;输入=std或基础别名/基础容器;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=inline+noexcept | 查找全局同功能；复核 import 方向 |
| `比较I64_三态` | 93 | `export inline constexpr 比较量 比较I64_三态(const I64 左, const I64 右) noexcept {` | 类别=比较/判定;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+inline+noexcept | 查找全局同功能；复核 import 方向 |
| `比较U64_三态` | 99 | `export inline constexpr 比较量 比较U64_三态(const std::uint64_t 左, const std::uint64_t 右) noexcept {` | 类别=比较/判定;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+inline+noexcept | 查找全局同功能；复核 import 方向 |
| `比较符号` | 105 | `export inline constexpr std::int8_t 比较符号(const 比较量 r) noexcept {` | 类别=比较/判定;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+inline+noexcept | 查找全局同功能；复核 import 方向 |
| `比较量转三向关系` | 111 | `export inline constexpr 枚举_三向关系 比较量转三向关系(const 比较量 r) noexcept {` | 类别=比较/判定;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+inline+noexcept | 查找全局同功能；复核 import 方向 |
| `比较量方向掩码` | 119 | `export inline constexpr 三向关系掩码 比较量方向掩码(const 比较量 r) noexcept {` | 类别=比较/判定;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+inline+noexcept | 查找全局同功能；复核 import 方向 |
| `比较量命中三向关系` | 125 | `export inline constexpr bool 比较量命中三向关系( const 比较量 r, const 三向关系掩码 掩码) noexcept {` | 类别=比较/判定;输入=std或基础别名/基础容器;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+inline+noexcept | 查找全局同功能；复核 import 方向 |
| `比较量命中方向` | 133 | `export inline constexpr bool 比较量命中方向( const 比较量 r, const 方向掩码 允许方向) noexcept {` | 类别=比较/判定;输入=std或基础别名/基础容器;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+inline+noexcept | 查找全局同功能；复核 import 方向 |
| `编码I64为U64_ZigZag` | 724 | `export inline constexpr std::uint64_t 编码I64为U64_ZigZag(const I64 值) noexcept {` | 类别=基础数据结构函数;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+inline+noexcept | 查找全局同功能；复核 import 方向 |
| `解码U64为I64_ZigZag` | 732 | `export inline constexpr I64 解码U64为I64_ZigZag(const std::uint64_t 值) noexcept {` | 类别=基础数据结构函数;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+inline+noexcept | 查找全局同功能；复核 import 方向 |
| `编码I64为U64` | 741 | `export inline constexpr std::uint64_t 编码I64为U64(const I64 值) noexcept {` | 类别=基础数据结构函数;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+inline+noexcept | 查找全局同功能；复核 import 方向 |
| `解码U64为I64` | 747 | `export inline constexpr I64 解码U64为I64(const std::uint64_t 值) noexcept {` | 类别=基础数据结构函数;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+inline+noexcept | 查找全局同功能；复核 import 方向 |
| `编码VecI64为VecIU64_ZigZag` | 753 | `export inline VecIU64 编码VecI64为VecIU64_ZigZag(const VecI64& 值) {` | 类别=基础数据结构函数;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=inline | 查找全局同功能；复核 import 方向 |
| `解码VecIU64为VecI64_ZigZag` | 764 | `export inline VecI64 解码VecIU64为VecI64_ZigZag(const VecIU64& 值) {` | 类别=基础数据结构函数;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=inline | 查找全局同功能；复核 import 方向 |
| `编码VecI64为VecIU64` | 775 | `export inline VecIU64 编码VecI64为VecIU64(const VecI64& 值) {` | 类别=基础数据结构函数;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=inline | 查找全局同功能；复核 import 方向 |
| `解码VecIU64为VecI64` | 781 | `export inline VecI64 解码VecIU64为VecI64(const VecIU64& 值) {` | 类别=基础数据结构函数;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=inline | 查找全局同功能；复核 import 方向 |

## 5. 逐函数用途签名

| 函数 | 行号 | 所属定义域 | 用途备注 | 原始签名 | 功能签名 | 迁移归属判断 |
| --- | ---: | --- | --- | --- | --- | --- |
| `有效` | 29 | `I64区间` | 有效性校验：有效 | `constexpr bool 有效() const noexcept { return 低值 <= 高值; }` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `是否点` | 32 | `I64区间` | 有效性校验：是否点 | `constexpr bool 是否点() const noexcept { return 低值 == 高值; }` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `点值` | 35 | `I64区间` | 索引/读取：点值 | `constexpr std::int64_t 点值() const noexcept { return 低值; }` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `包含` | 38 | `I64区间` | 比较/判定：包含 | `constexpr bool 包含(std::int64_t v) const noexcept { return v >= 低值 && v <= 高值; }` | 类别=比较/判定;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `点` | 41 | `I64区间` | 索引/读取：点 | `static constexpr I64区间 点(std::int64_t v) noexcept { return I64区间{ v, v }; }` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+noexcept+static | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `三向关系被接受` | 73 | `模块顶层` | 比较/判定：三向关系被接受 | `export inline bool 三向关系被接受( const 枚举_三向关系 关系, const 三向关系掩码 掩码) noexcept {` | 类别=比较/判定;输入=std或基础别名/基础容器;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=inline+noexcept | 全局共享候选：基础别名/基础容器允许，需查找同功能并确认 import 方向 |
| `比较I64_三态` | 93 | `模块顶层` | 有符号比较量规则工具： 比较函数可返回任意负数、0、任意正数；基础逻辑只读取符号。 | `export inline constexpr 比较量 比较I64_三态(const I64 左, const I64 右) noexcept {` | 类别=比较/判定;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+inline+noexcept | 全局共享候选：基础别名/基础容器允许，需查找同功能并确认 import 方向 |
| `比较U64_三态` | 99 | `模块顶层` | 比较当前值、目标值或历史基准。 | `export inline constexpr 比较量 比较U64_三态(const std::uint64_t 左, const std::uint64_t 右) noexcept {` | 类别=比较/判定;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+inline+noexcept | 全局共享候选：基础别名/基础容器允许，需查找同功能并确认 import 方向 |
| `比较符号` | 105 | `模块顶层` | 比较当前值、目标值或历史基准。 | `export inline constexpr std::int8_t 比较符号(const 比较量 r) noexcept {` | 类别=比较/判定;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+inline+noexcept | 全局共享候选：基础别名/基础容器允许，需查找同功能并确认 import 方向 |
| `比较量转三向关系` | 111 | `模块顶层` | 比较当前值、目标值或历史基准。 | `export inline constexpr 枚举_三向关系 比较量转三向关系(const 比较量 r) noexcept {` | 类别=比较/判定;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+inline+noexcept | 全局共享候选：基础别名/基础容器允许，需查找同功能并确认 import 方向 |
| `比较量方向掩码` | 119 | `模块顶层` | 比较当前值、目标值或历史基准。 | `export inline constexpr 三向关系掩码 比较量方向掩码(const 比较量 r) noexcept {` | 类别=比较/判定;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+inline+noexcept | 全局共享候选：基础别名/基础容器允许，需查找同功能并确认 import 方向 |
| `比较量命中三向关系` | 125 | `模块顶层` | 比较当前值、目标值或历史基准。 | `export inline constexpr bool 比较量命中三向关系( const 比较量 r, const 三向关系掩码 掩码) noexcept {` | 类别=比较/判定;输入=std或基础别名/基础容器;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+inline+noexcept | 全局共享候选：基础别名/基础容器允许，需查找同功能并确认 import 方向 |
| `比较量命中方向` | 133 | `模块顶层` | 比较当前值、目标值或历史基准。 | `export inline constexpr bool 比较量命中方向( const 比较量 r, const 方向掩码 允许方向) noexcept {` | 类别=比较/判定;输入=std或基础别名/基础容器;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+inline+noexcept | 全局共享候选：基础别名/基础容器允许，需查找同功能并确认 import 方向 |
| `可解析引用` | 156 | `可解析引用` | 解析输入文本、消息、场景或运行包。 | `可解析引用(T节点* 节点) { 绑定(节点); }` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=指针/引用/句柄;副作用=无明显外部副作用;领域=基础数据类型;约束=无显式约束 | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `可解析引用` | 159 | `可解析引用` | 解析输入文本、消息、场景或运行包。 | `可解析引用(T节点* 节点, const std::string& 主键值) { 绑定(节点, 主键值); }` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=指针/引用/句柄;副作用=无明显外部副作用;领域=基础数据类型;约束=无显式约束 | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `有效` | 162 | `可解析引用` | 有效性校验：有效 | `bool 有效() const noexcept { return 指针 != nullptr \|\| !主键.empty(); }` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `获取` | 165 | `可解析引用` | 读取并返回指定对象、状态或运行材料。 | `T节点* 获取() const noexcept { return 指针; }` | 类别=引用/绑定;输入=领域结构/成员所属类型;输出=指针/引用/句柄;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `operator T节点*` | 169 | `可解析引用` | 索引/读取：operator T节点* | `operator T节点* () const noexcept { return 指针; }` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=指针/引用/句柄;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `operator=` | 172 | `可解析引用` | 索引/读取：operator= | `可解析引用& operator=(T节点* 节点) {` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=指针/引用/句柄;副作用=修改对象状态;领域=基础数据类型;约束=无显式约束 | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `绑定` | 179 | `可解析引用` | 建立对象、任务、方法或因果之间的绑定关系。 | `void 绑定(T节点* 节点) {` | 类别=引用/绑定;输入=领域结构/成员所属类型;输出=void;副作用=修改对象状态;领域=基础数据类型;约束=无显式约束 | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `绑定` | 186 | `可解析引用` | 建立对象、任务、方法或因果之间的绑定关系。 | `void 绑定(T节点* 节点, const std::string& 主键值) {` | 类别=引用/绑定;输入=领域结构/成员所属类型;输出=void;副作用=修改对象状态;领域=基础数据类型;约束=无显式约束 | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `清空` | 195 | `可解析引用` | 清理：清空 | `void 清空() noexcept { 指针 = nullptr; 主键.clear(); }` | 类别=清理;输入=领域结构/成员所属类型;输出=void;副作用=修改对象状态;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `私有_提取主键` | 211 | `可解析引用` | 服务所在模块的内部辅助流程。 | `static std::string 私有_提取主键(T节点* 节点) {` | 类别=基础数据结构函数;输入=领域结构/成员所属类型;输出=指针/引用/句柄;副作用=无明显外部副作用;领域=基础数据类型;约束=static | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `有效` | 222 | `结构_基础信息轻引用` | 有效性校验：有效 | `bool 有效() const noexcept { return 指针 != 0 \|\| !主键.empty(); }` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `清空` | 238 | `结构_基础信息轻引用` | 清理：清空 | `void 清空() noexcept {` | 类别=清理;输入=领域结构/成员所属类型;输出=void;副作用=修改对象状态;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `记录观测` | 273 | `结构_统计` | 记录日志、动态、证据或运行痕迹。 | `void 记录观测(时间戳 now) noexcept {` | 类别=基础数据结构函数;输入=领域结构/成员所属类型;输出=void;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `有效` | 293 | `VecU句柄` | 有效性校验：有效 | `bool 有效() const noexcept { return 主信息指针 != 0; }` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `有效` | 300 | `指针句柄` | 有效性校验：有效 | `bool 有效() const noexcept { return 指针 != 0; }` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `operator==` | 304 | `模块顶层` | 索引/读取：operator== | `export inline bool operator==(const VecU句柄& a, const VecU句柄& b) noexcept {` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=bool;副作用=修改对象状态;领域=基础数据类型;约束=inline+noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `operator==` | 309 | `模块顶层` | 索引/读取：operator== | `export inline bool operator==(const 指针句柄& a, const 指针句柄& b) noexcept {` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=bool;副作用=修改对象状态;领域=基础数据类型;约束=inline+noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `特征值有效` | 317 | `模块顶层` | 有效性校验：特征值有效 | `export inline bool 特征值有效(const 特征值& v) noexcept {` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=inline+noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `特征快照有值` | 322 | `模块顶层` | 基础数据结构函数：特征快照有值 | `export inline bool 特征快照有值(const 特征快照值& v) noexcept {` | 类别=基础数据结构函数;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=inline+noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `结构体_分词` | 536 | `结构体_分词` | 基础数据结构函数：结构体_分词 | `结构体_分词(std::string 词_, std::string 词性_) :词(词_), 词性(词性_) {` | 类别=基础数据结构函数;输入=领域结构/成员所属类型;输出=其他;副作用=无明显外部副作用;领域=基础数据类型;约束=无显式约束 | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `operator==` | 546 | `结构体_空间坐标` | 索引/读取：operator== | `friend bool operator==(const 结构体_空间坐标& a, const 结构体_空间坐标& b) {` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=bool;副作用=修改对象状态;领域=基础数据类型;约束=无显式约束 | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `operator-` | 551 | `结构体_空间坐标` | 索引/读取：operator- | `结构体_空间坐标 operator-(const 结构体_空间坐标& o) const {` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=其他;副作用=无明显外部副作用;领域=基础数据类型;约束=无显式约束 | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `当前_微秒` | 605 | `结构体_时间戳` | 当前时间戳（微秒） | `static 时间戳 当前_微秒() noexcept {` | 类别=时间工具;输入=领域结构/成员所属类型;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept+static | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `当前_纳秒` | 616 | `结构体_时间戳` | 当前时间戳（纳秒，可选） | `static 时间戳 当前_纳秒() noexcept {` | 类别=时间工具;输入=领域结构/成员所属类型;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept+static | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `时间差_微秒` | 627 | `结构体_时间戳` | 计算时间差（微秒，返回无符号；如果担心顺序，提供一个有符号版本更安全） | `static 时间戳 时间差_微秒(时间戳 t0, 时间戳 t1) noexcept {` | 类别=时间工具;输入=领域结构/成员所属类型;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept+static | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `时间差_微秒_有符号` | 634 | `结构体_时间戳` | 更推荐：有符号差值（允许 t1 &lt; t0 的情况） | `static std::int64_t 时间差_微秒_有符号(时间戳 t0, 时间戳 t1) noexcept {` | 类别=时间工具;输入=领域结构/成员所属类型;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept+static | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `编码I64为U64_ZigZag` | 724 | `模块顶层` | 基础数据结构函数：编码I64为U64_ZigZag | `export inline constexpr std::uint64_t 编码I64为U64_ZigZag(const I64 值) noexcept {` | 类别=基础数据结构函数;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+inline+noexcept | 全局共享候选：基础别名/基础容器允许，需查找同功能并确认 import 方向 |
| `解码U64为I64_ZigZag` | 732 | `模块顶层` | 基础数据结构函数：解码U64为I64_ZigZag | `export inline constexpr I64 解码U64为I64_ZigZag(const std::uint64_t 值) noexcept {` | 类别=基础数据结构函数;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+inline+noexcept | 全局共享候选：基础别名/基础容器允许，需查找同功能并确认 import 方向 |
| `编码I64为U64` | 741 | `模块顶层` | 基础数据结构函数：编码I64为U64 | `export inline constexpr std::uint64_t 编码I64为U64(const I64 值) noexcept {` | 类别=基础数据结构函数;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+inline+noexcept | 全局共享候选：基础别名/基础容器允许，需查找同功能并确认 import 方向 |
| `解码U64为I64` | 747 | `模块顶层` | 基础数据结构函数：解码U64为I64 | `export inline constexpr I64 解码U64为I64(const std::uint64_t 值) noexcept {` | 类别=基础数据结构函数;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr+inline+noexcept | 全局共享候选：基础别名/基础容器允许，需查找同功能并确认 import 方向 |
| `编码VecI64为VecIU64_ZigZag` | 753 | `模块顶层` | 基础数据结构函数：编码VecI64为VecIU64_ZigZag | `export inline VecIU64 编码VecI64为VecIU64_ZigZag(const VecI64& 值) {` | 类别=基础数据结构函数;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=inline | 全局共享候选：基础别名/基础容器允许，需查找同功能并确认 import 方向 |
| `解码VecIU64为VecI64_ZigZag` | 764 | `模块顶层` | 基础数据结构函数：解码VecIU64为VecI64_ZigZag | `export inline VecI64 解码VecIU64为VecI64_ZigZag(const VecIU64& 值) {` | 类别=基础数据结构函数;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=inline | 全局共享候选：基础别名/基础容器允许，需查找同功能并确认 import 方向 |
| `编码VecI64为VecIU64` | 775 | `模块顶层` | 基础数据结构函数：编码VecI64为VecIU64 | `export inline VecIU64 编码VecI64为VecIU64(const VecI64& 值) {` | 类别=基础数据结构函数;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=inline | 全局共享候选：基础别名/基础容器允许，需查找同功能并确认 import 方向 |
| `解码VecIU64为VecI64` | 781 | `模块顶层` | 基础数据结构函数：解码VecIU64为VecI64 | `export inline VecI64 解码VecIU64为VecI64(const VecIU64& 值) {` | 类别=基础数据结构函数;输入=std或基础别名/基础容器;输出=整数/比较量/时间;副作用=无明显外部副作用;领域=基础数据类型;约束=inline | 全局共享候选：基础别名/基础容器允许，需查找同功能并确认 import 方向 |
| `有效` | 802 | `I64多维区间` | 有效性校验：有效 | `bool 有效() const noexcept {` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `维度数` | 809 | `I64多维区间` | 基础数据结构函数：维度数 | `std::size_t 维度数() const noexcept { return 维度.size(); }` | 类别=基础数据结构函数;输入=领域结构/成员所属类型;输出=其他;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `Vector3D` | 958 | `Vector3D` | 基础数据结构函数：Vector3D | `constexpr Vector3D(double xx, double yy, double zz) : x(xx), y(yy), z(zz) {}` | 类别=基础数据结构函数;输入=领域结构/成员所属类型;输出=浮点/几何值;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `operator*` | 963 | `Vector3D` | ===== 关键：标量乘法（成员函数）===== | `constexpr Vector3D operator*(double scalar) const {` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=浮点/几何值;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `operator/` | 970 | `Vector3D` | ===== 推荐同时添加：标量除法 ===== | `constexpr Vector3D operator/(double scalar) const {` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=浮点/几何值;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `operator+` | 978 | `Vector3D` | ===== 推荐添加：向量加法/减法 ===== | `constexpr Vector3D operator+(const Vector3D& other) const {` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=其他;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `operator-` | 983 | `Vector3D` | 索引/读取：operator- | `constexpr Vector3D operator-(const Vector3D& other) const {` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=其他;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `operator*=` | 990 | `Vector3D` | ===== 推荐添加：复合赋值运算符 ===== | `Vector3D& operator*=(double scalar) {` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=浮点/几何值;副作用=修改对象状态;领域=基础数据类型;约束=无显式约束 | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `operator/=` | 996 | `Vector3D` | 索引/读取：operator/= | `Vector3D& operator/=(double scalar) {` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=浮点/几何值;副作用=修改对象状态;领域=基础数据类型;约束=无显式约束 | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `operator+=` | 1004 | `Vector3D` | 索引/读取：operator+= | `Vector3D& operator+=(const Vector3D& other) {` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=其他;副作用=修改对象状态;领域=基础数据类型;约束=无显式约束 | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `operator-=` | 1010 | `Vector3D` | 索引/读取：operator-= | `Vector3D& operator-=(const Vector3D& other) {` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=其他;副作用=修改对象状态;领域=基础数据类型;约束=无显式约束 | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `单位` | 1028 | `Quaternion` | 几何/数学：单位 | `static Quaternion 单位() noexcept { return {}; }` | 类别=几何/数学;输入=领域结构/成员所属类型;输出=其他;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept+static | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `范数` | 1031 | `Quaternion` | 基础数据结构函数：范数 | `double 范数() const noexcept { return std::sqrt(w * w + x * x + y * y + z * z); }` | 类别=基础数据结构函数;输入=领域结构/成员所属类型;输出=浮点/几何值;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `归一化` | 1034 | `Quaternion` | 几何/数学：归一化 | `void 归一化() noexcept {` | 类别=几何/数学;输入=领域结构/成员所属类型;输出=void;副作用=修改对象状态;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `共轭` | 1041 | `Quaternion` | 几何/数学：共轭 | `Quaternion 共轭() const noexcept { return { w, -x, -y, -z }; }` | 类别=几何/数学;输入=领域结构/成员所属类型;输出=其他;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `operator*` | 1046 | `Quaternion` | 复合旋转：this ⊗ r | `Quaternion operator*(const Quaternion& r) const noexcept {` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=指针/引用/句柄;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `旋转` | 1058 | `Quaternion` | 旋转向量：v' = q ⊗ (0,v) ⊗ q* (注意：取决于你对 q 的语义约定；此处提供常用形式) | `Vector3D 旋转(const Vector3D& v) const noexcept {` | 类别=几何/数学;输入=领域结构/成员所属类型;输出=其他;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `operator*` | 1084 | `模块顶层` | ===== 额外：非成员函数版本，支持 double * Vector3D（如 0.5 * vec）===== | `export constexpr Vector3D operator*(double scalar, const Vector3D& vec) {` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=浮点/几何值;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `operator/` | 1089 | `模块顶层` | 索引/读取：operator/ | `export constexpr Vector3D operator/(double scalar, const Vector3D& vec) {` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=浮点/几何值;副作用=无明显外部副作用;领域=基础数据类型;约束=constexpr | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `valid` | 1188 | `RawImage` | 有效性校验：valid | `bool valid() const { return w > 0 && h > 0 && (int)data.size() == w * h; }` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=无显式约束 | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `at` | 1191 | `RawImage` | 索引/读取：at | `T& at(int x, int y) { return data[(size_t)y * (size_t)w + (size_t)x]; }` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=指针/引用/句柄;副作用=无明显外部副作用;领域=基础数据类型;约束=无显式约束 | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `at` | 1194 | `RawImage` | 索引/读取：at | `const T& at(int x, int y) const { return data[(size_t)y * (size_t)w + (size_t)x]; }` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=指针/引用/句柄;副作用=无明显外部副作用;领域=基础数据类型;约束=无显式约束 | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `有效深度` | 1254 | `结构体_原始场景帧` | 有效性校验：有效深度 | `[[nodiscard]] bool 有效深度() const noexcept {` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `有效颜色` | 1260 | `结构体_原始场景帧` | 有效性校验：有效颜色 | `[[nodiscard]] bool 有效颜色() const noexcept { return 颜色.size() == 深度.size(); }` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `有效点云` | 1263 | `结构体_原始场景帧` | 有效性校验：有效点云 | `[[nodiscard]] bool 有效点云() const noexcept { return 点云.size() == 深度.size(); }` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `有效深度掩膜` | 1266 | `结构体_原始场景帧` | 有效性校验：有效深度掩膜 | `[[nodiscard]] bool 有效深度掩膜() const noexcept { return 深度有效.size() == 深度.size(); }` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `有效前景提示` | 1269 | `结构体_原始场景帧` | 有效性校验：有效前景提示 | `[[nodiscard]] bool 有效前景提示() const noexcept { return 前景提示.size() == 深度.size(); }` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `索引` | 1272 | `结构体_原始场景帧` | 索引/读取：索引 | `[[nodiscard]] inline std::size_t 索引(int u, int v) const noexcept {` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=其他;副作用=无明显外部副作用;领域=基础数据类型;约束=inline+noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `深度_at` | 1277 | `结构体_原始场景帧` | 索引/读取：深度_at | `[[nodiscard]] inline double 深度_at(int u, int v) const noexcept { return 深度[索引(u, v)]; }` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=浮点/几何值;副作用=无明显外部副作用;领域=基础数据类型;约束=inline+noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `深度有效_at` | 1280 | `结构体_原始场景帧` | 有效性校验：深度有效_at | `[[nodiscard]] inline bool 深度有效_at(int u, int v) const noexcept {` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=inline+noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `颜色_at` | 1285 | `结构体_原始场景帧` | 索引/读取：颜色_at | `[[nodiscard]] inline Color 颜色_at(int u, int v) const noexcept {` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=其他;副作用=无明显外部副作用;领域=基础数据类型;约束=inline+noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `点云_at` | 1290 | `结构体_原始场景帧` | 索引/读取：点云_at | `[[nodiscard]] inline Vector3D 点云_at(int u, int v) const noexcept {` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=其他;副作用=无明显外部副作用;领域=基础数据类型;约束=inline+noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `operator\|` | 1313 | `模块顶层` | 索引/读取：operator\| | `export inline 枚举_存在观测缓冲 operator\|(枚举_存在观测缓冲 a, 枚举_存在观测缓冲 b) {` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=其他;副作用=无明显外部副作用;领域=基础数据类型;约束=inline | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `Has` | 1318 | `模块顶层` | 有效性校验：Has | `export inline bool Has(枚举_存在观测缓冲 mask, 枚举_存在观测缓冲 bit) {` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=inline | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `点索引` | 1329 | `点索引` | 索引/读取：点索引 | `点索引(int uu, int vv) : u(uu), v(vv) {}` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=其他;副作用=无明显外部副作用;领域=基础数据类型;约束=无显式约束 | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `点索引` | 1332 | `点索引` | 索引/读取：点索引 | `点索引() {}` | 类别=索引/读取;输入=领域结构/成员所属类型;输出=其他;副作用=无明显外部副作用;领域=基础数据类型;约束=无显式约束 | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `有效` | 1349 | `结构体_图像缓冲_u8` | 有效性校验：有效 | `inline bool 有效() const {` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=inline | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `期望字节数` | 1354 | `结构体_图像缓冲_u8` | 基础数据结构函数：期望字节数 | `inline std::size_t 期望字节数() const {` | 类别=基础数据结构函数;输入=领域结构/成员所属类型;输出=其他;副作用=无明显外部副作用;领域=基础数据类型;约束=inline | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `清空` | 1360 | `结构体_图像缓冲_u8` | 清理：清空 | `inline void 清空() { w = h = c = stride_bytes = 0; data.clear(); data.shrink_to_fit(); }` | 类别=清理;输入=领域结构/成员所属类型;输出=void;副作用=修改对象状态;领域=基础数据类型;约束=inline | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `有效` | 1369 | `结构体_图像缓冲_f32` | 有效性校验：有效 | `inline bool 有效() const { return w > 0 && h > 0 && c > 0 && !data.empty(); }` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=inline | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `期望元素数` | 1372 | `结构体_图像缓冲_f32` | 基础数据结构函数：期望元素数 | `inline std::size_t 期望元素数() const { return (std::size_t)w * (std::size_t)h * (std::size_t)c; }` | 类别=基础数据结构函数;输入=领域结构/成员所属类型;输出=指针/引用/句柄;副作用=无明显外部副作用;领域=基础数据类型;约束=inline | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `清空` | 1375 | `结构体_图像缓冲_f32` | 清理：清空 | `inline void 清空() { w = h = c = 0; data.clear(); data.shrink_to_fit(); }` | 类别=清理;输入=领域结构/成员所属类型;输出=void;副作用=修改对象状态;领域=基础数据类型;约束=inline | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `重新计算距离` | 1476 | `结构体_存在观测` | ===== 轻量工具函数 ===== | `inline void 重新计算距离() { 距离 = std::sqrt(中心坐标.x * 中心坐标.x + 中心坐标.y * 中心坐标.y + 中心坐标.z * 中心坐标.z); }` | 类别=基础数据结构函数;输入=领域结构/成员所属类型;输出=void;副作用=无明显外部副作用;领域=基础数据类型;约束=inline | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `清理大缓冲` | 1479 | `结构体_存在观测` | 清理缓存、临时状态或运行资源。 | `inline void 清理大缓冲() {` | 类别=清理;输入=领域结构/成员所属类型;输出=void;副作用=修改对象状态;领域=基础数据类型;约束=inline | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `有效` | 1851 | `结构_特征使用事件` | 有效性校验：有效 | `bool 有效() const noexcept {` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `是有效治理边` | 1857 | `结构_特征使用事件` | 有效性校验：是有效治理边 | `bool 是有效治理边() const noexcept {` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `有效` | 1871 | `结构_特征暴露事件` | 有效性校验：有效 | `bool 有效() const noexcept {` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `有效` | 1885 | `结构_特征使用边` | 有效性校验：有效 | `bool 有效() const noexcept {` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `是有效治理边` | 1891 | `结构_特征使用边` | 有效性校验：是有效治理边 | `bool 是有效治理边() const noexcept {` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `有效` | 1921 | `结构_特征使用统计` | 有效性校验：有效 | `bool 有效() const noexcept {` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `存在有效治理边` | 1927 | `结构_特征使用统计` | 有效性校验：存在有效治理边 | `bool 存在有效治理边() const noexcept {` | 类别=有效性校验;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
| `特征生命周期仍在试用暴露期` | 1934 | `模块顶层` | 基础数据结构函数：特征生命周期仍在试用暴露期 | `export inline bool 特征生命周期仍在试用暴露期(枚举_特征生命周期 生命周期) noexcept {` | 类别=基础数据结构函数;输入=领域结构/成员所属类型;输出=bool;副作用=无明显外部副作用;领域=基础数据类型;约束=inline+noexcept | 专用域：领域结构/成员函数，不进入全局共享函数类.ixx |
