# 20260628 D455观察中间层近组队列 Codex断点清单

## 0. 恢复摘要

当前切片：外设观察报告队列作为运行期中间层，只向扫描 / 观察 / 等待项匹配暴露最新同帧同外设报告组。

依据文件：

```text
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
docs/工程图谱/02_外设观察事实图谱.md
docs/工程图谱/05_规则原子索引.md
```

触碰文件：

```text
外设观察报告队列.ixx
实施记录/20260628_D455观察中间层近组队列_Codex断点清单.md
```

已完成判断：

```text
1. 中间层没有 SQL / ADO / INSERT / UPDATE / DELETE 写入入口；本切片不新增数据库写入。
2. 读取最新报告、读取报告队列视图、按等待项匹配报告，已改为先锚定最新同帧同外设报告组。
3. 扫描通过 `读取最新外设观察报告` 取扫描报告；观察正式读取通过 `读取外设观察报告队列视图` 取逐簇报告；等待项消费通过 `按等待项匹配外设观察报告`。调用方随队列层收口，不再各自扫全历史队列。
4. 报告读写、按 ID 回查、显式清理和队列状态读取路径会同步清理过期 D455 短期材料页。
```

已运行命令：

```text
git diff --check -- 外设观察报告队列.ixx
退出码 0。

rg -n "ADO|SQL|数据库|INSERT INTO|UPDATE \[|DELETE FROM|执行ADO" 外设观察报告队列.ixx
仅命中文件头“不得入库”说明，无数据库写入代码。
```

待运行命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验证结果：

```text
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
退出码 0；日志 `日志/20260628_D455观察中间层近组队列_build.log` 命中“已成功生成”。
```

禁止宣称：

```text
不宣称观察候选已入场景存在；
不宣称 6150 事实提交闭合；
不把中间层队列非空等同于世界树事实已提交；
不把旧帧回退当作“补齐队列”。
```
