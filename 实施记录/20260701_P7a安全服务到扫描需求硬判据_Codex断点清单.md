# 20260701 P7a 安全 / 服务到扫描需求硬判据 Codex 断点清单

## 0. 恢复摘要

当前切片：R24 / P7a，用户已选择 A「最小结构闭环法」。

目标：安全或服务治理链必须能通过因果信息闭环生成正式扫描需求；硬判据同时要求：

1. 存在安全或服务到扫描的可回查来源因果链。
2. 运行期需求树出现目标特征为 `已识别区域扫描入账状态` 的正式扫描需求节点。

禁止宣称：不宣称内部治理闭环完成，不宣称自我苏醒 / 初步成熟完成，不用外设扫描报告、等待项、日志、控制面板显示或扫描方法动作动态替代正式扫描需求节点。

## 1. 当前依据

- `计划/计划索引.md`：R24 当前停点为 P7a，确认方法后先执行安全 / 服务到扫描因果链和运行期正式扫描需求节点最小闭合切片。
- `计划/20260630_需求树简化生成与内部治理闭环计划_v0.1.md`：P7a 要求扫描需求通过正式需求提交入口入树，目标特征使用结构化扫描目标，并能回查安全或服务来源因果。
- `规范/0050_项目通用结构与禁止性规则总纲_20260701.md`：不得用日志、显示摘要、自由文本或新持久业务结构体承载系统逻辑。
- `规范/5110_子规范_需求树生长机制_20260701.md`：来源证据不得写入 `需求主信息类` 新字段；正式获取途径必须在生成更新指令前确定目标特征类型。
- `规范/5140_子规范_需求临时因果视图展开_20260701.md`：派生需求消息可携带来源因果，自我线程必须校验并经统一需求树更新入口入树。

## 2. 已确认代码锚点

- `自我线程模块.impl.cpp::私有_确保当前场景维护扫描需求_线程侧`：当前可生成正式扫描需求节点，但现状是服务根直接维护路径，缺显式来源因果。
- `自我线程模块.impl.cpp::私有_确保派生需求节点`：已校验 `结构_任务管理派生需求消息` 的来源因果 / 来源动态，并通过 `需求类::构造新增原子需求指令` 入树。
- `任务模块.工作线程消息协议.ixx::结构_任务管理派生需求消息`：已有 `来源因果指针 / 来源因果主键 / 来源动态指针 / 来源动态主键` 字段。
- `任务模块.筹办.ixx::构造正式派生需求消息草案_筹办查询`：任务筹办侧已有来源因果到正式派生需求消息的构造模式。

## 3. 执行预测

最小实现应优先：

1. 在自我线程扫描维护入口中，先基于结构化扫描目标状态比较查询世界树来源因果。
2. 命中来源因果时，构造携带来源因果的正式派生需求消息，并调用自我线程既有派生需求入树路径。
3. 未命中来源因果时，保留现有直接维护需求路径作为运行期扫描需求节点兜底，但本切片不得判定 P7a 硬判据通过。

## 4. 本轮执行结果

已完成：

- `自我线程模块.impl.cpp` 新增 `私有_尝试按来源因果提交当前场景扫描派生需求_线程侧`，并接入 `私有_确保当前场景维护扫描需求_线程侧`。
- 命中来源因果时，使用既有 `结构_任务管理派生需求消息` 携带来源因果、来源动态、父需求、目标宿主、目标特征、当前状态、目标状态和满足关系。
- 正式入树路径仍走 `同步任务管理派生需求消息语义槽` -> `私有_收到派生需求消息时完善需求树`，未新增 `需求主信息类` 字段，未用日志文案承载机器事实。
- 构建阻塞来自 `控制面板类.cpp` 调用未导出的 WebView2 自我场景截图函数；已在 `控制面板WebView2.ixx / .cpp` 补齐真实 PNG 截图入口，使既有验证入口恢复可编译。

## 5. 验证证据

命令：

```powershell
git diff --check
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
python .\tools\生成函数功能说明文档.py
.\x64\Debug\鱼巢.exe --self-check-log --demand-tree-growth-summary
```

结果：

- `git diff --check`：通过；仅提示 `说明书/函数功能说明文档.md` 后续由 Git 规范化换行。
- Debug x64 构建：退出码 0，0 警告，0 错误。
- 函数说明文档：生成成功，函数条目 7520。
- 真实运行：退出码 0，日志文件 `日志/鱼巢_run_20260701.低值g`。

关键运行证据：

```text
日志/鱼巢_run_20260701.低值g:269
任务管理界面线程/派生需求来源任务承接 | 任务=2 | 来源需求=2 | 承接类型=入树成功非阻塞重筹办 | 原消息键=P7a|服务到扫描|2|28P|ez|F0 | 已承接=1

日志/鱼巢_run_20260701.低值g:270
任务管理界面线程/派生需求入树回执 | 任务=2 | 父需求=2 | 派生需求=6 | 处理结果=已新增 | 来源任务已承接=1

日志/鱼巢_run_20260701.低值g:271
提交_生成缺口子需求 | 成功=是 | 来源因果=28P | 来源动态=28K | 父需求=2 | 新需求=6 | 目标特征=已识别区域扫描入账状态

日志/鱼巢_run_20260701.低值g:272
自我线程/当前场景扫描来源因果派生需求 | 成功=1 | 来源因果=28P | 来源动态=28K | 目标特征=已识别区域扫描入账状态 | 正式派生需求入树=1 | P7a来源因果链证据=1
```

## 6. P7a 结算

状态：子需求已满足，根需求未完成。

满足依据：

1. 因果链：运行期扫描派生需求消息携带来源因果 `28P` 和来源动态 `28K`，消息键为 `P7a|服务到扫描|2|28P|ez|F0`。
2. 运行期需求树扫描需求：派生需求入树回执显示 `派生需求=6`、`处理结果=已新增`，目标特征为 `已识别区域扫描入账状态`。

仍禁止宣称：

- 不宣称 R24 根需求完成。
- 不宣称需求树简化闭环、内部治理闭环、自我苏醒或初步成熟完成。
- 不把日志本身当作机器事实；日志只用于读取本次运行中结构化消息和需求树入树结果。

下一步：进入 P8，检查任务完成后结算、同源变化去重、服务 / 安全账务变化回到下一轮根状态判断的证据链。
