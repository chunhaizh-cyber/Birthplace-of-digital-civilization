# 20260627 P13b 路径UTF8显示工具迁移 Codex断点清单

## 0. 恢复摘要

当前切片目标：

```text
P13b：从剩余 B 类低风险候选中处理一个小函数族。
对象：
    控制面板WebView2.cpp::私有_路径UTF8(const std::filesystem::path&)
    双目相机本能适配器.cpp::路径UTF8(const std::filesystem::path&)
```

依据：

```text
计划/计划索引.md：P13b 继续选择低风险 B 类小函数族。
计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md：B 类纯工具函数迁入 全局共享函数类.ixx。
全局共享函数类.ixx 文件头规则：无领域自定义主信息 / 领域类型参与的通用函数可进入本模块。
```

Graph RAG 与源码复核：

```text
search_graph 精确定位：
    D.控制面板WebView2.私有_路径UTF8
    D.双目相机本能适配器.路径UTF8

get_code_snippet：
    两个函数体等价，均为 path.u8string() -> std::string。

trace_path inbound：
    控制面板WebView2.cpp 内部调用；
    双目相机本能适配器.cpp 内部调用。

rg 源码复核：
    两个本地函数仅用于路径文本显示 / 诊断错误消息；
    不读取 / 写入项目结构，不承载业务判断。
```

预测结果：

```text
迁移后：
    全局共享函数类.ixx 新增 路径UTF8文本(const std::filesystem::path&)。
    删除两个本地路径 UTF8 helper。
    两个调用方改用全局 路径UTF8文本。

验证：
    函数说明文档刷新成功。
    Debug x64 构建成功。
    源码与函数说明文档不再出现两个本地路径 UTF8 helper 定义。
```

禁止宣称：

```text
不宣称 P13 完成。
不宣称函数去重全部完成。
不宣称自我苏醒完成或初步成熟完成。
```

## 1. 执行记录

已执行：

```text
1. 在 `全局共享函数类.ixx` 新增 `路径UTF8文本(const std::filesystem::path&)`。
2. 删除 `控制面板WebView2.cpp::私有_路径UTF8`。
3. 删除 `双目相机本能适配器.cpp::路径UTF8`。
4. 两个调用方的路径诊断文本改为复用全局 `路径UTF8文本`。
5. `控制面板WebView2.cpp` 补入最小文件头部规则注释模块。
```

验证证据：

```text
python .\tools\生成函数功能说明文档.py
    成功；源码文件 127 个；函数条目 7213。

rg -n "私有_路径UTF8|std::string 路径UTF8\(|路径UTF8文本" 说明书/函数功能说明文档.md 全局共享函数类.ixx 控制面板WebView2.cpp 双目相机本能适配器.cpp
    源码中只剩全局 `路径UTF8文本` 定义和两个调用方调用点。
    函数说明文档只剩 `全局共享函数类.ixx:248` 的 `路径UTF8文本` 定义。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo
    已成功生成；0 警告；0 错误。

git diff --check -- 全局共享函数类.ixx 控制面板WebView2.cpp 双目相机本能适配器.cpp 说明书/函数功能说明文档.md 实施记录/20260627_P13b_路径UTF8显示工具迁移_Codex断点清单.md
    未发现空白错误；仅有既有 CRLF/LF 提示。
```

当前判断：

```text
本切片代码接入、函数说明刷新、构建验证和复扫均已闭合。
```
