# 20260618 项目结构体审计 Codex断点清单

## 0. 恢复摘要

当前主动计划：`计划/20260618_项目结构体审计与清理计划_v0.1.md`

当前切片：

```text
P3/P4 结构体审计连续清理；
已闭合到 P4n：删除 `结构_因果路径派生消息草案统计` 内部日志计数视图；
已闭合到 P4o：删除 `结构_因果路径查询壳统计` 内部查询统计返回视图。
已闭合到 P4p：删除 `结构_直接子需求聚合视图_筹办` 临时聚合视图。
已闭合到 P4q：删除 `鱼巢.cpp::结构_任务节点统计` 内部日志计数视图。
已闭合到 P4r：删除 `线程生命周期消息模块.ixx::结构_消息中间件维护统计` 内部维护计数视图。
已闭合到 P4s：删除 `任务模块.管理界面线程.impl.cpp::结构_任务调度消费统计` 局部统计 / 未读聚合视图。
已闭合到 P4t：删除 `自我线程模块.消息处理器.ixx::结构_需求列表刷新决策` 未用返回视图。
已闭合到 P4u：删除 `自我线程模块.消息协议.ixx::结构_治理消息轨迹` 单字段包装视图。
已闭合到 P4v：删除 `自我线程模块.消息协议.ixx::结构_结果回流提示段` 未用协议布尔段。
已闭合到 P4w：删除 `二次特征应用模块.ixx::结构_匹配结果` 和
    `二次特征匹配结果_桥接` 未引用匹配结果统计视图。
已闭合到 P4x：删除 `任务模块.治理协议.ixx::结构_治理实例堆快照`
    未引用治理统计视图。
已闭合到 P4y：删除 `方法类.cpp::结构_二次特征比较语义`
    内部临时返回视图，改为 bool 返回和输出参数。
当前下一步从最新结构体清单 512 / 含文本候选 289 继续筛选。
```

已复核依据：

```text
AGENTS.md：开工必读、硬规则、项目通用数据结构硬约束、模块边界。
规范/禁止性规范20260611.md：第 2 章项目通用数据结构闭环。
规范/规范目录.md：一级规范和相关二级规范索引。
计划/计划索引.md：当前主动计划已指向结构体审计计划。
docs/codex/实现验收审计模式.md：第 1-2 章及文本替代审计口径。
规范/主信息字段与结构新增准入约束规范20260519.md：新增字段 / 结构准入顺序和缓存 / 索引 / 去重边界。
```

恢复时已确认的无关未提交改动：

```text
git -c core.quotepath=false status --short --untracked-files=no
 M 规范/自我存在服务与生存基本因果闭环规范20260601.md
 M 规范/自我根特征抽象定义与自检缺口规范20260523.md
```

当前判断：

```text
P4y 已按最小切片完成代码改动；
`结构_二次特征比较语义` 没有源码引用；
方法结果能力比较语义仍由二次特征主信息、语素入口主键和比较结果编码读取。
```

下一步：

```text
1. 提交 P4y 切片相关文件。
2. 从最新结构体清单 512 / 含文本候选 289 继续筛选。
3. 优先审计内部计数、去重、显示摘要或临时返回视图结构体。
```

## 1. P0/P1/P2 执行记录

已生成：

```text
实施记录/20260618_项目结构体审计_文件清单.tsv
    file_count=135
    audit_count=134

实施记录/20260618_项目结构体审计_结构体清单.tsv
    struct_count=540
    text_field_candidates=295
    project_field_candidates=330
```

第一批 P3 审计对象：

```text
结构_任务管理消息头
结构_任务管理未满足项
```

选择原因：

```text
二者位于 任务模块.工作线程消息协议.ixx；
字段包含 std::string 主键 / 类型；
结构_任务管理消息头 被跨模块读取并进入 自我线程模块.消息协议.ixx 的治理消息构造路径；
结构_任务管理未满足项 被 结构_任务管理结果状态消息 持有并随任务管理结果上行。
```

已确认：

```text
结构_任务管理消息头 不是只剩内部计数 / 去重视图，不能直接删除。
当前第一独立问题是：任务管理上行消息中的文本主键 / 类型 / 摘要字段是否承担机器判断语义。
```

下一步验证：

```text
继续追查 未满足项类型文本 / 未满足项类型 的写入、读取、等价比较和是否进入特征变化项或治理消息去重。
```

## 2. P3/P4 第一批处理记录

第一批判定文件：

```text
实施记录/20260618_项目结构体审计_引用函数清单.tsv
实施记录/20260618_项目结构体审计_判定表.md
```

处理对象：

```text
结构_任务管理未满足项
结构_任务管理结果状态消息::未满足项类型文本
结构_任务管理结果状态消息::未满足项集
```

判定：

```text
结构_任务管理未满足项 写入后没有外部业务读取；
未满足项集 只在 任务管理结果消息语义相同 中比较 size；
未满足项类型文本 只由 私有_任务推进状况文本 生成，并只参与等价比较；
真实驱动字段是 是否仍有未满足项 / 是否需要父任务处理 / 是否需要自我线程重判 / 结果状态 枚举。
```

已改文件：

```text
任务模块.工作线程消息协议.ixx
任务模块.管理工作线程.ixx
自我线程模块.消息协议.ixx
实施记录/20260618_项目结构体审计_文件清单.tsv
实施记录/20260618_项目结构体审计_结构体清单.tsv
实施记录/20260618_项目结构体审计_引用函数清单.tsv
实施记录/20260618_项目结构体审计_判定表.md
实施记录/20260618_项目结构体审计_Codex断点清单.md
```

验证：

```text
rg 结构_任务管理未满足项|未满足项集|未满足项类型文本
    结果：源码引用 0。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    结果：首次并行构建 exit_code=1；
    错误：C3474 无法打开输出文件 鱼巢/x64/Debug/自我线程模块.ixx.ifc。
    判定：模块 IFC 输出文件占用 / 并行构建收尾问题，不是代码编译错误。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    日志：日志/20260618_结构体审计_未满足项删除_msbuild_serial.log
    结果：exit_code=0，0 警告，0 错误。
```

结构体清单刷新：

```text
实施记录/20260618_项目结构体审计_结构体清单.tsv
    struct_count=538
    text_field_candidates=293
    project_field_candidates=329
    deleted_struct_refs=0
```

仍需后续审计：

```text
结构_任务管理消息头 仍含文本主键，但跨模块读取并进入治理消息头数值主键，不能直接删除；
结构_任务管理状态变化项 / 结构_任务管理关键中间状态变化项 仍含文本主体类型、特征类型、动作语义键等字段，
需要下一批按项目结构映射继续审计。
```

## 3. 第二批审计堵点

第二批对象：

```text
结构_任务管理状态变化项
结构_任务管理关键中间状态变化项
```

已复核证据：

```text
rg "服务对象状态变化集\\.(push_back|emplace_back|insert)|关键中间状态变化集\\.(push_back|emplace_back|insert)|结构_任务管理状态变化项\\s+\\w+|结构_任务管理关键中间状态变化项\\s+\\w+"
    结果：无写入命中。

rg "\\.服务对象状态变化集\\s*=|\\.关键中间状态变化集\\s*=|服务对象状态变化集\\s*\\{|关键中间状态变化集\\s*\\{"
    结果：只命中结构字段、治理消息附加载荷字段和新增集合局部变量。

自我线程模块.impl.cpp:1130 附近：
    私有_评估服务价值结算证据_线程侧 读取 服务对象状态变化集，
    用集合数量、变化前后值和 变化原因键 判定服务结果证据。

自我线程模块.impl.cpp:13611 / 13654 附近：
    私有_落任务管理状态变化集到自我内部世界 和
    私有_落任务管理关键中间状态变化集到自我内部世界 读取这些结构。
    但 私有_推断治理存在类型 / 私有_推断治理特征类型 当前忽略文本输入并返回默认类型。
```

当前判断：

```text
这两个结构体不是“只剩内部计数 / 去重视图”：
    它们被服务价值结算证据、关键中间状态恢复判断和自我内部世界落账路径读取。

但它们当前也不是合格闭环：
    没有找到任务管理侧真实写入方；
    主体类型 / 特征类型 / 动作语义等关键字段仍为文本；
    文本推断函数当前不建立可靠项目结构映射。
```

堵点：

```text
不能直接删除：
    删除会弱化服务对象外在特征状态变化证据门槛和关键中间状态恢复判断。

不能直接宣称保留合格：
    没有写入方和项目结构映射，属于孤立证据入口 / 部分实现。

恢复条件：
    需要先形成独立设计切片：
    任务结果服务对象状态变化 / 关键中间状态变化
        -> 写入方
        -> 项目结构字段
        -> 自我内部世界落账
        -> 服务结算证据 / 恢复判断
        -> 验证路径。
```

禁止宣称：

```text
不宣称结构体清理完成。
不宣称项目结构闭环全部通过。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
未给出六类证据前，不写已实现 / 已完成 / 已支持 / 已接入 / 已闭环 / 可用 / 已修复 / 已验证。
```

## 4. P4b 状态 / 动态证据迁移执行前断点

当前用户口径：

```text
证据只能由状态、动态两类信息承担；
服务对象状态变化集 / 关键中间状态变化集 只能作为过渡视图，不能作为长期证据结构。
```

本切片目标：

```text
1. 将服务价值结算证据从 `服务对象状态变化集` 迁移到
   `结构_叶子任务正式结算证据` 中的 before / actual 状态、D0 业务动作动态、D1 任务状态动态。
2. 将 G0 硬门槛所需的服务前后状态可比较 / 目标特征改善结果同步到任务虚拟存在特征。
3. 删除 `结构_任务管理状态变化项`、`结构_任务管理关键中间状态变化项`
   及结果消息中的对应 vector 字段。
```

当前工作区保护：

```text
已存在用户侧修改：
    规范/自我存在服务与生存基本因果闭环规范20260601.md
    规范/自我根特征抽象定义与自检缺口规范20260523.md

本切片不修改上述两份规范。
```

计划验证：

```text
1. rg 确认源码不再引用两个结构体和两个结果消息 vector 字段。
2. 运行 tools/生成函数功能说明文档.py 更新函数说明文档。
3. 串行 Debug x64 构建验证。
```

## 5. P4b 执行结果

已改代码路径：

```text
任务模块.工作线程消息协议.ixx
    删除 `结构_任务管理状态变化项`。
    删除 `结构_任务管理关键中间状态变化项`。
    删除 `结构_任务管理结果状态消息::服务对象状态变化集`。
    删除 `结构_任务管理结果状态消息::关键中间状态变化集`。

自我线程模块.消息协议.ixx
    删除执行尝试汇总中的两个变化集缓存。
    删除两个变化集归并键和归并函数。
    结果消息重复判定不再比较变化集 size。

自我线程模块.impl.cpp
    新增正式结算状态比较、状态 / 动态证据同步、关键中间状态结果推导函数。
    服务价值结算证据改为读取 `结构_叶子任务正式结算证据`：
        before状态 / actual状态；
        D0_业务动作动态；
        任务完成动作动态。
    删除任务管理状态变化集 / 关键中间状态变化集落内部世界路径。

自我线程模块.消息批次执行器.ixx
    删除批次摘要中的两个变化集计数。

说明书/函数功能说明文档.md
    已由脚本刷新。
```

关键口径：

```text
服务对象状态改善证据由 before / actual 状态比较给出；
服务结果可验证由 actual 状态证据给出；
服务结果可归因由 D0 业务动作动态给出；
G0 硬门槛继续读取任务虚拟存在上的服务前后状态可比较 / 目标特征改善特征，
这些特征由正式结算证据同步写入，不再由消息视图结构承担。
```

验证：

```text
rg "结构_任务管理状态变化项|结构_任务管理关键中间状态变化项|服务对象状态变化集|关键中间状态变化集" -S -g '*.cpp' -g '*.h' -g '*.ixx'
    结果：源码引用 0。

python .\tools\生成函数功能说明文档.py
    结果：exit_code=0；函数条目=7304。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    日志：日志/20260618_结构体审计_重复目标诊断删除_msbuild_serial.log
    结果：exit_code=0；0 错误。
```

仍未宣称：

```text
不宣称结构体清理全部完成；
不宣称项目结构闭环全部通过；
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 55. P4y 二次特征比较语义临时返回视图删除执行前断点

当前切片：

```text
删除 `方法类.cpp::结构_二次特征比较语义`。
```

判定依据：

```text
`结构_二次特征比较语义` 只在 `方法类.cpp` 内部用于
`私有_二次特征比较语义` 返回特征类型主键和比较结果，
随后仅由 `私有_二次特征比较语义相同` 立即读取。

该结构体不写入存在 / 场景 / 特征 / 特征值 / 方法 / 需求；
不承担方法能力、状态证据或动作动态证据；
可分解为 `私有_二次特征比较语义` 的 bool 返回值和两个输出参数。

已补读方法规范和任务筹办方法查找规范：
本切片不改候选方法填充 / 选择规则，不改方法结果能力判断语义，
只删除内部临时返回视图。
```

项目结构映射：

```text
比较依据 -> 二次特征主信息 / 语素入口主键 / 比较结果编码 -> 局部变量读取；
方法能力匹配 -> 方法结果能力现有比较函数 -> 原调用路径继续读取；
删除结构体后不新增文本业务字段，不把日志作为证据。
```

最小实现：

```text
1. 删除 `结构_二次特征比较语义`。
2. 将 `私有_二次特征比较语义` 改为 bool 返回并通过引用输出
   `std::string` 特征类型主键和 `std::int32_t` 比较结果。
3. `私有_二次特征比较语义相同` 改为读取局部变量比较。
4. 结构体清单删除该记录，结构体总数预计从 513 降到 512；
   含文本字段候选预计从 290 降到 289。
```

不做范围：

```text
不改二次特征比较结果编码规则。
不改方法首节点结果能力判断规则。
不改候选方法填充、排序或选择。
```

预测验证：

```text
1. 源码中 `结构_二次特征比较语义` 引用为 0。
2. 函数说明文档刷新后 `私有_二次特征比较语义` 仍存在。
3. 结构体清单为 512 / 含文本候选 289。
4. Debug x64 串行构建通过。
```

## 56. P4y 执行结果

已改代码路径：

```text
方法类.cpp
    删除 `结构_二次特征比较语义`。
    `私有_二次特征比较语义` 改为 bool 返回和输出参数。
    `私有_二次特征比较语义相同` 改为读取局部变量后比较。

说明书/函数功能说明文档.md
    已由脚本刷新。
```

清单变化：

```text
结构体清单：512。
含文本字段结构体候选：289。
含项目结构字段结构体候选：307。
```

验证：

```text
rg "结构_二次特征比较语义" -n -g "*.cpp" -g "*.h" -g "*.ixx"
    结果：源码引用 0。

Select-String -Path '实施记录/20260618_项目结构体审计_结构体清单.tsv' -Pattern '结构_二次特征比较语义'
    结果：清单引用 0。

python .\tools\生成函数功能说明文档.py
    结果：exit_code=0；函数条目=7302。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    结果：exit_code=0；日志 `日志/20260618_结构体审计_二次特征比较语义视图删除_msbuild_serial.log` 记录已成功生成。
```

仍未宣称：

```text
不宣称结构体清理全部完成；
不宣称项目结构闭环全部通过；
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 53. P4x 治理实例堆快照未引用视图删除执行前断点

当前切片：

```text
删除 `任务模块.治理协议.ixx::结构_治理实例堆快照`。
```

判定依据：

```text
`rg "结构_治理实例堆快照" -n -g "*.cpp" -g "*.h" -g "*.ixx"`
    仅命中结构体定义。

`rg "治理实例堆快照" -n -g "*.cpp" -g "*.h" -g "*.ixx"`
    仅命中结构体定义。

`说明书/函数功能说明文档.md`
    未命中 `结构_治理实例堆快照` 或 `治理实例堆快照`。

该结构体只保存总数、最近摘要和 `结构_任务管理结果` 列表；
没有协议字段嵌入、函数参数/返回值、容器持有或外部读取路径；
不写入存在 / 场景 / 特征 / 特征值 / 方法 / 需求；
不承担状态证据或动作动态证据。
```

最小实现：

```text
1. 删除未引用结构体定义。
2. 结构体清单删除该记录，结构体总数预计从 514 降到 513；
   含文本字段候选预计从 291 降到 290。
3. 同步修正 `任务模块.治理协议.ixx` 后续结构体行号。
```

不做范围：

```text
不改 `结构_任务管理结果`。
不改治理恢复事件镜像、治理恢复快照和任务治理协议枚举。
不新增日志字段或文本替代证据。
```

预测验证：

```text
1. 源码中 `结构_治理实例堆快照` 引用为 0。
2. 结构体清单为 513 / 含文本候选 290。
3. Debug x64 串行构建通过。
```

## 54. P4x 执行结果

已改代码路径：

```text
任务模块.治理协议.ixx
    删除 `结构_治理实例堆快照`。
    保留 `结构_任务管理结果`、`结构_治理恢复事件镜像` 和 `结构_治理恢复快照`。

说明书/函数功能说明文档.md
    已由脚本刷新。
```

清单变化：

```text
结构体清单：513。
含文本字段结构体候选：290。
含项目结构字段结构体候选：307。
```

验证：

```text
rg "结构_治理实例堆快照|治理实例堆快照" -n -g "*.cpp" -g "*.h" -g "*.ixx"
    结果：源码引用 0。

Select-String -Path '实施记录/20260618_项目结构体审计_结构体清单.tsv' -Pattern '结构_治理实例堆快照'
    结果：清单引用 0。

python .\tools\生成函数功能说明文档.py
    结果：exit_code=0；函数条目=7303。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    结果：exit_code=0；日志 `日志/20260618_结构体审计_治理实例堆快照删除_msbuild_serial.log` 记录已成功生成。
```

仍未宣称：

```text
不宣称结构体清理全部完成；
不宣称项目结构闭环全部通过；
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 51. P4w 二次特征匹配结果统计视图删除执行前断点

当前切片：

```text
删除 `二次特征应用模块.ixx::结构_匹配结果` 和
`二次特征应用模块.ixx::二次特征匹配结果_桥接`。
```

判定依据：

```text
`rg "结构_匹配结果|二次特征匹配结果_桥接" -n -g "*.cpp" -g "*.h" -g "*.ixx"`
    仅命中两处结构体定义。

`说明书/函数功能说明文档.md`
    未命中两个结构体名称。

两个结构体只保存命中数、摘要数、目标数、精确率、召回率、得分等统计值；
不写入存在 / 场景 / 特征 / 特征值 / 方法 / 需求；
不承载二次特征本体、状态证据或动态证据；
也没有外部读取路径。
```

最小实现：

```text
1. 删除两个未引用结构体定义。
2. 结构体清单删除两个记录，结构体总数预计从 516 降到 514；
   含文本字段候选预计从 293 降到 291。
3. 不修改 `二次特征_满足` 和桥接满足函数。
```

不做范围：

```text
不改二次特征主信息、匹配逻辑、桥接配置和本能判断函数。
不把统计值改写为新的文本字段或日志证据。
```

预测验证：

```text
1. 源码中两个结构体名称引用为 0。
2. 结构体清单为 514 / 含文本候选 291。
3. Debug x64 串行构建通过。
```

## 52. P4w 执行结果

已改代码路径：

```text
二次特征应用模块.ixx
    删除 `结构_匹配结果`。
    删除 `二次特征匹配结果_桥接`。
    保留 `结构_匹配配置`、`二次特征匹配配置_桥接`、
        `二次特征_满足` 和 `二次特征满足_桥接`。

说明书/函数功能说明文档.md
    已由脚本刷新。
```

清单变化：

```text
结构体清单：514。
含文本字段结构体候选：291。
含项目结构字段结构体候选：307。
```

验证：

```text
rg "结构_匹配结果|二次特征匹配结果_桥接" -n -g "*.cpp" -g "*.h" -g "*.ixx"
    结果：源码引用 0。

Select-String -Path '实施记录/20260618_项目结构体审计_结构体清单.tsv' -Pattern '结构_匹配结果|二次特征匹配结果_桥接'
    结果：清单引用 0。

python .\tools\生成函数功能说明文档.py
    结果：exit_code=0；函数条目=7303。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    结果：exit_code=0；日志 `日志/20260618_结构体审计_二次特征匹配结果视图删除_msbuild_serial.log` 记录已成功生成。
```

仍未宣称：

```text
不宣称结构体清理全部完成；
不宣称项目结构闭环全部通过；
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 18. 当前恢复点

当前状态：

```text
P4h 已闭合并提交。
最新结构体清单：530。
最新含文本字段结构体候选：296。
```

下一步：

```text
从最新结构体清单继续筛选下一批候选；
优先处理只承担内部计数、去重、显示摘要或临时返回视图的结构体；
进入代码修改前先写新的 P4 断点。
```

仍需避开：

```text
规范/自我存在服务与生存基本因果闭环规范20260601.md
规范/自我根特征抽象定义与自检缺口规范20260523.md
```

禁止宣称：

```text
不宣称结构体清理全部完成；
不宣称项目结构闭环全部通过；
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 19. P4i 控制面板重复目标诊断视图删除执行前断点

当前切片：

```text
删除 `控制面板类.cpp::结构_重复目标诊断`。
```

判定依据：

```text
该结构体只在控制面板需求树统计块内作为局部诊断视图：
    `数量` 统计同父需求 / 同目标宿主 / 同目标特征 / 同方向的活动需求数量；
    `样本需求主键` 只用于运行日志中的样本展示。

它不写入项目结构，不承载状态或动态证据，也没有外部读取。
```

最小实现：

```text
1. 删除局部 `结构_重复目标诊断`。
2. 用 `std::unordered_map<std::string, std::size_t>` 保存重复目标数量。
3. 用 `std::unordered_map<std::string, std::vector<std::string>>` 保存最多 5 个样本需求主键。
4. 保持原有快照计数和运行日志输出口径。
```

预测验证：

```text
1. 源码中 `结构_重复目标诊断` 引用为 0。
2. 结构体清单删除该结构体，结构体总数从 530 降到 529；含文本字段候选从 296 降到 295。
3. Debug x64 串行构建应通过。
```

## 20. P4i 执行结果

已改代码路径：

```text
控制面板类.cpp
    删除局部 `结构_重复目标诊断`。
    `活动目标计数` 改为 `std::unordered_map<std::string, std::size_t>`。
    新增局部 `活动目标样本` map 保存最多 5 个样本需求主键。
    重复目标组数、需求数和运行日志样本输出保持原口径。

说明书/函数功能说明文档.md
    已由脚本刷新。
```

审计产物更新：

```text
实施记录/20260618_项目结构体审计_结构体清单.tsv
    删除 `结构_重复目标诊断` 行。
    结构体清单：529。
    含文本字段结构体候选：295。

实施记录/20260618_项目结构体审计_判定表.md
    增加 `结构_重复目标诊断` 判定行。

计划/计划索引.md
    当前下一步计数更新为 529 / 295。
```

验证：

```text
rg "结构_重复目标诊断" -S -g "!x64/**" -g "!Debug/**" -g "!日志/**" -g "!logs/**"
    结果：源码引用 0；剩余命中仅为审计断点和判定记录。

python .\tools\生成函数功能说明文档.py
    结果：exit_code=0；函数条目=7306。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    结果：exit_code=0；0 警告，0 错误。
```

仍未宣称：

```text
不宣称结构体清理全部完成；
不宣称项目结构闭环全部通过；
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 21. P4j 二次特征索引追加阶段耗时视图删除执行前断点

当前切片：

```text
删除 `二次特征类.cpp::结构_二次特征索引追加阶段耗时`。
```

判定依据：

```text
该结构体只在 `私有_追加二次特征索引` 和 `二次特征类::绑定场景` 内部作为慢路径日志的耗时包：
    6 个 std::uint64_t 阶段耗时；
    2 个 bool 查重结果。

真实项目结构写入仍由 `场景节点主信息类::关系索引` 和 `场景节点主信息类::二次特征索引` 承担。
该结构体不写入存在 / 场景 / 特征 / 状态 / 动态 / 方法 / 需求，
没有外部读取，也不承担状态或动态证据。
```

最小实现：

```text
1. 删除 `结构_二次特征索引追加阶段耗时`。
2. `私有_追加二次特征索引` 改为显式输出各阶段耗时和查重结果引用。
3. `二次特征类::绑定场景` 用局部变量接收这些输出并维持原慢日志字段。
4. 保持关系索引 / 二次特征索引写入逻辑不变。
```

预测验证：

```text
1. 源码中 `结构_二次特征索引追加阶段耗时` 引用为 0。
2. 结构体清单删除该结构体，结构体总数从 529 降到 528；含文本字段候选保持 295。
3. Debug x64 串行构建应通过。
```

## 22. P4j 执行结果

已改代码路径：

```text
二次特征类.cpp
    删除 `结构_二次特征索引追加阶段耗时`。
    `私有_追加二次特征索引` 改为显式输出 6 个阶段耗时和 2 个查重结果。
    `二次特征类::绑定场景` 改用局部变量接收这些输出。
    关系索引 / 二次特征索引写入逻辑保持不变。

说明书/函数功能说明文档.md
    已由脚本刷新。
```

审计产物更新：

```text
实施记录/20260618_项目结构体审计_结构体清单.tsv
    删除 `结构_二次特征索引追加阶段耗时` 行。
    结构体清单：528。
    含文本字段结构体候选：295。

实施记录/20260618_项目结构体审计_判定表.md
    增加 `结构_二次特征索引追加阶段耗时` 判定行。

计划/计划索引.md
    当前下一步计数更新为 528 / 295。
```

验证：

```text
rg "结构_二次特征索引追加阶段耗时|索引阶段\\." 二次特征类.cpp -n
    结果：源码引用 0。

python .\tools\生成函数功能说明文档.py
    结果：exit_code=0；函数条目=7307。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    日志：日志/20260618_结构体审计_二次特征索引耗时视图删除_msbuild_serial.log
    结果：exit_code=0；0 错误。
```

仍未宣称：

```text
不宣称结构体清理全部完成；
不宣称项目结构闭环全部通过；
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 23. P4k 控制面板线程状态页统计视图删除执行前断点

当前切片：

```text
删除 `控制面板类.cpp::结构_线程状态页统计`。
```

判定依据：

```text
该结构体只在控制面板线程状态页统计链路内部保存 5 个 std::size_t：
    已知逻辑线程数；
    当前运行数；
    未启动数；
    已结束数；
    故障数。

`规范/线程生命周期状态上报与控制面板线程信息规范20260609.md` 明确：
    线程状态信息只服务线程可见性、健康监控、控制面板展示和诊断排错；
    不作为任务状态、需求树、世界事实、方法动作动态或安全 / 服务结算依据。

真实线程状态来源仍是控制面板线程信息表、消息中间件和快照字段。
该统计结构体没有外部读取，不写入项目结构，只是显示聚合视图。
```

最小实现：

```text
1. 删除 `结构_线程状态页统计`。
2. 计入函数改为显式引用输出 5 个局部计数。
3. 计算函数改为向局部计数输出，不返回结构体。
4. 统计文本函数改为接收 5 个计数。
5. 线程树标题、详情统计文本和 HTML 徽标数量保持原口径。
```

预测验证：

```text
1. 源码中 `结构_线程状态页统计` 和 `统计.` 相关旧读法在该链路中消失。
2. 结构体清单删除该结构体，结构体总数从 528 降到 527；含文本字段候选保持 295。
3. 函数说明文档刷新后函数签名位置更新。
4. Debug x64 串行构建应通过。
```

## 24. P4k 执行结果

已改代码路径：

```text
控制面板类.cpp
    删除 `结构_线程状态页统计`。
    `私有_计入线程生命周期统计` / `私有_计入摘要线程生命周期统计`
        / `私有_计入布尔线程统计` / `私有_计入线程信息项统计`
        改为显式引用输出局部计数。
    `私有_计算线程状态页统计` / `私有_计算线程信息表统计`
        改为向 5 个局部计数输出，不再返回结构体。
    `私有_线程状态统计文本` 改为接收 5 个计数。
    线程树标题、统计详情文本、HTML 徽标数量口径保持不变。

说明书/函数功能说明文档.md
    已由脚本刷新。
```

审计产物更新：

```text
实施记录/20260618_项目结构体审计_结构体清单.tsv
    删除 `结构_线程状态页统计` 行。
    同步 `控制面板类.cpp` 后续结构体行号：
        `结构_SQL控制面板数据` -> 6903；
        `结构_SQL控制面板查询` -> 6922；
        `结构_需求列表分页结果` -> 8599。
    结构体清单：527。
    含文本字段结构体候选：295。

实施记录/20260618_项目结构体审计_判定表.md
    增加 `结构_线程状态页统计` 判定行。

计划/计划索引.md
    当前下一步计数更新为 527 / 295。
```

验证：

```text
rg "结构_线程状态页统计|线程统计\\.|统计\\.已知逻辑线程数|统计\\.当前运行数|统计\\.未启动数|统计\\.已结束数|统计\\.故障数" 控制面板类.cpp -n
    结果：源码引用 0。

python .\tools\生成函数功能说明文档.py
    结果：exit_code=0；函数条目=7307。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    日志：日志/20260618_结构体审计_线程状态页统计视图删除_msbuild_serial.log
    结果：exit_code=0；0 错误。
```

仍未宣称：

```text
不宣称结构体清理全部完成；
不宣称项目结构闭环全部通过；
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 25. P4l 控制面板需求列表分页结果视图删除执行前断点

当前切片：

```text
删除 `控制面板类.cpp::结构_需求列表分页结果`。
```

判定依据：

```text
该结构体只在控制面板需求列表分页读取链内部保存：
    std::vector<需求节点*> 节点集；
    bool 还有更多；
    std::size_t 已遍历数量。

真实需求事实仍由需求树节点承载；
分页结果只服务控制面板页面分段显示和“继续加载”节点，
不写入项目结构，不作为需求状态、任务状态、动作动态或结算证据。
```

最小实现：

```text
1. 删除 `结构_需求列表分页结果`。
2. `私有_读取需求列表分页` 改为显式输出节点集、还有更多、已遍历数量。
3. 三个调用点改为局部变量接收输出。
4. 控制面板需求列表第一页、页面刷新和“继续加载”行为保持原口径。
```

预测验证：

```text
1. 源码中 `结构_需求列表分页结果` 和 `分页结果.` 相关旧读法为 0。
2. 结构体清单删除该结构体，结构体总数从 527 降到 526；含文本字段候选保持 295。
3. 函数说明文档刷新后函数签名位置更新。
4. Debug x64 串行构建应通过。
```

## 26. P4l 执行结果

已改代码路径：

```text
控制面板类.cpp
    删除 `结构_需求列表分页结果`。
    `私有_读取需求列表分页` 改为显式输出：
        分页节点集；
        分页还有更多；
        分页已遍历数量。
    三个调用点改为局部变量接收输出：
        控制面板快照需求列表第一页；
        需求列表页面刷新；
        需求列表继续加载。

说明书/函数功能说明文档.md
    已由脚本刷新。
```

审计产物更新：

```text
实施记录/20260618_项目结构体审计_结构体清单.tsv
    删除 `结构_需求列表分页结果` 行。
    结构体清单：526。
    含文本字段结构体候选：295。

实施记录/20260618_项目结构体审计_判定表.md
    增加 `结构_需求列表分页结果` 判定行。

计划/计划索引.md
    当前下一步计数更新为 526 / 295。
```

验证：

```text
rg "结构_需求列表分页结果|分页结果\\." 控制面板类.cpp -n
    结果：源码引用 0。

python .\tools\生成函数功能说明文档.py
    结果：exit_code=0；函数条目=7307。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    日志：日志/20260618_结构体审计_需求列表分页结果视图删除_msbuild_serial.log
    结果：exit_code=0；0 错误。
```

仍未宣称：

```text
不宣称结构体清理全部完成；
不宣称项目结构闭环全部通过；
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 27. P4m 控制面板需求满足情况视图删除执行前断点

当前切片：

```text
删除 `控制面板类.cpp::结构_需求满足情况_控制面板`。
```

判定依据：

```text
该结构体只在控制面板需求详情构建链内部汇总显示：
    自身当前状态是否可比较 / 是否满足 / 比较关系；
    子需求计数；
    显示用满足情况文本。

真实需求事实仍由需求节点、当前状态、目标状态和满足关系承载。
该结构体不写入项目结构，不作为任务推进、动作动态或结算证据。

额外问题：
    递归评估子需求时用 `子评估.满足情况 == "全部满足"` / `"部分满足"` 做内部分支判断。
    即使只在控制面板展示层，也应改成离散显示态，不继续用文本作内部判断。
```

最小实现：

```text
1. 删除 `结构_需求满足情况_控制面板`。
2. 新增控制面板私有显示态枚举承接 全部满足 / 部分满足 / 未满足。
3. `私有_评估需求满足情况_控制面板` 改为返回显示态，并显式输出比较状态和计数。
4. `私有_需求满足情况说明_控制面板` 改为接收显示态和显式计数参数。
5. 控制面板需求详情仍只作为展示，不宣称需求满足事实已结算。
```

预测验证：

```text
1. 源码中 `结构_需求满足情况_控制面板` 和 `满足情况.` 相关旧读法为 0。
2. 结构体清单删除该结构体，结构体总数从 526 降到 525；含文本字段候选从 295 降到 294。
3. 函数说明文档刷新后函数签名位置更新。
4. Debug x64 串行构建应通过。
```

## 28. P4m 执行结果

已改代码路径：

```text
控制面板类.cpp
    删除 `结构_需求满足情况_控制面板`。
    新增控制面板私有显示态枚举 `枚举_需求满足显示状态_控制面板`。
    `私有_评估需求满足情况_控制面板` 改为返回显示态并显式输出：
        当前已截止；
        自身状态可比较 / 自身状态已满足 / 自身比较关系；
        直接子 / 活动阻塞子 / 全部满足子 / 部分满足子 / 未满足子 / 已截止子计数。
    递归子需求显示态判断不再比较文本 `"全部满足"` / `"部分满足"`。
    `私有_需求满足情况说明_控制面板` 改为接收显示态和局部计数。

说明书/函数功能说明文档.md
    已由脚本刷新。
```

审计产物更新：

```text
实施记录/20260618_项目结构体审计_结构体清单.tsv
    删除 `结构_需求满足情况_控制面板` 行。
    同步 `控制面板类.cpp` 后续结构体行号：
        `结构_SQL控制面板数据` -> 6993；
        `结构_SQL控制面板查询` -> 7012。
    结构体清单：525。
    含文本字段结构体候选：294。

实施记录/20260618_项目结构体审计_判定表.md
    增加 `结构_需求满足情况_控制面板` 判定行。

计划/计划索引.md
    当前下一步计数更新为 525 / 294。
```

验证：

```text
rg "结构_需求满足情况_控制面板|满足情况\\." 控制面板类.cpp -n
    结果：源码引用 0。

rg '子评估\\.满足情况|== "全部满足"|== "部分满足"' 控制面板类.cpp -n
    结果：源码引用 0。

python .\tools\生成函数功能说明文档.py
    结果：exit_code=0；函数条目=7308。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    日志：日志/20260618_结构体审计_需求满足情况视图删除_msbuild_serial.log
    结果：exit_code=0；0 错误。
```

仍未宣称：

```text
不宣称结构体清理全部完成；
不宣称项目结构闭环全部通过；
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 29. P4n 因果路径派生消息草案统计视图删除执行前断点

当前切片：

```text
删除 `任务模块.筹办.ixx::结构_因果路径派生消息草案统计`。
```

判定依据：

```text
该结构体只在 `记录派生需求消息草案统计_筹办查询` 内部累计日志统计：
    路径项数量、字段完整数量、正式消息数量、缺字段数量等。

调用方对返回值使用 `(void)` 丢弃；
正式可承接输出是 `输出正式消息集` 指向的派生需求消息集合；
真实约束仍由 `派生消息字段完整_筹办查询` 和后续提交入口裁决承担。

该结构体不写入项目结构，不作为需求树入树事实、动作动态或结算证据。
```

最小实现：

```text
1. 删除 `结构_因果路径派生消息草案统计`。
2. `记录派生需求消息草案统计_筹办查询` 改为 `void`。
3. 函数内部改用局部计数变量写同一条统计日志。
4. 保持 `输出正式消息集` 写入条件不变。
```

预测验证：

```text
1. 源码中 `结构_因果路径派生消息草案统计` 和 `统计.` 相关旧读法在该函数中消失。
2. 结构体清单删除该结构体，结构体总数从 525 降到 524；含文本字段候选保持 294。
3. 函数说明文档刷新后函数签名位置更新。
4. Debug x64 串行构建应通过。
```

## 29.1 P4n 执行结果

已改代码路径：

```text
任务模块.筹办.ixx
```

代码改动：

```text
1. 删除 `结构_因果路径派生消息草案统计`。
2. `记录派生需求消息草案统计_筹办查询` 改为 `void`。
3. 原结构体字段替换为函数内部局部计数变量。
4. `输出正式消息集` 写入条件保持不变，正式派生需求消息仍走既有消息集合和后续提交裁决。
```

验证：

```text
rg "结构_因果路径派生消息草案统计|统计\\." 任务模块.筹办.ixx -n
    结构体名无源码命中；`统计.` 命中均属于同文件其他统计结构，不属于本切片函数。

python .\tools\生成函数功能说明文档.py
    exit_code=0；函数条目=7308。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    exit_code=0；
    日志/20260618_结构体审计_因果路径派生消息草案统计视图删除_msbuild_serial.log
    精确错误模式无命中；日志行包含“已成功生成”。
```

审计产物更新：

```text
实施记录/20260618_项目结构体审计_结构体清单.tsv
    删除 `结构_因果路径派生消息草案统计` 行；
    `任务模块.筹办.ixx` 后续结构体行号已按当前源码校正；
    结构体清单：524；含文本字段候选：294。

实施记录/20260618_项目结构体审计_判定表.md
    追加 P4n 删除判定和验证证据。

计划/计划索引.md
    当前下一步计数更新为 524 / 294。
```

## 30. P4o 因果路径查询壳统计视图删除执行前断点

当前切片：

```text
删除 `任务模块.筹办.ixx::结构_因果路径查询壳统计`。
```

判定依据：

```text
该结构体字段分两类：
    1. 因果节点数量、有主信息数量、主果命中数量等查询统计，只用于 `执行因果路径查询壳` 内部日志。
    2. `派生需求消息集` 是唯一被调用方读取的有效输出。

正式承接链不是该统计结构体：
    查询壳读取世界树因果和需求目标比较；
    临时因果视图生成派生需求消息；
    后续 `尝试准备因果路径筹办入口壳` 把消息集合交给任务筹办结果；
    正式入树仍由后续提交入口裁决。

该结构体不写入项目结构，不作为状态 / 动态证据，不承担需求树事实或结算证据。
```

项目结构映射：

| 业务概念 | 当前承载结构体 | 项目承载结构 | 值类型 | 写入方 | 读取方 | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- |
| 查询统计日志 | `结构_因果路径查询壳统计` | 无业务承载；仅日志局部计数 | `std::size_t` | `执行因果路径查询壳` | `执行因果路径查询壳` 日志输出 | 删除结构体后用局部计数保持日志字段 |
| 派生需求正式承接消息 | `结构_因果路径查询壳统计::派生需求消息集` | 任务筹办结果中的派生需求消息集合，后续提交入口裁决 | `std::vector<结构_任务管理派生需求消息>` | `记录派生需求消息草案统计_筹办查询` | `尝试准备因果路径筹办入口壳` / 任务筹办结果 | 输出参数写入、源码引用检查、构建 |

最小实现：

```text
1. 删除 `结构_因果路径查询壳统计`。
2. `执行因果路径查询壳` 返回 `void`，新增 `输出派生需求消息集` 输出参数。
3. 函数内部统计改成局部计数变量。
4. 调用方直接传入 `结果.派生需求消息集`，不再构造查询统计返回视图。
```

预测验证：

```text
1. 源码中 `结构_因果路径查询壳统计` 和该函数内 `统计.` 旧读法消失。
2. `派生需求消息集` 后续承接路径保持不变。
3. 结构体清单删除该结构体，结构体总数从 524 降到 523；含文本字段候选保持 294。
4. 函数说明文档刷新后函数签名位置更新。
5. Debug x64 串行构建应通过。
```

## 31. P4o 执行结果

已改代码路径：

```text
任务模块.筹办.ixx
```

代码改动：

```text
1. 删除 `结构_因果路径查询壳统计`。
2. `执行因果路径查询壳` 改为 `void`，通过 `输出派生需求消息集` 写出消息集合。
3. 原结构体统计字段替换为函数内部局部计数变量。
4. `尝试准备因果路径筹办入口壳` 直接把 `结果.派生需求消息集` 传给查询壳。
```

验证：

```text
rg "结构_因果路径查询壳统计|统计\\.|执行因果路径查询壳" 任务模块.筹办.ixx -n
    结构体名无源码命中；`统计.` 命中均属于同文件其他统计结构，不属于本切片函数；
    `执行因果路径查询壳` 定义和调用各 1 处。

python .\tools\生成函数功能说明文档.py
    exit_code=0；函数条目=7307。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    exit_code=0；
    日志/20260618_结构体审计_因果路径查询壳统计视图删除_msbuild_serial.log
    精确错误模式无命中；日志行包含“已成功生成”。
```

审计产物更新：

```text
实施记录/20260618_项目结构体审计_结构体清单.tsv
    删除 `结构_因果路径查询壳统计` 行；
    `任务模块.筹办.ixx` 后续结构体行号已按当前源码校正；
    结构体清单：523；含文本字段候选：294。

实施记录/20260618_项目结构体审计_判定表.md
    追加 P4o 删除判定和验证证据。

计划/计划索引.md
    当前下一步计数更新为 523 / 294。
```

## 32. P4p 直接子需求聚合视图删除执行前断点

当前切片：

```text
删除 `任务模块.筹办.ixx::结构_直接子需求聚合视图_筹办`。
```

判定依据：

```text
该结构体只在 `返回直接子需求聚合筹办结果` 调用链内部传递：
    直接子需求数、已满足子需求数、未满足子需求数；
    首个未满足子需求主键。

真实判断来源是需求树直接子需求及 `需求已满足`；
正式结果写入 `结构_任务筹办结果::回执`，并通过任务管理后续生命周期承接；
`首个未满足子需求主键` 只进入说明 / 日志，不作为机器状态或动态证据。

该结构体不被跨文件读取，不写入项目结构，不作为需求状态、任务状态、动作动态或结算证据。
```

项目结构映射：

| 业务概念 | 当前承载结构体 | 项目承载结构 | 值类型 | 写入方 | 读取方 | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- |
| 子需求满足判断 | `结构_直接子需求聚合视图_筹办` | 来源需求节点的直接子需求链及需求主信息状态 | 需求节点指针 / I64 状态值 | 需求树生长 / 需求满足回写链 | `返回直接子需求聚合筹办结果` | 局部计数读取需求树，回执字段保持 |
| 首个未满足子需求说明 | `结构_直接子需求聚合视图_筹办::首个未满足子需求主键` | 无业务承载；仅说明 / 日志 | `std::string` | `统计直接子需求聚合_筹办` | 筹办说明和日志 | 删除结构体后仍只作日志说明 |

最小实现：

```text
1. 删除 `结构_直接子需求聚合视图_筹办`。
2. 将 `分析直接子需求聚合视图_筹办` 改为 `统计直接子需求聚合_筹办`，通过引用输出局部计数和说明主键。
3. `返回直接子需求聚合筹办结果` 使用局部 bool 判断结构缺口和全部满足。
4. 不改变筹办回执状态、任务建议、实际结果状态集或日志字段含义。
```

预测验证：

```text
1. 源码中 `结构_直接子需求聚合视图_筹办` 和 `聚合视图.` 旧读法消失。
2. `返回直接子需求聚合筹办结果` 仍写入同一组筹办回执字段。
3. 结构体清单删除该结构体，结构体总数从 523 降到 522；含文本字段候选从 294 降到 293。
4. 函数说明文档刷新后函数名 / 行号更新。
5. Debug x64 串行构建应通过。
```

## 33. P4p 执行结果

已改代码路径：

```text
任务模块.筹办.ixx
```

代码改动：

```text
1. 删除 `结构_直接子需求聚合视图_筹办`。
2. `分析直接子需求聚合视图_筹办` 改为 `统计直接子需求聚合_筹办`。
3. `返回直接子需求聚合筹办结果` 改用局部计数和局部 bool：
   `存在结构缺口`、`全部子需求已满足`。
4. 筹办回执状态、任务建议、实际结果状态集和日志字段含义保持不变。
```

验证：

```text
rg "结构_直接子需求聚合视图_筹办|分析直接子需求聚合视图_筹办|聚合视图\\.|统计直接子需求聚合_筹办" 任务模块.筹办.ixx -n
    旧结构体名、旧函数名和 `聚合视图.` 旧读法无命中；
    新统计函数定义 1 处、调用 1 处。

python .\tools\生成函数功能说明文档.py
    exit_code=0；函数条目=7304。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    exit_code=0；
    日志/20260618_结构体审计_直接子需求聚合视图删除_msbuild_serial.log
    精确错误模式无命中；日志行包含“已成功生成”。
```

审计产物更新：

```text
实施记录/20260618_项目结构体审计_结构体清单.tsv
    删除 `结构_直接子需求聚合视图_筹办` 行；
    `任务模块.筹办.ixx` 后续结构体行号已按当前源码校正；
    结构体清单：522；含文本字段候选：293。

实施记录/20260618_项目结构体审计_判定表.md
    追加 P4p 删除判定和验证证据。

计划/计划索引.md
    当前下一步计数更新为 522 / 293。
```

## 34. P4q 任务节点统计视图删除执行前断点

当前切片：

```text
删除 `鱼巢.cpp::结构_任务节点统计`。
```

判定依据：

```text
该结构体只在 `鱼巢.cpp` 内部用于 `私有_记录自我实现检查日志` 的任务树日志计数：
    头结点数；
    步骤节点数。

真实任务树状态来自世界树任务根节点和任务节点主信息；
两个计数只进入 `自我实现检查/任务树` 运行日志；
不写入项目结构，不作为任务状态、需求状态、动作动态或结算证据。
```

项目结构映射：

| 业务概念 | 当前承载结构体 | 项目承载结构 | 值类型 | 写入方 | 读取方 | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- |
| 任务树日志计数 | `结构_任务节点统计` | 任务树节点；计数仅为日志投影 | `std::size_t` | `私有_统计任务节点` | `私有_记录自我实现检查日志` | 删除结构体后局部计数保持日志字段 |

最小实现：

```text
1. 删除 `结构_任务节点统计`。
2. `私有_统计任务节点` 改为 `void`，引用输出两个局部计数。
3. `私有_记录自我实现检查日志` 使用局部 `任务头结点数` / `任务步骤节点数` 写原日志字段。
```

预测验证：

```text
1. 源码中 `结构_任务节点统计` 和 `任务统计.` 旧读法消失。
2. `自我实现检查/任务树` 日志字段名保持。
3. 结构体清单删除该结构体，结构体总数从 522 降到 521；含文本字段候选保持 293。
4. 函数说明文档刷新后函数签名位置更新。
5. Debug x64 串行构建应通过。
```

## 35. P4q 执行结果

已改代码路径：

```text
鱼巢.cpp
```

代码改动：

```text
1. 删除 `结构_任务节点统计`。
2. `私有_统计任务节点` 改为 `void`，引用输出头结点数和步骤节点数。
3. `私有_记录自我实现检查日志` 改用局部 `任务头结点数` / `任务步骤节点数`。
4. `自我实现检查/任务树` 日志字段名保持不变。
```

验证：

```text
rg "结构_任务节点统计|任务统计\\.|私有_统计任务节点|任务头结点数|任务步骤节点数" 鱼巢.cpp -n
    旧结构体名和 `任务统计.` 旧读法无命中；
    新统计函数定义 1 处、调用 1 处。

python .\tools\生成函数功能说明文档.py
    exit_code=0；函数条目=7304。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    exit_code=0；
    日志/20260618_结构体审计_任务节点统计视图删除_msbuild_serial.log
    精确错误模式无命中；日志行包含“已成功生成”。
```

审计产物更新：

```text
实施记录/20260618_项目结构体审计_结构体清单.tsv
    删除 `结构_任务节点统计` 行；
    `鱼巢.cpp` 后续结构体行号已按当前源码校正；
    结构体清单：521；含文本字段候选：293。

实施记录/20260618_项目结构体审计_判定表.md
    追加 P4q 删除判定和验证证据。

计划/计划索引.md
    当前下一步计数更新为 521 / 293。
```

## 36. P4r 消息中间件维护统计视图删除执行前断点

当前切片：

```text
删除 `线程生命周期消息模块.ixx::结构_消息中间件维护统计`。
```

判定依据：

```text
该结构体只在消息中间件维护链内部传递 7 个维护计数：
    归档消息数、删除已消费普通消息数、删除无效消息数、删除临时文件数、
    删除过期归档数、归档失败数、删除失败数。

这些计数只用于判断是否输出维护日志，以及构造维护日志字段；
真实消息状态由文件系统中的消息文件 / 已消费目录 / 归档目录承担；
删除失败仍通过错误日志记录，不由该结构体承载系统状态。

该结构体不跨文件读取，不写入项目结构，不作为线程生命周期消息、任务状态、动作动态或结算证据。
```

项目结构映射：

| 业务概念 | 当前承载结构体 | 项目承载结构 | 值类型 | 写入方 | 读取方 | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- |
| 消息中间件维护计数 | `结构_消息中间件维护统计` | 文件系统消息目录状态；计数仅为维护日志投影 | `std::size_t` | 消息中间件维护 helper | `私有_维护消息中间件文件` 日志输出 | 删除结构体后局部计数保持日志字段 |

最小实现：

```text
1. 删除 `结构_消息中间件维护统计`。
2. `私有_删除文件` 改为引用输出删除失败数。
3. 三个目录维护 helper 改为引用输出相关局部计数。
4. `私有_维护消息中间件文件` 使用局部计数计算变化数和写原日志字段。
```

预测验证：

```text
1. 源码中 `结构_消息中间件维护统计` 和 `统计.` 在该维护链中的旧读法消失。
2. 维护日志字段名保持。
3. 结构体清单删除该结构体，结构体总数从 521 降到 520；含文本字段候选保持 293。
4. 函数说明文档刷新后函数签名位置更新。
5. Debug x64 串行构建应通过。
```

## 37. P4r 执行结果

已改代码路径：

```text
线程生命周期消息模块.ixx
```

代码改动：

```text
1. 删除 `结构_消息中间件维护统计`。
2. `私有_删除文件` 改为引用输出删除失败数。
3. `私有_维护消息中间件根目录`、`私有_维护消息中间件已消费目录`、
   `私有_维护消息中间件归档目录` 改为引用输出相关局部计数。
4. `私有_维护消息中间件文件` 使用 7 个局部计数计算变化数并写原日志字段。
```

验证：

```text
rg "结构_消息中间件维护统计|统计\\." 线程生命周期消息模块.ixx -n
    旧结构体名和 `统计.` 旧读法无命中。

python .\tools\生成函数功能说明文档.py
    exit_code=0；函数条目=7304。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    exit_code=0；
    日志/20260618_结构体审计_消息中间件维护统计视图删除_msbuild_serial.log
    精确错误模式无命中；日志行包含“已成功生成”。
```

审计产物更新：

```text
实施记录/20260618_项目结构体审计_结构体清单.tsv
    删除 `结构_消息中间件维护统计` 行；
    `线程生命周期消息模块.ixx` 后续结构体行号已按当前源码校正；
    结构体清单：520；含文本字段候选：293。

实施记录/20260618_项目结构体审计_判定表.md
    追加 P4r 删除判定和验证证据。

计划/计划索引.md
    当前下一步计数更新为 520 / 293。
```

## 38. P4s 任务调度消费统计视图删除执行前断点

当前切片：

```text
删除 `任务模块.管理界面线程.impl.cpp::结构_任务调度消费统计`。
```

判定依据：

```text
该结构体只在 `任务模块.管理界面线程.impl.cpp` 内部引用。

字段：
    `派发工作项数`；
    `收到工作结果数`；
    `推进事件列表`；
    `上行消息集`。

引用证据：
    `派发工作项数` / `收到工作结果数` 只用于本轮消费后累计到界面线程快照计数；
    `推进事件列表` / `上行消息集` 只由 `私有_累计工作结果消息` 写入，未读取；
    真实承接消息仍由 `承接.推进事件列表` 和 `承接.上行消息集` 从工作结果拷贝后推入上行队列。

该结构体不跨文件读取，不写入存在 / 场景 / 特征 / 状态 / 动态 / 方法 / 需求，
不承担任务状态、动作动态或上行消息证据本体。
```

项目结构映射：

| 业务概念 | 当前承载结构体 | 项目承载结构 | 值类型 | 写入方 | 读取方 | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- |
| 本轮派发 / 收到工作结果计数 | `结构_任务调度消费统计` | 界面线程快照计数；计数仅为运行快照投影 | `std::uint64_t` | 调度队列消费 / 完成队列归并 helper | `归并任务工作完成队列` / `巡检并派发就绪任务` | 删除结构体后局部计数保持快照累计 |
| 推进事件 / 上行消息临时聚合 | `结构_任务调度消费统计` | `结构_任务界面承接结果` 中已有推进事件和上行消息集 | 项目协议结构 vector | `结构_任务工作结果` -> 承接结果 | 上行推送和承接结果记录 | 删除未读聚合字段，保留承接结果路径 |

最小实现：

```text
1. 删除 `结构_任务调度消费统计`。
2. 删除只写未读的 `私有_累计工作结果消息`。
3. `私有_归并完成队列项`、`私有_消费任务调度队列_按请求`、`私有_消费任务调度队列`
   改为引用输出局部计数。
4. 两个公开入口使用局部计数更新快照和返回条件。
```

预测验证：

```text
1. 源码中 `结构_任务调度消费统计`、`消费统计.`、`统计.推进事件列表`、
   `统计.上行消息集` 和 `私有_累计工作结果消息` 引用为 0。
2. 函数说明文档刷新后旧函数条目消失，相关函数行号更新。
3. 结构体清单删除该结构体，结构体总数从 520 降到 519；含文本字段候选保持 293。
4. Debug x64 串行构建应通过。
```

## 39. P4s 执行结果

已改代码路径：

```text
任务模块.管理界面线程.impl.cpp
```

代码改动：

```text
1. 删除 `结构_任务调度消费统计`。
2. 删除只写未读的 `私有_累计工作结果消息`。
3. `私有_归并完成队列项` 改为引用输出 `派发工作项数` 和 `收到工作结果数`。
4. 两个调度消费 helper 和两个公开入口改用局部计数变量。
```

验证：

```text
rg "结构_任务调度消费统计|消费统计\\.|统计\\.推进事件列表|统计\\.上行消息集|私有_累计工作结果消息" 任务模块.管理界面线程.impl.cpp -n
    旧结构体名、旧局部变量、未读聚合字段和旧 helper 无命中。

python .\tools\生成函数功能说明文档.py
    exit_code=0；函数条目=7304。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    exit_code=0；
    日志/20260618_结构体审计_任务调度消费统计视图删除_msbuild_serial.log
    日志行包含“已成功生成”。
```

审计产物更新：

```text
实施记录/20260618_项目结构体审计_结构体清单.tsv
    删除 `结构_任务调度消费统计` 行；
    结构体清单：519；含文本字段候选：293。

实施记录/20260618_项目结构体审计_判定表.md
    追加 P4s 删除判定。

计划/计划索引.md
    当前下一步计数更新为 519 / 293。
```

## 40. P4t 需求列表刷新决策未用返回视图删除执行前断点

当前切片：

```text
删除 `自我线程模块.消息处理器.ixx::结构_需求列表刷新决策`。
```

判定依据：

```text
该结构体只包含两个 bool：
    `需要刷新`；
    `可以局部刷新`。

引用证据：
    只被同文件 `判定需求列表刷新决策` 构造和返回；
    `判定需求列表刷新决策` 只有定义和 export using，无源码调用点；
    该结构体不写入存在 / 场景 / 特征 / 状态 / 动态 / 方法 / 需求；
    不承担当前主循环消息处理、需求树变更或任务调度证据。
```

项目结构映射：

| 业务概念 | 当前承载结构体 | 项目承载结构 | 值类型 | 写入方 | 读取方 | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- |
| 需求列表刷新显示决策 | `结构_需求列表刷新决策` | 当前无有效调用路径；真实需求树刷新由消息处理结果和需求树变更路径直接驱动 | bool | 未调用函数 `判定需求列表刷新决策` | 无源码读取方 | 删除结构体、未调用函数和导出后构建验证 |

最小实现：

```text
1. 删除 `结构_需求列表刷新决策`。
2. 删除未调用的 `判定需求列表刷新决策`。
3. 删除对应 export using。
```

预测验证：

```text
1. 源码中 `结构_需求列表刷新决策` 和 `判定需求列表刷新决策` 引用为 0。
2. 函数说明文档刷新后旧函数条目消失，函数条目数减少 1。
3. 结构体清单删除该结构体，结构体总数从 519 降到 518；含文本字段候选保持 293。
4. Debug x64 串行构建应通过。
```

## 41. P4t 执行结果

已改代码路径：

```text
自我线程模块.消息处理器.ixx
```

代码改动：

```text
1. 删除 `结构_需求列表刷新决策`。
2. 删除未调用的 `判定需求列表刷新决策`。
3. 删除对应 export using。
```

验证：

```text
rg "结构_需求列表刷新决策|判定需求列表刷新决策" -n -g "*.cpp" -g "*.h" -g "*.ixx"
    旧结构体名和旧函数名无命中。

python .\tools\生成函数功能说明文档.py
    exit_code=0；函数条目=7303。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    exit_code=0；
    日志/20260618_结构体审计_需求列表刷新决策视图删除_msbuild_serial.log
    日志行包含“已成功生成”。
```

审计产物更新：

```text
实施记录/20260618_项目结构体审计_结构体清单.tsv
    删除 `结构_需求列表刷新决策` 行；
    结构体清单：518；含文本字段候选：293。

实施记录/20260618_项目结构体审计_判定表.md
    追加 P4t 删除判定。

计划/计划索引.md
    当前下一步计数更新为 518 / 293。
```

## 42. P4u 治理消息轨迹单字段视图删除执行前断点

当前切片：

```text
删除 `自我线程模块.消息协议.ixx::结构_治理消息轨迹`。
```

判定依据：

```text
该结构体只包含一个字段：
    `std::uint32_t 重放代数`。

引用证据：
    只被 `结构_治理消息` 作为 `轨迹` 字段嵌入；
    只有 `自我线程模块.impl.cpp::投递治理消息` 幂等对账读取 `轨迹.重放代数`；
    不跨协议独立传递，不写入存在 / 场景 / 特征 / 状态 / 动态 / 方法 / 需求；
    不承担状态或动态证据，只是治理消息自身的重放分代数值。
```

项目结构映射：

| 业务概念 | 当前承载结构体 | 项目承载结构 | 值类型 | 写入方 | 读取方 | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- |
| 治理消息重放分代 | `结构_治理消息轨迹` | `结构_治理消息` 自身数值字段 | `std::uint32_t` | 治理消息构造方默认值 / 后续可能显式设置方 | `投递治理消息` 幂等对账 | 删除结构体后改读 `结构_治理消息::重放代数` |

最小实现：

```text
1. 删除 `结构_治理消息轨迹`。
2. `结构_治理消息` 直接保存 `std::uint32_t 重放代数`。
3. 对账读取从 `消息.轨迹.重放代数` 改为 `消息.重放代数`。
4. 删除对应 export using。
```

预测验证：

```text
1. 源码中 `结构_治理消息轨迹` 和 `.轨迹.重放代数` 引用为 0。
2. 函数说明文档刷新后相关行号更新。
3. 结构体清单删除该结构体，结构体总数从 518 降到 517；含文本字段候选保持 293。
4. Debug x64 串行构建应通过。
```

## 43. P4u 执行结果

已改代码路径：

```text
自我线程模块.消息协议.ixx
自我线程模块.impl.cpp
```

代码改动：

```text
1. 删除 `结构_治理消息轨迹`。
2. `结构_治理消息` 直接保存 `std::uint32_t 重放代数`。
3. `投递治理消息` 幂等对账读取改为 `已有事件.重放代数` / `副本.重放代数`。
4. 删除对应 export using。
```

验证：

```text
rg "结构_治理消息轨迹|\\.轨迹\\.重放代数" -n -g "*.cpp" -g "*.h" -g "*.ixx"
    旧结构体名和旧字段路径无命中。

python .\tools\生成函数功能说明文档.py
    exit_code=0；函数条目=7303。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    exit_code=0；
    日志/20260618_结构体审计_治理消息轨迹视图删除_msbuild_serial.log
    日志行包含“已成功生成”。
```

审计产物更新：

```text
实施记录/20260618_项目结构体审计_结构体清单.tsv
    删除 `结构_治理消息轨迹` 行；
    `结构_治理消息` 字段摘要改为直接包含 `重放代数`；
    结构体清单：517；含文本字段候选：293。

实施记录/20260618_项目结构体审计_判定表.md
    追加 P4u 删除判定。

计划/计划索引.md
    当前下一步计数更新为 517 / 293。
```

## 44. P4v 结果回流提示段未用协议视图删除执行前断点

当前切片：

```text
删除 `自我线程模块.消息协议.ixx::结构_结果回流提示段`。
```

判定依据：

```text
该结构体只包含四个 bool：
    `可能存在反向推进`；
    `可能形成阻塞`；
    `可能影响父任务`；
    `可能形成否定项候选`。

引用证据：
    只有定义、`结构_任务结果回流消息::提示段` 嵌入和 export using；
    四个字段无源码读写点；
    不写入存在 / 场景 / 特征 / 状态 / 动态 / 方法 / 需求；
    不承担结果回流事实或动作动态证据。
```

项目结构映射：

| 业务概念 | 当前承载结构体 | 项目承载结构 | 值类型 | 写入方 | 读取方 | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- |
| 结果回流提示布尔段 | `结构_结果回流提示段` | 当前无有效写入 / 读取路径；结果回流事实仍由 `结构_任务结果回流消息` 的任务、状态、结果值和关键中间变化集承担 | bool | 无源码写入方 | 无源码读取方 | 删除结构体、嵌入字段和导出后构建验证 |

最小实现：

```text
1. 删除 `结构_结果回流提示段`。
2. 删除 `结构_任务结果回流消息::提示段`。
3. 删除对应 export using。
```

预测验证：

```text
1. 源码中 `结构_结果回流提示段` 和 `.提示段` 引用为 0。
2. 函数说明文档刷新后相关行号更新。
3. 结构体清单删除该结构体，结构体总数从 517 降到 516；含文本字段候选保持 293。
4. Debug x64 串行构建应通过。
```

## 45. P4v 执行结果

已改代码路径：

```text
自我线程模块.消息协议.ixx
```

代码改动：

```text
1. 删除 `结构_结果回流提示段`。
2. 删除 `结构_任务结果回流消息::提示段`。
3. 删除对应 export using。
```

验证：

```text
rg "结构_结果回流提示段|\\.提示段" -n -g "*.cpp" -g "*.h" -g "*.ixx"
    旧结构体名和旧字段路径无命中。

python .\tools\生成函数功能说明文档.py
    exit_code=0；函数条目=7303。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    exit_code=0；
    日志/20260618_结构体审计_结果回流提示段视图删除_msbuild_serial.log
    日志行包含“已成功生成”。
```

审计产物更新：

```text
实施记录/20260618_项目结构体审计_结构体清单.tsv
    删除 `结构_结果回流提示段` 行；
    `自我线程模块.消息协议.ixx` 后续结构体行号已按当前源码校正；
    结构体清单：516；含文本字段候选：293。

实施记录/20260618_项目结构体审计_判定表.md
    追加 P4v 删除判定。

计划/计划索引.md
    当前下一步计数更新为 516 / 293。
```

## 16. P4h 自检基础信息预检结果视图删除执行前断点

当前切片：

```text
删除 `自检线程模块.impl.cpp::结构_自检基础信息预检结果`。
```

判定依据：

```text
该结构体只在自检线程内部作为预检返回视图：
    `通过` 是基础信息预检布尔闸门；
    `摘要` 只用于等待日志、警告日志和重复记录去重。

结构体本身不写入项目结构，不承担状态或动态证据。
```

最小实现：

```text
1. 删除 `结构_自检基础信息预检结果`。
2. `私有_检查自检基础信息_自检` 改为返回 bool，并通过 `std::string& 摘要` 输出日志摘要。
3. 两个预检调用点改为局部 `预检摘要` 和 `预检通过`。
```

不做范围：

```text
不改变基础信息预检的缺项判定。
不改变自检线程等待 / 休眠期自检请求逻辑。
不把摘要文本作为机器证据。
```

预测验证：

```text
1. 源码中 `结构_自检基础信息预检结果` 和 `预检.` 引用为 0。
2. 函数说明文档刷新后函数签名位置更新。
3. 结构体清单删除该结构体，结构体总数从 531 降到 530。
4. Debug x64 串行构建应通过。
5. 真实 self-check 窗口应 exit_code=0、stderr=0、异常日志不增长。
```

## 17. P4h 执行结果

已改代码路径：

```text
自检线程模块.impl.cpp
    删除 `结构_自检基础信息预检结果`。
    `私有_检查自检基础信息_自检` 改为返回 bool；
        通过 `std::string& 摘要文本` 输出原有摘要。
    两个预检调用点改为局部 `预检通过` / `预检摘要`。

说明书/函数功能说明文档.md
    已由脚本刷新。
```

清单变化：

```text
结构体清单：530。
含文本字段结构体候选：296。
```

验证：

```text
rg "结构_自检基础信息预检结果|预检\\." -S -g "*.cpp" -g "*.h" -g "*.ixx"
    结果：源码引用 0。

python .\tools\生成函数功能说明文档.py
    结果：exit_code=0；函数条目=7306。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    结果：exit_code=0；0 警告，0 错误。

.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    结果：exit_code=0；stdout_len=10048；stderr_len=0；
    exception_before=5909；exception_after=5909。
```

仍未宣称：

```text
不宣称结构体清理全部完成；
不宣称项目结构闭环全部通过；
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 14. P4g 自检线程方法结构计数视图删除执行前断点

当前切片：

```text
删除 `自检线程模块.impl.cpp` 中两个内部计数视图：
    `结构_方法结构计数`；
    `结构_条件结果对自检统计`。
```

判定依据：

```text
二者只在自检线程方法结构扫描中临时保存计数：
    直接条件数；
    直接结果数；
    条件结果对数量；
    条件结果对命中次数总和；
    条件结果对最大命中次数。

这些值来自方法树结构遍历，用于推导自检当前状态和生成缺口观察文本；
结构体本身不写入项目结构，也不承担状态或动态证据。
```

最小实现：

```text
1. 删除 `结构_方法结构计数`。
2. 删除 `结构_条件结果对自检统计`。
3. `私有_统计方法直接结构_自检` 通过引用输出条件数 / 结果数。
4. `私有_统计方法条件结果对_自检` 通过引用输出配对数量 / 命中总和 / 最大命中。
5. `私有_推导方法自检当前状态值_自检` 直接接收局部计数。
```

不做范围：

```text
不修改方法结构自检的缺口生成规则。
不修改方法状态目标值。
不把日志观察文本当作机器证据。
```

预测验证：

```text
1. 源码中两个结构体名引用为 0。
2. 函数说明文档刷新后旧统计函数名消失，新函数名出现。
3. 结构体清单删除两个结构体，结构体总数从 533 降到 531。
4. Debug x64 串行构建应通过。
5. 真实 self-check 窗口应 exit_code=0、stderr=0、异常日志不增长。
```

## 15. P4g 执行结果

已改代码路径：

```text
自检线程模块.impl.cpp
    删除 `结构_方法结构计数`。
    删除 `结构_条件结果对自检统计`。
    `私有_统计方法直接结构_自检` 通过引用输出条件数 / 结果数。
    `私有_统计方法条件结果对_自检` 通过引用输出条件结果对数量 / 命中总和 / 最大命中。
    `私有_推导方法自检当前状态值_自检` 直接接收局部计数。

说明书/函数功能说明文档.md
    已由脚本刷新。
```

清单变化：

```text
结构体清单：531。
含文本字段结构体候选：297。
```

验证：

```text
rg "结构_方法结构计数|结构_条件结果对自检统计" -S -g "*.cpp" -g "*.h" -g "*.ixx"
    结果：源码引用 0。

python .\tools\生成函数功能说明文档.py
    结果：exit_code=0；函数条目=7306。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    结果：exit_code=0；0 警告，0 错误。

.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    结果：exit_code=0；stdout_len=10117；stderr_len=0；
    exception_before=5909；exception_after=5909。
```

仍未宣称：

```text
不宣称结构体清理全部完成；
不宣称项目结构闭环全部通过；
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 13. P4f 执行结果

已改代码路径：

```text
控制面板类.cpp
    删除 `结构_方法直接结构计数`。
    `私有_统计方法直接条件结果_控制面板` 通过引用输出四个局部计数。
    三个调用点改为局部变量接收条件数 / 直接结果数 / 条件下结果数 / 条件无结果数。

控制面板摘要线程模块.impl.cpp
    删除 `结构_方法直接结构计数`。
    `私有_统计方法直接结构_控制面板摘要` 通过引用输出四个局部计数。
    摘要线程方法结构统计调用点改为局部变量接收计数。

说明书/函数功能说明文档.md
    已由脚本刷新。
```

清单变化：

```text
结构体清单：533。
含文本字段结构体候选：297。
```

验证：

```text
rg "结构_方法直接结构计数" -S -g "*.cpp" -g "*.h" -g "*.ixx"
    结果：源码引用 0。

python .\tools\生成函数功能说明文档.py
    结果：exit_code=0；函数条目=7306。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    结果：exit_code=0；0 警告，0 错误。
```

保留边界：

```text
`自检线程模块.impl.cpp::结构_方法结构计数` 是另一结构名和另一自检链路，
本切片只记录为后续候选，不混入控制面板计数视图删除提交。
```

仍未宣称：

```text
不宣称结构体清理全部完成；
不宣称项目结构闭环全部通过；
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 12. P4f 控制面板方法直接结构计数视图删除执行前断点

当前切片：

```text
删除控制面板同族 `结构_方法直接结构计数`：
    `控制面板类.cpp::结构_方法直接结构计数`；
    `控制面板摘要线程模块.impl.cpp::结构_方法直接结构计数`。
```

判定依据：

```text
`结构_方法直接结构计数` 只在控制面板内部临时保存四个计数：
    条件数；
    直接结果数；
    条件下结果数；
    条件无结果数。

这些计数来自方法树节点结构遍历，用于控制面板显示和结构异常提示；
不写入项目结构，不承担状态或动态证据，也没有外部读取。
```

最小实现：

```text
1. 删除两处 `结构_方法直接结构计数`。
2. 将 `私有_统计方法直接条件结果` 改为
   `私有_统计方法直接条件结果_控制面板`，
   通过引用输出四个局部计数。
3. 将 `控制面板摘要线程模块.impl.cpp::私有_统计方法直接结构`
   改为 `私有_统计方法直接结构_控制面板摘要`，
   通过引用输出四个局部计数。
4. 三个控制面板主模块调用点和一个摘要线程调用点改为局部变量接收计数。
```

不做范围：

```text
不修改方法树结构。
不改变方法条件 / 结果节点的业务判断。
不把控制面板计数当成方法能力证据。
```

预测验证：

```text
1. 源码中 `结构_方法直接结构计数` 和 `计数.` 相关旧读法在该函数调用链中消失。
2. 函数说明文档刷新后旧函数名消失，新函数名出现。
3. 结构体清单删除两处同族结构体，结构体总数从 535 降到 533。
4. Debug x64 串行构建应通过。
```

## 10. P4e 控制面板缺口承接快照视图删除执行前断点

当前切片：

```text
删除 `控制面板类.cpp::结构_控制面板缺口承接快照`。
```

判定依据：

```text
`结构_控制面板缺口承接快照` 只在控制面板内部把下列已有来源复制成局部聚合视图：
    自我特征 `自我_待处理方法数量`；
    `自我线程类::读取最近缺口恢复接口快照()` 返回的触发 / 恢复请求 / 摘要字段。

该结构体没有外部读取，不写入项目结构，也不承担状态或动态证据；
控制面板最终快照可以直接读取上述来源。
```

最小实现：

```text
1. 删除 `结构_控制面板缺口承接快照`。
2. 删除 `私有_读取缺口承接快照_控制面板`。
3. 新增 `私有_读取自我待处理方法数量_控制面板`。
4. 新增 `私有_缺口恢复反馈摘要_控制面板`。
5. 控制面板主快照构造处直接读取自我特征和自我线程缺口恢复接口快照。
```

不做范围：

```text
不修改 `自我线程类::结构_缺口恢复接口快照`。
不改缺口恢复请求的产生、调度或状态来源。
不把控制面板摘要文本作为机器证据。
```

预测验证：

```text
1. 源码中 `结构_控制面板缺口承接快照`、`私有_读取缺口承接快照_控制面板`
   和 `缺口承接快照.` 引用为 0。
2. 函数说明文档刷新后新增两个功能函数，旧函数条目消失。
3. 结构体清单删除该结构体，结构体总数从 536 降到 535。
4. Debug x64 串行构建应通过。
```

## 11. P4e 执行结果

已改代码路径：

```text
控制面板类.cpp
    删除 `结构_控制面板缺口承接快照`。
    删除 `私有_读取缺口承接快照_控制面板`。
    新增 `私有_读取自我待处理方法数量_控制面板`。
    新增 `私有_缺口恢复反馈摘要_控制面板`。
    控制面板主快照构造处直接读取：
        自我待处理方法数量特征；
        `自我线程类::读取最近缺口恢复接口快照()`。

说明书/函数功能说明文档.md
    已由脚本刷新。
```

清单变化：

```text
结构体清单：535。
含文本字段结构体候选：297。
```

验证：

```text
rg "结构_控制面板缺口承接快照|私有_读取缺口承接快照_控制面板|缺口承接快照\\." -S -g "*.cpp" -g "*.h" -g "*.ixx"
    结果：源码引用 0。

python .\tools\生成函数功能说明文档.py
    结果：exit_code=0；函数条目=7306。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    结果：exit_code=0；0 警告，0 错误。
```

仍未宣称：

```text
不宣称结构体清理全部完成；
不宣称项目结构闭环全部通过；
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 6. P4c 执行尝试标识视图删除执行前断点

当前切片：

```text
删除 `自我线程模块.消息协议.ixx::结构_执行尝试标识`。
```

判定依据：

```text
`结构_执行尝试标识` 由 `结构_任务管理消息头` 复制执行尝试主键、任务主键、父任务主键、
步骤主键和幂等键等文本字段，只用于执行尝试汇总 / 对账的内部对齐。
它没有独立写入项目结构，也不承载状态或动态证据。
```

项目结构映射：

| 业务概念 | 当前承载结构体 | 项目承载结构 | 值类型 | 写入方 | 读取方 | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- |
| 执行尝试归并基准 | `结构_执行尝试标识` | 任务管理消息头即时投影出的数值主键 / 路径签名 / 执行尝试号；汇总状态仍写入执行尝试汇总状态语义槽 | `std::uint64_t` / `bool` / 枚举状态值 | `合并线程状态消息到执行尝试汇总`、`合并结果消息到执行尝试汇总` | `是否任务管理消息头属于同一次执行尝试`、`刷新执行尝试汇总状态` | 源码引用检查、函数说明文档刷新、Debug x64 构建 |

最小实现：

```text
1. 用函数从 `结构_任务管理消息头` 直接生成数值对齐键。
2. `结构_执行尝试汇总` 增加 `已建立执行尝试基准` 和数值基准字段。
3. `结构_执行尝试对账结果` 不再携带 `结构_执行尝试标识`。
4. 删除 `从任务管理消息头提取执行尝试标识` 及相关导出。
```

不做范围：

```text
不在本切片改造 `结构_任务管理消息头` 的源协议文本字段。
不修改任务管理线程生成上行消息的路由语义。
不新增兼容字段或新旧双写。
```

预测验证：

```text
1. `结构_执行尝试标识` 及旧提取函数源码引用为 0。
2. 结构体清单从 538 降到 537，含文本字段结构体从 300 降到 299。
3. Debug x64 构建应通过。
4. 真实自检窗口不应因执行尝试对账视图删除产生编译后启动错误、stderr 或新增异常日志。
```

## 7. P4c 执行结果与验证中断处理

已改代码路径：

```text
自我线程模块.消息协议.ixx
    删除 `结构_执行尝试标识`。
    删除 `从任务管理消息头提取执行尝试标识`、
        `从任务管理线程状态消息提取执行尝试标识`、
        `从任务管理结果状态消息提取执行尝试标识`。
    新增 `任务管理消息头执行尝试主键值`、
        `建立执行尝试汇总基准`、
        `执行尝试汇总基准可对齐`。
    执行尝试对账改为直接比较任务管理消息头的数值投影；
    执行尝试汇总只保存数值基准，不再复制文本标识视图。

说明书/函数功能说明文档.md
    已由脚本刷新。
```

清单变化：

```text
文件清单：135。
结构体清单：537。
含文本字段结构体候选：299。
```

验证：

```text
rg "结构_执行尝试标识|从任务管理消息头提取执行尝试标识|从任务管理线程状态消息提取执行尝试标识|从任务管理结果状态消息提取执行尝试标识" -S -g "*.cpp" -g "*.h" -g "*.ixx"
    结果：源码引用 0。

python .\tools\生成函数功能说明文档.py
    结果：exit_code=0；函数条目=7303。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    首次构建发现旧字段残留 `对账结果.标识 = 汇总->标识`；
    删除残留赋值后复跑 exit_code=0；0 警告，0 错误。
```

验证中遇到的问题：

```text
真实自检窗口触发弹窗：
    标题：鱼巢 - 自检需求入树逻辑错误
    正文锚点：自检需求入树裁决：候选不进入当前普通需求树

日志位置：
    日志/鱼巢_exception_20260618.低值g
        13:38:02.791 记录正文；
        13:38:02.792 记录弹窗错误提示。

处理记录：
    计划/追根因_20260618_自检需求入树逻辑错误_v0.1.md
```

弹窗根因修复结果：

```text
根因：
    `私有_裁决自检候选父需求_线程侧`
    漏掉 `方法状态 -> 方法维护` 父域映射。

修改：
    自我线程模块.impl.cpp
        新增 `私有_目标特征是方法维护字段_线程侧`；
        `方法状态` 目标特征转入现有 `方法维护` 管理父域。

验证：
    python .\tools\生成函数功能说明文档.py
        exit_code=0；函数条目=7304。

    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
        exit_code=0；0 警告，0 错误。

    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
        exit_code=0；stderr_len=0；
        exception_before=5909；exception_after=5909。

    日志/鱼巢_run_20260618.低值g:1454
        自我线程/自检维护管理分支确保 | 域=方法维护 | 管理父需求=6。

日志/鱼巢_run_20260618.低值g:1456
        自我线程/自检正式需求入树 | 父需求=6 | 父域=方法维护 | 新需求=7。
```

## 8. P4d 控制面板 OR 组令牌只读视图删除执行前断点

当前切片：

```text
删除 `控制面板类.cpp::结构_OR组令牌只读视图_控制面板`。
```

判定依据：

```text
`结构_OR组令牌只读视图_控制面板` 只在控制面板内部承载两个显示字符串：
    状态文本；
    当前激活路径。

真实业务读取来自世界树特征：
    `OR组结算令牌状态`；
    `OR组领取路径`。

该结构体不写入存在 / 场景 / 特征 / 状态 / 动态 / 方法 / 需求，
没有外部读取，也不承担状态或动态证据。
```

最小实现：

```text
1. 删除 `结构_OR组令牌只读视图_控制面板`。
2. 删除 `私有_读取OR组令牌只读视图_控制面板`。
3. 新增两个功能函数：
       `私有_OR组令牌状态文本_控制面板`；
       `私有_OR组令牌当前激活路径文本_控制面板`。
4. 三个控制面板调用点先读取令牌记录，再分别调用两个显示文本函数。
```

不做范围：

```text
不改 OR 组令牌记录的世界树特征写入路径。
不把显示文本作为机器证据。
不改 `自我动作实现模块.ixx` 中 OR 组令牌动作动态或特征定义。
```

预测验证：

```text
1. 源码中 `结构_OR组令牌只读视图_控制面板`、
   `私有_读取OR组令牌只读视图_控制面板` 和 `令牌视图.` 引用为 0。
2. 函数说明文档刷新后新增两个功能函数，旧函数条目消失。
3. 结构体清单删除该结构体，结构体总数从 537 降到 536。
4. Debug x64 串行构建应通过。
```

## 9. P4d 执行结果

已改代码路径：

```text
控制面板类.cpp
    删除 `结构_OR组令牌只读视图_控制面板`。
    删除 `私有_读取OR组令牌只读视图_控制面板`。
    新增 `私有_OR组令牌状态文本_控制面板`。
    新增 `私有_OR组令牌当前激活路径文本_控制面板`。
    三个控制面板调用点改为先读取 OR 组令牌记录，
        再分别生成状态文本和当前激活路径文本。

说明书/函数功能说明文档.md
    已由脚本刷新。
```

清单变化：

```text
结构体清单：536。
含文本字段结构体候选：298。
```

验证：

```text
rg "结构_OR组令牌只读视图_控制面板|私有_读取OR组令牌只读视图_控制面板|令牌视图\\." -S -g "*.cpp" -g "*.h" -g "*.ixx"
    结果：源码引用 0。

python .\tools\生成函数功能说明文档.py
    结果：exit_code=0；函数条目=7305。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    结果：exit_code=0；0 警告，0 错误。
```

仍未宣称：

```text
不宣称结构体清理全部完成；
不宣称项目结构闭环全部通过；
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
