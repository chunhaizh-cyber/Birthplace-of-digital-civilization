# S6-N2a-P6 真实运行日志验证记录

日期：2026-06-02

## 1. 预测

验证命令：

```powershell
.\x64\Debug\鱼巢.exe --self-check-log
.\x64\Debug\鱼巢.exe --task-mgmt-summary
.\x64\Debug\鱼巢.exe --demand-tree-growth-summary
```

预期：

```text
1. 三条命令应走真实初始化 / 线程 / 摘要路径并正常退出。
2. 如果当前自然链路已生成结构化外设子需求，应能在日志中看到：
       任务管理界面线程/自动创建外设观察等待项
       结构化口径=识别 / 扫描 / 跟踪
       外设观察承接
       自我_识别外设观察材料 / 自我_扫描已识别区域 / 自我_跟踪指定存在
       提交_观察存在特征值变化事实 / 提交_指定存在跟踪事实
       安全评估本能方法结果 | 评估证据闭合=...
3. 如果自然链路尚未触发这些项，应记录真实停点：
       外设等待项未生成；
       外设报告未命中；
       自我组织方法未被调度；
       提交入口未被调度；
       安全评估仍因缺已入账扫描变化事实而条件不足。
4. 严禁通过构造样本、人工造来源需求或直接调用筹办函数补齐验证。
```

## 2. 实际结果

运行命令：

```powershell
.\x64\Debug\鱼巢.exe --self-check-log *> 运行输出\20260602_S6_N2a_P6_self_check.log
.\x64\Debug\鱼巢.exe --task-mgmt-summary *> 运行输出\20260602_S6_N2a_P6_task_mgmt_summary.log
.\x64\Debug\鱼巢.exe --demand-tree-growth-summary *> 运行输出\20260602_S6_N2a_P6_demand_growth.log
```

退出码：

```text
--self-check-log                -1073741819
--task-mgmt-summary             0
--demand-tree-growth-summary    0
```

结论：

```text
P6 未通过。

真实运行已经进入自我线程 / 任务管理 / D455 外设线程路径，
但 --self-check-log 在退出阶段发生 Windows 访问冲突：
    -1073741819

因此不能把本轮记录为真实链路验收通过。
```

## 3. 已确认事实

### 3.1 D455 材料和报告不是当前堵点

最新真实运行日志 `日志/鱼巢_run_20260602.低值g` 显示：

```text
外设线程_D455深度相机/逐簇识别报告入队
外设线程_D455深度相机/扫描变化报告入队
类型=扫描变化报告
等待项ID=1
短期观察材料页提交=1
ROI / 掩码 / 深度 / 空间候选 / 像素归属账本材料字段存在
```

这说明 D455 材料包、识别报告、扫描报告和等待项关联已经在真实线程中出现。

### 3.2 等待项选择没有恢复文本扫描

最新真实运行日志显示：

```text
任务管理界面线程/自动创建外设观察等待项
结构化口径=扫描
读取目标特征词=0

任务管理界面线程/外设观察等待项未生成
结构化口径=未映射
读取目标特征词=0
```

这说明 P4 后任务管理等待项选择按目标特征节点映射结构化口径。
未映射目标被暴露为缺口，没有用目标特征词面兜底。

### 3.3 当前自然链暴露的结构化目标缺口

运行中仍出现未映射结构化目标：

```text
目标特征主键=VZ
目标特征主键=Wj
目标特征主键=Wm
结构化口径=未映射
读取目标特征词=0
```

这些目标不能在任务管理中通过文本补丁直接映射为外设观察等待项。
下一步必须追溯其正式目标特征定义、需求侧条件、因果条件或提交入口生成来源，
确认应由哪一类结构化子需求或提交入口承接。

## 4. 阻塞点

### 4.1 第一阻塞：自检命令退出访问冲突

`--self-check-log` 输出了自检摘要，但进程退出码为 `-1073741819`。
日志末段显示退出守卫正在停止自检线程和自我线程：

```text
程序退出/守卫 | 开始停止自检线程
程序退出/守卫 | 自检线程已停止
程序退出/守卫 | 开始停止自我线程
自我线程/停止 | 等待工作线程join开始
自我线程/主循环 | 退出线程函数
自我线程/停止 | 等待工作线程join完成
程序退出/守卫 | 自我线程已停止
```

当前没有日志记录到访问冲突发生点。
必须先排查退出守卫、线程停止顺序、D455 外设线程停止、任务管理界面线程停止和静态析构阶段。

### 4.2 第二阻塞：未映射结构化目标仍需要正式承接

真实日志已证明总目标不会被直接映射为 D455 等待项：

```text
原因=当前场景特征值汇总目标不直接映射D455等待项
需要先拆成结构化材料型/确认型/扫描型/跟踪型子需求
读取目标特征词=0
```

但后续自然链中仍有若干目标特征缺结构化外设观察运行模式映射。
这不是恢复文本扫描的理由，而是需要补正式目标定义 / 获取途径 / 子需求生长 / 提交入口承接的证据。

## 5. 下一步

```text
1. 优先建立 S6-N2a-P6a：
       排查 --self-check-log 退出访问冲突。
       目标是让真实运行验证入口稳定退出。

2. 访问冲突未解决前：
       不把 S6-N2a-P6 记为通过；
       不继续扩大解释为业务链已验收；
       不提交安全评估已闭合结论。

3. 访问冲突解决后：
       继续核对结构化目标 VZ / Wj / Wm 的来源和承接方式；
       只允许按正式目标特征、因果条件、需求侧条件、子需求生长或提交入口补链；
       禁止恢复目标特征文本扫描。
```
