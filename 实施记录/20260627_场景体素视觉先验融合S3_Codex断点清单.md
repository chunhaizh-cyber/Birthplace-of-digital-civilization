# 20260627 场景体素视觉先验融合 S3 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260625_场景体素视觉先验融合实现计划_v0.1.md` / S3 视觉观察融合模块。

S0-S2 已完成：

```text
S0：规范准入，提交 91deceb。
S1/S2：场景体素只读快照模块和结构化缺口，提交 532684f。
```

S3 目标：

```text
新增 `视觉观察融合模块.ixx`；
输入合法外设材料、目标观察约束和 SceneVoxelPrior；
输出 `结构_视觉观察域结果包`；
生成先验匹配评分、深度一致评分、自由空间冲突候选和信息缺口；
不直接确认事实，不写世界树，不接入 6145 / 6146 / 6147 / 6150。
```

## 1. 范围修正

原计划 S3 允许修改范围缺少 `鱼巢.vcxproj` / `鱼巢.vcxproj.filters`。新增模块若不接入项目文件，`msbuild` 无法验证该模块。

本切片允许修改范围修正为：

```text
视觉观察融合模块.ixx
鱼巢.vcxproj
鱼巢.vcxproj.filters
说明书/函数功能说明文档.md
实施记录/20260627_场景体素视觉先验融合S3_Codex断点清单.md
计划/20260625_场景体素视觉先验融合实现计划_v0.1.md
计划/计划索引.md
```

禁止修改：

```text
6145 / 6146 / 6147 / 6150 业务入口
扫描入账逻辑
提交入口裁决逻辑
外设线程
世界树 / 需求树 / 动作动态写入逻辑
```

## 2. 前置核查

```text
未发现现成 `视觉观察融合模块`、`结构_视觉观察域结果包`、`结构_视觉先验匹配评分`、`结构_视觉自由空间冲突候选` 或 `结构_视觉信息缺口`。
`结构_已合法外设数据项` 是 `自我动作实现.扫描.ixx` 内部结构，不应被 S3 新模块直接依赖。
S3 应定义公开运行期合法外设材料项，后续 S4 再由扫描模块适配私有数据项。
```

## 3. 预测

```text
新增模块后，rg 能命中四个 S3 验收结构名；
禁止字符串机器状态、JSON、fallback、兼容等模式；
Debug x64 构建通过；
结果包仍是观察域候选 / 评分 / 缺口，不证明事实入账或业务闭合。
```

## 4. 待验证命令

```powershell
rg -n "结构_视觉观察域结果包|结构_视觉先验匹配评分|结构_视觉自由空间冲突候选|结构_视觉信息缺口" -g "*.ixx" -g "*.h" -g "*.cpp"
rg -n "std::string.*状态|std::string.*类型|if \(.*== \"已|if \(.*== \"未|JSON|fallback|兼容" 视觉观察融合模块.ixx
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

## 5. 本轮修改

```text
新增：视觉观察融合模块.ixx
修改：鱼巢.vcxproj
修改：鱼巢.vcxproj.filters
修改：说明书/函数功能说明文档.md
修改：实施记录/20260627_场景体素视觉先验融合S3_Codex断点清单.md
```

模块输出：

```text
结构_视觉融合目标约束摘要
结构_视觉融合合法外设材料项
结构_视觉先验匹配评分
结构_视觉自由空间冲突候选
结构_视觉信息缺口
结构_视觉观察候选
结构_视觉观察域结果包
结构_视觉融合输入
视觉观察融合类
```

已提供入口：

```text
视觉观察融合类::从扫描提交包提取合法外设材料
视觉观察融合类::融合观察域结果
```

## 6. 验证结果

实际命令：

```powershell
rg -n "结构_视觉观察域结果包|结构_视觉先验匹配评分|结构_视觉自由空间冲突候选|结构_视觉信息缺口" -g "*.ixx" -g "*.h" -g "*.cpp"
rg -n "std::string.*状态|std::string.*类型|if \(.*== \"已|if \(.*== \"未|JSON|fallback|兼容" 视觉观察融合模块.ixx
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
git diff --check
```

结果：

```text
结构名 rg 命中 `视觉观察融合模块.ixx` 中四个 S3 验收结构。
禁止模式 rg 无匹配；退出码 1 符合“无匹配”预期。

函数功能说明文档生成退出码 0。
源码文件：129
函数条目：7262

Debug x64 构建退出码 0。
0 个警告，0 个错误。

git diff --check 退出码 0。
仅提示 说明书/函数功能说明文档.md、鱼巢.vcxproj、鱼巢.vcxproj.filters 后续被 Git 触碰时 CRLF 将转 LF；无 whitespace error。
```

结论：

```text
S3 视觉观察融合模块已完成工程守卫验证。
S3 不接入 6145 / 6146 / 6147 / 6150，不证明观察事实入账或业务闭合。
下一切片进入 S4：6146 扫描先接入。
```
