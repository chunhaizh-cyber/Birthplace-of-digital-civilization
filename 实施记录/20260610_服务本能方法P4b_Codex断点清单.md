# 服务本能方法 P4b Codex断点清单

日期：2026-06-10

## 当前切片

```text
服务本能方法实现计划 P4b：
    D0 引用读取与合法性校验 helper。
```

## 依据文件

```text
AGENTS.md
规范/规范目录.md
计划/计划索引.md
实施记录/20260610_服务本能方法实现方案记录.md
实施记录/20260610_服务本能方法P4a_Codex断点清单.md
```

## 允许修改范围

```text
自我动作实现.服务模块.ixx
实施记录/20260610_服务本能方法P4b_Codex断点清单.md
计划/计划索引.md
```

## 禁止修改范围

```text
任务主信息类
需求主信息类
特征值主信息类
任务筹办 / 任务执行 本能方法化
服务事件E独立虚拟存在
裸写服务值
G0 / D2 结算实现
用“方法返回成功”伪造 D0
```

## 进入时工作区状态

```text
P0/P1 已提交：8c3673a
P2 已提交：aa2ca33
P3 已提交：1dd4662
P4a 已提交：7d59ece
存在提交前已有无关修改：
    AGENTS.md
    任务模块.管理工作线程.ixx
    控制面板.html
    实施记录/20260610_观察子任务完成推进_Codex断点清单.md
    消息中间件/

本切片不得混入上述文件。
```

## 预期结果

```text
1. 服务模块提供从任务虚拟存在 / 方法运行存在读取 D0 引用的 helper。
2. 服务模块提供 D0 基础合法性校验：
   自我动作主体、服务动作承受主体、方法运行存在、输入场景、输出场景、目标特征、初始状态、结果状态。
3. 校验结果只输出缺口与布尔判断，不生成成功服务结论。
4. 不写服务值，不生成 G0 / D2。
5. Debug x64 构建通过。
```

## 已运行命令

```text
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\鱼巢_build_20260610_服务模块P4b.log
```

## 关键退出码

```text
Debug x64 构建：ExitCode=0
关键错误检索：未检出 error C / fatal error / MSB / 错误。
```

## 已完成修改

```text
1. 新增 D0 专用引用读取 helper：
   从任务虚拟存在读取服务业务动作动态 / D0服务业务动作动态 / 方法调用动作动态 / 来源动作动态；
   从方法运行存在读取同类引用，并允许读取方法运行存在上的通用动作动态。

2. 新增方法运行存在读取 helper：
   优先读取任务虚拟存在中的 方法运行存在 / 当前方法运行存在。

3. 新增 D0 基础合法性校验 helper：
   校验 D0 存在、D0主信息存在、动作主体为自我、承受主体存在、来源动作存在、
   方法运行存在、输入场景、输出场景、目标特征、初始状态、结果状态。

4. 新增 D0 校验回写 helper：
   只回写布尔校验项、D0 引用和第一缺口；
   不生成成功服务判断，不生成 G0 / D2，不写服务值。
```

## 当前判断

```text
P4b 已闭合；服务模块已经具备 D0 引用读取与基础合法性校验 helper。
下一步进入 P5a：成功服务治理视图 G0 的宿主/字段骨架与硬门槛三值状态 helper。
```
