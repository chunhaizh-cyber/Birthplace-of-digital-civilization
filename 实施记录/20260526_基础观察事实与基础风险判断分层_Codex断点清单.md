# 基础观察事实与基础风险判断分层 Codex 断点清单

## 当前切片

R1/R2 方法级逐项执行中。第 1 项 `双目相机_取得当前观察特征帧`、第 2 项 `自我_建立观察帧存在`、第 3 项 `自我_建立像素观察事实索引`、第 4 项 `自我_承接像素特征值` 和第 5 项 `自我_校验观察事实完备性` 已完成；第 6 项 `自我_评估观察帧质量` 已构建并产出动作动态。最新口径收紧为：深度有效率 / 空间坐标有效率低于阈值只记录用途相关质量缺口，不推出整帧不可用，也不由 S1 或子任务裁决父任务是否阻塞。R2 已收口任务筹办边界：任务筹办只找路径、方法和条件，不再按观察质量 / 基础观察事实 / 基础风险领域词注入需求侧能力；需求侧前置声明已补，安全根能自然生成 `当前观察帧.基础观察事实可用状态 等于 可用` 子需求。该子需求已能进入方法 n / 本能ID=6125 执行，并在缺观察材料时派生 `当前观察特征帧取得状态 等于 已取得` 子需求。R3 已把该观察取得子需求接入 D455 外设等待项 / 观察报告队列 / 任务管理承接请求 / 自我观察事实承接，并完成 C4.5、C4.6-1、C5-3a、C5-3b、C5-3c、C4.7、C4.6-2、C6/C7 当前轮实现验证；R4 已补关键特征变化观察和安全因果候选构造两个安全对象作用方法；R5g 已把因果信息收束为被动获取：未收到负向因果证据时默认良好，安全评估输出 `I64MAX` 场景评估安全值，不派生持续观察、负向处置或降级保护链，且仍不结算自我安全值；R5h 已补安全评估明确项可比较状态节点，当前停止在自然来源需求驱动的回写验证前。

## 远程执行状态

```text
R0 已完成。
R1 第 1-8 项已完成；观察质量补救方法族 S0 已完成，S1 新口径代码已修订；观察质量事实到父需求依赖裁决通用计划首轮代码作为历史实现保留，后续需按新增边界迁移为需求侧 / 因果侧数据驱动。
R2 已完成通用目标化代码、任务筹办边界收口和需求侧前置声明；基础观察事实可用前置需求已能自然生成并执行到缺观察材料派生，后续需让 `当前观察特征帧取得状态 == 已取得` 由相机外设线程承接。
R3 C0-C4 已完成首轮真实链路验证。
R4 已完成当前轮最小安全前置桥。
R5g 因果被动获取、无负向因果默认良好场景评估结果提交已完成当前轮验证；R5h 安全评估结果可比较状态补齐已完成；风险安全值随时间投影仍待后续被动归因证据样本。
```

## 项目最近已知状态

```text
050003 基础观察事实可用与基础风险判断分层规范已建立。
040006 枚举型实例特征值合法来源规范已建立。
任务筹办因果路径筹办阶段已完成 P6 当前闸门。
枚举型实例特征值生产者分级已完成 E2 入树校验。
双目相机_取得当前观察特征帧 已能以方法动作动态生产 当前观察特征帧取得状态。
自我_建立观察帧存在 已能以方法动作动态生产 观察帧容器状态，并写入兼容的 当前观察帧存在状态。
自我_建立像素观察事实索引 已能以方法动作动态生产 像素覆盖状态，并写入像素观察存在数量、缺失像素数量、重复映射数量和两种回查可用状态。
自我_承接像素特征值 已能以方法动作动态生产 像素特征值承接状态，并写入颜色、深度有效性、深度值和空间坐标承接摘要。
自我_校验观察事实完备性 已能以方法动作动态生产 观察事实完备状态，并记录完备性失败条件数量和摘要。
自我_评估观察帧质量 已登记并构建通过，真实运行产出动作动态；当前真实帧低于深度 / 空间坐标质量阈值，只能作为依赖深度 / 空间坐标的后续处理质量缺口，不得直接判定整帧不可用。
观察质量补救方法族计划已重切；S1 第一版中的可用观察材料目标线索、S1 补观察需求建议和父任务继续旁路只作为历史证据。后续正式口径为：S1 仅写用途相关质量事实、缺口事实和线索；观察质量 / 基础观察事实 / 风险前置关系必须由需求侧条件、方法条件、条件结果对、因果模板或既有子需求提供，任务筹办只负责发现这些已声明路径。6119-6124 占位方法不得写 质量可用状态=可用、基础观察事实可用状态=可用或 基础风险判断状态=已明确。
当前尚未完成全部基础观察事实可用前置方法。
当前尚未实现基础风险必要因子统计或基础风险已明确判定。
```

## 已完成事项

- [x] R0：规范和计划定案。
- [x] R0：明确基础观察事实可用不等于基础风险已明确。
- [x] R0：明确枚举型实例特征值必须由合法生产者写入。
- [x] R0：计划索引已将 R1/R2 设为当前主动切片。
- [x] R1-1：双目相机_取得当前观察特征帧 已闭合 当前观察特征帧取得状态 的方法结果能力、动作动态和日志证据。
- [x] R1-2：自我_建立观察帧存在 已闭合 观察帧容器状态 / 当前观察帧存在状态 的方法结果能力、动作动态和日志证据。
- [x] R1-3：自我_建立像素观察事实索引 已闭合 像素覆盖状态、像素观察存在数量、缺失像素数量、重复映射数量和回查可用状态的方法结果能力、动作动态和日志证据。

## 修改文件

```text
规范/基础观察事实可用与基础风险判断分层规范20260526.md
规范/枚举型实例特征值合法来源规范20260526.md
规范/规范目录.md
计划/20260526_基础观察事实与基础风险判断分层实施计划_v0.1.md
计划/20260527_R1本能方法_双目相机取得当前观察特征帧实施计划_v0.1.md
计划/20260527_R1本能方法_自我建立观察帧存在实施计划_v0.1.md
计划/20260527_R1本能方法_自我建立像素观察事实索引实施计划_v0.1.md
计划/计划索引.md
实施记录/20260526_基础观察事实与基础风险判断分层_Codex断点清单.md
日志/20260527_R1_双目相机取得当前观察特征帧_验证预测.txt
日志/20260527_R1_自我建立观察帧存在_验证预测.txt
日志/20260527_R1_自我建立像素观察事实索引_验证预测.txt
自我动作实现.外设模块.ixx
自我动作实现.内部模块.ixx
自我动作实现.外部模块.ixx
自我动作实现模块.ixx
本能方法类.h
本能方法类.cpp
鱼巢.cpp
计划/20260527_观察质量补救本能方法族实施计划_v0.1.md
计划/20260527_R1补观察质量_自我诊断观察质量缺口并派生补观察需求实施计划_v0.1.md
计划/20260527_R1补观察质量_自我调整相机参数并重采样实施计划_v0.1.md
计划/20260527_R1补观察质量_自我按ROI重算观察质量实施计划_v0.1.md
计划/20260527_R1补观察质量_自我执行多帧重采样并择优实施计划_v0.1.md
计划/20260527_R1补观察质量_自我生成低置信补偿观察候选实施计划_v0.1.md
计划/20260527_R1补观察质量_自我调整外设观察姿态实施计划_v0.1.md
计划/20260527_R1补观察质量_自我调整观察位置实施计划_v0.1.md
```

## 验收证据

```text
R1-1 已先写预测，再真实运行并核对日志、状态快照、动作动态或错误日志。
Debug x64 构建通过：0 错误，1 个 LNK4075 增量链接警告。
真实入口：.\x64\Debug\鱼巢.exe --self-scene-html
日志证据：日志/鱼巢_run_20260527.低值g
关键字段：
    方法=双目相机_取得当前观察特征帧
    成功=1
    输出特征=当前观察特征帧取得状态
    动作动态=1eV
    当前观察特征帧取得状态=1
越界检查：
    基础风险判断状态=0
    基础风险判断状态=已明确=0
    直接写需求树=1=0
    安全值结算=0
    服务值结算=0

R1-2 已先写预测，再真实运行并核对日志、状态快照、动作动态或错误日志。
Debug x64 构建通过：0 错误，1 个 LNK4075 增量链接警告。
真实入口：.\x64\Debug\鱼巢.exe --self-scene-html
日志证据：日志/鱼巢_run_20260527.低值g
关键字段：
    方法=自我_建立观察帧存在
    成功=1
    输出特征=观察帧容器状态
    动作动态=3Nb
    宽度=640
    高度=480
    深度帧号=14
    彩色帧号=13
    预期像素数量=307200
    当前观察特征帧取得状态=1
    观察帧容器状态=1
越界检查：
    基础风险判断状态=0
    基础风险判断状态=已明确=0
    直接写需求树=1=0
    安全值结算=0
    服务值结算=0

R1-3 已先写预测，再真实运行并核对日志、状态快照、动作动态或错误日志。
Debug x64 构建通过：0 错误，1 个 LNK4075 增量链接警告。
真实入口：.\x64\Debug\鱼巢.exe --self-scene-html
日志证据：日志/鱼巢_run_20260527.低值g
关键字段：
    方法=自我_建立像素观察事实索引
    成功=1
    输出特征=像素覆盖状态
    动作动态=33n
    预期像素数量=307200
    像素观察存在数量=307200
    缺失像素数量=0
    重复映射数量=0
    像素覆盖状态=1
    坐标回查可用状态=1
    帧内编号回查可用状态=1
越界检查：
    基础风险判断状态=0
    基础风险判断状态=已明确=0
    直接写需求树=1=0
    安全值结算=0
    服务值结算=0

R1-4 已先写预测，再真实运行并核对日志、状态快照、动作动态或错误日志。
Debug x64 构建通过：0 错误，1 个 LNK4075 增量链接警告。
真实入口：.\x64\Debug\鱼巢.exe --self-scene-html
日志证据：日志/鱼巢_run_20260527.低值g
关键字段：
    方法=自我_承接像素特征值
    成功=1
    输出特征=像素特征值承接状态
    动作动态=35J
    预期像素数量=307200
    像素观察存在数量=307200
    缺失像素数量=0
    重复映射数量=0
    像素覆盖状态=1
    颜色像素数量=307200
    深度有效像素数量=228216
    点云有效像素数量=228216
    颜色RGB结构状态=1
    原始深度毫米结构状态=1
    深度有效性Mask状态=1
    空间坐标毫米XYZ结构状态=1
    像素特征值承接状态=1
越界检查：
    基础风险判断状态=0
    基础风险判断状态=已明确=0
    直接写需求树=1=0
    安全值结算=0
    服务值结算=0

R1-5 已先写预测，再真实运行并核对日志、状态快照、动作动态或错误日志。
Debug x64 构建通过：0 错误，1 个 LNK4075 增量链接警告。
真实入口：.\x64\Debug\鱼巢.exe --self-scene-html
日志证据：日志/鱼巢_run_20260527.低值g
关键字段：
    方法=自我_校验观察事实完备性
    成功=1
    输出特征=观察事实完备状态
    动作动态=3Nx
    预期像素数量=307200
    像素观察存在数量=307200
    缺失像素数量=0
    重复映射数量=0
    像素覆盖状态=1
    像素特征值承接状态=1
    坐标回查可用状态=1
    帧内编号回查可用状态=1
    观察事实完备状态=1
    观察事实完备失败条件数量=0
    观察事实完备失败原因摘要=无
越界检查：
    基础风险判断状态=0
    基础风险判断状态=已明确=0
    直接写需求树=1=0
    安全值结算=0
    服务值结算=0

R1-6 已先写预测，再真实运行并核对日志、状态快照、动作动态或错误日志；本轮遇到真实质量阈值不足，暂不闭合。
Debug x64 构建通过：0 错误，1 个 LNK4075 增量链接警告。
真实入口：.\x64\Debug\鱼巢.exe --self-scene-html
日志证据：日志/鱼巢_run_20260527.低值g
关键字段：
    方法=自我_评估观察帧质量
    成功=0
    输出特征=质量可用状态
    动作动态=4eZ
    质量可用状态=0
    质量失败条件数量=2
    质量失败原因摘要=深度有效率不足;空间坐标有效率不足
    质量最低帧质量评分=5000
    质量最低深度有效率=5000
    质量最低空间坐标有效率=5000
    观察帧质量诊断摘要=1
    帧质量评分=5545
    深度有效率=4357
    空间坐标有效率=4357
    融合深度有效率=7447
    当前观察新鲜度状态=1
    基础观察事实可用状态=0
    基础观察事实失败条件数量=1
越界检查：
    基础风险判断状态=0
    基础风险判断状态=已明确=0
    直接写需求树=1=0
    安全值结算=0
    服务值结算=0
    补全深度覆盖原始深度=0

观察质量补救方法族 S0 已落地：
    6118-6124 已登记占位方法头、占位规格和占位动作入口。
    占位执行返回失败回执 / 条件不足 / 待实现，输出特征限定为 补观察缺口状态，不写 质量可用状态=可用。
    未插入 `--self-scene-html` 自然观察链，不作为质量改善验收证据。
    git diff --check 通过，仅有既有换行符提示。
    Debug x64 构建通过：0 错误，1 个 LNK4075 增量链接警告。

观察质量补救方法族 S1 已落地：
    预测文件：日志/20260527_S1_自我诊断观察质量缺口_验证预测.txt
    实现入口：自我动作实现.外设模块.ixx::自我诊断观察质量缺口并派生补观察需求
    自然触发：鱼巢.cpp `--self-scene-html` 序列中 `自我_评估观察帧质量` 之后。
    git diff --check 通过，仅有既有换行符提示。
    Debug x64 构建通过：0 错误，1 个 LNK4075 增量链接警告。
    真实入口：.\x64\Debug\鱼巢.exe --self-scene-html
    真实结果：退出码 0，控制面板 HTML 已生成。
    日志证据：日志/鱼巢_run_20260527.低值g 第 649-650 行。
    关键字段：
        方法=自我_诊断观察质量缺口并派生补观察需求
        成功=1
        当前帧质量可用状态=0
        质量失败条件数量=2
        质量失败原因摘要=深度有效率不足;空间坐标有效率不足
        深度有效率=4293
        空间坐标有效率=4293
        当前帧不可用事实保留=1
        可用观察材料目标=当前观察材料.质量可用状态 等于 可用
        说明=以上两项属于 S1 第一版历史输出，后续不得作为最终口径；应改为用途相关质量缺口事实和新观察材料目标。
        临时因果视图线索=1
        新增候选方法召回线索=调整双目相机工作模式;内部世界细分
        已知路径派生候选=补观察需求建议
        未知路径学习缺口线索=观察质量改善路径待验证
    越界检查：
        直接写需求树=0
        写质量可用状态=0
        写基础观察事实可用状态=0
        写基础风险判断状态已明确=0
```

## 未完成事项

```text
R1-6 自我_评估观察帧质量：当前真实帧深度有效率和空间坐标有效率低于 5000；新口径下该事实不关闭整帧，只进入用途相关质量缺口。
S1 已修订为观察质量诊断事实入账器：
    写入当前观察帧.深度有效率 / 空间坐标有效率实际值；
    写入当前观察帧.深度观察质量状态 == 不足；
    写入当前观察帧.空间坐标观察质量状态 == 不足；
    写入当前观察帧.质量缺口状态 == 存在；
    写入当前观察帧.质量缺口原因集合包含 深度有效率不足 / 空间坐标有效率不足；
    写入当前观察帧.空间类后续处理适用状态 == 降级可用 或 不可用；
    可选写入观察质量改善线索状态，但不得直接生成正式需求、正式子任务、任务筹办结果或任务执行结果。
任务筹办已按通用父需求依赖裁决完成首轮重构：
    输入为当前观察帧质量事实、父需求所需观察特征集合、父需求降级继续允许状态和世界树因果路径；
    父需求依赖不足特征且不允许降级时，裁决为硬阻塞 / 等待子需求；
    父需求允许低置信继续时，裁决为软阻塞 / 降级继续；
    父需求不依赖不足特征时，裁决为旁路补救 / 并行补救；
    质量提升派生目标必须指向新观察材料 / 后续观察帧 / 融合观察帧，不得指向旧观察帧；
    不得把派生子需求字段解释成父需求阻塞裁决来源；
    不得按 S1 名称特判任务筹办或任务执行逻辑。
已完成构建证据：
    Debug x64 构建通过：0 错误，1 个 LNK4075 增量链接警告。
已完成真实运行证据：
    预测文件：日志/20260527_S1新口径_观察质量事实与父需求裁决_验证预测.txt
    运行入口：.\x64\Debug\鱼巢.exe --self-scene-html
    退出码：0
    控制面板输出：控制面板.html 已生成
    日志窗口：2026-05-27 14:26
    已出现：观察质量诊断事实入账、质量诊断状态=1、深度观察质量状态、空间坐标观察质量状态、质量缺口状态=1、观察质量改善线索状态=1、正式需求入树=0、父需求阻塞裁决=0、补观察需求建议=0。
    未出现：同一时间窗口未出现 当前帧不可用事实保留 / 可用观察材料目标 / 已知路径派生候选 / 父任务继续=1 等旧口径标记。
    本次真实帧：深度有效率=5016、空间坐标有效率=5016，高于 5000。
    预测文件：日志/20260527_S1低阈值和父需求裁决_第二次验证预测.txt
    运行入口：.\x64\Debug\鱼巢.exe --self-scene-html
    退出码：0
    日志窗口：2026-05-27 15:28
    已出现：观察质量诊断事实入账、质量诊断状态=1、深度观察质量状态=1、空间坐标观察质量状态=1、质量缺口状态=1、正式需求入树=0、父需求阻塞裁决=0。
    本次真实帧：深度有效率=5041、空间坐标有效率=5041，高于 5000。
    预测文件：日志/20260527_任务管理摘要_观察质量父需求裁决_验证预测.txt
    运行入口：.\x64\Debug\鱼巢.exe --task-mgmt-summary
    退出码：0
    日志窗口：2026-05-27 15:31
    已出现：任务管理派生需求消息 满足关系=2；自我线程/派生需求入树后父任务策略交回重筹办；直接置父任务等待=0；派生消息携带父筹办阻塞关系=是；直接置父任务等待=否。
    限制：本次任务管理自然链路命中的是普通学习缺口，临时视图统计显示 父需求依赖深度空间=0，尚未覆盖观察质量父需求依赖裁决专用路径。
已移入待完成项：
    计划/待完成项/20260527_观察质量低阈值与父需求裁决自然复验计划_v0.1.md
    内容：历史内容曾要求任务筹办读取通用质量事实和父需求目标特征并派生新观察材料质量提升；新增上位边界下，该关系需迁移为需求侧条件 / 方法条件 / 条件结果对 / 因果模板 / 既有子需求数据，任务筹办只消费已声明路径。
    恢复条件：真实日志出现 深度有效率 < 5000、空间坐标有效率 < 5000，且已有需求侧 / 因果侧前置声明可供任务筹办通用路径发现。
已完成：
    R1-7 当前观察帧新鲜度判定规则与合法生产者收口。
    口径：不登记 自我_判断当前观察帧新鲜度 独立本能方法；新鲜度作为观察材料承接和基础观察事实可用性判定中的时效证据。
    代码证据：双目相机调用结果携带系统到达时间 / 设备时间 / 时间域；D455 报告摘要写入时间戳毫秒；当前观察帧摘要写入采集时间、观察年龄、最大允许观察年龄、判定时间、失效状态、来源外设状态、新鲜度失败原因和摘要。
    预测文件：日志/20260529_R1当前观察帧新鲜度判定_验证预测.txt
    构建：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m，0 错误，1 个 LNK4075 增量链接警告。
    真实入口：.\x64\Debug\鱼巢.exe --self-scene-html，退出码 0。
    日志证据：日志/鱼巢_run_20260529.低值g 中 双目相机_取得当前观察特征帧 写入 当前观察帧采集时间=245632557136、当前观察帧观察年龄=501449、最大允许观察年龄=120000000、观察帧来源外设状态=1、当前观察新鲜度状态=1、新鲜度失败原因=0；双目相机_使达到可用状态 因 缺采集时间;来源外设不可追溯 写入 当前观察新鲜度状态=0。
    边界证据：最新运行段未出现 方法=自我_判断当前观察帧新鲜度、基础风险判断状态=已明确、安全值结算、服务值结算。
已完成：
    R1-8 自我_判断基础观察事实可用性。
    口径：读取已落账观察摘要、观察事实完备性、质量可用性和新鲜度证据；由独立方法动作动态写入 基础观察事实可用状态，不推出基础风险判断已明确。
    代码证据：登记 本能方法ID=6125；补齐默认种子、规格确认、内部 / 外部动作桥接、--self-scene-html 链路和日志字段 基础观察事实失败原因摘要。
    预测文件：日志/20260529_R1自我判断基础观察事实可用性_验证预测.txt
    构建：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m，0 错误，1 个 LNK4075 增量链接警告。
    真实入口：.\x64\Debug\鱼巢.exe --self-scene-html，退出码 0。
    日志证据：日志/鱼巢_run_20260529.低值g 第 106 行 方法=自我_判断基础观察事实可用性，动作动态=55T，输出特征=基础观察事实可用状态，当前观察帧采集时间=246723938980，当前观察帧观察年龄=8520351，最大允许观察年龄=120000000，观察帧来源外设状态=1，当前观察新鲜度状态=1，新鲜度失败原因=0，基础观察事实可用状态=1，基础观察事实失败条件数量=0，基础观察事实失败原因摘要=无。
    边界证据：最新运行段未出现 方法=自我_判断当前观察帧新鲜度、基础风险判断状态=已明确、直接写需求树=1、安全值结算、服务值结算。
已撤回的错误实现：
    R2 派生需求消息第一阶段目标化 曾尝试在 任务模块.筹办.ixx 内按 基础风险判断状态 / 风险状态明确状态 硬编码注入 当前观察帧.基础观察事实可用状态 等于 可用。
    撤回原因：任务筹办是通用筹办器，只负责找路径、找方法、找条件；不得把安全观察、基础观察事实或风险明确这类需求侧能力写成筹办器内部独立能力。
    当前代码状态：上述安全观察第一阶段硬编码注入已从 任务模块.筹办.ixx 撤回，因果路径查询壳恢复为通用路径：吸收因果视图 -> 通用学习缺口检查；观察质量父需求依赖裁决不再由查询壳主动注入。
    保留证据：此前真实入口 .\x64\Debug\鱼巢.exe --task-mgmt-summary / --gap-summary / --demand-tree-growth-summary 均退出码 0，且未出现 基础风险判断状态=已明确、基础风险判断状态已明确=1、直接写需求树=1、安全值结算、服务值结算。
    下一步：R2 仍未完成；必须先把“风险明确需要基础观察事实可用”表达为需求侧条件 / 条件结果对 / 因果视图数据，再让任务筹办器按通用机制找到条件缺口并派生需求。
已完成边界修正：
    R2 通用派生消息目标化第一步。
    代码证据：任务模块.筹办.ixx 新增通用状态比较路径项目标化逻辑，针对因果视图中的 因比较模板 / 反向因比较模板 解析当前状态、目标状态、目标宿主、目标特征和 等于 关系；未按 基础风险判断状态 / 风险状态明确状态 / 基础观察事实可用状态 写死任何安全观察领域规则。
    边界修正：因果路径查询壳不再主动调用 观察质量父需求依赖裁决 注入逻辑；观察质量、基础观察事实和风险前置关系必须由需求侧条件、方法条件、条件结果对、因果模板或既有子需求声明，任务筹办只消费已声明路径。
    预测文件：日志/20260529_R2派生需求消息第一阶段目标化_验证预测.txt。
    构建：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m，0 错误，1 个 LNK4075 增量链接警告。
    真实入口一：.\x64\Debug\鱼巢.exe --self-scene-html，退出码 0；用于确认观察链仍可真实运行，不作为 R2 任务筹办验收入口。
    真实入口二：.\x64\Debug\鱼巢.exe --demand-tree-growth-summary，退出码 0；最新日志窗口从 日志/鱼巢_run_20260529.低值g 第 4395 行之后开始。
    最新日志证据：因果路径筹办临时视图统计中 观察材料质量目标=0、父需求依赖深度空间=0；任务筹办生成 学习信息补齐状态 等于 已补齐 的通用派生消息草案，字段完整=1、直接写需求树=0；任务管理工作线程上报样本 实际发送派生需求消息=1；自我线程出现 派生需求落账开始。
    未完成证据：最新需求树摘要入口未出现 当前观察帧.基础观察事实可用状态 等于 可用 的派生消息；因果路径统计仍未出现 主果模板声明 / 主果严格命中 / 主果反向命中，说明缺少需求侧 / 因果侧前置声明数据，不能宣称 R2 目标闭合。
    边界证据：最新日志窗口未出现 基础风险判断状态、写基础风险判断状态已明确、直接写需求树=1、安全值结算、服务值结算、任务筹办/任务执行本能方法恢复。
已完成需求侧前置声明：
    R2b 安全根必要条件补口。
    代码证据：特征类.cpp 在抽象特征初始化中补齐观察相关布尔枚举值域，并在安全根必要条件集中新增 `安全.基础观察事实可用`，检查宿主 `当前观察帧`，检查 / 缺口目标特征 `基础观察事实可用状态`，目标状态 `可用`；自我线程模块.impl.cpp 将基础观察事实可用及其上游观察状态归入安全必要条件目标域。
    构建：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m，0 错误，1 个 LNK4075 增量链接警告。
    真实入口：.\x64\Debug\鱼巢.exe --self-check-log，退出码 0。
    日志证据：日志/鱼巢_run_20260529.低值g 第 706-710 行显示 自我线程/根特征必要条件定义读取 根特征=安全 条件数=8；第 710 行显示 条件=安全.基础观察事实可用，宿主=23o，检查特征=基础观察事实可用状态，目标特征=基础观察事实可用状态，当前值=0，目标值=1，满足关系=等于，缺口需求=取得基础观察事实。
    任务筹办证据：日志第 503-528 行显示任务=6 / 需求=6 为 基础观察事实可用状态；枚举目标校验 生产者分级=专用方法生产、比较符合法=1、值域命中=1、有生产者=1、合法=1；候选数=1，选择方法=n，本能ID=6125 已提交。
    边界证据：本次有效日志窗口未出现 基础风险判断状态、直接写需求树=1、安全值结算或服务值结算；任务筹办器仍只消费已声明前置关系。
    验证入口备注：本次先尝试 .\x64\Debug\鱼巢.exe --demand-tree-growth-summary，60 秒未退出且日志为 0 字节，已停止残留进程；该入口阻塞只作为命令入口问题记录，不作为业务验收失败。
    R2c 基础观察事实可用执行缺口三比较化。
    代码证据：自我动作实现.外设模块.ixx 在 `自我_判断基础观察事实可用性` 缺观察材料时写入宿主派生需求，目标主体为当前观察帧，缺口特征为 `当前观察特征帧取得状态`，当前值 0，目标值 1；任务模块.管理工作线程.ixx 不再把失败原因词单独当作派生需求，只有显式 `派生需求 + 当前状态 + 目标状态` 才允许构造执行缺失需求。
    构建：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m，0 错误，1 个 LNK4075 增量链接警告。
    真实入口：.\x64\Debug\鱼巢.exe --demand-tree-growth-summary，退出码 0。
    日志证据：日志/鱼巢_run_20260529.低值g 第 4802 行 `自我动作/宿主派生需求已写入`，来源 `自我_判断基础观察事实可用性`，目标主体=23o，特征类型=N5；第 4807 行 `任务管理工作线程/派生需求构造` 显示需求特征=N5、当前状态=3gx、目标状态=3hF、完整=1；第 4913 行自我线程裁决入树成功，新需求=h，需求类型=当前观察特征帧取得状态。
已完成安全评估对象作用方法首轮：
    R2-9 安全_评估当前场景安全性。
    口径：该方法是安全对象作用方法，不是筹办函数，也不是提交入口；它只读取当前场景原始事实、动态证据和因果信息，输出安全评估结果场景 / 缺口，不直接关闭需求、不写自我安全值、不写存在本体永久安全度。
    代码证据：已登记 本能方法ID=6126；补齐默认方法种子、分类 `安全对象作用方法`、规格确认、内部 / 外部动作桥接、`--self-scene-html` 调用链；`基础风险判断状态`、当前场景安全度明确、当前场景最低安全度存在明确、当前场景各存在安全评估明确等目标归入专用方法生产；任务筹办中按 `基础风险判断状态` 领域词注入观察前置的硬编码已撤回。
    构建：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m，0 错误，0 警告。
    真实入口：.\x64\Debug\鱼巢.exe --self-scene-html，退出码 0。
    日志证据：日志/鱼巢_run_20260529.低值g 第 5082 行 `安全评估本能方法结果`，方法=安全_评估当前场景安全性，成功=0，基础风险判断状态=3，风险状态明确状态=0，场景存在集合明确状态=1，当前场景存在数量=1，关键特征变化观察状态=0，安全因果候选构造状态=0，证据不足原因=12，未判断因子数量=2，当前场景安全度候选=5000，当前场景直接伤害可能性候选=5000，评估置信度=0，动作动态=7Wo。
    边界证据：本轮只证明方法运行并正确拒绝明确；不得宣称基础风险判断状态已明确、场景安全或安全值可入账。
当前下一项：
    R3 外设相机能力接入自我内外交互桥已完成第一条桥链；后续转入 R4 关键特征变化观察 / 安全因果候选构造，或 R5 评估结果满足后的正式回写自然证据。
    计划文件：计划/20260529_R3外设相机能力接入自我内外交互桥实施计划_v0.1.md。
    已完成方向：将 `当前观察特征帧取得状态 == 已取得` 正式需求目标接入 D455 外设等待项 / 订阅项、任务管理观察事实承接请求和自我观察事实承接；任务管理消费报告后再回填观察事实，不恢复相机线程内部本能方法作为相机能力。
    下一方向：把 `关键特征变化观察状态 == 已明确`、`安全因果候选构造状态 == 已明确` 从当前证据不足缺口继续推进到对象作用方法、因果候选和需求满足证据链。
    边界：外设相机能力接入是自我内外交互桥，不是相机线程直写世界真值；安全 / 服务根需求不得直接消费相机原始流。
已完成：
    R3 外设相机能力接入桥 C0-C4。
    代码证据：外设观察等待项增加最小稳定性和幂等键；任务管理界面线程按正式需求自动创建 D455 逐簇识别等待项、启动 D455 外设线程、巡检等待项并将命中报告转成观察事实承接请求；自我线程修正首次承接旧状态为空时的状态 / 动态落账路径。
    构建：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m，0 错误，1 个 LNK4075 增量链接警告。
    真实入口：普通启动 .\x64\Debug\鱼巢.exe，真实运行后按规范停止进程；日志证据为 日志/鱼巢_run_20260529.低值g 22:50-22:51 窗口。
    需求链证据：日志第 12829 行显示执行缺失派生需求入树成功，新需求=e，需求类型=当前观察特征帧取得状态；第 12818-12819 行显示任务管理按来源需求 e 启动 D455 并自动创建外设观察等待项，幂等键含 需求=e、目标特征=当前观察特征帧取得状态、模式=陌生环境逐簇识别。
    外设与任务管理证据：日志第 12871-12873 行显示任务管理按等待项命中 D455 逐簇识别报告ID=3，生成观察事实承接请求并上行，`直接写需求树=0`、`直接写世界树=0`；第 12909-12911 行再次命中报告ID=7。
    自我承接证据：日志第 12912-12913 行显示自我线程开始承接并按报告ID=3 回查成功；第 12942 行显示 `自我线程/外设观察事实承接完成 | 成功=是`；第 12965-12966 行显示报告ID=7 开始承接并回查成功；第 13017 行显示第二次 `成功=是`。
    边界证据：承接完成日志保留 `禁止直接写世界真值=是`；本阶段不宣称已确认观察存在、基础风险已明确或安全 / 服务值结算。
R3 外设相机能力接入桥 C4.5/C4.6-1/C5-3a/C5-3b/C5-3c/C4.7/C4.6-2/C6-C7。
    原阻塞：外设报告承接链只生成观察报告承接状态、观察像素簇集合、存在候选集合和识别摘要；现有 `自我_确认观察存在并更新帧解释状态` 需要原始观察帧、空间候选队列、像素归属验证结果和全帧像素归属账本。任务管理观察事实承接请求只携带报告ID和簇摘要，没有足够证据复用已确认观察存在链。按规范不能把外设内部跨帧簇ID或存在候选集合直接写成当前场景已确认观察存在。
    2026-05-30 定案：以方案 B 为主，新增 `自我_验证外设观察像素簇候选` 对象作用方法；方案 A 只做最小数据补强，让外设报告携带可回查的原始帧、簇掩码 / ROI / 点集、空间候选、像素归属账本等材料引用。该方法必须先验证候选，再生成已确认观察存在候选和当前场景存在集合明确状态；最终仍由提交入口回写需求满足。
    2026-05-30 首轮实现验证：C5-1/C5-2 已完成。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0。控制台显示外设报告含原始帧 / 深度帧 / 彩色帧 / 对齐帧句柄、空间候选队列引用、像素归属账本引用和可回查材料簇；日志 `日志/鱼巢_run_20260530.低值g` 第 1152 行显示 `自我_验证外设观察像素簇候选` 对报告ID=7 的 7 个簇验证全通过，缺失材料数量=0，且明确记录 `不生成已确认观察存在=1`、`不写当前场景存在集合明确=1`。
    当前断点：继续 C5-3/C6/C7，但不能立刻把验证成功承接为已确认观察存在。当前 d455:// 材料引用只是字符串句柄和簇摘要，仓库内尚未发现按句柄回查原始帧、像素掩码、点集或全帧像素归属账本的数据仓库 / 读取接口。下一步必须先补齐可解引用外设观察材料库或等价像素归属账本，再补“验证结果 -> 已确认观察存在 / 当前场景存在集合明确状态”的执行结果提取、满足验证和提交链。
    2026-05-30 新增 C4.5 纠偏断点：当前 `--auto-observe-stable` 诊断入口无条件创建逐簇识别、扫描变化、目标跟踪三个等待项；D455 线程每帧生成逐簇识别报告，并对所有簇做跨帧ID连续性缓存；扫描变化和目标跟踪虽然由等待项触发，但当前诊断入口无条件制造等待项。最新口径要求：未识别场景只需要逐簇识别；扫描变化必须基于已确认 / 已识别场景；目标跟踪必须由自我明确目标指令触发；第一版同一时刻只主动跟踪一个目标，确认 / 否定 / 丢失 / 交回后再选下一个。下一步必须先纠偏等待项创建和 D455 线程调度，不能继续把三模式同跑当成业务验收。
    2026-05-30 新增 C4.6 断点：外设层生成的数据统一视为临时观察材料；材料形态可按识别任务需要定义，包括帧缓存、ROI、掩码、点集、轮廓、局部截图、深度切片、像素归属账本、坐标系和时间戳。任务管理层负责接收外设消息、创建 / 推进识别任务；识别任务需要继续获取子存在 / 子特征时，按已确认观察存在内部结构递归细分规范，把父存在当作内部场景继续识别。例如人脸确认后，以人脸.内部世界为场景继续获取眼睛在人脸上的坐标、眼眶大小、深度等证据。自我方法层后续按需要补齐 `自我_确保存在内部世界`、`自我_建立存在内外场景接口`、`自我_将存在局部观察切片映射到内部世界`、`自我_在内部世界提取子候选`、`自我_确认内部世界子存在` 等对象作用方法；仍不得让外设临时材料直接成为已确认观察存在或世界真值。
    2026-05-30 新增 C4.7 断点：当一个存在已经被确认 / 识别，且已有可追溯轮廓、掩码或 ROI 后，下一帧应先用该轮廓拆分当前原始帧。轮廓覆盖且匹配可信的部分进入已知存在扫描区；未被任何已确认轮廓覆盖的部分进入新识别区；轮廓重叠、漂移、残差异常或遮挡区域进入冲突 / 待重识别区。扫描区用于更新已知存在的轮廓、位置、可见状态、遮挡状态和内部世界局部材料；识别区用于生成新簇候选、重识别任务或补观察缺口。轮廓分区不是目标跟踪指令，也不得直接写已确认观察存在、当前场景存在集合明确、安全评估或价值账。
    2026-05-30 执行顺序硬修订：C4.6 拆为 C4.6-1 和 C4.6-2；C5-3 拆为 C5-3a / C5-3b / C5-3c。C4.6-1 先补短期观察材料库、d455:// 句柄解引用、TTL、引用计数、线程安全和材料缺失回执；C5-3a 只验证外设观察像素簇候选并输出可确认候选；C5-3b 只确认候选的新建 / 合并 / 冲突 / 证据不足方案；C5-3c 由 `提交_确认观察存在事实` 正式创建或合并存在，写入原始观察事实、动态、二次特征和当前场景索引。C4.7 必须在 C5-3c 之后，只能消费已确认观察存在及其已提交轮廓 / ROI / 掩码原始事实；C4.6-2 再补已确认存在内部世界递归材料。安全评估只消费已确认存在集合，不消费未确认候选。
    2026-05-30 执行更新：C4.5、C4.6-1 和 C5-3a 已完成当前轮真实验证。代码补齐了空间候选簇内像素索引、D455 短期观察材料页、`解析D455观察材料句柄`，并让 `自我_验证外设观察像素簇候选` 按句柄真实解引用材料。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；最新日志显示 3 个逐簇识别报告均 `短期观察材料页提交=1`，验证结果为报告ID=3、候选簇数量=12、通过簇数量=12、缺失材料数量=0，仍明确 `不生成已确认观察存在=1`、`不写当前场景存在集合明确=1`。当时停止点在 C5-3b 前：已有旧方法 `自我_确认观察存在并更新帧解释状态` 会在对象方法内部直接写已确认观察存在逐项账本，不能作为 C5-3b 复用；后续已新增 / 改造 `自我_确认外设观察存在候选` 只输出确认方案，并由 C5-3c `提交_确认观察存在事实` 入账。
    2026-05-30 执行更新二：C5-3b 和 C5-3c 已完成当前轮真实验证。新增 `自我_确认外设观察存在候选` 本能方法 ID 6128，只生成确认方案结果场景；新增 `提交_确认观察存在事实` 本能方法 ID 6129，作为提交入口写入观察存在事实。旧 `自我_确认观察存在并更新帧解释状态` 已从默认本能方法表、内部注册和诊断链脱钩，并删除对象方法摘要中的已确认观察存在逐项账本直写调用。C5-3c 已补 `观察存在属于当前场景存在`、`观察存在来源于外设报告`、`观察存在对应外设像素簇` 三类二次特征关系，来源动态绑定提交动作动态。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；日志 `日志/鱼巢_run_20260530.低值g` 第 1290 行显示 C5-3b 对报告ID=3 输出新建=16、合并=0、冲突=0、证据不足=0、可提交=16，且明确 `不创建观察存在=1`、`不写当前场景存在集合明确=1`、`不结算安全值=1`；第 1292 行显示 C5-3c 提交新建=16、已提交=16、观察原始事实写入=1、当前场景索引更新=1、写入二次特征数量=48、场景存在集合明确状态=1，且明确 `不写基础风险明确=1`、`不结算安全值=1`。当时停止点移到 C5-3c 后；后续 C4.7 已完成当前轮实现验证。
    2026-05-30 执行更新三：C4.7 已完成当前轮实现验证。新增 `自我_按已确认轮廓分区当前帧` 本能方法 ID 6130，接入方法头、条件 / 结果场景、动作句柄、内部 / 外部包装和 `--auto-observe-stable` 真实诊断链；诊断入口在 C5-3c 提交已确认观察存在事实后，才启动下一帧 D455 采样并调用该方法。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；日志 `日志/鱼巢_run_20260530.低值g` 第 1335 行显示 `当前帧轮廓分区结果 | 方法=自我_按已确认轮廓分区当前帧 | 成功=1 | 报告ID=4 | 当前帧ID=3 | 已确认存在=9 | 已知扫描区=9 | 新识别区=6 | 冲突区=9 | 待重识别区=0 | 像素归属闭合状态=1`，且明确 `不是目标跟踪=1`、`不创建观察存在=1`、`不写基础风险明确=1`、`不结算安全值=1`。当时停止点移到 C4.7 后；后续 C4.6-2 已完成当前轮实现验证。
    2026-05-30 执行更新四：C4.6-2 已完成当前轮实现验证。诊断链在 C4.7 后调用 `自我_将存在局部观察切片映射到内部世界` 和 `自我_在内部世界提取子候选`；补齐 `存在内部世界局部材料建立状态`、`内部世界局部轮廓材料可回查状态`、`内部世界子候选验证状态` 目标特征写入和日志证据。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；日志 `日志/鱼巢_run_20260530.低值g` 第 1379 行显示局部切片映射成功，`存在内部世界局部材料建立状态=1`、`内部世界局部轮廓材料可回查状态=1`，并输出局部 ROI / 掩码 / 点集 / 彩色图 / 深度图 d455:// 句柄；第 1380 行显示内部世界子候选提取成功，`内部世界子候选集合生成状态=1`、`内部世界子候选验证状态=1`、`子存在=` 为空，且明确 `不确认内部世界子存在=1`、`不写基础风险明确=1`、`不结算安全值=1`。当时停止点移到 C4.6-2 后；后续 C6/C7 已完成当前轮实现验证。
    2026-05-30 执行更新四补：C4.6-3 内部世界子存在确认 / 提交链已完成当前轮实现验证。新增本能方法 ID 6137 `提交_确认内部世界子存在事实`，并将 `自我_确认内部世界子存在` 收口为只生成内部世界子存在候选确认方案，不在对象方法内创建内部世界成员。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；最新日志显示 `自我_确认内部世界子存在` 成功=1、`子存在=` 为空、`对象方法不创建内部世界子存在=1`，随后 `提交_确认内部世界子存在事实` 成功=1、`子存在=...`、`提交状态=1`、`写入子存在原始事实状态=1`、`内部世界关联状态=1`、`写入二次特征数量=3`。最终诊断显示 `内部世界子存在确认方案链路已执行=1`、`提交内部世界子存在事实链路已执行=1`，仍明确 `不写基础风险明确=1`、`不结算安全值=1`。
    2026-05-30 执行更新五：C6/C7 已完成当前轮实现验证。安全评估方法已调整为可在缺旧观察帧摘要时读取 C5-3c 提交后的已确认观察存在集合作为场景事实底稿；`--auto-observe-stable` 在 C4.6-2 后调用 `安全_评估当前场景安全性`，并提取输出结果状态，只在结果满足时才允许后续需求满足提交。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；日志 `日志/鱼巢_run_20260530.低值g` 第 1428 行显示 `安全评估本能方法结果`，场景存在集合明确状态=1，当前场景存在数量=13，关键特征变化观察状态=0，安全因果候选构造状态=0，证据不足原因=12，当前场景安全度候选=5000，当前场景直接伤害可能性候选=5000，评估置信度=0；第 1429 行显示 `C6/C7安全评估闭环` 已执行=1、结果满足=0、基础风险判断状态=3、风险状态明确状态=0、当前场景安全度明确状态=0、当前场景最低安全度存在明确状态=0、当前场景各存在安全评估明确状态=0，且明确 `不提交需求满足=1`、`不写基础风险明确=1`、`不结算安全值=1`。R3 第一条外设相机接入安全评估桥闭合；下一步进入 R4/R5 的关键特征变化观察、安全因果候选构造或低安全处置 / 持续观察链。
    2026-05-30 执行更新六：R4 关键特征变化观察 / 安全因果候选构造已完成当前轮最小实现验证。新增 `自我_读取自我关键特征变化` 本能方法 ID 6131 和 `安全_构造场景影响因果候选` 本能方法 ID 6132，均为安全对象作用方法，只输出观察窗口和低置信候选视图，不定义安全因素、不写基础风险明确、不结算安全值。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；日志 `日志/鱼巢_run_20260530.低值g` 第 1476 行显示关键特征变化观察状态=1、样本数量=13、负向变化数量=0；第 1478 行显示安全因果候选构造状态=1、候选数量=13、低置信数量=13、归因置信度=0；第 1481 行显示安全评估已读取关键特征变化观察状态=1 和安全因果候选构造状态=1，但仍因归因证据不足输出成功=0、证据不足原因=8、评估置信度=0；第 1482 行显示未提交需求满足、不写基础风险明确、不结算安全值。当前停止点：R4 安全前置桥闭合；R5 需要自然负向变化 / 归因证据或低安全处置 / 持续观察需求链样本。
    2026-05-30 执行更新七：R5a 证据不足默认不安全场景评估结果提交已完成当前轮验证。安全评估结果新增 `当前场景安全判定状态 / 来源` 和 `存在安全判定状态 / 来源` 结果视图；证据不足时安全判定状态写不安全，来源写证据不足默认不安全，并按 `0..I64_MAX` 范围输出场景评估安全值候选，其中 `I64_MAX` 只表示场景绝对安全，未知默认评估安全值为 `I64_MAX / 2`，条件不足为 `I64_MAX / 4`，已证不安全为 `0`，场景安全度由 `评估安全值 * 0.09` 得到；存在风险安全值按每个存在分别评估，历史缺失默认值为 `0`，不以固定常量伪造历史风险；不再因证据不足堵塞结果提交，但仍不结算安全值。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；日志显示 `C6/C7安全评估闭环` 已执行=1、结果满足=1、基础风险判断状态=1、风险状态明确状态=1、当前场景安全度明确状态=1、当前场景最低安全度存在明确状态=1、当前场景各存在安全评估明确状态=1，证据不足时 `当前场景评估安全值候选=4611686018427387903`、`当前场景安全度候选=415051741658464911`、`当前场景直接伤害可能性候选=4611686018427387904`、`当前场景安全判定状态=1`、`当前场景安全判定来源=3`、`I64MAX仅绝对安全=1`、`历史缺失默认值=0`、`未知默认评估安全值=I64MAX/2`、`条件不足评估安全值=I64MAX/4`、`已证不安全评估安全值=0`、`场景安全度=评估安全值*0.09`、`场景安全评估结果可提交=1`、`后续随时间影响自我风险安全值=1`、`不结算安全值=1`；控制台诊断显示 `场景安全评估结果可提交已验证=1` 和 `安全判定默认不安全已验证=1`。当前停止点：R5a 场景安全评估结果提交闭合；下一步是低安全处置 / 持续观察链，或风险安全值随时间投影链。
    2026-05-30 执行更新八：按“历史值如果没有就是 0”口径补齐实现与证据。存在风险评估函数在未接入可回查历史归因记录时令历史值为 0，证据不足保守风险由历史值计算，因此当前无历史样本时最高存在风险安全值候选为 0；安全评估日志和控制台摘要新增 `历史缺失默认值=0`。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；最新日志 `日志/鱼巢_run_20260530.低值g` 第 1693 行显示 `存在风险评估全部明确=0`、`最高存在风险安全值候选=0`、`最低存在安全值候选=9223372036854775807`、`当前场景评估安全值候选=4611686018427387903`、`历史缺失默认值=0`，第 1694 行显示 `C6/C7安全评估闭环` 结果满足=1、场景安全评估结果可提交=1、持续评估动作=1、不结算安全值=1。
    2026-05-30 规范更新九：按“层间十进衰减，层内同级因素平分”修订安全值组成规范。`安全服务值结算规范20260518` 新增同层因素平分规则：第 n 层权重为 `9 / 10^n`，该层多个因素先按因素集合版本取平均，缺失因素不使其他因素自动变重，风险压力类因素必须先反向成安全值方向；`基础观察事实可用与基础风险判断分层规范20260526` 改为场景中多个已确认存在的风险压力先平均，再转换为 `风险安全_场景影响部分 = I64_MAX - 场景风险压力值`，最低安全度存在只作为诊断 / 处置线索；`自我根特征抽象定义与自检缺口规范20260523` 同步收束。现有日志中的 `场景安全度=评估安全值*0.09` 只保留为旧实现证据；实现纠偏见后续执行更新十。
    2026-05-30 执行更新十：同层平分场景风险聚合实现已完成当前轮验证。安全评估实现改为先计算所有已确认存在风险压力平均值，再反向生成 `当前场景评估安全值候选`；`当前场景安全度候选` 改为兼容承接该场景影响安全子项，不再在场景内部额外乘 `0.09`；`当前场景最低安全度存在` 继续只作诊断 / 处置线索。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；最新日志 `日志/鱼巢_run_20260530.低值g` 第 1746 行显示 `场景风险压力平均值=0`、`当前场景评估安全值候选=4611686018427387903`、`当前场景安全度候选=4611686018427387903`、`场景存在风险聚合=平均`、`场景内部额外衰减=0`，第 1747 行显示 `C6/C7安全评估闭环` 结果满足=1、`风险安全层因素集合版本=1`、`风险安全层因素数量=5`、`风险安全层投影=风险安全*0.09`、场景安全评估结果可提交=1、持续评估动作=1、不结算安全值=1。
    2026-05-30 执行更新十一：R5b 低安全持续观察需求线索已完成当前轮验证。安全评估结果新增 `当前场景持续观察需求明确状态` 输出字段；当评估结果满足、场景判定为不安全且来源为证据不足默认不安全时，只在输出结果场景 / 方法运行视图中写 `当前场景持续观察需求明确状态=0`，并通过既有派生需求通道提示目标 `当前场景持续观察需求明确状态 -> 已明确`。该线索不表示持续观察需求已满足，不启动持续观察方法，不结算安全值。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；最新日志 `日志/鱼巢_run_20260530.低值g` 第 1799 行显示 `当前场景持续观察需求明确状态=0`、`低安全持续观察派生=1`、证据不足默认不安全=1、不安全判定允许提交=1；第 1800 行显示 `C6/C7安全评估闭环` 结果满足=1、场景安全评估结果可提交=1、低安全持续观察派生=1、持续评估动作=1、不结算安全值=1。
    2026-05-30 执行更新十二：R5c 低安全持续观察需求明确对象方法已完成当前轮验证。新增本能方法 ID 6133 `自我_明确当前场景持续观察需求`，分类为安全对象作用方法；方法规格、内部 / 外部包装、执行闭环和 `--auto-observe-stable` 诊断链已接入。该方法只承接安全评估低安全派生线索，把 `当前场景持续观察需求明确状态` 从未明确推进到已明确，不启动外设线程、不直接重跑安全评估、不结算安全值。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；最新日志 `日志/鱼巢_run_20260530.低值g` 第 1853 行显示安全评估低安全持续观察派生=1；第 1855 行显示 `自我_明确当前场景持续观察需求` 成功=1，输出特征为当前场景持续观察需求明确状态；第 1856 行显示原状态=0、新状态=1、由安全评估低安全线索承接=1、不启动外设线程=1、不直接重跑安全评估=1、不结算安全值=1；控制台最终诊断显示持续观察需求明确链路已执行=1。
    2026-05-30 执行更新十三：R5d 低安全持续观察 tick 已完成当前轮验证。新增本能方法 ID 6134 `安全_持续观察当前场景`，分类为安全对象作用方法；方法规格、内部 / 外部包装、执行闭环、枚举目标生产者和 `--auto-observe-stable` 诊断链已接入。该方法只在 `当前场景持续观察需求明确状态=已明确` 后记录一次性、可重复选择的持续观察 tick，写出 `当前场景持续观察状态` 和 `当前场景持续观察样本数量`，不启动外设线程、不直接重跑安全评估、不结算安全值。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；最新日志 `日志/鱼巢_run_20260530.低值g` 第 1909 行显示安全评估低安全持续观察派生=1；第 1912 行显示 `自我_明确当前场景持续观察需求` 成功=1，当前场景持续观察需求明确状态=1；第 1913 行显示 `安全_持续观察当前场景` 成功=1，输出特征为当前场景持续观察状态；第 1914 行显示当前场景持续观察状态=1、样本数量=12、一次性tick=1、可被任务重复选择=1、不启动外设线程=1、不直接重跑安全评估=1、不结算安全值=1；控制台最终诊断显示持续观察tick链路已执行=1。
    2026-05-30 执行更新十四：R5e 低安全负向影响处置需求明确已完成当前轮验证。安全评估结果新增 `负向影响处置需求明确状态` 输出字段；低安全时写未明确并记录低安全处置需求派生=1。新增本能方法 ID 6135 `自我_明确负向影响处置需求`，分类为安全对象作用方法；该方法只在持续观察 tick 后将处置需求推进为已明确，不执行降级保护、不启动外设线程、不直接重跑安全评估、不结算安全值。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；最新日志 `日志/鱼巢_run_20260530.低值g` 第 1966 行显示安全评估输出负向影响处置需求明确状态=0、低安全处置需求派生=1；第 1967 行显示 C6/C7 结果满足=1、低安全处置需求派生=1、不结算安全值=1；第 1973 行显示 `自我_明确负向影响处置需求` 成功=1，输出特征为负向影响处置需求明确状态；第 1974 行显示原状态=0、新状态=1、当前场景持续观察状态=1、由低安全持续观察tick承接=1、不执行降级保护=1、不结算安全值=1；控制台最终诊断显示负向影响处置需求明确链路已执行=1。
    2026-05-30 执行更新十五：R5f 降级保护方法头与因果守门已完成当前轮验证。新增本能方法 ID 6136 `安全_执行降级保护`，分类为安全对象作用方法；方法头、条件场景、结果场景、内部 / 外部包装、动作句柄和枚举目标生产者均已接入。该方法只在 `负向影响处置需求明确状态=已明确` 且 `安全影响因果明确状态=已明确` 时允许执行；当前诊断链故意保持安全影响因果未明确，因此方法输出守门失败、`降级保护状态=0`，不启动外设线程、不直接重跑安全评估、不结算安全值。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；最新日志 `日志/鱼巢_run_20260530.低值g` 第 2035 行显示 `安全_执行降级保护` 成功=0、消息为安全影响因果未明确、输出特征为降级保护状态；第 2036 行显示负向影响处置需求明确状态=1、安全影响因果明确状态=0、降级保护状态=0、守门通过=0、因果未明确不执行=1、不启动外设线程=1、不直接重跑安全评估=1、不结算安全值=1；控制台最终诊断显示降级保护方法头链路已验证=1。
    2026-05-30 执行更新十六：R5g 因果信息被动获取 / 无负向因果默认良好纠偏已完成当前轮验证。安全评估不再把因果缺省写成默认不安全；未收到负向因果证据时，存在风险压力候选为 0，场景评估安全值候选和当前场景安全度候选均为 `I64MAX`，当前场景直接伤害可能性候选为 0。低安全持续观察、负向影响处置和降级保护链不触发。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；控制台 `C6/C7安全评估闭环` 显示当前场景评估安全值候选=9223372036854775807、当前场景安全度候选=9223372036854775807、当前场景直接伤害可能性候选=0、当前场景安全判定状态=2、当前场景安全判定来源=5、因果信息被动获取=1、无负向因果默认安全=1、默认良好允许提交=1、低安全持续观察派生=0、低安全处置需求派生=0、不结算安全值=1；最终诊断显示持续观察需求明确链路已执行=0、持续观察tick链路已执行=0、负向影响处置需求明确链路已执行=0、降级保护方法头链路已验证=0、场景安全评估结果可提交已验证=1、安全判定默认良好已验证=1。
    2026-05-30 执行更新十七：R5h 安全评估结果可比较状态补齐已完成当前轮验证。安全评估输出结果场景新增可比较状态节点写入，覆盖风险状态明确状态、当前场景安全度明确状态、当前场景最低安全度存在明确状态、当前场景各存在安全评估明确状态、当前场景持续观察需求明确状态、负向影响处置需求明确状态和当前场景安全判定状态；对象方法仍不直接回写需求满足、不关闭需求、不结算安全值。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；日志 `日志/鱼巢_run_20260530.低值g` 显示 `安全评估本能方法结果` 中 `可比较评估状态写入数量=7`，同轮控制台仍显示默认良好、场景安全评估结果可提交、不结算安全值。提交回写自然链当前停止点：`--auto-observe-stable` 是诊断入口直接调用对象方法，没有真实来源需求，不能合法调用 `自我_回写需求满足`；`.\x64\Debug\鱼巢.exe --demand-tree-growth-summary` 复跑两次均为空输出、退出码 1，最新日志显示自然路径尚停在 `当前观察特征帧取得状态` 派生、D455 外设等待项与观察事实承接阶段，未进入安全评估任务结果回写。
    2026-05-30 执行更新十八：学习需求四类剧本自然样本验收按用户要求跳过，转入后续多候选方法确定性选择。`任务模块.筹办.ixx` 中候选评分比较已调整为直接满足等级优先于结果命中精度，并且只有结果能力能正向承接目标时才授予直接满足等级；这保证可直接达成候选优先于精确但不可直接执行 / 仅可补齐推进的候选，主键兜底仍保留，候选集仍先填充后选择。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --demand-tree-growth-summary` 退出码 0，摘要显示总数=45、派生来源因果=12、目标绑定完整=45、异常=0、权重异常=0、死信=0。该次只做基本不破坏检查，不作为学习需求四类剧本验收通过证据。
    2026-05-30 执行更新十九：R6 控制面板和日志展示已完成当前轮展示补线。`结构_控制面板快照` 新增安全评估宿主指针 / 标题，以及基础观察事实可用状态、基础风险判断状态、风险状态明确状态、基础风险值计算状态、当前场景评估安全值候选、当前场景安全度候选、当前场景安全判定状态、安全评估证据不足原因等字段；`控制面板类.cpp` 单独扫描安全评估结果宿主读取这些派生评估结果，空间复现仍按观察帧 / 轮廓 / 候选材料选宿主；文本摘要和自我所在场景 HTML 均展示上述状态，避免把观察事实可用误显示成风险已明确。Debug x64 构建通过；按用户要求跳过验收，本次只作为展示层构建烟雾检查，不作为控制面板业务验收通过证据。
    2026-05-30 执行更新二十：风险安全层候选投影视图已完成当前轮实现补线。`安全_评估当前场景安全性` 在输出结果场景 / 方法运行视图中新增 `风险安全_场景影响部分候选`、`风险安全层因素集合版本`、`风险安全层因素数量`、`风险安全层缺失因素数量`、`风险安全层候选`、`风险安全层投影候选`；第一版风险安全层 v1 固定 5 个同层因素，当前只将场景影响部分作为明确因素，运行 / 资源 / 权限 / 未知影响缺失时按 0 参与平均，不让场景影响自动吃满整层权重；投影候选按第二层权重 `0.09` 计算。控制台摘要和控制面板展示同步读取这些候选值。该链路只生成候选视图，不写自我风险安全值、不写根安全值、不绕过因果归因和价值结算入口。Debug x64 构建通过；按用户要求跳过自然验收。
    2026-05-30 执行更新二十一：C8-1 风险安全_场景影响部分候选可结算判定已完成当前轮实现验证。`安全_评估当前场景安全性` 的输出结果场景 / 方法运行视图新增 `风险安全_场景影响部分可结算状态`、`风险安全_场景影响部分不可结算原因`、`风险安全_场景影响部分已入账状态` 和变化方向视图；当前候选缺自然来源需求、来源任务、被动归因证据、持续稳定性证据、变化方向和 `提交_风险安全场景影响部分状态变化` 入口，因此输出不可结算、未入账。控制台摘要和控制面板同步展示可结算 / 入账 / 原因字段。本切片只补闸门视图，不写自我风险安全值、不写根安全值、不关闭安全根需求。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0，控制台显示 `风险安全_场景影响部分可结算状态=1`、`风险安全_场景影响部分不可结算原因=9603`、`风险安全_场景影响部分已入账状态=0`、`候选不改账=1`、`不结算安全值=1`。后续 C8-2 才进入正式提交入口设计。
    2026-05-30 执行更新二十二：C8-2 `提交_风险安全场景影响部分状态变化` 已完成当前轮实现验证。新增本能方法 ID 6138，作为提交入口登记默认方法种子、规格确认、内部 / 外部包装和 `--auto-observe-stable` 诊断链；`安全_评估当前场景安全性` 已将提交入口存在状态改为真实存在，因此不可结算原因不再包含“缺提交入口”。当前诊断路径仍缺自然来源需求、来源任务、被动归因证据、持续稳定性证据和变化方向，所以提交入口执行但拒绝写账，只写提交状态 / 拒绝原因 / 动作动态证据，不写 `自我风险安全值`、不写根 `自我安全值`、不关闭安全根需求。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；控制台显示 `风险安全_场景影响部分可结算状态=1`、`风险安全_场景影响部分不可结算原因=1411`、`风险安全场景影响提交链路已执行=1`、`预期拒绝写账=1`、`不写自我风险安全值=1`、`不写根安全值=1`；最新日志 `日志/鱼巢_run_20260530.低值g` 第 23516 行显示 `提交_风险安全场景影响部分状态变化 | 成功=0 | 候选值=9223372036854775807 | 可结算状态=1 | 不可结算原因=1411 | 变化方向=0 | 提交状态=1 | 已入账状态=0 | 缺条件拒绝=1 | 不写自我风险安全值=1 | 不写根安全值=1 | 不关闭安全根需求=1`。
    2026-05-30 执行更新二十三：C8-3 风险安全层聚合明确状态已完成当前轮实现验证。`安全_评估当前场景安全性` 的输出结果场景 / 方法运行视图新增 `风险安全层明确状态`、`风险安全层可聚合状态`、`风险安全层已入账状态` 和风险因素掩码族，控制面板快照和 CLI 摘要同步读取。后续按用户修订口径收紧：默认满足不是“没数据”，而是“按该风险因素的证据搜索规则查过且没有找到负向风险证据”；材料不足不作为稳定业务状态，只作为本次搜索缺口阻断默认满足。当前风险安全层 v1 中场景影响部分仍未入账，运行影响、资源影响、权限影响和未知影响四个同层因素尚未执行负向证据搜索，因此风险安全层候选 / 投影候选只作为候选视图，不能写 `自我风险安全值` 或根 `自我安全值`。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；最新日志显示 `风险安全层缺失因素数量=4 | 风险安全层明确状态=0 | 风险安全层可聚合状态=0 | 风险因素未搜索掩码=30 | 风险因素默认满足掩码=0 | 风险因素证据搜索未完成数量=4`，控制台闭环摘要同步显示这些字段并保留 `候选不改账=1`、`不结算安全值=1`。当前问题点：不能继续正式写自我风险安全值；必须先取得场景影响部分合法入账证据，并对运行 / 资源 / 权限 / 未知四个同层因素执行负向证据搜索，搜索完成且无负向证据后再经提交入口记录默认满足。
    2026-05-30 执行更新二十四：按“没查过不等于默认满足；查过但没找到风险证据才默认满足”完成 C8-3 掩码纠偏。旧 `风险安全层未明确原因` 单掩码已撤下，改为 `风险因素未搜索掩码`、`风险因素搜索缺口掩码`、`风险因素负向证据掩码`、`风险因素默认满足掩码`、`风险因素已入账掩码`、`风险因素证据搜索未完成数量` 和 `风险因素默认满足数量`；`材料不足` 不再作为风险因素稳定业务状态，只作为搜索缺口阻断默认满足。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；控制台显示 `风险因素未搜索掩码=30`、`风险因素搜索缺口掩码=0`、`风险因素默认满足掩码=0`、`风险因素已入账掩码=0`、`风险因素证据搜索未完成数量=4`、`风险因素默认满足数量=0`、`风险安全层可聚合状态=0`。当前问题点保持：不能继续正式写自我风险安全值；下一步应先 C8-4 场景影响部分合法入账，再 C8-5a/C8-5b 补运行 / 资源 / 权限 / 未知四类负向风险证据搜索和无负证据默认满足提交入口。
    2026-05-30 执行更新二十五：C8-4a 场景影响部分合法入账前置承接已完成当前轮代码验证。`安全_评估当前场景安全性` 已从输入条件包读取 `来源需求`、`任务信息节点`、`来源因果信息`，并按旧 `自我_风险安全_场景影响部分` 值计算变化方向；无旧值时记录为 `初始入账`，不再误判为变化方向未明确。被动归因证据仍要求来源因果信息、安全因果候选构造、方法动作动态和公式版本；持续稳定性证据仍要求当前场景持续观察样本和关键特征无负向变化。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；控制台显示 `风险安全_场景影响部分不可结算原因=387`，最新日志显示 `变化方向=4 | 来源需求存在=0 | 来源任务存在=0 | 来源因果信息存在=0 | 被动归因证据存在=0 | 持续稳定性证据存在=0`。当前停止点：C8-4 不能正式入账；需要自然任务路径携带来源需求 / 来源任务 / 来源因果信息，并建立持续观察窗口后再继续，不得伪造这些来源或稳定性证据。
    2026-05-30 执行更新二十六：C8-5a 安全因果层证据搜索通用入口已完成当前轮构建接入。按用户纠偏，旧“风险因素搜索”不再作为方法级主名；核心搜索提升为通用 `搜索安全因果因素证据`，并新增参数化对象作用方法 ID 6139 `安全_搜索安全因果因素证据`，输入层级、目标安全因果信息、因素ID、证据方向和搜索上下文，输出搜索状态、范围完整状态、负向 / 正向 / 稳定性 / 归因证据数量、材料缺失数量、冲突数量、搜索置信度、默认满足允许状态和评估值候选。新增提交入口 ID 6140 `提交_安全因果因素无负证据默认满足` 和 ID 6141 `提交_安全因果因素证据评估值`，二者只写该层该因素的入账值和提交动作动态，不写风险安全层总值、不写自我安全值、不关闭安全根需求。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0，真实入口未触发新搜索入账，安全评估仍显示 `风险因素未搜索掩码=30`、`风险因素默认满足掩码=0`、风险安全场景影响提交闸门拒绝写账、`不结算安全值=1`。当前尚未执行自然搜索任务，后续需由任务筹办带入具体层级 / 因素 / 搜索范围后，再对第二层运行、资源、权限、未知四项逐项搜索。
    2026-05-30 执行更新二十七：C8-5b 安全因果因素搜索任务参数承接已完成当前轮补线。`任务模块.筹办.ixx` 在通用候选方法输入包构造阶段新增 `安全_搜索安全因果因素证据` 专用参数承接：只复制目标宿主已存在的安全因果层级、因素ID、证据方向、搜索窗口、搜索范围完整状态、负向 / 正向 / 稳定性 / 归因证据数量、材料缺失数量、冲突数量、搜索置信度和评估值候选；不会生成需求，不写默认满足，不写层值。对第二层风险安全固定因素，只在因素ID已明确时补层级=2；只在风险安全层因素缺证据方向时补负向证据方向。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0，风险因素仍显示未搜索掩码=30、默认满足掩码=0、不结算安全值=1，说明补线未把未搜索因素误提交为默认满足。当前停止点：自然需求 / 任务还没有生成携带具体层级、因素ID和完整搜索范围的目标宿主，因此四个非场景因素不会自然进入搜索；后续必须先由因果反推 / 缺口提交链生成参数化搜索子需求，不能用诊断入口伪造搜索范围完整或无负证据。
    2026-05-30 计划更新二十八：按用户最新口径收紧安全因果风险树常驻任务边界。常驻任务只负责反推更新安全因果风险树：监听安全因果链、当前安全上下文、当前场景和已提交因素状态版本变化，刷新运行期树节点、父子关系、同层因素集合、来源因果路径和缺口报告。它不得执行证据搜索、不得提交默认满足、不得提交因素值、不得写风险安全层总值 / 自我安全值、不得回写需求满足或结算价值。后续 C8-5b-3 才实现树同步状态 / 树版本 / 视图引用维护；搜索仍由 `安全_搜索安全因果因素证据` 执行，入账仍由提交入口完成。
    2026-05-30 执行更新二十九：C8-5b-3 安全因果风险树常驻反推维护已完成当前轮实现验证。`任务模块.筹办.ixx` 新增 `维护安全因果风险树常驻任务`，自我线程在根层重判后调用该维护段；维护段读取安全根需求 / 安全根任务，按需求方向命中安全因果信息，调用 `筹办_展开安全因果风险树` 生成风险安全层 v1 五个同层因素节点和四个参数化搜索缺口报告，只写树同步状态、树版本、节点数量、搜索缺口数量和最近维护时间。树版本已收敛为显式定义版本：安全因果层级 + 同层因素集合版本 + 来源因果结构规则版本，不再使用来源因果指针或运行中增长的因果总数，避免等价因果命中漂移导致重复刷新。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --demand-tree-growth-summary` 退出码 0；最新日志 `日志/鱼巢_run_20260530.低值g` 显示 23:31:21 首次维护 `树节点数量=5`、`参数化搜索缺口报告数量=4`、`树版本=2001001009`，随后多次 `安全因果风险树常驻维护任务跳过 | 原因=已同步`、版本变化=0。边界证据：日志持续显示 `只反推更新风险树=1`、`不执行搜索=1`、`不写默认满足=1`、`不写因素值=1`、`不写风险安全层总值=1`、`不写自我安全值=1`、`不回写需求满足=1`、`不结算价值=1`；本切片未执行安全因果因素搜索，未提交默认满足或因素值。
    2026-05-30 执行更新三十：C8-5c 风险树搜索缺口提交入树与参数化搜索任务化已完成当前轮实现验证。`任务模块.筹办.ixx` 新增 `生成安全因果风险树搜索缺口派生需求消息集`，只从安全因果风险树节点上的参数化搜索缺口报告生成正式派生需求消息；`自我线程模块.impl.cpp` 在风险树常驻维护首次版本变化且存在搜索缺口时调用该函数，并交由既有 `提交_生成缺口子需求` 裁决入树。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --demand-tree-growth-summary` 退出码 0；日志 `日志/鱼巢_run_20260530.低值g` 显示 23:47:19 `安全因果风险树搜索缺口派生需求消息集` 输出消息数量=4、正式消息字段完整=4、输入只来自风险树缺口=1；随后四条 `提交_生成缺口子需求` 分别生成运行 / 资源 / 权限 / 未知影响的 `安全因果因素证据搜索状态` 子需求和任务 F/G/H/I，入树来源因果键均为 `2cD`。其中运行影响任务 F 已完成参数化搜索任务化：候选集填充命中方法 `1T`，选择当前方法后提交本能入口 ID 6139 `安全_搜索安全因果因素证据`。边界证据：相关日志均保留 `不执行搜索=1`、`不写默认满足=1`、`不写因素值=1`、`不写风险安全层总值=1`、`不写自我安全值=1`、`不结算价值=1`。当前停止点：该参数化搜索方法已有真实动作入口，但仍缺条件结果对；下一步 C8-5d 必须走方法结构缺口 / 学习承接 / `自我_建立条件结果对` / `自我_提交方法状态变化` 的方法域提交链，不能用方法规格确认或诊断入口伪造条件结果对。
    2026-05-31 执行更新三十一：按计划索引回到 C8-4 前，先处理 `--demand-tree-growth-summary` 在安全因果风险树派生需求上报处的断点。`任务模块.管理工作线程.ixx` 在筹办派生需求消息规范化阶段新增“由指针补齐主键槽”的显式归一化，并记录 `任务管理工作线程/因果路径派生需求主键槽补齐` 日志；该修正不创建需求、不写需求树，只补齐同一消息内已有指针对应的父需求、来源因果、目标主体、目标特征、当前状态和目标状态主键。Debug x64 构建通过。复跑 `.\x64\Debug\鱼巢.exe --demand-tree-growth-summary` 仍在 120 秒窗口超时，残留进程已停止；但最新日志已显示原 `字段不完整=4 / 上报数量=0` 改为 `字段不完整=0 / 上报数量=4`，四条 `安全因果因素证据搜索状态` 子需求已通过 `提交_生成缺口子需求` 入树并保留来源因果键 `2WD`。当前新停止点：摘要入口仍不能稳定退出，后续日志显示工作量转入泛化学习信息补齐需求与活动子需求等待链，需继续定位任务巡检 / 学习缺口派生 / 父子等待收束，不能据此推进 C8-4 场景影响部分正式入账。
    2026-05-31 执行更新三十二：按用户确认修订摘要入口语义。摘要只给人和协作代理做诊断观察，不作为鱼巢内部动作来源、任务结算依据、C8-4 业务闭合证据或“稳定退出”门槛；摘要能读到什么就输出什么，不等待队列清空、不等待首轮完成、不补齐业务状态。`鱼巢.cpp` 已删除摘要稳定等待 / 只读等待路径，四个摘要命令改为环境初始化后记录 `控制面板摘要/即时快照` 并立即读取轻量快照；`main` 不再为摘要命令主动启动自我线程或自检线程。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --demand-tree-growth-summary` 退出码 0、耗时约 12.48 秒，控制台显示 `快照状态: 即时读取`，最新日志显示 `命令行/确保自我环境初始化`、`控制面板摘要/即时快照`、`控制面板摘要/快照读取`，本轮摘要路径未新增 `命令行/启动自我线程`。
    2026-05-31 执行更新三十三：按用户要求暂停当前计划并清理非正式逻辑代码。`鱼巢.cpp` 已删除 `--self-scene-observe-once`、`--self-scene-html`、`--peripheral-autonomous-observe`、`--autonomous-observe`、`--auto-observe-stable` 等命令行硬串业务链入口，移除命令行中直接查找本能方法、人工创建外设等待项、直接调用自我 / 外设 / 安全评估 / 提交方法链、再以控制台字段判定业务闭合的非正式逻辑；同时撤回本轮临时新增的 `安全_评估当前场景安全性/C8持续稳定性证据缺口` 派生分支。保留基础相机状态 / 打开 / 释放 / 单帧采集工具、自检日志入口 `--self-check-log`、控制面板和只读摘要入口。Debug x64 构建通过，0 错误，1 个 LNK4075 增量链接警告。后续验证不得再依赖已删除诊断入口，必须走正式自我线程、任务管理生命周期、提交入口、动作动态、真实运行日志和只读快照证据。
R4 关键特征变化观察 / 安全因果候选构造已完成当前轮最小实现验证。
R5g 因果被动获取、无负向因果默认良好场景评估结果提交已完成当前轮验证。
R5h 安全评估结果可比较状态补齐已完成当前轮验证；提交回写自然链需等待自然来源需求跑到安全评估任务。
R5 风险安全值随时间投影已补候选视图；正式改写自我风险安全值仍需后续被动归因证据、持续时间 / 稳定性证据和价值结算入口，不阻塞默认良好评估路径。
R6 控制面板和日志展示已完成展示字段接入；真实 UI/日志人工验收按用户要求跳过，不据此宣称业务验收通过。
```

## 下一切片建议

```text
执行 R5 第二段安全闭环剩余部分：
    以 R3 已闭合的 外设报告 -> 已确认观察存在 -> 安全评估证据不足 链为输入；
    基于 R4/R5g/R5h 已产生的关键特征变化观察、因果候选视图、默认良好安全评估结果和可比较状态节点，继续验证评估结果满足后的提交回写自然链；真实自我关键特征负向变化与安全影响因果明确只作为后续被动更新风险的样本，不再阻塞默认路径；
    当前不能用诊断入口伪造来源需求；必须先让自然任务链从观察取得子需求推进到安全评估来源需求；
    先写预测，再走真实运行路径，最后用日志 / 状态快照 / 动作动态 / 错误日志核对；
    不直接写基础风险判断已明确，不直接结算自我安全值。
```

## 禁止宣称项

```text
不宣称基础观察事实可用总目标已完整闭合。
不宣称基础风险判断状态已明确。
不宣称风险值已计算或场景安全。
不宣称完整视觉分割、跨帧身份稳定或内部循环全部完成。
不宣称自我苏醒完成或初步成熟完成。
```
