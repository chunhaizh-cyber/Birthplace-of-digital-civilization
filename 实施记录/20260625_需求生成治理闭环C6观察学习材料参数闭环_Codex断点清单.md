# 20260625 需求生成治理闭环 C6 观察学习材料参数闭环 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260625_需求生成与治理闭环代码修订计划_v0.1.md` 的 C6“观察验证、学习、材料和参数闭环”。

当前目标：

```text
1. 三态比较不可判定 / 目标主体不可判定 / 当前状态缺失等观察类拒绝进入观察验证闭环承接。
2. 学习缺口按学习闭环承接，不普通需求化。
3. 外设内部材料字段进入外设材料闭环承接。
4. 参数诊断不需求化进入来源任务内部承接、参数重绑或方法候选降权方向。
```

依据文件：

```text
规范/需求入树拒绝回执与开发诊断闭环规范20260625.md
规范/任务筹办与执行边界规范20260512.md
docs/工程图谱/01_任务需求方法闭环图谱.md
docs/工程图谱/05_规则原子索引.md
计划/计划索引.md
实施记录/20260625_需求生成治理闭环C5开发诊断闭环_Codex断点清单.md
```

计划触碰文件：

```text
任务模块.管理线程协议.ixx
自我线程模块.impl.cpp
任务模块.管理界面线程.impl.cpp
说明书/函数功能说明文档.md
计划/计划索引.md
计划/20260625_需求生成与治理闭环代码修订计划_v0.1.md
实施记录/20260625_需求生成治理闭环C6观察学习材料参数闭环_Codex断点清单.md
```

已知无关脏改：

```text
控制面板.html
鱼巢.vcxproj
鱼巢.vcxproj.filters
tools/__pycache__/
```

## 1. 预测

```text
本切片先补结构化转派项和回执 ID 回填。
不会实现真正的观察任务生成、学习方法补全任务、外设材料仓库写入或参数绑定算法。
构建通过后，只能说明四类非普通需求化路径有结构化转派承接，不再只靠日志。
```

## 2. 实施结果

```text
1. `结构_需求入树拒绝回执` 新增 `关联转派项ID`。
2. `任务模块.管理线程协议.ixx` 新增 `枚举_拒绝闭环转派状态` 和 `结构_拒绝闭环转派项`。
3. `自我线程模块.impl.cpp` 新增拒绝闭环转派账本和转派项登记入口。
4. 观察验证闭环、学习闭环、外设材料闭环、来源任务内部闭环、内部治理闭环、合并冷却闭环会登记运行期转派项。
5. 来源任务内部闭环中，参数绑定重试、方法候选降权、来源任务重筹办映射为 `等待参数重绑`。
6. 开发诊断闭环仍固定走开发诊断项，不进入运行期转派项。
```

## 3. 验证结果

```text
git diff --check -- 任务模块.管理线程协议.ixx 自我线程模块.impl.cpp 任务模块.管理界面线程.impl.cpp 计划/计划索引.md 计划/20260625_需求生成与治理闭环代码修订计划_v0.1.md 实施记录/20260625_需求生成治理闭环C6观察学习材料参数闭环_Codex断点清单.md
退出码：0

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：已成功生成，0 个警告，0 个错误。

python .\tools\生成函数功能说明文档.py
结果：函数条目 7175。

python .\tools\check_specs.py
结果：hard issues 0，warnings 81。
```

## 4. 当前判断

```text
C6 已形成结构化转派承接骨架，可以从代码差异中看到观察验证、学习、外设材料、参数诊断四类非普通需求化路径不再只依赖日志。
本切片没有实现真正观察任务生成、学习补全任务、外设材料仓库写入或参数绑定算法。
不得宣称观察验证闭环、学习闭环、外设材料闭环、参数闭环、拒绝回执治理闭环或需求生成与治理闭环已经完整实现。
```

## 5. 下一步

```text
按计划索引推进 C7：结果事实可信、价值结算硬闸门和父链重判补强。
```
