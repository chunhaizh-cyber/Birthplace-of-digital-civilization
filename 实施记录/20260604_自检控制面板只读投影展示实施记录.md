# 20260604 自检控制面板只读投影展示实施记录

## 目标

闭合 `计划/20260531_自检问题因果投影入树逻辑修复计划_v0.1.md` 的 P6：
控制面板只读展示自检需求的投影和来源解释，不新增权威字段。

## 已改

```text
控制面板类.cpp
    在需求详情中识别自检派生需求。
    对自检派生需求追加“自检投影只读视图”子节点。
    视图通过安全根需求 / 任务引用、需求目标宿主 / 目标特征和因果只读投影查询派生。
    展示固定来源任务、固定来源需求、投影维度、候选权重类型、
    结算贡献层级、因果距离层级、来源因果主键、来源因果类型、
    来源因果命中数量、父需求选择原因、缺失证据。

计划/20260531_自检问题因果投影入树逻辑修复计划_v0.1.md
    将旧“遗传因果命中数量”口径改为“来源因果命中数量”。
```

## 验证

```text
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：通过。
警告：LNK4075 忽略 /EDITANDCONTINUE，由 /INCREMENTAL:NO 引起。
```

## 未验收

```text
未跑真实控制面板页面。
未跑自然样本或真实业务运行验收。
P7 仍是验证项；按当前用户要求跳过验收时，不作为继续执行挡板。
```
