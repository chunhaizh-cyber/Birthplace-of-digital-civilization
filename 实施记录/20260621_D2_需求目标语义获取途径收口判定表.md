# 20260621 D2 需求目标语义 / 获取途径收口判定表

## 0. 判定口径

```text
所属计划：计划/20260620_需求类逻辑纠偏计划_v0.1.md P2。
执行边界：本表只复核目标语义、获取途径和显示名称的消费边界；不修改源码。
上级边界：
1. 获取途径只用于入树前生成规则选择，不进入需求主信息。
2. 正式入树后的需求语义继续由目标特征类型和抽象特征定义视图解释。
3. `语义名称`、`获取途径名称`、`需求目标语义文本`、`需求获取途径文本` 只能用于日志、显示、SQL 投影和人类可读原因。
4. 机器判断必须读取枚举、布尔、语素入口节点同一性、目标特征类型、状态和满足关系，不得读取文本名称。
```

## 1. 扫描命令

```powershell
rg -n "枚举_需求获取途径|结构_需求获取途径语义视图|需求获取途径语义视图|需求获取途径文本|获取途径名称|语义名称|需求目标语义文本|普通候选方法筹办禁止原因|结构_需求目标语义视图" -g "*.cpp" -g "*.h" -g "*.ixx" -g "!x64/**" -g "!Debug/**" -g "!日志/**" -g "!运行输出/**"

rg -n "语义名称\s*==|语义名称\s*!=|获取途径名称\s*==|获取途径名称\s*!=|strcmp\([^\n]*(语义名称|获取途径名称)|需求目标语义文本\([^\n]*\)\s*==|需求获取途径文本\([^\n]*\)\s*==" -g "*.cpp" -g "*.h" -g "*.ixx" -g "!x64/**" -g "!Debug/**" -g "!日志/**" -g "!运行输出/**"

rg -n "需求获取途径语义视图\([^\n]*\)|需求目标语义_按获取途径\([^\n]*\)|逻辑组织需求类型_按获取途径\([^\n]*\)|需求获取途径默认目标特征类型\([^\n]*\)|构造获取途径逻辑组织需求更新指令\([^\n]*\)" -g "*.cpp" -g "*.h" -g "*.ixx" -g "!x64/**" -g "!Debug/**" -g "!日志/**" -g "!运行输出/**"
```

## 2. 调用点判定

| 调用点 | 消费字段 | 机器判断依据 | 文本用途 | 判定 |
| --- | --- | --- | --- | --- |
| `需求类.h:187-229` | `枚举_需求获取途径`、`结构_需求目标语义视图`、`结构_需求获取途径语义视图` | 枚举、语素入口指针、布尔字段 | `语义名称` / `获取途径名称` 作为显示字段 | 保留；文本字段不作机器字段。 |
| `需求类.cpp:1403-1416` | `需求目标语义视图` | `目标特征类型` -> `需求目标语义_按目标特征类型`，并填充布尔闸门 | `语义名称` 由枚举转文本 | 保留；入树后目标语义由目标特征类型解释。 |
| `需求类.cpp:1420-1506` | `需求目标语义_按获取途径`、`逻辑组织需求类型_按获取途径`、`需求获取途径语义视图` | 获取途径枚举映射默认语义、默认逻辑类型和默认目标特征类型 | `获取途径名称` 由枚举转文本 | 保留；只供入树前生成规则选择。 |
| `需求类.cpp:1874-1905` | 普通候选筹办准入 / 禁止原因 | `视图.语义`、`可进入普通候选方法筹办` | 禁止原因是人类可读说明 | 保留；原因文本不被下游比较。 |
| `需求类.cpp:1919-2014` | 构造逻辑组织需求更新指令 / 按获取途径构造 | `枚举_逻辑组织需求类型`、`默认逻辑组织类型`、目标特征语素入口 | 日志写 `逻辑组织需求类型文本`、`获取途径名称` | 保留；文本只写日志，指令写入目标特征类型。 |
| `任务模块.筹办.ixx:684-746` | 目标语义闸门 | `视图.语义`、`视图.可进入普通候选方法筹办`、`视图.目标特征类型` | `语义名称` 用于摘要 / 筹办日志 | 保留；机器分支未读取 `语义名称`。 |
| `自我线程模块.impl.cpp:5443-5650` | 父需求聚合、任务化准入 | `视图.语义`、`可进入普通候选方法筹办`、目标特征类型、状态和满足关系 | `语义名称` 写运行日志 | 保留；线程侧不按文本判断语义。 |
| `自我线程模块.impl.cpp:10097-10173` | 当前场景特征值逻辑组织子需求创建 | `枚举_需求获取途径` -> 默认目标特征类型 -> 更新指令 | 无文本判断 | 保留；这是入树前生成规则选择点。 |
| `需求类.cpp:128-180` | 需求树 SQL 行 | 只收集查询投影 | `安全获取词`、`语义名称` 写 SQL 显示列 | P6 后置；不是当前机器逻辑阻塞。 |
| `控制面板类.cpp:4608-4640`、`5450-5472`、`9618-9653`、`9828-9858` | 控制面板字段 / 快照统计 | 枚举和布尔用于统计，文本写显示字段 | `语义名称`、显示原因和令牌状态文本 | P6 后置；不作为需求树权威写源。 |

## 3. 反向扫描结果

```text
1. `语义名称 ==` / `语义名称 !=` / `获取途径名称 ==` / `获取途径名称 !=` / `strcmp(...语义名称...)` / `strcmp(...获取途径名称...)` 无命中。
2. `需求获取途径语义视图(...)` 的消费点只在 `需求类.cpp:1999`，用于入树前构造逻辑组织需求更新指令。
3. `枚举_需求获取途径` 外部正式消费点只在 `自我线程模块.impl.cpp:10101-10165`，传入的是枚举，不是文本。
4. `需求目标语义文本`、`需求获取途径文本` 和 `普通候选方法筹办禁止原因` 当前只返回人类可读文本；下游没有按这些文本做分支。
```

## 4. 当前结论

```text
P2 当前范围没有发现文本名称参与需求机器查找 / 生成 / 准入判断。
获取途径未进入需求主信息；它只在入树前把枚举映射为默认目标语义、默认逻辑类型和目标特征类型。
入树后路径读取 `需求类::需求目标语义视图(需求)` 或 `需求类::需求目标语义视图(目标特征类型)`，语义来源是目标特征类型。
显示 / SQL / 控制面板读取点继续保留在 P6 后置，不作为 P2 阻塞。
```

## 5. 下一步

```text
转入 P3 更新指令协议简化预切片：
1. 统计 `结构_需求树更新指令` 外部构造点。
2. 判断能否拆成新增 / 更新 / 满足 / 删除专用函数。
3. 不新增平行协议结构，不把显示文本塞入机器字段。
```
