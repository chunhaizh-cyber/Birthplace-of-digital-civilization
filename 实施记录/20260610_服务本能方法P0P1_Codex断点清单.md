# 服务本能方法 P0/P1 Codex断点清单

日期：2026-06-10

## 当前切片

```text
服务本能方法实现计划 P0/P1：
    P0 服务模块骨架。
    P1 任务字段绑定辅助函数。
```

## 依据文件

```text
AGENTS.md
规范/规范目录.md
计划/计划索引.md
实施记录/20260610_服务本能方法实现方案记录.md
规范/方法动作动态硬规则规范20260512.md
规范/自我存在服务与生存基本因果闭环规范20260601.md
规范/安全服务值结算规范20260518.md
规范/方法规范20260425.md
规范/任务筹办与执行边界规范20260512.md
```

## 允许修改范围

```text
自我动作实现.服务模块.ixx
自我动作实现模块.ixx
鱼巢.vcxproj
鱼巢.vcxproj.filters
实施记录/20260610_服务本能方法P0P1_Codex断点清单.md
计划/计划索引.md
```

## 禁止修改范围

```text
任务主信息类
需求主信息类
特征值主信息类
任务筹办 / 任务执行 本能方法化
服务事件E独立虚拟存在
裸写服务值
G0 / D2 结算实现
```

## 进入时工作区状态

```text
存在提交前已有无关修改：
    AGENTS.md
    任务模块.管理工作线程.ixx
    控制面板.html

本切片不得混入上述文件。
```

## 预期结果

```text
1. 新增 `自我动作实现.服务模块.ixx`。
2. `自我动作实现模块.ixx` 能 import 新模块。
3. 工程文件包含新模块。
4. 服务模块提供任务字段绑定 helper。
5. 不注册新的服务本能方法。
6. Debug x64 构建通过。
```

## 已运行命令

```text
Get-Content -Path C:\Users\zhchh\.codex\skills\yu-chao-execute\SKILL.md -TotalCount 220
rg -n "读取特征_指针|写入特征_指针|取或创建子特征_按类型|写入特征_I64" 世界树类.h 特征类.h
Get-Content -Path 任务类.h -TotalCount 180
Get-Content -Path 任务主信息类.h -TotalCount 220
Get-Content -Path 世界树类.h | Select-Object -Skip 100 -First 120
git status --short --untracked-files=no
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\鱼巢_build_20260610_服务模块P0P1.log
```

## 关键退出码

```text
Debug x64 构建：ExitCode=0
关键错误检索：未检出 error C / fatal error / error MSB / warning C
```

## 已完成修改

```text
1. 新增 `自我动作实现.服务模块.ixx`。
2. `自我动作实现模块.ixx` 已 import `自我动作实现.服务模块`。
3. `鱼巢.vcxproj` 和 `鱼巢.vcxproj.filters` 已纳入服务模块。
4. 服务模块已提供任务字段绑定 helper：
   - 读取服务输入任务
   - 读取任务来源需求
   - 读取任务虚拟存在
   - 绑定任务指针字段 / I64字段
   - 绑定任务交互者 / 权限者 / 服务对象 / 权限边界 / 目标宿主 / 目标特征类型 / 目标值 / 输入场景 / 输出场景 / 实现方法虚拟存在 / 方法运行存在
   - 记录任务字段来源方法 / 方法版本 / 激活信息
5. 本切片未注册新的服务本能方法，未生成 G0 / D2，未写服务值。
```

## 当前判断

```text
P0/P1 已闭合；服务字段绑定基础设施可构建。
下一步进入 P2：字段补齐类本能方法注册与最小叶子动作实现。
```
