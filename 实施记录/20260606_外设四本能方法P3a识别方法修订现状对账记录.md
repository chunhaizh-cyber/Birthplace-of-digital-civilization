# 外设四本能方法 P3a 识别方法修订现状对账记录

## 目标

对账 `自我_识别外设观察材料` 是否已经从“组织识别材料”收口为“外设观察存在与自我场景存在的同一性比较方法”，并拆出下一片最小代码改动。

## 依据

```text
计划/20260605_外设四本能方法需求树生成因果链修订计划_v0.1.md
计划/计划索引.md
实施记录/20260606_外设四本能方法P2d观察稳定复现状态派生接入记录.md
```

P3 要求：

```text
读取外设观察存在特征集合；
读取自我所在场景存在候选集合；
按同一性值域计算二次特征；
多帧累计同一性证据；
写归属映射特征和归属状态动态；
冲突、候选切换、证据不足时输出结构化缺口。
```

## 当前代码事实

```text
自我动作实现.外设模块.ixx：
    已有 外设观察存在_自我存在同一性状态 / 同一性评分；
    已有 外设观察存在自我归属状态 / 归属目标；
    已有 外设观察存在归属建立动态 / 归属冲突动态；
    已有 同一性状态_由确认方案项、归属状态_由确认方案项、聚合同一性状态、聚合归属状态 helper；
    自我_识别外设观察材料 会读取最新逐簇识别报告作为材料；
    会读取当前场景已确认存在集合并构造外设观察存在确认方案；
    会写同一性状态、同一性评分、归属状态、归属目标和归属动态；
    运行日志显式写 不组织外设提交包=1、识别不写变化动态=1、不结算安全值=1。

提交_当前观察范围可观测单位存在对应事实：
    已消费识别正式结果中的方案取得状态、确认状态、可提交数量、冲突数量、证据不足数量；
    已用 来源动作动态 串接识别上游动作动态；
    不再读取 外设提交包候选事实数量。
```

## 已满足部分

```text
识别结果已落到同一性和归属正式特征；
识别没有直接写扫描 / 跟踪完成；
识别没有直接结算安全值 / 服务值；
外设报告仍作为材料来源和回查索引，不是正式需求目标；
提交入口已经能消费识别正式结果并串接来源动作动态。
```

## 剩余缺口

```text
1. 识别尚未读取 P2d 新增的外设观察存在稳定复现状态、观察完成状态、可复验状态；
   因此当前只要逐簇识别报告和像素簇候选验证成立，就可能进入同一性 / 归属方案生成。

2. 识别规格结果能力只声明 已同一 / 未命中已知存在、已归属 / 待建立；
   但执行路径实际可能输出 冲突 / 证据不足。

3. 多帧累计同一性证据仍未单独实现；
   P3b 只先接观察完成 / 可复验闸门，不能宣称多帧身份稳定已闭合。
```

## P3b 最小代码片

允许修改：

```text
自我动作实现.外设模块.ixx
计划/计划索引.md
实施记录/20260605_外设四本能方法P0_Codex断点清单.md
实施记录/20260606_外设四本能方法P3b识别前置观察完成闸门接入记录.md
```

拟实现：

```text
确认自我识别外设观察材料规格：
    新增条件特征 外设观察存在稳定复现状态；
    新增条件特征 外设观察存在观察完成状态；
    新增条件特征 外设观察存在可复验状态；
    补充冲突 / 证据不足结果能力。

自我_识别外设观察材料：
    从目标宿主 / 输出场景读取三项观察存在状态；
    未达到 稳定 / 已完成 / 可复验 时不生成同一性归属方案；
    输出结构化失败原因并保留正式缺口口径。
```

禁止：

```text
不得把逐簇识别报告本身写成正式识别结果；
不得把识别成功写成扫描 / 跟踪完成；
不得把 P3b 宣称为多帧同一性累计已闭合；
不得新增模拟测试、夹具测试、伪造入口或伪造动作动态。
```

验证：

```text
先写预测；
运行 git diff --check；
运行 Debug x64 构建；
不真实运行鱼巢.exe，除非后续有自然运行条件或用户明确要求。
```

## 验收状态

```text
P3a 已完成；
P3a 未修改业务代码；
P3a 未构建；
P3a 未真实运行鱼巢.exe。
```
