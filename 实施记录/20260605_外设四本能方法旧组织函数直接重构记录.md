# 20260605_外设四本能方法旧组织函数直接重构记录

## 切片

```text
来源计划：计划/20260605_外设四本能方法需求树生成因果链修订计划_v0.1.md
触发指令：删掉原来的方法函数直接重构
切片目标：
    删除 自我_识别外设观察材料 / 自我_扫描已识别区域 / 自我_跟踪指定存在 原先共用的外设提交包组织函数壳；
    将三方法直接改为识别归属、扫描发现 / 变化、跟踪状态三个正式方法；
    将四个提交入口改为读取正式结果特征，不再依赖 外设提交包组织状态_已组织。
```

## 已改代码

```text
自我动作实现.外设模块.ixx
    已删除：
        结构_外设提交包组织摘要
        外设组织_* 辅助函数
        组织外设识别提交包摘要
        组织外设扫描提交包摘要
        组织外设跟踪提交包摘要
        写入外设提交包组织摘要
        执行外设提交包组织方法

    已重构：
        自我_识别外设观察材料
            直接读取 D455 逐簇识别报告和上游像素簇候选验证状态；
            生成 外设观察存在候选确认状态 / 方案取得状态 / 方案明细 / 对应计数 / 扫描可启动状态；
            失败时派生像素簇候选验证状态补齐需求。

        自我_扫描已识别区域
            直接读取 当前观察存在 / 历史观察存在；
            首次扫描写 观察存在发现状态 和发现事实；
            有历史时写 观察存在变化结果 / 观察变化事件 / 已识别区域扫描入账状态。

        自我_跟踪指定存在
            直接读取 目标存在 或当前观察存在；
            写 目标存在跟踪状态 / 目标观察窗口状态 / 目标存在特征变化动态。

    已改提交入口：
        提交_当前观察范围可观测单位存在对应事实
            从 外设识别结果场景状态 改为 外设观察存在候选确认状态 / 方案取得状态。
        提交_观察存在发现事实
            从 外设扫描结果场景状态 改为 观察存在发现状态 / 已识别区域扫描入账状态。
        提交_观察存在特征值变化事实
            从 外设扫描结果场景状态 改为 已识别区域扫描入账状态 + 观察存在变化结果。
        提交_指定存在跟踪事实
            从 外设跟踪结果场景状态 改为 目标存在跟踪状态 / 目标观察窗口状态。
```

## 构建前预测

```text
预测：
    旧组织函数调用点已清空，删除本体后不应出现未定义引用。
    可能风险：
        新提交入口读正式结果状态后，局部变量名或状态常量若冲突，会在编译阶段暴露。
        三方法直接重构后的业务链路尚未真实运行，不能宣称需求树自然生成四方法目标已通过。

待运行：
    git diff --check
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

## 验证结果

```text
git diff --check：
    退出码 0。
    仅有工作区 LF/CRLF 提示，无空白错误。

首次构建：
    退出码 1。
    错误：
        自我动作实现.外设模块.ixx: 取或创建观察存在发现事实 找不到标识符。
        自我动作实现.外设模块.ixx: 读取提交入口指针 找不到标识符。
    原因：
        三方法直接重构后，扫描 / 跟踪方法调用了后文定义的提交辅助函数。
    修正：
        在三方法之前补充 读取提交入口指针 和 取或创建观察存在发现事实 的前置声明。

二次构建：
    命令：
        msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码 0。
    结果：
        0 个错误。
        1 个链接警告：LNK4075，因 /INCREMENTAL:NO 忽略 /EDITANDCONTINUE。
```

## 验收边界

```text
本切片只以代码证据和 Debug x64 构建为验收。
不新增模拟入口、夹具测试或 --*-check 回归命令。
不真实运行鱼巢.exe。
不宣称自我苏醒完成、内部循环全部完成或真实业务链路已通过。
结论：
    旧外设提交包组织方法函数已删除；
    三方法和四个提交入口的直接重构已通过 Debug x64 构建；
    真实业务链路仍待后续日志验证。
```
