# P10f 目标观察约束可用性原因字段删除 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-c 后续 string / 摘要 / 原因类字段专项，独立处理 `外设观察报告队列.ixx::结构_目标观察约束可用性判定::原因`。

依据文件：

- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `规范/相机外设综合工作流程规范20260607.md`
- `规范/外设观察特征与自我场景认知本能方法分层规范20260525.md`
- `计划/计划索引.md`

问题锚点：

- `外设观察报告队列.ixx::结构_目标观察约束可用性判定::原因`
- `外设观察报告队列.ixx::判断目标观察约束可用性`
- `外设观察报告队列.ixx::构造目标观察约束摘要`
- `自我动作实现.外设模块.ixx` 两处 `可用性.原因` 显示读取

当前判断：

- `原因` 文本只重复描述 `枚举_目标观察约束可用性` 状态，不应继续作为结构字段流转。
- 可用性机器状态继续由 `状态 / 可强匹配 / 可降级匹配` 承载；显示文本由 `目标观察约束可用性文本(状态)` 派生。

预测验收：

- 源码范围复扫不再存在 `判定.原因` / `可用性.原因` / `结构_目标观察约束可用性判定::原因`。
- `Debug|x64` 构建通过。
- 不宣称自我苏醒完成、初步成熟完成或外设观察链全部闭环。

## 1. 实施记录

已改文件：

- `外设观察报告队列.ixx`
- `自我动作实现.外设模块.ixx`
- `计划/计划索引.md`
- `实施记录/20260623_P10f_目标观察约束可用性原因字段删除_Codex断点清单.md`

改动结果：

- 删除 `结构_目标观察约束可用性判定::原因`。
- `判断目标观察约束可用性` 只返回 `状态 / 可强匹配 / 可降级匹配`。
- `构造目标观察约束摘要` 不再拼接 `原因=`。
- 外设模块两处失败说明不再读取 `可用性.原因`，改由 `目标观察约束可用性文本(可用性.状态)` 派生显示文本；未定义状态保留用户可读兜底显示。

验证命令与结果：

```powershell
rg -n "判定\.原因|可用性\.原因|std::string\s+原因\{\}" 外设观察报告队列.ixx 自我动作实现.外设模块.ixx
```

结果：无命中。

```powershell
git diff --check -- 外设观察报告队列.ixx 自我动作实现.外设模块.ixx 实施记录/20260623_P10f_目标观察约束可用性原因字段删除_Codex断点清单.md
```

结果：通过，无空白错误。

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo
```

结果：已成功生成，0 个警告，0 个错误。

当前判断：

- 本切片已闭合。
- 未新增函数，未删除或改名函数，本切片不需要刷新 `说明书/函数功能说明文档.md`。
- 后续仍回到 P10f-c，按外设报告证据卡其他显示字段、自检候选证据摘要 / 需求摘要、服务 / 风险安全聚合日志摘要分别专项复核。
