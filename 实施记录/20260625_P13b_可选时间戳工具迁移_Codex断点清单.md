# 20260625 P13b 可选时间戳工具迁移 Codex断点清单

## 0. 恢复摘要

当前切片已闭合。目标是处理 `任务模块.管理工作线程.ixx::私有_时间(时间戳)`。

依据：

```text
计划/计划索引.md：P13b 全局共享迁移；B 类候选逐个判定迁移 / 复用 / 保留。
实施记录/20260624_P13_函数自定义主信息分类候选表.tsv：`私有_时间` 为 B 类候选，未命中自定义主信息 / 节点 / 结构 / 协议对象。
全局共享函数类.ixx 文件头规则：无领域自定义主信息 / 领域类型参与的通用函数可进入本模块。
```

当前判断：

```text
`私有_时间` 只执行 `now != 0 ? now : 结构体_时间戳::当前_微秒()`。
该函数依赖 `时间戳` 和 `结构体_时间戳`，二者来自 `基础数据类型.h`，符合全局共享函数类包含边界。
```

已执行：

```text
1. 在 `全局共享函数类.ixx` 新增 `有效或当前时间戳_微秒(时间戳)`。
2. 给 `任务模块.管理工作线程.ixx` 补最小“文件头部规则注释模块”。
3. 删除本地 `私有_时间`，2 个调用点改为全局函数。
4. 已运行函数说明文档生成脚本和 Debug x64 构建。
```

验证证据：

```text
python .\tools\生成函数功能说明文档.py 成功；扫描源文件 127 个，函数条目 7163。
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo 成功；0 警告，0 错误。
复扫确认：`任务模块.管理工作线程.ixx` 源码中不再存在 `私有_时间` 定义或调用。
复扫确认：`有效或当前时间戳_微秒` 已出现在 `全局共享函数类.ixx`、2 个任务管理调用点和函数说明文档。
```

禁止宣称：不宣称 P13 完成，不宣称函数去重全部完成，不宣称自我苏醒完成或初步成熟完成。
