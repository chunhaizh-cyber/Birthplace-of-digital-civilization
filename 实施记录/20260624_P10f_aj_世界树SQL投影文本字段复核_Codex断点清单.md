# P10f-aj 世界树 SQL 投影文本字段复核 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-aj `世界树类.cpp::结构_世界树SQL节点行 / 结构_世界树SQL关系行` 文本字段边界复核。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_run_package_completion_condition.tsv`

复核字段：

- `结构_世界树SQL节点行::路径 / 节点类别 / 显示文本 / 主信息类型文本 / 名称文本 / 类型文本 / 值类别 / 值文本 / 辅助文本`
- `结构_世界树SQL关系行::关系名 / 目标类别 / 目标文本`

当前判断：

- 这些字段是 `世界树类::重写世界树SQL投影(...)` 的局部 SQL 投影行结构。
- `主信息类型文本 / 节点类别` 由 `枚举_主信息类型` 渲染；同一行保留 `主信息类型值`。
- `名称文本 / 类型文本` 来自语素入口显示文本；同一行保留 `名称主键 / 类型主键`。
- `值类别 / 值文本 / 辅助文本` 由特征值、状态值、场景 / 动态 / 二次特征 / 因果主信息生成展示文本。
- `关系名 / 目标类别 / 目标文本` 是世界树 SQL 关系投影的展示列；结构化列仍有宿主主键、目标主键、目标行号和序号。

检索证据：

- `世界树类.cpp` 中文本字段只在 SQL 行填充、建表 / 建视图、INSERT 脚本和根树派生 SQL 表中出现。
- `控制面板类.cpp` 读取 `fishnest.v_current_world_tree_nodes / relations` 的文本列，用于控制面板 SQL 表格、世界树子层和因果信息页展示。
- `需求类.cpp` 只在需求 SQL 展示视图中用世界树文本列生成宿主、场景、当前状态、目标状态等显示名 / 类型名。
- 控制面板 SQL 中存在 `node_kind = N'因果'` 等展示筛选，但该路径是控制面板 SQL 读模型，不作为世界树事实判断、任务推进、结算或因果匹配依据。

处理结论：

- 本切片不做代码删除或改名。
- 这些字段按 SQL / 控制面板投影文本登记边界；世界树机器事实仍由节点、主信息类型枚举、主键、特征值和引用关系承载。
- 未改代码，未运行构建。

下一步：

- 返回 P10f 最新复扫清单，复核 `数据库ADO模块.ixx::结构_ADO查询结果` 的列名 / 行集 string 向量是否只是数据库查询结果容器。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
