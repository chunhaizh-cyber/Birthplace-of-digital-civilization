# 20260621 方法类逻辑纠偏 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_方法类逻辑纠偏计划_v0.1.md` 的 P0 方法主信息类结构体清理预切片。

已复核证据：

```text
日志/20260621_M0_方法条件面候选引用扫描.txt
rg 复扫命中仅限：
    方法主信息类.h: 结构_方法条件项 / 结构_方法条件组 / 结构_方法条件面 / 条件面 字段定义
    方法类.cpp: SQL 投影 condition_group_count
    控制面板类.cpp: 条件面可用条件组数显示
```

当前判断：

```text
条件面.可用条件组集 未参与任务筹办、任务执行、参数绑定或条件判断；只剩 SQL / 控制面板展示统计。
按计划 P0.2 删除空转条件面结构，不为显示或 SQL 投影保留长期主信息结构。
```

预测：

```text
1. 删除 方法主信息类.h 中三层条件面结构及 结构_方法条件节点信息::条件面 后，条件节点仍保留 条件场景 / 条件判定索引 / 时间边界。
2. 删除 SQL 投影 condition_group_count 和控制面板条件组数展示后，不影响方法条件节点的机器判断。
3. 复扫不再命中 结构_方法条件项 / 结构_方法条件组 / 结构_方法条件面 / 条件面.可用条件组集。
4. Debug x64 构建应通过。
```

下一步：

```text
1. 删除条件面结构和 SQL / 控制面板展示字段。
2. 运行函数说明文档生成脚本。
3. 复扫候选引用、git diff --check、Debug x64 构建。
4. 用真实结果更新本断点和计划文件，再提交本闭合切片。
```

执行结果：

```text
已删除：
    方法主信息类.h:
        结构_方法条件项
        结构_方法条件组
        结构_方法条件面
        结构_方法条件节点信息::条件面
    方法类.cpp:
        结构_方法树SQL行::条件组数量
        method_main_info.condition_group_count 当前投影列
        v_current_method_tree_nodes 中的 condition_group_count 输出
        INSERT 字段和值中的 condition_group_count
    控制面板类.cpp:
        角色.条件节点.条件面.可用条件组数 展示字段

保留：
    method_tree_node.condition_group_count / method_main_info.condition_group_count 仅作为旧 SQL 投影列删除迁移语句，不参与当前投影读写。

验证：
    python .\tools\生成函数功能说明文档.py
        退出码=0；源码文件=128；函数条目=7182。
    rg 候选源码复扫
        退出码=1；`结构_方法条件项 / 结构_方法条件组 / 结构_方法条件面 / 条件面 / 可用条件组集` 在源码零命中。
    rg condition_group_count 复扫
        只剩旧 SQL 投影列删除迁移语句。
    git diff --check
        退出码=0；仅报告 LF/CRLF 工作区换行提示。
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        日志=日志/20260621_M0_方法条件面删除_build.log
        退出码=0；已成功生成；0 个错误。
```

P0.2 结论：

```text
已闭合。条件面空转结构和只为显示 / SQL 投影存在的条件组数量字段已删除。
下一切片转入 P0.3：审计 `公共.节点种类 =` 双事实源写入口。
```

## 1. P0.3 节点种类双事实源审计

预测：

```text
1. `公共.节点种类 =` 写入口应主要集中在 方法主信息类 构造函数和 方法类 内部补齐路径。
2. 若发现外部类直接写入，先登记判定表，再转入独立回迁切片。
3. 本切片只生成判定表，不删改源码，不构建。
```

执行结果：

```text
扫描命令：
    rg -n -P "公共\.节点种类\s*=(?!=)" -g "*.h" -g "*.cpp" -g "*.ixx" --glob "!x64/**" --glob "!Debug/**" --glob "!.git/**" .

命中：
    总命中 7 处。
    方法主信息类.h 构造期设置 3 处。
    方法类.cpp 内部归一化 3 处。
    本能方法类.cpp 外部直接改写 1 处。

判定表：
    实施记录/20260621_方法节点种类双事实源审计表.md
```

P0.3 结论：

```text
审计表已生成。
外部直接改写阻塞节点种类收口，下一切片转入 P0.3a：回迁 本能方法类.cpp:358。
```

## 2. P0.3a 本能方法类外部直接写节点种类回迁

预测：

```text
1. `本能方法类.cpp:358` 位于私有_补齐本能方法结构。
2. 该函数只由私有_查找或创建本能方法首节点调用；新节点来自 方法主信息类::构造首节点，已有节点来自按本能方法首节点过滤的查找路径。
3. 因此 `主信息.公共.节点种类 = 枚举_方法节点种类::方法首节点;` 是冗余外部写入，可直接删除。
4. 删除后复扫 `公共.节点种类 =` 应只剩 方法主信息类 构造期设置和 方法类 内部归一化，不再有外部直接改写。
5. Debug x64 构建应通过。
```

执行结果：

```text
已改：
    本能方法类.cpp:
        删除 私有_补齐本能方法结构 中冗余的 主信息.公共.节点种类 = 枚举_方法节点种类::方法首节点。

验证：
    python .\tools\生成函数功能说明文档.py
        退出码=0；源码文件=128；函数条目=7182。
    rg -n -P "公共\.节点种类\s*=(?!=)" ...
        只剩 方法主信息类.h 构造期设置 3 处、方法类.cpp 内部归一化 3 处。
        本能方法类.cpp 外部直接改写已清零。
    git diff --check
        退出码=0；仅报告 LF/CRLF 工作区换行提示。
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        日志=日志/20260621_P0.3a_本能方法节点种类回迁_build.log
        退出码=0；已成功生成；0 个错误。
```

P0.3a 结论：

```text
已闭合。本能方法类不再裸写 方法主信息类::公共.节点种类。
下一切片转入 P1：方法信息操作入口审计。
```

## 3. P1 方法信息操作入口审计

预测：

```text
1. 方法类 / 方法主信息类 / 本能方法类 内部实现会有保留项。
2. 任务执行、任务筹办、动态类、自我动作实现、自检线程、控制面板和摘要线程可能仍有直接读取 方法主信息类 字段或遍历方法子链的路径。
3. 本切片只生成判定表，不先改源码；若发现明确机器逻辑回迁点，转入后续最小实现切片。
4. 控制面板 / SQL / 日志显示层可后置，但不得反向参与机器判断。
```

执行结果：

```text
扫描输出：
    日志/20260621_P1_方法主信息直接操作扫描.txt
    日志/20260621_P1_方法树遍历扫描.txt

统计：
    直接方法信息命中最高文件：
        方法类.cpp=215
        自我动作实现模块.ixx=204
        方法虚拟存在服务类.cpp=107
        自我动作实现.外设模块.ixx=105
        任务模块.筹办.ixx=69
        控制面板类.cpp=66
        任务模块.执行.ixx=47
    方法树遍历 / 结构计数命中最高文件：
        任务模块.执行.ixx=56
        任务模块.筹办.ixx=41
        任务模块.管理工作线程.ixx=17

判定表：
    实施记录/20260621_方法信息操作入口判定表.md
```

P1 结论：

```text
已闭合入口审计。
第一优先实现切到 P2：任务执行最小 helper 回迁。
理由：任务模块.执行.ixx 同时存在直接字段读取和方法树子链遍历，并直接参与方法可执行性、缺口判断和运行事实写入。
```

## 4. P2 任务执行最小 helper 回迁

预测：

```text
1. 方法类补齐只读 helper：动作名、动作句柄、本能ID、结构计数、首个条件场景、最低结构状态和可用最低判据。
2. 任务模块.执行.ixx 中同功能私有桥函数改为调用方法类。
3. 运行时本能动作表查询仍保留在任务执行层，不迁入方法类。
4. 复扫任务执行侧不再直接读取 方法主信息类::首节点信息 / 公共.节点种类 / 子链遍历。
5. Debug x64 构建应通过。
```

执行结果：

```text
已改：
    方法类.h / 方法类.cpp:
        新增 方法动作名 / 方法动作句柄 / 方法本能ID / 方法指定本能方法。
        新增 方法动作数量 / 方法可执行入口数量 / 方法条件节点数量 / 方法结果节点数量 / 方法条件结果对数量。
        新增 方法首个条件场景 / 方法最低结构状态值 / 方法满足可用最低判据。
    任务模块.执行.ixx:
        动作名、本能ID、首个条件场景、方法结构计数改为调用 方法类。
        运行时本能动作表查询仍留在任务执行层。

验证：
    python .\tools\生成函数功能说明文档.py
        退出码=0；源码文件=128；函数条目=7205。
    rg 复扫任务模块.执行.ixx
        `主信息.首节点信息 / 取首节点信息 / 公共.节点种类 / 能力.结果包.结果项集 / 动作句柄 / 动作名 / 方法虚拟存在 / 方法->子 / 方法->下` 零命中。
    git diff --check
        退出码=0；仅报告 LF/CRLF 工作区换行提示。
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        日志=日志/20260621_P2_任务执行方法helper回迁_build.log
        退出码=0；已成功生成；0 个错误。
```

P2 结论：

```text
已闭合。任务执行侧不再直接解释方法主信息和方法子链结构。
下一切片转入 P3：任务筹办候选方法 helper 回迁。
```

## 5. P3a 任务筹办方法只读 helper 回迁

预测：

```text
1. 方法类补齐方法首节点角色、动作存在、来源、成熟度阶段只读 helper。
2. 任务模块.筹办.ixx 中本能ID、动作句柄、结构计数、方法虚拟存在读取改为调用 方法类 / 方法虚拟存在服务类。
3. 不迁移方法结果能力命中循环；该逻辑依赖任务来源需求、目标特征集合和方向适配，作为 P3b 独立切片处理。
4. 复扫任务筹办侧应不再命中本轮已替换的动作句柄、本能ID、成熟度阶段、方法虚拟存在直读和执行桥计数。
5. Debug x64 构建应通过。
```

执行结果：

```text
已改：
    方法类.h / 方法类.cpp:
        新增 方法是首节点 / 方法有动作 / 方法来源 / 方法成熟度阶段 只读 helper。
    任务模块.筹办.ixx:
        方法本能ID / 方法原始本能ID值改为调用 方法类::方法本能ID。
        运行时空占位、候选评分、练习目标预检、不可就绪状况和缺口日志中的方法结构计数改为调用 方法类。
        方法虚拟存在读取改为调用 方法虚拟存在服务类::取或创建方法虚拟存在。
        日志动作句柄读取改为调用 方法类::方法动作句柄。

验证：
    python .\tools\生成函数功能说明文档.py
        退出码=0；源码文件=128；函数条目=7213。
    rg P3a 目标复扫
        日志=日志/20260621_P3a_任务筹办方法helper回迁_复扫.txt
        退出码=1；本轮目标直读零命中。
    git diff --check
        退出码=0；仅报告 LF/CRLF 工作区换行提示。
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        日志=日志/20260621_P3a_任务筹办方法helper回迁_build.log
        退出码=0；已成功生成；0 个错误。
```

P3a 结论：

```text
已闭合。任务筹办侧本轮可低风险回迁的动作入口、结构计数、方法虚拟存在和日志动作句柄读取已收口。
下一切片转入 P3b：方法结果能力匹配 helper 回迁，并清理机器查找 / 生成逻辑中使用 语素集.安全获取词(...) 文本的问题。
```

## 6. P3b 方法结果能力读取 helper 回迁

预测：

```text
1. 方法类只补结果项只读入口和结果能力查找属性设置入口，不接管需求方向、目标特征集合展开和目标值评分。
2. 任务模块.筹办.ixx 中 `能力.结果包.结果项集` 遍历改为经 方法类 读取；历史写入原语标记改为经 方法类 写入口。
3. 本切片先复核 `语素集.安全获取词(...)` 命中来源；仅处理进入机器查找 / 生成字段的路径，日志 / SQL / 控制面板显示不作为机器判断。
4. 复扫任务筹办侧不再直接命中 `取首节点信息()` / `能力.结果包.结果项集` 的机器逻辑。
5. Debug x64 构建应通过。
```

执行结果：

```text
已改：
    方法类.h / 方法类.cpp:
        新增 方法结果项集 只读入口。
        新增 设置方法结果能力查找属性 写入口。
    任务模块.筹办.ixx:
        方法结果能力命中任务需求 / 方法结果能力命中精度 / 方法结果能力需要目标执行值 改为经 方法类::方法结果项集 遍历。
        历史特征写入入口标记改为经 方法类::设置方法结果能力查找属性。
        按方法主键查找中的方法首节点判断改为 方法类::方法是首节点。
    自我线程模块.impl.cpp:
        `私有_目标特征类型命中任一_线程侧` 改为接收语素入口集合，不再接收中文字符串集合。
        新增线程侧特征入口集，机器判断通过具体特征入口比较。
        服务因果链 / 间接服务维护条件构造的检查特征改为传入特征入口指针。

验证：
    python .\tools\生成函数功能说明文档.py
        退出码=0；源码文件=128；函数条目=7218。
    rg 任务筹办结果能力复扫
        日志=日志/20260621_P3b_任务筹办结果能力helper回迁_复扫.txt
        退出码=1；`取首节点信息()` / `能力.结果包.结果项集` 等 P3b 目标零命中。
    rg 自我线程词面机器判断复扫
        日志=日志/20260621_P3b_自我线程特征词面机器判断_复扫.txt
        退出码=1；`私有_目标特征类型命中任一_线程侧` 字符串列表 / 检查特征字符串参数零命中。
    git diff --check
        退出码=0；仅报告 LF/CRLF 工作区换行提示。
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        日志=日志/20260621_P3b_任务筹办结果能力helper回迁_build.log
        退出码=0；已成功生成；0 个错误。
```

P3b 结论：

```text
已闭合。任务筹办侧结果能力读取已回迁到方法类；当前定位到的机器判断字符串列表已改为特征入口集合比较。
下一切片转入 P4：需求类旧 helper 回迁，复查方法引用解析和方法结果能力判断。
```

## 7. P4 需求类旧 helper 回迁复查

预测：

```text
1. 需求类中若仍有方法主信息直读、结果能力项遍历或方法节点裸解析，应回迁到方法类。
2. 若只剩方法类入口调用，本切片作为审计闭合，不新增无意义包装。
3. 本切片不改源码时不运行构建，只记录复扫证据。
```

执行结果：

```text
扫描：
    rg 方法主信息直读候选 需求类.cpp 需求类.h
        日志=日志/20260621_P4_需求类方法helper回迁_复扫.txt
        退出码=1；旧 helper / 方法主信息直读 / 结果项集遍历零命中。
    rg 方法类入口确认 需求类.cpp 需求类.h
        日志=日志/20260621_P4_需求类方法类入口确认.txt
        退出码=0；仅剩：
            需求类.cpp:803 方法类::方法树存在生产目标特征
            需求类.cpp:841 方法类::方法首节点生产目标特征
```

P4 结论：

```text
已闭合。需求类旧 helper 已无可回迁源码，当前保留的方法判断均经方法类入口。
下一切片转入 P5：动态类 / 语言类只读动作名回迁。
```

## 8. P5 动态类 / 语言类只读动作名回迁

预测：

```text
1. 动态类中来源方法动作名、动作句柄有效性和动态来源动作名回填应改为调用 方法类 只读入口。
2. 语言类方法节点命名只保留显示职责，但不得直接读取方法首节点动作名。
3. 复扫动态类 / 语言类不再直接命中方法首节点信息或方法主信息字段。
4. Debug x64 构建应通过。
```

执行结果：

```text
已改：
    动态类.cpp:
        私有_来源方法动作名 改为 方法类::方法动作名。
        私有_方法有可执行动作入口 改为 方法类::方法是首节点 + 方法类::方法动作句柄。
        创建 / 同步动态来源动作名时改为 方法类::方法动作名。
    语言类_基础信息命名.cpp / 语言类_二次特征表达.cpp:
        方法节点名称显示改为经 方法类::方法动作名 读取动作名入口。

验证：
    python .\tools\生成函数功能说明文档.py
        退出码=0；源码文件=128；函数条目=7218。
    rg 动态语言方法动作名回迁复扫
        日志=日志/20260621_P5_动态语言方法动作名回迁_复扫.txt
        退出码=0；唯一命中为 方法类::方法动作句柄 调用，不是方法主信息直读。
    rg 动态语言方法主信息直读复扫
        日志=日志/20260621_P5_动态语言方法主信息直读_复扫.txt
        退出码=1；方法主信息直读零命中。
    git diff --check
        退出码=0；仅报告 LF/CRLF 工作区换行提示。
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        日志=日志/20260621_P5_动态语言方法动作名回迁_build.log
        退出码=0；已成功生成；0 个错误。
```

P5 结论：

```text
已闭合。动态类和语言类当前只通过方法类入口读取动作名 / 动作句柄。
下一切片转入 P6：自我动作实现模块写入口回迁。
```

## 9. P6 自我动作实现模块写入口回迁

预测：

```text
1. `自我动作实现模块.ixx` 和 `自我动作实现.外设模块.ixx` 中直接追加 `能力.结果包.结果项集` 的路径应回迁到方法类结果能力写入口。
2. `提交方法可执行入口` 中直接设置目标方法动作句柄 / 动作名 / 来源的路径应回迁到方法类本能动作入口写入口。
3. 自我动作实现模块仍负责动作输入、输出、动态和特征事实写入；方法类只接管方法首节点主信息写入，不承接动作业务语义。
4. 复扫两个自我动作实现文件时，P6 目标写入口不再直接命中；Debug x64 构建应通过。
```

当前扫描锚点：

```text
自我动作实现模块.ixx:
    3137-3174 / 3178-3209：确保方法结果能力I64 / 方向 直接读写首节点能力包。
    11207-11221：提交方法可执行入口直接设置动作句柄、来源、动作名。
自我动作实现.外设模块.ixx:
    8335-8372 / 8376-8407：外设侧同名结果能力 helper 直接读写首节点能力包。
```

执行结果：

```text
已改：
    方法类.h / 方法类.cpp:
        新增 方法类::声明方法结果能力。
        新增 方法类::设置方法本能动作入口。
        方法结果项去重补入目标值比较，避免同一特征不同目标值被误合并。
    自我动作实现模块.ixx:
        确保方法结果能力I64 / 方向 改为构造结构化结果项后调用 方法类::声明方法结果能力。
        确认提交方法可执行入口规格中的方法动作状态结果能力声明改为 方法类::声明方法结果能力。
        提交方法可执行入口的动作句柄 / 动作名 / 来源写入改为 方法类::设置方法本能动作入口。
        相邻动作名、动作句柄、本能ID、结果项集读取改为 方法类 只读入口。
    自我动作实现.外设模块.ixx:
        外设侧同名结果能力 helper 改为 方法类::声明方法结果能力。
        相邻本能ID、动作名读取改为 方法类 只读入口。

验证：
    python .\tools\生成函数功能说明文档.py
        退出码=0；源码文件=128；函数条目=7220。
    rg 自我动作方法写入口回迁复扫
        日志=日志/20260621_P6_自我动作方法写入口回迁_复扫.txt
        退出码=1；`能力.结果包.结果项集` / `取首节点信息()` / `首节点信息().动作句柄` / `首节点信息().动作名` 在两个自我动作实现文件零命中。
    rg 自我动作方法类入口确认
        日志=日志/20260621_P6_自我动作方法类入口确认.txt
        退出码=0；确认调用 方法类::声明方法结果能力、设置方法本能动作入口、方法结果项集、方法动作名、方法本能ID。
    git diff --check
        退出码=0；仅报告 LF/CRLF 工作区换行提示。
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        日志=日志/20260621_P6_自我动作方法写入口回迁_build.log
        退出码=0；已成功生成。
```

P6 结论：

```text
已闭合。自我动作实现模块和外设模块当前不再直接读写 P6 目标方法首节点主信息字段；方法结果能力和本能动作入口写入已收口到方法类。
下一切片转入 P7：控制面板和摘要线程显示层回迁。
```

## 10. P7 控制面板和摘要线程显示层回迁

预测：

```text
1. 控制面板类和控制面板摘要线程中用于显示标题、字段、摘要、计数的直接方法主信息读取，应回迁到方法类只读入口。
2. 若发现任务管理机器逻辑直接读取方法结果能力或本能动作句柄，不混入本显示层切片，先登记为后续任务类缺口。
3. P7 不改变控制面板显示结构，不把显示文本作为机器判断依据。
4. 复扫控制面板 / 摘要线程显示文件时，动作名、动作句柄、结果能力集、方法虚拟存在等方法首节点主信息直读应收束到方法类入口；Debug x64 构建应通过。
```

当前扫描锚点：

```text
控制面板类.cpp:
    方法标题、方法主信息详情、方法树节点字段、方法能力方向收集、方法虚拟存在解析存在直接方法主信息读取。
控制面板摘要线程模块.impl.cpp:
    方法运行账时间、方法树摘要、最近方法标题存在直接方法主信息读取。
任务模块.管理界面线程.impl.cpp:
    `私有_外设观察需求口径_按方法供料要求` 和本能方法匹配 / 能力值读取属于任务管理机器逻辑，不纳入 P7 显示层；后续按任务类缺口处理。
```

执行结果：

```text
已改：
    方法类.h / 方法类.cpp:
        新增方法节点种类、首个结果能力特征、结果能力查找属性、方法虚拟存在引用 / 只读解析、来源任务、父方法、前置方法、后续方法、是否允许自动查找等只读入口。
    控制面板类.cpp:
        方法标题、方法骨架、候选方法能力方向收集、方法主信息详情、方法树节点字段、方法虚拟存在解析改为方法类只读入口。
    控制面板摘要线程模块.impl.cpp:
        方法运行账时间、方法结构摘要、方法补齐问题统计、最近方法标题改为方法类只读入口。
    计划/20260620_任务类逻辑纠偏计划_v0.1.md:
        登记 P7 初扫发现的任务管理机器逻辑直读，后续按任务类计划处理；未混入本显示层切片。

验证：
    python .\tools\生成函数功能说明文档.py
        退出码=0；源码文件=128；函数条目=7248。
    rg 控制面板摘要方法显示回迁复扫
        日志=日志/20260621_P7_控制面板摘要方法显示回迁_复扫.txt
        退出码=1；控制面板 / 摘要线程中 `首节点信息()`、`取首节点信息()`、`能力.结果包.结果项集`、方法动作名 / 动作句柄 / 方法虚拟存在直读零命中。
    rg 控制面板摘要方法类入口确认
        日志=日志/20260621_P7_控制面板摘要方法类入口确认.txt
        退出码=0；确认显示层调用方法类只读入口。
    git diff --check
        退出码=0；仅报告 LF/CRLF 工作区换行提示。
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        日志=日志/20260621_P7_控制面板摘要方法显示回迁_build.log
        退出码=0；已成功生成。
```

P7 结论：

```text
已闭合。控制面板和摘要线程显示层当前通过方法类只读入口消费方法信息；任务管理机器逻辑直读已登记到任务类计划，未阻塞当前方法类显示层切片。
下一切片转入 P8：函数说明文档、扫描复查、Debug x64 构建。
```

## 11. P8 复扫与追加小切片

初扫结果：

```text
日志/20260621_P8_方法类逻辑纠偏_外部直读复扫.txt
    仍命中多个外部文件，不满足“当前可执行切片收束”条件。

已分类：
    任务模块.管理界面线程.impl.cpp 的任务管理机器逻辑直读已登记到任务类计划 6.4。
    方法虚拟存在服务类.cpp / 本能方法类.cpp 属方法域服务和本能方法注册侧，后续按方法域服务内部边界单独审查。

当前追加小切片：
    自我动作实现.扫描.ixx:1288-1317 直接追加方法结果能力项。
    自我动作实现.扫描.ixx:1322-1327 直接读取方法动作名。
```

预测：

```text
1. 扫描模块的 `确保方法结果能力I64` 可改为构造结构化结果项后调用 方法类::声明方法结果能力。
2. 扫描模块的 `方法动作名` 可直接转调 方法类::方法动作名。
3. 修改后 `自我动作实现.扫描.ixx` 不再命中 `取首节点信息()` / `能力.结果包.结果项集` / 动作名直读。
4. Debug x64 构建应通过。
```

执行结果：

```text
已改：
    自我动作实现.扫描.ixx:
        确保方法结果能力I64 改为 方法类::声明方法结果能力。
        局部 方法动作名 helper 改为 方法类::方法动作名。

验证：
    rg 扫描模块方法结果能力回迁复扫
        日志=日志/20260621_P8a_扫描模块方法结果能力回迁_复扫.txt
        退出码=1；扫描模块中 `取首节点信息()` / `能力.结果包.结果项集` / 方法动作名直读零命中。
    rg 扫描模块方法类入口确认
        日志=日志/20260621_P8a_扫描模块方法类入口确认.txt
        退出码=0；确认调用 方法类::声明方法结果能力、方法类::方法动作名。
    git diff --check
        退出码=0；仅报告 LF/CRLF 工作区换行提示。
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        日志=日志/20260621_P8a_扫描模块方法结果能力回迁_build.log
        退出码=0；已成功生成。
```

追加小切片 P8b：

```text
锚点：
    本能动作管理类.ixx:280-285
    本能动作管理类.ixx:719-742

预测：
    1. 方法首节点判断改为 方法类::方法是首节点。
    2. 方法动作入口判断改为 方法类::方法有动作、方法动作句柄、方法动作名。
    3. 方法虚拟存在读取优先用 方法类::方法虚拟存在只读；缺失时仍由服务层显式创建。
    4. Debug x64 构建应通过。

执行结果：
    本能动作管理类.ixx 已改为调用 方法类::方法是首节点、方法虚拟存在只读、方法有动作、方法动作句柄、方法动作名。
    复扫日志=日志/20260621_P8b_本能动作管理方法读取回迁_复扫.txt，退出码=1；目标直读零命中。
    入口确认日志=日志/20260621_P8b_本能动作管理方法类入口确认.txt，退出码=0。
    构建日志=日志/20260621_P8b_本能动作管理方法读取回迁_build.log，退出码=0；已成功生成。
```

追加小切片 P8c：

```text
锚点：
    自我动作实现模块.ixx:方法虚拟存在 / 方法节点种类 / 方法动作数量 / 条件结果对统计 / 任务状态提交来源方法判定。
    自我动作实现.外设模块.ixx:方法虚拟存在 / 方法节点种类 / 条件结果对统计。

预测：
    1. 已有方法类入口可覆盖本切片：方法节点种类、方法有动作、方法虚拟存在只读、方法条件节点数量、方法条件结果对数量。
    2. 两个自我动作实现模块中的 `主信息.节点种类()` / `主信息.有方法动作()` / `首节点信息().方法虚拟存在` 目标直读应清零。
    3. 删除本地重复统计 helper 后需要重新生成函数功能说明文档。
    4. Debug x64 构建应通过。

执行结果：
    自我动作实现模块.ixx 已回迁 方法虚拟存在 / 方法节点种类 / 方法动作判断 / 条件与条件结果对统计读取。
    自我动作实现.外设模块.ixx 已回迁 方法虚拟存在 / 方法节点种类 / 条件与条件结果对统计读取。
    删除本地重复统计 helper：
        自我动作实现模块.ixx::方法真实条件节点数量
        自我动作实现模块.ixx::方法真实条件结果对数量
        自我动作实现.外设模块.ixx::方法真实条件节点数量
        自我动作实现.外设模块.ixx::方法真实条件结果对数量
    python .\tools\生成函数功能说明文档.py
        退出码=0；源码文件=128；函数条目=7244。
    目标直读复扫
        日志=日志/20260621_P8c_自我动作方法读取回迁_复扫.txt
        退出码=1；目标直读零命中。
    方法类入口确认
        日志=日志/20260621_P8c_自我动作方法类入口确认.txt
        退出码=0。
    git diff --check
        退出码=0；仅报告 LF/CRLF 工作区换行提示。
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        日志=日志/20260621_P8c_自我动作方法读取回迁_build.log
        退出码=0；已成功生成；0 个错误。
```

追加小切片 P8d：

```text
锚点：
    自检线程模块.impl.cpp:方法练习目标判断、方法虚拟存在只读解析、方法首节点枚举、方法结构统计、方法动作判断、方法标题、缺口候选来源能力值、休眠方法虚拟存在完整性检查。

预测：
    1. 自检线程中动作句柄 / 本能ID / 动作名 / 首个结果能力 / 方法虚拟存在 / 节点种类 / 条件和结果统计读取可回迁到现有方法类入口。
    2. 结果节点命中次数统计仍需读取结果节点统计字段，但节点角色判断必须改为 方法类::方法节点种类。
    3. `自检线程模块.impl.cpp` 中目标方法主信息直读应清零。
    4. Debug x64 构建应通过。

执行结果：
    自检线程模块.impl.cpp 已改为调用 方法类::方法动作句柄、方法有动作、方法虚拟存在只读、方法虚拟存在引用、方法节点种类、方法条件节点数量、方法结果节点数量、方法动作名、方法首个结果能力特征类型、方法本能ID。
    rg 目标直读复扫
        日志=日志/20260621_P8d_自检线程方法读取回迁_复扫.txt
        退出码=1；目标直读零命中。
    rg 方法类入口确认
        日志=日志/20260621_P8d_自检线程方法类入口确认.txt
        退出码=0。
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        日志=日志/20260621_P8d_自检线程方法读取回迁_build.log
        退出码=0；已成功生成；0 个错误。
```

追加小切片 P8e：

```text
锚点：
    自我类.cpp:本能方法首节点查找、方法条件结果对镜像统计、方法可用镜像口径、来源方法动作名绑定、可用方法数量镜像统计。

预测：
    1. 自我类中的方法节点种类、动作句柄、动作名和方法虚拟存在读取可回迁到现有方法类入口。
    2. 自我类仍只负责自我镜像统计和动态记录，不新增方法类职责或自我类桥接入口。
    3. `自我类.cpp` 中目标方法主信息直读应清零。
    4. Debug x64 构建应通过。

执行结果：
    自我类.cpp 已改为调用 方法类::方法节点种类、方法动作句柄、方法动作名、方法虚拟存在只读。
    rg 目标直读复扫
        日志=日志/20260621_P8e_自我类方法读取回迁_复扫.txt
        退出码=1；目标直读零命中。
    rg 方法类入口确认
        日志=日志/20260621_P8e_自我类方法类入口确认.txt
        退出码=0。
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        日志=日志/20260621_P8e_自我类方法读取回迁_build.log
        退出码=0；已成功生成；0 个错误。
```

追加小切片 P8f：

```text
锚点：
    任务模块.执行.ixx:执行桥方法首节点确认。
    任务模块.管理工作线程.ixx:方法自我能力值、学习承接本能判断、派生需求 / 执行结果方法虚拟存在同步、执行入口日志本能ID。
    任务模块.管理界面线程.impl.cpp:外设观察需求口径结果能力读取、本能方法首节点匹配、方法自我能力值、回执方法虚拟存在取得。
    自我线程模块.impl.cpp:按方法虚拟存在引用反查方法首节点。

预测：
    1. 上述文件中目标方法主信息直读可回迁到现有方法类入口。
    2. 不改变任务管理协议字段和自我线程消息结构，只替换方法信息读取来源。
    3. 四个文件目标直读应清零。
    4. Debug x64 构建应通过。

执行结果：
    任务模块.执行.ixx 已改为通过 方法类::方法是首节点 / 方法动作句柄 判断执行桥方法入口。
    任务模块.管理工作线程.ixx 已改为通过 方法类::方法动作句柄、方法本能ID、方法虚拟存在只读读取方法信息。
    任务模块.管理界面线程.impl.cpp 已改为通过 方法类::方法是首节点、方法结果项集、方法动作句柄、方法虚拟存在只读读取方法信息。
    自我线程模块.impl.cpp 已改为通过 方法类::方法虚拟存在引用反查方法首节点。
    rg 目标直读复扫
        日志=日志/20260621_P8f_任务自我线程方法读取回迁_复扫.txt
        退出码=1；目标直读零命中。
    rg 方法类入口确认
        日志=日志/20260621_P8f_任务自我线程方法类入口确认.txt
        退出码=0。
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        日志=日志/20260621_P8f_任务自我线程方法读取回迁_build.log
        退出码=0；已成功生成；0 个错误。
```

追加小切片 P8g：

```text
锚点：
    本能方法类.cpp:默认本能方法首节点查找、可执行判断、默认本能方法结构补齐、公开补齐入口、新建默认方法节点。

预测：
    1. 本能方法类仍负责默认本能方法种子、动作名称和分类特征。
    2. 方法首节点种类、动作句柄、动作名、方法动作存在、方法虚拟存在绑定和本能动作入口写入改为调用方法类入口。
    3. `本能方法类.cpp` 中目标方法主信息直读应清零。
    4. Debug x64 构建应通过。

执行结果：
    本能方法类.cpp 已改为调用 方法类::方法节点种类、方法动作句柄、方法动作名、方法是首节点、方法有动作、设置方法本能动作入口，并通过 方法虚拟存在服务类::取或创建方法虚拟存在 取得方法虚拟存在。
    rg 目标直读复扫
        日志=日志/20260621_P8g_本能方法类方法读取写入回迁_复扫.txt
        退出码=1；目标直读零命中。
    rg 方法类 / 方法虚拟存在服务入口确认
        日志=日志/20260621_P8g_本能方法类方法类入口确认.txt
        退出码=0。
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        日志=日志/20260621_P8g_本能方法类方法读取写入回迁_build.log
        退出码=0；已成功生成；0 个错误。
```

追加小切片 P8h：

```text
锚点：
    方法虚拟存在服务类.cpp:方法首节点归一、方法类型和成熟度推导、方法概念补齐、身份默认特征同步、方法状态同步、条件 / 结果场景读取、结构特征同步。

预测：
    1. 方法虚拟存在服务类仍负责方法虚拟存在特征写入、概念补齐和结构镜像同步。
    2. 方法首节点种类、动作句柄、动作名、来源、成熟度、自动查找、方法关系引用和结果能力项读取改为调用方法类只读入口。
    3. `方法虚拟存在服务类.cpp` 中目标方法主信息直读应清零。
    4. Debug x64 构建应通过。

执行结果：
    方法虚拟存在服务类.cpp 已改为调用 方法类::方法节点种类、方法动作句柄、方法来源、方法动作名、方法虚拟存在只读、方法成熟度阶段、方法允许自动查找、方法父方法引用、方法前置方法引用、方法后续方法引用、方法结果项集等只读入口。
    rg 目标直读复扫
        日志=日志/20260621_P8h_方法虚拟存在服务方法读取回迁_复扫.txt
        退出码=1；目标直读零命中。
    rg 方法类入口确认
        日志=日志/20260621_P8h_方法虚拟存在服务方法类入口确认.txt
        退出码=0。
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
        日志=日志/20260621_P8h_方法虚拟存在服务方法读取回迁_build.log
        退出码=0；已成功生成；0 个错误。
```

禁止宣称：

```text
不宣称方法类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
