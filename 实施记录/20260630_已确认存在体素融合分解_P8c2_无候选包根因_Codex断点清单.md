# 20260630 已确认存在体素融合分解 P8c2 无候选包根因断点清单

## 0. 恢复摘要

- 当前切片：R23 / P8c2，追查 `voxelCandidatePackageCount=0` 的根因。
- 用户纠正：没有自然候选包不能作为终点，必须继续追“为什么没有”。
- 当前判断：P8c 的诊断字段只能证明候选包为 0，不能证明自然链无问题，也不能证明体素子轮廓提取已经成功。
- 上级依据：
  - `规范/已确认存在体素融合与分解规范20260630.md`
  - `规范/场景体素服务与视觉先验融合规范20260625.md`
  - `docs/工程图谱/05_规则原子索引.md`
- 核心规则：
  - `RULE-VOXEL-FUSION-001`：体素加工只能由已确认存在新增未消费观察体素特征值触发。
  - `RULE-VOXEL-FUSION-008`：体素加工线程只能输出候选，不能直接写正式事实。
  - `RULE-VISION-PRIOR-003`：业务入口不得绕过合法材料层。
- 禁止宣称：
  - 不宣称子轮廓提取成功。
  - 不宣称 P8d / P8e / P9 完成。
  - 不用外设原始帧、控制面板刷新、场景体素快照或伪造样本触发体素加工。

## 1. 已知证据

- P8c 已接入控制面板 / self-scene 诊断字段，并取得 Debug x64 构建和自我场景截图运行证据。
- 真实运行解析结果为：
  - `voxelCandidatePackageCount=0`
  - `voxelCandidateLatestChildContourCount=0`
  - `voxelCandidateLatestLocalChildVoxelCount=0`
  - `voxelCandidateDiagnosticBranch=0`
- 该证据只说明当前没有体素加工候选结果包；不能停止在“待自然样本”，还需定位自然链断点。

## 2. 本轮追查路线

1. 查 `视觉观察融合模块.ixx` 中体素加工线程 API 是否有非显示路径调用。
2. 查是否存在“新增观察体素特征值 -> 结构_体素加工任务 -> 提交体素加工任务”的任务生成器。
3. 查 6146 / 6150 观察链是否写入了规范要求的新增观察体素特征值，或只写了当前体素 / 场景体素 / 诊断显示。
4. 若断点是缺触发桥，给出并实施最小桥接方案；若断点是合法观察体素特征值未产生，则继续追 6146 / 6150 的写入路径。

## 3. 当前停止点

- 调用面检索第一结论：
  - `视觉观察融合模块.ixx` 定义了 `启动体素加工工作线程`、`提交体素加工任务` 和候选包读取接口。
  - 当前非定义调用只发现 `控制面板类.cpp` 读取候选包诊断、`自我动作实现.外设模块.ixx` 读取候选包用于颜色候选匹配。
  - 尚未发现任何业务路径调用 `启动体素加工工作线程` 或 `提交体素加工任务`。
- 当前判断：
  - 候选包为 0 的首要断点不是子轮廓算法失败，而是体素加工任务未被自然提交。
  - 需要继续确认 6146 / 6150 观察链是否已经写入“新增观察体素特征值”，以及是否缺少该特征值到 `结构_体素加工任务` 的消费桥。
- 下一步：
  - 检查 `自我动作实现.外设模块.ixx` 中观察存在三维体素写入和候选读取位置。
  - 检查 `视觉观察融合模块.ixx` 线程是否需要显式启动，确认无任务情况下候选包自然为 0。

## 4. 2026-06-30 15:15 追因更新

- 已补代码桥：
  - `自我动作实现.外设模块.ixx` 中 `写入观察存在三维体素特征` 已调用 `提交观察体素加工任务_由观察簇`。
  - `提交观察体素加工任务_由观察簇` 已调用 `视觉观察融合模块::提交体素加工任务`，边界仍是 worker 只产候选，不写事实。
- 已验证：
  - `日志/20260630_P8c2_voxel_candidate_bridge_msbuild.log`：Debug x64 构建通过，0 警告，0 错误。
  - 15:07 新桥运行窗口中，`外设观察等待项创建延后` 出现 5 次。
  - 同一 15:07 窗口中，以下关键节点均为 0：`按外设观察等待项启动D455线程`、`D455深度相机/逐簇识别报告入队`、`外设等待项巡检生成上行`、`任务管理外设消息承接/生成观察事实承接请求`、`自我线程/外设观察事实承接完成`、`体素观察证据`、`体素加工`。
- 对照窗口：
  - 13:17 旧运行窗口曾出现 D455 报告、等待项匹配、观察事实承接完成，但该窗口早于 P8c2 桥接构建，不足以验证新桥。
- 当前判断：
  - 最新 `voxelCandidatePackageCount=0` 的直接原因不是子轮廓算法失败，而是新桥运行窗口未越过外设等待项创建。
  - 15:07 命令等待窗口过短，进程停止早于 D455 报告和观察事实承接。
- 下一步预测：
  - 用更长等待窗口复跑时，若 D455 报告和观察事实承接出现，新桥应至少提交体素加工任务；若仍无候选包，再追写入观察体素特征或 worker 出包。
  - 若长窗口仍没有 D455 报告或等待项匹配，则根因转入外设等待项启动 / D455 报告生产 / 等待项匹配层。

## 5. 2026-06-30 15:25 长窗口追因更新

- 已确认 15:18 长窗口不是“没有自然候选”：
  - `日志/鱼巢_run_20260630.低值g:110814-110820`：D455 报告、扫描变化报告、等待项匹配、观察事实承接请求均出现。
  - `日志/鱼巢_run_20260630.低值g:111357-111361`：报告 5 观察事实承接完成，但来源需求 7 记录为“不在当前需求树”，最终写入安全根任务 1 输入。
  - `日志/鱼巢_run_20260630.低值g:111979-111987`：任务 8 / 需求 7 的 6085 在关闭阶段后完成，目标结果符合。
- 当前第一分歧点：
  - 程序在截图后进入关闭流程，`自我线程/主循环` 已在 `日志/鱼巢_run_20260630.低值g:111977` 退出。
  - 任务 8 的执行结果在 `日志/鱼巢_run_20260630.低值g:111979-111987` 才完成，已经晚于自我线程停止，因此父任务 6 没机会消费子需求 7 的完成结果并继续重筹办到 6150。
- 下一步预测：
  - 用更长等待窗口复跑，如果只是退出时序截断，则任务 8 应在停止前完成，随后应看到子需求 7 满足 / 收束、父任务 6 重筹办、6150 被选择或出现新的评分 / 输入缺口日志。
  - 如果任务 8 提前完成后父任务 6 仍反复显示“子需求满足状态未成立”，根因转入子需求 7 满足状态收束 / 父任务等待解除链。
  - 如果父任务 6 重筹办但仍选择 6146 而非 6150，根因转入 6150 输入评分和任务输入材料写入链。

## 6. 2026-06-30 15:35 180 秒窗口追因更新

- 用户纠正口径：
  - 没有自然候选不能作为终点；必须继续追为什么没有自然候选。
- 已确认 15:27-15:30 的 180 秒窗口不是“没有 D455 自然材料”：
  - `日志/鱼巢_run_20260630.低值g:113048-113049`：父需求 6 派生需求 7，生成任务 7。
  - `日志/鱼巢_run_20260630.低值g:113222`：等待项巡检生成 2 条上行消息并关闭 2 个等待项。
  - `日志/鱼巢_run_20260630.低值g:113594`：报告 6 完成观察事实承接，来源需求 6。
  - `日志/鱼巢_run_20260630.低值g:113977`：报告 5 完成观察事实承接，来源需求 7，生成候选=是，统计识别=是。
  - `日志/鱼巢_run_20260630.低值g:113290,114193,114500,114790,115089,115376,115660,115966,116255`：自我线程反复确认需求 7 对应任务 7。
- 当前第一分歧点已前移：
  - `日志/鱼巢_run_20260630.低值g:113978`：自我线程观察事实更新后唤醒来源需求跳过，原因是 `来源需求不在当前需求树 | 来源需求=7 | 报告ID=5 | 等待项ID=2`。
  - `日志/鱼巢_run_20260630.低值g:113981`：同一报告 5 随后被写入任务 1 输入，而不是任务 7 输入。
- 当前判断：
  - 候选包为 0 的当前根因链不是“没有自然样本”，也不是“体素分解算法未产出子轮廓”。
  - 自然观察材料已经到达；断点在来源需求 7 无法被观察事实唤醒链识别为当前需求树节点，导致报告 5 没有回到任务 7 / 父需求 6 继续推进 6150。
- 下一步：
  - 只读追 `需求类::执行更新指令`、`自我线程模块.impl.cpp::私有_确保派生需求节点`、`私有_按主键解析需求节点`、`私有_观察事实更新后触发来源需求重筹办_线程侧`。
  - 判定需求 7 是入树时孤立成根、当前需求树根选择漂移，还是按主键解析时被误判为结构根 / 非当前树节点。

## 7. 2026-06-30 15:50 代码层第一分歧点

- 已定位首个实现缺陷：
  - `双向链表模板核心.h::添加子节点_已加锁` 用独立根节点 `主键` 推进最新编号，并把推进后的主键赋给新子节点。
  - `自我线程模块.impl.cpp::私有_按主键扫描独立树` 先判断根节点主键，再扫描根的全链子节点。
  - 因此新增子需求 7 后，需求结构根的主键也变为 7；来源需求解析请求 `7` 时先命中结构根，随后 `私有_按主键解析需求节点` 因“结构根不可作为需求”返回空，真正的子需求 7 被遮蔽。
- 当前根因判断：
  - 需求 7 不是没有生成，也不是没有任务化；它被独立树结构根同号遮蔽，导致观察事实更新链无法把报告 5 唤醒回需求 7 / 任务 7。
- 最小修复：
  - 调整 `私有_按主键扫描独立树` 的搜索顺序：先扫描全链业务节点，未命中时才检查根节点。
  - 该修复同时避免任务独立树最新任务主键被任务根同号遮蔽。
- 预测：
  - 修复后同类运行中，报告 5 的来源需求 7 应能解析到真实子需求；日志应出现 `需求裁决/观察事实更新命中来源需求`，报告 ID 应写入任务 7，而不是任务 1。

## 8. 构建前预测

- 预期构建：
  - `自我线程模块.impl.cpp` 只调整独立树扫描顺序，不改变类型签名；Debug x64 应通过。
- 预期真实运行：
  - 若自然链再次生成需求 7 和报告 5，`观察事实更新来源需求唤醒跳过 | 原因=来源需求不在当前需求树 | 来源需求=7` 不应再出现。
  - 应出现来源需求命中日志，并把报告 5 写回任务 7；随后继续观察是否推进 6150、写入观察体素特征和提交体素加工任务。

## 9. 构建结果

- 已运行：
  - `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
- 证据：
  - `日志/20260630_R23_rootkey_scan_fix_msbuild.log`
  - 退出码：0
  - 结果：0 警告，0 错误
- 下一步：
  - 真实运行自我场景捕获窗口，验证来源需求 7 是否可命中。

## 10. 2026-06-30 根主键遮蔽修复后新分歧点

- 已确认根主键遮蔽修复有效：
  - `日志/20260630_R23_rootkey_scan_fix_msbuild.log`：Debug x64 构建通过，0 警告，0 错误。
  - `实施记录/P8c2_self_scene_capture_after_rootkey_fix_180s.png`：真实运行窗口完成。
  - 最新日志出现 `需求裁决/观察事实更新命中来源需求`，说明来源需求解析不再被独立树结构根主键遮蔽。
- 新第一分歧点：
  - `日志/鱼巢_run_20260630.低值g:117378`：任务 7 / 需求 7 / 方法 1B / 本能 6085 创建外设观察等待项 2，但日志记录 `需要等待=0`、`触发口径=执行缺口方法供料`。
  - `日志/鱼巢_run_20260630.低值g:117379`：同一轮 `方法供料等待项=1`、`当前等待缺口唤醒=1`，但建议任务状态仍为 `待重筹办`。
  - 后续任务 7 在报告承接完成前进入完成 / 已结算；观察事实唤醒终态任务会被 `任务状态不允许由观察事实唤醒` 拦截。
- 当前判断：
  - 不能放开已结算任务唤醒；那会掩盖生命周期顺序错误。
  - 正确修复点应在任务管理界面线程应用工作结果阶段：只要方法供料等待项创建成功，本轮就必须进入等待，由观察事实承接回来后再唤醒重筹办。
- 下一步预测：
  - 修复后，同类 `执行缺口方法供料` 日志应显示本轮 `需要等待=1` 或尾部证据显示建议状态为 `等待中`。
  - 报告承接成功时任务应仍处于可重筹办状态，`提交观察事实更新任务唤醒通知` 应写入报告 ID 并改为 `待重筹办`，而不是跳过终态任务。
  - 之后再验证是否推进 6150 / 写入已确认观察存在 / 写入观察存在三维体素特征 / 提交体素加工任务 / 产生候选包。

## 11. 2026-06-30 方法供料等待修复后新分歧点

- 已确认方法供料等待状态修复生效：
  - `任务管理界面线程/方法供料等待项转等待` 已出现。
  - 来源需求 6 的观察事实更新能命中并唤醒。
  - 最新窗口未再出现新的 `任务状态不允许由观察事实唤醒`。
- 仍未闭合：
  - 最新窗口多次出现 6085 `观察本能闭环` 成功，且有 `观察新建存在数量=12/12/11`、`观察写入特征数量=437/440/403`。
  - 同一窗口仍没有 `写入观察存在三维体素特征`、`提交观察体素加工任务`、`体素加工` 或候选包证据。
- 当前第一分歧点：
  - `写入已确认观察存在节点原始事实` 已调用 `写入观察存在三维体素特征`，但体素写入函数内部缺少早退诊断，无法判断是入口未调用、空间范围拒绝、证据已消费、版本闸门拒绝、轮廓视角读取失败、父候选为空，还是 worker 提交失败。
- 已补最小诊断：
  - `自我动作实现.外设模块.ixx::写入观察存在三维体素特征` 增加 `观察体素特征写入诊断`。
  - `自我动作实现.外设模块.ixx::提交观察体素加工任务_由观察簇` 增加 `观察体素加工任务提交诊断`。
  - 诊断只输出日志，不参与机器判断，不改变体素触发条件。
- 构建前预测：
  - Debug x64 应通过。
  - 真实运行若 6085 再次成功，应至少出现 `观察体素特征写入诊断`。
  - 若出现 `观察体素加工任务提交诊断 | 阶段=提交任务 | 成功=1`，再追 worker 是否产生候选包。
  - 若只出现入口/证据/版本/轮廓/父候选拒绝，下一步按具体拒绝原因修复上游合法材料或体素证据口径。

## 12. 2026-06-30 P8c2 自然候选包根因闭合

- 用户纠正口径已落实：
  - `没有自然候选` 不能作为终点；必须继续追为什么没有。
  - 本轮已经追到自然链的后续节点，初次 `voxelCandidatePackageCount=0` 不再成立。
- 已验证自然链路：
  - 外设等待项、D455 报告、等待项匹配、观察事实承接请求在最新窗口中出现。
  - 观察体素特征写入诊断出现，且有粗体素写入成功。
  - `观察体素加工任务提交诊断 | 阶段=提交任务 | 成功=1` 出现 15 次。
  - `体素加工工作线程/候选结果包处理` 出现 9 次。
  - 9 个 worker 候选结果包均有 `子轮廓候选数量 > 0` 且 `局部子体素候选数量 > 0`。
  - 3 个候选包有 `融合点=1` 且 `可提交状态=0`。
- 关键证据：
  - `日志/鱼巢_run_20260630.低值g:140688`：体素加工任务提交成功，worker 已启动，worker 候选包已有 4 个。
  - `日志/鱼巢_run_20260630.低值g:140689`：观察体素特征粗体素写入成功，已提交体素加工任务。
  - `日志/鱼巢_run_20260630.低值g:141083`：候选包 `任务ID=41000003`，`融合点=1`，`可提交状态=0`，`子轮廓候选数量=106`，`局部子体素候选数量=106`。
  - `日志/鱼巢_run_20260630.低值g:141998`：候选包 `任务ID=79000013`，`融合点=1`，`可提交状态=0`，`子轮廓候选数量=128`，`局部子体素候选数量=128`。
  - `日志/鱼巢_run_20260630.低值g:143614`：候选包 `任务ID=79000010`，`融合点=1`，`可提交状态=0`，`子轮廓候选数量=128`，`局部子体素候选数量=128`。
- 构建证据：
  - `日志/20260630_R23_voxel_write_diagnostic_msbuild.log`：Debug x64 构建通过，0 警告，0 错误。
  - `日志/20260630_R23_voxel_worker_summary_msbuild.log`：Debug x64 构建通过，0 警告，0 错误。
- 当前判断：
  - P8c2 根因切片闭合：自然体素候选包已经生成；worker 已生成子轮廓候选和局部子体素候选。
  - 可以进入 P8d 身份映射。
- 保留旁支：
  - 部分观察证据仍因 `父体素候选快照为空` 无法提交体素加工任务。
  - 部分观察证据仍因 `未找到D455簇三维体素轮廓材料页` 无法提交体素加工任务。
  - 6085 仍有 `忽略输入报告ID=1` 诊断口径，后续若阻塞 P8d / P8e 再单独追。
- 禁止宣称：
  - 不宣称 P8d / P8e / P9 完成。
  - 不宣称内部世界子存在映射完成。
  - 不宣称子平均颜色正式落账完成。
  - 不宣称视觉系统完成、自我苏醒完成或初步成熟完成。
