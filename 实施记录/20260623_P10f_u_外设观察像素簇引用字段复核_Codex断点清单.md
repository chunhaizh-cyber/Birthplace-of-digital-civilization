# P10f-u 外设观察像素簇引用字段复核 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-u `外设观察报告队列.ixx::结构_外设观察像素簇摘要` 引用字段边界复核。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_target_device_id.tsv`

当前问题：

- `ROI引用 / 点集引用 / 空间候选引用 / 像素归属验证材料引用` 是非主键类 `std::string` 字段名。
- 写入方使用 `D455_构造观察簇资源句柄(...)`，读取方使用 `解析D455观察材料句柄 / 簇句柄可解引用 / 外设提交_追加非空句柄` 等句柄逻辑。
- 这些字段实际是材料句柄，不是自由文本；应收束为 `...句柄` 字段。

构建前预测：

- 统一将目标像素簇摘要和目标观察约束里的同源 ROI / 点集 / 空间候选 / 像素归属材料字段改为 `...句柄`。
- 同步更新 D455 写入、约束匹配、材料可用性判断、提交包、日志摘要和自我动作外设模块读取。
- 预期 Debug x64 构建通过，0 警告，0 错误。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。

## 1. 完成证据

已改文件：

- `外设观察报告队列.ixx`
- `外设线程_D455深度相机.ixx`
- `自我动作实现.外设模块.ixx`
- `任务模块.管理工作线程.ixx`
- `自我线程模块.impl.cpp`
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_cluster_handle_fields.tsv`

完成内容：

- `结构_外设观察像素簇摘要` 中 `ROI引用 / 点集引用 / 空间候选引用 / 像素归属验证材料引用` 已收束为 `ROI句柄 / 点集句柄 / 空间候选句柄 / 像素归属验证材料句柄`。
- `结构_目标观察约束特征组` 中 `ROI引用 / 点集引用` 已收束为 `ROI句柄 / 点集句柄`。
- D455 写入、约束匹配、材料可用性判断、提交包和自我动作外设模块读取均已同步到句柄字段。
- 任务管理工作线程材料字段白名单和自我线程侧特征入口已删除旧 `ROI引用`，并统一到 `空间候选句柄 / 像素归属验证材料句柄`。

复核证据：

- `rg -n "ROI引用|点集引用|空间候选引用|像素归属验证材料引用" -g "*.ixx" -g "*.cpp" -g "*.h"` 无当前源码命中。
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_cluster_handle_fields.tsv` 已生成，剩余记录 624 行，其中主键 / ID / 键 / 句柄类 324 行、非主键类 300 行。
- `git diff --check` 通过。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 通过，0 警告，0 错误。

下一候选：

- P10f-v：复核 `外设观察报告队列.ixx::结构_外设观察像素簇摘要::失败原因` 及同文件外设报告失败原因字段是否仅用于日志 / 控制面板显示，还是进入机器判断。
