# 20260621 D3 需求树更新指令协议简化预判定表

## 0. 判定口径

```text
所属计划：计划/20260620_需求类逻辑纠偏计划_v0.1.md P3。
执行边界：本表只统计和预判 `结构_需求树更新指令` 外部构造点，不直接改源码。
上级边界：
1. 需求树归自我线程管理，任务管理线程和筹办 / 执行函数不得直接写需求树。
2. 需求树正式改写仍必须通过提交入口或统一需求树更新入口落账。
3. 本切片不得新增平行输入包；若简化，只能收束成需求类专用构造函数或专用提交函数。
4. 文本说明只能进入日志 / 错误 / 显示，不得作为指令类型或字段选择依据。
```

## 1. 协议保留点

| 位置 | 角色 | 判定 |
| --- | --- | --- |
| `需求类.h:44-75` | `结构_需求树更新指令` 统一载荷 | 保留；当前同时承载新增、更新、满足、删除四类入口。 |
| `需求类.h:1165-1194` | `执行更新指令_已加锁` 分派 | 保留；这是需求树写入口的集中执行点。 |
| `自我线程模块.消息协议.ixx:397-400` | 治理消息附加载荷可携带需求树更新 | 保留；线程消息通道需要统一协议对象。 |
| `自我线程模块.消息处理器.ixx:111-147` | 新增需求原子目标输入校验 | 保留；这是入树前硬校验，不是显示逻辑。 |
| `自我线程模块.消息处理器.ixx:432-496` | 应用需求树变更批次 | 保留；它只消费协议并调用需求类统一入口。 |

## 2. 外部手写指令点

| 位置 | 指令类型 | 用途 | 字段写入特征 | 初判 |
| --- | --- | --- | --- | --- |
| `自我线程模块.impl.cpp:5320-5336` | 更新需求 | 已完成叶需求收束后清权重、解除阻塞、记观测 | 目标需求、权重、阻塞、观测统计 | 可转为“目标需求权重 / 阻塞更新”专用构造函数；不应直接散写字段。 |
| `自我线程模块.impl.cpp:8521-8539` | 更新需求 | 观察事实更新材料子需求当前状态刷新 | 父 / 目标需求、当前状态、目标状态、满足关系、目标特征、观测统计 | 可转为“目标状态镜像刷新”专用构造函数；需保留父需求上下文。 |
| `自我线程模块.impl.cpp:9098-9127` | 新增需求 | 自检维护管理分支确保 | 父需求、主体、场景、目标宿主、状态、权重、阻塞、观测统计 | 可转为“新增原子需求”专用构造函数；字段多，先不作为第一代码切片。 |
| `自我线程模块.impl.cpp:9448-9496` | 新增需求 | 自检正式需求入树 | 父需求、主体、场景、目标宿主、状态、满足关系、权重、目标特征、阻塞、观测统计 | 可转为“新增原子需求”专用构造函数；依赖自检父域裁决，先不作为第一代码切片。 |
| `自我线程模块.impl.cpp:9737-9774` | 更新需求 | 已有派生需求补齐主信息 | 目标需求、父需求、目标特征、可选当前 / 目标状态、阻塞、观测统计 | 可转为“补齐目标关系更新”专用构造函数；与派生需求入树同域。 |
| `自我线程模块.impl.cpp:9894-9978` | 新增需求 | 任务管理派生需求入树 | 父需求、主体、场景、目标宿主、状态、满足关系、权重 | 可转为“新增原子需求”专用构造函数；字段最多，需拆小切片。 |
| `自我线程模块.impl.cpp:10165-10172` | 已经走构造函数 | 当前场景特征值逻辑组织子需求 | `构造获取途径逻辑组织需求更新指令` | 已收口；不列为手写协议缺口。 |
| `自我线程模块.impl.cpp:11216-11247` | 满足需求 | 根特征必要条件子需求收束 | 父 / 目标需求、当前 / 目标状态、满足关系、权重清零、阻塞解除、观测统计 | 第一优先可转为“满足需求指令”专用构造函数。 |
| `自我线程模块.impl.cpp:11288-11296` | 删除需求 | 删除遗留根子需求 | 父 / 目标需求 | 第一优先可转为“删除需求指令”专用构造函数。 |
| `自我线程模块.impl.cpp:11570-11601` | 新增需求 | 根特征必要条件子需求入树 | 父需求、主体、场景、目标宿主、状态、满足关系、权重、目标特征、阻塞、观测统计 | 可转为“新增原子需求”专用构造函数；等待新增类构造函数切片。 |
| `自我线程模块.impl.cpp:11861-11883` | 更新需求 | 根特征必要条件子需求重激活 | 父 / 目标需求、目标特征、权重、阻塞、观测统计 | 可转为“目标需求权重 / 阻塞更新”专用构造函数。 |
| `自我线程模块.impl.cpp:11978-12008` | 满足需求 | 根需求差距子需求退出活动集 | 父 / 目标需求、当前 / 目标状态、满足关系、权重清零、阻塞解除、观测统计 | 第一优先可复用“满足需求指令”专用构造函数。 |
| `自我线程模块.impl.cpp:12094-12102` | 更新需求 | 根需求权重更新 | 目标需求、权重 | 可转为“目标需求权重更新”专用构造函数。 |
| `自我线程模块.impl.cpp:13419-13449` | 新增需求 | 扫描入账子需求 | 父需求、主体、场景、目标存在、状态、满足关系、目标特征、权重、阻塞、观测统计 | 可转为“新增原子需求”专用构造函数；等待新增类构造函数切片。 |

## 3. 简化顺序

```text
P3.1 满足 / 删除专用构造函数：
    目标：先把 `满足需求` 和 `删除需求` 两类低字段量构造从自我线程手写字段中收口。
    范围：
        需求类.h / 需求类.cpp
        自我线程模块.impl.cpp 中 11216-11247、11288-11296、11978-12008 三处。
    验证：
        函数说明文档生成；
        Debug x64 构建；
        扫描上述三处不再出现手写 `指令.指令类型 = 满足需求 / 删除需求`。

P3.2 更新专用构造函数：
    目标：收束权重、阻塞、状态镜像更新指令。
    范围：5320-5336、8521-8539、9737-9774、11861-11883、12094-12102。

P3.3 新增原子需求专用构造函数：
    目标：收束新增原子需求的重复字段写入。
    范围：9098-9127、9448-9496、9894-9978、11570-11601、13419-13449。
```

## 4. 当前结论

```text
统一 `结构_需求树更新指令` 不能直接删除；它仍是消息协议和需求类统一写入口的必要载荷。
当前应先减少外部手写协议字段，而不是新增另一套平行输入包。
第一代码切片选择 P3.1：满足 / 删除专用构造函数，字段少、调用点少、构建验证边界清楚。
```
