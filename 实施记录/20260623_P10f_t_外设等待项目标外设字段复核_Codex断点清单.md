# P10f-t 外设等待项目标外设字段复核 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-t `外设观察报告队列.ixx::结构_外设观察等待项::目标外设` 边界复核。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_wait_target_split.tsv`

当前问题：

- `结构_外设观察等待项::目标外设` 是非主键类 `std::string` 字段名。
- 实际写入值为 D455 外设 ID，并进入报告满足等待项、按外设过滤、任务管理 D455 判断和承接结果摘要。
- `任务模块.管理工作线程.ixx::结构_外设消息承接结果::目标外设` 复制同一身份字段。

构建前预测：

- 将等待项和承接结果字段统一改为 `目标外设ID`。
- 机器判断仍按外设 ID 匹配，日志只渲染字段名。
- 预期 Debug x64 构建通过，0 警告，0 错误。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。

## 1. 完成证据

- 已将 `结构_外设观察等待项::目标外设` 改名为 `目标外设ID`。
- 已将 `任务模块.管理工作线程.ixx::结构_外设消息承接结果::目标外设` 改名为 `目标外设ID`。
- 已同步更新报告满足等待项、等待项集合按外设过滤、D455 等待项判断、任务管理等待项构造、承接结果摘要和等待项摘要日志。
- 已生成 `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_target_device_id.tsv`，剩余记录 624 行，其中主键 / ID 类 318 行、非主键类 306 行。
- `rg` 复核 `.目标外设 / std::string 目标外设{} / 目标外设 =` 仅剩其它模块日志或输入参数名文本，不属于本切片目标结构字段。
- `git diff --check` 通过。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 通过，0 警告，0 错误。

## 2. 下一候选

- 最新清单中 `任务模块.管理工作线程.ixx::结构_工作线程实例快照` 的 `最近*` 字段属于工作线程显示快照，当前未作为机器逻辑堵点。
- 下一可执行候选转向 `外设观察报告队列.ixx::结构_外设观察像素簇摘要` 的 `ROI引用 / 点集引用 / 空间候选引用 / 像素归属验证材料引用`，复核这些字段是否应收束为句柄 / ID 类字段或是否仅为显示材料引用。
