# 20260701 结算贡献规则生成 Codex 断点清单

## 0. 恢复摘要

当前切片：按用户确认口径生成 R24 / P8 所需的结算贡献规则。

当前判断：

```text
已把“以任务自身安全 / 服务权重为基数，按任务执行完成时目标特征当前值 / 目标值比例结算”的口径写入正式 6100 规范。
已明确任务权重继承来源需求对应权重。
已明确子需求权重来自父需求对应权重；无显式 OR 组语义时，活跃可结算子需求平分父需求对应权重。
本切片只补正式规则和图谱索引，不改代码，不宣称 P8 完成或 R24 根需求完成。
```

禁止宣称：

```text
不宣称结算代码已接入。
不宣称服务 G0 / D2 正向闭环完成。
不宣称 R24 根需求完成、内部治理闭环完成、自我苏醒完成或初步成熟完成。
```

## 1. 修改范围

```text
规范/6100_子规范_安全服务值结算_20260701.md
docs/工程图谱/03_安全服务结算图谱.md
docs/工程图谱/05_规则原子索引.md
计划/20260630_需求树简化生成与内部治理闭环计划_v0.1.md
计划/计划索引.md
```

## 2. 规则摘要

```text
1. 需求分别持有安全权重和服务权重。
2. 子需求继承父需求对应权重；无显式 OR 组语义时，同轮活跃可结算子需求平分父需求对应权重。
3. 任务自身安全 / 服务权重继承来源需求对应权重，并在任务承接时固化为结算权重上限。
4. 任务执行完成并形成合法完成 / 已结算被动因后，按目标特征当前值 / 目标值比例生成目标达成比例。
5. 安全 / 服务贡献候选 = 任务对应权重 * 目标达成比例。
6. 该公式只生成预算内贡献候选，不绕过 D0/G0、父链归因、世界树因果证据、贡献层级、副作用审查、防重复令牌和安全加法。
```

## 3. 下一步

```text
已运行 python .\tools\check_specs.py，退出码 0。
输出摘要：spec files: 86；indexed specs: 86；rule atoms: 68；hard issues: 0；warnings: 0。
提交本规则生成切片。
后续若继续 R24，需要进入代码接入切片：读取 6100 第 4 章、任务/需求权重现有字段和自我线程结算入口，先判断能否复用现有任务承接权重和目标达成比例计算。
```
