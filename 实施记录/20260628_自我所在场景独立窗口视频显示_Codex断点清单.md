# 20260628 自我所在场景独立窗口视频显示 Codex断点清单

## 0. 恢复摘要

当前切片：实现自我所在场景信息在独立窗口中的持续视频式刷新显示。

依据文件：
- AGENTS.md
- 规范/规范目录.md
- 规范/禁止性规范20260611.md
- docs/工程图谱/05_规则原子索引.md
- docs/工程图谱/08_事实来源与写入权限图谱.md

边界：
- 复用现有 WebView2 自我场景独立窗口。
- 控制面板 / 独立窗口只读显示，不写世界树事实、任务状态、动作动态或需求满足。
- 不新增业务结构体，不把显示结果当业务闭合证据。

当前判断：
- 现有窗口已有 WebGL 自我场景渲染和启动快照。
- 缺少独立窗口持续请求 / 接收自我场景帧 JSON 的通道。

下一步：
- 暴露只读自我场景复现 JSON 生成函数。
- WebView2 增加 scene:refresh 消息处理和页面帧推送。
- 页面端把自我场景复现数据改为可更新，并更新 WebGL buffer / 统计 / 覆盖层。

禁止宣称项：
- 不宣称观察链、扫描链、D455 入场景存在闭合。
- 不宣称显示层证明世界树事实成立。

## 1. 实施结果

已改文件：
- 控制面板类.ixx
- 控制面板类.cpp
- 控制面板WebView2.cpp
- 说明书/函数功能说明文档.md

实现内容：
- 新增只读导出函数 `生成自我场景复现JSON(const 结构_控制面板快照&)`，复用现有 `私有_自我场景复现JSON`。
- WebView2 增加 `scene:refresh` 消息处理，读取控制面板快照后向页面执行 `window.__panelApplySelfSceneFrame(...)`。
- 自我场景独立窗口页面将启动快照改为可更新数据，收到帧后更新统计、存在列表、WebGL 线/点缓冲和诊断覆盖层。
- 独立窗口模式下每 1000ms 自动请求一次自我场景帧，启动后 250ms 请求首帧。

## 2. 验证证据

预测：
- Debug x64 构建应通过。
- `--self-scene-window` 入口应能启动进程并保持运行，不应立即编译或启动崩溃。

验证命令：
- `python .\tools\生成函数功能说明文档.py`
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
- 受控启动：`Start-Process .\x64\Debug\鱼巢.exe --self-scene-window`，等待 6 秒后关闭。

结果：
- 函数说明文档生成成功，函数条目 7326。
- msbuild 成功，0 警告，0 错误。
- 受控启动进程 PID=44892，6 秒后仍运行，随后已强制关闭；未保留后台 `鱼巢.exe`。

证据等级：
- E2 构建证据已满足。
- GUI 入口启动为辅助证据；未把显示结果当作业务闭合证据。

禁止宣称项：
- 不宣称扫描 / 观察 / D455 入场景存在闭合。
- 不宣称显示层证明世界树事实成立。
