# 20260620 动态创建追加场景索引回迁 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.2 P3.3b，回迁 `动态类` 创建动态时的动态索引追加路径，复用 `场景类::追加动态`。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
实施记录/20260620_状态创建追加场景索引回迁_Codex断点清单.md
```

本切片范围：

```text
1. 只回迁 创建动态路径中的动态索引追加路径。
2. 将动态索引按动态时间排序的原行为补入 场景类::追加动态。
3. 删除回迁后无调用的 动态类.cpp 私有_按时间追加动态索引。
4. 不处理 动态类 内部其它动态索引扫描路径。
```

函数文档预查：

```text
已查 说明书/函数功能说明文档.md 和源码引用。
场景类::追加动态 已存在，但当前只追加不排序。
动态类.cpp 私有_按时间追加动态索引 仅有 创建动态路径一个调用点。
```

验证前预测：

```text
1. 场景类::追加动态 增加按动态时间稳定排序后，替换 动态类 私有追加函数不会改变动态索引顺序。
2. 创建动态 改为调用 场景类::追加动态 后，动态类创建路径不再直接写 场景主信息->动态索引。
3. 删除 私有_按时间追加动态索引 后，函数文档中该条目应消失。
4. Debug x64 构建应通过。
```

禁止宣称：

```text
不宣称所有场景主信息直读已回迁。
不宣称场景类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 1. 实施与验证

已实施：

```text
1. 场景类::追加动态 增加按动态时间稳定排序，保留原 动态类 私有追加函数的排序口径。
2. 动态类::创建动态 改为通过 `场景类::追加动态(场景, 节点)` 维护场景动态事实目录。
3. 删除无调用的 动态类.cpp 私有_按时间追加动态索引。
4. 动态创建后续二次特征生成和聚合条件保持原来的 `节点 && 场景` 判断。
```

已运行：

```powershell
python .\tools\生成函数功能说明文档.py *> 日志\20260620_动态创建追加场景索引回迁_函数文档.log
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\20260620_动态创建追加场景索引回迁_msbuild.log
```

验证结果：

```text
函数文档生成退出码：0
函数文档统计：源码文件 128；函数条目 6895
函数文档删除条目：私有_按时间追加动态索引 已无命中
Debug x64 构建退出码：0
构建关键日志：已成功生成；0 个错误
```

当前结论：

```text
P3.3b 已闭合。
下一步进入 P3.3c：回迁 二次特征类::绑定场景 中的二次特征索引追加路径，优先复用 场景类::追加二次特征。
```
