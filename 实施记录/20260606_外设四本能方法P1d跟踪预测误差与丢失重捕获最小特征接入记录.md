# 20260606_外设四本能方法P1d跟踪预测误差与丢失重捕获最小特征接入记录

## 切片目标

```text
给 自我_跟踪指定存在 补齐指定目标范围内的预测区间、预测误差、区间迁移、丢失 / 重捕获正式特征和结果能力。
跟踪只面向指定目标，不替代全场扫描，不写安全值 / 服务值 / 需求满足。
```

## 修改内容

```text
自我动作实现.外设模块.ixx：
    新增 跟踪预测区间状态、跟踪预测误差状态 / 值、跟踪区间迁移状态 / 值、目标存在丢失状态、目标存在重捕获状态常量；
    新增 目标存在跟踪稳定动态、目标存在丢失动态、目标存在重捕获动态、目标存在预测偏差超限动态等特征入口；
    新增 指定存在跟踪正式结果 结构和 计算指定存在跟踪正式结果 helper；
    自我_跟踪指定存在 不再在缺 目标存在 时回退 当前观察存在；
    目标存在和预测区间材料同时成立时才写 目标存在跟踪状态_已更新 / 目标观察窗口状态_已更新；
    预测误差、区间迁移、丢失 / 重捕获只在读到残差或匹配分数材料时判断正常 / 超限，否则写证据不足。

特征类.cpp：
    初始化 P1d 新增跟踪状态值域；
    跟踪预测误差值和跟踪区间迁移值初始化为非负 I64 区间。
```

## 验证证据

```text
git diff --check -- 自我动作实现.外设模块.ixx 特征类.cpp 实施记录/20260605_外设四本能方法P0_Codex断点清单.md
    退出码 0；
    仅 LF/CRLF 提示。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码 0；
    0 个错误；
    1 个链接警告 LNK4075：因 /INCREMENTAL:NO 忽略 /EDITANDCONTINUE。
```

## 边界和未完成项

```text
本切片未真实运行鱼巢.exe，不宣称四方法自然业务链已通过。
本切片不把跟踪证据当作全场明确状态。
提交_指定存在跟踪事实 仍需后续 P1e 收口：上游方法动作动态串接、缺目标不回退当前观察存在、兼容字段撤退。
```
