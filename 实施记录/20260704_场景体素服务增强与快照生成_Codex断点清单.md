# 20260704 场景体素服务增强与快照生成 Codex 断点清单

## 0. 恢复摘要

当前切片：

```text
E5 / P7：场景体素服务增强与快照生成实现映射计划
S0：代码事实扫描
```

依据：

```text
计划/计划索引.md
计划/20260703_场景体素服务增强与快照生成实现映射计划_v0.1.md
规范/详细设计/待完成/20260703_场景体素服务增强与快照生成详细设计_v0.1.md
规范/6250_子规范_场景体素服务与视觉先验融合_20260701.md
规范/6240_子规范_已确认存在体素融合与分解_20260701.md
规范/1175_根规范_特征表达与判断收束规则_20260703.md
```

边界：

```text
S0 只读扫描场景体素模块、视觉观察融合模块、外设观察报告队列和 D5 缺口承接相关入口。
允许修改本实施记录。
禁止修改源码、规范、详细设计和计划索引。
```

禁止宣称：

```text
不宣称场景体素服务增强已代码实现。
不宣称空间占据索引、轴对齐包围盒投影索引或先验图层已可用。
不宣称快照可用性 / 新鲜度已完成收束。
不宣称视觉中间层已使用增强场景体素先验快照。
不宣称场景体素缺口已接入需求治理链。
```

当前注意：

```text
计划/20260703_场景体素服务增强与快照生成实现映射计划_v0.1.md 在进入本切片前已有未提交差异，主要表现为术语汉化和代码块标记调整。
本切片先不修改该计划文件，避免混入用户既有改动。
```

## 1. S0 只读扫描记录

### 1.1 已读取依据

```text
docs/current-state.md
docs/decision-log.md
README.md
docs/00_文档总索引.md
规范/规范目录.md
规范/0050_项目通用结构与禁止性规则总纲_20260701.md
规范/1175_根规范_特征表达与判断收束规则_20260703.md
规范/6240_子规范_已确认存在体素融合与分解_20260701.md
规范/6250_子规范_场景体素服务与视觉先验融合_20260701.md
计划/计划索引.md
计划/20260703_场景体素服务增强与快照生成实现映射计划_v0.1.md
规范/详细设计/待完成/20260703_场景体素服务增强与快照生成详细设计_v0.1.md
```

关键上级边界：

```text
6250：场景体素服务只能从当前场景已确认存在、坐标和稳定体素等既有事实只读生成运行期快照；不得直接写世界树事实、需求满足、安全 / 服务结算或方法动作动态。
6250：视觉融合层读取合法外设材料和 SceneVoxelPrior 后，只能输出观察域候选、评分、解释和结构化缺口。
6240：场景体素快照不得触发体素融合，不得直接修改稳定存在体素。
1175：先验可用、新鲜度、遮挡解释、自由空间冲突等判断型状态不能作为外设、日志或普通函数直接写入的一阶事实。
```

### 1.2 场景体素模块真实入口

代码文件：`场景体素模块.ixx`

已确认文件头边界：

```text
1-3：本模块只提供当前场景体素只读快照和查询服务，不写世界树事实、需求满足、价值结算或动作动态；场景体素先验只能辅助观察域候选、评分、解释和缺口。
```

已存在可复用结构：

```text
结构_场景体素同步参数：33-54
结构_场景体素存在项：75-96
枚举_场景体素缺口类型：98-106
结构_场景体素缺口项：108-115
结构_场景体素快照统计：117-127
结构_场景体素只读快照：129-138
结构_SceneVoxelPrior = 结构_场景体素只读快照：140
```

当前缺口值域：

```text
缺体素特征类型
缺存在场景绝对坐标
缺三维体素特征
缺三维体素句柄
缺坐标系版本
缺内部子体素局部原点
```

已存在同步 / 查询入口：

```text
同步当前场景快照：613-615
读取最新快照：878-881
查询AABB：885-907
统计AABB：911-931
查询点附近：935-962
射线粗查询：966-1038
```

已确认当前查询边界：

```text
查询AABB 只查询快照中位于 AABB 内的存在原点，不读取或修改世界树事实。
统计AABB 只统计存在原点数量，不创建缺口节点。
查询点附近只作为视觉先验粗过滤。
射线粗查询只作为先验过滤，不证明遮挡或命中事实。
```

已存在维护线程入口：

```text
枚举_场景体素维护事件类型：493-496，当前只有“同步当前场景快照”。
结构_场景体素维护事件：515-521。
提交事件：1115-1137，只进入运行期事件队列。
处理事件_：1181-1202，只处理同步当前场景快照；其它事件返回未知事件类型。
提交场景体素维护事件：1298-1300。
读取场景体素维护线程最新快照：1310-1312。
```

### 1.3 视觉观察融合模块真实入口

代码文件：`视觉观察融合模块.ixx`

已确认文件头边界：

```text
1-4：只把合法外设材料和 SceneVoxelPrior 融合为观察域候选、评分、解释和缺口；不得创建存在、确认消失、写一阶特征、写需求满足、写价值结算或生成动作动态。
```

可复用输入 / 输出结构：

```text
枚举_视觉观察解释类型：33-41
枚举_视觉信息缺口类型：43-53
结构_视觉先验匹配评分：83-94
结构_视觉自由空间冲突候选：96-102
结构_视觉信息缺口：104-112
结构_视觉观察候选：114-118
结构_视觉观察域结果包：120-134
结构_视觉融合输入：136-140，其中 SceneVoxelPrior 字段读取场景体素模块::结构_SceneVoxelPrior。
```

已存在 D5 缺口映射：

```text
视觉信息缺口_结构化缺口大类：790-810
视觉信息缺口_结构化阻断阶段：814-834
视觉信息缺口_需求化处理建议：838-850 起
```

已存在融合路径：

```text
融合观察域结果：893-1025 起。
读取 SceneVoxelPrior 版本和坐标系版本：897-898。
SceneVoxelPrior 版本缺失 -> 缺SceneVoxelPrior：903-910。
坐标系版本缺失 -> 先验坐标系版本缺失：911-918。
SceneVoxelPrior.缺口集合 -> 先验结构缺口：919-927。
合法外设材料为空 -> 缺合法外设材料：928-935。
合法材料按空间范围调用 场景体素缓存类::查询AABB：966-969。
输出先验匹配评分、观察候选、遮挡候选计数和自由空间冲突候选：974-1014。
```

当前边界判断：

```text
视觉融合已经能消费现有 SceneVoxelPrior 别名快照，但只按存在原点 AABB 粗查询评分。
当前未看到视觉侧读取增强先验图层、预计深度范围图、已知空闲区、未知区、投影索引或快照新鲜度收束入口。
```

### 1.4 外设观察报告队列真实入口

代码文件：`外设观察报告队列.ixx`

可复用目标观察约束和每帧处理槽：

```text
报告项目标观察约束ID：524
本帧处理模式：525
本帧处理优先级：526
本帧处理掩码句柄：527
目标约束使用状态：528
等待项目标观察约束ID / 本帧处理模式 / 优先级 / 掩码：562-570
结构_目标观察约束特征组：575 起
```

目标观察约束入口：

```text
判断目标观察约束可用性：3506-3566。
提交目标观察约束特征组：3569-3589。
读取目标观察约束_按等待项：3593-3611。
读取目标观察约束_按目标特征：3615-3630。
选择最新可用目标观察约束：3634-3655。
```

已存在外设缺口到 D5 映射：

```text
外设提交缺口_结构化缺口大类：2521-2540。
外设提交缺口_结构化阻断阶段：2544-2563。
外设提交缺口_需求化处理建议：2567 起。
```

风险记录：

```text
判断目标观察约束可用性 内部仍有 可强匹配 / 可降级匹配 布尔值输出，当前只能视为运行期协议兼容位；后续若进入 D7 或本计划新鲜度 / 可用性收束，不得把这些布尔值扩张为一阶事实。
目标观察约束中仍存在字符串 ID 和字符串句柄字段；本计划 S0 只登记现状，不新增字符串承载。
```

### 1.5 D5 结构化缺口承接入口

代码文件：`任务模块.工作线程消息协议.ixx`

已确认可复用入口：

```text
结构_任务管理派生需求消息：187-231。
任务管理派生需求消息必备槽完整：378-395。
填充任务管理派生需求消息结构化缺口槽：398-428。
```

边界：

```text
该入口只负责把结构化缺口必备槽填入现有派生需求消息，不直接写需求树。
场景体素缺口目前尚未看到专用的“场景体素缺口 -> D5 结构化缺口”适配函数。
```

### 1.6 S0 缺失项清单

当前未闭合 / 待后续切片处理：

```text
1. 场景体素缺口值域缺少：缺稳定体素版本、缺存在位姿版本、缺场景边界、缺体素块索引、缺先验投影索引、缺可见性预计算、缺预计深度范围图、缺已知空闲区、缺未知区、快照过期、坐标系版本失配、来源场景版本失配、解码超过预算、先验图层生成失败。
2. 结构_场景体素存在项 没有场景轴对齐包围盒摘要、投影索引或占据体素索引摘要。
3. 结构_场景体素只读快照 没有感兴趣区域、预计深度范围图、预计遮挡区域、已知空闲区、未知区、自由空间冲突候选区等增强先验图层句柄。
4. 快照统计缺少快照年龄、严重缺口数量、投影索引数量、可见性图层数量、解码预算等新鲜度 / 可用性收束输入材料。
5. 维护事件值域只包含同步当前场景快照，尚未覆盖场景切换、位姿变化、存在集合变化、稳定体素变化、观察候选入账等事件。
6. 场景体素缺口尚无独立标准化适配到 D5 结构化信息缺口链的函数。
7. 视觉融合当前只用 AABB 粗查询生成评分和候选，未消费增强先验图层。
```

### 1.7 S0 当前判断

```text
S0 已完成代码事实扫描。
当前代码已有只读快照、存在项集合、缺口集合、统计、AABB / 点附近 / 射线粗查询、维护线程和 SceneVoxelPrior 别名。
当前代码还没有完整场景占据索引、投影索引、先验图层、新鲜度 / 可用性收束、事件扩展和场景体素缺口到 D5 的专用标准化适配。
本轮 S0 未修改源码、规范、详细设计或计划索引。
```

下一切片准入：

```text
允许进入 S1：补场景体素缺口值域。
S1 修改前必须再次读取 场景体素模块.ixx 文件头规则注释模块，并按 AGENTS.md 扫描 说明书/函数功能说明文档.md 中相关函数命中段。
S1 不得写世界树事实、稳定存在体素、需求满足、动作动态或任务完成。
```

已执行命令摘要：

```text
git status --short --untracked-files=no
Get-ChildItem 计划/.计划索引.lock
Get-ChildItem .codex_locks/build-run.lock
Get-Content / Select-String 读取必读文档、计划、详细设计和相关规范
rg 场景体素模块.ixx / 视觉观察融合模块.ixx / 外设观察报告队列.ixx / 任务模块.工作线程消息协议.ixx
```

## 2. S1 补场景体素缺口值域

### 2.1 修改范围

```text
场景体素模块.ixx
实施记录/20260704_场景体素服务增强与快照生成_Codex断点清单.md
```

未修改：

```text
需求树
任务管理
世界树事实写入链
稳定存在体素融合链
规范
详细设计
计划索引
```

### 2.2 代码变更

在 `场景体素模块.ixx` 的 `枚举_场景体素缺口类型` 中登记 S1 要求的缺口值域：

```text
缺稳定体素版本
缺存在位姿版本
缺场景边界
缺体素块索引
缺先验投影索引
缺可见性预计算
缺预计深度范围图
缺已知空闲区
缺未知区
快照过期
坐标系版本失配
来源场景版本失配
解码超过预算
先验图层生成失败
```

承载方式：

```text
继续使用现有 结构_场景体素缺口项：
    类型
    快照版本
    坐标系版本
    场景
    存在
    特征
```

边界：

```text
本切片只补值域，不新增长期业务结构体。
本切片不让缺口自动需求化。
本切片不新增世界树事实写入。
本切片不修改稳定存在体素融合链。
本切片不声明新增缺口已有全部生产路径。
```

### 2.3 验证计划

本切片需要验证：

```text
git diff --check -- 场景体素模块.ixx 实施记录/20260704_场景体素服务增强与快照生成_Codex断点清单.md
python .\tools\check_specs.py
```

C++ 构建：

```text
S1 只扩展枚举值域，仍会在本计划代码切片合适阶段执行 Debug x64 构建。
若后续 S2-S6 继续修改 C++，统一在进入验证切片或计划要求处执行构建。
```

### 2.4 当前判断

```text
S1 已完成代码值域登记。
新增缺口目前只能作为结构_场景体素缺口项的类型值被后续切片使用。
后续 S2/S3/S4/S5/S6/S7 仍需分别补摘要、统计、事件、图层、视觉消费和 D5 标准化。
```

### 2.5 验证结果

```text
git diff --check -- 场景体素模块.ixx 实施记录/20260704_场景体素服务增强与快照生成_Codex断点清单.md：通过。
python .\tools\check_specs.py：hard issues = 0，warnings = 0。
```

未执行：

```text
Debug x64 构建未在 S1 单独执行；本切片只扩展枚举值域，后续本计划继续代码切片时按计划聚合执行构建验证。
真实 D455 / 自然样本未执行。
```

## 3. S2 补存在体素场景轴对齐包围盒摘要

### 3.1 修改范围

```text
场景体素模块.ixx
实施记录/20260704_场景体素服务增强与快照生成_Codex断点清单.md
```

未修改：

```text
视觉观察融合模块.ixx
外设观察报告队列.ixx
需求树
任务管理
世界树事实写入链
稳定存在体素融合链
规范
详细设计
计划索引
```

### 3.2 代码变更

承载结构：

```text
复用 结构_场景体素AABB。
将 结构_场景体素AABB 移到 结构_场景体素存在项 前定义，默认值调整为无效闭区间。
结构_场景体素存在项 新增 场景AABB_mm。
```

摘要生成：

```text
新增 detail::私有_构造场景AABB摘要(...)。
直接场景存在项和内部世界子存在项生成时，基于原点与 默认存在最小体素边长_mm 填充第一版场景 AABB 摘要。
若最小体素边长为 0，AABB 摘要保持无效，后续查询退回原点过滤。
```

查询路径：

```text
新增 detail::私有_AABB相交(...)。
新增 detail::私有_存在项命中AABB(...)。
场景体素缓存类::查询AABB(...) 由“只看存在原点”改为优先按存在项 AABB 摘要相交过滤，缺摘要时退回原点过滤。
场景体素缓存类::统计AABB(...) 同步使用存在项 AABB 摘要相交过滤。
```

边界：

```text
本切片生成的是运行期只读先验摘要，不证明当前帧命中。
本切片不解码完整稳定体素占据块。
本切片不生成投影索引、可见性图层、预计深度范围图、空闲区或未知区。
本切片不写观察事实、世界树事实、需求满足、动作动态或稳定存在体素。
```

### 3.3 当前判断

```text
S2 已完成第一版场景 AABB 摘要承载和查询接入。
AABB 查询现在返回先验候选集合，仍不做遮挡、命中或存在消失裁决。
完整占据索引和投影索引仍属后续切片 / 后续计划范围。
```

### 3.4 待验证

```text
git diff --check -- 场景体素模块.ixx 实施记录/20260704_场景体素服务增强与快照生成_Codex断点清单.md
python .\tools\check_specs.py
后续聚合 Debug x64 构建验证
```

### 3.5 验证结果

```text
git diff --check -- 场景体素模块.ixx 实施记录/20260704_场景体素服务增强与快照生成_Codex断点清单.md：通过。
python .\tools\check_specs.py：hard issues = 0，warnings = 0。
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m：0 警告，0 错误。
```

构建锁：

```text
构建前已创建 .codex_locks/build-run.lock。
构建完成后已释放 .codex_locks/build-run.lock。
```

未执行：

```text
真实 D455 / 自然样本未执行。
```
