# 20260625 P13b 线程字段写入工具迁移 Codex断点清单

## 0. 恢复摘要

当前切片已闭合。目标是处理 `线程生命周期消息模块.ixx::私有_转义字段值(std::string_view)` 与 `线程生命周期消息模块.ixx::私有_写字段(std::ofstream&, std::string_view, std::string_view)`。

依据：

```text
计划/计划索引.md：P13b 全局共享迁移；B 类候选逐个判定迁移 / 复用 / 保留。
实施记录/20260624_P13_函数自定义主信息分类候选表.tsv：`私有_写字段` 为 B 类候选，未命中自定义主信息 / 节点 / 结构 / 协议对象。
全局共享函数类.ixx 文件头规则：无领域自定义主信息 / 领域类型参与的通用函数可进入本模块。
```

当前判断：

```text
`私有_转义字段值` 只把反斜杠、换行、回车、制表符转为文本转义序列。
`私有_写字段` 只向 std::ofstream 写入 `键=转义值\n`。
二者只依赖 C++ 标准库，不读取 / 写入项目结构，不参与机器业务状态判断。
`私有_写项字段` 暂不迁移：它依赖线程生命周期缓存的 `项.<索引>.<键>` 命名约定，保留在本模块。
```

已执行：

```text
1. 在 `全局共享函数类.ixx` 新增 `转义键值字段值(std::string_view)`。
2. 在 `全局共享函数类.ixx` 新增 `写入转义键值字段行(std::ostream&, std::string_view, std::string_view)`。
3. 删除本地 `私有_转义字段值` 和 `私有_写字段`，调用点改为全局函数。
4. 运行函数说明文档生成脚本和 Debug x64 构建。
```

验证证据：

```text
python .\tools\生成函数功能说明文档.py 成功；扫描源文件 127 个，函数条目 7163。
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo 成功；0 警告，0 错误。
复扫确认：`私有_转义字段值`、`私有_写字段` 已从源码和函数说明文档清除。
复扫确认：`转义键值字段值`、`写入转义键值字段行` 已进入 `全局共享函数类.ixx` 和函数说明文档。
复扫确认：`私有_写项字段` 仍保留在线程生命周期消息模块内，仅承接本模块 `项.<索引>.<键>` 命名约定。
```

禁止宣称：不宣称 P13 完成，不宣称函数去重全部完成，不宣称自我苏醒完成或初步成熟完成。
