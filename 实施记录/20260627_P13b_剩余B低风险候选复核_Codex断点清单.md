# 20260627 P13b 剩余 B 低风险候选复核 Codex断点清单

## 0. 恢复摘要

当前切片目标：

```text
P13b：在已迁移第五十小切片后，复核剩余 B 类低风险候选是否仍有可安全迁入 全局共享函数类.ixx 的小函数族。
```

依据：

```text
计划/计划索引.md：P13b 继续选择低风险 B 类小函数族。
计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md：B 类纯工具函数迁入 全局共享函数类.ixx；不触碰项目结构承载、业务事实、任务 / 方法 / 需求链路。
全局共享函数类.ixx 文件头规则：无领域自定义主信息 / 领域类型参与的通用函数可进入本模块；涉及领域类型或领域边界的共享函数不进入本模块。
```

已执行复核：

```text
按当前源码、函数说明文档、候选表和 P13 计划记录交叉过滤：
    1. 只取候选表中 分类A或B=B、迁移候选=候选、模块import风险=低-待复核。
    2. 排除计划记录中已闭合的小切片名称。
    3. 只保留当前源码仍能命中的函数名。
```

剩余候选判断：

```text
日志模块.ixx::g_init_mtx / g_inited / g_shutdown
    日志模块内部状态单例；不是可迁入全局的纯工具函数。

日志模块.ixx::utf8_path
    依赖日志模块 detail::utf8_to_wide_lossy 和 WinAPI 宽字符转换策略；
    迁入全局会把 Windows 转换策略带入全局共享模块，当前不迁。

数据库ADO模块.cpp::私有_BSTR转UTF8
    依赖 BSTR / COM 字符串语义；不属于无平台依赖纯工具。

特征类.cpp::私有_I64值域项宽度
    参数为 I64值域项，属于特征值域结构；不进入全局共享函数类。

任务模块.管理界面线程.impl.cpp::私有_worker逻辑ID
    生成任务管理 worker 生命周期逻辑 ID，属于任务管理线程命名协议；保留在任务管理模块。

线程生命周期消息模块.ixx::私有_消息目录 / 私有_已消费目录 / 私有_归档目录
    属于线程生命周期消息文件目录合同；保留在所属模块。

鱼巢.cpp::私有_解析状态引用_控制命令
    返回状态节点类*，参与控制命令状态引用解析；候选表误判，不是全局纯工具。

方法虚拟存在服务类.cpp::私有_默认允许自动召回 / 私有_默认允许默认执行
    属于方法虚拟存在服务策略默认值；保留在所属领域模块。

基础数据类型.h::编码I64为U64 / 编码I64为U64_ZigZag / 编码VecI64为VecIU64 /
基础数据类型.h::解码U64为I64 / 解码U64为I64_ZigZag / 解码VecIU64为VecI64
    已在基础数据类型.h 中作为底层类型编码工具承载；不迁入全局共享函数类。
```

当前判断：

```text
在当前候选表和 P13b 边界下，剩余 B 类低风险候选没有可继续安全迁入 全局共享函数类.ixx 的对象。
继续迁移会越过当前计划边界，进入以下任一路线之一：
    A. 中风险 B 类候选人工复核；
    B. A 类误判候选人工复核；
    C. 重新生成当前源码下的新分类候选表；
    D. 转入无调用 / 兼容路径清理队列。
```

当前停止点：

```text
P13b B 类低风险候选迁移暂止于第五十小切片。
需要下一步计划选择后再继续，不应在当前边界下硬迁领域函数或模块状态函数。
```

禁止宣称：

```text
不宣称 P13 完成。
不宣称函数去重全部完成。
不宣称自我苏醒完成或初步成熟完成。
```
