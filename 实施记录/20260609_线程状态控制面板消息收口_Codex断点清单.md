# 线程状态控制面板消息收口 Codex 断点清单

更新时间：2026-06-09 14:22 +08:00

## 当前切片

```text
计划：计划/20260609_线程状态控制面板消息收口实施计划_v0.1.md
范围：P1 / P1a / P2 起步，落线程生命周期消息结构、消息中间件文件缓存和控制面板消费入口。
目标：让项目线程创建、状态变化和退出消息先写入 消息中间件，再由控制面板线程信息模块消费成线程信息表。
```

## 依据文件

```text
AGENTS.md
规范/规范目录.md
规范/方法动作动态硬规则规范20260512.md
规范/自我线程与任务管理线程权责边界规范20260512.md
规范/主信息字段与结构新增准入约束规范20260519.md
规范/线程生命周期状态上报与控制面板线程信息规范20260609.md
规范/自我内部循环实现规范20260512.md
计划/计划索引.md
计划/20260609_线程状态控制面板消息收口实施计划_v0.1.md
```

## 禁止混入

```text
不改任务状态提交入口。
不把线程状态消息投递到自我治理邮箱。
不生成主动动态、被动动态、方法动作动态、世界树事实或需求输入。
不显示 OS / 系统线程枚举。
不新增 mock、伪入口、伪线程样本或伪动作动态。
不恢复任务筹办 / 任务执行为本能方法。
```

## 当前判断

```text
T1 显示层已闭合并提交。
本轮从 P1/P1a 起继续执行，先实现文件型消息中间件和控制面板信息表消费。
消息文件目录为 消息中间件。
```

## 已运行命令

```text
2026-06-09 14:22 读取 AGENTS.md、规范目录、002002 规范、计划索引和实施计划。
2026-06-09 14:22 读取 001 / 002 / 005 / 006001003 相关规范。
2026-06-09 14:22 git status --short --untracked-files=no：干净。
```

## 下一步

```text
1. 盘点现有线程启动、生命周期快照、控制面板快照生成和任务管理上行消息入口。
2. 设计最小线程生命周期消息结构和文件中间件 API。
3. 接入控制面板消费消息文件并更新页面线程状态树。
4. 接入自我线程、任务管理界面线程、任务管理工作线程、控制面板摘要线程创建 / 退出 / 状态上报。
5. 构建、生成控制面板 HTML，并用真实日志 / 消息文件 / 页面快照核对。
```

## 本轮验证预测

```text
预测时间：2026-06-09 14:50 +08:00
预测 1：Debug x64 构建应通过；若仅出现 /EDITANDCONTINUE 与 /INCREMENTAL:NO 相关 LNK4075，不作为本切片失败。
预测 2：真实运行 self-check-log 后，消息中间件应出现 thread_lifecycle_*.msg 或已消费/thread_lifecycle_*.msg，字段至少包含 消息ID、事件类型、发生时间、线程逻辑ID、系统线程ID、线程名称、线程用途、新生命周期状态。
预测 3：首批真实线程应至少覆盖 self-main、task-interface、task-worker-pool、task-worker-1 和 control-panel-summary；worker 数量按当前任务管理线程池配置自然产生，不伪造。
预测 4：panel-html 生成的 控制面板.html 应优先显示消息中间件消费后的项目线程信息，包含 来源=消息中间件/控制面板线程生命周期消息表，不显示系统线程枚举。
预测 5：本切片不应新增或依赖任务状态动作动态、世界树事实、需求输入或自我治理邮箱的新线程状态入口；旧任务管理上行线程状态路径若仍存在，记录为后续 P5 收口范围，不把 P1/P3 验证扩大成 P5 已完成。
```

## 本轮实际结果

```text
实际时间：2026-06-09 15:03 +08:00
已新增 线程生命周期消息模块.ixx，并接入 鱼巢.vcxproj / 鱼巢.vcxproj.filters。
线程生命周期消息先写入 消息中间件/thread_lifecycle_*.msg，控制面板消费后移动到 消息中间件/已消费。
控制面板线程信息表新增持久缓存 消息中间件/control_panel_thread_info_table.cache，避免已消费消息只留在进程内表导致跨进程生成 HTML 时丢失线程项。
控制面板线程状态页已改为优先读取 消息中间件 消费后的控制面板线程生命周期消息表；无消息时保留原项目快照占位。
首批发送点已接入 self-main、task-interface、task-worker-pool、task-worker-1..20、control-panel-summary。
本切片未新增线程状态到任务状态、需求、世界事实或方法动作动态的写入。
旧自我治理线程状态路径仍保留，属于后续 P5 收口范围；本轮不宣称 P5 完成。
```

## 本轮验证证据

```text
构建命令：
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：Exit 0；仅有 D455相机类.obj LNK4075 (/EDITANDCONTINUE 被 /INCREMENTAL:NO 忽略) 警告。

短等待真实运行命令：
.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=1000
结果：Exit 0；未触发弹窗；自检线程日志写入 ./日志。

HTML 生成命令：
.\x64\Debug\鱼巢.exe --panel-html
结果：Exit 0；生成 D:\鱼巢\控制面板.html。

消息/缓存证据：
消息中间件/control_panel_thread_info_table.cache 中 项数=24。
缓存包含 线程逻辑ID=self-main、task-interface、task-worker-pool、task-worker-1、control-panel-summary。
HTML 中线程列表显示 已知项目线程=24，包含 来源=消息中间件/控制面板线程生命周期消息表。
HTML 中显示 self-main、task-interface、task-worker-pool、task-worker-1 和 control-panel-summary 的线程详细信息。
检索未发现 线程生命周期消息/写入失败、扫描失败、消费跳过、消费失败、解析失败、线程信息表缓存失败。
检索未发现 系统线程列表、当前进程线程数、ToolHelp、TID= 作为控制面板线程信息来源。
```

## debug error 弹窗边界

```text
用户反馈 debug error 弹窗报错后已查日志。
日志/鱼巢_run_20260609.低值g:214382-214702 显示弹窗标题为 鱼巢 - 执行桥逻辑错误。
错误文本：执行缺失派生需求缺少执行桥输出的目标状态，任务=9，方法=q，需求特征=25B，当前状态和目标状态为空，已拒绝发送派生需求。
同类执行桥弹窗在本轮改动前 2026-06-09 14:06:37 已出现，日志行 189899-191707，任务=A，方法=1x，需求特征=24M。
当前判断：该弹窗是既有执行桥业务逻辑错误，不是本轮线程生命周期消息模块的写入、消费或控制面板显示错误。
本轮后续短等待 self-check-log 验证未复现弹窗。
```

## 下一步建议

```text
若继续本计划，下一切片应进入 P5：清理旧自我治理邮箱中的纯线程状态投递路径。
若优先处理 debug error 弹窗，应另开执行桥切片，围绕“执行缺失派生需求缺少执行桥输出的目标状态”定位方法 q / 1x 的执行桥输出字段来源，不并入线程消息收口切片。
```

## P5 自我治理邮箱收口预测

```text
预测时间：2026-06-09 15:16 +08:00
当前切片：P5 自我治理邮箱收口。
允许修改范围：计划/计划索引.md、自我线程消息协议相关模块、自我线程模块.impl.cpp、实施记录/20260609_线程状态控制面板消息收口_Codex断点清单.md；若构建发现直接依赖，再补充说明。
预测 1：纯线程状态变化不再通过 自我线程消息协议::从线程状态变化构造治理消息 投递到自我治理邮箱。
预测 2：自我线程生命周期变化仍会发送到 线程生命周期消息模块，并经 消息中间件 / 控制面板线程信息表显示。
预测 3：任务管理结果状态、派生需求、执行前许可、观察事实承接和自检报告仍保留治理消息路径。
预测 4：Debug x64 构建应通过；若仅出现 /EDITANDCONTINUE 与 /INCREMENTAL:NO 相关 LNK4075，不作为失败。
预测 5：真实运行 self-check-log 后，最新日志窗口不应出现由纯线程状态治理消息产生的 线程状态变更 内部世界入账；同时消息中间件缓存仍有线程生命周期消息。
预测 6：本切片不处理既有 执行桥逻辑错误 弹窗，也不宣称 P5 以外的 D455 / 任务执行链闭合。
```

## P5 协议源头修正复跑预测

```text
预测时间：2026-06-09 15:19 +08:00
代码状态：任务管理线程状态消息已从 工作线程状态消息 / 线程状态变化载荷 改为 结果与关键中间状态回流消息，仍保留 附加载荷.任务管理线程状态 供执行尝试归并。
日志窗口：日志/鱼巢_run_20260609.低值g 第 229446 行之后。
预测 1：Debug x64 构建已通过，真实运行 self-check-log 应 Exit 0，不应新增 debug error 弹窗。
预测 2：最新日志窗口中不应再出现 线程状态治理消息跳过落账；若出现，需要继续定位是否仍有纯线程状态治理消息进入邮箱。
预测 3：最新日志窗口中任务管理线程状态可继续入队，但不应再以 工作线程状态消息 / 线程状态变化上报 作为处理类型或动作语义。
预测 4：任务管理结果状态、派生需求、执行前许可、观察事实承接、自检报告仍应可见。
预测 5：消息中间件/已消费 或 control_panel_thread_info_table.cache 仍应保留线程生命周期消息证据。
```

## P5/P6 实际结果

```text
实际时间：2026-06-09 15:22 +08:00

代码结果：
    自我线程生命周期切换不再投递 `线程状态变化 -> 自我治理邮箱`。
    `上报线程状态变化` / `切换线程状态并上报` 已停用治理投递，仅记录停用日志。
    自我治理消息消费端遇到旧线程状态治理消息时跳过内部世界落账。
    `从任务管理线程状态消息构造治理消息` 不再调用 `从线程状态变化构造治理消息`，不再设置 `附加载荷.线程状态变化`，处理类型改为 `结果与关键中间状态回流消息`。
    任务管理线程状态仍保留 `附加载荷.任务管理线程状态`、执行尝试汇总和执行对账结果。

验证命令：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=1000
    .\x64\Debug\鱼巢.exe --panel-html

验证结果：
    构建 Exit 0；仅有 D455相机类.obj LNK4075 (/EDITANDCONTINUE 被 /INCREMENTAL:NO 忽略) 警告。
    self-check-log Exit 0；未新增 debug error 弹窗。
    panel-html Exit 0；生成 D:\鱼巢\控制面板.html。

日志证据：
    日志文件：日志/鱼巢_run_20260609.低值g。
    最新窗口起点：第 229446 行之后。
    线程状态治理消息跳过落账：0。
    工作线程状态消息：0。
    线程状态变化上报：0。
    自我线程/任务管理上行消息入队 | 消息=任务管理线程状态：70。
    任务管理结果状态：35。
    任务管理派生需求：71。
    任务管理执行前许可：3。
    任务管理观察事实：12。
    自检报告：9。
    执行桥逻辑错误：0。
    线程生命周期消息/写入失败、消费失败、解析失败、线程信息表缓存失败：0。

控制面板证据：
    控制面板.html 中 `已知项目线程=24` 出现 3 次。
    控制面板.html 中 `消息中间件/项目线程快照` 出现 1 次。
    控制面板.html 中 self-main、task-interface、task-worker-pool、task-worker-1、control-panel-summary 均出现。
    控制面板.html 中 系统线程列表、当前进程线程数、ToolHelp、TID= 均为 0。
    消息中间件/control_panel_thread_info_table.cache 项数=24，包含 self-main、task-interface、task-worker-pool、task-worker-1、control-panel-summary。
    消息中间件/已消费 中存在 2026-06-09 15:20:47 的 thread_lifecycle_*.msg。

当前判断：
    计划/20260609_线程状态控制面板消息收口实施计划_v0.1.md 的 P0-P6 已闭合。
    当前计划索引已更新为无正在执行的主动切片。

禁止宣称：
    不宣称自我苏醒完成、初步成熟完成或内部循环全部完成。
    不把控制面板线程信息表当任务状态、需求满足、世界事实或安全 / 服务结算权威。
    不把既有执行桥逻辑错误并入线程消息收口完成证据。
```
