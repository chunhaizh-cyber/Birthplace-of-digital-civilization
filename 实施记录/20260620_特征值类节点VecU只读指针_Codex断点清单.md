# 20260620 特征值类节点 VecU 只读指针 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_特征值类逻辑纠偏计划_v0.1.md` 的 1.1，新增节点指针版本 VecU 只读函数。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_特征值类逻辑纠偏计划_v0.1.md
特征值类.h
说明书/函数功能说明文档.md
```

本切片范围：

```text
1. 在 特征值类.h 增加 特征值类::取节点VecU只读指针。
2. 函数只读取 特征值类::节点类 内部 VecIU64 值的 const 指针。
3. 不修改特征值写入、哈希缓存、分层索引或句柄语义。
4. 不替换 特征类.cpp direct 主信息.值；替换工作返回特征类 1.2 后执行。
```

验证前预测：

```text
1. rg -n "取节点VecU只读指针|取VecU只读指针" 特征值类.h 说明书/函数功能说明文档.md 应命中新旧两个只读函数。
2. 函数说明文档生成器应通过，函数条目增加 1。
3. Debug x64 构建应通过。
```

## 1. 实施记录

```text
1. 特征值类.h 新增：
   static const VecIU64* 取节点VecU只读指针(const 节点类* n) noexcept;
2. 该函数返回 n ? &n->主信息.值 : nullptr。
3. 已运行 python .\tools\生成函数功能说明文档.py，函数条目从 6880 增至 6881。
```

## 2. 验证结果

```text
1. rg -n "取节点VecU只读指针|取VecU只读指针" 特征值类.h 说明书/函数功能说明文档.md
   结果：
        特征值类.h:115 取VecU只读指针
        特征值类.h:122 取节点VecU只读指针
        说明书/函数功能说明文档.md:1154 取VecU只读指针
        说明书/函数功能说明文档.md:1212 取节点VecU只读指针

2. python .\tools\生成函数功能说明文档.py
   结果：退出码 0；源码文件 128；函数条目 6881；已生成 说明书/函数功能说明文档.md。

3. msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\20260620_特征值类节点VecU只读指针_msbuild.log
   结果：退出码 0；已成功生成；0 个警告；0 个错误；生成 D:\鱼巢\x64\Debug\鱼巢.exe。
```

## 3. 当前判断

```text
1. 本切片已闭合：特征值类已提供节点指针版本 VecU 只读函数。
2. 不宣称特征值类逻辑纠偏完成。
3. 下一步返回 计划/20260620_特征类逻辑纠偏计划_v0.1.md 的 1.2，替换三维体素链 direct 主信息.值。
```
