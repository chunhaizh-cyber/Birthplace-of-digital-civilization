# 20260606_外设四本能方法P8a真实日志验证准备与预测记录

## 切片

```text
计划：计划/20260605_外设四本能方法需求树生成因果链修订计划_v0.1.md
切片：P8a 真实日志验证准备与预测
目标：先写预测，再走真实运行路径，用日志模块、状态快照、动作动态和错误日志核对四方法链路。
```

## 可用真实运行入口

```text
保留入口：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=<毫秒>

依据：
    鱼巢.cpp 中 --self-check-log 会启动自我线程；
    自检线程可启动时请求休眠期自检；
    最后输出自检线程摘要，并把日志写入 ./日志；
    该入口不是已删除的 --*-check 回归入口。
```

## 验证前预测

```text
若真实运行路径自然触发外设四方法链：
    需求树应生成目标宿主 + 目标特征 + 目标状态需求，而不是 调用观察 / 调用识别 / 调用扫描 / 调用跟踪 需求；
    任务筹办应按结果能力填充候选集，再选择当前方法；
    方法缺外设材料时应生成观察验证 / 识别 / 扫描 / 跟踪对应等待项；
    方法执行成功后应写正式结果特征；
    提交入口执行时应生成当前提交方法动作动态，并通过 来源动作动态 串接上游方法动作动态。

若当前运行路径没有真实 D455 自然触发或自然需求未走到该链：
    只能记录未触发 / 证据不足；
    不得用模拟入口、伪造样本、伪造动作动态或已删除回归入口替代。
```

## 计划命令

```powershell
.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=3000
```

## 预期核对关键词

```text
目标特征
候选集填充
当前方法
外设观察等待项
自我_验证外设观察像素簇候选
自我_识别外设观察材料
自我_扫描已识别区域
自我_跟踪指定存在
提交_当前观察范围可观测单位存在对应事实
提交_观察存在发现事实
提交_观察存在特征值变化事实
提交_指定存在跟踪事实
来源动作动态
不写需求满足
不写安全值
```

## 待执行

```text
P8a 预测已写入，下一步执行真实运行命令并检索日志证据。
```

## 执行结果

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=3000

输出文件：
    运行输出/20260606_外设四本能方法P8a_self_check.log

退出码：
    0

最新运行日志：
    日志/鱼巢_run_20260606.低值g
```

## 日志核对

```text
已命中：
    命令行/自检日志等待开始；
    命令行/自检日志等待完成；
    自检线程/休眠期自检 开始 / 完成；
    任务筹办跟踪日志中有 目标宿主、目标特征、当前状态、目标状态；
    因果路径派生需求草案字段完整，包含 目标宿主、目标特征、当前状态、目标状态；
    候选集填充全树遍历完成；
    外设观察等待项创建延后，且记录 直接写需求树=0、直接写世界树=0。

未命中：
    自我_验证外设观察像素簇候选；
    自我_识别外设观察材料；
    自我_扫描已识别区域；
    自我_跟踪指定存在；
    四个提交入口日志；
    来源动作动态。

错误 / 异常：
    未发现 ERR / FTL / 弹窗 / 访问冲突 / 崩溃；
    日志中存在任务管理结果状态“部分完成 / 成功=否”，属于当前需求执行未闭合证据，不是进程失败。
```

## 判断

```text
P8a 只能证明：
    --self-check-log 真实运行入口可用；
    当前运行路径仍按正式目标特征进入筹办；
    候选集填充仍走结果能力；
    本轮没有自然触发四方法和四提交入口。

P8a 不能证明：
    四方法自然业务链已通过；
    提交入口来源动作动态已在自然运行中入账；
    D455 观察报告已由任务 / 方法 / 提交入口自然闭环消费。
```

## 下一步

```text
进入 P8b：真实 D455 单帧材料入口补充验证。
该入口只能作为真实外设材料证据，不得替代任务管理 -> 自我方法 -> 提交入口自然链验收。
```
