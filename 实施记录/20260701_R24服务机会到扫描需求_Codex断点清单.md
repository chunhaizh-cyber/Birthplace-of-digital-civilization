# 20260701 R24 服务机会到扫描需求 Codex断点清单

## 0. 恢复摘要

当前切片：外设模块优化完成后，继续 R24 内部治理闭环测试，修正“服务机会检查子需求没有继续生成正式扫描需求”的源头缺口。

## 1. 依据

- `计划/计划索引.md`：R24 仍为明确堵点；外设报告队列供料恢复，但运行期需求树未出现扫描需求。
- `规范/6120_子规范_自我存在服务与生存基本因果闭环_20260701.md`：无直接服务触发事实时允许生成间接服务维护需求；间接服务感知需求来自服务触发事实监听、服务机会检查、服务能力校准和服务根缺口检查。
- `规范/6320_子规范_外设观察特征与自我场景认知分层_20260701.md`、`规范/6360_子规范_相机外设综合工作流程_20260701.md`：当前阶段内部治理为了安全 / 服务增加、任务结果验证或当前场景状态维护生成扫描需求；扫描需求不得由外设报告或日志替代。
- `规范/5230_子规范_任务筹办与执行边界_20260701.md`：不得在任务筹办中按安全、服务、观察、外设等领域词硬编码缺口目标。

## 2. 当前判断

旧的服务根直接生成扫描需求已删除，不能恢复。当前可执行修正点在自我线程需求树生长源头：服务根生成 `服务机会检查状态` 子需求后，该子需求应按自身必要条件继续生成 `已识别区域扫描入账状态` 扫描子需求。

## 3. 预测

- 服务根需求 2 仍直接生成服务触发事实监听、服务机会检查、服务能力校准三类间接服务维护子需求。
- 不出现 `父需求=2 -> 已识别区域扫描入账状态` 的直接扫描需求。
- 运行期需求树出现正式 `已识别区域扫描入账状态` 需求，直接父需求为 `服务机会检查状态` 子需求。
- 扫描子需求经 `需求类::构造新增原子需求指令` 入树，权重由 `刷新子需求权重` 按父需求活跃子需求整数平分规则刷新。

## 4. 禁止宣称

不宣称 R24 完成、内部治理闭环完成、直接服务正向触发事实路径已接入、服务 G0 / D2 正向闭环完成、自我苏醒完成或初步成熟完成，直到真实运行满足扫描需求硬判据并记录证据。

## 5. 实施改动

- `自我线程模块.impl.cpp`：新增 `私有_刷新服务机会检查扫描子需求_线程侧`，在服务根必要条件刷新后查找已有 `服务机会检查状态` 子需求，并在该直接父需求下生成 `已识别区域扫描入账状态` 扫描子需求。
- 删除新增函数内不必要的向上追溯根节点局部逻辑，直接使用已传入的服务根作为更新入口树根；直接父需求仍由 `需求类::构造新增原子需求指令(服务机会需求, ...)` 写入。
- `说明书/函数功能说明文档.md`：已重新生成函数说明。

## 6. 验证结果

- `python .\tools\check_specs.py`：hard issues 0，warnings 0。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`：退出码 0，0 警告，0 错误。
- `.\x64\Debug\鱼巢.exe --d455-report-queue-smoke`：退出码 0；`观察报告入队=120`。
- `.\x64\Debug\鱼巢.exe --demand-tree-growth-summary-live`：退出码 0；输出 `需求树生长摘要`，规模 `总数=6`；运行日志出现扫描需求入树。
- 最终普通复跑 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000`：退出码 0。

关键日志锚点：

```text
日志/鱼巢_run_20260701.低值g:22371
自我线程/服务机会检查扫描子需求入树 | 服务根=2 | 服务机会需求=4 | 新需求=6 | 目标特征=已识别区域扫描入账状态 | 直接父需求=4 | 服务根直连扫描=0

日志/鱼巢_run_20260701.低值g:23369
自我_扫描当前场景/函数自身结果 | 外设数据项总数=0 | 已识别区域扫描入账状态=1 | 扫描主动因果信息数量=10

日志/鱼巢_run_20260701.低值g:23838
自我线程/外设观察事实承接完成 | 成功=是 | 报告可回查=是 | 来源需求=6

最终普通复跑统计：
服务机会检查扫描子需求入树=1
父需求2直连扫描=0
旧P7a服务到扫描消息=0
扫描函数自身结果=1
报告可回查是=6
报告可回查否=3
观察事实承接成功=2
观察事实承接失败=1
逻辑错误=0
```

## 7. 当前结算

P7a 硬判据已满足：

```text
一. 因果 / 来源链：
服务根需求 2
-> 服务机会检查状态子需求 4
-> 服务机会检查必要条件
-> 已识别区域扫描入账状态扫描需求 6

二. 运行期需求树：
运行日志和 live 需求树摘要均显示正式扫描需求入树。
```

R24 根需求仍未整体闭合：

- `自我_扫描当前场景` 仍显示 `外设数据项总数=0`，说明扫描函数自身尚未消费已入队外设数据项。
- 外设观察事实承接已经出现 `报告可回查=是` 的成功样本，但同轮仍有失败样本；这属于下游扫描 / 观察材料承接边界，不再否定“扫描需求已生成”硬判据。
- 一次最终 self-check 曾退出 `-1073741819`；ProcDump 复跑未生成 dump 且进程退出 0，随后普通复跑退出 0。该访问冲突已登记为 `计划/追根因_20260701_R24自检访问冲突_v0.1.md`，恢复条件为再次复现或取得 dump / 调试栈。

完成声明边界：本切片只能声明“服务机会检查到扫描需求的硬判据已满足”，不能声明 R24 完成、内部治理真正闭环完成、服务 G0 / D2 正向闭环完成、自我苏醒完成或初步成熟完成。

## 8. 外设优化后复测预测

复测触发：外设模块优化完成后，继续内部治理闭环测试。

预测：

- `--d455-report-queue-smoke` 仍应证明外设报告队列可入队，且不直接替代扫描需求节点。
- `--demand-tree-growth-summary-live` 和 self-check 仍应出现 `服务机会需求=4 -> 扫描需求=6`，且 `服务根直连扫描=0`。
- 关键新增观察点是 `自我_扫描当前场景/函数自身结果` 的 `外设数据项总数`。若外设优化已打通扫描消费，期望从旧证据的 `0` 变为非 `0`；若仍为 `0`，则 R24 继续停在“扫描需求已生成，但扫描方法未消费已合法外设数据项”的下游堵点。
- 观察事实承接若仍出现成功 / 失败混合，不能否定扫描需求硬判据，但会继续阻止声明 R24 根需求整体闭合。

## 9. 外设优化后复测结果与新堵点

复测命令：

- `python .\tools\check_specs.py`：退出码 0；hard issues 0，warnings 0。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`：退出码 0；0 警告，0 错误。
- `.\x64\Debug\鱼巢.exe --d455-report-queue-smoke`：退出码 0；观察报告入队=120，综合稳定=稳定。
- `.\x64\Debug\鱼巢.exe --demand-tree-growth-summary-live`：退出码 0；需求树总数=6，权重异常=0，目标绑定异常=0。
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000`：退出码 0，未复现访问冲突。

关键证据：

```text
日志/鱼巢_run_20260701.低值g:44939
自我线程/服务机会检查扫描子需求入树 | 服务根=2 | 服务机会需求=4 | 新需求=6 | 目标特征=已识别区域扫描入账状态 | 直接父需求=4 | 服务根直连扫描=0

日志/鱼巢_run_20260701.低值g:46723
自我_扫描当前场景/函数自身结果 | 报告ID=0 | 来源帧ID=0 | 外设数据项总数=0 | 已识别区域扫描入账状态=1 | 扫描主动因果信息数量=10

日志/鱼巢_run_20260701.低值g:47351
自我线程/外设观察事实承接完成 | 成功=是 | 报告可回查=是 | 来源需求=A
```

当前判断：

- 外设优化已恢复供料，smoke 与 self-check 均显示外设线程稳定入队。
- 运行期扫描需求仍由 `服务机会需求=4` 派生，未回退到服务根直连扫描。
- 扫描方法自身仍未消费外设数据项，`外设数据项总数` 仍为 0。

新增独立堵点：

```text
日志/鱼巢_exception_20260701.低值g:7-8
任务管理工作线程/派生需求消息构造逻辑错误 | 缺失槽位=父需求 | 任务=8 | 类型=缺条件 | 需求特征=ez | 直接写需求树=0 | 实际发送派生需求消息=0
```

该堵点说明：扫描后续的因果路径派生需求构造仍有路径没有携带直接父需求。下一步只追这条来源任务 / 因果路径派生链，不新增审计字段、不恢复根追溯字段、不用拦截兜底替代源头修正。

## 10. 派生需求父需求槽复核预测

复核触发：外设模块优化后继续 R24 内部治理闭环测试，上一轮 self-check 暴露 `任务管理工作线程/派生需求消息构造逻辑错误 | 缺失槽位=父需求 | 任务=8`。

已核对代码链：

- `任务模块.筹办.ixx::构造正式派生需求消息草案_筹办查询` 直接把 `视图.来源需求` 写入 `父需求主键`。
- `任务模块.筹办.ixx::记录派生需求消息草案统计_筹办查询` 只把 `派生消息字段完整_筹办查询` 通过的消息推入待上报集合。
- `任务模块.管理工作线程.ixx::私有_规范化筹办派生需求消息` 不写空 `父需求主键`，只补齐来源因果、目标特征、目标主体、状态等主键槽。

当前预测：

- 若 Debug x64 模块接口 / 增量编译缓存存在旧结构布局，干净重建后同一链路应不再出现 `稳定键=...||3W1...` 这类父需求空位。
- 若干净重建后仍复现，则根因不再按缓存处理，转为检查 `结构_任务管理派生需求消息` 在 `任务模块.筹办` 到 `任务模块.管理工作线程` 跨模块返回 / 移动时的结构定义、导入顺序或旧字段残留。

锁处理：

- `.codex_locks/build-run.lock` 记录 PID `27000`，本轮检查该 PID 已不存在，未发现正在运行的 `msbuild` / `鱼巢.exe`；判定为上一轮陈旧锁，允许清理后执行本轮 Rebuild。

## 11. 父需求槽丢失根因与修正

干净 Rebuild 后复跑仍复现父需求槽丢失：

```text
日志/鱼巢_run_20260701.低值g:48906
因果路径筹办正式派生需求消息草案样本 | 父需求=6 | 来源因果=3Nn | 需求特征=ez | 字段完整=1

日志/鱼巢_run_20260701.低值g:48940
任务管理工作线程/因果路径派生需求主键槽补齐 | 稳定键=...|1||3Nn|... | 缺父需求
```

源码根因：

- `任务模块.筹办.ixx::丢弃已绑定方法时的泛化学习缺口` 在未确认是否存在泛化学习缺口前，先把所有待上报消息 `std::move` 到临时 `保留`。
- 当 `丢弃数量 == 0` 时函数直接返回，原 `结果.待上报派生需求消息集` 中的消息已经处于 moved-from 状态。
- 管理线程后续能用指针补回来源因果、目标主体、目标特征和状态主键，但父需求只有 `父需求主键`，没有指针兜底，因此稳定复现父需求空槽。

实施修正：

- `任务模块.筹办.ixx`：先扫描是否存在需要丢弃的泛化学习信息补齐派生消息；不存在时直接返回，不移动原消息。
- 该修正不新增追溯根字段、审计字段或拦截兜底；修的是筹办源头清理逻辑，保证已经构造完整的派生需求消息不会被无意义移动破坏。

## 12. 父需求槽修正后验证预测

验证命令：

- `python .\tools\check_specs.py`
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000`

预测：

- 规范治理仍应 hard issues 0、warnings 0。
- Debug x64 构建应通过。
- self-check 应退出码 0，不再出现 `任务管理工作线程/派生需求消息构造逻辑错误 | 缺失槽位=父需求`。
- 若仍出现 `自我_扫描当前场景/函数自身结果 | 外设数据项总数=0`，只能说明 R24 下游扫描消费仍未闭合，不能否定本切片父需求槽修正。
- 不宣称 R24 根需求整体闭合、内部治理闭环完成、自我苏醒完成或初步成熟完成。

## 13. 父需求槽修正后验证结果

验证命令：

- `python .\tools\check_specs.py`：退出码 0；hard issues 0，warnings 0。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`：退出码 0；0 警告，0 错误。
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000`：退出码 0。

关键证据：

```text
日志/鱼巢_run_20260702.低值g:47
自我线程/服务机会检查扫描子需求入树 | 服务根=2 | 服务机会需求=4 | 新需求=6 | 目标特征=已识别区域扫描入账状态 | 直接父需求=4 | 服务根直连扫描=0

日志/鱼巢_run_20260702.低值g:1300
因果路径筹办正式派生需求消息草案样本 | 父需求=6 | 来源因果=3Od | 需求特征=ez | 字段完整=1 | 直接写需求树=0 | 实际发送派生需求消息=0
```

复核计数：

```text
派生需求消息构造逻辑错误=0
缺失槽位=父需求=0
因果路径派生需求主键槽补齐=0
缺父需求=[1-9]=0
父需求=6=22
逻辑错误=0
```

当前结算：

- `任务模块.筹办.ixx::丢弃已绑定方法时的泛化学习缺口` 的 moved-from 父需求槽丢失缺陷已修正并通过真实 self-check 复跑验证。
- 本切片没有新增追溯根字段、父需求指针兜底、审计兜底或拦截式修正。
- R24 仍未整体闭合。

## 14. 下游未闭合问题

同轮 self-check 仍显示：

```text
日志/鱼巢_run_20260702.低值g:841
自我_扫描当前场景/函数自身结果 | 报告ID=0 | 来源帧ID=0 | 外设数据项总数=0 | 已取出=0 | 已解析=0 | 已闭合=0 | 已识别区域扫描入账状态=1 | 扫描主动因果信息数量=10

日志/鱼巢_run_20260702.低值g:1255
自我线程/外设观察事实承接完成 | 成功=是 | 报告可回查=是 | 来源需求=6

日志/鱼巢_run_20260702.低值g:2274
自我线程/外设观察事实承接完成 | 成功=否 | 报告可回查=否 | 来源需求=6

日志/鱼巢_exception_20260702.低值g:1-2
双目相机本能方法结果 | 方法=自我_验证外设观察像素簇候选 | 成功=0 | 失败原因=执行缺口_当前方法条件不满足 | 消息=外设观察像素簇候选验证条件不足:缺少D455逐簇识别报告
```

下游判断：

- 扫描需求已经入树，且来源仍为 `服务机会需求=4 -> 扫描需求=6`，没有回退到服务根直连扫描。
- 扫描函数自身仍未消费外设数据项，`外设数据项总数=0`。
- 外设观察事实承接仍有成功 / 失败混合样本；候选验证条件不足是 R24 下游外设 / 扫描材料条件问题，不属于本切片父需求槽缺陷。
- 下一切片若继续 R24，应只追 `自我_扫描当前场景` 为什么没有取到已合法外设当前数据集，或追 `缺少D455逐簇识别报告` 是否属于外设材料条件缺口；不得把日志、等待项或外设报告文本反推成业务闭合。

## 15. 872dc58 后外设数据项复跑预测

触发：用户已完成并提交 `872dc58 优化D455跨帧稳定子集发布门`。该切片已将 D455 L1 稳定子集发布改为首次稳定 / 每 5 帧复验 / 活跃等待项驱动，并补深度、空间、可回查材料门；Debug x64 构建、`--d455-report-queue-smoke` 和 `--d455-video-queue-smoke` 已由用户报告通过。

本轮复跑目标：

- 只检查新提交后 `自我_扫描当前场景/函数自身结果` 的 `外设数据项总数` 是否从 0 变为非 0。
- 同步观察 `已取出`、`已解析`、`已闭合`、`报告ID`、`来源帧ID`、外设等待项和观察事实承接是否形成同轮可解释链。

验证命令：

- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000`

预测：

- 若稳定子集发布门已经打通扫描消费链，期望最新 `自我_扫描当前场景/函数自身结果` 出现 `外设数据项总数>0`，且 `已取出/已解析/已闭合` 至少部分非 0，报告 ID 或来源帧 ID 不再全为 0。
- 若仍为 `外设数据项总数=0`，则说明 D455 报告生产 / 稳定子集发布已改善，但扫描方法自身仍未读取合法外设当前数据集，R24 继续停在扫描消费边界。
- 不用 `--d455-report-queue-smoke`、等待项创建、D455 入队日志或控制台输出替代扫描函数自身消费证据。
- 不宣称 R24 根需求整体闭合、内部治理闭环完成、自我苏醒完成或初步成熟完成。

## 16. 872dc58 后外设数据项复跑结果

验证命令：

- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000`

命令层结果：

- 外层 PowerShell 命令超时，未捕获 exe 退出码。
- 后续复核确认 `鱼巢.exe` 进程已退出，锁记录进程 `pid=33608` 也已退出；`.codex_locks/build-run.lock` 判定为本轮遗留 stale 锁，可清理。
- 重定向文件 `日志/codex_R24_external_data_selfcheck_20260702_after_872dc58.log` 长度为 0；业务判断以项目运行日志为准。

关键证据：

```text
日志/鱼巢_run_20260702.低值g:18868
自我_扫描当前场景/函数自身结果 | 报告ID=0 | 来源帧ID=0 | 外设数据项总数=0 | 已取出=0 | 已解析=0 | 已闭合=0 | 已识别区域扫描入账状态=1 | 扫描主动因果信息数量=10

日志/鱼巢_run_20260702.低值g:21112
命令行/自检日志等待完成 | 等待毫秒=60000

日志/鱼巢_run_20260702.低值g:21215
任务管理界面线程/观察事实更新任务唤醒裁决 | 任务=A | 来源需求=C | 原状态=等待中 | 新状态=筹办中 | 报告ID=1444 | 等待项ID=4 | 报告ID写入任务输入=1

日志/鱼巢_run_20260702.低值g:21240
任务筹办跟踪 | 外设观察报告ID参数筹办 | 任务=A | 报告ID=1444 | 输入场景=4T0 | 写输入场景=1 | 写输入条件包=1 | 来源=任务虚拟存在观察事实唤醒

日志/鱼巢_run_20260702.低值g:21255
自我线程/观察事实更新事件发布 | 事件类型=外设报告承接完成 | 报告ID=1444 | 等待项ID=4 | 来源需求=C | 承接成功=是 | 报告可回查=是

日志/鱼巢_run_20260702.低值g:21276
外设线程_D455深度相机/停止 | 样本=1200 | 观察报告入队=1200 | 观察报告入队FPSx100=2911 | 综合稳定=稳定
```

复核计数：

```text
最新扫描函数外设数据项总数=0
外设数据项总数非0=0
已取出/已解析/已闭合非0=0
本轮新增缺少D455逐簇识别报告=0
```

当前判断：

- `872dc58` 后，D455 报告生产、稳定材料承接、观察事实唤醒和报告 ID 参数筹办已有真实日志证据。
- 但 `自我_扫描当前场景` 自身仍在报告到达前结算，最新扫描函数结果仍为 `外设数据项总数=0`，不能判定扫描消费链已闭合。
- 下一步应追扫描任务/方法在观察事实唤醒后为什么没有重新进入 `自我_扫描当前场景` 消费报告，或为什么消费入口没有读取 `任务虚拟存在观察事实唤醒` 写入的报告 ID。
- 不宣称 R24 根需求整体闭合、内部治理闭环完成、自我苏醒完成或初步成熟完成。

## 17. 延长自检窗口复跑预测

触发：上一轮 60 秒 self-check 中，外设报告 `1444` 在 `命令行/自检日志等待完成 | 等待毫秒=60000` 后才完成承接，任务 `A` 已从等待重筹办进入就绪并入队执行请求，但随即遇到 `停止请求中` 和 `状态提交确认停止丢弃`。

本轮验证命令：

- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=120000`

预测：

- 若问题只是 60 秒运行窗口不足，120 秒窗口内应能看到报告 ID 写入后的任务 `A` 执行入口、对应本能方法执行结果，并进一步推动父任务或扫描相关任务重筹办。
- 若仍只出现报告 ID 写入、筹办就绪和执行请求入队，但没有后续执行入口或消费证据，则根因应转向任务管理停止/重入调度语义，而不是 D455 供料不足。
- 若出现新的 `自我_扫描当前场景/函数自身结果` 且 `外设数据项总数>0`、`已取出/已解析/已闭合` 非 0，才可判定扫描函数开始消费合法外设当前数据集。
- 等待项、报告可回查、外设入队、任务筹办就绪或日志文本仍不能替代扫描函数自身消费证据。
- 不宣称 R24 根需求整体闭合、内部治理闭环完成、自我苏醒完成或初步成熟完成。

## 18. 外设报告等待项匹配根因锚点

触发：120 秒 self-check 复跑后，D455 已持续生成 `扫描变化报告`，且报告携带 `匹配等待项ID=2`，但任务管理外设消息承接仍反复输出 `状态=无匹配报告 | 等待项ID=2 | 报告ID=0`。

证据锚点：

```text
日志/鱼巢_run_20260702.低值g:29852
外设线程_D455深度相机/扫描变化报告入队 | 报告ID=7050 | 类型=扫描变化报告 | 模式=已识别场景扫描变化 | 匹配等待项ID=2 | 等待项ID=2

日志/鱼巢_run_20260702.低值g:29855
任务管理外设消息承接/处理一次 | 状态=无匹配报告 | 等待项ID=2 | 报告ID=0 | 观察运行模式=已识别场景扫描变化 | 报告类型=扫描变化报告
```

代码锚点：

```text
外设观察报告队列.ixx:3420
按等待项匹配外设观察报告 当前先用最新外设报告建立同帧锚点，再只在该帧内查找等待项报告。
```

当前判断：

- D455 供料已经出现合法扫描变化报告，等待项 ID 也不是空字段。
- 任务管理无法匹配的直接嫌疑是队列匹配口径：高频逐簇 / 稳定子集报告会在扫描报告后进入下一帧，导致“最新帧锚点”不是扫描报告所在帧，显式等待项报告被跳过。
- 修复范围应限制在等待项匹配读取逻辑：对已经绑定 `匹配等待项ID` 的报告先按等待项 ID、类型、模式、年龄和约束做显式命中；没有显式命中时保留原来的最新同帧组匹配。
- 不能让扫描方法绕过外设观察报告队列直接读取 D455 内部材料，不能把普通旧报告作为扫描输入兜底。

## 19. 显式等待项报告匹配修复验证预测

代码变更：

- `外设观察报告队列.ixx::按等待项匹配外设观察报告` 在原“最新同帧组”匹配前，先扫描队列中未消费且 `匹配等待项ID == 等待项.等待项ID` 的报告，并继续复用 `外设观察_报告满足等待项` 检查外设 ID、运行模式、报告类型、约束 generation、目标特征 generation 和最大年龄。
- 没有显式等待项报告命中时，仍走原来的最新同帧组匹配路径。

验证命令：

- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=120000`

预测：

- 构建应为 Debug x64 退出码 0。
- self-check 中 `任务管理外设消息承接/处理一次` 应出现 `等待项ID=2`、`报告ID>0`、`命中报告=1` 的样本，不再持续只有 `无匹配报告 | 报告ID=0`。
- 若承接请求进入扫描任务重筹办链，后续 `自我_扫描当前场景/函数自身结果` 应至少出现一次 `报告ID>0`、`来源帧ID>0`、`外设数据项总数>0`。
- 若仅修复承接匹配但扫描仍为 `外设数据项总数=0`，则下一堵点转向报告 ID 从承接请求到扫描输入场景 / 输入条件包的传递或任务重入时序。
- 不用 D455 入队日志、等待项存在或承接日志单独替代扫描函数自身消费证据；不宣称 R24 根需求整体闭合、内部治理闭环完成、自我苏醒完成或初步成熟完成。

## 20. 等待项报告容量裁剪修复预测

触发：上一轮显式等待项匹配修复后，Debug x64 构建通过，但 120 秒 self-check 仍出现 `自我_扫描当前场景/函数自身结果 | 报告ID=0 | 来源帧ID=0 | 外设数据项总数=0`。日志中 D455 仍持续入队 `扫描变化报告` 且携带 `匹配等待项ID=2`，但任务管理承接仍显示 `等待项ID=2 | 报告ID=0`。

当前根因判断：

- 显式等待项匹配路径本身不足以命中，说明报告很可能在承接读取前已不在 `状态.报告队列` 中。
- `外设观察报告_裁剪容量已加锁` 默认容量只有 256，旧 `外设观察报告_容量裁剪保护` 仅保护 L1 `逐簇识别报告`；未消费的 `扫描变化报告` / `跟踪报告` 即使已经绑定 `匹配等待项ID`，也可能被高频入队裁剪掉。
- 这属于队列中间层保留语义缺口，不是扫描函数绕过队列取 D455 内部材料的理由。

代码变更：

- `外设观察报告队列.ixx::外设观察报告_容量裁剪保护` 在未过期前新增保护：`!已消费 && 匹配等待项ID != 0`。
- 文件头规则同步明确：上层可消费“最新同帧报告组”或“已绑定显式等待项的未消费报告”，仍禁止跨帧回退旧材料伪造队列非空。
- 未改变 TTL 过期清理、未新增字段 / 结构体、未让扫描函数直连 D455 内部材料。

验证命令：

- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=120000`

预测：

- 构建应为 Debug x64 退出码 0。
- self-check 中应出现 `任务管理外设消息承接/处理一次 | 等待项ID=2 | 报告ID>0 | 命中报告=1 | 报告类型=扫描变化报告`。
- 若承接后的报告 ID 成功进入扫描输入，后续 `自我_扫描当前场景/函数自身结果` 应出现 `报告ID>0`、`来源帧ID>0`、`外设数据项总数>0`。
- 若承接命中但扫描仍为 0，则下一堵点转向报告 ID 写入任务输入后是否触发扫描方法重入、以及扫描读取报告 ID 的输入/运行存在路径。
- 不用 D455 入队日志、等待项存在或承接日志单独替代扫描函数自身消费证据；不宣称 R24 根需求整体闭合、内部治理闭环完成、自我苏醒完成或初步成熟完成。

## 21. 等待项报告容量裁剪修复验证结果

验证命令：

- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=120000`

命令层结果：

- Debug x64 构建退出码 0，0 警告，0 错误。
- 120 秒 self-check 退出码 0。

本次运行关键计数（从 `日志/鱼巢_run_20260702.低值g` 第 40000 行后统计）：

```text
自我_扫描当前场景/函数自身结果=5
扫描函数 报告ID>0=1
扫描函数 外设数据项总数>0=0
扫描变化报告承接 命中报告=1=5
扫描变化报告承接 状态=无匹配报告=0
D455 扫描变化报告入队且匹配等待项ID非0=7
缺少D455逐簇识别报告=0
访问冲突/逻辑错误=0
```

关键证据：

```text
日志/鱼巢_run_20260702.低值g:41881
自我_扫描当前场景/函数自身结果 | 报告ID=523 | 来源帧ID=289 | 外设数据项总数=0 | 已取出=0 | 已解析=0 | 已闭合=0

日志/鱼巢_run_20260702.低值g:41909
任务管理外设消息承接/处理一次 | 状态=已生成观察事实承接请求 | 等待项ID=2 | 报告ID=1119 | 报告类型=扫描变化报告 | 命中报告=1 | 报告已消费=1

日志/鱼巢_run_20260702.低值g:45018
任务管理外设消息承接/处理一次 | 状态=已生成观察事实承接请求 | 等待项ID=5 | 报告ID=5132 | 报告类型=扫描变化报告 | 命中报告=1 | 报告已消费=1

日志/鱼巢_run_20260702.低值g:45219
外设线程_D455深度相机/停止 | 样本=2833 | 观察报告入队=2833 | 观察报告入队FPSx100=2999 | 综合稳定=稳定
```

当前结论：

- 本切片修复了等待项绑定扫描报告在队列中被容量裁剪后无法承接的问题；最新运行中扫描变化报告承接持续命中，旧 `等待项ID=2 | 报告ID=0 | 无匹配报告` 未在本次运行区间复现。
- R24 仍未闭合：`自我_扫描当前场景` 已经至少一次拿到 `报告ID>0` 和 `来源帧ID>0`，但 `外设数据项总数`、`已取出`、`已解析`、`已闭合` 仍为 0。
- 下一步应追 `自我动作实现.扫描.ixx` 中报告 ID 读取后，为什么没有从报告/材料页生成合法外设数据项；不得把“报告承接命中”误判为“扫描消费链闭合”。

## 22. 消费后等待项报告回查保护预测

触发：继续追踪第 21 节后，发现扫描变化报告已经被任务管理命中，但自我线程回查失败：

```text
日志/鱼巢_run_20260702.低值g:41909
任务管理外设消息承接/处理一次 | 等待项ID=2 | 报告ID=1119 | 报告类型=扫描变化报告 | 命中报告=1 | 报告已消费=1

日志/鱼巢_run_20260702.低值g:42001
自我线程/外设观察事实承接回查完成 | 报告ID=1119 | 报告可回查=否

日志/鱼巢_run_20260702.低值g:42006
自我线程/观察事实更新来源需求唤醒跳过 | 原因=报告不可回查 | 报告ID=1119 | 等待项ID=2 | 来源需求=6
```

当前根因判断：

- 第 20 节修复只保护 `!已消费 && 匹配等待项ID != 0`，但任务管理生成上行前已经调用 `标记外设观察报告已消费`。
- 报告消费后仍需要被自我线程按报告 ID 回查，供观察事实承接、来源需求唤醒和后续扫描重筹办使用。
- 因此容量裁剪保护必须覆盖“已被等待项消费但仍未过期”的短期可回查报告。

代码变更：

- `外设观察报告_容量裁剪保护` 改为在未过期前保护 `匹配等待项ID != 0 || 消费等待项ID != 0`。
- 文件头规则同步改为“已绑定显式等待项的短期可回查报告”，不限定未消费。
- TTL 过期清理仍优先，仍不允许扫描函数绕过队列直连 D455 内部材料。

验证预测：

- `任务管理外设消息承接/处理一次` 仍应出现扫描变化报告 `命中报告=1`。
- `自我线程/外设观察事实承接回查完成` 对同一报告应从 `报告可回查=否` 变为 `报告可回查=是`。
- 如果回查成功并触发来源需求唤醒，后续扫描函数应优先读取扫描变化报告 ID；成功标准仍是 `自我_扫描当前场景/函数自身结果 | 外设数据项总数>0`。
- 若回查成功但扫描仍为 0，则下一堵点转向报告 ID 写入扫描输入 / 运行存在的具体路径，不能把回查成功误判为 R24 闭合。

## 23. 等待项报告强保护与普通 L1 缓存裁剪预测

触发：第 22 节复跑中，扫描变化报告 `6` 已完成任务管理承接并被自我线程回查成功，但后续逐簇识别等待项报告仍在 30 秒内出现 `报告可回查=否`：

```text
日志/鱼巢_run_20260702.低值g:46876
自我线程/外设观察事实承接回查完成 | 报告ID=6 | 报告可回查=是

日志/鱼巢_run_20260702.低值g:48805
自我线程/外设观察事实承接回查完成 | 报告ID=1221 | 报告可回查=否
```

当前根因判断：

- `外设观察报告_容量裁剪保护` 同时保护等待项绑定报告和普通 L1 稳定子集报告。
- 高频 D455 入队会让 256 容量内全部变成“受保护项”，旧裁剪逻辑在找不到可裁剪项时直接 `pop_front()`，仍可能把更重要的已消费等待项报告挤掉。
- 等待项绑定 / 消费报告是自我线程回查、来源需求唤醒和后续任务重入的短期证据；普通 L1 稳定子集只是缓存层材料，容量紧张时应先被裁。

代码变更：

- `外设观察报告队列.ixx::外设观察报告_裁剪容量已加锁` 在所有报告都被第一层保护时，优先裁剪没有 `匹配等待项ID`、没有 `消费等待项ID` 的普通 L1 `逐簇识别报告`。
- 若仍找不到普通 L1 缓存项，才保留原兜底 `pop_front()`，避免无限增长。
- 未新增字段 / 结构体，未扩大 TTL，未允许扫描函数绕过外设观察报告队列直读 D455 内部材料。

验证预测：

- Debug x64 构建应退出码 0。
- 120 秒 self-check 中，等待项报告回查成功样本应增加，尤其是逐簇识别等待项报告不应继续稳定出现 `报告可回查=否`。
- 若回查成功后形成来源需求唤醒和重筹办，后续应看到扫描相关任务或派生任务继续推进；但只有 `自我_扫描当前场景/函数自身结果 | 外设数据项总数>0` 才能证明扫描函数开始消费外设数据项。
- 若回查成功但扫描仍未重入，则下一堵点转向“观察事实承接完成后是否把报告 ID 写回扫描任务输入 / 运行存在并触发扫描方法重入”。

## 24. 等待项报告强保护验证结果与下一堵点

验证命令：

- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=120000`

命令层结果：

- Debug x64 构建退出码 0，0 警告，0 错误。
- 120 秒 self-check 退出码 0。

本次运行关键计数（从 `日志/鱼巢_run_20260702.低值g` 第 50630 行后统计）：

```text
自我_扫描当前场景/函数自身结果=8
扫描函数 报告ID>0=0
扫描函数 外设数据项总数>0=0
扫描变化报告承接 命中报告=1=7
扫描变化报告承接 无匹配报告=0
自我线程/外设观察事实承接回查完成=5
报告可回查=是=5
报告可回查=否=0
报告不可回查唤醒跳过=0
缺少D455逐簇识别报告=0
访问冲突=0
逻辑错误=0
```

关键证据：

```text
日志/鱼巢_run_20260702.低值g:51638
任务管理外设消息承接/处理一次 | 状态=已生成观察事实承接请求 | 等待项ID=1 | 报告ID=6 | 报告类型=扫描变化报告 | 命中报告=1

日志/鱼巢_run_20260702.低值g:51709
自我线程/外设观察事实承接回查完成 | 报告ID=6 | 报告可回查=是

日志/鱼巢_run_20260702.低值g:52213
自我线程/观察事实更新来源需求唤醒事件发布 | 报告ID=6 | 报告可回查=是
```

当前结论：

- D455 供料、扫描变化报告入队、等待项匹配、消费后回查保护和来源需求唤醒已经取得同轮真实证据。
- R24 仍未闭合：最新 8 次 `自我_扫描当前场景/函数自身结果` 均为 `报告ID=0`、`来源帧ID=0`、`外设数据项总数=0`。
- 2026-07-02 用户确认新口径：外设将数据放入队列，由本能方法在执行期间自行从队列取数据；因此不能把“写报告 ID 到任务输入”作为扫描消费闭合目标。
- 2026-07-02 用户进一步纠正边界：任务管理不负责“队列报告到达后唤醒 / 重筹办”。队列报告到达只是外设队列事实；任务管理只推进任务生命周期，不拥有外设队列消费语义。
- 下一堵点改判为：扫描本能执行期没有从外设观察报告队列自取到合法扫描变化报告，或当前运行剧本中扫描本能执行发生在队列具备可消费数据之前。
- 下一步追代码锚点：`外设观察报告队列.ixx` 的队列有数证据；`自我动作实现.扫描.ixx::读取扫描报告` 的执行期队列自取路径；`自我动作实现.外设模块.ixx` 中观察 / 识别类本能执行期从队列取数路径。
- 不宣称 R24 根需求整体闭合、内部治理闭环完成、自我苏醒完成或初步成熟完成。

## 25. 用户确认队列自取口径后的修复预测

触发：第 24 节后，用户明确口径：

```text
外设将数据放入队列，由本能方法在执行期间自行拿取数据。
```

代码锚点：

```text
自我动作实现.扫描.ixx::读取扫描报告
外设观察报告队列.ixx::读取最新外设观察报告
自我线程模块.impl.cpp::私有_观察事实更新后触发来源需求重筹办_线程侧
```

当前根因判断：

- “把扫描变化报告 ID 写入任务输入参数场景 / 输入条件包”不是当前正确方向；报告 ID 可以用于队列回查、幂等和诊断，但不应成为扫描方法的业务输入。
- `自我动作实现.扫描.ixx::读取扫描报告` 已有执行期自取逻辑：无显式报告 ID 时调用 `读取最新外设观察报告({}, 扫描变化报告)`。
- 最新日志仍显示扫描函数 `报告ID=0`，说明真正问题在执行期队列自取没有命中，或扫描方法执行时队列中尚无符合其读取口径的报告。

处置：

- 已撤回刚才对 `任务模块.管理界面线程.impl.cpp::私有_写观察事实报告ID到任务输入` 的改动，保留“扫描变化报告只作扫描承接材料，不写任务输入”现状。
- 下一步不再验证 `外设观察报告ID参数筹办` 是否写入任务输入。
- 下一步应验证 `读取最新外设观察报告({}, 扫描变化报告)` 在扫描执行期为什么返回空：队列最新报告口径、已消费过滤、报告类型过滤、TTL / 容量裁剪，以及扫描方法执行时序是否早于队列可消费数据。

验证命令：

- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=120000`

预测：

- Debug x64 构建应退出码 0。
- 若扫描方法在报告到达后重入且队列自取命中，应出现 `自我_扫描当前场景/函数自身结果 | 报告ID>0 | 来源帧ID>0 | 外设数据项总数>0`。
- 若队列有扫描变化报告但扫描函数结果仍为 `报告ID=0`，下一堵点优先定位扫描执行期自取队列入口，而不是任务输入参数补齐或任务管理唤醒。
- 若扫描函数出现 `报告ID>0` 但 `外设数据项总数=0`，下一堵点转向扫描报告到 `结构_外设扫描提交包` / 合法外设数据项展开链。
- 不宣称 R24 根需求整体闭合、内部治理闭环完成、自我苏醒完成或初步成熟完成。

## 26. 按队列读后清理口径修复预测

触发：用户进一步确认当前验收口径：

```text
队列有数据不需要额外证明可用性；测试只需看到入队增加，并且删除 / 清理动作发生在扫描、观察等读取之后。
扫描、观察只读取最新最近的数据。
任务管理不负责队列报告到达后的唤醒 / 重筹办。
```

当前代码不一致点：

- `外设观察报告队列.ixx::读取最新外设观察报告` 和 `读取外设观察报告队列视图` 先用任意类型最新报告建立同帧锚点，再按目标报告类型过滤；当最新帧是逐簇识别 / 稳定子集而扫描变化报告在更近但不同类型窗口内时，扫描执行期可能读不到最新扫描变化报告。
- `外设观察报告_裁剪容量已加锁` 已保护等待项绑定 / 消费后短期回查报告，但没有保护未过期的同外设同类型最新报告；容量紧张时仍可能在本能方法读取前裁掉最新扫描变化报告。
- `自我动作实现.扫描.ixx::读取扫描报告` 在输入 / 输出 / 运行存在里读到报告 ID 时会直接按 ID 回查，可能回退到旧报告，不符合“扫描只读队列最新最近扫描变化报告”的口径。

代码变更：

- 队列读取改为：指定报告类型时，先以该类型的最新未过期报告建立近组锚点，再返回同帧同类型报告；未指定类型时仍读最新报告组。
- 容量裁剪改为：未过期的同外设同类型最新报告不作为容量裁剪对象；若队列全是等待项绑定报告或各类型最新报告，容量裁剪本轮停止，等待 TTL 过期清理。
- 扫描读取改为：执行期始终先读队列最新扫描变化报告；输入中的报告 ID 只有与最新扫描报告一致时才接受，不再用旧报告 ID 回退历史材料。
- 未新增字段 / 结构体；未让扫描或观察绕过外设观察报告队列直连 D455 内部材料；未把任务管理写报告 ID 作为闭合目标。

验证命令：

- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=120000`

预测：

- 构建应退出码 0。
- self-check 中观察 / 识别类本能仍应能读到 `逐簇识别报告`，扫描本能应至少不再因“任意最新帧锚点”遮蔽而读不到扫描变化报告。
- 成功闭合证据仍只认 `自我_扫描当前场景/函数自身结果 | 报告ID>0 | 来源帧ID>0 | 外设数据项总数>0`。
- 若扫描报告 ID 变为非 0 但 `外设数据项总数=0`，下一堵点转向扫描报告自身是否生成合法扫描提交包 / 合法外设数据项。
- 不宣称 R24 根需求整体闭合、内部治理闭环完成、自我苏醒完成或初步成熟完成。

## 27. 按类型最新队列读取修复验证结果

验证命令：

- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=120000`

命令层结果：

- Debug x64 构建退出码 0，0 警告，0 错误。
- self-check 外层 PowerShell 命令超时，未捕获 exe 退出码；复核时 `鱼巢.exe` 已退出，项目日志已写到程序退出守卫和 SQL 投影刷新完成。

本次运行统计（从 `日志/鱼巢_run_20260702.低值g` 第 56988 行后统计）：

```text
自我_扫描当前场景/函数自身结果=3
扫描函数 报告ID>0=2
扫描函数 外设数据项总数>0=2
观察本能闭环=2
观察本能 报告ID>0=1
逐簇识别报告入队=292
扫描变化报告入队=3
任务管理外设消息承接/处理一次=5
自我线程/外设观察事实承接回查完成=3
缺少D455逐簇识别报告=0
访问冲突=0
逻辑错误=0
```

关键证据：

```text
日志/鱼巢_run_20260702.低值g:58225
外设线程_D455深度相机/扫描变化报告入队 | 报告ID=6 | 类型=扫描变化报告 | 像素簇=24 | 空间候选队列句柄=1 | 来源外设帧ID=1 | 匹配等待项ID=1

日志/鱼巢_run_20260702.低值g:60297
自我_扫描当前场景/函数自身结果 | 报告ID=9 | 来源帧ID=1 | 外设数据项总数=411 | 已取出=411 | 已解析=411 | 已闭合=411

日志/鱼巢_run_20260702.低值g:60353
外设线程_D455深度相机/扫描变化报告入队 | 报告ID=2068 | 类型=扫描变化报告 | 像素簇=24 | 空间候选队列句柄=1 | 来源外设帧ID=1485 | 匹配等待项ID=3

日志/鱼巢_run_20260702.低值g:62616
自我_扫描当前场景/函数自身结果 | 报告ID=2068 | 来源帧ID=1485 | 外设数据项总数=411 | 已取出=411 | 已解析=411 | 已闭合=411

日志/鱼巢_run_20260702.低值g:60244
观察本能闭环 | 报告存在=1 | 报告ID=1838 | 材料可进入识别=1 | 观察新建存在数量=23 | 观察写入特征数量=861 | 中间层近组取数=1 | 只取中间层近组最新材料=1

日志/鱼巢_run_20260702.低值g:62536
外设线程_D455深度相机/停止 | 样本=2894 | 视频帧入队=2894 | 观察报告入队=2894 | 观察报告入队FPSx100=2996 | 综合稳定=稳定
```

当前结论：

- 用户要求的两条检查已满足：外设报告确实入队；扫描和观察本能方法在执行期从外设观察报告队列读取到了最新 / 最近材料。
- 原堵点“扫描函数自身仍未消费外设数据项”已解除：扫描本能已出现 `报告ID>0`、`来源帧ID>0`、`外设数据项总数=411`、`已取出/已解析/已闭合=411`。
- 本轮未把任务管理写报告 ID 当作闭合目标；任务管理仍只按任务生命周期和结构化承接消息工作，不拥有外设队列消费语义。
- R24 根需求仍未整体闭合：本轮扫描虽然消费了外设数据项，但 `覆盖计数=0`、`缺口数量=413`、`场景体素先验可用=0`、`视觉证据不足数量=24`，后续应转向扫描数据项到已知存在覆盖 / 场景体素先验 / 最终结算回写链。
- 不宣称 R24 根需求整体闭合、内部治理闭环完成、自我苏醒完成或初步成熟完成。

## 28. 扫描 SceneVoxelPrior 同轮报告坐标系修复预测

触发：第 27 节证明扫描 / 观察本能已经在执行期从外设观察报告队列读取最新 / 最近材料，但扫描函数自身结果仍显示：

```text
覆盖计数=0
缺口数量=413
场景体素先验可用=0
视觉先验匹配数量=0
视觉证据不足数量=24
```

当前代码不一致点：

- `自我动作实现.扫描.ixx::自我扫描当前场景` 先生成 SceneVoxelPrior，后读取扫描报告；同轮报告中的 D455 坐标系材料无法进入先验坐标系版本。
- `生成扫描SceneVoxelPrior` 未填写 `结构_场景体素同步参数::三维体素特征类型`，场景体素模块会把每个存在记录为 `缺体素特征类型`，入选存在数量无法成为可靠空间先验。
- 现有控制面板和体素 smoke 已使用 `三维体素模型` 特征入口和坐标系版本 `1`；扫描链缺同等入口，不应继续让动态运行信息依赖空字段。
- 覆盖计数仍可能为 0 的独立原因是扫描提交包中 `已知存在结果` 当前均缺 `已确认存在ID`，本切片只关闭 SceneVoxelPrior 自身缺口，不把体素先验可用误判为 R24 根闭合。

代码变更预测：

- 扫描执行期先调用 `读取扫描报告`，再生成 SceneVoxelPrior。
- SceneVoxelPrior 同步参数固定传入 `三维体素模型`、局部坐标和体素颜色相关既有特征入口，不新增持久结构体。
- 若同轮 D455 报告坐标系 ID 为当前已知 `v1` 协议 ID，则生成场景体素坐标系版本 `1`；否则仍保留结构化缺口，不硬填未知坐标系。
- 输出 / 运行存在继续写入场景体素先验版本、坐标系版本、可用状态和视觉缺口，不把外设报告、队列项或 SceneVoxelPrior 当世界树事实源。

验证命令：

- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=120000`

预期证据：

- Debug x64 构建退出码 0。
- 真实运行日志中 `自我_扫描当前场景/函数自身结果` 应继续出现 `报告ID>0`、`外设数据项总数>0`。
- 若当前场景已有三维体素模型和坐标事实，`场景体素先验可用` 应从 0 变为 1，`场景体素先验坐标系版本` 应为 1，`视觉先验匹配数量` 应有机会大于 0。
- 若 `场景体素先验可用` 仍为 0，则根据新增固定入口排除“缺三维体素特征类型 / 缺坐标系版本”后，下一堵点转向当前场景已确认存在是否缺三维体素句柄或场景绝对坐标。
- 若先验可用但 `覆盖计数` 仍为 0，则下一堵点转向扫描提交包缺 `已确认存在ID` 的正式对应闭合，不把本切片误判为 R24 根需求完成。

## 29. 扫描 SceneVoxelPrior 诊断字段补充预测

触发：第 28 节代码已让扫描执行期先读取同轮扫描报告，并把报告坐标系与固定体素特征入口用于 SceneVoxelPrior，但最新日志摘要仍只显示 `场景体素先验版本` 和 `场景体素先验可用`，无法区分：

- 坐标系版本仍缺失；
- 三维体素特征类型仍未进入同步参数；
- 当前场景已确认存在缺三维体素句柄；
- 当前场景已确认存在缺场景绝对坐标；
- 先验已可用但覆盖计数仍被 `已确认存在ID` 缺口阻断。

本轮最小变更：

- 仅在 `自我_扫描当前场景/函数自身结果` 日志中补充输出已经写入运行存在 / 输出结果场景的 SceneVoxelPrior 统计字段：坐标系版本、入选存在数量、先验缺口数量、缺体素特征类型数量、缺坐标系版本数量、缺体素存在数量、缺坐标存在数量。
- 不改变扫描业务判断，不新增持久结构体，不把日志字段作为机器逻辑或验收事实。

验证命令：

- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=120000`

预期证据：

- Debug x64 构建退出码 0。
- self-check 退出码 0；无 `逻辑错误`、`扫描上游契约错误`、`项目弹窗错误`、`访问冲突`。
- 扫描函数结果继续显示 `报告ID>0`、`外设数据项总数>0`。
- 若 `场景体素先验坐标系版本=1` 且 `场景体素缺体素特征类型数量=0`，则本轮排除同轮坐标系 / 体素特征入口缺口。
- 若 `场景体素先验可用=0` 且 `场景体素先验入选存在数量=0`，下一堵点转向当前场景已确认存在缺三维体素句柄或场景绝对坐标。
- 若 `场景体素先验可用=1` 但 `覆盖计数=0`，下一堵点转向扫描提交包缺 `已确认存在ID` 的正式对应闭合。
- 不宣称 R24 根需求整体闭合、内部治理闭环完成、自我苏醒完成或初步成熟完成。

## 30. 扫描现实数据队列读取与内部事实场景解析预测

触发：用户确认扫描方法获取现实数据的方式必须是在函数体内部读取外设写入的数据队列；任务输入不应承载外设现实数据。

本轮边界：

- 外设现实数据仍只由 `自我动作实现.扫描.ixx::读取扫描报告` 在函数体内调用 `读取最新外设观察报告({}, 扫描变化报告)` 获取。
- `输入参数场景` 只作为任务参数容器使用；扫描新增只读解析逻辑，从输入条件包、任务参数包、现实场景包或调用信息中读取 `当前场景引用`、`当前观察场景`、`目标现实场景` 或 `目标宿主` 场景指针。
- 解析出的 `扫描事实场景` 用于 SceneVoxelPrior、扫描关注集合和动作动态来源输入场景；没有解析到事实场景时记录结构化缺口，不从外设队列项或日志反推世界树事实。
- 不改任务管理，不让任务管理负责队列到达后的唤醒 / 重筹办，不把报告 ID 写入任务输入当作闭合目标。

验证命令：

- `python .\tools\生成函数功能说明文档.py`
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=120000`

预期证据：

- 函数说明文档更新成功，Debug x64 构建退出码 0。
- self-check 中扫描函数结果仍应显示 `报告ID>0`、`外设数据项总数>0`，证明现实数据仍来自队列读取。
- 日志新增 `输入参数场景` 与 `扫描事实场景` 诊断字段；若二者不同，说明任务参数容器和内部事实场景已分离。
- 若 `扫描事实场景` 指向真实当前场景且已有体素 / 坐标事实，`场景体素先验入选存在数量` 应有机会大于 0；若仍为 0，下一堵点转向当前场景尚无可入选体素存在或事实场景指针来源仍未绑定。
- 不宣称 R24 根需求整体闭合、内部治理闭环完成、自我苏醒完成或初步成熟完成。

## 31. 扫描报告ID输入残留收口预测

触发：继续复核第 30 节后发现两个残留路径：

- `自我动作实现.扫描.ixx::读取扫描报告` 虽已优先读最新扫描变化报告，但仍读取输入参数场景 / 输出结果场景 / 运行存在中的 `外设观察报告ID` 做一致性判断。
- `任务模块.筹办.ixx::通用绑定候选方法输入参数_筹办` 无条件调用 `补齐外设观察报告ID输入参数_筹办`，可能把任务虚拟存在上的报告 ID 复制进输入条件包。
- `任务模块.管理界面线程.impl.cpp::私有_写观察事实报告ID到任务输入` 对扫描任务收到逐簇报告的分支仍可能写任务虚拟存在材料引用。

本轮最小变更：

- `读取扫描报告` 改为零输入函数，只在扫描函数体内调用 `读取最新外设观察报告({}, 扫描变化报告)`。
- `补齐外设观察报告ID输入参数_筹办` 增加目标方法形参检查；只有方法显式声明 `外设观察报告ID` 形参时才补齐，扫描方法未声明该形参，因此不会被补。
- 扫描任务收到逐簇报告时，任务管理不写任务虚拟存在报告 ID，日志原因改为“扫描任务现实数据由扫描函数体自取队列”。
- 保留扫描运行存在、扫描覆盖记录和治理记录中的 `外设观察报告ID` 结果证据写入；该字段只表示执行后来源材料，不作为取数输入。

验证命令：

- `python .\tools\生成函数功能说明文档.py`
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=120000`

预期证据：

- 静态搜索中 `自我动作实现.扫描.ixx::读取扫描报告` 不再读取 `输入参数场景`、`输出结果场景` 或 `运行存在` 的 `外设观察报告ID`。
- self-check 中扫描函数结果仍应显示 `报告ID>0`、`外设数据项总数>0`，证明去掉输入报告 ID 后仍能从队列取到现实数据。
- 日志中不应出现本轮扫描任务路径的 `外设观察报告ID参数筹办` 写入；如果出现，应证明目标方法不是扫描方法且显式声明报告 ID 形参。
- 日志中不应出现本轮扫描任务路径的 `报告ID写入任务输入=1`。
- 不宣称 R24 根需求整体闭合、内部治理闭环完成、自我苏醒完成或初步成熟完成。

## 32. 观察体素已写入但扫描未取到体素的根因切片

触发：用户要求先解决“观察没有获取到体素”的问题。

当前证据：

- 最新日志已证明观察提交入口多次写入粗体素，父存在样本包括 `6OM`、`6PG`、`6Q9`、`6Rl`，日志字段为 `观察体素特征写入诊断 | 阶段=粗体素写入 | 原因=三维体素粗模型已写入 | 成功=1`。
- 同轮观察本能闭环显示 `当前观察场景=Ez | 报告ID=1984 | 观察写入特征数量=152`。
- 后续扫描结果显示 `输入参数场景=3uF/6kq | 扫描事实场景=3uF/6kq | 场景体素先验入选存在数量=0`，说明扫描在枚举任务输入参数场景，而不是枚举已写入观察体素的当前观察场景 `Ez`。

本轮最小变更：

- `自我动作实现.扫描.ixx::确认自我扫描当前场景规格` 声明必需形参 `当前观察场景`，值类型为场景指针，来源为输入条件包。
- 同步声明 `当前观察场景` 条件特征，复用 `任务模块.筹办.ixx` 已有的目标宿主场景绑定逻辑。
- 不改观察体素写入入口，不改队列供料逻辑，不把外设现实数据重新写回任务输入。
- 构建前发现 `.codex_locks/build-run.lock` 记录的旧 PID `43160` 已不存在，已按规则移除陈旧锁后再执行构建。

验证命令：

- `python .\tools\生成函数功能说明文档.py`
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=120000`

预期证据：

- 函数说明文档更新成功，Debug x64 构建退出码 0。
- self-check 日志中观察粗体素写入成功样本仍存在。
- 扫描函数结果中的 `扫描事实场景` 应从任务输入参数场景转为正式当前观察场景，优先预期为 `Ez`。
- 若当前观察场景下已有体素和坐标事实，`场景体素先验入选存在数量` 应大于 0；若仍为 0，则下一堵点才转向体素句柄 / 坐标事实读取，而不是观察未写体素。
- 不宣称 R24 根需求整体闭合、内部治理闭环完成、自我苏醒完成或初步成熟完成。

## 33. 当前观察场景实参筹办修复记录

触发：第 32 节只给扫描方法声明了 `当前观察场景` 必需形参，但后续运行暴露父扫描任务目标宿主是自我存在 `F0`，不是正式当前观察场景 `Ez`，导致通用绑定仍缺 `当前观察场景` 实参。

本轮最小变更：

- `任务模块.筹办.ixx` 新增只读解析当前观察场景实参的筹办辅助：优先使用需求目标宿主 / 被需求存在中已经是场景的节点，其次使用自我现实场景、世界树自我所在场景、需求场景、任务场景。
- `方法候选输入闭合预估分` 和 `通用绑定候选方法输入参数_筹办` 共用该解析结果，给观察 / 扫描类方法的 `当前观察场景` 必需形参写入输入条件包。
- 不从外设报告、日志、报告 ID 或临时文本反推世界树事实；扫描 / 观察执行函数仍只读输入条件包中的正式场景指针。

已运行验证：

- `python .\tools\生成函数功能说明文档.py`：退出码 0，函数条目 7603。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`：退出码 0，0 警告，0 错误。
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=120000`：本轮按用户“暂停”中止；超时后仍在运行的 `鱼巢.exe` 进程已停止。未形成扫描事实场景或体素先验入选数量的业务通过证据。

当前判断：

- 代码层已修复 `当前观察场景` 实参只能由目标宿主为场景时绑定的问题。
- 业务验证仍待恢复：下一次运行需要确认 `候选方法通用参数绑定` 中扫描方法必需实参已绑定，且 `自我_扫描当前场景/函数自身结果` 的 `扫描事实场景` 指向正式当前观察场景，优先预期为 `Ez`。
- 不宣称观察体素问题已闭合、R24 根需求整体闭合、内部治理闭环完成、自我苏醒完成或初步成熟完成。
