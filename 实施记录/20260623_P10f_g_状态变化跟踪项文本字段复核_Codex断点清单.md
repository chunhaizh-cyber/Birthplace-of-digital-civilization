# P10f-g 状态变化跟踪项文本字段复核 Codex 断点清单

## 0. 恢复摘要

当前切片：P10f-g `鱼巢.cpp::结构_状态变化跟踪项` 文本字段边界复核。

依据文件：

- `计划/计划索引.md`
- `规范/禁止性规范20260611.md`
- `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_causal_structured.tsv`

执行口径：

- `状态主键 / 目标宿主主键 / 特征类型主键` 属于主键类，不作为本切片清理目标。
- `特征类型文本 / 特征值文本` 若只用于日志 / 调试输出，不硬删。
- 若 `特征类型文本 / 特征值文本` 进入状态变化机器跟踪、稳定键、排序或判断，则必须改为特征类型主键、特征值结构化值或枚举 / 数值方向。

当前候选：

- `鱼巢.cpp:1640` `结构_状态变化跟踪项::特征类型文本`
- `鱼巢.cpp:1642` `结构_状态变化跟踪项::特征值文本`

预测：

- 该结构大概率用于启动 / 演示路径中的状态变化跟踪日志。
- 若字段只拼入日志输出，记录无堵点；若参与状态变化去重或判定，应删除文本字段并改用已有主键 / 结构化值。

复核结果：

- `结构_状态变化跟踪项::特征类型文本 / 特征值文本` 只由 `私有_读取状态变化跟踪项` 写入。
- 两个文本字段只被 `私有_状态变化跟踪行` 读取，用于拼接 `完成任务变化跟踪` 输出文本。
- 状态变化跟踪项同时保留 `状态主键 / 目标宿主主键 / 特征类型主键`；文本字段未进入稳定键、排序、候选选择、状态推进或机器判断。

当前判断：

- 本切片无需要删除的代码字段；按用户口径，纯日志 / 输出报告文本不强行删除。

验证记录：

- `rg -n "结构_状态变化跟踪项|特征类型文本|特征值文本|状态变化跟踪" 鱼巢.cpp`：只命中结构体定义、读取函数和输出行。
- `rg -n "状态变化跟踪行\(|读取状态变化跟踪项\(|特征类型文本|特征值文本" -g '*.cpp' -g '*.ixx' -g '*.h'`：跨仓命中仍只显示 `鱼巢.cpp` 本结构的输出使用；其它命中为独立显示 / SQL / 命名函数，不属于本切片。
- 本切片未修改代码，未重新构建。

下一步：

- 按计划索引进入 P10f-h：`二次特征应用模块.ixx::结构_场景二次特征写入请求` 的 `概念名称 / 概念词性` 边界复核。

禁止宣称：

- 不宣称需求类逻辑纠偏完成。
- 不宣称任务类逻辑纠偏完成。
- 不宣称因果类功能补完完成。
- 不宣称项目结构闭环全部通过。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
