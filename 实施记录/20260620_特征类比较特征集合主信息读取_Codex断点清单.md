# 20260620 特征类比较特征集合主信息读取 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_特征类逻辑纠偏计划_v0.1.md` 的 1.2，处理 `比较特征集合` 中直接读取 `节点->主信息` 并 `dynamic_cast<const 特征节点主信息类*>`。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_特征类逻辑纠偏计划_v0.1.md
实施记录/20260620_特征类直接子节点明确函数_Codex断点清单.md
说明书/函数功能说明文档.md
```

本切片范围：

```text
1. 只处理 特征类::比较特征集合 中构建索引阶段的 direct 主信息读取。
2. 改用既有 特征类::取特征主信息。
3. 不处理三维体素链读取 特征值类 节点 主信息.值。
```

已复核事实：

```text
1. 比较特征集合 当前在构建索引 lambda 内检查 节点->主信息，并 dynamic_cast<const 特征节点主信息类*>(节点->主信息)。
2. 同一函数后续比较阶段已经使用 取特征主信息。
3. 说明书/函数功能说明文档.md 已有 特征类::取特征主信息 与 特征类::比较特征集合。
```

验证前预测：

```text
1. 比较特征集合 局部不再命中 节点->主信息 / dynamic_cast<const 特征节点主信息类*>。
2. 宽口径审查仍会命中三维体素链读取 特征值类 节点 主信息.值。
3. 函数说明文档生成器和 Debug x64 构建应通过。
```

## 1. 实施记录

```text
1. 特征类::比较特征集合 的 构建索引 lambda 改为捕获 this。
2. 构建索引不再检查 节点->主信息，也不再 dynamic_cast<const 特征节点主信息类*>(节点->主信息)。
3. 构建索引改用既有 特征类::取特征主信息(节点)。
4. 已运行 python .\tools\生成函数功能说明文档.py，函数条目保持 6880。
```

## 2. 验证结果

```text
1. 比较特征集合 局部复核：
   特征类.cpp:4127 构建索引 = [this](...)
   特征类.cpp:4129 const auto* 主信息 = 取特征主信息(节点);

2. rg -n "节点->主信息|dynamic_cast<const 特征节点主信息类" 特征类.cpp
   结果：比较特征集合 无命中；剩余命中均为三维体素链读取 特征值类 节点 主信息.值。

3. rg -n "取主信息<[^>]+>|dynamic_cast<[^>]+>|->主信息|\\.主信息\\.值|状态节点主信息类|场景节点主信息类|动态节点主信息类|二次特征主信息类|因果主信息类|需求主信息类|方法主信息类" 特征类.cpp 特征类.h
   结果：比较特征集合 direct cast 已消失；仍保留已登记残留：
        特征类.cpp:376、3491、3511、3532、3796 三维体素链读取 特征值类 节点 主信息.值。
        特征节点主信息类 / 抽象特征主信息类 取主信息 命中属于特征类职责内明确读取。

4. python .\tools\生成函数功能说明文档.py
   结果：退出码 0；源码文件 128；函数条目 6880；已生成 说明书/函数功能说明文档.md。

5. msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\20260620_特征类比较特征集合主信息读取_msbuild.log
   结果：退出码 0；已成功生成；0 个警告；0 个错误；生成 D:\鱼巢\x64\Debug\鱼巢.exe。
```

## 3. 当前判断

```text
1. 本切片已闭合：比较特征集合 构建索引阶段不再直接读取 节点->主信息。
2. 不宣称特征类逻辑纠偏完成。
3. 下一步仍回到 特征类 1.2，评估三维体素链读取 特征值类 节点 主信息.值 是否需要转入 特征值类 只读函数缺口。
```
