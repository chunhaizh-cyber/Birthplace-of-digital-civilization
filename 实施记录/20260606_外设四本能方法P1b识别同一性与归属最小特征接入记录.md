# 20260606_外设四本能方法P1b识别同一性与归属最小特征接入记录

## 1. 切片范围

```text
计划：计划/20260605_外设四本能方法需求树生成因果链修订计划_v0.1.md
切片：P1b 识别同一性与归属最小特征接入
目标：让 自我_识别外设观察材料 输出正式同一性 / 归属特征，而不是只输出候选确认方案和旧工程字段。
边界：不改扫描、跟踪执行逻辑；不让识别输出扫描 / 跟踪变化动态；不新增模拟测试或 --*-check 入口。
```

## 2. 实施内容

```text
自我动作实现.外设模块.ixx：
    新增 外设观察存在自我存在同一性状态：
        未比较 / 已同一 / 未命中已知存在 / 冲突 / 证据不足。
    新增 外设观察存在自我归属状态：
        未归属 / 待建立 / 已归属 / 冲突 / 证据不足。
    新增正式特征入口：
        外设观察存在_自我存在同一性状态
        外设观察存在_自我存在同一性评分
        外设观察存在自我归属状态
        外设观察存在归属目标
        外设观察存在归属建立动态
        外设观察存在归属冲突动态
    自我_识别外设观察材料：
        聚合写入同一性状态、同一性评分、自我归属状态；
        逐项方案节点写入同一性状态、评分、归属状态和可选归属目标；
        仅在已归属 / 冲突时把识别动作动态写成归属建立 / 冲突动态来源；
        日志补充同一性状态、评分和自我归属状态。

特征类.cpp：
    初始化 外设观察存在_自我存在同一性状态 点状态；
    初始化 外设观察存在_自我存在同一性评分 0..10000 区间；
    初始化 外设观察存在自我归属状态 点状态；
    归属目标和归属动态保持为指针特征，不作为布尔状态初始化。
```

## 3. 验证

```text
git diff --check -- 自我动作实现.外设模块.ixx 特征类.cpp
    退出码 0；仅 LF/CRLF 提示。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码 0；
    0 个错误；
    1 个 LNK4075 链接警告：因 /INCREMENTAL:NO 忽略 /EDITANDCONTINUE。
```

## 4. 当前判断

```text
P1b 代码切片完成。
识别已有正式同一性 / 归属最小特征和结果能力。
本次未真实运行鱼巢.exe，因此不宣称四方法自然业务链已通过。
本次不宣称需求树自然生成四方法目标已通过。
```

## 5. 下一步

```text
进入 P1c：扫描基准差异与值域冲突最小特征接入。
P1c 不改变跟踪逻辑。
首次扫描只写发现 / 当前特征，不把首次扫描写成变化动态。
```
