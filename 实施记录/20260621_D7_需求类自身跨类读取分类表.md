# 20260621 D7 需求类自身跨类读取分类表

## 0. 口径

```text
所属计划：计划/20260620_需求类逻辑纠偏计划_v0.1.md P7。
扫描范围：需求类.h、需求类.cpp。
扫描日志：
    日志/20260621_D7_需求类跨类主信息读取扫描.txt
    日志/20260621_D7_需求类P7锚点扫描.txt
边界：
    需求类可以读取和维护 需求主信息类。
    需求类不应直接解释 状态节点主信息类、特征节点主信息类、二次特征主信息类 或 方法主信息类 的字段。
    SQL、控制面板、日志文本只归显示 / 诊断，不得反向作为机器判断。
```

## 1. 首轮分类

| 分类 | 位置 | 当前用途 | 判定 | 下一步 |
| --- | --- | --- | --- | --- |
| 显示 / SQL 后置 | `需求类.cpp:128-215`, `需求类.cpp:2392-2449` | 构造需求树 SQL 投影；`语素集.安全获取词(...)` 只生成显示节点名 | 不作为机器查找 / 生成逻辑；归 P6 后置 | 本轮不改，后续只允许显示层读取 |
| 需求类内部树操作 | `需求类.h:942-1019`, `需求类.h:1060-1195`, `需求类.h:1284-1310`, `需求类.cpp:1510-1722`, `需求类.cpp:2466-2477` | 需求树去重、合并、更新、只读 helper、任务绑定 | 可保留需求主信息读取；但其中状态 / 特征拆解需回迁 | 拆出状态 / 特征读取缺口 |
| 状态 / 特征跨类读取 | `需求类.h:719-740`, `需求类.h:845-875`, `需求类.h:995-1019`, `需求类.cpp:2482-2523`, `需求类.cpp:2530-2561` | 从目标状态 / 当前状态提取主体、特征类型、状态值、时间并生成目标键、任务上下文、需求方向 | 机器逻辑，不能继续由需求类直接拆状态 / 特征主信息 | 第一优先：补状态类引用级读取 helper 与特征类静态特征类型 helper，再回迁需求类调用 |
| 二次特征跨类读取 | `需求类.cpp:1733-1770`, `需求类.cpp:1774-1780` | 从二次特征主信息生成需求方向签名 | 机器逻辑，应由二次特征类提供结构化读取 | 状态 / 特征首切片后转入二次特征类计划 |
| 方法类跨类读取 | `需求类.cpp:923-943`, `需求类.cpp:947-1087` | 判断方法首节点是否生产目标特征、遍历方法树查找生产者 | `方法类::方法首节点生产目标特征` 已存在；方法树生产者查询仍在需求类内 | 后续优先复用方法类公开函数；若缺树级查询，修订方法类计划 |
| 枚举目标校验混合逻辑 | `需求类.cpp:2283-2371` | 校验目标特征、抽象特征值域、生产者分级并写错误日志 | 机器字段为枚举 / 布尔；`说明` 只允许日志 / 诊断 | 后续按特征类 / 方法类缺口拆分，不让 `说明` 参与机器判断 |

## 2. 第一执行切片

```text
切片：P7a 状态 / 特征目标材料读取回迁。
原因：
    生成需求目标键、从状态补齐目标主体和特征缓存、生成任务初始化上下文都依赖状态主体、状态特征、状态值。
    状态类当前只有 pointer 级读取，缺少可保留主键回退的可解析引用级读取。
    特征类当前有实例函数 `读取特征语义类型`，但需求类头文件内无法安全绑定基础信息实例，缺少不创建不修改的静态读取入口。
计划动作：
    1. 状态类新增只读函数，读取状态主体、状态特征和状态值的结构化引用。
    2. 特征类新增只读静态函数，从特征节点读取语义类型。
    3. 需求类把 `状态主信息_已加锁` / `状态特征类型主键_已加锁` 相关机器读取回迁到上述函数。
验证：
    python .\tools\生成函数功能说明文档.py
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

## 3. P7a 执行结果

```text
已新增：
    状态类::读取状态主体特征和值引用
    特征类::读取特征语义类型_直接

已回迁：
    需求类.h 的 生成需求目标键_已加锁 / 从状态补齐目标主体和特征缓存_已加锁。
    需求类.cpp 的 生成任务初始化上下文。

已验证：
    python .\tools\生成函数功能说明文档.py 退出码 0，函数条目 7161。
    Debug x64 构建退出码 0，日志 日志/20260621_P7a_状态特征目标材料读取_build.log 显示“已成功生成”。
    `状态主信息_已加锁`、状态主信息 direct cast、特征主信息 direct cast、`主体->主信息` 在 需求类.h / 需求类.cpp 中无命中。

后续：
    返回需求类 P7，下一小切片处理二次特征方向签名跨类读取。
```
