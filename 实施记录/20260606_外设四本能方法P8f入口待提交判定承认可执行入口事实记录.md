# 20260606_外设四本能方法P8f入口待提交判定承认可执行入口事实记录

## 切片

```text
计划：计划/20260605_外设四本能方法需求树生成因果链修订计划_v0.1.md
切片：P8f 入口待提交判定承认可执行入口事实
目标：让任务筹办在已有真实可执行入口事实时不再重复派生 方法可执行入口数量 缺口。
```

## 依据

```text
P8e 真实日志：
    Rx / 当前观察材料集取得状态 已通过生产者校验；
    后续在多个目标方法上仍生成 1Al / 方法可执行入口数量 缺口；
    日志同时显示 方法动作数=1、可执行入口数=1、条件结果对数=1。

任务模块.筹办.ixx：
    候选方法不可就绪状况 已承认 有已注册本能入口 或 方法可执行入口数量>0 即有可执行入口；
    已有单步本能入口待提交动作状态 只检查 方法动作状态已提交，未承认可执行入口事实。

规范边界：
    任务筹办不得现场直调提交方法；
    真正缺入口时仍应派生入口补齐需求；
    已有入口事实时应进入后续条件 / 参数 / 执行路径；
    值域由特征类内部自动匹配或按规则分解，自我层不主动裁决值域。
```

## 修改前预测

```text
代码修改：
    已有单步本能入口待提交动作状态 增加已有可执行入口事实判定；
    若 私有_方法有已注册本能入口 或 方法可执行入口数量>0，则返回 0；
    真正没有入口事实的目标方法仍按原路径上报 方法缺可执行入口。

不修改：
    任务筹办现场直调；
    提交方法可执行入口的执行体；
    值域匹配 / 分解逻辑；
    需求目标枚举校验。

验证预测：
    Debug x64 构建应通过；
    30 秒 --self-check-log 不应再在 方法动作数=1 / 可执行入口数=1 的目标方法上反复生成同一 1Al 缺口；
    若出现参数缺口、条件结果对缺口、正式方法执行、提交入口缺口或真实运行崩溃，按新的证据继续切片；
    除非日志出现四方法 / 四提交入口 / 来源动作动态 串接，否则不宣称自然链通过。
```

## 当前状态

```text
已修改 任务模块.筹办.ixx：
    已有单步本能入口待提交动作状态 增加已有可执行入口事实判定；
    私有_方法有已注册本能入口 或 方法可执行入口数量>0 时返回 0；
    真正缺入口事实的目标方法仍保留原缺口上报路径。

已同步修复 鱼巢.cpp：
    程序退出守卫在关闭项目日志前停止 D455 外设线程；
    避免 self-check 退出阶段 D455 线程仍采样导致访问冲突。

已运行：
    git diff --check：
        退出码 0，仅 LF/CRLF 提示。
    Debug x64 msbuild：
        退出码 0，0 错误，1 个 LNK4075 链接警告。
    30 秒 --self-check-log：
        退出码 0；
        程序退出守卫已记录 D455 外设线程停止，未留下残留鱼巢.exe。

真实日志复验：
    最新运行区间未再出现 缺口特征=1Al / 已有真实单步本能入口待提交 / 学习本能真实单步入口 重复缺口；
    P8f 闭合了已有入口事实被重复报缺的问题。

新暴露阻塞：
    后续 方法动作数量 缺口被路由到 自我_定向学习；
    日志中任务 E/F 选中 当前本能ID=6061，并出现 自我_定向学习/动作目标不可闭合；
    这与学习规范中“内部方法补齐闭环不触发定向学习”冲突。

下一步：
    进入 P8g；
    将内部 方法动作数量 / 方法动作状态 缺口结果能力从 定向学习 撤回到 尝试学习；
    不伪造方法动作，不把内部因果样本缺失解释成定向学习。
```
