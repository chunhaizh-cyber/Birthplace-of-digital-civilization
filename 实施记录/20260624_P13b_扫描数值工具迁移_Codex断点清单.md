# 20260624 P13b 扫描数值工具迁移 Codex 断点清单

## 0. 恢复摘要

当前切片：P13b 全局共享迁移第八小切片。

已完成：

```text
1. 将 `自我动作实现.扫描.ixx` 中两个不依赖项目领域结构的数值 helper 迁入 `全局共享函数类.ixx`：
   - `绝对差F64I64(double, I64)`
   - `计算TTL毫秒(I64, I64)`
2. 删除 `自我动作实现.扫描.ixx` 本地定义。
3. 原调用点保持函数名不变，解析全局共享函数导出。
4. 已刷新 `说明书/函数功能说明文档.md`。
5. 已按本次触碰范围处理 `说明书/函数功能说明文档.md` 的 LF 换行提示。
```

依据：

```text
计划/计划索引.md：P13b 从 B 类低风险候选中逐个迁移不带项目自定义主信息 / 领域类型的通用函数。
计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md：P13 分类迁移规则。
```

验证：

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo
```

结果：

```text
函数说明文档生成成功：源码文件 127，函数条目 7190。
Debug x64 构建通过：0 警告，0 错误。
```

下一步：

```text
继续 P13b，优先复核剩余纯数学 / 转换 helper。
参数生成策略、日志、数据库、控制面板、线程生命周期和领域评分策略先保留或另开专项复核。
```

禁止宣称：

```text
不宣称全部函数迁移完成。
不宣称全部重复函数已清理完成。
不宣称内部循环完成 / 自我苏醒完成 / 初步成熟完成。
```
