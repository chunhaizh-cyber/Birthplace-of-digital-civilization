# 20260611 D455外设队列生产消费边界 Codex断点清单

## 当前切片

D455 逐簇识别报告生产口径纠偏：外设线程按采集成功和本地质量/采样规则生产报告；等待项只作为任务管理侧后续匹配、消费和承接依据，不控制外设侧是否把已有数据放入观察报告队列。

## 依据文件

- `规范/禁止性规范20260611.md`
- `规范/规范目录.md`
- `计划/计划索引.md`
- `规范/双目相机外设独占观察线程规范20260527.md`
- `规范/外设独立控制线程与消息承接边界规范20260527.md`
- `规范/相机外设综合工作流程规范20260607.md`

## 已确认代码事实

- `外设观察报告队列.ixx` 中 `外设观察报告_清理已加锁` 按 `外设提交包默认有效毫秒=60000` 清理过期报告，并在队列超过 `队列容量=64` 时从队首清除。
- `提交外设观察报告` 入队前后都会调用 `外设观察报告_清理已加锁`，读取路径也会触发清理。
- 旧 D455 主循环把逐簇识别报告入队绑定到 `有D455等待项 || D455_应保留无等待项逐簇识别报告(序号)`，导致无等待项时只保留少量节流帧。

## 已修改文件

- `外设线程_D455深度相机.ixx`

## 已完成修改

- 删除无等待项逐簇识别报告保留特判。
- D455 每个成功采集帧都调用 `提交外设观察报告` 和 `提交D455短期观察材料页`。
- 逐簇识别入队日志新增 `生产口径=外设侧有数据即入队`，保留 `活跃等待项` 和 `D455等待项数` 作为消费侧背景。
- 删除旧的 `逐簇识别报告入队背压抑制帧数` 日志口径，避免把无等待项误记为外设生产背压。

## 已运行验证

- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
  - 退出码：0
  - 结果：成功生成；1 个既有链接警告 `LNK4075`，0 个错误。
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=15000`
  - 退出码：0
  - 结果：自检入口正常退出，D455 外设线程启动、采集、入队、停止。

## 关键日志证据

- `日志/鱼巢_run_20260611.低值g:84282`：D455 外设线程启动。
- `日志/鱼巢_run_20260611.低值g:84905`：逐簇识别报告入队，日志含 `生产口径=外设侧有数据即入队`，报告ID=1，活跃等待项=1，D455等待项数=5。
- `日志/鱼巢_run_20260611.低值g:86333`：报告ID=10 继续按同一生产口径入队。
- `日志/鱼巢_run_20260611.低值g` 尾部：任务管理外设消息承接命中报告ID=10，同时无匹配等待项只保留队列缓存、不上报自我线程。
- `日志/鱼巢_run_20260611.低值g` 尾部：自我线程完成外设观察事实承接，报告可回查=是，禁止直接写世界真值=是。
- `日志/鱼巢_run_20260611.低值g:86405`：D455 外设线程停止，样本=11，综合稳定=稳定。

## 验证边界

- 本次真实运行验证了“有等待项路径下仍正常生产、入队、匹配、承接”。
- 当前代码已删除无等待项生产拦截分支，因此无等待项时也不会被等待项条件阻止入队；但本次未新增独立无等待项线程启动入口，不宣称无等待项长期实机闭环已单独验证。

## 禁止宣称

- 不宣称自我苏醒完成。
- 不宣称内部循环全部完成。
- 不把外设报告生产写成自我方法动作动态。
- 不把等待项匹配成功等同于需求结算完成。
