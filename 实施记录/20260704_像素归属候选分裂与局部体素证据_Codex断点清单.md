# 20260704 像素归属候选分裂与局部体素证据 Codex 断点清单

## 0. 恢复摘要

当前切片：

```text
E7 / P5：像素归属候选分裂与局部体素证据实现映射计划
S0：代码事实扫描和冻结
```

依据文件：

```text
计划/计划索引.md
计划/20260703_像素归属候选分裂与局部体素证据实现映射计划_v0.1.md
规范/详细设计/待完成/20260703_像素归属候选分裂与局部体素证据详细设计_v0.1.md
规范/详细设计/待完成/20260703_观察域对象与中间层目标投影详细设计_v0.1.md
规范/规范目录.md
docs/工程图谱/00_图谱总索引.md
```

当前主动队列：

```text
E6/P8 已闭合；当前连续执行入口为 E7/P5 S0。
E7/P5 禁止宣称：像素归属图、候选分裂、局部体素证据挂 C 或局部体素隔离闸已代码实现。
```

禁止宣称：

```text
不宣称 D4 已实现。
不宣称像素归属图材料页已代码实现。
不宣称候选分裂完整接入场景体素投影、承载面边界、遮挡边界和低质量区域。
不宣称局部体素证据已正式挂到观察候选 C。
不宣称局部体素可写稳定存在体素或世界树事实。
不宣称真实 D455 / 自然样本已验证。
```

## 1. S0 代码事实扫描

### 1.1 规范和设计边界

```text
D3 固定 F/C/R/T/O/G/结构化信息缺口为概念名、运行期视图名或特征族名，不表示新增 C++ 结构体。
D3 冻结 C 的第一版承载：结构_外设观察像素簇摘要、结构_外设观察报告队列项.观察像素簇集合、结构_视觉观察候选。
D4 固定像素只能归属观察候选 C，不允许直接归属世界树稳定存在 E。
D4 固定局部体素证据挂 C，不直接写 E 的稳定体素。
D4 禁止新增结构体承载本设计对象，禁止新增普通布尔值特征表达可用 / 可提交 / 可融合。
```

### 1.2 现有句柄表

```text
外设观察报告队列.ixx:469 结构_外设观察报告队列项 已有：
原始观察帧句柄
深度帧句柄
彩色帧句柄
对齐帧句柄
空间候选队列句柄
像素归属账本句柄
本帧处理掩码句柄
观察像素簇集合

外设观察报告队列.ixx:261 结构_外设观察像素簇摘要 已有：
外设内部簇ID
来源空间候选ID
像素数量
投影范围
空间范围
深度有效率 / 空间坐标有效率 / 空间连续性评分
像素集合掩码句柄
彩色轮廓局部图句柄
深度轮廓局部图句柄
ROI句柄
点集句柄
空间候选句柄
像素归属验证材料句柄
```

### 1.3 现有特征和值域入口

```text
外设观察报告队列.ixx 已有 枚举_外设提交缺口码::缺像素归属账本。
外设观察报告队列.ixx 已有 外设提交缺口_结构化缺口大类(...)，把缺像素归属账本映射到像素归属缺口。
外设观察报告队列.ixx 已有 外设提交缺口_结构化阻断阶段(...)，把缺像素归属账本映射到像素归属阶段。
外设观察报告队列.ixx 已有 外设提交缺口_需求化处理建议(...)，但只返回 D5 建议值域，不直接创建需求。
自我动作实现.外设模块.ixx 已有像素归属验证相关特征入口和计算全帧像素归属账本(...)。
```

S0 未确认到：

```text
独立的像素观察归属图材料页结构。
独立的像素状态值域版本字段。
读取像素观察归属图材料页(...)。
挂接局部体素证据到观察候选(...)。
```

### 1.4 现有材料页读写入口

```text
外设观察报告队列.ixx:899 结构_D455短期观察材料页 保存报告ID、创建时间、过期时间、D455 调用结果和报告项。
外设观察报告队列.ixx:746 提交D455短期观察材料页(...) 是 D455 短期材料页写入入口。
外设观察报告队列.ixx:752 / 3405 解析D455观察材料句柄(...) 是通用句柄解析入口。
外设观察报告队列.ixx:1548 D455_查找材料页空间候选(...) 可按候选编号回查空间候选。
外设观察报告队列.ixx:1551-1665 D455_尝试匹配帧句柄(...) 已能识别像素归属账本句柄，并用空间候选像素索引集合计算数据元素数量。
外设观察报告队列.ixx:3451 读取D455簇三维体素轮廓视角(...) 可从簇句柄对应短期材料页重建局部体素轮廓视角。
```

判断：

```text
像素归属账本不是完全缺入口；它已作为报告项句柄和通用 D455 短期材料页资源可回查。
但当前只能解析摘要和回查簇局部体素视角，缺 D4 要求的像素观察归属图材料页契约、状态值域版本和每像素候选编号图读取入口。
```

### 1.5 C / O / 视觉融合入口

```text
视觉观察融合模块.ixx:114 结构_视觉观察候选 目前包含数据项ID、解释类型和先验评分。
视觉观察融合模块.ixx:124 结构_视觉观察域结果包 包含候选集合、先验匹配评分集合、自由空间冲突候选集合和信息缺口集合。
视觉观察融合模块.ixx:894 融合观察域结果(...) 融合合法外设材料和 SceneVoxelPrior，输出观察域候选、评分、解释和缺口。
自我动作实现.扫描.ixx:1356 查找视觉候选_按视觉数据项ID(...) 可按视觉数据项回读候选。
自我动作实现.扫描.ixx:1368 查找视觉候选_按已确认存在ID(...) 可把候选解释映射回扫描数据项。
自我动作实现.扫描.ixx:2774 扫描路径已调用 融合观察域结果(...)。
```

判断：

```text
C 的真实代码承载入口已确认：结构_外设观察像素簇摘要 / 观察像素簇集合 / 结构_视觉观察候选。
当前 C 仍偏运行期视图和摘要，未包含 D4 要求的像素归属质量、候选局部体素句柄、局部体素完整性评分等完整特征族。
```

### 1.6 现有体素加工入口

```text
视觉观察融合模块.ixx:173 枚举_体素子轮廓候选状态 已有 可生成局部体素候选 状态。
视觉观察融合模块.ixx:201 结构_体素子轮廓候选 已有局部体素占据候选、深度支撑、空间支撑和局部体素检查。
视觉观察融合模块.ixx:233 结构_体素加工候选结果包 已有子轮廓候选集合、处理状态和可提交状态。
视觉观察融合模块.ixx:625 生成子轮廓候选集合(...) 按彩色连通分区和深度支撑生成子轮廓候选集合。
视觉观察融合模块.ixx:1195 处理任务_(...) 把体素加工任务转换成候选结果包，正式入账留给提交入口。
视觉观察融合模块.ixx:1393 提交体素加工任务(...) 是全局工作线程提交入口。
视觉观察融合模块.ixx:1399 读取体素加工候选结果包集合(...) 读取候选结果包副本。
```

判断：

```text
体素加工候选结果包和局部体素候选生成入口已存在。
当前未发现把 结构_体素加工候选结果包 / 结构_体素子轮廓候选 正式挂接到观察候选 C 的函数。
S4 前必须补或复用 C 特征族挂接入口，且不得直接写 E 的稳定体素。
```

### 1.7 函数说明文档扫描

```text
已命中：
生成子轮廓候选集合：视觉观察融合模块.ixx:625
融合观察域结果：视觉观察融合模块.ixx:894
提交体素加工任务：视觉观察融合模块.ixx:1393
读取体素加工候选结果包集合：视觉观察融合模块.ixx:1399
解析D455观察材料句柄：外设观察报告队列.ixx:3405
读取D455簇三维体素轮廓视角：外设观察报告队列.ixx:3451
计算全帧像素归属账本：自我动作实现.外设模块.ixx:13594

未命中：
读取像素观察归属图材料页
像素观察归属图
挂接局部体素证据到观察候选
```

### 1.8 缺入口清单

```text
缺像素观察归属图材料页契约：缺图宽、图高、来源报告编号、来源帧编号、坐标系版本、状态值域版本、每像素观察候选局部编号、每像素状态值等结构化读取结果。
缺像素状态值域第一版代码值域 / 状态值域版本承载。
缺读取像素观察归属图材料页(...)。
缺把局部体素候选结果包挂接到 C 的正式入口。
缺候选分裂使用场景体素投影、承载面边界、遮挡边界和低质量区域的完整路径；当前视觉融合主要输出先验评分、遮挡 / 自由空间冲突候选和缺口。
```

### 1.9 S0 当前判断

```text
S0 只读扫描完成。
S1 可执行：先登记像素状态值域和像素归属图特征族契约，不新增普通布尔值特征。
S2 可执行但必须补专用材料页契约和读取入口；现有通用 D455 短期材料页可作为底座。
S4 前置条件已部分满足：C 的真实承载入口已确认；但必须补局部体素证据挂 C 入口后才能进入挂接实现。
本切片未修改 C++ 代码，未运行构建，未执行真实 D455 / 自然样本验证。
```
