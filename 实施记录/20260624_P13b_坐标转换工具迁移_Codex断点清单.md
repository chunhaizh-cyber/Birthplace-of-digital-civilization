# 20260624 P13b 坐标转换工具迁移 Codex断点清单

## 0. 恢复摘要

当前切片已闭合。`坐标分量转I64(double, I64&)`、`坐标转I64三元(const Vector3D&, I64&, I64&, I64&)` 已迁入 `全局共享函数类.ixx` 并导出，`世界树类.cpp`、`场景类.cpp`、`存在类.cpp` 三处重复本地坐标转换 helper 已删除，场景 / 存在坐标写入调用已改用全局函数。

验证证据：

```text
python .\tools\生成函数功能说明文档.py
结果：已生成 D:\鱼巢\说明书\函数功能说明文档.md；源码文件 127；函数条目 7180。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m /nologo
结果：已成功生成；0 个警告；0 个错误。
```

下一步：继续 P13b，从 B 类低风险候选中选择下一个小函数族，迁移前先查 `全局共享函数类.ixx` 和 `说明书/函数功能说明文档.md` 是否已有同功能函数。

禁止宣称：不宣称 P13 全部完成，不宣称项目函数去重全部完成，不宣称自我苏醒完成或初步成熟完成。

## 1. 修改范围

```text
全局共享函数类.ixx
世界树类.cpp
场景类.cpp
存在类.cpp
说明书/函数功能说明文档.md
计划/计划索引.md
计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md
```

## 2. 关键决策

```text
1. `坐标分量转I64` 只依赖 double、I64、标准库有限值 / 四舍五入判断，不涉及领域主信息。
2. `坐标转I64三元` 只依赖 `基础数据类型.h` 中的 Vector3D 和 I64，符合全局共享函数类当前包含边界。
3. 保留原语义：非有限值或 I64 越界返回 false，成功时使用 llround 四舍五入。
```
