# 20260606 自我任务管理边界纠偏 Codex 断点清单

## 当前切片

计划：`计划/20260606_自我线程与任务管理状态推进边界纠偏计划_v0.1.md`

当前执行：P0 / P1 / P2 / P3 / P4 / P5 已代码层推进；P4 待真实日志复验。

```text
P0：修订 002 / 002001 规范，固化自我线程只做需求侧治理、任务创建意图、真实执行动作前一次性许可和结算事件。
P1：把跨线程公共任务状态推进请求拆成任务生命周期事件与执行前许可请求 / 结果。
P2：替换自我线程直接构造任务状态推进请求的路径。
P3：观察事实、父任务等待条件变化、价值结算完成由任务管理界面线程内部裁决状态。
P4：执行前许可改为任务管理上行请求、自我线程 mailbox 固定允许审批、任务管理按工作项ID接收许可结果。
P5：移除自我线程同步消费任务调度队列的 fallback。
```

## 依据文件

```text
规范/规范目录.md
计划/计划索引.md
规范/自我线程与任务管理线程权责边界规范20260512.md
规范/任务筹办与执行边界规范20260512.md
规范/自我内部循环实现规范20260512.md
规范/需求树生长机制规范20260512.md
计划/20260606_自我线程与任务管理状态推进边界纠偏计划_v0.1.md
```

## 已改文件

```text
任务模块.管理线程协议.ixx
任务模块.工作线程消息协议.ixx
任务模块.管理界面线程.ixx
任务模块.管理界面线程.impl.cpp
自我线程模块.消息协议.ixx
自我线程模块.impl.cpp
规范/自我线程与任务管理线程权责边界规范20260512.md
规范/任务筹办与执行边界规范20260512.md
规范/自我内部循环实现规范20260512.md
规范/需求树生长机制规范20260512.md
计划/计划索引.md
计划/20260606_自我线程与任务管理状态推进边界纠偏计划_v0.1.md
实施记录/20260606_自我任务管理边界纠偏_Codex断点清单.md
```

## 已运行命令

```text
读取规范目录、计划索引、P0 相关规范片段。
读取当前 Git 状态，发现已有多项未提交代码改动，后续提交必须只纳入本切片相关文件。
rg -n "结构_任务状态推进请求|提交任务状态推进请求\\(" 自我线程模块.impl.cpp 任务模块.管理界面线程.ixx 任务模块.管理线程协议.ixx 任务模块.管理界面线程.impl.cpp
rg -n "私有_消费任务调度队列_按请求\\(|私有_消费任务调度队列\\(" 任务模块.管理界面线程.impl.cpp
rg -n "巡检并派发就绪任务\\(" --glob '!x64/**' --glob '!Debug/**' --glob '!日志/**' --glob '!logs/**'
rg -n "执行前许可请求|任务管理执行前许可|提交任务执行前许可结果|处理执行前许可门控|执行前许可等待表|执行前许可结果表|审批任务执行前许可" 任务模块.工作线程消息协议.ixx 自我线程模块.消息协议.ixx 自我线程模块.impl.cpp 任务模块.管理界面线程.ixx 任务模块.管理界面线程.impl.cpp 任务模块.管理线程协议.ixx
git diff --check -- 任务模块.工作线程消息协议.ixx 任务模块.管理线程协议.ixx 任务模块.管理界面线程.ixx 任务模块.管理界面线程.impl.cpp 自我线程模块.消息协议.ixx 自我线程模块.impl.cpp
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

## 关键证据

### P4 真实运行预测

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=3000

预测：
    如果本轮自检自然推进到执行工作项，日志应按同一工作项ID出现：
        任务管理界面线程/执行前许可请求上行；
        自我线程/执行前许可审批；
        任务管理界面线程/执行前许可结果入队；
        任务管理界面线程/执行前许可通过。
    同一工作项ID在工作线程未结束前不应重复出现多次审批。
    如果本轮没有自然形成执行工作项，则上述日志不会出现；这只能证明入口未触发，不能作为 P4 业务验收通过。
```

### P4 首轮真实运行结果

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=3000

结果：
    退出码 0。
    日志文件：日志/鱼巢_run_20260606.低值g。
    rg -n "执行前许可" 日志/鱼巢_run_20260606.低值g 无命中。
    运行尾部可见任务 6 / 7 仍停在筹办和方法结构缺口，未自然形成执行工作项。

判断：
    该运行没有触发执行前许可入口；只能证明本入口未触发，不能证明 P4 业务链闭合。
```

### P4 加长真实运行预测

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000

预测：
    如果加长运行自然推进到执行工作项，日志应按同一工作项ID出现：
        任务管理界面线程/执行前许可请求上行；
        自我线程/执行前许可审批；
        任务管理界面线程/执行前许可结果入队；
        任务管理界面线程/执行前许可通过。
    许可通过后才能出现同一工作项的 已执行方法=1 结果归并。
    如果仍没有执行工作项或没有执行前许可日志，则当前真实入口不足以验证 P4，需要登记为验证阻塞点。
```

### P4 加长真实运行结果与修正

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000

结果：
    退出码 0。
    20:49-20:50 日志段出现执行前许可链路。
    执行前许可请求上行=30。
    自我线程执行前许可审批=2。
    执行前许可结果入队=2。
    执行前许可通过=2。
    执行工作项等待自我审批=1241。
    已执行方法=1 的归并证据只对应工作项 27 / 28。

偏差：
    执行前许可请求已能进入自我线程 mailbox，但在派生需求、自检报告、观察承接等消息量较大时，
    自我线程只审批了少量许可，后续执行工作项长期等待许可结果。

修正：
    新增自我线程执行前许可抢占审批路径。
    该路径仍由自我线程主循环调用 私有_审批任务执行前许可请求_线程侧，
    只改变 mailbox 中执行前许可请求的消费优先级，不让任务管理线程自行放行。
    接入点：规范化消息后、派生需求暂停前任务管理上行后、主派发后、任务巡检前、任务管理后台上行后。

构建：
    Debug x64 构建通过；0 错误，1 个链接警告 LNK4075。
```

### P4 抢占审批复验预测

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000

预测：
    日志应出现 自我线程/执行前许可抢占抽取完成 与 自我线程/执行前许可抢占审批完成。
    同一工作项ID应按顺序出现：
        任务管理界面线程/执行前许可请求上行；
        自我线程/执行前许可审批；
        任务管理界面线程/执行前许可结果入队；
        任务管理界面线程/执行前许可通过；
        同一工作项 已执行方法=1 结果归并。
    允许短暂出现 执行工作项等待自我审批，但不应再出现大量请求长期没有审批结果。
```

### P4 抢占审批复验结果与二次修正

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000

结果：
    退出码 0。
    20:55-20:56 日志段出现执行前许可链路。
    执行前许可请求上行=33。
    自我线程/执行前许可抢占抽取完成=2。
    自我线程执行前许可审批=31。
    执行前许可结果入队=31。
    执行前许可通过=17。
    执行工作项等待自我审批=1087。
    已执行方法=1 的归并证据=4。

偏差：
    首次抢占能把大部分请求补审批，证明 mailbox 固定允许审批通道有效；
    但两次抢占抽取相隔约 35 秒，期间任务管理执行工作项仍大量等待自我审批。
    这不是许可策略阻断，而是自我主循环中执行前许可请求的消费优先级仍偏低。

二次修正：
    在自我线程主循环增加执行前许可抢占检查点：
        二次特征重算后；
        治理帧刷新后；
        需求整理后。
    目标是让执行前许可请求在自我侧重计算 / 重治理 / 需求整理之后尽快被固定允许，
    避免继续排在普通需求治理消息之后形成长积压。

构建：
    Debug x64 构建通过；0 错误，1 个链接警告 LNK4075。
```

### P4 二次抢占复验预测

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000

预测：
    日志应比上一轮更频繁出现 自我线程/执行前许可抢占抽取完成。
    执行前许可请求上行后，应在更近时间内出现同一工作项ID的：
        自我线程/执行前许可审批；
        任务管理界面线程/执行前许可结果入队；
        任务管理界面线程/执行前许可通过。
    运行窗口尾部允许存在尚未完成审批或尚未通过的在途工作项；
    非尾部工作项不应再出现请求长期无审批结果。
    同一工作项ID在工作线程未结束前仍不得重复审批。
```

### P4 二次抢占复验结果与三次修正

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000

结果：
    退出码 0。
    21:00-21:01 日志段出现执行前许可链路。
    执行前许可请求上行=30。
    自我线程/执行前许可抢占抽取完成=2。
    自我线程/执行前许可抢占审批完成=2。
    自我线程执行前许可审批=30。
    执行前许可结果入队=30。
    执行前许可通过=16。
    执行工作项等待自我审批=1209。
    已执行方法=1 的归并证据=4。
    请求 / 审批 / 结果入队的工作项集合完全一致：
        29,30,32,34,36,38,39,40,41,43,44,45,47,48,49,51,52,53,54,56,57,58,59,61,62,63,64,65,66,67。
    未发现同一工作项重复审批。

偏差：
    固定允许策略已覆盖全部请求，但审批延迟仍偏大。
    工作项 32 从 21:00:26.836 请求，到 21:01:08.931 才审批，最大延迟 42095ms；
    30 个请求平均请求到审批延迟 17144.4ms。
    新增的二次特征 / 治理帧 / 需求整理后检查点没有触发，
    因为自我线程长时间停留在 规范化消息并落一次特征账 这一长步骤内。

三次修正：
    在 自我线程模块.impl.cpp 的 步骤_规范化消息并落一次特征账_ 逐条落账循环内，
    每处理完一条治理消息，就由自我主循环线程抢占抽取并审批最多 16 条执行前许可请求。
    该修正不改变审批策略，现阶段仍固定允许；
    也不让任务管理线程自行放行，审批日志仍应来自自我主循环线程。

构建：
    Debug x64 构建通过；0 错误，1 个链接警告 LNK4075。
```

### P4 逐条落账抢占复验预测

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000

预测：
    日志应出现 自我线程/执行前许可抢占抽取完成，来源包含 规范化消息逐条落账后。
    请求 / 审批 / 结果入队的工作项集合应一致。
    非停止窗口内的请求到审批延迟应从上一轮最大 42095ms 明显下降，
    不应再出现 20s 以上的普通请求积压。
    同一工作项ID在工作线程未结束前不得重复审批。
    执行前许可通过数量可以少于审批数量，
    因为运行窗口结束和同任务在途租约会让部分已允许工作项尚未派发。
```

### P4 逐条落账抢占复验结果与许可缓存修正

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000

结果：
    退出码 0。
    21:05-21:06 日志段出现执行前许可链路，审批线程为自我主循环线程 T25032。
    执行前许可请求上行=13。
    自我线程/执行前许可抢占抽取完成=7，来源均为 规范化消息逐条落账后。
    自我线程执行前许可审批=10。
    执行前许可结果入队=10。
    执行前许可通过=10。
    执行工作项等待自我审批=72。
    已执行方法=1 的归并证据=18。
    已审批工作项请求到审批最大延迟 4247ms，平均 1470.6ms。

判断：
    逐条落账抢占已把上一轮最大 42095ms / 平均 17144.4ms 的积压明显压低。
    48 / 49 是自我主循环退出后的停止窗口请求，不作为未审批缺陷。
    工作项 31 出现重复 执行前许可请求上行：
        21:05:47.831 直接通知=1；
        21:05:52.578 直接通知=0。
    该重复没有导致二次审批，但说明任务管理在同一工作项未真正完成前消耗并删除了许可结果，
    后续因同任务在途重排再次走到许可入口时会重新记录请求。

许可缓存修正：
    执行前许可结果改为绑定工作项缓存复用；
    私有_取出执行前许可结果 不再读取后删除许可结果 / 等待记录；
    私有_登记工作完成项 在工作项完成入队时清理该工作项的许可结果 / 等待记录。
    这保证同一工作项未完成前不重复申请自我审批，完成后不保留旧许可。

构建：
    Debug x64 构建通过；0 错误，1 个链接警告 LNK4075。
```

### P4 许可缓存复验预测

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000

预测：
    非停止窗口内，任务管理界面线程/执行前许可请求上行 的工作项ID不应重复。
    请求 / 自我线程审批 / 结果入队 / 许可通过的工作项集合应一致，尾部停止窗口请求除外。
    自我线程/执行前许可抢占抽取完成 应继续出现，来源可包含 规范化消息逐条落账后。
    请求到审批延迟不应回退到 20s 以上积压。
    同一工作项未完成前不得重复向自我线程审批。
```

### P4 同任务在途前置检查复验预测

```text
新增修正：
    任务管理界面线程在执行前许可门控入口先检查同任务在途工作项。
    如果已有在途工作项，只按任务管理线程规则重排，不向自我线程发送执行前许可请求。
    许可结果仍按工作项ID缓存到工作项完成；同一工作项的 执行前许可通过 日志只记录一次。

预测：
    Debug x64 构建继续通过。
    非停止窗口内，执行前许可请求上行 工作项ID不重复。
    非停止窗口内，自我线程/执行前许可审批 与 任务管理界面线程/执行前许可结果入队 的工作项集合一致。
    同一工作项ID的 任务管理界面线程/执行前许可通过 不重复。
    同任务在途期间只出现 任务管理界面线程/同任务在途工作项重排，不再反复消耗许可门控。
```

### P4 同任务执行前许可占用复验预测

```text
新增修正：
    任务管理界面线程新增同任务执行前许可占用表。
    同一任务已有执行工作项申请或持有许可时，其他同任务执行工作项只重排，不再并发申请自我线程审批。
    占用在工作项完成、死信、过期终止或任务管理界面线程停止时释放。

预测：
    Debug x64 构建继续通过。
    非停止窗口内，执行前许可请求上行 / 自我线程审批 / 结果入队 的工作项集合一致。
    同一任务主键在一个执行许可占用未释放前，不应出现第二个执行前许可请求上行。
    被占用挡住的执行工作项应记录 任务管理界面线程/同任务执行前许可占用重排。
    同一工作项ID的 执行前许可请求上行、执行前许可审批、执行前许可通过 均不重复。
```

### P4 执行前许可免审批方法白名单预测

```text
新增修正：
    任务管理界面线程新增执行前许可免审批方法白名单。
    白名单按方法主键注册、移除、清空。
    执行工作项当前方法命中白名单时，任务管理界面线程记录 执行前许可白名单通过，
    不向自我线程发送 执行前许可请求上行。

预测：
    Debug x64 构建继续通过。
    历史预测时尚未配置默认白名单方法；该口径已被后续 外设四方法加入免审批白名单验证结果 覆盖。
    当前最新口径：外设四方法默认进入白名单，命中时不出现 执行前许可请求上行 或 自我线程审批。
```

### P4 执行前许可免审批方法白名单验证结果

```text
构建：
    Debug x64 构建通过；0 错误，1 个 LNK4075 链接警告。

命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000

结果：
    程序退出码 0。
    当前未注册白名单方法：
        任务管理界面线程/执行前许可白名单通过 = 0；
        任务管理界面线程/注册执行前许可免审批方法 = 0。
    非停止窗口内：
        执行前许可请求上行 = 6；
        自我线程/执行前许可审批 = 6；
        执行前许可结果入队 = 6；
        执行前许可通过 = 6。
    工作项集合一致：
        27,28,32,34,39,42。
    执行前许可请求上行 / 自我线程审批 / 执行前许可通过 均无重复工作项ID。
    同任务执行前许可占用重排 = 11，说明同任务并发申请仍被任务管理界面线程挡住。
```

### P4 外设四方法加入免审批白名单预测

```text
新增修正：
    任务管理界面线程启动 / 工作线程池启动时，按本能ID解析并注册外设四方法的实际方法节点主键：
        6145 自我_识别外设观察材料；
        6146 自我_扫描已识别区域；
        6147 自我_跟踪指定存在；
        6150 提交_当前观察范围可观测单位存在对应事实。

预测：
    Debug x64 构建继续通过。
    真实运行日志应出现 任务管理界面线程/注册外设方法执行前许可免审批白名单。
    注册完成数量应为 4；若任务管理界面线程早于自我存在启动，允许先出现注册延后，后续工作线程池启动应完成注册。
    当前方法命中上述四个方法主键时，应出现 任务管理界面线程/执行前许可白名单通过。
    白名单通过的工作项不应出现 任务管理界面线程/执行前许可请求上行 或 自我线程/执行前许可审批。
```

### P4 外设四方法加入免审批白名单验证结果

```text
代码修正：
    任务管理界面线程默认解析并注册外设四方法方法主键：
        6145 -> 方法 1i；
        6146 -> 方法 1l；
        6147 -> 方法 1o；
        6150 -> 方法 1x。
    默认注册增加 外设四方法执行前许可白名单已注册 标记和注册互斥；
    初次成功注册后不再在后续启动守卫 / 巡检路径重复解析和刷日志。

构建结果：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    通过；0 错误，1 个 LNK4075 链接警告。

真实运行：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000
    通过；退出码 0。

21:38-21:39 日志核对：
    任务管理界面线程/注册外设方法执行前许可免审批白名单 = 4；
    任务管理界面线程/外设方法执行前许可白名单注册完成 = 1，注册数量=4；
    任务管理界面线程/执行前许可白名单通过 = 4，工作项=28,32,34,31；
    白名单通过方法分布：1x 出现 3 次，1i 出现 1 次；
    白名单通过工作项与 执行前许可请求上行 / 自我线程审批 / 执行前许可结果入队 / 执行前许可通过 均无交集。

当前结论：
    外设四方法已默认进入执行前许可免审批白名单。
    命中白名单时，任务管理界面线程直接通过许可门控，不向自我线程提请审批。
```

```text
构建结果：
    Debug x64 构建通过；0 错误，1 个链接警告 LNK4075（/EDITANDCONTINUE 被 /INCREMENTAL:NO 忽略）。

搜索结果：
    自我线程模块.impl.cpp 中不再出现 结构_任务状态推进请求 或 提交任务状态推进请求 调用。
    提交任务状态推进请求 只保留在 任务模块.管理界面线程.impl.cpp 内部事件裁决路径。
    私有_消费任务调度队列_按请求 / 私有_消费任务调度队列 只剩定义，没有调用点。
    巡检并派发就绪任务 不再由自我线程模块.impl.cpp 调用；调用点只剩任务管理界面线程后台循环。
    执行前许可请求 已加入任务管理上行消息协议。
    自我线程收到执行前许可请求后，现阶段固定返回允许。
    任务管理界面线程按 工作项ID 维护执行前许可等待表 / 结果表，许可未返回前执行工作项重排等待，不调用任务执行。
```

## 当前判断

```text
自我线程不应直接构造带目标任务状态值的状态推进请求。
任务管理界面线程负责全部任务状态值裁决和提交。
自我线程只在真实执行动作前提供一次性工作项许可；工作线程未结束前不得重复审批同一工作项。
任务管理调度队列不得由自我线程同步 fallback 消费。
不能用任务管理线程直接读取自我状态来冒充“自我线程审批”；P4 已补异步审批通道。
现阶段审批策略按用户口径固定返回允许；禁止项和方向不一致字段保留，暂不阻断执行。
```

## 下一步

```text
1. 恢复 P9d 6147 跟踪目标上下文 / 任务主体桥。
2. 先定位 6147 如何取得目标存在、已归属状态、观察窗口、跟踪基准和预测区间。
3. 保持当前边界：外设四方法白名单只跳过执行前许可审批，不改变任务状态裁决职责。
```

## 禁止宣称项

```text
不得宣称自我苏醒完成。
不得宣称初步成熟完成。
不得把任务筹办 / 任务执行恢复为本能方法。
不得把线程作为动作来源。
不得把外设材料或观察报告直接当世界真值。
不得用模拟测试、伪造入口或伪造动作动态补验收。
```
