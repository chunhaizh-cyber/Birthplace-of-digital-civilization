# 20260625 P13b 运行包错误文本工具迁移 Codex断点清单

## 0. 恢复摘要

当前切片已闭合。已处理 `任务模块.运行包.impl.cpp::私有_写错误(std::string*, const char*)`。

依据：

```text
计划/计划索引.md：P13b 全局共享迁移；B 类候选逐个判定迁移 / 复用 / 保留。
实施记录/20260624_P13_函数自定义主信息分类候选表.tsv：`私有_写错误` 为 B 类候选，未命中自定义主信息 / 节点 / 结构 / 协议对象。
全局共享函数类.ixx 文件头规则：无领域自定义主信息 / 领域类型参与的通用函数可进入本模块。
```

当前判断：

```text
`私有_写错误` 只在 `任务模块.运行包.impl.cpp` 内部使用，用于把可选 `std::string*` 错误输出写入 `const char*` 文本。
该函数不读取 / 写入项目主信息、节点、任务状态或机器判断字段；错误文本只作为调用方可读诊断输出。
`全局共享函数类.ixx` 和 `说明书/函数功能说明文档.md` 未发现同功能函数。
```

已完成：

```text
1. 在 `全局共享函数类.ixx` 新增通用 `写入可选错误文本`。
2. 给 `任务模块.运行包.impl.cpp` 补最小“文件头部规则注释模块”。
3. 删除本地 `私有_写错误`，调用点改为 `写入可选错误文本`。
4. 运行函数说明文档生成脚本：扫描源文件 127 个，函数条目 7163。
5. Debug x64 构建通过：0 警告，0 错误。
```

复扫结果：

```text
`说明书/函数功能说明文档.md` 已登记 `全局共享函数类.ixx::写入可选错误文本`。
`任务模块.运行包.impl.cpp::私有_写错误` 不再出现在函数说明文档和当前源码中。
```

禁止宣称：不宣称 P13 完成，不宣称函数去重全部完成，不宣称自我苏醒完成或初步成熟完成。
