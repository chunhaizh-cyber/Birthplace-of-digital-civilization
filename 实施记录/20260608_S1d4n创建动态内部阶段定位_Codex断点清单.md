# 20260608 S1d4n 创建动态内部阶段定位 Codex 断点清单

## 当前切片

- 主动计划：`计划/20260607_运行速度常驻路径优先优化计划_v0.1.md`
- 执行方案：`计划/20260607_S1d4n创建动态内部阶段定位方案_v0.1.md`
- 切片：S1d-4n 创建动态内部阶段定位。

## 依据文件

- `AGENTS.md`
- `规范/规范目录.md`
- `计划/计划索引.md`
- `规范/动态信息未实现功能补齐详细设计20260430.md`
- `规范/方法动作动态硬规则规范20260512.md`
- `规范/动作动态即因果账本规范20260512.md`
- `计划/20260607_S1d4m相邻前状态查找内部阶段定位方案_v0.1.md`
- `实施记录/20260608_S1d4m相邻前状态查找内部阶段定位_Codex断点清单.md`

## 当前判断

- S1d-4m 已闭合并提交：`6386a6d`。
- `查找相邻前状态` 未出现慢采样，不是剩余主耗时。
- 剩余热点在 `创建动态` 内部，需区分查重、动态索引追加排序和二次特征 / 聚合生成。
- 本切片只做内部阶段采样，不删除动态二次特征和聚合生成。

## 构建 / 运行前预测

- Debug x64 构建通过。
- 真实运行退出码 0。
- 日志出现 `创建动态/内部阶段耗时`。
- 日志保留 `任务状态动作动态=<动态主键>`。
- 不出现 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`。
- 不出现旧路径锚点 `准备本能调用`、`执行本能方法`、`任务域本能输入包`。

## 已改文件

- `计划/20260607_S1d4n创建动态内部阶段定位方案_v0.1.md`
- `实施记录/20260608_S1d4n创建动态内部阶段定位_Codex断点清单.md`
- `动态类.cpp`

## 已完成修改

- `动态类.cpp` 新增 `私有_创建动态采样序号`。
- `动态类::创建动态(场景, 动态节点主信息类*)` 增加 `创建动态/内部阶段耗时` 采样。
- 采样阶段包括状态引用解析、状态动态合法检查、已有状态动态查找、动态主体特征补齐、动态名称补齐、业务语义刷新、添加动态节点、场景主信息、追加动态索引、动态二次特征、相邻动态协同、连续同向聚合、同类事件段聚合。
- 保留拒绝生成、状态值相同、已有动态和完整创建的原返回语义。

## 已运行命令

- `git diff --check -- '动态类.cpp' '计划/20260607_S1d4n创建动态内部阶段定位方案_v0.1.md' '实施记录/20260608_S1d4n创建动态内部阶段定位_Codex断点清单.md'`
- `Get-Process | Where-Object { $_.ProcessName -eq '鱼巢' }`
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000`
- `rg -n "创建动态/内部阶段耗时|任务状态动作动态=|状态创建/内部阶段耗时|任务状态被动提交/内部阶段耗时|任务状态动作动态创建/内部阶段耗时" '日志/鱼巢_run_20260608.低值g'`
- `rg -n "任务状态被动提交/逻辑错误|任务状态动作动态逻辑错误|准备本能调用|执行本能方法|任务域本能输入包|后台worker工作项进入死信|拒绝归并完成项" '日志/鱼巢_run_20260608.低值g'`
- PowerShell 汇总最新线程创建动态 / 状态创建 / 被动提交 / 动作动态创建内部阶段采样。

## 实际验证结果

- `git diff --check` 通过，仅有工作区 LF/CRLF 提示。
- Debug x64 构建通过；链接阶段仅见既有 `/EDITANDCONTINUE` 与 `/INCREMENTAL:NO` 组合提示。
- 真实运行退出码 0。
- 最新日志：`日志/鱼巢_run_20260608.低值g`，更新时间 2026-06-08 09:48:38。
- 正向证据：日志出现 `创建动态/内部阶段耗时`；最新线程 T11876 有 50 条 `任务状态动作动态=<动态主键>` 状态提交日志。
- 负向证据：未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`、`后台worker工作项进入死信`、`拒绝归并完成项`。

## 关键耗时

- 最新线程：T11876。
- 最新线程状态创建内部采样：26 条。
- `状态动态刷新` 平均约 582597us。
- 创建动态内部采样：44 条。
- 创建动态最慢阶段分布：`同类事件段聚合=26`、`动态二次特征=18`。
- 平均创建动态总耗时：约 588715us。
- `已有状态动态查找`：平均约 380us。
- `追加动态索引`：平均约 563us。
- `动态二次特征`：平均约 290448us。
- `同类事件段聚合`：平均约 292324us，最大 790481us。
- 层级 0 样本：26 条，最慢阶段全部为 `同类事件段聚合`。
- 层级 1 样本：18 条，最慢阶段全部为 `动态二次特征`。

## 当前结论

- S1d-4n 没有破坏任务状态动作动态生成和旧路径删除边界。
- `已有状态动态查找` 和 `追加动态索引` 不是主耗时。
- 层级 0 的 `同类事件段聚合` 慢主要包含递归创建层级 1 聚合动态；层级 1 的实际主耗时是 `动态二次特征`。
- 下一步应进入 S1d-4o：拆 `二次特征应用模块.ixx` 中动态二次特征生成内部阶段。

## 下一步

1. 更新计划索引和总性能计划，登记 S1d-4n 闭合证据。
2. 自动提交 S1d-4n 闭合切片。
3. 转入 S1d-4o：动态二次特征生成内部阶段定位。

## 禁止宣称

- 不宣称自我苏醒完成。
- 不宣称初步成熟完成。
- 不把阶段采样当成动态二次特征或聚合可删除。
