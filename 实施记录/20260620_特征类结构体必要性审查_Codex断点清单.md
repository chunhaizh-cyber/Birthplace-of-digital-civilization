# 20260620 特征类结构体必要性审查 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_特征类逻辑纠偏计划_v0.1.md` 的 1.1 P2.1，建立 `特征类.h` 结构体 / 枚举必要性判定表。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
规范/特征类规范20260429.md
规范/主信息字段与结构新增准入约束规范20260519.md
计划/计划索引.md
计划/20260620_特征类逻辑纠偏计划_v0.1.md
实施记录/20260620_特征类第一优先只读函数_Codex断点清单.md
```

本切片范围：

```text
1. 列出 特征类.h 全部 struct / enum class。
2. 标记字段、公开接口使用、文本字段、初步保留 / 可删 / 需纠偏判断。
3. 只形成判定表和下一步候选，不直接删除结构体。
4. 不吞并 1.2 非特征主信息直接操作审查。
```

验证前预测：

```text
1. rg -n "^struct |enum class |std::string 说明|失败原因|不可比较原因" 特征类.h 将列出结构体、枚举和文本字段关注点。
2. rg -n "存在比较结果|结构_特征状态比较结果|结构_三维体素轮廓融合结果|结构_三维体素局部轮廓相似度结果|归一化差异度" -g "*.h" -g "*.cpp" -g "*.ixx" 将定位计划已知关注项的创建点和消费点。
3. 本切片不修改代码；如仅更新计划 / 断点，不需要重新构建作为代码证据。
```

禁止宣称：

```text
不宣称特征类逻辑纠偏完成。
不宣称结构体清理完成。
不宣称可删结构体已删除。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 1. 判定表

| 名称 | 字段摘要 | 写入 / 创建点 | 读取 / 消费点 | 公开接口 / 持久化 | 文本字段 | 初判 |
| --- | --- | --- | --- | --- | --- | --- |
| 存在比较结果 | 总差异度、归一化差异度、命中数、比较数 | `特征类::比较特征集合` | `世界树类::比较存在`、`刷新存在比较二次特征`、`二次特征生成模块::由存在比较结果生成二次特征` | 公开返回包；不直接持久化，消费后写二次特征 | 无 | 结构体保留；`归一化差异度` 已计算但当前未被二次特征消费，列入 P2.2 字段级接入 / 删除判断。 |
| 稳态策略 | 连续帧、判稳阈值、命中阈值、Kmax、淘汰超时 | `写入参数.稳态` 默认构造 | `特征类::写入特征值_按参数` | 公开参数包的子结构；不持久化 | 无 | 暂保留；是特征写入稳态策略的结构化参数，不改为文本。 |
| 写入参数 | 时间戳、仅更新、允许创建 / 淘汰、稳态策略 | 调用方传入；`写入特征值` 局部默认构造 | `特征类::写入特征值_按参数` | 公开输入参数；不持久化 | 无 | 保留；避免扩散多个裸参数。 |
| 特征写入结果 | 特征指针、候选门、稳态变化 | `特征类::写入特征值_按参数` | 写入调用方可读取结果状态 | 公开返回包；不持久化 | 无 | 保留；承载多值返回。 |
| 集合比较参数 | 缺失惩罚、归一化 | 调用方传入默认值 | `特征类::比较特征集合`、`世界树类::比较存在` | 公开输入参数；不持久化 | 无 | 保留；是比较策略参数。 |
| 特征比较方法结果 | 可比较、比较方式、左右比较值、差值 | `特征类::获取比较方法结果` | `特征类::比较状态` | 公开返回包；不持久化 | 无 | 保留；承接比较模式与差值。 |
| 结构_特征状态比较结果 | 可比较、三向关系、特征类型、比较方式、差值、说明 | `特征类::比较状态` | `任务模块.管理工作线程.ixx` 目标状态比较、动作验证报告 | 公开返回包；不持久化 | `说明` | 暂保留；`说明` 当前只见日志输出，机器分支看 `可比较/关系/差值`，列入 P2.4 文本字段纠偏。 |
| 枚举_VecU解释规则 | VecU 解释类型 | `特征类::VecU解释规则_按特征类型` | VecU 坐标 / 体素解释函数 | 公开枚举；不持久化 | 无 | 保留；符合 VecI 解释视图边界。 |
| 结构_VecU特征解释规则 | 规则、坐标维度、是否 ZigZag | `特征类::VecU解释规则_按特征类型` | VecU 校验 / 解码链 | 公开返回包；不持久化 | 无 | 保留；薄解释视图，不新增底层存储类型。 |
| 枚举_轮廓比较状态 | 未定义、可比较、格式非法、维度不支持、点数量不一致、不可对齐 | 轮廓比较函数写入 | 世界树、二次特征、外设轮廓比较链 | 公开枚举；作为二次特征离散编码来源 | 无 | 保留；结构化失败状态替代文本原因。 |
| 结构_轮廓比较结果 | 状态、维度、点数、误差、评分 | `特征类::比较*轮廓VecI64`、`世界树类::比较轮廓特征_按特征类型` | 二次特征轮廓写入、外设轮廓比较汇总 | 公开返回包；消费后写二次特征 | 无 | 保留；跨世界树 / 二次特征 / 外设链路消费。 |
| 结构_三维体素二分层摘要 | 有效、边长、层数、尺寸、体素数 | 三维体素链写入结果内填充 | `结构_三维体素链写入结果.摘要` | 返回包子结构；不直接持久化 | 无 | 暂保留；P2.2 需确认摘要字段是否被调用方消费，未消费字段再判定。 |
| 枚举_三维体素链节点状态 | 空、满、混合 | 三维体素链编码 / 解析 | 查询、相似度比较、节点写入 | 公开枚举；编码进入 VecU 节点值 | 无 | 保留；属于体素链机器状态。 |
| 结构_三维体素链节点信息 | 层级、坐标、边长、状态、尺寸、占据数 | `解析三维体素链节点VecU`、写入链内部 | 查询链、相似度比较、写入链递归 | 公开 / 内部共用返回包；字段来源于 VecU 节点 | 无 | 保留；是 VecU 体素节点解释结果。 |
| 结构_三维体素链写入结果 | 成功、根句柄、叶子句柄、摘要 | 三维体素链写入函数 | 写入调用方 | 公开返回包；句柄指向 VecU 值池 | 无 | 保留；承载写入状态和句柄。 |
| 结构_三维体素链查询结果 | 命中、节点句柄、节点信息 | `查询三维体素二分层链` | 局部体素相似度比较 | 公开返回包；不持久化 | 无 | 保留；查询多值结果。 |
| 结构_三维体素空间点毫米 | x、y、z | 外设观察报告队列填充 | 轮廓视角输入、体素融合 | 公开输入子结构；不直接持久化 | 无 | 保留；空间点结构化坐标，不拆成文本。 |
| 结构_三维体素轮廓图视角 | 宽高、视角序号、掩码、深度、空间点、颜色 | 外设观察报告队列 / 调用方填充 | 体素融合、世界树局部轮廓查找 | 公开输入包；承载 Vec / 图像材料 | 无 | 保留；多数组输入无法用单一既有节点替代。 |
| 结构_三维体素轮廓融合参数 | 体素边长、补全间隔、确认视角数、最大体素数 | 调用方传入 | 体素融合、世界树局部轮廓查找 | 公开输入参数；不持久化 | 无 | 保留；体素融合策略参数。 |
| 结构_三维体素颜色项 | x、y、z、RGBA | 体素融合结果填充 | `结构_三维体素轮廓融合结果.体素颜色` | 返回包子结构；不直接持久化 | 无 | 暂保留；P2.2 需确认颜色结果消费情况。 |
| 结构_三维体素轮廓融合结果 | 成功、失败原因、占据位块、颜色、尺寸、原点、统计 | `从多视角轮廓图生成三维体素` | 世界树局部轮廓查找、局部体素链比较 | 公开返回包；占据位块为 VecIU64 | `失败原因` | 保留；机器分支看 `成功/尺寸/占据数`，`失败原因` 列入 P2.4 文本字段纠偏。 |
| 结构_三维体素存在空间绑定参数 | 原点、最小体素边长 | 世界树局部轮廓查找构造 | 局部体素链比较 | 公开输入参数；不持久化 | 无 | 保留；把存在空间坐标显式传入比较函数。 |
| 结构_三维体素局部轮廓相似度结果 | 可比较、不可比较原因、相似度、命中率、惩罚、统计 | 局部体素链比较 | 世界树候选排序 / 过滤 | 公开返回包；不持久化 | `不可比较原因` | 保留；机器分支看 `可比较/相似度Q10000`，`不可比较原因` 当前只在特征类内赋值，列入 P2.4 文本字段纠偏。 |
| 常用抽象特征初始化结果 | 抽象特征数、区间状态数、成功 | `特征类::初始化常用抽象特征` | 初始化调用方 | 公开返回包；不持久化 | 无 | 暂保留；P2.2 再确认调用方是否需要数量字段。 |

本轮未发现可在 P2.1 直接删除的无调用结构体。

优先后续候选：

```text
1. P2.2 字段级审查：存在比较结果::归一化差异度 当前已计算但未见二次特征消费。
2. P2.4 文本字段纠偏：结构_特征状态比较结果::说明、结构_三维体素轮廓融合结果::失败原因、结构_三维体素局部轮廓相似度结果::不可比较原因。
3. P2.2 消费确认：结构_三维体素二分层摘要、结构_三维体素颜色项、常用抽象特征初始化结果 的字段是否被公开调用方真实消费。
```

## 2. 验证记录

```text
rg -n "^struct |enum class |std::string 说明|失败原因|不可比较原因" 特征类.h
结果：列出 21 个 struct、3 个 enum class；文本字段命中 说明、失败原因、不可比较原因。

rg -n "存在比较结果|结构_特征状态比较结果|结构_三维体素轮廓融合结果|结构_三维体素局部轮廓相似度结果|归一化差异度" -g "*.h" -g "*.cpp" -g "*.ixx"
结果：定位存在比较结果、特征状态比较结果、三维体素融合结果、局部轮廓相似度结果的跨文件消费点。

日志/20260620_特征类结构体引用计数.txt
结果：按名称聚合引用计数，用于判定表初判。

rg -n "\.不可比较原因|不可比较原因" -g "*.h" -g "*.cpp" -g "*.ixx"
结果：特征类该字段仅见 特征类.h:217 和 特征类.cpp:3837；另有外设观察报告队列同名结构化缺口码字段，非本结构。

rg -n "局部体素\.失败原因|结果\.失败原因 =|\.失败原因" 特征类.cpp 世界树类.cpp 世界树类.h
结果：本结构失败原因在 特征类.cpp:3573 赋值，并在 3866 拼入不可比较说明；世界树机器分支不读取该文本。

rg -n "比较\.说明|结构_特征状态比较结果|比较结果\.可比较|三向关系被接受\(比较\.关系" 任务模块.管理工作线程.ixx 特征类.cpp
结果：说明字段用于日志；任务目标判断读取 可比较/关系。
```

当前判断：

```text
P2.1 判定表完成。
本切片只改实施记录和计划，不修改代码。
下一步进入 P2.2：从字段级高风险项开始做引用闭环，不直接删除结构体。
```
