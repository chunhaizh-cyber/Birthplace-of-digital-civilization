# 20260620 因果 P6 验收稳定度更新 Codex 断点清单

## 0. 恢复摘要

当前切片：

```text
计划/20260619_因果类功能函数补完计划_v0.1.md
P6 因果验收和稳定度更新入口。
```

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
规范/方法动作动态硬规则规范20260512.md
规范/动作动态即因果账本规范20260512.md
规范/因果补完流程规范20260516.md
规范/自我动作验证闭环规范20260612.md
计划/计划索引.md
计划/20260619_因果类功能函数补完计划_v0.1.md
说明书/函数功能说明文档.md
```

当前判断：

```text
1. 函数说明文档中已有 因果类::读取因果证据状态，未发现 更新因果证据计数 / 验收动作结果因果证据 同功能函数。
2. 因果主信息类已承载基础计数字段：观察次数、条件命中次数、因出现次数、果出现次数、成立次数、失败次数、最近命中时间、最近失败时间。
3. 当前直接计数写入点在 因果类::追加证据动态样本；P6 应收口到因果类功能函数。
```

预期验证：

```text
1. 新增 因果类::更新因果证据计数 和 因果类::验收动作结果因果证据。
2. 因果类::追加证据动态样本 改为调用 更新因果证据计数，不直接分散写计数字段。
3. 验收动作结果因果证据 以动作动态匹配因果模板，以预测结果状态筛选候选，以实际结果状态判断成立；缺预测或缺实际回读只返回缺失证据。
4. 重新生成函数说明文档并通过 Debug x64 构建。
```

验证结果：

```text
1. rg -n "成立次数|失败次数|最近失败时间|最近命中时间|观察次数|条件命中次数|因出现次数|果出现次数" -S -g "*.cpp" -g "*.h" -g "*.ixx"
   退出码：0；因果主信息计数字段写入集中在 因果类::更新因果证据计数，其余命中为结构定义、读取 / 显示、任务 / 外设自身失败统计和旧文件。
2. python .\tools\生成函数功能说明文档.py
   退出码：0；函数条目 6879。
3. msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
   退出码：0；0 个警告，0 个错误。
```

闭合判断：

```text
P6 已补齐 更新因果证据计数 与 验收动作结果因果证据。
P6 已通过字段扫描、函数说明文档生成和 Debug x64 构建。
```

禁止宣称：

```text
不宣称因果类功能补完完成，除非 P1-P6 全部闭合并有构建证据。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
