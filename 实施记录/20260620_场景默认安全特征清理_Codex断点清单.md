# 20260620 场景默认安全特征清理 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.3 P4.2，复核并清理 `私有_确保场景默认特征已初始化` 中的场景安全度默认特征创建 / 写入路径。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
规范/自我存在服务与生存基本因果闭环规范20260601.md
规范/安全服务值结算规范20260518.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
实施记录/20260620_场景类非场景主信息操作扫描_Codex断点清单.md
```

## 1. 问题锚点

```text
场景类.cpp 中 私有_确保场景默认特征已初始化 会在创建自我初始场景时：
    注册 / 读取 场景安全度 特征模板入口；
    直接 new 特征节点主信息类；
    写入 I64_MAX / 2 作为 当前值。
```

证据：

```text
场景类.cpp:108-124 直接创建 / 写入 场景安全度 当前值。
rg 场景安全度 结果显示该词仅在该旧初始化中作为“场景安全度”出现；当前安全评估链使用 当前场景安全度候选 / 当前场景评估安全值候选 等特征。
006 规范明确：安全不是场景事实，而是自我存在根控制特征。
060004 规范明确：场景影响候选和风险安全不得直接写自我安全值，安全值变化需要合法生产者和证据。
```

当前判断：

```text
该默认半刻度写入是旧默认值，不应迁入特征类。
本切片直接删除旧默认初始化路径，不新增替代默认值。
```

修改范围：

```text
场景类.cpp
说明书/函数功能说明文档.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
```

验证前预测：

```text
1. 场景类.cpp 不再出现 场景安全度 / 私有_确保场景默认特征已初始化 / 私有_查找子特征_按类型。
2. 删除私有函数后需重生成函数说明文档。
3. Debug x64 构建应通过。
```

## 2. 当前执行进展

已修改：

```text
1. 删除 场景类.cpp 中 <limits> 和 世界树类.h 依赖。
2. 删除 私有_查找子特征_按类型。
3. 删除 私有_确保场景默认特征已初始化。
4. 场景类::创建场景 改为只创建并返回场景节点，不再写入 场景安全度 默认特征。
```

已验证：

```text
rg "std::numeric_limits|<limits>|世界树类.h|私有_查找子特征_按类型|私有_确保场景默认特征已初始化|场景安全度" 场景类.cpp 场景类.h
结果：无命中。

python .\tools\生成函数功能说明文档.py
结果：exit=0，输出见 日志/20260620_场景默认安全特征清理_函数文档.log。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：exit=0，构建日志见 日志/20260620_场景默认安全特征清理_msbuild.log。
```

禁止宣称：

```text
不宣称安全评估链完成。
不宣称 P4 非场景主信息直接操作审查完成。
不宣称场景类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
