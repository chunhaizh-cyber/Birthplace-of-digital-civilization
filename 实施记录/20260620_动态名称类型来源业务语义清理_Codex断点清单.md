# 20260620 动态名称类型来源业务语义清理 Codex 断点清单

## 0. 恢复摘要

当前切片：按用户确认口径清理动态主信息字段定位。

执行口径：

```text
动态名称 = 动作 / 动态语义，如跑、跳、哭、笑、方法状态提交、状态变化。
动态类型 = 动态类别，至少区分主动动作、被动动作、无动作状态变化。
动态来源类型 = 删除候选，不再作为生成动态或因果生成的权威判断字段。
动态业务语义类型 = 删除候选，不再作为机器判断或显示缓存字段。
```

已读取依据：

```text
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
```

当前判断：

```text
来源类型 / 业务语义类型仍存在于 动态节点主信息类，并被动态类、因果类、控制面板和世界树显示层读取。
本切片应把因果生成类别收束为动态类功能函数，由来源动作、来源动作名、初始/结果状态等项目结构字段推导。
不保留来源类型 / 业务语义类型双写、缓存或兼容判断。
```

下一步：

```text
1. 删除动态主信息中的 来源类型 / 业务语义类型 及相关枚举。
2. 将动态名称/类型补齐改为名称写动作语义、类型写动态类别语素入口。
3. 因果类调用动态类功能函数取得因果生成类别，不直接读取旧动态来源/业务语义。
4. 更新函数说明文档，执行 Debug x64 构建。
```

## 1. 执行结果

已改文件：

```text
基础信息主信息类.h
动态类.h
动态类.cpp
因果类.cpp
二次特征应用模块.ixx
世界树类.cpp
控制面板类.cpp
方法虚拟存在服务类.cpp
自我线程模块.impl.cpp
说明书/动态类说明.md
说明书/函数功能说明文档.md
计划/20260620_动态类逻辑纠偏计划_v0.1.md
```

核心结果：

```text
1. 已删除 枚举_动态来源类型、枚举_动态业务语义类型、动态节点主信息类::来源类型、动态节点主信息类::业务语义类型。
2. 动态类新增/公开 推导因果生成类别、推导动态名称、推导动态类型、动态因果生成类别文本。
3. 动态类::读取动态因果生成输入 改为返回 枚举_动态因果生成类别。
4. 因果类不再推导或记录旧来源类型 / 业务语义类型，改用动态类返回的因果生成类别。
5. 治理动态写入处名称保留动作语义，类型改为动态类推导的被动动作类别入口。
6. 控制面板和世界树显示层不再显示旧来源类型 / 业务语义缓存。
```

验证：

```text
残留检查：
rg -n "枚举_动态来源类型|枚举_动态业务语义类型|业务语义类型|设置业务语义类型|推导业务语义类型|动态业务语义|动态主信息->来源类型|\.来源类型 = 枚举_动态" -g "*.h" -g "*.cpp" -g "*.ixx"
结果：无输出。

函数文档：
python .\tools\生成函数功能说明文档.py
结果：源码文件 128，函数条目 6874。

构建：
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
日志：日志/20260620_动态名称类型来源业务语义清理_msbuild.log
结果：已成功生成；0 个警告；0 个错误。
```

当前停止点：

```text
本动态字段清理切片已闭合。
返回原计划索引中的 因果类功能函数补完计划 P5。
```

禁止宣称：

```text
不宣称因果类功能补完完成。
不宣称动态类逻辑纠偏全部完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
