# S6-N2a-P2 自我外设组织方法壳实施记录

日期：2026-06-02

## 1. 目标

补齐三个自我外设组织方法壳：

```text
自我_识别外设观察材料
自我_扫描已识别区域
自我_跟踪指定存在
```

第一版只做：

```text
读取外设识别 / 扫描 / 跟踪提交包；
检查 TTL、generation、材料句柄可回查、特征值可比较；
输出组织结果场景和本轮运行虚拟存在；
记录方法动作动态。
```

## 2. 已改文件

```text
本能方法类.h
本能方法类.cpp
自我动作实现.外设模块.ixx
自我动作实现.外部模块.ixx
自我动作实现.内部模块.ixx
自我动作实现模块.ixx
计划/计划索引.md
计划/20260601_安全因果链反推当前场景特征值需求纠偏计划_v0.2.md
```

## 3. 代码结果

```text
新增本能方法 ID：
    自我_识别外设观察材料 = 6145
    自我_扫描已识别区域 = 6146
    自我_跟踪指定存在 = 6147

新增默认本能方法表项：
    识别外设观察材料
    扫描已识别区域
    跟踪指定存在

新增方法规格：
    确认自我识别外设观察材料规格
    确认自我扫描已识别区域规格
    确认自我跟踪指定存在规格

新增执行壳：
    自我识别外设观察材料
    自我扫描已识别区域
    自我跟踪指定存在

新增组织摘要特征：
    外设提交包ID
    外设提交包类型
    外设提交包组织状态
    外设提交包材料可回查状态
    外设提交包过期状态
    外设提交包generation明确状态
    外设提交包可比较状态
    外设提交包缺失材料数量
    外设提交包质量问题数量
    外设提交包材料句柄数量
    外设提交包特征值条目数量
    外设提交包候选事实数量
    外设识别结果场景状态
    外设扫描结果场景状态
    外设跟踪结果场景状态
```

三个方法统一走：

```text
输入报告 ID 或最新对应类型报告
-> 构造对应外设提交包
-> 检查材料和包头
-> 写入外设提交包组织结果
-> 写入输出结果场景 / 本轮运行虚拟存在
-> 创建方法动作动态
```

## 4. 边界

本切片明确没有做：

```text
不创建已确认观察存在；
不写当前场景存在集合明确；
不写需求满足；
不写安全值；
不把外设提交包当提交入口；
不在自我线程硬编码材料型 / 确认型 / 扫描型 / 跟踪型子需求表；
不恢复目标特征文本扫描。
```

## 5. 验证

预测：

```text
新增方法、规格、内部注册和外部包装后，Debug x64 应能编译通过；
本切片不做真实运行验证，因为扫描 / 跟踪正式提交入口仍未实现，业务链尚不能闭合。
```

实际：

```powershell
git diff --check
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

结果：

```text
git diff --check：0 空白错误，仅有 LF/CRLF 提示。
msbuild：成功，0 错误，1 个既有 LNK4075 警告。
```

## 6. 下一步

```text
S6-N2a-P3：
    新增 提交_观察存在特征值变化事实；
    新增 提交_指定存在跟踪事实；
    让扫描 / 跟踪组织结果经提交入口正式入账后，才能被安全 / 服务因果链读取。
```
