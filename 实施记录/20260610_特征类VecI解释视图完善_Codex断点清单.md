# 20260610_特征类VecI解释视图完善_Codex断点清单

## 当前切片

```text
计划：计划/20260610_特征类VecI解释视图完善计划_v0.1.md
切片：P0 规范口径冻结
状态：发现计划 / 规范兼容冲突，暂停进入代码实现。
```

## 依据文件

```text
AGENTS.md
规范/规范目录.md
计划/计划索引.md
计划/20260610_特征类VecI解释视图完善计划_v0.1.md
规范/特征类规范20260429.md
规范/观察像素簇与存在候选分层规范20260527.md
规范/相机外设综合工作流程规范20260607.md
规范/识别本能方法唯一性收敛规范20260607.md
```

## 已运行命令

```powershell
rg -n "平面轮廓|空间极值轮廓|VecIU64|I64 到 VecIU64|直接存入|编码规则|字典序|二次特征" 规范/特征类规范20260429.md 规范/观察像素簇与存在候选分层规范20260527.md 规范/相机外设综合工作流程规范20260607.md 规范/识别本能方法唯一性收敛规范20260607.md
rg -n "平面轮廓|空间极值轮廓|轮廓编码|边界点序列|写入特征_VecU|获取或创建句柄|VecIU64\{|边界点" -g "!x64/**" -g "!Debug/**" -g "!.git/**" -g "!日志/**" -g "!logs/**"
```

## 关键证据

```text
规范/观察像素簇与存在候选分层规范20260527.md:
    平面轮廓值类型为 VecIU64 / VECIU64。
    坐标为当前观察平面坐标。
    若使用全帧图像坐标且坐标非负，可以直接存入 VecIU64。
    若使用 ROI 局部坐标、相对坐标或可能为负的坐标，必须沿用既有 I64 到 VecIU64 的编码规则。
    VecIU64 本体不直接进入 I64 区间比较，识别 / 扫描 / 跟踪比较需生成二次特征。

规范/相机外设综合工作流程规范20260607.md:
    平面轮廓值类型为 VecIU64 / VECIU64。
    第一个二维坐标为轮廓中心点，后续二维坐标为按轮廓遍历顺序保存的边界点。
    平面轮廓匹配状态必须由量化二次特征推出。

计划/20260610_特征类VecI解释视图完善计划_v0.1.md:
    P3 当前写法为 平面轮廓 = VecIU64_ZigZag_I64_二维坐标链。
```

## 当前判断

```text
计划 P3 的“一律 ZigZag 解释平面轮廓”与现有 050004 的“全帧非负坐标可直接 VecIU64 存储”存在兼容冲突。

如果直接实现 P2/P3 的统一 ZigZag 读取：
    旧的非负直存平面轮廓中，奇数坐标会被解码成负数；
    识别 / 扫描 / 跟踪比较会误读历史轮廓；
    这属于数据解释规则冲突，不是普通代码实现问题。

当前不应继续进入编码工具和读取包装实现。
```

## 可选解决方向

```text
方向 A：兼容现有规范。
    保留全帧非负坐标直接 VecIU64 解释。
    新增 VecI64/ZigZag 只用于 ROI 局部坐标、相对坐标、可能为负坐标或新明确声明的有符号坐标类特征。
    P3 需改为“特征类型 + 坐标系 / 来源材料规则”共同决定是否 ZigZag。

方向 B：统一迁移为 ZigZag。
    修订 050004 / 040011 等规范，废止“非负坐标可直接存入 VecIU64”的口径。
    需要制定旧 VecIU64 轮廓迁移或版本识别规则。
    在迁移完成前不得让读取端一律 ZigZag 解码旧数据。
```

## 下一步

```text
等待确认采用方向 A 还是方向 B。
确认前不改 `特征值主信息类`，不新增 VecI64 底层字段，不拆轮廓子链，不进入 P1/P2 代码实现。
```

## 禁止宣称项

```text
不得宣称 P1/P2/P3 已实现。
不得宣称平面轮廓编码规则已统一。
不得宣称识别 / 扫描 / 跟踪比较链已通过。
不得宣称自我苏醒完成或初步成熟完成。
```
