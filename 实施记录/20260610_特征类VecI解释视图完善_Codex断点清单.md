# 20260610_特征类VecI解释视图完善_Codex断点清单

## 当前切片

```text
计划：计划/20260610_特征类VecI解释视图完善计划_v0.1.md
切片：P7/P8 识别 / 扫描 / 跟踪读取比较接入与验证断点
状态：P0-P6 已闭合；P7 已接入识别 / 当前帧分区 raw 簇 VecI64 轮廓评分，扫描 / 跟踪存在-存在轮廓比较和二次特征写入；P8 已完成差异检查、禁止字段扫描、Debug x64 构建和普通自检烟测。普通自检不负责获取 D455 识别 / 扫描 / 跟踪轮廓比较样本，真实业务命中待真实 D455 运行样本。
```

## 2026-06-11 任务失败状态纠偏：6150 外设提交输入等待

```text
当前切片：
    分析任务列表中失败的 `当前观察范围有效可观测单位计数事实取得状态` 与 `稳定对应存在数量明确状态`，纠正 6150 在缺正式报告 / 材料不可组织时把任务推到失败的状态映射。

本次修改：
    任务模块.管理工作线程.ixx。
    任务模块.管理界面线程.impl.cpp。
    将 `提交_当前观察范围可观测单位存在对应事实` 纳入外设观察供料等待类方法。
    将 6150 相关目标特征纳入外设观察供料等待特征集。
    当执行输出没有派生缺口、但当前任务目标特征本身属于这些 6150 等待特征时，回执改为 `方法前置条件未满足`，建议任务状态为 `等待中`，不再把自然材料未成熟压成终态失败。
    观察事实更新唤醒入口只对同类外设观察等待失败任务开放从 `失败` 到 `待重筹办` 的恢复；普通失败、取消、超时仍不放开。

已运行命令：
    git diff --check -- '任务模块.管理工作线程.ixx'
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> '日志/鱼巢_build_20260611_6150_wait_state.log'
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000 *> '日志/鱼巢_run_20260611_6150_wait_state_30s_console.log'
    git diff --check -- '任务模块.管理工作线程.ixx' '任务模块.管理界面线程.impl.cpp' '实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md'
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> '日志/鱼巢_build_20260611_6150_failed_recover.log'
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000 *> '日志/鱼巢_run_20260611_6150_failed_recover_30s_console.log'
    Stop-Process -Id 30740 -Force

关键退出码：
    git diff --check 退出码 0。
    Debug x64 构建退出码 0。
    真实自我线程 30 秒窗口退出码 0。
    第二轮 Debug x64 构建退出码 0。
    第二轮真实运行命令被工具超时中断，进程已手动停止；主日志已写入 11:41-11:42 窗口证据。

真实日志定位：
    日志/鱼巢_run_20260611.低值g line 20491-20496：6150 因 `来源报告ID=0`、`提交质量依据=缺外设观察报告` 失败，随后任务 C 被解释为 `动作执行失败`。
    日志/鱼巢_run_20260611.低值g line 20645-20650：任务 E 同一路径被解释为 `动作执行失败`。
    日志/鱼巢_run_20260611.低值g line 34945-35007：修正后任务 E 在 6150 缺报告时回 `等待外设观察供料`，`推进状况=方法前置条件未满足`，`实际任务状态=等待中`。
    日志/鱼巢_run_20260611.低值g line 35043-35054：修正后任务 F 同一路径回 `等待中`。
    日志/鱼巢_run_20260611.低值g line 35353、35561：历史已失败任务 C 仍保留失败状态，说明还需要观察事实更新唤醒入口允许同类外设等待失败恢复。
    日志/鱼巢_run_20260611.低值g line 39356：历史失败任务 C 在观察事实更新后从 `失败` 恢复到 `待重筹办`，`外设观察等待失败恢复=1`。
    日志/鱼巢_run_20260611.低值g line 40725：6150 已拿到 `来源报告ID=4` 且 `可进入提交入口组织=1`，但仍因缺扫描上下文 / 识别方案等条件未完成而 `成功=0`。
    日志/鱼巢_run_20260611.低值g line 40729-40733：任务 C 的 6150 执行失败被转为 `方法前置条件未满足`，`等待外设观察供料`，不生成阻塞子需求。
    日志/鱼巢_run_20260611.低值g line 42350、42654、42970：任务 C 后续重复进入 `实际任务状态=等待中`。
    日志/鱼巢_run_20260611.低值g line 43114-43127：任务 E 同一路径进入 `实际任务状态=等待中`。
    日志/鱼巢_run_20260611.低值g line 43127-43165：最终巡检窗口为等待 / 挂起 / 已结算，不再出现 C/E 因 6150 材料未成熟而进入失败。

构建预测：
    Debug x64 构建应通过。

真实运行预测：
    复跑后若 6150 仍缺正式报告或材料不可组织，对应任务应停在 `等待中`，日志应出现 `执行失败回执外设观察供料等待` 且 `等待特征=` 指向目标特征。
    若后续自然出现 `来源报告ID>0` 且 `可进入提交入口组织=1`，6150 才应继续写对应事实、计数事实和稳定数量明确状态。

禁止宣称：
    不把等待状态宣称为 6150 已成功。
    不把任务不再失败宣称为 3.13 通过。
    不伪造外设报告、等待项、动作动态或扫描 / 跟踪样本。
```

## P7 预测

```text
预计改动：
    自我动作实现.外设模块.ixx。
    识别 / 当前帧分区：外设簇可形成 VecI64 轮廓时，用 P5 轮廓比较评分参与已确认观察存在匹配；不为候选簇新造世界一阶轮廓。
    扫描 / 跟踪：当前存在与历史 / 基准存在两端已有平面轮廓或空间极值轮廓特征节点时，通过 `世界树.比较轮廓特征_按特征类型` 读取 VecI64 解释视图并写入轮廓比较二次特征。
预计验证：
    禁止字段扫描仍不命中 `特征值主信息类`。
    P7 关键符号扫描能命中识别 / 扫描 / 跟踪接入点。
    Debug x64 构建通过。
    普通自检窗口退出码 0 只证明路径可构建和无新增运行错误；真实 D455 轮廓样本必须来自真实 D455 运行，不能用普通自检替代。
禁止宣称：
    P7 未经真实 D455 样本命中前，不宣称真实识别 / 扫描 / 跟踪业务通过。
```

## 依据文件

```text
AGENTS.md
规范/规范目录.md
计划/计划索引.md
计划/20260610_特征类VecI解释视图完善计划_v0.1.md
规范/特征类规范20260429.md
规范/观察像素簇与存在候选分层规范20260527.md
规范/相机外设综合工作流程规范20260607.md
规范/识别本能方法唯一性收敛规范20260607.md
```

## 已运行命令

```powershell
rg -n "平面轮廓|空间极值轮廓|VecIU64|I64 到 VecIU64|直接存入|编码规则|字典序|二次特征" 规范/特征类规范20260429.md 规范/观察像素簇与存在候选分层规范20260527.md 规范/相机外设综合工作流程规范20260607.md 规范/识别本能方法唯一性收敛规范20260607.md
rg -n "平面轮廓|空间极值轮廓|轮廓编码|边界点序列|写入特征_VecU|获取或创建句柄|VecIU64\{|边界点" -g "!x64/**" -g "!Debug/**" -g "!.git/**" -g "!日志/**" -g "!logs/**"
rg -n "非负|直接存入|直接存|坐标非负|可以直接存入|一律 ZigZag|等待确认|暂停等待" 规范 计划 实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md
rg -n "编码I64为U64_ZigZag|解码U64为I64_ZigZag|编码VecI64为VecIU64_ZigZag|解码VecIU64为VecI64_ZigZag" 基础数据类型.h 基础数据类型.ixx
git diff --check -- 基础数据类型.h 基础数据类型.ixx 实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
rg -n "VecI64|std::variant|枚举_特征值编码类型|编码类型" 特征值主信息类.h
rg -n "获取或创建VecI64|写入特征_VecI64|读取特征VecI64" 特征值类.h 世界树类.h 世界树类.cpp
git diff --check -- 特征值类.h 世界树类.h 世界树类.cpp 计划/计划索引.md 计划/20260610_特征类VecI解释视图完善计划_v0.1.md 实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
rg -n "VecU解释规则_按特征类型|轮廓坐标维度_按特征类型|VecIU64_ZigZag_I64_二维坐标链|VecIU64_ZigZag_I64_三维坐标链" 特征类.h 特征类.cpp
git diff --check -- 特征类.h 特征类.cpp 实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
rg -n "校验坐标链VecI64|校验平面轮廓VecI64|校验空间极值轮廓VecI64|校验坐标类VecI64_按特征类型" 特征类.h 特征类.cpp
git diff --check -- 特征类.h 特征类.cpp 实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
rg -n "枚举_轮廓比较状态|结构_轮廓比较结果|比较坐标轮廓VecI64|比较平面轮廓VecI64|比较空间极值轮廓VecI64|私有_坐标点L1误差|私有_计算点链平均最大误差" 特征类.h 特征类.cpp
git diff --check -- 特征类.h 特征类.cpp 实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
rg -n "比较轮廓VecI64_按特征类型|比较平面轮廓VecI64|比较空间极值轮廓VecI64|维度不支持" 特征类.h 特征类.cpp
rg -n "VecI64|std::variant|枚举_特征值编码类型|编码类型|坐标维度|轮廓比较" 特征值主信息类.h
git diff --check -- 特征类.h 特征类.cpp 实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
rg -n "比较轮廓特征_按特征类型|比较轮廓VecI64_按特征类型|读取特征VecI64\\(" 世界树类.h 世界树类.cpp 特征类.h
rg -n "VecI64|std::variant|枚举_特征值编码类型|编码类型|坐标维度|轮廓比较" 特征值主信息类.h
git diff --check -- 世界树类.h 世界树类.cpp 特征类.h 实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
rg -n "写入轮廓比较二次特征|轮廓中心偏差|轮廓点数量差异|轮廓点平均误差|轮廓点最大误差|轮廓匹配评分|轮廓比较状态|轮廓比较失败原因|来源动态层级|比较结果\\.可比较" 二次特征应用模块.ixx
rg -n "VecI64|std::variant|枚举_特征值编码类型|编码类型|坐标维度|轮廓比较" 特征值主信息类.h
git diff --check -- 二次特征应用模块.ixx 实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
git diff --exit-code -- 特征值主信息类.h
rg -n "VecI64|std::variant|枚举_特征值编码类型|编码类型|坐标维度|轮廓比较" 特征值主信息类.h
rg -n "枚举_轮廓比较状态|格式非法|维度不支持|点数量不一致|中心L1误差|点链平均L1误差|点链最大L1误差|匹配评分Q10000|比较轮廓VecI64_按特征类型|比较轮廓特征_按特征类型|写入轮廓比较二次特征" 特征类.h 特征类.cpp 世界树类.h 世界树类.cpp 二次特征应用模块.ixx
git diff --check -- 实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

## 关键证据

```text
修订前 规范/观察像素簇与存在候选分层规范20260527.md:
    平面轮廓值类型为 VecIU64 / VECIU64。
    坐标为当前观察平面坐标。
    当时允许全帧图像非负坐标直接存入 VecIU64。
    ROI 局部坐标、相对坐标或可能为负的坐标沿用既有 I64 到 VecIU64 的编码规则。
    VecIU64 本体不直接进入 I64 区间比较，识别 / 扫描 / 跟踪比较需生成二次特征。

规范/相机外设综合工作流程规范20260607.md:
    平面轮廓值类型为 VecIU64 / VECIU64。
    第一个二维坐标为轮廓中心点，后续二维坐标为按轮廓遍历顺序保存的边界点。
    平面轮廓匹配状态必须由量化二次特征推出。

计划/20260610_特征类VecI解释视图完善计划_v0.1.md:
    P3 当前写法为 平面轮廓 = VecIU64_ZigZag_I64_二维坐标链。
```

## 当前判断

```text
历史判断：
    计划 P3 的“一律 ZigZag 解释平面轮廓”与原 050004 的“全帧非负坐标可直接 VecIU64 存储”存在兼容冲突。

已按用户确认口径修订：
    规范/特征类规范20260429.md
    规范/抽象特征规范20260518.md
    规范/观察像素簇与存在候选分层规范20260527.md
    规范/相机外设综合工作流程规范20260607.md

当前口径：
    读取特征值类信息时，根据特征类型决定是否需要转换以及如何转换。
    平面轮廓按 VecIU64_ZigZag_I64_二维坐标链解释。
    空间极值轮廓按 VecIU64_ZigZag_I64_三维坐标链解释。
    类型信息不进入 `特征值主信息类`。
    `特征值主信息类::比较` 只用于去重、排序、快速查找和绝对相同；轮廓相似、扫描变化、跟踪连续性、识别同一性由特征类型专用比较函数生成二次特征。

P0 阻塞已解除，可进入 P1 编码工具和读取包装实现。
```

## P1 预测结果

```text
新增函数只落在：
    基础数据类型.h
    基础数据类型.ixx

预期：
    0 -> 0
    -1 -> 1
    1 -> 2
    -2 -> 3
    2 -> 4
    I64_MIN / I64_MAX 可往返

构建期 static_assert 应覆盖上述标量映射和极值往返。

不应发生：
    修改 `特征值主信息类` 字段；
    新增 `特征值主信息类::VecI64`；
    新增编码类型字段；
    拆轮廓子链；
    让 VecI64 变成新的底层证据池。
```

## P1 已改文件

```text
基础数据类型.h
基础数据类型.ixx
计划/计划索引.md
计划/20260610_特征类VecI解释视图完善计划_v0.1.md
实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md
```

## P1 验证结果

```text
rg 工具函数落点：
    基础数据类型.h / 基础数据类型.ixx 均命中 ZigZag 标量函数、Vec 包装函数和 static_assert。

禁止字段扫描：
    `rg -n "VecI64|std::variant|枚举_特征值编码类型|编码类型" 特征值主信息类.h`
    退出码 1，无命中，表示 `特征值主信息类` 未新增 VecI64 / variant / 编码类型字段。

git diff --check：
    退出码 0。

Debug x64 构建：
    命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码：0。
    构建日志：日志/鱼巢_build_20260610_VecI_P1.log。
```

## P2 预测结果

```text
新增入口：
    特征值类::获取或创建VecI64特征值节点
    特征值类::获取或创建VecI64句柄
    世界树类::写入特征_VecI64(特征节点类*, ...)
    世界树类::写入特征_VecI64(基础信息节点类*, 特征类型, ...)
    世界树类::读取特征VecI64(特征节点类*, ...)
    世界树类::读取特征VecI64(宿主, 特征类型, ...)

预期：
    写入 VecI64 时先调用 P1 的 VecI64 -> VecIU64 编码，再进入现有 VecIU64 证据池。
    读取 VecI64 时只通过现有 VecU句柄只读指针取得原始 VecIU64，再解码为 VecI64 输出。
    不复制或修改证据池原值。

不应发生：
    修改 `特征值主信息类` 字段；
    新增 VecI64 底层值池；
    新增编码类型字段；
    绕开 `写入特征_VecU` / `读取特征VecU` 的新裸写路径；
    拆轮廓子链。
```

## P2 已改文件

```text
特征值类.h
世界树类.h
世界树类.cpp
实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md
```

## P2 验证结果

```text
rg 包装入口落点：
    特征值类.h 命中 `获取或创建VecI64特征值节点`、`获取或创建VecI64句柄`。
    世界树类.h / 世界树类.cpp 命中 `写入特征_VecI64`、`读取特征VecI64`。

禁止字段扫描：
    `rg -n "VecI64|std::variant|枚举_特征值编码类型|编码类型" 特征值主信息类.h`
    退出码 1，无命中，表示 `特征值主信息类` 未新增 VecI64 / variant / 编码类型字段。

git diff --check：
    退出码 0。

Debug x64 构建：
    命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码：0。
    构建日志：日志/鱼巢_build_20260610_VecI_P2.log。
```

## P3 预测结果

```text
新增入口：
    枚举_VecU解释规则
    结构_VecU特征解释规则
    特征类::VecU解释规则_按特征类型
    特征类::轮廓坐标维度_按特征类型

预期：
    特征类型 == 平面轮廓：
        规则 = VecIU64_ZigZag_I64_二维坐标链
        坐标维度 = 2
        需要ZigZag解码 = true

    特征类型 == 空间极值轮廓：
        规则 = VecIU64_ZigZag_I64_三维坐标链
        坐标维度 = 3
        需要ZigZag解码 = true

    其他特征类型：
        规则 = 未定义
        坐标维度无值

不应发生：
    修改 `特征值主信息类` 字段；
    在值节点写入编码类型或维度字段；
    拆轮廓子链；
    把颜色摘要、掩码等未知类型猜成坐标链。
```

## P3 已改文件

```text
特征类.h
特征类.cpp
实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md
```

## P3 验证结果

```text
rg 解释规则落点：
    特征类.h / 特征类.cpp 命中 `VecU解释规则_按特征类型`、`轮廓坐标维度_按特征类型`、二维 / 三维 ZigZag 坐标链枚举值。

禁止字段扫描：
    `rg -n "VecI64|std::variant|枚举_特征值编码类型|编码类型|坐标维度" 特征值主信息类.h`
    退出码 1，无命中，表示 `特征值主信息类` 未新增 VecI64 / variant / 编码类型 / 坐标维度字段。

git diff --check：
    退出码 0。

Debug x64 构建：
    命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码：0。
    构建日志：日志/鱼巢_build_20260610_VecI_P3.log。
```

## P4 预测结果

```text
新增入口：
    特征类::校验坐标链VecI64
    特征类::校验平面轮廓VecI64
    特征类::校验空间极值轮廓VecI64
    特征类::校验坐标类VecI64_按特征类型

预期：
    平面轮廓：
        长度 >= 2
        长度 % 2 == 0

    空间极值轮廓：
        长度 >= 3
        长度 % 3 == 0

    按特征类型路由：
        平面轮廓 -> 二维校验
        空间极值轮廓 -> 三维校验
        未知特征类型 -> false

不应发生：
    截断尾值；
    补 0；
    静默忽略非法长度；
    修改 `特征值主信息类`；
    拆轮廓子链。
```

## P4 已改文件

```text
特征类.h
特征类.cpp
实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md
```

## P4 验证结果

```text
rg 校验函数落点：
    特征类.h / 特征类.cpp 命中 `校验坐标链VecI64`、`校验平面轮廓VecI64`、`校验空间极值轮廓VecI64`、`校验坐标类VecI64_按特征类型`。

禁止字段扫描：
    `rg -n "VecI64|std::variant|枚举_特征值编码类型|编码类型|坐标维度" 特征值主信息类.h`
    退出码 1，无命中，表示 `特征值主信息类` 未新增 VecI64 / variant / 编码类型 / 坐标维度字段。

git diff --check：
    退出码 0。

Debug x64 构建：
    命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码：0。
    构建日志：日志/鱼巢_build_20260610_VecI_P4.log。
```

## P5a-P5d 预测结果

```text
新增入口：
    枚举_轮廓比较状态
    结构_轮廓比较结果
    特征类::比较坐标轮廓VecI64
    特征类::比较平面轮廓VecI64
    特征类::比较空间极值轮廓VecI64

预期：
    坐标维度不是 2 / 3：
        状态 = 维度不支持

    任一向量格式不满足维度规则：
        状态 = 格式非法

    点数量不同：
        状态 = 点数量不一致
        写出左点数量、右点数量、点数量差异

    同维度、同点数：
        状态 = 可比较
        第一个点计算中心L1误差
        后续点按顺序计算点链平均L1误差和点链最大L1误差
        匹配评分Q10000 完全相同为 10000，误差越大越低

不应发生：
    点重采样；
    旋转、缩放、平移归一；
    掩码重合率计算；
    写二次特征；
    修改 `特征值主信息类::比较`；
    修改输入向量。
```

## P5a-P5d 已改文件

```text
特征类.h
特征类.cpp
实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md
```

## P5a-P5d 验证结果

```text
rg 比较入口落点：
    特征类.h / 特征类.cpp 命中 `枚举_轮廓比较状态`、`结构_轮廓比较结果`、`比较坐标轮廓VecI64`、`比较平面轮廓VecI64`、`比较空间极值轮廓VecI64`、`私有_坐标点L1误差`、`私有_计算点链平均最大误差`。

禁止字段扫描：
    `rg -n "VecI64|std::variant|枚举_特征值编码类型|编码类型|坐标维度|轮廓比较" 特征值主信息类.h`
    退出码 1，无命中，表示 `特征值主信息类` 未新增 VecI64 / variant / 编码类型 / 坐标维度 / 轮廓比较字段。

git diff --check：
    退出码 0。

Debug x64 构建：
    命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码：0。
    构建日志：日志/鱼巢_build_20260610_VecI_P5ad.log。
```

## P5e 预测结果

```text
新增入口：
    特征类::比较轮廓VecI64_按特征类型

预期：
    特征类型 == 平面轮廓：
        调用二维平面轮廓比较。

    特征类型 == 空间极值轮廓：
        调用三维空间极值轮廓比较。

    其他特征类型：
        状态 = 维度不支持。
        不猜测坐标维度。

不应发生：
    向 `特征值主信息类` 或值节点写入编码类型 / 维度字段；
    修改 `特征值主信息类::比较`；
    修改输入 VecI64；
    写二次特征。
```

## P5e 已改文件

```text
特征类.h
特征类.cpp
实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md
计划/计划索引.md
```

## P5e 验证结果

```text
rg 路由比较入口落点：
    特征类.h / 特征类.cpp 命中 `比较轮廓VecI64_按特征类型`，并命中平面 / 空间显式比较分派。

禁止字段扫描：
    `rg -n "VecI64|std::variant|枚举_特征值编码类型|编码类型|坐标维度|轮廓比较" 特征值主信息类.h`
    退出码 1，无命中，表示 `特征值主信息类` 未新增 VecI64 / variant / 编码类型 / 坐标维度 / 轮廓比较字段。

git diff --check：
    退出码 0。

Debug x64 构建：
    命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码：0。
    构建日志：日志/鱼巢_build_20260611_VecI_P5e.log。
```

## P5f 预测结果

```text
新增入口：
    世界树类::比较轮廓特征_按特征类型

预期：
    特征类型无轮廓维度：
        返回 P5e 的维度不支持结果。

    左右任一特征不能通过 `读取特征VecI64` 读取：
        状态 = 格式非法。

    左右特征均可读取：
        调用 `特征类::比较轮廓VecI64_按特征类型` 返回运行期比较结果。

不应发生：
    直接操作 VecU句柄或 VecIU64 原始值；
    读取失败时把缺值当 0 误差；
    写二次特征；
    修改 `特征值主信息类::比较`。
```

## P5f 已改文件

```text
世界树类.h
世界树类.cpp
特征类.h
实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md
计划/计划索引.md
```

## P5f 验证结果

```text
rg 特征节点比较入口落点：
    世界树类.h / 世界树类.cpp 命中 `比较轮廓特征_按特征类型`，并命中 `读取特征VecI64` 和 `比较轮廓VecI64_按特征类型` 调用。

禁止字段扫描：
    `rg -n "VecI64|std::variant|枚举_特征值编码类型|编码类型|坐标维度|轮廓比较" 特征值主信息类.h`
    退出码 1，无命中，表示 `特征值主信息类` 未新增 VecI64 / variant / 编码类型 / 坐标维度 / 轮廓比较字段。

git diff --check：
    退出码 0。

Debug x64 构建：
    命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码：0。
    构建日志：日志/鱼巢_build_20260611_VecI_P5f.log。
```

## P5g 预测结果

```text
新增入口：
    二次特征生成模块::写入轮廓比较二次特征

预期：
    可比较结果写入：
        轮廓中心偏差；
        轮廓点数量差异；
        轮廓点平均误差；
        轮廓点最大误差；
        轮廓匹配评分。

    不可比较结果写入：
        轮廓比较状态；
        轮廓比较失败原因；
        点数量差异，如果结果中已有点数量信息。

    每个二次特征绑定：
        左右特征；
        来源场景；
        可选来源动态及其层级 / 路径签名。

不应发生：
    写一阶轮廓特征；
    直接宣称存在同一、扫描变化成立、跟踪连续或需求满足；
    新增平行因果账本；
    修改 `特征值主信息类::比较`。
```

## P5g 已改文件

```text
二次特征应用模块.ixx
实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md
计划/计划索引.md
```

## P5g 验证结果

```text
rg 二次特征写入适配落点：
    二次特征应用模块.ixx 命中 `写入轮廓比较二次特征`、五个可比较指标、不可比较状态 / 失败原因、来源动态层级和 `比较结果.可比较` 分支。

禁止字段扫描：
    `rg -n "VecI64|std::variant|枚举_特征值编码类型|编码类型|坐标维度|轮廓比较" 特征值主信息类.h`
    退出码 1，无命中，表示 `特征值主信息类` 未新增 VecI64 / variant / 编码类型 / 坐标维度 / 轮廓比较字段。

git diff --check：
    退出码 0。

Debug x64 构建：
    命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码：0。
    构建日志：日志/鱼巢_build_20260611_VecI_P5g.log。
```

## P5h 预测结果

```text
预期：
    `特征值主信息类::比较` 未改变。
    `特征值主信息类` 无 VecI64 / 编码类型 / 坐标维度 / 轮廓比较字段。
    P5 代码入口覆盖：
        不可比较 / 格式非法 / 维度不支持 / 点数量不一致；
        中心偏差 / 平均误差 / 最大误差 / 匹配评分；
        按特征类型路由；
        从特征节点读取后比较；
        二次特征写入适配。
    Debug x64 构建通过。

不应发生：
    使用模拟样本、夹具入口、伪造动作动态或 `--*-check` 回归命令；
    宣称识别 / 扫描 / 跟踪真实业务通过。
```

## P5h 已改文件

```text
实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md
计划/计划索引.md
```

## P5h 验证结果

```text
`特征值主信息类::比较` / 主信息字段核对：
    `git diff --exit-code -- 特征值主信息类.h`
    退出码 0，表示 `特征值主信息类.h` 未修改。

禁止字段扫描：
    `rg -n "VecI64|std::variant|枚举_特征值编码类型|编码类型|坐标维度|轮廓比较" 特征值主信息类.h`
    退出码 1，无命中，表示 `特征值主信息类` 无 VecI64 / variant / 编码类型 / 坐标维度 / 轮廓比较字段。

P5 覆盖项扫描：
    命中 `枚举_轮廓比较状态`、`格式非法`、`维度不支持`、`点数量不一致`、`中心L1误差`、`点链平均L1误差`、`点链最大L1误差`、`匹配评分Q10000`、`比较轮廓VecI64_按特征类型`、`比较轮廓特征_按特征类型`、`写入轮廓比较二次特征`。

git diff --check：
    退出码 0。

Debug x64 构建：
    命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码：0。
    构建日志：日志/鱼巢_build_20260611_VecI_P5h.log。

业务验收边界：
    本切片没有真实轮廓自然样本；只能证明函数路径和构建通过，不能宣称识别 / 扫描 / 跟踪真实业务通过。
```

## P6 预测结果

```text
新增入口：
    自我动作实现.外设模块.ixx：
        特征_平面轮廓 / 特征_空间极值轮廓 及对应状态特征。
        写入基础节点VecI64轮廓。
        构造观察簇平面轮廓VecI64。
        构造观察簇空间极值轮廓VecI64。
        写入观察簇VecI64轮廓。

预期：
    已验证 D455 观察像素簇写入：
        平面轮廓 = 中心二维坐标 + 投影 AABB 四角，按 VecI64 二维坐标链校验后写入 VecIU64 值池。
        空间极值轮廓 = 空间中心 + 三维 AABB 八角，按 VecI64 三维坐标链校验后写入 VecIU64 值池。
    写入前调用 P4 的 `特征类::校验坐标类VecI64_按特征类型`。
    写入失败记录错误日志，并把本轮观察候选验证结果转为失败，不静默保留半截轮廓。

不应发生：
    修改 `特征值主信息类` 字段。
    新增 VecI64 底层值池、编码类型字段或维度字段。
    把外设内部轮廓直接宣称为世界存在真值。
    将识别 / 扫描 / 跟踪比较结果写成一阶轮廓特征。
```

## P6 已改文件

```text
自我动作实现.外设模块.ixx
实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md
```

## P6 真实运行预测

```text
命令：
    x64/Debug/鱼巢.exe --self-check-log --self-check-log-wait-ms=1000

预期：
    退出码为 0。
    普通自检窗口不承担真实 D455 已验证观察像素簇取样；本轮不以 `外设观察/VecI64轮廓写入汇总` 作为行为验收要求。
    普通自检只能证明程序运行窗口不崩溃，P6 行为命中仍待真实 D455 样本。

不应发生：
    弹错或崩溃。
    把未命中轮廓样本宣称为观察链业务通过。
```

## P6 验证结果

```text
rg 接入口扫描：
    `自我动作实现.外设模块.ixx` 命中 `平面轮廓`、`空间极值轮廓`、`写入基础节点VecI64轮廓`、`构造观察簇平面轮廓VecI64`、`构造观察簇空间极值轮廓VecI64`、`写入观察簇VecI64轮廓`、`校验坐标类VecI64_按特征类型`、`写入特征_VecI64`。

禁止字段扫描：
    `rg -n "VecI64|std::variant|枚举_特征值编码类型|编码类型|坐标维度|轮廓比较" 特征值主信息类.h`
    退出码 1，无命中，表示 `特征值主信息类` 未新增 VecI64 / variant / 编码类型 / 坐标维度 / 轮廓比较字段。

git diff --check：
    退出码 0。

Debug x64 构建：
    命令：msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码：0。
    构建日志：日志/鱼巢_build_20260611_VecI_P6.log。

普通自检烟测：
    命令：x64/Debug/鱼巢.exe --self-check-log --self-check-log-wait-ms=1000
    退出码：0。
    运行日志：日志/鱼巢_run_20260611_VecI_P6_selfcheck.log。
    `外设观察/VecI64轮廓写入汇总` 扫描退出码 1，无命中；该命令不负责获取真实 D455 已验证观察像素簇。
    错误 / 崩溃 / 异常关键字扫描退出码 1，无命中。

当前判断：
    P6 代码接入、静态验证、构建和普通自检烟测已闭合。
    P6 真实轮廓写入命中仍待真实 D455 已验证簇自然样本，不宣称观察链业务通过。
```

## 2026-06-11 P7/P8 执行记录

```text
已改文件：
    自我动作实现.外设模块.ixx
    计划/计划索引.md
    计划/20260610_特征类VecI解释视图完善计划_v0.1.md
    实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md

实现结果：
    识别 / 当前帧分区：外设簇可形成平面轮廓或空间极值轮廓 VecI64 时，用 P5 轮廓比较评分参与已确认观察存在匹配；不为候选簇新造世界一阶轮廓。
    识别：输入场景已给出当前观察存在且确认方案命中已知存在时，写当前观察存在与目标已知存在的轮廓比较二次特征。
    扫描：当前存在与历史存在两端已有平面轮廓或空间极值轮廓特征节点时，用 `世界树.比较轮廓特征_按特征类型` 生成轮廓变化量并在动作动态形成后写二次特征。
    跟踪：目标存在与上一轮 / 基准观察存在两端已有轮廓特征节点时，用 P5 比较结果生成轮廓匹配分数、轮廓残差、预测误差、迁移和丢失 / 重捕获候选材料，并写二次特征。

已运行命令：
    rg -n "VecI64|std::variant|枚举_特征值编码类型|编码类型|坐标维度|轮廓比较" 特征值主信息类.h
    git diff --check -- 自我动作实现.外设模块.ixx 实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    x64/Debug/鱼巢.exe --self-check-log --self-check-log-wait-ms=1000

关键退出码：
    禁止字段扫描退出码 1，无命中。
    git diff --check 退出码 0。
    Debug x64 构建退出码 0，构建日志 `日志/鱼巢_build_20260611_VecI_P7.log`，0 个错误。
    普通自检烟测退出码 0，运行日志 `日志/鱼巢_run_20260611_VecI_P7_selfcheck.log`。

关键日志证据：
    `VecI64轮廓比较特征数` / `VecI64轮廓二次特征写入数` 扫描退出码 1，无命中；该命令不负责获取真实 D455 可比轮廓样本。
    错误 / 异常 / 崩溃 / `轮廓比较二次特征写入失败` 扫描退出码 1，无命中。

当前判断：
    P7 代码接入和 P8 基础验证已闭合。
    真实 D455 识别 / 扫描 / 跟踪轮廓比较行为命中仍待自然样本，不宣称业务通过。
```

## 已定解决方向

```text
底层值容器仍保持 `特征值主信息类::VecIU64`。
VecI64 只作为读取、创建、校验、比较时的解释视图。
特征类型定义负责声明 VecIU64 的解释规则，不在特征值主信息层新增 VecI64 字段、业务类型字段或编码类型字段。
```

## 下一步

```text
完成 P1：
    已完成差异检查、禁止字段扫描和 Debug x64 构建。

完成 P2：
    已完成差异检查、禁止字段扫描和 Debug x64 构建。

完成 P3：
    已完成差异检查、禁止字段扫描和 Debug x64 构建。

完成 P4：
    已完成差异检查、禁止字段扫描和 Debug x64 构建。

完成 P5a-P5d：
    已完成差异检查、禁止字段扫描和 Debug x64 构建。

P5a-P5d 完成后进入 P5e：
    已完成差异检查、禁止字段扫描和 Debug x64 构建。

P5e 完成后进入 P5f：
    已完成差异检查、禁止字段扫描和 Debug x64 构建。

P5f 完成后进入 P5g：
    已完成差异检查、禁止字段扫描和 Debug x64 构建。

P5g 完成后进入 P5h：
    已完成禁止字段扫描、P5 覆盖项扫描和 Debug x64 构建。

P5h 完成后进入 P6：
    已完成观察写入接入、差异检查、禁止字段扫描、Debug x64 构建和普通自检烟测。

P6 完成后进入 P7：
    已完成识别 / 扫描 / 跟踪读取和比较接入，并通过基础验证。

P7/P8 后续：
    待真实 D455 样本自然命中后，再复核识别 / 扫描 / 跟踪轮廓比较二次特征写入日志。
```

## 禁止宣称项

```text
不得把 P6/P7 未命中真实 D455 轮廓样本宣称为观察链业务通过。
不得把 VecI 解释视图、校验或比较函数完成宣称为观察链业务通过。
不得宣称识别 / 扫描 / 跟踪真实业务链已通过。
不得宣称自我苏醒完成或初步成熟完成。
```

## 2026-06-11 真实 D455 样本复核运行前预测

```text
当前切片：
    复核 3.13 特征类 VecI 识别 / 扫描 / 跟踪真实轮廓样本。

依据文件：
    计划/计划索引.md
    规范/观察像素簇与存在候选分层规范20260527.md
    规范/相机外设综合工作流程规范20260607.md

运行前状态：
    当前没有正在运行的 鱼巢.exe。
    使用既有 Debug x64 可执行文件 x64/Debug/鱼巢.exe。
    工作树已有 AGENTS.md、任务模块.管理工作线程.ixx、控制面板.html 脏文件，本切片不改动这些文件。

预测：
    --d455-status 用于确认真实 D455 运行时 / 设备路径条件；若失败，阻塞归类为本机硬件或运行时条件不足。
    --d455-capture-once 只应打开真实 D455、采集真实帧、输出材料包 / 逐簇识别样本卡 / 单帧扫描样本卡；它不应伪造动作动态，不应写世界一阶事实，不应单独宣称 3.13 通过。
    --self-check-log --self-check-log-wait-ms=30000 只作为正式自我线程自然运行窗口；若自然形成识别 / 扫描 / 跟踪轮廓可比较样本，日志应命中外设观察/VecI64轮廓写入汇总、VecI64轮廓比较特征数、VecI64轮廓可比较数、VecI64轮廓二次特征写入数、轮廓匹配分数或轮廓残差。
    若只出现 D455 材料包、识别样本卡或扫描变化样本卡，而没有 VecI64 轮廓写入 / 比较日志，则 3.13 仍未解除，下一步定位缺的是观察入账、第二比较端点还是跟踪目标上下文。

禁止宣称：
    不把单帧真实采集写成可比较轮廓样本通过。
    不把普通自检烟测写成真实 D455 样本验收。
    不伪造外设报告、样本、动作动态或等待项。
```

## 2026-06-11 真实 D455 样本复核运行结果

```text
已运行命令：
    .\x64\Debug\鱼巢.exe --d455-status
    .\x64\Debug\鱼巢.exe --d455-capture-once
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000

关键退出码：
    --d455-status 退出码 0。
    --d455-capture-once 退出码 0。
    --self-check-log --self-check-log-wait-ms=30000 退出码 0。

日志文件：
    日志/鱼巢_run_20260611_D455_status.txt
    日志/鱼巢_run_20260611_D455_capture_once.log
    日志/鱼巢_run_20260611_D455_selfcheck_30s_console.log
    日志/鱼巢_run_20260611.低值g

真实 D455 条件：
    --d455-status 显示 状态成功，相机当前未打开。
    --d455-capture-once 真实采集成功，640x480，深度帧号=3，彩色帧号=9，观察簇=20，材料句柄总数=146。
    单帧扫描变化样本卡状态=首帧基准，上一帧=0，不构成 3.13 可比较样本。

30 秒正式自我线程窗口证据：
    D455 线程自然启动并提交逐簇识别报告。
    最新窗口 D455 线程停止摘要：样本=23，可比样本=23，稳定连接=22，综合稳定=稳定。
    外设观察/VecI64轮廓写入汇总：报告ID=1，候选簇数量=24，平面轮廓写入成功数量=24，空间极值轮廓写入成功数量=24，轮廓写入失败数量=0。
    外设观察像素簇候选验证结果：报告ID=1，成功=1，通过簇数量=24，观察材料等级=优质。
    自我_识别外设观察材料：报告ID=2，成功=1，新建=20，可提交=20，观察材料等级=优质，扫描可启动状态=1。

未满足 3.13 的证据：
    同一识别结果中 当前识别观察存在 为空。
    VecI64轮廓比较特征数=0。
    VecI64轮廓可比较数=0。
    VecI64轮廓二次特征写入数=0。
    最新窗口未自然出现 已识别区域扫描正式结果 或 指定存在跟踪正式结果。
    当前观察范围可观测单位存在对应事实提交在报告可用前多次 来源报告ID=0 / 缺报告失败快退，报告可用后未观察到重新提交成功日志。

当前判断：
    缺口已从“缺真实 D455 材料”收窄为“缺可比较两端 / 报告可用后的提交与扫描跟踪目标上下文承接”。
    真实硬件、报告、观察簇验证和 VecI64 轮廓写入不是当前阻塞。
    3.13 仍未通过，不宣称识别 / 扫描 / 跟踪真实轮廓比较行为验收完成。

下一步：
    只读追踪自我_识别外设观察材料成功后，当前观察存在 / 已知存在比较端点、提交_当前观察范围可观测单位存在对应事实重唤醒、稳定子集扫描目标上下文和跟踪目标上下文之间的断点。
```

## 2026-06-11 继续执行：识别后承接链验证预测

```text
当前切片：
    复核现有未提交补丁是否能把外设观察供料缺口保留为等待 / 唤醒路径，并验证报告可用后 6150 提交入口、稳定子集扫描目标上下文和后续扫描等待项是否自然承接。

已观察到的代码改动范围：
    任务模块.管理工作线程.ixx 已新增外设观察供料等待特征识别、外设观察本能方法识别、供料等待替代派生需求日志和执行失败回执等待中处理。

预测：
    git diff --check 不应报空白错误。
    Debug x64 构建应通过。
    真实自我线程窗口若自然走到外设观察供料缺口，应出现 `任务管理工作线程/外设观察供料等待替代派生需求` 或 `执行失败回执外设观察供料等待`，且不再把外设供料字段误生成普通阻塞子需求。
    若报告可用后 6150 被重新派发，应出现 `当前观察范围可观测单位存在对应事实提交` 且 `来源报告ID>0`；成功时应进一步出现稳定子集扫描目标上下文字段。
    若稳定子集扫描目标上下文写入成功，任务管理界面线程应有机会生成 `稳定子集全量扫描等待项承接` 日志。

禁止宣称：
    不把构建通过宣称为 3.13 通过。
    不把外设等待状态宣称为识别 / 扫描 / 跟踪轮廓比较完成。
    不伪造外设报告、等待项、动作动态或扫描 / 跟踪样本。
```

## 2026-06-11 继续执行：识别后承接链验证结果

```text
本次修改：
    任务模块.管理工作线程.ixx。
    外设观察供料缺口继续保留为等待 / 唤醒路径，不生成普通阻塞子需求。
    逐簇识别报告只有 `可进入提交入口组织=1` 才生成观察事实承接请求；可用但未优质的逐簇识别报告只生成任务回执并保留等待项。

已运行命令：
    git diff --check -- '任务模块.管理工作线程.ixx' '实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md' '计划/计划索引.md'
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> '日志/鱼巢_build_20260611_VecI_resume_quality_gate.log'
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000 *> '日志/鱼巢_run_20260611_VecI_resume_quality_gate_30s_console.log'

关键退出码：
    git diff --check 退出码 0。
    Debug x64 构建退出码 0。
    真实自我线程 30 秒窗口退出码 0。

构建证据：
    日志/鱼巢_build_20260611_VecI_resume_quality_gate.log line 31-33：已成功生成，0 个警告，0 个错误。

等待 / 派生需求边界证据：
    日志/鱼巢_run_20260611.低值g line 9036-9039：自我_验证外设观察像素簇候选失败时记录外设观察供料等待，建议任务状态=等待中，需要自我线程=0，需要子需求=0。
    日志/鱼巢_run_20260611.低值g line 9152-9160：自我_识别外设观察材料失败时记录外设观察供料等待，直接写需求树=0，直接写世界树=0。

逐簇识别质量门控证据：
    日志/鱼巢_run_20260611.低值g line 9708-9922：报告ID=17-25 均为可用但未优质，`可进入提交入口组织=0`；等待项只生成任务回执，`观察承接请求=0`，`上行消息数=0`，摘要为保留等待项等待优质识别材料。

未满足 3.13 的证据：
    本轮 10:37 窗口未自然出现 `可进入提交入口组织=1` 的逐簇识别报告。
    未观察到 `当前观察范围可观测单位存在对应事实提交` 以 `来源报告ID>0` 成功重派发。
    未观察到稳定子集扫描目标上下文写入、`已识别区域扫描正式结果` 或 `指定存在跟踪正式结果`。

当前判断：
    本次代码切片已闭合。
    当前阻塞不是构建错误或外设等待边界错误，而是最新真实运行窗口未自然命中优质逐簇识别材料。
    3.13 仍未通过，不宣称识别 / 扫描 / 跟踪真实轮廓比较行为验收完成。

下一步：
    等待或复跑真实 D455，自然命中 `可进入提交入口组织=1` 的逐簇识别报告后，再核对 6150 来源报告ID、稳定子集扫描目标上下文和后续扫描 / 跟踪正式结果。
```

## 2026-06-11 用户校正后：6145 质量门简化运行前预测

```text
当前切片：
    按用户校正简化 6145 入门前置：保留 `判断外设观察材料质量` 作为唯一质量门，保留确认方案内的识别证据门；不再把稳定复现 / 观察完成 / 可复验作为进入 6145 前的第二质量门。

本次修正口径：
    任务管理逐簇识别报告供料恢复为 `可进入识别方法=1` 即可生成观察事实承接请求草案。
    6145 在 `可进入识别方法=1` 时运行并构造确认方案。
    `构造外设观察存在确认方案` 内部记录上游未验证、材料未达提交质量、缺单簇材料、缺跨帧同一性等证据不足原因。
    6145 只有在确认方案存在可提交项时才把动作记为成功；证据不足只输出结构化数量和原因，不写成识别完成或允许提交。

已运行命令：
    git diff --check -- '任务模块.管理工作线程.ixx' '自我动作实现.外设模块.ixx'
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> '日志/鱼巢_build_20260611_6145_quality_gate_simplify.log'

关键退出码：
    git diff --check 退出码 0。
    Debug x64 构建退出码 0。

构建证据：
    日志/鱼巢_build_20260611_6145_quality_gate_simplify.log line 121：已成功生成。
    line 127-128：1 个 LNK4075 链接提示，0 个错误。

真实运行预测：
    若真实 D455 自然产生可用逐簇识别报告，任务管理应再次生成观察事实承接请求，不能因为 `可进入提交入口组织=0` 卡住 6145 入口。
    6145 应出现 `识别确认方案已生成=1`；若稳定复现 / 跨帧 / 提交质量不足，应出现 `成功=0`、`证据不足>0`、`可提交=0`，并写外设观察供料等待，不再生成稳定复现 / 观察完成 / 可复验普通子需求。
    若自然产生优质且有跨帧同一性证据的逐簇识别报告，才允许 `可提交>0` 和后续 6150 / 稳定子集扫描目标上下文继续。

禁止宣称：
不把 6145 结构化证据不足结果写成识别完成。
不把构建通过写成 3.13 通过。
不伪造外设报告、等待项、动作动态或扫描 / 跟踪样本。
```

## 2026-06-11 6150 提交桥下游承接定位预测

```text
当前切片：
    特征类 VecI 解释视图完善计划 3.13 下游承接定位：6150 提交入口到稳定子集扫描目标上下文。

依据文件：
    规范/禁止性规范20260611.md
    规范/规范目录.md
    规范/方法动作动态硬规则规范20260512.md
    规范/自我线程与任务管理线程权责边界规范20260512.md
    规范/任务筹办与执行边界规范20260512.md
    规范/相机外设综合工作流程规范20260607.md
    计划/计划索引.md

允许修改范围：
    计划/计划索引.md
    实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md
    自我动作实现.外设模块.ixx

禁止修改范围：
    不修改任务筹办 / 任务执行边界。
    不恢复任务筹办或任务执行为本能方法。
    不新增模拟样本、夹具入口、伪造外设报告或伪造动作动态。
    不把 D455 报告、队列项或等待项直接写成世界真值。
    不写需求满足、安全值或服务值。

已知真实日志证据：
    日志/鱼巢_run_20260611.低值g 已出现 `可进入提交入口组织=1`、`观察承接请求=1`、`自我线程/外设观察事实承接完成`，旧“缺真实优质逐簇识别材料”堵点已解除。
    后续 `提交_当前观察范围可观测单位存在对应事实` 仍反复 `成功=0`，典型状态为 `来源报告ID=4`、`已稳定对应存在可观测单位数量=21`、`稳定对应存在数量=21`、`扫描可启动状态=0`、`稳定子集扫描目标上下文状态=0`、`对应事实节点存在=1`、`映射表节点存在=1`。

预测结果：
    若根因是 6150 提交桥用 `可写对应事实` 同时约束整体提交成功和稳定子集扫描目标上下文，则在已有稳定对应存在时，稳定子集桥会被整体闭合状态拦截，导致 `扫描可启动状态=0` 且失败说明仍泛化。
    本切片应把“稳定子集扫描目标上下文可生成”与“当前观察范围可观测单位存在对应事实整体闭合”区分开：稳定子集只需报告、事实宿主、对应事实、映射表和至少一个稳定对应存在；全范围未闭合仍不得写当前场景明确、需求满足、安全值或服务值。

验收：
    最低验收：失败日志精确指出缺失输入，不再把已有节点和扫描可启动混在同一个泛化说明里。
    理想验收：真实运行中 `稳定子集扫描目标上下文状态=1`，然后再进入 `已识别区域扫描正式结果`、`指定存在跟踪正式结果` 和 VecI64 轮廓比较计数复核。

禁止宣称：
    不宣称 3.13 已通过，除非扫描 / 跟踪正式结果和 VecI64 比较计数自然命中。
    不宣称自我苏醒完成或初步成熟完成。
```

## 2026-06-11 6150 提交桥下游承接定位结果

```text
本次修改：
    自我动作实现.外设模块.ixx。
    6150 `提交_当前观察范围可观测单位存在对应事实` 不再用 `可写对应事实` 同时约束全范围提交闭合和稳定子集扫描目标上下文。
    稳定子集扫描可启动条件收束为：来源报告可组织正式对象，且稳定对应存在数量至少为 1。
    当前场景特征值明确状态、全范围提交成功和需求 / 安全 / 服务结算仍不由稳定子集直接写成。
    稳定子集桥失败说明已拆成事实宿主、对应事实、映射表、来源报告、来源报告质量、稳定对应存在计数和扫描可启动状态的逐项说明。

已运行命令：
    git diff --check -- '计划/计划索引.md' '实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md' '自我动作实现.外设模块.ixx'
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> '日志/鱼巢_build_20260611_6150_stable_subset_bridge.log'
    .\x64\Debug\鱼巢.exe --d455-status *> '日志/鱼巢_run_20260611_6150_d455_status_console.log'
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000 *> '日志/鱼巢_run_20260611_6150_stable_subset_bridge_30s_console.log'
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000 *> '日志/鱼巢_run_20260611_6150_stable_subset_bridge_30s_b_console.log'

关键退出码：
    git diff --check 退出码 0，仅有 CRLF 提示。
    Debug x64 构建退出码 0。
    --d455-status 退出码 0。
    14:41 真实自我线程 30 秒窗口退出码 0。
    14:50 真实自我线程 30 秒窗口退出码 0。

构建证据：
    日志/鱼巢_build_20260611_6150_stable_subset_bridge.log：已成功生成，1 个 LNK4075 链接提示，0 个错误。

真实运行证据：
    日志/鱼巢_run_20260611_6150_d455_status_console.log：独立状态命令显示 D455 未打开。
    日志/鱼巢_run_20260611.低值g line 55956-55957：14:41 窗口结束时停止 D455 外设线程。
    日志/鱼巢_run_20260611.低值g line 66644-66645：14:50 窗口结束时停止 D455 外设线程。
    14:50 窗口计数：
        `外设线程_D455深度相机/逐簇识别报告入队` = 0。
        `任务管理外设消息承接/生成观察事实承接请求` = 0。
        `自我线程/外设观察事实承接完成` = 0。
        `当前观察范围可观测单位存在对应事实提交` = 0。
        `稳定子集扫描目标上下文状态=1` = 0。
        `稳定子集全量扫描等待项承接` = 0。
        `稳定子集全量扫描等待项跳过` = 83，原因均为无可用扫描目标观察约束。
        `已识别区域扫描正式结果` = 0。
        `指定存在跟踪正式结果` = 0。

当前判断：
    6150 代码修正和构建验证已完成。
    补丁后的真实运行窗口未自然产生新的 D455 报告或新的 6150 方法摘要，无法验证稳定子集桥是否已在真实材料上生成 `稳定子集扫描目标上下文状态=1`。
    当前阻塞不是构建错误，也不是旧“缺真实优质逐簇识别材料”堵点；阻塞是缺补丁后的真实报告 / 6150 重跑材料。
    3.13 仍未通过，不宣称识别 / 扫描 / 跟踪真实轮廓比较行为验收完成。

下一步：
    等补丁后的真实 D455 报告和 6150 重跑窗口自然出现后，优先核对 `来源报告ID`、`稳定对应存在数量`、`扫描可启动状态`、`稳定子集扫描目标上下文状态` 和稳定子集桥失败说明。
    若真实 6150 仍失败，按新的精确失败说明继续最小修正。
    若出现 `稳定子集扫描目标上下文状态=1`，再继续 `已识别区域扫描正式结果`、`指定存在跟踪正式结果` 和 VecI64 轮廓比较计数复核。
```

## 2026-06-11 D455 外设线反查运行前预测

```text
当前切片：
    顺着外设线反查：外设观察报告队列 -> D455 外设线程 -> 单帧采集 -> 报告入队 -> 任务管理承接。

已知边界：
    D455 报告由 `外设线程_D455深度相机` 生成并调用 `提交外设观察报告` 入队。
    `任务管理工作线程` 只按等待项匹配、缓存和生成观察事实承接请求。
    `外设观察报告队列` 是进程内静态队列，独立 `--d455-status` 进程不能读取上一轮自我线程进程里的队列。
    `--d455-capture-once` 能验证真实相机打开和单帧采集，并通过 `提交D455单帧材料包证据卡` 形成材料包证据，但它不是任务管理承接和 6150 业务验收。

预测：
    若 `--d455-capture-once` 失败，问题在真实相机 / runtime / 打开 / 采集层，不进入报告队列承接。
    若 `--d455-capture-once` 成功但自我线程窗口仍无 `外设线程_D455深度相机/逐簇识别报告入队`，问题在 D455 外设线程启动、等待项触发、报告提交条件或日志限流 / 背压。
    若报告入队出现但 `任务管理外设消息承接/生成观察事实承接请求` 为 0，问题在等待项匹配或质量门。
    若观察事实承接出现但 6150 摘要为 0，问题在任务重筹办 / 当前方法派发到 6150 的链路。

禁止宣称：
    不把 `--d455-capture-once` 单帧成功宣称为 3.13 通过。
    不伪造外设报告、等待项、动作动态或扫描 / 跟踪样本。
```

## 2026-06-11 D455 外设线反查修复后验证预测

```text
定位结果：
    `--d455-capture-once` 已证明真实 D455 打开、采集一帧和材料包证据卡可用。
    15:00 自我线程窗口反查到任务层执行缺口持续出现，但工作结果 `需要等待=0`，导致 `私有_按当前方法供料要求提交外设等待项` 不提交外设观察等待项。
    因无等待项，D455 外设线程未启动；因此队列中无 `逐簇识别报告入队`，不是相机运行时失败。

本次修复：
    任务模块.管理界面线程.impl.cpp。
    方法供料等待项触发条件新增 `执行缺口方法供料`：当已执行方法返回 `方法前置条件未满足`，且当前方法能映射到外设观察供料口径时，允许提交外设观察等待项。
    不改任务状态推进为通用特判，不恢复任务筹办 / 任务执行本能方法。

验证预测：
    构建应通过。
    新自我线程窗口应出现 `任务管理界面线程/按当前方法供料要求创建外设观察等待项 | ... 触发口径=执行缺口方法供料`。
    随后应出现 `任务管理界面线程/按外设观察等待项启动D455线程` 和 `外设线程_D455深度相机/逐簇识别报告入队`。
    若报告入队后仍无观察事实承接，再定位等待项匹配或质量门。
    若观察事实承接后 6150 仍失败，再回到 `来源报告ID`、稳定对应存在数量和扫描可启动状态核对。
```

## 2026-06-11 D455 外设线反查修复后验证结果

```text
已修改文件：
    任务模块.管理界面线程.impl.cpp
    实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md

已运行命令：
    git diff --check -- '任务模块.管理界面线程.impl.cpp' '实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md'
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> '日志/鱼巢_build_20260611_d455_wait_supply_fix.log'
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000 *> '日志/鱼巢_run_20260611_d455_wait_supply_fix_30s_console.log'

关键退出码：
    git diff --check 退出码 0，仅有 CRLF 提示。
    Debug x64 构建退出码 0，构建日志只有 LNK4075 链接提示。
    自检命令被工具超时截断，未取得命令退出码；日志显示程序退出守卫已开始并完成自我线程、任务管理线程和 D455 外设线程停止，最终无残留 `鱼巢.exe` 进程。

真实运行证据：
    15:05:35.779 `任务管理界面线程/按当前方法供料要求创建外设观察等待项`，等待项ID=1，当前方法=q，`需要等待=0`，`触发口径=执行缺口方法供料`。
    15:05:35.779 `任务管理界面线程/按外设观察等待项启动D455线程 | 已启动=1`。
    15:05:35.779 `外设线程_D455深度相机/启动`。
    15:05:38.537 `外设线程_D455深度相机/逐簇识别报告入队`，报告ID=1，640x480，像素簇=11，深度有效率=7873，质量=8062。
    15:05:38.609 `任务管理外设消息承接/生成观察事实承接请求`，等待项ID=1，报告ID=1，质量等级=优质，可进入验证方法=1，可进入识别方法=1，可进入提交入口组织=1。
    15:05:38.610 `任务管理界面线程/外设等待项巡检生成上行`，上行消息数=1，已关闭等待项=1。
    15:05:39.202 `自我线程/外设观察事实承接完成 | 成功=是 | 报告可回查=是 | 禁止直接写世界真值=是`。
    15:07:02-15:07:07 程序退出守卫完成自我线程、任务管理线程和 D455 外设线程停止；D455 最终样本=52。

当前判断：
    外设线反查已定位根因：不是 D455 相机失败，也不是队列匹配失败；原问题在任务管理界面线程的方法供料等待项触发条件，只认 `工作结果.需要等待=1`，而首轮执行缺口是 `需要等待=0 / 待重筹办`，导致等待项无法创建。
    修复后已经真实取得一帧并入队，且完成任务管理观察事实承接和自我线程承接。
    本切片只证明外设供料链恢复，不宣称 3.13 已完成。

剩余未闭合：
    本窗口仍未看到 `当前观察范围可观测单位存在对应事实提交`、`稳定子集扫描目标上下文状态=1`、`已识别区域扫描正式结果` 或 `指定存在跟踪正式结果` 的闭合证据。
    下一步应从自我线程承接后的提交入口 / 6150 入账链继续追，不再卡在 D455 队列是否有帧。
```
