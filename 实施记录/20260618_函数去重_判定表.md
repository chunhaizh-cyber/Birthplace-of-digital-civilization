# 20260618 函数去重与无效代码清理判定表

生成时间：2026-06-19，按 `基础数据类型.h` 单入口收束、`动作验证报告类.h` 单入口收束、动作线程未接入 helper 删除、动作线程未接入环境 / 队列配置入口删除、`有方向约束` 删除、方法结果关系模板兼容输入收束、本能动作运行结果旧抽象特征 helper 删除、服务模块子任务服务摘要 / OR 胜出未接入 helper 删除、服务模块目标完成度评分 helper 删除、扫描来源 / 事务未接入 helper 删除、外设提交包 / 结果场景未接入 helper 删除、外设扫描结果场景状态重复 helper 删除、场景状态语义等价重复 helper 迁移、`声明条件特征` 重复 helper 迁移、自我初始化方法补齐自检 helper 簇删除、自我动作实现限制 / 差值包解析未接入 helper 簇删除、度量模板注册表链键便捷入口删除、任务管理工作线程未接入语素 / 写入 helper 删除、本能动作管理查询未接入公开成员删除、本能方法按能力值确保转调壳删除、基础比较量未调用 helper 删除、自我动作实现语素入口类型未调用 helper 删除、自我动作实现任务输入 / 回执读取未调用 helper 删除、自我动作实现条件项列表未调用 helper 删除、调试缓存 / 自检方法运行次数未调用 helper 删除、方法规格确认未接入 helper 删除、执行桥方法可用确认旧 helper 删除、任务管理运行存在构造旧 helper 删除后更新。

## 1. 本轮统计

| 项目 | 数量 |
| --- | ---: |
| 函数清单条目 | 6846 |
| 重复函数名组 | 848 |
| 重复函数功能签名清单条目 | 848 |
| 无调用粗筛候选 | 591 |

## 2. 补充判定规则

重复函数必须先梳理功能备注，再生成原始 C++ 签名和标准功能签名。功能签名、调用约束和副作用不一致时，禁止直接合并。无项目自定义数据类型参与的通用函数才允许进入 `全局共享函数类.ixx` 复核；有项目自定义数据类型参与的函数必须回到对应专用类、功能类或对应头文件。

本表是粗筛入口，不直接作为删除依据。进入删除切片前仍需逐项补足调用证据、入口 / 回调 / virtual 排除、项目结构读写证据和构建验证。

## 3. 当前首批待复核无调用候选

| 函数名 | 定义位置 | 出现次数 | 功能备注 | 迁移归属建议 | 风险标记 | 初步处理 |
| --- | --- | ---: | --- | --- | --- | --- |

### 3.1 已复核保留

| 函数名 | 位置 | 保留依据 | 后续约束 |
| --- | --- | --- | --- |
| 设置当前值 | 基础信息主信息类.h:446 | 源码无当前调用，但 `规范/存在作为特征值指针句柄规范20260509.md` 的当前结构示例列出该 API；函数直接写入 `特征节点主信息类::当前值`，属于特征值项目结构写入函数，命中保留规则 | 不按普通无调用 helper 删除；若后续要收束该 API，必须先修订规范示例和全部直接写入用法 |

## 4. 已执行删除

| 函数名 | 位置 | 删除依据 | 验证状态 |
| --- | --- | --- | --- |
| 总耗时_us | 动作线程类.ixx:75 | 仅定义、无真实调用、非入口/回调/override；只是 `结构_动作执行结果` 内部未接入耗时便捷视图，不写项目结构，也不应迁入全局共享模块 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 取消注册 | 动作线程类.ixx:214 | 仅定义、无真实调用、非入口/回调/override；涉及动作线程内部注册表和 `枚举_本能方法ID`，不是无自定义类型通用函数，且未接入当前注册生命周期 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 列出已注册动作 | 动作线程类.ixx:238 | 仅定义、无真实调用、非入口/回调/override；只是动作线程注册表展示视图，当前无外部读取方，删除后同步移除唯一依赖的 `<algorithm>` 头 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 动作线程未接入环境 / 队列配置入口 | 动作线程类.ixx:原 31-36、112-153、334-348、450-452 | 删除 `绑定环境`、`设置队列上限`、只由队列上限展示链互调的 `取队列上限` / `队列上限`、未接入的 `世界树` / `场景管理` 上下文字段、动作线程私有环境字段和队列满错误分支；仓库内没有 `动作线程模块` 外部导入方，动作函数也不读取 `动作执行上下文::世界树` 或 `场景管理`；当前正式动作线程路径只需要任务、场景、目标存在、方法根和请求 ID 上下文 | rg 源码、函数说明文档和无调用候选清单均未检出目标环境 / 队列配置符号；函数说明文档重生成，源码文件 128、函数条目 6883；无调用粗筛候选更新为 628；Debug x64 构建通过，0 警告 0 错误 |
| 本能方法按能力值确保转调壳 | 本能方法类.cpp:原 412 | 删除 `本能方法类::取或确保本能方法首节点_按能力值` 声明和定义；该函数只转调 `方法类::取或确保本能方法首节点_按能力值`，没有额外白名单或项目结构写入逻辑；正式白名单校验仍在 `方法类::取或确保本能方法首节点_按能力值` 内调用 `方法类::是有效本能方法能力值`，并通过 `本能方法类::是默认自我本能方法` 判断默认本能 ID | 源码只剩 `方法类` 正式声明 / 定义；函数说明文档重生成，源码文件 128、函数条目 6882；无调用粗筛候选更新为 627；Debug x64 构建通过，0 警告 0 错误 |
| 基础时间戳未调用工具（2项） | 基础数据类型.h:原 616、634 | 删除 `结构体_时间戳::当前_纳秒` 和 `结构体_时间戳::时间差_微秒_有符号`；二者是无自定义项目类型参与的基础时间戳工具，只命中源码定义、函数说明文档和无调用候选清单，不写项目结构，也没有调用方；正式路径保留 `当前_微秒` 和 `时间差_微秒` | 源码和函数说明文档均未检出目标名；函数说明文档重生成，源码文件 128、函数条目 6880；无调用粗筛候选更新为 625；Debug x64 构建通过，0 警告 0 错误 |
| 图像缓冲未调用尺寸视图（2项） | 基础数据类型.h:原 1348、1366 | 删除 `结构体_图像缓冲_u8::期望字节数` 和 `结构体_图像缓冲_f32::期望元素数`；二者只是相邻缓冲结构的未调用尺寸计算视图，不参与 `有效()` / `清空()` 当前运行路径，不写项目结构 | 源码和函数说明文档均未检出目标名；函数说明文档重生成，源码文件 128、函数条目 6878；无调用粗筛候选更新为 623；Debug x64 构建通过，0 警告 0 错误 |
| 原始场景帧未调用读取视图（3项） | 基础数据类型.h:原 1269、1280、1290 | 删除 `结构体_原始场景帧::有效前景提示`、`深度有效_at`、`点云_at`；三者只是原始场景帧内部缓冲读取便捷视图，源码无调用方，不写项目结构；相邻正式读取/校验路径保留 `有效深度`、`有效颜色`、`有效点云`、`有效深度掩膜`、`索引`、`深度_at` 和 `颜色_at` | 源码和函数说明文档均未检出目标名；函数说明文档重生成，源码文件 128、函数条目 6875；无调用粗筛候选更新为 620；Debug x64 构建通过，0 警告 0 错误 |
| 基础比较量未调用 helper（3项） | 基础数据类型.h:原 99、105、125 | 删除 `比较U64_三态`、`比较符号`、`比较量命中三向关系`；三者为无调用通用比较便捷函数，不写项目结构；正式路径保留 `比较I64_三态`、`比较量转三向关系`、`比较量方向掩码` 和 `比较量命中方向` | 源码、函数说明文档和无调用候选清单均未检出目标名；函数说明文档重生成，源码文件 128、函数条目 6872；无调用粗筛候选更新为 617；Debug x64 构建通过，0 警告 0 错误 |
| 基础数据类型未调用便捷视图（3项） | 基础数据类型.h:原 32、1413、1861 | 删除 `I64区间::是否点`、`结构体_存在观测::清理大缓冲`、`结构_特征使用统计::存在有效治理边`；三者只剩定义和清单登记，不写项目结构，不参与当前运行链；相邻正式接口保留 `有效`、`点值`、`包含`、`点`、`重新计算距离` 和特征生命周期统计函数 | 源码、函数说明文档和无调用候选清单均未检出目标名；函数说明文档重生成，源码文件 128、函数条目 6869；无调用粗筛候选更新为 614；Debug x64 构建通过，0 警告 0 错误 |
| 基础数据类型多维区间 / 观测距离便捷视图（2项） | 基础数据类型.h:原 768、1407 | 删除 `I64多维区间::维度数` 和 `结构体_存在观测::重新计算距离`；二者只剩定义和清单登记，不写项目结构；`I64多维区间::有效` 和 `结构体_存在观测::距离` 字段本体保留；`颜色_at` 因属原始场景帧相邻读取接口，本切片不删除 | 源码、函数说明文档和无调用候选清单均未检出目标名；函数说明文档重生成，源码文件 128、函数条目 6867；无调用粗筛候选更新为 612；Debug x64 构建通过，0 警告 0 错误 |
| 语素词性入口未调用 helper（2项） | 语素主信息类.h:原 199、226 | 删除 `语素_取人类词性信息入口候选` 和 `语素_推断人类词性默认信息入口类型`；二者只读静态候选表或转调默认选择，不写项目结构，未被语素注册、基础信息类型推断或信息入口路由调用；正式路径保留 `语素_选择默认信息入口类型` 和 `语素_推断人类词性默认基础信息类型` | 源码、函数说明文档和无调用候选清单均未检出目标名；函数说明文档重生成，源码文件 128、函数条目 6865；无调用粗筛候选更新为 610；Debug x64 构建通过，0 警告 0 错误 |
| 自我动作实现语素入口类型未调用 helper | 自我动作实现模块.ixx:原 4715 | 删除 `语素入口可作为特征类型`；该函数只做 `语素入口节点类*` 到抽象特征 / 特征主信息类型的未调用判断，不写项目结构，未被条件槽解析、方法规格、筹办或执行链调用；相邻正式路径保留 `读取条件项特征类型`、`吸收条件项指针值` 和 `解析条件项视图` | 源码、函数说明文档和无调用候选清单均未检出目标名；函数说明文档重生成，源码文件 128、函数条目 6864；无调用粗筛候选更新为 609；Debug x64 构建通过，0 警告 0 错误 |
| 自我动作实现任务输入 / 回执读取未调用 helper（3项） | 自我动作实现模块.ixx:原 5474、10520、10533 | 删除 `需求包有必需条件项`、`读取任务筹办输入任务`、`读取任务层回执宿主`；三者均为匿名 inline 未调用 helper，只读取条件项列表、任务信息节点或任务层回执宿主，不写项目结构，未被方法规格、筹办、执行或任务状态提交链调用；相邻正式路径保留 `读取条件槽列表`、`读取包指针`、`记录任务虚拟存在指针特征` 和任务状态提交链 | 源码、函数说明文档和无调用候选清单均未检出目标名；函数说明文档重生成，源码文件 128、函数条目 6861；无调用粗筛候选更新为 606；Debug x64 构建通过，0 警告 0 错误 |
| 自我动作实现条件项列表未调用 helper | 自我动作实现模块.ixx:原 4851 | 删除 `读取条件项列表`；该函数只读取条件包子特征并过滤有值约束条件项，匿名 inline 且无调用，不写项目结构；正式条件槽读取路径保留 `读取条件槽列表`、`解析条件项视图` 和 `结构_条件项视图` | 源码、函数说明文档和无调用候选清单均未检出目标名；函数说明文档重生成，源码文件 128、函数条目 6860；无调用粗筛候选更新为 605；Debug x64 构建通过，0 警告 0 错误 |
| 调试缓存 / 自检方法运行次数未调用 helper（2项） | 特征值类.h:原 33、自检线程模块.impl.cpp:原 94 | 删除 `调试清空缓存` 和 `私有_特征_方法运行次数_自检`；二者源码均无调用点，非入口、非回调、非 virtual / override、非协议函数；`调试清空缓存` 只是未调用调试清理入口，正式缓存重建路径保留 `初始化()` -> `私有_重建缓存_已加锁`；`私有_特征_方法运行次数_自检` 只是自检模块未接入语素 helper，正式方法运行次数入口仍由动作实现模块和方法虚拟存在服务承载 | 源码和函数说明文档均未检出目标名；函数说明文档重生成，源码文件 128、函数条目 6858；无调用粗筛候选更新为 603；Debug x64 构建通过，0 警告 0 错误 |
| 方法规格确认未接入 helper（8项） | 自我动作实现模块.ixx:原 3886、7596、7612、7627、7644、7659、8434、8449 | 删除 `确保学习本能自举条件结果对`、`确认挂起任务等待子需求规格`、`确认唤醒任务重筹办规格`、`确认派生需求入树规格`、`确认登记任务发起意图规格`、`确认承接需求创建任务规格`、`确认归并子结果到父任务筹办规格`、`确认检查父任务是否可重筹办规格`；8 个目标均只命中定义，未发现本能方法规格注册、switch 分支或调用路径；`确认筹办协作回执槽` 仍被正式规格函数调用并保留 | 源码和函数说明文档均未检出目标名；函数说明文档重生成，源码文件 128、函数条目 6850；无调用粗筛候选更新为 595；Debug x64 构建通过，0 警告 0 错误 |
| 执行桥方法可用确认旧 helper | 任务模块.执行.ixx:原 949 | 删除 `私有_填充方法可用确认执行结果_执行桥_最小_v0`；该函数是匿名 inline 旧执行桥 helper，只命中定义、函数说明文档和候选清单，不是入口 / 回调 / override / 协议函数；相邻正式执行结果填充链仍由缺动作、缺可执行入口、缺条件节点、缺结果节点、缺条件结果对、方法不可用、参数配对缺口、运行账不足和结果不稳定 helper 承载 | 源码、函数说明文档和无调用候选清单均未检出目标名；函数说明文档重生成，源码文件 128、函数条目 6849；无调用粗筛候选更新为 594；Debug x64 构建通过，0 警告 0 错误 |
| 任务管理运行存在构造旧 helper（3项） | 任务模块.管理工作线程.ixx:原 445、509、590 | 删除 `私有_从任务筹办运行存在构造结果`、`私有_从任务执行运行存在构造回执`、`私有_从任务执行运行存在构造结果`；3 个目标均只命中定义、函数说明文档和候选清单，非入口 / 回调 / override / 协议函数；当前正式任务执行路径仍为 `执行任务` 后由 `私有_构造执行推进回执` 生成推进回执 | 源码、函数说明文档和无调用候选清单均未检出目标名；函数说明文档重生成，源码文件 128、函数条目 6846；无调用粗筛候选更新为 591；Debug x64 构建通过，0 警告 0 错误 |
| Has | 基础数据类型.h:1318 | 仅定义、无真实调用、非入口/回调/override；只做 `枚举_存在观测缓冲` 未接入位掩码便捷视图，不写项目结构，不作为闭环证据；保留 `operator|` 和枚举字段本体 | rg 源码、函数说明文档和无调用候选清单均未检出 `inline bool Has`；Debug x64 构建通过，0 警告 0 错误 |
| 有方向约束 | 方法主信息类.h:154 | 仅定义、无真实调用、非入口/回调/override；只是 `结构_方法结果关系模板` 的未接入便捷视图，现有代码没有读取该函数；相邻兼容输入注释已另记独立切片，不在本删除切片内处理 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 安全风险树因素需要通用证据搜索_筹办查询 | 任务模块.筹办.ixx:2213 | 仅定义、无真实调用、非入口/回调、未写项目结构 | rg 源码和函数说明文档均未检出 |
| 包目标值类型为I64 | 自我动作实现模块.ixx:5847 | 仅定义、无真实调用、非入口/回调、未写项目结构 | rg 源码和函数说明文档均未检出 |
| 差值约束为唯一零区间 | 自我动作实现模块.ixx:5932 | 仅定义、无真实调用、非入口/回调、未写项目结构 | rg 源码和函数说明文档均未检出 |
| L5关键成功额外条件成立 | 自我动作实现.服务模块.ixx:2995 | 仅定义、无真实调用、非入口/回调/override、未写项目结构；L5 当前正式入账仍由 `允许L5关键成功` 与 G0 特征回写链承接，删除该未接入 helper 不改变行为 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 从回执复制指针到任务虚拟存在 | 自我动作实现模块.ixx:11154 | 仅定义、无真实调用、非入口/回调/override、未接入任务虚拟存在同步调用链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 从回执复制I64到任务虚拟存在 | 自我动作实现模块.ixx:11168 | 仅定义、无真实调用、非入口/回调/override、未接入任务虚拟存在同步调用链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 从回执同步候选方法头节点到任务 | 自我动作实现模块.ixx:11182 | 仅定义、无真实调用、非入口/回调/override、未接入候选方法头同步调用链；不能作为项目结构闭环证据 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 同步任务管理控制请求语义槽 | 任务模块.工作线程消息协议.ixx:371 | 仅定义、无真实调用、非入口/回调/override；`结构_任务管理控制请求::控制意图抽象特征指针` 和 `控制意图当前值` 只被该 helper 写入，未被线程状态消息、治理协议、运行包或自我线程读取；正式控制意图语义仍由线程状态消息、治理承接记录和运行包各自字段承载 | rg 源码、函数说明文档和无调用候选清单均未检出该函数；控制请求结构两个残余字段已删除；Debug x64 构建通过，0 警告 0 错误 |
| 复制包特征当前值 | 自我动作实现模块.ixx:5703 | 仅定义、无真实调用、非入口/回调/override；其底层正式 helper `复制特征当前标量到宿主` 仍有调用并保留 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 复制包I64 | 自我动作实现模块.ixx:5719 | 仅定义、无真实调用、非入口/回调/override、未接入包复制链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 复制包指针 | 自我动作实现模块.ixx:5736 | 仅定义、无真实调用、非入口/回调/override、未接入包复制链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 写入包区间 | 自我动作实现模块.ixx:5803 | 仅定义、无真实调用、非入口/回调/override、未接入包区间写入链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 命中差值约束 | 自我动作实现模块.ixx:5916 | 仅定义、无真实调用、非入口/回调/override；现有差值约束读取链未调用该 helper | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 写入实例特征I64 | 自我动作实现模块.ixx:5956 | 仅定义、无真实调用、非入口/回调/override、未接入实例特征写入链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 抽象节点可覆盖 | 自我动作实现模块.ixx:5575 | 仅定义、无真实调用、非入口/回调/override、未接入类型覆盖判断调用链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 查找宿主实例特征_按抽象特征 | 自我动作实现模块.ixx:5929 | 仅定义、无真实调用、非入口/回调/override、未接入宿主实例特征查找调用链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 创建宿主实例特征_按抽象特征 | 自我动作实现模块.ixx:5939 | 仅定义、无真实调用、非入口/回调/override、未接入宿主实例特征创建调用链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 从目标宿主复制指针实参_筹办 | 任务模块.筹办.ixx:5598 | 仅定义、无真实调用、非入口/回调/override、未接入筹办输入参数补齐链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 方法结果能力命中目标特征 | 任务模块.筹办.ixx:4833 | 仅定义、无真实调用、非入口/回调/override；现有筹办候选召回未调用该 helper | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 方法已有参数化结果能力 | 任务模块.筹办.ixx:4862 | 仅定义、无真实调用、非入口/回调/override；未接入参数化结果能力判断调用链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 构造安全风险树搜索缺口项_筹办查询 | 任务模块.筹办.ixx:2171 | 仅定义、无真实调用、非入口/回调/override；虽构造 `结构_临时因果路径项`，但没有调用方接入风险树筹办查询链；当前正式风险树同步 / 跳过路径不依赖该构造函数 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 构造安全风险树场景影响前置缺口项_筹办查询 | 任务模块.筹办.ixx:2220 | 仅定义、无真实调用、非入口/回调/override；虽构造 `结构_临时因果路径项`，但没有调用方接入风险树筹办查询链；当前正式风险树同步 / 跳过路径不依赖该构造函数 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 类型_筹办_新观察材料 | 任务模块.筹办.ixx:302 | 仅定义、无真实调用、非入口/回调/override；只服务未接入的观察质量依赖裁决簇，未进入筹办主链或需求树写入链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 父需求目标特征依赖深度空间_筹办查询 | 任务模块.筹办.ixx:2202 | 仅定义、无真实调用、非入口/回调/override；只服务未接入的观察质量依赖裁决簇，未进入筹办主链或需求树写入链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 父需求目标特征可降级继续_筹办查询 | 任务模块.筹办.ixx:2221 | 仅定义、无真实调用、非入口/回调/override；只服务未接入的观察质量依赖裁决簇，未进入筹办主链或需求树写入链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 父需求目标属于观察处理_筹办查询 | 任务模块.筹办.ixx:2230 | 仅定义、无真实调用、非入口/回调/override；只服务未接入的观察质量依赖裁决簇，未进入筹办主链或需求树写入链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 确保新观察材料目标主体_筹办查询 | 任务模块.筹办.ixx:2238 | 仅定义、无真实调用、非入口/回调/override；只服务未接入的观察质量依赖裁决簇，未进入筹办主链或需求树写入链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 父需求继续策略文本_筹办查询 | 任务模块.筹办.ixx:2253 | 仅定义、无真实调用、非入口/回调/override；只服务未接入的观察质量依赖裁决簇，返回文本只用于未接入诊断说明 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 观察质量阻塞关系文本_筹办查询 | 任务模块.筹办.ixx:2267 | 仅定义、无真实调用、非入口/回调/override；只服务未接入的观察质量依赖裁决簇，返回文本只用于未接入诊断说明 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 构造新观察材料质量目标项_筹办查询 | 任务模块.筹办.ixx:2281 | 仅定义、无真实调用、非入口/回调/override；虽构造 `结构_临时因果路径项`，但唯一使用点在未接入的观察质量依赖裁决簇内部 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 注入观察质量父需求依赖裁决_筹办查询 | 任务模块.筹办.ixx:2333 | 仅定义、无真实调用、非入口/回调/override；会读写 `结构_临时因果视图` 观察质量字段并构造派生路径，但没有调用方接入筹办主链，不能作为项目结构闭环证据 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 风险树同步/展开未接入函数簇（19项） | 任务模块.筹办.ixx:1938-2193 | 该簇包含任务筹办侧风险树节点、节点键、目标安全因果信息、证据方向、因素证据搜索状态、上级因素ID、同层版本、入账/聚合/同步/版本/节点数/搜索缺口/最近维护、节点键计算、输入版本计算、维护同步、展开跳过等 helper；固定函数名均只命中定义、函数说明文档或簇内引用；正式风险树节点、目标安全因果信息、证据方向、入账、聚合和节点数量由自我线程 / 外设模块正式路径承载 | rg 源码和函数说明文档均未检出 19 个目标函数名；Debug x64 构建通过，0 警告 0 错误 |
| 观察质量裁决残余未接入特征 helper（20项） | 任务模块.筹办.ixx:276-400 | 第 40 切片删除观察质量依赖裁决未接入函数簇后，该组 helper 固定函数名均只剩定义和函数说明文档；同名正式观察质量、空间、风险判断、补观察等业务特征由外设模块正式读写链承载；仍被学习缺口路径引用的 `特征_筹办_观察质量改善路径验证状态` 已保留 | rg 源码和函数说明文档均未检出 20 个目标函数名；Debug x64 构建通过，0 警告 0 错误 |
| 取或创建安全因果风险树因素节点_筹办查询 | 任务模块.筹办.ixx:2299 | 仅定义、无真实调用、非入口/回调/override；未被风险树搜索缺口项构造或筹办查询链调用 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 方法选择评分相同 | 任务模块.筹办.ixx:3827 | 仅定义、无真实调用、非入口/回调/override；现有选择逻辑直接使用 `方法选择评分更优`，未调用该等价判断 helper | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 方法结果项目标值适配需求 | 任务模块.筹办.ixx:3953 | 仅定义、无真实调用、非入口/回调/override；未接入方法结果项适配或候选召回判断链 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 条件结果对归并旧结果节点查找 helper（2项） | 自我动作实现模块.ixx:3735-3758 | `查找等价结果节点_条件结果对归并` 和 `查找复现结果节点_条件结果对归并` 固定名称只命中定义、函数说明文档和无调用候选清单；当前正式路径使用 `查找复现结果节点判定_条件结果对归并` 返回 `结构_结果节点复现判定`，保留可比较 / 复现分离信息 | rg 源码、函数说明文档和无调用候选清单均未检出 2 个目标函数名；函数说明文档重生成，源码文件 128、函数条目 7015；Debug x64 构建通过，0 警告 0 错误 |
| 回执_拼接诊断 | 本能动作类.ixx:146 | 仅定义、无真实调用、非入口/回调/override；只做未接入字符串诊断拼接，不写项目结构 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 已形成闭环事实 | 本能动作管理类.ixx:423 | 仅定义、无真实调用、非入口/回调/override；当前调用链直接读取运行视图字段并调用 `有执行缺口`，未使用该便捷判断 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 枚举默认本能动作ID | 本能动作管理类.ixx:1104 | 仅定义、无真实调用、非入口/回调/override；只是 `本能方法类::枚举默认自我本能方法ID()` 的未调用别名，正式默认本能方法注册 / 确认路径已直接调用后者 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 取能力结果包 | 方法主信息类.h:448 | 仅定义、无真实调用、非入口/回调/override；当前方法能力读取链直接读取 `首节点->能力.结果包` 或使用已有 `有结果能力` / `首个结果能力特征类型` | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 有最小出生锚点 | 方法主信息类.h:454 | 仅定义、无真实调用、非入口/回调/override；只是 `有方法动作 || 有结果能力` 的未接入便捷视图 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 是仅结果特征需求位 | 方法主信息类.h:459 | 仅定义、无真实调用、非入口/回调/override；只是 `有结果能力 && !有方法动作` 的未接入便捷视图 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 是内部变化本能方法 | 自我动作实现.内部模块.ixx:144 | 仅定义、无真实调用、非入口/回调/override；未接入本能动作注册或执行闭环，实际注册路径直接使用注册函数和枚举 ID | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 是外部影响本能方法 | 自我动作实现.外部模块.ixx:16 | 仅定义、无真实调用、非入口/回调/override；未接入外部本能动作注册或规格确认链，实际路径直接调用外设模块规格确认函数 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 取可写值域 | 基础信息主信息类.h:367 | 仅定义、无真实调用、非入口/回调/override；只是抽象特征值域的未接入可写视图，现有调用链使用 `取值域`、`设为叶子特征` 或直接结构字段 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 是否内部世界 | 基础信息主信息类.h:536 | 仅定义、无真实调用、非入口/回调/override；只是场景宿主存在字段的未接入便捷判断 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 是否现实世界 | 基础信息主信息类.h:539 | 仅定义、无真实调用、非入口/回调/override；只是场景世界类型字段的未接入便捷判断 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 可绑定基础信息类型 | 语素主信息类.h:458 | 仅定义、无真实调用、非入口/回调/override；只是未接入的语素基础信息类型兼容判断 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 是否叶子 | 双向链表模板核心.h:60 | 仅定义、无真实调用、非入口/回调/override；只是链表节点未接入便捷判断 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 是否同层孤立 | 双向链表模板核心.h:63 | 仅定义、无真实调用、非入口/回调/override；只是链表节点未接入便捷判断 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 取或创建本能动作运行结果抽象特征 | 本能动作管理类.ixx:271 | 仅定义、无真实调用；历史说明中的“运行结果抽象特征”旧 helper，当前正式路径由 `创建本能动作运行结果实例` 写运行虚拟存在、运行码、执行情况、失败原因、执行缺口类型和后续承接方式，不再创建运行结果抽象特征节点 | rg 源码和函数说明文档均未检出；说明书/本能动作管理类说明.md 已删除对应旧章节；Debug x64 构建通过，0 警告 0 错误 |
| 写入单个已确认观察存在 | 自我动作实现.外设模块.ixx:13699 | 仅定义、无真实调用、非入口/回调/override；只是 `取或创建已确认观察存在` + `写入已确认观察存在节点原始事实` 的未接入包装，正式提交路径已直接调用正式写入链并传入外设簇证据 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 基础节点是已确认观察存在 | 自我动作实现.外设模块.ixx:13976 | 仅定义、无真实调用、非入口/回调/override；只是 `基础信息节点类*` 到 `存在节点类*` 的未接入检查包装，正式收集 / 查找路径直接调用 `已确认观察存在节点` | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 清空差值结果 | 基础信息主信息类.h:752 | 仅定义、无真实调用、非入口/回调/override；只是 `二次特征主信息类` 内部未接入清理便捷函数，当前没有调用链需要复用该清理动作 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 标量值命中标量区间 | 基础信息主信息类.h:762 | 仅定义、无真实调用、非入口/回调/override；只是 `标量区间` 与 `标量值` 的未接入便捷判断，正式匹配路径直接读取 `标量区间` 并比较 `标量值` | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 状态节点I64区间 | 任务模块.筹办.ixx:481 | 仅定义、无真实调用、非入口/回调/override；只是状态节点到特征区间的未接入读取 helper；相邻 `状态节点I64值`、`状态节点主体`、`状态节点目标特征类型` 仍有真实调用并保留 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_筹办_补观察缺口状态 | 任务模块.筹办.ixx:296 | 仅定义、无真实调用、非入口/回调/override；只返回 `筹办特征词` 本地语素入口，未被筹办候选、缺口、回执或写入链调用；同名正式业务特征由外设观察链 `特征_补观察缺口状态` 承载 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_筹办_补观察需求建议 | 任务模块.筹办.ixx:302 | 仅定义、无真实调用、非入口/回调/override；只返回 `筹办特征词` 本地语素入口，未被筹办候选、缺口、回执或写入链调用；同名正式业务特征由外设观察链 `特征_补观察需求建议` 承载 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_筹办_基础风险判断状态 | 任务模块.筹办.ixx:404 | 仅定义、无真实调用、非入口/回调/override；只返回 `筹办特征词` 本地语素入口，未被筹办候选、缺口、回执或写入链调用；同名正式业务特征由外设风险判断链 `特征_基础风险判断状态` 承载 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_AND组满足状态_筹办 | 任务模块.筹办.ixx:766 | 仅定义、无真实调用、非入口/回调/override；只是未接入的筹办特征语素 helper，正式抽象特征定义仍由 `特征类.cpp` 和需求语义视图路径承载 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_OR组满足状态_筹办 | 任务模块.筹办.ixx:773 | 仅定义、无真实调用、非入口/回调/override；只是未接入的筹办特征语素 helper，正式抽象特征定义仍由 `特征类.cpp` 和需求语义视图路径承载 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_方法路径完成状态_筹办 | 任务模块.筹办.ixx:780 | 仅定义、无真实调用、非入口/回调/override；只是未接入的筹办特征语素 helper，正式抽象特征定义仍由 `特征类.cpp` 和需求语义视图路径承载 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_因果子链支撑状态_筹办 | 任务模块.筹办.ixx:766 | 仅定义、无真实调用、非入口/回调/override；只是未接入的筹办特征语素 helper，正式抽象特征定义仍由 `特征类.cpp`、需求目标语义视图和规范约束承载 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 需求目标要求正值 | 任务模块.筹办.ixx:706 | 仅定义、无真实调用、非入口/回调/override；只读取来源需求状态并返回 bool，未被筹办候选、缺口、回执或写入链调用；底层 `需求目标方向编码` 仍有真实调用并保留 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 需求目标要求至少 | 任务模块.筹办.ixx:726 | 仅定义、无真实调用、非入口/回调/override；只读取来源需求状态并返回 bool，未被筹办候选、缺口、回执或写入链调用 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_方法结构是否完整 | 任务模块.筹办.ixx:4508 | 仅定义、无真实调用、非入口/回调/override；只返回本地语素入口，未被筹办候选、缺口、回执或写入链调用；正式方法结构特征仍由特征 / 世界 / 方法虚拟存在路径承载 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_方法成熟度阶段 | 任务模块.筹办.ixx:4515 | 仅定义、无真实调用、非入口/回调/override；只返回本地语素入口，未被筹办候选、缺口、回执或写入链调用；正式方法成熟度特征仍由特征 / 世界 / 方法虚拟存在路径承载 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_方法允许自动召回 | 任务模块.筹办.ixx:4522 | 仅定义、无真实调用、非入口/回调/override；只返回本地语素入口，未被筹办候选、缺口、回执或写入链调用 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_方法允许默认执行 | 任务模块.筹办.ixx:4508 | 仅定义、无真实调用、非入口/回调/override；只返回本地语素入口，未被筹办候选、缺口、回执或写入链调用；正式 `允许默认执行` 特征仍由世界、特征和方法虚拟存在路径承载 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_安全结算增量_筹办 | 任务模块.筹办.ixx:4369 | 仅定义、无真实调用、非入口/回调/override；只返回本地语素入口，未被筹办候选、缺口、回执或写入链调用；正式安全结算增量特征仍由自我动作实现链和自我线程结算链承载 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_服务结算增量_筹办 | 任务模块.筹办.ixx:4376 | 仅定义、无真实调用、非入口/回调/override；只返回本地语素入口，未被筹办候选、缺口、回执或写入链调用；正式服务结算增量特征仍由自我动作实现链和自我线程结算链承载 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_任务完成度百万分比_筹办 | 任务模块.筹办.ixx:4383 | 仅定义、无真实调用、非入口/回调/override；只返回本地语素入口，未被筹办候选、缺口、回执或写入链调用；正式任务完成度特征仍由自我动作实现链和任务管理完成度计算链承载 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_安全因果层级_筹办 | 任务模块.筹办.ixx:4250 | 仅定义、无真实调用、非入口/回调/override；只返回本地语素入口，未被筹办链调用；同名正式特征仍由自我线程 / 自我动作实现的动态和外设风险因素链承载 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_安全因果因素ID_筹办 | 任务模块.筹办.ixx:4264 | 仅定义、无真实调用、非入口/回调/override；只返回本地语素入口，未被筹办链调用；同名正式特征仍由自我线程 / 自我动作实现的动态和外设风险因素链承载 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_安全因果层级_风险树筹办查询 | 任务模块.筹办.ixx:1993 | 仅定义、无真实调用、非入口/回调/override；只返回本地语素入口，未被风险树筹办查询链调用；同名正式特征仍由自我线程 / 自我动作实现的动态和外设风险因素链承载 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_安全因果因素ID_风险树筹办查询 | 任务模块.筹办.ixx:2007 | 仅定义、无真实调用、非入口/回调/override；只返回本地语素入口，未被风险树筹办查询链调用；同名正式特征仍由自我线程 / 自我动作实现的动态和外设风险因素链承载 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_关键特征变化观察状态_风险树筹办查询 | 任务模块.筹办.ixx:2014 | 仅定义、无真实调用、非入口/回调/override；只返回本地语素入口，未被风险树筹办查询候选、缺口、回执或写入链调用；正式观察状态特征仍由外设风险因素链承载 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_安全因果候选构造状态_风险树筹办查询 | 任务模块.筹办.ixx:2021 | 仅定义、无真实调用、非入口/回调/override；只返回本地语素入口，未被风险树筹办查询候选、缺口、回执或写入链调用；正式安全因果候选构造状态特征仍由外设风险因素链承载 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_当前场景持续观察需求明确状态_风险树筹办查询 | 任务模块.筹办.ixx:2028 | 仅定义、无真实调用、非入口/回调/override；只返回本地语素入口，未被风险树筹办查询候选、缺口、回执或写入链调用；`特征_当前场景持续观察状态_风险树筹办查询` 保留到下一小切片单独复核 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_当前场景持续观察状态_风险树筹办查询 | 任务模块.筹办.ixx:2014 | 仅定义、无真实调用、非入口/回调/override；只返回本地语素入口，未被风险树筹办查询候选、缺口、回执或写入链调用；同名正式业务特征由 `自我动作实现.外设模块.ixx::特征_当前场景持续观察状态` 及外设风险因素 / 持续观察链承载 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 子任务服务摘要 / OR 胜出未接入语素 helper（15项） | 自我动作实现.服务模块.ixx:693-805 | 该组包含子任务服务摘要、子任务指针键、必需/达成/数量、组合输出一致性、OR 胜出子任务/G0/摘要/未选路径/路径已选择等语素入口 helper；固定函数名只命中定义和函数说明文档，非入口/回调/override；未接入子任务服务摘要、OR 路径选择、输出一致性或服务结算项目结构，保留会误导为已有视图闭环 | rg 源码、函数说明文档和无调用候选清单均未检出 15 个目标函数名；Debug x64 构建通过，0 警告 0 错误 |
| 目标完成度评分未接入 helper（2项） | 自我动作实现.服务模块.ixx:2768-2794 | `计算数值目标完成度评分` 和 `计算枚举布尔目标完成度评分` 固定函数名只命中定义和函数说明文档，非入口/回调/override；二者依赖服务域离散评分枚举和服务评分满分常量，属于服务模块专用评分壳，不是全局共享函数类.ixx 候选；未接入 G0 评分、任务完成度、服务结算或候选状态项目结构 | rg 源码、函数说明文档和无调用候选清单均未检出 2 个目标函数名；Debug x64 构建通过，0 警告 0 错误 |
| 扫描来源 / 事务未接入语素 helper（5项） | 自我动作实现.扫描.ixx:449-548 | `特征_来源需求ID`、`特征_来源任务ID`、`特征_来源方法ID`、`特征_来源外设ID状态`、`特征_写入事务ID` 固定函数名只命中定义和函数说明文档，非入口/回调/override；只包装扫描绑定枚举到本地语素入口，未被扫描运行场景、外设提交包或写入事务链调用；正式扫描绑定枚举和绑定名称表仍保留 | rg 源码、函数说明文档和无调用候选清单均未检出 5 个目标函数名；Debug x64 构建通过，0 警告 0 错误 |
| 外设提交包 / 结果场景未接入语素 helper（14项） | 自我动作实现.外设模块.ixx:4553-4651 | 该组包含外设提交包 ID、类型、组织、材料、过期、generation、可比较、缺失/质量/句柄/特征值/候选数量以及识别/跟踪结果场景状态等本地语素入口 helper；固定函数名只命中定义和函数说明文档，非入口/回调/override；未被外设提交包组织、识别/跟踪结果场景或提交事实链调用；外设提交包类型、状态常量和相邻 `特征_外设扫描结果场景状态` 保留 | rg 源码、函数说明文档和无调用候选清单均未检出 14 个目标函数名；Debug x64 构建通过，0 警告 0 错误 |
| 特征_外设扫描结果场景状态（外设模块重复定义） | 自我动作实现.外设模块.ixx:4553 | 同名正式路径在 `自我动作实现.扫描.ixx` 中仍有定义和真实调用，用于扫描输出 I64 状态、方法结果能力和组合子状态；外设模块副本只返回本地语素入口，未被外设提交包、识别 / 跟踪结果场景或提交事实链调用，属于重复 helper 副本，不迁入全局共享模块 | rg 源码只检出 `自我动作实现.扫描.ixx` 的正式定义 / 调用和函数说明文档登记；重复函数名组 / 功能签名清单条目更新为 860；Debug x64 构建通过，0 警告 0 错误 |
| 自我初始化方法补齐自检 helper 簇 | 自我类.初始化.cpp:原 102-1062 | 12 个候选入口只命中定义、函数说明文档和无调用候选清单；两个自检结果结构体、方法补齐尝试/包状态枚举、方法补齐候选包/缺口写入 helper 只服务该未调用簇；删除后正式方法补齐语义仍由 `自我动作实现模块.ixx`、任务执行桥和 `自我类::更新方法补齐状态` 承载 | 函数说明文档重生成，源码文件 128、函数条目 6908；目标符号在源码函数文档、无调用候选清单和重复清单均未检出；重复函数名组 / 功能签名清单条目更新为 848；无调用粗筛候选更新为 647 |
| 限制 / 差值包解析未接入 helper 簇 | 自我动作实现模块.ixx:原 4604-5750 | 删除 `抽象特征区间集合`、`读取限制指针`、`读取包抽象特征引用`、`读取包语素入口指针`、`吸收差值约束抽象区间`、`吸收差值约束节点区间`、`读取差值约束区间集合`、`读取实例特征I64`、`解析查找限制包` 和只服务该解析入口的 `结构_查找限制`；其中 5 个函数在无调用粗筛中只命中定义，4 个函数只被同簇未接入入口内部调用；该簇未接入正式条件槽、方法规格、筹办或执行链，保留会误导为限制 / 差值包解析已闭环 | rg 源码、函数说明文档、无调用候选清单和重复清单均未检出目标名；函数说明文档重生成，源码文件 128、函数条目 6899；无调用粗筛候选更新为 642；Debug x64 构建通过，0 警告 0 错误 |
| 度量模板注册表链键便捷入口（2项） | 度量模板注册表类.ixx:原 132-147 | `生成链键_存在` 和 `生成链键_特征` 是公开便捷入口，但当前源码只命中定义、函数说明文档和无调用候选清单；现有正式读取路径为 `度量模板注册表桥接.cpp::获取度量默认刻度_由链键` 调用 `默认刻度_由链键`，模板内部 `生成链键` 虚函数和 `匹配链键` 仍保留；删除这两个未接入入口不影响链键到刻度的读取闭环 | rg 源码、函数说明文档、无调用候选清单和重复清单均未检出目标名；函数说明文档重生成，源码文件 128、函数条目 6897；无调用粗筛候选更新为 640；Debug x64 构建通过，0 警告 0 错误 |
| 任务管理工作线程未接入语素 / 写入 helper（8项） | 任务模块.管理工作线程.ixx:原 264-404 | 删除 `私有_特征_输入条件包`、`私有_特征_任务信息节点`、`私有_值_执行缺口_当前方法条件不满足`、`私有_值_执行缺口_可执行输入参数场景缺失`、`私有_值_执行缺口_当前方法缺失`、`私有_值_执行缺口_当前方法无执行入口`、`私有_写基础节点指针特征`、`私有_写基础节点I64特征`；8 个函数只命中定义、函数说明文档和无调用候选清单，未被任务筹办、执行回执、任务状态提交或管理线程协议读取 / 写入链调用 | rg 源码、函数说明文档、无调用候选清单和重复清单均未检出目标名；函数说明文档重生成，源码文件 128、函数条目 6889；无调用粗筛候选更新为 632；Debug x64 构建通过，0 警告 0 错误 |
| 本能动作管理查询未接入公开成员（2项） | 本能动作管理类.ixx:原 663-687 | 删除 `查询_按函数名` 和 `查询函数名称`；二者是公开成员但仓库内只命中定义、函数说明文档和无调用候选清单，未被注册、执行、任务执行桥或动作线程桥读取；`查询_按函数名` 只返回旧式 `(ID, 登记信息)` 视图，`查询函数名称` 只做 ID 到函数名显示视图；正式按名称解析入口保留 `查询ID`，并仍由 `检查是否可调用` 读取方法首节点动作名时调用 | rg 源码、函数说明文档、无调用候选清单和重复清单均未检出目标名；函数说明文档重生成，源码文件 128、函数条目 6887；无调用粗筛候选更新为 630；Debug x64 构建通过，0 警告 0 错误 |

## 5. 已执行迁移

| 函数名 | 处理范围 | 判定 | 验证状态 |
| --- | --- | --- | --- |
| 私有_安全词 / 安全取词包装 | 二次特征应用模块.ixx、任务类.cpp、语言类_二次特征表达.cpp、语言类_基础信息命名.cpp、自检线程模块.impl.cpp、控制面板类.cpp | 4 处等价 `私有_安全词` 实现迁移到语素专用头 `语素类.h::语素_安全获取词`；`自检线程模块.impl.cpp::私有_安全词_自检` 和 `控制面板类.cpp::私有_词文本` 保留各自树节点校验 / 解析边界后复用该 helper；`自我线程模块.impl.cpp` 保留树归属校验包装，`语言类_存在表达.cpp` 保留直接主键读取，二者不是等价重复 | 函数说明文档重生成，函数条目 7160；重复函数清单中 `私有_安全词` 已降为 2 处非等价实现；Debug x64 构建通过，0 警告 0 错误 |
| 私有_布尔文本 | 控制面板类.cpp、线程生命周期消息模块.ixx、自检线程模块.impl.cpp、鱼巢.cpp、自我线程模块.impl.cpp | 5 处均无自定义数据类型参与，但输出口径分为 `是/否`、`1/0`、`true/false`，禁止合并成单一函数；已迁移为 `全局共享函数类.ixx::布尔文本_是或否`、`布尔文本_一或零`、`布尔文本_true或false` 三个明确口径 helper | 函数说明文档重生成，函数条目 7158；重复函数清单中 `私有_布尔文本` 已移除，重复函数名组 / 功能签名清单条目均为 870；Debug x64 构建通过，0 警告 0 错误 |
| 场景等价_条件结果对归并 -> 方法类::场景状态语义等价 | 方法类.h、方法类.cpp、自我动作实现模块.ixx、自我动作实现.外设模块.ixx | 两个动作模块各自定义同一段场景状态语义等价判断；该逻辑依赖 `场景节点类`、世界树状态服务和特征比较服务，不能进入全局共享模块；复用既有方法域实现，将 `方法类.cpp::私有_场景状态语义等价` 提升为 `方法类::场景状态语义等价` 专用静态接口，删除两个本地重复定义并改调用方 | 旧函数名 `场景等价_条件结果对归并` 和旧私有名 `私有_场景状态语义等价` 源码 / 函数说明文档 / 重复清单均未检出；函数说明文档重生成，源码文件 128、函数条目 7021；重复函数名组 / 功能签名清单条目更新为 859；Debug x64 构建通过，0 警告 0 错误 |
| 声明条件特征 -> 方法类::声明条件特征 | 方法类.h、方法类.cpp、自我动作实现模块.ixx、自我动作实现.外设模块.ixx | 两个动作模块重复定义同一段方法条件集合声明逻辑；该逻辑依赖 `方法节点`、方法条件场景、条件集合、特征槽和 I64 必需标记，属于方法域专用结构写入函数，不进入 `全局共享函数类.ixx`；迁移为 `方法类::声明条件特征` 两个静态重载，删除两个动作模块本地定义并改调用方 | 旧本地 inline 定义源码未检出；函数说明文档只登记 `方法类.cpp` 两个正式定义；重复函数名清单和功能签名清单已移除 `声明条件特征` 旧行；函数说明文档重生成，源码文件 128、函数条目 7021；重复函数名组 / 功能签名清单条目更新为 858；Debug x64 构建通过，0 警告 0 错误 |
| 动作验证报告类.h / 动作验证报告类.ixx 双入口 | 动作验证报告类.h、动作验证报告类.ixx、鱼巢.vcxproj、鱼巢.vcxproj.filters | 两个入口定义同一套动作验证报告枚举、结构体和 inline helper；调用侧均 `#include "动作验证报告类.h"`，未发现 `import 动作验证报告类`；按用户补充口径收束为 `.h` 单入口，删除未被 import 的 `.ixx` 编译项；`.h` 中两个 `转文本` 是专用枚举重载，保留 | 函数说明文档重生成，源码文件 128、函数条目 7147；重复函数名组 / 功能签名清单条目均为 861；Debug x64 串行构建通过，0 警告 0 错误 |
| 任务结果状态摘要_兼容 -> 任务实际结果状态 | 任务主信息类.h、任务类.h、任务类.cpp、任务模块.筹办.ixx、任务模块.管理工作线程.ixx、任务模块.管理界面线程.impl.cpp、控制面板类.cpp、自我动作实现模块.ixx、自我线程模块.impl.cpp、鱼巢.cpp | 该路径仍参与任务筹办、任务管理、自我动作实现、自我线程结算证据和展示读取 / 写入，按 007 不能继续作为兼容摘要；不新增结构，以 `状态节点类` 承载事实本体，`任务主信息类::实际结果状态` 保存 `可解析引用<状态节点类>` 索引，旧 `_兼容` 函数名和 `结果状态信息` 字段名收束为正式入口 | 旧 `_兼容` 函数名、`结果状态信息` 字段名和迁移期兼容文案源码检索为 0；函数说明文档重生成，源码文件 128、函数条目 7147；Debug x64 串行构建通过，0 警告 0 错误 |
| 方法结果关系模板兼容输入 -> 关系模板唯一入口 | 方法主信息类.h、方法类.cpp、任务模块.筹办.ixx、控制面板类.cpp、自我动作实现模块.ixx、自我动作实现.外设模块.ixx、自我动作实现.扫描.ixx | 该路径仍参与方法结果能力写入、候选召回、结果能力匹配和展示读取；按 007 禁止保留旧字段兼容输入。删除 `结构_方法结果项` 旧字段和 `同步兼容字段到关系模板()`，方法结果能力统一读取 / 写入 `结构_方法结果关系模板 关系模板` | 旧兼容函数、旧字段机器路径和兼容文案源码 / 函数说明文档检索为 0；函数说明文档重生成，源码文件 128、函数条目 7142；Debug x64 串行构建通过，0 警告 0 错误 |
| 本能动作输入参数场景缺失语义收束 | 本能动作管理类.ixx、说明书/本能动作管理类说明.md | `本能动作执行缺口_可执行输入参数场景缺失词` 原来无调用，但不能按普通无调用 helper 删除；该语义应承接 `检查可执行输入参数` 中输入参数场景为空的真实缺口。新增调试运行码 `本能动作错误_输入参数场景缺失=-20010`，并映射到 `执行缺口_可执行输入参数场景缺失`，其它输入参数不满足仍映射为 `当前方法条件不满足` | `rg` 确认词函数已有正式调用；函数说明文档重生成，源码文件 128、函数条目 7141；Debug x64 串行构建通过，0 警告 0 错误 |

## 6. 输出文件

- 实施记录/20260618_函数去重_重复函数名清单.tsv
- 实施记录/20260618_函数去重_重复函数功能签名清单.tsv
- 实施记录/20260618_函数去重_无调用候选清单.tsv

## 7. 当前结论

P0 清单已按当前代码更新；`基础数据类型.ixx` 和 `动作验证报告类.ixx` 不再参与候选。`私有_安全词` 已完成一轮等价重复迁移，剩余 2 处同名函数因保护边界不同暂不合并。`私有_布尔文本` 已按三种输出口径迁入全局共享模块。`场景等价_条件结果对归并` 已迁移为 `方法类::场景状态语义等价` 专用接口。`声明条件特征` 已迁移为 `方法类::声明条件特征` 专用接口。`动作验证报告类.h` 保留专用枚举重载入口。`任务结果状态摘要_兼容` 已按独立切片收束为正式 `任务实际结果状态` 入口。`方法结果关系模板兼容输入` 已按独立切片收束为 `关系模板` 唯一入口。`本能动作执行缺口_可执行输入参数场景缺失词` 已按独立切片接入输入参数场景缺失运行码映射。`动作线程类.ixx` 已删除 3 个未接入注册表 / 耗时便捷函数，`基础数据类型.h` 已删除 1 个未接入存在观测缓冲位掩码 helper，`方法主信息类.h` 已删除 1 个未接入方向约束便捷函数，`本能动作管理类.ixx` 已删除 1 个旧运行结果抽象特征 helper和 1 个默认本能动作 ID 未调用别名 helper，`任务模块.工作线程消息协议.ixx` 已删除 1 个未接入控制请求语义槽 helper及其两个残余字段，`自我动作实现模块.ixx` 已删除 2 个条件结果对归并旧结果节点查找 helper，`自我动作实现.外设模块.ixx` 已删除 2 个已确认观察存在未接入包装 helper、14 个外设提交包 / 结果场景未接入语素 helper和 1 个外设扫描结果场景状态重复 helper，`自我动作实现.服务模块.ixx` 已删除 15 个子任务服务摘要 / OR 胜出未接入语素 helper和 2 个目标完成度评分未接入 helper，`自我动作实现.扫描.ixx` 已删除 5 个扫描来源 / 事务未接入语素 helper，`基础信息主信息类.h` 已删除 2 个二次特征未接入便捷函数，`任务模块.筹办.ixx` 已删除 1 个状态节点区间未接入读取 helper、20 个观察质量裁决残余未接入 helper、19 个风险树同步 / 展开未接入函数簇 helper、9 个观察质量依赖裁决未接入函数簇 helper、2 个安全风险树未接入缺口项构造 helper、3 个补观察 / 基础风险筹办未接入 helper、4 个未接入逻辑组织特征 helper、2 个未接入需求目标判断 helper、4 个未接入方法状态特征 helper、3 个未接入结算完成度特征 helper、2 个未接入安全因果通用筹办 helper、2 个未接入安全因果风险树 helper和 4 个风险树观察候选未接入特征 helper。无调用候选清单已移除 31 条已不在函数说明文档中的历史残留，当前残留比对数为 0。下一步继续按单个源代码定义文件推进函数用途和功能签名审计；后续兼容路径、语义分裂、大范围未接入簇、重复 helper 副本或视图结构残余问题另开独立切片。

补充：`自我类.初始化.cpp` 方法补齐自检 helper 簇已删除并移除对应清单旧行；`自我动作实现模块.ixx` 限制 / 差值包解析未接入 helper 簇已删除并移除对应清单旧行；`度量模板注册表类.ixx` 两个链键便捷入口已删除并移除对应清单旧行；`任务模块.管理工作线程.ixx` 8 个未接入语素 / 写入 helper 已删除并移除对应清单旧行；`本能动作管理类.ixx` 2 个查询未接入公开成员已删除并移除对应清单旧行。`基础信息主信息类.h::设置当前值` 已复核为规范示例 API 和项目结构写入函数，保留；`自我动作实现模块.ixx::语素入口可作为特征类型`、`需求包有必需条件项`、`读取任务筹办输入任务`、`读取任务层回执宿主`、`读取条件项列表` 已删除并移除对应清单旧行。当前仍不宣称函数清理完成，后续继续按单文件切片审计。
