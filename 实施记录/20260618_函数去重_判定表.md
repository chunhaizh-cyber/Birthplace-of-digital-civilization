# 20260618 函数去重与无效代码清理判定表

生成时间：2026-06-19 04:20 后，按 `基础数据类型.h` 单入口收束、`动作验证报告类.h` 单入口收束、动作线程未接入 helper 删除、`有方向约束` 删除、方法结果关系模板兼容输入收束和本能动作运行结果旧抽象特征 helper 删除后更新。

## 1. 本轮统计

| 项目 | 数量 |
| --- | ---: |
| 函数清单条目 | 7131 |
| 重复函数名组 | 861 |
| 重复函数功能签名清单条目 | 861 |
| 无调用粗筛候选 | 767 |

## 2. 补充判定规则

重复函数必须先梳理功能备注，再生成原始 C++ 签名和标准功能签名。功能签名、调用约束和副作用不一致时，禁止直接合并。无项目自定义数据类型参与的通用函数才允许进入 `全局共享函数类.ixx` 复核；有项目自定义数据类型参与的函数必须回到对应专用类、功能类或对应头文件。

本表是粗筛入口，不直接作为删除依据。进入删除切片前仍需逐项补足调用证据、入口 / 回调 / virtual 排除、项目结构读写证据和构建验证。

## 3. 当前首批待复核无调用候选

| 函数名 | 定义位置 | 出现次数 | 功能备注 | 迁移归属建议 | 风险标记 | 初步处理 |
| --- | --- | ---: | --- | --- | --- | --- |
| 同步任务管理控制请求语义槽 | 任务模块.工作线程消息协议.ixx:371 | 1 | 同步线程、任务、场景或对象之间的状态。 | 专用类/对应头文件：任务 | 待复核 | 待复核，未删除 |

## 4. 已执行删除

| 函数名 | 位置 | 删除依据 | 验证状态 |
| --- | --- | --- | --- |
| 总耗时_us | 动作线程类.ixx:75 | 仅定义、无真实调用、非入口/回调/override；只是 `结构_动作执行结果` 内部未接入耗时便捷视图，不写项目结构，也不应迁入全局共享模块 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 取消注册 | 动作线程类.ixx:214 | 仅定义、无真实调用、非入口/回调/override；涉及动作线程内部注册表和 `枚举_本能方法ID`，不是无自定义类型通用函数，且未接入当前注册生命周期 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 列出已注册动作 | 动作线程类.ixx:238 | 仅定义、无真实调用、非入口/回调/override；只是动作线程注册表展示视图，当前无外部读取方，删除后同步移除唯一依赖的 `<algorithm>` 头 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 有方向约束 | 方法主信息类.h:154 | 仅定义、无真实调用、非入口/回调/override；只是 `结构_方法结果关系模板` 的未接入便捷视图，现有代码没有读取该函数；相邻兼容输入注释已另记独立切片，不在本删除切片内处理 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 安全风险树因素需要通用证据搜索_筹办查询 | 任务模块.筹办.ixx:2213 | 仅定义、无真实调用、非入口/回调、未写项目结构 | rg 源码和函数说明文档均未检出 |
| 包目标值类型为I64 | 自我动作实现模块.ixx:5847 | 仅定义、无真实调用、非入口/回调、未写项目结构 | rg 源码和函数说明文档均未检出 |
| 差值约束为唯一零区间 | 自我动作实现模块.ixx:5932 | 仅定义、无真实调用、非入口/回调、未写项目结构 | rg 源码和函数说明文档均未检出 |
| L5关键成功额外条件成立 | 自我动作实现.服务模块.ixx:2995 | 仅定义、无真实调用、非入口/回调/override、未写项目结构；L5 当前正式入账仍由 `允许L5关键成功` 与 G0 特征回写链承接，删除该未接入 helper 不改变行为 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 从回执复制指针到任务虚拟存在 | 自我动作实现模块.ixx:11154 | 仅定义、无真实调用、非入口/回调/override、未接入任务虚拟存在同步调用链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 从回执复制I64到任务虚拟存在 | 自我动作实现模块.ixx:11168 | 仅定义、无真实调用、非入口/回调/override、未接入任务虚拟存在同步调用链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 从回执同步候选方法头节点到任务 | 自我动作实现模块.ixx:11182 | 仅定义、无真实调用、非入口/回调/override、未接入候选方法头同步调用链；不能作为项目结构闭环证据 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 复制包特征当前值 | 自我动作实现模块.ixx:5703 | 仅定义、无真实调用、非入口/回调/override；其底层正式 helper `复制特征当前标量到宿主` 仍有调用并保留 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 复制包I64 | 自我动作实现模块.ixx:5719 | 仅定义、无真实调用、非入口/回调/override、未接入包复制链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 复制包指针 | 自我动作实现模块.ixx:5736 | 仅定义、无真实调用、非入口/回调/override、未接入包复制链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 写入包区间 | 自我动作实现模块.ixx:5803 | 仅定义、无真实调用、非入口/回调/override、未接入包区间写入链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 命中差值约束 | 自我动作实现模块.ixx:5916 | 仅定义、无真实调用、非入口/回调/override；现有差值约束读取链未调用该 helper | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 写入实例特征I64 | 自我动作实现模块.ixx:5956 | 仅定义、无真实调用、非入口/回调/override、未接入实例特征写入链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 抽象节点可覆盖 | 自我动作实现模块.ixx:5575 | 仅定义、无真实调用、非入口/回调/override、未接入类型覆盖判断调用链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 查找宿主实例特征_按抽象特征 | 自我动作实现模块.ixx:5929 | 仅定义、无真实调用、非入口/回调/override、未接入宿主实例特征查找调用链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 创建宿主实例特征_按抽象特征 | 自我动作实现模块.ixx:5939 | 仅定义、无真实调用、非入口/回调/override、未接入宿主实例特征创建调用链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 从目标宿主复制指针实参_筹办 | 任务模块.筹办.ixx:5598 | 仅定义、无真实调用、非入口/回调/override、未接入筹办输入参数补齐链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 方法结果能力命中目标特征 | 任务模块.筹办.ixx:4833 | 仅定义、无真实调用、非入口/回调/override；现有筹办候选召回未调用该 helper | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 方法已有参数化结果能力 | 任务模块.筹办.ixx:4862 | 仅定义、无真实调用、非入口/回调/override；未接入参数化结果能力判断调用链 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 取或创建安全因果风险树因素节点_筹办查询 | 任务模块.筹办.ixx:2299 | 仅定义、无真实调用、非入口/回调/override；未被风险树搜索缺口项构造或筹办查询链调用 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 方法选择评分相同 | 任务模块.筹办.ixx:3827 | 仅定义、无真实调用、非入口/回调/override；现有选择逻辑直接使用 `方法选择评分更优`，未调用该等价判断 helper | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 方法结果项目标值适配需求 | 任务模块.筹办.ixx:3953 | 仅定义、无真实调用、非入口/回调/override；未接入方法结果项适配或候选召回判断链 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 回执_拼接诊断 | 本能动作类.ixx:146 | 仅定义、无真实调用、非入口/回调/override；只做未接入字符串诊断拼接，不写项目结构 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 已形成闭环事实 | 本能动作管理类.ixx:423 | 仅定义、无真实调用、非入口/回调/override；当前调用链直接读取运行视图字段并调用 `有执行缺口`，未使用该便捷判断 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 取能力结果包 | 方法主信息类.h:448 | 仅定义、无真实调用、非入口/回调/override；当前方法能力读取链直接读取 `首节点->能力.结果包` 或使用已有 `有结果能力` / `首个结果能力特征类型` | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 有最小出生锚点 | 方法主信息类.h:454 | 仅定义、无真实调用、非入口/回调/override；只是 `有方法动作 || 有结果能力` 的未接入便捷视图 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 是仅结果特征需求位 | 方法主信息类.h:459 | 仅定义、无真实调用、非入口/回调/override；只是 `有结果能力 && !有方法动作` 的未接入便捷视图 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 是内部变化本能方法 | 自我动作实现.内部模块.ixx:144 | 仅定义、无真实调用、非入口/回调/override；未接入本能动作注册或执行闭环，实际注册路径直接使用注册函数和枚举 ID | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 是外部影响本能方法 | 自我动作实现.外部模块.ixx:16 | 仅定义、无真实调用、非入口/回调/override；未接入外部本能动作注册或规格确认链，实际路径直接调用外设模块规格确认函数 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 取可写值域 | 基础信息主信息类.h:367 | 仅定义、无真实调用、非入口/回调/override；只是抽象特征值域的未接入可写视图，现有调用链使用 `取值域`、`设为叶子特征` 或直接结构字段 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 是否内部世界 | 基础信息主信息类.h:536 | 仅定义、无真实调用、非入口/回调/override；只是场景宿主存在字段的未接入便捷判断 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 是否现实世界 | 基础信息主信息类.h:539 | 仅定义、无真实调用、非入口/回调/override；只是场景世界类型字段的未接入便捷判断 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 可绑定基础信息类型 | 语素主信息类.h:458 | 仅定义、无真实调用、非入口/回调/override；只是未接入的语素基础信息类型兼容判断 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 是否叶子 | 双向链表模板核心.h:60 | 仅定义、无真实调用、非入口/回调/override；只是链表节点未接入便捷判断 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 是否同层孤立 | 双向链表模板核心.h:63 | 仅定义、无真实调用、非入口/回调/override；只是链表节点未接入便捷判断 | rg 源码、函数说明文档和无调用候选清单均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 取或创建本能动作运行结果抽象特征 | 本能动作管理类.ixx:271 | 仅定义、无真实调用；历史说明中的“运行结果抽象特征”旧 helper，当前正式路径由 `创建本能动作运行结果实例` 写运行虚拟存在、运行码、执行情况、失败原因、执行缺口类型和后续承接方式，不再创建运行结果抽象特征节点 | rg 源码和函数说明文档均未检出；说明书/本能动作管理类说明.md 已删除对应旧章节；Debug x64 构建通过，0 警告 0 错误 |
| 写入单个已确认观察存在 | 自我动作实现.外设模块.ixx:13699 | 仅定义、无真实调用、非入口/回调/override；只是 `取或创建已确认观察存在` + `写入已确认观察存在节点原始事实` 的未接入包装，正式提交路径已直接调用正式写入链并传入外设簇证据 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 基础节点是已确认观察存在 | 自我动作实现.外设模块.ixx:13976 | 仅定义、无真实调用、非入口/回调/override；只是 `基础信息节点类*` 到 `存在节点类*` 的未接入检查包装，正式收集 / 查找路径直接调用 `已确认观察存在节点` | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 清空差值结果 | 基础信息主信息类.h:752 | 仅定义、无真实调用、非入口/回调/override；只是 `二次特征主信息类` 内部未接入清理便捷函数，当前没有调用链需要复用该清理动作 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 标量值命中标量区间 | 基础信息主信息类.h:762 | 仅定义、无真实调用、非入口/回调/override；只是 `标量区间` 与 `标量值` 的未接入便捷判断，正式匹配路径直接读取 `标量区间` 并比较 `标量值` | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_AND组满足状态_筹办 | 任务模块.筹办.ixx:766 | 仅定义、无真实调用、非入口/回调/override；只是未接入的筹办特征语素 helper，正式抽象特征定义仍由 `特征类.cpp` 和需求语义视图路径承载 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_OR组满足状态_筹办 | 任务模块.筹办.ixx:773 | 仅定义、无真实调用、非入口/回调/override；只是未接入的筹办特征语素 helper，正式抽象特征定义仍由 `特征类.cpp` 和需求语义视图路径承载 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_方法路径完成状态_筹办 | 任务模块.筹办.ixx:780 | 仅定义、无真实调用、非入口/回调/override；只是未接入的筹办特征语素 helper，正式抽象特征定义仍由 `特征类.cpp` 和需求语义视图路径承载 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 特征_因果子链支撑状态_筹办 | 任务模块.筹办.ixx:766 | 仅定义、无真实调用、非入口/回调/override；只是未接入的筹办特征语素 helper，正式抽象特征定义仍由 `特征类.cpp`、需求目标语义视图和规范约束承载 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 需求目标要求正值 | 任务模块.筹办.ixx:706 | 仅定义、无真实调用、非入口/回调/override；只读取来源需求状态并返回 bool，未被筹办候选、缺口、回执或写入链调用；底层 `需求目标方向编码` 仍有真实调用并保留 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |
| 需求目标要求至少 | 任务模块.筹办.ixx:726 | 仅定义、无真实调用、非入口/回调/override；只读取来源需求状态并返回 bool，未被筹办候选、缺口、回执或写入链调用 | rg 源码和函数说明文档均未检出；Debug x64 构建通过，0 警告 0 错误 |

## 5. 已执行迁移

| 函数名 | 处理范围 | 判定 | 验证状态 |
| --- | --- | --- | --- |
| 私有_安全词 / 安全取词包装 | 二次特征应用模块.ixx、任务类.cpp、语言类_二次特征表达.cpp、语言类_基础信息命名.cpp、自检线程模块.impl.cpp、控制面板类.cpp | 4 处等价 `私有_安全词` 实现迁移到语素专用头 `语素类.h::语素_安全获取词`；`自检线程模块.impl.cpp::私有_安全词_自检` 和 `控制面板类.cpp::私有_词文本` 保留各自树节点校验 / 解析边界后复用该 helper；`自我线程模块.impl.cpp` 保留树归属校验包装，`语言类_存在表达.cpp` 保留直接主键读取，二者不是等价重复 | 函数说明文档重生成，函数条目 7160；重复函数清单中 `私有_安全词` 已降为 2 处非等价实现；Debug x64 构建通过，0 警告 0 错误 |
| 私有_布尔文本 | 控制面板类.cpp、线程生命周期消息模块.ixx、自检线程模块.impl.cpp、鱼巢.cpp、自我线程模块.impl.cpp | 5 处均无自定义数据类型参与，但输出口径分为 `是/否`、`1/0`、`true/false`，禁止合并成单一函数；已迁移为 `全局共享函数类.ixx::布尔文本_是或否`、`布尔文本_一或零`、`布尔文本_true或false` 三个明确口径 helper | 函数说明文档重生成，函数条目 7158；重复函数清单中 `私有_布尔文本` 已移除，重复函数名组 / 功能签名清单条目均为 870；Debug x64 构建通过，0 警告 0 错误 |
| 动作验证报告类.h / 动作验证报告类.ixx 双入口 | 动作验证报告类.h、动作验证报告类.ixx、鱼巢.vcxproj、鱼巢.vcxproj.filters | 两个入口定义同一套动作验证报告枚举、结构体和 inline helper；调用侧均 `#include "动作验证报告类.h"`，未发现 `import 动作验证报告类`；按用户补充口径收束为 `.h` 单入口，删除未被 import 的 `.ixx` 编译项；`.h` 中两个 `转文本` 是专用枚举重载，保留 | 函数说明文档重生成，源码文件 128、函数条目 7147；重复函数名组 / 功能签名清单条目均为 861；Debug x64 串行构建通过，0 警告 0 错误 |
| 任务结果状态摘要_兼容 -> 任务实际结果状态 | 任务主信息类.h、任务类.h、任务类.cpp、任务模块.筹办.ixx、任务模块.管理工作线程.ixx、任务模块.管理界面线程.impl.cpp、控制面板类.cpp、自我动作实现模块.ixx、自我线程模块.impl.cpp、鱼巢.cpp | 该路径仍参与任务筹办、任务管理、自我动作实现、自我线程结算证据和展示读取 / 写入，按 007 不能继续作为兼容摘要；不新增结构，以 `状态节点类` 承载事实本体，`任务主信息类::实际结果状态` 保存 `可解析引用<状态节点类>` 索引，旧 `_兼容` 函数名和 `结果状态信息` 字段名收束为正式入口 | 旧 `_兼容` 函数名、`结果状态信息` 字段名和迁移期兼容文案源码检索为 0；函数说明文档重生成，源码文件 128、函数条目 7147；Debug x64 串行构建通过，0 警告 0 错误 |
| 方法结果关系模板兼容输入 -> 关系模板唯一入口 | 方法主信息类.h、方法类.cpp、任务模块.筹办.ixx、控制面板类.cpp、自我动作实现模块.ixx、自我动作实现.外设模块.ixx、自我动作实现.扫描.ixx | 该路径仍参与方法结果能力写入、候选召回、结果能力匹配和展示读取；按 007 禁止保留旧字段兼容输入。删除 `结构_方法结果项` 旧字段和 `同步兼容字段到关系模板()`，方法结果能力统一读取 / 写入 `结构_方法结果关系模板 关系模板` | 旧兼容函数、旧字段机器路径和兼容文案源码 / 函数说明文档检索为 0；函数说明文档重生成，源码文件 128、函数条目 7142；Debug x64 串行构建通过，0 警告 0 错误 |
| 本能动作输入参数场景缺失语义收束 | 本能动作管理类.ixx、说明书/本能动作管理类说明.md | `本能动作执行缺口_可执行输入参数场景缺失词` 原来无调用，但不能按普通无调用 helper 删除；该语义应承接 `检查可执行输入参数` 中输入参数场景为空的真实缺口。新增调试运行码 `本能动作错误_输入参数场景缺失=-20010`，并映射到 `执行缺口_可执行输入参数场景缺失`，其它输入参数不满足仍映射为 `当前方法条件不满足` | `rg` 确认词函数已有正式调用；函数说明文档重生成，源码文件 128、函数条目 7141；Debug x64 串行构建通过，0 警告 0 错误 |

## 6. 输出文件

- 实施记录/20260618_函数去重_重复函数名清单.tsv
- 实施记录/20260618_函数去重_重复函数功能签名清单.tsv
- 实施记录/20260618_函数去重_无调用候选清单.tsv

## 7. 当前结论

P0 清单已按当前代码更新；`基础数据类型.ixx` 和 `动作验证报告类.ixx` 不再参与候选。`私有_安全词` 已完成一轮等价重复迁移，剩余 2 处同名函数因保护边界不同暂不合并。`私有_布尔文本` 已按三种输出口径迁入全局共享模块。`动作验证报告类.h` 保留专用枚举重载入口。`任务结果状态摘要_兼容` 已按独立切片收束为正式 `任务实际结果状态` 入口。`方法结果关系模板兼容输入` 已按独立切片收束为 `关系模板` 唯一入口。`本能动作执行缺口_可执行输入参数场景缺失词` 已按独立切片接入输入参数场景缺失运行码映射。`动作线程类.ixx` 已删除 3 个未接入注册表 / 耗时便捷函数，`方法主信息类.h` 已删除 1 个未接入方向约束便捷函数，`本能动作管理类.ixx` 已删除 1 个旧运行结果抽象特征 helper，`自我动作实现.外设模块.ixx` 已删除 2 个已确认观察存在未接入包装 helper，`基础信息主信息类.h` 已删除 2 个二次特征未接入便捷函数，`任务模块.筹办.ixx` 已删除 4 个未接入逻辑组织特征 helper和 2 个未接入需求目标判断 helper。下一步继续按单个源代码定义文件推进函数用途和功能签名审计；后续兼容路径或语义分裂问题另开独立切片。
