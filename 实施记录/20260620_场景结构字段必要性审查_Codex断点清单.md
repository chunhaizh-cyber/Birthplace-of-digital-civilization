# 20260620 场景结构字段必要性审查 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.1 P2.1，建立场景结构 / 字段必要性判定表。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
规范/主信息字段与结构新增准入约束规范20260519.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
基础信息主信息类.h
场景类.h
场景类.cpp
世界树类.h
世界树类.cpp
```

本切片范围：

```text
1. 只建立场景主信息字段和世界树场景相关运行期结构判定表。
2. 不删除 场景节点主信息类 字段。
3. 不删除或迁移 世界树类.h 中的场景坐标 / 轮廓查找结构。
4. 不补 场景类 功能函数；该工作留给后续 1.2。
```

验证前预测：

```text
1. 场景节点主信息类字段可在 基础信息主信息类.h:476-487 定位。
2. 世界树类.h 中可定位 场景相对坐标项、场景绝对坐标换算结果、结构_世界树场景轮廓查找参数、结构_世界树场景轮廓存在比较结果。
3. 关系索引 与 二次特征索引 会出现同类型引用集合和双写 / 双维护路径，需进入后续 P2.2，不在本切片直接删除。
4. 本切片不修改源码；不需要用构建作为代码变更证据。
```

禁止宣称：

```text
不宣称场景类逻辑纠偏完成。
不宣称世界树场景辅助结构归属完成。
不宣称关系索引 / 二次特征索引双轨已收束。
不宣称结构体清理完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 1. 实施记录

```text
1. 已读取必读规范和计划索引。
2. 已确认计划索引第 2 章下一优先队列：P0 特征类、P1 存在类当前可执行切片已闭合，下一候选为 P2 场景类逻辑纠偏计划。
3. 已读取 计划/20260620_场景类逻辑纠偏计划_v0.1.md 的 1.1，确认 P2.1 只做只读判定表。
4. 已运行核心扫描并写入 日志/20260620_场景结构字段必要性审查_核心扫描.txt。
5. 已运行全仓引用扫描并写入 日志/20260620_场景结构字段必要性审查_全仓引用.txt。
6. 已生成 实施记录/20260620_场景结构字段判定表.md。
```

## 2. 验证结果

```text
核心扫描：
    rg 场景类.h 场景类.cpp 基础信息主信息类.h 世界树类.h 世界树类.cpp
    输出 136 行，已落盘。

全仓引用扫描：
    rg 宿主存在 / 世界类型 / 来源文章 / 状态索引 / 动态索引 / 关系索引 / 二次特征索引 / 场景坐标 / 场景轮廓结构
    输出 460 行，已落盘。

引用计数摘要：
    宿主存在 155
    世界类型 83
    来源文章 5
    状态索引 79
    动态索引 33
    关系索引 36
    二次特征索引 47
    场景相对坐标项 8
    场景绝对坐标换算结果 4
    结构_世界树场景轮廓查找参数 4
    结构_世界树场景轮廓存在比较结果 8
```

## 3. 当前判断

```text
1. P2.1 已闭合：已形成场景结构 / 字段必要性判定表。
2. 本切片不修改源码，未运行 msbuild；构建不作为本轮只读文档切片的验收证据。
3. 下一步进入 P2.2：优先做 关系索引 与 二次特征索引 的引用闭环，判断是否存在可删除双轨；若需要功能函数，转入 场景类 1.2。
4. 世界树场景坐标 / 轮廓查找结构暂不删除，留到世界树场景辅助函数归属审查。
```
