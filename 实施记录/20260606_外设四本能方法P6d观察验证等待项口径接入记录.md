# 20260606_外设四本能方法P6d观察验证等待项口径接入记录

## 切片

```text
计划：计划/20260605_外设四本能方法需求树生成因果链修订计划_v0.1.md
切片：P6d 观察验证等待项任务管理口径最小接入
目标：新增任务管理内部 观察验证 口径，映射到 D455 逐簇识别报告供料，但不新增外设队列物理运行模式。
```

## 修改前预测

```text
自我_验证外设观察像素簇候选 的正式结果能力可映射为观察验证口径。
观察验证口径应使用 陌生环境逐簇识别 / 逐簇识别报告 等待项。
观察验证不要求目标区域或目标簇。
不新增外设观察运行模式，不把观察验证口径混写成识别。
```

## 已修改

```text
任务模块.管理界面线程.impl.cpp：
    枚举_外设观察需求口径_任务管理 新增 观察验证。
    私有_外设观察需求口径文本_任务管理 新增文本 观察验证。
    私有_外设观察需求口径_按方法供料要求 新增正式结果能力映射：
        外设观察像素簇候选已验证状态
        外设像素簇候选验证状态
        外设观察材料可回查状态
    兼容本能ID兜底新增 自我_验证外设观察像素簇候选 -> 观察验证。
    私有_按方法供料要求构造外设观察等待项 允许 观察验证 口径。

观察验证口径输出：
    观察运行模式=陌生环境逐簇识别
    期望报告类型=逐簇识别报告
    最大允许报告年龄=120000ms
    不要求目标区域或目标簇
    摘要结构化口径=观察验证
```

## 验证

```text
git diff --check -- 任务模块.管理界面线程.impl.cpp：
    退出码 0；
    仅 LF/CRLF 提示。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m：
    退出码 0；
    0 个错误；
    1 个链接警告 LNK4075：因 /INCREMENTAL:NO 忽略 /EDITANDCONTINUE。

未真实运行鱼巢.exe。
不宣称观察验证等待项自然业务链已通过。
```

## 下一步

```text
进入 P6e：
    对账需求目标是否能生成四方法正式目标特征；
    不把需求目标写成 调用观察 / 调用识别 / 调用扫描 / 调用跟踪。
```
