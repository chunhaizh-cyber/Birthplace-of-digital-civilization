# 20260620 存在场景绝对坐标 helper Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_存在类逻辑纠偏计划_v0.1.md` 的 1.5，补存在场景绝对坐标读取 / 写入 / 单参考换算 helper。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_存在类逻辑纠偏计划_v0.1.md
实施记录/20260620_世界树场景辅助函数归属判定表.md
```

## 1. 问题锚点

```text
世界树类::读取存在场景绝对坐标
世界树类::写入存在场景绝对坐标
世界树类::计算场景绝对坐标_由参考存在相对坐标
世界树类::写入存在场景绝对坐标_由参考存在相对坐标
```

当前判断：

```text
这些函数属于存在空间状态读取 / 写入 / 单参考换算职责，应先迁出业务细节到存在类。
本切片不处理相对坐标表 DTO / 批量换算，不新增存在主信息字段，不用自由文本承载坐标状态。
```

验证前预测：

```text
1. 存在类.h / 存在类.cpp 新增读取 / 写入 / 单参考换算 helper。
2. 只读读取不隐式创建特征；写入路径显式写入存在坐标相关特征结构。
3. 函数说明文档生成退出码应为 0。
4. Debug x64 构建应通过。
```

## 2. 当前执行进展

已修改：

```text
1. 存在类.h 新增读取存在场景绝对坐标、写入存在场景绝对坐标、按参考存在相对坐标计算和写入的公开 helper 声明。
2. 存在类.cpp 新增坐标特征入口、坐标 I64 化、只读 I64 读取、显式特征写入和坐标来源指针写入内部 helper。
3. 存在类::读取存在场景绝对坐标 只读取存在节点上的坐标明确状态和 X / Y / Z 特征，不创建特征。
4. 存在类::写入存在场景绝对坐标 显式写入 X / Y / Z、明确状态、时间戳、置信度特征，并在全部成功后同步最近观测缓存。
5. 存在类::写入存在场景绝对坐标_由参考存在相对坐标 要求目标和参考存在位于同一场景，写入绝对坐标后写入坐标来源指针特征。
```

已验证：

```text
rg -n "读取存在场景绝对坐标|写入存在场景绝对坐标|计算场景绝对坐标_由参考存在相对坐标|存在场景绝对坐标" 存在类.h 存在类.cpp 世界树类.cpp
结果：存在类.h / 存在类.cpp 已出现新增 helper；世界树旧函数仍保留，等待返回场景类 P5.2a 改薄封装。

python .\tools\生成函数功能说明文档.py
结果：exit=0，输出见 日志/20260620_存在场景绝对坐标helper_函数文档.log。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：exit=0，构建日志见 日志/20260620_存在场景绝对坐标helper_msbuild.log；0 警告。
```

当前判断：

```text
存在类 1.5 第一优先缺口已闭合。
下一步应返回 计划/20260620_场景类逻辑纠偏计划_v0.1.md 的 1.4 P5.2a：回迁世界树存在场景绝对坐标薄封装。
```

禁止宣称：

```text
不宣称存在类逻辑纠偏完成。
不宣称世界树场景辅助函数迁移完成。
不宣称场景类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
