# 20260620 二次特征信息操作入口判定表

扫描日志：`日志/20260620_二次特征信息操作入口扫描.txt`

扫描摘要：

```text
scan1_exit=0，scan1_count=280
scan2_exit=0，scan2_count=22
```

## 1. 文件分布

| 文件 | 命中数 | 初判 |
| --- | ---: | --- |
| `二次特征类.cpp` | 131 | owning class 内部实现，默认保留；若被外部绕过再补函数。 |
| `二次特征应用模块.ixx` | 76 | 已确认为 `export module 二次特征应用模块` 的二次特征匹配 / 应用内部；当前外部机器调用未绕过二次特征类直接解包，保留为 owning 模块内部实现。 |
| `语言类_二次特征表达.cpp` | 13 | 语言表达层；不得反向参与机器判断，后置显示读取收口。 |
| `世界树类.cpp` | 12 | SQL / 世界树投影为主；显示 / SQL 后置收口，不作为当前机器切片。 |
| `基础信息主信息类.h` | 11 | 二次特征主信息定义；保留。 |
| `方法类.cpp` | 9 | 机器逻辑：方法结果能力方向匹配直接解包二次特征方向和值，第一优先回迁。 |
| `任务模块.筹办.ixx` | 8 | 机器逻辑：需求目标方向编码直接解包二次特征方向和值，第一优先回迁。 |
| `自我动作实现.外设模块.ixx` | 8 | 直接 new 并拼装二次特征主信息；登记后续创建入口收口切片。 |
| `需求类.cpp / 需求类.h` | 9 | 已有方向签名材料回迁后的枚举类型使用为主；后续复扫确认无主信息字段直读。 |
| `状态类.cpp` | 4 | 机器逻辑：统计状态引用数量直接遍历二次特征来源状态；后续补二次特征类引用计数 / 引用枚举入口。 |
| `自我线程模块.impl.cpp` | 4 | 机器逻辑候选：读取二次特征差值 / 标量值形成治理摘要；后续补只读值入口。 |
| `控制面板类.cpp` | 3 | 控制面板显示层；后置显示读取收口。 |
| `基础信息类.cpp` | 5 | P8f 已回迁：概念模板创建和概念入口引用判断改为调用二次特征类入口，不再直接创建或解释二次特征主信息。 |
| `场景类.cpp / 场景类.h / 世界树类.h / 语素类.cpp / 语言类_基础信息命名.cpp` | 5 | 小量显示、声明或归属判断命中；后续按具体语义分类。 |

## 2. 第一优先机器逻辑切片

| 锚点 | 当前问题 | 处理方向 | 验证 |
| --- | --- | --- | --- |
| `方法类.cpp:633-645` | 方法结果能力匹配直接读取 `方向区间 / 差值 / 值形态 / 离散编码 / 是否满足` 并生成比较结果编码。 | 在 `二次特征类` 增加只读编码函数，`方法类` 改为调用该函数，不直接解释二次特征主信息字段。 | 复扫 `方法类.cpp` 中上述字段直读清零；构建通过。 |
| `任务模块.筹办.ixx:495-512` | 任务筹办需求目标方向编码直接 dynamic_cast 二次特征主信息并读取方向 / 差值 / 值形态。 | 同步调用 `二次特征类` 只读编码函数；不使用词面文本。 | 复扫 `任务模块.筹办.ixx` 中上述字段直读清零；构建通过。 |

## 3. 后续登记项

| 锚点 | 初判 | 后续切片 |
| --- | --- | --- |
| `状态类.cpp:624-628` | 状态引用计数直接遍历二次特征来源状态和左右对象，是机器逻辑。 | 补二次特征类“统计引用状态次数”或“枚举引用状态节点”入口后回迁。 |
| `自我动作实现.外设模块.ixx:13225-13251` | 外设动作实现直接 new 二次特征主信息并拼装观察关系二次特征。 | 补二次特征类创建观察关系二次特征入口，调用方只传结构化主体、客体、来源动态、场景和值。 |
| `自我线程模块.impl.cpp:6623,14562,14583,14593` | 自我线程直接读取差值 / 标量值形成治理摘要。 | 补二次特征类只读值入口后回迁。 |
| `基础信息类.cpp:298-332` | 基础信息层直接创建二次特征概念模板并比较概念名称。 | P8f 已补二次特征类概念模板主信息创建 / 概念入口引用判断入口后回迁。 |
| `世界树类.cpp:523-587` | SQL 引用 / 显示投影。 | 显示 / SQL 后置收口，不作为当前机器逻辑切片。 |
| `语言类_二次特征表达.cpp`、`语言类_基础信息命名.cpp`、`控制面板类.cpp` | 语言和显示层。 | 后置为二次特征类结构化视图读取；不得反向参与机器判断。 |

## 4. 当前执行决定

先执行第一优先机器逻辑切片：新增二次特征方向比较结果编码只读函数，并回迁 `方法类.cpp` 与 `任务模块.筹办.ixx`。

本判定表不宣称二次特征类逻辑纠偏完成；后续登记项仍需按计划索引继续执行。
