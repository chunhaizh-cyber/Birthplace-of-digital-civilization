# 20260703 观察材料质量收束 Codex断点清单

## 0. 恢复摘要

当前切片：

```text
E2/P4 `观察材料质量收束实现映射计划` S4 识别 / 扫描 / 跟踪提交包读取状态码已闭合。
```

当前判断：

```text
S0 已闭合；真实入口和字段类型已核查。
S1 已新增 `计算观察材料质量收束状态(...)`，可从报告项、兼容质量判定、等待项和材料句柄解析结果收束 I64 六态质量状态。
S2 已新增 `收集观察材料质量缺口码(...)`，可把采集失败、过期、缺材料、低质量、深度无效和空间坐标无效映射到现有外设提交缺口码。
S3 已让提交包构造路径按材料页解析结果过滤可回查句柄，不再把非空句柄直接当成可回查材料。
S4 已在代码中让识别、扫描、跟踪提交包构造读取六态质量状态、能力等级和质量缺口集合；`check_specs.py` 结果 hard issues = 0 / warnings = 0，Debug x64 构建 0 警告 / 0 错误。
目标约束材料失配仍缺专用缺口码，第一版不新增值域，已登记为 S5 / D5 后续债务。
```

下一步：

```text
回写计划索引并提交 S4；随后继续 S5 目标观察约束兼容债务登记。
```

禁止宣称：

```text
不宣称观察材料质量收束已完整代码实现。
不宣称质量状态已完整接入四方法、体素融合前置闸或结构化缺口链。
不宣称真实 D455 运行验证已完成。
不宣称质量状态已写入世界树事实。
```

## 1. S0 只读代码事实扫描

计划入口：

```text
计划/20260703_观察材料质量收束实现映射计划_v0.1.md
规范/详细设计/待完成/20260703_观察材料质量收束详细设计_v0.1.md
```

代码事实：

```text
外设观察报告队列.ixx:112 枚举_观察材料等级 存在，值域为 未定义 / 不合格 / 可用 / 优质。
外设观察报告队列.ixx:119 枚举_外设材料能力等级 存在，值域为 未定义 / 候选或缺口 / 平面轮廓 / 深度锚定 / 空间可用。
外设观察报告队列.ixx:191 枚举_外设提交缺口码 存在，包含缺报告、缺材料句柄、材料过期、低质量、深度无效、空间坐标无效、外设采集失败等。
外设观察报告队列.ixx:633 结构_观察材料质量判定 存在，但仍包含可进入验证方法、可进入识别方法、可生成扫描变化候选、可生成跟踪动态候选等兼容布尔位。
外设观察报告队列.ixx:2049 判断外设观察材料质量 存在，读取报告项、等待项、簇材料、跨帧稳定、锚点、深度、空间和目标约束使用状态，输出兼容材料等级、能力等级和布尔位。
外设观察报告队列.ixx:2708 解析D455观察材料句柄 存在，可作为材料可回查检查入口。
外设观察报告队列.ixx:2754 读取D455簇三维体素轮廓视角 存在，可作为局部体素 / 轮廓材料读取入口。
外设观察报告队列.ixx:2371 / 2432 / 2548 分别存在识别、扫描、跟踪提交包构造入口。
外设线程_D455深度相机.ixx:1513-1525 把 D455 调用结果中的深度有效率、空间坐标有效率、帧质量评分、低质量区域、未解释区域、稳定锚点和边界锚点覆盖率写入报告项。
双目相机本能适配器.h:184-216 已有深度有效率、空间坐标有效率、帧质量评分、轮廓支持、低置信轮廓数量、条件不足候选数量、待验证候选数量和部分确认候选数量等输入材料。
```

缺口：

```text
未找到 `计算观察材料质量收束状态`。
未找到 `收集观察材料质量缺口码`。
现有 `结构_观察材料质量判定` 布尔位只能作为兼容债务和运行期输入，不能直接替代 1175 六态质量状态。
```

已运行命令：

```text
rg -n "判断外设观察材料质量|构造观察材料质量判定摘要|结构_观察材料质量判定|枚举_观察材料等级|枚举_外设材料能力等级|外设材料可承接状态|平面轮廓稳定状态|深度锚定状态|空间可用状态" 外设观察报告队列.ixx 说明书/函数功能说明文档.md
rg -n "枚举_外设提交缺口码|缺报告|缺原始观察帧|缺深度帧|缺彩色帧|缺对齐帧|缺空间候选|缺像素归属账本|缺材料句柄|材料过期|低质量|深度无效|空间坐标无效" 外设观察报告队列.ixx 外设线程_D455深度相机.ixx
rg -n "深度有效率|空间坐标有效率|帧质量评分|轮廓闭合率|轮廓断裂数量|轮廓深度支持率|轮廓颜色支持率|轮廓空间支持率|融合轮廓置信度|稳定锚点|未解释区域|低置信|条件不足|待验证|部分确认|深度稳定|空间连续|范围稳定" 外设观察报告队列.ixx 外设线程_D455深度相机.ixx 双目相机本能适配器.h 双目相机本能适配器.cpp
rg -n "构造外设识别提交包_由观察报告|构造外设扫描提交包_由观察报告|构造外设跟踪提交包_由观察报告|D455_发布目标跟踪报告|D455_发布扫描变化报告|读取D455簇三维体素轮廓视角|解析D455观察材料句柄" 外设观察报告队列.ixx 外设线程_D455深度相机.ixx 自我动作实现.外设模块.ixx 自我动作实现.扫描.ixx
rg -n "计算观察材料质量收束状态|收集观察材料质量缺口码|质量收束状态|材料质量状态|观察材料质量收束" 外设观察报告队列.ixx 外设线程_D455深度相机.ixx 双目相机本能适配器.h 双目相机本能适配器.cpp 自我动作实现.外设模块.ixx 自我动作实现.扫描.ixx 说明书/函数功能说明文档.md
```

关键退出码：

```text
rg 质量判定入口：0
rg 缺口码：0
rg 质量输入材料：0
rg 提交包 / 句柄入口：0
rg 六态收束函数 / 缺口收集函数：1，表示未找到目标函数
```

S0 结算：

```text
S0 已闭合；入口一致，不需要修订计划；进入 S1。
```

## 2. S1 兼容质量状态只读函数

计划入口：

```text
计划/20260703_观察材料质量收束实现映射计划_v0.1.md
规范/详细设计/待完成/20260703_观察材料质量收束详细设计_v0.1.md
```

代码变更：

```text
外设观察报告队列.ixx:748 新增 `计算观察材料质量收束状态(...)` 导出声明。
外设观察报告队列.ixx:2305 新增 `计算观察材料质量收束状态(...)` 实现。
说明书/函数功能说明文档.md:7037 已登记该函数说明。
```

实现口径：

```text
函数返回 I64 六态：
0 未评估；
1 可用；
2 低质量；
3 不可用；
4 部分可用；
5 需复验。

函数只读取报告项、兼容质量判定、等待项和 `解析D455观察材料句柄` 结果。
函数不新增结构体、不新增普通布尔值、不写世界树事实、不改变 D455 采集线程行为。
句柄可用性按 `解析D455观察材料句柄` 判断，不把非空字符串句柄直接当成可回查材料。
```

已运行命令：

```text
python .\tools\生成函数功能说明文档.py
git diff --check -- '外设观察报告队列.ixx' '说明书/函数功能说明文档.md'
python .\tools\check_specs.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

关键退出码 / 结果：

```text
函数说明文档生成：0
git diff --check：0；仅提示 `说明书/函数功能说明文档.md` 下次 Git 触碰会把 CRLF 规范化为 LF。
check_specs：hard issues = 0；warnings = 0。
Debug x64 构建：0 警告；0 错误。
```

S1 结算：

```text
S1 已闭合；六态函数可编译，未发现状态映射冲突。
下一步进入 S2，新增或复用 `收集观察材料质量缺口码`，把质量状态映射到现有外设提交缺口码。
```

## 3. S2 质量缺口映射

计划入口：

```text
计划/20260703_观察材料质量收束实现映射计划_v0.1.md
规范/详细设计/待完成/20260703_观察材料质量收束详细设计_v0.1.md
```

代码变更：

```text
外设观察报告队列.ixx:752 新增 `收集观察材料质量缺口码(...)` 导出声明。
外设观察报告队列.ixx:2414 新增 `收集观察材料质量缺口码(...)` 实现。
说明书/函数功能说明文档.md:2109 已登记该函数说明。
```

实现口径：

```text
函数返回 `std::vector<枚举_外设提交缺口码>`，不返回字符串缺口。
函数复用 `计算观察材料质量收束状态(...)` 和 `解析D455观察材料句柄`，句柄非空但解析失败时仍进入缺口。
函数不新增结构体、不新增普通布尔值、不写世界树事实、不改变 D455 采集线程行为。
```

缺口映射结果：

```text
采集失败 -> 外设采集失败。
报告过期 / 目标约束截止时间过期 / 材料页已过期 -> 材料过期。
无报告 / 报告类型未指定 -> 缺报告。
核心材料句柄缺失 -> 缺原始观察帧 / 缺深度帧 / 缺彩色帧 / 缺对齐帧 / 缺空间候选 / 缺像素归属账本。
簇级材料不可回查或缺局部材料 -> 缺材料句柄。
低帧质量、低质量区域、不可提交簇、兼容质量判定不合格或候选/缺口能力 -> 低质量。
深度有效率不足 -> 深度无效。
空间坐标有效率不足或坐标系不一致 -> 空间坐标无效。
期望报告类型与实际报告类型冲突 -> 报告类型不匹配。
```

值域债务：

```text
现有 `枚举_外设提交缺口码` 没有“目标约束材料失配”专用值。
第一版对 `目标约束使用状态 >= 3` 保守归入 `低质量`，不新增值域。
该缺口留给 S5 目标观察约束兼容债务和后续 D5 结构化缺口链处理。
```

已运行命令：

```text
python .\tools\生成函数功能说明文档.py
git diff --check -- '外设观察报告队列.ixx' '说明书/函数功能说明文档.md'
python .\tools\check_specs.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

关键退出码 / 结果：

```text
函数说明文档生成：0
git diff --check：0；仅提示 `说明书/函数功能说明文档.md` 下次 Git 触碰会把 CRLF 规范化为 LF。
check_specs：hard issues = 0；warnings = 0。
Debug x64 构建：0 警告；0 错误。
```

S2 结算：

```text
S2 已闭合；质量缺口收集函数可编译。
下一步进入 S3，确认可回查材料检查是否已接入提交包构造路径，并避免把非空句柄直接当成可回查。
```

## 4. S3 可回查材料检查接入

计划入口：

```text
计划/20260703_观察材料质量收束实现映射计划_v0.1.md
规范/详细设计/待完成/20260703_观察材料质量收束详细设计_v0.1.md
```

代码变更：

```text
外设观察报告队列.ixx:927 新增 `外设提交_材料句柄可回查(...)`。
外设观察报告队列.ixx:933 新增 `外设提交_追加可回查句柄(...)`。
外设观察报告队列.ixx:984 新增 `外设提交_可回查簇材料句柄集(...)`。
外设观察报告队列.ixx:1016 新增 `外设提交_追加包头质量缺口(...)`。
`外设提交_汇总报告材料(...)` 改为只追加可回查材料句柄。
`外设提交_填充报告缺口(...)` 合并 `收集观察材料质量缺口码(...)`。
识别提交包 ROI / 掩码 / 点集 / 深度切片句柄、候选材料句柄集改为只带出可回查句柄。
扫描结果和跟踪包当前句柄改为可回查才带出。
```

实现口径：

```text
不在 D455 高频采集路径全量解析句柄。
只在提交包构造路径和质量收束准备输出时解析材料页状态。
非空句柄不再直接进入提交包材料集合。
过期或不可解析句柄通过包头缺失材料 / 质量问题缺口集合表达。
```

已运行命令：

```text
python .\tools\生成函数功能说明文档.py
git diff --check -- '外设观察报告队列.ixx' '说明书/函数功能说明文档.md'
python .\tools\check_specs.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

关键退出码 / 结果：

```text
函数说明文档生成：0
git diff --check：0；仅提示 `说明书/函数功能说明文档.md` 下次 Git 触碰会把 CRLF 规范化为 LF。
check_specs：hard issues = 0；warnings = 0。
Debug x64 构建：0 警告；0 错误。
真实 D455 自然样本验证：未执行。
```

S3 结算：

```text
S3 已闭合；提交包构造路径已接入可回查材料检查。
下一步进入 S4，让识别 / 扫描 / 跟踪提交包读取六态质量状态和缺口集合。
```

## 5. S4 识别 / 扫描 / 跟踪提交包读取状态码

计划入口：

```text
计划/20260703_观察材料质量收束实现映射计划_v0.1.md
规范/详细设计/待完成/20260703_观察材料质量收束详细设计_v0.1.md
```

代码变更：

```text
外设观察报告队列.ixx 新增内部 I64 六态质量状态常量，复用 1175 六态值域：未评估、可用、低质量、不可用、部分可用、需复验。
新增内部 helper `外设提交_合并缺口集合(...)`，把 `收集观察材料质量缺口码(...)` 的结果合并到包体缺口向量。
新增内部 helper `外设提交_能力等级达到(...)` 和 `外设提交_质量状态允许承接(...)`，让包体构造同时读取六态质量状态和能力等级。
`构造外设识别提交包_由观察报告(...)` 读取质量状态 / 能力等级 / 缺口集合；候选有效性不再只由 `簇.可提交观察区域` 决定。
`构造外设扫描提交包_由观察报告(...)` 读取质量状态 / 能力等级 / 缺口集合；质量不允许承接时，输出匹配状态转为需重识别，并把质量缺口写入包体和扫描结果原因。
`构造外设跟踪提交包_由观察报告(...)` 读取质量状态 / 能力等级 / 缺口集合；质量不允许承接时，跟踪状态转为证据不足。
说明书/函数功能说明文档.md 已重新生成，登记新增内部 helper。
```

实现口径：

```text
本切片不新增结构体、不新增普通布尔值字段、不删除兼容布尔位。
兼容布尔位仍作为旧输入保留，但识别 / 扫描 / 跟踪提交包不再只依赖这些布尔位作为唯一机器门。
质量状态只作为运行期收束状态参与提交包构造，不写世界树事实、不确认稳定存在、不确认需求满足。
```

已运行命令：

```text
python .\tools\生成函数功能说明文档.py
git diff --check -- '外设观察报告队列.ixx' '说明书/函数功能说明文档.md' '实施记录/20260703_观察材料质量收束_Codex断点清单.md'
python .\tools\check_specs.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

关键退出码 / 结果：

```text
函数说明文档生成：0。
git diff --check：0；仅提示 `说明书/函数功能说明文档.md` 下次 Git 触碰会把 CRLF 规范化为 LF。
check_specs：hard issues = 0；warnings = 0。
Debug x64 构建：0 警告；0 错误。
真实 D455 自然样本验证：未执行。
```

当前结算：

```text
S4 已闭合；提交包体已读取状态码、能力等级和缺口集合。
下一步进入 S5，登记目标观察约束兼容债务；S5 不新增目标观察约束结构，不把目标约束布尔值扩张为新机器协议。
```
