# 20260605_安全因果链计划按外设四本能方法修正记录

## 目标

根据 `计划/20260605_外设四本能方法需求树生成因果链修订计划_v0.1.md`，修正旧待完成项 `计划/待完成项/20260601_安全因果链反推当前场景特征值需求纠偏计划_v0.2.md` 的现行因果链口径。

本次只修计划，不改代码。

## 依据

```text
规范/规范目录.md
计划/计划索引.md
规范/当前场景特征值明确标准规范20260602.md
规范/观察像素簇与存在候选分层规范20260527.md
计划/20260605_外设四本能方法需求树生成因果链修订计划_v0.1.md
```

## 修正内容

1. 将旧计划的现行执行入口改为 `计划/20260605_外设四本能方法需求树生成因果链修订计划_v0.1.md`。
2. 明确旧 `S6-N2a-P7 D455路径内部标准子需求生长` 不再作为直接下一步。
3. 将 D455 路径从旧的候选验证 / 确认存在主干，修正为：

```text
缺观察材料
-> 当前观察特征帧取得状态
-> 外设观察存在 / 观察材料集可用
-> 外设识别结果场景状态
-> 当前观察范围可观测单位存在对应事实可提交
-> 提交_当前观察范围可观测单位存在对应事实
-> 当前观察范围可观测单位存在对应闭合状态 / 扫描可启动状态
```

4. 将扫描和跟踪改为条件具备后的并行需求，不作为 `当前场景特征值明确状态` 的完成子需求。
5. 明确 P0 先对账四方法目标特征是否能命中结果能力：

```text
当前观察特征帧取得状态 -> 双目相机_取得当前观察特征帧
外设识别结果场景状态 -> 自我_识别外设观察材料
外设扫描结果场景状态 -> 自我_扫描已识别区域
外设跟踪结果场景状态 -> 自我_跟踪指定存在
```

## 保留边界

```text
需求树生成目标特征需求，不生成方法调用需求。
任务筹办通过统一结果能力查找方法，不直接调用四方法。
外设报告、材料页、等待项和传输容器只作材料来源。
data/initial_causal_templates.tsv 只存被动因果。
第一次扫描只能写发现存在或发现特征，不能写发现动态。
```

## 验证

本次为计划文件修订，未运行构建。

已执行差异检查：

```text
git diff --stat -- 计划/待完成项/20260601_安全因果链反推当前场景特征值需求纠偏计划_v0.2.md
```
