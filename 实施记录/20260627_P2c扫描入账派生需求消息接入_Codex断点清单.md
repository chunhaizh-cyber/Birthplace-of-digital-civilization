# 20260627 P2c 扫描入账派生需求消息接入 Codex断点清单

## 0. 恢复摘要

当前切片：

```text
P1c 因果链驱动扫描需求自然生长计划 v0.1 / P2c
扫描入账派生需求消息最小接入。
```

本切片已完成代码接入：

```text
1. 在稳定子集扫描约束消费处，为每个可用目标观察约束构造一条现有
   `结构_任务管理派生需求消息`，再封装为现有 `结构_任务管理上行消息`。

2. 派生需求消息字段包含：
      父需求
      目标主体
      目标特征 = 已识别区域扫描入账状态
      当前状态
      目标状态
      满足关系 = 等于
      当前方法

3. 补齐 `私有_任务管理上行消息幂等键(...)` 对派生需求消息的幂等键分支。

4. 状态节点物化不使用目标观察约束字段冒充状态节点；
   当前 / 目标状态由世界树状态节点承载。

5. 按最新场域口径，扫描目标存在属于外部场景相关主体，
   P2c 新状态节点 helper 已改为当前现实场景优先；
   自我内部世界只作为无现实场景时的兜底。
```

当前验证：

```text
python .\tools\生成函数功能说明文档.py
    退出码：0
    函数条目：7215

git diff --check
    退出码：0
    结果：无空白错误；仅有 CRLF/LF 规范化 warning。

python .\tools\check_specs.py
    退出码：0
    hard issues：0
    warnings：73，按当前判断为存量规范治理告警。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码：0
    结果：0 warnings，0 errors。
```

真实运行预测：

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000

若自然命中稳定子集扫描约束：
    1. 日志出现 `任务管理界面线程/稳定子集全量扫描等待项承接`。
    2. `扫描入账派生需求消息生成数量 > 0`。
    3. 任务管理上行消息进入自我线程派生需求落账入口。
    4. 自我线程出现派生需求入树 / `提交_生成缺口子需求` 成功证据。
    5. 目标特征应为 `已识别区域扫描入账状态`。

若自然样本未命中：
    只可标记为 P2c 代码接入、静态检查和构建闭合；
    不得宣称扫描需求自然生长业务闭合；
    不得用旧日志、伪造样本、外设等待项数量或历史 TSV 替代行为证据。
```

禁止宣称：

```text
不宣称扫描需求自然生长闭合。
不宣称外设等待项提交等于需求已生成或需求已满足。
不宣称 P2c 已进入 P4 业务验证。
不宣称自我苏醒完成或初步成熟完成。
```

## 1. 真实运行结果

命令：

```powershell
.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000
```

结果：

```text
退出码：0
日志文件：日志/鱼巢_run_20260627.低值g
异常检索：日志/鱼巢_exception_20260627.低值g 未检出派生需求 / 扫描入账 / 稳定子集相关异常。
```

预测核对：

```text
P2c 目标自然样本未命中。

检索 `任务管理界面线程/稳定子集全量扫描等待项承接`
和 `扫描入账派生需求消息生成数量`：
    Count = 0

因此本次只证明：
    P2c 代码接入完成；
    函数说明文档刷新完成；
    静态清洁通过；
    规范治理检查 hard issues 为 0；
    Debug x64 构建通过；
    真实运行退出码为 0，未暴露相关异常。

不得证明：
    稳定子集扫描入账派生需求自然上行已命中；
    自我线程已接到该类派生需求并正式入树；
    扫描需求自然生长业务闭合。
```

当前判断：

```text
P2c 可标记为代码接入 / 工程验证闭合。
行为证据按计划后置到 P4，等待自然稳定子集扫描约束样本命中。

下一步进入 P3：任务筹办调用边界收口。
```
