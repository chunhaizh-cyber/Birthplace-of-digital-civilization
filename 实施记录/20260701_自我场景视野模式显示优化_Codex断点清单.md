# 20260701 自我场景视野模式显示优化 Codex 断点清单

## 0. 恢复摘要

当前切片：

```text
按用户确认的方法 B，优先接入真实视野数据源，再实现自我场景窗口视野模式。
```

当前判断：

```text
D455 原始 / 轻量观察帧已有深度内参，可派生 FOV。
控制面板快照当前没有相机姿态四元数 / FOV 字段。
外设模块已有“自我_场景绝对姿态明确状态”，但未发现可直接渲染的真实姿态数值生产者。
```

下一步：

```text
1. 补 FOV 只读承接链。
2. 补控制面板 JSON 视野元数据。
3. 补 WebGL 视野模式、鼠标旋转和彩色 / 灰度渲染。
4. 构建验证。
```

禁止宣称：

```text
不宣称真实相机姿态链路已闭合。
不宣称观察、扫描、跟踪、事实提交或自我苏醒阶段完成。
不把 UI 彩色 / 灰度显示反写为世界树事实、需求满足、动作动态或安全 / 服务结算。
```

## 1. 依据文件

```text
AGENTS.md
规范/0050_项目通用结构与禁止性规则总纲_20260701.md
规范/6220_子规范_自我所在场景认知工作区_20260701.md
规范/6250_子规范_场景体素服务与视觉先验融合_20260701.md
规范/6320_子规范_外设观察特征与自我场景认知分层_20260701.md
docs/工程图谱/02_外设观察事实图谱.md
docs/工程图谱/05_规则原子索引.md
计划/20260701_自我场景视野模式显示优化计划_v0.1.md
```

## 2. 已运行命令

```text
git status --short --untracked-files=no
rg -n "结构体_相机状态|相机\\.有效|\\.相机|相机状态|姿态|设备时间_微秒|IMU|ViewKey|视角键" ...
rg -n "intrinsic|intrinsics|内参|fx|fy|ppx|ppy|rs2_intrinsics|FOV|fov|视场|视角|焦距" ...
python .\tools\生成函数功能说明文档.py
git diff --check
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

关键结果：

```text
开工前工作区无本切片外的相关修改。
D455相机类.cpp 已写入 结构体_原始场景帧.深度内参，可派生 FOV。
双目相机本能适配器原先使用深度内参生成点云 / 空间候选，本切片把 FOV 作为只读视野元数据继续向外设观察摘要和控制面板传播。
函数说明文档生成成功：源码文件 129，函数条目 7555。
git diff --check 退出码 0；仅提示 控制面板类.cpp 和 说明书/函数功能说明文档.md 下次 Git 接触时会做 CRLF/LF 规范化。
Debug x64 msbuild 退出码 0：已成功生成，0 个警告，0 个错误。
```

## 3. 已改文件

```text
双目相机本能适配器.h
双目相机本能适配器.cpp
自我动作实现.外设模块.ixx
控制面板类.ixx
控制面板类.cpp
说明书/函数功能说明文档.md
计划/计划索引.md
计划/20260701_自我场景视野模式显示优化计划_v0.1.md
实施记录/20260701_自我场景视野模式显示优化_Codex断点清单.md
```

## 4. 验证记录

```text
1. 初始构建曾暴露 控制面板类.cpp 内自我场景窗口 HTML 原始字符串过长问题；已把自我场景面板附近 HTML/JS 分段输出，避免 MSVC raw string 体积限制。
2. 最终 Debug x64 构建通过，0 警告，0 错误。
3. --panel-html 曾成功运行，但该命令生成的是主 SQL 控制面板静态 HTML，不是独立自我场景窗口；生成物 控制面板.html 已恢复，未纳入本切片提交。
4. 本轮未伪造相机姿态数值；控制面板只暴露既有 自我_场景绝对姿态明确状态。
```

## 5. 本轮完成和待条件

已完成：

```text
1. D455 深度内参派生 水平视野角毫度 / 垂直视野角毫度 / 视野参数状态。
2. 外设观察结果、已落账观察摘要、场景复现上下文和控制面板 JSON 承接视野元数据。
3. 控制面板 JSON 新增 viewFovHorizontalMilliDeg、viewFovVerticalMilliDeg、viewParameterState、cameraPoseState、viewSignature。
4. 自我场景独立窗口新增视野模式；开启后停止自动旋转，鼠标拖拽只改变显示观察角。
5. 视野模式下按固定 FOV / viewSignature 判定视野内外：视野内保持彩色，视野外转灰度；鼠标旋转不重算视野内外。
```

待条件：

```text
S6 真实姿态闭合仍缺正式相机姿态数值生产者。后续只有补齐四元数 / 位姿生产和承接链后，才能宣称真实相机姿态变化会驱动视野更新闭合。
```

禁止扩大宣称：

```text
不把 UI 彩色 / 灰度反写为世界树事实、需求满足、动作动态或安全 / 服务结算。
不宣称观察链、扫描链、跟踪链、真实相机姿态链路、自我苏醒或初步成熟完成。
```
