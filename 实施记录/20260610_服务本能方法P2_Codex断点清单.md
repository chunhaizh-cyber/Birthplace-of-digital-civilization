# 服务本能方法 P2 Codex断点清单

日期：2026-06-10

## 当前切片

```text
服务本能方法实现计划 P2：
    注册并实现字段补齐类服务叶子方法。
```

## 依据文件

```text
AGENTS.md
规范/规范目录.md
计划/计划索引.md
实施记录/20260610_服务本能方法实现方案记录.md
实施记录/20260610_服务本能方法P0P1_Codex断点清单.md
```

## 允许修改范围

```text
本能方法类.h
本能方法类.cpp
自我动作实现.服务模块.ixx
自我动作实现.内部模块.ixx
自我动作实现模块.ixx
实施记录/20260610_服务本能方法P2_Codex断点清单.md
计划/计划索引.md
```

## 禁止修改范围

```text
任务主信息类
需求主信息类
特征值主信息类
任务筹办 / 任务执行 本能方法化
服务事件E独立虚拟存在
裸写服务值
G0 / D2 结算实现
```

## 进入时工作区状态

```text
P0/P1 已提交：8c3673a
存在提交前已有无关修改：
    AGENTS.md
    任务模块.管理工作线程.ixx
    控制面板.html
    实施记录/20260610_观察子任务完成推进_Codex断点清单.md
    消息中间件/

本切片不得混入上述文件。
```

## 预期结果

```text
1. 新增字段补齐类服务本能方法 ID 和名称映射。
2. 服务模块实现字段补齐类叶子动作。
3. 内部模块/主模块注册这些动作入口。
4. 方法规格只声明读取具体字段值并写任务虚拟存在字段。
5. 不写“明确状态=已明确”。
6. Debug x64 构建通过。
```

## 已运行命令

```text
rg -n "服务_绑定任务|服务绑定任务|执行任务字段绑定|确认服务绑定任务" 本能方法类.h 本能方法类.cpp 自我动作实现.服务模块.ixx 自我动作实现.内部模块.ixx 自我动作实现模块.ixx
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\鱼巢_build_20260610_服务模块P2.log
```

## 关键退出码

```text
Debug x64 构建：ExitCode=0
关键错误检索：未检出 error C / fatal error / error MSB / warning C
```

## 已完成修改

```text
1. 新增 8 个字段补齐类服务本能方法 ID：
   服务_绑定任务交互者 / 权限者 / 服务对象 / 权限边界 / 目标宿主 / 目标特征类型 / 目标值 / 输入场景。
2. 默认本能方法表已登记上述方法，分类为自我内部动作。
3. 内部本能函数集、内部变化判定、运行时注册闭环已接入上述方法。
4. 服务模块新增字段绑定 generic 执行器：
   - 从输入参数场景 / 调用信息 / 输入条件包读取 `任务信息节点` 和目标字段值。
   - 写入任务虚拟存在对应字段。
   - 记录字段来源方法虚拟存在。
   - 目标值支持指针值和 I64 值。
5. 主动作实现模块新增 8 个 wrapper 和规格确认函数。
6. 未写“明确状态=已明确”，未生成 G0 / D2，未写服务值。
```

## 当前判断

```text
P2 已闭合；字段补齐类服务叶子方法可构建。
下一步进入 P3：方法声明特征装配辅助函数。
```
