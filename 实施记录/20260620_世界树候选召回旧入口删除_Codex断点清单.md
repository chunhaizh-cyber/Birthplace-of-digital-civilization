# 20260620 世界树候选召回旧入口删除 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.4 P5.3b，删除世界树命名候选召回 DTO 和旧包装入口。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
实施记录/20260620_外设候选召回调用方切换_Codex断点清单.md
```

## 1. 问题锚点

```text
世界树类.h:
    结构_世界树场景轮廓查找参数
    结构_世界树场景轮廓存在比较结果
    世界树类::查找场景存在_按深度或彩图轮廓图 声明

世界树类.cpp:
    世界树类::查找场景存在_按深度或彩图轮廓图 定义
```

当前判断：

```text
外设调用方已切到场景类 DTO / helper，旧世界树 DTO 和包装入口可以删除。
本切片不处理场景相对坐标项 / 场景绝对坐标换算结果。
```

验证前预测：

```text
1. rg 不应再命中 结构_世界树场景轮廓查找参数 / 结构_世界树场景轮廓存在比较结果 / 查找场景存在_按深度或彩图轮廓图。
2. 函数说明文档生成退出码应为 0。
3. Debug x64 构建应通过。
```

## 2. 当前执行进展

已修改：

```text
1. 世界树类.h 删除 结构_世界树场景轮廓查找参数。
2. 世界树类.h 删除 结构_世界树场景轮廓存在比较结果。
3. 世界树类.h / 世界树类.cpp 删除 世界树类::查找场景存在_按深度或彩图轮廓图 声明和定义。
```

已验证：

```text
rg -n "结构_世界树场景轮廓查找参数|结构_世界树场景轮廓存在比较结果|查找场景存在_按深度或彩图轮廓图" 世界树类.h 世界树类.cpp 自我动作实现.外设模块.ixx 场景类.h 场景类.cpp
结果：无命中。

python .\tools\生成函数功能说明文档.py
结果：exit=0，输出见 日志/20260620_世界树候选召回旧入口删除_函数文档.log。

msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
结果：exit=0，构建日志见 日志/20260620_世界树候选召回旧入口删除_msbuild.log；0 警告。
```

当前判断：

```text
P5.3b 已闭合。
剩余世界树场景辅助相关命中为 场景相对坐标项 / 场景绝对坐标换算结果 / 相对坐标表批量换算函数，按判定表属于存在空间关系，下一步应转入存在类处理。
```

禁止宣称：

```text
不宣称世界树场景辅助函数迁移完成。
不宣称相对坐标表 DTO 已处理。
不宣称场景类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
