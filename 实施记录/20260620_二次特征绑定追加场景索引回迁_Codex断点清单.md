# 20260620 二次特征绑定追加场景索引回迁 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.2 P3.3c，回迁 `二次特征类::绑定场景` 中的二次特征索引追加路径，复用 `场景类::追加二次特征`。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
实施记录/20260620_动态创建追加场景索引回迁_Codex断点清单.md
```

本切片范围：

```text
1. 只回迁 二次特征类::绑定场景 中的二次特征索引追加路径。
2. 删除回迁后无调用的 二次特征类.cpp 私有_追加二次特征索引。
3. 同步精简绑定场景内部阶段日志中已不存在的索引查重 / 追加细分字段。
4. 不处理 语素类 场景索引挂载路径。
```

已实施：

```text
1. 二次特征类::绑定场景 改为通过 `场景类::追加二次特征(所属场景, 节点)` 维护场景二次特征事实目录。
2. 删除无调用的 二次特征类.cpp 私有_追加二次特征索引。
3. 精简二次特征绑定场景内部阶段日志，删除已不存在的二次特征索引查重 / 追加细分字段，保留外层索引追加耗时。
```

已运行：

```powershell
python .\tools\生成函数功能说明文档.py *> 日志\20260620_二次特征绑定追加场景索引回迁_函数文档.log
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\20260620_二次特征绑定追加场景索引回迁_msbuild.log
```

验证结果：

```text
函数文档生成退出码：0
函数文档统计：源码文件 128；函数条目 6894
函数文档删除条目：私有_追加二次特征索引 已无命中
Debug x64 构建退出码：0
构建关键日志：已成功生成；0 个错误
```

当前结论：

```text
P3.3c 已闭合。
下一步进入 P3.3d：复扫剩余 场景节点主信息类 字段直读 / 直写路径，重新分类下一优先。
```

禁止宣称：

```text
不宣称所有场景主信息直读已回迁。
不宣称场景类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
