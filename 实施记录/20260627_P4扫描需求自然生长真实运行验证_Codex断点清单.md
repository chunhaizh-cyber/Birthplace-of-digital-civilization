# 20260627 P4 扫描需求自然生长真实运行验证 Codex断点清单

## 0. 恢复摘要

当前切片：

```text
P1c 因果链驱动扫描需求自然生长计划 v0.1 / P4
真实运行验证。
```

验证前预测：

```text
命令：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000

若自然命中稳定子集扫描约束：
    1. `任务管理界面线程/稳定子集全量扫描等待项承接` 出现。
    2. `扫描入账派生需求消息生成数量 > 0`。
    3. 任务管理上行消息进入自我线程派生需求落账。
    4. `提交_生成缺口子需求` 成功。
    5. 目标特征为 `已识别区域扫描入账状态`。

若自然样本未命中：
    P4 不通过业务闭合；
    只记录真实运行退出码、异常状态和未命中锚点；
    当前计划转为待条件，不继续伪造样本或补历史 TSV。
```

禁止宣称：

```text
不宣称扫描需求自然生长闭合，除非本次真实运行命中上述业务锚点。
不把构建通过、无异常、外设等待项提交或旧日志当业务闭合证据。
不宣称自我苏醒完成或初步成熟完成。
```

## 1. 真实运行结果

命令：

```powershell
.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000
```

结果：

```text
退出码：0
日志文件：日志/鱼巢_run_20260627.低值g
异常检索：日志/鱼巢_exception_20260627.低值g 未检出派生需求 / 扫描入账 / 稳定子集相关异常。
```

锚点核对：

```text
`任务管理界面线程/稳定子集全量扫描等待项承接`
`扫描入账派生需求消息生成数量`
    Count = 0

最新派生需求入树命中：
    日志/鱼巢_run_20260627.低值g:11998-11999
    目标特征 = 方法最近连续成功次数
    不是 `已识别区域扫描入账状态`
```

当前判断：

```text
P4 真实运行退出码为 0，未暴露相关异常。
但 P4 业务锚点未自然命中，不能证明扫描需求自然生长闭合。

当前计划应按自然样本待条件停放：
    恢复条件 = 真实运行自然出现稳定子集扫描约束承接，
               且日志出现扫描入账派生需求消息上行、
               自我线程派生需求入树、
               `提交_生成缺口子需求` 成功、
               目标特征为 `已识别区域扫描入账状态`。
```

禁止宣称：

```text
不宣称 P4 通过。
不宣称扫描需求自然生长业务闭合。
不宣称 P2c 派生需求自然上行已命中。
```
