# 20260620 状态创建追加场景索引回迁 Codex 断点清单

## 0. 恢复摘要

当前切片：`计划/20260620_场景类逻辑纠偏计划_v0.1.md` 的 1.2 P3.3a，回迁 `状态类` 创建状态时的状态索引追加路径，复用既有 `场景类::追加状态`。

依据文件：

```text
AGENTS.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
计划/20260620_场景类逻辑纠偏计划_v0.1.md
实施记录/20260620_场景二次特征索引移除函数_Codex断点清单.md
```

本切片范围：

```text
1. 只回迁 创建内部状态 中的状态索引追加路径。
2. 删除回迁后无调用的 状态类::私有_按时间追加状态索引 声明和定义。
3. 不处理 状态类 内部其它状态索引清理 / 扫描路径。
4. 不新增 场景类 函数。
```

函数文档预查：

```text
已查 说明书/函数功能说明文档.md 和源码引用。
场景类::追加状态 已存在。
状态类::私有_按时间追加状态索引 仅有 创建内部状态 一个调用点。
```

验证前预测：

```text
1. 创建内部状态 改为调用 场景类::追加状态 后，新增状态仍会进入场景状态事实目录并按时间排序。
2. 删除 状态类::私有_按时间追加状态索引 后，函数文档中该条目应消失。
3. Debug x64 构建应通过。
```

禁止宣称：

```text
不宣称所有场景主信息直读已回迁。
不宣称场景类逻辑纠偏完成。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

## 1. 实施与验证

已实施：

```text
1. 状态类::创建状态 改为通过 `场景类::追加状态(场景, 节点)` 维护场景状态事实目录。
2. 删除无调用的 状态类::私有_按时间追加状态索引 声明和定义。
3. 保留创建状态后的动态刷新和状态写入后整理路径。
```

已运行：

```powershell
python .\tools\生成函数功能说明文档.py *> 日志\20260620_状态创建追加场景索引回迁_函数文档.log
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m *> 日志\20260620_状态创建追加场景索引回迁_msbuild.log
```

验证结果：

```text
函数文档生成退出码：0
函数文档统计：源码文件 128；函数条目 6895
函数文档删除条目：状态类::私有_按时间追加状态索引 已无命中
Debug x64 构建退出码：0
构建关键日志：已成功生成；0 个错误
```

当前结论：

```text
P3.3a 已闭合。
下一步进入 P3.3b：回迁 动态类 创建动态时的动态索引追加路径，优先复用已存在的 场景类::追加动态。
```
