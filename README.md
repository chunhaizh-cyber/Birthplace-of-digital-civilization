# HY-Ego（鱼巢）

`HY-Ego（鱼巢）` 是一个以“世界树 + 自我线程 + 需求/任务/方法 + 状态/动态/因果 + 自然语言显示”为核心的 C++ 实验工程。

当前仓库已经从“旧代码迁移混杂态”进入“有主线、有观测、可持续推进”的开发阶段。主程序是一个 Windows 控制台应用，启动后可自动拉起自我线程，并通过 WebView2 控制面板、文本摘要和运行日志观察系统状态。

更完整的架构说明见 [`说明书/项目说明.md`](./说明书/项目说明.md)。

## 项目定位

这个项目关注的不是单一算法或单一界面，而是一条持续运行的最小闭环：

1. 初始化世界树、自我锚点和运行环境。
2. 补齐根需求、根任务、任务管理任务与方法根。
3. 注册默认本能方法。
4. 启动自我线程，按 Tick 推进先天维护、任务治理和学习链路。
5. 把运行结果回写到状态、动态、因果、任务树和方法树。
6. 通过控制面板、摘要输出和日志回读当前真实状态。

当前工程的目标是先把这条主线跑稳，再在既有语义边界内继续补学习、方法生长、自然语言表达和更完整的业务步骤链。

## 当前工程能做什么

- 以 [`鱼巢.cpp`](./鱼巢.cpp) 为单一入口启动程序、自我线程和控制面板。
- 通过 [`自我模块.初始化.ixx`](./自我模块.初始化.ixx) 补齐世界根、自我、根需求、根任务、任务管理任务、方法根等核心骨架。
- 通过 [`自我线程类.h`](./自我线程类.h) 推进运行阶段、自检、先天动作、任务治理、学习调度和学习执行。
- 通过 [`任务管理任务模块.h`](./任务管理任务模块.h) 维护宿主任务、方法挂点、步骤节点、结果节点和治理证据。
- 通过 [`控制面板类.ixx`](./控制面板类.ixx) 输出文本摘要、HTML 和 WebView2 独立窗口观察面板。
- 通过 `2000+` 编号段规范和 `语言类_*.cpp` 把部分世界树信息映射到自然语言显示。

## 主要模块

| 模块 | 作用 |
| --- | --- |
| [`鱼巢.cpp`](./鱼巢.cpp) | 程序入口、命令行参数、控制台交互、自检日志触发 |
| [`自我模块.ixx`](./自我模块.ixx) | 自我实体、双值状态、根需求/根任务/当前主链的模块入口 |
| [`自我模块.初始化.ixx`](./自我模块.初始化.ixx) | 初始化世界树、自我锚点、默认本能方法与主链镜像 |
| [`自我线程类.h`](./自我线程类.h) | Tick 主循环、生命周期管理、阶段快照、运行摘要 |
| [`任务管理任务模块.h`](./任务管理任务模块.h) | 一步治理、宿主任务推进、步骤/结果节点生成、学习资产提交 |
| [`学习任务模块.ixx`](./学习任务模块.ixx) / [`学习调度模块.ixx`](./学习调度模块.ixx) / [`学习执行模块.ixx`](./学习执行模块.ixx) | 学习任务账本、调度快照、可执行学习项选择与执行回流 |
| [`控制面板类.ixx`](./控制面板类.ixx) / [`控制面板WebView2.ixx`](./控制面板WebView2.ixx) | 文本摘要、HTML 生成、独立窗口宿主 |
| [`规范`](./规范) | 当前规则、边界、字段口径和分层约束 |
| [`计划`](./计划) | 阶段计划、迁移记录和实施路线 |
| [`日志`](./日志) | 运行日志与自检日志 |

## 构建与运行

推荐环境：

- Windows 10/11
- Visual Studio 2022 或兼容 MSBuild 环境
- C++20
- x64
- 工程当前配置的 MSVC 工具集：`v145`

### Visual Studio

1. 打开 [`鱼巢.slnx`](./鱼巢.slnx)
2. 选择 `Debug|x64` 或 `Release|x64`
3. 构建并运行 [`鱼巢.vcxproj`](./鱼巢.vcxproj)

### MSBuild

```powershell
msbuild .\鱼巢.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64 /m
```

### 常用命令

```powershell
.\x64\Debug\鱼巢.exe
.\x64\Debug\鱼巢.exe --summary
.\x64\Debug\鱼巢.exe --task-summary
.\x64\Debug\鱼巢.exe --learning-summary
.\x64\Debug\鱼巢.exe --panel-html
.\x64\Debug\鱼巢.exe --self-check-log
```

补充说明：

- 文本摘要命令支持 `--summary` 和 `--panel-text`
- `--task-summary` 也支持 `--task-mgmt-summary` / `--task-management-summary`
- `--learning-summary` 也支持 `--study-summary`
- `--panel` 或 `--panel-open` 会直接打开控制面板窗口
- 仓库内置了最小本地门禁脚本：`.githooks/pre-push` 与 `.githooks/pre-push.ps1`；启用方式为 `git config core.hooksPath .githooks`
- 默认控制台交互命令为：`启动控制面板`、`检查自我实现`、`退出`

### 当前进入后续阶段前状态

当前工程已经把 `M1-M10` 的当前基线收成正式主线：`主体化 / 桥接字段 / 学习双通道 / 恢复重建一次性消费 / 最小 mailbox / 延迟事实车道 / 主循环最终去向裁决 / 事件优先级与队列观测 / 事件持久化与恢复待回判纪律 / 本地 pre-push 回归门 / 正式学习回流沉淀到主体长期策略 / 长期策略驱动下一轮方法回填 / 长期策略驱动业务步骤推进 / 长期策略驱动业务子任务派生与复用 / 事件轮业务扩张闸门 / 长期策略成功失败历史 / 长期策略降权与失效原因` 都已落地。现阶段最主要的缺口，已经从“事件能否回来”进一步转成：

- 把当前验证路径继续收敛到更稳定的日志、摘要、计数与快照检查
- 任务管理模块进一步内聚重构
- 更长期的业务步骤链深化与长期策略适用范围继续细化

## 当前状态

当前已经稳定形成的最小闭环包括：

- 初始化世界树和自我锚点
- 补齐根需求、根任务、任务管理任务和方法根
- 注册默认本能方法
- 启动自我线程
- 推进一步任务治理并写回任务树
- 产出状态、动态、因果与运行摘要
- 通过控制面板、文本摘要和日志回读主链

当前仍在持续推进的部分包括：

- 学习链路还没有完全迁成独立、稳定的运行时主链
- 真实业务步骤链仍少于治理链
- 方法条件、结果、成熟度、可靠度和学习回写仍需继续深化
- 自然语言显示有一部分还停留在名称层或短句层
- 日志化验证、任务管理模块内聚和业务步骤链深化仍可继续加强

## 阅读顺序

推荐按下面顺序接手这个仓库：

1. [`README.md`](./README.md)
2. [`说明书/项目说明.md`](./说明书/项目说明.md)
3. [`规范`](./规范)
4. [`计划`](./计划)
5. [`鱼巢.cpp`](./鱼巢.cpp)
6. `自我初始化模块 / 自我线程类 / 任务管理任务模块 / 控制面板类`
7. [`日志`](./日志) 中最新运行结果
