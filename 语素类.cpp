#include "语素类.h"

#include <algorithm>
#include <stdexcept>
#include <unordered_map>

namespace {
    const std::unordered_map<std::string, 枚举_词性>& 私有_文本到枚举映射表()
    {
        static const std::unordered_map<std::string, 枚举_词性> 映射表 = {
            { "名词", 枚举_词性::名词 },
            { "动词", 枚举_词性::动词 },
            { "形容词", 枚举_词性::形容词 },
            { "副词", 枚举_词性::副词 },
            { "连词", 枚举_词性::连词 },
            { "介词", 枚举_词性::介词 },
            { "专有名词", 枚举_词性::专有名词 },
            { "抽象名词", 枚举_词性::抽象名词 },
            { "代词", 枚举_词性::代词 },
            { "数词", 枚举_词性::数词 },
            { "量词", 枚举_词性::量词 },
            { "助词", 枚举_词性::助词 },
            { "叹词", 枚举_词性::叹词 },
            { "拟声词", 枚举_词性::拟声词 },
            { "标点符号", 枚举_词性::标点符号 },
            { "时间词", 枚举_词性::时间词 },
            { "状态词", 枚举_词性::状态词 },
            { "方位词", 枚举_词性::方位词 },
            { "习用语", 枚举_词性::习用语 },
            { "简称略语", 枚举_词性::简称略语 },
            { "非语素字", 枚举_词性::非语素字 },
            { "语气词", 枚举_词性::语气词 },
            { "外文字符", 枚举_词性::外文字符 },
            { "短语", 枚举_词性::短语 },
            { "未定义", 枚举_词性::未定义 },
            { "地名", 枚举_词性::地名 },
            { "n", 枚举_词性::n },
            { "f", 枚举_词性::f },
            { "s", 枚举_词性::s },
            { "t", 枚举_词性::t },
            { "nr", 枚举_词性::nr },
            { "ns", 枚举_词性::ns },
            { "nt", 枚举_词性::nt },
            { "nw", 枚举_词性::nw },
            { "nz", 枚举_词性::nz },
            { "v", 枚举_词性::v },
            { "vd", 枚举_词性::vd },
            { "vn", 枚举_词性::vn },
            { "a", 枚举_词性::a },
            { "ad", 枚举_词性::ad },
            { "an", 枚举_词性::an },
            { "d", 枚举_词性::d },
            { "m", 枚举_词性::m },
            { "q", 枚举_词性::q },
            { "r", 枚举_词性::r },
            { "p", 枚举_词性::p },
            { "c", 枚举_词性::c },
            { "u", 枚举_词性::u },
            { "xc", 枚举_词性::xc },
            { "w", 枚举_词性::w },
            { "PER", 枚举_词性::PER },
            { "LOC", 枚举_词性::LOC },
            { "ORG", 枚举_词性::ORG },
            { "TIME", 枚举_词性::TIME },
        };
        return 映射表;
    }

    std::unordered_map<枚举_词性, std::string>& 私有_枚举到文本动态映射表()
    {
        static std::unordered_map<枚举_词性, std::string> 映射表 = {
            { 枚举_词性::名词, "名词" },
            { 枚举_词性::动词, "动词" },
            { 枚举_词性::形容词, "形容词" },
            { 枚举_词性::副词, "副词" },
            { 枚举_词性::连词, "连词" },
            { 枚举_词性::介词, "介词" },
            { 枚举_词性::专有名词, "专有名词" },
            { 枚举_词性::抽象名词, "抽象名词" },
            { 枚举_词性::代词, "代词" },
            { 枚举_词性::数词, "数词" },
            { 枚举_词性::量词, "量词" },
            { 枚举_词性::助词, "助词" },
            { 枚举_词性::叹词, "叹词" },
            { 枚举_词性::拟声词, "拟声词" },
            { 枚举_词性::标点符号, "标点符号" },
            { 枚举_词性::时间词, "时间词" },
            { 枚举_词性::状态词, "状态词" },
            { 枚举_词性::方位词, "方位词" },
            { 枚举_词性::习用语, "习用语" },
            { 枚举_词性::简称略语, "简称略语" },
            { 枚举_词性::非语素字, "非语素字" },
            { 枚举_词性::语气词, "语气词" },
            { 枚举_词性::外文字符, "外文字符" },
            { 枚举_词性::短语, "短语" },
            { 枚举_词性::未定义, "未定义" },
            { 枚举_词性::地名, "地名" },
            { 枚举_词性::n, "n" },
            { 枚举_词性::f, "f" },
            { 枚举_词性::s, "s" },
            { 枚举_词性::t, "t" },
            { 枚举_词性::nr, "nr" },
            { 枚举_词性::ns, "ns" },
            { 枚举_词性::nt, "nt" },
            { 枚举_词性::nw, "nw" },
            { 枚举_词性::nz, "nz" },
            { 枚举_词性::v, "v" },
            { 枚举_词性::vd, "vd" },
            { 枚举_词性::vn, "vn" },
            { 枚举_词性::a, "a" },
            { 枚举_词性::ad, "ad" },
            { 枚举_词性::an, "an" },
            { 枚举_词性::d, "d" },
            { 枚举_词性::m, "m" },
            { 枚举_词性::q, "q" },
            { 枚举_词性::r, "r" },
            { 枚举_词性::p, "p" },
            { 枚举_词性::c, "c" },
            { 枚举_词性::u, "u" },
            { 枚举_词性::xc, "xc" },
            { 枚举_词性::w, "w" },
            { 枚举_词性::PER, "PER" },
            { 枚举_词性::LOC, "LOC" },
            { 枚举_词性::ORG, "ORG" },
            { 枚举_词性::TIME, "TIME" },
        };
        return 映射表;
    }
}

语素类 语素集{};

语素类::语素类() = default;

语素类::~语素类()
{
    auto lk = 获取锁();
    私有_清理主信息_已加锁();
}

void 语素类::初始化()
{
    static constexpr const char* 预热词表[][2] = {
        { "需求_权重", "名词" },
        { "二次特征_标量值", "名词" },
        { "安全需求", "名词" },
        { "服务需求", "名词" },
        { "自我根需求_安全", "名词" },
        { "自我根需求_服务", "名词" },
    };

    for (const auto& 项 : 预热词表) {
        (void)添加词性词(项[0], 项[1]);
    }
}

词性节点类* 语素类::生成语素节点(结构体_分词 分词信息)
{
    return 添加词性词(std::move(分词信息));
}

词节点类* 语素类::添加词(const std::string& 词字符串)
{
    if (词字符串.empty()) {
        throw std::invalid_argument("语素类::添加词 - 输入词字符串为空");
    }

    写锁守卫 lk(链表锁);

    链节点类* 游标 = 根指针;
    词节点类* 当前节点 = nullptr;
    std::string 前缀;

    for (std::size_t i = 0; i < 词字符串.size(); ) {
        const std::size_t step = std::min(私有_UTF8字符长度(static_cast<unsigned char>(词字符串[i])), 词字符串.size() - i);
        前缀.append(词字符串, i, step);
        i += step;

        当前节点 = 查找词_已加锁(前缀);
        if (当前节点) {
            游标 = 当前节点;
            continue;
        }

        auto* 新词节点 = new 词节点类{};
        新词节点->主信息 = new 词主信息类(前缀);

        链节点类* 新节点 = (游标 == 根指针)
            ? 添加节点_已加锁(游标, 新词节点)
            : 添加子节点_已加锁(游标, 新词节点);

        if (!新节点) {
            delete 新词节点->主信息;
            delete 新词节点;
            return nullptr;
        }

        当前节点 = static_cast<词节点类*>(新节点);
        游标 = 当前节点;
    }

    return 当前节点;
}

词节点类* 语素类::查找词节点(const std::string& 词字符串) const
{
    if (词字符串.empty()) {
        throw std::invalid_argument("语素类::查找词节点 - 输入词字符串为空");
    }

    读锁守卫 lk(链表锁);
    return 查找词_已加锁(词字符串);
}

词节点类* 语素类::获取词性词指针(const std::string& 词字符串) const
{
    if (词字符串.empty()) return nullptr;

    读锁守卫 lk(链表锁);
    return 查找词_已加锁(词字符串);
}

词性节点类* 语素类::查找词性节点(const 词节点类* 词节点, 枚举_词性 词性值) const
{
    读锁守卫 lk(链表锁);
    return 查找词性节点_已加锁(词节点, 词性值);
}

词性节点类* 语素类::查找词性节点(const 语素节点类* 词节点, const std::string& 词性值) const
{
    return 查找词性节点(static_cast<const 词节点类*>(词节点), 枚举_词性_工厂::根据文本获取枚举值(词性值));
}

词性节点类* 语素类::查找词性节点(结构体_分词 分词信息) const
{
    auto* 词节点 = 查找词节点(分词信息.词);
    if (!词节点) return nullptr;
    return 查找词性节点(词节点, 分词信息.词性);
}

词性节点类* 语素类::添加词性词(词节点类* 词节点, 枚举_词性 词性)
{
    if (!词节点) {
        throw std::invalid_argument("语素类::添加词性词 - 词节点为空");
    }
    if (词性 == 枚举_词性::未定义) {
        throw std::invalid_argument("语素类::添加词性词 - 词性未定义");
    }

    写锁守卫 lk(链表锁);

    if (auto* 已有节点 = 查找词性节点_已加锁(词节点, 词性)) {
        return 已有节点;
    }

    auto* 新词性节点 = new 词性节点类{};
    新词性节点->主信息 = new 词性主信息类(词性);

    auto* 节点 = 添加子节点_已加锁(const_cast<词节点类*>(词节点), 新词性节点);
    if (!节点) {
        delete 新词性节点->主信息;
        delete 新词性节点;
        return nullptr;
    }

    return static_cast<词性节点类*>(节点);
}

词性节点类* 语素类::添加词性词(const std::string& 词值, const std::string& 词性值)
{
    auto* 词节点 = 添加词(词值);
    return 添加词性词(词节点, 枚举_词性_工厂::根据文本获取枚举值(词性值));
}

词性节点类* 语素类::添加词性词(结构体_分词 词信息)
{
    return 添加词性词(词信息.词, 词信息.词性);
}

const 词性节点类* 语素类::绑定词性词到场景基础信息(
    const std::string& 词字符串,
    枚举_词性 词性,
    场景节点类* 场景,
    基础信息节点类* 基础信息节点
)
{
    (void)场景;

    if (词字符串.empty() || !基础信息节点) {
        throw std::invalid_argument("语素类::绑定词性词到场景基础信息 - 参数为空");
    }

    auto* 词节点 = 添加词(词字符串);
    auto* 词性节点 = 添加词性词(词节点, 词性);
    (void)添加对应基础信息(词性节点, 基础信息节点);
    return 词性节点;
}

std::vector<基础信息节点类*> 语素类::通过词和场景查找基础信息(
    const std::string& 词字符串,
    场景节点类* 当前场景,
    const 枚举_词性* 词性
) const
{
    (void)当前场景;

    std::vector<基础信息节点类*> 结果;
    if (词字符串.empty()) return 结果;

    读锁守卫 lk(链表锁);
    auto* 词节点 = 查找词_已加锁(词字符串);
    if (!词节点 || !词节点->子) return 结果;

    auto* 起点 = 词节点->子;
    auto* 游标 = 起点;
    do {
        auto* 主信息 = dynamic_cast<词性主信息类*>(游标->主信息);
        if (主信息) {
            if (!词性 || 主信息->词性 == *词性) {
                if (主信息->对应基础信息.指针) {
                    结果.push_back(主信息->对应基础信息.指针);
                }
            }
        }
        游标 = 游标->下;
    } while (游标 && 游标 != 起点);

    return 结果;
}

词节点类* 语素类::添加二次特征词(语素节点类* 参照物, 语素节点类* 比较对象, 语素节点类* 比较类型)
{
    (void)参照物;
    (void)比较对象;
    (void)比较类型;
    return nullptr;
}

std::int64_t 语素类::添加对应基础信息(const 词性节点类* 词节点, 基础信息节点类* 基础信息)
{
    if (!词节点) {
        throw std::invalid_argument("语素类::添加对应基础信息 - 词节点为空");
    }
    if (!基础信息) {
        throw std::invalid_argument("语素类::添加对应基础信息 - 基础信息为空");
    }

    写锁守卫 lk(链表锁);
    auto* 词性主信息 = dynamic_cast<词性主信息类*>(const_cast<词性节点类*>(词节点)->主信息);
    if (!词性主信息) {
        throw std::runtime_error("语素类::添加对应基础信息 - 词节点主信息不是词性主信息类");
    }

    词性主信息->对应基础信息.指针 = 基础信息;
    词性主信息->对应基础信息.主键.clear();
    return 1;
}

std::string 语素类::获取词(const 词性节点类* 词性词指针) const
{
    if (!词性词指针) {
        throw std::invalid_argument("语素类::获取词 - 词性词指针为空");
    }
    if (!词性词指针->父 || !词性词指针->父->主信息) {
        throw std::invalid_argument("语素类::获取词 - 词性词指针没有有效父词节点");
    }

    auto* 词主信息 = dynamic_cast<词主信息类*>(词性词指针->父->主信息);
    if (!词主信息) {
        throw std::runtime_error("语素类::获取词 - 父节点主信息不是词主信息类");
    }

    return 词主信息->词;
}

void 语素类::私有_清理主信息_已加锁() noexcept
{
    if (!根指针) return;

    for (auto* it = 根指针->链下; it && it != 根指针; it = it->链下) {
        delete it->主信息;
        it->主信息 = nullptr;
    }
}

词节点类* 语素类::查找词_已加锁(const std::string& 词字符串) const
{
    if (词字符串.empty() || !根指针 || 根指针->下 == 根指针) return nullptr;

    链节点类* 当前父节点 = 根指针;
    std::string 前缀;
    前缀.reserve(词字符串.size());

    for (std::size_t i = 0; i < 词字符串.size(); ) {
        std::size_t step = 私有_UTF8字符长度(static_cast<unsigned char>(词字符串[i]));
        if (i + step > 词字符串.size()) step = 1;

        前缀.append(词字符串, i, step);
        i += step;

        词节点类* 命中节点 = nullptr;
        if (当前父节点 == 根指针) {
            for (auto* 游标 = 当前父节点->下; 游标 && 游标 != 当前父节点; 游标 = 游标->下) {
                auto* 主信息 = dynamic_cast<词主信息类*>(游标->主信息);
                if (主信息 && 主信息->词 == 前缀) {
                    命中节点 = static_cast<词节点类*>(游标);
                    break;
                }
            }
        }
        else {
            auto* 首子节点 = 当前父节点->子;
            if (!首子节点) return nullptr;

            auto* 游标 = 首子节点;
            do {
                auto* 主信息 = dynamic_cast<词主信息类*>(游标->主信息);
                if (主信息 && 主信息->词 == 前缀) {
                    命中节点 = static_cast<词节点类*>(游标);
                    break;
                }
                游标 = 游标->下;
            } while (游标 && 游标 != 首子节点);
        }

        if (!命中节点) return nullptr;
        当前父节点 = 命中节点;
    }

    return 当前父节点 == 根指针 ? nullptr : static_cast<词节点类*>(当前父节点);
}

词性节点类* 语素类::查找词性节点_已加锁(const 链节点类* 词节点, 枚举_词性 词性值) const
{
    if (!词节点 || !词节点->子) return nullptr;

    auto* 起点 = 词节点->子;
    auto* 当前节点 = 起点;
    do {
        auto* 主信息 = dynamic_cast<词性主信息类*>(当前节点->主信息);
        if (主信息 && 主信息->词性 == 词性值) {
            return static_cast<词性节点类*>(当前节点);
        }

        当前节点 = 当前节点->下;
    } while (当前节点 && 当前节点 != 起点);

    return nullptr;
}

std::size_t 语素类::私有_UTF8字符长度(unsigned char c) noexcept
{
    if ((c & 0x80u) == 0) return 1;
    if ((c & 0xE0u) == 0xC0u) return 2;
    if ((c & 0xF0u) == 0xE0u) return 3;
    if ((c & 0xF8u) == 0xF0u) return 4;
    return 1;
}

std::string 枚举_词性_工厂::根据枚举类型获取文本(枚举_词性 词性值)
{
    const auto& 动态映射 = 私有_枚举到文本动态映射表();
    auto it = 动态映射.find(词性值);
    return it != 动态映射.end() ? it->second : "未知词性";
}

枚举_词性 枚举_词性_工厂::根据文本获取枚举值(const std::string& 文本)
{
    const auto& 映射表 = 私有_文本到枚举映射表();
    auto it = 映射表.find(文本);
    return it != 映射表.end() ? it->second : 枚举_词性::未定义;
}

void 枚举_词性_工厂::添加动态映射(枚举_词性 词性值, const std::string& 文本描述)
{
    私有_枚举到文本动态映射表()[词性值] = 文本描述;
}

void 枚举_词性_工厂::批量添加动态映射(const std::vector<std::pair<枚举_词性, std::string>>& 映射列表)
{
    for (const auto& 项 : 映射列表) {
        添加动态映射(项.first, 项.second);
    }
}

void 枚举_词性_工厂::清除动态映射()
{
    auto& 动态映射 = 私有_枚举到文本动态映射表();
    动态映射.clear();
    for (const auto& 项 : 私有_文本到枚举映射表()) {
        动态映射.emplace(项.second, 项.first);
    }
}
