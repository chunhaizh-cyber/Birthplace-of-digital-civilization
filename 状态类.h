#pragma once

#include <cstddef>
#include <cstdint>
#include <functional>
#include <mutex>
#include <string>
#include <vector>

#include "基础信息类.h"

enum class 枚举_存在状态事件 : std::uint8_t {
    未定义 = 0,
    创建 = 1,
    变化 = 2,
};

struct 状态创建参数 {
    枚举_存在状态事件 事件 = 枚举_存在状态事件::未定义;
    基础信息节点类* 状态主体 = nullptr;
    特征节点类* 状态特征 = nullptr;
    特征值 状态值{};
    基础信息节点类* 对应信息节点 = nullptr;
    // 兼容创建提示：仅控制是否按变化状态记录，不能作为任务完成、需求满足或结算事实。
    bool 是否变化 = false;
    时间戳 now = 0;
};

struct 结构_状态清理问题快照 {
    bool 需要自检报告 = false;
    std::uintptr_t 场景指针 = 0;
    std::string 场景主键{};
    std::size_t 活动状态数量 = 0;
    std::size_t 活动状态上限 = 0;
    std::size_t 目标清理数量 = 0;
    std::size_t 已清理数量 = 0;
    std::size_t 被引用状态数量 = 0;
    时间戳 记录时间 = 0;
    std::string 摘要{};
};

class 状态类 {
public:
    using 状态后处理回调 = std::function<void(场景节点类*, 状态节点类*, 时间戳)>;

    explicit 状态类(基础信息类* 基础信息 = nullptr) noexcept;

    void 绑定基础信息(基础信息类* 基础信息) noexcept;

    状态节点主信息类* 取状态主信息(状态节点类* 节点) const noexcept;
    const 状态节点主信息类* 取状态主信息(const 状态节点类* 节点) const noexcept;

    // 功能：按主键查找并解析状态节点，不创建或修改节点。
    状态节点类* 按主键解析状态节点(const std::string& 主键) const noexcept;

    // 功能：读取状态主体和状态特征，不创建或修改节点。
    bool 读取状态主体与特征(
        const 状态节点类* 状态,
        基础信息节点类*& 状态主体,
        特征节点类*& 状态特征) const noexcept;

    // 功能：读取状态特征，不创建或修改节点。
    特征节点类* 读取状态特征(const 状态节点类* 状态) const noexcept;

    // 功能：读取状态主体，不创建或修改节点。
    基础信息节点类* 读取状态主体(const 状态节点类* 状态) const noexcept;

    状态节点主信息类 生成状态(
        基础信息节点类* 主体,
        特征节点类* 特征,
        特征值 状态值,
        时间戳 发生时间 = 结构体_时间戳::当前_微秒(),
        时间戳 收到时间 = 结构体_时间戳::当前_微秒()) const;
    bool 状态有效检查(const 状态节点主信息类& 主信息) const noexcept;
    I64 获取状态二次特征(const 状态节点主信息类& 状态一, const 状态节点主信息类& 状态二) const noexcept;

    状态节点类* 创建状态(场景节点类* 场景, 状态节点主信息类* 主信息);
    状态节点类* 创建状态(
        场景节点类* 场景,
        const 状态创建参数& 参数,
        const 状态后处理回调& 后处理 = {}
    );
    状态节点类* 创建状态(
        场景节点类* 场景,
        基础信息节点类* 主体,
        特征节点类* 特征,
        const 特征值& 状态值,
        时间戳 发生时间 = 结构体_时间戳::当前_微秒(),
        时间戳 收到时间 = 结构体_时间戳::当前_微秒()
    );
    bool 记录状态到场景(
        场景节点类* 场景,
        const 状态创建参数& 参数,
        const 状态后处理回调& 后处理 = {}
    );
    状态节点类* 创建内部状态(
        场景节点类* 场景,
        基础信息节点类* 主体,
        特征节点类* 特征,
        const 特征值& 值,
        枚举_存在状态事件 事件 = 枚举_存在状态事件::未定义,
        bool 是否变化 = false,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const 状态后处理回调& 后处理 = {}
    );
    bool 记录内部状态到场景(
        场景节点类* 场景,
        基础信息节点类* 主体,
        特征节点类* 特征,
        const 特征值& 值,
        枚举_存在状态事件 事件 = 枚举_存在状态事件::未定义,
        bool 是否变化 = false,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const 状态后处理回调& 后处理 = {}
    );
    状态节点类* 创建世界状态(
        场景节点类* 场景,
        基础信息节点类* 主体,
        特征节点类* 特征,
        const 特征值& 值,
        枚举_存在状态事件 事件 = 枚举_存在状态事件::未定义,
        bool 是否变化 = false,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const 状态后处理回调& 后处理 = {}
    );
    bool 记录世界状态到场景(
        场景节点类* 场景,
        基础信息节点类* 主体,
        特征节点类* 特征,
        const 特征值& 值,
        枚举_存在状态事件 事件 = 枚举_存在状态事件::未定义,
        bool 是否变化 = false,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const 状态后处理回调& 后处理 = {}
    );
    状态节点类* 记录内部特征状态(
        场景节点类* 场景,
        基础信息节点类* 主体,
        特征节点类* 特征,
        const 特征值& 值,
        枚举_存在状态事件 事件 = 枚举_存在状态事件::未定义,
        bool 是否变化 = false,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const 状态后处理回调& 后处理 = {}
    );
    bool 删除状态(状态节点类* 节点);

    std::vector<状态节点类*> 枚举全部状态() const;
    std::vector<状态节点类*> 获取场景状态(const 场景节点类* 场景) const;
    bool 是否当前活动状态(const 场景节点类* 场景, const 状态节点类* 节点) const;
    std::uint64_t 统计状态引用数量(const 状态节点类* 节点) const;
    bool 可从活动状态链清理(const 场景节点类* 场景, const 状态节点类* 节点) const;
    std::size_t 清理已处理状态索引(
        场景节点类* 场景,
        基础信息节点类* 主体 = nullptr,
        特征节点类* 特征 = nullptr,
        std::size_t 最大清理数量 = 0
    );
    std::size_t 清理超过时间窗口的状态索引(
        场景节点类* 场景,
        时间戳 最早保留时间,
        std::size_t 最大清理数量 = 0
    );
    std::size_t 按活动状态限量清理(
        场景节点类* 场景,
        std::size_t 活动状态上限,
        std::size_t 最大清理数量 = 0
    );
    std::size_t 状态写入后整理(
        场景节点类* 场景,
        状态节点类* 新状态,
        std::size_t 最大清理数量 = 0
    );
    结构_状态清理问题快照 读取最近清理问题快照() const;
    void 清除最近清理问题快照() noexcept;
    bool 绑定主体特征(状态节点类* 节点, 基础信息节点类* 主体, 特征节点类* 特征);
    bool 写入状态值(状态节点类* 节点, const 特征值& 值, 时间戳 发生时间 = 0, 时间戳 收到时间 = 0);
    // 仅写入状态节点上的显示缓存；权威变化关系由动态或二次特征比较生成。
    bool 标记变化(状态节点类* 节点, bool 是否变化);

private:
    static 时间戳 私有_取状态排序时间(const 状态节点主信息类& 主信息) noexcept;
    void 私有_按时间追加状态索引(场景节点主信息类* 场景主信息, 状态节点类* 节点) const;

private:
    基础信息类* 基础信息_ = nullptr;
    mutable std::mutex 清理问题锁_{};
    结构_状态清理问题快照 最近清理问题_{};
};
