# S1d-4s 分步语义绑定剩余阶段定位与瘦身方案 v0.1

日期：2026-06-08

## 1. 目标

```text
承接 S1d-4r。

S1d-4r 已闭合：
    `分步值写入` 从 S1d-4q 约 66058us 降到本轮约 44281us；
    最新运行线程 T31648 中，分步结果样本 5 条，最慢阶段全部为 `分步语义绑定`；
    动态二次特征生成主耗时仍最频繁落在 `分步结果生成`。

本切片目标：
    拆分 `二次特征应用模块.ixx` 中分步结果的语义绑定内部阶段；
    定位概念名称取词、概念名称比较 / 写入、规则版本写入、场景绑定判断、场景绑定写入、动态粒度推断、生成语义判断、生成语义设置、签名刷新各自耗时；
    若证据显示某阶段是重复工作，再做最小快路径。
```

## 2. 允许修改

```text
二次特征应用模块.ixx
计划/20260607_S1d4s分步语义绑定剩余阶段定位与瘦身方案_v0.1.md
计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
计划/计划索引.md
实施记录/20260608_S1d4s分步语义绑定剩余阶段定位_Codex断点清单.md
```

## 3. 禁止修改

```text
不删除动态二次特征生成。
不跳过二次特征值写入。
不改变二次特征主信息结构。
不跳过场景绑定和生成语义。
不把“语义已满足”的判断扩大成裸跳过正式状态更新。
不改任务状态、需求满足、价值结算或外设事实规则。
不新增模拟入口、夹具、伪造样本或 `--*-check` 回归入口。
```

## 4. 实施步骤

```text
S1d-4s-1：
    增加 `动态二次特征分步语义绑定/内部阶段耗时` 慢采样。

S1d-4s-2：
    对标量结果和离散结果的语义绑定段分别拆分：
        种类检查写入；
        概念名称取词；
        概念名称比较写入；
        规则版本写入；
        场景绑定判断；
        场景绑定写入；
        动态粒度推断；
        生成语义判断；
        生成语义设置；
        签名刷新。

S1d-4s-3：
    构建 Debug x64，真实运行 `--self-check-log --self-check-log-wait-ms=10000`。

S1d-4s-4：
    若主耗时有明确重复阶段，在同一切片内做最小瘦身；
    若主耗时转移到条件候选收集或签名查找创建，停止在证据并切下一片。
```

## 5. 验证预测

```text
构建：
    Debug x64 构建通过。

真实运行：
    退出码=0。
    日志继续出现 `动态二次特征分步结果/内部阶段耗时`。
    日志出现 `动态二次特征分步语义绑定/内部阶段耗时`，或在 5ms 阈值下无样本但高层分步结果仍可统计。
    日志保留 `任务状态动作动态=<动态主键>`。
    不出现 `任务状态被动提交/逻辑错误`。
    不出现 `任务状态动作动态逻辑错误`。
    不出现旧路径锚点 `准备本能调用`、`执行本能方法`、`任务域本能输入包`。
```

## 6. 下一步判断

```text
如果主耗时在概念名称取词：
    优先判断同一常量概念名称是否可使用进程内缓存。

如果主耗时在场景绑定判断：
    优先判断引用主键是否可用已有指针快路径，不放宽绑定语义。

如果主耗时在生成语义判断：
    优先判断重复枚举 / 重复比较是否可合并，不跳过必要字段更新。

如果主耗时在生成语义设置或签名刷新：
    只在字段已经完全一致时跳过；规则版本、种类、粒度、基准类型、时间归一方式和值形态变化时仍必须刷新签名。

如果分步结果主耗时不再是语义绑定：
    停止本切片，按最新最慢阶段切下一片。
```

## 7. 落实结果

```text
已落实：
    `二次特征应用模块.ixx` 新增 `动态二次特征分步语义绑定/内部阶段耗时`；
    标量结果和离散结果语义绑定段均已拆分：
        种类检查写入；
        概念名称取词；
        概念名称比较写入；
        规则版本写入；
        场景绑定判断；
        场景绑定写入；
        动态粒度推断；
        生成语义判断；
        生成语义设置；
        签名刷新。

验证：
    `git diff --check` 通过，仅有 CRLF 工作区转换警告；
    Debug x64 构建通过；
    真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0。

本轮日志窗口：
    最新启动行为 `2026-06-08 11:08:19.721 [T16620] 程序启动`；
    `任务状态动作动态=` 52 条；
    `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包` 均为 0。

性能证据：
    `动态二次特征分步结果/内部阶段耗时` 6 条，最慢阶段全部为 `分步语义绑定`；
    分步结果平均总耗时约 185376us，`分步语义绑定` 平均约 93866us，`分步值写入` 平均约 37866us，`分步签名查找创建` 平均约 39144us；
    `动态二次特征分步语义绑定/内部阶段耗时` 6 条，最慢阶段全部为 `场景绑定写入`；
    `场景绑定写入` 平均约 89238us，最大约 176646us；
    `概念名称取词` 平均约 3275us，`生成语义设置` 平均约 1207us，其余语义判断阶段接近 0us；
    `动态二次特征生成/内部阶段耗时` 23 条，最慢阶段分布为 `分步结果生成=18`、`条件候选收集=3`、`命中回写因果实例=2`；
    动态二次特征生成平均总耗时约 495036us，`分步结果生成` 平均约 216876us，`条件候选收集` 平均约 186957us。

结论：
    S1d-4s 定位闭合；
    当前慢点不是概念名称取词、生成语义判断或签名刷新；
    慢点集中到 `二次特征类::绑定场景` 里的场景绑定写入。

下一步：
    进入 S1d-4t：二次特征场景绑定写入内部阶段定位 / 瘦身。
```
