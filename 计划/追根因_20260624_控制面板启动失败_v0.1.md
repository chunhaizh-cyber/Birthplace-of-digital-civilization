# 追根因：控制面板启动失败 v0.1

当前弹窗：
    暂无弹窗正文；用户现象为“无法正常启动控制面板”。当前复现已确认 `--panel-open` 能创建 WebView2 窗口，但控制台窗口可能遮挡控制面板。

最高优先级原因：
    A 主链根因候选：控制面板窗口实际已创建并加载内容，但新窗口没有在创建后稳定前置；从命令行启动时控制台可能遮挡控制面板，外观上表现为“没有正常启动”。同时成功路径缺少 WebView2 成功日志，容易被误判为窗口线程未创建。

当前层级：
    L1 直接生成层，已进入最小修复和复跑验证。

同层疑点记录：
    1. `--panel-open` 仍走耗时初始化：已暂排除；日志显示跳过自我线程启动前置，不同步刷新全量 SQL 投影，不触发自我环境初始化。
    2. WebView2 窗口线程未创建窗口或创建失败：已暂排除；Win32 进程快照显示主窗口标题为“鱼巢控制面板”，类名为“鱼巢控制面板WebView2重构窗口”。
    3. 窗口已创建但被控制台遮挡：命中；截图 `日志/20260624_panel_open_screenshot.png` 显示控制面板内容已加载但控制台位于前景。

选中下钻分支：
    在 `控制面板WebView2.cpp` 新建 / 复用窗口路径中显式前置窗口，并补成功运行日志。

主链判断：
    已用真实 `--panel-open` 证明窗口创建、类名 / 标题可见、日志出现 `控制面板WebView2/窗口已显示` 和 `控制面板WebView2/启动窗口线程返回成功`，且没有同步刷新全量 SQL 投影。

已确认事实：
    `--refresh-sql-projection` 显式路径保留字段恢复比对；常规投影默认跳过字段恢复比对。
    `--panel-open` 在当前代码中可以创建窗口；窗口关闭后进程正常退出。
    控制面板页面截图显示 SQL 控制面板内容已加载。
    修复后 `日志/20260624_panel_open_after_console_minimize.png` 显示控制面板位于前景；日志显示 `控制面板命令/命令行窗口已启动 | 控制台已最小化 | 等待控制面板窗口关闭`。

已排除：
    当前已排除 SQL 字段恢复比对阻塞 `--panel-open` 的窗口创建；已排除窗口类注册 / CreateWindow 失败。

旁支问题池：
    `--panel-html` 仍约 35 秒，属于刷新 SQL 读模型的保留设计；不作为本次 `--panel-open` 首要修复目标。

已改文件：
    控制面板WebView2.cpp。
    鱼巢.cpp。

已运行命令：
    已读取 AGENTS、规范目录、禁止性规范、计划索引、工程图谱、线程生命周期状态上报规范。
    已运行 `--panel-open` 进程快照验证和窗口截图验证。
    已运行 Debug x64 构建，0 警告 0 错误。
    已运行 `.\x64\Debug\鱼巢.exe --refresh-sql-projection`，退出码 0，五类字段恢复比对通过。
    已运行 `.\x64\Debug\鱼巢.exe --panel-text`，退出码 0，输出控制面板摘要。
    已运行修复后的 `--panel-open`，窗口标题“鱼巢控制面板”，类名“鱼巢控制面板WebView2重构窗口”，关闭后进程退出。

下一条命令：
    提交本切片相关文件。

验收证据：
    Debug x64 构建通过；`--refresh-sql-projection` 字段恢复比对通过；`--panel-text` 输出摘要；`--panel-open` 真实出现窗口或日志明确失败原因。

禁止宣称：
    不宣称自我苏醒完成、初步成熟完成；不把控制面板显示恢复等同于业务闭环完成。
