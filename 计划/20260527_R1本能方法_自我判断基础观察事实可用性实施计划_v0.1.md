# 20260527_R1本能方法_自我判断基础观察事实可用性实施计划 v0.1

## 0. 归属

```text
父计划：计划/20260526_基础观察事实与基础风险判断分层实施计划_v0.1.md
方法名：自我_判断基础观察事实可用性
方法分类：自我判定类本能方法
当前状态：待检查是否已有等价登记；未登记前不得宣称该方法已完成。
```

## 1. 目标

```text
综合第一阶段前置状态，判断当前观察帧基础观察事实是否可用；
写入 当前观察帧.基础观察事实可用状态 等于 可用 / 条件不足 / 不可用。
```

该方法是第一阶段收束方法。它只证明“当前观察事实可用”，不得推出“基础风险判断已明确”。

## 2. 输入与输出

输入：

```text
自我所在场景.当前观察特征帧取得状态
当前观察帧.观察帧容器状态 / 自我所在场景.当前观察帧存在状态
双目相机.可用状态
双目相机.运行可用性值
当前观察帧.像素覆盖状态
当前观察帧.像素特征值承接状态
当前观察帧.观察事实完备状态
当前观察帧.质量可用状态
自我所在场景.当前观察新鲜度状态
前置动作动态列表
```

输出：

```text
基础观察事实可用状态 = 可用 / 条件不足 / 不可用
基础观察事实失败条件数量
基础观察事实失败条件摘要
动作动态 / 来源动态证据
```

## 3. 允许修改范围

```text
本能方法类.h
本能方法类.cpp
自我动作实现模块.ixx
自我动作实现.外设模块.ixx
基础数据类型.h
基础数据类型.ixx
控制面板类.ixx
控制面板类.cpp
实施记录/20260526_基础观察事实与基础风险判断分层_Codex断点清单.md
计划/计划索引.md
```

`本能方法类.*` 只允许登记 `自我_判断基础观察事实可用性` 一个方法。

## 4. 禁止越界

```text
不得把基础观察事实可用推出基础风险判断状态已明确；
不得写基础风险值、风险等级、安全值或服务值；
不得绕过枚举型实例特征值生产者校验；
不得让任务管理线程直接写需求树。
```

## 5. 实施步骤

```text
B1 先检查是否已有等价方法头和动作入口；没有则只登记本方法。
B2 读取全部第一阶段前置状态和来源动作动态。
B3 逐项计算失败条件数量与原因。
B4 当且仅当前置全部成立时写 基础观察事实可用状态=可用。
B5 输出日志和控制面板摘要，明确基础风险仍未判断。
```

## 6. 验收

```text
Debug x64 构建通过；
真实日志中 自我_判断基础观察事实可用性 有动作动态；
日志列出全部前置状态和失败条件数量；
缺任一前置条件时不能写 可用；
最新运行段没有 基础风险判断状态=已明确、直接写需求树=1、安全值结算或服务值结算。
```

