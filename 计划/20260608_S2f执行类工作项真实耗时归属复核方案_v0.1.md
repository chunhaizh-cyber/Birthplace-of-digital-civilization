# S2f 执行类工作项真实耗时归属复核方案 v0.1

日期：2026-06-08

状态：已闭合；执行类慢样本归属 D455 / 外设缺报告失败路径，已转 S8e 专线。

## 1. 触发依据

```text
S2e 已闭合：
    复跑后 `状态提交回执同步` 平均约 35735us。
    `任务状态被动提交/内部阶段耗时`、`任务状态动作动态创建/内部阶段耗时`、`状态创建/内部阶段耗时`、`创建动态/内部阶段耗时` 均为 0 条慢采样。
    未发现可安全瘦身的状态提交重复账。

最新真实窗口：
    日志=日志/鱼巢_run_20260608.低值g
    窗口起点行=97058
    窗口起点=2026-06-08 16:57:22.425 程序启动

剩余主耗时：
    `任务工作项派发执行耗时` 84 条，平均约 54582us，最大约 602398us。
    筹办类 80 条，平均约 32451us，最大约 189003us。
    执行类 4 条，平均约 497208us，最大约 602398us。

执行类样本：
    任务 A / C / D 当前方法多为 1x，结果为执行阶段发现缺口：缺方法动作 / 动作执行失败。
    任务 B 当前方法为 1i，结果为动作执行失败并派生执行缺失需求。
    已出现 `提交_生成缺口子需求`，类型=执行缺失。
```

## 2. 目标

```text
第一目标：
    拆清执行类工作项真实耗时归属：
        执行前许可；
        任务管理工作线程执行阶段；
        方法动作入口调用；
        执行失败 / 缺口判定；
        派生执行缺失需求准备；
        D455 / 外设链路。

第二目标：
    如果耗时归属 D455 / 外设链路，只记录证据并转入 D455 专线，不混入任务管理状态提交优化。

第三目标：
    如果耗时归属缺方法动作 / 执行缺失的重复尝试或重复派生，优先减少重复执行类派发。
    不得把执行失败写成成功，不得跳过缺口派生的业务证据。
```

## 3. 硬边界

```text
任务筹办 / 任务执行仍是任务管理生命周期普通函数，不恢复为本能方法。
执行类工作项耗时日志只表示真实工作执行，不再表示外层派发锁持有。
不得让 worker 先执行后补任务状态账。
不得绕开 `提交任务状态变化`。
不得把动作执行失败、缺方法动作、D455 条件不足或等待子需求写成完成 / 满足。
不得新增 mock、fixture、伪入口、伪动作动态或 `--*-check` 回归入口。
```

## 4. 允许修改

```text
任务模块.管理界面线程.impl.cpp
任务模块.管理工作线程.ixx
任务模块.执行.ixx
计划/计划索引.md
计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
计划/20260608_S2f执行类工作项真实耗时归属复核方案_v0.1.md
实施记录/20260608_S2f执行类工作项真实耗时归属复核_Codex断点清单.md

若必须修改 D455 / 外设文件，先证明主耗时归属外设链路，并转入专线方案。
```

## 5. 执行步骤

```text
1. 写 S2f 断点清单和预测。
2. 读取执行类工作项处理路径，定位 `任务管理工作线程::提交任务工作项` 的执行分支。
3. 复核现有日志是否能拆分执行阶段；不足时只补最小阶段日志。
4. 若证据指向重复执行失败 / 重复执行缺失派生，做去重或跳过重复派发的小瘦身。
5. 若证据指向 D455 / 外设动作本体，停止本切片实现并转 D455 专线。
6. 构建 Debug x64，真实运行 self-check-log，按最新窗口核对预测。
```

## 6. 闭合结论

```text
闭合时间：2026-06-08。

结论：
    执行类慢样本归属 D455 / 外设动作本体。
    方法 1i = 本能 ID 6145 自我_识别外设观察材料。
    方法 1x = 本能 ID 6150 提交_当前观察范围可观测单位存在对应事实。
    日志显示缺外设观察报告、观察材料等级不合格、可进入提交入口组织=0。

处置：
    不在任务管理状态提交链做改动。
    转入 计划/20260608_S8e_D455缺外设报告执行失败快路径复核方案_v0.1.md。
```
