# S1d-5c 二次特征值写入批次观测时间戳复用瘦身方案 v0.1

日期：2026-06-08

## 1. 目标

```text
承接 S1d-5b。

S1d-5b 已闭合：
    复用已取 `二次特征主信息类*` 后，`分步值写入` 平均由约 82869us 降到约 70124us；
    `二次特征值写入/内部阶段耗时` 0 条，单次值写入未超过慢采样阈值；
    当前主因仍是高次数小写入聚合。

代码证据：
    `结构_统计::记录观测` 只维护 `创建时间`、`最后观测时间` 和 `命中次数`；
    当前每个分步结果值写入都会单独调用 `结构体_时间戳::当前_微秒()`；
    同一 `私有_获取动态二次特征` 分步批次中的派生结果来自同一动态二次特征生成过程，可共用一个非零观测时间戳。

本切片目标：
    在分步结果生成批次开始时生成一次观测时间戳；
    已取主信息值写入入口接收可选观测时间戳；
    每个节点仍调用 `统计.记录观测`，不减少命中次数；
    未传入批次时间戳的旧入口保持原行为。
```

## 2. 允许修改

```text
二次特征类.h
二次特征类.cpp
二次特征应用模块.ixx
计划/20260607_S1d5c二次特征值写入批次观测时间戳复用瘦身方案_v0.1.md
计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
计划/计划索引.md
实施记录/20260608_S1d5c二次特征值写入批次观测时间戳_Codex断点清单.md
```

## 3. 禁止修改

```text
不跳过 `统计.记录观测`。
不减少 `命中次数`。
不删除合法分步结果。
不改变二次特征值语义。
不改变概念模板匹配规则。
不改任务状态、需求满足、价值结算或外设事实规则。
不新增模拟入口、夹具、伪造样本或 `--*-check` 回归入口。
```

## 4. 实施步骤

```text
S1d-5c-1：
    为已取主信息值写入入口增加可选观测时间戳参数，旧入口不传参时仍在内部生成当前时间戳。

S1d-5c-2：
    `私有_获取动态二次特征` 分步结果生成开始后生成一次批次观测时间戳。

S1d-5c-3：
    标量 / 离散分步值写入调用传入批次观测时间戳。

S1d-5c-4：
    构建 Debug x64，真实运行 `--self-check-log --self-check-log-wait-ms=10000`。
```

## 5. 验证预测

```text
构建：
    Debug x64 构建通过。

真实运行：
    退出码=0。
    日志继续出现 `动态二次特征分步结果/内部阶段耗时`。
    `分步值写入` 平均耗时下降，或 `动态二次特征分步值写入/内部阶段耗时` 的标量 / 离散均值下降。
    日志保留 `任务状态动作动态=<动态主键>`。
    不出现 `任务状态被动提交/逻辑错误`。
    不出现 `任务状态动作动态逻辑错误`。
    不出现旧路径锚点 `准备本能调用`、`执行本能方法`、`任务域本能输入包`。
```

## 6. 下一步判断

```text
如果 `分步值写入` 明显下降：
    按最新高层最慢阶段继续切片。

如果下降不明显：
    停止分步值写入支线；剩余主因大概率是合法结果数量聚合，不再削减结果数量。

如果高层主慢点转回 `命中回写因果实例`：
    转回因果实例回写阶段。
```

## 7. 落实结果

```text
已落实：
    已取主信息值写入入口增加可选观测时间戳参数；
    旧 `写入标量值` / `写入离散值` 不传观测时间戳，保持内部生成当前时间戳的旧行为；
    `私有_获取动态二次特征` 分步结果生成批次开始时生成一次观测时间戳；
    标量 / 离散分步值写入调用已传入同一批次观测时间戳；
    每个节点仍调用 `统计.记录观测`，不减少命中次数。

验证：
    `git diff --check` 通过，仅有 CRLF 工作区转换警告；
    Debug x64 构建通过；
    真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0。

本轮日志窗口：
    最新启动行：`日志/鱼巢_run_20260608.低值g:58387`；
    `任务状态动作动态=` 69 条；
    `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包` 均为 0。

性能证据：
    `动态二次特征分步结果/内部阶段耗时` 6 条；
    `分步值写入` 平均约 69346us，较 S1d-5b 的约 70124us 下降不明显；
    标量值写入平均约 34973us，离散值写入平均约 34372us；
    平均标量 / 离散值写入次数均约 70；
    `二次特征值写入/内部阶段耗时` 0 条，单次值写入未超过慢采样阈值。

结论：
    S1d-5c 已闭合；
    批次观测时间戳复用安全但收益不足；
    剩余主因不是时间戳读取，而是值写入快路径内部逐次计时 / 采样的小成本聚合。

下一步：
    进入 S1d-5d：二次特征值写入批次静默快路径瘦身。
```
