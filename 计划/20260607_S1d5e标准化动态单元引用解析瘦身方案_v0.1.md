# S1d-5e 标准化动态单元引用解析瘦身方案 v0.1

日期：2026-06-08

## 1. 目标

```text
承接 S1d-5d。

S1d-5d 已闭合但性能收益不成立：
    `分步值写入` 平均约 73683us，较 S1d-5c 的约 69346us 未下降；
    值写入仍是合法标量 / 离散结果数量聚合；
    后续不再沿分步值写入支线削减合法结果。

最新高层证据：
    `动态二次特征生成/内部阶段耗时` 9 条，平均约 187254us；
    `分步结果生成` 平均约 109469us；
    `标准化动态单元` 平均约 33358us；
    `命中回写因果实例` 平均约 31698us；
    `分步语义绑定` 平均约 31298us。

本切片目标：
    先处理尚未细分过的 `标准化动态单元`；
    来源动态引用解析采用指针优先，主键查找保留为兜底；
    聚合来源动态列表按来源数量预留容量，减少 vector 扩容 / 复制；
    保留整体片段解析、来源链合法性判断、比较关系和二次特征生成语义。
```

## 2. 允许修改

```text
二次特征应用模块.ixx
计划/20260607_S1d5e标准化动态单元引用解析瘦身方案_v0.1.md
计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
计划/计划索引.md
实施记录/20260608_S1d5e标准化动态单元引用解析_Codex断点清单.md
```

## 3. 禁止修改

```text
不改变动态来源链合法性。
不跳过整体动态或来源动态的视图片段解析。
不减少标准动态单元数量。
不改变比较结果、二次特征生成结果、因果模板匹配或任务状态动作动态。
不修改任务状态、需求满足、价值结算、日志 flush 策略或外设事实规则。
不新增模拟入口、夹具、伪造样本或 `--*-check` 回归入口。
```

## 4. 实施步骤

```text
S1d-5e-1：
    调整 `私有_解析动态引用`，当引用指针有效且主键为空或与指针主键一致时，优先返回指针；主键查找保留兜底。

S1d-5e-2：
    `私有_标准化动态单元列表` 在来源链非空时按 `来源低层动态.size()` 预留结果容量。

S1d-5e-3：
    构建 Debug x64，真实运行 `--self-check-log --self-check-log-wait-ms=10000`。
```

## 5. 验证预测

```text
构建：
    Debug x64 构建通过。

真实运行：
    退出码=0。
    日志继续出现 `动态二次特征生成/内部阶段耗时`。
    `标准化动态单元` 平均耗时下降。
    日志保留 `任务状态动作动态=<动态主键>`。
    不出现 `任务状态被动提交/逻辑错误`。
    不出现 `任务状态动作动态逻辑错误`。
    不出现旧路径锚点 `准备本能调用`、`执行本能方法`、`任务域本能输入包`。
```

## 6. 下一步判断

```text
如果 `标准化动态单元` 明显下降：
    按最新高层最慢阶段继续切片。

如果下降不明显：
    标准化动态单元先转入内部阶段定位，不继续凭推断扩大改动。

如果高层主慢点转回 `命中回写因果实例`：
    转回因果实例回写阶段。
```

## 7. 落实结果

```text
已落实：
    `私有_解析动态引用` 改为引用指针有效且主键为空或与指针主键一致时优先返回指针；
    主键查找仍保留为兜底；
    `私有_标准化动态单元列表` 在来源链非空时按来源低层动态数量预留结果容量。

验证：
    `git diff --check` 通过，仅有 CRLF 工作区转换警告；
    Debug x64 构建通过；
    真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0。

本轮日志窗口：
    最新启动行：`日志/鱼巢_run_20260608.低值g:64017`；
    `任务状态动作动态=` 74 条；
    `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包` 均为 0。

性能证据：
    `动态二次特征生成/内部阶段耗时` 9 条，平均约 161980us；
    `标准化动态单元` 平均约 98us，较 S1d-5d 的约 33358us 明显下降；
    `分步结果生成` 平均约 108693us，最慢阶段仍均为 `分步结果生成`；
    `创建动态/内部阶段耗时` 18 条，最慢阶段分布转为 `同类事件段聚合=17`、`动态二次特征=1`；
    `同类事件段聚合` 平均约 163712us。

结论：
    S1d-5e 已闭合；
    标准化动态单元指针优先有效；
    新主慢点转到创建动态后处理中的 `同类事件段聚合`。

下一步：
    进入 S1d-5f：同类事件段聚合引用比较与路径签名预筛瘦身。
```
