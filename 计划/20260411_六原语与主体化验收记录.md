# 2026-04-11 六原语与主体化验收记录

## 1. 验收入口

本轮新增一键验收命令：

```powershell
.\x64\Debug\鱼巢.exe --acceptance-report
```

该命令覆盖 3 个场景：

1. 运行态任务治理摘要
2. 运行态学习补方法通道
3. 白盒补原语/治理本能通道

---

## 2. 代码落点

- [../鱼巢.cpp](../鱼巢.cpp)
- [../README.md](../README.md)

主要新增内容：

1. `--acceptance-report` 命令行入口
2. 白盒“补原语/治理本能”学习实体构造
3. 白盒学习执行 4 步推进
4. 场景级通过/未通过结论汇总
5. 运行日志同步写入

---

## 3. 验收结果

执行时间：`2026-04-11`

执行命令：

```powershell
.\x64\Debug\鱼巢.exe --acceptance-report
```

结果：`通过`

---

## 4. 场景结论

### 4.1 场景 1：运行态任务治理

已确认以下字段在运行态摘要中可见：

- `主体ID`
- `分身ID`
- `需求位`
- `本能缺口`
- `治理态型`

说明：

当前运行态治理链已经把主体层桥接字段带入专项摘要、治理实例堆摘要和恢复快照摘要。

### 4.2 场景 2：运行态学习补方法

已确认运行态学习摘要中可见：

- `通道=补方法`

说明：

当前自然运行态仍主要命中“补方法”学习链，这与当前主循环真实运行结果一致。

### 4.3 场景 3：白盒补原语/治理本能

白盒场景构造：

- `来源方法需求位 = 补结构`
- `来源本能能力缺口类型 = 缺状态差分取证`
- `来源最小原语位图 = 读取/写入/动作开始/动作结束`
- `来源主观察特征 = 白盒验收/主观察/状态差分`

执行结果：

1. 第 1 步进入 `采样`
2. 第 2 步进入 `更新`
3. 第 3 步进入 `验证`
4. 第 4 步进入 `回滚`

终态确认：

- `通道=补原语/治理本能`
- `策略=镜像验证`
- `结果=影子验证通过`
- `状态=已回滚`
- `未进入正式方法资产提交`

说明：

这证明“补原语/治理本能”已从纸面字段升级为可重复触发、可执行、可验收的工程分支。

---

## 5. 当前结论

截至本记录形成时：

1. `六原语 + 固定派生规则` 已完成工程落地
2. 主体/分身/需求位/本能缺口已进入运行态摘要
3. 学习链已能区分 `补方法` 与 `补原语/治理本能`
4. 白盒补原语分支已能稳定走到 `镜像验证 -> 影子验证通过 -> 回滚等待再次命中`

---

## 6. 下一步建议

下一步最值得做的是把这份一键验收继续扩成“真实运行态触发原语缺口”的半白盒场景，而不只依赖白盒构造。

建议优先补两类真实触发：

1. `缺状态差分取证`
2. `缺轻量因果链组织`

这样后续就可以同时验证：

- 自然运行态补方法
- 自然运行态补原语
- 白盒补原语
