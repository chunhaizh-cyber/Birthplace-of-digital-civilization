# 2026-04-10 旧计划核查与续写：20260403 自我主循环重构计划

## 对应旧计划

- 旧计划：`D:\鱼巢\计划\计划\归档\20260405_旧计划\20260403_自我主循环重构计划.md`

## 当前核查结论

- 状态：部分完成
- 已完成部分：
  - `自我线程类` 已正式接入
  - 时间本能、治理、学习调度、学习执行已串成最小运行链
- 仍缺部分：
  - 消息消费与结果归并车道
  - 更完整的根层重判 -> 门控 -> 最终去向
  - 多车道运行时和失败恢复面

## 新计划

### P1-R：主循环阶段正规化

1. 把当前线程摘要里的阶段，正式固化成主循环步骤
2. 让每一步都有稳定输入/输出

当前落实情况：

- 已完成
- 已把 `自我初始化 / 线程启动 / 运行态 / 主循环初始化补齐 / 主循环主链回填 / 主循环先天维护 / 主循环上位输入刷新 / 主循环任务治理 / 主循环学习调度 / 主循环学习执行 / 主循环心跳巡检 / 停止请求中 / 线程收尾 / 线程停止 / 线程故障` 固化成正式运行阶段枚举
- 已统一接入 `自我线程快照 / 运行阶段事件 / 自检日志 / 控制面板读取链`
- 已验证 `--task-mgmt-summary`、`--learning-summary`、`--self-check-log` 仍能稳定输出阶段文本

### P2-R：结果归并与最终去向

1. 补消息/结果归并
2. 补最终 `继续 / 等待 / 收束 / 停止 / 转学习` 正式裁决

当前落实情况：

- 已完成
- 已把任务管理已算出的 `总控结果 / 反馈类型 / 反馈摘要` 正式并入自我线程循环结果、线程快照、控制面板运行时摘要与自检日志
- 已确认上层读取链不再只看到 `根层重判 / 门控 / 缺口 / 去向`，而能直接看到当前治理后果裁决面
- 已把 `继续提交 / 回到重筹办 / 转入学习 / 进入收束 / 待机 / 停止` 收成线程主循环自己的统一最终去向枚举
- 已验证当前稳定等待治理面下，线程最终去向会拍成 `转入学习`
- 已把 `主循环/消息归并` 正式补入线程运行阶段，并补了最小归并面：`主循环归并来源 / 主循环归并摘要`
- 已让主循环把 `任务治理结果` 与 `学习执行/学习回流结果` 收成同一条下一轮输入摘要，再同步写入线程快照、控制面板和自检日志
- 已验证日志中出现 `当前阶段=主循环/消息归并` 与 `归并=...`

### P3-R：多车道与失败恢复

1. 拆学习、治理、延迟事实等运行时车道
2. 补线程失败恢复与重启后回读

当前落实情况：

- 已完成
- 已把 `主消息/心跳 / 治理 / 学习 / 延迟事实 / 根任务结构调整` 收成统一运行时车道摘要
- 已把车道状态正式并入 `自我线程循环结果 / 线程快照 / 控制面板摘要 / 自检日志`
- 已验证日志中出现 `车道=主消息/心跳=活跃 | 治理=... | 学习=... | 延迟事实=未接入 | 结构调整=...`
- 已补 `故障次数 / 恢复次数 / 上次故障 / 最近恢复摘要`
- 已把线程故障收口后的“待恢复启动”与下次启动时的回读说明并入线程快照
- 已把恢复启动状态同步到控制面板摘要与自检日志
