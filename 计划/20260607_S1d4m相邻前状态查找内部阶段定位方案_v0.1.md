# S1d-4m 相邻前状态查找内部阶段定位方案 v0.1

日期：2026-06-08

## 1. 目标

```text
承接 S1d-4l。

S1d-4l 已定位：
    指针优先解析后，最新线程 T39044 的 `状态动态刷新` 平均约 642089us；
    较 S1d-4k 的约 849007us 有下降；
    但最新线程 28 条状态创建慢样本最慢阶段仍全部是 `状态动态刷新`。

代码路径：
    `状态类::创建状态`
    -> `动态类::状态记录后刷新动态`
    -> `动态类::查找相邻前状态`
    -> `动态类::创建叶子动态`

本切片目标：
    拆 `动态类::查找相邻前状态` 内部阶段；
    确认剩余耗时来自状态索引快照复制、倒序扫描规模、引用解析、当前状态跳过还是候选匹配判定。
```

## 2. 允许修改

```text
动态类.cpp
计划/20260607_S1d4m相邻前状态查找内部阶段定位方案_v0.1.md
计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
计划/计划索引.md
实施记录/20260608_S1d4m相邻前状态查找内部阶段定位_Codex断点清单.md
```

## 3. 禁止修改

```text
不删除 `状态记录后刷新动态`。
不改变 `查找相邻前状态` 返回语义。
不改变 `创建叶子动态`、方法动作动态结构或任务状态动作动态来源。
不新增基础信息全局索引、候选方法索引或因果主果索引。
不把任务状态变化改成裸写状态。
不新增模拟入口、夹具、伪造样本或 `--*-check` 回归入口。
```

## 4. 实施步骤

```text
S1d-4m-1：
    在 `动态类.cpp` 中增加低频阶段采样工具。

S1d-4m-2：
    在 `动态类::查找相邻前状态` 内记录：
        前置检查耗时；
        状态索引快照耗时；
        倒序扫描耗时；
        状态索引数量；
        扫描数量；
        解析成功数量；
        当前状态跳过数量；
        基础动态判定失败数量；
        是否命中前状态。

S1d-4m-3：
    采样锚点：`相邻前状态查找/内部阶段耗时`。
    采样规则：
        总耗时低于 100000us 不输出；
        慢调用前 3 次输出；
        之后每 10 次输出；
        总耗时不低于 500000us 输出。

S1d-4m-4：
    构建 Debug x64，真实运行 `--self-check-log --self-check-log-wait-ms=10000`。

S1d-4m-5：
    汇总最新线程的 `相邻前状态查找/内部阶段耗时`，决定下一步是否可做状态索引扫描瘦身。
```

## 5. 验证预测

```text
构建：
    Debug x64 构建通过。

真实运行：
    退出码=0。
    日志出现 `相邻前状态查找/内部阶段耗时`。
    日志保留 `任务状态动作动态=<动态主键>`。
    不出现 `任务状态被动提交/逻辑错误`。
    不出现 `任务状态动作动态逻辑错误`。
    不出现旧路径锚点 `准备本能调用`、`执行本能方法`、`任务域本能输入包`。

性能判断：
    如果 `倒序扫描` 为主耗时且扫描数量接近状态索引数量，下一步优化状态索引扫描策略。
    如果 `状态索引快照` 为主耗时，下一步优化快照复制范围或锁内读取方式。
    如果两者都不明显，继续拆候选匹配判定。
```

## 6. 阻塞条件

```text
如果诊断日志破坏动作动态账或真实运行异常，停止并修正。
如果发现下一步必须新增基础信息全局索引才能继续，停止在证据和方案，不直接实现。
```

## 7. 落地结果

```text
已落实：
    `动态类::查找相邻前状态` 增加低频阶段采样；
    采样锚点为 `相邻前状态查找/内部阶段耗时`；
    采样不改变相邻前状态查找返回语义，不改变状态动态刷新或创建叶子动态。

验证：
    Debug x64 构建通过；
    `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；
    日志/鱼巢_run_20260608.低值g 保留 `任务状态动作动态=<动态主键>`；
    未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。

定位结果：
    最新线程 T29128 未出现 `相邻前状态查找/内部阶段耗时`；
    说明 `查找相邻前状态` 未超过 100000us 慢采样阈值；
    同线程状态创建内部采样 19 条，最慢阶段仍为 `状态动态刷新=19`；
    `状态动态刷新` 平均约 612992us，最大 908455us。

结论：
    剩余主耗时不在相邻前状态查找；
    下一步进入 S1d-4n，拆 `创建动态` 内部阶段，重点看已有状态动态扫描、动态索引追加排序和动态二次特征生成。
```
