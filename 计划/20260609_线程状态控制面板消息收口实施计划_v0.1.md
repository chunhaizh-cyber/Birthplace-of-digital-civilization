# 线程状态控制面板消息收口实施计划 v0.1

日期：2026-06-09

## 1. 依据

```text
规范/方法动作动态硬规则规范20260512.md
规范/自我线程与任务管理线程权责边界规范20260512.md
规范/线程生命周期状态上报与控制面板线程信息规范20260609.md
规范/自我内部循环实现规范20260512.md
计划/计划索引.md
```

当前计划索引已将本计划转入主动执行。当前只执行 T1 控制面板线程状态页先行闭合；P1-P5 的真实线程状态消息接入仍按后续切片推进。

2026-06-09 补充执行切片：

```text
T1 控制面板线程状态页先行闭合。
    上部显示线程统计：已知项目线程总数、当前运行数、各状态数量、已结束数和故障数。
    中间栏显示完整项目线程列表，不分页、不缩略、不省略。
    线程信息正式来源是 消息中间件 文件缓存和控制面板线程信息模块通过线程生命周期消息收到的数据。
    不显示 OS / 系统线程枚举；系统线程 ID 只作为项目线程详情字段。
    右侧栏显示选中线程详细信息，包括用途、名称、逻辑 ID、系统线程 ID、加载时间、运行时长、状态、最近事件和说明。
    字段来源不足时显示 未记录 / 待线程生命周期消息接入，不用控制面板快照时间伪装线程创建时间。
    本切片只改控制面板显示层和文档，不改变任务治理上行协议、自我治理邮箱或任务状态提交入口。
```

## 2. 目标

```text
1. 每个线程创建时，由创建者向控制面板线程发送创建消息。
2. 创建消息至少明确线程用途、名称、逻辑 ID、系统线程 ID、创建者、所属模块和初始状态。
3. 线程运行、阻塞、暂停、恢复、请求停止、退出前、已退出和故障等状态变化，统一发送给控制面板线程信息模块。
4. 控制面板收到消息后及时更新线程信息模块数据，并让控制面板线程状态页读取到最新事实镜像。
5. 为处理线程创建先后顺序，纯线程状态消息先写入 `消息中间件` 文件缓存，再由控制面板线程信息模块按发生时间和消息 ID 消费。
6. 纯线程状态不再进入自我线程治理邮箱，不生成主动动态、被动动态、方法动作动态、世界树事实或需求树输入。
7. 任务管理界面线程只收发任务管理工作线程相关消息；任务治理材料仍按任务管理上行协议进入自我线程。
```

## 3. 非目标

```text
1. 不改任务状态提交入口；任务状态变化仍必须通过 提交任务状态变化。
2. 不把控制面板线程信息表作为任务状态、需求满足、世界事实或安全 / 服务结算权威。
3. 不把线程创建、暂停、堵塞、退出解释为方法动作动态。
4. 不新增模拟测试、夹具入口或伪造线程状态样本。
5. 不恢复任务筹办 / 任务执行为本能方法。
```

## 4. 影响范围

```text
控制面板类.ixx / 控制面板类.cpp：
    增加线程状态消息接收、线程信息表更新和页面读取。

控制面板摘要线程模块.*：
    接入自身创建、运行状态变化和退出上报；避免摘要线程后台状态只存在于局部日志。

自我线程模块.*：
    接入自身创建、堵塞 / 等待 / 恢复、退出前上报；移除纯线程状态进入治理邮箱的入口或消费。

任务模块.管理界面线程.*：
    接入界面线程自身上报；拆分纯线程状态和任务治理上行消息。

任务模块.管理工作线程.* / 任务模块.管理工作线程池.*：
    由创建者上报工作线程创建；工作线程自身上报运行、阻塞、暂停、退出和故障。

外设独占线程 / 外设代理线程相关实现：
    按 002002 后续接入，不允许外设线程状态直接写自我治理邮箱或世界树真值。
```

## 5. 执行切片

```text
P0 只读盘点：
    搜索现有线程创建点、线程状态快照、控制面板线程状态树、自我治理邮箱中线程状态变化入口。
    输出允许修改文件清单和禁止触碰边界。

P1 协议与数据结构：
    新增或复用线程状态消息结构，包含消息 ID、事件类型、时间、线程用途、名称、逻辑 ID、系统线程 ID、创建者、模块、旧状态、新状态、堵塞 / 暂停 / 健康标志和原因键。
    新增控制面板线程信息表或扩展现有快照表，按逻辑线程 ID 幂等更新。

P1a 消息中间件缓存：
    使用 `消息中间件` 目录缓存线程生命周期消息文件。
    发送方先写临时文件，写完后原子改名为正式消息文件，避免半截消息被消费。
    文件名包含消息类型、发生时间和消息 ID；内容保存结构化字段，不保存自然语言摘要替代结构。
    控制面板线程启动时先扫描未消费文件，再消费运行期新增文件。
    已消费消息移动到已消费子目录或记录消费游标；不得删除未确认消费消息。
    消息中间件只缓存项目自身线程生命周期消息，不缓存 OS / 系统线程枚举。

P2 控制面板消费：
    控制面板线程及时消费 `消息中间件` 中的线程状态消息，更新线程信息模块数据。
    同状态重复消息可合并；退出 / 故障消息不得被旧运行消息覆盖。

P3 创建与退出上报：
    自我线程、任务管理界面线程、任务管理工作线程池、控制面板摘要线程先接入创建消息和退出前消息。
    创建者负责创建消息；线程自身负责退出前消息；异常退出由创建者或管理者补报故障。

P4 状态变化上报：
    在线程运行、阻塞 / 等待、暂停、恢复、请求停止、故障等状态变化点发送控制面板消息。
    发送失败只记录独立错误日志，不阻塞业务线程。

P5 自我治理邮箱收口：
    移除或停用纯线程状态变化进入自我线程治理邮箱的路径。
    保留任务管理派生需求、任务管理结果状态、执行前许可、观察事实承接和自检报告等治理消息。

P6 显示与真实验证：
    控制面板线程状态页上部展示线程统计：已知项目线程总数、当前运行数、各状态数量、已结束数和故障数。
    中间栏展示完整项目线程列表，不分页、不缩略、不用 “等 N 项” 代替真实线程条目。
    线程状态页只读取控制面板线程信息模块从 `消息中间件` 消费得到的项目线程信息；不得显示 OS / 系统线程枚举。
    右侧栏展示选中线程用途、名称、逻辑 ID、系统线程 ID、创建者、加载时间、运行时长、当前状态、最近事件和更新时间。
    用真实运行日志、状态快照和控制面板数据核对，不使用伪造入口。
```

## 6. 验证证据

```text
1. 修改代码前写预测：哪些线程应出现创建消息，哪些状态变化应刷新控制面板，哪些消息不得进入自我治理邮箱。
2. Debug x64 构建退出码 0。
3. 真实运行 self-check-log 退出码 0。
4. 最新日志窗口能看到自我线程、任务管理界面线程、任务管理工作线程和控制面板摘要线程的创建 / 退出或状态变化上报。
5. `消息中间件` 中能看到结构化线程生命周期消息文件，控制面板线程信息模块能消费并读到线程用途、名称、逻辑 ID、系统线程 ID、当前状态、加载时间、运行时长和最近更新时间。
6. 线程状态页上部统计能显示项目线程当前运行数、各状态数量、已结束数和故障数。
7. 线程状态页中间栏完整显示通过消息登记的项目线程列表，不省略任何已知项目线程条目；右侧栏显示选中线程详细信息。
8. 自我线程治理邮箱不再出现纯线程创建、暂停、堵塞、恢复、退出或故障消息。
9. 任务管理结果状态、派生需求、执行前许可和观察事实承接仍按原治理协议运行。
10. 未新增线程状态动作动态、世界树事实或需求树输入。
```

## 7. 风险与回退

```text
风险 1：线程状态消息发送点持锁过深，可能放大阻塞。
处理：状态上报只写轻量队列；不可在业务锁内等待控制面板消费。

风险 2：任务工作线程状态和任务治理结果混淆。
处理：纯状态只发控制面板；绑定任务执行尝试和任务治理语义的材料仍发任务管理界面线程，再由其按上行协议处理。

风险 3：控制面板不可用导致业务线程被拖住。
处理：发送失败记录独立日志并继续；控制面板线程信息表不是业务权威。

风险 4：退出消息丢失。
处理：线程自身退出前发送；创建者 / 管理者在 join 或异常路径补报最终状态。

风险 5：控制面板线程启动晚于其他线程，创建消息丢失。
处理：线程状态消息先写入 `消息中间件` 文件缓存；控制面板启动后扫描未消费消息重放。

风险 6：消息文件写到一半被消费。
处理：发送方先写临时文件，完成后原子改名为正式消息文件；控制面板只消费正式文件。
```

## 8. 暂停条件

```text
1. 发现现有代码把线程状态作为任务状态、需求输入、世界事实或动作动态来源使用。
2. 发现控制面板线程信息表需要新增主信息结构并可能违反 005 新增准入约束。
3. 发现线程状态发送需要在关键业务锁内同步等待控制面板消费。
4. 真实验证必须伪造线程样本才能通过。
```

## 9. T1 执行记录

```text
执行时间：2026-06-09

已完成：
    控制面板线程状态页上部统计已落地。
    中间栏线程列表改为完整显示，不分页、不缩略、不用 “等 N 项” 替代。
    右侧栏线程详情字段已补齐加载时间、运行时长、状态、用途、名称、逻辑 ID、系统线程 ID、最近事件和说明。
    字段来源不足时显示 未记录 / 待线程生命周期消息接入。
    已按用户修正撤销系统线程摘要和当前进程系统线程枚举项；页面只显示项目自身线程信息。

验证证据：
    Debug x64 构建退出码 0。
    --self-check-log --self-check-log-wait-ms=10000 退出码 0。
    --panel-html 退出码 0，已生成 控制面板.html。
    控制面板.html 已出现 项目线程统计、线程列表、完整线程列表提示、加载时间、运行时长和线程详细信息字段，且不再出现系统线程列表。

未完成：
    P1-P5 的线程状态消息结构、消息中间件文件缓存、真实上报发送点和自我治理邮箱收口尚未执行。
    任务管理界面线程与任务工作线程的真实状态上报仍待后续切片接入。
```

## 10. T1 修正记录

```text
修正时间：2026-06-09

用户修正：
    线程状态页不显示系统线程。
    只显示当前项目自身创建的线程信息。
    线程信息应通过消息获取。
    为处理线程创建先后问题，线程相关消息先通过 `消息中间件` 文件缓存。

本次修正：
    控制面板线程状态页撤销当前进程系统线程枚举。
    线程状态页统计改为已知项目线程、当前运行、未启动、已结束和故障。
    线程状态页菜单徽标改为项目线程数量，不再使用当前进程线程数。
    规范新增 `消息中间件` 文件缓存规则。
    计划新增 P1a 消息中间件缓存切片。

验证证据：
    Debug x64 构建退出码 0。
    --panel-html 退出码 0，已重新生成 控制面板.html。
    控制面板.html 已出现 已知项目线程、来源=消息中间件/项目线程快照、项目线程状态统计、加载时间、运行时长和线程详细信息字段。
    控制面板.html / 控制面板类.cpp 未检出 系统线程摘要、thread-system、系统线程数、当前进程线程数、TID= 或 ToolHelp 线程枚举。

验证风险：
    --self-check-log --self-check-log-wait-ms=10000 本次退出码 1。
    最新日志错误锚点为既有 D455 条件不足和执行桥逻辑错误，未检出线程状态页或系统线程枚举相关错误。
    本修正切片不把该自检非零退出作为通过证据；后续需要单独排查自检运行期错误。
```

## 11. P1-P6 闭合记录

```text
闭合时间：2026-06-09

已完成：
    P1/P1a：线程生命周期消息结构和 `消息中间件` 文件缓存已接入。
    P2：控制面板线程信息表可消费消息中间件并持久化项目线程快照。
    P3/P4：self-main、task-interface、task-worker-pool、task-worker-1..20、control-panel-summary 已接入创建、运行和退出类生命周期上报。
    P5：纯线程状态变化进入自我治理邮箱的旧路径已停用；任务管理线程状态消息不再借用 `工作线程状态消息` / `线程状态变化` 载荷。
    P6：控制面板线程状态页显示消息中间件项目线程快照，未显示 OS / 系统线程枚举。

验证证据：
    Debug x64 构建退出码 0。
    `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=1000` 退出码 0。
    `.\x64\Debug\鱼巢.exe --panel-html` 退出码 0。
    日志/鱼巢_run_20260609.低值g 第 229446 行后：
        `线程状态治理消息跳过落账` 0；
        `工作线程状态消息` 0；
        `线程状态变化上报` 0；
        任务管理线程状态入队 70；
        任务管理结果状态 35；
        任务管理派生需求 71；
        执行前许可 3；
        观察事实 12；
        自检报告 9；
        执行桥逻辑错误 0。
    控制面板.html：
        已知项目线程=24；
        来源=消息中间件/项目线程快照；
        包含 self-main、task-interface、task-worker-pool、task-worker-1、control-panel-summary；
        未检出 系统线程列表、当前进程线程数、ToolHelp、TID=。
    消息中间件/control_panel_thread_info_table.cache：
        项数=24；
        包含 self-main、task-interface、task-worker-pool、task-worker-1、control-panel-summary。

边界：
    未新增线程状态动作动态、世界树事实、需求树输入或任务状态提交入口。
    未处理既有执行桥逻辑错误；该问题不属于线程消息收口计划。
    不宣称自我苏醒完成或内部循环整体完成。
```
