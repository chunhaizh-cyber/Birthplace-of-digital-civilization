# S1d-4w 分步值写入聚合调用定位与瘦身方案 v0.1

日期：2026-06-08

## 1. 目标

```text
承接 S1d-4v。

S1d-4v 已闭合：
    单次 `二次特征签名查找创建/内部阶段耗时` 未超过 5000us 慢采样阈值；
    `分步签名查找创建` 聚合平均降到约 43477us；
    分步结果当前最慢阶段转到 `分步值写入`。

当前判断：
    最新线程 T24124 分步结果样本 5 条；
    平均 `结果数` 约 79，平均 `标量结果调用数` 约 39，平均 `离散结果调用数` 约 39；
    `分步值写入` 平均约 54277us，`分步差值写入` 平均约 15us；
    `二次特征值写入/内部阶段耗时` 和 `二次特征概念模板刷新/内部阶段耗时` 均未出现，说明单次值写入未超过 5000us，剩余是多次小调用聚合。

本切片目标：
    拆分分步值写入聚合耗时到标量 / 离散两类；
    保留值写入和观测统计；
    仅在值、形态和已有概念模板都未变化时，跳过重复概念模板刷新。
```

## 2. 允许修改

```text
二次特征应用模块.ixx
二次特征类.cpp
计划/20260607_S1d4w分步值写入聚合调用定位与瘦身方案_v0.1.md
计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
计划/计划索引.md
实施记录/20260608_S1d4w分步值写入_Codex断点清单.md
```

## 3. 禁止修改

```text
不跳过正式值写入。
不跳过观测统计记录。
不改变二次特征值语义。
不改变概念模板匹配规则。
不改任务状态、需求满足、价值结算或外设事实规则。
不新增模拟入口、夹具、伪造样本或 `--*-check` 回归入口。
```

## 4. 实施步骤

```text
S1d-4w-1：
    在 `动态二次特征分步结果/内部阶段耗时` 中补充分步标量值写入、分步离散值写入和对应次数。

S1d-4w-2：
    当 `分步值写入` 超过阈值时输出 `动态二次特征分步值写入/内部阶段耗时`。

S1d-4w-3：
    `写入标量值` / `写入离散值` 保留字段写入和观测记录；
    当值、形态和已有概念模板均未变化时，跳过重复 `刷新概念模板`。

S1d-4w-4：
    构建 Debug x64，真实运行 `--self-check-log --self-check-log-wait-ms=10000`。
```

## 5. 验证预测

```text
构建：
    Debug x64 构建通过。

真实运行：
    退出码=0。
    日志继续出现 `动态二次特征分步结果/内部阶段耗时`。
    若 `分步值写入` 仍超过 5000us，日志出现 `动态二次特征分步值写入/内部阶段耗时`。
    日志保留 `任务状态动作动态=<动态主键>`。
    不出现 `任务状态被动提交/逻辑错误`。
    不出现 `任务状态动作动态逻辑错误`。
    不出现旧路径锚点 `准备本能调用`、`执行本能方法`、`任务域本能输入包`。
```

## 6. 下一步判断

```text
如果 `分步值写入` 明显下降：
    观察分步结果主慢点是否转移，按最新慢点切下一片。

如果 `分步值写入` 仍高且标量 / 离散某一类占主导：
    下一片只针对占主导类别继续看底层写入、概念模板刷新或观测统计聚合。

如果 `动态二次特征生成` 主慢点转到 `命中回写因果实例`：
    停止分步结果线，切到因果实例回写阶段定位。
```

## 7. 落实结果

```text
已落实：
    `二次特征应用模块.ixx` 已拆分 `分步标量值写入`、`分步离散值写入` 和对应次数；
    `动态二次特征分步值写入/内部阶段耗时` 已在 `分步值写入` 超过 5000us 时输出；
    `二次特征类.cpp` 中 `写入标量值` / `写入离散值` 保留字段写入和观测记录；
    值、形态和已有概念模板均未变化时，重复 `刷新概念模板` 已跳过。

验证：
    `git diff --check` 通过，仅有 CRLF 工作区转换警告；
    Debug x64 构建通过；
    真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0。

本轮日志窗口：
    最新启动行为 `2026-06-08 11:47:06.220 [T13700] 程序启动`；
    `任务状态动作动态=` 64 条；
    `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包` 均为 0。

性能证据：
    `动态二次特征分步值写入/内部阶段耗时` 7 条；
    `分步值写入` 平均约 55753us，标量值写入平均约 27920us，离散值写入平均约 27832us；
    平均 `标量值写入次数` 约 54，平均 `离散值写入次数` 约 54，`分步差值写入` 平均约 2us；
    `二次特征值写入/内部阶段耗时` 和 `二次特征概念模板刷新/内部阶段耗时` 均为 0 条；
    分步结果样本 7 条，平均总耗时约 160309us，最慢阶段分布为 `分步值写入=4`、`分步签名查找创建=3`；
    动态二次特征生成样本 9 条，平均总耗时约 202715us，最慢阶段分布为 `分步结果生成=5`、`命中回写因果实例=4`。

结论：
    S1d-4w 已闭合；
    `分步值写入` 剩余主因是合法标量 / 离散结果数量聚合，不是单次值写入慢调用；
    继续削减结果数量有语义风险，本轮不跳过正式结果生成；
    下一步转到 `命中回写因果实例` 内部阶段定位 / 瘦身。

下一步：
    进入 S1d-4x：命中回写因果实例内部阶段定位 / 瘦身。
```
