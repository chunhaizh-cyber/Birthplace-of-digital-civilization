# S1d-5h 因果模板签名索引瘦身方案 v0.1

日期：2026-06-08

## 1. 目标

```text
承接 S1d-5g。

S1d-5g 已闭合：
    `按动态补全两层因果/内部阶段耗时` 91 条；
    最慢阶段分布 `动作模板查找=69`、`状态模板查找=1`；
    `动作模板查找` 平均约 15348us；
    `状态模板查找` 平均约 11523us。

当前判断：
    因果模板查找当前仍通过 `枚举因果模板()` 全量扫描并逐模板计算签名；
    真实日志已满足 S7 因果模板签名索引恢复条件；
    本切片只做状态变化签名 / 动作致变签名索引，不进入候选方法索引或基础信息全局索引。

本切片目标：
    为 `因果类` 增加状态变化关系模板索引和动作致变关系模板索引；
    索引按需懒构建，查找函数优先从索引返回；
    创建 / 模板签名相关字段变化 / 首次证据动态样本写入时刷新索引项；
    删除因果模板、绑定基础信息等低频结构边界仍整体失效；
    保留原模板签名计算和全量重建兜底。
```

## 2. 允许修改

```text
因果类.h
因果类.cpp
计划/20260607_S1d5h因果模板签名索引瘦身方案_v0.1.md
计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
计划/计划索引.md
实施记录/20260608_S1d5h因果模板签名索引_Codex断点清单.md
```

## 3. 禁止修改

```text
不改变因果模板签名定义。
不删除或跳过状态变化关系、动作致变关系、证据动态样本、证据实例或主结果桥接。
不新增基础信息全局索引、候选方法索引或因果主果索引。
不改变任务状态、需求满足、价值结算、二次特征生成结果或外设事实规则。
不新增模拟入口、夹具、伪造样本或 `--*-check` 回归入口。
```

## 4. 实施步骤

```text
S1d-5h-1：
    在 `因果类.h` 增加可变签名索引字段、索引互斥、确保索引和失效索引声明。

S1d-5h-2：
    在 `因果类.cpp` 实现懒构建：
        无动作模板进入状态变化签名索引；
        有动作模板进入动作致变签名索引；
        空签名不入索引。

S1d-5h-3：
    `查找状态变化关系模板` 和 `查找动作致变关系模板` 改为索引查找。

S1d-5h-4：
    创建因果、模板签名相关字段变更和首次证据动态样本写入时刷新单个索引项；
    删除因果、绑定基础信息时标记索引整体失效。

S1d-5h-5：
    构建 Debug x64，真实运行 `--self-check-log --self-check-log-wait-ms=10000`。
```

## 5. 验证预测

```text
构建：
    Debug x64 构建通过。

真实运行：
    退出码=0。
    日志继续出现 `按动态补全两层因果/内部阶段耗时`。
    `状态模板查找` 和 `动作模板查找` 平均耗时下降。
    日志保留 `任务状态动作动态=<动态主键>`。
    不出现 `任务状态被动提交/逻辑错误`。
    不出现 `任务状态动作动态逻辑错误`。
    不出现旧路径锚点 `准备本能调用`、`执行本能方法`、`任务域本能输入包`。
```

## 6. 下一步判断

```text
如果模板查找明显下降：
    按最新高层最慢阶段继续切片。

如果模板查找未下降：
    检查索引失效是否过频；不得直接扩大到全局基础信息索引。

如果主慢点转到主结果桥接或证据追加：
    单独开后续小切片。
```

## 7. 落实结果

```text
代码：
    `因果类` 已增加状态变化关系模板索引和动作致变关系模板索引；
    `查找状态变化关系模板` / `查找动作致变关系模板` 已改为按签名索引查找；
    创建模板、首次证据动态样本追加、果比较模板 / 条件模板 / 动作语义键变更改为索引项级刷新；
    删除因果和绑定基础信息仍保留整体失效；
    因果模板签名定义、证据动态样本、证据实例和主结果桥接均未跳过。

验证：
    `git diff --check -- 因果类.h 因果类.cpp 实施记录/20260608_S1d5h因果模板签名索引_Codex断点清单.md` 通过，仅 CRLF 提示；
    Debug x64 构建通过，只有既有 LNK4075 `/EDITANDCONTINUE` 与 `/INCREMENTAL:NO` 提示；
    直接前台运行因 Codex 工具管道超时留下孤立进程，已终止；随后用 stdout/stderr 重定向复跑同一命令，退出码 0；
    有效运行命令为 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000`。

性能证据：
    最新有效窗口 `日志/鱼巢_run_20260608.低值g:78951`；
    `按动态补全两层因果/内部阶段耗时` 16 条，平均约 7830us；
    `状态模板查找` 平均约 92us，较 S1d-5g 的约 11523us 明显下降；
    `动作模板查找` 平均约 48us，较 S1d-5g 的约 15348us 明显下降；
    最慢阶段分布转为 `主结果桥接=10`、`状态关系创建=4`、`动作关系创建=2`；
    任务状态动作动态数量 80，未检出任务状态提交逻辑错误或旧完整本能执行路径锚点。

下一步：
    最新高层 `创建动态/内部阶段耗时` 19 条，最慢阶段全部为 `同类事件段聚合`；
    进入 S1d-5i：同类事件段聚合回潮定位 / 瘦身。
```
