# S1d-4x 命中回写因果实例内部阶段定位与瘦身方案 v0.1

日期：2026-06-08

## 1. 目标

```text
承接 S1d-4w。

S1d-4w 已闭合：
    分步值写入剩余耗时来自合法标量 / 离散结果数量聚合；
    单次值写入和概念模板刷新均未超过慢采样阈值；
    动态二次特征生成高层最慢阶段开始接近 `命中回写因果实例`。

当前判断：
    `命中回写因果实例` 对应 `私有_回写因果模板命中`；
    当前实现先对每个命中模板追加证据动态样本，再调用 `按动态创建因果信息`；
    尚未拆分证据样本追加和动态因果创建的内部耗时。

本切片目标：
    增加 `动态二次特征命中回写因果实例/内部阶段耗时`；
    拆分命中模板证据动态追加、按动态创建因果信息；
    只在证据显示重复工作明确时做瘦身，不改变因果实例回写语义。
```

## 2. 允许修改

```text
二次特征应用模块.ixx
因果类.cpp
计划/20260607_S1d4x命中回写因果实例内部阶段定位与瘦身方案_v0.1.md
计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
计划/计划索引.md
实施记录/20260608_S1d4x命中回写因果实例_Codex断点清单.md
```

## 3. 禁止修改

```text
不跳过合法因果实例回写。
不改变因果模板命中语义。
不改变二次特征条件 / 结果匹配语义。
不改变因果主信息结构。
不改任务状态、需求满足、价值结算或外设事实规则。
不新增模拟入口、夹具、伪造样本或 `--*-check` 回归入口。
```

## 4. 实施步骤

```text
S1d-4x-1：
    新增 `动态二次特征命中回写因果实例/内部阶段耗时` 慢采样。

S1d-4x-2：
    拆分 `私有_回写因果模板命中`：
        命中模板证据动态追加；
        按动态创建因果信息。

S1d-4x-3：
    构建 Debug x64，真实运行 `--self-check-log --self-check-log-wait-ms=10000`。
```

## 5. 验证预测

```text
构建：
    Debug x64 构建通过。

真实运行：
    退出码=0。
    日志继续出现 `动态二次特征生成/内部阶段耗时`。
    若 `命中回写因果实例` 仍超过 5000us，日志出现 `动态二次特征命中回写因果实例/内部阶段耗时`。
    日志保留 `任务状态动作动态=<动态主键>`。
    不出现 `任务状态被动提交/逻辑错误`。
    不出现 `任务状态动作动态逻辑错误`。
    不出现旧路径锚点 `准备本能调用`、`执行本能方法`、`任务域本能输入包`。
```

## 6. 下一步判断

```text
如果主耗时在命中模板证据动态追加：
    继续看命中模板数、样本查重和互斥等待。

如果主耗时在按动态创建因果信息：
    下一片进入 `因果类::按动态补全两层因果` 内部阶段定位。

如果 `命中回写因果实例` 不再是动态二次特征生成主慢点：
    停止本切片，按最新最慢阶段切下一片。
```

## 7. 落实结果

```text
已落实：
    `二次特征应用模块.ixx` 新增 `动态二次特征命中回写因果实例/内部阶段耗时`；
    `私有_回写因果模板命中` 已拆分命中模板证据动态追加和按动态创建因果信息；
    本切片未跳过合法因果实例回写，未改变因果模板命中语义。

验证：
    `git diff --check` 通过，仅有 CRLF 工作区转换警告；
    Debug x64 构建通过；
    真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0。

本轮日志窗口：
    最新启动行为 `2026-06-08 11:54:23.243 [T15496] 程序启动`；
    `任务状态动作动态=` 61 条；
    `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包` 均为 0。

性能证据：
    `动态二次特征命中回写因果实例/内部阶段耗时` 101 条；
    平均总耗时约 60065us，最慢阶段分布为 `命中模板证据动态追加=78`、`按动态创建因果信息=23`；
    `命中模板证据动态追加` 平均约 36839us，`按动态创建因果信息` 平均约 23224us；
    平均命中模板数约 396，最大 631；
    动态二次特征生成样本 9 条，平均总耗时约 203370us，`命中回写因果实例` 平均约 53457us。

结论：
    S1d-4x 已闭合；
    当前可优化点是命中模板证据动态追加的多模板循环；
    下一步应在 `因果类` 增加批量追加证据动态样本，保留每个模板的证据和计数，但把全局互斥从每模板一次收束为每批一次。

下一步：
    进入 S1d-4y：命中模板证据动态样本批量追加瘦身。
```
