# 统一动态信息生成入口计划 v0.1

日期：2026-07-02

状态：需求契约、需求树和当前绑定方法已确认；执行 S0-S1。

## 0. 承接的根需求

统一动态信息生成入口。主动动作动态、被动动作动态必须通过同一个动作动态生成入口创建，并固定动作动态主信息必填字段、主动 / 被动判定规则和空字段逻辑错误检查。

## 1. 需求契约摘要

```text
1. 动作动态必须固定填写：
   动态主体、动态特征、初始状态、结果状态、来源输入场景、来源输出场景。
2. 缺任一必填项就是逻辑错误，不创建动作动态。
3. 主动动作：
   来源动作非空，必须是有效方法头，必须校验动作句柄。
4. 被动动作：
   来源动作必须为空，来源动作名固定为“被动动作”，不读取方法头、动作句柄或本能 ID。
5. 来源执行成功、来源错误码退出动作动态主信息口径；控制面板先不显示。
6. 动态路径签名暂时保留，继续服务动态聚合和二次特征匹配。
7. 空字段检查和相关错误日志由预编译控制诊断输出，但关闭输出不得改变错误检测、不创建动态和终止语义。
```

## 2. 项目结构映射表

| 业务概念 | 项目承载结构 | 值类型 | 写入方 | 读取方 | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| 动作动态主体 | 动态节点主信息 `动态主体` | 基础信息节点引用 | 统一动作动态入口 | 动态类、因果类、二次特征应用模块、控制面板 | 非空检查；动态主体可回读 |
| 动作动态特征 | 动态节点主信息 `动态特征` | 特征节点引用 | 统一动作动态入口 | 动态类、因果类、二次特征应用模块 | 非空检查；状态主体 / 特征一致 |
| 初始状态 | 动态节点主信息 `初始状态` | 状态节点引用 | 统一动作动态入口调用方先构造 | 因果类、二次特征应用模块 | 非空检查；状态主信息可解析 |
| 结果状态 | 动态节点主信息 `结果状态` | 状态节点引用 | 统一动作动态入口调用方先构造 | 因果类、二次特征应用模块 | 非空检查；结果特征可解析 |
| 主动动作来源 | 动态节点主信息 `来源动作` | 方法节点引用 | 主动动作调用方 | 动态类、因果类 | 主动非空；方法头和动作句柄有效 |
| 被动动作标记 | 动态节点主信息 `来源动作名` | 语素入口：被动动作 | 被动动作调用方 | 动态类、因果类 | 被动时来源动作为空且动作名为被动动作 |
| 输入材料 | 动态节点主信息 `来源输入场景` | 场景节点引用 | 统一动作动态入口 | 动态类、因果类、追溯查询 | 非空检查；允许正式空场景 |
| 输出材料 | 动态节点主信息 `来源输出场景` | 场景节点引用 | 统一动作动态入口 | 动态类、因果类、追溯查询 | 非空检查；失败原因落输出场景特征 |
| 因果账本来源链 | 动态节点主信息 `来源低层动态` | 动态节点引用集合 | 统一动作动态入口 | 因果类、二次特征应用模块 | 来源动态可回读；不使用自由文本 |
| 聚合兼容键 | 动态节点主信息 `动态路径签名` | U64 | 动态类现有逻辑 | 二次特征应用模块 | 本轮保留现有读写 |

## 3. 需求树

| 子需求 | 任务承接 | 依赖 | 完成证据 | 父需求回写 |
| --- | --- | --- | --- | --- |
| S0 动作动态结构口径固化 | 本计划 | 契约确认 | 字段填写规则、主动 / 被动判定、删除字段口径明确 | 作为实现约束 |
| S1 现有动态生成点盘点 | 本计划 | S0 | 列出所有 `创建方法动作动态` / 普通动态绑定来源方法调用点 | 确认迁移范围 |
| S2 统一动作动态入口 | 代码切片 | S0-S1 | 主动 / 被动都走同一入口；必填缺失报逻辑错误 | 满足核心实现 |
| S3 主动 / 被动判定规则落地 | 代码切片 | S2 | 主动来源动作非空；被动来源动作为空且动作为被动动作 | 保证边界不混 |
| S4 旧调用点迁移 | 代码切片 | S2-S3 | 执行桥、扫描、任务状态、方法域、外设、结算调用点统一改造 | 消除分散生成 |
| S5 字段清理 | 代码切片 | S2-S4 | 来源执行成功 / 来源错误码退出主口径；控制面板不显示；动态路径签名保留 | 主结构收敛 |
| S6 空字段检查与日志 | 代码切片 | S2-S5 | 预编译控制检查存在；错误日志可检索；关闭日志不改变错误检测语义 | 排除逻辑错误 |
| S7 因果生成兼容 | 代码切片 | S2-S6 | 统一入口可选择触发因果生成；被动因果不进候选 / 学习 | 避免因果污染 |
| S8 回归验证 | 验证切片 | S2-S7 | 构建通过；主动、被动、结算路径运行；日志无空字段相关错误 | 根需求满足判据 |

## 4. 当前活动子需求

```text
当前活动子需求：
    S2 统一动作动态入口

当前停止点：
    S0 字段契约和 S1 调用点盘点已完成；进入统一入口代码实现。

父需求回写条件：
    S2 完成后，才能迁移各调用点并做字段清理。
```

## 5. 候选方法待确认

### 方法 A：动态类 / 世界树统一入口优先

```text
承接子需求：
    S0-S8。
修改范围：
    动态类.h / 动态类.cpp
    世界树类.h / 世界树类.cpp
    基础信息主信息类.h
    控制面板类.cpp
    SQL 投影相关逻辑
    各动作动态调用点
复用结构：
    动态节点主信息类、状态节点、场景节点、来源低层动态链、动态路径签名。
新增内容：
    新增统一动作动态创建入口；不新增业务字段或结构体。
覆盖：
    主动动作、被动动作、结算动作、调用点迁移和空字段检查。
不覆盖：
    不一次性补齐所有因果信息；不删除动态路径签名。
风险：
    调用点多，需分切片迁移。
验证：
    静态调用点检查、Debug x64 构建、主动 / 被动 / 结算路径真实运行、日志错误检索。
回退方式：
    保留旧入口包装层作为过渡，单切片失败时回退对应调用点。
父需求回写：
    每迁移一类调用点，回写对应 S 子需求状态。
```

### 方法 B：兼容包装入口先行

```text
承接子需求：
    S0-S6。
修改范围：
    先新增包装函数，旧 `创建方法动作动态` 暂保留，调用点逐步转入包装函数。
优势：
    改动较小，便于逐项迁移。
风险：
    旧入口继续存在，容易留下绕过统一契约的路径。
验证：
    先验证新入口和少量调用点，再分批迁移。
回退方式：
    回到旧调用点。
父需求回写：
    只能回写阶段性子需求，不能声明根需求满足。
```

### 方法 C：各模块本地修正

```text
承接子需求：
    仅局部覆盖 S4-S6。
修改范围：
    各调用点分别补状态端点、场景和空字段检查。
优势：
    单点修改快。
风险：
    不满足“统一动态信息生成入口”的根需求，后续仍会分裂。
验证：
    只能验证单点，不能证明统一入口完成。
回退方式：
    按调用点回退。
父需求回写：
    不推荐，不能完整回写根需求。
```

推荐方法：

```text
推荐方法 A。
选择规则：
    根需求是统一入口，不是局部补丁；必须把主动 / 被动判定、必填字段检查、来源执行成功 / 来源错误码退出和调用点迁移收束到动态类 / 世界树统一入口。
```

```text
当前绑定方法：方法 A：动态类 / 世界树统一入口优先
```

## 6. 验证要求

```text
1. 静态检查所有动作动态创建点是否统一入口。
2. Debug x64 构建通过。
3. 至少覆盖主动动作、被动动作、结算动作各一类真实路径。
4. 回归日志不得出现：
   动作动态空主体
   动作动态空特征
   动作动态空初始状态
   动作动态空结果状态
   动作动态空输入场景
   动作动态空输出场景
   被动动作绑定来源方法
   主动动作缺来源方法
5. 不靠关闭检查通过；预编译开关只控制诊断输出，不改变错误检测、不创建动态和终止语义。
```

## 7. 禁止宣称

```text
不宣称因果信息全部补齐。
不宣称需求树简化闭环、内部治理闭环、自我苏醒完成或初步成熟完成。
不把代码改造完成误说成真实路径验证完成。
```

## 8. S0-S1 执行记录

状态：S0 字段契约已固化；S1 现有生成点已盘点。下一步进入 S2 统一入口实现。

### 8.1 字段填写契约

| 字段 | 主动动作填写规则 | 被动动作填写规则 | 检查方式 |
| --- | --- | --- | --- |
| 动态主体 | 必填。填写被方法运行改变或记录运行事实的存在 / 治理虚拟存在 / 方法执行实例存在 | 必填。填写被提交事实所在的任务虚拟存在、方法虚拟存在、需求虚拟存在、记录宿主或结算宿主 | 统一入口空字段检查；空则逻辑错误，不创建动态 |
| 动态特征 | 必填。必须与初始状态、结果状态的状态特征一致 | 必填。必须与初始状态、结果状态的状态特征一致 | 统一入口一致性检查；不一致则逻辑错误 |
| 初始状态 | 必填。由调用方在进入统一入口前构造；无目标值变化时必须改用运行存在 / 提交记录等可记录事实状态 | 必填。由提交链在进入统一入口前构造；不能因“被动”而留空 | 统一入口空字段检查；空则逻辑错误 |
| 结果状态 | 必填。由调用方在进入统一入口前构造；失败、缺口、等待应落为输出场景或运行存在特征事实 | 必填。由提交链在进入统一入口前构造；失败或拒绝也要落为正式结果事实 | 统一入口空字段检查；空则逻辑错误 |
| 来源动作 | 必填且必须是有效方法头；必须有可执行动作句柄 | 必须为空；非空即判为逻辑错误 | 先判动作类型；被动不读取方法头和动作句柄 |
| 来源动作名 | 主动可由来源方法动作名推导；显式传入时只能作为动作语义入口，不替代方法头 | 必填为 `被动动作`；它是空占位动作，不可主动调用 | 被动动作名 + 空来源动作共同定义被动；非被动缺方法则逻辑错误 |
| 来源输入场景 | 必填；没有实际输入内容时传正式空输入场景 | 必填；没有实际输入内容时传正式空输入场景 | 统一入口空字段检查 |
| 来源输出场景 | 必填；方法结果、失败、缺口、等待必须落到输出场景或运行存在特征事实 | 必填；提交结果、拒绝、幂等结果必须落到输出场景或记录宿主事实 | 统一入口空字段检查 |
| 来源低层动态 | 可选；若有同源状态迁移、D0/D1、满足证据或上游动作动态，创建后追加 | 可选；用于承接上游主动动作、状态迁移或提交资格证据 | 后续因果 / 二次特征读取，不能用自由文本替代 |
| 动态路径签名 | 统一入口按现有动态路径签名逻辑生成并保留 | 同主动 | 静态检查保留字段；动态聚合和二次特征继续读取 |
| 来源执行成功 | 不作为动作动态主口径；后续停止由统一入口写入，控制面板先不显示 | 同主动 | 不作为机器判断；SQL 兼容字段本轮不作为显示字段 |
| 来源错误码 | 不作为动作动态主口径；错误、失败、缺口必须特征化 | 同主动 | 不作为机器判断；SQL 兼容字段本轮不作为显示字段 |

### 8.2 现有生成点盘点

| 位置 | 当前类型 | 当前问题 | 迁移判断 |
| --- | --- | --- | --- |
| `动态类::创建方法动作动态` / `世界树类::创建方法动作动态` | 旧统一外观入口 | 只接受主动方法；允许状态端点为空；写 `来源执行成功 / 来源错误码` | S2 改为主动 / 被动共用实现入口 |
| `任务模块.执行.ixx::私有_创建方法调用动作动态_执行桥_最小_v0` | 主动方法调用过程动态 | 没有初始 / 结果状态，只记录方法调用过程 | 必须改为方法执行实例 / 运行存在状态端点，或不生成完整动作致变动态 |
| `自我动作实现模块.ixx:8551` 方法域提交 | 被动状态动作 | 传入提交方法头 + `被动动作`；无变化时状态端点为空 | 被动来源动作改空；状态端点必须始终构造 |
| `自我动作实现模块.ixx:10378` 任务状态提交 | 被动状态动作 | 传入提交方法头 + `被动动作` | 被动来源动作改空；保留任务虚拟存在状态端点 |
| `自我动作实现模块.ixx:11026` 方法动作状态入账 | 主动动作 | 无变化时状态端点为空 | 必须改成可记录运行事实状态端点或不创建完整动作动态 |
| `自我动作实现模块.ixx:12180` OR 组令牌动作动态 | 主动 / 结算辅助动作 | 未传初始 / 结果状态 | 必须补令牌状态端点 |
| `自我动作实现模块.ixx:12249` 叶子任务价值结算 | 主动结算动作 | 状态端点已具备 | 迁入统一入口，追加低层动态保持 |
| `自我动作实现.扫描.ixx:1769,2056` 扫描动作动态 | 主动动作 | 状态端点已具备 | 迁入统一入口 |
| `自我动作实现.扫描.ixx:2160` 扫描内部治理记录 | 被动状态动作 | 传入方法头 + `被动动作` | 被动来源动作改空，保留状态端点和来源动态 |
| `自我动作实现.外设模块.ixx:14455` 观察内部治理记录 | 被动状态动作 | 传入方法头 + `被动动作`；仍用成功 / 错误码表达结果 | 被动来源动作改空；结果写入状态 / 输出场景 |
| `自我动作实现.外设模块.ixx:19365` 相机 / 外设动作动态 | 主动动作 | 动作未达成时状态端点为空 | 失败必须落为运行存在或输出场景特征事实，不能空端点创建 |
| `自我动作实现.服务模块.ixx:3654` 服务值结算 D2 | 主动结算动作 | 状态端点已具备 | 迁入统一入口 |
| `自我类.cpp:520` 私有本能动作动态 | 旧旁路 | 先创建普通动态再绑定来源方法，手工写成功字段 | 迁入统一入口或降为状态迁移动态后补动作致变动态 |
| `方法虚拟存在服务类.cpp:350` / `自我线程模块.impl.cpp:13197` | 旧旁路治理动态 | 直接写 `来源动作名 / 来源执行成功` | 迁入统一入口或明确降为普通状态迁移 / 治理过程证据 |
| SQL / 控制面板 / 二次特征聚合 | 投影和显示 | 继续显示或拷贝 `来源执行成功 / 来源错误码` | 控制面板先不显示；聚合不再复制这两个字段；SQL 字段暂留兼容 |
