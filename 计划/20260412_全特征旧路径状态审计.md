# 2026-04-12 全特征旧路径状态审计

本文是 [20260412_全特征覆盖账本.md](</D:/鱼巢/计划/20260412_全特征覆盖账本.md>) 的第一轮代码侧核账结果，用来回答两件事：

1. 账本里标记的 `旧路径状态` 是否和当前代码事实一致。
2. 接下来应该先关哪批旧路径，才能让 `P0` 和 `六原语 P5` 一起推进。

## 1. 审计范围

- 范围覆盖账本内全部 `49` 个特征。
- 本轮只审三类旧路径：
  - `旧路径 helper`
  - `旧读取/旧摘要口径`
  - `旧展示/旧业务 setter`
- 结论口径与 [20260412_全特征改值本能函数化总计划.md](</D:/鱼巢/计划/20260412_全特征改值本能函数化总计划.md>) 的 `P0 统一口径与覆盖账本` 保持一致。

## 2. 全局分布

| 旧路径状态 | 数量 | 当前含义 |
| --- | ---: | --- |
| `已禁旧路径` | 2 | 旧 helper 已物理移除，新主路径已切到正式动作入口 |
| `受控兼容` | 36 | 主链已切到新口径，但旧壳或注册期 helper 仍保留为兼容层 |
| `仍有模块内 helper` | 0 | 当前阻断级 helper 已全部退出主路径 |
| `仍有业务 setter` | 4 | 仍通过业务 setter 进入状态写回 |
| `仍有直写` | 2 | 仍从业务/自我层直接改当前值 |
| `仅初始化` | 5 | 仅有默认值，占位未正式化 |

当前最重要的判断不是“受控兼容很多”，而是：

- `阻断级` 残留主要集中在 `摘要口径不统一` 与 `仍未正式动作化的 helper / setter / 直写`。
- `非阻断级` 残留主要是 `G7` 占位项，以及少量内部技术串仍沿用旧字段名。

## 3. 阻断级残留

### 3.1 `G3-G5` 的两处人读摘要 `原语=` 混合表达已完成首轮清理

本轮已完成的两处收口点：

- [任务管理任务模块.cpp](</D:/鱼巢/任务管理任务模块.cpp:2395>)  
  `桥接补层裁决` 已改成 `本轮原语命中=... | 正式已具备=... | 派生摘要=...`
- [任务管理任务模块.cpp](</D:/鱼巢/任务管理任务模块.cpp:10054>)  
  `缺口摘要` 已改成 `正式已具备=... | 派生摘要=...`

这两处现在已经开始和当前成型的新口径对齐。当前新样板是：

- [鱼巢.cpp](</D:/鱼巢/鱼巢.cpp:1123>)  
  `本轮原语命中=... | 正式已具备=...`
- [学习任务模块.cpp](</D:/鱼巢/学习任务模块.cpp:597>)  
  学习摘要也已跟随新口径

结论：

- `六原语 P5` 里最突出的两处 `原语=` 人读摘要残留已完成首轮清理。
- 当前还需要继续收的是“哪些摘要用 `派生摘要` 包裹、哪些摘要直接展开 `派生能力`”，但它已不再是 `原语=` 级别的混合阻断。

### 3.2 `G1-G2` 仍保留业务 setter/直写入口

账本里这批特征目前仍未进入正式动作化收口：

- `仍有直写`
  - `安全值`
  - `服务值`
- `仍有业务 setter`
  - `物理安全`
  - `当前主需求`
  - `当前主任务`
  - `当前主方法`

代码事实也一致：

- [自我类.cpp](</D:/鱼巢/自我类.cpp:562>) `设置物理安全值(...)`
- [自我类.cpp](</D:/鱼巢/自我类.cpp:772>) `设置当前主需求(...)`
- [自我类.cpp](</D:/鱼巢/自我类.cpp:784>) `设置当前主任务(...)`
- [自我类.cpp](</D:/鱼巢/自我类.cpp:796>) `设置当前主方法(...)`

这些入口本身已经会留下世界树状态证据，但它们仍属于 `业务 setter / 直接写当前值` 路线，而不是统一的正式本能函数入口。

结论：

- 这组不是当前最先要动的阻断点，但它决定了 `G1-G2` 何时能从“已能改值”提升到“正式动作化”。
- 在 `六原语 P5` 收口后，这组应该成为下一批正式动作化对象。

### 3.3 `G5-G6` 的 helper 主路径已完成收口

本轮已完成这 3 项的主路径纠偏：

- `方法类型`
- `待学习方法数量`
- `尝试学习状态`

对应代码位置：

- [本能方法类.cpp](</D:/鱼巢/本能方法类.cpp:129>) `私有_写入方法类型特征(...)`
- [任务管理任务模块.cpp](</D:/鱼巢/任务管理任务模块.cpp:7940>) `方法类型` 运行态写回已改走正式执行壳
- [自我类.cpp](</D:/鱼巢/自我类.cpp:1160>) `更新待学习方法数量(...)`
- [自我类.cpp](</D:/鱼巢/自我类.cpp:1194>) `更新尝试学习状态(...)`

结论：

- [任务管理任务模块.cpp](</D:/鱼巢/任务管理任务模块.cpp>) 里的 `私有_写入方法类型特征 / 私有_写回待学习方法数量 / 私有_写回尝试学习状态` 已退出主路径，其中后两项已物理删除。
- `待学习方法数量 / 尝试学习状态` 已可归档为 `已禁旧路径`。
- `方法类型` 的任务管理运行态已切到正式执行壳，但 [本能方法类.cpp](</D:/鱼巢/本能方法类.cpp:129>) 仍保留注册期 helper，因此更准确的状态是 `受控兼容`，而不是完全退场。

## 4. 非阻断级残留

### 4.1 `G7` 仍全部处于占位初始化态

当前仍只有默认值、尚未进入正式动作化的 5 项：

- `情绪ID`
- `情绪强度`
- `外显情绪ID`
- `外显情绪强度`
- `外设可用性`

这批没有发现新的错误宿主或错误摘要问题，仍按账本结论维持：

- 暂不阻断主闭环收口
- 在 `G1-G6` 收紧之前，不提前泛化

### 4.2 少量内部技术串仍沿用 `|原语=`，但目前不算人读摘要阻断

可以确认一处内部技术串：

- [自我线程类.cpp](</D:/鱼巢/自我线程类.cpp:368>)  
  治理邮箱事件幂等键仍使用 `|原语=`

这一处当前更像内部键串，而不是用户摘要或验收口径，因此本轮先归为 `提示级`，不和 `3.1` 那两处人读摘要混算。

## 5. 本轮审计结论

这轮可以正式落三个判断：

1. `P0` 首轮旧路径状态审计已经形成，账本不是只停留在纸面。
2. `G3-G5` 的两处 `原语=` 混合摘要已经收掉，`六原语 P5` 现在可以把主注意力转向剩余 helper 与读口径一致性。
3. `G5-G6` 的 helper 主路径已经收口，下一批真正的阻断点转成了 `G1-G2` 的 setter/直写，以及少量仍待统一的摘要层与内部技术串。

## 6. 推荐执行顺序

1. 先做 `安全值 / 服务值 / 物理安全 / 当前主需求 / 当前主任务 / 当前主方法` 的正式动作化收口。
2. 再继续收 `六原语 P5` 里剩余的 `派生摘要 / 派生能力` 层级统一与少量内部技术串。
3. 最后再决定 [本能方法类.cpp](</D:/鱼巢/本能方法类.cpp:129>) 这类注册期兼容 helper 是否要继续正式化。

一句话收口：

`现在不是缺方向，而是已经能明确按“先收 setter/直写，再压摘要兼容层，最后清注册期 helper”的顺序推进。`
