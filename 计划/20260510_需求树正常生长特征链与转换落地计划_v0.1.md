# 需求树正常生长特征链与转换落地计划 v0.1

日期：2026-05-10

## 0. 状态修订

```text
当前状态：未完成。

此前 D0-D7 的“完成”只表示：
1. 需求树生长相关诊断面已建立；
2. 原子目标入树闸门已建立；
3. 结构角色刷新、活动阻塞需求统计和摘要展示已建立；
4. 任务反馈结算观测字段已建立。

这些不等于需求树已经能真实自动生长。

真实未完成项：
1. 自我同一目标特征的特征值差距自动生成 / 刷新子需求；
2. 任务缺口 / 方法缺口稳定转为子需求；
3. 同目标需求复用 / 合并的可控剧本；
4. 父子权重随活跃子需求变化自动刷新；
5. 执行需求稳定任务化；
6. 任务反馈后需求退出活动集；
7. 新一轮主需求选择受到需求树变化影响。
```

承接：

```text
计划/20260510_自我内部循环最小生命闭环阶段总计划_v0.1.md
计划/20260510_自我内部循环P2b需求树生成成立计划_v0.1.md
计划/20260510_自我内部循环P3P4任务化与任务语义验收计划_v0.1.md
计划/20260510_因果补完学习探索与初步成熟计划_v0.1.md
规范/需求树生长本能转换规范20260510.md
详细设计/自我内部循环最小生命闭环差距分析与实现方法.md
```

## 1. 定位

需求树正常生长不是需求节点字段单点问题，而是跨自我、状态、需求、任务、方法、因果和结算的特征链问题。

本计划是 P2b 的扩展落地计划，用于把“需求树生成成立”从根需求压力选择推进到完整的需求层闭合。

需求树正常生长链路定义为：

```text
自我核心状态
→ 根需求激活
→ 同一目标特征的特征值差距识别
→ 子需求生成
→ 子需求去重 / 合并
→ 子需求权重刷新
→ 管理需求 / 执行需求分化
→ 执行需求任务化
→ 任务执行 / 阻塞 / 派生
→ 任务价值反馈
→ 需求结算
→ 父需求汇总
→ 下一轮主需求选择
```

更具体的特征链：

```text
自我状态特征
→ 需求目标特征
→ 需求结构特征
→ 权重调度特征
→ 任务承接特征
→ 方法 / 执行反馈特征
→ 结算回写特征
→ 新一轮需求生长
```

边界：

```text
特征值差距只能表达同一特征类型上的当前值与目标值不满足。
它不能表达不同特征类型之间的关系。

不同特征类型之间的关系、影响和因果，应由状态动态、二次特征 / 关系特征、因果实例 / 因果模板、方法结果能力承载。
```

## 2. 与其他计划的先后顺序

### 2.1 已完成前置

本计划依赖以下已完成工作，不重复实现：

```text
1. 存在概念和信息入口类型体系已完成。
   需求目标主体、状态主体、任务虚拟存在和方法虚拟存在都必须继续沿用当前存在 / 基础信息结构，不新增抽象存在或存在形态。

2. 任务管理界面线程边界已完成。
   需求任务化只创建 / 复用任务信息节点；任务虚拟存在由任务管理界面线程绑定。

3. 需求部分任务初始化衔接已完成。
   需求节点通过对应任务指向任务信息节点，不直接指向任务虚拟存在。

4. 内部循环 P0-P8 第一版已完成。
   当前已有压力评估、任务化、任务语义、回写、结算和多轮稳定的动态闭环第一版。

5. 自检线程边界已完成。
   自检线程只报送缺失，不生成修复需求。
```

### 2.2 当前应优先执行

本计划应排在以下事项之前：

```text
1. 自我苏醒 A4 的具体休眠期修复动作映射表。
   原因：休眠期修复动作不能凭空决定，必须先知道需求树如何承接正常缺口和异常缺口。

2. 因果补完学习探索计划的正式实现。
   原因：学习需求和外界探索需求必须从需求树正常生长和任务 / 方法反馈缺口中稳定产生。

3. 自检报告需求化试点。
   原因：自检报告是否能变成需求，必须遵守本计划的原子目标、父需求归属和去重规则。
```

### 2.3 可并行但不能抢权

以下计划可以继续保留，但不能绕过本计划的需求入树规则：

```text
1. 标准原子状态词典与自检报告候选承接计划。
   只提供状态词典和报告承接，不直接生成需求。

2. 自我苏醒阶段验收与稳定治理计划。
   可继续做诊断和报告，但不抢先定义休眠修复动作。

3. 因果补完学习探索与初步成熟计划。
   可继续做规范，不进入学习 / 探索需求自动生成代码。
```

## 3. 非目标

本计划不做：

```text
1. 不重写需求类。
2. 不改变需求节点仍是目标记录 / 动因节点的定位。
3. 不让需求直接指向任务虚拟存在。
4. 不让自检线程生成修复需求。
5. 不实现休眠期修复动作。
6. 不实现外界探索需求。
7. 不新增抽象存在主信息类、存在形态枚举或特征当前值信息。
8. 不把任务筹办、任务执行一步、自我线程主循环或任务管理线程调度重新注册成本能方法。
```

## 4. 最小特征链口径

本计划固定八类所有者。

| 所有者 | 必需特征 | 用途 |
| --- | --- | --- |
| 自我存在 | 安全值、服务值、目标安全 / 服务、待机状态、时序步长 | 产生根需求压力 |
| 状态节点 | 状态主体、状态特征、状态值、对应信息节点、是否变化 | 形成需求目标 |
| 需求节点 | 主体、场景、当前状态、目标状态、满足关系、目标特征缓存、权重、任务、结算、派生归因 | 需求树生长主体 |
| 需求树结构 | 父子关系、结构角色、阻塞语义、有效截止 | 组织管理 / 执行需求 |
| 任务信息节点 | 对应需求、目标状态、优先级、承接权重、已折算值 | 承接执行需求 |
| 方法 / 本能动作 | 方法结果、阻塞原因、能力缺口、派生来源 | 触发派生需求 |
| 因果实例 | 条件、动作、结果、来源任务 / 方法、置信度、验证状态 | 支持学习需求和因果补完 |
| 场景 / 内部世界 | 状态、动态、关系、因果索引 | 提供上下文索引 |

## 4.1 转换执行层口径

需求树正常生长中的“转换使用的方法”统一按以下三层表达：

```text
本能转换函数：
    C++ 内置确定性转换函数，保证需求树最小生长。

本能转换方法：
    本能转换函数的任务 / 方法系统包装，用于调度、记录、复盘和因果沉淀。

学习转换方法：
    后续学习、因果补完或探索阶段形成的增强方法，不作为需求树最小生长前提。
```

执行顺序：

```text
先核对 / 补齐本能转换函数；
再决定哪些函数需要包装成本能转换方法；
最后再进入学习转换方法和外界探索方法。
```

禁止反向依赖：

```text
不得让需求树最小生长依赖方法系统先学会普通方法。
不得恢复任务筹办 / 任务执行一步本能方法旧口径。
```

## 5. 分阶段计划

### D0：特征链详细设计落档

目标：

```text
把需求树正常生长的特征所有者、转换来源、转换目标和对应函数固定成详细设计。
```

动作：

```text
1. 新增详细设计文档：
   详细设计/需求树正常生长特征链与转换关系详细设计.md

2. 写入八类特征所有者总表。
3. 写入十类转换关系：
   自我状态 -> 根需求权重
   特征值差距 -> 需求节点
   状态节点 -> 需求目标视图
   父需求 -> 子需求权重
   重复目标 -> 合并需求
   任务 / 方法阻塞 -> 派生需求
   执行需求 -> 任务信息节点
   需求权重 -> 任务承接权重
   任务反馈 -> 自我值 + 需求结算
   结算 / 满足 -> 需求失效或继续生长

4. 标注每条转换的现有代码入口、缺口和错误处理口径。
```

验收：

```text
详细设计能回答：
每个特征由谁拥有；
从哪里来；
转换给谁；
由哪个函数完成；
何时算业务缺口；
何时算非正常逻辑错误。
```

### D1：现有代码入口核对

目标：

```text
确认当前代码是否已经具备每条转换入口，避免重复造函数。
```

重点核对：

```text
需求类：
    执行更新指令
    添加子需求节点_已加锁
    从状态补齐目标主体和特征缓存_已加锁
    查找直接子需求_按目标重叠_已加锁
    合并需求主信息_已加锁
    刷新子需求权重
    刷新需求结构角色
    绑定对应任务

自我线程：
    从需求树构建需求列表
    选择当前主需求候选
    收到派生需求消息时完善需求树
    确保派生需求节点
    按需求创建任务头
    处理任务价值反馈
    刷新父级需求已结算值

任务模块：
    任务筹办推进回执
    派生需求消息构造
    任务工作结果 / 结果状态消息

自我类：
    根需求压力评估
    内部循环诊断
    核心状态回写快照
    需求结算闭环快照
```

输出：

```text
每条转换标注：
已完成 / 需补诊断 / 需补代码 / 待确认 / 禁止实现。
同时标注：
已有本能转换函数 / 需新增本能转换函数 / 建议注册本能转换方法 / 暂不注册方法。
```

### D1b：本能转换函数与本能转换方法分层

目标：

```text
把“转换使用的方法”从泛称收口为本能转换函数 / 本能转换方法。
```

动作：

```text
1. 建立转换映射表：
   转换名称
   当前实现函数
   所属模块
   是否最小闭环必需
   是否已有本能转换函数
   是否建议注册本能转换方法
   是否属于学习转换方法

2. 首批必须确认的本能转换函数：
   自我状态 -> 根需求压力
   根压力 -> 当前主需求
   特征值差距 -> 需求节点
   状态节点 -> 目标特征缓存
   父需求 -> 子需求权重
   重复目标 -> 合并需求
   有子 / 无子 -> 管理 / 执行需求
   执行需求 -> 任务信息节点
   任务反馈 -> 自我安全 / 服务值
   任务反馈 -> 需求结算账本
   子需求结算 -> 父需求结算
   方法阻塞 -> 派生需求

3. 对需要注册成本能方法的转换，补充：
   方法能力签名
   结果能力声明
   输入条件面
   运行账记录字段
   是否允许任务系统直接调用

4. 对暂不注册成本能方法的转换，写明原因：
   仅内部维护
   调度流程不应进入方法系统
   输入输出尚未能稳定特征化
   会引入循环依赖
```

验收：

```text
每个需求树生长转换都能回答：
它是不是本能转换函数；
是否需要包装成本能方法；
若不包装，为什么；
若包装，能力签名是什么。
```

边界：

```text
任务筹办、任务执行、自我线程主循环、任务管理界面线程承接、工作线程调度不注册为本能方法。
```

### D2：需求目标视图与原子目标入树闸门

目标：

```text
需求入树必须有明确原子目标，不允许泛化缺口静默入树。
```

动作：

```text
1. 复核 `结构_需求目标视图项` 是否覆盖：
   目标主体
   目标特征类型
   当前状态
   目标状态
   方向掩码
   是否必需

2. 复核派生需求消息入树闸门：
   缺父需求 -> 报错并拒绝
   父需求解析为结构根 -> 报错并拒绝
   缺目标主体 / 当前状态 / 目标状态 / 满足关系 -> 报错或转报告，不能入树

3. 自检报告继续保持报告型，不走需求化。
```

验收：

```text
无状态泛化缺口不能进入需求树。
父需求结构根不能作为合法业务父需求。
同目标缺口能走目标重叠去重。
```

### D3：结构角色与活动子需求权重回归

目标：

```text
管理需求 / 执行需求角色、活动子需求、权重平分语义稳定。
```

动作：

```text
1. 确认所有新增 / 删除 / 合并 / 满足需求后刷新结构角色。
2. 活动子需求仅统计：
   未失效
   未满足
   未取消
   阻塞父任务执行

3. 父需求权重只分给活动阻塞子需求。
4. 非阻塞观察需求不阻塞父任务和父需求继续推进。
```

验收：

```text
有子需求 -> 管理需求。
无子需求 -> 执行需求。
历史已完成子需求不阻塞父需求。
非阻塞子需求不参与阻塞权重平分。
```

### D4：需求生成入口登记与诊断

目标：

```text
把需求生成原因显式分类，控制面板只展示结构化类别，不生成说明性摘要。
```

入口类别：

```text
自我状态压力
   特征值差距
任务筹办缺口
任务执行缺失
方法结构缺口
方法运行账 / 稳定性缺口
任务结果不符合
因果缺口
自检报告待决策
```

动作：

```text
1. 增加结构化“最近需求生成事件”或复用现有诊断快照。
2. 字段只放枚举、主键、计数、状态码，不放说明性文字。
3. 控制面板显示事件类型和主键。
4. 自检报告型候选只进入待处理队列，不作为需求生成入口。
```

验收：

```text
能区分需求来自状态压力、任务缺口、方法缺口还是反馈结果。
重复入树能显示为“复用 / 合并”，不是新建。
```

### D5：去重合并与任务承接一致性回归

目标：

```text
需求树生长后能稳定承接任务，不出现需求风暴或任务风暴。
```

动作：

```text
1. 同父需求下按目标主体、目标特征类型、方向掩码判断重叠。
2. 已有活动同目标需求时只合并 / 刷新。
3. 需求.对应任务 继续指向任务信息节点。
4. 任务列表记录历史 / 候选 / 重建痕迹。
5. 同一需求未终结任务数量超过阈值时记录非正常风险。
```

验收：

```text
连续两轮同一缺口不重复生成同目标需求。
连续两轮同一执行需求不重复创建未结束任务。
需求不直接指向任务虚拟存在。
```

### D6：反馈结算到下一轮生长验证

目标：

```text
证明任务反馈能改变自我值、结算需求，并影响下一轮需求树生长。
```

动作：

```text
1. 复用 P5-P6 的回写和结算快照。
2. 在需求树生长诊断中加入：
   本轮结算来源需求
   本轮安全 / 服务净变化
   结算后安全 / 服务压力
   下一轮主需求是否重判

3. 需求结算达到上限或目标满足时，需求退出活动子需求权重分配。
```

验收：

```text
任务成功 -> 来源需求累计结算变化。
结算后压力变化。
下一轮主需求选择受压力变化影响。
已满足 / 失效需求不继续阻塞父需求。
```

### D7：控制面板与回归命令

目标：

```text
需求树生长能够被独立观察和回归。
```

动作：

```text
1. 新增或扩展命令：
   --demand-tree-growth-summary

2. 展示结构化字段：
   需求树生成成立
   最近需求生成事件类型
   最近需求生成父需求
   最近目标主体
   最近目标特征
   最近入树结果：新建 / 复用 / 合并 / 拒绝
   管理需求数
   执行需求数
   活动阻塞子需求数
   最近结算后压力

3. 不展示说明性摘要，不输出推测性文字。
```

验收：

```text
命令 0 码返回。
同一剧本能稳定复现新建、复用 / 合并、结算退出活动集三类结果。
```

## 6. 错误处理口径

以下属于非正常逻辑错误，应弹错 / 报错并记录日志，不允许兜底：

```text
1. 显式请求创建需求但父需求为空。
2. 派生需求父需求解析为需求树结构根。
3. 执行需求任务化后仍无法绑定对应任务信息节点。
4. 任务建立流程完成后仍缺任务虚拟存在。
5. 实例入口缺主体或场景却试图落树。
6. 需求同时指向任务信息节点和任务虚拟存在。
```

以下属于业务缺口，可走状况回执、派生需求或学习需求：

```text
1. 方法缺动作骨架。
2. 方法缺条件结果对。
3. 参数不可配且筹办 / 执行选择由学习承接。
4. 方法运行账不足。
5. 方法结果不稳定。
6. 因果链缺少关键节点。
```

以下暂不需求化：

```text
1. 自检报告型候选。
2. 休眠期修复动作。
3. 外界探索需求。
```

## 7. 里程碑

| 里程碑 | 范围 | 完成结果 |
| --- | --- | --- |
| G1 | D0-D1b | 特征链详细设计、代码入口核对和本能转换分层完成 |
| G2 | D2-D3 | 原子目标入树闸门、结构角色、活动子需求和权重语义稳定 |
| G3 | D4-D5 | 需求生成入口、去重合并和任务承接一致性可诊断 |
| G4 | D6 | 反馈结算能反向影响下一轮需求生长 |
| G5 | D7 | 控制面板和回归命令可独立观察需求树生长 |

## 8. 验收剧本

### 剧本一：服务压力生成需求

```text
1. 降低服务值。
2. 自我线程运行一轮。
3. 服务根需求活跃或生成服务修复执行需求。
4. 再运行一轮，同目标需求复用 / 合并，不重复新建。
```

### 剧本二：方法结构缺口生成子需求

```text
1. 构造一个目标方法头。
2. 筹办发现缺动作骨架或条件结果对。
3. 生成以方法虚拟存在 / 方法头为目标主体的结构缺口需求。
4. 子需求不复制父需求目标主体和目标特征。
```

### 剧本三：任务反馈结算后退出活动集

```text
1. 执行需求任务化。
2. 任务成功产生服务 / 安全价值反馈。
3. 来源需求累计结算变化。
4. 需求满足或失效后不再参与活动阻塞子需求权重平分。
5. 下一轮主需求重判。
```

## 9. 当前状态

```text
D0-D1b 已完成。
已新增详细设计：
详细设计/需求树正常生长特征链与转换关系详细设计.md

已完成内容：
1. 八类特征所有者总表已落档。
2. 十二类转换关系已落档。
3. 每条转换的现有代码入口已核对。
4. 每条转换已标注为已有本能转换函数 / 候选本能转换方法 / 暂不注册方法。
5. 已记录待完成项：
   运行期显式创建需求父需求闸门；
   活动子需求取消态口径；
   最近需求生成事件结构化记录；
   需求树生长控制面板命令；
   反馈结算影响下一轮主需求选择回归。

D2-D3 第一轮已完成。

已完成内容：
1. 自我线程治理消息中的新增需求补入原子目标闸门：
   缺当前状态、目标状态或满足关系时，报错并拒绝入树。
2. 初始化根需求仍不走该消息入口，不受运行期闸门影响。
3. 结构角色刷新和活动阻塞子需求权重分配已复核：
   有子需求为管理需求；
   无子需求为执行需求；
   权重只分给未失效且阻塞父任务执行的直接子需求。
4. Debug x64 非增量链接构建通过。

待完成项：
需求主信息当前没有独立取消态字段，
“未取消”暂不能作为活动子需求判定条件；
后续如需要取消态，应另起需求生命周期字段计划。

D4-D5 第一轮已完成。

已完成内容：
1. 控制面板快照新增需求树生长结构化字段：
   需求树生成成立、管理需求数、执行需求数、角色未定义数、
   活动阻塞需求数、非阻塞需求数、已截止需求数、派生归因需求数、
   当前主需求主键、父主键、目标主体主键、目标特征主键、
   当前主需求是否有任务、重复任务风险、入树结果、最近事件类型。
2. 新增命令：
   `--demand-tree-growth-summary`
   兼容别名：
   `--demand-growth-summary`
   `--need-tree-growth-summary`
3. 自我根需求初始化后刷新结构角色，
   修复根需求直接创建绕过 `需求类::执行更新指令` 导致的角色未定义问题。
4. `--demand-tree-growth-summary` 已验证 0 码返回；
   需求树角色未定义数为 0；
   当前主需求已承接任务；
   重复任务风险为否。
5. `--self-loop-summary` 已验证 0 码返回，
   内部循环完成度仍为连续多轮稳定。

已验证：
Debug x64 非增量链接构建通过。

D6-D7 第一轮已完成。

已完成内容：
1. `--demand-tree-growth-summary` 已补入最近结算来源字段：
   最近结算是否有效推进、来源任务主键、来源需求主键、
   结算后安全压力、结算后服务压力、
   来源需求累计安全结算、来源需求累计服务结算。
2. 需求树生长摘要命令已作为固定回归入口接入命令行解析和主程序自我线程启动路径。
3. 控制面板 HTML 的需求树页摘要已展示需求生长成立、管理需求、执行需求和活动阻塞需求。
4. Debug x64 非增量链接构建通过。
5. `--demand-tree-growth-summary` 0 码返回。
6. `--self-loop-summary` 0 码返回。

本轮验证观测：
```text
需求树生成成立=是
管理需求=1
执行需求=2
角色未定义=0
当前主需求有任务=是
重复任务风险=否
最近结算有效推进=是
最近结算来源任务=4
最近结算来源需求=3
```

待完成项：
```text
三类完整剧本仍需后续专项自动化：
1. 新建需求路径；
2. 同目标复用 / 合并路径；
3. 结算后需求退出活动集路径。

当前第一轮已提供固定命令和结构化字段，
后续可在具备可控剧本驱动后补自动断言。
```
```
