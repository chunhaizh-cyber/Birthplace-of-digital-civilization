# 20260529_R3外设相机能力接入自我内外交互桥实施计划 v0.1

## 0. 归属

```text
父计划：计划/20260526_基础观察事实与基础风险判断分层实施计划_v0.1.md
承接计划：计划/20260529_R2本能方法_安全评估当前场景安全性实施计划_v0.1.md
相机线依据：
    计划/待完成项/20260527_双目相机观察队列承接代码纠偏与实现计划_v0.1.md
    计划/待完成项/20260527_双目相机观察像素簇接入实现方案_v0.1.md
    计划/待完成项/20260527_外设自主观察线程稳定采样实施计划_v0.1.md
当前状态：C0-C4 已完成首轮真实链路验证；C4.5 观察模式触发纠偏、C4.6-1 短期观察材料库、C5-3a 候选验证材料解引用、C5-3b 只输出确认方案、C5-3c 提交确认观察存在事实、C4.7 下一帧已确认轮廓分区、C4.6-2 已确认存在内部世界递归材料、C6/C7 安全性评估与结果满足验证均已完成当前轮真实验证；R4 已补 `自我_读取自我关键特征变化` 和 `安全_构造场景影响因果候选` 两个安全对象作用方法；R5g 已把安全因果口径修正为被动获取：未收到负向因果证据时默认良好，提交 `I64MAX` 场景评估安全值，持续观察、负向处置和降级保护链不触发；R5h 已补安全评估输出结果场景中的可比较状态节点，使 `风险状态明确状态`、`当前场景安全度明确状态`、`当前场景最低安全度存在明确状态`、`当前场景各存在安全评估明确状态` 等可被任务执行回执读取和比对。本计划第一条外设相机接入安全评估桥、第二段安全前置桥和因果缺省默认良好评估已闭合；场景安全度产出后才会随着共处时间、稳定性和被动获得的归因证据进一步影响自我风险安全值，不在安全评估方法内直接结算根安全值。当前自然链复验证据：机制侧已用现有 `因果主信息类` 写入来源类型为“遗传”的冷启动因果模板，根必要条件子需求经提交入口回写来源因果键；`--demand-tree-growth-summary` 摘要显示 `派生来源因果=12`，`--auto-observe-stable` 继续证明外设桥与安全评估默认良好链未回退。
追加纠偏证据：学习缺口这类以目标比较 / 二次特征为状态主体的派生需求，筹办和控制面板目标宿主判定已改为优先读取目标状态主体；最新 `--demand-tree-growth-summary` 摘要显示 `目标绑定: 完整=46 | 异常=0`，`--auto-observe-stable` 继续退出码 0。
```

本计划把 `安全_评估当前场景安全性` 首轮实现后暴露出的方向正式收束为：

```text
外设相机能力接入
= 自我内外交互桥的第一条正式感知桥。
```

它不是让相机直接理解世界，也不是让安全根需求直接消费相机流，而是把真实外部观察材料经过任务管理承接和提交入口裁决，转成自我内部可读取的场景事实。

## 1. 规范依据

```text
001 方法动作动态硬规则规范20260512.md
002 自我线程与任务管理线程权责边界规范20260512.md
003003 自我内部循环实现规范20260512.md
020003 需求树生长机制规范20260512.md
020005 需求临时因果视图展开规范20260526.md
030003001 任务筹办按方法能力查找到就绪因果链规范20260513.md
040005 外设观察特征与自我场景认知本能方法分层规范20260525.md
040007 双目相机外设独占观察线程规范20260527.md
040010 外设独立控制线程与消息承接边界规范20260527.md
050 自我所在场景认知工作区规范20260525.md
050003 基础观察事实可用与基础风险判断分层规范20260526.md
060004 安全服务值结算规范20260518.md
```

若本计划与上述规范冲突，按规范修订本计划，不按本计划改代码。

## 2. 桥梁定义

目标链路：

```text
外设线程_D455深度相机
-> 外设观察报告队列 / 最新观察缓存 / 稳定特征缓存
-> 任务管理按正式需求创建外设等待项 / 订阅项
-> 任务管理消费逐簇识别报告 / 扫描变化报告 / 跟踪报告
-> 生成任务回执 / 派生需求消息 / 观察事实承接请求
-> 提交入口裁决
-> 自我场景观察帧 / 观察像素簇 / 存在候选 / 已确认观察存在
-> 当前场景存在集合明确
-> 安全_评估当前场景安全性
-> 当前场景安全评估结果场景
```

这条桥只允许传递事实、证据、候选和评估结果视图，不允许直接传递安全值结算。

一句话：

```text
相机负责给外部事实材料；
任务管理负责领走和分拣；
自我负责裁决和入内部事实；
安全评估负责解释事实；
价值结算负责最后改账。
```

## 3. 当前已具备证据

```text
1. D455 外设线程已能真实采样并生成三种报告链路：
   陌生环境逐簇识别报告、已识别场景扫描变化报告、目标跟踪报告。

2. 外设观察报告队列、等待项 / 订阅项、按报告ID回查、任务管理承接 helper 已有首轮实现。

3. 真实 D455 入口已验证逐簇识别 / 扫描变化 / 目标跟踪三类等待项命中、报告回查和任务管理承接。

4. 掩码 / 彩色轮廓局部图 / 深度轮廓局部图已完成句柄级接入。

5. `--self-scene-html` 最新真实运行中，自我所在场景已有 `已确认观察存在=1`，并能使 `安全_评估当前场景安全性` 读取到 `场景存在集合明确状态=1`。

6. `安全_评估当前场景安全性` 已能在场景存在集合明确但关键特征变化和因果候选不足时输出证据不足，而不是误判安全或改写安全值。

7. 2026-05-29 真实普通运行已验证正式需求链路：
   `当前观察特征帧取得状态 == 已取得` 派生需求由任务管理自动创建 D455 外设观察等待项，
   D455 逐簇识别报告入队后被任务管理按等待项领走，
   任务管理生成观察事实承接请求并上行，
   自我线程按报告 ID 回查外设报告并完成观察事实承接。

8. 同一运行中连续两次自我承接成功：
   报告ID=3、报告ID=7 均出现 `自我线程/外设观察事实承接完成 | 成功=是`；
   日志同时保留 `直接写需求树=0`、`直接写世界树=0`、`禁止直接写世界真值=是` 边界证据。
```

这说明：

```text
相机事实进入自我场景的底座已经出现；
正式需求到外设等待项、任务管理承接请求和自我事实承接的第一孔已经打通；
但存在候选验证、当前场景存在集合满足回写、安全评估结果提交和安全因果候选仍需后续切片。
```

## 4. 仍缺的桥梁段

```text
1. 观察像素簇 -> 存在候选 -> 已确认观察存在 的需求驱动验证链。
2. 当前场景存在集合明确状态与具体观察事实 / 来源动态 / 来源报告之间的可追溯满足链。
3. 安全评估读取相机场景事实后，结果满足来源需求的提交回写链。
4. 关键特征变化观察和安全因果候选构造链。
5. 正式抽象特征生产者替代当前最小文本映射后的复验。
```

## 5. 实施切片

### C0 代码状态冻结与预测记录

执行状态：已完成。

目标：

```text
核对当前外设线程、外设报告队列、任务管理承接、自我承接和安全评估代码真实状态。
写明本轮预测：相机能力接入桥应能把正式需求转成相机等待项，但不得让相机线程直接写世界树真值。
```

必查：

```text
外设线程_D455深度相机.ixx
外设观察报告队列.ixx
任务模块.管理工作线程.ixx
任务模块.工作线程消息协议.ixx
任务模块.管理线程协议.ixx
自我线程模块.impl.cpp
自我动作实现.外设模块.ixx
鱼巢.cpp
```

### C1 正式需求到相机能力的目标映射

执行状态：已完成首轮。`当前观察特征帧取得状态 == 已取得` 已能从正式派生需求进入外设等待项；观察像素簇集合、存在候选集合、当前场景存在集合明确仍待继续补正式抽象特征生产者链和满足回写链。

目标：

```text
把这些需求目标稳定映射到相机能力链：
当前观察特征帧取得状态 == 已取得
当前观察帧.观察像素簇集合状态 == 已生成
当前观察帧.存在候选集合状态 == 已生成
当前场景存在集合明确状态 == 已明确
```

要求：

```text
任务筹办仍只按结果能力、方法条件、条件结果对和临时因果视图找路；
不得按相机 / 安全 / 观察领域词在筹办器里硬编码固定前置。
```

输出：

```text
候选方法或承接方法能被正式目标召回；
缺外设报告时生成外设等待项 / 订阅项；
缺承接方法时生成方法就绪或执行入口缺口。
```

### C2 外设等待项 / 订阅项正式化

执行状态：已完成首轮。等待项已携带来源需求、目标外设、观察运行模式、期望报告类型、最大年龄、最小稳定性、幂等键和质量要求摘要；幂等键用于避免重复创建。

目标：

```text
任务管理工作线程根据正式需求上下文创建或复用：
陌生环境逐簇识别等待项
已识别场景扫描变化等待项
目标跟踪观察等待项
```

要求：

```text
等待项必须携带来源需求、目标外设、观察运行模式、期望报告类型、最大允许报告年龄、最小稳定性要求和幂等键。
不得把等待项写成正式需求；等待项只是任务管理侧消费外设报告的桥接壳。
```

### C3 任务管理消费相机报告并生成承接请求

执行状态：已完成首轮。任务管理等待项巡检已能消费 D455 逐簇识别报告，生成观察事实承接请求并上行；日志确认 `直接写需求树=0`、`直接写世界树=0`。

目标：

```text
任务管理消费外设观察报告队列或稳定特征缓存；
命中等待项后生成任务回执或观察事实承接请求；
由提交入口裁决是否入世界树。
```

边界：

```text
任务管理不直接写世界树真值；
任务管理不直接关闭需求；
任务管理不直接结算安全 / 服务值。
```

### C4 自我承接观察报告为内部事实

执行状态：已完成首轮。自我线程已能接收任务管理观察事实承接请求，按报告 ID 回查外设报告，并将报告承接为观察报告、观察像素簇集合、存在候选集合和识别摘要等内部事实；连续报告ID=3、7 承接成功。

目标：

```text
自我线程接收观察事实承接请求后，调用自我侧对象作用方法或承接入口，将报告转成：
当前观察帧
观察像素簇集合
簇级掩码 / 局部图引用
存在候选集合
当前帧存在识别摘要
```

要求：

```text
外设报告只是来源材料；
自我承接动作才是自我内部事实写入来源；
写入必须留下来源报告、来源动态、来源场景或承接动作动态。
```

### C4.5 外设观察模式触发纠偏

执行状态：2026-05-30 检查发现当前实现与最新口径有出入，先制定纠偏计划，后续代码修改必须先执行本段。

当前实现事实：

```text
1. `--auto-observe-stable` 诊断入口会一次性提交：
       陌生环境逐簇识别等待项
       已识别场景扫描变化等待项
       目标跟踪观察等待项

2. D455 线程每个成功采样帧都会生成逐簇识别报告。

3. D455 线程只有在队列中存在扫描变化等待项时才生成扫描变化报告，
   但当前诊断入口无条件创建该等待项。

4. D455 线程只有在队列中存在目标跟踪等待项时才生成跟踪报告，
   但当前诊断入口无条件创建目标簇=0 的等待项。

5. D455 的逐簇识别跨帧缓存会为本帧所有簇分配或匹配跨帧簇ID，
   当前实现会统计多个跨帧稳定簇，而不是一次只围绕一个待确认候选进行目标跟踪。
```

最新口径：

```text
逐簇识别：
    是陌生场景下的基础本能观察模式；
    可持续输出簇候选材料；
    跨帧连续性只作为候选验证证据；
    不等于目标跟踪；
    不等于已确认观察存在。

目标跟踪：
    只有自我或任务管理基于明确来源需求发出目标跟踪等待项 / 指令后才执行；
    等待项必须携带目标簇、存在候选、ROI、掩码或目标特征摘要之一作为跟踪种子；
    同一时刻第一版只允许一个主动目标跟踪对象；
    该目标确认、否定、丢失或交回后，才选择下一个候选。

扫描变化：
    只在场景已确认 / 基准场景已建立 / 已识别场景需要守望时执行；
    未识别场景不生成扫描变化等待项；
    未建立基准时不得用相邻逐簇报告伪装成已识别场景扫描变化。
```

纠偏计划：

```text
C4.5-1 诊断入口拆分：
    `--auto-observe-stable` 默认只提交逐簇识别等待项；
    扫描变化等待项只能由“已识别场景守望”诊断或正式需求创建；
    目标跟踪等待项只能由“指定目标跟踪”诊断或正式需求创建；
    当前三等待项同跑入口保留时必须标记为全模式诊断，不作为业务验收入口。

C4.5-2 等待项前置条件补齐：
    扫描变化等待项必须记录基准场景 / 基准报告 / 已确认场景解释来源；
    目标跟踪等待项必须记录目标种子来源；
    缺前置时任务管理只生成缺口或等待，不让外设线程生产对应正式报告。

C4.5-3 D455 线程调度收口：
    逐簇识别报告可作为陌生场景基础报告持续生产或按等待项生产；
    扫描变化报告生成前检查已识别场景基准；
    跟踪报告生成前检查目标种子；
    若存在多个跟踪等待项，第一版按确定规则只选择一个，其余保持等待并记录跳过原因。

C4.5-4 跨帧簇ID降权：
    外设跨帧簇ID只表示外设内部候选连续性证据；
    不作为世界存在ID；
    不作为目标跟踪报告的充分条件；
    不作为当前场景存在集合明确证据。

C4.5-5 顺序确认候选：
    逐簇识别报告可以包含多个簇候选；
    自我侧每轮只选择一个待确认候选进入目标跟踪 / 像素归属深验证；
    候选确认、否定、拆分、合并或交回后，再选择下一个候选。

C4.5-6 验证：
    无扫描变化等待项时，不应出现扫描变化报告入队；
    无目标跟踪等待项或无目标种子时，不应出现正式跟踪报告入队；
    有一个目标种子时，只出现一个主动跟踪报告；
    逐簇识别报告中的多个跨帧簇只能作为候选连续性摘要；
    安全评估仍不得把这些外设报告直接解释成基础风险已明确或安全值结算。
```

### C4.6 外设临时材料与递归识别任务口径

执行状态：2026-05-30 根据最新口径新增，后续拆成两段执行：C4.6-1 先补 C5-3a 需要的 `d455://` 材料引用解引用和短期观察材料库；C4.6-2 在 C5-3c 生成已确认存在后，再补内部世界递归识别所需的局部材料和任务链。

定案：

```text
外设层生成的数据是临时观察材料；
需要什么材料可以按识别任务需要定义；
任务管理层负责接收外设消息、创建 / 推进识别任务；
识别任务通过自我对象作用方法验证、确认和递归细分；
自我方法层按规范补齐必要本能方法。
```

边界：

```text
外设层：
    只生产临时材料、候选材料和可回查句柄；
    可定义帧缓存、ROI、掩码、点集、轮廓、局部截图、深度切片、像素归属账本、坐标系和时间戳等材料；
    不生成已确认观察存在；
    不写当前场景存在集合明确；
    不写安全度、风险状态或自我价值账。

任务管理层：
    接收外设报告 / 材料消息；
    根据来源需求创建外设等待项、观察候选验证任务、内部世界递归识别任务；
    调用对象作用方法执行识别任务并收集回执；
    不绕过自我方法层直接写世界真值；
    不把任务筹办 / 任务执行恢复成本能方法。

自我方法层：
    提供候选验证、存在确认、内部世界建立、局部材料映射、子候选提取和子存在确认等对象作用方法；
    方法执行输出结果场景、动作动态、缺口和证据引用；
    正式需求满足、已确认存在入账和价值结算仍走提交入口。
```

外设临时材料第一版至少需要支持：

```text
帧级材料：
    原始帧ID / 深度帧ID / 彩色帧ID / 对齐帧ID；
    帧尺寸、时间戳、相机内参 / 外参、坐标系版本；
    原始帧句柄、深度帧句柄、彩色帧句柄、对齐帧句柄；
    全帧像素归属账本句柄或可重建输入句柄。

簇级材料：
    簇ID；
    ROI / 掩码 / 像素点集句柄；
    轮廓线集合句柄；
    深度统计和局部点云 / 空间候选句柄；
    跨帧簇ID，仅作候选连续性材料；
    识别方法ID、版本、置信度和材料有效期。

局部递归材料：
    父存在ID / 父簇ID；
    父存在局部坐标系；
    父存在局部截图、局部深度切片、局部掩码和局部轮廓；
    观察帧坐标 <-> 父存在内部世界坐标映射；
    子候选 ROI / 掩码 / 轮廓 / 深度切片句柄；
    材料来源外设报告、来源帧、来源任务和来源动作动态。
```

C4.6-1 短期观察材料库边界：

```text
目标：
    让 C5-3a 能真实回查验证材料；
    不把 d455:// 字符串句柄误当成材料已经可用；
    不把材料可回查误当成存在已确认。

句柄格式建议：
    d455://report/{报告ID}
    d455://frame/color/{帧ID}
    d455://frame/depth/{帧ID}
    d455://frame/aligned/{帧ID}
    d455://cluster/{报告ID}/{簇ID}
    d455://mask/{报告ID}/{簇ID}
    d455://roi/{报告ID}/{簇ID}
    d455://pointset/{报告ID}/{簇ID}
    d455://depthslice/{报告ID}/{簇ID}
    d455://pixel-ledger/{报告ID}
    d455://calibration/{相机配置版本}

最小解引用 API：
    解析D455材料句柄(句柄)
    读取外设报告(报告ID)
    读取原始彩色帧(帧ID)
    读取深度帧(帧ID)
    读取对齐帧(帧ID)
    读取簇掩码(报告ID, 簇ID)
    读取ROI(报告ID, 簇ID)
    读取点集(报告ID, 簇ID)
    读取深度切片(报告ID, 簇ID)
    读取像素归属账本(报告ID)

解引用结果必须区分：
    成功；
    不存在；
    已过期；
    材料类型不匹配；
    句柄格式错误；
    权限不足；
    仍在写入。
```

所有权和清理规则：

```text
外设线程 / 桥接线程：
    生产材料页和报告元数据。

短期观察材料库：
    拥有句柄索引、元数据、引用计数、TTL、清理策略。

大块图像：
    默认不复制；
    使用只读共享页 / 环形缓存 / 引用计数句柄。

掩码 / ROI / 点集：
    小块材料可以复制；
    大块点集优先引用共享页。

自我线程：
    只能通过句柄读取不可变快照。

报告发布后：
    对应材料对自我侧表现为不可变快照；
    每个句柄携带 generation / frame_id，防止环形缓存复用误读。

TTL 至少覆盖：
    所有关联等待项完成；
    所有关联任务结束；
    所有关联验证动作完成；
    最小保留帧数 / 时间窗口未过期。

清理线程：
    只清理引用计数为 0 且 TTL 到期的材料。
```

材料丢失回执：

```text
若验证方法回查失败，输出：
    外设观察验证材料不足。

细分原因：
    原始观察帧不可回查；
    深度帧不可回查；
    掩码不可回查；
    ROI不可回查；
    点集不可回查；
    像素归属账本不可回查；
    材料已过期。

此时：
    不确认存在；
    不写当前场景存在集合明确；
    不写基础风险明确；
    不结算安全值；
    只保留外设候选报告承接记录、候选验证失败结果场景和缺口需求。
```

C4.6-2 内部世界递归材料边界：

```text
执行前提：
    C5-3c 已经提交已确认观察存在事实；
    已确认存在具备来源帧、ROI、掩码、轮廓或空间范围原始事实；
    该存在产生内部世界细分相关需求。

目标：
    为已确认存在建立局部递归材料；
    让人脸、杯子等已确认存在可以作为内部世界场景继续识别子存在 / 子特征；
    不让外设局部材料直接变成子存在事实。
```

执行状态：

```text
2026-05-30 已完成当前轮实现和真实验证。
诊断链在 C5-3c 提交已确认观察存在事实、C4.7 生成下一帧轮廓分区结果后，调用：
    自我_将存在局部观察切片映射到内部世界；
    自我_在内部世界提取子候选。

本轮只建立内部世界局部材料和子候选待验证视图，不调用 `自我_确认内部世界子存在`，不把局部材料直接写成内部成员存在。
```

递归识别任务链：

```text
外设逐簇识别报告
-> 任务管理接收消息
-> 生成或推进 外设观察像素簇候选已验证 任务
-> 自我_验证外设观察像素簇候选
-> 验证通过后，经执行 / 提交链生成已确认观察存在
-> 若该存在需要内部细分，生成 存在内部世界细分状态 相关需求 / 任务
-> 自我_确保存在内部世界
-> 自我_建立存在内外场景接口
-> 自我_将存在局部观察切片映射到内部世界
-> 自我_在内部世界提取子候选
-> 自我_确认内部世界子存在
-> 必要时递归进入子存在内部世界
```

以人脸为例：

```text
已确认观察存在 = 人脸
-> 人脸.内部世界 = 人脸作为局部场景
-> 外设 / 材料库提供人脸 ROI、局部彩色截图、局部深度切片、轮廓、掩码和坐标映射
-> 自我方法在人脸内部世界提取眼部候选
-> 眼睛作为人脸内部世界的子存在
-> 眼睛在人脸上的局部坐标、眼眶大小、深度等写成有来源材料和动作动态的状态 / 二次特征 / 动态证据
-> 不把这些子特征写成外设报告的永久结论
```

需要补齐或登记的自我对象作用方法：

```text
自我_验证外设观察像素簇候选        已首轮实现，后续需接真实材料解引用。
自我_确认观察存在并更新帧解释状态    复用既有确认链，必须在材料验证充分后调用。
自我_确保存在内部世界                已有规范建议，后续按需要登记为对象作用方法。
自我_建立存在内外场景接口            已有规范建议，负责父场景 / 观察帧 / 内部世界映射。
自我_将存在局部观察切片映射到内部世界  已有规范建议，负责 ROI / 深度 / 掩码映射。
自我_在内部世界提取子候选            已有规范建议，负责轮廓、深度和局部图生成子候选。
自我_确认内部世界子存在              已有规范建议，负责子存在确认和挂入父存在内部世界。
自我_递归细分存在内部世界            已有规范建议，作为流程型对象作用方法或由任务管理拆步推进。
```

存储原则：

```text
外设临时材料：
    保存在外设材料库 / 短期观察材料库；
    通过 d455:// 或等价句柄解引用；
    带来源报告、时间戳、有效期和清理策略；
    不作为世界树主信息权威字段。

世界事实：
    只有经自我对象作用方法验证并经提交链承接后，才进入存在、场景、状态、动态、二次特征或因果实例。

内部世界：
    子存在、子轮廓、子结构和叶子特征块写入父存在.内部世界；
    局部材料保留到原观察帧坐标的可追溯映射；
    递归停止条件仍按内部结构递归细分规范执行。
```

验收预测：

```text
1. C4.5 后，默认陌生场景诊断只触发逐簇识别，不自动触发扫描变化和目标跟踪。
2. C4.6-1 后，d455:// 材料句柄可解引用到 C5-3a 所需的帧级、簇级、轮廓、掩码、ROI、点集和像素归属材料。
3. C4.6-2 后，d455:// 或内部材料句柄可解引用到局部递归材料。
4. 任务管理日志能显示：接收外设消息 -> 创建 / 推进识别任务 -> 调用自我对象作用方法。
5. 对一个已确认人脸存在，能生成或推进内部世界细分任务，并以人脸内部世界为场景记录眼部候选材料和子存在候选。
6. 未经自我确认和提交链，不出现已确认观察存在、当前场景存在集合明确、安全值结算或基础风险已明确。
```

### C4.7 已确认轮廓驱动的下一帧分区识别 / 扫描

执行状态：2026-05-30 已完成当前轮实现和真实验证；本节虽然上接在 C4.6 后说明，但实际执行已按硬依赖排在 C5-3c 之后，只消费已确认观察存在、已提交 ROI / 掩码 / 轮廓原始事实和来源动作动态。

定案：

```text
当一个存在已经被确认 / 识别，
并且已经有可追溯的外轮廓 / 掩码 / ROI 后，
下一帧不再默认把整帧都作为陌生区域逐簇识别。

应先用已确认存在的上一帧轮廓或预测轮廓拆分当前原始帧：
    轮廓覆盖且匹配可信的部分 -> 已知存在扫描区；
    未被任何已知轮廓覆盖的部分 -> 新识别区；
    轮廓重叠、漂移、残差异常或遮挡的部分 -> 冲突 / 待重识别区。
```

这里的“轮廓图”仍是观察材料和分区依据，不是永久世界真值。它必须能追溯到：

```text
来源观察帧；
来源已确认存在；
来源轮廓 / 掩码 / ROI；
来源像素归属账本；
来源确认动作动态；
轮廓有效期和置信度。
```

下一帧分区流程：

```text
1. 取得当前原始帧、深度帧和对齐帧。

2. 读取上一轮已确认观察存在集合。

3. 对每个已确认存在 E，读取：
       E 的上一帧外轮廓 / 掩码 / ROI；
       E 的空间范围、深度范围和轮廓置信度；
       E 的最近确认时间、来源帧和来源动作动态；
       E 是否需要继续扫描 / 守望 / 内部细分。

4. 将 E 的上一帧轮廓投影或预测到当前帧。
       第一版可以使用同坐标近邻投影 + ROI 扩张；
       后续可由目标跟踪、姿态变化或空间变换提供更准预测。

5. 生成分区材料：
       已知存在扫描区 = 已确认存在轮廓投影区域及必要扩张边界；
       新识别区 = 当前帧 - 所有已知存在扫描区；
       冲突区 = 多个轮廓重叠 / 像素归属不一致区域；
       待重识别区 = 轮廓匹配失败、残差过大、深度突变或遮挡区域。

6. 对已知存在扫描区执行扫描变化：
       比对当前局部彩图、深度、轮廓、空间范围；
       更新候选轮廓、位置、可见状态、遮挡状态和变化摘要；
       若变化小且证据充分，输出该存在当前帧扫描结果；
       若变化大或匹配失败，输出重识别缺口。

7. 对新识别区和待重识别区执行逐簇识别：
       只在这些区域生成新簇候选；
       不把已知存在扫描区重复当成陌生区域全量识别；
       若扫描区内部出现未解释残差，也可派生局部识别任务。

8. 若某个已确认存在需要内部结构识别：
       用它的当前轮廓 / ROI 截取局部材料；
       进入该存在.内部世界；
       继续执行内部世界递归细分任务。
```

C4.7 判据补齐：

```text
轮廓有效期：
    有效帧数窗口；
    有效时间窗口；
    相机位姿连续性；
    深度连续性；
    遮挡状态；
    最近匹配状态。

轮廓失效条件：
    超过有效期；
    连续匹配失败达到阈值；
    相机位姿跳变；
    深度残差过大；
    轮廓残差过大；
    可见比例过低且非遮挡；
    材料缺失。

投影 / 预测优先级：
    1. 有 3D 空间候选时，使用深度 + 相机内参 / 外参 / 位姿变化投影到当前帧；
    2. 无稳定 3D 时，使用上一帧 2D ROI / 掩码 + 运动估计 / 光流 / 安全扩张边界；
    3. 无运动估计时，使用上一帧 ROI 膨胀为预测区域。

预测输出：
    预测ROI；
    预测掩码；
    预测中心；
    预测深度区间；
    预测置信度。

匹配分数组成：
    ROI IoU；
    掩码重叠率；
    中心点距离；
    深度中位数残差；
    轮廓边界残差；
    像素归属一致性；
    外观 / 特征相似度，若已有。

匹配判定：
    分数 >= 匹配阈值 -> 匹配成功；
    低于阈值但有遮挡证据 -> 暂存为遮挡 / 待观察；
    低于阈值且无遮挡证据 -> 匹配失败。

残差记录：
    像素残差 = 预测掩码与当前归属像素差异；
    深度残差 = 当前深度与预测深度区间差异；
    轮廓残差 = 当前轮廓边界与预测轮廓边界距离。

遮挡成立条件：
    预测区域内出现更近深度像素；
    被遮挡区域与其他已知存在或未知簇重叠；
    原轮廓可见比例下降；
    残余可见部分仍与原存在匹配。

多个已知轮廓重叠优先级：
    更高匹配分数优先；
    更近深度优先获得重叠像素；
    最近确认时间更近者优先；
    历史连续性更强者优先；
    仍无法决定时进入冲突区域，不强行归属。

匹配失败转重识别：
    单帧失败 -> 标记为待重扫 / 暂失，不立即删除存在；
    连续失败达到阈值 -> 生成未知区域识别需求；
    残差严重超阈值 -> 立即转未知区域识别；
    材料缺失 -> 生成材料缺口，不做重识别结论；
    遮挡成立 -> 等待恢复或局部扫描，不直接重识别。
```

区域语义：

```text
需要扫描的部分：
    已确认存在在下一帧中的预测轮廓区域；
    已确认存在轮廓附近的边界带；
    已建立基准的场景区域；
    已识别存在的内部世界当前观察切片。

需要识别的部分：
    未被任何已确认轮廓覆盖的区域；
    新出现轮廓 / 新深度团块；
    已知轮廓投影失败或漂移过大的区域；
    多存在轮廓重叠导致归属不明的区域；
    已知存在扫描区内出现的未解释残差区域。
```

任务管理调度：

```text
任务管理接收当前帧材料和已确认轮廓材料；
先创建或推进 当前帧轮廓分区状态 相关任务；
分区结果产生两个任务队列：
    已知存在扫描任务队列；
    未知 / 冲突区域识别任务队列。

已知存在扫描任务可触发：
    已识别场景扫描变化报告；
    已确认存在当前帧轮廓更新；
    已确认存在内部世界局部材料更新。

未知 / 冲突区域识别任务可触发：
    陌生区域逐簇识别；
    外设观察像素簇候选验证；
    新已确认观察存在候选；
    旧存在重识别 / 丢失 / 遮挡缺口。
```

与目标跟踪的关系：

```text
轮廓分区不是目标跟踪指令。

轮廓分区可以为已知存在生成扫描区；
但只有自我或任务管理基于明确来源需求指定一个目标种子时，
才进入目标跟踪。

第一版同一时刻仍只允许一个主动目标跟踪对象；
其他已知存在只做轮廓扫描或保持等待。
```

输出材料：

```text
当前帧轮廓分区结果；
已知存在扫描区集合；
新识别区集合；
冲突区 / 待重识别区集合；
每个分区的来源轮廓、来源存在、来源帧和像素范围；
分区置信度；
派生扫描任务 / 识别任务建议；
禁止直接入账标记。
```

验收预测：

```text
1. 存在已确认且有轮廓时，下一帧日志应出现轮廓分区结果。
2. 已知存在轮廓覆盖区域进入扫描任务，不重复进入陌生全帧逐簇识别。
3. 轮廓外区域和冲突区域进入识别任务。
4. 轮廓匹配失败时，不伪造扫描成功，而是生成重识别 / 补观察缺口。
5. 分区结果仍是临时材料，不直接写已确认观察存在、场景安全度或价值账。
```

执行更新：

```text
2026-05-30 已新增 `自我_按已确认轮廓分区当前帧` 本能方法 ID 6130，并接入方法头、条件 / 结果场景、动作句柄、内部 / 外部包装和 `--auto-observe-stable` 真实诊断链。
诊断入口在 C5-3c 提交已确认观察存在事实后，才启动下一帧 D455 采样，并调用该方法生成当前帧轮廓分区结果。
Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0。
最新日志 `日志/鱼巢_run_20260530.低值g` 第 1335 行显示 `当前帧轮廓分区结果 | 方法=自我_按已确认轮廓分区当前帧 | 成功=1 | 报告ID=4 | 当前帧ID=3 | 已确认存在=9 | 已知扫描区=9 | 新识别区=6 | 冲突区=9 | 待重识别区=0 | 像素归属闭合状态=1`，并明确记录 `不是目标跟踪=1`、`不创建观察存在=1`、`不写基础风险明确=1`、`不结算安全值=1`。
```

### C5 存在候选验证到当前场景存在集合

执行状态：C5-1 外设报告证据引用补强、C5-2 / C5-3a `自我_验证外设观察像素簇候选`、C5-3b `自我_确认外设观察存在候选` 和 C5-3c `提交_确认观察存在事实` 均已完成当前轮实现和真实链路验证。

C5-3 分段定案：

```text
C5-3a 自我_验证外设观察像素簇候选：
    只做材料回查和候选验证；
    输出验证结果、可确认候选、失败原因和缺失材料；
    不创建存在节点；
    不写当前场景存在集合；
    不写已确认观察存在；
    不写安全风险明确；
    不写安全值。

C5-3b 自我_确认外设观察存在候选：
    接收 C5-3a 的可确认候选；
    判断新建、合并、冲突或证据不足；
    输出确认方案；
    仍不直接写世界账。

C5-3c 提交_确认观察存在事实：
    只在确认方案充分时正式创建或合并观察存在；
    写入存在原始观察事实；
    写入观察动态、二次特征和当前场景索引；
    回写相关需求满足状态；
    这是正式改账入口。
```

C5-3a 输入 / 动作 / 输出：

```text
输入：
    外设报告ID；
    d455:// 原始帧句柄；
    d455:// 深度帧句柄；
    d455:// 簇掩码 / ROI / 点集句柄；
    d455:// 空间候选句柄；
    d455:// 像素归属账本句柄或可重建材料。

动作：
    回查报告材料；
    验证像素簇是否来自当前帧；
    验证掩码 / ROI / 点集是否可解引用；
    验证像素归属账本一致性；
    验证深度 / 空间候选可用性；
    验证跨帧簇ID只作为连续性证据，不作为确认依据。

输出：
    外设像素簇候选验证通过 / 失败 / 材料不足；
    可确认候选列表；
    验证失败原因；
    缺失材料列表；
    输出结果场景。
```

C5-3b 输入 / 动作 / 输出：

```text
输入：
    C5-3a 输出的可确认候选；
    当前场景；
    已有观察存在集合；
    已有轮廓 / ROI / 掩码 / 空间位置原始事实。

动作：
    与已有存在做空间匹配；
    与已有轮廓 / ROI / 掩码做像素匹配；
    与当前帧像素归属账本做冲突检查；
    判断新建 / 合并 / 冲突 / 证据不足。

输出：
    确认方案；
    新建存在候选；
    合并到已有存在候选；
    冲突候选；
    证据不足候选。
```

C5-3c 正式写入内容：

```text
创建 / 合并：
    若确认方案 = 新建，创建具体观察存在节点；
    若确认方案 = 合并，更新已有观察存在的当前帧观察事实；
    若确认方案 = 冲突 / 证据不足，不创建已确认存在，只保留候选和缺口。

写入原始事实：
    存在_来源外设报告ID；
    存在_来源原始观察帧ID；
    存在_来源深度帧ID；
    存在_来源像素簇ID；
    存在_来源ROI句柄；
    存在_来源掩码句柄；
    存在_来源点集句柄；
    存在_当前场景可见状态；
    存在_当前帧观察时间；
    存在_当前帧观察序号；
    存在_当前帧空间候选；
    存在_当前帧像素归属验证状态。

写入动态：
    对象作用动态，来源方法 = 自我_确认外设观察存在候选；
    提交动作动态，来源提交入口 = 提交_确认观察存在事实。

写入二次特征 / 关系：
    具体存在E 属于 当前场景存在；
    具体存在E 来源于 外设报告ID=R；
    具体存在E 对应 像素簇ID=C；
    具体存在E 位于 当前场景S；
    具体存在E 拥有 当前帧ROI/掩码材料。

禁止写入：
    存在安全度；
    存在伤害可能性；
    场景安全度；
    基础风险已明确；
    安全值应增加。
```

当前场景存在集合明确状态判据：

```text
只有同时满足以下条件，才允许写 当前场景存在集合明确状态 = 明确：
    当前帧有效观察区域已扫描；
    有效像素簇全部处理完毕；
    验证通过簇已经确认或合并；
    验证失败簇有明确失败原因；
    未知区域已经被记录为未知区域，而不是静默忽略；
    当前帧像素归属账本闭合。

否则只能写：
    当前场景存在集合明确状态 = 部分明确 / 证据不足。
```

已验证证据：

```text
构建：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    结果：0 错误；仅保留 LNK4075 增量链接警告。

真实运行：
    .\x64\Debug\鱼巢.exe --auto-observe-stable
    结果：退出码 0。

控制台证据：
    D455 报告已输出原始帧 / 深度帧 / 彩色帧 / 对齐帧句柄；
    空间候选队列引用=1；
    像素归属账本引用=1；
    可回查材料簇=7；
    自我验证链路已执行=1。

日志证据：
    日志/鱼巢_run_20260530.低值g 第 1152 行：
    外设观察像素簇候选验证结果
    | 方法=自我_验证外设观察像素簇候选
    | 成功=1
    | 报告ID=7
    | 候选簇数量=7
    | 通过簇数量=7
    | 失败簇数量=0
    | 证据不足簇数量=0
    | 缺失材料数量=0
    | 验证状态=1
    | 已确认观察存在生成条件明确状态=1
    | 不生成已确认观察存在=1
    | 不写当前场景存在集合明确=1
```

已完成 C4.6-1 / C5-3a 前置：

```text
2026-05-30 已补：
    空间候选摘要携带簇内像素索引集合；
    外设观察报告队列维护 D455 短期观察材料页；
    d455:// 原始帧、深度帧、彩色帧、对齐帧、空间候选队列、像素归属账本、ROI、掩码、点集、局部图和像素归属材料可以按句柄解引用为材料摘要；
    自我_验证外设观察像素簇候选 不再只检查字符串非空，而是调用 `解析D455观察材料句柄` 验证材料可回查。

验证：
    Debug x64 构建通过；
    `.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；
    最新真实日志显示 3 个逐簇识别报告均 `短期观察材料页提交=1`；
    最新验证结果为报告ID=3、候选簇数量=12、通过簇数量=12、缺失材料数量=0；
    仍明确记录 `不生成已确认观察存在=1`、`不写当前场景存在集合明确=1`。
```

已完成 C5-3b / C5-3c：

```text
2026-05-30 已新增：
    `自我_确认外设观察存在候选`
        只读取 C5-3a 可确认候选和已有已确认观察存在集合；
        只输出新建 / 合并 / 冲突 / 证据不足确认方案结果场景；
        不写已确认观察存在。

    `提交_确认观察存在事实`
        作为 C5-3c 唯一正式入账入口；
        正式创建或合并观察存在；
        写入原始观察事实、动作动态、二次特征关系和当前场景索引。

旧 `自我_确认观察存在并更新帧解释状态` 已从默认本能方法登记、内部注册和 R3 诊断链脱钩。

验证：
    Debug x64 构建通过；
    `.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；
    最新日志第 1327 行显示 C5-3b 对报告ID=3 输出新建=14、合并=0、冲突=0、证据不足=0、可提交=14，并记录 `不创建观察存在=1`、`不写当前场景存在集合明确=1`、`不结算安全值=1`；
    第 1329 行显示 C5-3c 提交新建=14、已提交=14、提交状态=1、写入观察原始事实状态=1、更新当前场景索引=1、写入二次特征数量=42、场景存在集合明确状态=1，并记录 `不写基础风险明确=1`、`不结算安全值=1`。
```

方案定案：

```text
C5 选择方案 B 为主：
    新增 自我_验证外设观察像素簇候选 对象作用方法。

同时采用方案 A 的最小数据接口补强：
    外设报告只携带可回查的原始帧、像素簇、空间候选、像素归属账本等材料引用。

不采用：
    外设报告直接携带确认结论；
    外设簇候选直接写成已确认观察存在；
    跨帧簇ID直接写成具体存在；
    观察事实承接成功直接写基础风险明确或安全值结算。
```

原阻塞原因：

```text
当前外设报告承接链只把逐簇识别报告转成观察报告承接状态、观察像素簇集合、存在候选集合和识别摘要。
报告中的簇摘要可作为候选证据，但还不是已确认观察存在证据。

现有 `自我_确认观察存在并更新帧解释状态` 依赖：
    原始观察帧 / 空间候选队列
    观察存在假设
    像素归属验证结果
    全帧像素归属账本

而任务管理观察事实承接请求目前只携带报告ID和簇摘要字段，
没有携带足以复用该确认链的像素归属验证材料。
自我线程当前也只把承接事实写入自我内部世界的观察材料记录，
没有合法证据把外设簇候选直接写成当前场景已确认观察存在。
```

禁止临时绕过：

```text
不得把外设内部跨帧簇ID当作世界存在ID。
不得把存在候选集合状态直接解释为当前场景存在集合明确。
不得为了推进安全评估而把观察像素簇候选直接创建成已确认观察存在。
```

后续主链：

```text
当前观察特征帧取得状态 == 已取得
-> D455 产生逐簇识别报告
-> 外设报告承接生成观察材料 / 外设簇候选
-> C4.6-1 回查 d455:// 材料并确认材料可用
-> 生成或推进需求：外设观察像素簇候选已验证
-> C5-3a 自我_验证外设观察像素簇候选
-> 输出验证结果场景和可确认候选
-> 生成或推进需求：外设观察存在候选确认状态
-> C5-3b 自我_确认外设观察存在候选
-> 输出新建 / 合并 / 冲突 / 证据不足确认方案
-> C5-3c 提交_确认观察存在事实
-> 正式创建 / 合并已确认观察存在
-> 写入原始观察事实、动态、二次特征和当前场景索引
-> 提交_需求满足状态
-> 按判据更新当前场景存在集合明确状态
```

`自我_验证外设观察像素簇候选` 定义：

```text
方法类型：
    对象作用方法 / 观察验证类方法。

可满足需求：
    外设观察像素簇候选已验证状态
    当前观察候选存在验证状态
    已确认观察存在生成条件明确状态

间接支持：
    当前场景存在集合明确状态
    风险状态明确
    当前场景安全度明确

输入条件：
    外设报告ID存在；
    报告ID可回查原始观察帧 / 深度帧 / 对齐帧；
    报告ID可回查逐簇识别结果；
    每个簇有像素 / ROI / 掩码 / 点集引用之一；
    有深度信息或空间候选；
    有相机坐标系、时间戳、帧序号；
    当前场景存在；
    自我具备读取外设材料的权限。

输出结果：
    验证通过 / 验证失败 / 证据不足；
    像素归属验证结果；
    空间候选验证结果；
    跨帧一致性验证结果；
    可承接的观察存在候选；
    已确认观察存在候选；
    失败原因；
    缺失材料列表；
    来源外设报告、来源原始帧、来源像素归属账本、来源方法动作动态。
```

目标：

```text
把观察像素簇和存在候选通过自我验证链生成已确认观察存在；
更新当前场景存在集合明确状态；
为 `安全_评估当前场景安全性` 提供可读取的原始事实底稿。
```

不得做：

```text
不得把外设内部跨帧簇ID当作世界存在ID；
不得把存在候选直接等同于已确认观察存在；
不得把已确认观察存在直接等同于安全或危险。
```

### C5.5 任务目标特征补齐清单

目标：让任务筹办能按目标特征召回对象作用方法，而不是靠报告类型、领域词或线程状态硬连。

```text
C4.6-1 材料库目标特征：
    外设报告材料可回查状态；
    外设原始观察帧可回查状态；
    外设深度帧可回查状态；
    外设簇掩码可回查状态；
    外设ROI可回查状态；
    外设点集可回查状态；
    外设像素归属账本可回查状态。

C5-3 验证 / 确认目标特征：
    外设观察像素簇候选验证状态；
    外设观察存在候选确认状态；
    已确认观察存在取得状态；
    当前场景存在集合明确状态；
    观察存在原始事实写入状态；
    观察存在与当前场景关联状态。

C4.7 分区目标特征：
    当前帧轮廓分区状态；
    已知存在当前帧扫描状态；
    未知区域识别状态；
    当前帧遮挡判断状态；
    当前帧轮廓冲突区域明确状态；
    当前帧像素归属闭合状态。

C4.6-2 内部世界递归目标特征：
    存在内部世界局部材料建立状态；
    内部世界子候选集合生成状态；
    内部世界子候选验证状态；
    内部世界局部轮廓材料可回查状态。
```

要求：

```text
候选对象作用方法必须先形成候选集；
任务管理生命周期算法函数不能进入候选方法集；
缺目标特征生产者时，生成学习 / 补齐缺口，不用线程裸写绕过。
```

### C6 接入安全评估消费者

目标：

```text
当当前场景存在集合明确后，让安全评估自然读取相机承接后的场景事实：
存在出现时间
存在当前是否在场
存在动作 / 状态动态
自我关键特征变化动态
因果候选 / 因果实例
```

第一版允许结果：

```text
若缺关键特征变化或因果候选：
    安全评估输出证据不足；
    派生关键特征变化观察或安全因果候选构造缺口；
    不写基础风险已明确。
```

### C7 结果满足验证与提交回写

目标：

```text
对象作用方法输出结果后，由执行模块提取结果状态并比对来源需求；
只有输出结果满足目标比较，才允许通过需求满足提交入口回写。
```

要求：

```text
安全_评估当前场景安全性 不直接关闭需求；
相机承接方法不直接关闭安全需求；
安全值结算仍必须等待因果归因和价值投影闸门。
```

### C8 真实验证

总原则：

```text
不再使用三模式同跑作为业务验收；
每个切片单独写预测、单独运行、单独核对日志；
不得新增模拟样本、夹具入口或 `--*-check` 回归入口；
不得并发运行多个 鱼巢.exe。
```

基础验证命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
.\x64\Debug\鱼巢.exe --peripheral-autonomous-observe
.\x64\Debug\鱼巢.exe --self-scene-html
```

C4.5 验收：

```text
目标：
    默认只产生逐簇识别报告。

预测：
    D455 能产生报告ID和逐簇摘要；
    不会生成已确认观察存在；
    不会写当前场景存在集合明确；
    不会写基础风险明确；
    不会结算安全值。

必查日志：
    D455逐簇识别报告产生；
    外设等待项触发；
    观察事实承接请求生成；
    自我线程按报告ID回查。
```

C4.6-1 验收：

```text
目标：
    d455:// 句柄真实可解引用。

预测：
    报告ID能解析到原始帧、深度帧、簇掩码 / ROI / 点集、像素归属账本；
    材料缺失时返回明确缺口；
    TTL 到期时返回已过期，不伪造材料。

必查日志：
    句柄解析成功；
    句柄解析失败原因；
    材料引用计数；
    TTL状态；
    环形缓存generation匹配。
```

C5-3a 验收：

```text
目标：
    自我_验证外设观察像素簇候选 只输出验证结果和可确认候选。

预测：
    验证通过时输出可确认候选；
    验证失败时输出失败原因；
    材料不足时输出缺口；
    不写存在节点；
    不写当前场景存在集合明确。
```

C5-3b 验收：

```text
目标：
    确认方法生成新建 / 合并 / 冲突 / 证据不足方案。

预测：
    同一物理候选不会重复新建；
    冲突候选不入账；
    证据不足候选保留为候选。
```

C5-3c 验收：

```text
目标：
    存在和特征经提交链写入。

预测：
    提交_确认观察存在事实 创建或合并存在；
    写入原始观察事实；
    写入动态；
    写入二次特征 / 场景关联；
    提交_需求满足状态 回写相关需求。

必查日志：
    提交入口名称；
    创建 / 合并存在ID；
    来源报告ID；
    来源帧ID；
    来源簇ID；
    来源ROI/掩码句柄；
    写入状态列表；
    写入动态列表；
    写入二次特征列表；
    需求满足回写动作动态。
```

C4.7 验收：

```text
目标：
    下一帧基于已确认存在做轮廓分区。

预测：
    已知存在进入当前帧扫描；
    预测ROI / 掩码生成；
    匹配成功区域归属已知存在；
    匹配失败区域进入未知区域识别；
    遮挡区域标记遮挡；
    冲突区域进入冲突队列。

必查日志：
    当前帧轮廓分区状态；
    已知存在当前帧扫描状态；
    未知区域识别状态；
    预测ROI；
    匹配分数；
    残差；
    遮挡判断；
    冲突区域。
```

C4.6-2 验收：

```text
目标：
    内部世界递归材料建立。

预测：
    已确认存在可以生成内部世界局部材料；
    内部世界子候选集合可以生成；
    内部世界子候选仍需验证，不直接入账。

必查日志：
    存在内部世界局部材料建立状态；
    内部世界子候选集合生成状态；
    内部世界局部ROI/掩码/点集句柄。

实际验证：
    Debug x64 构建通过；
    `.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；
    最新日志 `日志/鱼巢_run_20260530.低值g` 第 1379 行显示 `自我_将存在局部观察切片映射到内部世界` 成功，`存在内部世界局部材料建立状态=1`、`内部世界局部轮廓材料可回查状态=1`，并输出局部 ROI / 掩码 / 点集 / 彩色图 / 深度图 d455:// 句柄；
    第 1380 行显示 `自我_在内部世界提取子候选` 成功，`内部世界子候选集合生成状态=1`、`内部世界子候选验证状态=1`、`子存在=` 为空，并记录 `不确认内部世界子存在=1`、`不写基础风险明确=1`、`不结算安全值=1`。
```

C6/C7 验收：

```text
目标：
    安全_评估当前场景安全性 能消费 C5-3c 已提交的当前场景已确认存在集合；
    执行链能提取安全评估结果并提交场景安全度评估结果；
    证据不足时按默认不安全输出低安全度结果，不堵塞结果提交，但不结算安全值。

实际验证：
    Debug x64 构建通过；
    `.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；
    最新日志显示 `C6/C7安全评估闭环` 已执行=1、结果满足=1、当前场景安全度明确状态=1、当前场景最低安全度存在明确状态=1、当前场景各存在安全评估明确状态=1；
    证据不足时当前场景评估安全值候选=4611686018427387903，当前场景安全度候选=4611686018427387903，当前场景直接伤害可能性候选=4611686018427387904；存在风险安全值按每个存在分别评估，历史缺失默认值为 0，不用固定常量伪造历史风险；场景存在风险聚合采用平均，场景内部额外衰减=0，并记录 `场景安全评估结果可提交=1`、`当前场景持续观察需求明确状态=0`、`低安全持续观察派生=1`、`后续随时间影响自我风险安全值=1`、`不结算安全值=1`。
```

R4 关键特征变化观察 / 安全因果候选构造验收：

```text
目标：
    `自我_读取自我关键特征变化` 作为安全对象作用方法，只建立自我关键特征变化观察窗口；
    `安全_构造场景影响因果候选` 作为安全对象作用方法，只基于已确认场景存在集合和关键特征观察结果生成低置信因果候选视图；
    两者均不定义安全因素、不写基础风险明确、不结算安全值；
    安全评估读取这两类前置事实后，若仍无归因证据，继续输出证据不足。

实际验证：
    Debug x64 构建通过；
    `.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；
    最新日志 `日志/鱼巢_run_20260530.低值g` 第 1476 行显示 `自我_读取自我关键特征变化` 成功=1、关键特征变化观察状态=1、关键特征观察样本数量=13、关键特征负向变化数量=0，并记录 `不定义安全因素=1`、`不写基础风险明确=1`、`不结算安全值=1`；
    第 1478 行显示 `安全_构造场景影响因果候选` 成功=1、安全因果候选构造状态=1、当前场景存在数量=13、安全因果候选数量=13、安全因果候选低置信数量=13、安全因果候选归因置信度=0，并记录 `不定义安全因素=1`、`不写基础风险明确=1`、`不结算安全值=1`；
    安全评估读取到关键特征变化观察状态=1、安全因果候选构造状态=1；归因证据不足时仍提交低安全度场景评估结果，保留证据不足原因和评估置信度=0；
    `C6/C7安全评估闭环` 记录结果满足=1、场景安全评估结果可提交=1、低安全持续观察派生=1、持续评估动作=1、不结算安全值=1。
```

R5a 证据不足默认不安全判定验收：

```text
目标：
    安全评估不再把证据不足输出为“未知安全”第三结论；
    证据状态仍保持低置信 / 证据不足；
    安全判定层输出默认不安全 / 不放行；
    当前场景安全度候选按 0..I64MAX 范围输出；
    I64MAX 只表示场景绝对安全；
    证据不足默认场景评估安全值为 I64MAX / 2，作为 `风险安全_场景影响部分` 候选；
    场景内部不再额外乘 0.09，风险安全层整体后续按第二层权重 0.09 投影；
    该默认不安全不等于已证高风险，不允许回写安全影响因果明确，不允许结算安全值；
    场景安全度后续才随时间推移和归因证据影响自我风险安全值。

实际验证：
    Debug x64 构建通过；
    `.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；
    最新日志显示 `C6/C7安全评估闭环` 结果满足=1、当前场景安全判定状态=1、当前场景安全判定来源=3、证据不足默认不安全=1、条件不足默认不安全=0；
    当前场景评估安全值候选=4611686018427387903，当前场景安全度候选=4611686018427387903，当前场景直接伤害可能性候选=4611686018427387904；
    最新复验日志 `日志/鱼巢_run_20260530.低值g` 第 1746 行显示存在风险评估全部明确=0、最高存在风险安全值候选=0、场景风险压力平均值=0、最低存在安全值候选=9223372036854775807；
    第 1800 行显示 C6/C7 结果满足=1，并记录 `风险安全层因素集合版本=1`、`风险安全层因素数量=5`、`场景存在风险聚合=平均`、`场景评估安全值=I64MAX-场景风险压力平均值`、`场景安全度=场景评估安全值候选`、`场景内部额外衰减=0`、`风险安全层投影=风险安全*0.09`、`场景安全评估结果可提交=1`、`当前场景持续观察需求明确状态=0`、`低安全持续观察派生=1`、`后续随时间影响自我风险安全值=1`、`不结算安全值=1`。
```

R5b 低安全持续观察需求线索验收：

```text
目标：
    安全评估结果满足后，如果安全判定来源为证据不足默认不安全，
    方法只派生 `当前场景持续观察需求明确状态 -> 已明确` 的后续线索；
    不直接把持续观察需求写成已满足，不直接执行持续观察对象方法，不结算安全值。

实际验证：
    Debug x64 构建通过；
    `.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；
    最新日志 `日志/鱼巢_run_20260530.低值g` 第 1799 行显示 `当前场景持续观察需求明确状态=0`、`低安全持续观察派生=1`、证据不足默认不安全=1、不安全判定允许提交=1；
    第 1800 行显示 C6/C7 结果满足=1、场景安全评估结果可提交=1、低安全持续观察派生=1、持续评估动作=1、不结算安全值=1。
```

R5c 低安全持续观察需求明确验收：

```text
目标：
    新增 `自我_明确当前场景持续观察需求` 作为安全对象作用方法；
    它只承接安全评估低安全派生线索，把 `当前场景持续观察需求明确状态` 从未明确推进到已明确；
    不启动外设线程，不直接重跑安全评估，不结算安全值。

实际验证：
    Debug x64 构建通过；
    `.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；
    最新日志 `日志/鱼巢_run_20260530.低值g` 第 1853 行显示安全评估仍输出 `当前场景持续观察需求明确状态=0`、低安全持续观察派生=1、场景安全评估结果可提交=1；
    第 1855 行显示 `自我_明确当前场景持续观察需求` 成功=1，输出特征为当前场景持续观察需求明确状态；
    第 1856 行显示原状态=0、新状态=1、由安全评估低安全线索承接=1、不启动外设线程=1、不直接重跑安全评估=1、不结算安全值=1；
    控制台最终诊断显示 `持续观察需求明确链路已执行=1`。
```

R5d 低安全持续观察 tick 验收：

```text
目标：
    新增 `安全_持续观察当前场景` 作为安全对象作用方法；
    它只在 `当前场景持续观察需求明确状态=已明确` 后记录一次持续观察 tick；
    输出 `当前场景持续观察状态` 和 `当前场景持续观察样本数量`，可被任务后续重复选择；
    不启动外设线程，不直接重跑安全评估，不结算安全值。

实际验证：
    Debug x64 构建通过；
    `.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；
    最新日志 `日志/鱼巢_run_20260530.低值g` 第 1909 行显示安全评估仍输出 `当前场景持续观察需求明确状态=0`、低安全持续观察派生=1、场景安全评估结果可提交=1；
    第 1912 行显示 `自我_明确当前场景持续观察需求` 成功=1，当前场景持续观察需求明确状态=1；
    第 1913 行显示 `安全_持续观察当前场景` 成功=1，输出特征为当前场景持续观察状态；
    第 1914 行显示当前场景持续观察需求明确状态=1、当前场景持续观察状态=1、当前场景持续观察样本数量=12、一次性tick=1、可被任务重复选择=1、不启动外设线程=1、不直接重跑安全评估=1、不结算安全值=1；
    控制台最终诊断显示 `持续观察tick链路已执行=1`。
```

R5e 低安全负向影响处置需求明确验收：

```text
目标：
    新增 `自我_明确负向影响处置需求` 作为安全对象作用方法；
    安全评估低安全结果同时输出 `负向影响处置需求明确状态=未明确`；
    该方法只在持续观察 tick 后把负向影响处置需求推进为已明确；
    不执行降级保护，不启动外设线程，不直接重跑安全评估，不结算安全值。

实际验证：
    Debug x64 构建通过；
    `.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；
    最新日志 `日志/鱼巢_run_20260530.低值g` 第 1966 行显示安全评估输出 `负向影响处置需求明确状态=0`、低安全处置需求派生=1；
    第 1967 行显示 C6/C7 结果满足=1、低安全处置需求派生=1、不结算安全值=1；
    第 1973 行显示 `自我_明确负向影响处置需求` 成功=1，输出特征为负向影响处置需求明确状态；
    第 1974 行显示原状态=0、新状态=1、当前场景持续观察状态=1、由低安全持续观察tick承接=1、不执行降级保护=1、不结算安全值=1；
    控制台最终诊断显示 `负向影响处置需求明确链路已执行=1`。
```

R5f 降级保护方法头与守门验收：

```text
目标：
    新增 `安全_执行降级保护` 作为安全对象作用方法；
    第一版只建立方法头、结果能力和守门实现；
    只有负向影响处置需求已明确且安全影响因果已明确时才允许执行；
    当前安全影响因果未明确时，必须输出条件不足，不执行降级保护，不结算安全值。

实际验证：
    Debug x64 构建通过；
    `.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；
    最新日志 `日志/鱼巢_run_20260530.低值g` 第 2035 行显示 `安全_执行降级保护` 成功=0，消息为安全影响因果未明确，输出特征为降级保护状态；
    第 2036 行显示负向影响处置需求明确状态=1、安全影响因果明确状态=0、降级保护状态=0、方法头已登记=1、守门通过=0、因果未明确不执行=1、不结算安全值=1；
    控制台最终诊断显示 `降级保护方法头链路已验证=1`。
```

R5g 因果信息被动获取与默认良好纠偏验收：

```text
目标：
    因果信息只作为被动获得的证据；
    没有负向因果证据时，不强求补齐因果链，不按默认不安全派生处置压力；
    存在风险压力候选为 0，场景评估安全值候选为 I64MAX；
    当前场景持续观察需求明确状态和负向影响处置需求明确状态均保持已明确；
    持续观察、负向处置和降级保护链均不触发。

实际验证：
    Debug x64 构建通过；
    `.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；
    控制台 `C6/C7安全评估闭环` 显示当前场景评估安全值候选=9223372036854775807、当前场景安全度候选=9223372036854775807、当前场景直接伤害可能性候选=0、当前场景安全判定状态=2、当前场景安全判定来源=5、因果信息被动获取=1、无负向因果默认安全=1、默认良好允许提交=1；
    同一轮显示低安全持续观察派生=0、低安全处置需求派生=0；
    最终诊断显示持续观察需求明确链路已执行=0、持续观察tick链路已执行=0、负向影响处置需求明确链路已执行=0、降级保护方法头链路已验证=0、场景安全评估结果可提交已验证=1、安全判定默认良好已验证=1。
```

R5h 安全评估结果可比较状态补齐与提交回写停止点：

```text
目标：
    安全评估方法不仅输出 I64 视图字段，还要把任务来源需求可能瞄准的安全评估明确项写成输出结果场景内的可比较状态节点；
    任务执行回执可以读取这些状态节点并与来源需求目标状态比较；
    仍不得由安全评估方法直接调用回写需求满足、关闭需求或结算安全值。

已补齐：
    风险状态明确状态；
    当前场景安全度明确状态；
    当前场景最低安全度存在明确状态；
    当前场景各存在安全评估明确状态；
    当前场景持续观察需求明确状态；
    负向影响处置需求明确状态；
    当前场景安全判定状态。

实际验证：
    Debug x64 构建通过；
    `.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；
    最新日志 `日志/鱼巢_run_20260530.低值g` 显示 `安全评估本能方法结果` 中 `可比较评估状态写入数量=7`；
    同轮控制台 `C6/C7安全评估闭环` 仍显示结果满足=1、当前场景评估安全值候选=9223372036854775807、当前场景直接伤害可能性候选=0、当前场景安全判定状态=2、当前场景安全判定来源=5、默认良好允许提交=1、不结算安全值=1。

复验更新：
    真实来源需求命中来源为“遗传”的现有因果信息已完成当前轮复验；`.\x64\Debug\鱼巢.exe --demand-tree-growth-summary` 退出码 0，摘要显示 `派生来源因果=12`；
    学习缺口派生需求的目标宿主判定已改为优先读取目标状态主体，最新摘要显示 `目标绑定: 完整=46 | 异常=0`；
    `--auto-observe-stable` 仍证明外设桥、安全评估默认良好链和“不结算安全值”边界未回退。
```

全程禁止：

```text
外设报告直接创建存在；
验证方法直接写场景；
线程裸写存在；
材料丢失后仍验证通过；
句柄解析失败但生成确认存在；
控制面板显示被当作业务验收；
硬件失败被扩大解释成业务逻辑失败；
安全值结算。
```

## 6. 验收标准

```text
1. Debug x64 构建通过。
2. 正式需求能驱动任务管理创建相机外设等待项 / 订阅项。
3. D455 外设线程报告能被任务管理按等待项领走。
4. 任务管理只生成回执 / 派生需求消息 / 观察事实承接请求，不直接写世界树。
5. 提交入口裁决后，观察报告只能先转成当前观察帧、观察像素簇和存在候选；已确认观察存在必须经过 C5-3a 验证、C5-3b 确认和 C5-3c 提交入账。
6. 当前场景存在集合明确状态能追溯到相机报告、自我验证动作、确认动作和提交动作动态。
7. `安全_评估当前场景安全性` 能读取该存在集合并输出安全评估结果场景，且关键明确项能作为可比较状态节点供任务执行回执读取。
8. 无负向因果证据时，安全评估默认良好并提交 `I64MAX` 场景评估安全值；只有被动获得负向因果证据后才进入低安全后续链。
9. 全链路不直接写自我安全值 / 服务值。
10. 日志能串起：外设报告 -> 任务管理承接 -> 提交入口裁决 -> 场景事实 -> 安全评估。
```

## 7. 下一步

```text
本计划第一条链路已闭合：
    D455 报告
    -> 任务管理承接
    -> C5-3a 验证候选
    -> C5-3b 确认方案
    -> C5-3c 提交观察存在事实
    -> C4.7 已确认轮廓分区
    -> C4.6-2 内部世界递归材料
    -> C6/C7 安全评估消费与结果提交验证
    -> R5g 因果被动获取 / 无负向因果默认良好
    -> R5h 安全评估结果可比较状态补齐。

后续转入第二段安全闭环的剩余部分：
    机制侧已补任务筹办按来源为“遗传”的现有因果信息反推必要状态的通道；
    任务筹办发现“要满足 A，必须先有 B”且 B 未满足时，只输出 B 的缺口报告 / 子需求建议；
    由 `提交_生成缺口子需求` 或统一需求树更新提交入口把 B 正式挂入需求树，并把来源因果键写入派生归因缓存；
    真实来源需求命中来源为“遗传”的现有因果信息已完成当前轮复验：`--demand-tree-growth-summary` 显示 `派生来源因果=12`，证明需求树自然链已有来源因果追溯样本；后续只在出现新的真实来源需求或负向因果证据时继续扩展提交回写证据；
    真实自我关键特征负向变化 / 安全影响因果明确只作为后续被动更新风险的样本。

关键约束：
    C4.6-2 只能消费已确认存在及其已提交 ROI / 掩码 / 局部材料原始事实；
    内部世界子候选仍须经过验证、确认方案和提交入口，不能由局部识别材料直接入账；
    安全评估只消费已确认存在集合，不消费未确认候选；
    因果信息被动获取；无负向因果证据时默认良好，不强求补因果链；
    遗传因果信息只给筹办提供冷启动因果路径，不直接生成需求、任务或安全结算；
    诊断入口不能伪造来源需求或直接调用回写需求满足；
    安全值结算仍需因果归因和价值结算闸门。
```
