# 追根因：观察材料练习过滤后访问冲突

当前弹窗 / 崩溃：

```text
真实窗口 `日志/鱼巢_run_20260612_observe_material_practice_filter_70s_console.log`
退出码=-1073741819，即 0xC0000005 访问冲突。
stderr 为空，当前尚未捕获崩溃栈。
```

最高优先级原因：

```text
未知，必须用 ProcDump Crash 捕获栈。
当前不能把访问冲突归因于 D455 缺报告或练习过滤本身。
```

当前层级：

```text
L0 现象层：真实窗口进程访问冲突退出。
```

同层疑点记录：

```text
A 主链根因候选：本轮练习过滤后运行路径触发空指针 / 悬空指针访问，待 dump 栈定位。
B 阻塞前置问题：最新日志显示 6118 / 6125 / 6126 / 6104 / 6105 / 6106 仍被通用练习试运行并因缺真实观察材料失败；这不解释访问冲突本身，但影响验收窗口。
C 旁支问题：6127 / 6145 缺 D455 逐簇识别报告仍应按等待链追踪，不纳入本轮练习过滤。
```

已确认事实：

```text
Debug x64 构建通过：日志/鱼巢_build_20260612_observe_material_practice_filter.log，退出码=0。
真实窗口起点：日志/鱼巢_run_20260612.低值g line 351170。
6115 / 6116 / 6117 / 6103 / 6144 已加入通用练习目标排除集。
ProcDump Crash 复现：日志/dump/run_Crash_20260612_141956，进程退出码=0，未生成 dump；访问冲突暂未稳定复现。
最新窗口显示 6118 / 6125 / 6126、6104 / 6105 / 6106 仍被通用练习试运行，已追加为同类过滤候选。
```

已改文件：

```text
自检线程模块.impl.cpp
任务模块.筹办.ixx
自我动作实现模块.ixx
实施记录/20260612_计划索引默认主线自然样本_Codex断点清单.md
```

下一条命令：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验收证据：

```text
访问冲突必须复现并定位栈；
修复后 Debug x64 构建通过；
真实窗口退出码 0；
不得出现弹窗错误提示、[FTL]、abort 或 Debug Error。
```

禁止宣称：

```text
不宣称学习子任务完成。
不宣称观察材料、D455 报告、已确认观察存在已满足。
不宣称内部循环完成。
```

## 2026-06-12 复验结论

```text
ProcDump Crash 复现：
    日志/dump/run_Crash_20260612_141956
    进程退出码=0，未生成 dump。

后续重定向真实窗口：
    round3 START_LINE=366703，70 秒控制台模式未退出，已抓 Snapshot：日志/dump/attach_Snapshot_20260612_142943。
    round4 START_LINE=378570，70 秒控制台模式未退出，已抓 Snapshot：日志/dump/attach_Snapshot_20260612_143444。
    round5 START_LINE=387428，70 秒控制台模式未退出，已抓 Snapshot：日志/dump/attach_Snapshot_20260612_143844。

共同结果：
    stderr 为空；
    弹窗错误提示=0；
    [FTL]=0；
    abort=0；
    Debug Error=0；
    访问冲突=0。

判定：
    初始 0xC0000005 未稳定复现，当前不再作为计划索引最高优先阻塞。
    本文件保留为旁支追根因记录；若后续再次出现访问冲突，需重新用 ProcDump Crash 捕获栈。
```

转出后的当前主线：

```text
通用练习目标过滤已收敛。
下一步转入 `6127 / 6145 / 6150 执行前供料预检`：
    真实执行链目前会先调用 6127 / 6145，记录缺 D455 逐簇识别报告 [ERR]；
随后任务管理才创建外设观察等待项并完成报告承接。
目标是复用既有供料等待结构，在执行前进入等待，不能通过降低错误级别或吞掉失败完成。
```

## 2026-06-12 供料预检复验补充

```text
供料预检切片已闭合：
    6127 / 6145 均补充 `外设观察报告ID` 必需形参；
    最新真实窗口 START_LINE=393610；
    `[ERR]`=0，弹窗错误提示=0，[FTL]=0，abort=0，Debug Error=0，访问冲突=0；
    6127 / 6145 缺 D455 逐簇识别报告时先进入任务等待 / 外设等待项；
    报告回填后能恢复执行并成功。

本追根因记录继续作为旁支记录保留。
若后续再次出现 0xC0000005 / Debug Error / abort，必须重新用 ProcDump Crash 捕获栈，不沿用本次未复现结论。
```
