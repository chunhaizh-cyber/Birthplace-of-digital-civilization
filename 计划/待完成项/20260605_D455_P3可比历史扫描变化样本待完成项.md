# D455 P3 可比历史扫描变化样本待完成项

日期：2026-06-05

来源：

```text
计划/20260604_外设三分法真实D455样本实施切片计划_v0.1.md
P3 已识别区域扫描变化样本。
```

## 已完成

```text
1. 已新增 D455扫描变化样本卡 摘要函数；
2. 已将 D455 自然扫描报告入队路径接入 扫描变化样本卡 日志；
3. 已将 --d455-capture-once 真实单帧命令接入 单帧扫描变化样本卡 输出；
4. 已修正首帧扫描口径：
   - 上一帧ID=0；
   - 状态=首帧基准；
   - 新增簇=0；
   - 事实口径=首次扫描仅可形成发现存在或发现特征候选。
```

验证：

```text
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
退出码=0；仅 LNK4075 既有警告。

.\x64\Debug\鱼巢.exe --d455-capture-once
退出码=0；
D455扫描变化样本卡 已输出；
状态=首帧基准；
新增簇=0；
首帧基准=1。
```

证据：

```text
实施记录/20260605_D455扫描变化样本卡_P3实施记录.md
日志/20260605_D455_P3_scan_sample_console_r2.txt
日志/鱼巢_run_20260605.低值g:6441
```

## 当前缺少条件

```text
.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000
退出码=0；
但本轮 20:20 自检窗口没有新的 扫描变化报告入队；
也没有新的 扫描变化样本卡。
```

堵点证据：

```text
日志/鱼巢_run_20260605.低值g:5642 起连续出现：
任务管理界面线程/外设观察等待项创建延后 |
依据=当前方法供料要求 |
入队阶段按目标特征名映射=0 |
直接写需求树=0 |
直接写世界树=0
```

## 恢复条件

满足任一条件后恢复：

```text
1. 自然任务链真实创建 已识别场景扫描变化 外设观察等待项，
   且 D455 线程至少采集两帧，可生成 状态=有可比历史 的扫描变化样本卡；
2. 用户明确允许新增独立真实双帧 D455 扫描样本命令，
   该命令只采集真实 D455 两帧，不构造假帧、不构造假等待项、不生成动作动态。
```

## 恢复后验收

```text
必须看到：
    D455扫描变化样本卡；
    状态=有可比历史；
    当前帧 和 上一帧 均非 0；
    新增簇 / 消失簇 / 变化簇 至少可回查；
    只读边界=扫描变化材料,不确认全场景明确,不写安全结论。

不得出现：
    首次扫描写成变化结论；
    D455 材料直接写安全值 / 服务值；
    伪造样本；
    伪造动作动态。
```
