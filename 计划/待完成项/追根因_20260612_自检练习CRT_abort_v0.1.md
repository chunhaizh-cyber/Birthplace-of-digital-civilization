# 追根因：自检练习 CRT abort v0.1

日期：2026-06-12

## 2026-06-12 15:23 后复核

```text
触发：
    用户再次给出 Microsoft Visual C++ Runtime Library / Debug Error! / abort() has been called 截图。
当前现场：
    PowerShell `Get-Process`、WMI CommandLine、窗口标题枚举均未发现正在运行或可附加的 `鱼巢.exe`。
    只发现 Visual Studio 主窗口 `鱼巢 - 基础信息类.cpp - Microsoft Visual Studio`。
最新业务日志：
    `日志/鱼巢_run_20260612.低值g` 最新程序启动线仍为 line 472398。
    从 line 472398 起未检出 `[ERR]`、`[FTL]`、`弹窗错误提示`、`abort`、`Debug Error`、`访问冲突` 或正常停止标记。
最新 dump：
    `日志/dump/attach_Snapshot_20260612_151622/鱼巢.exe_260612_151622.dmp` 是手工 snapshot；
    CDB 输出 `Minidump doesn't have an exception context`，主线程在控制台读取输入，不是 abort 现场。
当前判断：
    不能把截图当成已闭合验证通过；但当前没有可抓取现场。
    已用 ProcDump Crash 受控真实运行复核：
        `日志/dump/run_Crash_20260612_153055/procdump_run_console.log`
        进程 PID=41828，Exit Code=0x00000000，ProcDumpExitCode=0，Dump count not reached。
        未生成 dump。
    最新日志窗口 START_LINE=489878；
        `[ERR]`=0，`[FTL]`=0，`弹窗错误提示`=0，`abort/Debug Error/Assertion/_CrtIsValidHeapPointer`=0，
        `访问冲突 / Access violation`=0；
        line 493627 `程序退出/守卫 | 自我线程已停止`。
    本轮复核未复现 CRT abort；若用户侧再次出现同弹窗，必须在弹窗仍在时抓 Snapshot dump。
    当前恢复计划索引父层承接主线。
```

## 当前弹窗

```text
标题：Microsoft Visual C++ Runtime Library / Debug Error!
正文锚点：abort() has been called。
触发路径：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=70000
运行日志：
    日志/鱼巢_run_20260612_external_safety_practice_filter_70s_console.log
    日志/鱼巢_run_20260612_external_safety_practice_filter_70s_error.log
业务日志：
    日志/鱼巢_run_20260612.低值g
窗口起点：
    line 275499，2026-06-12 13:23:14.618 程序启动。
退出码：
    WATCHDOG_TIMEOUT=0；EXIT_CODE=-2147483645。
```

## 最高优先级原因

```text
CRT abort 属于 Debug Runtime 直接弹窗，不是项目 `项目弹窗错误提示` 包装日志。
当前不能把外设 / 安全过滤切片判为通过；必须先拿到 abort 调用栈并定位直接调用点。
```

## 当前层级

```text
L0 现象层：CRT Debug Error / abort() has been called，进程异常退出。
L1 直接生成层：std::abort / std::terminate / CRT invalid parameter / 方法主信息类 abort 调用点。
```

## 同层疑点记录

```text
L1-A 候选：方法主信息类.h 中显式 std::abort()。
证据：静态搜索唯一显式 std::abort 调用点在 方法主信息类.h。
反证 / 未证：尚无 dump 栈证明本次命中该调用点。
状态：需下钻。

L1-B 候选：项目弹窗错误提示内部异常后 std::terminate。
证据：日志模块.ixx 存在 MessageBox 失败异常路径；本次业务日志没有项目弹窗记录。
反证 / 未证：未见 `弹窗错误提示`；需要 dump 栈排除。
状态：候选。

L1-C 候选：Debug CRT invalid parameter / 未捕获异常转 terminate。
证据：标准输出和 stderr 均为空，业务日志未记录 [FTL]。
反证 / 未证：需要 dump 栈或 first-chance dump。
状态：候选。
```

## 选中下钻分支

```text
先用 ProcDump 复现并抓 crash / first-chance dump；
若 CDB 可用，导出 !analyze -v、.ecxr、kpn、~* kpn；
若 CDB 不可用，至少记录 dump 路径和 ProcDump 控制台输出，再转为可读日志 / 静态调用点追踪。
```

## 主链判断

```text
本次外设 / 安全方法过滤的部分预测已经从日志上出现正向证据：
    6134 / 6135 / 6136 已扫描为报告型，练习目标=否。

但真实运行未正常退出，且 6122 / 6124 等外设方法仍进入通用练习试运行并产生 [ERR]。
当前只能判断“过滤切片触发了新的 abort 验证阻塞”，不能宣称切片闭合。

2026-06-12 13:25:53 ProcDump Crash 复现窗口中 abort 未复现，进程退出码 0，未生成 dump。
因此当前主链状态调整为：abort 是一次真实出现但尚未稳定复现的阻塞；
下一步先做一次普通直接运行复核。若仍不复现，则恢复外设 / 安全练习目标边界主线；
若复现，再改用 FirstChance 或弹窗期间 Snapshot dump 抓栈。
```

## 已确认事实

```text
1. 构建通过：日志/鱼巢_build_20260612_external_safety_practice_filter.log，已成功生成，1 个 LNK4075 警告，0 个错误。
2. 本次运行控制台日志和 stderr 均为空。
3. 本次业务日志窗口起点 line 275499。
4. 本次业务日志未检出项目 `弹窗错误提示` 或 `[FTL]` 作为直接来源。
5. 本次运行进程已退出，无法对已消失进程抓 snapshot dump。
6. tools/debug-stack 下已有 ProcDump / CDB 分析脚本。
7. 首次 ProcDump 调用误把 `--self-check-log` 解析为 ExePath，未启动程序。
8. 显式 `-Arguments` 的 ProcDump Crash 窗口正常退出：`run_Crash_20260612_132553`，`Process Exit: Exit Code 0x00000000`，未生成 dump。
9. ProcDump 窗口业务日志起点 line 280005；`弹窗错误提示=0`、`[FTL]=0`、`abort/Debug Error 文本=0`、`程序退出/守卫 | 自我线程已停止=1`。
10. ProcDump 窗口中 6100 / 6101 / 6102、6119 / 6120 / 6121、6134 / 6135 / 6136 未进入练习预检或运行账。
11. ProcDump 窗口残余练习目标为 6122 / 6123 / 6124：
    line 281693 / 281820 / 281786 进入 `候选方法练习目标参数预检`；
    line 282605 / 282571 / 282623 进入 `自我动作/练习目标方法运行账`。
12. ProcDump 窗口残余 [ERR] 仍集中在缺当前观察帧、缺 D455 逐簇识别报告、6122 / 6123 / 6124 占位或缺真实外设控制接口。
13. 直接复跑期间出现第二类 CRT 弹窗：
    Microsoft Visual C++ Runtime Library / Debug Assertion Failed!
    File=minkernel\crts\ucrt\src\appcrt\heap\debug_heap.cpp
    Line=904
    Expression=_CrtIsValidHeapPointer(block)。
14. 弹窗期间已抓取 Snapshot dump：
    日志/dump/attach_Snapshot_20260612_133224/鱼巢.exe_260612_133225.dmp。
15. CDB 栈文本：
    日志/dump/attach_Snapshot_20260612_133224/鱼巢.exe_260612_133225.stacks.txt。
16. 关键栈：
    Thread 4：
        std::basic_string::~basic_string
        -> 存在类.cpp:22 私有_语素入口相同
        -> 存在类::查找子特征_按类型
        -> 存在类::根据概念扩充实例记忆
        -> 世界树类::添加实例概念
        -> 任务类::取或创建_任务虚拟存在
        -> 任务类::读取任务方法候选虚拟存在集
        -> 任务管理界面线程/同步工作回执方法到任务壳。
    Thread 15：
        存在类::获取子存在
        -> 世界树类::取或创建自我存在
        -> 自我类::确保自我存在
        -> 任务模块.筹办::解析任务目标方法。
17. `读取任务方法候选虚拟存在集` 名义是读取，却调用 `取或创建_任务虚拟存在`；
    `取或创建_任务虚拟存在` 每次都会补齐任务虚拟存在概念集，进入 `添加实例概念 / 根据概念扩充实例记忆 / 查找子特征 / 创建特征` 写路径。
18. 修复 B 后 Debug x64 构建通过：
    日志/鱼巢_build_20260612_task_candidate_read_no_ensure.log；
    退出码=0；已成功生成；0 个错误；保留既有 LNK4075 警告。
19. 修复 B 后普通直接 70 秒窗口未超时，但退出码=-1073741819，即 0xC0000005 访问冲突；
    控制台日志和 stderr 为空，仍无项目 `弹窗错误提示` 或 `[FTL]`。
20. 修复 B 后 ProcDump Crash 复现窗口：
    日志/dump/run_Crash_20260612_134957；
    Process Exit Code 0x00000000，未生成 dump。
    因此当前剩余异常为时序型访问冲突；下一步改用 Windows LocalDumps 抓直接运行崩溃 dump。
```

## 已排除

```text
1. 暂排除“项目包装弹窗直接给出业务文本”：最新窗口没有 `弹窗错误提示`。
2. 暂排除“构建失败导致无法运行”：Debug x64 构建退出码为 0。
3. 暂排除“过滤 6100 / 6101 / 6102、6119 / 6120 / 6121、6134 / 6135 / 6136 未生效”：ProcDump 窗口中这些目标不再进入预检或运行账。
4. 暂排除“基础信息链表公开添加子节点完全无锁”：链表模板公开添加 / 删除有写锁，枚举有读锁。
5. 暂不把语素入口本身判为根因：当前更早违反点是只读候选读取隐式进入任务虚拟存在创建 / 概念补齐写路径。
```

## 旁支问题池

```text
C1. 最新窗口中 6122 / 6124 等外设补观察方法仍作为通用练习目标进入运行账并返回占位 / 条件不足。
C2. 最新窗口中自我_练习本身作为目标方法时出现多次 `目标本能可练习=0 / 可试运行=0` 运行账，需在 abort 消除后判定是否为独立边界。
C3. 最新窗口中 6127 / 6128 / 6145 等外设识别 / 验证链仍因缺 D455 逐簇识别报告失败；是否过滤需另按真实材料边界判定，不能在本切片一次性扩大。
```

## 已改文件

```text
计划/追根因_20260612_自检练习CRT_abort_v0.1.md
计划/计划索引.md
任务类.cpp
```

## 已运行命令

```text
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=70000
rg -n "程序启动|弹窗错误提示|\[FTL\]|abort|Debug Error" 日志\鱼巢_run_20260612.低值g
rg -n "候选方法练习目标参数预检|自我动作/练习目标方法运行账|练习目标=否|6100|6101|6102|6119|6120|6121|6134|6135|6136" 日志\鱼巢_run_20260612.低值g
rg -n "\[ERR\]" 日志\鱼巢_run_20260612.低值g
rg -n "abort\(|std::abort|terminate\(|assert\(|_ASSERTE|Debug Error|invalid parameter|项目弹窗错误提示|MessageBoxW" -S --glob '!x64/**' --glob '!Debug/**' --glob '!.git/**'
.\tools\debug-stack\Invoke-FishnestProcDump.ps1 -Mode Crash -- --self-check-log --self-check-log-wait-ms=70000
.\tools\debug-stack\Invoke-FishnestProcDump.ps1 -Mode Crash -Arguments '--self-check-log','--self-check-log-wait-ms=70000'
winget install Microsoft.WinDbg
cdb -z 日志/dump/attach_Snapshot_20260612_133224/鱼巢.exe_260612_133225.dmp -lines -y x64/Debug -c "lm m 鱼巢; ~* kpn; q"
git diff --check -- 任务类.cpp 计划/追根因_20260612_自检练习CRT_abort_v0.1.md
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=70000
.\tools\debug-stack\Invoke-FishnestProcDump.ps1 -Mode Crash -Arguments '--self-check-log','--self-check-log-wait-ms=70000'
启用 HKCU:\Software\Microsoft\Windows\Windows Error Reporting\LocalDumps\鱼巢.exe
.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=70000
受控隐藏窗口：.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=70000，80 秒后进程已退出，EXIT_CODE=0
受控可见窗口：.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=70000，80 秒后进程已退出，EXIT_CODE=0
```

## 下一条命令

```text
本轮复核已闭合；恢复 `计划/计划索引.md` 当前主动计划：
    练习达成后父层承接 / 父任务继续推进验证。
若后续再次出现同弹窗：
    在弹窗仍在时优先执行：
        .\tools\debug-stack\Capture-ProcessDump.ps1 -Mode Snapshot -ProcessName 鱼巢
    再分析新 dump。
```

## 当前层级与下钻分支

```text
当前层级：
    L2 本地事实层 / L4 生命周期层。

同层疑点：
    A. 任务虚拟存在概念 / 特征补齐期间同一存在主信息或特征子链被重复写入。
       证据：heap assertion 栈落在 `根据概念扩充实例记忆 -> 查找子特征_按类型`。
       状态：主链候选。
    B. `读取任务方法候选虚拟存在集` 是只读 API，却隐式创建 / 补齐任务虚拟存在。
       证据：栈中读取入口直接进入 `取或创建_任务虚拟存在 -> 补齐任务虚拟存在概念集`。
       状态：第一分歧点，先修复。
    C. 语素入口节点自身主键损坏。
       证据：CRT 断在 `语素入口相同` 的临时 string 析构。
       反证：语素链表节点长期持有，尚无删除证据；更早存在只读入口写路径。
       状态：旁支保留，若修复 B 后仍复现再查。

选中下钻分支：
    B 已实施并复验闭合：
        `读取任务方法候选虚拟存在集` 只读取任务主信息中已存在的 `任务虚拟存在` 引用；
        不再调用 `取或创建_任务虚拟存在`。
        `取或创建_任务虚拟存在` 只在新建任务虚拟存在后自动补齐概念集；
        既有任务虚拟存在的再次补齐必须走显式 `补齐任务虚拟存在概念集`。
    复验：
        修复后 ProcDump Crash 窗口退出码 0 且未生成 dump；
        受控隐藏 70 秒窗口退出码 0；
        受控可见 70 秒窗口退出码 0；
        最新业务日志窗口完成 `程序退出/守卫 | 自我线程已停止`。
    剩余旁支：
        A / C 暂不继续下钻，除非后续真实窗口再次出现 CRT heap assertion / abort。
```

## 验收证据

```text
1. 拿到 abort 调用栈或等价的直接调用点证据。
2. 定位第一个违反数据契约、生命周期或方法规格预期的上游点。
3. 最小修复后 Debug x64 构建通过。
4. 真实 70 秒窗口退出码 0；`abort=0`、`[FTL]=0`、`弹窗错误提示=0`。
5. 不通过吞掉 abort、降级弹窗、屏蔽错误或伪造外设 / 安全材料完成验证。
```

## 禁止宣称

```text
1. 不宣称外设 / 安全练习目标过滤切片已闭合。
2. 不宣称学习子任务完成。
3. 不宣称条件结果对已建立。
4. 不宣称需求满足、安全值 / 服务值入账、自我苏醒完成或内部循环全部完成。
```

## 2026-06-12 16:06 复开：闭环视图读取子特征 abort

```text
现象：
    用户再次给出 Microsoft Visual C++ Runtime Library `Debug Error! abort() has been called`。
    后续截图出现 `_CrtIsValidHeapPointer(block)` assertion。

现场 dump：
    Snapshot dump：日志/dump/attach_Snapshot_20260612_155908/鱼巢.exe_260612_155908.dmp
    all-thread stack：日志/dump/attach_Snapshot_20260612_155908/鱼巢.exe_260612_155908.allthreads.cdb.txt

当前栈：
    abort / terminate 上游为 `VCRUNTIME140D!__RTDynamicCast`；
    业务栈为：
        基础信息类::取主信息<特征节点主信息类>
        特征类::取特征主信息
        特征类::查找子特征_按类型
        世界树类::读取特征_指针
        本能动作管理模块::本能动作读指针特征
        本能动作管理模块::本能动作补读闭环指针事实
        本能动作管理模块::读取本能方法运行闭环视图
        任务模块.管理工作线程::私有_执行输出需要派生原子需求

根因判断：
    `查找子特征_按类型` 先在读锁内枚举子特征指针，返回后再读主信息；
    工作线程运行期间该裸指针可能已被删除 / 更新，导致 dynamic_cast 触碰无效主信息并由 CRT 弹窗终止。
    历史上曾有全局“子链防崩保护”，后已按要求移除；本次不恢复全局防崩接口，只修正本栈上的按类型读取协议。

已改文件：
    特征类.h
    特征类.cpp
    世界树类.cpp
    本能动作管理类.ixx

修正：
    `特征类::查找子特征_按类型 / 按名称` 改为持基础信息读锁完成子节点筛选和主信息读取。
    新增 `特征类::读取子特征值_按类型`，在同一读锁内从宿主+特征类型直接复制特征值。
    `世界树类::读取特征快照 / I64 / 指针 / VecI64(宿主, 特征类型)` 改走按类型快照，不再跨锁返回子特征指针后复读。
    `本能动作补读闭环指针事实` 在读取前校验闭环宿主是否仍属于当前世界树；无效时记录 `本能动作管理/闭环视图无效基础宿主` 并拒绝读取。

构建前预测：
    Debug x64 构建应通过；若仅有 LNK4075 不作为失败。
    真实 90 秒 ProcDump Crash 窗口不应再在上述 `取主信息<特征节点主信息类>` 栈 abort。
    若上游仍传入已脱离世界树的闭环宿主，应有 `本能动作管理/闭环视图无效基础宿主` 日志，而不是 CRT 弹窗。
    验证仍以 `[ERR]`、`[FTL]`、`弹窗错误提示`、`abort / Debug Error / Assertion`、dump 生成情况和父层承接日志为准。
```
