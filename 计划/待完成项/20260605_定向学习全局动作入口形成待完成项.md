# 定向学习全局动作入口形成待完成项

日期：2026-06-05

## 来源

来自 `计划/20260605_学习函数量化缺口与方法状态派生纠偏计划_v0.1.md` 的 P5c 真实日志验证。

P5c 已验证：

```text
自检学习维护目标已收束为 方法状态>=好用；
任务筹办不再直接调用 自我_提交方法可执行入口；
方法缺真实单步入口时可以暴露 方法可执行入口数量(16x) 缺口；
16x 缺口进一步需要定向学习形成候选动作 / 候选入口。
```

## 用户确认口径

```text
定向学习的逻辑是：
    全局查找符合目标的动作动态；
    或提炼动作；
    或模仿。

这部分以后再实现。
```

## 当前边界

```text
自我_提交方法可执行入口(6081) 只负责提交已经明确的本能动作入口；
它不负责在未知动作线索中搜索、提炼或模仿动作；
入口候选未明确时，不得强行调用 6081；
任务筹办不得恢复现场直调 6081。
```

## 恢复条件

```text
1. 用户允许进入定向学习实现；
2. 先制定独立小切片方案；
3. 明确全局动作动态查找范围、动作提炼输入输出、模仿输入输出；
4. 明确候选入口如何交给 自我_提交方法可执行入口(6081)；
5. 验证仍必须走预测 -> 真实运行 -> 日志 / 状态快照 / 动作动态 / 错误日志核对。
```

## 禁止项

```text
不得把定向学习未实现解释成 P5c 代码失败；
不得在没有明确候选本能 ID 时写 方法可执行入口数量=1；
不得伪造动作动态、模拟样本或夹具样本；
不得把任务筹办 / 任务执行改成本能方法；
不得把 自我_提交方法可执行入口 当作动作搜索方法。
```
