# 20260527_R1本能方法_双目相机取得当前观察特征帧实施计划 v0.1

## 0. 归属

```text
父计划：计划/20260526_基础观察事实与基础风险判断分层实施计划_v0.1.md
父规范：040005 外设观察特征与自我场景认知本能方法分层规范
父规范：040006 枚举型实例特征值合法来源规范
父规范：050003 基础观察事实可用与基础风险判断分层规范
方法名：双目相机_取得当前观察特征帧
方法分类：外设使用类本能方法
当前状态：已完成。方法登记、动作派发、输出状态、动作动态目标特征和日志证据已复核通过。
```

## 1. 目标

```text
通过双目相机取得当前观察特征帧；
写入 自我所在场景.当前观察特征帧取得状态 等于 已取得；
提供后续 自我_建立观察帧存在 的输入证据。
```

该方法只说明“相机给出了当前帧观察特征”，不得输出候选存在、已确认观察存在、目标可见性、在场状态或基础风险判断状态。

## 2. 输入与输出

输入：

```text
目标外设 = 双目相机
目标宿主 = 自我所在场景 / 当前观察特征帧承接宿主
当前调用动作动态
外设可用状态
外设运行可用性
```

输出：

```text
当前观察特征帧取得状态 = 已取得 / 未取得 / 条件不足
帧宽度
帧高度
彩色帧号
深度帧号
预期像素数量
采集时间
来源外设
动作动态 / 来源动态证据
失败原因摘要
```

## 3. 允许修改范围

```text
自我动作实现.外设模块.ixx
自我动作实现模块.ixx
基础数据类型.h
基础数据类型.ixx
控制面板类.ixx
控制面板类.cpp
实施记录/20260526_基础观察事实与基础风险判断分层_Codex断点清单.md
```

若发现方法登记缺失，才允许补 `本能方法类.h` / `本能方法类.cpp` 中该方法的登记，不得顺手登记其他方法。

## 4. 禁止越界

```text
不得写入 当前观察帧.基础观察事实可用状态 等于 可用；
不得写入 自我所在场景.基础风险判断状态 等于 已明确；
不得把相机返回候选解释为自我已确认观察存在；
不得新增模拟样本、伪造入口或 --*-check 回归入口。
```

## 5. 实施步骤

```text
D1 复核方法登记、动作派发和外设适配器调用入口。
D2 复核帧宽、高、帧号、采集时间、预期像素数量和来源外设写入。
D3 写入或补齐 当前观察特征帧取得状态 的合法生产者证据。
D4 日志输出输入证据、输出宿主、输出特征、动作动态和失败原因。
D5 缺外设可用性或运行可用性时返回条件不足，不写 已取得。
```

## 6. 验收

```text
Debug x64 构建通过；
真实运行日志中能看到 双目相机_取得当前观察特征帧 的动作动态；
日志中 当前观察特征帧取得状态 只由该方法或其动作结果生产；
缺相机、缺运行可用性或采集失败时不写 已取得；
最新运行段没有 基础风险判断状态=已明确。
```

## 7. 执行记录

```text
执行日期：2026-05-27
实现结果：已将方法结果能力和动作动态目标收束到 当前观察特征帧取得状态。
修改文件：自我动作实现.外设模块.ixx
预测文件：日志/20260527_R1_双目相机取得当前观察特征帧_验证预测.txt
构建验证：Debug x64 msbuild 通过，0 错误，1 个 LNK4075 增量链接警告。
真实入口：.\x64\Debug\鱼巢.exe --self-scene-html
最新日志：日志/鱼巢_run_20260527.低值g
关键证据：
    方法=双目相机_取得当前观察特征帧
    成功=1
    输出特征=当前观察特征帧取得状态
    动作动态=1eV
    宽度=640
    高度=480
    深度帧号=3
    彩色帧号=40
    预期像素数量=307200
    像素特征数量=307200
    当前观察特征帧取得状态=1
越界检查：
    基础风险判断状态=0
    基础风险判断状态=已明确=0
    直接写需求树=1=0
    安全值结算=0
    服务值结算=0
结论：本方法计划完成，只证明当前观察特征帧已取得；不证明基础观察事实可用总目标，也不推进基础风险判断。
下一项：计划/20260527_R1本能方法_自我建立观察帧存在实施计划_v0.1.md
```
