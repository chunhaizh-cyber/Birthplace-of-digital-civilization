# S8e D455 缺外设报告执行失败快路径复核方案 v0.1

日期：2026-06-08

状态：已闭合；承接 S2f 执行类慢样本归属结论，闭合证据见第 6 节。

## 1. 触发依据

```text
S2f 已闭合：
    `任务工作项派发执行耗时` 的 4 条执行类慢样本不归属任务状态提交或旧派发锁。
    方法 1i 对应本能 ID 6145 自我_识别外设观察材料。
    方法 1x 对应本能 ID 6150 提交_当前观察范围可观测单位存在对应事实。

真实日志证据：
    日志=日志/鱼巢_run_20260608.低值g
    窗口起点行=97058
    执行样本=工作项 37/39/40/41
    执行耗时=305920us, 491774us, 602398us, 588738us
    外设事实日志包含：
        观察材料等级=不合格
        提交质量依据=缺外设观察报告
        可进入提交入口组织=0
        不写需求满足=1
        不写安全值=1
```

## 2. 目标

```text
第一目标：
    拆清 6145 / 6150 在缺外设报告、材料不合格时的失败路径耗时。

第二目标：
    如果缺报告时仍重复写目标方法实际结果、重复组织提交入口或重复派生同类执行缺失，
    优先做缺报告失败路径快退或幂等跳过。

第三目标：
    若缺报告失败路径已经只保留必须动作动态和缺口回执，
    不强行改外设链路，记录等待真实 D455 报告条件。
```

## 3. 硬边界

```text
不得把 D455 条件不足、缺报告、等待或未满足写成完成 / 满足。
不得把外设报告、材料页、队列项或目标观察约束当世界真值。
不得绕过任务状态提交，不删除任务状态动作动态。
不得把外部变化伪装成自我方法动作动态。
不得新增 mock、fixture、伪入口、伪动作动态或 `--*-check` 回归入口。
```

## 4. 允许修改

```text
自我动作实现.外设模块.ixx
任务模块.管理界面线程.impl.cpp
计划/计划索引.md
计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
计划/20260608_S8e_D455缺外设报告执行失败快路径复核方案_v0.1.md
实施记录/20260608_S8e_D455缺外设报告执行失败快路径复核_Codex断点清单.md

若必须修改其他文件，先补充本方案并说明原因。
```

## 5. 执行步骤

```text
1. 写 S8e 断点清单和预测。
2. 读取 6145 / 6150 本能方法实现，定位缺报告 / 材料不合格分支。
3. 复核现有日志是否足以拆分失败路径耗时；不足时只补最小阶段日志。
4. 若缺报告分支可安全快退，瘦身该分支，保留失败回执、动作动态边界和派生缺口证据。
5. 构建 Debug x64，真实运行 self-check-log，按最新窗口核对预测。
```

## 6. 闭合证据

```text
已落实：
    6150 在缺正式外设报告 / 来源材料不可组织时进入缺报告失败快退。
    快退分支不创建 当前观察范围可观测单位存在对应事实 节点。
    快退分支不创建 可观测单位到存在映射表 节点。
    来源材料不可组织时跳过正式计数读取。
    同一进程、同一 6150 方法首节点重复运行时，跳过重复规格确认、方法头补齐和方法虚拟存在同步。
    已增加 6150 提交内部阶段日志和 6145 / 6150 自身环境内部阶段日志，均只对缺报告 / 慢样本输出。

真实验证：
    Debug x64 构建通过，仅既有 LNK4075 警告。
    真实运行命令：
        .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    最新有效窗口：
        日志/鱼巢_run_20260609.低值g 起点 29184。

性能结果：
    6150 三条提交阶段样本总耗时：
        263641us, 45364us, 49279us。
    6150 后两条重复样本自身环境：
        661us, 1289us。
    对比 S8e 早期阶段日志中的 6150 样本：
        304454us, 544958us, 555103us。

边界核对：
    对应事实节点存在=0 3 条。
    映射表节点存在=0 3 条。
    不写需求满足=1 3 条。
    不写安全值=1 3 条。
    任务状态动作动态=111 条。
    旧完整本能调用锚点、任务状态逻辑错误和 [FTL] 均为 0。
    [ERR] 1 条为 6145 缺报告失败日志，属于本切片预期失败回执。

剩余不在 S8e 内继续处理：
    6150 首次环境暖账仍约 215188us。
    6145 首次环境暖账约 268373us。
    二者主慢点均为 确保本能方法 / 同步方法节点到虚拟存在。
    历史判断为若继续优化需单独进入 S8f；2026-06-09 用户已明确相机外设四个方法先不用继续优化，因此该后续已转出当前主动队列，等待整体流程计划 P5。
```
