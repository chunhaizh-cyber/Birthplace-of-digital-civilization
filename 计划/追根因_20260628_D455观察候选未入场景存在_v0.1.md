# 追根因 20260628 D455观察候选未入场景存在 v0.1

## 当前现象

```text
D455 能生成观察材料、候选簇和稳定观察子集报告；
但自我所在场景当前直接子存在仍只有“自我”一个。
```

## 当前目标

```text
顺着“扫描需求 -> 扫描缺口 -> 观察 / 识别派生需求 -> 观察事实提交”链继续追根因并修复，
直到自我所在场景通过观察事实提交形成多个存在。
```

## 上级依据

```text
规范/禁止性规范20260611.md
规范/相机外设综合工作流程规范20260607.md
规范/外设独立控制线程与消息承接边界规范20260527.md
规范/观察像素簇与存在候选分层规范20260527.md
规范/当前场景特征值明确标准规范20260602.md
docs/工程图谱/02_外设观察事实图谱.md
docs/工程图谱/08_事实来源与写入权限图谱.md
docs/工程图谱/05_规则原子索引.md
```

## 硬边界

```text
1. 候选不能直接变成存在。
2. 外设线程只供料，不直接写世界树真值。
3. 自我应先产生扫描需求；扫描比较过程中的对应关系、材料质量、当前特征值等缺口只能作为结构化需求来源。
4. 观察 / 识别需求应由扫描缺口经需求树和任务筹办派生，不得把观察需求当根起点。
5. 观察仍是候选变为自我所在场景存在的事实闸门。
6. 候选入账必须经过需求任务链、任务管理等待项、观察事实承接、候选验证、候选确认和提交_确认观察存在事实。
7. 不得用 --d455-capture-once 的材料包证据卡冒充场景存在入账。
8. 不得把历史日志中曾经入账成功冒充当前数据库最新快照。
9. 外设观察事实承接完成后，不得反向直接确保 6146 / 6147 任务主体。
10. 稳定观察子集不得反向直接生成“稳定子集全量扫描等待项”或扫描入账派生需求。
11. 执行链条必须由任务筹办查询因果关系建立；扫描缺口后续是否派生观察、识别、跟踪或再扫描，只能由需求树、任务筹办和方法选择决定。
```

## 已删除旧反向路径

```text
代码切片：
    已删除自我线程中“观察事实承接后确保 6146 / 6147 任务主体”的机器路径。
    已删除任务管理界面线程中“稳定子集全量扫描等待项”的机器路径。

保留路径：
    任务管理按当前方法供料要求创建外设观察等待项。
    任务筹办通过因果关系选择方法。
    任务执行结果后处理读取输出结果场景 / 运行存在中的结构化缺口并上行派生需求。

验证：
    函数功能说明文档已重新生成。
    Debug x64 构建通过，0 错误，0 警告。
```

## 四方法口径

```text
当前正式四方法：
    6085 自我_观察单个空间候选并组合存在假设 = 观察
    6145 自我_识别外设观察材料 = 识别
    6146 自我_扫描当前场景 = 扫描
    6147 自我_跟踪指定存在 = 跟踪

辅助入口：
    6127 自我_验证外设观察像素簇候选 = 候选验证辅助入口。
    6150 提交_当前观察范围可观测单位存在对应事实 = 观察事实提交入口。

处理规则：
    6127 / 6150 不再作为四方法本体参与口径判断；
    但不得删除其实现，因为候选验证和事实提交链仍依赖它们。
```

## 已确认事实

```text
当前 DB 投影：
    自我初始场景主键 Ez。
    自我初始场景直接子存在数量=1。
    直接子存在只有 F0 自我。

D455 单帧采集历史证据：
    观察簇=16。
    候选存在=16。
    新候选=16。
    只读边界=材料归属和存在候选,不确认世界存在。

当前运行日志：
    D455 外设线程能生成稳定子集报告。
    可承接能力等级=空间可用。
    稳定子集簇数量=7 / 8 / 10 等。
    但同轮报告显示 活跃等待项=0、D455等待项数=0。

服务治理周期：
    候选复核=0。
    确认入账=0。
```

## 当前层级

```text
L4 生命周期层：
    自我扫描需求、扫描任务、扫描缺口派生需求、任务管理等待项 / 订阅项之间未形成闭合，
    导致 D455 稳定观察报告无法经观察 / 识别派生链被合法消费。
```

## 同层疑点记录

```text
疑点 A：扫描需求没有生成或没有入树。
状态：待查。

疑点 B：扫描需求已生成但没有任务化。
状态：待查。

疑点 C：扫描任务已创建但筹办没有选中 6146 扫描方法。
状态：待查。

疑点 D：6146 扫描未记录结构化缺口，或缺口未进入需求生成。
状态：当前最高优先级。

疑点 E：扫描缺口已生成，但没有派生观察 / 识别需求或没有任务化。
状态：待查。

疑点 F：观察 / 识别任务已推进，但没有创建 D455 等待项，或等待项曾创建但外设报告没有匹配 / 被清理。
状态：待查。

疑点 G：报告被消费，但观察事实承接请求没有上行或提交入口未调用。
状态：待查。
```

## 选中下钻分支

```text
先查 A-D：扫描需求生成、任务筹办因果查询、6146 扫描选择、扫描缺口记录和缺口派生需求链。

原因：
    最新口径要求自我先生成扫描需求；
    观察 / 识别需求必须由扫描缺口派生；
    直接从 D455 稳定材料查观察等待项会跳过扫描父需求；
    旧反向确保 6146 / 6147 和稳定子集全量扫描等待项路径已删除，不能作为恢复入口。
```

## 下一步

```text
1. 查当前自然需求树是否生成“当前观察范围稳定对应存在扫描事实 / 动态取得状态”等扫描父需求。
2. 查任务筹办是否通过因果关系从该扫描需求选择 6146 自我_扫描当前场景。
3. 查 6146 是否在缺合法观察材料、对应关系、材料质量或当前特征值时，把结构化缺口写入输出结果场景 / 运行存在。
4. 查结构化缺口是否进入需求生成，并派生观察 / 识别需求，再由任务筹办选择 6085 / 6145 / 6150 所属链路。
5. 查任务管理按当前方法供料要求创建等待项的路径是否因当前方法为空、候选数为 0 或缺口未派生而未调用。
6. 优先复用既有扫描输出结果场景、派生需求消息、等待项、观察事实承接函数和提交入口，不新增结构体。
7. 修复后真实运行，刷新数据库投影，用 DB 验证自我初始场景直接子存在数量大于 1；日志只作辅助定位。
```

## 验收证据

```text
最低验收：
    自我先生成扫描需求并选择 / 执行 6146；
    扫描缺口形成结构化需求来源；
    观察 / 识别需求由扫描缺口派生并任务化；
    D455 稳定报告被等待项消费；
    候选验证 / 确认 / 提交链出现真实日志；
    数据库当前世界树投影中，自我初始场景直接子存在数量 > 1。

辅助证据：
    外设线程日志显示 D455等待项数 > 0 或命中等待项；
    服务治理日志显示 复核候选 > 0 或 确认入账 > 0；
    控制面板运行态或当前世界显示项能看到新增场景存在。
```

## 禁止宣称

```text
不宣称视觉系统完成。
不宣称所有 D455 候选都正确入账。
不宣称当前场景全部存在明确。
不宣称安全 / 服务闭环完成。
不宣称自我苏醒或初步成熟完成。
```
