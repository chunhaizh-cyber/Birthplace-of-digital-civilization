# S1d-4u 条件候选收集内部阶段定位与瘦身方案 v0.1

日期：2026-06-08

## 1. 目标

```text
承接 S1d-4t。

S1d-4t 已闭合：
    最新运行线程 T29964 中，动态二次特征生成样本 19 条；
    最慢阶段分布为 `条件候选收集=16`、`命中回写因果实例=2`、`分步结果生成=1`；
    `条件候选收集` 平均约 225225us，已成为当前主慢点。

当前判断：
    `条件候选收集` 对应 `私有_收集条件候选`；
    当前实现先枚举场景下全部二次特征，再对每个候选与每个结果候选嵌套比较；
    嵌套比较中会重复读取节点主键。

本切片目标：
    拆分 `条件候选收集` 内部耗时；
    用结果候选指针集 / 主键集替代嵌套比较；
    保留条件候选收集、因果模板匹配和正式二次特征语义。
```

## 2. 允许修改

```text
二次特征应用模块.ixx
计划/20260607_S1d4u条件候选收集内部阶段定位与瘦身方案_v0.1.md
计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
计划/计划索引.md
实施记录/20260608_S1d4u条件候选收集_Codex断点清单.md
```

## 3. 禁止修改

```text
不删除条件候选收集。
不跳过因果模板匹配。
不改变因果模板语义。
不改变二次特征主信息结构。
不改任务状态、需求满足、价值结算或外设事实规则。
不新增模拟入口、夹具、伪造样本或 `--*-check` 回归入口。
```

## 4. 实施步骤

```text
S1d-4u-1：
    增加 `动态二次特征条件候选收集/内部阶段耗时` 慢采样。

S1d-4u-2：
    拆分 `私有_收集条件候选`：
        全部候选枚举；
        结果候选索引准备；
        候选过滤；
        条件候选追加。

S1d-4u-3：
    用 `结果候选指针集 + 结果候选主键集` 替代候选与结果候选的嵌套比较。

S1d-4u-4：
    构建 Debug x64，真实运行 `--self-check-log --self-check-log-wait-ms=10000`。
```

## 5. 验证预测

```text
构建：
    Debug x64 构建通过。

真实运行：
    退出码=0。
    日志继续出现 `动态二次特征生成/内部阶段耗时`。
    日志出现 `动态二次特征条件候选收集/内部阶段耗时`，或在 5ms 阈值下无样本但高层动态二次特征生成仍可统计。
    日志保留 `任务状态动作动态=<动态主键>`。
    不出现 `任务状态被动提交/逻辑错误`。
    不出现 `任务状态动作动态逻辑错误`。
    不出现旧路径锚点 `准备本能调用`、`执行本能方法`、`任务域本能输入包`。
```

## 6. 下一步判断

```text
如果主耗时在全部候选枚举：
    下一片再看场景子二次特征枚举或场景二次特征索引，不在本片改底层索引结构。

如果主耗时在结果候选索引准备：
    优化结果候选主键取得或指针集构造。

如果主耗时在候选过滤：
    继续收窄无效候选前置过滤条件，但不得跳过合法因果条件候选。

如果条件候选收集不再是动态二次特征生成主慢点：
    停止本切片，按最新最慢阶段切下一片。
```

## 7. 落实结果

```text
已落实：
    `二次特征应用模块.ixx` 新增 `动态二次特征条件候选收集/内部阶段耗时`；
    `私有_收集条件候选` 已拆分全部候选枚举、结果候选索引准备、候选过滤、条件候选追加；
    结果候选排除逻辑已由候选与结果候选嵌套比较改为结果候选指针集 / 主键集查找。

验证：
    `git diff --check` 通过，仅有 CRLF 工作区转换警告；
    Debug x64 构建通过；
    真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0。

本轮日志窗口：
    最新启动行为 `2026-06-08 11:24:32.582 [T24772] 程序启动`；
    `任务状态动作动态=` 63 条；
    `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包` 均为 0。

性能证据：
    `动态二次特征条件候选收集/内部阶段耗时` 11 条，平均总耗时约 5872us，最慢阶段全部为 `候选过滤`；
    `条件候选收集` 高层平均约 5054us，较 S1d-4t 的约 225225us 明显下降；
    `动态二次特征生成/内部阶段耗时` 10 条，最慢阶段分布为 `分步结果生成=7`、`命中回写因果实例=3`；
    动态二次特征生成平均总耗时约 219887us，`分步结果生成` 平均约 126897us；
    `动态二次特征分步结果/内部阶段耗时` 7 条，最慢阶段全部为 `分步签名查找创建`；
    分步结果平均总耗时约 178166us，`分步签名查找创建` 平均约 83114us，`分步值写入` 平均约 55497us，`分步语义绑定` 平均约 17707us。

结论：
    S1d-4u 已闭合；
    条件候选收集不再是动态二次特征生成主慢点；
    当前慢点转到分步结果里的 `分步签名查找创建`。

下一步：
    进入 S1d-4v：分步签名查找创建内部阶段定位 / 瘦身。
```
