# 20260630 已确认存在体素融合分解施工计划 v0.1

上级依据：

```text
规范/已确认存在体素融合与分解规范20260630.md
规范/场景体素服务与视觉先验融合规范20260625.md
规范/已确认观察存在内部结构递归细分规范20260525.md
规范/观察特征质量诊断与认知补偿规范20260525.md
详细设计/已确认存在体素融合与分解实现方案20260630.md
```

当前状态：

```text
P0 已完成并提交；P1 数据契约和消费状态已完成当前代码切片，验证见 `实施记录/20260630_已确认存在体素融合分解_P1_Codex断点清单.md`。
P2 正立方体包围盒 helper 已完成当前代码切片，验证见 `实施记录/20260630_已确认存在体素融合分解_P2_Codex断点清单.md`。
P3 体素加工工作线程骨架已完成当前代码切片，验证见 `实施记录/20260630_已确认存在体素融合分解_P3_Codex断点清单.md`。
P4 深度轮廓融合候选已完成当前代码切片，验证见 `实施记录/20260630_已确认存在体素融合分解_P4_Codex断点清单.md`。
P5 父体素提交和版本闸门已完成当前代码切片，验证见 `实施记录/20260630_已确认存在体素融合分解_P5_Codex断点清单.md`。
P6 彩色分解和深度补空间已完成当前代码切片，验证见 `实施记录/20260630_已确认存在体素融合分解_P6_Codex断点清单.md`。
P7 内部世界子存在提交已完成当前代码切片，验证见 `实施记录/20260630_已确认存在体素融合分解_P7_Codex断点清单.md`。
P8a 控制面板和场景体素只读父子体素可见性已完成当前代码切片，验证见 `实施记录/20260630_已确认存在体素融合分解_P8_Codex断点清单.md`。
P8b 当前安全切片已完成：父基础颜色已落为项目结构特征并可由控制面板只读显示；子平均颜色因缺子候选映射只能写结构化缺口，不能宣称子颜色覆盖闭合。
P8c 体素加工候选结果包诊断未实施；P8d 子轮廓候选到内部世界子存在身份映射未实施；P8e 子平均颜色正式落账和覆盖显示未实施。
P9 自然链路验证和最终实施记录等待 P8c / P8d / P8e 证据闭合后推进。
```

## 1. 目标

把已确认存在当前的粗体素模型升级为可持续融合、可分解内部结构、可递归承载子存在体素的加工链。

完成后的最小能力：

```text
新增观察体素特征值触发体素加工任务；
深度轮廓生成父体素融合候选；
彩色轮廓生成子轮廓候选；
深度轮廓补充子轮廓空间占据；
父存在和子存在均使用正立方体体素包围盒；
体素加工线程只产候选；
提交入口裁决后才写父体素或创建子存在。
```

## 2. 不解决什么

```text
不解决 SLAM。
不解决复杂姿态估计。
不解决跨相机融合。
不解决语义部件命名。
不把颜色块直接定义为子存在。
不把体素线程定义为事实写入方。
不宣称视觉系统完成、自我苏醒完成或初步成熟完成。
```

## 3. 修改范围

预计涉及：

```text
特征类.*
存在类.*
场景类.*
场景体素模块.ixx
视觉观察融合模块.ixx
外设观察报告队列.ixx
自我动作实现.外设模块.ixx
自我动作实现.扫描.ixx
控制面板类.cpp / 控制面板显示相关文件
说明书/函数功能说明文档.md
```

实际实施前必须重新按当前工作区状态确认文件头规则、函数说明文档和脏改归属。

## 4. 分步施工

### P0 规范和索引准入

目标：

```text
规范、规则原子、详细设计、流程图和施工计划落盘。
```

验收：

```text
规范目录挂载 050008。
规则原子索引包含 RULE-VOXEL-FUSION-001..008。
流程图 md/html 文件存在。
git diff --check 通过。
```

### P1 数据契约和消费状态

状态：

```text
已完成当前代码切片。
```

目标：

```text
定义新增观察体素特征值的消费口径；
定义体素加工任务和候选结果包；
确保同一观察特征值只触发一次。
```

验收：

```text
旧当前特征值不会重复生成体素加工任务。
融合结果写回不会递归触发自己。
候选结果包字段均使用项目结构类型。
```

证据：

```text
实施记录/20260630_已确认存在体素融合分解_P1_Codex断点清单.md
Debug x64 构建通过。
```

### P2 正立方体包围盒 helper

状态：

```text
已完成当前代码切片。
```

目标：

```text
实现 N*N*N 正立方体包围盒规则；
外边界空白检查；
最长实际尺寸方向贴有效区边界；
连通体局部包围盒压紧检查。
```

验收：

```text
8*64 彩色轮廓得到 64*64*64 包围盒。
完整存在最长方向最大占据格数为 N-2，不得误写成 N-1。
最外层占据体素被判定为不完整。
局部连通体整层空白会被发现。
凹陷、孔洞、非满边界面不被强行补满。
```

证据：

```text
实施记录/20260630_已确认存在体素融合分解_P2_Codex断点清单.md
Debug x64 构建通过。
```

### P3 体素加工工作线程骨架

状态：

```text
已完成当前代码切片。
```

目标：

```text
新增单工作线程或复用合适工作线程承接体素加工任务；
线程只读输入快照并输出候选结果包。
```

验收：

```text
线程不直接写存在特征。
线程不直接创建子存在。
线程不生成方法动作动态。
停止流程不影响现有线程生命周期。
```

证据：

```text
实施记录/20260630_已确认存在体素融合分解_P3_Codex断点清单.md
Debug x64 构建通过。
```

### P4 深度轮廓融合候选

状态：

```text
已完成当前代码切片。
```

目标：

```text
用深度轮廓生成父体素融合候选；
检查融合点；
无融合点时输出待融合缺口。
```

验收：

```text
有边界相同 / 相邻 / 投影重叠时生成候选。
无融合点时不写父体素。
补全深度不被提升为真实占据。
```

证据：

```text
实施记录/20260630_已确认存在体素融合分解_P4_Codex断点清单.md
Debug x64 构建通过。
```

### P5 父体素提交和版本闸门

状态：

```text
已完成当前代码切片。
```

目标：

```text
提交入口检查基准体素版本；
版本未变才允许裁决父体素候选；
版本变化时重算、合并或挂起。
```

验收：

```text
V1 加工、V1 提交可以进入裁决。
V1 加工、V2 提交不得覆盖。
父体素版本更新可回查。
```

证据：

```text
实施记录/20260630_已确认存在体素融合分解_P5_Codex断点清单.md
Debug x64 构建通过。
```

### P6 彩色分解和深度补空间

状态：

```text
已完成当前代码切片。
```

目标：

```text
彩色轮廓生成子轮廓候选；
深度轮廓补充空间占据和边界；
统计父基础颜色和子平均颜色。
```

验收：

```text
颜色块无深度支撑时只形成表面区域或未解释候选。
有深度支撑时形成子存在局部体素候选。
父颜色扣除已确认子轮廓后计算。
```

证据：

```text
实施记录/20260630_已确认存在体素融合分解_P6_Codex断点清单.md
Debug x64 构建通过。
```

### P7 内部世界子存在提交

目标：

```text
通过提交入口把稳定子候选写入父存在内部世界；
子存在拥有自己的局部正立方体体素模型；
保存父子坐标映射。
```

验收：

```text
子存在不挂在父存在普通字段。
子体素模型不是父完整模型复制。
子体素可投影回父体素空间和观察证据。
```

实施证据：

```text
实施记录/20260630_已确认存在体素融合分解_P7_Codex断点清单.md
日志/msbuild_P7_体素融合分解_20260630.log
```

### P8 控制面板和场景体素只读显示

状态：

```text
P8a 已完成：场景体素只读快照可选包含父存在内部世界下的子体素；控制面板 JSON / 列表 / WebGL 能区分父体素与内部世界子体素，并显示颜色事实缺失状态。
P8b 已完成当前安全切片：父基础颜色已按父存在、观察证据 ID 和基准体素版本匹配体素加工候选包后写入项目结构特征，控制面板可只读显示真实父基础颜色。
P8b 保留缺口：子平均颜色因缺少体素加工子轮廓候选与内部世界子存在的身份映射契约，只写入结构化状态“缺子候选映射”，不能宣称子颜色覆盖闭合。
```

目标：

```text
显示父体素基础颜色；
子体素颜色覆盖对应区域；
显示层只读，不写回事实。
```

验收：

```text
父颜色和子颜色分开读取。
渲染覆盖不改变父颜色事实。
控制面板显示不能作为世界树事实源。
```

证据：

```text
实施记录/20260630_已确认存在体素融合分解_P8_Codex断点清单.md
日志/msbuild_P8a_体素只读显示_20260630.log：已成功生成；0 个警告；0 个错误。
日志/msbuild_P8b_体素颜色事实_20260630.log：已成功生成；0 个警告；0 个错误。
```

### P8c 体素加工候选结果包诊断

状态：

```text
未实施。
P8b 后续第一切片；先证明候选结果包里真实存在什么，再决定是否进入身份映射和子颜色落账。
```

目标：

```text
补候选结果包诊断，不从控制面板颜色、CSS、列表顺序或“第一个候选”推断子颜色事实。
诊断输出只用于排查和只读显示，不作为正式事实源。
```

必须暴露 / 记录的候选包字段：

```text
候选包是否存在。
输入观察证据 ID。
基准体素版本。
父基础颜色统计是否有效。
子轮廓候选数量。
每个子轮廓候选 ID。
每个子轮廓候选像素数量。
每个子轮廓候选深度支撑状态。
每个子轮廓候选颜色统计是否有效。
每个子轮廓候选对应的局部子体素候选数量。
```

验收分支：

```text
子轮廓候选数量 = 0：判定为彩色输入或彩色分解问题，停止进入 P8d / P8e。
子轮廓候选数量 > 0 且局部子体素候选数量 = 0：判定为深度空间支撑问题，停止进入 P8d / P8e。
子轮廓候选数量 > 0 且局部子体素候选数量 > 0：允许进入 P8d 身份映射。
```

验收证据：

```text
新增 P8c 断点清单。
Debug x64 构建通过。
真实运行日志或控制面板只读诊断能看到上述候选结果包字段。
诊断结论明确落入三类验收分支之一。
```

### P8d 子轮廓候选到内部世界子存在身份映射

状态：

```text
未实施。
前置条件：P8c 证明至少存在一个同时具备子轮廓候选和局部子体素候选的候选项。
```

目标：

```text
建立体素加工子轮廓候选到内部世界子存在的稳定身份映射契约。
禁止按数组顺序、第一候选、显示颜色或 CSS 颜色猜测映射。
```

映射键：

```text
父存在。
输入观察证据 ID。
基准体素版本。
子轮廓候选 ID。
子体素候选 ID 或局部子体素候选索引。
```

正式写入子存在的映射事实：

```text
体素子候选 ID。
颜色来源观察证据 ID。
颜色来源基准体素版本。
子候选映射状态。
```

验收：

```text
一个子轮廓候选只能映射到一个明确的内部世界子存在，或写结构化缺口并拒绝子颜色落账。
多候选、无候选、版本不一致、父存在不一致时不得静默选取。
控制面板只读显示映射事实，不能反向参与映射裁决。
```

### P8e 子平均颜色正式落账和覆盖显示

状态：

```text
未实施。
前置条件：P8d 已建立无歧义映射。
```

目标：

```text
把子平均颜色从已映射的体素加工候选正式写入内部世界子存在。
控制面板继续只读真实事实；显示层允许用子颜色覆盖子体素占据区域，但覆盖不能改写父颜色事实。
```

正式写入子存在的颜色事实：

```text
体素子平均颜色RGBA。
体素子平均颜色像素数量。
体素子平均颜色事实状态 = 已写入。
```

验收：

```text
子平均颜色来自 P8d 映射后的候选统计。
父基础颜色和子平均颜色分开读取、分开显示。
子颜色覆盖仅是渲染策略，不改变父体素基础颜色事实。
缺映射、缺像素统计、缺颜色统计时只写结构化缺口，不写猜测颜色。
```

### P9 自然链路验证和实施记录

目标：

```text
用真实观察链或自然任务链验证 P8c / P8d / P8e 完整接入，而不是只用构建通过替代业务闭合。
```

验收：

```text
Debug x64 构建通过。
真实合法观察材料命中；如果没有合法材料，本阶段记为待条件，不记为成功或失败。
新增观察体素特征值触发体素加工。
体素加工候选结果包生成。
父存在、观察证据 ID、基准体素版本闸门通过。
父基础颜色事实写入。
子轮廓候选生成。
局部子体素候选生成。
内部世界子存在提交或命中。
子平均颜色事实写入。
控制面板和 SQL / 项目视图只读显示父 / 子体素事实。
实施记录写明命令、退出码、关键证据和禁止宣称。
```

## 5. 失败处理

```text
缺尺寸信息：降级为缺尺寸候选，不提交稳定等比例体素。
缺融合点：挂起待融合候选，不扩张父体素。
触碰外边界：标记不完整，不提交稳定模型。
版本变化：重算、合并或挂起，不覆盖新版本。
彩色有分区但深度无支撑：只作为表面颜色区域或未解释区域。
线程异常：报错并记录根因，不静默吞掉。
```

## 6. 完成后需要更新

```text
实施记录/YYYYMMDD_已确认存在体素融合分解_Codex断点清单.md
说明书/函数功能说明文档.md
docs/工程图谱/06_规范到代码映射.md
docs/工程图谱/07_规范到测试映射.md
docs/工程图谱/09_规范计划实施记录追踪图谱.md
计划/计划索引.md（仅在用户确认切换主动队列时更新）
```
