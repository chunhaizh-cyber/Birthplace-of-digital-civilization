# 特征类 VecI 解释视图完善计划 v0.1

日期：2026-06-10

## 1. 目标

```text
在不改 `特征值主信息类` 结构、不把坐标集合拆入特征子链的前提下，
完善特征类对有符号坐标集合的表达、读取、校验和比较能力。

核心路线：
    底层权威容器仍为 `特征值主信息类::VecIU64`；
    `VecI64` 只作为坐标类特征的解释视图；
    有符号坐标通过确定性 ZigZag 编码写入 VecIU64；
    平面轮廓 / 空间极值轮廓由特征类型解释 VecIU64 的坐标维度和语义；
    识别 / 扫描 / 跟踪只通过专用比较函数生成二次特征，不覆盖 `特征值主信息类::比较`。
```

## 2. 依据

```text
规范/规范目录.md
计划/计划索引.md
规范/特征类规范20260429.md
规范/抽象特征规范20260518.md
规范/二次特征规范20260520.md
规范/观察像素簇与存在候选分层规范20260527.md
规范/观察特征质量诊断与认知补偿规范20260525.md
规范/外设观察特征与自我场景认知本能方法分层规范20260525.md
规范/相机外设综合工作流程规范20260607.md
规范/识别本能方法唯一性收敛规范20260607.md
计划/完成项/20260519_特征值主信息字段纠偏分计划_v0.1.md
```

## 3. 当前代码事实

```text
已有：
    基础数据类型.h / 基础数据类型.ixx:
        using VecI64 = std::vector<I64>;
        using VecIU64 = std::vector<std::uint64_t>;
        枚举_特征值类型::VecIU64;
        枚举_比较模式 已区分字典序向量、L1/L2/余弦等距离类模式。

    特征值主信息类.h:
        VecIU64 值;
        内容哈希;
        命中次数;
        最后命中;
        `比较(特征值_值)` 为 VecIU64 字典序比较。

    特征值类.h:
        获取或创建(VecIU64);
        获取或创建句柄(VecIU64);
        取VecU只读指针(VecU句柄);
        哈希缓存和轮廓分层索引只服务去重 / 近似索引，不改变值语义。

    世界树类.h / 世界树类.cpp:
        写入特征_VecU(..., VecIU64, 枚举_轮廓维度);
        读取特征VecU(const 特征节点类*);

    特征类.h / 特征类.cpp:
        已有特征值读取 / 写入、值域命中、比较方式和特征集合比较入口。

    二次特征类.h / 二次特征类.cpp:
        已有标量值、离散值和差值结果写入入口，可承接轮廓比较结果。
```

```text
现状缺口：
    1. 没有统一 I64 <-> U64 ZigZag 编码 / 解码工具。
    2. 没有 VecI64 -> VecIU64 / VecIU64 -> VecI64 的统一包装入口。
    3. 写入 / 读取 VecI64 坐标类特征时，调用处容易绕过解释规则。
    4. 平面轮廓和空间极值轮廓缺统一格式校验函数。
    5. 轮廓相似、扫描变化、跟踪连续性缺特征类型专用比较入口。
    6. 当前规范已有“平面轮廓 VecIU64”口径，但 “可能为负坐标必须沿用 I64 到 VecIU64 编码” 的具体编码规则还需要落到代码和计划验收中。
```

## 4. 硬边界

必须遵守：

```text
1. 不修改 `特征值主信息类` 字段。
2. 不新增 `特征值主信息类::VecI64`。
3. 不在 `特征值主信息类` 中增加“编码类型”或“业务类型”字段。
4. 不把平面轮廓、空间极值轮廓、掩码、局部图这类整体值拆成特征子链。
5. `VecI64` 是解释视图，不是新的底层存储河道。
6. `特征值主信息类::比较` 只保留去重、排序、绝对相同用途。
7. 轮廓相似、扫描变化、跟踪连续性必须由特征类型专用比较函数生成二次特征。
8. 观察是合法写入新一阶平面轮廓的路径；识别 / 扫描 / 跟踪只读轮廓并生成二次特征、状态动态或缺口。
9. 任何真实行为验收不得使用模拟样本、夹具、伪造入口、伪造动作动态或 `--*-check` 回归命令。
```

允许例外：

```text
只有当某个点本身需要成为可独立认知的存在、子存在、操作目标或需求目标时，才另行制定子链 / 子存在计划。
当前平面轮廓、空间极值轮廓、掩码、局部图不进入该例外。
```

## 5. 允许修改范围

第一轮实现允许修改：

```text
基础数据类型.h
基础数据类型.ixx
特征值类.h
世界树类.h
世界树类.cpp
特征类.h
特征类.cpp
二次特征类.h
二次特征类.cpp
二次特征应用模块.ixx
自我类.特征定义.ixx
自我类.特征定义.cpp
自我动作实现.外设模块.ixx
外设观察报告队列.ixx
外设线程_D455深度相机.ixx
任务模块.管理工作线程.ixx
说明书/特征值类说明.md
说明书/世界树类说明.md
计划/20260610_特征类VecI解释视图完善计划_v0.1.md
计划/计划索引.md
实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md
```

按切片实际需要收窄修改范围；不得一次性打开全部文件做大改。

## 6. 禁止修改范围

```text
不得改 `特征值主信息类` 的字段布局、序列化字段或比较语义。
不得新增平行的 VecI 值池。
不得新增绕开 `写入特征_VecU` / `读取特征VecU` 的裸写路径。
不得把特征类型解释规则塞进特征值节点。
不得把轮廓比较结果写成一阶轮廓特征。
不得把外设线程内部结果直接写成世界真值、需求满足、安全值或服务值。
不得恢复任务筹办 / 任务执行为本能方法。
```

## 7. 实施切片

### P0：规范口径冻结

目标：

```text
先把“VecIU64 存储 + VecI64 解释视图 + ZigZag 编码 + 类型专用比较”冻结为本轮实现边界。
```

执行：

```text
1. 复核 011 / 050004 / 040011 / 050006 中平面轮廓、空间极值轮廓和 VecIU64 表述。
2. 如发现“直接非负 VecIU64”与“统一 ZigZag 解释视图”发生历史兼容冲突，先记录冲突并暂停实现。
3. 明确旧运行期数据是否需要兼容读取；若需要，单独制定迁移或版本识别方案，不靠猜测解码。
4. 只在抽象特征 / 特征类型定义侧记录编码解释规则，不写入 `特征值主信息类`。
```

验收：

```text
计划和规范口径能同时回答：
    平面轮廓 VecIU64 如何解释；
    空间极值轮廓 VecIU64 如何解释；
    为什么不拆子链；
    为什么不改特征值主信息；
    历史直接 VecIU64 轮廓是否存在兼容问题。
```

### P1：编码工具

目标：

```text
新增无业务语义的通用 ZigZag 编码 / 解码工具。
```

建议接口：

```cpp
std::uint64_t 编码I64为U64_ZigZag(I64 值) noexcept;
I64 解码U64为I64_ZigZag(std::uint64_t 值) noexcept;

VecIU64 编码VecI64为VecIU64_ZigZag(const VecI64& 值);
VecI64 解码VecIU64为VecI64_ZigZag(const VecIU64& 值);
```

落点：

```text
优先放在基础数据类型公共工具区；若 `.h` 与 `.ixx` 仍需镜像，两个文件同步。
函数必须是纯转换，不读取特征类型，不访问世界树。
```

验收：

```text
0 -> 0
-1 -> 1
1 -> 2
-2 -> 3
2 -> 4
I64_MIN / I64_MAX 可往返。
Debug x64 构建通过。
```

### P2：VecI64 特征值包装入口

目标：

```text
让坐标类调用处显式表达“我要写 / 读 VecI64 解释视图”，底层仍走 VecIU64 证据池。
```

建议接口：

```cpp
特征值节点类* 获取或创建VecI64特征值节点(const VecI64& 值, 枚举_轮廓维度 维度);
VecU句柄 获取或创建VecI64句柄(const VecI64& 值, 枚举_轮廓维度 维度);

bool 写入特征_VecI64(特征节点类* 节点, const VecI64& 值, 枚举_轮廓维度 维度, 时间戳 now);
bool 写入特征_VecI64(基础信息节点类* 宿主, const 语素入口节点类* 特征类型, const VecI64& 值, 枚举_轮廓维度 维度, 时间戳 now);
bool 读取特征VecI64(const 特征节点类* 节点, VecI64& 输出值) const;
```

落点：

```text
特征值类.h:
    增加获取或创建 VecI64 的包装，内部编码后调用现有 VecIU64 入口。

世界树类.h / 世界树类.cpp:
    增加写入 / 读取 VecI64 包装，内部仍使用 VecU 句柄和特征值池。
```

验收：

```text
rg 确认 `特征值主信息类` 未新增 VecI64 / variant / 编码类型字段。
写入 VecI64 后，证据池仍只保存 VecIU64。
读取 VecI64 时只通过 VecU句柄只读指针解码，不复制或修改证据池原值。
```

### P3：特征类型解释规则

目标：

```text
由特征类型决定 VecIU64 的解释方式，不由值节点决定。
```

规则：

```text
平面轮廓:
    VecIU64_ZigZag_I64_二维坐标链
    每 2 个 I64 为一个二维坐标
    第一个二维坐标为轮廓中心
    后续二维坐标为边界点
    后续点按轮廓遍历顺序解释

空间极值轮廓:
    VecIU64_ZigZag_I64_三维坐标链
    每 3 个 I64 为一个三维坐标
    第一个三维坐标为空间中心
    后续三维坐标为空间极值点 / 边界点
    后续点按采集顺序解释
```

执行：

```text
1. 优先复用现有语素词入口。
2. 若调用处需要统一引用，再在 `自我类.特征定义` 中补最小特征类型 helper。
3. 不新增 `枚举_特征值编码类型` 到 `特征值主信息类`。
```

验收：

```text
能从特征类型判断轮廓维度和解释规则。
同一个 VecIU64 仍允许被不同特征类型按不同规则解释；值节点本身不携带业务解释。
```

### P4：格式校验函数

目标：

```text
把轮廓格式错误尽早报出，不让错误 VecIU64 静默进入识别 / 扫描 / 跟踪。
```

建议接口：

```cpp
bool 校验平面轮廓VecI64(const VecI64& 值) noexcept;
bool 校验空间极值轮廓VecI64(const VecI64& 值) noexcept;
bool 校验坐标类VecI64_按特征类型(const 语素入口节点类* 特征类型, const VecI64& 值) noexcept;
```

校验规则：

```text
平面轮廓:
    size >= 2
    size % 2 == 0

空间极值轮廓:
    size >= 3
    size % 3 == 0
```

验收：

```text
非法长度必须返回失败并记录明确错误或缺口。
不得通过截断、补 0、静默忽略尾值来兜底。
```

### P5：轮廓专用比较函数

目标：

```text
新增平面轮廓 / 空间极值轮廓的专用比较入口，输出可入账的二次特征材料。
```

前置条件：

```text
P2 已提供 VecI64 读写包装。
P3 已能由特征类型判断解释维度。
P4 已提供平面轮廓 / 空间极值轮廓格式校验函数。
```

#### P5a：比较结果结构

目标：

```text
先定义一次比较的最小结果结构，避免用散落的 I64 临时值承接业务语义。
该结构只作为运行期返回值，不新增持久主信息字段。
```

建议落点：

```text
特征类.h
```

建议接口：

```cpp
enum class 枚举_轮廓比较状态 : std::uint8_t {
    未定义 = 0,
    可比较 = 1,
    格式非法 = 2,
    维度不支持 = 3,
    点数量不一致 = 4,
    不可对齐 = 5,
};

struct 结构_轮廓比较结果 {
    枚举_轮廓比较状态 状态 = 枚举_轮廓比较状态::未定义;
    std::uint32_t 坐标维度 = 0;
    I64 左点数量 = 0;
    I64 右点数量 = 0;
    I64 点数量差异 = 0;
    I64 中心L1误差 = 0;
    I64 点链平均L1误差 = 0;
    I64 点链最大L1误差 = 0;
    I64 匹配评分Q10000 = 0;

    bool 可比较() const noexcept;
};
```

验收：

```text
结果结构不进入 `特征值主信息类`。
状态能表达格式非法、点数不一致、不可对齐等失败原因。
```

#### P5b：坐标误差私有 helper

目标：

```text
把二维 / 三维坐标点误差和点链误差封装为无业务写入的纯计算函数。
```

建议落点：

```text
特征类.cpp 匿名 namespace
```

建议接口：

```cpp
I64 私有_坐标点L1误差(
    const VecI64& 左值,
    std::size_t 左起点,
    const VecI64& 右值,
    std::size_t 右起点,
    std::uint32_t 坐标维度) noexcept;

bool 私有_计算点链平均最大误差(
    const VecI64& 左值,
    const VecI64& 右值,
    std::uint32_t 坐标维度,
    I64& 输出平均L1误差,
    I64& 输出最大L1误差) noexcept;

I64 私有_计算轮廓匹配评分Q10000(
    I64 中心L1误差,
    I64 点链平均L1误差,
    I64 点链最大L1误差) noexcept;
```

计算边界：

```text
使用饱和加法 / 饱和绝对值，避免 I64_MIN 取绝对值溢出。
首版只做同点数、同顺序点链比较。
点链平均误差使用整数除法；不能因为除不尽引入浮点依赖。
评分 Q10000 首版只需单调稳定：
    误差越大，评分越低；
    完全相同为 10000；
    不可比较为 0。
```

验收：

```text
helper 不读取世界树，不写二次特征，不访问特征值池。
```

#### P5c：通用坐标轮廓比较函数

目标：

```text
实现二维 / 三维共用的坐标链比较核心。
```

建议落点：

```text
特征类.h / 特征类.cpp
```

建议接口：

```cpp
static 结构_轮廓比较结果 比较坐标轮廓VecI64(
    const VecI64& 左值,
    const VecI64& 右值,
    std::uint32_t 坐标维度) noexcept;
```

处理规则：

```text
坐标维度只允许 2 或 3。
长度不满足维度倍数，返回 格式非法。
点数量不同，返回 点数量不一致，同时写出点数量差异。
同维度、同点数时：
    第一个点计算中心L1误差；
    后续点按顺序计算点链平均L1误差和最大L1误差；
    写出匹配评分Q10000。
```

验收：

```text
不做点重采样。
不做旋转、缩放、平移归一。
不做掩码重合率。
不修改输入向量。
```

#### P5d：平面 / 空间轮廓显式入口

目标：

```text
给调用处提供语义清晰的二维 / 三维入口，避免到处裸传维度数字。
```

建议接口：

```cpp
static 结构_轮廓比较结果 比较平面轮廓VecI64(
    const VecI64& 左值,
    const VecI64& 右值) noexcept;

static 结构_轮廓比较结果 比较空间极值轮廓VecI64(
    const VecI64& 左值,
    const VecI64& 右值) noexcept;
```

验收：

```text
平面轮廓入口固定调用二维比较。
空间极值轮廓入口固定调用三维比较。
接口名不能让调用处误以为 VecIU64 字典序比较可以替代轮廓相似比较。
```

#### P5e：按特征类型路由比较

目标：

```text
由特征类型选择平面轮廓或空间极值轮廓比较规则。
```

建议接口：

```cpp
static std::optional<std::uint32_t> 轮廓坐标维度_按特征类型(
    const 语素入口节点类* 特征类型) noexcept;

static 结构_轮廓比较结果 比较轮廓VecI64_按特征类型(
    const 语素入口节点类* 特征类型,
    const VecI64& 左值,
    const VecI64& 右值) noexcept;
```

处理规则：

```text
特征类型 == 平面轮廓 -> 二维。
特征类型 == 空间极值轮廓 -> 三维。
其他特征类型返回 维度不支持，不尝试猜测。
```

验收：

```text
解释规则仍由特征类型提供。
不向值节点写入编码类型或维度字段。
```

#### P5f：特征节点读取后比较入口

目标：

```text
为识别 / 扫描 / 跟踪提供“从特征节点读取 VecI64 后比较”的统一入口。
该入口需要访问 VecU 值池，因此落在世界树层；特征类只保留纯 VecI64 比较函数。
```

建议落点：

```text
世界树类.h / 世界树类.cpp
```

建议接口：

```cpp
结构_轮廓比较结果 比较轮廓特征_按特征类型(
    const 语素入口节点类* 特征类型,
    const 特征节点类* 左特征,
    const 特征节点类* 右特征) const;
```

处理规则：

```text
读取失败返回 格式非法或不可对齐，不静默当作 0 误差。
读取路径只通过 P2 的 `读取特征VecI64` 包装。
该入口不写二次特征，只返回比较结果。
比较本体调用 P5e 的 `比较轮廓VecI64_按特征类型`。
```

验收：

```text
识别 / 扫描 / 跟踪调用处不需要直接操作 VecU句柄。
仍不允许裸用 `特征值主信息类::比较` 判断轮廓相似。
```

#### P5g：二次特征写入适配

目标：

```text
把 P5 的运行期比较结果转成二次特征材料，供 P7 接入识别 / 扫描 / 跟踪。
```

建议落点：

```text
二次特征应用模块.ixx 或二次特征类的薄包装入口。
```

建议接口：

```cpp
bool 写入轮廓比较二次特征(
    基础信息节点类* 父节点,
    const 语素入口节点类* 特征类型,
    const 结构_轮廓比较结果& 比较结果,
    特征节点类* 左特征,
    特征节点类* 右特征,
    场景节点类* 来源场景,
    动态节点类* 来源动态);
```

写入规则：

```text
可比较时至少写入：
    中心偏差；
    点数量差异；
    轮廓点平均误差；
    轮廓点最大误差；
    轮廓匹配评分。

不可比较时至少写入：
    不可比较状态；
    失败原因；
    点数量差异，如果可得。
```

边界：

```text
该入口只写二次特征材料，不写一阶轮廓特征。
不直接宣称存在同一、扫描变化成立、跟踪连续或需求满足。
来源特征、来源场景、来源动态能回查。
```

验收：

```text
二次特征节点能追到左右特征或来源对象。
日志或状态快照能看到中心偏差 / 平均误差 / 最大误差 / 匹配评分。
不可比较结果不会被当成匹配成功。
```

#### P5h：P5 切片总验收

```text
`特征值主信息类::比较` 不变。
`特征值主信息类` 不新增 VecI64 / 编码类型 / 维度字段。
比较函数能区分：
    不可比较；
    格式非法；
    维度不支持；
    点数量不一致；
    中心偏差；
    平均误差；
    最大误差；
    匹配评分。
Debug x64 构建通过。
若无真实轮廓样本，只能证明函数路径可构建，不能宣称识别 / 扫描 / 跟踪业务通过。
```


### P6：观察写入接入

目标：

```text
观察方法写平面轮廓 / 空间极值轮廓时，先形成 VecI64 解释视图，再编码为 VecIU64 入池。
```

执行：

```text
1. 定位当前外设观察存在、观察像素簇和 D455 材料页写平面轮廓的位置。
2. 将坐标打包入口收敛到 P2 的 VecI64 写入包装。
3. 写入前调用 P4 校验函数。
4. 保留来源帧、ROI、坐标系、generation、质量状态等伴随材料，不把这些写进值节点。
```

验收：

```text
观察链仍是唯一合法写入新一阶轮廓的路径。
写入失败时有明确错误 / 缺口，不静默创建半截轮廓。
不把外设内部轮廓直接宣称为世界存在真值。
```

### P7：识别 / 扫描 / 跟踪读取和比较接入

目标：

```text
识别、扫描、跟踪读取轮廓时解码为 VecI64，并使用 P5 专用比较生成二次特征。
```

执行：

```text
识别：
    读取外设观察存在.平面轮廓 和候选自我存在 / 已有存在的平面轮廓；
    生成平面轮廓匹配评分、平均误差、最大误差和唯一性 / 冲突材料；
    不直接写新的世界存在一阶轮廓。

扫描：
    有可比历史时读取当前 / 基准平面轮廓；
    生成轮廓差异值、变化候选状态、值域冲突或无变化结论；
    首次扫描仍只生成发现存在 / 发现特征事实，不伪造成变化动态。

跟踪：
    读取目标当前轮廓和上一轮 / 基准轮廓；
    用中心偏差和轮廓匹配评分生成连续性、丢失、重捕获候选材料；
    不把跟踪结果当成全场扫描或全场明确。
```

验收：

```text
识别 / 扫描 / 跟踪没有裸用 VecIU64 字典序比较判断相似。
二次特征结果可在日志或状态快照中追到来源特征、来源场景和来源动态。
```

### P8：验证和断点

每个闭合切片必须先写预测，再执行验证。

最低验证：

```text
git diff --check
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
rg -n "VecI64.*特征值主信息类|枚举_特征值编码类型|std::variant<.*VecI64" 特征值主信息类.h 基础数据类型.h 特征值类.h
rg -n "平面轮廓|空间极值轮廓|轮廓点平均误差|轮廓点最大误差|VecI64|VecIU64" 特征类.* 世界树类.* 特征值类.h 自我动作实现.外设模块.ixx 外设线程_D455深度相机.ixx
```

真实运行验证：

```text
允许使用真实 `--self-check-log` 运行窗口或真实 D455 运行窗口。
不得新增模拟样本、夹具入口、伪造动作动态或 `--*-check` 回归命令。
如果缺真实轮廓样本，只能把行为验收标记为待自然样本，不能宣称业务通过。
```

断点清单：

```text
实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md
```

至少记录：

```text
当前切片
依据文件
已改文件
已运行命令
关键退出码
关键日志证据
当前判断
下一步
禁止宣称项
```

## 8. 暂停条件

出现以下情况必须暂停当前切片，先回到规范 / 计划确认：

```text
1. 发现已有持久化平面轮廓数据按“非负 VecIU64 直存”保存，且会被新 ZigZag 解码误读。
2. 发现现有规范要求在特征值主信息内携带编码类型。
3. 发现实现必须拆轮廓点为子特征才能继续。
4. 发现识别 / 扫描 / 跟踪当前只能通过 VecIU64 字典序比较推进业务判断。
5. 发现真实行为验收只能靠伪造样本或旧 `--*-check` 入口。
```

## 9. 完成判定

```text
P0-P5 完成：
    特征类具备 VecI64 解释视图、校验和轮廓专用比较能力；
    `特征值主信息类` 未变化；
    Debug x64 构建通过。

P6-P7 完成：
    观察写入、识别、扫描、跟踪已改用 VecI64 解释视图和专用比较；
    真实运行日志或状态快照能证明新路径命中；
    如果缺自然样本，只能记录为待条件具备后继续。
```

禁止宣称：

```text
不得宣称自我苏醒完成。
不得宣称初步成熟完成。
不得把“轮廓编码 / 解码工具完成”宣称为“观察链业务通过”。
不得把“构建通过”宣称为“识别 / 扫描 / 跟踪真实行为通过”。
```

## 10. 执行记录

### 2026-06-10 P0 规范口径冻结

```text
已复核：
    规范/观察像素簇与存在候选分层规范20260527.md
    规范/相机外设综合工作流程规范20260607.md
    规范/识别本能方法唯一性收敛规范20260607.md

发现冲突：
    本计划 P3 当前写法为“平面轮廓 = VecIU64_ZigZag_I64_二维坐标链”。
    修订前 050004 规范允许“全帧图像坐标且坐标非负时直接存入 VecIU64”。
    若读取端一律 ZigZag 解码旧非负直存轮廓，奇数坐标会被误解为负数。

当前处理：
    已记录断点：
        实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md

后续修订：
    已按用户确认口径修订 011 / 011001 / 050004 / 040011。
    新口径为：读取 VecIU64 时必须根据特征类型决定是否转换以及如何转换。
    平面轮廓按 VecIU64_ZigZag_I64_二维坐标链解释。
    空间极值轮廓按 VecIU64_ZigZag_I64_三维坐标链解释。
    类型信息不进入 `特征值主信息类`。
    P0 冲突已解除，下一步可进入 P1 编码工具实现。
```

### 2026-06-10 P1 编码工具

```text
已实施：
    在 `基础数据类型.h` 和 `基础数据类型.ixx` 新增无业务语义的 ZigZag 编解码工具：
        编码I64为U64_ZigZag
        解码U64为I64_ZigZag
        编码VecI64为VecIU64_ZigZag
        解码VecIU64为VecI64_ZigZag

    同步新增无后缀薄别名：
        编码I64为U64
        解码U64为I64
        编码VecI64为VecIU64
        解码VecIU64为VecI64

构建期验收：
    static_assert 覆盖：
        0 -> 0
        -1 -> 1
        1 -> 2
        -2 -> 3
        2 -> 4
        I64_MIN / I64_MAX 往返

边界：
    未修改 `特征值主信息类`。
    未新增 VecI64 底层字段。
    未新增编码类型字段。
    未拆轮廓子链。

验证：
    git diff --check 通过。
    Debug x64 构建通过。
    构建日志：`日志/鱼巢_build_20260610_VecI_P1.log`。

下一步：
    进入 P2：新增 VecI64 特征值包装入口，底层仍调用 VecIU64 证据池。
```

### 2026-06-10 P2 VecI64 特征值包装入口

```text
已实施：
    特征值类.h：
        获取或创建VecI64特征值节点
        获取或创建VecI64句柄

    世界树类.h / 世界树类.cpp：
        写入特征_VecI64(特征节点类*, ...)
        写入特征_VecI64(基础信息节点类*, 特征类型, ...)
        读取特征VecI64(特征节点类*, ...)
        读取特征VecI64(宿主, 特征类型, ...)

边界：
    VecI64 写入先编码为 VecIU64，再进入现有 VecU 证据池。
    VecI64 读取只通过 VecU句柄取得只读 VecIU64 指针，再解码为 VecI64 输出。
    未修改 `特征值主信息类`。
    未新增 VecI64 底层字段。
    未新增编码类型字段。
    未拆轮廓子链。

验证：
    git diff --check 通过。
    Debug x64 构建通过。
    构建日志：`日志/鱼巢_build_20260610_VecI_P2.log`。

下一步：
    进入 P3：由特征类型决定 VecIU64 的解释规则。
```

### 2026-06-10 P3 特征类型解释规则

```text
已实施：
    特征类.h / 特征类.cpp：
        枚举_VecU解释规则
        结构_VecU特征解释规则
        特征类::VecU解释规则_按特征类型
        特征类::轮廓坐标维度_按特征类型

解释规则：
    平面轮廓：
        VecIU64_ZigZag_I64_二维坐标链
        坐标维度 = 2
        需要 ZigZag 解码

    空间极值轮廓：
        VecIU64_ZigZag_I64_三维坐标链
        坐标维度 = 3
        需要 ZigZag 解码

边界：
    规则由特征类型查询得到。
    未修改 `特征值主信息类`。
    未在值节点写入编码类型或维度字段。
    未拆轮廓子链。
    其他未知 VecU 特征类型不被猜测为坐标链。

验证：
    git diff --check 通过。
    Debug x64 构建通过。
    构建日志：`日志/鱼巢_build_20260610_VecI_P3.log`。

下一步：
    进入 P4：新增平面轮廓 / 空间极值轮廓 VecI64 格式校验函数。
```

### 2026-06-10 P4 格式校验函数

```text
已实施：
    特征类.h / 特征类.cpp：
        校验坐标链VecI64
        校验平面轮廓VecI64
        校验空间极值轮廓VecI64
        校验坐标类VecI64_按特征类型

校验规则：
    平面轮廓：
        size >= 2
        size % 2 == 0

    空间极值轮廓：
        size >= 3
        size % 3 == 0

边界：
    非法长度只返回失败，不截断、不补 0、不静默忽略尾值。
    未修改 `特征值主信息类`。
    未新增 VecI64 底层字段。
    未拆轮廓子链。

验证：
    git diff --check 通过。
    Debug x64 构建通过。
    构建日志：`日志/鱼巢_build_20260610_VecI_P4.log`。

下一步：
    进入 P5：新增轮廓专用比较函数。
```

### 2026-06-11 P6 观察写入接入

```text
已实施：
    自我动作实现.外设模块.ixx：
        新增 `平面轮廓` / `空间极值轮廓` 特征词 helper 及对应状态特征。
        新增 `写入基础节点VecI64轮廓`，写入前调用 `特征类::校验坐标类VecI64_按特征类型`。
        新增 D455 观察像素簇平面轮廓构造：中心二维坐标 + 投影 AABB 四角。
        新增 D455 观察像素簇空间极值轮廓构造：空间中心 + 三维 AABB 八角。
        已验证观察像素簇写入平面轮廓 / 空间极值轮廓时走 `世界树.写入特征_VecI64`，底层仍入 VecIU64 值池。

边界：
    未修改 `特征值主信息类`。
    未新增 VecI64 底层字段。
    未新增编码类型字段。
    未拆轮廓子链。
    不把外设内部轮廓直接宣称为世界存在真值。
    写入失败记录错误日志，并把本轮观察候选验证结果转为失败，不静默保留半截轮廓。

验证：
    git diff --check 通过。
    禁止字段扫描无命中。
    Debug x64 构建通过。
    构建日志：`日志/鱼巢_build_20260611_VecI_P6.log`。
    `--self-check-log --self-check-log-wait-ms=1000` 退出码 0。
    运行日志：`日志/鱼巢_run_20260611_VecI_P6_selfcheck.log`。
    普通自检窗口未命中 `外设观察/VecI64轮廓写入汇总`，P6 真实轮廓写入行为命中待真实 D455 已验证簇自然样本。

下一步：
    进入 P7：识别 / 扫描 / 跟踪读取轮廓时解码为 VecI64，并使用 P5 专用比较生成二次特征。
```
