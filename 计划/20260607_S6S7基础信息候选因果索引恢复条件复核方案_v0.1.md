# 20260607 S6S7 基础信息候选因果索引恢复条件复核方案 v0.1

## 1. 目标

```text
只读复核 S6 基础信息运行期索引、S7 候选方法索引和因果主果索引是否满足恢复条件。
确认是否已有真实日志证明基础信息查找、候选方法扫描或因果主果查询成为当前主瓶颈。
```

## 2. 边界

```text
本切片只读定位和登记恢复条件。
不实现基础信息索引、候选方法索引或因果主果索引。
不改变任务状态、需求满足、价值结算、方法候选裁决或动作动态规则。
不新增模拟测试、夹具测试、伪造入口或伪造动作动态。
```

## 3. 预测

```text
候选方法和因果路径已有耗时日志，预期当前仍是毫秒级。
基础信息底层预期仍是链表 / 子链扫描结构风险，但缺少证明它成为当前主瓶颈的阶段耗时日志。
若预测成立，本轮不进入索引实现，只登记恢复条件。
```

## 4. 真实日志证据

```text
实施记录/20260607_运行速度架构缺陷检测报告.md：
    候选集填充全树遍历完成 count=131；
    最大访问节点数=230；
    最大单次耗时约 32ms；
    因果路径筹办查询壳统计 count=137；
    最大因果节点=3738；
    最大单次耗时约 17ms；
    报告已判断：当前单次仍是毫秒级，不是本次摘要命令第一瓶颈。

日志/鱼巢_run_20260607.低值g 全日志统计：
    候选集填充全树遍历完成 count=756；
    候选最大耗时=18ms；
    候选最大访问节点数=228；
    候选平均耗时约 1.01ms；
    因果路径筹办查询壳统计 count=843；
    因果最大耗时=9ms；
    因果最大因果节点数=2147；
    因果平均耗时约 1.16ms。

近期窗口样本：
    61151 行：候选访问节点数=227，新增候选数=9，耗时ms=9。
    59914 行：因果节点=718，耗时ms=2。
    60448 行：因果节点=1000，耗时ms=2。
    62200 行：候选访问节点数=227，新增候选数=0，耗时ms=0。
```

## 5. 代码路径

```text
任务模块.筹办.ixx：
    2950 执行因果路径查询壳；
    2970 枚举全部因果；
    2980-3040 遍历因果节点并按主果比较模板筛选；
    3050-3075 输出 因果路径筹办查询壳统计 和 耗时ms；
    4630 填充候选集_按结果能力；
    4689 从方法根开始做全树遍历；
    4712-4723 输出 候选集填充全树遍历完成、访问节点数和耗时ms。

基础信息类.h / 基础信息类.cpp：
    枚举全部节点、枚举节点_按类型、枚举子节点、枚举子节点_按类型均基于链表 / 子链扫描；
    当前没有独立基础信息查找耗时日志证明其成为主瓶颈。

双向链表模板核心.h：
    查找主键_已加锁 沿 根指针->链下 全链扫描；
    查找主键 只是在读锁下调用全链扫描。
```

## 6. 判断

```text
S6 / S7 的结构风险成立：
    基础信息主键查找和按类型枚举仍是线性扫描；
    候选方法填充仍从方法根全树遍历；
    因果查询壳仍枚举全部因果后筛主果模板。

但当前恢复条件不成立：
    真实日志中候选方法扫描和因果查询仍为毫秒级；
    当前统计没有显示候选方法或因果主果查询超过 S1d 状态提交热点；
    基础信息路径缺少阶段耗时日志，不能直接证明基础信息查找是当前主瓶颈。
```

## 7. 结论

```text
S6/S7 本轮只读闭合，不进入索引实现。

恢复条件：
    候选集填充全树遍历完成 出现持续高耗时，例如 P95 超过 50ms 或单次超过 100ms；
    或 因果路径筹办查询壳统计 出现持续高耗时，例如 P95 超过 50ms 或单次超过 100ms；
    或 真实任务推进日志证明基础信息查找 / 枚举成为最慢阶段；
    或 方法节点 / 因果节点规模增长到现有线性扫描开始支配常驻路径；
    或 用户明确要求优先实现索引。

下一步：
    当前性能计划中可证明 P0/P1/P2/P3 本轮均已处理到证据边界；
    剩余 S1d-4g 自我_提交任务状态变化 本体写账瘦身仍是待优化登记项，需要单独方案确认允许修改范围；
    在没有新瓶颈日志或用户明确恢复条件前，不自动进入大范围索引代码改造。
```
