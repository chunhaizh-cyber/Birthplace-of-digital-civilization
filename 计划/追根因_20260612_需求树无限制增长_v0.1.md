# 追根因：需求树无限制增长 v0.1

日期：2026-06-12

## 当前问题

原始树视图显示需求树异常增长：

```text
需求数=102 | 已满足=8 | 状态已达未截止=1 | 未满足=93
```

主要膨胀形态：

```text
1. 安全根需求 -> 当前观察范围可观测单位存在对应闭合状态 下，重复出现 已识别区域扫描入账状态 / 目标存在跟踪状态 子需求。
2. 方法维护管理状态 下，重复出现 方法状态 子需求。
```

## 最高优先级原因

需求树无限制增长会污染后续筹办、任务派发、学习需求验证和控制面板判断。该问题未闭合前，计划索引默认主线暂停，当前以本追根因计划为第一优先计划。

## 主链判断

当前只确认症状和初步方向，不宣称根因已找到。

已知追踪方向：

```text
1. 统一入树函数存在同父目标合并逻辑。
2. 方法维护链另有按被维护方法主键查重逻辑。
3. 因此首要怀疑不是“完全没有去重”，而是上游每轮生成了不同目标宿主 / 不同目标状态 / 不同来源字段，导致同父目标合并无法命中。
4. 外设观察链已显示为：识别对应事实提交成功后，为每个稳定对应存在创建扫描约束和跟踪约束，再由承接请求转成需求和任务。
```

## 分层查找规则

本追根因计划必须从最近层开始，同层疑点先记录完整，再选择一条下钻。

当前层级划分：

```text
L0 现象层：原始树视图需求数=102，重复显示扫描/跟踪子需求与方法状态子需求。
L1 直接生成层：需求树视图生成、需求节点枚举、需求计数统计、控制面板摘要是否重复显示同一节点。
L2 本地事实层：需求入树、同父合并、目标宿主/目标特征/满足关系去重字段。
L3 上游请求层：外设观察扫描/跟踪派生请求、方法维护派生请求、任务回执。
L4 生命周期层：自我线程需求树生长、任务管理承接、外设报告交接、父任务重筹办。
L5 规范契约层：扫描/跟踪是否应作为当前明确状态子需求，方法状态维护需求是否同被维护方法幂等。
```

同层疑点记录模板：

```text
层级：
疑似原因：
证据：
反证 / 未证：
下一验证：
状态：候选 / 暂排除 / 需下钻 / 阻塞
```

当前同层候选：

```text
L1-A 候选：控制面板 / 原始树视图重复显示同一需求节点，实际树未重复增长。
L1-B 候选：需求计数统计重复计数或枚举路径重复进入同一子链。
L1-C 候选：实际需求树中存在不同主键的等价扫描 / 跟踪 / 方法状态需求。
```

选择规则：

```text
1. 先用只读方式区分 L1-A/L1-B/L1-C。
2. 若只是显示 / 统计重复，先修 L1，不下钻 L2/L3。
3. 若确认实际不同主键等价需求增长，再进入 L2 入树合并字段核对。
4. L2 未命中后，才分别进入 L3 外设观察派生链和方法维护派生链。
```

## 已确认事实

```text
1. 当前计划索引原主动主线不是本问题；本问题是新出现的状态偏差。
2. 规范口径要求同父直接子需求应按目标宿主 / 目标特征类型 / 满足关系合并，派生归因不应导致重复。
3. 树视图中重复链至少包括外设观察扫描 / 跟踪派生链和方法维护 方法状态 派生链。
4. 上一次追踪在进入跨帧幂等和规范边界核对前发生 remote compact / stream disconnected；该中断属于执行通道问题，不是项目实现失败。
```

## 已排除

```text
尚未形成可排除项。
```

## 旁支问题池

```text
C1. 方法维护管理状态下 方法状态 子需求重复增长，可能与被维护方法主键、目标宿主或目标状态构造有关。
C2. 外设观察链扫描 / 跟踪约束重复增长，可能与稳定对应存在跨帧幂等或扫描 / 跟踪边界误入当前明确状态子需求有关。
```

## 已改文件

```text
计划/追根因_20260612_需求树无限制增长_v0.1.md
计划/计划索引.md
```

## 已运行命令

```text
已读取：
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md

已检查：
git status --short --untracked-files=no
python C:\Users\zhchh\.codex\skills\.system\skill-creator\scripts\quick_validate.py C:\Users\zhchh\.codex\skills\yu-chao-root-cause
git diff --check -- 计划/计划索引.md 计划/追根因_20260612_需求树无限制增长_v0.1.md
```

当前校验：

```text
1. `yu-chao-root-cause` skill 校验通过：Skill is valid!
2. `计划/计划索引.md` diff check 通过；仅提示 LF 下次由 Git 接触时会替换为 CRLF。
3. 本追根因记录已按规则放入 `计划/` 根目录并在计划索引中置为第一优先计划。
4. 2026-06-12 本轮恢复已读取 AGENTS.md、禁止性规范、规范目录、计划索引、本计划、需求树生长 / 任务筹办边界 / 相机综合流程 / 当前场景明确标准 / 远程断点规范，并确认工作区已有未提交改动；后续修改必须避开无关差异。
5. 2026-06-12 已补入分层追根因规则：先从 L0/L1 最近层开始，同层候选记录完整后再选一条下钻；`yu-chao-root-cause` skill 校验通过，计划文件和计划索引 diff check 通过。
```

## 下一条命令

先完成 L1 同层候选的只读区分，确认是显示/计数重复还是实际不同主键需求增长：

```powershell
rg -n "需求数=|需求树|原始树视图|结构节点与字段摘要|需求列表|任务树|子需求|主键=" -S --glob '!x64/**' --glob '!Debug/**' --glob '!.git/**' 控制面板类.cpp 控制面板摘要线程模块.impl.cpp 任务模块.管理界面线程.impl.cpp
```

若 L1 证明实际存在不同主键等价需求，再定位 L2/L3：

```powershell
rg -n "已识别区域扫描入账状态|目标存在跟踪状态|扫描约束|跟踪约束|当前观察范围可观测单位存在对应闭合状态|稳定对应存在|方法状态" -S --glob '!x64/**' --glob '!Debug/**' --glob '!.git/**'
```

核对：

```text
1. L1 是否重复显示或重复计数同一主键。
2. L2 是否同父合并字段不一致，导致不同主键等价需求入树。
3. L3 是否每帧生成新宿主或新目标状态。
4. L3/L5 是否把扫描 / 跟踪误当成当前观察范围明确状态的完成子需求。
5. 是否已有跨帧幂等登记但写入字段不一致。
```

## 验收证据

```text
1. 真实运行窗口中需求树总量不再因同一外设观察事实持续线性增长。
2. 同一父需求下不重复创建等价的 已识别区域扫描入账状态 / 目标存在跟踪状态 子需求。
3. 方法维护管理状态下同一被维护方法不重复创建等价 方法状态 子需求。
4. 不通过删除弹窗、吞错误、清空需求树、屏蔽派生需求或伪造样本完成验证。
5. 若修复涉及需求入树，必须保留动作动态 / 来源动态 / 日志证据链。
```

## 上下文控制

```text
1. 每轮最多追一条主链：先外设观察扫描 / 跟踪派生链，方法维护链只入旁支池。
2. 每层先记录同层全部候选，再选择一条下钻；不得发现第一条疑似就直接进入深层修改。
3. 每 6 条工具命令、每次真实运行前、完成同层候选枚举后、每次发现两个以上旁支问题时，必须先更新本文件和计划索引关键状态。
4. 只把当前层和选中分支证据放入对话；同层未选疑点只写问题名、证据状态和恢复条件。
5. remote compact / stream disconnected 只作为执行通道中断；恢复时从 AGENTS.md -> 规范/禁止性规范20260611.md -> 规范/规范目录.md -> 计划/计划索引.md -> 本文件 -> git status 恢复。
```

## 禁止宣称

```text
1. 不宣称需求树生长机制已整体闭合。
2. 不宣称当前场景特征值明确。
3. 不宣称需求满足、安全值 / 服务值入账。
4. 不宣称自我苏醒完成或内部循环全部完成。
```
