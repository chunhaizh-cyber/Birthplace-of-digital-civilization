# 动态类逻辑纠偏计划 v0.1

日期：2026-06-20

状态：由因果类外部数据读取纠偏触发；当前只执行第一优先缺口，闭合后返回 `计划/20260620_因果类外部数据读取纠偏协调计划_v0.1.md`。

## 0. 目标

补齐动态类只读功能函数，让因果类不再直接解释 `动态节点主信息类` 字段。

当前触发缺口：

```text
因果类.cpp 直接读取：
    动态节点主信息类::来源类型
    动态节点主信息类::业务语义类型
    动态节点主信息类::来源动作
    动态节点主信息类::来源动作名
    动态节点主信息类::初始状态
    动态节点主信息类::结果状态
    动态节点主信息类::动态层级
```

## 1. 第一优先缺口

建议函数：

```cpp
动态节点类* 按主键解析动态节点(const std::string& 主键) const noexcept;

bool 读取动态因果生成输入(
    const 动态节点类* 动态,
    状态节点类*& 初始状态,
    状态节点类*& 结果状态,
    方法节点类*& 来源动作,
    const 语素入口节点类*& 来源动作名,
    枚举_动态来源类型& 来源类型,
    枚举_动态业务语义类型& 业务语义类型,
    std::uint32_t& 动态层级) const noexcept;
```

实现边界：

```text
1. 函数归属 动态类.h/.cpp。
2. 不新增结构体，不新增动态主信息字段。
3. `业务语义类型` 输出必须使用 动态类::推导业务语义类型 后的正式结果；主信息缓存只作辅助。
4. 输出状态、方法和语素入口均使用现有节点 / 指针 / 引用结构。
5. 不把动作语义文本作为新业务状态承载；只返回现有动作名称入口或既有动作来源。
```

完成后返回：

```text
计划/20260620_因果类外部数据读取纠偏协调计划_v0.1.md
P2 状态类逻辑纠偏计划。
```

## 2. 跨类缺口转向规则

执行本计划时，如果发现当前切片必须依赖其它类新增或补齐功能函数，按以下规则处理：

```text
1. 后续任意类缺少当前切片所需功能函数时，不在动态类或调用方临时拼字段、解释其它类主信息或新增平行结构体。
2. 若对应类已有逻辑纠偏计划，则把当前缺口加入该计划，并放到第一优先位置。
3. 若对应类没有逻辑纠偏计划，则生成 `计划/YYYYMMDD_<类名>逻辑纠偏计划_v0.1.md`，并把当前缺口作为第一优先切片。
4. 新生成或被修改的逻辑纠偏计划必须包含本“跨类缺口转向规则”，保证后续继续发现缺口时仍按同一调度口径处理。
5. 更新 `计划/计划索引.md`：把新生成或新修改的计划临时提升为第一优先计划；原计划和原切片记录为返回点。
6. 只执行新计划的第一优先缺口切片，不顺带推进新计划后续候选项。
7. 第一优先缺口闭合后，立即停止该新计划，更新计划索引返回原计划和原切片。
8. 若第一优先缺口切片不能闭合，保留该计划为当前阻塞，不回到原计划继续补丁化绕过。
```

## 3. 验收

```powershell
rg -n "动态节点主信息类|来源类型|业务语义类型|来源动作|来源动作名|初始状态|结果状态|动态层级" 因果类.cpp
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

## 4. 禁止宣称

```text
不宣称因果类外部数据读取纠偏完成。
不宣称动态类逻辑纠偏完成，除非本计划全部缺口闭合并完成因果类回迁验证。
```
