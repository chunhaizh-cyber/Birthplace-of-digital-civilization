# 20260411 M5-M6 事件模型与最终去向实施计划

状态：已完成（2026-04-11）

## 目标

在 `M1-M4` 已完成基线上，先把文档与规范收口到“当前代码真实状态”，再落地：

- `M5`：最小 `mailbox / 显式唤醒队列`
- `M6`：延迟事实车道与最终去向裁决显式化
- 回归增强：让 `--acceptance-report` 能覆盖事件单次消费、顺序、恢复和最终去向边界

## 当前判断

当前代码已具备：

- `请求 / 单步决策 / 写回结果 / 一步结果` 四层壳
- `主体ID / 分身ID / 方法需求位 / 本能能力缺口 / 触发来源 / 影子验证 / 正式提交门槛`
- 半白盒/白盒验收
- `M4` 恢复重建一次性消费与恢复快照投影

当前已完成并收口：

- `mailbox` 已作为显式单消费者队列存在
- `学习回流 / 外部反馈 / 执行结果 / 恢复重建 / 普通推进` 已统一事件语义
- `延迟事实车道` 已从摘要占位升级成正式待消费事实
- `最终去向裁决` 已显式吃到“消费事件 + 延迟事实 + 根层结果”输入
- `README / 详细设计 / 相关规范` 已回写到 `M1-M6` 已完成口径

## 实施结果

### D1 文档与规范收口（已完成）

已更新：

- `详细设计/任务管理任务详细设计.md`
- `README.md`
- `规范/1213_任务管理一步治理结果最小返回面规范.md`
- `规范/1214_任务管理上下文最小字段表规范.md`
- `规范/1219_任务管理实例堆与恢复快照规范.md`
- `规范/1207_学习任务作为任务管理内部治理子任务规范.md`
- `规范/1266_自我线程主循环最小职责表规范.md`

### M5 最小 mailbox / 显式唤醒队列（已完成）

已落地：

1. `治理唤醒原因 / 治理事件类型 / 治理事件实体`
2. 线程侧最小单消费者 `mailbox`
3. `学习回流 / 外部反馈 / 执行结果 / 恢复重建 / 普通推进` 五类统一事件源
4. 任务管理请求优先消费 `mailbox` 事件
5. `恢复重建` 一次性消费语义

### M6 延迟事实车道与最终去向裁决（已完成）

已落地：

1. 最小延迟事实结构
2. 主循环显式收集：
   - `学习待外层裁决`
   - `等待正式提交放行`
   - `恢复重建待回判`
   - `外部反馈待裁决`
3. 主循环统一“最终去向裁决”函数
4. `继续 / 待机 / 停止 / 收束 / 转入学习 / 再提交` 只由主循环统一拍板

### R1 回归增强（已完成）

`--acceptance-report` 已扩展为 `M1-M6` 共 14 个场景，并支持：

- `--acceptance-repeat=<n>`
- `--acceptance-report-repeat=<n>`

## 验收结果

当前已通过的关键场景包括：

1. 运行态任务治理摘要可见 `主体ID / 分身ID / 需求位 / 本能缺口 / 治理态型 / 触发来源`
2. 自然运行态补方法通道可区分
3. 两个半白盒补原语场景可稳定命中并走影子验证后回滚
4. 白盒补原语/治理本能不会误提正式资产
5. 白盒补方法正式提交闸门会进入 `等待正式提交放行`
6. `M4` 恢复重建与等待态投影通过
7. `M5` 学习回流 / 外部反馈 / 恢复重建事件单次消费通过
8. `M5` 双事件顺序与一步纪律通过
9. `M6` 学习回流待机裁决通过
10. `M6` 外部反馈收束裁决通过
11. `M6` 延迟事实与恢复事件统一裁决通过

最终验收命令：

```powershell
D:\鱼巢\x64\Debug\鱼巢.exe --acceptance-report
```

最终结果：`总体验收: 通过`

## 后续方向

后续重点已经从“事件能否进入主链”转为：

- 事件优先级与更细粒度的队列观测
- 更完整的业务步骤链与学习回流长期策略
- 把当前验收入口继续沉到自动化回归门
