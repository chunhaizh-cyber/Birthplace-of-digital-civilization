# S1d-5f 同类事件段聚合预筛瘦身方案 v0.1

日期：2026-06-08

## 1. 目标

```text
承接 S1d-5e。

S1d-5e 已闭合：
    `标准化动态单元` 平均约 98us，较 S1d-5d 的约 33358us 明显下降。

最新高层证据：
    `创建动态/内部阶段耗时` 18 条；
    最慢阶段分布 `同类事件段聚合=17`、`动态二次特征=1`；
    `同类事件段聚合` 平均约 163712us；
    `动态二次特征` 平均约 60077us。

当前代码判断：
    `尝试生成同类事件段聚合动态` 会扫描场景动态，解析同主体同特征候选；
    `私有_同类事件来源流` 每次比较会提取多个主键字符串；
    已有聚合同源链判断会对同层同聚合方式动态逐个比较来源链；
    `动态路径签名` 已存在，可作为同源链全量比较前的预筛条件。

本切片目标：
    引用和节点比较先用指针快路径，主键比较保留兜底；
    同类事件来源流减少重复字符串键构造；
    已有同类事件段聚合查重先按 `动态路径签名` 预筛，再做来源链精确比较；
    保留场景动态扫描、候选解析、聚合创建和来源链合法性。
```

## 2. 允许修改

```text
二次特征应用模块.ixx
计划/20260607_S1d5f同类事件段聚合预筛瘦身方案_v0.1.md
计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
计划/计划索引.md
实施记录/20260608_S1d5f同类事件段聚合预筛_Codex断点清单.md
```

## 3. 禁止修改

```text
不新增同类事件段全局索引。
不跳过场景动态扫描。
不跳过候选动态解析。
不改变同主体同特征、同来源流、状态值连续、时间顺序或来源链相同的判断语义。
不改变动态创建、二次特征生成、因果回写、任务状态、需求满足、价值结算或外设事实规则。
不新增模拟入口、夹具、伪造样本或 `--*-check` 回归入口。
```

## 4. 实施步骤

```text
S1d-5f-1：
    增加节点主键相等和可解析引用相等的指针优先 helper，用于减少同类事件来源流中的重复字符串键构造。

S1d-5f-2：
    `私有_同一主体特征流` 和 `私有_同类事件来源流` 改用指针优先比较，主键比较保留兜底。

S1d-5f-3：
    `尝试生成同类事件段聚合动态` 在已有聚合查重前计算一次聚合路径签名，先用路径签名预筛，再调用 `私有_聚合来源链相同`。

S1d-5f-4：
    构建 Debug x64，真实运行 `--self-check-log --self-check-log-wait-ms=10000`。
```

## 5. 验证预测

```text
构建：
    Debug x64 构建通过。

真实运行：
    退出码=0。
    日志继续出现 `创建动态/内部阶段耗时`。
    `同类事件段聚合` 平均耗时下降。
    日志保留 `任务状态动作动态=<动态主键>`。
    不出现 `任务状态被动提交/逻辑错误`。
    不出现 `任务状态动作动态逻辑错误`。
    不出现旧路径锚点 `准备本能调用`、`执行本能方法`、`任务域本能输入包`。
```

## 6. 下一步判断

```text
如果 `同类事件段聚合` 明显下降：
    按最新创建动态最慢阶段继续切片。

如果下降不明显：
    转入同类事件段聚合内部阶段定位，不继续凭推断扩大成全局索引。

如果 `动态二次特征` 再次成为主慢点：
    回到动态二次特征生成最新高层分布继续切片。
```

## 7. 落实结果

```text
已落实：
    增加节点主键相等和同来源流引用主键相等 helper；
    `私有_同一主体特征流` 改为指针 / 主键快路径比较；
    `私有_同类事件来源流` 改为引用主键快路径和语素主键比较，减少重复字符串构造；
    `尝试生成同类事件段聚合动态` 对已有同类事件段聚合查重先按 `动态路径签名` 预筛，再做来源链精确比较；
    新建同类事件段聚合动态时复用已计算路径签名，并预留来源低层动态容量。

验证：
    `git diff --check` 通过，仅有 CRLF 工作区转换警告；
    Debug x64 构建通过；
    真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0。

本轮日志窗口：
    最新启动行：`日志/鱼巢_run_20260608.低值g:66962`；
    `任务状态动作动态=` 61 条；
    `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包` 均为 0。

性能证据：
    `创建动态/内部阶段耗时` 14 条，平均约 149066us；
    最慢阶段分布 `动态二次特征=9`、`同类事件段聚合=5`；
    `同类事件段聚合` 平均约 34013us，较 S1d-5e 的约 163712us 明显下降；
    `动态二次特征` 平均约 111211us，重新成为创建动态主慢点；
    `动态二次特征命中回写因果实例/内部阶段耗时` 42 条，`按动态创建因果信息` 平均约 21613us，`命中模板证据动态追加` 平均约 18111us。

结论：
    S1d-5f 已闭合；
    同类事件段聚合预筛有效；
    下一主线回到动态二次特征生成中的因果回写剩余阶段。

下一步：
    进入 S1d-5g：按动态创建因果信息内部阶段定位。
```
