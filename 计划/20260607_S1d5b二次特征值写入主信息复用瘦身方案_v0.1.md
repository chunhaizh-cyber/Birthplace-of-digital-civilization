# S1d-5b 二次特征值写入主信息复用瘦身方案 v0.1

日期：2026-06-08

## 1. 目标

```text
承接 S1d-5a。

S1d-5a 已闭合：
    父节点 + 度量签名缓存已把 `分步签名查找创建` 平均降到约 4121us；
    `子节点枚举` 和 `签名比较` 不再构成当前累计瓶颈；
    当前主慢点转为 `分步值写入`。

最新分步值写入证据：
    `动态二次特征分步值写入/内部阶段耗时` 8 条；
    `分步值写入` 平均约 82869us；
    标量值写入平均约 41488us；
    离散值写入平均约 41381us；
    平均标量 / 离散写入次数均约 76。

本切片目标：
    复用 `创建或更新标量结果` / `创建或更新离散结果` 中已经读取到的 `二次特征主信息类*`；
    避免同一节点在值写入阶段再次通过节点读取主信息；
    旧公开入口保留，但改为调用同一套已取主信息写入实现；
    保留字段写入、观测统计和概念模板刷新规则。
```

## 2. 允许修改

```text
二次特征类.h
二次特征类.cpp
二次特征应用模块.ixx
计划/20260607_S1d5b二次特征值写入主信息复用瘦身方案_v0.1.md
计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
计划/计划索引.md
实施记录/20260608_S1d5b二次特征值写入主信息复用_Codex断点清单.md
```

## 3. 禁止修改

```text
不跳过正式值写入。
不跳过观测统计记录。
不删除合法分步结果。
不改变二次特征值语义。
不改变概念模板匹配规则。
不改任务状态、需求满足、价值结算或外设事实规则。
不新增模拟入口、夹具、伪造样本或 `--*-check` 回归入口。
```

## 4. 实施步骤

```text
S1d-5b-1：
    在 `二次特征类` 增加已取主信息值写入入口，旧 `写入标量值` / `写入离散值` 只负责取主信息后转调。

S1d-5b-2：
    将标量 / 离散值写入主体逻辑统一到已取主信息入口，删除重复实现。

S1d-5b-3：
    `二次特征应用模块.ixx` 的分步结果生成复用已经读取到的 `主信息` 调用新入口。

S1d-5b-4：
    构建 Debug x64，真实运行 `--self-check-log --self-check-log-wait-ms=10000`。
```

## 5. 验证预测

```text
构建：
    Debug x64 构建通过。

真实运行：
    退出码=0。
    日志继续出现 `动态二次特征分步结果/内部阶段耗时`。
    日志继续出现或允许出现 `动态二次特征分步值写入/内部阶段耗时`。
    `分步值写入` 平均耗时下降，或至少 `二次特征值写入/内部阶段耗时` 不新增主慢点。
    日志保留 `任务状态动作动态=<动态主键>`。
    不出现 `任务状态被动提交/逻辑错误`。
    不出现 `任务状态动作动态逻辑错误`。
    不出现旧路径锚点 `准备本能调用`、`执行本能方法`、`任务域本能输入包`。
```

## 6. 下一步判断

```text
如果 `分步值写入` 明显下降：
    按最新分步结果或高层动态二次特征最慢阶段继续切片。

如果下降不明显：
    说明主因仍是合法结果数量聚合或概念模板 / 观测统计外的固定调用成本；
    不削减合法结果数量，改为只读复核是否应停止分步值写入支线。

如果高层主慢点转回 `命中回写因果实例`：
    停止分步值写入支线，转回因果实例回写阶段。
```
