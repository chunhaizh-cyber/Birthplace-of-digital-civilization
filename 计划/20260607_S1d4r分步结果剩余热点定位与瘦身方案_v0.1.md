# S1d-4r 分步结果剩余热点定位与瘦身方案 v0.1

日期：2026-06-08

## 1. 目标

```text
承接 S1d-4q。

S1d-4q 已闭合：
    分步语义绑定平均耗时从约 208468us 降到约 83632us；
    最新运行线程 T21484 中，分步结果最慢阶段分布为 `分步语义绑定=2`、`分步值写入=2`；
    动态二次特征生成主耗时仍为 `分步结果生成`。

当前判断：
    `分步值写入` 内部包含字段写入、观测统计和概念模板刷新；
    `写入标量值` / `写入离散值` 每次都会刷新概念模板；
    统计观测和概念模板归属属于正式二次特征账，不能无证据跳过。

本切片目标：
    先拆分 `写入标量值` / `写入离散值` / `刷新概念模板` 内部耗时；
    找出剩余主耗时是否在字段写入、记录观测、度量链根获取、概念名称反查、差值命中或概念模板写回；
    若证据显示可安全瘦身，再做最小快路径。
```

## 2. 允许修改

```text
二次特征类.cpp
二次特征应用模块.ixx
计划/20260607_S1d4r分步结果剩余热点定位与瘦身方案_v0.1.md
计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
计划/计划索引.md
实施记录/20260608_S1d4r分步结果剩余热点定位_Codex断点清单.md
```

## 3. 禁止修改

```text
不删除动态二次特征生成。
不跳过正式二次特征值写入。
不跳过观测统计，除非真实证据和规范确认它只是重复缓存。
不跳过概念模板刷新，除非能证明概念模板已指向同一节点且值 / 语义未变化。
不改变二次特征主信息结构。
不改任务状态、需求满足、价值结算或外设事实规则。
不新增模拟入口、夹具、伪造样本或 `--*-check` 回归入口。
```

## 4. 实施步骤

```text
S1d-4r-1：
    在 `二次特征类.cpp` 增加慢采样内部阶段日志：
        `二次特征值写入/内部阶段耗时`
        `二次特征概念模板刷新/内部阶段耗时`

S1d-4r-2：
    `写入标量值` / `写入离散值` 拆分：
        主信息读取；
        字段写入；
        记录观测；
        刷新概念模板。

S1d-4r-3：
    `刷新概念模板` 拆分：
        主信息读取；
        签名准备；
        度量链根获取；
        概念名称反查；
        差值命中；
        概念模板写回。

S1d-4r-4：
    构建 Debug x64，真实运行 `--self-check-log --self-check-log-wait-ms=10000`。
```

## 5. 验证预测

```text
构建：
    Debug x64 构建通过。

真实运行：
    退出码=0。
    日志继续出现 `动态二次特征分步结果/内部阶段耗时`。
    日志出现 `二次特征值写入/内部阶段耗时` 或 `二次特征概念模板刷新/内部阶段耗时`。
    日志保留 `任务状态动作动态=<动态主键>`。
    不出现 `任务状态被动提交/逻辑错误`。
    不出现 `任务状态动作动态逻辑错误`。
    不出现旧路径锚点 `准备本能调用`、`执行本能方法`、`任务域本能输入包`。
```

## 6. 下一步判断

```text
如果主耗时在概念模板刷新：
    继续拆或瘦身概念模板刷新，优先判断“值/概念模板未变时是否可跳过反查和写回”。

如果主耗时在记录观测：
    判断统计记录是否有重复写入快路径，不能删除统计账。

如果主耗时在签名查找 / 创建：
    另起 S1d-4s，处理子二次特征按签名查找。

如果新增日志本身改变性能主耗时：
    保留定位结论，优先减少日志采样阈值或停止在证据。
```

## 7. 落实结果

```text
已落实：
    `二次特征类.cpp` 增加 `二次特征值写入/内部阶段耗时` 和 `二次特征概念模板刷新/内部阶段耗时` 慢采样；
    `写入标量值` / `写入离散值` 已拆分主信息读取、字段写入、记录观测、刷新概念模板；
    `刷新概念模板` 已拆分主信息读取、签名准备、度量链根获取、概念名称反查、差值命中、概念模板写回；
    `刷新概念模板` 保留 `获取或创建度量链根`，仍由该入口确保默认概念刻度；
    概念名称反查和差值命中复用同一次子二次特征枚举，删除本函数内重复公开 helper 调用造成的重复枚举。

验证：
    `git diff --check` 通过，仅有 CRLF 工作区转换警告；
    Debug x64 构建通过；
    真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0。

本轮日志窗口：
    最新启动行为 `2026-06-08 10:58:56.127 [T31648] 程序启动`；
    `任务状态动作动态=` 51 条；
    `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包` 均为 0。

性能证据：
    `动态二次特征分步结果/内部阶段耗时` 5 条，最慢阶段全部为 `分步语义绑定`；
    分步结果平均总耗时约 184478us，`分步语义绑定` 平均约 88482us，`分步值写入` 平均约 44281us，`分步签名查找创建` 平均约 33486us；
    `动态二次特征生成/内部阶段耗时` 19 条，最慢阶段分布为 `分步结果生成=13`、`条件候选收集=3`、`命中回写因果实例=3`；
    动态二次特征生成平均总耗时约 448683us，`分步结果生成` 平均约 192838us，`条件候选收集` 平均约 163720us。

结论：
    `分步值写入` 已从 S1d-4q 约 66058us 降到本轮约 44281us；
    `分步结果生成` 仍是动态二次特征生成中最频繁慢阶段；
    分步结果剩余慢点重新集中到 `分步语义绑定`。

下一步：
    进入 S1d-4s：分步语义绑定剩余阶段定位 / 瘦身。
```
