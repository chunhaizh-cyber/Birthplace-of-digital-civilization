# 计划索引

更新时间：2026-05-19

## 0. 索引用途

```text
本文件只记录当前可执行计划、阻塞项、最近完成项和索引维护规则。
历史完成细节以 计划/完成项 下的计划正文为准，不在索引中重复展开。
过期口径、旧链路说明、观测字段阶段完成说明，不再作为当前执行依据。
```

## 1. 当前执行顺序

### 1.0 当前执行顺序

| 顺序 | 计划 | 状态 | 当前处理口径 |
| --- | --- | --- | --- |
| 1 | `20260518_状态转换因果树与目标投影层级生成计划_v0.2.md` | 首轮可验证项完成，V1.0 后置能力等待真实运行期 | 安全 / 服务结算长期规范要求父链归因、世界树因果证据、结算贡献层级、目标实际达成比例和副作用审查；但当前项目尚不具备真实运行条件，本阶段不把执行风险、副作用审查、竞争原因和净贡献抵扣作为实施闸门。该计划首轮已根据世界树根链上的状态节点、动作动态、因果模板、因果实例、状态变化线索，以及任务 / 需求父链生成“世界状态转换因果树 + 目标投影层级”的只读投影，用于判定叶子任务改了什么、属于哪类权重、在对应权重下是第几层结算贡献。安全 / 服务 / 学习 / 自维护都只是目标投影，安全值变化只是账务状态转换投影，不是因果树总根。该步骤只读，不直接结算，不写需求树、不写任务状态、不生成动作动态。v0.4 结构接口版已落地：已定义状态转换因果节点 / 边、目标投影路径、叶子任务因果投影预判结构，并在因果类实现只读投影接口。v0.7a 已把投影预判接入任务价值反馈结算前路径：目标宿主 / 目标特征缺失会阻断结算。v0.7b 已移除按特征主键文本猜测安全 / 服务 / 方法 / 任务 / 学习维度的旧逻辑，改为只按规范特征类型语义入口、特征类型指针等价关系和来源需求方向判定；目标特征主键只作为身份，不作为语义。v0.7c 已把“缺正式投影路径 / 缺 D0-D1-actual 闸门证据”从日志提示升级为 D2 价值结算硬闸门：缺正式证据时任务价值反馈不再调用 `结算叶子任务价值`，但不阻断需求满足回写；已新增并通过 `--formal-settlement-gate-check`。v0.7d 已接入 `结构_叶子任务正式结算证据` 与只读采集函数：从当前方法运行存在 / 输出结果场景读取 D0 与输出场景，从输出场景或任务结果摘要读取 actual，并记录 before / target / actual满足目标 / actual有推进 / 缺失证据；当前仍保持 D2 硬闸门，不开放结算。v0.7e 已将 `回写需求满足` 线程侧调用升级为结构化返回，并从运行存在读取 D1 需求满足动作动态；读不到 D1 视为回写失败。v0.7f 已完成 `20260518_D0D1Actual正式结算路径替换过渡路径方案_v0.1.md` 的 P5 首轮：任务满足反馈路径先采集 D0 / actual，actual 满足目标时先调用 回写需求满足 生成 D1，再把 D0 + actual + D1 证据包交给 D2 正式入口；旧数值反馈仅作为候选额度输入，不能绕过正式证据闸门。v0.7g 已把 `--subtask-parent-flow-check` 从旧结果摘要夹具升级为正式叶子业务动作证据夹具，并修正 actual 采集优先取 D0 结果状态 / 场景最新同目标状态；正式 D0 + actual + D1 链路已能进入 D2。v0.9a 已新增 `--state-transition-causal-projection-check` / `--safety-causal-tree-check`：构造 D0 物理安全事实边和 D2 安全值账务边，验证状态转换因果投影节点 / 边、账务边不冒充物理安全边、安全一层、服务任务二层、方法安全三层与方法维护一层预判。V1.0 第 9 件中的防重复结算首轮已落地：自我线程用 `任务 + 来源需求 + D0业务动作动态 + 目标宿主 + 目标特征类型` 形成正式结算去重键，同一 D0 对同一目标特征已真实入账后，后续反馈跳过去重，不再重复调用 `结算叶子任务价值`。V1.0 第 15 件 I64_MAX 权重哨兵闸门已由独立命令 `--formal-settlement-i64max-sentinel-check` 覆盖：该命令构造正式 D0 + actual + D1 证据链，任务承接安全权重上限为 I64_MAX 时视为未设置 / 异常哨兵，安全值和服务值均不得真实入账，且不依赖父任务重筹办观察。V1.0 第 11 件固定点整数比例首轮已由 `--formal-settlement-fixed-point-ratio-check` 覆盖。Debug x64 构建通过，`--formal-settlement-fixed-point-ratio-check`、`--formal-settlement-i64max-sentinel-check`、`--formal-settlement-gate-check`、`--state-transition-causal-projection-check`、`--subtask-parent-flow-check`、`--demand-tree-growth-check` 通过。预算守恒完整拆分、副作用审查、竞争原因和净贡献抵扣仍后置。 |
| 2 | `20260518_主信息类型按符号三态比较规范收口计划_v0.1.md` | P2 / P4 / P5 / P6 / P7 / P8 / P9 首轮完成，新增隔离回归已接入并通过 | 该计划由 004 上级基础规范触发，目标是把二次特征、因果、方法结果能力、需求 / 任务满足判断和各主信息 `比较(...)` 语义统一到“有符号比较量 <0 / =0 / >0”。当前已完成 P0/P1 和 P3 首轮落地：新增 `比较量` 工具层，二次特征主信息类以 `允许方向` 承载比较量符号命中约束，差值改为安全计算；已按“三态比较协议不是数据结构”口径删除 `二次特征主信息类::比较量缓存` 和 `二次特征主信息类::置信度`。P2 已拆开命中与比较量入口：链表当前链查找优先调用 `命中比较条件(...)`，旧 `比较(...) == 1` 约定已收口到 `私有_旧比较接口命中(...)`；基础信息 / 语素 / 需求 / 任务 / 方法 / 特征值主信息已新增 `生成比较量(...)` 与 `命中比较条件(...)`，其中需求 / 任务 / 方法主信息类旧 `比较(...)` 已改为委托 `生成比较量(...)`，不再保留 1/0 命中语义；源码中已无裸 `比较(...) == 1` / `比较(...) != 1` 命中判断。P4 首轮已把因果主信息字段收口到比较模板命名：`条件比较模板 / 因比较模板 / 果比较模板 / 主果比较模板`；旧 `追加条件模板(...)` / `追加状态迁移模板(...)` 函数名暂作为兼容 API 保留，但内部读写新字段。P5 已复核方法结果能力表达：候选召回和评分读取 `结构_方法结果关系模板` 的目标特征类型、参数化目标、二次特征模板和允许方向；旧 `枚举_结果变化方向` 仅作为兼容输入 / 显示口径。P6 已复核需求 / 任务满足判断：任务完成由实际结果状态与目标状态比较、满足关系和满足证据支撑；有推进只写本轮推进量 / 有推进未达成，不推出完成；动作成功无可比较目标结果会记录诊断并等待重筹办。P7 已收口动态 / 因果生成入口：状态转换因果边类型不再读取 `动态业务语义类型` 或标题片段推导结果，而是由目标特征、来源类型、来源动作和状态转换事实判定；二次特征同类事件聚合不再用动态业务语义参与分组。P8 已把 060 因果补完 / 因果用途 / 状态转换因果树规范和 040 方法规范中的旧 `主果状态迁移`、`因状态迁移模板`、`果状态迁移模板` 等权威口径迁移为 `主果比较模板`、`因比较模板`、`果比较模板`，并明确生成 / 消除 / 增强 / 减弱等词只作为显示层或查询层临时命名。P9 已完成 Debug x64 构建、基础回归和新增隔离回归：`--demand-difference-refresh-check`、`--demand-difference-child-check`、`--demand-difference-child-exit-check`、`--demand-tree-growth-check`、`--task-method-multicandidate-selection-check`、`--signed-tristate-comparison-check`、`--secondary-feature-comparison-template-check`、`--causal-comparison-template-check`、`--method-result-comparison-ability-check` 均通过。最新口径确认：`方向区间` 是三大比较方向内部的量域细分字段，`差值` 是可用于距离、余量和多次特征生成的关系量字段，二者均需保留且不与三态比较协议冲突；`是否满足` 与 `动态业务语义类型` 仍为兼容 / 显示缓存。后续只剩尚未迁移类型的旧 `比较(...)` 返回 1/0 语义清理，以及更大范围历史规范自然语言迁移。 |
| 3 | `20260514_特征值差距刷新成子需求闭环剧本计划_v0.1.md` | 差距子需求闭环与叶子结算回流已完成，后置等待投影 / 结算后续 | 已验证根需求当前 / 目标状态差距可自动生成或刷新同目标执行子需求，且子需求字段完整、同目标复用、已任务化；二次缩差已验证同一子需求镜像刷新，活动期间权重保持单位值 1；差距消失后子需求退出活动集已通过；子任务完成后叶需求收束、权重归零、父任务重筹办已通过；叶子任务完成后安全值可通过结算本能方法入账，服务反馈不进入真实服务值结算。下一步需等待状态转换因果树投影提供权重类型、结算贡献层级和因果置信度；副作用审查输入后置，再继续结算公式与学习任务安全贡献闭合。 |
| 4 | `20260510_自我内部循环最小生命闭环阶段总计划_v0.1.md` | 历史通过，当前因允许缺失自我动作组合而降级等待 | `--internal-loop-check` / `--internal-loop-multirun-check` 曾在差距执行子需求接入后通过；当前最新复跑停在“缺少可直接绑定的自我动作组合 / 等待因果反推或动作序列入口规则”，因此不再把该历史结果作为当前闭合证据。此降级不证明代码错误，也不证明自我苏醒或初步成熟。 |
| 5 | `20260510_自我内部循环P2b需求树生成成立计划_v0.1.md` | 差距子需求、父任务回流通过；多轮闭合等待动作组合 | 需求树已能通过当前“任务化、父子权重、任务反馈退出活动集、主动同目标复用、根需求差距镜像与权重刷新、差距子需求生成 / 复用 / 二次缩差镜像刷新、差距消失退出活动集、子任务完成父任务回流”等硬回归；当前不再把多轮稳定态采样列为已通过项，需等待“因果反推自我动作组合 / 动作序列入口规则”补齐后复验。 |
| 6 | `20260510_需求树正常生长特征链与转换落地计划_v0.1.md` | 当前硬回归通过，待扩展后续闭合剧本 | 当前可控剧本已通过；多候选方法选择评分排序规则已接入合成回归并通过；继续补学习需求正式入树剧本、自然多候选样本稳定复现、叶子任务完成直接结算与父链回流细化剧本。 |
| 7 | `20260510_自我苏醒阶段验收与稳定治理计划_v0.1.md` | 后置等待 | 必须等待内部循环真实闭合；当前不得判定自我苏醒。 |
| 8 | `20260510_因果补完学习探索与初步成熟计划_v0.1.md` | 后置等待 | 必须等待自我苏醒；当前不得判定因果补完期或初步成熟。 |
| 9 | `20260510_因果补完C4学习需求正式入树计划_v0.2.md` | 当前执行，方法动作 / 可执行入口闭合已拆成自然观察与隔离回归 | 第一版正式学习需求当前只承认候选方法不可用对应的 `方法状态=可用`、以及明确缺条件结果对对应的条件结果对补齐；“无既有语义候选方法”降级为候选填充安全兜底。参数 / 条件补齐需求不归入学习需求。`--learning-demand-execution-entry-check` / `--learning-demand-execution-entry-closure-check` 已按新口径改为反向验收：明确缺执行入口不得生成正式学习需求，也不得由 `自我_定向学习` 伪闭合。旧 `--learning-demand-entry-check`、`--learning-demand-placeholder-followup-check` 在当前自然分支下不再作为通过项，因为当前流程先停在“缺自我动作组合”。`--learning-demand-condition-result-check` 已降级为自然观察回归：未读到自然真实入口样本时返回“等待真实动作入口 / 无自然样本”诊断并通过，不作为 CI 硬失败项，且不写假目标宿主、假目标特征、假父需求或假父任务。已新增并通过 `--reverse-action-combination-single-instinct-check`、`--submit-method-entry-from-reverse-single-instinct-check`、`--condition-result-gap-after-real-entry-check`、`--condition-result-demand-entry-after-real-entry-check` 四条隔离回归，证明只读反推可返回合法单步本能候选、明确单步本能 ID 可提交为真实动作句柄、真实入口后可识别明确缺条件结果对，并可经自我线程正式派生条件结果对学习需求入树、任务化、选中 `自我_尝试学习` 且父任务等待；但这些仍是合法夹具隔离回归，不证明自然流程已经稳定产出真实入口样本，也不证明学习子任务完成、条件结果对建立或父任务最终就绪。当前继续执行时不能伪造入口数量、不能绑定 `改变特征值`、不能把 `自我_定向学习` 当作内部方法入口闭合路径。 |
补充同步：`20260518_状态转换因果树与目标投影层级生成计划_v0.2.md` 已完成 V1.0 第 15 件中的 I64_MAX 权重哨兵闸门小步，并新增独立隔离命令 `--formal-settlement-i64max-sentinel-check`；该命令验证正式 D0 + actual + D1 证据采集成立、任务承接安全权重上限为 I64_MAX 时视为未设置 / 异常哨兵、安全值与服务值均不得真实入账，且不调用、不依赖 `--subtask-parent-flow-check` 的父任务重筹办观察。

补充同步：`20260518_状态转换因果树与目标投影层级生成计划_v0.2.md` 已把 V1.0 第 9 件“同一 D0 不得重复结算同一目标特征”接入 `--subtask-parent-flow-check`；同一 D0 再次反馈会命中正式 D0 目标去重，安全值不得二次入账。I64_MAX 哨兵闸门已由 `--formal-settlement-i64max-sentinel-check` 独立覆盖，不混入父子回流主剧本。

补充同步：`20260518_状态转换因果树与目标投影层级生成计划_v0.2.md` 已完成 V1.0 第 11 件“整数计算安全规则”首轮：自我线程正式结算路径中的安全乘除函数已由 `long double` 改为 128 位整数乘除或安全整数回退路径；新增独立命令 `--formal-settlement-fixed-point-ratio-check`，验证正式 D0 + actual + D1 + D2 路径下 `floor(7 * 333333 / 1000000) = 2`，安全净变化与任务已折算安全值均为 2。已复跑 `--formal-settlement-fixed-point-ratio-check`、`--formal-settlement-i64max-sentinel-check`、`--formal-settlement-gate-check`、`--state-transition-causal-projection-check`、`--subtask-parent-flow-check`、`--demand-tree-growth-check`。

### 1.1 当前实际执行边界修订（2026-05-19）

```text
状态转换因果树与目标投影层级计划：
    v0.4 结构接口、v0.7 正式结算接入、v0.9a 投影诊断、防重复结算、I64_MAX 哨兵闸门和固定点整数比例首轮均已有隔离回归覆盖。
    当前不再把“能否生成状态转换因果树 / 目标投影层级”作为第一硬阻塞。
    剩余预算守恒完整拆分、副作用审查、竞争原因、净贡献抵扣和 DAG 环检测 / 深度去重完整化，归入真实运行期后置能力，不阻塞当前计划索引继续推进。

主信息类型按符号三态比较规范收口计划：
    源码级裸 `比较(...) == 1` / `比较(...) != 1` 命中判断已无剩余命中。
    当前剩余主要是兼容 API 逐步收口和历史规范自然语言迁移，不作为当前第一阻塞点。

当前第一阻塞点调整为：
    自我动作组合 / 真实动作入口自然样本尚未闭合。

当前执行方向：
    继续沿 `20260510_因果补完C4学习需求正式入树计划_v0.2.md`
    与 `20260516_方法可执行入口真实动作来源规则计划_v0.1.md` 推进；
    不把 `--learning-demand-condition-result-check` 作为硬通过目标；
    先补自然流程中从因果反推自我动作组合、真实动作入口、再到条件结果对学习需求的前置链路。
```

### 1.2 当前实际执行顺序修订（2026-05-19）

| 顺序 | 计划 | 当前状态 | 执行口径 |
| --- | --- | --- | --- |
| 1 | `20260510_因果补完C4学习需求正式入树计划_v0.2.md` | 当前实际第一执行项 | 继续补自然流程的真实动作入口 / 自我动作组合前置；`--learning-demand-condition-result-check` 只作自然观察，不作硬通过目标。 |
| 2 | `20260516_方法可执行入口真实动作来源规则计划_v0.1.md` | 与 C4 并行支撑 | 继续保持单步本能入口严格门控；后续若进入多步动作组合，只能先作为只读候选，写成动作序列入口时再走提交方法。 |
| 3 | `20260518_状态转换因果树与目标投影层级生成计划_v0.2.md` | 首轮可验证项完成 | 预算守恒、副作用、竞争原因、净贡献抵扣和 DAG 完整去重后置；不再作为当前第一阻塞点。 |
| 4 | `20260518_主信息类型按符号三态比较规范收口计划_v0.1.md` | 首轮完成 | 源码级裸旧比较命中已无剩余；后续只做兼容 API 和历史规范语言迁移。 |

## 2. 当前阻塞项

```text
已解除阻塞：
    原第一阻塞点“需求树真实生长”已通过当前硬回归，不再作为当前阻塞项。
    当前已通过特征值差距刷新成子需求剧本的生成 / 复用 / 任务化 / 二次缩差镜像刷新 / 差距消失退出活动集 / 子任务完成父任务回流部分。
    内部循环最小闭环与多轮稳定态曾在早期链路下通过；但最新代码已按“允许缺失自我动作组合”口径停在等待态，当前不得继续引用历史多轮稳定通过作为闭合证据。

具体表现：
    1. 同一目标特征的主动复用 / 合并剧本已接入 `--demand-tree-growth-check` 主动检查。
    2. 特征值差距镜像与根权重刷新已由 `--demand-difference-refresh-check` 验证通过；差距自动生成 / 刷新子需求已由 `--demand-difference-child-check` 验证通过，覆盖子需求生成、字段完整、任务化、同目标复用和二次缩差镜像刷新。
    3. 任务方法绑定已改为“先入候选集，再从候选选择；单候选直接绑定”；多候选选择评分排序规则已由 `--task-method-multicandidate-selection-check` 合成回归验证通过。
    4. 根需求权重已改为安全 / 服务活动单位权重；安全 / 服务压力保留为运行时方向评分，根需求当前 / 目标状态镜像已随需求列表刷新；
       同目标复用 / 合并已接入重复目标风险结构化回归检查和主动复用剧本；
       活动执行需求任务化已接入硬回归；
       父子权重刷新已接入硬回归；
       任务反馈退出活动集已接入硬回归。
    5. 自我苏醒和初步成熟必须等待内部循环闭合，不能提前验收。

当前阻塞点：
    1. 自我动作组合 / 真实动作入口自然样本尚未闭合。
       原因：条件结果对学习需求需要已有真实动作入口作为前置；当前自然流程仍可能停在“缺少能造成 A -> B 的自我动作组合 / 动作序列入口规则”之前。
       已完成：单步本能反推、提交真实入口、真实入口后识别明确缺条件结果对、真实入口后条件结果对学习需求正式入树 / 任务化四条隔离回归已经通过。
       最新复跑：`--learning-demand-condition-result-closure-check` 失败，失败点为自然命令未读到条件结果对学习需求闭合对象（有学习需求=否、学习任务=空、父需求=空、父任务=空）。
       当前执行方向：继续补自然流程中的自我动作组合 / 真实动作入口前置，以及学习子任务完成 -> 条件结果对建立 -> 父任务继续推进的闭合段；不伪造入口数量，不绑定 `改变特征值`，不使用 `自我_定向学习` 伪闭合内部方法入口。
    2. 背景保留：世界状态转换因果树 / 目标投影层级剩余后置项。
       原因：安全 / 服务结算规范长期要求“父链归因 + 世界树因果证据 + 结算贡献层级 + 目标实际达成比例 + 副作用审查”。当前项目尚不具备真实运行条件，本阶段先不把副作用审查作为实施闸门；即使学习任务继承很大的安全承接权重，只要能明确其属于第三层安全贡献，结算比例也会自然衰减，不会大幅稀释直接安全任务。但前提是必须清楚该任务的目标投影、权重类型和安全结算贡献层级，且不能把安全因果距离层级直接当成结算贡献层级。
       当前执行方向：
           从世界树根链上的状态节点、因果模板、动作动态、状态变化线索、因果实例，以及任务 / 需求父链构造全局状态转换因果投影；
           按安全 / 服务 / 学习 / 自维护等目标维度生成只读投影路径；
           输出权重类型、目标投影路径、安全因果距离层级、结算贡献层级、可结算路径、因果置信度和缺失证据；
           副作用、竞争原因和净贡献抵扣作为真实运行期后置能力，不阻塞当前 D0-D1-actual 正式证据链替换。
           不直接写安全值、不写需求树、不写任务状态、不生成动作动态。
       已形成计划：
           `20260518_状态转换因果树与目标投影层级生成计划_v0.2.md`。
       已形成规范：
           `规范/状态转换因果树与目标投影层级规范20260518.md`，规范编号 060005。
       V1.0 路线：
           v0.2 概念修正版 -> v0.4 结构接口版 -> v0.7 结算接入版 -> v0.9 回归诊断版 -> V1.0 可定稿版。
    2. 学习需求正式入树自然三类剧本能否稳定闭合。
       重点验证：候选查找必须排除 `改变特征值` 这类先天特征写入逻辑；若无语义方法命中，任务筹办应创建面向目标特征的占位方法头并加入候选集，后续围绕该方法的 `方法状态=可用`、执行入口、条件结果对等具体缺口生长需求。
       已撤销旧通过口径：此前“明确缺方法动作 -> `方法动作状态` 学习子需求入树并选中 `自我_定向学习`”只证明旧映射链路可跑通，不能作为当前闭合依据。当前条件下若该分支仍选中 `自我_定向学习`，唯一合理解释是尚未找到能够让目标状态 A -> B 的自我动作组合，或该动作组合 / 方法尚未声明可命中这些结果特征。
       兼容口径：若自然命中分支为候选方法不可用 -> `方法状态=可用`，应由 `自我_尝试学习` 承接；若命中条件结果对状态，也应由 `自我_尝试学习` 承接。
       已确认子项：`改变特征值` 已按 D 口径下沉为先天写入逻辑。
           原因：它是所有语义方法共享的底层特征提交原语，不是任务可选择方法，不得作为动作动态来源。
           后续检查重点改为：候选集不得包含先天写入逻辑；无语义候选时必须创建占位方法头；占位方法的可用性、执行入口、条件结果对缺口必须进入具体学习需求，而不是通过通用写入逻辑直接设值。
       已补子项：无语义候选占位方法后的具体学习需求识别逻辑。
       当前复跑口径：若自然流程先停在“缺自我动作组合 / 真实动作序列入口”，则该识别逻辑可能没有自然样本可读，按允许缺失记录，不作为闭合失败。
       待补子项：候选方法不可用闭合后的父任务重筹办 / 就绪闭合、明确缺条件结果对自然正式入树剧本、无语义候选占位方法后的具体学习需求闭合。
       当前新增阻塞：条件结果对自然正式入树剧本需要先越过真实可执行入口缺口；`--learning-demand-condition-result-check` 仅作为自然观察回归，未读到真实入口样本时返回等待诊断并通过，不再作为硬失败项。隔离回归已证明单步本能反推、提交真实入口、真实入口后识别明确缺条件结果对三段成立；但尚不证明自然正式学习需求已挂父需求并任务化。现有 `自我_定向学习` 只能补出方法动作语义痕迹，不能合法生成真实动作句柄。当前内部循环中出现定向学习，不是应被强行禁止的逻辑分支，而是尚未找到能造成 A -> B 的自我动作组合的诊断信号；不能通过写 `方法可执行入口数量=1` 伪造完成。
       判定标准补充：并非所有操作都需要本能方法；因果反推、候选筛选、诊断、评分、排序、预测、回执整理等只读中间过程可以由普通逻辑代码实现。只有真正持久改变方法、任务、需求或世界树特征状态的提交段，才需要明确动作来源和动作动态。
       已形成子计划：`20260516_方法可执行入口真实动作来源规则计划_v0.1.md`。
       当前执行方向：先用普通逻辑接口从因果信息反推能造成目标状态的自我动作组合；若组合只有一个已注册本能方法，后续由方法域提交动作绑定本能函数 ID；若组合包含多个动作，可先作为中间候选返回，只有写成动作序列入口时才需要提交动作，不退给 `自我_定向学习` 伪闭合。
       2026-05-16 P1 复核结果：
           当前已有 `结构体_动作句柄` 可承载 本能函数ID / 外部实现主键 / 动作序列主键；
           真实执行桥当前只支持 本能函数ID；
           尚未发现正式特征或接口可表达“能造成 A -> B 的自我动作组合已确定 / 自我可产生状态集 / 动作可诱发判定 / 动作组合结果可验收证据”；
           因此不得新造结构体或伪造入口；已先实现“因果反推自我动作组合”的只读最小接口，持久写入表达另行确认。
       2026-05-16 P2b 实现结果：
           `因果类::反推自我动作组合(...)` 已接入编译；
           该接口只读因果模板和方法树，返回无目标 / 无匹配 / 有因果无映射 / 单步本能 / 多候选 / 动作序列待定义等状态；
           不写方法、任务、需求或世界树特征，不生成动作动态；
           Debug x64 非增量链接构建通过。
       2026-05-16 P2b.1 接入结果：
           任务筹办中的“缺方法动作 / 缺可执行入口”分支已调用 `因果类::反推自我动作组合(...)`；
           筹办日志会记录来源需求、目标结果状态、方法根、反推状态、候选数量和前 3 个候选；
           本轮仅更新筹办结果说明 / 回执摘要，不写方法动作句柄、不写任务状态、不生成正式学习需求；
           `--learning-demand-execution-entry-check` 与 `--learning-demand-execution-entry-closure-check` 通过。
       2026-05-16 P4 实现结果：
           已新增 `自我_提交方法可执行入口` 本能方法；
           该方法只在输入包含明确单步本能方法 ID 时提交目标方法动作句柄；
           显式拒绝 `自我_改变特征值`、历史任务筹办 / 任务执行生命周期编号 6062 / 6063、非默认自我本能方法、以及与既有动作句柄冲突的入口；
           成功时写入 `方法动作状态=1`、输出可验收实际结果状态，并以 `自我_提交方法可执行入口` 为动作来源生成方法域动作动态；
           不直接把方法状态设为可用，仍需条件结果对与方法状态提交链；
           Debug x64 非增量链接构建通过；
           `--learning-demand-execution-entry-check` 与 `--learning-demand-execution-entry-closure-check` 通过。
       2026-05-16 计划修订：
           已按上级规范明确“中间过程普通逻辑、持久提交才方法化”；
           当前执行不得为因果反推、候选筛选、诊断或评分新增本能方法；
           后续只在写入方法动作句柄、动作序列入口、正式学习需求、任务回执或任务状态时进入对应提交方法。
       2026-05-16 追加学习启动门控：
           尝试学习 / 练习不得仅因存在学习需求而启动；
           必须具备“尝试/练习 -> 因果证据 -> 因果模板补全或稳定度提升 -> 方法改善 -> 父任务解阻 -> 父需求推进”的学习收益因果链；
           练习需要已有完整因果模板且稳定度不足；
           尝试学习需要候选因果和可安全尝试的条件或动作；
           缺动作且无候选动作时，只能生成寻找候选动作或外部信息的候选，不能启动尝试或练习。
    2. 自然多候选样本出现时，候选集完整填充 -> 当前方法选择能否按同一确定性规则稳定复现。
       当前已完成：`--task-method-multicandidate-selection-check` 验证评分排序优先级为 直接满足 > 固定命中 > 禁止项少 > 风险/冲突少 > 效率高 > 耗时/能耗/资源/副作用少 > 可补齐 > 不确定性少 > 稳定性高。
       后续只在自然任务确实产生多候选样本时验证候选集完整填充，不为回归强造治理结构。
    3. 内部循环最小闭环 / 多轮稳定态需在“因果反推自我动作组合”或“动作序列入口规则”补齐后重新复验。
       当前最新复跑结果：
           `--internal-loop-check` 未出现崩溃或任务状态裸写错误，但因没有完成任务和已满足需求而失败；
           `--internal-loop-multirun-check` 任务全部收束为等待态，但自我线程仍持续提交，最近说明为“缺少选中自我能力值或可执行方法”；
           该失败与当前允许缺失项一致，不能反向判定需求树真实生长失效。

已确认但后置执行项：
    1. 学习需求正式入树第一版自然剧本只覆盖三类：候选方法不可用、明确缺执行入口、明确缺条件结果对。
       “无既有语义候选方法”保留为候选填充安全兜底：若完整遍历并排除先天写入逻辑后候选集仍为空，任务筹办创建面向目标特征的方法头并返回；后续围绕该方法的具体缺口生长学习需求。
    2. 学习需求挂在触发它的父需求下面，不挂根需求，也不建全局学习树。
    3. 学习需求权重不再来自父需求 / 父任务剩余压力；正式活动学习需求对应方向活动权重为 1，非活动为 0；`I64_MAX` 不得进入真实服务结算。
    4. 参数 / 条件补齐需求归入筹办条件需求，不归入学习需求。
    5. `存在类::取存在主信息 / 获取需求根节点 / 获取任务根节点 / 获取方法根节点` 需要拆分只读读取与显式确保，避免只读诊断触发隐式创建。
    6. `20260516_因果信息用途接口落地计划_v0.1.md` 已创建为后置计划；它规定因果学习六小闭环接口以及因果解释、预测、反推、需求候选、执行审查、执行验收和稳定度更新的接口落地顺序，不进入当前第一执行顺序。
    7. 安全推进当前不走“自检完成 -> 物理安全值直接增加”的旧路径。
       当前确认的安全闭环顺序为：
           因果系统最小沉淀
           -> 自检产生安全 / 方法状态观测动态
           -> 因果系统沉淀状态变化线索或观测因果模板
           -> 安全特征刷新
           -> 安全需求候选 / 正式安全子需求
           -> 后续寻找真正能结算安全值的叶子任务。
       自检可以检查方法状态、方法动作状态、可执行入口、条件结果对、运行账和好用度；
       其直接产物是方法维护信息、安全观测信息或不确定性下降证据，不得直接提交 `方法状态=可用`，也不得直接结算物理安全值。
       同时确认：自检发现的问题都属于安全问题；解决这些问题会增加安全值。
       标准链路是：
           自检发现问题
           -> 正式安全子需求入树
           -> 叶子任务解决问题
           -> 回写需求满足
           -> 结算叶子任务价值
           -> 安全值增加。
       因此当前实现不得把“自检发现问题”提前结算为安全值，但后续必须允许“问题已解决”进入安全值结算。
       结算方法定位：
           `结算叶子任务价值` 是本能提交方法，不是根任务可选择的业务方法；
           它不得被任务筹办选作根任务当前方法来凭空满足安全根需求；
           它只在叶子任务完成、来源需求满足、输出结果证据存在且未重复结算时被治理链调用。
       安全值因果链：
           D0 = 叶子任务业务动作动态，说明安全问题如何被解决；
           D1 = 回写需求满足动作动态，说明该结果满足了哪个安全需求；
           D2 = 结算叶子任务价值动作动态，引用 D0 / D1 / 输出结果场景并写入安全值变化。
       阻塞降级规则：
           安全问题无法分类，或暂时没有明确父任务时，不作为阻塞；
           只要原子目标完整，正式安全需求默认挂安全根需求下；
           对应安全任务默认挂安全根需求的根承接任务 / 安全根任务下；
           后续再根据目标宿主、特征类型、来源、缺口类型和任务结果归纳迁移。
    8. 任务、方法与本能方法绑定规则已修订：
       每个任务都必须有方法承接，但不是每个任务都需要绑定单步本能方法。
       父任务 / 汇总任务可以没有当前单步业务本能方法，但必须有复合方法 / 任务抽象方法承接；
       该复合方法可由复合需求、复合特征分解、子需求链、子任务链和结果归并规则抽象得到。
       当任务来源需求是复合需求时，优先走：
           复合需求 -> 复合特征分解 -> 子需求 -> 子任务。
       只有叶子任务必须最终具备可执行本能方法 / 动作入口。
       因此，任务筹办不得因为父任务当前单步本能方法为空就直接生成缺方法学习需求；必须先判断该任务是否有复合特征分解、可生长子需求、可等待子任务或可归并子结果。
       叶子任务才进入完整候选填充、当前方法选择、可执行入口、条件结果对和参数绑定检查。
```

## 3. 最近完成项

```text
2026-05-19:
    已按“条件结果对自然剧本不再作为当前硬通过目标”口径拆分回归：
        `--learning-demand-condition-result-check` 降级为自然观察回归；
        未读到自然真实入口样本时输出“等待真实动作入口 / 无自然样本”诊断并通过；
        不写假目标宿主、假目标特征、假父需求、假父任务。
    新增并通过四条隔离回归：
        `--reverse-action-combination-single-instinct-check`
        `--submit-method-entry-from-reverse-single-instinct-check`
        `--condition-result-gap-after-real-entry-check`
        `--condition-result-demand-entry-after-real-entry-check`
    已验证：
        因果反推只读接口可返回合法单步本能候选；
        明确单步本能 ID 可通过 `自我_提交方法可执行入口` 写入真实动作句柄；
        提交入口只写 `方法动作状态=1` 和方法域动作动态，不直接设置 `方法状态=可用`；
        真实入口存在后可识别明确缺条件结果对，不误判为缺执行入口；
        合法真实入口 + 条件结果对缺口夹具下，学习需求可经自我线程正式入树、字段完整、已任务化、选中 `自我_尝试学习`，父任务进入等待。
    边界：
        这些仍是隔离回归，不证明自然流程已经稳定产出真实入口样本；
        也不证明学习子任务完成、条件结果对建立或父任务最终就绪。
    验收：
        Debug x64 构建通过，仅有 LNK4075 非增量链接常规警告；
        `--learning-demand-condition-result-check` 通过；
        `--reverse-action-combination-single-instinct-check` 通过；
        `--submit-method-entry-from-reverse-single-instinct-check` 通过；
        `--condition-result-gap-after-real-entry-check` 通过；
        `--condition-result-demand-entry-after-real-entry-check` 通过。

2026-05-19:
    按计划索引继续推进到条件结果对闭合段，复跑：
        `--learning-demand-condition-result-closure-check`
    结果失败，失败点为自然命令未读取到条件结果对学习需求闭合对象：
        有学习需求=否；
        学习任务=空；
        父需求=空；
        父任务=空。
    判定：
        这不是隔离入树回归失败；
        `--condition-result-demand-entry-after-real-entry-check` 已证明合法夹具下可正式入树 / 任务化；
        当前问题是自然流程尚未稳定产出真实入口样本和可闭合学习对象。
    当前停止点：
        继续推进需要补自然流程的自我动作组合 / 真实动作入口样本生成，或新增合法隔离闭合回归验证“学习任务完成 -> 建立条件结果对 -> 父任务继续推进”段。

2026-05-19:
    已执行状态转换因果树投影 v0.7d 小步：
        新增线程侧 `结构_叶子任务正式结算证据`；
        新增 `私有_收集叶子任务正式结算证据_线程侧` 只读采集函数；
        证据包记录 D0、D1、输出结果场景、before、target、actual、目标宿主、目标特征、投影预判、actual满足目标、actual有推进和缺失证据；
        D0 仅从当前方法运行存在 / 输出结果场景读取动作动态，不把任务状态动作动态冒充为叶子业务动作动态；
        actual 优先从输出结果场景按目标宿主 + 目标特征查找，其次读取任务结果状态摘要兼容字段；
        任务价值反馈入口追加结构化证据采集日志，D2 硬闸门保持不变。
    验收：
        Debug x64 构建通过，仅有 LNK4075 非增量链接常规警告；
        `--formal-settlement-gate-check` 通过；
        `--demand-tree-growth-check` 通过。

2026-05-19:
    已执行 D0-D1-actual 正式结算路径 P4 小步：
        `私有_调用回写需求满足本能方法_线程侧` 从 bool 返回升级为结构化返回；
        返回内容包含成功标记、回写运行存在、D1 需求满足动作动态和摘要；
        调用后从运行存在读取 `动作动态` 作为 D1；
        若本能方法未返回运行存在或未写回 D1，线程侧视为回写失败并记录错误日志。
    验收：
        Debug x64 构建通过，仅有 LNK4075 非增量链接常规警告；
        `--formal-settlement-gate-check` 通过；
        `--demand-tree-growth-check` 通过。

2026-05-18:
    已执行状态转换因果树投影 v0.7c 小步：
        任务价值反馈结算前的投影预判新增“缺正式结算证据”硬闸门；
        目标宿主 / 目标特征缺失仍作为流程错误阻断；
        缺 D0 / D1 / actual 等正式证据时阻断 D2 `结算叶子任务价值`，不再走旧数值反馈入账；
        该阻断不视为需求满足反馈失败，后续仍可根据需求目标实际满足情况调用 `回写需求满足`。
    细节：
        `因果类::查询叶子任务权重类型与层级` 已把候选路径内部的 D0 / D1 / actual 缺口上卷到预判缺失证据；
        新增 `--formal-settlement-gate-check`，验证规则投影只能形成 D0 候选，缺正式 D0 / D1 / actual 时不得进入 D2 价值结算候选。
    验收：
        Debug x64 构建通过，仅有 LNK4075 非增量链接常规警告；
        `--formal-settlement-gate-check` 通过；
        `--demand-tree-growth-check` 通过；
        `--demand-difference-child-exit-check` 通过。

2026-05-18:
    已执行 `20260518_抽象特征v0.2代码修正计划_v0.1.md`：
        抽象特征主信息新增叶子 / 复合形态；
        新增明确特征值域结构；
        新增叶子 / 复合抽象特征创建接口；
        新增抽象特征子链校验、叶子值域匹配、复合命名子特征递归匹配；
        世界树新增叶子 / 复合抽象特征包装接口，抽象特征根改为复合抽象特征。
    迁移边界：
        旧 `std::optional<I64区间> 区间` 与 `组合规则` 暂保留为兼容入口；
        后续需迁往 `特征值域`、二次特征、条件模板或外部策略；
        未恢复重叠命中次数、最近重叠命中时间、升格时间等抽象特征本体统计字段。
    验收：
        Debug x64 构建通过，仅有 LNK4075 非增量链接常规警告；
        `--demand-tree-growth-check` 通过；
        `--task-method-multicandidate-selection-check` 通过；
        `--demand-difference-child-check` 通过。

2026-05-16:
    已实现 `自我_提交方法可执行入口` 第一版严格门控壳：
        `本能方法类.h` 新增枚举 `自我_提交方法可执行入口 = 6081`；
        `本能方法类.cpp` 新增默认方法元数据；
        `自我动作实现模块.ixx` 新增规格、动作注册和实现。
    实现边界：
        只接受明确单步本能方法 ID；
        拒绝 `自我_改变特征值`、历史任务筹办 / 任务执行生命周期编号 6062 / 6063、非默认自我本能方法、入口冲突；
        成功时提交目标方法真实动作句柄、写入 `方法动作状态=1`、输出实际结果状态、生成方法域动作动态；
        不直接提交 `方法状态=可用`。
    验收：
        Debug x64 非增量链接构建通过；
        `--learning-demand-execution-entry-check` 通过；
        `--learning-demand-execution-entry-closure-check` 通过。

2026-05-16:
    已把任务筹办“缺自我动作组合 / 缺真实可执行入口”分支接入只读因果反推：
        `任务模块.筹办.ixx` 在返回等待回执前调用 `因果类::反推自我动作组合(...)`；
        日志记录目标结果状态、方法根、反推状态、候选数量和候选摘要；
        根据反推状态更新本轮筹办说明，但不提交方法入口、不生成需求、不写任务状态。
    验收：
        Debug x64 非增量链接构建通过；
        `--learning-demand-execution-entry-check` 通过；
        `--learning-demand-execution-entry-closure-check` 通过。

2026-05-16:
    已根据 `方法动作动态硬规则规范20260512.md` 的“本能方法判定标准”修订当前计划：
        `20260510_因果补完C4学习需求正式入树计划_v0.2.md`
        `20260516_方法可执行入口真实动作来源规则计划_v0.1.md`
        `20260516_因果信息用途接口落地计划_v0.1.md`
        `计划索引.md`
    修订口径：
        因果反推、候选筛选、诊断、评分、排序、预测、回执整理等中间过程可用普通逻辑代码实现；
        只有持久改写方法、任务、需求或世界树特征状态的提交段才需要明确动作来源；
        计划不得因为某段逻辑重要就强行新增本能方法。

2026-05-16:
    已按“中间过程可用普通逻辑，持久特征变化才需要动作来源”口径实现：
        `因果类::反推自我动作组合(...)`。
    本轮代码边界：
        该接口只读目标结果状态、已沉淀因果模板和可选方法根节点；
        根据因果模板动作信息尝试映射已有方法；
        若候选方法具备本能函数 ID 动作句柄，则返回单步本能候选；
        若只有因果无方法映射，返回“有因果但无可映射方法”；
        若映射方法不是单步本能入口，返回“动作序列待定义”。
    明确未做：
        未写目标方法动作句柄；
        未写任务状态或任务回执；
        未写正式需求节点；
        未写世界事实；
        未生成动作动态。
    验收：
        Debug x64 非增量链接构建通过，只有 LNK4075 非增量链接常规警告。

2026-05-16:
    已按“允许缺失自我动作组合 / 动作序列入口规则”复跑学习需求剩余自然分支：
        `--learning-demand-execution-entry-check` 通过，反向证明明确缺执行入口当前不生成正式学习需求；
        `--learning-demand-execution-entry-closure-check` 通过，反向证明明确缺执行入口当前无正式学习需求可闭合；
        `--learning-demand-placeholder-followup-check` 未读到自然样本；
        `--learning-demand-condition-result-check` 未读到自然样本。
    处理口径：
        后两项最新失败与当前允许缺失项一致，均停在“缺自我动作组合 / 真实可执行入口”之前；
        不再把旧占位后续入树通过结果作为当前自然闭合证据；
        继续后续计划时允许跳过这些缺失，但不得伪造入口数量、不得绑定 `改变特征值`、不得用 `自我_定向学习` 伪闭合内部方法入口。
    同步修订判定标准：
        只读中间过程不需要本能方法或动作动态；
        只有持久改变方法、任务、需求或世界树特征状态的提交段，才需要明确动作来源。

2026-05-16:
    已按“任务完成必须以任务目标达成为准”修正 `--subtask-parent-flow-check` 回归夹具：
        回归不再假设写入“完成”必然成功；
        先制造子任务结果状态达到任务目标，再通过提交任务状态变化链路置完成；
        若提交方法拒绝完成，回归辅助函数会检测实际任务状态并失败。
    已扩展同一回归覆盖叶子任务价值结算：
        通过任务权重固化请求制造本轮可结算上限；
        通过自我线程调用 `结算叶子任务价值` 本能方法；
        断言安全值真实入账，服务反馈被忽略且服务值不变。
    当前运行结果：
        Debug x64 非增量链接构建通过；
        `--subtask-parent-flow-check` 通过；
        `--demand-difference-child-exit-check` 通过。
    同轮结算口径修正：
        自我线程任务价值反馈中服务上限只作为调度 / 承接权重；
        服务值不参与当前真实结算，服务反馈存在时写日志说明“服务结算已忽略”；
        叶子任务价值结算仍以后续显式安全 / 服务结算规则为准，`I64_MAX` 不得进入真实结算。
    当前阻塞降级：
        `--internal-loop-check` / `--internal-loop-multirun-check` 最新复跑均停在“缺自我动作组合 / 可执行方法”等待态；
        该等待态与当前允许缺失项一致，索引不再把历史多轮稳定通过作为当前闭合证据。

2026-05-16:
    已补入并验证多候选方法选择评分规则回归：
        `--task-method-multicandidate-selection-check` 通过。
    本轮实现：
        `任务模块.筹办.ixx` 新增候选方法选择规则自检函数；
        `控制面板类.ixx` / `控制面板类.cpp` 新增控制命令；
        `鱼巢.cpp` 新增命令执行入口和运行日志。
    验收边界：
        该命令只验证多候选评分排序优先级，不构造需求 / 任务，不证明自然多候选样本已经出现，也不证明父任务最终就绪。
    同轮复核：
        Debug x64 非增量链接构建通过；
        `--change-feature-is-not-method-check` 通过。

2026-05-16:
    已按“能造因，能控动，验得果，才成法”口径修订：
        `规范/因果信息在学习中的作用规范20260516.md`
        `计划/20260516_因果信息用途接口落地计划_v0.1.md`
        `计划/20260516_方法可执行入口真实动作来源规则计划_v0.1.md`
    新增明确：
        因果信息转化为自我方法前，必须判断自我能否产生条件状态、执行或诱发动作、验收结果状态；
        占位方法有方法动作语义不等于有可执行入口；
        条件结果对学习必须等待真实动作来源 / 动作句柄存在后再进入；
        若占位方法无法反推出可执行自我动作组合，应进入“缺少动作组合 / 等待实现或外部补充”分支。

2026-05-16:
    已沿计划索引复跑 `--learning-demand-condition-result-check`：
    结果仍失败，且未读取到条件结果对学习需求；
    目标宿主、目标特征、父需求、父任务均为空。
    复核代码后确认：
        当前方法可执行入口数量由有效动作句柄推导；
        执行桥当前真实执行路径只支持 `本能函数ID`；
        `自我_定向学习` 只能补出方法动作语义痕迹和 `方法动作状态`，
        不能合法生成真实动作句柄。
    已新增计划：
        `20260516_方法可执行入口真实动作来源规则计划_v0.1.md`。
    当前处理结论：
        暂停对 `--learning-demand-condition-result-check` 的硬通过实现；
        不伪造入口数量，不绑定 `改变特征值`；
        下一步需确认“因果信息获取自我动作组合”流程与动作序列 / 可执行入口的绑定规则。

2026-05-16:
    已记录 C4 学习需求剩余自然剧本本轮实现：
    `--learning-demand-placeholder-followup-check` 通过；
    `--learning-demand-execution-entry-check` 通过；
    Debug x64 非增量链接构建通过。
    本轮代码修正点：
        `鱼巢.cpp` 中“无语义候选占位方法后的具体学习需求”分支，
        不再要求派生来源方法仍无动作；
        只要派生来源方法来自 `运行时方法沉淀`，即可识别为占位方法后续具体学习需求。
    当前未闭合项：
        `--learning-demand-condition-result-check` 尚未通过；
        原因是真实流程仍停在“缺可执行入口”，执行桥要求有效动作句柄；
        当前已确认：内部方法补齐场景出现 `自我_定向学习` 只说明尚未找到能造成 A -> B 的自我动作组合，不应把它作为闭合路径；`提交方法可执行入口` 只负责在单个本能方法 ID 或未来动作序列入口已明确后绑定真实动作句柄。

2026-05-15:
    已采用 D 口径：
    `改变特征值` 不再是任务可选择本能方法，而是先天特征写入逻辑；
    它不进入候选方法集，不作为任务当前方法，不作为方法动作动态来源；
    候选方法查找必须排除先天写入逻辑、内核原语、状态节点创建器、动作动态创建器等底层工具；
    若排除后无语义候选，任务筹办创建面向目标特征的占位方法头，并围绕该方法的状态、入口、条件结果对等具体缺口继续生长需求。
    已同步修订上级规范及直接下级规范，旧 `--change-value-delta-source-check` 命令别名已改为指向“改变特征值不作为方法候选 / 动作来源”的回归检查。
    历史回归曾修订学习需求入树 / 闭合回归：
        `--learning-demand-entry-check` 不再硬编码为“方法状态=可用 -> 尝试学习”单一路径；
        当时可识别 方法状态=可用 / 方法动作状态 / 方法可执行入口数量 / 条件结果对状态 四类明确学习目标，
        并按目标匹配 `自我_尝试学习` 或 `自我_定向学习`。
    当前已按新口径撤销其中“方法动作 / 可执行入口 -> 定向学习即闭合”的解释：
        `自我_定向学习` 仍可服务外界信息或未知动作语义链；
        但在当前内部方法补齐场景，若它被选中，表示尚未找到能造成 A -> B 的自我动作组合，或对应组合 / 方法缺少结果能力声明。
    当前保留结果：
        `--change-feature-is-not-method-check` 通过；
        方法状态=可用 / 条件结果对状态仍应由 `自我_尝试学习` 承接。

2026-05-13:
    已修订任务筹办 / 任务执行口径：
    任务筹办、任务执行不再是本能方法，而是任务管理生命周期普通函数；
    任务筹办保持单步循环执行：检查任务状态，根据任务目标填充/选择候选方法，再按方法状态形成可就绪或派生缺口回执；
    已删除公开本能方法枚举中的 自我_任务筹办 / 自我_任务执行；
    任务筹办候选集仍用私有历史 ID 6062 / 6063 过滤旧方法树残留节点；
    代码中已无 自我_任务筹办 / 自我_任务执行 / 确认任务筹办规格 / 确认任务执行规格 残留；
    Debug x64 非增量链接构建通过。

2026-05-13 09:25:
    已纠偏任务动作绑定顺序：
    同步筹办回执到任务 先把方法虚拟存在写入 任务方法候选集；
    候选数量等于 1 时才直接写入任务执行方法 / 当前方法虚拟存在；
    筹办解析目标方法时优先从任务方法候选集中选择。
    Debug x64 v143 构建通过，需求树摘要命令可运行。

2026-05-13:
    已新增需求树生长回归检查命令：
    --demand-tree-growth-check / --demand-growth-check / --need-tree-growth-check。
    当前检查覆盖：需求派生、结构角色刷新、当前主需求任务化、任务管理工作结果。
    Debug x64 v143 构建通过，检查命令返回通过。

2026-05-13:
    需求树生长回归检查已补入同目标重复风险断言：
    控制面板快照按 同父需求 + 目标宿主 + 目标特征 + 方向 聚合活动需求，
    若出现重复目标组则 --demand-tree-growth-check 失败并记录日志。
    当前运行结果：重复目标组=0，重复目标需求=0。
    Debug x64 v143 构建通过，检查命令返回通过。

2026-05-13:
    已按“需求需要任务化”口径修正：
    自我线程刷新需求列表后会扫描活动需求，缺少当前承接任务时向任务管理界面线程提交任务发起意图；
    已有任务列表项时只提升为当前对应任务，不重复创建。
    `--demand-tree-growth-check` 恢复硬断言：活动执行需求未任务化数 == 0。
    当前运行结果：活动执行=2，未任务化执行=0。
    Debug x64 v143 构建通过，检查命令返回通过。

2026-05-13:
    已补入父子权重刷新回归检查：
    对每个有子需求的需求节点，按“未截止且阻塞父任务执行的直接子需求”检查活动单位权重；
    活跃子需求对应方向权重必须为 1，非活跃子需求权重必须归零。
    `--demand-tree-growth-check` 新增硬断言：父子权重异常 == 无。
    当前运行结果：权重检查父=2，权重异常=0。
    Debug x64 v143 构建通过，检查命令返回通过。

2026-05-13:
    已把根需求列表权重从“当前安全/服务值”修正为“安全/服务活动单位权重”：
    私有_从需求树构建需求列表 仍复用 自我类::评估根需求压力 判断方向，但写入需求权重时只写 0 / 1；
    私有_选择当前主需求候选_按根权重 也改为复用同一压力评估结果；
    同步刷新安全根需求 / 服务根需求的当前状态和目标状态镜像；
    根权重与子权重刷新放入需求树全局互斥区，控制面板读取需求树时也加锁，避免半帧权重快照。
    当前运行结果：权重检查父=4，权重异常=0，状态已达未截止=1。
    Debug x64 v143 构建通过，--demand-tree-growth-check 返回通过。

2026-05-13:
    已补入任务反馈退出活动集硬回归：
    控制面板快照检查活动需求任务列表中是否仍挂已完成任务，
    并检查已完成任务头是否缺对应需求。
    `--demand-tree-growth-check` 新增硬断言：
    完成任务仍活动需求数 == 0；
    完成任务缺对应需求数 == 0。
    当前运行结果：完成任务仍活动需求=0，完成缺需求=0。
    Debug x64 v143 构建通过，检查命令返回通过。

2026-05-14:
    复跑 `--demand-tree-growth-check` 通过：
    需求树总数=4，已满足=3，状态已达未截止=1，未满足=0；
    角色未定义=0，父子权重异常=0，完成任务仍活动需求=0，完成缺需求=0。
    本轮确认前一轮“完成任务仍活动需求”已经被回写需求满足链路和快照时机修正覆盖。
    后续不得把 P2b 当前硬回归通过误判为自我内部循环已经闭合。

2026-05-14:
    已新增并验证 `--internal-loop-check` / `--self-internal-loop-check` / `--self-loop-check`：
    该命令不恢复旧观测字段，只用控制面板真实快照检查自我锚点、需求任务化、任务管理工作结果、完成任务、需求满足、活动完成任务退出和父子权重等最小闭环证据。
    当前运行结果：`--demand-tree-growth-check` 通过；`--internal-loop-check` 通过。
    注意：这只证明最小内部闭环硬回归通过，不证明连续多轮稳定、自我苏醒或初步成熟。

2026-05-14:
    已把主动同目标复用剧本接入 `--demand-tree-growth-check`：
    回归检查会在稳定快照后主动选取一个活动原子目标需求，
    对同一父需求再次提交同目标“新增需求”指令，
    并断言返回旧需求节点、需求总数不增长、父需求直接子数不增长。
    当前运行结果：同目标主动复用=通过，示例为 父需求=4、候选=1、返回=1、总数 4->4、直接子数 2->2。
    Debug x64 非增量链接构建通过，`--demand-tree-growth-check` 和 `--internal-loop-check` 均通过。

2026-05-14:
    已按“第一阻塞点降级”规则补入多轮稳定态回归：
    回写需求满足提交后会刷新父级子需求权重，避免已截止子需求保留旧权重；
    服务承接上限为 `I64_MAX` 时按未设置 / 调度权重处理，不进入真实服务结算；
    控制面板“完成任务仍活动需求”诊断收窄为执行叶需求当前任务完成但需求未截止，根 / 管理需求的历史完成任务不再误报；
    `--internal-loop-multirun-check` 改为每轮采稳定态样本。
    当前运行结果：`--demand-difference-refresh-check`、`--demand-tree-growth-check`、`--internal-loop-check`、`--internal-loop-multirun-check` 均通过。
    注意：这仍然只是内部循环多轮稳定硬回归通过，不证明自我苏醒或初步成熟。

2026-05-14:
    已补入“子任务完成 -> 子需求收束 -> 父任务重筹办”回归：
    `--subtask-parent-flow-check` 会选择差距分支下带任务的活动叶执行需求，
    将父任务置为等待中、子任务置为完成，
    并断言叶需求收束、退出活动集、权重归零、父任务重筹办 / 唤醒、完成任务不再拉动活动需求、父子权重无异常。
    为此已修正已完成叶需求直接收束路径：截止后同步清除阻塞父任务标记、清零安全 / 服务权重、刷新父级子权重，并提交父任务重筹办意图。
    当前运行结果：`--subtask-parent-flow-check`、`--demand-difference-child-exit-check`、`--internal-loop-multirun-check` 均通过。
    注意：这仍然只是父任务回流硬回归通过，不证明学习需求闭合、自我苏醒或初步成熟。

2026-05-14:
    已补入并加严“候选方法不可用 -> 方法状态=可用 学习需求入树”回归：
    `--learning-demand-entry-check` 会观察真实筹办缺口，不手动创建学习需求；
    断言候选方法结果能力命中但不可用时，派生子需求目标为 `候选方法虚拟存在.方法状态 = 可用`，
    且该子需求字段完整、挂在触发父需求下、已任务化、阻塞父任务、父任务进入等待；
    本轮进一步断言学习子任务已进入就绪态并选中 `自我_尝试学习` 作为当前方法。
    当前运行结果：`--learning-demand-entry-check` 通过；防回归命令 `--demand-difference-child-exit-check`、
    `--subtask-parent-flow-check`、`--internal-loop-multirun-check` 均通过。
    注意：这只覆盖学习需求自然三类剧本中的“候选方法不可用”到学习方法承接阶段，不证明学习子任务完成、父任务就绪、自我苏醒或初步成熟。

2026-05-14:
    已补入并验证 `--learning-demand-closure-check`：
    在“候选方法不可用 -> 方法状态=可用”链路中，学习子任务已完成，目标达成、本轮有推进、剩余差距归零、当前方法运行存在有效且运行成功；
    学习需求已截止；父链检查已区分“已可继续推进”和“仍被其他活动阻塞子需求合法等待”，避免把已闭合学习子需求之后产生的新阻塞枝条误判成闭合失败。
    当前运行结果：`--learning-demand-closure-check` 通过。
    注意：这只覆盖候选方法不可用子剧本的闭合段，不证明明确缺执行入口、明确缺条件结果对两类自然剧本，也不证明无语义候选占位方法后的具体学习需求或父任务最终就绪。

2026-05-14:
    已调整 `--learning-demand-closure-check` 的父链断言：
    学习子需求自身仍必须完成、目标达成、剩余差距归零、需求截止；
    父任务若没有等待缺口唤醒或待重筹办，也必须能明确说明同父下仍存在其他活动阻塞子需求，才允许判定为合法等待。
    Debug x64 非增量链接构建通过；`--learning-demand-closure-check`、`--learning-demand-entry-check`、`--subtask-parent-flow-check` 单独回归通过。
    备注：任务 / 自我线程回归命令会启动内部线程，后续应顺序运行，不再并行运行多个状态型回归命令。

2026-05-14:
    历史口径曾用 `--change-value-delta-source-check` 验证旧通用改值入口不会默认补改变量。
    该口径已被 2026-05-15 的 D 口径替代；同名旧命令别名仅保留兼容，实际执行新“非方法化”边界检查。
```

| 完成项 | 保留原因 |
| --- | --- |
| `完成项/20260519_主信息字段准入约束全量审查纠偏计划_v0.1.md` | M1-M23 主信息字段准入分计划已首轮完成并归档；各分计划正文已移入完成项，后续只保留新增字段准入复查和遗留兼容字段自然迁移边界。 |
| `完成项/20260513_任务筹办协作本能方法新增实现计划_v0.1.md` | 已执行并修订：仅保留协作本能方法；任务筹办 / 任务执行已从本能方法口径移除。 |
| `完成项/20260513_任务筹办按方法能力查找到就绪流程修正计划_v0.1.md` | 已执行并修订：候选集填充、选择、方法不可用缺口归因仍有效；`自我_任务执行` 旧表述已废止。 |
| `完成项/20260512_自我内部循环代码偏差纠偏计划_v0.1.md` | 已执行并修订：P6/P7 的“过程本能方法”旧口径已被普通生命周期函数口径替换。 |
| `完成项/20260513_P5复核与P6P7工作线程过程方法调用子计划_v0.1.md` | 历史记录；其中“自我_任务筹办 / 自我_任务执行”主链口径已废止，不作为当前执行依据。 |
| `完成项/20260512_自我线程任务域写入收口计划_v0.1.md` | 自我线程任务域裸写收口的历史记录。 |
| `完成项/20260512_任务执行升级为本能方法计划_v0.1.md` | 已废止历史记录；当前任务执行是普通生命周期函数。 |
| `完成项/20260511_本能函数按抽象方法概念初始化方法虚拟存在修订计划_v0.1.md` | 本能方法自身方法环境按抽象方法概念初始化的历史记录。 |
| `完成项/20260511_三个学习本能函数按方法状态链修订计划_v0.1.md` | 已废止旧“练习承接方法状态=可用”口径；保留为历史，不作为当前实现依据。 |
| `完成项/20260510_观测字段彻底删除计划_v0.1.md` | 观测字段清理边界的历史记录。 |
| `完成项/20260510_内部闭环本能函数九件套实现计划_v0.1.md` | 九件套诊断与注册历史记录；不代表内部循环闭合。 |
| `完成项/20260510_本能方法自身环境与专属规格确认计划_v0.1.md` | 本能方法执行前必须确保自身方法环境的历史记录。 |
| `完成项/20260510_信息入口类型命名收口与程序词性兼容清理计划_v0.1.md` | 信息入口类型命名收口的历史记录。 |
| `完成项/20260510_语素入口节点命名与人类词性入口边界计划_v0.1.md` | 语素入口节点命名边界的历史记录。 |

## 4. 已从索引正文清理的内容

```text
1. 大段历史完成说明。
2. 已归档完成项的详细实现记录。
3. 旧“任务筹办迁出本能方法 / 任务执行迁出本能函数”的历史口径。
4. 观测字段、summary、诊断快照作为阶段完成证据的描述。
5. 已完成但不再指导当前工作的旧计划清单。
6. 与最新 C 方案冲突的直接状态提交表述。
```

清理说明：

```text
上述内容没有删除对应计划文件。
它们只是从索引主线中移除，避免干扰当前执行顺序。
若需要追溯历史，以 计划/完成项 下的原计划正文为准。
```

## 5. 索引维护规则

```text
1. 已完成计划进入 计划/完成项，索引只保留最近且仍有参考价值的完成项。
2. 未完成且仍需要执行的计划保留在 计划 根目录，并进入“当前执行顺序”。
3. 待确认但不能直接执行的计划必须标明“待确认”，不得混入执行中计划。
4. 引用旧模块、旧字段、旧路径或与上级规范冲突的计划，不在索引中作为执行依据。
5. 若旧方向仍有价值，另起新计划，不把旧计划重新塞回当前主线。
6. 上级规范优先于计划；实施中如发现冲突，暂停该步骤并等待确认。
7. 非正常逻辑错误必须弹错 / 报错并记录日志，不允许静默兜底；业务缺口才允许走状况回执、派生需求或学习需求。
8. 当前有效口径：任务筹办 / 任务执行是任务管理生命周期普通函数，不作为本能方法注册、选择或动作动态来源。
```
