# 计划索引

更新时间：2026-06-18

## 0. 索引用途

本文件只作为当前执行路由，不承载完整历史流水账。历史细节从对应计划、实施记录、真实日志、`计划/完成项/` 和 `计划/待完成项/` 查询。

维护规则：

```text
1. 规范优先于计划；计划优先于临时推断。
2. 当前主动队列只保留一个可闭合切片。
3. 已运行内容只保留检查点、证据范围和恢复条件。
4. 待条件项不得混入当前主动队列。
5. 已完成计划必须移入 计划/完成项/，不得继续留在根目录顶层保留计划；索引只保留短查询入口和证据指针。已实现但待自然样本、待真实外设业务样本或待用户切入的计划移入 计划/待完成项/，不得误归为完成项。
6. 旧版本被新版本替代时，只保留仍有恢复价值的版本；与当前规范冲突或已被替代且无引用价值的版本删除。
7. 每次行为验证必须先写预测，再走真实运行路径，最后用日志、状态快照、动作动态和错误日志核对。
8. 严禁新增或依赖模拟测试、夹具测试、伪造样本、伪造入口、伪造动作动态或 `--*-check` 回归命令。
9. 任务筹办 / 任务执行是任务管理生命周期普通函数，不恢复为本能方法。
10. 不提前宣称自我苏醒完成或初步成熟完成。
11. Codex 恢复执行默认只读第 0 章和第 1 章当前主动队列；读到 `### 1.1` 或 `## 2` 前停止。后续章节、历史摘录和待条件项按当前切片引用再读取。
12. 主动缩减上下文：第 1 章只保留当前切片、当前堵点、下一步和证据指针；长输出、长日志和完整历史落盘到实施记录或日志文件，汇报只给关键行。
```

默认恢复读取范围：

```text
1. 第 0 章：索引用途和维护规则。
2. 第 1 章：当前主动队列、当前阻塞、下一步、预测验收、禁止宣称。
3. 最近断点清单：只读当前主动队列指向的断点文件的 `## 0. 恢复摘要`；只有该摘要明确要求时再读取后续章节。
4. 归档历史、顶层保留计划、待条件项：默认不读；只有当前问题明确指向时才按标题小段读取。
```

## 1. 当前主动队列

```text
第一优先计划 / 当前主动计划：
    计划/追根因_20260614_D455存在生成不稳定_v0.1.md
        目标：确认 D455 稳定材料后为什么仍不能自然提交观察存在发现事实。
        当前方式：只读追根因；详细证据从当前断点摘要按需展开。
        已闭合并不再重追：
            D455 原始材料质量；
            6150 有效可观测单位计数 / 对应闭合；
            扫描等待项目标观察约束精确匹配；
            下一轮扫描基准承接；
            扫描 / 跟踪按指定外设观察报告ID读取。
        当前判断：
            `提交_观察存在特征值变化事实=0` 在当前材料中不是异常；
            无新增 / 消失 / 变化簇时不应生成变化事实。
            Vj 派生需求已经能过任务管理派生闸门、进入自我线程并推进到
            任务化 / 6151 候选召回。
            20260617 新证据显示，`来源因果=3Nx` 的因果条件补齐派生
            已能通过统一提交入口入树，不再被旧的需求目标重叠口径误拒。
            当前直接堵点继续后移到目标特征 16E
            `方法最近连续成功次数` 候选集为空，任务挂起等待事实、
            外部输入或正式学习机制。
            20260618 用户确认选择 A：保持现有细则；
            16E 无候选挂起是正确行为，本切片不做学习需求代码修改。
            20260618 已完成体素 / 轮廓世界树查找最小接入：
                外设观察报告队列可从 D455 簇短期材料页只读重建
                三维体素轮廓图视角；
                当前帧轮廓分区可调用
                `世界树类::查找场景存在_按深度或彩图轮廓图`，
                将三维体素相似度并入轮廓匹配分数；
                查找只使用已存在的自我所在场景，
                不在当前帧分区读取中隐式创建场景。
        当前下一步：
            当前无可强行继续的代码切片。
            后续只在 D455 自然业务样本或等待项命中时复核：
                自我_按已确认轮廓分区当前帧是否自然运行；
                当前场景存在是否已具备三维体素模型特征；
                世界树轮廓查找是否返回相似度并影响分区结果。

当前证据指针：
    默认只读：
        实施记录/20260614_D455扫描跟踪后续事实链_Codex断点清单.md 的 `## 0. 恢复摘要`
        同一断点中的 `20260616 继续断点`、`20260616 实施断点`
        和 `20260616 追加断点`
        计划/追根因_20260614_D455存在生成不稳定_v0.1.md 的目标和当前断点小段
    按需展开：
        日志/20260616_132536_D455发现事实提交上下文_selfcheck_60000ms.stdout.log
        日志/20260616_132536_D455发现事实提交上下文_selfcheck_60000ms.stderr.log
        实施记录/20260614_D455存在生成6150计数闭合_Codex断点清单.md
        实施记录/20260614_D455稳定轮廓模块重构_Codex断点清单.md 的 `## 0. 恢复摘要`
        计划/待完成项/20260614_D455稳定轮廓模块重构计划_v0.1.md
        说明书/函数功能说明文档.md 中：
            世界树类::查找场景存在_按深度或彩图轮廓图
            特征类::比较局部三维体素与三维体素链
            特征类::比较局部轮廓深度图与三维体素链
            读取D455簇三维体素轮廓视角
        日志/20260618_D455_体素轮廓接入_msbuild.log
        日志/20260618_D455_体素轮廓只读场景修正_msbuild.log
        日志/20260618_D455_体素轮廓接入_capture_once.log

当前堵点：
    最新堵点不是 D455 原始材料质量，也不是 6150 计数闭合、
    扫描报告匹配、扫描基准承接、Vj 派生闸门、6151 候选召回，
    也不是 `3Nx` 因果路径派生入树证据不合法。
    已确认不再追：
        目标特征 16E / 方法最近连续成功次数 的需求已经能由 3Nx
        条件补齐派生入树，但随后候选集为空；
        任务=8 / 来源需求=9 记录 `方法查找未命中`，
        回执摘要为“无既有候选方法且无可用确认路径，任务挂起等待事实、
        外部输入或正式学习机制”。
        用户已选择 A，按现有细则保留挂起行为，不做无候选自动入学习需求。
    已接入：
        体素 / 轮廓世界树查找能力已接入当前帧轮廓分区路径；
        体素查找只读使用已存在自我所在场景，不隐式创建场景；
        Debug x64 构建通过；
        `--d455-capture-once` 验证 D455 打开、采集、材料页提交、
        逐簇识别报告 / 扫描变化样本卡和释放均成功。
    当前只剩待条件复核：
        需要 D455 自然业务样本或等待项命中，才能证明自我侧扫描分区
        实际命中三维体素相似度。

下一步执行内容：
    当前无主动代码切片。
    只有以下条件之一出现时恢复：
        1. D455 自然业务样本或等待项命中，能运行自我侧扫描 / 识别 / 跟踪正式路径；
        2. 当前场景存在已有三维体素模型特征，可验证世界树体素相似度命中；
        3. 用户指定新的主动主线或新根目录计划进入计划索引。

上下文硬闸：
    默认读取到 `### 1.1` 前停止。
    不整读大断点清单；只读 `## 0. 恢复摘要` 和被摘要点名的短章节。
    `MEMORY.md` 搜索默认不带 `-Context`，先限制 `Select-Object -First 20`。
    大范围 `rg`、构建、运行和日志输出先落盘，再回传关键行。
    单轮新增输出超过 20KB 或 token 接近 210K 时，先写最小断点并建议开新线程。

禁止宣称：
    不宣称 D455 自然业务样本完整通过。
    不宣称识别 / 扫描 / 跟踪边界已经全部动态闭合。
    不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

### 1.1 最近闭合历史摘录索引

```text
最近闭合切片：
    计划/追根因_20260616_扫描动作动态弹窗_v0.1.md
        目标：修复扫描复合子特征动作动态补丁引入的弹窗。
        结论：已闭合。根因是无变化子状态 `0 -> 0` 被误送入动作动态创建；
        修复为无变化子状态不生成动作动态、不弹窗。
        验证：Debug x64 构建通过；`--self-check-log --self-check-log-wait-ms=60000`
        复跑 exit_code=0，stderr 为空，异常日志长度 18443 -> 18443；
        运行段输出 `当前场景扫描组合状态=1`、`扫描状态变化动作动态数量=9`、
        `扫描主动因果信息数量=9`、`扫描主动因果信息状态=1`。

    计划/待完成项/20260614_D455稳定轮廓模块重构计划_v0.1.md
        目标：不删除 `外设线程_D455深度相机.ixx`，在现有 D455 报告链基础上，
        按 v0.2 流程图分切片完成稳定材料能力、坐标系闸门、约束调度、
        Tracker 状态和观察承接边界收口。
        P0 只读审计与断点已完成。
        P1 前置规范术语修订已完成：
        040011 / 050002 / 050004 及规范目录已将外设侧旧“提交”术语收束为
        “外设材料可承接 / 稳定材料可交付 / 可交付观察方法材料”。
        P1 代码字段收口已完成：
        `结构_观察材料质量判定::可进入提交入口组织`
        已重命名为 `可交付观察方法材料`，任务管理和自我动作读取点已同步。
        P2 稳定材料能力等级已完成：
        外设报告和质量判定已显式输出平面轮廓稳定、深度锚定、空间可用、
        外设材料可承接状态和可承接能力等级。
        P3 坐标系闸门与 anchor 三指标已完成：
        外设报告和质量判定已显式输出主分割坐标系、分源对齐状态、
        坐标系一致性状态、anchor 数量 / 密度 / 覆盖率。
        P4 外设分割约束调度已完成：
        普通等待项、批量扫描等待项和 D455 报告摘要均可输出本帧处理模式、
        本帧处理优先级和本帧处理掩码。
        P5 Tracker 状态扩展已完成：
        观察像素簇材料和报告项已具备轨迹状态、generation、连续帧数、
        状态变化原因和状态变化动态候选。
        P6 外设线程编排层首轮收缩已完成：
        `D455_线程主循环` 中采集成功后的报告发布流水线已抽为局部 helper，
        未新增模块或修改工程文件。
        P7 端到端验证与收束已完成当前可执行部分：
        静态边界扫描、Debug x64 构建和真实 D455 capture-once 均通过。
        未覆盖任务管理等待项自然命中、稳定子集自然入队、观察事实承接请求动态生成。
        本计划已移入待完成项，等待真实外设自然业务样本恢复。

当前证据指针：
    实施记录/20260614_D455稳定轮廓模块重构_Codex断点清单.md
    实施记录/20260614_D455存在生成6150计数闭合_Codex断点清单.md
    实施记录/20260614_D455扫描跟踪后续事实链_Codex断点清单.md
    计划/待完成项/20260614_D455稳定轮廓模块重构计划_v0.1.md
    流程图/20260614_D455稳定轮廓项目适配流程图_v0.2.md
    流程图/20260614_D455稳定轮廓项目适配流程图_v0.2.html
    计划/完成项/20260612_日志输出预编译控制实时计划_v0.1.md
    实施记录/20260612_日志输出预编译控制_Codex断点清单.md
    实施记录/20260612_日志输出预编译控制登记表.md
    实施记录/20260613_日志输出预编译控制P4P5_Codex断点清单.md
    实施记录/20260613_待完成项校验清理_Codex断点清单.md
    实施记录/20260613_待完成项继续执行_Codex断点清单.md
    关键日志：
        日志/20260614_D455_P7_收束_msbuild.log
            P7 收束 Debug x64 构建 exit_code=0，已成功生成。
        日志/20260614_D455_P7_真实运行_d455_capture_once.log
            真实 D455 capture-once exit_code=0；打开、采集、材料包证据卡生成和释放均成功。
        日志/20260614_D455_P7_边界静态扫描.txt
        日志/20260614_D455_P7_承接边界扫描.txt
        日志/20260614_D455_P7_结构能力扫描.txt
        日志/20260614_D455_P7_禁止性扫描.txt
            P7 边界、承接、结构能力和禁止性扫描证据。
        日志/20260614_D455_P4_分割约束调度_msbuild.log
            P4 分割约束调度后 Debug x64 构建 exit_code=0，仅 LNK4075。
        日志/20260614_D455_P4_禁止性扫描.txt
            P4 禁止性关键词扫描 scan_lines=433；本切片新增行相关命中=0。
        日志/20260614_D455_P5_Tracker状态_msbuild.log
            P5 Tracker 状态扩展构建首次 exit_code=1，修正簇级 generation 时间戳来源后复跑 exit_code=0，仅 LNK4075。
        日志/20260614_D455_P5_禁止性扫描.txt
            P5 禁止性关键词扫描 scan_lines=433；本切片两个代码文件相关命中=0。
        日志/20260614_D455_P6_编排层收缩_msbuild.log
            P6 编排层首轮收缩后 Debug x64 构建 exit_code=0，仅 LNK4075。
        日志/20260614_D455_P6_禁止性扫描.txt
            P6 禁止性关键词扫描 scan_lines=433；本切片新增行相关命中=0。
        日志/20260614_D455_P3_坐标系_anchor_msbuild.log
            P3 坐标系与 anchor 后 Debug x64 构建 exit_code=0，仅 LNK4075。
        日志/20260614_D455_P2_材料能力等级_msbuild.log
            P2 能力等级后 Debug x64 构建 exit_code=0，仅 LNK4075。
        日志/20260614_D455_P1_代码字段收口_msbuild.log
            P1 字段收口后 Debug x64 构建 exit_code=0，仅 LNK4075。
        日志/20260613_P4P5_输出入口静态扫描.txt
            scan_lines=854。
        日志/20260613_P4P5_default_msbuild.log
        日志/20260613_P4P5_all_output_off_rebuild.log
        日志/20260613_P4P5_default_rebuild_restore.log
        日志/20260613_P4P5_all_output_off_rebuild_for_run.log
        日志/20260613_P4P5_default_rebuild_restore_after_all_off_run.log
            构建均 exit_code=0，仅 LNK4075。
        日志/20260613_P4P5_default_run_5000ms.console.log
            默认运行 exit_code=0，stderr=0。
        日志/20260613_P4P5_all_off_run_5000ms.console.log
            全关运行 exit_code=0，console=0，stderr=0，run/exception/selfcheck_orphan 文件大小 delta=0。
        日志/20260613_pending_continue_selfcheck_20000ms.console.log
        日志/20260613_pending_continue_run_tail.txt
            待完成项继续复核 exit_code=0，stderr=0；
            旧主动动作组合映射堵点、被动 / 治理镜像误入候选、
            创建子任务异常分支回潮关键词命中=0。

当前堵点：
    当前无代码堵点。
    剩余为待条件验证：
        等待 D455 等待项自然命中或真实外设自然业务样本出现；
        复核逐簇识别报告入队、稳定子集报告入队、来源报告可回查、
        任务管理按等待项匹配、观察事实承接请求；
        复核稳定材料仍不直连识别 / 扫描 / 跟踪正式逻辑。
    不使用模拟样本、伪造入口或伪造等待项补齐上述动态样本。

下一步执行内容：
    当前不继续强行执行代码切片。
    只有以下条件之一出现时恢复：
        1. 用户指定新的主动主线；
        2. D455 自然业务样本或等待项命中；
        3. 新根目录计划进入计划索引。

允许修改：
    当前无主动代码切片，不预授权修改代码文件。
    若只做本次收束，允许修改：
        实施记录/20260614_D455稳定轮廓模块重构_Codex断点清单.md
        计划/计划索引.md
        计划/待完成项/20260614_D455稳定轮廓模块重构计划_v0.1.md

先只读定位：
    恢复时先读：
        规范/禁止性规范20260611.md
        规范/规范目录.md
        计划/计划索引.md 第 0-1 章
        计划/待完成项/20260614_D455稳定轮廓模块重构计划_v0.1.md
        流程图/20260614_D455稳定轮廓项目适配流程图_v0.2.md
        最近 `实施记录/20260614_D455稳定轮廓模块重构_Codex断点清单.md`，若已存在。
    若自然业务样本恢复，不重新展开 P0-P7 搜索；直接从断点清单第 24 章恢复待条件验证。

上下文缩减规则：
    本轮只保留当前索引路由状态。
    搜索、构建、运行、日志长输出必须落盘到 日志/ 或 实施记录/。
    对话只汇报关键行：文件、函数、日志行、退出码、当前判断、下一步。

全量切片任务路由表：
    主动执行：
        当前无。

    完成归档：
        计划/完成项/20260612_日志输出预编译控制实时计划_v0.1.md
            P0-P5 已闭合；
            P4 已修正错误入口写入 exception 文件，P5 默认 / 全关构建和运行验证通过。
        计划/完成项/20260612_动作验证闭环工程落地实施计划_v0.1.md
            流程图接口契约和最小闭环路线已由 20260613 闭环验证与结算流程修正计划承接；
            不再作为并行主动计划。
        计划/完成项/追根因_20260612_自检练习CRT_abort_v0.1.md
            20260613 真实自检窗口未复现 Debug Error / abort / heap assertion，按用户口径默认收束。
        计划/完成项/追根因_20260612_需求树无限制增长_v0.1.md
            20260613 真实自检窗口未复现需求数=102，当前摘要需求数=19，按用户口径默认收束。
        计划/完成项/20260510_需求树正常生长特征链与转换落地计划_v0.1.md
            文件已声明剩余事项由后续计划承接，不再作为当前执行索引依据。
        计划/完成项/20260510_自我内部循环P2b需求树生成成立计划_v0.1.md
            文件已声明需求树生成成立只保留历史背景；
            当前不得按旧 P2b 阻塞口径执行，后续复验需按新日志验证法重开条目。
        计划/完成项/20260531_运行性能优化实施计划_v0.1.md
            文件已声明不再作为当前主动性能计划，后续热点由 20260607 常驻路径优先优化线承接。
        计划/完成项/20260522_自我动作实现类本能动作函数边界收口改造计划_v0.1.md
            20260613 真实自检窗口未复现主动动作组合语义映射旧堵点；
            未出现被动整理动作或治理镜像作为自我动作组合反推候选，按用户口径默认收束。
        计划/完成项/20260612_创建子任务异常分支代码逻辑修订计划_v0.1.md
            P1-P5 代码层已闭合，P6 已命中父子目标自复制保护样本；
            20260613 真实自检窗口未复现字段缺失、稳定键重复、入树拒绝分类或父子目标自复制回潮，按用户口径默认收束。
        计划/完成项/20260518_D0D1Actual正式结算路径替换过渡路径方案_v0.1.md
            P5/P6/P7 首轮实现和历史验证已有记录；
            2026-05-23 结算硬闸门口径已修订，后续由 20260613 闭环验证与结算流程修正计划承接。
        计划/完成项/20260518_状态转换因果树与目标投影层级生成计划_v0.2.md
            状态转换因果树、目标投影、防重复结算、I64_MAX 哨兵和整数比例计算等首轮实现已有记录；
            后续普通价值 / G0 自然样本复核由 20260613 闭环验证与结算流程修正计划承接。

    待条件具备后继续：
        计划/待完成项/20260614_D455稳定轮廓模块重构计划_v0.1.md
            P0-P7 当前可执行代码切片已闭合；
            Debug x64 构建和真实 D455 capture-once 通过；
            等待 D455 等待项自然命中或真实外设自然业务样本，
            再复核稳定子集入队、任务管理等待项匹配和观察事实承接请求。
        计划/待完成项/20260613_闭环验证与结算流程修正计划_v0.1.md
            P1-P7 代码层和真实运行窗口已闭合；
            普通价值 / 共享横向 / G0 服务确认自然样本未自然出现，待真实样本恢复。
        计划/待完成项/20260610_特征类VecI解释视图完善计划_v0.1.md
            P0-P8 代码切片已闭合；
            D455 设备可用；待 D455 自然形成可比较轮廓业务样本后，
            复核识别 / 扫描 / 跟踪二次特征写入。
        计划/待完成项/20260601_自我关键特征负向变化定义因果纠偏计划_v0.1.md
            P0-P3 已刷新并补初始因果模板种子；
            P4/P5 代码层已改为读取可回读关键特征事实并追溯候选具体特征，构建和自检窗口 exit_code=0；
            当前真实窗口未自然命中 自我_读取自我关键特征变化 / 安全_构造场景影响因果候选，后续需自然样本复核；
            P6-P7 已完成当前真实窗口需求树口径和价值结算边界复核；
            暂无可强行继续的代码切片，等待自然样本或用户指定主线。
        计划/待完成项/
            剩余 34 个待完成项保留为 D455 自然业务样本、自然样本、真实动作入口、动作序列入口规则、服务/G0样本或用户指定主线恢复入口；
            本轮审计证据见 `实施记录/20260613_待完成项校验清理_Codex断点清单.md`
            和 `实施记录/20260613_待完成项继续执行_Codex断点清单.md`。

验收：
    根目录计划只保留计划索引。
    计划索引能直接说明所有切片任务的状态、路径、恢复条件和下一步。
    后续“按照计划索引执行”不得因待条件项存在而强行执行；
    只有新根目录计划、用户指定主线或待条件恢复命中时才进入下一代码切片。

禁止宣称：
    不宣称动作验证闭环已完成。
    不宣称任务执行及结算流程图已完整实现。
    不宣称普通价值核心入账业务样本已通过。
    不宣称学习主线完成。
    不把父组治理任务强行验收为普通价值任务满足反馈。
    本轮未改任务状态、需求满足、方法选择、动作动态、世界树、因果账本或价值结算。
    不宣称自我苏醒完成或内部循环全部完成。
```

## 2. 顶层保留计划

### 2.1 运行性能优化实施计划

入口：`计划/完成项/20260531_运行性能优化实施计划_v0.1.md`

保留原因：

```text
该计划已归档为性能优化历史基线，不再作为主动或待条件计划。
20260607-20260609 的 S1d/S2/S3/S4/S6S7/S8 系列小方案已归档到 计划/完成项/。
性能线当前没有主动切片；若后续用户要求继续优化，应先从最新真实日志重新定位热点，再开小方案。
```

### 2.2 特征类 VecI 解释视图完善计划

入口：`计划/待完成项/20260610_特征类VecI解释视图完善计划_v0.1.md`

保留原因：

```text
该计划 P0-P7/P8 代码切片已闭合，保留为 VecI 解释视图和后续真实样本复核入口。
P0 规范修订已完成；P1 I64 <-> U64 ZigZag 编解码工具和 VecI64 <-> VecIU64 包装函数已实施并通过 Debug x64 构建；P2 VecI64 特征值包装入口已实施并通过 Debug x64 构建；P3 特征类型解释规则已实施并通过 Debug x64 构建；P4 格式校验函数已实施并通过 Debug x64 构建；P5 轮廓专用比较函数已完成总验收并通过 Debug x64 构建；P6 观察写入接入已实施并通过 Debug x64 构建和普通自检烟测；P7/P8 识别 / 扫描 / 跟踪读取比较接入已实施并通过 Debug x64 构建和普通自检烟测。
当前停在真实样本复核：需要真实 D455 运行自然形成可比较轮廓样本，普通自检不承担取样。
断点：实施记录/20260610_特征类VecI解释视图完善_Codex断点清单.md。
恢复时必须继续保持底层使用 `特征值主信息类::VecIU64`，读取 VecIU64 时由特征类型决定是否转换以及如何转换；不得改 `特征值主信息类`，不得拆轮廓子链。
```

### 2.3 闭环验证与结算流程修正入口

入口：

```text
计划/完成项/20260612_动作验证闭环工程落地实施计划_v0.1.md
计划/待完成项/20260613_闭环验证与结算流程修正计划_v0.1.md
```

保留原因：

```text
该入口保留为任务执行闭环验证、VX0/VC0 接口契约、结算域分类和核心价值硬闸门的总查询入口。
20260613 计划 P1-P7 已完成代码层和真实运行窗口验证；普通价值任务、共享横向、G0 服务确认路径未自然出现，转待条件项。
后续若用户要求继续闭环验证，应从普通价值 / G0 服务确认自然样本或 VAL1 硬闸门复验切小片，不恢复旧的“执行报告即事实”路径。
断点：实施记录/20260613_闭环验证与结算流程修正_Codex断点清单.md。
```

## 3. 待条件具备后继续

### 3.1 子任务完成后父任务权重和活动集变化自然样本

入口：`计划/待完成项/20260514_特征值差距刷新成子需求闭环剧本计划_v0.1.md`

恢复条件：

```text
真实运行自然出现子任务完成后的父子回流闭合链：
子任务完成 / 目标结果符合
-> 自我线程收束已完成叶需求
-> 子需求退出活动集或阻塞父任务标记解除
-> 安全 / 服务权重归零或父级子权重刷新
-> 父任务收到重筹办意图或等待解除。
不得调用旧 `--subtask-parent-flow-check` 等回归入口，不得伪造父子任务样本。
```

### 3.2 D455 可比历史扫描变化样本

入口：`计划/待完成项/20260605_D455_P3可比历史扫描变化样本待完成项.md`

恢复条件：

```text
真实 D455 运行中自然形成同目标 / 同区域 / 可比较历史扫描材料，并能证明变化入账边界。
不得伪造扫描变化样本。
```

### 3.3 学习量化自然样本复验

入口：`计划/待完成项/20260605_P5学习量化真实日志验证自然样本待完成项.md`

恢复条件：

```text
真实运行自然出现学习量化缺口、方法状态派生或结果能力样本。
不得用旧夹具、伪造入口或 `--*-check` 替代。
```

### 3.4 定向学习全局动作入口形成

入口：`计划/待完成项/20260605_定向学习全局动作入口形成待完成项.md`

恢复条件：

```text
自然出现明确学习缺口，且缺口能绑定到正式方法结构、执行入口或条件结果对。
```

### 3.5 主动能力与被动触发初始因果自然复验

入口：`计划/待完成项/20260604_主动能力与被动触发遗传因果落实计划_v0.1.md`

恢复条件：

```text
真实运行出现主动能力或被动触发初始因果样本，需要复核遗传因果链和因果解释证据。
```

### 3.6 自我场景绝对坐标 / 姿态 / 自我视图坐标系

入口：`计划/待完成项/20260602_自我初始场景坐标锚点与真实D455验证计划_v0.1.md`

恢复条件：

```text
真实 D455 或等价外设提供合法坐标 / 姿态材料，并能形成自我视图坐标系锚点。
```

### 3.7 双目相机外设实现线

入口：

```text
计划/待完成项/20260524_外设本能方法三分类与双目相机方法登记实施计划_v0.1.md
计划/待完成项/20260527_双目相机观察队列承接代码纠偏与实现计划_v0.1.md
计划/待完成项/20260527_双目相机观察像素簇接入实现方案_v0.1.md
计划/待完成项/20260527_外设自主观察线程稳定采样实施计划_v0.1.md
```

恢复条件：

```text
切入外设观察队列、像素簇、稳定采样或双目相机方法登记实现。
D455 仍只能作为观察材料生产者，不直接证明需求满足、安全增加或世界事实为真。
```

### 3.8 任务持续执行 / 同目标任务自然复核

入口：`计划/待完成项/20260608_任务持续执行与同目标任务去重纠偏计划_v0.1.md`

恢复条件：

```text
自然出现新强目标升级样本、共享任务成功 / 收束完成样本，或任务工作线程执行结果任务特征评价 / 有推进未达成 / 执行中样本。
```

### 3.9 相机四本能方法持续执行结果自然复核

入口：`计划/待完成项/20260608_相机四本能方法持续执行结果协议修订计划_v0.1.md`

恢复条件：

```text
6146 / 6147 自然命中扫描 / 跟踪执行样本，或出现 P8 所需任务结果评价样本。
```

### 3.10 外设四本能方法条件包 / P9 线

入口：`计划/待完成项/20260606_外设四本能方法任务真实运行条件包闭环计划_v0.1.md`

恢复条件：

```text
明确恢复 P9d 或真实 D455 条件包链；不得伪造外设报告、条件包或动作动态。
```

### 3.11 目标观察约束与相机链路修正线

入口：

```text
计划/待完成项/20260606_目标观察约束特征组实现计划_v0.2.md
计划/待完成项/20260607_相机外设观察链多帧唯一性扫描跟踪修正计划_v0.1.md
计划/待完成项/20260607_识别链路唯一性识别与提交分级修正计划_v0.1.md
计划/待完成项/20260607_扫描链路比较材料与变化入账边界修正计划_v0.1.md
计划/待完成项/20260607_跟踪链路观测材料与状态动态边界修正计划_v0.1.md
```

恢复条件：

```text
主动队列切到相机观察约束、识别、扫描或跟踪链路实现。
`20260606_目标观察约束特征组实现计划_v0.1.md` 已删除；v0.2 是唯一保留版本。
```

### 3.12 服务值 v1.1 自然运行验证

入口：`实施记录/20260610_服务值v1.1自然运行验证_Codex断点清单.md`

恢复条件：

```text
真实主循环运行足够久，至少跨过服务治理维护间隔；
或自然出现计划服务增量 / 成功服务候选；
或候选池中已有待复核候选记录。
```

恢复后验证关键字：

```text
服务治理周期维护
成功服务G0候选生成
成功服务G0饱和入账
服务值直接入账已关闭
定时衰减服务值
休眠候选
```

### 3.13 特征类 VecI 识别 / 扫描 / 跟踪真实轮廓样本

入口：`计划/待完成项/20260610_特征类VecI解释视图完善计划_v0.1.md`

恢复条件：

```text
真实 D455 运行中自然形成至少一条可比较轮廓样本：
识别：当前观察存在与已知存在两端已有平面轮廓或空间极值轮廓；
或扫描：当前观察存在与历史 / 基准观察存在两端已有平面轮廓或空间极值轮廓；
或跟踪：目标存在与上一轮 / 基准观察存在两端已有平面轮廓或空间极值轮廓。
恢复后复核日志中的 `VecI64轮廓比较特征数`、`VecI64轮廓可比较数`、`VecI64轮廓二次特征写入数`、`轮廓匹配分数`、`轮廓残差`，并核对二次特征能追到左右特征、来源场景和来源动态。
不得新增模拟样本、夹具入口、伪造动作动态或旧 `--*-check` 回归命令。
```

### 3.14 D455 扫描 / 跟踪新技术储备后续计划

入口：`计划/待完成项/20260611_D455扫描跟踪新技术储备后续计划_v0.1.md`

恢复条件：

```text
当前主动 `D455 稳定观察子集与轮廓跨帧稳定化实现计划` 至少完成 P0-P3；
真实 D455 运行中自然出现 L1 稳定观察子集；
任务管理观察事实承接请求已能来自 L1；
用户明确切入扫描或跟踪增强；
若要接入 RTAB-Map / ORB-SLAM3 / Open3D / OctoMap / ByteTrack / SAM 2 / Cutie 等第三方技术，先单独开隔离层设计切片。
```

边界：

```text
该计划只作技术储备，不是当前主动队列；
扫描 / 跟踪算法只能生成观察材料、变化候选、轨迹候选、质量缺口或可回查证据；
不把 SLAM 地图、TSDF 网格、占据图、tracklet、mask propagation 或检测框直接当世界真值；
不写需求满足、安全值或服务值。
```

### 3.15 创建子任务异常分支代码逻辑修订

入口：`计划/完成项/20260612_创建子任务异常分支代码逻辑修订计划_v0.1.md`

归档状态：

```text
P1-P5 代码层已闭合；P6 真实窗口命中父子目标自复制保护样本。
2026-06-13 真实自检窗口未复现字段缺失、稳定键重复、入树拒绝分类或父子目标自复制回潮。
按用户口径，真实环境中旧问题不再出现则默认已解决。
若后续真实日志再次复现，重新开追踪项并重点检查：
    任务管理工作线程/派生需求消息构造逻辑错误
    任务管理工作线程/派生需求消息幂等去重
    自我线程/派生需求入树原因分类
    自我线程/父子目标自复制保护
不得把字段缺失、父子目标自复制或边界字段误入普通需求处理成普通跳过、正常停等或自由文本机器决策。
```

## 4. 本次归档结果

本次索引梳理完成以下目录整理：

```text
1. 顶层保留计划章节只保留仍有继续或恢复价值的顶层入口；已闭合计划不再作为顶层保留。
2. 已完成计划移入 计划/完成项/。
3. 待自然样本、待真实外设业务样本或待明确切入的计划移入 计划/待完成项/。
4. 已被 v0.2 替代且无独立恢复价值的 20260606_目标观察约束特征组实现计划_v0.1.md 已删除。
5. 2026-06-13 已将任务执行回执闭环和 D455 稳定观察子集两个已闭合计划移入 计划/完成项/。
6. 2026-06-13 已将观察材料练习过滤 AV 旁支追根因记录移入 计划/完成项/。
7. 2026-06-13 已检查 计划/待完成项；可校验的 CRT abort / 需求树 102 追根因计划经真实自检窗口未复现，按用户口径默认收束并移入 计划/完成项/。
8. 2026-06-13 已将明确过期的 20260510 需求树正常生长特征链计划、20260531 运行性能优化实施计划移入 计划/完成项/。
```

本次新归档到完成项的主要范围：

```text
1. 线程状态控制面板消息收口：计划/完成项/20260609_线程状态控制面板消息收口实施计划_v0.1.md。
2. 自我线程与任务管理状态推进边界纠偏：计划/完成项/20260606_自我线程与任务管理状态推进边界纠偏计划_v0.1.md。
3. 运行速度常驻路径优先优化主计划及 S1d/S2/S3/S4/S6S7/S8a-S8f 小方案。
4. 任务持续执行与同目标任务去重已闭合小方案：P1、P2a、P3a、P3b、P3c、P4。
5. 任务执行回执与本能方法闭环整改：计划/完成项/20260611_任务执行回执与本能方法闭环整改计划_v0.1.md。
6. D455 稳定观察子集与轮廓跨帧稳定化：计划/完成项/20260611_D455稳定观察子集与轮廓跨帧稳定化实现计划_v0.1.md。
7. 观察材料练习过滤后访问冲突旁支记录：计划/完成项/追根因_20260612_观察材料练习过滤AV_v0.1.md。
8. 待完成项校验清理：
   - 计划/完成项/追根因_20260612_自检练习CRT_abort_v0.1.md。
   - 计划/完成项/追根因_20260612_需求树无限制增长_v0.1.md。
   - 计划/完成项/20260510_需求树正常生长特征链与转换落地计划_v0.1.md。
   - 计划/完成项/20260531_运行性能优化实施计划_v0.1.md。
```

本次新归档到待完成项的主要范围：

```text
1. 外设四本能方法真实条件包线。
2. 目标观察约束特征组 v0.2。
3. 相机外设观察链、识别链、扫描链、跟踪链边界修正线。
4. 任务持续执行与同目标任务去重总计划。
5. 相机四本能方法持续执行结果协议修订总计划。
```

## 5. 证据闸门

所有计划恢复、完成或移动必须满足以下证据边界：

```text
1. 构建证据只能来自真实构建命令和退出码。
2. 行为证据只能来自真实运行日志、状态快照、动作动态、错误日志或控制面板真实输出。
3. 业务通过不得由旧日志、伪造样本、模拟入口、夹具测试或 `--*-check` 回归命令证明。
4. remote compact / stream disconnected / websocket / network error 只表示远程执行通道中断，不得记成项目实现、业务验证或计划逻辑失败。
5. 纯线程生命周期消息只能进入控制面板信息域；不能生成方法动作动态，不能进入自我治理邮箱。
6. 任务筹办、任务执行、任务管理后处理不作为本能方法注册、选择或动作动态来源。
7. 价值结算只能在叶子任务完成后由任务价值反馈链携带实际改变结果状态、完成度和任务自身权重调用；不得作为安全值 / 服务值根任务的普通筹办候选或当前执行方法。
8. `I64_MAX` 是异常哨兵或未设置值，不得进入真实服务结算。
```

## 6. 验证模板

每个新切片闭合时，在对应计划或实施记录中记录：

```text
切片编号：
目标：
依据文件：
允许修改范围：
禁止修改范围：
预测结果：
已改文件：
验证命令：
退出码：
关键日志窗口：
关键日志证据：
预测偏差：
当前判断：
下一步：
禁止宣称项：
```
