# 计划索引

更新时间：2026-06-23

## 0. 索引用途

本文件只作为当前执行路由，不承载完整历史流水账。历史细节从对应计划、实施记录、真实日志、`计划/完成项/` 和 `计划/待完成项/` 查询。

维护规则：

```text
1. 规范优先于计划；计划优先于临时推断。
2. 当前主动队列只保留一个可闭合切片。
3. 已运行内容只保留检查点、证据范围和恢复条件。
4. 待条件项不得混入当前主动队列。
5. 已完成计划必须移入 计划/完成项/，不得继续留在根目录顶层保留计划；索引只保留短查询入口和证据指针。已实现但待自然样本、待真实外设业务样本或待用户切入的计划移入 计划/待完成项/，不得误归为完成项。
6. 旧版本被新版本替代时，只保留仍有恢复价值的版本；与当前规范冲突或已被替代且无引用价值的版本删除。
7. 每次行为验证必须先写预测，再走真实运行路径，最后用日志、状态快照、动作动态和错误日志核对。
8. 严禁新增或依赖模拟测试、夹具测试、伪造样本、伪造入口、伪造动作动态或 `--*-check` 回归命令。
9. 任务筹办 / 任务执行是任务管理生命周期普通函数，不恢复为本能方法。
10. 不提前宣称自我苏醒完成或初步成熟完成。
11. Codex 恢复执行默认只读第 0 章和第 1 章当前主动队列；读到 `### 1.1` 或 `## 2` 前停止。后续章节、历史摘录和待条件项按当前切片引用再读取。
12. 主动缩减上下文：第 1 章只保留当前切片、当前堵点、下一步和证据指针；长输出、长日志和完整历史落盘到实施记录或日志文件，汇报只给关键行。
13. 执行中发现缺口时先分类：是否阻塞当前切片、是否属于当前类、是否已有对应类逻辑纠偏计划。
14. 不阻塞当前切片的缺口，只登记到对应计划的待执行缺口，不打断当前主动计划；索引第 1 章只保留短入口。
15. 阻塞当前切片且属于当前类的缺口，优先补入当前计划当前切片或下一子切片，并同步更新当前目标 / 下一步。
16. 阻塞当前切片且属于其它类的缺口，按“缺哪个类补哪个类第一优先”规则更新或新建对应类逻辑纠偏计划；确需转向时才修改当前主动计划和返回点。
17. 新增或扩容计划后必须写清返回点；被临时提升的计划只执行第一优先闭合切片，闭合后自动返回原计划。
18. 若用户只要求登记计划内容，默认不改变当前主动计划；只有用户明确要求执行，或缺口已阻塞当前切片，才调整当前下一步执行计划。
19. 执行中确认无用、过时、已被替代且无恢复价值的代码，应在当前闭合切片或独立清理切片中及时删除；不得长期保留兼容壳、死代码、临时兜底或无人调用的旧入口。
20. 无调用函数清理优先：live 扫描确认没有直接调用，也没有构造 / 析构隐式调用、virtual / override、入口 / 回调 / 导出、注册表、消息分发、模板实例、宏拼接或外部公开合同引用，且不承担项目结构写入 / 读取证据的函数，直接删除；同文件 / 同族可批量处理，删除后刷新函数说明文档、构建、复扫，若仍有符合条件的无调用函数则继续删除。
```

默认恢复读取范围：

```text
1. 第 0 章：索引用途和维护规则。
2. 第 1 章：当前主动队列、当前阻塞、下一步、预测验收、禁止宣称。
3. 最近断点清单：只读当前主动队列指向的断点文件的 `## 0. 恢复摘要`；只有该摘要明确要求时再读取后续章节。
4. 归档历史、顶层保留计划、待条件项：默认不读；只有当前问题明确指向时才按标题小段读取。
```

## 1. 当前主动队列

```text
第一优先计划 / 当前主动计划：
    P10 横向审计清理线 - string / 摘要 / 原因类字段后续专项

临时暂停返回点：
    P11h 无调用函数循环删除已收束；当前 live 复扫未发现新的“私有、无调用、无入口/回调/注册/公开合同、且不承担项目结构读写证据”的可直接删除函数。

来源插入计划：
    需求类逻辑纠偏计划当前可执行机器逻辑切片已收束；不宣称需求类逻辑纠偏完成。
    任务类逻辑纠偏计划当前可执行切片已收束；不宣称任务类逻辑纠偏完成。
    方法类逻辑纠偏计划 v0.1 已闭合，转入完成项；不宣称项目结构闭环全部通过。
    控制面板 / SQL / 摘要显示读源不作为机器判断，动态控制面板 SQL 化按控制面板后续切片处理。
    二次特征类 P8 当前可执行机器逻辑审计切片已收束；剩余显示 / SQL / 语言层直读登记为后置收口，不作为当前机器逻辑堵点。
    存在类当前登记切片 1.1-1.7 已有闭合记录；场景类 P5.4 已有闭合记录；因果类 P1-P6 已有闭合记录，外部绕过清理转入横向审计线。
    P10f 低引用局部返回壳 / 局部结果壳专项当前可执行批量瘦身已清到边界；按用户最新口径，剩余候选只继续分析含非主键类 string 字段的结构体，不含 string 的结构体以及只有主键 / ID / 键 / 句柄类 string 的对象排除，不再为降低结构体数量单独考虑。
    `详细设计/因果类用途接口草案20260516.ixx` 已确认不是项目代码，不进入当前代码结构体执行队列。
    P11 旧“普通低风险无调用 helper 删除切片暂止”已被用户 2026-06-22 最新口径覆盖：无调用函数优先清理，满足 live 无调用且未命中入口 / 回调 / 导出 / virtual / override / 注册 / 构造析构隐式调用 / 公开合同 / 项目结构证据的函数直接删除，删完复扫，仍有则继续。
    启动卡住临时追根因已关闭：直接环境原因为 SQL Server 未启动；不改代码、不阻塞当前队列。

原计划返回点：
    P10 后续专项串行清理。P11h 已完成本轮可执行无调用函数清理，返回 P10f 后续 string / 原因类专项。

当前执行切片：
    P10f-c 后续：string / 摘要 / 原因类字段专项清理。
    第一候选入口：自我线程摘要显示链中有调用但当前为空实现的 `读取上次故障摘要 / 读取最近恢复摘要` 及其字段写入 / 控制面板显示链。
    第二候选入口：D455 / 外设 / 控制面板显示字段中的备注、说明、摘要、原因类 string 字段，按“进入机器判断则彻底替代；仅显示则专项清理或保留显示边界”复核。

当前目标：
    1. 继续清理备注 / 摘要 / 说明 / 原因类 string 字段。
    2. 若字段进入机器查找、生成、选择、排序、等待状态判断或稳定键，必须替换为结构化状态、枚举、特征、方向或项目内部身份。
    3. 若字段只服务控制面板 / 日志 / 用户可读显示，先确认显示边界；无实际显示价值或只返回空文本的链路优先删除。
    4. 删除函数或字段后刷新函数说明文档、Debug x64 构建、按触碰文件处理 CRLF 提示。
    5. 不把任务筹办 / 任务执行恢复成本能方法；不把文本原因恢复为机器判断依据。

当前停止点：
    P11h 无调用函数循环删除本轮已收束：
        删除过期未跟踪文件 `旧_外设线程_D455深度相机.ixx`；
        删除未调用语言表达函数 7 个，并删除未接入工程且只剩私有 helper 的 `语言类_存在表达.cpp`；
        删除 `自检线程类::读取最近事件列表`；
        删除 `自我线程类::读取最近Tick时间 / 读取上次故障时间`；
        删除 `自我类::重置时序步长为默认值_`；
        已刷新 `说明书/函数功能说明文档.md`，Debug x64 构建通过；
        复扫表见 `实施记录/20260622_P11h_live_无调用候选重扫_after_round4.tsv`；
        剩余低计数候选按项目结构读写、生命周期专项、公开合同或显示专项排除，不在无调用函数切片硬删。
    方法类 P8 已闭合：外部方法主信息直读复扫清零；`语素集.安全获取词(...)` 剩余命中已分类为 SQL / 控制面板 / 日志 / 语言表达文本，不参与机器查找、候选生成、需求生成、方法选择或排序；Debug x64 构建通过。
    任务类 6.4 已闭合：任务管理方法主信息机器逻辑直读已在方法类 P8f 回迁，复扫无残留，Debug x64 构建通过。
    状态类 / 动态类 / 特征类当前登记切片已有闭合记录；不重复执行已闭合项。
    二次特征类 P8 审计已生成扫描日志和判定表；P8a 方向编码回迁已闭合；P8b 方法类二次特征特征类型材料回迁已闭合；P8c 状态类二次特征引用计数回迁已闭合；P8d 自我线程二次特征值读取回迁已闭合；P8e 外设观察关系二次特征创建入口已闭合。
    P8f 基础信息二次特征概念模板创建 / 概念入口引用判断已回迁到二次特征类入口，Debug x64 构建通过；P8 当前可执行机器逻辑切片已收束。
    P10 P4bc-P4bi 已按低风险单体切片清理：
        `自我动作实现.扫描.ixx::结构_扫描关注集合` dead vector；
        `双目相机本能适配器.cpp::深度空洞区域收集结果`；
        `自我线程模块.impl.cpp::结构_治理影响项`；
        `基础数据类型.h` 中若干未接入旧结构壳；
        每个闭合切片均已通过 Debug x64 构建并提交。
    P10 P4bi 后复扫显示，剩余 count<=3 候选已不满足当前低风险单体删除条件：
        控制面板泛型模板特化实际被 helper 使用；
        方法主信息、基础数据嵌套结构、任务治理协议、外设真实材料、自我线程 RAII 守卫
        需要专项边界，不在当前切片硬删；
        `详细设计/因果类用途接口草案20260516.ixx` 不是项目代码，已从当前代码执行队列排除。
    P10a 任务治理协议结构审计首轮已闭合九个只剩定义 / 成员声明、无字段读取的结构：
        `结构_任务管理写回结果`、`结构_缺口承接决策`、
        `结构_原语候选项`、`结构_任务管理分身继承面`、
        `结构_任务管理方法需求位特征面`、`结构_任务管理本能能力缺口面`、
        `结构_任务管理主体虚拟存在`、`结构_任务管理上下文` 和 `结构_任务管理一步结果`；
        已删除对应结构和未读成员，Debug x64 构建通过。
    P10b 公共基础数据嵌套结构审计首轮已闭合：
        `基础数据类型.h::结构体_ROI / 结构体_存在观测 / 点簇边界框 / 点簇增强结果 / 帧处理结果`
        以及只服务已删存在观测缓冲字段的 `枚举_存在观测缓冲 / operator|`；
        已删除，函数说明文档已刷新，Debug x64 构建通过。
    P10c 外设真实材料结构化改造前置审计第一小切片已闭合：
        `自我动作实现.扫描.ixx::结构_外设特征材料` 和 `构造外设特征材料` 已删除；
        `结构_已合法外设数据项::特征材料集合` 改为 `特征材料数量`；
        可回查状态由来源材料句柄和包头材料句柄直接合并，合同检查和诊断日志改读 `特征材料数量`；
        函数说明文档已刷新，Debug x64 构建通过。
    P10d 源码结构体候选复扫与通用低引用批量删除已闭合：
        `基础数据类型.h::点阵图`、`结构体_图像缓冲_u8/f32`、基础旧结构壳、抽象特征组合规则兼容字段、基础轻引用 / 颜色壳已删除；
        `自我线程模块.impl.cpp::结构_自检报告休眠处置映射` 和 `结构_消息归并与对账结果` 两个局部返回壳已瘦身删除；
        after_P10de 有效复扫结果：source_files=128，struct_defs=388，low_ref_count=102；
        Debug x64 构建通过。
    P10f-a 服务模块节点创建返回壳批量瘦身已闭合：
        `自我动作实现.服务模块.ixx::服务G0治理视图结果` 和 `服务候选池结果` 已删除；
        对应 helper 改为直接返回 `存在节点类*` 并通过引用输出失败原因；
        after_P10fa 有效复扫结果：source_files=128，struct_defs=386，low_ref_count=100；
        Debug x64 构建通过。
    P10f-b 剩余低引用候选边界复核已收束：
        任务协议 / 工作线程协议、D455 / 外设 / 原始帧材料、方法 / 特征 / 语素权威映射、自我线程快照 / 队列治理 / RAII、
        服务 / 安全 / 动作动态闭环以及服务模块剩余结果壳均已登记为专项或保留判定；
        当前没有无堵点直接批删对象。
    P10f-c string 字段聚焦清理首批已闭合：
        `任务模块.工作线程消息协议.ixx::结构_任务管理控制请求` 只有定义和未读成员，已删除；
        `任务模块.运行包.ixx::结构_根需求项::触发原因 / 结构_方法候选项::命中原因 / 结构_状态变化项::变化原因` 已删除；
        `任务模块.管理线程协议.ixx::结构_任务筹办情况::诊断摘要 / 结构_任务执行情况::诊断摘要 / 结构_任务工作完成项::完成摘要` 及对应只写赋值已删除；
        本次触碰的 6 个源码文件已按项目 eol=lf 归一化，Debug x64 构建通过；
        首批后 live 复扫：原 100 个低引用候选中 1 个结构体已删除，活体候选 99 个；61 个为不含 string 或仅主键类 string，排除；38 个含非主键类 string 字段，后续继续分析。
    P10f-c 补充字段清理首批已闭合：
        按用户最新口径，`备注 / 摘要 / 说明 / 原因` 类 string 字段即使被读取也不作为保留理由；若进入逻辑判断，同步清理文本判断逻辑；
        已删除 `任务模块::筹办::结构_任务推进回执::摘要`、`结构_任务筹办结果::说明`、`结构_任务推进事件::说明 / 等待原因`；
        已删除任务管理工作线程、任务管理界面线程和自我线程消息转换中的对应读写与摘要承接路径；
        Debug x64 构建通过。
    P10f-c 补充字段清理第二批已闭合：
        已删除 `任务模块.筹办.ixx::结构_安全因果风险树常驻维护结果::跳过原因`；
        已删除 `结构_需求生命周期上下文_筹办::诊断摘要`、`结构_筹办生命周期判断结果_筹办::诊断摘要` 及只写赋值；
        已删除自我线程风险树维护日志对 `风险树维护结果.跳过原因` 的读写；
        Debug x64 构建通过。
    P10f-c 补充字段清理第三批已闭合：
        已删除 `结构_任务管理派生需求消息::派生原因 / 证据摘要`，并删除派生原因进入稳定键的逻辑；
        已删除 `结构_因果路径筹办入口壳结果::拒绝原因`、`结构_临时因果路径项::目标化说明`；
        已删除 `结构_任务管理线程状态消息::控制拒绝原因`、补充特征枚举 `控制拒绝原因`、执行尝试汇总缓存 `最近控制拒绝原因`；
        已删除 `结构_任务管理观察事实承接请求::报告摘要 / 证据摘要` 及生产端赋值；
        自我线程治理二次特征候选生成不再读取控制拒绝原因文本，控制拒绝只由结构化控制响应状态值判定；
        Debug x64 构建通过。
    P10f-c 补充字段清理第四批已闭合：
        已删除 `自我线程模块.impl.cpp::结构_安全因果风险树线程维护结果::跳过原因`、`结构_父任务重筹办唤醒项_线程侧::触发原因`、`结构_任务结算域判定_线程侧::原因`；
        删除对应只写赋值和任务结算域分类日志中的原因拼接；结算域继续由枚举域、完成度、actual 状态和动作验证报告摘要表达；
        Debug x64 构建通过。
    P10f-c 补充字段清理第五批已闭合：
        已删除 `任务模块.治理协议.ixx` 中全源码仅结构声明命中的 `原因说明 / 升阶原因 / 未决原因 / 未绑定原因 / 拒绝原因 / 最近原因说明` 字段；
        涉及 `结构_原语判定结果`、`结构_原语判定汇总`、`结构_高阶排序请求`、`结构_高阶排序结果`、`结构_高阶胜出绑定`、`结构_任务管理控制承接记录`、`结构_治理恢复快照`；
        Debug x64 构建通过。
    P10f-c 补充字段清理第六批已闭合：
        已删除全源码仅结构声明命中的 `任务模块.运行包.ixx::结构_回流结算包::止损原因`、`自我类.ixx::结构_自我根需求压力评估::选择原因`；
        Debug x64 构建通过。
    P10f-c 补充字段清理第七批已闭合：
        已删除 `自我线程模块.ixx::结构_线程状态切换上报参数::原因摘要`、`自我线程模块.消息协议.ixx::结构_线程状态变化消息段::原因摘要`；
        删除线程状态变化摘要函数、治理消息语义和实现赋值中对该摘要字段的读取；线程状态变化继续保留 `原因键` 和结构化状态前后值；
        Debug x64 构建通过。
    P10f-c 补充字段清理第八批已闭合：
        已删除 `自我线程模块.impl.cpp::结构_治理二次特征候选::原因摘要`；
        删除布尔 / 标量治理二次特征候选构造参数、候选摘要函数、调用处原因文本拼接和 `当前等待原因` 文本读取；
        治理二次特征候选继续由概念名称、枚举域、基准类型、主体 / 客体、标量值、是否满足和置信度写入结构化二次特征；Debug x64 构建通过。
    P10f-c 补充字段清理第九批已闭合：
        已删除 `自我线程模块.impl.cpp::结构_自检候选父需求裁决_线程侧::原因`；
        删除父需求裁决 lambdas / 维护父需求确保函数的原因文本参数，以及不入树日志 / 弹窗和正式入树日志中读取 `父裁决.原因` 的路径；
        自检父需求裁决继续由父域、父需求、可入树布尔、弹错布尔和来源因果主键承载；Debug x64 构建通过。
    P10f-c 补充字段清理第十批已闭合：
        已删除 `自我线程模块.ixx::结构_方法禁止项快照::原因摘要`、`结构_否定项快照::原因摘要`、`结构_否定项候选::原因摘要`；
        三个字段只有结构声明、无源码赋值读取；`自我线程模块.ixx` 按触碰文件换行提示归一化为 LF；
        Debug x64 构建通过。
    P10f-c 补充字段清理第十一批已闭合：
        已删除 `自我线程模块.ixx` 中 5 个 `下一轮唤醒原因 / 唤醒原因` 字段，并删除 `鱼巢.cpp` 唯一显示读取；
        源码范围复扫 `下一轮唤醒原因|唤醒原因` 无命中；`鱼巢.cpp` 按触碰文件换行提示归一化为 LF；
        Debug x64 构建通过。
    P10f-c 补充字段清理第十二批已闭合：
        已删除 `任务模块.管理线程协议.ixx::结构_任务执行前许可结果::原因`；
        删除自我线程固定许可结果赋值，以及任务管理界面线程对 `许可结果.原因` 的错误说明、等待原因、摘要、固定允许阶段日志和等待重排读取；
        执行前许可结果继续由审批、允许执行、等待、收束、禁止项和方向证据布尔承载；Debug x64 构建通过。
    P10f-c 补充字段清理第十三批已闭合：
        已删除 `任务模块.管理线程协议.ixx::结构_派生需求入树回执::原因摘要`；
        删除自我线程生产端赋值，以及任务管理界面线程消费端用 `回执.原因摘要` 生成摘要和日志 `原因=` 的路径；
        派生需求入树回执继续由处理结果、阻塞父任务布尔、节点指针、时间和原消息键承载；Debug x64 构建通过。
    P10f-c 等待 / 最近原因字段结构化替换批次已闭合：
        已删除 `结构_任务局部运行态::当前等待原因`、`结构_任务工作结果::等待原因`、工作线程快照 `最近等待原因`、任务界面线程快照 `最近入队原因 / 最近排序原因 / 最近不可推进原因 / 最近后台worker原因`；
        删除工作线程 `当前等待原因` 补充特征，自我线程侧改为 `当前阻塞状态`，线程信息缓存 `最近原因键` 改为 `最近事件键`；
        函数说明文档已刷新；目标复扫 `等待原因 / 当前等待原因 / 最近原因键 / 最近原因` 无源码命中；Debug x64 构建通过。
    P10f-c 入队原因字段结构化替换批次已闭合：
        已删除 `任务模块.管理线程协议.ixx::结构_任务工作项::入队原因` 和 `任务模块.管理界面线程.impl.cpp::结构_状态提交待确认项::入队原因`；
        登记等待工作项 / 状态提交待确认 / 合并等待工作项 / 租约重派 / 同任务重排 / 死信恢复 / 后台 worker 快照不再传递入队原因文本；
        入队、待确认和重排继续由工作项 ID、工作类型、调度事件类型、状态提交确认令牌、请求状态、调度动作、租约 / 重试时间和执行前许可等待表承载；
        函数说明文档已刷新；目标复扫 `入队原因` 无源码命中；Debug x64 构建通过。
    P10f-c 自我线程空摘要显示链删除批次已闭合：
        已删除 `自我线程类::读取上次故障摘要 / 读取最近恢复摘要 / 读取最近故障摘要 / 读取最近运行摘要` 四个空实现函数及声明；
        已删除 `上次故障摘要_ / 最近恢复摘要_ / 最近故障摘要_` 三个未读字段和只写赋值；
        控制面板和 `鱼巢.cpp` 不再读取这些空摘要，自我线程状态显示继续由生命周期、阶段、最终去向、Tick、累计故障次数和累计恢复次数承载；
        函数说明文档已刷新；目标复扫空摘要函数 / 字段链无源码命中；Debug x64 构建通过。
    P10f-c 任务管理空恢复摘要显示链删除批次已闭合：
        已删除 `控制面板类.ixx::结构_控制面板快照::任务管理最近恢复摘要` 和任务管理摘要中的空 `恢复快照=` 输出；
        已删除 `鱼巢.cpp` 中固定 false 的治理恢复快照占位和恒空 `恢复点=` 输出；
        真实治理恢复快照协议结构未触碰；函数说明文档已刷新；Debug x64 构建通过。
    P10f-c 动作步骤结果备注字段删除批次已闭合：
        已删除 `动作线程类.ixx::结构_动作步骤结果::备注`、本能动作运行结果文本写入、立即失败备注参数以及未注册 / 异常路径的步骤备注赋值；
        动作步骤结果继续由动作 ID、成功码、质量、成本和输出场景表达，异常诊断继续进入错误日志；
        函数说明文档已刷新；目标复扫 `动作线程类.ixx` 中 `备注 / 本能动作运行结果文本` 无命中；Debug x64 构建通过。
    P10f-c 任务执行方法运行说明字段删除批次已闭合：
        已删除 `任务模块.执行.ixx::结构_任务步骤执行结果_最小::方法运行说明` 及任务执行 / 工作线程所有写入；
        已删除无调用文本 helper `本能动作运行结果文本`，并清理手写说明书中的对应小节；
        任务执行推进继续由运行存在、记账结果、建议状态 / 阶段、场景和动作验证报告摘要承载；函数说明文档已刷新；目标复扫无命中；Debug x64 构建通过。
    P10f-c 任务治理请求摘要字段删除批次已闭合：
        已删除 `结构_任务治理请求::请求摘要`、`结构_任务工作项::任务摘要 / 摘要`、`结构_任务管理执行前许可请求消息::请求摘要` 及生产端赋值 / 显示输出；
        任务调度和执行前许可继续由请求 ID、工作项 ID、工作类型、调度事件类型、指针 / 主键、方向证据、禁止项布尔和许可结果承载；
        函数说明文档已刷新；目标字段复扫无命中；Debug x64 构建通过。
    P10f-c 任务通知事件摘要字段删除批次已闭合：
        已删除 `结构_观察事实更新任务唤醒通知 / 结构_父任务等待条件变化通知 / 结构_任务价值结算完成通知` 的 `事件摘要` 字段及自我线程固定赋值；
        通知语义继续由结构化任务 / 需求 / 子任务 / 报告 / 等待项 / 动作动态 / 时间字段承载；
        函数说明文档已刷新；目标范围复扫无命中；Debug x64 构建通过。
    P10f-c 工作线程错误说明字段删除批次已闭合：
        已删除 `结构_任务工作结果::错误说明`、`结构_工作线程状态快照::最近错误说明` 及失败路径赋值；
        工作线程失败状态继续由错误码、任务失败布尔、异常上报事件、控制态 / 局部运行态和累计失败数表达；
        函数说明文档已刷新；目标字段复扫无命中；Debug x64 构建通过。
    P10f-c 任务状态推进结果孤立摘要字段删除批次已闭合：
        已删除 `结构_任务状态推进结果::摘要` 及管理界面线程 4 处残留赋值；
        状态推进结果继续由任务 / 需求、状态 / 阶段、提交标记、等待缺口唤醒和任务状态动作动态表达；
        函数说明文档已刷新；Debug x64 第二次构建通过。
    P10f-c 派生需求入树回执摘要字段删除批次已闭合：
        已删除 `结构_派生需求入树回执::原派生消息摘要`、`结构_任务界面线程快照::最近派生需求入树摘要` 及对应赋值；
        派生需求入树回执继续由处理结果、阻塞父任务推进布尔、原派生消息幂等键、任务 / 需求指针和时间承载；
        函数说明文档已刷新；目标字段复扫无命中；Debug x64 构建通过。
    P10f-c 外设消息承接摘要字段删除批次已闭合：
        已删除 `结构_任务工作结果::外设消息承接摘要` 及工作线程赋值；外设承接日志 helper 因仍有日志调用而保留在日志边界；
        外设承接结果继续由命中布尔、等待项 ID、报告 ID 和上行消息数承载；
        函数说明文档已刷新；目标字段复扫无命中；Debug x64 构建通过。
    P10f-c 一步治理任务包孤立摘要说明字段删除批次已闭合：
        已删除 `结构_一步治理任务包::本轮要缩小的差额说明 / 高阶排序绑定摘要 / 控制意图摘要`；
        一步治理任务包继续由主键、枚举、候选项、控制意图和确认布尔承载；
        函数说明文档已刷新；目标字段复扫无命中；Debug x64 构建通过。
    P10f-c 任务治理协议孤立摘要字段批量删除已闭合：
        已删除 `任务模块.治理协议.ixx` 中 11 个 only-definition 摘要字段；
        `最近反馈摘要 / 最近上层反馈摘要 / 目标结果差额摘要 / 锚点裁决摘要` 等仍有读取或显示用途的字段未混入本批；
        函数说明文档已刷新；目标字段复扫无命中；Debug x64 构建通过。
    P10f-c 任务工作结果摘要字段删除批次已闭合：
        已删除 `结构_任务工作结果::摘要` 及工作线程 / 界面线程读写；
        任务工作结果继续由请求 / 工作项 ID、错误码、控制态 / 局部运行态、推进事件、上行消息、下一步去向和治理结果承载；
        函数说明文档已刷新；目标字段复扫无命中；Debug x64 构建通过。
    P10f-c 任务界面线程最近说明显示缓存删除批次已闭合：
        已删除 `结构_任务界面线程快照::最近说明`、控制面板快照字段 `任务管理界面线程最近说明` 及全部读写显示；
        控制面板继续由结构化计数、调度动作、任务 / 需求主键、巡检和 worker 状态展示任务管理界面线程状态；
        函数说明文档已刷新；目标字段复扫无命中；Debug x64 构建通过。
    P10f-c count=1 孤立摘要字段批量删除已闭合：
        已删除 `任务模块.治理协议.ixx::恢复重建摘要 / 最近上层反馈摘要`；
        已删除 `自我线程模块.ixx::最近主派发判定二次特征摘要 / 根层判定摘要 / 最近变化摘要 / 整理请求摘要 / 否定项摘要`；
        函数说明文档已刷新；目标字段复扫无命中；Debug x64 构建通过。
    P10f-c count=2 定义 / 单赋值摘要字段批量删除已闭合：
        已删除 `自我线程模块.消息协议.ixx::最近结果摘要`；
        已删除 `自我线程模块.ixx` 中 `最近根值摘要 / 最近结算摘要 / 当前任务虚拟存在概念摘要 / 最近报告摘要 / 成员来源摘要 / 最近动作摘要 / 控制投影摘要 / 治理桥预判摘要 / 治理桥摘要 / 结算结论摘要 / 恢复建议摘要 / 故障摘要 / 固定机制摘要`；
        已删除对应唯一赋值 / clear 和只为字段服务的概念摘要文本构造；函数说明文档已刷新；目标字段复扫无命中；Debug x64 构建通过。
    P10f-c 第二组 count=2 摘要字段删除已闭合：
        已删除两处 `来源摘要`、两处 `主派发判定二次特征摘要`、字符串字段 `故障收口摘要`；
        已删除控制面板 `缺口首个兜底切换就绪摘要 / 缺口首个兜底切换采样摘要` 及 clear 残留；
        函数说明文档已刷新；目标字段复扫无 string 残留；Debug x64 构建通过。
    P10f-c 自我线程定义-only 摘要字段删除已闭合：
        已删除自我线程模块内两处 `错误摘要`、三处 `父任务局部更新摘要`、三处 `特征类型摘要`；
        外设队列 `错误摘要` 属外设材料专项，本批保留；函数说明文档已刷新；目标字段复扫符合预期；Debug x64 构建通过。
    P10f-c 自检报告门控摘要中转字段删除已闭合：
        已删除 `结构_自检报告修复治理快照::最近门控摘要`；
        门控说明仅保留为局部日志文本，不进入快照结构；函数说明文档已刷新；目标字段复扫无命中；Debug x64 构建通过。
    P10f-c 治理冻结消息影响项摘要字段删除已闭合：
        已删除 `结构_需求与权重治理输出快照::最近影响项摘要`；
        已删除 `步骤_刷新线程上下文并生成治理帧_` 中只服务该文本字段的消息计数和摘要拼接；
        `本轮最高优先级候选` 的结构化数值更新路径保留；函数说明文档已刷新；目标字段复扫无命中；Debug x64 构建通过。
    P10f-c 高阶排序候选摘要定义字段删除已闭合：
        已删除 `结构_高阶排序请求::候选摘要` 和 `结构_高阶排序结果::候选摘要`；
        这两个字段只有结构定义命中，未参与排序、选择、显示或日志；函数说明文档已刷新；目标字段复扫符合预期；Debug x64 构建通过。
    P10f-c 任务治理协议未读摘要字段批量删除已闭合：
        已删除 `结构_任务治理二次特征证据::摘要`、`结构_任务管理结果::最近反馈摘要 / 摘要`、`结构_治理恢复事件镜像::摘要`、`结构_治理恢复快照::最近反馈摘要`；
        已删除恢复事件镜像中只为该字段服务的 `镜像.摘要 = 事件.摘要`；控制面板仍读取的 `结构_治理恢复快照::摘要` 保留；
        函数说明文档已刷新；目标字段复扫符合预期；Debug x64 构建通过。
    P10f-c 自我循环结果未读摘要字段删除已闭合：
        已删除 `结构_循环结果::最近二次特征摘要 / 最近回流摘要` 以及三处只服务前者的 clear 残留；
        `任务管理证据触发` 等结构化布尔承载路径保留；函数说明文档已刷新；目标字段复扫符合预期；Debug x64 构建通过。
    P10f-c 控制面板缺口空摘要显示字段删除已闭合：
        已删除控制面板快照中的 `缺口调度摘要 / 缺口最近摘要 / 缺口最近失败摘要 / 缺口最近回流摘要`；
        已删除对应固定赋值、clear、HTML / 控制台输出行；实际有来源的 `缺口最近反馈摘要` 保留；
        函数说明文档已刷新；目标字段复扫无命中；Debug x64 构建通过。
    P10f-c 控制面板需求树原因显示字段删除已闭合：
        已删除控制面板快照中的 `需求树首个目标绑定异常原因 / 需求树当前主需求不可普通任务化原因`；
        目标绑定异常和当前主需求排查继续由结构化计数、主键、布尔、结构形态、目标语义、OR 令牌状态和激活路径表达；
        函数说明文档已刷新；目标字段复扫无命中；Debug x64 构建通过。
    P10f-c 特征状态比较说明字段删除已闭合：
        已删除 `结构_特征状态比较结果::说明`、`特征类::比较状态` 的说明赋值，以及任务工作线程比较日志中的 `比较.说明` 输出；
        特征状态比较继续由 `可比较 / 关系 / 比较方式 / 差值 / 特征类型` 承载；函数说明文档已刷新；目标字段复扫无命中；Debug x64 构建通过。
    P10f-c 特征三维体素文本失败原因字段删除已闭合：
        已删除 `结构_三维体素轮廓融合结果::失败原因` 和 `结构_三维体素局部轮廓相似度结果::不可比较原因`；
        已删除对应拒绝路径的文本原因传参和拼接；成功 / 可比较布尔、体素尺寸、数量、相似度、命中率、越界惩罚和层级信息保留；
        函数说明文档已刷新；目标字段复扫无命中；Debug x64 构建通过。
    P10f-c 自我线程 only-definition 缺口变化摘要字段删除已闭合：
        已删除 `结构_回流结算快照::缺口摘要`、`结构_关键中间状态快照::变化摘要`、`结构_回流结算项::缺口摘要`、`结构_关键中间状态沉淀项::变化摘要`、`结构_本轮结算封口::缺口摘要`；
        这些字段只有声明命中，无赋值和读取；函数说明文档已刷新；目标字段复扫无命中；Debug x64 构建通过。
    P10f-c 任务治理协议残留摘要说明字段删除已闭合：
        已删除 `结构_任务管理控制承接记录::说明`、`结构_任务管理结果::目标结果差额摘要 / 锚点裁决摘要`；
        已删除 `鱼巢.cpp` 中对应恒空显示占位；治理结果继续由根层重判、门控、总控、反馈、节点指针、状态 / 动态 / 因果证据和结构化控制承接字段表达；
        函数说明文档已刷新；目标字段复扫无命中；Debug x64 构建通过。
    P10f-c 治理恢复快照 only-definition 摘要字段删除已闭合：
        已删除 `结构_治理恢复快照::摘要`；同名控制面板 `恢复快照.摘要` 命中属于 `自我线程类::结构_缺口恢复接口快照`，不是本字段；
        治理恢复快照继续由版本、宿主任务、治理枚举、时间、主体 / 分身 ID、恢复事件列表和允许正式资产提交布尔承载；函数说明文档已刷新；Debug x64 构建通过。
    P10f-c 自检线程重复摘要显示字段删除已闭合：
        已删除 `结构_自检线程摘要::最近需求摘要 / 最近事件摘要` 和 `自检线程类::最近需求摘要_`；
        控制台和控制面板摘要改从 `最近事件列表.back()` 读取最近事件显示；基础信息预检等待去重字段未触碰；函数说明文档已刷新；Debug x64 构建通过。
    P10f-c 自检基础信息预检文本去重改位图已闭合：
        `私有_检查自检基础信息_自检` 改为返回结构化预检结果，缺项由 `缺项位图` 承载；
        已删除 `最近基础信息预检摘要_` 和局部 `预检摘要` 文本判断路径，等待去重改比较缺项位图和模式，日志文本只由结构化结果生成；函数说明文档已刷新；Debug x64 构建通过。
    P10f-c 风险树外层触发原因局部文本改枚举已闭合：
        `风险树外层触发原因` 从局部 `std::string` 改为局部枚举，日志输出时再转换为文本；
        Debug x64 构建通过。
    P10f-c 横向候选数值拒绝原因改枚举已闭合：
        `私有_填充横向候选数值_线程侧` 改为输出 `枚举_横向候选数值拒绝原因_线程侧`，唯一调用点日志输出时再转文本；
        函数说明文档已刷新；目标字段复扫无命中；Debug x64 构建通过。
    P10f-c 派生需求原子目标缺失原因改枚举已闭合：
        `新增需求更新指令缺原子目标输入` 改为输出 `枚举_新增需求原子目标缺失项`，调用点弹错时再转文本；
        函数说明文档已刷新；目标字段复扫无命中；Debug x64 构建通过。
    P10f-c 枚举目标校验说明字段改结论枚举已闭合：
        `结构_枚举目标校验结果::说明` 改为 `枚举_枚举目标校验结论`，校验过程写结论枚举，任务筹办日志再转文本；
        函数说明文档已刷新；目标字段复扫无命中；Debug x64 构建通过。
    P10f-c 自我线程治理日志删除已闭合：
        已删除 `自我线程模块.impl.cpp` 中 3 处 `std::ostringstream 治理日志` 和对应 `项目运行日志(治理日志.str())`；
        这些日志只把任务管理重筹办承接结果拼成文本输出，删除后实际承接、上行消息、计数和汇总日志路径不变；
        源码复扫 `治理日志` 无命中；Debug x64 构建通过。
    P10f-c 外设消息承接内部摘要字段删除已闭合：
        已删除 `任务模块.管理工作线程.ixx::结构_外设消息承接结果::承接摘要` 及所有文本赋值；
        原只拼入摘要的消费结果改为结构化布尔 `报告已消费`，外设承接继续由处理状态、报告 / 等待项 ID、报告类型、运行模式、上行消息数和消费布尔表达；
        目标字段复扫无 `承接摘要` 字段残留；Debug x64 构建通过。
    P11a 函数去重自我线程核心链单函数复核已闭合：
        `自我线程模块.impl.cpp::私有_状态需求条件兼容_线程侧` 已删除；
        函数说明文档已刷新，无调用候选清单已移除目标行，Debug x64 构建通过。
    P11b 函数去重根特征目标值 helper 单函数复核已闭合：
        `自我线程模块.impl.cpp::私有_根特征抽象目标值或当前值_线程侧` 已删除；
        函数说明文档已刷新，无调用候选清单已移除目标行，源码 / 函数说明文档 / 候选清单复扫无命中，Debug x64 构建通过。
    P11c 函数去重任务独立树根 helper 单函数复核已闭合：
        `自我线程模块.impl.cpp::私有_任务独立树根_任务管理新链` 已删除；
        函数说明文档已刷新，无调用候选清单已移除目标行，源码 / 函数说明文档 / 候选清单复扫无命中，Debug x64 构建通过。
    P11d 函数去重直接子需求特征 helper 单函数复核已闭合：
        `自我线程模块.impl.cpp::私有_直接子需求存在目标特征_线程侧` 已删除；
        函数说明文档已刷新，无调用候选清单已移除目标行，源码 / 函数说明文档 / 候选清单复扫无命中，Debug x64 构建通过。
    P11e 函数去重直接子需求查找 helper 单函数复核已闭合：
        `自我线程模块.impl.cpp::私有_查找直接子需求_按目标特征_线程侧` 已删除；
        函数说明文档已刷新，无调用候选清单已移除目标行，源码 / 函数说明文档 / 候选清单复扫无命中，Debug x64 构建通过。
    P11f 函数去重主果命中因果枚举 helper 单函数复核已闭合：
        `自我线程模块.impl.cpp::私有_枚举主果命中因果信息_线程侧` 已删除；
        函数说明文档已刷新，无调用候选清单已移除目标行，源码 / 函数说明文档 / 候选清单复扫无命中，Debug x64 构建通过。
    P11g 剩余非成员候选边界复核已闭合：
        任务创建 / 任务列表刷新 / 任务调度消费 / 线程状态落账与上报 / 安全风险树比较确保 / 当前场景路径宿主确保 / 逻辑组织子需求创建 / 缺口派生消息追加均有创建、确保、写入、队列消费或状态推进副作用；
        `基础信息主信息类.h::设置当前值` 和 `基础数据类型.h::颜色_at` 已有保留证据；
        当前普通低风险删除队列不再继续硬删，后续必须另开专项或保留判定。
    存在类计划中 P5 外部直读回迁、1.5 坐标 helper、1.6 实例判定 helper、1.7 相对坐标表 helper 均已有闭合记录。
    场景类计划中 P5.4 已记录存在类 1.7 闭合和场景类第一优先停止。
    因果类功能函数补完计划中 P6 已闭合；下一登记缺口曾指向状态类，但状态类当前登记切片已有闭合记录。
    不宣称需求类逻辑纠偏完成；不宣称任务类逻辑纠偏完成。

当前下一步：
    1. 继续 P10f-c：live 复扫剩余 `备注 / 说明 / 摘要 / 原因` 类 string 字段。
    2. 优先处理仍进入任务管理、自我线程、方法选择、需求生成、候选生成、排序、等待状态判断或稳定键的文本字段。
    3. 若字段只服务控制面板 / SQL / 日志 / 用户显示，先确认显示边界；无实际显示价值、只返回空文本或可由结构化状态展示的链路优先删除。
    4. 每个小批次删除后刷新函数说明文档、Debug x64 构建、目标字段复扫；遇到 CRLF 提示只归一化本批触碰文件。

当前证据指针：
    默认只读：
        计划/20260620_二次特征类逻辑纠偏计划_v0.1.md
        计划/完成项/20260620_方法类逻辑纠偏计划_v0.1.md
        实施记录/20260621_方法类逻辑纠偏_Codex断点清单.md
        日志/20260621_P8_方法类逻辑纠偏_外部直读复扫_after_P8h.txt
        日志/20260621_P8_安全获取词使用复扫_after_P8h.txt
        日志/20260621_P8h_方法虚拟存在服务方法读取回迁_build.log
        计划/20260620_任务类逻辑纠偏计划_v0.1.md
        日志/20260621_P8f_任务自我线程方法读取回迁_复扫.txt
        日志/20260621_P8f_任务自我线程方法读取回迁_build.log
        实施记录/20260620_二次特征类逻辑纠偏_Codex断点清单.md
        实施记录/20260620_二次特征信息操作入口判定表.md
        日志/20260620_二次特征信息操作入口扫描.txt
        日志/20260621_P8a_二次特征方向编码回迁_窄口径复扫.txt
        日志/20260621_P8a_二次特征方向编码回迁_build.log
        日志/20260621_P8b_方法二次特征特征类型材料回迁_复扫.txt
        日志/20260621_P8b_方法二次特征特征类型材料回迁_build.log
        日志/20260621_P8c_状态二次特征引用计数回迁_复扫.txt
        日志/20260621_P8c_状态二次特征引用计数回迁_build.log
        日志/20260621_P8d_自我线程二次特征值读取回迁_复扫.txt
        日志/20260621_P8d_自我线程二次特征值读取回迁_build.log
        日志/20260621_P8e_外设观察关系二次特征创建回迁_窄口径复扫.txt
        日志/20260621_P8e_外设观察关系二次特征创建回迁_build.log
        日志/20260621_P8f_基础信息二次特征概念模板回迁_窄口径复扫.txt
        日志/20260621_P8_二次特征信息操作入口复扫_after_P8f.txt
        日志/20260621_P8f_基础信息二次特征概念模板回迁_build.log
        计划/20260620_存在类逻辑纠偏计划_v0.1.md
        计划/20260620_场景类逻辑纠偏计划_v0.1.md
        计划/20260619_因果类功能函数补完计划_v0.1.md
        计划/20260618_项目结构体审计与清理计划_v0.1.md
        日志/20260621_P10_结构体低命中候选_after_P4bi.txt
        任务模块.治理协议.ixx
        实施记录/20260618_项目结构体审计_判定表.md
        实施记录/20260618_项目结构体审计_Codex断点清单.md
        实施记录/20260622_任务治理协议结构审计_Codex断点清单.md
        日志/20260622_P10a_任务治理协议结构审计_build.log
        日志/20260622_P10a_任务治理协议结构审计_第二组_build.log
        日志/20260622_P10a_任务治理协议结构审计_第三组_build.log
        日志/20260622_P10a_任务治理协议结构审计_第四组_build.log
        基础数据类型.h
        实施记录/20260622_公共基础数据嵌套结构审计_Codex断点清单.md
        日志/20260622_P10b_公共基础数据嵌套结构删除_build.log
        说明书/函数功能说明文档.md
        自我动作实现.扫描.ixx
        实施记录/20260622_外设真实材料结构化改造_Codex断点清单.md
        日志/20260622_P10c_外设材料结构瘦身_build.log
        自我线程模块.impl.cpp
        实施记录/20260622_P10d_源码结构体候选复扫_Codex断点清单.md
        日志/20260622_P10d_结构体当前扫描_after_P10de.txt
        日志/20260622_P10d_结构体低引用候选_after_P10de.tsv
        自我动作实现.服务模块.ixx
        实施记录/20260622_P10f_低引用专项化清理_Codex断点清单.md
        日志/20260622_P10f_结构体当前扫描_after_P10fa.txt
        日志/20260622_P10f_结构体低引用候选_after_P10fa.tsv
        日志/20260623_P10f_最近影响项摘要字段删除_build.log
        日志/20260623_P10f_候选摘要字段删除_build.log
        日志/20260623_P10f_任务治理协议未读摘要字段删除_build.log
        日志/20260623_P10f_自我循环结果未读摘要字段删除_build.log
        日志/20260623_P10f_控制面板缺口空摘要字段删除_build.log
        日志/20260623_P10f_控制面板需求树原因字段删除_build.log
        日志/20260623_P10f_特征状态比较说明字段删除_build.log
        日志/20260623_P10f_特征三维体素文本失败原因字段删除_build.log
        日志/20260623_P10f_自我线程only_definition缺口变化摘要字段删除_build.log
        计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md
        实施记录/20260618_函数去重与无效代码清理_Codex断点清单.md
        实施记录/20260618_函数去重_无调用候选清单.tsv
        实施记录/20260618_函数去重_判定表.md
        实施记录/20260622_函数去重自我线程兼容helper_Codex断点清单.md
        日志/20260622_P11a_状态需求条件兼容helper删除_build.log
        实施记录/20260622_函数去重根特征目标值helper_Codex断点清单.md
        日志/20260622_P11b_根特征目标值helper删除_build.log
        实施记录/20260622_函数去重任务独立树根helper_Codex断点清单.md
        日志/20260622_P11c_任务独立树根helper删除_build.log
        实施记录/20260622_函数去重直接子需求特征helper_Codex断点清单.md
        日志/20260622_P11d_直接子需求特征helper删除_build.log
        实施记录/20260622_函数去重直接子需求查找helper_Codex断点清单.md
        日志/20260622_P11e_直接子需求查找helper删除_build.log
        实施记录/20260622_函数去重主果命中因果枚举helper_Codex断点清单.md
        日志/20260622_P11f_主果命中因果枚举helper删除_build.log
        实施记录/20260622_函数去重剩余候选边界复核_Codex断点清单.md

    按需展开：
        规范/禁止性规范20260611.md
        规范/规范目录.md
        说明书/函数功能说明文档.md

上下文硬闸：
    默认读取到 `### 1.1` 前停止。
    不整读全仓库源码；清单输出落盘后只回传统计和关键异常。
    大范围 `rg`、结构体清单、引用函数清单、构建和运行输出先落盘，再回传关键行。
    单轮新增输出超过 20KB 或 token 接近 210K 时，先写最小断点并建议开新线程。

禁止宣称：
    不宣称需求类逻辑纠偏完成。
    不宣称任务类逻辑纠偏完成。
    不宣称因果类功能补完完成。
    不宣称项目结构闭环全部通过。
    不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

### 1.1 最近闭合历史入口

```text
用途：
    只保留近期闭合 / 待条件复核入口，具体证据不再展开到索引。

函数去重与无效代码清理：
    入口：计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md。
    状态：因果类功能函数补完期间暂停，不作为当前主动队列。
    完整证据：实施记录/20260618_函数去重与无效代码清理_Codex断点清单.md。

需求类头文件结构体清理与目标逻辑纠偏：
    入口：计划/完成项/20260619_需求类头文件结构体清理与目标逻辑纠偏计划_v0.1.md。
    状态：4.5 / 4.6 第一优先依赖函数已闭合，已返回因果类外部数据读取纠偏协调计划；不作为当前未完成队列。

方法类逻辑纠偏：
    入口：计划/完成项/20260619_方法类逻辑纠偏计划_v0.1.md。
    状态：方法引用解析和因果类 P7 可调用的方法引用解析已闭合，已返回协调计划 P7；不作为当前未完成队列。

自检需求入树逻辑错误追根因：
    入口：计划/完成项/追根因_20260618_自检需求入树逻辑错误_v0.1.md。
    状态：已定位并验证方法状态 -> 方法维护父域映射；若同标题弹窗复现，按追根因计划重新打开。

需求树结构错挂追根因：
    入口：计划/完成项/追根因_20260619_需求树结构错挂_v0.1.md。
    状态：已验证根层只剩安全 / 服务，方法维护管理状态挂到安全链；若控制面板或 SQL 投影再次异常，按追根因计划重新打开。

D455 存在生成不稳定追根因：
    入口：计划/待完成项/追根因_20260614_D455存在生成不稳定_v0.1.md。
    状态：已转为待真实外设自然业务样本复核；不作为当前主动队列。

扫描动作动态弹窗：
    入口：计划/完成项/追根因_20260616_扫描动作动态弹窗_v0.1.md。
    状态：已闭合；若弹窗复现，按追根因计划重新打开。

D455 稳定轮廓模块重构：
    入口：计划/待完成项/20260614_D455稳定轮廓模块重构计划_v0.1.md。
    状态：当前可执行部分已完成，等待真实外设自然业务样本恢复。

日志输出预编译控制：
    入口：计划/完成项/20260612_日志输出预编译控制实时计划_v0.1.md。
    状态：已完成；只在日志开关体系回归时读取。
```

## 2. 当前未完成计划优先队列

本章只登记 `计划/` 根目录当前可恢复的未完成计划顺序；不等同于开始执行。用户要求“执行 / 继续执行计划”时，先按第 1 章当前主动队列取一个最小闭合切片；若第 1 章堵住，再按本章可执行性检查下一无堵点切片。

执行顺序原则：

```text
1. P0-P8 是 owning-class 边界纠偏池；当前登记可执行切片已按第 1 章停止点处理，后续新缺口按“缺哪个类补哪个类第一优先”重新登记。
2. P9 因果类功能函数补完 P1-P6 已具备构建证据；后续新因果缺口按触发点登记。
3. P10-P11 是横向审计 / 清理线，当前主动队列已停止在 P10f-b；当前没有无堵点批删对象。后续必须先选明确专项，执行前重新用 live rg 确认引用，若触碰代码再补函数说明文档和真实构建证据。
4. 执行中若发现阻塞性跨类缺口，按“缺哪个类补哪个类第一优先”临时提升对应类第一优先切片，闭合后返回当前横向审计线。
5. `计划/` 根目录未完成计划已纳入本章；`计划/完成项/` 只保留短查询入口，不回拉为当前主动队列；`计划/待完成项/` 只在恢复条件成立或用户明确切入时进入当前主动队列。
```

| 优先级 | 计划入口 | 当前定位 | 堵点 / 可执行性 | 下一候选切片 |
| --- | --- | --- | --- | --- |
| P0 | `计划/20260620_需求类逻辑纠偏计划_v0.1.md` | 需求主信息外部直读、目标语义、权重和跨类操作审查。 | 当前可执行机器逻辑切片已收束；控制面板 / SQL / 摘要显示读源后置，不作为机器逻辑堵点。 | 不主动执行；后续新需求类缺口按触发点登记。 |
| P1 | `计划/20260620_任务类逻辑纠偏计划_v0.1.md` | 任务状态、任务虚拟存在、当前方法、任务执行方法和任务模块协议边界。 | 当前可执行切片已收束；任务模块协议结构审计仍可作为 P10 专项候选，不直接混入任务类当前队列。 | 不主动执行；后续新任务类缺口按触发点登记。 |
| P2 | `计划/完成项/20260620_方法类逻辑纠偏计划_v0.1.md` | 方法节点三类角色、动作入口、结果能力和方法虚拟存在边界。 | v0.1 当前范围已闭合；只作查询入口，不作为当前主动队列。 | 无；后续新方法类缺口按跨类规则重新登记。 |
| P3 | `计划/20260620_状态类逻辑纠偏计划_v0.1.md` | 状态节点读取、创建、排序时间、状态特征和值读取边界。 | 当前登记切片已闭合；不重复执行。 | 后续新状态类缺口按触发点登记。 |
| P4 | `计划/20260620_动态类逻辑纠偏计划_v0.1.md` | 动态来源、聚合视图、排序时间、动态主信息清理边界。 | 当前登记切片已闭合；不重复执行。 | 后续新动态类缺口按触发点登记。 |
| P5 | `计划/20260620_特征类逻辑纠偏计划_v0.1.md` | 特征结构体必要性、字段引用、非特征主信息直接操作审查。 | 当前登记切片已闭合；不重复执行。 | 后续新特征类缺口按触发点登记。 |
| P6 | `计划/20260620_二次特征类逻辑纠偏计划_v0.1.md` | 二次特征信息统一入口、动态聚合链和标量区间边界。 | P8 当前可执行机器逻辑审计切片已收束；剩余显示 / SQL / 语言层直读后置。 | 不主动执行；后续新二次特征缺口按触发点登记。 |
| P7 | `计划/20260620_存在类逻辑纠偏计划_v0.1.md` | 存在结构、三类根节点只读 / 确保拆分、坐标 helper 和外部直读回迁。 | 当前登记切片已有闭合记录；不重复执行。 | 后续新存在类缺口按触发点登记。 |
| P8 | `计划/20260620_场景类逻辑纠偏计划_v0.1.md` | 场景结构、场景功能函数、非场景主信息操作和世界树场景辅助函数归属。 | 当前登记切片已有闭合记录；不重复执行。 | 后续新场景类缺口按触发点登记。 |
| P9 | `计划/20260619_因果类功能函数补完计划_v0.1.md` | 因果类功能函数补完主线；多次类级缺口的返回目标。 | P1-P6 已具备构建证据；外部绕过清理由横向审计线继续。 | 后续新因果类缺口按触发点登记。 |
| P10 | `计划/20260618_项目结构体审计与清理计划_v0.1.md` | 项目通用数据结构闭环横向审计。 | P10a/P10b/P10c/P10d/P10f-a 已闭合；P10f-c string 字段直接清理已到当前边界。 | 暂让位给 P11 无调用函数优先清理；后续再选任务协议瘦身 / 控制面板 DTO/快照 / D455 原始帧材料 / 服务安全结算结果壳 / 方法主信息能力面专项。 |
| P11 | `计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md` | 函数去重和无效代码清理质量线。 | 用户 2026-06-22 新口径已恢复并提升为当前主动队列；无调用函数优先直接删除，删后复扫直到无可删候选。 | 当前执行 P11h：删除过期旧文件，重建 live 无调用候选并批量清理。 |

## 3. 待条件具备后继续

### 3.0 扫描链路待复核与路线合并

入口：

```text
计划/待完成项/20260615_自我扫描当前场景完整实现与测试计划_v0.1.md
计划/待完成项/20260615_自我扫描当前场景本能方法重建计划_v0.1.md
```

恢复条件：

```text
1. 清理 任务模块.管理界面线程.impl.cpp 中外设供料等待项的兼容本能ID兜底路径；
2. 或真实 D455 / 自然任务链出现扫描供料和 6146 相关样本；
3. 若只恢复本能方法重建计划，必须先判定其与完整实现计划的关系，不直接改扫描实现。
```

### 3.1 子任务完成后父任务权重和活动集变化自然样本

入口：`计划/待完成项/20260514_特征值差距刷新成子需求闭环剧本计划_v0.1.md`

恢复条件：

```text
真实运行自然出现子任务完成后的父子回流闭合链：
子任务完成 / 目标结果符合
-> 自我线程收束已完成叶需求
-> 子需求退出活动集或阻塞父任务标记解除
-> 安全 / 服务权重归零或父级子权重刷新
-> 父任务收到重筹办意图或等待解除。
不得调用旧 `--subtask-parent-flow-check` 等回归入口，不得伪造父子任务样本。
```

### 3.2 D455 可比历史扫描变化样本

入口：`计划/待完成项/20260605_D455_P3可比历史扫描变化样本待完成项.md`

恢复条件：

```text
真实 D455 运行中自然形成同目标 / 同区域 / 可比较历史扫描材料，并能证明变化入账边界。
不得伪造扫描变化样本。
```

### 3.2b D455 存在生成不稳定自然复核

入口：`计划/待完成项/追根因_20260614_D455存在生成不稳定_v0.1.md`

恢复条件：

```text
真实 D455 外设自然业务样本再次出现稳定数据到存在生成闭合异常；
不得用伪造外设样本或旧日志替代当前复核。
```

### 3.3 学习量化自然样本复验

入口：`计划/待完成项/20260605_P5学习量化真实日志验证自然样本待完成项.md`

恢复条件：

```text
真实运行自然出现学习量化缺口、方法状态派生或结果能力样本。
不得用旧夹具、伪造入口或 `--*-check` 替代。
```

### 3.4 定向学习全局动作入口形成

入口：`计划/待完成项/20260605_定向学习全局动作入口形成待完成项.md`

恢复条件：

```text
自然出现明确学习缺口，且缺口能绑定到正式方法结构、执行入口或条件结果对。
```

### 3.5 主动能力与被动触发初始因果自然复验

入口：`计划/待完成项/20260604_主动能力与被动触发遗传因果落实计划_v0.1.md`

恢复条件：

```text
真实运行出现主动能力或被动触发初始因果样本，需要复核遗传因果链和因果解释证据。
```

### 3.6 自我场景绝对坐标 / 姿态 / 自我视图坐标系

入口：`计划/待完成项/20260602_自我初始场景坐标锚点与真实D455验证计划_v0.1.md`

恢复条件：

```text
真实 D455 或等价外设提供合法坐标 / 姿态材料，并能形成自我视图坐标系锚点。
```

### 3.7 双目相机外设实现线

入口：

```text
计划/待完成项/20260524_外设本能方法三分类与双目相机方法登记实施计划_v0.1.md
计划/待完成项/20260527_双目相机观察队列承接代码纠偏与实现计划_v0.1.md
计划/待完成项/20260527_双目相机观察像素簇接入实现方案_v0.1.md
计划/待完成项/20260527_外设自主观察线程稳定采样实施计划_v0.1.md
```

恢复条件：

```text
切入外设观察队列、像素簇、稳定采样或双目相机方法登记实现。
D455 仍只能作为观察材料生产者，不直接证明需求满足、安全增加或世界事实为真。
```

### 3.8 任务持续执行 / 同目标任务自然复核

入口：`计划/待完成项/20260608_任务持续执行与同目标任务去重纠偏计划_v0.1.md`

恢复条件：

```text
自然出现新强目标升级样本、共享任务成功 / 收束完成样本，或任务工作线程执行结果任务特征评价 / 有推进未达成 / 执行中样本。
```

### 3.9 相机四本能方法持续执行结果自然复核

入口：`计划/待完成项/20260608_相机四本能方法持续执行结果协议修订计划_v0.1.md`

恢复条件：

```text
6146 / 6147 自然命中扫描 / 跟踪执行样本，或出现 P8 所需任务结果评价样本。
```

### 3.10 外设四本能方法条件包 / P9 线

入口：`计划/待完成项/20260606_外设四本能方法任务真实运行条件包闭环计划_v0.1.md`

恢复条件：

```text
明确恢复 P9d 或真实 D455 条件包链；不得伪造外设报告、条件包或动作动态。
```

### 3.11 目标观察约束与相机链路修正线

入口：

```text
计划/待完成项/20260606_目标观察约束特征组实现计划_v0.2.md
计划/待完成项/20260607_相机外设观察链多帧唯一性扫描跟踪修正计划_v0.1.md
计划/待完成项/20260607_识别链路唯一性识别与提交分级修正计划_v0.1.md
计划/待完成项/20260607_扫描链路比较材料与变化入账边界修正计划_v0.1.md
计划/待完成项/20260607_跟踪链路观测材料与状态动态边界修正计划_v0.1.md
```

恢复条件：

```text
主动队列切到相机观察约束、识别、扫描或跟踪链路实现。
`20260606_目标观察约束特征组实现计划_v0.1.md` 已删除；v0.2 是唯一保留版本。
```

### 3.12 服务值 v1.1 自然运行验证

入口：`实施记录/20260610_服务值v1.1自然运行验证_Codex断点清单.md`

恢复条件：

```text
真实主循环运行足够久，至少跨过服务治理维护间隔；
或自然出现计划服务增量 / 成功服务候选；
或候选池中已有待复核候选记录。
```

恢复后验证关键字：

```text
服务治理周期维护
成功服务G0候选生成
成功服务G0饱和入账
服务值直接入账已关闭
定时衰减服务值
休眠候选
```

### 3.13 特征类 VecI 识别 / 扫描 / 跟踪真实轮廓样本

入口：`计划/待完成项/20260610_特征类VecI解释视图完善计划_v0.1.md`

恢复条件：

```text
真实 D455 运行中自然形成至少一条可比较轮廓样本：
识别：当前观察存在与已知存在两端已有平面轮廓或空间极值轮廓；
或扫描：当前观察存在与历史 / 基准观察存在两端已有平面轮廓或空间极值轮廓；
或跟踪：目标存在与上一轮 / 基准观察存在两端已有平面轮廓或空间极值轮廓。
恢复后复核日志中的 `VecI64轮廓比较特征数`、`VecI64轮廓可比较数`、`VecI64轮廓二次特征写入数`、`轮廓匹配分数`、`轮廓残差`，并核对二次特征能追到左右特征、来源场景和来源动态。
不得新增模拟样本、夹具入口、伪造动作动态或旧 `--*-check` 回归命令。
```

### 3.14 D455 扫描 / 跟踪新技术储备后续计划

入口：`计划/待完成项/20260611_D455扫描跟踪新技术储备后续计划_v0.1.md`

恢复条件：

```text
当前主动 `D455 稳定观察子集与轮廓跨帧稳定化实现计划` 至少完成 P0-P3；
真实 D455 运行中自然出现 L1 稳定观察子集；
任务管理观察事实承接请求已能来自 L1；
用户明确切入扫描或跟踪增强；
若要接入 RTAB-Map / ORB-SLAM3 / Open3D / OctoMap / ByteTrack / SAM 2 / Cutie 等第三方技术，先单独开隔离层设计切片。
```

边界：

```text
该计划只作技术储备，不是当前主动队列；
扫描 / 跟踪算法只能生成观察材料、变化候选、轨迹候选、质量缺口或可回查证据；
不把 SLAM 地图、TSDF 网格、占据图、tracklet、mask propagation 或检测框直接当世界真值；
不写需求满足、安全值或服务值。
```

### 3.15 创建子任务异常分支代码逻辑修订

入口：`计划/完成项/20260612_创建子任务异常分支代码逻辑修订计划_v0.1.md`

归档状态：

```text
P1-P5 代码层已闭合；P6 真实窗口命中父子目标自复制保护样本。
2026-06-13 真实自检窗口未复现字段缺失、稳定键重复、入树拒绝分类或父子目标自复制回潮。
按用户口径，真实环境中旧问题不再出现则默认已解决。
若后续真实日志再次复现，重新开追踪项并重点检查：
    任务管理工作线程/派生需求消息构造逻辑错误
    任务管理工作线程/派生需求消息幂等去重
    自我线程/派生需求入树原因分类
    自我线程/父子目标自复制保护
不得把字段缺失、父子目标自复制或边界字段误入普通需求处理成普通跳过、正常停等或自由文本机器决策。
```

### 3.16 闭环验证 / 价值结算自然样本复核

入口：`计划/待完成项/20260613_闭环验证与结算流程修正计划_v0.1.md`

恢复条件：

```text
普通价值任务、共享横向任务或 G0 服务确认路径自然出现；
或需要复验 VAL1 硬闸门、VX0/VC0 接口契约、结算域分类。
不得恢复旧的“执行报告即事实”路径。
```

### 3.17 因果生成 / 根因果 / 目标投影待条件集合

入口：

```text
计划/待完成项/20260617_因果信息生成三分口径修正计划_v0.1.md
计划/待完成项/20260601_006自我存在服务生存闭环规范落地计划_v0.2.md
计划/待完成项/20260601_安全因果链反推当前场景特征值需求纠偏计划_v0.2.md
计划/待完成项/20260601_因果条件按特征值判断纠偏计划_v0.1.md
计划/待完成项/20260601_自我关键特征负向变化定义因果纠偏计划_v0.1.md
计划/待完成项/20260603_硬编码路径迁移为遗传因果链实施计划_v0.1.md
```

恢复条件：

```text
当前主动队列切回因果生成三分口径、根因果、目标投影或遗传因果链；
或真实日志显示动作动态 / 状态变化 / 因果模板三者口径再次冲突。
恢复时必须先确认 006 根因果规范和 007 禁止性规范，不用文本规则名或日志摘要承载因果判断。
```

### 3.18 学习需求 / 方法入口 / 条件结果对待条件集合

入口：

```text
计划/待完成项/20260510_因果补完C4学习需求正式入树计划_v0.2.md
计划/待完成项/20260516_方法可执行入口真实动作来源规则计划_v0.1.md
计划/待完成项/20260520_尝试学习生成条件结果对与练习复用条件组纠错计划_v0.1.md
计划/待完成项/20260605_任务筹办OR路径闭合与学习拦截实现计划_v0.1.md
```

恢复条件：

```text
真实运行自然出现学习缺口四类之一：
无确定候选方法、候选方法不可用、明确缺执行入口、明确缺条件结果对；
或用户明确切回学习需求正式入树、方法动作入口、条件结果对生成。
不得把参数 / 条件补齐需求归入学习需求。
```

### 3.19 需求生命周期 / 需求树组织待条件集合

入口：

```text
计划/待完成项/20260523_需求生命周期通用函数与提交入口实施计划_v0.1.md
计划/待完成项/20260523_自我根特征抽象定义自检缺口需求链实施计划_v0.1.md
计划/待完成项/20260531_需求树逻辑组织语义实现计划_v0.1.md
计划/待完成项/20260531_自检问题因果投影入树逻辑修复计划_v0.1.md
计划/待完成项/20260527_观察质量低阈值与父需求裁决自然复验计划_v0.1.md
```

恢复条件：

```text
真实运行自然出现需求提交入口、父需求裁决、需求树组织、观察质量低阈值或自检因果投影入树异常；
恢复时先按当前硬规则确认：只有需求需要任务化，任务筹办 / 任务执行本身不建成子任务。
```

## 4. 归档维护口径

```text
1. 已完成计划查询：计划/完成项/。
2. 待自然样本、待真实外设业务样本或待用户明确切入计划查询：计划/待完成项/。
3. 根目录计划只保留当前主动计划或仍有直接恢复价值的顶层入口。
4. 历史归档流水账不再写入索引；需要复核时按计划文件名、实施记录和真实日志查证。
5. 旧版本被新版本替代且无独立恢复价值时，删除旧版本或移入完成项，并在对应计划 / 断点清单保留证据。
```
## 5. 证据闸门

所有计划恢复、完成或移动必须满足以下证据边界：

```text
1. 构建证据只能来自真实构建命令和退出码。
2. 行为证据只能来自真实运行日志、状态快照、动作动态、错误日志或控制面板真实输出。
3. 业务通过不得由旧日志、伪造样本、模拟入口、夹具测试或 `--*-check` 回归命令证明。
4. remote compact / stream disconnected / websocket / network error 只表示远程执行通道中断，不得记成项目实现、业务验证或计划逻辑失败。
5. 纯线程生命周期消息只能进入控制面板信息域；不能生成方法动作动态，不能进入自我治理邮箱。
6. 任务筹办、任务执行、任务管理后处理不作为本能方法注册、选择或动作动态来源。
7. 价值结算只能在叶子任务完成后由任务价值反馈链携带实际改变结果状态、完成度和任务自身权重调用；不得作为安全值 / 服务值根任务的普通筹办候选或当前执行方法。
8. `I64_MAX` 是异常哨兵或未设置值，不得进入真实服务结算。
```

## 6. 验证模板

每个新切片闭合时，在对应计划或实施记录中记录：

```text
切片编号：
目标：
依据文件：
允许修改范围：
禁止修改范围：
预测结果：
已改文件：
验证命令：
退出码：
关键日志窗口：
关键日志证据：
预测偏差：
当前判断：
下一步：
禁止宣称项：
```
