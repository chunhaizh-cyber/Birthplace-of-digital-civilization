# 计划索引

更新时间：2026-06-01

## 0. 索引用途

本文件只保留当前主动切片、已运行压缩摘要、待条件恢复项和验证口径。历史完成细节从 `计划/完成项`、`计划/待完成项`、`实施记录` 和真实日志查询；等待真实证据复验的计划统一放入 `计划/待完成项`，不再作为当前阻塞项占用主动推进队列。

维护规则：

```text
1. 规范优先于计划；计划优先于临时推断。
2. 每次验证必须先写预测，再走真实运行路径，最后用日志、状态快照、动作动态和错误日志核对。
3. 严禁新增或依赖模拟测试、夹具测试、伪造样本、伪造入口、伪造动作动态或 `--*-check` 回归命令。
4. `--self-check-log` 只用于日志系统自检，不替代业务链路验收。
5. 任务筹办 / 任务执行是任务管理生命周期普通函数，不恢复为本能方法。
6. 不提前宣称自我苏醒完成或初步成熟完成。
7. 按 070 规范，已运行项在索引中只保留状态、检查点、下一切片和证据闸门；完整流水账不得继续堆入索引。
```

## 1. 当前主动队列

```text
当前主动计划：

优先纠偏：20260601_006自我存在服务生存闭环规范落地计划_v0.2.md
   状态：v0.2 草案已把 v0.1 的“启动期固定服务必要条件保留哪些节点”继续上提为“觉醒 -> 自我定义域 -> 根闭环因果信息 -> 当前事实挂载 -> 缺口候选 -> 需求节点”。006 规范已补入自我觉醒根闭环：觉醒不是自我节点初始化，而是系统内部需求因果、价值因果、任务因果、服务因果和生存因果开始可从自我位置解释、验证、归因和回写的根因果坐标。当前真堵点不再是删减固定节点篮子，也不只是定义词条未生成根闭环，而是任何当前主动需求入树前都必须能追溯到自我觉醒根闭环、闭环位置、触发事实、验证归因和下一轮回写。
   当前切片：P0 计划和索引收束已完成；P1 已同步 006 主规范、006001、006001001、006001002、006001003 和规范目录的觉醒根闭环口径；P2 已完成第一轮代码映射和服务链最小闸门修订；P3/P4 已在 `自我线程模块.impl.cpp` 增加根闭环信息位只读模板、当前事实挂载和闭合状态日志；P5 已把根必要条件子需求新增 / 重激活接入 `自我线程/根闭环缺口入树准入`，记录自我觉醒根闭环来源、闭环位置、目标语义、可转需求和“不直接创建任务 / 方法头”；P6 无服务触发事实分支已验证；P7 已同步 006001、006001001、006001002、006001003、020003、011003 等下级规范口径。追加切片已清理原来的虚假服务需求来源：`特征类.cpp` 不再把当前人类服务对象、当前交互者、当前权限者、当前服务目标写入服务值定义必要条件；`自我线程模块.impl.cpp` 默认忽略并清理遗留固定服务上下文子需求，只在服务触发事实存在时生成 `服务因果链.*位` 条件并走闭环缺口准入。2026-06-01 11:22 继续清理旧固定安全需求来源：`特征类.cpp` 不再把自我运行状态、资源状态、权限边界、基础观察事实、风险状态等 8 个固定字段写入安全值定义必要条件；`自我线程模块.impl.cpp` 默认忽略并清理遗留固定安全子需求，安全根必要条件子需求生成交由安全因果风险树、具体负向触发事实或来源因果信息；`任务模块.筹办.ixx` 将风险树来源因果结构版本从 2 提升到 3。Debug x64 构建通过，0 错误，1 个 LNK4075 警告；`--demand-tree-growth-summary` 退出码 0；摘要显示总需求=2、目标绑定完整=2、异常=0；最新日志显示 `自我线程/遗留固定安全需求清理`、`旧固定必要条件入树=0`、`重新根据安全因果链生成=1`、`生成来源=安全因果风险树`、`自我线程/安全需求因果链交由风险树生成`、`固定安全需求生成=0`。当前停止点：仓库没有可直接运行的正式服务事件、当前会话输入、人类表达、外部请求或已确认服务目标入口，不能自然验证“有服务触发事实时”的服务需求生成分支；本轮摘要入口为即时只读快照，日志显示自我首轮完成=否，因此只验证旧固定安全需求不再裸生成和交接到风险树口径，不宣称风险树 v3 已在正式工作线程中重新完整入树；不得用模拟样本或伪造入口补验。恢复条件：正式服务触发事实承接入口具备后，再验证服务根闭环信息位触发、缺口准入和服务因果链上下文位条件型入树；正式安全工作线程路径再次维护风险树后，再验证风险树 v3 缺口视图重新入树和后续安全因果因素任务承接。

1. 20260526_基础观察事实与基础风险判断分层实施计划_v0.1.md
   状态：R0 规范与计划定案已完成；前置的任务筹办因果路径计划已按“派生需求入树 + 子任务生成 + 父任务等待”口径完成 P6；枚举型实例特征值生产者分级计划已完成 E2 入树校验；R1 本能方法实现已拆成 8 个独立方法计划；第 1 项 `双目相机_取得当前观察特征帧`、第 2 项 `自我_建立观察帧存在`、第 3 项 `自我_建立像素观察事实索引`、第 4 项 `自我_承接像素特征值` 和第 5 项 `自我_校验观察事实完备性` 已完成。观察质量、基础观察事实和基础风险前置关系不再由任务筹办按领域词硬编码；父需求是否等待 / 降级继续 / 并行补救，后续必须来自需求侧条件、方法条件、条件结果对、现有因果信息或既有子需求。2026-05-31 起，“基础风险判断”仍只作兼容名称；历史直接伤害先沉淀为负向安全因果信息，运行时风险判断读取负向因果信息，没有负向因果则默认安全；不得定义安全因素表、预设危险对象、固定风险公式或历史伤害直查旁路。
   当前切片：R2 `派生需求消息第一阶段目标化`。R1/R2 第 8 项 `自我判断基础观察事实可用性` 已完成；2026-05-29 Debug x64 构建通过，真实 `--self-scene-html` 日志出现 `方法=自我_判断基础观察事实可用性`、动作动态 `55T`、输出特征 `基础观察事实可用状态`，本次真实帧写入 `基础观察事实可用状态=1`、失败条件数量 `0`。最新 `--demand-tree-growth-summary` 真实日志显示任务 6 已从基础观察事实可用目标筹办到方法 n / 本能ID=6125 执行；泛化学习信息补齐草案在已绑定专用方法后被丢弃；执行失败时不再用失败原因冒充派生需求，而是输出完整三比较缺口报告 / 派生消息 `当前观察特征帧取得状态: 0 -> 1`，并由 `提交_生成缺口子需求` 或统一需求树更新提交入口裁决入树。第 7 项 `当前观察帧新鲜度判定规则与合法生产者收口` 已按“不登记独立本能方法，只作为观察材料承接和基础观察事实可用性判定中的时效证据”口径完成；第 6 项 `自我_评估观察帧质量`、S1 新口径事实入账、通用父需求依赖裁决代码和派生消息父任务交回重筹办均已形成构建 / 真实日志证据；低于 5000 阈值场景和观察质量父需求自然裁决已移入待完成项，不再阻塞当前主动队列。
   当前子切片：观察质量补救方法族 S0 已完成；S1 已按 `观察质量诊断事实入账器` 新口径修订，不再以“可用观察材料目标线索 / S1补观察需求建议 / 父任务继续”作为最终口径。`20260527_观察质量事实到父需求依赖裁决通用计划_v0.1.md` 的既有“任务筹办读取质量事实并注入观察质量父需求依赖裁决”只作为历史实现事实保留，不再作为 R2 新增能力基线；新增上位边界下，任务筹办只负责找路径、找方法、找条件，观察质量 / 基础观察事实 / 风险前置关系必须先由需求侧条件、方法条件、条件结果对、现有因果信息或既有子需求提供。
   方法级计划：已完成 双目相机取得当前观察特征帧 -> 已完成 自我建立观察帧存在 -> 已完成 自我建立像素观察事实索引 -> 已完成 自我承接像素特征值 -> 已完成 自我校验观察事实完备性 -> 已完成 自我评估观察帧质量首轮证据 -> 已完成代码修订 S1 用途相关质量事实入账 -> 历史完成 通用父需求依赖裁决与新观察材料质量提升派生（待按新增上位边界迁移为需求侧 / 因果侧数据驱动） -> 已移入待完成项 低于 5000 阈值场景及观察质量父需求裁决自然复验 -> 已完成 当前观察帧新鲜度判定规则与合法生产者收口 -> 已完成 自我判断基础观察事实可用性 -> 当前 R2 派生需求消息第一阶段目标化：已完成通用状态比较路径项目标化代码与任务筹办边界收口；旧安全根必要条件自然生成 `当前观察帧.基础观察事实可用状态 == 可用` 子需求的口径已废止，后续该目标若需推进，必须来自安全因果风险树缺口、具体观察 / 安全触发事实或来源因果信息，并经提交入口入树；既有专用生产者能力只保留为合法缺口入树后的执行路径。新增方法级计划 `20260529_R2本能方法_安全评估当前场景安全性实施计划_v0.1.md` 已首轮执行：`安全_评估当前场景安全性` 登记为本能方法 ID 6126 和安全对象作用方法，只生成评估结果视图 / 缺口，不写存在本体安全度或自我安全值；最新真实日志显示场景存在集合已明确但关键特征变化和安全因果候选不足，因此只输出证据不足，不宣称基础风险已明确。当前下一主线按“自我内外交互桥梁”方向收束到新增计划 `20260529_R3外设相机能力接入自我内外交互桥实施计划_v0.1.md`：先把正式需求稳定接到 D455 外设等待项 / 订阅项、任务管理承接请求和自我场景事实入账，再让安全评估自然读取这些事实；关键特征变化观察、安全因果候选构造和评估结果提交回写作为该桥打通后的第二段。
   R3 最新状态：C0-C4 已完成首轮真实链路验证；C4.5 已纠偏为默认只跑逐簇识别等待项，扫描变化和目标跟踪不再作为无指令默认路径；C4.6-1 已补短期观察材料库和 `d455://` 解引用；C5-3a 已验证外设观察像素簇候选；C5-3b 已只输出外设观察存在候选确认方案；C5-3c 已通过提交入口正式创建 / 合并观察存在、写入原始观察事实、动作动态、二次特征和当前场景索引；C4.7 已在 C5-3c 之后基于已确认观察存在和已提交 ROI / 掩码 / 轮廓原始事实生成下一帧轮廓分区结果；C4.6-2 已基于已确认存在建立内部世界局部材料和子候选视图；C4.6-3 已把内部世界子候选拆成 `自我_确认内部世界子存在` 只输出确认方案、`提交_确认内部世界子存在事实` 才写入内部世界成员存在；C6/C7 已让 `安全_评估当前场景安全性` 消费已确认存在集合；R4 已补 `自我_读取自我关键特征变化` 和 `安全_构造场景影响因果候选` 两个安全对象作用方法；R5g 已把安全因果口径改为被动获取：没有负向因果证据时默认良好，提交 `I64MAX` 场景评估安全值，不派生持续观察、负向处置或降级保护链；R5h 已补安全评估结果可比较状态节点，便于任务执行回执读取评估明确项。2026-05-30 口径保持：外设层数据都是临时观察材料，任务管理层负责接收消息并推进识别任务，存在内部子特征识别按内部世界递归细分执行；不得把外设报告、外设簇候选、跨帧簇ID、外设临时材料、轮廓分区结果或内部世界子候选直接写成已确认观察存在 / 内部子存在；场景安全度后续才随时间和被动获得的归因证据影响自我风险安全值，不得直接结算根安全值。
   R3 执行顺序修订：C4.5、C4.6-1、C5-3a、C5-3b、C5-3c、C4.7、C4.6-2、C4.6-3、C6/C7、R4 安全前置桥、R5g 因果被动默认良好评估和 R5h 可比较状态补齐均已完成当前轮实现验证；本计划第一条外设相机接入安全评估桥、第二段安全前置桥和默认良好评估已闭合。C5-3a 已升级为按 `d455://` 短期材料页真实解引用后再输出验证结果 / 可确认候选；C5-3b 已新增 `自我_确认外设观察存在候选`，只输出新建 / 合并 / 冲突 / 证据不足确认方案，不创建观察存在、不写当前场景存在集合明确、不结算安全值；C5-3c 已新增 `提交_确认观察存在事实`，由提交入口正式创建或合并存在、写原始观察事实、动作动态、二次特征关系和当前场景索引；C4.7 已新增 `自我_按已确认轮廓分区当前帧`，只消费已确认观察存在及其已提交 ROI / 掩码 / 轮廓原始事实，输出下一帧分区结果，不消费外设簇候选或验证通过结果；C4.6-2 只建立内部世界局部材料和子候选；C4.6-3 中 `自我_确认内部世界子存在` 只生成可提交确认方案，不创建子存在，`提交_确认内部世界子存在事实` 才创建 / 复用内部世界子存在、写原始事实、动作动态、二次特征关系和成员集合；R4 两个安全方法只生成观察窗口和因果候选视图，不定义安全因素、不写风险明确、不结算安全值；R5g 不再把因果缺省当成低安全；R5h 只补输出状态可比较性，不伪造来源需求。
   R3/R4/R5g/R5h 本轮证据：Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；控制台 `C6/C7安全评估闭环` 显示结果满足=1、当前场景评估安全值候选=9223372036854775807、当前场景安全度候选=9223372036854775807、当前场景直接伤害可能性候选=0、当前场景安全判定状态=2、当前场景安全判定来源=5、因果信息被动获取=1、无负向因果默认安全=1、默认良好允许提交=1、低安全持续观察派生=0、低安全处置需求派生=0、不结算安全值=1；最新日志 `安全评估本能方法结果` 显示 `可比较评估状态写入数量=7`；最终诊断显示持续观察需求明确链路已执行=0、持续观察tick链路已执行=0、负向影响处置需求明确链路已执行=0、降级保护方法头链路已验证=0、场景安全评估结果可提交已验证=1、安全判定默认良好已验证=1。2026-05-30 后续修正已补：旧计划废弃标记、通用计划因果反推主链、场景风险安全必要条件、外设 / 场景安全对象方法清单、I64MAX 满刻度口径、因果反推验收项、`枚举_因果来源类型::遗传`、派生需求入树后的 `派生来源因果主键` 追溯缓存，以及筹办因果反推日志中的命中因果模板 ID / 来源类型 / 来源因果键 / 中间必要状态 / 缺口报告字段；本轮 `--auto-observe-stable` 再次退出码 0，任务管理承接输出已改为“等待提交入口裁决”。当前状态：机制侧已能保留来源因果键并由提交入口裁决入树；真实来源需求命中“遗传”因果信息的自然链验收已完成；真实负向因果证据只作为后续被动更新样本，不再是默认路径阻塞项。
   追加复验：上述“剩余自然链验收”已在本轮完成。机制侧已用现有 `因果主信息类` 写入来源类型为“遗传”的冷启动因果模板，根必要条件子需求经提交入口回写来源因果键；最新 `.\x64\Debug\鱼巢.exe --demand-tree-growth-summary` 退出码 0，摘要显示 `派生来源因果=12`，证明真实需求树自然链已有来源因果追溯样本；最新 `.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0，外设桥和安全评估默认良好链未回退。
   追加纠偏：学习缺口这类以目标比较 / 二次特征为状态主体的派生需求，筹办和控制面板目标宿主判定已改为优先读取目标状态主体；`.\x64\Debug\鱼巢.exe --demand-tree-growth-summary` 最新摘要显示 `目标绑定: 完整=45 | 异常=0`，`.\x64\Debug\鱼巢.exe --auto-observe-stable` 仍退出码 0。
   追加执行：内部世界子存在确认链已按提交边界补齐。`自我_确认内部世界子存在` 不再在对象方法内创建成员存在，只生成内部世界子存在候选确认方案；新增 `提交_确认内部世界子存在事实` 作为提交入口，正式创建 / 复用内部世界子存在、写子存在原始事实、动作动态、二次特征和内部世界成员集合。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；最新日志显示 `自我_确认内部世界子存在` 输出 `子存在=` 且 `对象方法不创建内部世界子存在=1`，随后 `提交_确认内部世界子存在事实` 输出 `子存在=...`、`提交状态=1`、`写入子存在原始事实状态=1`、`内部世界关联状态=1`、`写入二次特征数量=3`，最终诊断显示 `内部世界子存在确认方案链路已执行=1`、`提交内部世界子存在事实链路已执行=1`。
   追加执行二：按用户要求跳过学习需求四类剧本自然样本验收，转入后续 `多候选方法确定性选择`。任务筹办候选评分比较已改为 `直接满足等级` 优先于 `结果命中精度`，且只有结果能力确实可承接目标时才授予直接满足等级，避免“精确但不可直接执行”的候选压过可直接达成候选；原有主键兜底仍保留，候选集仍必须先填充后选择。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --demand-tree-growth-summary` 退出码 0，摘要保持 `派生来源因果=12`、`目标绑定: 完整=45 | 异常=0`、权重异常=0、死信=0；学习需求四类自然剧本仍未验收，不据此宣称学习链闭合。
   追加执行三：按用户要求跳过验收门槛，继续完成 R5/R6 控制面板和日志展示补线。控制面板快照新增安全评估宿主与 `基础观察事实可用状态`、`基础风险判断状态`、`风险状态明确状态`、`基础风险值计算状态`、`当前场景评估安全值候选`、`当前场景安全度候选`、`当前场景安全判定状态`、`安全评估证据不足原因` 等展示字段；读取逻辑单独扫描安全评估结果宿主，避免空间复现宿主和安全评估结果互相抢占；HTML 自我所在场景页和文本摘要已展示这些字段，明确区分“观察事实可用”和“风险状态明确”。Debug x64 构建通过；本次只做构建烟雾检查，不作为控制面板业务验收通过证据。
   追加执行四：继续补 `安全_评估当前场景安全性` 的风险安全层候选投影视图。安全评估结果场景新增 `风险安全_场景影响部分候选`、`风险安全层因素集合版本`、`风险安全层因素数量`、`风险安全层缺失因素数量`、`风险安全层候选`、`风险安全层投影候选`；第一版只把场景影响作为已明确同层因素，运行 / 资源 / 权限 / 未知影响缺失时按 0 参与固定 5 因素平均，避免缺失因素让场景影响自动变重；第二层投影候选按 `风险安全层候选 * 0.09` 生成。该结果仍是候选视图，不写自我风险安全值、不写根安全值、不替代因果归因和价值结算入口。Debug x64 构建通过；按用户要求未执行自然验收。
   追加执行五：C8-1 `风险安全_场景影响部分候选可结算判定` 已完成当前轮实现验证。安全评估结果场景 / 方法运行视图新增 `风险安全_场景影响部分可结算状态`、`风险安全_场景影响部分不可结算原因`、`风险安全_场景影响部分已入账状态` 和变化方向视图，用来区分“候选已算出 / 是否可入账 / 是否已经入账”。当前切片只实现闸门视图：缺来源需求、来源任务、被动归因证据、持续稳定性证据、提交入口或变化方向未明确时，候选标记为不可结算且未入账；不得写 `自我风险安全值`，不得写根 `自我安全值`，不得关闭安全根需求。控制面板和控制台摘要同步展示该闸门字段。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0，控制台显示 `风险安全_场景影响部分可结算状态=1`、`风险安全_场景影响部分不可结算原因=9603`、`风险安全_场景影响部分已入账状态=0`、`候选不改账=1`、`不结算安全值=1`。后续 C8-2 才能新增 `提交_风险安全场景影响部分状态变化`。
   追加执行六：C8-2 `提交_风险安全场景影响部分状态变化` 已完成当前轮实现验证。新增本能方法 ID 6138，作为提交入口登记、接入内部 / 外部包装和 `--auto-observe-stable` 诊断链；安全评估闸门已识别该提交入口存在，因此不可结算原因不再包含“缺提交入口”。当前真实路径仍缺来源需求、来源任务、被动归因证据、持续稳定性证据和变化方向，提交入口执行但拒绝写账，只写提交状态 / 拒绝原因 / 动作动态证据，不写 `自我风险安全值`、不写根 `自我安全值`、不关闭安全根需求。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；控制台显示 `风险安全_场景影响部分可结算状态=1`、`风险安全_场景影响部分不可结算原因=1411`、`风险安全场景影响提交链路已执行=1`、`预期拒绝写账=1`、`不写自我风险安全值=1`、`不写根安全值=1`；最新日志显示 `提交_风险安全场景影响部分状态变化 | 成功=0 | 候选值=9223372036854775807 | 可结算状态=1 | 不可结算原因=1411 | 提交状态=1 | 已入账状态=0 | 缺条件拒绝=1 | 不关闭安全根需求=1`。
   追加执行七：C8-3 `风险安全层聚合明确状态` 已完成当前轮实现验证。安全评估结果场景 / 方法运行视图 / 控制面板快照新增 `风险安全层明确状态`、`风险安全层可聚合状态`、`风险安全层已入账状态` 和风险因素掩码族，把风险安全层 v1 的聚合边界显式化。2026-05-30 后续修订口径：默认满足不是“没数据”，而是“按该风险因素的证据搜索规则查过且没有找到负向风险证据”；材料不足不作为稳定业务状态，只作为搜索缺口阻断默认满足。当前运行 / 资源 / 权限 / 未知四个同层因素尚未执行负向证据搜索，因此风险安全层候选和投影候选只能作为候选视图，不能作为正式 `自我风险安全值` 或根安全值账务输入。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；控制台显示 `风险安全层缺失因素数量=4`、`风险安全层明确状态=0`、`风险安全层可聚合状态=0`、`风险因素未搜索掩码=30`、`风险因素默认满足掩码=0`、`风险因素证据搜索未完成数量=4`、`不结算安全值=1`。当前问题点修订：不能继续正式写 `自我风险安全值`；场景影响部分入账前必须先补 `当前场景特征值明确` 缺口，并取得关键特征变化观察、场景影响因果候选和持续稳定性证据；运行 / 资源 / 权限 / 未知四项仍需执行负向证据搜索，搜索完成且无负向证据后再经提交入口记录默认满足。
   追加执行八：按“没查过不等于默认满足；查过但没找到风险证据才默认满足”口径完成 C8-3 纠偏。`风险安全层未明确原因` 旧单掩码已撤下，改为 `风险因素未搜索掩码`、`风险因素搜索缺口掩码`、`风险因素负向证据掩码`、`风险因素默认满足掩码`、`风险因素已入账掩码`、`风险因素证据搜索未完成数量` 和 `风险因素默认满足数量`；`材料不足` 不再作为风险因素稳定状态，只作为搜索缺口阻断本次默认满足。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；控制台显示 `风险因素未搜索掩码=30`、`风险因素搜索缺口掩码=0`、`风险因素默认满足掩码=0`、`风险因素已入账掩码=0`、`风险因素证据搜索未完成数量=4`、`风险因素默认满足数量=0`、`风险安全层可聚合状态=0`。历史顺序“C8-4 先让场景影响部分合法入账”已被追加计划二十三修正：现在必须先生成并入树 `当前场景特征值明确` 缺口，取得场景特征值、关键特征变化观察、场景影响因果候选和持续稳定性证据后，才允许继续 C8-4 场景影响部分合法入账；C8-5/C8-6 仍保持搜索、默认满足提交和风险安全层聚合的边界。
   追加执行九：C8-4a `风险安全_场景影响部分合法入账前置承接` 已完成当前轮代码验证。`安全_评估当前场景安全性` 已把 `来源需求`、`任务信息节点`、`来源因果信息` 作为可选输入条件读取；候选相对旧 `自我_风险安全_场景影响部分` 的方向现在能区分上调、下调、不变和初始入账，初始入账不再被误记为变化方向未明确。被动归因证据必须来自来源因果信息 + 安全因果候选构造 + 方法动作动态 + 公式版本；持续稳定性证据必须来自持续观察状态 / 样本和关键特征无负向变化。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0；当前诊断路径仍是裸命令调用，控制台显示 `风险安全_场景影响部分不可结算原因=387`，日志显示 `变化方向=4`、`来源需求存在=0`、`来源任务存在=0`、`来源因果信息存在=0`、`被动归因证据存在=0`、`持续稳定性证据存在=0`。当前问题点：C8-4 尚不能正式入账；需要自然任务路径携带来源需求 / 来源任务 / 来源因果信息，并建立持续观察窗口后才能打开提交闸门。
   追加执行十：C8-5a `安全因果层证据搜索通用入口` 已完成当前轮构建接入。原“风险因素搜索”提升为通用 `搜索安全因果因素证据` 核心函数；新增参数化安全对象作用方法 ID 6139 `安全_搜索安全因果因素证据`，输入为安全因果层级、目标安全因果信息、因素ID、证据方向和搜索上下文，输出证据搜索结果场景和动作动态；新增提交入口 ID 6140 `提交_安全因果因素无负证据默认满足` 和 ID 6141 `提交_安全因果因素证据评估值`，只负责写该层该因素默认满足值或证据评估值，不写风险安全层总值、不写自我安全值、不关闭安全根需求。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0，真实入口未触发安全因果因素搜索入账，仍显示风险因素未搜索掩码=30、默认满足掩码=0、风险安全场景影响提交闸门拒绝写账、不结算安全值=1；后续仍需自然任务参数携带具体层级 / 因素 / 搜索范围后，才能对第二层运行、资源、权限、未知四项逐项搜索并进入默认满足或扣分提交。
   追加执行十一：C8-5b `安全因果因素搜索任务参数承接` 已完成当前轮补线。任务筹办在构造候选方法输入包时，对参数化对象作用方法 `安全_搜索安全因果因素证据` 只从目标宿主已有事实复制安全因果层级、因素ID、证据方向、搜索窗口、搜索范围完整状态、证据数量、材料缺失数量、冲突数量、搜索置信度和评估值候选；不会凭空生成搜索结果，也不会写默认满足。对第二层风险安全固定因素，若因素ID已明确但层级缺失，可补层级=2；若层级/因素指向风险安全层且证据方向缺失，可补证据方向=负向。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0，风险因素仍保持未搜索掩码=30、默认满足掩码=0、不结算安全值=1。当前停止点：自然需求 / 任务尚未生成携带具体层级、因素ID和完整搜索范围的目标宿主，因此不会自然执行四个非场景因素搜索；下一步必须先让因果反推或提交入口生成这些参数化搜索子需求，不能用诊断入口伪造搜索范围完整。
   追加计划十二：安全因果风险树口径已落入规范和主动计划。`安全因果风险树` 定义为当前安全目标沿因果信息反向展开形成的运行期因素树，不是预设风险表、抽象存在字段、需求树、任务树或价值账。2026-06-01 已修正所有权：安全因果风险树由自我线程侧维护段持有和更新；任务筹办只有使用权，不得在查询壳内刷新树视图、创建风险树节点或写同步状态。缺口仍经提交入口正式入树，任务管理链再选择 `安全_搜索安全因果因素证据`。
   追加计划十三：安全因果风险树常驻维护任务口径已收紧。维护段只负责在首次、输入变化或低频兜底触发时，反推更新安全因果风险树运行期视图、树节点、同层因素集合、来源因果路径和缺口报告；主循环不等待风险树“稳定”才继续运行。它不得执行证据搜索、不得提交无负证据默认满足、不得提交因素值、不得写风险安全层总值、自我安全值、需求满足或价值账。搜索和入账仍分别由 `安全_搜索安全因果因素证据` 与提交入口完成。
   追加执行十二：C8-5b-3 安全因果风险树常驻反推维护已完成当前轮边界修订。自我线程根层重判后先经外层闸门判断：首次维护、需求 / 任务管理输入变化立即维护，空转轮只按 120 tick 或约 30 秒低频兜底；未触发时不调用维护函数，也不刷缺口提交跳过日志。`任务模块.筹办.ixx` 的旧 `维护安全因果风险树常驻任务` 和 `生成安全因果风险树缺口派生需求消息集` 已改为“任务筹办无安全因果风险树更新权”跳过，普通因果路径查询壳不再调用 `筹办_展开安全因果风险树`。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --self-check-log` 退出码 0；最新日志显示 `更新持有者=自我线程`、`任务筹办更新权=0`、`外层触发原因=首次维护/输入变化`。
   追加执行十三：C8-5c 风险树搜索缺口提交入树与参数化搜索任务化已完成当前轮边界修订。自我线程维护风险树时直接生成缺口派生需求消息集，只从安全因果风险树缺口报告生成 `安全因果因素证据搜索状态 == 已完成` 等派生需求消息，再由既有 `提交_生成缺口子需求` 正式入树；任务筹办不再生成或刷新风险树，只在后续筹办这些合法入树的搜索子需求时消费已有参数。边界保持：输入只来自风险树缺口，不执行搜索，不写默认满足，不写因素值，不写风险安全层总值，不写自我安全值，不结算价值。
   追加执行十四：C8-5d 前置纠偏 `任务筹办通用参数绑定器` 已完成当前轮代码修订。新增计划 `20260531_任务筹办通用参数绑定器纠偏计划_v0.1.md`；任务筹办已集中调用 `通用绑定候选方法输入参数_筹办` 写入通用实参，并按当前方法形参表读取 `参数值类型` 做保守绑定：I64 只从目标宿主同名事实复制，指针只绑定输入条件包、目标方法、目标宿主、目标特征、任务、来源需求、任务虚拟存在等通用上下文或目标宿主同名指针事实；无明确来源的形参继续保留为参数缺口。学习 / 练习内部逻辑已拆口：`自我_尝试学习` 按目标方法头 / 形参规格构造探索候选，`自我_练习` 优先按目标方法条件节点 / 条件结果对构造练习候选，外层筹办仍一视同仁走同一输入闭合流程。Debug x64 构建通过；`.\x64\Debug\鱼巢.exe --auto-observe-stable` 退出码 0，外设桥和安全评估链未回退。
   追加执行十五：C8-5d / C8-5e 已取得第二层四个非场景安全因果因素的自然执行和默认满足提交证据。`.\x64\Debug\鱼巢.exe --demand-tree-growth-summary` 退出码 0；日志显示运行 / 资源 / 权限 / 未知四个因素子任务均通过任务筹办选中 ID 6139 `安全_搜索安全因果因素证据`，真实执行后输出 层级=2、证据方向=1、搜索范围完整状态=1、负向证据数量=0、材料缺失数量=0、冲突数量=0、默认满足允许状态=1；随后经需求满足回写和 ID 6140 `提交_安全因果因素无负证据默认满足` 写入因素值 I64MAX，重复触发时按“已提交或已入账”跳过。提交链保持边界：不写风险安全层总值、不写自我安全值、不写根安全值。诊断入口同步修正：`--demand-tree-growth-summary` 稳定等待已纳入待筹办 / 待执行队列计数，等待窗口由 6 秒调为 30 秒；若摘要超时输出中间态，需以同轮后续日志中的 D1 回写和提交入口收束为准。
   追加执行十六：C8-6 风险安全层聚合候选后处理已接入因素默认满足提交链。每次非场景因素默认满足提交成功或重复入账跳过时，自我线程读取同一安全因果风险树父宿主下的五个同层因素节点，刷新风险安全层候选、投影候选、已入账掩码、默认满足掩码、未搜索掩码、缺失因素数量和可聚合状态。Debug x64 构建通过；最新 `.\x64\Debug\鱼巢.exe --demand-tree-growth-summary` 退出码 0，日志最终显示 `自我线程/风险安全层聚合候选更新 | 已入账掩码=30 | 默认满足掩码=30 | 未搜索掩码=0 | 缺失因素数量=1 | 可聚合状态=0 | 场景影响已入账=0`。这证明非场景四项已可作为候选聚合输入，但场景影响部分尚未合法入账，所以风险安全层仍不能正式聚合入账，更不能写自我风险安全值或根安全值。
   历史下一步修订：原“C8-4 仍是下一业务实现切片”已过时。当前 C8-4 前置为 `当前场景特征值明确` 缺口生成、提交入树和获取路径任务化；`--demand-tree-growth-summary` 等摘要入口不再作为“稳定退出 / 业务闭合”前置门槛。2026-05-31 已修复安全因果风险树派生需求在任务管理工作线程上报时主键槽为空的问题：最新日志显示四条 `安全因果因素证据搜索状态` 子需求已从 `字段不完整=4 / 上报数量=0` 变为 `字段不完整=0 / 上报数量=4` 并正式入树。摘要入口按用户口径降级为给人和协作代理使用的只读即时诊断快照：不等待队列清空，不等待自我线程首轮完成，不主动启动自我线程 / 自检线程，不补齐或完善业务状态；`.\x64\Debug\鱼巢.exe --demand-tree-growth-summary` 最新退出码 0、耗时约 12.48 秒，日志仅显示环境初始化、`控制面板摘要/即时快照` 和轻量快照读取。场景影响部分未入账前，C8-6 只能保留候选视图，不进入 `提交_风险安全层状态变化`，仍禁止把搜索结果场景、默认满足候选、聚合候选或摘要快照解释为风险安全层已入账、自我安全值已结算或安全根需求已满足。
   追加规范梳理十七：需求树逻辑组织语义已按“树形和语义分离”收束。叶子 / 非叶子只判断结构是否展开，不能决定 AND / OR / 因果子链 / 方法路径语义，也不能单独决定是否任务化；需求主信息不得新增 AND / OR 类型、因果子链类型、方法路径组类型、OR组结算令牌或组内权重字段。新增计划 `20260531_需求树逻辑组织语义实现计划_v0.1.md` 已制定。2026-05-31 已执行 P0 / P1 / P3 / P2 / P4 最小切片：常用抽象特征已补入五类目标语义特征，任务筹办已在普通候选方法填充前增加目标语义闸门，需求类已补逻辑组统一更新指令 helper，AND组筹办已补只读聚合视图和组节点满足回写输入；Debug x64 构建通过，`--self-check-log` 通过，`--demand-tree-growth-summary` 退出码 0、耗时约 12.5 秒。摘要入口已改为即时只读诊断，不再作为 P5 / P6 / P7 的退出阻塞或业务验收证据；下一步按该计划 P5 继续 OR组令牌提交和防双花。
   追加清理十八：按用户要求暂停当前计划并清理非正式逻辑代码。`鱼巢.cpp` 已删除 `--self-scene-observe-once`、`--self-scene-html`、`--peripheral-autonomous-observe`、`--autonomous-observe`、`--auto-observe-stable` 等命令行硬串业务链入口，移除命令行中直接查找本能方法、人工创建外设等待项、直接调用自我 / 外设 / 安全评估 / 提交方法链、再以控制台字段判定业务闭合的非正式逻辑。基础相机状态 / 打开 / 释放 / 单帧采集工具、自检日志入口 `--self-check-log`、控制面板和只读摘要入口保留；后续验证不得再依赖上述已删除诊断入口，必须走正式自我线程、任务管理生命周期、提交入口、动作动态、真实运行日志和只读快照证据。Debug x64 构建通过，0 错误，1 个 LNK4075 增量链接警告。
   追加规范梳理十九：需求树逻辑组织语义计划已继续执行到 P7 最小切片。P5 新增 `提交_领取OR组结算令牌` 本能提交入口，令牌状态写入独立 `OR组结算令牌记录` 存在，任务筹办和练习链排除该入口，避免进入普通候选方法或成为任务当前方法；P6 将直接子需求聚合视图扩展到方法路径组 / 因果子链支撑；P7 控制面板快照和需求树摘要新增逻辑组织计数、当前主需求结构形态、目标语义、普通筹办闸门原因、OR令牌状态和当前激活路径，且 OR令牌记录只读查找、不创建记录。Debug x64 构建通过；最新 `.\x64\Debug\鱼巢.exe --demand-tree-growth-summary` 退出码 0、耗时约 12.9 秒，摘要显示 `逻辑组织: 总数=0 | AND=0 | OR=0 | 方法路径=0 | 因果子链=0 | OR令牌目标=0`，当前主需求行显示结构形态、目标语义、普通筹办原因和 OR令牌字段。当前仍缺自然 OR组 / 方法路径组 / 因果子链样本，首次领取 / 重复领取 / 聚合剧本不得宣称验收通过；控制面板展示只作为只读诊断，不作为业务闭合证据。
   追加规范梳理二十：需求获取途径与目标语义口径已写入规范。获取途径只用于提交入口选择生成规则和校验上下文，不写入需求主信息；正式入树后由目标特征类型及抽象特征定义视图承载普通原子目标、AND组、OR组、方法路径组、因果子链和OR令牌语义。自检 / 身体反馈只作为来源输入，自检原子缺口和观察反馈原子缺口生成普通原子目标，自检沿安全 / 服务因果链展开时才走 `自检因果影响链生成 -> 因果子链支撑状态`。任务筹办和临时因果视图只能输出带明确获取途径的草案或回执，不能直接写需求树、不能用控制面板摘要或临时推断反向猜逻辑类型。
   追加执行二十一：C8-4b / C8-4c 场景影响部分前置证据缺口入树来源已接入。安全因果风险树生成场景影响因素节点时，会在目标宿主缺 `关键特征变化观察状态 == 已明确`、`安全因果候选构造状态 == 已明确`、`当前场景持续观察需求明确状态 == 已明确` 或 `当前场景持续观察状态 == 已明确` 时，在同一轮产出正式派生需求消息草案；自我线程提交风险树缺口时已把场景影响前置缺口纳入触发条件和日志，并把消息入口 / 日志口径从“搜索缺口”收口为“风险树缺口”。风险树来源因果结构版本已从 1 提升到 2，避免已有旧同步状态跳过新前置缺口视图。该切片不执行安全因果因素搜索、不写默认满足、不写因素值、不写风险安全层总值、不写自我安全值。Debug x64 构建通过，1 个 LNK4075 增量链接警告，0 错误；自然验收仍等待正式链路执行这些前置需求并重评估场景影响，未宣称 C8-4 已入账。
   追加执行二十二：当前主线按“需求树正确生长优先”收口。正式缺口应先合理入树，活动需求再任务化；任务无法执行时只阻塞该任务及必要父链，调度继续寻找其它可运行任务。已修正活动需求任务化闸门：不再用 `对应任务.有效()` 这种弱引用直接跳过，而是必须解析到真实任务头才视为已有承接任务；旧任务引用不可解析时允许重新提交任务发起意图，避免“需求已入树但旧引用卡住任务生成”。该修正不改任务筹办 / 任务执行边界，不新增模拟样本或诊断业务入口。Debug x64 构建通过，1 个 LNK4075 增量链接警告，0 错误；`--demand-tree-growth-summary` 短时烟雾检查退出码 0，目标绑定完整=14、异常=0、权重异常=0。摘要入口为即时只读，显示任务数=0、未任务化执行需求=12 不作为业务失败或任务化链路验收结论。追加口径：历史直接伤害会生成负向安全因果信息，因此当前风险判断保留 `无负向因果默认安全`；没有负向因果时不阻塞需求树和任务推进，但不直接结算自我安全值。
   追加计划二十三：安全场景影响链的下一步改为先补“当前场景特征值明确”缺口生成。规范已确认：自我定义自身为需要维护安全值的存在，安全值判断依赖安全因果链；风险安全_场景影响部分依赖当前场景风险安全值；当前场景风险安全值依赖当前场景已确认存在及其当前特征值、历史动态、关键特征变化观察、因果归因证据和无负向证据搜索结果。特征值未知时，任务筹办只能使用已有因果信息 / 已有风险树只读视图形成 `当前场景特征值明确状态 == 已明确` 的缺口报告或派生需求草案，正式入树仍走提交入口；任务筹办不得更新安全因果风险树、不得写默认满足、不得写因素值、不得结算安全值。D455 只作为 OR 获取路径之一提供观察帧、材料句柄、像素簇候选、ROI / 掩码 / 深度切片等材料，不直接证明需求满足、场景集合明确、风险明确或安全值增加。
   追加执行二十三：`当前场景特征值明确` 缺口生成已完成当前轮自然验证。`特征类.cpp` 已补 `当前场景特征值明确状态` 常用抽象特征，`需求类.cpp` 已把该枚举目标纳入专用方法生产者分级；`自我线程模块.impl.cpp` 在安全因果风险树缺来源时先确保遗传来源 `自我安全值目标 <- 风险安全层明确状态`，只作为来源因果锚点，不直接生成需求、创建任务、执行搜索或写安全值。风险树场景影响节点若发现当前场景缺 `当前场景特征值明确状态 == 已明确`，会生成携带 `自我安全值->风险安全层->场景影响部分->当前场景风险安全值->场景存在特征值` 与 `OR路径草案=D455观察确认/已有场景缓存/交互者说明` 的缺口消息，再经 `提交_生成缺口子需求` 正式入树。Debug x64 构建通过，0 错误，1 个 LNK4075 警告；`--self-check-log` 退出码 0；最新日志显示 `遗传因果信息入账`、`场景特征值明确缺口报告数量=1`、`提交_生成缺口子需求 | 成功=是 | 需求类型=当前场景特征值明确状态`。边界保持：不执行搜索、不写默认满足、不写因素值、不写风险安全层总值、不写自我安全值，不宣称 C8-4 场景影响部分已入账。
   当前下一步修订：执行 `20260601_安全因果链反推当前场景特征值需求纠偏计划_v0.2.md`。C8-4 仍不得直接追 `风险安全_场景影响部分` 入账；下一切片改为让已入树的 `当前场景特征值明确` 需求继续展开 OR 获取路径：D455观察确认路径 / 已有场景缓存路径 / 交互者说明路径。优先补可由现有正式方法承接的路径目标，如当前观察特征帧取得、外设观察材料可回查、外设像素簇候选已验证、外设观察存在候选确认方案已取得、已确认观察存在已提交入账、当前场景存在集合明确、当前场景各存在特征值可读；D455 材料仍不得直接证明需求满足、风险明确或安全值增加。
   追加计划二十四：006 框架已从单一安全感知扩展为“从自我定义最小推导安全感知与服务感知需求的因果框架”。服务轴口径：服务主体是自我；服务对象限定为人类，运行期可为当前权限者 / 当前交互者 / 权限者授权对象；服务值来源不是任务完成或输出存在，而是自我动作导致服务对象目标状态正向改善，且该改善被权限允许、结果可验证、因果可归因。服务侧感知需求包括当前交互环境事实、当前交互者、当前权限者、当前人类服务对象、当前服务目标、服务对象当前状态、服务结果验证和服务归因等广义事实取得，不限相机观察。无服务触发事实时仍不得仅凭服务值差距生成这些主动子需求。
   服务链下一步：在服务触发事实、当前会话输入、人类表达、外部请求、已确认服务目标、服务动作结果或反馈事实具备后，先让临时因果视图 / 服务根闭环信息位产出服务感知缺口草案，经提交入口入树，再由读取输入、解析会话、询问澄清、读取服务对象状态、检查输出结果场景或获取反馈等对象作用方法取得证据。服务评价改为分级口径：单独无反馈、未知结果、任务运行成功或输出文本存在，不能默认推出服务对象已经受益；若服务对象已经接受服务且反馈窗口内无负反馈，可以保守评价为 `可以 / 可接受`；该候选不得升级为很好、优秀、满分或无条件服务值结算。
   允许修改范围：基础数据类型、本能方法登记表中当前方法的单项登记、观察链自我动作、外设观察动作、内部 / 外部本能函数注册包装、枚举目标生产者分级校验、必要的任务筹办派生消息映射、控制面板展示、对应实施记录和计划索引。
   禁止修改范围：安全值 / 服务值结算、任务筹办 / 任务执行本能方法恢复、模拟样本或伪造入口、真实硬件验证结论扩大化。
   验收证据：先写预测；Debug x64 构建；真实运行日志或状态快照证明各枚举实例值由合法生产者写入，并能在缺条件时拒绝写入 `可用`。
   禁止宣称：不得把观察帧存在、像素级可回查底座或质量可用直接解释成基础风险已明确；不得宣称内部循环全部完成、自我苏醒完成或初步成熟完成。
```

## 1.1 双目相机外设功能实现索引

本节用于在计划索引中收束相机外设实现线。它暂不覆盖 `## 1. 当前主动队列`；只有当主动队列切换到相机外设功能实现，或真实 D455 条件具备并需要推进三种观察运行模式订阅时，才按本节顺序执行。

### 1.1.1 规范依据

```text
040005 外设观察特征与自我场景认知本能方法分层规范20260525
040007 双目相机外设独占观察线程规范20260527
040008 双目相机外设功能能力与实现途径总结20260527
040010 外设独立控制线程与消息承接边界规范20260527
050004 观察像素簇与存在候选分层规范20260527
001 方法动作动态硬规则规范20260512
002 自我线程与任务管理线程权责边界规范20260512
004 特征值三态比较底层逻辑规范20260518
005 主信息字段与结构新增准入约束规范20260519
```

执行边界：

```text
双目相机是稳定观察特征供应器，不是世界存在识别器。
对外高层运行模式只收敛为陌生环境逐簇识别、已识别场景扫描变化、目标跟踪观察。
观察像素簇、掩码、彩色轮廓局部图、深度轮廓局部图是正式簇级特征。
外设线程只管理外设自身和外设消息。
每个外设实例必须有独立控制域；高实时、高带宽、状态复杂外设宜每个物理实例独占 OS 线程。
相机线程内部能力直接用工程代码实现，不通过本能方法拆解采集、滤波、分割、跟踪或学习步骤。
任务管理工作线程负责外设消息承接、等待项匹配和回执 / 派生消息。
提交入口负责裁决是否入需求树 / 世界树。
旧像素级 100% 只表示帧内可寻址、可回查、无遗漏、无重复。
ROI、补观察、质量诊断、重捕获、配置学习和局部细分只作为模式内部策略、等待项参数或报告字段。
```

说明：本节下方历史实施摘要里的“扫描 / 跟踪”等旧命名只表示既有代码与计划的过渡事实；后续新切片应按逐簇识别报告、扫描变化报告、跟踪报告三类重新收束，不得恢复“扫描 / 跟踪 / 补观察 / 局部细分”等多外显模式口径。

### 1.1.2 待执行计划角色

```text
主实施驱动：
    计划/待完成项/20260527_双目相机观察队列承接代码纠偏与实现计划_v0.1.md

功能蓝图：
    计划/待完成项/20260527_双目相机观察像素簇接入实现方案_v0.1.md

外设线程基础与硬件诊断：
    计划/待完成项/20260527_外设自主观察线程稳定采样实施计划_v0.1.md

外设本能方法三分类历史底座：
    计划/待完成项/20260524_外设本能方法三分类与双目相机方法登记实施计划_v0.1.md

旁路自然复验：
    计划/待完成项/20260527_观察质量低阈值与父需求裁决自然复验计划_v0.1.md
```

执行时以“主实施驱动”为切片入口；“功能蓝图”用于核对字段、报告和验收；“稳定采样计划”只验证外设线程和真实硬件条件；外设方法三分类计划只提供自我 / 任务管理侧控制、检查、使用边界，不作为相机线程内部实现方案，也不恢复旧的“发现候选存在 / 判断在场”口径。

### 1.1.3 推荐实施顺序

```text
A0 代码状态冻结和术语纠偏
    目标：确认当前 `外设线程_D455深度相机.ixx`、双目相机适配器、观察特征帧、像素索引、空间候选链的真实现状。
    依据：队列承接计划 C0。
    禁止：不改算法、不加模拟入口、不宣称正式队列已完成。

A1 建立双目相机独立控制域 / 外设线程壳
    目标：把 D455 外设线程作为双目相机物理实例的独立控制域，明确命令队列、状态缓存、错误状态和生命周期。
    依据：040010、稳定采样计划 S0-S1。
    实现：线程内部直接调用相机适配器和私有工程模块，不把采集 / 稳定性比较拆成本能方法。
    验收：无 D455 时只返回明确硬件失败原因；有 D455 时能连续采样并给出稳定性依据。

A2 补齐外设观察报告队列、等待项和缓存结构
    目标：新增外设观察报告队列项、三种观察运行模式等待项、最新逐簇识别报告缓存、最新扫描变化报告缓存、最新跟踪报告缓存、稳定特征缓存。
    依据：队列承接计划 C3、040007、040010。
    字段：报告ID、来源外设、报告类型、观察运行模式=陌生环境逐簇识别/已识别场景扫描变化/目标跟踪观察、目标区域/簇/候选、ROI/掩码/跟踪种子、时效、质量要求、内部策略提示。

A3 先落陌生环境逐簇识别报告最小闭环
    目标：从现有真实单帧 / 空间候选过渡生成逐簇识别报告。
    输出：观察像素簇集合、簇ID掩码、低质量区域集合、未归簇区域集合、不可观测区域集合、失败原因。
    依据：观察像素簇接入方案 S1-S3、050004、040008。
    实现：逐簇识别报告、分簇、掩码和质量字段由相机线程内部代码直接生成；边界只暴露报告和队列项。
    禁止：不得把逐簇识别报告写成稳定世界存在、语义识别完成或已确认观察存在。

A4 生成观察像素簇正式特征
    目标：把空间候选过渡为观察像素簇，逐步补齐像素集合掩码、彩色轮廓局部图、深度轮廓局部图、颜色/深度/空间摘要和稳定性摘要。
    依据：观察像素簇接入方案 S2、050004、040008。
    验收：每个簇至少有来源帧、外设内部簇ID、像素数量、边界框、中心点和失败原因。

A5 实现任务管理外设消息承接 helper
    目标：任务管理工作线程消费外设队列和缓存，匹配等待项 / 活跃需求。
    输出：任务回执、派生需求消息、动作动态输入、观察事实承接请求。
    依据：队列承接计划 C5-C6、040010。
    禁止：任务管理不得直接写需求树或世界树真值。

A6 实现自我侧观察报告承接
    目标：自我只承接被任务管理领走并请求裁决的报告。
    方法：自我_承接双目相机观察报告、自我_承接观察像素簇集合、自我_由观察像素簇生成存在候选、自我_统计当前帧存在识别情况。
    依据：观察像素簇接入方案 S5-S7、040008、050004。
    禁止：存在候选不得直接等同于已确认观察存在。

A7 再落跟踪报告闭环
    目标：在已有逐簇识别报告、扫描变化报告和观察像素簇基础上实现目标跟踪等待项和跟踪报告。
    输出：跟踪ID、跟踪种子、最近观察簇、跟踪状态、丢失状态、稳定特征集合、复现离散度、连续成功次数、重捕获候选集合。
    依据：观察像素簇接入方案 S3/S8、040007、040008。
    禁止：外设内部跟踪ID不得写成自我世界存在ID。

A8 接入实时性、舍弃策略和控制面板复现
    目标：报告年龄、处理耗时、队列积压、丢帧率、处理策略、丢弃原因可回查；控制面板只显示自我承接后的结果。
    依据：040008、040010、观察像素簇接入方案 S9。
    禁止：控制面板显示成功不得替代业务验收。

A9 真实 D455 验证
    目标：只用真实入口、真实硬件日志、状态快照、动作动态和错误日志验证。
    验收顺序：构建 -> 缺硬件失败路径 -> 连续采样 -> 逐簇识别等待项命中 -> 任务管理承接 -> 自我承接 -> 目标跟踪等待项命中。
    禁止：不得使用模拟样本、夹具入口、伪造动作动态或把硬件失败扩大解释成业务失败。
```

### 1.1.4 第一可执行切片

主动切到相机外设线时，第一可执行切片应是：

```text
切片名：D455 外设观察报告队列与逐簇识别报告最小闭环

做：
    1. 固化外设观察报告队列项和三种观察运行模式等待项结构。
    2. 让 `外设线程_D455深度相机` 在真实采样成功时构造逐簇识别报告队列项。
    3. 逐簇识别报告第一版复用现有观察特征帧 / 空间候选摘要作为观察像素簇过渡来源。
    4. 任务管理承接 helper 能按逐簇识别等待项匹配逐簇识别报告或缓存。
    5. 命中后只生成任务回执或观察事实承接请求。
    6. 相机线程内部实现直接用代码完成，不新增相机内部本能方法。

不做：
    1. 不实现跟踪闭环。
    2. 不补全所有局部图真实缓冲句柄。
    3. 不把质量诊断、补观察、ROI、局部细分做成外设模式。
    4. 不让自我根需求直接消费外设队列。
    5. 不宣称存在候选已确认或基础风险已明确。
```

执行状态 2026-05-28：

```text
已落地 `外设观察报告队列.ixx`，包含扫描 / 跟踪等待项、外设观察报告队列项、最新报告读取、按报告ID回查和按等待项匹配函数。
外设观察报告队列项已补入跟踪报告字段，包括跟踪ID、跟踪种子、跟踪状态值、丢失状态值、最近观测年龄、复现离散度、连续成功次数、失败次数、重捕获候选数量和稳定特征样本数量。
`外设线程_D455深度相机` 已在真实采样成功时构造扫描报告队列项；如果存在跟踪等待项，会基于当帧扫描材料生成跟踪报告草案，缺少种子或匹配不到目标簇时明确输出丢失 / 失败原因；同一等待项 / 种子的跟踪稳定缓存已在外设线程内部维护，连续命中后才累计连续成功次数、稳定特征样本数量和复现离散度。
`--peripheral-autonomous-observe` 诊断入口已能清空队列、启动 D455 线程、创建扫描等待项、尝试匹配扫描报告，并在命中时按报告ID回查。
`任务模块.管理工作线程` 已新增最小外设消息承接 helper，能够按扫描等待项匹配外设观察报告，并在命中后形成任务回执 / 观察事实承接请求上行消息；未命中时只保留缓存结果，不上报自我线程。
任务管理上行协议已新增 `观察事实承接请求`，自我线程消息协议已能转换为治理消息并保留载荷；自我线程已能把任务管理领走的观察报告请求落成自我内部承接记录，并按报告ID回查报告缓存；报告可回查时可承接观察像素簇集合、记录候选数量和识别摘要状态、为每个观察像素簇生成候选记录的一阶摘要特征，但仍不直接生成已确认观察存在或基础风险真值。
任务工作项已新增外设观察等待项承接入口；任务管理工作线程在本轮筹办 / 执行后可消费该等待项，并把命中的观察事实承接请求补齐任务主键、幂等键后合并进本轮上行消息集。
任务管理界面线程已能按正式需求目标特征文本自动创建扫描 / 跟踪型外设观察等待项并放入任务工作项；该匹配目前仍是最小文本映射，后续需继续收束到正式抽象特征生产者。
自我线程邮箱幂等键已纳入观察事实承接请求的等待项ID、报告ID、来源外设、报告类型和簇数量，避免不同观察报告被误合并。
关键日志已补强：扫描 / 跟踪报告入队日志会携带外设观察报告摘要，任务管理生成观察事实承接请求时会输出完整承接请求文本，便于真实 D455 验证时直接核对报告、等待项、承接请求和自我回查链路。
Debug x64 构建通过。
当前环境无 D455，真实运行进入硬件打开失败路径，队列数量为 0，扫描等待项匹配结果为无，任务管理承接结果为无匹配报告，上行消息数为 0。
```

执行状态 2026-05-29：

```text
已将外设观察等待项字段从旧 `观察意图类型` 迁移为 `观察运行模式`，枚举收束为陌生环境逐簇识别、已识别场景扫描变化、目标跟踪观察三类；报告类型收束为逐簇识别报告、扫描变化报告、跟踪报告，并保留旧命名别名仅作兼容。
`外设线程_D455深度相机` 已在真实采样成功时默认生成逐簇识别报告；若存在扫描变化等待项，会用上一帧逐簇识别报告作基准生成扫描变化报告；若存在目标跟踪等待项，会用逐簇识别材料和跟踪种子生成跟踪报告。
`--peripheral-autonomous-observe` 诊断入口已在启动线程前创建逐簇识别 / 扫描变化 / 目标跟踪三个等待项，并分别验证队列匹配、按报告ID回查和任务管理承接。
任务管理承接 helper 已按三类报告分流：逐簇识别报告生成观察事实承接请求并允许承接簇、生成存在候选和统计当前帧识别；扫描变化报告生成观察事实承接请求但不生成存在候选；跟踪报告生成稳定特征承接请求。
Debug x64 构建通过。真实 D455 运行出现 3 个真实样本、9 条报告队列项；逐簇识别等待项命中报告ID=7，扫描变化等待项命中报告ID=8，目标跟踪等待项命中报告ID=9，三者均可按ID回查并生成任务管理上行承接请求。
后续已将 D455 诊断稳定性拆成基础采样稳定与观察候选稳定两层，并实现外设线程内部跨帧观察簇缓存。逐簇识别报告中的 `外设内部簇ID` 已改为跨帧稳定ID，原始单帧分簇编号保留为 `来源空间候选ID`；报告新增跨帧匹配簇、新建簇、稳定簇、丢失簇和跨帧采集摘要。真实 `--peripheral-autonomous-observe` 运行退出码为 0，3 个真实样本均可比，基础采样稳定连接=2，观察候选稳定连接=2，综合稳定=稳定；最新逐簇识别报告命中报告ID=7，跨帧匹配簇=73，跨帧稳定簇=68；逐簇识别 / 扫描变化 / 目标跟踪三种观察链路均通过。
掩码 / 局部图已完成句柄级最小接入：外设线程在有效投影簇上生成像素集合掩码句柄、彩色轮廓局部图句柄和深度轮廓局部图句柄，报告摘要新增掩码簇、彩色局部图簇和深度局部图簇计数。Debug x64 构建通过；真实 `--peripheral-autonomous-observe` 运行退出码为 0，基础采样稳定=1，观察候选稳定=1，三种观察链路通过=1，综合稳定=1；最新逐簇识别报告为像素簇=86、掩码簇=86、彩色局部图簇=86、深度局部图簇=86，扫描变化报告为新增簇=22、消失簇=15、变化簇=0 且同样携带 86 个句柄簇，目标跟踪报告携带 1 个句柄簇。
```

剩余：

```text
真实 D455 连续样本已通过基础采样稳定和观察候选跨帧稳定判据；默认路径只跑逐簇识别等待项，扫描变化和目标跟踪只在有明确指令 / 已确认存在后进入。短期观察材料库和 `d455://` 解引用已完成当前轮接入，逐簇识别报告能回查原始帧、深度帧、对齐帧、簇掩码 / ROI / 点集、深度切片和像素归属账本。
C5-3a / C5-3b / C5-3c 已完成当前轮真实链路：自我侧先验证外设观察像素簇候选，再输出外设观察存在候选确认方案，最后由 `提交_确认观察存在事实` 正式创建 / 合并观察存在、写原始观察事实、动作动态、二次特征和当前场景索引。不得再把外设报告、验证通过候选或跨帧簇ID当成已确认观察存在。
C4.7 已改为只消费已确认观察存在及其已提交 ROI / 掩码 / 轮廓原始事实；C4.6-2 已能基于已确认存在建立内部世界局部材料和子候选视图；C4.6-3 已补内部世界子候选确认方案和提交入账链，子候选不能直接入账，后续若继续细分则必须消费已提交的内部世界子存在。
自然需求链来源因果复验已完成当前轮证据：真实来源需求生成的根必要条件子需求已命中来源为“遗传”的现有因果信息，提交入口已把来源因果键写入派生归因缓存；`--demand-tree-growth-summary` 摘要显示 `派生来源因果=12`。无负向因果证据时默认良好、场景评估安全值提交、外设观察、存在确认和安全评估链仍由 `--auto-observe-stable` 真实日志验证。
```

### 1.1.5 证据闸门

```text
1. 每个切片先写预测，再构建，再真实运行或记录缺硬件条件。
2. Debug x64 构建必须通过。
3. 缺 D455 时只允许记录 `No device connected` 或等价硬件失败原因。
4. 有 D455 时必须出现外设线程启动、采样、停止日志。
5. 队列项必须能按报告ID、来源外设、报告类型、时间戳回查。
6. 无等待项 / 无活跃需求时只更新缓存，不入树。
7. 任务管理命中等待项后只能生成回执、派生需求消息、动作动态输入或观察事实承接请求。
8. 提交入口裁决后，才允许观察像素簇集合、存在候选或识别摘要进入自我认知链。
9. 所有目标表达继续使用 大于 / 小于 / 等于。
10. 不得把相机线程、任务管理 helper 或控制面板显示当成世界树真值来源。
```

### 1.1.6 明日真实 D455 验证清单

```text
验证日期建议：2026-05-29

准备：
    1. 确认只运行一个 `鱼巢.exe`。
    2. 接入 D455，并确认系统能枚举设备。
    3. 先执行 Debug x64 构建。

验证命令：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    .\x64\Debug\鱼巢.exe --peripheral-autonomous-observe

预测结果：
    1. 有 D455 时应出现外设线程启动、采样、逐簇识别报告入队日志。
    2. 逐簇识别报告摘要应包含报告ID、来源外设、逐簇识别报告、帧尺寸、像素簇数量、深度有效率、空间坐标有效率和质量评分。
    3. 逐簇识别等待项应命中逐簇识别报告，按报告ID回查为有。
    4. 任务管理外设消息承接应生成观察事实承接请求。
    5. 若存在正式跟踪需求或跟踪等待项，应出现跟踪报告入队；缺种子或目标簇未命中时应明确报告丢失 / 失败原因，不得伪装稳定。

必查日志：
    外设线程_D455深度相机/启动
    外设线程_D455深度相机/采样
    外设线程_D455深度相机/逐簇识别报告入队
    外设线程_D455深度相机/跟踪报告入队
    任务管理界面线程/自动创建外设观察等待项
    任务管理外设消息承接/处理一次
    任务管理外设消息承接/生成观察事实承接请求
    自我线程/外设观察事实承接开始
    自我线程/外设观察事实承接回查成功
    自我线程/外设观察事实承接完成

通过判据：
    1. Debug x64 构建 0 错误。
    2. 逐簇识别报告至少 1 条真实入队。
    3. 逐簇识别等待项至少 1 次命中，任务管理生成观察事实承接请求。
    4. 自我线程只记录观察报告、观察像素簇候选和存在候选摘要，不出现已确认观察存在、基础风险已明确或安全 / 服务值结算宣称。

失败 / 待条件判据：
    1. `No device connected` 只记为硬件条件不足，不记为业务逻辑失败。
    2. 逐簇识别报告入队但等待项不命中，优先查目标外设、报告类型、观察运行模式和报告年龄。
    3. 任务管理命中但自我无法回查报告，优先查报告ID是否仍在队列容量内。
    4. 跟踪报告未出现，优先查是否确实存在跟踪等待项和可解析跟踪种子。
```

## 2. 已运行压缩项

以下计划已有运行结果或阶段结论，当前只保留检查点和恢复条件，不作为主动队列阻塞项。

1. `20260524_双目相机像素级观察存在化阶段目标计划_v0.1.md`
   压缩状态：该旧计划成果现降级为“像素级可回查底座”历史材料，只表示帧内可寻址、可回查、无遗漏、无重复；不得继续解释为像素级识别目标或逐像素物化主线。最新真实 D455 单帧结果保留在原计划与 `实施记录/20260524_双目相机像素级观察存在化_Codex断点清单.md`。
   恢复条件：继续视觉线时优先取回观察像素簇接入线；必要时另拆像素级可回查索引、轮廓线集合、帧级归属掩码、真实彩图 / 深度图驱动递归分割或 GPU 绘制缓冲。

2. `20260525_外设观察特征与自我认知本能方法补齐计划_v0.1.md`
   压缩状态：S1-S8 已完成摘要级外设观察特征帧接收、自我观察帧建立、像素索引、空间候选、存在确认、内部世界递归、变化发现和复现数据导出。
   恢复条件：只在继续外设 / 自我观察方法层能力时取回；不得把摘要级账本解释成完整视觉分割或跨帧稳定身份。

3. `20260525_观察特征质量诊断与认知补偿实施方案_v0.1.md`
   压缩状态：Q1-Q8.3b 已完成 RGB、原始深度、滤波 / 补全 / 融合深度口径、质量诊断、轮廓置信度、短帧组融合、补观察缺口和控制面板诊断区域展示。
   恢复条件：继续视觉诊断时另拆轮廓线集合、mask 明细或递归视觉分割；不得把补全 / 融合深度覆盖原始深度。

4. `20260526_任务筹办因果路径筹办阶段实现计划_v0.2.md`
   压缩状态：S1-P6 已完成；任务筹办可生成学习缺口派生需求消息，经任务管理上行后由 `提交_生成缺口子需求` 或统一需求树更新提交入口裁决入树并回执任务管理侧，子任务生成后父任务转等待。
   检查点：`实施记录/20260526_任务筹办因果路径筹办阶段_Codex断点清单.md`
   恢复条件：后续真实运行出现子需求完成并解除父任务等待时，再复验父需求目标回查和因果反馈统计。

5. `20260526_枚举型实例特征值生产者分级实施计划_v0.1.md`
   压缩状态：E0/E1/E2 已完成；枚举值域初始化与实例值写入已分层，入树校验会拒绝比较符不合法、目标值不属于值域、生产者未登记或待补齐的枚举目标。
   检查点：`实施记录/20260526_枚举型实例特征值生产者分级_Codex断点清单.md`
   恢复条件：只有计划索引重新选择枚举生产者线时，才继续 E3 外设状态生产者或 E4 观察事实状态生产者。

## 3. 待条件具备后继续

`计划/待完成项` 只存放当前阶段默认完成但仍需真实证据复验或保留历史背景的计划，后续仅在真实运行日志、状态快照、动作动态或错误日志出现明确恢复条件时取回。

1. [20260524_外设本能方法三分类与双目相机方法登记实施计划_v0.1.md](待完成项/20260524_外设本能方法三分类与双目相机方法登记实施计划_v0.1.md)
   状态：当前阶段实现与验证准备完成；S1/S2/S3/S4/S5/S6/S8 已闭合，Debug x64 构建通过。
   恢复条件：具备真实 D455 硬件、真实目标样本和真实需求链路运行日志。
   恢复后验证：使用需求先命中使用类方法；缺外设可用性 / 运行状态时派生控制 / 检查子需求；控制 / 检查 / 使用动作动态落账来源清楚；`双目相机本能方法结果` 日志含帧号、轮廓数量和失败原因。
   禁止宣称：不得宣称真实 D455 链路通过、`获取特征值` 已可执行、目标可见性 / 在场状态已闭合、内部循环全部完成、自我苏醒完成或初步成熟完成。
   最新口径：该历史计划只作为边界契约参考；相机线程内部观察、扫描、跟踪和报告生成不再按本能方法实现。

2. [20260527_观察质量低阈值与父需求裁决自然复验计划_v0.1.md](待完成项/20260527_观察质量低阈值与父需求裁决自然复验计划_v0.1.md)
   状态：实现和可得真实证据已完成；但其中“观察质量父需求依赖裁决”属历史实现口径，后续按新增上位边界迁移为需求侧 / 因果侧数据驱动。
   恢复条件：真实日志出现 `深度有效率 < 5000`、`空间坐标有效率 < 5000`，并且已有需求侧条件、方法条件、条件结果对、现有因果信息或既有子需求声明观察质量提升前置关系。
   恢复后验证：只走真实入口和日志核对；确认 S1 只写事实、旧帧不被洗白、任务筹办不按 S1 名称或领域特征名特判、不直接注入质量提升目标、自我线程不直接把父任务置为等待。
   禁止宣称：不得用模拟样本、夹具入口或强改任务筹办逻辑制造验证条件；不得把未触发写成失败或通过。

3. [20260527_外设自主观察线程稳定采样实施计划_v0.1.md](待完成项/20260527_外设自主观察线程稳定采样实施计划_v0.1.md)
   状态：`外设线程_D455深度相机.ixx` 独立模块已建立；Debug x64 构建通过；`--peripheral-autonomous-observe` 已在真实 D455 样本上跑通逐簇识别 / 扫描变化 / 目标跟踪三种等待项、报告入队、按ID回查和任务管理承接；稳定性已拆成基础采样稳定与观察候选稳定，并已通过外设内部跨帧观察簇缓存取得真实跨帧证据。扫描变化已改用专用跨帧簇ID匹配，当前真实样本基础采样稳定=1、观察候选稳定=1、三种观察链路通过=1、综合稳定=1、扫描变化报告变化簇=0。真实存在候选验证和正式场景入账仍未实现。
   恢复条件：开始处理掩码 / 局部图句柄、真实存在候选验证 / 正式场景入账，或开始把正式需求目标特征映射从最小文本规则收束到正式抽象特征生产者。
   恢复后验证：连续出现 `外设线程_D455深度相机/启动`、至少 3 条 `外设线程_D455深度相机/采样` 和 `外设线程_D455深度相机/停止`；三种观察运行模式报告均可命中等待项并由任务管理承接；基础采样稳定必须由帧尺寸、像素特征、深度有效率、空间坐标有效率和质量评分确认，观察候选稳定必须用跨帧匹配簇、稳定簇、丢失簇和主跨帧簇连续命中确认。
   禁止宣称：不得把线程启动、硬件失败或等待超时宣称为真实稳定采样通过；不得新增模拟样本、夹具入口或伪造动作动态；不得为不同观察运行模式各造一套外设系统。

4. [20260527_双目相机观察像素簇接入实现方案_v0.1.md](待完成项/20260527_双目相机观察像素簇接入实现方案_v0.1.md)
   状态：方案已按“同一外设线程 + 三种观察运行模式订阅 + 外设生产、队列缓存、需求驱动消费、任务管理承接、提交入口裁决入账”口径开始实施；队列、等待项、报告类型、诊断入口和任务管理承接已完成首轮三模式化。掩码、局部图、正式存在候选验证和正式场景入账仍未实施。
   恢复条件：计划索引继续选择双目相机观察像素簇接入线，或需要推进掩码、局部图、观察像素簇正式特征、自我承接为存在候选和存在候选验证。
   恢复后验证：按 S0-S9 小切片推进；重点补验正式需求链路创建带观察运行模式的等待项并消费队列或缓存；每切片先写预测，再用构建、真实运行日志、状态快照、动作动态和错误日志核对；不得使用模拟样本、夹具入口或伪造动作动态。
   禁止宣称：不得宣称相机识别世界、观察像素簇是稳定世界对象、存在候选已确认、基础风险判断已明确、安全 / 服务根需求可直接消费外设原始队列、控制面板显示即业务验收通过，或不同观察运行模式需要拆成多套外设系统。

5. [20260527_双目相机观察队列承接代码纠偏与实现计划_v0.1.md](待完成项/20260527_双目相机观察队列承接代码纠偏与实现计划_v0.1.md)
   状态：已实施首轮三模式化代码：`外设观察报告队列.ixx` 支持观察运行模式和逐簇识别 / 扫描变化 / 跟踪三类报告；`外设线程_D455深度相机.ixx` 在采样成功时生成逐簇识别报告、按等待项生成扫描变化报告和目标跟踪报告；任务管理最小承接 helper 已按报告类型生成观察事实承接请求或稳定特征承接请求；诊断入口已能验证三类等待项命中、报告回查和上行承接。真实存在候选验证和正式场景入账仍未正式接入。
   恢复条件：计划索引继续选择双目相机观察队列承接实现线，或开始推进正式需求链路、稳定样本达标、正式掩码 / 局部图、存在候选验证和自我所在场景入账。
   恢复后验证：按 C0-C9 小切片推进；继续补三种观察运行模式的正式需求创建、稳定特征缓存、观察像素簇正式特征、自我承接观察像素簇和存在候选验证；每切片先写预测，再用 Debug x64 构建、真实运行日志、状态快照、动作动态和错误日志核对。
   禁止宣称：不得宣称基础 D455 线程模块已经完成任务管理承接；不得宣称安全 / 服务根需求可直接消费外设原始队列；不得把观察像素簇写成稳定世界对象、存在候选已确认或基础风险判断已明确；不得把不同观察运行模式拆成多套外设系统。

## 4. 根目录已清理计划

根目录旧计划已按完成、替代或后置原因清理，不再作为当前执行依据；需要复盘时从 `计划/完成项`、`计划/待完成项` 或历史记录查询。

2026-05-27 本轮按计划生成约束完成根目录清理：

```text
保留：
    计划/计划索引.md
    计划/20260526_基础观察事实与基础风险判断分层实施计划_v0.1.md
    计划/20260527_R1_当前观察帧新鲜度判定规则与合法生产者收口计划_v0.1.md
    计划/20260527_R1本能方法_自我判断基础观察事实可用性实施计划_v0.1.md
    计划/20260529_R2本能方法_安全评估当前场景安全性实施计划_v0.1.md
    计划/20260529_R3外设相机能力接入自我内外交互桥实施计划_v0.1.md
    计划/20260531_需求树逻辑组织语义实现计划_v0.1.md
    计划/20260531_当前交互者明确状态与学习缺口简化纠偏计划_v0.1.md

保留但不作为执行计划：
    计划/20260526_枚举型实例特征值生产者登记表.md

已删除根目录过期计划：
    已运行压缩项对应旧计划
    已被新口径替代的像素级识别 / 观察质量补救旧计划
    已完成并压缩进索引和断点清单的 R1 方法小票
    仅占位、未进入当前主动队列的具体质量补救方法小票
```

## 5. 当前证据闸门

```text
1. 当前主动目标只推进基础观察事实可用第一阶段；基础风险判断已明确属于后续阶段。
2. 缺自然样本只能记录“等待真实样本”，不得补模拟样本。
2a. 观察质量低阈值帧和观察质量父需求裁决自然复验已移入待完成项；未出现恢复条件前不得继续强行验证，也不得阻塞当前主动队列。
2b. `外设线程_D455深度相机` 基础模块已建立，真实 D455 基础采样稳定、观察候选跨帧稳定和三种观察链路已通过；硬件失败或等待超时只说明外设链路未形成可用样本，不得记成业务逻辑失败或观察候选稳定通过；对外只推进同一外设线程上的陌生环境逐簇识别、已识别场景扫描变化、目标跟踪观察三种订阅，质量诊断、ROI、补观察、稳定取值、重捕获、局部细分、变化监测和配置学习只作为内部策略、报告字段或自我后续方法。
3. 服务结算通过分支仍等待服务对象外在特征状态变化、反馈验证和归因证据。
4. 学习需求四类剧本不作为当前主动验收目标；无路径 / 无方法的普通任务先形成明确挂起回执并让调度继续其他任务，不得伪造候选方法、执行入口、条件结果对缺口或泛化学习信息补齐需求。
5. 正式结算仍等待真实 D0 / D1 / actual / D2 证据链样本。
6. 只能把有证据的范围标为通过，不扩大解释为内部循环闭合、自我苏醒或初步成熟。
7. 第一阶段观察闭环只能证明 `当前观察帧.基础观察事实可用状态 == 可用`；不得把当前观察帧存在、像素级可回查底座或质量可用直接解释成 `基础风险判断状态 == 已明确`。
7a. 安全观察链只产生场景事实、存在集合、自我关键特征变化和安全因果候选；安全值结算必须等因果归因和价值投影闸门达标后再发生。
7b. 外设相机能力接入是自我内外交互桥，不是相机线程直写世界真值；正式链路必须是 外设报告 -> 任务管理承接 -> 提交入口裁决入账 -> 场景事实 -> 安全评估，不能让安全 / 服务根需求直接消费相机原始流。
8. 已建立 / 已取得 / 已覆盖 / 已完成 / 已完备 / 新鲜 / 可用 / 已明确 等枚举型实例特征值必须由合法生产者写入；没有输入证据、判定标准、输出宿主、输出特征和动作动态 / 来源动态 / 规则证据时，只能记录为缺口、待验证或待补齐，不能入账为已成立状态。枚举值域初始化不受此写入约束，不得把抽象特征树 I64 点状态误当成实例值生产。
9. remote compact / stream disconnected / websocket / network error 只表示远程执行通道中断，不改变项目实现状态、验收状态或业务结论。
10. 叶子 / 非叶子只作为结构初筛；AND组、OR组、因果子链、方法路径组和是否可任务化必须由目标特征及抽象特征定义视图判定。不得把逻辑组语义、OR结算令牌或组内权重塞进需求主信息。
```

## 6. 验证模板

```text
计划：
真实触发路径：
预测结果：
必查日志：
禁止出现：
缺自然样本判定：
实际日志证据：
结论：
下一步：
```
