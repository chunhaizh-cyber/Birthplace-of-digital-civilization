# 计划索引

更新时间：2026-06-08

## 0. 索引用途

本文件只作为当前执行路由，不承载完整历史流水账。历史细节从对应计划、实施记录、真实日志、`计划/待完成项` 和 `计划/完成项` 查询。

维护规则：

```text
1. 规范优先于计划；计划优先于临时推断。
2. 当前主动队列只保留一个可闭合切片。
3. 已运行内容只保留检查点、证据范围和恢复条件。
4. 待条件项不得混入当前主动队列。
5. 每次验证必须先写预测，再走真实运行路径，最后用日志、状态快照、动作动态和错误日志核对。
6. 严禁新增或依赖模拟测试、夹具测试、伪造样本、伪造入口、伪造动作动态或 `--*-check` 回归命令。
7. 任务筹办 / 任务执行是任务管理生命周期普通函数，不恢复为本能方法。
8. 不提前宣称自我苏醒完成或初步成熟完成。
```

## 1. 当前主动队列

```text
第一优先计划 / 当前主动计划：
    计划/20260607_运行速度常驻路径优先优化计划_v0.1.md

当前主动切片：
    性能优化仍为第一优先。
    已到证据边界：S6/S7 基础信息、候选方法和因果主果索引恢复条件复核已闭合，本轮不进入索引实现。
    已闭合：S1d-4g 自我_提交任务状态变化 本体写账瘦身已按 计划/20260607_S1d4g任务状态提交被动动作入口落实计划_v0.1.md 落实；旧完整本能函数执行路径、任务域本能输入包、运行存在读回动态路径和旧注册入口已删除；提交 48fd455。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 已输出 `任务状态动作动态=<动态主键>`，未检出 `准备本能调用`、`执行本能方法`、`任务域本能输入包`、`任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`。
    已闭合：S1d-4h 任务状态被动提交内部写账耗时定位与最小瘦身，按 计划/20260607_S1d4h任务状态被动提交内部写账定位与瘦身方案_v0.1.md 落实；重复来源规格确认已改为方法首节点确认缓存快路径。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 已输出 `任务状态动作动态=<动态主键>`，未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
    性能证据：最新线程 T15584 完整任务状态提交 29 次，平均总耗时约 299762us；`准备被动提交` 平均约 3737us，首轮后多为 1-3us；`执行被动提交` 平均约 286657us，仍为主耗时。
    已闭合：S1d-4i 任务状态被动提交内部写账低频阶段定位，按 计划/20260607_S1d4i任务状态被动提交内部写账低频阶段定位方案_v0.1.md 落实；内部阶段采样未改变状态写入和动作动态账。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 出现 `任务状态被动提交/内部阶段耗时`，保留 `任务状态动作动态=<动态主键>`，未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
    性能证据：最新线程 T15884 内部采样 21 条，最慢阶段分布 `动作动态创建=20`、`来源规格确认=1`；`动作动态创建` 平均约 559845us，最大 841400us。
    已闭合：S1d-4j 动作动态创建内部阶段定位与最小瘦身，按 计划/20260607_S1d4j动作动态创建内部阶段定位与瘦身方案_v0.1.md 落实；动作动态创建复用调用侧已知任务虚拟存在，并增加内部阶段采样。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 出现 `任务状态动作动态创建/内部阶段耗时`，保留 `任务状态动作动态=<动态主键>`，未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
    性能证据：最新线程 T13928 动作动态创建内部采样 19 条，最慢阶段分布 `新状态节点=19`；`新状态节点` 平均约 408635us，最大 538594us；`任务虚拟存在` 平均为 0us。
    已闭合：S1d-4k 状态创建后处理内部阶段定位，按 计划/20260607_S1d4k状态创建后处理内部阶段定位方案_v0.1.md 落实；状态创建内部采样未改变状态动态刷新或状态写入后整理语义。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 出现 `状态创建/内部阶段耗时`，保留 `任务状态动作动态=<动态主键>`，未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`。
    性能证据：最新线程 T24008 状态创建内部采样 33 条，最慢阶段分布 `状态动态刷新=33`；`状态动态刷新` 平均约 849007us，最大 2081942us；`状态写入后整理` 平均约 207us。
    已闭合：S1d-4l 动态类索引引用解析指针优先，按 计划/20260607_S1d4l动态类索引引用解析指针优先方案_v0.1.md 落实；状态 / 动态索引引用解析已改为指针有效且主键为空或匹配时优先使用指针，主键查找和全量扫描保留兜底。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 保留 `任务状态动作动态=<动态主键>`，未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
    性能证据：最新线程 T39044 状态创建内部采样 28 条，最慢阶段分布 `状态动态刷新=28`；`状态动态刷新` 平均约 642089us，最大 1366770us；较 S1d-4k 最新线程平均约 849007us 有下降，但热点仍在状态动态刷新。
    已闭合：S1d-4m 动态类相邻前状态查找内部阶段定位，按 计划/20260607_S1d4m相邻前状态查找内部阶段定位方案_v0.1.md 落实；相邻前状态查找慢采样未出现，确认剩余主耗时不在前状态查找。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 保留 `任务状态动作动态=<动态主键>`，未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
    性能证据：最新线程 T29128 未出现 `相邻前状态查找/内部阶段耗时`；状态创建内部采样 19 条，最慢阶段仍为 `状态动态刷新=19`；`状态动态刷新` 平均约 612992us，最大 908455us。
    已闭合：S1d-4n 动态类创建动态内部阶段定位，按 计划/20260607_S1d4n创建动态内部阶段定位方案_v0.1.md 落实；已排除已有状态动态查重和动态索引追加排序为主耗时。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 出现 `创建动态/内部阶段耗时`，保留 `任务状态动作动态=<动态主键>`，未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
    性能证据：最新线程 T11876 创建动态慢样本 44 条，最慢阶段分布 `同类事件段聚合=26`、`动态二次特征=18`；层级 0 慢样本全部为 `同类事件段聚合`，层级 1 慢样本全部为 `动态二次特征`；`已有状态动态查找` 平均约 380us，`追加动态索引` 平均约 563us。
    已闭合：S1d-4o 动态二次特征生成内部阶段定位，按 计划/20260607_S1d4o动态二次特征生成内部阶段定位方案_v0.1.md 落实；已确认慢点主集中在分步结果生成，条件候选收集为次级耗时。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 出现 `动态二次特征生成/内部阶段耗时`，保留 `任务状态动作动态=<动态主键>`，未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
    性能证据：最新线程 T39244 动态二次特征生成慢样本 15 条，最慢阶段分布 `分步结果生成=14`、`条件候选收集=1`；平均总耗时约 581304us；`分步结果生成` 平均约 292628us，`条件候选收集` 平均约 201310us，`因果模板匹配` 平均约 4270us。
    已闭合：S1d-4p 分步结果生成内部阶段定位，按 计划/20260607_S1d4p分步结果生成内部阶段定位方案_v0.1.md 落实；已确认慢点主集中在分步语义绑定。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 出现 `动态二次特征分步结果/内部阶段耗时`，保留 `任务状态动作动态=<动态主键>`，未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
    性能证据：最新线程 T15856 分步结果慢样本 8 条，最慢阶段分布 `分步语义绑定=8`；平均总耗时约 382336us；`分步语义绑定` 平均约 208468us，`分步值写入` 平均约 88690us，`分步签名查找 / 创建` 平均约 67221us。
    已闭合：S1d-4q 分步语义绑定瘦身，按 计划/20260607_S1d4q分步语义绑定瘦身方案_v0.1.md 落实；重复 `绑定场景` / `设置生成语义` 已增加快路径，必要签名刷新保留。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 出现 `动态二次特征分步结果/内部阶段耗时` 和 `动态二次特征生成/内部阶段耗时`，保留 `任务状态动作动态=<动态主键>`，未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
    性能证据：最新运行线程 T21484 分步结果样本 4 条，最慢阶段分布 `分步语义绑定=2`、`分步值写入=2`；平均总耗时约 201273us；`分步语义绑定` 平均约 83632us，`分步值写入` 平均约 66058us，`分步签名查找 / 创建` 平均约 39011us。
    已闭合：S1d-4r 分步结果剩余热点定位 / 瘦身，按 计划/20260607_S1d4r分步结果剩余热点定位与瘦身方案_v0.1.md 落实；概念模板刷新保留默认刻度确保，复用同一次子二次特征枚举完成概念名称反查和差值命中。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 本轮窗口保留 `任务状态动作动态=<动态主键>`，未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
    性能证据：最新运行线程 T31648 分步结果样本 5 条，最慢阶段全部为 `分步语义绑定`；平均总耗时约 184478us；`分步语义绑定` 平均约 88482us，`分步值写入` 平均约 44281us，`分步签名查找创建` 平均约 33486us。同线程动态二次特征生成样本 19 条，平均总耗时约 448683us，`分步结果生成` 平均约 192838us，`条件候选收集` 平均约 163720us。
    已闭合：S1d-4s 分步语义绑定剩余阶段定位，按 计划/20260607_S1d4s分步语义绑定剩余阶段定位与瘦身方案_v0.1.md 落实；语义绑定内部阶段采样已定位到 `场景绑定写入`。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 本轮窗口保留 `任务状态动作动态=<动态主键>`，未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
    性能证据：最新运行线程 T16620 分步结果样本 6 条，最慢阶段全部为 `分步语义绑定`；平均总耗时约 185376us；`分步语义绑定` 平均约 93866us。同线程语义绑定内部样本 6 条，最慢阶段全部为 `场景绑定写入`；`场景绑定写入` 平均约 89238us，最大约 176646us。
    已闭合：S1d-4t 二次特征场景绑定写入内部阶段定位 / 瘦身，按 计划/20260607_S1d4t二次特征场景绑定写入内部阶段定位与瘦身方案_v0.1.md 落实；索引查重已复用一次性取得的节点主键。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 本轮窗口保留 `任务状态动作动态=<动态主键>`，未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
    性能证据：最新运行线程 T29964 未出现 `二次特征绑定场景/内部阶段耗时`，说明 `绑定场景` 单次未超过 5000us 慢采样阈值；分步结果样本 5 条，平均总耗时约 129704us，`分步语义绑定` 平均约 12217us，`分步值写入` 平均约 50029us，`分步签名查找创建` 平均约 50774us。同线程动态二次特征生成样本 19 条，最慢阶段分布为 `条件候选收集=16`、`命中回写因果实例=2`、`分步结果生成=1`。
    已闭合：S1d-4u 条件候选收集内部阶段定位 / 瘦身，按 计划/20260607_S1d4u条件候选收集内部阶段定位与瘦身方案_v0.1.md 落实；结果候选排除逻辑已由嵌套比较改为结果候选指针集 / 主键集查找。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 本轮窗口保留 `任务状态动作动态=<动态主键>`，未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
    性能证据：最新运行线程 T24772 条件候选收集内部样本 11 条，平均总耗时约 5872us，最慢阶段全部为 `候选过滤`；动态二次特征生成样本 10 条，平均总耗时约 219887us，最慢阶段分布为 `分步结果生成=7`、`命中回写因果实例=3`。分步结果样本 7 条，最慢阶段全部为 `分步签名查找创建`，平均约 83114us。
    已闭合：S1d-4v 分步签名查找创建内部阶段定位 / 瘦身，按 计划/20260607_S1d4v分步签名查找创建内部阶段定位与瘦身方案_v0.1.md 落实；模板已有 `度量签名_链键` 时直接用于查找，不再先复制模板。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 本轮窗口保留 `任务状态动作动态=<动态主键>`，未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
    性能证据：最新运行线程 T24124 未出现 `二次特征签名查找创建/内部阶段耗时`；分步结果样本 5 条，平均总耗时约 141344us，最慢阶段分布为 `分步值写入=4`、`分步签名查找创建=1`；`分步签名查找创建` 平均约 43477us，较 S1d-4u 的约 83114us 下降。
    已闭合：S1d-4w 分步值写入聚合调用次数定位 / 瘦身，按 计划/20260607_S1d4w分步值写入聚合调用定位与瘦身方案_v0.1.md 落实；值写入保留字段写入和观测记录，可复用概念模板时跳过重复刷新。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 本轮窗口保留 `任务状态动作动态=<动态主键>`，未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
    性能证据：最新运行线程 T13700 `动态二次特征分步值写入/内部阶段耗时` 7 条；`分步值写入` 平均约 55753us，标量 / 离散值写入各约 54 次；单次底层值写入和概念模板刷新均未超过 5000us 慢采样阈值。
    已闭合：S1d-4x 命中回写因果实例内部阶段定位 / 瘦身，按 计划/20260607_S1d4x命中回写因果实例内部阶段定位与瘦身方案_v0.1.md 落实；已定位主耗时在命中模板证据动态追加。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 本轮窗口保留 `任务状态动作动态=<动态主键>`，未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
    性能证据：最新运行线程 T15496 `动态二次特征命中回写因果实例/内部阶段耗时` 101 条；平均命中模板数约 396；`命中模板证据动态追加` 平均约 36839us，`按动态创建因果信息` 平均约 23224us。
    已闭合：S1d-4y 命中模板证据动态样本批量追加瘦身，按 计划/20260607_S1d4y命中模板证据动态样本批量追加瘦身方案_v0.1.md 落实；调用侧旧逐模板追加循环已删除，因果类保留批量追加入口。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 本轮窗口保留 `任务状态动作动态=<动态主键>`，未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
    性能证据：最新运行线程 T10324 / T34984 `动态二次特征命中回写因果实例/内部阶段耗时` 108 条；`命中模板证据动态追加` 平均约 37357us，最慢阶段仍为 `命中模板证据动态追加=84`；预测偏差说明重复互斥不是主因。
    已闭合：S1d-4z 证据动态样本追加键缓存瘦身，按 计划/20260607_S1d4z证据动态样本追加键缓存瘦身方案_v0.1.md 落实；批量追加入口已改为批内只读取一次证据动态主键。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 本轮窗口保留 `任务状态动作动态=<动态主键>`，未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
    性能证据：最新运行线程 T35712 / T7500 `动态二次特征命中回写因果实例/内部阶段耗时` 35 条；`命中模板证据动态追加` 平均约 16649us，较 S1d-4y 下降；高层 `动态二次特征生成` 最慢阶段全部转回 `分步结果生成`。
    已闭合：S1d-5a 二次特征签名父节点索引瘦身，按 计划/20260607_S1d5a二次特征签名父节点索引瘦身方案_v0.1.md 落实；父节点 + 度量签名缓存已接入，绑定基础信息、新建二次特征和删除二次特征均维护缓存。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 本轮窗口保留 `任务状态动作动态=<动态主键>`，未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
    性能证据：最新运行线程 T27148 `动态二次特征分步结果/内部阶段耗时` 8 条；`分步签名查找创建` 平均约 4121us，较 S1d-4z 的约 69292us 明显下降；`二次特征签名查找创建/内部阶段耗时` 仅 1 条，最慢阶段为 `节点创建`。
    已闭合：S1d-5b 二次特征值写入主信息复用瘦身，按 计划/20260607_S1d5b二次特征值写入主信息复用瘦身方案_v0.1.md 落实；标量 / 离散写入已增加已取主信息快路径，旧公开入口转调同一套私有实现，旧重复主体已删除。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 本轮窗口保留 `任务状态动作动态=<动态主键>`，未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
    性能证据：最新运行线程 T15412 `动态二次特征分步结果/内部阶段耗时` 7 条；`分步值写入` 平均约 70124us，较 S1d-5a 的约 82869us 下降但仍为最慢阶段；`二次特征值写入/内部阶段耗时` 0 条。
    已闭合：S1d-5c 二次特征值写入批次观测时间戳复用瘦身，按 计划/20260607_S1d5c二次特征值写入批次观测时间戳复用瘦身方案_v0.1.md 落实；分步结果生成批次已复用一次观测时间戳，每个节点仍调用 `统计.记录观测`。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 本轮窗口保留 `任务状态动作动态=<动态主键>`，未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
    性能证据：最新运行线程 T17880 `动态二次特征分步结果/内部阶段耗时` 6 条；`分步值写入` 平均约 69346us，较 S1d-5b 的约 70124us 下降不明显；`二次特征值写入/内部阶段耗时` 0 条。
    已闭合：S1d-5d 二次特征值写入批次静默快路径瘦身，按 计划/20260607_S1d5d二次特征值写入批次静默快路径瘦身方案_v0.1.md 落实；分步批量路径跳过内部值写入采样，字段写入、观测记录、必要概念模板刷新和外层分步值写入聚合日志均保留。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 本轮窗口保留 `任务状态动作动态=<动态主键>`，未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
    性能证据：最新运行线程 T32644 `动态二次特征分步结果/内部阶段耗时` 7 条；`分步值写入` 平均约 73683us，较 S1d-5c 的约 69346us 未下降；值写入支线停止，不继续削减合法结果数量。
    已闭合：S1d-5e 标准化动态单元引用解析瘦身，按 计划/20260607_S1d5e标准化动态单元引用解析瘦身方案_v0.1.md 落实；动态来源引用解析已改为指针优先、主键查找兜底，来源列表按数量预留容量。
    验证证据：Debug x64 构建通过；真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；日志/鱼巢_run_20260608.低值g 本轮窗口保留 `任务状态动作动态=<动态主键>`，未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。
    性能证据：最新运行线程 T32644 `标准化动态单元` 平均约 98us，较 S1d-5d 的约 33358us 明显下降；`创建动态/内部阶段耗时` 18 条，最慢阶段转为 `同类事件段聚合=17`、`动态二次特征=1`，`同类事件段聚合` 平均约 163712us。
    当前主动切片：S1d-5f 同类事件段聚合预筛瘦身。
    S1d-5f 目标：同类事件来源流比较使用指针优先、主键兜底；已有同类事件段聚合查重先按 `动态路径签名` 预筛，再做来源链精确比较。
    S1d-5f 边界：不新增同类事件段全局索引，不跳过场景动态扫描或候选动态解析，不改变同主体同特征、同来源流、状态值连续、时间顺序或来源链相同判断语义。
    已补充执行：自我_提交任务状态变化 本能方法内成功诊断日志降级、非完成状态提交跳过重复推进特征同步。
    已补充登记：调用侧继续细分 私有_执行任务域本能方法 外层阶段，不在本能方法内部加耗时日志。
    已完成方案：计划/20260607_S1d3状态提交待确认队列方案_v0.1.md。
    已完成方案：计划/20260607_S1d4a状态提交本能调用外层阶段与规格快路径方案_v0.1.md。
    已完成方案：计划/20260607_S1d4b本能动作管理器外层阶段拆分方案_v0.1.md。
    已完成方案：计划/20260607_S1d4c本能方法自身环境阶段诊断方案_v0.1.md。
    已完成方案：计划/20260607_S1d4d任务状态提交本能方法通用环境快路径方案_v0.1.md。
    已完成方案：计划/20260607_S1d4e任务状态提交调用侧准备阶段拆分方案_v0.1.md。
    已完成方案：计划/20260607_S1d4f任务状态提交调用侧方法首节点缓存方案_v0.1.md。
    已完成方案并已落实：计划/20260607_S1d4g任务状态提交被动动作入口落实计划_v0.1.md。
    已完成方案：计划/20260607_S3a日志分级批量flush最小方案_v0.1.md。
    已完成方案：计划/20260607_S4a需求树生长摘要offline_live拆分方案_v0.1.md。
    已完成方案：计划/20260607_S8a_D455外设性能专线只读定位方案_v0.1.md。
    已完成方案：计划/20260607_S8b_D455高频报告日志限流小方案_v0.1.md。
    已完成方案：计划/20260607_S8c_D455报告背压与无等待项入队限流方案_v0.1.md。
    已完成方案：计划/20260607_S8d_D455等待项索引与目标约束匹配定位方案_v0.1.md。
    已完成方案：计划/20260607_S6S7基础信息候选因果索引恢复条件复核方案_v0.1.md。
    已完成方案：计划/20260607_S1d5a二次特征签名父节点索引瘦身方案_v0.1.md。
    已完成方案：计划/20260607_S1d5b二次特征值写入主信息复用瘦身方案_v0.1.md。
    已完成方案：计划/20260607_S1d5c二次特征值写入批次观测时间戳复用瘦身方案_v0.1.md。
    当前执行方案：计划/20260607_S1d5d二次特征值写入批次静默快路径瘦身方案_v0.1.md。

性能插队原因：
    按性能优先看，S1d、S3a、S4a 和 S8 当前可证明项已完成当前轮闭合。
    S1d 第一层去重和第二层拆锁已执行并通过真实运行验证。
    S6/S7 已只读复核：结构风险成立，但当前真实日志未证明基础信息、候选方法或因果主果扫描成为主瓶颈。

当前主堵点：
    1. S1d-4g 已移除旧完整本能函数执行壳，状态提交锁等待已不再是当前主耗时。
    2. 最新真实日志窗口 T34244 显示 23 次完整任务状态提交：平均总耗时约 299382us，最大约 517577us；`准备被动提交` 平均约 5875us，`状态提交锁等待` 平均约 0.35us，`执行被动提交` 平均约 284286us。
    3. S1d-3 pending 队列已经接入并通过真实运行；确认前不进入可派发队列，确认后同步状态版本再入队。
    4. 当前剩余热点集中在任务状态被动提交内部写账，而不是规格确认、查找本能方法、完整本能动作管理器外壳或锁等待。
    5. 20:10 复测显示 本能动作管理器外层桥不是主耗时，动作函数调用平均约 275322us。
    6. 20:17 复测显示 自我_提交任务状态变化 的通用环境 helper 平均约 135512us，执行函数平均约 314430us；确认规格已近零，剩余热点集中在确保本能方法、方法虚拟存在两次同步和方法本体写账。
    7. 20:26 复测显示通用环境快路径有效，完整任务状态提交平均约 285352us；当前剩余热点是调用侧准备本能调用约 105677us 和具体方法体写账。
    8. 20:31 复测显示准备本能调用阶段主耗时为 查找本能方法，平均约 100035us。
    9. 20:45 复测显示 S1d-4f 已把准备查找本能方法压到缓存命中级别，状态版本错配旧工作项不再进入死信。
    10. S1d-4g 已明确并落实为任务域被动提交动作入口，不作为普通候选本能方法执行路径；旧完整本能函数入口和任务管理旧调用链已删除。
    11. 20:52 复测显示 S3a 日志分级 / 批量 flush 已闭合，普通日志改为 250ms 间隔 flush，错误 / 致命日志保持立即 flush。
    12. 21:03 复测显示 S4a 已闭合，offline 短摘要约 82ms，live 摘要约 9554ms。
    13. S8a 只读定位显示，自我线程仍主动调用外设等待项巡检；第一刀收束为 S8a-1 外设等待项巡检所有者收束。
    14. 21:14 复测显示 S8a-1 已闭合：任务管理外设承接后台线程仍启动，自我线程主动巡检分支日志消失，负向锚点为空。
    15. 21:24 复测显示 S8b 已闭合：D455 逐簇识别报告入队 / 采样已按 1/2/3/10/20/30 周期输出短摘要并带抑制条数，退出前输出日志限流收口。
    16. 21:32 复测显示 S8c 已闭合：无等待项时逐簇识别报告ID 1/2/3 后跳到 4（序号10）、5（序号20），入队背压抑制帧数=6/9。
    17. S8d 只读定位显示当前真实样本中活跃 D455 等待项数量为 1，且首个报告后已被任务管理承接；没有真实证据证明等待项集合、报告倒序扫描或目标观察约束缓冲区扫描成为瓶颈。
    18. 当前进入 S6/S7：基础信息、候选方法和因果主果索引恢复条件复核。
    19. S6/S7 复核显示：候选方法全日志 count=756，最大 18ms，平均约 1.01ms；因果查询 count=843，最大 9ms，平均约 1.16ms；基础信息底层线性扫描风险成立但缺少主瓶颈耗时证据。
    20. S1d-4o 复测显示动态二次特征生成主耗时在 `分步结果生成`，最新线程 T39244 慢样本 15 条中 `分步结果生成=14`，下一步进入 S1d-4p。
    21. S1d-4p 复测显示分步结果生成主耗时在 `分步语义绑定`，最新线程 T15856 慢样本 8 条全部为 `分步语义绑定`，下一步进入 S1d-4q。
    22. S1d-4u 复测显示条件候选收集高层平均由约 225225us 降到约 5054us，动态二次特征生成主慢点回到 `分步结果生成`；分步结果内部当前主慢点为 `分步签名查找创建`。
    23. S1d-4v 复测显示单次签名查找创建未超过 5000us 慢采样阈值，`分步签名查找创建` 聚合平均降到约 43477us；当前分步结果主慢点转到 `分步值写入`。
    24. S1d-4w 复测显示 `分步值写入` 是合法标量 / 离散结果数量聚合，单次值写入无慢样本；动态二次特征生成高层慢点开始接近 `命中回写因果实例`。
    25. S1d-4x 复测显示 `命中回写因果实例` 主耗时为命中模板证据动态追加，平均命中模板约 396 个，下一步做批量追加证据动态样本。
    26. S1d-4y 复测显示批量互斥收束后 `命中模板证据动态追加` 平均约 37357us，仍是主慢点；下一步做同一动态主键批内缓存。
    27. S1d-4z 复测显示 `命中模板证据动态追加` 平均降到约 16649us，高层主慢点回到 `分步结果生成`；下一步做二次特征签名父节点局部索引。
    28. S1d-5a 复测显示 `分步签名查找创建` 平均降到约 4121us，当前主慢点转为 `分步值写入`，标量 / 离散值写入平均分别约 41488us / 41381us。
    29. S1d-5b 复测显示 `分步值写入` 平均降到约 70124us，但仍是最慢阶段；单次值写入无慢采样，下一步复用批次观测时间戳。
    30. S1d-5c 复测显示批次观测时间戳复用收益不明显，`分步值写入` 平均约 69346us；下一步只让分步批量路径跳过内部值写入采样。
    31. S1d-5d 复测显示内部值写入采样静默后 `分步值写入` 平均约 73683us，较 S1d-5c 未下降；停止分步值写入支线，下一步转向同一高层窗口中平均约 33358us 的 `标准化动态单元`。
    32. S1d-5e 复测显示 `标准化动态单元` 平均降到约 98us；创建动态主慢点转为 `同类事件段聚合`，平均约 163712us。

硬边界：
    任务状态变化必须通过任务域被动提交动作入口生成动作动态，动作来源方法固定为 提交任务状态变化。
    不能把状态提交裸异步化，不能由任务管理层裸写状态值，不能让 worker 先执行后补账。
    状态值、动作动态、任务壳快照、worker 派发顺序必须一致。

S1d 解决顺序：
    1. 已完成：同任务在途重排、同任务已有执行前许可占用重排跳过重复入队前状态提交。
    2. 已完成：新增任务互斥慢锁日志，区分任务状态提交锁、任务壳应用锁、任务工作项派发锁。
    3. 已完成：拆分 worker 派发、任务壳应用、任务状态提交用途锁；worker 派发不再占用状态提交锁。
    4. 已完成：自我_提交任务状态变化 内部成功诊断日志默认关闭，非完成提交不再重复同步任务推进特征。
    5. 已登记：调用侧外层阶段细分作为后续待优化项，不在本能方法内部新增耗时日志。
    6. 已完成：S1d-3 状态提交待确认队列方案。
    7. 已完成：S1d-3a 待确认队列结构与快照计数字段预备，不改变现有同步入队路径。
    8. 已完成：S1d-3b / S1d-3c pending 队列接入与真实运行验证。
    9. 已完成：S1d-4a 状态提交规格确认进程内快路径，验证安全但收益不足。
    10. 已完成：S1d-4b 本能动作管理器外层执行阶段拆分，验证慢点在动作函数调用。
    11. 已完成：S1d-4c 统一本能方法自身环境 helper 阶段诊断。
    12. 已完成：S1d-4d 任务状态提交本能方法通用环境快路径小方案。
    13. 已完成：S1d-4e 任务状态提交调用侧准备本能调用阶段拆分。
    14. 已完成：S1d-4f 任务状态提交调用侧方法首节点缓存，旧版本工作项状态版本错配统一终止过期工作项。
    15. 已完成：S1d-4g 自我_提交任务状态变化 本体写账瘦身，按任务域被动提交动作入口落实，并删除旧完整本能函数执行代码。
    16. 已完成：S1d-4h 任务状态被动提交内部写账耗时定位与最小瘦身，重复来源规格确认改为缓存快路径。
    17. 已完成：S1d-4i 任务状态被动提交内部写账低频阶段定位，定位到 `动作动态创建` 为主耗时。
    18. 已完成：S1d-4j 动作动态创建内部阶段定位与最小瘦身，定位到 `新状态节点` 为主耗时。
    19. 已完成：S1d-4k 状态创建后处理内部阶段定位，定位到 `状态动态刷新` 为主耗时。
    20. 已完成：S1d-4l 动态类索引引用解析指针优先，状态动态刷新均值下降但仍为热点。
    21. 已完成：S1d-4m 动态类相邻前状态查找内部阶段定位，确认查找相邻前状态不是剩余主耗时。
    22. 已完成：S1d-4n 动态类创建动态内部阶段定位，定位到动态二次特征生成是下一主线。
    23. 已完成：S1d-4o 动态二次特征生成内部阶段定位，定位到分步结果生成为当前主耗时。
    24. 已完成：S1d-4p 分步结果生成内部阶段定位，定位到分步语义绑定为当前主耗时。
    25. 已完成：S1d-4q 分步语义绑定瘦身，重复语义绑定快路径已通过真实日志验证。
    26. 已完成：S1d-4r 分步结果剩余热点定位 / 瘦身，值写入与概念模板刷新重复枚举已瘦身。
    27. 已完成：S1d-4s 分步语义绑定剩余阶段定位，定位到场景绑定写入。
    28. 已完成：S1d-4t 二次特征场景绑定写入内部阶段定位 / 瘦身。
    29. 已完成：S1d-4u 条件候选收集内部阶段定位 / 瘦身。
    30. 已完成：S1d-4v 分步签名查找创建内部阶段定位 / 瘦身。
    31. 已完成：S1d-4w 分步值写入聚合调用次数定位 / 瘦身。
    32. 已完成：S1d-4x 命中回写因果实例内部阶段定位 / 瘦身。
    33. 已完成：S1d-4y 命中模板证据动态样本批量追加瘦身。
    34. 已完成：S1d-4z 证据动态样本追加键缓存瘦身。
    35. 已完成：S1d-5a 二次特征签名父节点索引瘦身。
    36. 已完成：S1d-5b 二次特征值写入主信息复用瘦身。
    37. 已完成：S1d-5c 二次特征值写入批次观测时间戳复用瘦身。
    38. 已完成：S1d-5d 二次特征值写入批次静默快路径瘦身，验证显示收益不成立，值写入支线停止。
    39. 已完成：S1d-5e 标准化动态单元引用解析瘦身。
    40. 当前主动：S1d-5f 同类事件段聚合预筛瘦身。
    41. 已完成：S3a 日志分级 / 批量 flush 最小切片。
    42. 已完成：S4a 需求树生长摘要 offline/live 拆分。
    43. 已完成：S8a-1 外设等待项巡检所有者收束。
    44. 已完成：S8b D455 高频报告 / 日志限流小方案。
    45. 已完成：S8c D455 报告背压 / 无等待项场景报告入队限流小方案。
    46. 已完成：S8d D455 等待项索引 / 按目标约束匹配优化只读定位，本轮不进入等待项索引实现。
    47. 已完成：S6/S7 基础信息、候选方法和因果主果索引恢复条件复核，本轮不进入索引实现。

当前性能切片允许修改范围：
    二次特征应用模块.ixx；
    计划/20260607_S1d5f同类事件段聚合预筛瘦身方案_v0.1.md；
    计划/20260607_运行速度常驻路径优先优化计划_v0.1.md；
    计划/计划索引.md；
    实施记录/20260608_S1d5f同类事件段聚合预筛_Codex断点清单.md。
    S1d-5f 只做同类事件段聚合内部引用比较和路径签名预筛，不新增全局索引，不改变动态聚合语义。

本性能切片禁止混入：
    日志模块 flush 策略；
    D455 / 外设链路继续改造；
    任务完成、需求满足、价值结算或外设事实规则改动。
    未经复核方案确认，不直接实现基础信息索引、候选方法索引或因果主果索引。

后续优先级：
    当前停点：性能计划剩余索引线未满足恢复条件，不能自动进入大范围索引代码改造；S1d-5e 已闭合，创建动态主慢点转为同类事件段聚合。
    下一步主动切片：S1d-5f 同类事件段聚合预筛瘦身，入口已生成：计划/20260607_S1d5f同类事件段聚合预筛瘦身方案_v0.1.md。
    待条件恢复：S6 / S7 基础信息索引、候选方法索引、因果主果索引；恢复条件见 S6/S7 复核方案。
    待条件恢复：S8 D455 等待项索引 / 目标约束索引；恢复条件见 S8d 方案。

已闭合且不再占用主动队列：
    计划/20260606_自我线程与任务管理状态推进边界纠偏计划_v0.1.md
    已完成 P0-P5：任务管理 worker 池随自我线程启动；自我线程不再同步消费任务管理队列；执行前许可改为一次性工作项许可；外设四方法已进入免审批白名单。

暂停固定的外设线：
    计划/20260606_外设四本能方法任务真实运行条件包闭环计划_v0.1.md

暂停前切片：
    P9d：识别结果到扫描 / 跟踪目标上下文桥。

暂停固定点：
    P9a-P9c 已闭合；
    P9d 已推进到 6150 稳定子集桥、稳定子集全量扫描等待项承接、6146 任务主体与输入条件桥；
    6146 已自然执行，归属状态已读=1，扫描可启动状态已读=1，扫描闸门成立=1。

暂停前真实堵点：
    当前扫描材料等级=可用；
    可生成扫描变化候选=0；
    可生成跟踪动态候选=0；
    可进入提交入口组织=0；
    6147 自我_跟踪指定存在 仍未自然出现；
    当前日志没有 当前本能ID=6147 / 指定存在跟踪正式结果。

恢复下一步：
    S1d-3 方案完成或用户明确恢复外设线后，
    恢复 P9d 时从“6147 跟踪目标上下文 / 任务主体桥”切片继续：
        先定位 6147 如何取得目标存在、已归属状态、观察窗口、跟踪基准和预测区间；
        再检查任务管理是否能从正式目标存在生成 目标存在跟踪状态 任务主体与目标跟踪等待项；
        仍不得把扫描报告、队列项、目标观察约束或材料页当事实主体；
        质量门继续分级消费：
            可用但非优质材料只能形成材料承接 / 待复验缺口；
            只有达到可生成跟踪动态候选时，才允许进入稳定跟踪动态候选或提交入口组织。
    不能把 当前观察范围可观测单位存在对应闭合状态 强行写成 1。

恢复依据：
    实施记录/20260606_P9a外设四方法条件包闭环对账记录.md
    实施记录/20260606_P9b观察验证最小闭环记录.md
    实施记录/20260606_P9c识别前置承接记录.md
    实施记录/20260606_P9d识别结果到扫描跟踪目标上下文桥记录.md
```

## 2. 顶层保留计划

```text
1. 计划/20260531_当前交互者明确状态与学习缺口简化纠偏计划_v0.1.md
   保留原因：
       仍有服务链价值。
   恢复条件：
       切到服务链；
       或出现服务触发事实，例如当前会话输入、人类表达、外部请求、已确认服务目标、服务动作结果或反馈事实。
   当前限制：
       无服务触发事实时不得主动生成当前交互者、权限者、服务对象或服务目标等服务子需求。

2. 计划/20260531_运行性能优化实施计划_v0.1.md
   保留原因：
       P0-P4 已完成，P5 / P6 仍可在真实任务推进热点出现后提供候选能力索引和因果主果目标索引优化路线；
       当前只作为 计划/20260607_运行速度常驻路径优先优化计划_v0.1.md 的 S6 / S7 背景，不竞争 S1d 当前主动切片。
   恢复条件：
       真实任务推进日志证明候选索引或因果查询成为瓶颈。
   当前限制：
       性能优化不得改任务完成、需求满足、价值结算或外设真实验收规则。
```

## 3. 本次归档

以下计划已完成、被后续计划吸收，或剩余恢复条件已单列到 `计划/待完成项`，已从 `计划/` 移入 `计划/完成项/`。

```text
外设三分法 / D455 样本线：
    计划/完成项/20260602_外设三分法技术储备与实施路线_v0.1.md
    计划/完成项/20260604_外设三分法真实D455样本实施切片计划_v0.1.md
    计划/完成项/20260605_外设四本能方法需求树生成因果链修订计划_v0.1.md

学习量化 / 方法状态派生线：
    计划/完成项/20260605_学习函数量化缺口与方法状态派生纠偏计划_v0.1.md
    计划/完成项/20260605_P1a任务筹办缺口映射量化目标小切片方案_v0.1.md
    计划/完成项/20260605_P1b任务管理工作线程派生消息无损搬运小切片方案_v0.1.md
    计划/完成项/20260605_P1c自我线程学习任务去重键扩展小切片方案_v0.1.md
    计划/完成项/20260605_P2P3学习本能量化结果能力与缺口输出纠偏小切片方案_v0.1.md
    计划/完成项/20260605_P4方法状态被动派生链主动缺口清理小切片方案_v0.1.md
    计划/完成项/20260605_P4b1同条件统计宿主与新结果分支最小落账方案_v0.1.md
    计划/完成项/20260605_P4b方法状态被动因果模板与同条件统计宿主堵点方案_v0.1.md
    计划/完成项/20260605_P4b2被动因果内部治理应用口径纠偏方案_v0.1.md
    计划/完成项/20260605_P4b2b学习本能方法虚拟存在服务类收口方案_v0.1.md
    计划/完成项/20260605_P4b2c任务筹办方法可执行入口直调撤出方案_v0.1.md
    计划/完成项/20260605_P4b2d安全需求树消费边界复核方案_v0.1.md
    计划/完成项/20260605_P4b2e被动因果模板接入决策方案_v0.1.md
    计划/完成项/20260605_P5学习量化真实日志验证剧本_v0.1.md
    计划/完成项/20260605_P5a学习任务未执行追因计划_v0.1.md

被动动作因果桥线：
    计划/完成项/20260603_被动动作因果桥与结算动态统一结构修订计划_v0.1.md
```

归档判断：

```text
1. 外设三分法路线已被 P9 四本能方法任务真实运行条件包闭环计划吸收。
2. 真实 D455 样本计划 P1 / P2 / P3 已有实施记录；有可比历史的扫描变化样本已单列待完成项。
3. 学习量化 P0-P5c 已有实施记录；定向学习全局动作入口和自然样本复验已单列待完成项。
4. 被动动作因果桥的可继续问题已转入主动能力与被动触发初始因果自然复验待条件项。
```

## 4. 待条件具备后继续

```text
1. D455 可比历史扫描变化样本
   入口：
       计划/待完成项/20260605_D455_P3可比历史扫描变化样本待完成项.md
   恢复条件：
       真实 D455 运行自然形成同一区域 / 同一稳定对应存在的可比历史。
   边界：
       首帧基准只能形成发现存在或发现特征候选，不能伪造成变化动态。

2. 学习量化自然样本复验
   入口：
       计划/待完成项/20260605_P5学习量化真实日志验证自然样本待完成项.md
   恢复条件：
       真实运行自然出现学习量化链样本。
   边界：
       不用模拟学习样本补验收。

3. 定向学习全局动作入口形成
   入口：
       计划/待完成项/20260605_定向学习全局动作入口形成待完成项.md
   恢复条件：
       当前主线允许恢复定向学习；
       或四外设方法任务链闭合后仍需要通过定向学习提炼动作。
   边界：
       3a / 3b 内部因果动作样本问题暂不作为当前 P9 优先阻塞。

4. 主动能力与被动触发初始因果自然复验
   入口：
       计划/待完成项/20260604_主动能力与被动触发遗传因果落实计划_v0.1.md
   恢复条件：
       用户明确恢复自然日志验收；
       或真实运行自然出现 子需求满足 -> 父任务等待解除；
       或真实运行自然出现 叶子任务完成 -> 任务状态=已结算；
       或真实运行自然出现 风险安全层组成因来源因果命中 / 缺口暴露日志。
   边界：
       不得用模拟任务完成、伪造动作动态或直接调用任务筹办函数补验收样本。

5. 自我场景绝对坐标 / 姿态 / 自我视图坐标系
   恢复条件：
       需要自我场景绝对姿态真实来源；
       需要把 D455 / 场景坐标稳定转换为自我边界参照并自然通过生产方法。
   当前限制：
       第一版只保留平移换算；D455 gyro / accel / motion stream 未接入；
       不得把像素簇边界、ROI、权限边界或场景绝对坐标替代为自我边界。

6. 双目相机外设实现线
   入口：
       计划/待完成项/20260527_双目相机观察队列承接代码纠偏与实现计划_v0.1.md
       计划/待完成项/20260527_双目相机观察像素簇接入实现方案_v0.1.md
       计划/待完成项/20260527_外设自主观察线程稳定采样实施计划_v0.1.md
   恢复条件：
       主动队列切换到真实相机外设功能实现；
       或真实 D455 条件具备并需要推进三种观察运行模式订阅。
   边界：
       相机是稳定观察特征供应器，不是世界存在识别器。
```

## 5. 证据闸门

```text
1. 每个切片必须先写预测，再走真实运行路径，最后用日志、状态快照、动作动态和错误日志核对。
2. 缺真实硬件、真实目标样本或真实日志时，只能记录等待条件，不得补模拟样本、夹具入口或伪造动作动态。
3. D455 外设只提供观察帧、报告、d455://句柄、ROI / 掩码 / 点集 / 深度切片等材料。
4. 外设报告、簇候选、模型输出、几何聚类、地图更新或轨迹关联不得直接证明需求满足、当前场景存在集合明确、风险明确或安全值增加。
5. 当前场景特征值明确只表示当前观察范围有效可观测单位全部稳定对应存在；至少一个稳定对应存在只说明扫描可启动，不等于全场景明确。
6. 扫描 / 跟踪不是当前场景特征值明确的完成子需求；第一次扫描只生成发现存在或发现特征事实，不得伪造成动态或变化动态。
7. 因果条件缺口必须带目标宿主、特征类型、当前值、比较符、目标值和来源因果。
8. 未知不能作为已安全证据；没查过、材料缺失、冲突、不可回查或低质量都不得默认满足。
9. 安全候选和安全评估只能生成候选视图；不得写自我风险安全值、风险安全层总值、自我安全值，不得关闭安全根需求。
10. remote compact / stream disconnected / websocket / network error 只表示远程执行通道中断，不改变项目实现状态、验收状态或业务结论。
11. 只能把有证据的范围标为通过，不扩大解释为内部循环闭合、自我苏醒完成或初步成熟完成。
```

## 6. 验证模板

```text
计划：
真实触发路径：
预测：
命令：
退出码：
关键日志：
状态快照：
动作动态：
错误日志：
实际与预测偏差：
结论：
下一步：
```
