# 计划索引

更新时间：2026-06-24

## 0. 索引用途

本文件只作为当前执行路由，不承载完整历史流水账。历史细节从对应计划、实施记录、真实日志、`计划/完成项/` 和 `计划/待完成项/` 查询。

维护规则：

```text
1. 规范优先于计划；计划优先于临时推断。
2. 当前主动队列只保留一个可闭合切片。
3. 已运行内容只保留检查点、证据范围和恢复条件。
4. 待条件项不得混入当前主动队列。
5. 已完成计划必须移入 计划/完成项/，不得继续留在根目录顶层保留计划；索引只保留短查询入口和证据指针。已实现但待自然样本、待真实外设业务样本或待用户切入的计划移入 计划/待完成项/，不得误归为完成项。
6. 旧版本被新版本替代时，只保留仍有恢复价值的版本；与当前规范冲突或已被替代且无引用价值的版本删除。
7. 每次行为验证必须先写预测，再走真实运行路径，最后用日志、状态快照、动作动态和错误日志核对。
8. 严禁新增或依赖模拟测试、夹具测试、伪造样本、伪造入口、伪造动作动态或 `--*-check` 回归命令。
9. 任务筹办 / 任务执行是任务管理生命周期普通函数，不恢复为本能方法。
10. 不提前宣称自我苏醒完成或初步成熟完成。
11. Codex 恢复执行默认只读第 0 章和第 1 章当前主动队列；读到 `### 1.1` 或 `## 2` 前停止。后续章节、历史摘录和待条件项按当前切片引用再读取。
12. 主动缩减上下文：第 1 章只保留当前切片、当前堵点、下一步和证据指针；长输出、长日志和完整历史落盘到实施记录或日志文件，汇报只给关键行。
13. 执行中发现缺口时先分类：是否阻塞当前切片、是否属于当前类、是否已有对应类逻辑纠偏计划。
14. 不阻塞当前切片的缺口，只登记到对应计划的待执行缺口，不打断当前主动计划；索引第 1 章只保留短入口。
15. 阻塞当前切片且属于当前类的缺口，优先补入当前计划当前切片或下一子切片，并同步更新当前目标 / 下一步。
16. 阻塞当前切片且属于其它类的缺口，按“缺哪个类补哪个类第一优先”规则更新或新建对应类逻辑纠偏计划；确需转向时才修改当前主动计划和返回点。
17. 新增或扩容计划后必须写清返回点；被临时提升的计划只执行第一优先闭合切片，闭合后自动返回原计划。
18. 若用户只要求登记计划内容，默认不改变当前主动计划；只有用户明确要求执行，或缺口已阻塞当前切片，才调整当前下一步执行计划。
19. 执行中确认无用、过时、已被替代且无恢复价值的代码，应在当前闭合切片或独立清理切片中及时删除；不得长期保留兼容壳、死代码、临时兜底或无人调用的旧入口。
20. 无调用函数清理优先：live 扫描确认没有直接调用，也没有构造 / 析构隐式调用、virtual / override、入口 / 回调 / 导出、注册表、消息分发、模板实例、宏拼接或外部公开合同引用，且不承担项目结构写入 / 读取证据的函数，直接删除；同文件 / 同族可批量处理，删除后刷新函数说明文档、构建、复扫，若仍有符合条件的无调用函数则继续删除。
```

默认恢复读取范围：

```text
1. 第 0 章：索引用途和维护规则。
2. 第 1 章：当前主动队列、当前阻塞、下一步、预测验收、禁止宣称。
3. 最近断点清单：只读当前主动队列指向的断点文件的 `## 0. 恢复摘要`；只有该摘要明确要求时再读取后续章节。
4. 归档历史、顶层保留计划、待条件项：默认不读；只有当前问题明确指向时才按标题小段读取。
```

## 1. 当前主动队列

```text
第一优先计划 / 当前主动计划：
    P10 横向审计清理线 - string / 摘要 / 原因类字段后续专项

临时暂停返回点：
    P11 live 无调用函数优先复扫已闭合；后续若新增函数清理线索，再按 P11 规则重开独立切片。

来源插入计划：
    需求类逻辑纠偏计划当前可执行机器逻辑切片已收束；不宣称需求类逻辑纠偏完成。
    任务类逻辑纠偏计划当前可执行切片已收束；不宣称任务类逻辑纠偏完成。
    方法类逻辑纠偏计划 v0.1 已闭合，转入完成项；不宣称项目结构闭环全部通过。
    控制面板 / SQL / 摘要显示读源不作为机器判断，动态控制面板 SQL 化按控制面板后续切片处理。
    二次特征类 P8 当前可执行机器逻辑审计切片已收束；剩余显示 / SQL / 语言层直读登记为后置收口，不作为当前机器逻辑堵点。
    存在类当前登记切片 1.1-1.7 已有闭合记录；场景类 P5.4 已有闭合记录；因果类 P1-P6 已有闭合记录，外部绕过清理转入横向审计线。
    P10f 低引用局部返回壳 / 局部结果壳专项当前可执行批量瘦身已清到边界；按用户最新口径，剩余候选只继续分析含非主键类 string 字段的结构体，不含 string 的结构体以及只有主键 / ID / 键 / 句柄类 string 的对象排除，不再为降低结构体数量单独考虑。
    `详细设计/因果类用途接口草案20260516.ixx` 已确认不是项目代码，不进入当前代码结构体执行队列。
    P11 旧“普通低风险无调用 helper 删除切片暂止”已被用户 2026-06-22 最新口径覆盖：无调用函数优先清理，满足 live 无调用且未命中入口 / 回调 / 导出 / virtual / override / 注册 / 构造析构隐式调用 / 公开合同 / 项目结构证据的函数直接删除，删完复扫，仍有则继续。
    启动卡住临时追根因已关闭：直接环境原因为 SQL Server 未启动；不改代码、不阻塞当前队列。

原计划返回点：
    已从 P11i 返回 P10f-c 后续 string / 摘要 / 原因类字段专项。

当前执行切片：
    P10f 当前轮收束：P10f-av / P10f-aw 已闭合；最新复扫剩余字段暂未发现新的无堵点机器逻辑清理候选。
    P10f-aw `语言类_二次特征表达.cpp::结构_二次特征方向模板` 文本方向模板匹配清理已闭合。
    P10f-av `自我动作实现.外设模块.ixx::结构_双目相机调用结果::消息` 复核已闭合。
    P10f-au `自我动作实现.外设模块.ixx::结构_稳定子集扫描目标上下文桥结果::首个失败说明` 复核已闭合。
    P10f-at `任务模块.工作线程消息协议.ixx::结构_任务管理派生需求消息` 触发动作字段复核已闭合。
    P10f-as `自我线程模块.消息协议.ixx` 线程状态变化消息文本字段复核已闭合。
    P10f-ar `自我类.ixx` 初始化记录 / 线程存在初始化参数 string 字段复核已闭合。
    P10f-aq `自我线程模块.ixx` 快照 / 摘要类 string 字段批量复核已闭合。
    P10f-ap `自我线程模块.impl.cpp::结构_根特征必要条件定义_线程侧` 文本字段清理已闭合。
    P10f-ao `自我线程模块.impl.cpp::结构_根闭环信息位_线程侧` 文本字段机器逻辑复核已闭合。
    P10f-an `语素类.cpp::结构_语素SQL行` 文本字段边界复核已闭合。
    P10f-am `需求类.cpp::结构_需求树SQL行` 文本字段边界复核已闭合。
    P10f-al `双目相机本能适配器.h::调用结果::消息` 与 D455 样本消息字段边界复核已闭合。
    P10f-ak `数据库ADO模块.ixx::结构_ADO查询结果` 列名 / 行集 string 容器边界复核已闭合。
    P10f-aj `世界树类.cpp::结构_世界树SQL节点行 / 结构_世界树SQL关系行` 文本字段边界复核已闭合。
    P10f-ai `日志模块.ixx` 日志基础设施 string 字段边界复核已闭合。
    P10f-ah `任务模块.运行包.ixx::结构_一步治理任务包::完成条件` 边界复核已闭合。
    P10f-ag `任务模块.管理线程协议.ixx::结构_任务界面线程快照` 最近状态 / 调度 / worker 文本字段边界复核已闭合。
    P10f-af `任务模块.管理界面线程.impl.cpp::结构_任务界面线程状态` string-key map / set 边界复核已闭合。
    P10f-ae `任务模块.管理界面线程.impl.cpp::结构_后台worker当前工作快照` 文本字段边界复核已闭合。
    P10f-ad `任务类.cpp::结构_任务树SQL行` 文本字段边界复核已闭合。
    P10f-ac `任务模块.管理工作线程.ixx::结构_工作线程实例快照` 最近状态 / 去向 / 缺口类 string 字段边界复核已闭合。
    P10f-ab `外设观察报告队列.ixx` 外设提交包头标定版本与 D455 材料资源类型边界复核已闭合。
    P10f-aa `外设观察报告队列.ixx` 摘要类 string 字段边界复核已闭合。
    P10f-z `外设观察报告队列.ixx` 版本 / 坐标系类 string 字段边界复核已闭合。
    P10f-y `外设观察报告队列.ixx` 外设材料句柄类 string 字段边界复核已闭合。
    P10f-x `外设观察报告队列.ixx::结构_外设特征值条目::单位` 边界复核已闭合。
    P10f-w `外设观察报告队列.ixx::结构_外设特征值条目::文本值 / 来源材料 / 来源动态` 边界复核已闭合。
    P10f-v `外设观察报告队列.ixx::结构_外设观察像素簇摘要::失败原因` 及外设报告失败原因字段边界复核已闭合，无代码改动。
    P10f-u `外设观察报告队列.ixx::结构_外设观察像素簇摘要` 引用字段边界复核已闭合。
    P10f-t `外设观察报告队列.ixx::结构_外设观察等待项::目标外设` 边界复核已闭合。
    P10f-s `外设观察报告队列.ixx::结构_外设观察等待项::目标区域或目标簇` 与 `外设线程_D455深度相机.ixx::结构_D455跟踪稳定缓存项::跟踪种子` 边界复核已闭合。
    P10f-r `外设观察报告队列.ixx::结构_外设观察报告队列项::跟踪种子` 边界复核已闭合。
    P10f-q `外设观察报告队列.ixx::结构_外设观察报告队列项::来源外设` 边界复核已闭合。
    P10f-p `任务模块.工作线程消息协议.ixx::结构_任务管理观察事实承接请求::来源外设` 边界复核已闭合。
    P10f-o `任务模块.工作线程消息协议.ixx::结构_任务管理线程补充特征::文本值` 边界复核已闭合。
    P10f-n `任务模块.筹办.ixx::结构_临时因果视图::父需求继续策略` 边界复核已闭合。
    P10f-m `基础数据类型.h::结构体_分词` 的 `词 / 词性` 边界复核已闭合。
    P10f-l `方法类.cpp::结构_方法树SQL行` 文本字段边界复核已闭合，无代码改动。
    P10f-k `度量模板注册表类.ixx::结构_刻度片::名称` 与 `度量模板注册表桥接.h::结构_度量刻度片_桥接::名称` 边界复核已闭合。
    P10f-j `本能动作管理类.ixx::结构体_本能动作登记信息::函数名称` 边界复核已闭合。
    P10f-i `自我线程模块.impl.cpp::结构_治理二次特征候选` 概念名称字段边界复核已闭合。
    P10f-h `二次特征应用模块.ixx::结构_场景二次特征写入请求` 概念名称 / 词性字段边界复核已闭合。
    P10f-g `鱼巢.cpp::结构_状态变化跟踪项` 文本字段边界复核已闭合，无代码改动。
    P10f-f 任务管理界面线程 string 键 / 等待状态相关字段边界复核已闭合。
    P10f-e 外设观察报告队列与线程 / 控制面板显示文本边界复核已闭合。
    P10f-d 结构体 string 字段 live 复扫与下一清理候选选择已闭合。
    P10f-c 因果投影缺失证据结构化接口专项已闭合；不再作为当前堵点。
    第一候选入口：暂无新的无堵点机器逻辑清理候选；后续若复扫发现 string 进入候选选择、状态推进、等待状态、稳定键、结算或因果匹配，再按 P10f 规则重开。
当前目标：
    1. 保持 P10f 最新复扫清单为入口，优先复核非主键类 string 字段是否进入机器判断。
    2. 控制面板、SQL 投影、语言表达和日志诊断文本未进入机器判断时不按字段名硬删。
    3. 不把任务筹办 / 任务执行恢复成本能方法；不把文本原因、文本状态或显示类型恢复为机器判断依据。
当前停止点：
    P10f-aw 已闭合：
        已删除 `语言类_二次特征表达.cpp::结构_二次特征方向模板`、`私有_匹配二次特征方向模板(...)`、`私有_二次特征轴键(...)`、`私有_二次特征方向模板_按主信息(...)` 和无调用的空间关系文本拼装 helper；
        二次特征自然语言方向结果改为读取 `方向区间 / 差值 / 标量值 / 值形态 / 是否满足 / 域` 等结构字段；
        `语素集.安全获取词(...)` 在本路径只保留为节点名称、方法名、概念名等人类可读输出，不再用于方向模板匹配；
        已生成 `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_language_direction_template.tsv`，剩余记录 563 行，其中主键 / ID / 键 / 句柄类 331 行、非主键类 232 行；
        `说明书/函数功能说明文档.md` 已刷新，函数条目 7183；本次触碰文档的 CRLF 提示已按遇到时处理为 LF；
        `Debug|x64` 构建通过，0 警告 0 错误。
    P10f-av 已闭合：
        已复核 `结构_双目相机调用结果::消息` 的读取命中；
        `消息.empty()` 只用于日志输出保护，不进入方法结果判断、运行闭环、提交准入、等待状态、稳定键或因果匹配；
        机器判断读取 `成功 / 失败原因 / I64 状态字段 / 观察事实失败条件数量 / 质量失败条件数量 / 新鲜度失败原因` 等结构化字段；
        本字段登记为日志 / 诊断显示文本，不强行删除；本切片无代码改动。
    P10f-au 已闭合：
        已删除 `结构_稳定子集扫描目标上下文桥结果::首个失败说明`；
        新增局部枚举 `枚举_稳定子集桥失败原因`，桥结果保存 `首个失败原因 / 首个失败可用性状态`；
        日志 `稳定子集桥失败说明=` 由枚举和状态即时渲染，不再由结构体 string 字段承载；
        机器判断仍读取 `目标上下文状态 / 方案项数量 / 扫描约束写入数量 / 扫描约束失败数量 / 首个扫描目标观察约束ID` 等结构化字段；
        已生成 `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_stable_subset_bridge_reason.tsv`，剩余记录 567 行，其中主键 / ID / 键 / 句柄类 332 行、非主键类 235 行；
        `git diff --check` 通过；`Debug|x64` 构建通过，0 警告 0 错误。
    P10f-at 已闭合：
        已删除 `结构_任务管理派生需求消息::触发动作主体主键 / 触发动作语义键`；
        `任务模块.工作线程消息协议.ixx`、`任务模块.管理工作线程.ixx`、`任务模块.筹办.ixx` 中未发现这两个字段参与完整性判断、稳定键、派生需求构造或日志文本；
        `从任务管理派生需求消息构造治理消息(...)` 不再从 `触发动作语义键` 覆盖 `治理消息.语义.动作语义键`，治理动作类型由 `枚举_动作类型::派生需求上报` 承载；
        已生成 `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_task_derived_action_fields.tsv`，剩余记录 568 行，其中主键 / ID / 键 / 句柄类 332 行、非主键类 236 行；
        `git diff --check` 通过；`Debug|x64` 构建通过，0 警告 0 错误。
    P10f-as 已闭合：
        已将 `结构_线程状态变化消息段::决策者标签 / 线程标签 / 线程种类` 改为 `const 语素入口节点类* 决策者名称 / 线程名称 / 线程类型`；
        已将 `结构_线程状态变化消息段::触发动作` 改为 `枚举_动作类型 触发动作类型`，不再把消息段文本写入 `动作语义键`；
        `结构_线程状态切换上报参数` 同步删除 `决策者标签 / 触发动作` 文本字段；
        线程初始化推断和线程存在匹配直接读取语素入口，不再用消息段文本注册语素后参与机器查找；
        治理消息幂等键改读 `动作类型` 枚举值，不再拼接 `动作语义键` 文本；
        `结构_治理消息::摘要` 本切片未发现进入路由、去重、状态推进或落账，登记为日志 / 显示边界；
        已生成 `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_thread_state_message_fields.tsv`，剩余记录 570 行，其中主键 / ID / 键 / 句柄类 334 行、非主键类 236 行；
        `git diff --check` 通过；`Debug|x64` 构建通过，0 警告 0 错误。
    P10f-ar 已闭合：
        已删除 `结构_自我初始化结果::缺失项`；初始化完成、冷启动、治理入口、任务治理等判断均由 bool 字段计算；
        `结构_自我初始化步骤记录::名称` 仅用于步骤记录和异常日志，初始化判断读取 `阶段 / 成功 / 发生异常 / 初始化完成` 等结构化字段；
        已将 `结构_线程存在初始化参数::线程标签 / 线程类型 / 所属线程池标签 / 所属线程池类型` 改为 `const 语素入口节点类*` 承载；
        外部线程状态消息文本只作为语素入口名称种子，推断阶段立即转换为存在实例入口 / 存在概念入口；线程存在创建和匹配读取语素入口、存在类型枚举、工作线程 / 自我线程布尔值和主键；
        自我线程上报消息中的人类可读线程标签 / 种类由语素入口渲染，仅用于消息显示 / 控制面板展示；
        已生成 `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_self_init_thread_params.tsv`，剩余记录 576 行，其中主键 / ID / 键 / 句柄类 334 行、非主键类 242 行；
        `git diff --check` 通过；`Debug|x64` 构建通过，0 警告 0 错误。
    P10f-aq 已闭合：
        已删除 `结构_待处理自检报告项::处置类型 / 休眠期候选动作 / 摘要` 三个只有写入、无后续机器读取的文本字段；
        已删除定义后无引用的私有壳：`结构_队列治理对象 / 结构_关键中间状态沉淀项 / 结构_主派发动作 / 结构_队列裁决 / 结构_派发回执 / 结构_派发占用登记 / 结构_结果路由动作 / 结构_回流结算草稿 / 结构_否定项候选 / 结构_缺口触发项 / 结构_本轮结算封口`，以及对应未使用成员 / deque；
        `结构_循环结果::任务管理当前去向` 未进入机器分支，流程判断使用 `当前最终去向 / 批次执行去向 / 任务状态 / 线程裁决结果 / 布尔门控`，本次登记为输出文本边界；
        已生成 `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_self_snapshot_fields.tsv`，剩余记录 581 行，其中主键 / ID / 键 / 句柄类 334 行、非主键类 247 行；
        `git diff --check` 通过；`Debug|x64` 构建通过，0 警告 0 错误。
    P10f-ap 已闭合：
        已删除 `自我线程模块.impl.cpp::结构_根特征必要条件定义_线程侧` 的 `检查宿主语义 / 目标状态文本 / 缺口需求名称文本` 三个 string 字段；
        `检查宿主语义` 改为局部 `检查宿主入口名称种子`，注册后由 `检查宿主类型` 承接；
        `缺口需求名称文本` 改为局部 `缺口需求入口名称种子`，注册后由 `缺口需求名称` 承接；
        `目标状态文本` 只有赋值无读取，已删除；
        保留机器承载字段：`条件键 / 检查宿主类型 / 检查特征类型 / 目标值 / 缺口需求名称 / 缺口目标特征类型 / 条件定义节点`；
        `rg` 复核旧字段名无残留；
        `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_root_condition_text_fields.tsv` 已生成，剩余记录 612 行，其中主键 / ID / 键 / 句柄类 341 行、非主键类 271 行；
        `git diff --check` 通过；
        `Debug|x64` 构建通过，0 警告 0 错误。
    P10f-ao 已闭合：
        已复核 `自我线程模块.impl.cpp::结构_根闭环信息位_线程侧` 的 `根定义编号 / 闭环位置 / 事实类型 / 目标语义要求 / 闭合证据 / 缺口类型 / 回写目标 / 下一轮判断入口`；
        同一结构保留机器判断字段：`根特征类型 / 事实特征类型 / 可转需求 / 服务上下文字段 / 间接服务维护字段 / 服务触发事实 / 当前事实存在 / 当前值 / 目标值 / 闭合状态`；
        根闭环信息位查找使用 `根特征类型 + 事实特征类型 + 目标值`，不读取文本字段；
        根特征必要条件子需求刷新准入读取 `闭环信息位->可转需求` 和 `闭合状态`，不读取 `缺口类型` 文本；
        文本字段命中均为赋值或日志输出；
        未发现这些文本字段参与需求生成、入树、等待状态、任务派发、调度、结算或因果匹配；
        本切片无代码改动，未重新构建。
    P10f-an 已闭合：
        已复核 `语素类.cpp::结构_语素SQL行` 的 `路径 / 节点类型 / 词面 / 显示文本 / 词性文本 / 信息入口类型文本 / 基础信息类型文本`；
        该结构是 `语素类::重写语素SQL投影(...)` 使用的局部 SQL 投影行结构；
        同一行保留 `有词性 / 词性值 / 有信息入口类型 / 信息入口类型值 / 有基础信息类型 / 基础信息类型值 / 对应基础信息主键 / 已绑定基础信息`；
        控制面板读取 `fishnest.v_current_lexeme_nodes` 的文本列用于语素树 SQL 表格、子链 JSON 和页面展示；
        `需求类.cpp` 只在需求 SQL 展示视图中 join 语素文本列生成目标特征显示名 / 类型名；
        语素身份判断、信息入口路由、主信息类型选择和只读查找仍读取语素入口节点、词节点、词性枚举、信息入口类型枚举、主信息类型值和基础信息主键；
        未发现这些 SQL 投影文本字段进入语素身份判断、信息入口路由、主信息类型选择或机器查找；
        本切片无代码改动，未重新构建。
    P10f-am 已闭合：
        已复核 `需求类.cpp::结构_需求树SQL行` 的 `路径 / 节点名称 / 目标语义 / 逻辑组织类型`；
        该结构是 `需求类::重写需求树SQL投影(...)` 使用的局部 SQL 投影行结构；
        `节点名称` 来自根 / 安全 / 服务固定显示名，或 `语素集.安全获取词(目标特征类型)` 的显示文本；
        `目标语义 / 逻辑组织类型` 由 `需求目标语义视图` 的枚举结果渲染；
        同一行保留 `节点主键 / 父节点主键 / 需求主体主键 / 需求场景主键 / 当前状态主键 / 目标状态主键 / 目标特征类型主键 / 对应任务主键 / 满足关系 / 权重 / 结算值` 等结构化承载字段；
        `需求类` 内部机器逻辑读取 `需求目标语义视图(...)` 的枚举、逻辑组织类型、目标特征类型、目标状态、需求节点和主键，不读取 SQL 投影文本字段；
        控制面板读取 `fishnest.v_current_demand_panel_nodes` 的文本列用于表格、子链 JSON 和页面展示；
        未发现这些文本字段进入需求生成、任务化、目标比较、调度、结算或因果匹配；
        本切片无代码改动，未重新构建。
    P10f-al 已闭合：
        已复核 `双目相机本能适配器.h::调用结果::消息` 与 `外设线程_D455深度相机.ixx::结构_D455深度相机观察样本::消息`；
        `双目相机本能适配器.cpp::打开 / 释放 / 检查 / 采集一帧 / 从帧生成结果` 写入 `结果.消息`，机器状态由 `成功 / 相机已打开 / 失败原因 原因 / 帧尺寸 / 数量 / 质量评分` 承载；
        D455 样本生成把 `来源.原因` 写为 `样本.失败原因` 数值，把 `来源.成功` 写为 `样本.采集成功`，`样本.消息` 只承接诊断文本；
        逐簇识别报告只在失败时把 `来源.消息` 复制到 `报告.失败原因` 展示文本；
        `鱼巢.cpp` 命令行相机命令退出码读取 `结果.成功` 或 `结果.原因`，不读取消息文本；
        定向检索未发现 `消息` 文本参与运行模式、结果选择、任务推进、等待状态、调度、结算或因果匹配；
        本切片无代码改动，未重新构建。
    P10f-ak 已闭合：
        已删除 `数据库ADO模块.ixx::结构_ADO查询结果::列名`；
        已删除 `数据库ADO模块.cpp::执行ADO查询(...)` 中对 `结果.列名` 的填充循环；
        `结构_ADO查询结果::行集` 保留为 SQL 查询 / 控制面板结果容器；
        `行集` 读取点集中在 `控制面板类.cpp` 的 SQL 控制面板读模型、HTML 表格和 JSON 输出；
        控制面板对子链节点键、展示列和 SQL 页面过滤的读取不作为任务推进、调度、结算或因果匹配依据；
        本次触碰的 `数据库ADO模块.cpp` 已规范化为 LF；
        `rg` 复核 `列名 / .列名 / std::vector<std::string> 列名` 无残留；
        `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_ado_query_result.tsv` 已生成，剩余记录 615 行，其中主键 / ID / 键 / 句柄类 341 行、非主键类 274 行；
        `git diff --check` 通过；
        `Debug|x64` 构建通过，0 警告 0 错误。
    P10f-aj 已闭合：
        已复核 `世界树类.cpp::结构_世界树SQL节点行 / 结构_世界树SQL关系行` 的 `路径 / 节点类别 / 显示文本 / 主信息类型文本 / 名称文本 / 类型文本 / 值类别 / 值文本 / 辅助文本 / 关系名 / 目标类别 / 目标文本`；
        这些字段是 `世界树类::重写世界树SQL投影(...)` 的局部 SQL 投影行结构；
        `主信息类型文本 / 节点类别` 由 `枚举_主信息类型` 渲染，同一行保留 `主信息类型值`；
        `名称文本 / 类型文本` 来自语素入口显示文本，同一行保留 `名称主键 / 类型主键`；
        `关系名 / 目标类别 / 目标文本` 是世界树 SQL 关系投影展示列，结构化列仍有宿主主键、目标主键、目标行号和序号；
        控制面板读取 `fishnest.v_current_world_tree_nodes / relations` 的文本列用于 SQL 表格、世界树子层和因果信息页展示；
        `需求类.cpp` 只在需求 SQL 展示视图中用世界树文本列生成显示名 / 类型名；
        未发现这些文本字段进入世界树事实判断、任务推进、调度、结算或因果匹配；
        本切片无代码改动，未重新构建。
    P10f-ai 已闭合：
        已复核 `日志模块.ixx` 的日志参数、单文件日志器文件前缀、当前日期、文件标识、时间戳和行文本；
        `文件前缀 / 文件标识 / 当前日期` 只用于生成日志文件路径、切日重开文件和清理日志器内部状态；
        `ts / 行文本` 只用于拼接日志行并写入文件或调试输出；
        日志类别和级别已有 `枚举_日志类别 / 枚举_日志级别` 承载；
        未发现这些 string 字段进入业务状态、任务推进、调度、许可、结算或因果判断；
        本切片无代码改动，未重新构建。
    P10f-ah 已闭合：
        已删除 `任务模块.运行包.ixx::结构_一步治理任务包::完成条件`；
        该字段只有定义，无赋值和读取；
        `rg` 复核 `std::string 完成条件 / .完成条件` 无残留；
        `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_run_package_completion_condition.tsv` 已生成，剩余记录 616 行，其中主键 / ID / 键 / 句柄类 341 行、非主键类 275 行；
        `git diff --check` 通过；
        `Debug|x64` 构建通过，0 警告 0 错误。
    P10f-ag 已闭合：
        已复核 `任务模块.管理线程协议.ixx::结构_任务界面线程快照` 的 `最近任务状态 / 最近调度动作 / 最近调度事件类型 / 最近工作项类型 / 最近派生需求入树处理结果 / 最近后台worker工作项类型 / 最近后台worker状态 / 最近状态提交确认状态`；
        `最近任务状态 / 最近调度动作 / 最近调度事件类型 / 最近工作项类型 / 最近后台worker工作项类型` 均由结构化枚举渲染；
        `最近后台worker状态 / 最近状态提交确认状态 / 最近派生需求入树处理结果` 是界面线程快照和控制面板诊断文本；
        任务推进、调度、许可、等待和结算判断仍读取请求状态枚举、调度动作枚举、工作项类型枚举、任务状态枚举、工作项 ID 和任务 / 方法主键；
        控制面板读取这些字段后只复制到控制面板快照、详情叶字段和页面摘要；
        定向条件检索唯一命中为局部枚举变量 `最近工作项类型 == 枚举_任务工作项类型::执行`，不是协议快照 string 字段；
        本切片无代码改动，未重新构建。
    P10f-af 已闭合：
        已复核 `任务模块.管理界面线程.impl.cpp::结构_任务界面线程状态` 的 `幂等索引 / 外设承接上行幂等表 / 在途工作项索引 / 执行前许可任务占用表 / 执行前许可免审批方法白名单 / 任务状态快照表 / 等待任务索引 / 挂起任务索引`；
        `幂等索引` 使用请求 / 承接幂等键，等待态复用判断读取结构化请求状态、调度动作和下一步去向枚举；
        `外设承接上行幂等表` 使用消息幂等键防重；
        在途 / 许可占用 / 任务状态快照 / 等待 / 挂起索引均以任务主键为 key；
        执行前许可免审批白名单以方法主键为 key；
        等待 / 挂起分类由 `枚举_任务状态::等待中 / 挂起` 判断，执行前许可免审批由方法主键命中判断；
        未发现这些 string-key 容器以自由文本状态、原因、说明或摘要作为机器判断依据；
        本切片无代码改动，未重新构建。
    P10f-ae 已闭合：
        已复核 `任务模块.管理界面线程.impl.cpp::结构_后台worker当前工作快照` 的 `工作项类型 / 状态`；
        `工作项类型` 由 `枚举_任务工作项类型` 渲染为文本；
        `状态` 只记录后台 worker 运行阶段，如启动 / 停止 / 取项 / 派发；
        worker 派发、等待、完成队列归并和错误处理仍读取 `结构_任务工作项::工作类型` 枚举、请求状态枚举、调度动作枚举以及队列 / 在途记录；
        `私有_后台worker当前快照文本(...)` 只把这两个字段拼入 join 诊断日志；
        控制面板只显示界面线程快照中的最近 worker 文本；
        定向检索未发现这两个快照 string 字段用于 `if / while / switch` 机器判断；
        本切片无代码改动，未重新构建。
    P10f-ad 已闭合：
        已复核 `任务类.cpp::结构_任务树SQL行` 的 `路径 / 节点种类文本 / 任务状态文本 / 名称文本 / 类型文本`；
        该结构是 `任务类::重写任务树SQL投影(...)` 的局部 SQL 投影行结构；
        `节点种类文本 / 任务状态文本` 由枚举渲染，同一 SQL 行同时保留 `节点种类值 / 任务状态值`；
        `名称文本 / 类型文本` 来自任务节点主信息的语素入口显示文本；
        控制面板读取 `fishnest.v_current_task_tree_nodes` 的文本列用于任务树 SQL 表格和子层 JSON；
        `需求类.cpp` 只在需求 SQL 展示视图中用任务文本列生成展示名 / 类型名；
        未发现这些文本字段进入任务状态推进、候选选择、等待判断、调度选择、结算或因果匹配；
        本切片无代码改动，未重新构建。
    P10f-ac 已闭合：
        已复核 `任务模块.管理工作线程.ixx::结构_工作线程实例快照` 的 `最近下一步去向 / 最近总控结果 / 最近因果链状态 / 最近缺口归类 / 最近推进阶段 / 最近推进状况 / 最近筹办承接`；
        写入点集中在 `私有_刷新快照_已加锁(...)`，文本由已有结构化枚举或推进回执渲染而来；
        上层机器承接仍读取 `结构_任务管理工作线程结果::下一步去向 / 最近总控结果` 等枚举字段；
        文本读取点集中在控制面板线程详情 / 树节点展示和 `鱼巢.cpp` 运行日志摘要；
        `rg` 定向复核未发现这些快照 string 字段进入状态推进、等待判断、缺口归类、调度选择、结算或因果匹配；
        本切片无代码改动，未重新构建。
    P10f-ab 已闭合：
        已删除 `结构_外设提交包头::标定版本`；
        已删除只为标定版本拼接文本的 `外设提交_标定版本(...)`；
        已删除 `构造外设提交包头_由观察报告(...)` 中的 `包头.标定版本` 赋值；
        已将 `结构_D455观察材料句柄摘要::资源类型` 收束为 `资源类型ID`，并同步材料页读取写入、未找到摘要初始化和读取失败日志；
        已刷新 `说明书/函数功能说明文档.md`，函数条目 7184；
        `rg` 复核 `外设提交_标定版本 / .标定版本 / std::string 标定版本 / .资源类型 / std::string 资源类型` 无残留；
        `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_calibration_resource_fields.tsv` 已生成，剩余记录 617 行，其中主键 / ID / 键 / 句柄类 341 行、非主键类 276 行；
        `git diff --check` 通过；
        `Debug|x64` 构建通过，0 警告 0 错误。
    P10f-aa 已闭合：
        已删除 `结构_外设观察报告队列项::配置摘要 / 差异摘要 / 跨帧采集摘要`；
        已删除 D455 写入侧对这些摘要字段的赋值、拼接和日志输出；
        已保留跨帧数量、扫描变化数量、目标约束状态、材料句柄、坐标系 ID、方法 ID、失败原因等结构化字段；
        `rg` 复核 `.配置摘要 / .差异摘要 / .跨帧采集摘要` 和对应 `std::string` 字段声明无残留；
        `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_report_summary_fields.tsv` 已生成，剩余记录 618 行，其中主键 / ID / 键 / 句柄类 340 行、非主键类 278 行；
        `git diff --check` 通过；
        `Debug|x64` 构建通过，0 警告 0 错误。
    P10f-z 已闭合：
        已将 `相机内参版本 / 相机外参版本` 收束为 `相机内参版本ID / 相机外参版本ID`；
        已将 `坐标系版本` 收束为 `坐标系ID`；
        已将 `报告生成方法版本` 收束为 `报告生成方法版本ID`；
        已同步 D455 报告写入、稳定子集派生、提交包头生成、特征值构造、扫描数据项坐标系读取和报告 generation 标识生成；
        `rg` 复核旧字段访问无残留；
        `实施记录/20260624_P10f_live_结构体string字段声明复扫_after_version_id_fields.tsv` 已生成，剩余记录 621 行，其中主键 / ID / 键 / 句柄类 340 行、非主键类 281 行；
        `git diff --check` 通过；
        `Debug|x64` 构建通过，0 警告 0 错误。
    P10f-y 已闭合：
        已将 `空间候选队列引用 / 像素归属账本引用` 收束为 `空间候选队列句柄 / 像素归属账本句柄`；
        已将识别提交包 `像素归属账本` 收束为 `像素归属账本句柄`；
        已将扫描 / 跟踪提交包 `当前ROI / 当前掩码 / 当前点集 / 当前深度切片` 收束为 `当前ROI句柄 / 当前掩码句柄 / 当前点集句柄 / 当前深度切片句柄`；
        已同步 D455 写入、外设材料汇总 / 缺口判断、材料页摘要、扫描可回查判断、自我线程日志、外设方法验证、任务管理白名单和线程侧特征入口；
        `rg` 复核旧字段访问无残留；
        `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_material_handle_fields.tsv` 已生成，剩余记录 621 行，其中主键 / ID / 键 / 句柄类 336 行、非主键类 285 行；
        `git diff --check` 通过；
        `Debug|x64` 构建通过，0 警告 0 错误。
    P10f-x 已闭合：
        已删除 `结构_外设特征值条目::单位` 字段；
        已删除 `外设提交_构造I64特征(...)` 的 `单位` 参数；
        已删除所有调用点中的 `"像素" / "毫米" / "百万分比" / "数量" / "枚举值"` 单位文本传参；
        `rg` 复核目标文件中 `.单位 / std::string 单位 / const std::string& 单位 / "像素" / "毫米" / "百万分比" / "数量" / "枚举值"` 无残留；
        `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_feature_value_unit.tsv` 已生成，剩余记录 621 行，其中主键 / ID / 键 / 句柄类 325 行、非主键类 296 行；
        `git diff --check` 通过；
        `Debug|x64` 构建通过，0 警告 0 错误。
    P10f-w 已闭合：
        已删除 `结构_外设特征值条目::文本值`；
        已删除 `结构_外设特征值条目::来源动态`；
        已将 `结构_外设特征值条目::来源材料` 收束为 `来源材料句柄集`；
        已同步更新 `外设提交_构造I64特征(...)` 参数、所有特征构造调用点，以及 `自我动作实现.扫描.ixx` 中的 `可回查` 判断；
        首次构建失败暴露 `自我动作实现.扫描.ixx` 读取 `来源材料`，已修正为 `来源材料句柄集`；
        `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_feature_value_entry.tsv` 已生成，剩余记录 622 行，其中主键 / ID / 键 / 句柄类 325 行、非主键类 297 行；
        已处理本次触碰的 `自我动作实现.扫描.ixx` CRLF 提示，未做全仓换行；
        `git diff --check` 通过；
        `Debug|x64` 构建第二次通过，0 警告 0 错误。
    P10f-v 已闭合：
        已复核 `结构_外设观察像素簇摘要::失败原因`、`结构_外设观察报告队列项::失败原因`、`结构_D455控制面板视频快照::失败原因`、`结构_D455三维体素轮廓视角读取结果::失败原因`；
        D455 写入侧只在采集失败、目标约束未命中、目标种子缺失、当前簇匹配失败等路径赋值失败原因文本；
        队列侧对 `报告项.失败原因` 和 `簇.失败原因` 的 `empty()` 判断只用于是否拼接摘要日志或控制面板可读输出；
        控制面板视频快照和三维体素轮廓视角读取结果由 `成功` 布尔值承载机器成功状态，失败原因文本只作为读取失败时的人类可读说明；
        未发现这些 string 字段进入候选选择、等待匹配、状态推进、稳定键、结算或因果匹配；
        本切片无代码改动，未重新构建。
    P10f-u 已闭合：
        已将 `结构_外设观察像素簇摘要` 的 `ROI引用 / 点集引用 / 空间候选引用 / 像素归属验证材料引用` 收束为 `ROI句柄 / 点集句柄 / 空间候选句柄 / 像素归属验证材料句柄`；
        已将 `结构_目标观察约束特征组` 的 `ROI引用 / 点集引用` 收束为 `ROI句柄 / 点集句柄`；
        已同步更新 D455 写入、目标观察约束匹配、材料可用性判断、提交包、自我动作外设模块读取、任务管理材料字段白名单和自我线程侧特征入口；
        `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_cluster_handle_fields.tsv` 已生成，剩余记录 624 行，其中主键 / ID / 键 / 句柄类 324 行、非主键类 300 行；
        `rg` 复核 `ROI引用 / 点集引用 / 空间候选引用 / 像素归属验证材料引用` 在当前 `.ixx/.cpp/.h` 源码无命中；
        `git diff --check` 通过；
        `Debug|x64` 构建通过，0 警告 0 错误。
    P10f-t 已闭合：
        已将 `结构_外设观察等待项::目标外设` 改名为 `目标外设ID`；
        已将 `任务模块.管理工作线程.ixx::结构_外设消息承接结果::目标外设` 改名为 `目标外设ID`；
        已同步更新报告满足等待项、等待项集合按外设过滤、D455 等待项判断、任务管理等待项构造、承接结果摘要和等待项摘要日志；
        `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_target_device_id.tsv` 已生成，剩余记录 624 行，其中主键 / ID 类 318 行、非主键类 306 行；
        `rg` 复核 `.目标外设 / std::string 目标外设{} / 目标外设 =` 仅剩其它模块日志或输入参数名文本；
        `git diff --check` 通过；
        `Debug|x64` 构建通过，0 警告 0 错误。
    P10f-s 已闭合：
        已将 `结构_外设观察等待项::目标区域或目标簇` 替换为数值字段 `目标观察簇ID`，已有 `目标存在ID` 继续承载目标存在主键；
        已将任务管理界面线程构造等待项逻辑改为直接写 `目标存在ID`；
        已删除目标观察约束匹配和可用性判定中基于 `目标区域或目标簇` 的兼容 fallback；
        已将 D455 跟踪稳定缓存项的 `跟踪种子` 改为 `目标观察簇ID`；
        已删除 `D455_尝试解析跟踪种子簇ID(...)`，跟踪降级匹配只读取 `等待项.目标观察簇ID`；
        已删除临时扫描等待项中的 `"单帧材料包证据卡"` 文本赋值；
        已刷新 `说明书/函数功能说明文档.md`，函数条目 7185；
        `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_wait_target_split.tsv` 已生成，剩余记录 624 行，其中主键 / ID 类 316 行、非主键类 308 行；
        `Debug|x64` 构建通过，0 警告 0 错误。
    P10f-r 已闭合：
        已删除 `结构_外设观察报告队列项::跟踪种子`；
        已删除 D455 跟踪报告生成时对 `报告.跟踪种子` 的文本复制；
        `构造外设跟踪提交包_由观察报告` 的缺目标种子判断改为读取 `跟踪ID / 目标观察约束ID / 匹配等待项ID`；
        已删除报告摘要中的 `跟踪种子` 文本输出；
        `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_tracking_seed.tsv` 已生成，剩余记录 626 行，其中主键 / ID 类 316 行、非主键类 310 行；
        首次构建暴露报告项无 `目标存在ID`，已改为报告内实际存在的 `匹配等待项ID`；
        `rg` 复核 `报告.跟踪种子 / 报告项.跟踪种子` 无残留；
        `git diff --check` 通过；
        `Debug|x64` 构建第二次通过，0 警告 0 错误。
    P10f-q 已闭合：
        已将 `结构_外设观察报告队列项::来源外设` 改名为 `来源外设ID`；
        已同步更新 D455 报告写入、等待项目标外设匹配、提交包头外设 ID 生成、按外设 ID 查询 / 过滤、任务管理观察事实承接写入、正式读取口径匹配和报告摘要日志；
        `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_observation_report_device_id.tsv` 已生成，剩余记录 627 行，其中主键 / ID 类 316 行、非主键类 311 行；
        `rg` 复核无旧字段访问残留；
        `git diff --check` 通过；
        `Debug|x64` 构建通过，0 警告 0 错误。
    P10f-p 已闭合：
        已将 `结构_任务管理观察事实承接请求::来源外设` 改名为 `来源外设ID`；
        已同步更新任务管理工作线程写入、任务管理观察事实承接请求日志、自我线程消息幂等摘要和自我线程观察事实承接日志；
        `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_observation_device_id.tsv` 已生成，剩余记录 627 行，其中主键 / ID 类 315 行、非主键类 312 行；
        `rg` 复核目标文件无旧字段访问残留；
        `git diff --check` 通过；
        `Debug|x64` 构建通过，0 警告 0 错误。
    P10f-o 已闭合：
        已删除 `结构_任务管理线程补充特征::文本值`；
        唯一聚合初始化点不再传递步骤主键文本，步骤主键仍由消息头 `步骤主键` 保存；
        首次构建暴露 `自我线程模块.impl.cpp::私有_读取线程补充特征文本` 仍读取 `文本值`，已删除该文本读取入口；
        自我线程治理判断改为读取 `当前步骤主键` 补充特征的 `指针值`，并以消息头 `步骤主键` 作为结构化补充判断；
        控制请求存在判断改为读取 `线程消息.控制请求主键`；
        `说明书/函数功能说明文档.md` 已刷新，函数条目 7186；
        `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_thread_extra_feature.tsv` 已生成，剩余记录 627 行，其中主键类 314 行、非主键类 313 行；
        `rg` 复核 `私有_读取线程补充特征文本 / .文本值 / 文本值` 在目标任务管理线程补充特征路径无残留；
        `Debug|x64` 构建通过，0 警告 0 错误。
    P10f-n 已闭合：
        已新增筹办内部枚举 `枚举_父需求继续策略_筹办` 表示父需求继续策略；
        已将 `结构_临时因果视图::父需求继续策略` 从 `std::string` 改为枚举，默认保持原行为“继续”；
        `确保临时视图学习缺口_筹办查询` 的机器判断已改为 `父需求继续策略允许继续_筹办(...)`；
        日志输出通过 `父需求继续策略文本_筹办日志(...)` 渲染文本；
        `说明书/函数功能说明文档.md` 已刷新，函数条目 7187；
        `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_parent_strategy.tsv` 已生成，剩余记录 628 行，其中主键类 314 行、非主键类 314 行；
        已处理本次触碰的 `说明书/函数功能说明文档.md` CRLF 提示，未做全仓换行；
        `Debug|x64` 构建通过，0 警告 0 错误。
    P10f-m 已闭合：
        已将 `基础数据类型.h::结构体_分词::词性` 从 `std::string` 改为 `枚举_词性`；
        `结构体_分词` 构造函数已改为接收 `枚举_词性`；
        `语素类` 的 `结构体_分词` 重载直接走枚举词性路径，不再经由文本词性重载；
        `结构体_分词::词` 保留为语素词面入口种子，注册后仍由语素入口节点承接；
        `说明书/函数功能说明文档.md` 已刷新，函数条目 7185；
        `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_lexeme_token.tsv` 已生成，剩余记录 629 行，其中主键类 314 行、非主键类 315 行；
        已处理本次触碰的 `说明书/函数功能说明文档.md` CRLF 提示，未做全仓换行；
        `Debug|x64` 构建通过，0 警告 0 错误。
    P10f-l 已闭合：
        已确认 `方法类.cpp::结构_方法树SQL行` 是方法树 SQL 投影局部结构；
        `路径 / 节点种类文本 / 动作名文本 / 动作句柄文本 / 来源文本` 由方法节点、枚举、动作句柄和主键生成，仅写入 `fishnest.method_tree_node / fishnest.method_main_info / fishnest.v_current_method_tree_nodes`；
        仓内反查显示这些文本列除 `方法类.cpp` SQL 投影生成外，只被 `控制面板类.cpp` 根层查询和子层 JSON 查询读取；
        方法树机器列仍有 `节点种类值 / 来源值 / 方法虚拟存在主键 / 条件场景主键 / 结果场景主键 / 主结果特征主键 / 结果项数量 / 允许自动查找 / 有方法动作 / 有结果能力`；
        未发现文本字段进入方法查找、排序、选择或执行准入；
        本切片无代码改动，未重新构建。
    P10f-k 已闭合：
        已将 `度量模板注册表类::结构_刻度片::名称` 改为模块内 `枚举_刻度语义`；
        已将 `结构_度量刻度片_桥接::名称` 改为 `枚举_度量刻度语义代码`；
        `二次特征类::私有_确保默认概念刻度` 已改为按刻度语义代码生成语素入口名称种子，不再读取刻度文本字段作为概念模板生成依据；
        `说明书/函数功能说明文档.md` 已刷新，函数条目 7185；
        `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_causal_structured.tsv` 已刷新，最新记录 630 行，其中主键类 314 行、非主键类 316 行；
        `rg` 复核无 `刻度.名称` 或目标结构 `std::string 名称` 残留；
        `Debug|x64` 构建通过，0 警告 0 错误。
    P10f-j 已闭合：
        已删除 `结构体_本能动作登记信息::函数名称` 文本字段；
        已删除 `注册(...)` 的 `std::string 函数名称` 参数，注册合法性改为 `枚举_本能动作ID` + `本能方法类::是有效本能方法能力值(...)`；
        已删除 `查询ID(const std::string&)` 文本反查入口，以及 `检查是否可调用` 中基于 `方法动作名(方法)->获取主键()` 的文本 fallback；
        本能动作准入必须通过 `方法动作句柄` 的 `本能函数ID / 本能ID`；
        6150 诊断日志中的动作名只按 `动作ID` 渲染显示，不参与机器判断；
        `说明书/函数功能说明文档.md` 已刷新，函数条目 7182；
        `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_causal_structured.tsv` 已刷新，最新记录 632 行，其中主键类 314 行、非主键类 318 行；
        `rg` 复核无 `函数名称 / 查询ID / 默认函数名称 / 默认动作ID列表 / 注册函数名` 残留；
        `Debug|x64` 构建通过，0 警告 0 错误。
    P10f-i 已闭合：
        已删除 `结构_治理二次特征候选::概念名称` 文本字段；
        候选结构改为保存 `概念入口 / 特征入口`，候选构造阶段立即把文本种子转换为关系模板入口和治理特征入口；
        `世界树类::写入治理二次特征` 已改为接收 `const 语素入口节点类* 概念入口`，跨模块写入路径不再以文本为机器依据；
        `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_causal_structured.tsv` 已刷新，最新记录 633 行，其中主键类 314 行、非主键类 319 行；
        `rg` 复核无 `候选.概念名称` 或 `结构_治理二次特征候选` string 字段残留；
        `Debug|x64` 构建通过，0 警告 0 错误。
    P10f-h 已闭合：
        已删除 `结构_场景二次特征写入请求::概念名称 / 概念词性`；
        写入请求构造处改为直接写入 `概念入口`，`写入场景二次特征` 只读取内部身份结构；
        `世界树类.cpp::写入治理二次特征` 已在跨模块入口处把外部概念名称转换为关系模板语素入口；
        `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_causal_structured.tsv` 已刷新，最新记录 634 行，其中主键类 314 行、非主键类 320 行；
        `Debug|x64` 构建通过，0 警告 0 错误。
    P10f-g 已闭合：
        已确认 `结构_状态变化跟踪项::特征类型文本 / 特征值文本` 只用于 `完成任务变化跟踪` 输出行；
        状态跟踪仍保留 `状态主键 / 目标宿主主键 / 特征类型主键`，文本未进入稳定键、排序、候选选择、状态推进或机器判断；
        本切片无代码改动，未重新构建。
    P10f-f 已闭合：
        已将任务治理幂等键中的请求类型从显示文本改为枚举数值；
        已将外设观察等待项幂等键中的观察口径和观察运行模式从显示文本改为枚举数值；
        已去除外设观察等待项幂等键中的中文 `"空"` 占位，空主键 / 空 generation 保留为空值段；
        已确认任务管理界面线程等待 / 挂起 / 在途 / 执行前许可占用索引以任务主键为键，白名单以方法主键为键，任务状态判断使用 `枚举_任务状态`，后台 worker 文本快照只用于显示；
        `rg` 复核无显示文本参与幂等键命中；`Debug|x64` 构建通过，0 警告 0 错误。
    P10f-e 已闭合：
        已删除 `线程生命周期消息模块.ixx` 中按中文 `事件类型` 文本反解析机器事件的兼容逻辑；消费路径只接受 `事件类型代码` 数值，缺失或非法代码按无效线程生命周期消息处理；
        已删除 `结构_任务管理观察事实承接请求` 跨线程协议中的 `观察运行模式文本 / 报告类型文本`，改为 `观察运行模式值 / 报告类型值`；
        已将 `结构_外设消息承接结果` 内部保存值从显示文本改为 `枚举_外设观察运行模式 / 枚举_外设观察报告类型`，日志摘要按枚举渲染文本；
        已刷新 `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_causal_structured.tsv`，最新记录 636 行，其中主键类 314 行、非主键类 322 行；
        `rg` 复核无旧字段访问或中文事件文本反解析命中；`Debug|x64` 构建通过，0 警告 0 错误。
    P10f-d 已闭合：
        已删除任务治理协议中无消费的 `影子验证状态 / 当前恢复点类型 / 排序关系 / 主域 / 小域 / 快照版本 / 最近治理态型 / 动态标题` 等文本状态或占位字段；
        已删除自我治理来源诊断中的 `影子验证状态 / 来源主观察特征` 复制链，以及 `投递治理外部反馈事件` 无消费文本参数；
        已删除低引用只声明字段：`触发动作主体类型文本 / 停止条件 / 治理桥文本占位 / 变化方向语义 / 占用队列`；
        `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_causal_structured.tsv` 已刷新，最新记录 636 行，其中主键类 314 行、非主键类 322 行；
        关键词复核未发现 `摘要 / 原因 / 说明` 直接参与候选选择、状态推进、等待状态、稳定键或结算判断的当前无堵点项；
        `Debug|x64` 构建通过，0 警告 0 错误。
    P10f-c 因果投影缺失证据结构化接口专项已闭合：
        已将 `因果类 / 世界树` 缺失证据接口从 `std::vector<std::string>` 改为 `std::vector<结构_因果缺失证据项>`；
        已删除 `结构_线程结算因果投影预判 / 结构_叶子任务正式结算证据` 的 `摘要` 字段，并将其 `缺失证据` 改为结构化缺失项；
        `任务模块.筹办.ixx` 只按结构化缺失项数量统计，不按文本判断；
        已刷新 `说明书/函数功能说明文档.md`；
        `Debug|x64` 构建通过，0 警告 0 错误。
    P10f-c 本轮无堵点摘要 / 原因类清理已收束：
        已删除服务治理维护、非核心结算闭账、需求收束、价值结算等结果壳摘要字段；
        已将派生需求入树原因分类跨函数 string 输出改为枚举输出；
        各代码切片均已通过 Debug x64 构建，0 警告 0 错误。
    P10f-c 最新复扫停止点：
        `实施记录/20260623_P10f_live_结构体string字段声明复扫_after_value.tsv` 复扫剩余 103 行；
        当前未再发现 `摘要 / 原因 / 说明 / 备注 / 依据 / 建议 / 参数结果` 进入幂等键、归档判断、稳定键、状态落账文本参数、等待状态判断、动作验证摘要或候选排序 / 选择的无堵点直接清理项；
        剩余非显示项集中在 `因果类 / 世界树 / 自我线程 / 任务筹办` 的 `std::vector<std::string> 缺失证据` 因果投影接口链，已构成当前 P10f-c 继续删除正式结算证据文本字段的结构化接口堵点。
    P11i live 无调用函数优先复扫已闭合：
        当前无新的无堵点函数删除队列；后续若新增函数清理线索，再按 P11 规则重开独立切片。
    不作为当前堵点：
        控制面板 / 线程生命周期 / 自我线程快照 / 外设报告证据卡 / 语言表达 / 日志诊断文本；P10f-e 已确认当前显示文本不作为当前机器逻辑堵点，后续只有发现进入机器判断时再开专项。
    禁止宣称：
        不宣称需求类逻辑纠偏完成、任务类逻辑纠偏完成、因果类功能补完完成、项目结构闭环全部通过、内部循环完成、自我苏醒完成或初步成熟完成。
当前下一步：
    1. 当前主动队列暂无新的无堵点执行切片。
    2. P10f 后续只在发现 string / 摘要 / 原因 / 说明进入候选选择、状态推进、等待状态、稳定键、结算或因果匹配时重开。
    3. P11i live 无调用函数优先复扫已闭合；当前无新的无堵点函数删除队列。
    4. 控制面板 / 线程生命周期 / 自我线程快照 / 外设报告证据卡 / SQL 投影 / 日志诊断文本不作为当前机器逻辑堵点，只有发现进入机器判断时再开专项。

当前证据指针：
    默认只读：
        实施记录/20260624_P10f_aw_二次特征表达方向模板文本匹配清理_Codex断点清单.md
        实施记录/20260624_P10f_live_结构体string字段声明复扫_after_language_direction_template.tsv
        实施记录/20260624_P10f_av_双目相机调用结果消息字段复核_Codex断点清单.md
        实施记录/20260624_P10f_au_稳定子集桥失败说明字段复核_Codex断点清单.md
        实施记录/20260624_P10f_live_结构体string字段声明复扫_after_stable_subset_bridge_reason.tsv
        实施记录/20260624_P10f_at_任务管理派生需求动作字段复核_Codex断点清单.md
        实施记录/20260624_P10f_live_结构体string字段声明复扫_after_task_derived_action_fields.tsv
        实施记录/20260624_P10f_as_线程状态消息文本字段复核_Codex断点清单.md
        实施记录/20260624_P10f_live_结构体string字段声明复扫_after_thread_state_message_fields.tsv
        实施记录/20260624_P10f_ar_自我初始化线程参数字段复核_Codex断点清单.md
        实施记录/20260624_P10f_live_结构体string字段声明复扫_after_self_init_thread_params.tsv
        实施记录/20260624_P10f_aq_自我线程快照摘要字段复核_Codex断点清单.md
        实施记录/20260624_P10f_live_结构体string字段声明复扫_after_self_snapshot_fields.tsv
        实施记录/20260624_P10f_ap_根特征必要条件文本字段清理_Codex断点清单.md
        实施记录/20260624_P10f_live_结构体string字段声明复扫_after_root_condition_text_fields.tsv
        实施记录/20260624_P10f_ao_根闭环信息位文本字段复核_Codex断点清单.md
        实施记录/20260624_P10f_an_语素SQL行文本字段复核_Codex断点清单.md
        实施记录/20260624_P10f_am_需求树SQL行文本字段复核_Codex断点清单.md
        实施记录/20260624_P10f_al_相机调用结果消息字段边界复核_Codex断点清单.md
        实施记录/20260624_P10f_ak_ADO查询结果文本容器复核_Codex断点清单.md
        实施记录/20260624_P10f_live_结构体string字段声明复扫_after_ado_query_result.tsv
        实施记录/20260624_P10f_aj_世界树SQL投影文本字段复核_Codex断点清单.md
        实施记录/20260624_P10f_ai_日志模块string字段边界复核_Codex断点清单.md
        实施记录/20260624_P10f_ah_运行包完成条件字段清理_Codex断点清单.md
        实施记录/20260624_P10f_live_结构体string字段声明复扫_after_run_package_completion_condition.tsv
        实施记录/20260624_P10f_ag_任务界面线程快照文本字段复核_Codex断点清单.md
        实施记录/20260624_P10f_af_任务界面线程状态字符串键复核_Codex断点清单.md
        实施记录/20260624_P10f_ae_后台worker快照文本字段复核_Codex断点清单.md
        实施记录/20260624_P10f_ad_任务树SQL行文本字段复核_Codex断点清单.md
        实施记录/20260624_P10f_ac_任务管理工作线程快照文本字段复核_Codex断点清单.md
        实施记录/20260624_P10f_ab_外设标定版本资源类型字段复核_Codex断点清单.md
        实施记录/20260624_P10f_live_结构体string字段声明复扫_after_calibration_resource_fields.tsv
        实施记录/20260623_P10f_w_外设特征值条目文本字段清理_Codex断点清单.md
        实施记录/20260624_P10f_aa_外设报告摘要字段清理_Codex断点清单.md
        实施记录/20260624_P10f_live_结构体string字段声明复扫_after_report_summary_fields.tsv
        实施记录/20260624_P10f_z_外设版本坐标系字段复核_Codex断点清单.md
        实施记录/20260624_P10f_live_结构体string字段声明复扫_after_version_id_fields.tsv
        实施记录/20260623_P10f_y_外设材料句柄字段复核_Codex断点清单.md
        实施记录/20260623_P10f_live_结构体string字段声明复扫_after_material_handle_fields.tsv
        实施记录/20260623_P10f_x_外设特征值条目单位字段清理_Codex断点清单.md
        实施记录/20260623_P10f_live_结构体string字段声明复扫_after_feature_value_unit.tsv
        实施记录/20260623_P10f_live_结构体string字段声明复扫_after_feature_value_entry.tsv
        实施记录/20260623_P10f_v_外设观察失败原因字段边界复核_Codex断点清单.md
        实施记录/20260623_P10f_u_外设观察像素簇引用字段复核_Codex断点清单.md
        实施记录/20260623_P10f_live_结构体string字段声明复扫_after_cluster_handle_fields.tsv
        实施记录/20260623_P10f_o_任务管理线程补充特征文本值清理_Codex断点清单.md
        实施记录/20260623_P10f_t_外设等待项目标外设字段复核_Codex断点清单.md
        实施记录/20260623_P10f_live_结构体string字段声明复扫_after_target_device_id.tsv
        实施记录/20260623_P10f_s_外设等待项目标文本拆分_Codex断点清单.md
        实施记录/20260623_P10f_live_结构体string字段声明复扫_after_wait_target_split.tsv
        实施记录/20260623_P10f_r_外设观察报告跟踪种子文本清理_Codex断点清单.md
        实施记录/20260623_P10f_live_结构体string字段声明复扫_after_tracking_seed.tsv
        实施记录/20260623_P10f_q_外设观察报告来源外设字段复核_Codex断点清单.md
        实施记录/20260623_P10f_live_结构体string字段声明复扫_after_observation_report_device_id.tsv
        实施记录/20260623_P10f_p_观察事实承接来源外设字段复核_Codex断点清单.md
        实施记录/20260623_P10f_live_结构体string字段声明复扫_after_observation_device_id.tsv
        实施记录/20260623_P10f_live_结构体string字段声明复扫_after_thread_extra_feature.tsv
        实施记录/20260623_P10f_n_临时因果视图父需求继续策略文本清理_Codex断点清单.md
        实施记录/20260623_P10f_live_结构体string字段声明复扫_after_parent_strategy.tsv
        实施记录/20260623_P10f_m_分词结构词性文本边界复核_Codex断点清单.md
        实施记录/20260623_P10f_live_结构体string字段声明复扫_after_lexeme_token.tsv
        实施记录/20260623_P10f_l_方法树SQL行文本字段边界复核_Codex断点清单.md
        实施记录/20260623_P10f_h_二次特征写入请求概念文本复核_Codex断点清单.md
        实施记录/20260623_P10f_i_治理二次特征候选概念文本复核_Codex断点清单.md
        实施记录/20260623_P10f_j_本能动作登记函数名称文本反查复核_Codex断点清单.md
        实施记录/20260623_P10f_k_度量刻度名称文本语义复核_Codex断点清单.md
        实施记录/20260623_P10f_g_状态变化跟踪项文本字段复核_Codex断点清单.md
        实施记录/20260623_P10f_f_任务管理界面线程string键边界复核_Codex断点清单.md
        实施记录/20260623_P10f_e_显示文本边界复核_Codex断点清单.md
        实施记录/20260623_P10f_因果投影缺失证据结构化接口_Codex断点清单.md
        实施记录/20260623_P10f_d_live_string字段复扫_Codex断点清单.md
        实施记录/20260623_P10f_live_结构体string字段声明复扫_after_causal_structured.tsv
        实施记录/20260623_P10f_string字段复扫当前停止点_Codex断点清单.md
        实施记录/20260623_P10f_live_结构体string字段声明复扫_after_value.tsv
        实施记录/20260623_P10f_正式结算证据缺失项文本接口阻塞分析.md
        基础数据类型.h
        语素类.h
        语素类.cpp
        方法类.cpp
        控制面板类.cpp
        因果类.h
        因果类.cpp
        世界树类.h
        世界树类.cpp
        自我线程模块.impl.cpp
        任务模块.筹办.ixx

    按需展开：
        规范/禁止性规范20260611.md
        规范/规范目录.md
        计划/20260618_项目结构体审计与清理计划_v0.1.md
        计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md
        说明书/函数功能说明文档.md
上下文硬闸：
    默认读取到 `### 1.1` 前停止。
    不整读全仓库源码；清单输出落盘后只回传统计和关键异常。
    大范围 `rg`、结构体清单、引用函数清单、构建和运行输出先落盘，再回传关键行。
    单轮新增输出超过 20KB 或 token 接近 210K 时，先写最小断点并建议开新线程。

禁止宣称：
    不宣称需求类逻辑纠偏完成。
    不宣称任务类逻辑纠偏完成。
    不宣称因果类功能补完完成。
    不宣称项目结构闭环全部通过。
    不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```

### 1.1 最近闭合历史入口

```text
用途：
    只保留近期闭合 / 待条件复核入口，具体证据不再展开到索引。

函数去重与无效代码清理：
    入口：计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md。
    状态：因果类功能函数补完期间暂停，不作为当前主动队列。
    完整证据：实施记录/20260618_函数去重与无效代码清理_Codex断点清单.md。

需求类头文件结构体清理与目标逻辑纠偏：
    入口：计划/完成项/20260619_需求类头文件结构体清理与目标逻辑纠偏计划_v0.1.md。
    状态：4.5 / 4.6 第一优先依赖函数已闭合，已返回因果类外部数据读取纠偏协调计划；不作为当前未完成队列。

方法类逻辑纠偏：
    入口：计划/完成项/20260619_方法类逻辑纠偏计划_v0.1.md。
    状态：方法引用解析和因果类 P7 可调用的方法引用解析已闭合，已返回协调计划 P7；不作为当前未完成队列。

自检需求入树逻辑错误追根因：
    入口：计划/完成项/追根因_20260618_自检需求入树逻辑错误_v0.1.md。
    状态：已定位并验证方法状态 -> 方法维护父域映射；若同标题弹窗复现，按追根因计划重新打开。

需求树结构错挂追根因：
    入口：计划/完成项/追根因_20260619_需求树结构错挂_v0.1.md。
    状态：已验证根层只剩安全 / 服务，方法维护管理状态挂到安全链；若控制面板或 SQL 投影再次异常，按追根因计划重新打开。

D455 存在生成不稳定追根因：
    入口：计划/待完成项/追根因_20260614_D455存在生成不稳定_v0.1.md。
    状态：已转为待真实外设自然业务样本复核；不作为当前主动队列。

扫描动作动态弹窗：
    入口：计划/完成项/追根因_20260616_扫描动作动态弹窗_v0.1.md。
    状态：已闭合；若弹窗复现，按追根因计划重新打开。

D455 稳定轮廓模块重构：
    入口：计划/待完成项/20260614_D455稳定轮廓模块重构计划_v0.1.md。
    状态：当前可执行部分已完成，等待真实外设自然业务样本恢复。

日志输出预编译控制：
    入口：计划/完成项/20260612_日志输出预编译控制实时计划_v0.1.md。
    状态：已完成；只在日志开关体系回归时读取。
```

## 2. 当前未完成计划优先队列

本章只登记 `计划/` 根目录当前可恢复的未完成计划顺序；不等同于开始执行。用户要求“执行 / 继续执行计划”时，先按第 1 章当前主动队列取一个最小闭合切片；若第 1 章堵住，再按本章可执行性检查下一无堵点切片。

执行顺序原则：

```text
1. P0-P8 是 owning-class 边界纠偏池；当前登记可执行切片已按第 1 章停止点处理，后续新缺口按“缺哪个类补哪个类第一优先”重新登记。
2. P9 因果类功能函数补完 P1-P6 已具备构建证据；后续新因果缺口按触发点登记。
3. P10-P11 是横向审计 / 清理线，当前主动队列已停止在 P10f-b；当前没有无堵点批删对象。后续必须先选明确专项，执行前重新用 live rg 确认引用，若触碰代码再补函数说明文档和真实构建证据。
4. 执行中若发现阻塞性跨类缺口，按“缺哪个类补哪个类第一优先”临时提升对应类第一优先切片，闭合后返回当前横向审计线。
5. `计划/` 根目录未完成计划已纳入本章；`计划/完成项/` 只保留短查询入口，不回拉为当前主动队列；`计划/待完成项/` 只在恢复条件成立或用户明确切入时进入当前主动队列。
```

| 优先级 | 计划入口 | 当前定位 | 堵点 / 可执行性 | 下一候选切片 |
| --- | --- | --- | --- | --- |
| P0 | `计划/20260620_需求类逻辑纠偏计划_v0.1.md` | 需求主信息外部直读、目标语义、权重和跨类操作审查。 | 当前可执行机器逻辑切片已收束；控制面板 / SQL / 摘要显示读源后置，不作为机器逻辑堵点。 | 不主动执行；后续新需求类缺口按触发点登记。 |
| P1 | `计划/20260620_任务类逻辑纠偏计划_v0.1.md` | 任务状态、任务虚拟存在、当前方法、任务执行方法和任务模块协议边界。 | 当前可执行切片已收束；任务模块协议结构审计仍可作为 P10 专项候选，不直接混入任务类当前队列。 | 不主动执行；后续新任务类缺口按触发点登记。 |
| P2 | `计划/完成项/20260620_方法类逻辑纠偏计划_v0.1.md` | 方法节点三类角色、动作入口、结果能力和方法虚拟存在边界。 | v0.1 当前范围已闭合；只作查询入口，不作为当前主动队列。 | 无；后续新方法类缺口按跨类规则重新登记。 |
| P3 | `计划/20260620_状态类逻辑纠偏计划_v0.1.md` | 状态节点读取、创建、排序时间、状态特征和值读取边界。 | 当前登记切片已闭合；不重复执行。 | 后续新状态类缺口按触发点登记。 |
| P4 | `计划/20260620_动态类逻辑纠偏计划_v0.1.md` | 动态来源、聚合视图、排序时间、动态主信息清理边界。 | 当前登记切片已闭合；不重复执行。 | 后续新动态类缺口按触发点登记。 |
| P5 | `计划/20260620_特征类逻辑纠偏计划_v0.1.md` | 特征结构体必要性、字段引用、非特征主信息直接操作审查。 | 当前登记切片已闭合；不重复执行。 | 后续新特征类缺口按触发点登记。 |
| P6 | `计划/20260620_二次特征类逻辑纠偏计划_v0.1.md` | 二次特征信息统一入口、动态聚合链和标量区间边界。 | P8 当前可执行机器逻辑审计切片已收束；剩余显示 / SQL / 语言层直读后置。 | 不主动执行；后续新二次特征缺口按触发点登记。 |
| P7 | `计划/20260620_存在类逻辑纠偏计划_v0.1.md` | 存在结构、三类根节点只读 / 确保拆分、坐标 helper 和外部直读回迁。 | 当前登记切片已有闭合记录；不重复执行。 | 后续新存在类缺口按触发点登记。 |
| P8 | `计划/20260620_场景类逻辑纠偏计划_v0.1.md` | 场景结构、场景功能函数、非场景主信息操作和世界树场景辅助函数归属。 | 当前登记切片已有闭合记录；不重复执行。 | 后续新场景类缺口按触发点登记。 |
| P9 | `计划/20260619_因果类功能函数补完计划_v0.1.md` | 因果类功能函数补完主线；多次类级缺口的返回目标。 | P1-P6 已具备构建证据；外部绕过清理由横向审计线继续。 | 后续新因果类缺口按触发点登记。 |
| P10 | `计划/20260618_项目结构体审计与清理计划_v0.1.md` | 项目通用数据结构闭环横向审计。 | P10a/P10b/P10c/P10d/P10f-a 已闭合；P10f-c string 字段直接清理已到当前边界。 | 暂让位给 P11 无调用函数优先清理；后续再选任务协议瘦身 / 控制面板 DTO/快照 / D455 原始帧材料 / 服务安全结算结果壳 / 方法主信息能力面专项。 |
| P11 | `计划/20260618_项目功能函数去重与无效代码清理计划_v0.1.md` | 函数去重和无效代码清理质量线。 | 用户 2026-06-22 新口径已恢复并提升为当前主动队列；无调用函数优先直接删除，删后复扫直到无可删候选。 | 当前执行 P11h：删除过期旧文件，重建 live 无调用候选并批量清理。 |

## 3. 待条件具备后继续

### 3.0 扫描链路待复核与路线合并

入口：

```text
计划/待完成项/20260615_自我扫描当前场景完整实现与测试计划_v0.1.md
计划/待完成项/20260615_自我扫描当前场景本能方法重建计划_v0.1.md
```

恢复条件：

```text
1. 清理 任务模块.管理界面线程.impl.cpp 中外设供料等待项的兼容本能ID兜底路径；
2. 或真实 D455 / 自然任务链出现扫描供料和 6146 相关样本；
3. 若只恢复本能方法重建计划，必须先判定其与完整实现计划的关系，不直接改扫描实现。
```

### 3.1 子任务完成后父任务权重和活动集变化自然样本

入口：`计划/待完成项/20260514_特征值差距刷新成子需求闭环剧本计划_v0.1.md`

恢复条件：

```text
真实运行自然出现子任务完成后的父子回流闭合链：
子任务完成 / 目标结果符合
-> 自我线程收束已完成叶需求
-> 子需求退出活动集或阻塞父任务标记解除
-> 安全 / 服务权重归零或父级子权重刷新
-> 父任务收到重筹办意图或等待解除。
不得调用旧 `--subtask-parent-flow-check` 等回归入口，不得伪造父子任务样本。
```

### 3.2 D455 可比历史扫描变化样本

入口：`计划/待完成项/20260605_D455_P3可比历史扫描变化样本待完成项.md`

恢复条件：

```text
真实 D455 运行中自然形成同目标 / 同区域 / 可比较历史扫描材料，并能证明变化入账边界。
不得伪造扫描变化样本。
```

### 3.2b D455 存在生成不稳定自然复核

入口：`计划/待完成项/追根因_20260614_D455存在生成不稳定_v0.1.md`

恢复条件：

```text
真实 D455 外设自然业务样本再次出现稳定数据到存在生成闭合异常；
不得用伪造外设样本或旧日志替代当前复核。
```

### 3.3 学习量化自然样本复验

入口：`计划/待完成项/20260605_P5学习量化真实日志验证自然样本待完成项.md`

恢复条件：

```text
真实运行自然出现学习量化缺口、方法状态派生或结果能力样本。
不得用旧夹具、伪造入口或 `--*-check` 替代。
```

### 3.4 定向学习全局动作入口形成

入口：`计划/待完成项/20260605_定向学习全局动作入口形成待完成项.md`

恢复条件：

```text
自然出现明确学习缺口，且缺口能绑定到正式方法结构、执行入口或条件结果对。
```

### 3.5 主动能力与被动触发初始因果自然复验

入口：`计划/待完成项/20260604_主动能力与被动触发遗传因果落实计划_v0.1.md`

恢复条件：

```text
真实运行出现主动能力或被动触发初始因果样本，需要复核遗传因果链和因果解释证据。
```

### 3.6 自我场景绝对坐标 / 姿态 / 自我视图坐标系

入口：`计划/待完成项/20260602_自我初始场景坐标锚点与真实D455验证计划_v0.1.md`

恢复条件：

```text
真实 D455 或等价外设提供合法坐标 / 姿态材料，并能形成自我视图坐标系锚点。
```

### 3.7 双目相机外设实现线

入口：

```text
计划/待完成项/20260524_外设本能方法三分类与双目相机方法登记实施计划_v0.1.md
计划/待完成项/20260527_双目相机观察队列承接代码纠偏与实现计划_v0.1.md
计划/待完成项/20260527_双目相机观察像素簇接入实现方案_v0.1.md
计划/待完成项/20260527_外设自主观察线程稳定采样实施计划_v0.1.md
```

恢复条件：

```text
切入外设观察队列、像素簇、稳定采样或双目相机方法登记实现。
D455 仍只能作为观察材料生产者，不直接证明需求满足、安全增加或世界事实为真。
```

### 3.8 任务持续执行 / 同目标任务自然复核

入口：`计划/待完成项/20260608_任务持续执行与同目标任务去重纠偏计划_v0.1.md`

恢复条件：

```text
自然出现新强目标升级样本、共享任务成功 / 收束完成样本，或任务工作线程执行结果任务特征评价 / 有推进未达成 / 执行中样本。
```

### 3.9 相机四本能方法持续执行结果自然复核

入口：`计划/待完成项/20260608_相机四本能方法持续执行结果协议修订计划_v0.1.md`

恢复条件：

```text
6146 / 6147 自然命中扫描 / 跟踪执行样本，或出现 P8 所需任务结果评价样本。
```

### 3.10 外设四本能方法条件包 / P9 线

入口：`计划/待完成项/20260606_外设四本能方法任务真实运行条件包闭环计划_v0.1.md`

恢复条件：

```text
明确恢复 P9d 或真实 D455 条件包链；不得伪造外设报告、条件包或动作动态。
```

### 3.11 目标观察约束与相机链路修正线

入口：

```text
计划/待完成项/20260606_目标观察约束特征组实现计划_v0.2.md
计划/待完成项/20260607_相机外设观察链多帧唯一性扫描跟踪修正计划_v0.1.md
计划/待完成项/20260607_识别链路唯一性识别与提交分级修正计划_v0.1.md
计划/待完成项/20260607_扫描链路比较材料与变化入账边界修正计划_v0.1.md
计划/待完成项/20260607_跟踪链路观测材料与状态动态边界修正计划_v0.1.md
```

恢复条件：

```text
主动队列切到相机观察约束、识别、扫描或跟踪链路实现。
`20260606_目标观察约束特征组实现计划_v0.1.md` 已删除；v0.2 是唯一保留版本。
```

### 3.12 服务值 v1.1 自然运行验证

入口：`实施记录/20260610_服务值v1.1自然运行验证_Codex断点清单.md`

恢复条件：

```text
真实主循环运行足够久，至少跨过服务治理维护间隔；
或自然出现计划服务增量 / 成功服务候选；
或候选池中已有待复核候选记录。
```

恢复后验证关键字：

```text
服务治理周期维护
成功服务G0候选生成
成功服务G0饱和入账
服务值直接入账已关闭
定时衰减服务值
休眠候选
```

### 3.13 特征类 VecI 识别 / 扫描 / 跟踪真实轮廓样本

入口：`计划/待完成项/20260610_特征类VecI解释视图完善计划_v0.1.md`

恢复条件：

```text
真实 D455 运行中自然形成至少一条可比较轮廓样本：
识别：当前观察存在与已知存在两端已有平面轮廓或空间极值轮廓；
或扫描：当前观察存在与历史 / 基准观察存在两端已有平面轮廓或空间极值轮廓；
或跟踪：目标存在与上一轮 / 基准观察存在两端已有平面轮廓或空间极值轮廓。
恢复后复核日志中的 `VecI64轮廓比较特征数`、`VecI64轮廓可比较数`、`VecI64轮廓二次特征写入数`、`轮廓匹配分数`、`轮廓残差`，并核对二次特征能追到左右特征、来源场景和来源动态。
不得新增模拟样本、夹具入口、伪造动作动态或旧 `--*-check` 回归命令。
```

### 3.14 D455 扫描 / 跟踪新技术储备后续计划

入口：`计划/待完成项/20260611_D455扫描跟踪新技术储备后续计划_v0.1.md`

恢复条件：

```text
当前主动 `D455 稳定观察子集与轮廓跨帧稳定化实现计划` 至少完成 P0-P3；
真实 D455 运行中自然出现 L1 稳定观察子集；
任务管理观察事实承接请求已能来自 L1；
用户明确切入扫描或跟踪增强；
若要接入 RTAB-Map / ORB-SLAM3 / Open3D / OctoMap / ByteTrack / SAM 2 / Cutie 等第三方技术，先单独开隔离层设计切片。
```

边界：

```text
该计划只作技术储备，不是当前主动队列；
扫描 / 跟踪算法只能生成观察材料、变化候选、轨迹候选、质量缺口或可回查证据；
不把 SLAM 地图、TSDF 网格、占据图、tracklet、mask propagation 或检测框直接当世界真值；
不写需求满足、安全值或服务值。
```

### 3.15 创建子任务异常分支代码逻辑修订

入口：`计划/完成项/20260612_创建子任务异常分支代码逻辑修订计划_v0.1.md`

归档状态：

```text
P1-P5 代码层已闭合；P6 真实窗口命中父子目标自复制保护样本。
2026-06-13 真实自检窗口未复现字段缺失、稳定键重复、入树拒绝分类或父子目标自复制回潮。
按用户口径，真实环境中旧问题不再出现则默认已解决。
若后续真实日志再次复现，重新开追踪项并重点检查：
    任务管理工作线程/派生需求消息构造逻辑错误
    任务管理工作线程/派生需求消息幂等去重
    自我线程/派生需求入树原因分类
    自我线程/父子目标自复制保护
不得把字段缺失、父子目标自复制或边界字段误入普通需求处理成普通跳过、正常停等或自由文本机器决策。
```

### 3.16 闭环验证 / 价值结算自然样本复核

入口：`计划/待完成项/20260613_闭环验证与结算流程修正计划_v0.1.md`

恢复条件：

```text
普通价值任务、共享横向任务或 G0 服务确认路径自然出现；
或需要复验 VAL1 硬闸门、VX0/VC0 接口契约、结算域分类。
不得恢复旧的“执行报告即事实”路径。
```

### 3.17 因果生成 / 根因果 / 目标投影待条件集合

入口：

```text
计划/待完成项/20260617_因果信息生成三分口径修正计划_v0.1.md
计划/待完成项/20260601_006自我存在服务生存闭环规范落地计划_v0.2.md
计划/待完成项/20260601_安全因果链反推当前场景特征值需求纠偏计划_v0.2.md
计划/待完成项/20260601_因果条件按特征值判断纠偏计划_v0.1.md
计划/待完成项/20260601_自我关键特征负向变化定义因果纠偏计划_v0.1.md
计划/待完成项/20260603_硬编码路径迁移为遗传因果链实施计划_v0.1.md
```

恢复条件：

```text
当前主动队列切回因果生成三分口径、根因果、目标投影或遗传因果链；
或真实日志显示动作动态 / 状态变化 / 因果模板三者口径再次冲突。
恢复时必须先确认 006 根因果规范和 007 禁止性规范，不用文本规则名或日志摘要承载因果判断。
```

### 3.18 学习需求 / 方法入口 / 条件结果对待条件集合

入口：

```text
计划/待完成项/20260510_因果补完C4学习需求正式入树计划_v0.2.md
计划/待完成项/20260516_方法可执行入口真实动作来源规则计划_v0.1.md
计划/待完成项/20260520_尝试学习生成条件结果对与练习复用条件组纠错计划_v0.1.md
计划/待完成项/20260605_任务筹办OR路径闭合与学习拦截实现计划_v0.1.md
```

恢复条件：

```text
真实运行自然出现学习缺口四类之一：
无确定候选方法、候选方法不可用、明确缺执行入口、明确缺条件结果对；
或用户明确切回学习需求正式入树、方法动作入口、条件结果对生成。
不得把参数 / 条件补齐需求归入学习需求。
```

### 3.19 需求生命周期 / 需求树组织待条件集合

入口：

```text
计划/待完成项/20260523_需求生命周期通用函数与提交入口实施计划_v0.1.md
计划/待完成项/20260523_自我根特征抽象定义自检缺口需求链实施计划_v0.1.md
计划/待完成项/20260531_需求树逻辑组织语义实现计划_v0.1.md
计划/待完成项/20260531_自检问题因果投影入树逻辑修复计划_v0.1.md
计划/待完成项/20260527_观察质量低阈值与父需求裁决自然复验计划_v0.1.md
```

恢复条件：

```text
真实运行自然出现需求提交入口、父需求裁决、需求树组织、观察质量低阈值或自检因果投影入树异常；
恢复时先按当前硬规则确认：只有需求需要任务化，任务筹办 / 任务执行本身不建成子任务。
```

## 4. 归档维护口径

```text
1. 已完成计划查询：计划/完成项/。
2. 待自然样本、待真实外设业务样本或待用户明确切入计划查询：计划/待完成项/。
3. 根目录计划只保留当前主动计划或仍有直接恢复价值的顶层入口。
4. 历史归档流水账不再写入索引；需要复核时按计划文件名、实施记录和真实日志查证。
5. 旧版本被新版本替代且无独立恢复价值时，删除旧版本或移入完成项，并在对应计划 / 断点清单保留证据。
```
## 5. 证据闸门

所有计划恢复、完成或移动必须满足以下证据边界：

```text
1. 构建证据只能来自真实构建命令和退出码。
2. 行为证据只能来自真实运行日志、状态快照、动作动态、错误日志或控制面板真实输出。
3. 业务通过不得由旧日志、伪造样本、模拟入口、夹具测试或 `--*-check` 回归命令证明。
4. remote compact / stream disconnected / websocket / network error 只表示远程执行通道中断，不得记成项目实现、业务验证或计划逻辑失败。
5. 纯线程生命周期消息只能进入控制面板信息域；不能生成方法动作动态，不能进入自我治理邮箱。
6. 任务筹办、任务执行、任务管理后处理不作为本能方法注册、选择或动作动态来源。
7. 价值结算只能在叶子任务完成后由任务价值反馈链携带实际改变结果状态、完成度和任务自身权重调用；不得作为安全值 / 服务值根任务的普通筹办候选或当前执行方法。
8. `I64_MAX` 是异常哨兵或未设置值，不得进入真实服务结算。
```

## 6. 验证模板

每个新切片闭合时，在对应计划或实施记录中记录：

```text
切片编号：
目标：
依据文件：
允许修改范围：
禁止修改范围：
预测结果：
已改文件：
验证命令：
退出码：
关键日志窗口：
关键日志证据：
预测偏差：
当前判断：
下一步：
禁止宣称项：
```
