# 20260421 执行期前置任务接入步骤节点前置列表并保留定向学习挂接

## 完成内容
- 在 `D:\鱼巢\任务管理任务模块.impl.cpp` 新增了执行期前置任务登记 helper：
  - `私有_任务节点引用已存在(...)`
  - `私有_前置任务挂接步骤(...)`
  - `私有_登记步骤前置任务(...)`
- `补条件子任务` 现在优先以“当前步骤节点”为挂接父节点；如果复用了旧的宿主级子任务，也会回填到当前步骤的 `并发前置任务头结点列表 / 当前前置任务头结点`。
- `定向学习子任务` 继续保留原来的学习树挂接方式，但会把对应学习子任务登记到来源步骤的前置任务列表，避免打断现有学习树统计和学习步骤推进链。
- 学习资产正式入账时，如果输入已经带了 `来源步骤 + 学习子任务`，也会再次确保该学习子任务登记到步骤前置列表。

## 影响文件
- `D:\鱼巢\任务管理任务模块.impl.cpp`

## 结果
- 执行期发现的缺口任务，开始显式落入“步骤节点的前置任务列表”。
- 主步骤链与前置任务边的语义分离更清楚：
  - 主步骤继续走 `头 -> 步骤 -> 结果 -> 下一步`
  - 前置任务挂到步骤节点的 `并发前置任务头结点列表`

## 验证
- 已执行：
  - `msbuild D:\鱼巢\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /m /clp:ErrorsOnly`
- 结果：
  - 编译通过，`0` 错误
