## 完成事项

- 将 `任务管理任务模块` 中一批仍会回退读取旧上下文、旧状态、旧文本的消费口继续收窄。
- 本轮目标不是新增判断，而是让 `原语层 / 编排层` 更明确地只消费：
  - `任务/方法结构字段`
  - `任务治理二次特征证据`
  - 已形成的原语结果

## 本轮收口点

### 1. 原语执行继续去兼容回退

文件：
- `D:\鱼巢\任务管理任务模块.impl.cpp`

处理：
- `私有_P0_执行原语(...)`

调整后：
- `条件满足 / 方向一致 / 结果符合 / 冲突判定 / 因果稳定 / 可等待 / 已完成 / 需收束 / 可回接 / 可继续推进`
  这几类原语，改为优先只读 `请求.当前治理二次特征证据`。
- 当治理证据缺失时，不再回退去读：
  - `最近功能域`
  - `最近根层重判结果`
  - `最近执行前门控结果`
  - `影子验证状态` 文本
  - 宿主任务的旧目标/结果即时比较
- 改成保守返回：
  - `满足度=0`
  - `置信度=0`
  - `原因=证据缺失`
  - `建议=先生成对应治理证据`

这一步把原语层正式收成了“证据解释器”，而不是“证据缺了就自己猜”的兼容壳。

### 2. 等待/完成/命中当前治理的旧局部判断继续后撤

处理：
- `私有_可进入等待分支(...)`
- `私有_业务子任务执行已完成(...)`
- `私有_结果节点匹配当前治理(...)`

调整后：
- 三者统一改成只读 `结果.当前治理二次特征证据`
- 不再在这些消费口里自行重新判断：
  - 节点状态
  - 结果归属
  - 方法匹配
  - 名称/类型匹配

这些判断现在应由治理证据刷新阶段先完成，消费口只解释结果。

### 3. 学习触发中的“稳定等待复盘”改为结构/证据驱动

处理：
- `私有_稳定等待态复盘学习(...)`
- `私有_推导学习承接类型(...)`
- `私有_应创建学习子任务(...)`

调整后：
- `稳定等待态复盘学习` 不再直接吃一组裸参数组合推理；
- 改成只读：
  - `当前缺口类型`
  - `当前下一步去向`
  - `当前方法来源`
  - `当前步骤语义类型`
  - `当前治理二次特征证据.当前需等待`
  - `宿主目标结果待推进`
- `学习承接类型` 的推导也从“旧私有参数推导”改成“结构字段优先 + 结构/证据兜底”。

这一步把学习触发继续往“结构字段 / 证据”前移了一层。

### 4. 步骤结果子链继续削掉本地补判

处理：
- `私有_推进步骤结果子链(...)`

调整后：
- `有可回接最近结果`
- `最近结果命中当前治理`
- `当前步骤可复用`

这三类判断改成只读 `当前治理二次特征证据`，不再在子链推进时重新从节点结构临时计算。

同时：
- `当前步骤位类型`
- `当前步骤语义类型`

在进入子链时会优先吃现有正式结构字段，只有字段未定义时才继续走最小推导。

### 5. 步骤语义推导继续去掉对宿主节点即时读取

处理：
- `私有_推导步骤语义类型(...)`

调整后：
- 在 `执行位` 下是否判为 `业务推进步骤`，改成读 `结果.宿主目标结果待推进`
- 不再在这里直接读宿主节点即时双面状态

这一步继续把“步骤语义”从即时节点推理改成结果结构字段驱动。

## 当前收口后的口径

- 任务树/方法树：负责表达结构
- 二次特征：负责表达当前判断证据
- 原语：只解释这些证据
- 任务管理任务模块：只做编排

本轮完成后，这条口径在 `原语执行 / 等待完成判据 / 学习触发 / 步骤结果子链` 四个消费面上又更硬了一层。

## 验证

- 已执行：
  - `msbuild D:\鱼巢\鱼巢.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64 /m`
- 结果：
  - `0 error / 0 warning`

## 尚未覆盖

- 仍未做专门运行态回放验证。
- 治理证据刷新阶段本身仍会读取节点结构与上下文，这是当前允许的“证据生成层”；后续如果继续收口，应优先再清理剩余的 `业务子任务应转入执行位` 等局部旧推导。
