# 20260607 S8d D455 等待项索引与目标约束匹配定位方案 v0.1

## 1. 目标

```text
只读定位 D455 活跃等待项、按等待项匹配报告、目标观察约束读取和任务管理承接路径。
判断是否已经具备实现等待项索引 / 目标约束索引的真实性能证据。
```

## 2. 依据

```text
规范/规范目录.md
规范/外设独立控制线程与消息承接边界规范20260527.md
规范/相机外设综合工作流程规范20260607.md
计划/计划索引.md
计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
计划/20260607_S8a_D455外设性能专线只读定位方案_v0.1.md
计划/20260607_S8b_D455高频报告日志限流小方案_v0.1.md
计划/20260607_S8c_D455报告背压与无等待项入队限流方案_v0.1.md
日志/鱼巢_run_20260607.低值g
```

## 3. 代码路径

```text
外设观察报告队列.ixx：
    等待项集合当前为 vector；
    提交外设观察等待项 使用幂等键去重；
    完成外设观察等待项 按等待项ID erase；
    读取外设观察等待项集合 会在队列 mutex 下复制匹配等待项；
    按等待项匹配外设观察报告 会在队列 mutex 下倒序扫描报告队列；
    读取目标观察约束_按等待项 会倒序扫描目标观察约束缓冲区并做可用性判定。

任务模块.管理界面线程.impl.cpp：
    私有_巡检外设观察等待项并生成上行 每轮读取等待项集合，按上限处理；
    任务管理界面线程/外设承接后台线程 每 100ms 巡检；
    等待项创建前会检查方法供料要求和目标观察约束。

任务模块.管理工作线程.ixx：
    任务管理外设消息承接_处理一次 调用 按等待项匹配外设观察报告；
    匹配后做观察材料质量门控，生成观察事实承接请求；
    匹配成功后由任务管理界面线程完成等待项。

外设线程_D455深度相机.ixx：
    S8c 后每帧只读取一次本外设全部等待项；
    无等待项时逐簇识别报告和材料页已按周期保留；
    有等待项时仍保留当前帧报告和材料页。
```

## 4. 真实日志证据

```text
21:31-21:32 真实 D455 窗口：
    61731 行：按外设观察等待项启动D455线程，等待项ID=1，观察运行模式=陌生环境逐簇识别。
    61732 行：创建外设观察等待项，等待项ID=1。
    61757 行：D455 逐簇识别报告入队，报告ID=1，活跃等待项=1，D455等待项数=1。
    61759 行：任务管理外设消息承接生成观察事实承接请求，等待项ID=1，报告ID=1。
    61763 行：外设承接后台巡检完成，上行消息数=1。
    61786 行以后：D455等待项数=0，报告按无等待项背压策略保留。

同时存在大量：
    任务管理界面线程/外设观察等待项创建延后。

这些是等待项创建前置条件未满足，不是等待项集合堆积或匹配扫描慢。
```

## 5. 判断

```text
当前没有真实证据证明等待项集合数量、报告队列倒序扫描或目标观察约束缓冲区扫描已经成为性能瓶颈。
当前真实样本中活跃 D455 等待项数量为 1，且已在首个 D455 报告后被任务管理承接。
S8a / S8b / S8c 已处理当前可证明的 D455 常驻性能放大点：
    巡检所有者重复；
    高频长日志；
    无等待项报告 / 材料页逐帧入队。
```

## 6. 结论

```text
S8d 本轮只读闭合，不进入等待项索引代码实现。

恢复条件：
    真实日志出现 D455等待项数 持续大于 8；
    或 外设承接后台巡检完成 上行消息数长期低于等待项数量；
    或 按等待项匹配外设观察报告 / 读取目标观察约束_按等待项 出现明确阶段耗时日志并成为主瓶颈；
    或 用户明确要求优先实现等待项索引。

下一步：
    当前 D455 专线 P0 已完成可证明闭合；
    计划索引可转入 S6 / S7 基础信息、候选方法和因果主果索引恢复条件复核。
```
