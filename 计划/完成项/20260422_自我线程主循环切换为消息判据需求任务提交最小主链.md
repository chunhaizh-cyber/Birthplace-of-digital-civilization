# 20260422_自我线程主循环切换为消息判据需求任务提交最小主链

> 2026-05-22 验证口径修订：
> 后续所有计划的验证统一改为日志模块验证，以真实运行产生的日志、状态快照、动作动态与错误日志为证据。
> 严禁新增或依赖模拟测试、夹具测试、伪造样本、伪造入口、伪造动作动态或 `--*-check` 回归命令。
> 历史计划中已有的回归命令、夹具和模拟测试描述仅作为历史记录，不再作为当前执行或验收依据。

1. 自我线程主循环已停止调用旧的 `主派发判定二次特征 / 结果回流协调 / 结算学习故障收口` 运行步骤。
2. `步骤_生成并执行主派发决议_()` 已改成最小主链：
   - 读取当前主需求
   - 判断需求是否适合任务化
   - 缺任务时按需求直接创建任务头
   - 设置当前主任务
   - 生成线程裁决结果
   - 提交当前主任务到任务管理工作线程推进
   - 回收工作线程上行消息与推进事件
3. 旧的大型治理桥/联合裁决/回流结算逻辑已退出主路径，但暂未整块物理删除。
4. 本轮编译卡在 `D:\鱼巢\x64\Debug\鱼巢.exe` 被占用，待释放文件锁后重新链接验证。
