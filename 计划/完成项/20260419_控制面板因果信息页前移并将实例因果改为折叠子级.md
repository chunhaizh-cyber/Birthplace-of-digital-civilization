# 20260419 控制面板因果信息页前移并将实例因果改为折叠子级

> 2026-05-22 验证口径修订：
> 后续所有计划的验证统一改为日志模块验证，以真实运行产生的日志、状态快照、动作动态与错误日志为证据。
> 严禁新增或依赖模拟测试、夹具测试、伪造样本、伪造入口、伪造动作动态或 `--*-check` 回归命令。
> 历史计划中已有的回归命令、夹具和模拟测试描述仅作为历史记录，不再作为当前执行或验收依据。

## 本次完成

- 将 `因果信息` 菜单前移到 `先天动作` 之前，让因果观察面优先于专项动作观察面出现。
- 调整 `因果信息` 页面显示顺序：
  - 主显示区优先展示 `抽象因果模板`
  - `实例因果` 改成其下级折叠区，需要点击展开后查看
- `实例因果` 折叠区显示上限改为 `50` 条，避免页面一次性铺开过多实例证据。

## 当前效果

- 左侧菜单里，`因果信息` 会比 `先天动作` 更靠前。
- 点击 `因果信息` 后，右侧会先看到抽象因果模板；
- `实例因果` 作为下级证据放在折叠区里，展开后最多显示 50 条。

## 验证

- 本轮仅做编译级验证，未做运行态点击验证。
