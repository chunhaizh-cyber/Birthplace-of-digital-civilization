# 20260419 控制面板新增因果信息菜单并显示全部因果列表

> 2026-05-22 验证口径修订：
> 后续所有计划的验证统一改为日志模块验证，以真实运行产生的日志、状态快照、动作动态与错误日志为证据。
> 严禁新增或依赖模拟测试、夹具测试、伪造样本、伪造入口、伪造动作动态或 `--*-check` 回归命令。
> 历史计划中已有的回归命令、夹具和模拟测试描述仅作为历史记录，不再作为当前执行或验收依据。

## 本次完成

- 在控制面板左侧菜单中新增 `因果信息` 菜单，位置放在 `先天动作` 后面，作为动态相关观察面的下一层入口。
- 在 `结构_控制面板快照` 中新增 `因果实例列表` 与 `因果模板列表`，用于承载全部实例因果和全部抽象因果模板。
- `读取控制面板快照(...)` 现在会正式收集：
  - 全部 `因果实例`
  - 全部 `因果模板`
  - 原有 `先天动作因果` 继续保留为专项观察面
- 右侧内容区新增 `因果信息` 页面，分两栏显示：
  - `实例因果`
  - `抽象因果模板`
- 指针替换、自检预览、文本导出也同步接入了这两组新列表。

## 当前效果

- 控制面板里现在可以直接查看“所有因果信息”，不再只限于 `先天动作因果`。
- 菜单与右侧页面都能同时看见：
  - 全量实例因果
  - 全量抽象因果模板

## 验证

- 本轮仅做编译级验证，未做运行态点击回放验证。
