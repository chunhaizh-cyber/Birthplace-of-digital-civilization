# 20260419 阶段4 原语执行改为原语ID权威并降级本能兼容

## 本步完成

- 让 `任务管理最小原语` 的执行入口改成：
  - `原语ID` 为权威
  - `本能ID` 仅作为兼容映射和校验来源
- 进一步削弱 `任务管理任务` 对 `本能元信息` 的硬依赖。

## 主要改动

- 更新 `任务管理最小原语模块.cpp`
  - 执行入口不再要求 `本能ID` 必填
  - 当仅提供 `原语ID` 时，允许直接执行
  - 仅在显式提供 `本能ID` 时校验映射一致性
- 更新 `任务管理任务模块.impl.cpp`
  - `私有_任务管理最小原语已正式注册(...)` 改成优先按原语对应的兼容方法头存在性判断
  - 去掉对 `本能方法元信息` 的硬门槛依赖

## 收口后的口径

- `原语`：面向任务虚拟存在的最小内部方法
- `本能函数`：面向世界树的最小外部方法
- 当前实现仍保留原语到本能方法头的兼容映射，但执行权已经优先落在 `原语ID`

## 验证

- `msbuild D:\鱼巢\鱼巢.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64 /m`
- 结果：`0 error / 0 warning`
