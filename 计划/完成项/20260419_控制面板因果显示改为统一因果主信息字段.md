## 本次完成

- 将 [D:\鱼巢\控制面板类.cpp](/D:/鱼巢/控制面板类.cpp) 中仍依赖旧 `因果实例主信息类` 字段的显示与引用判断逻辑，改为统一读取 `因果主信息类` 的新字段：
  - `条件状态模板`
  - `动作模板`
  - `动作语义键`
  - `状态迁移模板`
  - `主状态迁移`
  - `证据动态样本`
  - `适用锚点类型`
  - `适用层级`
  - `来源类型`
  - `置信度`
  - `已验证`
  - `因果签名`

- 修正了以下控制面板因果相关逻辑：
  - 历史宿主残留检查不再读取旧的 `条件状态 / 结果状态 / 主结果状态`，改为检查 `条件状态模板 + 状态迁移模板 + 证据动态样本`
  - 实例因果引用状态判断改为基于 `条件状态模板 + 状态迁移模板`
  - 实例因果条件/动作/结果/结果方向摘要改为统一走 `条件状态模板 + 动作模板 + 主状态迁移/状态迁移模板`
  - 实例因果树节点字段改为与统一因果主信息一致
  - 状态迁移引用状态的实现从旧的“宿主比对”改为“节点/主键比对”

- 补了控制面板内部前置声明，保证统一因果辅助函数在较早的调用点也能被正常引用。

## 结果

- 控制面板因果显示链已经不再依赖已清理掉的旧实例因果字段。
- `因果主信息统一 + 动态直达因果` 这一轮在显示层也完成了收口。

## 验证

- 运行：

```powershell
msbuild D:\鱼巢\鱼巢.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64 /m
```

- 结果：
  - `0 error`
  - `0 warning`

## 备注

- 构建日志中 `pwsh.exe` 的 `9009` 来自 `vcpkg applocal` 的辅助脚本调用，不影响本次工程编译成功与产物链接完成。
