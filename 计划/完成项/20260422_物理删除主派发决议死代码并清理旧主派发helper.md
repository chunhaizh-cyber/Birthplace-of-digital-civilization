# 2026-04-22 物理删除主派发决议死代码并清理旧主派发 helper

## 完成内容
- 将 [D:\鱼巢\自我线程模块.impl.cpp](D:/鱼巢/自我线程模块.impl.cpp) 中 `步骤_生成并执行主派发决议_` 的 `return` 之后整段旧死代码彻底裁掉，只保留当前生效的最小主派发主链。
- 将主派发和需求整理摘要中的任务标题显示改成稳定文本 `已选任务头`，避免再次因为旧语素指针解引用导致崩溃。
- 物理删除同文件中只服务旧主派发长链的 helper，包括：
  - 旧主派发判定二次特征读取结构与函数
  - 旧主派发判据原语结构与计算函数
  - 旧主派发路由原语结构与计算函数

## 当前保留的主派发流程
- 选择当前主需求
- 选择或创建当前主任务
- 判断是否适合任务化
- 提交任务管理工作线程推进
- 吸收工作线程返回结果
- 生成最小主派发快照

## 验证
- 已执行：

```powershell
msbuild D:\鱼巢\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:TargetName=鱼巢_tmp /m /clp:ErrorsOnly
```

- 结果：通过
