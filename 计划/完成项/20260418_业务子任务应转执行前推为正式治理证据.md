## 完成事项

- 将 `业务子任务应转入执行位` 从 `任务管理任务模块.impl.cpp` 里的局部节点关系推理，前推为正式的 `任务治理二次特征证据`。
- 让步骤结果子链、运行时方法证据沉淀、后续编排都统一消费这份证据，而不是各处再重复判断一次。

## 本轮代码落点

### 1. 正式证据字段

文件：
- `D:\鱼巢\任务管理任务模块.ixx`

新增字段：
- `结构_任务治理二次特征证据::业务子任务应转执行`

这意味着“业务子任务当前是否应从回接位切入执行位”已经有了正式证据位，不再只是局部 helper 的私有结论。

### 2. 覆盖与透传

文件：
- `D:\鱼巢\任务管理任务模块.impl.cpp`

更新：
- `私有_P1_覆盖任务治理二次特征证据(...)`

现在合并治理证据时，会把 `业务子任务应转执行` 一并透传。

### 3. 证据生成前推

文件：
- `D:\鱼巢\任务管理任务模块.impl.cpp`

在 `私有_P1_刷新任务治理二次特征证据(...)` 中：
- 将旧的“业务子任务是否应转执行”判断逻辑前推到证据生成层。
- 生成后写入：
  - `证据.业务子任务应转执行`
- 同时写入：
  - 摘要片段 `业务子任务应转执行`
  - 场景二次特征 `业务子任务应转执行`

这样“节点关系 -> 二次特征证据 -> 原语/编排消费”这条链正式立住了。

### 4. 消费口改为只读证据

文件：
- `D:\鱼巢\任务管理任务模块.impl.cpp`

更新：
- `私有_业务子任务应转入执行位(...)`

调整后：
- 不再重新读取：
  - 当前方法
  - 当前缺口/下一步去向
  - 最近结果和最近步骤
  - 业务回接位类型
- 改成只读：
  - `结果.当前治理二次特征证据.业务子任务应转执行`

### 5. 运行时方法证据沉淀同步接入

文件：
- `D:\鱼巢\任务管理任务模块.impl.cpp`

在 `私有_P2_补齐运行方法证据索引(...)` 中新增：
- 条件证据：`业务子任务应转执行`
- 结果证据：`业务子任务应转执行`

这样当前运行方法的条件节点/结果节点，也能沉淀这一条结构化证据。

## 当前收口后的意义

这一步把原先还比较像“步骤结果子链内部局部规则”的一条判断，正式推进到了：

- 任务树/方法树：表达结构
- 二次特征：表达当前判断证据
- 原语/编排：消费证据

也就是继续逼近：

`任务管理任务模块 只做编排`

而不是继续在子链推进时临时推导“该不该转执行位”。

## 验证

- 已执行：
  - `msbuild D:\鱼巢\鱼巢.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64 /m`
- 结果：
  - `0 error / 0 warning`

## 尚未覆盖

- 运行态回放尚未做。
- 当前仍有少量旧局部推理留在治理证据生成层本身；后续如果继续收口，应优先继续清理：
  - `当前动作长时间无推进`
  - `当前动作需重试`
  - `当前步骤可复用`
  - `最近结果命中当前治理`

这些都可以继续往“更稳定的结构字段 + 更清晰的证据生成职责”推进。
