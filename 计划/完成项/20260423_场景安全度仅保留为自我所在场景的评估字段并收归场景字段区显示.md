# 2026-04-23 场景安全度仅保留为自我所在场景的评估字段并收归场景字段区显示

## 完成内容
- 修正了 [D:\鱼巢\场景类.cpp](D:/鱼巢/场景类.cpp) 中场景默认特征的语义：
  - 不再给所有场景统一补 `安全度`
  - 只给 `自我所在场景 / 自我初始场景（现实世界）` 补该评估特征
  - 特征类型名从 `安全度` 收正为 `场景安全度`
- 为兼容旧节点类型：
  - 若旧场景下已有类型为 `安全度` 的场景特征节点
  - 会在命中时转正为 `场景安全度`
- 修正了 [D:\鱼巢\控制面板类.cpp](D:/鱼巢/控制面板类.cpp) 的世界树显示：
  - 场景的直接子特征节点不再作为结构子节点平铺显示
  - 会收归到场景节点下的 `字段 | 场景特征字段`
  - 双击该字段后，继续展开其特征节点原始字段

## 验证
- 已执行：

```powershell
msbuild D:\鱼巢\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:TargetName=鱼巢_tmp_worldtree_0423c /m /clp:ErrorsOnly
& 'D:\鱼巢\x64\Debug\鱼巢_tmp_worldtree_0423c.exe' --panel-html
```

- 结果：
  - 编译通过
  - 控制面板 HTML 导出通过

## 产物
- 控制面板 HTML 已更新：
  - [D:\鱼巢\控制面板.html](D:/鱼巢/控制面板.html)
