# 状态类逻辑纠偏计划 v0.1

日期：2026-06-20

状态：由因果类外部数据读取纠偏触发；第一优先只读函数已闭合；2026-06-20 已闭合 1.1 状态创建事件枚举与内部参数结构清理、1.2 状态清理问题快照结构化纠偏。2026-06-20 由特征类 1.2 审查新增“读取状态特征和值”只读函数缺口，按跨类缺口转向规则临时提升为第一优先。

## 0. 目标

补齐状态类只读功能函数，让因果类不再直接解释 `状态节点主信息类` 字段；同时登记状态类剩余结构体清理 / 纠偏缺口，后续按计划索引自动闭环调度执行。

当前触发缺口：

```text
因果类.cpp 直接读取：
    状态节点主信息类::状态主体
    状态节点主信息类::状态特征
```

## 1. 第一优先缺口

建议函数：

```cpp
状态节点类* 按主键解析状态节点(const std::string& 主键) const noexcept;

bool 读取状态主体与特征(
    const 状态节点类* 状态,
    基础信息节点类*& 状态主体,
    特征节点类*& 状态特征) const noexcept;

特征节点类* 读取状态特征(const 状态节点类* 状态) const noexcept;
基础信息节点类* 读取状态主体(const 状态节点类* 状态) const noexcept;
```

实现边界：

```text
1. 函数归属 状态类.h/.cpp。
2. 不新增状态主信息字段。
3. 不创建状态，不改状态值，只读解析。
4. 主键解析不允许隐式创建节点。
```

## 1.1 已登记待执行缺口：状态创建事件枚举与内部参数结构清理

触发点：

```text
状态类剩余结构体必要性审查发现：
    枚举_存在状态事件 只作为创建 / 变化入口提示，未写入 状态节点主信息类。
    状态创建参数 只在 状态类.cpp 内部临时打包参数，没有外部构造调用。
    状态节点事实已经由 场景 + 状态主体 + 状态特征 + 状态值 + 发生 / 收到时间 表达。
```

处理目标：

```text
1. 删除 枚举_存在状态事件。
2. 删除 状态创建参数。
3. 删除 创建状态(场景, const 状态创建参数&, 后处理) 和 记录状态到场景(场景, const 状态创建参数&, 后处理) 这类内部 DTO 入口。
4. 清理 创建内部状态 / 记录内部状态到场景 / 创建世界状态 / 记录世界状态到场景 / 记录内部特征状态 的 事件 参数。
5. 保留状态节点权威事实字段：状态主体、状态特征、状态值、发生时间、收到时间、对应信息节点。
```

执行边界：

```text
1. 本缺口当前只登记，不提升为计划索引当前主动计划。
2. 删除时不得新增兼容枚举、兼容参数包或旧入口适配层。
3. `是否变化` 仍按既有口径视为创建提示 / 显示缓存，不得提升为状态变化权威事实。
4. 状态变化权威判断仍由动态初始 / 结果状态或二次特征比较得到。
5. 若删除公开函数参数导致调用方缺少更明确入口，优先拆成状态类命名函数，不新增平行结构体。
```

验收命令：

```powershell
rg -n "枚举_存在状态事件|状态创建参数|创建状态\\([^\\n]*状态创建参数|记录状态到场景\\([^\\n]*状态创建参数" -g "*.h" -g "*.cpp" -g "*.ixx"
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

执行结果（2026-06-20）：

```text
1. 已删除 枚举_存在状态事件。
2. 已删除 状态创建参数。
3. 已删除 创建状态 / 记录状态到场景 的 状态创建参数 内部 DTO 入口。
4. 创建内部状态 / 记录内部状态到场景 / 创建世界状态 / 记录世界状态到场景 / 记录内部特征状态 已删除 事件 参数。
5. 调用方不再传入 创建 / 变化 事件枚举；状态节点仍由 场景、状态主体、状态特征、状态值、发生时间、收到时间、对应信息节点、是否变化 承载。
```

验证结果（2026-06-20）：

```text
rg 验收命令：退出码 1；无命中。
python .\tools\生成函数功能说明文档.py：退出码 0；函数条目 6877。
Debug x64 构建：日志/20260620_状态创建事件枚举清理_msbuild.log；退出码 0；0 个警告；0 个错误。
```

## 1.2 已登记待执行缺口：状态清理问题快照结构化纠偏

触发点：

```text
结构_状态清理问题快照 当前由 状态类 在活动状态索引超限且无法清理时写入。
自检线程会读取该快照并提交“状态清理阻塞”报告，因此它不是单纯显示结构。
但当前结构包含 场景主键 和 摘要 字符串，摘要还会进入自检候选说明，存在文本承载机器链路风险。
```

处理目标：

```text
1. 暂不裸删 结构_状态清理问题快照，因为自检线程仍在读取它。
2. 先把快照收束为结构化事实：场景节点引用或可解析引用、活动状态数量、活动状态上限、目标清理数量、已清理数量、被引用状态数量、记录时间。
3. 删除或下沉 摘要 / 场景主键 这类显示文本；显示层需要时临时从结构化字段生成。
4. 自检线程提交状态清理阻塞时，应读取结构化字段构造去重键和候选事实，不把日志摘要作为机器依据。
```

执行边界：

```text
1. 本缺口当前只登记，不提升为计划索引当前主动计划。
2. 若需要调整自检线程承接入口，先按跨类缺口转向规则登记到自检线程对应计划，不在状态类里临时拼文本。
3. 不新增 JSON、自由文本字段或显示摘要字段作为系统逻辑承载。
4. 闭合前不得宣称状态清理阻塞自检链已结构化闭环。
```

验收命令：

```powershell
rg -n "快照\\.摘要|快照\\.场景主键|场景指针" 状态类.h 状态类.cpp 自检线程模块.impl.cpp
rg -n "结构_状态清理问题快照|读取最近清理问题快照|清除最近清理问题快照|状态清理阻塞|快照\\.场景|快照\\.活动状态数量|快照\\.被引用状态数量" 状态类.h 状态类.cpp 自检线程模块.impl.cpp
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

执行结果（2026-06-20）：

```text
1. 结构_状态清理问题快照 已用 可解析引用<场景节点类> 场景 替换 场景指针 / 场景主键。
2. 结构_状态清理问题快照 已删除 摘要 字段。
3. 状态类清理失败时只写结构化快照；运行警告日志即时生成，不进入快照。
4. 自检线程状态清理阻塞提交从结构化字段构造去重键和显示证据，不再读取 快照.摘要。
```

验证结果（2026-06-20）：

```text
旧字段 rg：退出码 1；快照.摘要 / 快照.场景主键 / 场景指针 无命中。
结构化字段 rg：退出码 0；命中为结构体定义、状态类写入、自检线程读取和清除点。
python .\tools\生成函数功能说明文档.py：退出码 0；函数条目 6877。
Debug x64 构建：日志/20260620_状态清理快照结构化_msbuild.log；退出码 0；0 个警告；0 个错误。
```

## 1.3 新增第一优先缺口：读取状态特征和值

触发点：

```text
计划/20260620_特征类逻辑纠偏计划_v0.1.md 的 1.2 审查发现：
    特征类::比较状态 直接读取 状态节点主信息类::状态特征 和 状态节点主信息类::状态值。

状态类当前已有：
    读取状态主体与特征
    读取状态特征
    读取状态主体

但缺少：
    同时读取 状态特征 和 状态值 的只读函数。
```

建议函数：

```cpp
bool 读取状态特征和值(
    const 状态节点类* 状态,
    特征节点类*& 状态特征,
    特征值& 状态值) const noexcept;
```

实现边界：

```text
1. 函数归属 状态类.h/.cpp。
2. 不新增状态主信息字段。
3. 不创建状态，不修改状态值，只读解析。
4. 状态值按 特征值 结构复制输出，不返回主信息指针。
5. 本切片闭合后返回 特征类 1.2，用该函数替换 特征类::比较状态 的 状态节点主信息类 直读。
```

验收命令：

```powershell
rg -n "读取状态特征和值|状态节点主信息类|状态特征|状态值" 状态类.h 状态类.cpp
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

执行结果（2026-06-20）：

```text
1. 已新增 状态类::读取状态特征和值。
2. 函数只读复制 状态特征.指针 和 状态值，不创建状态，不修改状态值，不返回主信息指针。
3. python .\tools\生成函数功能说明文档.py 退出码 0，函数条目 6878。
4. Debug x64 构建退出码 0，日志 日志/20260620_状态类读取状态特征和值_msbuild.log 显示 0 个警告、0 个错误。
证据：实施记录/20260620_状态类读取状态特征和值_Codex断点清单.md。

返回点：
    计划/20260620_特征类逻辑纠偏计划_v0.1.md 的 1.2，替换 特征类::比较状态 的 状态节点主信息类 直读。
```

## 1.4 新增第一优先缺口：读取状态排序时间

触发点：

```text
计划/20260620_场景类逻辑纠偏计划_v0.1.md 的 1.3 P4.3 审查发现：
    场景类::追加状态 在排序场景状态索引时直接读取 状态节点主信息类::发生时间 / 收到时间。

状态类当前已有私有排序时间辅助：
    状态类::私有_取状态排序时间(const 状态节点主信息类&)

但缺少：
    面向调用方的 状态节点类* 只读排序时间函数。
```

建议函数：

```cpp
时间戳 读取状态排序时间(const 状态节点类* 状态) const noexcept;
```

实现边界：

```text
1. 函数归属 状态类.h/.cpp。
2. 不新增状态主信息字段。
3. 不创建状态，不修改状态值，只读返回排序时间。
4. 状态不存在或主信息不存在时返回 0，与现有场景排序缺省行为一致。
5. 本切片闭合后返回 场景类 1.3 P4.3，用该函数替换 场景类::追加状态 的 状态节点主信息类 直读。
```

验收命令：

```powershell
rg -n "读取状态排序时间|状态节点主信息类|私有_取状态排序时间" 状态类.h 状态类.cpp
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

执行结果（2026-06-20）：

```text
1. 已新增 状态类::读取状态排序时间(const 状态节点类*) const noexcept。
2. 函数只读解析状态主信息，返回 发生时间 非 0 优先，否则返回 收到时间；状态为空或主信息不存在时返回 0。
3. python .\tools\生成函数功能说明文档.py 退出码 0，函数条目 6896。
4. Debug x64 构建退出码 0，日志 日志/20260620_状态排序时间读取函数_msbuild.log 显示 0 个错误。
证据：实施记录/20260620_状态排序时间读取函数_Codex断点清单.md。

返回点：
    计划/20260620_场景类逻辑纠偏计划_v0.1.md 的 1.3 P4.3，用该函数替换 场景类::追加状态 的 状态节点主信息类 直读。
```

## 1.5 新增第一优先缺口：读取状态主体特征和值引用

触发点：

```text
计划/20260620_需求类逻辑纠偏计划_v0.1.md P7 首轮分类发现：
    需求类.h 中 生成需求目标键_已加锁 / 从状态补齐目标主体和特征缓存_已加锁
    需求类.cpp 中 生成任务初始化上下文
    直接读取 状态节点主信息类::状态主体 / 状态特征 / 状态值。

状态类当前已有 pointer 级读取函数：
    读取状态主体与特征
    读取状态特征
    读取状态主体
    读取状态特征和值

但缺少：
    保留 可解析引用 主键回退的状态主体 / 状态特征 / 状态值只读入口。
```

建议函数：

```cpp
static bool 读取状态主体特征和值引用(
    const 状态节点类* 状态,
    可解析引用<基础信息节点类>& 状态主体,
    可解析引用<特征节点类>& 状态特征,
    特征值& 状态值) noexcept;
```

实现边界：

```text
1. 函数归属 状态类.h/.cpp。
2. 不新增状态主信息字段。
3. 不创建状态，不修改状态值，只读复制结构化引用和值。
4. 保留 状态主体 / 状态特征 的指针和主键，不丢失未解析主键。
5. 闭合后返回 需求类 P7a，用该函数替换需求类中状态主信息直接读取。
```

验收命令：

```powershell
rg -n "读取状态主体特征和值引用|状态主信息_已加锁|状态节点主信息类" 状态类.h 状态类.cpp 需求类.h 需求类.cpp
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

执行结果（2026-06-21）：

```text
1. 已新增 状态类::读取状态主体特征和值引用。
2. 函数只读复制 状态主体 / 状态特征 的可解析引用和 状态值，不创建状态，不修改状态值。
3. 需求类 P7a 已使用该函数回迁目标键、状态补齐和任务初始化上下文中的状态材料读取。
4. python .\tools\生成函数功能说明文档.py 退出码 0，函数条目 7161。
5. Debug x64 构建退出码 0，日志 日志/20260621_P7a_状态特征目标材料读取_build.log 显示“已成功生成”。
```

返回点：

```text
计划/20260620_需求类逻辑纠偏计划_v0.1.md 的 P7，继续处理二次特征方向签名跨类读取。
```

## 2. 跨类缺口转向规则

执行本计划时，如果发现当前切片必须依赖其它类新增或补齐功能函数，按以下规则处理：

```text
1. 后续任意类缺少当前切片所需功能函数时，不在状态类或调用方临时拼字段、解释其它类主信息或新增平行结构体。
2. 若对应类已有逻辑纠偏计划，则把当前缺口加入该计划，并放到第一优先位置。
3. 若对应类没有逻辑纠偏计划，则生成 `计划/YYYYMMDD_<类名>逻辑纠偏计划_v0.1.md`，并把当前缺口作为第一优先切片。
4. 新生成或被修改的逻辑纠偏计划必须包含本“跨类缺口转向规则”，保证后续继续发现缺口时仍按同一调度口径处理。
5. 更新 `计划/计划索引.md`：把新生成或新修改的计划临时提升为第一优先计划；原计划和原切片记录为返回点。
6. 只执行新计划的第一优先缺口切片，不顺带推进新计划后续候选项。
7. 第一优先缺口闭合后，立即停止该新计划，更新计划索引返回原计划和原切片。
8. 若第一优先缺口切片不能闭合，保留该计划为当前阻塞，不回到原计划继续补丁化绕过。
```

## 3. 验收

```powershell
rg -n "状态节点主信息类|状态主体|状态特征" 因果类.cpp
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

## 4. 禁止宣称

```text
不把当前登记切片闭合扩大为全部结构体清理完成、全部状态类逻辑纠偏完成或内部循环完成。
```

## 5. 第一优先执行结果

复核结果：

```text
1. 状态类.h / 状态类.cpp 已存在第一优先要求的只读函数：
   - 状态类::按主键解析状态节点
   - 状态类::读取状态主体与特征
   - 状态类::读取状态特征
   - 状态类::读取状态主体
2. 因果类.cpp 已通过这些状态类函数读取状态主体 / 状态特征，不再直接解释 状态节点主信息类 字段。
3. 本切片无需新增代码。
```

验证结果：

```text
函数说明文档命中：状态类::按主键解析状态节点、状态类::读取状态主体与特征、状态类::读取状态特征、状态类::读取状态主体。
rg -n "状态节点主信息类|状态主体|状态特征" 因果类.cpp 仍有命中，但均为状态类函数调用后的局部变量名或函数名；无 状态节点主信息类 直接命中。
Debug x64 构建沿用 20260620 因果 P6 构建证据：退出码 0；0 个警告，0 个错误。
```

返回关系：

```text
状态类第一优先缺口已闭合。
状态类 1.1 / 1.2 已闭合。
本计划当前登记切片已闭合；后续新发现状态类缺口按计划索引重新登记，不从本结论外推全部状态类逻辑纠偏完成。
```
