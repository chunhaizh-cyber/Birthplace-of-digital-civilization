## 本次完成

- 在 `D:\鱼巢\因果类.cpp` 的 `私有_同步抽象因果信息(...)` 中，补上了实例因果升格为抽象因果时的模板写回。
- 新增了“实例因果所属场景解析 / 状态引用命中 / 实例结果证据识别 / 实例条件证据识别 / 二次特征模板泛化写回”这组内部 helper。
- 当前抽象因果会优先从实例因果所在场景里抽取：
  - 挂在该实例条件状态上的前置二次特征，写入 `条件模板`
  - 由该实例前后状态生成出来的结果二次特征，写入 `结果模板`
- 写入模板前会先做一次“去实例化”：
  - 清空 `来源起始状态 / 来源结果状态 / 来源动态 / 来源场景 / 时间段 / 左右对象`
  - 刷新 `度量签名_链键`
  - 然后把模板二次特征挂到抽象因果节点下面

## 目的

- 修复“抽象因果里 `条件模板/结果模板` 永远为空”的根因。
- 避免继续把抽象因果只当成“动作模板 + 证据桶 + 命中统计”。
- 让控制面板里显示的模板字段开始对应真实写回链，而不是永远显示 `无`。

## 当前边界

- `结果模板` 优先从场景里已经存在的结果二次特征抽取；如果当前场景还没有这类结果二次特征，则回退为“由条件状态与主结果状态生成状态比较二次特征”作为兜底模板。
- `条件模板` 当前只从场景里已有的前置二次特征抽取，不会凭空捏造单状态条件模板。
- 这一步没有改控制面板显示逻辑，修的是因果模板生成链本身。

## 验证

- 已执行：
  - `msbuild D:\鱼巢\鱼巢.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64 /m`
- 结果：
  - `0 error / 0 warning`

## 后续建议

- 继续做一条运行态回放，确认控制面板中的抽象因果节点下，`条件模板/结果模板` 已经不再长期为空。
- 如果后面发现某些抽象因果仍然只有结果模板没有条件模板，再单独补“单状态条件如何稳定抽象为条件二次特征”的专门口径。
