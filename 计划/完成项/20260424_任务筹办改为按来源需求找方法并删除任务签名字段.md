# 完成项：任务筹办改为按来源需求找方法并删除任务签名字段

时间：2026-04-24

## 本次完成

1. 删除任务主信息中的 `目标维度签名` 及其控制面板展示，不再在任务信息里保留“签名”派生字段。
2. `任务筹办` 本能函数不再自循环补“任务筹办方法头”，改为直接按 `任务.来源需求` 的目标特征类型在方法树中查找目标方法。
3. 若方法树中不存在对应方法头，则按目标特征类型建立最小需求位方法头，并把它作为学习对象挂回任务。
4. `任务执行一步` 本能函数不再按步骤名称临时创建执行方法头，而是直接读取任务当前已选目标方法并执行一步。
5. 原来那条“按步骤名创建执行方法头”的旧辅助逻辑已经删除，避免继续把步骤名误当方法来源。

## 当前主线

- 任务目标：直接以 `来源需求` 为准。
- 任务筹办：根据 `来源需求 -> 需求状态/被需求当前状态` 提取目标特征类型，再到方法树中查找匹配方法。
- 任务执行一步：执行任务当前已选中的目标方法；如果方法还不具备最小可执行骨架，则转学习补齐。

## 影响文件

- `D:\鱼巢\自我初始化模块.cpp`
- `D:\鱼巢\任务主信息类.h`
- `D:\鱼巢\任务类.cpp`
- `D:\鱼巢\本能方法类.cpp`
- `D:\鱼巢\控制面板类.cpp`

## 说明

这次没有继续沿旧的“任务上下文镜像”主链扩改，而是先把最底层两条本能函数的语义收正。后续如果继续清旧治理壳，应以这条新主线为准往上收口。
