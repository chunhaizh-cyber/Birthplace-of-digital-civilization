# 20260419 规范收口任务管理界面线程与提交承接边界

## 本次目标

按最新口径收口以下边界：

- 自我线程负责根据需求与根层裁决装配 `任务管理上下文` 和 `任务管理请求`
- 任务管理实例/工作线程负责承接宿主任务的一步治理推进
- 新增 `任务管理界面线程`，但它只负责观察与控制意图输入，不持有推进权

## 本次修改

### 1. 更新主链描述

修改了 [0010_自我实现逻辑与运行闭环规范.md](D:\鱼巢\规范\0010_自我实现逻辑与运行闭环规范.md)：

- 将“提交任务管理任务”改成“生成任务管理请求并提交给对应任务管理实例”
- 将主链改写为：
  `需求治理 -> 任务管理上下文 -> 任务管理请求 -> 任务管理实例一步治理 -> 一步治理结果 -> 外层再重判`
- 增补了 `任务管理界面线程` 只观察和发控制意图、不持推进权的边界

### 2. 更新任务管理任务职责边界

修改了 [1115_任务管理任务最小职责表规范.md](D:\鱼巢\规范\1115_任务管理任务最小职责表规范.md)：

- 补充了 `任务管理界面线程` 不是推进者的正式结论
- 明确只有 `任务管理实例/工作线程` 持有推进权
- 在统一主链位置中补充：
  `任务管理界面线程只观察和发控制意图，不直接推进任务树`

### 3. 更新线程池运行模型

修改了 [1205_任务管理任务线程池运行模型规范.md](D:\鱼巢\规范\1205_任务管理任务线程池运行模型规范.md)：

- 在目标问题中加入“界面线程的位置和推进权”
- 在核心结论中增加：
  - 允许存在 `任务管理界面线程`
  - 界面线程不持有推进权
- 在统一主链中补充界面线程旁路观察链
- 新增 `4.4 任务管理界面线程` 小节，定义其职责、禁止越权项和读取快照口径

### 4. 更新运行承载三层边界

修改了 [1219_任务管理实例堆与恢复快照规范.md](D:\鱼巢\规范\1219_任务管理实例堆与恢复快照规范.md)：

- 明确 `任务管理界面线程` 不属于 `实例堆 / 宿主任务虚拟存在 / 恢复快照` 这组运行承载三层
- 明确界面线程若存在，只能读取：
  - `宿主任务虚拟存在镜像`
  - `恢复快照摘要`
  - `工作线程快照`
- 新增 `4.5 任务管理界面线程（观察/交互层）`

## 本次收口后的统一口径

现在规范统一为：

- `自我线程` 负责提交请求
- `任务管理实例/工作线程` 负责推进一步
- `宿主任务虚拟存在` 负责镜像治理状态
- `恢复快照` 负责跨退出续跑
- `任务管理界面线程` 只负责观察与控制意图输入

可压成一句话：

`自我线程提交，任务管理实例推进，界面线程观察。`

## 本次未做

- 没有改运行代码
- 没有改消息协议
- 没有新增界面线程控制指令类型
- 没有跑编译验证，因为本轮只改规范文档
