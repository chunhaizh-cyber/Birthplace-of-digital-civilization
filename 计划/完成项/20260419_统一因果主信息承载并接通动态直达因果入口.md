# 20260419_统一因果主信息承载并接通动态直达因果入口

## 本次完成

- 在 `基础信息主信息类.h` 中新增 `因果主信息类`
- 将 `因果实例主信息类 / 因果模板主信息类` 收成继承自 `因果主信息类` 的兼容外壳
- 新增 `枚举_因果形态`，统一区分 `实例 / 抽象`
- 在 `因果类.h/.cpp` 中补齐：
  - `取因果主信息(...)`
  - `按动态创建因果信息(...)`
- 在 `世界树类.h/.cpp` 中补齐：
  - `按动态创建因果信息(...)`
- 已迁移的核心调用点：
  - `二次特征应用模块.ixx`
  - `任务模块.最小回写器.cpp`
  - `自我类.cpp`

## 当前口径

- `动态信息 = 实例因果事实`
- `抽象因果` 仍由实例事实升格
- 为避免打断现有控制面板、学习层、枚举逻辑，保留 `因果实例主信息类 / 因果模板主信息类` 作为兼容外壳
- 统一字段承载已经收口到 `因果主信息类`

## 本次价值

- 不再要求调用侧先手工拼一份完整实例因果主信息，再去桥接动态
- `动态 -> 因果` 现在有正式主链入口
- 统一主信息承载已经具备，后面可以继续逐步清理“手工 new 因果实例主信息类”的残留

## 验证

- 已执行：
  - `msbuild D:\鱼巢\鱼巢.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64 /m`
- 结果：
  - `0 error / 0 warning`

## 后续建议

- 继续迁移 `任务管理任务模块.impl.cpp` 中剩余的“先建动态、再手工建实例因果”路径
- 等 `任务管理 / 学习 / 控制面板 / 语言层` 都能稳定只读统一因果主信息后，再考虑真正退掉兼容外壳
