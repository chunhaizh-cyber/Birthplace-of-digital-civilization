# S1d-5a 二次特征签名父节点索引瘦身方案 v0.1

日期：2026-06-08

## 1. 目标

```text
承接 S1d-4z。

S1d-4z 已闭合：
    `命中模板证据动态追加` 平均由约 37357us 降到约 16649us；
    `命中回写因果实例` 支线不再是高层主慢点；
    高层 `动态二次特征生成` 的最慢阶段回到 `分步结果生成`。

最新分步结果证据：
    `动态二次特征分步结果/内部阶段耗时` 7 条；
    最慢阶段分布为 `分步值写入=4`、`分步签名查找创建=3`；
    `分步签名查找创建` 平均约 69292us，`分步值写入` 平均约 57884us。

本切片目标：
    只在 `二次特征类::获取或创建子二次特征_按签名` 建立父节点 + 度量签名缓存；
    缓存已建立父节点的签名到节点映射；
    创建新二次特征后同步写入缓存；
    删除二次特征或重新绑定基础信息时清理缓存；
    避免每次分步结果生成都枚举同一父节点下全部二次特征。
```

## 2. 允许修改

```text
二次特征类.h
二次特征类.cpp
计划/20260607_S1d5a二次特征签名父节点索引瘦身方案_v0.1.md
计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
计划/计划索引.md
实施记录/20260608_S1d5a二次特征签名父节点索引_Codex断点清单.md
```

## 3. 禁止修改

```text
不改变二次特征主信息结构。
不改变二次特征生成语义。
不减少合法分步结果节点。
不改变任务状态、需求满足、价值结算或外设事实规则。
不实现 S6/S7 基础信息全局索引、候选方法索引或因果主果索引。
不新增模拟入口、夹具、伪造样本或 `--*-check` 回归入口。
```

## 4. 实施步骤

```text
S1d-5a-1：
    在 `二次特征类` 增加父节点 + 度量签名缓存成员。

S1d-5a-2：
    `绑定基础信息` 清空缓存，避免跨基础信息实例复用旧指针。

S1d-5a-3：
    `获取或创建子二次特征_按签名`：
        先准备有效签名；
        为父节点建立一次签名索引；
        命中缓存时直接返回；
        未命中时创建节点并写入缓存。

S1d-5a-4：
    `删除二次特征` 在删除前移除对应缓存项。

S1d-5a-5：
    构建 Debug x64，真实运行 `--self-check-log --self-check-log-wait-ms=10000`。
```

## 5. 验证预测

```text
构建：
    Debug x64 构建通过。

真实运行：
    退出码=0。
    日志继续出现 `动态二次特征分步结果/内部阶段耗时`。
    `分步签名查找创建` 平均耗时下降，或 `二次特征签名查找创建/内部阶段耗时` 中子节点枚举 / 签名比较不再构成累计瓶颈。
    日志保留 `任务状态动作动态=<动态主键>`。
    不出现 `任务状态被动提交/逻辑错误`。
    不出现 `任务状态动作动态逻辑错误`。
    不出现旧路径锚点 `准备本能调用`、`执行本能方法`、`任务域本能输入包`。
```

## 6. 下一步判断

```text
如果 `分步签名查找创建` 下降：
    按最新分步结果最慢阶段继续切片，预计转向 `分步值写入`。

如果下降不明显：
    继续拆 `节点创建` 与分步结果数量本身，判断是否需要批量创建或停止此支线。

如果 `动态二次特征生成` 高层主慢点转移：
    按最新高层最慢阶段重排主动切片。
```

## 7. 落实结果

```text
已落实：
    `二次特征类` 已增加父节点 + 度量签名缓存；
    `绑定基础信息` 清空缓存；
    `获取或创建子二次特征_按签名` 命中缓存时直接返回；
    父节点缓存首次建立时枚举已有子节点并写入签名映射；
    新建二次特征后同步写入缓存；
    `删除二次特征` 删除前移除对应缓存项。

验证：
    `git diff --check` 通过，仅有 CRLF 工作区转换警告；
    Debug x64 构建通过；
    真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0。

本轮日志窗口：
    最新启动行：`日志/鱼巢_run_20260608.低值g:52337`；
    `任务状态动作动态=` 75 条；
    `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包` 均为 0。

性能证据：
    `动态二次特征分步结果/内部阶段耗时` 8 条；
    `分步签名查找创建` 平均约 4121us，较 S1d-4z 的约 69292us 明显下降；
    `二次特征签名查找创建/内部阶段耗时` 1 条，最慢阶段为 `节点创建`，`子节点枚举` 和 `签名比较` 均为 0us；
    `动态二次特征分步值写入/内部阶段耗时` 8 条，`分步值写入` 平均约 82869us；
    标量值写入平均约 41488us，离散值写入平均约 41381us，平均标量 / 离散写入次数均约 76。

结论：
    S1d-5a 已闭合；
    父节点 + 度量签名缓存有效，重复子节点枚举和签名比较已不再是当前主耗时；
    当前主慢点转为合法分步结果的标量 / 离散值写入聚合。

下一步：
    进入 S1d-5b：二次特征值写入主信息复用瘦身。
```
