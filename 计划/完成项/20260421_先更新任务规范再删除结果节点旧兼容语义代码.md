# 2026-04-21 先更新任务规范再删除结果节点旧兼容语义代码

## 本次目标

先把任务规范收紧到统一口径，再删除仍然依赖旧口径的兼容代码。

本次收紧的核心规则是：

1. 任务与方法的虚拟存在是主逻辑读写特征的权威面。
2. 一个步骤下面允许同时存在预测结果与实际结果。
3. 实际结果不得直接覆写预测结果，必须允许并存。
4. 结果节点到下一步的权威枝路只允许写在 `下一跳步骤节点`。
5. 实际结果若偏离命中预测，则任务应回到 `待重筹办`。

## 已更新规范

- `D:\鱼巢\规范\0905_任务统一三段式与标的规范.md`
- `D:\鱼巢\规范\1000_任务执行类实现规范（当前顺序执行骨架）.md`
- `D:\鱼巢\规范\1300_任务树数据结构与步骤子任务执行规范.md`
- `D:\鱼巢\规范\1310_任务树结构体字段草案与状态机转移表.md`
- `D:\鱼巢\规范\2090_任务信息自然语言表达规范.md`

## 已删除/收口的不符合规范代码

### 1. 删除结果节点拿 `后续任务` 充当下一跳的兼容逻辑

已从以下位置删除“结果节点优先读/写 `后续任务` 作为下一步”的旧兼容语义：

- `D:\鱼巢\任务类.cpp`
- `D:\鱼巢\自我_执行桥模块.ixx`
- `D:\鱼巢\学习执行模块.cpp`
- `D:\鱼巢\任务管理任务模块.impl.cpp`

当前口径改为：

- 结果节点下一跳优先读 `下一跳步骤节点`
- 若为空，则回退到结果节点真实子步骤

### 2. 删除学习步骤把预测结果直接改成实际结果的旧逻辑

已从：

- `D:\鱼巢\学习执行模块.cpp`

删除“把当前预测结果节点直接改成实际结果”的写法。

当前改为：

- 学习步骤写回真实产物时，若当前结果仍是预测结果，则新建一个实际结果节点
- 预测结果继续保留，供后续命中和回比

### 3. 删除控制面板对结果节点 `后续任务` 的误导展示

已从：

- `D:\鱼巢\控制面板类.cpp`

删除结果节点通用关系里的 `后续任务` 展示，避免把它继续看成权威下一跳。

## 当前结果

现在的任务树结果语义已经统一为：

- 步骤节点可以挂多个结果节点
- 预测结果与实际结果允许并存
- 结果节点显式决定下一跳
- 结果偏离预测时回到重筹办

## 验证

已通过：

```powershell
msbuild D:\鱼巢\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /m /clp:ErrorsOnly
```
