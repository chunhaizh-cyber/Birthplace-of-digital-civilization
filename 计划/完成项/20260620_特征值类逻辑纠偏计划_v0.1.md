# 20260620 特征值类逻辑纠偏计划 v0.1

## 1. 当前主动切片：节点 VecU 只读指针

来源：

```text
计划/20260620_特征类逻辑纠偏计划_v0.1.md 的 1.2：
    三维体素链函数直接读取 特征值类 节点的 主信息.值。
```

触发点：

```text
特征类.cpp 当前三维体素链读取中存在：
    根节点->主信息.值
    当前节点->主信息.值
    子节点->主信息.值

特征值类 已有：
    取VecU只读指针(VecU句柄)

但当前三维体素链持有的是 特征值节点类*，缺少节点指针版本只读函数。
```

项目结构映射：

```text
业务概念 -> 项目承载结构 -> 值类型 -> 写入方 -> 读取方 -> 验证方式
特征值节点 VecU 值 -> 特征值类::节点类 / 特征值主信息类::值 -> const VecIU64* -> 特征值类 创建值节点/创建子值节点 -> 特征值类::取节点VecU只读指针 / 特征类三维体素链解析 -> rg direct 主信息.值 残留 + Debug x64 构建
```

执行步骤：

```text
1. 在 特征值类.h 增加静态只读函数：取节点VecU只读指针(const 节点类* n) noexcept。
2. 函数只返回节点内部 VecIU64 值的 const 指针，不创建、不写入、不改变索引。
3. 运行 python .\tools\生成函数功能说明文档.py。
4. 运行 Debug x64 构建。
5. 闭合后返回 计划/20260620_特征类逻辑纠偏计划_v0.1.md 的 1.2，替换三维体素链 direct 主信息.值。
```

验收命令：

```powershell
rg -n "取节点VecU只读指针|取VecU只读指针" 特征值类.h 说明书/函数功能说明文档.md
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

闭合结果（2026-06-20）：

```text
1. 特征值类.h 已新增 取节点VecU只读指针(const 节点类* n) noexcept。
2. 函数只返回节点内部 VecIU64 值的 const 指针，不创建、不写入、不改变索引。
3. python .\tools\生成函数功能说明文档.py 已通过，函数条目 6881。
4. Debug x64 构建已通过：日志/20260620_特征值类节点VecU只读指针_msbuild.log，0 个警告，0 个错误。
5. 证据：实施记录/20260620_特征值类节点VecU只读指针_Codex断点清单.md。
6. 返回点：计划/20260620_特征类逻辑纠偏计划_v0.1.md 的 1.2，替换三维体素链 direct 主信息.值。
```

禁止宣称：

```text
不宣称特征值类逻辑纠偏完成。
不宣称特征类逻辑纠偏完成。
不宣称三维体素链读取回迁完成，直到 特征类.cpp direct 主信息.值 替换并构建通过。
```
