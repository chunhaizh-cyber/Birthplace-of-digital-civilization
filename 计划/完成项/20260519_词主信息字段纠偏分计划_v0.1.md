# 词主信息字段纠偏分计划 v0.1

更新时间：2026-05-19

对应主信息：`词主信息类`

## 审查结论

`词主信息类` 属于语素层词项结构。当前不作为世界事实、需求目标或方法动作权威来源。

## 纠偏动作

```text
P1. 检查词项字段是否只用于词汇、词性和语言入口。
P2. 若词项被用作特征类型，应通过正式语义入口或特征类型引用进入，不新增业务标签。
P3. 词项本身不得承载任务 / 需求 / 方法状态。
```

## 验收

```powershell
rg -n "词主信息类|class 词主信息类" -S 语素主信息类.h 语素类.cpp
```

通过条件：

```text
词主信息只服务语言与语义入口，不保存治理业务事实。
```

## 执行记录

```text
2026-05-19:
    已检查 `词主信息类`。
    审查结论：
        当前词主信息只保存 `std::string 词`；
        该字段只作为词面和词典路径身份；
        不保存任务、需求、方法、世界事实或结算相关字段。
    代码调整：
        `语素主信息类.h` 为 `词主信息类::词` 补充边界注释；
        `词主信息类::比较(...)` 标注为词典节点排序 / 查找，不表达治理业务语义；
        `语素类.cpp` 的词创建和词查找路径补充“词典结构 / 业务语义走语素入口和基础信息引用”的边界说明。
    验收：
        `rg -n "词主信息类|class 词主信息类" -S 语素主信息类.h 语素类.cpp` 通过；
        Debug x64 构建通过，仅有 LNK4075 非增量链接常规警告；
        `--demand-tree-growth-check` 通过。
```
