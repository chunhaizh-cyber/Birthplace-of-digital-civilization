# 20260419 阶段1-2 学习主需求收口并下沉旧动作型需求

## 本步完成

- 将学习主目标从旧的 `补全方法头节点 / 验证候选方法` 收口为以 `让方法可用` 为主轴的新目标集合。
- 将旧的动作型“学习需求”从顶层需求层下沉为 `学习动作类型`。
- 让 `任务管理任务` 先生成顶层学习需求，再分解出学习动作集合和原函数需求集合。

## 主要改动

- 更新 `任务管理任务模块.ixx` 中的：
  - `枚举_学习目标类型`
  - `枚举_学习需求类型`
  - `枚举_学习动作类型`
  - `结构_学习动作描述`
- 更新 `任务管理任务模块.impl.cpp` 中的：
  - `私有_构建学习目标描述(...)`
  - `私有_构建学习需求集合(...)`
  - `私有_构建学习动作集合(...)`
  - `私有_构建原函数需求集合(...)`
  - `私有_形成学习承接决策(...)`
- 更新 `学习任务模块.ixx/.cpp` 与 `控制面板类.cpp`，让学习实体和观察面同步显示“学习动作”。

## 收口后的口径

- 顶层学习目标：围绕“让当前待学习方法头可用”展开。
- 顶层学习需求：不再直接使用 `样本收集/差异比较/...` 这类动作名。
- `样本收集/差异比较/共同条件提取/候选动作生成/...` 下沉为学习动作层。

## 验证

- `msbuild D:\鱼巢\鱼巢.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64 /m`
- 结果：`0 error / 0 warning`
