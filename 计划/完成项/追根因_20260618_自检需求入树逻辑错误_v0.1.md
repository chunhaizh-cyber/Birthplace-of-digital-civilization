# 追根因 20260618 自检需求入树逻辑错误 v0.1

## 当前弹窗

```text
标题：鱼巢 - 自检需求入树逻辑错误
正文锚点：自检需求入树裁决：候选不进入当前普通需求树
触发路径：结构体审计 P4c 自检窗口 `--self-check-log --self-check-log-wait-ms=10000`
```

## 已确认事实

```text
日志位置：日志/鱼巢_exception_20260618.低值g
13:38:02.791 [ERR] 自检需求入树裁决：候选不进入当前普通需求树
13:38:02.792 [ERR] 弹窗错误提示 | 标题=鱼巢 - 自检需求入树逻辑错误

直接调用点：
自我线程模块.impl.cpp:9213 `私有_提示自检需求入树逻辑错误`
自我线程模块.impl.cpp:9751-9758 父需求裁决失败且 `父裁决.需要弹错` 时触发。

候选事实：
自检来源=安全根任务
固定来源任务主键=2
固定来源需求主键=1
目标宿主=xJ
目标特征=方法状态
目标方法=1c
因果链边=2
投影命中=2
来源因果数量=1
来源因果=3E7
方法=188
本能ID=6143
本能动作=建立最小自我边界参照
练习目标=是
正式目标=方法状态>=好用
```

## 当前层级

```text
L1 直接生成层：自检候选父需求裁决返回 未判定 / 可入普通需求树=false / 需要弹错=true。
```

## 同层疑点记录

```text
1. 父需求裁决函数没有把“方法状态好用练习需求”映射到固定来源需求或学习/方法状态父域。
2. 因果投影命中 2 但父需求仍未判定，可能是投影结果只用于日志，没有参与父需求选择。
3. 固定来源任务 / 固定来源需求已存在，但裁决逻辑仍要求目标特征类型和目标因果查询判定父需求。
```

## 选中下钻分支

```text
先读 `私有_裁决自检候选父需求_线程侧` 和调用方，确认父域判定规则。
```

## 主链判断

```text
已确认 `私有_裁决自检候选父需求_线程侧` 只识别安全、服务、根层遗留学习和缺失结构 7/8/9。
本轮弹窗候选目标特征为 `方法状态`，历史闭合证据要求此类候选进入 `方法维护` 父域。
因此第一分歧点是父域判定缺少 `方法状态 -> 方法维护` 映射。
```

## 已排除

```text
不是“没有日志”：异常日志已记录正文、弹窗标题和弹窗返回前记录。
不是 P4c 执行尝试标识删除的直接编译问题：Debug x64 构建已通过。
```

## 已改文件

```text
自我线程模块.impl.cpp
    新增 `私有_目标特征是方法维护字段_线程侧`。
    `私有_裁决自检候选父需求_线程侧` 将 `方法状态` 目标特征映射到 `方法维护` 父域。

说明书/函数功能说明文档.md
    已由 `python .\tools\生成函数功能说明文档.py` 刷新。
```

## 已运行命令

```text
Select-String 日志/鱼巢_exception_20260618.低值g -Pattern 自检需求入树...
rg -n "自检需求入树缺失|自检需求入树逻辑错误|候选不进入当前普通需求树"
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m:1 /nr:false
    exit_code=0；0 警告，0 错误。
```

## 下一条命令

```text
复跑 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000`。
```

## 验收证据

```text
修复后需复跑同一真实自检窗口，确认：
1. 不再出现同标题同正文弹窗；
2. `日志/鱼巢_exception_20260618.低值g` 新窗口不再出现同标题 `弹窗错误提示`；
3. 需求候选若入树，必须有明确父需求和项目结构承载，不得用文本原因兜底。
```

## 验证预测

```text
1. 同一真实自检窗口不再弹出 `鱼巢 - 自检需求入树逻辑错误`。
2. `日志/鱼巢_exception_20260618.低值g` 在新窗口不再新增同标题 `弹窗错误提示`。
3. `日志/鱼巢_run_20260618.低值g` 应出现 `父域=方法维护` 的自检正式需求入树、或复用既有被维护方法需求。
```

## 验证结果

```text
.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000
    exit_code=0
    stdout_len=10117
    stderr_len=0
    exception_before=5909
    exception_after=5909

日志/鱼巢_run_20260618.低值g:1454
    自我线程/自检维护管理分支确保 | 域=方法维护 | 管理父需求=6

日志/鱼巢_run_20260618.低值g:1456
    自我线程/自检正式需求入树 | 父需求=6 | 父域=方法维护 | 新需求=7
```

## 结论

```text
根因：父域裁决函数漏掉 `方法状态 -> 方法维护` 映射。
修复：`方法状态` 自检需求型候选进入现有方法维护管理分支。
同标题弹窗：复跑窗口未新增。
```

## 禁止宣称

```text
不宣称结构体审计完成。
不宣称自检需求入树问题已修复。
不宣称内部循环完成、自我苏醒完成或初步成熟完成。
```
