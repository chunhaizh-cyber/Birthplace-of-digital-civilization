# 语素语言记录主信息字段纠偏分计划 v0.1

更新时间：2026-05-19

对应主信息：`语素语言记录主信息类`

## 审查结论

`语素语言记录主信息类` 用于语素层语言记录，属于记录和追溯结构。

## 纠偏动作

```text
P1. 确认字段只用于语素语言记录。
P2. 若记录被用于生成动态或因果证据，应通过正式来源引用，不把结论写入记录主信息。
P3. 不新增业务语义枚举。
```

## 验收

```powershell
rg -n "语素语言记录主信息类" -S 语素主信息类.h 语素类.cpp
```

通过条件：

```text
语素语言记录只作为记录来源，不作为业务事实权威。
```

## 执行记录

### 2026-05-19 首轮执行

```text
已完成：
- 标注语素语言记录只作语言功能词入口记录和追溯索引。
- 标注语言记录不得作为业务事实、动态结论或因果证据本体。
- 标注比较只服务记录节点排序 / 查找，业务语义必须通过正式来源引用、特征或二次特征表达。
- 将语言词性指针比较统一为安全顺序比较。

验收：
- rg -n "语素语言记录主信息类|语言词性" -S 语素主信息类.h 语素类.cpp
- rg -n "语言功能词入口记录|追溯索引|业务事实|因果证据本体|正式来源引用" -S 语素主信息类.h 语素类.cpp
- msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
- .\x64\Debug\鱼巢.exe --demand-tree-growth-check

结果：
- Debug x64 构建通过，仅有 LNK4075 既有警告。
- 需求树生长回归通过。
```
