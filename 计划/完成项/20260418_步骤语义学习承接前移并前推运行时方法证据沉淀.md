## 完成事项

- 将 `步骤位 / 步骤语义 / 学习承接` 继续前移为正式结构字段，不再只在 `任务管理任务模块.impl.cpp` 内部做临时私有推导。
- 将重复出现的 `实例因果 + 二次特征证据` 前推到运行主链，对当前运行方法自动补齐 `条件判定索引 / 结果摘要索引`，不再只依赖学习提交路径补方法证据。
- 收窄 `组装任务管理一步结果(...)` 里的本地兼容回退，使一步结果优先读取 `请求上下文 / 当前需求位特征 / 当前治理二次特征证据`，不再额外引用不存在的旧结果壳字段。

## 本轮代码落点

- `D:\鱼巢\任务管理任务模块.ixx`
  - `结构_任务管理方法需求位特征面` 新增：
    - `当前步骤位类型`
    - `当前步骤语义类型`
    - `已触发学习承接`
    - `当前学习承接类型`
    - `学习承接摘要`
  - `结构_任务管理上下文` 同步新增以上字段，作为正式治理上下文字段。

- `D:\鱼巢\任务管理任务模块.impl.cpp`
  - 新增并接通治理特征键：
    - `任务管理_当前步骤语义类型`
    - `任务管理_当前学习承接类型`
    - `任务管理_当前学习承接摘要`
  - 将 `读取任务管理上下文(...)`、`刷新任务管理方法需求位(...)`、`同步最近治理结果到上下文(...)`、`从治理恢复快照补齐主体桥接字段(...)` 接到这批正式字段。
  - 将 `私有_推导学习承接类型(...)`、`形成治理特征面(...)`、`同步学习承接决策(...)` 改成优先读写正式字段与证据。
  - 新增运行时方法证据自动沉淀：
    - `私有_P2_创建或复用运行方法条件节点(...)`
    - `私有_P2_创建或复用运行方法结果节点(...)`
    - `私有_P2_补齐运行方法证据索引(...)`
  - 将 `当前步骤为等待维持 / 当前步骤为业务推进 / 当前已触发学习承接` 这组运行时证据写入当前运行方法的条件/结果节点。
  - 在 `私有_M9_执行学习承接与业务派生阶段(...)` 中前推运行时方法证据沉淀。
  - 修正 `组装任务管理一步结果(const 结构_任务管理请求&, const 结构_任务管理单步决策&, const 结构_任务管理写回结果&, ...)`，使其学习承接字段只读 `请求.上下文` 与当前写回结果，去掉错误的旧结果引用。

## 收口后的口径

- 任务树 / 方法树：表达结构
- 二次特征：表达当前判断证据
- 原语：解释证据
- 任务管理任务模块：编排结构、证据、原语与写回

这一步完成后，`步骤语义 / 学习承接` 已经不再主要依赖本地临时推导；运行中的实例因果与二次特征证据，也开始直接沉淀到当前方法节点，而不是必须等到学习资产正式提交后才补齐。

## 验证

- 已执行：
  - `msbuild D:\鱼巢\鱼巢.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64 /m`
- 结果：
  - `0 error / 0 warning`

## 尚未覆盖

- 尚未做专门运行态回放验证。
- 原语层与编排层仍有少量兼容回退分支，但主链已经优先读取结构字段与二次特征证据。
