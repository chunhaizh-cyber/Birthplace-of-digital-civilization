# 20260418 原语执行改为通过统一原语证据视图解释

## 本次目标

继续收口 `任务管理任务模块` 的原语层，让 `私有_P0_执行原语(...)` 更接近“只解释结构字段 + 二次特征证据”的纯原语壳，减少原语内部对局部拆证据变量的依赖。

## 本次完成

1. 在 `D:\鱼巢\任务管理任务模块.impl.cpp` 中新增 `结构_P0_原语证据视图`。
2. 新增 `私有_P0_构建原语证据视图(...)`，统一从：
   - `当前治理二次特征证据`
   - 结构优先字段
   - 上下文已回写稳定字段
   构建原语执行所需的最小证据面。
3. 将 `私有_P0_执行原语(...)` 改为优先消费 `结构_P0_原语证据视图`。
4. 修正 `升阶判定` 分支中残留的旧局部变量引用，统一改为读取 `视图.存在显式冲突`。

## 当前效果

- `条件满足 / 方向一致 / 结果符合 / 冲突判定 / 因果稳定 / 可等待 / 已完成 / 需收束 / 可回接 / 可继续推进 / 升阶判定`
  这批原语，已经进一步收口到统一证据视图。
- 原语层继续从“局部拆变量解释器”向“统一证据解释器”收窄。
- 为后续继续清掉编排层和原语层里的兼容回退分支打下了更稳定的入口。

## 验证

已执行：

```powershell
msbuild D:\鱼巢\鱼巢.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64 /m
```

结果：

- `0 error`
- `0 warning`

## 尚未完成

1. `私有_P0_执行原语(...)` 里仍存在少量兼容回退口径，后续还需继续压缩。
2. `步骤结果子链` 和学习触发链虽然已经开始优先读结构字段与治理证据，但仍可继续削减旧上下文兜底。
3. 这轮仅做了编译级验证，尚未做运行态回放验证。
