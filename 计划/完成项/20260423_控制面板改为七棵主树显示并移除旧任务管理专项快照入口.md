标题：控制面板改为七棵主树显示并移除旧任务管理专项快照入口

时间：2026-04-23

目标：
- 按新的主线逻辑重构控制面板显示。
- 页面按 `世界树 / 需求树 / 需求列表 / 任务树 / 方法树 / 待学习列表 / 因果树` 排列。
- 所有主页面统一改为树形显示。
- 先移除旧 `任务管理专项快照` 入口，避免控制面板继续沿旧治理上下文崩溃。

变更：
- 在 [控制面板类.ixx](D:\鱼巢\控制面板类.ixx) 新增：
  - `需求列表树根`
  - `待学习列表树根`
- 在 [控制面板类.cpp](D:\鱼巢\控制面板类.cpp) 新增：
  - `私有_构建列表项树节点(...)`
  - `私有_从列表构建树根(...)`
- `读取控制面板快照(...)` 不再调用：
  - `私有_构建任务管理专项快照(...)`
  - `私有_构建主派发输出快照(...)`
  - `私有_构建线程状态列表(...)`
- 需求列表和待学习列表改为先构建树根，再统一走树形 HTML 渲染。
- 因果树标题收口成 `因果树`。
- HTML 菜单改为只暴露七个主页面：
  - 世界树
  - 需求树
  - 需求列表
  - 任务树
  - 方法树
  - 待学习列表
  - 因果树
- 世界树、需求树、需求列表、任务树、方法树、待学习列表、因果树统一走 `私有_树HTML(...)`，默认只显示根链节点，双击逐层展开。

结果：
- 控制面板不再依赖旧任务管理专项快照链读取任务管理上下文。
- 导出的 HTML 主入口已经收口为七棵主树。
- 旧页面代码仍在文件中作为未暴露兼容壳，但不再是当前 HTML 主显示入口。

验证：
- `msbuild D:\鱼巢\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:TargetName=鱼巢_tmp_panel_rework_0423 /m /clp:ErrorsOnly`
- `D:\鱼巢\x64\Debug\鱼巢_tmp_panel_rework_0423.exe --panel-html`
