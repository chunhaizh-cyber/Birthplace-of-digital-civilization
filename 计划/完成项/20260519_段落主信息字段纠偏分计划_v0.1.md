# 段落主信息字段纠偏分计划 v0.1

> 2026-05-22 验证口径修订：
> 后续所有计划的验证统一改为日志模块验证，以真实运行产生的日志、状态快照、动作动态与错误日志为证据。
> 严禁新增或依赖模拟测试、夹具测试、伪造样本、伪造入口、伪造动作动态或 `--*-check` 回归命令。
> 历史计划中已有的回归命令、夹具和模拟测试描述仅作为历史记录，不再作为当前执行或验收依据。

更新时间：2026-05-19

对应主信息：`段落主信息类`

## 审查结论

`段落主信息类` 属于文档段落结构，低风险。

## 纠偏动作

```text
P1. 确认字段只表达段落文本和结构位置。
P2. 不把解析出的需求、任务、方法结论写回段落主信息。
```

## 验收

```powershell
rg -n "段落主信息类" -S 语言主信息类.h
```

通过条件：

```text
段落主信息只服务文本结构。
```

## 执行记录

### 2026-05-19 首轮执行

```text
已完成：
- 标注段落主信息只表达文档中的段落位置和原始文本。
- 标注段落解析出的需求、任务、方法或世界事实必须写入对应领域结构，不回写为段落字段。

验收：
- rg -n "段落主信息类|在文章中的序号|原始文本" -S 语言主信息类.h 语言类.cpp
- rg -n "段落位置|段落解析|对应领域结构|不回写" -S 语言主信息类.h 语言类.cpp
- msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
- .\x64\Debug\鱼巢.exe --demand-tree-growth-check

结果：
- Debug x64 构建通过，仅有 LNK4075 既有警告。
- 需求树生长回归通过。
```
