# S1d-4t 二次特征场景绑定写入内部阶段定位与瘦身方案 v0.1

日期：2026-06-08

## 1. 目标

```text
承接 S1d-4s。

S1d-4s 已闭合：
    最新运行线程 T16620 中，分步语义绑定内部样本 6 条；
    最慢阶段全部为 `场景绑定写入`；
    `场景绑定写入` 平均约 89238us，最大约 176646us。

当前判断：
    `场景绑定写入` 对应 `二次特征类::绑定场景`；
    该函数写入所属场景引用后，会追加场景主信息中的 `关系索引` 和 `二次特征索引`；
    追加索引当前持有全局场景索引锁，并分别做重复引用扫描。

本切片目标：
    拆分 `绑定场景` / 场景索引追加内部耗时；
    优先优化重复主键读取、重复查重扫描和锁内工作；
    保留二次特征所属场景和场景索引账。
```

## 2. 允许修改

```text
二次特征类.cpp
计划/20260607_S1d4t二次特征场景绑定写入内部阶段定位与瘦身方案_v0.1.md
计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
计划/计划索引.md
实施记录/20260608_S1d4t二次特征场景绑定写入_Codex断点清单.md
```

## 3. 禁止修改

```text
不跳过二次特征所属场景写入。
不删除 `关系索引` 或 `二次特征索引`。
不改变二次特征主信息结构。
不把动态二次特征生成降级成普通日志。
不改任务状态、需求满足、价值结算或外设事实规则。
不新增模拟入口、夹具、伪造样本或 `--*-check` 回归入口。
```

## 4. 实施步骤

```text
S1d-4t-1：
    在 `二次特征类.cpp` 增加 `二次特征绑定场景/内部阶段耗时` 慢采样。

S1d-4t-2：
    拆分 `绑定场景`：
        主信息读取；
        场景主信息读取；
        所属场景引用写入；
        索引追加。

S1d-4t-3：
    拆分 `私有_追加二次特征索引`：
        节点引用准备；
        场景索引锁等待；
        关系索引查重；
        关系索引追加；
        二次特征索引查重；
        二次特征索引追加。

S1d-4t-4：
    若慢点在重复主键读取或重复查重扫描，先做最小瘦身：
        节点主键只取一次；
        同一锁内复用同一个引用；
        已绑定且索引已有时直接返回。

S1d-4t-5：
    构建 Debug x64，真实运行 `--self-check-log --self-check-log-wait-ms=10000`。
```

## 5. 验证预测

```text
构建：
    Debug x64 构建通过。

真实运行：
    退出码=0。
    日志继续出现 `动态二次特征分步语义绑定/内部阶段耗时`。
    日志出现 `二次特征绑定场景/内部阶段耗时`，或在 5ms 阈值下无样本但高层分步结果仍可统计。
    日志保留 `任务状态动作动态=<动态主键>`。
    不出现 `任务状态被动提交/逻辑错误`。
    不出现 `任务状态动作动态逻辑错误`。
    不出现旧路径锚点 `准备本能调用`、`执行本能方法`、`任务域本能输入包`。
```

## 6. 下一步判断

```text
如果主耗时在索引锁等待：
    记录为全局场景索引锁竞争，下一片再评估锁粒度或场景级索引锁，不能在本片直接拆锁。

如果主耗时在索引查重：
    优先优化查重函数，减少重复主键读取和重复扫描。

如果主耗时在索引追加本身：
    评估是否有容量扩展或索引结构风险；不得删除索引账。

如果优化后 `场景绑定写入` 不再是主耗时：
    停止本切片，按最新最慢阶段切下一片。
```

## 7. 落实结果

```text
已落实：
    `二次特征类.cpp` 新增 `二次特征绑定场景/内部阶段耗时`；
    `绑定场景` 已拆分主信息读取、场景主信息读取、所属场景引用写入、索引追加；
    `私有_追加二次特征索引` 已拆分节点引用准备、场景索引锁等待、关系索引查重、关系索引追加、二次特征索引查重、二次特征索引追加；
    索引查重已改为一次性取得节点主键并复用，避免在每个索引项比较时重复读取节点主键。

验证：
    `git diff --check` 通过，仅有 CRLF 工作区转换警告；
    Debug x64 构建通过；
    真实运行 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0。

本轮日志窗口：
    最新启动行为 `2026-06-08 11:17:25.511 [T29964] 程序启动`；
    `任务状态动作动态=` 55 条；
    `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包` 均为 0。

性能证据：
    本轮未出现 `二次特征绑定场景/内部阶段耗时`，说明 `绑定场景` 单次未超过 5000us 慢采样阈值；
    `动态二次特征分步结果/内部阶段耗时` 5 条，最慢阶段分布为 `分步值写入=3`、`分步签名查找创建=2`；
    分步结果平均总耗时约 129704us，`分步语义绑定` 平均约 12217us，`分步值写入` 平均约 50029us，`分步签名查找创建` 平均约 50774us；
    `动态二次特征分步语义绑定/内部阶段耗时` 5 条，`场景绑定写入` 平均约 6366us，`概念名称取词` 平均约 4392us；
    `动态二次特征生成/内部阶段耗时` 19 条，最慢阶段分布为 `条件候选收集=16`、`命中回写因果实例=2`、`分步结果生成=1`；
    动态二次特征生成平均总耗时约 441874us，`条件候选收集` 平均约 225225us，`分步结果生成` 平均约 134553us。

结论：
    S1d-4t 已把语义绑定主耗时从场景绑定写入移走；
    分步结果不再以语义绑定为最慢阶段；
    动态二次特征生成当前主慢点转移到 `条件候选收集`。

下一步：
    进入 S1d-4u：条件候选收集内部阶段定位 / 瘦身。
```
