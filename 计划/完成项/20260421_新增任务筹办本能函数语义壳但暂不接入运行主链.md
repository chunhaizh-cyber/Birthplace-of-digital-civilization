# 20260421 新增任务筹办本能函数语义壳但暂不接入运行主链

## 本次目标

按最新口径，先补出 `任务筹办` 这条本能函数的最小语义壳：

- 有一个正式的本能方法 ID 与元信息
- 有一份最小函数实现语义
- 允许后续再把它接到初始化、执行桥、自我线程实例化主链

本次**不接入运行**，避免边改边把未稳定逻辑推到主流程。

## 本次完成

1. 在 `D:\鱼巢\本能方法类.h` / `D:\鱼巢\本能方法类.cpp` 中新增：
   - `枚举_本能方法ID::自我_任务筹办 = 6037`
   - `任务筹办` 的本能方法元信息
   - 两个专用结果码：
     - `本能方法结果_任务筹办已建立方法头待重入`
     - `本能方法结果_任务筹办已激活学习任务`

2. 在 `D:\鱼巢\自我初始化模块.cpp` 中补出 `任务筹办` 本能函数的最小语义实现辅助：
   - 确保 `任务筹办` 方法头
   - 确保 `尝试学习` 方法头
   - 确保 `学习任务_任务筹办_*` 子任务
   - 任务筹办本能函数运行时注册 helper

3. 明确保持“未接入运行”：
   - 未在 `执行自我初始化(...)` 中调用运行时注册 helper
   - 未把这条逻辑正式挂入初始化主链
   - 未把新函数切成当前任务执行默认入口

4. 恢复并保持当前运行链原行为：
   - `D:\鱼巢\自我线程模块.impl.cpp` 中仍保持原来的 `任务筹办` 成长方法头创建逻辑
   - 不提前把新本能函数强行接到实例化任务头的执行链上

## 边界说明

这一步只是“把函数补出来”，不是“已经投入运行”。

后续真正接入时，至少还要再做三件事：

1. 决定谁来注册 `任务筹办` 本能动作运行时实现
2. 决定任务头/步骤头什么时候优先走 `任务筹办`
3. 决定 `已建立方法头待重入 / 已激活学习任务` 两类结果如何进入正式状态迁移

## 验证

已通过：

```powershell
msbuild D:\鱼巢\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /m /clp:ErrorsOnly
```
