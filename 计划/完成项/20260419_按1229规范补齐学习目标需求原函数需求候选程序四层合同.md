# 20260419 按1229规范补齐学习目标需求原函数需求候选程序四层合同

## 本次目的

按 `1229_学习方法生成链与任务管理编排规范.md`，把当前学习承接链里缺失的四层正式合同补出来：

- `学习目标`
- `学习需求`
- `原函数需求集合`
- `候选学习程序`

目标不是推翻现有学习链，而是在现有

`任务管理学习承接决策 -> 学习任务同步输入 -> 学习任务实体 -> 学习调度/执行`

这条主链上，先把规范要求的层级显式落成结构，避免继续只靠摘要串联。

## 本次改动

### 1. 任务管理侧补齐四层正式结构

在 `D:\鱼巢\任务管理任务模块.ixx` 中新增：

- `枚举_学习目标类型`
- `枚举_学习需求类型`
- `枚举_学习原函数类型`
- `枚举_学习程序决策`
- `结构_学习目标描述`
- `结构_学习需求描述`
- `结构_学习原函数需求项`
- `结构_候选学习程序摘要`

并把它们挂进 `结构_学习承接决策`。

### 2. 学习承接决策不再只有摘要

在 `D:\鱼巢\任务管理任务模块.impl.cpp` 中补了：

- 学习目标类型文本
- 学习需求类型文本
- 学习原函数类型文本
- 学习程序决策文本
- `私有_构建学习目标描述(...)`
- `私有_构建学习需求集合(...)`
- `私有_构建原函数需求集合(...)`
- `私有_构建候选学习程序摘要(...)`

`私有_形成学习承接决策(...)` 现在会正式生成这四层合同，再落进承接决策，不再只有“学习承接摘要 + 方法选项摘要”。

### 3. 学习实体同步承接这四层合同

在 `D:\鱼巢\学习任务模块.ixx` 中：

- `结构_学习任务同步输入`
- `结构_学习任务实体`

都新增了：

- `当前学习目标`
- `当前学习需求集合`
- `当前原函数需求集合`
- `当前候选学习程序`

并在 `D:\鱼巢\学习任务模块.cpp` 的 `创建或更新学习任务(...)` 里完成写入。

### 4. 调度/执行观察面开始可见这四层结构

本次没有重写学习调度和学习执行逻辑，但已把它们接进观察摘要：

- `D:\鱼巢\学习调度模块.cpp`
- `D:\鱼巢\学习执行模块.cpp`

调度和执行摘要现在会带：

- `目标=...`
- `程序=...`

便于后续继续把“原函数需求 -> 原函数匹配 -> 元函数组合 -> 程序改写”拆得更细。

## 这次纠偏后的对齐点

当前已经从“只有承接摘要”前进一步，变成：

`学习目标 -> 学习需求集合 -> 原函数需求集合 -> 候选学习程序 -> 学习任务实体`

虽然仍是基于当前治理证据的映射生成，但层级已经显式存在，不再只在规范和注释里。

## 还没做的部分

这次是“补正式合同”，不是“完整重写学习生成器”。因此还留着几块后续工作：

1. `学习调度模块` 还没有单独立出“原函数需求 -> 原函数匹配 -> 元函数组合”的独立结构层，目前只是先能看见。
2. `学习执行模块` 还没有把 `候选学习程序` 做成真正可被二次特征动态改写的执行体，目前仍以现有阶段推进为主。
3. `候选学习程序`、`候选学习方法节点`、`正式学习方法节点` 之间虽然已有结构边界，但还没有形成完全独立的状态机。

## 验证

已执行：

```powershell
msbuild D:\鱼巢\鱼巢.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64 /m
```

结果：

- `0 error`
- `0 warning`

本次仍未做运行态回放验证。
