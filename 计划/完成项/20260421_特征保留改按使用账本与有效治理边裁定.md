# 2026-04-21 特征保留改按使用账本与有效治理边裁定

> 2026-05-22 验证口径修订：
> 后续所有计划的验证统一改为日志模块验证，以真实运行产生的日志、状态快照、动作动态与错误日志为证据。
> 严禁新增或依赖模拟测试、夹具测试、伪造样本、伪造入口、伪造动作动态或 `--*-check` 回归命令。
> 历史计划中已有的回归命令、夹具和模拟测试描述仅作为历史记录，不再作为当前执行或验收依据。

## 本次完成

本次先完成规范修订，把“特征是否保留”的正式口径从“抽象价值评估优先”改成“使用事实优先”。

新的核心结论是：

- `特征保留不看抽象价值，只看有效参与治理闭环的使用事实`
- `被读取 / 被计算 / 被展示` 都不自动等于 `被使用`
- `有效使用` 的正式判断以 `有效治理边` 与 `使用事件/暴露事件` 为准
- `试用暴露期` 内的候选特征允许暂存
- `被使用但误导` 的特征不直接删除，而是转入修复或学习

## 新增规范

- [0579_特征使用账本与生命周期规范.md](D:\鱼巢\规范\0579_特征使用账本与生命周期规范.md)

## 同步修订

- [0000_规范总览与优先级.md](D:\鱼巢\规范\0000_规范总览与优先级.md)
- [0550_核心术语与概念定义规范.md](D:\鱼巢\规范\0550_核心术语与概念定义规范.md)
- [0575_二次特征泛称与n次特征递推生成规范.md](D:\鱼巢\规范\0575_二次特征泛称与n次特征递推生成规范.md)
- [0578_理解环与治理特征空间生长规范.md](D:\鱼巢\规范\0578_理解环与治理特征空间生长规范.md)

## 这次固定下来的最小规则

1. 从未被使用，且试用期结束：归档。
2. 被用于决策、派生、风险、需求显现、验证之一：保留。
3. 被高阶特征依赖：保留。
4. 被使用但结果经常不好：不删除，转修复/学习。

## 说明

- 这次只修订规范，没有动代码。
- 这次没有运行 `msbuild`。
