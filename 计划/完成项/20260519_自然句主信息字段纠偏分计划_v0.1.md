# 自然句主信息字段纠偏分计划 v0.1

> 2026-05-22 验证口径修订：
> 后续所有计划的验证统一改为日志模块验证，以真实运行产生的日志、状态快照、动作动态与错误日志为证据。
> 严禁新增或依赖模拟测试、夹具测试、伪造样本、伪造入口、伪造动作动态或 `--*-check` 回归命令。
> 历史计划中已有的回归命令、夹具和模拟测试描述仅作为历史记录，不再作为当前执行或验收依据。

更新时间：2026-05-19

对应主信息：`自然句主信息类`

## 审查结论

`自然句主信息类` 属于自然语言句子结构，不能作为需求或任务结论容器。

## 纠偏动作

```text
P1. 检查字段只表达句子文本、位置、结构。
P2. 句子解析出的治理含义必须转入正式特征、需求、任务或因果链。
P3. 不新增业务语义缓存字段。
```

## 验收

```powershell
rg -n "自然句主信息类" -S 语言主信息类.h
```

通过条件：

```text
自然句主信息不保存治理事实结论。
```

## 执行记录

### 2026-05-19 首轮执行

```text
已完成：
- 标注自然句主信息只保存句子在段落中的位置、词序列和原始文本。
- 标注句子解析出的治理含义必须转入正式特征、需求、任务或因果链，不在自然句字段中缓存结论。

验收：
- rg -n "自然句主信息类|在段落中的序号|词序列|原始文本" -S 语言主信息类.h 语言类.cpp
- rg -n "句子在段落中的位置|治理含义|缓存结论|正式特征、需求、任务或因果链" -S 语言主信息类.h 语言类.cpp
- msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
- .\x64\Debug\鱼巢.exe --demand-tree-growth-check

结果：
- Debug x64 构建通过，仅有 LNK4075 既有警告。
- 需求树生长回归通过。
```
