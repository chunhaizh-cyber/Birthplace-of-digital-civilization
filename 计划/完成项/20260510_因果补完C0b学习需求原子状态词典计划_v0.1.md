# 因果补完 C0b 学习需求原子状态词典计划 v0.1

日期：2026-05-10

前置计划：

```text
20260510_因果补完C0a学习映射候选诊断计划_v0.1.md
```

## 1. 目标

为后续学习需求正式入树补最小原子状态词典。

```text
学习需求入树前必须能构造：
    目标主体
    目标特征类型
    当前状态
    目标状态
    满足关系
```

本计划只补特征入口和状态值常量，不创建学习需求。

## 2. 新增特征入口

```text
学习目标状态
学习信息补齐状态
学习因果验证状态
学习方法可靠度状态
学习风险评估状态
```

## 3. 新增状态词典

```text
未补齐 = 0
已补齐 = 1
未验证 = 0
已验证 = 1
不可靠 = 0
可靠 = 1
未评估 = 0
已评估 = 1
```

## 4. 边界

```text
1. 只增加学习需求可用的原子状态词典。
2. 不生成学习需求。
3. 不修改需求树。
4. 不生成探索需求。
```

## 5. 验收

```text
1. Debug x64 构建通过。
2. 特征定义接口可编译链接。
3. 现有 summary 命令 0 码返回。
```

## 6. 完成记录

```text
2026-05-10 已完成。

实现内容：
1. `自我类.特征定义` 新增学习需求原子状态词典：
   - 未补齐 / 已补齐
   - 未验证 / 已验证
   - 不可靠 / 可靠
   - 未评估 / 已评估
2. 新增学习需求相关特征入口：
   - 学习目标状态
   - 学习信息补齐状态
   - 学习因果验证状态
   - 学习方法可靠度状态
   - 学习风险评估状态
3. 当前只提供原子状态和特征入口，不生成学习需求，不修改需求树。

验证：
1. Debug x64 构建通过。
2. `--self-loop-summary` 0 码返回。
3. `--task-mgmt-summary` 0 码返回。
4. `--demand-tree-growth-summary` 0 码返回。
5. `--self-check-log` 0 码返回。
```
