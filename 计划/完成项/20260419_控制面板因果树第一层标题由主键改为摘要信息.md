# 20260419 控制面板因果树第一层标题由主键改为摘要信息

## 本次完成

- 将因果树第一层节点标题从 `主键` 改成 `摘要信息`。
- `主键` 不再占据第一层展示位置，而是下沉到字段明细中显示。
- 这次调整同时作用于：
  - `抽象因果` 节点
  - `实例因果` 节点

## 当前效果

- 因果页展开前看到的是更容易理解的摘要信息。
- 继续展开后，仍然可以在字段明细里看到对应的 `主键`。

## 验证

- 本轮仅做编译级验证，未做运行态点击验证。
