# 自检缺口特征化与去重计划 v0.1

## 1. 目标

自检线程已经能独立发现缺口并上报。本计划负责继续收口：

```text
用自检缺口条件包替代旧派生需求消息作为主载体。
实现方法结构学习积压检测。
防止重复生成同一方法结构缺口需求。
将缺口语义迁移到抽象特征和实例特征槽。
```

## 2. 子计划

### P1 自检缺口条件包

目标结构：

```text
自检缺口条件包
  缺口类型
  来源线程
  来源对象
  来源方法能力值
  来源方法头节点
  目标特征类型
  缺失结构类型
  建议需求类型
  证据集合
  严重程度
  是否建议生成需求
```

验收：

```text
缺口类型使用抽象特征点区间值。
来源对象使用可解析引用。
自检线程只生成缺口条件包。
自我线程负责把缺口条件包转换成需求节点。
```

### P2 旧派生需求消息降级

目标：

```text
旧任务管理派生需求消息只保留为兼容入口。
自检主链不再以字符串消息包作为权威语义。
```

验收：

```text
自检报告载荷包含特征化缺口候选。
字符串摘要只用于显示和调试。
```

### P3 方法结构学习积压检测

判断依据：

```text
需求树中已有同目标方法的结构缺口需求。
已有对应任务处于筹办、执行或等待子需求。
已有选中自我能力值为自我_尝试学习的任务正在处理该方法。
已有未完成的学习尝试运行事实。
已有候选条件结果对处于候选或试用状态。
```

验收：

```text
同一目标方法已有学习积压时，自检线程不重复提交同类方法结构缺口。
```

### P4 去重账与需求树协同

目标：

```text
内存去重账只做短期抑制。
需求树和任务状态负责长期去重。
```

验收：

```text
重启或长时间运行后，仍能通过需求树和任务状态识别已存在的缺口处理。
```

## 3. 完成口径

```text
自检缺口有特征化主载体。
旧派生需求消息降级为兼容。
方法结构缺口不会对同一方法反复生成重复需求。
```

## 4. 执行记录

执行日期：2026-05-04

代码落点：

```text
自我类.特征定义.ixx
自我类.特征定义.cpp
自我线程模块.消息协议.ixx
自我线程模块.impl.cpp
自检线程模块.impl.cpp
```

完成内容：

```text
新增自检缺口候选相关抽象特征：
  自检范围
  自检缺口类型
  自检来源对象类型
  自检来源方法能力
  自检来源方法头节点
  自检目标特征类型
  自检缺失结构类型
  自检建议需求类型
  自检证据类型
  自检严重程度
  自检是否建议生成需求

扩展 自检缺口候选消息：
  缺口类型值
  来源对象类型值
  来源方法能力值
  来源方法头节点值
  目标特征类型值
  缺失结构类型值
  目标主键
  目标主键文本
  目标指针
  证据值
  严重程度值
  建议需求类型值
  建议生成需求值
  幂等键
  证据摘要

新增 同步自检缺口候选消息语义槽 和 同步自检报告消息语义槽。

自检线程主链改为先构造自检缺口候选消息，再投递自检报告。
旧任务管理派生需求消息不再由自检线程直接构造。
自我线程收到自检报告后，才把候选包转换成兼容派生需求消息并落入需求树。
```

去重收口：

```text
内存去重账继续提供短期抑制。
方法结构学习积压检测已扫描任务树。
本轮补充需求树扫描：
  若已有同一来源方法且未截止的缺口需求，则自检线程不重复提交同类方法结构缺口。
```

验证：

```text
msbuild .\鱼巢.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
.\x64\Debug\鱼巢.exe --selfcheck-log
.\x64\Debug\鱼巢.exe --summary
git diff --check
git diff --unified=0 | Select-String -Pattern '^\+.*(调用点|reason|callpoint)'
```

验证结果：

```text
Debug x64 生成成功，0 个错误。
自检命令返回成功。
摘要命令返回成功。
差异检查未发现新增调用点 / reason / callpoint 文本。
```

验收结论：

```text
本计划完成。
运行事实索引和更细的学习尝试运行事实去重由后续运行事实树索引计划承接。
```
