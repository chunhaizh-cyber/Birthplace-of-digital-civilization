# 当前步骤复用与最近结果命中等前移为正式结构字段

完成时间：2026-04-18

## 本步目标

继续收窄 `任务管理任务模块` 的局部推导，把以下 4 个运行时判据前移成正式结构字段，并让治理证据层优先读取这些字段：

- `当前步骤可复用`
- `最近结果命中当前治理`
- `当前动作长时间无推进`
- `当前动作需重试`

目标口径：

- `任务树/方法树` 表达结构
- `二次特征` 表达当前判断证据
- `原语` 只解释这些证据
- `任务管理任务模块` 只做编排

## 本步改动

### 1. 正式字段前移

在 [D:\鱼巢\任务管理任务模块.ixx:560](D:/鱼巢/任务管理任务模块.ixx:560) 的 `结构_任务管理方法需求位特征面` 中新增：

- `当前步骤可复用`
- `最近结果命中当前治理`
- `当前动作长时间无推进`
- `当前动作需重试`

在 [D:\鱼巢\任务管理任务模块.ixx:677](D:/鱼巢/任务管理任务模块.ixx:677) 的 `结构_任务管理上下文` 中同步新增同名字段。

### 2. 宿主治理特征写回/读取打通

在 [D:\鱼巢\任务管理任务模块.impl.cpp:796](D:/鱼巢/任务管理任务模块.impl.cpp:796) 到 [D:\鱼巢\任务管理任务模块.impl.cpp:814](D:/鱼巢/任务管理任务模块.impl.cpp:814) 新增正式治理特征类型：

- `任务管理_当前步骤可复用`
- `任务管理_最近结果命中当前治理`
- `任务管理_当前动作长时间无推进`
- `任务管理_当前动作需重试`

并在：

- [D:\鱼巢\任务管理任务模块.impl.cpp:7575](D:/鱼巢/任务管理任务模块.impl.cpp:7575) 到 [D:\鱼巢\任务管理任务模块.impl.cpp:7620](D:/鱼巢/任务管理任务模块.impl.cpp:7620)
- [D:\鱼巢\任务管理任务模块.impl.cpp:13137](D:/鱼巢/任务管理任务模块.impl.cpp:13137) 到 [D:\鱼巢\任务管理任务模块.impl.cpp:13172](D:/鱼巢/任务管理任务模块.impl.cpp:13172)

完成了稳定字段写回与上下文读取。

### 3. 需求位特征与上下文字段同步

在 [D:\鱼巢\任务管理任务模块.impl.cpp:2882](D:/鱼巢/任务管理任务模块.impl.cpp:2882) 的 `私有_P0_刷新步骤与学习结构字段(...)` 中，新增“治理证据 -> 需求位特征/上下文”的同步。

在 [D:\鱼巢\任务管理任务模块.impl.cpp:15776](D:/鱼巢/任务管理任务模块.impl.cpp:15776) 的 `刷新任务管理方法需求位(...)` 中，正式把这 4 个字段从上下文前移到需求位特征面。

### 4. 治理证据生成改成结构优先

在 [D:\鱼巢\任务管理任务模块.impl.cpp:10655](D:/鱼巢/任务管理任务模块.impl.cpp:10655) 的 `私有_P1_刷新任务治理二次特征证据(...)` 中，以下判据已改成“结构字段优先，局部推导兜底”：

- `当前动作长时间无推进`
- `当前步骤可复用`
- `最近结果命中当前治理`
- `当前动作需重试`

这样治理证据层不再总是从节点关系和局部状态重新硬算一遍。

## 结果

本步完成后，这 4 个判据已经形成：

`宿主治理特征 -> 上下文 -> 需求位特征 -> 治理二次特征证据`

的正式链路。

这意味着：

- 下一轮治理可以优先消费上轮稳定写回的结构字段
- 需求位刷新和步骤结果子链不必再默认回到局部节点关系重推
- 证据层开始更像“收敛层”，而不是“重复计算层”

## 仍保留的兜底

当前仍保留了“字段为假时的最小局部推导兜底”。这是刻意保守处理，用来避免历史账本尚未写回这些字段时直接丢判断。

下一步可以继续推进：

- 继续把这些兜底缩到更小
- 让 `治理证据生成层` 更少依赖当前轮节点关系
- 进一步把 `步骤结果子链` 和 `学习触发` 收口成只读结构字段与证据

## 验证

已执行：

```powershell
msbuild D:\鱼巢\鱼巢.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64 /m
```

结果：

- `0 error`
- `0 warning`

本轮未做运行态回放验证。
