# 20260419 控制面板因果信息改为树形显示并以主键为标题

> 2026-05-22 验证口径修订：
> 后续所有计划的验证统一改为日志模块验证，以真实运行产生的日志、状态快照、动作动态与错误日志为证据。
> 严禁新增或依赖模拟测试、夹具测试、伪造样本、伪造入口、伪造动作动态或 `--*-check` 回归命令。
> 历史计划中已有的回归命令、夹具和模拟测试描述仅作为历史记录，不再作为当前执行或验收依据。

## 本次完成

- 将 `因果信息` 页面从列表显示改成树形显示。
- 顶层按两组组织：
  - `抽象因果模板`
  - `实例因果(最多50条)`
- 每条因果节点标题改为对应节点的 `主键`。
- 因果摘要统一做简化处理，删除自然语言里的 `通常` 两字。
- 每条因果节点下方继续展开字段明细，包含：
  - 摘要
  - 关键时间
  - 锚点/来源
  - 条件/结果
  - 证据实例
  - 稳定度/成立失败次数

## 当前效果

- `因果信息` 页面不再只是两张列表，而是可以按树形展开查看单条因果。
- 抽象因果和实例因果都能继续展开到字段级。
- 实例因果依然限制最多显示 50 条。

## 验证

- 本轮仅做编译级验证，未做运行态点击验证。
