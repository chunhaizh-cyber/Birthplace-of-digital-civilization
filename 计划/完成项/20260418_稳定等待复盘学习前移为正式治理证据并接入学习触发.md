# 20260418 稳定等待复盘学习前移为正式治理证据并接入学习触发

## 本次目标

继续沿着：

- 任务树/方法树负责表达结构
- 二次特征负责表达当前判断证据
- 原语只负责解释这些证据
- 任务管理任务模块只做编排

这条主线，把学习触发里仍然偏“本地私算”的 `稳定等待态复盘学习` 前移成正式结构字段与治理二次特征证据。

## 本次完成

1. 在 `D:\鱼巢\任务管理任务模块.ixx` 中，为以下正式结构新增字段：
   - `结构_任务治理二次特征证据::稳定等待态复盘学习`
   - `结构_任务管理方法需求位特征面::稳定等待态复盘学习`
   - `结构_任务管理上下文::稳定等待态复盘学习`

2. 在 `D:\鱼巢\任务管理任务模块.impl.cpp` 中新增宿主治理稳定特征：
   - `任务管理_稳定等待态复盘学习`

3. 打通了这条字段的贯通链：
   - 治理证据刷新
   - 稳定字段写回
   - 上下文读取
   - 需求位刷新
   - 方法证据沉淀

4. `私有_稳定等待态复盘学习(...)` 不再自己重新拆一套判断，改为转读
   - `私有_P0_稳定等待态复盘学习(...)`

5. `私有_推导学习承接类型(...)` 开始直接消费这条正式证据：
   - 命中时优先推导为 `广泛学习`

## 当前效果

- `稳定等待态复盘学习` 已经从“学习链内部的局部推导”前移成
  “结构字段 + 宿主治理稳定特征 + 治理二次特征证据”。
- 学习触发现在更明确地走“证据 -> 解释 -> 决策”这条线。
- 方法条件/结果证据索引也开始显式承接这条学习相关证据。

## 验证

已执行：

```powershell
msbuild D:\鱼巢\鱼巢.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64 /m
```

结果：

- `0 error`
- `0 warning`

## 尚未完成

1. `稳定等待态复盘学习` 的生成仍然在 `治理证据刷新` 阶段基于现有上下文做最小推导，后续还可以继续往更稳定的结构字段与因果证据上收。
2. 学习链虽然已经开始优先读这条证据，但运行态回放验证还没做。
