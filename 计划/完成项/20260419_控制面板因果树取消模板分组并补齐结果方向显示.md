## 完成事项

- 取消控制面板因果信息页里的 `抽象因果模板(...)` 外层分组，改为将每个抽象因果节点直接挂到 `因果信息` 根节点下。
- 保留 `未归纳实例因果` 作为兜底分组，仅收纳未挂到任何抽象因果模板上的实例因果。
- 因果摘要继续保持 `条件: ... | 动作: ... | 结果: ...` 结构，并补充 `方向: ...`。
- 结果方向口径改为：
  - `结果状态比初始状态大`
  - `结果状态比初始状态小`
  - `结果状态与初始状态相同`
  - 无法比较时显示 `结果状态方向未知`
- 实例因果的方向通过 `结果状态` 对应匹配 `条件状态` 后比较得到。
- 抽象因果模板的方向通过其证据实例的结果方向做多数归纳得到。
- 因果树字段明细中新增 `结果方向=...` 展示。

## 主要修改文件

- `D:\鱼巢\控制面板类.cpp`

## 验证

- 运行：
  - `msbuild D:\鱼巢\鱼巢.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64 /m`
- 结果：
  - `0 error / 0 warning`

## 备注

- 这轮仍然只做了编译级验证，还没做控制面板点击展开的运行态验证。
