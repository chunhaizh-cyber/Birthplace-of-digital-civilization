# 20260419 抽象因果开始直接补齐方法骨架并接入任务管理

## 本次目标

让已经稳定归并出来的抽象因果，不再只停留在观察与统计层，而是开始直接补齐方法头、方法条件节点、方法结果节点，成为任务管理编排的实际燃料。

## 本次完成

1. 在 `任务管理任务模块.impl.cpp` 新增“抽象因果 -> 方法结构”桥接 helper。
   - 按抽象因果的 `动作名称` 补方法头 `动作名`
   - 在可判定为方法节点时，复用抽象因果的 `动作模板` 来补方法 `动作句柄`

2. 抽象因果的 `条件模板` 开始桥接到方法条件场景。
   - 将条件特征模板写入方法条件场景的二次特征
   - 形成正式的 `条件判定索引`

3. 抽象因果的 `状态迁移模板` 开始直接写入方法结果摘要索引。
   - 主状态迁移会补到 `结果主键`
   - 左状态会补到 `结果初始状态`

4. 方法条件节点与方法结果节点开始自动补齐。
   - 自动确保条件场景/结果场景存在
   - 自动创建或更新方法条件节点
   - 自动创建或更新方法结果节点

5. 学习路径与运行时路径都已接入。
   - `私有_P2_补齐学习方法证据索引(...)`
   - `私有_P2_补齐运行方法证据索引(...)`
   现在都会优先吸收 `最近抽象因果`

## 当前结果

任务管理主链已经从：

`抽象因果只做观察摘要`

推进到：

`抽象因果开始直接补齐方法骨架并进入任务管理编排`

这意味着前面稳定下来的 9 条抽象因果，已经开始真正影响方法生成，而不是只停留在控制面板和学习统计里。

## 验证

已执行：

```powershell
msbuild D:\鱼巢\鱼巢.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64 /m
```

结果：

- `0 error`
- `0 warning`

## 未完成

- 还没有做运行态回放验证
- 还没有直接统计“哪条抽象因果实际补出了哪些方法条件/结果节点”

