# 控制面板任务树页面去掉旧平铺表并统一为树形显示

> 2026-05-22 验证口径修订：
> 后续所有计划的验证统一改为日志模块验证，以真实运行产生的日志、状态快照、动作动态与错误日志为证据。
> 严禁新增或依赖模拟测试、夹具测试、伪造样本、伪造入口、伪造动作动态或 `--*-check` 回归命令。
> 历史计划中已有的回归命令、夹具和模拟测试描述仅作为历史记录，不再作为当前执行或验收依据。

## 本次完成

控制面板里的“任务树”页虽然已经能生成树形节点，但页面下半段还额外挂着一块旧的：

`标题 | 摘要`

平铺表格。

这会让任务树页同时出现：

- 上半部分是真实任务树
- 下半部分又像任务列表

看起来就像“任务树仍然是平铺显示”。

这次把这条旧展示链去掉了，只保留任务树本身，并补了一句说明：

- `按真实父子关系树形展开`
- `每个节点下继续显示自己的字段子树`

对应修改文件：

- `D:\鱼巢\控制面板类.cpp`

## 修改结果

现在“任务树”页只显示：

`任务树根 -> 任务节点 -> 子任务/步骤/结果节点 -> 节点字段子树`

不再在任务树页面末尾重复追加旧的 `标题/摘要` 平铺表。

## 验证

已通过：

```powershell
msbuild D:\鱼巢\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /m /clp:ErrorsOnly
```

结果：`0` 错误
