## 完成项

任务管理代码已按新规范收口到 `目标 -> 筹办 -> 执行`。

### 本次完成

1. 将 `可执行判断 / 允许执行判断` 从独立主阶段合并为 `执行入口闸门`，并纳入筹办阶段内部完成。
2. 删除任务管理主链中“本地直接创建业务子任务 / 补条件子任务 / 学习子任务”的旧驱动。
3. 将筹办缺口统一改成正式 `派生需求消息` 上报，由自我线程据此判断是否可满足并决定后续实例化。
4. 工作线程现在会针对：
   - 补条件需求
   - 学习需求
   - 业务派生需求
   生成正式 `结构_任务管理派生需求消息` 并上行。
5. 自我线程不再根据 `已创建子任务` 兼容标志判断“有派生需求待实例化”，而是改为检测真实上行消息中的 `派生需求消息`。

### 当前保留

1. 部分旧 helper 仍保留在代码中，作为后续彻底删除前的兼容壳。
2. 旧的 `已创建业务子任务 / 已创建补条件子任务 / 已创建学习子任务` 字段仍保留，但已不再作为主链裁决依据。

### 验证

已通过：

```powershell
msbuild D:\鱼巢\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /m /clp:ErrorsOnly
```
