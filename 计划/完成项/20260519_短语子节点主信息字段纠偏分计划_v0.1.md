# 短语子节点主信息字段纠偏分计划 v0.1

> 2026-05-22 验证口径修订：
> 后续所有计划的验证统一改为日志模块验证，以真实运行产生的日志、状态快照、动作动态与错误日志为证据。
> 严禁新增或依赖模拟测试、夹具测试、伪造样本、伪造入口、伪造动作动态或 `--*-check` 回归命令。
> 历史计划中已有的回归命令、夹具和模拟测试描述仅作为历史记录，不再作为当前执行或验收依据。

更新时间：2026-05-19

对应主信息：`短语子节点主信息类`

## 审查结论

`短语子节点主信息类` 表达短语结构中的子节点关系，属于语言结构层。

## 纠偏动作

```text
P1. 确认字段只用于短语子结构和顺序关系。
P2. 若需要表示语义关系，优先走语义入口或二次特征，不扩写短语子节点字段。
P3. 不作为任务 / 需求 / 方法关系的替代结构。
```

## 验收

```powershell
rg -n "短语子节点主信息类" -S 语素主信息类.h 语素类.cpp
```

通过条件：

```text
短语子节点只表达语言结构关系。
```

## 执行记录

### 2026-05-19 首轮执行

```text
已完成：
- 标注短语子节点只表达短语结构中的语言槽位关系。
- 标注 比较入口 / 结果词 不是治理比较和业务结果，语义关系应由语义入口、二次特征或因果链表达。
- 标注短语子节点比较只服务结构节点查找与排序，不作为任务、需求、方法关系判断。
- 将短语子节点入口指针比较改为安全顺序比较，避免裸指针差值。

验收：
- rg -n "短语子节点主信息类|语素_短语子节点_比较入口|语素_短语子节点_结果词" -S 语素主信息类.h 语素类.cpp
- rg -n "短语子节点只表达|语言槽位|治理比较|业务结果|任务、需求、方法关系" -S 语素主信息类.h 语素类.cpp
- msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
- .\x64\Debug\鱼巢.exe --demand-tree-growth-check

结果：
- Debug x64 构建通过，仅有 LNK4075 既有警告。
- 需求树生长回归通过。
```
