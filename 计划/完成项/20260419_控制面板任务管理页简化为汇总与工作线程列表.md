# 20260419 控制面板任务管理页简化为汇总与工作线程列表

## 本次目标

按当前口径简化 `任务管理` 页面：

- 第一部分只显示 `任务管理任务界面信息` 汇总
- 第二部分显示 `工作线程列表`
- 第一部分重点呈现：
  - 汇总
  - 当前任务管理任务实体数量
  - 不同状态数量
  - 收发消息情况汇总

## 已完成

### 1. 工作线程壳快照补齐消息计数

在 `D:\鱼巢\任务模块.管理工作线程.ixx` 的 `结构_工作线程实例快照` 中新增：

- `累计收到请求数`
- `累计收到控制请求数`
- `累计发送上行消息数`
- `最近一次上行消息数`

并在工作线程壳中完成累计更新：

- 入队请求时增加 `累计收到请求数`
- 完成推进时根据桥接结果更新：
  - `累计收到控制请求数`
  - `累计发送上行消息数`
  - `最近一次上行消息数`

### 2. 控制面板快照新增任务管理汇总字段

在 `D:\鱼巢\控制面板类.ixx` 的 `结构_控制面板快照` 中新增：

- `任务管理任务实体数量`
- `任务管理状态数量摘要`
- `任务管理消息汇总摘要`
- `任务管理汇总列表`
- 以及对应的工作线程消息计数字段

### 3. 第一部分改为汇总视角

在 `D:\鱼巢\控制面板类.cpp` 的 `私有_构建任务管理专项快照(...)` 中：

- 统计当前任务管理界面涉及的任务实体：
  - 当前管理任务
  - 当前宿主任务
  - 当前步骤
  - 最近结果
  - 当前派生子任务
  - 当前步骤节点
  - 最新结果节点
- 去重后作为 `当前任务管理任务实体数量`
- 按 `任务状态` 统计数量，形成 `任务管理状态数量摘要`
- 读取工作线程壳消息计数，形成 `任务管理消息汇总摘要`
- 生成 `任务管理汇总列表`

### 4. 页面展示口径调整

在 `D:\鱼巢\控制面板类.cpp` 中：

- `task-mgmt` 页面第一部分改成只显示：
  - 汇总摘要
  - 汇总列表
- 删除原来的三栏：
  - 输入面
  - 触发环节
  - 输出面
- 第二部分继续保留 `工作线程列表`

### 5. 文本摘要同步简化

在 `渲染任务管理摘要(...)` 中同步改为：

- 汇总
- 当前任务管理任务实体数量
- 不同状态数量
- 收发消息情况汇总
- 工作线程列表

### 6. 菜单徽标调整

左侧 `任务管理` 菜单徽标改为：

- `任务实体数 / 工作线程数`

## 当前口径说明

这里的 `任务管理任务实体数量` 是 **当前任务管理界面涉及的去重任务节点数**，不是全任务树总数。

它用于回答：

- 当前这一轮任务管理到底围绕哪些任务实体在运行
- 这些实体的状态分布是什么

## 验证

已执行：

```powershell
msbuild D:\鱼巢\鱼巢.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64 /m
```

结果：

- `0 error`
- `0 warning`

## 后续建议

- 如果后面需要再细化，可再补：
  - 当前任务管理实体来源分组
  - 各状态实体明细点击展开
  - 工作线程上行消息类型分布
