# 20260421 工作线程统一执行期派生需求草案生成并替换硬编码上报

## 完成内容
- 在 `D:\鱼巢\任务模块.管理工作线程.ixx` 新增了：
  - `结构_执行期派生需求草案`
  - `私有_加入执行期派生需求草案(...)`
  - `生成执行期派生需求草案集(...)`
- 把工作线程末尾原先按三段硬编码判断的：
  - `已上报补条件需求`
  - `已上报学习需求`
  - `已上报业务派生需求`
  收成统一的“执行期派生需求草案生成器”。
- 当前草案生成规则会综合：
  - `当前步骤语义类型`
  - `当前主方法需求位`
  - `当前学习承接决策`
  - 以及已有的 `已上报*需求` 标志
  自动生成对应的派生需求消息草案，再统一发成上行消息。

## 影响文件
- `D:\鱼巢\任务模块.管理工作线程.ixx`

## 结果
- 任务管理工作线程不再手写三段“补条件/学习/业务派生”需求上报。
- “执行期缺口 -> 派生需求消息” 已经收成一处统一逻辑。
- 这一步实现的是“自动生成补链需求逻辑”，不是“直接实例化前置任务链”。

## 后续空档
- 自我线程目前仍然只是识别 `有派生需求待实例化`，还没有把这些草案统一实例化成前置任务头。
- 下一步应把自我线程消费派生需求消息后的实例化逻辑，也收成同一套规则驱动入口。

## 验证
- 已执行：
  - `msbuild D:\鱼巢\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /m /clp:ErrorsOnly`
- 结果：
  - 编译通过，`0` 错误
