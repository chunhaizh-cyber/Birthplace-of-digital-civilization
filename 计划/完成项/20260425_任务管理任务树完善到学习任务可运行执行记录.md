# 2026-04-25 任务管理任务树完善到学习任务可运行执行记录

## 来源计划

- `计划/待完成项/20260425_任务管理任务树完善到学习任务可运行计划_v0.1.md`

## 执行目标

- 按最新口径执行计划：自我线程同步需求并创建任务，任务管理任务界面线程调度工作线程并收取消息，工作线程只执行当前步骤方法，学习任务通过需求链路真正进入调度和执行。

## 施工记录

- 已建立执行记录，作为本轮变更台账。
- 已在 `计划/待完成项/20260425_任务管理任务树子任务暂存/` 拆分 5 个后续子任务，作为主计划执行过程中的临时小票。
- 已移除 `自我初始化模块.cpp` 中旧的直接创建学习任务帮助函数路径，保留新口径：任务筹办/执行只提出补方法或学习类需求，学习任务由自我线程按需求创建。
- 已补齐需求结构化字段链路：需求主信息、需求树更新指令、任务管理派生需求消息、自我线程需求落点、控制面板字段展示均携带方法需求位、本能能力缺口、主观察特征、被需求当前状态和需求状态。
- 已收窄工作线程执行后职责：当前步骤方法返回转学习时，只上报等待自我线程按需求创建学习任务，不再执行后补跑完整治理。

## 验证记录

- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /m` 已通过，0 错误，0 警告。
- `.\x64\Debug\鱼巢.exe --task-mgmt-summary` 本轮超过 124 秒未退出，后台残留进程已停止；该问题已登记为子任务 `01_摘要超时与主循环退出.md`，下一步优先处理。

## 2026-04-25 14:13 继续执行记录

- 已修正学习任务等待态链路：`学习调度模块` 将 `提交/等待正式提交放行` 的学习账本放入等待表，不再作为可执行候选反复派发。
- 已扩展 `结构_任务管理学习推进结果`，向工作线程回传学习任务是否等待、是否失败、是否终结，以及当前学习阶段/状态文本。
- 已修正工作线程学习分支：学习任务由 `执行一步学习推进` 独占执行，不再落入普通当前步骤方法执行器，避免同一学习步骤被双执行。
- 已修正自我线程闸门：学习任务进入正式提交闸门等待后，不再被任务管理树闸门唤醒为 `待重筹办`，也不再继续派发到工作线程空转。
- 最新验证通过：
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /m`：0 错误，0 警告。
- `.\x64\Debug\鱼巢.exe --task-mgmt-summary`：约 1.4 秒退出，`需求=5 | 任务节点=10 | 学习账本=1 | 去向=保持等待 | 总控=宿主保持等待`。
- `.\x64\Debug\鱼巢.exe --learning-summary`：约 1.3 秒退出，`学习任务表=1 | 就绪=0 | 等待=1 | 执行中=0`，当前学习任务处于 `提交/挂起/等待正式提交放行`。
- 日志复核：最新尾部不再出现新的 `运行状态=故障` 或 `结果=失败`，结果节点稳定在同一个节点，不再按轮增长。

## 2026-04-25 任务管理实例参数与条件场景边界

- 已将自我线程派发改为先确保任务管理任务实例，再把该实例交给任务管理界面线程；任务管理任务实例通过 `管理对象任务` 指向被治理任务。
- 已按三层口径解析本能动作上下文的 `目标存在`：管理自身时使用任务管理任务虚拟存在，管理学习时使用学习对象方法虚拟存在，管理任务时使用被管理任务虚拟存在。
- 已收窄主任务调度：非“管理自身”的任务管理任务实例不再被主调度器当作业务任务直接选中，而是在其被管理任务被选中后作为治理 wrapper 运行。
- 已修正执行桥的输入语义：筹办和治理本能方法不消耗方法的 `条件场景`，只有非治理本能方法的真实任务执行阶段才把方法 `条件场景` 作为输入参数传入。

## 验证记录

- `MSBuild.exe 鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /m:1 /v:minimal` 已通过，生成 `D:\鱼巢\x64\Debug\鱼巢.exe`。

## 2026-04-25 结果场景承载变化明细

- 已按“变化结果记录在结果场景中”的口径收口：本能动作默认把结果存在集场景作为结果事件集场景，避免再把事件细节散落到执行输入场景。
- 已修正执行桥：筹办、执行与错误回传均优先采用方法返回的结果场景；任务执行结果只保留结果场景指针作为细账入口。
- 已修正工作线程结果上行：结果状态消息写入 `结果节点场景主键`，上游可据此回读本能函数落在结果场景中的缺条件、执行结果和特征变化账。
- 最新验证通过：`MSBuild.exe 鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /m:1 /v:minimal`，生成 `D:\鱼巢\x64\Debug\鱼巢.exe`。

## 2026-04-25 本能函数运行账写入方法虚拟存在

- 已在 `本能动作管理类::运行` 外层接入统一运行记账壳：本能函数每次运行都会确保方法虚拟存在，并记录运行状态、运行阶段、请求 ID、开始/结束时间、耗时、错误码、成本、质量、结果场景、输入场景、目标存在和宿主任务。
- 已补齐四段式运行特征：确保、解析绑定、执行、输出四个阶段的状态与耗时写入方法虚拟存在；功能结果、缺口和特征变化仍保留在结果场景。
- 已将 `结构_方法执行结果` 和任务管理结果状态消息扩展为携带方法虚拟存在入口，工作线程上行消息同时带回 `结果节点场景主键` 和 `方法虚拟存在主键`。
- 最新验证通过：`MSBuild.exe 鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /m:1 /v:minimal`，生成 `D:\鱼巢\x64\Debug\鱼巢.exe`。
- `.\x64\Debug\鱼巢.exe --task-mgmt-summary` 正常退出，摘要为 `需求=2 | 任务节点=3 | 方法=34 | 学习账本=0`。

## 2026-04-25 删除摘要字段与旧摘要索引

- 已删除需求派生链路中的 `需求摘要/派生需求摘要` 数据字段，派生需求只保留结构化状态、特征、方法需求位、本能缺口和等待原因。
- 已删除任务执行记录中的自然语言摘要字段，执行记录只保留时间、前后状态、方法、输入输出状态和错误码。
- 已将方法主信息中的 `结果摘要索引` 改为 `结果方向索引`，保留可复用的结果方向证据，但不再使用“摘要”作为方法结构语义。
- 已删除任务旧占位字段 `评估摘要存在` 和任务旧结果摘要索引，并同步控制面板字段显示。
- 已将任务管理消息协议和工作线程协议中的若干摘要字段改为 `文本/说明`，避免摘要继续作为结构合同字段。
- 最新验证通过：`MSBuild.exe 鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /m:1 /v:minimal`，生成 `D:\鱼巢\x64\Debug\鱼巢.exe`。
- 运行验证仍存在旧问题：`.\x64\Debug\鱼巢.exe --task-mgmt-summary` 与 `--learning-summary` 均返回访问冲突 `EXIT=-1073741819`，该崩溃在本次摘要删除前已经存在，后续需继续沿派生需求落账路径排查。

## 2026-04-25 筹办/执行分别产生方法与条件缺口

- 已将 `执行任务` 的主路径收口为：先由筹办按任务/需求目标特征定位候选方法，方法不存在或动作骨架缺失时由筹办产生补方法缺口；方法已有动作骨架后再进入执行。
- 已将条件缺口收回到 `执行方法_同步等待` 内部：执行方法根据方法条件节点实例化条件场景，缺少条件节点时产生 `方法条件节点数量: 当前值 -> 1` 的补条件需求。
- 已细化筹办补方法返回：目标方法已有占位但缺动作骨架时，返回 `方法是否可用: 0 -> 1` 的方法虚拟存在状态需求，并让上行派生需求带上方法虚拟存在字段。
- 已删除“本能函数返回缺方法但没带状态时自动补成当前方法可用需求”的旧兜底，避免把筹办找不到目标方法误归因到筹办本能自身。
- 已补齐目标特征兜底：需求状态和任务目标状态没有特征时，按 `任务_状态` 创建目标占位方法，使根需求/任务管理任务也能进入统一方法查找链。
- 已调整任务筹办本能：筹办负责选中或创建目标方法，并在缺动作骨架时提出补方法；目标方法已有动作骨架但缺条件节点时，筹办切到 `任务执行一步`，由执行方法内部提出补条件。
- 最新验证通过：`MSBuild.exe 鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /m:1 /v:minimal`，生成 `D:\鱼巢\x64\Debug\鱼巢.exe`。
- 运行验证：`.\x64\Debug\鱼巢.exe --task-mgmt-summary` 仍返回访问冲突 `EXIT=-1073741819`；但日志已出现新链路证据：`方法=17 | 主观察=方法是否可用 | 当前状态=L1 | 目标状态=L2 | 等待原因=执行方法发现当前方法虚拟存在尚未达到 方法是否可用=1`，说明方法可用需求已经由执行目标方法内部产生。
