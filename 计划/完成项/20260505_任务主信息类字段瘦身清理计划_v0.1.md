# 2026-05-05 任务主信息类字段瘦身清理计划 v0.1

## 1. 计划目标

本计划目标是清理 `D:/鱼巢/任务主信息类.h` 中不再符合新任务模型的字段。

新任务模型固定为：

```text
需求树是主结构。
每个需求节点对应一个任务头。
任务只有筹办和执行两个核心阶段。
筹办阶段选择自我能力值。
执行阶段根据自我能力值配对参数并调用本能函数。
缺口生长为子需求。
```

因此，`任务主信息类` 最终应只保留最小运行承载字段：

```text
任务身份
对应需求
任务虚拟存在
当前阶段
选中自我能力值
```

旧的步骤节点、结果节点、任务树父子链、方法首节点长期缓存和旧学习任务字段，应删除。若字段仍被旧执行链活跃读取，不再用保留字段遮蔽问题，应把阻塞点记录为下一轮旧链拆除目标。

## 2. 上位规则

1. 不用字段删除掩盖真实断点。
2. 字段删除前必须先找到所有读写点。
3. 可以先暂停并提出结构问题，不允许用默认成功、白名单或兜底通过遮蔽问题。
4. 语义字段不得用新枚举或新结构体替代；长期语义应迁移到抽象特征和实例特征。
5. 结构体字段只允许作为运行期缓存、消息包或局部算法缓存。
6. 方法首节点只能作为运行期缓存，不能作为任务长期权威语义。
7. 删除字段后必须通过编译，并用日志模块确认新的断点和旧断点变化。

## 3. 当前字段分层

### 3.1 保留为新主链核心字段

这些字段符合当前模型，暂不删除：

```text
名称
类型
任务角色
状态
当前阶段
创建时间
启动时间
完成时间
对应需求
任务虚拟存在
场景
目标状态
结果状态信息
选中自我能力值
```

保留口径：

```text
权威语义优先写入任务虚拟存在实例特征。
```

### 3.2 暂保留但需要重命名或转义的字段

这些字段还有现实引用，但不是新模型长期权威语义：

```text
执行方法
等待缺口唤醒
管理对象任务
节点种类
```

处理目标：

```text
执行方法
  只允许作为运行期缓存；后续由选中自我能力值解析方法首节点。

等待缺口唤醒
  后续应由需求节点缺口状态和任务阶段推导。

管理对象任务
  阻塞在旧任务管理任务承接壳；删除前必须先拆该壳。

节点种类
  阻塞在旧步骤/结果任务树；删除前必须先拆旧步骤节点和结果节点。
```

### 3.3 旧步骤/结果链待拆字段

这些字段与新模型冲突，必须逐步移除：

```text
节点种类
任务树类型
结果角色
是否真根任务
是否常驻任务
是否预测外结果
允许切换方法
步骤序号
已重试次数
允许重试次数
超时截止时间
最近执行时间
最近完成时间
时间起
时间止
错误码
置信度_Q10000
安全权重
服务权重
动作名
动作句柄
主结果特征类型
条件主键
条件场景模板主键
结果主键
来源方法主键
来源需求
父任务
父任务头结点
所属任务头结点
来源任务
来源任务头结点
来源父步骤节点
来源父结果节点
来源步骤节点
下一跳步骤节点
后续任务
当前步骤节点
当前子任务头结点
最近实际结果节点
当前前置任务头结点
命中预测结果节点
相关任务
直属子任务头结点列表
并发前置任务头结点列表
派生子任务头结点列表
可用方法首节点
来源方法首节点
条件场景
结果场景
需求场景
方法条件节点
方法结果节点
对应方法结果节点
结果方向二次特征
命中后动作
结果初始状态
条件判定索引
```

这些字段删除前必须先替换以下旧依赖：

```text
当前步骤节点 -> 当前阶段
执行方法 -> 选中自我能力值解析方法
步骤节点结果 -> 运行虚拟存在和任务执行记录
任务树父子关系 -> 需求树父子关系
旧学习任务字段 -> 缺口需求和练习能力
```

## 3.4 已完成删除记录

```text
候选方法首节点
最近调度时间
统计
缺口目标方法首节点
父任务
来源步骤节点
后续任务
描述信息
完成口径
所属任务头结点
当前步骤节点
枚举_分支选择动作
```

验证口径：

```text
任务主信息类.h 不再声明上述字段。
代码中不再存在上述任务主信息字段读写点。
Debug x64 编译通过。
```

## 3.5 当前暂停点

继续删除以下字段会触发旧执行链拆除，不属于单纯字段瘦身：

```text
管理对象任务
任务角色::任务管理任务
节点种类
执行方法
```

原因：

```text
管理对象任务 / 任务角色::任务管理任务
  仍被旧任务管理任务承接壳用于定位被治理任务。

节点种类
  仍被旧步骤/结果任务树用于区分头结点、步骤节点、结果节点。

执行方法
  仍被旧步骤执行和结果追踪使用；删除前需要把执行链完全切到选中自我能力值。
```

下一轮不应再在任务主信息类中增加过渡字段，而应开拆：

```text
旧任务管理任务承接壳
旧步骤/结果任务树
旧执行方法缓存链
```

## 4. 清理总顺序

### 阶段一：字段读写点总表

目标：

```text
生成任务主信息字段读写矩阵。
确认每个字段是新主链、运行缓存、旧链待拆还是直接删除候选。
```

工作项：

```text
1. 用 rg 列出每个字段读写位置。
2. 标记读写来源模块。
3. 标记是否在工作线程、执行桥、自我线程、任务类、旧任务管理模块中使用。
4. 形成字段迁移表。
```

验收：

```text
每个字段都有去向：
  保留
  重命名
  迁移到特征
  迁移到需求树
  删除
  旧链待拆
```

### 阶段二：先替换执行主链字段

目标：

```text
工作线程不再依赖 当前步骤节点。
执行桥不再把步骤节点作为唯一可执行入口。
```

替换顺序：

```text
1. 工作线程读取 当前阶段。
2. 当前阶段 = 筹办 时，进入筹办协议。
3. 当前阶段 = 执行 时，读取 选中自我能力值。
4. 选中自我能力值解析方法首节点。
5. 执行阶段进入配对参数。
6. 配对成功后调用本能函数。
```

可删除候选：

```text
当前步骤节点
所属任务头结点
来源步骤节点
下一跳步骤节点
后续任务
最近实际结果节点
步骤序号
允许切换方法
```

验收：

```text
日志中不再出现“任务阶段执行协议未接入”。
工作线程日志不再以 当前步骤 为执行入口。
任务节点统计中 步骤节点 可以为 0 且不阻断推进。
```

### 阶段三：替换旧方法首节点权威字段

目标：

```text
任务长期保存 选中自我能力值。
方法首节点引用只作为运行期缓存。
```

迁移字段：

```text
执行方法
候选方法首节点
可用方法首节点
等待学习方法首节点
来源方法首节点
来源方法主键
动作句柄
动作名
```

替换口径：

```text
选中自我能力值 -> 确保本能方法 -> 方法首节点
```

验收：

```text
任务筹办成功后写入 选中自我能力值。
执行阶段不要求任务主信息长期保存方法首节点。
查找方法返回能力值，而不是方法首节点。
```

### 阶段四：删除旧结果节点字段

目标：

```text
结果不再作为任务节点子链存在。
结果写入运行虚拟存在、输出结果场景、任务执行记录。
```

迁移字段：

```text
结果角色
是否预测外结果
结果主键
结果场景
结果方向二次特征
命中预测结果节点
命中后动作
结果初始状态
对应方法结果节点
置信度_Q10000
错误码
最近实际结果节点
```

验收：

```text
任务完成判断来自需求状态和运行事实。
不再创建任务结果节点作为执行事实权威。
任务执行记录能追踪开始、结束、执行前后状态和错误码。
```

### 阶段五：删除旧任务树父子关系字段

目标：

```text
任务组织关系从需求树推导。
任务主信息不再保存旧任务树父子链。
```

迁移字段：

```text
父任务
父任务头结点
来源任务
来源任务头结点
来源父步骤节点
来源父结果节点
当前子任务头结点
当前前置任务头结点
相关任务
直属子任务头结点列表
并发前置任务头结点列表
派生子任务头结点列表
```

替换口径：

```text
父需求
子需求集合
需求节点.对应任务
需求节点.任务列表
```

验收：

```text
一个需求节点只产生一个权威任务头。
任务数量不会因为旧任务管理壳重复增长。
父子回传走需求树，不走任务树子链。
```

### 阶段六：清理旧学习字段

目标：

```text
学习不再作为特殊任务。
练习是缺口需求可选择的自我能力。
```

迁移字段：

```text
等待学习方法首节点
等待学习唤醒
转入学习次数
任务角色::学习任务
完成口径::方法已完善
```

替换口径：

```text
缺口需求
练习能力
候选条件结果对
方法结构缺口
```

验收：

```text
主链不再判断“学习任务”特殊类型。
练习通过选中自我能力值进入执行。
方法骨架缺口由自检或执行失败生成需求。
```

## 5. 字段删除批次建议

### 批次 A：纯旧步骤构造字段

优先删除：

```text
步骤序号
已重试次数
允许重试次数
允许切换方法
时间起
时间止
条件主键
条件场景模板主键
```

风险：

```text
任务类.cpp 创建旧步骤节点仍可能引用。
需要先删除或隔离旧创建步骤节点主链。
```

### 批次 B：旧结果节点字段

删除：

```text
结果角色
是否预测外结果
结果主键
结果场景
结果方向二次特征
命中预测结果节点
命中后动作
结果初始状态
对应方法结果节点
```

风险：

```text
任务类.cpp 和工作线程结果同步仍可能引用。
应先让运行虚拟存在和任务执行记录成为唯一执行事实。
```

### 批次 C：旧任务树关系字段

删除：

```text
父任务
父任务头结点
所属任务头结点
来源父步骤节点
来源父结果节点
来源步骤节点
下一跳步骤节点
后续任务
当前步骤节点
最近实际结果节点
当前前置任务头结点
直属子任务头结点列表
并发前置任务头结点列表
派生子任务头结点列表
```

风险：

```text
这是最大风险批次。
必须等工作线程阶段协议接通后再删。
```

### 批次 D：旧方法缓存字段

删除或重命名：

```text
候选方法首节点
可用方法首节点
来源方法首节点
来源方法主键
动作名
动作句柄
```

风险：

```text
自我线程、任务管理模块、执行桥仍可能通过这些字段判断方法。
必须先完成能力值解析方法首节点。
```

## 6. 必须先完成的前置计划

字段删除前，至少完成以下实现：

```text
1. 工作线程任务阶段协议。
2. 筹办阶段查找方法并写入选中自我能力值。
3. 执行阶段根据选中自我能力值配对参数。
4. 本能函数执行后写运行虚拟存在和任务执行记录。
5. 缺口需求去重，避免同一断点无限生长。
```

## 7. 验收清单

每删一批字段必须完成：

```text
1. rg 确认字段无引用。
2. 编译通过。
3. 运行 task-summary。
4. 用日志模块确认：
   - 没有旧当前步骤执行入口。
   - 没有恢复旧步骤链的提示。
   - 需求能入树。
   - 任务头数量与需求数量关系可解释。
   - 新断点是阶段、能力、参数或本能函数真实断点。
5. 不新增说明性参数文字进入代码。
```

## 8. 暂停条件

遇到以下情况必须暂停并提出问题：

```text
1. 某字段同时被新主链和旧链依赖，无法判断权威来源。
2. 删除字段会导致需求树无法生成任务。
3. 删除字段后只能通过默认成功或特殊放行恢复运行。
4. 字段语义找不到对应抽象特征或实例特征承载位置。
5. 工作线程必须重新创建旧步骤节点才能推进。
```

## 9. 最终目标形态

`任务主信息类` 最终应收敛为：

```text
任务身份：
  名称
  描述信息
  类型
  任务角色

阶段与状态：
  状态
  当前阶段
  完成口径
  创建时间
  启动时间
  完成时间
  最近调度时间
  统计

需求与虚拟存在：
  对应需求
  任务虚拟存在
  场景
  目标状态
  结果状态信息

能力与执行缓存：
  选中自我能力值
  方法配对结果
  参数配对结果
  可执行输入参数场景
  最近运行虚拟存在

执行事实缓存：
  执行记录列表
  最近执行记录

已迁移字段：
  基准优先级
  局部优先级偏移
  调度优先级
  运行次数
  成功次数
  失败次数
  连续失败次数
  最近错误码
```

一句话：

```text
任务主信息只保存任务壳必要缓存；任务组织归需求树，执行事实归运行虚拟存在，长期语义和统计调度字段归实例特征。
```

## 10. 执行记录

### 2026-05-05 第一批字段删除

已删除字段：

```text
相关任务
是否常驻任务
是否预测外结果
允许切换方法
来源父结果节点
命中后动作
```

删除依据：

```text
相关任务在声明之外无引用。
其余字段只被旧步骤/结果节点初始化或控制面板展示引用，不参与调度、执行、需求入树、方法选择或结果判断。
```

同步移除位置：

```text
任务主信息类.h
任务类.cpp
控制面板类.cpp
```

验证结果：

```text
msbuild Debug x64 通过。
task-summary 可运行。
日志仍显性显示当前真实断点：
  任务阶段执行协议未接入：当前任务没有可执行阶段上下文
未恢复旧当前步骤执行入口。
未出现 当前本能ID=6037 或 当前本能ID=6038 的旧任务两步壳执行。
```

### 2026-05-05 第二批字段删除

已删除字段：

```text
是否真根任务
方法条件节点
方法结果节点
```

删除依据：

```text
是否真根任务只被旧任务树挂接和控制面板展示使用，不再参与需求树任务关系判断。
方法条件节点、方法结果节点是旧任务步骤 / 结果节点上的实例缓存，已不再作为任务头权威字段。
方法树自身的 方法条件节点 / 方法结果节点 概念未删除。
```

同步移除位置：

```text
任务主信息类.h
任务类.cpp
任务管理任务模块.impl.cpp
控制面板类.cpp
自我线程模块.impl.cpp
```

验证结果：

```text
msbuild Debug x64 通过。
rg 确认 主信息.是否真根任务 / 主信息.方法条件节点 / 主信息.方法结果节点 无剩余引用。
task-summary 可运行。
```

### 2026-05-05 第三批字段特征迁移

已迁移为任务虚拟存在实例特征的字段：

```text
基准优先级
局部优先级偏移
调度优先级
运行次数
成功次数
失败次数
连续失败次数
转入学习次数
最近错误码
```

新增任务虚拟存在特征槽：

```text
任务基准优先级
任务局部优先级偏移
任务调度优先级
任务运行次数
任务成功次数
任务失败次数
任务连续失败次数
任务转入缺口需求次数
任务最近错误码
```

实现口径：

```text
本批执行后曾短暂保留任务主信息字段作运行期缓存。
随后第五批已删除这些过渡字段，权威读写直接落到任务虚拟存在实例特征。
任务创建、任务需求绑定、管理实例整理、执行桥记账改为调用 任务类 特征读写接口。
```

验证结果：

```text
msbuild Debug x64 通过。
task-summary 可运行。
任务树规模保持 需求=5，任务头=5，步骤=0，结果=0。
日志继续显性显示真实断点：
  任务尚未完成筹办：缺少选中自我能力值
派生需求入树仍成功。
```

### 2026-05-05 阶段协议第一处接点

已调整：

```text
执行桥在没有旧当前步骤节点时，不再直接以“缺少阶段上下文”失败。
若任务头已有选中自我能力值，则以任务头作为本轮执行承载并解析方法。
若任务头没有选中自我能力值，则显性失败为：任务尚未完成筹办：缺少选中自我能力值。
工作线程日志补充输出 选中自我能力值。
缺少选中自我能力值时，不再直接生成旧需求方向方法头，而是派生补方法需求。
```

验证结果：

```text
msbuild Debug x64 通过。
task-summary 显示：需求=5，任务节点=5，头=5，步骤=0，结果=0。
日志显示缺口原因已从“任务阶段执行协议未接入”前移为“任务尚未完成筹办：缺少选中自我能力值”。
日志显示补方法派生需求成功入树。
本轮新增日志未出现 当前本能ID=6037 或 当前本能ID=6038。
```

### 2026-05-05 第四批死缓存字段删除

已删除字段：

```text
方法配对结果
参数配对结果
可执行输入参数场景
最近运行虚拟存在
```

删除依据：

```text
这些字段只在任务主信息类中声明，并由自我初始化模块尝试同步到任务虚拟存在。
当前主链没有把这些结果先写入任务主信息再同步的可靠来源。
继续保留会让任务主信息被误认为方法配对、参数配对和运行事实的权威承载。
```

同步移除位置：

```text
任务主信息类.h
自我类.初始化.cpp
说明书/任务类说明.md
```

新口径：

```text
方法配对结果直接写入任务虚拟存在或输出结果场景。
参数配对结果直接写入任务虚拟存在或输出结果场景。
可执行输入参数场景由配对参数结果或运行虚拟存在引用。
最近运行虚拟存在由执行桥或本能函数输出回写，不经过任务主信息字段中转。
```

### 2026-05-05 第五批过渡字段删除

已从 `任务主信息类` 删除字段：

```text
基准优先级
局部优先级偏移
调度优先级
运行次数
成功次数
失败次数
连续失败次数
转入缺口需求次数
最近错误码
```

替代口径：

```text
这些语义全部通过任务虚拟存在实例特征承载。
任务类 提供统一读取 / 写入接口。
旧代码中对 主信息 字段的读写已改为调用 任务类 特征接口。
控制面板展示也从任务虚拟存在实例特征读取。
```

静态验收：

```text
rg 确认任务主信息中上述字段无剩余代码引用。
说明书/任务类说明.md 已改为实例特征口径。
msbuild Debug x64 通过。
```

### 2026-05-05 第六批旧任务树缓存字段删除

已从 `任务主信息类` 删除字段和枚举：

```text
候选方法首节点
最近调度时间
统计
缺口目标方法首节点
父任务
来源步骤节点
后续任务
描述信息
完成口径
所属任务头结点
当前步骤节点
枚举_分支选择动作
```

替代口径：

```text
候选方法首节点 / 缺口目标方法首节点：
  不再作为长期方法选择字段，执行方法只保留运行期缓存，权威选择为选中自我能力值。

父任务 / 来源步骤节点 / 后续任务 / 所属任务头结点 / 当前步骤节点：
  不再保存在任务主信息中。
  父子归属、步骤结果关系和当前步骤由任务树结构推导。

描述信息：
  不再复制需求说明，任务说明以对应需求为权威来源。

完成口径：
  不再作为任务主信息枚举字段，完成判断回到需求状态、任务阶段和任务虚拟存在特征。

统计 / 最近调度时间：
  不再进入任务主信息，统计和调度事实由任务虚拟存在实例特征与运行事实承载。

枚举_分支选择动作：
  已无业务读写路径，控制面板文本函数一并删除。
```

静态验收：

```text
rg 确认上述任务主信息字段无剩余代码引用。
msbuild Debug x64 多轮通过。
```

当前边界：

```text
剩余字段仍有实际执行路径依赖。
继续删除 管理对象任务 / 任务角色 / 节点种类 等字段，需要先正式拆除旧任务管理任务承接壳和旧步骤/结果任务树。
这已经超出“无用字段”删除，属于任务管理执行架构清理。
```

### 2026-05-05 第七批任务实体结构特征化

已新增任务实体语义特征类型：

```text
任务根ID
任务来源需求
任务世界任务根
任务父任务
任务管理对象任务
任务初始授权等级
任务只读状态
任务全局优先级
任务解阻加权
任务时间预算
任务资源预算
任务绑定工作线程
任务已派发
任务等待子结果
任务请求收束
任务请求停止
任务已响应控制意图
任务控制请求待确认
任务当前步骤前沿
任务最近局部结果
任务局部完成度
```

已从 `任务模块.实体.ixx` 删除结构体内的语义镜像字段：

```text
任务控制态：
  队列状态抽象特征指针
  队列状态值
  当前控制请求主键
  当前控制意图说明
  控制说明

任务局部运行态：
  当前状态抽象特征指针
  当前状态值
  派生需求缓存
  局部错误说明
  局部运行说明

任务根节点：
  方向说明
  创建者
  根约束说明
```

替代口径：

```text
任务根节点、任务控制态、任务局部运行态只作为工作线程局部运行包。
工作线程在接收任务和每轮推进后，将任务根、控制态、局部运行态的语义写入任务虚拟存在实例特征。
空的结构体语义同步函数已删除，不再保留旧桥。
```

当前保留边界：

```text
当前等待原因 仍是消息 / 日志运输字段。
原因文本不作为权威语义；权威阻塞语义应继续迁移到缺口类型、任务局部运行状态和派生需求条件包。
```

静态验收：

```text
rg 确认目标模块中旧语义镜像字段和空同步函数无剩余引用。
msbuild Debug x64 /p:LinkIncremental=false 通过。
普通增量链接曾触发 MSVC LINK LNK1000 内部错误；编译阶段通过，关闭增量链接后完整链接通过。
git diff --check 通过；仅保留仓库既有 LF/CRLF 行尾提示。
```

### 2026-05-05 第十五批执行方法字段访问收口

已新增任务执行方法特征槽：

```text
自我特征定义类::类型_任务_执行方法
```

已将外部模块对 `主信息.执行方法` 的直接读写收口到 `任务类`：

```text
任务类::读取任务执行方法
任务类::任务执行方法指向
任务类::写入任务执行方法
```

已同步替换：

```text
任务模块.管理工作线程
自我_执行桥模块
自我初始化模块
自我线程模块.impl.cpp
自检线程模块.impl.cpp
控制面板类.cpp
自我类.cpp
```

迁移后边界：

```text
任务头节点的执行方法引用可写入任务虚拟存在实例特征。
外部模块不再直接读写 主信息.执行方法。
任务长期权威仍是 选中自我能力值，执行方法只作为运行期方法引用。
```

当前保留边界：

```text
任务主信息类::执行方法 仍在任务类内部保留。
原因是旧步骤节点 / 结果节点仍把执行方法当作每步运行事实引用，而当前任务虚拟存在只按任务头归一。
若现在直接删除该字段，会把步骤 / 结果节点的来源方法事实误写到任务头虚拟存在，反而遮蔽旧逻辑问题。
后续必须先删除旧步骤 / 结果任务树，或建立明确的运行虚拟存在事实链，再删除该字段。
```

静态验收：

```text
rg 确认 主信息.执行方法 的外部读写已清空，只剩任务类内部兼容缓存。
msbuild Debug x64 /p:LinkIncremental=false 通过。
git diff --check 通过；仅保留仓库既有 LF/CRLF 行尾提示。
```

### 2026-05-05 第十六批等待缺口唤醒字段特征化删除

已从 `任务主信息类` 删除字段：

```text
等待缺口唤醒
```

新增任务虚拟存在特征槽：

```text
自我特征定义类::类型_任务_等待缺口唤醒
```

新增任务类读写入口：

```text
任务类::读取任务等待缺口唤醒
任务类::写入任务等待缺口唤醒
```

已替换直接读写位置：

```text
任务模块.管理工作线程
自我初始化模块
自检线程模块.impl.cpp
控制面板类.cpp
```

迁移后边界：

```text
缺口承接后是否等待唤醒不再是任务主信息字段。
该语义由任务虚拟存在实例特征承载。
控制面板和自检线程通过任务类读取，不再直接访问结构体字段。
```

静态验收：

```text
rg 确认 任务主信息类.h 不再声明 等待缺口唤醒。
msbuild Debug x64 /p:LinkIncremental=false 通过。
git diff --check 通过；仅保留仓库既有 LF/CRLF 行尾提示。
```

### 2026-05-05 第十七批选中自我能力值字段特征化删除

已新增任务选中自我能力值读写入口：

```text
任务类::读取任务选中自我能力值
任务类::写入任务选中自我能力值
```

已将外部模块对 `主信息.选中自我能力值` 的直接读写收口到 `任务类`：

```text
任务模块.管理工作线程
自我_执行桥模块
自我初始化模块
自我线程模块
自检线程模块
自我类
```

已从 `任务主信息类` 删除字段：

```text
选中自我能力值
```

迁移后语义落点：

```text
任务虚拟存在
  实例特征：任务选中自我能力
```

边界修正：

```text
任务仍然需要方法关系。
本批删除的是单个结构体缓存字段，不删除任务和方法的关系。
候选方法集合、当前选中能力、执行方法缓存和每次运行事实应分别落在任务虚拟存在、方法配对结果和运行虚拟存在中。
```

保留项：

```text
任务主信息类::执行方法 仍在任务类内部保留为运行期兼容缓存。
原因是旧步骤节点 / 结果节点仍把执行方法当作每步运行事实引用。
删除该缓存前，需要先把每步来源方法迁入运行虚拟存在事实链，或删除旧步骤 / 结果任务树。
```

静态验收：

```text
rg 确认代码中不再直接访问 主信息.选中自我能力值。
rg 确认 任务主信息类.h 不再声明 选中自我能力值。
msbuild Debug x64 /p:LinkIncremental=false 通过。
git diff --check 通过；仅保留仓库既有 LF/CRLF 行尾提示。
```

### 2026-05-05 第十八批任务角色字段特征化删除

已新增任务角色读写入口：

```text
任务类::读取任务角色
任务类::写入任务角色
```

已将外部模块对 `主信息.任务角色` 的直接读写收口到 `任务类`：

```text
任务模块.管理工作线程
自我_执行桥模块
自我线程模块
自我类
```

已从 `任务主信息类` 删除字段：

```text
任务角色
```

迁移后语义落点：

```text
任务虚拟存在
  实例特征：任务角色
```

边界：

```text
任务角色只表达需求树职责分类。
它不能作为旧任务管理任务中心模型的调度权威。
任务推进仍由需求节点对应任务、筹办 / 执行阶段和工作线程承接。
```

静态验收：

```text
rg 确认代码中不再直接访问 主信息.任务角色。
rg 确认 任务主信息类.h 不再声明 任务角色。
msbuild Debug x64 /p:LinkIncremental=false 通过。
git diff --check 通过；仅保留仓库既有 LF/CRLF 行尾提示。
```

### 2026-05-05 第十九批当前阶段字段特征化删除

已新增任务当前阶段读写入口：

```text
任务类::读取任务当前阶段
任务类::写入任务当前阶段
```

已将外部模块对 `主信息.当前阶段` 的直接读写收口到 `任务类`：

```text
任务模块.管理工作线程
控制面板摘要线程模块
自我初始化模块
自我线程模块
自检线程模块
```

已从 `任务主信息类` 删除字段：

```text
当前阶段
```

迁移后语义落点：

```text
任务虚拟存在
  实例特征：任务当前阶段
```

读取策略：

```text
优先读取任务虚拟存在中的任务当前阶段。
若阶段特征尚未写入，则按任务状态推导筹办、执行、阻塞或已完成。
```

静态验收：

```text
rg 确认代码中不再直接访问 主信息.当前阶段。
rg 确认 任务主信息类.h 不再声明 当前阶段。
msbuild Debug x64 /p:LinkIncremental=false 通过。
git diff --check 通过；仅保留仓库既有 LF/CRLF 行尾提示。
```

### 2026-05-05 今日收尾与明日接手点

今日收口字段：

```text
选中自我能力值
任务角色
当前阶段
```

已完成口径：

```text
以上三项不再由 任务主信息类 承载。
外部模块不再直接访问对应主信息字段。
语义统一落入任务虚拟存在实例特征。
任务类提供读取 / 写入入口。
```

保留边界：

```text
执行方法 不删除。
任务仍然需要方法关系，而且一个任务可能有多个候选方法、多次尝试和不同执行事实。
后续应迁移的是候选方法集合、选中能力、执行方法缓存、运行事实链之间的关系，而不是切断任务和方法的关系。
```

明日建议接手：

```text
任务状态特征化。

第一步：
  新增 任务类::读取任务状态 / 写入任务状态。

第二步：
  将 任务状态已终结、任务允许派发、应用任务状态 等任务类内部函数改为以任务状态实例特征为权威。

第三步：
  替换外部 主信息.状态 直接访问。

第四步：
  删除 任务主信息类::状态 字段。
```

注意：

```text
枚举_任务状态 可以暂时保留为点区间状态值的代码别名。
权威语义应迁入任务虚拟存在上的 任务状态 实例特征。
```

### 2026-05-05 第十三批旧任务两步壳识别死代码删除

已删除工作线程中不再被调用的旧任务两步壳识别逻辑：

```text
私有_任务管理本体步骤类型词
私有_任务管理本体步骤有效
私有_当前步骤是任务两步壳
私有_方法是任务治理本能方法
私有_词性同一
私有_任务节点归属任务头
```

删除后边界：

```text
工作线程不再保留按 自我_任务筹办 / 自我_任务执行一步 识别旧步骤壳的未使用函数。
仍保留的 私有_准备任务管理任务实例 当前有实际调用，不能在本批直接删除。
下一步应先把 管理对象任务 引用迁入任务类接口，再清理这段兼容承接入口。
```

静态验收：

```text
rg 确认旧两步壳识别函数不再存在。
msbuild Debug x64 /p:LinkIncremental=false 通过。
```

### 2026-05-05 第十四批管理对象任务字段特征化

已将 `任务主信息类::管理对象任务` 删除，并把管理对象读写收口到 `任务类`：

```text
任务类::读取任务管理对象任务
任务类::读取任务管理对象或自身
任务类::任务管理对象指向
任务类::写入任务管理对象任务
```

迁移后边界：

```text
任务管理对象不再是任务主信息字段。
任务管理对象只通过任务虚拟存在实例特征承载。
任务模块.管理工作线程、自我线程、自我初始化模块、自我执行桥模块不再直接读写 主信息.管理对象任务。
```

当前保留边界：

```text
任务根节点结构中仍保留 管理对象任务指针。
这是工作线程一次推进请求的局部运行包字段，不是任务主信息长期语义字段。
后续若要继续清理，应把任务根节点中的管理对象指针也替换为需求节点任务上下文或任务类特征读取。
```

静态验收：

```text
rg 确认 主信息.管理对象任务 只剩任务类内部访问，不再出现在外部模块。
msbuild Debug x64 /p:LinkIncremental=false 通过。
```

### 2026-05-05 第十二批任务基础状态落点继续迁入任务类

已将工作线程中的直接任务状态落点继续迁入 `任务类`：

```text
任务类::确保任务初始状态
任务类::标记任务待重筹办
任务类::标记任务等待
任务类::应用任务状态
```

迁移后边界：

```text
任务模块.管理工作线程 不再直接写入任务主信息状态。
未定义到未启动、待重筹办、等待中、工作线程回传状态应用，都由任务类统一维护。
工作线程仍可以计算 宿主任务新状态 / 推导状态，但它们只是结果字段或局部推导值，不是任务节点状态的权威写入口。
```

静态验收：

```text
rg 确认工作线程中剩余的 主信息.状态 命中为状态比较，不是直接写入。
msbuild Debug x64 /p:LinkIncremental=false 通过。
git diff --check 通过；仅保留仓库既有 LF/CRLF 行尾提示。
```

### 2026-05-05 第八批任务根节点主键镜像删除

已从 `结构_任务根节点` 删除字段：

```text
来源需求主键
管理对象任务主键
```

替代口径：

```text
任务根节点只保留运行期指针句柄。
需要主键时由 来源需求指针 / 管理对象任务指针 指向的节点即时读取。
任务虚拟存在中的 来源需求 / 管理对象任务 仍以实例特征引用承载。
```

当前边界：

```text
选中自我能力值、任务阶段、任务状态、任务角色、任务节点种类仍有大量直接执行路径依赖。
下一步应先补齐任务类读取 / 写入这些特征的统一 API，再替换直接读写，最后删除主信息字段。
```

### 2026-05-05 第九批旧任务管理模块主体删除

已删除旧任务管理任务模块主体和旧最小原语 / 最小回写入口：

```text
任务管理任务模块.impl.cpp
任务管理任务模块_治理快照.cpp
任务管理最小原语模块.cpp
任务管理最小原语模块.h
任务模块.管理任务.ixx
任务模块.最小回写器.ixx
任务模块.最小回写器.cpp
```

已将仍被工作线程和自我线程使用的消息合同迁入任务模块命名：

```text
任务管理任务模块.消息协议.ixx -> 任务模块.工作线程消息协议.ixx
任务管理任务模块.ixx -> 任务模块.治理协议.ixx
```

已同步调整：

```text
工程文件与 filters
自我线程消息协议 / 批次执行器 / 主循环实现
任务管理工作线程
控制面板读取入口
自检线程报告入口
初始化与程序入口
README 活模块清单
```

删除后的主链边界：

```text
不再注册旧任务管理本能动作。
不再读取旧任务管理治理快照。
不再调用旧最小回写器。
任务创建、派发和推进由任务实体、工作线程协议、自我线程消息和需求树承接。
```

当前保留边界：

```text
任务模块.治理协议 中仍有若干类型名沿用 任务管理 字样。
这些类型目前是工作线程 / 自我线程之间的消息合同，不再对应旧模块执行逻辑。
后续应单独做命名清理，把协议类型也改成需求任务工作线程口径。
```

静态验收：

```text
rg 确认代码与工程文件中不再引用旧模块名、旧最小原语和旧最小回写器。
msbuild Debug x64 /p:LinkIncremental=false 通过。
git diff --check 通过；仅保留仓库既有 LF/CRLF 行尾提示。
```

### 2026-05-05 第十批任务实体特征写入迁入任务类

已将新逻辑中的任务实体特征写入权威迁入 `任务类`：

```text
任务类::写入任务根实体特征
任务类::写入任务控制实体特征
任务类::写入任务局部运行实体特征
```

迁移后边界：

```text
任务模块.管理工作线程 不再直接调用 记录任务虚拟存在特征。
任务模块.管理工作线程 只把任务根节点、控制态、局部运行态的字段提交给任务类。
任务虚拟存在实例特征的写入特征类型和值格式由任务类统一维护。
```

当前保留边界：

```text
工作线程内仍保留一个局部适配函数，用于把 任务模块.实体 的运行包结构拆成任务类接口参数。
该函数不再包含实际特征写入规则。
若后续要彻底消除适配函数，需要先把任务运行包结构移动到任务类可见的稳定头文件，避免模块接口和任务类头文件互相牵连。
```

静态验收：

```text
rg 确认 任务模块.管理工作线程 中不再直接调用 记录任务虚拟存在特征 / 写入特征_I64 / 写入特征_指针。
msbuild Debug x64 /p:LinkIncremental=false 通过。
```

### 2026-05-05 第十一批任务状态基础函数迁入任务类

已将工作线程中的任务状态基础函数迁入 `任务类`：

```text
任务类::任务状态已终结
任务类::任务允许派发
任务类::标记任务已承接
任务类::标记任务已派发
任务类::标记任务负载等待
```

迁移后边界：

```text
任务是否已终结、是否允许派发、承接 / 派发 / 负载等待的基础状态标记由任务类统一维护。
任务模块.管理工作线程 不再保留同名私有函数，只调用任务类接口。
这些函数不负责完整调度，不生成需求，不选择方法，只维护任务自身状态。
```

静态验收：

```text
rg 确认工作线程中不再存在旧私有状态函数定义。
msbuild Debug x64 /p:LinkIncremental=false 通过。
git diff --check 通过；仅保留仓库既有 LF/CRLF 行尾提示。
```

### 2026-05-06 第二十批任务主信息最终瘦身完成

已从 `任务主信息类` 删除最后三类运行缓存字段：

```text
状态
执行方法
节点种类
```

已新增并接通任务类统一入口：

```text
任务类::读取任务状态
任务类::写入任务状态
任务类::读取任务节点种类
任务类::写入任务节点种类
```

迁移后语义落点：

```text
任务虚拟存在
  实例特征：任务状态
  实例特征：任务节点种类
  实例特征：任务执行方法
```

完成口径：

```text
任务主信息类只保留任务壳必要字段：
  名称
  类型
  创建时间
  启动时间
  完成时间
  对应需求
  任务虚拟存在
  场景
  目标状态
  结果状态信息

代码中不再直接访问任务主信息字段：
  主信息.状态
  主信息.执行方法
  主信息.节点种类
  主信息.任务角色
  主信息.当前阶段
  主信息.选中自我能力值
  主信息.等待缺口唤醒
  主信息.管理对象任务
```

边界修正：

```text
任务虚拟存在名称改为包含任务节点主键，避免多个任务节点在同一承载世界下共用同一个“任务虚拟存在”导致状态、节点种类和执行方法特征互相覆盖。
旧步骤 / 结果节点仍作为兼容结构存在，但其结构角色不再保存在任务主信息字段中，而是通过任务节点种类实例特征读取。
```

静态验收：

```text
rg 确认 任务主信息类.h 不再声明 状态 / 执行方法 / 节点种类。
rg 确认代码中不再直接访问任务主信息字段 主信息.状态 / 主信息.执行方法 / 主信息.节点种类。
Debug x64 构建使用当前机器可用工具集覆盖通过：
  MSBuild 鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:PlatformToolset=v143 /p:LinkIncremental=false /m
原项目指定 v145 平台工具集，但当前机器未安装 v145；未覆盖工具集时会在编译前报 MSB8020。
git diff --check 通过；仅保留仓库既有 LF/CRLF 行尾提示。
```
