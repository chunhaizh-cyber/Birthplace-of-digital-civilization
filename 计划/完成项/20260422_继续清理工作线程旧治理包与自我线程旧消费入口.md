# 完成项：继续清理工作线程旧治理包与自我线程旧消费入口

时间：2026-04-22

## 本次完成

1. `任务模块.管理工作线程.ixx`
   - 删除了桥接结果中的一批旧运行字段：
     - 已消费控制请求
     - 已消费控制请求对象
     - 当前控制承接
     - 上层反馈摘要
     - 已写入动态节点
     - 已写入因果候选
     - 已构建学习调度快照
     - 已选中学习任务
     - 当前学习账本ID
     - 学习调度摘要
     - 学习执行摘要
     - 学习反馈摘要
   - 删除了对应的旧状态消息拼装分支。
   - 状态消息的执行尝试键不再复用旧治理包主键，而是直接按 `任务根ID|步骤主键` 生成。

2. `自我线程模块.消息批次执行器.ixx`
   - `裁决批次执行去向(...)` 不再读取旧最小回写结算包的“建议进入收束”，改为直接读取新主路径下的 `治理结果.当前下一步去向 / 最近总控结果`。

3. `自我线程模块.impl.cpp`
   - 主派发动作包不再复用桥接返回的旧一步治理任务包。
   - 主派发动作包也不再把旧桥接治理包主键挂成父包主键。
   - 工作线程证据触发与结果路由改成直接消费新主路径的推进事件。

## 兼容保留

为保证当前自我线程后处理编译稳定，`结构_工作线程桥接结果` 中仍保留了最小兼容占位：

- `已生成一步治理任务包`
- `一步治理请求包`
- `已生成回流结算包`
- `最小回写结果`

这些字段当前不再由新运行主路径真实生产，只是为了让尚未清完的旧后处理消费点继续编译。后续需要继续把自我线程里剩余的旧结算消费完全替换掉，再删这些占位。

## 验证

已通过：

```powershell
msbuild D:\鱼巢\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /m /clp:ErrorsOnly
```
