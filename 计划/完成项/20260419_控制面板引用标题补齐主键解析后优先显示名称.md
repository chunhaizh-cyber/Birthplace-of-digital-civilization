# 20260419 控制面板引用标题补齐主键解析后优先显示名称

## 本次完成

- 控制面板中的 `基础引用标题` 和 `状态引用标题` 现在会先尝试：
  - 用 `主键`
  - 到 `世界树.基础信息()` 里解析节点
  - 解析成功后优先显示节点名称
- 只有在主键无法解析成有效节点时，才继续退回显示 `前缀#主键`。

## 直接影响

- `因果信息` 页面里这些字段会优先显示名称，而不是主键：
  - `动作主体`
  - `动作语义`
  - `锚点信息`
  - `动作模板`
  - 条件/结果状态引用

## 验证

- 本轮仅做编译级验证，未做运行态点击验证。
