# 20260418 自我线程控制意图发起接入任务管理承接方

## 完成内容

本步把 `自我线程` 从“可能直接动他线程状态”收回到“只发控制请求”的位置。

已完成：

- 在 [D:\鱼巢\自我线程模块.impl.cpp](D:\鱼巢\自我线程模块.impl.cpp) 新增：
  - `私有_映射任务控制事件到控制意图(...)`
  - `私有_构造任务管理控制请求(...)`
- 在主派发工作线程入口前，把 `结构_任务控制事件` 显式映射成 `结构_任务管理控制请求`，并通过：
  - `任务管理任务模块::设置治理控制请求(...)`
  先交给承接方。
- 收紧 `私有_切换线程状态并上报_(...)`：
  - `自我线程` 直接改写目标线程状态时，如果目标不是自我线程自身，直接拒绝。

## 当前口径

- `自我线程` 现在对外线程只保留：
  - 控制意图发起
  - 上行消息投递
  - 回报消费
- 不再保留“直接写对方事实状态”的合法路径。

## 验证

- 已通过编译验证：
  - `msbuild D:\鱼巢\鱼巢.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64 /m`
- 本步未做运行态验证。
