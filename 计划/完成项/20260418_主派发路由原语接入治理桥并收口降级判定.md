# 2026-04-18 主派发路由原语接入治理桥并收口降级判定

## 本次目标

在已经完成的“主派发判据原语”基础上，继续把主派发后半段的路由层证据收口出来，并让 `自我_主循环治理模块` 开始看到：

- 主派发判据二次特征
- 主派发路由原语

而不只消费前一层的布尔判据。

## 已完成

1. 在 [自我线程模块.impl.cpp](/D:/鱼巢/自我线程模块.impl.cpp) 中新增 `结构_主派发路由原语结果` 与 `私有_计算主派发路由原语(...)`。
2. 这份路由原语现在会统一产出主派发后半段的关键路由层结果：
   - `收益放行层 / 禁止降级层 / 常规放行层`
   - `收益恢复放行`
   - `绝对证据覆盖 / 长期根方向覆盖 / 收益恢复覆盖 / 已验证降根覆盖 / 目标反向仅降级 / 冲突仅降级`
   - `联合收益恢复规则`
   - `逐类覆盖摘要 / 多证据覆盖摘要 / 联合阈值细则摘要 / 覆盖优先细则摘要 / 逐类路由细则摘要 / 路由原语摘要`
3. `步骤_生成并执行主派发决议_` 现在会在二次特征覆盖之后，再计算一次“覆盖后主派发路由原语”，并把这份结果回填到主派发摘要与治理桥输入。
4. `自我_主循环治理模块` 已扩出新的输入面：
   - `主派发命中可改路由证据`
   - `主派发命中仅能降级证据`
   - `主派发命中收益恢复放行`
   - `主派发路由原语摘要`
5. 治理桥新增一条最小执行口径：
   - 当执行链上命中“仅能降级证据”且没有收益恢复放行时，治理桥会要求执行链降到等待，而不是继续硬进执行。
6. `读取一步治理结果快照_最小_v0(...)` 现在也会从主派发快照中回拼一份最小路由原语摘要，使桥层快照既能看到“判据层”，也能看到“路由层”。

## 本次口径

- 二次特征层负责主派发证据节点。
- 判据原语负责主派发前半段母式。
- 路由原语负责主派发后半段覆盖与降级语义。
- 治理桥现在消费的是“判据证据 + 路由证据”，而不是只看线程最终去向。

## 验证

已执行：

```powershell
msbuild D:\鱼巢\鱼巢.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64 /m
```

结果：`0 error / 0 warning`

## 仍未完成

1. 当前线程里真正改写 `当前最终去向` 的主逻辑仍然主要留在 `自我线程模块.impl.cpp`，还没有彻底前推给治理桥。
2. 路由原语还没有单独投影进学习承接或治理记忆侧形成经验样本。
3. 还没有做运行态验证，当前完成的是编译级验证。

## 下一步建议

1. 继续把“最终去向改写”从线程内联逻辑前推到治理桥建议，让桥层不只观察路由原语，而是真正消费它。
2. 把 `主派发路由原语摘要` 接到学习承接或治理记忆，形成“什么路由证据导致什么最终去向”的可复盘样本。
3. 跑一轮真实线程主循环，核对 `主派发判据摘要 / 主派发路由摘要 / 治理桥反馈 / 最终去向` 是否一致。
