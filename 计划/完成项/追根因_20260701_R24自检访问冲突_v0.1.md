# 追根因 20260701 R24 自检访问冲突 v0.1

## 当前弹窗 / 现象

无项目弹窗文本；命令 `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000` 在最终复跑中退出码 `-1073741819`，按 Windows 口径为访问冲突。

## 最高优先级原因

先按崩溃处理，不用日志成功片段替代退出码。当前状态已变为待复现：ProcDump Crash 复跑未产生 dump，进程退出码 0；随后普通 self-check 复跑也退出 0。

## 当前层级

L0 现象层 -> L1 崩溃栈捕获。

## 同层疑点记录

| 层级 | 疑似原因 | 证据 | 反证 / 未证 | 下一验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| L0 | 最终 self-check 后段访问冲突 | 退出码 `-1073741819`，无正常退出守卫收口日志 | ProcDump Crash 复跑未生成 dump；随后普通复跑退出 0 | 等同命令再次复现时捕获 dump | 待条件 |
| L0 | R24 扫描需求入树导致崩溃 | 同段有 `服务机会检查扫描子需求入树` | 崩溃前已继续运行到扫描执行、外设等待项和报告入队；未见入树点逻辑错误 | 用 dump 栈确认崩溃线程 | 候选 |
| L0 | 外设报告等待项承接后段异常 | 崩溃前反复出现等待项 `2` 与扫描变化报告入队、`无匹配报告` | 只是最近日志，不等于崩溃点 | dump 栈 + 崩溃前日志窗口对齐 | 候选 |

## 已确认事实

- `check_specs.py`：hard issues 0，warnings 0。
- Debug x64 构建：退出码 0，0 警告，0 错误。
- 最终 self-check 新段：`服务机会检查扫描子需求入树=1`、`父需求2直连扫描=0`、`旧P7a服务到扫描消息=0`。
- 同段扫描任务 `需求=6` 已执行 `自我_扫描当前场景`，但 `外设数据项总数=0` 仍出现。
- 同段外设线程持续生成扫描变化报告并匹配等待项 `2`，任务管理外设消息承接多次显示 `无匹配报告`。
- `.\tools\debug-stack\Invoke-FishnestProcDump.ps1 -ExePath .\x64\Debug\鱼巢.exe -Mode Crash -Arguments '--self-check-log','--self-check-log-wait-ms=60000'`：未生成 dump，ProcDump 记录进程退出码 `0x00000000`。
- 随后普通 `--self-check-log --self-check-log-wait-ms=60000` 复跑退出码 0，日志存在完整退出守卫；同段 `报告可回查是=6`、`观察事实承接成功=2`。

## 已排除

- 没有证据显示服务根 `2` 直接生成扫描需求。
- 没有项目逻辑错误日志或 Debug Error 文本可直接定位。

## 旁支问题池

- 扫描函数自身结果仍为 `外设数据项总数=0`，但外设线程已有扫描变化报告入队；该问题可能与崩溃同链，也可能是独立下游缺口。

## 已改文件

- `自我线程模块.impl.cpp`
- `说明书/函数功能说明文档.md`

## 已运行命令

- `python .\tools\check_specs.py`
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`
- `.\x64\Debug\鱼巢.exe --d455-report-queue-smoke`
- `.\x64\Debug\鱼巢.exe --demand-tree-growth-summary-live`
- `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=60000`

## 下一条命令

等待同命令再次复现访问冲突，或取得 Visual Studio / dump 调试栈后继续。若复现，使用显式参数形式：

```powershell
.\tools\debug-stack\Invoke-FishnestProcDump.ps1 -ExePath .\x64\Debug\鱼巢.exe -Mode Crash -Arguments '--self-check-log','--self-check-log-wait-ms=60000'
```

## 验收证据

当前不能宣称访问冲突已修复，只能宣称未复现。若后续复现，修复后至少需要：Debug x64 构建通过、ProcDump 复跑不产生崩溃 dump、`--self-check-log --self-check-log-wait-ms=60000` 退出码 0，并保留 R24 扫描需求父链证据。

## 禁止宣称

不得宣称 R24 完成、内部治理闭环完成、自我苏醒完成或初步成熟完成；不得用已有扫描需求硬判据覆盖最终 self-check 访问冲突。
