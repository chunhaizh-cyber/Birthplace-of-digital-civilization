# 场景节点主信息字段纠偏分计划 v0.1

> 2026-05-22 验证口径修订：
> 后续所有计划的验证统一改为日志模块验证，以真实运行产生的日志、状态快照、动作动态与错误日志为证据。
> 严禁新增或依赖模拟测试、夹具测试、伪造样本、伪造入口、伪造动作动态或 `--*-check` 回归命令。
> 历史计划中已有的回归命令、夹具和模拟测试描述仅作为历史记录，不再作为当前执行或验收依据。

更新时间：2026-05-19

对应主信息：`场景节点主信息类`

## 审查结论

`场景节点主信息类` 主要承载状态、动态、关系、因果实例索引。它是事实集合容器，不应新增业务语义判断字段。

## 字段口径

```text
可保留：
    世界类型
    状态索引
    动态索引
    关系索引
    实例因果索引
    来源 / 时间 / 场景归属

不得新增：
    任务完成判断
    需求满足判断
    安全 / 服务结算判断
```

## 纠偏动作

```text
P1. 确认场景节点只保存事实索引。
P2. 业务判断通过场景内状态、动态、二次特征和因果实例推导。
P3. 输出结果场景不得承载任务状态回执。
```

## 验收

```powershell
rg -n "场景节点主信息类|状态索引|动态索引|关系索引|实例因果索引" -S 基础信息主信息类.h 基础信息类.cpp
```

通过条件：

```text
场景字段不直接表达任务状态、需求满足或结算结论。
```

## 执行记录

### 2026-05-19

```text
P1-P3 首轮完成：
- 已在 场景节点主信息类 标注：场景是事实集合容器，宿主和来源只用于归属追溯。
- 已在状态 / 动态 / 二次特征 / 因果实例追加函数标注：场景只登记事实索引，不解释任务、需求或结算业务语义。
- 动态业务语义仍由来源动作、动态特征、状态比较和二次特征 / 因果证据推导，场景不保存平行结论。
```

验收：

```text
rg 场景节点主信息类 / 状态索引 / 动态索引 / 关系索引 / 实例因果索引：通过。
Debug x64 构建：通过。
--demand-tree-growth-check：通过。
--demand-difference-child-exit-check：通过。
```
