# 追根因 20260629 自我场景体素窗口读特征卡住 v0.1

当前弹窗：
- 自我场景体素窗口可显示，但刷新后卡住；随后在 `基础信息类::取主信息<特征节点主信息类>` 报错。
- 用户提供栈顶链路：`私有_发送自我场景帧到页面 -> 读取控制面板快照 -> 私有_读取自我场景复现快照 -> 私有_选择自我场景安全评估宿主 -> 私有_场景安全评估宿主评分 -> 私有_读取场景复现I64 -> 世界树类::读取特征_I64 -> 世界树类::读取特征快照 -> 特征类::读取子特征值_按类型 -> 基础信息类::取主信息<特征节点主信息类>`。

最高优先级原因：
- 显示层安全评估评分把非特征子节点或非合格宿主送入特征读取路径，导致底层按 `特征节点主信息类` 取主信息时报错。
- 用户补充窗口刷新频率要求：变化先记录，每 3 秒集中刷新；不得为了追求高刷新率让后端重复深扫场景树。

当前层级：
- L1 直接生成层 / L2 本地事实读取层。

同层疑点记录：
- A 主链根因候选：`私有_选择自我场景安全评估宿主` 候选过滤不足，把场景、存在或观察材料子节点中不满足特征读取契约的节点交给 `私有_场景安全评估宿主评分`。
- B 候选：`世界树类::读取特征快照` 或 `特征类::读取子特征值_按类型` 缺少只读失败返回路径，遇到非特征节点时直接触发主信息类型错误。
- C 独立风险：自我场景窗口每帧读取完整控制面板快照，可能放大卡顿，但不解释当前主信息类型报错。

选中下钻分支：
- 先下钻 A：显示层候选宿主过滤不足；若证据显示输入宿主合法，再回到 B 检查底层只读读取契约。

主链判断：
- 当前修复不得隐藏报错；需要把错误节点挡在显示层只读候选过滤处，或把底层只读读取改成明确“类型不匹配则读取失败”的契约。
- 场景体素窗口只显示只读快照，不写世界树事实、需求满足、安全 / 服务结算、任务状态或方法动作动态。
- 本切片先做显示层刷新请求合并、3 秒批处理和稳定签名无变化不重绘；严格“世界树存在增删改事件源触发”需另接世界树 / 场景 / 存在写入路径，不混入止血切片。

已确认事实：
- 相关规则：`RULE-VISION-PRIOR-001`、`RULE-VISION-PRIOR-002`、`RULE-FACT-001`。
- 场景体素快照不是事实源；控制面板显示层只读。
- `场景体素维护事件` 目前只有命令行 smoke 提交点，未接入实时自我场景写入链，不能直接冒充存在增删改事件源。

已排除：
- 本次复跑未再出现原 `取主信息<特征节点主信息类>` 栈、弹窗错误或逻辑错误。

旁支问题池：
- 严格事件触发刷新：后续切片需要在世界树 / 场景 / 存在合法写入入口记录自我所在场景存在级变更版本或事件队列，再由显示层 3 秒读取批次。
- 自我场景帧刷新频率按 3 秒批处理；若上一帧仍在处理，新请求只记录刷新请求，不允许重入扩大卡顿。

已改文件：
- `控制面板类.cpp`：限制场景复现特征读取宿主为场景 / 存在节点；新增稳定签名，排除每次同步递增的体素版本。
- `控制面板类.ixx`：导出 `生成自我场景复现签名`。
- `控制面板WebView2.cpp`：自我场景帧请求合并、3 秒集中刷新、签名无变化轻量返回、请求超时保护。
- `说明书/函数功能说明文档.md`：已由生成脚本更新新增函数条目。
- `计划/计划索引.md`：排查期间曾将本追根因项放入当前默认主动项顶部；验证闭合后已从当前主动队列移除。
- 本记录。

已运行命令：
- 已限载读取 AGENTS 提供规则、`规范/禁止性规范20260611.md`、`规范/规范目录.md`、`计划/计划索引.md`、工程图谱和相关规则原子。
- `python .\tools\生成函数功能说明文档.py` 成功，函数条目 7382。
- `git diff --check -- 控制面板类.ixx 控制面板类.cpp 控制面板WebView2.cpp 说明书\函数功能说明文档.md 计划\计划索引.md 计划\追根因_20260629_自我场景体素窗口读特征卡住_v0.1.md` 通过，仅 CRLF 提示。
- `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m` 成功，0 警告 0 错误。
- 真实运行 `.\x64\Debug\鱼巢.exe --self-scene-video-window`：约 43 秒内窗口出现，枚举到标题 `鱼巢自我场景视频窗口`，继续运行约 14 秒后关闭。
- 本次 12:35 运行日志单独检索 `特征节点主信息类`、`取主信息<特征节点主信息类>`、`弹窗错误提示`、`逻辑错误` 为空。

下一条命令：
- 最终检查差异范围，提交本切片相关文件；严格事件源接入另开后续切片。

验收证据：
- Debug x64 构建通过。
- 复跑 `--self-scene-video-window`，窗口出现并经过多轮 3 秒刷新，未再触发该栈报错。

禁止宣称：
- 不宣称观察链、扫描链、D455 入场景存在闭合。
- 不宣称内部循环完成、自我苏醒完成或初步成熟完成。
