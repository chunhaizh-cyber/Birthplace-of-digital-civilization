# 20260422_删除旧根任务直建并改由自我线程按根需求创建

1. 删除了 `自我类::确保自我根需求与根任务已初始化_()` 中直接创建 `安全根任务/服务根任务` 的旧逻辑，只保留根需求初始化与任务管理任务补齐。
2. `任务类::创建任务头结点(...)` 现在对 `安全根需求/服务根需求` 做默认名与默认类型映射，按新路径创建时会得到 `安全根任务/服务根任务` 与 `根任务`。
3. `自我线程类::自我初始化(...)` 现在会先执行一次“按需求补任务”，并在必要时回填 `当前主任务`，因此根任务创建入口正式切到自我线程。
4. 初始化完成判据已去掉“根任务必须由初始化阶段直建”的旧假设，允许根任务由自我线程按根需求补齐。
5. 编译验证通过：`msbuild D:\鱼巢\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /m /clp:ErrorsOnly`
