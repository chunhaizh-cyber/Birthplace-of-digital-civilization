# 20260509 未完成计划一致性梳理

更新：存在概念相关计划已完成归档。`20260509_存在概念与实例创建最小闭环实现计划_v0.1.md` 和 `20260509_存在概念抽象存在最小启动链实施计划_v0.2.md` 已移入 `计划/完成项`；本文件中早前“保留为未完成”的判断不再适用。

## 1. 本次检查依据

本次只梳理 `计划` 根目录中尚未进入 `计划/完成项` 的计划文件，并对照当前最新口径检查是否仍可直接执行。

最新口径按以下规范和已落地代码理解：

```text
存在只有一种结构：存在节点主信息类。
概念归属第一阶段以 概念集 为权威。
存在 / 场景 / 动态 / 任务 / 方法等基础信息节点作为特征值时，用 特征值::指针句柄。
不新增 当前值信息。
不新增 存在形态枚举。

需求节点不是存在，也不直接变成虚拟存在。
需求节点通过 对应任务 指向任务信息节点。
任务信息节点保留，是运行壳 / 调度壳 / 任务树节点。
任务信息节点内部绑定任务虚拟存在。
任务虚拟存在是语义身份和记忆对象。

自我线程是治理协调层。
自我线程不直接调用任务管理工作线程。
自我线程提交任务治理请求给任务管理界面线程。
任务管理界面线程负责承接、绑定任务虚拟存在、初始化任务语义、生成工作包、归并工作结果和上行消息。
任务管理工作线程只处理任务工作包，推进一步任务，返回任务工作结果。

方法信息节点 / 方法树保留。
方法虚拟存在是方法语义对象，不替代方法首节点、条件节点、结果节点。
```

## 2. 总体结论

本文件最初检查时，`计划` 根目录里有 7 份计划正文和 1 份索引；存在概念相关计划已在后续执行中归档。逐份判断如下：

最新归档状态：

```text
2026-05-10 已按索引规则完成扫尾：
1. 已完成专项移入 `计划/完成项`。
2. 被后续“信息入口类型”计划覆盖的旧“程序词性”阶段计划已补归档说明。
3. 本一致性梳理文件自身已完成使命，随本轮收口移入完成项。
4. `计划` 根目录应只保留 `计划索引.md` 和后续新建的未完成主计划。
```

| 文件 | 当前判定 | 与最新精神关系 | 建议动作 |
| --- | --- | --- | --- |
| `20260508_任务筹办闭环缺口归属与父任务唤醒实现计划_v0.1.md` | 仍有价值，但不能原样执行 | 局部冲突 | 修订为界面线程边界版 v0.2 |
| `20260508_任务推进状况回执闭环补完计划_v0.1.md` | 旧总计划 | 已被 v0.2 和后续专项覆盖 | 移入完成项或删除为历史 |
| `20260508_任务推进状况回执闭环代码修订计划_v0.2.md` | 历史阶段计划 | 部分完成，剩余项被新线程边界改写 | 收口归档，剩余工作拆新计划 |
| `20260508_任务推进状况回执闭环与目标方法头待重入计划_v0.1.md` | 已完成专项 | 基本一致 | 移入完成项 |
| `20260508_子任务完成触发父任务重筹办与活动子需求判定计划_v0.1.md` | 已完成专项 | 基本一致，但有历史旧例 | 移入完成项，保留历史说明 |
| `20260509_存在概念与实例创建最小闭环实现计划_v0.1.md` | 已完成归档 | 一致 | 已移入完成项，后续由 v0.2 完成承接 |
| `20260509_任务根据需求初始化任务类基础函数实现计划_v0.2.md` | 方向部分正确，但边界需改 | 明显需要修订 | 改为 v0.3：任务类低层函数 + 界面线程编排 |
| `计划索引.md` | 已滞后 | 当前主计划和完成归档不准 | 更新索引 |

核心问题不是某一个计划写错，而是 2026-05-09 后半段已经把任务管理链路拆成：

```text
自我线程 -> 任务管理界面线程 -> 任务管理工作线程
```

所以所有仍把“自我线程直接发工作线程”“工作线程直接归并上行给自我线程”“任务类承担完整任务语义编排”的计划，都需要降级或重写。

### 2.1 删除记录

2026-05-09 已按本梳理删除以下不符合最新边界的计划正文：

```text
计划/20260508_任务筹办闭环缺口归属与父任务唤醒实现计划_v0.1.md
计划/20260508_任务推进状况回执闭环补完计划_v0.1.md
计划/20260508_任务推进状况回执闭环代码修订计划_v0.2.md
计划/20260509_任务根据需求初始化任务类基础函数实现计划_v0.2.md
```

删除原因：

```text
它们仍把任务管理工作线程、自我线程或任务类放在旧职责位置上；
继续保留在未完成计划根目录，会诱导后续恢复直达桥、双入口或任务虚拟存在替代运行壳。
```

## 3. 逐份梳理

### 3.1 `20260508_任务筹办闭环缺口归属与父任务唤醒实现计划_v0.1.md`

可保留的部分：

```text
1. 筹办缺口必须带明确目标主体。
2. 方法结构缺口子需求必须指向方法虚拟存在，不复制父需求目标。
3. 参数不可配不再映射到 方法是否可用。
4. 子任务完成后父任务必须待重筹办。
5. 活动子需求需要阻塞语义。
6. 新建目标方法头补待学习、不可执行、不可默认召回等特征。
7. 任务统计和方法条件结果统计分账。
```

冲突或需修订点：

```text
1. 影响范围仍列出 `自我_执行桥模块.ixx`，该旧模块名不应作为新计划入口。
2. 多处写“管理工作线程派生需求消息构造入口 / 自我线程创建子需求入口”。
   最新边界应改为：
       工作线程生成任务工作结果
       界面线程归并上行消息
       自我线程消费界面线程上行消息并改需求树
3. P6 写“优先在需求虚拟存在或需求主信息增加是否阻塞父需求推进”。
   最新口径是第一阶段不做需求虚拟存在。
   应改为：
       需求主信息字段 / 派生需求消息字段 / 任务推进回执字段
4. P1/P2 中“缺口目标主体”类型可保留，但创建需求时必须走自我线程消息吸收链，不应让工作线程直改需求树。
```

建议：

```text
不原样执行。
新建 v0.2，标题建议：
20260509_任务筹办缺口归属与父任务唤醒界面线程边界修订计划_v0.2.md
```

v0.2 应明确：

```text
任务管理工作线程只返回 任务工作结果。
任务管理界面线程负责把工作结果归并为上行消息。
自我线程只消费上行消息并改需求树 / 任务树。
阻塞语义落在需求主信息和派生消息，不引入需求虚拟存在。
```

### 3.2 `20260508_任务推进状况回执闭环补完计划_v0.1.md`

可保留的部分：

```text
1. 筹办 / 执行都返回结构化回执。
2. 方法查找未命中创建或复用目标方法头。
3. 方法头不可执行才生成可执行性子需求。
4. 子任务完成后父任务待重筹办。
5. 动作失败、无可比较结果、目标不符合、禁止结果统计分账。
```

冲突或过期点：

```text
1. 该计划正文末尾仍写“建议从阶段一开始”，但同主题后续 v0.2 和目标方法头专项已经完成大量内容。
2. 它仍以“工作线程回报中携带回执”为主线；最新结构应是工作线程返回 结构_任务工作结果，由界面线程归并。
3. 它不包含任务管理界面线程边界，不能作为当前执行计划。
```

建议：

```text
作为历史总计划归档。
不要继续在该计划上追加任务。
剩余事项转入新的界面线程边界版筹办缺口计划。
```

### 3.3 `20260508_任务推进状况回执闭环代码修订计划_v0.2.md`

可保留的部分：

```text
1. 规范优先。
2. 动作成功不直通任务完成。
3. 结果状态消息按推进回执判定。
4. 筹办前置状况显式化。
5. 参数不可配、运行账不足、结果不稳定的目标归属口径。
```

冲突或过期点：

```text
1. 非目标写“不重写整个任务管理工作线程”。
   最新已经把界面线程 / 工作包边界拆出，工作线程职责已显著重构。
2. 阶段一仍提 `结构_工作线程桥接结果`。
   最新应使用 `任务管理线程协议::结构_任务工作结果`。
3. 末尾仍写“工作线程快照和界面回报”。
   最新控制面板已经转为“任务管理界面线程 + 任务管理工作线程”双层观察。
4. 该计划不包含“自我线程不直连工作线程”的边界，继续执行会把旧通道带回来。
```

建议：

```text
收口归档为阶段计划。
后续不得按其中的“桥接结果 / 直达回报”继续扩展。
仍未完成的“运行账 / 稳定性真实判据”应单独开新计划，输入输出走任务工作包和任务工作结果。
```

### 3.4 `20260508_任务推进状况回执闭环与目标方法头待重入计划_v0.1.md`

可保留的部分：

```text
1. 找不到方法 -> 建目标方法头。
2. 建方法头后父任务待重筹办。
3. 方法头不可执行 -> 后续由方法结构目标承接。
4. 子需求不复制父需求目标。
5. 学习 / 练习只是可匹配方法，不是硬编码通道。
```

冲突或过期点：

```text
无实质冲突。
只有“三层职责”里继续用“本能函数产出状况”的短句略旧。
按当前规范，应理解为：
    筹办 / 执行 / 本能动作等推进执行单元产出状况。
```

建议：

```text
该计划正文已有“状态：已完成”和完成记录。
应移动到 `计划/完成项`。
```

### 3.5 `20260508_子任务完成触发父任务重筹办与活动子需求判定计划_v0.1.md`

可保留的部分：

```text
1. 子任务完成不是父任务完成。
2. 子任务完成触发父任务待重筹办。
3. 活动直接子需求替代子节点数量判断。
4. 历史已满足子需求不阻塞父任务。
5. 父子同目标硬阻断。
```

冲突或过期点：

```text
1. 风险点中“缺失事实必须携带候选方法存在状态”是旧例。
   最新口径是方法查找未命中先建目标方法头，不创建候选方法存在状态子需求。
2. 该计划已经在实施记录中完成，继续放在未完成根目录会误导后续执行。
```

建议：

```text
移动到 `计划/完成项`。
如果保留原文，不需要重写历史正文，但计划索引应注明：
    其中“候选方法存在状态”只是历史旧例，当前以目标方法头待重入计划为准。
```

### 3.6 `20260509_存在概念与实例创建最小闭环实现计划_v0.1.md`

可保留的部分：

```text
1. 不新增抽象存在主信息类。
2. 不新增存在形态枚举。
3. 概念集为第一阶段权威。
4. 概念存在只作为自然语言入口和实例创建模板。
5. 集合表达用概念集 + 特征条件查询。
6. 只做创建时记忆扩充，不做动态继承、冲突集和复杂合并。
```

冲突或过期点：

```text
无主要冲突。
本计划和 `规范一致性检查与修订说明20260509.md` 对齐。
```

当前状态：

```text
正文执行记录显示 P1-P6 已基本完成。
当前主要剩余：
    没有新增独立测试用例跑 “飞机 -> B-1234” 完整样例。
    旧 `概念模板` 兼容字段后续是否物理清理仍待决定。
```

最新处理：

```text
已移入 `计划/完成项`。
正文已标注“当前代码现状 / 还缺”为历史制定时状态。
核心概念树、自我初始化概念层、任务 / 方法虚拟存在概念集统一入口已由
`20260509_存在概念抽象存在最小启动链实施计划_v0.2.md` 完成承接。
```

### 3.7 `20260509_任务根据需求初始化任务类基础函数实现计划_v0.2.md`

可保留的部分：

```text
1. 任务信息节点保留。
2. 任务信息节点绑定任务虚拟存在。
3. 需求仍指向任务信息节点，不直接指向任务虚拟存在。
4. 任务虚拟存在不挂入任务根节点。
5. 子任务树不与任务虚拟存在.任务根共享裸指针所有权。
6. 方法信息节点不挂入任务根节点。
7. 引用型特征值用指针句柄。
```

冲突或需修订点：

```text
1. 计划目标写“让根据需求初始化任务由任务类统一承接”。
   最新边界下，完整编排应由任务管理界面线程承接。
   任务类只应提供低层公共函数：
       创建 / 复用任务信息节点
       绑定任务虚拟存在
       记录任务虚拟存在特征
       同步运行状态镜像
   不应成为新的任务治理入口层。

2. P7 写“自我线程按需求补任务改为调用 任务类::根据需求初始化任务”。
   最新应改为：
       自我线程确保任务信息节点存在
       自我线程提交任务治理请求
       任务管理界面线程确保任务虚拟存在并初始化语义

3. 计划第 13 节写“任务类负责 任务信息节点挂载 + 任务虚拟存在绑定 + 概念补槽 + 需求填值 + 状态镜像”。
   最新应拆成：
       任务类负责底层写入和节点工具
       任务管理界面线程负责编排和调用

4. 计划仍把“选中自我能力值”列为可迁移项。
   当前代码仍存在：
       任务类::读取任务选中自我能力值
       任务类::写入任务选中自我能力值
       任务模块.管理工作线程 中相关读取
   这与“当前方法信息节点 / 方法虚拟存在”口径冲突，应另开清理项。
```

建议：

```text
不原样执行。
修订为 v0.3。
标题建议：
20260509_任务信息节点基础函数与界面线程初始化编排计划_v0.3.md
```

v0.3 应把职责写成：

```text
任务类：低层节点和镜像工具。
任务管理界面线程：根据任务治理请求绑定任务虚拟存在、按需求初始化语义、生成工作包。
自我线程：只确保任务信息节点和提交请求。
任务管理工作线程：只处理工作包，不补建任务虚拟存在。
```

### 3.8 `计划索引.md`

删除前问题：

```text
1. 当前主计划仍是 `20260509_任务根据需求初始化任务类基础函数实现计划_v0.2.md`。
   该计划已被最新界面线程边界改写，不能作为当前主计划原样执行。

2. 索引正文说 `20260508_子任务完成触发父任务重筹办与活动子需求判定计划_v0.1.md` 已完成，
   但该文件仍在 `计划` 根目录。

3. 索引正文说任务推进状况回执代码修订已完成规范优先收口、P0 和 P1 第一段，
   但相关旧计划仍留在未完成根目录，且未标明被界面线程边界覆盖。

4. 索引没有记录 2026-05-09 后半段新增的边界事实：
   自我线程不直连工作线程；
   任务管理界面线程是任务系统入口；
   工作线程只处理工作包；
   方法虚拟存在服务类已升级为正式门面；
   控制面板已显示界面线程与工作线程双层。
```

建议：

```text
索引已更新：
1. 删除旧当前主计划引用。
2. 记录自我线程 -> 任务管理界面线程 -> 任务管理工作线程的新边界。
3. 当前主计划暂置为“暂无”，等待新建界面线程边界版计划。
```

## 4. 额外观察：`详细设计/待完成`

`详细设计/待完成` 下仍有：

```text
20260412_抽象状态生成详细设计.md
20260412_抽象动态生成详细设计.md
```

这两份不是 `计划` 目录里的实施计划，本次不判定为未完成计划。但它们时间较早，后续如果要执行，必须先按以下新口径复核：

```text
存在 / 特征 / 动态 / 状态不新增并行账本。
存在作为特征值走指针句柄。
概念集为概念归属第一阶段权威。
动态仍按父层观察直接子层状态变化生成，不能跨层。
```

## 5. 推荐下一步

建议按这个顺序处理：

```text
1. 移动已完成专项：
   - 20260508_任务推进状况回执闭环与目标方法头待重入计划_v0.1.md
   - 20260508_子任务完成触发父任务重筹办与活动子需求判定计划_v0.1.md

2. 将旧总计划标记为历史覆盖：
   - 20260508_任务推进状况回执闭环补完计划_v0.1.md
   - 20260508_任务推进状况回执闭环代码修订计划_v0.2.md

3. 存在概念计划已完成归档：
   - 20260509_存在概念与实例创建最小闭环实现计划_v0.1.md
   - 20260509_存在概念抽象存在最小启动链实施计划_v0.2.md

4. 重写任务初始化计划：
   - 20260509_任务根据需求初始化任务类基础函数实现计划_v0.2.md
   改为 v0.3，核心是“任务类低层工具 + 界面线程编排”。

5. 重写筹办缺口计划：
   - 20260508_任务筹办闭环缺口归属与父任务唤醒实现计划_v0.1.md
   改为 v0.2，核心是“工作结果经界面线程上行，自我线程再改树”。

6. 更新 `计划/计划索引.md`。
```

## 6. 当前可执行主线建议

如果现在继续执行，推荐不要从旧的 v0.2 任务初始化计划直接开刀，而是先生成两份新计划：

```text
P1：任务信息节点基础函数与界面线程初始化编排计划 v0.3
P2：任务筹办缺口归属与父任务唤醒界面线程边界修订计划 v0.2
```

原因：

```text
任务管理界面线程已经成为新边界。
未完成计划必须先把自我线程、界面线程、工作线程三层分清。
否则继续按旧计划执行，会重新引入直达桥、双入口或任务虚拟存在替代任务信息节点的问题。
```
