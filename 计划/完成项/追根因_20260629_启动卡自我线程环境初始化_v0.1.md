# 追根因：启动卡在自我线程启动前环境初始化 v0.1

状态：明确堵点。

当前弹窗：
    无弹窗。用户截图显示 `D:\鱼巢\x64\Debug\鱼巢.exe` 默认启动后停在：
    `启动说明 | 当前正在启动：自我线程启动前环境初始化 | 距上一启动项=0ms | 启动累计=3ms`。

最高优先级原因：
    A 主链根因候选：`私有_确保自我线程已启动` 中前置 `私有_初始化控制面板运行态SQL投影()` 连接 SQL Server 失败；失败后世界树等控制面板显示镜像同步继续反复连接 SQL，导致 `初始化自我环境()` 被多次连接超时拖住。

当前层级：
    L0 现象层 / L1 直接生成层：控制台启动说明停在自我线程启动前环境初始化总项。

同层疑点记录：
    1. SQL Server 未启动或连接等待导致启动阻塞；20260622 同类现象曾由 SQL Server 未启动触发，当前需重新验证。
    2. `初始化自我环境()` 内部某一阶段长耗时或死循环；当前总项后没有明细，需抓日志或线程栈定位。
    3. 控制台输出停住但后台日志仍推进；需检查最新运行日志和进程状态。
    4. 运行的 `x64\Debug\鱼巢.exe` 与当前源码不同步；需核对构建时间和必要时重建。

选中下钻分支：
    先在 `数据库ADO模块.cpp` 内为控制面板运行态 SQL 显示镜像建立进程内可用性闸门；初始化失败后后续同步直接返回“显示镜像不可用”，不再反复连接 SQL。按用户要求，对 SQL Server 未启动 / 拒绝访问增加一次性弹窗提醒。

主链判断：
    当前不能把截图直接判定为项目代码失败；需先区分外部 SQL 环境阻塞、初始化内部逻辑阻塞和可观测性不足三类原因。

已确认事实：
    已读取 AGENTS 提供规则、`docs/00_文档总索引.md`、`规范/禁止性规范20260611.md`、`规范/规范目录.md`、`计划/计划索引.md`、工程图谱总索引、规则原子线程条目、线程边界规范和线程生命周期规范。
    `鱼巢.cpp` 中截图文本来自 `私有_确保自我线程已启动`，总项输出后依次调用 `私有_初始化控制面板运行态SQL投影(标记)` 和 `初始化自我环境()`，成功返回后才输出初始化明细。
    `计划/完成项/追根因_20260622_启动卡自我线程环境初始化_v0.1.md` 记录同类现象曾由 SQL Server 未启动导致，但该结论不能直接套用当前运行。
    2026-06-29 只读检查：`MSSQL$SQLEXPRESS` 当前 `Stopped`；无正在运行的 `鱼巢.exe`。
    最新运行日志显示 `08:44:36` 首次 `控制面板运行态SQL初始化失败`，原因为 `ADO打开连接失败 | ... SQL Server 不存在或拒绝访问`；随后世界 SQL 控制面板同步继续多次连接失败并每次等待超时。
    修复实现后，`MSSQL$SQLEXPRESS` 已恢复为 `Running`，正常数据库状态下受控启动没有触发 `数据库未启动提醒` 误弹；默认启动能进入自我线程初始化和任务线程运行。

已排除：
    暂排除“数据库正常时误弹数据库未启动提醒”：受控启动日志未新增 `数据库未启动提醒` 或 `弹窗错误提示`。

旁支问题池：
    停库验证未形成有效证据：尝试临时停 `MSSQL$SQLEXPRESS` 后服务状态恢复为 `Running`，但本轮未抓到新弹窗日志；不把数据库未启动弹窗运行分支宣称为已实机跑通。
    2026-06-30 再次复核：当前 SQL 服务为 `Running`；`Start-Service -Name 'MSSQL$SQLEXPRESS' -ErrorAction Stop` 即使在服务已运行状态下也返回 `Cannot open 'MSSQL$SQLEXPRESS' service on computer '.'`，说明当前执行环境没有可靠服务控制权限，不能安全完成停库 / 复启验证闭环。

已改文件：
    `计划/追根因_20260629_启动卡自我线程环境初始化_v0.1.md`
    `计划/计划索引.md`
    `数据库ADO模块.cpp`
    `说明书/函数功能说明文档.md`

已运行命令：
    `git status --short --untracked-files=no`
    限载读取开工必读文档、工程图谱、规则原子、线程相关规范。
    `rg -n "自我线程启动前环境初始化|当前正在启动|启动说明|默认控制台命令模式|鱼巢主流程|项目日志初始化" -S --glob '!x64/**' --glob '!Debug/**' --glob '!日志/**' --glob '!logs/**' --glob '!.git/**'`
    `Get-Service | Where-Object { $_.Name -like '*SQL*' -or $_.DisplayName -like '*SQL*' }`
    `Get-Process | Where-Object { $_.ProcessName -like '*鱼巢*' }`
    `Get-Content -Path '.\日志\鱼巢_run_20260629.低值g' -Tail 120`
    `python .\tools\生成函数功能说明文档.py`，函数条目 7373。
    `msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m`，0 警告 0 错误。
    受控启动 `.\x64\Debug\鱼巢.exe` 后停止测试进程；当时 SQL 服务已为 `Running`，日志显示 `控制面板运行态SQL初始化完成`、`命令行/自我线程启动前初始化 | 成功=是`，未新增数据库未启动弹窗。
    `git diff --check -- 数据库ADO模块.cpp 计划/计划索引.md 计划/追根因_20260629_启动卡自我线程环境初始化_v0.1.md 说明书/函数功能说明文档.md`，退出码 0。

下一条命令：
    恢复条件具备后复跑：在管理员服务控制权限终端中停止 `MSSQL$SQLEXPRESS`，或由用户先提供 SQL Server 已停止状态 -> 启动 `鱼巢.exe` -> 关闭 `鱼巢 - 数据库未启动提醒` -> 检查 `弹窗错误提示已返回` 和 `SQL控制面板运行态不可用` -> 恢复 SQL 服务。

验收证据：
    已有：Debug x64 构建通过；正常 SQL 运行状态下默认启动不误弹，能进入自我线程初始化。
    待补：SQL Server 停止状态下真实启动应记录一次 `弹窗错误提示 | 标题=鱼巢 - 数据库未启动提醒`，并在关闭弹窗后继续初始化，后续同步失败应走 `SQL控制面板运行态不可用` 快速返回。
    堵点：当前执行环境不能可靠控制 `MSSQL$SQLEXPRESS` 停止 / 启动，不能安全补齐停库实机证据。

禁止宣称：
    不宣称项目代码启动卡住已修复；不宣称内部循环完成、自我苏醒完成或初步成熟完成。
