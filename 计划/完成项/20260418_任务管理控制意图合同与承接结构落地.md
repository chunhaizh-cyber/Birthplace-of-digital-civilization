# 20260418 任务管理控制意图合同与承接结构落地

## 完成内容

本步先把 `任务管理任务模块` 收成正式控制意图承接方。

已完成：

- 在 [D:\鱼巢\任务管理任务模块.消息协议.ixx](D:\鱼巢\任务管理任务模块.消息协议.ixx) 新增正式值域：
  - `枚举_任务管理控制意图值`
  - `枚举_任务管理控制响应状态`
- 新增正式消息结构：
  - `结构_任务管理控制请求`
  - `结构_任务管理控制响应摘要`
- 在线程状态消息里补齐控制字段：
  - `控制请求主键`
  - `当前控制意图`
  - `当前控制响应状态`
  - `是否控制已执行完成`
  - `控制拒绝原因`
- 在 [D:\鱼巢\任务管理任务模块.ixx](D:\鱼巢\任务管理任务模块.ixx) / [D:\鱼巢\任务管理任务模块.impl.cpp](D:\鱼巢\任务管理任务模块.impl.cpp) 新增正式承接结构与接口：
  - `结构_任务管理控制承接记录`
  - `设置治理控制请求(...)`
  - `清除治理控制请求()`
  - `写入治理控制响应(...)`
  - `读取最近治理控制承接记录(...)`
- 把控制请求接进 `结构_任务管理请求 / 单步决策 / 一步结果 / 总结果` 的正式壳层。

## 当前口径

- `自我线程` 不直接改其它线程事实状态。
- 控制意图先进入 `任务管理任务模块`，再由工作线程消费并回报。

## 验证

- 已通过编译验证：
  - `msbuild D:\鱼巢\鱼巢.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64 /m`
- 本步未做运行态验证。
