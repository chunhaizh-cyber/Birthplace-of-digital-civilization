# 追根因 20260630 R23 体素候选包为 0 v0.1

当前异常：

```text
R23 / P8c 初次真实自我场景只读诊断中 voxelCandidatePackageCount=0；P8c2 已追到自然候选包生成，当前异常闭合。
```

最高优先级原因：

```text
已确认不能停成“没有自然样本”的口径正确。
当前追踪已越过自然触发链：
外设等待项 -> D455报告 -> 等待项匹配 -> 观察事实承接请求 -> 观察事实写入 -> 写入观察存在三维体素特征 -> 提交体素加工任务 -> worker 候选结果包。
根因链闭合为：缺触发桥、来源需求被独立树根主键遮蔽、方法供料等待未强制转等待、体素写入/任务提交缺诊断共同导致初次窗口停在候选包为 0。
```

当前层级：

```text
已闭合；剩余旁支为 P8d 之前的非阻塞诊断池。
```

同层疑点记录：

```text
1. 15:07 新桥接代码运行窗口只创建外设观察等待项，未出现 D455 报告、等待项匹配、观察事实承接、体素观察证据或体素加工任务。
2. 13:17 旧运行曾出现 D455 报告、等待项匹配和观察事实承接完成，但该窗口早于 P8c2 触发桥接代码，不足以验证新桥。
3. 若长等待复跑仍没有候选包，需要继续拆分为：没有 D455 报告；有报告但未匹配等待项；有承接但未写观察体素特征；有写入但未提交体素加工任务；有任务但 worker 未出包。
4. 15:18 长窗口已越过 D455 报告、等待项匹配和观察事实承接，但截图后程序开始退出；任务 8 执行 6085 在自我线程停止后才完成，父任务 6 没有机会消费子需求 7 的完成结果并继续重筹办到 6150。
5. 15:27-15:30 的 180 秒窗口已经证明不是单纯停止时序截断：D455 报告、等待项匹配、观察事实承接均出现；任务管理持续识别需求 7 -> 任务 7 的关联，但来源需求 7 在观察事实唤醒入口被判定为“不在当前需求树”。
6. 同层待查候选：
   - 需求 7 入树时未挂到父需求 6，实际成为结构根或孤立根。
   - 自我线程当前需求树根选择漂移，导致已存在需求 7 不在当前扫描根下。
   - 按主键解析需求节点过严，把合法子需求误判为结构根或非当前树节点。
   - 等待项只携带来源需求 ID，不携带来源任务 ID，来源需求解析失败后只能落到安全根任务 1。
7. 代码层第一命中：
   - `双向链表模板核心.h::添加子节点_已加锁` 使用独立根节点 `主键` 作为最新主键计数器；新增子需求时根主键被推进为新子节点主键。
   - `自我线程模块.impl.cpp::私有_按主键扫描独立树` 当前先比较根节点主键，再扫描全链子节点。
   - 因此新增子需求 7 后，结构根主键也为 7；解析来源需求 7 时先命中结构根，`私有_按主键解析需求节点` 又拒绝结构根，真正的子需求 7 没有机会被扫描到。
8. 根主键遮蔽修复后，来源需求已能命中；新的第一分歧点是任务 7 在报告承接完成前进入完成 / 已结算，观察事实唤醒只能跳过终态任务。
9. 15:52:37 任务 7 的 6085 执行缺口创建了等待项 2，但日志显示 `需要等待=0`、`触发口径=执行缺口方法供料`，随后任务被推到 `待重筹办` / `筹办中`，没有真正等待报告 5。
10. 6085 当前仍使用“中间层近组最新材料”，日志明示 `忽略输入报告ID=1`，这会让任务在等待项对应报告承接前就可能用最新材料返回成功。
11. 方法供料等待项转等待修复后，`任务管理界面线程/方法供料等待项转等待` 已出现，父需求 6 可被 D455 报告唤醒；但同一运行窗口仍没有 `写入观察存在三维体素特征`、`提交观察体素加工任务`、`体素加工` 或候选包日志。
12. 16:09-16:11 窗口中 6085 多次成功：`观察新建存在数量=12/12/11`，`观察写入特征数量=437/440/403`，说明自然观察治理写入已经发生；新的第一分歧点下移到 `写入观察存在三维体素特征` 内部或 `提交观察体素加工任务_由观察簇` 内部的早退条件。
13. 16:36-16:38 最新窗口已经出现体素加工任务提交成功和 worker 候选包处理；`voxelCandidatePackageCount=0` 的自然链根因当前闭合，不再作为阻塞停点。
```

选中下钻分支：

```text
已完成下钻分支：`写入观察存在三维体素特征` 和 `提交观察体素加工任务_由观察簇` 的入口、证据检查、轮廓视角读取、父候选快照、worker 启动和任务提交诊断已经给出真实路径证据。
下一步转入 R23 / P8d：子轮廓候选到内部世界子存在身份映射。
保留待查分支：6085 后续还应绑定已承接报告 ID，而不是长期忽略输入报告 ID；部分观察证据仍会因父体素候选快照为空或缺 D455 簇三维体素轮廓材料页无法提交体素加工任务，若阻塞 P8d/P8e 再单独追。
```

已确认事实：

```text
规范要求体素加工只能由已确认存在新增未消费观察体素特征值触发。
P8c2 代码已存在写入观察存在三维体素特征到提交观察体素加工任务的桥。
最新 15:07 运行没有出现体素观察证据或体素加工日志。
15:18 长窗口中报告 6 和报告 5 均完成观察事实承接；报告 6 是扫描变化报告，只作扫描承接材料，未写入任务输入；报告 5 写入安全根任务 1 输入，来源需求 7 因“不在当前需求树”未直接唤醒。
15:18 长窗口中任务 8 / 需求 7 的 6085 执行结果在关闭流程中完成，目标结果符合，但当时自我线程已进入停止，无法继续完成父任务 6 后续推进。
15:27-15:30 的 180 秒窗口中，自然链已出现：D455 逐簇识别报告和扫描变化报告入队，等待项巡检生成 2 条上行消息，报告 6 / 报告 5 均完成观察事实承接。
15:27-15:30 的 180 秒窗口中，需求 7 在 `活动需求任务化已确认关联任务引用` 中反复对应任务 7，但 `观察事实更新来源需求唤醒跳过` 仍记录 `原因=来源需求不在当前需求树 | 来源需求=7 | 报告ID=5 | 等待项ID=2`。
同一窗口中报告 5 随后写入任务 1 输入，而不是任务 7 输入；6150、写入已确认观察存在、写入观察存在三维体素特征、提交观察体素加工任务和体素加工日志均未出现。
已定位到代码解释：独立根 `主键` 不是稳定业务身份，而是当前链的最新编号；需求 7 被结构根同号遮蔽后，观察事实更新链误以为来源需求不存在。
根主键遮蔽修复后构建通过，真实运行中已出现 `需求裁决/观察事实更新命中来源需求`，证明该层不再是当前主堵点。
新主堵点证据：`任务管理界面线程/按当前方法供料要求创建外设观察等待项` 对任务 7 创建等待项 2 时 `需要等待=0`，同一轮 `工作结果应用尾部证据` 显示 `方法供料等待项=1`、`当前等待缺口唤醒=1`，但建议状态仍为 `待重筹办`。
后续日志显示任务 7 已结算后，观察事实更新任务唤醒会因 `任务状态不允许由观察事实唤醒` 跳过；不能通过放开已结算任务唤醒修复，应修正等待项创建后的状态推进顺序。
方法供料等待项转等待修复后，最新日志已出现 `任务管理界面线程/方法供料等待项转等待`，且没有新的终态任务唤醒拦截；自然链已推进到 6085 观察治理写入成功，但仍没有体素观察证据/体素加工日志。
体素写入诊断复跑后，最新日志出现 `观察体素加工任务提交诊断 | 阶段=提交任务 | 成功=1` 15 次。
最新日志出现 `体素加工工作线程/候选结果包处理` 9 次。
9 个 worker 候选结果包均同时具备子轮廓候选和局部子体素候选。
其中 3 个候选包具备 `融合点=1` 且 `可提交状态=0`：
`日志/鱼巢_run_20260630.低值g:141083` 子轮廓候选数量=106，局部子体素候选数量=106；
`日志/鱼巢_run_20260630.低值g:141998` 子轮廓候选数量=128，局部子体素候选数量=128；
`日志/鱼巢_run_20260630.低值g:143614` 子轮廓候选数量=128，局部子体素候选数量=128。
```

已排除：

```text
不能用控制面板刷新、场景体素快照、外设原始帧或伪造样本触发体素加工。
不能把候选包为 0 当作子轮廓算法失败或成功。
```

下一条命令：

```text
更新 R23 施工计划和计划索引：P8c2 关闭，下一步进入 P8d 身份映射。
```

验收证据：

```text
已满足当前 P8c2 根因切片：
同一运行窗口内出现新增观察体素特征值触发体素加工任务。
同一运行窗口内 worker 生成候选结果包。
同一运行窗口内出现子轮廓候选数量 > 0 且局部子体素候选数量 > 0。
同一运行窗口内至少 3 个候选包具备融合点=1 且可提交状态=0。
```

禁止宣称：

```text
不宣称子轮廓提取成功。
不宣称 P8d / P8e / P9 完成。
不宣称视觉系统完成、自我苏醒完成或初步成熟完成。
```
