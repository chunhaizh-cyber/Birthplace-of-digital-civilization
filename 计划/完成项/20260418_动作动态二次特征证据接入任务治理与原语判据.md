# 20260418 动作动态二次特征证据接入任务治理与原语判据

## 本步完成

- 将任务管理现有两条动作动态线正式折入 `结构_任务治理二次特征证据`
  - `当前动作已启动`
  - `当前动作已完成`
  - `当前动作失败退出`
  - `当前处于方法位推进中`
  - `当前处于原语边界执行中`
  - `当前动作已形成稳定结果`
- 复用现有 `当前方法位专项动态 / 当前最小原语动态`，不额外创建第三套动作动态入口。
- 在 `私有_P1_刷新任务治理二次特征证据(...)` 中新增动作动态判定：
  - 优先读取桥接动态主信息中的 `来源动作相位 / 业务语义类型`
  - 标题只作为缺省补判
- 将动作失败退出并入显式冲突与重筹办判据。
- 将“动作完成且形成稳定结果”并入 `因果稳定` 原语的高可信证据来源。
- 将“动作失败退出”并入 `可继续推进` 原语的否决面。
- 将新增动作动态证据写入场景二次特征和治理证据摘要，保证请求/决策/一步结果/学习链可观察。

## 本步判断

- 当前最值得接入治理的不是“更多动作动态种类”，而是把已经存在的：
  - 方法位专项动态
  - 最小原语边界动态
  真正升级成任务治理证据。
- 这一步之后，任务管理已经能把“动作过程”当作正式判断证据，而不只是标题或桥接锚点。

## 暂未处理

- 还没有增加“长时间无推进 / 超时 / 需重试”这类时间窗动作证据。
- 还没有把动作动态直接沉成专门的实例因果节点，当前仍是先接入治理证据与原语解释层。

## 后续建议

- 下一步优先补“长时间无推进 / 重试必要 / 动作被中断”这组三态证据。
- 再把稳定出现的动作动态结果对正式沉成实例因果，继续收口到可靠方法候选链。
