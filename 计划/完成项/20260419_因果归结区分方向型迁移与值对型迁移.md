# 20260419 因果归结区分方向型迁移与值对型迁移

> 2026-05-22 验证口径修订：
> 后续所有计划的验证统一改为日志模块验证，以真实运行产生的日志、状态快照、动作动态与错误日志为证据。
> 严禁新增或依赖模拟测试、夹具测试、伪造样本、伪造入口、伪造动作动态或 `--*-check` 回归命令。
> 历史计划中已有的回归命令、夹具和模拟测试描述仅作为历史记录，不再作为当前执行或验收依据。

## 本次完成

- 在 `因果类.cpp` 中把状态迁移归结正式拆成两类：
  - `方向型迁移`
  - `值对型迁移`
- 对于 `差值型` 状态迁移：
  - 继续使用 `方向区间 / 差值 / 标量区间` 做方向一致性验证。
- 对于 `判等型 / 差异度型` 状态迁移：
  - 不再强行解释为“增加 / 减少”；
  - 改为按 `因值 -> 果值` 这组状态对进行归结；
  - 只有值对完全一致，才允许并入同一条抽象因果。

## 关键修正

- `私有_由状态生成状态迁移特征(...)`
  - 对 `值对型` 迁移不再保留伪方向；
  - 会清空差值/方向，并统一把迁移概念改成 `变为`。
- `私有_按动态补齐抽象因果模板(...)`
  - 对 `方向型` 迁移继续做方向一致性校验；
  - 对 `值对型` 迁移改成“值对一致性校验”；
  - `值对型` 迁移如果值对不同，不再错误并入既有模板。

## 当前口径

- 数值型特征：
  - `条件范围 + 动作 + 方向一致的状态迁移`
  - 才归入同一条抽象因果。
- 枚举型 / 无序型特征：
  - `条件范围 + 动作 + 因值对果值一致`
  - 才归入同一条抽象因果。
- 因此：
  - 数值型因果看方向；
  - 枚举型因果看值对。

## 验证

- 运行：
  - `msbuild D:\鱼巢\鱼巢.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64 /m`
- 结果：
  - `0 error / 0 warning`

## 后续建议

- 下一步可继续把控制面板中的迁移摘要也分成两种显示：
  - 数值型：显示 `结果状态比初始状态大/小`
  - 值对型：显示 `由X变为Y`
