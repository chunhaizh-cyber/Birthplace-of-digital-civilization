# 20260418 任务管理运行时判断迁移总表阶段0完成

## 本次完成内容

本次完成了《任务管理任务二次特征化与因果驱动重构计划 v0.1》的阶段 0：

`建立判断迁移总表`

正式产物为：

- `D:\鱼巢\计划\待完成项\20260418_任务管理运行时判断迁移总表_v0.1.md`


## 本次落定的口径

本次把 `任务管理任务模块` 中的运行时判断，统一分成了三类：

1. `任务/方法结构表达`
2. `二次特征证据`
3. `最小判断原语`

并明确了：

- 结构关系不再误迁成二次特征
- 当前判断证据应优先沉为二次特征
- 原语只负责解释证据，不再继续扩大成业务判断容器


## 本次梳理出的第一批关键迁移对象

第一批直接列入后续施工对象的是：

1. `私有_P0_执行原语(...)`
2. `私有_可进入等待分支(...)`
3. `私有_业务子任务执行已完成(...)`
4. `私有_结果节点匹配当前治理(...)`

这四处覆盖了：

- 原语解释入口
- 等待判据
- 完成判据
- 回接命中判据


## 本次确认的工程判断

本次再次确认：

`当前不需要推倒重写。`

更稳的路线仍然是：

`保留请求/原语/高阶排序/选择/写回骨架，逐段迁移判断证据源，并前推因果驱动方法树/任务树生成。`


## 未做事项

本次没有改运行时代码。

本次没有跑编译。

本次只完成了阶段 0 的文档分拣与迁移入口收口。


## 下一步

下一步进入阶段 1：

`建立第一批任务治理二次特征证据层`

优先顺序保持为：

1. `私有_P0_执行原语(...)`
2. `私有_可进入等待分支(...)`
3. `私有_业务子任务执行已完成(...)`
4. `私有_结果节点匹配当前治理(...)`
