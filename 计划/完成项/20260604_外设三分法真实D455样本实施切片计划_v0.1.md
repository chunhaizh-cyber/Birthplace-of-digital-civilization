# 外设三分法真实 D455 样本实施切片计划 v0.1

日期：2026-06-04

状态：由 `计划/20260602_外设三分法技术储备与实施路线_v0.1.md` 拆出的当前执行计划。

## 1. 目标

把外设三分法从技术储备拆成可连续推进的真实 D455 样本链：

```text
D455 材料包可回查
-> 逐簇识别样本
-> 已识别区域扫描变化样本
-> 指定目标跟踪样本
-> 自我提交入口承接
-> 日志 / 命令行样本卡
```

本计划只建立真实样本生产和承接条件，不宣称识别、扫描、跟踪已经完整实现。

## 2. 边界

```text
1. D455 / RGB-D 仍是材料供应器，不是世界存在识别器。
2. 识别报告只处理观察材料归属，不直接确认稳定世界存在。
3. 扫描报告只处理已识别区域 / 已确认存在的变化，不替代全场景明确。
4. 跟踪报告只处理指定目标，不证明全场景安全或全场景扫描完成。
5. 外设提交包必须经自我组织和提交入口裁决，不能直接写安全 / 服务结论。
6. 不伪造样本；没有真实 D455 数据时只做接口、字段、队列和日志 / 命令行证据准备。
```

## 3. 执行切片

### P1 D455 材料包最小可回查

目标：

```text
对账并补齐真实 D455 样本所需材料字段：
来源外设、帧号、时间戳、尺寸、深度 / 彩色句柄或摘要、
ROI / 掩码 / 点集 / 深度切片句柄、材料质量、失败原因。
```

验收：

```text
真实运行日志或命令行输出能看到一张 D455 材料包的可回查证据卡。
没有相机样本时，只能记录“待真实 D455 数据”，不得构造假帧。
```

### P2 逐簇识别样本

目标：

```text
建立逐簇识别报告的最小样本卡：
观察像素簇集合、有效观察区域、未识别 / 候选 / 冲突区域、
像素归属账本、候选存在集合、证据不足原因。
```

验收：

```text
真实运行日志或命令行输出能展示逐簇识别报告样本卡；
报告只表达材料归属与候选，不写已确认观察存在最终结论。
```

### P3 已识别区域扫描变化样本

目标：

```text
建立扫描变化报告的最小样本卡：
来源已识别区域 / 已确认存在、上一状态、当前状态、
变化摘要、未解释变化、需重识别区域、失败原因。
```

验收：

```text
扫描报告能证明指定已识别区域出现可回查变化；
不得把扫描报告当作全场景明确或安全结论。
```

### P4 指定目标跟踪样本

目标：

```text
建立跟踪报告的最小样本卡：
目标存在、目标窗口 / ROI、目标特征、连续观测摘要、
丢失 / 遮挡 / 重捕获状态、忽略非目标说明。
```

验收：

```text
跟踪报告只证明指定目标特征值持续可观察；
不得替代扫描或全场景识别。
```

### P5 自我提交入口承接

目标：

```text
识别提交包 / 扫描提交包 / 跟踪提交包进入自我提交入口；
自我组织后再决定是否生成存在候选、变化动态、跟踪事实或缺口需求。
```

验收：

```text
外设供包 -> 自我组织 -> 提交入账 -> 因果解释的链路可在日志、状态快照、动作动态和错误日志追踪。
```

## 4. 当前切片

```text
P3 已识别区域扫描变化样本。
P1 已完成接口 / 字段 / 命令行材料包证据卡补齐，并已通过真实 D455 单帧材料包验收。
P2 已完成逐簇识别报告最小样本卡和真实 D455 单帧样本卡验收。
P3 已完成扫描变化报告 / 扫描提交包对账和首帧基准样本卡输出；
当前停在有可比历史的真实扫描变化样本卡验证：
    自然 self-check 本轮没有创建扫描等待项；
    待恢复条件见 计划/待完成项/20260605_D455_P3可比历史扫描变化样本待完成项.md。
不得把首帧基准宣称为已出现可回查变化；
不得把扫描报告当作全场景明确或安全结论。
```

## 5. 已完成检查点

```text
P1 D455 材料包最小可回查：
    已补 构造D455材料包证据卡摘要；
    --d455-capture-once 输出最新材料包证据卡；
    无报告时不构造假帧。

验证：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0；仅 LNK4075 既有链接警告。

真实验收：
    .\x64\Debug\鱼巢.exe --d455-capture-once
    退出码=0；
    输出 D455材料包证据卡；
    尺寸=640x480；
    观察簇=61；
    材料句柄总数=433；
    缺失材料=0；
    失败簇=0；
    短期观察材料页提交=1。

P2 逐簇识别样本：
    已补 构造D455逐簇识别样本卡摘要；
    D455 单帧材料包证据卡输出同步附带逐簇识别样本卡；
    --d455-capture-once 输出最新逐簇识别样本卡；
    无报告时不构造假帧。

验证：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0；仅 LNK4075 既有链接警告。

真实验收：
    .\x64\Debug\鱼巢.exe --d455-capture-once
    退出码=0；
    输出 D455逐簇识别样本卡；
    观察像素簇=11；
    有效观察区域=11；
    未识别区域=10；
    不可观测区域=1063；
    候选存在=11；
    报告证据不足=1；
    只读边界=材料归属和存在候选,不确认世界存在。

控制面板展示：
    已按 2026-06-05 要求撤回当前 D455 样本卡的控制面板展示绑定；
    控制面板不作为 P1 / P2 / P3 当前验收面。

P3 已识别区域扫描变化样本：
    已补 构造D455扫描变化样本卡摘要；
    自然 D455 扫描报告入队路径已同步追加 扫描变化样本卡 日志；
    --d455-capture-once 已同步输出 单帧扫描变化样本卡；
    首帧基准时 上一帧ID=0；
    首帧基准时 新增簇=0，事实口径只写发现存在 / 发现特征候选；
    扫描样本卡明确只读边界，不确认全场景明确，不写安全结论。

验证：
    msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
    退出码=0；仅 LNK4075 既有链接警告。

真实单帧验收：
    .\x64\Debug\鱼巢.exe --d455-capture-once
    退出码=0；
    输出 D455扫描变化样本卡；
    当前帧=1；
    上一帧=0；
    状态=首帧基准；
    当前观察簇=13；
    发现候选=13；
    新增簇=0；
    消失簇=0；
    变化簇=0；
    首帧基准=1。

自然自检验证：
    .\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=30000
    退出码=0；
    本轮 20:20 自检窗口没有新的 扫描变化报告入队 / 扫描变化样本卡；
    日志显示 外设观察等待项创建延后，入队阶段按目标特征名映射=0。

P3 当前停点：
    真实单帧首帧基准样本卡已通过；
    状态=有可比历史 的真实扫描变化样本卡尚未取得；
    缺少自然扫描等待项入队条件，不伪造两帧或等待项；
    待完成项：计划/待完成项/20260605_D455_P3可比历史扫描变化样本待完成项.md。
```
