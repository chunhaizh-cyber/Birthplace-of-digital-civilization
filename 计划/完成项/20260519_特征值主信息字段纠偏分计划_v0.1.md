# 特征值主信息字段纠偏分计划 v0.1

更新时间：2026-05-19

对应主信息：`特征值主信息类`

## 审查结论

`特征值主信息类` 目前结构合理：值是本体，哈希与命中统计是索引 / 缓存。

## 字段口径

```text
权威字段：
    值

索引 / 缓存：
    内容哈希
    命中次数
    最后命中
```

## 纠偏动作

```text
P1. 确认 内容哈希 只用于索引和去重。
P2. 命中次数 / 最后命中 只用于缓存统计。
P3. 不向特征值主信息类加入业务语义字段。
```

## 验收

```powershell
rg -n "特征值主信息类|内容哈希|命中次数|最后命中" -S 特征值主信息类.h 特征值类.cpp
```

通过条件：

```text
特征值主信息只表达值与索引统计，不承载业务判断。
```

## 执行记录

### 2026-05-19

```text
P1-P3 首轮完成：
- 已标注 VecU 值为特征值主信息的权威本体。
- 已标注 内容哈希 为去重 / 快速查找索引缓存，不表达业务特征类型。
- 已标注 命中次数 / 最后命中 为证据池复用统计，不作为任务、需求、结算判断来源。
- 已标注旧 特征值_类型 比较字段名为兼容入口，实际比较内容哈希。
- 已标注特征值类中的哈希候选表与轮廓分层索引均为运行期索引，不改变特征值语义。
```

验收：

```text
rg 特征值主信息类 / 内容哈希 / 命中次数 / 最后命中：通过。
Debug x64 构建：通过。
--demand-tree-growth-check：通过。
--task-method-multicandidate-selection-check：通过。
```
