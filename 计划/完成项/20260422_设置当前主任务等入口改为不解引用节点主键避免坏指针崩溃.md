# 完成项

> 2026-05-22 验证口径修订：
> 后续所有计划的验证统一改为日志模块验证，以真实运行产生的日志、状态快照、动作动态与错误日志为证据。
> 严禁新增或依赖模拟测试、夹具测试、伪造样本、伪造入口、伪造动作动态或 `--*-check` 回归命令。
> 历史计划中已有的回归命令、夹具和模拟测试描述仅作为历史记录，不再作为当前执行或验收依据。

- 时间：2026-04-22
- 主题：设置当前主需求/任务/方法时不再为记录标题解引用节点主键

# 背景

自我线程初始化阶段虽然已经删除了“设置当前主任务”的主动调用，但旧运行体或失效指针一旦仍然流入 `自我类::设置当前主任务(...)`，原实现会先调用 `节点->获取主键()`，从而在坏指针场景下直接崩溃。

# 修改

- 修改文件：[D:\鱼巢\自我类.cpp](D:/鱼巢/自我类.cpp)
- 调整 `设置当前主需求(...)`、`设置当前主任务(...)`、`设置当前主方法(...)`
- 这三个入口现在不再为了记录变化标题去解引用传入节点的主键
- 当传入节点非空时，改为写入固定安全标题：
  - `已切换到需求节点`
  - `已切换到任务节点`
  - `已切换到方法节点`

# 结果

- 即便还有旧路径或坏指针误调这三个入口，也不会再在“获取主键”阶段崩溃
- 初始化阶段“不设置当前主任务”的边界继续保留

# 验证

```powershell
msbuild D:\鱼巢\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:TargetName=鱼巢_tmp /m /clp:ErrorsOnly
```

