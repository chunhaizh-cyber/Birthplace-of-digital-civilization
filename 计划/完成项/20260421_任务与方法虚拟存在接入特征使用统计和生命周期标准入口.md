## 完成项

在任务和方法两条虚拟存在链上，补齐了“特征使用统计/生命周期”的最小标准读写入口。

## 改动文件

- `D:\鱼巢\任务类.h`
- `D:\鱼巢\任务类.cpp`
- `D:\鱼巢\方法类.h`
- `D:\鱼巢\方法类.cpp`

## 方法侧新增

新增对外入口：

- `方法类::同步方法特征使用统计(...)`
- `方法类::读取方法特征账本生命周期标准特征(...)`

并在 `初始化方法虚拟存在信息(...)` 中默认补齐这些标准镜像特征：

- 生命周期
- 暴露次数
- 读取次数
- 有效使用次数
- 历史使用次数
- 近期使用次数
- 决策输入次数
- 派生父特征次数
- 需求触发次数
- 风险闸门次数
- 验证信号次数
- 解释依据次数
- 结果改变次数
- 坏结果次数
- 下游依赖特征数
- 最近使用时间
- 最近有效使用时间

## 任务侧新增

新增对外入口：

- `任务类::取或创建_任务虚拟存在(...)`
- `任务类::记录任务虚拟存在特征(...)`
- `任务类::初始化任务虚拟存在信息(...)`
- `任务类::同步任务特征使用统计(...)`
- `任务类::读取任务特征账本生命周期标准特征(...)`

并把 `创建任务头结点(...)` 收口成默认调用 `初始化任务虚拟存在信息(...)`，保证任务头出生后就有一套可读写的使用账本镜像。

## 当前边界

- 这一步只补“标准入口”和“虚拟存在镜像特征”，还没有把自我线程、任务管理、学习执行里的真实 `FeatureUseEvent / FeatureExposureEvent` 自动落到账本。
- 也还没有把高阶特征父依赖、需求显现、风险闸门这些使用边统一汇总成统计。
- 当前仍是“有标准入口可接”，下一步应开始从真实治理链写入这些入口。

## 建议下一步

优先从这三条真实主链开始接账：

1. 任务管理的“需求显现 / 学习任务创建 / 风险闸门”
2. 学习执行的“高阶父特征依赖 / 验证信号 / 坏结果”
3. 下一步合成器的“决策输入 / 排序信号 / 结果改变”

## 验证

已执行：

```powershell
msbuild D:\鱼巢\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /m /clp:ErrorsOnly
```

结果：

- 编译通过
- `0` 错误
