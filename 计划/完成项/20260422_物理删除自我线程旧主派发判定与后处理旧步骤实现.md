# 2026-04-22 物理删除自我线程旧主派发判定与后处理旧步骤实现

## 本次目标

彻底清理已经退出运行主路径的旧自我线程逻辑代码，不再只停留在“断开调用”，而是物理删除未再使用的旧步骤实现与辅助读取函数。

## 本次完成

1. 从 `D:\鱼巢\自我线程模块.impl.cpp` 物理删除旧主派发判定二次特征读取结构与辅助函数：
   - `结构_主派发判定二次特征读取结果`
   - `私有_读取主派发判定二次特征(...)`

2. 从 `D:\鱼巢\自我线程模块.impl.cpp` 物理删除旧步骤实现：
   - `步骤_生成主派发判定二次特征_(...)`
   - `步骤_路由结果并协调父任务_(...)`
   - `步骤_本轮结算学习触发故障收口_(...)`

3. 清理 `D:\鱼巢\自我线程模块.ixx` 中对应的旧步骤声明，仅保留仍在新主路径里的：
   - `步骤_冻结本轮消息批次_(...)`
   - `步骤_规范化消息并落一次特征账_(...)`
   - `步骤_重算受影响二次特征_(...)`
   - `步骤_刷新线程上下文并生成治理帧_(...)`
   - `步骤_整理需求并做根层重判_(...)`
   - `步骤_生成并执行主派发决议_(...)`

4. 保留并整理 `步骤_生成并执行主派发决议_(...)` 的新最小主链实现，使其只负责：
   - 读取当前主需求
   - 判断需求是否适合任务化
   - 按需求补任务
   - 设置当前主任务
   - 将当前主任务提交给任务管理工作线程推进
   - 回收上行消息与推进事件

## 当前结果

当前自我线程主路径已经收成：

`收消息 -> 规范化 -> 重算特征 -> 刷新线程上下文 -> 整理需求 -> 生成并执行主派发决议`

旧的“主派发判定二次特征生成 / 结果路由协调 / 本轮结算学习故障收口”不再存在于运行代码中。

## 剩余说明

全仓仍能搜到这些旧步骤名的地方，仅剩计划文档与历史完成项，不再属于运行代码。

## 验证

已执行：

```powershell
msbuild D:\鱼巢\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:TargetName=鱼巢_tmp /m /clp:ErrorsOnly
```

编译通过。
