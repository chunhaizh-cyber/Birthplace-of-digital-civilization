# 语言记录主信息字段纠偏分计划 v0.1

> 2026-05-22 验证口径修订：
> 后续所有计划的验证统一改为日志模块验证，以真实运行产生的日志、状态快照、动作动态与错误日志为证据。
> 严禁新增或依赖模拟测试、夹具测试、伪造样本、伪造入口、伪造动作动态或 `--*-check` 回归命令。
> 历史计划中已有的回归命令、夹具和模拟测试描述仅作为历史记录，不再作为当前执行或验收依据。

更新时间：2026-05-19

对应主信息：`语言记录主信息类`

## 审查结论

`语言记录主信息类` 属于语言记录层本体结构，当前不直接承担任务、需求、方法、世界事实判断。

## 字段口径

```text
可保留：
    语言记录本体字段
    来源语言 / 文本 / 记录索引字段

不得新增：
    需求状态
    任务状态
    方法状态
    动态业务语义
    因果作用解释
```

## 纠偏动作

```text
P1. 检查该类字段是否只用于语言记录。
P2. 若发现字段参与治理判断，迁移为特征 / 二次特征 / 因果证据。
P3. 标注语言记录字段不得作为世界事实权威来源。
```

## 验收

```powershell
rg -n "语言记录主信息类|语言记录" -S 基础信息主信息类.h 基础信息类.cpp 控制面板类.cpp
```

通过条件：

```text
语言记录字段只服务文本记录和追溯，不参与任务完成、需求满足或结算判断。
```

## 执行记录

```text
2026-05-19:
    已检查 `语言记录主信息类`。
    代码口径：
        语言记录只保存语言结构入口；
        不作为存在、特征、任务、需求或结算事实的权威来源；
        业务判断必须通过特征 / 二次特征 / 因果证据链表达。
    代码调整：
        `基础信息主信息类.h` 为 `语言词性` 补充边界注释；
        `比较(...)` 改为安全指针顺序比较，不再使用裸指针差值；
        `基础信息类.cpp` 中语言记录创建和语素入口引用扫描补充边界说明。
    验收：
        `rg -n "语言记录主信息类|语言记录" -S 基础信息主信息类.h 基础信息类.cpp 控制面板类.cpp` 通过；
        Debug x64 构建通过，仅有 LNK4075 非增量链接常规警告；
        `--demand-tree-growth-check` 通过。
```
