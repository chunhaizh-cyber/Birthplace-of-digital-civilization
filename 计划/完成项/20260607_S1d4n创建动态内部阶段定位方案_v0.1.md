# S1d-4n 创建动态内部阶段定位方案 v0.1

日期：2026-06-08

## 1. 目标

```text
承接 S1d-4m。

S1d-4m 已定位：
    `查找相邻前状态` 未出现 100000us 以上慢采样；
    最新线程 T29128 的 `状态动态刷新` 平均约 612992us，最大 908455us；
    剩余热点应在 `创建动态` 内部。

代码路径：
    `状态记录后刷新动态`
    -> `查找相邻前状态`
    -> `创建叶子动态`
    -> `创建动态(场景, 初始状态, 结果状态, 0, now)`
    -> `创建动态(场景, 动态节点主信息类*)`

本切片目标：
    拆 `动态类::创建动态(场景, 动态节点主信息类*)` 内部阶段；
    定位慢点是已有状态动态查重、动态索引追加排序，还是动态二次特征 / 聚合生成。
```

## 2. 允许修改

```text
动态类.cpp
计划/20260607_S1d4n创建动态内部阶段定位方案_v0.1.md
计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
计划/计划索引.md
实施记录/20260608_S1d4n创建动态内部阶段定位_Codex断点清单.md
```

## 3. 禁止修改

```text
不删除动态二次特征生成。
不删除相邻动态协同、连续同向聚合或同类事件段聚合。
不改变 `创建动态` 返回语义。
不改变动作动态结构或任务状态动作动态来源。
不新增基础信息全局索引、候选方法索引或因果主果索引。
不新增模拟入口、夹具、伪造样本或 `--*-check` 回归入口。
```

## 4. 实施步骤

```text
S1d-4n-1：
    在 `动态类.cpp` 复用低频阶段计时工具，新增创建动态采样序号。

S1d-4n-2：
    在 `动态类::创建动态(场景, 动态节点主信息类*)` 内记录：
        状态引用解析；
        状态动态合法检查；
        已有状态动态查找；
        动态主体特征补齐；
        动态名称补齐；
        业务语义刷新；
        添加动态节点；
        场景主信息；
        追加动态索引；
        动态二次特征；
        相邻动态协同；
        连续同向聚合；
        同类事件段聚合。

S1d-4n-3：
    采样锚点：`创建动态/内部阶段耗时`。
    采样规则：
        总耗时低于 100000us 不输出；
        慢调用前 3 次输出；
        之后每 10 次输出；
        总耗时不低于 500000us 输出。

S1d-4n-4：
    构建 Debug x64，真实运行 `--self-check-log --self-check-log-wait-ms=10000`。

S1d-4n-5：
    汇总最新线程的 `创建动态/内部阶段耗时`，确定下一步瘦身目标。
```

## 5. 验证预测

```text
构建：
    Debug x64 构建通过。

真实运行：
    退出码=0。
    日志出现 `创建动态/内部阶段耗时`。
    日志保留 `任务状态动作动态=<动态主键>`。
    不出现 `任务状态被动提交/逻辑错误`。
    不出现 `任务状态动作动态逻辑错误`。
    不出现旧路径锚点 `准备本能调用`、`执行本能方法`、`任务域本能输入包`。

性能判断：
    如果 `已有状态动态查找` 为主耗时，下一步优化状态动态查重路径。
    如果 `追加动态索引` 为主耗时，下一步优化动态索引追加排序。
    如果二次特征 / 聚合为主耗时，下一步按具体生成器拆分或瘦身。
```

## 6. 阻塞条件

```text
如果诊断日志破坏动作动态账或真实运行异常，停止并修正。
如果下一步必须删除动态二次特征或聚合生成才能提速，停止在证据和方案。
```

## 7. 落地结果

```text
已落实：
    `动态类::创建动态(场景, 动态节点主信息类*)` 增加低频内部阶段采样；
    输出锚点为 `创建动态/内部阶段耗时`；
    保留拒绝生成、状态值相同、已有动态和完整创建的原返回语义。

验证：
    Debug x64 构建通过；
    `.\x64\Debug\鱼巢.exe --self-check-log --self-check-log-wait-ms=10000` 退出码 0；
    日志/鱼巢_run_20260608.低值g 出现 `创建动态/内部阶段耗时`；
    日志保留 `任务状态动作动态=<动态主键>`；
    未检出 `任务状态被动提交/逻辑错误`、`任务状态动作动态逻辑错误`、`准备本能调用`、`执行本能方法`、`任务域本能输入包`。

定位结果：
    最新线程 T11876 创建动态慢样本 44 条；
    最慢阶段分布：`同类事件段聚合=26`、`动态二次特征=18`；
    层级 0 慢样本 26 条，最慢阶段全部为 `同类事件段聚合`；
    层级 1 慢样本 18 条，最慢阶段全部为 `动态二次特征`；
    `已有状态动态查找` 平均约 380us，`追加动态索引` 平均约 563us，不是主耗时。

结论：
    层级 0 的 `同类事件段聚合` 慢主要包含递归创建层级 1 聚合动态；
    真正需要继续拆的是 `二次特征应用模块.ixx` 中动态二次特征生成内部阶段；
    下一步进入 S1d-4o：动态二次特征生成内部阶段定位。
```
