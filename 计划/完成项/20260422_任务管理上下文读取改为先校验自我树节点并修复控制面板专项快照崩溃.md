已完成项：任务管理上下文读取改为先校验自我树节点并修复控制面板专项快照崩溃

时间：2026-04-22

本次改动：
- 在 `D:\鱼巢\任务管理任务模块.impl.cpp` 中新增自我需求树 / 任务树 / 方法树节点校验 helper。
- `读取任务管理上下文(...)` 不再直接信任 `自我.获取当前主需求/任务/方法()` 返回的裸指针，而是先校验节点是否属于当前自我树，再写入上下文。
- `私有_主体锚点任务(...)` 现在会先校验宿主任务属于当前自我任务树，并在向上追父任务时继续校验父任务节点，避免失效宿主任务引用进入主体桥接链。
- `读取任务管理主体虚拟存在(...)` 也补上了宿主任务安全校验，避免其他入口直接用旧宿主任务裸指针触发同类崩溃。

验证：
- `msbuild D:\鱼巢\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:TargetName=鱼巢_tmp_fix_0422_1 /m /clp:ErrorsOnly`
- `D:\鱼巢\x64\Debug\鱼巢_tmp_fix_0422_1.exe --panel-html`

结果：
- 编译通过。
- 控制面板 HTML 导出通过，不再在 `读取任务管理上下文 -> 读取任务管理主体虚拟存在 -> 私有_主体锚点任务` 这条链上崩溃。
