# 20260418 自我线程首批接入自我桥模块

> 2026-05-22 验证口径修订：
> 后续所有计划的验证统一改为日志模块验证，以真实运行产生的日志、状态快照、动作动态与错误日志为证据。
> 严禁新增或依赖模拟测试、夹具测试、伪造样本、伪造入口、伪造动作动态或 `--*-check` 回归命令。
> 历史计划中已有的回归命令、夹具和模拟测试描述仅作为历史记录，不再作为当前执行或验收依据。

## 本次目标

将 `自我线程模块.impl.cpp` 中最稳定、最底层的自我读写入口，开始改为显式走 `自我_*` 桥模块，而不是继续直接读取 `自我类` 的初始化与核心状态细节。

## 本次完成

1. 在 `自我线程模块.impl.cpp` 中新增 `import 自我_初始化模块;` 与 `import 自我_核心状态模块;`
2. 将 `自我线程类::自我初始化(...)` 改为调用 `执行自我初始化_最小_v0(...)`
3. 将 `刷新初始化标记_已加锁()` 改为调用 `读取自我初始化结果_最小_v0(...)`
4. 将需求树构建、主需求候选选择、根值归结摘要等位置的双值读取，改为先读取 `读取自我核心状态快照_最小_v0(...)`，再消费快照中的 `安全值 / 服务值 / 物理安全值 / 风险安全值`
5. 归结后根需求权重回填与根值摘要输出，也统一改成基于“归结后核心状态快照”而不是再次直接读 `自我类`

## 本次未做

1. 还没有把 `自我线程` 中的固定机制执行接到 `自我_固定机制模块`
2. 还没有把结果治理、学习承接、恢复快照主干改成统一走 `自我_*` 桥
3. 还没有处理 `自我_主循环治理模块 / 自我_学习承接模块 / 自我_恢复快照模块` 对 `自我线程模块` 的反向依赖

## 验证

已执行：

```powershell
msbuild D:\鱼巢\鱼巢.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64 /m
```

结果：

- `0 error`
- `0 warning`

本次仅做了编译级验证，未做运行态验证。

## 下一步建议

下一刀优先把 `自我线程模块.impl.cpp` 中“固定机制巡检/心跳”相关逻辑接到 `自我_固定机制模块`，形成：

`主循环心跳阶段 -> 固定机制桥 -> 核心状态快照 -> 线程摘要`

这样自我线程接入自我类就会从“只读桥”推进到“读写桥”。
