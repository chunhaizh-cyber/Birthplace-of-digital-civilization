# 20260418 当前需等待/当前需收束/可继续推进前移为正式结构字段

## 本次目标

继续按“任务树/方法树表达结构，二次特征表达证据，原语只解释证据，任务管理只做编排”的口径推进，
把 `当前需等待 / 当前需收束 / 可继续推进` 从仅存在于治理二次特征证据中的判断项，
继续前移为：

- 正式结构字段
- 宿主任务治理稳定特征
- 结构优先 helper

## 已完成

1. 在 `任务管理任务模块.ixx` 中补入正式结构字段
   - `结构_任务管理方法需求位特征面`
   - `结构_任务管理上下文`

2. 在 `任务管理任务模块.impl.cpp` 中新增宿主治理特征
   - `任务管理_当前需等待`
   - `任务管理_当前需收束`
   - `任务管理_可继续推进`

3. 打通稳定写回与上下文读取
   - `私有_写回治理稳定字段(...)` 开始写回这三项
   - `读取任务管理上下文(...)` 开始正式读取这三项

4. 新增结构优先 helper
   - `私有_P0_当前需收束(...)`
   - `私有_P0_可继续推进(...)`
   - `私有_P0_当前需等待(...)` 也改成结构优先

5. 证据刷新改成“结构优先，局部推导兜底”
   - `私有_P1_刷新任务治理二次特征证据(...)` 先读 `上下文.当前需等待 / 当前需收束 / 可继续推进`
   - 仅在结构没有明确值时，才继续走局部推导

6. 结构透传链补齐
   - `私有_P0_刷新步骤与学习结构字段(...)`
   - `私有_命中方法需求位(...)`
   - `刷新任务管理方法需求位(...)`
   - `私有_P0_覆盖上下文字段(...)`
   - `私有_P0_覆盖兼容到上下文字段(...)`

## 当前效果

这三项现在不再只是“治理证据层里的布尔结论”，而是已经成为：

- 可落账的宿主治理字段
- 可读取的上下文字段
- 可透传的方法需求位字段
- 可被 helper 统一消费的结构化状态

这样后续 `步骤结果子链 / 学习触发 / 原语解释` 再用到它们时，
可以先读结构，再读证据，继续缩小对局部推导的依赖。

## 验证

已执行：

```powershell
msbuild D:\鱼巢\鱼巢.vcxproj /t:Build /p:Configuration=Debug /p:Platform=x64 /m
```

结果：

- 0 error
- 0 warning

## 未完成

- 还未做运行态回放验证
- `私有_P0_执行原语(...)` 仍然主要直接解释治理证据，下一步可继续把更多“局部推理”前推成结构字段或更稳定的二次特征证据
