# S8f D455 首次本能方法自身环境暖账复核方案 v0.1

日期：2026-06-09

状态：已暂停；二轮验证后按用户 2026-06-09 口径转出当前主动队列，后续由整体流程计划 P5 承接。

## 1. 触发依据

```text
S8e 已闭合：
    6150 缺报告重复提交样本已由约 0.44s-0.53s 降到约 0.045s-0.049s。
    缺报告快退仍保留失败动作动态、不写需求满足、不写安全值。

最新真实日志证据：
    日志=日志/鱼巢_run_20260609.低值g
    窗口起点行=29184
    6150 首次自身环境样本：
        总耗时 215188us
        确保本能方法 86421us
        首次同步方法虚拟存在 63525us
        二次同步方法虚拟存在 64265us
    6145 首次自身环境样本：
        总耗时 268373us
        确保本能方法 104200us
        首次同步方法虚拟存在 96509us
        二次同步方法虚拟存在 62803us
```

## 2. 目标

```text
第一目标：
    只读复核 首次暖账 是否来自 方法虚拟存在服务层 的重复幂等写入，而不是 6145 / 6150 的缺报告业务失败路径。

第二目标：
    若可证明同一方法首节点的虚拟存在同步在结构未变化时可跳过，制定并落实最小幂等快路径。

第三目标：
    若缺少方法结构版本、同步完成标记或可验证的不变条件，不直接改服务层，只记录阻塞点和所需契约。
```

## 3. 硬边界

```text
首次完整方法环境确保必须保留。
不得跳过方法虚拟存在首次创建。
不得让缺报告、材料不合格或等待状态写成完成 / 满足。
不得跳过失败动作动态、失败回执、派生缺口或任务状态提交。
不得把 6145 / 6150 的外设失败路径泛化成所有本能方法的成功快路径。
不得新增 mock、fixture、伪入口、伪动作动态或 `--*-check` 回归入口。
```

## 4. 允许修改

```text
方法虚拟存在服务类.cpp
方法虚拟存在服务类.h
本能方法类.cpp
本能方法类.h
自我动作实现.外设模块.ixx
计划/计划索引.md
计划/20260607_运行速度常驻路径优先优化计划_v0.1.md
计划/20260609_S8f_D455首次本能方法自身环境暖账复核方案_v0.1.md
实施记录/20260609_S8f_D455首次本能方法自身环境暖账复核_Codex断点清单.md

若必须修改其他文件，先补充本方案并说明原因。
```

## 5. 执行步骤

```text
1. 写 S8f 断点清单和实施前预测。
2. 只读复核 方法虚拟存在服务类::同步方法节点到虚拟存在 及其子阶段是否已有结构版本、同步完成特征或等值快路径。
3. 若日志不足，只补最小阶段日志；不得先改服务层同步语义。
4. 若能证明幂等同步可按方法首节点 / 结构版本跳过，做最小快路径并保留首次同步。
5. 构建 Debug x64，真实运行 self-check-log，按最新窗口核对 6145 / 6150 首次自身环境、动作动态和错误日志。
```

暂停口径：

```text
已完成到二轮：
    6145 / 6150 规格调用前已确认时跳过二次 同步方法节点到虚拟存在。
    构建和真实 self-check-log 均通过。
    缺报告失败快退、不写需求满足、不写安全值、任务状态动作动态边界均保持。

不继续执行：
    不再追 6145 / 6150 首次 同步方法节点到虚拟存在 暖账。
    不在 方法虚拟存在服务类 中新增 D455 专项同步阶段日志或结构签名快路径。
    不继续优化相机外设四个方法，等待整体流程计划 P5。
```

## 6. 验收证据

```text
必须提供：
    Debug x64 构建退出码。
    真实 self-check-log 退出码。
    最新日志窗口起点。
    6145 / 6150 外设本能方法自身环境/阶段耗时。
    任务工作项派发执行耗时 类型=执行 样本。
    任务状态动作动态计数。
    缺报告失败快退、不写需求满足、不写安全值证据。
    旧完整本能调用锚点、任务状态逻辑错误、[FTL] 负向锚点。

关闭条件：
    若首次暖账平均明显下降且边界证据保持，S8f 闭合。
    若无法证明服务层幂等同步契约，S8f 以阻塞登记闭合，不做代码快路径。
    当前按用户口径暂停，恢复条件为整体流程计划 P5 启动或真实日志重新证明该路径是当前主瓶颈。
```
