# 场景类逻辑纠偏计划 v0.1

日期：2026-06-20

状态：第一优先缺口已闭合；已停止本计划并返回因果类外部数据读取纠偏协调计划 P7。

## 0. 目标

补齐 `场景类` 对外只读功能函数，使调用方不再直接通过 `基础信息类::查找主键` 解析场景节点。

当前缺口：

```text
因果类 P7 回迁需要解析 动态主信息.来源输出场景 / 来源输入场景 中的场景引用。
现有 场景类 没有 按主键解析场景节点 公开函数。
```

## 1. 第一优先切片

```text
P1 场景类按主键解析场景节点：
    新增 场景类::按主键解析场景节点(const std::string&) const noexcept。
    只读基础信息主键并校验 场景节点主信息类。
    不创建、不删除、不修改场景节点。
```

允许修改：

```text
场景类.h
场景类.cpp
说明书/函数功能说明文档.md
实施记录/20260620_因果类外部数据读取纠偏_Codex断点清单.md
计划/计划索引.md
```

返回点：

```text
计划/20260620_因果类外部数据读取纠偏协调计划_v0.1.md
P7 因果类回迁
```

## 2. 跨类缺口转向规则

执行本计划时，如果发现当前切片必须依赖其它类新增或补齐功能函数，按以下规则处理：

```text
1. 后续任意类缺少当前切片所需功能函数时，不在当前调用方临时拼字段、解释其它类主信息或新增平行结构体。
2. 若对应类已有逻辑纠偏计划，则把当前缺口加入该计划，并放到第一优先位置。
3. 若对应类没有逻辑纠偏计划，则生成 `计划/YYYYMMDD_<类名>逻辑纠偏计划_v0.1.md`，并把当前缺口作为第一优先切片。
4. 新生成或被修改的逻辑纠偏计划必须包含本“跨类缺口转向规则”，保证后续继续发现缺口时仍按同一调度口径处理。
5. 更新 `计划/计划索引.md`：把新生成或新修改的计划临时提升为第一优先计划；原计划和原切片记录为返回点。
6. 只执行新计划的第一优先缺口切片，不顺带推进新计划后续候选项。
7. 第一优先缺口闭合后，立即停止该新计划，更新计划索引返回原计划和原切片。
8. 若第一优先缺口切片不能闭合，保留该计划为当前阻塞，不回到原计划继续补丁化绕过。
```

## 3. 验收

```powershell
python .\tools\生成函数功能说明文档.py
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

验收口径：

```text
1. 场景类提供只读按主键解析函数。
2. 函数文档包含新增函数。
3. Debug x64 构建通过。
4. P1 闭合后停止本计划，返回因果类外部数据读取纠偏协调计划 P7。
```

## 4. 执行结果

已改文件：

```text
场景类.h
场景类.cpp
说明书/函数功能说明文档.md
```

新增函数：

```text
场景类::按主键解析场景节点
```

验证结果：

```text
函数说明文档生成成功：源码文件 128，函数条目 6880。
Debug x64 构建退出码：0。
构建日志：日志/20260620_场景类P1场景节点解析_msbuild.log。
日志尾部：已成功生成；0 个警告；0 个错误。
```

返回关系：

```text
场景类第一优先已闭合，停止场景类计划。
返回 计划/20260620_因果类外部数据读取纠偏协调计划_v0.1.md P7。
```
