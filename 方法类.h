#pragma once

#include "方法主信息类.h"

enum class 枚举_本能方法ID : std::uint32_t;
struct 结构_本能方法元信息;

class 方法类 : public 链表模板<方法信息基类> {
public:
    using 基类 = 链表模板<方法信息基类>;
    using 节点类 = 基类::节点类;

public:
    static 节点类* 查找方法首节点_按动作句柄(
        节点类* 方法根节点,
        const 结构体_动作句柄& 动作句柄) noexcept;
    static const 节点类* 查找方法首节点_按动作句柄(
        const 节点类* 方法根节点,
        const 结构体_动作句柄& 动作句柄) noexcept;

    static 节点类* 查找方法首节点_按动作名(
        节点类* 方法根节点,
        const 词性节点类* 动作名) noexcept;
    static const 节点类* 查找方法首节点_按动作名(
        const 节点类* 方法根节点,
        const 词性节点类* 动作名) noexcept;

    static 节点类* 注册方法首节点(
        节点类* 方法根节点,
        const 词性节点类* 动作名,
        const 结构体_动作句柄& 动作句柄,
        枚举_方法作用域 作用域 = 枚举_方法作用域::未定义,
        const std::vector<std::string>& 影响度量签名 = {},
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const std::string& 调用点 = "方法类::注册方法首节点",
        const 词性节点类* 主结果特征类型 = nullptr);

    static 节点类* 注册需求位方法首节点(
        节点类* 方法根节点,
        const 词性节点类* 主结果特征类型,
        枚举_方法来源 来源 = 枚举_方法来源::未定义,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const std::string& 调用点 = "方法类::注册需求位方法首节点");

    static std::size_t 补齐全部方法首节点到根链(
        节点类* 方法根节点,
        存在节点类* 宿主存在 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const std::string& 调用点 = "方法类::补齐全部方法首节点到根链");

    static 节点类* 查找或创建_本能方法首节点(
        存在节点类* 宿主存在,
        枚举_本能方法ID 本能ID,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const std::string& 调用点 = "方法类::查找或创建_本能方法首节点");

    static bool 补齐本能方法首节点(
        节点类* 方法首节点,
        const 结构_本能方法元信息& 元信息,
        存在节点类* 宿主存在 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const std::string& 调用点 = "方法类::补齐本能方法首节点");

    static 存在节点类* 取或创建_方法虚拟存在(
        节点类* 方法首节点,
        存在节点类* 宿主存在 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const std::string& 调用点 = "方法类::取或创建_方法虚拟存在");

    static 场景节点类* 取或创建_方法条件场景(
        节点类* 方法首节点,
        存在节点类* 宿主存在 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const std::string& 调用点 = "方法类::取或创建_方法条件场景");

    static 场景节点类* 取或创建_方法结果场景(
        节点类* 方法首节点,
        存在节点类* 宿主存在 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const std::string& 调用点 = "方法类::取或创建_方法结果场景");

    static bool 记录方法虚拟存在特征(
        节点类* 方法首节点,
        const 词性节点类* 特征类型,
        const 特征值& 值,
        存在节点类* 宿主存在 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const std::string& 调用点 = "方法类::记录方法虚拟存在特征");

    static bool 初始化方法虚拟存在信息(
        节点类* 方法首节点,
        存在节点类* 宿主存在 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const std::string& 调用点 = "方法类::初始化方法虚拟存在信息");

    static I64 读取方法成熟度阶段标准特征(
        节点类* 方法首节点,
        I64 默认值 = 0,
        存在节点类* 宿主存在 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const std::string& 调用点 = "方法类::读取方法成熟度阶段标准特征");

    static I64 读取方法允许自动召回标准特征(
        节点类* 方法首节点,
        I64 默认值 = 0,
        存在节点类* 宿主存在 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const std::string& 调用点 = "方法类::读取方法允许自动召回标准特征");

    static I64 读取方法允许默认执行标准特征(
        节点类* 方法首节点,
        I64 默认值 = 0,
        存在节点类* 宿主存在 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const std::string& 调用点 = "方法类::读取方法允许默认执行标准特征");

    static bool 同步方法成熟度与权限标准特征(
        节点类* 方法首节点,
        I64 成熟度阶段,
        I64 允许自动召回 = -1,
        I64 允许默认执行 = -1,
        存在节点类* 宿主存在 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const std::string& 调用点 = "方法类::同步方法成熟度与权限标准特征");

    static bool 按注册入口对齐方法作用域(
        节点类* 方法首节点,
        枚举_方法作用域 注册作用域,
        const std::string& 调用点 = "方法类::按注册入口对齐方法作用域");

    static 节点类* 创建条件节点(
        节点类* 首节点,
        场景节点类* 条件场景模板 = nullptr,
        const std::vector<可解析引用<二次特征节点类>>& 条件判定索引 = {},
        std::string 条件模板主键 = {},
        时间戳 时间起 = 0,
        时间戳 时间止 = 0,
        const std::string& 调用点 = "方法类::创建条件节点");

    static 节点类* 创建结果节点(
        节点类* 父节点,
        场景节点类* 结果场景模板 = nullptr,
        const std::vector<可解析引用<二次特征节点类>>& 结果摘要索引 = {},
        const std::string& 调用点 = "方法类::创建结果节点");
};
