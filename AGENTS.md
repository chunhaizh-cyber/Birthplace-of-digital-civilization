# AGENTS.md

本文件是本项目给 Codex / 代码代理 / 协作代理使用的项目级工作说明。

## 1. 项目定位

鱼巢是一个以“自我内部循环”为核心的 C++ 工程。当前主线围绕：

```text
自我状态
-> 需求树生长
-> 任务发起
-> 任务筹办
-> 方法选择 / 执行
-> 结果回写
-> 需求结算
-> 下一轮状态判断
```

当前不得提前宣称：

```text
自我苏醒完成
初步成熟完成
```

这些判断必须以后续规范和回归剧本为准。

## 2. 开工前必须读取

本章的“必须读取”默认按第 9 章限载规则执行，不等于全文加载。除非用户要求全文审计、正在修改对应文件、或排查规则冲突，否则只读取目录、标题、当前主题命中段和当前主动队列。

每次执行计划前,必须先限载读取：

```text
docs/00_文档总索引.md
规范/禁止性规范20260611.md
规范/规范目录.md
计划/计划索引.md
```

每次制定或修改计划时,至少限载读取：

```text
规范/规范目录.md
计划/计划索引.md
```

每次修改代码前,还必须限载读取：

```text
docs/00_文档总索引.md
docs/工程图谱/00_图谱总索引.md
当前主题对应的 docs/工程图谱/*.md
```

如果计划涉及内部循环、需求树、任务管理、方法学习，还要按当前切片命中主题读取对应规范的相关章节：

```text
规范/自我内部循环实现规范20260512.md
规范/需求树生长机制规范20260512.md
规范/任务筹办与执行边界规范20260512.md
规范/任务筹办按方法能力查找到就绪因果链规范20260513.md
规范/任务筹办父方法与协作子方法链规范20260513.md
规范/方法动作动态硬规则规范20260512.md
```

如果涉及计划生成、远程 Codex 执行、断点续跑、上下文压缩或网络中断处理，还要限载读取：

```text
规范/远程执行通道与断点续跑规范20260524.md
```

上级规范优先于下级规范。若发现计划、代码、下级规范与上级规范冲突，必须暂停该步骤并等待确认。

## 3. 当前有效硬规则

```text
1. 规范优先级高于计划；计划优先级高于临时推断。
2. 线程不是动作来源；能生成方法动作动态的变化必须绑定明确方法。
3. 外部变化不能伪装成自我方法动作动态，只能记录为观察动态或外部事件动态。
4. 非正常逻辑错误必须弹错 / 报错并记录日志，不允许静默兜底。
5. 所有弹窗错误都按逻辑错误处理，必须追究根因并解决；不得通过降级日志、屏蔽弹窗或吞掉错误来完成验证。
6. 业务缺口才允许走回执、派生需求或学习需求。
7. 只读读取和显式确保必须区分；诊断和回归检查不得通过只读路径隐式创建结构。
8. I64_MAX 是异常哨兵或未设置值，不得进入真实服务结算。
9. 任务筹办 / 任务执行是任务管理生命周期普通函数，不作为本能方法注册、选择或动作动态来源。
10. 只有需求需要任务化；任务筹办和任务执行本身不应被建成子任务来解决鸡生蛋蛋生鸡问题。
11. 方法查找必须先填充候选集，再从候选集中选择；单候选可直接绑定，多候选必须按确定规则选择。
12. remote compact / stream disconnected / websocket / network error 只表示远程执行通道中断，不得记成项目实现、业务验证或计划逻辑失败。
13. 运行日志、预测日志、逻辑错误排查日志、调试日志、控制台 / UI / 调试器输出等所有输出不得承载正式机器逻辑或业务状态；必须通过 `预处理开关变量.h` 总开关和细分开关一键启停，关闭输出不得改变项目结构事实、错误检测或终止语义。
```

## 文档权力关系与工程流水线

```text
1. 规范 = 能否这样做，是最高权威规则层。
2. 详细设计 = 如何这样做，只解释实现方案，不覆盖规范。
3. 流程图 = 视觉化解释，不是权威来源。
4. 计划 = 本轮做哪一段，不改变系统口径。
5. 实施记录 = 实际做成了什么和跑出了什么证据，不产生新规则。
6. 工程图谱 = 概念、规范、代码、证据入口的短索引，不替代规范和计划。
7. 说明书 / README / 书稿映射 = 给人读的对外说明，不指导代码覆盖规范。
```

正式流转只允许沿以下链路推进：

```text
问题 / 新口径
-> 规范变更
-> 详细设计解释
-> 计划拆分
-> 代码实施
-> 实施记录
-> 回归验证
-> 更新索引
-> 完成 / 归档
```

反向覆盖禁止：

```text
实施记录直接改规则
流程图直接改规则
计划直接改规则
详细设计覆盖规范
旧说明书覆盖新规范
图谱替代规范或计划
```

## 规范使用规则

```text
1. 修改代码前，先根据任务目标查询 `docs/工程图谱/05_规则原子索引.md`。
2. 找到相关 RULE 后，必须回读来源规范。
3. 若规范编号或规范文件未进入 `规范/规范目录.md`，不得把它当作正式权威依据，只能作为参考或待确认材料。
4. 若规范状态为草案 / 待修订 / 历史参考，不得直接按其修改核心代码。
5. 若规范、计划、详细设计冲突，以规范目录中的上级规范和编号优先级为准。
6. 修改后必须说明遵守了哪些 RULE，是否新增或修改了规则原子，是否需要更新工程图谱，以及运行了哪些回归。
7. 规范治理检查使用 `python .\tools\check_specs.py`；第一版以告警为主，只有明确要求治理检查阻断时才使用 `--strict`。
8. 新增文档性治理、映射、验证、状态和追踪材料默认放入 `docs/`；现有权威目录物理迁移必须另开计划，先处理引用、脚本和本文件路径。
```

## 项目通用数据结构硬约束

权威细则在 `规范/禁止性规范20260611.md` 第 2 章；完整审计格式在 `docs/codex/实现验收审计模式.md`。本节只保留默认执行摘要。

```text
1. 系统内部逻辑必须通过项目通用数据结构表达和流转。
2. 设计和实现不仅要证明“闭环”，还要证明闭环由项目结构闭合。
3. 业务状态、存在关系、特征归属、方法关系、需求关系必须落到：
   存在 / 场景 / 特征 / 特征值 / I64 / VecU句柄 / VecI64 / VecIU64 / 指针句柄 / 方法 / 需求 / 基础信息节点。
4. 禁止用自由文本、字符串、日志、注释、临时 JSON、文本标签、自由字段冒充系统逻辑。
5. 文本只允许用于日志、调试、测试名、用户可读错误、语素入口名称种子和文档说明；语素注册后必须转为项目内部身份结构再参与判断。
6. 设计进入实现前必须给出项目结构映射表：
   业务概念 -> 项目承载结构 -> 值类型 -> 写入方 -> 读取方 -> 验证方式。
7. 验收“已实现 / 已支持 / 已完成 / 已接入 / 已闭环”声明时，必须给出六类证据：
   输入入口、核心处理、项目结构承载证据、输出 / 状态变化、调用路径、测试 / 验证。
8. 没有项目结构闭环时，必须降级为：
   文本替代实现不合格 / 仅定义壳 / 孤立实现未接入项目结构闭环 / 部分实现。
```

触发以下场景时必须读取完整审计细则：用户要求验收、审计、证明已实现、检查是否真实现、按实现验收审计模式验收，或回答中需要写“已实现 / 已完成 / 已支持 / 已接入 / 已闭环 / 可用 / 已修复 / 已验证”。

## 4. 术语口径

保持全项目术语一致：

```text
二次特征：
    对原始状态、动态、任务、需求等事实的派生判断或聚合特征。

动作动态：
    方法运行造成事实变化的记录。动作动态不是普通日志，也不以成功 / 失败为根概念。

因果账本：
    由动作动态及其来源动作动态、满足证据动态等引用链构成，不另造一套因果记录结构。

需求：
    对“目标宿主上的目标特征达到目标状态”的原子目标或管理目标。

任务：
    需求的过程承接壳。任务状态由任务管理生命周期推进。

方法：
    可被查找、选择、调用、记录的动作能力。方法头用于候选召回；条件节点用于参数和前置条件匹配；结果能力用于目标特征命中。

反馈：
    任务或方法运行后返回给上层治理链的运行事实、回执、缺口、输出场景或结算证据。

记忆：
    方法虚拟存在、任务虚拟存在、自我内部世界等结构中的持久或半持久运行痕迹。

边界：
    各线程、各领域和各类写入权的分工；不要用线程裸写代替领域动作。
```

## 5. 模块边界

### 自我线程

负责：

```text
需求树生长
需求估权
任务发起意图
调度意图
需求回写
```

不应直接裸写任务状态。

### 任务管理线程

负责：

```text
任务创建承接
任务虚拟存在维护
任务状态推进
任务筹办
任务执行
任务回执同步
任务状态动作动态相关提交链
```

任务筹办仍是单步循环推进：

```text
检查任务状态
-> 根据任务目标填充候选方法集
-> 从候选集中选择当前方法
-> 根据方法状态 / 条件 / 参数形成可就绪或缺口回执
```

### 方法层

负责：

```text
方法头
方法虚拟存在
方法状态
条件节点
结果节点
条件结果对
动作句柄 / 方法动作
```

方法头只做大范围召回；具体执行依据是条件节点、结果节点、条件结果对和方法虚拟存在。

### 世界层

负责真实世界树事实：

```text
存在
特征
状态
动态
输出结果场景
```

输出结果场景只承载真实事实，不承载任务状态回执。

## 6. 推荐代理角色

| 角色 | 职责 | 适用任务 |
| --- | --- | --- |
| 规范代理 | 读取规范目录，确认上级规范；发现冲突则暂停；维护术语、编号、层级和下级一致性。 | 修订规范、检查冲突、统一术语、生成阶段定义。 |
| 计划代理 | 读取计划索引；清理过期计划；写清阻塞点、下一步剧本；完成项移入完成目录。 | 制定 / 修订计划、清理旧计划、拆分子计划。 |
| 实现代理 | 按计划和详细设计做最小代码修改；新增函数写函数级注释；不引入规范冲突结构。 | 实现本能函数、修正任务筹办 / 需求树生长、补日志和错误报告。 |
| 回归代理 | 构建 Debug x64；按顺序验证；不得并发运行多个 `鱼巢.exe`；记录失败命令、退出码和关键输出。 | 验证构建、真实运行验证、定位失败、更新验收状态。 |
| 日志排错代理 | 按问题补日志；弹窗错误处追加日志；用日志回答状态问题，不用猜测替代证据。 | 确认需求来源、方法查找、候选集填充、任务推进、弹窗或崩溃。 |
| 文档代理 | 维护说明书、详细设计、规范、计划口径一致；摘要说明边界，不夸大阶段完成度。 | 统稿、术语一致性检查、详细设计落地、说明书更新。 |

## 7. 构建与运行验证

构建：

```powershell
msbuild .\鱼巢.vcxproj /p:Configuration=Debug /p:Platform=x64 /p:LinkIncremental=false /m
```

运行规则：

```text
1.采用预处理方式管理断点日志验证和空指针检测，不同逻辑定义不同预处理开关，所有开关统一到一个头文件管理。
2.采用预测结果和记录日志的方式替代模拟测试、夹具测试、伪造样本、伪造入口或伪造动作动态。
3. 构建和 exe 运行不要并发执行。
4.不要假设我我清楚自己想要什么。动机或目标不清晰时停下来讨论。
5.遇到问题追根因，不打补丁。
6.输出说重点，砍掉一切不改变决策的信息。
7.目标清晰但路径不是最短，直接告诉我并建议更好的办法。
```

## 7.1 计划连续执行语义

```text
1. 用户说“执行计划”时，默认授权连续执行该目标计划的全部阶段。
2. 不得因完成 P0、完成一个闭合切片、完成一次提交、完成一次构建、静态检查通过或阶段标题切换而停止。
3. 每个闭合切片完成后，必须更新断点 / 计划证据，按 Git 规则提交本切片相关文件，然后重新读取当前计划停止点并自动进入下一可执行切片。
4. 计划中“第一轮只执行 P0”“本轮只执行某阶段”等表述只视为切片边界，不视为停止条件；只有明确写“硬停止 / 必须等待用户确认 / 不得自动继续”时才停。
5. 连续执行不跳过开工必读、代码修改前读取、函数说明文档扫描、计划索引锁、构建运行锁、断点清单、真实验证和提交前差异检查。
6. 只有以下情况允许停止：规范冲突需要确认；真实硬件 / SQL / 自然样本 / 凭证不可得；锁被其他新鲜持有者占用且用户未要求等待；继续会伪造业务证据；继续会越过计划明确硬停止；上下文硬闸已触发且已写最小断点；或出现当前轮无法解决的构建 / 运行 / 契约阻塞。
```

## 8. 代码编辑规则

```text
1. 优先使用 rg 搜索。
2. 手工编辑使用 apply_patch。
3. 不使用 git reset --hard 或 checkout 回滚用户改动。
4. 不把用户未要求的重构混入当前步骤。
5. 新增结构前先确认是否能用已有特征表达。
6. 不新增“说明性摘要字段”误导阶段判断。
7. 尽量保持中文术语与既有代码一致。
8. 新建函数前必须先扫描 `说明书/函数功能说明文档.md`；若已有同名或同功能函数，按文档注明位置直接调用或复用，不得重复新建；确认没有后才允许新建。
9. 新建函数时必须在代码中用简短注释说明函数功能；新增、删除或改名函数后必须重新运行 `python .\tools\生成函数功能说明文档.py` 更新文档。
10. 新建函数放置规则：`全局共享函数类.ixx` 只放不依赖项目自定义数据类型头文件的通用函数，且不得包含语素、基础信息、主信息类等自定义类型头；涉及自定义数据类型的共享函数放入对应功能类或对应功能头文件，例如语素入口同一性判断放在 `语素类.h`。
11. 每个代码文件头部必须保留“文件头部规则注释模块”；相关提示、禁忌、时序、数据加载边界和易忘规则写入该模块。
12. 修改代码文件前必须先读取该文件的头部规则注释模块；若本次触碰的代码文件缺少该模块，先补最小模块再改业务代码。该模块只供人读，不得作为机器逻辑、业务状态或验收证据承载。
13. 更新既有函数前，先判断改动规模：小改且调用面清晰时可直接最小修改；改动较大、调用面广、行为风险高或需要重写主体逻辑时，优先按“原函数名 + 版本号”新建并行函数，例如 `原函数名_v2`，保持旧函数可回退；新版本经构建、真实路径验证和用户 / 计划确认后，再用新函数直接替换旧函数及调用点，并按第 8、9 条更新函数说明文档。版本函数过渡期不得注册为新的业务能力或绕过原有权限、状态、动态和验证链。
```

## 9. 上下文与工具输出控制

```text
1. 先判定任务类型：问答 / 计划 / 代码 / 验收 / 排错 / 恢复。只读取该类型的必要规则层，不把所有入口一次性展开。
2. 必读文件采用三段式读取：先读目录或标题；再用 `rg` 命中当前主题；最后按行号小段读取。只有正在修改该文件、排查规则冲突或用户要求全文审计时才全文读取。
3. 计划只写当前必要摘要：目标 / 依据 / 下一步 / 验证证据；执行时引用计划编号和关键结论。
4. 搜索和读取先限定范围；非排查需要时避开 x64、Debug、.git、日志 / logs、生成物。
5. 大文件按标题、函数或行号小段读取；单文件超过 20KB 时默认不整文件读取。
6. 计划索引默认限载读取采用两段式：先读第 0 章维护规则摘要并在 `## 0.1` 前停止；再从 `## 1. 当前主动队列` 读取到 `### 1.1` 前停止。`0.1/0.2/0.3` 只在遇到困难、阻塞回执或类边界缺口时读取；第 2 章只在允许切换计划时读取。
7. 规范目录默认限载读取：先读一级规范表和当前主题相关条目；只有涉及下级规范冲突、编号调整、规范修订或规则原子回读时再展开对应下级规范。
8. 工程图谱默认限载读取：先读 `docs/工程图谱/00_图谱总索引.md` 定位主题；只读当前主题图谱和命中的规则原子行；规则原子来源规范按命中章节回读，不全文展开。
9. AGENTS.md 默认限载读取：恢复执行时优先读取开工必读、硬规则、项目通用数据结构硬约束、构建运行、上下文控制、Git 规则和当前未完成主线提醒；全文审计只在修改 AGENTS.md 或排查代理规则冲突时进行。
10. `说明书/函数功能说明文档.md` 默认只用 `rg` 查同名、近名和功能关键词；命中后只读对应函数段，不全文加载。
11. 单次工具输出超过 200 行、或文件超过 20KB 时，先落入日志 / 实施记录 / 临时分析文件，再用 `Select-String`、行号小段或统计结果回传关键证据。
    `MEMORY.md` 搜索默认不带 `-Context`，先限制 `Select-Object -First 20`；确需上下文时按命中行号二次小段读取。
12. Git 默认用 `git status --short --untracked-files=no`、`git diff --name-only`、`git diff --stat -- <path>`；提交 / 同步 / 明确需要时再展开完整输出。
13. 构建和运行只回传退出码、关键错误、预测偏差和关键日志证据；长输出落文件后检索关键行。
14. 长任务开始、进入构建 / 真实运行 / 大范围修改前，先更新仓库内上下文镜像断点；优先使用 `实施记录/YYYYMMDD_主题_Codex断点清单.md`，至少记录当前切片、依据文件、已改文件、已运行命令、关键退出码、关键日志证据、当前判断、下一步和禁止宣称项。
15. 长任务完成后建议开新线程，用短交接摘要承接：已改文件、验证命令、阻塞点、下一步。
16. 远程 Codex 任务一次只执行一个闭合切片，必须写清允许修改文件、禁止修改范围、验收证据和仓库内断点清单；该限制只约束外发远程子任务，不限制本地“执行计划”在同一目标计划内逐切片连续推进。
17. 远程执行中断时保持项目最近已知状态和验收状态不变，从最近断点清单重新开小任务继续。最近断点清单默认只读 `## 0. 恢复摘要`；没有该章节时只读标题、当前切片、下一步、禁止宣称和最后一段验证结果。
18. remote compact / stream disconnected 后用户输入“继续”时，不尝试恢复隐藏会话上下文；按 AGENTS.md 限载规则、规范目录限载规则、计划索引当前主动队列、相关规范、最近断点清单、`git status --short --untracked-files=no` 的顺序恢复，再选择一个最小闭合切片继续。
19. 断点清单只记录真实完成和真实证据；压缩失败前未写入的内容不得凭记忆补写成已完成、已验证或业务通过。
20. 发现第一个独立问题后，先写入当前断点清单或临时分析文件：问题锚点、证据行、已排除路径、当前判断、下一步验证；不得继续扩大检索后再补记。
21. 如果继续定位时发现第二个独立问题，立即把第一个问题收束为独立切片或待办项，明确保留/暂停状态，再切换到第二问题；不要在同一上下文里同时推进两个未收束问题。
22. 切换切片前必须清理检索信息：只保留当前切片必需的文件、函数、日志锚点和验证命令；已排除的大段搜索结果不带入下一轮，只在断点清单中留摘要。
23. 上下文硬闸：单轮新增输出超过 20KB、或日志显示 `last_token_usage.input_tokens` 接近 210K 时，先写最小断点并建议开新线程，不继续依赖自动压缩。
```

## 10. Git 规则

```text
1. 每个闭合切片完成后自动提交本次 Codex 修改的文件。
2. 自动提交前必须检查状态和差异；只提交本次切片相关文件，不混入用户已有未提交改动。
3. 用户要求“同步到云端”时，先检查状态、远端和分支，再提交和推送。
4. 当前常用远端为 birthplace，当前主分支为 main。
5. 提交前尽量说明已跑过的关键构建或运行验证；文档 / 计划小改若未构建，需明确说明未运行构建。
```

## 11. 当前未完成主线提醒

计划索引已经是当前权威状态，不需要在进入下一步前先更新计划索引。

```text
当前顺序：子任务完成后父任务权重和活动集变化 -> 父任务重筹办 -> 学习需求正式入树四类剧本 -> 多候选方法确定性选择。
学习需求四类：无确定候选方法；候选方法不可用；明确缺执行入口；明确缺条件结果对。
边界：差距子需求退出活动集旧回归只作历史事实；当前验证以计划索引的新日志验证法为准。
价值结算：结算叶子任务价值是任务完成后的任务管理后处理方法，不作为安全值 / 服务值根任务的普通筹办候选或当前执行方法。它只能在叶子任务完成后，由任务价值反馈链携带实际改变结果状态、完成度和任务自身权重调用，并建立任务完成 -> 安全值 / 服务值账务变化的因果关系。D0 / D1 是因果解释证据，缺 D0 / D1 不再单独阻断入账。
参数 / 条件补齐需求不归入学习需求；`I64_MAX` 不得进入真实服务结算。
任务筹办、任务执行不再是本能方法；不要恢复相关本能方法枚举或规格确认入口。
不要把“需求树真实生长已通过”误读为“内部循环全部完成”。
```
