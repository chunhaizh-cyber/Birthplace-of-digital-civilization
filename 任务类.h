#pragma once

#include "任务主信息类.h"

class 任务类 : public 链表模板<任务主信息类> {
public:
    using 基类 = 链表模板<任务主信息类>;
    using 节点类 = typename 基类::节点类;

public:
    static 存在节点类* 取或创建_任务虚拟存在(
        节点类* 任务头结点,
        场景节点类* 承载场景 = nullptr,
        const std::string& 调用点 = "任务类::取或创建_任务虚拟存在");

    static bool 记录任务虚拟存在特征(
        节点类* 任务头结点,
        const 词性节点类* 特征类型,
        const 特征值& 值,
        场景节点类* 承载场景 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const std::string& 调用点 = "任务类::记录任务虚拟存在特征");

    static bool 初始化任务虚拟存在信息(
        节点类* 任务头结点,
        场景节点类* 承载场景 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const std::string& 调用点 = "任务类::初始化任务虚拟存在信息");

    static bool 同步任务特征使用统计(
        节点类* 任务头结点,
        const 结构_特征使用统计& 统计,
        枚举_特征生命周期 生命周期 = 枚举_特征生命周期::候选,
        场景节点类* 承载场景 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const std::string& 调用点 = "任务类::同步任务特征使用统计");

    I64 读取任务特征账本生命周期标准特征(
        节点类* 任务头结点,
        I64 默认值 = 0,
        场景节点类* 承载场景 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const std::string& 调用点 = "任务类::读取任务特征账本生命周期标准特征");


    节点类* 创建任务头结点(
        需求节点类* 来源需求,
        节点类* 上级任务节点 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const std::string& 调用点 = "任务类::创建任务头结点");

    // 兼容占位接口：旧步骤/结果任务树调用方仍会引用，后续随新需求树执行模型删除。
    节点类* 创建任务步骤节点(
        节点类* 所属任务头结点,
        节点类* 上级任务节点 = nullptr,
        方法节点类* 当前方法首节点 = nullptr,
        状态节点类* 步骤目标状态 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const std::string& 调用点 = "任务类::创建任务步骤节点");

    节点类* 创建任务结果节点(
        节点类* 所属任务头结点,
        节点类* 来源步骤节点,
        方法节点类* 当前方法首节点 = nullptr,
        枚举_任务结果角色 结果角色 = 枚举_任务结果角色::实际结果,
        状态节点类* 结果状态 = nullptr,
        时间戳 now = 结构体_时间戳::当前_微秒(),
        const std::string& 调用点 = "任务类::创建任务结果节点");


};
